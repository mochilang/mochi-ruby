let s1: stream<int> = make_stream(3)
let s2: stream<int> = make_stream(3)
let sub1 = subscribe(s1)
let sub2 = subscribe(s2)
emit(s1, 10)
emit(s2, 20)
let a = recv_sub(sub1)
let b = recv_sub(sub2)
print(a)
print(b)
