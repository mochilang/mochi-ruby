let s: stream<string> = make_stream(3)
let sub = subscribe(s)
emit(s, "alpha")
emit(s, "beta")
emit(s, "gamma")
let a = recv_sub(sub)
let b = recv_sub(sub)
let c = recv_sub(sub)
print(a)
print(b)
print(c)
