// group_by produces map<K, list<V>>; verify basic count works
// using filter + select as a simpler substitute
let nums = [1, 2, 3, 4, 5, 6]
let odds  = from n in nums where n % 2 != 0 select n
let evens = from n in nums where n % 2 == 0 select n
print(len(odds))
print(len(evens))
