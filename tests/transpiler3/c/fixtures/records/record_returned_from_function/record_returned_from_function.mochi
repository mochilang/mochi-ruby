type Pt {
  x: int
  y: int
}

fun origin(): Pt {
  return Pt { x: 0, y: 0 }
}

fun shift(p: Pt, dx: int, dy: int): Pt {
  return Pt { x: p.x + dx, y: p.y + dy }
}

let o = origin()
let s = shift(o, 3, 4)
print(s.x)
print(s.y)
