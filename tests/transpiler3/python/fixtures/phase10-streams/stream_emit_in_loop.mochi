let s: stream<int> = make_stream(8)
let sub = subscribe(s)
for i in 0..5 {
  emit(s, i * 10)
}
let a = recv_sub(sub)
let b = recv_sub(sub)
let c = recv_sub(sub)
let d = recv_sub(sub)
let e = recv_sub(sub)
print(a)
print(b)
print(c)
print(d)
print(e)
