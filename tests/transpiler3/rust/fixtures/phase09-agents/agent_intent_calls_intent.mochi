agent Counter {
    var count: int = 0
    intent bump() { count = count + 1 }
    intent bump_twice() {
        count = count + 1
        count = count + 1
    }
    intent value(): int { return count }
}
let c = Counter { count: 0 }
c.bump()
c.bump_twice()
print(c.value())
