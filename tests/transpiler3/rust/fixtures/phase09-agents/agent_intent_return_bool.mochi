agent Range {
    var lo: int = 0
    var hi: int = 10
    intent contains(n: int): bool {
        return (n >= lo) && (n <= hi)
    }
}
let r = Range { lo: 0, hi: 10 }
print(r.contains(5))
print(r.contains(10))
print(r.contains(11))
print(r.contains(-1))
