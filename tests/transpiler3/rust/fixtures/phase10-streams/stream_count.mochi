let s: stream<int> = make_stream(10)
let sub = subscribe(s)
for i in 1..6 {
    emit(s, i)
}
let a = recv_sub(sub)
let b = recv_sub(sub)
let c = recv_sub(sub)
let d = recv_sub(sub)
let e = recv_sub(sub)
print(a + b + c + d + e)
