// Generated by Mochi Zig transpiler on 2025-07-21 10:57 +0000
const std = @import("std");
const Entry = struct {
    n: i64,
    l: []u8,
    b: bool,
};
pub fn main() void {
    const nums = [_]i64{1, 2};
    const letters = [_][]u8{"A", "B"};
    const bools = ;
    const combos: []Entry = blk: {
        const arr = std.ArrayList(Entry).init(std.heap.page_allocator);
        for (nums) |n| {
            for (letters) |l| {
                for (bools) |b| {
                    arr.append(.{.n = n, .l = l, .b = b});
                }
            }
        }
        const tmp = arr.toOwnedSlice();
        break :blk tmp;
    };
    try std.io.getStdOut().writer().print("{s}\n", .{"--- Cross Join of three lists ---"});
    for (combos) |c| {
        try std.io.getStdOut().writer().print("{any} {any} {any}\n", .{c.n, c.l, c.b});
    }
}
