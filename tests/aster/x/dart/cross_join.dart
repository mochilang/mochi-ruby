// Generated by Mochi transpiler
class Customer {
  final int id;
  final String name;
  const Customer({required this.id, required this.name});
}
class Order {
  final int id;
  final int customerId;
  final int total;
  const Order({required this.id, required this.customerId, required this.total});
}
class Result {
  final int orderId;
  final int orderCustomerId;
  final String pairedCustomerName;
  final int orderTotal;
  const Result({required this.orderId, required this.orderCustomerId, required this.pairedCustomerName, required this.orderTotal});
}
void main() {
  final List<Customer> customers = [Customer(id: 1, name: "Alice"), Customer(id: 2, name: "Bob"), Customer(id: 3, name: "Charlie")];
  final List<Order> orders = [Order(id: 100, customerId: 1, total: 250), Order(id: 101, customerId: 2, total: 125), Order(id: 102, customerId: 1, total: 300)];
  final List<Result> result = [for (var o in orders) for (var c in customers) Result(orderId: o.id, orderCustomerId: o.customerId, pairedCustomerName: c.name, orderTotal: o.total)];
  print("--- Cross Join: All order-customer pairs ---");
  for (var entry in result) {
    print(["Order", entry.orderId, "(customerId:", entry.orderCustomerId, ", total: \$", entry.orderTotal, ") paired with", entry.pairedCustomerName].join(" "));
  }
}
