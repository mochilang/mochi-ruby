// Generated by Mochi transpiler
class Customer {
  final int id;
  final String name;
  const Customer({required this.id, required this.name});
}
class Order {
  final int id;
  final int customerId;
  const Order({required this.id, required this.customerId});
}
class Item {
  final int orderId;
  final String sku;
  const Item({required this.orderId, required this.sku});
}
class Result {
  final String name;
  final String sku;
  const Result({required this.name, required this.sku});
}
void main() {
  final List<Customer> customers = [Customer(id: 1, name: "Alice"), Customer(id: 2, name: "Bob")];
  final List<Order> orders = [Order(id: 100, customerId: 1), Order(id: 101, customerId: 2)];
  final List<Item> items = [Item(orderId: 100, sku: "a"), Item(orderId: 101, sku: "b")];
  final List<Result> result = [for (var o in orders) for (var c in customers) for (var i in items) if (o.customerIdequality_operator && o.idequality_operator) Result(name: c.name, sku: i.sku)];
  print("--- Multi Join ---");
  for (var r in result) {
    print([r.name, "bought item", r.sku].join(" "));
  }
}
