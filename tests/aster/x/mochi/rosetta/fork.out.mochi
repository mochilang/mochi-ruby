var nextPID: int = 1
fun run(hasArg: bool): void {
  let pid: int = nextPID
  nextPID = (nextPID + 1)
  print(("PID: " + str(pid)))
  if !hasArg {
    print("Done.")
  }
  let childPID: int = nextPID
  print(("Child's PID: " + str(childPID)))
  run(false)
}
run(true)
