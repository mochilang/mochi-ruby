;; Generated by Mochi 0.10.35 on 2025-07-22 16:30 +0700

#lang racket/base

(require racket/list racket/string json)

(define people
  (list
    (hash "name" "Alice" "age" 30 "email" "alice@example.com")
    (hash "name" "Bob" "age" 15 "email" "bob@example.com")
    (hash "name" "Charlie" "age" 20 "email" "charlie@example.com")))

(define adults
  (for/list
    ((p people) #:when
      (>=
        (hash-ref p "age") 18))
    (hash "name"
      (hash-ref p "name") "email"
      (hash-ref p "email"))))

(for
  ((a adults))
  (displayln
    (string-join
      (map
        (lambda
          (x)
          (format "~a" x))
        (list
          (hash-ref a "name")
          (hash-ref a "email"))) " ")))
