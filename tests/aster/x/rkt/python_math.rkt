;; Generated by Mochi 0.10.36 on 2025-07-22 16:50 +0700

#lang racket/base

(require racket/list racket/string racket/math json)

(define math_pi pi)

(define math_e
  (exp 1))

(define
  (math_sqrt x)
  (sqrt x))

(define
  (math_pow x y)
  (expt x y))

(define
  (math_sin x)
  (sin x))

(define
  (math_log x)
  (log x))

(define r 3.0)

(define area
  (* math_pi
    (math_pow r 2.0)))

(define root
  (math_sqrt 49.0))

(define sin45
  (math_sin
    (/ math_pi 4.0)))

(define log_e
  (math_log math_e))

(displayln
  (string-join
    (map
      (lambda
        (x)
        (format "~a" x))
      (list "Circle area with r =" r "=>" area)) " "))

(displayln
  (string-join
    (map
      (lambda
        (x)
        (format "~a" x))
      (list "Square root of 49:" root)) " "))

(displayln
  (string-join
    (map
      (lambda
        (x)
        (format "~a" x))
      (list "sin(π/4):" sin45)) " "))

(displayln
  (string-join
    (map
      (lambda
        (x)
        (format "~a" x))
      (list "log(e):" log_e)) " "))
