;; Generated by Mochi 0.10.36 on 2025-07-22 16:50 +0700

#lang racket/base

(require racket/list racket/string racket/math json)

(define
  (strings_TrimSpace s)
  (string-trim s))

(define
  (strings_ToUpper s)
  (string-upcase s))

(define math_pi pi)

(define math_e
  (exp 1))

(define
  (math_sqrt x)
  (sqrt x))

(define
  (math_pow x y)
  (expt x y))

(define
  (math_sin x)
  (sin x))

(define
  (math_log x)
  (log x))

(define rawName "   alice  ")

(define radius 3.0)

(define name
  (strings_ToUpper
    (strings_TrimSpace rawName)))

(define area
  (* math_pi
    (math_pow radius 2.0)))

(displayln
  (string-join
    (map
      (lambda
        (x)
        (format "~a" x))
      (list "Hello"
        (string-append name "!"))) " "))

(displayln
  (string-join
    (map
      (lambda
        (x)
        (format "~a" x))
      (list "The area of a circle with radius" radius "is" area)) " "))
