# Code generated by Mochi transpiler.
# Version 0.10.36, generated on 2025-07-22 17:46 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
import dataclasses
import json
@dataclass
class People:
    name: str
    city: str
people = [People("Alice", "Paris"), People("Bob", "Hanoi"), People("Charlie", "Paris"), People("Diana", "Hanoi"), People("Eve", "Paris"), People("Frank", "Hanoi"), People("George", "Paris")]
@dataclass
class BigGroup:
    key: str
    items: list
_big_groups = {}
for p in people:
    _g = _big_groups.setdefault(p.city, BigGroup(p.city, []))
    _g.items.append(p)
@dataclass
class Big:
    city: str
    num: int
big = [Big(g.key, len(g.items)) for g in _big_groups.values() if len(g.items) >= 4]
print(json.dumps([dataclasses.asdict(_x) for _x in big], indent=2))
