# Code generated by Mochi transpiler.
# Version 0.10.36, generated on 2025-07-22 17:46 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
@dataclass
class Customer:
    id: int
    name: str
customers = [Customer(1, "Alice"), Customer(2, "Bob")]
@dataclass
class Order:
    id: int
    customerId: int
orders = [Order(100, 1), Order(101, 2)]
@dataclass
class Item:
    orderId: int
    sku: str
items = [Item(100, "a"), Item(101, "b")]
@dataclass
class Result:
    name: str
    sku: str
result = [Result(c.name, i.sku) for o in orders for c in customers for i in items if o.customerId == c.id and o.id == i.orderId]
print("--- Multi Join ---")
for r in result:
    print(r.name, "bought item", r.sku)
