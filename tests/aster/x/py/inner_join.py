# Code generated by Mochi transpiler.
# Version 0.10.35, generated on 2025-07-22 13:13 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
@dataclass
class Customer:
    id: int
    name: str
customers = [Customer(1, "Alice"), Customer(2, "Bob"), Customer(3, "Charlie")]
@dataclass
class Order:
    id: int
    customerId: int
    total: int
orders = [Order(100, 1, 250), Order(101, 2, 125), Order(102, 1, 300), Order(103, 4, 80)]
@dataclass
class Result:
    orderId: int
    customerName: str
    total: int
result = [Result(o.id, c.name, o.total) for o in orders for c in customers if o.customerId == c.id]
print("--- Orders with customer info ---")
for entry in result:
    print("Order", entry.orderId, "by", entry.customerName, "- $", entry.total)
