#!/usr/bin/env escript
-module(main).

-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1]).

% Generated by Mochi transpiler v0.10.39 (d0bf63391b) on 2025-07-25 01:49 +0700
mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->         case os:getenv("MOCHI_NOW_SEED") of
            false ->             erlang:system_time(nanosecond)
;
            S ->             case catch list_to_integer(S) of
                {'EXIT', _} ->                 erlang:system_time(nanosecond)
;
                Seed ->                 erlang:put(now_seed, Seed),
                mochi_now()

            end

        end
;
        Seed ->         Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
        erlang:put(now_seed, Seed2),
        Seed2

    end
.

main(_) ->
    Start = mochi_now(),
    N = 1000,
    S = 0,
    Fun = fun Fun_loop(List, N, S) ->     case List of
        [] ->         {N, S}
;
        [I|Fun_loop_rest] ->         S_2 = (S + I),
        Fun_loop(Fun_loop_rest, N, S_2)

    end
 end,
    {N, S_2} = Fun(lists:seq(1, (1000) - 1), N, S),
    End = mochi_now(),
    DurationUs = (End - Start) div 1000,
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": 0,~n  \"name\": \"~s\"~n}
", [DurationUs, "simple"])
.
