#!/usr/bin/env escript
-module(main).

-export([main/1]).

% Generated by Mochi transpiler v0.10.33 (9f2177b9e) on 2025-07-21 18:06 +0700
main(_) ->
    Nums = [1, 2],
    Letters = ["A", "B"],
    Bools = [true, false],
    Combos = [#{"n" => N, "l" => L, "b" => B} || N <- Nums, L <- Letters, B <- Bools],
    io:format("~s~n", ["--- Cross Join of three lists ---"]),
    lists:foreach(fun (C) ->     io:format("~p ~p ~p~n", [maps:get("n", C), maps:get("l", C), maps:get("b", C)])
 end, Combos)
.
