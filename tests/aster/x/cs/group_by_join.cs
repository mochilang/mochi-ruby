// Generated by Mochi 0.10.33 on 2025-07-21 18:06 +0700
using System;
using System.Collections.Generic;
using System.Linq;
struct Customer {
    public int id;
    public string name;
    public override string ToString() => $"{{id: {id}, name: {name}}}";
}
struct Order {
    public int id;
    public int customerId;
    public override string ToString() => $"{{id: {id}, customerId: {customerId}}}";
}
struct GGroup {
    public string key;
    public Order[] items;
    public override string ToString() => $"{{key: {key}, items: {items}}}";
}
struct GResult {
    public string name;
    public int count;
    public override string ToString() => $"{{name: {name}, count: {count}}}";
}
class Program {
    static Customer[] customers = new Customer[]{new Customer{id = 1, name = "Alice"}, new Customer{id = 2, name = "Bob"}};
    static Order[] orders = new Order[]{new Order{id = 100, customerId = 1}, new Order{id = 101, customerId = 1}, new Order{id = 102, customerId = 2}};
    static GResult[] stats = (from o in orders ccustomerso.customerIdc.id group o by c.name into gTmp let g = new GGroup{key = gTmp.Key, items = gTmp.ToArray()} select new GResult{name = g.key, count = g.items.Length}).ToArray();
    static void Main() {
        Console.WriteLine("--- Orders per customer ---");
        foreach (var s in stats) {
            Console.WriteLine(string.Join(" ", new object[]{s.name, "orders:", s.count}).TrimEnd());
        }
    }
}
