// Generated by Mochi v0.10.35 on 2025-07-22 14:07:06 GMT+7
export interface Result { id: number; name: string }
export interface Customer { id: number; name: string }
export interface Order { id: number; customerId: number }
export interface Item { orderId: number; sku: string }
const customers: Customer[] = [{id: 1, name: "Alice"}, {id: 2, name: "Bob"}];
const orders: Order[] = [{id: 100, customerId: 1}, {id: 101, customerId: 2}];
const items: Item[] = [{orderId: 100, sku: "a"}];
const result: Result[] = (() => {
    let _items = orders.map(v => [v]);
    {
        const _joined = [];
        const _arr = customers;
        for (const _left of _items) {
            const [o] = _left;
            let _m = false;
            for (let _ri = 0; _ri < _arr.length; ++_ri) {
                const c = _arr[_ri];
                if ((!((o["customerId"] == c["id"]))))
                    continue
                _m = true;
                _joined.push([..._left, c]);
            }
        }
        _items = _joined;
    }
    {
        const _joined = [];
        const _arr = items;
        for (const _left of _items) {
            const [o, c] = _left;
            let _m = false;
            for (let _ri = 0; _ri < _arr.length; ++_ri) {
                const i = _arr[_ri];
                if ((!((o["id"] == i["orderId"]))))
                    continue
                _m = true;
                _joined.push([..._left, i]);
            }
            if ((!_m))
                _joined.push([..._left, null]);
        }
        _items = _joined;
    }
    let _rows = _items;
    const result: Result[] = [];
    for (const r of _rows) {
        const [o, c, i] = r;
        result.push({orderId: o["id"], name: c["name"], item: i});
    }
    return result;
})();
console.log(String("--- Left Join Multi ---"));
for (const r of result) {
    console.log((String(r["orderId"]) + " " + String(r["name"]) + " " + String(r["item"])).trim());
}
