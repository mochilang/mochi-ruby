// Generated by Mochi v0.10.35 on 2025-07-22 14:07:07 GMT+7
export interface Result { name: string; email: string }
export interface Person { name: string; age: number; email: string }
const people: Person[] = (() => {
    const _t = Deno.readTextFileSync(new URL("../../../interpreter/valid/people.yaml", ).pathname).trim().split();
    const _o: any[] = [];
    let c: any = {};
    for (const line of _t) {
        if ((line.startsWith("- "))) {
            if ((Object.keys(c).length))
                _o.push(c);
            c = {};
            line = line.slice(2);
        }
        if ((!line))
            continue
        const [k, v] = line.split(":");
        const val = v.trim();
        c[k.trim()] = (val) ? +val : val;
    }
    if ((Object.keys(c).length))
        _o.push(c);
    return _o;
})();
const adults: Result[] = (() => {
    const result: Result[] = [];
    for (const p of people) {
        if (((p["age"] >= 18))) {
            result.push({name: p["name"], email: p["email"]});
        }
    }
    const out = result;
    return out;
})();
for (const a of adults) {
    console.log((String(a["name"]) + " " + String(a["email"])).trim());
}
