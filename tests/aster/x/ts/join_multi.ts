// Generated by Mochi v0.10.35 on 2025-07-22 14:07:05 GMT+7
export interface Result { id: number; name: string }
export interface Customer { id: number; name: string }
export interface Order { id: number; customerId: number }
export interface Item { orderId: number; sku: string }
const customers: Customer[] = [{id: 1, name: "Alice"}, {id: 2, name: "Bob"}];
const orders: Order[] = [{id: 100, customerId: 1}, {id: 101, customerId: 2}];
const items: Item[] = [{orderId: 100, sku: "a"}, {orderId: 101, sku: "b"}];
const result: Result[] = (() => {
    const result: Result[] = [];
    for (const o of orders) {
        for (const c of customers) {
            if ((!((o["customerId"] == c["id"]))))
                continue
            for (const i of items) {
                if ((!((o["id"] == i["orderId"]))))
                    continue
                result.push({name: c["name"], sku: i["sku"]});
            }
        }
    }
    return result;
})();
console.log(String("--- Multi Join ---"));
for (const r of result) {
    console.log((String(r["name"]) + " " + String("bought item") + " " + String(r["sku"])).trim());
}
