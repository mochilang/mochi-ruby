// Generated by Mochi transpiler v0.10.33 on 2025-07-21 18:20:21 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <vector>
#include <type_traits>
auto prefix = std::string("fore");
auto s1 = std::string("forest");
auto s2 = std::string("desert");
int main() {
    std::cout << std::boolalpha << (((const auto &c) {
        if (std::is_same_v<std::decay_t<decltype>, std::string>) {
            return c.substr(0, prefix.size() - 0);
        }
    })(s1) == prefix) << std::endl;
    std::cout << std::boolalpha << (((const auto &c) {
        if (std::is_same_v<std::decay_t<decltype>, std::string>) {
            return c.substr(0, prefix.size() - 0);
        }
    })(s2) == prefix) << std::endl;
    return 0;
}
