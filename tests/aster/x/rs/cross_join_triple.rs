// Generated by Mochi transpiler v0.10.33 on 2025-07-21 18:06 +0700
#[derive(Debug, Clone)]
struct QueryItem {
    n: i64,
    l: String,
    b: bool,
}
impl std::fmt::Display for QueryItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"n\": {}", self.n)?;
        write!(f, ", ")?;
        write!(f, "\"l\": \"{}\"", self.l)?;
        write!(f, ", ")?;
        write!(f, "\"b\": {}", self.b)?;
        write!(f, "}}")
    }
}
fn main() {
    let nums: Vec<i64> = vec![1, 2];
    let letters: Vec<String> = vec![String::from("A"), String::from("B")];
    let bools: Vec<bool> = vec![true, false];
    let combos: Vec<QueryItem> = {
        let mut _q: Vec<QueryItem> = Vec::new();
        for n in nums {
            for l in &letters {
                for b in &bools {
                    _q.push(QueryItem {n: n, l: l.clone(), b: *b});
                }
            }
        }
        _q
    };
    println!("{}", "--- Cross Join of three lists ---");
    for c in &combos {
        println!("{}", format!("{} {} {}", c.n, c.l, if c.b { 1 } else { 0 }).trim_end());
    }
}
