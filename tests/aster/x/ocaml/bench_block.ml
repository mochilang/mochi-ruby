(* Generated by Mochi transpiler v0.10.39 on 2025-07-25 07:45 +0700 *)
let __show v = let open open Obj in let list_aux o =
  if  then "" in
let r = repr v in
if is_int r then string_of_int (magic v)
let _now_seed = ref 0
let _now_seeded = ref false
let _now () =
  if not !_now_seeded then (Sys.getenv_opt "MOCHI_NOW_SEED" Some s (try _now_seed := int_of_string s;
  _now_seeded := true with _ -> ()) None ());
    if !_now_seeded then (_now_seed := ;
  !_now_seed)
exception Break
exception Continue
exception Return
let () =
_now_seeded := true;
  _now_seed := 1;
    let start = _now () in
  let n = 1000 in
  let s = ref (0) in
(try   for i = 1 to (n - 1) do
try s := (!s + i) with Continue -> ()
  done with Break -> ());
    let finish = _now () in
Printf.printf () "simple"
