// MEP-16 N1 (else form): `if x == none { ... } else { ... }` narrows
// x from Option<T> to T inside the else-branch.
let p: int? = none
let total = if p == none { 0 } else { p + 1 }
expect total == 0
