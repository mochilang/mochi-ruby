// Generated by Mochi compiler v0.10.25 on 2025-07-13T10:54:08Z
// q4.mochi
import java.util.*;

class IdInfo {
    int id;
    String info;
    IdInfo(int id, String info) {
        this.id = id;
        this.info = info;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof IdInfo other)) return false;
        return Objects.equals(this.id, other.id) && Objects.equals(this.info, other.info);
    }
    @Override public int hashCode() {
        return Objects.hash(id, info);
    }
    int size() { return 2; }
}
class IdKeyword {
    int id;
    String keyword;
    IdKeyword(int id, String keyword) {
        this.id = id;
        this.keyword = keyword;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof IdKeyword other)) return false;
        return Objects.equals(this.id, other.id) && Objects.equals(this.keyword, other.keyword);
    }
    @Override public int hashCode() {
        return Objects.hash(id, keyword);
    }
    int size() { return 2; }
}
class IdTitleProductionYear {
    int id;
    String title;
    int production_year;
    IdTitleProductionYear(int id, String title, int production_year) {
        this.id = id;
        this.title = title;
        this.production_year = production_year;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof IdTitleProductionYear other)) return false;
        return Objects.equals(this.id, other.id) && Objects.equals(this.title, other.title) && Objects.equals(this.production_year, other.production_year);
    }
    @Override public int hashCode() {
        return Objects.hash(id, title, production_year);
    }
    int size() { return 3; }
}
class MovieIdKeywordId {
    int movie_id;
    int keyword_id;
    MovieIdKeywordId(int movie_id, int keyword_id) {
        this.movie_id = movie_id;
        this.keyword_id = keyword_id;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof MovieIdKeywordId other)) return false;
        return Objects.equals(this.movie_id, other.movie_id) && Objects.equals(this.keyword_id, other.keyword_id);
    }
    @Override public int hashCode() {
        return Objects.hash(movie_id, keyword_id);
    }
    int size() { return 2; }
}
class MovieIdInfoTypeIdInfo {
    int movie_id;
    int info_type_id;
    String info;
    MovieIdInfoTypeIdInfo(int movie_id, int info_type_id, String info) {
        this.movie_id = movie_id;
        this.info_type_id = info_type_id;
        this.info = info;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof MovieIdInfoTypeIdInfo other)) return false;
        return Objects.equals(this.movie_id, other.movie_id) && Objects.equals(this.info_type_id, other.info_type_id) && Objects.equals(this.info, other.info);
    }
    @Override public int hashCode() {
        return Objects.hash(movie_id, info_type_id, info);
    }
    int size() { return 3; }
}
class RatingTitle {
    String rating;
    String title;
    RatingTitle(String rating, String title) {
        this.rating = rating;
        this.title = title;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof RatingTitle other)) return false;
        return Objects.equals(this.rating, other.rating) && Objects.equals(this.title, other.title);
    }
    @Override public int hashCode() {
        return Objects.hash(rating, title);
    }
    int size() { return 2; }
}
class RatingMovieTitle {
    int rating;
    int movie_title;
    RatingMovieTitle(int rating, int movie_title) {
        this.rating = rating;
        this.movie_title = movie_title;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof RatingMovieTitle other)) return false;
        return Objects.equals(this.rating, other.rating) && Objects.equals(this.movie_title, other.movie_title);
    }
    @Override public int hashCode() {
        return Objects.hash(rating, movie_title);
    }
    int size() { return 2; }
}
public class Q4 {
    static Map<String,Object> asMap(Object o) {
        if (o instanceof Map<?,?> mm) {
            LinkedHashMap<String,Object> m = new LinkedHashMap<>();
            for (Map.Entry<?,?> e : mm.entrySet()) m.put(String.valueOf(e.getKey()), e.getValue());
            return m;
        }
        LinkedHashMap<String,Object> m = new LinkedHashMap<>();
        for (var f : o.getClass().getDeclaredFields()) { try { f.setAccessible(true); m.put(f.getName(), f.get(o)); } catch (Exception e) { throw new RuntimeException(e); } }
        return m;
    }
    static void saveJsonl(List<?> list) {
        for (Object obj : list) {
            Map<String,Object> m = asMap(obj);
            List<String> parts = new ArrayList<>();
            for (Map.Entry<?,?> e : m.entrySet()) { parts.add("\"" + e.getKey() + "\":" + e.getValue()); }
            System.out.println("{" + String.join(",", parts) + "}");
        }
    }
    static String toJson(Object o) {
        if (o instanceof Map<?,?> m) {
            StringJoiner j = new StringJoiner(",", "{", "}");
            for (Map.Entry<?,?> e : m.entrySet()) j.add("\"" + e.getKey() + "\":" + toJson(e.getValue()));
            return j.toString();
        } else if (o instanceof Collection<?> c) {
            StringJoiner j = new StringJoiner(",", "[", "]");
            for (var x : c) j.add(toJson(x));
            return j.toString();
        } else if (o instanceof String s) {
            return "\"" + s + "\"";
        } else if (o instanceof Number || o instanceof Boolean || o instanceof Character) {
            return String.valueOf(o);
        } else {
            Map<String,Object> m = asMap(o);
            StringJoiner j = new StringJoiner(",", "{", "}");
            for (Map.Entry<String,Object> e : m.entrySet()) j.add("\"" + e.getKey() + "\":" + toJson(e.getValue()));
            return j.toString();
        }
    }
    static void json(Object o) { System.out.println(toJson(o)); }
    public static void main(String[] args) {
    List<IdInfo> info_type = new ArrayList<>(Arrays.asList(new IdInfo(1, "rating"), new IdInfo(2, "other")));
    List<IdKeyword> keyword = new ArrayList<>(Arrays.asList(new IdKeyword(1, "great sequel"), new IdKeyword(2, "prequel")));
    List<IdTitleProductionYear> title = new ArrayList<>(Arrays.asList(new IdTitleProductionYear(10, "Alpha Movie", 2006), new IdTitleProductionYear(20, "Beta Film", 2007), new IdTitleProductionYear(30, "Old Film", 2004)));
    List<MovieIdKeywordId> movie_keyword = new ArrayList<>(Arrays.asList(new MovieIdKeywordId(10, 1), new MovieIdKeywordId(20, 1), new MovieIdKeywordId(30, 1)));
    List<MovieIdInfoTypeIdInfo> movie_info_idx = new ArrayList<>(Arrays.asList(new MovieIdInfoTypeIdInfo(10, 1, "6.2"), new MovieIdInfoTypeIdInfo(20, 1, "7.8"), new MovieIdInfoTypeIdInfo(30, 1, "4.5")));
    List<RatingTitle> rows = (new java.util.function.Supplier<List<RatingTitle>>(){public List<RatingTitle> get(){
    List<RatingTitle> res0 = new ArrayList<>();
    for (var it : info_type) {
        for (var mi : movie_info_idx) {
            if (!(it.id == mi.info_type_id)) continue;
            for (var t : title) {
                if (!(t.id == mi.movie_id)) continue;
                for (var mk : movie_keyword) {
                    if (!(mk.movie_id == t.id)) continue;
                    for (var k : keyword) {
                        if (!(k.id == mk.keyword_id)) continue;
                        if (!(Objects.equals(it.info, "rating") && k.keyword.contains("sequel") != null && String.valueOf(mi.info).compareTo(String.valueOf("5.0")) > 0 && t.production_year > 2005 && mk.movie_id == mi.movie_id)) continue;
                        res0.add(new RatingTitle(mi.info, t.title));
                    }
                }
            }
        }
    }
    return res0;
}}).get();
    List<RatingMovieTitle> result = new ArrayList<>(Arrays.asList(new RatingMovieTitle((new java.util.function.Supplier<List<String>>(){public List<String> get(){
    List<String> res1 = new ArrayList<>();
    for (var r : rows) {
        res1.add(r.rating);
    }
    return res1;
}}).get().stream().mapToInt(n -> ((Number)n).intValue()).min().orElse(Integer.MAX_VALUE), (new java.util.function.Supplier<List<String>>(){public List<String> get(){
    List<String> res2 = new ArrayList<>();
    for (var r : rows) {
        res2.add(r.title);
    }
    return res2;
}}).get().stream().mapToInt(n -> ((Number)n).intValue()).min().orElse(Integer.MAX_VALUE))));
    json(result);
    if (!(Objects.equals(result, Arrays.asList(new RatingMovieTitle("6.2", "Alpha Movie"))))) throw new AssertionError("expect failed");
    }
}
