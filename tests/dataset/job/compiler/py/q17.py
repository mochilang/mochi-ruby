# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:45:24Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Auto1:
    member_in_charnamed_american_movie: str
    a1: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto2:
    movie_id: int
    person_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto3:
    id: int
    country_code: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto4:
    id: int
    keyword: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto5:
    movie_id: int
    company_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto6:
    movie_id: int
    keyword_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto7:
    id: int
    name: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto8:
    id: int
    title: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


def _min(v):
    if hasattr(v, "Items"):
        v = v.Items
    if not isinstance(v, list):
        raise Exception("min() expects list or group")
    vals = [it for it in v if it is not None]
    if not vals:
        return 0
    return min(vals)


def _query(src, joins, opts):
    items = [[v] for v in src]
    for j in joins:
        joined = []
        if j.get("right") and j.get("left"):
            matched = [False] * len(j["items"])
            for left in items:
                m = False
                for ri, right in enumerate(j["items"]):
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    matched[ri] = True
                    joined.append(left + [right])
                if not m:
                    joined.append(left + [None])
            for ri, right in enumerate(j["items"]):
                if not matched[ri]:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        elif j.get("right"):
            for right in j["items"]:
                m = False
                for left in items:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if not m:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        else:
            for left in items:
                m = False
                for right in j["items"]:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if j.get("left") and (not m):
                    joined.append(left + [None])
        items = joined
    if opts.get("where"):
        items = [r for r in items if opts["where"](*r)]
    if opts.get("sortKey"):

        def _key(it):
            k = opts["sortKey"](*it)
            if isinstance(k, (list, tuple, dict)):
                return str(k)
            return k

        items.sort(key=_key)
    if "skip" in opts:
        n = opts["skip"]
        if n < 0:
            n = 0
        items = items[n:] if n < len(items) else []
    if "take" in opts:
        n = opts["take"]
        if n < 0:
            n = 0
        items = items[:n] if n < len(items) else items
    res = []
    for r in items:
        res.append(opts["select"](*r))
    return res


def test_Q17_finds_US_character_name_movie_with_actor_starting_with_B():
    assert result == [
        Auto1(member_in_charnamed_american_movie="Bob Smith", a1="Bob Smith")
    ]


cast_info = [Auto2(movie_id=1, person_id=1), Auto2(movie_id=2, person_id=2)]
company_name = [Auto3(id=1, country_code="[us]"), Auto3(id=2, country_code="[ca]")]
keyword = [
    Auto4(id=10, keyword="character-name-in-title"),
    Auto4(id=20, keyword="other"),
]
movie_companies = [Auto5(movie_id=1, company_id=1), Auto5(movie_id=2, company_id=2)]
movie_keyword = [Auto6(movie_id=1, keyword_id=10), Auto6(movie_id=2, keyword_id=20)]
name = [Auto7(id=1, name="Bob Smith"), Auto7(id=2, name="Alice Jones")]
title = [Auto8(id=1, title="Bob's Journey"), Auto8(id=2, title="Foreign Film")]
matches = _query(
    name,
    [
        {"items": cast_info, "on": lambda n, ci: ci["person_id"] == n["id"]},
        {"items": title, "on": lambda n, ci, t: t["id"] == ci["movie_id"]},
        {"items": movie_keyword, "on": lambda n, ci, t, mk: mk["movie_id"] == t["id"]},
        {"items": keyword, "on": lambda n, ci, t, mk, k: k["id"] == mk["keyword_id"]},
        {
            "items": movie_companies,
            "on": lambda n, ci, t, mk, k, mc: mc["movie_id"] == t["id"],
        },
        {
            "items": company_name,
            "on": lambda n, ci, t, mk, k, mc, cn: cn["id"] == mc["company_id"],
        },
    ],
    {
        "select": lambda n, ci, t, mk, k, mc, cn: n["name"],
        "where": lambda n, ci, t, mk, k, mc, cn: (
            (
                (
                    (
                        cn["country_code"] == "[us]"
                        and k["keyword"] == "character-name-in-title"
                    )
                    and str(n["name"]).startswith("B")
                )
                and ci["movie_id"] == mk["movie_id"]
            )
            and ci["movie_id"] == mc["movie_id"]
        )
        and mc["movie_id"] == mk["movie_id"],
    },
)
result = [Auto1(member_in_charnamed_american_movie=_min(matches), a1=_min(matches))]
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_Q17_finds_US_character_name_movie_with_actor_starting_with_B()
