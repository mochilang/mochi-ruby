# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:45:19Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Auto1:
    from_company: str
    movie_link_type: str
    non_polish_sequel_movie: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto10:
    company: object
    link: object
    title: object

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto2:
    id: int
    name: str
    country_code: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto3:
    id: int
    kind: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto4:
    id: int
    keyword: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto5:
    id: int
    link: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto6:
    movie_id: int
    company_id: int
    company_type_id: int
    note: object

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto7:
    movie_id: int
    keyword_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto8:
    movie_id: int
    link_type_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto9:
    id: int
    production_year: int
    title: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


def _min(v):
    if hasattr(v, "Items"):
        v = v.Items
    if not isinstance(v, list):
        raise Exception("min() expects list or group")
    vals = [it for it in v if it is not None]
    if not vals:
        return 0
    return min(vals)


def _query(src, joins, opts):
    items = [[v] for v in src]
    for j in joins:
        joined = []
        if j.get("right") and j.get("left"):
            matched = [False] * len(j["items"])
            for left in items:
                m = False
                for ri, right in enumerate(j["items"]):
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    matched[ri] = True
                    joined.append(left + [right])
                if not m:
                    joined.append(left + [None])
            for ri, right in enumerate(j["items"]):
                if not matched[ri]:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        elif j.get("right"):
            for right in j["items"]:
                m = False
                for left in items:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if not m:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        else:
            for left in items:
                m = False
                for right in j["items"]:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if j.get("left") and (not m):
                    joined.append(left + [None])
        items = joined
    if opts.get("where"):
        items = [r for r in items if opts["where"](*r)]
    if opts.get("sortKey"):

        def _key(it):
            k = opts["sortKey"](*it)
            if isinstance(k, (list, tuple, dict)):
                return str(k)
            return k

        items.sort(key=_key)
    if "skip" in opts:
        n = opts["skip"]
        if n < 0:
            n = 0
        items = items[n:] if n < len(items) else []
    if "take" in opts:
        n = opts["take"]
        if n < 0:
            n = 0
        items = items[:n] if n < len(items) else items
    res = []
    for r in items:
        res.append(opts["select"](*r))
    return res


def test_Q11_returns_min_company__link_type_and_title():
    assert result == [
        Auto1(
            from_company="Best Film Co",
            movie_link_type="follow-up",
            non_polish_sequel_movie="Alpha",
        )
    ]


company_name = [
    Auto2(id=1, name="Best Film Co", country_code="[us]"),
    Auto2(id=2, name="Warner Studios", country_code="[de]"),
    Auto2(id=3, name="Polish Films", country_code="[pl]"),
]
company_type = [
    Auto3(id=1, kind="production companies"),
    Auto3(id=2, kind="distributors"),
]
keyword = [Auto4(id=1, keyword="sequel"), Auto4(id=2, keyword="thriller")]
link_type = [
    Auto5(id=1, link="follow-up"),
    Auto5(id=2, link="follows from"),
    Auto5(id=3, link="remake"),
]
movie_companies = [
    Auto6(movie_id=10, company_id=1, company_type_id=1, note=None),
    Auto6(movie_id=20, company_id=2, company_type_id=1, note=None),
    Auto6(movie_id=30, company_id=3, company_type_id=1, note=None),
]
movie_keyword = [
    Auto7(movie_id=10, keyword_id=1),
    Auto7(movie_id=20, keyword_id=1),
    Auto7(movie_id=20, keyword_id=2),
    Auto7(movie_id=30, keyword_id=1),
]
movie_link = [
    Auto8(movie_id=10, link_type_id=1),
    Auto8(movie_id=20, link_type_id=2),
    Auto8(movie_id=30, link_type_id=3),
]
title = [
    Auto9(id=10, production_year=1960, title="Alpha"),
    Auto9(id=20, production_year=1970, title="Beta"),
    Auto9(id=30, production_year=1985, title="Polish Movie"),
]
matches = _query(
    company_name,
    [
        {"items": movie_companies, "on": lambda cn, mc: mc["company_id"] == cn["id"]},
        {
            "items": company_type,
            "on": lambda cn, mc, ct: ct["id"] == mc["company_type_id"],
        },
        {"items": title, "on": lambda cn, mc, ct, t: t["id"] == mc["movie_id"]},
        {
            "items": movie_keyword,
            "on": lambda cn, mc, ct, t, mk: mk["movie_id"] == t["id"],
        },
        {
            "items": keyword,
            "on": lambda cn, mc, ct, t, mk, k: k["id"] == mk["keyword_id"],
        },
        {
            "items": movie_link,
            "on": lambda cn, mc, ct, t, mk, k, ml: ml["movie_id"] == t["id"],
        },
        {
            "items": link_type,
            "on": lambda cn, mc, ct, t, mk, k, ml, lt: lt["id"] == ml["link_type_id"],
        },
    ],
    {
        "select": lambda cn, mc, ct, t, mk, k, ml, lt: Auto10(
            company=cn["name"], link=lt["link"], title=t["title"]
        ),
        "where": lambda cn, mc, ct, t, mk, k, ml, lt: (
            (
                (
                    (
                        (
                            (
                                (
                                    (
                                        (
                                            cn["country_code"] != "[pl]"
                                            and (
                                                "Film" in cn["name"]
                                                or "Warner" in cn["name"]
                                            )
                                        )
                                        and ct["kind"] == "production companies"
                                    )
                                    and k["keyword"] == "sequel"
                                )
                                and "follow" in lt["link"]
                            )
                            and mc["note"] == None
                        )
                        and t["production_year"] >= 1950
                    )
                    and t["production_year"] <= 2000
                )
                and ml["movie_id"] == mk["movie_id"]
            )
            and ml["movie_id"] == mc["movie_id"]
        )
        and mk["movie_id"] == mc["movie_id"],
    },
)
result = [
    Auto1(
        from_company=_min([x.company for x in matches]),
        movie_link_type=_min([x.link for x in matches]),
        non_polish_sequel_movie=_min([x.title for x in matches]),
    )
]
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_Q11_returns_min_company__link_type_and_title()
