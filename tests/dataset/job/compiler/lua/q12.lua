-- Generated by Mochi compiler v0.10.25 on 2025-07-13T11:45:19Z
function __eq(a, b)
    if type(a) ~= type(b) then return false end
    if type(a) == 'number' then return math.abs(a-b) < 1e-9 end
    if type(a) ~= 'table' then return a == b end
    if (a[1] ~= nil or #a > 0) and (b[1] ~= nil or #b > 0) then
        if #a ~= #b then return false end
        for i = 1, #a do if not __eq(a[i], b[i]) then return false end end
        return true
    end
    for k, v in pairs(a) do if not __eq(v, b[k]) then return false end end
    for k, _ in pairs(b) do if a[k] == nil then return false end end
    return true
end
function __json(v)
    if type(v) == 'table' and next(v) == nil then print('[]'); return end
    local function sort(x)
        if type(x) ~= 'table' then return x end
        if x[1] ~= nil or #x > 0 then
            local out = {}
            for i=1,#x do out[i] = sort(x[i]) end
            return out
        end
        local keys = {}
        for k in pairs(x) do keys[#keys+1] = k end
        table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
        local out = {}
        for _,k in ipairs(keys) do out[k] = sort(x[k]) end
        return out
    end
    local ok, json = pcall(require, 'json')
    if not ok then ok, json = pcall(require, 'cjson') end
    if ok then
        print(json.encode(sort(v)))
        return
    end
    local function enc(x)
        local t = type(x)
        if t == 'nil' then
            return 'null'
        elseif t == 'boolean' or t == 'number' then
            return tostring(x)
        elseif t == 'string' then
            return string.format('%q', x)
        elseif t == 'table' then
            if x[1] ~= nil or #x > 0 then
                local parts = {}
                for i=1,#x do parts[#parts+1] = enc(x[i]) end
                return '['..table.concat(parts, ',')..']'
            else
                local keys = {}
                for k in pairs(x) do keys[#keys+1] = k end
                table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
                local parts = {}
                for _,k in ipairs(keys) do parts[#parts+1] = enc(k)..':'..enc(x[k]) end
                return '{'..table.concat(parts, ',')..'}'
            end
        else
            return 'null'
        end
    end
    print(enc(sort(v)))
end
function __query(src, joins, opts)
    local whereFn = opts.where
    local items = {}
    if #joins == 0 and whereFn then
        for _, v in ipairs(src) do if whereFn(v) then items[#items+1] = {v} end end
    else
        for _, v in ipairs(src) do items[#items+1] = {v} end
    end
    for ji, j in ipairs(joins) do
        local joined = {}
        local jitems = j.items or {}
        if j.right and j.left then
            local matched = {}
            for _, left in ipairs(items) do
                local m = false
                for ri, right in ipairs(jitems) do
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left)}
                        args[#args+1] = right
                        keep = j.on(table.unpack(args))
                    end
                    if keep then
                        m = true; matched[ri] = true
                        local row = {table.unpack(left)}
                        row[#row+1] = right
                        if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if not m then
                    local row = {table.unpack(left)}
                    row[#row+1] = nil
                    if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
            for ri, right in ipairs(jitems) do
                if not matched[ri] then
                    local row = {}
                    for _=1,ji do row[#row+1] = nil end
                    row[#row+1] = right
                    if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        elseif j.right then
            for _, right in ipairs(jitems) do
                local m = false
                for _, left in ipairs(items) do
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left)}
                        args[#args+1] = right
                        keep = j.on(table.unpack(args))
                    end
                    if keep then
                        m = true
                        local row = {table.unpack(left)}
                        row[#row+1] = right
                        if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if not m then
                    local row = {}
                    for _=1,ji do row[#row+1] = nil end
                    row[#row+1] = right
                    if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        else
            for _, left in ipairs(items) do
                local m = false
                for _, right in ipairs(jitems) do
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left)}
                        args[#args+1] = right
                        keep = j.on(table.unpack(args))
                    end
                    if keep then
                        m = true
                        local row = {table.unpack(left)}
                        row[#row+1] = right
                        if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if j.left and not m then
                    local row = {table.unpack(left)}
                    row[#row+1] = nil
                    if ji == #joins and whereFn and not whereFn(table.unpack(row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        end
        items = joined
    end
    if opts.sortKey then
        local pairs = {}
        for _, it in ipairs(items) do pairs[#pairs+1] = {item=it, key=opts.sortKey(table.unpack(it))} end
        table.sort(pairs, function(a,b)
            local ak, bk = a.key, b.key
            if type(ak)=='number' and type(bk)=='number' then return ak < bk end
            if type(ak)=='string' and type(bk)=='string' then return ak < bk end
            return tostring(ak) < tostring(bk)
        end)
        items = {}
        for i,p in ipairs(pairs) do items[i] = p.item end
    end
    if opts.skip ~= nil then
        local n = opts.skip
        if n < #items then
            for i=1,n do table.remove(items,1) end
        else
            items = {}
        end
    end
    if opts.take ~= nil then
        local n = opts.take
        if n < #items then
            for i=#items, n+1, -1 do table.remove(items) end
        end
    end
    local res = {}
    for _, r in ipairs(items) do res[#res+1] = opts.selectFn(table.unpack(r)) end
    return res
end
function __run_tests(tests)
    local function format_duration(d)
        if d < 1e-6 then return string.format('%dns', math.floor(d*1e9)) end
        if d < 1e-3 then return string.format('%.1fµs', d*1e6) end
        if d < 1 then return string.format('%.1fms', d*1e3) end
        return string.format('%.2fs', d)
    end
    local failures = 0
    for _, t in ipairs(tests) do
        io.write('   test ' .. t.name .. ' ...')
        local start = os.clock()
        local ok, err = pcall(t.fn)
        local dur = os.clock() - start
        if ok then
            io.write(' ok (' .. format_duration(dur) .. ')\n')
        else
            io.write(' fail ' .. tostring(err) .. ' (' .. format_duration(dur) .. ')\n')
            failures = failures + 1
        end
    end
    if failures > 0 then
        io.write('\n[FAIL] ' .. failures .. ' test(s) failed.\n')
    end
end
function test_Q12_finds_high_rated_US_drama_or_horror_with_company()
    if not (__eq(result, {{["movie_company"]="Best Pictures", ["rating"]=8.3, ["drama_horror_movie"]="Great Drama"}})) then error('expect failed') end
end

company_name = {{["id"]=1, ["name"]="Best Pictures", ["country_code"]="[us]"}, {["id"]=2, ["name"]="Foreign Films", ["country_code"]="[uk]"}}
company_type = {{["id"]=10, ["kind"]="production companies"}, {["id"]=20, ["kind"]="distributors"}}
info_type = {{["id"]=100, ["info"]="genres"}, {["id"]=200, ["info"]="rating"}}
movie_companies = {{["movie_id"]=1000, ["company_id"]=1, ["company_type_id"]=10}, {["movie_id"]=2000, ["company_id"]=2, ["company_type_id"]=10}}
movie_info = {{["movie_id"]=1000, ["info_type_id"]=100, ["info"]="Drama"}, {["movie_id"]=2000, ["info_type_id"]=100, ["info"]="Horror"}}
movie_info_idx = {{["movie_id"]=1000, ["info_type_id"]=200, ["info"]=8.3}, {["movie_id"]=2000, ["info_type_id"]=200, ["info"]=7.5}}
title = {{["id"]=1000, ["production_year"]=2006, ["title"]="Great Drama"}, {["id"]=2000, ["production_year"]=2007, ["title"]="Low Rated"}}
result = (function()
    local _src = company_name
    return __query(_src, {
        { items = movie_companies, on = function(cn, mc) return __eq(mc.company_id, cn.id) end },
        { items = company_type, on = function(cn, mc, ct) return __eq(ct.id, mc.company_type_id) end },
        { items = title, on = function(cn, mc, ct, t) return __eq(t.id, mc.movie_id) end },
        { items = movie_info, on = function(cn, mc, ct, t, mi) return __eq(mi.movie_id, t.id) end },
        { items = info_type, on = function(cn, mc, ct, t, mi, it1) return __eq(it1.id, mi.info_type_id) end },
        { items = movie_info_idx, on = function(cn, mc, ct, t, mi, it1, mi_idx) return __eq(mi_idx.movie_id, t.id) end },
        { items = info_type, on = function(cn, mc, ct, t, mi, it1, mi_idx, it2) return __eq(it2.id, mi_idx.info_type_id) end }
    }, { selectFn = function(cn, mc, ct, t, mi, it1, mi_idx, it2) return {["movie_company"]=cn.name, ["rating"]=mi_idx.info, ["drama_horror_movie"]=t.title} end, where = function(cn, mc, ct, t, mi, it1, mi_idx, it2) return ((((((((__eq(cn.country_code, "[us]") and __eq(ct.kind, "production companies")) and __eq(it1.info, "genres")) and __eq(it2.info, "rating")) and ((__eq(mi.info, "Drama") or __eq(mi.info, "Horror")))) and (mi_idx.info > 8.0)) and (t.production_year >= 2005)) and (t.production_year <= 2008))) end })
end)()
__json(result)
local __tests = {
    {name="Q12 finds high-rated US drama or horror with company", fn=test_Q12_finds_high_rated_US_drama_or_horror_with_company},
}
