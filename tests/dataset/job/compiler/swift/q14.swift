// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
import Foundation

func _json(_ v: Any) {
    func _sort(_ x: Any) -> Any {
        if let a = x as? [Any] { return a.map { _sort($0) } }
        if let m = x as? [String:Any] {
            var out: [String:Any] = [:]
            for k in m.keys.sorted() { out[k] = _sort(m[k]!) }
            return out
        }
        return x
    }
    let obj = _sort(v)
    if JSONSerialization.isValidJSONObject(obj),
       let data = try? JSONSerialization.data(withJSONObject: obj, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        print(s)
    } else {
        print(obj)
    }
}
func expect(_ cond: Bool) {
    if !cond { fatalError("expect failed") }
}
func _structMap(_ v: Any) -> [String:Any]? {
    let mirror = Mirror(reflecting: v)
    if mirror.displayStyle == .struct || mirror.displayStyle == .class {
        var m: [String:Any] = [:]
        for child in mirror.children {
            if let k = child.label { m[k] = child.value }
        }
        return m
    }
    return nil
}
func _equal(_ a: Any, _ b: Any) -> Bool {
    if let am = _structMap(a), let bm = _structMap(b) {
        return _equal(am, bm)
    }
    if let am = _structMap(a), let bd = b as? [String: Any] {
        return _equal(am, bd)
    }
    if let ad = a as? [String: Any], let bm = _structMap(b) {
        return _equal(ad, bm)
    }
    switch (a, b) {
    case let (x as [Any], y as [Any]):
        if x.count != y.count { return false }
        for i in 0..<x.count {
            if !_equal(x[i], y[i]) { return false }
        }
        return true
    case let (x as [String: Any], y as [String: Any]):
        if x.count != y.count { return false }
        for (k, av) in x {
            guard let bv = y[k] else { return false }
            if !_equal(av, bv) { return false }
        }
        return true
    case let (ai as Double, bi as Int):
        return ai == Double(bi)
    case let (ai as Int, bi as Double):
        return Double(ai) == bi
    case let (ai as Double, bi as Double):
        return ai == bi
    case let (ai as Int, bi as Int):
        return ai == bi
    case let (sa as String, sb as String):
        return sa == sb
    case let (ab as Bool, bb as Bool):
        return ab == bb
    default:
        return false
    }
}
class _Group {
    var key: Any
    var Items: [Any] = []
    init(_ k: Any) { self.key = k }
}


func _keyStr(_ v: Any) -> String {
    if let data = try? JSONSerialization.data(withJSONObject: v, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        return s
    }
    return String(describing: v)
}
func _min(_ v: Any) -> Any {
    var list: [Any]? = nil
    if let g = v as? _Group { list = g.Items }
    else if let arr = v as? [Any] { list = arr }
    else if let arr = v as? [Int] { return arr.min() ?? 0 }
    else if let arr = v as? [Double] { return arr.min() ?? 0.0 }
    else if let arr = v as? [String] { return arr.min() ?? "" }
    guard let items = list else { fatalError("min() expects list or group") }
    if items.isEmpty { return 0 }
    if let s = items[0] as? String {
        var m = s
        for it in items.dropFirst() {
            if let v = it as? String, v < m { m = v }
        }
        return m
    }
    func toDouble(_ v: Any) -> Double {
        if let i = v as? Int { return Double(i) }
        if let d = v as? Double { return d }
        if let f = v as? Float { return Double(f) }
        if let i = v as? Int64 { return Double(i) }
        return 0
    }
    var m = toDouble(items[0])
    var isFloat = items[0] is Double || items[0] is Float
    for it in items.dropFirst() {
        if it is Double || it is Float { isFloat = true }
        let d = toDouble(it)
        if d < m { m = d }
    }
    return isFloat ? m : Int(m)
}
struct InfoType: Equatable {
    var id: Int
    var info: String
}

struct Keyword: Equatable {
    var id: Int
    var keyword: String
}

struct KindType: Equatable {
    var id: Int
    var kind: String
}

struct Matche: Equatable {
    var rating: Int
    var title: String
}

struct MovieInfo: Equatable {
    var info: String
    var info_type_id: Int
    var movie_id: Int
}

struct MovieInfoIdx: Equatable {
    var info: Int
    var info_type_id: Int
    var movie_id: Int
}

struct MovieKeyword: Equatable {
    var keyword_id: Int
    var movie_id: Int
}

struct Title: Equatable {
    var id: Int
    var kind_id: Int
    var production_year: Int
    var title: String
}

var info_type = [InfoType(id: 1, info: "countries"), InfoType(id: 2, info: "rating")]
var keyword = [Keyword(id: 1, keyword: "murder"), Keyword(id: 2, keyword: "blood"), Keyword(id: 3, keyword: "romance")]
var kind_type = [KindType(id: 1, kind: "movie")]
var title = [Title(id: 1, kind_id: 1, production_year: 2012, title: "A Dark Movie"), Title(id: 2, kind_id: 1, production_year: 2013, title: "Brutal Blood"), Title(id: 3, kind_id: 1, production_year: 2008, title: "Old Film")]
var movie_info = [MovieInfo(info: "Sweden", info_type_id: 1, movie_id: 1), MovieInfo(info: "USA", info_type_id: 1, movie_id: 2), MovieInfo(info: "USA", info_type_id: 1, movie_id: 3)]
var movie_info_idx = [MovieInfoIdx(info: 7.0, info_type_id: 2, movie_id: 1), MovieInfoIdx(info: 7.5, info_type_id: 2, movie_id: 2), MovieInfoIdx(info: 9.1, info_type_id: 2, movie_id: 3)]
var movie_keyword = [MovieKeyword(keyword_id: 1, movie_id: 1), MovieKeyword(keyword_id: 2, movie_id: 2), MovieKeyword(keyword_id: 3, movie_id: 3)]
var allowed_keywords = ["murder", "murder-in-title", "blood", "violence"]
var allowed_countries = ["Sweden", "Norway", "Germany", "Denmark", "Swedish", "Denish", "Norwegian", "German", "USA", "American"]
var matches = ({
	var _res: [(rating: Int, title: String)] = []
	for it1 in info_type {
		for it2 in info_type {
			for k in keyword {
				for kt in kind_type {
					for mi in movie_info {
						for mi_idx in movie_info_idx {
							for mk in movie_keyword {
								for t in title {
									if !((it1.info == "countries" && it2.info == "rating" && (allowed_keywords.contains(k.keyword)) && kt.kind == "movie" && (allowed_countries.contains(mi.info)) && mi_idx.info < 8.5 && t.production_year > 2010 && kt.id == t.kind_id && t.id == mi.movie_id && t.id == mk.movie_id && t.id == mi_idx.movie_id && mk.movie_id == mi.movie_id && mk.movie_id == mi_idx.movie_id && mi.movie_id == mi_idx.movie_id && k.id == mk.keyword_id && it1.id == mi.info_type_id && it2.id == mi_idx.info_type_id) != nil) { continue }
									_res.append(["rating": mi_idx.info, "title": t.title])
								}
							}
						}
					}
				}
			}
		}
	}
	return _res
}())
let result = ["rating": _min(matches.map { x in x.rating }), "northern_dark_movie": _min(matches.map { x in x.title })]
_json(result)
expect(_equal(result, ["rating": 7.0, "northern_dark_movie": "A Dark Movie"]))
