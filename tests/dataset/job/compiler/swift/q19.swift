// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
import Foundation

func _json(_ v: Any) {
    func _sort(_ x: Any) -> Any {
        if let a = x as? [Any] { return a.map { _sort($0) } }
        if let m = x as? [String:Any] {
            var out: [String:Any] = [:]
            for k in m.keys.sorted() { out[k] = _sort(m[k]!) }
            return out
        }
        return x
    }
    let obj = _sort(v)
    if JSONSerialization.isValidJSONObject(obj),
       let data = try? JSONSerialization.data(withJSONObject: obj, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        print(s)
    } else {
        print(obj)
    }
}
func expect(_ cond: Bool) {
    if !cond { fatalError("expect failed") }
}
func _structMap(_ v: Any) -> [String:Any]? {
    let mirror = Mirror(reflecting: v)
    if mirror.displayStyle == .struct || mirror.displayStyle == .class {
        var m: [String:Any] = [:]
        for child in mirror.children {
            if let k = child.label { m[k] = child.value }
        }
        return m
    }
    return nil
}
func _equal(_ a: Any, _ b: Any) -> Bool {
    if let am = _structMap(a), let bm = _structMap(b) {
        return _equal(am, bm)
    }
    if let am = _structMap(a), let bd = b as? [String: Any] {
        return _equal(am, bd)
    }
    if let ad = a as? [String: Any], let bm = _structMap(b) {
        return _equal(ad, bm)
    }
    switch (a, b) {
    case let (x as [Any], y as [Any]):
        if x.count != y.count { return false }
        for i in 0..<x.count {
            if !_equal(x[i], y[i]) { return false }
        }
        return true
    case let (x as [String: Any], y as [String: Any]):
        if x.count != y.count { return false }
        for (k, av) in x {
            guard let bv = y[k] else { return false }
            if !_equal(av, bv) { return false }
        }
        return true
    case let (ai as Double, bi as Int):
        return ai == Double(bi)
    case let (ai as Int, bi as Double):
        return Double(ai) == bi
    case let (ai as Double, bi as Double):
        return ai == bi
    case let (ai as Int, bi as Int):
        return ai == bi
    case let (sa as String, sb as String):
        return sa == sb
    case let (ab as Bool, bb as Bool):
        return ab == bb
    default:
        return false
    }
}
class _Group {
    var key: Any
    var Items: [Any] = []
    init(_ k: Any) { self.key = k }
}


func _keyStr(_ v: Any) -> String {
    if let data = try? JSONSerialization.data(withJSONObject: v, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        return s
    }
    return String(describing: v)
}
func _min(_ v: Any) -> Any {
    var list: [Any]? = nil
    if let g = v as? _Group { list = g.Items }
    else if let arr = v as? [Any] { list = arr }
    else if let arr = v as? [Int] { return arr.min() ?? 0 }
    else if let arr = v as? [Double] { return arr.min() ?? 0.0 }
    else if let arr = v as? [String] { return arr.min() ?? "" }
    guard let items = list else { fatalError("min() expects list or group") }
    if items.isEmpty { return 0 }
    if let s = items[0] as? String {
        var m = s
        for it in items.dropFirst() {
            if let v = it as? String, v < m { m = v }
        }
        return m
    }
    func toDouble(_ v: Any) -> Double {
        if let i = v as? Int { return Double(i) }
        if let d = v as? Double { return d }
        if let f = v as? Float { return Double(f) }
        if let i = v as? Int64 { return Double(i) }
        return 0
    }
    var m = toDouble(items[0])
    var isFloat = items[0] is Double || items[0] is Float
    for it in items.dropFirst() {
        if it is Double || it is Float { isFloat = true }
        let d = toDouble(it)
        if d < m { m = d }
    }
    return isFloat ? m : Int(m)
}
struct AkaName: Equatable {
    var name: String
    var person_id: Int
}

struct Auto12: Equatable {
    var voiced_movie: String
    var voicing_actress: String
}

struct CastInfo: Equatable {
    var movie_id: Int
    var note: String
    var person_id: Int
    var person_role_id: Int
    var role_id: Int
}

struct CharName: Equatable {
    var id: Int
    var name: String
}

struct CompanyName: Equatable {
    var country_code: String
    var id: Int
}

struct InfoType: Equatable {
    var id: Int
    var info: String
}

struct Matche: Equatable {
    var actress: String
    var movie: String
}

struct MovieCompany: Equatable {
    var company_id: Int
    var movie_id: Int
    var note: String
}

struct MovieInfo: Equatable {
    var info: String
    var info_type_id: Int
    var movie_id: Int
}

struct Name: Equatable {
    var gender: String
    var id: Int
    var name: String
}

struct RoleType: Equatable {
    var id: Int
    var role: String
}

struct Title: Equatable {
    var id: Int
    var production_year: Int
    var title: String
}

var aka_name = [AkaName(name: "A. Stone", person_id: 1), AkaName(name: "J. Doe", person_id: 2)]
var char_name = [CharName(id: 1, name: "Protagonist"), CharName(id: 2, name: "Extra")]
var cast_info = [CastInfo(movie_id: 1, note: "(voice)", person_id: 1, person_role_id: 1, role_id: 1), CastInfo(movie_id: 2, note: "Cameo", person_id: 2, person_role_id: 2, role_id: 2)]
var company_name = [CompanyName(country_code: "[us]", id: 10), CompanyName(country_code: "[gb]", id: 20)]
var info_type = [InfoType(id: 100, info: "release dates")]
var movie_companies = [MovieCompany(company_id: 10, movie_id: 1, note: "Studio (USA)"), MovieCompany(company_id: 20, movie_id: 2, note: "Other (worldwide)")]
var movie_info = [MovieInfo(info: "USA: June 2006", info_type_id: 100, movie_id: 1), MovieInfo(info: "UK: 1999", info_type_id: 100, movie_id: 2)]
var name = [Name(gender: "f", id: 1, name: "Angela Stone"), Name(gender: "m", id: 2, name: "Bob Angstrom")]
var role_type = [RoleType(id: 1, role: "actress"), RoleType(id: 2, role: "actor")]
var title = [Title(id: 1, production_year: 2006, title: "Voiced Movie"), Title(id: 2, production_year: 2010, title: "Other Movie")]
var matches = ({
	var _res: [(actress: String, movie: String)] = []
	for an in aka_name {
		for n in name {
			if !(n.id == an.person_id) { continue }
			for ci in cast_info {
				if !(ci.person_id == an.person_id) { continue }
				for chn in char_name {
					if !(chn.id == ci.person_role_id) { continue }
					for rt in role_type {
						if !(rt.id == ci.role_id) { continue }
						for t in title {
							if !(t.id == ci.movie_id) { continue }
							for mc in movie_companies {
								if !(mc.movie_id == t.id) { continue }
								for cn in company_name {
									if !(cn.id == mc.company_id) { continue }
									for mi in movie_info {
										if !(mi.movie_id == t.id) { continue }
										for it in info_type {
											if !(it.id == mi.info_type_id) { continue }
											if !(["(voice)", "(voice: Japanese version)", "(voice) (uncredited)", "(voice: English version)"].contains(ci.note) && cn.country_code == "[us]" && it.info == "release dates" && mc.note != nil && (mc.note.contains("(USA)") || mc.note.contains("(worldwide)")) && mi.info != nil && ((mi.info.contains("Japan:") && mi.info.contains("200")) || (mi.info.contains("USA:") && mi.info.contains("200"))) && n.gender == "f" && n.name.contains("Ang") && rt.role == "actress" && t.production_year >= 2005 && t.production_year <= 2009) { continue }
											_res.append(["actress": n.name, "movie": t.title])
										}
									}
								}
							}
						}
					}
				}
			}
		}
	}
	return _res
}())
var result = [["voicing_actress": _min(matches.map { r in r.actress }), "voiced_movie": _min(matches.map { r in r.movie })]]
_json(result)
expect(_equal(result, [Auto12(voiced_movie: "Voiced Movie", voicing_actress: "Angela Stone")]))
