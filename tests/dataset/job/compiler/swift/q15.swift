// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
import Foundation

func _json(_ v: Any) {
    func _sort(_ x: Any) -> Any {
        if let a = x as? [Any] { return a.map { _sort($0) } }
        if let m = x as? [String:Any] {
            var out: [String:Any] = [:]
            for k in m.keys.sorted() { out[k] = _sort(m[k]!) }
            return out
        }
        return x
    }
    let obj = _sort(v)
    if JSONSerialization.isValidJSONObject(obj),
       let data = try? JSONSerialization.data(withJSONObject: obj, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        print(s)
    } else {
        print(obj)
    }
}
func expect(_ cond: Bool) {
    if !cond { fatalError("expect failed") }
}
func _structMap(_ v: Any) -> [String:Any]? {
    let mirror = Mirror(reflecting: v)
    if mirror.displayStyle == .struct || mirror.displayStyle == .class {
        var m: [String:Any] = [:]
        for child in mirror.children {
            if let k = child.label { m[k] = child.value }
        }
        return m
    }
    return nil
}
func _equal(_ a: Any, _ b: Any) -> Bool {
    if let am = _structMap(a), let bm = _structMap(b) {
        return _equal(am, bm)
    }
    if let am = _structMap(a), let bd = b as? [String: Any] {
        return _equal(am, bd)
    }
    if let ad = a as? [String: Any], let bm = _structMap(b) {
        return _equal(ad, bm)
    }
    switch (a, b) {
    case let (x as [Any], y as [Any]):
        if x.count != y.count { return false }
        for i in 0..<x.count {
            if !_equal(x[i], y[i]) { return false }
        }
        return true
    case let (x as [String: Any], y as [String: Any]):
        if x.count != y.count { return false }
        for (k, av) in x {
            guard let bv = y[k] else { return false }
            if !_equal(av, bv) { return false }
        }
        return true
    case let (ai as Double, bi as Int):
        return ai == Double(bi)
    case let (ai as Int, bi as Double):
        return Double(ai) == bi
    case let (ai as Double, bi as Double):
        return ai == bi
    case let (ai as Int, bi as Int):
        return ai == bi
    case let (sa as String, sb as String):
        return sa == sb
    case let (ab as Bool, bb as Bool):
        return ab == bb
    default:
        return false
    }
}
class _Group {
    var key: Any
    var Items: [Any] = []
    init(_ k: Any) { self.key = k }
}


func _keyStr(_ v: Any) -> String {
    if let data = try? JSONSerialization.data(withJSONObject: v, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        return s
    }
    return String(describing: v)
}
func _min(_ v: Any) -> Any {
    var list: [Any]? = nil
    if let g = v as? _Group { list = g.Items }
    else if let arr = v as? [Any] { list = arr }
    else if let arr = v as? [Int] { return arr.min() ?? 0 }
    else if let arr = v as? [Double] { return arr.min() ?? 0.0 }
    else if let arr = v as? [String] { return arr.min() ?? "" }
    guard let items = list else { fatalError("min() expects list or group") }
    if items.isEmpty { return 0 }
    if let s = items[0] as? String {
        var m = s
        for it in items.dropFirst() {
            if let v = it as? String, v < m { m = v }
        }
        return m
    }
    func toDouble(_ v: Any) -> Double {
        if let i = v as? Int { return Double(i) }
        if let d = v as? Double { return d }
        if let f = v as? Float { return Double(f) }
        if let i = v as? Int64 { return Double(i) }
        return 0
    }
    var m = toDouble(items[0])
    var isFloat = items[0] is Double || items[0] is Float
    for it in items.dropFirst() {
        if it is Double || it is Float { isFloat = true }
        let d = toDouble(it)
        if d < m { m = d }
    }
    return isFloat ? m : Int(m)
}
struct AkaTitle: Equatable {
    var movie_id: Int
}

struct CompanyName: Equatable {
    var country_code: String
    var id: Int
}

struct CompanyType: Equatable {
    var id: Int
}

struct InfoType: Equatable {
    var id: Int
    var info: String
}

struct MovieCompany: Equatable {
    var company_id: Int
    var company_type_id: Int
    var movie_id: Int
    var note: String
}

struct MovieInfo: Equatable {
    var info: String
    var info_type_id: Int
    var movie_id: Int
    var note: String
}

struct MovieKeyword: Equatable {
    var keyword_id: Int
    var movie_id: Int
}

struct Row: Equatable {
    var internet_movie: String
    var release_date: String
}

struct Title: Equatable {
    var id: Int
    var production_year: Int
    var title: String
}

var aka_title = [AkaTitle(movie_id: 1), AkaTitle(movie_id: 2)]
var company_name = [CompanyName(country_code: "[us]", id: 1), CompanyName(country_code: "[gb]", id: 2)]
var company_type = [CompanyType(id: 10), CompanyType(id: 20)]
var info_type = [InfoType(id: 5, info: "release dates"), InfoType(id: 6, info: "other")]
var keyword = [CompanyType(id: 100), CompanyType(id: 200)]
var movie_companies = [MovieCompany(company_id: 1, company_type_id: 10, movie_id: 1, note: "release (2005) (worldwide)"), MovieCompany(company_id: 2, company_type_id: 20, movie_id: 2, note: "release (1999) (worldwide)")]
var movie_info = [MovieInfo(info: "USA: March 2005", info_type_id: 5, movie_id: 1, note: "internet"), MovieInfo(info: "USA: May 1999", info_type_id: 5, movie_id: 2, note: "theater")]
var movie_keyword = [MovieKeyword(keyword_id: 100, movie_id: 1), MovieKeyword(keyword_id: 200, movie_id: 2)]
var title = [Title(id: 1, production_year: 2005, title: "Example Movie"), Title(id: 2, production_year: 1999, title: "Old Movie")]
var rows = ({
	var _res: [(internet_movie: String, release_date: String)] = []
	for t in title {
		for at in aka_title {
			if !(at.movie_id == t.id) { continue }
			for mi in movie_info {
				if !(mi.movie_id == t.id) { continue }
				for mk in movie_keyword {
					if !(mk.movie_id == t.id) { continue }
					for mc in movie_companies {
						if !(mc.movie_id == t.id) { continue }
						for k in keyword {
							if !(k.id == mk.keyword_id) { continue }
							for it1 in info_type {
								if !(it1.id == mi.info_type_id) { continue }
								for cn in company_name {
									if !(cn.id == mc.company_id) { continue }
									for ct in company_type {
										if !(ct.id == mc.company_type_id) { continue }
										if !(cn.country_code == "[us]" && it1.info == "release dates" && mc.note.contains("200") && mc.note.contains("worldwide") && mi.note.contains("internet") && mi.info.contains("USA:") && mi.info.contains("200") && t.production_year > 2000) { continue }
										_res.append(["release_date": mi.info, "internet_movie": t.title])
									}
								}
							}
						}
					}
				}
			}
		}
	}
	return _res
}())
var result = [["release_date": _min(rows.map { r in r.release_date }), "internet_movie": _min(rows.map { r in r.internet_movie })]]
_json(result)
expect(_equal(result, [Row(internet_movie: "Example Movie", release_date: "USA: March 2005")]))
