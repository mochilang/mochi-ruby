// Generated by Mochi compiler v0.10.25 on 2025-07-13T13:02:44Z
fun String.starts_with(prefix: String): Boolean = this.startsWith(prefix)

fun json(v: Any?) {
    println(toJson(v))
}

fun toJson(v: Any?): String = when (v) {
    null -> "null"
    is String -> "\"" + v.replace("\"", "\\\"") + "\""
    is Boolean, is Number -> v.toString()
    is Map<*, *> -> v.entries.joinToString(prefix = "{", postfix = "}") { toJson(it.key.toString()) + ":" + toJson(it.value) }
    is Iterable<*> -> v.joinToString(prefix = "[", postfix = "]") { toJson(it) }
    else -> toJson(v.toString())
}
// Code generated from q6.mochi

data class Cast_info(var movie_id: Int, var person_id: Int)

data class Keyword(var id: Int, var keyword: String)

data class Movie_keyword(var movie_id: Int, var keyword_id: Int)

data class Name(var id: Int, var name: String)

data class Title(var id: Int, var title: String, var production_year: Int)

val cast_info = mutableListOf(Cast_info(movie_id = 1, person_id = 101), Cast_info(movie_id = 2, person_id = 102))

val keyword = mutableListOf(Keyword(id = 100, keyword = "marvel-cinematic-universe"), Keyword(id = 200, keyword = "other"))

val movie_keyword = mutableListOf(Movie_keyword(movie_id = 1, keyword_id = 100), Movie_keyword(movie_id = 2, keyword_id = 200))

val name = mutableListOf(Name(id = 101, name = "Downey Robert Jr."), Name(id = 102, name = "Chris Evans"))

val title = mutableListOf(Title(id = 1, title = "Iron Man 3", production_year = 2013), Title(id = 2, title = "Old Movie", production_year = 2000))

val result = run {
    val __res = mutableListOf<Any?>()
    for (ci in cast_info) {
        for (mk in movie_keyword) {
            if (ci.movie_id == mk.movie_id) {
                for (k in keyword) {
                    if (mk.keyword_id == k.id) {
                        for (n in name) {
                            if (ci.person_id == n.id) {
                                for (t in title) {
                                    if (ci.movie_id == t.id) {
                                        if (k.keyword == "marvel-cinematic-universe" && n.name.contains("Downey") && n.name.contains("Robert") && t.production_year > 2010) {
                                            __res.add(mutableMapOf("movie_keyword" to k.keyword, "actor_name" to n.name, "marvel_movie" to t.title))
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    __res
}

fun main() {
    json(result)
    check(result == mutableListOf(mutableMapOf("movie_keyword" to "marvel-cinematic-universe", "actor_name" to "Downey Robert Jr.", "marvel_movie" to "Iron Man 3")))
}
