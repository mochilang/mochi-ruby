// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:38:43Z
// Source: /workspace/mochi/tests/dataset/job/q25.mochi

let allowed_keywords: string[];
let allowed_notes: string[];
let cast_info: Record<string, any>[];
let info_type: Record<string, any>[];
let keyword: Record<string, any>[];
let matches: Record<string, any>[];
let movie_info: Record<string, any>[];
let movie_info_idx: { [key: string]: number }[];
let movie_keyword: { [key: string]: number }[];
let name: Record<string, any>[];
let result: Record<string, any>[];
let title: Record<string, any>[];

function test_Q25_finds_male_horror_writer_with_violent_keywords(): void {
  if (
    !(_equal(result, [
      {
        "movie_budget": "Horror",
        "movie_votes": 100,
        "male_writer": "Mike",
        "violent_movie_title": "Scary Movie",
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  cast_info = [
    {
      "movie_id": 1,
      "person_id": 1,
      "note": "(writer)",
    },
    {
      "movie_id": 2,
      "person_id": 2,
      "note": "(writer)",
    },
  ];
  info_type = [
    {
      "id": 1,
      "info": "genres",
    },
    {
      "id": 2,
      "info": "votes",
    },
  ];
  keyword = [
    {
      "id": 1,
      "keyword": "murder",
    },
    {
      "id": 2,
      "keyword": "romance",
    },
  ];
  movie_info = [
    {
      "movie_id": 1,
      "info_type_id": 1,
      "info": "Horror",
    },
    {
      "movie_id": 2,
      "info_type_id": 1,
      "info": "Comedy",
    },
  ];
  movie_info_idx = [
    {
      "movie_id": 1,
      "info_type_id": 2,
      "info": 100,
    },
    {
      "movie_id": 2,
      "info_type_id": 2,
      "info": 50,
    },
  ];
  movie_keyword = [
    {
      "movie_id": 1,
      "keyword_id": 1,
    },
    {
      "movie_id": 2,
      "keyword_id": 2,
    },
  ];
  name = [
    {
      "id": 1,
      "name": "Mike",
      "gender": "m",
    },
    {
      "id": 2,
      "name": "Sue",
      "gender": "f",
    },
  ];
  title = [
    {
      "id": 1,
      "title": "Scary Movie",
    },
    {
      "id": 2,
      "title": "Funny Movie",
    },
  ];
  allowed_notes = [
    "(writer)",
    "(head writer)",
    "(written by)",
    "(story)",
    "(story editor)",
  ];
  allowed_keywords = [
    "murder",
    "blood",
    "gore",
    "death",
    "female-nudity",
  ];
  matches = (() => {
    const _src = cast_info;
    const _res = [];
    for (const ci of _src) {
      for (const it1 of info_type) {
        for (const it2 of info_type) {
          for (const k of keyword) {
            for (const mi of movie_info) {
              for (const mi_idx of movie_info_idx) {
                for (const mk of movie_keyword) {
                  for (const n of name) {
                    for (const t of title) {
                      if (
                        !((((((((((((((((((((allowed_notes.includes(ci.note)) &&
                          (it1.info == "genres")) && (it2.info == "votes")) &&
                          (allowed_keywords.includes(k.keyword))) &&
                          (mi.info == "Horror")) && (n.gender == "m")) &&
                          (t.id == mi.movie_id)) &&
                          (t.id == mi_idx.movie_id)) &&
                          (t.id == ci.movie_id)) && (t.id == mk.movie_id)) &&
                          (ci.movie_id == mi.movie_id)) &&
                          (ci.movie_id == mi_idx.movie_id)) &&
                          (ci.movie_id == mk.movie_id)) &&
                          (mi.movie_id == mi_idx.movie_id)) &&
                          (mi.movie_id == mk.movie_id)) &&
                          (mi_idx.movie_id == mk.movie_id)) &&
                          (n.id == ci.person_id)) &&
                          (it1.id == mi.info_type_id)) &&
                          (it2.id == mi_idx.info_type_id)) &&
                          (k.id == mk.keyword_id))
                      ) continue;
                      _res.push({
                        "budget": mi.info,
                        "votes": mi_idx.info,
                        "writer": n.name,
                        "title": t.title,
                      });
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return _res;
  })();
  result = [
    {
      "movie_budget": _min(matches.map((x) => x.budget)),
      "movie_votes": _min(matches.map((x) => x.votes)),
      "male_writer": _min(matches.map((x) => x.writer)),
      "violent_movie_title": _min(matches.map((x) => x.title)),
    },
  ];
  console.log(_json(result));
  test_Q25_finds_male_horror_writer_with_violent_keywords();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v), null, 2);
}

function _min(v: any): any {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m: any = list[0];
  if (typeof m === "string") {
    for (const s of list) if (typeof s === "string" && s < m) m = s;
    return m;
  }
  let mv = Number(m);
  for (const n of list) {
    const num = Number(n);
    if (num < mv) mv = num;
  }
  return mv;
}

main();
