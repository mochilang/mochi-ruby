// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:38:43Z
// Source: /workspace/mochi/tests/dataset/job/q16.mochi

let aka_name: Record<string, any>[];
let cast_info: { [key: string]: number }[];
let company_name: Record<string, any>[];
let keyword: Record<string, any>[];
let movie_companies: { [key: string]: number }[];
let movie_keyword: { [key: string]: number }[];
let name: { [key: string]: number }[];
let result: Record<string, any>[];
let rows: Record<string, any>[];
let title: Record<string, any>[];

function test_Q16_finds_series_named_after_a_character_between_episodes_50_and_99(): void {
  if (
    !(_equal(result, [
      {
        "cool_actor_pseudonym": "Alpha",
        "series_named_after_char": "Hero Bob",
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  aka_name = [
    {
      "person_id": 1,
      "name": "Alpha",
    },
    {
      "person_id": 2,
      "name": "Beta",
    },
  ];
  cast_info = [
    {
      "person_id": 1,
      "movie_id": 101,
    },
    {
      "person_id": 2,
      "movie_id": 102,
    },
  ];
  company_name = [
    {
      "id": 1,
      "country_code": "[us]",
    },
    {
      "id": 2,
      "country_code": "[de]",
    },
  ];
  keyword = [
    {
      "id": 1,
      "keyword": "character-name-in-title",
    },
    {
      "id": 2,
      "keyword": "other",
    },
  ];
  movie_companies = [
    {
      "movie_id": 101,
      "company_id": 1,
    },
    {
      "movie_id": 102,
      "company_id": 2,
    },
  ];
  movie_keyword = [
    {
      "movie_id": 101,
      "keyword_id": 1,
    },
    {
      "movie_id": 102,
      "keyword_id": 2,
    },
  ];
  name = [
    { "id": 1 },
    { "id": 2 },
  ];
  title = [
    {
      "id": 101,
      "title": "Hero Bob",
      "episode_nr": 60,
    },
    {
      "id": 102,
      "title": "Other Show",
      "episode_nr": 40,
    },
  ];
  rows = (() => {
    const _src = aka_name;
    const _res = [];
    for (const an of _src) {
      for (const n of name) {
        if (!(n.id == an.person_id)) continue;
        for (const ci of cast_info) {
          if (!(ci.person_id == n.id)) continue;
          for (const t of title) {
            if (!(t.id == ci.movie_id)) continue;
            for (const mk of movie_keyword) {
              if (!(mk.movie_id == t.id)) continue;
              for (const k of keyword) {
                if (!(k.id == mk.keyword_id)) continue;
                for (const mc of movie_companies) {
                  if (!(mc.movie_id == t.id)) continue;
                  for (const cn of company_name) {
                    if (!(cn.id == mc.company_id)) continue;
                    if (
                      !((((cn.country_code == "[us]") &&
                        (k.keyword == "character-name-in-title")) &&
                        (t.episode_nr >= 50)) && (t.episode_nr < 100))
                    ) continue;
                    _res.push({
                      "pseudonym": an.name,
                      "series": t.title,
                    });
                  }
                }
              }
            }
          }
        }
      }
    }
    return _res;
  })();
  result = [
    {
      "cool_actor_pseudonym": _min(rows.map((r) => r.pseudonym)),
      "series_named_after_char": _min(rows.map((r) => r.series)),
    },
  ];
  console.log(_json(result));
  test_Q16_finds_series_named_after_a_character_between_episodes_50_and_99();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v), null, 2);
}

function _min(v: any): any {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m: any = list[0];
  if (typeof m === "string") {
    for (const s of list) if (typeof s === "string" && s < m) m = s;
    return m;
  }
  let mv = Number(m);
  for (const n of list) {
    const num = Number(n);
    if (num < mv) mv = num;
  }
  return mv;
}

main();
