// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:38:43Z
// Source: /workspace/mochi/tests/dataset/job/q19.mochi

let aka_name: Record<string, any>[];
let cast_info: Record<string, any>[];
let char_name: Record<string, any>[];
let company_name: Record<string, any>[];
let info_type: Record<string, any>[];
let matches: Record<string, any>[];
let movie_companies: Record<string, any>[];
let movie_info: Record<string, any>[];
let name: Record<string, any>[];
let result: Record<string, any>[];
let role_type: Record<string, any>[];
let title: Record<string, any>[];

function test_Q19_finds_female_voice_actress_in_US_Japan_release_between_2005_and_2009(): void {
  if (
    !(_equal(result, [
      {
        "voicing_actress": "Angela Stone",
        "voiced_movie": "Voiced Movie",
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  aka_name = [
    {
      "person_id": 1,
      "name": "A. Stone",
    },
    {
      "person_id": 2,
      "name": "J. Doe",
    },
  ];
  char_name = [
    {
      "id": 1,
      "name": "Protagonist",
    },
    {
      "id": 2,
      "name": "Extra",
    },
  ];
  cast_info = [
    {
      "movie_id": 1,
      "person_role_id": 1,
      "person_id": 1,
      "role_id": 1,
      "note": "(voice)",
    },
    {
      "movie_id": 2,
      "person_role_id": 2,
      "person_id": 2,
      "role_id": 2,
      "note": "Cameo",
    },
  ];
  company_name = [
    {
      "id": 10,
      "country_code": "[us]",
    },
    {
      "id": 20,
      "country_code": "[gb]",
    },
  ];
  info_type = [
    {
      "id": 100,
      "info": "release dates",
    },
  ];
  movie_companies = [
    {
      "movie_id": 1,
      "company_id": 10,
      "note": "Studio (USA)",
    },
    {
      "movie_id": 2,
      "company_id": 20,
      "note": "Other (worldwide)",
    },
  ];
  movie_info = [
    {
      "movie_id": 1,
      "info_type_id": 100,
      "info": "USA: June 2006",
    },
    {
      "movie_id": 2,
      "info_type_id": 100,
      "info": "UK: 1999",
    },
  ];
  name = [
    {
      "id": 1,
      "name": "Angela Stone",
      "gender": "f",
    },
    {
      "id": 2,
      "name": "Bob Angstrom",
      "gender": "m",
    },
  ];
  role_type = [
    {
      "id": 1,
      "role": "actress",
    },
    {
      "id": 2,
      "role": "actor",
    },
  ];
  title = [
    {
      "id": 1,
      "title": "Voiced Movie",
      "production_year": 2006,
    },
    {
      "id": 2,
      "title": "Other Movie",
      "production_year": 2010,
    },
  ];
  matches = (() => {
    const _src = aka_name;
    const _res = [];
    for (const an of _src) {
      for (const n of name) {
        if (!(n.id == an.person_id)) continue;
        for (const ci of cast_info) {
          if (!(ci.person_id == an.person_id)) continue;
          for (const chn of char_name) {
            if (!(chn.id == ci.person_role_id)) continue;
            for (const rt of role_type) {
              if (!(rt.id == ci.role_id)) continue;
              for (const t of title) {
                if (!(t.id == ci.movie_id)) continue;
                for (const mc of movie_companies) {
                  if (!(mc.movie_id == t.id)) continue;
                  for (const cn of company_name) {
                    if (!(cn.id == mc.company_id)) continue;
                    for (const mi of movie_info) {
                      if (!(mi.movie_id == t.id)) continue;
                      for (const it of info_type) {
                        if (!(it.id == mi.info_type_id)) continue;
                        if (
                          !((((((((((([
                            "(voice)",
                            "(voice: Japanese version)",
                            "(voice) (uncredited)",
                            "(voice: English version)",
                          ].includes(ci.note) && (cn.country_code == "[us]")) &&
                            (it.info == "release dates")) &&
                            (mc.note != null)) &&
                            (mc.note.includes("(USA)") ||
                              mc.note.includes("(worldwide)"))) &&
                            (mi.info != null)) &&
                            ((mi.info.includes("Japan:") &&
                              mi.info.includes("200")) ||
                              (mi.info.includes("USA:") &&
                                mi.info.includes("200")))) &&
                            (n.gender == "f")) && n.name.includes("Ang")) &&
                            (rt.role == "actress")) &&
                            (t.production_year >= 2005)) &&
                            (t.production_year <= 2009))
                        ) continue;
                        _res.push({
                          "actress": n.name,
                          "movie": t.title,
                        });
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return _res;
  })();
  result = [
    {
      "voicing_actress": _min(matches.map((r) => r.actress)),
      "voiced_movie": _min(matches.map((r) => r.movie)),
    },
  ];
  console.log(_json(result));
  test_Q19_finds_female_voice_actress_in_US_Japan_release_between_2005_and_2009();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v), null, 2);
}

function _min(v: any): any {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m: any = list[0];
  if (typeof m === "string") {
    for (const s of list) if (typeof s === "string" && s < m) m = s;
    return m;
  }
  let mv = Number(m);
  for (const n of list) {
    const num = Number(n);
    if (num < mv) mv = num;
  }
  return mv;
}

main();
