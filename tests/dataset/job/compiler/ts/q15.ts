// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:38:43Z
// Source: /workspace/mochi/tests/dataset/job/q15.mochi

let aka_title: { [key: string]: number }[];
let company_name: Record<string, any>[];
let company_type: { [key: string]: number }[];
let info_type: Record<string, any>[];
let keyword: { [key: string]: number }[];
let movie_companies: Record<string, any>[];
let movie_info: Record<string, any>[];
let movie_keyword: { [key: string]: number }[];
let result: Record<string, any>[];
let rows: Record<string, any>[];
let title: Record<string, any>[];

function test_Q15_finds_the_earliest_US_internet_movie_release_after_2000(): void {
  if (
    !(_equal(result, [
      {
        "release_date": "USA: March 2005",
        "internet_movie": "Example Movie",
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  aka_title = [
    { "movie_id": 1 },
    { "movie_id": 2 },
  ];
  company_name = [
    {
      "id": 1,
      "country_code": "[us]",
    },
    {
      "id": 2,
      "country_code": "[gb]",
    },
  ];
  company_type = [
    { "id": 10 },
    { "id": 20 },
  ];
  info_type = [
    {
      "id": 5,
      "info": "release dates",
    },
    {
      "id": 6,
      "info": "other",
    },
  ];
  keyword = [
    { "id": 100 },
    { "id": 200 },
  ];
  movie_companies = [
    {
      "movie_id": 1,
      "company_id": 1,
      "company_type_id": 10,
      "note": "release (2005) (worldwide)",
    },
    {
      "movie_id": 2,
      "company_id": 2,
      "company_type_id": 20,
      "note": "release (1999) (worldwide)",
    },
  ];
  movie_info = [
    {
      "movie_id": 1,
      "info_type_id": 5,
      "note": "internet",
      "info": "USA: March 2005",
    },
    {
      "movie_id": 2,
      "info_type_id": 5,
      "note": "theater",
      "info": "USA: May 1999",
    },
  ];
  movie_keyword = [
    {
      "movie_id": 1,
      "keyword_id": 100,
    },
    {
      "movie_id": 2,
      "keyword_id": 200,
    },
  ];
  title = [
    {
      "id": 1,
      "title": "Example Movie",
      "production_year": 2005,
    },
    {
      "id": 2,
      "title": "Old Movie",
      "production_year": 1999,
    },
  ];
  rows = (() => {
    const _src = title;
    const _res = [];
    for (const t of _src) {
      for (const at of aka_title) {
        if (!(at.movie_id == t.id)) continue;
        for (const mi of movie_info) {
          if (!(mi.movie_id == t.id)) continue;
          for (const mk of movie_keyword) {
            if (!(mk.movie_id == t.id)) continue;
            for (const mc of movie_companies) {
              if (!(mc.movie_id == t.id)) continue;
              for (const k of keyword) {
                if (!(k.id == mk.keyword_id)) continue;
                for (const it1 of info_type) {
                  if (!(it1.id == mi.info_type_id)) continue;
                  for (const cn of company_name) {
                    if (!(cn.id == mc.company_id)) continue;
                    for (const ct of company_type) {
                      if (!(ct.id == mc.company_type_id)) continue;
                      if (
                        !((((((((cn.country_code == "[us]") &&
                          (it1.info == "release dates")) &&
                          mc.note.includes("200")) &&
                          mc.note.includes("worldwide")) &&
                          mi.note.includes("internet")) &&
                          mi.info.includes("USA:")) &&
                          mi.info.includes("200")) &&
                          (t.production_year > 2000))
                      ) continue;
                      _res.push({
                        "release_date": mi.info,
                        "internet_movie": t.title,
                      });
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
    return _res;
  })();
  result = [
    {
      "release_date": _min(rows.map((r) => r.release_date)),
      "internet_movie": _min(rows.map((r) => r.internet_movie)),
    },
  ];
  console.log(_json(result));
  test_Q15_finds_the_earliest_US_internet_movie_release_after_2000();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v), null, 2);
}

function _min(v: any): any {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m: any = list[0];
  if (typeof m === "string") {
    for (const s of list) if (typeof s === "string" && s < m) m = s;
    return m;
  }
  let mv = Number(m);
  for (const n of list) {
    const num = Number(n);
    if (num < mv) mv = num;
  }
  return mv;
}

main();
