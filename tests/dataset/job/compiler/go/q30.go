//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:06:47Z

package main

import (
	"encoding/json"
	"fmt"
	"reflect"
	"slices"
	"time"

	"golang.org/x/exp/constraints"
)

type Cast_info struct {
	Movie_id  int    `json:"movie_id"`
	Person_id int    `json:"person_id"`
	Note      string `json:"note"`
}

type Comp_cast_type struct {
	ID   int    `json:"id"`
	Kind string `json:"kind"`
}

type Complete_cast struct {
	Movie_id   int `json:"movie_id"`
	Subject_id int `json:"subject_id"`
	Status_id  int `json:"status_id"`
}

type Info_type struct {
	ID   int    `json:"id"`
	Info string `json:"info"`
}

type Keyword struct {
	ID      int    `json:"id"`
	Keyword string `json:"keyword"`
}

type Matche struct {
	Budget string `json:"budget"`
	Votes  int    `json:"votes"`
	Writer string `json:"writer"`
	Movie  string `json:"movie"`
}

type Movie_info struct {
	Movie_id     int    `json:"movie_id"`
	Info_type_id int    `json:"info_type_id"`
	Info         string `json:"info"`
}

type Movie_info_idx struct {
	Movie_id     int `json:"movie_id"`
	Info_type_id int `json:"info_type_id"`
	Info         int `json:"info"`
}

type Movie_keyword struct {
	Movie_id   int `json:"movie_id"`
	Keyword_id int `json:"keyword_id"`
}

type Name struct {
	ID     int    `json:"id"`
	Name   string `json:"name"`
	Gender string `json:"gender"`
}

type Result struct {
	Movie_budget           string `json:"movie_budget"`
	Movie_votes            int    `json:"movie_votes"`
	Writer                 string `json:"writer"`
	Complete_violent_movie string `json:"complete_violent_movie"`
}

type Title struct {
	ID              int    `json:"id"`
	Title           string `json:"title"`
	Production_year int    `json:"production_year"`
}

type v = Result

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q30_finds_violent_horror_thriller_movies_with_male_writer() {
	expect(_equal(result, []v{v{
		Movie_budget:           "Horror",
		Movie_votes:            2000,
		Writer:                 "John Writer",
		Complete_violent_movie: "Violent Horror",
	}}))
}

var comp_cast_type []Comp_cast_type
var complete_cast []Complete_cast
var cast_info []Cast_info
var info_type []Info_type
var keyword []Keyword
var movie_info []Movie_info
var movie_info_idx []Movie_info_idx
var movie_keyword []Movie_keyword
var name []Name
var title []Title
var violent_keywords []string
var writer_notes []string
var matches []Matche
var result []Result

func main() {
	comp_cast_type = []Comp_cast_type{Comp_cast_type{
		ID:   1,
		Kind: "cast",
	}, Comp_cast_type{
		ID:   2,
		Kind: "complete+verified",
	}, Comp_cast_type{
		ID:   3,
		Kind: "crew",
	}}
	complete_cast = []Complete_cast{Complete_cast{
		Movie_id:   1,
		Subject_id: 1,
		Status_id:  2,
	}, Complete_cast{
		Movie_id:   2,
		Subject_id: 3,
		Status_id:  2,
	}}
	cast_info = []Cast_info{Cast_info{
		Movie_id:  1,
		Person_id: 10,
		Note:      "(writer)",
	}, Cast_info{
		Movie_id:  2,
		Person_id: 11,
		Note:      "(actor)",
	}}
	info_type = []Info_type{Info_type{
		ID:   1,
		Info: "genres",
	}, Info_type{
		ID:   2,
		Info: "votes",
	}}
	keyword = []Keyword{Keyword{
		ID:      1,
		Keyword: "murder",
	}, Keyword{
		ID:      2,
		Keyword: "comedy",
	}}
	movie_info = []Movie_info{Movie_info{
		Movie_id:     1,
		Info_type_id: 1,
		Info:         "Horror",
	}, Movie_info{
		Movie_id:     2,
		Info_type_id: 1,
		Info:         "Comedy",
	}}
	movie_info_idx = []Movie_info_idx{Movie_info_idx{
		Movie_id:     1,
		Info_type_id: 2,
		Info:         2000,
	}, Movie_info_idx{
		Movie_id:     2,
		Info_type_id: 2,
		Info:         150,
	}}
	movie_keyword = []Movie_keyword{Movie_keyword{
		Movie_id:   1,
		Keyword_id: 1,
	}, Movie_keyword{
		Movie_id:   2,
		Keyword_id: 2,
	}}
	name = []Name{Name{
		ID:     10,
		Name:   "John Writer",
		Gender: "m",
	}, Name{
		ID:     11,
		Name:   "Jane Actor",
		Gender: "f",
	}}
	title = []Title{Title{
		ID:              1,
		Title:           "Violent Horror",
		Production_year: 2005,
	}, Title{
		ID:              2,
		Title:           "Old Comedy",
		Production_year: 1995,
	}}
	violent_keywords = []string{
		"murder",
		"violence",
		"blood",
		"gore",
		"death",
		"female-nudity",
		"hospital",
	}
	writer_notes = []string{
		"(writer)",
		"(head writer)",
		"(written by)",
		"(story)",
		"(story editor)",
	}
	matches = func() []Matche {
		results := []Matche{}
		for _, ccRaw := range complete_cast {
			cc := ccRaw.(Complete_cast)
			for _, cct1 := range comp_cast_type {
				if !(cct1.ID == cc.Subject_id) {
					continue
				}
				for _, cct2 := range comp_cast_type {
					if !(cct2.ID == cc.Status_id) {
						continue
					}
					for _, ci := range cast_info {
						if !(ci.Movie_id == cc.Movie_id) {
							continue
						}
						for _, mi := range movie_info {
							if !(mi.Movie_id == cc.Movie_id) {
								continue
							}
							for _, mi_idx := range movie_info_idx {
								if !(mi_idx.Movie_id == cc.Movie_id) {
									continue
								}
								for _, mk := range movie_keyword {
									if !(mk.Movie_id == cc.Movie_id) {
										continue
									}
									for _, it1 := range info_type {
										if !(it1.ID == mi.Info_type_id) {
											continue
										}
										for _, it2 := range info_type {
											if !(it2.ID == mi_idx.Info_type_id) {
												continue
											}
											for _, k := range keyword {
												if !(k.ID == mk.Keyword_id) {
													continue
												}
												for _, n := range name {
													if !(n.ID == ci.Person_id) {
														continue
													}
													for _, t := range title {
														if !(t.ID == cc.Movie_id) {
															continue
														}
														if ((((((((slices.Contains([]string{"cast", "crew"}, cct1.Kind)) && (cct2.Kind == "complete+verified")) && (slices.Contains(writer_notes, ci.Note))) && (it1.Info == "genres")) && (it2.Info == "votes")) && (slices.Contains(violent_keywords, k.Keyword))) && (slices.Contains([]string{"Horror", "Thriller"}, mi.Info))) && (n.Gender == "m")) && (t.Production_year > 2000) {
															if ((((((((slices.Contains([]string{"cast", "crew"}, cct1.Kind)) && (cct2.Kind == "complete+verified")) && (slices.Contains(writer_notes, ci.Note))) && (it1.Info == "genres")) && (it2.Info == "votes")) && (slices.Contains(violent_keywords, k.Keyword))) && (slices.Contains([]string{"Horror", "Thriller"}, mi.Info))) && (n.Gender == "m")) && (t.Production_year > 2000) {
																results = append(results, Matche{
																	Budget: mi.Info,
																	Votes:  mi_idx.Info,
																	Writer: n.Name,
																	Movie:  t.Title,
																})
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
				}
			}
		}
		return results
	}()
	result = []Result{Result{
		Movie_budget: _minOrdered[string](func() []string {
			results := []string{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Budget)
			}
			return results
		}()),
		Movie_votes: _minOrdered[int](func() []int {
			results := []int{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Votes)
			}
			return results
		}()),
		Writer: _minOrdered[string](func() []string {
			results := []string{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Writer)
			}
			return results
		}()),
		Complete_violent_movie: _minOrdered[string](func() []string {
			results := []string{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Movie)
			}
			return results
		}()),
	}}
	func() { b, _ := json.Marshal(result); fmt.Println(string(b)) }()
	test_Q30_finds_violent_horror_thriller_movies_with_male_writer()
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _minOrdered[T constraints.Ordered](s []T) T {
	if len(s) == 0 {
		var zero T
		return zero
	}
	m := s[0]
	for _, v := range s[1:] {
		if v < m {
			m = v
		}
	}
	return m
}
