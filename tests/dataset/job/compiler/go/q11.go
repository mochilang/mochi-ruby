//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:06:45Z

package main

import (
	"encoding/json"
	"fmt"
	"reflect"
	"strings"
	"time"

	"golang.org/x/exp/constraints"
)

type Company_name struct {
	ID           int    `json:"id"`
	Name         string `json:"name"`
	Country_code string `json:"country_code"`
}

type Company_type struct {
	ID   int    `json:"id"`
	Kind string `json:"kind"`
}

type Keyword struct {
	ID      int    `json:"id"`
	Keyword string `json:"keyword"`
}

type Link_type struct {
	ID   int    `json:"id"`
	Link string `json:"link"`
}

type Matche struct {
	Company string `json:"company"`
	Link    string `json:"link"`
	Title   string `json:"title"`
}

type Movie_companie struct {
	Movie_id        int `json:"movie_id"`
	Company_id      int `json:"company_id"`
	Company_type_id int `json:"company_type_id"`
	Note            any `json:"note"`
}

type Movie_keyword struct {
	Movie_id   int `json:"movie_id"`
	Keyword_id int `json:"keyword_id"`
}

type Movie_link struct {
	Movie_id     int `json:"movie_id"`
	Link_type_id int `json:"link_type_id"`
}

type Result struct {
	From_company            string `json:"from_company"`
	Movie_link_type         string `json:"movie_link_type"`
	Non_polish_sequel_movie string `json:"non_polish_sequel_movie"`
}

type Title struct {
	ID              int    `json:"id"`
	Production_year int    `json:"production_year"`
	Title           string `json:"title"`
}

type v = Result

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q11_returns_min_company__link_type_and_title() {
	expect(_equal(result, []v{v{
		From_company:            "Best Film Co",
		Movie_link_type:         "follow-up",
		Non_polish_sequel_movie: "Alpha",
	}}))
}

var company_name []Company_name
var company_type []Company_type
var keyword []Keyword
var link_type []Link_type
var movie_companies []Movie_companie
var movie_keyword []Movie_keyword
var movie_link []Movie_link
var title []Title
var matches []Matche
var result []Result

func main() {
	company_name = []Company_name{Company_name{
		ID:           1,
		Name:         "Best Film Co",
		Country_code: "[us]",
	}, Company_name{
		ID:           2,
		Name:         "Warner Studios",
		Country_code: "[de]",
	}, Company_name{
		ID:           3,
		Name:         "Polish Films",
		Country_code: "[pl]",
	}}
	company_type = []Company_type{Company_type{
		ID:   1,
		Kind: "production companies",
	}, Company_type{
		ID:   2,
		Kind: "distributors",
	}}
	keyword = []Keyword{Keyword{
		ID:      1,
		Keyword: "sequel",
	}, Keyword{
		ID:      2,
		Keyword: "thriller",
	}}
	link_type = []Link_type{Link_type{
		ID:   1,
		Link: "follow-up",
	}, Link_type{
		ID:   2,
		Link: "follows from",
	}, Link_type{
		ID:   3,
		Link: "remake",
	}}
	movie_companies = []Movie_companie{Movie_companie{
		Movie_id:        10,
		Company_id:      1,
		Company_type_id: 1,
		Note:            nil,
	}, Movie_companie{
		Movie_id:        20,
		Company_id:      2,
		Company_type_id: 1,
		Note:            nil,
	}, Movie_companie{
		Movie_id:        30,
		Company_id:      3,
		Company_type_id: 1,
		Note:            nil,
	}}
	movie_keyword = []Movie_keyword{
		Movie_keyword{
			Movie_id:   10,
			Keyword_id: 1,
		},
		Movie_keyword{
			Movie_id:   20,
			Keyword_id: 1,
		},
		Movie_keyword{
			Movie_id:   20,
			Keyword_id: 2,
		},
		Movie_keyword{
			Movie_id:   30,
			Keyword_id: 1,
		},
	}
	movie_link = []Movie_link{Movie_link{
		Movie_id:     10,
		Link_type_id: 1,
	}, Movie_link{
		Movie_id:     20,
		Link_type_id: 2,
	}, Movie_link{
		Movie_id:     30,
		Link_type_id: 3,
	}}
	title = []Title{Title{
		ID:              10,
		Production_year: 1960,
		Title:           "Alpha",
	}, Title{
		ID:              20,
		Production_year: 1970,
		Title:           "Beta",
	}, Title{
		ID:              30,
		Production_year: 1985,
		Title:           "Polish Movie",
	}}
	matches = func() []Matche {
		results := []Matche{}
		for _, cnRaw := range company_name {
			cn := cnRaw.(Company_name)
			for _, mc := range movie_companies {
				if !(mc.Company_id == cn.ID) {
					continue
				}
				for _, ct := range company_type {
					if !(ct.ID == mc.Company_type_id) {
						continue
					}
					for _, t := range title {
						if !(t.ID == mc.Movie_id) {
							continue
						}
						for _, mk := range movie_keyword {
							if !(mk.Movie_id == t.ID) {
								continue
							}
							for _, k := range keyword {
								if !(k.ID == mk.Keyword_id) {
									continue
								}
								for _, ml := range movie_link {
									if !(ml.Movie_id == t.ID) {
										continue
									}
									for _, lt := range link_type {
										if !(lt.ID == ml.Link_type_id) {
											continue
										}
										if ((((((((((cn.Country_code != "[pl]") && (strings.Contains(cn.Name, "Film") || strings.Contains(cn.Name, "Warner"))) && (ct.Kind == "production companies")) && (k.Keyword == "sequel")) && strings.Contains(lt.Link, "follow")) && _equal(mc.Note, nil)) && (t.Production_year >= 1950)) && (t.Production_year <= 2000)) && (ml.Movie_id == mk.Movie_id)) && (ml.Movie_id == mc.Movie_id)) && (mk.Movie_id == mc.Movie_id) {
											if ((((((((((cn.Country_code != "[pl]") && (strings.Contains(cn.Name, "Film") || strings.Contains(cn.Name, "Warner"))) && (ct.Kind == "production companies")) && (k.Keyword == "sequel")) && strings.Contains(lt.Link, "follow")) && _equal(mc.Note, nil)) && (t.Production_year >= 1950)) && (t.Production_year <= 2000)) && (ml.Movie_id == mk.Movie_id)) && (ml.Movie_id == mc.Movie_id)) && (mk.Movie_id == mc.Movie_id) {
												results = append(results, Matche{
													Company: cn.Name,
													Link:    lt.Link,
													Title:   t.Title,
												})
											}
										}
									}
								}
							}
						}
					}
				}
			}
		}
		return results
	}()
	result = []Result{Result{
		From_company: _minOrdered[string](func() []string {
			results := []string{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Company)
			}
			return results
		}()),
		Movie_link_type: _minOrdered[string](func() []string {
			results := []string{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Link)
			}
			return results
		}()),
		Non_polish_sequel_movie: _minOrdered[string](func() []string {
			results := []string{}
			for _, xRaw := range matches {
				x := xRaw.(Matche)
				results = append(results, x.Title)
			}
			return results
		}()),
	}}
	func() { b, _ := json.Marshal(result); fmt.Println(string(b)) }()
	test_Q11_returns_min_company__link_type_and_title()
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _minOrdered[T constraints.Ordered](s []T) T {
	if len(s) == 0 {
		var zero T
		return zero
	}
	m := s[0]
	for _, v := range s[1:] {
		if v < m {
			m = v
		}
	}
	return m
}
