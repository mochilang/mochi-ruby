//go:build ignore

// Generated by Mochi compiler v0.10.25 on 2025-07-15T03:06:46Z

package main

import (
	"encoding/json"
	"fmt"
	"reflect"
	"strings"
	"time"

	"golang.org/x/exp/constraints"
)

type Aka_title struct {
	Movie_id int `json:"movie_id"`
}

type Company_name struct {
	ID           int    `json:"id"`
	Country_code string `json:"country_code"`
}

type Company_type struct {
	ID int `json:"id"`
}

type Info_type struct {
	ID   int    `json:"id"`
	Info string `json:"info"`
}

type Keyword struct {
	ID int `json:"id"`
}

type Movie_companie struct {
	Movie_id        int    `json:"movie_id"`
	Company_id      int    `json:"company_id"`
	Company_type_id int    `json:"company_type_id"`
	Note            string `json:"note"`
}

type Movie_info struct {
	Movie_id     int    `json:"movie_id"`
	Info_type_id int    `json:"info_type_id"`
	Note         string `json:"note"`
	Info         string `json:"info"`
}

type Movie_keyword struct {
	Movie_id   int `json:"movie_id"`
	Keyword_id int `json:"keyword_id"`
}

type Result struct {
	Release_date   string `json:"release_date"`
	Internet_movie string `json:"internet_movie"`
}

type Row struct {
	Release_date   string `json:"release_date"`
	Internet_movie string `json:"internet_movie"`
}

type Title struct {
	ID              int    `json:"id"`
	Title           string `json:"title"`
	Production_year int    `json:"production_year"`
}

type v = Result

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q15_finds_the_earliest_US_internet_movie_release_after_2000() {
	expect(_equal(result, []v{v{
		Release_date:   "USA: March 2005",
		Internet_movie: "Example Movie",
	}}))
}

var aka_title []Aka_title
var company_name []Company_name
var company_type []Company_type
var info_type []Info_type
var keyword []Keyword
var movie_companies []Movie_companie
var movie_info []Movie_info
var movie_keyword []Movie_keyword
var title []Title
var rows []Row
var result []Result

func main() {
	aka_title = []Aka_title{Aka_title{Movie_id: 1}, Aka_title{Movie_id: 2}}
	company_name = []Company_name{Company_name{
		ID:           1,
		Country_code: "[us]",
	}, Company_name{
		ID:           2,
		Country_code: "[gb]",
	}}
	company_type = []Company_type{Company_type{ID: 10}, Company_type{ID: 20}}
	info_type = []Info_type{Info_type{
		ID:   5,
		Info: "release dates",
	}, Info_type{
		ID:   6,
		Info: "other",
	}}
	keyword = []Keyword{Keyword{ID: 100}, Keyword{ID: 200}}
	movie_companies = []Movie_companie{Movie_companie{
		Movie_id:        1,
		Company_id:      1,
		Company_type_id: 10,
		Note:            "release (2005) (worldwide)",
	}, Movie_companie{
		Movie_id:        2,
		Company_id:      2,
		Company_type_id: 20,
		Note:            "release (1999) (worldwide)",
	}}
	movie_info = []Movie_info{Movie_info{
		Movie_id:     1,
		Info_type_id: 5,
		Note:         "internet",
		Info:         "USA: March 2005",
	}, Movie_info{
		Movie_id:     2,
		Info_type_id: 5,
		Note:         "theater",
		Info:         "USA: May 1999",
	}}
	movie_keyword = []Movie_keyword{Movie_keyword{
		Movie_id:   1,
		Keyword_id: 100,
	}, Movie_keyword{
		Movie_id:   2,
		Keyword_id: 200,
	}}
	title = []Title{Title{
		ID:              1,
		Title:           "Example Movie",
		Production_year: 2005,
	}, Title{
		ID:              2,
		Title:           "Old Movie",
		Production_year: 1999,
	}}
	rows = func() []Row {
		results := []Row{}
		for _, tRaw := range title {
			t := tRaw.(Title)
			for _, at := range aka_title {
				if !(at.Movie_id == t.ID) {
					continue
				}
				for _, mi := range movie_info {
					if !(mi.Movie_id == t.ID) {
						continue
					}
					for _, mk := range movie_keyword {
						if !(mk.Movie_id == t.ID) {
							continue
						}
						for _, mc := range movie_companies {
							if !(mc.Movie_id == t.ID) {
								continue
							}
							for _, k := range keyword {
								if !(k.ID == mk.Keyword_id) {
									continue
								}
								for _, it1 := range info_type {
									if !(it1.ID == mi.Info_type_id) {
										continue
									}
									for _, cn := range company_name {
										if !(cn.ID == mc.Company_id) {
											continue
										}
										if (((((((cn.Country_code == "[us]") && (it1.Info == "release dates")) && strings.Contains(mc.Note, "200")) && strings.Contains(mc.Note, "worldwide")) && strings.Contains(mi.Note, "internet")) && strings.Contains(mi.Info, "USA:")) && strings.Contains(mi.Info, "200")) && (t.Production_year > 2000) {
											for _, ct := range company_type {
												if !(ct.ID == mc.Company_type_id) {
													continue
												}
												results = append(results, Row{
													Release_date:   mi.Info,
													Internet_movie: t.Title,
												})
											}
										}
									}
								}
							}
						}
					}
				}
			}
		}
		return results
	}()
	result = []Result{Result{
		Release_date: _minOrdered[string](func() []string {
			results := []string{}
			for _, rRaw := range rows {
				r := rRaw.(Row)
				results = append(results, r.Release_date)
			}
			return results
		}()),
		Internet_movie: _minOrdered[string](func() []string {
			results := []string{}
			for _, rRaw := range rows {
				r := rRaw.(Row)
				results = append(results, r.Internet_movie)
			}
			return results
		}()),
	}}
	func() { b, _ := json.Marshal(result); fmt.Println(string(b)) }()
	test_Q15_finds_the_earliest_US_internet_movie_release_after_2000()
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _minOrdered[T constraints.Ordered](s []T) T {
	if len(s) == 0 {
		var zero T
		return zero
	}
	m := s[0]
	for _, v := range s[1:] {
		if v < m {
			m = v
		}
	}
	return m
}
