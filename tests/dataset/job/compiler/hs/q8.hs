-- Generated by Mochi compiler v0.10.25 on 2025-07-13T13:02:37Z
-- Code generated by Mochi compiler; DO NOT EDIT.
{-# LANGUAGE DeriveGeneric #-}

module Main where

import qualified Data.Aeson as Aeson
import qualified Data.ByteString.Lazy.Char8 as BSL
import qualified Data.Map as Map
import Data.Maybe (fromMaybe)

forLoop :: Int -> Int -> (Int -> Maybe a) -> Maybe a
forLoop start end f = go start
  where
    go i
      | i < end =
          case f i of
            Just v -> Just v
            Nothing -> go (i + 1)
      | otherwise = Nothing

whileLoop :: (() -> Bool) -> (() -> Maybe a) -> Maybe a
whileLoop cond body = go ()
  where
    go _
      | cond () =
          case body () of
            Just v -> Just v
            Nothing -> go ()
      | otherwise = Nothing

avg :: (Integral a) => [a] -> a
avg xs
  | null xs = 0
  | otherwise = div (sum xs) (fromIntegral (length xs))

data MGroup k a = MGroup {key :: k, items :: [a]} deriving (Show)

_group_by :: (Ord k) => [a] -> (a -> k) -> [MGroup k a]
_group_by src keyfn =
  let go [] m order = (m, order)
      go (x : xs) m order =
        let k = keyfn x
         in case Map.lookup k m of
              Just is -> go xs (Map.insert k (is ++ [x]) m) order
              Nothing -> go xs (Map.insert k [x] m) (order ++ [k])
      (m, order) = go src Map.empty []
   in [MGroup k (fromMaybe [] (Map.lookup k m)) | k <- order]

_indexString :: String -> Int -> String
_indexString s i =
  let idx = if i < 0 then i + length s else i
   in if idx < 0 || idx >= length s
        then error "index out of range"
        else [s !! idx]

_append :: [a] -> a -> [a]
_append xs x = xs ++ [x]

_input :: IO String
_input = getLine

_readInput :: Maybe String -> IO String
_readInput Nothing = getContents
_readInput (Just p)
  | null p || p == "-" = getContents
  | otherwise = readFile p

_writeOutput :: Maybe String -> String -> IO ()
_writeOutput mp text = case mp of
  Nothing -> putStr text
  Just p
    | null p || p == "-" -> putStr text
    | otherwise -> writeFile p text

_split :: Char -> String -> [String]
_split _ "" = [""]
_split d s =
  let (h, t) = break (== d) s
   in h : case t of
        [] -> []
        (_ : rest) -> _split d rest

_parseCSV :: String -> Bool -> Char -> [Map.Map String String]
_parseCSV text header delim =
  let ls = filter (not . null) (lines text)
   in if null ls
        then []
        else
          let heads =
                if header
                  then _split delim (head ls)
                  else ["c" ++ show i | i <- [0 .. length (_split delim (head ls)) - 1]]
              start = if header then 1 else 0
              row line =
                let parts = _split delim line
                 in Map.fromList
                      [ (heads !! j, if j < length parts then parts !! j else "")
                        | j <- [0 .. length heads - 1]
                      ]
           in map row (drop start ls)

_json :: (Aeson.ToJSON a) => a -> IO ()
_json v = BSL.putStrLn (Aeson.encode v)

expect :: Bool -> IO ()
expect True = pure ()
expect False = error "expect failed"

aka_name = [Map.fromList [("person_id", (1 :: Int)), ("name", "Y. S.")]]

cast_info = [Map.fromList [("person_id", (1 :: Int)), ("movie_id", (10 :: Int)), ("note", "(voice: English version)"), ("role_id", (1000 :: Int))]]

company_name = [Map.fromList [("id", (50 :: Int)), ("country_code", "[jp]")]]

movie_companies = [Map.fromList [("movie_id", (10 :: Int)), ("company_id", (50 :: Int)), ("note", "Studio (Japan)")]]

name = [Map.fromList [("id", (1 :: Int)), ("name", "Yoko Ono")], Map.fromList [("id", (2 :: Int)), ("name", "Yuichi")]]

role_type = [Map.fromList [("id", (1000 :: Int)), ("role", "actress")]]

title = [Map.fromList [("id", (10 :: Int)), ("title", "Dubbed Film")]]

eligible = [Map.fromList [("pseudonym", fromMaybe (error "missing") (Map.lookup "name" an1)), ("movie_title", fromMaybe (error "missing") (Map.lookup "title" t))] | an1 <- aka_name, n1 <- name, ci <- cast_info, t <- title, mc <- movie_companies, cn <- company_name, rt <- role_type, (fromMaybe (error "missing") (Map.lookup "id" (n1)) == fromMaybe (error "missing") (Map.lookup "person_id" (an1))), (fromMaybe (error "missing") (Map.lookup "person_id" (ci)) == fromMaybe (error "missing") (Map.lookup "person_id" (an1))), (fromMaybe (error "missing") (Map.lookup "id" (t)) == fromMaybe (error "missing") (Map.lookup "movie_id" (ci))), (fromMaybe (error "missing") (Map.lookup "movie_id" (mc)) == fromMaybe (error "missing") (Map.lookup "movie_id" (ci))), (fromMaybe (error "missing") (Map.lookup "id" (cn)) == fromMaybe (error "missing") (Map.lookup "company_id" (mc))), (fromMaybe (error "missing") (Map.lookup "id" (rt)) == fromMaybe (error "missing") (Map.lookup "role_id" (ci))), (((((((((fromMaybe (error "missing") (Map.lookup "note" ci) == "(voice: English version)") && fromMaybe (error "missing") (Map.lookup "country_code" cn)) == "[jp]") && fromMaybe (error "missing") (Map.lookup "contains" (fromMaybe (error "missing") (Map.lookup "note" mc))) "(Japan)") && (not fromMaybe (error "missing") (Map.lookup "contains" (fromMaybe (error "missing") (Map.lookup "note" mc))) "(USA)")) && fromMaybe (error "missing") (Map.lookup "contains" (fromMaybe (error "missing") (Map.lookup "name" n1))) "Yo") && (not fromMaybe (error "missing") (Map.lookup "contains" (fromMaybe (error "missing") (Map.lookup "name" n1))) "Yu")) && fromMaybe (error "missing") (Map.lookup "role" rt)) == "actress")]

result = [Map.fromList [("actress_pseudonym", minimum [fromMaybe (error "missing") (Map.lookup "pseudonym" x) | x <- eligible]), ("japanese_movie_dubbed", minimum [fromMaybe (error "missing") (Map.lookup "movie_title" x) | x <- eligible])]]

test_Q8_returns_the_pseudonym_and_movie_title_for_Japanese_dubbing :: IO ()
test_Q8_returns_the_pseudonym_and_movie_title_for_Japanese_dubbing = do
  expect ((result == [Map.fromList [("actress_pseudonym", "Y. S."), ("japanese_movie_dubbed", "Dubbed Film")]]))

main :: IO ()
main = do
  _json result
  test_Q8_returns_the_pseudonym_and_movie_title_for_Japanese_dubbing
