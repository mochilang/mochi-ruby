// Generated by Mochi compiler v0.10.25 on 2025-07-13T13:10:04Z
import 'dart:io';
import 'dart:convert';

var cast_info = [{'movie_id': 1, 'person_id': 1}, {'movie_id': 2, 'person_id': 2}];

var company_name = [{'id': 1, 'country_code': '[us]'}, {'id': 2, 'country_code': '[ca]'}];

var keyword = [{'id': 10, 'keyword': 'character-name-in-title'}, {'id': 20, 'keyword': 'other'}];

var movie_companies = [{'movie_id': 1, 'company_id': 1}, {'movie_id': 2, 'company_id': 2}];

var movie_keyword = [{'movie_id': 1, 'keyword_id': 10}, {'movie_id': 2, 'keyword_id': 20}];

var name = [{'id': 1, 'name': 'Bob Smith'}, {'id': 2, 'name': 'Alice Jones'}];

var title = [{'id': 1, 'title': 'Bob\'s Journey'}, {'id': 2, 'title': 'Foreign Film'}];

var matches = (() {
  var _q0 = <dynamic>[];
  for (var n in name) {
    for (var ci in cast_info) {
      if (!(ci['person_id'] == n['id'])) continue;
      for (var t in title) {
        if (!(t['id'] == ci['movie_id'])) continue;
        for (var mk in movie_keyword) {
          if (!(mk['movie_id'] == t['id'])) continue;
          for (var k in keyword) {
            if (!(k['id'] == mk['keyword_id'])) continue;
            for (var mc in movie_companies) {
              if (!(mc['movie_id'] == t['id'])) continue;
              for (var cn in company_name) {
                if (!(cn['id'] == mc['company_id'])) continue;
                if (!((cn['country_code'] == '[us]' && k['keyword'] == 'character-name-in-title' && (n['name'] as dynamic).toString().startsWith('B'.toString()) && ci['movie_id'] == mk['movie_id'] && ci['movie_id'] == mc['movie_id'] && mc['movie_id'] == mk['movie_id']) ?? false)) continue;
                _q0.add(n['name']);
              }
            }
          }
        }
      }
    }
  }
  return _q0;
})();

var result = [{'member_in_charnamed_american_movie': _min(matches), 'a1': _min(matches)}];

void main() {
  print(jsonEncode(result));
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
