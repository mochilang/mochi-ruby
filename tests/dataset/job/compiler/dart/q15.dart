// Generated by Mochi compiler v0.10.25 on 2025-07-13T13:10:03Z
import 'dart:io';
import 'dart:convert';

var aka_title = [{'movie_id': 1}, {'movie_id': 2}];

var company_name = [{'id': 1, 'country_code': '[us]'}, {'id': 2, 'country_code': '[gb]'}];

var company_type = [{'id': 10}, {'id': 20}];

var info_type = [{'id': 5, 'info': 'release dates'}, {'id': 6, 'info': 'other'}];

var keyword = [{'id': 100}, {'id': 200}];

var movie_companies = [{'movie_id': 1, 'company_id': 1, 'company_type_id': 10, 'note': 'release (2005) (worldwide)'}, {'movie_id': 2, 'company_id': 2, 'company_type_id': 20, 'note': 'release (1999) (worldwide)'}];

var movie_info = [{'movie_id': 1, 'info_type_id': 5, 'note': 'internet', 'info': 'USA: March 2005'}, {'movie_id': 2, 'info_type_id': 5, 'note': 'theater', 'info': 'USA: May 1999'}];

var movie_keyword = [{'movie_id': 1, 'keyword_id': 100}, {'movie_id': 2, 'keyword_id': 200}];

var title = [{'id': 1, 'title': 'Example Movie', 'production_year': 2005}, {'id': 2, 'title': 'Old Movie', 'production_year': 1999}];

var rows = (() {
  var _q0 = <dynamic>[];
  for (var t in title) {
    for (var at in aka_title) {
      if (!(at['movie_id'] == t['id'])) continue;
      for (var mi in movie_info) {
        if (!(mi['movie_id'] == t['id'])) continue;
        for (var mk in movie_keyword) {
          if (!(mk['movie_id'] == t['id'])) continue;
          for (var mc in movie_companies) {
            if (!(mc['movie_id'] == t['id'])) continue;
            for (var k in keyword) {
              if (!(k['id'] == mk['keyword_id'])) continue;
              for (var it1 in info_type) {
                if (!(it1['id'] == mi['info_type_id'])) continue;
                for (var cn in company_name) {
                  if (!(cn['id'] == mc['company_id'])) continue;
                  for (var ct in company_type) {
                    if (!(ct['id'] == mc['company_type_id'])) continue;
                    if (!((cn['country_code'] == '[us]' && it1['info'] == 'release dates' && (mc['note'] as dynamic).contains('200') && (mc['note'] as dynamic).contains('worldwide') && (mi['note'] as dynamic).contains('internet') && (mi['info'] as dynamic).contains('USA:') && (mi['info'] as dynamic).contains('200') && t['production_year'].toString().compareTo(2000.toString()) > 0) ?? false)) continue;
                    _q0.add({'release_date': mi['info'], 'internet_movie': t['title']});
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  return _q0;
})();

var result = [{'release_date': (() { var _t2 = (() {
  var _q1 = <dynamic>[];
  for (var r in rows) {
    _q1.add(r['release_date']);
  }
  return _q1;
})(); return _min(_t2); })(), 'internet_movie': (() { var _t4 = (() {
  var _q3 = <dynamic>[];
  for (var r in rows) {
    _q3.add(r['internet_movie']);
  }
  return _q3;
})(); return _min(_t4); })()}];

void main() {
  print(jsonEncode(result));
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
