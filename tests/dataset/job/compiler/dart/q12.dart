// Generated by Mochi compiler v0.10.25 on 2025-07-13T13:10:02Z
import 'dart:io';
import 'dart:convert';

var company_name = [{'id': 1, 'name': 'Best Pictures', 'country_code': '[us]'}, {'id': 2, 'name': 'Foreign Films', 'country_code': '[uk]'}];

var company_type = [{'id': 10, 'kind': 'production companies'}, {'id': 20, 'kind': 'distributors'}];

var info_type = [{'id': 100, 'info': 'genres'}, {'id': 200, 'info': 'rating'}];

var movie_companies = [{'movie_id': 1000, 'company_id': 1, 'company_type_id': 10}, {'movie_id': 2000, 'company_id': 2, 'company_type_id': 10}];

var movie_info = [{'movie_id': 1000, 'info_type_id': 100, 'info': 'Drama'}, {'movie_id': 2000, 'info_type_id': 100, 'info': 'Horror'}];

var movie_info_idx = [{'movie_id': 1000, 'info_type_id': 200, 'info': 8.3}, {'movie_id': 2000, 'info_type_id': 200, 'info': 7.5}];

var title = [{'id': 1000, 'production_year': 2006, 'title': 'Great Drama'}, {'id': 2000, 'production_year': 2007, 'title': 'Low Rated'}];

var result = (() {
  var _q0 = <dynamic>[];
  for (var cn in company_name) {
    for (var mc in movie_companies) {
      if (!(mc['company_id'] == cn['id'])) continue;
      for (var ct in company_type) {
        if (!(ct['id'] == mc['company_type_id'])) continue;
        for (var t in title) {
          if (!(t['id'] == mc['movie_id'])) continue;
          for (var mi in movie_info) {
            if (!(mi['movie_id'] == t['id'])) continue;
            for (var it1 in info_type) {
              if (!(it1['id'] == mi['info_type_id'])) continue;
              for (var mi_idx in movie_info_idx) {
                if (!(mi_idx['movie_id'] == t['id'])) continue;
                for (var it2 in info_type) {
                  if (!(it2['id'] == mi_idx['info_type_id'])) continue;
                  if (!(cn['country_code'] == '[us]' && ct['kind'] == 'production companies' && it1['info'] == 'genres' && it2['info'] == 'rating' && (mi['info'] == 'Drama' || mi['info'] == 'Horror') && mi_idx['info'].toString().compareTo(8.toString()) > 0 && t['production_year'].toString().compareTo(2005.toString()) >= 0 && t['production_year'].toString().compareTo(2008.toString()) <= 0)) continue;
                  _q0.add({'movie_company': cn['name'], 'rating': mi_idx['info'], 'drama_horror_movie': t['title']});
                }
              }
            }
          }
        }
      }
    }
  }
  return _q0;
})();

void main() {
  print(jsonEncode(result));
}

bool _equal(dynamic a, dynamic b) {
    if (a is List && b is List) {
        if (a.length != b.length) return false;
        for (var i = 0; i < a.length; i++) { if (!_equal(a[i], b[i])) return false; }
        return true;
    }
    if (a is Map && b is Map) {
        if (a.length != b.length) return false;
        for (var k in a.keys) { if (!b.containsKey(k) || !_equal(a[k], b[k])) return false; }
        return true;
    }
    return a == b;
}

String _formatDuration(Duration d) {
    if (d.inMicroseconds < 1000) return '${d.inMicroseconds}µs';
    if (d.inMilliseconds < 1000) return '${d.inMilliseconds}ms';
    return '${(d.inMilliseconds/1000).toStringAsFixed(2)}s';
}

void _json(dynamic v) {
    print(jsonEncode(v));
}


dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

bool _runTest(String name, void Function() f) {
    stdout.write('   test $name ...');
    var start = DateTime.now();
    try {
        f();
        var d = DateTime.now().difference(start);
        stdout.writeln(' ok (${_formatDuration(d)})');
        return true;
    } catch (e) {
        var d = DateTime.now().difference(start);
        stdout.writeln(' fail $e (${_formatDuration(d)})');
        return false;
    }
}
