; Generated by Mochi compiler v0.10.25 on 2025-07-13T12:05:31Z
(ns main)

(defn _min [v]
  (let [lst (cond
              (and (map? v) (contains? v :Items)) (:Items v)
              (sequential? v) v
              :else (throw (ex-info "min() expects list or group" {})))]
    (if (empty? lst)
      0
      (reduce (fn [a b] (if (neg? (compare a b)) a b)) lst))))

(defn _equal [a b]
  (cond
    (and (sequential? a) (sequential? b))
      (and (= (count a) (count b)) (every? true? (map _equal a b)))
    (and (map? a) (map? b))
      (and (= (count a) (count b))
           (every? (fn [k] (_equal (get a k) (get b k))) (keys a)))
    (and (number? a) (number? b))
      (= (double a) (double b))
    :else
      (= a b)))

(defn _escape_json [s]
  (-> s
      (clojure.string/replace "\\" "\\\\")
      (clojure.string/replace "\"" "\\\"")))

(defn _to_json [v]
  (cond
    (nil? v) "null"
    (string? v) (str "\"" (_escape_json v) "\"")
    (number? v) (str v)
    (boolean? v) (str v)
    (sequential? v) (str "[" (clojure.string/join "," (map _to_json v)) "]")
    (map? v) (str "{" (clojure.string/join "," (map (fn [[k val]]
                                        (str "\"" (_escape_json (name k)) "\":" (_to_json val))) v)) "}")
    :else (str "\"" (_escape_json (str v)) "\"")))

(defn _json [v]
  (println (_to_json v)))

(declare char_name cast_info company_name company_type movie_companies role_type title matches result)

(defn test_Q10_finds_uncredited_voice_actor_in_Russian_movie []
  (assert (_equal result [{:uncredited_voiced_character "Ivan" :russian_movie "Vodka Dreams"}]) "expect failed")
)

(defn -main []
  (def char_name [{:id 1 :name "Ivan"} {:id 2 :name "Alex"}]) ;; list of
  (def cast_info [{:movie_id 10 :person_role_id 1 :role_id 1 :note "Soldier (voice) (uncredited)"} {:movie_id 11 :person_role_id 2 :role_id 1 :note "(voice)"}]) ;; list of
  (def company_name [{:id 1 :country_code "[ru]"} {:id 2 :country_code "[us]"}]) ;; list of
  (def company_type [{:id 1} {:id 2}]) ;; list of
  (def movie_companies [{:movie_id 10 :company_id 1 :company_type_id 1} {:movie_id 11 :company_id 2 :company_type_id 1}]) ;; list of
  (def role_type [{:id 1 :role "actor"} {:id 2 :role "director"}]) ;; list of
  (def title [{:id 10 :title "Vodka Dreams" :production_year 2006} {:id 11 :title "Other Film" :production_year 2004}]) ;; list of
  (def matches (vec (->> (for [chn char_name ci cast_info :when (_equal (:id chn) (:person_role_id ci)) rt role_type :when (_equal (:id rt) (:role_id ci)) t title :when (_equal (:id t) (:movie_id ci)) mc movie_companies :when (_equal (:movie_id mc) (:id t)) cn company_name :when (_equal (:id cn) (:company_id mc)) :when (and (and (and (and (clojure.string/includes? (:note ci) "(voice)") (clojure.string/includes? (:note ci) "(uncredited)")) (_equal (:country_code cn) "[ru]")) (_equal (:role rt) "actor")) (> (:production_year t) 2005)) ct company_type :when (_equal (:id ct) (:company_type_id mc))] {:character (:name chn) :movie (:title t)})))) ;; list of
  (def result [{:uncredited_voiced_character (_min (vec (->> (for [x matches] (:character x))))) :russian_movie (_min (vec (->> (for [x matches] (:movie x)))))}]) ;; list of
  (_json result)
  (test_Q10_finds_uncredited_voice_actor_in_Russian_movie)
)

(-main)
