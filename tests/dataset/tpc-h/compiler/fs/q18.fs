// Generated by Mochi compiler v0.10.28 on 2025-07-18T03:38:00Z
open System
open System.Text.Json

type Anon1 = {
    n_nationkey: int
    n_name: string
}
type Anon2 = {
    c_custkey: int
    c_name: string
    c_acctbal: float
    c_nationkey: int
    c_address: string
    c_phone: string
    c_comment: string
}
type Anon3 = {
    o_orderkey: int
    o_custkey: int
}
type Anon4 = {
    l_orderkey: int
    l_quantity: int
    l_extendedprice: float
    l_discount: float
}
type Anon5 = {
    c_name: obj
    c_custkey: obj
    revenue: obj
    c_acctbal: obj
    n_name: obj
    c_address: obj
    c_phone: obj
    c_comment: obj
}
type Anon6 = {
    c_name: obj
    c_custkey: obj
    c_acctbal: obj
    c_address: obj
    c_phone: obj
    c_comment: obj
    n_name: obj
}
type Anon7 = {
    c_name: string
    c_custkey: int
    c_acctbal: float
    c_address: string
    c_phone: string
    c_comment: string
    n_name: string
}
type Anon8 = {
    c_name: string
    c_custkey: int
    revenue: float
    c_acctbal: float
    n_name: string
    c_address: string
    c_phone: string
    c_comment: string
}
type _Group<'K,'T>(key: 'K) =
    member val key = key with get, set
    member val Items = System.Collections.Generic.List<'T>() with get
    member this.size = this.Items.Count
    interface System.Collections.Generic.IEnumerable<'T> with
        member this.GetEnumerator() = (this.Items :> seq<'T>).GetEnumerator()
    interface System.Collections.IEnumerable with
        member this.GetEnumerator() = (this.Items :> System.Collections.IEnumerable).GetEnumerator()

let _group_by (src: 'T list) (keyfn: 'T -> 'K) : _Group<'K,'T> list =
    let groups = System.Collections.Generic.Dictionary<string,_Group<'K,'T>>()
    let order = System.Collections.Generic.List<string>()
    for it in src do
        let key = keyfn it
        let ks = string key
        let mutable g = Unchecked.defaultof<_Group<'K,'T>>
        if groups.TryGetValue(ks, &g) then ()
        else
            g <- _Group<'K,'T>(key)
            groups.Add(ks, g)
            order.Add(ks)
        g.Items.Add(it)
    [ for ks in order -> groups.[ks] ]

let nation: Anon1 list = [{ n_nationkey = 1; n_name = "GERMANY" }]
let customer: Anon2 list = [{ c_custkey = 1; c_name = "Alice"; c_acctbal = 1000.0; c_nationkey = 1; c_address = "123 Market St"; c_phone = "123-456"; c_comment = "Premium client" }; { c_custkey = 2; c_name = "Bob"; c_acctbal = 200.0; c_nationkey = 1; c_address = "456 Side St"; c_phone = "987-654"; c_comment = "Frequent returns" }]
let orders: Anon3 list = [{ o_orderkey = 100; o_custkey = 1 }; { o_orderkey = 200; o_custkey = 1 }; { o_orderkey = 300; o_custkey = 2 }]
let lineitem: Anon4 list = [{ l_orderkey = 100; l_quantity = 150; l_extendedprice = 1000.0; l_discount = 0.1 }; { l_orderkey = 200; l_quantity = 100; l_extendedprice = 800.0; l_discount = 0.0 }; { l_orderkey = 300; l_quantity = 30; l_extendedprice = 300.0; l_discount = 0.05 }]
let threshold: int = 200
let result: Anon5 list = [ for g in _group_by [ for c in customer do 
  for o in orders do 
  for l in lineitem do 
  for n in nation do if o.o_custkey = c.c_custkey && l.l_orderkey = o.o_orderkey && n.n_nationkey = c.c_nationkey then yield (c, o, l, n) ] (fun (c, o, l, n) -> { c_name = c.c_name; c_custkey = c.c_custkey; c_acctbal = c.c_acctbal; c_address = c.c_address; c_phone = c.c_phone; c_comment = c.c_comment; n_name = n.n_name }) |> List.filter (fun gTmp -> let g = gTmp in List.sum [ for x in g do yield (x |> fun (c, o, l, n) -> l).l_quantity ] > threshold) |> List.sortByDescending (fun gTmp -> let g = gTmp in List.sum [ for x in g do yield (x |> fun (c, o, l, n) -> l).l_extendedprice * (1 - (x |> fun (c, o, l, n) -> l).l_discount) ]) do
    yield { c_name = g.key.c_name; c_custkey = g.key.c_custkey; revenue = List.sum [ for x in g do yield (x |> fun (c, o, l, n) -> l).l_extendedprice * (1 - (x |> fun (c, o, l, n) -> l).l_discount) ]; c_acctbal = g.key.c_acctbal; n_name = g.key.n_name; c_address = g.key.c_address; c_phone = g.key.c_phone; c_comment = g.key.c_comment } ]
printfn "%A" (JsonSerializer.Serialize(result))
assert (result = [{ c_name = "Alice"; c_custkey = 1; revenue = 1700.0; c_acctbal = 1000.0; n_name = "GERMANY"; c_address = "123 Market St"; c_phone = "123-456"; c_comment = "Premium client" }])
