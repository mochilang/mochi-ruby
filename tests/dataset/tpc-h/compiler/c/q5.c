// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  char *key;
  list_int items;
} _GroupString;
typedef struct {
  int len;
  int cap;
  _GroupString *data;
} list_group_string;
static list_group_string _group_by_string(list_string src) {
  list_group_string res;
  res.len = 0;
  res.cap = 0;
  res.data = NULL;
  for (int i = 0; i < src.len; i++) {
    char *key = src.data[i];
    int idx = -1;
    for (int j = 0; j < res.len; j++)
      if (strcmp(res.data[j].key, key) == 0) {
        idx = j;
        break;
      }
    if (idx == -1) {
      if (res.len >= res.cap) {
        res.cap = res.cap ? res.cap * 2 : 4;
        res.data =
            (_GroupString *)realloc(res.data, sizeof(_GroupString) * res.cap);
      }
      res.data[res.len].key = key;
      res.data[res.len].items = list_int_create(0);
      idx = res.len++;
    }
    _GroupString *g = &res.data[idx];
    g->items.data =
        (int *)realloc(g->items.data, sizeof(int) * (g->items.len + 1));
    g->items.data[g->items.len++] = i;
  }
  return res;
}
typedef struct {
  char *n_name;
  int revenue;
} tmp1_t;
typedef struct {
  int len;
  tmp1_t *data;
} tmp1_list_t;
tmp1_list_t create_tmp1_list(int len) {
  tmp1_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp1_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *n_name;
  int revenue;
} tmp_item_t;
typedef struct {
  int len;
  tmp_item_t *data;
} tmp_item_list_t;
tmp_item_list_t create_tmp_item_list(int len) {
  tmp_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int r_regionkey;
  char *r_name;
} region_t;
typedef struct {
  int len;
  region_t *data;
} region_list_t;
region_list_t create_region_list(int len) {
  region_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(region_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  int n_regionkey;
  char *n_name;
} nation_t;
typedef struct {
  int len;
  nation_t *data;
} nation_list_t;
nation_list_t create_nation_list(int len) {
  nation_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(nation_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  int c_nationkey;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  int s_nationkey;
} supplier_t;
typedef struct {
  int len;
  supplier_t *data;
} supplier_list_t;
supplier_list_t create_supplier_list(int len) {
  supplier_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(supplier_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  int o_custkey;
  char *o_orderdate;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  int l_suppkey;
  double l_extendedprice;
  double l_discount;
} lineitem_t;
typedef struct {
  int len;
  lineitem_t *data;
} lineitem_list_t;
lineitem_list_t create_lineitem_list(int len) {
  lineitem_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(lineitem_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *nation;
  double revenue;
} local_customer_supplier_orders_item_t;
typedef struct {
  int len;
  local_customer_supplier_orders_item_t *data;
} local_customer_supplier_orders_item_list_t;
local_customer_supplier_orders_item_list_t
create_local_customer_supplier_orders_item_list(int len) {
  local_customer_supplier_orders_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(local_customer_supplier_orders_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *n_name;
  double revenue;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers_result;
static void test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers() {
  tmp1_t tmp1[] = {(tmp1_t){.n_name = "JAPAN", .revenue = 950},
                   (tmp_item_t){.n_name = "INDIA", .revenue = 720}};
  int tmp1_len = sizeof(tmp1) / sizeof(tmp1[0]);
  int tmp2 = 1;
  if (test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers_result
          .len != tmp1_len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers_result
             .len;
         i3++) {
      if (test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers_result
              .data[i3] != tmp1[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  region_t region[] = {(region_t){.r_regionkey = 0, .r_name = "ASIA"},
                       (region_t){.r_regionkey = 1, .r_name = "EUROPE"}};
  int region_len = sizeof(region) / sizeof(region[0]);
  nation_t nation[] = {
      (nation_t){.n_nationkey = 10, .n_regionkey = 0, .n_name = "JAPAN"},
      (nation_t){.n_nationkey = 20, .n_regionkey = 0, .n_name = "INDIA"},
      (nation_t){.n_nationkey = 30, .n_regionkey = 1, .n_name = "FRANCE"}};
  int nation_len = sizeof(nation) / sizeof(nation[0]);
  customer_t customer[] = {(customer_t){.c_custkey = 1, .c_nationkey = 10},
                           (customer_t){.c_custkey = 2, .c_nationkey = 20}};
  int customer_len = sizeof(customer) / sizeof(customer[0]);
  supplier_t supplier[] = {(supplier_t){.s_suppkey = 100, .s_nationkey = 10},
                           (supplier_t){.s_suppkey = 200, .s_nationkey = 20}};
  int supplier_len = sizeof(supplier) / sizeof(supplier[0]);
  order_t orders[] = {
      (order_t){
          .o_orderkey = 1000, .o_custkey = 1, .o_orderdate = "1994-03-15"},
      (order_t){
          .o_orderkey = 2000, .o_custkey = 2, .o_orderdate = "1994-06-10"},
      (order_t){
          .o_orderkey = 3000, .o_custkey = 2, .o_orderdate = "1995-01-01"}};
  int orders_len = sizeof(orders) / sizeof(orders[0]);
  lineitem_t lineitem[] = {(lineitem_t){.l_orderkey = 1000,
                                        .l_suppkey = 100,
                                        .l_extendedprice = 1000.0,
                                        .l_discount = 0.05},
                           (lineitem_t){.l_orderkey = 2000,
                                        .l_suppkey = 200,
                                        .l_extendedprice = 800.0,
                                        .l_discount = 0.1},
                           (lineitem_t){.l_orderkey = 3000,
                                        .l_suppkey = 200,
                                        .l_extendedprice = 900.0,
                                        .l_discount = 0.05}};
  int lineitem_len = sizeof(lineitem) / sizeof(lineitem[0]);
  nation_list_t tmp4 = create_nation_list(region_len * nation_len);
  int tmp5 = 0;
  for (int tmp6 = 0; tmp6 < region_len; tmp6++) {
    region_t r = region[tmp6];
    for (int tmp7 = 0; tmp7 < nation_len; tmp7++) {
      nation_t n = nation[tmp7];
      if (!(n.n_regionkey == r.r_regionkey)) {
        continue;
      }
      if (!((strcmp(r.r_name, "ASIA") == 0))) {
        continue;
      }
      tmp4.data[tmp5] = n;
      tmp5++;
    }
  }
  tmp4.len = tmp5;
  nation_list_t asia_nations = tmp4;
  local_customer_supplier_orders_item_list_t tmp8 =
      create_local_customer_supplier_orders_item_list(
          customer_len * asia_nations.len * orders_len * lineitem_len *
          supplier_len);
  int tmp9 = 0;
  for (int tmp10 = 0; tmp10 < customer_len; tmp10++) {
    customer_t c = customer[tmp10];
    for (int tmp11 = 0; tmp11 < asia_nations.len; tmp11++) {
      nation_t n = asia_nations.data[tmp11];
      if (!(c.c_nationkey == n.n_nationkey)) {
        continue;
      }
      for (int tmp12 = 0; tmp12 < orders_len; tmp12++) {
        order_t o = orders[tmp12];
        if (!(o.o_custkey == c.c_custkey)) {
          continue;
        }
        for (int tmp13 = 0; tmp13 < lineitem_len; tmp13++) {
          lineitem_t l = lineitem[tmp13];
          if (!(l.l_orderkey == o.o_orderkey)) {
            continue;
          }
          for (int tmp14 = 0; tmp14 < supplier_len; tmp14++) {
            supplier_t s = supplier[tmp14];
            if (!(s.s_suppkey == l.l_suppkey)) {
              continue;
            }
            if (!((strcmp(o.o_orderdate, "1994-01-01") >= 0) &&
                  o.o_orderdate < "1995-01-01" &&
                  s.s_nationkey == c.c_nationkey)) {
              continue;
            }
            tmp8.data[tmp9] = (local_customer_supplier_orders_item_t){
                .nation = n.n_name,
                .revenue = l.l_extendedprice * (1 - l.l_discount)};
            tmp9++;
          }
        }
      }
    }
  }
  tmp8.len = tmp9;
  local_customer_supplier_orders_item_list_t local_customer_supplier_orders =
      tmp8;
  local_customer_supplier_orders_item_list_t tmp15 =
      create_local_customer_supplier_orders_item_list(
          local_customer_supplier_orders.len);
  list_string tmp16 = list_string_create(local_customer_supplier_orders.len);
  int tmp17 = 0;
  for (int i18 = 0; i18 < local_customer_supplier_orders.len; i18++) {
    local_customer_supplier_orders_item_t r =
        local_customer_supplier_orders.data[i18];
    tmp15.data[tmp17] = r;
    tmp16.data[tmp17] = r.nation;
    tmp17++;
  }
  tmp15.len = tmp17;
  tmp16.len = tmp17;
  list_group_string tmp19 = _group_by_string(tmp16);
  result_item_list_t tmp20 = create_result_item_list(tmp19.len);
  double *tmp22 = (double *)malloc(sizeof(double) * tmp19.len);
  int tmp21 = 0;
  for (int gi = 0; gi < tmp19.len; gi++) {
    _GroupString _gp = tmp19.data[gi];
    local_customer_supplier_orders_item_list_t tmp23 =
        create_local_customer_supplier_orders_item_list(_gp.items.len);
    for (int i24 = 0; i24 < _gp.items.len; i24++) {
      tmp23.data[i24] = tmp15.data[_gp.items.data[i24]];
    }
    tmp23.len = _gp.items.len;
    struct {
      char *key;
      local_customer_supplier_orders_item_list_t items;
    } g = {_gp.key, tmp23};
    list_float tmp25 = list_float_create(g.items.len);
    int tmp26 = 0;
    for (int i27 = 0; i27 < g.items.len; i27++) {
      local_customer_supplier_orders_item_t x = g.items.data[i27];
      tmp25.data[tmp26] = x.revenue;
      tmp26++;
    }
    tmp25.len = tmp26;
    tmp20.data[tmp21] =
        (result_item_t){.n_name = g.key, .revenue = _sum_float(tmp25)};
    list_float tmp28 = list_float_create(g.items.len);
    int tmp29 = 0;
    for (int i30 = 0; i30 < g.items.len; i30++) {
      local_customer_supplier_orders_item_t x = g.items.data[i30];
      tmp28.data[tmp29] = x.revenue;
      tmp29++;
    }
    tmp28.len = tmp29;
    tmp22[tmp21] = (-_sum_float(tmp28));
    tmp21++;
  }
  tmp20.len = tmp21;
  for (int i33 = 0; i33 < tmp21 - 1; i33++) {
    for (int i34 = i33 + 1; i34 < tmp21; i34++) {
      if (tmp22[i33] > tmp22[i34]) {
        double tmp31 = tmp22[i33];
        tmp22[i33] = tmp22[i34];
        tmp22[i34] = tmp31;
        result_item_t tmp32 = tmp20.data[i33];
        tmp20.data[i33] = tmp20.data[i34];
        tmp20.data[i34] = tmp32;
      }
    }
  }
  result_item_list_t result = tmp20;
  printf("[");
  for (int i35 = 0; i35 < result.len; i35++) {
    if (i35 > 0)
      printf(",");
    result_item_t it = result.data[i35];
    printf("{");
    _json_string("n_name");
    printf(":");
    _json_string(it.n_name);
    printf(",");
    _json_string("revenue");
    printf(":");
    _json_float(it.revenue);
    printf("}");
  }
  printf("]");
  test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers_result =
      result;
  test_Q5_returns_revenue_per_nation_in_ASIA_with_local_suppliers();
  return 0;
}
int main() { return _mochi_main(); }
