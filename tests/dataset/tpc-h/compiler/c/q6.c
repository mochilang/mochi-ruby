// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
  int l_quantity;
} lineitem_t;
typedef struct {
  int len;
  lineitem_t *data;
} lineitem_list_t;
lineitem_list_t create_lineitem_list(int len) {
  lineitem_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(lineitem_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static double test_Q6_calculates_revenue_from_qualified_lineitems_result;
static void test_Q6_calculates_revenue_from_qualified_lineitems() {
  if (!(test_Q6_calculates_revenue_from_qualified_lineitems_result ==
        ((1000.0 * 0.06) + (500.0 * 0.07)))) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  lineitem_t lineitem[] = {(lineitem_t){.l_extendedprice = 1000.0,
                                        .l_discount = 0.06,
                                        .l_shipdate = "1994-02-15",
                                        .l_quantity = 10},
                           (lineitem_t){.l_extendedprice = 500.0,
                                        .l_discount = 0.07,
                                        .l_shipdate = "1994-03-10",
                                        .l_quantity = 23},
                           (lineitem_t){.l_extendedprice = 400.0,
                                        .l_discount = 0.04,
                                        .l_shipdate = "1994-04-10",
                                        .l_quantity = 15},
                           (lineitem_t){.l_extendedprice = 200.0,
                                        .l_discount = 0.06,
                                        .l_shipdate = "1995-01-01",
                                        .l_quantity = 5}};
  int lineitem_len = sizeof(lineitem) / sizeof(lineitem[0]);
  list_float tmp1 = list_float_create(lineitem_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < lineitem_len; tmp3++) {
    lineitem_t l = lineitem[tmp3];
    if (!(((strcmp(l.l_shipdate, "1994-01-01") >= 0)) &&
          ((strcmp(l.l_shipdate, "1995-01-01") < 0)) &&
          (l.l_discount >= 0.05) && (l.l_discount <= 0.07) &&
          (l.l_quantity < 24))) {
      continue;
    }
    tmp1.data[tmp2] = l.l_extendedprice * l.l_discount;
    tmp2++;
  }
  tmp1.len = tmp2;
  double result = _sum_float(tmp1);
  _json_float(result);
  test_Q6_calculates_revenue_from_qualified_lineitems_result = result;
  test_Q6_calculates_revenue_from_qualified_lineitems();
  return 0;
}
int main() { return _mochi_main(); }
