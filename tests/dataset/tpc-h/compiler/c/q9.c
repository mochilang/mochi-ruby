// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *prefix = "green";
static char *start_date = "1995-01-01";
static char *end_date = "1996-12-31";

typedef struct {
  char *nation;
  char *o_year;
  double profit;
} tmp1_t;
typedef struct {
  int len;
  tmp1_t *data;
} tmp1_list_t;
tmp1_list_t create_tmp1_list(int len) {
  tmp1_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp1_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  char *n_name;
} nation_t;
typedef struct {
  int len;
  nation_t *data;
} nation_list_t;
nation_list_t create_nation_list(int len) {
  nation_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(nation_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  int s_nationkey;
} supplier_t;
typedef struct {
  int len;
  supplier_t *data;
} supplier_list_t;
supplier_list_t create_supplier_list(int len) {
  supplier_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(supplier_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int p_partkey;
  char *p_name;
} part_t;
typedef struct {
  int len;
  part_t *data;
} part_list_t;
part_list_t create_part_list(int len) {
  part_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(part_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int ps_partkey;
  int ps_suppkey;
  double ps_supplycost;
} partsupp_t;
typedef struct {
  int len;
  partsupp_t *data;
} partsupp_list_t;
partsupp_list_t create_partsupp_list(int len) {
  partsupp_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(partsupp_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  char *o_orderdate;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  int l_partkey;
  int l_suppkey;
  int l_quantity;
  double l_extendedprice;
  double l_discount;
} lineitem_t;
typedef struct {
  int len;
  lineitem_t *data;
} lineitem_list_t;
lineitem_list_t create_lineitem_list(int len) {
  lineitem_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(lineitem_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int nation;
  char *o_year;
  double profit;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q9_computes_profit_for_green_parts_by_nation_and_year_result;
static void test_Q9_computes_profit_for_green_parts_by_nation_and_year() {
  double revenue = 1000.0 * 0.9;
  double cost = 5 * 10.0;
  tmp1_t tmp1[] = {
      (tmp1_t){.nation = "BRAZIL", .o_year = "1995", .profit = revenue - cost}};
  int tmp1_len = sizeof(tmp1) / sizeof(tmp1[0]);
  int tmp2 = 1;
  if (test_Q9_computes_profit_for_green_parts_by_nation_and_year_result.len !=
      tmp1_len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q9_computes_profit_for_green_parts_by_nation_and_year_result.len;
         i3++) {
      if (test_Q9_computes_profit_for_green_parts_by_nation_and_year_result
              .data[i3] != tmp1[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  nation_t nation[] = {(nation_t){.n_nationkey = 1, .n_name = "BRAZIL"},
                       (nation_t){.n_nationkey = 2, .n_name = "CANADA"}};
  int nation_len = sizeof(nation) / sizeof(nation[0]);
  supplier_t supplier[] = {(supplier_t){.s_suppkey = 100, .s_nationkey = 1},
                           (supplier_t){.s_suppkey = 200, .s_nationkey = 2}};
  int supplier_len = sizeof(supplier) / sizeof(supplier[0]);
  part_t part[] = {(part_t){.p_partkey = 1000, .p_name = "green metal box"},
                   (part_t){.p_partkey = 2000, .p_name = "red plastic crate"}};
  int part_len = sizeof(part) / sizeof(part[0]);
  partsupp_t partsupp[] = {
      (partsupp_t){
          .ps_partkey = 1000, .ps_suppkey = 100, .ps_supplycost = 10.0},
      (partsupp_t){
          .ps_partkey = 1000, .ps_suppkey = 200, .ps_supplycost = 8.0}};
  int partsupp_len = sizeof(partsupp) / sizeof(partsupp[0]);
  order_t orders[] = {(order_t){.o_orderkey = 1, .o_orderdate = "1995-02-10"},
                      (order_t){.o_orderkey = 2, .o_orderdate = "1997-01-01"}};
  int orders_len = sizeof(orders) / sizeof(orders[0]);
  lineitem_t lineitem[] = {(lineitem_t){.l_orderkey = 1,
                                        .l_partkey = 1000,
                                        .l_suppkey = 100,
                                        .l_quantity = 5,
                                        .l_extendedprice = 1000.0,
                                        .l_discount = 0.1},
                           (lineitem_t){.l_orderkey = 2,
                                        .l_partkey = 1000,
                                        .l_suppkey = 200,
                                        .l_quantity = 10,
                                        .l_extendedprice = 800.0,
                                        .l_discount = 0.05}};
  int lineitem_len = sizeof(lineitem) / sizeof(lineitem[0]);
  result_item_list_t result = (result_item_list_t){0, NULL};
  printf("[");
  for (int i4 = 0; i4 < result.len; i4++) {
    if (i4 > 0)
      printf(",");
    result_item_t it = result.data[i4];
    printf("{");
    _json_string("nation");
    printf(":");
    _json_int(it.nation);
    printf(",");
    _json_string("o_year");
    printf(":");
    _json_string(it.o_year);
    printf(",");
    _json_string("profit");
    printf(":");
    _json_float(it.profit);
    printf("}");
  }
  printf("]");
  test_Q9_computes_profit_for_green_parts_by_nation_and_year_result = result;
  test_Q9_computes_profit_for_green_parts_by_nation_and_year();
  return 0;
}
int main() { return _mochi_main(); }
