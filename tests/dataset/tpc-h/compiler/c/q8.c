// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *start_date = "1995-01-01";
static char *end_date = "1996-12-31";
static char *target_type = "ECONOMY ANODIZED STEEL";
static char *target_nation = "BRAZIL";

typedef struct {
  char *o_year;
  double mkt_share;
} tmp1_t;
typedef struct {
  int len;
  tmp1_t *data;
} tmp1_list_t;
tmp1_list_t create_tmp1_list(int len) {
  tmp1_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp1_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int r_regionkey;
  char *r_name;
} region_t;
typedef struct {
  int len;
  region_t *data;
} region_list_t;
region_list_t create_region_list(int len) {
  region_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(region_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  int n_regionkey;
  char *n_name;
} nation_t;
typedef struct {
  int len;
  nation_t *data;
} nation_list_t;
nation_list_t create_nation_list(int len) {
  nation_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(nation_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int c_custkey;
  int c_nationkey;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int o_orderkey;
  int o_custkey;
  char *o_orderdate;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_orderkey;
  int l_suppkey;
  int l_partkey;
  double l_extendedprice;
  double l_discount;
} lineitem_t;
typedef struct {
  int len;
  lineitem_t *data;
} lineitem_list_t;
lineitem_list_t create_lineitem_list(int len) {
  lineitem_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(lineitem_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
} supplier_t;
typedef struct {
  int len;
  supplier_t *data;
} supplier_list_t;
supplier_list_t create_supplier_list(int len) {
  supplier_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(supplier_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int p_partkey;
  char *p_type;
} part_t;
typedef struct {
  int len;
  part_t *data;
} part_list_t;
part_list_t create_part_list(int len) {
  part_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(part_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *o_year;
  double mkt_share;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result;
static void test_Q8_returns_correct_market_share_for_BRAZIL_in_1995() {
  double numerator = 1000.0 * 0.9;
  double denominator = numerator + (500.0 * 0.95);
  double share = numerator / denominator;
  tmp1_t tmp1[] = {(tmp1_t){.o_year = "1995", .mkt_share = share}};
  int tmp1_len = sizeof(tmp1) / sizeof(tmp1[0]);
  int tmp2 = 1;
  if (test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result.len !=
      tmp1_len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 <
         test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result.len;
         i3++) {
      if (test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result
              .data[i3] != tmp1[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  region_t region[] = {(region_t){.r_regionkey = 0, .r_name = "AMERICA"}};
  int region_len = sizeof(region) / sizeof(region[0]);
  nation_t nation[] = {
      (nation_t){.n_nationkey = 10, .n_regionkey = 0, .n_name = "BRAZIL"},
      (nation_t){.n_nationkey = 20, .n_regionkey = 0, .n_name = "CANADA"}};
  int nation_len = sizeof(nation) / sizeof(nation[0]);
  customer_t customer[] = {(customer_t){.c_custkey = 1, .c_nationkey = 10},
                           (customer_t){.c_custkey = 2, .c_nationkey = 20}};
  int customer_len = sizeof(customer) / sizeof(customer[0]);
  order_t orders[] = {
      (order_t){.o_orderkey = 100, .o_custkey = 1, .o_orderdate = "1995-04-10"},
      (order_t){
          .o_orderkey = 200, .o_custkey = 2, .o_orderdate = "1995-07-15"}};
  int orders_len = sizeof(orders) / sizeof(orders[0]);
  lineitem_t lineitem[] = {(lineitem_t){.l_orderkey = 100,
                                        .l_suppkey = 1000,
                                        .l_partkey = 5000,
                                        .l_extendedprice = 1000.0,
                                        .l_discount = 0.1},
                           (lineitem_t){.l_orderkey = 200,
                                        .l_suppkey = 2000,
                                        .l_partkey = 5000,
                                        .l_extendedprice = 500.0,
                                        .l_discount = 0.05}};
  int lineitem_len = sizeof(lineitem) / sizeof(lineitem[0]);
  supplier_t supplier[] = {(supplier_t){.s_suppkey = 1000},
                           (supplier_t){.s_suppkey = 2000}};
  int supplier_len = sizeof(supplier) / sizeof(supplier[0]);
  part_t part[] = {
      (part_t){.p_partkey = 5000, .p_type = "ECONOMY ANODIZED STEEL"},
      (part_t){.p_partkey = 6000, .p_type = "SMALL BRASS"}};
  int part_len = sizeof(part) / sizeof(part[0]);
  result_item_list_t result = (result_item_list_t){0, NULL};
  printf("[");
  for (int i4 = 0; i4 < result.len; i4++) {
    if (i4 > 0)
      printf(",");
    result_item_t it = result.data[i4];
    printf("{");
    _json_string("o_year");
    printf(":");
    _json_string(it.o_year);
    printf(",");
    _json_string("mkt_share");
    printf(":");
    _json_float(it.mkt_share);
    printf("}");
  }
  printf("]");
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995_result = result;
  test_Q8_returns_correct_market_share_for_BRAZIL_in_1995();
  return 0;
}
int main() { return _mochi_main(); }
