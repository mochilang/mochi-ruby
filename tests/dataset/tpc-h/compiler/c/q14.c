// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static int contains_string(char *s, char *sub) {
  return strstr(s, sub) != NULL;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
static char *start_date = "1995-09-01";
static char *end_date = "1995-10-01";

typedef struct {
  int p_partkey;
  char *p_type;
} part_t;
typedef struct {
  int len;
  part_t *data;
} part_list_t;
part_list_t create_part_list(int len) {
  part_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(part_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_partkey;
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
} lineitem_t;
typedef struct {
  int len;
  lineitem_t *data;
} lineitem_list_t;
lineitem_list_t create_lineitem_list(int len) {
  lineitem_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(lineitem_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int is_promo;
  double revenue;
} filtered_item_t;
typedef struct {
  int len;
  filtered_item_t *data;
} filtered_item_list_t;
filtered_item_list_t create_filtered_item_list(int len) {
  filtered_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(filtered_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static double test_Q14_calculates_promo_revenue_percent_in_1995_09_result;
static void test_Q14_calculates_promo_revenue_percent_in_1995_09() {
  double promo = 1000.0 * 0.9;
  double total = 900 + 800.0;
  double expected = 100.0 * promo / total;
  if (!(test_Q14_calculates_promo_revenue_percent_in_1995_09_result ==
        expected)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  part_t part[] = {(part_t){.p_partkey = 1, .p_type = "PROMO LUXURY"},
                   (part_t){.p_partkey = 2, .p_type = "STANDARD BRASS"}};
  int part_len = sizeof(part) / sizeof(part[0]);
  lineitem_t lineitem[] = {(lineitem_t){.l_partkey = 1,
                                        .l_extendedprice = 1000.0,
                                        .l_discount = 0.1,
                                        .l_shipdate = "1995-09-05"},
                           (lineitem_t){.l_partkey = 2,
                                        .l_extendedprice = 800.0,
                                        .l_discount = 0.0,
                                        .l_shipdate = "1995-09-20"},
                           (lineitem_t){.l_partkey = 1,
                                        .l_extendedprice = 500.0,
                                        .l_discount = 0.2,
                                        .l_shipdate = "1995-10-02"}};
  int lineitem_len = sizeof(lineitem) / sizeof(lineitem[0]);
  filtered_item_list_t tmp1 =
      create_filtered_item_list(lineitem_len * part_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < lineitem_len; tmp3++) {
    lineitem_t l = lineitem[tmp3];
    for (int tmp4 = 0; tmp4 < part_len; tmp4++) {
      part_t p = part[tmp4];
      if (!(p.p_partkey == l.l_partkey)) {
        continue;
      }
      if (!((strcmp(l.l_shipdate, start_date) >= 0) &&
            l.l_shipdate < end_date)) {
        continue;
      }
      tmp1.data[tmp2] =
          (filtered_item_t){.is_promo = contains_string(p.p_type, "PROMO"),
                            .revenue = l.l_extendedprice * (1 - l.l_discount)};
      tmp2++;
    }
  }
  tmp1.len = tmp2;
  filtered_item_list_t filtered = tmp1;
  list_float tmp5 = list_float_create(filtered.len);
  int tmp6 = 0;
  for (int tmp7 = 0; tmp7 < filtered.len; tmp7++) {
    filtered_item_t x = filtered.data[tmp7];
    if (!(x.is_promo)) {
      continue;
    }
    tmp5.data[tmp6] = x.revenue;
    tmp6++;
  }
  tmp5.len = tmp6;
  double promo_sum = _sum_float(tmp5);
  list_float tmp8 = list_float_create(filtered.len);
  int tmp9 = 0;
  for (int tmp10 = 0; tmp10 < filtered.len; tmp10++) {
    filtered_item_t x = filtered.data[tmp10];
    tmp8.data[tmp9] = x.revenue;
    tmp9++;
  }
  tmp8.len = tmp9;
  double total_sum = _sum_float(tmp8);
  double result = 100.0 * promo_sum / total_sum;
  _json_float(result);
  test_Q14_calculates_promo_revenue_percent_in_1995_09_result = result;
  test_Q14_calculates_promo_revenue_percent_in_1995_09();
  free(tmp5.data);
  free(tmp8.data);
  return 0;
}
int main() { return _mochi_main(); }
