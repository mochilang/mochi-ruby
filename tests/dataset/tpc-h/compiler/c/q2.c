// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:21Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _min_float(list_float v) {
  if (v.len == 0)
    return 0;
  double m = v.data[0];
  for (int i = 1; i < v.len; i++)
    if (v.data[i] < m)
      m = v.data[i];
  return m;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  double s_acctbal;
  char *s_name;
  char *n_name;
  int p_partkey;
  char *p_mfgr;
  char *s_address;
  char *s_phone;
  char *s_comment;
  double ps_supplycost;
} tmp1_t;
typedef struct {
  int len;
  tmp1_t *data;
} tmp1_list_t;
tmp1_list_t create_tmp1_list(int len) {
  tmp1_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp1_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int r_regionkey;
  char *r_name;
} region_t;
typedef struct {
  int len;
  region_t *data;
} region_list_t;
region_list_t create_region_list(int len) {
  region_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(region_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int n_nationkey;
  int n_regionkey;
  char *n_name;
} nation_t;
typedef struct {
  int len;
  nation_t *data;
} nation_list_t;
nation_list_t create_nation_list(int len) {
  nation_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(nation_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  int s_nationkey;
  char *s_phone;
  double s_acctbal;
  char *s_comment;
} supplier_t;
typedef struct {
  int len;
  supplier_t *data;
} supplier_list_t;
supplier_list_t create_supplier_list(int len) {
  supplier_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(supplier_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int p_partkey;
  char *p_type;
  int p_size;
  char *p_mfgr;
} part_t;
typedef struct {
  int len;
  part_t *data;
} part_list_t;
part_list_t create_part_list(int len) {
  part_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(part_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int ps_partkey;
  int ps_suppkey;
  double ps_supplycost;
} partsupp_t;
typedef struct {
  int len;
  partsupp_t *data;
} partsupp_list_t;
partsupp_list_t create_partsupp_list(int len) {
  partsupp_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(partsupp_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  supplier_t s;
  nation_t n;
} europe_suppliers_item_t;
typedef struct {
  int len;
  europe_suppliers_item_t *data;
} europe_suppliers_item_list_t;
europe_suppliers_item_list_t create_europe_suppliers_item_list(int len) {
  europe_suppliers_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(europe_suppliers_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  double s_acctbal;
  char *s_name;
  char *n_name;
  int p_partkey;
  char *p_mfgr;
  char *s_address;
  char *s_phone;
  char *s_comment;
  double ps_supplycost;
} target_partsupp_item_t;
typedef struct {
  int len;
  target_partsupp_item_t *data;
} target_partsupp_item_list_t;
target_partsupp_item_list_t create_target_partsupp_item_list(int len) {
  target_partsupp_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(target_partsupp_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int
    test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result;
static void
test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part() {
  tmp1_t tmp1[] = {(tmp1_t){.s_acctbal = 1000.0,
                            .s_name = "BestSupplier",
                            .n_name = "FRANCE",
                            .p_partkey = 1000,
                            .p_mfgr = "M1",
                            .s_address = "123 Rue",
                            .s_phone = "123",
                            .s_comment = "Fast and reliable",
                            .ps_supplycost = 10.0}};
  int tmp1_len = sizeof(tmp1) / sizeof(tmp1[0]);
  int tmp2 = 1;
  if (test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result
          .len != tmp1_len) {
    tmp2 = 0;
  } else {
    for (
        int i3 = 0;
        i3 <
        test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result
            .len;
        i3++) {
      if (test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result
              .data[i3] != tmp1[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  region_t region[] = {(region_t){.r_regionkey = 1, .r_name = "EUROPE"},
                       (region_t){.r_regionkey = 2, .r_name = "ASIA"}};
  int region_len = sizeof(region) / sizeof(region[0]);
  nation_t nation[] = {
      (nation_t){.n_nationkey = 10, .n_regionkey = 1, .n_name = "FRANCE"},
      (nation_t){.n_nationkey = 20, .n_regionkey = 2, .n_name = "CHINA"}};
  int nation_len = sizeof(nation) / sizeof(nation[0]);
  supplier_t supplier[] = {(supplier_t){.s_suppkey = 100,
                                        .s_name = "BestSupplier",
                                        .s_address = "123 Rue",
                                        .s_nationkey = 10,
                                        .s_phone = "123",
                                        .s_acctbal = 1000.0,
                                        .s_comment = "Fast and reliable"},
                           (supplier_t){.s_suppkey = 200,
                                        .s_name = "AltSupplier",
                                        .s_address = "456 Way",
                                        .s_nationkey = 20,
                                        .s_phone = "456",
                                        .s_acctbal = 500.0,
                                        .s_comment = "Slow"}};
  int supplier_len = sizeof(supplier) / sizeof(supplier[0]);
  part_t part[] = {(part_t){.p_partkey = 1000,
                            .p_type = "LARGE BRASS",
                            .p_size = 15,
                            .p_mfgr = "M1"},
                   (part_t){.p_partkey = 2000,
                            .p_type = "SMALL COPPER",
                            .p_size = 15,
                            .p_mfgr = "M2"}};
  int part_len = sizeof(part) / sizeof(part[0]);
  partsupp_t partsupp[] = {
      (partsupp_t){
          .ps_partkey = 1000, .ps_suppkey = 100, .ps_supplycost = 10.0},
      (partsupp_t){
          .ps_partkey = 1000, .ps_suppkey = 200, .ps_supplycost = 15.0}};
  int partsupp_len = sizeof(partsupp) / sizeof(partsupp[0]);
  nation_list_t tmp4 = create_nation_list(region_len * nation_len);
  int tmp5 = 0;
  for (int tmp6 = 0; tmp6 < region_len; tmp6++) {
    region_t r = region[tmp6];
    for (int tmp7 = 0; tmp7 < nation_len; tmp7++) {
      nation_t n = nation[tmp7];
      if (!(n.n_regionkey == r.r_regionkey)) {
        continue;
      }
      if (!((strcmp(r.r_name, "EUROPE") == 0))) {
        continue;
      }
      tmp4.data[tmp5] = n;
      tmp5++;
    }
  }
  tmp4.len = tmp5;
  nation_list_t europe_nations = tmp4;
  europe_suppliers_item_list_t tmp8 =
      create_europe_suppliers_item_list(supplier_len * europe_nations.len);
  int tmp9 = 0;
  for (int tmp10 = 0; tmp10 < supplier_len; tmp10++) {
    supplier_t s = supplier[tmp10];
    for (int tmp11 = 0; tmp11 < europe_nations.len; tmp11++) {
      nation_t n = europe_nations.data[tmp11];
      if (!(s.s_nationkey == n.n_nationkey)) {
        continue;
      }
      tmp8.data[tmp9] = (europe_suppliers_item_t){.s = s, .n = n};
      tmp9++;
    }
  }
  tmp8.len = tmp9;
  europe_suppliers_item_list_t europe_suppliers = tmp8;
  part_list_t tmp12 = create_part_list(part_len);
  int tmp13 = 0;
  for (int tmp14 = 0; tmp14 < part_len; tmp14++) {
    part_t p = part[tmp14];
    if (!(p.p_size == 15 && p.p_type == "LARGE BRASS")) {
      continue;
    }
    tmp12.data[tmp13] = p;
    tmp13++;
  }
  tmp12.len = tmp13;
  part_list_t target_parts = tmp12;
  target_partsupp_item_list_t tmp15 = create_target_partsupp_item_list(
      partsupp_len * target_parts.len * europe_suppliers.len);
  int tmp16 = 0;
  for (int tmp17 = 0; tmp17 < partsupp_len; tmp17++) {
    partsupp_t ps = partsupp[tmp17];
    for (int tmp18 = 0; tmp18 < target_parts.len; tmp18++) {
      part_t p = target_parts.data[tmp18];
      if (!(ps.ps_partkey == p.p_partkey)) {
        continue;
      }
      for (int tmp19 = 0; tmp19 < europe_suppliers.len; tmp19++) {
        europe_suppliers_item_t s = europe_suppliers.data[tmp19];
        if (!(ps.ps_suppkey == s.s.s_suppkey)) {
          continue;
        }
        tmp15.data[tmp16] =
            (target_partsupp_item_t){.s_acctbal = s.s.s_acctbal,
                                     .s_name = s.s.s_name,
                                     .n_name = s.n.n_name,
                                     .p_partkey = p.p_partkey,
                                     .p_mfgr = p.p_mfgr,
                                     .s_address = s.s.s_address,
                                     .s_phone = s.s.s_phone,
                                     .s_comment = s.s.s_comment,
                                     .ps_supplycost = ps.ps_supplycost};
        tmp16++;
      }
    }
  }
  tmp15.len = tmp16;
  target_partsupp_item_list_t target_partsupp = tmp15;
  list_float tmp20 = list_float_create(target_partsupp.len);
  int tmp21 = 0;
  for (int tmp22 = 0; tmp22 < target_partsupp.len; tmp22++) {
    target_partsupp_item_t x = target_partsupp.data[tmp22];
    tmp20.data[tmp21] = x.ps_supplycost;
    tmp21++;
  }
  tmp20.len = tmp21;
  list_float costs = tmp20;
  double min_cost = _min_float(costs);
  target_partsupp_item_list_t tmp23 =
      create_target_partsupp_item_list(target_partsupp.len);
  double *tmp26 = (double *)malloc(sizeof(double) * target_partsupp.len);
  int tmp24 = 0;
  for (int tmp25 = 0; tmp25 < target_partsupp.len; tmp25++) {
    target_partsupp_item_t x = target_partsupp.data[tmp25];
    if (!(x.ps_supplycost == min_cost)) {
      continue;
    }
    tmp23.data[tmp24] = x;
    tmp26[tmp24] = (-x.s_acctbal);
    tmp24++;
  }
  tmp23.len = tmp24;
  for (int i29 = 0; i29 < tmp24 - 1; i29++) {
    for (int i30 = i29 + 1; i30 < tmp24; i30++) {
      if (tmp26[i29] > tmp26[i30]) {
        double tmp27 = tmp26[i29];
        tmp26[i29] = tmp26[i30];
        tmp26[i30] = tmp27;
        target_partsupp_item_t tmp28 = tmp23.data[i29];
        tmp23.data[i29] = tmp23.data[i30];
        tmp23.data[i30] = tmp28;
      }
    }
  }
  target_partsupp_item_list_t result = tmp23;
  printf("[");
  for (int i31 = 0; i31 < result.len; i31++) {
    if (i31 > 0)
      printf(",");
    target_partsupp_item_t it = result.data[i31];
    printf("{");
    _json_string("s_acctbal");
    printf(":");
    _json_float(it.s_acctbal);
    printf(",");
    _json_string("s_name");
    printf(":");
    _json_string(it.s_name);
    printf(",");
    _json_string("n_name");
    printf(":");
    _json_string(it.n_name);
    printf(",");
    _json_string("p_partkey");
    printf(":");
    _json_int(it.p_partkey);
    printf(",");
    _json_string("p_mfgr");
    printf(":");
    _json_string(it.p_mfgr);
    printf(",");
    _json_string("s_address");
    printf(":");
    _json_string(it.s_address);
    printf(",");
    _json_string("s_phone");
    printf(":");
    _json_string(it.s_phone);
    printf(",");
    _json_string("s_comment");
    printf(":");
    _json_string(it.s_comment);
    printf(",");
    _json_string("ps_supplycost");
    printf(":");
    _json_float(it.ps_supplycost);
    printf("}");
  }
  printf("]");
  test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part_result =
      result;
  test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part();
  free(target_parts.data);
  free(costs.data);
  free(result.data);
  return 0;
}
int main() { return _mochi_main(); }
