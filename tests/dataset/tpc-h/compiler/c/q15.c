// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:26:22Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static double _max_float(list_float v) {
  if (v.len == 0)
    return 0;
  double m = v.data[0];
  for (int i = 1; i < v.len; i++)
    if (v.data[i] > m)
      m = v.data[i];
  return m;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  int key;
  list_int items;
} _GroupInt;
typedef struct {
  int len;
  int cap;
  _GroupInt *data;
} list_group_int;
static list_group_int _group_by_int(list_int src) {
  list_group_int res;
  res.len = 0;
  res.cap = 0;
  res.data = NULL;
  for (int i = 0; i < src.len; i++) {
    int key = src.data[i];
    int idx = -1;
    for (int j = 0; j < res.len; j++)
      if (res.data[j].key == key) {
        idx = j;
        break;
      }
    if (idx == -1) {
      if (res.len >= res.cap) {
        res.cap = res.cap ? res.cap * 2 : 4;
        res.data = (_GroupInt *)realloc(res.data, sizeof(_GroupInt) * res.cap);
      }
      res.data[res.len].key = key;
      res.data[res.len].items = list_int_create(0);
      idx = res.len++;
    }
    _GroupInt *g = &res.data[idx];
    g->items.data =
        (int *)realloc(g->items.data, sizeof(int) * (g->items.len + 1));
    g->items.data[g->items.len++] = src.data[i];
  }
  return res;
}
static char *start_date = "1996-01-01";
static char *end_date = "1996-04-01";

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  char *s_phone;
  double total_revenue;
} tmp1_t;
typedef struct {
  int len;
  tmp1_t *data;
} tmp1_list_t;
tmp1_list_t create_tmp1_list(int len) {
  tmp1_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp1_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  char *s_phone;
} supplier_t;
typedef struct {
  int len;
  supplier_t *data;
} supplier_list_t;
supplier_list_t create_supplier_list(int len) {
  supplier_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(supplier_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int l_suppkey;
  double l_extendedprice;
  double l_discount;
  char *l_shipdate;
} lineitem_t;
typedef struct {
  int len;
  lineitem_t *data;
} lineitem_list_t;
lineitem_list_t create_lineitem_list(int len) {
  lineitem_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(lineitem_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int supplier_no;
  double total_revenue;
} revenue0_item_t;
typedef struct {
  int len;
  revenue0_item_t *data;
} revenue0_item_list_t;
revenue0_item_list_t create_revenue0_item_list(int len) {
  revenue0_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(revenue0_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int s_suppkey;
  char *s_name;
  char *s_address;
  char *s_phone;
  double total_revenue;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static list_int test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result;
static void test_Q15_returns_top_revenue_supplier_s__for_Q1_1996() {
  double rev = 1000.0 * 0.9 + 500.0;
  tmp1_t tmp1[] = {(tmp1_t){.s_suppkey = 100,
                            .s_name = "Best Supplier",
                            .s_address = "123 Market St",
                            .s_phone = "123-456",
                            .total_revenue = rev}};
  int tmp1_len = sizeof(tmp1) / sizeof(tmp1[0]);
  int tmp2 = 1;
  if (test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result.len !=
      tmp1_len) {
    tmp2 = 0;
  } else {
    for (int i3 = 0;
         i3 < test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result.len;
         i3++) {
      if (test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result
              .data[i3] != tmp1[i3]) {
        tmp2 = 0;
        break;
      }
    }
  }
  if (!(tmp2)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int _mochi_main() {
  supplier_t supplier[] = {(supplier_t){.s_suppkey = 100,
                                        .s_name = "Best Supplier",
                                        .s_address = "123 Market St",
                                        .s_phone = "123-456"},
                           (supplier_t){.s_suppkey = 200,
                                        .s_name = "Second Supplier",
                                        .s_address = "456 Elm St",
                                        .s_phone = "987-654"}};
  int supplier_len = sizeof(supplier) / sizeof(supplier[0]);
  lineitem_t lineitem[] = {(lineitem_t){.l_suppkey = 100,
                                        .l_extendedprice = 1000.0,
                                        .l_discount = 0.1,
                                        .l_shipdate = "1996-01-15"},
                           (lineitem_t){.l_suppkey = 100,
                                        .l_extendedprice = 500.0,
                                        .l_discount = 0.0,
                                        .l_shipdate = "1996-03-20"},
                           (lineitem_t){.l_suppkey = 200,
                                        .l_extendedprice = 800.0,
                                        .l_discount = 0.05,
                                        .l_shipdate = "1995-12-30"}};
  int lineitem_len = sizeof(lineitem) / sizeof(lineitem[0]);
  lineitem_list_t tmp4 = create_lineitem_list(lineitem_len);
  int tmp5_data[lineitem_len];
  list_int tmp5 = {0, tmp5_data};
  int tmp6 = 0;
  for (int i7 = 0; i7 < lineitem_len; i7++) {
    lineitem_t l = lineitem[i7];
    if (!((strcmp(l.l_shipdate, start_date) >= 0) && l.l_shipdate < end_date)) {
      continue;
    }
    tmp4.data[tmp6] = l;
    tmp5.data[tmp6] = l.l_suppkey;
    tmp6++;
  }
  tmp4.len = tmp6;
  tmp5.len = tmp6;
  list_group_int tmp8 = _group_by_int(tmp5);
  revenue0_item_list_t tmp9 = create_revenue0_item_list(tmp8.len);
  int tmp10 = 0;
  for (int gi = 0; gi < tmp8.len; gi++) {
    _GroupInt _gp = tmp8.data[gi];
    lineitem_list_t tmp11 = create_lineitem_list(_gp.items.len);
    for (int i12 = 0; i12 < _gp.items.len; i12++) {
      tmp11.data[i12] = tmp4.data[_gp.items.data[i12]];
    }
    tmp11.len = _gp.items.len;
    struct {
      int key;
      lineitem_list_t items;
    } g = {_gp.key, tmp11};
    list_float tmp13 = list_float_create(g.items.len);
    int tmp14 = 0;
    for (int i15 = 0; i15 < g.items.len; i15++) {
      lineitem_t x = g.items.data[i15];
      tmp13.data[tmp14] = x.l_extendedprice * (1 - x.l_discount);
      tmp14++;
    }
    tmp13.len = tmp14;
    tmp9.data[tmp10] = (revenue0_item_t){.supplier_no = g.key,
                                         .total_revenue = _sum_float(tmp13)};
    tmp10++;
  }
  tmp9.len = tmp10;
  revenue0_item_list_t revenue0 = tmp9;
  list_float tmp16 = list_float_create(revenue0.len);
  int tmp17 = 0;
  for (int tmp18 = 0; tmp18 < revenue0.len; tmp18++) {
    revenue0_item_t x = revenue0.data[tmp18];
    tmp16.data[tmp17] = x.total_revenue;
    tmp17++;
  }
  tmp16.len = tmp17;
  list_float revenues = tmp16;
  double max_revenue = _max_float(revenues);
  result_item_list_t tmp19 =
      create_result_item_list(supplier_len * revenue0.len);
  int tmp20 = 0;
  for (int tmp21 = 0; tmp21 < supplier_len; tmp21++) {
    supplier_t s = supplier[tmp21];
    for (int tmp22 = 0; tmp22 < revenue0.len; tmp22++) {
      revenue0_item_t r = revenue0.data[tmp22];
      if (!(s.s_suppkey == r.supplier_no)) {
        continue;
      }
      if (!(r.total_revenue == max_revenue)) {
        continue;
      }
      tmp19.data[tmp20] = (result_item_t){.s_suppkey = s.s_suppkey,
                                          .s_name = s.s_name,
                                          .s_address = s.s_address,
                                          .s_phone = s.s_phone,
                                          .total_revenue = r.total_revenue};
      tmp20++;
    }
  }
  tmp19.len = tmp20;
  result_item_list_t result = tmp19;
  printf("[");
  for (int i23 = 0; i23 < result.len; i23++) {
    if (i23 > 0)
      printf(",");
    result_item_t it = result.data[i23];
    printf("{");
    _json_string("s_suppkey");
    printf(":");
    _json_int(it.s_suppkey);
    printf(",");
    _json_string("s_name");
    printf(":");
    _json_string(it.s_name);
    printf(",");
    _json_string("s_address");
    printf(":");
    _json_string(it.s_address);
    printf(",");
    _json_string("s_phone");
    printf(":");
    _json_string(it.s_phone);
    printf(",");
    _json_string("total_revenue");
    printf(":");
    _json_float(it.total_revenue);
    printf("}");
  }
  printf("]");
  test_Q15_returns_top_revenue_supplier_s__for_Q1_1996_result = result;
  test_Q15_returns_top_revenue_supplier_s__for_Q1_1996();
  free(revenues.data);
  return 0;
}
int main() { return _mochi_main(); }
