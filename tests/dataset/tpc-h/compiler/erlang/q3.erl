#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2020-01-02T15:04:05Z
% q3.erl - generated from q3.mochi

main(_) ->
    Customer = [#{c_custkey => 1, c_mktsegment => "BUILDING"}, #{c_custkey => 2, c_mktsegment => "AUTOMOBILE"}],
    Orders = [#{o_orderkey => 100, o_custkey => 1, o_orderdate => "1995-03-14", o_shippriority => 1}, #{o_orderkey => 200, o_custkey => 2, o_orderdate => "1995-03-10", o_shippriority => 2}],
    Lineitem = [#{l_orderkey => 100, l_extendedprice => 1000, l_discount => 0.05, l_shipdate => "1995-03-16"}, #{l_orderkey => 100, l_extendedprice => 500, l_discount => 0, l_shipdate => "1995-03-20"}, #{l_orderkey => 200, l_extendedprice => 1000, l_discount => 0.1, l_shipdate => "1995-03-14"}],
    Building_customers = [C || C <- Customer, (maps:get(c_mktsegment, C, undefined) == "BUILDING")],
    Valid_orders = [O || O <- Orders, C <- Building_customers, (maps:get(o_custkey, O, undefined) == mochi_get(c_custkey, C)), (maps:get(o_orderdate, O, undefined) < "1995-03-15")],
    Valid_lineitems = [L || L <- Lineitem, (maps:get(l_shipdate, L, undefined) > "1995-03-15")],
    Order_line_join = [V || {_, V} <- lists:keysort(1, [{[-lists:sum([(maps:get(l_extendedprice, maps:get(l, R, undefined), undefined) * ((1 - maps:get(l_discount, maps:get(l, R, undefined), undefined)))) || R <- Val0]), maps:get(o_orderdate, Key0, undefined)], #{l_orderkey => maps:get(o_orderkey, Key0, undefined), revenue => lists:sum([(maps:get(l_extendedprice, maps:get(l, R, undefined), undefined) * ((1 - maps:get(l_discount, maps:get(l, R, undefined), undefined)))) || R <- Val0]), o_orderdate => maps:get(o_orderdate, Key0, undefined), o_shippriority => maps:get(o_shippriority, Key0, undefined)}} || {Key0, Val0} <- maps:to_list(lists:foldl(fun({Key0, Val0}, Acc0) -> L = maps:get(Key0, Acc0, []), maps:put(Key0, [Val0 | L], Acc0) end, #{}, [{#{o_orderkey => mochi_get(o_orderkey, O), o_orderdate => mochi_get(o_orderdate, O), o_shippriority => mochi_get(o_shippriority, O)}, #{o => O, l => L}} || O <- Valid_orders, L <- Valid_lineitems, (mochi_get(l_orderkey, L) == mochi_get(o_orderkey, O))]))])],
    mochi_json(Order_line_join),
    (case (Order_line_join == [#{l_orderkey => 100, revenue => ((1000 * 0.95) + 500), o_orderdate => "1995-03-14", o_shippriority => 1}]) of true -> ok; _ -> erlang:error(test_failed) end).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
    
    mochi_escape_json([]) -> [];
    mochi_escape_json([H|T]) ->
        E = case H of
            $\ -> "\\";
            $" -> "\"";
            _ -> [H]
        end,
        E ++ mochi_escape_json(T).
    
    mochi_to_json(true) -> "true";
    mochi_to_json(false) -> "false";
    mochi_to_json(V) when is_integer(V); is_float(V) -> lists:flatten(io_lib:format("~p", [V]));
    mochi_to_json(V) when is_binary(V) -> "\"" ++ mochi_escape_json(binary_to_list(V)) ++ "\"";
    mochi_to_json(V) when is_list(V), (V =:= [] orelse is_integer(hd(V))) -> "\"" ++ mochi_escape_json(V) ++ "\"";
    mochi_to_json(V) when is_list(V) -> "[" ++ lists:join(",", [mochi_to_json(X) || X <- V]) ++ "]";
    mochi_to_json(V) when is_map(V) -> "{" ++ lists:join(",", ["\"" ++ atom_to_list(K) ++ "\":" ++ mochi_to_json(Val) || {K,Val} <- maps:to_list(V)]) ++ "}".
    
    mochi_json(V) -> io:format("~s
", [mochi_to_json(V)]).
