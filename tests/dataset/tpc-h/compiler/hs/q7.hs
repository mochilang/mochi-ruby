-- Generated by Mochi compiler v0.10.25 on 2025-07-13T07:10:13Z
-- Code generated by Mochi compiler; DO NOT EDIT.
{-# LANGUAGE DeriveGeneric #-}

module Main where

import qualified Data.Aeson as Aeson
import qualified Data.ByteString.Lazy.Char8 as BSL
import Data.List (intercalate, isInfixOf, isPrefixOf)
import qualified Data.List as List
import qualified Data.Map as Map
import Data.Maybe (fromMaybe)

forLoop :: Int -> Int -> (Int -> Maybe a) -> Maybe a
forLoop start end f = go start
  where
    go i
      | i < end =
          case f i of
            Just v -> Just v
            Nothing -> go (i + 1)
      | otherwise = Nothing

whileLoop :: (() -> Bool) -> (() -> Maybe a) -> Maybe a
whileLoop cond body = go ()
  where
    go _
      | cond () =
          case body () of
            Just v -> Just v
            Nothing -> go ()
      | otherwise = Nothing

avg :: (Integral a) => [a] -> a
avg xs
  | null xs = 0
  | otherwise = div (sum xs) (fromIntegral (length xs))

data MGroup k a = MGroup {key :: k, items :: [a]} deriving (Show)

_group_by :: (Ord k) => [a] -> (a -> k) -> [MGroup k a]
_group_by src keyfn =
  let go [] m order = (m, order)
      go (x : xs) m order =
        let k = keyfn x
         in case Map.lookup k m of
              Just is -> go xs (Map.insert k (is ++ [x]) m) order
              Nothing -> go xs (Map.insert k [x] m) (order ++ [k])
      (m, order) = go src Map.empty []
   in [MGroup k (fromMaybe [] (Map.lookup k m)) | k <- order]

_indexString :: String -> Int -> String
_indexString s i =
  let idx = if i < 0 then i + length s else i
   in if idx < 0 || idx >= length s
        then error "index out of range"
        else [s !! idx]

_append :: [a] -> a -> [a]
_append xs x = xs ++ [x]

_input :: IO String
_input = getLine

_readInput :: Maybe String -> IO String
_readInput Nothing = getContents
_readInput (Just p)
  | null p || p == "-" = getContents
  | otherwise = readFile p

_writeOutput :: Maybe String -> String -> IO ()
_writeOutput mp text = case mp of
  Nothing -> putStr text
  Just p
    | null p || p == "-" -> putStr text
    | otherwise -> writeFile p text

_split :: Char -> String -> [String]
_split _ "" = [""]
_split d s =
  let (h, t) = break (== d) s
   in h : case t of
        [] -> []
        (_ : rest) -> _split d rest

_parseCSV :: String -> Bool -> Char -> [Map.Map String String]
_parseCSV text header delim =
  let ls = filter (not . null) (lines text)
   in if null ls
        then []
        else
          let heads =
                if header
                  then _split delim (head ls)
                  else ["c" ++ show i | i <- [0 .. length (_split delim (head ls)) - 1]]
              start = if header then 1 else 0
              row line =
                let parts = _split delim line
                 in Map.fromList
                      [ (heads !! j, if j < length parts then parts !! j else "")
                        | j <- [0 .. length heads - 1]
                      ]
           in map row (drop start ls)

_json :: (Aeson.ToJSON a) => a -> IO ()
_json v = BSL.putStrLn (Aeson.encode v)

expect :: Bool -> IO ()
expect True = pure ()
expect False = error "expect failed"

nation = [Map.fromList [("n_nationkey", (1 :: Int)), ("n_name", "FRANCE")], Map.fromList [("n_nationkey", (2 :: Int)), ("n_name", "GERMANY")]]

supplier = [Map.fromList [("s_suppkey", (100 :: Int)), ("s_nationkey", (1 :: Int))]]

customer = [Map.fromList [("c_custkey", (200 :: Int)), ("c_nationkey", (2 :: Int))]]

orders = [Map.fromList [("o_orderkey", (1000 :: Int)), ("o_custkey", (200 :: Int))]]

lineitem = [Map.fromList [("l_orderkey", (1000 :: Int)), ("l_suppkey", (100 :: Int)), ("l_extendedprice", (1000.0 :: Double)), ("l_discount", (0.1 :: Double)), ("l_shipdate", "1995-06-15")], Map.fromList [("l_orderkey", (1000 :: Int)), ("l_suppkey", (100 :: Int)), ("l_extendedprice", (800.0 :: Double)), ("l_discount", (0.05 :: Double)), ("l_shipdate", "1997-01-01")]]

start_date = "1995-01-01"

end_date = "1996-12-31"

nation1 = "FRANCE"

nation2 = "GERMANY"

result = map snd (List.sortOn fst [([supp_nation, cust_nation, l_year], Map.fromList [("supp_nation", fromMaybe (error "missing") (Map.lookup "supp_nation" (key (g)))), ("cust_nation", fromMaybe (error "missing") (Map.lookup "cust_nation" (key (g)))), ("l_year", fromMaybe (error "missing") (Map.lookup "l_year" (key (g)))), ("revenue", (sum [(fromMaybe (error "missing") (Map.lookup "l_extendedprice" (fromMaybe (error "missing") (Map.lookup "l" (x)))) * ((1 - _asInt (fromMaybe (error "missing") (Map.lookup "l_discount" (fromMaybe (error "missing") (Map.lookup "l" (x)))))))) | x <- g] :: Double))]) | g <- _group_by [(l, o, c, s, n1, n2) | l <- lineitem, o <- orders, c <- customer, s <- supplier, n1 <- nation, n2 <- nation, (fromMaybe (error "missing") (Map.lookup "o_orderkey" (o)) == fromMaybe (error "missing") (Map.lookup "l_orderkey" (l))), (fromMaybe (error "missing") (Map.lookup "c_custkey" (c)) == fromMaybe (error "missing") (Map.lookup "o_custkey" (o))), (fromMaybe (error "missing") (Map.lookup "s_suppkey" (s)) == fromMaybe (error "missing") (Map.lookup "l_suppkey" (l))), (fromMaybe (error "missing") (Map.lookup "n_nationkey" (n1)) == fromMaybe (error "missing") (Map.lookup "s_nationkey" (s))), (fromMaybe (error "missing") (Map.lookup "n_nationkey" (n2)) == fromMaybe (error "missing") (Map.lookup "c_nationkey" (c))), ((((((fromMaybe (error "missing") (Map.lookup "l_shipdate" l) >= start_date) && fromMaybe (error "missing") (Map.lookup "l_shipdate" l)) <= end_date) && ((((fromMaybe (error "missing") (Map.lookup "n_name" n1) == nation1) && fromMaybe (error "missing") (Map.lookup "n_name" n2)) == nation2))) || ((((fromMaybe (error "missing") (Map.lookup "n_name" n1) == nation2) && fromMaybe (error "missing") (Map.lookup "n_name" n2)) == nation1))))] (\(l, o, c, s, n1, n2) -> Map.fromList [("supp_nation", fromMaybe (error "missing") (Map.lookup "n_name" n1)), ("cust_nation", fromMaybe (error "missing") (Map.lookup "n_name" n2)), ("l_year", take (4 - 0) (drop 0 fromMaybe (error "missing") (Map.lookup "l_shipdate" l)))])])

test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year :: IO ()
test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year = do
  expect ((result == [Map.fromList [("supp_nation", "FRANCE"), ("cust_nation", "GERMANY"), ("l_year", "1995"), ("revenue", (900.0 :: Double))]]))

main :: IO ()
main = do
  _json result
  test_Q7_computes_revenue_between_FRANCE_and_GERMANY_by_year
