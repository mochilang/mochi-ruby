-- Generated by Mochi compiler v0.10.25 on 2025-07-13T07:10:13Z
-- Code generated by Mochi compiler; DO NOT EDIT.
{-# LANGUAGE DeriveGeneric #-}

module Main where

import qualified Data.Aeson as Aeson
import qualified Data.ByteString.Lazy.Char8 as BSL
import Data.List (intercalate, isInfixOf, isPrefixOf)
import qualified Data.List as List
import qualified Data.Map as Map
import Data.Maybe (fromMaybe)

forLoop :: Int -> Int -> (Int -> Maybe a) -> Maybe a
forLoop start end f = go start
  where
    go i
      | i < end =
          case f i of
            Just v -> Just v
            Nothing -> go (i + 1)
      | otherwise = Nothing

whileLoop :: (() -> Bool) -> (() -> Maybe a) -> Maybe a
whileLoop cond body = go ()
  where
    go _
      | cond () =
          case body () of
            Just v -> Just v
            Nothing -> go ()
      | otherwise = Nothing

avg :: (Integral a) => [a] -> a
avg xs
  | null xs = 0
  | otherwise = div (sum xs) (fromIntegral (length xs))

data MGroup k a = MGroup {key :: k, items :: [a]} deriving (Show)

_group_by :: (Ord k) => [a] -> (a -> k) -> [MGroup k a]
_group_by src keyfn =
  let go [] m order = (m, order)
      go (x : xs) m order =
        let k = keyfn x
         in case Map.lookup k m of
              Just is -> go xs (Map.insert k (is ++ [x]) m) order
              Nothing -> go xs (Map.insert k [x] m) (order ++ [k])
      (m, order) = go src Map.empty []
   in [MGroup k (fromMaybe [] (Map.lookup k m)) | k <- order]

_indexString :: String -> Int -> String
_indexString s i =
  let idx = if i < 0 then i + length s else i
   in if idx < 0 || idx >= length s
        then error "index out of range"
        else [s !! idx]

_append :: [a] -> a -> [a]
_append xs x = xs ++ [x]

_input :: IO String
_input = getLine

_readInput :: Maybe String -> IO String
_readInput Nothing = getContents
_readInput (Just p)
  | null p || p == "-" = getContents
  | otherwise = readFile p

_writeOutput :: Maybe String -> String -> IO ()
_writeOutput mp text = case mp of
  Nothing -> putStr text
  Just p
    | null p || p == "-" -> putStr text
    | otherwise -> writeFile p text

_split :: Char -> String -> [String]
_split _ "" = [""]
_split d s =
  let (h, t) = break (== d) s
   in h : case t of
        [] -> []
        (_ : rest) -> _split d rest

_parseCSV :: String -> Bool -> Char -> [Map.Map String String]
_parseCSV text header delim =
  let ls = filter (not . null) (lines text)
   in if null ls
        then []
        else
          let heads =
                if header
                  then _split delim (head ls)
                  else ["c" ++ show i | i <- [0 .. length (_split delim (head ls)) - 1]]
              start = if header then 1 else 0
              row line =
                let parts = _split delim line
                 in Map.fromList
                      [ (heads !! j, if j < length parts then parts !! j else "")
                        | j <- [0 .. length heads - 1]
                      ]
           in map row (drop start ls)

_json :: (Aeson.ToJSON a) => a -> IO ()
_json v = BSL.putStrLn (Aeson.encode v)

expect :: Bool -> IO ()
expect True = pure ()
expect False = error "expect failed"

region = [Map.fromList [("r_regionkey", (1 :: Int)), ("r_name", "EUROPE")], Map.fromList [("r_regionkey", (2 :: Int)), ("r_name", "ASIA")]]

nation = [Map.fromList [("n_nationkey", (10 :: Int)), ("n_regionkey", (1 :: Int)), ("n_name", "FRANCE")], Map.fromList [("n_nationkey", (20 :: Int)), ("n_regionkey", (2 :: Int)), ("n_name", "CHINA")]]

supplier = [Map.fromList [("s_suppkey", (100 :: Int)), ("s_name", "BestSupplier"), ("s_address", "123 Rue"), ("s_nationkey", (10 :: Int)), ("s_phone", "123"), ("s_acctbal", (1000.0 :: Double)), ("s_comment", "Fast and reliable")], Map.fromList [("s_suppkey", (200 :: Int)), ("s_name", "AltSupplier"), ("s_address", "456 Way"), ("s_nationkey", (20 :: Int)), ("s_phone", "456"), ("s_acctbal", (500.0 :: Double)), ("s_comment", "Slow")]]

part = [Map.fromList [("p_partkey", (1000 :: Int)), ("p_type", "LARGE BRASS"), ("p_size", (15 :: Int)), ("p_mfgr", "M1")], Map.fromList [("p_partkey", (2000 :: Int)), ("p_type", "SMALL COPPER"), ("p_size", (15 :: Int)), ("p_mfgr", "M2")]]

partsupp = [Map.fromList [("ps_partkey", (1000 :: Int)), ("ps_suppkey", (100 :: Int)), ("ps_supplycost", (10.0 :: Double))], Map.fromList [("ps_partkey", (1000 :: Int)), ("ps_suppkey", (200 :: Int)), ("ps_supplycost", (15.0 :: Double))]]

europe_nations = [n | r <- region, n <- nation, (fromMaybe (error "missing") (Map.lookup "n_regionkey" (n)) == fromMaybe (error "missing") (Map.lookup "r_regionkey" (r))), (fromMaybe (error "missing") (Map.lookup "r_name" r) == "EUROPE")]

europe_suppliers = [Map.fromList [("s", s), ("n", n)] | s <- supplier, n <- europe_nations, (fromMaybe (error "missing") (Map.lookup "s_nationkey" (s)) == fromMaybe (error "missing") (Map.lookup "n_nationkey" (n)))]

target_parts = [p | p <- filter (\p -> (((_asInt (fromMaybe (error "missing") (Map.lookup "p_size" p)) == 15) && fromMaybe (error "missing") (Map.lookup "p_type" p)) == "LARGE BRASS")) part]

target_partsupp = [Map.fromList [("s_acctbal", fromMaybe (error "missing") (Map.lookup "s_acctbal" fromMaybe (error "missing") (Map.lookup "s" s))), ("s_name", fromMaybe (error "missing") (Map.lookup "s_name" fromMaybe (error "missing") (Map.lookup "s" s))), ("n_name", fromMaybe (error "missing") (Map.lookup "n_name" fromMaybe (error "missing") (Map.lookup "n" s))), ("p_partkey", fromMaybe (error "missing") (Map.lookup "p_partkey" p)), ("p_mfgr", fromMaybe (error "missing") (Map.lookup "p_mfgr" p)), ("s_address", fromMaybe (error "missing") (Map.lookup "s_address" fromMaybe (error "missing") (Map.lookup "s" s))), ("s_phone", fromMaybe (error "missing") (Map.lookup "s_phone" fromMaybe (error "missing") (Map.lookup "s" s))), ("s_comment", fromMaybe (error "missing") (Map.lookup "s_comment" fromMaybe (error "missing") (Map.lookup "s" s))), ("ps_supplycost", fromMaybe (error "missing") (Map.lookup "ps_supplycost" ps))] | ps <- partsupp, p <- target_parts, s <- europe_suppliers, (fromMaybe (error "missing") (Map.lookup "ps_partkey" (ps)) == fromMaybe (error "missing") (Map.lookup "p_partkey" (p))), (fromMaybe (error "missing") (Map.lookup "ps_suppkey" (ps)) == fromMaybe (error "missing") (Map.lookup "s_suppkey" (fromMaybe (error "missing") (Map.lookup "s" (s)))))]

costs = [fromMaybe (error "missing") (Map.lookup "ps_supplycost" x) | x <- target_partsupp]

min_cost = minimum costs

result = map snd (List.sortOn fst [((-(_asInt (fromMaybe (error "missing") (Map.lookup "s_acctbal" (x))))), x) | x <- target_partsupp, (fromMaybe (error "missing") (Map.lookup "ps_supplycost" x) == min_cost)])

test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part :: IO ()
test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part = do
  expect ((result == [Map.fromList [("s_acctbal", (1000.0 :: Double)), ("s_name", "BestSupplier"), ("n_name", "FRANCE"), ("p_partkey", (1000 :: Int)), ("p_mfgr", "M1"), ("s_address", "123 Rue"), ("s_phone", "123"), ("s_comment", "Fast and reliable"), ("ps_supplycost", (10.0 :: Double))]]))

main :: IO ()
main = do
  _json result
  test_Q2_returns_only_supplier_with_min_cost_in_Europe_for_brass_part
