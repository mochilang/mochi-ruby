# Generated by Mochi compiler v0.10.27 on 2025-07-17T18:24:25Z
defmodule Main do
  def main do
    # nation :: list(map())
    nation = [%{n_nationkey: 1, n_name: "SAUDI ARABIA"}, %{n_nationkey: 2, n_name: "FRANCE"}]
    # supplier :: list(map())
    supplier = [
      %{s_suppkey: 100, s_name: "Desert Trade", s_nationkey: 1},
      %{s_suppkey: 200, s_name: "Euro Goods", s_nationkey: 2}
    ]

    # orders :: list(map())
    orders = [%{o_orderkey: 500, o_orderstatus: "F"}, %{o_orderkey: 600, o_orderstatus: "O"}]
    # lineitem :: list(map())
    lineitem = [
      %{l_orderkey: 500, l_suppkey: 100, l_receiptdate: "1995-04-15", l_commitdate: "1995-04-10"},
      %{l_orderkey: 500, l_suppkey: 200, l_receiptdate: "1995-04-12", l_commitdate: "1995-04-12"},
      %{l_orderkey: 600, l_suppkey: 100, l_receiptdate: "1995-05-01", l_commitdate: "1995-04-25"}
    ]

    # result :: list(map())
    result =
      (fn ->
         src = supplier

         rows =
           _query(
             src,
             [
               %{items: lineitem, on: fn s, l1 -> s.s_suppkey == l1.l_suppkey end},
               %{items: orders, on: fn s, l1, o -> o.o_orderkey == l1.l_orderkey end},
               %{items: nation, on: fn s, l1, o, n -> n.n_nationkey == s.s_nationkey end}
             ],
             %{
               select: fn s, l1, o, n ->
                 _merge_map(_merge_map(_merge_map(_merge_map(s, l1), o), n), %{
                   s: s,
                   l1: l1,
                   o: o,
                   n: n
                 })
               end,
               where: fn [s, l1, o, n] ->
                 o.o_orderstatus == "F" && l1.l_receiptdate > l1.l_commitdate &&
                   n.n_name == "SAUDI ARABIA" &&
                   !length(
                     for x <- lineitem,
                         x.l_orderkey == l1.l_orderkey && x.l_suppkey != l1.l_suppkey &&
                           x.l_receiptdate > x.l_commitdate,
                         do: x
                   ) > 0
               end
             }
           )

         groups = _group_by(rows, fn %{s: s, l1: l1, o: o, n: n} -> s.s_name end)
         items = groups
         items = Enum.sort_by(items, fn g -> [-length(g.items), g.key] end)
         Enum.map(items, fn g -> %{s_name: g.key, numwait: length(g.items)} end)
       end).()

    _json(result)
  end

  defmodule Group do
    defstruct key: nil, items: []

    def fetch(g, k) do
      case k do
        :key -> {:ok, g.key}
        :items -> {:ok, g.items}
        _ -> :error
      end
    end

    def get_and_update(g, k, f) do
      case k do
        :key ->
          {v, nv} = f.(g.key)
          {v, %{g | key: nv}}

        :items ->
          {v, nv} = f.(g.items)
          {v, %{g | items: nv}}

        _ ->
          {nil, g}
      end
    end
  end

  defp _group_by(src, keyfn) do
    {groups, order} =
      Enum.reduce(src, {%{}, []}, fn it, {groups, order} ->
        key =
          if is_list(it) do
            arity = :erlang.fun_info(keyfn, :arity) |> elem(1)
            if arity == 1, do: keyfn.(it), else: apply(keyfn, it)
          else
            keyfn.(it)
          end

        ks = :erlang.phash2(key)

        {groups, order} =
          if Map.has_key?(groups, ks) do
            {groups, order}
          else
            {Map.put(groups, ks, %Group{key: key}), order ++ [ks]}
          end

        val = if is_list(it) and length(it) == 1, do: hd(it), else: it
        groups = Map.update!(groups, ks, fn g -> %{g | items: g.items ++ [val]} end)
        {groups, order}
      end)

    Enum.map(order, fn k -> groups[k] end)
  end

  defp _escape_json(<<>>), do: ""
  defp _escape_json(<<"\\", rest::binary>>), do: "\\\\" <> _escape_json(rest)
  defp _escape_json(<<"\"", rest::binary>>), do: "\\\"" <> _escape_json(rest)
  defp _escape_json(<<c::binary-size(1), rest::binary>>), do: c <> _escape_json(rest)
  defp _to_json(v) when is_binary(v), do: "\"" <> _escape_json(v) <> "\""
  defp _to_json(v) when is_number(v), do: to_string(v)
  defp _to_json(v) when is_boolean(v), do: if(v, do: "true", else: "false")
  defp _to_json(v) when is_list(v), do: "[" <> Enum.map_join(v, ",", &_to_json/1) <> "]"

  defp _to_json(v) when is_map(v) do
    keys = Map.keys(v) |> Enum.map(&to_string/1) |> Enum.sort()

    inner =
      Enum.map_join(keys, ",", fn k ->
        _to_json(k) <> ":" <> _to_json(Map.get(v, String.to_atom(k), Map.get(v, k)))
      end)

    "{" <> inner <> "}"
  end

  defp _to_json(_), do: "null"
  defp _json(v), do: IO.puts(_to_json(v))

  defp _merge_map(a, b) do
    cond do
      is_map(a) and is_map(b) -> Map.merge(a, b)
      is_map(a) -> a
      is_map(b) -> b
      true -> %{}
    end
  end

  defp _query(src, joins, opts \\ %{}) do
    where = Map.get(opts, :where)
    items = Enum.map(src, fn v -> [v] end)

    items =
      Enum.reduce(joins, items, fn j, items ->
        joined =
          cond do
            Map.get(j, :right) && Map.get(j, :left) ->
              matched = for _ <- j[:items], do: false

              {res, matched} =
                Enum.reduce(items, {[], matched}, fn left, {acc, matched} ->
                  {acc, matched, m} =
                    Enum.reduce(Enum.with_index(j[:items]), {acc, matched, false}, fn {right, ri},
                                                                                      {acc,
                                                                                       matched,
                                                                                       m} ->
                      keep =
                        if Map.has_key?(j, :on) and j[:on],
                          do: apply(j[:on], left ++ [right]),
                          else: true

                      if keep do
                        matched = List.replace_at(matched, ri, true)
                        {acc ++ [left ++ [right]], matched, true}
                      else
                        {acc, matched, m}
                      end
                    end)

                  acc = if !m, do: acc ++ [left ++ [nil]], else: acc
                  {acc, matched}
                end)

              Enum.reduce(Enum.with_index(j[:items]), res, fn {right, ri}, acc ->
                if Enum.at(matched, ri) do
                  acc
                else
                  undef = List.duplicate(nil, if(items == [], do: 0, else: length(hd(items))))
                  acc ++ [undef ++ [right]]
                end
              end)

            Map.get(j, :right) ->
              Enum.reduce(j[:items], [], fn right, acc ->
                {acc2, m} =
                  Enum.reduce(items, {acc, false}, fn left, {acc, m} ->
                    keep =
                      if Map.has_key?(j, :on) and j[:on],
                        do: apply(j[:on], left ++ [right]),
                        else: true

                    if keep, do: {acc ++ [left ++ [right]], true}, else: {acc, m}
                  end)

                if !m do
                  undef = List.duplicate(nil, if(items == [], do: 0, else: length(hd(items))))
                  acc2 ++ [undef ++ [right]]
                else
                  acc2
                end
              end)

            true ->
              Enum.reduce(items, [], fn left, acc ->
                {acc2, m} =
                  Enum.reduce(j[:items], {acc, false}, fn right, {acc, m} ->
                    keep =
                      if Map.has_key?(j, :on) and j[:on],
                        do: apply(j[:on], left ++ [right]),
                        else: true

                    if keep, do: {acc ++ [left ++ [right]], true}, else: {acc, m}
                  end)

                if Map.get(j, :left) && !m do
                  acc2 ++ [left ++ [nil]]
                else
                  acc2
                end
              end)
          end

        joined
      end)

    items = if where, do: Enum.filter(items, fn r -> where.(r) end), else: items

    items =
      if Map.has_key?(opts, :sortKey),
        do: Enum.sort_by(items, fn r -> apply(opts[:sortKey], r) end),
        else: items

    items =
      if Map.has_key?(opts, :skip),
        do:
          (
            n = opts[:skip]
            if n < length(items), do: Enum.drop(items, n), else: []
          ),
        else: items

    items =
      if Map.has_key?(opts, :take),
        do:
          (
            n = opts[:take]
            if n < length(items), do: Enum.take(items, n), else: items
          ),
        else: items

    Enum.map(items, fn r -> apply(opts[:select], r) end)
  end
end

Main.main()
