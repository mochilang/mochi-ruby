# Generated by Mochi compiler v0.10.27 on 2025-07-17T18:23:40Z
defmodule Main do
  def main do
    # lineitem :: list(map())
    lineitem = [
      %{l_extendedprice: 1000, l_discount: 0.06, l_shipdate: "1994-02-15", l_quantity: 10},
      %{l_extendedprice: 500, l_discount: 0.07, l_shipdate: "1994-03-10", l_quantity: 23},
      %{l_extendedprice: 400, l_discount: 0.04, l_shipdate: "1994-04-10", l_quantity: 15},
      %{l_extendedprice: 200, l_discount: 0.06, l_shipdate: "1995-01-01", l_quantity: 5}
    ]

    # result :: float()
    result =
      _sum(
        for l <- lineitem,
            l.l_shipdate >= "1994-01-01" && l.l_shipdate < "1995-01-01" && l.l_discount >= 0.05 &&
              l.l_discount <= 0.07 && l.l_quantity < 24,
            do: l.l_extendedprice * l.l_discount
      )

    _json(result)
  end

  defp _escape_json(<<>>), do: ""
  defp _escape_json(<<"\\", rest::binary>>), do: "\\\\" <> _escape_json(rest)
  defp _escape_json(<<"\"", rest::binary>>), do: "\\\"" <> _escape_json(rest)
  defp _escape_json(<<c::binary-size(1), rest::binary>>), do: c <> _escape_json(rest)
  defp _to_json(v) when is_binary(v), do: "\"" <> _escape_json(v) <> "\""
  defp _to_json(v) when is_number(v), do: to_string(v)
  defp _to_json(v) when is_boolean(v), do: if(v, do: "true", else: "false")
  defp _to_json(v) when is_list(v), do: "[" <> Enum.map_join(v, ",", &_to_json/1) <> "]"

  defp _to_json(v) when is_map(v) do
    keys = Map.keys(v) |> Enum.map(&to_string/1) |> Enum.sort()

    inner =
      Enum.map_join(keys, ",", fn k ->
        _to_json(k) <> ":" <> _to_json(Map.get(v, String.to_atom(k), Map.get(v, k)))
      end)

    "{" <> inner <> "}"
  end

  defp _to_json(_), do: "null"
  defp _json(v), do: IO.puts(_to_json(v))

  defp _sum(v) do
    list =
      cond do
        is_map(v) and Map.has_key?(v, :items) -> Map.get(v, :items)
        is_list(v) -> v
        true -> raise "sum() expects list or group"
      end

    Enum.sum(list)
  end
end

Main.main()
