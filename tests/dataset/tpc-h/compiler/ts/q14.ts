// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
// Source: /workspace/mochi/tests/dataset/tpc-h/q14.mochi

let end_date: string;
let filtered: Record<string, any>[];
let lineitem: Record<string, any>[];
let part: Record<string, any>[];
let promo_sum: number;
let result: number;
let start_date: string;
let total_sum: number;

function test_Q14_calculates_promo_revenue_percent_in_1995_09(): void {
  let promo = 1000 * 0.9;
  let total = 900 + 800;
  let expected = (100 * promo) / total;
  if (!(result == expected)) throw new Error("expect failed");
}

function main(): void {
  part = [
    {
      "p_partkey": 1,
      "p_type": "PROMO LUXURY",
    },
    {
      "p_partkey": 2,
      "p_type": "STANDARD BRASS",
    },
  ];
  lineitem = [
    {
      "l_partkey": 1,
      "l_extendedprice": 1000,
      "l_discount": 0.1,
      "l_shipdate": "1995-09-05",
    },
    {
      "l_partkey": 2,
      "l_extendedprice": 800,
      "l_discount": 0,
      "l_shipdate": "1995-09-20",
    },
    {
      "l_partkey": 1,
      "l_extendedprice": 500,
      "l_discount": 0.2,
      "l_shipdate": "1995-10-02",
    },
  ];
  start_date = "1995-09-01";
  end_date = "1995-10-01";
  filtered = (() => {
    const _src = lineitem;
    const _res = [];
    for (const l of _src) {
      for (const p of part) {
        if (!(p.p_partkey == l.l_partkey)) continue;
        if (!((l.l_shipdate >= start_date) && (l.l_shipdate < end_date))) {
          continue;
        }
        _res.push({
          "is_promo": (_contains(p.p_type, "PROMO") ? 1 : 0),
          "revenue": (l.l_extendedprice * (1 - l.l_discount)),
        });
      }
    }
    return _res;
  })();
  promo_sum = filtered.filter((x) => (x.is_promo)).map((x) => x.revenue).reduce(
    (a, b) => a + Number(b),
    0,
  );
  total_sum = filtered.map((x) => x.revenue).reduce((a, b) => a + Number(b), 0);
  result = (100 * promo_sum) / total_sum;
  console.log(_json(result));
  test_Q14_calculates_promo_revenue_percent_in_1995_09();
}
function _contains(c: unknown, v: unknown): boolean {
  if (Array.isArray(c)) return c.includes(v);
  if (typeof c === "string") return c.includes(String(v));
  if (c && typeof c === "object") {
    return Object.prototype.hasOwnProperty.call(c, String(v));
  }
  return false;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
