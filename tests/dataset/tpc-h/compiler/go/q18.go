//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2025-07-18T06:59:53Z

package main

import (
	"encoding/json"
	"fmt"
	"mochi/runtime/data"
	"reflect"
	"sort"
	"strings"
	"time"

	"golang.org/x/exp/constraints"
)

type v = Result

type Nation struct {
	N_nationkey int    `json:"n_nationkey"`
	N_name      string `json:"n_name"`
}

type Customer struct {
	C_custkey   int     `json:"c_custkey"`
	C_name      string  `json:"c_name"`
	C_acctbal   float64 `json:"c_acctbal"`
	C_nationkey int     `json:"c_nationkey"`
	C_address   string  `json:"c_address"`
	C_phone     string  `json:"c_phone"`
	C_comment   string  `json:"c_comment"`
}

type Order struct {
	O_orderkey int `json:"o_orderkey"`
	O_custkey  int `json:"o_custkey"`
}

type Lineitem struct {
	L_orderkey      int     `json:"l_orderkey"`
	L_quantity      int     `json:"l_quantity"`
	L_extendedprice float64 `json:"l_extendedprice"`
	L_discount      float64 `json:"l_discount"`
}

type Result struct {
	C_name    any     `json:"c_name"`
	C_custkey any     `json:"c_custkey"`
	Revenue   float64 `json:"revenue"`
	C_acctbal any     `json:"c_acctbal"`
	N_name    any     `json:"n_name"`
	C_address any     `json:"c_address"`
	C_phone   any     `json:"c_phone"`
	C_comment any     `json:"c_comment"`
}

type GKey struct {
	C_name    string  `json:"c_name"`
	C_custkey int     `json:"c_custkey"`
	C_acctbal float64 `json:"c_acctbal"`
	C_address string  `json:"c_address"`
	C_phone   string  `json:"c_phone"`
	C_comment string  `json:"c_comment"`
	N_name    string  `json:"n_name"`
}

type GRow struct {
	C Customer `json:"c"`
	O Order    `json:"o"`
	L Lineitem `json:"l"`
	N Nation   `json:"n"`
}

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_Q18_returns_large_volume_customers_with_total_quantity___200() {
	expect(_equal(result, []map[string]any{map[string]any{
		"c_name":    any("Alice"),
		"c_custkey": any(1),
		"revenue":   any(1700.0),
		"c_acctbal": any(1000.0),
		"n_name":    any("GERMANY"),
		"c_address": any("123 Market St"),
		"c_phone":   any("123-456"),
		"c_comment": any("Premium client"),
	}}))
}

var nation []Nation
var customer []Customer
var orders []Order
var lineitem []Lineitem
var threshold int
var result []Result

func main() {
	nation = []Nation{Nation{
		N_nationkey: 1,
		N_name:      "GERMANY",
	}}
	customer = []Customer{Customer{
		C_custkey:   1,
		C_name:      "Alice",
		C_acctbal:   1000.0,
		C_nationkey: 1,
		C_address:   "123 Market St",
		C_phone:     "123-456",
		C_comment:   "Premium client",
	}, Customer{
		C_custkey:   2,
		C_name:      "Bob",
		C_acctbal:   200.0,
		C_nationkey: 1,
		C_address:   "456 Side St",
		C_phone:     "987-654",
		C_comment:   "Frequent returns",
	}}
	orders = []Order{Order{
		O_orderkey: 100,
		O_custkey:  1,
	}, Order{
		O_orderkey: 200,
		O_custkey:  1,
	}, Order{
		O_orderkey: 300,
		O_custkey:  2,
	}}
	lineitem = []Lineitem{Lineitem{
		L_orderkey:      100,
		L_quantity:      150,
		L_extendedprice: 1000.0,
		L_discount:      0.1,
	}, Lineitem{
		L_orderkey:      200,
		L_quantity:      100,
		L_extendedprice: 800.0,
		L_discount:      0.0,
	}, Lineitem{
		L_orderkey:      300,
		L_quantity:      30,
		L_extendedprice: 300.0,
		L_discount:      0.05,
	}}
	threshold = 200
	result = func() []Result {
		groups := map[string]*data.Group{}
		order := []string{}
		for _, c := range customer {
			for _, o := range orders {
				if !(o.O_custkey == c.C_custkey) {
					continue
				}
				for _, l := range lineitem {
					if !(l.L_orderkey == o.O_orderkey) {
						continue
					}
					for _, n := range nation {
						if !(n.N_nationkey == c.C_nationkey) {
							continue
						}
						key := GKey{
							C_name:    c.C_name,
							C_custkey: c.C_custkey,
							C_acctbal: c.C_acctbal,
							C_address: c.C_address,
							C_phone:   c.C_phone,
							C_comment: c.C_comment,
							N_name:    n.N_name,
						}
						ks := fmt.Sprint(key)
						g, ok := groups[ks]
						if !ok {
							g = &data.Group{Key: key}
							groups[ks] = g
							order = append(order, ks)
						}
						g.Items = append(g.Items, GRow{C: c, O: o, L: l, N: n})
					}
				}
			}
		}
		items := []*data.Group{}
		for _, ks := range order {
			items = append(items, groups[ks])
		}
		type pair struct {
			item *data.Group
			key  any
		}
		pairs := make([]pair, len(items))
		for idx, it := range items {
			g := it
			pairs[idx] = pair{item: it, key: -_sumOrdered[float64](func() []float64 {
				results := []float64{}
				for _, xRaw := range g.Items {
					x := xRaw.(GRow)
					results = append(results, (x.L.L_extendedprice * (float64(1) - x.L.L_discount)))
				}
				return results
			}())}
		}
		sort.Slice(pairs, func(i, j int) bool {
			a, b := pairs[i].key, pairs[j].key
			switch av := a.(type) {
			case int:
				switch bv := b.(type) {
				case int:
					return av < bv
				case float64:
					return float64(av) < bv
				}
			case float64:
				switch bv := b.(type) {
				case int:
					return av < float64(bv)
				case float64:
					return av < bv
				}
			case string:
				bs, _ := b.(string)
				return av < bs
			}
			return fmt.Sprint(a) < fmt.Sprint(b)
		})
		for idx, p := range pairs {
			items[idx] = p.item
		}
		results := []Result{}
		for _, g := range items {
			if !(_sumOrdered[int](func() []int {
				results := []int{}
				for _, xRaw := range g.Items {
					x := xRaw.(GRow)
					results = append(results, x.L.L_quantity)
				}
				return results
			}()) > float64(threshold)) {
				continue
			}
			results = append(results, Result{
				C_name:    any(g.Key.(GKey).C_name),
				C_custkey: any(g.Key.(GKey).C_custkey),
				Revenue: _sumOrdered[float64](func() []float64 {
					results := []float64{}
					for _, xRaw := range g.Items {
						x := xRaw.(GRow)
						results = append(results, (x.L.L_extendedprice * (float64(1) - x.L.L_discount)))
					}
					return results
				}()),
				C_acctbal: any(g.Key.(GKey).C_acctbal),
				N_name:    any(g.Key.(GKey).N_name),
				C_address: any(g.Key.(GKey).C_address),
				C_phone:   any(g.Key.(GKey).C_phone),
				C_comment: any(g.Key.(GKey).C_comment),
			})
		}
		return results
	}()
	func() { b, _ := json.Marshal(any(result)); fmt.Println(string(b)) }()
	test_Q18_returns_large_volume_customers_with_total_quantity___200()
}

func _convertMapAny(m map[any]any) map[string]any {
	out := make(map[string]any, len(m))
	for k, v := range m {
		key := fmt.Sprint(k)
		if sub, ok := v.(map[any]any); ok {
			out[key] = _convertMapAny(sub)
		} else {
			out[key] = v
		}
	}
	return out
}

func _copyToMap(dst map[string]any, src any) {
	switch m := src.(type) {
	case map[string]any:
		for k, v := range m {
			dst[k] = v
		}
	case map[string]string:
		for k, v := range m {
			dst[k] = v
		}
	case map[any]any:
		for k, v := range _convertMapAny(m) {
			dst[k] = v
		}
	default:
		rv := reflect.ValueOf(m)
		if rv.Kind() == reflect.Struct {
			rt := rv.Type()
			for i := 0; i < rv.NumField(); i++ {
				name := rt.Field(i).Name
				if tag := rt.Field(i).Tag.Get("json"); tag != "" {
					if c := strings.Index(tag, ","); c >= 0 {
						tag = tag[:c]
					}
					if tag != "-" {
						name = tag
					}
				}
				dst[name] = rv.Field(i).Interface()
			}
		}
	}
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Struct && bv.Kind() == reflect.Map {
		am := map[string]any{}
		_copyToMap(am, a)
		bm := map[string]any{}
		_copyToMap(bm, b)
		return _equal(am, bm)
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Struct {
		am := map[string]any{}
		_copyToMap(am, a)
		bm := map[string]any{}
		_copyToMap(bm, b)
		return _equal(am, bm)
	}
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}

func _sumOrdered[T constraints.Integer | constraints.Float](s []T) float64 {
	var sum float64
	for _, v := range s {
		sum += float64(v)
	}
	return sum
}
