-- Generated by Mochi compiler v0.10.26 on 2025-07-15T07:50:43Z
_Group = {}
function _Group.new(k)
    return {key = k, items = {}}
end
function __group_by_rows(rows, keyfn, valfn)
    local groups = {}
    local order = {}
    for _, r in ipairs(rows) do
        local key = keyfn(table.unpack(r,1,r.n or #r))
        local ks
        if type(key) == 'table' then
            local fields = {}
            for k,_ in pairs(key) do fields[#fields+1] = k end
            table.sort(fields)
            local parts = {}
            for _,k in ipairs(fields) do parts[#parts+1] = tostring(k)..'='..tostring(key[k]) end
            ks = table.concat(parts, ',')
        else
            ks = tostring(key)
        end
        local g = groups[ks]
        if not g then
            g = _Group.new(key)
            groups[ks] = g
            order[#order+1] = ks
        end
        table.insert(g.items, valfn(table.unpack(r,1,r.n or #r)))
    end
    local res = {}
    for _, ks in ipairs(order) do
        res[#res+1] = groups[ks]
    end
    return res
end
function __count(v)
    if type(v) == 'table' then
        if v.items ~= nil then return #v.items end
        if v[1] ~= nil or #v > 0 then return #v end
        local n = 0
        for _ in pairs(v) do n = n + 1 end
        return n
    elseif type(v) == 'string' then
        return #v
    else
        error('count() expects list or group')
    end
end
function __eq(a, b)
    if type(a) ~= type(b) then return false end
    if type(a) == 'number' then return math.abs(a-b) < 1e-9 end
    if type(a) ~= 'table' then return a == b end
    if (a[1] ~= nil or #a > 0) and (b[1] ~= nil or #b > 0) then
        if #a ~= #b then return false end
        for i = 1, #a do if not __eq(a[i], b[i]) then return false end end
        return true
    end
    for k, v in pairs(a) do if not __eq(v, b[k]) then return false end end
    for k, _ in pairs(b) do if a[k] == nil then return false end end
    return true
end
function __json(v)
    if type(v) == 'table' and next(v) == nil then print('[]'); return end
    local function sort(x)
        if type(x) ~= 'table' then return x end
        if x[1] ~= nil or #x > 0 then
            local out = {}
            for i=1,#x do out[i] = sort(x[i]) end
            return out
        end
        local keys = {}
        for k in pairs(x) do keys[#keys+1] = k end
        table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
        local out = {}
        for _,k in ipairs(keys) do out[k] = sort(x[k]) end
        return out
    end
    local function enc(x)
        local t = type(x)
        if t == 'nil' then
            return 'null'
        elseif t == 'boolean' or t == 'number' then
            return tostring(x)
        elseif t == 'string' then
            return string.format('%q', x)
        elseif t == 'table' then
            if x[1] ~= nil or #x > 0 then
                local parts = {}
                for i=1,#x do parts[#parts+1] = enc(x[i]) end
                return '['..table.concat(parts, ',')..']'
            else
                local keys = {}
                for k in pairs(x) do keys[#keys+1] = k end
                table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
                local parts = {}
                for _,k in ipairs(keys) do parts[#parts+1] = enc(k)..':'..enc(x[k]) end
                return '{'..table.concat(parts, ',')..'}'
            end
        else
            return 'null'
        end
    end
    print(enc(sort(v)))
end
function __merge(...)
    local res = {}
    for i=1,select('#', ...) do
        local t = select(i, ...)
        if type(t) == 'table' then
            for k,v in pairs(t) do res[k] = v end
        end
    end
    return res
end
function __query(src, joins, opts)
    local whereFn = opts.where
    local items = {}
    if #joins == 0 and whereFn then
        for _, v in ipairs(src) do if whereFn(v) then items[#items+1] = {v, n=1} end end
    else
        for _, v in ipairs(src) do items[#items+1] = {v, n=1} end
    end
    for ji, j in ipairs(joins) do
        local joined = {}
        local jitems = j.items or {}
        if j.right and j.left then
            local matched = {}
            for _, left in ipairs(items) do
                local m = false
                for ri, right in ipairs(jitems) do
                    local leftLen = left.n or #left
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left,1,leftLen)}
                        args[leftLen+1] = right
                        keep = j.on(table.unpack(args,1,leftLen+1))
                    end
                    if keep then
                        m = true; matched[ri] = true
                        local row = {table.unpack(left,1,leftLen)}
                        row[leftLen+1] = right
                        row.n = leftLen + 1
                        if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if not m then
                    local leftLen = left.n or #left
                    local row = {table.unpack(left,1,leftLen)}
                    row[leftLen+1] = nil
                    row.n = leftLen + 1
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
            for ri, right in ipairs(jitems) do
                if not matched[ri] then
                    local row = {}
                    for _=1,ji do row[#row+1] = nil end
                    row[#row+1] = right
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        elseif j.right then
            for _, right in ipairs(jitems) do
                local m = false
                for _, left in ipairs(items) do
                    local leftLen = left.n or #left
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left,1,leftLen)}
                        args[leftLen+1] = right
                        keep = j.on(table.unpack(args,1,leftLen+1))
                    end
                    if keep then
                        m = true
                        local row = {table.unpack(left,1,leftLen)}
                        row[leftLen+1] = right
                        row.n = leftLen + 1
                        if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if not m then
                    local row = {}
                    for _=1,ji do row[#row+1] = nil end
                    row[#row+1] = right
                    row.n = ji + 1
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        else
            for _, left in ipairs(items) do
                local m = false
                for _, right in ipairs(jitems) do
                    local leftLen = left.n or #left
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left,1,leftLen)}
                        args[leftLen+1] = right
                        keep = j.on(table.unpack(args,1,leftLen+1))
                    end
                    if keep then
                        m = true
                        local row = {table.unpack(left,1,leftLen)}
                        row[leftLen+1] = right
                        row.n = leftLen + 1
                        if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if j.left and not m then
                    local leftLen = left.n or #left
                    local row = {table.unpack(left,1,leftLen)}
                    row[leftLen+1] = nil
                    row.n = leftLen + 1
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        end
        items = joined
    end
    if opts.sortKey then
        local pairs = {}
        for _, it in ipairs(items) do pairs[#pairs+1] = {item=it, key=opts.sortKey(table.unpack(it,1,it.n or #it))} end
        table.sort(pairs, function(a,b)
            local ak, bk = a.key, b.key
            if type(ak)=='number' and type(bk)=='number' then return ak < bk end
            if type(ak)=='string' and type(bk)=='string' then return ak < bk end
            return tostring(ak) < tostring(bk)
        end)
        items = {}
        for i,p in ipairs(pairs) do items[i] = p.item end
    end
    if opts.skip ~= nil then
        local n = opts.skip
        if n < #items then
            for i=1,n do table.remove(items,1) end
        else
            items = {}
        end
    end
    if opts.take ~= nil then
        local n = opts.take
        if n < #items then
            for i=#items, n+1, -1 do table.remove(items) end
        end
    end
    local res = {}
    for _, r in ipairs(items) do res[#res+1] = opts.selectFn(table.unpack(r,1,r.n or #r)) end
    return res
end
function __run_tests(tests)
    local function format_duration(d)
        if d < 1e-6 then return string.format('%dns', math.floor(d*1e9)) end
        if d < 1e-3 then return string.format('%.1fµs', d*1e6) end
        if d < 1 then return string.format('%.1fms', d*1e3) end
        return string.format('%.2fs', d)
    end
    local failures = 0
    for _, t in ipairs(tests) do
        io.write('   test ' .. t.name .. ' ...')
        local start = os.clock()
        local ok, err = pcall(t.fn)
        local dur = os.clock() - start
        if ok then
            io.write(' ok (' .. format_duration(dur) .. ')\n')
        else
            io.write(' fail ' .. tostring(err) .. ' (' .. format_duration(dur) .. ')\n')
            failures = failures + 1
        end
    end
    if failures > 0 then
        io.write('\n[FAIL] ' .. failures .. ' test(s) failed.\n')
    end
end
function __slice(obj, i, j)
    if i == nil then i = 0 end
    if type(obj) == 'string' then
        local len = #obj
        if j == nil then j = len end
        if i < 0 then i = len + i end
        if j < 0 then j = len + j end
        if i < 0 then i = 0 end
        if j > len then j = len end
        return string.sub(obj, i+1, j)
    elseif type(obj) == 'table' then
        local len = #obj
        if j == nil then j = len end
        if i < 0 then i = len + i end
        if j < 0 then j = len + j end
        if i < 0 then i = 0 end
        if j > len then j = len end
        local out = {}
        for k = i+1, j do
            out[#out+1] = obj[k]
        end
        return out
    else
        return {}
    end
end
CatalogSale = {}
CatalogSale.__index = CatalogSale
function CatalogSale.new(o)
    o = o or {}
    setmetatable(o, CatalogSale)
    return o
end

Warehouse = {}
Warehouse.__index = Warehouse
function Warehouse.new(o)
    o = o or {}
    setmetatable(o, Warehouse)
    return o
end

ShipMode = {}
ShipMode.__index = ShipMode
function ShipMode.new(o)
    o = o or {}
    setmetatable(o, ShipMode)
    return o
end

CallCenter = {}
CallCenter.__index = CallCenter
function CallCenter.new(o)
    o = o or {}
    setmetatable(o, CallCenter)
    return o
end

function test_TPCDS_Q99_buckets()
    if not (__eq(grouped, {{["warehouse"]="Warehouse1", ["sm_type"]="EXP", ["cc_name"]="CC1", ["d30"]=1, ["d60"]=1, ["d90"]=1, ["d120"]=1, ["dmore"]=1}})) then error('expect failed') end
end

catalog_sales = {{["cs_ship_date_sk"]=31, ["cs_sold_date_sk"]=1, ["cs_warehouse_sk"]=1, ["cs_ship_mode_sk"]=1, ["cs_call_center_sk"]=1}, {["cs_ship_date_sk"]=51, ["cs_sold_date_sk"]=1, ["cs_warehouse_sk"]=1, ["cs_ship_mode_sk"]=1, ["cs_call_center_sk"]=1}, {["cs_ship_date_sk"]=71, ["cs_sold_date_sk"]=1, ["cs_warehouse_sk"]=1, ["cs_ship_mode_sk"]=1, ["cs_call_center_sk"]=1}, {["cs_ship_date_sk"]=101, ["cs_sold_date_sk"]=1, ["cs_warehouse_sk"]=1, ["cs_ship_mode_sk"]=1, ["cs_call_center_sk"]=1}, {["cs_ship_date_sk"]=131, ["cs_sold_date_sk"]=1, ["cs_warehouse_sk"]=1, ["cs_ship_mode_sk"]=1, ["cs_call_center_sk"]=1}}
warehouse = {{["w_warehouse_sk"]=1, ["w_warehouse_name"]="Warehouse1"}}
ship_mode = {{["sm_ship_mode_sk"]=1, ["sm_type"]="EXP"}}
call_center = {{["cc_call_center_sk"]=1, ["cc_name"]="CC1"}}
grouped = (function()
    local _src = catalog_sales
    local _rows = __query(_src, {
        { items = warehouse, on = function(cs, w) return __eq(cs.cs_warehouse_sk, w.w_warehouse_sk) end },
        { items = ship_mode, on = function(cs, w, sm) return __eq(cs.cs_ship_mode_sk, sm.sm_ship_mode_sk) end },
        { items = call_center, on = function(cs, w, sm, cc) return __eq(cs.cs_call_center_sk, cc.cc_call_center_sk) end }
    }, { selectFn = function(cs, w, sm, cc) return {cs, w, sm, cc} end })
    local _groups = __group_by_rows(_rows, function(cs, w, sm, cc) return {["warehouse"]=__slice(w.w_warehouse_name, 0, 20), ["sm_type"]=sm.sm_type, ["cc_name"]=cc.cc_name} end, function(cs, w, sm, cc) local _row = __merge(cs, w, sm, cc); _row.cs = cs; _row.w = w; _row.sm = sm; _row.cc = cc; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["warehouse"]=g.key.warehouse, ["sm_type"]=g.key.sm_type, ["cc_name"]=g.key.cc_name, ["d30"]=__count((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        if ((x.cs_ship_date_sk - x.cs_sold_date_sk) <= 30) then
            _res[#_res+1] = x
        end
    end
    return _res
end)()), ["d60"]=__count((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        if (((x.cs_ship_date_sk - x.cs_sold_date_sk) > 30) and ((x.cs_ship_date_sk - x.cs_sold_date_sk) <= 60)) then
            _res[#_res+1] = x
        end
    end
    return _res
end)()), ["d90"]=__count((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        if (((x.cs_ship_date_sk - x.cs_sold_date_sk) > 60) and ((x.cs_ship_date_sk - x.cs_sold_date_sk) <= 90)) then
            _res[#_res+1] = x
        end
    end
    return _res
end)()), ["d120"]=__count((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        if (((x.cs_ship_date_sk - x.cs_sold_date_sk) > 90) and ((x.cs_ship_date_sk - x.cs_sold_date_sk) <= 120)) then
            _res[#_res+1] = x
        end
    end
    return _res
end)()), ["dmore"]=__count((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        if ((x.cs_ship_date_sk - x.cs_sold_date_sk) > 120) then
            _res[#_res+1] = x
        end
    end
    return _res
end)())}
    end
    return _res
end)()
__json(grouped)
local __tests = {
    {name="TPCDS Q99 buckets", fn=test_TPCDS_Q99_buckets},
}
