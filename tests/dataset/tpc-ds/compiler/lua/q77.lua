-- Generated by Mochi compiler v0.10.26 on 2025-07-15T07:50:43Z
_Group = {}
function _Group.new(k)
    return {key = k, items = {}}
end
function __group_by_rows(rows, keyfn, valfn)
    local groups = {}
    local order = {}
    for _, r in ipairs(rows) do
        local key = keyfn(table.unpack(r,1,r.n or #r))
        local ks
        if type(key) == 'table' then
            local fields = {}
            for k,_ in pairs(key) do fields[#fields+1] = k end
            table.sort(fields)
            local parts = {}
            for _,k in ipairs(fields) do parts[#parts+1] = tostring(k)..'='..tostring(key[k]) end
            ks = table.concat(parts, ',')
        else
            ks = tostring(key)
        end
        local g = groups[ks]
        if not g then
            g = _Group.new(key)
            groups[ks] = g
            order[#order+1] = ks
        end
        table.insert(g.items, valfn(table.unpack(r,1,r.n or #r)))
    end
    local res = {}
    for _, ks in ipairs(order) do
        res[#res+1] = groups[ks]
    end
    return res
end
function __concat(a, b)
    local res = {}
    if a then for i=1,#a do res[#res+1] = a[i] end end
    if b then for i=1,#b do res[#res+1] = b[i] end end
    return res
end
function __eq(a, b)
    if type(a) ~= type(b) then return false end
    if type(a) == 'number' then return math.abs(a-b) < 1e-9 end
    if type(a) ~= 'table' then return a == b end
    if (a[1] ~= nil or #a > 0) and (b[1] ~= nil or #b > 0) then
        if #a ~= #b then return false end
        for i = 1, #a do if not __eq(a[i], b[i]) then return false end end
        return true
    end
    for k, v in pairs(a) do if not __eq(v, b[k]) then return false end end
    for k, _ in pairs(b) do if a[k] == nil then return false end end
    return true
end
function __json(v)
    if type(v) == 'table' and next(v) == nil then print('[]'); return end
    local function sort(x)
        if type(x) ~= 'table' then return x end
        if x[1] ~= nil or #x > 0 then
            local out = {}
            for i=1,#x do out[i] = sort(x[i]) end
            return out
        end
        local keys = {}
        for k in pairs(x) do keys[#keys+1] = k end
        table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
        local out = {}
        for _,k in ipairs(keys) do out[k] = sort(x[k]) end
        return out
    end
    local function enc(x)
        local t = type(x)
        if t == 'nil' then
            return 'null'
        elseif t == 'boolean' or t == 'number' then
            return tostring(x)
        elseif t == 'string' then
            return string.format('%q', x)
        elseif t == 'table' then
            if x[1] ~= nil or #x > 0 then
                local parts = {}
                for i=1,#x do parts[#parts+1] = enc(x[i]) end
                return '['..table.concat(parts, ',')..']'
            else
                local keys = {}
                for k in pairs(x) do keys[#keys+1] = k end
                table.sort(keys, function(a,b) return tostring(a)<tostring(b) end)
                local parts = {}
                for _,k in ipairs(keys) do parts[#parts+1] = enc(k)..':'..enc(x[k]) end
                return '{'..table.concat(parts, ',')..'}'
            end
        else
            return 'null'
        end
    end
    print(enc(sort(v)))
end
function __merge(...)
    local res = {}
    for i=1,select('#', ...) do
        local t = select(i, ...)
        if type(t) == 'table' then
            for k,v in pairs(t) do res[k] = v end
        end
    end
    return res
end
function __query(src, joins, opts)
    local whereFn = opts.where
    local items = {}
    if #joins == 0 and whereFn then
        for _, v in ipairs(src) do if whereFn(v) then items[#items+1] = {v, n=1} end end
    else
        for _, v in ipairs(src) do items[#items+1] = {v, n=1} end
    end
    for ji, j in ipairs(joins) do
        local joined = {}
        local jitems = j.items or {}
        if j.right and j.left then
            local matched = {}
            for _, left in ipairs(items) do
                local m = false
                for ri, right in ipairs(jitems) do
                    local leftLen = left.n or #left
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left,1,leftLen)}
                        args[leftLen+1] = right
                        keep = j.on(table.unpack(args,1,leftLen+1))
                    end
                    if keep then
                        m = true; matched[ri] = true
                        local row = {table.unpack(left,1,leftLen)}
                        row[leftLen+1] = right
                        row.n = leftLen + 1
                        if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if not m then
                    local leftLen = left.n or #left
                    local row = {table.unpack(left,1,leftLen)}
                    row[leftLen+1] = nil
                    row.n = leftLen + 1
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
            for ri, right in ipairs(jitems) do
                if not matched[ri] then
                    local row = {}
                    for _=1,ji do row[#row+1] = nil end
                    row[#row+1] = right
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        elseif j.right then
            for _, right in ipairs(jitems) do
                local m = false
                for _, left in ipairs(items) do
                    local leftLen = left.n or #left
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left,1,leftLen)}
                        args[leftLen+1] = right
                        keep = j.on(table.unpack(args,1,leftLen+1))
                    end
                    if keep then
                        m = true
                        local row = {table.unpack(left,1,leftLen)}
                        row[leftLen+1] = right
                        row.n = leftLen + 1
                        if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if not m then
                    local row = {}
                    for _=1,ji do row[#row+1] = nil end
                    row[#row+1] = right
                    row.n = ji + 1
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        else
            for _, left in ipairs(items) do
                local m = false
                for _, right in ipairs(jitems) do
                    local leftLen = left.n or #left
                    local keep = true
                    if j.on then
                        local args = {table.unpack(left,1,leftLen)}
                        args[leftLen+1] = right
                        keep = j.on(table.unpack(args,1,leftLen+1))
                    end
                    if keep then
                        m = true
                        local row = {table.unpack(left,1,leftLen)}
                        row[leftLen+1] = right
                        row.n = leftLen + 1
                        if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                        else
                            joined[#joined+1] = row
                        end
                    end
                end
                if j.left and not m then
                    local leftLen = left.n or #left
                    local row = {table.unpack(left,1,leftLen)}
                    row[leftLen+1] = nil
                    row.n = leftLen + 1
                    if ji == #joins and whereFn and not whereFn(table.unpack(row,1,row.n or #row)) then
                    else
                        joined[#joined+1] = row
                    end
                end
            end
        end
        items = joined
    end
    if opts.sortKey then
        local pairs = {}
        for _, it in ipairs(items) do pairs[#pairs+1] = {item=it, key=opts.sortKey(table.unpack(it,1,it.n or #it))} end
        table.sort(pairs, function(a,b)
            local ak, bk = a.key, b.key
            if type(ak)=='number' and type(bk)=='number' then return ak < bk end
            if type(ak)=='string' and type(bk)=='string' then return ak < bk end
            return tostring(ak) < tostring(bk)
        end)
        items = {}
        for i,p in ipairs(pairs) do items[i] = p.item end
    end
    if opts.skip ~= nil then
        local n = opts.skip
        if n < #items then
            for i=1,n do table.remove(items,1) end
        else
            items = {}
        end
    end
    if opts.take ~= nil then
        local n = opts.take
        if n < #items then
            for i=#items, n+1, -1 do table.remove(items) end
        end
    end
    local res = {}
    for _, r in ipairs(items) do res[#res+1] = opts.selectFn(table.unpack(r,1,r.n or #r)) end
    return res
end
function __run_tests(tests)
    local function format_duration(d)
        if d < 1e-6 then return string.format('%dns', math.floor(d*1e9)) end
        if d < 1e-3 then return string.format('%.1fµs', d*1e6) end
        if d < 1 then return string.format('%.1fms', d*1e3) end
        return string.format('%.2fs', d)
    end
    local failures = 0
    for _, t in ipairs(tests) do
        io.write('   test ' .. t.name .. ' ...')
        local start = os.clock()
        local ok, err = pcall(t.fn)
        local dur = os.clock() - start
        if ok then
            io.write(' ok (' .. format_duration(dur) .. ')\n')
        else
            io.write(' fail ' .. tostring(err) .. ' (' .. format_duration(dur) .. ')\n')
            failures = failures + 1
        end
    end
    if failures > 0 then
        io.write('\n[FAIL] ' .. failures .. ' test(s) failed.\n')
    end
end
function __sum(v)
    local items
    if type(v) == 'table' and v.items ~= nil then
        items = v.items
    elseif type(v) == 'table' then
        items = v
    else
        error('sum() expects list or group')
    end
    local sum = 0
    for _, it in ipairs(items) do sum = sum + it end
    return sum
end
function test_TPCDS_Q77_simplified()
    if not (__eq(result, {{["channel"]="catalog channel", ["id"]=1, ["sales"]=150.0, ["returns"]=7.0, ["profit"]=12.0}, {["channel"]="store channel", ["id"]=1, ["sales"]=100.0, ["returns"]=5.0, ["profit"]=9.0}, {["channel"]="web channel", ["id"]=1, ["sales"]=200.0, ["returns"]=10.0, ["profit"]=18.0}})) then error('expect failed') end
end

date_dim = {{["d_date_sk"]=1, ["d_date"]=1}}
store_sales = {{["ss_sold_date_sk"]=1, ["s_store_sk"]=1, ["ss_ext_sales_price"]=100.0, ["ss_net_profit"]=10.0}}
store_returns = {{["sr_returned_date_sk"]=1, ["s_store_sk"]=1, ["sr_return_amt"]=5.0, ["sr_net_loss"]=1.0}}
catalog_sales = {{["cs_sold_date_sk"]=1, ["cs_call_center_sk"]=1, ["cs_ext_sales_price"]=150.0, ["cs_net_profit"]=15.0}}
catalog_returns = {{["cr_returned_date_sk"]=1, ["cr_call_center_sk"]=1, ["cr_return_amount"]=7.0, ["cr_net_loss"]=3.0}}
web_sales = {{["ws_sold_date_sk"]=1, ["ws_web_page_sk"]=1, ["ws_ext_sales_price"]=200.0, ["ws_net_profit"]=20.0}}
web_returns = {{["wr_returned_date_sk"]=1, ["wr_web_page_sk"]=1, ["wr_return_amt"]=10.0, ["wr_net_loss"]=2.0}}
ss = (function()
    local _src = store_sales
    local _rows = __query(_src, {
        { items = date_dim, on = function(ss, d) return __eq(d.d_date_sk, ss.ss_sold_date_sk) end }
    }, { selectFn = function(ss, d) return {ss, d} end })
    local _groups = __group_by_rows(_rows, function(ss, d) return ss.s_store_sk end, function(ss, d) local _row = __merge(ss, d); _row.ss = ss; _row.d = d; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["s_store_sk"]=g.key, ["sales"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.ss.ss_ext_sales_price
    end
    return _res
end)()), ["profit"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.ss.ss_net_profit
    end
    return _res
end)())}
    end
    return _res
end)()
sr = (function()
    local _src = store_returns
    local _rows = __query(_src, {
        { items = date_dim, on = function(sr, d) return __eq(d.d_date_sk, sr.sr_returned_date_sk) end }
    }, { selectFn = function(sr, d) return {sr, d} end })
    local _groups = __group_by_rows(_rows, function(sr, d) return sr.s_store_sk end, function(sr, d) local _row = __merge(sr, d); _row.sr = sr; _row.d = d; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["s_store_sk"]=g.key, ["returns"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.sr.sr_return_amt
    end
    return _res
end)()), ["profit_loss"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.sr.sr_net_loss
    end
    return _res
end)())}
    end
    return _res
end)()
cs = (function()
    local _src = catalog_sales
    local _rows = __query(_src, {
        { items = date_dim, on = function(cs, d) return __eq(d.d_date_sk, cs.cs_sold_date_sk) end }
    }, { selectFn = function(cs, d) return {cs, d} end })
    local _groups = __group_by_rows(_rows, function(cs, d) return cs.cs_call_center_sk end, function(cs, d) local _row = __merge(cs, d); _row.cs = cs; _row.d = d; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["cs_call_center_sk"]=g.key, ["sales"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.cs.cs_ext_sales_price
    end
    return _res
end)()), ["profit"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.cs.cs_net_profit
    end
    return _res
end)())}
    end
    return _res
end)()
cr = (function()
    local _src = catalog_returns
    local _rows = __query(_src, {
        { items = date_dim, on = function(cr, d) return __eq(d.d_date_sk, cr.cr_returned_date_sk) end }
    }, { selectFn = function(cr, d) return {cr, d} end })
    local _groups = __group_by_rows(_rows, function(cr, d) return cr.cr_call_center_sk end, function(cr, d) local _row = __merge(cr, d); _row.cr = cr; _row.d = d; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["cr_call_center_sk"]=g.key, ["returns"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.cr.cr_return_amount
    end
    return _res
end)()), ["profit_loss"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.cr.cr_net_loss
    end
    return _res
end)())}
    end
    return _res
end)()
ws = (function()
    local _src = web_sales
    local _rows = __query(_src, {
        { items = date_dim, on = function(ws, d) return __eq(d.d_date_sk, ws.ws_sold_date_sk) end }
    }, { selectFn = function(ws, d) return {ws, d} end })
    local _groups = __group_by_rows(_rows, function(ws, d) return ws.ws_web_page_sk end, function(ws, d) local _row = __merge(ws, d); _row.ws = ws; _row.d = d; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["wp_web_page_sk"]=g.key, ["sales"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.ws.ws_ext_sales_price
    end
    return _res
end)()), ["profit"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.ws.ws_net_profit
    end
    return _res
end)())}
    end
    return _res
end)()
wr = (function()
    local _src = web_returns
    local _rows = __query(_src, {
        { items = date_dim, on = function(wr, d) return __eq(d.d_date_sk, wr.wr_returned_date_sk) end }
    }, { selectFn = function(wr, d) return {wr, d} end })
    local _groups = __group_by_rows(_rows, function(wr, d) return wr.wr_web_page_sk end, function(wr, d) local _row = __merge(wr, d); _row.wr = wr; _row.d = d; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["wp_web_page_sk"]=g.key, ["returns"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.wr.wr_return_amt
    end
    return _res
end)()), ["profit_loss"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.wr.wr_net_loss
    end
    return _res
end)())}
    end
    return _res
end)()
per_channel = __concat(__concat((function()
    local _src = ss
    return __query(_src, {
        { items = sr, on = function(s, r) return __eq(s.s_store_sk, r.s_store_sk) end, left = true }
    }, { selectFn = function(s, r) return {["channel"]="store channel", ["id"]=s.s_store_sk, ["sales"]=s.sales, ["returns"]=(function()
    if __eq(r, nil) then
        return 0.0
    else
        return r.returns
    end
end)(), ["profit"]=(s.profit - ((function()
    if __eq(r, nil) then
        return 0.0
    else
        return r.profit_loss
    end
end)()))} end })
end)(), (function()
    local _src = cs
    return __query(_src, {
        { items = cr, on = function(c, r) return __eq(c.cs_call_center_sk, r.cr_call_center_sk) end }
    }, { selectFn = function(c, r) return {["channel"]="catalog channel", ["id"]=c.cs_call_center_sk, ["sales"]=c.sales, ["returns"]=r.returns, ["profit"]=(c.profit - r.profit_loss)} end })
end)()), (function()
    local _src = ws
    return __query(_src, {
        { items = wr, on = function(w, r) return __eq(w.wp_web_page_sk, r.wp_web_page_sk) end, left = true }
    }, { selectFn = function(w, r) return {["channel"]="web channel", ["id"]=w.wp_web_page_sk, ["sales"]=w.sales, ["returns"]=(function()
    if __eq(r, nil) then
        return 0.0
    else
        return r.returns
    end
end)(), ["profit"]=(w.profit - ((function()
    if __eq(r, nil) then
        return 0.0
    else
        return r.profit_loss
    end
end)()))} end })
end)())
result = (function()
    local _src = per_channel
    local _rows = __query(_src, {
    }, { selectFn = function(p) return {p} end })
    local _groups = __group_by_rows(_rows, function(p) return {["channel"]=p.channel, ["id"]=p.id} end, function(p) local _row = __merge(p); _row.p = p; return _row end)
    local _res = {}
    for _, g in ipairs(_groups) do
        _res[#_res+1] = {["channel"]=g.key.channel, ["id"]=g.key.id, ["sales"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.p.sales
    end
    return _res
end)()), ["returns"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.p.returns
    end
    return _res
end)()), ["profit"]=__sum((function()
    local _res = {}
    for _, x in ipairs(g.items) do
        _res[#_res+1] = x.p.profit
    end
    return _res
end)())}
    end
    return _res
end)()
__json(result)
local __tests = {
    {name="TPCDS Q77 simplified", fn=test_TPCDS_Q77_simplified},
}
