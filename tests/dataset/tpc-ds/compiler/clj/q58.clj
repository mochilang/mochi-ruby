; Generated by Mochi compiler v0.10.25 on 2025-07-15T04:46:38Z
(ns main)

(defn _equal [a b]
  (cond
    (and (sequential? a) (sequential? b))
      (and (= (count a) (count b)) (every? true? (map _equal a b)))
    (and (map? a) (map? b))
      (and (= (count a) (count b))
           (every? (fn [k] (_equal (get a k) (get b k))) (keys a)))
    (and (number? a) (number? b))
      (= (double a) (double b))
    :else
      (= a b)))

(defn _escape_json [s]
  (-> s
      (clojure.string/replace "\\" "\\\\")
      (clojure.string/replace "\"" "\\\"")))

(defn _to_json [v]
  (cond
    (nil? v) "null"
    (string? v) (str "\"" (_escape_json v) "\"")
    (number? v) (str v)
    (boolean? v) (str v)
    (sequential? v) (str "[" (clojure.string/join "," (map _to_json v)) "]")
    (map? v) (str "{" (clojure.string/join "," (map (fn [[k val]]
                                        (str "\"" (_escape_json (name k)) "\":" (_to_json val))) v)) "}")
    :else (str "\"" (_escape_json (str v)) "\"")))

(defn _json [v]
  (println (_to_json v)))

(declare ss_items cs_items ws_items avg result)

(defn test_TPCDS_Q58_simplified []
  (assert (_equal result [{:item_id 1 :average 58.0}]) "expect failed")
)

(defn -main []
  (def ss_items {:item_id 1 :ss_item_rev 50.0}) ;;
  (def cs_items {:item_id 1 :cs_item_rev 60.0}) ;;
  (def ws_items {:item_id 1 :ws_item_rev 64.0}) ;;
  (def avg (/ (+ (+ (:ss_item_rev ss_items) (:cs_item_rev cs_items)) (:ws_item_rev ws_items)) 3.0)) ;; float
  (def result [{:item_id (:item_id ss_items) :average avg}]) ;; list of
  (_json result)
  (test_TPCDS_Q58_simplified)
)

(-main)
