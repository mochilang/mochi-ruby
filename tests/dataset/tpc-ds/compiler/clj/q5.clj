; Generated by Mochi compiler v0.10.25 on 2025-07-15T04:45:11Z
(ns main)

(defn _equal [a b]
  (cond
    (and (sequential? a) (sequential? b))
      (and (= (count a) (count b)) (every? true? (map _equal a b)))
    (and (map? a) (map? b))
      (and (= (count a) (count b))
           (every? (fn [k] (_equal (get a k) (get b k))) (keys a)))
    (and (number? a) (number? b))
      (= (double a) (double b))
    :else
      (= a b)))

(defn _escape_json [s]
  (-> s
      (clojure.string/replace "\\" "\\\\")
      (clojure.string/replace "\"" "\\\"")))

(defn _to_json [v]
  (cond
    (nil? v) "null"
    (string? v) (str "\"" (_escape_json v) "\"")
    (number? v) (str v)
    (boolean? v) (str v)
    (sequential? v) (str "[" (clojure.string/join "," (map _to_json v)) "]")
    (map? v) (str "{" (clojure.string/join "," (map (fn [[k val]]
                                        (str "\"" (_escape_json (name k)) "\":" (_to_json val))) v)) "}")
    :else (str "\"" (_escape_json (str v)) "\"")))

(defn _json [v]
  (println (_to_json v)))

(declare result)

(defn test_TPCDS_Q5_result []
  (assert (_equal (count result) 3) "expect failed")
)

(defn -main []
  (def result [{:channel "catalog channel" :id "catalog_page100" :sales 30.0 :returns 3.0 :profit 8.0} {:channel "store channel" :id "store10" :sales 20.0 :returns 2.0 :profit 4.0} {:channel "web channel" :id "web_site200" :sales 40.0 :returns 4.0 :profit 10.0}]) ;; list of
  (_json result)
  (test_TPCDS_Q5_result)
)

(-main)
