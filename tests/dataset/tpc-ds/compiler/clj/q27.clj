; Generated by Mochi compiler v0.10.25 on 2025-07-15T04:45:49Z
(ns main)

(defn _avg [v]
  (let [lst (cond
              (and (map? v) (contains? v :Items)) (:Items v)
              (sequential? v) v
              :else (throw (ex-info "avg() expects list or group" {})))]
    (if (empty? lst)
      0
      (/ (reduce + lst) (double (count lst)))))
  )

(defn _equal [a b]
  (cond
    (and (sequential? a) (sequential? b))
      (and (= (count a) (count b)) (every? true? (map _equal a b)))
    (and (map? a) (map? b))
      (and (= (count a) (count b))
           (every? (fn [k] (_equal (get a k) (get b k))) (keys a)))
    (and (number? a) (number? b))
      (= (double a) (double b))
    :else
      (= a b)))

(defrecord _Group [key Items])

(defn _group_by [src keyfn]
  (let [groups (transient {})
        order (transient [])]
    (doseq [it src]
      (let [k (keyfn it)
            ks (str k)
            g (get groups ks)]
        (if g
          (assoc! groups ks (assoc g :Items (conj (:Items g) it)))
          (do
            (assoc! groups ks (_Group. k [it]))
            (conj! order ks))))
    )
    (let [g (persistent! groups)
          o (persistent! order)]
      (mapv #(get g %) o))))

(defn _escape_json [s]
  (-> s
      (clojure.string/replace "\\" "\\\\")
      (clojure.string/replace "\"" "\\\"")))

(defn _to_json [v]
  (cond
    (nil? v) "null"
    (string? v) (str "\"" (_escape_json v) "\"")
    (number? v) (str v)
    (boolean? v) (str v)
    (sequential? v) (str "[" (clojure.string/join "," (map _to_json v)) "]")
    (map? v) (str "{" (clojure.string/join "," (map (fn [[k val]]
                                        (str "\"" (_escape_json (name k)) "\":" (_to_json val))) v)) "}")
    :else (str "\"" (_escape_json (str v)) "\"")))

(defn _json [v]
  (println (_to_json v)))

(defn _sort_key [k]
  (cond
    (map? k) (pr-str (into (sorted-map) k))
    (sequential? k) (vec k)
    :else k))
(defn _query [src joins opts]
  (let [items (atom (mapv vector src))]
    (doseq [j joins]
      (let [joined (atom [])]
        (if (and (:leftKey j) (:rightKey j))
          (let [idx (group-by (fn [it] (apply (:rightKey j) [it])) (:items j))
                seen (atom #{})]
            (doseq [left @items]
              (let [k (apply (:leftKey j) left)
                    rs (get idx k)]
                (if rs
                  (do
                    (swap! seen conj k)
                    (doseq [right rs]
                      (swap! joined conj (conj left right))))
                  (when (:left j)
                    (swap! joined conj (conj left nil)))))
            (when (:right j)
              (doseq [right (:items j)]
                (when-not (contains? @seen (apply (:rightKey j) [right]))
                  (swap! joined conj (vec (concat (repeat (count (first (or @items []))) nil) [right]))))))
            (reset! items @joined))
          (cond
            (and (:right j) (:left j))
              (let [matched (boolean-array (count (:items j)))]
                (doseq [left @items]
                  (let [m (atom false)]
                    (doseq [[ri right] (map-indexed vector (:items j))]
                      (let [keep (if-let [f (:on j)]
                                   (apply f (conj left right))
                                   true)]
                        (when keep
                          (reset! m true)
                          (aset matched ri true)
                          (swap! joined conj (conj left right))))
                    (when-not @m
                      (swap! joined conj (conj left nil))))
                (doseq [[ri right] (map-indexed vector (:items j))]
                  (when-not (aget matched ri)
                    (swap! joined conj (vec (concat (repeat (count (first (or @items []))) nil) [right])))))
              (reset! items @joined)
            (:right j)
              (do
                (doseq [right (:items j)]
                  (let [m (atom false)]
                    (doseq [left @items]
                      (let [keep (if-let [f (:on j)]
                                   (apply f (conj left right))
                                   true)]
                        (when keep
                          (reset! m true)
                          (swap! joined conj (conj left right))))
                    (when-not @m
                      (swap! joined conj (vec (concat (repeat (count (first (or @items []))) nil) [right])))))
                (reset! items @joined))
            :else
              (do
                (doseq [left @items]
                  (let [m (atom false)]
                    (doseq [right (:items j)]
                      (let [keep (if-let [f (:on j)]
                                   (apply f (conj left right))
                                   true)]
                        (when keep
                          (reset! m true)
                          (swap! joined conj (conj left right))))
                    (when (and (:left j) (not @m))
                      (swap! joined conj (conj left nil))))
                (reset! items @joined)))))
    (let [it @items
          it (if-let [w (:where opts)] (vec (filter #(apply w %) it)) it)
          it (if-let [sk (:sortKey opts)]
               (vec (sort-by #(let [k (apply sk %)] (_sort_key k)) it))
               it)
          it (if (contains? opts :skip) (vec (drop (:skip opts) it)) it)
          it (if (contains? opts :take) (vec (take (:take opts) it)) it)]
      (mapv #(apply (:select opts) %) it)))))))))))))
(declare store_sales customer_demographics date_dim store item result)

(defn StoreSale [ss_item_sk ss_store_sk ss_cdemo_sk ss_sold_date_sk ss_quantity ss_list_price ss_coupon_amt ss_sales_price]
  {:__name "StoreSale" :ss_item_sk ss_item_sk :ss_store_sk ss_store_sk :ss_cdemo_sk ss_cdemo_sk :ss_sold_date_sk ss_sold_date_sk :ss_quantity ss_quantity :ss_list_price ss_list_price :ss_coupon_amt ss_coupon_amt :ss_sales_price ss_sales_price}
)


(defn CustomerDemo [cd_demo_sk cd_gender cd_marital_status cd_education_status]
  {:__name "CustomerDemo" :cd_demo_sk cd_demo_sk :cd_gender cd_gender :cd_marital_status cd_marital_status :cd_education_status cd_education_status}
)


(defn DateDim [d_date_sk d_year]
  {:__name "DateDim" :d_date_sk d_date_sk :d_year d_year}
)


(defn Store [s_store_sk s_state]
  {:__name "Store" :s_store_sk s_store_sk :s_state s_state}
)


(defn Item [i_item_sk i_item_id]
  {:__name "Item" :i_item_sk i_item_sk :i_item_id i_item_id}
)


(defn test_TPCDS_Q27_averages_by_state []
  (assert (_equal result [{:i_item_id "ITEM1" :s_state "CA" :agg1 5.0 :agg2 100.0 :agg3 10.0 :agg4 90.0}]) "expect failed")
)

(defn -main []
  (def store_sales [{:ss_item_sk 1 :ss_store_sk 1 :ss_cdemo_sk 1 :ss_sold_date_sk 1 :ss_quantity 5 :ss_list_price 100.0 :ss_coupon_amt 10.0 :ss_sales_price 90.0} {:ss_item_sk 2 :ss_store_sk 2 :ss_cdemo_sk 2 :ss_sold_date_sk 1 :ss_quantity 2 :ss_list_price 50.0 :ss_coupon_amt 5.0 :ss_sales_price 45.0}]) ;; list of
  (def customer_demographics [{:cd_demo_sk 1 :cd_gender "F" :cd_marital_status "M" :cd_education_status "College"} {:cd_demo_sk 2 :cd_gender "M" :cd_marital_status "S" :cd_education_status "College"}]) ;; list of
  (def date_dim [{:d_date_sk 1 :d_year 2000}]) ;; list of
  (def store [{:s_store_sk 1 :s_state "CA"} {:s_store_sk 2 :s_state "TX"}]) ;; list of
  (def item [{:i_item_sk 1 :i_item_id "ITEM1"} {:i_item_sk 2 :i_item_id "ITEM2"}]) ;; list of
  (def result (let [_src store_sales
      _rows (_query _src [
        {:items customer_demographics :leftKey (fn [ss] (:ss_cdemo_sk ss)) :rightKey (fn [cd] (:cd_demo_sk cd))}
        {:items date_dim :leftKey (fn [ss cd] (:ss_sold_date_sk ss)) :rightKey (fn [d] (:d_date_sk d))}
        {:items store :leftKey (fn [ss cd d] (:ss_store_sk ss)) :rightKey (fn [s] (:s_store_sk s))}
        {:items item :leftKey (fn [ss cd d s] (:ss_item_sk ss)) :rightKey (fn [i] (:i_item_sk i))}
      ] { :select (fn [ss cd d s i] [ss cd d s i]) :where (fn [ss cd d s i] (and (and (and (and (_equal (:cd_gender cd) "F") (_equal (:cd_marital_status cd) "M")) (_equal (:cd_education_status cd) "College")) (_equal (:d_year d) 2000)) (some #(= (:s_state s) %) ["CA"]))) })
      _groups (_group_by _rows (fn [ss cd d s i] {:item_id (:i_item_id i) :state (:s_state s)}))
      ]
  (vec (map (fn [g] {:i_item_id (:item_id (:key g)) :s_state (:state (:key g)) :agg1 (let [xs (vec (->> (for [x (:Items g)] (:ss_quantity x))))] (if (empty? xs) 0 (/ (reduce + xs) (double (count xs))))) :agg2 (let [xs (vec (->> (for [x (:Items g)] (:ss_list_price x))))] (if (empty? xs) 0 (/ (reduce + xs) (double (count xs))))) :agg3 (let [xs (vec (->> (for [x (:Items g)] (:ss_coupon_amt x))))] (if (empty? xs) 0 (/ (reduce + xs) (double (count xs))))) :agg4 (let [xs (vec (->> (for [x (:Items g)] (:ss_sales_price x))))] (if (empty? xs) 0 (/ (reduce + xs) (double (count xs)))))}) _groups)))) ;; list of
  (_json result)
  (test_TPCDS_Q27_averages_by_state)
)

(-main)
