; Generated by Mochi compiler v0.10.25 on 2025-07-15T04:47:18Z
(ns main)

(defn _equal [a b]
  (cond
    (and (sequential? a) (sequential? b))
      (and (= (count a) (count b)) (every? true? (map _equal a b)))
    (and (map? a) (map? b))
      (and (= (count a) (count b))
           (every? (fn [k] (_equal (get a k) (get b k))) (keys a)))
    (and (number? a) (number? b))
      (= (double a) (double b))
    :else
      (= a b)))

(defn _escape_json [s]
  (-> s
      (clojure.string/replace "\\" "\\\\")
      (clojure.string/replace "\"" "\\\"")))

(defn _to_json [v]
  (cond
    (nil? v) "null"
    (string? v) (str "\"" (_escape_json v) "\"")
    (number? v) (str v)
    (boolean? v) (str v)
    (sequential? v) (str "[" (clojure.string/join "," (map _to_json v)) "]")
    (map? v) (str "{" (clojure.string/join "," (map (fn [[k val]]
                                        (str "\"" (_escape_json (name k)) "\":" (_to_json val))) v)) "}")
    :else (str "\"" (_escape_json (str v)) "\"")))

(defn _json [v]
  (println (_to_json v)))

(declare web_returns result)

(defn test_TPCDS_Q85_sample []
  (assert (_equal result 85.0) "expect failed")
)

(defn -main []
  (def web_returns [{:qty 60 :cash 20.0 :fee 1.0} {:qty 100 :cash 30.0 :fee 2.0} {:qty 95 :cash 25.0 :fee 3.0}]) ;; list of
  (def result (let [xs (vec (->> (for [r web_returns] (:qty r))))] (if (empty? xs) 0 (/ (reduce + xs) (double (count xs)))))) ;; float
  (_json result)
  (test_TPCDS_Q85_sample)
)

(-main)
