; Generated by Mochi compiler v0.10.25 on 2025-07-15T04:47:15Z
(ns main)

(defn _equal [a b]
  (cond
    (and (sequential? a) (sequential? b))
      (and (= (count a) (count b)) (every? true? (map _equal a b)))
    (and (map? a) (map? b))
      (and (= (count a) (count b))
           (every? (fn [k] (_equal (get a k) (get b k))) (keys a)))
    (and (number? a) (number? b))
      (= (double a) (double b))
    :else
      (= a b)))

(defn _escape_json [s]
  (-> s
      (clojure.string/replace "\\" "\\\\")
      (clojure.string/replace "\"" "\\\"")))

(defn _to_json [v]
  (cond
    (nil? v) "null"
    (string? v) (str "\"" (_escape_json v) "\"")
    (number? v) (str v)
    (boolean? v) (str v)
    (sequential? v) (str "[" (clojure.string/join "," (map _to_json v)) "]")
    (map? v) (str "{" (clojure.string/join "," (map (fn [[k val]]
                                        (str "\"" (_escape_json (name k)) "\":" (_to_json val))) v)) "}")
    :else (str "\"" (_escape_json (str v)) "\"")))

(defn _json [v]
  (println (_to_json v)))

(declare sr_items cr_items wr_items result)

(defn test_TPCDS_Q83_sample []
  (assert (_equal result 83) "expect failed")
)

(defn -main []
  (def sr_items [{:qty 10} {:qty 5}]) ;; list of
  (def cr_items [{:qty 25} {:qty 20}]) ;; list of
  (def wr_items [{:qty 10} {:qty 13}]) ;; list of
  (def result (+ (+ (reduce + 0 (vec (->> (for [x sr_items] (:qty x))))) (reduce + 0 (vec (->> (for [x cr_items] (:qty x)))))) (reduce + 0 (vec (->> (for [x wr_items] (:qty x))))))) ;; float
  (_json result)
  (test_TPCDS_Q83_sample)
)

(-main)
