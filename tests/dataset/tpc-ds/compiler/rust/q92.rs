// Generated by Mochi compiler v0.10.26 on 2025-07-15T07:22:56Z
#[derive(Default, Debug, Clone, PartialEq)]
struct WebSale {
        ws_item_sk: i32,
        ws_sold_date_sk: i32,
        ws_ext_discount_amt: f64,
}

#[derive(Default, Debug, Clone, PartialEq, PartialOrd)]
struct Web_sale {
    ws_item_sk: i32,
    ws_sold_date_sk: i32,
    ws_ext_discount_amt: f64,
}

#[derive(Default, Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord)]
struct Item {
    i_item_sk: i32,
    i_manufact_id: i32,
}

#[derive(Default, Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord)]
struct Date_dim {
    d_date_sk: i32,
    d_date: &'static str,
}

fn avg<T>(v: &[T]) -> f64 where T: Into<f64> + Copy {
    let sum: f64 = v.iter().map(|&x| x.into()).sum();
    sum / v.len() as f64
}

fn sum<T>(v: &[T]) -> T where T: std::iter::Sum<T> + Copy {
    v.iter().copied().sum()
}

fn _json<T: std::fmt::Debug>(value: &T) {
    println!("{:?}", value);
}

fn main() {
    let web_sales = vec![Web_sale { ws_item_sk: 1, ws_sold_date_sk: 1, ws_ext_discount_amt: 1.0 }, Web_sale { ws_item_sk: 1, ws_sold_date_sk: 1, ws_ext_discount_amt: 1.0 }, Web_sale { ws_item_sk: 1, ws_sold_date_sk: 1, ws_ext_discount_amt: 2.0 }];
    let item = vec![Item { i_item_sk: 1, i_manufact_id: 1 }];
    let date_dim = vec![Date_dim { d_date_sk: 1, d_date: "2000-01-02" }];
    let sum_amt = sum(&{ let mut tmp1 = Vec::new();for ws in &web_sales { tmp1.push(ws.ws_ext_discount_amt); } tmp1 });
    let avg_amt = avg(&{ let mut tmp2 = Vec::new();for ws in &web_sales { tmp2.push(ws.ws_ext_discount_amt); } tmp2 });
    let result = if sum_amt > avg_amt * 1.3 { sum_amt } else { 0.0 };
    _json(&result);
    assert!(result == 4.0);
}
