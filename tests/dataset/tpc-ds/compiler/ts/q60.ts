// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:15Z
// Source: tests/dataset/tpc-ds/q60.mochi

let all_sales: { [key: string]: number }[];
let catalog_sales: { [key: string]: number }[];
let result: number;
let store_sales: { [key: string]: number }[];
let web_sales: { [key: string]: number }[];

function test_TPCDS_Q60_simplified(): void {
  if (!(result == 60)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  store_sales = [
    {
      "item": 1,
      "price": 10,
    },
    {
      "item": 1,
      "price": 20,
    },
  ];
  catalog_sales = [
    {
      "item": 1,
      "price": 15,
    },
  ];
  web_sales = [
    {
      "item": 1,
      "price": 15,
    },
  ];
  all_sales = store_sales.concat(catalog_sales).concat(web_sales);
  result = all_sales.map((s) => s.price).reduce((a, b) => a + b, 0);
  console.log(_json(result));
  test_TPCDS_Q60_simplified();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
