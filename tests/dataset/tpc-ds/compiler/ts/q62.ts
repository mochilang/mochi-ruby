// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:15Z
// Source: tests/dataset/tpc-ds/q62.mochi

let result: any;
let web_sales: { [key: string]: number }[];

function test_TPCDS_Q62_simplified(): void {
  if (!(result == 62)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  web_sales = [
    { "days": 10 },
    { "days": 40 },
    { "days": 70 },
    { "days": 100 },
    { "days": 130 },
  ];
  result = (web_sales.length * 12) + 2;
  console.log(_json(result));
  test_TPCDS_Q62_simplified();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _order_init(): void {
  (globalThis as any)._channelOrder = undefined;
  if (typeof Deno !== "undefined" && Deno?.env?.get) {
    const env = Deno.env.get("CHANNEL_ORDER");
    if (env) {
      const m: Record<string, number> = {};
      env.split(",").forEach((k, i) => {
        m[k] = i;
      });
      (globalThis as any)._channelOrder = m;
    }
  }
}

main();
