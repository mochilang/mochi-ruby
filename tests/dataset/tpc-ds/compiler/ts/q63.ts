// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:15Z
// Source: tests/dataset/tpc-ds/q63.mochi

let by_mgr: Record<string, any>[];
let result: number;
let sales: { [key: string]: number }[];

function test_TPCDS_Q63_simplified(): void {
  if (!(result == 63)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  sales = [
    {
      "mgr": 1,
      "amount": 30,
    },
    {
      "mgr": 2,
      "amount": 33,
    },
  ];
  by_mgr = (() => {
    const _src = sales;
    const _map = new Map<string, any>();
    var _items = [];
    for (const s of _src) {
      const _key = { "mgr": s.mgr };
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
      }
      _g.items.push({ ...s, s: s });
    }
    let _groups = Array.from(_map.values());
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "mgr": g.key.mgr,
        "sum_sales": g.items.map((x) => x.amount).reduce((a, b) => a + b, 0),
      });
    }
    return _res;
  })();
  result = by_mgr.map((x) => x.sum_sales).reduce((a, b) => a + Number(b), 0);
  console.log(_json(result));
  test_TPCDS_Q63_simplified();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
