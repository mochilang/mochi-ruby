// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:17Z
// Source: tests/dataset/tpc-ds/q87.mochi

let catalog_customers: string[];
let catalog_sales: { [key: string]: string }[];
let result: number;
let store_customers: any[];
let store_only: any[];
let store_sales: Record<string, any>[];
let web_customers: any[];
let web_sales: any[];

function test_TPCDS_Q87_sample(): void {
  if (!(result == 87)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  store_sales = [
    {
      "cust": "A",
      "price": 5,
    },
    {
      "cust": "B",
      "price": 30,
    },
    {
      "cust": "C",
      "price": 57,
    },
  ];
  catalog_sales = [{ "cust": "A" }];
  web_sales = [];
  store_customers = store_sales.map((s) => s.cust);
  catalog_customers = catalog_sales.map((s) => s.cust);
  web_customers = web_sales.map((s) => s.cust);
  store_only = store_customers.filter(
    (c) => ((catalog_customers.filter((x) => (x == c)).map((x) => x).length ==
      0) && (web_customers.filter((x) => (x == c)).map((x) => x).length == 0))
  ).map((c) => c);
  result = store_sales.filter(
    (s) => (store_only.filter((x) => (x == s.cust)).map((x) => x).length > 0)
  ).map((s) => s.price).reduce((a, b) => a + Number(b), 0);
  console.log(_json(result));
  test_TPCDS_Q87_sample();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
