// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:17Z
// Source: tests/dataset/tpc-ds/q85.mochi

let result: number;
let web_returns: Record<string, any>[];

function test_TPCDS_Q85_sample(): void {
  if (!(result == 85)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  web_returns = [
    {
      "qty": 60,
      "cash": 20,
      "fee": 1,
    },
    {
      "qty": 100,
      "cash": 30,
      "fee": 2,
    },
    {
      "qty": 95,
      "cash": 25,
      "fee": 3,
    },
  ];
  result = web_returns.map((r) => r.qty).reduce((a, b) => a + Number(b), 0) /
    web_returns.map((r) => r.qty).length;
  console.log(_json(result));
  test_TPCDS_Q85_sample();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
