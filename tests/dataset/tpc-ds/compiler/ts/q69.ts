// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:16Z
// Source: tests/dataset/tpc-ds/q69.mochi

let result: number;
let store_sales: { [key: string]: number }[];
let web_sales: { [key: string]: number }[];

function test_TPCDS_Q69_simplified(): void {
  if (!(result == 69)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  web_sales = [{ "amount": 34 }];
  store_sales = [{ "amount": 35 }];
  result = web_sales.map((w) => w.amount).reduce((a, b) => a + b, 0) +
    store_sales.map((s) => s.amount).reduce((a, b) => a + b, 0);
  console.log(_json(result));
  test_TPCDS_Q69_simplified();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
