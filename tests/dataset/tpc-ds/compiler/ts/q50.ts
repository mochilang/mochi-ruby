// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:15Z
// Source: tests/dataset/tpc-ds/q50.mochi

let date_dim: { [key: string]: number }[];
let joined: Record<string, any>[];
let month: number;
let result: Record<string, any>[];
let store: Record<string, any>[];
let store_returns: { [key: string]: number }[];
let store_sales: { [key: string]: number }[];
let year: number;

function test_TPCDS_Q50_simplified(): void {
  if (
    !(_equal(result, [
      {
        "s_store_name": "Main",
        "d30": 1,
        "d31_60": 1,
        "d61_90": 1,
        "d91_120": 1,
        "d_gt_120": 1,
      },
    ]))
  ) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  store_sales = [
    {
      "ticket": 1,
      "item": 101,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 2,
      "item": 102,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 3,
      "item": 103,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 4,
      "item": 104,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
    {
      "ticket": 5,
      "item": 105,
      "sold": 1,
      "customer": 1,
      "store": 1,
    },
  ];
  store_returns = [
    {
      "ticket": 1,
      "item": 101,
      "returned": 16,
      "customer": 1,
    },
    {
      "ticket": 2,
      "item": 102,
      "returned": 46,
      "customer": 1,
    },
    {
      "ticket": 3,
      "item": 103,
      "returned": 76,
      "customer": 1,
    },
    {
      "ticket": 4,
      "item": 104,
      "returned": 111,
      "customer": 1,
    },
    {
      "ticket": 5,
      "item": 105,
      "returned": 151,
      "customer": 1,
    },
  ];
  date_dim = [
    {
      "d_date_sk": 1,
      "d_year": 2001,
      "d_moy": 7,
    },
    {
      "d_date_sk": 16,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 46,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 76,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 111,
      "d_year": 2001,
      "d_moy": 8,
    },
    {
      "d_date_sk": 151,
      "d_year": 2001,
      "d_moy": 8,
    },
  ];
  store = [
    {
      "s_store_sk": 1,
      "s_store_name": "Main",
      "s_company_id": 1,
      "s_street_number": "1",
      "s_street_name": "Main",
      "s_street_type": "St",
      "s_suite_number": "100",
      "s_city": "City",
      "s_county": "County",
      "s_state": "CA",
      "s_zip": "12345",
    },
  ];
  year = 2001;
  month = 8;
  joined = (() => {
    const _src = store_sales;
    const _res = [];
    for (const ss of _src) {
      for (const sr of store_returns) {
        if (
          !(((ss.ticket == sr.ticket) && (ss.item == sr.item)) &&
            (ss.customer == sr.customer))
        ) continue;
        for (const d1 of date_dim) {
          if (!(ss.sold == d1.d_date_sk)) continue;
          for (const d2 of date_dim) {
            if (
              !(((sr.returned == d2.d_date_sk) && (d2.d_year == year)) &&
                (d2.d_moy == month))
            ) continue;
            for (const s of store) {
              if (!(ss.store == s.s_store_sk)) continue;
              _res.push({
                "s": s,
                "diff": (sr.returned - ss.sold),
              });
            }
          }
        }
      }
    }
    return _res;
  })();
  result = (() => {
    const _src = joined;
    const _map = new Map<string, any>();
    var _items = [];
    for (const j of _src) {
      const _key = j.s;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
      }
      _g.items.push({ ...j, j: j });
    }
    let _groups = Array.from(_map.values());
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "s_store_name": g.key.s_store_name,
        "d30": g.items.filter((x) => (x.diff <= 30)).map((x) => 1).length,
        "d31_60":
          g.items.filter((x) => ((x.diff > 30) && (x.diff <= 60))).map((x) => 1)
            .length,
        "d61_90":
          g.items.filter((x) => ((x.diff > 60) && (x.diff <= 90))).map((x) => 1)
            .length,
        "d91_120":
          g.items.filter((x) => ((x.diff > 90) && (x.diff <= 120))).map((x) =>
            1
          ).length,
        "d_gt_120": g.items.filter((x) => (x.diff > 120)).map((x) => 1).length,
      });
    }
    return _res;
  })();
  console.log(_json(result));
  test_TPCDS_Q50_simplified();
}
function _equal(a: unknown, b: unknown): boolean {
  if (typeof a === "number" && typeof b === "number") {
    return Math.abs(a - b) < 1e-9;
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _order_init(): void {
  (globalThis as any)._channelOrder = undefined;
  if (typeof Deno !== "undefined" && Deno?.env?.get) {
    const env = Deno.env.get("CHANNEL_ORDER");
    if (env) {
      const m: Record<string, number> = {};
      env.split(",").forEach((k, i) => {
        m[k] = i;
      });
      (globalThis as any)._channelOrder = m;
    }
  }
}

main();
