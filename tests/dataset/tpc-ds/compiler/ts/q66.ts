// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:15Z
// Source: tests/dataset/tpc-ds/q66.mochi

let catalog_sales: { [key: string]: number }[];
let result: number;
let web_sales: { [key: string]: number }[];

function test_TPCDS_Q66_simplified(): void {
  if (!(result == 66)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  web_sales = [{ "net": 30 }];
  catalog_sales = [{ "net": 36 }];
  result = web_sales.map((w) => w.net).reduce((a, b) => a + b, 0) +
    catalog_sales.map((c) => c.net).reduce((a, b) => a + b, 0);
  console.log(_json(result));
  test_TPCDS_Q66_simplified();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
