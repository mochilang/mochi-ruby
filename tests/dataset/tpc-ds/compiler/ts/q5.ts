// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:12Z
// Source: tests/dataset/tpc-ds/q5.mochi

let result: Record<string, any>[];

function test_TPCDS_Q5_result(): void {
  if (!(result.length == 3)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  result = [
    {
      "channel": "catalog channel",
      "id": "catalog_page100",
      "sales": 30,
      "returns": 3,
      "profit": 8,
    },
    {
      "channel": "store channel",
      "id": "store10",
      "sales": 20,
      "returns": 2,
      "profit": 4,
    },
    {
      "channel": "web channel",
      "id": "web_site200",
      "sales": 40,
      "returns": 4,
      "profit": 10,
    },
  ];
  console.log(_json(result));
  test_TPCDS_Q5_result();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

function _order_init(): void {
  (globalThis as any)._channelOrder = undefined;
  if (typeof Deno !== "undefined" && Deno?.env?.get) {
    const env = Deno.env.get("CHANNEL_ORDER");
    if (env) {
      const m: Record<string, number> = {};
      env.split(",").forEach((k, i) => {
        m[k] = i;
      });
      (globalThis as any)._channelOrder = m;
    }
  }
}

main();
