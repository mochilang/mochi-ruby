// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:15Z
// Source: tests/dataset/tpc-ds/q61.mochi

let promotions: number;
let result: number;
let sales: Record<string, any>[];
let total: number;

function test_TPCDS_Q61_simplified(): void {
  if (!(result == 61)) throw new Error("expect failed");
}

function main(): void {
  _order_init();
  sales = [
    {
      "promo": true,
      "price": 20,
    },
    {
      "promo": true,
      "price": 41,
    },
    {
      "promo": false,
      "price": 39,
    },
  ];
  promotions = sales.filter((s) => (s.promo)).map((s) => s.price).reduce(
    (a, b) => a + Number(b),
    0,
  );
  total = sales.map((s) => s.price).reduce((a, b) => a + Number(b), 0);
  result = (promotions * 100) / total;
  console.log(_json(result));
  test_TPCDS_Q61_simplified();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
