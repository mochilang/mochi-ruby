// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:46:16Z
// Source: tests/dataset/tpc-ds/q83.mochi

let cr_items: { [key: string]: number }[];
let result: number;
let sr_items: { [key: string]: number }[];
let wr_items: { [key: string]: number }[];

function test_TPCDS_Q83_sample(): void {
  if (!(result == 83)) throw new Error("expect failed");
}

function main(): void {
  sr_items = [{ "qty": 10 }, { "qty": 5 }];
  cr_items = [{ "qty": 25 }, { "qty": 20 }];
  wr_items = [{ "qty": 10 }, { "qty": 13 }];
  result =
    (sr_items.map((x) => x.qty).reduce((a, b) => a + b, 0) +
      cr_items.map((x) => x.qty).reduce((a, b) => a + b, 0)) +
    wr_items.map((x) => x.qty).reduce((a, b) => a + b, 0);
  console.log(_json(result));
  test_TPCDS_Q83_sample();
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
