//go:build ignore

// Generated by Mochi compiler v0.10.26 on 2025-07-16T01:06:02Z

package main

import (
	"encoding/json"
	"fmt"
	"time"

	"golang.org/x/exp/constraints"
)

type Catalog_sale struct {
	Item   int `json:"item"`
	Profit int `json:"profit"`
}

type Store_sale struct {
	Item   int `json:"item"`
	Profit int `json:"profit"`
}

type v map[string]any

func expect(cond bool) {
	if !cond {
		panic("expect failed")
	}
}

func formatDuration(d time.Duration) string {
	switch {
	case d < time.Microsecond:
		return fmt.Sprintf("%dns", d.Nanoseconds())
	case d < time.Millisecond:
		return fmt.Sprintf("%.1fµs", float64(d.Microseconds()))
	case d < time.Second:
		return fmt.Sprintf("%.1fms", float64(d.Milliseconds()))
	default:
		return fmt.Sprintf("%.2fs", d.Seconds())
	}
}

func printTestStart(name string) {
	fmt.Printf("   test %-30s ...", name)
}

func printTestPass(d time.Duration) {
	fmt.Printf(" ok (%s)\n", formatDuration(d))
}

func printTestFail(err error, d time.Duration) {
	fmt.Printf(" fail %v (%s)\n", err, formatDuration(d))
}

func test_TPCDS_Q68_simplified() {
	expect((result == 68))
}

var catalog_sales []Catalog_sale
var store_sales []Store_sale
var result float64

func main() {
	catalog_sales = []Catalog_sale{Catalog_sale{
		Item:   1,
		Profit: 30,
	}, Catalog_sale{
		Item:   2,
		Profit: 38,
	}}
	store_sales = []Store_sale{Store_sale{
		Item:   1,
		Profit: 30,
	}}
	result = ((_sumOrdered[int](func() []int {
		results := []int{}
		for _, c := range catalog_sales {
			results = append(results, c.Profit)
		}
		return results
	}()) - _sumOrdered[int](func() []int {
		results := []int{}
		for _, s := range store_sales {
			results = append(results, s.Profit)
		}
		return results
	}())) + float64(30))
	func() { b, _ := json.Marshal(result); fmt.Println(string(b)) }()
	test_TPCDS_Q68_simplified()
}

func _sumOrdered[T constraints.Integer | constraints.Float](s []T) float64 {
	var sum float64
	for _, v := range s {
		sum += float64(v)
	}
	return sum
}
