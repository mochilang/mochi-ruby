#!/usr/bin/env escript
% Generated by Mochi compiler v0.10.26 on 2025-07-15T07:26:13Z
% q92.erl - generated from q92.mochi

main(_) ->
    Web_sales = [#{ws_item_sk => 1, ws_sold_date_sk => 1, ws_ext_discount_amt => 1}, #{ws_item_sk => 1, ws_sold_date_sk => 1, ws_ext_discount_amt => 1}, #{ws_item_sk => 1, ws_sold_date_sk => 1, ws_ext_discount_amt => 2}],
    Item = [#{i_item_sk => 1, i_manufact_id => 1}],
    Date_dim = [#{d_date_sk => 1, d_date => "2000-01-02"}],
    Sum_amt = lists:sum([mochi_get(ws_ext_discount_amt, Ws) || Ws <- Web_sales]),
    Avg_amt = (lists:sum([mochi_get(ws_ext_discount_amt, Ws) || Ws <- Web_sales]) / length([mochi_get(ws_ext_discount_amt, Ws) || Ws <- Web_sales])),
    Result = (case (Sum_amt > (Avg_amt * 1.3)) of true -> Sum_amt; _ -> 0 end),
    mochi_json(Result),
    (case (Result == 4) of true -> ok; _ -> erlang:error(test_failed) end).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
    
    mochi_escape_json([]) -> [];
    mochi_escape_json([H|T]) ->
        E = case H of
            $\ -> "\\";
            $" -> "\"";
            _ -> [H]
        end,
        E ++ mochi_escape_json(T).
    
    mochi_to_json(true) -> "true";
    mochi_to_json(false) -> "false";
    mochi_to_json(V) when is_integer(V); is_float(V) -> lists:flatten(io_lib:format("~p", [V]));
    mochi_to_json(V) when is_binary(V) -> "\"" ++ mochi_escape_json(binary_to_list(V)) ++ "\"";
    mochi_to_json(V) when is_list(V), (V =:= [] orelse is_integer(hd(V))) -> "\"" ++ mochi_escape_json(V) ++ "\"";
    mochi_to_json(V) when is_list(V) -> "[" ++ lists:join(",", [mochi_to_json(X) || X <- V]) ++ "]";
    mochi_to_json(V) when is_map(V) -> "{" ++ lists:join(",", ["\"" ++ atom_to_list(K) ++ "\":" ++ mochi_to_json(Val) || {K,Val} <- maps:to_list(V)]) ++ "}".
    
    mochi_json(V) -> io:format("~s
", [mochi_to_json(V)]).
