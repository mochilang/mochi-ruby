#!/usr/bin/env escript
% Generated by Mochi compiler v0.10.26 on 2025-07-15T07:25:45Z
% q22.erl - generated from q22.mochi

main(_) ->
    Inventory = [#{inv_item_sk => 1, inv_date_sk => 1, inv_quantity_on_hand => 10}, #{inv_item_sk => 1, inv_date_sk => 2, inv_quantity_on_hand => 20}, #{inv_item_sk => 1, inv_date_sk => 3, inv_quantity_on_hand => 10}, #{inv_item_sk => 1, inv_date_sk => 4, inv_quantity_on_hand => 20}, #{inv_item_sk => 2, inv_date_sk => 1, inv_quantity_on_hand => 50}],
    Date_dim = [#{d_date_sk => 1, d_month_seq => 0}, #{d_date_sk => 2, d_month_seq => 1}, #{d_date_sk => 3, d_month_seq => 2}, #{d_date_sk => 4, d_month_seq => 3}],
    Item = [#{i_item_sk => 1, i_product_name => "Prod1", i_brand => "Brand1", i_class => "Class1", i_category => "Cat1"}, #{i_item_sk => 2, i_product_name => "Prod2", i_brand => "Brand2", i_class => "Class2", i_category => "Cat2"}],
    Qoh = [#{i_product_name => mochi_get(product_name, Key0), i_brand => mochi_get(brand, Key0), i_class => mochi_get(class, Key0), i_category => mochi_get(category, Key0), qoh => (lists:sum([mochi_get(inv_quantity_on_hand, X) || X <- Val0]) / length([mochi_get(inv_quantity_on_hand, X) || X <- Val0]))} || {Key0, Val0} <- maps:to_list(lists:foldl(fun({Key0, Val0}, Acc0) -> L = maps:get(Key0, Acc0, []), maps:put(Key0, [Val0 | L], Acc0) end, #{}, [{#{product_name => mochi_get(i_product_name, I), brand => mochi_get(i_brand, I), class => mochi_get(i_class, I), category => mochi_get(i_category, I)}, #{inv => Inv, d => D, i => I}} || Inv <- Inventory, D <- Date_dim, I <- Item, (mochi_get(inv_date_sk, Inv) == mochi_get(d_date_sk, D)), (mochi_get(inv_item_sk, Inv) == mochi_get(i_item_sk, I)), ((mochi_get(d_month_seq, D) >= 0) andalso (mochi_get(d_month_seq, D) =< 11))]))],
    mochi_json(Qoh),
    (case (Qoh == [#{i_product_name => "Prod1", i_brand => "Brand1", i_class => "Class1", i_category => "Cat1", qoh => 15}, #{i_product_name => "Prod2", i_brand => "Brand2", i_class => "Class2", i_category => "Cat2", qoh => 50}]) of true -> ok; _ -> erlang:error(test_failed) end).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
    
    mochi_escape_json([]) -> [];
    mochi_escape_json([H|T]) ->
        E = case H of
            $\ -> "\\";
            $" -> "\"";
            _ -> [H]
        end,
        E ++ mochi_escape_json(T).
    
    mochi_to_json(true) -> "true";
    mochi_to_json(false) -> "false";
    mochi_to_json(V) when is_integer(V); is_float(V) -> lists:flatten(io_lib:format("~p", [V]));
    mochi_to_json(V) when is_binary(V) -> "\"" ++ mochi_escape_json(binary_to_list(V)) ++ "\"";
    mochi_to_json(V) when is_list(V), (V =:= [] orelse is_integer(hd(V))) -> "\"" ++ mochi_escape_json(V) ++ "\"";
    mochi_to_json(V) when is_list(V) -> "[" ++ lists:join(",", [mochi_to_json(X) || X <- V]) ++ "]";
    mochi_to_json(V) when is_map(V) -> "{" ++ lists:join(",", ["\"" ++ atom_to_list(K) ++ "\":" ++ mochi_to_json(Val) || {K,Val} <- maps:to_list(V)]) ++ "}".
    
    mochi_json(V) -> io:format("~s
", [mochi_to_json(V)]).
