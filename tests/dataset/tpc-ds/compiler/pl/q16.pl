% Generated by Mochi compiler v0.10.26 on 1970-01-01T00:00:00Z
:- style_check(-singleton).
get_item(Container, Key, Val) :-
    is_dict(Container), !, (string(Key) -> atom_string(A, Key) ; A = Key), get_dict(A, Container, Val).
get_item(Container, Index, Val) :-
    string(Container), !, string_chars(Container, Chars), nth0(Index, Chars, Val).
get_item(List, Index, Val) :- nth0(Index, List, Val).

len_any(Value, Len) :-
    string(Value), !, string_length(Value, Len).
len_any(Value, Len) :-
    is_dict(Value), !, dict_pairs(Value, _, Pairs), length(Pairs, Len).
len_any(Value, Len) :- length(Value, Len).

to_list(Str, L) :-
    string(Str), !,
    string_chars(Str, L).
to_list(L, L).

count(V, R) :-
    is_dict(V), !, get_dict('Items', V, Items), length(Items, R).
count(V, R) :-
    string(V), !, string_chars(V, C), length(C, R).
count(V, R) :-
    is_list(V), !, length(V, R).
count(_, _) :- throw(error('count expects list or group')).

avg(V, R) :-
    is_dict(V), !, get_dict('Items', V, Items), avg_list(Items, R).
avg(V, R) :-
    is_list(V), !, avg_list(V, R).
avg(_, _) :- throw(error('avg expects list or group')).
avg_list([], 0).
avg_list(L, R) :- sum_list(L, S), length(L, N), N > 0, R is S / N.

sum(V, R) :-
    is_dict(V), !, get_dict('Items', V, Items), sum_list(Items, R).
sum(V, R) :-
    is_list(V), !, sum_list(V, R).
sum(_, _) :- throw(error('sum expects list or group')).

group_insert(Key, Item, [], [_{key:Key, 'Items':[Item]}]).
group_insert(Key, Item, [G|Gs], [NG|Gs]) :- get_dict(key, G, Key), !, get_dict('Items', G, Items), append(Items, [Item], NItems), put_dict('Items', G, NItems, NG).
group_insert(Key, Item, [G|Gs], [G|Rs]) :- group_insert(Key, Item, Gs, Rs).
group_pairs([], Acc, Res) :- reverse(Acc, Res).
group_pairs([K-V|T], Acc, Res) :- group_insert(K, V, Acc, Acc1), group_pairs(T, Acc1, Res).
group_by(List, Fn, Groups) :- findall(K-V, (member(V, List), call(Fn, V, K)), Pairs), group_pairs(Pairs, [], Groups).

:- use_module(library(http/json)).
load_data(Path, Opts, Rows) :-
    (is_dict(Opts), get_dict(format, Opts, Fmt) -> true ; Fmt = 'json'),
    (Path == '' ; Path == '-' -> read_string(user_input, _, Text) ; read_file_to_string(Path, Text, [])),
    (Fmt == 'jsonl' ->
        split_string(Text, '\n', ' \t\r', Lines0),
        exclude(=(''), Lines0, Lines),
        findall(D, (member(L, Lines), open_string(L, S), json_read_dict(S, D), close(S)), Rows)
    ;
        open_string(Text, S), json_read_dict(S, Data), close(S),
        (is_list(Data) -> Rows = Data ; Rows = [Data])
    ).

expect(Cond) :- (Cond -> true ; throw(error('expect failed'))).

distinct(Xs, _Res) :-
    nb_setval(out, []),
    catch(
        (
            member(X, Xs),
                catch(
                    (
                        nb_getval(out, _V0),
                        contains(_V0, X, _V1),
                        (_V1 -> _V2 = false ; _V2 = true),
                        (_V2 \= nil ->
                            nb_getval(out, _V3),
                            append(_V3, [X], _V4),
                            nb_setval(out, _V4),
                            true
                        ; true
                        ),
                        true
                    ), continue, true),
                    fail
                ; true
            ), break, true),
            nb_getval(out, _V5),
            _Res = _V5.
        
        :- initialization(main, main).
        main :-
            dict_create(_V0, map, [cs_order_number-1, cs_ship_date_sk-1, cs_ship_addr_sk-1, cs_call_center_sk-1, cs_warehouse_sk-1, cs_ext_ship_cost-5, cs_net_profit-20]),
            dict_create(_V1, map, [cs_order_number-1, cs_ship_date_sk-1, cs_ship_addr_sk-1, cs_call_center_sk-1, cs_warehouse_sk-2, cs_ext_ship_cost-0, cs_net_profit-0]),
            Catalog_sales = [_V0, _V1],
            dict_create(_V2, map, [d_date_sk-1, d_date-"2000-03-01"]),
            Date_dim = [_V2],
            dict_create(_V3, map, [ca_address_sk-1, ca_state-"CA"]),
            Customer_address = [_V3],
            dict_create(_V4, map, [cc_call_center_sk-1, cc_county-"CountyA"]),
            Call_center = [_V4],
            Catalog_returns = [],
            findall(_V26, (member(Cs1, Catalog_sales), member(D, Date_dim), get_item(Cs1, 'cs_ship_date_sk', _V5), get_item(D, 'd_date_sk', _V6), get_item(D, 'd_date', _V7), get_item(D, 'd_date', _V8), (((_V5 == _V6), (_V7 >= "2000-03-01")), (_V8 =< "2000-04-30")), member(Ca, Customer_address), get_item(Cs1, 'cs_ship_addr_sk', _V9), get_item(Ca, 'ca_address_sk', _V10), get_item(Ca, 'ca_state', _V11), ((_V9 == _V10), (_V11 == "CA")), member(Cc, Call_center), get_item(Cs1, 'cs_call_center_sk', _V12), get_item(Cc, 'cc_call_center_sk', _V13), get_item(Cc, 'cc_county', _V14), ((_V12 == _V13), (_V14 == "CountyA")), get_item(Cs1, 'cs_order_number', _V16), get_item(Cs2, 'cs_order_number', _V17), get_item(Cs1, 'cs_warehouse_sk', _V18), get_item(Cs2, 'cs_warehouse_sk', _V19), (once((member(Cs2, Catalog_sales), ((_V16 == _V17), (_V18 \== _V19)))) -> _V15 = true ; _V15 = false), get_item(Cs1, 'cs_order_number', _V21), get_item(Cr, 'cr_order_number', _V22), (once((member(Cr, Catalog_returns), (_V21 == _V22))) -> _V20 = true ; _V20 = false), (_V15, (_V20 == false)), dict_create(_V23, map, []), _V24 = _V23, dict_create(_V25, map, ['Cs1'-Cs1, 'D'-D, 'Ca'-Ca, 'Cc'-Cc]), _V26 = _V24-_V25), _V27),
            group_pairs(_V27, [], _V28),
            findall(_V43, (member(G, _V28), findall(_V30, (member(X, G), true, get_item(X, 'cs_order_number', _V29), _V30 = _V29), _V31), distinct(_V31, _V32), len_any(_V32, _V33), findall(_V35, (member(X, G), true, get_item(X, 'cs_ext_ship_cost', _V34), _V35 = _V34), _V36), sum(_V36, _V37), findall(_V39, (member(X, G), true, get_item(X, 'cs_net_profit', _V38), _V39 = _V38), _V40), sum(_V40, _V41), dict_create(_V42, map, [order_count-_V33, total_shipping_cost-_V37, total_net_profit-_V41]), _V43 = _V42), _V44),
            Filtered = _V44,
            json_write_dict(current_output, Filtered), nl,
            true,
            dict_create(_V45, map, [order_count-1, total_shipping_cost-5, total_net_profit-20]),
            expect((Filtered == [_V45])),
            true.
