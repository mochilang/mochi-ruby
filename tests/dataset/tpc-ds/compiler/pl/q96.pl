% Generated by Mochi compiler v0.10.26 on 2025-07-15T07:11:28Z
:- style_check(-singleton).
get_item(Container, Key, Val) :-
    is_dict(Container), !, (string(Key) -> atom_string(A, Key) ; A = Key), get_dict(A, Container, Val).
get_item(Container, Index, Val) :-
    string(Container), !, string_chars(Container, Chars), nth0(Index, Chars, Val).
get_item(List, Index, Val) :- nth0(Index, List, Val).

to_list(Str, L) :-
    string(Str), !,
    string_chars(Str, L).
to_list(L, L).

count(V, R) :-
    is_dict(V), !, get_dict('Items', V, Items), length(Items, R).
count(V, R) :-
    string(V), !, string_chars(V, C), length(C, R).
count(V, R) :-
    is_list(V), !, length(V, R).
count(_, _) :- throw(error('count expects list or group')).

avg(V, R) :-
    is_dict(V), !, get_dict('Items', V, Items), avg_list(Items, R).
avg(V, R) :-
    is_list(V), !, avg_list(V, R).
avg(_, _) :- throw(error('avg expects list or group')).
avg_list([], 0).
avg_list(L, R) :- sum_list(L, S), length(L, N), N > 0, R is S / N.

sum(V, R) :-
    is_dict(V), !, get_dict('Items', V, Items), sum_list(Items, R).
sum(V, R) :-
    is_list(V), !, sum_list(V, R).
sum(_, _) :- throw(error('sum expects list or group')).

group_insert(Key, Item, [], [_{key:Key, 'Items':[Item]}]).
group_insert(Key, Item, [G|Gs], [NG|Gs]) :- get_dict(key, G, Key), !, get_dict('Items', G, Items), append(Items, [Item], NItems), put_dict('Items', G, NItems, NG).
group_insert(Key, Item, [G|Gs], [G|Rs]) :- group_insert(Key, Item, Gs, Rs).
group_pairs([], Acc, Res) :- reverse(Acc, Res).
group_pairs([K-V|T], Acc, Res) :- group_insert(K, V, Acc, Acc1), group_pairs(T, Acc1, Res).
group_by(List, Fn, Groups) :- findall(K-V, (member(V, List), call(Fn, V, K)), Pairs), group_pairs(Pairs, [], Groups).

:- use_module(library(http/json)).
load_data(Path, Opts, Rows) :-
    (is_dict(Opts), get_dict(format, Opts, Fmt) -> true ; Fmt = 'json'),
    (Path == '' ; Path == '-' -> read_string(user_input, _, Text) ; read_file_to_string(Path, Text, [])),
    (Fmt == 'jsonl' ->
        split_string(Text, '\n', ' \t\r', Lines0),
        exclude(=(''), Lines0, Lines),
        findall(D, (member(L, Lines), open_string(L, S), json_read_dict(S, D), close(S)), Rows)
    ;
        open_string(Text, S), json_read_dict(S, Data), close(S),
        (is_list(Data) -> Rows = Data ; Rows = [Data])
    ).

expect(Cond) :- (Cond -> true ; throw(error('expect failed'))).

:- initialization(main, main).
main :-
    dict_create(_V0, map, [ss_sold_time_sk-1, ss_hdemo_sk-1, ss_store_sk-1]),
    dict_create(_V1, map, [ss_sold_time_sk-1, ss_hdemo_sk-1, ss_store_sk-1]),
    dict_create(_V2, map, [ss_sold_time_sk-2, ss_hdemo_sk-1, ss_store_sk-1]),
    Store_sales = [_V0, _V1, _V2],
    dict_create(_V3, map, [hd_demo_sk-1, hd_dep_count-3]),
    Household_demographics = [_V3],
    dict_create(_V4, map, [t_time_sk-1, t_hour-20, t_minute-35]),
    dict_create(_V5, map, [t_time_sk-2, t_hour-20, t_minute-45]),
    Time_dim = [_V4, _V5],
    dict_create(_V6, map, [s_store_sk-1, s_store_name-"ese"]),
    Store = [_V6],
    findall(_V17, (member(Ss, Store_sales), member(Hd, Household_demographics), get_item(Ss, 'ss_hdemo_sk', _V7), get_item(Hd, 'hd_demo_sk', _V8), (_V7 == _V8), member(T, Time_dim), get_item(Ss, 'ss_sold_time_sk', _V9), get_item(T, 't_time_sk', _V10), (_V9 == _V10), member(S, Store), get_item(Ss, 'ss_store_sk', _V11), get_item(S, 's_store_sk', _V12), (_V11 == _V12), get_item(T, 't_hour', _V13), get_item(T, 't_minute', _V14), get_item(Hd, 'hd_dep_count', _V15), get_item(S, 's_store_name', _V16), ((((_V13 == 20), (_V14 >= 30)), (_V15 == 3)), (_V16 == "ese")), _V17 = Ss), _V18),
    count(_V18, _V19),
    Result is _V19,
    json_write_dict(current_output, Result), nl,
    true,
    expect((Result == 3)),
    true.
