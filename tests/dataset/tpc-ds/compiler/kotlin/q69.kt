// Generated by Mochi compiler v0.10.26 on 2025-07-15T07:17:51Z
fun sum(list: List<Any?>): Number {
    var s = 0.0
    var allInt = true
    for (n in list) {
        val d = toDouble(n)
        if (d % 1.0 != 0.0) allInt = false
        s += d
    }
    return if (allInt) s.toInt() else s
}

fun String.starts_with(prefix: String): Boolean = this.startsWith(prefix)

fun toDouble(v: Any?): Double = when (v) {
    is Double -> v
    is Int -> v.toDouble()
    is String -> v.toDouble()
    else -> 0.0
}

fun json(v: Any?) {
    println(toJson(v))
}

fun toJson(v: Any?): String = when (v) {
    null -> "null"
    is String -> "\"" + v.replace("\"", "\\\"") + "\""
    is Boolean, is Number -> v.toString()
    is Map<*, *> -> v.entries.joinToString(prefix = "{", postfix = "}") { toJson(it.key.toString()) + ":" + toJson(it.value) }
    is Iterable<*> -> v.joinToString(prefix = "[", postfix = "]") { toJson(it) }
    else -> toJson(v.toString())
}
// Code generated from q69.mochi

data class Store_sale(var amount: Int)

data class Web_sale(var amount: Int)

val web_sales = mutableListOf(Web_sale(amount = 34))

val store_sales = mutableListOf(Store_sale(amount = 35))

val result = toDouble(sum(run {
    val __res = mutableListOf<Int>()
    for (w in web_sales) {
        __res.add(w.amount)
    }
    __res
})) + toDouble(sum(run {
    val __res = mutableListOf<Int>()
    for (s in store_sales) {
        __res.add(s.amount)
    }
    __res
}))

fun main() {
    json(result)
}
