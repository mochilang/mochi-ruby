// Generated by Mochi compiler v0.10.26 on 2025-07-15T07:18:29Z
fun sum(list: List<Any?>): Number {
    var s = 0.0
    var allInt = true
    for (n in list) {
        val d = toDouble(n)
        if (d % 1.0 != 0.0) allInt = false
        s += d
    }
    return if (allInt) s.toInt() else s
}

fun String.starts_with(prefix: String): Boolean = this.startsWith(prefix)

fun toDouble(v: Any?): Double = when (v) {
    is Double -> v
    is Int -> v.toDouble()
    is String -> v.toDouble()
    else -> 0.0
}

fun json(v: Any?) {
    println(toJson(v))
}

fun toJson(v: Any?): String = when (v) {
    null -> "null"
    is String -> "\"" + v.replace("\"", "\\\"") + "\""
    is Boolean, is Number -> v.toString()
    is Map<*, *> -> v.entries.joinToString(prefix = "{", postfix = "}") { toJson(it.key.toString()) + ":" + toJson(it.value) }
    is Iterable<*> -> v.joinToString(prefix = "[", postfix = "]") { toJson(it) }
    else -> toJson(v.toString())
}
// Code generated from q80.mochi

data class Catalog_sale(var price: Double, var ret: Double)

data class Store_sale(var price: Double, var ret: Double)

data class Web_sale(var price: Double, var ret: Double)

val store_sales = mutableListOf(Store_sale(price = 20.0, ret = 5.0), Store_sale(price = 10.0, ret = 2.0), Store_sale(price = 5.0, ret = 0.0))

val catalog_sales = mutableListOf(Catalog_sale(price = 15.0, ret = 3.0), Catalog_sale(price = 8.0, ret = 1.0))

val web_sales = mutableListOf(Web_sale(price = 25.0, ret = 5.0), Web_sale(price = 15.0, ret = 8.0), Web_sale(price = 8.0, ret = 2.0))

val total_profit = toDouble(toDouble(sum(run {
    val __res = mutableListOf<Double>()
    for (s in store_sales) {
        __res.add(toDouble(s.price) - toDouble(s.ret))
    }
    __res
})) + toDouble(sum(run {
    val __res = mutableListOf<Double>()
    for (c in catalog_sales) {
        __res.add(toDouble(c.price) - toDouble(c.ret))
    }
    __res
}))) + toDouble(sum(run {
    val __res = mutableListOf<Double>()
    for (w in web_sales) {
        __res.add(toDouble(w.price) - toDouble(w.ret))
    }
    __res
}))

fun main() {
    json(total_profit)
}
