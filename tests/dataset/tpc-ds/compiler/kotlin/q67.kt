// Generated by Mochi compiler v0.10.26 on 2025-07-15T07:17:46Z
fun sum(list: List<Any?>): Number {
    var s = 0.0
    var allInt = true
    for (n in list) {
        val d = toDouble(n)
        if (d % 1.0 != 0.0) allInt = false
        s += d
    }
    return if (allInt) s.toInt() else s
}

fun String.starts_with(prefix: String): Boolean = this.startsWith(prefix)

fun toDouble(v: Any?): Double = when (v) {
    is Double -> v
    is Int -> v.toDouble()
    is String -> v.toDouble()
    else -> 0.0
}

fun json(v: Any?) {
    println(toJson(v))
}

fun toJson(v: Any?): String = when (v) {
    null -> "null"
    is String -> "\"" + v.replace("\"", "\\\"") + "\""
    is Boolean, is Number -> v.toString()
    is Map<*, *> -> v.entries.joinToString(prefix = "{", postfix = "}") { toJson(it.key.toString()) + ":" + toJson(it.value) }
    is Iterable<*> -> v.joinToString(prefix = "[", postfix = "]") { toJson(it) }
    else -> toJson(v.toString())
}
// Code generated from q67.mochi

data class Reason(var id: Int, var name: String)

data class Store_sale(var reason: Int, var price: Int)

val store_sales = mutableListOf(Store_sale(reason = 1, price = 40), Store_sale(reason = 2, price = 27))

val reason = mutableListOf(Reason(id = 1, name = "PROMO"), Reason(id = 2, name = "RETURN"))

val result = sum(run {
    val __res = mutableListOf<Int>()
    for (ss in store_sales) {
        for (r in reason) {
            if (ss.reason == r.id) {
                if (r.name in mutableListOf("PROMO", "RETURN")) {
                    __res.add(ss.price)
                }
            }
        }
    }
    __res
})

fun main() {
    json(result)
}
