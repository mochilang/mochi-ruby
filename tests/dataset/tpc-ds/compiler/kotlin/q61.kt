// Generated by Mochi compiler v0.10.26 on 2025-07-15T07:17:29Z
fun sum(list: List<Any?>): Number {
    var s = 0.0
    var allInt = true
    for (n in list) {
        val d = toDouble(n)
        if (d % 1.0 != 0.0) allInt = false
        s += d
    }
    return if (allInt) s.toInt() else s
}

fun String.starts_with(prefix: String): Boolean = this.startsWith(prefix)

fun toDouble(v: Any?): Double = when (v) {
    is Double -> v
    is Int -> v.toDouble()
    is String -> v.toDouble()
    else -> 0.0
}

fun json(v: Any?) {
    println(toJson(v))
}

fun toJson(v: Any?): String = when (v) {
    null -> "null"
    is String -> "\"" + v.replace("\"", "\\\"") + "\""
    is Boolean, is Number -> v.toString()
    is Map<*, *> -> v.entries.joinToString(prefix = "{", postfix = "}") { toJson(it.key.toString()) + ":" + toJson(it.value) }
    is Iterable<*> -> v.joinToString(prefix = "[", postfix = "]") { toJson(it) }
    else -> toJson(v.toString())
}
// Code generated from q61.mochi

data class Sale(var promo: Boolean, var price: Int)

val sales = mutableListOf(Sale(promo = true, price = 20), Sale(promo = true, price = 41), Sale(promo = false, price = 39))

val promotions = sum(run {
    val __res = mutableListOf<Int>()
    for (s in sales) {
        if (s.promo) {
            __res.add(s.price)
        }
    }
    __res
})

val total = sum(run {
    val __res = mutableListOf<Int>()
    for (s in sales) {
        __res.add(s.price)
    }
    __res
})

val result = (toDouble(promotions) * 100).toDouble() / toDouble(total)

fun main() {
    json(result)
}
