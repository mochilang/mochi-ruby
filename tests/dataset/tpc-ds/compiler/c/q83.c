// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static int _sum_int(list_int v) {
  int sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  int qty;
} sr_item_t;
typedef struct {
  int len;
  sr_item_t *data;
} sr_item_list_t;
sr_item_list_t create_sr_item_list(int len) {
  sr_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(sr_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int qty;
} cr_item_t;
typedef struct {
  int len;
  cr_item_t *data;
} cr_item_list_t;
cr_item_list_t create_cr_item_list(int len) {
  cr_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(cr_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int qty;
} wr_item_t;
typedef struct {
  int len;
  wr_item_t *data;
} wr_item_list_t;
wr_item_list_t create_wr_item_list(int len) {
  wr_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(wr_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static double test_TPCDS_Q83_sample_result;
static void test_TPCDS_Q83_sample() {
  if (!(test_TPCDS_Q83_sample_result == 83)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  sr_item_t sr_items[] = {(sr_item_t){.qty = 10}, (sr_item_t){.qty = 5}};
  int sr_items_len = sizeof(sr_items) / sizeof(sr_items[0]);
  cr_item_t cr_items[] = {(cr_item_t){.qty = 25}, (cr_item_t){.qty = 20}};
  int cr_items_len = sizeof(cr_items) / sizeof(cr_items[0]);
  wr_item_t wr_items[] = {(wr_item_t){.qty = 10}, (wr_item_t){.qty = 13}};
  int wr_items_len = sizeof(wr_items) / sizeof(wr_items[0]);
  list_int tmp1 = list_int_create(sr_items_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < sr_items_len; tmp3++) {
    sr_item_t x = sr_items[tmp3];
    tmp1.data[tmp2] = x.qty;
    tmp2++;
  }
  tmp1.len = tmp2;
  list_int tmp4 = list_int_create(cr_items_len);
  int tmp5 = 0;
  for (int tmp6 = 0; tmp6 < cr_items_len; tmp6++) {
    cr_item_t x = cr_items[tmp6];
    tmp4.data[tmp5] = x.qty;
    tmp5++;
  }
  tmp4.len = tmp5;
  list_int tmp7 = list_int_create(wr_items_len);
  int tmp8 = 0;
  for (int tmp9 = 0; tmp9 < wr_items_len; tmp9++) {
    wr_item_t x = wr_items[tmp9];
    tmp7.data[tmp8] = x.qty;
    tmp8++;
  }
  tmp7.len = tmp8;
  double result = _sum_int(tmp1) + _sum_int(tmp4) + _sum_int(tmp7);
  _json_float(result);
  test_TPCDS_Q83_sample_result = result;
  test_TPCDS_Q83_sample();
  free(tmp1.data);
  free(tmp4.data);
  free(tmp7.data);
  return 0;
}
