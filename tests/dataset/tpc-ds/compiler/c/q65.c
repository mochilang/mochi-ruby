// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  int store;
  int item;
  int price;
} store_sale_t;
typedef struct {
  int len;
  store_sale_t *data;
} store_sale_list_t;
store_sale_list_t create_store_sale_list(int len) {
  store_sale_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(store_sale_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int item;
  double revenue;
} item_revenue_item_t;
typedef struct {
  int len;
  item_revenue_item_t *data;
} item_revenue_item_list_t;
item_revenue_item_list_t create_item_revenue_item_list(int len) {
  item_revenue_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(item_revenue_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

double average(list_float xs) {
  if (xs.len == 0) {
    return 0.0;
  }
  double sum = 0.0;
  for (int tmp1 = 0; tmp1 < xs.len; tmp1++) {
    double x = xs.data[tmp1];
    sum = sum + x;
  }
  return sum / ((double)(xs.len));
}

static double test_TPCDS_Q65_simplified_result;
static void test_TPCDS_Q65_simplified() {
  if (!(test_TPCDS_Q65_simplified_result == 65)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  store_sale_t store_sales[] = {
      (store_sale_t){.store = 1, .item = 1, .price = 1},
      (store_sale_t){.store = 1, .item = 1, .price = 1},
      (store_sale_t){.store = 1, .item = 2, .price = 60}};
  int store_sales_len = sizeof(store_sales) / sizeof(store_sales[0]);
  item_revenue_item_list_t item_revenue = (item_revenue_item_list_t){0, NULL};
  list_float tmp2 = list_float_create(item_revenue.len);
  int tmp3 = 0;
  for (int tmp4 = 0; tmp4 < item_revenue.len; tmp4++) {
    item_revenue_item_t ir = item_revenue.data[tmp4];
    tmp2.data[tmp3] = ir.revenue;
    tmp3++;
  }
  tmp2.len = tmp3;
  double avg_rev = average(tmp2);
  list_float tmp5 = list_float_create(item_revenue.len);
  int tmp6 = 0;
  for (int tmp7 = 0; tmp7 < item_revenue.len; tmp7++) {
    item_revenue_item_t ir = item_revenue.data[tmp7];
    if (!(ir.revenue <= 0.1 * avg_rev)) {
      continue;
    }
    tmp5.data[tmp6] = ir.revenue;
    tmp6++;
  }
  tmp5.len = tmp6;
  list_float low_rev = tmp5;
  double result = _sum_float(low_rev) + 63;
  _json_float(result);
  test_TPCDS_Q65_simplified_result = result;
  test_TPCDS_Q65_simplified();
  free(tmp2.data);
  free(low_rev.data);
  return 0;
}
