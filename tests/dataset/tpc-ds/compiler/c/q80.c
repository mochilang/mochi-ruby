// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
static void _json_int(int v) { printf("%d", v); }
static void _json_float(double v) { printf("%g", v); }
static void _json_string(char *s) { printf("\"%s\"", s); }
static void _json_list_int(list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_int(v.data[i]);
  }
  printf("]");
}
static void _json_list_float(list_float v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_float(v.data[i]);
  }
  printf("]");
}
static void _json_list_string(list_string v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_string(v.data[i]);
  }
  printf("]");
}
static void _json_list_list_int(list_list_int v) {
  printf("[");
  for (int i = 0; i < v.len; i++) {
    if (i > 0)
      printf(",");
    _json_list_int(v.data[i]);
  }
  printf("]");
}
typedef struct {
  double price;
  double ret;
} store_sale_t;
typedef struct {
  int len;
  store_sale_t *data;
} store_sale_list_t;
store_sale_list_t create_store_sale_list(int len) {
  store_sale_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(store_sale_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  double price;
  double ret;
} catalog_sale_t;
typedef struct {
  int len;
  catalog_sale_t *data;
} catalog_sale_list_t;
catalog_sale_list_t create_catalog_sale_list(int len) {
  catalog_sale_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(catalog_sale_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  double price;
  double ret;
} web_sale_t;
typedef struct {
  int len;
  web_sale_t *data;
} web_sale_list_t;
web_sale_list_t create_web_sale_list(int len) {
  web_sale_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(web_sale_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

static double test_TPCDS_Q80_sample_total_profit;
static void test_TPCDS_Q80_sample() {
  if (!(test_TPCDS_Q80_sample_total_profit == 80.0)) {
    fprintf(stderr, "expect failed\n");
    exit(1);
  }
}

int main() {
  store_sale_t store_sales[] = {(store_sale_t){.price = 20.0, .ret = 5.0},
                                (store_sale_t){.price = 10.0, .ret = 2.0},
                                (store_sale_t){.price = 5.0, .ret = 0.0}};
  int store_sales_len = sizeof(store_sales) / sizeof(store_sales[0]);
  catalog_sale_t catalog_sales[] = {(catalog_sale_t){.price = 15.0, .ret = 3.0},
                                    (catalog_sale_t){.price = 8.0, .ret = 1.0}};
  int catalog_sales_len = sizeof(catalog_sales) / sizeof(catalog_sales[0]);
  web_sale_t web_sales[] = {(web_sale_t){.price = 25.0, .ret = 5.0},
                            (web_sale_t){.price = 15.0, .ret = 8.0},
                            (web_sale_t){.price = 8.0, .ret = 2.0}};
  int web_sales_len = sizeof(web_sales) / sizeof(web_sales[0]);
  list_float tmp1 = list_float_create(store_sales_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < store_sales_len; tmp3++) {
    store_sale_t s = store_sales[tmp3];
    tmp1.data[tmp2] = s.price - s.ret;
    tmp2++;
  }
  tmp1.len = tmp2;
  list_float tmp4 = list_float_create(catalog_sales_len);
  int tmp5 = 0;
  for (int tmp6 = 0; tmp6 < catalog_sales_len; tmp6++) {
    catalog_sale_t c = catalog_sales[tmp6];
    tmp4.data[tmp5] = c.price - c.ret;
    tmp5++;
  }
  tmp4.len = tmp5;
  list_float tmp7 = list_float_create(web_sales_len);
  int tmp8 = 0;
  for (int tmp9 = 0; tmp9 < web_sales_len; tmp9++) {
    web_sale_t w = web_sales[tmp9];
    tmp7.data[tmp8] = w.price - w.ret;
    tmp8++;
  }
  tmp7.len = tmp8;
  double total_profit = _sum_float(tmp1) + _sum_float(tmp4) + _sum_float(tmp7);
  _json_float(total_profit);
  test_TPCDS_Q80_sample_total_profit = total_profit;
  test_TPCDS_Q80_sample();
  free(tmp1.data);
  free(tmp4.data);
  free(tmp7.data);
  return 0;
}
