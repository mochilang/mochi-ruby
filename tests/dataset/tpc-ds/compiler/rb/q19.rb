# Generated by Mochi compiler v0.10.26 on 2025-07-15T06:46:47Z
require 'ostruct'

class MGroup
  include Enumerable
  attr_accessor :key, :Items
  def initialize(k)
    @key = k
    @Items = []
  end
  def length
    @Items.length
  end
  def items
    @Items
  end
  def each(&block)
    @Items.each(&block)
  end
end
def _group_by(src, keyfn)
grouped = src.group_by do |it|
  if it.is_a?(Array)
    keyfn.call(*it)
  else
    keyfn.call(it)
  end
end
grouped.map do |k, items|
g = MGroup.new(k)
items.each do |it|
  if it.is_a?(Array) && it.length == 1
    g.Items << it[0]
  else
    g.Items << it
  end
end
g
end
end
def _json(v)
  require 'json'
  obj = v
  if v.is_a?(Array)
    obj = v.map { |it| it.respond_to?(:to_h) ? it.to_h : it }
  elsif v.respond_to?(:to_h)
    obj = v.to_h
  end
  puts(JSON.generate(obj))
end
def _query(src, joins, opts)
  where_fn = opts['where']
  items = []
  if joins.empty?
    src.each do |v|
      row = [v]
      next if where_fn && !where_fn.call(*row)
      items << row
    end
  else
    items = src.map { |v| [v] }
    joins.each_with_index do |j, idx|
      joined = []
      jitems = j['items']
      on = j['on']
      left = j['left']
      right = j['right']
      last = idx == joins.length - 1
      if right && left
        matched = Array.new(jitems.length, false)
        items.each do |l|
          m = false
          jitems.each_with_index do |r, ri|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            matched[ri] = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          row = l + [nil]
          if left && !m
            if last && where_fn && !where_fn.call(*row)
              # skip
            else
              joined << row
            end
          end
        end
        jitems.each_with_index do |r, ri|
          next if matched[ri]
          _undef = Array.new(items[0]&.length || 0, nil)
          row = _undef + [r]
          if last && where_fn && !where_fn.call(*row)
            next
          end
          joined << row
        end
      elsif right
        jitems.each do |r|
          m = false
          items.each do |l|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          unless m
            _undef = Array.new(items[0]&.length || 0, nil)
            row = _undef + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
        end
      else
        items.each do |l|
          m = false
          jitems.each do |r|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          if left && !m
            row = l + [nil]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
        end
      end
      items = joined
    end
  end
  if opts['sortKey']
    items = items.map { |it| [it, opts['sortKey'].call(*it)] }
    items.sort_by! { |p| p[1] }
    items.map!(&:first)
  end
  if opts.key?('skip')
    n = opts['skip']
    items = n < items.length ? items[n..-1] : []
  end
  if opts.key?('take')
    n = opts['take']
    items = n < items.length ? items[0...n] : items
  end
  res = []
  items.each { |r| res << opts['select'].call(*r) }
  res
end
def _sliceString(s, i, j)
  start = i
  finish = j
  chars = s.chars
  n = chars.length
  start += n if start < 0
  finish += n if finish < 0
  start = 0 if start < 0
  finish = n if finish > n
  finish = start if finish < start
  chars[start...finish].join
end
def _sum(v)
  list = nil
  if defined?(MGroup) && v.is_a?(MGroup)
    list = v.Items
  elsif v.is_a?(Array)
    list = v
  elsif v.respond_to?(:to_a)
    list = v.to_a
  end
  return 0 if !list || list.empty?
  s = 0.0
  list.each { |n| s += n.to_f }
  s
end

StoreSale = Struct.new(:ss_sold_date_sk, :ss_item_sk, :ss_customer_sk, :ss_store_sk, :ss_ext_sales_price, keyword_init: true)

DateDim = Struct.new(:d_date_sk, :d_year, :d_moy, keyword_init: true)

Item = Struct.new(:i_item_sk, :i_brand_id, :i_brand, :i_manufact_id, :i_manufact, :i_manager_id, keyword_init: true)

Customer = Struct.new(:c_customer_sk, :c_current_addr_sk, keyword_init: true)

CustomerAddress = Struct.new(:ca_address_sk, :ca_zip, keyword_init: true)

Store = Struct.new(:s_store_sk, :s_zip, keyword_init: true)

$store_sales = [OpenStruct.new(ss_sold_date_sk: 1, ss_item_sk: 1, ss_customer_sk: 1, ss_store_sk: 1, ss_ext_sales_price: 100.0)]
$date_dim = [OpenStruct.new(d_date_sk: 1, d_year: 1999, d_moy: 11)]
$item = [OpenStruct.new(i_item_sk: 1, i_brand_id: 1, i_brand: "B1", i_manufact_id: 1, i_manufact: "M1", i_manager_id: 10)]
$customer = [OpenStruct.new(c_customer_sk: 1, c_current_addr_sk: 1)]
$customer_address = [OpenStruct.new(ca_address_sk: 1, ca_zip: "11111")]
$store = [OpenStruct.new(s_store_sk: 1, s_zip: "99999")]
$result = (begin
	src = $date_dim
	_rows = _query(src, [
		{ 'items' => $store_sales, 'on' => ->(d, ss){ (ss.ss_sold_date_sk == d.d_date_sk) } },
		{ 'items' => $item, 'on' => ->(d, ss, i){ ((ss.ss_item_sk == i.i_item_sk) && (i.i_manager_id == 10)) } },
		{ 'items' => $customer, 'on' => ->(d, ss, i, c){ (ss.ss_customer_sk == c.c_customer_sk) } },
		{ 'items' => $customer_address, 'on' => ->(d, ss, i, c, ca){ (c.c_current_addr_sk == ca.ca_address_sk) } },
		{ 'items' => $store, 'on' => ->(d, ss, i, c, ca, s){ ((ss.ss_store_sk == s.s_store_sk) && (_sliceString(ca.ca_zip, 0, 5) != _sliceString(s.s_zip, 0, 5))) } }
	], { 'select' => ->(d, ss, i, c, ca, s){ [d, ss, i, c, ca, s] }, 'where' => ->(d, ss, i, c, ca, s){ ((d.d_moy == 11) && (d.d_year == 1999)) } })
	_groups = _group_by(_rows, ->(d, ss, i, c, ca, s){ OpenStruct.new(brand: i.i_brand, brand_id: i.i_brand_id, man_id: i.i_manufact_id, man: i.i_manufact) })
	_items0 = _groups
	_items0 = _items0.sort_by { |g| [g.key.brand] }
	_res = []
	for g in _items0
		_res << OpenStruct.new(i_brand: g.key.brand, i_brand_id: g.key.brand_id, i_manufact_id: g.key.man_id, i_manufact: g.key.man, ext_price: _sum(((g)).map { |x| x.ss_ext_sales_price }))
	end
	_res
end)
_json($result)
raise "expect failed" unless ($result == [OpenStruct.new(i_brand: "B1", i_brand_id: 1, i_manufact_id: 1, i_manufact: "M1", ext_price: 100.0)])
