# Generated by Mochi compiler v0.10.26 on 2025-07-15T06:46:49Z
require 'ostruct'

def _avg(v)
  list = nil
  if defined?(MGroup) && v.is_a?(MGroup)
    list = v.Items
  elsif v.is_a?(Array)
    list = v
  elsif v.respond_to?(:to_a)
    list = v.to_a
  end
  return 0 if !list || list.empty?
  list.sum(0.0) / list.length
end
class MGroup
  include Enumerable
  attr_accessor :key, :Items
  def initialize(k)
    @key = k
    @Items = []
  end
  def length
    @Items.length
  end
  def items
    @Items
  end
  def each(&block)
    @Items.each(&block)
  end
end
def _group_by(src, keyfn)
grouped = src.group_by do |it|
  if it.is_a?(Array)
    keyfn.call(*it)
  else
    keyfn.call(it)
  end
end
grouped.map do |k, items|
g = MGroup.new(k)
items.each do |it|
  if it.is_a?(Array) && it.length == 1
    g.Items << it[0]
  else
    g.Items << it
  end
end
g
end
end
def _json(v)
  require 'json'
  obj = v
  if v.is_a?(Array)
    obj = v.map { |it| it.respond_to?(:to_h) ? it.to_h : it }
  elsif v.respond_to?(:to_h)
    obj = v.to_h
  end
  puts(JSON.generate(obj))
end
def _query(src, joins, opts)
  where_fn = opts['where']
  items = []
  if joins.empty?
    src.each do |v|
      row = [v]
      next if where_fn && !where_fn.call(*row)
      items << row
    end
  else
    items = src.map { |v| [v] }
    joins.each_with_index do |j, idx|
      joined = []
      jitems = j['items']
      on = j['on']
      left = j['left']
      right = j['right']
      last = idx == joins.length - 1
      if right && left
        matched = Array.new(jitems.length, false)
        items.each do |l|
          m = false
          jitems.each_with_index do |r, ri|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            matched[ri] = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          row = l + [nil]
          if left && !m
            if last && where_fn && !where_fn.call(*row)
              # skip
            else
              joined << row
            end
          end
        end
        jitems.each_with_index do |r, ri|
          next if matched[ri]
          _undef = Array.new(items[0]&.length || 0, nil)
          row = _undef + [r]
          if last && where_fn && !where_fn.call(*row)
            next
          end
          joined << row
        end
      elsif right
        jitems.each do |r|
          m = false
          items.each do |l|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          unless m
            _undef = Array.new(items[0]&.length || 0, nil)
            row = _undef + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
        end
      else
        items.each do |l|
          m = false
          jitems.each do |r|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          if left && !m
            row = l + [nil]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
        end
      end
      items = joined
    end
  end
  if opts['sortKey']
    items = items.map { |it| [it, opts['sortKey'].call(*it)] }
    items.sort_by! { |p| p[1] }
    items.map!(&:first)
  end
  if opts.key?('skip')
    n = opts['skip']
    items = n < items.length ? items[n..-1] : []
  end
  if opts.key?('take')
    n = opts['take']
    items = n < items.length ? items[0...n] : items
  end
  res = []
  items.each { |r| res << opts['select'].call(*r) }
  res
end
def _sum(v)
  list = nil
  if defined?(MGroup) && v.is_a?(MGroup)
    list = v.Items
  elsif v.is_a?(Array)
    list = v
  elsif v.respond_to?(:to_a)
    list = v.to_a
  end
  return 0 if !list || list.empty?
  s = 0.0
  list.each { |n| s += n.to_f }
  s
end

$web_returns = [OpenStruct.new(wr_returning_customer_sk: 1, wr_returned_date_sk: 1, wr_return_amt: 100.0, wr_returning_addr_sk: 1), OpenStruct.new(wr_returning_customer_sk: 2, wr_returned_date_sk: 1, wr_return_amt: 30.0, wr_returning_addr_sk: 2), OpenStruct.new(wr_returning_customer_sk: 1, wr_returned_date_sk: 1, wr_return_amt: 50.0, wr_returning_addr_sk: 1)]
$date_dim = [OpenStruct.new(d_date_sk: 1, d_year: 2000)]
$customer_address = [OpenStruct.new(ca_address_sk: 1, ca_state: "CA"), OpenStruct.new(ca_address_sk: 2, ca_state: "CA")]
$customer = [OpenStruct.new(c_customer_sk: 1, c_customer_id: "C1", c_first_name: "John", c_last_name: "Doe", c_current_addr_sk: 1), OpenStruct.new(c_customer_sk: 2, c_customer_id: "C2", c_first_name: "Jane", c_last_name: "Smith", c_current_addr_sk: 2)]
$customer_total_return = (begin
	src = $web_returns
	_rows = _query(src, [
		{ 'items' => $date_dim, 'on' => ->(wr, d){ (wr.wr_returned_date_sk == d.d_date_sk) } },
		{ 'items' => $customer_address, 'on' => ->(wr, d, ca){ (wr.wr_returning_addr_sk == ca.ca_address_sk) } }
	], { 'select' => ->(wr, d, ca){ [wr, d, ca] }, 'where' => ->(wr, d, ca){ ((d.d_year == 2000) && (ca.ca_state == "CA")) } })
	_groups = _group_by(_rows, ->(wr, d, ca){ OpenStruct.new(cust: wr.wr_returning_customer_sk, state: ca.ca_state) })
	_items0 = _groups
	_res = []
	for g in _items0
		_res << OpenStruct.new(ctr_customer_sk: g.key.cust, ctr_state: g.key.state, ctr_total_return: _sum(((g)).map { |x| x.wr_return_amt }))
	end
	_res
end)
$avg_by_state = _group_by($customer_total_return, ->(ctr){ ctr.ctr_state }).map { |g| OpenStruct.new(state: g.key, avg_return: _avg(((g)).map { |x| x.ctr_total_return })) }
$result = (begin
	_res = []
	for ctr in $customer_total_return
		for avg in $avg_by_state
			if (ctr.ctr_state == avg.state)
				for c in $customer
					if (ctr.ctr_customer_sk == c.c_customer_sk)
						if (ctr.ctr_total_return > (avg.avg_return * 1.2))
							_res << OpenStruct.new(c_customer_id: c.c_customer_id, c_first_name: c.c_first_name, c_last_name: c.c_last_name, ctr_total_return: ctr.ctr_total_return)
						end
					end
				end
			end
		end
	end
	_res
end)
_json($result)
raise "expect failed" unless ($result == [OpenStruct.new(c_customer_id: "C1", c_first_name: "John", c_last_name: "Doe", ctr_total_return: 150.0)])
