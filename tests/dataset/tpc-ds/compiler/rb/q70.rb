# Generated by Mochi compiler v0.10.27 on 2025-07-17T06:41:37Z
require 'ostruct'

class MGroup
  include Enumerable
  attr_accessor :key, :Items
  def initialize(k)
    @key = k
    @Items = []
  end
  def length
    @Items.length
  end
  def items
    @Items
  end
  def each(&block)
    @Items.each(&block)
  end
end
def _group_by(src, keyfn)
grouped = src.group_by do |it|
  if it.is_a?(Array)
    keyfn.call(*it)
  else
    keyfn.call(it)
  end
end
grouped.map do |k, items|
g = MGroup.new(k)
items.each do |it|
  if it.is_a?(Array) && it.length == 1
    g.Items << it[0]
  else
    g.Items << it
  end
end
g
end
end
def _json(v)
  require 'json'
  obj = v
  if v.is_a?(Array)
    obj = v.map { |it| it.respond_to?(:to_h) ? it.to_h : it }
  elsif v.respond_to?(:to_h)
    obj = v.to_h
  end
  puts(JSON.generate(obj))
end
def _query(src, joins, opts)
  where_fn = opts['where']
  items = []
  if joins.empty?
    src.each do |v|
      row = [v]
      next if where_fn && !where_fn.call(*row)
      items << row
    end
  else
    items = src.map { |v| [v] }
    joins.each_with_index do |j, idx|
      joined = []
      jitems = j['items']
      on = j['on']
      left = j['left']
      right = j['right']
      last = idx == joins.length - 1
      if right && left
        matched = Array.new(jitems.length, false)
        items.each do |l|
          m = false
          jitems.each_with_index do |r, ri|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            matched[ri] = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          row = l + [nil]
          if left && !m
            if last && where_fn && !where_fn.call(*row)
              # skip
            else
              joined << row
            end
          end
        end
        jitems.each_with_index do |r, ri|
          next if matched[ri]
          _undef = Array.new(items[0]&.length || 0, nil)
          row = _undef + [r]
          if last && where_fn && !where_fn.call(*row)
            next
          end
          joined << row
        end
      elsif right
        jitems.each do |r|
          m = false
          items.each do |l|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          unless m
            _undef = Array.new(items[0]&.length || 0, nil)
            row = _undef + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
        end
      else
        items.each do |l|
          m = false
          jitems.each do |r|
            keep = true
            keep = on.call(*l, r) if on
            next unless keep
            m = true
            row = l + [r]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
          if left && !m
            row = l + [nil]
            if last && where_fn && !where_fn.call(*row)
              next
            end
            joined << row
          end
        end
      end
      items = joined
    end
  end
  if opts['sortKey']
    items = items.map { |it| [it, opts['sortKey'].call(*it)] }
    items.sort_by! { |p| p[1] }
    items.map!(&:first)
  end
  if opts.key?('skip')
    n = opts['skip']
    items = n < items.length ? items[n..-1] : []
  end
  if opts.key?('take')
    n = opts['take']
    items = n < items.length ? items[0...n] : items
  end
  res = []
  items.each { |r| res << opts['select'].call(*r) }
  res
end
def _sum(v)
  list = nil
  if defined?(MGroup) && v.is_a?(MGroup)
    list = v.Items
  elsif v.is_a?(Array)
    list = v
  elsif v.respond_to?(:to_a)
    list = v.to_a
  end
  return 0 if !list || list.empty?
  list.sum(0.0)
end

$store = [OpenStruct.new(s_store_sk: 1, s_state: "CA", s_county: "Orange"), OpenStruct.new(s_store_sk: 2, s_state: "CA", s_county: "Orange"), OpenStruct.new(s_store_sk: 3, s_state: "TX", s_county: "Travis")]
$date_dim = [OpenStruct.new(d_date_sk: 1, d_month_seq: 1200), OpenStruct.new(d_date_sk: 2, d_month_seq: 1201)]
$store_sales = [OpenStruct.new(ss_sold_date_sk: 1, ss_store_sk: 1, ss_net_profit: 10.0), OpenStruct.new(ss_sold_date_sk: 1, ss_store_sk: 2, ss_net_profit: 5.0), OpenStruct.new(ss_sold_date_sk: 2, ss_store_sk: 3, ss_net_profit: 20.0)]
$dms = 1200
$result = (begin
	src = $store_sales
	_rows = _query(src, [
		{ 'items' => $date_dim, 'on' => ->(ss, d){ (d.d_date_sk == ss.ss_sold_date_sk) } },
		{ 'items' => $store, 'on' => ->(ss, d, s){ (s.s_store_sk == ss.ss_store_sk) } }
	], { 'select' => ->(ss, d, s){ [ss, d, s] }, 'where' => ->(ss, d, s){ ((d.d_month_seq >= $dms) && (d.d_month_seq <= ($dms + 11))) } })
	_groups = _group_by(_rows, ->(ss, d, s){ OpenStruct.new(state: s.s_state, county: s.s_county) })
	_items0 = _groups
	_items0 = _items0.sort_by { |g| [g.key.state, g.key.county] }
	_res = []
	for g in _items0
		_res << OpenStruct.new(s_state: g.key.state, s_county: g.key.county, total_sum: _sum(((g)).map { |x| x[0].ss_net_profit }))
	end
	_res
end)
_json($result)
raise "expect failed" unless ($result == [OpenStruct.new(s_state: "CA", s_county: "Orange", total_sum: 15.0), OpenStruct.new(s_state: "TX", s_county: "Travis", total_sum: 20.0)])
