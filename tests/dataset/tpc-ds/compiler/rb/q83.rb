# Generated by Mochi compiler v0.10.27 on 2025-07-17T06:41:40Z
require 'ostruct'

def _json(v)
  require 'json'
  obj = v
  if v.is_a?(Array)
    obj = v.map { |it| it.respond_to?(:to_h) ? it.to_h : it }
  elsif v.respond_to?(:to_h)
    obj = v.to_h
  end
  puts(JSON.generate(obj))
end
def _sum(v)
  list = nil
  if defined?(MGroup) && v.is_a?(MGroup)
    list = v.Items
  elsif v.is_a?(Array)
    list = v
  elsif v.respond_to?(:to_a)
    list = v.to_a
  end
  return 0 if !list || list.empty?
  list.sum(0.0)
end

$sr_items = [OpenStruct.new(qty: 10), OpenStruct.new(qty: 5)]
$cr_items = [OpenStruct.new(qty: 25), OpenStruct.new(qty: 20)]
$wr_items = [OpenStruct.new(qty: 10), OpenStruct.new(qty: 13)]
$result = ((_sum((($sr_items)).map { |x| x.qty }) + _sum((($cr_items)).map { |x| x.qty })) + _sum((($wr_items)).map { |x| x.qty }))
_json($result)
raise "expect failed" unless ($result == 83)
