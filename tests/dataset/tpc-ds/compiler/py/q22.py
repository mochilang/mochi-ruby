# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:49:50Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Auto1:
    i_product_name: str
    i_brand: str
    i_class: str
    i_category: str
    qoh: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto2:
    product_name: str
    brand: str
    _class: str
    category: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class DateDim:
    d_date_sk: int
    d_month_seq: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Inventory:
    inv_item_sk: int
    inv_date_sk: int
    inv_quantity_on_hand: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Item:
    i_item_sk: int
    i_product_name: str
    i_brand: str
    i_class: str
    i_category: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


class _Group(Generic[K, T]):

    def __init__(self, key: K):
        self.key = key
        self.Items: list[T] = []
        self.items = self.Items

    def __iter__(self):
        return iter(self.Items)

    def __len__(self):
        return len(self.Items)


def _group_by(src: list[T], keyfn: Callable[[T], K]) -> list[_Group[K, T]]:
    groups: dict[str, _Group[K, T]] = {}
    order: list[str] = []
    for it in src:
        if isinstance(it, (list, tuple)):
            key = keyfn(*it)
        else:
            key = keyfn(it)
        if isinstance(key, dict):
            import types

            key = types.SimpleNamespace(**key)
        ks = str(key)
        g = groups.get(ks)
        if not g:
            g = _Group(key)
            groups[ks] = g
            order.append(ks)
        g.Items.append(it)
    return [groups[k] for k in order]


def _query(src, joins, opts):
    items = [[v] for v in src]
    for j in joins:
        joined = []
        if j.get("right") and j.get("left"):
            matched = [False] * len(j["items"])
            for left in items:
                m = False
                for ri, right in enumerate(j["items"]):
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    matched[ri] = True
                    joined.append(left + [right])
                if not m:
                    joined.append(left + [None])
            for ri, right in enumerate(j["items"]):
                if not matched[ri]:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        elif j.get("right"):
            for right in j["items"]:
                m = False
                for left in items:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if not m:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        else:
            for left in items:
                m = False
                for right in j["items"]:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if j.get("left") and (not m):
                    joined.append(left + [None])
        items = joined
    if opts.get("where"):
        items = [r for r in items if opts["where"](*r)]
    if opts.get("sortKey"):

        def _key(it):
            k = opts["sortKey"](*it)
            if isinstance(k, (list, tuple, dict)):
                return str(k)
            return k

        items.sort(key=_key)
    if "skip" in opts:
        n = opts["skip"]
        if n < 0:
            n = 0
        items = items[n:] if n < len(items) else []
    if "take" in opts:
        n = opts["take"]
        if n < 0:
            n = 0
        items = items[:n] if n < len(items) else items
    res = []
    for r in items:
        res.append(opts["select"](*r))
    return res


@dataclasses.dataclass
class Inventory:
    inv_item_sk: int
    inv_date_sk: int
    inv_quantity_on_hand: int

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class DateDim:
    d_date_sk: int
    d_month_seq: int

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Item:
    i_item_sk: int
    i_product_name: str
    i_brand: str
    i_class: str
    i_category: str

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q22_average_inventory():
    assert qoh == [
        Auto1(
            i_product_name="Prod1",
            i_brand="Brand1",
            i_class="Class1",
            i_category="Cat1",
            qoh=15.0,
        ),
        Auto1(
            i_product_name="Prod2",
            i_brand="Brand2",
            i_class="Class2",
            i_category="Cat2",
            qoh=50.0,
        ),
    ]


inventory = [
    Inventory(inv_item_sk=1, inv_date_sk=1, inv_quantity_on_hand=10),
    Inventory(inv_item_sk=1, inv_date_sk=2, inv_quantity_on_hand=20),
    Inventory(inv_item_sk=1, inv_date_sk=3, inv_quantity_on_hand=10),
    Inventory(inv_item_sk=1, inv_date_sk=4, inv_quantity_on_hand=20),
    Inventory(inv_item_sk=2, inv_date_sk=1, inv_quantity_on_hand=50),
]
date_dim = [
    DateDim(d_date_sk=1, d_month_seq=0),
    DateDim(d_date_sk=2, d_month_seq=1),
    DateDim(d_date_sk=3, d_month_seq=2),
    DateDim(d_date_sk=4, d_month_seq=3),
]
item = [
    Item(
        i_item_sk=1,
        i_product_name="Prod1",
        i_brand="Brand1",
        i_class="Class1",
        i_category="Cat1",
    ),
    Item(
        i_item_sk=2,
        i_product_name="Prod2",
        i_brand="Brand2",
        i_class="Class2",
        i_category="Cat2",
    ),
]


def _q0():
    _src = inventory
    _rows = _query(
        _src,
        [
            {"items": date_dim, "on": lambda inv, d: inv.inv_date_sk == d.d_date_sk},
            {"items": item, "on": lambda inv, d, i: inv.inv_item_sk == i.i_item_sk},
        ],
        {
            "select": lambda inv, d, i: (inv, d, i),
            "where": lambda inv, d, i: d.d_month_seq >= 0 and d.d_month_seq <= 11,
        },
    )
    _groups = _group_by(
        _rows,
        lambda inv, d, i: Auto2(
            product_name=i.i_product_name,
            brand=i.i_brand,
            _class=i.i_class,
            category=i.i_category,
        ),
    )
    _items1 = _groups
    return [
        Auto1(
            i_product_name=g.key["product_name"],
            i_brand=g.key["brand"],
            i_class=g.key["_class"],
            i_category=g.key["category"],
            qoh=(
                sum([x[0].inv_quantity_on_hand for x in g])
                / len([x[0].inv_quantity_on_hand for x in g])
                if [x[0].inv_quantity_on_hand for x in g]
                else 0
            ),
        )
        for g in _items1
    ]


qoh = _q0()
print(json.dumps(qoh, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q22_average_inventory()
