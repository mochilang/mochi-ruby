# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:49:43Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Auto1:
    channel: str
    i_brand_id: int
    i_class_id: int
    i_category_id: int
    sales: float
    number_sales: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto2:
    channel: str
    sales: float
    number_sales: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto3:
    brand_id: int
    class_id: int
    category_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class CatalogSale:
    cs_item_sk: int
    cs_list_price: float
    cs_quantity: int
    cs_sold_date_sk: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class CrossItem:
    ss_item_sk: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class DateDim:
    d_date_sk: int
    d_year: int
    d_moy: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Item:
    i_item_sk: int
    i_brand_id: int
    i_class_id: int
    i_category_id: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class StoreSale:
    ss_item_sk: int
    ss_list_price: float
    ss_quantity: int
    ss_sold_date_sk: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class WebSale:
    ws_item_sk: int
    ws_list_price: float
    ws_quantity: int
    ws_sold_date_sk: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


class _Group(Generic[K, T]):

    def __init__(self, key: K):
        self.key = key
        self.Items: list[T] = []
        self.items = self.Items

    def __iter__(self):
        return iter(self.Items)

    def __len__(self):
        return len(self.Items)


def _group_by(src: list[T], keyfn: Callable[[T], K]) -> list[_Group[K, T]]:
    groups: dict[str, _Group[K, T]] = {}
    order: list[str] = []
    for it in src:
        if isinstance(it, (list, tuple)):
            key = keyfn(*it)
        else:
            key = keyfn(it)
        if isinstance(key, dict):
            import types

            key = types.SimpleNamespace(**key)
        ks = str(key)
        g = groups.get(ks)
        if not g:
            g = _Group(key)
            groups[ks] = g
            order.append(ks)
        g.Items.append(it)
    return [groups[k] for k in order]


def _query(src, joins, opts):
    items = [[v] for v in src]
    for j in joins:
        joined = []
        if j.get("right") and j.get("left"):
            matched = [False] * len(j["items"])
            for left in items:
                m = False
                for ri, right in enumerate(j["items"]):
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    matched[ri] = True
                    joined.append(left + [right])
                if not m:
                    joined.append(left + [None])
            for ri, right in enumerate(j["items"]):
                if not matched[ri]:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        elif j.get("right"):
            for right in j["items"]:
                m = False
                for left in items:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if not m:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        else:
            for left in items:
                m = False
                for right in j["items"]:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if j.get("left") and (not m):
                    joined.append(left + [None])
        items = joined
    if opts.get("where"):
        items = [r for r in items if opts["where"](*r)]
    if opts.get("sortKey"):

        def _key(it):
            k = opts["sortKey"](*it)
            if isinstance(k, (list, tuple, dict)):
                return str(k)
            return k

        items.sort(key=_key)
    if "skip" in opts:
        n = opts["skip"]
        if n < 0:
            n = 0
        items = items[n:] if n < len(items) else []
    if "take" in opts:
        n = opts["take"]
        if n < 0:
            n = 0
        items = items[:n] if n < len(items) else items
    res = []
    for r in items:
        res.append(opts["select"](*r))
    return res


@dataclasses.dataclass
class StoreSale:
    ss_item_sk: int
    ss_list_price: float
    ss_quantity: int
    ss_sold_date_sk: int

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class CatalogSale:
    cs_item_sk: int
    cs_list_price: float
    cs_quantity: int
    cs_sold_date_sk: int

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class WebSale:
    ws_item_sk: int
    ws_list_price: float
    ws_quantity: int
    ws_sold_date_sk: int

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Item:
    i_item_sk: int
    i_brand_id: int
    i_class_id: int
    i_category_id: int

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class DateDim:
    d_date_sk: int
    d_year: int
    d_moy: int

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q14_cross_channel():
    assert result == [
        Auto1(
            channel="store",
            i_brand_id=1,
            i_class_id=1,
            i_category_id=1,
            sales=60.0,
            number_sales=1,
        )
    ]


store_sales = [
    StoreSale(ss_item_sk=1, ss_list_price=10.0, ss_quantity=2, ss_sold_date_sk=1),
    StoreSale(ss_item_sk=1, ss_list_price=20.0, ss_quantity=3, ss_sold_date_sk=2),
]
catalog_sales = [
    CatalogSale(cs_item_sk=1, cs_list_price=10.0, cs_quantity=2, cs_sold_date_sk=1)
]
web_sales = [
    WebSale(ws_item_sk=1, ws_list_price=30.0, ws_quantity=1, ws_sold_date_sk=1)
]
item = [Item(i_item_sk=1, i_brand_id=1, i_class_id=1, i_category_id=1)]
date_dim = [
    DateDim(d_date_sk=1, d_year=2000, d_moy=12),
    DateDim(d_date_sk=2, d_year=2002, d_moy=11),
]
cross_items = [CrossItem(ss_item_sk=1)]
avg_sales = (
    sum([20.0, 20.0, 30.0]) / len([20.0, 20.0, 30.0]) if [20.0, 20.0, 30.0] else 0
)


def _q0():
    _src = store_sales
    _rows = _query(
        _src,
        [
            {
                "items": date_dim,
                "on": lambda ss, d: (
                    ss.ss_sold_date_sk == d.d_date_sk and d.d_year == 2002
                )
                and d.d_moy == 11,
            }
        ],
        {
            "select": lambda ss, d: (ss, d),
            "where": lambda ss, d: ss.ss_item_sk
            in [ci.ss_item_sk for ci in cross_items],
        },
    )
    _groups = _group_by(
        _rows, lambda ss, d: Auto3(brand_id=1, class_id=1, category_id=1)
    )
    _items1 = _groups
    return [
        Auto2(
            channel="store",
            sales=sum([x[0].ss_quantity * x[0].ss_list_price for x in g]),
            number_sales=len([_ for _ in g]),
        )
        for g in _items1
    ]


store_filtered = _q0()
result = [
    Auto1(
        channel=r.channel,
        i_brand_id=1,
        i_class_id=1,
        i_category_id=1,
        sales=r.sales,
        number_sales=r.number_sales,
    )
    for r in store_filtered
    if r.sales > avg_sales
]
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q14_cross_channel()
