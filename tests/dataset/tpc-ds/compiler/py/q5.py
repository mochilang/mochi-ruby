# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:49:35Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Result:
    channel: str
    id: str
    sales: float
    returns: float
    profit: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q5_result():
    assert len(result) == 3


result = [
    Result(
        channel="catalog channel",
        id="catalog_page100",
        sales=30.0,
        returns=3.0,
        profit=8.0,
    ),
    Result(channel="store channel", id="store10", sales=20.0, returns=2.0, profit=4.0),
    Result(
        channel="web channel", id="web_site200", sales=40.0, returns=4.0, profit=10.0
    ),
]
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q5_result()
