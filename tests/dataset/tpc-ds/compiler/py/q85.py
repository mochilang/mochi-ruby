# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:51:07Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class WebReturn:
    qty: int
    cash: float
    fee: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q85_sample():
    assert result == 85.0


web_returns = [
    WebReturn(qty=60, cash=20.0, fee=1.0),
    WebReturn(qty=100, cash=30.0, fee=2.0),
    WebReturn(qty=95, cash=25.0, fee=3.0),
]
result = (
    sum([r.qty for r in web_returns]) / len([r.qty for r in web_returns])
    if [r.qty for r in web_returns]
    else 0
)
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q85_sample()
