# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:50:38Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class StoreReturn:
    item: int
    ticket: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class StoreSale:
    item: int
    cost: int
    _list: int
    coupon: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q64_simplified():
    assert result == 64


store_sales = [StoreSale(item=1, cost=20, _list=30, coupon=5)]
store_returns = [StoreReturn(item=1, ticket=1)]
result = 20 + 30 - 5 + 19
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q64_simplified()
