# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:50:33Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class StoreSale:
    item: int
    price: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q60_simplified():
    assert result == 60


store_sales = [StoreSale(item=1, price=10), StoreSale(item=1, price=20)]
catalog_sales = [StoreSale(item=1, price=15)]
web_sales = [StoreSale(item=1, price=15)]
all_sales = store_sales + catalog_sales + web_sales
result = sum([s["price"] for s in all_sales])
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q60_simplified()
