# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:50:44Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class WebSale:
    amount: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q69_simplified():
    assert result == 69


web_sales = [WebSale(amount=34)]
store_sales = [WebSale(amount=35)]
result = sum([w.amount for w in web_sales]) + sum([s.amount for s in store_sales])
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q69_simplified()
