# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:51:08Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class WebSale:
    cat: str
    _class: str
    net: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q86_sample():
    assert result == 86.0


web_sales = [
    WebSale(cat="A", _class="B", net=40.0),
    WebSale(cat="A", _class="B", net=46.0),
    WebSale(cat="A", _class="C", net=10.0),
    WebSale(cat="B", _class="B", net=20.0),
]
result = sum([ws.net for ws in web_sales if ws.cat == "A" and ws._class == "B"])
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q86_sample()
