# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:51:05Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class SrItem:
    qty: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q83_sample():
    assert result == 83


sr_items = [SrItem(qty=10), SrItem(qty=5)]
cr_items = [SrItem(qty=25), SrItem(qty=20)]
wr_items = [SrItem(qty=10), SrItem(qty=13)]
result = (
    sum([x.qty for x in sr_items])
    + sum([x.qty for x in cr_items])
    + sum([x.qty for x in wr_items])
)
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q83_sample()
