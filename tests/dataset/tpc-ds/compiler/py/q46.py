# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:50:15Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Auto1:
    c_last_name: str
    c_first_name: str
    ca_city: str
    bought_city: str
    ss_ticket_number: int
    amt: float
    profit: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto2:
    ss_ticket_number: object
    ss_customer_sk: object
    bought_city: object
    amt: float
    profit: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto3:
    ss_ticket_number: int
    ss_customer_sk: int
    ca_city: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Customer:
    c_customer_sk: int
    c_last_name: str
    c_first_name: str
    c_current_addr_sk: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class CustomerAddres:
    ca_address_sk: int
    ca_city: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class DateDim:
    d_date_sk: int
    d_dow: int
    d_year: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class HouseholdDemographic:
    hd_demo_sk: int
    hd_dep_count: int
    hd_vehicle_count: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Store:
    s_store_sk: int
    s_city: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class StoreSale:
    ss_ticket_number: int
    ss_customer_sk: int
    ss_addr_sk: int
    ss_hdemo_sk: int
    ss_store_sk: int
    ss_sold_date_sk: int
    ss_coupon_amt: float
    ss_net_profit: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


class _Group(Generic[K, T]):

    def __init__(self, key: K):
        self.key = key
        self.Items: list[T] = []
        self.items = self.Items

    def __iter__(self):
        return iter(self.Items)

    def __len__(self):
        return len(self.Items)


def _group_by(src: list[T], keyfn: Callable[[T], K]) -> list[_Group[K, T]]:
    groups: dict[str, _Group[K, T]] = {}
    order: list[str] = []
    for it in src:
        if isinstance(it, (list, tuple)):
            key = keyfn(*it)
        else:
            key = keyfn(it)
        if isinstance(key, dict):
            import types

            key = types.SimpleNamespace(**key)
        ks = str(key)
        g = groups.get(ks)
        if not g:
            g = _Group(key)
            groups[ks] = g
            order.append(ks)
        g.Items.append(it)
    return [groups[k] for k in order]


def _query(src, joins, opts):
    items = [[v] for v in src]
    for j in joins:
        joined = []
        if j.get("right") and j.get("left"):
            matched = [False] * len(j["items"])
            for left in items:
                m = False
                for ri, right in enumerate(j["items"]):
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    matched[ri] = True
                    joined.append(left + [right])
                if not m:
                    joined.append(left + [None])
            for ri, right in enumerate(j["items"]):
                if not matched[ri]:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        elif j.get("right"):
            for right in j["items"]:
                m = False
                for left in items:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if not m:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        else:
            for left in items:
                m = False
                for right in j["items"]:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if j.get("left") and (not m):
                    joined.append(left + [None])
        items = joined
    if opts.get("where"):
        items = [r for r in items if opts["where"](*r)]
    if opts.get("sortKey"):

        def _key(it):
            k = opts["sortKey"](*it)
            if isinstance(k, (list, tuple, dict)):
                return str(k)
            return k

        items.sort(key=_key)
    if "skip" in opts:
        n = opts["skip"]
        if n < 0:
            n = 0
        items = items[n:] if n < len(items) else []
    if "take" in opts:
        n = opts["take"]
        if n < 0:
            n = 0
        items = items[:n] if n < len(items) else items
    res = []
    for r in items:
        res.append(opts["select"](*r))
    return res


def _sum(v):
    if hasattr(v, "Items"):
        v = v.Items
    if not isinstance(v, list):
        raise Exception("sum() expects list or group")
    s = 0.0
    for it in v:
        if it is None:
            continue
        if isinstance(it, (int, float)):
            s += float(it)
        else:
            raise Exception("sum() expects numbers")
    return s


def test_TPCDS_Q46_simplified():
    assert result == [
        Auto1(
            c_last_name="Doe",
            c_first_name="John",
            ca_city="Seattle",
            bought_city="Portland",
            ss_ticket_number=1,
            amt=5.0,
            profit=20.0,
        )
    ]


store_sales = [
    StoreSale(
        ss_ticket_number=1,
        ss_customer_sk=1,
        ss_addr_sk=1,
        ss_hdemo_sk=1,
        ss_store_sk=1,
        ss_sold_date_sk=1,
        ss_coupon_amt=5.0,
        ss_net_profit=20.0,
    )
]
date_dim = [DateDim(d_date_sk=1, d_dow=6, d_year=2020)]
store = [Store(s_store_sk=1, s_city="CityA")]
household_demographics = [
    HouseholdDemographic(hd_demo_sk=1, hd_dep_count=2, hd_vehicle_count=0)
]
customer_address = [
    CustomerAddres(ca_address_sk=1, ca_city="Portland"),
    CustomerAddres(ca_address_sk=2, ca_city="Seattle"),
]
customer = [
    Customer(
        c_customer_sk=1, c_last_name="Doe", c_first_name="John", c_current_addr_sk=2
    )
]
depcnt = 2
vehcnt = 0
year = 2020
cities = ["CityA"]


def _q0():
    _src = store_sales
    _rows = _query(
        _src,
        [
            {"items": date_dim, "on": lambda ss, d: ss.ss_sold_date_sk == d.d_date_sk},
            {"items": store, "on": lambda ss, d, s: ss.ss_store_sk == s.s_store_sk},
            {
                "items": household_demographics,
                "on": lambda ss, d, s, hd: ss.ss_hdemo_sk == hd.hd_demo_sk,
            },
            {
                "items": customer_address,
                "on": lambda ss, d, s, hd, ca: ss.ss_addr_sk == ca.ca_address_sk,
            },
        ],
        {
            "select": lambda ss, d, s, hd, ca: (ss, d, s, hd, ca),
            "where": lambda ss, d, s, hd, ca: (
                (
                    (hd.hd_dep_count == depcnt or hd.hd_vehicle_count == vehcnt)
                    and d.d_dow in [6, 0]
                )
                and d.d_year == year
            )
            and s.s_city in cities,
        },
    )
    _groups = _group_by(
        _rows,
        lambda ss, d, s, hd, ca: Auto3(
            ss_ticket_number=ss.ss_ticket_number,
            ss_customer_sk=ss.ss_customer_sk,
            ca_city=ca.ca_city,
        ),
    )
    _items1 = _groups
    return [
        Auto2(
            ss_ticket_number=g.key["ss_ticket_number"],
            ss_customer_sk=g.key["ss_customer_sk"],
            bought_city=g.key["ca_city"],
            amt=_sum([x[0].ss_coupon_amt for x in g]),
            profit=_sum([x[0].ss_net_profit for x in g]),
        )
        for g in _items1
    ]


dn = _q0()
base = _query(
    dn,
    [
        {
            "items": customer,
            "on": lambda dnrec, c: dnrec.ss_customer_sk == c.c_customer_sk,
        },
        {
            "items": customer_address,
            "on": lambda dnrec, c, current_addr: c.c_current_addr_sk
            == current_addr.ca_address_sk,
        },
    ],
    {
        "select": lambda dnrec, c, current_addr: Auto1(
            c_last_name=c.c_last_name,
            c_first_name=c.c_first_name,
            ca_city=current_addr.ca_city,
            bought_city=dnrec.bought_city,
            ss_ticket_number=dnrec.ss_ticket_number,
            amt=dnrec.amt,
            profit=dnrec.profit,
        ),
        "where": lambda dnrec, c, current_addr: current_addr.ca_city
        != dnrec.bought_city,
        "sortKey": lambda dnrec, c, current_addr: [
            c.c_last_name,
            c.c_first_name,
            current_addr.ca_city,
            dnrec.bought_city,
            dnrec.ss_ticket_number,
        ],
    },
)
result = base
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q46_simplified()
