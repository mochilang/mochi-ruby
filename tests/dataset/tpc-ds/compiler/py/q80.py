# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:51:02Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class StoreSale:
    price: float
    ret: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


def test_TPCDS_Q80_sample():
    assert total_profit == 80.0


store_sales = [
    StoreSale(price=20.0, ret=5.0),
    StoreSale(price=10.0, ret=2.0),
    StoreSale(price=5.0, ret=0.0),
]
catalog_sales = [StoreSale(price=15.0, ret=3.0), StoreSale(price=8.0, ret=1.0)]
web_sales = [
    StoreSale(price=25.0, ret=5.0),
    StoreSale(price=15.0, ret=8.0),
    StoreSale(price=8.0, ret=2.0),
]
total_profit = (
    sum([s.price - s.ret for s in store_sales])
    + sum([c.price - c.ret for c in catalog_sales])
    + sum([w.price - w.ret for w in web_sales])
)
print(json.dumps(total_profit, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q80_sample()
