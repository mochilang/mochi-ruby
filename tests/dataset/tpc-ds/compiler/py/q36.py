# Generated by Mochi compiler v0.10.27 on 2025-07-17T17:50:04Z
from __future__ import annotations
import dataclasses
import json


@dataclasses.dataclass
class Auto1:
    i_category: str
    i_class: str
    gross_margin: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Auto2:
    category: str
    _class: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class DateDim:
    d_date_sk: int
    d_year: int

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Item:
    i_item_sk: int
    i_category: str
    i_class: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class Store:
    s_store_sk: int
    s_state: str

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


@dataclasses.dataclass
class StoreSale:
    ss_item_sk: int
    ss_store_sk: int
    ss_sold_date_sk: int
    ss_ext_sales_price: float
    ss_net_profit: float

    def __getitem__(self, key):
        return getattr(self, key)

    def __contains__(self, key):
        return hasattr(self, key)


from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()


class _Group(Generic[K, T]):

    def __init__(self, key: K):
        self.key = key
        self.Items: list[T] = []
        self.items = self.Items

    def __iter__(self):
        return iter(self.Items)

    def __len__(self):
        return len(self.Items)


def _group_by(src: list[T], keyfn: Callable[[T], K]) -> list[_Group[K, T]]:
    groups: dict[str, _Group[K, T]] = {}
    order: list[str] = []
    for it in src:
        if isinstance(it, (list, tuple)):
            key = keyfn(*it)
        else:
            key = keyfn(it)
        if isinstance(key, dict):
            import types

            key = types.SimpleNamespace(**key)
        ks = str(key)
        g = groups.get(ks)
        if not g:
            g = _Group(key)
            groups[ks] = g
            order.append(ks)
        g.Items.append(it)
    return [groups[k] for k in order]


def _query(src, joins, opts):
    items = [[v] for v in src]
    for j in joins:
        joined = []
        if j.get("right") and j.get("left"):
            matched = [False] * len(j["items"])
            for left in items:
                m = False
                for ri, right in enumerate(j["items"]):
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    matched[ri] = True
                    joined.append(left + [right])
                if not m:
                    joined.append(left + [None])
            for ri, right in enumerate(j["items"]):
                if not matched[ri]:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        elif j.get("right"):
            for right in j["items"]:
                m = False
                for left in items:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if not m:
                    undef = [None] * (len(items[0]) if items else 0)
                    joined.append(undef + [right])
        else:
            for left in items:
                m = False
                for right in j["items"]:
                    keep = True
                    if j.get("on"):
                        keep = j["on"](*left, right)
                    if not keep:
                        continue
                    m = True
                    joined.append(left + [right])
                if j.get("left") and (not m):
                    joined.append(left + [None])
        items = joined
    if opts.get("where"):
        items = [r for r in items if opts["where"](*r)]
    if opts.get("sortKey"):

        def _key(it):
            k = opts["sortKey"](*it)
            if isinstance(k, (list, tuple, dict)):
                return str(k)
            return k

        items.sort(key=_key)
    if "skip" in opts:
        n = opts["skip"]
        if n < 0:
            n = 0
        items = items[n:] if n < len(items) else []
    if "take" in opts:
        n = opts["take"]
        if n < 0:
            n = 0
        items = items[:n] if n < len(items) else items
    res = []
    for r in items:
        res.append(opts["select"](*r))
    return res


def _sort_key(k):
    if hasattr(k, "__dataclass_fields__"):
        return str(k)
    if isinstance(k, list):
        return tuple((_sort_key(x) for x in k))
    if isinstance(k, tuple):
        return tuple((_sort_key(x) for x in k))
    if isinstance(k, dict):
        return str(k)
    return k


def test_TPCDS_Q36_simplified():
    assert result == [
        Auto1(i_category="Books", i_class="C1", gross_margin=0.2),
        Auto1(i_category="Books", i_class="C2", gross_margin=0.25),
        Auto1(i_category="Electronics", i_class="C3", gross_margin=0.2),
    ]


store_sales = [
    StoreSale(
        ss_item_sk=1,
        ss_store_sk=1,
        ss_sold_date_sk=1,
        ss_ext_sales_price=100.0,
        ss_net_profit=20.0,
    ),
    StoreSale(
        ss_item_sk=2,
        ss_store_sk=1,
        ss_sold_date_sk=1,
        ss_ext_sales_price=200.0,
        ss_net_profit=50.0,
    ),
    StoreSale(
        ss_item_sk=3,
        ss_store_sk=2,
        ss_sold_date_sk=1,
        ss_ext_sales_price=150.0,
        ss_net_profit=30.0,
    ),
]
item = [
    Item(i_item_sk=1, i_category="Books", i_class="C1"),
    Item(i_item_sk=2, i_category="Books", i_class="C2"),
    Item(i_item_sk=3, i_category="Electronics", i_class="C3"),
]
store = [Store(s_store_sk=1, s_state="A"), Store(s_store_sk=2, s_state="B")]
date_dim = [DateDim(d_date_sk=1, d_year=2000)]


def _q0():
    _src = store_sales
    _rows = _query(
        _src,
        [
            {"items": date_dim, "on": lambda ss, d: ss.ss_sold_date_sk == d.d_date_sk},
            {"items": item, "on": lambda ss, d, i: ss.ss_item_sk == i.i_item_sk},
            {"items": store, "on": lambda ss, d, i, s: ss.ss_store_sk == s.s_store_sk},
        ],
        {
            "select": lambda ss, d, i, s: (ss, d, i, s),
            "where": lambda ss, d, i, s: d.d_year == 2000
            and (s.s_state == "A" or s.s_state == "B"),
        },
    )
    _groups = _group_by(
        _rows, lambda ss, d, i, s: Auto2(category=i.i_category, _class=i.i_class)
    )
    _items1 = _groups
    _items1 = sorted(
        _items1, key=lambda g: _sort_key([g.key["category"], g.key["_class"]])
    )
    return [
        Auto1(
            i_category=g.key["category"],
            i_class=g.key["_class"],
            gross_margin=sum([x[0].ss_net_profit for x in g])
            / sum([x[0].ss_ext_sales_price for x in g]),
        )
        for g in _items1
    ]


result = _q0()
print(json.dumps(result, separators=(",", ":"), default=lambda o: vars(o)))
test_TPCDS_Q36_simplified()
