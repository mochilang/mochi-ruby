// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
import Foundation

func _json(_ v: Any) {
    func _sort(_ x: Any) -> Any {
        if let a = x as? [Any] { return a.map { _sort($0) } }
        if let m = x as? [String:Any] {
            var out: [String:Any] = [:]
            for k in m.keys.sorted() { out[k] = _sort(m[k]!) }
            return out
        }
        return x
    }
    let obj = _sort(v)
    if JSONSerialization.isValidJSONObject(obj),
       let data = try? JSONSerialization.data(withJSONObject: obj, options: [.sortedKeys]),
       let s = String(data: data, encoding: .utf8) {
        print(s)
    } else {
        print(obj)
    }
}
func expect(_ cond: Bool) {
    if !cond { fatalError("expect failed") }
}
func _structMap(_ v: Any) -> [String:Any]? {
    let mirror = Mirror(reflecting: v)
    if mirror.displayStyle == .struct || mirror.displayStyle == .class {
        var m: [String:Any] = [:]
        for child in mirror.children {
            if let k = child.label { m[k] = child.value }
        }
        return m
    }
    return nil
}
func _equal(_ a: Any, _ b: Any) -> Bool {
    if let am = _structMap(a), let bm = _structMap(b) {
        return _equal(am, bm)
    }
    if let am = _structMap(a), let bd = b as? [String: Any] {
        return _equal(am, bd)
    }
    if let ad = a as? [String: Any], let bm = _structMap(b) {
        return _equal(ad, bm)
    }
    switch (a, b) {
    case let (x as [Any], y as [Any]):
        if x.count != y.count { return false }
        for i in 0..<x.count {
            if !_equal(x[i], y[i]) { return false }
        }
        return true
    case let (x as [String: Any], y as [String: Any]):
        if x.count != y.count { return false }
        for (k, av) in x {
            guard let bv = y[k] else { return false }
            if !_equal(av, bv) { return false }
        }
        return true
    case let (ai as Double, bi as Int):
        return ai == Double(bi)
    case let (ai as Int, bi as Double):
        return Double(ai) == bi
    case let (ai as Double, bi as Double):
        return ai == bi
    case let (ai as Int, bi as Int):
        return ai == bi
    case let (sa as String, sb as String):
        return sa == sb
    case let (ab as Bool, bb as Bool):
        return ab == bb
    default:
        return false
    }
}
struct HouseholdDemographic: Equatable {
    var hd_demo_sk: Int
    var hd_dep_count: Int
}

struct TimeDim: Equatable {
    var t_hour: Int
    var t_time_sk: Int
}

struct WebPage: Equatable {
    var wp_char_count: Int
    var wp_web_page_sk: Int
}

struct WebSale: Equatable {
    var ws_sold_time_sk: Int
    var ws_ship_hdemo_sk: Int
    var ws_web_page_sk: Int
}

struct WebSale: Equatable {
    var ws_sold_time_sk: Int
    var ws_ship_hdemo_sk: Int
    var ws_web_page_sk: Int
}
var web_sales = [WebSale(ws_sold_time_sk: 1, ws_ship_hdemo_sk: 1, ws_web_page_sk: 10), WebSale(ws_sold_time_sk: 1, ws_ship_hdemo_sk: 1, ws_web_page_sk: 10), WebSale(ws_sold_time_sk: 2, ws_ship_hdemo_sk: 1, ws_web_page_sk: 10)]
var household_demographics = [HouseholdDemographic(hd_demo_sk: 1, hd_dep_count: 2)]
var time_dim = [TimeDim(t_hour: 7, t_time_sk: 1), TimeDim(t_hour: 14, t_time_sk: 2)]
var web_page = [WebPage(wp_char_count: 5100, wp_web_page_sk: 10)]
let amc = ({
	var _res: [WebSale] = []
	for ws in web_sales {
		for hd in household_demographics {
			if !(ws.ws_ship_hdemo_sk == hd.hd_demo_sk) { continue }
			for t in time_dim {
				if !(ws.ws_sold_time_sk == t.t_time_sk) { continue }
				for wp in web_page {
					if !(ws.ws_web_page_sk == wp.wp_web_page_sk) { continue }
					if !(t.t_hour >= 7 && t.t_hour <= 8 && hd.hd_dep_count == 2 && wp.wp_char_count >= 5000 && wp.wp_char_count <= 5200) { continue }
					_res.append(ws)
				}
			}
		}
	}
	return _res
}()).count
let pmc = ({
	var _res: [WebSale] = []
	for ws in web_sales {
		for hd in household_demographics {
			if !(ws.ws_ship_hdemo_sk == hd.hd_demo_sk) { continue }
			for t in time_dim {
				if !(ws.ws_sold_time_sk == t.t_time_sk) { continue }
				for wp in web_page {
					if !(ws.ws_web_page_sk == wp.wp_web_page_sk) { continue }
					if !(t.t_hour >= 14 && t.t_hour <= 15 && hd.hd_dep_count == 2 && wp.wp_char_count >= 5000 && wp.wp_char_count <= 5200) { continue }
					_res.append(ws)
				}
			}
		}
	}
	return _res
}()).count
let result = (Double(amc)) / (Double(pmc))
_json(result)
expect(_equal(result, 2.0))
