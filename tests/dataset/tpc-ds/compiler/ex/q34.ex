# Generated by Mochi Elixir compiler
defmodule Main do
  def main do
    store_sales = [%{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 1, ss_customer_sk: 1, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 1}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}, %{ss_ticket_number: 2, ss_customer_sk: 2, ss_sold_date_sk: 1, ss_store_sk: 1, ss_hdemo_sk: 2}]
    date_dim = [%{d_date_sk: 1, d_dom: 2, d_year: 2000}]
    store = [%{s_store_sk: 1, s_county: "A"}]
    household_demographics = [%{hd_demo_sk: 1, hd_buy_potential: ">10000", hd_vehicle_count: 2, hd_dep_count: 3}, %{hd_demo_sk: 2, hd_buy_potential: ">10000", hd_vehicle_count: 2, hd_dep_count: 1}]
    customer = [%{c_customer_sk: 1, c_last_name: "Smith", c_first_name: "John", c_salutation: "Mr.", c_preferred_cust_flag: "Y"}, %{c_customer_sk: 2, c_last_name: "Jones", c_first_name: "Alice", c_salutation: "Ms.", c_preferred_cust_flag: "N"}]
    dn = (fn ->
  src = store_sales
  rows = _query(src, [
    %{items: date_dim, on: fn ss, d -> (ss.ss_sold_date_sk == d.d_date_sk) end},
    %{items: store, on: fn ss, d, s -> (ss.ss_store_sk == s.s_store_sk) end},
    %{items: household_demographics, on: fn ss, d, s, hd -> (ss.ss_hdemo_sk == hd.hd_demo_sk) end}
  ], %{select: fn ss, d, s, hd -> [ss, d, s, hd] end, where: fn ss, d, s, hd -> (((((((d.d_dom >= 1) && (d.d_dom <= 3)) && (hd.hd_buy_potential == ">10000")) && (hd.hd_vehicle_count > 0)) && ((hd.hd_dep_count / hd.hd_vehicle_count) > 1.2)) && (d.d_year == 2000)) && (s.s_county == "A")) end })
  groups = _group_by(rows, fn ss, d, s, hd -> %{ticket: ss.ss_ticket_number, cust: ss.ss_customer_sk} end)
  groups = Enum.map(groups, fn g -> %{g | items: Enum.map(g.items, fn [ss, d, s, hd] -> ss end)} end)
  items = groups
  Enum.map(items, fn g -> %{ss_ticket_number: g.key.ticket, ss_customer_sk: g.key.cust, cnt: _count(g)} end)
end).()
    result = Enum.map(Enum.sort_by((for dn1 <- dn, c <- customer, ((dn1.ss_customer_sk == c.c_customer_sk)) && (((dn1.cnt >= 15) && (dn1.cnt <= 20))), do: {%{c_last_name: c.c_last_name, c_first_name: c.c_first_name, c_salutation: c.c_salutation, c_preferred_cust_flag: c.c_preferred_cust_flag, ss_ticket_number: dn1.ss_ticket_number, cnt: dn1.cnt}, c.c_last_name}), fn {_, k} -> k end), fn {v, _} -> v end)
    _json(result)
  end
  defp _count(v) do
  cond do
    is_list(v) -> length(v)
    is_map(v) and Map.has_key?(v, :items) -> length(v[:items])
    true -> raise "count() expects list or group"
  end
end

  defmodule Group do
  defstruct key: nil, items: []
end

  defp _group_by(src, keyfn) do
  {groups, order} = Enum.reduce(src, {%{}, []}, fn it, {groups, order} ->
    key = keyfn.(it)
    ks = :erlang.phash2(key)
    {groups, order} = if Map.has_key?(groups, ks) do
      {groups, order}
    else
      {Map.put(groups, ks, %Group{key: key}), order ++ [ks]}
    end
    groups = Map.update!(groups, ks, fn g -> %{g | items: g.items ++ [it]} end)
    {groups, order}
  end)
  Enum.map(order, fn k -> groups[k] end)
end

  defp _escape_json(<<>>), do: ""
defp _escape_json(<<"\\", rest::binary>>), do: "\\\\" <> _escape_json(rest)
defp _escape_json(<<"\"", rest::binary>>), do: "\\\"" <> _escape_json(rest)
defp _escape_json(<<c::binary-size(1), rest::binary>>), do: c <> _escape_json(rest)
defp _to_json(v) when is_binary(v), do: "\"" <> _escape_json(v) <> "\""
defp _to_json(v) when is_number(v), do: to_string(v)
defp _to_json(v) when is_boolean(v), do: if v, do: "true", else: "false"
defp _to_json(v) when is_list(v), do: "[" <> Enum.map_join(v, ",", &_to_json/1) <> "]"
defp _to_json(v) when is_map(v) do
  keys = Map.keys(v) |> Enum.map(&to_string/1) |> Enum.sort()
  inner = Enum.map_join(keys, ",", fn k -> _to_json(k) <> ":" <> _to_json(Map.get(v, String.to_atom(k), Map.get(v, k))) end)
  "{" <> inner <> "}"
end
defp _to_json(_), do: "null"
defp _json(v), do: IO.puts(_to_json(v))

  defp _query(src, joins, opts \\ %{}) do
  where = Map.get(opts, :where)
  items = Enum.map(src, fn v -> [v] end)
  items = (if where, do: Enum.filter(items, fn r -> where.(r) end), else: items)
  items = Enum.reduce(joins, items, fn j, items ->
    joined = cond do
      Map.get(j, :right) && Map.get(j, :left) ->
        matched = for _ <- j[:items], do: false
        {res, matched} = Enum.reduce(items, {[], matched}, fn left, {acc, matched} ->
          {acc, matched, m} = Enum.reduce(Enum.with_index(j[:items]), {acc, matched, false}, fn {right, ri}, {acc, matched, m} ->
            keep = (if Map.has_key?(j, :on) and j[:on], do: apply(j[:on], left ++ [right]), else: true)
            if keep do
              matched = List.replace_at(matched, ri, true)
              {acc ++ [left ++ [right]], matched, true}
            else
              {acc, matched, m}
            end
          end)
          acc = (if !m, do: acc ++ [left ++ [nil]], else: acc)
          {acc, matched}
        end)
        Enum.reduce(Enum.with_index(j[:items]), res, fn {right, ri}, acc ->
          if Enum.at(matched, ri) do
            acc
          else
            undef = List.duplicate(nil, (if items == [], do: 0, else: length(hd(items))))
            acc ++ [undef ++ [right]]
          end
        end)
      Map.get(j, :right) ->
        Enum.reduce(j[:items], [], fn right, acc ->
          {acc2, m} = Enum.reduce(items, {acc, false}, fn left, {acc, m} ->
            keep = (if Map.has_key?(j, :on) and j[:on], do: apply(j[:on], left ++ [right]), else: true)
            if keep, do: {acc ++ [left ++ [right]], true}, else: {acc, m}
          end)
          if !m do
            undef = List.duplicate(nil, (if items == [], do: 0, else: length(hd(items))))
            acc2 ++ [undef ++ [right]]
          else
            acc2
          end
        end)
      true ->
        Enum.reduce(items, [], fn left, acc ->
          {acc2, m} = Enum.reduce(j[:items], {acc, false}, fn right, {acc, m} ->
            keep = (if Map.has_key?(j, :on) and j[:on], do: apply(j[:on], left ++ [right]), else: true)
            if keep, do: {acc ++ [left ++ [right]], true}, else: {acc, m}
          end)
          if Map.get(j, :left) && !m do
            acc2 ++ [left ++ [nil]]
          else
            acc2
          end
        end)
    end
    joined = (if where, do: Enum.filter(joined, fn r -> where.(r) end), else: joined)
    joined
  end)
  items = (if Map.has_key?(opts, :sortKey), do: Enum.sort_by(items, fn r -> apply(opts[:sortKey], r) end), else: items)
  items = (if Map.has_key?(opts, :skip), do: (n = opts[:skip]; if n < length(items), do: Enum.drop(items, n), else: []), else: items)
  items = (if Map.has_key?(opts, :take), do: (n = opts[:take]; if n < length(items), do: Enum.take(items, n), else: items), else: items)
  Enum.map(items, fn r -> apply(opts[:select], r) end)
end

  end
Main.main()
