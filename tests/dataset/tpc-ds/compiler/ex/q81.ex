# Generated by Mochi compiler v0.10.26 on 2025-07-16T17:36:38Z
defmodule Main do
  def main do
    # catalog_returns :: list(map())
    catalog_returns = [
      %{cust: 1, state: "CA", amt: 40},
      %{cust: 2, state: "CA", amt: 50},
      %{cust: 3, state: "CA", amt: 81},
      %{cust: 4, state: "TX", amt: 30},
      %{cust: 5, state: "TX", amt: 20}
    ]

    # avg_list :: list(map())
    avg_list =
      Enum.map(_group_by(catalog_returns, fn r -> r.state end), fn g ->
        %{state: g.key, avg_amt: _avg(for x <- g.items, do: x.amt)}
      end)

    # avg_state :: any()
    avg_state = _first(for a <- avg_list, a.state == "CA", do: a)
    # result_list :: list(any())
    result_list =
      for r <- catalog_returns, r.state == "CA" && r.amt > avg_state.avg_amt * 1.2, do: r.amt

    # result :: any()
    result = _first(result_list)
    _json(result)
  end

  defp _avg(v) do
    list =
      cond do
        is_map(v) and Map.has_key?(v, :items) -> Map.get(v, :items)
        is_list(v) -> v
        true -> raise "avg() expects list or group"
      end

    if Enum.count(list) == 0 do
      0
    else
      Enum.sum(list) / Enum.count(list)
    end
  end

  defp _first(v) do
    cond do
      is_map(v) and Map.has_key?(v, :items) ->
        items = Map.get(v, :items)
        if length(items) == 0, do: nil, else: hd(items)

      is_list(v) ->
        if v == [], do: nil, else: hd(v)

      true ->
        nil
    end
  end

  defmodule Group do
    defstruct key: nil, items: []

    def fetch(g, k) do
      case k do
        :key -> {:ok, g.key}
        :items -> {:ok, g.items}
        _ -> :error
      end
    end

    def get_and_update(g, k, f) do
      case k do
        :key ->
          {v, nv} = f.(g.key)
          {v, %{g | key: nv}}

        :items ->
          {v, nv} = f.(g.items)
          {v, %{g | items: nv}}

        _ ->
          {nil, g}
      end
    end
  end

  defp _group_by(src, keyfn) do
    {groups, order} =
      Enum.reduce(src, {%{}, []}, fn it, {groups, order} ->
        key =
          if is_list(it) do
            arity = :erlang.fun_info(keyfn, :arity) |> elem(1)
            if arity == 1, do: keyfn.(it), else: apply(keyfn, it)
          else
            keyfn.(it)
          end

        ks = :erlang.phash2(key)

        {groups, order} =
          if Map.has_key?(groups, ks) do
            {groups, order}
          else
            {Map.put(groups, ks, %Group{key: key}), order ++ [ks]}
          end

        val = if is_list(it) and length(it) == 1, do: hd(it), else: it
        groups = Map.update!(groups, ks, fn g -> %{g | items: g.items ++ [val]} end)
        {groups, order}
      end)

    Enum.map(order, fn k -> groups[k] end)
  end

  defp _escape_json(<<>>), do: ""
  defp _escape_json(<<"\\", rest::binary>>), do: "\\\\" <> _escape_json(rest)
  defp _escape_json(<<"\"", rest::binary>>), do: "\\\"" <> _escape_json(rest)
  defp _escape_json(<<c::binary-size(1), rest::binary>>), do: c <> _escape_json(rest)
  defp _to_json(v) when is_binary(v), do: "\"" <> _escape_json(v) <> "\""
  defp _to_json(v) when is_number(v), do: to_string(v)
  defp _to_json(v) when is_boolean(v), do: if(v, do: "true", else: "false")
  defp _to_json(v) when is_list(v), do: "[" <> Enum.map_join(v, ",", &_to_json/1) <> "]"

  defp _to_json(v) when is_map(v) do
    keys = Map.keys(v) |> Enum.map(&to_string/1) |> Enum.sort()

    inner =
      Enum.map_join(keys, ",", fn k ->
        _to_json(k) <> ":" <> _to_json(Map.get(v, String.to_atom(k), Map.get(v, k)))
      end)

    "{" <> inner <> "}"
  end

  defp _to_json(_), do: "null"
  defp _json(v), do: IO.puts(_to_json(v))
end

Main.main()
