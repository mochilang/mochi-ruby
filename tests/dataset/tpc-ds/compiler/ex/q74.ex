# Generated by Mochi compiler v0.10.26 on 2025-07-16T17:36:19Z
defmodule Main do
  def main do
    # customer :: list(map())
    customer = [
      %{c_customer_sk: 1, c_customer_id: 1, c_first_name: "Alice", c_last_name: "Smith"}
    ]

    # date_dim :: list(map())
    date_dim = [%{d_date_sk: 1, d_year: 1998}, %{d_date_sk: 2, d_year: 1999}]
    # store_sales :: list(map())
    store_sales = [
      %{ss_customer_sk: 1, ss_sold_date_sk: 1, ss_net_paid: 100},
      %{ss_customer_sk: 1, ss_sold_date_sk: 2, ss_net_paid: 110}
    ]

    # web_sales :: list(map())
    web_sales = [
      %{ws_bill_customer_sk: 1, ws_sold_date_sk: 1, ws_net_paid: 40},
      %{ws_bill_customer_sk: 1, ws_sold_date_sk: 2, ws_net_paid: 80}
    ]

    # year_total :: list(any())
    year_total =
      _concat(
        (fn ->
           src = customer

           rows =
             _query(
               src,
               [
                 %{items: store_sales, on: fn c, ss -> c.c_customer_sk == ss.ss_customer_sk end},
                 %{items: date_dim, on: fn c, ss, d -> d.d_date_sk == ss.ss_sold_date_sk end}
               ],
               %{
                 select: fn c, ss, d ->
                   Map.merge(Map.merge(Map.merge(c, ss), d), %{c: c, ss: ss, d: d})
                 end,
                 where: fn [c, ss, d] -> d.d_year == 1998 || d.d_year == 1999 end
               }
             )

           groups =
             _group_by(rows, fn %{c: c, ss: ss, d: d} ->
               %{id: c.c_customer_id, first: c.c_first_name, last: c.c_last_name, year: d.d_year}
             end)

           items = groups

           Enum.map(items, fn g ->
             %{
               customer_id: g.key.id,
               customer_first_name: g.key.first,
               customer_last_name: g.key.last,
               year: g.key.year,
               year_total: _sum(for x <- g.items, do: x.ss.ss_net_paid),
               sale_type: "s"
             }
           end)
         end).(),
        (fn ->
           src = customer

           rows =
             _query(
               src,
               [
                 %{
                   items: web_sales,
                   on: fn c, ws -> c.c_customer_sk == ws.ws_bill_customer_sk end
                 },
                 %{items: date_dim, on: fn c, ws, d -> d.d_date_sk == ws.ws_sold_date_sk end}
               ],
               %{
                 select: fn c, ws, d ->
                   Map.merge(Map.merge(Map.merge(c, ws), d), %{c: c, ws: ws, d: d})
                 end,
                 where: fn [c, ws, d] -> d.d_year == 1998 || d.d_year == 1999 end
               }
             )

           groups =
             _group_by(rows, fn %{c: c, ws: ws, d: d} ->
               %{id: c.c_customer_id, first: c.c_first_name, last: c.c_last_name, year: d.d_year}
             end)

           items = groups

           Enum.map(items, fn g ->
             %{
               customer_id: g.key.id,
               customer_first_name: g.key.first,
               customer_last_name: g.key.last,
               year: g.key.year,
               year_total: _sum(for x <- g.items, do: x.ws.ws_net_paid),
               sale_type: "w"
             }
           end)
         end).()
      )

    # s_firstyear :: any()
    s_firstyear = _first(for y <- year_total, y.sale_type == "s" && y.year == 1998, do: y)
    # s_secyear :: any()
    s_secyear = _first(for y <- year_total, y.sale_type == "s" && y.year == 1999, do: y)
    # w_firstyear :: any()
    w_firstyear = _first(for y <- year_total, y.sale_type == "w" && y.year == 1998, do: y)
    # w_secyear :: any()
    w_secyear = _first(for y <- year_total, y.sale_type == "w" && y.year == 1999, do: y)
    # result :: list(map())
    result =
      (fn ->
         if s_firstyear.year_total > 0 && w_firstyear.year_total > 0 &&
              w_secyear.year_total / w_firstyear.year_total >
                s_secyear.year_total / s_firstyear.year_total do
           [
             %{
               customer_id: s_secyear.customer_id,
               customer_first_name: s_secyear.customer_first_name,
               customer_last_name: s_secyear.customer_last_name
             }
           ]
         else
           []
         end
       end).()

    _json(result)
  end

  defp _concat(a, b) do
    if is_list(a) and is_list(b) do
      a ++ b
    else
      raise "concat expects lists"
    end
  end

  defp _first(v) do
    cond do
      is_map(v) and Map.has_key?(v, :items) ->
        items = Map.get(v, :items)
        if length(items) == 0, do: nil, else: hd(items)

      is_list(v) ->
        if v == [], do: nil, else: hd(v)

      true ->
        nil
    end
  end

  defmodule Group do
    defstruct key: nil, items: []

    def fetch(g, k) do
      case k do
        :key -> {:ok, g.key}
        :items -> {:ok, g.items}
        _ -> :error
      end
    end

    def get_and_update(g, k, f) do
      case k do
        :key ->
          {v, nv} = f.(g.key)
          {v, %{g | key: nv}}

        :items ->
          {v, nv} = f.(g.items)
          {v, %{g | items: nv}}

        _ ->
          {nil, g}
      end
    end
  end

  defp _group_by(src, keyfn) do
    {groups, order} =
      Enum.reduce(src, {%{}, []}, fn it, {groups, order} ->
        key =
          if is_list(it) do
            arity = :erlang.fun_info(keyfn, :arity) |> elem(1)
            if arity == 1, do: keyfn.(it), else: apply(keyfn, it)
          else
            keyfn.(it)
          end

        ks = :erlang.phash2(key)

        {groups, order} =
          if Map.has_key?(groups, ks) do
            {groups, order}
          else
            {Map.put(groups, ks, %Group{key: key}), order ++ [ks]}
          end

        val = if is_list(it) and length(it) == 1, do: hd(it), else: it
        groups = Map.update!(groups, ks, fn g -> %{g | items: g.items ++ [val]} end)
        {groups, order}
      end)

    Enum.map(order, fn k -> groups[k] end)
  end

  defp _escape_json(<<>>), do: ""
  defp _escape_json(<<"\\", rest::binary>>), do: "\\\\" <> _escape_json(rest)
  defp _escape_json(<<"\"", rest::binary>>), do: "\\\"" <> _escape_json(rest)
  defp _escape_json(<<c::binary-size(1), rest::binary>>), do: c <> _escape_json(rest)
  defp _to_json(v) when is_binary(v), do: "\"" <> _escape_json(v) <> "\""
  defp _to_json(v) when is_number(v), do: to_string(v)
  defp _to_json(v) when is_boolean(v), do: if(v, do: "true", else: "false")
  defp _to_json(v) when is_list(v), do: "[" <> Enum.map_join(v, ",", &_to_json/1) <> "]"

  defp _to_json(v) when is_map(v) do
    keys = Map.keys(v) |> Enum.map(&to_string/1) |> Enum.sort()

    inner =
      Enum.map_join(keys, ",", fn k ->
        _to_json(k) <> ":" <> _to_json(Map.get(v, String.to_atom(k), Map.get(v, k)))
      end)

    "{" <> inner <> "}"
  end

  defp _to_json(_), do: "null"
  defp _json(v), do: IO.puts(_to_json(v))

  defp _query(src, joins, opts \\ %{}) do
    where = Map.get(opts, :where)
    items = Enum.map(src, fn v -> [v] end)

    items =
      Enum.reduce(joins, items, fn j, items ->
        joined =
          cond do
            Map.get(j, :right) && Map.get(j, :left) ->
              matched = for _ <- j[:items], do: false

              {res, matched} =
                Enum.reduce(items, {[], matched}, fn left, {acc, matched} ->
                  {acc, matched, m} =
                    Enum.reduce(Enum.with_index(j[:items]), {acc, matched, false}, fn {right, ri},
                                                                                      {acc,
                                                                                       matched,
                                                                                       m} ->
                      keep =
                        if Map.has_key?(j, :on) and j[:on],
                          do: apply(j[:on], left ++ [right]),
                          else: true

                      if keep do
                        matched = List.replace_at(matched, ri, true)
                        {acc ++ [left ++ [right]], matched, true}
                      else
                        {acc, matched, m}
                      end
                    end)

                  acc = if !m, do: acc ++ [left ++ [nil]], else: acc
                  {acc, matched}
                end)

              Enum.reduce(Enum.with_index(j[:items]), res, fn {right, ri}, acc ->
                if Enum.at(matched, ri) do
                  acc
                else
                  undef = List.duplicate(nil, if(items == [], do: 0, else: length(hd(items))))
                  acc ++ [undef ++ [right]]
                end
              end)

            Map.get(j, :right) ->
              Enum.reduce(j[:items], [], fn right, acc ->
                {acc2, m} =
                  Enum.reduce(items, {acc, false}, fn left, {acc, m} ->
                    keep =
                      if Map.has_key?(j, :on) and j[:on],
                        do: apply(j[:on], left ++ [right]),
                        else: true

                    if keep, do: {acc ++ [left ++ [right]], true}, else: {acc, m}
                  end)

                if !m do
                  undef = List.duplicate(nil, if(items == [], do: 0, else: length(hd(items))))
                  acc2 ++ [undef ++ [right]]
                else
                  acc2
                end
              end)

            true ->
              Enum.reduce(items, [], fn left, acc ->
                {acc2, m} =
                  Enum.reduce(j[:items], {acc, false}, fn right, {acc, m} ->
                    keep =
                      if Map.has_key?(j, :on) and j[:on],
                        do: apply(j[:on], left ++ [right]),
                        else: true

                    if keep, do: {acc ++ [left ++ [right]], true}, else: {acc, m}
                  end)

                if Map.get(j, :left) && !m do
                  acc2 ++ [left ++ [nil]]
                else
                  acc2
                end
              end)
          end

        joined
      end)

    items = if where, do: Enum.filter(items, fn r -> where.(r) end), else: items

    items =
      if Map.has_key?(opts, :sortKey),
        do: Enum.sort_by(items, fn r -> apply(opts[:sortKey], r) end),
        else: items

    items =
      if Map.has_key?(opts, :skip),
        do:
          (
            n = opts[:skip]
            if n < length(items), do: Enum.drop(items, n), else: []
          ),
        else: items

    items =
      if Map.has_key?(opts, :take),
        do:
          (
            n = opts[:take]
            if n < length(items), do: Enum.take(items, n), else: items
          ),
        else: items

    Enum.map(items, fn r -> apply(opts[:select], r) end)
  end

  defp _sum(v) do
    list =
      cond do
        is_map(v) and Map.has_key?(v, :items) -> Map.get(v, :items)
        is_list(v) -> v
        true -> raise "sum() expects list or group"
      end

    Enum.sum(list)
  end
end

Main.main()
