error[P062]: load/save path is the empty string
  --> tests/parser/errors/save_empty_path.mochi:2:1

  2 | save rows to ""
    | ^

help:
  Omit the literal entirely to use stdin/stdout (e.g. `load as T` or `save x`), or supply a real path.
