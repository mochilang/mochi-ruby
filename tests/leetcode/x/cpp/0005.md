# Longest Palindromic Substring

Use expand-around-center.

Every palindrome is centered either on one character (odd length) or between two characters (even length). For each position, expand outward while the characters match, then keep the longest substring found so far.

Update the best answer only when a strictly longer palindrome appears. That keeps ties stable and preserves the leftmost longest palindrome for cases like `babad`.
