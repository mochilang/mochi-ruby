# 0001 Two Sum

For each case, try all pairs in index order until `nums[i] + nums[j] == target`.
The problem guarantees exactly one solution, so the search can stop as soon as a
matching pair is found.
