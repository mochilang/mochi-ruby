# 0026 Remove Duplicates from Sorted Array

We use two pointers: `k` points to the position where the next unique element should be placed,
and `i` iterates through the array. When `nums[i]` is different from the last unique element
`nums[k-1]`, we copy it to `nums[k]` and increment `k`.
