Run a BFS from `beginWord` to discover the shortest distance to each reachable word. Instead of storing just one predecessor, keep a list of all parents that reach a word at the same minimum depth. Stop expanding after the first level that reaches `endWord`, then backtrack through the parent lists to build every shortest ladder. Sort the reconstructed paths so the output is deterministic.
