Flatten the BST into its inorder-sorted values, then use binary search to find the split point around `target`. Two pointers expand outward from that split, each step choosing whichever side is currently closer until `k` values have been collected.
