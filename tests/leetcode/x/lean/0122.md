Walk through the array once and add every positive increase between consecutive days. If `prices[i] > prices[i-1]`, that rise can always be taken as profit without hurting any later trade, because a longer rising segment is exactly the sum of its day-to-day gains. This greedy scan runs in `O(n)` time with `O(1)` extra space.
