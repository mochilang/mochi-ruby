Use a sliding window with character counts. First count how many times each character is needed from `t`. Expand the right end of the window across `s`, decrementing the remaining need for each character and tracking how many required characters are still missing. Whenever the window covers all required characters, try to shrink it from the left while it remains valid, updating the best window seen so far. The smallest recorded valid window is the answer.
