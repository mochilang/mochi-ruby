# 0020 Valid Parentheses

Use a stack of opening brackets. Push `(`, `[` and `{`, and whenever a closing bracket appears, require the matching opener on top of the stack. The string is valid only if every closer matches and the stack is empty at the end.
