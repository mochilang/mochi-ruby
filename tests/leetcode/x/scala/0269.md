Build a directed graph from the first differing character between each adjacent pair of words. If a longer word appears before its own prefix, the order is invalid. Then run a topological sort over all seen characters; if a cycle prevents visiting them all, return the empty string.
