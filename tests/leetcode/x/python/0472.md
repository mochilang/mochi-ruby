Sort the words by length and process them from shortest to longest. When testing one word, only previously seen shorter words are available, so a successful word-break DP means the current word can be built from at least two smaller dictionary words; after the check, insert the word into the set for later candidates. The final answer is sorted lexicographically so every language emits the same deterministic fixture output.
