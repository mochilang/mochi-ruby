Keep the values seen so far and report snapshots as sorted disjoint ranges. To build a snapshot, scan the sorted values, extend the current interval across consecutive numbers, and start a new interval whenever a gap appears; duplicate adds do not change the set.
