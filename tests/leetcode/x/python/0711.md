Collect each island as a list of coordinates relative to one starting cell. To compare shapes up to rotation and reflection, generate all eight transformed versions of those coordinates, normalize each one by shifting it so its smallest `(x, y)` becomes `(0, 0)`, sort the points, and pick the lexicographically smallest normalized form as the island’s canonical signature. The number of distinct signatures is the answer.
