Maintain one pointer into each sorted list. Put the current pointed values in a min-heap and track the maximum among them. The current heap minimum and that maximum define the smallest range covering one value from every list for this pointer state. Repeatedly advance the list that contributed the minimum, update the heap and maximum, and stop when one list runs out because no complete cover is possible after that.
