The nearest palindrome must come from a very small candidate set. Take the left half of the number, mirror it directly, and also mirror the values formed by decrementing and incrementing that half. Add the two edge cases `99...9` and `100...001`, because crossing a digit-length boundary is the other way the nearest answer can appear. Remove the original number itself, then choose the candidate with minimum absolute difference, breaking ties toward the smaller value.
