The intended constant-time structure keeps keys in buckets ordered by count: each bucket stores all keys with the same count, and hash maps point from key to its bucket. Incrementing or decrementing moves a key to the neighboring count bucket, creating or removing buckets as needed, while the head and tail buckets expose a min and max key. The test harness uses deterministic tie-breaking by returning the lexicographically smallest key among tied counts.
