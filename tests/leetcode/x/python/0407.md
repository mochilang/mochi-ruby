Start from every boundary cell, because boundary height is the first possible escape level for water. Pop the lowest reachable wall from a min-heap, visit each unvisited neighbor, and trap max(0, wall_height - neighbor_height) before pushing the neighbor back with height max(wall_height, neighbor_height). This grows the basin from the outside inward, so each cell is processed at the lowest waterline that can contain it.
