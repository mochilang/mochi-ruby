Scan the whole image, track the smallest and largest row and column containing a black pixel, then return `(bottom - top + 1) * (right - left + 1)`. The given `(x, y)` just guarantees there is at least one black pixel in the connected region.
