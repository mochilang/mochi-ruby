# Sudoku Solver

Use backtracking.

Find the next empty cell. Try digits `1` through `9`. A digit is valid when it does not already appear in the same row, column, or 3x3 box. If a choice leads to a dead end, undo it and try the next digit. Because the puzzle is guaranteed to have one solution, the recursion stops as soon as the grid is filled.
