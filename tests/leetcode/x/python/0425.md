Build a prefix map from every possible prefix to the words that have it. Backtracking keeps a partial square; at depth d, the next word must start with the column prefix formed by row[ d ] from every previous row. Trying candidates from the prefix map avoids scanning all words at each step, and reaching depth n yields one complete word square.
