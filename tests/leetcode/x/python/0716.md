Keep the actual stack in order. `push`, `pop`, and `top` are direct. `peekMax` scans for the current maximum, and `popMax` pops elements into a temporary buffer until it reaches the top-most occurrence of that maximum, removes it, and then pushes the buffered elements back. This preserves the required rule that `popMax` removes the most recently added maximum value.
