Fix a pair of top and bottom rows and collapse the matrix between them into column sums. For each collapsed array, scan prefix sums and keep previous prefixes sorted; the best subarray ending at the current prefix uses the smallest prior prefix at least `current - k`.
