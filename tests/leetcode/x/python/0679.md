Use recursive search on the multiset of current numeric values. At each step, pick two numbers, replace them with every result obtainable from `+`, `-`, `*`, and valid `/`, and recurse on the smaller list. When only one value remains, accept if it is within a tiny epsilon of `24`. Because the hand size is always four, brute-forcing all pairings and operations is small enough.
