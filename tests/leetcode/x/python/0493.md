Count reverse pairs during merge sort. After sorting the left and right halves recursively, scan them with two pointers to count how many indices satisfy `left[i] > 2 * right[j]`, then merge the halves normally. Using a divide-and-conquer sort keeps the time complexity at `O(n log n)`, and promoting values during the comparison avoids overflow on large inputs.
