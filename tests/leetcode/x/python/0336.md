Index every word by value, then try every split of every word. If the prefix is a palindrome, a reversed suffix can be placed before it; if the suffix is a palindrome, a reversed prefix can be placed after it. Sorting the collected pairs keeps the test output deterministic.
