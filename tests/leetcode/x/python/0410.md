Binary search the largest allowed subarray sum. A candidate cap is feasible when a greedy left-to-right scan can keep every running segment sum at or below the cap using at most k segments. The answer is between the maximum element and the total sum, and shrinking that range gives the minimized possible largest split sum.
