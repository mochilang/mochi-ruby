Use merge sort on indices. While merging two sorted halves, count how many right-half values move before each left-half index; that number is exactly how many smaller elements lie to its right in the original array.
