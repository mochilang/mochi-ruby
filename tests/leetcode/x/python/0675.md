Collect all trees, sort them by height, and cut them in that order. The hard part is moving between consecutive targets, which is just an unweighted shortest-path problem on the current walkable cells, so a BFS gives the minimum steps for each leg. Summing those BFS distances works because the order is fixed by the statement; if any target is unreachable, the whole answer is `-1`.
