The sequence of positive integers with every digit `9` removed is exactly the sequence of base-9 numbers, written with ordinary decimal digits. So the `n`th valid number is obtained by converting `n` to base 9 and reading that representation as a decimal integer. There is no separate digit-DP or skipping process needed.
