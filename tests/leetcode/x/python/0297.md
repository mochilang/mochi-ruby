Use a breadth-first format with `null` markers. `deserialize` rebuilds the tree level by level from the token stream, and `serialize` runs BFS, records children, then trims trailing `null`s so the output stays canonical.
