Use the monotonic-chain convex hull, but keep collinear points on the boundary. Sort the points lexicographically, build the lower and upper hulls, and only pop while the turn is strictly clockwise. That preserves points lying on straight boundary segments. Finally combine both halves, deduplicate, and sort for deterministic output.
