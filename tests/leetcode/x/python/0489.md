This repo cannot use LeetCode’s hidden `Robot` interface directly, so the test harness simulates a robot on an explicit grid and checks how many reachable cells get cleaned. The cleaning algorithm itself stays faithful to the original solution: depth-first search over relative coordinates, try all four directions from each cell, and after each successful move use a fixed “go back” sequence to restore the robot’s position and heading before exploring the next branch.
