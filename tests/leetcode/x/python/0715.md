Keep a sorted list of disjoint half-open intervals. `addRange` skips intervals completely to the left, merges every interval that overlaps or touches the new range, and leaves the rest unchanged. `queryRange` only needs to find whether some stored interval starts at or before `left` and ends at or after `right`. `removeRange` rebuilds the list, trimming or splitting any interval that intersects the removed segment.
