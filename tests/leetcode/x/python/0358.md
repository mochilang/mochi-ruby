Count each character and build the answer left to right. At each position choose the remaining character with the largest count whose last use is at least `k` positions back; if no character is available before the string is complete, no valid rearrangement exists.
