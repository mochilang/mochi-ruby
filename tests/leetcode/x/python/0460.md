An LFU cache needs the least frequency first, and least recent use as the tie-breaker inside that frequency. The optimal O(1) structure keeps keys in frequency buckets and moves a key to the next bucket after every get or successful put update. The test harness here uses a deterministic simulation with frequency and recency timestamps so the golden outputs stay fixed while still exercising the same eviction behavior.
