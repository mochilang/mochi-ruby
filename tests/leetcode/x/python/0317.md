Run BFS from each building. For every reachable empty land cell, accumulate the travel distance and how many buildings reached it. The answer is the smallest total distance among empty cells reached by all buildings; if none exist, return `-1`.
