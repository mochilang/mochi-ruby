Use DFS over every possible operand split. Track the current expression string, the running value, and the last operand contributed to that value. For multiplication, remove the last operand’s previous contribution and replace it with the multiplied result so operator precedence is handled incrementally.
