Represent an abbreviation by a bit mask of target characters that are kept. Same-length dictionary words are the only possible conflicts; for each such word, build a difference mask of positions where it differs from the target. A kept mask is unique when it intersects every difference mask. Enumerate masks, compute the abbreviation length by counting kept characters and runs of skipped characters, and emit the shortest valid abbreviation.
