Use a DFS codec that writes each node as its value followed by its child count, then recursively writes the children. Deserialization reads that stream with an index and rebuilds exactly the same N-ary structure. The tests parse LeetCode level-order notation, serialize and deserialize with the codec, then print the canonical trimmed level-order form to verify the round trip.
