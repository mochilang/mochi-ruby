Maintain `miss`, the smallest positive sum not yet representable. If the next array value is at most `miss`, it extends coverage from `[1, miss)` to `[1, miss + value)`; otherwise patching `miss` is optimal because it covers the largest possible new prefix without leaving a gap.
