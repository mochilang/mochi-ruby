For each index, keep a map from difference to the number of length-at-least-2 subsequences ending there with that difference. For every pair j < i, the pair itself starts one new length-2 chain at i, while every existing chain at j with the same difference can be extended through i and contributes to the answer. Differences are stored in wide integers so boundary values do not overflow.
