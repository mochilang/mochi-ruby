Run Dijkstra on maze stopping positions, where each edge means rolling in one direction until hitting a wall or the hole. The priority is ordered first by traveled distance and then by the path string itself, so the first time the hole is popped we already have the lexicographically smallest shortest path. Because a roll may pass over many cells, each relaxation simulates the full roll and stops early if the hole is encountered.
