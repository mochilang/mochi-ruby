Store each cell as either a plain value or a formula map from referenced cells to multiplicity. `set` overwrites the cell with a plain value, while `sum` parses single cells and ranges into that formula map. `get` evaluates lazily by recursively expanding the current dependencies, so later writes automatically flow through every formula that references them.
