Treat every cell as a node with directed edges to strictly larger four-neighbors. A depth-first search with memoization stores the best path length starting from each cell, so each edge is explored once and the answer is the maximum cached value.
