Store every occurrence in an array and keep a map from value to the indices where it appears. Insert appends an occurrence and reports whether the value was new; remove deletes one stored index by swapping the last array entry into that slot and updating both index sets. The fixture uses a deterministic `getRandom` result for repeatable tests.
