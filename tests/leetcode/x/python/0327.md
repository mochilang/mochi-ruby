Build prefix sums so every range sum is a difference of two prefixes. During merge sort, each right-half prefix counts how many sorted left-half prefixes land between `prefix - upper` and `prefix - lower`; two moving pointers keep that count linear for each merge level.
