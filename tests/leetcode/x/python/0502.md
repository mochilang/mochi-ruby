Sort projects by required capital. Sweep through that sorted list as current capital grows, pushing every newly affordable profit into a max-heap. For each of the at most `k` picks, take the best profit currently in the heap, add it to capital, and continue; if the heap is empty, no more projects can be started and we stop early.
