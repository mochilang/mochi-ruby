Potential abbreviation conflicts only matter among words with the same length and the same first and last character. Group words by that signature, sort each group lexicographically, and for each word compute how many starting characters must stay visible: it is one more than the maximum common-prefix length with its sorted neighbors. Then build the abbreviation from that prefix, the omitted count, and the last character; if that would not shorten the word, keep the original word.
