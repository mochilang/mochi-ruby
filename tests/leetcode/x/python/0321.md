Try every split of `k` digits between the two arrays. For each split, use a monotonic stack to choose the largest subsequence from each array, then greedily merge the two subsequences by comparing the remaining suffixes lexicographically.
