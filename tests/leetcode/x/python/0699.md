Process the squares from left to right. For the current square, look at every earlier square whose interval overlaps it and take the maximum landing height among them. The new square’s top becomes that base height plus its own side length. Recording each square’s absolute top height lets us update the running maximum after every drop in `O(n^2)`, which is enough for the small fixture-driven harness here.
