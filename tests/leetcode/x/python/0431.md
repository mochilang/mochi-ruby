Encode an N-ary node as a binary node with the same value. Its left pointer stores the encoded first child, and each encoded child uses its right pointer to link to the next sibling. Decoding walks the left-child/right-sibling chain to rebuild the children list. The tests parse LeetCode N-ary level-order input, encode then decode it, and print the canonical trimmed level-order form.
