Count the minimum number of unmatched `(` and `)` that must be removed, then run DFS. At each parenthesis you can either delete it, if you still owe removals of that type, or keep it if the balance stays valid. Skip duplicate removals and sort the final set for deterministic output.
