Add virtual balloons with value `1` to both ends, then use interval DP. `dp[left][right]` stores the best score for bursting everything strictly between `left` and `right`, and you try every possible balloon as the last one burst in that interval.
