Sort envelopes by width ascending and by height descending when widths tie. After that ordering, equal-width envelopes cannot be chosen together, so the answer is the length of the longest strictly increasing subsequence of heights found with binary-search tails.
