Sort each departure's arrivals in reverse lexical order, then run Hierholzer's algorithm from `JFK`. Popping the smallest remaining destination last-first gives the lexicographically smallest Eulerian itinerary, and appending airports on the way back out produces the final route in reverse.
