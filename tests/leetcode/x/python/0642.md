Keep a frequency table for all known sentences and a buffer for the current query prefix. On each character input other than `#`, scan the sentences that start with the current prefix, sort them by descending frequency and then lexicographically, and return the top three. On `#`, add the buffered sentence back into the frequency table and clear the buffer.
