# Merge K Sorted Lists

Collect all values from every sorted list, sort the combined array, and print the result as one merged sorted list.

This keeps the behavior of the problem simple in the test harness: each input case provides `k` sorted lists, we flatten all elements into one array, sort once, and emit the merged list in ascending order.
