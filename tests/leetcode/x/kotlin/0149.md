Fix one point as an anchor and count how many other points share the same normalized slope relative to it. Represent the slope as a reduced `(dy, dx)` pair divided by `gcd(|dy|, |dx|)` and normalized so equivalent directions get the same sign convention. For each anchor, the most frequent slope count plus the anchor itself is the best line through that point. Taking the maximum over all anchors gives the answer.
