Work from the bottom row upward. Keep a one-dimensional array `dp` that starts as a copy of the last row. For each cell in the row above, overwrite `dp[j]` with `triangle[i][j] + min(dp[j], dp[j+1])`. After finishing the top row, `dp[0]` is the minimum path sum.
