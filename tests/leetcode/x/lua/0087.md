Use recursion on substrings with memoization. For a pair of substrings, first reject quickly if their character frequencies differ; otherwise try every split point and recurse on both the non-swapped and swapped arrangements. Cache each `(start1, start2, length)` result so repeated subproblems are solved only once.
