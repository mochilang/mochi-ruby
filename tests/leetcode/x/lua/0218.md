Treat each building as two events: a start edge that adds its height and an end edge that removes it. Sweep from left to right while keeping the current active heights in a max-heap plus a counter for lazy deletions. Whenever the maximum active height changes, emit a new key point. Processing all start events before lower priorities at the same x-coordinate ensures consecutive equal-height segments are merged correctly.
