Count the contribution of each decimal position independently. For a place value `m`, split `n` into `high`, `cur`, and `low`; then the number of `1`s contributed at that position depends on whether `cur` is `0`, `1`, or greater than `1`.
