# Substring With Concatenation Of All Words

Use a direct check at every possible start position.

All words have the same length. Sort the input words once. For each starting index, take the next `len(words) * wordLen` characters, split them into word-sized chunks, sort those chunks, and compare them to the sorted target words. If they match, record the starting index.
