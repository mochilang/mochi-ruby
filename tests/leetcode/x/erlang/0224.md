Scan the expression once while keeping a running total, the current sign, and a stack of outer `(result, sign)` frames. When we enter parentheses we save the current frame, evaluate the inside as a fresh expression, and fold it back into the outer result when we see `)`.
