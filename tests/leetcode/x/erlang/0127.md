Run a BFS from `beginWord`, because BFS reaches words in increasing number of transformations. For each word in the current frontier, try changing every character to every lowercase letter. If the new word is in the dictionary and has not been visited before, add it to the next frontier. The first time we reach `endWord`, the current BFS depth is the shortest ladder length. If BFS finishes without reaching it, return `0`.
