# String to Integer (atoi)

Parse from left to right.

Skip leading spaces, read an optional sign, then consume consecutive digits. Keep the numeric accumulator positive while parsing and clamp before appending the next digit.

If the next digit would move the value past the 32-bit signed range, return `2147483647` or `-2147483648` immediately depending on the sign. If no digits are read, return `0`.
