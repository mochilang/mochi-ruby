Simulate the persistent reader state directly. Keep a cursor into the file contents. For each query `n`, return the next `n` characters starting at the cursor, or the remaining suffix if fewer than `n` characters are left, then advance the cursor by the number of characters actually returned. This mirrors the observable behavior of the `read4`-based API across multiple calls without relying on LeetCode’s hidden interactive harness.
