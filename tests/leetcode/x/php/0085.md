Process the matrix one row at a time while maintaining a histogram of consecutive `1` heights for each column. After updating the histogram for the current row, compute the largest rectangle that can be formed inside that histogram. The best rectangle over all rows is the maximal rectangle in the whole matrix.
