Convert the number three digits at a time. A helper handles values below 1000, and the main loop attaches the appropriate scale word (`Thousand`, `Million`, `Billion`) to each non-zero chunk before joining everything from largest chunk to smallest.
