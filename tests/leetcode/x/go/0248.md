Generate strobogrammatic numbers by length from the inside out using the valid digit pairs `00, 11, 69, 88, 96`. Skip outermost leading zeroes, and count only the generated strings that stay within the inclusive `[low, high]` bounds.
