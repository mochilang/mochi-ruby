Pack words greedily into each line. For a chosen group of words, if it is the last line or contains only one word, join them with single spaces and pad the right side with spaces until the line reaches `maxWidth`. Otherwise, compute how many total spaces must be inserted, divide them evenly across the gaps, and give the leftmost gaps one extra space when the division is uneven. Repeat until all words are consumed.
