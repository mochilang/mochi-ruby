Use backtracking row by row. Keep three occupancy structures: `cols[c]` for used columns, `diag1[r+c]` for the main diagonal, and `diag2[r-c+n-1]` for the anti-diagonal. When a spot is free in all three, place a queen, recurse to the next row, and then undo the placement. Because columns are tried in ascending order, the output order is deterministic. For these fixtures, print the number of solutions, then each board as `n` lines, with `-` between solutions and `=` between test cases.