Group employees by department, sort each department's distinct salaries from high to low, and keep only the first three unique salary values. Include every employee whose salary is in that kept set, then sort the final rows by department name, salary descending, and employee name so the output is deterministic.
