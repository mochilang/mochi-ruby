Scan the prices once from left to right. Keep the lowest price seen so far and the best profit seen so far. On each day, the only profitable sale ending on that day is `price - minPrice`, so update the answer with that value, then update `minPrice` if the current price is smaller. This gives `O(n)` time and `O(1)` extra space.
