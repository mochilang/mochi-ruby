# Reverse Nodes In K-Group

Treat the linked list as an array of values for the test harness.

Walk through the array in chunks of size `k`. Every full chunk is reversed in place. If the remaining suffix has fewer than `k` elements, leave it unchanged. The final array matches the linked-list order after reversing each full group.
