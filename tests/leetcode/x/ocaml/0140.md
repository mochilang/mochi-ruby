Use DFS with memoization on string suffixes. For each start index, try every dictionary word that matches the prefix beginning there. Recursively build all sentences for the remaining suffix, memoizing the result so each suffix is solved once. If a word reaches the end of the string, it forms a complete sentence by itself; otherwise prepend it to each sentence returned by the recursive call. Sort the final sentence list so the output stays deterministic.
