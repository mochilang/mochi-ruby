# Longest Valid Parentheses

Use a stack of indices.

Start the stack with `-1` as a base. When you see `'('`, push its index. When you see `')'`, pop once. If the stack becomes empty, push the current index as the new base. Otherwise, the current valid suffix length is `i - stack.top`, so update the best answer.
