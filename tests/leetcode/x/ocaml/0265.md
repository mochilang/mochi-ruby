For each house, you only need the smallest and second-smallest total costs from the previous row. If a color uses the previous row’s best color, add the second-best total; otherwise add the best total. That keeps each row linear instead of comparing against every other color.
