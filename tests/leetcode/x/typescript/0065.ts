import * as fs from 'fs';
function isNumber(s: string): boolean { let seenDigit=false, seenDot=false, seenExp=false, digitAfterExp=true; for(let i=0;i<s.length;i++){ const ch=s[i]; if(ch>='0'&&ch<='9'){ seenDigit=true; if(seenExp) digitAfterExp=true; } else if(ch==='+'||ch==='-'){ if(i!==0&&s[i-1]!=='e'&&s[i-1]!=='E') return false; } else if(ch==='.') { if(seenDot||seenExp) return false; seenDot=true; } else if(ch==='e'||ch==='E'){ if(seenExp||!seenDigit) return false; seenExp=true; digitAfterExp=false; } else return false; } return seenDigit&&digitAfterExp; }
const lines = fs.readFileSync(0,'utf8').split(/\r?\n/); if(lines.length>0&&lines[0].trim()!==''){ const t=Number(lines[0].trim()); const out:string[]=[]; for(let i=0;i<t;i++) out.push(isNumber(lines[i+1])?'true':'false'); process.stdout.write(out.join('\n')); }
