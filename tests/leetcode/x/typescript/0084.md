For each possible left boundary, extend the right boundary one bar at a time while maintaining the minimum height seen in that interval. The rectangle spanning `i..j` has height equal to that running minimum and width `j - i + 1`, so its area is easy to update in constant time. Track the maximum area over all intervals.
