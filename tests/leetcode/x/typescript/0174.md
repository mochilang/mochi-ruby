Work backward from the princess. Let `dp[j]` be the minimum health needed to enter column `j` in the current row and still survive to the end. Initialize the boundary so the cell just right of the princess needs `1` health, then for each cell take the cheaper of going right or down, subtract the room value, and clamp the result to at least `1`.
