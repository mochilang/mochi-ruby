# 0003 Longest Substring Without Repeating Characters

Use a sliding window. Track the last position of each character, move the left edge past any repeated character that falls inside the current window, and keep the maximum window length seen so far.
