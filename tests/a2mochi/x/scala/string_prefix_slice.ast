(program
  (let prefix (string fore))
  (let s1 (string forest))
  (call print
    (binary ==
      (call
        (selector slice (selector s1))
        (int 0)
        (selector size (selector prefix))
      )
      (selector prefix)
    )
  )
  (let s2 (string desert))
  (call print
    (binary ==
      (call
        (selector slice (selector s2))
        (int 0)
        (selector size (selector prefix))
      )
      (selector prefix)
    )
  )
)
