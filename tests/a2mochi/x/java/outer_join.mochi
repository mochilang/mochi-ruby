// Generated by a2mochi v0.10.48 on 2025-07-30 00:39:00 GMT+7
let customers = [unknown, unknown, unknown, unknown]
let orders = [unknown, unknown, unknown, unknown]
let result = unknown
print("--- Outer Join using syntax ---")
for row in result {
  if row.order {
    if row.customer {
      print("Order", row.order.id, " by ", row.customer.name, " - $ ", row.order.total)
    } else {
      print("Order", row.order.id, " by Unknown - $ ", row.order.total)
    }
  } else {
    print("Customer", row.customer.name, " has no orders")
  }
}
