let customers = [alist_to_hash_table([cons("id", 1), cons("name", "Alice")]), alist_to_hash_table([cons("id", 2), cons("name", "Bob")])]
let orders = [alist_to_hash_table([cons("id", 100), cons("customerId", 1), cons("total", 250)]), alist_to_hash_table([cons("id", 101), cons("customerId", 3), cons("total", 80)])]
let result = res38
print("--- Left Join ---")
