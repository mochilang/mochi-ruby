let items = [alist_to_hash_table([cons("n", 1), cons("v", "a")]), alist_to_hash_table([cons("n", 1), cons("v", "b")]), alist_to_hash_table([cons("n", 2), cons("v", "c")])]
let result = res45
print(result)
