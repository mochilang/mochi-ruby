// Generated by Mochi v0.10.48 on 2025-07-30 00:52:02 GMT+7
/*
<?php
$nums = [1, 2];
$letters = ["A", "B"];
$bools = [true, false];
$combos = [];
foreach ($nums as $n) {
  foreach ($letters as $l) {
    foreach ($bools as $b) {
      $combos[] = ["n" => $n, "l" => $l, "b" => $b];
    }
  }
}

echo "--- Cross Join of three lists ---", PHP_EOL;
foreach ($combos as $c) {
  echo (is_float($c["n"]) ? json_encode($c["n"], 1344) : $c["n"]) . " " . (is_float($c["l"]) ? json_encode($c["l"], 1344) : $c["l"]) . " " . (is_float($c["b"]) ? json_encode($c["b"], 1344) : $c["b"]), PHP_EOL;
}
*/
let nums = [1, 2]
let letters = ["A", "B"]
var combos = []
for n in nums {
  for l in letters {
    for b in bools {
      combos = append(combos, {"n": n, "l": l, "b": b})
    }
  }
}
print("--- Cross Join of three lists ---")
for c in combos {
  print((c as map<string, any>)["n"], (c as map<string, any>)["l"], (c as map<string, any>)["b"])
}
