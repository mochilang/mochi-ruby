// Generated by Mochi v0.10.48 on 2025-07-30 00:52:02 GMT+7
/*
<?php
$nums = [1, 2, 3];
$letters = ["A", "B"];
$pairs = [];
foreach ($nums as $n) {
  foreach ($letters as $l) {
    if ($n % 2 == 0) {
      $pairs[] = ["n" => $n, "l" => $l];
    }
  }
}

echo "--- Even pairs ---", PHP_EOL;
foreach ($pairs as $p) {
  echo (is_float($p["n"]) ? json_encode($p["n"], 1344) : $p["n"]) . " " . (is_float($p["l"]) ? json_encode($p["l"], 1344) : $p["l"]), PHP_EOL;
}
*/
let nums = [1, 2, 3]
let letters = ["A", "B"]
var pairs = []
for n in nums {
  for l in letters {
    if (((n % 2)) == 0) {
      pairs = append(pairs, {"n": n, "l": l})
    }
  }
}
print("--- Even pairs ---")
for p in pairs {
  print((p as map<string, any>)["n"], (p as map<string, any>)["l"])
}
