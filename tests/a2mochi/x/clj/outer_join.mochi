// Generated by Mochi v0.10.48 on 2025-07-30 00:03:56 GMT+7
[defrecord, Orders, [id, customerId, total]]
[defrecord, Customers, [id, name]]
let customers
let orders
let result = [concat, [for, [o, orders, let, [c, [some, [fn, [c], when(([customerId, o] == [id, c]), c)], customers]]], {customer: c, order: o}], [for, [c, customers, when, [not_any_p, [fn, [o], ([customerId, o] == [id, c])], orders], let, [o, _]], {customer: c, order: o}]]
fun main() {
  print( Outer Join using syntax ___)
  for row in result {
    if [order, row] {
      [customer, row]
      print(Order, [id, [order, row]], by, [name, [customer, row]],  $, [total, [order, row]])
      print(Order, [id, [order, row]], by, Unknown,  $, [total, [order, row]])
    } else     print(Customer, [name, [customer, row]], has no orders)
  }
}
main()
