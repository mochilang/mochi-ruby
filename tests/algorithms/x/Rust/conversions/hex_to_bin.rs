// Generated by Mochi transpiler v0.10.59
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn panic(msg: &str) {
    println!("{}", msg);
};
    fn trim_spaces(mut s: String) -> String {
    let mut start: i64 = 0;
    let mut end = (s.len() as i64);
    while ((start < end) && ({ let tmp = &s; tmp.chars().skip(start as usize).take(((start + 1) - start) as usize).collect::<String>() }.as_str() == " ")) {
        start = (start + 1);
    }
    while ((end > start) && ({ let tmp = &s; tmp.chars().skip((end - 1) as usize).take((end - (end - 1)) as usize).collect::<String>() }.as_str() == " ")) {
        end = (end - 1);
    }
    return { let tmp = &s; tmp.chars().skip(start as usize).take((end - start) as usize).collect::<String>() }.clone()
};
    fn hex_digit_value(ch: &str) -> i64 {
    if (ch == "0") {
        return 0
    }
    if (ch == "1") {
        return 1
    }
    if (ch == "2") {
        return 2
    }
    if (ch == "3") {
        return 3
    }
    if (ch == "4") {
        return 4
    }
    if (ch == "5") {
        return 5
    }
    if (ch == "6") {
        return 6
    }
    if (ch == "7") {
        return 7
    }
    if (ch == "8") {
        return 8
    }
    if (ch == "9") {
        return 9
    }
    if ((ch == "a") || (ch == "A")) {
        return 10
    }
    if ((ch == "b") || (ch == "B")) {
        return 11
    }
    if ((ch == "c") || (ch == "C")) {
        return 12
    }
    if ((ch == "d") || (ch == "D")) {
        return 13
    }
    if ((ch == "e") || (ch == "E")) {
        return 14
    }
    if ((ch == "f") || (ch == "F")) {
        return 15
    }
    panic!("{}", &"Invalid value was passed to the function");
};
    fn hex_to_bin(hex_num: &str) -> i64 {
    let mut trimmed: String = trim_spaces(hex_num.to_string()).clone();
    if ((trimmed.len() as i64) == 0) {
        panic!("{}", &"No value was passed to the function");
    }
    let mut s: String = trimmed.clone();
    let mut is_negative: bool = false;
    if ({ let tmp = &s; tmp.chars().skip(0 as usize).take((1 - 0) as usize).collect::<String>() }.as_str() == "-") {
        is_negative = true;
        s = { let tmp = &s; tmp.chars().skip(1 as usize).take(((s.len() as i64) - 1) as usize).collect::<String>() };
    }
    let mut int_num: i64 = 0;
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        let mut ch: String = { let tmp = &s; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.clone();
        let mut val: i64 = hex_digit_value(&ch);
        int_num = ((int_num * 16) + val);
        i = (i + 1);
    }
    let mut bin_str: String = String::from("").clone();
    let mut n: i64 = int_num;
    if (n == 0) {
        bin_str = String::from("0");
    }
    while (n > 0) {
        bin_str = format!("{}{}", (n % 2).to_string(), bin_str).to_string();
        n = (n / 2);
    }
    let mut result = { let n: i64 = bin_str.parse().unwrap(); n };
    if is_negative {
        result = -result;
    }
    return result
};
    println!("{}", hex_to_bin(&"AC").to_string());
    println!("{}", hex_to_bin(&"9A4").to_string());
    println!("{}", hex_to_bin(&"   12f   ").to_string());
    println!("{}", hex_to_bin(&"FfFf").to_string());
    println!("{}", hex_to_bin(&"-fFfF").to_string());
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
