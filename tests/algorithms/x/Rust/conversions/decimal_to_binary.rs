// Generated by Mochi transpiler v0.10.59
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn decimal_to_binary_iterative(mut num: i64) -> String {
    if (num == 0) {
        return String::from("0b0")
    }
    let mut negative: bool = false;
    let mut n: i64 = num;
    if (n < 0) {
        negative = true;
        n = -n;
    }
    let mut result: String = String::from("").clone();
    while (n > 0) {
        result = format!("{}{}", (n % 2).to_string(), result).to_string();
        n = (n / 2);
    }
    if negative {
        return format!("{}{}", "-0b", result)
    }
    return format!("{}{}", "0b", result).clone()
};
    fn decimal_to_binary_recursive_helper(mut n: i64) -> String {
    if (n == 0) {
        return String::from("0")
    }
    if (n == 1) {
        return String::from("1")
    }
    let mut div: i64 = (n / 2);
    let mut r#mod: i64 = (n % 2);
    return format!("{}{}", decimal_to_binary_recursive_helper(div), r#mod.to_string()).clone()
};
    fn decimal_to_binary_recursive(mut num: i64) -> String {
    if (num == 0) {
        return String::from("0b0")
    }
    if (num < 0) {
        return format!("{}{}", "-0b", decimal_to_binary_recursive_helper(-num))
    }
    return format!("{}{}", "0b", decimal_to_binary_recursive_helper(num)).clone()
};
    println!("{}", decimal_to_binary_iterative(0));
    println!("{}", decimal_to_binary_iterative(2));
    println!("{}", decimal_to_binary_iterative(7));
    println!("{}", decimal_to_binary_iterative(35));
    println!("{}", decimal_to_binary_iterative(-2));
    println!("{}", decimal_to_binary_recursive(0));
    println!("{}", decimal_to_binary_recursive(40));
    println!("{}", decimal_to_binary_recursive(-40));
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
