// Generated by Mochi transpiler v0.10.63
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn list_min(mut xs: Vec<i64>) -> i64 {
    let mut i: i64 = 1;
    let mut m: i64 = xs[0 as usize];
    while (i < (xs.len() as i64)) {
        if (xs[i as usize] < m) {
            m = xs[i as usize];
        }
        i = (i + 1);
    }
    return m
};
    fn list_max(mut xs: Vec<i64>) -> i64 {
    let mut i: i64 = 1;
    let mut m: i64 = xs[0 as usize];
    while (i < (xs.len() as i64)) {
        if (xs[i as usize] > m) {
            m = xs[i as usize];
        }
        i = (i + 1);
    }
    return m
};
    fn remove_once(mut xs: Vec<i64>, mut value: i64) -> Vec<i64> {
    let mut res: Vec<i64> = vec![];
    let mut removed: bool = false;
    let mut i: i64 = 0;
    while (i < (xs.len() as i64)) {
        if (!removed && (xs[i as usize] == value)) {
            removed = true;
        } else {
            res = { let mut _v = res.clone(); _v.push(xs[i as usize]); _v };
        }
        i = (i + 1);
    }
    return res
};
    fn reverse_list(mut xs: Vec<i64>) -> Vec<i64> {
    let mut res: Vec<i64> = vec![];
    let mut i = ((xs.len() as i64) - 1);
    while (i >= 0) {
        res = { let mut _v = res.clone(); _v.push(xs[i as usize]); _v };
        i = (i - 1);
    }
    return res
};
    fn merge_sort(mut collection: Vec<i64>) -> Vec<i64> {
    let mut start: Vec<i64> = vec![];
    let mut end: Vec<i64> = vec![];
    let mut coll: Vec<i64> = collection.clone();
    while ((coll.len() as i64) > 1) {
        let mut mn: i64 = list_min(coll.clone());
        let mut mx: i64 = list_max(coll.clone());
        start = { let mut _v = start.clone(); _v.push(mn); _v };
        end = { let mut _v = end.clone(); _v.push(mx); _v };
        coll = remove_once(coll.clone(), mn);
        coll = remove_once(coll.clone(), mx);
    }
    end = reverse_list(end.clone());
    return vec![vec![start.clone(), coll.clone()].concat().clone(), end.clone()].concat()
};
    fn test_merge_sort() {
    if (merge_sort(vec![0, 5, 3, 2, 2]) != vec![0, 2, 2, 3, 5]) {
        panic!("case1 failed");
    }
    if (merge_sort(vec![]) != vec![]) {
        panic!("case2 failed");
    }
    if (merge_sort(vec![-2, -5, -45]) != vec![-45, -5, -2]) {
        panic!("case3 failed");
    }
};
    fn mochi_main() {
    test_merge_sort();
    println!("{}", format!("{:?}", merge_sort(vec![0, 5, 3, 2, 2])));
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
