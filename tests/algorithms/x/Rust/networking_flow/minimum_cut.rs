// Generated by Mochi transpiler v0.10.68
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn bfs(mut graph: Vec<Vec<i64>>, mut s: i64, mut t: i64, parent: &mut Vec<i64>) -> bool {
    let mut visited: Vec<bool> = vec![];
    let mut i: i64 = 0;
    while (i < (graph.len() as i64)) {
        visited = { let mut _v = visited.clone(); _v.push(false); _v };
        i = (i + 1);
    }
    let mut queue: Vec<i64> = vec![s];
    let mut head: i64 = 0;
    visited[s as usize] = true;
    while (head < (queue.len() as i64)) {
        let u: i64 = queue[head as usize];
        head = (head + 1);
        let mut ind: i64 = 0;
        while (ind < (graph[u as usize].clone().len() as i64)) {
            if ((visited[ind as usize] == false) && (graph[u as usize].clone()[ind as usize] > 0)) {
                queue = { let mut _v = queue.clone(); _v.push(ind); _v };
                visited[ind as usize] = true;
                (*parent)[ind as usize] = u;
            }
            ind = (ind + 1);
        }
    }
    return visited[t as usize]
};
    let mut mincut = |mut graph: Vec<Vec<i64>>, mut source: i64, mut sink: i64| -> Vec<Vec<i64>> {
    let mut g: Vec<Vec<i64>> = graph.clone();
    let mut parent: Vec<i64> = vec![];
    let mut i: i64 = 0;
    while (i < (g.len() as i64)) {
        parent = { let mut _v = parent.clone(); _v.push(-1); _v };
        i = (i + 1);
    }
    let mut temp: Vec<Vec<i64>> = vec![];
    i = 0;
    while (i < (g.len() as i64)) {
        let mut row: Vec<i64> = vec![];
        let mut j: i64 = 0;
        while (j < (g[i as usize].clone().len() as i64)) {
            row = { let mut _v = row.clone(); _v.push(g[i as usize].clone()[j as usize]); _v };
            j = (j + 1);
        }
        temp = { let mut _v = temp.clone(); _v.push(row.clone()); _v };
        i = (i + 1);
    }
    while bfs(g.clone(), source, sink, &mut parent) {
        let mut path_flow: i64 = 1000000000;
        let mut s: i64 = sink;
        while (s != source) {
            let p: i64 = parent[s as usize];
            let cap: i64 = g[p as usize].clone()[s as usize];
            if (cap < path_flow) {
                path_flow = cap;
            }
            s = p;
        }
        let mut v: i64 = sink;
        while (v != source) {
            let u: i64 = parent[v as usize];
            g[u as usize][v as usize] = (g[u as usize].clone()[v as usize] - path_flow);
            g[v as usize][u as usize] = (g[v as usize].clone()[u as usize] + path_flow);
            v = u;
        }
    }
    let mut res: Vec<Vec<i64>> = vec![];
    i = 0;
    while (i < (g.len() as i64)) {
        let mut j: i64 = 0;
        while (j < (g[0 as usize].clone().len() as i64)) {
            if ((g[i as usize].clone()[j as usize] == 0) && (temp[i as usize].clone()[j as usize] > 0)) {
                res = { let mut _v = res.clone(); _v.push(vec![i, j]); _v };
            }
            j = (j + 1);
        }
        i = (i + 1);
    }
    return res
};
    let test_graph: Vec<Vec<i64>> = vec![vec![0, 16, 13, 0, 0, 0].clone(), vec![0, 0, 10, 12, 0, 0].clone(), vec![0, 4, 0, 0, 14, 0].clone(), vec![0, 0, 9, 0, 0, 20].clone(), vec![0, 0, 0, 7, 0, 4].clone(), vec![0, 0, 0, 0, 0, 0].clone()];
    let result: Vec<Vec<i64>> = mincut(test_graph.clone(), 0, 5);
    println!("{}", format!("{:?}", result));
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
