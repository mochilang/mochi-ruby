// Generated by Mochi transpiler v0.10.64
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn sum_of_harmonic_progression(mut first_term: f64, mut common_difference: f64, mut number_of_terms: i64) -> f64 {
    let mut arithmetic_progression: Vec<f64> = vec![(1.0 / first_term)];
    let mut term: f64 = (1.0 / first_term);
    let mut i: i64 = 0;
    while (i < (number_of_terms - 1)) {
        term = (term + common_difference);
        arithmetic_progression = { let mut _v = arithmetic_progression.clone(); _v.push(term); _v };
        i = (i + 1);
    }
    let mut total: f64 = 0.0;
    let mut j: i64 = 0;
    while (j < (arithmetic_progression.len() as i64)) {
        total = (total + (1.0 / arithmetic_progression[j as usize]));
        j = (j + 1);
    }
    return total
};
    fn abs_val(mut num: f64) -> f64 {
    if (num < 0.0) {
        return -num
    }
    return num
};
    fn test_sum_of_harmonic_progression() {
    let result1: f64 = sum_of_harmonic_progression(0.5, 2.0, 2);
    if (abs_val((result1 - 0.75)) > 0.0000001) {
        panic!("test1 failed");
    }
    let result2: f64 = sum_of_harmonic_progression(0.2, 5.0, 5);
    if (abs_val((result2 - 0.45666666666666667)) > 0.0000001) {
        panic!("test2 failed");
    }
};
    fn mochi_main() {
    test_sum_of_harmonic_progression();
    println!("{}", format!("{:?}", sum_of_harmonic_progression(0.5, 2.0, 2)));
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
