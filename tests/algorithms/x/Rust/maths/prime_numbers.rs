// Generated by Mochi transpiler v0.10.67
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn slow_primes(mut max_n: i64) -> Vec<i64> {
    let mut result: Vec<i64> = vec![];
    let mut i: i64 = 2;
    while (i <= max_n) {
        let mut j: i64 = 2;
        let mut is_prime: bool = true;
        while (j < i) {
            if ((i % j) == 0) {
                is_prime = false;
                break
            }
            j = (j + 1);
        }
        if is_prime {
            result = { let mut _v = result.clone(); _v.push(i); _v };
        }
        i = (i + 1);
    }
    return result
};
    fn primes(mut max_n: i64) -> Vec<i64> {
    let mut result: Vec<i64> = vec![];
    let mut i: i64 = 2;
    while (i <= max_n) {
        let mut j: i64 = 2;
        let mut is_prime: bool = true;
        while ((j * j) <= i) {
            if ((i % j) == 0) {
                is_prime = false;
                break
            }
            j = (j + 1);
        }
        if is_prime {
            result = { let mut _v = result.clone(); _v.push(i); _v };
        }
        i = (i + 1);
    }
    return result
};
    fn fast_primes(mut max_n: i64) -> Vec<i64> {
    let mut result: Vec<i64> = vec![];
    if (max_n >= 2) {
        result = { let mut _v = result.clone(); _v.push(2); _v };
    }
    let mut i: i64 = 3;
    while (i <= max_n) {
        let mut j: i64 = 3;
        let mut is_prime: bool = true;
        while ((j * j) <= i) {
            if ((i % j) == 0) {
                is_prime = false;
                break
            }
            j = (j + 2);
        }
        if is_prime {
            result = { let mut _v = result.clone(); _v.push(i); _v };
        }
        i = (i + 2);
    }
    return result
};
    println!("{}", format!("{:?}", slow_primes(25)));
    println!("{}", format!("{:?}", primes(25)));
    println!("{}", format!("{:?}", fast_primes(25)));
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
