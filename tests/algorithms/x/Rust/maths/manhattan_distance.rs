// Generated by Mochi transpiler v0.10.63
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn abs_val(mut x: f64) -> f64 {
    if (x < 0.0) {
        return -x
    }
    return x
};
    fn validate_point(mut p: Vec<f64>) {
    if ((p.len() as i64) == 0) {
        panic!("Missing an input");
    }
};
    fn manhattan_distance(mut a: Vec<f64>, mut b: Vec<f64>) -> f64 {
    validate_point(a.clone());
    validate_point(b.clone());
    if ((a.len() as i64) != (b.len() as i64)) {
        panic!("Both points must be in the same n-dimensional space");
    }
    let mut total: f64 = 0.0;
    let mut i: i64 = 0;
    while (i < (a.len() as i64)) {
        total = (total + abs_val((a[i as usize] - b[i as usize])));
        i = (i + 1);
    }
    return total
};
    fn manhattan_distance_one_liner(mut a: Vec<f64>, mut b: Vec<f64>) -> f64 {
    return manhattan_distance(a.clone(), b.clone())
};
    println!("{}", manhattan_distance(vec![1.0, 1.0], vec![2.0, 2.0]).to_string());
    println!("{}", manhattan_distance(vec![1.5, 1.5], vec![2.0, 2.0]).to_string());
    println!("{}", manhattan_distance_one_liner(vec![1.5, 1.5], vec![2.5, 2.0]).to_string());
    println!("{}", manhattan_distance_one_liner(vec![-3.0, -3.0, -3.0], vec![0.0, 0.0, 0.0]).to_string());
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
