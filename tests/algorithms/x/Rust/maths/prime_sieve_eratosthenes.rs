// Generated by Mochi transpiler v0.10.67
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn prime_sieve_eratosthenes(mut num: i64) -> Vec<i64> {
    if (num <= 0) {
        panic!("Input must be a positive integer");
    }
    let mut primes: Vec<bool> = vec![];
    let mut i: i64 = 0;
    while (i <= num) {
        primes = { let mut _v = primes.clone(); _v.push(true); _v };
        i = (i + 1);
    }
    let mut p: i64 = 2;
    while ((p * p) <= num) {
        if primes[p as usize] {
            let mut j: i64 = (p * p);
            while (j <= num) {
                primes[j as usize] = false;
                j = (j + p);
            }
        }
        p = (p + 1);
    }
    let mut result: Vec<i64> = vec![];
    let mut k: i64 = 2;
    while (k <= num) {
        if primes[k as usize] {
            result = { let mut _v = result.clone(); _v.push(k); _v };
        }
        k = (k + 1);
    }
    return result
};
    fn list_eq(mut a: Vec<i64>, mut b: Vec<i64>) -> bool {
    if ((a.len() as i64) != (b.len() as i64)) {
        return false
    }
    let mut i: i64 = 0;
    while (i < (a.len() as i64)) {
        if (a[i as usize] != b[i as usize]) {
            return false
        }
        i = (i + 1);
    }
    return true
};
    let mut test_prime_sieve_eratosthenes = || {
    if !list_eq(prime_sieve_eratosthenes(10), vec![2, 3, 5, 7]) {
        panic!("test 10 failed");
    }
    if !list_eq(prime_sieve_eratosthenes(20), vec![2, 3, 5, 7, 11, 13, 17, 19]) {
        panic!("test 20 failed");
    }
    if !list_eq(prime_sieve_eratosthenes(2), vec![2]) {
        panic!("test 2 failed");
    }
    if ((prime_sieve_eratosthenes(1).len() as i64) != 0) {
        panic!("test 1 failed");
    }
};
    let mut mochi_main = || {
    test_prime_sieve_eratosthenes();
    println!("{}", format!("{:?}", prime_sieve_eratosthenes(20)));
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
