// Generated by Mochi transpiler v0.10.63
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn max_sum_sliding_window(mut arr: Vec<i64>, mut k: i64) -> i64 {
    if ((k < 0) || ((arr.len() as i64) < k)) {
        panic!("Invalid Input");
    }
    let mut idx: i64 = 0;
    let mut current_sum: i64 = 0;
    while (idx < k) {
        current_sum = (current_sum + arr[idx as usize]);
        idx = (idx + 1);
    }
    let mut max_sum: i64 = current_sum;
    let mut i: i64 = 0;
    while (i < ((arr.len() as i64) - k)) {
        current_sum = ((current_sum - arr[i as usize]) + arr[(i + k) as usize]);
        if (current_sum > max_sum) {
            max_sum = current_sum;
        }
        i = (i + 1);
    }
    return max_sum
};
    fn test_max_sum_sliding_window() {
    let arr1: Vec<i64> = vec![1, 4, 2, 10, 2, 3, 1, 0, 20];
    if (max_sum_sliding_window(arr1.clone(), 4) != 24) {
        panic!("test1 failed");
    }
    let arr2: Vec<i64> = vec![1, 4, 2, 10, 2, 13, 1, 0, 2];
    if (max_sum_sliding_window(arr2.clone(), 4) != 27) {
        panic!("test2 failed");
    }
};
    fn mochi_main() {
    test_max_sum_sliding_window();
    let sample: Vec<i64> = vec![1, 4, 2, 10, 2, 3, 1, 0, 20];
    println!("{}", max_sum_sliding_window(sample.clone(), 4).to_string());
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
