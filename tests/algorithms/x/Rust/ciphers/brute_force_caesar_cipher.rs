// Generated by Mochi transpiler v0.10.59
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
static mut g_LETTERS: String = String::new();
fn main() {
    unsafe {
        g_LETTERS = String::from("ABCDEFGHIJKLMNOPQRSTUVWXYZ").clone();
                let _start: i64 = _now();
        unsafe fn index_of(mut s: String, ch: &str) -> i64 {
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        if ({ let tmp = &s; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() } == ch) {
            return i
        }
        i = (i + 1);
    }
    return (0 - 1)
};
        unsafe fn decrypt(message: &str) {
    for key in 0..(g_LETTERS.len() as i64) {
        let mut translated: String = String::from("").clone();
        for i in 0..(message.len() as i64) {
            let mut symbol: String = { let tmp = &message; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.clone();
            let mut idx: i64 = index_of(g_LETTERS.clone(), &symbol);
            if (idx != (0 - 1)) {
                let mut num: i64 = (idx - key);
                if (num < 0) {
                    num = (num + (g_LETTERS.len() as i64));
                }
                translated = format!("{}{}", translated, { let tmp = &g_LETTERS; tmp.chars().skip(num as usize).take(((num + 1) - num) as usize).collect::<String>() });
            } else {
                translated = format!("{}{}", translated, symbol);
            }
        }
        println!("{}", format!("{}{}", format!("{}{}", format!("{}{}", "Decryption using Key #", key.to_string()), ": "), translated));
    }
};
        decrypt(&"TMDETUX PMDVU");
        let _end: i64 = _now();
        let duration_us: i64 = ((_end - _start) / 1000);
        let memory_bytes: i64 = _mem();
        println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

    }
}
