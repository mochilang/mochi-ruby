// Generated by Mochi transpiler v0.10.73
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();;
    fn focal_length_of_lens(mut object_distance_from_lens: f64, mut image_distance_from_lens: f64) -> f64 {
    if ((object_distance_from_lens == 0.0) || (image_distance_from_lens == 0.0)) {
        panic!("Invalid inputs. Enter non zero values with respect to the sign convention.");;
    }
    return (1.0 / ((1.0 / image_distance_from_lens) - (1.0 / object_distance_from_lens)))
};
    fn object_distance(mut focal_length_of_lens: f64, mut image_distance_from_lens: f64) -> f64 {
    if ((image_distance_from_lens == 0.0) || (focal_length_of_lens == 0.0)) {
        panic!("Invalid inputs. Enter non zero values with respect to the sign convention.");;
    }
    return (1.0 / ((1.0 / image_distance_from_lens) - (1.0 / focal_length_of_lens)))
};
    fn image_distance(mut focal_length_of_lens: f64, mut object_distance_from_lens: f64) -> f64 {
    if ((object_distance_from_lens == 0.0) || (focal_length_of_lens == 0.0)) {
        panic!("Invalid inputs. Enter non zero values with respect to the sign convention.");;
    }
    return (1.0 / ((1.0 / object_distance_from_lens) + (1.0 / focal_length_of_lens)))
};
    println!("{}", focal_length_of_lens(10.0, 4.0).to_string());;
    println!("{}", focal_length_of_lens(2.7, 5.8).to_string());;
    println!("{}", object_distance(10.0, 40.0).to_string());;
    println!("{}", object_distance(6.2, 1.5).to_string());;
    println!("{}", image_distance(50.0, 40.0).to_string());;
    println!("{}", image_distance(5.3, 7.9).to_string());;
    let _end: i64 = _now();;
    let duration_us: i64 = ((_end - _start) / 1000);;
    let memory_bytes: i64 = _mem();;
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
