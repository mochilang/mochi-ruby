// Generated by Mochi transpiler v0.10.59
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn int_to_binary(mut n: i64) -> String {
    if (n == 0) {
        return String::from("0")
    }
    let mut res: String = String::from("").clone();
    let mut num: i64 = n;
    while (num > 0) {
        res = format!("{}{}", (num % 2).to_string(), res).to_string();
        num = (num / 2);
    }
    return res.clone()
};
    fn pad_left(s: &str, mut width: i64) -> String {
    let mut res: String = s.to_string().clone();
    while ((res.len() as i64) < width) {
        res = format!("{}{}", "0", res);
    }
    return res.clone()
};
    fn binary_xor(mut a: i64, mut b: i64) -> String {
    if ((a < 0) || (b < 0)) {
        panic!("the value of both inputs must be positive");
    }
    let mut a_bin: String = int_to_binary(a).clone();
    let mut b_bin: String = int_to_binary(b).clone();
    let mut max_len = if ((a_bin.len() as i64) > (b_bin.len() as i64)) { (a_bin.len() as i64) } else { (b_bin.len() as i64) };
    let mut a_pad: String = pad_left(&a_bin, max_len).clone();
    let mut b_pad: String = pad_left(&b_bin, max_len).clone();
    let mut i: i64 = 0;
    let mut result: String = String::from("").clone();
    while (i < max_len) {
        if (a_pad.chars().nth(i as usize).unwrap().to_string().as_str() != b_pad.chars().nth(i as usize).unwrap().to_string().as_str()) {
            result = format!("{}{}", result, "1");
        } else {
            result = format!("{}{}", result, "0");
        }
        i = (i + 1);
    }
    return format!("{}{}", "0b", result).clone()
};
    println!("{}", binary_xor(25, 32));
    println!("{}", binary_xor(37, 50));
    println!("{}", binary_xor(21, 30));
    println!("{}", binary_xor(58, 73));
    println!("{}", binary_xor(0, 255));
    println!("{}", binary_xor(256, 256));
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
