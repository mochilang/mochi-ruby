// Generated by Mochi transpiler v0.10.72
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn min3(mut a: i64, mut b: i64, mut c: i64) -> i64 {
    let mut m: i64 = a;
    if (b < m) {
        m = b;
    }
    if (c < m) {
        m = c;
    }
    return m
};
    fn helper(mut word1: String, mut word2: String, cache: &mut Vec<Vec<i64>>, mut i: i64, mut j: i64, mut len1: i64, mut len2: i64) -> i64 {
    if (i >= len1) {
        return (len2 - j)
    }
    if (j >= len2) {
        return (len1 - i)
    }
    if (cache[i as usize].clone()[j as usize] != (-1)) {
        return cache[i as usize].clone()[j as usize]
    }
    let mut diff: i64 = 0;
    if ({ let tmp = &word1; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.as_str() != { let tmp = &word2; tmp.chars().skip(j as usize).take(((j + 1) - j) as usize).collect::<String>() }.as_str()) {
        diff = 1;
    }
    let delete_cost: i64 = (1 + helper(word1.clone(), word2.clone(), cache, (i + 1), j, len1, len2));
    let insert_cost: i64 = (1 + helper(word1.clone(), word2.clone(), cache, i, (j + 1), len1, len2));
    let replace_cost: i64 = (diff + helper(word1.clone(), word2.clone(), cache, (i + 1), (j + 1), len1, len2));
    (*cache)[i as usize][j as usize] = min3(delete_cost, insert_cost, replace_cost);
    return cache[i as usize].clone()[j as usize]
};
    fn min_distance_up_bottom(mut word1: String, mut word2: String) -> i64 {
    let len1: i64 = (word1.len() as i64);
    let len2: i64 = (word2.len() as i64);
    let mut cache: Vec<Vec<i64>> = vec![];
    for _ in 0..len1 {
        let mut row: Vec<i64> = vec![];
        for _2 in 0..len2 {
            row = { let mut _v = row.clone(); _v.push((-1)); _v };
        }
        cache = { let mut _v = cache.clone(); _v.push(row.clone()); _v };
    }
    return helper(word1.clone(), word2.clone(), &mut cache, 0, 0, len1, len2)
};
    println!("{}", min_distance_up_bottom(String::from("intention"), String::from("execution")).to_string());
    println!("{}", min_distance_up_bottom(String::from("intention"), String::new()).to_string());
    println!("{}", min_distance_up_bottom(String::new(), String::new()).to_string());
    println!("{}", min_distance_up_bottom(String::from("zooicoarchaeologist"), String::from("zoologist")).to_string());
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
