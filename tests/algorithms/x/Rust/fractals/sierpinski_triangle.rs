// Generated by Mochi transpiler v0.10.65
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
#[derive(Debug, Clone, Default)]
struct Point {
    x: i64,
    y: i64,
}
impl std::fmt::Display for Point {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"x\": {}", self.x)?;
        write!(f, ", ")?;
        write!(f, "\"y\": {}", self.y)?;
        write!(f, "}}")
    }
}

fn main() {
        let _start: i64 = _now();
    fn get_mid(mut p1: Point, mut p2: Point) -> Point {
    return Point {x: ((p1.x + p2.x) / 2), y: ((p1.y + p2.y) / 2)}
};
    fn point_to_string(mut p: Point) -> String {
    return format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", "(", p.x.to_string()), ","), p.y.to_string()), ")").clone()
};
    fn triangle(mut v1: Point, mut v2: Point, mut v3: Point, mut depth: i64) {
    println!("{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", point_to_string(v1.clone()), " "), point_to_string(v2.clone())), " "), point_to_string(v3.clone())));
    if (depth == 0) {
        return
    }
    triangle(v1.clone(), get_mid(v1.clone(), v2.clone()), get_mid(v1.clone(), v3.clone()), (depth - 1));
    triangle(v2.clone(), get_mid(v1.clone(), v2.clone()), get_mid(v2.clone(), v3.clone()), (depth - 1));
    triangle(v3.clone(), get_mid(v3.clone(), v2.clone()), get_mid(v1.clone(), v3.clone()), (depth - 1));
};
    triangle(Point {x: -175, y: -125}, Point {x: 0, y: 175}, Point {x: 175, y: -125}, 2);
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
