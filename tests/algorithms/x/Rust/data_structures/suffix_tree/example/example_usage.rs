// Generated by Mochi transpiler v0.10.61
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
#[derive(Debug, Clone, Default)]
struct SuffixTree {
    text: String,
}
impl std::fmt::Display for SuffixTree {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"text\": \"{}\"", self.text)?;
        write!(f, "}}")
    }
}

fn main() {
        let _start: i64 = _now();
    fn new_suffix_tree(text: &str) -> SuffixTree {
    return SuffixTree {text: text.to_string()}
};
    fn search(tree: &SuffixTree, pattern: &str) -> bool {
    let mut n = (tree.text.clone().len() as i64);
    let mut m = (pattern.len() as i64);
    if (m == 0) {
        return true
    }
    if (m > n) {
        return false
    }
    let mut i: i64 = 0;
    while (i <= (n - m)) {
        if (tree.text.clone().chars().skip(i as usize).take(((i + m) - i) as usize).collect::<String>() == pattern) {
            return true
        }
        i = (i + 1);
    }
    return false
};
    fn mochi_main() {
    let mut text: String = String::from("monkey banana").clone();
    let mut suffix_tree: SuffixTree = new_suffix_tree(&text);
    let mut patterns: Vec<String> = vec![String::from("ana").clone(), String::from("ban").clone(), String::from("na").clone(), String::from("xyz").clone(), String::from("mon").clone()];
    let mut i: i64 = 0;
    while (i < (patterns.len() as i64)) {
        let mut pattern: String = patterns[i as usize].clone().clone();
        let mut found: bool = search(&suffix_tree, &pattern);
        println!("{}", format!("{}{}", format!("{}{}", format!("{}{}", "Pattern '", pattern), "' found: "), found.to_string()));
        i = (i + 1);
    }
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
