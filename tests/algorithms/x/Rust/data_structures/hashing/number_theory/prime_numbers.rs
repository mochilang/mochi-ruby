// Generated by Mochi transpiler v0.10.61
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn isPrime(mut number: i64) -> bool {
    if (number < 2) {
        return false
    }
    if (number < 4) {
        return true
    }
    if ((number % 2) == 0) {
        return false
    }
    let mut i: i64 = 3;
    while ((i * i) <= number) {
        if ((number % i) == 0) {
            return false
        }
        i = (i + 2);
    }
    return true
};
    fn nextPrime(mut value: i64, mut factor: i64, mut desc: bool) -> i64 {
    let mut v: i64 = (value * factor);
    let mut firstValue: i64 = v;
    while !isPrime(v) {
        if desc {
            v = (v - 1);
        } else {
            v = (v + 1);
        }
    }
    if (v == firstValue) {
        if desc {
            return nextPrime((v - 1), 1, desc)
        } else {
            return nextPrime((v + 1), 1, desc)
        }
    }
    return v
};
    println!("{}", if isPrime(0) { 1 } else { 0 });
    println!("{}", if isPrime(1) { 1 } else { 0 });
    println!("{}", if isPrime(2) { 1 } else { 0 });
    println!("{}", if isPrime(3) { 1 } else { 0 });
    println!("{}", if isPrime(27) { 1 } else { 0 });
    println!("{}", if isPrime(87) { 1 } else { 0 });
    println!("{}", if isPrime(563) { 1 } else { 0 });
    println!("{}", if isPrime(2999) { 1 } else { 0 });
    println!("{}", if isPrime(67483) { 1 } else { 0 });
    println!("{}", nextPrime(14, 1, false));
    println!("{}", nextPrime(14, 1, true));
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
