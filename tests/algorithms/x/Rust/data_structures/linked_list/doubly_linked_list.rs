// Generated by Mochi transpiler v0.10.72
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
#[derive(Debug, Clone, Default)]
struct DoublyLinkedList {
    data: Vec<i64>,
}
impl std::fmt::Display for DoublyLinkedList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"data\": {:?}", self.data)?;
        write!(f, "}}")
    }
}

#[derive(Debug, Clone, Default)]
struct DeleteResult {
    list: DoublyLinkedList,
    value: i64,
}
impl std::fmt::Display for DeleteResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"list\": {}", self.list)?;
        write!(f, ", ")?;
        write!(f, "\"value\": {}", self.value)?;
        write!(f, "}}")
    }
}

fn main() {
        let _start: i64 = _now();
    fn empty_list() -> DoublyLinkedList {
    return DoublyLinkedList {data: vec![]}
};
    fn length(mut list: DoublyLinkedList) -> i64 {
    return (list.data.clone().len() as i64)
};
    fn is_empty(mut list: DoublyLinkedList) -> bool {
    return ((list.data.clone().len() as i64) == 0)
};
    fn to_string(mut list: DoublyLinkedList) -> String {
    if ((list.data.clone().len() as i64) == 0) {
        return String::new()
    }
    let mut s: String = list.data.clone()[0 as usize].to_string().clone();
    let mut i: i64 = 1;
    while (i < (list.data.clone().len() as i64)) {
        s = format!("{}{}", format!("{}{}", s, "->"), list.data.clone()[i as usize].to_string());
        i = (i + 1);
    }
    return s.clone()
};
    fn insert_nth(mut list: DoublyLinkedList, mut index: i64, mut value: i64) -> DoublyLinkedList {
    if ((index < 0) || (index > (list.data.clone().len() as i64))) {
        panic!("index out of range");
    }
    let mut res: Vec<i64> = vec![];
    let mut i: i64 = 0;
    while (i < index) {
        res = { let mut _v = res.clone(); _v.push(list.data.clone()[i as usize]); _v };
        i = (i + 1);
    }
    res = { let mut _v = res.clone(); _v.push(value); _v };
    while (i < (list.data.clone().len() as i64)) {
        res = { let mut _v = res.clone(); _v.push(list.data.clone()[i as usize]); _v };
        i = (i + 1);
    }
    return DoublyLinkedList {data: res.clone()}
};
    fn insert_head(mut list: DoublyLinkedList, mut value: i64) -> DoublyLinkedList {
    return insert_nth(list.clone(), 0, value)
};
    fn insert_tail(mut list: DoublyLinkedList, mut value: i64) -> DoublyLinkedList {
    return insert_nth(list.clone(), (list.data.clone().len() as i64), value)
};
    fn delete_nth(mut list: DoublyLinkedList, mut index: i64) -> DeleteResult {
    if ((index < 0) || (index >= (list.data.clone().len() as i64))) {
        panic!("index out of range");
    }
    let mut res: Vec<i64> = vec![];
    let mut i: i64 = 0;
    let mut removed: i64 = 0;
    while (i < (list.data.clone().len() as i64)) {
        if (i == index) {
            removed = list.data.clone()[i as usize];
        } else {
            res = { let mut _v = res.clone(); _v.push(list.data.clone()[i as usize]); _v };
        }
        i = (i + 1);
    }
    return DeleteResult {list: DoublyLinkedList {data: res.clone()}, value: removed}
};
    fn delete_head(mut list: DoublyLinkedList) -> DeleteResult {
    return delete_nth(list.clone(), 0)
};
    fn delete_tail(mut list: DoublyLinkedList) -> DeleteResult {
    return delete_nth(list.clone(), ((list.data.clone().len() as i64) - 1))
};
    fn delete_value(mut list: DoublyLinkedList, mut value: i64) -> DeleteResult {
    let mut idx: i64 = 0;
    let mut found: bool = false;
    while (idx < (list.data.clone().len() as i64)) {
        if (list.data.clone()[idx as usize] == value) {
            found = true;
            break
        }
        idx = (idx + 1);
    }
    if !found {
        panic!("value not found");
    }
    return delete_nth(list.clone(), idx)
};
    let mut mochi_main = || {
    let mut dll: DoublyLinkedList = empty_list();
    dll = insert_tail(dll.clone(), 1);
    dll = insert_tail(dll.clone(), 2);
    dll = insert_tail(dll.clone(), 3);
    println!("{}", to_string(dll.clone()));
    dll = insert_head(dll.clone(), 0);
    println!("{}", to_string(dll.clone()));
    dll = insert_nth(dll.clone(), 2, 9);
    println!("{}", to_string(dll.clone()));
    let mut res: DeleteResult = delete_nth(dll.clone(), 2);
    dll = res.list.clone();
    println!("{}", res.value);
    println!("{}", to_string(dll.clone()));
    res = delete_tail(dll.clone());
    dll = res.list.clone();
    println!("{}", res.value);
    println!("{}", to_string(dll.clone()));
    res = delete_value(dll.clone(), 1);
    dll = res.list.clone();
    println!("{}", res.value);
    println!("{}", to_string(dll.clone()));
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
