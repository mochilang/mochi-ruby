// Generated by Mochi transpiler v0.10.61
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
#[derive(Debug, Clone, Default)]
struct LinkedList {
    data: Vec<i64>,
}
impl std::fmt::Display for LinkedList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"data\": {:?}", self.data)?;
        write!(f, "}}")
    }
}

fn main() {
        let _start: i64 = _now();
    fn to_string(list: &LinkedList) -> String {
    if ((list.clone().data.clone().len() as i64) == 0) {
        return String::from("")
    }
    let mut s = list.clone().data.clone()[0 as usize].to_string();
    let mut i: i64 = 1;
    while (i < (list.clone().data.clone().len() as i64)) {
        s = format!("{}{}", format!("{}{}", s, " -> "), list.clone().data.clone()[i as usize].to_string());
        i = (i + 1);
    }
    return s.to_string().clone()
};
    fn reverse_k_nodes(list: &LinkedList, mut k: i64) -> LinkedList {
    if (k <= 1) {
        return (*list).clone()
    }
    let mut res: Vec<i64> = vec![];
    let mut i: i64 = 0;
    while (i < (list.clone().data.clone().len() as i64)) {
        let mut j: i64 = 0;
        let mut group: Vec<i64> = vec![];
        while ((j < k) && ((i + j) < (list.clone().data.clone().len() as i64))) {
            group = { let mut _v = group.clone(); _v.push(list.clone().data.clone()[(i + j) as usize]); _v };
            j = (j + 1);
        }
        if ((group.len() as i64) == k) {
            let mut g: i64 = (k - 1);
            while (g >= 0) {
                res = { let mut _v = res.clone(); _v.push(group[g as usize]); _v };
                g = (g - 1);
            }
        } else {
            let mut g: i64 = 0;
            while (g < (group.len() as i64)) {
                res = { let mut _v = res.clone(); _v.push(group[g as usize]); _v };
                g = (g + 1);
            }
        }
        i = (i + k);
    }
    return LinkedList {data: res}
};
    fn mochi_main() {
    let mut ll: LinkedList = LinkedList {data: vec![1, 2, 3, 4, 5]};
    println!("{}", format!("{}{}", "Original Linked List: ", to_string(&ll)));
    let mut k: i64 = 2;
    ll = reverse_k_nodes(&ll, k);
    println!("{}", format!("{}{}", format!("{}{}", format!("{}{}", "After reversing groups of size ", k.to_string()), ": "), to_string(&ll)));
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
