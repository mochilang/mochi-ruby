// Generated by Mochi transpiler v0.10.61
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn largest_rectangle_area(mut heights: Vec<i64>) -> i64 {
    let mut stack: Vec<i64> = vec![];
    let mut max_area: i64 = 0;
    let mut hs: Vec<i64> = heights.clone();
    hs = { let mut _v = hs.clone(); _v.push(0); _v };
    let mut i: i64 = 0;
    while (i < (hs.len() as i64)) {
        while (((stack.len() as i64) > 0) && (hs[i as usize] < hs[stack[((stack.len() as i64) - 1) as usize] as usize])) {
            let mut top: i64 = stack[((stack.len() as i64) - 1) as usize];
            stack = stack[0 as usize..((stack.len() as i64) - 1) as usize].to_vec();
            let mut height: i64 = hs[top as usize];
            let mut width: i64 = i;
            if ((stack.len() as i64) > 0) {
                width = ((i - stack[((stack.len() as i64) - 1) as usize]) - 1);
            }
            let mut area: i64 = (height * width);
            if (area > max_area) {
                max_area = area;
            }
        }
        stack = { let mut _v = stack.clone(); _v.push(i); _v };
        i = (i + 1);
    }
    return max_area
};
    println!("{}", largest_rectangle_area(vec![2, 1, 5, 6, 2, 3]).to_string());
    println!("{}", largest_rectangle_area(vec![2, 4]).to_string());
    println!("{}", largest_rectangle_area(vec![6, 2, 5, 4, 5, 1, 6]).to_string());
    println!("{}", largest_rectangle_area(vec![1]).to_string());
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
