// Generated by Mochi transpiler v0.10.61
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
static mut g_test_expression: String = String::new();
static mut g_test_expression2: String = String::new();
static mut g_test_expression3: String = String::new();
fn main() {
    unsafe {
        g_test_expression = String::from("+ 9 * 2 6").clone();
        g_test_expression2 = String::from("/ * 10 2 + 4 1 ").clone();
        g_test_expression3 = String::from("+ * 2 3 / 8 4").clone();
                let _start: i64 = _now();
        unsafe fn split_custom(s: &str, sep: &str) -> Vec<String> {
    let mut res: Vec<String> = vec![];
    let mut current: String = String::from("").clone();
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        let mut ch: String = { let tmp = &s; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.clone();
        if (ch == sep) {
            res = { let mut _v = res.clone(); _v.push(current.clone()); _v };
            current = String::from("");
        } else {
            current = format!("{}{}", current, ch);
        }
        i = (i + 1);
    }
    res = { let mut _v = res.clone(); _v.push(current.clone()); _v };
    return res
};
        unsafe fn tokenize(s: &str) -> Vec<String> {
    let mut parts: Vec<String> = split_custom(s, &" ");
    let mut res: Vec<String> = vec![];
    let mut i: i64 = 0;
    while (i < (parts.len() as i64)) {
        let mut p: String = parts[i as usize].clone().clone();
        if (p.as_str() != "") {
            res = { let mut _v = res.clone(); _v.push(p.clone()); _v };
        }
        i = (i + 1);
    }
    return res
};
        unsafe fn is_digit(ch: &str) -> bool {
    return ((ch >= "0") && (ch <= "9"))
};
        unsafe fn is_operand(token: &str) -> bool {
    if (token == "") {
        return false
    }
    let mut i: i64 = 0;
    while (i < (token.len() as i64)) {
        let mut ch: String = { let tmp = &token; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.clone();
        if !is_digit(&ch) {
            return false
        }
        i = (i + 1);
    }
    return true
};
        unsafe fn to_int(mut token: String) -> i64 {
    let mut res: i64 = 0;
    let mut i: i64 = 0;
    while (i < (token.len() as i64)) {
        res = ((res * 10) + { let n: i64 = { let tmp = &token; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.parse().unwrap(); n });
        i = (i + 1);
    }
    return res
};
        unsafe fn apply_op(op: &str, mut a: f64, mut b: f64) -> f64 {
    if (op == "+") {
        return (a + b)
    }
    if (op == "-") {
        return (a - b)
    }
    if (op == "*") {
        return (a * b)
    }
    if (op == "/") {
        return (a / b)
    }
    return 0.0
};
        unsafe fn evaluate(expression: &str) -> f64 {
    let mut tokens: Vec<String> = tokenize(expression);
    let mut stack: Vec<f64> = vec![];
    let mut i = ((tokens.len() as i64) - 1);
    while (i >= 0) {
        let mut token: String = tokens[i as usize].clone().clone();
        if (token.as_str() != "") {
            if is_operand(&token) {
                stack = { let mut _v = stack.clone(); _v.push((to_int(token.clone()) as f64)); _v };
            } else {
                let mut o1: f64 = stack[((stack.len() as i64) - 1) as usize];
                let mut o2: f64 = stack[((stack.len() as i64) - 2) as usize];
                stack = stack[0 as usize..((stack.len() as i64) - 2) as usize].to_vec();
                let mut res: f64 = apply_op(&token, o1, o2);
                stack = { let mut _v = stack.clone(); _v.push(res); _v };
            }
        }
        i = (i - 1);
    }
    return stack[0 as usize]
};
        unsafe fn eval_rec(mut tokens: Vec<String>, mut pos: i64) -> Vec<f64> {
    let mut token: String = tokens[pos as usize].clone().clone();
    let mut next: i64 = (pos + 1);
    if is_operand(&token) {
        return vec![(to_int(token.clone()) as f64), (next as f64)]
    }
    let mut left: Vec<f64> = eval_rec(tokens.clone(), next);
    let mut a: f64 = left[0 as usize];
    let mut p1: i64 = (left[1 as usize] as i64);
    let mut right: Vec<f64> = eval_rec(tokens.clone(), p1);
    let mut b: f64 = right[0 as usize];
    let mut p2: f64 = right[1 as usize];
    return vec![apply_op(&token, a, b), p2]
};
        unsafe fn evaluate_recursive(expression: &str) -> f64 {
    let mut tokens: Vec<String> = tokenize(expression);
    let mut res: Vec<f64> = eval_rec(tokens.clone(), 0);
    return res[0 as usize]
};
        println!("{}", evaluate(&g_test_expression).to_string());
        println!("{}", evaluate(&g_test_expression2).to_string());
        println!("{}", evaluate_recursive(&g_test_expression3).to_string());
        let _end: i64 = _now();
        let duration_us: i64 = ((_end - _start) / 1000);
        let memory_bytes: i64 = _mem();
        println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

    }
}
