# Code generated by Mochi transpiler 2025-08-25 22:27 +0700
defmodule Main do
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> Kernel.length(x)
    end
  end
  def search_in_sorted_matrix(mat, m, n, key) do
    try do
      i = m - 1
      j = 0
      while_fun = fn while_fun, i, j ->
        if i >= 0 && j < n do
          if key == Enum.at(Enum.at(mat, i, []), j, 0) do
            IO.puts(((((("Key " <> String.trim_trailing(Kernel.to_string(key), ".0")) <> " found at row- ") <> Kernel.to_string(i + 1)) <> " column- ") <> Kernel.to_string(j + 1)))
            throw {:return, mat}
          end
          {i, j} = if key < Enum.at(Enum.at(mat, i, []), j, 0) do
            i = i - 1
            {i, j}
          else
            j = j + 1
            {i, j}
          end
          while_fun.(while_fun, i, j)
        else
          {i, j}
        end
      end
      {_, _} = try do
          while_fun.(while_fun, i, j)
        catch
          {:break, {_, _}} -> {0, 0}
        end

      IO.puts((("Key " <> String.trim_trailing(Kernel.to_string(key), ".0")) <> " not found"))
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      mat = [[2.0, 5.0, 7.0], [4.0, 8.0, 13.0], [9.0, 11.0, 15.0], [12.0, 17.0, 20.0]]
      mat = search_in_sorted_matrix(mat, _len(mat), _len(Enum.at(mat, 0, [])), 5.0)
      mat = search_in_sorted_matrix(mat, _len(mat), _len(Enum.at(mat, 0, [])), 21.0)
      mat2 = [[2.1, 5.0, 7.0], [4.0, 8.0, 13.0], [9.0, 11.0, 15.0], [12.0, 17.0, 20.0]]
      mat2 = search_in_sorted_matrix(mat2, _len(mat2), _len(Enum.at(mat2, 0, [])), 2.1)
      mat2 = search_in_sorted_matrix(mat2, _len(mat2), _len(Enum.at(mat2, 0, [])), 2.2)
    catch
      {:return, val} -> val
    end
  end
  def bench_main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    main()
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
