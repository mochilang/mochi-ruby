# Code generated by Mochi transpiler 2025-08-12 10:04 +0700
defmodule Main do
  def kinetic_energy(mass, velocity) do
    try do
      if mass < 0.0 do
        raise("The mass of a body cannot be negative")
      end
      v = velocity
      {v} = if v < 0.0 do
        v = -v
        {v}
      else
        {v}
      end
      throw {:return, 0.5 * mass * v * v}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    IO.puts(Kernel.inspect(kinetic_energy(10.0, 10.0)))
    IO.puts(Kernel.inspect(kinetic_energy(0.0, 10.0)))
    IO.puts(Kernel.inspect(kinetic_energy(10.0, 0.0)))
    IO.puts(Kernel.inspect(kinetic_energy(20.0, -20.0)))
    IO.puts(Kernel.inspect(kinetic_energy(0.0, 0.0)))
    IO.puts(Kernel.inspect(kinetic_energy(2.0, 2.0)))
    IO.puts(Kernel.inspect(kinetic_energy(100.0, 100.0)))
  end
end
Main.main()
