# Code generated by Mochi transpiler 2025-08-24 23:57 +0700
defmodule Main do
  @data_dir nil
  import Kernel, except: [length: 1, to_string: 1]
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> Kernel.length(x)
    end
  end
  def empty_list() do
    try do
      throw {:return, %{data: []}}
    catch
      {:return, val} -> val
    end
  end
  def length(list) do
    try do
      throw {:return, _len(Map.get(list, :data, []))}
    catch
      {:return, val} -> val
    end
  end
  def is_empty(list) do
    try do
      throw {:return, _len(Map.get(list, :data, [])) == 0}
    catch
      {:return, val} -> val
    end
  end
  def to_string(list) do
    try do
      if _len(Map.get(list, :data, [])) == 0 do
        throw {:return, ""}
      end
      s = Kernel.to_string(Enum.at(list.data, 0, 0))
      i = 1
      while_fun = fn while_fun, i, s ->
        if i < _len(Map.get(list, :data, [])) do
          s = ((s <> "->") <> Kernel.to_string(Enum.at(list.data, i, 0)))
          i = i + 1
          while_fun.(while_fun, i, s)
        else
          {i, s}
        end
      end
      {_, s} = try do
          while_fun.(while_fun, i, s)
        catch
          {:break, {_, s}} -> {0, s}
        end

      throw {:return, s}
    catch
      {:return, val} -> val
    end
  end
  def insert_nth(list, index, value) do
    try do
      if index < 0 || index > _len(Map.get(list, :data, [])) do
        raise("index out of range")
      end
      res = []
      i = 0
      while_fun_2 = fn while_fun_2, i, res ->
        if i < index do
          res = (res ++ [Enum.at(list.data, i, 0)])
          i = i + 1
          while_fun_2.(while_fun_2, i, res)
        else
          {i, res}
        end
      end
      {_, res} = try do
          while_fun_2.(while_fun_2, i, res)
        catch
          {:break, {_, res}} -> {0, res}
        end

      res = (res ++ [value])
      while_fun_3 = fn while_fun_3, i, res ->
        if i < _len(Map.get(list, :data, [])) do
          res = (res ++ [Enum.at(list.data, i, 0)])
          i = i + 1
          while_fun_3.(while_fun_3, i, res)
        else
          {i, res}
        end
      end
      {_, res} = try do
          while_fun_3.(while_fun_3, i, res)
        catch
          {:break, {_, res}} -> {0, res}
        end

      throw {:return, %{data: res}}
    catch
      {:return, val} -> val
    end
  end
  def insert_head(list, value) do
    try do
      throw {:return, insert_nth(list, 0, value)}
    catch
      {:return, val} -> val
    end
  end
  def insert_tail(list, value) do
    try do
      throw {:return, insert_nth(list, _len(Map.get(list, :data, [])), value)}
    catch
      {:return, val} -> val
    end
  end
  def delete_nth(list, index) do
    try do
      if index < 0 || index >= _len(Map.get(list, :data, [])) do
        raise("index out of range")
      end
      res = []
      i = 0
      val = 0
      while_fun_4 = fn while_fun_4, i, res, val ->
        if i < _len(Map.get(list, :data, [])) do
          {res, val} = if i == index do
            val = Enum.at(list.data, i, 0)
            {res, val}
          else
            res = (res ++ [Enum.at(list.data, i, 0)])
            {res, val}
          end
          i = i + 1
          while_fun_4.(while_fun_4, i, res, val)
        else
          {i, res, val}
        end
      end
      {_, res, val} = try do
          while_fun_4.(while_fun_4, i, res, val)
        catch
          {:break, {_, res, val}} -> {0, res, val}
        end

      throw {:return, %{list: %{data: res}, value: val}}
    catch
      {:return, val} -> val
    end
  end
  def delete_front(list) do
    try do
      throw {:return, delete_nth(list, 0)}
    catch
      {:return, val} -> val
    end
  end
  def delete_tail(list) do
    try do
      throw {:return, delete_nth(list, _len(Map.get(list, :data, [])) - 1)}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      cll = empty_list()
      i = 0
      while_fun_5 = fn while_fun_5, cll, i ->
        if i < 5 do
          cll = insert_tail(cll, i + 1)
          i = i + 1
          while_fun_5.(while_fun_5, cll, i)
        else
          {cll, i}
        end
      end
      {cll, _} = try do
          while_fun_5.(while_fun_5, cll, i)
        catch
          {:break, {cll, _}} -> {cll, 0}
        end

      IO.puts(to_string(cll))
      cll = insert_tail(cll, 6)
      IO.puts(to_string(cll))
      cll = insert_head(cll, 0)
      IO.puts(to_string(cll))
      res = delete_front(cll)
      cll = res.list
      IO.puts(res.value)
      res = delete_tail(cll)
      cll = res.list
      IO.puts(res.value)
      res = delete_nth(cll, 2)
      cll = res.list
      IO.puts(res.value)
      IO.puts(to_string(cll))
    catch
      {:return, val} -> val
    end
  end
  def bench_main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    main()
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
