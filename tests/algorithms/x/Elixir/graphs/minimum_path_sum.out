warning: variable "cell_n" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 25 │       {cell_n, current_row} = try do
    │        ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/minimum_path_sum.exs:25:8: Main.fill_row/2

    warning: variable "cell_n" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 51 │       {cell_n, grid} = try do
    │        ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/minimum_path_sum.exs:51:8: Main.min_path_sum/1

    warning: variable "row_above" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 70 │       {grid, row_above, row_n} = try do
    │              ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/minimum_path_sum.exs:70:14: Main.min_path_sum/1

    warning: variable "row_n" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 70 │       {grid, row_above, row_n} = try do
    │                         ~
    │
    └─ /workspace/mochi/tests/algorithms/x/Elixir/graphs/minimum_path_sum.exs:70:25: Main.min_path_sum/1

7
20