# Code generated by Mochi transpiler 2025-08-08 09:18 +0700
defmodule Main do
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  def partition(m) do
    try do
      memo = []
      i = 0
      while_fun = fn while_fun, i, memo ->
        if i < m + 1 do
          row = []
          j = 0
          while_fun_2 = fn while_fun_2, j, row ->
            if j < m do
              row = (row ++ [0])
              j = j + 1
              while_fun_2.(while_fun_2, j, row)
            else
              {j, row}
            end
          end
          {j, row} = try do
              while_fun_2.(while_fun_2, j, row)
            catch
              {:break, {j, row}} -> {j, row}
            end

          memo = (memo ++ [row])
          i = i + 1
          while_fun.(while_fun, i, memo)
        else
          {i, memo}
        end
      end
      {i, memo} = try do
          while_fun.(while_fun, i, memo)
        catch
          {:break, {i, memo}} -> {i, memo}
        end

      i = 0
      while_fun_3 = fn while_fun_3, i, memo ->
        if i < m + 1 do
          memo = List.replace_at(memo, i, List.replace_at(Enum.at(memo, i), 0, 1))
          i = i + 1
          while_fun_3.(while_fun_3, i, memo)
        else
          {i, memo}
        end
      end
      {i, memo} = try do
          while_fun_3.(while_fun_3, i, memo)
        catch
          {:break, {i, memo}} -> {i, memo}
        end

      n = 0
      while_fun_4 = fn while_fun_4, memo, n ->
        if n < m + 1 do
          k = 1
          while_fun_5 = fn while_fun_5, k, memo ->
            if k < m do
              memo = List.replace_at(memo, n, List.replace_at(Enum.at(memo, n), k, Enum.at(Enum.at(memo, n), k) + Enum.at(Enum.at(memo, n), k - 1)))
              {memo} = if n - k > 0 do
                memo = List.replace_at(memo, n, List.replace_at(Enum.at(memo, n), k, Enum.at(Enum.at(memo, n), k) + Enum.at(Enum.at(memo, n - k - 1), k)))
                {memo}
              else
                {memo}
              end
              k = k + 1
              while_fun_5.(while_fun_5, k, memo)
            else
              {k, memo}
            end
          end
          {k, memo} = try do
              while_fun_5.(while_fun_5, k, memo)
            catch
              {:break, {k, memo}} -> {k, memo}
            end

          n = n + 1
          while_fun_4.(while_fun_4, memo, n)
        else
          {memo, n}
        end
      end
      {memo, n} = try do
          while_fun_4.(while_fun_4, memo, n)
        catch
          {:break, {memo, n}} -> {memo, n}
        end

      throw {:return, Enum.at(Enum.at(memo, m), m - 1)}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    IO.puts(Kernel.inspect(partition(5)))
    IO.puts(Kernel.inspect(partition(7)))
    IO.puts(Kernel.inspect(partition(100)))
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.main()
