# Code generated by Mochi transpiler 2025-08-12 13:41 +0700
defmodule Main do
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  def exp_approx(x) do
    try do
      neg = false
      y = x
      {neg, y} = if x < 0.0 do
        neg = true
        y = -x
        {neg, y}
      else
        {neg, y}
      end
      term = 1.0
      sum = 1.0
      n = 1
      while_fun = fn while_fun, n, sum, term ->
        if n < 30 do
          term = term * y / (:erlang.float(n))
          sum = sum + term
          n = n + 1
          while_fun.(while_fun, n, sum, term)
        else
          {n, sum, term}
        end
      end
      {n, sum, term} = try do
          while_fun.(while_fun, n, sum, term)
        catch
          {:break, {n, sum, term}} -> {n, sum, term}
        end

      if neg do
        throw {:return, 1.0 / sum}
      end
      throw {:return, sum}
    catch
      {:return, val} -> val
    end
  end
  def ln_series(x) do
    try do
      t = (x - 1.0) / (x + 1.0)
      term = t
      acc = 0.0
      n = 1
      while_fun_2 = fn while_fun_2, acc, n, term ->
        if n <= 19 do
          acc = acc + term / (:erlang.float(n))
          term = term * t * t
          n = n + 2
          while_fun_2.(while_fun_2, acc, n, term)
        else
          {acc, n, term}
        end
      end
      {acc, n, term} = try do
          while_fun_2.(while_fun_2, acc, n, term)
        catch
          {:break, {acc, n, term}} -> {acc, n, term}
        end

      throw {:return, 2.0 * acc}
    catch
      {:return, val} -> val
    end
  end
  def ln(x) do
    try do
      y = x
      k = 0
      while_fun_3 = fn while_fun_3, k, y ->
        if y >= 10.0 do
          y = y / 10.0
          k = k + 1
          while_fun_3.(while_fun_3, k, y)
        else
          {k, y}
        end
      end
      {k, y} = try do
          while_fun_3.(while_fun_3, k, y)
        catch
          {:break, {k, y}} -> {k, y}
        end

      while_fun_4 = fn while_fun_4, k, y ->
        if y < 1.0 do
          y = y * 10.0
          k = k - 1
          while_fun_4.(while_fun_4, k, y)
        else
          {k, y}
        end
      end
      {k, y} = try do
          while_fun_4.(while_fun_4, k, y)
        catch
          {:break, {k, y}} -> {k, y}
        end

      throw {:return, ln_series(y) + (:erlang.float(k)) * ln_series(10.0)}
    catch
      {:return, val} -> val
    end
  end
  def softplus(x) do
    try do
      throw {:return, ln(1.0 + exp_approx(x))}
    catch
      {:return, val} -> val
    end
  end
  def tanh_approx(x) do
    try do
      throw {:return, (2.0 / (1.0 + exp_approx(-2.0 * x))) - 1.0}
    catch
      {:return, val} -> val
    end
  end
  def mish(vector) do
    try do
      result = []
      i = 0
      while_fun_5 = fn while_fun_5, i, result ->
        if i < _len(vector) do
          x = Enum.at(vector, i, [])
          sp = softplus(x)
          y = x * tanh_approx(sp)
          result = (result ++ [y])
          i = i + 1
          while_fun_5.(while_fun_5, i, result)
        else
          {i, result}
        end
      end
      {i, result} = try do
          while_fun_5.(while_fun_5, i, result)
        catch
          {:break, {i, result}} -> {i, result}
        end

      throw {:return, result}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      v1 = [2.3, 0.6, -2.0, -3.8]
      v2 = [-9.2, -0.3, 0.45, -4.56]
      IO.puts(Kernel.inspect(mish(v1)))
      IO.puts(Kernel.inspect(mish(v2)))
    catch
      {:return, val} -> val
    end
  end
  def bench_main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    main()
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
