# Code generated by Mochi transpiler 2025-08-08 20:44 +0700
defmodule Main do
  def arc_length(angle, radius) do
    try do
      throw {:return, 2.0 * Process.get(:pi) * radius * (angle / 360.0)}
    catch
      {:return, val} -> val
    end
  end
  Process.put(:pi, 3.141592653589793)
  def main() do
    IO.puts(Kernel.inspect(arc_length(45.0, 5.0)))
    IO.puts(Kernel.inspect(arc_length(120.0, 15.0)))
    IO.puts(Kernel.inspect(arc_length(90.0, 10.0)))
  end
end
Main.main()
