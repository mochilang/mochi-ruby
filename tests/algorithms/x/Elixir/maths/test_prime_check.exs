# Code generated by Mochi transpiler 2025-08-25 22:27 +0700
defmodule Main do
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  def is_prime(number) do
    try do
      if number < 0 do
        raise("is_prime() only accepts positive integers")
      end
      if number < 2 do
        throw {:return, false}
      end
      if number < 4 do
        throw {:return, true}
      end
      if rem(number, 2) == 0 || rem(number, 3) == 0 do
        throw {:return, false}
      end
      i = 5
      while_fun = fn while_fun, i ->
        if i * i <= number do
          if rem(number, i) == 0 || rem(number, (i + 2)) == 0 do
            throw {:return, false}
          end
          i = i + 6
          while_fun.(while_fun, i)
        else
          i
        end
      end
      _ = try do
          while_fun.(while_fun, i)
        catch
          {:break, {_}} -> 0
        end

      throw {:return, true}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    IO.puts(Kernel.inspect(is_prime(2)))
    IO.puts(Kernel.inspect(is_prime(3)))
    IO.puts(Kernel.inspect(is_prime(5)))
    IO.puts(Kernel.inspect(is_prime(7)))
    IO.puts(Kernel.inspect(is_prime(11)))
    IO.puts(Kernel.inspect(is_prime(13)))
    IO.puts(Kernel.inspect(is_prime(17)))
    IO.puts(Kernel.inspect(is_prime(19)))
    IO.puts(Kernel.inspect(is_prime(23)))
    IO.puts(Kernel.inspect(is_prime(29)))
    IO.puts(Kernel.inspect(is_prime(0)))
    IO.puts(Kernel.inspect(is_prime(1)))
    IO.puts(Kernel.inspect(is_prime(4)))
    IO.puts(Kernel.inspect(is_prime(6)))
    IO.puts(Kernel.inspect(is_prime(9)))
    IO.puts(Kernel.inspect(is_prime(15)))
    IO.puts(Kernel.inspect(is_prime(105)))
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.main()
