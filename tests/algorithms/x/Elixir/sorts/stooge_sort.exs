# Code generated by Mochi transpiler 2025-08-24 08:57 +0700
defmodule Main do
  @data_dir nil
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  def stooge(arr, i, h) do
    try do
      if i >= h do
        throw {:return, nil}
      end
      arr = if Enum.at(arr, i, []) > Enum.at(arr, h, []) do
        tmp = Enum.at(arr, i, [])
        arr = List.replace_at(arr, i, Enum.at(arr, h, []))
        arr = List.replace_at(arr, h, tmp)
        arr
      else
        arr
      end
      arr = if h - i + 1 > 2 do
        t = (fn v -> if is_binary(v), do: String.to_integer(v), else: trunc(v) end).((div((h - i + 1), 3)))
        arr = stooge(arr, i, h - t)
        arr = stooge(arr, i + t, h)
        arr = stooge(arr, i, h - t)
        arr
      else
        arr
      end
    catch
      {:return, val} -> val
    end
  end
  def stooge_sort(arr) do
    try do
      arr = stooge(arr, 0, _len(arr) - 1)
      throw {:return, arr}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    IO.puts(Kernel.inspect(stooge_sort([18, 0, -7, -1, 2, 2])))
  end
end
Main.main()
