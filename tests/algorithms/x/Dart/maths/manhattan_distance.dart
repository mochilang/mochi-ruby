// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

double abs_val(double x) {
  if (x < 0.0) {
    return -x;
  }
  return x;
}

void validate_point(List<double> p) {
  if (p.length == 0) {
    _error("Missing an input");
  }
}

double manhattan_distance(List<double> a, List<double> b) {
  validate_point(a);
  validate_point(b);
  if (a.length != b.length) {
    _error("Both points must be in the same n-dimensional space");
  }
  double total = 0.0;
  int i = 0;
  while (i < a.length) {
    total = total + abs_val(a[i] - b[i]);
    i = i + 1;
  }
  return total;
}

double manhattan_distance_one_liner(List<double> a, List<double> b) {
  return manhattan_distance(a, b);
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(manhattan_distance([1.0, 1.0], [2.0, 2.0])));
  print(_str(manhattan_distance([1.5, 1.5], [2.0, 2.0])));
  print(_str(manhattan_distance_one_liner([1.5, 1.5], [2.5, 2.0])));
  print(_str(manhattan_distance_one_liner([-3.0, -3.0, -3.0], [0.0, 0.0, 0.0])));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
