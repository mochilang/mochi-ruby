// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

int normalize_index(int index, int n) {
  if (index < 0) {
    return n + index;
  }
  return index;
}

double find_max_iterative(List<double> nums) {
  if (nums.length == 0) {
    _error("find_max_iterative() arg is an empty sequence");
  }
  double max_num = nums[0];
  int i = 0;
  while (i < nums.length) {
    double x = nums[i];
    if (x > max_num) {
    max_num = x;
  }
    i = i + 1;
  }
  return max_num;
}

double find_max_recursive(List<double> nums, int left, int right) {
  int n = nums.length;
  if (n == 0) {
    _error("find_max_recursive() arg is an empty sequence");
  }
  if (left >= n || left < 0 - n || right >= n || right < 0 - n) {
    _error("list index out of range");
  }
  int l = normalize_index(left, n);
  int r = normalize_index(right, n);
  if (l == r) {
    return nums[l];
  }
  int mid = (l + r) ~/ 2;
  double left_max = find_max_recursive(nums, l, mid);
  double right_max = find_max_recursive(nums, mid + 1, r);
  if (left_max >= right_max) {
    return left_max;
  }
  return right_max;
}

void test_find_max() {
  List<double> arr = [2.0, 4.0, 9.0, 7.0, 19.0, 94.0, 5.0];
  if (find_max_iterative(arr) != 94.0) {
    _error("find_max_iterative failed");
  }
  if (find_max_recursive(arr, 0, arr.length - 1) != 94.0) {
    _error("find_max_recursive failed");
  }
  if (find_max_recursive(arr, -arr.length, -1) != 94.0) {
    _error("negative index handling failed");
  }
}

void _main() {
  test_find_max();
  List<double> nums = [2.0, 4.0, 9.0, 7.0, 19.0, 94.0, 5.0];
  print(find_max_iterative(nums));
  print(find_max_recursive(nums, 0, nums.length - 1));
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
