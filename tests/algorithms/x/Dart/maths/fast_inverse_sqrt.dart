// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

int pow2_int(int n) {
  int result = 1;
  int i = 0;
  while (i < n) {
    result = result * 2;
    i = i + 1;
  }
  return result;
}

double pow2_float(int n) {
  double result = 1.0;
  if (n >= 0) {
    int i = 0;
    while (i < n) {
    result = result * 2.0;
    i = i + 1;
  };
  } else {
    int i = 0;
    int m = 0 - n;
    while (i < m) {
    result = result / 2.0;
    i = i + 1;
  };
  }
  return result;
}

int lshift(int _num, int k) {
  int result = _num;
  int i = 0;
  while (i < k) {
    result = result * 2;
    i = i + 1;
  }
  return result;
}

int rshift(int _num, int k) {
  int result = _num;
  int i = 0;
  while (i < k) {
    result = (result - result % 2) ~/ 2;
    i = i + 1;
  }
  return result;
}

int log2_floor(double x) {
  double n = x;
  int e = 0;
  while (n >= 2.0) {
    n = n / 2.0;
    e = e + 1;
  }
  while (n < 1.0) {
    n = n * 2.0;
    e = e - 1;
  }
  return e;
}

int float_to_bits(double x) {
  double _num = x;
  int sign = 0;
  if (_num < 0.0) {
    sign = 1;
    _num = -_num;
  }
  int exp = log2_floor(_num);
  double pow = pow2_float(exp);
  double normalized = _num / pow;
  double frac = normalized - 1.0;
  int mantissa = (frac * pow2_float(23)).toInt();
  int exp_bits = exp + 127;
  return lshift(sign, 31) + lshift(exp_bits, 23) + mantissa;
}

double bits_to_float(int bits) {
  int sign_bit = rshift(bits, 31) % 2;
  double sign = 1.0;
  if (sign_bit == 1) {
    sign = -1.0;
  }
  int exp_bits = rshift(bits, 23) % 256;
  int exp = exp_bits - 127;
  int mantissa_bits = bits % pow2_int(23);
  double mantissa = 1.0 + mantissa_bits.toDouble() / pow2_float(23);
  return sign * mantissa * pow2_float(exp);
}

double absf(double x) {
  if (x < 0.0) {
    return -x;
  }
  return x;
}

double sqrtApprox(double x) {
  if (x <= 0.0) {
    return 0.0;
  }
  double guess = x / 2.0;
  int i = 0;
  while (i < 20) {
    guess = (guess + x / guess) / 2.0;
    i = i + 1;
  }
  return guess;
}

bool is_close(double a, double b, double rel_tol) {
  return absf(a - b) <= rel_tol * absf(b);
}

double fast_inverse_sqrt(double number) {
  if (number <= 0.0) {
    _error("Input must be a positive number.");
  }
  int i = float_to_bits(number);
  int magic = 1597463007;
  int y_bits = magic - rshift(i, 1);
  double y = bits_to_float(y_bits);
  y = y * (1.5 - 0.5 * number * y * y);
  return y;
}

void test_fast_inverse_sqrt() {
  if (absf(fast_inverse_sqrt(10.0) - 0.3156857923527257) > 0.0001) {
    _error("fast_inverse_sqrt(10) failed");
  }
  if (absf(fast_inverse_sqrt(4.0) - 0.49915357479239103) > 0.0001) {
    _error("fast_inverse_sqrt(4) failed");
  }
  if (absf(fast_inverse_sqrt(4.1) - 0.4932849504615651) > 0.0001) {
    _error("fast_inverse_sqrt(4.1) failed");
  }
  int i = 50;
  while (i < 60) {
    double y = fast_inverse_sqrt(i.toDouble());
    double actual = 1.0 / sqrtApprox(i.toDouble());
    if (!is_close(y, actual, 0.00132)) {
    _error("relative error too high");
  }
    i = i + 1;
  }
}

void _main() {
  test_fast_inverse_sqrt();
  int i = 5;
  while (i <= 100) {
    double diff = 1.0 / sqrtApprox(i.toDouble()) - fast_inverse_sqrt(i.toDouble());
    print(_str(i) + ": " + _str(diff));
    i = i + 5;
  }
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
