// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

double f(double x) {
  return (x - 0.0) * (x - 0.0);
}

List<double> make_points(double a, double b, double h) {
  List<double> points = <double>[];
  double x = a + h;
  while (x < b - h) {
    points = [...points, x];
    x = x + h;
  }
  return points;
}

double simpson_rule(List<double> boundary, int steps) {
  if (steps <= 0) {
    _error("Number of steps must be greater than zero");
  }
  double a = boundary[0];
  double b = boundary[1];
  double h = (b - a) / steps.toDouble();
  List<double> pts = make_points(a, b, h);
  double y = h / 3.0 * f(a);
  int cnt = 2;
  int i = 0;
  while (i < pts.length) {
    double coeff = 4.0 - 2.0 * (cnt % 2).toDouble();
    y = y + h / 3.0 * coeff * f(pts[i]);
    cnt = cnt + 1;
    i = i + 1;
  }
  y = y + h / 3.0 * f(b);
  return y;
}

double result = simpson_rule([0.0, 1.0], 10);
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(result));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
