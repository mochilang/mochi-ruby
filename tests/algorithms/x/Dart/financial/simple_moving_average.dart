// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}


bool _listEq(List a, List b) {
  if (a.length != b.length) return false;
  for (var i = 0; i < a.length; i++) {
    final x = a[i];
    final y = b[i];
    if (x is List && y is List) {
      if (!_listEq(x, y)) return false;
    } else if (x != y) {
      return false;
    }
  }
  return true;
}

String _str(dynamic v) => v.toString();


Never _error(String msg) {
  throw Exception(msg);
}

class SMAValue {
  double value;
  bool ok;
  SMAValue({required this.value, required this.ok});
}

List<SMAValue> simple_moving_average(List<double> data, int window_size) {
  if (window_size < 1) {
    _error("Window size must be a positive integer");
  }
  List<SMAValue> result = <SMAValue>[];
  double window_sum = 0.0;
  int i = 0;
  while (i < data.length) {
    window_sum = window_sum + data[i];
    if (i >= window_size) {
    window_sum = window_sum - data[i - window_size];
  }
    if (i >= window_size - 1) {
    double avg = window_sum / window_size;
    result = [...result, SMAValue(value: avg, ok: true)];
  } else {
    result = [...result, SMAValue(value: 0.0, ok: false)];
  }
    i = i + 1;
  }
  return result;
}

List<double> data = [10.0, 12.0, 15.0, 13.0, 14.0, 16.0, 18.0, 17.0, 19.0, 21.0];
int window_size = 3;
List<SMAValue> sma_values = simple_moving_average(data, window_size);
int idx = 0;
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  while (idx < sma_values.length) {
    SMAValue item = sma_values[idx];
    if (item.ok) {
    print("Day " + _str(idx + 1) + ": " + _str(item.value));
  } else {
    print("Day " + _str(idx + 1) + ": Not enough data for SMA");
  }
    idx = idx + 1;
  }
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
