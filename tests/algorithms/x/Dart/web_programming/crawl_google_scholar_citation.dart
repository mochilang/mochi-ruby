// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

String DIGITS = "0123456789";
bool is_digit(String ch) {
  int i = 0;
  while (i < DIGITS.length) {
    if (DIGITS.substring(i, i + 1) == ch) {
    return true;
  }
    i = i + 1;
  }
  return false;
}

int find_substring(String haystack, String needle) {
  int i = 0;
  while (i <= haystack.length - needle.length) {
    int j = 0;
    while (j < needle.length) {
    if (haystack.substring(i + j, i + j + 1) != needle.substring(j, j + 1)) {
    break;
  }
    j = j + 1;
  }
    if (j == needle.length) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

String extract_citation(String html) {
  String marker = "Cited by ";
  int idx = find_substring(html, marker);
  if (idx < 0) {
    return "";
  }
  int pos = idx + marker.length;
  String result = "";
  while (pos < html.length) {
    String ch = html.substring(pos, pos + 1);
    if (!is_digit(ch)) {
    break;
  }
    result = result + ch;
    pos = pos + 1;
  }
  return result;
}

String get_citation(String base_url, Map<String, String> params) {
  String html = "<div class=\"gs_ri\"><div class=\"gs_fl\"><a>Cited by 123</a></div></div>";
  return extract_citation(html);
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  if (__name__ == "__main__") {
    Map<String, String> params = {"title": "Precisely geometry controlled microsupercapacitors for ultrahigh areal capacitance, volumetric capacitance, and energy density", "journal": "Chem. Mater.", "volume": "30", "pages": "3979-3990", "year": "2018", "hl": "en"};
    print(get_citation("https://scholar.google.com/scholar_lookup", params));
  }
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
