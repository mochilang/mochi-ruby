// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

int find(String text, String pattern, int start) {
  int i = start;
  int limit = text.length - pattern.length;
  while (i <= limit) {
    if (_substr(text, i, i + pattern.length) == pattern) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

String stock_price(String symbol) {
  Map<String, String> pages = {"AAPL": "<span data-testid=\"qsp-price\">228.43</span>", "AMZN": "<span data-testid=\"qsp-price\">201.85</span>", "IBM": "<span data-testid=\"qsp-price\">210.30</span>", "GOOG": "<span data-testid=\"qsp-price\">177.86</span>", "MSFT": "<span data-testid=\"qsp-price\">414.82</span>", "ORCL": "<span data-testid=\"qsp-price\">188.87</span>"};
  if (pages.containsKey(symbol)) {
    String html = pages[symbol]!;
    String marker = "<span data-testid=\"qsp-price\">";
    int start_idx = find(html, marker, 0);
    if (start_idx != -1) {
    int price_start = start_idx + marker.length;
    int end_idx = find(html, "</span>", price_start);
    if (end_idx != -1) {
    return _substr(html, price_start, end_idx);
  };
  };
  }
  return "No <fin-streamer> tag with the specified data-testid attribute found.";
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  for (String symbol in ["AAPL", "AMZN", "IBM", "GOOG", "MSFT", "ORCL"]) {
    print("Current " + symbol + " stock price is " + stock_price(symbol));
  }
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
