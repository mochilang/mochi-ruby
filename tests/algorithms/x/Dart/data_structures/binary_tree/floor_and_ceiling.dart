// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();

class Node {
  int key;
  int left;
  int right;
  Node({required this.key, required this.left, required this.right});
}

List<int> inorder(List<Node> nodes, int idx) {
  if (idx == -1) {
    return ([] as List<dynamic>).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
  }
  Node node = nodes[idx];
  List<int> result = inorder(nodes, node.left);
  result = [...result, node.key];
  result = ([...result, ...inorder(nodes, node.right)] as List<dynamic>).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
  return result;
}

List<int> floor_ceiling(List<Node> nodes, int idx, int key) {
  dynamic? floor_val = null;
  dynamic? ceiling_val = null;
  int current = idx;
  while (current != -1) {
    Node node = nodes[current];
    if (node.key == key) {
    floor_val = node.key;
    ceiling_val = node.key;
    break;
  }
    if (key < node.key) {
    ceiling_val = node.key;
    current = node.left;
  } else {
    floor_val = node.key;
    current = node.right;
  }
  }
  return ([floor_val!, ceiling_val!] as List<dynamic>).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
}

List<Node> tree = [Node(key: 10, left: 1, right: 2), Node(key: 5, left: 3, right: 4), Node(key: 20, left: 5, right: 6), Node(key: 3, left: -1, right: -1), Node(key: 7, left: -1, right: -1), Node(key: 15, left: -1, right: -1), Node(key: 25, left: -1, right: -1)];
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(inorder(tree, 0)));
  print(_str(floor_ceiling(tree, 0, 8)));
  print(_str(floor_ceiling(tree, 0, 14)));
  print(_str(floor_ceiling(tree, 0, -1)));
  print(_str(floor_ceiling(tree, 0, 30)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
