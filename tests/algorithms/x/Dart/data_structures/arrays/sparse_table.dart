// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

int pow2(int n) {
  int result = 1;
  int i = 0;
  while (i < n) {
    result = result * 2;
    i = i + 1;
  }
  return result;
}

int int_log2(int n) {
  int v = n;
  int res = 0;
  while (v > 1) {
    v = v ~/ 2;
    res = res + 1;
  }
  return res;
}

List<List<int>> build_sparse_table(List<int> number_list) {
  if (number_list.length == 0) {
    _error("empty number list not allowed");
  }
  int length = number_list.length;
  int row = int_log2(length) + 1;
  List<List<int>> sparse_table = <List<int>>[];
  int j = 0;
  while (j < row) {
    List<int> inner = <int>[];
    int i = 0;
    while (i < length) {
    inner = [...inner, 0];
    i = i + 1;
  }
    sparse_table = ([...sparse_table, inner] as List<dynamic>).map((e) => ((e as List<dynamic>).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList() as List<int>)).toList();
    j = j + 1;
  }
  int i = 0;
  while (i < length) {
    while (sparse_table[0]!.length <= i) { sparse_table[0]!.add(0); } sparse_table[0]![i] = number_list[i];
    i = i + 1;
  }
  j = 1;
  while (pow2(j) <= length) {
    i = 0;
    while (i + pow2(j) - 1 < length) {
    int left = sparse_table[j - 1][i + pow2(j - 1)];
    int right = sparse_table[j - 1][i];
    if (left < right) {
    while (sparse_table[j]!.length <= i) { sparse_table[j]!.add(0); } sparse_table[j]![i] = left;
  } else {
    while (sparse_table[j]!.length <= i) { sparse_table[j]!.add(0); } sparse_table[j]![i] = right;
  }
    i = i + 1;
  }
    j = j + 1;
  }
  return sparse_table;
}

int query(List<List<int>> sparse_table, int left_bound, int right_bound) {
  if (left_bound < 0 || right_bound >= sparse_table[0].length) {
    _error("list index out of range");
  }
  int interval = right_bound - left_bound + 1;
  int j = int_log2(interval);
  int val1 = sparse_table[j][right_bound - pow2(j) + 1];
  int val2 = sparse_table[j][left_bound];
  if (val1 < val2) {
    return val1;
  }
  return val2;
}

List<List<int>> st1 = build_sparse_table([8, 1, 0, 3, 4, 9, 3]);
List<List<int>> st2 = build_sparse_table([3, 1, 9]);
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(st1));
  print(_str(st2));
  print(_str(query(st1, 0, 4)));
  print(_str(query(st1, 4, 6)));
  print(_str(query(st2, 2, 2)));
  print(_str(query(st2, 0, 1)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
