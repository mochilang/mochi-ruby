// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();

List<int> sort_triplet(int a, int b, int c) {
  int x = a;
  int y = b;
  int z = c;
  if (x > y) {
    int t = x;
    x = y;
    y = t;
  }
  if (y > z) {
    int t = y;
    y = z;
    z = t;
  }
  if (x > y) {
    int t = x;
    x = y;
    y = t;
  }
  return [x, y, z];
}

bool contains_triplet(List<List<int>> arr, List<int> target) {
  for (int i = 0; i < arr.length; i++) {
    List<int> item = arr[i];
    bool same = true;
    for (int j = 0; j < target.length; j++) {
    if (item[j] != target[j]) {
    same = false;
    break;
  }
  }
    if (same) {
    return true;
  }
  }
  return false;
}

bool contains_int(List<int> arr, int value) {
  for (int i = 0; i < arr.length; i++) {
    if (arr[i] == value) {
    return true;
  }
  }
  return false;
}

List<List<int>> find_triplets_with_0_sum(List<int> nums) {
  int n = nums.length;
  List<List<int>> result = <List<int>>[];
  for (int i = 0; i < n; i++) {
    for (int j = i + 1; j < n; j++) {
    for (int k = j + 1; k < n; k++) {
    int a = nums[i];
    int b = nums[j];
    int c = nums[k];
    if (a + b + c == 0) {
    List<int> trip = sort_triplet(a, b, c);
    if (!contains_triplet(result, trip)) {
    result = ([...result, trip] as List<dynamic>).map((e) => ((e as List<dynamic>).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList() as List<int>)).toList();
  };
  }
  }
  }
  }
  return result;
}

List<List<int>> find_triplets_with_0_sum_hashing(List<int> arr) {
  int target_sum = 0;
  List<List<int>> output = <List<int>>[];
  for (int i = 0; i < arr.length; i++) {
    List<int> seen = <int>[];
    int current_sum = target_sum - arr[i];
    for (int j = i + 1; j < arr.length; j++) {
    int other = arr[j];
    int required = current_sum - other;
    if (contains_int(seen, required)) {
    List<int> trip = sort_triplet(arr[i], other, required);
    if (!contains_triplet(output, trip)) {
    output = ([...output, trip] as List<dynamic>).map((e) => ((e as List<dynamic>).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList() as List<int>)).toList();
  };
  }
    seen = [...seen, other];
  }
  }
  return output;
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(find_triplets_with_0_sum([-1, 0, 1, 2, -1, -4])));
  print(_str(find_triplets_with_0_sum(<int>[])));
  print(_str(find_triplets_with_0_sum([0, 0, 0])));
  print(_str(find_triplets_with_0_sum([1, 2, 3, 0, -1, -2, -3])));
  print(_str(find_triplets_with_0_sum_hashing([-1, 0, 1, 2, -1, -4])));
  print(_str(find_triplets_with_0_sum_hashing(<int>[])));
  print(_str(find_triplets_with_0_sum_hashing([0, 0, 0])));
  print(_str(find_triplets_with_0_sum_hashing([1, 2, 3, 0, -1, -2, -3])));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
