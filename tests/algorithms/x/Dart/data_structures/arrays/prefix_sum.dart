// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

class PrefixSum {
  List<int> prefix_sum;
  PrefixSum({required this.prefix_sum});
}

PrefixSum make_prefix_sum(List<int> arr) {
  List<int> prefix = <int>[];
  int running = 0;
  int i = 0;
  while (i < arr.length) {
    running = running + arr[i];
    prefix = [...prefix, running];
    i = i + 1;
  }
  return PrefixSum(prefix_sum: prefix);
}

int get_sum(PrefixSum ps, int start, int end) {
  List<int> prefix = ps.prefix_sum;
  if (prefix.length == 0) {
    _error("The array is empty.");
  }
  if (start < 0 || end >= prefix.length || start > end) {
    _error("Invalid range specified.");
  }
  if (start == 0) {
    return prefix[end];
  }
  return prefix[end] - prefix[start - 1];
}

bool contains_sum(PrefixSum ps, int target_sum) {
  List<int> prefix = ps.prefix_sum;
  List<int> sums = [0];
  int i = 0;
  while (i < prefix.length) {
    int sum_item = prefix[i];
    int j = 0;
    while (j < sums.length) {
    if (sums[j] == sum_item - target_sum) {
    return true;
  }
    j = j + 1;
  }
    sums = [...sums, sum_item];
    i = i + 1;
  }
  return false;
}

PrefixSum ps = make_prefix_sum([1, 2, 3]);
PrefixSum ps2 = make_prefix_sum([1, -2, 3]);
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(get_sum(ps, 0, 2)));
  print(_str(get_sum(ps, 1, 2)));
  print(_str(get_sum(ps, 2, 2)));
  print(_str(contains_sum(ps, 6)));
  print(_str(contains_sum(ps, 5)));
  print(_str(contains_sum(ps, 3)));
  print(_str(contains_sum(ps, 4)));
  print(_str(contains_sum(ps, 7)));
  print(_str(contains_sum(ps2, 2)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
