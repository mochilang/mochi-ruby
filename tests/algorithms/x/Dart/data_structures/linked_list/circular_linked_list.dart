// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

class CircularLinkedList {
  List<int> data;
  CircularLinkedList({required this.data});
}

class DeleteResult {
  CircularLinkedList list;
  int value;
  DeleteResult({required this.list, required this.value});
}

CircularLinkedList empty_list() {
  return CircularLinkedList(data: []);
}

int length(CircularLinkedList list) {
  return list.data.length;
}

bool is_empty(CircularLinkedList list) {
  return list.data.length == 0;
}

String to_string(CircularLinkedList list) {
  if (list.data.length == 0) {
    return "";
  }
  String s = (list.data[0]).toString();
  int i = 1;
  while (i < list.data.length) {
    s = s + "->" + (list.data[i]).toString();
    i = i + 1;
  }
  return s;
}

CircularLinkedList insert_nth(CircularLinkedList list, int index, int value) {
  if (index < 0 || index > list.data.length) {
    throw Exception("index out of range");
  }
  List<int> res = <int>[];
  int i = 0;
  while (i < index) {
    res = [...res, list.data[i]];
    i = i + 1;
  }
  res = [...res, value];
  while (i < list.data.length) {
    res = [...res, list.data[i]];
    i = i + 1;
  }
  return CircularLinkedList(data: res);
}

CircularLinkedList insert_head(CircularLinkedList list, int value) {
  return insert_nth(list, 0, value);
}

CircularLinkedList insert_tail(CircularLinkedList list, int value) {
  return insert_nth(list, list.data.length, value);
}

DeleteResult delete_nth(CircularLinkedList list, int index) {
  if (index < 0 || index >= list.data.length) {
    throw Exception("index out of range");
  }
  List<int> res = <int>[];
  int i = 0;
  int val = 0;
  while (i < list.data.length) {
    if (i == index) {
    val = list.data[i];
  } else {
    res = [...res, list.data[i]];
  }
    i = i + 1;
  }
  return DeleteResult(list: CircularLinkedList(data: res), value: val);
}

DeleteResult delete_front(CircularLinkedList list) {
  return delete_nth(list, 0);
}

DeleteResult delete_tail(CircularLinkedList list) {
  return delete_nth(list, list.data.length - 1);
}

void _main() {
  CircularLinkedList cll = empty_list();
  int i = 0;
  while (i < 5) {
    cll = insert_tail(cll, i + 1);
    i = i + 1;
  }
  print(to_string(cll));
  cll = insert_tail(cll, 6);
  print(to_string(cll));
  cll = insert_head(cll, 0);
  print(to_string(cll));
  DeleteResult res = delete_front(cll);
  cll = res.list;
  print(res.value);
  res = delete_tail(cll);
  cll = res.list;
  print(res.value);
  res = delete_nth(cll, 2);
  cll = res.list;
  print(res.value);
  print(to_string(cll));
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
