// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();

class Job {
  int id;
  int deadline;
  int profit;
  Job({required this.id, required this.deadline, required this.profit});
}

List<Job> sort_jobs_by_profit(List<Job> jobs) {
  List<Job> js = jobs;
  int i = 0;
  while (i < js.length) {
    int j = 0;
    while (j < js.length - i - 1) {
    Job a = js[j];
    Job b = js[j + 1];
    if (a.profit < b.profit) {
    while (js.length <= j) { js.add(null as dynamic); } js[j] = b;
    while (js.length <= j + 1) { js.add(null as dynamic); } js[j + 1] = a;
  }
    j = j + 1;
  }
    i = i + 1;
  }
  return js;
}

int max_deadline(List<Job> jobs) {
  int max_d = 0;
  int i = 0;
  while (i < jobs.length) {
    Job job = jobs[i];
    int d = job.deadline;
    if (d > max_d) {
    max_d = d;
  }
    i = i + 1;
  }
  return max_d;
}

List<int> job_sequencing_with_deadlines(List<Job> jobs) {
  List<Job> js = sort_jobs_by_profit(jobs);
  int max_d = max_deadline(js);
  List<int> time_slots = <int>[];
  int t = 0;
  while (t < max_d) {
    time_slots = [...time_slots, 0 - 1];
    t = t + 1;
  }
  int count = 0;
  int max_profit = 0;
  int i = 0;
  while (i < js.length) {
    Job job = js[i];
    int j = job.deadline - 1;
    while (j >= 0) {
    if (time_slots[j] == 0 - 1) {
    while (time_slots.length <= j) { time_slots.add(0); } time_slots[j] = job.id;
    count = count + 1;
    max_profit = max_profit + job.profit;
    break;
  }
    j = j - 1;
  }
    i = i + 1;
  }
  List<int> result = <int>[];
  result = [...result, count];
  result = [...result, max_profit];
  return result;
}

List<Job> jobs1 = <Job>[];
List<Job> jobs2 = <Job>[];
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  jobs1 = [...jobs1, Job(id: 1, deadline: 4, profit: 20)];
  jobs1 = [...jobs1, Job(id: 2, deadline: 1, profit: 10)];
  jobs1 = [...jobs1, Job(id: 3, deadline: 1, profit: 40)];
  jobs1 = [...jobs1, Job(id: 4, deadline: 1, profit: 30)];
  print(_str(job_sequencing_with_deadlines(jobs1)));
  jobs2 = [...jobs2, Job(id: 1, deadline: 2, profit: 100)];
  jobs2 = [...jobs2, Job(id: 2, deadline: 1, profit: 19)];
  jobs2 = [...jobs2, Job(id: 3, deadline: 2, profit: 27)];
  jobs2 = [...jobs2, Job(id: 4, deadline: 1, profit: 25)];
  jobs2 = [...jobs2, Job(id: 5, deadline: 1, profit: 15)];
  print(_str(job_sequencing_with_deadlines(jobs2)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
