// Generated by Mochi transpiler
String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

String _str(dynamic v) { if (v is double && v == v.roundToDouble()) { return v.toInt().toString(); } return v.toString(); }

List<int> subarray(List<int> xs, int start, int end) {
  List<int> result = <int>[];
  int k = start;
  while (k < end) {
    result = (result..add(xs[k]));
    k = k + 1;
  }
  return result;
}

List<int> merge(List<int> left_half, List<int> right_half) {
  List<int> result = <int>[];
  int i = 0;
  int j = 0;
  while (i < left_half.length && j < right_half.length) {
    if (left_half[i] < right_half[j]) {
    result = (result..add(left_half[i]));
    i = i + 1;
  } else {
    result = (result..add(right_half[j]));
    j = j + 1;
  }
  }
  while (i < left_half.length) {
    result = (result..add(left_half[i]));
    i = i + 1;
  }
  while (j < right_half.length) {
    result = (result..add(right_half[j]));
    j = j + 1;
  }
  return result;
}

List<int> merge_sort(List<int> array) {
  if (array.length <= 1) {
    return array;
  }
  int middle = array.length ~/ 2;
  List<int> left_half = subarray(array, 0, middle);
  List<int> right_half = subarray(array, middle, array.length);
  List<int> sorted_left = merge_sort(left_half);
  List<int> sorted_right = merge_sort(right_half);
  return merge(sorted_left, sorted_right);
}

int h_index(List<int> citations) {
  int idx = 0;
  while (idx < citations.length) {
    if (citations[idx] < 0) {
    throw Exception("The citations should be a list of non negative integers.");
  }
    idx = idx + 1;
  }
  List<int> sorted = merge_sort(citations);
  int n = sorted.length;
  int i = 0;
  while (i < n) {
    if (sorted[n - 1 - i] <= i) {
    return i;
  }
    i = i + 1;
  }
  return n;
}

void main() {
  print(_str(h_index([3, 0, 6, 1, 5])));
  print(_str(h_index([1, 3, 1])));
  print(_str(h_index([1, 2, 3])));
}
