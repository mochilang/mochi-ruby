// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

Map<String, String> OPEN_TO_CLOSED = {"(": ")", "[": "]", "{": "}"};
List<String> slice_without_last(List<String> xs) {
  List<String> res = <String>[];
  int i = 0;
  while (i < xs.length - 1) {
    res = [...res, xs[i]];
    i = i + 1;
  }
  return res;
}

bool is_balanced(String s) {
  List<String> stack = <String>[];
  int i = 0;
  while (i < s.length) {
    String symbol = _substr(s, i, i + 1);
    if (OPEN_TO_CLOSED.containsKey(symbol)) {
    stack = [...stack, symbol];
  } else {
    if (symbol == ")" || symbol == "]" || symbol == "}") {
    if (stack.length == 0) {
    return false;
  };
    String top = stack[stack.length - 1];
    if ((OPEN_TO_CLOSED[top] ?? "") != symbol) {
    return false;
  };
    stack = slice_without_last(stack);
  };
  }
    i = i + 1;
  }
  return stack.length == 0;
}

void _main() {
  print(is_balanced(""));
  print(is_balanced("()"));
  print(is_balanced("[]"));
  print(is_balanced("{}"));
  print(is_balanced("()[]{}"));
  print(is_balanced("(())"));
  print(is_balanced("[["));
  print(is_balanced("([{}])"));
  print(is_balanced("(()[)]"));
  print(is_balanced("([)]"));
  print(is_balanced("[[()]]"));
  print(is_balanced("(()(()))"));
  print(is_balanced("]"));
  print(is_balanced("Life is a bowl of cherries."));
  print(is_balanced("Life is a bowl of che{}ies."));
  print(is_balanced("Life is a bowl of che}{ies."));
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
