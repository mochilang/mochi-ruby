// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();

int round_to_int(double x) {
  if (x >= 0.0) {
    return (x + 0.5).toInt();
  }
  return (x - 0.5).toInt();
}

int molarity_to_normality(double nfactor, double moles, double volume) {
  return round_to_int(moles / volume * nfactor);
}

int moles_to_pressure(double volume, double moles, double temperature) {
  return round_to_int(moles * 0.0821 * temperature / volume);
}

int moles_to_volume(double pressure, double moles, double temperature) {
  return round_to_int(moles * 0.0821 * temperature / pressure);
}

int pressure_and_volume_to_temperature(double pressure, double moles, double volume) {
  return round_to_int(pressure * volume / (0.0821 * moles));
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(molarity_to_normality(2.0, 3.1, 0.31)));
  print(_str(molarity_to_normality(4.0, 11.4, 5.7)));
  print(_str(moles_to_pressure(0.82, 3.0, 300.0)));
  print(_str(moles_to_pressure(8.2, 5.0, 200.0)));
  print(_str(moles_to_volume(0.82, 3.0, 300.0)));
  print(_str(moles_to_volume(8.2, 5.0, 200.0)));
  print(_str(pressure_and_volume_to_temperature(0.82, 1.0, 2.0)));
  print(_str(pressure_and_volume_to_temperature(8.2, 5.0, 3.0)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
