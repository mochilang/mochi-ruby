// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _str(dynamic v) => v.toString();


Never _error(dynamic msg) {
  throw Exception(msg.toString());
}

List<String> units = ["km/h", "m/s", "mph", "knot"];
List<double> speed_chart = [1.0, 3.6, 1.609344, 1.852];
List<double> speed_chart_inverse = [1.0, 0.277777778, 0.621371192, 0.539956803];
int index_of(List<String> arr, String value) {
  int i = 0;
  while (i < arr.length) {
    if (arr[i] == value) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

String units_string(List<String> arr) {
  String s = "";
  int i = 0;
  while (i < arr.length) {
    if (i > 0) {
    s = s + ", ";
  }
    s = s + arr[i];
    i = i + 1;
  }
  return s;
}

double round3(double x) {
  double y = x * 1000.0 + 0.5;
  int z = (y).toInt();
  double zf = z.toDouble();
  return zf / 1000.0;
}

double convert_speed(double speed, String unit_from, String unit_to) {
  int from_index = index_of(units, unit_from);
  int to_index = index_of(units, unit_to);
  if (from_index < 0 || to_index < 0) {
    String msg = "Incorrect 'from_type' or 'to_type' value: " + unit_from + ", " + unit_to + "\nValid values are: " + units_string(units);
    _error(msg);
  }
  double result = speed * speed_chart[from_index] * speed_chart_inverse[to_index];
  double r = round3(result);
  return r;
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(convert_speed(100.0, "km/h", "m/s")));
  print(_str(convert_speed(100.0, "km/h", "mph")));
  print(_str(convert_speed(100.0, "km/h", "knot")));
  print(_str(convert_speed(100.0, "m/s", "km/h")));
  print(_str(convert_speed(100.0, "m/s", "mph")));
  print(_str(convert_speed(100.0, "m/s", "knot")));
  print(_str(convert_speed(100.0, "mph", "km/h")));
  print(_str(convert_speed(100.0, "mph", "m/s")));
  print(_str(convert_speed(100.0, "mph", "knot")));
  print(_str(convert_speed(100.0, "knot", "km/h")));
  print(_str(convert_speed(100.0, "knot", "m/s")));
  print(_str(convert_speed(100.0, "knot", "mph")));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
