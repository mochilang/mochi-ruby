// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String lower = "abcdefghijklmnopqrstuvwxyz";
String upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
int index_of(String s, String ch) {
  int i = 0;
  while (i < s.length) {
    if (_substr(s, i, i + 1) == ch) {
    return i;
  }
    i = i + 1;
  }
  return -1;
}

String to_title_case(String word) {
  if (word.length == 0) {
    return "";
  }
  String first = _substr(word, 0, 1);
  int idx = index_of(lower, first);
  String result = (idx >= 0 ? _substr(upper, idx, idx + 1) : first);
  int i = 1;
  while (i < word.length) {
    String ch = _substr(word, i, i + 1);
    int uidx = index_of(upper, ch);
    if (uidx >= 0) {
    result = result + _substr(lower, uidx, uidx + 1);
  } else {
    result = result + ch;
  }
    i = i + 1;
  }
  return result;
}

List<String> split_words(String s) {
  List<String> words = <String>[];
  String current = "";
  int i = 0;
  while (i < s.length) {
    String ch = _substr(s, i, i + 1);
    if (ch == " ") {
    if (current.length > 0) {
    words = [...words, current];
    current = "";
  };
  } else {
    current = current + ch;
  }
    i = i + 1;
  }
  if (current.length > 0) {
    words = [...words, current];
  }
  return words;
}

String sentence_to_title_case(String sentence) {
  List<String> words = split_words(sentence);
  String res = "";
  int i = 0;
  while (i < words.length) {
    res = res + to_title_case(words[i]);
    if (i + 1 < words.length) {
    res = res + " ";
  }
    i = i + 1;
  }
  return res;
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(to_title_case("Aakash"));
  print(to_title_case("aakash"));
  print(to_title_case("AAKASH"));
  print(to_title_case("aAkAsH"));
  print(sentence_to_title_case("Aakash Giri"));
  print(sentence_to_title_case("aakash giri"));
  print(sentence_to_title_case("AAKASH GIRI"));
  print(sentence_to_title_case("aAkAsH gIrI"));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
