// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

bool validate_initial_digits(String cc) {
  return _substr(cc, 0, 2) == "34" || _substr(cc, 0, 2) == "35" || _substr(cc, 0, 2) == "37" || _substr(cc, 0, 1) == "4" || _substr(cc, 0, 1) == "5" || _substr(cc, 0, 1) == "6";
}

bool luhn_validation(String cc) {
  int sum = 0;
  bool double_digit = false;
  int i = cc.length - 1;
  while (i >= 0) {
    int n = int.parse(_substr(cc, i, i + 1));
    if (double_digit) {
    n = n * 2;
    if (n > 9) {
    n = n - 9;
  };
  }
    sum = sum + n;
    double_digit = !double_digit;
    i = i - 1;
  }
  return sum % 10 == 0;
}

bool is_digit_string(String s) {
  int i = 0;
  while (i < s.length) {
    String c = _substr(s, i, i + 1);
    if (c.compareTo("0") < 0 || c.compareTo("9") > 0) {
    return false;
  }
    i = i + 1;
  }
  return true;
}

bool validate_credit_card_number(String cc) {
  String error_message = cc + " is an invalid credit card number because";
  if (!is_digit_string(cc)) {
    print(error_message + " it has nonnumerical characters.");
    return false;
  }
  if (!(cc.length >= 13 && cc.length <= 16)) {
    print(error_message + " of its length.");
    return false;
  }
  if (!validate_initial_digits(cc)) {
    print(error_message + " of its first two digits.");
    return false;
  }
  if (!luhn_validation(cc)) {
    print(error_message + " it fails the Luhn check.");
    return false;
  }
  print(cc + " is a valid credit card number.");
  return true;
}

void _main() {
  validate_credit_card_number("4111111111111111");
  validate_credit_card_number("32323");
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
