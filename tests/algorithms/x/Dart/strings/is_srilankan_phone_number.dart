// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();

bool starts_with(String s, String prefix) {
  if (s.length < prefix.length) {
    return false;
  }
  return _substr(s, 0, prefix.length) == prefix;
}

bool all_digits(String s) {
  int i = 0;
  while (i < s.length) {
    String c = _substr(s, i, i + 1);
    if (c.compareTo("0") < 0 || c.compareTo("9") > 0) {
    return false;
  }
    i = i + 1;
  }
  return true;
}

bool is_sri_lankan_phone_number(String phone) {
  String p = phone;
  if (starts_with(p, "+94")) {
    p = _substr(p, 3, p.length);
  } else {
    if (starts_with(p, "0094")) {
    p = _substr(p, 4, p.length);
  } else {
    if (starts_with(p, "94")) {
    p = _substr(p, 2, p.length);
  } else {
    if (starts_with(p, "0")) {
    p = _substr(p, 1, p.length);
  } else {
    return false;
  };
  };
  };
  }
  if (p.length != 9 && p.length != 10) {
    return false;
  }
  if (_substr(p, 0, 0 + 1) != "7") {
    return false;
  }
  String second = _substr(p, 1, 1 + 1);
  List<String> allowed = ["0", "1", "2", "4", "5", "6", "7", "8"];
  if (!allowed.contains(second)) {
    return false;
  }
  int idx = 2;
  if (p.length == 10) {
    String sep = _substr(p, 2, 2 + 1);
    if (sep != "-" && sep != " ") {
    return false;
  };
    idx = 3;
  }
  if (p.length - idx != 7) {
    return false;
  }
  String rest = _substr(p, idx, p.length);
  return all_digits(rest);
}

String phone = "0094702343221";
void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(is_sri_lankan_phone_number(phone)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
