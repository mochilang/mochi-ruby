// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();

bool all_digits(String s) {
  if (s.length == 0) {
    return false;
  }
  int i = 0;
  while (i < s.length) {
    String c = _substr(s, i, i + 1);
    if (c.compareTo("0") < 0 || c.compareTo("9") > 0) {
    return false;
  }
    i = i + 1;
  }
  return true;
}

bool indian_phone_validator(String phone) {
  String s = phone;
  if (s.length >= 3 && _substr(s, 0, 3) == "+91") {
    s = _substr(s, 3, s.length);
    if (s.length > 0) {
    String c = _substr(s, 0, 0 + 1);
    if (c == "-" || c == " ") {
    s = _substr(s, 1, s.length);
  };
  };
  }
  if (s.length > 0 && _substr(s, 0, 0 + 1) == "0") {
    s = _substr(s, 1, s.length);
  }
  if (s.length >= 2 && _substr(s, 0, 2) == "91") {
    s = _substr(s, 2, s.length);
  }
  if (s.length != 10) {
    return false;
  }
  String first = _substr(s, 0, 0 + 1);
  if (!(first == "7" || first == "8" || first == "9")) {
    return false;
  }
  if (!all_digits(s)) {
    return false;
  }
  return true;
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(indian_phone_validator("+91123456789")));
  print(_str(indian_phone_validator("+919876543210")));
  print(_str(indian_phone_validator("01234567896")));
  print(_str(indian_phone_validator("919876543218")));
  print(_str(indian_phone_validator("+91-1234567899")));
  print(_str(indian_phone_validator("+91-9876543218")));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
