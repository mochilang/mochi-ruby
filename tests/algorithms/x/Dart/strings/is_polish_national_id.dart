// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();

int parse_int(String s) {
  int value = 0;
  int i = 0;
  while (i < s.length) {
    String c = _substr(s, i, i + 1);
    value = value * 10 + (int.parse(c));
    i = i + 1;
  }
  return value;
}

bool is_polish_national_id(String id) {
  if (id.length == 0) {
    return false;
  }
  if (_substr(id, 0, 1) == "-") {
    return false;
  }
  int input_int = parse_int(id);
  if (input_int < 10100000 || input_int > 99923199999) {
    return false;
  }
  int month = parse_int(_substr(id, 2, 4));
  if (!(month >= 1 && month <= 12 || month >= 21 && month <= 32 || month >= 41 && month <= 52 || month >= 61 && month <= 72 || month >= 81 && month <= 92)) {
    return false;
  }
  int day = parse_int(_substr(id, 4, 6));
  if (day < 1 || day > 31) {
    return false;
  }
  List<int> multipliers = [1, 3, 7, 9, 1, 3, 7, 9, 1, 3];
  int subtotal = 0;
  int i = 0;
  while (i < multipliers.length) {
    int digit = parse_int(_substr(id, i, i + 1));
    subtotal = subtotal + digit * multipliers[i] % 10;
    i = i + 1;
  }
  int checksum = 10 - subtotal % 10;
  return checksum == input_int % 10;
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(is_polish_national_id("02070803628")));
  print(_str(is_polish_national_id("02150803629")));
  print(_str(is_polish_national_id("02075503622")));
  print(_str(is_polish_national_id("-99012212349")));
  print(_str(is_polish_national_id("990122123499999")));
  print(_str(is_polish_national_id("02070803621")));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
