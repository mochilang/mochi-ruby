// Generated by Mochi transpiler
dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

int gcd(int a, int b) {
  int x = a;
  int y = b;
  while (y != 0) {
    int t = x % y;
    x = y;
    y = t;
  }
  if (x < 0) {
    return -x;
  }
  return x;
}

int pow_mod(int base, int exp, int mod) {
  int result = 1;
  int b = base % mod;
  int e = exp;
  while (e > 0) {
    if (e % 2 == 1) {
    result = result * b % mod;
  }
    e = e ~/ 2;
    b = b * b % mod;
  }
  return result;
}

List<int> rsa_factor(int d, int e, int n) {
  int k = d * e - 1;
  int p = 0;
  int q = 0;
  int g = 2;
  while (p == 0 && g < n) {
    int t = k;
    while (t % 2 == 0) {
    t = t ~/ 2;
    int x = pow_mod(g, t, n);
    int y = gcd(x - 1, n);
    if (x > 1 && y > 1) {
    p = y;
    q = n ~/ y;
    break;
  }
  }
    g = g + 1;
  }
  if (p > q) {
    return [q, p];
  }
  return [p, q];
}

void main() {
  print("[" + rsa_factor(3, 16971, 25777).join(', ') + "]");
  print("[" + rsa_factor(7331, 11, 27233).join(', ') + "]");
  print("[" + rsa_factor(4021, 13, 17711).join(', ') + "]");
}
