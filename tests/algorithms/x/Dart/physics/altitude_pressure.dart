// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

String _str(dynamic v) => v.toString();


Never _error(String msg) {
  throw Exception(msg);
}

double to_float(int x) {
  return x * 1.0;
}

double ln(double x) {
  if (x <= 0.0) {
    _error("ln domain error");
  }
  double y = (x - 1.0) / (x + 1.0);
  double y2 = y * y;
  double term = y;
  double sum = 0.0;
  int k = 0;
  while (k < 10) {
    double denom = (2 * k + 1).toDouble();
    sum = sum + term / denom;
    term = term * y2;
    k = k + 1;
  }
  return 2.0 * sum;
}

double exp(double x) {
  double term = 1.0;
  double sum = 1.0;
  int n = 1;
  while (n < 20) {
    term = term * x / n.toDouble();
    sum = sum + term;
    n = n + 1;
  }
  return sum;
}

double pow_float(double base, double exponent) {
  return exp(exponent * ln(base));
}

double get_altitude_at_pressure(double pressure) {
  if (pressure > 101325.0) {
    _error("Value Higher than Pressure at Sea Level !");
  }
  if (pressure < 0.0) {
    _error("Atmospheric Pressure can not be negative !");
  }
  double ratio = pressure / 101325.0;
  return 44330.0 * (1.0 - pow_float(ratio, 1.0 / 5.5255));
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(get_altitude_at_pressure(100000.0)));
  print(_str(get_altitude_at_pressure(101325.0)));
  print(_str(get_altitude_at_pressure(80000.0)));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
