// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, num start, num end) {
  var n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  return s.substring(s0, e0);
}

String _str(dynamic v) { if (v is double && v == v.roundToDouble()) { return v.toInt().toString(); } return v.toString(); }

class Node {
  int value;
  int left;
  int right;
  Node({required this.value, required this.left, required this.right});
}

class TreeState {
  List<Node> nodes;
  int root;
  TreeState({required this.nodes, required this.root});
}

int new_node(TreeState state, int value) {
  state.nodes = [...state.nodes, Node(value: value, left: -1, right: -1)];
  return state.nodes.length - 1;
}

void insert(TreeState state, int value) {
  if (state.root == -1) {
    state.root = new_node(state, value);
    return;
  }
  int current = state.root;
  List<Node> nodes = state.nodes;
  while (true) {
    Node node = nodes[current];
    if (value < node.value) {
    if (node.left == -1) {
    int idx = new_node(state, value);
    nodes = state.nodes;
    node.left = idx;
    while (nodes.length <= current) { nodes.add(null as dynamic); } nodes[current] = node;
    state.nodes = nodes;
    return;
  };
    current = node.left;
  } else {
    if (value > node.value) {
    if (node.right == -1) {
    int idx = new_node(state, value);
    nodes = state.nodes;
    node.right = idx;
    while (nodes.length <= current) { nodes.add(null as dynamic); } nodes[current] = node;
    state.nodes = nodes;
    return;
  };
    current = node.right;
  } else {
    return;
  };
  }
  }
}

List<int> inorder(TreeState state, int idx) {
  if (idx == -1) {
    return ([] as List).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
  }
  Node node = state.nodes[idx];
  List<int> result = inorder(state, node.left);
  result = [...result, node.value];
  List<int> right_part = inorder(state, node.right);
  int i = 0;
  while (i < right_part.length) {
    result = [...result, right_part[i]];
    i = i + 1;
  }
  return result;
}

List<int> tree_sort(List<int> arr) {
  TreeState state = TreeState(nodes: [], root: -1);
  int i = 0;
  while (i < arr.length) {
    insert(state, arr[i]);
    i = i + 1;
  }
  if (state.root == -1) {
    return ([] as List).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
  }
  return inorder(state, state.root);
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(_str(tree_sort(<int>[])));
  print(_str(tree_sort([1])));
  print(_str(tree_sort([1, 2])));
  print(_str(tree_sort([5, 2, 7])));
  print(_str(tree_sort([5, -4, 9, 2, 7])));
  print(_str(tree_sort([5, 6, 1, -1, 4, 37, 2, 7])));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
