// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

dynamic _substr(dynamic s, num start, num end) {
  int n = s.length;
  int s0 = start.toInt();
  int e0 = end.toInt();
  if (s0 < 0) s0 += n;
  if (e0 < 0) e0 += n;
  if (s0 < 0) s0 = 0;
  if (s0 > n) s0 = n;
  if (e0 < 0) e0 = 0;
  if (e0 > n) e0 = n;
  if (s0 > e0) s0 = e0;
  if (s is String) {
    return s.substring(s0, e0);
  }
  return s.sublist(s0, e0);
}

List<int> quick_sort(List<int> items) {
  if (items.length < 2) {
    return items;
  }
  int pivot = items[0];
  List<int> lesser = <int>[];
  List<int> greater = <int>[];
  int i = 1;
  while (i < items.length) {
    int item = items[i];
    if (item <= pivot) {
    lesser = (lesser..add(item));
  } else {
    greater = (greater..add(item));
  }
    i = i + 1;
  }
  return ([...[...quick_sort(lesser), ...[pivot]], ...quick_sort(greater)] as List).map((e) => (e is BigInt ? e.toInt() : (e as int))).toList();
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  print(["sorted1:", "[" + quick_sort([0, 5, 3, 2, 2]).join(', ') + "]"].join(" "));
  print(["sorted2:", "[" + quick_sort(<int>[]).join(', ') + "]"].join(" "));
  print(["sorted3:", "[" + quick_sort([-2, 5, 0, -45]).join(', ') + "]"].join(" "));
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
