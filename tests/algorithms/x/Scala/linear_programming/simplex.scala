// Generated by Mochi v0.10.66 on 2025-08-16 12:13:59 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => java.math.BigDecimal.valueOf(d).stripTrailingZeros.toPlainString
  case other => String.valueOf(other)
}

def pivot(t: ArrayBuffer[ArrayBuffer[Double]], row: BigInt, col: BigInt): ArrayBuffer[ArrayBuffer[Double]] = {
  var pivotRow: ArrayBuffer[Double] = ArrayBuffer[Double]()
  var pivotVal: Double = t.lift((row.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((col.toInt).toInt).getOrElse(0.0)
  for (j <- (BigInt(0)).toInt until (BigInt((t.lift((row.toInt).toInt).getOrElse(ArrayBuffer[Double]())).size)).toInt) {
    pivotRow = (pivotRow :+ t.lift((row.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((j.toInt).toInt).getOrElse(0.0) / pivotVal)
  }
  t((row).toInt) = pivotRow
  for (i <- (BigInt(0)).toInt until (BigInt((t).size)).toInt) {
    if (i != row) {
      var factor: Double = t.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((col.toInt).toInt).getOrElse(0.0)
      var newRow: ArrayBuffer[Double] = ArrayBuffer[Double]()
      for (j <- (BigInt(0)).toInt until (BigInt((t.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]())).size)).toInt) {
        var value: Double = t.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((j.toInt).toInt).getOrElse(0.0) - factor * pivotRow.lift((j.toInt).toInt).getOrElse(0.0)
        newRow = (newRow :+ value)
      }
      t((i).toInt) = newRow
    }
  }
  return t
}

def findPivot(t: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[BigInt] = {
  var col: BigInt = BigInt(0)
  var minVal: Double = 0.0
  for (j <- (BigInt(0)).toInt until (BigInt((t.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]())).size) - BigInt(1)).toInt) {
    var v: Double = t.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((j.toInt).toInt).getOrElse(0.0)
    if (v < minVal) {
      minVal = v
      col = BigInt(j.toString.toDouble.toInt)
    }
  }
  if (minVal >= 0.0) {
    return ArrayBuffer(BigInt((-BigInt(1)).toString.toDouble.toInt), BigInt((-BigInt(1)).toString.toDouble.toInt))
  }
  var row: BigInt = -BigInt(1)
  var minRatio: Double = 0.0
  var first: Boolean = true
  for (i <- (BigInt(1)).toInt until (BigInt((t).size)).toInt) {
    var coeff: Double = t.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((col.toInt).toInt).getOrElse(0.0)
    if (coeff > 0.0) {
      var rhs: Double = t.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift(((BigInt((t.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]())).size) - BigInt(1)).toInt).toInt).getOrElse(0.0)
      var ratio: Double = rhs / coeff
      if ((first || ratio < minRatio)) {
        minRatio = ratio
        row = BigInt(i.toString.toDouble.toInt)
        first = false
      }
    }
  }
  return ArrayBuffer(row, col)
}

def interpret(t: ArrayBuffer[ArrayBuffer[Double]], nVars: BigInt): scala.collection.mutable.Map[String,Double] = {
  var lastCol: BigInt = BigInt((t.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]())).size) - BigInt(1)
  var p: Double = t.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((lastCol.toInt).toInt).getOrElse(0.0)
  if (p < 0.0) {
    p = -p
  }
  var result: scala.collection.mutable.Map[String,Double] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  result.update("P", p)
  for (i <- (BigInt(0)).toInt until (nVars).toInt) {
    var nzRow: BigInt = -BigInt(1)
    var nzCount: BigInt = BigInt(0)
    for (r <- (BigInt(0)).toInt until (BigInt((t).size)).toInt) {
      var `val`: Double = t.lift((r.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((i.toInt).toInt).getOrElse(0.0)
      if (`val` != 0.0) {
        nzCount = nzCount + BigInt(1)
        nzRow = BigInt(r.toString.toDouble.toInt)
      }
    }
    if ((nzCount == BigInt(1) && t.lift((nzRow.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((i.toInt).toInt).getOrElse(0.0) == 1.0)) {
      result.update("x" + _str(i + BigInt(1)), t.lift((nzRow.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((lastCol.toInt).toInt).getOrElse(0.0))
    }
  }
  return result
}

def simplex(tab: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[ArrayBuffer[Double]] = {
  var t: ArrayBuffer[ArrayBuffer[Double]] = tab
  val _br7 = new Breaks
  _br7.breakable {
    while (true) {
      var p: ArrayBuffer[BigInt] = findPivot(t)
      var row: BigInt = p.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0))
      var col: BigInt = p.lift((BigInt(1).toInt).toInt).getOrElse(BigInt(0))
      if (row < BigInt(0)) {
        _br7.break()
      }
      t = pivot(t, row, col)
    }
  }
  return t
}

var tableau: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(-1.0, -1.0, 0.0, 0.0, 0.0), ArrayBuffer(1.0, 3.0, 1.0, 0.0, 4.0), ArrayBuffer(3.0, 1.0, 0.0, 1.0, 4.0))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    var finalTab: ArrayBuffer[ArrayBuffer[Double]] = simplex(tableau)
    var res: scala.collection.mutable.Map[String,Double] = interpret(finalTab, BigInt(2))
    println(_str("P: " + _str(res.getOrElse("P", 0.0).asInstanceOf[Double])))
    for (i <- (BigInt(0)).toInt until (BigInt(2)).toInt) {
      var key: String = "x" + _str(i + BigInt(1))
      if ((res.contains(key))) {
        println(_str(key + ": " + _str(res.getOrElse(key.toString, 0.0).asInstanceOf[Double])))
      }
    }
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
