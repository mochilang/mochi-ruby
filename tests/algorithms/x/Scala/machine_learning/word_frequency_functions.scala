// Generated by Mochi v0.10.67 on 2025-08-16 21:32:14 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val s = java.lang.Double.toString(d); if (s.indexOf('.') < 0 && !s.contains("e") && !s.contains("E")) s + ".0" else s }
  case other => String.valueOf(other)
}

def to_lowercase(s: String): String = {
  var res: String = ""
  var i: BigInt = BigInt(0)
  val _br0 = new Breaks
  _br0.breakable {
    while (i < BigInt((s).size)) {
      var c: String = s.slice((i.toInt).toInt, (i.toInt).toInt + 1)
      var j: BigInt = BigInt(0)
      var found: Boolean = false
      val _br1 = new Breaks
      _br1.breakable {
        while (j < BigInt((UPPER).length)) {
          if (c == UPPER.slice((j.toInt).toInt, (j.toInt).toInt + 1)) {
            res = res + LOWER.slice((j.toInt).toInt, (j.toInt).toInt + 1)
            found = true
            _br1.break()
          }
          j = j + BigInt(1)
        }
      }
      if (!found) {
        res = res + c
      }
      i = i + BigInt(1)
    }
  }
  return res
}

def is_punct(c: String): Boolean = {
  var i: BigInt = BigInt(0)
  while (i < BigInt((PUNCT).length)) {
    if (c == PUNCT.slice((i.toInt).toInt, (i.toInt).toInt + 1)) {
      return true
    }
    i = i + BigInt(1)
  }
  return false
}

def clean_text(text: String, keep_newlines: Boolean): String = {
  var lower: String = to_lowercase(text)
  var res: String = ""
  var i: BigInt = BigInt(0)
  while (i < BigInt((lower).size)) {
    var ch: String = lower.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    if (is_punct(ch)) {
    } else {
      if (ch == "\n") {
        if (keep_newlines) {
          res = res + "\n"
        }
      } else {
        res = res + ch
      }
    }
    i = i + BigInt(1)
  }
  return res
}

def split(s: String, sep: String): ArrayBuffer[String] = {
  var res: ArrayBuffer[String] = ArrayBuffer[String]()
  var current: String = ""
  var i: BigInt = BigInt(0)
  while (i < BigInt((s).size)) {
    var ch: String = s.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    if (ch == sep) {
      res = (res :+ current)
      current = ""
    } else {
      current = current + ch
    }
    i = i + BigInt(1)
  }
  res = (res :+ current)
  return res
}

def contains(s: String, sub: String): Boolean = {
  var n: BigInt = BigInt((s).size)
  var m: BigInt = BigInt((sub).size)
  if (m == BigInt(0)) {
    return true
  }
  var i: BigInt = BigInt(0)
  val _br5 = new Breaks
  _br5.breakable {
    while (i <= n - m) {
      var j: BigInt = BigInt(0)
      var is_match: Boolean = true
      val _br6 = new Breaks
      _br6.breakable {
        while (j < m) {
          if (s.slice(((i + j).toInt).toInt, ((i + j).toInt).toInt + 1) != sub.slice((j.toInt).toInt, (j.toInt).toInt + 1)) {
            is_match = false
            _br6.break()
          }
          j = j + BigInt(1)
        }
      }
      if (is_match) {
        return true
      }
      i = i + BigInt(1)
    }
  }
  return false
}

def floor(x: Double): Double = {
  var i: BigInt = BigInt(x.toString.toDouble.toInt)
  if ((i.toString.toDouble) > x) {
    i = i - BigInt(1)
  }
  return i.toString.toDouble
}

def round3(x: Double): Double = {
  return Math.floor(x * 1000.0 + 0.5) / 1000.0
}

def ln(x: Double): Double = {
  var t: Double = (x - 1.0) / (x + 1.0)
  var term: Double = t
  var sum: Double = 0.0
  var k: BigInt = BigInt(1)
  while (k <= BigInt(99)) {
    sum = sum + term / k.toString.toDouble
    term = term * t * t
    k = k + BigInt(2)
  }
  return 2.0 * sum
}

def log10(x: Double): Double = {
  return math.log(x) / math.log(10.0)
}

def term_frequency(term: String, document: String): BigInt = {
  var clean: String = clean_text(document, false)
  var tokens: ArrayBuffer[String] = ArrayBuffer(clean.split(" "): _*)
  var t: String = to_lowercase(term)
  var count: BigInt = BigInt(0)
  var i: BigInt = BigInt(0)
  while (i < BigInt((tokens).size)) {
    if ((tokens.lift((i.toInt).toInt).getOrElse("") != "" && tokens.lift((i.toInt).toInt).getOrElse("") == t)) {
      count = count + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return count
}

def document_frequency(term: String, corpus: String): ArrayBuffer[BigInt] = {
  var clean: String = clean_text(corpus, true)
  var docs: ArrayBuffer[String] = ArrayBuffer(clean.split("\n"): _*)
  var t: String = to_lowercase(term)
  var matches: BigInt = BigInt(0)
  var i: BigInt = BigInt(0)
  while (i < BigInt((docs).size)) {
    if ((docs.lift((i.toInt).toInt).getOrElse("").contains(t))) {
      matches = matches + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return ArrayBuffer(matches, BigInt((docs).size))
}

def inverse_document_frequency(df: BigInt, n: BigInt, smoothing: Boolean): Double = {
  if (smoothing) {
    if (n == BigInt(0)) {
      throw new RuntimeException(String.valueOf("log10(0) is undefined."))
    }
    var ratio: Double = n.toString.toDouble / (1.0 + df.toString.toDouble)
    var l: Double = log10(ratio)
    var result: Double = round3(1.0 + l)
    println(_str(result))
    return result
  }
  if (df == BigInt(0)) {
    throw new RuntimeException(String.valueOf("df must be > 0"))
  }
  if (n == BigInt(0)) {
    throw new RuntimeException(String.valueOf("log10(0) is undefined."))
  }
  var ratio: Double = n.toString.toDouble / df.toString.toDouble
  var l: Double = log10(ratio)
  var result: Double = round3(l)
  println(_str(result))
  return result
}

def tf_idf(tf: BigInt, idf: Double): Double = {
  var prod: Double = tf.toString.toDouble * idf
  var result: Double = round3(prod)
  println(_str(result))
  return result
}

var LOWER: String = "abcdefghijklmnopqrstuvwxyz"

var UPPER: String = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"

var PUNCT: String = "!\"#$%&'()*+,-./:;<=>?@[\\]^_{|}~"

var corpus: String = "This is the first document in the corpus.\nThIs is the second document in the corpus.\nTHIS is the third document in the corpus."

var idf_val: Double = inverse_document_frequency(BigInt(1), BigInt(3), false)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(term_frequency("to", "To be, or not to be")))
    println(_str(_str(document_frequency("first", corpus))))
    tf_idf(BigInt(2), idf_val)
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
