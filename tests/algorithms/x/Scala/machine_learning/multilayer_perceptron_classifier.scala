// Generated by Mochi v0.10.67 on 2025-08-16 21:31:06 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val s = java.lang.Double.toString(d); if (s.indexOf('.') < 0 && !s.contains("e") && !s.contains("E")) s + ".0" else s }
  case other => String.valueOf(other)
}

def exp_taylor(x: Double): Double = {
  var term: Double = 1.0
  var sum: Double = 1.0
  var i: Double = 1.0
  while (i < 20.0) {
    term = term * x / i
    sum = sum + term
    i = i + 1.0
  }
  return sum
}

def sigmoid(x: Double): Double = {
  return 1.0 / (1.0 + exp_taylor(-x))
}

def train(epochs: BigInt, lr: Double): Any = {
  var e: BigInt = BigInt(0)
  while (e < epochs) {
    var i: BigInt = BigInt(0)
    while (i < BigInt((X).size)) {
      var x0: Double = X.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0)
      var x1: Double = X.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0)
      var target: Double = Y.lift((i.toInt).toInt).getOrElse(0.0)
      var z1: Double = w1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) * x0 + w1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) * x1 + b1.lift((BigInt(0).toInt).toInt).getOrElse(0.0)
      var z2: Double = w1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) * x0 + w1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) * x1 + b1.lift((BigInt(1).toInt).toInt).getOrElse(0.0)
      var h1: Double = sigmoid(z1)
      var h2: Double = sigmoid(z2)
      var z3: Double = w2.lift((BigInt(0).toInt).toInt).getOrElse(0.0) * h1 + w2.lift((BigInt(1).toInt).toInt).getOrElse(0.0) * h2 + b2
      var out: Double = sigmoid(z3)
      var error: Double = out - target
      var d1: Double = h1 * (1.0 - h1) * w2.lift((BigInt(0).toInt).toInt).getOrElse(0.0) * error
      var d2: Double = h2 * (1.0 - h2) * w2.lift((BigInt(1).toInt).toInt).getOrElse(0.0) * error
      w2((BigInt(0)).toInt) = w2.lift((BigInt(0).toInt).toInt).getOrElse(0.0) - lr * error * h1
      w2((BigInt(1)).toInt) = w2.lift((BigInt(1).toInt).toInt).getOrElse(0.0) - lr * error * h2
      b2 = b2 - lr * error
      w1((BigInt(0)).toInt)((BigInt(0)).toInt) = w1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) - lr * d1 * x0
      w1((BigInt(1)).toInt)((BigInt(0)).toInt) = w1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) - lr * d1 * x1
      b1((BigInt(0)).toInt) = b1.lift((BigInt(0).toInt).toInt).getOrElse(0.0) - lr * d1
      w1((BigInt(0)).toInt)((BigInt(1)).toInt) = w1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) - lr * d2 * x0
      w1((BigInt(1)).toInt)((BigInt(1)).toInt) = w1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) - lr * d2 * x1
      b1((BigInt(1)).toInt) = b1.lift((BigInt(1).toInt).toInt).getOrElse(0.0) - lr * d2
      i = i + BigInt(1)
    }
    e = e + BigInt(1)
  }
}

def predict(samples: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[BigInt] = {
  var preds: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((samples).size)) {
    var x0: Double = samples.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0)
    var x1: Double = samples.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0)
    var z1: Double = w1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) * x0 + w1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) * x1 + b1.lift((BigInt(0).toInt).toInt).getOrElse(0.0)
    var z2: Double = w1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) * x0 + w1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) * x1 + b1.lift((BigInt(1).toInt).toInt).getOrElse(0.0)
    var h1: Double = sigmoid(z1)
    var h2: Double = sigmoid(z2)
    var z3: Double = w2.lift((BigInt(0).toInt).toInt).getOrElse(0.0) * h1 + w2.lift((BigInt(1).toInt).toInt).getOrElse(0.0) * h2 + b2
    var out: Double = sigmoid(z3)
    var label: BigInt = BigInt(0)
    if (out >= 0.5) {
      label = BigInt(1)
    }
    preds = (preds :+ label)
    i = i + BigInt(1)
  }
  return preds
}

def wrapper(y: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  return y
}

var X: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.0, 0.0), ArrayBuffer(1.0, 1.0), ArrayBuffer(1.0, 0.0), ArrayBuffer(0.0, 1.0))

var Y: ArrayBuffer[Double] = ArrayBuffer(0.0, 1.0, 0.0, 0.0)

var test_data: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.0, 0.0), ArrayBuffer(0.0, 1.0), ArrayBuffer(1.0, 1.0))

var w1: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.5, -0.5), ArrayBuffer(0.5, 0.5))

var b1: ArrayBuffer[Double] = ArrayBuffer(0.0, 0.0)

var w2: ArrayBuffer[Double] = ArrayBuffer(0.5, -0.5)

var b2: Double = 0.0

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    train(BigInt(4000), 0.5)
    var preds: ArrayBuffer[BigInt] = wrapper(predict(test_data))
    println(_str(_str(preds)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
