// Generated by Mochi v0.10.66 on 2025-08-15 15:24:34 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def longest_distance(graph: ArrayBuffer[ArrayBuffer[BigInt]]): BigInt = {
  var n: BigInt = BigInt((graph).size)
  var indegree: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < n) {
    indegree = (indegree :+ BigInt(0))
    i = i + BigInt(1)
  }
  var long_dist: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var j: BigInt = BigInt(0)
  while (j < n) {
    long_dist = (long_dist :+ BigInt(1))
    j = j + BigInt(1)
  }
  var u: BigInt = BigInt(0)
  while (u < n) {
    for (v <- graph.lift((u.toInt).toInt).getOrElse(ArrayBuffer[BigInt]())) {
      indegree((v).toInt) = indegree.lift((v.toInt).toInt).getOrElse(BigInt(0)) + BigInt(1)
    }
    u = u + BigInt(1)
  }
  var queue: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var head: BigInt = BigInt(0)
  var k: BigInt = BigInt(0)
  while (k < n) {
    if (indegree.lift((k.toInt).toInt).getOrElse(BigInt(0)) == BigInt(0)) {
      queue = (queue :+ k)
    }
    k = k + BigInt(1)
  }
  while (head < BigInt((queue).size)) {
    var vertex: BigInt = queue.lift((head.toInt).toInt).getOrElse(BigInt(0))
    head = head + BigInt(1)
    for (x <- graph.lift((vertex.toInt).toInt).getOrElse(ArrayBuffer[BigInt]())) {
      indegree((x).toInt) = indegree.lift((x.toInt).toInt).getOrElse(BigInt(0)) - BigInt(1)
      var new_dist: BigInt = long_dist.lift((vertex.toInt).toInt).getOrElse(BigInt(0)) + BigInt(1)
      if (new_dist > long_dist.lift((x.toInt).toInt).getOrElse(BigInt(0))) {
        long_dist((x).toInt) = new_dist
      }
      if (indegree.lift((x.toInt).toInt).getOrElse(BigInt(0)) == BigInt(0)) {
        queue = (queue :+ x)
      }
    }
  }
  var max_len: BigInt = long_dist.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0))
  var m: BigInt = BigInt(1)
  while (m < n) {
    if (long_dist.lift((m.toInt).toInt).getOrElse(BigInt(0)) > max_len) {
      max_len = long_dist.lift((m.toInt).toInt).getOrElse(BigInt(0))
    }
    m = m + BigInt(1)
  }
  return max_len
}

var graph: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(BigInt(2), BigInt(3), BigInt(4)), ArrayBuffer(BigInt(2), BigInt(7)), ArrayBuffer(BigInt(5)), ArrayBuffer(BigInt(5), BigInt(7)), ArrayBuffer(BigInt(7)), ArrayBuffer(BigInt(6)), ArrayBuffer(BigInt(7)), ArrayBuffer[BigInt]())

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(longest_distance(graph)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
