// Generated by Mochi v0.10.72 on 2025-08-24 22:29:24 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class RadixNode(var prefix: String, var is_leaf: Boolean, var children: scala.collection.mutable.Map[String,BigInt])

case class RadixTree(var nodes: ArrayBuffer[RadixNode])

case class MatchResult(var common: String, var rem_prefix: String, var rem_word: String)

def new_node(prefix: String, is_leaf: Boolean): RadixNode = {
  return RadixNode(prefix, is_leaf, scala.collection.mutable.Map())
}

def new_tree(): RadixTree = {
  var nodes: ArrayBuffer[RadixNode] = ArrayBuffer((new_node("", false)).asInstanceOf[RadixNode])
  return RadixTree(nodes)
}

def match_prefix(node: RadixNode, word: String): MatchResult = {
  var x: BigInt = BigInt(0)
  var p: String = node.asInstanceOf[RadixNode].prefix
  var w: String = word
  var min_len: BigInt = BigInt((p).size)
  if (BigInt((w).size) < min_len) {
    min_len = BigInt((w).size)
  }
  val _br0 = new Breaks
  _br0.breakable {
    while (x < min_len) {
      if (p.slice((x).toInt, (x + BigInt(1)).toInt) != w.slice((x).toInt, (x + BigInt(1)).toInt)) {
        _br0.break()
      }
      x = x + BigInt(1)
    }
  }
  var common: String = p.slice((BigInt(0)).toInt, (x).toInt)
  var rem_prefix: String = p.slice((x).toInt, (BigInt((p).size)).toInt)
  var rem_word: String = w.slice((x).toInt, (BigInt((w).size)).toInt)
  return MatchResult(common, rem_prefix, rem_word)
}

def insert_many(tree: RadixTree, words: ArrayBuffer[String]): Any = {
  for (w <- words) {
    insert(tree, BigInt(0), w)
  }
}

def insert(tree: RadixTree, idx: BigInt, word: String): Any = {
  var nodes: ArrayBuffer[RadixNode] = tree.asInstanceOf[RadixTree].nodes
  var node: RadixNode = nodes.lift((idx.toInt).toInt).getOrElse(null)
  if ((node.asInstanceOf[RadixNode].prefix == word && !node.asInstanceOf[RadixNode].is_leaf)) {
    node.asInstanceOf[RadixNode].is_leaf = true
    nodes((idx).toInt) = node
    tree.asInstanceOf[RadixTree].nodes = nodes
    return
  }
  var first: String = word.slice((BigInt(0)).toInt, (BigInt(1)).toInt)
  var children: scala.collection.mutable.Map[String,BigInt] = node.asInstanceOf[RadixNode].children
  if (!has_key(children, first)) {
    var new_idx: BigInt = BigInt((nodes).size)
    nodes = ({nodes += (new_node(word, true)); nodes})
    children.update(first, new_idx)
    node.asInstanceOf[RadixNode].children = children
    nodes((idx).toInt) = node
    tree.asInstanceOf[RadixTree].nodes = nodes
    return
  }
  var child_idx: BigInt = children.getOrElse(first.toString, BigInt(0)).asInstanceOf[BigInt]
  var child: RadixNode = nodes.lift((child_idx.toInt).toInt).getOrElse(null)
  var res: MatchResult = match_prefix(child, word)
  if (res.asInstanceOf[MatchResult].rem_prefix == "") {
    insert(tree, child_idx, res.asInstanceOf[MatchResult].rem_word)
    return
  }
  child.asInstanceOf[RadixNode].prefix = res.asInstanceOf[MatchResult].rem_prefix
  nodes((child_idx).toInt) = child
  var new_children: scala.collection.mutable.Map[String,BigInt] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,BigInt]]
  new_children.update(res.asInstanceOf[MatchResult].rem_prefix.slice((BigInt(0)).toInt, (BigInt(1)).toInt), child_idx)
  var new_idx: BigInt = BigInt((nodes).size)
  nodes = ({nodes += (new_node(res.asInstanceOf[MatchResult].common, false)); nodes})
  nodes.lift((new_idx).toInt).getOrElse(null).children = new_children
  if (res.asInstanceOf[MatchResult].rem_word == "") {
    nodes.lift((new_idx).toInt).getOrElse(null).is_leaf = true
  } else {
    insert(tree, new_idx, res.asInstanceOf[MatchResult].rem_word)
  }
  children.update(first, new_idx)
  node.asInstanceOf[RadixNode].children = children
  nodes((idx).toInt) = node
  tree.asInstanceOf[RadixTree].nodes = nodes
  return null
}

def find(tree: RadixTree, idx: BigInt, word: String): Boolean = {
  var nodes: ArrayBuffer[RadixNode] = tree.asInstanceOf[RadixTree].nodes
  var node: RadixNode = nodes.lift((idx.toInt).toInt).getOrElse(null)
  var first: String = word.slice((BigInt(0)).toInt, (BigInt(1)).toInt)
  var children: scala.collection.mutable.Map[String,BigInt] = node.asInstanceOf[RadixNode].children
  if (!has_key(children, first)) {
    return false
  }
  var child_idx: BigInt = children.getOrElse(first.toString, BigInt(0)).asInstanceOf[BigInt]
  var child: RadixNode = nodes.lift((child_idx.toInt).toInt).getOrElse(null)
  var res: MatchResult = match_prefix(child, word)
  if (res.asInstanceOf[MatchResult].rem_prefix != "") {
    return false
  }
  if (res.asInstanceOf[MatchResult].rem_word == "") {
    return child.asInstanceOf[RadixNode].is_leaf
  }
  return find(tree, child_idx, res.asInstanceOf[MatchResult].rem_word)
}

def remove_key(m: scala.collection.mutable.Map[String,BigInt], k: String): scala.collection.mutable.Map[String,BigInt] = {
  var out: scala.collection.mutable.Map[String,BigInt] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,BigInt]]
  for (key <- (m.keys.toSeq.sorted)) {
    if (key != k) {
      out.update(key, m.getOrElse(key.toString, BigInt(0)).asInstanceOf[BigInt])
    }
  }
  return out
}

def has_key(m: scala.collection.mutable.Map[String,BigInt], k: String): Boolean = {
  for (key <- (m.keys.toSeq.sorted)) {
    if (key == k) {
      return true
    }
  }
  return false
}

def delete(tree: RadixTree, idx: BigInt, word: String): Boolean = {
  var nodes: ArrayBuffer[RadixNode] = tree.asInstanceOf[RadixTree].nodes
  var node: RadixNode = nodes.lift((idx.toInt).toInt).getOrElse(null)
  var first: String = word.slice((BigInt(0)).toInt, (BigInt(1)).toInt)
  var children: scala.collection.mutable.Map[String,BigInt] = node.asInstanceOf[RadixNode].children
  if (!has_key(children, first)) {
    return false
  }
  var child_idx: BigInt = children.getOrElse(first.toString, BigInt(0)).asInstanceOf[BigInt]
  var child: RadixNode = nodes.lift((child_idx.toInt).toInt).getOrElse(null)
  var res: MatchResult = match_prefix(child, word)
  if (res.asInstanceOf[MatchResult].rem_prefix != "") {
    return false
  }
  if (res.asInstanceOf[MatchResult].rem_word != "") {
    var deleted: Boolean = delete(tree, child_idx, res.asInstanceOf[MatchResult].rem_word)
    if (deleted) {
      nodes = tree.asInstanceOf[RadixTree].nodes
      node = nodes.lift((idx.toInt).toInt).getOrElse(null)
    }
    return deleted
  }
  if (!child.asInstanceOf[RadixNode].is_leaf) {
    return false
  }
  if (BigInt((child.asInstanceOf[RadixNode].children).size) == BigInt(0)) {
    children = remove_key(children, first)
    node.asInstanceOf[RadixNode].children = children
    nodes((idx).toInt) = node
    tree.asInstanceOf[RadixTree].nodes = nodes
    if ((BigInt((children).size) == BigInt(1) && !node.asInstanceOf[RadixNode].is_leaf)) {
      var only_key: String = ""
      for (k <- (children.keys.toSeq.sorted)) {
        only_key = k
      }
      var merge_idx: BigInt = children.getOrElse(only_key.toString, BigInt(0)).asInstanceOf[BigInt]
      var merge_node: RadixNode = nodes.lift((merge_idx.toInt).toInt).getOrElse(null)
      node.asInstanceOf[RadixNode].is_leaf = merge_node.asInstanceOf[RadixNode].is_leaf
      node.asInstanceOf[RadixNode].prefix = node.asInstanceOf[RadixNode].prefix + merge_node.asInstanceOf[RadixNode].prefix
      node.asInstanceOf[RadixNode].children = merge_node.asInstanceOf[RadixNode].children
      nodes((idx).toInt) = node
      tree.asInstanceOf[RadixTree].nodes = nodes
    }
  } else {
    if (BigInt((child.asInstanceOf[RadixNode].children).size) > BigInt(1)) {
      child.asInstanceOf[RadixNode].is_leaf = false
      nodes((child_idx).toInt) = child
      tree.asInstanceOf[RadixTree].nodes = nodes
    } else {
      var only_key: String = ""
      for (k <- (child.asInstanceOf[RadixNode].children.keys.toSeq.sorted)) {
        only_key = k
      }
      var merge_idx: BigInt = child.asInstanceOf[RadixNode].children.getOrElse(only_key.toString, BigInt(0)).asInstanceOf[BigInt]
      var merge_node: RadixNode = nodes.lift((merge_idx.toInt).toInt).getOrElse(null)
      child.asInstanceOf[RadixNode].is_leaf = merge_node.asInstanceOf[RadixNode].is_leaf
      child.asInstanceOf[RadixNode].prefix = child.asInstanceOf[RadixNode].prefix + merge_node.asInstanceOf[RadixNode].prefix
      child.asInstanceOf[RadixNode].children = merge_node.asInstanceOf[RadixNode].children
      nodes((child_idx).toInt) = child
      tree.asInstanceOf[RadixTree].nodes = nodes
    }
  }
  return true
}

def print_tree(tree: RadixTree, idx: BigInt, height: BigInt): Any = {
  var nodes: ArrayBuffer[RadixNode] = tree.asInstanceOf[RadixTree].nodes
  var node: RadixNode = nodes.lift((idx.toInt).toInt).getOrElse(null)
  if (node.asInstanceOf[RadixNode].prefix != "") {
    var line: String = ""
    var i: BigInt = BigInt(0)
    while (i < height) {
      line = line + "-"
      i = i + BigInt(1)
    }
    line = line + " " + node.asInstanceOf[RadixNode].prefix
    if (node.asInstanceOf[RadixNode].is_leaf) {
      line = line + "  (leaf)"
    }
    println(_str(line))
  }
  var children: scala.collection.mutable.Map[String,BigInt] = node.asInstanceOf[RadixNode].children
  for (k <- (children.keys.toSeq.sorted)) {
    var child_idx: BigInt = children.getOrElse(k.toString, BigInt(0)).asInstanceOf[BigInt]
    print_tree(tree, child_idx, height + BigInt(1))
  }
}

def test_trie(): Boolean = {
  var words: ArrayBuffer[String] = ArrayBuffer("banana", "bananas", "bandana", "band", "apple", "all", "beast")
  var tree: RadixTree = new_tree()
  insert_many(tree, words)
  var ok: Boolean = true
  for (w <- words) {
    if (!find(tree, BigInt(0), w)) {
      ok = false
    }
  }
  if (find(tree, BigInt(0), "bandanas")) {
    ok = false
  }
  if (find(tree, BigInt(0), "apps")) {
    ok = false
  }
  delete(tree, BigInt(0), "all")
  if (find(tree, BigInt(0), "all")) {
    ok = false
  }
  delete(tree, BigInt(0), "banana")
  if (find(tree, BigInt(0), "banana")) {
    ok = false
  }
  if (!find(tree, BigInt(0), "bananas")) {
    ok = false
  }
  return ok
}

def pytests(): Any = {
  if (!test_trie()) {
    throw new RuntimeException(String.valueOf("test failed"))
  }
}

def main(): Any = {
  var tree: RadixTree = new_tree()
  var words: ArrayBuffer[String] = ArrayBuffer("banana", "bananas", "bandanas", "bandana", "band", "apple", "all", "beast")
  insert_many(tree, words)
  println(_str("Words: " + _str(words)))
  println(_str("Tree:"))
  print_tree(tree, BigInt(0), BigInt(0))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
