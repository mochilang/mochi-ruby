// Generated by Mochi v0.10.72 on 2025-08-24 23:40:14 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class Node(var data: BigInt, var next: BigInt, var prev: BigInt)

case class Stack(var nodes: ArrayBuffer[Node], var head: BigInt)

case class PopResult(var stack: Stack, var value: BigInt, var ok: Boolean)

case class TopResult(var value: BigInt, var ok: Boolean)

def empty_stack(): Stack = {
  return Stack(ArrayBuffer(), BigInt(-1))
}

def push(stack: Stack, value: BigInt): Stack = {
  var nodes: ArrayBuffer[Node] = stack.asInstanceOf[Stack].nodes
  val idx: BigInt = BigInt((nodes).size)
  var new_node: Node = Node(value, stack.asInstanceOf[Stack].head, BigInt(-1))
  nodes = ({nodes += (new_node); nodes})
  if (stack.asInstanceOf[Stack].head != BigInt(-1)) {
    var head_node: Node = nodes.lift((stack.asInstanceOf[Stack].head.toInt).toInt).getOrElse(null)
    head_node.asInstanceOf[Node].prev = idx
    nodes((stack.asInstanceOf[Stack].head).toInt) = head_node
  }
  return Stack(nodes, idx)
}

def pop(stack: Stack): PopResult = {
  if (stack.asInstanceOf[Stack].head == BigInt(-1)) {
    return PopResult(stack, BigInt(0), false)
  }
  var nodes: ArrayBuffer[Node] = stack.asInstanceOf[Stack].nodes
  var head_node: Node = nodes.lift((stack.asInstanceOf[Stack].head.toInt).toInt).getOrElse(null)
  val value: BigInt = head_node.asInstanceOf[Node].data
  val next_idx: BigInt = head_node.asInstanceOf[Node].next
  if (next_idx != BigInt(-1)) {
    var next_node: Node = nodes.lift((next_idx.toInt).toInt).getOrElse(null)
    next_node.asInstanceOf[Node].prev = BigInt(-1)
    nodes((next_idx).toInt) = next_node
  }
  val new_stack: Stack = Stack(nodes, next_idx)
  return PopResult(new_stack, value, true)
}

def top(stack: Stack): TopResult = {
  if (stack.asInstanceOf[Stack].head == BigInt(-1)) {
    return TopResult(BigInt(0), false)
  }
  val node: Node = stack.asInstanceOf[Stack].nodes.lift((stack.asInstanceOf[Stack].head.toInt).toInt).getOrElse(null)
  return TopResult(node.asInstanceOf[Node].data, true)
}

def size(stack: Stack): BigInt = {
  var count: BigInt = BigInt(0)
  var idx: BigInt = stack.asInstanceOf[Stack].head
  while (idx != BigInt(-1)) {
    count = count + BigInt(1)
    val node: Node = stack.asInstanceOf[Stack].nodes.lift((idx.toInt).toInt).getOrElse(null)
    idx = node.asInstanceOf[Node].next
  }
  return count
}

def is_empty(stack: Stack): Boolean = {
  return stack.asInstanceOf[Stack].head == BigInt(-1)
}

def print_stack(stack: Stack): Any = {
  println(_str("stack elements are:"))
  var idx: BigInt = stack.asInstanceOf[Stack].head
  var s: String = ""
  while (idx != BigInt(-1)) {
    val node: Node = stack.asInstanceOf[Stack].nodes.lift((idx.toInt).toInt).getOrElse(null)
    s = s + _str(node.asInstanceOf[Node].data) + "->"
    idx = node.asInstanceOf[Node].next
  }
  if (BigInt((s).size) > BigInt(0)) {
    println(_str(s))
  }
}

def main(): Any = {
  var stack: Stack = empty_stack()
  println(_str("Stack operations using Doubly LinkedList"))
  stack = push(stack, BigInt(4))
  stack = push(stack, BigInt(5))
  stack = push(stack, BigInt(6))
  stack = push(stack, BigInt(7))
  print_stack(stack)
  val t: TopResult = top(stack)
  if (t.ok) {
    println(_str("Top element is " + _str(t.value)))
  } else {
    println(_str("Top element is None"))
  }
  println(_str("Size of the stack is " + _str(size(stack))))
  var p: PopResult = pop(stack)
  stack = p.asInstanceOf[PopResult].stack
  p = pop(stack)
  stack = p.asInstanceOf[PopResult].stack
  print_stack(stack)
  println(_str("stack is empty: " + _str(is_empty(stack))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
