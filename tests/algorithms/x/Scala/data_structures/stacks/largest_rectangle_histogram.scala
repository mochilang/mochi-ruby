// Generated by Mochi v0.10.72 on 2025-08-24 23:39:18 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def largest_rectangle_area(heights: ArrayBuffer[BigInt]): BigInt = {
  var stack: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var max_area: BigInt = BigInt(0)
  var hs: ArrayBuffer[BigInt] = heights
  hs = (hs :+ (BigInt(0)))
  var i: BigInt = BigInt(0)
  while (i < BigInt((hs).size)) {
    while (BigInt((stack).size) > BigInt(0) && hs.lift((i.toInt).toInt).getOrElse(BigInt(0)) < hs.lift((stack.lift(((BigInt((stack).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0)).toInt).toInt).getOrElse(BigInt(0))) {
      val top: BigInt = stack.lift(((BigInt((stack).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0))
      stack = stack.slice((BigInt(0)).toInt, (BigInt((stack).size) - BigInt(1)).toInt)
      val height: BigInt = hs.lift((top.toInt).toInt).getOrElse(BigInt(0))
      var width: BigInt = i
      if (BigInt((stack).size) > BigInt(0)) {
        width = i - stack.lift(((BigInt((stack).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0)) - BigInt(1)
      }
      val area: BigInt = height * width
      if (area > max_area) {
        max_area = area
      }
    }
    stack = (stack :+ (i))
    i = i + BigInt(1)
  }
  return max_area
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(_str(largest_rectangle_area(ArrayBuffer(BigInt(2), BigInt(1), BigInt(5), BigInt(6), BigInt(2), BigInt(3))))))
    println(_str(_str(largest_rectangle_area(ArrayBuffer(BigInt(2), BigInt(4))))))
    println(_str(_str(largest_rectangle_area(ArrayBuffer(BigInt(6), BigInt(2), BigInt(5), BigInt(4), BigInt(5), BigInt(1), BigInt(6))))))
    println(_str(_str(largest_rectangle_area(ArrayBuffer(BigInt(1))))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
