// Generated by Mochi v0.10.72 on 2025-08-24 23:38:54 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def is_digit(ch: String): Boolean = {
  return (((((((((ch == "0" || ch == "1").asInstanceOf[Boolean] || ch == "2").asInstanceOf[Boolean] || ch == "3").asInstanceOf[Boolean] || ch == "4").asInstanceOf[Boolean] || ch == "5").asInstanceOf[Boolean] || ch == "6").asInstanceOf[Boolean] || ch == "7").asInstanceOf[Boolean] || ch == "8").asInstanceOf[Boolean] || ch == "9").asInstanceOf[Boolean]
}

def slice_without_last_int(xs: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var res: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((xs).size) - BigInt(1)) {
    res = (res :+ (xs.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i + BigInt(1)
  }
  return res
}

def slice_without_last_string(xs: ArrayBuffer[String]): ArrayBuffer[String] = {
  var res: ArrayBuffer[String] = ArrayBuffer[String]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((xs).size) - BigInt(1)) {
    res = (res :+ (xs.lift((i.toInt).toInt).getOrElse("")))
    i = i + BigInt(1)
  }
  return res
}

def dijkstras_two_stack_algorithm(equation: String): BigInt = {
  var operand_stack: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var operator_stack: ArrayBuffer[String] = ArrayBuffer[String]()
  var idx: BigInt = BigInt(0)
  while (idx < BigInt((equation).length)) {
    val ch: String = equation.slice((idx).toInt, (idx + BigInt(1)).toInt)
    if (is_digit(ch)) {
      operand_stack = (operand_stack :+ (BigInt(ch.toString.toDouble.toInt)))
    } else {
      if ((((ch == "+" || ch == "-").asInstanceOf[Boolean] || ch == "*").asInstanceOf[Boolean] || ch == "/")) {
        operator_stack = (operator_stack :+ (ch))
      } else {
        if (ch == ")") {
          val opr: String = operator_stack.lift(((BigInt((operator_stack).size) - BigInt(1)).toInt).toInt).getOrElse("")
          operator_stack = slice_without_last_string(operator_stack)
          val num1: BigInt = operand_stack.lift(((BigInt((operand_stack).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0))
          operand_stack = slice_without_last_int(operand_stack)
          val num2: BigInt = operand_stack.lift(((BigInt((operand_stack).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0))
          operand_stack = slice_without_last_int(operand_stack)
          val total: BigInt = (if (opr == "+") num2 + num1 else (if (opr == "-") num2 - num1 else (if (opr == "*") num2 * num1 else num2 / num1)))
          operand_stack = (operand_stack :+ (total))
        }
      }
    }
    idx = idx + BigInt(1)
  }
  return operand_stack.lift(((BigInt((operand_stack).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0))
}

val equation: String = "(5 + ((4 * 2) * (2 + 3)))"

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(equation + " = " + _str(dijkstras_two_stack_algorithm(equation))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
