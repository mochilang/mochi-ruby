// Generated by Mochi v0.10.72 on 2025-08-24 23:38:31 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class Queue(var stack1: ArrayBuffer[BigInt], var stack2: ArrayBuffer[BigInt])

case class GetResult(var queue: Queue, var value: BigInt)

def new_queue(items: ArrayBuffer[BigInt]): Queue = {
  return Queue(items, ArrayBuffer())
}

def len_queue(q: Queue): BigInt = {
  return BigInt((q.stack1).size) + BigInt((q.stack2).size)
}

def str_queue(q: Queue): String = {
  var items: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt((q.stack2).size) - BigInt(1)
  while (i >= BigInt(0)) {
    items = (items :+ (q.stack2.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i - BigInt(1)
  }
  var j: BigInt = BigInt(0)
  while (j < BigInt((q.stack1).size)) {
    items = (items :+ (q.stack1.lift((j.toInt).toInt).getOrElse(BigInt(0))))
    j = j + BigInt(1)
  }
  var s: String = "Queue(("
  var k: BigInt = BigInt(0)
  while (k < BigInt((items).size)) {
    s = s + _str(items.lift((k.toInt).toInt).getOrElse(BigInt(0)))
    if (k < BigInt((items).size) - BigInt(1)) {
      s = s + ", "
    }
    k = k + BigInt(1)
  }
  s = s + "))"
  return s
}

def put(q: Queue, item: BigInt): Queue = {
  var s1: ArrayBuffer[BigInt] = q.stack1
  s1 = ({s1 += (item); s1})
  return Queue(s1, q.stack2)
}

def get(q: Queue): GetResult = {
  var s1: ArrayBuffer[BigInt] = q.stack1
  var s2: ArrayBuffer[BigInt] = q.stack2
  if (BigInt((s2).size) == BigInt(0)) {
    while (BigInt((s1).size) > BigInt(0)) {
      val idx: BigInt = BigInt((s1).size) - BigInt(1)
      val v: BigInt = s1.lift((idx.toInt).toInt).getOrElse(BigInt(0))
      var new_s1: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
      var i: BigInt = BigInt(0)
      while (i < idx) {
        new_s1 = (new_s1 :+ (s1.lift((i.toInt).toInt).getOrElse(BigInt(0))))
        i = i + BigInt(1)
      }
      s1 = new_s1
      s2 = ({s2 += (v); s2})
    }
  }
  if (BigInt((s2).size) == BigInt(0)) {
    throw new RuntimeException(String.valueOf("Queue is empty"))
  }
  val idx2: BigInt = BigInt((s2).size) - BigInt(1)
  val value: BigInt = s2.lift((idx2.toInt).toInt).getOrElse(BigInt(0))
  var new_s2: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var j: BigInt = BigInt(0)
  while (j < idx2) {
    new_s2 = (new_s2 :+ (s2.lift((j.toInt).toInt).getOrElse(BigInt(0))))
    j = j + BigInt(1)
  }
  s2 = new_s2
  return GetResult(Queue(s1, s2), value)
}

var q: Queue = new_queue(ArrayBuffer(BigInt(10), BigInt(20), BigInt(30)))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    val r1: GetResult = get(q)
    q = r1.queue
    println(_str(r1.value))
    q = put(q, BigInt(40))
    val r2: GetResult = get(q)
    q = r2.queue
    println(_str(r2.value))
    val r3: GetResult = get(q)
    q = r3.queue
    println(_str(r3.value))
    println(_str(len_queue(q)))
    val r4: GetResult = get(q)
    q = r4.queue
    println(_str(r4.value))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
