// Generated by Mochi v0.10.72 on 2025-08-24 23:34:00 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class CircularLinkedList(var data: ArrayBuffer[BigInt])

case class DeleteResult(var list: CircularLinkedList, var value: BigInt)

def empty_list(): CircularLinkedList = {
  return CircularLinkedList(ArrayBuffer())
}

def length(list: CircularLinkedList): BigInt = {
  return BigInt((list.asInstanceOf[CircularLinkedList].data).size)
}

def is_empty(list: CircularLinkedList): Boolean = {
  return BigInt((list.asInstanceOf[CircularLinkedList].data).size) == BigInt(0)
}

def to_string(list: CircularLinkedList): String = {
  if (BigInt((list.asInstanceOf[CircularLinkedList].data).size) == BigInt(0)) {
    return ""
  }
  var s: String = _str(list.asInstanceOf[CircularLinkedList].data.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0)))
  var i: BigInt = BigInt(1)
  while (i < BigInt((list.asInstanceOf[CircularLinkedList].data).size)) {
    s = s + "->" + _str(list.asInstanceOf[CircularLinkedList].data.lift((i.toInt).toInt).getOrElse(BigInt(0)))
    i = i + BigInt(1)
  }
  return s
}

def insert_nth(list: CircularLinkedList, index: BigInt, value: BigInt): CircularLinkedList = {
  if ((index < BigInt(0) || index > BigInt((list.asInstanceOf[CircularLinkedList].data).size))) {
    throw new RuntimeException(String.valueOf("index out of range"))
  }
  var res: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < index) {
    res = (res :+ (list.asInstanceOf[CircularLinkedList].data.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i + BigInt(1)
  }
  res = (res :+ (value))
  while (i < BigInt((list.asInstanceOf[CircularLinkedList].data).size)) {
    res = (res :+ (list.asInstanceOf[CircularLinkedList].data.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i + BigInt(1)
  }
  return CircularLinkedList(res)
}

def insert_head(list: CircularLinkedList, value: BigInt): CircularLinkedList = {
  return insert_nth(list, BigInt(0), value)
}

def insert_tail(list: CircularLinkedList, value: BigInt): CircularLinkedList = {
  return insert_nth(list, BigInt((list.asInstanceOf[CircularLinkedList].data).size), value)
}

def delete_nth(list: CircularLinkedList, index: BigInt): DeleteResult = {
  if ((index < BigInt(0) || index >= BigInt((list.asInstanceOf[CircularLinkedList].data).size))) {
    throw new RuntimeException(String.valueOf("index out of range"))
  }
  var res: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  var `val`: BigInt = BigInt(0)
  while (i < BigInt((list.asInstanceOf[CircularLinkedList].data).size)) {
    if (i == index) {
      `val` = list.asInstanceOf[CircularLinkedList].data.lift((i.toInt).toInt).getOrElse(BigInt(0))
    } else {
      res = (res :+ (list.asInstanceOf[CircularLinkedList].data.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    }
    i = i + BigInt(1)
  }
  return DeleteResult(CircularLinkedList(res), `val`)
}

def delete_front(list: CircularLinkedList): DeleteResult = {
  return delete_nth(list, BigInt(0))
}

def delete_tail(list: CircularLinkedList): DeleteResult = {
  return delete_nth(list, BigInt((list.asInstanceOf[CircularLinkedList].data).size) - BigInt(1))
}

def main(): Any = {
  var cll: CircularLinkedList = empty_list()
  var i: BigInt = BigInt(0)
  while (i < BigInt(5)) {
    cll = insert_tail(cll, i + BigInt(1))
    i = i + BigInt(1)
  }
  println(_str(to_string(cll)))
  cll = insert_tail(cll, BigInt(6))
  println(_str(to_string(cll)))
  cll = insert_head(cll, BigInt(0))
  println(_str(to_string(cll)))
  var res: DeleteResult = delete_front(cll)
  cll = res.asInstanceOf[DeleteResult].list
  println(_str(res.asInstanceOf[DeleteResult].value))
  res = delete_tail(cll)
  cll = res.asInstanceOf[DeleteResult].list
  println(_str(res.asInstanceOf[DeleteResult].value))
  res = delete_nth(cll, BigInt(2))
  cll = res.asInstanceOf[DeleteResult].list
  println(_str(res.asInstanceOf[DeleteResult].value))
  println(_str(to_string(cll)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
