// Generated by Mochi v0.10.72 on 2025-08-24 23:35:06 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class SortedLinkedList(var values: ArrayBuffer[BigInt])

def sort_list(nums: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var arr: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((nums).size)) {
    arr = (arr :+ (nums.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i + BigInt(1)
  }
  var j: BigInt = BigInt(0)
  while (j < BigInt((arr).size)) {
    var k: BigInt = j + BigInt(1)
    while (k < BigInt((arr).size)) {
      if (arr.lift((k.toInt).toInt).getOrElse(BigInt(0)) < arr.lift((j.toInt).toInt).getOrElse(BigInt(0))) {
        val tmp: BigInt = arr.lift((j.toInt).toInt).getOrElse(BigInt(0))
        arr((j).toInt) = arr.lift((k.toInt).toInt).getOrElse(BigInt(0))
        arr((k).toInt) = tmp
      }
      k = k + BigInt(1)
    }
    j = j + BigInt(1)
  }
  return arr
}

def make_sorted_linked_list(ints: ArrayBuffer[BigInt]): SortedLinkedList = {
  return SortedLinkedList(sort_list(ints))
}

def len_sll(sll: SortedLinkedList): BigInt = {
  return BigInt((sll.asInstanceOf[SortedLinkedList].values).size)
}

def str_sll(sll: SortedLinkedList): String = {
  var res: String = ""
  var i: BigInt = BigInt(0)
  while (i < BigInt((sll.asInstanceOf[SortedLinkedList].values).size)) {
    res = res + _str(sll.asInstanceOf[SortedLinkedList].values.lift((i.toInt).toInt).getOrElse(BigInt(0)))
    if (i + BigInt(1) < BigInt((sll.asInstanceOf[SortedLinkedList].values).size)) {
      res = res + " -> "
    }
    i = i + BigInt(1)
  }
  return res
}

def merge_lists(a: SortedLinkedList, b: SortedLinkedList): SortedLinkedList = {
  var combined: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((a.asInstanceOf[SortedLinkedList].values).size)) {
    combined = (combined :+ (a.asInstanceOf[SortedLinkedList].values.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i + BigInt(1)
  }
  i = BigInt(0)
  while (i < BigInt((b.asInstanceOf[SortedLinkedList].values).size)) {
    combined = (combined :+ (b.asInstanceOf[SortedLinkedList].values.lift((i.toInt).toInt).getOrElse(BigInt(0))))
    i = i + BigInt(1)
  }
  return make_sorted_linked_list(combined)
}

def main(): Any = {
  val test_data_odd: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(3), BigInt(9), BigInt(-11), BigInt(0), BigInt(7), BigInt(5), BigInt(1), BigInt(-1))
  val test_data_even: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(4), BigInt(6), BigInt(2), BigInt(0), BigInt(8), BigInt(10), BigInt(3), BigInt(-2))
  val sll_one: SortedLinkedList = make_sorted_linked_list(test_data_odd)
  val sll_two: SortedLinkedList = make_sorted_linked_list(test_data_even)
  val merged: SortedLinkedList = merge_lists(sll_one, sll_two)
  println(_str(_str(len_sll(merged))))
  println(_str(str_sll(merged)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
