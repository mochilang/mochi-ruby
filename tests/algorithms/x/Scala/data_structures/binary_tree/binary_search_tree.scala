// Generated by Mochi v0.10.71 on 2025-08-24 15:50:44 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def create_node(value: BigInt): ArrayBuffer[Any] = {
  return ArrayBuffer(value.asInstanceOf[Any], null.asInstanceOf[Any], null.asInstanceOf[Any])
}

def insert(node: ArrayBuffer[Any], value: BigInt): ArrayBuffer[Any] = {
  if (node == null) {
    return create_node(value)
  }
  if (value < BigInt((node.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)) {
    node((BigInt(1)).toInt) = insert((node.lift((BigInt(1).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], value)
  } else {
    if (value > BigInt((node.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)) {
      node((BigInt(2)).toInt) = insert((node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], value)
    }
  }
  return node
}

def search(node: ArrayBuffer[Any], value: BigInt): Boolean = {
  if (node == null) {
    return false
  }
  if (value == node.lift((BigInt(0).toInt).toInt).getOrElse(null)) {
    return true
  }
  if (value < BigInt((node.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)) {
    return search((node.lift((BigInt(1).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], value)
  }
  return search((node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], value)
}

def inorder(node: ArrayBuffer[Any], acc: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  if (node == null) {
    return acc
  }
  var left_acc: ArrayBuffer[BigInt] = inorder((node.lift((BigInt(1).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], acc)
  var with_node: ArrayBuffer[BigInt] = (left_acc :+ (BigInt((node.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)))
  return inorder((node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], with_node)
}

def find_min(node: ArrayBuffer[Any]): BigInt = {
  var current: ArrayBuffer[Any] = node.asInstanceOf[ArrayBuffer[Any]]
  while (current.lift((BigInt(1).toInt).toInt).getOrElse(null) != null) {
    current = (current.lift((BigInt(1).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]]
  }
  return BigInt((current.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)
}

def find_max(node: ArrayBuffer[Any]): BigInt = {
  var current: ArrayBuffer[Any] = node.asInstanceOf[ArrayBuffer[Any]]
  while (current.lift((BigInt(2).toInt).toInt).getOrElse(null) != null) {
    current = (current.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]]
  }
  return BigInt((current.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)
}

def delete(node: ArrayBuffer[Any], value: BigInt): ArrayBuffer[Any] = {
  if (node == null) {
    return null.asInstanceOf[ArrayBuffer[Any]]
  }
  if (value < BigInt((node.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)) {
    node((BigInt(1)).toInt) = delete((node.lift((BigInt(1).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], value)
  } else {
    if (value > BigInt((node.lift((BigInt(0).toInt).toInt).getOrElse(null)).toString.toDouble.toInt)) {
      node((BigInt(2)).toInt) = delete((node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], value)
    } else {
      if (node.lift((BigInt(1).toInt).toInt).getOrElse(null) == null) {
        return (node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]]
      }
      if (node.lift((BigInt(2).toInt).toInt).getOrElse(null) == null) {
        return (node.lift((BigInt(1).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]]
      }
      var min_val: BigInt = find_min((node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]])
      node((BigInt(0)).toInt) = min_val
      node((BigInt(2)).toInt) = delete((node.lift((BigInt(2).toInt).toInt).getOrElse(null)).asInstanceOf[ArrayBuffer[Any]], min_val)
    }
  }
  return node
}

def main(): Any = {
  var root: Any = null
  var nums: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(8), BigInt(3), BigInt(6), BigInt(1), BigInt(10), BigInt(14), BigInt(13), BigInt(4), BigInt(7))
  for (v <- nums) {
    root = insert(root.asInstanceOf[ArrayBuffer[Any]], v)
  }
  println(_str(_str(inorder(root.asInstanceOf[ArrayBuffer[Any]], ArrayBuffer[BigInt]()))))
  println(_str(search(root.asInstanceOf[ArrayBuffer[Any]], BigInt(6))))
  println(_str(search(root.asInstanceOf[ArrayBuffer[Any]], BigInt(20))))
  println(_str(find_min(root.asInstanceOf[ArrayBuffer[Any]])))
  println(_str(find_max(root.asInstanceOf[ArrayBuffer[Any]])))
  root = delete(root.asInstanceOf[ArrayBuffer[Any]], BigInt(6))
  println(_str(_str(inorder(root.asInstanceOf[ArrayBuffer[Any]], ArrayBuffer[BigInt]()))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
