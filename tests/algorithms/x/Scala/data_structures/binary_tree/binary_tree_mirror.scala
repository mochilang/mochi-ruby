// Generated by Mochi v0.10.72 on 2025-08-24 17:03:42 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def binary_tree_mirror_dict(tree: scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]], root: BigInt): Any = {
  if ((root == BigInt(0) || !(tree.contains(root)))) {
    return
  }
  var children: ArrayBuffer[BigInt] = tree.getOrElse(root, ArrayBuffer[BigInt]()).asInstanceOf[ArrayBuffer[BigInt]]
  var left: BigInt = children.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0))
  var right: BigInt = children.lift((BigInt(1).toInt).toInt).getOrElse(BigInt(0))
  tree.update(root, ArrayBuffer(right, left))
  binary_tree_mirror_dict(tree, left)
  binary_tree_mirror_dict(tree, right)
  return null
}

def binary_tree_mirror(binary_tree: scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]], root: BigInt): scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]] = {
  if (BigInt((binary_tree).size) == BigInt(0)) {
    throw new RuntimeException(String.valueOf("binary tree cannot be empty"))
  }
  if (!(binary_tree.contains(root))) {
    throw new RuntimeException(String.valueOf("root " + _str(root) + " is not present in the binary_tree"))
  }
  var tree_copy: scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]]]
  for (k <- (binary_tree.keys.toSeq.sorted)) {
    tree_copy.update(k, binary_tree.getOrElse(k, ArrayBuffer[BigInt]()).asInstanceOf[ArrayBuffer[BigInt]])
  }
  binary_tree_mirror_dict(tree_copy, root)
  return tree_copy
}

def main(): Any = {
  var binary_tree: scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]] = scala.collection.mutable.Map(BigInt(1) -> (ArrayBuffer(BigInt(2), BigInt(3))), BigInt(2) -> (ArrayBuffer(BigInt(4), BigInt(5))), BigInt(3) -> (ArrayBuffer(BigInt(6), BigInt(7))), BigInt(7) -> (ArrayBuffer(BigInt(8), BigInt(9))))
  println(_str("Binary tree: " + _str(binary_tree)))
  var mirrored: scala.collection.mutable.Map[BigInt,ArrayBuffer[BigInt]] = binary_tree_mirror(binary_tree, BigInt(1))
  println(_str("Binary tree mirror: " + _str(mirrored)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
