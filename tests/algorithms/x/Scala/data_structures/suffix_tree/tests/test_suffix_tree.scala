// Generated by Mochi v0.10.72 on 2025-08-24 22:29:16 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class SuffixTree(var text: String)

def suffix_tree_new(text: String): SuffixTree = {
  return SuffixTree(text)
}

def suffix_tree_search(st: SuffixTree, pattern: String): Boolean = {
  if (BigInt((pattern).size) == BigInt(0)) {
    return true
  }
  var i: BigInt = BigInt(0)
  var n: BigInt = BigInt((st.text).length)
  var m: BigInt = BigInt((pattern).size)
  val _br0 = new Breaks
  _br0.breakable {
    while (i <= n - m) {
      var j: BigInt = BigInt(0)
      var found: Boolean = true
      val _br1 = new Breaks
      _br1.breakable {
        while (j < m) {
          if (st.text.slice(((i + j).toInt).toInt, ((i + j).toInt).toInt + 1) != pattern.slice((j.toInt).toInt, (j.toInt).toInt + 1)) {
            found = false
            _br1.break()
          }
          j = j + BigInt(1)
        }
      }
      if (found) {
        return true
      }
      i = i + BigInt(1)
    }
  }
  return false
}

var text: String = "banana"

var patterns_exist: ArrayBuffer[String] = ArrayBuffer("ana", "ban", "na")

var i: BigInt = BigInt(0)

var patterns_none: ArrayBuffer[String] = ArrayBuffer("xyz", "apple", "cat")

var substrings: ArrayBuffer[String] = ArrayBuffer("ban", "ana", "a", "na")

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    var st: SuffixTree = suffix_tree_new(text)
    while (i < BigInt((patterns_exist).size)) {
      println(_str(_str(suffix_tree_search(st, patterns_exist.lift((i.toInt).toInt).getOrElse("")))))
      i = i + BigInt(1)
    }
    i = BigInt(0)
    while (i < BigInt((patterns_none).size)) {
      println(_str(_str(suffix_tree_search(st, patterns_none.lift((i.toInt).toInt).getOrElse("")))))
      i = i + BigInt(1)
    }
    println(_str(_str(suffix_tree_search(st, ""))))
    println(_str(_str(suffix_tree_search(st, text))))
    i = BigInt(0)
    while (i < BigInt((substrings).size)) {
      println(_str(_str(suffix_tree_search(st, substrings.lift((i.toInt).toInt).getOrElse("")))))
      i = i + BigInt(1)
    }
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
