// Generated by Mochi v0.10.59 on 2025-08-06 22:13:51 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def segment_image(image: ArrayBuffer[ArrayBuffer[BigInt]], thresholds: ArrayBuffer[BigInt]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var segmented: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((image).size)) {
    var row: ArrayBuffer[BigInt] = ArrayBuffer()
    var j: BigInt = 0
    while (j < BigInt((image((i.toInt).toInt)).size)) {
      val pixel: BigInt = image((i.toInt).toInt)((j.toInt).toInt)
      var label: BigInt = 0
      var k: BigInt = 0
      while (k < BigInt((thresholds).size)) {
        if (pixel > thresholds((k.toInt).toInt)) {
          label = k + 1
        }
        k = k + 1
      }
      row = row :+ label
      j = j + 1
    }
    segmented = segmented :+ row
    i = i + 1
  }
  return segmented
}

def main(): Any = {
  val image: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(80, 120, 180), ArrayBuffer(40, 90, 150), ArrayBuffer(20, 60, 100))
  val thresholds: ArrayBuffer[BigInt] = ArrayBuffer(50, 100, 150)
  val segmented: ArrayBuffer[ArrayBuffer[BigInt]] = segment_image(image, thresholds)
  println(segmented)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
