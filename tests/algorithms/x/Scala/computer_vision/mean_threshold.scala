// Generated by Mochi v0.10.59 on 2025-08-06 22:13:58 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def mean_threshold(image: ArrayBuffer[ArrayBuffer[BigInt]]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  val height: BigInt = BigInt((image).size)
  val width: BigInt = BigInt((image((0.toInt).toInt)).size)
  var total: BigInt = 0
  var i: BigInt = 0
  while (i < height) {
    var j: BigInt = 0
    while (j < width) {
      total = total + image((i.toInt).toInt)((j.toInt).toInt)
      j = j + 1
    }
    i = i + 1
  }
  val mean: BigInt = total / (height * width)
  i = 0
  while (i < height) {
    var j: BigInt = 0
    while (j < width) {
      if (image((i.toInt).toInt)((j.toInt).toInt) > mean) {
        image((i).toInt)((j).toInt) = 255
      } else {
        image((i).toInt)((j).toInt) = 0
      }
      j = j + 1
    }
    i = i + 1
  }
  return image
}

def print_image(image: ArrayBuffer[ArrayBuffer[BigInt]]): Any = {
  var i: BigInt = 0
  while (i < BigInt((image).size)) {
    println(image((i.toInt).toInt))
    i = i + 1
  }
}

val img: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(10, 200, 50), ArrayBuffer(100, 150, 30), ArrayBuffer(90, 80, 220))

val result: ArrayBuffer[ArrayBuffer[BigInt]] = mean_threshold(img)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    print_image(result)
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
