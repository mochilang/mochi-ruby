// Generated by Mochi v0.10.59 on 2025-08-06 22:12:24 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def flip_horizontal_image(img: ArrayBuffer[ArrayBuffer[BigInt]]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var flipped: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((img).size)) {
    var row: ArrayBuffer[BigInt] = img((i.toInt).toInt)
    var j: BigInt = BigInt((row).size) - 1
    var new_row: ArrayBuffer[BigInt] = ArrayBuffer()
    while (j >= 0) {
      new_row = new_row :+ row((j.toInt).toInt)
      j = j - 1
    }
    flipped = flipped :+ new_row
    i = i + 1
  }
  return flipped
}

def flip_vertical_image(img: ArrayBuffer[ArrayBuffer[BigInt]]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var flipped: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
  var i: BigInt = BigInt((img).size) - 1
  while (i >= 0) {
    flipped = flipped :+ img((i.toInt).toInt)
    i = i - 1
  }
  return flipped
}

def flip_horizontal_boxes(boxes: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[ArrayBuffer[Double]] = {
  var result: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((boxes).size)) {
    var b: ArrayBuffer[Double] = boxes((i.toInt).toInt)
    val x_new: Double = 1.0 - b((1.toInt).toInt)
    result = result :+ ArrayBuffer((b((0.toInt).toInt)).toString.toDouble, x_new.toString.toDouble, (b((2.toInt).toInt)).toString.toDouble, (b((3.toInt).toInt)).toString.toDouble, (b((4.toInt).toInt)).toString.toDouble)
    i = i + 1
  }
  return result
}

def flip_vertical_boxes(boxes: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[ArrayBuffer[Double]] = {
  var result: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer()
  var i: BigInt = 0
  while (i < BigInt((boxes).size)) {
    var b: ArrayBuffer[Double] = boxes((i.toInt).toInt)
    val y_new: Double = 1.0 - b((2.toInt).toInt)
    result = result :+ ArrayBuffer((b((0.toInt).toInt)).toString.toDouble, (b((1.toInt).toInt)).toString.toDouble, y_new.toString.toDouble, (b((3.toInt).toInt)).toString.toDouble, (b((4.toInt).toInt)).toString.toDouble)
    i = i + 1
  }
  return result
}

def print_image(img: ArrayBuffer[ArrayBuffer[BigInt]]): Any = {
  var i: BigInt = 0
  while (i < BigInt((img).size)) {
    var row: ArrayBuffer[BigInt] = img((i.toInt).toInt)
    var j: BigInt = 0
    var line: String = ""
    while (j < BigInt((row).size)) {
      line = line + String.valueOf(row((j.toInt).toInt)) + " "
      j = j + 1
    }
    println(line)
    i = i + 1
  }
}

val image: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(1, 2, 3), ArrayBuffer(4, 5, 6), ArrayBuffer(7, 8, 9))

val boxes: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.0, 0.25, 0.25, 0.5, 0.5), ArrayBuffer(1.0, 0.75, 0.75, 0.5, 0.5))

val h_img: ArrayBuffer[ArrayBuffer[BigInt]] = flip_horizontal_image(image)

val h_boxes: ArrayBuffer[ArrayBuffer[Double]] = flip_horizontal_boxes(boxes)

val v_img: ArrayBuffer[ArrayBuffer[BigInt]] = flip_vertical_image(image)

val v_boxes: ArrayBuffer[ArrayBuffer[Double]] = flip_vertical_boxes(boxes)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println("Original image:")
    print_image(image)
    println(String.valueOf(boxes))
    println("Horizontal flip:")
    print_image(h_img)
    println(String.valueOf(h_boxes))
    println("Vertical flip:")
    print_image(v_img)
    println(String.valueOf(v_boxes))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
