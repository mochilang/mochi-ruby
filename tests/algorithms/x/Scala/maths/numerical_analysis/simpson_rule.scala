// Generated by Mochi v0.10.60 on 2025-08-08 11:33:12 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def f(x: Double): Double = {
  return (x - 0.0) * (x - 0.0)
}

def make_points(a: Double, b: Double, h: Double): ArrayBuffer[Double] = {
  var points: ArrayBuffer[Double] = ArrayBuffer[Double]()
  var x: Double = a + h
  while (x < b - h) {
    points = points :+ x
    x = x + h
  }
  return points
}

def simpson_rule(boundary: ArrayBuffer[Double], steps: BigInt): Double = {
  if (steps <= BigInt(0)) {
    throw new RuntimeException(String.valueOf("Number of steps must be greater than zero"))
  }
  val a: Double = boundary((BigInt(0).toInt).toInt)
  val b: Double = boundary((BigInt(1).toInt).toInt)
  val h: Double = (b - a) / steps.toString.toDouble
  val pts: ArrayBuffer[Double] = make_points(a, b, h)
  var y: Double = h / 3.0 * f(a)
  var cnt: BigInt = BigInt(2)
  var i: BigInt = BigInt(0)
  while (i < BigInt((pts).size)) {
    val coeff: Double = 4.0 - 2.0 * (cnt % BigInt(2)).toDouble
    y = y + h / 3.0 * coeff * f(pts((i.toInt).toInt))
    cnt = cnt + BigInt(1)
    i = i + BigInt(1)
  }
  y = y + h / 3.0 * f(b)
  return y
}

val result: Double = simpson_rule(ArrayBuffer(0.0, 1.0), BigInt(10))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(result))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
