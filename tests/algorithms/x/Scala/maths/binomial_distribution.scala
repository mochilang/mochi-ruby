// Generated by Mochi v0.10.67 on 2025-08-16 21:34:57 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def abs(x: Double): Double = {
  if (x < 0.0) {
    return -x
  }
  return x
}

def factorial(n: BigInt): BigInt = {
  if (n < BigInt(0)) {
    throw new RuntimeException(String.valueOf("factorial is undefined for negative numbers"))
  }
  var result: BigInt = BigInt(1)
  var i: BigInt = BigInt(2)
  while (i <= n) {
    result = result * i
    i = i + BigInt(1)
  }
  return result
}

def pow_float(base: Double, exp: BigInt): Double = {
  var result: Double = 1.0
  var i: BigInt = BigInt(0)
  while (i < exp) {
    result = result * base
    i = i + BigInt(1)
  }
  return result
}

def binomial_distribution(successes: BigInt, trials: BigInt, prob: Double): Double = {
  if (successes > trials) {
    throw new RuntimeException(String.valueOf("successes must be lower or equal to trials"))
  }
  if ((trials < BigInt(0) || successes < BigInt(0))) {
    throw new RuntimeException(String.valueOf("the function is defined for non-negative integers"))
  }
  if (!(0.0 < prob && prob < 1.0)) {
    throw new RuntimeException(String.valueOf("prob has to be in range of 1 - 0"))
  }
  var probability: Double = pow_float(prob, successes) * pow_float(1.0 - prob, trials - successes)
  var numerator: Double = (factorial(trials)).toString.toDouble
  var denominator: Double = (factorial(successes) * factorial(trials - successes)).toString.toDouble
  var coefficient: Double = numerator / denominator
  return probability * coefficient
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
