// Generated by Mochi v0.10.67 on 2025-08-17 12:21:39 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def prime_factors(n: BigInt): ArrayBuffer[BigInt] = {
  if (n < BigInt(2)) {
    return ArrayBuffer[BigInt]()
  }
  var num: BigInt = n
  var i: BigInt = BigInt(2)
  var factors: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  while (i * i <= num) {
    if (num % i == BigInt(0)) {
      factors = (factors :+ i)
      num = num / i
    } else {
      i = i + BigInt(1)
    }
  }
  if (num > BigInt(1)) {
    factors = (factors :+ num)
  }
  return factors
}

def list_eq(a: ArrayBuffer[BigInt], b: ArrayBuffer[BigInt]): Boolean = {
  if (BigInt((a).size) != BigInt((b).size)) {
    return false
  }
  var i: BigInt = BigInt(0)
  while (i < BigInt((a).size)) {
    if (a.lift((i.toInt).toInt).getOrElse(BigInt(0)) != b.lift((i.toInt).toInt).getOrElse(BigInt(0))) {
      return false
    }
    i = i + BigInt(1)
  }
  return true
}

def test_prime_factors(): Any = {
  if (!list_eq(prime_factors(BigInt(0)), ArrayBuffer[BigInt]())) {
    throw new RuntimeException(String.valueOf("prime_factors(0) failed"))
  }
  if (!list_eq(prime_factors(BigInt(100)), ArrayBuffer(BigInt(2), BigInt(2), BigInt(5), BigInt(5)))) {
    throw new RuntimeException(String.valueOf("prime_factors(100) failed"))
  }
  if (!list_eq(prime_factors(BigInt(2560)), ArrayBuffer(BigInt(2), BigInt(2), BigInt(2), BigInt(2), BigInt(2), BigInt(2), BigInt(2), BigInt(2), BigInt(2), BigInt(5)))) {
    throw new RuntimeException(String.valueOf("prime_factors(2560) failed"))
  }
  if (!list_eq(prime_factors(BigInt(97)), ArrayBuffer(BigInt(97)))) {
    throw new RuntimeException(String.valueOf("prime_factors(97) failed"))
  }
}

def main(): Any = {
  test_prime_factors()
  println(_str(_str(prime_factors(BigInt(100)))))
  println(_str(_str(prime_factors(BigInt(2560)))))
  println(_str(_str(prime_factors(BigInt(97)))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
