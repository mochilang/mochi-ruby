// Generated by Mochi v0.10.67 on 2025-08-17 12:26:19 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def pow_int(base: BigInt, exp: BigInt): BigInt = {
  var result: BigInt = BigInt(1)
  var i: BigInt = BigInt(0)
  while (i < exp) {
    result = result * base
    i = i + BigInt(1)
  }
  return result
}

def armstrong_number(n: BigInt): Boolean = {
  if (n < BigInt(1)) {
    return false
  }
  var digits: BigInt = BigInt(0)
  var temp: BigInt = n
  while (temp > BigInt(0)) {
    temp = temp / BigInt(10)
    digits = digits + BigInt(1)
  }
  var total: BigInt = BigInt(0)
  temp = n
  while (temp > BigInt(0)) {
    var rem: BigInt = temp % BigInt(10)
    total = total + pow_int(rem, digits)
    temp = temp / BigInt(10)
  }
  return total == n
}

def pluperfect_number(n: BigInt): Boolean = {
  if (n < BigInt(1)) {
    return false
  }
  var digit_histogram: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt(10)) {
    digit_histogram = (digit_histogram :+ BigInt(0))
    i = i + BigInt(1)
  }
  var digit_total: BigInt = BigInt(0)
  var temp: BigInt = n
  while (temp > BigInt(0)) {
    var rem: BigInt = temp % BigInt(10)
    digit_histogram((rem).toInt) = digit_histogram.lift((rem.toInt).toInt).getOrElse(BigInt(0)) + BigInt(1)
    digit_total = digit_total + BigInt(1)
    temp = temp / BigInt(10)
  }
  var total: BigInt = BigInt(0)
  i = BigInt(0)
  while (i < BigInt(10)) {
    if (digit_histogram.lift((i.toInt).toInt).getOrElse(BigInt(0)) > BigInt(0)) {
      total = total + digit_histogram.lift((i.toInt).toInt).getOrElse(BigInt(0)) * pow_int(i, digit_total)
    }
    i = i + BigInt(1)
  }
  return total == n
}

def narcissistic_number(n: BigInt): Boolean = {
  if (n < BigInt(1)) {
    return false
  }
  var digits: BigInt = BigInt(0)
  var temp: BigInt = n
  while (temp > BigInt(0)) {
    temp = temp / BigInt(10)
    digits = digits + BigInt(1)
  }
  temp = n
  var total: BigInt = BigInt(0)
  while (temp > BigInt(0)) {
    var rem: BigInt = temp % BigInt(10)
    total = total + pow_int(rem, digits)
    temp = temp / BigInt(10)
  }
  return total == n
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(armstrong_number(BigInt(371))))
    println(_str(armstrong_number(BigInt(200))))
    println(_str(pluperfect_number(BigInt(371))))
    println(_str(pluperfect_number(BigInt(200))))
    println(_str(narcissistic_number(BigInt(371))))
    println(_str(narcissistic_number(BigInt(200))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
