// Generated by Mochi v0.10.61 on 2025-08-08 16:00:59 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def multiplicative_persistence(num: BigInt): BigInt = {
  if (num < BigInt(0)) {
    throw new RuntimeException(String.valueOf("multiplicative_persistence() does not accept negative values"))
  }
  var steps: BigInt = BigInt(0)
  var n: BigInt = num
  while (n >= BigInt(10)) {
    var product: BigInt = BigInt(1)
    var temp: BigInt = n
    while (temp > BigInt(0)) {
      val digit: BigInt = temp % BigInt(10)
      product = product * digit
      temp = temp / BigInt(10)
    }
    n = product
    steps = steps + BigInt(1)
  }
  return steps
}

def additive_persistence(num: BigInt): BigInt = {
  if (num < BigInt(0)) {
    throw new RuntimeException(String.valueOf("additive_persistence() does not accept negative values"))
  }
  var steps: BigInt = BigInt(0)
  var n: BigInt = num
  while (n >= BigInt(10)) {
    var total: BigInt = BigInt(0)
    var temp: BigInt = n
    while (temp > BigInt(0)) {
      val digit: BigInt = temp % BigInt(10)
      total = total + digit
      temp = temp / BigInt(10)
    }
    n = total
    steps = steps + BigInt(1)
  }
  return steps
}

def test_persistence(): Any = {
  if (multiplicative_persistence(BigInt(217)) != BigInt(2)) {
    throw new RuntimeException(String.valueOf("multiplicative_persistence failed"))
  }
  if (additive_persistence(BigInt(199)) != BigInt(3)) {
    throw new RuntimeException(String.valueOf("additive_persistence failed"))
  }
}

def main(): Any = {
  test_persistence()
  println(String.valueOf(multiplicative_persistence(BigInt(217))))
  println(String.valueOf(additive_persistence(BigInt(199))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
