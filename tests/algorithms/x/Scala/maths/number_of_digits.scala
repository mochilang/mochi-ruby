// Generated by Mochi v0.10.60 on 2025-08-08 11:28:35 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def abs_int(n: BigInt): BigInt = {
  if (n < BigInt(0)) {
    return -n
  }
  return n
}

def num_digits(n: BigInt): BigInt = {
  var x: BigInt = abs_int(n)
  var digits: BigInt = BigInt(1)
  while (x >= BigInt(10)) {
    x = x / BigInt(10)
    digits = digits + BigInt(1)
  }
  return digits
}

def num_digits_fast(n: BigInt): BigInt = {
  var x: BigInt = abs_int(n)
  var digits: BigInt = BigInt(1)
  var power: BigInt = BigInt(10)
  while (x >= power) {
    power = power * BigInt(10)
    digits = digits + BigInt(1)
  }
  return digits
}

def num_digits_faster(n: BigInt): BigInt = {
  val s: String = String.valueOf(abs_int(n))
  return BigInt((s).size)
}

def test_num_digits(): Any = {
  if (num_digits(BigInt(12345)) != BigInt(5)) {
    throw new RuntimeException(String.valueOf("num_digits 12345 failed"))
  }
  if (num_digits(BigInt(123)) != BigInt(3)) {
    throw new RuntimeException(String.valueOf("num_digits 123 failed"))
  }
  if (num_digits(BigInt(0)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("num_digits 0 failed"))
  }
  if (num_digits(-BigInt(1)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("num_digits -1 failed"))
  }
  if (num_digits(-BigInt(123456)) != BigInt(6)) {
    throw new RuntimeException(String.valueOf("num_digits -123456 failed"))
  }
  if (num_digits_fast(BigInt(12345)) != BigInt(5)) {
    throw new RuntimeException(String.valueOf("num_digits_fast 12345 failed"))
  }
  if (num_digits_fast(BigInt(123)) != BigInt(3)) {
    throw new RuntimeException(String.valueOf("num_digits_fast 123 failed"))
  }
  if (num_digits_fast(BigInt(0)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("num_digits_fast 0 failed"))
  }
  if (num_digits_fast(-BigInt(1)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("num_digits_fast -1 failed"))
  }
  if (num_digits_fast(-BigInt(123456)) != BigInt(6)) {
    throw new RuntimeException(String.valueOf("num_digits_fast -123456 failed"))
  }
  if (num_digits_faster(BigInt(12345)) != BigInt(5)) {
    throw new RuntimeException(String.valueOf("num_digits_faster 12345 failed"))
  }
  if (num_digits_faster(BigInt(123)) != BigInt(3)) {
    throw new RuntimeException(String.valueOf("num_digits_faster 123 failed"))
  }
  if (num_digits_faster(BigInt(0)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("num_digits_faster 0 failed"))
  }
  if (num_digits_faster(-BigInt(1)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("num_digits_faster -1 failed"))
  }
  if (num_digits_faster(-BigInt(123456)) != BigInt(6)) {
    throw new RuntimeException(String.valueOf("num_digits_faster -123456 failed"))
  }
}

def main(): Any = {
  test_num_digits()
  println(String.valueOf(num_digits(BigInt(12345))))
  println(String.valueOf(num_digits_fast(BigInt(12345))))
  println(String.valueOf(num_digits_faster(BigInt(12345))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
