// Generated by Mochi v0.10.66 on 2025-08-16 14:55:08 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val s = java.math.BigDecimal.valueOf(d).stripTrailingZeros.toPlainString; if (s.indexOf('.') < 0 && !s.contains("e") && !s.contains("E")) s + ".0" else s }
  case other => String.valueOf(other)
}

def double_factorial_recursive(n: BigInt): BigInt = {
  if (n < BigInt(0)) {
    throw new RuntimeException(String.valueOf("double_factorial_recursive() not defined for negative values"))
  }
  if (n <= BigInt(1)) {
    return BigInt(1)
  }
  return n * double_factorial_recursive(n - BigInt(2))
}

def double_factorial_iterative(n: BigInt): BigInt = {
  if (n < BigInt(0)) {
    throw new RuntimeException(String.valueOf("double_factorial_iterative() not defined for negative values"))
  }
  var result: BigInt = BigInt(1)
  var i: BigInt = n
  while (i > BigInt(0)) {
    result = result * i
    i = i - BigInt(2)
  }
  return result
}

def test_double_factorial(): Any = {
  if (double_factorial_recursive(BigInt(0)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("0!! recursive failed"))
  }
  if (double_factorial_iterative(BigInt(0)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("0!! iterative failed"))
  }
  if (double_factorial_recursive(BigInt(1)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("1!! recursive failed"))
  }
  if (double_factorial_iterative(BigInt(1)) != BigInt(1)) {
    throw new RuntimeException(String.valueOf("1!! iterative failed"))
  }
  if (double_factorial_recursive(BigInt(5)) != BigInt(15)) {
    throw new RuntimeException(String.valueOf("5!! recursive failed"))
  }
  if (double_factorial_iterative(BigInt(5)) != BigInt(15)) {
    throw new RuntimeException(String.valueOf("5!! iterative failed"))
  }
  if (double_factorial_recursive(BigInt(6)) != BigInt(48)) {
    throw new RuntimeException(String.valueOf("6!! recursive failed"))
  }
  if (double_factorial_iterative(BigInt(6)) != BigInt(48)) {
    throw new RuntimeException(String.valueOf("6!! iterative failed"))
  }
  var n: BigInt = BigInt(0)
  while (n <= BigInt(10)) {
    if (double_factorial_recursive(n) != double_factorial_iterative(n)) {
      throw new RuntimeException(String.valueOf("double factorial mismatch"))
    }
    n = n + BigInt(1)
  }
}

def main(): Any = {
  test_double_factorial()
  println(_str(double_factorial_iterative(BigInt(10))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
