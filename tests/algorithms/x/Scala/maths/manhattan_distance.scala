// Generated by Mochi v0.10.60 on 2025-08-08 11:24:55 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def abs_val(x: Double): Double = {
  if (x < 0.0) {
    return -x
  }
  return x
}

def validate_point(p: ArrayBuffer[Double]): Any = {
  if (BigInt((p).size) == BigInt(0)) {
    throw new RuntimeException(String.valueOf("Missing an input"))
  }
}

def manhattan_distance(a: ArrayBuffer[Double], b: ArrayBuffer[Double]): Double = {
  validate_point(a)
  validate_point(b)
  if (BigInt((a).size) != BigInt((b).size)) {
    throw new RuntimeException(String.valueOf("Both points must be in the same n-dimensional space"))
  }
  var total: Double = 0.0
  var i: BigInt = BigInt(0)
  while (i < BigInt((a).size)) {
    total = total + abs_val(a((i.toInt).toInt) - b((i.toInt).toInt))
    i = i + BigInt(1)
  }
  return total
}

def manhattan_distance_one_liner(a: ArrayBuffer[Double], b: ArrayBuffer[Double]): Double = {
  return manhattan_distance(a, b)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(manhattan_distance(ArrayBuffer(1.0, 1.0), ArrayBuffer(2.0, 2.0))))
    println(String.valueOf(manhattan_distance(ArrayBuffer(1.5, 1.5), ArrayBuffer(2.0, 2.0))))
    println(String.valueOf(manhattan_distance_one_liner(ArrayBuffer(1.5, 1.5), ArrayBuffer(2.5, 2.0))))
    println(String.valueOf(manhattan_distance_one_liner(ArrayBuffer((-3.0).toString.toDouble, (-3.0).toString.toDouble, (-3.0).toString.toDouble), ArrayBuffer(0.0, 0.0, 0.0))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
