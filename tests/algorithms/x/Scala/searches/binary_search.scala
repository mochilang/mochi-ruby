// Generated by Mochi v0.10.63 on 2025-08-11 16:31:45 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def is_sorted(arr: ArrayBuffer[BigInt]): Boolean = {
  var i: BigInt = BigInt(1)
  while (i < BigInt((arr).size)) {
    if (arr.lift(((i - BigInt(1)).toInt).toInt).getOrElse(null) > arr.lift((i.toInt).toInt).getOrElse(null)) {
      return false
    }
    i = i + BigInt(1)
  }
  return true
}

def binary_search(sorted_collection: ArrayBuffer[BigInt], item: BigInt): BigInt = {
  if (!is_sorted(sorted_collection)) {
    return -BigInt(1)
  }
  var left: BigInt = BigInt(0)
  var right: BigInt = BigInt((sorted_collection).size) - BigInt(1)
  while (left <= right) {
    val midpoint: BigInt = left + (right - left) / BigInt(2)
    val current_item: BigInt = sorted_collection.lift((midpoint.toInt).toInt).getOrElse(null)
    if (current_item == item) {
      return midpoint
    }
    if (item < current_item) {
      right = midpoint - BigInt(1)
    } else {
      left = midpoint + BigInt(1)
    }
  }
  return -BigInt(1)
}

def binary_search_by_recursion(sorted_collection: ArrayBuffer[BigInt], item: BigInt, left: BigInt, right: BigInt): BigInt = {
  if (right < left) {
    return -BigInt(1)
  }
  val midpoint: BigInt = left + (right - left) / BigInt(2)
  if (sorted_collection.lift((midpoint.toInt).toInt).getOrElse(null) == item) {
    return midpoint
  }
  if (sorted_collection.lift((midpoint.toInt).toInt).getOrElse(null) > item) {
    return binary_search_by_recursion(sorted_collection, item, left, midpoint - BigInt(1))
  }
  return binary_search_by_recursion(sorted_collection, item, midpoint + BigInt(1), right)
}

def exponential_search(sorted_collection: ArrayBuffer[BigInt], item: BigInt): BigInt = {
  if (!is_sorted(sorted_collection)) {
    return -BigInt(1)
  }
  if (BigInt((sorted_collection).size) == BigInt(0)) {
    return -BigInt(1)
  }
  var bound: BigInt = BigInt(1)
  while (bound < BigInt((sorted_collection).size) && sorted_collection.lift((bound.toInt).toInt).getOrElse(null) < item) {
    bound = bound * BigInt(2)
  }
  val left: BigInt = bound / BigInt(2)
  val right: BigInt = ArrayBuffer(bound, BigInt((sorted_collection).size) - BigInt(1)).min
  return binary_search_by_recursion(sorted_collection, item, left, right)
}

def main(): Any = {
  val data: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(0), BigInt(5), BigInt(7), BigInt(10), BigInt(15))
  println(_str(binary_search(data, BigInt(0))))
  println(_str(binary_search(data, BigInt(15))))
  println(_str(binary_search(data, BigInt(5))))
  println(_str(binary_search(data, BigInt(6))))
  println(_str(binary_search_by_recursion(data, BigInt(0), BigInt(0), BigInt((data).size) - BigInt(1))))
  println(_str(binary_search_by_recursion(data, BigInt(15), BigInt(0), BigInt((data).size) - BigInt(1))))
  println(_str(binary_search_by_recursion(data, BigInt(5), BigInt(0), BigInt((data).size) - BigInt(1))))
  println(_str(binary_search_by_recursion(data, BigInt(6), BigInt(0), BigInt((data).size) - BigInt(1))))
  println(_str(exponential_search(data, BigInt(0))))
  println(_str(exponential_search(data, BigInt(15))))
  println(_str(exponential_search(data, BigInt(5))))
  println(_str(exponential_search(data, BigInt(6))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
