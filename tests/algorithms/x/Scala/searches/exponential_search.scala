// Generated by Mochi v0.10.63 on 2025-08-11 16:31:59 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def is_sorted(xs: ArrayBuffer[BigInt]): Boolean = {
  var i: BigInt = BigInt(1)
  while (i < BigInt((xs).size)) {
    if (xs.lift(((i - BigInt(1)).toInt).toInt).getOrElse(null) > xs.lift((i.toInt).toInt).getOrElse(null)) {
      return false
    }
    i = i + BigInt(1)
  }
  return true
}

def exponential_search(arr: ArrayBuffer[BigInt], item: BigInt): BigInt = {
  if (!is_sorted(arr)) {
    throw new RuntimeException(String.valueOf("sorted_collection must be sorted in ascending order"))
  }
  if (BigInt((arr).size) == BigInt(0)) {
    return -BigInt(1)
  }
  if (arr.lift((BigInt(0).toInt).toInt).getOrElse(null) == item) {
    return BigInt(0)
  }
  var bound: BigInt = BigInt(1)
  while (bound < BigInt((arr).size) && arr.lift((bound.toInt).toInt).getOrElse(null) < item) {
    bound = bound * BigInt(2)
  }
  var left: BigInt = bound / BigInt(2)
  var right: BigInt = bound
  if (right >= BigInt((arr).size)) {
    right = BigInt((arr).size) - BigInt(1)
  }
  while (left <= right) {
    val mid: BigInt = left + (right - left) / BigInt(2)
    if (arr.lift((mid.toInt).toInt).getOrElse(null) == item) {
      return mid
    }
    if (arr.lift((mid.toInt).toInt).getOrElse(null) > item) {
      right = mid - BigInt(1)
    } else {
      left = mid + BigInt(1)
    }
  }
  return -BigInt(1)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
