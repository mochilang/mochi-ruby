// Generated by Mochi v0.10.63 on 2025-08-11 16:31:54 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def search(list_data: ArrayBuffer[BigInt], key: BigInt, left: BigInt, right: BigInt): BigInt = {
  var r: BigInt = right
  if (r == BigInt(0)) {
    r = BigInt((list_data).size) - BigInt(1)
  }
  if (left > r) {
    return -BigInt(1)
  } else {
    if (list_data.lift((left.toInt).toInt).getOrElse(null) == key) {
      return left
    } else {
      if (list_data.lift((r.toInt).toInt).getOrElse(null) == key) {
        return r
      } else {
        return search(list_data, key, left + BigInt(1), r - BigInt(1))
      }
    }
  }
  return null
}

def main(): Any = {
  println(search(ArrayBuffer(BigInt(0), BigInt(1), BigInt(2), BigInt(3), BigInt(4), BigInt(5), BigInt(6), BigInt(7), BigInt(8), BigInt(9), BigInt(10)), BigInt(5), BigInt(0), BigInt(0)))
  println(search(ArrayBuffer(BigInt(1), BigInt(2), BigInt(4), BigInt(5), BigInt(3)), BigInt(4), BigInt(0), BigInt(0)))
  println(search(ArrayBuffer(BigInt(1), BigInt(2), BigInt(4), BigInt(5), BigInt(3)), BigInt(6), BigInt(0), BigInt(0)))
  println(search(ArrayBuffer(BigInt(5)), BigInt(5), BigInt(0), BigInt(0)))
  println(search(ArrayBuffer[BigInt](), BigInt(1), BigInt(0), BigInt(0)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
