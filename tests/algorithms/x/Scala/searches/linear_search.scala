// Generated by Mochi v0.10.63 on 2025-08-11 16:32:21 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def linear_search(sequence: ArrayBuffer[BigInt], target: BigInt): BigInt = {
  var i: BigInt = BigInt(0)
  while (i < BigInt((sequence).size)) {
    if (sequence.lift((i.toInt).toInt).getOrElse(null) == target) {
      return i
    }
    i = i + BigInt(1)
  }
  return -BigInt(1)
}

def rec_linear_search(sequence: ArrayBuffer[BigInt], low: BigInt, high: BigInt, target: BigInt): BigInt = {
  if (!(((BigInt(0) <= high && high < BigInt((sequence).size)).asInstanceOf[Boolean] && BigInt(0) <= low).asInstanceOf[Boolean] && low < BigInt((sequence).size)).asInstanceOf[Boolean]) {
    throw new RuntimeException(String.valueOf("Invalid upper or lower bound!"))
  }
  if (high < low) {
    return -BigInt(1)
  }
  if (sequence.lift((low.toInt).toInt).getOrElse(null) == target) {
    return low
  }
  if (sequence.lift((high.toInt).toInt).getOrElse(null) == target) {
    return high
  }
  return rec_linear_search(sequence, low + BigInt(1), high - BigInt(1), target)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(linear_search(ArrayBuffer(BigInt(0), BigInt(5), BigInt(7), BigInt(10), BigInt(15)), BigInt(0))))
    println(_str(linear_search(ArrayBuffer(BigInt(0), BigInt(5), BigInt(7), BigInt(10), BigInt(15)), BigInt(15))))
    println(_str(linear_search(ArrayBuffer(BigInt(0), BigInt(5), BigInt(7), BigInt(10), BigInt(15)), BigInt(5))))
    println(_str(linear_search(ArrayBuffer(BigInt(0), BigInt(5), BigInt(7), BigInt(10), BigInt(15)), BigInt(6))))
    println(_str(rec_linear_search(ArrayBuffer(BigInt(0), BigInt(30), BigInt(500), BigInt(100), BigInt(700)), BigInt(0), BigInt(4), BigInt(0))))
    println(_str(rec_linear_search(ArrayBuffer(BigInt(0), BigInt(30), BigInt(500), BigInt(100), BigInt(700)), BigInt(0), BigInt(4), BigInt(700))))
    println(_str(rec_linear_search(ArrayBuffer(BigInt(0), BigInt(30), BigInt(500), BigInt(100), BigInt(700)), BigInt(0), BigInt(4), BigInt(30))))
    println(_str(rec_linear_search(ArrayBuffer(BigInt(0), BigInt(30), BigInt(500), BigInt(100), BigInt(700)), BigInt(0), BigInt(4), -BigInt(6))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
