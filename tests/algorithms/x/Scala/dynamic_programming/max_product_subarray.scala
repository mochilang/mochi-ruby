// Generated by Mochi v0.10.59 on 2025-08-07 08:40:19 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def max_product_subarray(numbers: ArrayBuffer[BigInt]): BigInt = {
  if (BigInt((numbers).size) == 0) {
    return 0
  }
  var max_till_now: BigInt = numbers((0.toInt).toInt)
  var min_till_now: BigInt = numbers((0.toInt).toInt)
  var max_prod: BigInt = numbers((0.toInt).toInt)
  var i: BigInt = 1
  while (i < BigInt((numbers).size)) {
    val number: BigInt = numbers((i.toInt).toInt)
    if (number < 0) {
      val temp: BigInt = max_till_now
      max_till_now = min_till_now
      min_till_now = temp
    }
    val prod_max: BigInt = max_till_now * number
    if (number > prod_max) {
      max_till_now = number
    } else {
      max_till_now = prod_max
    }
    val prod_min: BigInt = min_till_now * number
    if (number < prod_min) {
      min_till_now = number
    } else {
      min_till_now = prod_min
    }
    if (max_till_now > max_prod) {
      max_prod = max_till_now
    }
    i = i + 1
  }
  return max_prod
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(max_product_subarray(ArrayBuffer(2, 3, BigInt((-2).toString.toDouble.toInt), 4)))
    println(max_product_subarray(ArrayBuffer(BigInt((-2).toString.toDouble.toInt), 0, BigInt((-1).toString.toDouble.toInt))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
