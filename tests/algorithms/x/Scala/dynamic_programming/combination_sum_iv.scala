// Generated by Mochi v0.10.59 on 2025-08-07 00:57:53 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def make_list(len: BigInt, value: BigInt): ArrayBuffer[BigInt] = {
  var arr: ArrayBuffer[BigInt] = ArrayBuffer()
  var i: BigInt = 0
  while (i < len) {
    arr = arr :+ value
    i = i + 1
  }
  return arr
}

def count_recursive(array: ArrayBuffer[BigInt], target: BigInt): BigInt = {
  if (target < 0) {
    return 0
  }
  if (target == 0) {
    return 1
  }
  var total: BigInt = 0
  var i: BigInt = 0
  while (i < BigInt((array).size)) {
    total = total + count_recursive(array, target - array((i.toInt).toInt))
    i = i + 1
  }
  return total
}

def combination_sum_iv(array: ArrayBuffer[BigInt], target: BigInt): BigInt = {
  return count_recursive(array, target)
}

def count_dp(array: ArrayBuffer[BigInt], target: BigInt, dp: ArrayBuffer[BigInt]): BigInt = {
  if (target < 0) {
    return 0
  }
  if (target == 0) {
    return 1
  }
  if (dp((target.toInt).toInt) > 0 - 1) {
    return dp((target.toInt).toInt)
  }
  var total: BigInt = 0
  var i: BigInt = 0
  while (i < BigInt((array).size)) {
    total = total + count_dp(array, target - array((i.toInt).toInt), dp)
    i = i + 1
  }
  dp((target).toInt) = total
  return total
}

def combination_sum_iv_dp_array(array: ArrayBuffer[BigInt], target: BigInt): BigInt = {
  var dp: ArrayBuffer[BigInt] = make_list(target + 1, -1)
  return count_dp(array, target, dp)
}

def combination_sum_iv_bottom_up(n: BigInt, array: ArrayBuffer[BigInt], target: BigInt): BigInt = {
  var dp: ArrayBuffer[BigInt] = make_list(target + 1, 0)
  dp((0).toInt) = 1
  var i: BigInt = 1
  while (i <= target) {
    var j: BigInt = 0
    while (j < n) {
      if (i - array((j.toInt).toInt) >= 0) {
        dp((i).toInt) = dp((i.toInt).toInt) + dp(((i - array((j.toInt).toInt)).toInt).toInt)
      }
      j = j + 1
    }
    i = i + 1
  }
  return dp((target.toInt).toInt)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(combination_sum_iv(ArrayBuffer(1, 2, 5), 5)))
    println(String.valueOf(combination_sum_iv_dp_array(ArrayBuffer(1, 2, 5), 5)))
    println(String.valueOf(combination_sum_iv_bottom_up(3, ArrayBuffer(1, 2, 5), 5)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
