// Generated by Mochi v0.10.62 on 2025-08-09 16:11:20 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole()) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def min3(a: BigInt, b: BigInt, c: BigInt): BigInt = {
  var m: BigInt = a
  if (b < m) {
    m = b
  }
  if (c < m) {
    m = c
  }
  return m
}

def helper_top_down(word1: String, word2: String, dp: ArrayBuffer[ArrayBuffer[BigInt]], i: BigInt, j: BigInt): BigInt = {
  if (i < BigInt(0)) {
    return j + BigInt(1)
  }
  if (j < BigInt(0)) {
    return i + BigInt(1)
  }
  if (dp.lift((i.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((j.toInt).toInt).getOrElse(null) != BigInt(-1)) {
    return dp.lift((i.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((j.toInt).toInt).getOrElse(null)
  }
  if (word1.slice((i).toInt, (i + BigInt(1)).toInt) == word2.slice((j).toInt, (j + BigInt(1)).toInt)) {
    dp((i).toInt)((j).toInt) = helper_top_down(word1, word2, dp, i - BigInt(1), j - BigInt(1))
  } else {
    val insert: BigInt = helper_top_down(word1, word2, dp, i, j - BigInt(1))
    val delete: BigInt = helper_top_down(word1, word2, dp, i - BigInt(1), j)
    val replace: BigInt = helper_top_down(word1, word2, dp, i - BigInt(1), j - BigInt(1))
    dp((i).toInt)((j).toInt) = BigInt(1) + min3(insert, delete, replace)
  }
  return dp.lift((i.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((j.toInt).toInt).getOrElse(null)
}

def min_dist_top_down(word1: String, word2: String): BigInt = {
  val m: BigInt = BigInt((word1).size)
  val n: BigInt = BigInt((word2).size)
  var dp: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer[ArrayBuffer[BigInt]]()
  for (_ <- (BigInt(0)).toInt until (m).toInt) {
    var row: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
    for (_2 <- (BigInt(0)).toInt until (n).toInt) {
      row = row :+ (BigInt(-1))
    }
    dp = dp :+ (row)
  }
  return helper_top_down(word1, word2, dp, m - BigInt(1), n - BigInt(1))
}

def min_dist_bottom_up(word1: String, word2: String): BigInt = {
  val m: BigInt = BigInt((word1).size)
  val n: BigInt = BigInt((word2).size)
  var dp: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer[ArrayBuffer[BigInt]]()
  for (_ <- (BigInt(0)).toInt until (m + BigInt(1)).toInt) {
    var row: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
    for (_2 <- (BigInt(0)).toInt until (n + BigInt(1)).toInt) {
      row = row :+ (BigInt(0))
    }
    dp = dp :+ (row)
  }
  for (i <- (BigInt(0)).toInt until (m + BigInt(1)).toInt) {
    for (j <- (BigInt(0)).toInt until (n + BigInt(1)).toInt) {
      if (i == BigInt(0)) {
        dp((i).toInt)((j).toInt) = BigInt(j.toString.toDouble.toInt)
      } else {
        if (j == BigInt(0)) {
          dp((i).toInt)((j).toInt) = BigInt(i.toString.toDouble.toInt)
        } else {
          if (word1.slice((i - BigInt(1)).toInt, (i).toInt) == word2.slice((j - BigInt(1)).toInt, (j).toInt)) {
            dp((i).toInt)((j).toInt) = dp.lift(((i - BigInt(1)).toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift(((j - BigInt(1)).toInt).toInt).getOrElse(null)
          } else {
            val insert: BigInt = dp.lift((i.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift(((j - BigInt(1)).toInt).toInt).getOrElse(null)
            val delete: BigInt = dp.lift(((i - BigInt(1)).toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((j.toInt).toInt).getOrElse(null)
            val replace: BigInt = dp.lift(((i - BigInt(1)).toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift(((j - BigInt(1)).toInt).toInt).getOrElse(null)
            dp((i).toInt)((j).toInt) = BigInt(1) + min3(insert, delete, replace)
          }
        }
      }
    }
  }
  return dp.lift((m.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((n.toInt).toInt).getOrElse(null)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(min_dist_top_down("intention", "execution")))
    println(_str(min_dist_top_down("intention", "")))
    println(_str(min_dist_top_down("", "")))
    println(_str(min_dist_bottom_up("intention", "execution")))
    println(_str(min_dist_bottom_up("intention", "")))
    println(_str(min_dist_bottom_up("", "")))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
