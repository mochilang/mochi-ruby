// Generated by Mochi v0.10.71 on 2025-08-24 09:42:16 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

private def _ord(s: String): Int = {
  if (s != null && s.nonEmpty) s.codePointAt(0) else 0
}

def index_of(s: String, ch: String): BigInt = {
  var i: BigInt = BigInt(0)
  while (i < BigInt((s).size)) {
    if (s.slice((i.toInt).toInt, (i.toInt).toInt + 1) == ch) {
      return i
    }
    i = i + BigInt(1)
  }
  return BigInt(-1)
}

def ord(ch: String): BigInt = {
  var upper: String = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
  var lower: String = "abcdefghijklmnopqrstuvwxyz"
  var idx: BigInt = index_of(upper, ch)
  if (idx >= BigInt(0)) {
    return BigInt(65) + idx
  }
  idx = index_of(lower, ch)
  if (idx >= BigInt(0)) {
    return BigInt(97) + idx
  }
  return BigInt(0)
}

def chr(n: BigInt): String = {
  var upper: String = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
  var lower: String = "abcdefghijklmnopqrstuvwxyz"
  if ((n >= BigInt(65) && n < BigInt(91))) {
    return upper.slice((n - BigInt(65)).toInt, (n - BigInt(64)).toInt)
  }
  if ((n >= BigInt(97) && n < BigInt(123))) {
    return lower.slice((n - BigInt(97)).toInt, (n - BigInt(96)).toInt)
  }
  return "?"
}

def to_upper_char(c: String): String = {
  var code: BigInt = _ord(c)
  if ((code >= BigInt(97) && code <= BigInt(122))) {
    return chr(code - BigInt(32))
  }
  return c
}

def is_lower(c: String): Boolean = {
  var code: BigInt = _ord(c)
  return (code >= BigInt(97) && code <= BigInt(122)).asInstanceOf[Boolean]
}

def abbr(a: String, b: String): Boolean = {
  var n: BigInt = BigInt((a).size)
  var m: BigInt = BigInt((b).size)
  var dp: ArrayBuffer[ArrayBuffer[Boolean]] = ArrayBuffer[ArrayBuffer[Boolean]]()
  var i: BigInt = BigInt(0)
  while (i <= n) {
    var row: ArrayBuffer[Boolean] = ArrayBuffer[Boolean]()
    var j: BigInt = BigInt(0)
    while (j <= m) {
      row = ({row += (false); row})
      j = j + BigInt(1)
    }
    dp = ({dp += (row); dp})
    i = i + BigInt(1)
  }
  dp((BigInt(0)).toInt)((BigInt(0)).toInt) = true
  i = BigInt(0)
  while (i < n) {
    var j: BigInt = BigInt(0)
    while (j <= m) {
      if (dp.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Boolean]()).lift((j.toInt).toInt).getOrElse(false)) {
        if ((j < m && to_upper_char((a.slice((i.toInt).toInt, (i.toInt).toInt + 1)).toString) == b.slice((j.toInt).toInt, (j.toInt).toInt + 1))) {
          dp((i + BigInt(1)).toInt)((j + BigInt(1)).toInt) = true
        }
        if (is_lower((a.slice((i.toInt).toInt, (i.toInt).toInt + 1)).toString)) {
          dp((i + BigInt(1)).toInt)((j).toInt) = true
        }
      }
      j = j + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return dp.lift((n.toInt).toInt).getOrElse(ArrayBuffer[Boolean]()).lift((m.toInt).toInt).getOrElse(false)
}

def print_bool(b: Boolean): Any = {
  if (b) {
    println(_str(true))
  } else {
    println(_str(false))
  }
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    print_bool(abbr("daBcd", "ABC"))
    print_bool(abbr("dBcd", "ABC"))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
