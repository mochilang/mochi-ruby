// Generated by Mochi v0.10.59 on 2025-08-07 08:40:11 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def maximum_non_adjacent_sum(nums: ArrayBuffer[BigInt]): BigInt = {
  if (BigInt((nums).size) == 0) {
    return 0
  }
  var max_including: BigInt = nums((0.toInt).toInt)
  var max_excluding: BigInt = 0
  var i: BigInt = 1
  while (i < BigInt((nums).size)) {
    val num: BigInt = nums((i.toInt).toInt)
    val new_including: BigInt = max_excluding + num
    val new_excluding: BigInt = (if (max_including > max_excluding) max_including else max_excluding)
    max_including = new_including
    max_excluding = new_excluding
    i = i + 1
  }
  if (max_including > max_excluding) {
    return max_including
  }
  return max_excluding
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(maximum_non_adjacent_sum(ArrayBuffer(1, 2, 3))))
    println(String.valueOf(maximum_non_adjacent_sum(ArrayBuffer(1, 5, 3, 7, 2, 2, 6))))
    println(String.valueOf(maximum_non_adjacent_sum(ArrayBuffer(BigInt((-1).toString.toDouble.toInt), BigInt((-5).toString.toDouble.toInt), BigInt((-3).toString.toDouble.toInt), BigInt((-7).toString.toDouble.toInt), BigInt((-2).toString.toDouble.toInt), BigInt((-2).toString.toDouble.toInt), BigInt((-6).toString.toDouble.toInt)))))
    println(String.valueOf(maximum_non_adjacent_sum(ArrayBuffer(499, 500, BigInt((-3).toString.toDouble.toInt), BigInt((-7).toString.toDouble.toInt), BigInt((-2).toString.toDouble.toInt), BigInt((-2).toString.toDouble.toInt), BigInt((-6).toString.toDouble.toInt)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
