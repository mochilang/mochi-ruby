// Generated by Mochi v0.10.59 on 2025-08-06 21:27:43 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def index_of(s: String, c: String): BigInt = {
  var i: BigInt = 0
  while (i < BigInt((s).size)) {
    if (s.slice((i.toInt).toInt, (i.toInt).toInt + 1) == c) {
      return i
    }
    i = i + 1
  }
  return BigInt((-1).toString.toDouble.toInt)
}

def atbash(sequence: String): String = {
  val lower: String = "abcdefghijklmnopqrstuvwxyz"
  val upper: String = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
  val lower_rev: String = "zyxwvutsrqponmlkjihgfedcba"
  val upper_rev: String = "ZYXWVUTSRQPONMLKJIHGFEDCBA"
  var result: String = ""
  var i: BigInt = 0
  while (i < BigInt((sequence).size)) {
    val ch = sequence.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    val idx: BigInt = index_of(lower, ch)
    if (idx != -1) {
      result = result + lower_rev.slice((idx.toInt).toInt, (idx.toInt).toInt + 1)
    } else {
      val idx2: BigInt = index_of(upper, ch)
      if (idx2 != -1) {
        result = result + upper_rev.slice((idx2.toInt).toInt, (idx2.toInt).toInt + 1)
      } else {
        result = result + ch
      }
    }
    i = i + 1
  }
  return result
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(atbash("ABCDEFGH"))
    println(atbash("123GGjj"))
    println(atbash("testStringtest"))
    println(atbash("with space"))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
