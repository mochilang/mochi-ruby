// Generated by Mochi v0.10.59 on 2025-08-06 21:27:31 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def gcd(a: BigInt, b: BigInt): BigInt = {
  var x: BigInt = a
  var y: BigInt = b
  while (y != 0) {
    val temp: BigInt = x % y
    x = y
    y = temp
  }
  return x
}

def mod_inverse(a: BigInt, m: BigInt): BigInt = {
  if (gcd(a, m) != 1) {
    throw new RuntimeException(String.valueOf("mod inverse of " + String.valueOf(a) + " and " + String.valueOf(m) + " does not exist"))
  }
  var u1: BigInt = 1
  var u2: BigInt = 0
  var u3: BigInt = a
  var v1: BigInt = 0
  var v2: BigInt = 1
  var v3: BigInt = m
  while (v3 != 0) {
    val q: BigInt = u3 / v3
    val t1: BigInt = u1 - q * v1
    val t2: BigInt = u2 - q * v2
    val t3: BigInt = u3 - q * v3
    u1 = v1
    u2 = v2
    u3 = v3
    v1 = t1
    v2 = t2
    v3 = t3
  }
  val res: BigInt = u1 % m
  if (res < 0) {
    return res + m
  }
  return res
}

def find_symbol(ch: String): BigInt = {
  var i: BigInt = 0
  while (i < BigInt((SYMBOLS).length)) {
    if (SYMBOLS.slice((i.toInt).toInt, (i.toInt).toInt + 1) == ch) {
      return i
    }
    i = i + 1
  }
  return BigInt((-1).toString.toDouble.toInt)
}

def check_keys(key_a: BigInt, key_b: BigInt, mode: String): Any = {
  val m: BigInt = BigInt((SYMBOLS).length)
  if (mode == "encrypt") {
    if (key_a == 1) {
      throw new RuntimeException(String.valueOf("The affine cipher becomes weak when key A is set to 1. Choose different key"))
    }
    if (key_b == 0) {
      throw new RuntimeException(String.valueOf("The affine cipher becomes weak when key B is set to 0. Choose different key"))
    }
  }
  if (((key_a < 0 || key_b < 0).asInstanceOf[Boolean] || key_b > m - 1).asInstanceOf[Boolean]) {
    throw new RuntimeException(String.valueOf("Key A must be greater than 0 and key B must be between 0 and " + String.valueOf(m - 1)))
  }
  if (gcd(key_a, m) != 1) {
    throw new RuntimeException(String.valueOf("Key A " + String.valueOf(key_a) + " and the symbol set size " + String.valueOf(m) + " are not relatively prime. Choose a different key."))
  }
}

def encrypt_message(key: BigInt, message: String): String = {
  val m: BigInt = BigInt((SYMBOLS).length)
  val key_a: BigInt = key / m
  val key_b: BigInt = key % m
  check_keys(key_a, key_b, "encrypt")
  var cipher_text: String = ""
  var i: BigInt = 0
  while (i < BigInt((message).size)) {
    val ch = message.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    val index: BigInt = find_symbol(ch)
    if (index >= 0) {
      cipher_text = cipher_text + SYMBOLS.slice((((index * key_a + key_b) % m).toInt).toInt, (((index * key_a + key_b) % m).toInt).toInt + 1)
    } else {
      cipher_text = cipher_text + ch
    }
    i = i + 1
  }
  return cipher_text
}

def decrypt_message(key: BigInt, message: String): String = {
  val m: BigInt = BigInt((SYMBOLS).length)
  val key_a: BigInt = key / m
  val key_b: BigInt = key % m
  check_keys(key_a, key_b, "decrypt")
  val inv: BigInt = mod_inverse(key_a, m)
  var plain_text: String = ""
  var i: BigInt = 0
  while (i < BigInt((message).size)) {
    val ch = message.slice((i.toInt).toInt, (i.toInt).toInt + 1)
    val index: BigInt = find_symbol(ch)
    if (index >= 0) {
      var n: BigInt = (index - key_b) * inv
      val pos: BigInt = n % m
      val `final`: BigInt = (if (pos < 0) pos + m else pos)
      plain_text = plain_text + SYMBOLS.slice((`final`.toInt).toInt, (`final`.toInt).toInt + 1)
    } else {
      plain_text = plain_text + ch
    }
    i = i + 1
  }
  return plain_text
}

def main(): Any = {
  val key: BigInt = 4545
  val msg: String = "The affine cipher is a type of monoalphabetic substitution cipher."
  val enc: String = encrypt_message(key, msg)
  println(enc)
  println(decrypt_message(key, enc))
}

val SYMBOLS: String = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~"

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
