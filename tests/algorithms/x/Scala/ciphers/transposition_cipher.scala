// Generated by Mochi v0.10.59 on 2025-08-06 22:00:21 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def join_strings(xs: ArrayBuffer[String]): String = {
  var res: String = ""
  var i: BigInt = 0
  while (i < BigInt((xs).size)) {
    res = res + xs((i.toInt).toInt)
    i = i + 1
  }
  return res
}

def encrypt_message(key: BigInt, message: String): String = {
  var result: String = ""
  var col: BigInt = 0
  while (col < key) {
    var pointer: BigInt = col
    while (pointer < BigInt((message).size)) {
      result = result + message.slice((pointer).toInt, (pointer + 1).toInt)
      pointer = pointer + key
    }
    col = col + 1
  }
  return result
}

def decrypt_message(key: BigInt, message: String): String = {
  val num_cols: BigInt = (BigInt((message).size) + key - 1) / key
  val num_rows: BigInt = key
  val num_shaded_boxes: BigInt = num_cols * num_rows - BigInt((message).size)
  var plain_text: ArrayBuffer[String] = ArrayBuffer()
  var i: BigInt = 0
  while (i < num_cols) {
    plain_text = plain_text :+ ""
    i = i + 1
  }
  var col: BigInt = 0
  var row: BigInt = 0
  var index: BigInt = 0
  while (index < BigInt((message).size)) {
    plain_text((col).toInt) = plain_text((col.toInt).toInt) + message.slice((index).toInt, (index + 1).toInt)
    col = col + 1
    if ((col == num_cols || (col == num_cols - 1 && row >= num_rows - num_shaded_boxes).asInstanceOf[Boolean]).asInstanceOf[Boolean]) {
      col = 0
      row = row + 1
    }
    index = index + 1
  }
  return join_strings(plain_text)
}

def main(): Any = {
  println("Enter message: ")
  val message: String = (Option(scala.io.StdIn.readLine())).getOrElse("q")
  val max_key: BigInt = BigInt((message).size) - 1
  println("Enter key [2-" + String.valueOf(max_key) + "]: ")
  val key: BigInt = (Option(scala.io.StdIn.readLine())).getOrElse("q").toInt
  println("Encryption/Decryption [e/d]: ")
  val mode: String = (Option(scala.io.StdIn.readLine())).getOrElse("q")
  var text: String = ""
  val first: String = mode.slice((0).toInt, (1).toInt)
  if ((first == "e" || first == "E").asInstanceOf[Boolean]) {
    text = encrypt_message(key, message)
  } else {
    if ((first == "d" || first == "D").asInstanceOf[Boolean]) {
      text = decrypt_message(key, message)
    }
  }
  println("Output:\n" + text + "|")
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
