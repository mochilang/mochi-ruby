// Generated by Mochi v0.10.63 on 2025-08-11 16:32:57 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def bead_sort(sequence: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  val n: BigInt = BigInt((sequence).size)
  var i: BigInt = BigInt(0)
  while (i < n) {
    if (sequence.lift((i.toInt).toInt).getOrElse(null) < BigInt(0)) {
      throw new RuntimeException(String.valueOf("Sequence must be list of non-negative integers"))
    }
    i = i + BigInt(1)
  }
  var pass: BigInt = BigInt(0)
  while (pass < n) {
    var j: BigInt = BigInt(0)
    while (j < n - BigInt(1)) {
      val upper: BigInt = sequence.lift((j.toInt).toInt).getOrElse(null)
      val lower: BigInt = sequence.lift(((j + BigInt(1)).toInt).toInt).getOrElse(null)
      if (upper > lower) {
        val diff: BigInt = upper - lower
        sequence((j).toInt) = upper - diff
        sequence((j + BigInt(1)).toInt) = lower + diff
      }
      j = j + BigInt(1)
    }
    pass = pass + BigInt(1)
  }
  return sequence
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(bead_sort(ArrayBuffer(BigInt(6), BigInt(11), BigInt(12), BigInt(4), BigInt(1), BigInt(5)))))
    println(_str(bead_sort(ArrayBuffer(BigInt(9), BigInt(8), BigInt(7), BigInt(6), BigInt(5), BigInt(4), BigInt(3), BigInt(2), BigInt(1)))))
    println(_str(bead_sort(ArrayBuffer(BigInt(5), BigInt(0), BigInt(4), BigInt(3)))))
    println(_str(bead_sort(ArrayBuffer(BigInt(8), BigInt(2), BigInt(1)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
