// Generated by Mochi v0.10.63 on 2025-08-11 17:31:19 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def concat(a: ArrayBuffer[BigInt], b: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var result: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  for (x <- a) {
    result = result :+ (x)
  }
  for (x <- b) {
    result = result :+ (x)
  }
  return result
}

def quick_sort(data: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  if (BigInt((data).size) <= BigInt(1)) {
    return data
  }
  val pivot: BigInt = data.lift((BigInt(0).toInt).toInt).getOrElse(null)
  var left: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var right: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(1)
  while (i < BigInt((data).size)) {
    val e: BigInt = data.lift((i.toInt).toInt).getOrElse(null)
    if (e <= pivot) {
      left = left :+ (e)
    } else {
      right = right :+ (e)
    }
    i = i + BigInt(1)
  }
  val sorted_left: ArrayBuffer[BigInt] = quick_sort(left)
  val sorted_right: ArrayBuffer[BigInt] = quick_sort(right)
  val left_pivot: ArrayBuffer[BigInt] = sorted_left :+ (pivot)
  return (left_pivot ++ sorted_right).asInstanceOf[ArrayBuffer[BigInt]]
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(quick_sort(ArrayBuffer(BigInt(2), BigInt(1), BigInt(0)))))
    println(_str(quick_sort(ArrayBuffer(BigInt(3), BigInt(5), BigInt(2), BigInt(4), BigInt(1)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
