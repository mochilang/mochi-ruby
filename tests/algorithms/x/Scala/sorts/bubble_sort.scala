// Generated by Mochi v0.10.63 on 2025-08-11 16:33:13 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def bubble_sort_iterative(collection: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var n: BigInt = BigInt((collection).size)
  val _br0 = new Breaks
  _br0.breakable {
    while (n > BigInt(0)) {
      var swapped: Boolean = false
      var j: BigInt = BigInt(0)
      while (j < n - BigInt(1)) {
        if (collection.lift((j.toInt).toInt).getOrElse(null) > collection.lift(((j + BigInt(1)).toInt).toInt).getOrElse(null)) {
          val temp: BigInt = collection.lift((j.toInt).toInt).getOrElse(null)
          collection((j).toInt) = collection.lift(((j + BigInt(1)).toInt).toInt).getOrElse(null)
          collection((j + BigInt(1)).toInt) = temp
          swapped = true
        }
        j = j + BigInt(1)
      }
      if (!swapped) {
        _br0.break()
      }
      n = n - BigInt(1)
    }
  }
  return collection
}

def bubble_sort_recursive(collection: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var n: BigInt = BigInt((collection).size)
  var swapped: Boolean = false
  var i: BigInt = BigInt(0)
  while (i < n - BigInt(1)) {
    if (collection.lift((i.toInt).toInt).getOrElse(null) > collection.lift(((i + BigInt(1)).toInt).toInt).getOrElse(null)) {
      val temp: BigInt = collection.lift((i.toInt).toInt).getOrElse(null)
      collection((i).toInt) = collection.lift(((i + BigInt(1)).toInt).toInt).getOrElse(null)
      collection((i + BigInt(1)).toInt) = temp
      swapped = true
    }
    i = i + BigInt(1)
  }
  if (swapped) {
    return bubble_sort_recursive(collection)
  }
  return collection
}

def copy_list(xs: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var out: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((xs).size)) {
    out = out :+ (xs.lift((i.toInt).toInt).getOrElse(null))
    i = i + BigInt(1)
  }
  return out
}

def list_eq(a: ArrayBuffer[BigInt], b: ArrayBuffer[BigInt]): Boolean = {
  if (BigInt((a).size) != BigInt((b).size)) {
    return false
  }
  var k: BigInt = BigInt(0)
  while (k < BigInt((a).size)) {
    if (a.lift((k.toInt).toInt).getOrElse(null) != b.lift((k.toInt).toInt).getOrElse(null)) {
      return false
    }
    k = k + BigInt(1)
  }
  return true
}

def test_bubble_sort(): Any = {
  val example: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(0), BigInt(5), BigInt(2), BigInt(3), BigInt(2))
  val expected: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(0), BigInt(2), BigInt(2), BigInt(3), BigInt(5))
  if (!list_eq(bubble_sort_iterative(copy_list(example)), expected)) {
    throw new RuntimeException(String.valueOf("iterative failed"))
  }
  if (!list_eq(bubble_sort_recursive(copy_list(example)), expected)) {
    throw new RuntimeException(String.valueOf("recursive failed"))
  }
  val empty: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  if (BigInt((bubble_sort_iterative(copy_list(empty))).size) != BigInt(0)) {
    throw new RuntimeException(String.valueOf("empty iterative failed"))
  }
  if (BigInt((bubble_sort_recursive(copy_list(empty))).size) != BigInt(0)) {
    throw new RuntimeException(String.valueOf("empty recursive failed"))
  }
}

def main(): Any = {
  test_bubble_sort()
  val arr: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(5), BigInt(1), BigInt(4), BigInt(2), BigInt(8))
  println(_str(bubble_sort_iterative(copy_list(arr))))
  println(_str(bubble_sort_recursive(copy_list(arr))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
