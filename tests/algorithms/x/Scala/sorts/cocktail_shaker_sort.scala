// Generated by Mochi v0.10.63 on 2025-08-11 16:33:26 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def cocktail_shaker_sort(arr: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var start: BigInt = BigInt(0)
  var end: BigInt = BigInt((arr).size) - BigInt(1)
  val _br0 = new Breaks
  _br0.breakable {
    while (start < end) {
      var swapped: Boolean = false
      var i: BigInt = start
      while (i < end) {
        if (arr.lift((i.toInt).toInt).getOrElse(null) > arr.lift(((i + BigInt(1)).toInt).toInt).getOrElse(null)) {
          val temp: BigInt = arr.lift((i.toInt).toInt).getOrElse(null)
          arr((i).toInt) = arr.lift(((i + BigInt(1)).toInt).toInt).getOrElse(null)
          arr((i + BigInt(1)).toInt) = temp
          swapped = true
        }
        i = i + BigInt(1)
      }
      if (!swapped) {
        _br0.break()
      }
      end = end - BigInt(1)
      i = end
      while (i > start) {
        if (arr.lift((i.toInt).toInt).getOrElse(null) < arr.lift(((i - BigInt(1)).toInt).toInt).getOrElse(null)) {
          val temp2: BigInt = arr.lift((i.toInt).toInt).getOrElse(null)
          arr((i).toInt) = arr.lift(((i - BigInt(1)).toInt).toInt).getOrElse(null)
          arr((i - BigInt(1)).toInt) = temp2
          swapped = true
        }
        i = i - BigInt(1)
      }
      if (!swapped) {
        _br0.break()
      }
      start = start + BigInt(1)
    }
  }
  return arr
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(cocktail_shaker_sort(ArrayBuffer(BigInt(4), BigInt(5), BigInt(2), BigInt(1), BigInt(2)))))
    println(_str(cocktail_shaker_sort(ArrayBuffer(-BigInt(4), BigInt(5), BigInt(0), BigInt(1), BigInt(2), BigInt(11)))))
    println(_str(cocktail_shaker_sort(ArrayBuffer(BigInt(1), BigInt(2), BigInt(3), BigInt(4), BigInt(5)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
