// Generated by Mochi v0.10.63 on 2025-08-11 17:31:48 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def swap(seq: ArrayBuffer[BigInt], i: BigInt, j: BigInt): Any = {
  val temp: BigInt = seq.lift((i.toInt).toInt).getOrElse(null)
  seq((i).toInt) = seq.lift((j.toInt).toInt).getOrElse(null)
  seq((j).toInt) = temp
}

def slowsort_recursive(seq: ArrayBuffer[BigInt], start: BigInt, end_index: BigInt): Any = {
  if (start >= end_index) {
    return
  }
  val mid: BigInt = (start + end_index) / BigInt(2)
  slowsort_recursive(seq, start, mid)
  slowsort_recursive(seq, mid + BigInt(1), end_index)
  if (seq.lift((end_index.toInt).toInt).getOrElse(null) < seq.lift((mid.toInt).toInt).getOrElse(null)) {
    swap(seq, end_index, mid)
  }
  slowsort_recursive(seq, start, end_index - BigInt(1))
  return null
}

def slow_sort(seq: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  if (BigInt((seq).size) > BigInt(0)) {
    slowsort_recursive(seq, BigInt(0), BigInt((seq).size) - BigInt(1))
  }
  return seq
}

val seq1: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(1), BigInt(6), BigInt(2), BigInt(5), BigInt(3), BigInt(4), BigInt(4), BigInt(5))

var seq2: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()

val seq3: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(2))

val seq4: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(1), BigInt(2), BigInt(3), BigInt(4))

val seq5: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(4), BigInt(3), BigInt(2), BigInt(1))

val seq6: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(9), BigInt(8), BigInt(7), BigInt(6), BigInt(5), BigInt(4), BigInt(3), BigInt(2), BigInt(1), BigInt(0))

val seq7: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(9), BigInt(8), BigInt(7), BigInt(6), BigInt(5), BigInt(4), BigInt(3), BigInt(2), BigInt(1), BigInt(0))

val seq8: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(9), BigInt(8), BigInt(7), BigInt(6), BigInt(5), BigInt(4), BigInt(3), BigInt(2), BigInt(1), BigInt(0))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(slow_sort(seq1)))
    println(_str(slow_sort(seq2)))
    println(_str(slow_sort(seq3)))
    println(_str(slow_sort(seq4)))
    println(_str(slow_sort(seq5)))
    slowsort_recursive(seq6, BigInt(2), BigInt(7))
    println(_str(seq6))
    slowsort_recursive(seq7, BigInt(0), BigInt(4))
    println(_str(seq7))
    slowsort_recursive(seq8, BigInt(5), BigInt((seq8).size) - BigInt(1))
    println(_str(seq8))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
