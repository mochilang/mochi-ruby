// Generated by Mochi v0.10.63 on 2025-08-11 16:33:39 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def cycle_sort(arr: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var n: BigInt = BigInt((arr).size)
  var cycle_start: BigInt = BigInt(0)
  val _ct0 = new Breaks
  while (cycle_start < n - BigInt(1)) {
    _ct0.breakable {
      var item: BigInt = arr.lift((cycle_start.toInt).toInt).getOrElse(null)
      var pos: BigInt = cycle_start
      var i: BigInt = cycle_start + BigInt(1)
      while (i < n) {
        if (arr.lift((i.toInt).toInt).getOrElse(null) < item) {
          pos = pos + BigInt(1)
        }
        i = i + BigInt(1)
      }
      if (pos == cycle_start) {
        cycle_start = cycle_start + BigInt(1)
        _ct0.break()
      }
      while (item == arr.lift((pos.toInt).toInt).getOrElse(null)) {
        pos = pos + BigInt(1)
      }
      val temp: BigInt = arr.lift((pos.toInt).toInt).getOrElse(null)
      arr((pos).toInt) = item
      item = temp
      while (pos != cycle_start) {
        pos = cycle_start
        i = cycle_start + BigInt(1)
        while (i < n) {
          if (arr.lift((i.toInt).toInt).getOrElse(null) < item) {
            pos = pos + BigInt(1)
          }
          i = i + BigInt(1)
        }
        while (item == arr.lift((pos.toInt).toInt).getOrElse(null)) {
          pos = pos + BigInt(1)
        }
        val temp2: BigInt = arr.lift((pos.toInt).toInt).getOrElse(null)
        arr((pos).toInt) = item
        item = temp2
      }
      cycle_start = cycle_start + BigInt(1)
    }
  }
  return arr
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(cycle_sort(ArrayBuffer(BigInt(4), BigInt(3), BigInt(2), BigInt(1)))))
    println(_str(cycle_sort(ArrayBuffer(-BigInt(4), BigInt(20), BigInt(0), -BigInt(50), BigInt(100), -BigInt(1)))))
    println(_str(cycle_sort(ArrayBuffer[BigInt]())))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
