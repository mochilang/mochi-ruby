// Generated by Mochi v0.10.63 on 2025-08-11 17:29:26 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def subarray(xs: ArrayBuffer[BigInt], start: BigInt, end: BigInt): ArrayBuffer[BigInt] = {
  var result: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = start
  while (i < end) {
    result = result :+ (xs.lift((i.toInt).toInt).getOrElse(null))
    i = i + BigInt(1)
  }
  return result
}

def merge(left: ArrayBuffer[BigInt], right: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var result: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  var j: BigInt = BigInt(0)
  while (i < BigInt((left).size) && j < BigInt((right).size)) {
    if (left.lift((i.toInt).toInt).getOrElse(null) <= right.lift((j.toInt).toInt).getOrElse(null)) {
      result = result :+ (left.lift((i.toInt).toInt).getOrElse(null))
      i = i + BigInt(1)
    } else {
      result = result :+ (right.lift((j.toInt).toInt).getOrElse(null))
      j = j + BigInt(1)
    }
  }
  while (i < BigInt((left).size)) {
    result = result :+ (left.lift((i.toInt).toInt).getOrElse(null))
    i = i + BigInt(1)
  }
  while (j < BigInt((right).size)) {
    result = result :+ (right.lift((j.toInt).toInt).getOrElse(null))
    j = j + BigInt(1)
  }
  return result
}

def merge_sort(collection: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  if (BigInt((collection).size) <= BigInt(1)) {
    return collection
  }
  val mid_index: BigInt = BigInt((collection).size) / BigInt(2)
  val left: ArrayBuffer[BigInt] = subarray(collection, BigInt(0), mid_index)
  val right: ArrayBuffer[BigInt] = subarray(collection, mid_index, BigInt((collection).size))
  val sorted_left: ArrayBuffer[BigInt] = merge_sort(left)
  val sorted_right: ArrayBuffer[BigInt] = merge_sort(right)
  return merge(sorted_left, sorted_right)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(merge_sort(ArrayBuffer(BigInt(0), BigInt(5), BigInt(3), BigInt(2), BigInt(2)))))
    println(_str(merge_sort(ArrayBuffer[BigInt]())))
    println(_str(merge_sort(ArrayBuffer(BigInt((-BigInt(2)).toString.head.asDigit), BigInt((-BigInt(5)).toString.head.asDigit), BigInt((-BigInt(45)).toString.head.asDigit)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
