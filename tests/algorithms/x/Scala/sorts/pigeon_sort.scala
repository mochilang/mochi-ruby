// Generated by Mochi v0.10.63 on 2025-08-11 17:30:26 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def make_list(n: BigInt, value: BigInt): ArrayBuffer[BigInt] = {
  var result: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < n) {
    result = result :+ (value)
    i = i + BigInt(1)
  }
  return result
}

def min_value(arr: ArrayBuffer[BigInt]): BigInt = {
  var m: BigInt = arr.lift((BigInt(0).toInt).toInt).getOrElse(null)
  var i: BigInt = BigInt(1)
  while (i < BigInt((arr).size)) {
    if (arr.lift((i.toInt).toInt).getOrElse(null) < m) {
      m = arr.lift((i.toInt).toInt).getOrElse(null)
    }
    i = i + BigInt(1)
  }
  return m
}

def max_value(arr: ArrayBuffer[BigInt]): BigInt = {
  var m: BigInt = arr.lift((BigInt(0).toInt).toInt).getOrElse(null)
  var i: BigInt = BigInt(1)
  while (i < BigInt((arr).size)) {
    if (arr.lift((i.toInt).toInt).getOrElse(null) > m) {
      m = arr.lift((i.toInt).toInt).getOrElse(null)
    }
    i = i + BigInt(1)
  }
  return m
}

def pigeon_sort(array: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  if (BigInt((array).size) == BigInt(0)) {
    return array
  }
  val mn: BigInt = min_value(array)
  val mx: BigInt = max_value(array)
  val holes_range: BigInt = mx - mn + BigInt(1)
  var holes: ArrayBuffer[BigInt] = make_list(holes_range, BigInt(0))
  var holes_repeat: ArrayBuffer[BigInt] = make_list(holes_range, BigInt(0))
  var i: BigInt = BigInt(0)
  while (i < BigInt((array).size)) {
    val index: BigInt = array.lift((i.toInt).toInt).getOrElse(null) - mn
    holes((index).toInt) = array.lift((i.toInt).toInt).getOrElse(null)
    holes_repeat((index).toInt) = holes_repeat.lift((index.toInt).toInt).getOrElse(null) + BigInt(1)
    i = i + BigInt(1)
  }
  var array_index: BigInt = BigInt(0)
  var h: BigInt = BigInt(0)
  while (h < holes_range) {
    while (holes_repeat.lift((h.toInt).toInt).getOrElse(null) > BigInt(0)) {
      array((array_index).toInt) = holes.lift((h.toInt).toInt).getOrElse(null)
      array_index = array_index + BigInt(1)
      holes_repeat((h).toInt) = holes_repeat.lift((h.toInt).toInt).getOrElse(null) - BigInt(1)
    }
    h = h + BigInt(1)
  }
  return array
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(pigeon_sort(ArrayBuffer(BigInt(0), BigInt(5), BigInt(3), BigInt(2), BigInt(2)))))
    println(_str(pigeon_sort(ArrayBuffer[BigInt]())))
    println(_str(pigeon_sort(ArrayBuffer(BigInt((-BigInt(2)).toString.head.asDigit), BigInt((-BigInt(5)).toString.head.asDigit), BigInt((-BigInt(45)).toString.head.asDigit)))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
