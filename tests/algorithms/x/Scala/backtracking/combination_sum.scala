// Generated by Mochi v0.10.59 on 2025-08-06 18:32:43 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def backtrack(candidates: ArrayBuffer[BigInt], start: BigInt, target: BigInt, path: ArrayBuffer[BigInt], _result: ArrayBuffer[ArrayBuffer[BigInt]]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var result: ArrayBuffer[ArrayBuffer[BigInt]] = _result
  if (target == 0) {
    return result :+ path
  }
  var i: BigInt = start
  while (i < BigInt((candidates).size)) {
    val value: BigInt = candidates((i.toInt).toInt)
    if (value <= target) {
      val new_path: ArrayBuffer[BigInt] = path :+ value
      result = backtrack(candidates, i, target - value, new_path, result)
    }
    i = i + 1
  }
  return result
}

def combination_sum(candidates: ArrayBuffer[BigInt], target: BigInt): ArrayBuffer[ArrayBuffer[BigInt]] = {
  val path: ArrayBuffer[BigInt] = ArrayBuffer()
  val result: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
  return backtrack(candidates, 0, target, path, result)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(combination_sum(ArrayBuffer(2, 3, 5), 8)))
    println(String.valueOf(combination_sum(ArrayBuffer(2, 3, 6, 7), 7)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
