// Generated by Mochi v0.10.71 on 2025-08-24 09:40:29 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

case class InvResult(var arr: ArrayBuffer[BigInt], var inv: BigInt)

def slice_list(arr: ArrayBuffer[BigInt], start: BigInt, end: BigInt): ArrayBuffer[BigInt] = {
  var res: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var k: BigInt = start
  while (k < end) {
    res = ({res += (arr.lift((k.toInt).toInt).getOrElse(BigInt(0))); res})
    k = k + BigInt(1)
  }
  return res
}

def count_inversions_bf(arr: ArrayBuffer[BigInt]): BigInt = {
  var n: BigInt = BigInt((arr).size)
  var inv: BigInt = BigInt(0)
  var i: BigInt = BigInt(0)
  while (i < n - BigInt(1)) {
    var j: BigInt = i + BigInt(1)
    while (j < n) {
      if (arr.lift((i.toInt).toInt).getOrElse(BigInt(0)) > arr.lift((j.toInt).toInt).getOrElse(BigInt(0))) {
        inv = inv + BigInt(1)
      }
      j = j + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return inv
}

def count_cross_inversions(p: ArrayBuffer[BigInt], q: ArrayBuffer[BigInt]): InvResult = {
  var r: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  var j: BigInt = BigInt(0)
  var inv: BigInt = BigInt(0)
  while (i < BigInt((p).size) && j < BigInt((q).size)) {
    if (p.lift((i.toInt).toInt).getOrElse(BigInt(0)) > q.lift((j.toInt).toInt).getOrElse(BigInt(0))) {
      inv = inv + (BigInt((p).size) - i)
      r = ({r += (q.lift((j.toInt).toInt).getOrElse(BigInt(0))); r})
      j = j + BigInt(1)
    } else {
      r = ({r += (p.lift((i.toInt).toInt).getOrElse(BigInt(0))); r})
      i = i + BigInt(1)
    }
  }
  if (i < BigInt((p).size)) {
    r = ((r ++ slice_list(p, i, BigInt((p).size))).asInstanceOf[ArrayBuffer[BigInt]]).asInstanceOf[ArrayBuffer[BigInt]]
  } else {
    r = ((r ++ slice_list(q, j, BigInt((q).size))).asInstanceOf[ArrayBuffer[BigInt]]).asInstanceOf[ArrayBuffer[BigInt]]
  }
  return InvResult(r, inv)
}

def count_inversions_recursive(arr: ArrayBuffer[BigInt]): InvResult = {
  if (BigInt((arr).size) <= BigInt(1)) {
    return InvResult(arr, BigInt(0))
  }
  var mid: BigInt = BigInt((arr).size) / BigInt(2)
  var p: ArrayBuffer[BigInt] = slice_list(arr, BigInt(0), mid)
  var q: ArrayBuffer[BigInt] = slice_list(arr, mid, BigInt((arr).size))
  var res_p: InvResult = count_inversions_recursive(p)
  var res_q: InvResult = count_inversions_recursive(q)
  var res_cross: InvResult = count_cross_inversions(res_p.asInstanceOf[InvResult].arr, res_q.asInstanceOf[InvResult].arr)
  var total: BigInt = res_p.asInstanceOf[InvResult].inv + res_q.asInstanceOf[InvResult].inv + res_cross.asInstanceOf[InvResult].inv
  return InvResult(res_cross.asInstanceOf[InvResult].arr, total)
}

var arr_1: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(10), BigInt(2), BigInt(1), BigInt(5), BigInt(5), BigInt(2), BigInt(11))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    var nbf: BigInt = count_inversions_bf(arr_1)
    var nrec: BigInt = (count_inversions_recursive(arr_1)).asInstanceOf[InvResult].inv
    println(_str((List("number of inversions = ", nbf)).mkString(" ")))
    arr_1 = ArrayBuffer(BigInt(1), BigInt(2), BigInt(2), BigInt(5), BigInt(5), BigInt(10), BigInt(11))
    var nbf2: BigInt = count_inversions_bf(arr_1)
    var nrec2: BigInt = (count_inversions_recursive(arr_1)).asInstanceOf[InvResult].inv
    println(_str((List("number of inversions = ", nbf2)).mkString(" ")))
    arr_1 = ArrayBuffer[BigInt]()
    var nbf3: BigInt = count_inversions_bf(arr_1)
    var nrec3: BigInt = (count_inversions_recursive(arr_1)).asInstanceOf[InvResult].inv
    println(_str((List("number of inversions = ", nbf3)).mkString(" ")))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
