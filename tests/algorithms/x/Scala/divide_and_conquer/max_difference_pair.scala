// Generated by Mochi v0.10.71 on 2025-08-24 09:40:44 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def min_slice(a: ArrayBuffer[BigInt], start: BigInt, end: BigInt): BigInt = {
  var m: BigInt = a.lift((start.toInt).toInt).getOrElse(BigInt(0))
  var i: BigInt = start + BigInt(1)
  while (i < end) {
    if (a.lift((i.toInt).toInt).getOrElse(BigInt(0)) < m) {
      m = a.lift((i.toInt).toInt).getOrElse(BigInt(0))
    }
    i = i + BigInt(1)
  }
  return m
}

def max_slice(a: ArrayBuffer[BigInt], start: BigInt, end: BigInt): BigInt = {
  var m: BigInt = a.lift((start.toInt).toInt).getOrElse(BigInt(0))
  var i: BigInt = start + BigInt(1)
  while (i < end) {
    if (a.lift((i.toInt).toInt).getOrElse(BigInt(0)) > m) {
      m = a.lift((i.toInt).toInt).getOrElse(BigInt(0))
    }
    i = i + BigInt(1)
  }
  return m
}

def max_diff_range(a: ArrayBuffer[BigInt], start: BigInt, end: BigInt): ArrayBuffer[BigInt] = {
  if (end - start == BigInt(1)) {
    var v: BigInt = a.lift((start.toInt).toInt).getOrElse(BigInt(0))
    return ArrayBuffer(v, v)
  }
  var mid: BigInt = (start + end) / BigInt(2)
  var left: ArrayBuffer[BigInt] = max_diff_range(a, start, mid)
  var right: ArrayBuffer[BigInt] = max_diff_range(a, mid, end)
  var small1: BigInt = left.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0))
  var big1: BigInt = left.lift((BigInt(1).toInt).toInt).getOrElse(BigInt(0))
  var small2: BigInt = right.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0))
  var big2: BigInt = right.lift((BigInt(1).toInt).toInt).getOrElse(BigInt(0))
  var min_left: BigInt = min_slice(a, start, mid)
  var max_right: BigInt = max_slice(a, mid, end)
  var cross_diff: BigInt = max_right - min_left
  var left_diff: BigInt = big1 - small1
  var right_diff: BigInt = big2 - small2
  if ((right_diff > cross_diff && right_diff > left_diff)) {
    return ArrayBuffer(small2, big2)
  } else {
    if (left_diff > cross_diff) {
      return ArrayBuffer(small1, big1)
    } else {
      return ArrayBuffer(min_left, max_right)
    }
  }
  return ArrayBuffer[BigInt]()
}

def max_difference(a: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  return max_diff_range(a, BigInt(0), BigInt((a).size))
}

def main(): Any = {
  var result: ArrayBuffer[BigInt] = max_difference(ArrayBuffer(BigInt(5), BigInt(11), BigInt(2), BigInt(1), BigInt(7), BigInt(9), BigInt(0), BigInt(7)))
  println(_str(_str(result)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
