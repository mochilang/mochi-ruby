// Generated by Mochi v0.10.71 on 2025-08-24 09:40:07 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

case class Point(var x: BigInt, var y: BigInt)

def cross(o: Point, a: Point, b: Point): BigInt = {
  return (a.asInstanceOf[Point].x - o.asInstanceOf[Point].x) * (b.asInstanceOf[Point].y - o.asInstanceOf[Point].y) - (a.asInstanceOf[Point].y - o.asInstanceOf[Point].y) * (b.asInstanceOf[Point].x - o.asInstanceOf[Point].x)
}

def sortPoints(ps: ArrayBuffer[Point]): ArrayBuffer[Point] = {
  var arr: ArrayBuffer[Point] = ps
  var n: BigInt = BigInt((arr).size)
  var i: BigInt = BigInt(0)
  while (i < n) {
    var j: BigInt = BigInt(0)
    while (j < n - BigInt(1)) {
      var p: Point = arr.lift((j.toInt).toInt).getOrElse(null)
      var q: Point = arr.lift(((j + BigInt(1)).toInt).toInt).getOrElse(null)
      if ((p.asInstanceOf[Point].x > q.asInstanceOf[Point].x || (p.asInstanceOf[Point].x == q.asInstanceOf[Point].x && p.asInstanceOf[Point].y > q.asInstanceOf[Point].y).asInstanceOf[Boolean])) {
        arr((j).toInt) = q
        arr((j + BigInt(1)).toInt) = p
      }
      j = j + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return arr
}

def convex_hull(_ps: ArrayBuffer[Point]): ArrayBuffer[Point] = {
  var ps: ArrayBuffer[Point] = _ps
  ps = sortPoints(ps)
  var lower: ArrayBuffer[Point] = ArrayBuffer[Point]()
  for (p <- ps) {
    while (BigInt((lower).size) >= BigInt(2) && cross(lower.lift(((BigInt((lower).size) - BigInt(2)).toInt).toInt).getOrElse(null), lower.lift(((BigInt((lower).size) - BigInt(1)).toInt).toInt).getOrElse(null), p) <= BigInt(0)) {
      lower = lower.slice((BigInt(0)).toInt, (BigInt((lower).size) - BigInt(1)).toInt)
    }
    lower = ({lower += (p); lower})
  }
  var upper: ArrayBuffer[Point] = ArrayBuffer[Point]()
  var i: BigInt = BigInt((ps).size) - BigInt(1)
  while (i >= BigInt(0)) {
    var p: Point = ps.lift((i.toInt).toInt).getOrElse(null)
    while (BigInt((upper).size) >= BigInt(2) && cross(upper.lift(((BigInt((upper).size) - BigInt(2)).toInt).toInt).getOrElse(null), upper.lift(((BigInt((upper).size) - BigInt(1)).toInt).toInt).getOrElse(null), p) <= BigInt(0)) {
      upper = upper.slice((BigInt(0)).toInt, (BigInt((upper).size) - BigInt(1)).toInt)
    }
    upper = ({upper += (p); upper})
    i = i - BigInt(1)
  }
  var hull: ArrayBuffer[Point] = lower.slice((BigInt(0)).toInt, (BigInt((lower).size) - BigInt(1)).toInt)
  var j: BigInt = BigInt(0)
  while (j < BigInt((upper).size) - BigInt(1)) {
    hull = ({hull += (upper.lift((j.toInt).toInt).getOrElse(null)); hull})
    j = j + BigInt(1)
  }
  return hull
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
