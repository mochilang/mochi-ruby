// Generated by Mochi v0.10.71 on 2025-08-24 09:40:22 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def copy_list(arr: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var result: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((arr).size)) {
    result = ({result += (arr.lift((i.toInt).toInt).getOrElse(BigInt(0))); result})
    i = i + BigInt(1)
  }
  return result
}

def heaps(arr: ArrayBuffer[BigInt]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  if (BigInt((arr).size) <= BigInt(1)) {
    var single: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer[ArrayBuffer[BigInt]]()
    return ({single += (copy_list(arr)); single})
  }
  var n: BigInt = BigInt((arr).size)
  var c: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < n) {
    c = ({c += (BigInt(0)); c})
    i = i + BigInt(1)
  }
  var res: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer[ArrayBuffer[BigInt]]()
  res = ({res += (copy_list(arr)); res})
  i = BigInt(0)
  while (i < n) {
    if (c.lift((i.toInt).toInt).getOrElse(BigInt(0)) < i) {
      if (i % BigInt(2) == BigInt(0)) {
        var temp: BigInt = arr.lift((BigInt(0).toInt).toInt).getOrElse(BigInt(0))
        arr((BigInt(0)).toInt) = arr.lift((i.toInt).toInt).getOrElse(BigInt(0))
        arr((i).toInt) = temp
      } else {
        var temp: BigInt = arr.lift((c.lift((i.toInt).toInt).getOrElse(BigInt(0)).toInt).toInt).getOrElse(BigInt(0))
        arr((c.lift((i.toInt).toInt).getOrElse(BigInt(0))).toInt) = arr.lift((i.toInt).toInt).getOrElse(BigInt(0))
        arr((i).toInt) = temp
      }
      res = ({res += (copy_list(arr)); res})
      c((i).toInt) = c.lift((i.toInt).toInt).getOrElse(BigInt(0)) + BigInt(1)
      i = BigInt(0)
    } else {
      c((i).toInt) = BigInt(0)
      i = i + BigInt(1)
    }
  }
  return res
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(_str(heaps(ArrayBuffer(BigInt(1), BigInt(2), BigInt(3))))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
