// Generated by Mochi v0.10.71 on 2025-08-24 09:39:36 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def mat_inverse3(m: ArrayBuffer[ArrayBuffer[Double]]): ArrayBuffer[ArrayBuffer[Double]] = {
  var a: Double = m.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0)
  var b: Double = m.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0)
  var c: Double = m.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(2).toInt).toInt).getOrElse(0.0)
  var d: Double = m.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0)
  var e: Double = m.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0)
  var f: Double = m.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(2).toInt).toInt).getOrElse(0.0)
  var g: Double = m.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0)
  var h: Double = m.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0)
  var i: Double = m.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(2).toInt).toInt).getOrElse(0.0)
  var det: Double = a * (e * i - f * h) - b * (d * i - f * g) + c * (d * h - e * g)
  if (det == 0.0) {
    throw new RuntimeException(String.valueOf("singular matrix"))
  }
  var adj00: Double = e * i - f * h
  var adj01: Double = c * h - b * i
  var adj02: Double = b * f - c * e
  var adj10: Double = f * g - d * i
  var adj11: Double = a * i - c * g
  var adj12: Double = c * d - a * f
  var adj20: Double = d * h - e * g
  var adj21: Double = b * g - a * h
  var adj22: Double = a * e - b * d
  var inv: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer[ArrayBuffer[Double]]()
  inv = ({inv += (ArrayBuffer((adj00 / det).toString.toDouble, (adj01 / det).toString.toDouble, (adj02 / det).toString.toDouble)); inv})
  inv = ({inv += (ArrayBuffer((adj10 / det).toString.toDouble, (adj11 / det).toString.toDouble, (adj12 / det).toString.toDouble)); inv})
  inv = ({inv += (ArrayBuffer((adj20 / det).toString.toDouble, (adj21 / det).toString.toDouble, (adj22 / det).toString.toDouble)); inv})
  return inv
}

def mat_vec_mul(m: ArrayBuffer[ArrayBuffer[Double]], v: ArrayBuffer[Double]): ArrayBuffer[Double] = {
  var res: ArrayBuffer[Double] = ArrayBuffer[Double]()
  var i: BigInt = BigInt(0)
  while (i < BigInt(3)) {
    var `val`: Double = m.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0) * v.lift((BigInt(0).toInt).toInt).getOrElse(0.0) + m.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0) * v.lift((BigInt(1).toInt).toInt).getOrElse(0.0) + m.lift((i.toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(2).toInt).toInt).getOrElse(0.0) * v.lift((BigInt(2).toInt).toInt).getOrElse(0.0)
    res = ({res += (`val`); res})
    i = i + BigInt(1)
  }
  return res
}

def create_matrix(rows: BigInt, cols: BigInt, value: BigInt): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var result: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer[ArrayBuffer[BigInt]]()
  var r: BigInt = BigInt(0)
  while (r < rows) {
    var row: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
    var c: BigInt = BigInt(0)
    while (c < cols) {
      row = ({row += (value); row})
      c = c + BigInt(1)
    }
    result = ({result += (row); result})
    r = r + BigInt(1)
  }
  return result
}

def round_to_int(x: Double): BigInt = {
  if (x >= 0.0) {
    return BigInt((x + 0.5).toInt)
  }
  return BigInt((x - 0.5).toInt)
}

def get_rotation(img: ArrayBuffer[ArrayBuffer[BigInt]], pt1: ArrayBuffer[ArrayBuffer[Double]], pt2: ArrayBuffer[ArrayBuffer[Double]], rows: BigInt, cols: BigInt): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var src: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(pt1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0), pt1.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0), 1.0), ArrayBuffer(pt1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0), pt1.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0), 1.0), ArrayBuffer(pt1.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0), pt1.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0), 1.0))
  var inv: ArrayBuffer[ArrayBuffer[Double]] = mat_inverse3(src)
  var vecx: ArrayBuffer[Double] = ArrayBuffer(pt2.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0), pt2.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0), pt2.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(0).toInt).toInt).getOrElse(0.0))
  var vecy: ArrayBuffer[Double] = ArrayBuffer(pt2.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0), pt2.lift((BigInt(1).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0), pt2.lift((BigInt(2).toInt).toInt).getOrElse(ArrayBuffer[Double]()).lift((BigInt(1).toInt).toInt).getOrElse(0.0))
  var avec: ArrayBuffer[Double] = mat_vec_mul(inv, vecx)
  var bvec: ArrayBuffer[Double] = mat_vec_mul(inv, vecy)
  var a0: Double = avec.lift((BigInt(0).toInt).toInt).getOrElse(0.0)
  var a1: Double = avec.lift((BigInt(1).toInt).toInt).getOrElse(0.0)
  var a2: Double = avec.lift((BigInt(2).toInt).toInt).getOrElse(0.0)
  var b0: Double = bvec.lift((BigInt(0).toInt).toInt).getOrElse(0.0)
  var b1: Double = bvec.lift((BigInt(1).toInt).toInt).getOrElse(0.0)
  var b2: Double = bvec.lift((BigInt(2).toInt).toInt).getOrElse(0.0)
  var out: ArrayBuffer[ArrayBuffer[BigInt]] = create_matrix(rows, cols, BigInt(0))
  var y: BigInt = BigInt(0)
  while (y < rows) {
    var x: BigInt = BigInt(0)
    while (x < cols) {
      var xf: Double = a0 * (1.0 * x.toString.toDouble) + a1 * (1.0 * y.toString.toDouble) + a2
      var yf: Double = b0 * (1.0 * x.toString.toDouble) + b1 * (1.0 * y.toString.toDouble) + b2
      var sx: BigInt = round_to_int(xf)
      var sy: BigInt = round_to_int(yf)
      if ((((sx >= BigInt(0) && sx < cols).asInstanceOf[Boolean] && sy >= BigInt(0)).asInstanceOf[Boolean] && sy < rows)) {
        out((sy).toInt)((sx).toInt) = img.lift((y.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((x.toInt).toInt).getOrElse(BigInt(0))
      }
      x = x + BigInt(1)
    }
    y = y + BigInt(1)
  }
  return out
}

var img: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(BigInt(1), BigInt(2), BigInt(3)), ArrayBuffer(BigInt(4), BigInt(5), BigInt(6)), ArrayBuffer(BigInt(7), BigInt(8), BigInt(9)))

var pts1: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.0, 0.0), ArrayBuffer(2.0, 0.0), ArrayBuffer(0.0, 2.0))

var pts2: ArrayBuffer[ArrayBuffer[Double]] = ArrayBuffer(ArrayBuffer(0.0, 2.0), ArrayBuffer(0.0, 0.0), ArrayBuffer(2.0, 2.0))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    var rotated: ArrayBuffer[ArrayBuffer[BigInt]] = get_rotation(img, pts1, pts2, BigInt(3), BigInt(3))
    println(_str(_str(rotated)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
