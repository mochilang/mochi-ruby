// Generated by Mochi v0.10.64 on 2025-08-12 13:50:49 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def depth_first_search(grid: ArrayBuffer[ArrayBuffer[BigInt]], row: BigInt, col: BigInt, visit: ArrayBuffer[ArrayBuffer[Boolean]]): BigInt = {
  val row_length: BigInt = BigInt((grid).size)
  val col_length: BigInt = BigInt((grid.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[BigInt]())).size)
  if ((((row < BigInt(0) || col < BigInt(0)).asInstanceOf[Boolean] || row == row_length).asInstanceOf[Boolean] || col == col_length).asInstanceOf[Boolean]) {
    return BigInt(0)
  }
  if (visit.lift((row.toInt).toInt).getOrElse(ArrayBuffer[Boolean]()).lift((col.toInt).toInt).getOrElse(false)) {
    return BigInt(0)
  }
  if (grid.lift((row.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((col.toInt).toInt).getOrElse(BigInt(0)) == BigInt(1)) {
    return BigInt(0)
  }
  if ((row == row_length - BigInt(1) && col == col_length - BigInt(1)).asInstanceOf[Boolean]) {
    return BigInt(1)
  }
  visit((row).toInt)((col).toInt) = true
  var count: BigInt = BigInt(0)
  count = count + depth_first_search(grid, row + BigInt(1), col, visit)
  count = count + depth_first_search(grid, row - BigInt(1), col, visit)
  count = count + depth_first_search(grid, row, col + BigInt(1), visit)
  count = count + depth_first_search(grid, row, col - BigInt(1), visit)
  visit((row).toInt)((col).toInt) = false
  return count
}

def count_paths(grid: ArrayBuffer[ArrayBuffer[BigInt]]): BigInt = {
  val rows: BigInt = BigInt((grid).size)
  val cols: BigInt = BigInt((grid.lift((BigInt(0).toInt).toInt).getOrElse(ArrayBuffer[BigInt]())).size)
  var visit: ArrayBuffer[ArrayBuffer[Boolean]] = ArrayBuffer[ArrayBuffer[Boolean]]()
  var i: BigInt = BigInt(0)
  while (i < rows) {
    var row_visit: ArrayBuffer[Boolean] = ArrayBuffer[Boolean]()
    var j: BigInt = BigInt(0)
    while (j < cols) {
      row_visit = row_visit :+ (false)
      j = j + BigInt(1)
    }
    visit = visit :+ (row_visit)
    i = i + BigInt(1)
  }
  return depth_first_search(grid, BigInt(0), BigInt(0), visit)
}

def main(): Any = {
  val grid1: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(BigInt(0), BigInt(0), BigInt(0), BigInt(0)), ArrayBuffer(BigInt(1), BigInt(1), BigInt(0), BigInt(0)), ArrayBuffer(BigInt(0), BigInt(0), BigInt(0), BigInt(1)), ArrayBuffer(BigInt(0), BigInt(1), BigInt(0), BigInt(0)))
  println(_str(count_paths(grid1)))
  val grid2: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(BigInt(0), BigInt(0), BigInt(0), BigInt(0), BigInt(0)), ArrayBuffer(BigInt(0), BigInt(1), BigInt(1), BigInt(1), BigInt(0)), ArrayBuffer(BigInt(0), BigInt(1), BigInt(1), BigInt(1), BigInt(0)), ArrayBuffer(BigInt(0), BigInt(0), BigInt(0), BigInt(0), BigInt(0)))
  println(_str(count_paths(grid2)))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
