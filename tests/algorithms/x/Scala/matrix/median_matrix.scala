// Generated by Mochi v0.10.64 on 2025-08-12 13:51:47 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def bubble_sort(a: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var arr: ArrayBuffer[BigInt] = a
  val n: BigInt = BigInt((arr).size)
  var i: BigInt = BigInt(0)
  while (i < n) {
    var j: BigInt = BigInt(0)
    while (j + BigInt(1) < n - i) {
      if (arr.lift((j.toInt).toInt).getOrElse(BigInt(0)) > arr.lift(((j + BigInt(1)).toInt).toInt).getOrElse(BigInt(0))) {
        val temp: BigInt = arr.lift((j.toInt).toInt).getOrElse(BigInt(0))
        arr((j).toInt) = arr.lift(((j + BigInt(1)).toInt).toInt).getOrElse(BigInt(0))
        arr((j + BigInt(1)).toInt) = temp
      }
      j = j + BigInt(1)
    }
    i = i + BigInt(1)
  }
  return arr
}

def median(matrix: ArrayBuffer[ArrayBuffer[BigInt]]): BigInt = {
  var linear: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((matrix).size)) {
    val row: ArrayBuffer[BigInt] = matrix.lift((i.toInt).toInt).getOrElse(ArrayBuffer[BigInt]())
    var j: BigInt = BigInt(0)
    while (j < BigInt((row).size)) {
      linear = linear :+ (row.lift((j.toInt).toInt).getOrElse(BigInt(0)))
      j = j + BigInt(1)
    }
    i = i + BigInt(1)
  }
  val sorted: ArrayBuffer[BigInt] = bubble_sort(linear)
  val mid: BigInt = (BigInt((sorted).size) - BigInt(1)) / BigInt(2)
  return sorted.lift((mid.toInt).toInt).getOrElse(BigInt(0))
}

val matrix1: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(BigInt(1), BigInt(3), BigInt(5)), ArrayBuffer(BigInt(2), BigInt(6), BigInt(9)), ArrayBuffer(BigInt(3), BigInt(6), BigInt(9)))

val matrix2: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer(ArrayBuffer(BigInt(1), BigInt(2), BigInt(3)), ArrayBuffer(BigInt(4), BigInt(5), BigInt(6)))

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(median(matrix1)))
    println(_str(median(matrix2)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
