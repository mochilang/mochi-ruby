// Generated by Mochi v0.10.69 on 2025-08-19 16:46:35 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def exp_approx(x: Double): Double = {
  var y: Double = x
  var is_neg: Boolean = false
  if (x < 0.0) {
    is_neg = true
    y = -x
  }
  var term: Double = 1.0
  var sum: Double = 1.0
  var n: BigInt = BigInt(1)
  while (n < BigInt(30)) {
    term = term * y / n.toString.toDouble
    sum = sum + term
    n = n + BigInt(1)
  }
  if (is_neg) {
    return 1.0 / sum
  }
  return sum
}

def ln_series(x: Double): Double = {
  var t: Double = (x - 1.0) / (x + 1.0)
  var term: Double = t
  var sum: Double = 0.0
  var n: BigInt = BigInt(1)
  while (n <= BigInt(19)) {
    sum = sum + term / n.toString.toDouble
    term = term * t * t
    n = n + BigInt(2)
  }
  return 2.0 * sum
}

def ln(x: Double): Double = {
  var y: Double = x
  var k: BigInt = BigInt(0)
  while (y >= 10.0) {
    y = y / 10.0
    k = k + BigInt(1)
  }
  while (y < 1.0) {
    y = y * 10.0
    k = k - BigInt(1)
  }
  return ln_series(y) + k.toString.toDouble * ln_series(10.0)
}

def powf(base: Double, exponent: Double): Double = {
  return exp_approx(exponent * math.log(base))
}

def rainfall_intensity(coefficient_k: Double, coefficient_a: Double, coefficient_b: Double, coefficient_c: Double, return_period: Double, duration: Double): Double = {
  if (coefficient_k <= 0.0) {
    throw new RuntimeException(String.valueOf("All parameters must be positive."))
  }
  if (coefficient_a <= 0.0) {
    throw new RuntimeException(String.valueOf("All parameters must be positive."))
  }
  if (coefficient_b <= 0.0) {
    throw new RuntimeException(String.valueOf("All parameters must be positive."))
  }
  if (coefficient_c <= 0.0) {
    throw new RuntimeException(String.valueOf("All parameters must be positive."))
  }
  if (return_period <= 0.0) {
    throw new RuntimeException(String.valueOf("All parameters must be positive."))
  }
  if (duration <= 0.0) {
    throw new RuntimeException(String.valueOf("All parameters must be positive."))
  }
  var numerator: Double = coefficient_k * powf(return_period, coefficient_a)
  var denominator: Double = powf(duration + coefficient_b, coefficient_c)
  return numerator / denominator
}

var r1: Double = rainfall_intensity(1000.0, 0.2, 11.6, 0.81, 10.0, 60.0)

var r2: Double = rainfall_intensity(1000.0, 0.2, 11.6, 0.81, 10.0, 30.0)

var r3: Double = rainfall_intensity(1000.0, 0.2, 11.6, 0.81, 5.0, 60.0)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(_str(r1)))
    println(_str(_str(r2)))
    println(_str(_str(r3)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
