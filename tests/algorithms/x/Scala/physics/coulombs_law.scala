// Generated by Mochi v0.10.73 on 2025-08-25 23:58:20 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

private def _div(a: BigInt, b: BigInt): BigInt = {
  var q = a / b
  var r = a % b
  if (r != 0 && ((r < 0) != (b < 0))) q -= 1
  q
}

private def _mod(a: BigInt, b: BigInt): BigInt = {
  var r = a % b
  if (r != 0 && ((r < 0) != (b < 0))) r += b
  r
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def format2(x: Double): String = {
  val sign: String = (if (x < 0.0) "-" else "")
  val y: Double = (if (x < 0.0) -x else x)
  val m: Double = 100.0
  val scaled: Double = y * m
  var i: BigInt = BigInt(scaled.toString.toDouble.toInt)
  if ((scaled - i.toString.toDouble) >= 0.5) {
    i = i + BigInt(1)
  }
  val int_part: BigInt = _div(i, BigInt(100))
  val frac_part: BigInt = _mod(i, BigInt(100))
  var frac_str: String = _str(frac_part)
  if (frac_part < BigInt(10)) {
    frac_str = "0" + frac_str
  }
  return sign + _str(int_part) + "." + frac_str
}

def coulombs_law(q1: Double, q2: Double, radius: Double): Double = {
  if (radius <= 0.0) {
    throw new RuntimeException(String.valueOf("radius must be positive"))
  }
  val force: Double = K * q1 * q2 / (radius * radius)
  return force
}

val K: Double = 8.9875517923e+09

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(format2(coulombs_law(15.5, 20.0, 15.0))))
    println(_str(format2(coulombs_law(1.0, 15.0, 5.0))))
    println(_str(format2(coulombs_law(20.0, -50.0, 15.0))))
    println(_str(format2(coulombs_law(-5.0, -8.0, 10.0))))
    println(_str(format2(coulombs_law(50.0, 100.0, 50.0))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
