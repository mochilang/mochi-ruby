// Generated by Mochi v0.10.63 on 2025-08-11 17:33:25 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def match_in_pattern(pat: String, ch: String): BigInt = {
  var i: BigInt = BigInt((pat).size) - BigInt(1)
  while (i >= BigInt(0)) {
    if (pat.slice((i).toInt, (i + BigInt(1)).toInt) == ch) {
      return i
    }
    i = i - BigInt(1)
  }
  return -BigInt(1)
}

def mismatch_in_text(text: String, pat: String, current_pos: BigInt): BigInt = {
  var i: BigInt = BigInt((pat).size) - BigInt(1)
  while (i >= BigInt(0)) {
    if (pat.slice((i).toInt, (i + BigInt(1)).toInt) != text.slice((current_pos + i).toInt, (current_pos + i + BigInt(1)).toInt)) {
      return current_pos + i
    }
    i = i - BigInt(1)
  }
  return -BigInt(1)
}

def bad_character_heuristic(text: String, pat: String): ArrayBuffer[BigInt] = {
  val textLen: BigInt = BigInt((text).size)
  val patLen: BigInt = BigInt((pat).size)
  var positions: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i <= textLen - patLen) {
    val mismatch_index: BigInt = mismatch_in_text(text, pat, i)
    if (mismatch_index < BigInt(0)) {
      positions = positions :+ (i)
      i = i + BigInt(1)
    } else {
      val ch: String = text.slice((mismatch_index).toInt, (mismatch_index + BigInt(1)).toInt)
      val match_index: BigInt = match_in_pattern(pat, ch)
      if (match_index < BigInt(0)) {
        i = mismatch_index + BigInt(1)
      } else {
        i = mismatch_index - match_index
      }
    }
  }
  return positions
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
