// Generated by Mochi v0.10.63 on 2025-08-11 15:28:22 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def run(n: BigInt): ArrayBuffer[BigInt] = {
  val limit: BigInt = BigInt(200000)
  var counts: scala.collection.mutable.Map[BigInt,BigInt] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[BigInt,BigInt]]
  var p: BigInt = BigInt(2)
  while (p <= limit) {
    if (!(counts.contains(p)).asInstanceOf[Boolean]) {
      var m: BigInt = p
      while (m <= limit) {
        if ((counts.contains(m)).asInstanceOf[Boolean]) {
          counts.update(m, counts.getOrElse(m, null).asInstanceOf[BigInt] + BigInt(1))
        } else {
          counts.update(m, BigInt(1))
        }
        m = m + p
      }
    }
    p = p + BigInt(1)
  }
  var streak: BigInt = BigInt(0)
  var num: BigInt = BigInt(2)
  while (num <= limit) {
    val c: BigInt = (if (counts.contains(num)) counts.getOrElse(num, null).asInstanceOf[BigInt] else BigInt(0))
    if (c == n) {
      streak = streak + BigInt(1)
      if (streak == n) {
        var result: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
        var start: BigInt = num - n + BigInt(1)
        var j: BigInt = BigInt(0)
        while (j < n) {
          result = result :+ (start + j)
          j = j + BigInt(1)
        }
        return result
      }
    } else {
      streak = BigInt(0)
    }
    num = num + BigInt(1)
  }
  return ArrayBuffer[BigInt]()
}

def solution(n: BigInt): BigInt = {
  val res: ArrayBuffer[BigInt] = run(n)
  return res.lift((BigInt(0).toInt).toInt).getOrElse(null)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(solution(BigInt(4))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
