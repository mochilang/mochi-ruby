// Generated by Mochi v0.10.62 on 2025-08-09 16:42:27 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def solution(n: BigInt): BigInt = {
  val ones_counts: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(0), BigInt(3), BigInt(3), BigInt(5), BigInt(4), BigInt(4), BigInt(3), BigInt(5), BigInt(5), BigInt(4), BigInt(3), BigInt(6), BigInt(6), BigInt(8), BigInt(8), BigInt(7), BigInt(7), BigInt(9), BigInt(8), BigInt(8))
  val tens_counts: ArrayBuffer[BigInt] = ArrayBuffer(BigInt(0), BigInt(0), BigInt(6), BigInt(6), BigInt(5), BigInt(5), BigInt(5), BigInt(7), BigInt(6), BigInt(6))
  var count: BigInt = BigInt(0)
  var i: BigInt = BigInt(1)
  while (i <= n) {
    if (i < BigInt(1000)) {
      if (i >= BigInt(100)) {
        count = count + ones_counts.lift(((i / BigInt(100)).toInt).toInt).getOrElse(null) + BigInt(7)
        if (i % BigInt(100) != BigInt(0)) {
          count = count + BigInt(3)
        }
      }
      val remainder: BigInt = i % BigInt(100)
      if ((remainder > BigInt(0) && remainder < BigInt(20)).asInstanceOf[Boolean]) {
        count = count + ones_counts.lift((remainder.toInt).toInt).getOrElse(null)
      } else {
        count = count + ones_counts.lift(((i % BigInt(10)).toInt).toInt).getOrElse(null)
        count = count + tens_counts.lift((((remainder - i % BigInt(10)) / BigInt(10)).toInt).toInt).getOrElse(null)
      }
    } else {
      count = count + ones_counts.lift(((i / BigInt(1000)).toInt).toInt).getOrElse(null) + BigInt(8)
    }
    i = i + BigInt(1)
  }
  return count
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(_str(solution(BigInt(1000))))
    println(_str(solution(BigInt(5))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
