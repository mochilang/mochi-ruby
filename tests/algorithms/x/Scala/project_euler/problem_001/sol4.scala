// Generated by Mochi v0.10.69 on 2025-08-19 16:47:33 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def contains(xs: ArrayBuffer[BigInt], value: BigInt): Boolean = {
  var i: BigInt = BigInt(0)
  while (i < BigInt((xs).size)) {
    if (xs.lift((i.toInt).toInt).getOrElse(BigInt(0)) == value) {
      return true
    }
    i = i + BigInt(1)
  }
  return false
}

def solution(n: BigInt): BigInt = {
  var zmulti: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var xmulti: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var temp: BigInt = BigInt(1)
  val _br1 = new Breaks
  _br1.breakable {
    while (true) {
      var result: BigInt = BigInt(3) * temp
      if (result < n) {
        zmulti = (zmulti :+ (result))
        temp = temp + BigInt(1)
      } else {
        _br1.break()
      }
    }
  }
  temp = BigInt(1)
  val _br2 = new Breaks
  _br2.breakable {
    while (true) {
      var result: BigInt = BigInt(5) * temp
      if (result < n) {
        xmulti = (xmulti :+ (result))
        temp = temp + BigInt(1)
      } else {
        _br2.break()
      }
    }
  }
  var collection: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((zmulti).size)) {
    var v: BigInt = zmulti.lift((i.toInt).toInt).getOrElse(BigInt(0))
    if (!(collection.contains(v))) {
      collection = (collection :+ (v))
    }
    i = i + BigInt(1)
  }
  i = BigInt(0)
  while (i < BigInt((xmulti).size)) {
    var v: BigInt = xmulti.lift((i.toInt).toInt).getOrElse(BigInt(0))
    if (!(collection.contains(v))) {
      collection = (collection :+ (v))
    }
    i = i + BigInt(1)
  }
  var total: BigInt = BigInt(0)
  i = BigInt(0)
  while (i < BigInt((collection).size)) {
    total = total + collection.lift((i.toInt).toInt).getOrElse(BigInt(0))
    i = i + BigInt(1)
  }
  return total
}

def test_solution(): Any = {
  if (solution(BigInt(3)) != BigInt(0)) {
    throw new RuntimeException(String.valueOf("solution(3) failed"))
  }
  if (solution(BigInt(4)) != BigInt(3)) {
    throw new RuntimeException(String.valueOf("solution(4) failed"))
  }
  if (solution(BigInt(10)) != BigInt(23)) {
    throw new RuntimeException(String.valueOf("solution(10) failed"))
  }
  if (solution(BigInt(600)) != BigInt(83700)) {
    throw new RuntimeException(String.valueOf("solution(600) failed"))
  }
}

def main(): Any = {
  test_solution()
  println(_str("solution() = " + _str(solution(BigInt(1000)))))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
