// Generated by Mochi v0.10.63 on 2025-08-11 16:30:55 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def parse_row(row_str: String): ArrayBuffer[BigInt] = {
  var nums: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var current: BigInt = BigInt(0)
  var has_digit: Boolean = false
  var i: BigInt = BigInt(0)
  while (i < BigInt((row_str).size)) {
    val ch: String = row_str.slice((i).toInt, (i + BigInt(1)).toInt)
    if (ch == " ") {
      if (has_digit) {
        nums = nums :+ (current)
        current = BigInt(0)
        has_digit = false
      }
    } else {
      current = current * BigInt(10) + BigInt(ch.toString.head.asDigit)
      has_digit = true
    }
    i = i + BigInt(1)
  }
  if (has_digit) {
    nums = nums :+ (current)
  }
  return nums
}

def parse_matrix(matrix_str: ArrayBuffer[String]): ArrayBuffer[ArrayBuffer[BigInt]] = {
  var matrix: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer[ArrayBuffer[BigInt]]()
  for (row_str <- matrix_str) {
    val row: ArrayBuffer[BigInt] = parse_row(row_str)
    matrix = matrix :+ (row)
  }
  return matrix
}

def bitcount(x: BigInt): BigInt = {
  var count: BigInt = BigInt(0)
  var y: BigInt = x
  while (y > BigInt(0)) {
    if (y % BigInt(2) == BigInt(1)) {
      count = count + BigInt(1)
    }
    y = y / BigInt(2)
  }
  return count
}

def build_powers(n: BigInt): ArrayBuffer[BigInt] = {
  var powers: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  var current: BigInt = BigInt(1)
  while (i <= n) {
    powers = powers :+ (current)
    current = current * BigInt(2)
    i = i + BigInt(1)
  }
  return powers
}

def solution(matrix_str: ArrayBuffer[String]): BigInt = {
  val arr: ArrayBuffer[ArrayBuffer[BigInt]] = parse_matrix(matrix_str)
  val n: BigInt = BigInt((arr).size)
  val powers: ArrayBuffer[BigInt] = build_powers(n)
  val size: BigInt = powers.lift((n.toInt).toInt).getOrElse(null)
  var dp: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < size) {
    dp = dp :+ (BigInt(0))
    i = i + BigInt(1)
  }
  var mask: BigInt = BigInt(0)
  while (mask < size) {
    val row: BigInt = bitcount(mask)
    if (row < n) {
      var col: BigInt = BigInt(0)
      while (col < n) {
        if (mask / powers.lift((col.toInt).toInt).getOrElse(null) % BigInt(2) == BigInt(0)) {
          val new_mask: BigInt = mask + powers.lift((col.toInt).toInt).getOrElse(null)
          val value: BigInt = dp.lift((mask.toInt).toInt).getOrElse(null) + arr.lift((row.toInt).toInt).getOrElse(ArrayBuffer[BigInt]()).lift((col.toInt).toInt).getOrElse(null)
          if (value > dp.lift((new_mask.toInt).toInt).getOrElse(null)) {
            dp((new_mask).toInt) = value
          }
        }
        col = col + BigInt(1)
      }
    }
    mask = mask + BigInt(1)
  }
  return dp.lift(((size - BigInt(1)).toInt).toInt).getOrElse(null)
}

val MATRIX_2: ArrayBuffer[String] = ArrayBuffer("7 53 183 439 863 497 383 563 79 973 287 63 343 169 583", "627 343 773 959 943 767 473 103 699 303 957 703 583 639 913", "447 283 463 29 23 487 463 993 119 883 327 493 423 159 743", "217 623 3 399 853 407 103 983 89 463 290 516 212 462 350", "960 376 682 962 300 780 486 502 912 800 250 346 172 812 350", "870 456 192 162 593 473 915 45 989 873 823 965 425 329 803", "973 965 905 919 133 673 665 235 509 613 673 815 165 992 326", "322 148 972 962 286 255 941 541 265 323 925 281 601 95 973", "445 721 11 525 473 65 511 164 138 672 18 428 154 448 848", "414 456 310 312 798 104 566 520 302 248 694 976 430 392 198", "184 829 373 181 631 101 969 613 840 740 778 458 284 760 390", "821 461 843 513 17 901 711 993 293 157 274 94 192 156 574", "34 124 4 878 450 476 712 914 838 669 875 299 823 329 699", "815 559 813 459 522 788 168 586 966 232 308 833 251 631 107", "813 883 451 509 615 77 281 613 459 205 380 274 302 35 805")

val result: BigInt = solution(MATRIX_2)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println("solution() = " + _str(result))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
