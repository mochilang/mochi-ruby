// Generated by Mochi v0.10.63 on 2025-08-11 15:26:15 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def is_prime(number: BigInt): Boolean = {
  if ((BigInt(1) < number && number < BigInt(4)).asInstanceOf[Boolean]) {
    return true
  } else {
    if (((number < BigInt(2) || number % BigInt(2) == BigInt(0)).asInstanceOf[Boolean] || number % BigInt(3) == BigInt(0)).asInstanceOf[Boolean]) {
      return false
    }
  }
  var i: BigInt = BigInt(5)
  while (i * i <= number) {
    if ((number % i == BigInt(0) || number % (i + BigInt(2)) == BigInt(0)).asInstanceOf[Boolean]) {
      return false
    }
    i = i + BigInt(6)
  }
  return true
}

def remove_at(xs: ArrayBuffer[BigInt], index: BigInt): ArrayBuffer[BigInt] = {
  var res: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(0)
  while (i < BigInt((xs).size)) {
    if (i != index) {
      res = res :+ (xs.lift((i.toInt).toInt).getOrElse(null))
    }
    i = i + BigInt(1)
  }
  return res
}

def collect_primes(nums: ArrayBuffer[BigInt], current: BigInt, _primes: ArrayBuffer[BigInt]): ArrayBuffer[BigInt] = {
  var primes: ArrayBuffer[BigInt] = _primes
  if (BigInt((nums).size) == BigInt(0)) {
    if (is_prime(current)) {
      primes = primes :+ (current)
    }
    return primes
  }
  var i: BigInt = BigInt(0)
  var res: ArrayBuffer[BigInt] = primes
  while (i < BigInt((nums).size)) {
    val digit: BigInt = nums.lift((i.toInt).toInt).getOrElse(null)
    val remaining: ArrayBuffer[BigInt] = remove_at(nums, i)
    res = collect_primes(remaining, current * BigInt(10) + digit, res)
    i = i + BigInt(1)
  }
  return res
}

def max_list(nums: ArrayBuffer[BigInt]): BigInt = {
  var m: BigInt = BigInt(0)
  var i: BigInt = BigInt(0)
  while (i < BigInt((nums).size)) {
    if (nums.lift((i.toInt).toInt).getOrElse(null) > m) {
      m = nums.lift((i.toInt).toInt).getOrElse(null)
    }
    i = i + BigInt(1)
  }
  return m
}

def solution(n: BigInt): BigInt = {
  var digits: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  var i: BigInt = BigInt(1)
  while (i <= n) {
    digits = digits :+ (i)
    i = i + BigInt(1)
  }
  val primes: ArrayBuffer[BigInt] = collect_primes(digits, BigInt(0), (ArrayBuffer[BigInt]()).asInstanceOf[ArrayBuffer[BigInt]])
  if (BigInt((primes).size) == BigInt(0)) {
    return BigInt(0)
  }
  return max_list(primes)
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println("solution() = " + _str(solution(BigInt(7))))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
