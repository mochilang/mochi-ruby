// Generated by Mochi v0.10.63 on 2025-08-11 15:26:48 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => if (d.isWhole) d.toLong.toString else d.toString
  case other => String.valueOf(other)
}

def to_float(x: BigInt): Double = {
  return x.toString.toDouble * 1.0
}

def sqrt(x: Double): Double = {
  if (x <= 0.0) {
    return 0.0
  }
  var guess: Double = x
  var i: BigInt = BigInt(0)
  while (i < BigInt(10)) {
    guess = (guess + x / guess) / 2.0
    i = i + BigInt(1)
  }
  return guess
}

def floor(x: Double): BigInt = {
  var n: BigInt = BigInt(0)
  var y: Double = x
  while (y >= 1.0) {
    y = y - 1.0
    n = n + BigInt(1)
  }
  return n
}

def hexagonal_num(n: BigInt): BigInt = {
  return n * (BigInt(2) * n - BigInt(1))
}

def is_pentagonal(n: BigInt): Boolean = {
  val root: Double = sqrt(1.0 + 24.0 * (n.toString()).toDouble)
  val `val`: Double = (1.0 + root) / 6.0
  return `val` == ((Math.floor(`val`)).toString()).toDouble
}

def solution(start: BigInt): BigInt = {
  var idx: BigInt = start
  var num: BigInt = hexagonal_num(idx)
  while (!is_pentagonal(num)) {
    idx = idx + BigInt(1)
    num = hexagonal_num(idx)
  }
  return num
}

def test_hexagonal_num(): Any = {
  if (hexagonal_num(BigInt(143)) != BigInt(40755)) {
    throw new RuntimeException(String.valueOf("hexagonal_num(143) failed"))
  }
  if (hexagonal_num(BigInt(21)) != BigInt(861)) {
    throw new RuntimeException(String.valueOf("hexagonal_num(21) failed"))
  }
  if (hexagonal_num(BigInt(10)) != BigInt(190)) {
    throw new RuntimeException(String.valueOf("hexagonal_num(10) failed"))
  }
}

def test_is_pentagonal(): Any = {
  if (!is_pentagonal(BigInt(330))) {
    throw new RuntimeException(String.valueOf("330 should be pentagonal"))
  }
  if (is_pentagonal(BigInt(7683))) {
    throw new RuntimeException(String.valueOf("7683 should not be pentagonal"))
  }
  if (!is_pentagonal(BigInt(2380))) {
    throw new RuntimeException(String.valueOf("2380 should be pentagonal"))
  }
}

def test_solution(): Any = {
  if (solution(BigInt(144)) != BigInt(1533776805)) {
    throw new RuntimeException(String.valueOf("solution failed"))
  }
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    test_hexagonal_num()
    test_is_pentagonal()
    test_solution()
    println(_str(solution(BigInt(144))) + " = ")
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
