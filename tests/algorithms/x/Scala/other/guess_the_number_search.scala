// Generated by Mochi v0.10.73 on 2025-08-25 23:54:17 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

private def _div(a: BigInt, b: BigInt): BigInt = {
  var q = a / b
  var r = a % b
  if (r != 0 && ((r < 0) != (b < 0))) q -= 1
  q
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case m: scala.collection.Map[_, _] => scala.collection.immutable.ListMap(m.toSeq.sortBy(_._1.toString): _*).toString.replace("ListMap", "Map")
  case d: Double => { val i = d.toLong; if (d == i.toDouble) java.lang.Long.toString(i) else java.lang.Double.toString(d) }
  case other => String.valueOf(other)
}

def get_avg(number_1: BigInt, number_2: BigInt): BigInt = {
  return BigInt((_div(number_1 + number_2, BigInt(2))).toString.toDouble.toInt)
}

def guess_the_number(lower: BigInt, higher: BigInt, to_guess: BigInt): ArrayBuffer[BigInt] = {
  if (lower > higher) {
    throw new RuntimeException(String.valueOf("argument value for lower and higher must be(lower > higher)"))
  }
  if (!(lower < to_guess && to_guess < higher)) {
    throw new RuntimeException(String.valueOf("guess value must be within the range of lower and higher value"))
  }
  def answer(number: BigInt): String = {
    if (number > to_guess) {
      return "high"
    } else {
      if (number < to_guess) {
        return "low"
      } else {
        return "same"
      }
    }
    return ""
  }
  println(_str("started..."))
  var last_lowest: BigInt = lower
  var last_highest: BigInt = higher
  var last_numbers: ArrayBuffer[BigInt] = ArrayBuffer[BigInt]()
  val _br0 = new Breaks
  _br0.breakable {
    while (true) {
      val number: BigInt = get_avg(last_lowest, last_highest)
      last_numbers = (last_numbers :+ (number))
      val resp: String = answer(number)
      if (resp == "low") {
        last_lowest = number
      } else {
        if (resp == "high") {
          last_highest = number
        } else {
          _br0.break()
        }
      }
    }
  }
  println(_str("guess the number : " + _str(last_numbers.lift(((BigInt((last_numbers).size) - BigInt(1)).toInt).toInt).getOrElse(BigInt(0)))))
  println(_str("details : " + _str(last_numbers)))
  return last_numbers
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    guess_the_number(BigInt(10), BigInt(1000), BigInt(17))
    guess_the_number(BigInt(-10000), BigInt(10000), BigInt(7))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start).abs / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
