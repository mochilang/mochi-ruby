// Generated by Mochi v0.10.64 on 2025-08-13 16:17:43 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def ind_reactance(inductance: Double, frequency: Double, reactance: Double): scala.collection.mutable.Map[String,Double] = {
  var zero_count: BigInt = BigInt(0)
  if (inductance == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (frequency == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (reactance == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (zero_count != BigInt(1)) {
    throw new RuntimeException(String.valueOf("One and only one argument must be 0"))
  }
  if (inductance < 0.0) {
    throw new RuntimeException(String.valueOf("Inductance cannot be negative"))
  }
  if (frequency < 0.0) {
    throw new RuntimeException(String.valueOf("Frequency cannot be negative"))
  }
  if (reactance < 0.0) {
    throw new RuntimeException(String.valueOf("Inductive reactance cannot be negative"))
  }
  if (inductance == 0.0) {
    return (scala.collection.mutable.Map("inductance" -> (reactance / (2.0 * PI * frequency)))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  if (frequency == 0.0) {
    return (scala.collection.mutable.Map("frequency" -> (reactance / (2.0 * PI * inductance)))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  return (scala.collection.mutable.Map("reactance" -> (2.0 * PI * frequency * inductance))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
}

val PI: Double = 3.141592653589793

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(ind_reactance(0.0, 10000.0, 50.0))
    println(ind_reactance(0.035, 0.0, 50.0))
    println(ind_reactance(0.000035, 1000.0, 0.0))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
