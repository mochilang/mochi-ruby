// Generated by Mochi v0.10.64 on 2025-08-13 16:16:34 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => d.toString
  case other => String.valueOf(other)
}

case class CarrierResult(var name: String, var value: Double)

def sqrtApprox(x: Double): Double = {
  var guess: Double = x / 2.0
  var i: BigInt = BigInt(0)
  while (i < BigInt(20)) {
    guess = (guess + x / guess) / 2.0
    i = i + BigInt(1)
  }
  return guess
}

def carrier_concentration(electron_conc: Double, hole_conc: Double, intrinsic_conc: Double): CarrierResult = {
  var zero_count: BigInt = BigInt(0)
  if (electron_conc == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (hole_conc == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (intrinsic_conc == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (zero_count != BigInt(1)) {
    throw new RuntimeException(String.valueOf("You cannot supply more or less than 2 values"))
  }
  if (electron_conc < 0.0) {
    throw new RuntimeException(String.valueOf("Electron concentration cannot be negative in a semiconductor"))
  }
  if (hole_conc < 0.0) {
    throw new RuntimeException(String.valueOf("Hole concentration cannot be negative in a semiconductor"))
  }
  if (intrinsic_conc < 0.0) {
    throw new RuntimeException(String.valueOf("Intrinsic concentration cannot be negative in a semiconductor"))
  }
  if (electron_conc == 0.0) {
    return CarrierResult("electron_conc", intrinsic_conc * intrinsic_conc / hole_conc)
  }
  if (hole_conc == 0.0) {
    return CarrierResult("hole_conc", intrinsic_conc * intrinsic_conc / electron_conc)
  }
  if (intrinsic_conc == 0.0) {
    return CarrierResult("intrinsic_conc", sqrtApprox(electron_conc * hole_conc))
  }
  return CarrierResult("", -1.0)
}

val r1: CarrierResult = carrier_concentration(25.0, 100.0, 0.0)

val r2: CarrierResult = carrier_concentration(0.0, 1600.0, 200.0)

val r3: CarrierResult = carrier_concentration(1000.0, 0.0, 1200.0)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(r1.name + ", " + _str(r1.value))
    println(r2.name + ", " + _str(r2.value))
    println(r3.name + ", " + _str(r3.value))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
