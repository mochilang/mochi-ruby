// Generated by Mochi v0.10.64 on 2025-08-13 16:17:06 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _str(x: Any): String = x match {
  case d: Double => d.toString
  case other => String.valueOf(other)
}

def abs(x: Double): Double = {
  if (x < 0.0) {
    return -x
  }
  return x
}

def sqrtApprox(x: Double): Double = {
  if (x <= 0.0) {
    return 0.0
  }
  var guess: Double = x
  var i: BigInt = BigInt(0)
  while (i < BigInt(20)) {
    guess = (guess + x / guess) / 2.0
    i = i + BigInt(1)
  }
  return guess
}

def coulombs_law(force: Double, charge1: Double, charge2: Double, distance: Double): scala.collection.mutable.Map[String,Double] = {
  val charge_product: Double = Math.abs(charge1 * charge2)
  var zero_count: BigInt = BigInt(0)
  if (force == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (charge1 == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (charge2 == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (distance == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (zero_count != BigInt(1)) {
    throw new RuntimeException(String.valueOf("One and only one argument must be 0"))
  }
  if (distance < 0.0) {
    throw new RuntimeException(String.valueOf("Distance cannot be negative"))
  }
  if (force == 0.0) {
    val f: Double = COULOMBS_CONSTANT * charge_product / (distance * distance)
    return (scala.collection.mutable.Map("force" -> (f))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  if (charge1 == 0.0) {
    val c1: Double = Math.abs(force) * (distance * distance) / (COULOMBS_CONSTANT * charge2)
    return (scala.collection.mutable.Map("charge1" -> (c1))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  if (charge2 == 0.0) {
    val c2: Double = Math.abs(force) * (distance * distance) / (COULOMBS_CONSTANT * charge1)
    return (scala.collection.mutable.Map("charge2" -> (c2))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  val d: Double = sqrtApprox(COULOMBS_CONSTANT * charge_product / Math.abs(force))
  return (scala.collection.mutable.Map("distance" -> (d))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
}

def print_map(m: scala.collection.mutable.Map[String,Double]): Any = {
  for (k <- m.keys) {
    println("{\"" + k + "\": " + _str(m.getOrElse(k.toString, 0.0).asInstanceOf[Double]) + "}")
  }
}

val COULOMBS_CONSTANT: Double = 8988000000.0

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    print_map(coulombs_law(0.0, 3.0, 5.0, 2000.0))
    print_map(coulombs_law(10.0, 3.0, 5.0, 0.0))
    print_map(coulombs_law(10.0, 0.0, 5.0, 2000.0))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
