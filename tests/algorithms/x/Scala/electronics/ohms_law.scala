// Generated by Mochi v0.10.64 on 2025-08-13 16:17:50 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def ohms_law(voltage: Double, current: Double, resistance: Double): scala.collection.mutable.Map[String,Double] = {
  var zeros: BigInt = BigInt(0)
  if (voltage == 0.0) {
    zeros = zeros + BigInt(1)
  }
  if (current == 0.0) {
    zeros = zeros + BigInt(1)
  }
  if (resistance == 0.0) {
    zeros = zeros + BigInt(1)
  }
  if (zeros != BigInt(1)) {
    println("One and only one argument must be 0")
    return (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  if (resistance < 0.0) {
    println("Resistance cannot be negative")
    return (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  if (voltage == 0.0) {
    return (scala.collection.mutable.Map("voltage" -> (current * resistance))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  if (current == 0.0) {
    return (scala.collection.mutable.Map("current" -> (voltage / resistance))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  }
  return (scala.collection.mutable.Map("resistance" -> (voltage / current))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(toJson(ohms_law(10.0, 0.0, 5.0)))
    println(toJson(ohms_law(-10.0, 1.0, 0.0)))
    println(toJson(ohms_law(0.0, -1.5, 2.0)))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
