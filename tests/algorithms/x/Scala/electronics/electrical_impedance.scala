// Generated by Mochi v0.10.64 on 2025-08-13 16:17:28 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
    if (!_nowSeeded) { _nowSeed = 0L; _nowSeeded = true }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def sqrtApprox(x: Double): Double = {
  if (x <= 0.0) {
    return 0.0
  }
  var guess: Double = x / 2.0
  var i: BigInt = BigInt(0)
  while (i < BigInt(20)) {
    guess = (guess + x / guess) / 2.0
    i = i + BigInt(1)
  }
  return guess
}

def electrical_impedance(resistance: Double, reactance: Double, impedance: Double): scala.collection.mutable.Map[String,Double] = {
  var zero_count: BigInt = BigInt(0)
  if (resistance == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (reactance == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (impedance == 0.0) {
    zero_count = zero_count + BigInt(1)
  }
  if (zero_count != BigInt(1)) {
    throw new RuntimeException(String.valueOf("One and only one argument must be 0"))
  }
  if (resistance == 0.0) {
    val value: Double = sqrtApprox(impedance * impedance - reactance * reactance)
    return (scala.collection.mutable.Map("resistance" -> (value))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
  } else {
    if (reactance == 0.0) {
      val value: Double = sqrtApprox(impedance * impedance - resistance * resistance)
      return (scala.collection.mutable.Map("reactance" -> (value))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
    } else {
      if (impedance == 0.0) {
        val value: Double = sqrtApprox(resistance * resistance + reactance * reactance)
        return (scala.collection.mutable.Map("impedance" -> (value))).asInstanceOf[scala.collection.mutable.Map[String,Double]]
      } else {
        throw new RuntimeException(String.valueOf("Exactly one argument must be 0"))
      }
    }
  }
  return null
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(electrical_impedance(3.0, 4.0, 0.0))
    println(electrical_impedance(0.0, 4.0, 5.0))
    println(electrical_impedance(3.0, 0.0, 5.0))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
