// Generated by Mochi v0.10.59 on 2025-08-06 22:18:59 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def panic(msg: String): Any = {
  println(msg)
}

def trim_spaces(s: String): String = {
  var start: BigInt = 0
  var end: BigInt = BigInt((s).size)
  while (start < end && s.slice((start).toInt, (start + 1).toInt) == " ") {
    start = start + 1
  }
  while (end > start && s.slice((end - 1).toInt, (end).toInt) == " ") {
    end = end - 1
  }
  return s.slice((start).toInt, (end).toInt)
}

def hex_digit_value(ch: String): BigInt = {
  if (ch == "0") {
    return 0
  }
  if (ch == "1") {
    return 1
  }
  if (ch == "2") {
    return 2
  }
  if (ch == "3") {
    return 3
  }
  if (ch == "4") {
    return 4
  }
  if (ch == "5") {
    return 5
  }
  if (ch == "6") {
    return 6
  }
  if (ch == "7") {
    return 7
  }
  if (ch == "8") {
    return 8
  }
  if (ch == "9") {
    return 9
  }
  if ((ch == "a" || ch == "A").asInstanceOf[Boolean]) {
    return 10
  }
  if ((ch == "b" || ch == "B").asInstanceOf[Boolean]) {
    return 11
  }
  if ((ch == "c" || ch == "C").asInstanceOf[Boolean]) {
    return 12
  }
  if ((ch == "d" || ch == "D").asInstanceOf[Boolean]) {
    return 13
  }
  if ((ch == "e" || ch == "E").asInstanceOf[Boolean]) {
    return 14
  }
  if ((ch == "f" || ch == "F").asInstanceOf[Boolean]) {
    return 15
  }
  throw new RuntimeException(String.valueOf("Invalid value was passed to the function"))
  return null
}

def hex_to_bin(hex_num: String): BigInt = {
  val trimmed: String = trim_spaces(hex_num)
  if (BigInt((trimmed).size) == 0) {
    throw new RuntimeException(String.valueOf("No value was passed to the function"))
  }
  var s: String = trimmed
  var is_negative: Boolean = false
  if (s.slice((0).toInt, (1).toInt) == "-") {
    is_negative = true
    s = s.slice((1).toInt, (BigInt((s).size)).toInt)
  }
  var int_num: BigInt = 0
  var i: BigInt = 0
  while (i < BigInt((s).size)) {
    val ch: String = s.slice((i).toInt, (i + 1).toInt)
    val `val`: BigInt = hex_digit_value(ch)
    int_num = int_num * 16 + `val`
    i = i + 1
  }
  var bin_str: String = ""
  var n: BigInt = int_num
  if (n == 0) {
    bin_str = "0"
  }
  while (n > 0) {
    bin_str = String.valueOf(n % 2) + bin_str
    n = n / 2
  }
  var result: BigInt = BigInt(bin_str.toString.toDouble.toInt)
  if (is_negative) {
    result = -result
  }
  return result
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(hex_to_bin("AC")))
    println(String.valueOf(hex_to_bin("9A4")))
    println(String.valueOf(hex_to_bin("   12f   ")))
    println(String.valueOf(hex_to_bin("FfFf")))
    println(String.valueOf(hex_to_bin("-fFfF")))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
