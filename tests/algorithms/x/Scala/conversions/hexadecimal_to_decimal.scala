// Generated by Mochi v0.10.59 on 2025-08-06 22:19:29 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def strip(s: String): String = {
  var start: BigInt = 0
  var end: BigInt = BigInt((s).size)
  while (start < end && s.slice((start).toInt, (start + 1).toInt) == " ") {
    start = start + 1
  }
  while (end > start && s.slice((end - 1).toInt, (end).toInt) == " ") {
    end = end - 1
  }
  return s.slice((start).toInt, (end).toInt)
}

def hex_digit_value(c: String): BigInt = {
  if (c == "0") {
    return 0
  }
  if (c == "1") {
    return 1
  }
  if (c == "2") {
    return 2
  }
  if (c == "3") {
    return 3
  }
  if (c == "4") {
    return 4
  }
  if (c == "5") {
    return 5
  }
  if (c == "6") {
    return 6
  }
  if (c == "7") {
    return 7
  }
  if (c == "8") {
    return 8
  }
  if (c == "9") {
    return 9
  }
  if ((c == "a" || c == "A").asInstanceOf[Boolean]) {
    return 10
  }
  if ((c == "b" || c == "B").asInstanceOf[Boolean]) {
    return 11
  }
  if ((c == "c" || c == "C").asInstanceOf[Boolean]) {
    return 12
  }
  if ((c == "d" || c == "D").asInstanceOf[Boolean]) {
    return 13
  }
  if ((c == "e" || c == "E").asInstanceOf[Boolean]) {
    return 14
  }
  if ((c == "f" || c == "F").asInstanceOf[Boolean]) {
    return 15
  }
  println("Non-hexadecimal value was passed to the function")
  return 0
}

def hex_to_decimal(hex_string: String): BigInt = {
  var s: String = strip(hex_string)
  if (BigInt((s).size) == 0) {
    println("Empty string was passed to the function")
    return 0
  }
  var is_negative: Boolean = false
  if (s.slice((0).toInt, (1).toInt) == "-") {
    is_negative = true
    s = s.slice((1).toInt, (BigInt((s).size)).toInt)
  }
  var decimal_number: BigInt = 0
  var i: BigInt = 0
  while (i < BigInt((s).size)) {
    val c: String = s.slice((i).toInt, (i + 1).toInt)
    val value: BigInt = hex_digit_value(c)
    decimal_number = 16 * decimal_number + value
    i = i + 1
  }
  if (is_negative) {
    return -decimal_number
  }
  return decimal_number
}

def main(): Any = {
  println(String.valueOf(hex_to_decimal("a")))
  println(String.valueOf(hex_to_decimal("12f")))
  println(String.valueOf(hex_to_decimal("   12f   ")))
  println(String.valueOf(hex_to_decimal("FfFf")))
  println(String.valueOf(hex_to_decimal("-Ff")))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
