// Generated by Mochi v0.10.59 on 2025-08-06 22:14:28 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.util.control.Breaks
import scala.util.control.Breaks._
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def trim(s: String): String = {
  var start: BigInt = 0
  val _br0 = new Breaks
  _br0.breakable {
    while (start < BigInt((s).size)) {
      val ch: String = s.slice((start).toInt, (start + 1).toInt)
      if ((((ch != " " && ch != "\n").asInstanceOf[Boolean] && ch != "\t").asInstanceOf[Boolean] && ch != "\r").asInstanceOf[Boolean]) {
        _br0.break()
      }
      start = start + 1
    }
  }
  var end: BigInt = BigInt((s).size)
  val _br1 = new Breaks
  _br1.breakable {
    while (end > start) {
      val ch: String = s.slice((end - 1).toInt, (end).toInt)
      if ((((ch != " " && ch != "\n").asInstanceOf[Boolean] && ch != "\t").asInstanceOf[Boolean] && ch != "\r").asInstanceOf[Boolean]) {
        _br1.break()
      }
      end = end - 1
    }
  }
  return s.slice((start).toInt, (end).toInt)
}

def bin_to_decimal(bin_string: String): BigInt = {
  val trimmed: String = trim(bin_string)
  if (trimmed == "") {
    throw new RuntimeException(String.valueOf("Empty string was passed to the function"))
  }
  var is_negative: Boolean = false
  var s: String = trimmed
  if (s.slice((0).toInt, (1).toInt) == "-") {
    is_negative = true
    s = s.slice((1).toInt, (BigInt((s).size)).toInt)
  }
  var i: BigInt = 0
  while (i < BigInt((s).size)) {
    val c: String = s.slice((i).toInt, (i + 1).toInt)
    if ((c != "0" && c != "1").asInstanceOf[Boolean]) {
      throw new RuntimeException(String.valueOf("Non-binary value was passed to the function"))
    }
    i = i + 1
  }
  var decimal_number: BigInt = 0
  i = 0
  while (i < BigInt((s).size)) {
    val c: String = s.slice((i).toInt, (i + 1).toInt)
    val digit: BigInt = c.toInt
    decimal_number = 2 * decimal_number + digit
    i = i + 1
  }
  if (is_negative) {
    return -decimal_number
  }
  return decimal_number
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(bin_to_decimal("101")))
    println(String.valueOf(bin_to_decimal(" 1010   ")))
    println(String.valueOf(bin_to_decimal("-11101")))
    println(String.valueOf(bin_to_decimal("0")))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
