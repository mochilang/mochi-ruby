<?php
ini_set('memory_limit', '-1');
function _str($x) {
    if (is_array($x)) {
        $isList = array_keys($x) === range(0, count($x) - 1);
        if ($isList) {
            $parts = [];
            foreach ($x as $v) { $parts[] = _str($v); }
            return '[' . implode(' ', $parts) . ']';
        }
        $parts = [];
        foreach ($x as $k => $v) { $parts[] = _str($k) . ':' . _str($v); }
        return 'map[' . implode(' ', $parts) . ']';
    }
    if (is_bool($x)) return $x ? 'true' : 'false';
    if ($x === null) return 'null';
    return strval($x);
}
function _iadd($a, $b) {
    if (function_exists('bcadd')) {
        $sa = is_int($a) ? strval($a) : (is_string($a) ? $a : sprintf('%.0f', $a));
        $sb = is_int($b) ? strval($b) : (is_string($b) ? $b : sprintf('%.0f', $b));
        return bcadd($sa, $sb, 0);
    }
    return $a + $b;
}
function _isub($a, $b) {
    if (function_exists('bcsub')) {
        $sa = is_int($a) ? strval($a) : (is_string($a) ? $a : sprintf('%.0f', $a));
        $sb = is_int($b) ? strval($b) : (is_string($b) ? $b : sprintf('%.0f', $b));
        return bcsub($sa, $sb, 0);
    }
    return $a - $b;
}
function _imul($a, $b) {
    if (function_exists('bcmul')) {
        $sa = is_int($a) ? strval($a) : (is_string($a) ? $a : sprintf('%.0f', $a));
        $sb = is_int($b) ? strval($b) : (is_string($b) ? $b : sprintf('%.0f', $b));
        return bcmul($sa, $sb, 0);
    }
    return $a * $b;
}
function _idiv($a, $b) {
    return _intdiv($a, $b);
}
function _imod($a, $b) {
    if (function_exists('bcmod')) {
        $sa = is_int($a) ? strval($a) : (is_string($a) ? $a : sprintf('%.0f', $a));
        $sb = is_int($b) ? strval($b) : (is_string($b) ? $b : sprintf('%.0f', $b));
        return intval(bcmod($sa, $sb));
    }
    return $a % $b;
}
function word_occurrence($sentence) {
  $occurrence = [];
  $word = '';
  $i = 0;
  while ($i < strlen($sentence)) {
  $ch = substr($sentence, $i, _iadd($i, 1) - $i);
  if ($ch == ' ') {
  if ($word != '') {
  if (array_key_exists($word, $occurrence)) {
  $occurrence[$word] = _iadd($occurrence[$word], 1);
} else {
  $occurrence[$word] = 1;
};
  $word = '';
};
} else {
  $word = $word . $ch;
}
  $i = _iadd($i, 1);
};
  if ($word != '') {
  if (array_key_exists($word, $occurrence)) {
  $occurrence[$word] = _iadd($occurrence[$word], 1);
} else {
  $occurrence[$word] = 1;
};
}
  return $occurrence;
}
function main() {
  $result = word_occurrence('INPUT STRING');
  foreach (array_keys($result) as $w) {
  echo rtrim($w . ': ' . _str($result[$w])), PHP_EOL;
};
}
main();
