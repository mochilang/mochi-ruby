;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (list_contains xs v)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let/ec _cont
    (if (string=? (if xs (if (hash? xs) (hash-ref xs i #f) (list-ref xs (int i))) #f) v) (let ()
(_return #t)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define (is_power_of_two n)
  (let/ec _return (begin
(if (< n 1) (let ()
(_return #f)
) (void))
(define x n)
(let/ec _break (let loop ()
  (if (> x 1) (let/ec _cont
    (if (not (equal? (modulo x 2) 0)) (let ()
(_return #f)
) (void))
    (set! x (quotient x 2))
    (loop)) (void))))
(_return #t)
))
)
(define (bin_string n)
  (let/ec _return (begin
(if (equal? n 0) (let ()
(_return "0")
) (void))
(define res "")
(define x n)
(let/ec _break (let loop ()
  (if (> x 0) (let/ec _cont
    (define bit (modulo x 2))
    (set! res (string-append (format "~a" bit) res))
    (set! x (quotient x 2))
    (loop)) (void))))
(_return res)
))
)
(define (decompress_data data_bits)
  (let/ec _return (begin
(define lexicon (hash "0" "0" "1" "1"))
(define keys (list "0" "1"))
(define result "")
(define curr_string "")
(define index 2)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? data_bits) (string-length data_bits)] [(hash? data_bits) (hash-count data_bits)] [else (length data_bits)])) (let/ec _cont
    (set! curr_string (string-append curr_string (slice data_bits i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
    (if (not (list_contains keys curr_string)) (let ()
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_cont)
) (void))
    (define last_match_id (if lexicon (hash-ref lexicon curr_string #f) #f))
    (set! result (string-append result last_match_id))
    (set! lexicon (hash-set (or lexicon (hash)) curr_string (string-append last_match_id "0")))
    (if (is_power_of_two index) (let ()
(define new_lex (hash))
(define new_keys (list))
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(string? keys) (string-length keys)] [(hash? keys) (hash-count keys)] [else (length keys)])) (let/ec _cont
    (define curr_key (if keys (if (hash? keys) (hash-ref keys j #f) (list-ref keys (int j))) #f))
    (set! new_lex (hash-set (or new_lex (hash)) (string-append "0" curr_key) (if lexicon (hash-ref lexicon curr_key #f) #f)))
    (set! new_keys (append new_keys (list (string-append "0" curr_key))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! lexicon new_lex)
(set! keys new_keys)
) (void))
    (define new_key (bin_string index))
    (set! lexicon (hash-set (or lexicon (hash)) new_key (string-append last_match_id "1")))
    (set! keys (append keys (list new_key)))
    (set! index (let ([__l index] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! curr_string "")
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define sample "1011001")
(define decompressed (decompress_data sample))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln decompressed)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
