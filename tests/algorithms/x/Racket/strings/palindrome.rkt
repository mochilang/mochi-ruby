;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (reverse s)
  (let/ec _return (begin
(define res "")
(define i (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (>= (int __l) (int __r))) (begin
    (let/ec _cont
      (set! res (string-append res (substring s i (+ i 1))))
      (set! i (- i 1))
    )
    (loop)) (void))))
(_return res)
))
)
(define (is_palindrome s)
  (let/ec _return (begin
(define start_i 0)
(define end_i (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l start_i] [__r end_i]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (string=? (substring s start_i (+ start_i 1)) (substring s end_i (+ end_i 1))) (let ()
(set! start_i (let ([__l start_i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! end_i (- end_i 1))
) (let ()
(_return #f)
))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (is_palindrome_traversal s)
  (let/ec _return (begin
(define end (floor (/ (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 2)))
(define n (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r end]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (not (string=? (substring s i (+ i 1)) (substring s (- (- n i) 1) (+ (- (- n i) 1) 1)))) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (is_palindrome_recursive s)
  (let/ec _return (begin
(if (let ([__l (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])] [__r 1]) (<= (int __l) (int __r))) (let ()
(_return #t)
) (void))
(if (string=? (substring s 0 (+ 0 1)) (substring s (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1) (+ (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1) 1))) (let ()
(_return (is_palindrome_recursive (slice s 1 (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 1))))
) (void))
(_return #f)
))
)
(define (is_palindrome_slice s)
  (let/ec _return (begin
(_return (string=? s (reverse s)))
))
)
(define test_data (list (let ([h (make-hash)]) (hash-set! h "text" "MALAYALAM") (hash-set! h "expected" #t) h) (let ([h (make-hash)]) (hash-set! h "text" "String") (hash-set! h "expected" #f) h) (let ([h (make-hash)]) (hash-set! h "text" "rotor") (hash-set! h "expected" #t) h) (let ([h (make-hash)]) (hash-set! h "text" "level") (hash-set! h "expected" #t) h) (let ([h (make-hash)]) (hash-set! h "text" "A") (hash-set! h "expected" #t) h) (let ([h (make-hash)]) (hash-set! h "text" "BB") (hash-set! h "expected" #t) h) (let ([h (make-hash)]) (hash-set! h "text" "ABC") (hash-set! h "expected" #f) h) (let ([h (make-hash)]) (hash-set! h "text" "amanaplanacanalpanama") (hash-set! h "expected" #t) h)))
(define (main)
  (let/ec _return (begin
(let/ec _break (for ([t test_data])
  (let/ec _cont
(define s (if t (hash-ref t "text" #f) #f))
(define expected (if t (hash-ref t "expected" #f) #f))
(define r1 (is_palindrome s))
(define r2 (is_palindrome_traversal s))
(define r3 (is_palindrome_recursive s))
(define r4 (is_palindrome_slice s))
(if (or (or (or (not (equal? r1 expected)) (not (equal? r2 expected))) (not (equal? r3 expected))) (not (equal? r4 expected))) (let ()
(panic "algorithm mismatch")
) (void))
(displayln (string-append (string-append s " ") (to-string expected)))
  )))
(displayln "a man a plan a canal panama")
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
