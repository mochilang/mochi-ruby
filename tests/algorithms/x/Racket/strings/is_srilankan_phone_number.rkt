;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (starts_with s prefix)
  (let/ec _return (begin
(if (let ([__l (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])] [__r (cond [(not prefix) 0] [(string? prefix) (string-length prefix)] [(hash? prefix) (hash-count prefix)] [else (length prefix)])]) (< (int __l) (int __r))) (let ()
(_return #f)
) (void))
(_return (string=? (slice s 0 (cond [(not prefix) 0] [(string? prefix) (string-length prefix)] [(hash? prefix) (hash-count prefix)] [else (length prefix)])) prefix))
))
)
(define (all_digits s)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define c (substring s i (+ i 1)))
      (if (or (string<? c "0") (string>? c "9")) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (is_sri_lankan_phone_number phone)
  (let/ec _return (begin
(define p phone)
(if (starts_with p "+94") (let ()
(set! p (slice p 3 (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)])))
) (let ()
(if (starts_with p "0094") (let ()
(set! p (slice p 4 (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)])))
) (let ()
(if (starts_with p "94") (let ()
(set! p (slice p 2 (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)])))
) (let ()
(if (starts_with p "0") (let ()
(set! p (slice p 1 (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)])))
) (let ()
(_return #f)
))
))
))
))
(if (and (not (equal? (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)]) 9)) (not (equal? (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)]) 10))) (let ()
(_return #f)
) (void))
(if (not (string=? (substring p 0 (+ 0 1)) "7")) (let ()
(_return #f)
) (void))
(define second (substring p 1 (+ 1 1)))
(define allowed (list "0" "1" "2" "4" "5" "6" "7" "8"))
(if (not (not (not (member second (or allowed '()))))) (let ()
(_return #f)
) (void))
(define idx 2)
(if (equal? (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)]) 10) (let ()
(define sep (substring p 2 (+ 2 1)))
(if (and (not (string=? sep "-")) (not (string=? sep " "))) (let ()
(_return #f)
) (void))
(set! idx 3)
) (void))
(if (not (equal? (- (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)]) idx) 7)) (let ()
(_return #f)
) (void))
(define rest (slice p idx (cond [(not p) 0] [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)])))
(_return (all_digits rest))
))
)
(define phone "0094702343221")
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (is_sri_lankan_phone_number phone)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
