;; Generated by Mochi 0.10.71 on 2025-08-24 08:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/strings")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [(vector? x) (format "~a" (vector->list x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a b)]
    [(list? a) (append a (vector->list b))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (split s sep)
  (let/ec _return (begin
(define res (vector))
(define current "")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(vector? s) (vector-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define ch (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (if (string=? ch sep) (let ()
(set! res (appendv (or res (vector)) (vector current)))
(set! current "")
) (let ()
(set! current (string-append current ch))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! res (appendv (or res (vector)) (vector current)))
(_return res)
))
)
(define (insertion_sort arr)
  (let/ec _return (begin
(define a arr)
(define i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(vector? a) (vector-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define key (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f))
      (define j (- i 1))
      (let/ec _break (let loop ()
  (if (and (let ([__l j] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (string>? (if a (if (hash? a) (hash-ref a j #f) (safe-index a (int j))) #f) key)) (begin
    (let/ec _cont
      (vector-set! a (int (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (if a (if (hash? a) (hash-ref a j #f) (safe-index a (int j))) #f))
      (set! j (- j 1))
    )
    (loop)) (void))))
      (vector-set! a (int (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) key)
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return a)
))
)
(define (sort_chars word)
  (let/ec _return (begin
(define chars (vector))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not word) 0] [(string? word) (string-length word)] [(vector? word) (vector-length word)] [(hash? word) (hash-count word)] [else (length word)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! chars (appendv (or chars (vector)) (vector (slice word i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! chars (insertion_sort chars))
(define res "")
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not chars) 0] [(string? chars) (string-length chars)] [(vector? chars) (vector-length chars)] [(hash? chars) (hash-count chars)] [else (length chars)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! res (string-append res (if chars (if (hash? chars) (hash-ref chars i #f) (safe-index chars (int i))) #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (unique_sorted words)
  (let/ec _return (begin
(define seen (let ([h (make-hash)]) h))
(define res (vector))
(let ([__seq words]) (let/ec _break (for ([w (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (and (not (string=? w "")) (not (hash-has-key? seen w))) (let ()
(set! res (appendv (or res (vector)) (vector w)))
(if (hash? seen) (hash-set! seen w #t) (begin (set! seen (make-hash)) (hash-set! seen w #t)))
) (void))
  ))))
(set! res (insertion_sort res))
(_return res)
))
)
(define word_by_signature (let ([h (make-hash)]) h))
(define (build_map words)
  (let/ec _return (begin
(let ([__seq words]) (let/ec _break (for ([w (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define sig (sort_chars w))
(define arr (vector))
(if (hash-has-key? word_by_signature sig) (let ()
(set! arr (if word_by_signature (hash-ref word_by_signature sig (vector)) #f))
) (void))
(set! arr (appendv (or arr (vector)) (vector w)))
(if (hash? word_by_signature) (hash-set! word_by_signature sig arr) (begin (set! word_by_signature (make-hash)) (hash-set! word_by_signature sig arr)))
  ))))
))
)
(define (anagram my_word)
  (let/ec _return (begin
(define sig (sort_chars my_word))
(if (hash-has-key? word_by_signature sig) (let ()
(_return (if word_by_signature (hash-ref word_by_signature sig (vector)) #f))
) (void))
(_return (vector))
))
)
(define (main)
  (let/ec _return (begin
(define text (read_file "words.txt"))
(define lines (split text "\n"))
(define words (unique_sorted lines))
(build_map words)
(let ([__seq words]) (let/ec _break (for ([w (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define anas (anagram w))
(if (let ([__l (cond [(not anas) 0] [(string? anas) (string-length anas)] [(vector? anas) (vector-length anas)] [(hash? anas) (hash-count anas)] [else (length anas)])] [__r 1]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(define line (string-append w ":"))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not anas) 0] [(string? anas) (string-length anas)] [(vector? anas) (vector-length anas)] [(hash? anas) (hash-count anas)] [else (length anas)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! line (string-append line ","))
) (void))
      (set! line (string-append line (if anas (if (hash? anas) (hash-ref anas i #f) (safe-index anas (int i))) #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(displayln line)
) (void))
  ))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
