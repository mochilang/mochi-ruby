;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (split_words s)
  (let/ec _return (begin
(define words (list))
(define current "")
 (let/ec _break (for ([__i (in-range (string-length s))])
  (define ch (substring s __i (+ __i 1)))
  (let/ec _cont
(if (string=? ch " ") (let ()
(if (not (string=? current "")) (let ()
(set! words (append (or words (list)) (list current)))
(set! current "")
) (void))
) (let ()
(set! current (string-append current ch))
))
  )))
(if (not (string=? current "")) (let ()
(set! words (append (or words (list)) (list current)))
) (void))
(_return words)
))
)
(define (is_alnum c)
  (let/ec _return (begin
(_return (or (or (or (string-contains? "0123456789" c) (string-contains? "abcdefghijklmnopqrstuvwxyz" c)) (string-contains? "ABCDEFGHIJKLMNOPQRSTUVWXYZ" c)) (string=? c " ")))
))
)
(define (split_input text)
  (let/ec _return (begin
(define result (list))
(define current "")
 (let/ec _break (for ([__i (in-range (string-length text))])
  (define ch (substring text __i (+ __i 1)))
  (let/ec _cont
(if (is_alnum ch) (let ()
(set! current (string-append current ch))
) (let ()
(if (not (string=? current "")) (let ()
(set! result (append (or result (list)) (list (split_words current))))
(set! current "")
) (void))
))
  )))
(if (not (string=? current "")) (let ()
(set! result (append (or result (list)) (list (split_words current))))
) (void))
(_return result)
))
)
(define (capitalize word)
  (let/ec _return (begin
(if (equal? (cond [(not word) 0] [(string? word) (string-length word)] [(hash? word) (hash-count word)] [else (length word)]) 0) (let ()
(_return "")
) (void))
(if (equal? (cond [(not word) 0] [(string? word) (string-length word)] [(hash? word) (hash-count word)] [else (length word)]) 1) (let ()
(_return (upper word))
) (void))
(_return (let ([__l (upper (slice word 0 1))] [__r (lower (slice word 1 (string-length word)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (to_simple_case text)
  (let/ec _return (begin
(define parts (split_input text))
(define res "")
(let/ec _break (for ([sub parts])
  (let/ec _cont
(let/ec _break (for ([w sub])
  (let/ec _cont
(set! res (string-append res (capitalize w)))
  )))
  )))
(_return res)
))
)
(define (to_complex_case text upper_flag sep)
  (let/ec _return (begin
(define parts (split_input text))
(define res "")
(let/ec _break (for ([sub parts])
  (let/ec _cont
(define first #t)
(let/ec _break (for ([w sub])
  (let/ec _cont
(define word (if upper_flag (upper w) (lower w)))
(if first (let ()
(set! res (string-append res word))
(set! first #f)
) (let ()
(set! res (string-append (string-append res sep) word))
))
  )))
  )))
(_return res)
))
)
(define (to_pascal_case text)
  (let/ec _return (begin
(_return (to_simple_case text))
))
)
(define (to_camel_case text)
  (let/ec _return (begin
(define s (to_simple_case text))
(if (equal? (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 0) (let ()
(_return "")
) (void))
(_return (let ([__l (lower (slice s 0 1))] [__r (slice s 1 (string-length s))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (to_snake_case text upper_flag)
  (let/ec _return (begin
(_return (to_complex_case text upper_flag "_"))
))
)
(define (to_kebab_case text upper_flag)
  (let/ec _return (begin
(_return (to_complex_case text upper_flag "-"))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to_pascal_case "one two 31235three4four"))
(displayln (to_camel_case "one two 31235three4four"))
(displayln (to_snake_case "one two 31235three4four" #t))
(displayln (to_snake_case "one two 31235three4four" #f))
(displayln (to_kebab_case "one two 31235three4four" #t))
(displayln (to_kebab_case "one two 31235three4four" #f))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
