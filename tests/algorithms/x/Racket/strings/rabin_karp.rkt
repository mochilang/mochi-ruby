;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define alphabet_size 256)
(define modulus 1000003)
(define (index_of_char s ch)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (string=? (substring s i (+ i 1)) ch) (let ()
(_return i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (- 1))
))
)
(define (ord ch)
  (let/ec _return (begin
(define upper_ "ABCDEFGHIJKLMNOPQRSTUVWXYZ")
(define lower_ "abcdefghijklmnopqrstuvwxyz")
(define digits "0123456789")
(define idx (index_of_char upper_ ch))
(if (let ([__l idx] [__r 0]) (>= (int __l) (int __r))) (let ()
(_return (let ([__l 65] [__r idx]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(set! idx (index_of_char lower_ ch))
(if (let ([__l idx] [__r 0]) (>= (int __l) (int __r))) (let ()
(_return (let ([__l 97] [__r idx]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(set! idx (index_of_char digits ch))
(if (let ([__l idx] [__r 0]) (>= (int __l) (int __r))) (let ()
(_return (let ([__l 48] [__r idx]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(if (string=? ch "ü") (let ()
(_return 252)
) (void))
(if (string=? ch "Ü") (let ()
(_return 220)
) (void))
(if (string=? ch " ") (let ()
(_return 32)
) (void))
(_return 0)
))
)
(define (rabin_karp pattern text)
  (let/ec _return (begin
(define p_len (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]))
(define t_len (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]))
(if (let ([__l p_len] [__r t_len]) (> (int __l) (int __r))) (let ()
(_return #f)
) (void))
(define p_hash 0)
(define t_hash 0)
(define modulus_power 1)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r p_len]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (set! p_hash (modulo (let ([__l (ord (substring pattern i (+ i 1)))] [__r (* p_hash alphabet_size)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) modulus))
      (set! t_hash (modulo (let ([__l (ord (substring text i (+ i 1)))] [__r (* t_hash alphabet_size)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) modulus))
      (if (not (equal? i (- p_len 1))) (let ()
(set! modulus_power (modulo (* modulus_power alphabet_size) modulus))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (- t_len p_len)]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (if (and (equal? t_hash p_hash) (string=? (slice text j (let ([__l j] [__r p_len]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) pattern)) (let ()
(_return #t)
) (void))
      (if (equal? j (- t_len p_len)) (let ()
(set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_cont)
) (void))
      (set! t_hash (modulo (let ([__l (* (- t_hash (* (ord (substring text j (+ j 1))) modulus_power)) alphabet_size)] [__r (ord (substring text (let ([__l j] [__r p_len]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (+ (let ([__l j] [__r p_len]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) modulus))
      (if (let ([__l t_hash] [__r 0]) (< (int __l) (int __r))) (let ()
(set! t_hash (let ([__l t_hash] [__r modulus]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #f)
))
)
(define (test_rabin_karp)
  (let/ec _return (begin
(define pattern1 "abc1abc12")
(define text1 "alskfjaldsabc1abc1abc12k23adsfabcabc")
(define text2 "alskfjaldsk23adsfabcabc")
(if (or (not (rabin_karp pattern1 text1)) (rabin_karp pattern1 text2)) (let ()
(displayln "Failure")
(_return void)
) (void))
(define pattern2 "ABABX")
(define text3 "ABABZABABYABABX")
(if (not (rabin_karp pattern2 text3)) (let ()
(displayln "Failure")
(_return void)
) (void))
(define pattern3 "AAAB")
(define text4 "ABAAAAAB")
(if (not (rabin_karp pattern3 text4)) (let ()
(displayln "Failure")
(_return void)
) (void))
(define pattern4 "abcdabcy")
(define text5 "abcxabcdabxabcdabcdabcy")
(if (not (rabin_karp pattern4 text5)) (let ()
(displayln "Failure")
(_return void)
) (void))
(define pattern5 "Lü")
(define text6 "Lüsai")
(if (not (rabin_karp pattern5 text6)) (let ()
(displayln "Failure")
(_return void)
) (void))
(define pattern6 "Lue")
(if (rabin_karp pattern6 text6) (let ()
(displayln "Failure")
(_return void)
) (void))
(displayln "Success.")
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(test_rabin_karp)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
