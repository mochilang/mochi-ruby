;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (z_function s)
  (let/ec _return (begin
(define z (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (set! z (append (or z (list)) (list 0)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define l 0)
(define r 0)
(set! i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (let ([__l i] [__r r]) (<= (int __l) (int __r))) (let ()
(define min_edge (let ([__l (- r i)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define zi (if z (if (hash? z) (hash-ref z (- i l) #f) (safe-index z (int (- i l)))) #f))
(if (let ([__l zi] [__r min_edge]) (< (int __l) (int __r))) (let ()
(set! min_edge zi)
) (void))
(set! z (list-set z (int i) min_edge))
) (void))
      (let/ec _break (let loop ()
  (if (go_next i z s) (begin
    (let/ec _cont
      (set! z (list-set z (int i) (let ([__l (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    )
    (loop)) (void))))
      (if (let ([__l (- (let ([__l i] [__r (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1)] [__r r]) (> (int __l) (int __r))) (let ()
(set! l i)
(set! r (- (let ([__l i] [__r (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return z)
))
)
(define (go_next i z s)
  (let/ec _return (begin
(_return (and (let ([__l (let ([__l i] [__r (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (< (int __l) (int __r))) (string=? (substring s (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f) (+ (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f) 1)) (substring s (let ([__l i] [__r (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (+ (let ([__l i] [__r (if z (if (hash? z) (hash-ref z i #f) (safe-index z (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1)))))
))
)
(define (find_pattern pattern input_str)
  (let/ec _return (begin
(define answer 0)
(define z_res (z_function (string-append pattern input_str)))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not z_res) 0] [(string? z_res) (string-length z_res)] [(hash? z_res) (hash-count z_res)] [else (length z_res)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (let ([__l (if z_res (if (hash? z_res) (hash-ref z_res i #f) (safe-index z_res (int i))) #f)] [__r (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])]) (>= (int __l) (int __r))) (let ()
(set! answer (let ([__l answer] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return answer)
))
)
(define (list_eq_int a b)
  (let/ec _return (begin
(if (not (equal? (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)]) (cond [(not b) 0] [(string? b) (string-length b)] [(hash? b) (hash-count b)] [else (length b)]))) (let ()
(_return #f)
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (not (equal? (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f))) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (test_z_function)
  (let/ec _return (begin
(define s1 "abracadabra")
(define expected1 (list 0 0 0 1 0 1 0 4 0 0 1))
(define r1 (z_function s1))
(if (not (list_eq_int r1 expected1)) (let ()
(panic "z_function abracadabra failed")
) (void))
(define s2 "aaaa")
(define expected2 (list 0 3 2 1))
(define r2 (z_function s2))
(if (not (list_eq_int r2 expected2)) (let ()
(panic "z_function aaaa failed")
) (void))
(define s3 "zxxzxxz")
(define expected3 (list 0 0 0 4 0 0 1))
(define r3 (z_function s3))
(if (not (list_eq_int r3 expected3)) (let ()
(panic "z_function zxxzxxz failed")
) (void))
))
)
(define (test_find_pattern)
  (let/ec _return (begin
(if (not (equal? (find_pattern "abr" "abracadabra") 2)) (let ()
(panic "find_pattern abr failed")
) (void))
(if (not (equal? (find_pattern "a" "aaaa") 4)) (let ()
(panic "find_pattern aaaa failed")
) (void))
(if (not (equal? (find_pattern "xz" "zxxzxxz") 2)) (let ()
(panic "find_pattern xz failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_z_function)
(test_find_pattern)
(define r1 (z_function "abracadabra"))
(define r2 (z_function "aaaa"))
(define r3 (z_function "zxxzxxz"))
(displayln (to-string r1))
(displayln (to-string r2))
(displayln (to-string r3))
(displayln (to-string (find_pattern "abr" "abracadabra")))
(displayln (to-string (find_pattern "a" "aaaa")))
(displayln (to-string (find_pattern "xz" "zxxzxxz")))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
