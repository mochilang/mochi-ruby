;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define freq_map (let ([h (make-hash)]) h))
(define (heapify arr index heap_size)
  (let/ec _return (begin
(define largest index)
(define left (let ([__l (* 2 index)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define right (let ([__l (* 2 index)] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (let ([__l left] [__r heap_size]) (< (int __l) (int __r))) (let ()
(define left_item (if arr (if (hash? arr) (hash-ref arr left #f) (safe-index arr (int left))) #f))
(define largest_item (if arr (if (hash? arr) (hash-ref arr largest #f) (safe-index arr (int largest))) #f))
(if (let ([__l (if left_item (hash-ref left_item "count" #f) #f)] [__r (if largest_item (hash-ref largest_item "count" #f) #f)]) (> (int __l) (int __r))) (let ()
(set! largest left)
) (void))
) (void))
(if (let ([__l right] [__r heap_size]) (< (int __l) (int __r))) (let ()
(define right_item (if arr (if (hash? arr) (hash-ref arr right #f) (safe-index arr (int right))) #f))
(define largest_item2 (if arr (if (hash? arr) (hash-ref arr largest #f) (safe-index arr (int largest))) #f))
(if (let ([__l (if right_item (hash-ref right_item "count" #f) #f)] [__r (if largest_item2 (hash-ref largest_item2 "count" #f) #f)]) (> (int __l) (int __r))) (let ()
(set! largest right)
) (void))
) (void))
(if (not (equal? largest index)) (let ()
(define temp (if arr (if (hash? arr) (hash-ref arr largest #f) (safe-index arr (int largest))) #f))
(set! arr (list-set arr (int largest) (if arr (if (hash? arr) (hash-ref arr index #f) (safe-index arr (int index))) #f)))
(set! arr (list-set arr (int index) temp))
(heapify arr largest heap_size)
) (void))
))
)
(define (build_max_heap arr)
  (let/ec _return (begin
(define i (- (floor (/ (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 2)) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (>= (int __l) (int __r))) (begin
    (let/ec _cont
      (heapify arr i (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]))
      (set! i (- i 1))
    )
    (loop)) (void))))
))
)
(define (top_k_frequent_words words k_value)
  (let/ec _return (begin
(set! freq_map (let ([h (make-hash)]) h))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not words) 0] [(string? words) (string-length words)] [(hash? words) (hash-count words)] [else (length words)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define w (if words (if (hash? words) (hash-ref words i #f) (safe-index words (int i))) #f))
      (if (hash-has-key? freq_map w) (let ()
(if (hash? freq_map) (hash-set! freq_map w (let ([__l (if freq_map (hash-ref freq_map w 0) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! freq_map (make-hash)) (hash-set! freq_map w (let ([__l (if freq_map (hash-ref freq_map w 0) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (let ()
(if (hash? freq_map) (hash-set! freq_map w 1) (begin (set! freq_map (make-hash)) (hash-set! freq_map w 1)))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define heap (list))
(let/ec _break (for ([w (in-hash-keys freq_map)])
  (let/ec _cont
(set! heap (append (or heap (list)) (list (let ([h (make-hash)]) (hash-set! h "word" w) (hash-set! h "count" (if freq_map (hash-ref freq_map w 0) #f)) h))))
  )))
(build_max_heap heap)
(define result (list))
(define heap_size (cond [(not heap) 0] [(string? heap) (string-length heap)] [(hash? heap) (hash-count heap)] [else (length heap)]))
(define limit k_value)
(if (let ([__l limit] [__r heap_size]) (> (int __l) (int __r))) (let ()
(set! limit heap_size)
) (void))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r limit]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define item (if heap (if (hash? heap) (hash-ref heap 0 #f) (safe-index heap (int 0))) #f))
      (set! result (append (or result (list)) (list (if item (hash-ref item "word" #f) #f))))
      (set! heap (list-set heap (int 0) (if heap (if (hash? heap) (hash-ref heap (- heap_size 1) #f) (safe-index heap (int (- heap_size 1)))) #f)))
      (set! heap (list-set heap (int (- heap_size 1)) item))
      (set! heap_size (- heap_size 1))
      (heapify heap 0 heap_size)
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (main)
  (let/ec _return (begin
(define sample (list "a" "b" "c" "a" "c" "c"))
(displayln (top_k_frequent_words sample 3))
(displayln (top_k_frequent_words sample 2))
(displayln (top_k_frequent_words sample 1))
(displayln (top_k_frequent_words sample 0))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
