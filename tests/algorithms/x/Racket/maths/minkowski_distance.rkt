;; Generated by Mochi 0.10.61 on 2025-08-08 16:10 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (abs_val x)
  (let/ec _return (begin
(if (< x 0.0) (let ()
(_return (- x))
) (void))
(_return x)
))
)
(define (pow_float base exp)
  (let/ec _return (begin
(define result 1.0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i exp) (let/ec _cont
    (set! result (* result base))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (nth_root value n)
  (let/ec _return (begin
(if (equal? value 0.0) (let ()
(_return 0.0)
) (void))
(define x (/ value (exact->inexact n)))
(define i 0)
(let/ec _break (let loop ()
  (if (< i 20) (let/ec _cont
    (define num (let ([__l (* (exact->inexact (- n 1)) x)] [__r (/ value (pow_float x (- n 1)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! x (/ num (exact->inexact n)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return x)
))
)
(define (minkowski_distance point_a point_b order)
  (let/ec _return (begin
(if (< order 1) (let ()
(panic "The order must be greater than or equal to 1.")
) (void))
(if (not (equal? (cond [(not point_a) 0] [(string? point_a) (string-length point_a)] [(hash? point_a) (hash-count point_a)] [else (length point_a)]) (cond [(not point_b) 0] [(string? point_b) (string-length point_b)] [(hash? point_b) (hash-count point_b)] [else (length point_b)]))) (let ()
(panic "Both points must have the same dimension.")
) (void))
(define total 0.0)
(define idx 0)
(let/ec _break (let loop ()
  (if (< idx (cond [(not point_a) 0] [(string? point_a) (string-length point_a)] [(hash? point_a) (hash-count point_a)] [else (length point_a)])) (let/ec _cont
    (define diff (abs_val (- (if point_a (if (hash? point_a) (hash-ref point_a idx #f) (list-ref point_a (int idx))) #f) (if point_b (if (hash? point_b) (hash-ref point_b idx #f) (list-ref point_b (int idx))) #f))))
    (set! total (let ([__l total] [__r (pow_float diff order)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (nth_root total order))
))
)
(define (test_minkowski)
  (let/ec _return (begin
(if (> (abs_val (- (minkowski_distance (list 1.0 1.0) (list 2.0 2.0) 1) 2.0)) 0.0001) (let ()
(panic "minkowski_distance test1 failed")
) (void))
(if (> (abs_val (- (minkowski_distance (list 1.0 2.0 3.0 4.0) (list 5.0 6.0 7.0 8.0) 2) 8.0)) 0.0001) (let ()
(panic "minkowski_distance test2 failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_minkowski)
(displayln (minkowski_distance (list 5.0) (list 0.0) 3))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us (inexact->exact (round (* _dur 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
