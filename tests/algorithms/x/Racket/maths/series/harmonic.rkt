;; Generated by Mochi 0.10.67 on 2025-08-17 12:10 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (let ([s (~r x #:precision 15 #:notation 'positional)])
       (if (and (inexact? x) (integer? x))
           (number->string (inexact->exact x))
           s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (is_harmonic_series series)
  (let/ec _return (begin
(if (equal? (cond [(not series) 0] [(string? series) (string-length series)] [(hash? series) (hash-count series)] [else (length series)]) 0) (let ()
(panic "Input list must be a non empty list")
) (void))
(if (equal? (cond [(not series) 0] [(string? series) (string-length series)] [(hash? series) (hash-count series)] [else (length series)]) 1) (let ()
(if (equal? (if series (if (hash? series) (hash-ref series 0 #f) (safe-index series (int 0))) #f) 0.0) (let ()
(panic "Input series cannot have 0 as an element")
) (void))
(_return #t)
) (void))
(define rec_series (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not series) 0] [(string? series) (string-length series)] [(hash? series) (hash-count series)] [else (length series)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define val (if series (if (hash? series) (hash-ref series i #f) (safe-index series (int i))) #f))
      (if (equal? val 0.0) (let ()
(panic "Input series cannot have 0 as an element")
) (void))
      (set! rec_series (append (or rec_series (list)) (list (/ 1.0 val))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define common_diff (- (if rec_series (if (hash? rec_series) (hash-ref rec_series 1 #f) (safe-index rec_series (int 1))) #f) (if rec_series (if (hash? rec_series) (hash-ref rec_series 0 #f) (safe-index rec_series (int 0))) #f)))
(define idx 2)
(let/ec _break (let loop ()
  (if (let ([__l idx] [__r (cond [(not rec_series) 0] [(string? rec_series) (string-length rec_series)] [(hash? rec_series) (hash-count rec_series)] [else (length rec_series)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (equal? (- (if rec_series (if (hash? rec_series) (hash-ref rec_series idx #f) (safe-index rec_series (int idx))) #f) (if rec_series (if (hash? rec_series) (hash-ref rec_series (- idx 1) #f) (safe-index rec_series (int (- idx 1)))) #f)) common_diff)) (let ()
(_return #f)
) (void))
      (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (harmonic_mean series)
  (let/ec _return (begin
(if (equal? (cond [(not series) 0] [(string? series) (string-length series)] [(hash? series) (hash-count series)] [else (length series)]) 0) (let ()
(panic "Input list must be a non empty list")
) (void))
(define total 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not series) 0] [(string? series) (string-length series)] [(hash? series) (hash-count series)] [else (length series)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! total (let ([__l total] [__r (/ 1.0 (if series (if (hash? series) (hash-ref series i #f) (safe-index series (int i))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (/ (exact->inexact (cond [(not series) 0] [(string? series) (string-length series)] [(hash? series) (hash-count series)] [else (length series)])) total))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (is_harmonic_series (list 1.0 (/ 2.0 3.0) (/ 1.0 2.0) (/ 2.0 5.0) (/ 1.0 3.0))))
(displayln (is_harmonic_series (list 1.0 (/ 2.0 3.0) (/ 2.0 5.0) (/ 1.0 3.0))))
(displayln (harmonic_mean (list 1.0 4.0 4.0)))
(displayln (harmonic_mean (list 3.0 6.0 9.0 12.0)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
