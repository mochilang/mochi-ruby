;; Generated by Mochi 0.10.61 on 2025-08-08 16:10 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (normalize_index index n)
  (let/ec _return (begin
(if (< index 0) (let ()
(_return (let ([__l n] [__r index]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(_return index)
))
)
(define (find_max_iterative nums)
  (let/ec _return (begin
(if (equal? (cond [(not nums) 0] [(string? nums) (string-length nums)] [(hash? nums) (hash-count nums)] [else (length nums)]) 0) (let ()
(panic "find_max_iterative() arg is an empty sequence")
) (void))
(define max_num (if nums (if (hash? nums) (hash-ref nums 0 #f) (list-ref nums (int 0))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not nums) 0] [(string? nums) (string-length nums)] [(hash? nums) (hash-count nums)] [else (length nums)])) (let/ec _cont
    (define x (if nums (if (hash? nums) (hash-ref nums i #f) (list-ref nums (int i))) #f))
    (if (> x max_num) (let ()
(set! max_num x)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return max_num)
))
)
(define (find_max_recursive nums left right)
  (let/ec _return (begin
(define n (cond [(not nums) 0] [(string? nums) (string-length nums)] [(hash? nums) (hash-count nums)] [else (length nums)]))
(if (equal? n 0) (let ()
(panic "find_max_recursive() arg is an empty sequence")
) (void))
(if (or (or (or (>= left n) (< left (- 0 n))) (>= right n)) (< right (- 0 n))) (let ()
(panic "list index out of range")
) (void))
(define l (normalize_index left n))
(define r (normalize_index right n))
(if (equal? l r) (let ()
(_return (if nums (if (hash? nums) (hash-ref nums l #f) (list-ref nums (int l))) #f))
) (void))
(define mid (floor (/ (let ([__l l] [__r r]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2)))
(define left_max (find_max_recursive nums l mid))
(define right_max (find_max_recursive nums (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) r))
(if (>= left_max right_max) (let ()
(_return left_max)
) (void))
(_return right_max)
))
)
(define (test_find_max)
  (let/ec _return (begin
(define arr (list 2.0 4.0 9.0 7.0 19.0 94.0 5.0))
(if (not (equal? (find_max_iterative arr) 94.0)) (let ()
(panic "find_max_iterative failed")
) (void))
(if (not (equal? (find_max_recursive arr 0 (- (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 1)) 94.0)) (let ()
(panic "find_max_recursive failed")
) (void))
(if (not (equal? (find_max_recursive arr (- (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (- 1)) 94.0)) (let ()
(panic "negative index handling failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_find_max)
(define nums (list 2.0 4.0 9.0 7.0 19.0 94.0 5.0))
(displayln (find_max_iterative nums))
(displayln (find_max_recursive nums 0 (- (cond [(not nums) 0] [(string? nums) (string-length nums)] [(hash? nums) (hash-count nums)] [else (length nums)]) 1)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us (inexact->exact (round (* _dur 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
