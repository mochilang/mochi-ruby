;; Generated by Mochi 0.10.61 on 2025-08-08 16:56 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (runge_kutta f y0 x0 h x_end)
  (let/ec _return (begin
(define span (/ (- x_end x0) h))
(define n (int span))
(if (< (float n) span) (let ()
(set! n (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(define y (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (let/ec _cont
    (set! y (append y (list 0.0)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! y (list-set y (int 0) y0))
(define x x0)
(define k 0)
(let/ec _break (let loop ()
  (if (< k n) (let/ec _cont
    (define k1 (f x (if y (if (hash? y) (hash-ref y k #f) (list-ref y (int k))) #f)))
    (define k2 (f (let ([__l x] [__r (* 0.5 h)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l (if y (if (hash? y) (hash-ref y k #f) (list-ref y (int k))) #f)] [__r (* (* 0.5 h) k1)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (define k3 (f (let ([__l x] [__r (* 0.5 h)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l (if y (if (hash? y) (hash-ref y k #f) (list-ref y (int k))) #f)] [__r (* (* 0.5 h) k2)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (define k4 (f (let ([__l x] [__r h]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l (if y (if (hash? y) (hash-ref y k #f) (list-ref y (int k))) #f)] [__r (* h k3)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (set! y (list-set y (int (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (let ([__l (if y (if (hash? y) (hash-ref y k #f) (list-ref y (int k))) #f)] [__r (* (* (/ 1.0 6.0) h) (let ([__l (let ([__l (let ([__l k1] [__r (* 2.0 k2)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (* 2.0 k3)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r k4]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (set! x (let ([__l x] [__r h]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return y)
))
)
(define (test_runge_kutta)
  (let/ec _return (begin
(define (f x y)
  (let/ec _return (begin
(_return y)
))
)
(define result (runge_kutta f 1.0 0.0 0.01 5.0))
(define last (if result (if (hash? result) (hash-ref result (- (cond [(not result) 0] [(string? result) (string-length result)] [(hash? result) (hash-count result)] [else (length result)]) 1) #f) (list-ref result (int (- (cond [(not result) 0] [(string? result) (string-length result)] [(hash? result) (hash-count result)] [else (length result)]) 1)))) #f))
(define expected 148.41315904125113)
(define diff (- last expected))
(if (< diff 0.0) (let ()
(set! diff (- diff))
) (void))
(if (> diff 1e-06) (let ()
(panic "runge_kutta failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_runge_kutta)
(define (f x y)
  (let/ec _return (begin
(_return y)
))
)
(define r (runge_kutta f 1.0 0.0 0.1 1.0))
(displayln (format "~a" (if r (if (hash? r) (hash-ref r (- (cond [(not r) 0] [(string? r) (string-length r)] [(hash? r) (hash-count r)] [else (length r)]) 1) #f) (list-ref r (int (- (cond [(not r) 0] [(string? r) (string-length r)] [(hash? r) (hash-count r)] [else (length r)]) 1)))) #f)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
