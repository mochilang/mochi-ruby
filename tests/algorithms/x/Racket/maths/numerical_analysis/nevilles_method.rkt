;; Generated by Mochi 0.10.61 on 2025-08-08 16:10 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (neville_interpolate x_points y_points x0)
  (let/ec _return (begin
(define n (cond [(not x_points) 0] [(string? x_points) (string-length x_points)] [(hash? x_points) (hash-count x_points)] [else (length x_points)]))
(define q (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j n) (let/ec _cont
    (set! row (append row (list 0.0)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! q (append q (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (set! q (list-set q i (list-set (list-ref q i) 1 (if y_points (if (hash? y_points) (hash-ref y_points i #f) (list-ref y_points (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define col 2)
(let/ec _break (let loop ()
  (if (< col n) (let/ec _cont
    (define row_idx col)
    (let/ec _break (let loop ()
  (if (< row_idx n) (let/ec _cont
    (set! q (list-set q row_idx (list-set (list-ref q row_idx) col (/ (- (* (- x0 (if x_points (if (hash? x_points) (hash-ref x_points (let ([__l (- row_idx col)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (list-ref x_points (int (let ([__l (- row_idx col)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f)) (if (if q (if (hash? q) (hash-ref q row_idx #f) (list-ref q (int row_idx))) #f) (if (hash? (if q (if (hash? q) (hash-ref q row_idx #f) (list-ref q (int row_idx))) #f)) (hash-ref (if q (if (hash? q) (hash-ref q row_idx #f) (list-ref q (int row_idx))) #f) (- col 1) #f) (list-ref (if q (if (hash? q) (hash-ref q row_idx #f) (list-ref q (int row_idx))) #f) (int (- col 1)))) #f)) (* (- x0 (if x_points (if (hash? x_points) (hash-ref x_points row_idx #f) (list-ref x_points (int row_idx))) #f)) (if (if q (if (hash? q) (hash-ref q (- row_idx 1) #f) (list-ref q (int (- row_idx 1)))) #f) (if (hash? (if q (if (hash? q) (hash-ref q (- row_idx 1) #f) (list-ref q (int (- row_idx 1)))) #f)) (hash-ref (if q (if (hash? q) (hash-ref q (- row_idx 1) #f) (list-ref q (int (- row_idx 1)))) #f) (- col 1) #f) (list-ref (if q (if (hash? q) (hash-ref q (- row_idx 1) #f) (list-ref q (int (- row_idx 1)))) #f) (int (- col 1)))) #f))) (- (if x_points (if (hash? x_points) (hash-ref x_points row_idx #f) (list-ref x_points (int row_idx))) #f) (if x_points (if (hash? x_points) (hash-ref x_points (let ([__l (- row_idx col)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (list-ref x_points (int (let ([__l (- row_idx col)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f))))))
    (set! row_idx (let ([__l row_idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! col (let ([__l col] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "value" (if (if q (if (hash? q) (hash-ref q (- n 1) #f) (list-ref q (int (- n 1)))) #f) (if (hash? (if q (if (hash? q) (hash-ref q (- n 1) #f) (list-ref q (int (- n 1)))) #f)) (hash-ref (if q (if (hash? q) (hash-ref q (- n 1) #f) (list-ref q (int (- n 1)))) #f) (- n 1) #f) (list-ref (if q (if (hash? q) (hash-ref q (- n 1) #f) (list-ref q (int (- n 1)))) #f) (int (- n 1)))) #f)) (hash-set! h "table" q) h))
))
)
(define (test_neville)
  (let/ec _return (begin
(define xs (list 1.0 2.0 3.0 4.0 6.0))
(define ys (list 6.0 7.0 8.0 9.0 11.0))
(define r1 (neville_interpolate xs ys 5.0))
(if (not (equal? (if r1 (hash-ref r1 "value" #f) #f) 10.0)) (let ()
(panic "neville_interpolate at 5 failed")
) (void))
(define r2 (neville_interpolate xs ys 99.0))
(if (not (equal? (if r2 (hash-ref r2 "value" #f) #f) 104.0)) (let ()
(panic "neville_interpolate at 99 failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_neville)
(define xs (list 1.0 2.0 3.0 4.0 6.0))
(define ys (list 6.0 7.0 8.0 9.0 11.0))
(define r (neville_interpolate xs ys 5.0))
(displayln (if r (hash-ref r "value" #f) #f))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us (exact-round (* _dur 1000))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
