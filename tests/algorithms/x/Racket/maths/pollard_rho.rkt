;; Generated by Mochi 0.10.67 on 2025-08-17 12:10 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (let ([s (~r x #:precision 17 #:notation 'positional)])
       (if (and (inexact? x) (integer? x))
           (number->string (inexact->exact x))
           s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (gcd a b)
  (let/ec _return (begin
(define x (if (let ([__l a] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (- a) a))
(define y (if (let ([__l b] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (- b) b))
(let/ec _break (let loop ()
  (if (not (equal? y 0)) (begin
    (let/ec _cont
      (define t (modulo x y))
      (set! x y)
      (set! y t)
    )
    (loop)) (void))))
(_return x)
))
)
(define (rand_fn value step modulus)
  (let/ec _return (begin
(_return (modulo (let ([__l (* value value)] [__r step]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) modulus))
))
)
(define (pollard_rho num seed step attempts)
  (let/ec _return (begin
(if (let ([__l num] [__r 2]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(panic "The input value cannot be less than 2")
) (void))
(if (and (let ([__l num] [__r 2]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (equal? (modulo num 2) 0)) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "factor" 2) (hash-set! h "ok" #t) h))
) (void))
(define s seed)
(define st step)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r attempts]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define tortoise s)
      (define hare s)
      (let/ec _break (let loop ()
  (if #t (begin
    (let/ec _cont
      (set! tortoise (rand_fn tortoise st num))
      (set! hare (rand_fn hare st num))
      (set! hare (rand_fn hare st num))
      (define divisor (gcd (- hare tortoise) num))
      (if (equal? divisor 1) (let ()
(_cont)
) (let ()
(if (equal? divisor num) (let ()
(_break)
) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "factor" divisor) (hash-set! h "ok" #t) h))
))
))
    )
    (loop)) (void))))
      (set! s hare)
      (set! st (let ([__l st] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "factor" 0) (hash-set! h "ok" #f) h))
))
)
(define (test_pollard_rho)
  (let/ec _return (begin
(define r1 (pollard_rho 8051 2 1 5))
(if (or (not (if r1 (hash-ref r1 "ok" #f) #f)) (and (not (equal? (if r1 (hash-ref r1 "factor" #f) #f) 83)) (not (equal? (if r1 (hash-ref r1 "factor" #f) #f) 97)))) (let ()
(panic "test1 failed")
) (void))
(define r2 (pollard_rho 10403 2 1 5))
(if (or (not (if r2 (hash-ref r2 "ok" #f) #f)) (and (not (equal? (if r2 (hash-ref r2 "factor" #f) #f) 101)) (not (equal? (if r2 (hash-ref r2 "factor" #f) #f) 103)))) (let ()
(panic "test2 failed")
) (void))
(define r3 (pollard_rho 100 2 1 3))
(if (or (not (if r3 (hash-ref r3 "ok" #f) #f)) (not (equal? (if r3 (hash-ref r3 "factor" #f) #f) 2))) (let ()
(panic "test3 failed")
) (void))
(define r4 (pollard_rho 17 2 1 3))
(if (if r4 (hash-ref r4 "ok" #f) #f) (let ()
(panic "test4 failed")
) (void))
(define r5 (pollard_rho (* (* 17 17) 17) 2 1 3))
(if (or (not (if r5 (hash-ref r5 "ok" #f) #f)) (not (equal? (if r5 (hash-ref r5 "factor" #f) #f) 17))) (let ()
(panic "test5 failed")
) (void))
(define r6 (pollard_rho (* (* 17 17) 17) 2 1 1))
(if (if r6 (hash-ref r6 "ok" #f) #f) (let ()
(panic "test6 failed")
) (void))
(define r7 (pollard_rho (* (* 3 5) 7) 2 1 3))
(if (or (not (if r7 (hash-ref r7 "ok" #f) #f)) (not (equal? (if r7 (hash-ref r7 "factor" #f) #f) 21))) (let ()
(panic "test7 failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_pollard_rho)
(define a (pollard_rho 100 2 1 3))
(if (if a (hash-ref a "ok" #f) #f) (let ()
(displayln (to-string (if a (hash-ref a "factor" #f) #f)))
) (let ()
(displayln "None")
))
(define b (pollard_rho 17 2 1 3))
(if (if b (hash-ref b "ok" #f) #f) (let ()
(displayln (to-string (if b (hash-ref b "factor" #f) #f)))
) (let ()
(displayln "None")
))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
