;; Generated by Mochi 0.10.59 on 2025-08-06 21:08 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define stdout (hash "write" (lambda (s) (display s))))

(define (gcd a b)
  (let/ec _return (begin
(define x (if (< a 0) (- a) a))
(define y (if (< b 0) (- b) b))
(let/ec _break (let loop ()
  (if (not (equal? y 0)) (let/ec _cont
    (define t (modulo x y))
    (set! x y)
    (set! y t)
    (loop)) (void))))
(_return x)
))
)
(define (extended_gcd a b)
  (let/ec _return (begin
(if (equal? b 0) (let ()
(_return (list a 1 0))
) (void))
(define res (extended_gcd b (modulo a b)))
(define d (if res (if (hash? res) (hash-ref res 0 #f) (list-ref res (int 0))) #f))
(define p (if res (if (hash? res) (hash-ref res 1 #f) (list-ref res (int 1))) #f))
(define q (if res (if (hash? res) (hash-ref res 2 #f) (list-ref res (int 2))) #f))
(define x q)
(define y (- p (* q (quotient a b))))
(_return (list d x y))
))
)
(define (diophantine a b c)
  (let/ec _return (begin
(define d (gcd a b))
(if (not (equal? (modulo c d) 0)) (let ()
(panic "No solution")
) (void))
(define eg (extended_gcd a b))
(define r (quotient c d))
(define x (* (if eg (if (hash? eg) (hash-ref eg 1 #f) (list-ref eg (int 1))) #f) r))
(define y (* (if eg (if (hash? eg) (hash-ref eg 2 #f) (list-ref eg (int 2))) #f) r))
(_return (list x y))
))
)
(define (diophantine_all_soln a b c n)
  (let/ec _return (begin
(define base (diophantine a b c))
(define x0 (if base (if (hash? base) (hash-ref base 0 #f) (list-ref base (int 0))) #f))
(define y0 (if base (if (hash? base) (hash-ref base 1 #f) (list-ref base (int 1))) #f))
(define d (gcd a b))
(define p (quotient a d))
(define q (quotient b d))
(define sols (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (define x (let ([__l x0] [__r (* i q)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (define y (- y0 (* i p)))
    (set! sols (append sols (list (list x y))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return sols)
))
)
(define s1 (diophantine 10 6 14))
(define sols (diophantine_all_soln 10 6 14 4))
(define j 0)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" s1))
(let/ec _break (let loop ()
  (if (< j (cond [(string? sols) (string-length sols)] [(hash? sols) (hash-count sols)] [else (length sols)])) (let/ec _cont
    (displayln (format "~a" (if sols (if (hash? sols) (hash-ref sols j #f) (list-ref sols (int j))) #f)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln (format "~a" (diophantine 391 299 (- 69))))
(displayln (format "~a" (extended_gcd 10 6)))
(displayln (format "~a" (extended_gcd 7 5)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
