;; Generated by Mochi 0.10.65 on 2025-08-14 17:09 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (make_matrix n)
  (let/ec _return (begin
(define matrix (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list #f)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! matrix (append (or matrix (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return matrix)
))
)
(define (dfs u graph visited_edge path)
  (let/ec _return (begin
(set! path (append (or path (list)) (list u)))
(if (hash-has-key? graph u) (let ()
(define neighbors (if graph (hash-ref graph u (list)) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not neighbors) 0] [(string? neighbors) (string-length neighbors)] [(hash? neighbors) (hash-count neighbors)] [else (length neighbors)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define v (if neighbors (if (hash? neighbors) (hash-ref neighbors i #f) (safe-index neighbors (int i))) #f))
      (if (not (if (if visited_edge (if (hash? visited_edge) (hash-ref visited_edge u #f) (safe-index visited_edge (int u))) #f) (if (hash? (if visited_edge (if (hash? visited_edge) (hash-ref visited_edge u #f) (safe-index visited_edge (int u))) #f)) (hash-ref (if visited_edge (if (hash? visited_edge) (hash-ref visited_edge u #f) (safe-index visited_edge (int u))) #f) v #f) (safe-index (if visited_edge (if (hash? visited_edge) (hash-ref visited_edge u #f) (safe-index visited_edge (int u))) #f) (int v))) #f)) (let ()
(set! visited_edge (list-set visited_edge u (list-set (list-ref visited_edge u) v #t)))
(set! visited_edge (list-set visited_edge v (list-set (list-ref visited_edge v) u #t)))
(set! path (dfs v graph visited_edge path))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
) (void))
(_return path)
))
)
(define (check_circuit_or_path graph max_node)
  (let/ec _return (begin
(define odd_degree_nodes 0)
(define odd_node (- 1))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r max_node]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (hash-has-key? graph i) (let ()
(if (equal? (modulo (cond [(not (if graph (hash-ref graph i (list)) #f)) 0] [(string? (if graph (hash-ref graph i (list)) #f)) (string-length (if graph (hash-ref graph i (list)) #f))] [(hash? (if graph (hash-ref graph i (list)) #f)) (hash-count (if graph (hash-ref graph i (list)) #f))] [else (length (if graph (hash-ref graph i (list)) #f))]) 2) 1) (let ()
(set! odd_degree_nodes (let ([__l odd_degree_nodes] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! odd_node i)
) (void))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if (equal? odd_degree_nodes 0) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "status" 1) (hash-set! h "odd_node" odd_node) h))
) (void))
(if (equal? odd_degree_nodes 2) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "status" 2) (hash-set! h "odd_node" odd_node) h))
) (void))
(_return (let ([h (make-hash)]) (hash-set! h "status" 3) (hash-set! h "odd_node" odd_node) h))
))
)
(define (check_euler graph max_node)
  (let/ec _return (begin
(define visited_edge (make_matrix max_node))
(define res (check_circuit_or_path graph max_node))
(if (equal? (if res (hash-ref res "status" #f) #f) 3) (let ()
(displayln "graph is not Eulerian")
(displayln "no path")
(_return void)
) (void))
(define start_node 1)
(if (equal? (if res (hash-ref res "status" #f) #f) 2) (let ()
(set! start_node (if res (hash-ref res "odd_node" #f) #f))
(displayln "graph has a Euler path")
) (void))
(if (equal? (if res (hash-ref res "status" #f) #f) 1) (let ()
(displayln "graph has a Euler cycle")
) (void))
(define path (dfs start_node graph visited_edge (list)))
(displayln (to-string path))
))
)
(define g1 (let ([h (make-hash)]) (hash-set! h 1 (list 2 3 4)) (hash-set! h 2 (list 1 3)) (hash-set! h 3 (list 1 2)) (hash-set! h 4 (list 1 5)) (hash-set! h 5 (list 4)) h))
(define g2 (let ([h (make-hash)]) (hash-set! h 1 (list 2 3 4 5)) (hash-set! h 2 (list 1 3)) (hash-set! h 3 (list 1 2)) (hash-set! h 4 (list 1 5)) (hash-set! h 5 (list 1 4)) h))
(define g3 (let ([h (make-hash)]) (hash-set! h 1 (list 2 3 4)) (hash-set! h 2 (list 1 3 4)) (hash-set! h 3 (list 1 2)) (hash-set! h 4 (list 1 2 5)) (hash-set! h 5 (list 4)) h))
(define g4 (let ([h (make-hash)]) (hash-set! h 1 (list 2 3)) (hash-set! h 2 (list 1 3)) (hash-set! h 3 (list 1 2)) h))
(define g5 (let ([h (make-hash)]) (hash-set! h 1 (list)) (hash-set! h 2 (list)) h))
(define max_node 10)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(check_euler g1 max_node)
(check_euler g2 max_node)
(check_euler g3 max_node)
(check_euler g4 max_node)
(check_euler g5 max_node)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
