;; Generated by Mochi 0.10.66 on 2025-08-15 15:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (node_to_string n)
  (let/ec _return (begin
(_return (string-append (string-append (string-append (string-append (string-append (string-append "<node=" (if n (hash-ref n "name" #f) #f)) " inbound=") (if n (hash-ref n "inbound" #f) #f)) " outbound=") (if n (hash-ref n "outbound" #f) #f)) ">"))
))
)
(define (page_rank nodes limit d)
  (let/ec _return (begin
(define ranks (let ([h (make-hash)]) h))
(let ([__seq nodes]) (let/ec _break (for ([n (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (hash? ranks) (hash-set! ranks (if n (hash-ref n "name" #f) #f) 1.0) (begin (set! ranks (make-hash)) (hash-set! ranks (if n (hash-ref n "name" #f) #f) 1.0)))
  ))))
(define outbounds (let ([h (make-hash)]) h))
(let ([__seq nodes]) (let/ec _break (for ([n (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (hash? outbounds) (hash-set! outbounds (if n (hash-ref n "name" #f) #f) (* 1.0 (cond [(not (if n (hash-ref n "outbound" #f) #f)) 0] [(string? (if n (hash-ref n "outbound" #f) #f)) (string-length (if n (hash-ref n "outbound" #f) #f))] [(hash? (if n (hash-ref n "outbound" #f) #f)) (hash-count (if n (hash-ref n "outbound" #f) #f))] [else (length (if n (hash-ref n "outbound" #f) #f))]))) (begin (set! outbounds (make-hash)) (hash-set! outbounds (if n (hash-ref n "name" #f) #f) (* 1.0 (cond [(not (if n (hash-ref n "outbound" #f) #f)) 0] [(string? (if n (hash-ref n "outbound" #f) #f)) (string-length (if n (hash-ref n "outbound" #f) #f))] [(hash? (if n (hash-ref n "outbound" #f) #f)) (hash-count (if n (hash-ref n "outbound" #f) #f))] [else (length (if n (hash-ref n "outbound" #f) #f))])))))
  ))))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r limit]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (displayln (string-append (string-append "======= Iteration " (to-string (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))) " ======="))
      (let ([__seq nodes]) (let/ec _break (for ([n (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define sum_val 0.0)
(let ([__seq (if n (hash-ref n "inbound" #f) #f)]) (let/ec _break (for ([ib (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! sum_val (let ([__l sum_val] [__r (/ (if ranks (hash-ref ranks ib 0.0) #f) (if outbounds (hash-ref outbounds ib 0.0) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
  ))))
(if (hash? ranks) (hash-set! ranks (if n (hash-ref n "name" #f) #f) (let ([__l (- 1.0 d)] [__r (* d sum_val)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! ranks (make-hash)) (hash-set! ranks (if n (hash-ref n "name" #f) #f) (let ([__l (- 1.0 d)] [__r (* d sum_val)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
  ))))
      (displayln ranks)
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return ranks)
))
)
(define names (list "A" "B" "C"))
(define graph (list (list 0 1 1) (list 0 0 1) (list 1 0 0)))
(define nodes (list))
(define ri 0)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(let ([__seq names]) (let/ec _break (for ([name (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! nodes (append (or nodes (list)) (list (let ([h (make-hash)]) (hash-set! h "name" name) (hash-set! h "inbound" (list)) (hash-set! h "outbound" (list)) h))))
  ))))
(let/ec _break (let loop ()
  (if (let ([__l ri] [__r (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (if graph (if (hash? graph) (hash-ref graph ri #f) (safe-index graph (int ri))) #f))
      (define ci 0)
      (let/ec _break (let loop ()
  (if (let ([__l ci] [__r (cond [(not row) 0] [(string? row) (string-length row)] [(hash? row) (hash-count row)] [else (length row)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (equal? (if row (if (hash? row) (hash-ref row ci #f) (safe-index row (int ci))) #f) 1) (let ()
(define n_in (if nodes (if (hash? nodes) (hash-ref nodes ci #f) (safe-index nodes (int ci))) #f))
(if (hash? n_in) (hash-set! n_in "inbound" (append (or (if n_in (hash-ref n_in "inbound" #f) #f) (list)) (list (if names (if (hash? names) (hash-ref names ri #f) (safe-index names (int ri))) #f)))) (begin (set! n_in (make-hash)) (hash-set! n_in "inbound" (append (or (if n_in (hash-ref n_in "inbound" #f) #f) (list)) (list (if names (if (hash? names) (hash-ref names ri #f) (safe-index names (int ri))) #f))))))
(set! nodes (list-set nodes (int ci) n_in))
(define n_out (if nodes (if (hash? nodes) (hash-ref nodes ri #f) (safe-index nodes (int ri))) #f))
(if (hash? n_out) (hash-set! n_out "outbound" (append (or (if n_out (hash-ref n_out "outbound" #f) #f) (list)) (list (if names (if (hash? names) (hash-ref names ci #f) (safe-index names (int ci))) #f)))) (begin (set! n_out (make-hash)) (hash-set! n_out "outbound" (append (or (if n_out (hash-ref n_out "outbound" #f) #f) (list)) (list (if names (if (hash? names) (hash-ref names ci #f) (safe-index names (int ci))) #f))))))
(set! nodes (list-set nodes (int ri) n_out))
) (void))
      (set! ci (let ([__l ci] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! ri (let ([__l ri] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(displayln "======= Nodes =======")
(let ([__seq nodes]) (let/ec _break (for ([n (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(displayln n)
  ))))
(page_rank nodes 3 0.85)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
