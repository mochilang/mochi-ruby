;; Generated by Mochi 0.10.65 on 2025-08-14 17:09 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (key p)
  (let/ec _return (begin
(_return (string-append (string-append (to-string (if p (hash-ref p "x" #f) #f)) ",") (to-string (if p (hash-ref p "y" #f) #f))))
))
)
(define (path_to_string path)
  (let/ec _return (begin
(define s "[")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not path) 0] [(string? path) (string-length path)] [(hash? path) (hash-count path)] [else (length path)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define pt (if path (if (hash? path) (hash-ref path i #f) (safe-index path (int i))) #f))
      (set! s (string-append (string-append (string-append (string-append (string-append s "(") (to-string (if pt (hash-ref pt "x" #f) #f))) ", ") (to-string (if pt (hash-ref pt "y" #f) #f))) ")"))
      (if (let ([__l i] [__r (- (cond [(not path) 0] [(string? path) (string-length path)] [(hash? path) (hash-count path)] [else (length path)]) 1)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! s (string-append s ", "))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! s (string-append s "]"))
(_return s)
))
)
(define (dijkstra grid source destination allow_diagonal)
  (let/ec _return (begin
(define rows (cond [(not grid) 0] [(string? grid) (string-length grid)] [(hash? grid) (hash-count grid)] [else (length grid)]))
(define cols (cond [(not (if grid (if (hash? grid) (hash-ref grid 0 #f) (safe-index grid (int 0))) #f)) 0] [(string? (if grid (if (hash? grid) (hash-ref grid 0 #f) (safe-index grid (int 0))) #f)) (string-length (if grid (if (hash? grid) (hash-ref grid 0 #f) (safe-index grid (int 0))) #f))] [(hash? (if grid (if (hash? grid) (hash-ref grid 0 #f) (safe-index grid (int 0))) #f)) (hash-count (if grid (if (hash? grid) (hash-ref grid 0 #f) (safe-index grid (int 0))) #f))] [else (length (if grid (if (hash? grid) (hash-ref grid 0 #f) (safe-index grid (int 0))) #f))]))
(define dx (list (- 1) 1 0 0))
(define dy (list 0 0 (- 1) 1))
(if allow_diagonal (let ()
(set! dx (append dx (list (- 1) (- 1) 1 1)))
(set! dy (append dy (list (- 1) 1 (- 1) 1)))
) (void))
(define INF 1000000000000.0)
(define queue (list source))
(define front 0)
(define dist_map (let ([h (make-hash)]) (hash-set! h (key source) 0.0) h))
(define prev (let ([h (make-hash)]) h))
(let/ec _break (let loop ()
  (if (let ([__l front] [__r (cond [(not queue) 0] [(string? queue) (string-length queue)] [(hash? queue) (hash-count queue)] [else (length queue)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define current (if queue (if (hash? queue) (hash-ref queue front #f) (safe-index queue (int front))) #f))
      (set! front (let ([__l front] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define cur_key (key current))
      (if (and (equal? (if current (hash-ref current "x" #f) #f) (if destination (hash-ref destination "x" #f) #f)) (equal? (if current (hash-ref current "y" #f) #f) (if destination (hash-ref destination "y" #f) #f))) (let ()
(_break)
) (void))
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not dx) 0] [(string? dx) (string-length dx)] [(hash? dx) (hash-count dx)] [else (length dx)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define nx (let ([__l (if current (hash-ref current "x" #f) #f)] [__r (if dx (if (hash? dx) (hash-ref dx i #f) (safe-index dx (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define ny (let ([__l (if current (hash-ref current "y" #f) #f)] [__r (if dy (if (hash? dy) (hash-ref dy i #f) (safe-index dy (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (if (and (and (and (let ([__l nx] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ([__l nx] [__r rows]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (let ([__l ny] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))]))) (let ([__l ny] [__r cols]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (let ()
(if (equal? (if (if grid (if (hash? grid) (hash-ref grid nx #f) (safe-index grid (int nx))) #f) (if (hash? (if grid (if (hash? grid) (hash-ref grid nx #f) (safe-index grid (int nx))) #f)) (hash-ref (if grid (if (hash? grid) (hash-ref grid nx #f) (safe-index grid (int nx))) #f) ny #f) (safe-index (if grid (if (hash? grid) (hash-ref grid nx #f) (safe-index grid (int nx))) #f) (int ny))) #f) 1) (let ()
(define n_key (string-append (string-append (to-string nx) ",") (to-string ny)))
(if (not (hash-has-key? dist_map n_key)) (let ()
(if (hash? dist_map) (hash-set! dist_map n_key (let ([__l (if dist_map (hash-ref dist_map cur_key 0.0) #f)] [__r 1.0]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! dist_map (make-hash)) (hash-set! dist_map n_key (let ([__l (if dist_map (hash-ref dist_map cur_key 0.0) #f)] [__r 1.0]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(if (hash? prev) (hash-set! prev n_key current) (begin (set! prev (make-hash)) (hash-set! prev n_key current)))
(set! queue (append (or queue (list)) (list (let ([h (make-hash)]) (hash-set! h "x" nx) (hash-set! h "y" ny) h))))
) (void))
) (void))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
    )
    (loop)) (void))))
(define dest_key (key destination))
(if (hash-has-key? dist_map dest_key) (let ()
(define path_rev (list destination))
(define step_key dest_key)
(define step_pt destination)
(let/ec _break (let loop ()
  (if (not (string=? step_key (key source))) (begin
    (let/ec _cont
      (set! step_pt (if prev (hash-ref prev step_key (hash)) #f))
      (set! step_key (key step_pt))
      (set! path_rev (append (or path_rev (list)) (list step_pt)))
    )
    (loop)) (void))))
(define path (list))
(define k (- (cond [(not path_rev) 0] [(string? path_rev) (string-length path_rev)] [(hash? path_rev) (hash-count path_rev)] [else (length path_rev)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l k] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! path (append (or path (list)) (list (if path_rev (if (hash? path_rev) (hash-ref path_rev k #f) (safe-index path_rev (int k))) #f))))
      (set! k (- k 1))
    )
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "distance" (if dist_map (hash-ref dist_map dest_key 0.0) #f)) (hash-set! h "path" path) h))
) (void))
(_return (let ([h (make-hash)]) (hash-set! h "distance" INF) (hash-set! h "path" (list)) h))
))
)
(define (print_result res)
  (let/ec _return (begin
(displayln (string-append (string-append (to-string (if res (hash-ref res "distance" #f) #f)) ", ") (path_to_string (if res (hash-ref res "path" #f) #f))))
))
)
(define grid1 (list (list 1 1 1) (list 0 1 0) (list 0 1 1)))
(define grid2 (list (list 1 1 1) (list 0 0 1) (list 0 1 1)))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(print_result (dijkstra grid1 (let ([h (make-hash)]) (hash-set! h "x" 0) (hash-set! h "y" 0) h) (let ([h (make-hash)]) (hash-set! h "x" 2) (hash-set! h "y" 2) h) #f))
(print_result (dijkstra grid1 (let ([h (make-hash)]) (hash-set! h "x" 0) (hash-set! h "y" 0) h) (let ([h (make-hash)]) (hash-set! h "x" 2) (hash-set! h "y" 2) h) #t))
(print_result (dijkstra grid2 (let ([h (make-hash)]) (hash-set! h "x" 0) (hash-set! h "y" 0) h) (let ([h (make-hash)]) (hash-set! h "x" 2) (hash-set! h "y" 2) h) #f))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
