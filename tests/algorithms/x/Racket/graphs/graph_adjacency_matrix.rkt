;; Generated by Mochi 0.10.66 on 2025-08-15 15:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (make_graph vertices edges directed)
  (let/ec _return (begin
(define g (let ([h (make-hash)]) (hash-set! h "directed" directed) (hash-set! h "vertex_to_index" (let ([h (make-hash)]) h)) (hash-set! h "adj_matrix" (list)) h))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not vertices) 0] [(string? vertices) (string-length vertices)] [(hash? vertices) (hash-count vertices)] [else (length vertices)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (add_vertex g (if vertices (if (hash? vertices) (hash-ref vertices i #f) (safe-index vertices (int i))) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define e (if edges (if (hash? edges) (hash-ref edges j #f) (safe-index edges (int j))) #f))
      (add_edge g (if e (if (hash? e) (hash-ref e 0 #f) (safe-index e (int 0))) #f) (if e (if (hash? e) (hash-ref e 1 #f) (safe-index e (int 1))) #f))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return g)
))
)
(define (contains_vertex g v)
  (let/ec _return (begin
(_return (hash-has-key? (if g (hash-ref g "vertex_to_index" #f) #f) v))
))
)
(define (add_vertex g v)
  (let/ec _return (begin
(if (contains_vertex g v) (let ()
(panic "vertex already exists")
) (void))
(define matrix (if g (hash-ref g "adj_matrix" #f) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! matrix (list-set matrix (int i) (append (or (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (list)) (list 0))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define row (list))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (let ([__l (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)])] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list 0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! matrix (append (or matrix (list)) (list row)))
(if (hash? g) (hash-set! g "adj_matrix" matrix) (begin (set! g (make-hash)) (hash-set! g "adj_matrix" matrix)))
(define idx_map (if g (hash-ref g "vertex_to_index" #f) #f))
(if (hash? idx_map) (hash-set! idx_map v (- (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]) 1)) (begin (set! idx_map (make-hash)) (hash-set! idx_map v (- (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]) 1))))
(if (hash? g) (hash-set! g "vertex_to_index" idx_map) (begin (set! g (make-hash)) (hash-set! g "vertex_to_index" idx_map)))
))
)
(define (remove_key m k)
  (let/ec _return (begin
(define out (let ([h (make-hash)]) h))
(let ([__seq m]) (let/ec _break (for ([key (in-hash-keys __seq)])
  (let/ec _cont
(if (not (equal? key k)) (let ()
(if (hash? out) (hash-set! out key (if m (hash-ref m key 0) #f)) (begin (set! out (make-hash)) (hash-set! out key (if m (hash-ref m key 0) #f))))
) (void))
  ))))
(_return out)
))
)
(define (decrement_indices m start)
  (let/ec _return (begin
(define out (let ([h (make-hash)]) h))
(let ([__seq m]) (let/ec _break (for ([key (in-hash-keys __seq)])
  (let/ec _cont
(define idx (if m (hash-ref m key 0) #f))
(if (let ([__l idx] [__r start]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(if (hash? out) (hash-set! out key (- idx 1)) (begin (set! out (make-hash)) (hash-set! out key (- idx 1))))
) (let ()
(if (hash? out) (hash-set! out key idx) (begin (set! out (make-hash)) (hash-set! out key idx)))
))
  ))))
(_return out)
))
)
(define (remove_vertex g v)
  (let/ec _return (begin
(if (not (contains_vertex g v)) (let ()
(panic "vertex does not exist")
) (void))
(define idx (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) v #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int v))) #f))
(define new_matrix (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not (if g (hash-ref g "adj_matrix" #f) #f)) 0] [(string? (if g (hash-ref g "adj_matrix" #f) #f)) (string-length (if g (hash-ref g "adj_matrix" #f) #f))] [(hash? (if g (hash-ref g "adj_matrix" #f) #f)) (hash-count (if g (hash-ref g "adj_matrix" #f) #f))] [else (length (if g (hash-ref g "adj_matrix" #f) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (equal? i idx)) (let ()
(define row (if (if g (hash-ref g "adj_matrix" #f) #f) (if (hash? (if g (hash-ref g "adj_matrix" #f) #f)) (hash-ref (if g (hash-ref g "adj_matrix" #f) #f) i #f) (safe-index (if g (hash-ref g "adj_matrix" #f) #f) (int i))) #f))
(define new_row (list))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not row) 0] [(string? row) (string-length row)] [(hash? row) (hash-count row)] [else (length row)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (equal? j idx)) (let ()
(set! new_row (append (or new_row (list)) (list (if row (if (hash? row) (hash-ref row j #f) (safe-index row (int j))) #f))))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! new_matrix (append (or new_matrix (list)) (list new_row)))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if (hash? g) (hash-set! g "adj_matrix" new_matrix) (begin (set! g (make-hash)) (hash-set! g "adj_matrix" new_matrix)))
(define m (remove_key (if g (hash-ref g "vertex_to_index" #f) #f) v))
(if (hash? g) (hash-set! g "vertex_to_index" (decrement_indices m idx)) (begin (set! g (make-hash)) (hash-set! g "vertex_to_index" (decrement_indices m idx))))
))
)
(define (add_edge g u v)
  (let/ec _return (begin
(if (not (and (contains_vertex g u) (contains_vertex g v))) (let ()
(panic "missing vertex")
) (void))
(define i (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) u #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int u))) #f))
(define j (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) v #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int v))) #f))
(define matrix (if g (hash-ref g "adj_matrix" #f) #f))
(set! matrix (list-set matrix i (list-set (list-ref matrix i) j 1)))
(if (not (if g (hash-ref g "directed" #f) #f)) (let ()
(set! matrix (list-set matrix j (list-set (list-ref matrix j) i 1)))
) (void))
(if (hash? g) (hash-set! g "adj_matrix" matrix) (begin (set! g (make-hash)) (hash-set! g "adj_matrix" matrix)))
))
)
(define (remove_edge g u v)
  (let/ec _return (begin
(if (not (and (contains_vertex g u) (contains_vertex g v))) (let ()
(panic "missing vertex")
) (void))
(define i (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) u #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int u))) #f))
(define j (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) v #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int v))) #f))
(define matrix (if g (hash-ref g "adj_matrix" #f) #f))
(set! matrix (list-set matrix i (list-set (list-ref matrix i) j 0)))
(if (not (if g (hash-ref g "directed" #f) #f)) (let ()
(set! matrix (list-set matrix j (list-set (list-ref matrix j) i 0)))
) (void))
(if (hash? g) (hash-set! g "adj_matrix" matrix) (begin (set! g (make-hash)) (hash-set! g "adj_matrix" matrix)))
))
)
(define (contains_edge g u v)
  (let/ec _return (begin
(if (not (and (contains_vertex g u) (contains_vertex g v))) (let ()
(panic "missing vertex")
) (void))
(define i (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) u #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int u))) #f))
(define j (if (if g (hash-ref g "vertex_to_index" #f) #f) (if (hash? (if g (hash-ref g "vertex_to_index" #f) #f)) (hash-ref (if g (hash-ref g "vertex_to_index" #f) #f) v #f) (safe-index (if g (hash-ref g "vertex_to_index" #f) #f) (int v))) #f))
(define matrix (if g (hash-ref g "adj_matrix" #f) #f))
(_return (equal? (if (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) j #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (int j))) #f) 1))
))
)
(define (clear_graph g)
  (let/ec _return (begin
(if (hash? g) (hash-set! g "vertex_to_index" (let ([h (make-hash)]) h)) (begin (set! g (make-hash)) (hash-set! g "vertex_to_index" (let ([h (make-hash)]) h))))
(if (hash? g) (hash-set! g "adj_matrix" (list)) (begin (set! g (make-hash)) (hash-set! g "adj_matrix" (list))))
))
)
(define g (make_graph (list 1 2 3) (list (list 1 2) (list 2 3)) #f))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (if g (hash-ref g "adj_matrix" #f) #f)))
(displayln (to-string (contains_edge g 1 2)))
(displayln (to-string (contains_edge g 2 1)))
(remove_edge g 1 2)
(displayln (to-string (contains_edge g 1 2)))
(remove_vertex g 2)
(displayln (to-string (if g (hash-ref g "adj_matrix" #f) #f)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
