;; Generated by Mochi 0.10.65 on 2025-08-14 17:09 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (make_int_list n value)
  (let/ec _return (begin
(define lst (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! lst (append (or lst (list)) (list value)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return lst)
))
)
(define (make_bool_list n)
  (let/ec _return (begin
(define lst (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! lst (append (or lst (list)) (list #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return lst)
))
)
(define (dijkstra graph src)
  (let/ec _return (begin
(define n (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)]))
(define dist (make_int_list n 1000000000))
(define visited (make_bool_list n))
(set! dist (list-set dist (int src) 0))
(define count 0)
(let/ec _break (let loop ()
  (if (let ([__l count] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define u (- 1))
      (define min_dist 1000000000)
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (and (not (if visited (if (hash? visited) (hash-ref visited i #f) (safe-index visited (int i))) #f)) (let ([__l (if dist (if (hash? dist) (hash-ref dist i #f) (safe-index dist (int i))) #f)] [__r min_dist]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (let ()
(set! min_dist (if dist (if (hash? dist) (hash-ref dist i #f) (safe-index dist (int i))) #f))
(set! u i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (if (let ([__l u] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_break)
) (void))
      (set! visited (list-set visited (int u) #t))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) 0] [(string? (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) (string-length (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f))] [(hash? (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) (hash-count (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f))] [else (length (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define e (if (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (if (hash? (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) (hash-ref (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) j #f) (safe-index (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (int j))) #f))
      (define v (if e (hash-ref e "node" #f) #f))
      (define w (if e (hash-ref e "weight" #f) #f))
      (if (not (if visited (if (hash? visited) (hash-ref visited v #f) (safe-index visited (int v))) #f)) (let ()
(define new_dist (let ([__l (if dist (if (hash? dist) (hash-ref dist u #f) (safe-index dist (int u))) #f)] [__r w]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (let ([__l new_dist] [__r (if dist (if (hash? dist) (hash-ref dist v #f) (safe-index dist (int v))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! dist (list-set dist (int v) new_dist))
) (void))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return dist)
))
)
(define graph (list (list (let ([h (make-hash)]) (hash-set! h "node" 1) (hash-set! h "weight" 10) h) (let ([h (make-hash)]) (hash-set! h "node" 3) (hash-set! h "weight" 5) h)) (list (let ([h (make-hash)]) (hash-set! h "node" 2) (hash-set! h "weight" 1) h) (let ([h (make-hash)]) (hash-set! h "node" 3) (hash-set! h "weight" 2) h)) (list (let ([h (make-hash)]) (hash-set! h "node" 4) (hash-set! h "weight" 4) h)) (list (let ([h (make-hash)]) (hash-set! h "node" 1) (hash-set! h "weight" 3) h) (let ([h (make-hash)]) (hash-set! h "node" 2) (hash-set! h "weight" 9) h) (let ([h (make-hash)]) (hash-set! h "node" 4) (hash-set! h "weight" 2) h)) (list (let ([h (make-hash)]) (hash-set! h "node" 0) (hash-set! h "weight" 7) h) (let ([h (make-hash)]) (hash-set! h "node" 2) (hash-set! h "weight" 6) h))))
(define dist (dijkstra graph 0))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (if dist (if (hash? dist) (hash-ref dist 0 #f) (safe-index dist (int 0))) #f)))
(displayln (to-string (if dist (if (hash? dist) (hash-ref dist 1 #f) (safe-index dist (int 1))) #f)))
(displayln (to-string (if dist (if (hash? dist) (hash-ref dist 2 #f) (safe-index dist (int 2))) #f)))
(displayln (to-string (if dist (if (hash? dist) (hash-ref dist 3 #f) (safe-index dist (int 3))) #f)))
(displayln (to-string (if dist (if (hash? dist) (hash-ref dist 4 #f) (safe-index dist (int 4))) #f)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
