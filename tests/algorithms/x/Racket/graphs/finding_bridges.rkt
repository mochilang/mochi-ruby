;; Generated by Mochi 0.10.65 on 2025-08-14 17:09 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (dfs graph at parent visited ids low id bridges)
  (let/ec _return (begin
(set! visited (list-set visited (int at) #t))
(set! ids (list-set ids (int at) id))
(set! low (list-set low (int at) id))
(define current_id (let ([__l id] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define res_bridges bridges)
(let/ec _break (for ([to (if graph (hash-ref graph at (list)) #f)])
  (let/ec _cont
(if (equal? to parent) (let ()
(_cont)
) (let ()
(if (not (if visited (if (hash? visited) (hash-ref visited to #f) (safe-index visited (int to))) #f)) (let ()
(define result (dfs graph to at visited ids low current_id res_bridges))
(set! current_id (if result (hash-ref result "id" #f) #f))
(set! res_bridges (if result (hash-ref result "bridges" #f) #f))
(if (let ([__l (if low (if (hash? low) (hash-ref low at #f) (safe-index low (int at))) #f)] [__r (if low (if (hash? low) (hash-ref low to #f) (safe-index low (int to))) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! low (list-set low (int at) (if low (if (hash? low) (hash-ref low to #f) (safe-index low (int to))) #f)))
) (void))
(if (let ([__l (if ids (if (hash? ids) (hash-ref ids at #f) (safe-index ids (int at))) #f)] [__r (if low (if (hash? low) (hash-ref low to #f) (safe-index low (int to))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(define edge (if (let ([__l at] [__r to]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (list at to) (list to at)))
(set! res_bridges (append (or res_bridges (list)) (list edge)))
) (void))
) (let ()
(if (let ([__l (if low (if (hash? low) (hash-ref low at #f) (safe-index low (int at))) #f)] [__r (if ids (if (hash? ids) (hash-ref ids to #f) (safe-index ids (int to))) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! low (list-set low (int at) (if ids (if (hash? ids) (hash-ref ids to #f) (safe-index ids (int to))) #f)))
) (void))
))
))
  )))
(_return (let ([h (make-hash)]) (hash-set! h "id" current_id) (hash-set! h "bridges" res_bridges) h))
))
)
(define (compute_bridges graph)
  (let/ec _return (begin
(define n (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)]))
(define visited (list))
(define ids (list))
(define low (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! visited (append (or visited (list)) (list #f)))
      (set! ids (append (or ids (list)) (list 0)))
      (set! low (append (or low (list)) (list 0)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define bridges (list))
(define id 0)
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (if visited (if (hash? visited) (hash-ref visited i #f) (safe-index visited (int i))) #f)) (let ()
(define result (dfs graph i (- 1) visited ids low id bridges))
(set! id (if result (hash-ref result "id" #f) #f))
(set! bridges (if result (hash-ref result "bridges" #f) #f))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return bridges)
))
)
(define (get_demo_graph index)
  (let/ec _return (begin
(if (equal? index 0) (let ()
(_return (let ([h (make-hash)]) (hash-set! h 0 (list 1 2)) (hash-set! h 1 (list 0 2)) (hash-set! h 2 (list 0 1 3 5)) (hash-set! h 3 (list 2 4)) (hash-set! h 4 (list 3)) (hash-set! h 5 (list 2 6 8)) (hash-set! h 6 (list 5 7)) (hash-set! h 7 (list 6 8)) (hash-set! h 8 (list 5 7)) h))
) (void))
(if (equal? index 1) (let ()
(_return (let ([h (make-hash)]) (hash-set! h 0 (list 6)) (hash-set! h 1 (list 9)) (hash-set! h 2 (list 4 5)) (hash-set! h 3 (list 4)) (hash-set! h 4 (list 2 3)) (hash-set! h 5 (list 2)) (hash-set! h 6 (list 0 7)) (hash-set! h 7 (list 6)) (hash-set! h 8 (list)) (hash-set! h 9 (list 1)) h))
) (void))
(if (equal? index 2) (let ()
(_return (let ([h (make-hash)]) (hash-set! h 0 (list 4)) (hash-set! h 1 (list 6)) (hash-set! h 2 (list)) (hash-set! h 3 (list 5 6 7)) (hash-set! h 4 (list 0 6)) (hash-set! h 5 (list 3 8 9)) (hash-set! h 6 (list 1 3 4 7)) (hash-set! h 7 (list 3 6 8 9)) (hash-set! h 8 (list 5 7)) (hash-set! h 9 (list 5 7)) h))
) (void))
(_return (let ([h (make-hash)]) (hash-set! h 0 (list 1 3)) (hash-set! h 1 (list 0 2 4)) (hash-set! h 2 (list 1 3 4)) (hash-set! h 3 (list 0 2 4)) (hash-set! h 4 (list 1 2 3)) h))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (compute_bridges (get_demo_graph 0)))
(displayln (compute_bridges (get_demo_graph 1)))
(displayln (compute_bridges (get_demo_graph 2)))
(displayln (compute_bridges (get_demo_graph 3)))
(displayln (compute_bridges (let ([h (make-hash)]) h)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
