;; Generated by Mochi 0.10.65 on 2025-08-14 17:09 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define tree (let ([h (make-hash)]) h))
(define (dfs start visited)
  (let/ec _return (begin
(define size 1)
(define cuts 0)
(if (hash? visited) (hash-set! visited start #t) (begin (set! visited (make-hash)) (hash-set! visited start #t)))
(let/ec _break (for ([v (if tree (hash-ref tree start (list)) #f)])
  (let/ec _cont
(if (not (hash-has-key? visited v)) (let ()
(define res (dfs v visited))
(set! size (let ([__l size] [__r (if res (if (hash? res) (hash-ref res 0 #f) (safe-index res (int 0))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! cuts (let ([__l cuts] [__r (if res (if (hash? res) (hash-ref res 1 #f) (safe-index res (int 1))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
  )))
(if (equal? (modulo size 2) 0) (let ()
(set! cuts (let ([__l cuts] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(_return (list size cuts))
))
)
(define (even_tree)
  (let/ec _return (begin
(define visited (let ([h (make-hash)]) h))
(define res (dfs 1 visited))
(_return (- (if res (if (hash? res) (hash-ref res 1 #f) (safe-index res (int 1))) #f) 1))
))
)
(define (main)
  (let/ec _return (begin
(define edges (list (list 2 1) (list 3 1) (list 4 3) (list 5 2) (list 6 1) (list 7 2) (list 8 6) (list 9 8) (list 10 8)))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not edges) 0] [(string? edges) (string-length edges)] [(hash? edges) (hash-count edges)] [else (length edges)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define u (if (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f) 0 #f) (safe-index (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f) (int 0))) #f))
      (define v (if (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f) (if (hash? (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f)) (hash-ref (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f) 1 #f) (safe-index (if edges (if (hash? edges) (hash-ref edges i #f) (safe-index edges (int i))) #f) (int 1))) #f))
      (if (not (hash-has-key? tree u)) (let ()
(if (hash? tree) (hash-set! tree u (list)) (begin (set! tree (make-hash)) (hash-set! tree u (list))))
) (void))
      (if (not (hash-has-key? tree v)) (let ()
(if (hash? tree) (hash-set! tree v (list)) (begin (set! tree (make-hash)) (hash-set! tree v (list))))
) (void))
      (if (hash? tree) (hash-set! tree u (append (or (if tree (hash-ref tree u (list)) #f) (list)) (list v))) (begin (set! tree (make-hash)) (hash-set! tree u (append (or (if tree (hash-ref tree u (list)) #f) (list)) (list v)))))
      (if (hash? tree) (hash-set! tree v (append (or (if tree (hash-ref tree v (list)) #f) (list)) (list u))) (begin (set! tree (make-hash)) (hash-set! tree v (append (or (if tree (hash-ref tree v (list)) #f) (list)) (list u)))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(displayln (to-string (even_tree)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
