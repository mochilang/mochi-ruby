;; Generated by Mochi 0.10.66 on 2025-08-15 15:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (dfs u graph visit stack)
  (let/ec _return (begin
(if (if visit (if (hash? visit) (hash-ref visit u #f) (safe-index visit (int u))) #f) (let ()
(_return stack)
) (void))
(set! visit (list-set visit (int u) #t))
(let ([__seq (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)]) (let/ec _break (for ([v (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! stack (dfs v graph visit stack))
  ))))
(set! stack (append (or stack (list)) (list u)))
(_return stack)
))
)
(define (dfs2 u reversed_graph visit component)
  (let/ec _return (begin
(if (if visit (if (hash? visit) (hash-ref visit u #f) (safe-index visit (int u))) #f) (let ()
(_return component)
) (void))
(set! visit (list-set visit (int u) #t))
(set! component (append (or component (list)) (list u)))
(let ([__seq (if reversed_graph (if (hash? reversed_graph) (hash-ref reversed_graph u #f) (safe-index reversed_graph (int u))) #f)]) (let/ec _break (for ([v (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! component (dfs2 v reversed_graph visit component))
  ))))
(_return component)
))
)
(define (kosaraju graph)
  (let/ec _return (begin
(define n (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)]))
(define reversed_graph (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! reversed_graph (append (or reversed_graph (list)) (list (list))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (let ([__seq (if graph (if (hash? graph) (hash-ref graph i #f) (safe-index graph (int i))) #f)]) (let/ec _break (for ([v (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! reversed_graph (list-set reversed_graph (int v) (append (or (if reversed_graph (if (hash? reversed_graph) (hash-ref reversed_graph v #f) (safe-index reversed_graph (int v))) #f) (list)) (list i))))
  ))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define visit (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! visit (append (or visit (list)) (list #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define stack (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (if visit (if (hash? visit) (hash-ref visit i #f) (safe-index visit (int i))) #f)) (let ()
(set! stack (dfs i graph visit stack))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! visit (list-set visit (int i) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define scc (list))
(define idx (- (cond [(not stack) 0] [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l idx] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define node (if stack (if (hash? stack) (hash-ref stack idx #f) (safe-index stack (int idx))) #f))
      (if (not (if visit (if (hash? visit) (hash-ref visit node #f) (safe-index visit (int node))) #f)) (let ()
(define component (list))
(set! component (dfs2 node reversed_graph visit component))
(set! scc (append (or scc (list)) (list component)))
) (void))
      (set! idx (- idx 1))
    )
    (loop)) (void))))
(_return scc)
))
)
(define (main)
  (let/ec _return (begin
(define graph (list (list 1) (list 2) (list 0 3) (list 4) (list)))
(define comps (kosaraju graph))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not comps) 0] [(string? comps) (string-length comps)] [(hash? comps) (hash-count comps)] [else (length comps)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (displayln (if comps (if (hash? comps) (hash-ref comps i #f) (safe-index comps (int i))) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
