;; Generated by Mochi 0.10.65 on 2025-08-14 17:09 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (minimum_distance distances visited)
  (let/ec _return (begin
(define minimum 10000000)
(define min_index 0)
(define vertex 0)
(let/ec _break (let loop ()
  (if (let ([__l vertex] [__r (cond [(not distances) 0] [(string? distances) (string-length distances)] [(hash? distances) (hash-count distances)] [else (length distances)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (and (let ([__l (if distances (if (hash? distances) (hash-ref distances vertex #f) (safe-index distances (int vertex))) #f)] [__r minimum]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (not (if visited (if (hash? visited) (hash-ref visited vertex #f) (safe-index visited (int vertex))) #f))) (let ()
(set! minimum (if distances (if (hash? distances) (hash-ref distances vertex #f) (safe-index distances (int vertex))) #f))
(set! min_index vertex)
) (void))
      (set! vertex (let ([__l vertex] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return min_index)
))
)
(define (dijkstra graph source)
  (let/ec _return (begin
(define vertices (cond [(not graph) 0] [(string? graph) (string-length graph)] [(hash? graph) (hash-count graph)] [else (length graph)]))
(define distances (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r vertices]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! distances (append (or distances (list)) (list 10000000)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! distances (list-set distances (int source) 0))
(define visited (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r vertices]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! visited (append (or visited (list)) (list #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define count 0)
(let/ec _break (let loop ()
  (if (let ([__l count] [__r vertices]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define u (minimum_distance distances visited))
      (set! visited (list-set visited (int u) #t))
      (define v 0)
      (let/ec _break (let loop ()
  (if (let ([__l v] [__r vertices]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (and (and (let ([__l (if (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (if (hash? (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) (hash-ref (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) v #f) (safe-index (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (int v))) #f)] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (not (if visited (if (hash? visited) (hash-ref visited v #f) (safe-index visited (int v))) #f))) (let ([__l (if distances (if (hash? distances) (hash-ref distances v #f) (safe-index distances (int v))) #f)] [__r (let ([__l (if distances (if (hash? distances) (hash-ref distances u #f) (safe-index distances (int u))) #f)] [__r (if (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (if (hash? (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) (hash-ref (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) v #f) (safe-index (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (int v))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (let ()
(set! distances (list-set distances (int v) (let ([__l (if distances (if (hash? distances) (hash-ref distances u #f) (safe-index distances (int u))) #f)] [__r (if (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (if (hash? (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f)) (hash-ref (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) v #f) (safe-index (if graph (if (hash? graph) (hash-ref graph u #f) (safe-index graph (int u))) #f) (int v))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (void))
      (set! v (let ([__l v] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return distances)
))
)
(define (print_solution distances)
  (let/ec _return (begin
(displayln "Vertex \t Distance from Source")
(define v 0)
(let/ec _break (let loop ()
  (if (let ([__l v] [__r (cond [(not distances) 0] [(string? distances) (string-length distances)] [(hash? distances) (hash-count distances)] [else (length distances)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (displayln (string-append (string-append (to-string v) "\t\t") (to-string (if distances (if (hash? distances) (hash-ref distances v #f) (safe-index distances (int v))) #f))))
      (set! v (let ([__l v] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
))
)
(define graph (list (list 0 4 0 0 0 0 0 8 0) (list 4 0 8 0 0 0 0 11 0) (list 0 8 0 7 0 4 0 0 2) (list 0 0 7 0 9 14 0 0 0) (list 0 0 0 9 0 10 0 0 0) (list 0 0 4 14 10 0 2 0 0) (list 0 0 0 0 0 2 0 1 6) (list 8 11 0 0 0 0 1 0 7) (list 0 0 2 0 0 0 6 7 0)))
(define distances (dijkstra graph 0))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(print_solution distances)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
