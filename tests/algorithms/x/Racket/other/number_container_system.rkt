;; Generated by Mochi 0.10.73 on 2025-08-26 08:36 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/other")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (remove_at xs idx)
  (let/ec _return (begin
(define res (vector))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(vector? xs) (vector-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (equal? i idx)) (let ()
(set! res (appendv (or res (vector)) (vector (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (insert_at xs idx val)
  (let/ec _return (begin
(define res (vector))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(vector? xs) (vector-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (equal? i idx) (let ()
(set! res (appendv (or res (vector)) (vector val)))
) (void))
      (set! res (appendv (or res (vector)) (vector (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if (equal? idx (cond [(not xs) 0] [(string? xs) (string-length xs)] [(vector? xs) (vector-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])) (let ()
(set! res (appendv (or res (vector)) (vector val)))
) (void))
(_return res)
))
)
(define (binary_search_delete array item)
  (let/ec _return (begin
(define low 0)
(define high (- (cond [(not array) 0] [(string? array) (string-length array)] [(vector? array) (vector-length array)] [(hash? array) (hash-count array)] [else (length array)]) 1))
(define arr array)
(let/ec _break (let loop ()
  (if (let ([__l low] [__r high]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define mid (floor (/ (let ([__l low] [__r high]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2)))
      (if (equal? (if arr (if (hash? arr) (hash-ref arr mid #f) (safe-index arr (int mid))) #f) item) (let ()
(set! arr (remove_at arr mid))
(_return arr)
) (let ()
(if (let ([__l (if arr (if (hash? arr) (hash-ref arr mid #f) (safe-index arr (int mid))) #f)] [__r item]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! low (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! high (- mid 1))
))
))
    )
    (loop)) (void))))
(displayln (to-string "ValueError: Either the item is not in the array or the array was unsorted"))
(_return arr)
))
)
(define (binary_search_insert array index)
  (let/ec _return (begin
(define low 0)
(define high (- (cond [(not array) 0] [(string? array) (string-length array)] [(vector? array) (vector-length array)] [(hash? array) (hash-count array)] [else (length array)]) 1))
(define arr array)
(let/ec _break (let loop ()
  (if (let ([__l low] [__r high]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define mid (floor (/ (let ([__l low] [__r high]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2)))
      (if (equal? (if arr (if (hash? arr) (hash-ref arr mid #f) (safe-index arr (int mid))) #f) index) (let ()
(set! arr (insert_at arr (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) index))
(_return arr)
) (let ()
(if (let ([__l (if arr (if (hash? arr) (hash-ref arr mid #f) (safe-index arr (int mid))) #f)] [__r index]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! low (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! high (- mid 1))
))
))
    )
    (loop)) (void))))
(set! arr (insert_at arr low index))
(_return arr)
))
)
(define (change cont idx num)
  (let/ec _return (begin
(define numbermap (if cont (hash-ref cont "numbermap" #f) #f))
(define indexmap (if cont (hash-ref cont "indexmap" #f) #f))
(if (hash-has-key? indexmap idx) (let ()
(define old (if indexmap (hash-ref indexmap idx 0) #f))
(define indexes (if numbermap (hash-ref numbermap old (vector)) #f))
(if (equal? (cond [(not indexes) 0] [(string? indexes) (string-length indexes)] [(vector? indexes) (vector-length indexes)] [(hash? indexes) (hash-count indexes)] [else (length indexes)]) 1) (let ()
(if (hash? numbermap) (hash-set! numbermap old (vector)) (begin (set! numbermap (make-hash)) (hash-set! numbermap old (vector))))
) (let ()
(if (hash? numbermap) (hash-set! numbermap old (binary_search_delete indexes idx)) (begin (set! numbermap (make-hash)) (hash-set! numbermap old (binary_search_delete indexes idx))))
))
) (void))
(if (hash? indexmap) (hash-set! indexmap idx num) (begin (set! indexmap (make-hash)) (hash-set! indexmap idx num)))
(if (hash-has-key? numbermap num) (let ()
(if (hash? numbermap) (hash-set! numbermap num (binary_search_insert (if numbermap (hash-ref numbermap num (vector)) #f) idx)) (begin (set! numbermap (make-hash)) (hash-set! numbermap num (binary_search_insert (if numbermap (hash-ref numbermap num (vector)) #f) idx))))
) (let ()
(if (hash? numbermap) (hash-set! numbermap num (vector idx)) (begin (set! numbermap (make-hash)) (hash-set! numbermap num (vector idx))))
))
(_return (let ([h (make-hash)]) (hash-set! h "numbermap" numbermap) (hash-set! h "indexmap" indexmap) h))
))
)
(define (find cont num)
  (let/ec _return (begin
(define numbermap (if cont (hash-ref cont "numbermap" #f) #f))
(if (hash-has-key? numbermap num) (let ()
(define arr (if numbermap (hash-ref numbermap num (vector)) #f))
(if (let ([__l (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(_return (if arr (if (hash? arr) (hash-ref arr 0 #f) (safe-index arr (int 0))) #f))
) (void))
) (void))
(_return (- 1))
))
)
(define nm (let ([h (make-hash)]) h))
(define im (let ([h (make-hash)]) h))
(define cont (let ([h (make-hash)]) (hash-set! h "numbermap" nm) (hash-set! h "indexmap" im) h))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (find cont 10)))
(set! cont (change cont 0 10))
(displayln (to-string (find cont 10)))
(set! cont (change cont 0 20))
(displayln (to-string (find cont 10)))
(displayln (to-string (find cont 20)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
