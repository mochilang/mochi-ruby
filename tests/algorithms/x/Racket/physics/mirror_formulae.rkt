;; Generated by Mochi 0.10.69 on 2025-08-19 09:22 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (abs_float x)
  (let/ec _return (begin
(if (let ([__l x] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return (- x))
) (void))
(_return x)
))
)
(define (isclose a b tolerance)
  (let/ec _return (begin
(_return (let ([__l (abs_float (- a b))] [__r tolerance]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])))
))
)
(define (focal_length distance_of_object distance_of_image)
  (let/ec _return (begin
(if (or (equal? distance_of_object 0.0) (equal? distance_of_image 0.0)) (let ()
(panic "Invalid inputs. Enter non zero values with respect to the sign convention.")
) (void))
(_return (/ 1.0 (let ([__l (/ 1.0 distance_of_object)] [__r (/ 1.0 distance_of_image)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
)
(define (object_distance focal_length distance_of_image)
  (let/ec _return (begin
(if (or (equal? distance_of_image 0.0) (equal? focal_length 0.0)) (let ()
(panic "Invalid inputs. Enter non zero values with respect to the sign convention.")
) (void))
(_return (/ 1.0 (- (/ 1.0 focal_length) (/ 1.0 distance_of_image))))
))
)
(define (image_distance focal_length distance_of_object)
  (let/ec _return (begin
(if (or (equal? distance_of_object 0.0) (equal? focal_length 0.0)) (let ()
(panic "Invalid inputs. Enter non zero values with respect to the sign convention.")
) (void))
(_return (/ 1.0 (- (/ 1.0 focal_length) (/ 1.0 distance_of_object))))
))
)
(define (test_focal_length)
  (let/ec _return (begin
(define f1 (focal_length 10.0 20.0))
(if (not (isclose f1 6.66666666666666 1e-08)) (let ()
(panic "focal_length test1 failed")
) (void))
(define f2 (focal_length 9.5 6.7))
(if (not (isclose f2 3.929012346 1e-08)) (let ()
(panic "focal_length test2 failed")
) (void))
))
)
(define (test_object_distance)
  (let/ec _return (begin
(define u1 (object_distance 30.0 20.0))
(if (not (isclose u1 (- 60.0) 1e-08)) (let ()
(panic "object_distance test1 failed")
) (void))
(define u2 (object_distance 10.5 11.7))
(if (not (isclose u2 102.375 1e-08)) (let ()
(panic "object_distance test2 failed")
) (void))
))
)
(define (test_image_distance)
  (let/ec _return (begin
(define v1 (image_distance 10.0 40.0))
(if (not (isclose v1 13.33333333 1e-08)) (let ()
(panic "image_distance test1 failed")
) (void))
(define v2 (image_distance 1.5 6.7))
(if (not (isclose v2 1.932692308 1e-08)) (let ()
(panic "image_distance test2 failed")
) (void))
))
)
(define (main)
  (let/ec _return (begin
(test_focal_length)
(test_object_distance)
(test_image_distance)
(displayln (to-string (focal_length 10.0 20.0)))
(displayln (to-string (object_distance 30.0 20.0)))
(displayln (to-string (image_distance 10.0 40.0)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
