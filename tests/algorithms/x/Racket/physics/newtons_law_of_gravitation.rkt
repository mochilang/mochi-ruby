;; Generated by Mochi 0.10.69 on 2025-08-19 09:22 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define GRAVITATIONAL_CONSTANT 6.6743e-11)
(define (sqrtApprox x)
  (let/ec _return (begin
(define guess (/ x 2.0))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 20]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! guess (/ (let ([__l guess] [__r (/ x guess)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2.0))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return guess)
))
)
(define (gravitational_law force mass_1 mass_2 distance)
  (let/ec _return (begin
(define zero_count 0)
(if (equal? force 0.0) (let ()
(set! zero_count (let ([__l zero_count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(if (equal? mass_1 0.0) (let ()
(set! zero_count (let ([__l zero_count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(if (equal? mass_2 0.0) (let ()
(set! zero_count (let ([__l zero_count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(if (equal? distance 0.0) (let ()
(set! zero_count (let ([__l zero_count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
(if (not (equal? zero_count 1)) (let ()
(panic "One and only one argument must be 0")
) (void))
(if (let ([__l force] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(panic "Gravitational force can not be negative")
) (void))
(if (let ([__l distance] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(panic "Distance can not be negative")
) (void))
(if (let ([__l mass_1] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(panic "Mass can not be negative")
) (void))
(if (let ([__l mass_2] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(panic "Mass can not be negative")
) (void))
(define product_of_mass (* mass_1 mass_2))
(if (equal? force 0.0) (let ()
(define f (/ (* GRAVITATIONAL_CONSTANT product_of_mass) (* distance distance)))
(_return (let ([h (make-hash)]) (hash-set! h "kind" "force") (hash-set! h "value" f) h))
) (void))
(if (equal? mass_1 0.0) (let ()
(define m1 (/ (* force (* distance distance)) (* GRAVITATIONAL_CONSTANT mass_2)))
(_return (let ([h (make-hash)]) (hash-set! h "kind" "mass_1") (hash-set! h "value" m1) h))
) (void))
(if (equal? mass_2 0.0) (let ()
(define m2 (/ (* force (* distance distance)) (* GRAVITATIONAL_CONSTANT mass_1)))
(_return (let ([h (make-hash)]) (hash-set! h "kind" "mass_2") (hash-set! h "value" m2) h))
) (void))
(define d (sqrtApprox (/ (* GRAVITATIONAL_CONSTANT product_of_mass) force)))
(_return (let ([h (make-hash)]) (hash-set! h "kind" "distance") (hash-set! h "value" d) h))
))
)
(define r1 (gravitational_law 0.0 5.0 10.0 20.0))
(define r2 (gravitational_law 7367.382 0.0 74.0 3048.0))
(define r3 (gravitational_law 100.0 5.0 0.0 3.0))
(define r4 (gravitational_law 100.0 5.0 10.0 0.0))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (string-append (string-append (if r1 (hash-ref r1 "kind" #f) #f) " ") (to-string (if r1 (hash-ref r1 "value" #f) #f))))
(displayln (string-append (string-append (if r2 (hash-ref r2 "kind" #f) #f) " ") (to-string (if r2 (hash-ref r2 "value" #f) #f))))
(displayln (string-append (string-append (if r3 (hash-ref r3 "kind" #f) #f) " ") (to-string (if r3 (hash-ref r3 "value" #f) #f))))
(displayln (string-append (string-append (if r4 (hash-ref r4 "kind" #f) #f) " ") (to-string (if r4 (hash-ref r4 "value" #f) #f))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
