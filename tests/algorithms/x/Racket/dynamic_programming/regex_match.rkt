;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (if (inexact? x) (real->decimal-string x) (format "~a" x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (recursive_match text pattern)
  (let/ec _return (begin
(if (equal? (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 0) (let ()
(_return (equal? (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]) 0))
) (void))
(if (equal? (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]) 0) (let ()
(if (and (let ([__l (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])] [__r 2]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (string=? (slice pattern (- (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 1) (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])) "*")) (let ()
(_return (recursive_match text (slice pattern 0 (- (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 2))))
) (void))
(_return #f)
) (void))
(define last_text (slice text (- (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]) 1) (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)])))
(define last_pattern (slice pattern (- (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 1) (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])))
(if (or (string=? last_text last_pattern) (string=? last_pattern ".")) (let ()
(_return (recursive_match (slice text 0 (- (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]) 1)) (slice pattern 0 (- (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 1))))
) (void))
(if (string=? last_pattern "*") (let ()
(if (recursive_match (slice text 0 (- (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]) 1)) pattern) (let ()
(_return #t)
) (void))
(_return (recursive_match text (slice pattern 0 (- (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 2))))
) (void))
(_return #f)
))
)
(define (dp_match text pattern)
  (let/ec _return (begin
(define m (cond [(not text) 0] [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)]))
(define n (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]))
(define dp (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r m]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list #f)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! dp (append (or dp (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! dp (list-set dp 0 (list-set (list-ref dp 0) 0 #t)))
(define j 1)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (and (string=? (slice pattern (- j 1) j) "*") (let ([__l j] [__r 2]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))]))) (let ()
(if (if (if dp (if (hash? dp) (hash-ref dp 0 #f) (safe-index dp (int 0))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp 0 #f) (safe-index dp (int 0))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp 0 #f) (safe-index dp (int 0))) #f) (- j 2) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp 0 #f) (safe-index dp (int 0))) #f) (int (- j 2)))) #f) (let ()
(set! dp (list-set dp 0 (list-set (list-ref dp 0) j #t)))
) (void))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r m]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! j 1)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define p_char (slice pattern (- j 1) j))
      (define t_char (slice text (- i 1) i))
      (if (or (string=? p_char ".") (string=? p_char t_char)) (let ()
(if (if (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (- j 1) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (int (- j 1)))) #f) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j #t)))
) (void))
) (let ()
(if (string=? p_char "*") (let ()
(if (let ([__l j] [__r 2]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(if (if (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (- j 2) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (int (- j 2)))) #f) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j #t)))
) (void))
(define prev_p (slice pattern (- j 2) (- j 1)))
(if (or (string=? prev_p ".") (string=? prev_p t_char)) (let ()
(if (if (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) j #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (int j))) #f) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j #t)))
) (void))
) (void))
) (void))
) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j #f)))
))
))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (if (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f) n #f) (safe-index (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f) (int n))) #f))
))
)
(define (print_bool b)
  (let/ec _return (begin
(if b (let ()
(displayln #t)
) (let ()
(displayln #f)
))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(print_bool (recursive_match "abc" "a.c"))
(print_bool (recursive_match "abc" "af*.c"))
(print_bool (recursive_match "abc" "a.c*"))
(print_bool (recursive_match "abc" "a.c*d"))
(print_bool (recursive_match "aa" ".*"))
(print_bool (dp_match "abc" "a.c"))
(print_bool (dp_match "abc" "af*.c"))
(print_bool (dp_match "abc" "a.c*"))
(print_bool (dp_match "abc" "a.c*d"))
(print_bool (dp_match "aa" ".*"))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
