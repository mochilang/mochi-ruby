;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (if (inexact? x) (real->decimal-string x) (format "~a" x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (min_int a b)
  (let/ec _return (begin
(if (let ([__l a] [__r b]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return a)
) (void))
(_return b)
))
)
(define (minimum_cost_path matrix)
  (let/ec _return (begin
(define rows (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]))
(define cols (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))]))
(define j 1)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r cols]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row0 (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))
      (set! row0 (list-set row0 (int j) (let ([__l (if row0 (if (hash? row0) (hash-ref row0 j #f) (safe-index row0 (int j))) #f)] [__r (if row0 (if (hash? row0) (hash-ref row0 (- j 1) #f) (safe-index row0 (int (- j 1)))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! matrix (list-set matrix (int 0) row0))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r rows]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))
      (set! row (list-set row (int 0) (let ([__l (if row (if (hash? row) (hash-ref row 0 #f) (safe-index row (int 0))) #f)] [__r (if (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f) 0 #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f) (int 0))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! matrix (list-set matrix (int i) row))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r rows]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))
      (set! j 1)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r cols]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define up (if (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f) j #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix (- i 1) #f) (safe-index matrix (int (- i 1)))) #f) (int j))) #f))
      (define left (if row (if (hash? row) (hash-ref row (- j 1) #f) (safe-index row (int (- j 1)))) #f))
      (define best (min_int up left))
      (set! row (list-set row (int j) (let ([__l (if row (if (hash? row) (hash-ref row j #f) (safe-index row (int j))) #f)] [__r best]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! matrix (list-set matrix (int i) row))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (if (if matrix (if (hash? matrix) (hash-ref matrix (- rows 1) #f) (safe-index matrix (int (- rows 1)))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix (- rows 1) #f) (safe-index matrix (int (- rows 1)))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix (- rows 1) #f) (safe-index matrix (int (- rows 1)))) #f) (- cols 1) #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix (- rows 1) #f) (safe-index matrix (int (- rows 1)))) #f) (int (- cols 1)))) #f))
))
)
(define m1 (list (list 2 1) (list 3 1) (list 4 2)))
(define m2 (list (list 2 1 4) (list 2 1 3) (list 3 2 1)))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (minimum_cost_path m1)))
(displayln (to-string (minimum_cost_path m2)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
