;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (make_2d n)
  (let/ec _return (begin
(define res (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list 0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! res (append (or res (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (matrix_chain_order arr)
  (let/ec _return (begin
(define n (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]))
(define m (make_2d n))
(define s (make_2d n))
(define chain_length 2)
(let/ec _break (let loop ()
  (if (let ([__l chain_length] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define a 1)
      (let/ec _break (let loop ()
  (if (let ([__l a] [__r (let ([__l (- n chain_length)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define b (- (let ([__l a] [__r chain_length]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1))
      (set! m (list-set m a (list-set (list-ref m a) b 1000000000)))
      (define c a)
      (let/ec _break (let loop ()
  (if (let ([__l c] [__r b]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define cost (let ([__l (let ([__l (if (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f) (if (hash? (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f)) (hash-ref (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f) c #f) (safe-index (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f) (int c))) #f)] [__r (if (if m (if (hash? m) (hash-ref m (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (safe-index m (int (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f) (if (hash? (if m (if (hash? m) (hash-ref m (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (safe-index m (int (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f)) (hash-ref (if m (if (hash? m) (hash-ref m (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (safe-index m (int (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f) b #f) (safe-index (if m (if (hash? m) (hash-ref m (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (safe-index m (int (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f) (int b))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (* (* (if arr (if (hash? arr) (hash-ref arr (- a 1) #f) (safe-index arr (int (- a 1)))) #f) (if arr (if (hash? arr) (hash-ref arr c #f) (safe-index arr (int c))) #f)) (if arr (if (hash? arr) (hash-ref arr b #f) (safe-index arr (int b))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (if (let ([__l cost] [__r (if (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f) (if (hash? (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f)) (hash-ref (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f) b #f) (safe-index (if m (if (hash? m) (hash-ref m a #f) (safe-index m (int a))) #f) (int b))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! m (list-set m a (list-set (list-ref m a) b cost)))
(set! s (list-set s a (list-set (list-ref s a) b c)))
) (void))
      (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! a (let ([__l a] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! chain_length (let ([__l chain_length] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "matrix" m) (hash-set! h "solution" s) h))
))
)
(define (optimal_parenthesization s i j)
  (let/ec _return (begin
(if (equal? i j) (let ()
(_return (string-append "A" (to-string i)))
) (let ()
(define left (optimal_parenthesization s i (if (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f) (if (hash? (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f)) (hash-ref (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f) j #f) (safe-index (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f) (int j))) #f)))
(define right (optimal_parenthesization s (let ([__l (if (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f) (if (hash? (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f)) (hash-ref (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f) j #f) (safe-index (if s (if (hash? s) (hash-ref s i #f) (safe-index s (int i))) #f) (int j))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) j))
(_return (string-append (string-append (string-append (string-append "( " left) " ") right) " )"))
))
))
)
(define (main)
  (let/ec _return (begin
(define arr (list 30 35 15 5 10 20 25))
(define n (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]))
(define res (matrix_chain_order arr))
(define m (if res (hash-ref res "matrix" #f) #f))
(define s (if res (hash-ref res "solution" #f) #f))
(displayln (string-append "No. of Operation required: " (to-string (if (if m (if (hash? m) (hash-ref m 1 #f) (safe-index m (int 1))) #f) (if (hash? (if m (if (hash? m) (hash-ref m 1 #f) (safe-index m (int 1))) #f)) (hash-ref (if m (if (hash? m) (hash-ref m 1 #f) (safe-index m (int 1))) #f) (- n 1) #f) (safe-index (if m (if (hash? m) (hash-ref m 1 #f) (safe-index m (int 1))) #f) (int (- n 1)))) #f))))
(define seq (optimal_parenthesization s 1 (- n 1)))
(displayln seq)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
