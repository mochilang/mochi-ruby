;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (longest_common_substring text1 text2)
  (let/ec _return (begin
(if (or (equal? (cond [(not text1) 0] [(string? text1) (string-length text1)] [(hash? text1) (hash-count text1)] [else (length text1)]) 0) (equal? (cond [(not text2) 0] [(string? text2) (string-length text2)] [(hash? text2) (hash-count text2)] [else (length text2)]) 0)) (let ()
(_return "")
) (void))
(define m (cond [(not text1) 0] [(string? text1) (string-length text1)] [(hash? text1) (hash-count text1)] [else (length text1)]))
(define n (cond [(not text2) 0] [(string? text2) (string-length text2)] [(hash? text2) (hash-count text2)] [else (length text2)]))
(define dp (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list 0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! dp (append (or dp (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define end_pos 0)
(define max_len 0)
(define ii 1)
(let/ec _break (let loop ()
  (if (let ([__l ii] [__r m]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define jj 1)
      (let/ec _break (let loop ()
  (if (let ([__l jj] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (string=? (slice text1 (- ii 1) ii) (slice text2 (- jj 1) jj)) (let ()
(set! dp (list-set dp ii (list-set (list-ref dp ii) jj (let ([__l 1] [__r (if (if dp (if (hash? dp) (hash-ref dp (- ii 1) #f) (safe-index dp (int (- ii 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- ii 1) #f) (safe-index dp (int (- ii 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- ii 1) #f) (safe-index dp (int (- ii 1)))) #f) (- jj 1) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- ii 1) #f) (safe-index dp (int (- ii 1)))) #f) (int (- jj 1)))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(if (let ([__l (if (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f) jj #f) (safe-index (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f) (int jj))) #f)] [__r max_len]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! max_len (if (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f) jj #f) (safe-index (if dp (if (hash? dp) (hash-ref dp ii #f) (safe-index dp (int ii))) #f) (int jj))) #f))
(set! end_pos ii)
) (void))
) (void))
      (set! jj (let ([__l jj] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! ii (let ([__l ii] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (slice text1 (- end_pos max_len) end_pos))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (longest_common_substring "abcdef" "xabded"))
(displayln "\n")
(displayln (longest_common_substring "zxabcdezy" "yzabcdezx"))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
