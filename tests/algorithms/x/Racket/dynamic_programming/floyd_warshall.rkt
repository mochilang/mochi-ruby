;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define INF 1000000000)
(define (new_graph n)
  (let/ec _return (begin
(define dp (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (equal? i j) (let ()
(set! row (append (or row (list)) (list 0)))
) (let ()
(set! row (append (or row (list)) (list INF)))
))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! dp (append (or dp (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "n" n) (hash-set! h "dp" dp) h))
))
)
(define (add_edge g u v w)
  (let/ec _return (begin
(define dp (if g (hash-ref g "dp" #f) #f))
(define row (if dp (if (hash? dp) (hash-ref dp u #f) (safe-index dp (int u))) #f))
(set! row (list-set row (int v) w))
(set! dp (list-set dp (int u) row))
(if (hash? g) (hash-set! g "dp" dp) (begin (set! g (make-hash)) (hash-set! g "dp" dp)))
))
)
(define (floyd_warshall g)
  (let/ec _return (begin
(define dp (if g (hash-ref g "dp" #f) #f))
(define k 0)
(let/ec _break (let loop ()
  (if (let ([__l k] [__r (if g (hash-ref g "n" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r (if g (hash-ref g "n" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r (if g (hash-ref g "n" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define alt (let ([__l (if (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) k #f) (safe-index (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (int k))) #f)] [__r (if (if dp (if (hash? dp) (hash-ref dp k #f) (safe-index dp (int k))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp k #f) (safe-index dp (int k))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp k #f) (safe-index dp (int k))) #f) j #f) (safe-index (if dp (if (hash? dp) (hash-ref dp k #f) (safe-index dp (int k))) #f) (int j))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define row (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f))
      (if (let ([__l alt] [__r (if row (if (hash? row) (hash-ref row j #f) (safe-index row (int j))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! row (list-set row (int j) alt))
(set! dp (list-set dp (int i) row))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if (hash? g) (hash-set! g "dp" dp) (begin (set! g (make-hash)) (hash-set! g "dp" dp)))
))
)
(define (show_min g u v)
  (let/ec _return (begin
(_return (if (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (safe-index (if g (hash-ref g "dp" #f) #f) (int u))) #f) (if (hash? (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (safe-index (if g (hash-ref g "dp" #f) #f) (int u))) #f)) (hash-ref (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (safe-index (if g (hash-ref g "dp" #f) #f) (int u))) #f) v #f) (safe-index (if (if g (hash-ref g "dp" #f) #f) (if (hash? (if g (hash-ref g "dp" #f) #f)) (hash-ref (if g (hash-ref g "dp" #f) #f) u #f) (safe-index (if g (hash-ref g "dp" #f) #f) (int u))) #f) (int v))) #f))
))
)
(define graph (new_graph 5))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(add_edge graph 0 2 9)
(add_edge graph 0 4 10)
(add_edge graph 1 3 5)
(add_edge graph 2 3 7)
(add_edge graph 3 0 10)
(add_edge graph 3 1 2)
(add_edge graph 3 2 1)
(add_edge graph 3 4 6)
(add_edge graph 4 1 3)
(add_edge graph 4 2 4)
(add_edge graph 4 3 9)
(floyd_warshall graph)
(displayln (to-string (show_min graph 1 4)))
(displayln (to-string (show_min graph 0 3)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
