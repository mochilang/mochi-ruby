;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (if (inexact? x) (real->decimal-string x) (format "~a" x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (min3 a b c)
  (let/ec _return (begin
(define m a)
(if (let ([__l b] [__r m]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! m b)
) (void))
(if (let ([__l c] [__r m]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! m c)
) (void))
(_return m)
))
)
(define (helper word1 word2 cache i j len1 len2)
  (let/ec _return (begin
(if (let ([__l i] [__r len1]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(_return (- len2 j))
) (void))
(if (let ([__l j] [__r len2]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(_return (- len1 i))
) (void))
(if (not (equal? (if (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) (if (hash? (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f)) (hash-ref (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) j #f) (safe-index (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) (int j))) #f) (- 0 1))) (let ()
(_return (if (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) (if (hash? (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f)) (hash-ref (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) j #f) (safe-index (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) (int j))) #f))
) (void))
(define diff 0)
(if (not (string=? (slice word1 i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (slice word2 j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) (let ()
(set! diff 1)
) (void))
(define delete_cost (let ([__l 1] [__r (helper word1 word2 cache (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) j len1 len2)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define insert_cost (let ([__l 1] [__r (helper word1 word2 cache i (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) len1 len2)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define replace_cost (let ([__l diff] [__r (helper word1 word2 cache (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) len1 len2)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! cache (list-set cache i (list-set (list-ref cache i) j (min3 delete_cost insert_cost replace_cost))))
(_return (if (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) (if (hash? (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f)) (hash-ref (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) j #f) (safe-index (if cache (if (hash? cache) (hash-ref cache i #f) (safe-index cache (int i))) #f) (int j))) #f))
))
)
(define (min_distance_up_bottom word1 word2)
  (let/ec _return (begin
(define len1 (cond [(not word1) 0] [(string? word1) (string-length word1)] [(hash? word1) (hash-count word1)] [else (length word1)]))
(define len2 (cond [(not word2) 0] [(string? word2) (string-length word2)] [(hash? word2) (hash-count word2)] [else (length word2)]))
(define cache (list))
(let/ec _break (let ([_ 0])
  (let loop ()
    (when (< _ len1)
(define row (list))
(let/ec _break (let ([_2 0])
  (let loop ()
    (when (< _2 len2)
(set! row (append (or row (list)) (list (- 0 1))))
      (set! _2 (+ _2 1))
      (loop)))
))
(set! cache (append (or cache (list)) (list row)))
      (set! _ (+ _ 1))
      (loop)))
))
(_return (helper word1 word2 cache 0 0 len1 len2))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (min_distance_up_bottom "intention" "execution")))
(displayln (to-string (min_distance_up_bottom "intention" "")))
(displayln (to-string (min_distance_up_bottom "" "")))
(displayln (to-string (min_distance_up_bottom "zooicoarchaeologist" "zoologist")))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
