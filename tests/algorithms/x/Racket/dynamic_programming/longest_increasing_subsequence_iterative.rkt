;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (copy_list xs)
  (let/ec _return (begin
(define res (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! res (append (or res (list)) (list (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (longest_subsequence arr)
  (let/ec _return (begin
(define n (cond [(not arr) 0] [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]))
(define lis (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define single (list))
      (set! single (append (or single (list)) (list (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))))
      (set! lis (append (or lis (list)) (list single)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define prev 0)
      (let/ec _break (let loop ()
  (if (let ([__l prev] [__r i]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (and (let ([__l (if arr (if (hash? arr) (hash-ref arr prev #f) (safe-index arr (int prev))) #f)] [__r (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f)]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ([__l (let ([__l (cond [(not (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f)) 0] [(string? (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f)) (string-length (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f))] [(hash? (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f)) (hash-count (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f))] [else (length (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f))])] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (cond [(not (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f)) 0] [(string? (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f)) (string-length (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))] [(hash? (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f)) (hash-count (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))] [else (length (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))])]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (let ()
(define temp (copy_list (if lis (if (hash? lis) (hash-ref lis prev #f) (safe-index lis (int prev))) #f)))
(define temp2 (append (or temp (list)) (list (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))))
(set! lis (list-set lis (int i) temp2))
) (void))
      (set! prev (let ([__l prev] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define result (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l (cond [(not (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f)) 0] [(string? (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f)) (string-length (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))] [(hash? (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f)) (hash-count (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))] [else (length (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))])] [__r (cond [(not result) 0] [(string? result) (string-length result)] [(hash? result) (hash-count result)] [else (length result)])]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! result (if lis (if (hash? lis) (hash-ref lis i #f) (safe-index lis (int i))) #f))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (main)
  (let/ec _return (begin
(displayln (to-string (longest_subsequence (list 10 22 9 33 21 50 41 60 80))))
(displayln (to-string (longest_subsequence (list 4 8 7 5 1 12 2 3 9))))
(displayln (to-string (longest_subsequence (list 9 8 7 6 5 7))))
(displayln (to-string (longest_subsequence (list 28 26 12 23 35 39))))
(displayln (to-string (longest_subsequence (list 1 1 1))))
(displayln (to-string (longest_subsequence (list))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
