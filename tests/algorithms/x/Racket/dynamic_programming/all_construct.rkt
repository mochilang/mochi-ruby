;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (allConstruct target wordBank)
  (let/ec _return (begin
(define tableSize (let ([__l (cond [(string? target) (string-length target)] [(hash? target) (hash-count target)] [else (length target)])] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define table (list))
(define idx 0)
(let/ec _break (let loop ()
  (if (< idx tableSize) (let/ec _cont
    (define empty (list))
    (set! table (append table (list empty)))
    (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define base (list))
(set! table (list-set table (int 0) (list base)))
(define i 0)
(let/ec _break (let loop ()
  (if (< i tableSize) (let/ec _cont
    (if (not (equal? (cond [(string? (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f)) (string-length (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f))] [(hash? (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f)) (hash-count (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f))] [else (length (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f))]) 0)) (let ()
(define w 0)
(let/ec _break (let loop ()
  (if (< w (cond [(string? wordBank) (string-length wordBank)] [(hash? wordBank) (hash-count wordBank)] [else (length wordBank)])) (let/ec _cont
    (define word (if wordBank (if (hash? wordBank) (hash-ref wordBank w #f) (list-ref wordBank (int w))) #f))
    (define wordLen (cond [(string? word) (string-length word)] [(hash? word) (hash-count word)] [else (length word)]))
    (if (string=? (slice target i (let ([__l i] [__r wordLen]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) word) (let ()
(define k 0)
(let/ec _break (let loop ()
  (if (< k (cond [(string? (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f)) (string-length (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f))] [(hash? (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f)) (hash-count (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f))] [else (length (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f))])) (let/ec _cont
    (define way (if (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f) (if (hash? (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f)) (hash-ref (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f) k #f) (list-ref (if table (if (hash? table) (hash-ref table i #f) (list-ref table (int i))) #f) (int k))) #f))
    (define combination (list))
    (define m 0)
    (let/ec _break (let loop ()
  (if (< m (cond [(string? way) (string-length way)] [(hash? way) (hash-count way)] [else (length way)])) (let/ec _cont
    (set! combination (append combination (list (if way (if (hash? way) (hash-ref way m #f) (list-ref way (int m))) #f))))
    (set! m (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! combination (append combination (list word)))
    (define nextIndex (let ([__l i] [__r wordLen]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! table (list-set table (int nextIndex) (append (if table (if (hash? table) (hash-ref table nextIndex #f) (list-ref table (int nextIndex))) #f) (list combination))))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (void))
    (set! w (let ([__l w] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (if table (if (hash? table) (hash-ref table (cond [(string? target) (string-length target)] [(hash? target) (hash-count target)] [else (length target)]) #f) (list-ref table (int (cond [(string? target) (string-length target)] [(hash? target) (hash-count target)] [else (length target)])))) #f))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (allConstruct "jwajalapa" (list "jwa" "j" "w" "a" "la" "lapa"))))
(displayln (format "~a" (allConstruct "rajamati" (list "s" "raj" "amat" "raja" "ma" "i" "t"))))
(displayln (format "~a" (allConstruct "hexagonosaurus" (list "h" "ex" "hex" "ag" "ago" "ru" "auru" "rus" "go" "no" "o" "s"))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
