;; Generated by Mochi 0.10.64 on 2025-08-13 07:12 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (zeros_matrix rows cols)
  (let/ec _return (begin
(define matrix (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r rows]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r cols]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list 0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! matrix (append (or matrix (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return matrix)
))
)
(define (longest_common_subsequence x y)
  (let/ec _return (begin
(define m (cond [(not x) 0] [(string? x) (string-length x)] [(hash? x) (hash-count x)] [else (length x)]))
(define n (cond [(not y) 0] [(string? y) (string-length y)] [(hash? y) (hash-count y)] [else (length y)]))
(define dp (zeros_matrix m n))
(define i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r m]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define j 1)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (string=? (substring x (- i 1) (+ (- i 1) 1)) (substring y (- j 1) (+ (- j 1) 1))) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j (let ([__l (if (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (- j 1) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (int (- j 1)))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (let ()
(if (let ([__l (if (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) j #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (int j))) #f)] [__r (if (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (- j 1) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (int (- j 1)))) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j (if (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) j #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- i 1) #f) (safe-index dp (int (- i 1)))) #f) (int j))) #f))))
) (let ()
(set! dp (list-set dp i (list-set (list-ref dp i) j (if (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (- j 1) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp i #f) (safe-index dp (int i))) #f) (int (- j 1)))) #f))))
))
))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define seq "")
(define i2 m)
(define j2 n)
(let/ec _break (let loop ()
  (if (and (let ([__l i2] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ([__l j2] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (begin
    (let/ec _cont
      (if (string=? (substring x (- i2 1) (+ (- i2 1) 1)) (substring y (- j2 1) (+ (- j2 1) 1))) (let ()
(set! seq (string-append (substring x (- i2 1) (+ (- i2 1) 1)) seq))
(set! i2 (- i2 1))
(set! j2 (- j2 1))
) (let ()
(if (let ([__l (if (if dp (if (hash? dp) (hash-ref dp (- i2 1) #f) (safe-index dp (int (- i2 1)))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp (- i2 1) #f) (safe-index dp (int (- i2 1)))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp (- i2 1) #f) (safe-index dp (int (- i2 1)))) #f) j2 #f) (safe-index (if dp (if (hash? dp) (hash-ref dp (- i2 1) #f) (safe-index dp (int (- i2 1)))) #f) (int j2))) #f)] [__r (if (if dp (if (hash? dp) (hash-ref dp i2 #f) (safe-index dp (int i2))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp i2 #f) (safe-index dp (int i2))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp i2 #f) (safe-index dp (int i2))) #f) (- j2 1) #f) (safe-index (if dp (if (hash? dp) (hash-ref dp i2 #f) (safe-index dp (int i2))) #f) (int (- j2 1)))) #f)]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(set! i2 (- i2 1))
) (let ()
(set! j2 (- j2 1))
))
))
    )
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "length" (if (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f) (if (hash? (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f)) (hash-ref (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f) n #f) (safe-index (if dp (if (hash? dp) (hash-ref dp m #f) (safe-index dp (int m))) #f) (int n))) #f)) (hash-set! h "sequence" seq) h))
))
)
(define a "AGGTAB")
(define b "GXTXAYB")
(define res (longest_common_subsequence a b))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (string-append (string-append (string-append "len = " (to-string (if res (hash-ref res "length" #f) #f))) ", sub-sequence = ") (if res (hash-ref res "sequence" #f) #f)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
