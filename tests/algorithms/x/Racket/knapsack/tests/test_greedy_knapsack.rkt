;; Generated by Mochi 0.10.60 on 2025-08-08 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (calc_profit profit weight max_weight)
  (let/ec _return (begin
(if (not (equal? (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)]) (cond [(not weight) 0] [(string? weight) (string-length weight)] [(hash? weight) (hash-count weight)] [else (length weight)]))) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "ok" #f) (hash-set! h "value" 0.0) (hash-set! h "error" "The length of profit and weight must be same.") h))
) (void))
(if (<= max_weight 0) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "ok" #f) (hash-set! h "value" 0.0) (hash-set! h "error" "max_weight must greater than zero.") h))
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)])) (let/ec _cont
    (if (< (if profit (if (hash? profit) (hash-ref profit i #f) (list-ref profit (int i))) #f) 0) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "ok" #f) (hash-set! h "value" 0.0) (hash-set! h "error" "Profit can not be negative.") h))
) (void))
    (if (< (if weight (if (hash? weight) (hash-ref weight i #f) (list-ref weight (int i))) #f) 0) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "ok" #f) (hash-set! h "value" 0.0) (hash-set! h "error" "Weight can not be negative.") h))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define used (list))
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)])) (let/ec _cont
    (set! used (append used (list #f)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define limit 0)
(define gain 0.0)
(let/ec _break (let loop ()
  (if (< limit max_weight) (let/ec _cont
    (define max_ratio (- 1.0))
    (define idx (- 0 1))
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k (cond [(not profit) 0] [(string? profit) (string-length profit)] [(hash? profit) (hash-count profit)] [else (length profit)])) (let/ec _cont
    (if (not (if used (if (hash? used) (hash-ref used k #f) (list-ref used (int k))) #f)) (let ()
(define ratio (/ (exact->inexact (if profit (if (hash? profit) (hash-ref profit k #f) (list-ref profit (int k))) #f)) (exact->inexact (if weight (if (hash? weight) (hash-ref weight k #f) (list-ref weight (int k))) #f))))
(if (> ratio max_ratio) (let ()
(set! max_ratio ratio)
(set! idx k)
) (void))
) (void))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (if (equal? idx (- 0 1)) (let ()
(_break)
) (void))
    (set! used (list-set used (int idx) #t))
    (if (>= (- max_weight limit) (if weight (if (hash? weight) (hash-ref weight idx #f) (list-ref weight (int idx))) #f)) (let ()
(set! limit (let ([__l limit] [__r (if weight (if (hash? weight) (hash-ref weight idx #f) (list-ref weight (int idx))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! gain (let ([__l gain] [__r (exact->inexact (if profit (if (hash? profit) (hash-ref profit idx #f) (list-ref profit (int idx))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! gain (let ([__l gain] [__r (* (/ (exact->inexact (- max_weight limit)) (exact->inexact (if weight (if (hash? weight) (hash-ref weight idx #f) (list-ref weight (int idx))) #f))) (exact->inexact (if profit (if (hash? profit) (hash-ref profit idx #f) (list-ref profit (int idx))) #f)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_break)
))
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "ok" #t) (hash-set! h "value" gain) (hash-set! h "error" "") h))
))
)
(define (test_sorted)
  (let/ec _return (begin
(define profit (list 10 20 30 40 50 60))
(define weight (list 2 4 6 8 10 12))
(define res (calc_profit profit weight 100))
(_return (and (if res (hash-ref res "ok" #f) #f) (equal? (if res (hash-ref res "value" #f) #f) 210.0)))
))
)
(define (test_negative_max_weight)
  (let/ec _return (begin
(define profit (list 10 20 30 40 50 60))
(define weight (list 2 4 6 8 10 12))
(define res (calc_profit profit weight (- 15)))
(_return (and (not (if res (hash-ref res "ok" #f) #f)) (string=? (if res (hash-ref res "error" #f) #f) "max_weight must greater than zero.")))
))
)
(define (test_negative_profit_value)
  (let/ec _return (begin
(define profit (list 10 (- 20) 30 40 50 60))
(define weight (list 2 4 6 8 10 12))
(define res (calc_profit profit weight 15))
(_return (and (not (if res (hash-ref res "ok" #f) #f)) (string=? (if res (hash-ref res "error" #f) #f) "Profit can not be negative.")))
))
)
(define (test_negative_weight_value)
  (let/ec _return (begin
(define profit (list 10 20 30 40 50 60))
(define weight (list 2 (- 4) 6 (- 8) 10 12))
(define res (calc_profit profit weight 15))
(_return (and (not (if res (hash-ref res "ok" #f) #f)) (string=? (if res (hash-ref res "error" #f) #f) "Weight can not be negative.")))
))
)
(define (test_null_max_weight)
  (let/ec _return (begin
(define profit (list 10 20 30 40 50 60))
(define weight (list 2 4 6 8 10 12))
(define res (calc_profit profit weight 0))
(_return (and (not (if res (hash-ref res "ok" #f) #f)) (string=? (if res (hash-ref res "error" #f) #f) "max_weight must greater than zero.")))
))
)
(define (test_unequal_list_length)
  (let/ec _return (begin
(define profit (list 10 20 30 40 50))
(define weight (list 2 4 6 8 10 12))
(define res (calc_profit profit weight 100))
(_return (and (not (if res (hash-ref res "ok" #f) #f)) (string=? (if res (hash-ref res "error" #f) #f) "The length of profit and weight must be same.")))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (test_sorted))
(displayln (test_negative_max_weight))
(displayln (test_negative_profit_value))
(displayln (test_negative_weight_value))
(displayln (test_null_max_weight))
(displayln (test_unequal_list_length))
(displayln #t)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
