;; Generated by Mochi 0.10.66 on 2025-08-16 11:48 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (dot x y)
  (let/ec _return (begin
(define sum 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not x) 0] [(string? x) (string-length x)] [(hash? x) (hash-count x)] [else (length x)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! sum (let ([__l sum] [__r (* (if x (if (hash? x) (hash-ref x i #f) (safe-index x (int i))) #f) (if y (if (hash? y) (hash-ref y i #f) (safe-index y (int i))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return sum)
))
)
(define (run_steep_gradient_descent data_x data_y len_data alpha theta)
  (let/ec _return (begin
(define gradients (list))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not theta) 0] [(string? theta) (string-length theta)] [(hash? theta) (hash-count theta)] [else (length theta)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! gradients (append (or gradients (list)) (list 0.0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r len_data]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define prediction (dot theta (if data_x (if (hash? data_x) (hash-ref data_x i #f) (safe-index data_x (int i))) #f)))
      (define error (- prediction (if data_y (if (hash? data_y) (hash-ref data_y i #f) (safe-index data_y (int i))) #f)))
      (define k 0)
      (let/ec _break (let loop ()
  (if (let ([__l k] [__r (cond [(not theta) 0] [(string? theta) (string-length theta)] [(hash? theta) (hash-count theta)] [else (length theta)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! gradients (list-set gradients (int k) (let ([__l (if gradients (if (hash? gradients) (hash-ref gradients k #f) (safe-index gradients (int k))) #f)] [__r (* error (if (if data_x (if (hash? data_x) (hash-ref data_x i #f) (safe-index data_x (int i))) #f) (if (hash? (if data_x (if (hash? data_x) (hash-ref data_x i #f) (safe-index data_x (int i))) #f)) (hash-ref (if data_x (if (hash? data_x) (hash-ref data_x i #f) (safe-index data_x (int i))) #f) k #f) (safe-index (if data_x (if (hash? data_x) (hash-ref data_x i #f) (safe-index data_x (int i))) #f) (int k))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define t (list))
(define g 0)
(let/ec _break (let loop ()
  (if (let ([__l g] [__r (cond [(not theta) 0] [(string? theta) (string-length theta)] [(hash? theta) (hash-count theta)] [else (length theta)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! t (append (or t (list)) (list (- (if theta (if (hash? theta) (hash-ref theta g #f) (safe-index theta (int g))) #f) (* (/ alpha len_data) (if gradients (if (hash? gradients) (hash-ref gradients g #f) (safe-index gradients (int g))) #f))))))
      (set! g (let ([__l g] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return t)
))
)
(define (sum_of_square_error data_x data_y len_data theta)
  (let/ec _return (begin
(define total 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r len_data]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define prediction (dot theta (if data_x (if (hash? data_x) (hash-ref data_x i #f) (safe-index data_x (int i))) #f)))
      (define diff (- prediction (if data_y (if (hash? data_y) (hash-ref data_y i #f) (safe-index data_y (int i))) #f)))
      (set! total (let ([__l total] [__r (* diff diff)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (/ total (* 2.0 len_data)))
))
)
(define (run_linear_regression data_x data_y)
  (let/ec _return (begin
(define iterations 10)
(define alpha 0.01)
(define no_features (cond [(not (if data_x (if (hash? data_x) (hash-ref data_x 0 #f) (safe-index data_x (int 0))) #f)) 0] [(string? (if data_x (if (hash? data_x) (hash-ref data_x 0 #f) (safe-index data_x (int 0))) #f)) (string-length (if data_x (if (hash? data_x) (hash-ref data_x 0 #f) (safe-index data_x (int 0))) #f))] [(hash? (if data_x (if (hash? data_x) (hash-ref data_x 0 #f) (safe-index data_x (int 0))) #f)) (hash-count (if data_x (if (hash? data_x) (hash-ref data_x 0 #f) (safe-index data_x (int 0))) #f))] [else (length (if data_x (if (hash? data_x) (hash-ref data_x 0 #f) (safe-index data_x (int 0))) #f))]))
(define len_data (cond [(not data_x) 0] [(string? data_x) (string-length data_x)] [(hash? data_x) (hash-count data_x)] [else (length data_x)]))
(define theta (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r no_features]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! theta (append (or theta (list)) (list 0.0)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define iter 0)
(let/ec _break (let loop ()
  (if (let ([__l iter] [__r iterations]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! theta (run_steep_gradient_descent data_x data_y len_data alpha theta))
      (define error (sum_of_square_error data_x data_y len_data theta))
      (displayln (string-append (string-append (string-append "At Iteration " (to-string (let ([__l iter] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))) " - Error is ") (to-string error)))
      (set! iter (let ([__l iter] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return theta)
))
)
(define (absf x)
  (let/ec _return (begin
(if (let ([__l x] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return (- x))
) (let ()
(_return x)
))
))
)
(define (mean_absolute_error predicted_y original_y)
  (let/ec _return (begin
(define total 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not predicted_y) 0] [(string? predicted_y) (string-length predicted_y)] [(hash? predicted_y) (hash-count predicted_y)] [else (length predicted_y)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define diff (absf (- (if predicted_y (if (hash? predicted_y) (hash-ref predicted_y i #f) (safe-index predicted_y (int i))) #f) (if original_y (if (hash? original_y) (hash-ref original_y i #f) (safe-index original_y (int i))) #f))))
      (set! total (let ([__l total] [__r diff]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (/ total (cond [(not predicted_y) 0] [(string? predicted_y) (string-length predicted_y)] [(hash? predicted_y) (hash-count predicted_y)] [else (length predicted_y)])))
))
)
(define data_x (list (list 1.0 1.0) (list 1.0 2.0) (list 1.0 3.0)))
(define data_y (list 1.0 2.0 3.0))
(define theta (run_linear_regression data_x data_y))
(define i 0)
(define predicted_y (list 3.0 (- 0.5) 2.0 7.0))
(define original_y (list 2.5 0.0 2.0 8.0))
(define mae (mean_absolute_error predicted_y original_y))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln "Resultant Feature vector :")
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not theta) 0] [(string? theta) (string-length theta)] [(hash? theta) (hash-count theta)] [else (length theta)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (displayln (to-string (if theta (if (hash? theta) (hash-ref theta i #f) (safe-index theta (int i))) #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(displayln (string-append "Mean Absolute Error : " (to-string mae)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
