;; Generated by Mochi 0.10.67 on 2025-08-17 08:49 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (let ([s (~r x #:precision 15 #:notation 'positional)])
       (if (and (inexact? x) (not (string-contains? s ".")))
           (string-append s ".0")
           s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (absf x)
  (let/ec _return (begin
(if (let ([__l x] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return (- x))
) (void))
(_return x)
))
)
(define (hypothesis_value input params)
  (let/ec _return (begin
(define value (if params (if (hash? params) (hash-ref params 0 #f) (safe-index params (int 0))) #f))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not input) 0] [(string? input) (string-length input)] [(hash? input) (hash-count input)] [else (length input)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! value (let ([__l value] [__r (* (if input (if (hash? input) (hash-ref input i #f) (safe-index input (int i))) #f) (if params (if (hash? params) (hash-ref params (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (safe-index params (int (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return value)
))
)
(define (calc_error dp params)
  (let/ec _return (begin
(_return (- (hypothesis_value (if dp (hash-ref dp "x" #f) #f) params) (if dp (hash-ref dp "y" #f) #f)))
))
)
(define (summation_of_cost_derivative index params data)
  (let/ec _return (begin
(define sum 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not data) 0] [(string? data) (string-length data)] [(hash? data) (hash-count data)] [else (length data)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define dp (if data (if (hash? data) (hash-ref data i #f) (safe-index data (int i))) #f))
      (define e (calc_error dp params))
      (if (equal? index (- 1)) (let ()
(set! sum (let ([__l sum] [__r e]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! sum (let ([__l sum] [__r (* e (if (if dp (hash-ref dp "x" #f) #f) (if (hash? (if dp (hash-ref dp "x" #f) #f)) (hash-ref (if dp (hash-ref dp "x" #f) #f) index #f) (safe-index (if dp (hash-ref dp "x" #f) #f) (int index))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return sum)
))
)
(define (get_cost_derivative index params data)
  (let/ec _return (begin
(_return (/ (summation_of_cost_derivative index params data) (exact->inexact (cond [(not data) 0] [(string? data) (string-length data)] [(hash? data) (hash-count data)] [else (length data)]))))
))
)
(define (allclose a b atol rtol)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define diff (absf (- (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f))))
      (define limit (let ([__l atol] [__r (* rtol (absf (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (if (let ([__l diff] [__r limit]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (run_gradient_descent train_data initial_params)
  (let/ec _return (begin
(define learning_rate 0.009)
(define absolute_error_limit 2e-06)
(define relative_error_limit 0.0)
(define j 0)
(define params initial_params)
(let/ec _break (let loop ()
  (if #t (begin
    (let/ec _cont
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define temp (list))
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not params) 0] [(string? params) (string-length params)] [(hash? params) (hash-count params)] [else (length params)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define deriv (get_cost_derivative (- i 1) params train_data))
      (set! temp (append (or temp (list)) (list (- (if params (if (hash? params) (hash-ref params i #f) (safe-index params (int i))) #f) (* learning_rate deriv)))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (if (allclose params temp absolute_error_limit relative_error_limit) (let ()
(displayln (string-append "Number of iterations:" (to-string j)))
(_break)
) (void))
      (set! params temp)
    )
    (loop)) (void))))
(_return params)
))
)
(define (test_gradient_descent test_data params)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not test_data) 0] [(string? test_data) (string-length test_data)] [(hash? test_data) (hash-count test_data)] [else (length test_data)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define dp (if test_data (if (hash? test_data) (hash-ref test_data i #f) (safe-index test_data (int i))) #f))
      (displayln (string-append "Actual output value:" (to-string (if dp (hash-ref dp "y" #f) #f))))
      (displayln (string-append "Hypothesis output:" (to-string (hypothesis_value (if dp (hash-ref dp "x" #f) #f) params))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
))
)
(define train_data (list (let ([h (make-hash)]) (hash-set! h "x" (list 5.0 2.0 3.0)) (hash-set! h "y" 15.0) h) (let ([h (make-hash)]) (hash-set! h "x" (list 6.0 5.0 9.0)) (hash-set! h "y" 25.0) h) (let ([h (make-hash)]) (hash-set! h "x" (list 11.0 12.0 13.0)) (hash-set! h "y" 41.0) h) (let ([h (make-hash)]) (hash-set! h "x" (list 1.0 1.0 1.0)) (hash-set! h "y" 8.0) h) (let ([h (make-hash)]) (hash-set! h "x" (list 11.0 12.0 13.0)) (hash-set! h "y" 41.0) h)))
(define test_data (list (let ([h (make-hash)]) (hash-set! h "x" (list 515.0 22.0 13.0)) (hash-set! h "y" 555.0) h) (let ([h (make-hash)]) (hash-set! h "x" (list 61.0 35.0 49.0)) (hash-set! h "y" 150.0) h)))
(define parameter_vector (list 2.0 4.0 1.0 5.0))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(set! parameter_vector (run_gradient_descent train_data parameter_vector))
(displayln "\nTesting gradient descent for a linear hypothesis function.\n")
(test_gradient_descent test_data parameter_vector)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
