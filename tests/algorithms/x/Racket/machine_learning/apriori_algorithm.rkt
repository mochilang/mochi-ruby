;; Generated by Mochi 0.10.66 on 2025-08-16 11:48 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (load_data)
  (let/ec _return (begin
(_return (list (list "milk") (list "milk" "butter") (list "milk" "bread") (list "milk" "bread" "chips")))
))
)
(define (contains_string xs s)
  (let/ec _return (begin
(let ([__seq xs]) (let/ec _break (for ([v (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (string=? v s) (let ()
(_return #t)
) (void))
  ))))
(_return #f)
))
)
(define (is_subset candidate transaction)
  (let/ec _return (begin
(let ([__seq candidate]) (let/ec _break (for ([it (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (not (contains_string transaction it)) (let ()
(_return #f)
) (void))
  ))))
(_return #t)
))
)
(define (lists_equal a b)
  (let/ec _return (begin
(if (not (equal? (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)]) (cond [(not b) 0] [(string? b) (string-length b)] [(hash? b) (hash-count b)] [else (length b)]))) (let ()
(_return #f)
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (string=? (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f))) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (contains_list itemset item)
  (let/ec _return (begin
(let ([__seq itemset]) (let/ec _break (for ([l (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (lists_equal l item) (let ()
(_return #t)
) (void))
  ))))
(_return #f)
))
)
(define (count_list itemset item)
  (let/ec _return (begin
(define c 0)
(let ([__seq itemset]) (let/ec _break (for ([l (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (lists_equal l item) (let ()
(set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
  ))))
(_return c)
))
)
(define (slice_list xs start)
  (let/ec _return (begin
(define res (list))
(define i start)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! res (append (or res (list)) (list (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (combinations_lists xs k)
  (let/ec _return (begin
(define result (list))
(if (equal? k 0) (let ()
(set! result (append (or result (list)) (list (list))))
(_return result)
) (void))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define head (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f))
      (define tail (slice_list xs (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (define tail_combos (combinations_lists tail (- k 1)))
      (let ([__seq tail_combos]) (let/ec _break (for ([combo (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define new_combo (list))
(set! new_combo (append (or new_combo (list)) (list head)))
(let ([__seq combo]) (let/ec _break (for ([c (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! new_combo (append (or new_combo (list)) (list c)))
  ))))
(set! result (append (or result (list)) (list new_combo)))
  ))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (prune itemset candidates length_)
  (let/ec _return (begin
(define pruned (list))
(let ([__seq candidates]) (let/ec _break (for ([candidate (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define is_subsequence #t)
(let ([__seq candidate]) (let/ec _break (for ([item (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (or (not (contains_list itemset item)) (let ([__l (count_list itemset item)] [__r (- length_ 1)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (let ()
(set! is_subsequence #f)
(_break)
) (void))
  ))))
(if is_subsequence (let ()
(define merged (list))
(let ([__seq candidate]) (let/ec _break (for ([item (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(let ([__seq item]) (let/ec _break (for ([s (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (not (contains_string merged s)) (let ()
(set! merged (append (or merged (list)) (list s)))
) (void))
  ))))
  ))))
(set! pruned (append (or pruned (list)) (list merged)))
) (void))
  ))))
(_return pruned)
))
)
(define (sort_strings xs)
  (let/ec _return (begin
(define res (list))
(let ([__seq xs]) (let/ec _break (for ([s (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! res (append (or res (list)) (list s)))
  ))))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not res) 0] [(string? res) (string-length res)] [(hash? res) (hash-count res)] [else (length res)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define j (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not res) 0] [(string? res) (string-length res)] [(hash? res) (hash-count res)] [else (length res)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (string<? (if res (if (hash? res) (hash-ref res j #f) (safe-index res (int j))) #f) (if res (if (hash? res) (hash-ref res i #f) (safe-index res (int i))) #f)) (let ()
(define tmp (if res (if (hash? res) (hash-ref res i #f) (safe-index res (int i))) #f))
(set! res (list-set res (int i) (if res (if (hash? res) (hash-ref res j #f) (safe-index res (int j))) #f)))
(set! res (list-set res (int j) tmp))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (itemset_to_string xs)
  (let/ec _return (begin
(define s "[")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not xs) 0] [(string? xs) (string-length xs)] [(hash? xs) (hash-count xs)] [else (length xs)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! s (string-append s ", "))
) (void))
      (set! s (string-append (string-append (string-append s "'") (if xs (if (hash? xs) (hash-ref xs i #f) (safe-index xs (int i))) #f)) "'"))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! s (string-append s "]"))
(_return s)
))
)
(define (apriori data min_support)
  (let/ec _return (begin
(define itemset (list))
(let ([__seq data]) (let/ec _break (for ([transaction (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define t (list))
(let ([__seq transaction]) (let/ec _break (for ([v (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! t (append (or t (list)) (list v)))
  ))))
(set! itemset (append (or itemset (list)) (list t)))
  ))))
(define frequent (list))
(define length_ 1)
(let/ec _break (let loop ()
  (if (let ([__l (cond [(not itemset) 0] [(string? itemset) (string-length itemset)] [(hash? itemset) (hash-count itemset)] [else (length itemset)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (begin
    (let/ec _cont
      (define counts (list))
      (define idx 0)
      (let/ec _break (let loop ()
  (if (let ([__l idx] [__r (cond [(not itemset) 0] [(string? itemset) (string-length itemset)] [(hash? itemset) (hash-count itemset)] [else (length itemset)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! counts (append (or counts (list)) (list 0)))
      (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (let ([__seq data]) (let/ec _break (for ([transaction (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not itemset) 0] [(string? itemset) (string-length itemset)] [(hash? itemset) (hash-count itemset)] [else (length itemset)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define candidate (if itemset (if (hash? itemset) (hash-ref itemset j #f) (safe-index itemset (int j))) #f))
      (if (is_subset candidate transaction) (let ()
(set! counts (list-set counts (int j) (let ([__l (if counts (if (hash? counts) (hash-ref counts j #f) (safe-index counts (int j))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
  ))))
      (define new_itemset (list))
      (define k 0)
      (let/ec _break (let loop ()
  (if (let ([__l k] [__r (cond [(not itemset) 0] [(string? itemset) (string-length itemset)] [(hash? itemset) (hash-count itemset)] [else (length itemset)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l (if counts (if (hash? counts) (hash-ref counts k #f) (safe-index counts (int k))) #f)] [__r min_support]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(set! new_itemset (append (or new_itemset (list)) (list (if itemset (if (hash? itemset) (hash-ref itemset k #f) (safe-index itemset (int k))) #f))))
) (void))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! itemset new_itemset)
      (define m 0)
      (let/ec _break (let loop ()
  (if (let ([__l m] [__r (cond [(not itemset) 0] [(string? itemset) (string-length itemset)] [(hash? itemset) (hash-count itemset)] [else (length itemset)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define sorted_item (sort_strings (if itemset (if (hash? itemset) (hash-ref itemset m #f) (safe-index itemset (int m))) #f)))
      (set! frequent (append (or frequent (list)) (list (let ([h (make-hash)]) (hash-set! h "items" sorted_item) (hash-set! h "support" (if counts (if (hash? counts) (hash-ref counts m #f) (safe-index counts (int m))) #f)) h))))
      (set! m (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! length_ (let ([__l length_] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define combos (combinations_lists itemset length_))
      (set! itemset (prune itemset combos length_))
    )
    (loop)) (void))))
(_return frequent)
))
)
(define frequent_itemsets (apriori (load_data) 2))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(let ([__seq frequent_itemsets]) (let/ec _break (for ([fi (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(displayln (string-append (string-append (itemset_to_string (if fi (hash-ref fi "items" #f) #f)) ": ") (to-string (if fi (hash-ref fi "support" #f) #f))))
  ))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
