;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (subarray xs start end)
  (let/ec _return (begin
(define result (list))
(define k start)
(let/ec _break (let loop ()
  (if (< k end) (let/ec _cont
    (set! result (append result (list (if xs (if (hash? xs) (hash-ref xs k #f) (list-ref xs (int k))) #f))))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (merge left_half right_half)
  (let/ec _return (begin
(define result (list))
(define i 0)
(define j 0)
(let/ec _break (let loop ()
  (if (and (< i (cond [(string? left_half) (string-length left_half)] [(hash? left_half) (hash-count left_half)] [else (length left_half)])) (< j (cond [(string? right_half) (string-length right_half)] [(hash? right_half) (hash-count right_half)] [else (length right_half)]))) (let/ec _cont
    (if (< (if left_half (if (hash? left_half) (hash-ref left_half i #f) (list-ref left_half (int i))) #f) (if right_half (if (hash? right_half) (hash-ref right_half j #f) (list-ref right_half (int j))) #f)) (let ()
(set! result (append result (list (if left_half (if (hash? left_half) (hash-ref left_half i #f) (list-ref left_half (int i))) #f))))
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! result (append result (list (if right_half (if (hash? right_half) (hash-ref right_half j #f) (list-ref right_half (int j))) #f))))
(set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (< i (cond [(string? left_half) (string-length left_half)] [(hash? left_half) (hash-count left_half)] [else (length left_half)])) (let/ec _cont
    (set! result (append result (list (if left_half (if (hash? left_half) (hash-ref left_half i #f) (list-ref left_half (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (< j (cond [(string? right_half) (string-length right_half)] [(hash? right_half) (hash-count right_half)] [else (length right_half)])) (let/ec _cont
    (set! result (append result (list (if right_half (if (hash? right_half) (hash-ref right_half j #f) (list-ref right_half (int j))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (merge_sort array)
  (let/ec _return (begin
(if (<= (cond [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)]) 1) (let ()
(_return array)
) (void))
(define middle (floor (/ (cond [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)]) 2)))
(define left_half (subarray array 0 middle))
(define right_half (subarray array middle (cond [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)])))
(define sorted_left (merge_sort left_half))
(define sorted_right (merge_sort right_half))
(_return (merge sorted_left sorted_right))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (merge_sort (list 5 3 1 4 2))))
(displayln (format "~a" (merge_sort (list (- 2) 3 (- 10) 11 99 100000 100 (- 200)))))
(displayln (format "~a" (merge_sort (list (- 200)))))
(displayln (format "~a" (merge_sort (list))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
