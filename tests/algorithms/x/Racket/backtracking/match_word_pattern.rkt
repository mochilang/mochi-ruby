;; Generated by Mochi 0.10.61 on 2025-08-08 18:48 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (get_value keys values key)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not keys) 0] [(string? keys) (string-length keys)] [(hash? keys) (hash-count keys)] [else (length keys)])) (begin
    (let/ec _cont
      (if (string=? (if keys (if (hash? keys) (hash-ref keys i #f) (safe-index keys (int i))) #f) key) (let ()
(_return (if values (if (hash? values) (hash-ref values i #f) (safe-index values (int i))) #f))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #f)
))
)
(define (contains_value values value)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not values) 0] [(string? values) (string-length values)] [(hash? values) (hash-count values)] [else (length values)])) (begin
    (let/ec _cont
      (if (string=? (if values (if (hash? values) (hash-ref values i #f) (safe-index values (int i))) #f) value) (let ()
(_return #t)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #f)
))
)
(define (backtrack pattern input_string pi si keys values)
  (let/ec _return (begin
(if (and (equal? pi (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])) (equal? si (cond [(not input_string) 0] [(string? input_string) (string-length input_string)] [(hash? input_string) (hash-count input_string)] [else (length input_string)]))) (let ()
(_return #t)
) (void))
(if (or (equal? pi (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])) (equal? si (cond [(not input_string) 0] [(string? input_string) (string-length input_string)] [(hash? input_string) (hash-count input_string)] [else (length input_string)]))) (let ()
(_return #f)
) (void))
(define ch (slice pattern pi (let ([__l pi] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(define mapped (get_value keys values ch))
(if mapped (let ()
(if (string=? (slice input_string si (let ([__l si] [__r (cond [(not mapped) 0] [(string? mapped) (string-length mapped)] [(hash? mapped) (hash-count mapped)] [else (length mapped)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) mapped) (let ()
(_return (backtrack pattern input_string (let ([__l pi] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l si] [__r (cond [(not mapped) 0] [(string? mapped) (string-length mapped)] [(hash? mapped) (hash-count mapped)] [else (length mapped)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) keys values))
) (void))
(_return #f)
) (void))
(define end (let ([__l si] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(let/ec _break (let loop ()
  (if (<= end (cond [(not input_string) 0] [(string? input_string) (string-length input_string)] [(hash? input_string) (hash-count input_string)] [else (length input_string)])) (begin
    (let/ec _cont
      (define substr (slice input_string si end))
      (if (contains_value values substr) (let ()
(set! end (let ([__l end] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_cont)
) (void))
      (define new_keys (append keys (list ch)))
      (define new_values (append values (list substr)))
      (if (backtrack pattern input_string (let ([__l pi] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) end new_keys new_values) (let ()
(_return #t)
) (void))
      (set! end (let ([__l end] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #f)
))
)
(define (match_word_pattern pattern input_string)
  (let/ec _return (begin
(define keys (list))
(define values (list))
(_return (backtrack pattern input_string 0 0 keys values))
))
)
(define (main)
  (let/ec _return (begin
(displayln (match_word_pattern "aba" "GraphTreesGraph"))
(displayln (match_word_pattern "xyx" "PythonRubyPython"))
(displayln (match_word_pattern "GG" "PythonJavaPython"))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
