;; Generated by Mochi 0.10.61 on 2025-08-08 18:48 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (create_board n)
  (let/ec _return (begin
(define board (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (< j n) (begin
    (let/ec _cont
      (set! row (append row (list 0)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! board (append board (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return board)
))
)
(define (is_safe board row column)
  (let/ec _return (begin
(define n (cond [(not board) 0] [(string? board) (string-length board)] [(hash? board) (hash-count board)] [else (length board)]))
(define i 0)
(let/ec _break (let loop ()
  (if (< i row) (begin
    (let/ec _cont
      (if (equal? (if (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) (if (hash? (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f)) (hash-ref (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) column #f) (safe-index (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) (int column))) #f) 1) (let ()
(_return #f)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i (- row 1))
(define j (- column 1))
(let/ec _break (let loop ()
  (if (and (>= i 0) (>= j 0)) (begin
    (let/ec _cont
      (if (equal? (if (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) (if (hash? (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f)) (hash-ref (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) j #f) (safe-index (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) (int j))) #f) 1) (let ()
(_return #f)
) (void))
      (set! i (- i 1))
      (set! j (- j 1))
    )
    (loop)) (void))))
(set! i (- row 1))
(set! j (let ([__l column] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(let/ec _break (let loop ()
  (if (and (>= i 0) (< j n)) (begin
    (let/ec _cont
      (if (equal? (if (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) (if (hash? (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f)) (hash-ref (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) j #f) (safe-index (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f) (int j))) #f) 1) (let ()
(_return #f)
) (void))
      (set! i (- i 1))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define (row_string row)
  (let/ec _return (begin
(define s "")
(define j 0)
(let/ec _break (let loop ()
  (if (< j (cond [(not row) 0] [(string? row) (string-length row)] [(hash? row) (hash-count row)] [else (length row)])) (begin
    (let/ec _cont
      (if (equal? (if row (if (hash? row) (hash-ref row j #f) (safe-index row (int j))) #f) 1) (let ()
(set! s (string-append s "Q "))
) (let ()
(set! s (string-append s ". "))
))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return s)
))
)
(define (printboard board)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not board) 0] [(string? board) (string-length board)] [(hash? board) (hash-count board)] [else (length board)])) (begin
    (let/ec _cont
      (displayln (row_string (if board (if (hash? board) (hash-ref board i #f) (safe-index board (int i))) #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
))
)
(define (solve board row)
  (let/ec _return (begin
(if (>= row (cond [(not board) 0] [(string? board) (string-length board)] [(hash? board) (hash-count board)] [else (length board)])) (let ()
(printboard board)
(displayln "")
(_return 1)
) (void))
(define count 0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(not board) 0] [(string? board) (string-length board)] [(hash? board) (hash-count board)] [else (length board)])) (begin
    (let/ec _cont
      (if (is_safe board row i) (let ()
(set! board (list-set board row (list-set (list-ref board row) i 1)))
(set! count (let ([__l count] [__r (solve board (let ([__l row] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! board (list-set board row (list-set (list-ref board row) i 0)))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return count)
))
)
(define (n_queens n)
  (let/ec _return (begin
(define board (create_board n))
(define total (solve board 0))
(displayln (string-append "The total number of solutions are: " (to-string total)))
(_return total)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(n_queens 4)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
