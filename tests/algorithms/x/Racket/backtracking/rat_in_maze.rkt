;; Generated by Mochi 0.10.59 on 2025-08-06 20:36 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define stdout (hash "write" (lambda (s) (display s))))

(define (run_maze maze i j dr dc sol)
  (let/ec _return (begin
(define size (cond [(string? maze) (string-length maze)] [(hash? maze) (hash-count maze)] [else (length maze)]))
(if (and (and (equal? i dr) (equal? j dc)) (equal? (if (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f) (if (hash? (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f)) (hash-ref (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f) j #f) (list-ref (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f) (int j))) #f) 0)) (let ()
(set! sol (list-set sol i (list-set (list-ref sol i) j 0)))
(_return #t)
) (void))
(define lower_flag (and (>= i 0) (>= j 0)))
(define upper_flag (and (< i size) (< j size)))
(if (and lower_flag upper_flag) (let ()
(define block_flag (and (equal? (if (if sol (if (hash? sol) (hash-ref sol i #f) (list-ref sol (int i))) #f) (if (hash? (if sol (if (hash? sol) (hash-ref sol i #f) (list-ref sol (int i))) #f)) (hash-ref (if sol (if (hash? sol) (hash-ref sol i #f) (list-ref sol (int i))) #f) j #f) (list-ref (if sol (if (hash? sol) (hash-ref sol i #f) (list-ref sol (int i))) #f) (int j))) #f) 1) (equal? (if (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f) (if (hash? (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f)) (hash-ref (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f) j #f) (list-ref (if maze (if (hash? maze) (hash-ref maze i #f) (list-ref maze (int i))) #f) (int j))) #f) 0)))
(if block_flag (let ()
(set! sol (list-set sol i (list-set (list-ref sol i) j 0)))
(if (or (or (or (run_maze maze (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) j dr dc sol) (run_maze maze i (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) dr dc sol)) (run_maze maze (- i 1) j dr dc sol)) (run_maze maze i (- j 1) dr dc sol)) (let ()
(_return #t)
) (void))
(set! sol (list-set sol i (list-set (list-ref sol i) j 1)))
(_return #f)
) (void))
) (void))
(_return #f)
))
)
(define (solve_maze maze sr sc dr dc)
  (let/ec _return (begin
(define size (cond [(string? maze) (string-length maze)] [(hash? maze) (hash-count maze)] [else (length maze)]))
(if (not (and (and (and (and (and (and (and (<= 0 sr) (< sr size)) (<= 0 sc)) (< sc size)) (<= 0 dr)) (< dr size)) (<= 0 dc)) (< dc size))) (let ()
(panic "Invalid source or destination coordinates")
) (void))
(define sol (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i size) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j size) (let/ec _cont
    (set! row (append row (list 1)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! sol (append sol (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define solved (run_maze maze sr sc dr dc sol))
(if solved (let ()
(_return sol)
) (let ()
(panic "No solution exists!")
))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(define maze (list (list 0 1 0 1 1) (list 0 0 0 0 0) (list 1 0 1 0 1) (list 0 0 1 0 0) (list 1 0 0 1 0)))
(define n (- (cond [(string? maze) (string-length maze)] [(hash? maze) (hash-count maze)] [else (length maze)]) 1))
(displayln (format "~a" (solve_maze maze 0 0 n n)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
