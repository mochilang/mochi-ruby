;; Generated by Mochi 0.10.65 on 2025-08-14 10:00 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (stringify fs)
  (let/ec _return (begin
(_return (string-append (string-append (string-append (string-append (string-append (string-append (string-append (if fs (hash-ref fs "name" #f) #f) ": [") (to-string (if fs (hash-ref fs "left_boundary" #f) #f))) ", ") (to-string (if fs (hash-ref fs "peak" #f) #f))) ", ") (to-string (if fs (hash-ref fs "right_boundary" #f) #f))) "]"))
))
)
(define (max2 a b)
  (let/ec _return (begin
(if (let ([__l a] [__r b]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(_return a)
) (void))
(_return b)
))
)
(define (min2 a b)
  (let/ec _return (begin
(if (let ([__l a] [__r b]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return a)
) (void))
(_return b)
))
)
(define (complement fs)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "name" (string-append "¬" (if fs (hash-ref fs "name" #f) #f))) (hash-set! h "left_boundary" (- 1.0 (if fs (hash-ref fs "right_boundary" #f) #f))) (hash-set! h "peak" (- 1.0 (if fs (hash-ref fs "left_boundary" #f) #f))) (hash-set! h "right_boundary" (- 1.0 (if fs (hash-ref fs "peak" #f) #f))) h))
))
)
(define (intersection a b)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "name" (string-append (string-append (if a (hash-ref a "name" #f) #f) " ∩ ") (if b (hash-ref b "name" #f) #f))) (hash-set! h "left_boundary" (max2 (if a (hash-ref a "left_boundary" #f) #f) (if b (hash-ref b "left_boundary" #f) #f))) (hash-set! h "peak" (min2 (if a (hash-ref a "right_boundary" #f) #f) (if b (hash-ref b "right_boundary" #f) #f))) (hash-set! h "right_boundary" (/ (let ([__l (if a (hash-ref a "peak" #f) #f)] [__r (if b (hash-ref b "peak" #f) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2.0)) h))
))
)
(define (union a b)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "name" (string-append (string-append (if a (hash-ref a "name" #f) #f) " U ") (if b (hash-ref b "name" #f) #f))) (hash-set! h "left_boundary" (min2 (if a (hash-ref a "left_boundary" #f) #f) (if b (hash-ref b "left_boundary" #f) #f))) (hash-set! h "peak" (max2 (if a (hash-ref a "right_boundary" #f) #f) (if b (hash-ref b "right_boundary" #f) #f))) (hash-set! h "right_boundary" (/ (let ([__l (if a (hash-ref a "peak" #f) #f)] [__r (if b (hash-ref b "peak" #f) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2.0)) h))
))
)
(define (membership fs x)
  (let/ec _return (begin
(if (or (let ([__l x] [__r (if fs (hash-ref fs "left_boundary" #f) #f)]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ([__l x] [__r (if fs (hash-ref fs "right_boundary" #f) #f)]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))]))) (let ()
(_return 0.0)
) (void))
(if (and (let ([__l (if fs (hash-ref fs "left_boundary" #f) #f)] [__r x]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ([__l x] [__r (if fs (hash-ref fs "peak" #f) #f)]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))]))) (let ()
(_return (/ (- x (if fs (hash-ref fs "left_boundary" #f) #f)) (- (if fs (hash-ref fs "peak" #f) #f) (if fs (hash-ref fs "left_boundary" #f) #f))))
) (void))
(if (and (let ([__l (if fs (hash-ref fs "peak" #f) #f)] [__r x]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ([__l x] [__r (if fs (hash-ref fs "right_boundary" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (let ()
(_return (/ (- (if fs (hash-ref fs "right_boundary" #f) #f) x) (- (if fs (hash-ref fs "right_boundary" #f) #f) (if fs (hash-ref fs "peak" #f) #f))))
) (void))
(_return 0.0)
))
)
(define sheru (let ([h (make-hash)]) (hash-set! h "name" "Sheru") (hash-set! h "left_boundary" 0.4) (hash-set! h "peak" 1.0) (hash-set! h "right_boundary" 0.6) h))
(define siya (let ([h (make-hash)]) (hash-set! h "name" "Siya") (hash-set! h "left_boundary" 0.5) (hash-set! h "peak" 1.0) (hash-set! h "right_boundary" 0.7) h))
(define sheru_comp (complement sheru))
(define inter (intersection siya sheru))
(define uni (union siya sheru))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (stringify sheru))
(displayln (stringify siya))
(displayln (stringify sheru_comp))
(displayln (stringify inter))
(displayln (string-append "Sheru membership 0.5: " (to-string (membership sheru 0.5))))
(displayln (string-append "Sheru membership 0.6: " (to-string (membership sheru 0.6))))
(displayln (stringify uni))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
