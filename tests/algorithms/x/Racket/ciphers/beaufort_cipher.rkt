;; Generated by Mochi 0.10.59 on 2025-08-06 21:26 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define stdout (hash "write" (lambda (s) (display s))))

(define ALPHABET "ABCDEFGHIJKLMNOPQRSTUVWXYZ")
(define (index_of ch)
  (let/ec _return (begin
(let/ec _break (let ([i 0])
  (let loop ()
    (when (< i (cond [(string? ALPHABET) (string-length ALPHABET)] [(hash? ALPHABET) (hash-count ALPHABET)] [else (length ALPHABET)]))
(if (string=? (substring ALPHABET i (+ i 1)) ch) (let ()
(_return i)
) (void))
      (set! i (+ i 1))
      (loop)))
))
(_return (- 1))
))
)
(define (generate_key message key)
  (let/ec _return (begin
(define key_new key)
(define i 0)
(let/ec _break (let loop ()
  (if (< (cond [(string? key_new) (string-length key_new)] [(hash? key_new) (hash-count key_new)] [else (length key_new)]) (cond [(string? message) (string-length message)] [(hash? message) (hash-count message)] [else (length message)])) (let/ec _cont
    (set! key_new (string-append key_new (substring key i (+ i 1))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (if (equal? i (cond [(string? key) (string-length key)] [(hash? key) (hash-count key)] [else (length key)])) (let ()
(set! i 0)
) (void))
    (loop)) (void))))
(_return key_new)
))
)
(define (cipher_text message key_new)
  (let/ec _return (begin
(define res "")
(define i 0)
(let/ec _break (let ([idx 0])
  (let loop ()
    (when (< idx (cond [(string? message) (string-length message)] [(hash? message) (hash-count message)] [else (length message)]))
(define ch (substring message idx (+ idx 1)))
(if (string=? ch " ") (let ()
(set! res (string-append res " "))
) (let ()
(define x (modulo (let ([__l (- (index_of ch) (index_of (substring key_new i (+ i 1))))] [__r 26]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 26))
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! res (string-append res (substring ALPHABET x (+ x 1))))
))
      (set! idx (+ idx 1))
      (loop)))
))
(_return res)
))
)
(define (original_text cipher key_new)
  (let/ec _return (begin
(define res "")
(define i 0)
(let/ec _break (let ([idx 0])
  (let loop ()
    (when (< idx (cond [(string? cipher) (string-length cipher)] [(hash? cipher) (hash-count cipher)] [else (length cipher)]))
(define ch (substring cipher idx (+ idx 1)))
(if (string=? ch " ") (let ()
(set! res (string-append res " "))
) (let ()
(define x (modulo (let ([__l (let ([__l (index_of ch)] [__r (index_of (substring key_new i (+ i 1)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r 26]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 26))
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! res (string-append res (substring ALPHABET x (+ x 1))))
))
      (set! idx (+ idx 1))
      (loop)))
))
(_return res)
))
)
(define message "THE GERMAN ATTACK")
(define key "SECRET")
(define key_new (generate_key message key))
(define encrypted (cipher_text message key_new))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (string-append "Encrypted Text = " encrypted))
(displayln (string-append "Original Text = " (original_text encrypted key_new)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
