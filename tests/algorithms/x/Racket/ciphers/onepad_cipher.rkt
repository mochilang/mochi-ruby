;; Generated by Mochi 0.10.59 on 2025-08-06 21:26 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define stdout (hash "write" (lambda (s) (display s))))

(define seed 1)
(define (set_seed s)
  (let/ec _return (begin
(set! seed s)
))
)
(define (randint a b)
  (let/ec _return (begin
(set! seed (modulo (let ([__l (* seed 1103515245)] [__r 12345]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2147483648))
(_return (let ([__l (modulo seed (let ([__l (- b a)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))] [__r a]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define ascii_chars " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~")
(define (ord ch)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? ascii_chars) (string-length ascii_chars)] [(hash? ascii_chars) (hash-count ascii_chars)] [else (length ascii_chars)])) (let/ec _cont
    (if (string=? (substring ascii_chars i (+ i 1)) ch) (let ()
(_return (let ([__l 32] [__r i]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return 0)
))
)
(define (chr code)
  (let/ec _return (begin
(if (or (< code 32) (> code 126)) (let ()
(_return "")
) (void))
(_return (substring ascii_chars (- code 32) (+ (- code 32) 1)))
))
)
(define (encrypt text)
  (let/ec _return (begin
(define cipher (list))
(define key (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? text) (string-length text)] [(hash? text) (hash-count text)] [else (length text)])) (let/ec _cont
    (define p (ord (substring text i (+ i 1))))
    (define k (randint 1 300))
    (define c (* (let ([__l p] [__r k]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) k))
    (set! cipher (append cipher (list c)))
    (set! key (append key (list k)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define res (hash))
(set! res (hash-set (or res (hash)) "cipher" cipher))
(set! res (hash-set (or res (hash)) "key" key))
(_return res)
))
)
(define (decrypt cipher key)
  (let/ec _return (begin
(define plain "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? key) (string-length key)] [(hash? key) (hash-count key)] [else (length key)])) (let/ec _cont
    (define p (quotient (- (if cipher (if (hash? cipher) (hash-ref cipher i #f) (list-ref cipher (int i))) #f) (* (if key (if (hash? key) (hash-ref key i #f) (list-ref key (int i))) #f) (if key (if (hash? key) (hash-ref key i #f) (list-ref key (int i))) #f))) (if key (if (hash? key) (hash-ref key i #f) (list-ref key (int i))) #f)))
    (set! plain (string-append plain (chr p)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return plain)
))
)
(define res (encrypt "Hello"))
(define cipher (if res (hash-ref res "cipher" #f) #f))
(define key (if res (hash-ref res "key" #f) #f))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(set_seed 1)
(displayln cipher)
(displayln key)
(displayln (decrypt cipher key))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
