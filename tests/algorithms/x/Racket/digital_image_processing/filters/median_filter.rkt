;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (insertion_sort a)
  (let/ec _return (begin
(define i 1)
(let/ec _break (let loop ()
  (if (< i (cond [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)])) (let/ec _cont
    (define key (if a (if (hash? a) (hash-ref a i #f) (list-ref a (int i))) #f))
    (define j (- i 1))
    (let/ec _break (let loop ()
  (if (and (>= j 0) (> (if a (if (hash? a) (hash-ref a j #f) (list-ref a (int j))) #f) key)) (let/ec _cont
    (set! a (list-set a (int (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (if a (if (hash? a) (hash-ref a j #f) (list-ref a (int j))) #f)))
    (set! j (- j 1))
    (loop)) (void))))
    (set! a (list-set a (int (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) key))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return a)
))
)
(define (median_filter gray_img mask)
  (let/ec _return (begin
(define rows (cond [(string? gray_img) (string-length gray_img)] [(hash? gray_img) (hash-count gray_img)] [else (length gray_img)]))
(define cols (cond [(string? (if gray_img (if (hash? gray_img) (hash-ref gray_img 0 #f) (list-ref gray_img (int 0))) #f)) (string-length (if gray_img (if (hash? gray_img) (hash-ref gray_img 0 #f) (list-ref gray_img (int 0))) #f))] [(hash? (if gray_img (if (hash? gray_img) (hash-ref gray_img 0 #f) (list-ref gray_img (int 0))) #f)) (hash-count (if gray_img (if (hash? gray_img) (hash-ref gray_img 0 #f) (list-ref gray_img (int 0))) #f))] [else (length (if gray_img (if (hash? gray_img) (hash-ref gray_img 0 #f) (list-ref gray_img (int 0))) #f))]))
(define bd (floor (/ mask 2)))
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i rows) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j cols) (let/ec _cont
    (set! row (append row (list 0)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! result (append result (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! i bd)
(let/ec _break (let loop ()
  (if (< i (- rows bd)) (let/ec _cont
    (define j bd)
    (let/ec _break (let loop ()
  (if (< j (- cols bd)) (let/ec _cont
    (define kernel (list))
    (define x (- i bd))
    (let/ec _break (let loop ()
  (if (<= x (let ([__l i] [__r bd]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (let/ec _cont
    (define y (- j bd))
    (let/ec _break (let loop ()
  (if (<= y (let ([__l j] [__r bd]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (let/ec _cont
    (set! kernel (append kernel (list (if (if gray_img (if (hash? gray_img) (hash-ref gray_img x #f) (list-ref gray_img (int x))) #f) (if (hash? (if gray_img (if (hash? gray_img) (hash-ref gray_img x #f) (list-ref gray_img (int x))) #f)) (hash-ref (if gray_img (if (hash? gray_img) (hash-ref gray_img x #f) (list-ref gray_img (int x))) #f) y #f) (list-ref (if gray_img (if (hash? gray_img) (hash-ref gray_img x #f) (list-ref gray_img (int x))) #f) (int y))) #f))))
    (set! y (let ([__l y] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! x (let ([__l x] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! kernel (insertion_sort kernel))
    (define idx (floor (/ (* mask mask) 2)))
    (set! result (list-set result i (list-set (list-ref result i) j (if kernel (if (hash? kernel) (hash-ref kernel idx #f) (list-ref kernel (int idx))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (main)
  (let/ec _return (begin
(define img (list (list 10 10 10 10 10) (list 10 255 10 255 10) (list 10 10 10 10 10) (list 10 255 10 255 10) (list 10 10 10 10 10)))
(define filtered (median_filter img 3))
(displayln filtered)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
