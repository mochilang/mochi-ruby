;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (make_matrix rows cols value)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i rows) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j cols) (let/ec _cont
    (set! row (append row (list value)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! result (append result (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (my_laplacian src ksize)
  (let/ec _return (begin
(define kernel (list))
(if (equal? ksize 1) (let ()
(set! kernel (list (list 0 (- 1) 0) (list (- 1) 4 (- 1)) (list 0 (- 1) 0)))
) (let ()
(if (equal? ksize 3) (let ()
(set! kernel (list (list 0 1 0) (list 1 (- 4) 1) (list 0 1 0)))
) (let ()
(if (equal? ksize 5) (let ()
(set! kernel (list (list 0 0 (- 1) 0 0) (list 0 (- 1) (- 2) (- 1) 0) (list (- 1) (- 2) 16 (- 2) (- 1)) (list 0 (- 1) (- 2) (- 1) 0) (list 0 0 (- 1) 0 0)))
) (let ()
(if (equal? ksize 7) (let ()
(set! kernel (list (list 0 0 0 (- 1) 0 0 0) (list 0 0 (- 2) (- 3) (- 2) 0 0) (list 0 (- 2) (- 7) (- 10) (- 7) (- 2) 0) (list (- 1) (- 3) (- 10) 68 (- 10) (- 3) (- 1)) (list 0 (- 2) (- 7) (- 10) (- 7) (- 2) 0) (list 0 0 (- 2) (- 3) (- 2) 0 0) (list 0 0 0 (- 1) 0 0 0)))
) (let ()
(panic "ksize must be in (1, 3, 5, 7)")
))
))
))
))
(define rows (cond [(string? src) (string-length src)] [(hash? src) (hash-count src)] [else (length src)]))
(define cols (cond [(string? (if src (if (hash? src) (hash-ref src 0 #f) (list-ref src (int 0))) #f)) (string-length (if src (if (hash? src) (hash-ref src 0 #f) (list-ref src (int 0))) #f))] [(hash? (if src (if (hash? src) (hash-ref src 0 #f) (list-ref src (int 0))) #f)) (hash-count (if src (if (hash? src) (hash-ref src 0 #f) (list-ref src (int 0))) #f))] [else (length (if src (if (hash? src) (hash-ref src 0 #f) (list-ref src (int 0))) #f))]))
(define k (cond [(string? kernel) (string-length kernel)] [(hash? kernel) (hash-count kernel)] [else (length kernel)]))
(define pad (floor (/ k 2)))
(define output (make_matrix rows cols 0))
(define i 0)
(let/ec _break (let loop ()
  (if (< i rows) (let/ec _cont
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j cols) (let/ec _cont
    (define sum 0)
    (define ki 0)
    (let/ec _break (let loop ()
  (if (< ki k) (let/ec _cont
    (define kj 0)
    (let/ec _break (let loop ()
  (if (< kj k) (let/ec _cont
    (define ii (- (let ([__l i] [__r ki]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) pad))
    (define jj (- (let ([__l j] [__r kj]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) pad))
    (define val 0)
    (if (and (and (and (>= ii 0) (< ii rows)) (>= jj 0)) (< jj cols)) (let ()
(set! val (if (if src (if (hash? src) (hash-ref src ii #f) (list-ref src (int ii))) #f) (if (hash? (if src (if (hash? src) (hash-ref src ii #f) (list-ref src (int ii))) #f)) (hash-ref (if src (if (hash? src) (hash-ref src ii #f) (list-ref src (int ii))) #f) jj #f) (list-ref (if src (if (hash? src) (hash-ref src ii #f) (list-ref src (int ii))) #f) (int jj))) #f))
) (void))
    (set! sum (let ([__l sum] [__r (* val (if (if kernel (if (hash? kernel) (hash-ref kernel ki #f) (list-ref kernel (int ki))) #f) (if (hash? (if kernel (if (hash? kernel) (hash-ref kernel ki #f) (list-ref kernel (int ki))) #f)) (hash-ref (if kernel (if (hash? kernel) (hash-ref kernel ki #f) (list-ref kernel (int ki))) #f) kj #f) (list-ref (if kernel (if (hash? kernel) (hash-ref kernel ki #f) (list-ref kernel (int ki))) #f) (int kj))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! kj (let ([__l kj] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! ki (let ([__l ki] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! output (list-set output i (list-set (list-ref output i) j sum)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return output)
))
)
(define image (list (list 0 0 0 0 0) (list 0 10 10 10 0) (list 0 10 10 10 0) (list 0 10 10 10 0) (list 0 0 0 0 0)))
(define result (my_laplacian image 3))
(define r 0)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(let/ec _break (let loop ()
  (if (< r (cond [(string? result) (string-length result)] [(hash? result) (hash-count result)] [else (length result)])) (let/ec _cont
    (define row_str "[")
    (define c 0)
    (let/ec _break (let loop ()
  (if (< c (cond [(string? (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f)) (string-length (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f))] [(hash? (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f)) (hash-count (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f))] [else (length (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f))])) (let/ec _cont
    (set! row_str (string-append row_str (format "~a" (if (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f) (if (hash? (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f)) (hash-ref (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f) c #f) (list-ref (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f) (int c))) #f))))
    (if (< (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (cond [(string? (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f)) (string-length (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f))] [(hash? (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f)) (hash-count (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f))] [else (length (if result (if (hash? result) (hash-ref result r #f) (list-ref result (int r))) #f))])) (let ()
(set! row_str (string-append row_str ", "))
) (void))
    (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! row_str (string-append row_str "]"))
    (displayln row_str)
    (set! r (let ([__l r] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
