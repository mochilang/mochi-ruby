;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (zeros3d h w c)
  (let/ec _return (begin
(define arr (list))
(define y 0)
(let/ec _break (let loop ()
  (if (< y h) (let/ec _cont
    (define row (list))
    (define x 0)
    (let/ec _break (let loop ()
  (if (< x w) (let/ec _cont
    (define pixel (list))
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k c) (let/ec _cont
    (set! pixel (append pixel (list 0)))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! row (append row (list pixel)))
    (set! x (let ([__l x] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! arr (append arr (list row)))
    (set! y (let ([__l y] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return arr)
))
)
(define (resize_nn img dst_w dst_h)
  (let/ec _return (begin
(define src_h (cond [(string? img) (string-length img)] [(hash? img) (hash-count img)] [else (length img)]))
(define src_w (cond [(string? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (string-length (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f))] [(hash? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (hash-count (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f))] [else (length (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f))]))
(define channels (cond [(string? (if (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (if (hash? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (hash-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) 0 #f) (list-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (int 0))) #f)) (string-length (if (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (if (hash? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (hash-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) 0 #f) (list-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (int 0))) #f))] [(hash? (if (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (if (hash? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (hash-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) 0 #f) (list-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (int 0))) #f)) (hash-count (if (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (if (hash? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (hash-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) 0 #f) (list-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (int 0))) #f))] [else (length (if (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (if (hash? (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f)) (hash-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) 0 #f) (list-ref (if img (if (hash? img) (hash-ref img 0 #f) (list-ref img (int 0))) #f) (int 0))) #f))]))
(define ratio_x (/ (exact->inexact src_w) (exact->inexact dst_w)))
(define ratio_y (/ (exact->inexact src_h) (exact->inexact dst_h)))
(define out (zeros3d dst_h dst_w channels))
(define i 0)
(let/ec _break (let loop ()
  (if (< i dst_h) (let/ec _cont
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j dst_w) (let/ec _cont
    (define src_x (int (* ratio_x (exact->inexact j))))
    (define src_y (int (* ratio_y (exact->inexact i))))
    (set! out (list-set out i (list-set (list-ref out i) j (if (if img (if (hash? img) (hash-ref img src_y #f) (list-ref img (int src_y))) #f) (if (hash? (if img (if (hash? img) (hash-ref img src_y #f) (list-ref img (int src_y))) #f)) (hash-ref (if img (if (hash? img) (hash-ref img src_y #f) (list-ref img (int src_y))) #f) src_x #f) (list-ref (if img (if (hash? img) (hash-ref img src_y #f) (list-ref img (int src_y))) #f) (int src_x))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return out)
))
)
(define (main)
  (let/ec _return (begin
(define img (list (list (list 0 0 0) (list 255 255 255)) (list (list 255 0 0) (list 0 255 0))))
(define resized (resize_nn img 4 4))
(displayln resized)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
