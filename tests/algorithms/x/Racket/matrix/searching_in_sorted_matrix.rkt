;; Generated by Mochi 0.10.67 on 2025-08-17 13:28 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (let ([s (~r x #:precision 15 #:notation 'positional)])
       (if (and (inexact? x) (integer? x))
           (number->string (inexact->exact x))
           s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (search_in_sorted_matrix mat m n key)
  (let/ec _return (begin
(define i (- m 1))
(define j 0)
(let/ec _break (let loop ()
  (if (and (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (begin
    (let/ec _cont
      (if (equal? key (if (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) (if (hash? (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)) (hash-ref (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) j #f) (safe-index (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) (int j))) #f)) (let ()
(displayln (string-append (string-append (string-append (string-append (string-append "Key " (to-string key)) " found at row- ") (to-string (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))) " column- ") (to-string (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(_return void)
) (void))
      (if (let ([__l key] [__r (if (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) (if (hash? (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)) (hash-ref (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) j #f) (safe-index (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) (int j))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! i (- i 1))
) (let ()
(set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
    )
    (loop)) (void))))
(displayln (string-append (string-append "Key " (to-string key)) " not found"))
))
)
(define (main)
  (let/ec _return (begin
(define mat (list (list 2.0 5.0 7.0) (list 4.0 8.0 13.0) (list 9.0 11.0 15.0) (list 12.0 17.0 20.0)))
(search_in_sorted_matrix mat (cond [(not mat) 0] [(string? mat) (string-length mat)] [(hash? mat) (hash-count mat)] [else (length mat)]) (cond [(not (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f)) 0] [(string? (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f)) (string-length (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f))] [(hash? (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f)) (hash-count (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f))] [else (length (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f))]) 5.0)
(search_in_sorted_matrix mat (cond [(not mat) 0] [(string? mat) (string-length mat)] [(hash? mat) (hash-count mat)] [else (length mat)]) (cond [(not (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f)) 0] [(string? (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f)) (string-length (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f))] [(hash? (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f)) (hash-count (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f))] [else (length (if mat (if (hash? mat) (hash-ref mat 0 #f) (safe-index mat (int 0))) #f))]) 21.0)
(define mat2 (list (list 2.1 5.0 7.0) (list 4.0 8.0 13.0) (list 9.0 11.0 15.0) (list 12.0 17.0 20.0)))
(search_in_sorted_matrix mat2 (cond [(not mat2) 0] [(string? mat2) (string-length mat2)] [(hash? mat2) (hash-count mat2)] [else (length mat2)]) (cond [(not (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f)) 0] [(string? (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f)) (string-length (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f))] [(hash? (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f)) (hash-count (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f))] [else (length (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f))]) 2.1)
(search_in_sorted_matrix mat2 (cond [(not mat2) 0] [(string? mat2) (string-length mat2)] [(hash? mat2) (hash-count mat2)] [else (length mat2)]) (cond [(not (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f)) 0] [(string? (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f)) (string-length (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f))] [(hash? (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f)) (hash-count (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f))] [else (length (if mat2 (if (hash? mat2) (hash-ref mat2 0 #f) (safe-index mat2 (int 0))) #f))]) 2.2)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
