;; Generated by Mochi 0.10.67 on 2025-08-17 13:28 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (let ([s (~r x #:precision 15 #:notation 'positional)])
       (if (and (inexact? x) (integer? x))
           (number->string (inexact->exact x))
           s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (add matrices)
  (let/ec _return (begin
(define rows (cond [(not (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) 0] [(string? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (string-length (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f))] [(hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-count (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f))] [else (length (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f))]))
(define cols (cond [(not (if (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) 0 #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (int 0))) #f)) 0] [(string? (if (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) 0 #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (int 0))) #f)) (string-length (if (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) 0 #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (int 0))) #f))] [(hash? (if (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) 0 #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (int 0))) #f)) (hash-count (if (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) 0 #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (int 0))) #f))] [else (length (if (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) 0 #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices 0 #f) (safe-index matrices (int 0))) #f) (int 0))) #f))]))
(define r 0)
(define result (list))
(let/ec _break (let loop ()
  (if (let ([__l r] [__r rows]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define c 0)
      (let/ec _break (let loop ()
  (if (let ([__l c] [__r cols]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define sum 0.0)
      (define m 0)
      (let/ec _break (let loop ()
  (if (let ([__l m] [__r (cond [(not matrices) 0] [(string? matrices) (string-length matrices)] [(hash? matrices) (hash-count matrices)] [else (length matrices)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! sum (let ([__l sum] [__r (if (if (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) r #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (int r))) #f) (if (hash? (if (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) r #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (int r))) #f)) (hash-ref (if (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) r #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (int r))) #f) c #f) (safe-index (if (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (if (hash? (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f)) (hash-ref (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) r #f) (safe-index (if matrices (if (hash? matrices) (hash-ref matrices m #f) (safe-index matrices (int m))) #f) (int r))) #f) (int c))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! m (let ([__l m] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! row (append (or row (list)) (list sum)))
      (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! r (let ([__l r] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (subtract a b)
  (let/ec _return (begin
(define rows (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)]))
(define cols (cond [(not (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f)) 0] [(string? (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f)) (string-length (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f))] [(hash? (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f)) (hash-count (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f))] [else (length (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f))]))
(define r 0)
(define result (list))
(let/ec _break (let loop ()
  (if (let ([__l r] [__r rows]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define c 0)
      (let/ec _break (let loop ()
  (if (let ([__l c] [__r cols]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list (- (if (if a (if (hash? a) (hash-ref a r #f) (safe-index a (int r))) #f) (if (hash? (if a (if (hash? a) (hash-ref a r #f) (safe-index a (int r))) #f)) (hash-ref (if a (if (hash? a) (hash-ref a r #f) (safe-index a (int r))) #f) c #f) (safe-index (if a (if (hash? a) (hash-ref a r #f) (safe-index a (int r))) #f) (int c))) #f) (if (if b (if (hash? b) (hash-ref b r #f) (safe-index b (int r))) #f) (if (hash? (if b (if (hash? b) (hash-ref b r #f) (safe-index b (int r))) #f)) (hash-ref (if b (if (hash? b) (hash-ref b r #f) (safe-index b (int r))) #f) c #f) (safe-index (if b (if (hash? b) (hash-ref b r #f) (safe-index b (int r))) #f) (int c))) #f)))))
      (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! r (let ([__l r] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (scalar_multiply matrix n)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list (* (if (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) j #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (int j))) #f) n))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (multiply a b)
  (let/ec _return (begin
(define rowsA (cond [(not a) 0] [(string? a) (string-length a)] [(hash? a) (hash-count a)] [else (length a)]))
(define colsA (cond [(not (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f)) 0] [(string? (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f)) (string-length (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f))] [(hash? (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f)) (hash-count (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f))] [else (length (if a (if (hash? a) (hash-ref a 0 #f) (safe-index a (int 0))) #f))]))
(define rowsB (cond [(not b) 0] [(string? b) (string-length b)] [(hash? b) (hash-count b)] [else (length b)]))
(define colsB (cond [(not (if b (if (hash? b) (hash-ref b 0 #f) (safe-index b (int 0))) #f)) 0] [(string? (if b (if (hash? b) (hash-ref b 0 #f) (safe-index b (int 0))) #f)) (string-length (if b (if (hash? b) (hash-ref b 0 #f) (safe-index b (int 0))) #f))] [(hash? (if b (if (hash? b) (hash-ref b 0 #f) (safe-index b (int 0))) #f)) (hash-count (if b (if (hash? b) (hash-ref b 0 #f) (safe-index b (int 0))) #f))] [else (length (if b (if (hash? b) (hash-ref b 0 #f) (safe-index b (int 0))) #f))]))
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r rowsA]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r colsB]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define sum 0.0)
      (define k 0)
      (let/ec _break (let loop ()
  (if (let ([__l k] [__r colsA]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! sum (let ([__l sum] [__r (* (if (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if (hash? (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f)) (hash-ref (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) k #f) (safe-index (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (int k))) #f) (if (if b (if (hash? b) (hash-ref b k #f) (safe-index b (int k))) #f) (if (hash? (if b (if (hash? b) (hash-ref b k #f) (safe-index b (int k))) #f)) (hash-ref (if b (if (hash? b) (hash-ref b k #f) (safe-index b (int k))) #f) j #f) (safe-index (if b (if (hash? b) (hash-ref b k #f) (safe-index b (int k))) #f) (int j))) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! row (append (or row (list)) (list sum)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (identity n)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (equal? i j) (let ()
(set! row (append (or row (list)) (list 1.0)))
) (let ()
(set! row (append (or row (list)) (list 0.0)))
))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (transpose matrix)
  (let/ec _return (begin
(define rows (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]))
(define cols (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))]))
(define result (list))
(define c 0)
(let/ec _break (let loop ()
  (if (let ([__l c] [__r cols]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define r 0)
      (let/ec _break (let loop ()
  (if (let ([__l r] [__r rows]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list (if (if matrix (if (hash? matrix) (hash-ref matrix r #f) (safe-index matrix (int r))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix r #f) (safe-index matrix (int r))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix r #f) (safe-index matrix (int r))) #f) c #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix r #f) (safe-index matrix (int r))) #f) (int c))) #f))))
      (set! r (let ([__l r] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (minor matrix row column)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (equal? i row)) (let ()
(define new_row (list))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (not (equal? j column)) (let ()
(set! new_row (append (or new_row (list)) (list (if (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) j #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix i #f) (safe-index matrix (int i))) #f) (int j))) #f))))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! result (append (or result (list)) (list new_row)))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (determinant matrix)
  (let/ec _return (begin
(if (equal? (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]) 1) (let ()
(_return (if (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f) 0 #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f) (int 0))) #f))
) (void))
(define det 0.0)
(define c 0)
(let/ec _break (let loop ()
  (if (let ([__l c] [__r (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define sub (minor matrix 0 c))
      (define sign (if (equal? (modulo c 2) 0) 1.0 (- 1.0)))
      (set! det (let ([__l det] [__r (* (* (if (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f) c #f) (safe-index (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (safe-index matrix (int 0))) #f) (int c))) #f) (determinant sub)) sign)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return det)
))
)
(define (inverse matrix)
  (let/ec _return (begin
(define det (determinant matrix))
(if (equal? det 0.0) (let ()
(_return (list))
) (void))
(define size (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]))
(define matrix_minor (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define m (minor matrix i j))
      (set! row (append (or row (list)) (list (determinant m))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! matrix_minor (append (or matrix_minor (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define cofactors (list))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define sign (if (equal? (modulo (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2) 0) 1.0 (- 1.0)))
      (set! row (append (or row (list)) (list (* (if (if matrix_minor (if (hash? matrix_minor) (hash-ref matrix_minor i #f) (safe-index matrix_minor (int i))) #f) (if (hash? (if matrix_minor (if (hash? matrix_minor) (hash-ref matrix_minor i #f) (safe-index matrix_minor (int i))) #f)) (hash-ref (if matrix_minor (if (hash? matrix_minor) (hash-ref matrix_minor i #f) (safe-index matrix_minor (int i))) #f) j #f) (safe-index (if matrix_minor (if (hash? matrix_minor) (hash-ref matrix_minor i #f) (safe-index matrix_minor (int i))) #f) (int j))) #f) sign))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! cofactors (append (or cofactors (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define adjugate (transpose cofactors))
(_return (scalar_multiply adjugate (/ 1.0 det)))
))
)
(define (main)
  (let/ec _return (begin
(define matrix_a (list (list 12.0 10.0) (list 3.0 9.0)))
(define matrix_b (list (list 3.0 4.0) (list 7.0 4.0)))
(define matrix_c (list (list 11.0 12.0 13.0 14.0) (list 21.0 22.0 23.0 24.0) (list 31.0 32.0 33.0 34.0) (list 41.0 42.0 43.0 44.0)))
(define matrix_d (list (list 3.0 0.0 2.0) (list 2.0 0.0 (- 2.0)) (list 0.0 1.0 1.0)))
(displayln (string-append (string-append "Add Operation, add(matrix_a, matrix_b) = " (to-string (add (list matrix_a matrix_b)))) " \n"))
(displayln (string-append (string-append "Multiply Operation, multiply(matrix_a, matrix_b) = " (to-string (multiply matrix_a matrix_b))) " \n"))
(displayln (string-append (string-append "Identity: " (to-string (identity 5))) "\n"))
(displayln (string-append (string-append (string-append (string-append "Minor of " (to-string matrix_c)) " = ") (to-string (minor matrix_c 1 2))) " \n"))
(displayln (string-append (string-append (string-append (string-append "Determinant of " (to-string matrix_b)) " = ") (to-string (determinant matrix_b))) " \n"))
(displayln (string-append (string-append (string-append (string-append "Inverse of " (to-string matrix_d)) " = ") (to-string (inverse matrix_d))) "\n"))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
