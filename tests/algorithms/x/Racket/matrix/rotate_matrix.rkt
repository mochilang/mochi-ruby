;; Generated by Mochi 0.10.67 on 2025-08-17 13:28 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (let ([s (~r x #:precision 15 #:notation 'positional)])
       (if (and (inexact? x) (integer? x))
           (number->string (inexact->exact x))
           s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (abs_int n)
  (let/ec _return (begin
(if (let ([__l n] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return (- n))
) (void))
(_return n)
))
)
(define (make_matrix row_size)
  (let/ec _return (begin
(define size (abs_int row_size))
(if (equal? size 0) (let ()
(set! size 4)
) (void))
(define mat (list))
(define y 0)
(let/ec _break (let loop ()
  (if (let ([__l y] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define x 0)
      (let/ec _break (let loop ()
  (if (let ([__l x] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list (let ([__l (let ([__l 1] [__r x]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (* y size)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
      (set! x (let ([__l x] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! mat (append (or mat (list)) (list row)))
      (set! y (let ([__l y] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return mat)
))
)
(define (transpose mat)
  (let/ec _return (begin
(define n (cond [(not mat) 0] [(string? mat) (string-length mat)] [(hash? mat) (hash-count mat)] [else (length mat)]))
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list (if (if mat (if (hash? mat) (hash-ref mat j #f) (safe-index mat (int j))) #f) (if (hash? (if mat (if (hash? mat) (hash-ref mat j #f) (safe-index mat (int j))) #f)) (hash-ref (if mat (if (hash? mat) (hash-ref mat j #f) (safe-index mat (int j))) #f) i #f) (safe-index (if mat (if (hash? mat) (hash-ref mat j #f) (safe-index mat (int j))) #f) (int i))) #f))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (reverse_row mat)
  (let/ec _return (begin
(define result (list))
(define i (- (cond [(not mat) 0] [(string? mat) (string-length mat)] [(hash? mat) (hash-count mat)] [else (length mat)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (append (or result (list)) (list (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f))))
      (set! i (- i 1))
    )
    (loop)) (void))))
(_return result)
))
)
(define (reverse_column mat)
  (let/ec _return (begin
(define result (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not mat) 0] [(string? mat) (string-length mat)] [(hash? mat) (hash-count mat)] [else (length mat)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define row (list))
      (define j (- (cond [(not (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)) 0] [(string? (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)) (string-length (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f))] [(hash? (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)) (hash-count (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f))] [else (length (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f))]) 1))
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! row (append (or row (list)) (list (if (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) (if (hash? (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)) (hash-ref (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) j #f) (safe-index (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f) (int j))) #f))))
      (set! j (- j 1))
    )
    (loop)) (void))))
      (set! result (append (or result (list)) (list row)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (rotate_90 mat)
  (let/ec _return (begin
(define t (transpose mat))
(define rr (reverse_row t))
(_return rr)
))
)
(define (rotate_180 mat)
  (let/ec _return (begin
(define rc (reverse_column mat))
(define rr (reverse_row rc))
(_return rr)
))
)
(define (rotate_270 mat)
  (let/ec _return (begin
(define t (transpose mat))
(define rc (reverse_column t))
(_return rc)
))
)
(define (row_to_string row)
  (let/ec _return (begin
(define line "")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not row) 0] [(string? row) (string-length row)] [(hash? row) (hash-count row)] [else (length row)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (equal? i 0) (let ()
(set! line (to-string (if row (if (hash? row) (hash-ref row i #f) (safe-index row (int i))) #f)))
) (let ()
(set! line (string-append (string-append line " ") (to-string (if row (if (hash? row) (hash-ref row i #f) (safe-index row (int i))) #f))))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return line)
))
)
(define (print_matrix mat)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not mat) 0] [(string? mat) (string-length mat)] [(hash? mat) (hash-count mat)] [else (length mat)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (displayln (row_to_string (if mat (if (hash? mat) (hash-ref mat i #f) (safe-index mat (int i))) #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
))
)
(define mat (make_matrix 4))
(define r90 (rotate_90 mat))
(define r180 (rotate_180 mat))
(define r270 (rotate_270 mat))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln "\norigin:\n")
(print_matrix mat)
(displayln "\nrotate 90 counterclockwise:\n")
(print_matrix r90)
(set! mat (make_matrix 4))
(displayln "\norigin:\n")
(print_matrix mat)
(displayln "\nrotate 180:\n")
(print_matrix r180)
(set! mat (make_matrix 4))
(displayln "\norigin:\n")
(print_matrix mat)
(displayln "\nrotate 270 counterclockwise:\n")
(print_matrix r270)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
