;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define units (list "km/h" "m/s" "mph" "knot"))
(define speed_chart (list 1.0 3.6 1.609344 1.852))
(define speed_chart_inverse (list 1.0 0.277777778 0.621371192 0.539956803))
(define (index_of arr value)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (if (string=? (if arr (if (hash? arr) (hash-ref arr i #f) (list-ref arr (int i))) #f) value) (let ()
(_return i)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (- 1))
))
)
(define (units_string arr)
  (let/ec _return (begin
(define s "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (if (> i 0) (let ()
(set! s (string-append s ", "))
) (void))
    (set! s (string-append s (if arr (if (hash? arr) (hash-ref arr i #f) (list-ref arr (int i))) #f)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return s)
))
)
(define (round3 x)
  (let/ec _return (begin
(define y (let ([__l (* x 1000.0)] [__r 0.5]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define z (int y))
(define zf (exact->inexact z))
(_return (/ zf 1000.0))
))
)
(define (convert_speed speed unit_from unit_to)
  (let/ec _return (begin
(define from_index (index_of units unit_from))
(define to_index (index_of units unit_to))
(if (or (< from_index 0) (< to_index 0)) (let ()
(define msg (string-append (string-append (string-append (string-append (string-append "Incorrect 'from_type' or 'to_type' value: " unit_from) ", ") unit_to) "\nValid values are: ") (units_string units)))
(panic msg)
) (void))
(define result (* (* speed (if speed_chart (if (hash? speed_chart) (hash-ref speed_chart from_index #f) (list-ref speed_chart (int from_index))) #f)) (if speed_chart_inverse (if (hash? speed_chart_inverse) (hash-ref speed_chart_inverse to_index #f) (list-ref speed_chart_inverse (int to_index))) #f)))
(define r (round3 result))
(_return r)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (convert_speed 100.0 "km/h" "m/s")))
(displayln (format "~a" (convert_speed 100.0 "km/h" "mph")))
(displayln (format "~a" (convert_speed 100.0 "km/h" "knot")))
(displayln (format "~a" (convert_speed 100.0 "m/s" "km/h")))
(displayln (format "~a" (convert_speed 100.0 "m/s" "mph")))
(displayln (format "~a" (convert_speed 100.0 "m/s" "knot")))
(displayln (format "~a" (convert_speed 100.0 "mph" "km/h")))
(displayln (format "~a" (convert_speed 100.0 "mph" "m/s")))
(displayln (format "~a" (convert_speed 100.0 "mph" "knot")))
(displayln (format "~a" (convert_speed 100.0 "knot" "km/h")))
(displayln (format "~a" (convert_speed 100.0 "knot" "m/s")))
(displayln (format "~a" (convert_speed 100.0 "knot" "mph")))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
