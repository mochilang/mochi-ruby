;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define units (list "cubic meter" "litre" "kilolitre" "gallon" "cubic yard" "cubic foot" "cup"))
(define from_factors (list 1.0 0.001 1.0 0.00454 0.76455 0.028 0.000236588))
(define to_factors (list 1.0 1000.0 1.0 264.172 1.30795 35.3147 4226.75))
(define (supported_values)
  (let/ec _return (begin
(define result (if units (if (hash? units) (hash-ref units 0 #f) (list-ref units (int 0))) #f))
(define i 1)
(let/ec _break (let loop ()
  (if (< i (cond [(string? units) (string-length units)] [(hash? units) (hash-count units)] [else (length units)])) (let/ec _cont
    (set! result (string-append (string-append result ", ") (if units (if (hash? units) (hash-ref units i #f) (list-ref units (int i))) #f)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (find_index name)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? units) (string-length units)] [(hash? units) (hash-count units)] [else (length units)])) (let/ec _cont
    (if (string=? (if units (if (hash? units) (hash-ref units i #f) (list-ref units (int i))) #f) name) (let ()
(_return i)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (- 1))
))
)
(define (get_from_factor name)
  (let/ec _return (begin
(define idx (find_index name))
(if (< idx 0) (let ()
(panic (string-append (string-append (string-append "Invalid 'from_type' value: '" name) "' Supported values are: ") (supported_values)))
) (void))
(_return (if from_factors (if (hash? from_factors) (hash-ref from_factors idx #f) (list-ref from_factors (int idx))) #f))
))
)
(define (get_to_factor name)
  (let/ec _return (begin
(define idx (find_index name))
(if (< idx 0) (let ()
(panic (string-append (string-append (string-append "Invalid 'to_type' value: '" name) "' Supported values are: ") (supported_values)))
) (void))
(_return (if to_factors (if (hash? to_factors) (hash-ref to_factors idx #f) (list-ref to_factors (int idx))) #f))
))
)
(define (volume_conversion value from_type to_type)
  (let/ec _return (begin
(define from_factor (get_from_factor from_type))
(define to_factor (get_to_factor to_type))
(_return (* (* value from_factor) to_factor))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (volume_conversion 4.0 "cubic meter" "litre")))
(displayln (format "~a" (volume_conversion 1.0 "litre" "gallon")))
(displayln (format "~a" (volume_conversion 1.0 "kilolitre" "cubic meter")))
(displayln (format "~a" (volume_conversion 3.0 "gallon" "cubic yard")))
(displayln (format "~a" (volume_conversion 2.0 "cubic yard" "litre")))
(displayln (format "~a" (volume_conversion 4.0 "cubic foot" "cup")))
(displayln (format "~a" (volume_conversion 1.0 "cup" "kilolitre")))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
