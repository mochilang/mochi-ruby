;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define PRESSURE_CONVERSION (hash "atm" (hash "from_factor" 1.0 "to_factor" 1.0) "pascal" (hash "from_factor" 9.8e-06 "to_factor" 101325.0) "bar" (hash "from_factor" 0.986923 "to_factor" 1.01325) "kilopascal" (hash "from_factor" 0.00986923 "to_factor" 101.325) "megapascal" (hash "from_factor" 9.86923 "to_factor" 0.101325) "psi" (hash "from_factor" 0.068046 "to_factor" 14.6959) "inHg" (hash "from_factor" 0.0334211 "to_factor" 29.9213) "torr" (hash "from_factor" 0.00131579 "to_factor" 760.0)))
(define (pressure_conversion value from_type to_type)
  (let/ec _return (begin
(if (not (hash-has-key? PRESSURE_CONVERSION from_type)) (let ()
(define keys ((if ((if Object (hash-ref Object "keys" #f) #f) Object PRESSURE_CONVERSION) (hash-ref ((if Object (hash-ref Object "keys" #f) #f) Object PRESSURE_CONVERSION) "join" #f) #f) ((if Object (hash-ref Object "keys" #f) #f) Object PRESSURE_CONVERSION) ", "))
(panic (string-append (string-append (string-append "Invalid 'from_type' value: '" from_type) "'  Supported values are:\n") keys))
) (void))
(if (not (hash-has-key? PRESSURE_CONVERSION to_type)) (let ()
(define keys ((if ((if Object (hash-ref Object "keys" #f) #f) Object PRESSURE_CONVERSION) (hash-ref ((if Object (hash-ref Object "keys" #f) #f) Object PRESSURE_CONVERSION) "join" #f) #f) ((if Object (hash-ref Object "keys" #f) #f) Object PRESSURE_CONVERSION) ", "))
(panic (string-append (string-append (string-append "Invalid 'to_type' value: '" to_type) ".  Supported values are:\n") keys))
) (void))
(define from (if PRESSURE_CONVERSION (hash-ref PRESSURE_CONVERSION from_type #f) #f))
(define to (if PRESSURE_CONVERSION (hash-ref PRESSURE_CONVERSION to_type #f) #f))
(_return (* (* value (if from (hash-ref from "from_factor" #f) #f)) (if to (hash-ref to "to_factor" #f) #f)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (pressure_conversion 4.0 "atm" "pascal"))
(displayln (pressure_conversion 1.0 "pascal" "psi"))
(displayln (pressure_conversion 1.0 "bar" "atm"))
(displayln (pressure_conversion 3.0 "kilopascal" "bar"))
(displayln (pressure_conversion 2.0 "megapascal" "psi"))
(displayln (pressure_conversion 4.0 "psi" "torr"))
(displayln (pressure_conversion 1.0 "inHg" "atm"))
(displayln (pressure_conversion 1.0 "torr" "psi"))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
