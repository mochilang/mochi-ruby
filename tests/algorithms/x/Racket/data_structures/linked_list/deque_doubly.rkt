;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/linked_list")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (new_deque)
  (let/ec _return (begin
(define nodes (vector))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" "") (hash-set! h "prev" (- 1)) (hash-set! h "next" 1) h)))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" "") (hash-set! h "prev" 0) (hash-set! h "next" (- 1)) h)))
(_return (let ([h (make-hash)]) (hash-set! h "nodes" nodes) (hash-set! h "header" 0) (hash-set! h "trailer" 1) (hash-set! h "size" 0) h))
))
)
(define (is_empty d)
  (let/ec _return (begin
(_return (equal? (if d (hash-ref d "size" #f) #f) 0))
))
)
(define (front d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "List is empty")
) (void))
(define head (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "header" #f) #f) #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "header" #f) #f)))) #f))
(define idx (if head (hash-ref head "next" #f) #f))
(define node (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) idx #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int idx))) #f))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (back d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "List is empty")
) (void))
(define tail (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "trailer" #f) #f) #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "trailer" #f) #f)))) #f))
(define idx (if tail (hash-ref tail "prev" #f) #f))
(define node (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) idx #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int idx))) #f))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (insert d pred value succ)
  (let/ec _return (begin
(define nodes (if d (hash-ref d "nodes" #f) #f))
(define new_idx (cond [(not nodes) 0] [(string? nodes) (string-length nodes)] [(vector? nodes) (vector-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" value) (hash-set! h "prev" pred) (hash-set! h "next" succ) h)))
(define pred_node (if nodes (if (hash? nodes) (hash-ref nodes pred #f) (safe-index nodes (int pred))) #f))
(if (hash? pred_node) (hash-set! pred_node "next" new_idx) (begin (set! pred_node (make-hash)) (hash-set! pred_node "next" new_idx)))
(vector-set! nodes (int pred) pred_node)
(define succ_node (if nodes (if (hash? nodes) (hash-ref nodes succ #f) (safe-index nodes (int succ))) #f))
(if (hash? succ_node) (hash-set! succ_node "prev" new_idx) (begin (set! succ_node (make-hash)) (hash-set! succ_node "prev" new_idx)))
(vector-set! nodes (int succ) succ_node)
(if (hash? d) (hash-set! d "nodes" nodes) (begin (set! d (make-hash)) (hash-set! d "nodes" nodes)))
(if (hash? d) (hash-set! d "size" (let ([__l (if d (hash-ref d "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! d (make-hash)) (hash-set! d "size" (let ([__l (if d (hash-ref d "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(_return d)
))
)
(define (delete d idx)
  (let/ec _return (begin
(define nodes (if d (hash-ref d "nodes" #f) #f))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (safe-index nodes (int idx))) #f))
(define pred (if node (hash-ref node "prev" #f) #f))
(define succ (if node (hash-ref node "next" #f) #f))
(define pred_node (if nodes (if (hash? nodes) (hash-ref nodes pred #f) (safe-index nodes (int pred))) #f))
(if (hash? pred_node) (hash-set! pred_node "next" succ) (begin (set! pred_node (make-hash)) (hash-set! pred_node "next" succ)))
(vector-set! nodes (int pred) pred_node)
(define succ_node (if nodes (if (hash? nodes) (hash-ref nodes succ #f) (safe-index nodes (int succ))) #f))
(if (hash? succ_node) (hash-set! succ_node "prev" pred) (begin (set! succ_node (make-hash)) (hash-set! succ_node "prev" pred)))
(vector-set! nodes (int succ) succ_node)
(define val (if node (hash-ref node "data" #f) #f))
(if (hash? d) (hash-set! d "nodes" nodes) (begin (set! d (make-hash)) (hash-set! d "nodes" nodes)))
(if (hash? d) (hash-set! d "size" (- (if d (hash-ref d "size" #f) #f) 1)) (begin (set! d (make-hash)) (hash-set! d "size" (- (if d (hash-ref d "size" #f) #f) 1))))
(_return (let ([h (make-hash)]) (hash-set! h "deque" d) (hash-set! h "value" val) h))
))
)
(define (add_first d value)
  (let/ec _return (begin
(define head (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "header" #f) #f) #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "header" #f) #f)))) #f))
(define succ (if head (hash-ref head "next" #f) #f))
(_return (insert d (if d (hash-ref d "header" #f) #f) value succ))
))
)
(define (add_last d value)
  (let/ec _return (begin
(define tail (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "trailer" #f) #f) #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "trailer" #f) #f)))) #f))
(define pred (if tail (hash-ref tail "prev" #f) #f))
(_return (insert d pred value (if d (hash-ref d "trailer" #f) #f)))
))
)
(define (remove_first d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "remove_first from empty list")
) (void))
(define head (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "header" #f) #f) #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "header" #f) #f)))) #f))
(define idx (if head (hash-ref head "next" #f) #f))
(_return (delete d idx))
))
)
(define (remove_last d)
  (let/ec _return (begin
(if (is_empty d) (let ()
(panic "remove_first from empty list")
) (void))
(define tail (if (if d (hash-ref d "nodes" #f) #f) (if (hash? (if d (hash-ref d "nodes" #f) #f)) (hash-ref (if d (hash-ref d "nodes" #f) #f) (if d (hash-ref d "trailer" #f) #f) #f) (safe-index (if d (hash-ref d "nodes" #f) #f) (int (if d (hash-ref d "trailer" #f) #f)))) #f))
(define idx (if tail (hash-ref tail "prev" #f) #f))
(_return (delete d idx))
))
)
(define (main)
  (let/ec _return (begin
(define d (new_deque))
(set! d (add_first d "A"))
(displayln (to-string (front d)))
(set! d (add_last d "B"))
(displayln (to-string (back d)))
(define r (remove_first d))
(set! d (if r (hash-ref r "deque" #f) #f))
(displayln (to-string (if r (hash-ref r "value" #f) #f)))
(set! r (remove_last d))
(set! d (if r (hash-ref r "deque" #f) #f))
(displayln (to-string (if r (hash-ref r "value" #f) #f)))
(displayln (to-string (to-string (is_empty d))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
