;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/linked_list")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (empty_list)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "nodes" (vector)) (hash-set! h "head_idx" (- 1)) (hash-set! h "tail_idx" (- 1)) h))
))
)
(define (get_head_data ll)
  (let/ec _return (begin
(if (equal? (if ll (hash-ref ll "head_idx" #f) #f) (- 1)) (let ()
(_return (- 1))
) (void))
(define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) (if ll (hash-ref ll "head_idx" #f) #f) #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int (if ll (hash-ref ll "head_idx" #f) #f)))) #f))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (get_tail_data ll)
  (let/ec _return (begin
(if (equal? (if ll (hash-ref ll "tail_idx" #f) #f) (- 1)) (let ()
(_return (- 1))
) (void))
(define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) (if ll (hash-ref ll "tail_idx" #f) #f) #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int (if ll (hash-ref ll "tail_idx" #f) #f)))) #f))
(_return (if node (hash-ref node "data" #f) #f))
))
)
(define (insert_before_node ll idx new_idx)
  (let/ec _return (begin
(define nodes (if ll (hash-ref ll "nodes" #f) #f))
(define new_node (if nodes (if (hash? nodes) (hash-ref nodes new_idx #f) (safe-index nodes (int new_idx))) #f))
(if (hash? new_node) (hash-set! new_node "next_index" idx) (begin (set! new_node (make-hash)) (hash-set! new_node "next_index" idx)))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (safe-index nodes (int idx))) #f))
(define p (if node (hash-ref node "prev_index" #f) #f))
(if (hash? new_node) (hash-set! new_node "prev_index" p) (begin (set! new_node (make-hash)) (hash-set! new_node "prev_index" p)))
(vector-set! nodes (int new_idx) new_node)
(if (equal? p (- 1)) (let ()
(if (hash? ll) (hash-set! ll "head_idx" new_idx) (begin (set! ll (make-hash)) (hash-set! ll "head_idx" new_idx)))
) (let ()
(define prev_node (if nodes (if (hash? nodes) (hash-ref nodes p #f) (safe-index nodes (int p))) #f))
(if (hash? prev_node) (hash-set! prev_node "next_index" new_idx) (begin (set! prev_node (make-hash)) (hash-set! prev_node "next_index" new_idx)))
(vector-set! nodes (int p) prev_node)
))
(if (hash? node) (hash-set! node "prev_index" new_idx) (begin (set! node (make-hash)) (hash-set! node "prev_index" new_idx)))
(vector-set! nodes (int idx) node)
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
))
)
(define (insert_after_node ll idx new_idx)
  (let/ec _return (begin
(define nodes (if ll (hash-ref ll "nodes" #f) #f))
(define new_node (if nodes (if (hash? nodes) (hash-ref nodes new_idx #f) (safe-index nodes (int new_idx))) #f))
(if (hash? new_node) (hash-set! new_node "prev_index" idx) (begin (set! new_node (make-hash)) (hash-set! new_node "prev_index" idx)))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (safe-index nodes (int idx))) #f))
(define nxt (if node (hash-ref node "next_index" #f) #f))
(if (hash? new_node) (hash-set! new_node "next_index" nxt) (begin (set! new_node (make-hash)) (hash-set! new_node "next_index" nxt)))
(vector-set! nodes (int new_idx) new_node)
(if (equal? nxt (- 1)) (let ()
(if (hash? ll) (hash-set! ll "tail_idx" new_idx) (begin (set! ll (make-hash)) (hash-set! ll "tail_idx" new_idx)))
) (let ()
(define next_node (if nodes (if (hash? nodes) (hash-ref nodes nxt #f) (safe-index nodes (int nxt))) #f))
(if (hash? next_node) (hash-set! next_node "prev_index" new_idx) (begin (set! next_node (make-hash)) (hash-set! next_node "prev_index" new_idx)))
(vector-set! nodes (int nxt) next_node)
))
(if (hash? node) (hash-set! node "next_index" new_idx) (begin (set! node (make-hash)) (hash-set! node "next_index" new_idx)))
(vector-set! nodes (int idx) node)
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
))
)
(define (set_head ll idx)
  (let/ec _return (begin
(if (equal? (if ll (hash-ref ll "head_idx" #f) #f) (- 1)) (let ()
(if (hash? ll) (hash-set! ll "head_idx" idx) (begin (set! ll (make-hash)) (hash-set! ll "head_idx" idx)))
(if (hash? ll) (hash-set! ll "tail_idx" idx) (begin (set! ll (make-hash)) (hash-set! ll "tail_idx" idx)))
) (let ()
(insert_before_node ll (if ll (hash-ref ll "head_idx" #f) #f) idx)
))
))
)
(define (set_tail ll idx)
  (let/ec _return (begin
(if (equal? (if ll (hash-ref ll "tail_idx" #f) #f) (- 1)) (let ()
(if (hash? ll) (hash-set! ll "head_idx" idx) (begin (set! ll (make-hash)) (hash-set! ll "head_idx" idx)))
(if (hash? ll) (hash-set! ll "tail_idx" idx) (begin (set! ll (make-hash)) (hash-set! ll "tail_idx" idx)))
) (let ()
(insert_after_node ll (if ll (hash-ref ll "tail_idx" #f) #f) idx)
))
))
)
(define (insert ll value)
  (let/ec _return (begin
(define nodes (if ll (hash-ref ll "nodes" #f) #f))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" value) (hash-set! h "prev_index" (- 1)) (hash-set! h "next_index" (- 1)) h)))
(define idx (- (cond [(not nodes) 0] [(string? nodes) (string-length nodes)] [(vector? nodes) (vector-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]) 1))
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
(if (equal? (if ll (hash-ref ll "head_idx" #f) #f) (- 1)) (let ()
(if (hash? ll) (hash-set! ll "head_idx" idx) (begin (set! ll (make-hash)) (hash-set! ll "head_idx" idx)))
(if (hash? ll) (hash-set! ll "tail_idx" idx) (begin (set! ll (make-hash)) (hash-set! ll "tail_idx" idx)))
) (let ()
(insert_after_node ll (if ll (hash-ref ll "tail_idx" #f) #f) idx)
))
))
)
(define (insert_at_position ll position value)
  (let/ec _return (begin
(define current (if ll (hash-ref ll "head_idx" #f) #f))
(define current_pos 1)
(let/ec _break (let loop ()
  (if (not (equal? current (- 1))) (begin
    (let/ec _cont
      (if (equal? current_pos position) (let ()
(define nodes (if ll (hash-ref ll "nodes" #f) #f))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" value) (hash-set! h "prev_index" (- 1)) (hash-set! h "next_index" (- 1)) h)))
(define new_idx (- (cond [(not nodes) 0] [(string? nodes) (string-length nodes)] [(vector? nodes) (vector-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]) 1))
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
(insert_before_node ll current new_idx)
(_return void)
) (void))
      (define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) current #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int current))) #f))
      (set! current (if node (hash-ref node "next_index" #f) #f))
      (set! current_pos (let ([__l current_pos] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(insert ll value)
))
)
(define (get_node ll item)
  (let/ec _return (begin
(define current (if ll (hash-ref ll "head_idx" #f) #f))
(let/ec _break (let loop ()
  (if (not (equal? current (- 1))) (begin
    (let/ec _cont
      (define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) current #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int current))) #f))
      (if (equal? (if node (hash-ref node "data" #f) #f) item) (let ()
(_return current)
) (void))
      (set! current (if node (hash-ref node "next_index" #f) #f))
    )
    (loop)) (void))))
(_return (- 1))
))
)
(define (remove_node_pointers ll idx)
  (let/ec _return (begin
(define nodes (if ll (hash-ref ll "nodes" #f) #f))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (safe-index nodes (int idx))) #f))
(define nxt (if node (hash-ref node "next_index" #f) #f))
(define p (if node (hash-ref node "prev_index" #f) #f))
(if (not (equal? nxt (- 1))) (let ()
(define nxt_node (if nodes (if (hash? nodes) (hash-ref nodes nxt #f) (safe-index nodes (int nxt))) #f))
(if (hash? nxt_node) (hash-set! nxt_node "prev_index" p) (begin (set! nxt_node (make-hash)) (hash-set! nxt_node "prev_index" p)))
(vector-set! nodes (int nxt) nxt_node)
) (void))
(if (not (equal? p (- 1))) (let ()
(define prev_node (if nodes (if (hash? nodes) (hash-ref nodes p #f) (safe-index nodes (int p))) #f))
(if (hash? prev_node) (hash-set! prev_node "next_index" nxt) (begin (set! prev_node (make-hash)) (hash-set! prev_node "next_index" nxt)))
(vector-set! nodes (int p) prev_node)
) (void))
(if (hash? node) (hash-set! node "next_index" (- 1)) (begin (set! node (make-hash)) (hash-set! node "next_index" (- 1))))
(if (hash? node) (hash-set! node "prev_index" (- 1)) (begin (set! node (make-hash)) (hash-set! node "prev_index" (- 1))))
(vector-set! nodes (int idx) node)
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
))
)
(define (delete_value ll value)
  (let/ec _return (begin
(define idx (get_node ll value))
(if (equal? idx (- 1)) (let ()
(_return void)
) (void))
(if (equal? idx (if ll (hash-ref ll "head_idx" #f) #f)) (let ()
(define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) idx #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int idx))) #f))
(if (hash? ll) (hash-set! ll "head_idx" (if node (hash-ref node "next_index" #f) #f)) (begin (set! ll (make-hash)) (hash-set! ll "head_idx" (if node (hash-ref node "next_index" #f) #f))))
) (void))
(if (equal? idx (if ll (hash-ref ll "tail_idx" #f) #f)) (let ()
(define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) idx #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int idx))) #f))
(if (hash? ll) (hash-set! ll "tail_idx" (if node (hash-ref node "prev_index" #f) #f)) (begin (set! ll (make-hash)) (hash-set! ll "tail_idx" (if node (hash-ref node "prev_index" #f) #f))))
) (void))
(remove_node_pointers ll idx)
))
)
(define (contains ll value)
  (let/ec _return (begin
(_return (not (equal? (get_node ll value) (- 1))))
))
)
(define (is_empty ll)
  (let/ec _return (begin
(_return (equal? (if ll (hash-ref ll "head_idx" #f) #f) (- 1)))
))
)
(define (to_string ll)
  (let/ec _return (begin
(define res "")
(define first #t)
(define current (if ll (hash-ref ll "head_idx" #f) #f))
(let/ec _break (let loop ()
  (if (not (equal? current (- 1))) (begin
    (let/ec _cont
      (define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) current #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int current))) #f))
      (define val (to-string (if node (hash-ref node "data" #f) #f)))
      (if first (let ()
(set! res val)
(set! first #f)
) (let ()
(set! res (string-append (string-append res " ") val))
))
      (set! current (if node (hash-ref node "next_index" #f) #f))
    )
    (loop)) (void))))
(_return res)
))
)
(define (print_list ll)
  (let/ec _return (begin
(define current (if ll (hash-ref ll "head_idx" #f) #f))
(let/ec _break (let loop ()
  (if (not (equal? current (- 1))) (begin
    (let/ec _cont
      (define node (if (if ll (hash-ref ll "nodes" #f) #f) (if (hash? (if ll (hash-ref ll "nodes" #f) #f)) (hash-ref (if ll (hash-ref ll "nodes" #f) #f) current #f) (safe-index (if ll (hash-ref ll "nodes" #f) #f) (int current))) #f))
      (displayln (to-string (to-string (if node (hash-ref node "data" #f) #f))))
      (set! current (if node (hash-ref node "next_index" #f) #f))
    )
    (loop)) (void))))
))
)
(define (main)
  (let/ec _return (begin
(define ll (empty_list))
(displayln (to-string (to-string (get_head_data ll))))
(displayln (to-string (to-string (get_tail_data ll))))
(displayln (to-string (to-string (is_empty ll))))
(insert ll 10)
(displayln (to-string (to-string (get_head_data ll))))
(displayln (to-string (to-string (get_tail_data ll))))
(insert_at_position ll 3 20)
(displayln (to-string (to-string (get_head_data ll))))
(displayln (to-string (to-string (get_tail_data ll))))
(define nodes (if ll (hash-ref ll "nodes" #f) #f))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" 1000) (hash-set! h "prev_index" (- 1)) (hash-set! h "next_index" (- 1)) h)))
(define idx_head (- (cond [(not nodes) 0] [(string? nodes) (string-length nodes)] [(vector? nodes) (vector-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]) 1))
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
(set_head ll idx_head)
(set! nodes (if ll (hash-ref ll "nodes" #f) #f))
(set! nodes (appendv (or nodes (vector)) (let ([h (make-hash)]) (hash-set! h "data" 2000) (hash-set! h "prev_index" (- 1)) (hash-set! h "next_index" (- 1)) h)))
(define idx_tail (- (cond [(not nodes) 0] [(string? nodes) (string-length nodes)] [(vector? nodes) (vector-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]) 1))
(if (hash? ll) (hash-set! ll "nodes" nodes) (begin (set! ll (make-hash)) (hash-set! ll "nodes" nodes)))
(set_tail ll idx_tail)
(print_list ll)
(displayln (to-string (to-string (is_empty ll))))
(print_list ll)
(displayln (to-string (to-string (contains ll 10))))
(delete_value ll 10)
(displayln (to-string (to-string (contains ll 10))))
(delete_value ll 2000)
(displayln (to-string (to-string (get_tail_data ll))))
(delete_value ll 1000)
(displayln (to-string (to-string (get_tail_data ll))))
(displayln (to-string (to-string (get_head_data ll))))
(print_list ll)
(delete_value ll 20)
(print_list ll)
(define i 1)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 10]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (insert ll i)
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(print_list ll)
(define ll2 (empty_list))
(insert_at_position ll2 1 10)
(displayln (to-string (to_string ll2)))
(insert_at_position ll2 2 20)
(displayln (to-string (to_string ll2)))
(insert_at_position ll2 1 30)
(displayln (to-string (to_string ll2)))
(insert_at_position ll2 3 40)
(displayln (to-string (to_string ll2)))
(insert_at_position ll2 5 50)
(displayln (to-string (to_string ll2)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
