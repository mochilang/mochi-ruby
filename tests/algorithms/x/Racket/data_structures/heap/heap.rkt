;; Generated by Mochi 0.10.72 on 2025-08-24 23:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/heap")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (parent_index child_idx)
  (let/ec _return (begin
(if (let ([__l child_idx] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(_return (floor (/ (- child_idx 1) 2)))
) (void))
(_return (- 1))
))
)
(define (left_child_idx parent_idx)
  (let/ec _return (begin
(_return (let ([__l (* 2 parent_idx)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (right_child_idx parent_idx)
  (let/ec _return (begin
(_return (let ([__l (* 2 parent_idx)] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (max_heapify h heap_size index)
  (let/ec _return (begin
(define largest index)
(define left (left_child_idx index))
(define right (right_child_idx index))
(if (and (let ([__l left] [__r heap_size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ([__l (if h (if (hash? h) (hash-ref h left #f) (safe-index h (int left))) #f)] [__r (if h (if (hash? h) (hash-ref h largest #f) (safe-index h (int largest))) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (let ()
(set! largest left)
) (void))
(if (and (let ([__l right] [__r heap_size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ([__l (if h (if (hash? h) (hash-ref h right #f) (safe-index h (int right))) #f)] [__r (if h (if (hash? h) (hash-ref h largest #f) (safe-index h (int largest))) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (let ()
(set! largest right)
) (void))
(if (not (equal? largest index)) (let ()
(define temp (if h (if (hash? h) (hash-ref h index #f) (safe-index h (int index))) #f))
(vector-set! h (int index) (if h (if (hash? h) (hash-ref h largest #f) (safe-index h (int largest))) #f))
(vector-set! h (int largest) temp)
(max_heapify h heap_size largest)
) (void))
))
)
(define (build_max_heap h)
  (let/ec _return (begin
(define heap_size (cond [(not h) 0] [(string? h) (string-length h)] [(vector? h) (vector-length h)] [(hash? h) (hash-count h)] [else (length h)]))
(define i (- (floor (/ heap_size 2)) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (max_heapify h heap_size i)
      (set! i (- i 1))
    )
    (loop)) (void))))
(_return heap_size)
))
)
(define (extract_max h heap_size)
  (let/ec _return (begin
(define max_value (if h (if (hash? h) (hash-ref h 0 #f) (safe-index h (int 0))) #f))
(vector-set! h (int 0) (if h (if (hash? h) (hash-ref h (- heap_size 1) #f) (safe-index h (int (- heap_size 1)))) #f))
(max_heapify h (- heap_size 1) 0)
(_return max_value)
))
)
(define (insert h heap_size value)
  (let/ec _return (begin
(if (let ([__l heap_size] [__r (cond [(not h) 0] [(string? h) (string-length h)] [(vector? h) (vector-length h)] [(hash? h) (hash-count h)] [else (length h)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(vector-set! h (int heap_size) value)
) (let ()
(set! h (appendv (or h (vector)) (vector value)))
))
(set! heap_size (let ([__l heap_size] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(define idx (floor (/ (- heap_size 1) 2)))
(let/ec _break (let loop ()
  (if (let ([__l idx] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (max_heapify h heap_size idx)
      (set! idx (floor (/ (- idx 1) 2)))
    )
    (loop)) (void))))
(_return heap_size)
))
)
(define (heap_sort h heap_size)
  (let/ec _return (begin
(define size heap_size)
(define j (- size 1))
(let/ec _break (let loop ()
  (if (let ([__l j] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (begin
    (let/ec _cont
      (define temp (if h (if (hash? h) (hash-ref h 0 #f) (safe-index h (int 0))) #f))
      (vector-set! h (int 0) (if h (if (hash? h) (hash-ref h j #f) (safe-index h (int j))) #f))
      (vector-set! h (int j) temp)
      (set! size (- size 1))
      (max_heapify h size 0)
      (set! j (- j 1))
    )
    (loop)) (void))))
))
)
(define (heap_to_string h heap_size)
  (let/ec _return (begin
(define s "[")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r heap_size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! s (string-append s (to-string (if h (if (hash? h) (hash-ref h i #f) (safe-index h (int i))) #f))))
      (if (let ([__l i] [__r (- heap_size 1)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! s (string-append s ", "))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! s (string-append s "]"))
(_return s)
))
)
(define heap (vector 103.0 9.0 1.0 7.0 11.0 15.0 25.0 201.0 209.0 107.0 5.0))
(define size (build_max_heap heap))
(define m 0.0)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (heap_to_string heap size)))
(set! m (extract_max heap size))
(set! size (- size 1))
(displayln (to-string (to-string m)))
(displayln (to-string (heap_to_string heap size)))
(set! size (insert heap size 100.0))
(displayln (to-string (heap_to_string heap size)))
(heap_sort heap size)
(displayln (to-string (heap_to_string heap size)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
