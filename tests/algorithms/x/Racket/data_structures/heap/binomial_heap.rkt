;; Generated by Mochi 0.10.59 on 2025-08-06 23:22 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (new_heap)
  (let/ec _return (begin
(_return (hash "data" (list)))
))
)
(define (swap data i j)
  (let/ec _return (begin
(define tmp (if data (if (hash? data) (hash-ref data i #f) (list-ref data (int i))) #f))
(set! data (list-set data (int i) (if data (if (hash? data) (hash-ref data j #f) (list-ref data (int j))) #f)))
(set! data (list-set data (int j) tmp))
))
)
(define (sift_up data idx)
  (let/ec _return (begin
(define i idx)
(let/ec _break (let loop ()
  (if (> i 0) (let/ec _cont
    (define parent (quotient (- i 1) 2))
    (if (<= (if data (if (hash? data) (hash-ref data parent #f) (list-ref data (int parent))) #f) (if data (if (hash? data) (hash-ref data i #f) (list-ref data (int i))) #f)) (let ()
(_break)
) (void))
    (swap data parent i)
    (set! i parent)
    (loop)) (void))))
))
)
(define (sift_down data idx)
  (let/ec _return (begin
(define i idx)
(define n (cond [(string? data) (string-length data)] [(hash? data) (hash-count data)] [else (length data)]))
(let/ec _break (let loop ()
  (if #t (let/ec _cont
    (define left (let ([__l (* 2 i)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (define right (let ([__l left] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (define smallest i)
    (if (and (< left n) (< (if data (if (hash? data) (hash-ref data left #f) (list-ref data (int left))) #f) (if data (if (hash? data) (hash-ref data smallest #f) (list-ref data (int smallest))) #f))) (let ()
(set! smallest left)
) (void))
    (if (and (< right n) (< (if data (if (hash? data) (hash-ref data right #f) (list-ref data (int right))) #f) (if data (if (hash? data) (hash-ref data smallest #f) (list-ref data (int smallest))) #f))) (let ()
(set! smallest right)
) (void))
    (if (equal? smallest i) (let ()
(_break)
) (void))
    (swap data i smallest)
    (set! i smallest)
    (loop)) (void))))
))
)
(define (insert heap v)
  (let/ec _return (begin
(define d (if heap (hash-ref heap "data" #f) #f))
(set! d (append d (list v)))
(sift_up d (- (cond [(string? d) (string-length d)] [(hash? d) (hash-count d)] [else (length d)]) 1))
(_return (hash "data" d))
))
)
(define (peek heap)
  (let/ec _return (begin
(_return (if (if heap (hash-ref heap "data" #f) #f) (if (hash? (if heap (hash-ref heap "data" #f) #f)) (hash-ref (if heap (hash-ref heap "data" #f) #f) 0 #f) (list-ref (if heap (hash-ref heap "data" #f) #f) (int 0))) #f))
))
)
(define (is_empty heap)
  (let/ec _return (begin
(_return (equal? (cond [(string? (if heap (hash-ref heap "data" #f) #f)) (string-length (if heap (hash-ref heap "data" #f) #f))] [(hash? (if heap (hash-ref heap "data" #f) #f)) (hash-count (if heap (hash-ref heap "data" #f) #f))] [else (length (if heap (hash-ref heap "data" #f) #f))]) 0))
))
)
(define (delete_min heap)
  (let/ec _return (begin
(define d (if heap (hash-ref heap "data" #f) #f))
(define min (if d (if (hash? d) (hash-ref d 0 #f) (list-ref d (int 0))) #f))
(set! d (list-set d (int 0) (if d (if (hash? d) (hash-ref d (- (cond [(string? d) (string-length d)] [(hash? d) (hash-count d)] [else (length d)]) 1) #f) (list-ref d (int (- (cond [(string? d) (string-length d)] [(hash? d) (hash-count d)] [else (length d)]) 1)))) #f)))
(set! d (slice d 0 (- (cond [(string? d) (string-length d)] [(hash? d) (hash-count d)] [else (length d)]) 1)))
(if (> (cond [(string? d) (string-length d)] [(hash? d) (hash-count d)] [else (length d)]) 0) (let ()
(sift_down d 0)
) (void))
(_return (hash "heap" (hash "data" d) "value" min))
))
)
(define (main)
  (let/ec _return (begin
(define h (new_heap))
(set! h (insert h 10))
(set! h (insert h 3))
(set! h (insert h 7))
(displayln (format "~a" (peek h)))
(define d1 (delete_min h))
(set! h (if d1 (hash-ref d1 "heap" #f) #f))
(displayln (format "~a" (if d1 (hash-ref d1 "value" #f) #f)))
(define d2 (delete_min h))
(set! h (if d2 (hash-ref d2 "heap" #f) #f))
(displayln (format "~a" (if d2 (hash-ref d2 "value" #f) #f)))
(define d3 (delete_min h))
(set! h (if d3 (hash-ref d3 "heap" #f) #f))
(displayln (format "~a" (if d3 (hash-ref d3 "value" #f) #f)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
