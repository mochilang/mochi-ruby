;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/heap")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [(vector? x) (format "~a" (vector->list x))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a b)]
    [(list? a) (append a (vector->list b))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (new_heap key)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "arr" (vector)) (hash-set! h "pos_map" (let ([h (make-hash)]) h)) (hash-set! h "size" 0) (hash-set! h "key" key) h))
))
)
(define (parent i)
  (let/ec _return (begin
(if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(_return (floor (/ (- i 1) 2)))
) (void))
(_return (- 1))
))
)
(define (left i size)
  (let/ec _return (begin
(define l (let ([__l (* 2 i)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (let ([__l l] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return l)
) (void))
(_return (- 1))
))
)
(define (right i size)
  (let/ec _return (begin
(define r (let ([__l (* 2 i)] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (let ([__l r] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return r)
) (void))
(_return (- 1))
))
)
(define (swap h i j)
  (let/ec _return (begin
(define arr (if h (hash-ref h "arr" #f) #f))
(define item_i (if (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f) (if (hash? (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f)) (hash-ref (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f) 0 #f) (safe-index (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f) (int 0))) #f))
(define item_j (if (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f) (if (hash? (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f)) (hash-ref (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f) 0 #f) (safe-index (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f) (int 0))) #f))
(define pm (if h (hash-ref h "pos_map" #f) #f))
(if (hash? pm) (hash-set! pm item_i (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! pm (make-hash)) (hash-set! pm item_i (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(if (hash? pm) (hash-set! pm item_j (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! pm (make-hash)) (hash-set! pm item_j (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(if (hash? h) (hash-set! h "pos_map" pm) (begin (set! h (make-hash)) (hash-set! h "pos_map" pm)))
(define tmp (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))
(vector-set! arr (int i) (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f))
(vector-set! arr (int j) tmp)
(if (hash? h) (hash-set! h "arr" arr) (begin (set! h (make-hash)) (hash-set! h "arr" arr)))
))
)
(define (cmp h i j)
  (let/ec _return (begin
(define arr (if h (hash-ref h "arr" #f) #f))
(_return (let ([__l (if (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f) (if (hash? (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f)) (hash-ref (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f) 1 #f) (safe-index (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f) (int 1))) #f)] [__r (if (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f) (if (hash? (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f)) (hash-ref (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f) 1 #f) (safe-index (if arr (if (hash? arr) (hash-ref arr j #f) (safe-index arr (int j))) #f) (int 1))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])))
))
)
(define (get_valid_parent h i)
  (let/ec _return (begin
(define vp i)
(define l (left i (if h (hash-ref h "size" #f) #f)))
(if (and (not (equal? l (- 0 1))) (not (cmp h l vp))) (let ()
(set! vp l)
) (void))
(define r (right i (if h (hash-ref h "size" #f) #f)))
(if (and (not (equal? r (- 0 1))) (not (cmp h r vp))) (let ()
(set! vp r)
) (void))
(_return vp)
))
)
(define (heapify_up h index)
  (let/ec _return (begin
(define idx index)
(define p (parent idx))
(let/ec _break (let loop ()
  (if (and (not (equal? p (- 0 1))) (not (cmp h idx p))) (begin
    (let/ec _cont
      (swap h idx p)
      (set! idx p)
      (set! p (parent p))
    )
    (loop)) (void))))
))
)
(define (heapify_down h index)
  (let/ec _return (begin
(define idx index)
(define vp (get_valid_parent h idx))
(let/ec _break (let loop ()
  (if (not (equal? vp idx)) (begin
    (let/ec _cont
      (swap h idx vp)
      (set! idx vp)
      (set! vp (get_valid_parent h idx))
    )
    (loop)) (void))))
))
)
(define (update_item h item item_value)
  (let/ec _return (begin
(define pm (if h (hash-ref h "pos_map" #f) #f))
(if (equal? (if pm (hash-ref pm item 0) #f) 0) (let ()
(_return void)
) (void))
(define index (- (if pm (hash-ref pm item 0) #f) 1))
(define arr (if h (hash-ref h "arr" #f) #f))
(vector-set! arr (int index) (vector item ((if h (hash-ref h "key" #f) #f) item_value)))
(if (hash? h) (hash-set! h "arr" arr) (begin (set! h (make-hash)) (hash-set! h "arr" arr)))
(if (hash? h) (hash-set! h "pos_map" pm) (begin (set! h (make-hash)) (hash-set! h "pos_map" pm)))
(heapify_up h index)
(heapify_down h index)
))
)
(define (delete_item h item)
  (let/ec _return (begin
(define pm (if h (hash-ref h "pos_map" #f) #f))
(if (equal? (if pm (hash-ref pm item 0) #f) 0) (let ()
(_return void)
) (void))
(define index (- (if pm (hash-ref pm item 0) #f) 1))
(if (hash? pm) (hash-set! pm item 0) (begin (set! pm (make-hash)) (hash-set! pm item 0)))
(define arr (if h (hash-ref h "arr" #f) #f))
(define last_index (- (if h (hash-ref h "size" #f) #f) 1))
(if (not (equal? index last_index)) (let ()
(vector-set! arr (int index) (if arr (if (hash? arr) (hash-ref arr last_index #f) (safe-index arr (int last_index))) #f))
(define moved (if (if arr (if (hash? arr) (hash-ref arr index #f) (safe-index arr (int index))) #f) (if (hash? (if arr (if (hash? arr) (hash-ref arr index #f) (safe-index arr (int index))) #f)) (hash-ref (if arr (if (hash? arr) (hash-ref arr index #f) (safe-index arr (int index))) #f) 0 #f) (safe-index (if arr (if (hash? arr) (hash-ref arr index #f) (safe-index arr (int index))) #f) (int 0))) #f))
(if (hash? pm) (hash-set! pm moved (let ([__l index] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! pm (make-hash)) (hash-set! pm moved (let ([__l index] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
) (void))
(if (hash? h) (hash-set! h "size" (- (if h (hash-ref h "size" #f) #f) 1)) (begin (set! h (make-hash)) (hash-set! h "size" (- (if h (hash-ref h "size" #f) #f) 1))))
(if (hash? h) (hash-set! h "arr" arr) (begin (set! h (make-hash)) (hash-set! h "arr" arr)))
(if (hash? h) (hash-set! h "pos_map" pm) (begin (set! h (make-hash)) (hash-set! h "pos_map" pm)))
(if (let ([__l (if h (hash-ref h "size" #f) #f)] [__r index]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(heapify_up h index)
(heapify_down h index)
) (void))
))
)
(define (insert_item h item item_value)
  (let/ec _return (begin
(define arr (if h (hash-ref h "arr" #f) #f))
(define arr_len (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]))
(if (equal? arr_len (if h (hash-ref h "size" #f) #f)) (let ()
(set! arr (appendv (or arr (vector)) (vector (vector item ((if h (hash-ref h "key" #f) #f) item_value)))))
) (let ()
(vector-set! arr (int (if h (hash-ref h "size" #f) #f)) (vector item ((if h (hash-ref h "key" #f) #f) item_value)))
))
(define pm (if h (hash-ref h "pos_map" #f) #f))
(if (hash? pm) (hash-set! pm item (let ([__l (if h (hash-ref h "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! pm (make-hash)) (hash-set! pm item (let ([__l (if h (hash-ref h "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(if (hash? h) (hash-set! h "size" (let ([__l (if h (hash-ref h "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! h (make-hash)) (hash-set! h "size" (let ([__l (if h (hash-ref h "size" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(if (hash? h) (hash-set! h "arr" arr) (begin (set! h (make-hash)) (hash-set! h "arr" arr)))
(if (hash? h) (hash-set! h "pos_map" pm) (begin (set! h (make-hash)) (hash-set! h "pos_map" pm)))
(heapify_up h (- (if h (hash-ref h "size" #f) #f) 1))
))
)
(define (get_top h)
  (let/ec _return (begin
(define arr (if h (hash-ref h "arr" #f) #f))
(if (let ([__l (if h (hash-ref h "size" #f) #f)] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(_return (if arr (if (hash? arr) (hash-ref arr 0 #f) (safe-index arr (int 0))) #f))
) (void))
(_return (vector))
))
)
(define (extract_top h)
  (let/ec _return (begin
(define top (get_top h))
(if (let ([__l (cond [(not top) 0] [(string? top) (string-length top)] [(vector? top) (vector-length top)] [(hash? top) (hash-count top)] [else (length top)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(delete_item h (if top (if (hash? top) (hash-ref top 0 #f) (safe-index top (int 0))) #f))
) (void))
(_return top)
))
)
(define (identity x)
  (let/ec _return (begin
(_return x)
))
)
(define (negate x)
  (let/ec _return (begin
(_return (- 0 x))
))
)
(define h (new_heap identity))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(insert_item h 5 34)
(insert_item h 6 31)
(insert_item h 7 37)
(displayln (to-string (get_top h)))
(displayln (to-string (extract_top h)))
(displayln (to-string (extract_top h)))
(displayln (to-string (extract_top h)))
(set! h (new_heap negate))
(insert_item h 5 34)
(insert_item h 6 31)
(insert_item h 7 37)
(displayln (to-string (get_top h)))
(displayln (to-string (extract_top h)))
(displayln (to-string (extract_top h)))
(displayln (to-string (extract_top h)))
(insert_item h 8 45)
(insert_item h 9 40)
(insert_item h 10 50)
(displayln (to-string (get_top h)))
(update_item h 10 30)
(displayln (to-string (get_top h)))
(delete_item h 10)
(displayln (to-string (get_top h)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
