;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define A (list))
(define N 0)
(define st (list))
(define (left_child idx)
  (let/ec _return (begin
(_return (* idx 2))
))
)
(define (right_child idx)
  (let/ec _return (begin
(_return (let ([__l (* idx 2)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (build idx left right)
  (let/ec _return (begin
(if (equal? left right) (let ()
(set! st (list-set st (int idx) (if A (if (hash? A) (hash-ref A left #f) (list-ref A (int left))) #f)))
) (let ()
(define mid (quotient (let ([__l left] [__r right]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2))
(build (left_child idx) left mid)
(build (right_child idx) (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) right)
(define left_val (if st (if (hash? st) (hash-ref st (left_child idx) #f) (list-ref st (int (left_child idx)))) #f))
(define right_val (if st (if (hash? st) (hash-ref st (right_child idx) #f) (list-ref st (int (right_child idx)))) #f))
(set! st (list-set st (int idx) (if (> left_val right_val) left_val right_val)))
))
))
)
(define (update_recursive idx left right a b val)
  (let/ec _return (begin
(if (or (< right a) (> left b)) (let ()
(_return #t)
) (void))
(if (equal? left right) (let ()
(set! st (list-set st (int idx) val))
(_return #t)
) (void))
(define mid (quotient (let ([__l left] [__r right]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2))
(update_recursive (left_child idx) left mid a b val)
(update_recursive (right_child idx) (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) right a b val)
(define left_val (if st (if (hash? st) (hash-ref st (left_child idx) #f) (list-ref st (int (left_child idx)))) #f))
(define right_val (if st (if (hash? st) (hash-ref st (right_child idx) #f) (list-ref st (int (right_child idx)))) #f))
(set! st (list-set st (int idx) (if (> left_val right_val) left_val right_val)))
(_return #t)
))
)
(define (update a b val)
  (let/ec _return (begin
(_return (update_recursive 1 0 (- N 1) (- a 1) (- b 1) val))
))
)
(define NEG_INF (- 1000000000))
(define (query_recursive idx left right a b)
  (let/ec _return (begin
(if (or (< right a) (> left b)) (let ()
(_return NEG_INF)
) (void))
(if (and (>= left a) (<= right b)) (let ()
(_return (if st (if (hash? st) (hash-ref st idx #f) (list-ref st (int idx))) #f))
) (void))
(define mid (quotient (let ([__l left] [__r right]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2))
(define q1 (query_recursive (left_child idx) left mid a b))
(define q2 (query_recursive (right_child idx) (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) right a b))
(_return (if (> q1 q2) q1 q2))
))
)
(define (query a b)
  (let/ec _return (begin
(_return (query_recursive 1 0 (- N 1) (- a 1) (- b 1)))
))
)
(define (show_data)
  (let/ec _return (begin
(define i 0)
(define show_list (list))
(let/ec _break (let loop ()
  (if (< i N) (let/ec _cont
    (set! show_list (append show_list (list (query (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln show_list)
))
)
(define (main)
  (let/ec _return (begin
(set! A (list 1 2 (- 4) 7 3 (- 5) 6 11 (- 20) 9 14 15 5 2 (- 8)))
(set! N (cond [(string? A) (string-length A)] [(hash? A) (hash-count A)] [else (length A)]))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (* 4 N)) (let/ec _cont
    (set! st (append st (list 0)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(if (> N 0) (let ()
(build 1 0 (- N 1))
) (void))
(displayln (query 4 6))
(displayln (query 7 11))
(displayln (query 7 12))
(update 1 3 111)
(displayln (query 1 15))
(update 7 8 235)
(show_data)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
