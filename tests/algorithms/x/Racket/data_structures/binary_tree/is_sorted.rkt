;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define NONE (- 0 1))
(define (inorder tree index)
  (let/ec _return (begin
(define res (list))
(if (equal? index NONE) (let ()
(_return res)
) (void))
(define left_idx (if (if tree (hash-ref tree "left" #f) #f) (if (hash? (if tree (hash-ref tree "left" #f) #f)) (hash-ref (if tree (hash-ref tree "left" #f) #f) index #f) (list-ref (if tree (hash-ref tree "left" #f) #f) (int index))) #f))
(if (not (equal? left_idx NONE)) (let ()
(set! res (append res (inorder tree left_idx)))
) (void))
(set! res (append res (list (if (if tree (hash-ref tree "data" #f) #f) (if (hash? (if tree (hash-ref tree "data" #f) #f)) (hash-ref (if tree (hash-ref tree "data" #f) #f) index #f) (list-ref (if tree (hash-ref tree "data" #f) #f) (int index))) #f))))
(define right_idx (if (if tree (hash-ref tree "right" #f) #f) (if (hash? (if tree (hash-ref tree "right" #f) #f)) (hash-ref (if tree (hash-ref tree "right" #f) #f) index #f) (list-ref (if tree (hash-ref tree "right" #f) #f) (int index))) #f))
(if (not (equal? right_idx NONE)) (let ()
(set! res (append res (inorder tree right_idx)))
) (void))
(_return res)
))
)
(define (is_sorted tree index)
  (let/ec _return (begin
(if (equal? index NONE) (let ()
(_return #t)
) (void))
(define left_idx (if (if tree (hash-ref tree "left" #f) #f) (if (hash? (if tree (hash-ref tree "left" #f) #f)) (hash-ref (if tree (hash-ref tree "left" #f) #f) index #f) (list-ref (if tree (hash-ref tree "left" #f) #f) (int index))) #f))
(if (not (equal? left_idx NONE)) (let ()
(if (< (if (if tree (hash-ref tree "data" #f) #f) (if (hash? (if tree (hash-ref tree "data" #f) #f)) (hash-ref (if tree (hash-ref tree "data" #f) #f) index #f) (list-ref (if tree (hash-ref tree "data" #f) #f) (int index))) #f) (if (if tree (hash-ref tree "data" #f) #f) (if (hash? (if tree (hash-ref tree "data" #f) #f)) (hash-ref (if tree (hash-ref tree "data" #f) #f) left_idx #f) (list-ref (if tree (hash-ref tree "data" #f) #f) (int left_idx))) #f)) (let ()
(_return #f)
) (void))
(if (not (is_sorted tree left_idx)) (let ()
(_return #f)
) (void))
) (void))
(define right_idx (if (if tree (hash-ref tree "right" #f) #f) (if (hash? (if tree (hash-ref tree "right" #f) #f)) (hash-ref (if tree (hash-ref tree "right" #f) #f) index #f) (list-ref (if tree (hash-ref tree "right" #f) #f) (int index))) #f))
(if (not (equal? right_idx NONE)) (let ()
(if (> (if (if tree (hash-ref tree "data" #f) #f) (if (hash? (if tree (hash-ref tree "data" #f) #f)) (hash-ref (if tree (hash-ref tree "data" #f) #f) index #f) (list-ref (if tree (hash-ref tree "data" #f) #f) (int index))) #f) (if (if tree (hash-ref tree "data" #f) #f) (if (hash? (if tree (hash-ref tree "data" #f) #f)) (hash-ref (if tree (hash-ref tree "data" #f) #f) right_idx #f) (list-ref (if tree (hash-ref tree "data" #f) #f) (int right_idx))) #f)) (let ()
(_return #f)
) (void))
(if (not (is_sorted tree right_idx)) (let ()
(_return #f)
) (void))
) (void))
(_return #t)
))
)
(define tree1 (hash "data" (list 2.1 2.0 2.2) "left" (list 1 NONE NONE) "right" (list 2 NONE NONE)))
(define tree2 (hash "data" (list 2.1 2.0 2.0) "left" (list 1 NONE NONE) "right" (list 2 NONE NONE)))
(define tree3 (hash "data" (list 2.1 2.0 2.1) "left" (list 1 NONE NONE) "right" (list 2 NONE NONE)))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (string-append (string-append (string-append "Tree " (format "~a" (inorder tree1 0))) " is sorted: ") (format "~a" (is_sorted tree1 0))))
(displayln (string-append (string-append (string-append "Tree " (format "~a" (inorder tree2 0))) " is sorted: ") (format "~a" (is_sorted tree2 0))))
(displayln (string-append (string-append (string-append "Tree " (format "~a" (inorder tree3 0))) " is sorted: ") (format "~a" (is_sorted tree3 0))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
