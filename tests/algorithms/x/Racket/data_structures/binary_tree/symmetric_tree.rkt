;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (make_symmetric_tree)
  (let/ec _return (begin
(_return (list (list 1 1 2) (list 2 3 4) (list 2 5 6) (list 3 (- 1) (- 1)) (list 4 (- 1) (- 1)) (list 4 (- 1) (- 1)) (list 3 (- 1) (- 1))))
))
)
(define (make_asymmetric_tree)
  (let/ec _return (begin
(_return (list (list 1 1 2) (list 2 3 4) (list 2 5 6) (list 3 (- 1) (- 1)) (list 4 (- 1) (- 1)) (list 3 (- 1) (- 1)) (list 4 (- 1) (- 1))))
))
)
(define (is_symmetric_tree tree)
  (let/ec _return (begin
(define stack (list (if (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f) (if (hash? (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f)) (hash-ref (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f) 1 #f) (list-ref (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f) (int 1))) #f) (if (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f) (if (hash? (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f)) (hash-ref (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f) 2 #f) (list-ref (if tree (if (hash? tree) (hash-ref tree 0 #f) (list-ref tree (int 0))) #f) (int 2))) #f)))
(let/ec _break (let loop ()
  (if (>= (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2) (let/ec _cont
    (define left (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2)))) #f))
    (define right (if stack (if (hash? stack) (hash-ref stack (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1) #f) (list-ref stack (int (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1)))) #f))
    (set! stack (slice stack 0 (- (cond [(string? stack) (string-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 2)))
    (if (and (equal? left (- 1)) (equal? right (- 1))) (let ()
(_cont)
) (void))
    (if (or (equal? left (- 1)) (equal? right (- 1))) (let ()
(_return #f)
) (void))
    (define lnode (if tree (if (hash? tree) (hash-ref tree left #f) (list-ref tree (int left))) #f))
    (define rnode (if tree (if (hash? tree) (hash-ref tree right #f) (list-ref tree (int right))) #f))
    (if (not (equal? (if lnode (if (hash? lnode) (hash-ref lnode 0 #f) (list-ref lnode (int 0))) #f) (if rnode (if (hash? rnode) (hash-ref rnode 0 #f) (list-ref rnode (int 0))) #f))) (let ()
(_return #f)
) (void))
    (set! stack (append stack (list (if lnode (if (hash? lnode) (hash-ref lnode 1 #f) (list-ref lnode (int 1))) #f))))
    (set! stack (append stack (list (if rnode (if (hash? rnode) (hash-ref rnode 2 #f) (list-ref rnode (int 2))) #f))))
    (set! stack (append stack (list (if lnode (if (hash? lnode) (hash-ref lnode 2 #f) (list-ref lnode (int 2))) #f))))
    (set! stack (append stack (list (if rnode (if (hash? rnode) (hash-ref rnode 1 #f) (list-ref rnode (int 1))) #f))))
    (loop)) (void))))
(_return #t)
))
)
(define symmetric_tree (make_symmetric_tree))
(define asymmetric_tree (make_asymmetric_tree))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (is_symmetric_tree symmetric_tree)))
(displayln (format "~a" (is_symmetric_tree asymmetric_tree)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
