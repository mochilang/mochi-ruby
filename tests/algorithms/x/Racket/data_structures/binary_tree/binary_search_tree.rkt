;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (create_node value)
  (let/ec _return (begin
(_return (list value #f #f))
))
)
(define (insert node value)
  (let/ec _return (begin
(if (not node) (let ()
(_return (create_node value))
) (void))
(if (< value (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f)) (let ()
(set! node (list-set node (int 1) (insert (if node (if (hash? node) (hash-ref node 1 #f) (list-ref node (int 1))) #f) value)))
) (let ()
(if (> value (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f)) (let ()
(set! node (list-set node (int 2) (insert (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f) value)))
) (void))
))
(_return node)
))
)
(define (search node value)
  (let/ec _return (begin
(if (not node) (let ()
(_return #f)
) (void))
(if (equal? value (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f)) (let ()
(_return #t)
) (void))
(if (< value (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f)) (let ()
(_return (search (if node (if (hash? node) (hash-ref node 1 #f) (list-ref node (int 1))) #f) value))
) (void))
(_return (search (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f) value))
))
)
(define (inorder node acc)
  (let/ec _return (begin
(if (not node) (let ()
(_return acc)
) (void))
(define left_acc (inorder (if node (if (hash? node) (hash-ref node 1 #f) (list-ref node (int 1))) #f) acc))
(define with_node (append left_acc (list (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f))))
(_return (inorder (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f) with_node))
))
)
(define (find_min node)
  (let/ec _return (begin
(define current node)
(let/ec _break (let loop ()
  (if (if current (if (hash? current) (hash-ref current 1 #f) (list-ref current (int 1))) #f) (let/ec _cont
    (set! current (if current (if (hash? current) (hash-ref current 1 #f) (list-ref current (int 1))) #f))
    (loop)) (void))))
(_return (if current (if (hash? current) (hash-ref current 0 #f) (list-ref current (int 0))) #f))
))
)
(define (find_max node)
  (let/ec _return (begin
(define current node)
(let/ec _break (let loop ()
  (if (if current (if (hash? current) (hash-ref current 2 #f) (list-ref current (int 2))) #f) (let/ec _cont
    (set! current (if current (if (hash? current) (hash-ref current 2 #f) (list-ref current (int 2))) #f))
    (loop)) (void))))
(_return (if current (if (hash? current) (hash-ref current 0 #f) (list-ref current (int 0))) #f))
))
)
(define (delete node value)
  (let/ec _return (begin
(if (not node) (let ()
(_return #f)
) (void))
(if (< value (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f)) (let ()
(set! node (list-set node (int 1) (delete (if node (if (hash? node) (hash-ref node 1 #f) (list-ref node (int 1))) #f) value)))
) (let ()
(if (> value (if node (if (hash? node) (hash-ref node 0 #f) (list-ref node (int 0))) #f)) (let ()
(set! node (list-set node (int 2) (delete (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f) value)))
) (let ()
(if (not (if node (if (hash? node) (hash-ref node 1 #f) (list-ref node (int 1))) #f)) (let ()
(_return (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f))
) (void))
(if (not (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f)) (let ()
(_return (if node (if (hash? node) (hash-ref node 1 #f) (list-ref node (int 1))) #f))
) (void))
(define min_val (find_min (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f)))
(set! node (list-set node (int 0) min_val))
(set! node (list-set node (int 2) (delete (if node (if (hash? node) (hash-ref node 2 #f) (list-ref node (int 2))) #f) min_val)))
))
))
(_return node)
))
)
(define (main)
  (let/ec _return (begin
(define root #f)
(define nums (list 8 3 6 1 10 14 13 4 7))
(let/ec _break (for ([v nums])
  (let/ec _cont
(set! root (insert root v))
  )))
(displayln (format "~a" (inorder root (list))))
(displayln (search root 6))
(displayln (search root 20))
(displayln (find_min root))
(displayln (find_max root))
(set! root (delete root 6))
(displayln (format "~a" (inorder root (list))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
