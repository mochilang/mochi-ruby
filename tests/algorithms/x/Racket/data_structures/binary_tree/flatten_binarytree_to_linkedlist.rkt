;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define node_data (list 0))
(define left_child (list 0))
(define right_child (list 0))
(define (new_node value)
  (let/ec _return (begin
(set! node_data (append node_data (list value)))
(set! left_child (append left_child (list 0)))
(set! right_child (append right_child (list 0)))
(_return (- (cond [(string? node_data) (string-length node_data)] [(hash? node_data) (hash-count node_data)] [else (length node_data)]) 1))
))
)
(define (build_tree)
  (let/ec _return (begin
(define root (new_node 1))
(define n2 (new_node 2))
(define n5 (new_node 5))
(define n3 (new_node 3))
(define n4 (new_node 4))
(define n6 (new_node 6))
(set! left_child (list-set left_child (int root) n2))
(set! right_child (list-set right_child (int root) n5))
(set! left_child (list-set left_child (int n2) n3))
(set! right_child (list-set right_child (int n2) n4))
(set! right_child (list-set right_child (int n5) n6))
(_return root)
))
)
(define (flatten root)
  (let/ec _return (begin
(if (equal? root 0) (let ()
(_return (list))
) (void))
(define res (list (if node_data (if (hash? node_data) (hash-ref node_data root #f) (list-ref node_data (int root))) #f)))
(define left_vals (flatten (if left_child (if (hash? left_child) (hash-ref left_child root #f) (list-ref left_child (int root))) #f)))
(define right_vals (flatten (if right_child (if (hash? right_child) (hash-ref right_child root #f) (list-ref right_child (int root))) #f)))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? left_vals) (string-length left_vals)] [(hash? left_vals) (hash-count left_vals)] [else (length left_vals)])) (let/ec _cont
    (set! res (append res (list (if left_vals (if (hash? left_vals) (hash-ref left_vals i #f) (list-ref left_vals (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? right_vals) (string-length right_vals)] [(hash? right_vals) (hash-count right_vals)] [else (length right_vals)])) (let/ec _cont
    (set! res (append res (list (if right_vals (if (hash? right_vals) (hash-ref right_vals i #f) (list-ref right_vals (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (display values)
  (let/ec _return (begin
(define s "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? values) (string-length values)] [(hash? values) (hash-count values)] [else (length values)])) (let/ec _cont
    (if (equal? i 0) (let ()
(set! s (format "~a" (if values (if (hash? values) (hash-ref values i #f) (list-ref values (int i))) #f)))
) (let ()
(set! s (string-append (string-append s " ") (format "~a" (if values (if (hash? values) (hash-ref values i #f) (list-ref values (int i))) #f))))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln s)
))
)
(define root (build_tree))
(define vals (flatten root))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln "Flattened Linked List:")
(display vals)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
