;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (new_node state value)
  (let/ec _return (begin
(set! state (hash-set (or state (hash)) "nodes" (append (if state (hash-ref state "nodes" #f) #f) (list (hash "data" value "left" (- 1) "right" (- 1))))))
(_return (- (cond [(string? (if state (hash-ref state "nodes" #f) #f)) (string-length (if state (hash-ref state "nodes" #f) #f))] [(hash? (if state (hash-ref state "nodes" #f) #f)) (hash-count (if state (hash-ref state "nodes" #f) #f))] [else (length (if state (hash-ref state "nodes" #f) #f))]) 1))
))
)
(define (insert state value)
  (let/ec _return (begin
(if (equal? (if state (hash-ref state "root" #f) #f) (- 1)) (let ()
(set! state (hash-set (or state (hash)) "root" (new_node state value)))
(_return void)
) (void))
(define current (if state (hash-ref state "root" #f) #f))
(define nodes (if state (hash-ref state "nodes" #f) #f))
(let/ec _break (let loop ()
  (if #t (let/ec _cont
    (define node (if nodes (if (hash? nodes) (hash-ref nodes current #f) (list-ref nodes (int current))) #f))
    (if (< value (if node (hash-ref node "data" #f) #f)) (let ()
(if (equal? (if node (hash-ref node "left" #f) #f) (- 1)) (let ()
(set! node (hash-set (or node (hash)) "left" (new_node state value)))
(set! nodes (list-set nodes (int current) node))
(set! state (hash-set (or state (hash)) "nodes" nodes))
(_return void)
) (void))
(set! current (if node (hash-ref node "left" #f) #f))
) (let ()
(if (equal? (if node (hash-ref node "right" #f) #f) (- 1)) (let ()
(set! node (hash-set (or node (hash)) "right" (new_node state value)))
(set! nodes (list-set nodes (int current) node))
(set! state (hash-set (or state (hash)) "nodes" nodes))
(_return void)
) (void))
(set! current (if node (hash-ref node "right" #f) #f))
))
    (loop)) (void))))
))
)
(define (inorder state idx)
  (let/ec _return (begin
(if (equal? idx (- 1)) (let ()
(_return (list))
) (void))
(define node (if (if state (hash-ref state "nodes" #f) #f) (if (hash? (if state (hash-ref state "nodes" #f) #f)) (hash-ref (if state (hash-ref state "nodes" #f) #f) idx #f) (list-ref (if state (hash-ref state "nodes" #f) #f) (int idx))) #f))
(define result (inorder state (if node (hash-ref node "left" #f) #f)))
(set! result (append result (list (if node (hash-ref node "data" #f) #f))))
(define right_part (inorder state (if node (hash-ref node "right" #f) #f)))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? right_part) (string-length right_part)] [(hash? right_part) (hash-count right_part)] [else (length right_part)])) (let/ec _cont
    (set! result (append result (list (if right_part (if (hash? right_part) (hash-ref right_part i #f) (list-ref right_part (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return result)
))
)
(define (make_tree)
  (let/ec _return (begin
(define state (hash "nodes" (list) "root" (- 1)))
(insert state 15)
(insert state 10)
(insert state 25)
(insert state 6)
(insert state 14)
(insert state 20)
(insert state 60)
(_return state)
))
)
(define (main)
  (let/ec _return (begin
(define state (make_tree))
(displayln "Printing values of binary search tree in Inorder Traversal.")
(displayln (inorder state (if state (hash-ref state "root" #f) #f)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
