;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (count_nodes nodes idx)
  (let/ec _return (begin
(if (equal? idx 0) (let ()
(_return 0)
) (void))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (list-ref nodes (int idx))) #f))
(_return (let ([__l (let ([__l (count_nodes nodes (if node (hash-ref node "left" #f) #f))] [__r (count_nodes nodes (if node (hash-ref node "right" #f) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (count_coins nodes idx)
  (let/ec _return (begin
(if (equal? idx 0) (let ()
(_return 0)
) (void))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (list-ref nodes (int idx))) #f))
(_return (let ([__l (let ([__l (count_coins nodes (if node (hash-ref node "left" #f) #f))] [__r (count_coins nodes (if node (hash-ref node "right" #f) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (if node (hash-ref node "data" #f) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define total_moves 0)
(define (iabs x)
  (let/ec _return (begin
(if (< x 0) (let ()
(_return (- x))
) (void))
(_return x)
))
)
(define (dfs nodes idx)
  (let/ec _return (begin
(if (equal? idx 0) (let ()
(_return 0)
) (void))
(define node (if nodes (if (hash? nodes) (hash-ref nodes idx #f) (list-ref nodes (int idx))) #f))
(define left_excess (dfs nodes (if node (hash-ref node "left" #f) #f)))
(define right_excess (dfs nodes (if node (hash-ref node "right" #f) #f)))
(define abs_left (iabs left_excess))
(define abs_right (iabs right_excess))
(set! total_moves (let ([__l (let ([__l total_moves] [__r abs_left]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r abs_right]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_return (- (let ([__l (let ([__l (if node (hash-ref node "data" #f) #f)] [__r left_excess]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r right_excess]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1))
))
)
(define (distribute_coins nodes root)
  (let/ec _return (begin
(if (equal? root 0) (let ()
(_return 0)
) (void))
(if (not (equal? (count_nodes nodes root) (count_coins nodes root))) (let ()
(panic "The nodes number should be same as the number of coins")
) (void))
(set! total_moves 0)
(dfs nodes root)
(_return total_moves)
))
)
(define (main)
  (let/ec _return (begin
(define example1 (list (hash "data" 0 "left" 0 "right" 0) (hash "data" 3 "left" 2 "right" 3) (hash "data" 0 "left" 0 "right" 0) (hash "data" 0 "left" 0 "right" 0)))
(define example2 (list (hash "data" 0 "left" 0 "right" 0) (hash "data" 0 "left" 2 "right" 3) (hash "data" 3 "left" 0 "right" 0) (hash "data" 0 "left" 0 "right" 0)))
(define example3 (list (hash "data" 0 "left" 0 "right" 0) (hash "data" 0 "left" 2 "right" 3) (hash "data" 0 "left" 0 "right" 0) (hash "data" 3 "left" 0 "right" 0)))
(displayln (distribute_coins example1 1))
(displayln (distribute_coins example2 1))
(displayln (distribute_coins example3 1))
(displayln (distribute_coins (list (hash "data" 0 "left" 0 "right" 0)) 0))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
