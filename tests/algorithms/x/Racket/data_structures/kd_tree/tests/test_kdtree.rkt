;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/kd_tree/tests")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define INF 1000000000.0)
(define seed 1)
(define (rand_float)
  (let/ec _return (begin
(set! seed (modulo (let ([__l (* seed 1103515245)] [__r 12345]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2147483648))
(_return (/ (exact->inexact seed) 2147483648.0))
))
)
(define (hypercube_points num_points cube_size num_dimensions)
  (let/ec _return (begin
(define pts (vector))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r num_points]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define p (vector))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r num_dimensions]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define v (* cube_size (rand_float)))
      (set! p (appendv (or p (vector)) (vector v)))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! pts (appendv (or pts (vector)) (vector p)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return pts)
))
)
(define (build_kdtree points depth)
  (let/ec _return (begin
(_return points)
))
)
(define (distance_sq a b)
  (let/ec _return (begin
(define sum 0.0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(vector? a) (vector-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define d (- (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f)))
      (set! sum (let ([__l sum] [__r (* d d)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return sum)
))
)
(define (nearest_neighbour_search points query)
  (let/ec _return (begin
(if (equal? (cond [(not points) 0] [(string? points) (string-length points)] [(vector? points) (vector-length points)] [(hash? points) (hash-count points)] [else (length points)]) 0) (let ()
(_return (let ([h (make-hash)]) (hash-set! h "index" (- 1.0)) (hash-set! h "dist" INF) (hash-set! h "visited" 0.0) h))
) (void))
(define nearest_idx 0)
(define nearest_dist INF)
(define visited 0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not points) 0] [(string? points) (string-length points)] [(vector? points) (vector-length points)] [(hash? points) (hash-count points)] [else (length points)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define d (distance_sq query (if points (if (hash? points) (hash-ref points i #f) (safe-index points (int i))) #f)))
      (set! visited (let ([__l visited] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (if (let ([__l d] [__r nearest_dist]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! nearest_dist d)
(set! nearest_idx i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (let ([h (make-hash)]) (hash-set! h "index" (exact->inexact nearest_idx)) (hash-set! h "dist" nearest_dist) (hash-set! h "visited" (exact->inexact visited)) h))
))
)
(define (test_build_cases)
  (let/ec _return (begin
(define empty_pts (vector))
(define tree0 (build_kdtree empty_pts 0))
(if (equal? (cond [(not tree0) 0] [(string? tree0) (string-length tree0)] [(vector? tree0) (vector-length tree0)] [(hash? tree0) (hash-count tree0)] [else (length tree0)]) 0) (let ()
(displayln (to-string "case1 true"))
) (let ()
(displayln (to-string "case1 false"))
))
(define pts1 (hypercube_points 10 10.0 2))
(define tree1 (build_kdtree pts1 2))
(if (and (let ([__l (cond [(not tree1) 0] [(string? tree1) (string-length tree1)] [(vector? tree1) (vector-length tree1)] [(hash? tree1) (hash-count tree1)] [else (length tree1)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (equal? (cond [(not (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f)) 0] [(string? (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f)) (string-length (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f))] [(vector? (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f)) (vector-length (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f))] [(hash? (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f)) (hash-count (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f))] [else (length (if tree1 (if (hash? tree1) (hash-ref tree1 0 #f) (safe-index tree1 (int 0))) #f))]) 2)) (let ()
(displayln (to-string "case2 true"))
) (let ()
(displayln (to-string "case2 false"))
))
(define pts2 (hypercube_points 10 10.0 3))
(define tree2 (build_kdtree pts2 (- 2)))
(if (and (let ([__l (cond [(not tree2) 0] [(string? tree2) (string-length tree2)] [(vector? tree2) (vector-length tree2)] [(hash? tree2) (hash-count tree2)] [else (length tree2)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (equal? (cond [(not (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f)) 0] [(string? (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f)) (string-length (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f))] [(vector? (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f)) (vector-length (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f))] [(hash? (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f)) (hash-count (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f))] [else (length (if tree2 (if (hash? tree2) (hash-ref tree2 0 #f) (safe-index tree2 (int 0))) #f))]) 3)) (let ()
(displayln (to-string "case3 true"))
) (let ()
(displayln (to-string "case3 false"))
))
))
)
(define (test_search)
  (let/ec _return (begin
(define pts (hypercube_points 10 10.0 2))
(define tree (build_kdtree pts 0))
(define qp (if (hypercube_points 1 10.0 2) (if (hash? (hypercube_points 1 10.0 2)) (hash-ref (hypercube_points 1 10.0 2) 0 #f) (safe-index (hypercube_points 1 10.0 2) (int 0))) #f))
(define res (nearest_neighbour_search tree qp))
(if (and (and (not (equal? (if res (hash-ref res "index" 0.0) #f) (- 1.0))) (let ([__l (if res (hash-ref res "dist" 0.0) #f)] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))]))) (let ([__l (if res (hash-ref res "visited" 0.0) #f)] [__r 0.0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (let ()
(displayln (to-string "search true"))
) (let ()
(displayln (to-string "search false"))
))
))
)
(define (test_edge)
  (let/ec _return (begin
(define empty_pts (vector))
(define tree (build_kdtree empty_pts 0))
(define query (vector 0.0 0.0))
(define res (nearest_neighbour_search tree query))
(if (and (and (equal? (if res (hash-ref res "index" 0.0) #f) (- 1.0)) (let ([__l (if res (hash-ref res "dist" 0.0) #f)] [__r 100000000.0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (equal? (if res (hash-ref res "visited" 0.0) #f) 0.0)) (let ()
(displayln (to-string "edge true"))
) (let ()
(displayln (to-string "edge false"))
))
))
)
(define (main)
  (let/ec _return (begin
(set! seed 1)
(test_build_cases)
(test_search)
(test_edge)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
