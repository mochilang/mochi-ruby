;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/kd_tree")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (square_distance a b)
  (let/ec _return (begin
(define i 0)
(define total 0.0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not a) 0] [(string? a) (string-length a)] [(vector? a) (vector-length a)] [(hash? a) (hash-count a)] [else (length a)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define diff (- (if a (if (hash? a) (hash-ref a i #f) (safe-index a (int i))) #f) (if b (if (hash? b) (hash-ref b i #f) (safe-index b (int i))) #f)))
      (set! total (let ([__l total] [__r (* diff diff)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return total)
))
)
(define (search nodes index query_point depth best)
  (let/ec _return (begin
(if (equal? index (- 1)) (let ()
(_return best)
) (void))
(define result best)
(if (hash? result) (hash-set! result "nodes_visited" (let ([__l (if result (hash-ref result "nodes_visited" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (begin (set! result (make-hash)) (hash-set! result "nodes_visited" (let ([__l (if result (hash-ref result "nodes_visited" #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(define node (if nodes (if (hash? nodes) (hash-ref nodes index #f) (safe-index nodes (int index))) #f))
(define current_point (if node (hash-ref node "point" #f) #f))
(define current_dist (square_distance query_point current_point))
(if (or (equal? (cond [(not (if result (hash-ref result "point" #f) #f)) 0] [(string? (if result (hash-ref result "point" #f) #f)) (string-length (if result (hash-ref result "point" #f) #f))] [(vector? (if result (hash-ref result "point" #f) #f)) (vector-length (if result (hash-ref result "point" #f) #f))] [(hash? (if result (hash-ref result "point" #f) #f)) (hash-count (if result (hash-ref result "point" #f) #f))] [else (length (if result (hash-ref result "point" #f) #f))]) 0) (let ([__l current_dist] [__r (if result (hash-ref result "distance" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (let ()
(if (hash? result) (hash-set! result "point" current_point) (begin (set! result (make-hash)) (hash-set! result "point" current_point)))
(if (hash? result) (hash-set! result "distance" current_dist) (begin (set! result (make-hash)) (hash-set! result "distance" current_dist)))
) (void))
(define k (cond [(not query_point) 0] [(string? query_point) (string-length query_point)] [(vector? query_point) (vector-length query_point)] [(hash? query_point) (hash-count query_point)] [else (length query_point)]))
(define axis (modulo depth k))
(define nearer (if node (hash-ref node "left" #f) #f))
(define further (if node (hash-ref node "right" #f) #f))
(if (let ([__l (if query_point (if (hash? query_point) (hash-ref query_point axis #f) (safe-index query_point (int axis))) #f)] [__r (if current_point (if (hash? current_point) (hash-ref current_point axis #f) (safe-index current_point (int axis))) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! nearer (if node (hash-ref node "right" #f) #f))
(set! further (if node (hash-ref node "left" #f) #f))
) (void))
(set! result (search nodes nearer query_point (let ([__l depth] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) result))
(define diff (- (if query_point (if (hash? query_point) (hash-ref query_point axis #f) (safe-index query_point (int axis))) #f) (if current_point (if (hash? current_point) (hash-ref current_point axis #f) (safe-index current_point (int axis))) #f)))
(if (let ([__l (* diff diff)] [__r (if result (hash-ref result "distance" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! result (search nodes further query_point (let ([__l depth] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) result))
) (void))
(_return result)
))
)
(define (nearest_neighbour_search nodes root query_point)
  (let/ec _return (begin
(define initial (let ([h (make-hash)]) (hash-set! h "point" (vector)) (hash-set! h "distance" 1e+30) (hash-set! h "nodes_visited" 0) h))
(_return (search nodes root query_point 0 initial))
))
)
(define nodes (vector (let ([h (make-hash)]) (hash-set! h "point" (vector 9.0 1.0)) (hash-set! h "left" 1) (hash-set! h "right" 4) h) (let ([h (make-hash)]) (hash-set! h "point" (vector 2.0 7.0)) (hash-set! h "left" 2) (hash-set! h "right" 3) h) (let ([h (make-hash)]) (hash-set! h "point" (vector 3.0 6.0)) (hash-set! h "left" (- 1)) (hash-set! h "right" (- 1)) h) (let ([h (make-hash)]) (hash-set! h "point" (vector 6.0 12.0)) (hash-set! h "left" (- 1)) (hash-set! h "right" (- 1)) h) (let ([h (make-hash)]) (hash-set! h "point" (vector 17.0 15.0)) (hash-set! h "left" 5) (hash-set! h "right" 6) h) (let ([h (make-hash)]) (hash-set! h "point" (vector 13.0 15.0)) (hash-set! h "left" (- 1)) (hash-set! h "right" (- 1)) h) (let ([h (make-hash)]) (hash-set! h "point" (vector 10.0 19.0)) (hash-set! h "left" (- 1)) (hash-set! h "right" (- 1)) h)))
(define queries (vector (vector 9.0 2.0) (vector 12.0 15.0) (vector 1.0 3.0)))
(define q 0)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(let/ec _break (let loop ()
  (if (let ([__l q] [__r (cond [(not queries) 0] [(string? queries) (string-length queries)] [(vector? queries) (vector-length queries)] [(hash? queries) (hash-count queries)] [else (length queries)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define res (nearest_neighbour_search nodes 0 (if queries (if (hash? queries) (hash-ref queries q #f) (safe-index queries (int q))) #f)))
      (displayln (to-string (string-append (string-append (string-append (string-append (string-append (to-string (if res (hash-ref res "point" #f) #f)) " ") (to-string (if res (hash-ref res "distance" #f) #f))) " ") (to-string (if res (hash-ref res "nodes_visited" #f) #f))) "\n")))
      (set! q (let ([__l q] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
