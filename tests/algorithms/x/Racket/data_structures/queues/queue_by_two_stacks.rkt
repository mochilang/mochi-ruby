;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/queues")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (new_queue items)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "stack1" items) (hash-set! h "stack2" (vector)) h))
))
)
(define (len_queue q)
  (let/ec _return (begin
(_return (let ([__l (cond [(not (if q (hash-ref q "stack1" #f) #f)) 0] [(string? (if q (hash-ref q "stack1" #f) #f)) (string-length (if q (hash-ref q "stack1" #f) #f))] [(vector? (if q (hash-ref q "stack1" #f) #f)) (vector-length (if q (hash-ref q "stack1" #f) #f))] [(hash? (if q (hash-ref q "stack1" #f) #f)) (hash-count (if q (hash-ref q "stack1" #f) #f))] [else (length (if q (hash-ref q "stack1" #f) #f))])] [__r (cond [(not (if q (hash-ref q "stack2" #f) #f)) 0] [(string? (if q (hash-ref q "stack2" #f) #f)) (string-length (if q (hash-ref q "stack2" #f) #f))] [(vector? (if q (hash-ref q "stack2" #f) #f)) (vector-length (if q (hash-ref q "stack2" #f) #f))] [(hash? (if q (hash-ref q "stack2" #f) #f)) (hash-count (if q (hash-ref q "stack2" #f) #f))] [else (length (if q (hash-ref q "stack2" #f) #f))])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
)
(define (str_queue q)
  (let/ec _return (begin
(define items (vector))
(define i (- (cond [(not (if q (hash-ref q "stack2" #f) #f)) 0] [(string? (if q (hash-ref q "stack2" #f) #f)) (string-length (if q (hash-ref q "stack2" #f) #f))] [(vector? (if q (hash-ref q "stack2" #f) #f)) (vector-length (if q (hash-ref q "stack2" #f) #f))] [(hash? (if q (hash-ref q "stack2" #f) #f)) (hash-count (if q (hash-ref q "stack2" #f) #f))] [else (length (if q (hash-ref q "stack2" #f) #f))]) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! items (appendv (or items (vector)) (vector (if (if q (hash-ref q "stack2" #f) #f) (if (hash? (if q (hash-ref q "stack2" #f) #f)) (hash-ref (if q (hash-ref q "stack2" #f) #f) i #f) (safe-index (if q (hash-ref q "stack2" #f) #f) (int i))) #f))))
      (set! i (- i 1))
    )
    (loop)) (void))))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not (if q (hash-ref q "stack1" #f) #f)) 0] [(string? (if q (hash-ref q "stack1" #f) #f)) (string-length (if q (hash-ref q "stack1" #f) #f))] [(vector? (if q (hash-ref q "stack1" #f) #f)) (vector-length (if q (hash-ref q "stack1" #f) #f))] [(hash? (if q (hash-ref q "stack1" #f) #f)) (hash-count (if q (hash-ref q "stack1" #f) #f))] [else (length (if q (hash-ref q "stack1" #f) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! items (appendv (or items (vector)) (vector (if (if q (hash-ref q "stack1" #f) #f) (if (hash? (if q (hash-ref q "stack1" #f) #f)) (hash-ref (if q (hash-ref q "stack1" #f) #f) j #f) (safe-index (if q (hash-ref q "stack1" #f) #f) (int j))) #f))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define s "Queue((")
(define k 0)
(let/ec _break (let loop ()
  (if (let ([__l k] [__r (cond [(not items) 0] [(string? items) (string-length items)] [(vector? items) (vector-length items)] [(hash? items) (hash-count items)] [else (length items)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! s (string-append s (to-string (if items (if (hash? items) (hash-ref items k #f) (safe-index items (int k))) #f))))
      (if (let ([__l k] [__r (- (cond [(not items) 0] [(string? items) (string-length items)] [(vector? items) (vector-length items)] [(hash? items) (hash-count items)] [else (length items)]) 1)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! s (string-append s ", "))
) (void))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! s (string-append s "))"))
(_return s)
))
)
(define (put q item)
  (let/ec _return (begin
(define s1 (if q (hash-ref q "stack1" #f) #f))
(set! s1 (appendv (or s1 (vector)) (vector item)))
(_return (let ([h (make-hash)]) (hash-set! h "stack1" s1) (hash-set! h "stack2" (if q (hash-ref q "stack2" #f) #f)) h))
))
)
(define (get q)
  (let/ec _return (begin
(define s1 (if q (hash-ref q "stack1" #f) #f))
(define s2 (if q (hash-ref q "stack2" #f) #f))
(if (equal? (cond [(not s2) 0] [(string? s2) (string-length s2)] [(vector? s2) (vector-length s2)] [(hash? s2) (hash-count s2)] [else (length s2)]) 0) (let ()
(let/ec _break (let loop ()
  (if (let ([__l (cond [(not s1) 0] [(string? s1) (string-length s1)] [(vector? s1) (vector-length s1)] [(hash? s1) (hash-count s1)] [else (length s1)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (begin
    (let/ec _cont
      (define idx (- (cond [(not s1) 0] [(string? s1) (string-length s1)] [(vector? s1) (vector-length s1)] [(hash? s1) (hash-count s1)] [else (length s1)]) 1))
      (define v (if s1 (if (hash? s1) (hash-ref s1 idx #f) (safe-index s1 (int idx))) #f))
      (define new_s1 (vector))
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r idx]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! new_s1 (appendv (or new_s1 (vector)) (vector (if s1 (if (hash? s1) (hash-ref s1 i #f) (safe-index s1 (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! s1 new_s1)
      (set! s2 (appendv (or s2 (vector)) (vector v)))
    )
    (loop)) (void))))
) (void))
(if (equal? (cond [(not s2) 0] [(string? s2) (string-length s2)] [(vector? s2) (vector-length s2)] [(hash? s2) (hash-count s2)] [else (length s2)]) 0) (let ()
(panic "Queue is empty")
) (void))
(define idx2 (- (cond [(not s2) 0] [(string? s2) (string-length s2)] [(vector? s2) (vector-length s2)] [(hash? s2) (hash-count s2)] [else (length s2)]) 1))
(define value (if s2 (if (hash? s2) (hash-ref s2 idx2 #f) (safe-index s2 (int idx2))) #f))
(define new_s2 (vector))
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r idx2]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! new_s2 (appendv (or new_s2 (vector)) (vector (if s2 (if (hash? s2) (hash-ref s2 j #f) (safe-index s2 (int j))) #f))))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! s2 new_s2)
(_return (let ([h (make-hash)]) (hash-set! h "queue" (let ([h (make-hash)]) (hash-set! h "stack1" s1) (hash-set! h "stack2" s2) h)) (hash-set! h "value" value) h))
))
)
(define q (new_queue (vector 10 20 30)))
(define r1 (get q))
(define r2 (hash))
(define r3 (hash))
(define r4 (hash))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(set! q (if r1 (hash-ref r1 "queue" #f) #f))
(displayln (to-string (if r1 (hash-ref r1 "value" #f) #f)))
(set! q (put q 40))
(set! r2 (get q))
(set! q (if r2 (hash-ref r2 "queue" #f) #f))
(displayln (to-string (if r2 (hash-ref r2 "value" #f) #f)))
(set! r3 (get q))
(set! q (if r3 (hash-ref r3 "queue" #f) #f))
(displayln (to-string (if r3 (hash-ref r3 "value" #f) #f)))
(displayln (to-string (len_queue q)))
(set! r4 (get q))
(set! q (if r4 (hash-ref r4 "queue" #f) #f))
(displayln (to-string (if r4 (hash-ref r4 "value" #f) #f)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
