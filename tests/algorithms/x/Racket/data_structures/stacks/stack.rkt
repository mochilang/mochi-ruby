;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/stacks")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (make_stack limit)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "items" (vector)) (hash-set! h "limit" limit) h))
))
)
(define (is_empty s)
  (let/ec _return (begin
(_return (equal? (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))]) 0))
))
)
(define (size s)
  (let/ec _return (begin
(_return (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))]))
))
)
(define (is_full s)
  (let/ec _return (begin
(_return (let ([__l (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))])] [__r (if s (hash-ref s "limit" #f) #f)]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])))
))
)
(define (push s item)
  (let/ec _return (begin
(if (is_full s) (let ()
(panic "stack overflow")
) (void))
(if (hash? s) (hash-set! s "items" (appendv (or (if s (hash-ref s "items" #f) #f) (vector)) (vector item))) (begin (set! s (make-hash)) (hash-set! s "items" (appendv (or (if s (hash-ref s "items" #f) #f) (vector)) (vector item)))))
))
)
(define (pop s)
  (let/ec _return (begin
(if (is_empty s) (let ()
(panic "stack underflow")
) (void))
(define n (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))]))
(define val (if (if s (hash-ref s "items" #f) #f) (if (hash? (if s (hash-ref s "items" #f) #f)) (hash-ref (if s (hash-ref s "items" #f) #f) (- n 1) #f) (safe-index (if s (hash-ref s "items" #f) #f) (int (- n 1)))) #f))
(if (hash? s) (hash-set! s "items" (slice (if s (hash-ref s "items" #f) #f) 0 (- n 1))) (begin (set! s (make-hash)) (hash-set! s "items" (slice (if s (hash-ref s "items" #f) #f) 0 (- n 1)))))
(_return val)
))
)
(define (peek s)
  (let/ec _return (begin
(if (is_empty s) (let ()
(panic "peek from empty stack")
) (void))
(_return (if (if s (hash-ref s "items" #f) #f) (if (hash? (if s (hash-ref s "items" #f) #f)) (hash-ref (if s (hash-ref s "items" #f) #f) (- (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))]) 1) #f) (safe-index (if s (hash-ref s "items" #f) #f) (int (- (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))]) 1)))) #f))
))
)
(define (contains s item)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not (if s (hash-ref s "items" #f) #f)) 0] [(string? (if s (hash-ref s "items" #f) #f)) (string-length (if s (hash-ref s "items" #f) #f))] [(vector? (if s (hash-ref s "items" #f) #f)) (vector-length (if s (hash-ref s "items" #f) #f))] [(hash? (if s (hash-ref s "items" #f) #f)) (hash-count (if s (hash-ref s "items" #f) #f))] [else (length (if s (hash-ref s "items" #f) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (equal? (if (if s (hash-ref s "items" #f) #f) (if (hash? (if s (hash-ref s "items" #f) #f)) (hash-ref (if s (hash-ref s "items" #f) #f) i #f) (safe-index (if s (hash-ref s "items" #f) #f) (int i))) #f) item) (let ()
(_return #t)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #f)
))
)
(define (stack_repr s)
  (let/ec _return (begin
(_return (to-string (if s (hash-ref s "items" #f) #f)))
))
)
(define (main)
  (let/ec _return (begin
(define s (make_stack 5))
(displayln (to-string (to-string (is_empty s))))
(push s 0)
(push s 1)
(push s 2)
(displayln (to-string (to-string (peek s))))
(displayln (to-string (to-string (size s))))
(displayln (to-string (to-string (is_full s))))
(push s 3)
(push s 4)
(displayln (to-string (to-string (is_full s))))
(displayln (to-string (stack_repr s)))
(displayln (to-string (to-string (pop s))))
(displayln (to-string (to-string (peek s))))
(displayln (to-string (to-string (contains s 1))))
(displayln (to-string (to-string (contains s 9))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
