;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (make_prefix_sum arr)
  (let/ec _return (begin
(define prefix (list))
(define running 0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])) (let/ec _cont
    (set! running (let ([__l running] [__r (if arr (if (hash? arr) (hash-ref arr i #f) (list-ref arr (int i))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! prefix (append prefix (list running)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "prefix_sum" prefix))
))
)
(define (get_sum ps start end)
  (let/ec _return (begin
(define prefix (if ps (hash-ref ps "prefix_sum" #f) #f))
(if (equal? (cond [(string? prefix) (string-length prefix)] [(hash? prefix) (hash-count prefix)] [else (length prefix)]) 0) (let ()
(panic "The array is empty.")
) (void))
(if (or (or (< start 0) (>= end (cond [(string? prefix) (string-length prefix)] [(hash? prefix) (hash-count prefix)] [else (length prefix)]))) (> start end)) (let ()
(panic "Invalid range specified.")
) (void))
(if (equal? start 0) (let ()
(_return (if prefix (if (hash? prefix) (hash-ref prefix end #f) (list-ref prefix (int end))) #f))
) (void))
(_return (- (if prefix (if (hash? prefix) (hash-ref prefix end #f) (list-ref prefix (int end))) #f) (if prefix (if (hash? prefix) (hash-ref prefix (- start 1) #f) (list-ref prefix (int (- start 1)))) #f)))
))
)
(define (contains_sum ps target_sum)
  (let/ec _return (begin
(define prefix (if ps (hash-ref ps "prefix_sum" #f) #f))
(define sums (list 0))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? prefix) (string-length prefix)] [(hash? prefix) (hash-count prefix)] [else (length prefix)])) (let/ec _cont
    (define sum_item (if prefix (if (hash? prefix) (hash-ref prefix i #f) (list-ref prefix (int i))) #f))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j (cond [(string? sums) (string-length sums)] [(hash? sums) (hash-count sums)] [else (length sums)])) (let/ec _cont
    (if (equal? (if sums (if (hash? sums) (hash-ref sums j #f) (list-ref sums (int j))) #f) (- sum_item target_sum)) (let ()
(_return #t)
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! sums (append sums (list sum_item)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define ps (make_prefix_sum (list 1 2 3)))
(define ps2 (make_prefix_sum (list 1 (- 2) 3)))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (get_sum ps 0 2)))
(displayln (format "~a" (get_sum ps 1 2)))
(displayln (format "~a" (get_sum ps 2 2)))
(displayln (format "~a" (contains_sum ps 6)))
(displayln (format "~a" (contains_sum ps 5)))
(displayln (format "~a" (contains_sum ps 3)))
(displayln (format "~a" (contains_sum ps 4)))
(displayln (format "~a" (contains_sum ps 7)))
(displayln (format "~a" (contains_sum ps2 2)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
