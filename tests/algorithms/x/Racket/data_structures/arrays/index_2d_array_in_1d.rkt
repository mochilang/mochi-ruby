;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (iterator_values matrix)
  (let/ec _return (begin
(define result (list))
(let/ec _break (for ([row matrix])
  (let/ec _cont
(let/ec _break (for ([value row])
  (let/ec _cont
(set! result (append result (list value)))
  )))
  )))
(_return result)
))
)
(define (index_2d_array_in_1d array index)
  (let/ec _return (begin
(define rows (cond [(string? array) (string-length array)] [(hash? array) (hash-count array)] [else (length array)]))
(define cols (cond [(string? (if array (if (hash? array) (hash-ref array 0 #f) (list-ref array (int 0))) #f)) (string-length (if array (if (hash? array) (hash-ref array 0 #f) (list-ref array (int 0))) #f))] [(hash? (if array (if (hash? array) (hash-ref array 0 #f) (list-ref array (int 0))) #f)) (hash-count (if array (if (hash? array) (hash-ref array 0 #f) (list-ref array (int 0))) #f))] [else (length (if array (if (hash? array) (hash-ref array 0 #f) (list-ref array (int 0))) #f))]))
(if (or (equal? rows 0) (equal? cols 0)) (let ()
(panic "no items in array")
) (void))
(if (or (< index 0) (>= index (* rows cols))) (let ()
(panic "index out of range")
) (void))
(_return (if (if array (if (hash? array) (hash-ref array (int (quotient index cols)) #f) (list-ref array (int (int (quotient index cols))))) #f) (if (hash? (if array (if (hash? array) (hash-ref array (int (quotient index cols)) #f) (list-ref array (int (int (quotient index cols))))) #f)) (hash-ref (if array (if (hash? array) (hash-ref array (int (quotient index cols)) #f) (list-ref array (int (int (quotient index cols))))) #f) (modulo index cols) #f) (list-ref (if array (if (hash? array) (hash-ref array (int (quotient index cols)) #f) (list-ref array (int (int (quotient index cols))))) #f) (int (modulo index cols)))) #f))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln (format "~a" (iterator_values (list (list 5) (list (- 523)) (list (- 1)) (list 34) (list 0)))))
(displayln (format "~a" (iterator_values (list (list 5 (- 523) (- 1)) (list 34 0)))))
(displayln (format "~a" (index_2d_array_in_1d (list (list 0 1 2 3) (list 4 5 6 7) (list 8 9 10 11)) 5)))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
