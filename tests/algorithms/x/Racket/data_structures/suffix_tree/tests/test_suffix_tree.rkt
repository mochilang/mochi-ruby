;; Generated by Mochi 0.10.60 on 2025-08-07 14:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (suffix_tree_new text)
  (let/ec _return (begin
(_return (hash "text" text))
))
)
(define (suffix_tree_search st pattern)
  (let/ec _return (begin
(if (equal? (cond [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]) 0) (let ()
(_return #t)
) (void))
(define i 0)
(define n (cond [(string? (if st (hash-ref st "text" #f) #f)) (string-length (if st (hash-ref st "text" #f) #f))] [(hash? (if st (hash-ref st "text" #f) #f)) (hash-count (if st (hash-ref st "text" #f) #f))] [else (length (if st (hash-ref st "text" #f) #f))]))
(define m (cond [(string? pattern) (string-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)]))
(let/ec _break (let loop ()
  (if (<= i (- n m)) (let/ec _cont
    (define j 0)
    (define found #t)
    (let/ec _break (let loop ()
  (if (< j m) (let/ec _cont
    (if (not (string=? (if (if st (hash-ref st "text" #f) #f) (if (hash? (if st (hash-ref st "text" #f) #f)) (hash-ref (if st (hash-ref st "text" #f) #f) (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (list-ref (if st (hash-ref st "text" #f) #f) (int (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f) (substring pattern j (+ j 1)))) (let ()
(set! found #f)
(_break)
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (if found (let ()
(_return #t)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return #f)
))
)
(define text "banana")
(define st (suffix_tree_new text))
(define patterns_exist (list "ana" "ban" "na"))
(define i 0)
(define patterns_none (list "xyz" "apple" "cat"))
(define substrings (list "ban" "ana" "a" "na"))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(let/ec _break (let loop ()
  (if (< i (cond [(string? patterns_exist) (string-length patterns_exist)] [(hash? patterns_exist) (hash-count patterns_exist)] [else (length patterns_exist)])) (let/ec _cont
    (displayln (format "~a" (suffix_tree_search st (if patterns_exist (if (hash? patterns_exist) (hash-ref patterns_exist i #f) (list-ref patterns_exist (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? patterns_none) (string-length patterns_none)] [(hash? patterns_none) (hash-count patterns_none)] [else (length patterns_none)])) (let/ec _cont
    (displayln (format "~a" (suffix_tree_search st (if patterns_none (if (hash? patterns_none) (hash-ref patterns_none i #f) (list-ref patterns_none (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln (format "~a" (suffix_tree_search st "")))
(displayln (format "~a" (suffix_tree_search st text)))
(set! i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? substrings) (string-length substrings)] [(hash? substrings) (hash-count substrings)] [else (length substrings)])) (let/ec _cont
    (displayln (format "~a" (suffix_tree_search st (if substrings (if (hash? substrings) (hash-ref substrings i #f) (list-ref substrings (int i))) #f))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
