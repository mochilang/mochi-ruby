;; Generated by Mochi 0.10.72 on 2025-08-24 23:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/data_structures/suffix_tree")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (new_node)
  (let/ec _return (begin
(_return (let ([h (make-hash)]) (hash-set! h "children" (let ([h (make-hash)]) h)) (hash-set! h "is_end_of_string" #f) (hash-set! h "start" (- 1)) (hash-set! h "end" (- 1)) h))
))
)
(define (has_key m k)
  (let/ec _return (begin
(let ([__seq m]) (let/ec _break (for ([key (in-hash-keys __seq)])
  (let/ec _cont
(if (string=? key k) (let ()
(_return #t)
) (void))
  ))))
(_return #f)
))
)
(define (add_suffix tree suffix index)
  (let/ec _return (begin
(define nodes (if tree (hash-ref tree "nodes" #f) #f))
(define node_idx 0)
(define j 0)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not suffix) 0] [(string? suffix) (string-length suffix)] [(vector? suffix) (vector-length suffix)] [(hash? suffix) (hash-count suffix)] [else (length suffix)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define ch (slice suffix j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (define node (if nodes (if (hash? nodes) (hash-ref nodes node_idx #f) (safe-index nodes (int node_idx))) #f))
      (define children (if node (hash-ref node "children" #f) #f))
      (if (not (has_key children ch)) (let ()
(set! nodes (appendv (or nodes (vector)) (new_node)))
(define new_idx (- (cond [(not nodes) 0] [(string? nodes) (string-length nodes)] [(vector? nodes) (vector-length nodes)] [(hash? nodes) (hash-count nodes)] [else (length nodes)]) 1))
(if (hash? children) (hash-set! children ch new_idx) (begin (set! children (make-hash)) (hash-set! children ch new_idx)))
) (void))
      (if (hash? node) (hash-set! node "children" children) (begin (set! node (make-hash)) (hash-set! node "children" children)))
      (vector-set! nodes (int node_idx) node)
      (set! node_idx (if children (hash-ref children ch 0) #f))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define node (if nodes (if (hash? nodes) (hash-ref nodes node_idx #f) (safe-index nodes (int node_idx))) #f))
(if (hash? node) (hash-set! node "is_end_of_string" #t) (begin (set! node (make-hash)) (hash-set! node "is_end_of_string" #t)))
(if (hash? node) (hash-set! node "start" index) (begin (set! node (make-hash)) (hash-set! node "start" index)))
(if (hash? node) (hash-set! node "end" (- (let ([__l index] [__r (cond [(not suffix) 0] [(string? suffix) (string-length suffix)] [(vector? suffix) (vector-length suffix)] [(hash? suffix) (hash-count suffix)] [else (length suffix)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1)) (begin (set! node (make-hash)) (hash-set! node "end" (- (let ([__l index] [__r (cond [(not suffix) 0] [(string? suffix) (string-length suffix)] [(vector? suffix) (vector-length suffix)] [(hash? suffix) (hash-count suffix)] [else (length suffix)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1))))
(vector-set! nodes (int node_idx) node)
(if (hash? tree) (hash-set! tree "nodes" nodes) (begin (set! tree (make-hash)) (hash-set! tree "nodes" nodes)))
(_return tree)
))
)
(define (build_suffix_tree tree)
  (let/ec _return (begin
(define text (if tree (hash-ref tree "text" #f) #f))
(define n (cond [(not text) 0] [(string? text) (string-length text)] [(vector? text) (vector-length text)] [(hash? text) (hash-count text)] [else (length text)]))
(define i 0)
(define t tree)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define suffix "")
      (define k i)
      (let/ec _break (let loop ()
  (if (let ([__l k] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! suffix (string-append suffix (slice text k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
      (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! t (add_suffix t suffix i))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return t)
))
)
(define (new_suffix_tree text)
  (let/ec _return (begin
(define tree (let ([h (make-hash)]) (hash-set! h "text" text) (hash-set! h "nodes" (vector)) h))
(if (hash? tree) (hash-set! tree "nodes" (appendv (or (if tree (hash-ref tree "nodes" #f) #f) (vector)) (new_node))) (begin (set! tree (make-hash)) (hash-set! tree "nodes" (appendv (or (if tree (hash-ref tree "nodes" #f) #f) (vector)) (new_node)))))
(set! tree (build_suffix_tree tree))
(_return tree)
))
)
(define (search tree pattern)
  (let/ec _return (begin
(define node_idx 0)
(define i 0)
(define nodes (if tree (hash-ref tree "nodes" #f) #f))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not pattern) 0] [(string? pattern) (string-length pattern)] [(vector? pattern) (vector-length pattern)] [(hash? pattern) (hash-count pattern)] [else (length pattern)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define ch (slice pattern i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (define node (if nodes (if (hash? nodes) (hash-ref nodes node_idx #f) (safe-index nodes (int node_idx))) #f))
      (define children (if node (hash-ref node "children" #f) #f))
      (if (not (has_key children ch)) (let ()
(_return #f)
) (void))
      (set! node_idx (if children (hash-ref children ch 0) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return #t)
))
)
(define st (new_suffix_tree "bananas"))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (to-string (search st "ana"))))
(displayln (to-string (to-string (search st "apple"))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
