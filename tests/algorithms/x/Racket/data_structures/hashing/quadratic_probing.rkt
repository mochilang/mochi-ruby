;; Generated by Mochi 0.10.59 on 2025-08-06 22:47 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (create_hash_table size)
  (let/ec _return (begin
(define vals (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i size) (let/ec _cont
    (set! vals (append vals (list #f)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (hash "size_table" size "values" vals "lim_charge" 0.75))
))
)
(define (hash_function table key)
  (let/ec _return (begin
(_return (modulo key (if table (hash-ref table "size_table" #f) #f)))
))
)
(define (balanced_factor table)
  (let/ec _return (begin
(define count 0)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if table (hash-ref table "values" #f) #f)) (string-length (if table (hash-ref table "values" #f) #f))] [(hash? (if table (hash-ref table "values" #f) #f)) (hash-count (if table (hash-ref table "values" #f) #f))] [else (length (if table (hash-ref table "values" #f) #f))])) (let/ec _cont
    (if (if (if table (hash-ref table "values" #f) #f) (if (hash? (if table (hash-ref table "values" #f) #f)) (hash-ref (if table (hash-ref table "values" #f) #f) i #f) (list-ref (if table (hash-ref table "values" #f) #f) (int i))) #f) (let ()
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (/ (exact->inexact count) (exact->inexact (if table (hash-ref table "size_table" #f) #f))))
))
)
(define (collision_resolution table key)
  (let/ec _return (begin
(define i 1)
(define new_key (hash_function table (let ([__l key] [__r (* i i)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(let/ec _break (let loop ()
  (if (and (if (if table (hash-ref table "values" #f) #f) (if (hash? (if table (hash-ref table "values" #f) #f)) (hash-ref (if table (hash-ref table "values" #f) #f) new_key #f) (list-ref (if table (hash-ref table "values" #f) #f) (int new_key))) #f) (not (equal? (if (if table (hash-ref table "values" #f) #f) (if (hash? (if table (hash-ref table "values" #f) #f)) (hash-ref (if table (hash-ref table "values" #f) #f) new_key #f) (list-ref (if table (hash-ref table "values" #f) #f) (int new_key))) #f) key))) (let/ec _cont
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (if (>= (balanced_factor table) (if table (hash-ref table "lim_charge" #f) #f)) (let ()
(_return (if table (hash-ref table "size_table" #f) #f))
) (void))
    (set! new_key (hash_function table (let ([__l key] [__r (* i i)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (loop)) (void))))
(_return new_key)
))
)
(define (insert_data table data)
  (let/ec _return (begin
(define key (hash_function table data))
(define vals (if table (hash-ref table "values" #f) #f))
(if (not (if vals (if (hash? vals) (hash-ref vals key #f) (list-ref vals (int key))) #f)) (let ()
(set! vals (list-set vals (int key) data))
) (let ()
(if (equal? (if vals (if (hash? vals) (hash-ref vals key #f) (list-ref vals (int key))) #f) data) (let ()
(set! table (hash-set (or table (hash)) "values" vals))
(_return void)
) (let ()
(define new_key (collision_resolution table key))
(if (and (< new_key (cond [(string? vals) (string-length vals)] [(hash? vals) (hash-count vals)] [else (length vals)])) (not (if vals (if (hash? vals) (hash-ref vals new_key #f) (list-ref vals (int new_key))) #f))) (let ()
(set! vals (list-set vals (int new_key) data))
) (void))
))
))
(set! table (hash-set (or table (hash)) "values" vals))
))
)
(define (int_to_string n)
  (let/ec _return (begin
(if (equal? n 0) (let ()
(_return "0")
) (void))
(define num n)
(define neg #f)
(if (< num 0) (let ()
(set! neg #t)
(set! num (- num))
) (void))
(define res "")
(let/ec _break (let loop ()
  (if (> num 0) (let/ec _cont
    (define digit (modulo num 10))
    (define ch (slice "0123456789" digit (let ([__l digit] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (set! res (string-append ch res))
    (set! num (quotient num 10))
    (loop)) (void))))
(if neg (let ()
(set! res (string-append "-" res))
) (void))
(_return res)
))
)
(define (keys_to_string table)
  (let/ec _return (begin
(define result "{")
(define first #t)
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? (if table (hash-ref table "values" #f) #f)) (string-length (if table (hash-ref table "values" #f) #f))] [(hash? (if table (hash-ref table "values" #f) #f)) (hash-count (if table (hash-ref table "values" #f) #f))] [else (length (if table (hash-ref table "values" #f) #f))])) (let/ec _cont
    (define v (if (if table (hash-ref table "values" #f) #f) (if (hash? (if table (hash-ref table "values" #f) #f)) (hash-ref (if table (hash-ref table "values" #f) #f) i #f) (list-ref (if table (hash-ref table "values" #f) #f) (int i))) #f))
    (if v (let ()
(if (not first) (let ()
(set! result (string-append result ", "))
) (void))
(set! result (string-append (string-append (string-append result (int_to_string i)) ": ") (int_to_string v)))
(set! first #f)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! result (string-append result "}"))
(_return result)
))
)
(define qp (create_hash_table 8))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(insert_data qp 0)
(insert_data qp 999)
(insert_data qp 111)
(displayln (keys_to_string qp))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
