;; Generated by Mochi 0.10.66 on 2025-08-15 15:17 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (number->string x)]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (get_gas_stations gas_quantities costs)
  (let/ec _return (begin
(define stations (list))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not gas_quantities) 0] [(string? gas_quantities) (string-length gas_quantities)] [(hash? gas_quantities) (hash-count gas_quantities)] [else (length gas_quantities)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! stations (append (or stations (list)) (list (let ([h (make-hash)]) (hash-set! h "gas_quantity" (if gas_quantities (if (hash? gas_quantities) (hash-ref gas_quantities i #f) (safe-index gas_quantities (int i))) #f)) (hash-set! h "cost" (if costs (if (hash? costs) (hash-ref costs i #f) (safe-index costs (int i))) #f)) h))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return stations)
))
)
(define (can_complete_journey gas_stations)
  (let/ec _return (begin
(define total_gas 0)
(define total_cost 0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not gas_stations) 0] [(string? gas_stations) (string-length gas_stations)] [(hash? gas_stations) (hash-count gas_stations)] [else (length gas_stations)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! total_gas (let ([__l total_gas] [__r (if (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f) (if (hash? (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f)) (hash-ref (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f) "gas_quantity" #f) (safe-index (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f) (int "gas_quantity"))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! total_cost (let ([__l total_cost] [__r (if (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f) (if (hash? (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f)) (hash-ref (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f) "cost" #f) (safe-index (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f) (int "cost"))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if (let ([__l total_gas] [__r total_cost]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(_return (- 1))
) (void))
(define start 0)
(define net 0)
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not gas_stations) 0] [(string? gas_stations) (string-length gas_stations)] [(hash? gas_stations) (hash-count gas_stations)] [else (length gas_stations)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define station (if gas_stations (if (hash? gas_stations) (hash-ref gas_stations i #f) (safe-index gas_stations (int i))) #f))
      (set! net (- (let ([__l net] [__r (if station (if (hash? station) (hash-ref station "gas_quantity" #f) (safe-index station (int "gas_quantity"))) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (if station (if (hash? station) (hash-ref station "cost" #f) (safe-index station (int "cost"))) #f)))
      (if (let ([__l net] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! start (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! net 0)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return start)
))
)
(define example1 (get_gas_stations (list 1 2 3 4 5) (list 3 4 5 1 2)))
(define example2 (get_gas_stations (list 2 3 4) (list 3 4 3)))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (can_complete_journey example1)))
(displayln (to-string (can_complete_journey example2)))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
