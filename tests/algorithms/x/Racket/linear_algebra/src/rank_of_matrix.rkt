;; Generated by Mochi 0.10.60 on 2025-08-08 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (rank_of_matrix matrix)
  (let/ec _return (begin
(define rows (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]))
(if (equal? rows 0) (let ()
(_return 0)
) (void))
(define columns (if (> (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f))]) 0) (cond [(not (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f)) 0] [(string? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f)) (string-length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f))] [(hash? (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f)) (hash-count (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f))] [else (length (if matrix (if (hash? matrix) (hash-ref matrix 0 #f) (list-ref matrix (int 0))) #f))]) 0))
(define rank (if (< rows columns) rows columns))
(define row 0)
(let/ec _break (let loop ()
  (if (< row rank) (let/ec _cont
    (if (not (equal? (if (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) row #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) (int row))) #f) 0.0)) (let ()
(define col (let ([__l row] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(let/ec _break (let loop ()
  (if (< col rows) (let/ec _cont
    (define mult (/ (if (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f) row #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f) (int row))) #f) (if (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) row #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) (int row))) #f)))
    (define i row)
    (let/ec _break (let loop ()
  (if (< i columns) (let/ec _cont
    (set! matrix (list-set matrix col (list-set (list-ref matrix col) i (- (if (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f) i #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix col #f) (list-ref matrix (int col))) #f) (int i))) #f) (* mult (if (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) i #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f) (int i))) #f))))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! col (let ([__l col] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (let ()
(define reduce #t)
(define i (let ([__l row] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(let/ec _break (let loop ()
  (if (< i rows) (let/ec _cont
    (if (not (equal? (if (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f) row #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f) (int row))) #f) 0.0)) (let ()
(define temp (if matrix (if (hash? matrix) (hash-ref matrix row #f) (list-ref matrix (int row))) #f))
(set! matrix (list-set matrix (int row) (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f)))
(set! matrix (list-set matrix (int i) temp))
(set! reduce #f)
(_break)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(if reduce (let ()
(set! rank (- rank 1))
(define j 0)
(let/ec _break (let loop ()
  (if (< j rows) (let/ec _cont
    (set! matrix (list-set matrix j (list-set (list-ref matrix j) row (if (if matrix (if (hash? matrix) (hash-ref matrix j #f) (list-ref matrix (int j))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix j #f) (list-ref matrix (int j))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix j #f) (list-ref matrix (int j))) #f) rank #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix j #f) (list-ref matrix (int j))) #f) (int rank))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (void))
(set! row (- row 1))
))
    (set! row (let ([__l row] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return rank)
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
