;; Generated by Mochi 0.10.60 on 2025-08-08 11:18 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))

(define (invert_matrix matrix)
  (let/ec _return (begin
(define n (cond [(not matrix) 0] [(string? matrix) (string-length matrix)] [(hash? matrix) (hash-count matrix)] [else (length matrix)]))
(define aug (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let/ec _cont
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j n) (let/ec _cont
    (set! row (append row (list (if (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f) (if (hash? (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f)) (hash-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f) j #f) (list-ref (if matrix (if (hash? matrix) (hash-ref matrix i #f) (list-ref matrix (int i))) #f) (int j))) #f))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (define k 0)
    (let/ec _break (let loop ()
  (if (< k n) (let/ec _cont
    (if (equal? i k) (let ()
(set! row (append row (list 1.0)))
) (let ()
(set! row (append row (list 0.0)))
))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! aug (append aug (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define col 0)
(let/ec _break (let loop ()
  (if (< col n) (let/ec _cont
    (define pivot_row col)
    (define r col)
    (let/ec _break (let loop ()
  (if (< r n) (let/ec _cont
    (if (not (equal? (if (if aug (if (hash? aug) (hash-ref aug r #f) (list-ref aug (int r))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug r #f) (list-ref aug (int r))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug r #f) (list-ref aug (int r))) #f) col #f) (list-ref (if aug (if (hash? aug) (hash-ref aug r #f) (list-ref aug (int r))) #f) (int col))) #f) 0.0)) (let ()
(set! pivot_row r)
(_break)
) (void))
    (set! r (let ([__l r] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (if (equal? (if (if aug (if (hash? aug) (hash-ref aug pivot_row #f) (list-ref aug (int pivot_row))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug pivot_row #f) (list-ref aug (int pivot_row))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug pivot_row #f) (list-ref aug (int pivot_row))) #f) col #f) (list-ref (if aug (if (hash? aug) (hash-ref aug pivot_row #f) (list-ref aug (int pivot_row))) #f) (int col))) #f) 0.0) (let ()
(panic "Matrix is not invertible")
) (void))
    (if (not (equal? pivot_row col)) (let ()
(define temp (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f))
(set! aug (list-set aug (int col) (if aug (if (hash? aug) (hash-ref aug pivot_row #f) (list-ref aug (int pivot_row))) #f)))
(set! aug (list-set aug (int pivot_row) temp))
) (void))
    (define pivot (if (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) col #f) (list-ref (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) (int col))) #f))
    (define c 0)
    (let/ec _break (let loop ()
  (if (< c (* 2 n)) (let/ec _cont
    (set! aug (list-set aug col (list-set (list-ref aug col) c (/ (if (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) c #f) (list-ref (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) (int c))) #f) pivot))))
    (set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (define r2 0)
    (let/ec _break (let loop ()
  (if (< r2 n) (let/ec _cont
    (if (not (equal? r2 col)) (let ()
(define factor (if (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f) col #f) (list-ref (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f) (int col))) #f))
(define c2 0)
(let/ec _break (let loop ()
  (if (< c2 (* 2 n)) (let/ec _cont
    (set! aug (list-set aug r2 (list-set (list-ref aug r2) c2 (- (if (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f) c2 #f) (list-ref (if aug (if (hash? aug) (hash-ref aug r2 #f) (list-ref aug (int r2))) #f) (int c2))) #f) (* factor (if (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) c2 #f) (list-ref (if aug (if (hash? aug) (hash-ref aug col #f) (list-ref aug (int col))) #f) (int c2))) #f))))))
    (set! c2 (let ([__l c2] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (void))
    (set! r2 (let ([__l r2] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! col (let ([__l col] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define inv (list))
(define r3 0)
(let/ec _break (let loop ()
  (if (< r3 n) (let/ec _cont
    (define row (list))
    (define c3 0)
    (let/ec _break (let loop ()
  (if (< c3 n) (let/ec _cont
    (set! row (append row (list (if (if aug (if (hash? aug) (hash-ref aug r3 #f) (list-ref aug (int r3))) #f) (if (hash? (if aug (if (hash? aug) (hash-ref aug r3 #f) (list-ref aug (int r3))) #f)) (hash-ref (if aug (if (hash? aug) (hash-ref aug r3 #f) (list-ref aug (int r3))) #f) (let ([__l c3] [__r n]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) #f) (list-ref (if aug (if (hash? aug) (hash-ref aug r3 #f) (list-ref aug (int r3))) #f) (int (let ([__l c3] [__r n]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) #f))))
    (set! c3 (let ([__l c3] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! inv (append inv (list row)))
    (set! r3 (let ([__l r3] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return inv)
))
)
(define mat (list (list 4.0 7.0) (list 2.0 6.0)))
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(displayln "Original Matrix:")
(displayln mat)
(displayln "Inverted Matrix:")
(displayln (invert_matrix mat))
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
