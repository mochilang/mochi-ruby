;; Generated by Mochi 0.10.59 on 2025-08-06 21:08 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define stdout (hash "write" (lambda (s) (display s))))

(define (repeat_char ch count)
  (let/ec _return (begin
(define res "")
(define i 0)
(let/ec _break (let loop ()
  (if (< i count) (let/ec _cont
    (set! res (string-append res ch))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (abs_int n)
  (let/ec _return (begin
(if (< n 0) (let ()
(_return (- n))
) (void))
(_return n)
))
)
(define (pow2 exp)
  (let/ec _return (begin
(define res 1)
(define i 0)
(let/ec _break (let loop ()
  (if (< i exp) (let/ec _cont
    (set! res (* res 2))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return res)
))
)
(define (to_binary_no_prefix n)
  (let/ec _return (begin
(define v n)
(if (< v 0) (let ()
(set! v (- v))
) (void))
(if (equal? v 0) (let ()
(_return "0")
) (void))
(define res "")
(let/ec _break (let loop ()
  (if (> v 0) (let/ec _cont
    (set! res (string-append (format "~a" (modulo v 2)) res))
    (set! v (quotient v 2))
    (loop)) (void))))
(_return res)
))
)
(define (logical_left_shift number shift_amount)
  (let/ec _return (begin
(if (or (< number 0) (< shift_amount 0)) (let ()
(panic "both inputs must be positive integers")
) (void))
(define binary_number (string-append "0b" (to_binary_no_prefix number)))
(_return (string-append binary_number (repeat_char "0" shift_amount)))
))
)
(define (logical_right_shift number shift_amount)
  (let/ec _return (begin
(if (or (< number 0) (< shift_amount 0)) (let ()
(panic "both inputs must be positive integers")
) (void))
(define binary_number (to_binary_no_prefix number))
(if (>= shift_amount (cond [(string? binary_number) (string-length binary_number)] [(hash? binary_number) (hash-count binary_number)] [else (length binary_number)])) (let ()
(_return "0b0")
) (void))
(define shifted (slice binary_number 0 (- (cond [(string? binary_number) (string-length binary_number)] [(hash? binary_number) (hash-count binary_number)] [else (length binary_number)]) shift_amount)))
(_return (string-append "0b" shifted))
))
)
(define (arithmetic_right_shift number shift_amount)
  (let/ec _return (begin
(define binary_number "")
(if (>= number 0) (let ()
(set! binary_number (string-append "0" (to_binary_no_prefix number)))
) (let ()
(define length_ (cond [(string? (to_binary_no_prefix (- number))) (string-length (to_binary_no_prefix (- number)))] [(hash? (to_binary_no_prefix (- number))) (hash-count (to_binary_no_prefix (- number)))] [else (length (to_binary_no_prefix (- number)))]))
(define intermediate (- (abs_int number) (pow2 length_)))
(define bin_repr (to_binary_no_prefix intermediate))
(set! binary_number (string-append (string-append "1" (repeat_char "0" (- length_ (cond [(string? bin_repr) (string-length bin_repr)] [(hash? bin_repr) (hash-count bin_repr)] [else (length bin_repr)])))) bin_repr))
))
(if (>= shift_amount (cond [(string? binary_number) (string-length binary_number)] [(hash? binary_number) (hash-count binary_number)] [else (length binary_number)])) (let ()
(define sign (slice binary_number 0 1))
(_return (string-append "0b" (repeat_char sign (cond [(string? binary_number) (string-length binary_number)] [(hash? binary_number) (hash-count binary_number)] [else (length binary_number)]))))
) (void))
(define sign (slice binary_number 0 1))
(define shifted (slice binary_number 0 (- (cond [(string? binary_number) (string-length binary_number)] [(hash? binary_number) (hash-count binary_number)] [else (length binary_number)]) shift_amount)))
(_return (string-append (string-append "0b" (repeat_char sign shift_amount)) shifted))
))
)
(define (main)
  (let/ec _return (begin
(displayln (logical_left_shift 17 2))
(displayln (logical_right_shift 1983 4))
(displayln (arithmetic_right_shift (- 17) 2))
))
)
(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
