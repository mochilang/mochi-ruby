;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (index_of_from s sub start)
  (let/ec _return (begin
(define i start)
(define max (- (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) (cond [(not sub) 0] [(string? sub) (string-length sub)] [(hash? sub) (hash-count sub)] [else (length sub)])))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r max]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (if (string=? (slice s i (let ([__l i] [__r (cond [(not sub) 0] [(string? sub) (string-length sub)] [(hash? sub) (hash-count sub)] [else (length sub)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) sub) (let ()
(_return i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (- 1))
))
)
(define (extract_links html)
  (let/ec _return (begin
(define res (list))
(define i 0)
(let/ec _break (let loop ()
  (if #t (begin
    (let/ec _cont
      (define tag_start (index_of_from html "<a class=\"eZt8xd\"" i))
      (if (equal? tag_start (- 1)) (let ()
(_break)
) (void))
      (define href_start (index_of_from html "href=\"" tag_start))
      (if (equal? href_start (- 1)) (let ()
(_break)
) (void))
      (set! href_start (let ([__l href_start] [__r (cond [(not "href=\"") 0] [(string? "href=\"") (string-length "href=\"")] [(hash? "href=\"") (hash-count "href=\"")] [else (length "href=\"")])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define href_end (index_of_from html "\"" href_start))
      (if (equal? href_end (- 1)) (let ()
(_break)
) (void))
      (define href (slice html href_start href_end))
      (define text_start (let ([__l (index_of_from html ">" href_end)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define text_end (index_of_from html "</a>" text_start))
      (if (equal? text_end (- 1)) (let ()
(_break)
) (void))
      (define text (slice html text_start text_end))
      (define link (let ([h (make-hash)]) (hash-set! h "href" href) (hash-set! h "text" text) h))
      (set! res (append (or res (list)) (list link)))
      (set! i (let ([__l text_end] [__r (cond [(not "</a>") 0] [(string? "</a>") (string-length "</a>")] [(hash? "</a>") (hash-count "</a>")] [else (length "</a>")])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return res)
))
)
(define (main)
  (let/ec _return (begin
(define html (string-append (string-append "<div><a class=\"eZt8xd\" href=\"/url?q=http://example1.com\">Example1</a>" "<a class=\"eZt8xd\" href=\"/maps\">Maps</a>") "<a class=\"eZt8xd\" href=\"/url?q=http://example2.com\">Example2</a></div>"))
(define links (extract_links html))
(displayln (to-string (cond [(not links) 0] [(string? links) (string-length links)] [(hash? links) (hash-count links)] [else (length links)])))
(define i 0)
(let/ec _break (let loop ()
  (if (and (let ([__l i] [__r (cond [(not links) 0] [(string? links) (string-length links)] [(hash? links) (hash-count links)] [else (length links)])]) (< (int __l) (int __r))) (let ([__l i] [__r 5]) (< (int __l) (int __r)))) (begin
    (let/ec _cont
      (define link (if links (if (hash? links) (hash-ref links i #f) (safe-index links (int i))) #f))
      (define href (if link (hash-ref link "href" "") #f))
      (define text (if link (hash-ref link "text" "") #f))
      (if (string=? text "Maps") (let ()
(displayln href)
) (let ()
(displayln (string-append "https://google.com" href))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
