;; Generated by Mochi 0.10.63 on 2025-08-11 15:32 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x) (let* ([s (format "~a" x)]) (if (regexp-match? #rx"[.]0$" s) (substring s 0 (- (string-length s) 2)) s))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (safe-index lst idx) (let ([i (int idx)]) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) #f)))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (list-set lst idx val)
  (define i (int idx))
  (define len (length lst))
  (cond
    [(>= i len) (append lst (make-list (- i len) '()) (list val))]
    [(zero? i) (cons val (cdr lst))]
    [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (parse_int s)
  (let/ec _return (begin
(define value 0)
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (define ch (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
      (if (string=? ch ",") (let ()
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(_cont)
) (void))
      (set! value (let ([__l (* value 10)] [__r (int ch)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return value)
))
)
(define (find haystack needle start)
  (let/ec _return (begin
(define nlen (cond [(not needle) 0] [(string? needle) (string-length needle)] [(hash? needle) (hash-count needle)] [else (length needle)]))
(define i start)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (- (cond [(not haystack) 0] [(string? haystack) (string-length haystack)] [(hash? haystack) (hash-count haystack)] [else (length haystack)]) nlen)]) (<= (int __l) (int __r))) (begin
    (let/ec _cont
      (define j 0)
      (define matched #t)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r nlen]) (< (int __l) (int __r))) (begin
    (let/ec _cont
      (if (not (string=? (slice haystack (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l (let ([__l i] [__r j]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (slice needle j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))) (let ()
(set! matched #f)
(_break)
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (if matched (let ()
(_return i)
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (- 0 1))
))
)
(define (extract_numbers html)
  (let/ec _return (begin
(define nums (list))
(define pos 0)
(define start_tag "<span>")
(define end_tag "</span>")
(let/ec _break (let loop ()
  (if #t (begin
    (let/ec _cont
      (define s (find html start_tag pos))
      (if (equal? s (- 0 1)) (let ()
(_break)
) (void))
      (define content_start (let ([__l s] [__r (cond [(not start_tag) 0] [(string? start_tag) (string-length start_tag)] [(hash? start_tag) (hash-count start_tag)] [else (length start_tag)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define e (find html end_tag content_start))
      (if (equal? e (- 0 1)) (let ()
(_break)
) (void))
      (define num_str (slice html content_start e))
      (set! nums (append (or nums (list)) (list (parse_int num_str))))
      (set! pos (let ([__l e] [__r (cond [(not end_tag) 0] [(string? end_tag) (string-length end_tag)] [(hash? end_tag) (hash-count end_tag)] [else (length end_tag)])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return nums)
))
)
(define (covid_stats html)
  (let/ec _return (begin
(define nums (extract_numbers html))
(_return (let ([h (make-hash)]) (hash-set! h "cases" (if nums (if (hash? nums) (hash-ref nums 0 #f) (safe-index nums (int 0))) #f)) (hash-set! h "deaths" (if nums (if (hash? nums) (hash-ref nums 1 #f) (safe-index nums (int 1))) #f)) (hash-set! h "recovered" (if nums (if (hash? nums) (hash-ref nums 2 #f) (safe-index nums (int 2))) #f)) h))
))
)
(define (main)
  (let/ec _return (begin
(define sample_html (string-append (string-append "<div class=\"maincounter-number\"><span>123456</span></div>" "<div class=\"maincounter-number\"><span>7890</span></div>") "<div class=\"maincounter-number\"><span>101112</span></div>"))
(define stats (covid_stats sample_html))
(displayln (string-append "Total COVID-19 cases in the world: " (to-string (if stats (hash-ref stats "cases" #f) #f))))
(displayln (string-append "Total deaths due to COVID-19 in the world: " (to-string (if stats (hash-ref stats "deaths" #f) #f))))
(displayln (string-append "Total COVID-19 patients recovered in the world: " (to-string (if stats (hash-ref stats "recovered" #f) #f))))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
