;; Generated by Mochi 0.10.71 on 2025-08-24 08:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/sorts")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [(vector? x) (format "~a" (vector->list x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a b)]
    [(list? a) (append a (vector->list b))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (new_node state value)
  (let/ec _return (begin
(if (hash? state) (hash-set! state "nodes" (appendv (or (if state (hash-ref state "nodes" #f) #f) (vector)) (vector (let ([h (make-hash)]) (hash-set! h "value" value) (hash-set! h "left" (- 1)) (hash-set! h "right" (- 1)) h)))) (begin (set! state (make-hash)) (hash-set! state "nodes" (appendv (or (if state (hash-ref state "nodes" #f) #f) (vector)) (vector (let ([h (make-hash)]) (hash-set! h "value" value) (hash-set! h "left" (- 1)) (hash-set! h "right" (- 1)) h))))))
(_return (- (cond [(not (if state (hash-ref state "nodes" #f) #f)) 0] [(string? (if state (hash-ref state "nodes" #f) #f)) (string-length (if state (hash-ref state "nodes" #f) #f))] [(vector? (if state (hash-ref state "nodes" #f) #f)) (vector-length (if state (hash-ref state "nodes" #f) #f))] [(hash? (if state (hash-ref state "nodes" #f) #f)) (hash-count (if state (hash-ref state "nodes" #f) #f))] [else (length (if state (hash-ref state "nodes" #f) #f))]) 1))
))
)
(define (insert state value)
  (let/ec _return (begin
(if (equal? (if state (hash-ref state "root" #f) #f) (- 1)) (let ()
(if (hash? state) (hash-set! state "root" (new_node state value)) (begin (set! state (make-hash)) (hash-set! state "root" (new_node state value))))
(_return void)
) (void))
(define current (if state (hash-ref state "root" #f) #f))
(define nodes (if state (hash-ref state "nodes" #f) #f))
(let/ec _break (let loop ()
  (if #t (begin
    (let/ec _cont
      (define node (if nodes (if (hash? nodes) (hash-ref nodes current #f) (safe-index nodes (int current))) #f))
      (if (let ([__l value] [__r (if node (hash-ref node "value" #f) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(if (equal? (if node (hash-ref node "left" #f) #f) (- 1)) (let ()
(define idx (new_node state value))
(set! nodes (if state (hash-ref state "nodes" #f) #f))
(if (hash? node) (hash-set! node "left" idx) (begin (set! node (make-hash)) (hash-set! node "left" idx)))
(vector-set! nodes (int current) node)
(if (hash? state) (hash-set! state "nodes" nodes) (begin (set! state (make-hash)) (hash-set! state "nodes" nodes)))
(_return void)
) (void))
(set! current (if node (hash-ref node "left" #f) #f))
) (let ()
(if (let ([__l value] [__r (if node (hash-ref node "value" #f) #f)]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(if (equal? (if node (hash-ref node "right" #f) #f) (- 1)) (let ()
(define idx (new_node state value))
(set! nodes (if state (hash-ref state "nodes" #f) #f))
(if (hash? node) (hash-set! node "right" idx) (begin (set! node (make-hash)) (hash-set! node "right" idx)))
(vector-set! nodes (int current) node)
(if (hash? state) (hash-set! state "nodes" nodes) (begin (set! state (make-hash)) (hash-set! state "nodes" nodes)))
(_return void)
) (void))
(set! current (if node (hash-ref node "right" #f) #f))
) (let ()
(_return void)
))
))
    )
    (loop)) (void))))
))
)
(define (inorder state idx)
  (let/ec _return (begin
(if (equal? idx (- 1)) (let ()
(_return (vector))
) (void))
(define node (if (if state (hash-ref state "nodes" #f) #f) (if (hash? (if state (hash-ref state "nodes" #f) #f)) (hash-ref (if state (hash-ref state "nodes" #f) #f) idx #f) (safe-index (if state (hash-ref state "nodes" #f) #f) (int idx))) #f))
(define result (inorder state (if node (hash-ref node "left" #f) #f)))
(set! result (appendv (or result (vector)) (vector (if node (hash-ref node "value" #f) #f))))
(define right_part (inorder state (if node (hash-ref node "right" #f) #f)))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not right_part) 0] [(string? right_part) (string-length right_part)] [(vector? right_part) (vector-length right_part)] [(hash? right_part) (hash-count right_part)] [else (length right_part)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (appendv (or result (vector)) (vector (if right_part (if (hash? right_part) (hash-ref right_part i #f) (safe-index right_part (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return result)
))
)
(define (tree_sort arr)
  (let/ec _return (begin
(define state (let ([h (make-hash)]) (hash-set! h "nodes" (vector)) (hash-set! h "root" (- 1)) h))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (insert state (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if (equal? (if state (hash-ref state "root" #f) #f) (- 1)) (let ()
(_return (vector))
) (void))
(_return (inorder state (if state (hash-ref state "root" #f) #f)))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (tree_sort (vector))))
(displayln (to-string (tree_sort (vector 1))))
(displayln (to-string (tree_sort (vector 1 2))))
(displayln (to-string (tree_sort (vector 5 2 7))))
(displayln (to-string (tree_sort (vector 5 (- 4) 9 2 7))))
(displayln (to-string (tree_sort (vector 5 6 1 (- 1) 4 37 2 7))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
