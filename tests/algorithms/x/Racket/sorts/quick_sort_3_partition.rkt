;; Generated by Mochi 0.10.71 on 2025-08-24 08:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/sorts")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [(vector? x) (format "~a" (vector->list x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a b)]
    [(list? a) (append a (vector->list b))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (quick_sort_3partition arr left right)
  (let/ec _return (begin
(if (let ([__l right] [__r left]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ()
(_return arr)
) (void))
(define a left)
(define i left)
(define b right)
(define pivot (if arr (if (hash? arr) (hash-ref arr left #f) (safe-index arr (int left))) #f))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r b]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f)] [__r pivot]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(define temp (if arr (if (hash? arr) (hash-ref arr a #f) (safe-index arr (int a))) #f))
(vector-set! arr (int a) (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))
(vector-set! arr (int i) temp)
(set! a (let ([__l a] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(if (let ([__l (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f)] [__r pivot]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(define temp (if arr (if (hash? arr) (hash-ref arr b #f) (safe-index arr (int b))) #f))
(vector-set! arr (int b) (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))
(vector-set! arr (int i) temp)
(set! b (- b 1))
) (let ()
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
))
    )
    (loop)) (void))))
(set! arr (quick_sort_3partition arr left (- a 1)))
(set! arr (quick_sort_3partition arr (let ([__l b] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) right))
(_return arr)
))
)
(define (quick_sort_lomuto_partition arr left right)
  (let/ec _return (begin
(if (let ([__l left] [__r right]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(define pivot_index (lomuto_partition arr left right))
(set! arr (quick_sort_lomuto_partition arr left (- pivot_index 1)))
(set! arr (quick_sort_lomuto_partition arr (let ([__l pivot_index] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) right))
) (void))
(_return arr)
))
)
(define (lomuto_partition arr left right)
  (let/ec _return (begin
(define pivot (if arr (if (hash? arr) (hash-ref arr right #f) (safe-index arr (int right))) #f))
(define store_index left)
(define i left)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r right]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (let ([__l (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f)] [__r pivot]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(define temp (if arr (if (hash? arr) (hash-ref arr store_index #f) (safe-index arr (int store_index))) #f))
(vector-set! arr (int store_index) (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))
(vector-set! arr (int i) temp)
(set! store_index (let ([__l store_index] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define temp (if arr (if (hash? arr) (hash-ref arr right #f) (safe-index arr (int right))) #f))
(vector-set! arr (int right) (if arr (if (hash? arr) (hash-ref arr store_index #f) (safe-index arr (int store_index))) #f))
(vector-set! arr (int store_index) temp)
(_return store_index)
))
)
(define (three_way_radix_quicksort arr)
  (let/ec _return (begin
(if (let ([__l (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])] [__r 1]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ()
(_return arr)
) (void))
(define pivot (if arr (if (hash? arr) (hash-ref arr 0 #f) (safe-index arr (int 0))) #f))
(define less (vector))
(define equal (vector))
(define greater (vector))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define val (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))
      (if (let ([__l val] [__r pivot]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! less (appendv (or less (vector)) (vector val)))
) (let ()
(if (let ([__l val] [__r pivot]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ()
(set! greater (appendv (or greater (vector)) (vector val)))
) (let ()
(set! equal (appendv (or equal (vector)) (vector val)))
))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define sorted_less (three_way_radix_quicksort less))
(define sorted_greater (three_way_radix_quicksort greater))
(define result (append sorted_less equal))
(set! result (append result sorted_greater))
(_return result)
))
)
(define array1 (vector 5 (- 1) (- 1) 5 5 24 0))
(define array2 (vector))
(define array3 (vector))
(define nums1 (vector))
(define nums2 (vector))
(define nums3 (vector))
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(set! array1 (quick_sort_3partition array1 0 (- (cond [(not array1) 0] [(string? array1) (string-length array1)] [(vector? array1) (vector-length array1)] [(hash? array1) (hash-count array1)] [else (length array1)]) 1)))
(displayln (to-string array1))
(set! array2 (vector 9 0 2 6))
(set! array2 (quick_sort_3partition array2 0 (- (cond [(not array2) 0] [(string? array2) (string-length array2)] [(vector? array2) (vector-length array2)] [(hash? array2) (hash-count array2)] [else (length array2)]) 1)))
(displayln (to-string array2))
(set! array3 (vector))
(set! array3 (quick_sort_3partition array3 0 (- (cond [(not array3) 0] [(string? array3) (string-length array3)] [(vector? array3) (vector-length array3)] [(hash? array3) (hash-count array3)] [else (length array3)]) 1)))
(displayln (to-string array3))
(set! nums1 (vector 0 5 3 1 2))
(set! nums1 (quick_sort_lomuto_partition nums1 0 (- (cond [(not nums1) 0] [(string? nums1) (string-length nums1)] [(vector? nums1) (vector-length nums1)] [(hash? nums1) (hash-count nums1)] [else (length nums1)]) 1)))
(displayln (to-string nums1))
(set! nums2 (vector))
(set! nums2 (quick_sort_lomuto_partition nums2 0 (- (cond [(not nums2) 0] [(string? nums2) (string-length nums2)] [(vector? nums2) (vector-length nums2)] [(hash? nums2) (hash-count nums2)] [else (length nums2)]) 1)))
(displayln (to-string nums2))
(set! nums3 (vector (- 2) 5 0 (- 4)))
(set! nums3 (quick_sort_lomuto_partition nums3 0 (- (cond [(not nums3) 0] [(string? nums3) (string-length nums3)] [(vector? nums3) (vector-length nums3)] [(hash? nums3) (hash-count nums3)] [else (length nums3)]) 1)))
(displayln (to-string nums3))
(displayln (to-string (three_way_radix_quicksort (vector))))
(displayln (to-string (three_way_radix_quicksort (vector 1))))
(displayln (to-string (three_way_radix_quicksort (vector (- 5) (- 2) 1 (- 2) 0 1))))
(displayln (to-string (three_way_radix_quicksort (vector 1 2 5 1 2 0 0 5 2 (- 1)))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
