;; Generated by Mochi 0.10.71 on 2025-08-24 08:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/sorts")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [(vector? x) (format "~a" (vector->list x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a b)]
    [(list? a) (append a (vector->list b))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (merge a low mid high)
  (let/ec _return (begin
(define left (slice a low mid))
(define right (slice a mid (let ([__l high] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(define result (vector))
(let/ec _break (let loop ()
  (if (and (let ([__l (cond [(not left) 0] [(string? left) (string-length left)] [(vector? left) (vector-length left)] [(hash? left) (hash-count left)] [else (length left)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ([__l (cond [(not right) 0] [(string? right) (string-length right)] [(vector? right) (vector-length right)] [(hash? right) (hash-count right)] [else (length right)])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))]))) (begin
    (let/ec _cont
      (if (let ([__l (if left (if (hash? left) (hash-ref left 0 #f) (safe-index left (int 0))) #f)] [__r (if right (if (hash? right) (hash-ref right 0 #f) (safe-index right (int 0))) #f)]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ()
(set! result (appendv (or result (vector)) (vector (if left (if (hash? left) (hash-ref left 0 #f) (safe-index left (int 0))) #f))))
(set! left (slice left 1 (cond [(vector? left) (vector-length left)] [else (length left)])))
) (let ()
(set! result (appendv (or result (vector)) (vector (if right (if (hash? right) (hash-ref right 0 #f) (safe-index right (int 0))) #f))))
(set! right (slice right 1 (cond [(vector? right) (vector-length right)] [else (length right)])))
))
    )
    (loop)) (void))))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not left) 0] [(string? left) (string-length left)] [(vector? left) (vector-length left)] [(hash? left) (hash-count left)] [else (length left)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (appendv (or result (vector)) (vector (if left (if (hash? left) (hash-ref left i #f) (safe-index left (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not right) 0] [(string? right) (string-length right)] [(vector? right) (vector-length right)] [(hash? right) (hash-count right)] [else (length right)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! result (appendv (or result (vector)) (vector (if right (if (hash? right) (hash-ref right i #f) (safe-index right (int i))) #f))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not result) 0] [(string? result) (string-length result)] [(vector? result) (vector-length result)] [(hash? result) (hash-count result)] [else (length result)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! a (int (let ([__l low] [__r i]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) (if result (if (hash? result) (hash-ref result i #f) (safe-index result (int i))) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return a)
))
)
(define (iter_merge_sort items)
  (let/ec _return (begin
(define n (cond [(not items) 0] [(string? items) (string-length items)] [(vector? items) (vector-length items)] [(hash? items) (hash-count items)] [else (length items)]))
(if (let ([__l n] [__r 1]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (let ()
(_return items)
) (void))
(define arr (slice items 0 (cond [(vector? items) (vector-length items)] [else (length items)])))
(define p 2)
(let/ec _break (let loop ()
  (if (let ([__l p] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define high (- (let ([__l i] [__r p]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 1))
      (if (let ([__l high] [__r n]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(set! high (- n 1))
) (void))
      (define low i)
      (define mid (floor (/ (let ([__l (let ([__l low] [__r high]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2)))
      (set! arr (merge arr low mid high))
      (set! i (let ([__l i] [__r p]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (if (let ([__l (* p 2)] [__r n]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ()
(define mid2 (- i p))
(set! arr (merge arr 0 mid2 (- n 1)))
(_break)
) (void))
      (set! p (* p 2))
    )
    (loop)) (void))))
(_return arr)
))
)
(define (list_to_string arr)
  (let/ec _return (begin
(define s "[")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! s (string-append s (to-string (if arr (if (hash? arr) (hash-ref arr i #f) (safe-index arr (int i))) #f))))
      (if (let ([__l i] [__r (- (cond [(not arr) 0] [(string? arr) (string-length arr)] [(vector? arr) (vector-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 1)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! s (string-append s ", "))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return (string-append s "]"))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (list_to_string (iter_merge_sort (vector 5 9 8 7 1 2 7))))
(displayln (list_to_string (iter_merge_sort (vector 1))))
(displayln (list_to_string (iter_merge_sort (vector 2 1))))
(displayln (list_to_string (iter_merge_sort (vector 4 3 2 1))))
(displayln (list_to_string (iter_merge_sort (vector 5 4 3 2 1))))
(displayln (list_to_string (iter_merge_sort (vector (- 2) (- 9) (- 1) (- 4)))))
(displayln (list_to_string (iter_merge_sort (vector))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
