;; Generated by Mochi 0.10.71 on 2025-08-24 08:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/github/TheAlgorithms/Mochi/sorts")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (if (and (inexact? x) (integer? x))
         (number->string (inexact->exact x))
         (number->string x))]
    [(vector? x) (format "~a" (vector->list x))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [else (length seq)]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a b)]
    [(list? a) (append a (vector->list b))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) '())] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) '())] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (bisect_left stacks value)
  (let/ec _return (begin
(define low 0)
(define high (cond [(not stacks) 0] [(string? stacks) (string-length stacks)] [(vector? stacks) (vector-length stacks)] [(hash? stacks) (hash-count stacks)] [else (length stacks)]))
(let/ec _break (let loop ()
  (if (let ([__l low] [__r high]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define mid (floor (/ (let ([__l low] [__r high]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2)))
      (define stack (if stacks (if (hash? stacks) (hash-ref stacks mid #f) (safe-index stacks (int mid))) #f))
      (define top_idx (- (cond [(not stack) 0] [(string? stack) (string-length stack)] [(vector? stack) (vector-length stack)] [(hash? stack) (hash-count stack)] [else (length stack)]) 1))
      (define top (if stack (if (hash? stack) (hash-ref stack top_idx #f) (safe-index stack (int top_idx))) #f))
      (if (let ([__l top] [__r value]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! low (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! high mid)
))
    )
    (loop)) (void))))
(_return low)
))
)
(define (reverse_list src)
  (let/ec _return (begin
(define res (vector))
(define i (- (cond [(not src) 0] [(string? src) (string-length src)] [(vector? src) (vector-length src)] [(hash? src) (hash-count src)] [else (length src)]) 1))
(let/ec _break (let loop ()
  (if (let ([__l i] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! res (appendv (or res (vector)) (vector (if src (if (hash? src) (hash-ref src i #f) (safe-index src (int i))) #f))))
      (set! i (- i 1))
    )
    (loop)) (void))))
(_return res)
))
)
(define (patience_sort collection)
  (let/ec _return (begin
(define stacks (vector))
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not collection) 0] [(string? collection) (string-length collection)] [(vector? collection) (vector-length collection)] [(hash? collection) (hash-count collection)] [else (length collection)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define element (if collection (if (hash? collection) (hash-ref collection i #f) (safe-index collection (int i))) #f))
      (define idx (bisect_left stacks element))
      (if (not (equal? idx (cond [(not stacks) 0] [(string? stacks) (string-length stacks)] [(vector? stacks) (vector-length stacks)] [(hash? stacks) (hash-count stacks)] [else (length stacks)]))) (let ()
(define stack (if stacks (if (hash? stacks) (hash-ref stacks idx #f) (safe-index stacks (int idx))) #f))
(vector-set! stacks (int idx) (appendv (or stack (vector)) (vector element)))
) (let ()
(define new_stack (vector element))
(set! stacks (appendv (or stacks (vector)) (vector new_stack)))
))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not stacks) 0] [(string? stacks) (string-length stacks)] [(vector? stacks) (vector-length stacks)] [(hash? stacks) (hash-count stacks)] [else (length stacks)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! stacks (int i) (reverse_list (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define indices (vector))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not stacks) 0] [(string? stacks) (string-length stacks)] [(vector? stacks) (vector-length stacks)] [(hash? stacks) (hash-count stacks)] [else (length stacks)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! indices (appendv (or indices (vector)) (vector 0)))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define total 0)
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not stacks) 0] [(string? stacks) (string-length stacks)] [(vector? stacks) (vector-length stacks)] [(hash? stacks) (hash-count stacks)] [else (length stacks)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! total (let ([__l total] [__r (cond [(not (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f)) 0] [(string? (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f)) (string-length (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f))] [(vector? (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f)) (vector-length (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f))] [(hash? (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f)) (hash-count (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f))] [else (length (if stacks (if (hash? stacks) (hash-ref stacks i #f) (safe-index stacks (int i))) #f))])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define result (vector))
(define count 0)
(let/ec _break (let loop ()
  (if (let ([__l count] [__r total]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define min_val 0)
      (define min_stack (- 1))
      (define j 0)
      (let/ec _break (let loop ()
  (if (let ([__l j] [__r (cond [(not stacks) 0] [(string? stacks) (string-length stacks)] [(vector? stacks) (vector-length stacks)] [(hash? stacks) (hash-count stacks)] [else (length stacks)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define idx (if indices (if (hash? indices) (hash-ref indices j #f) (safe-index indices (int j))) #f))
      (if (let ([__l idx] [__r (cond [(not (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f)) 0] [(string? (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f)) (string-length (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f))] [(vector? (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f)) (vector-length (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f))] [(hash? (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f)) (hash-count (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f))] [else (length (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f))])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(define val (if (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f) (if (hash? (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f)) (hash-ref (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f) idx #f) (safe-index (if stacks (if (hash? stacks) (hash-ref stacks j #f) (safe-index stacks (int j))) #f) (int idx))) #f))
(if (let ([__l min_stack] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! min_val val)
(set! min_stack j)
) (let ()
(if (let ([__l val] [__r min_val]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(set! min_val val)
(set! min_stack j)
) (void))
))
) (void))
      (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (set! result (appendv (or result (vector)) (vector min_val)))
      (vector-set! indices (int min_stack) (let ([__l (if indices (if (hash? indices) (hash-ref indices min_stack #f) (safe-index indices (int min_stack))) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(set! i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not result) 0] [(string? result) (string-length result)] [(vector? result) (vector-length result)] [(hash? result) (hash-count result)] [else (length result)])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! collection (int i) (if result (if (hash? result) (hash-ref result i #f) (safe-index result (int i))) #f))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return collection)
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(displayln (to-string (patience_sort (vector 1 9 5 21 17 6))))
(displayln (to-string (patience_sort (vector))))
(displayln (to-string (patience_sort (vector (- 3) (- 17) (- 48)))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
