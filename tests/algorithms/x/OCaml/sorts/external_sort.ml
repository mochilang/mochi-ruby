(* Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:20 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec subarray xs start end_ =
  let __ret = ref ([] : (int) list) in
  (try
  let xs = (Obj.magic xs : int list) in
  let start = (Obj.magic start : int) in
  let end_ = (Obj.magic end_ : int) in
  let result = ref (([] : (int) list)) in
  let k = ref (start) in
  (try while (!k < end_) do
    try
  result := (Obj.magic (List.append (!result) [(Obj.magic ((let __l = xs in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : int)]) : int list);
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and merge left_half right_half =
  let __ret = ref ([] : (int) list) in
  (try
  let left_half = (Obj.magic left_half : int list) in
  let right_half = (Obj.magic right_half : int list) in
  let result = ref (([] : (int) list)) in
  let i = ref (0) in
  let j = ref (0) in
  (try while ((!i < List.length (left_half)) && (!j < List.length (right_half))) do
    try
  if ((let __l = left_half in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) < (let __l = right_half in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) then (
  result := (Obj.magic (List.append (!result) [(Obj.magic ((let __l = left_half in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : int)]) : int list);
  i := (!i + 1);
  ) else (
  result := (Obj.magic (List.append (!result) [(Obj.magic ((let __l = right_half in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : int)]) : int list);
  j := (!j + 1);
  );
    with Continue -> ()
  done with Break -> ());
  (try while (!i < List.length (left_half)) do
    try
  result := (Obj.magic (List.append (!result) [(Obj.magic ((let __l = left_half in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : int)]) : int list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  (try while (!j < List.length (right_half)) do
    try
  result := (Obj.magic (List.append (!result) [(Obj.magic ((let __l = right_half in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : int)]) : int list);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and merge_sort array =
  let __ret = ref ([] : (int) list) in
  (try
  let array = (Obj.magic array : int list) in
  if (List.length (array) <= 1) then (
  __ret := (Obj.magic (array) : int list); raise Return
  );
  let middle = (List.length (array) / 2) in
  let left_half = ref (subarray (array) (Obj.repr (0)) (Obj.repr (middle))) in
  let right_half = ref (subarray (array) (Obj.repr (middle)) (Obj.repr (List.length (array)))) in
  let sorted_left = ref (merge_sort (!left_half)) in
  let sorted_right = ref (merge_sort (!right_half)) in
  __ret := (Obj.magic (merge (!sorted_left) (!sorted_right)) : int list); raise Return
  with Return -> !__ret)

and split_into_blocks data block_size =
  let __ret = ref ([] : (int list) list) in
  (try
  let data = (Obj.magic data : int list) in
  let block_size = (Obj.magic block_size : int) in
  let blocks = ref (([] : (int list) list)) in
  let i = ref (0) in
  (try while (!i < List.length (data)) do
    try
  let end_ = if ((!i + block_size) < List.length (data)) then (!i + block_size) else List.length (data) in
  let block = ref (subarray (data) (Obj.repr (!i)) (Obj.repr (end_))) in
  let sorted_block = ref (merge_sort (!block)) in
  blocks := (Obj.magic (List.append (!blocks) [(Obj.magic (!sorted_block) : int list)]) : int list list);
  i := end_;
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!blocks) : int list list); raise Return
  with Return -> !__ret)

and merge_blocks blocks =
  let __ret = ref ([] : (int) list) in
  (try
  let blocks = (Obj.magic blocks : int list list) in
  let num_blocks = List.length (blocks) in
  let indices = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < num_blocks) do
    try
  indices := (Obj.magic (List.append (!indices) [(Obj.magic (0) : int)]) : int list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let result = ref (([] : (int) list)) in
  let done_ = ref (false) in
  (try while not (!done_) do
    try
  done_ := true;
  let min_val = ref (0) in
  let min_block = ref ((0 - 1)) in
  let j = ref (0) in
  (try while (!j < num_blocks) do
    try
  let idx = (let __l = !indices in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in
  if (idx < List.length ((let __l = blocks in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) then (
  let val_ = (let __l = (let __l = blocks in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in
  if ((!min_block = (0 - 1)) || (val_ < !min_val)) then (
  min_val := val_;
  min_block := !j;
  );
  done_ := false;
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  if not (!done_) then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (!min_val) : int)]) : int list);
  indices := (List.mapi (fun __i __x -> if __i = !min_block then ((let __l = !indices in let __i = !min_block in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) + 1) else __x) (!indices));
  );
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and external_sort data block_size =
  let __ret = ref ([] : (int) list) in
  (try
  let data = (Obj.magic data : int list) in
  let block_size = (Obj.magic block_size : int) in
  let blocks = ref (split_into_blocks (data) (Obj.repr (block_size))) in
  __ret := (Obj.magic (merge_blocks (!blocks)) : int list); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let data = ref ([7; 1; 5; 3; 9; 2; 6; 4; 8; 0]) in
  let sorted_data = ref (external_sort (!data) (Obj.repr (3))) in
  print_endline (__show_list !sorted_data);
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
