(* Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:24 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec dfs_skip graph visited skip at =
  let __ret = ref 0 in
  (try
  let graph = (Obj.magic graph : int list list) in
  visited := (Obj.magic !visited : bool list);
  let skip = (Obj.magic skip : int) in
  let at = (Obj.magic at : int) in
  visited := (List.mapi (fun __i __x -> if __i = at then true else __x) (!visited));
  let count = ref (1) in
  (try List.iter (fun to_ ->
    try
  if (to_ = skip) then (
  raise Continue;
  );
  if (List.nth (!visited) (to_) = false) then (
  count := (!count + dfs_skip (graph) (visited) (Obj.repr (skip)) (Obj.repr (to_)));
  );
    with Continue -> ()) (List.nth (graph) (at)) with Break -> ());
  __ret := (Obj.magic (!count) : int); raise Return
  with Return -> !__ret)

and articulation_points graph =
  let __ret = ref ([] : (int) list) in
  (try
  let graph = (Obj.magic graph : int list list) in
  let n = List.length (graph) in
  let result = ref (([] : (int) list)) in
  let v = ref (0) in
  (try while (!v < n) do
    try
  let visited = ref (([] : (bool) list)) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  visited := (Obj.magic (List.append (!visited) [(Obj.magic (false) : bool)]) : bool list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let start = ref (0) in
  (try while ((!start = !v) && (!start < n)) do
    try
  start := (!start + 1);
    with Continue -> ()
  done with Break -> ());
  let reach = dfs_skip (graph) (visited) (Obj.repr (!v)) (Obj.repr (!start)) in
  if (reach < (n - 1)) then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (!v) : int)]) : int list);
  print_endline (string_of_int !v);
  );
  v := (!v + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let graph = ref ([[1; 2]; [0; 2]; [0; 1; 3; 5]; [2; 4]; [3]; [2; 6; 8]; [5; 7]; [6; 8]; [5; 7]]) in
  ignore (articulation_points (!graph));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
