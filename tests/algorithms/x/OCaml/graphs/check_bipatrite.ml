(* Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:24 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec is_bipartite_bfs graph =
  let __ret = ref false in
  (try
  let graph = (Obj.magic graph : (string * int list) list) in
  let visited = ref ([] : (string * int) list) in
  (try List.iter (fun (node, _) ->
    try
  if not ((List.mem_assoc node !visited)) then (
  let queue = ref (([] : (int) list)) in
  queue := (Obj.magic (List.append (!queue) [(Obj.magic (node) : int)]) : int list);
  visited := ((__str (node), 0) :: List.remove_assoc (__str (node)) (!visited));
  (try while (List.length (!queue) > 0) do
    try
  let curr = List.nth (!queue) (0) in
  queue := (Obj.magic List.of_seq (Seq.take (List.length (!queue) - 1) (Seq.drop 1 (List.to_seq (!queue)))) : int list);
  (try List.iter (fun neighbor ->
    try
  if not ((List.mem_assoc neighbor !visited)) then (
  visited := ((__str (neighbor), (1 - List.assoc (__str (curr)) (!visited))) :: List.remove_assoc (__str (neighbor)) (!visited));
  queue := (Obj.magic (List.append (!queue) [(Obj.magic (neighbor) : int)]) : int list);
  ) else (
  if (List.assoc (__str (neighbor)) (!visited) = List.assoc (__str (curr)) (!visited)) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  );
    with Continue -> ()) (List.assoc (__str (curr)) (graph)) with Break -> ());
    with Continue -> ()
  done with Break -> ());
  );
    with Continue -> ()) (graph) with Break -> ());
  __ret := (Obj.magic (true) : bool); raise Return
  with Return -> !__ret)


let graph = ref ([(__str (0), [1; 3]); (__str (1), [0; 2]); (__str (2), [1; 3]); (__str (3), [0; 2])])
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((string_of_bool (is_bipartite_bfs ((Obj.magic !graph : (string * int list) list)))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
