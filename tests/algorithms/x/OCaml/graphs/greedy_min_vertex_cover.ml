(* Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:24 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec remove_value lst val_ =
  let __ret = ref ([] : (int) list) in
  (try
  let lst = (Obj.magic lst : int list) in
  let val_ = (Obj.magic val_ : int) in
  let res = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < List.length (lst)) do
    try
  if (List.nth (lst) (!i) <> val_) then (
  res := (Obj.magic (List.append (!res) [(Obj.magic (List.nth (lst) (!i)) : int)]) : int list);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : int list); raise Return
  with Return -> !__ret)

and greedy_min_vertex_cover graph =
  let __ret = ref ([] : (int) list) in
  (try
  let graph = (Obj.magic graph : (string * int list) list) in
  let g = ref (graph) in
  let cover = ref (([] : (int) list)) in
  (try while true do
    try
  let max_v = ref (0) in
  let max_deg = ref (0) in
  (try List.iter (fun (v, _) ->
    try
  let key = int_of_string (v) in
  let deg = List.length (List.assoc (__str (key)) (!g)) in
  if (deg > !max_deg) then (
  max_deg := deg;
  max_v := key;
  );
    with Continue -> ()) (!g) with Break -> ());
  if (!max_deg = 0) then (
  raise Break;
  );
  cover := (Obj.magic (List.append (!cover) [(Obj.magic (!max_v) : int)]) : int list);
  let neighbors = ref (List.assoc (__str (!max_v)) (!g)) in
  let i = ref (0) in
  (try while (!i < List.length (!neighbors)) do
    try
  let n = List.nth (!neighbors) (!i) in
  g := ((__str (n), remove_value (List.assoc (__str (n)) (!g)) (Obj.repr (!max_v))) :: List.remove_assoc (__str (n)) (!g));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  g := ((__str (!max_v), []) :: List.remove_assoc (__str (!max_v)) (!g));
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!cover) : int list); raise Return
  with Return -> !__ret)


let graph = ref ([(__str (0), [1; 3]); (__str (1), [0; 3]); (__str (2), [0; 3; 4]); (__str (3), [0; 1; 2]); (__str (4), [2; 3])])
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (__show_list (greedy_min_vertex_cover ((Obj.magic !graph : (string * int list) list))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
