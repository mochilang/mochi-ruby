(* Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:24 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec contains xs x =
  let __ret = ref false in
  (try
  let xs = (Obj.magic xs : string list) in
  let i = ref (0) in
  (try while (!i < List.length (xs)) do
    try
  if (List.nth (xs) (!i) = x) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and contains_key m key =
  let __ret = ref false in
  (try
  let m = (Obj.magic m : (string * string list) list) in
  (try List.iter (fun (k, _) ->
    try
  if (k = key) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
    with Continue -> ()) (m) with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and bfs_shortest_path graph start goal =
  let __ret = ref ([] : (string) list) in
  (try
  let graph = (Obj.magic graph : (string * string list) list) in
  let explored = ref (([] : (string) list)) in
  let queue = ref ([[start]]) in
  if (start = goal) then (
  __ret := (Obj.magic ([start]) : string list); raise Return
  );
  (try while (List.length (!queue) > 0) do
    try
  let path = ref (List.nth (!queue) (0)) in
  queue := (Obj.magic List.of_seq (Seq.take (List.length (!queue) - 1) (Seq.drop 1 (List.to_seq (!queue)))) : string list list);
  let node = List.nth (!path) ((List.length (!path) - 1)) in
  if not (contains (!explored) (node)) then (
  let neighbours = ref (List.assoc (__str (node)) (graph)) in
  let i = ref (0) in
  (try while (!i < List.length (!neighbours)) do
    try
  let neighbour = List.nth (!neighbours) (!i) in
  let new_path = ref (!path) in
  new_path := (Obj.magic (List.append (!new_path) [(Obj.magic (neighbour) : string)]) : string list);
  queue := (Obj.magic (List.append (!queue) [(Obj.magic (!new_path) : string list)]) : string list list);
  if (neighbour = goal) then (
  __ret := (Obj.magic (!new_path) : string list); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  explored := (Obj.magic (List.append (!explored) [(Obj.magic (node) : string)]) : string list);
  );
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ([]) : string list); raise Return
  with Return -> !__ret)

and bfs_shortest_path_distance graph start target =
  let __ret = ref 0 in
  (try
  let graph = (Obj.magic graph : (string * string list) list) in
  if ((contains_key ((Obj.magic graph : (string * string list) list)) (start) = false) || (contains_key ((Obj.magic graph : (string * string list) list)) (target) = false)) then (
  __ret := (Obj.magic (-(1)) : int); raise Return
  );
  if (start = target) then (
  __ret := (Obj.magic (0) : int); raise Return
  );
  let queue = ref ([start]) in
  let visited = ref ([start]) in
  let dist = ref ([] : (string * int) list) in
  dist := ((__str (start), 0) :: List.remove_assoc (__str (start)) (!dist));
  dist := ((__str (target), -(1)) :: List.remove_assoc (__str (target)) (!dist));
  (try while (List.length (!queue) > 0) do
    try
  let node = List.nth (!queue) (0) in
  queue := (Obj.magic List.of_seq (Seq.take (List.length (!queue) - 1) (Seq.drop 1 (List.to_seq (!queue)))) : string list);
  if (node = target) then (
  if ((List.assoc (__str (target)) (!dist) = -(1)) || (List.assoc (__str (node)) (!dist) < List.assoc (__str (target)) (!dist))) then (
  dist := ((__str (target), List.assoc (__str (node)) (!dist)) :: List.remove_assoc (__str (target)) (!dist));
  );
  );
  let adj = ref (List.assoc (__str (node)) (graph)) in
  let i = ref (0) in
  (try while (!i < List.length (!adj)) do
    try
  let next = List.nth (!adj) (!i) in
  if not (contains (!visited) (next)) then (
  visited := (Obj.magic (List.append (!visited) [(Obj.magic (next) : string)]) : string list);
  queue := (Obj.magic (List.append (!queue) [(Obj.magic (next) : string)]) : string list);
  dist := ((__str (next), (List.assoc (__str (node)) (!dist) + 1)) :: List.remove_assoc (__str (next)) (!dist));
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (List.assoc (__str (target)) (!dist)) : int); raise Return
  with Return -> !__ret)


let demo_graph = ref ([(__str ("A"), ["B"; "C"; "E"]); (__str ("B"), ["A"; "D"; "E"]); (__str ("C"), ["A"; "F"; "G"]); (__str ("D"), ["B"]); (__str ("E"), ["A"; "B"; "D"]); (__str ("F"), ["C"]); (__str ("G"), ["C"])])
let () =
  let mem_start = _mem () in
  let start = _now () in
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
