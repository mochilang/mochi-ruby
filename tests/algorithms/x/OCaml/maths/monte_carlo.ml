(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 07:47 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _pi = 3.141592653589793
let rand_seed = ref (123456789)
let rec rand_float () =
  let __ret = ref 0.0 in
  (try
  rand_seed := ((((1103515245 * !rand_seed) + 12345) mod 2147483648 + 2147483648) mod 2147483648);
  __ret := (Obj.magic ((float_of_int (!rand_seed) /. 2147483648.0)) : float); raise Return
  with Return -> !__ret)

and rand_range min_val max_val =
  let __ret = ref 0.0 in
  (try
  let min_val = (Obj.magic min_val : float) in
  let max_val = (Obj.magic max_val : float) in
  __ret := (Obj.magic (((rand_float () *. (max_val -. min_val)) +. min_val)) : float); raise Return
  with Return -> !__ret)

and abs_float x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x < 0.0) then (
  __ret := (Obj.magic ((-.(x))) : float); raise Return
  );
  __ret := (Obj.magic (x) : float); raise Return
  with Return -> !__ret)

and sqrtApprox x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x = 0.0) then (
  __ret := (Obj.magic (0.0) : float); raise Return
  );
  let guess = ref ((x /. 2.0)) in
  let i = ref (0) in
  (try while (!i < 20) do
    try
  guess := ((!guess +. (x /. !guess)) /. 2.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!guess) : float); raise Return
  with Return -> !__ret)

and pi_estimator iterations =
  let __ret = ref (Obj.magic 0) in
  (try
  let iterations = (Obj.magic iterations : int) in
  let inside = ref (0.0) in
  let i = ref (0) in
  (try while (!i < iterations) do
    try
  let x = rand_range (Obj.repr ((-.(1.0)))) (Obj.repr (1.0)) in
  let y = rand_range (Obj.repr ((-.(1.0)))) (Obj.repr (1.0)) in
  if (((x *. x) +. (y *. y)) <= 1.0) then (
  inside := (!inside +. 1.0);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let proportion = (!inside /. float_of_int (iterations)) in
  let pi_estimate = (proportion *. 4.0) in
  print_endline ("The estimated value of pi is");
  print_endline (Printf.sprintf "%.15f" (pi_estimate));
  print_endline ("The numpy value of pi is");
  print_endline (Printf.sprintf "%.15f" (_pi));
  print_endline ("The total error is");
  print_endline (string_of_float (abs_float (Obj.repr ((_pi -. pi_estimate)))));
    !__ret
  with Return -> !__ret)

and area_under_curve_estimator iterations f min_value max_value =
  let __ret = ref 0.0 in
  (try
  let iterations = (Obj.magic iterations : int) in
  let min_value = (Obj.magic min_value : float) in
  let max_value = (Obj.magic max_value : float) in
  let sum = ref (0.0) in
  let i = ref (0) in
  (try while (!i < iterations) do
    try
  let x = rand_range (Obj.repr (min_value)) (Obj.repr (max_value)) in
  sum := (!sum +. f (Obj.repr (x)));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let expected = (!sum /. float_of_int (iterations)) in
  __ret := (Obj.magic ((expected *. (max_value -. min_value))) : float); raise Return
  with Return -> !__ret)

and area_under_line_estimator_check iterations min_value max_value =
  let __ret = ref (Obj.magic 0) in
  (try
  let iterations = (Obj.magic iterations : int) in
  let min_value = (Obj.magic min_value : float) in
  let max_value = (Obj.magic max_value : float) in
let rec identity_function x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  __ret := (Obj.magic (x) : float); raise Return
  with Return -> !__ret) in
  let estimated_value = area_under_curve_estimator (Obj.repr (iterations)) (identity_function) (Obj.repr (min_value)) (Obj.repr (max_value)) in
  let expected_value = (((max_value *. max_value) -. (min_value *. min_value)) /. 2.0) in
  print_endline ("******************");
  print_endline ("Estimating area under y=x where x varies from");
  print_endline (Printf.sprintf "%.15f" (min_value));
  print_endline ("Estimated value is");
  print_endline (Printf.sprintf "%.15f" (estimated_value));
  print_endline ("Expected value is");
  print_endline (Printf.sprintf "%.15f" (expected_value));
  print_endline ("Total error is");
  print_endline (string_of_float (abs_float (Obj.repr ((estimated_value -. expected_value)))));
  print_endline ("******************");
    !__ret
  with Return -> !__ret)

and pi_estimator_using_area_under_curve iterations =
  let __ret = ref (Obj.magic 0) in
  (try
  let iterations = (Obj.magic iterations : int) in
let rec semi_circle x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  let y = (4.0 -. (x *. x)) in
  let s = sqrtApprox (Obj.repr (y)) in
  __ret := (Obj.magic (s) : float); raise Return
  with Return -> !__ret) in
  let estimated_value = area_under_curve_estimator (Obj.repr (iterations)) (semi_circle) (Obj.repr (0.0)) (Obj.repr (2.0)) in
  print_endline ("******************");
  print_endline ("Estimating pi using area_under_curve_estimator");
  print_endline ("Estimated value is");
  print_endline (Printf.sprintf "%.15f" (estimated_value));
  print_endline ("Expected value is");
  print_endline (Printf.sprintf "%.15f" (_pi));
  print_endline ("Total error is");
  print_endline (string_of_float (abs_float (Obj.repr ((estimated_value -. _pi)))));
  print_endline ("******************");
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (pi_estimator (Obj.repr (1000)));
  ignore (area_under_line_estimator_check (Obj.repr (1000)) (Obj.repr (0.0)) (Obj.repr (1.0)));
  ignore (pi_estimator_using_area_under_curve (Obj.repr (1000)));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
