(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 07:47 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec slow_primes max_n =
  let __ret = ref ([] : (int) list) in
  (try
  let max_n = (Obj.magic max_n : int) in
  let result = ref (([] : (int) list)) in
  let i = ref (2) in
  (try while (!i <= max_n) do
    try
  let j = ref (2) in
  let is_prime = ref (true) in
  (try while (!j < !i) do
    try
  if (((!i mod !j + !j) mod !j) = 0) then (
  is_prime := false;
  raise Break;
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  if !is_prime then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (!i) : int)]) : int list);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and primes max_n =
  let __ret = ref ([] : (int) list) in
  (try
  let max_n = (Obj.magic max_n : int) in
  let result = ref (([] : (int) list)) in
  let i = ref (2) in
  (try while (!i <= max_n) do
    try
  let j = ref (2) in
  let is_prime = ref (true) in
  (try while ((!j * !j) <= !i) do
    try
  if (((!i mod !j + !j) mod !j) = 0) then (
  is_prime := false;
  raise Break;
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  if !is_prime then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (!i) : int)]) : int list);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and fast_primes max_n =
  let __ret = ref ([] : (int) list) in
  (try
  let max_n = (Obj.magic max_n : int) in
  let result = ref (([] : (int) list)) in
  if (max_n >= 2) then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (2) : int)]) : int list);
  );
  let i = ref (3) in
  (try while (!i <= max_n) do
    try
  let j = ref (3) in
  let is_prime = ref (true) in
  (try while ((!j * !j) <= !i) do
    try
  if (((!i mod !j + !j) mod !j) = 0) then (
  is_prime := false;
  raise Break;
  );
  j := (!j + 2);
    with Continue -> ()
  done with Break -> ());
  if !is_prime then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (!i) : int)]) : int list);
  );
  i := (!i + 2);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((__str (slow_primes (Obj.repr (25)))));
  print_endline ((__str (primes (Obj.repr (25)))));
  print_endline ((__str (fast_primes (Obj.repr (25)))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
