(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 07:47 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec min_int a b =
  let __ret = ref 0 in
  (try
  let a = (Obj.magic a : int) in
  let b = (Obj.magic b : int) in
  if (a < b) then (
  __ret := (Obj.magic (a) : int); raise Return
  );
  __ret := (Obj.magic (b) : int); raise Return
  with Return -> !__ret)

and int_sqrt n =
  let __ret = ref 0 in
  (try
  let n = (Obj.magic n : int) in
  let r = ref (0) in
  (try while (((!r + 1) * (!r + 1)) <= n) do
    try
  r := (!r + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!r) : int); raise Return
  with Return -> !__ret)

and sieve n =
  let __ret = ref ([] : (int) list) in
  (try
  let n = (Obj.magic n : int) in
  if (n <= 0) then (
  (failwith ("Number must instead be a positive integer"));
  );
  let in_prime = ref (([] : (int) list)) in
  let start = ref (2) in
  let end_ = int_sqrt (Obj.repr (n)) in
  let temp = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < (end_ + 1)) do
    try
  temp := (Obj.magic (List.append (!temp) [(Obj.magic (1) : int)]) : int list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let prime = ref (([] : (int) list)) in
  (try while (!start <= end_) do
    try
  if ((let __l = !temp in let __i = !start in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) = 1) then (
  in_prime := (Obj.magic (List.append (!in_prime) [(Obj.magic (!start) : int)]) : int list);
  let j = ref ((!start * !start)) in
  (try while (!j <= end_) do
    try
  temp := (List.mapi (fun __i __x -> if __i = !j then 0 else __x) (!temp));
  j := (!j + !start);
    with Continue -> ()
  done with Break -> ());
  );
  start := (!start + 1);
    with Continue -> ()
  done with Break -> ());
  i := 0;
  (try while (!i < List.length (!in_prime)) do
    try
  prime := (Obj.magic (List.append (!prime) [(Obj.magic ((let __l = !in_prime in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : int)]) : int list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let low = ref ((end_ + 1)) in
  let high = ref (min_int (Obj.repr ((2 * end_))) (Obj.repr (n))) in
  (try while (!low <= n) do
    try
  let tempSeg = ref (([] : (int) list)) in
  let size = ref (((!high - !low) + 1)) in
  let k = ref (0) in
  (try while (!k < !size) do
    try
  tempSeg := (Obj.magic (List.append (!tempSeg) [(Obj.magic (1) : int)]) : int list);
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  let idx = ref (0) in
  (try while (!idx < List.length (!in_prime)) do
    try
  let each = (let __l = !in_prime in let __i = !idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in
  let t = ref (((!low / each) * each)) in
  if (!t < !low) then (
  t := (!t + each);
  );
  let j2 = ref (!t) in
  (try while (!j2 <= !high) do
    try
  tempSeg := (List.mapi (fun __i __x -> if __i = (!j2 - !low) then 0 else __x) (!tempSeg));
  j2 := (!j2 + each);
    with Continue -> ()
  done with Break -> ());
  idx := (!idx + 1);
    with Continue -> ()
  done with Break -> ());
  let j3 = ref (0) in
  (try while (!j3 < List.length (!tempSeg)) do
    try
  if ((let __l = !tempSeg in let __i = !j3 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) = 1) then (
  prime := (Obj.magic (List.append (!prime) [(Obj.magic ((!j3 + !low)) : int)]) : int list);
  );
  j3 := (!j3 + 1);
    with Continue -> ()
  done with Break -> ());
  low := (!high + 1);
  high := min_int (Obj.repr ((!high + end_))) (Obj.repr (n));
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!prime) : int list); raise Return
  with Return -> !__ret)

and lists_equal a b =
  let __ret = ref false in
  (try
  let a = (Obj.magic a : int list) in
  let b = (Obj.magic b : int list) in
  if (List.length (a) <> List.length (b)) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let m = ref (0) in
  (try while (!m < List.length (a)) do
    try
  if ((let __l = a in let __i = !m in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) <> (let __l = b in let __i = !m in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  m := (!m + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (true) : bool); raise Return
  with Return -> !__ret)

and test_sieve () =
  let __ret = ref (Obj.magic 0) in
  (try
  let e1 = ref (sieve (Obj.repr (8))) in
  if not (lists_equal (!e1) ([2; 3; 5; 7])) then (
  (failwith ("sieve(8) failed"));
  );
  let e2 = ref (sieve (Obj.repr (27))) in
  if not (lists_equal (!e2) ([2; 3; 5; 7; 11; 13; 17; 19; 23])) then (
  (failwith ("sieve(27) failed"));
  );
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (test_sieve ());
  print_endline ((__str (sieve (Obj.repr (30)))));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
