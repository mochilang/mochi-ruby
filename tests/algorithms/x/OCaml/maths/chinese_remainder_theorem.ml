(* Generated by Mochi transpiler v0.10.67 on 2025-08-16 21:23 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0 else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Return

let rec extended_euclid a b =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let a = (Obj.magic a : int) in
  let b = (Obj.magic b : int) in
  if (b = 0) then (
  __ret := (Obj.magic ([(__str ("x"), Obj.repr (1)); (__str ("y"), Obj.repr (0))]) : (string * Obj.t) list); raise Return
  );
  let res = ref (extended_euclid (Obj.repr (b)) (Obj.repr (((a mod b + b) mod b)))) in
  let k = (a / b) in
  __ret := (Obj.magic ([(__str ("x"), Obj.repr ((match List.assoc_opt (__str ("y")) (!res) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0))); (__str ("y"), Obj.repr (((match List.assoc_opt (__str ("x")) (!res) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0) - (k * (match List.assoc_opt (__str ("y")) (!res) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0)))))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and chinese_remainder_theorem n1 r1 n2 r2 =
  let __ret = ref 0 in
  (try
  let n1 = (Obj.magic n1 : int) in
  let r1 = (Obj.magic r1 : int) in
  let n2 = (Obj.magic n2 : int) in
  let r2 = (Obj.magic r2 : int) in
  let res = ref (extended_euclid (Obj.repr (n1)) (Obj.repr (n2))) in
  let x = (match List.assoc_opt (__str ("x")) (!res) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0) in
  let y = (match List.assoc_opt (__str ("y")) (!res) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0) in
  let m = (n1 * n2) in
  let n = (((r2 * x) * n1) + ((r1 * y) * n2)) in
  __ret := (Obj.magic ((((((n mod m + m) mod m) + m) mod m + m) mod m)) : int); raise Return
  with Return -> !__ret)

and invert_modulo a n =
  let __ret = ref 0 in
  (try
  let a = (Obj.magic a : int) in
  let n = (Obj.magic n : int) in
  let res = ref (extended_euclid (Obj.repr (a)) (Obj.repr (n))) in
  let b = ref ((match List.assoc_opt (__str ("x")) (!res) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0)) in
  if (!b < 0) then (
  b := (((((!b mod n + n) mod n) + n) mod n + n) mod n);
  );
  __ret := (Obj.magic (!b) : int); raise Return
  with Return -> !__ret)

and chinese_remainder_theorem2 n1 r1 n2 r2 =
  let __ret = ref 0 in
  (try
  let n1 = (Obj.magic n1 : int) in
  let r1 = (Obj.magic r1 : int) in
  let n2 = (Obj.magic n2 : int) in
  let r2 = (Obj.magic r2 : int) in
  let x = invert_modulo (Obj.repr (n1)) (Obj.repr (n2)) in
  let y = invert_modulo (Obj.repr (n2)) (Obj.repr (n1)) in
  let m = (n1 * n2) in
  let n = (((r2 * x) * n1) + ((r1 * y) * n2)) in
  __ret := (Obj.magic ((((((n mod m + m) mod m) + m) mod m + m) mod m)) : int); raise Return
  with Return -> !__ret)


let e1 = ref (extended_euclid (Obj.repr (10)) (Obj.repr (6)))
let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  print_endline ((((string_of_int (Obj.magic ((match List.assoc_opt (__str ("x")) (!e1) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0)) : int)) ^ ",") ^ (string_of_int (Obj.magic ((match List.assoc_opt (__str ("y")) (!e1) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0)) : int))));
  let e2 = ref (extended_euclid (Obj.repr (7)) (Obj.repr (5))) in
  print_endline ((((string_of_int (Obj.magic ((match List.assoc_opt (__str ("x")) (!e2) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0)) : int)) ^ ",") ^ (string_of_int (Obj.magic ((match List.assoc_opt (__str ("y")) (!e2) with Some v -> (Obj.obj (v : Obj.t) : int) | None -> 0)) : int))));
  print_endline ((string_of_int (Obj.magic (chinese_remainder_theorem (Obj.repr (5)) (Obj.repr (1)) (Obj.repr (7)) (Obj.repr (3))) : int)));
  print_endline ((string_of_int (Obj.magic (chinese_remainder_theorem (Obj.repr (6)) (Obj.repr (1)) (Obj.repr (4)) (Obj.repr (3))) : int)));
  print_endline ((string_of_int (Obj.magic (invert_modulo (Obj.repr (2)) (Obj.repr (5))) : int)));
  print_endline ((string_of_int (Obj.magic (invert_modulo (Obj.repr (8)) (Obj.repr (7))) : int)));
  print_endline ((string_of_int (Obj.magic (chinese_remainder_theorem2 (Obj.repr (5)) (Obj.repr (1)) (Obj.repr (7)) (Obj.repr (3))) : int)));
  print_endline ((string_of_int (Obj.magic (chinese_remainder_theorem2 (Obj.repr (6)) (Obj.repr (1)) (Obj.repr (4)) (Obj.repr (3))) : int)));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
