(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 07:47 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec prime_sieve_eratosthenes num =
  let __ret = ref ([] : (int) list) in
  (try
  let num = (Obj.magic num : int) in
  if (num <= 0) then (
  (failwith ("Input must be a positive integer"));
  );
  let primes = ref (([] : (bool) list)) in
  let i = ref (0) in
  (try while (!i <= num) do
    try
  primes := (Obj.magic (List.append (!primes) [(Obj.magic (true) : bool)]) : bool list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let p = ref (2) in
  (try while ((!p * !p) <= num) do
    try
  if (let __l = !primes in let __i = !p in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) then (
  let j = ref ((!p * !p)) in
  (try while (!j <= num) do
    try
  primes := (List.mapi (fun __i __x -> if __i = !j then false else __x) (!primes));
  j := (!j + !p);
    with Continue -> ()
  done with Break -> ());
  );
  p := (!p + 1);
    with Continue -> ()
  done with Break -> ());
  let result = ref (([] : (int) list)) in
  let k = ref (2) in
  (try while (!k <= num) do
    try
  if (let __l = !primes in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) then (
  result := (Obj.magic (List.append (!result) [(Obj.magic (!k) : int)]) : int list);
  );
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int list); raise Return
  with Return -> !__ret)

and list_eq a b =
  let __ret = ref false in
  (try
  let a = (Obj.magic a : int list) in
  let b = (Obj.magic b : int list) in
  if (List.length (a) <> List.length (b)) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let i = ref (0) in
  (try while (!i < List.length (a)) do
    try
  if ((let __l = a in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) <> (let __l = b in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (true) : bool); raise Return
  with Return -> !__ret)

and test_prime_sieve_eratosthenes () =
  let __ret = ref (Obj.magic 0) in
  (try
  if not (list_eq (prime_sieve_eratosthenes (Obj.repr (10))) ([2; 3; 5; 7])) then (
  (failwith ("test 10 failed"));
  );
  if not (list_eq (prime_sieve_eratosthenes (Obj.repr (20))) ([2; 3; 5; 7; 11; 13; 17; 19])) then (
  (failwith ("test 20 failed"));
  );
  if not (list_eq (prime_sieve_eratosthenes (Obj.repr (2))) ([2])) then (
  (failwith ("test 2 failed"));
  );
  if (List.length (prime_sieve_eratosthenes (Obj.repr (1))) <> 0) then (
  (failwith ("test 1 failed"));
  );
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (test_prime_sieve_eratosthenes ());
  print_endline ((__str (prime_sieve_eratosthenes (Obj.repr (20)))));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
