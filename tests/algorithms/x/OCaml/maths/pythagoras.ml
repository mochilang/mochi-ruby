(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 07:47 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec absf x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x < 0.0) then (
  __ret := (Obj.magic ((-.(x))) : float); raise Return
  );
  __ret := (Obj.magic (x) : float); raise Return
  with Return -> !__ret)

and sqrt_approx x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x <= 0.0) then (
  __ret := (Obj.magic (0.0) : float); raise Return
  );
  let guess = ref ((x /. 2.0)) in
  let i = ref (0) in
  (try while (!i < 20) do
    try
  guess := ((!guess +. (x /. !guess)) /. 2.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!guess) : float); raise Return
  with Return -> !__ret)

and distance a b =
  let __ret = ref 0.0 in
  (try
  let a = (Obj.magic a : (string * Obj.t) list) in
  let b = (Obj.magic b : (string * Obj.t) list) in
  let dx = ((Obj.obj (List.assoc (__str ("x")) (b) : Obj.t) : float) -. (Obj.obj (List.assoc (__str ("x")) (a) : Obj.t) : float)) in
  let dy = ((Obj.obj (List.assoc (__str ("y")) (b) : Obj.t) : float) -. (Obj.obj (List.assoc (__str ("y")) (a) : Obj.t) : float)) in
  let dz = ((Obj.obj (List.assoc (__str ("z")) (b) : Obj.t) : float) -. (Obj.obj (List.assoc (__str ("z")) (a) : Obj.t) : float)) in
  __ret := (Obj.magic (sqrt_approx (Obj.repr (absf (Obj.repr ((((dx *. dx) +. (dy *. dy)) +. (dz *. dz))))))) : float); raise Return
  with Return -> !__ret)

and point_to_string p =
  let __ret = ref "" in
  (try
  let p = (Obj.magic p : (string * Obj.t) list) in
  __ret := (Obj.magic ((((((("Point(" ^ (Printf.sprintf "%.16g" ((Obj.obj (List.assoc (__str ("x")) (p) : Obj.t) : float)))) ^ ", ") ^ (Printf.sprintf "%.16g" ((Obj.obj (List.assoc (__str ("y")) (p) : Obj.t) : float)))) ^ ", ") ^ (Printf.sprintf "%.16g" ((Obj.obj (List.assoc (__str ("z")) (p) : Obj.t) : float)))) ^ ")")) : string); raise Return
  with Return -> !__ret)

and test_distance () =
  let __ret = ref (Obj.magic 0) in
  (try
  let p1 = [(__str ("x"), Obj.repr (2.0)); (__str ("y"), Obj.repr ((-.(1.0)))); (__str ("z"), Obj.repr (7.0))] in
  let p2 = [(__str ("x"), Obj.repr (1.0)); (__str ("y"), Obj.repr ((-.(3.0)))); (__str ("z"), Obj.repr (5.0))] in
  let d = distance (p1) (p2) in
  if (absf (Obj.repr ((d -. 3.0))) > 0.0001) then (
  (failwith ("distance test failed"));
  );
  print_endline (((((("Distance from " ^ point_to_string (p1)) ^ " to ") ^ point_to_string (p2)) ^ " is ") ^ (Printf.sprintf "%.16g" (d))));
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (test_distance ());
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
