(* Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:36 +0700 *)


let rec __is_list v =
  let open Obj in
  let rec loop x =
    let r = repr x in
    if is_int r then (magic r : int) = 0
    else
      match tag r with
      | 0 -> loop (field r 1)
      | _ -> false
  in
  loop v

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec get_avg number_1 number_2 =
  let __ret = ref 0 in
  (try
  let number_1 = (Obj.magic number_1 : int) in
  let number_2 = (Obj.magic number_2 : int) in
  __ret := (Obj.magic (((number_1 + number_2) / 2)) : int); raise Return
  with Return -> !__ret)

and guess_the_number lower higher to_guess =
  let __ret = ref ([] : (int) list) in
  (try
  let lower = (Obj.magic lower : int) in
  let higher = (Obj.magic higher : int) in
  let to_guess = (Obj.magic to_guess : int) in
  if (lower > higher) then (
  (failwith ("argument value for lower and higher must be(lower > higher)"));
  );
  if not (((lower < to_guess) && (to_guess < higher))) then (
  (failwith ("guess value must be within the range of lower and higher value"));
  );
let rec answer number =
  let __ret = ref "" in
  (try
  let number = (Obj.magic number : int) in
  if (number > to_guess) then (
  __ret := (Obj.magic ("high") : string); raise Return
  ) else (
  if (number < to_guess) then (
  __ret := (Obj.magic ("low") : string); raise Return
  ) else (
  __ret := (Obj.magic ("same") : string); raise Return
  );
  );
    !__ret
  with Return -> !__ret) in
  print_endline ("started...");
  let last_lowest = ref (lower) in
  let last_highest = ref (higher) in
  let last_numbers = ref (([] : (int) list)) in
  (try while true do
    try
  let number = get_avg (Obj.repr (!last_lowest)) (Obj.repr (!last_highest)) in
  last_numbers := (Obj.magic ((List.append (!last_numbers) [(Obj.magic (number) : int)])) : int list);
  let resp = answer (Obj.repr (number)) in
  if (resp = "low") then (
  last_lowest := number;
  ) else (
  if (resp = "high") then (
  last_highest := number;
  ) else (
  raise Break;
  );
  );
    with Continue -> ()
  done with Break -> ());
  print_endline (("guess the number : " ^ (string_of_int (Obj.magic ((let __l = !last_numbers in let __i = (List.length (!last_numbers) - 1) in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0)) : int))));
  print_endline (("details : " ^ (__str (Obj.magic (!last_numbers) : Obj.t))));
  __ret := (Obj.magic (!last_numbers) : int list); raise Return
  with Return -> !__ret)


let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  ignore (guess_the_number (Obj.repr (10)) (Obj.repr (1000)) (Obj.repr (17)));
  ignore (guess_the_number (Obj.repr (-(10000))) (Obj.repr (10000)) (Obj.repr (7)));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
