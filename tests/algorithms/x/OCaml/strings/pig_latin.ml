(* Generated by Mochi transpiler v0.10.63 on 2025-08-11 15:32 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _vowels = "aeiou"
let rec strip s =
  let __ret = ref "" in
  (try
  let start = ref (0) in
  let end_ = ref (String.length (s)) in
  (try while ((!start < !end_) && ((let __s = s in let __st = !start in let __len = ((!start + 1) - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "") = " ")) do
    try
  start := (!start + 1);
    with Continue -> ()
  done with Break -> ());
  (try while ((!end_ > !start) && ((let __s = s in let __st = (!end_ - 1) in let __len = (!end_ - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "") = " ")) do
    try
  end_ := (!end_ - 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((let __s = s in let __st = !start in let __len = (!end_ - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "")) : string); raise Return
  with Return -> !__ret)

and is_vowel c =
  let __ret = ref false in
  (try
  let i = ref (0) in
  (try while (!i < String.length (_vowels)) do
    try
  if (c = (let __s = _vowels in let __st = !i in let __len = ((!i + 1) - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "")) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and pig_latin word =
  let __ret = ref "" in
  (try
  let trimmed = strip (word) in
  if (String.length (trimmed) = 0) then (
  __ret := (Obj.magic ("") : string); raise Return
  );
  let w = String.lowercase_ascii (trimmed) in
  let first = (let __s = w in let __st = 0 in let __len = (1 - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "") in
  if is_vowel (first) then (
  __ret := (Obj.magic ((w ^ "way")) : string); raise Return
  );
  let i = ref (0) in
  (try while (!i < String.length (w)) do
    try
  let ch = (let __s = w in let __st = !i in let __len = ((!i + 1) - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "") in
  if is_vowel (ch) then (
  raise Break;
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((((let __s = w in let __st = !i in let __len = (String.length (w) - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "") ^ (let __s = w in let __st = 0 in let __len = (!i - __st) in if __st + __len <= String.length __s then String.sub __s __st __len else "")) ^ "ay")) : string); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (("pig_latin('friends') = " ^ pig_latin ("friends")));
  print_endline (("pig_latin('smile') = " ^ pig_latin ("smile")));
  print_endline (("pig_latin('eat') = " ^ pig_latin ("eat")));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
