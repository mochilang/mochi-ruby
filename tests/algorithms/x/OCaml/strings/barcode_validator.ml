(* Generated by Mochi transpiler v0.10.63 on 2025-08-11 17:23 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec has_alpha s =
  let __ret = ref false in
  (try
  let i = ref (0) in
  (try while (!i < String.length (s)) do
    try
  let c = (let __s = s in let __i = !i in let __len = String.length __s in String.make 1 (String.get __s (if __i >= 0 then __i else __len + __i))) in
  if (((c >= "a") && (c <= "z")) || ((c >= "A") && (c <= "Z"))) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and parse_decimal s =
  let __ret = ref 0 in
  (try
  let value = ref (0) in
  let i = ref (0) in
  (try while (!i < String.length (s)) do
    try
  let c = (let __s = s in let __i = !i in let __len = String.length __s in String.make 1 (String.get __s (if __i >= 0 then __i else __len + __i))) in
  if ((c < "0") || (c > "9")) then (
  (failwith ("Non-digit character encountered"));
  );
  value := ((!value * 10) + int_of_string (c));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!value) : int); raise Return
  with Return -> !__ret)

and get_barcode barcode =
  let __ret = ref 0 in
  (try
  if has_alpha (barcode) then (
  (failwith ((("Barcode '" ^ barcode) ^ "' has alphabetic characters.")));
  );
  if ((String.length (barcode) > 0) && ((let __s = barcode in let __i = 0 in let __len = String.length __s in String.make 1 (String.get __s (if __i >= 0 then __i else __len + __i))) = "-")) then (
  (failwith ("The entered barcode has a negative value. Try again."));
  );
  __ret := (Obj.magic (parse_decimal (barcode)) : int); raise Return
  with Return -> !__ret)

and get_check_digit barcode =
  let __ret = ref 0 in
  (try
  let barcode = (Obj.magic barcode : int) in
  let num = ref ((barcode / 10)) in
  let s = ref (0) in
  let position = ref (0) in
  (try while (!num <> 0) do
    try
  let mult = if (((!position mod 2 + 2) mod 2) = 0) then 3 else 1 in
  s := (!s + (mult * ((!num mod 10 + 10) mod 10)));
  num := (!num / 10);
  position := (!position + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((((10 - ((!s mod 10 + 10) mod 10)) mod 10 + 10) mod 10)) : int); raise Return
  with Return -> !__ret)

and is_valid barcode =
  let __ret = ref false in
  (try
  let barcode = (Obj.magic barcode : int) in
  __ret := (Obj.magic (((String.length ((string_of_int (barcode))) = 13) && (get_check_digit (Obj.repr (barcode)) = ((barcode mod 10 + 10) mod 10)))) : bool); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((string_of_int (get_check_digit (Obj.repr (8718452538119)))));
  print_endline ((string_of_int (get_check_digit (Obj.repr (87184523)))));
  print_endline ((string_of_int (get_check_digit (Obj.repr (87193425381086)))));
  let res = ref (([] : (int) list)) in
  let x = ref (0) in
  (try while (!x < 100) do
    try
  res := (Obj.magic (List.append (!res) [(Obj.magic (get_check_digit (Obj.repr (!x))) : int)]) : int list);
  x := (!x + 10);
    with Continue -> ()
  done with Break -> ());
  print_endline (__show_list !res);
  print_endline ((string_of_bool (is_valid (Obj.repr (8718452538119)))));
  print_endline ((string_of_bool (is_valid (Obj.repr (87184525)))));
  print_endline ((string_of_bool (is_valid (Obj.repr (87193425381089)))));
  print_endline ((string_of_bool (is_valid (Obj.repr (0)))));
  print_endline ((string_of_int (get_barcode ("8718452538119"))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
