(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 08:38 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec identity n =
  let __ret = ref ([] : (float list) list) in
  (try
  let n = (Obj.magic n : int) in
  let mat = ref (([] : (float list) list)) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  let row = ref (([] : (float) list)) in
  let j = ref (0) in
  (try while (!j < n) do
    try
  if (!i = !j) then (
  row := (Obj.magic (List.append (!row) [(Obj.magic (1.0) : float)]) : float list);
  ) else (
  row := (Obj.magic (List.append (!row) [(Obj.magic (0.0) : float)]) : float list);
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  mat := (Obj.magic (List.append (!mat) [(Obj.magic (!row) : float list)]) : float list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!mat) : float list list); raise Return
  with Return -> !__ret)

and transpose mat =
  let __ret = ref ([] : (float list) list) in
  (try
  let mat = (Obj.magic mat : float list list) in
  let rows = List.length (mat) in
  let cols = List.length ((let __l = mat in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  let res = ref (([] : (float list) list)) in
  let j = ref (0) in
  (try while (!j < cols) do
    try
  let row = ref (([] : (float) list)) in
  let i = ref (0) in
  (try while (!i < rows) do
    try
  row := (Obj.magic (List.append (!row) [(Obj.magic ((let __l = (let __l = mat in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : float)]) : float list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  res := (Obj.magic (List.append (!res) [(Obj.magic (!row) : float list)]) : float list list);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list list); raise Return
  with Return -> !__ret)

and matmul a b =
  let __ret = ref ([] : (float list) list) in
  (try
  let a = (Obj.magic a : float list list) in
  let b = (Obj.magic b : float list list) in
  let rows = List.length (a) in
  let cols = List.length ((let __l = b in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  let inner = List.length ((let __l = a in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  let res = ref (([] : (float list) list)) in
  let i = ref (0) in
  (try while (!i < rows) do
    try
  let row = ref (([] : (float) list)) in
  let j = ref (0) in
  (try while (!j < cols) do
    try
  let sum = ref (0.0) in
  let k = ref (0) in
  (try while (!k < inner) do
    try
  sum := (!sum +. ((let __l = (let __l = a in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) *. (let __l = (let __l = b in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))));
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  row := (Obj.magic (List.append (!row) [(Obj.magic (!sum) : float)]) : float list);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  res := (Obj.magic (List.append (!res) [(Obj.magic (!row) : float list)]) : float list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list list); raise Return
  with Return -> !__ret)

and mat_sub a b =
  let __ret = ref ([] : (float list) list) in
  (try
  let a = (Obj.magic a : float list list) in
  let b = (Obj.magic b : float list list) in
  let rows = List.length (a) in
  let cols = List.length ((let __l = a in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  let res = ref (([] : (float list) list)) in
  let i = ref (0) in
  (try while (!i < rows) do
    try
  let row = ref (([] : (float) list)) in
  let j = ref (0) in
  (try while (!j < cols) do
    try
  row := (Obj.magic (List.append (!row) [(Obj.magic (((let __l = (let __l = a in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) -. (let __l = (let __l = b in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) : float)]) : float list);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  res := (Obj.magic (List.append (!res) [(Obj.magic (!row) : float list)]) : float list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list list); raise Return
  with Return -> !__ret)

and inverse mat =
  let __ret = ref ([] : (float list) list) in
  (try
  let mat = (Obj.magic mat : float list list) in
  let n = List.length (mat) in
  let id = ref (identity (Obj.repr (n))) in
  let aug = ref (([] : (float list) list)) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  let row = ref (((let __l = mat in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) @ (let __l = !id in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) in
  aug := (Obj.magic (List.append (!aug) [(Obj.magic (!row) : float list)]) : float list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let col = ref (0) in
  (try while (!col < n) do
    try
  let pivot_row = ref ((let __l = !aug in let __i = !col in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  let pivot = (let __l = !pivot_row in let __i = !col in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in
  if (pivot = 0.0) then (
  (failwith ("matrix is singular"));
  );
  let j = ref (0) in
  (try while (!j < (2 * n)) do
    try
  pivot_row := (List.mapi (fun __i __x -> if __i = !j then ((let __l = !pivot_row in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) /. pivot) else __x) (!pivot_row));
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  aug := (List.mapi (fun __i __x -> if __i = !col then !pivot_row else __x) (!aug));
  let r = ref (0) in
  (try while (!r < n) do
    try
  if (!r <> !col) then (
  let row_r = ref ((let __l = !aug in let __i = !r in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  let factor = (let __l = !row_r in let __i = !col in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in
  j := 0;
  (try while (!j < (2 * n)) do
    try
  row_r := (List.mapi (fun __i __x -> if __i = !j then ((let __l = !row_r in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) -. (factor *. (let __l = !pivot_row in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) else __x) (!row_r));
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  aug := (List.mapi (fun __i __x -> if __i = !r then !row_r else __x) (!aug));
  );
  r := (!r + 1);
    with Continue -> ()
  done with Break -> ());
  col := (!col + 1);
    with Continue -> ()
  done with Break -> ());
  let inv = ref (([] : (float list) list)) in
  let r = ref (0) in
  (try while (!r < n) do
    try
  let row = ref (([] : (float) list)) in
  let c = ref (n) in
  (try while (!c < (2 * n)) do
    try
  row := (Obj.magic (List.append (!row) [(Obj.magic ((let __l = (let __l = !aug in let __i = !r in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) in let __i = !c in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : float)]) : float list);
  c := (!c + 1);
    with Continue -> ()
  done with Break -> ());
  inv := (Obj.magic (List.append (!inv) [(Obj.magic (!row) : float list)]) : float list list);
  r := (!r + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!inv) : float list list); raise Return
  with Return -> !__ret)

and schur_complement mat_a mat_b mat_c pseudo_inv =
  let __ret = ref ([] : (float list) list) in
  (try
  let mat_a = (Obj.magic mat_a : float list list) in
  let mat_b = (Obj.magic mat_b : float list list) in
  let mat_c = (Obj.magic mat_c : float list list) in
  let pseudo_inv = (Obj.magic pseudo_inv : (string * Obj.t) list) in
  let a_rows = List.length (mat_a) in
  let a_cols = List.length ((let __l = mat_a in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  if (a_rows <> a_cols) then (
  (failwith ("Matrix A must be square"));
  );
  if (a_rows <> List.length (mat_b)) then (
  (failwith ("Expected the same number of rows for A and B"));
  );
  if (List.length ((let __l = mat_b in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) <> List.length ((let __l = mat_c in let __i = 0 in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) then (
  (failwith ("Expected the same number of columns for B and C"));
  );
  let a_inv = ref (([] : (float list) list)) in
  if (Obj.obj (List.assoc (__str ("ok")) (pseudo_inv) : Obj.t) : bool) then (
  a_inv := (Obj.magic (Obj.obj (List.assoc (__str ("value")) (pseudo_inv) : Obj.t) : float list list) : float list list);
  ) else (
  a_inv := (Obj.magic inverse (mat_a) : float list list);
  );
  let bt = ref (transpose (mat_b)) in
  let a_inv_b = ref (matmul (!a_inv) (mat_b)) in
  let bt_a_inv_b = ref (matmul (!bt) (!a_inv_b)) in
  __ret := (Obj.magic (mat_sub (mat_c) (!bt_a_inv_b)) : float list list); raise Return
  with Return -> !__ret)

and print_matrix mat =
  let __ret = ref (Obj.magic 0) in
  (try
  let mat = (Obj.magic mat : float list list) in
  let i = ref (0) in
  (try while (!i < List.length (mat)) do
    try
  let line = ref ("") in
  let j = ref (0) in
  let row = ref ((let __l = mat in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  (try while (!j < List.length (!row)) do
    try
  line := (!line ^ (Printf.sprintf "%.16g" ((let __l = !row in let __i = !j in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))));
  if ((!j + 1) < List.length (!row)) then (
  line := (!line ^ " ");
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline (!line);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let a = ref ([[1.0; 2.0]; [2.0; 1.0]]) in
  let b = ref ([[0.0; 3.0]; [3.0; 0.0]]) in
  let c = ref ([[2.0; 1.0]; [6.0; 3.0]]) in
  let none = [(__str ("value"), Obj.repr ([])); (__str ("ok"), Obj.repr (false))] in
  let s = ref (schur_complement (!a) (!b) (!c) (none)) in
  ignore (print_matrix (!s));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
