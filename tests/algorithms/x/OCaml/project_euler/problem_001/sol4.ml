(* Generated by Mochi transpiler v0.10.70 on 2025-08-23 14:49 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic r : int) = 0
  else
    match tag r with
    | 0 -> __is_list (field r 1)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec contains xs value =
  let __ret = ref false in
  (try
  let xs = (Obj.magic xs : int list) in
  let value = (Obj.magic value : int) in
  let i = ref (0) in
  (try while (!i < List.length (xs)) do
    try
  if ((let __l = xs in let __i = !i in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) = value) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and solution n =
  let __ret = ref 0 in
  (try
  let n = (Obj.magic n : int) in
  let zmulti = ref (([] : (int) list)) in
  let xmulti = ref (([] : (int) list)) in
  let temp = ref (1) in
  (try while true do
    try
  let result = (3 * !temp) in
  if (result < n) then (
  zmulti := (Obj.magic ((List.append (!zmulti) [(Obj.magic (result) : int)])) : int list);
  temp := (!temp + 1);
  ) else (
  raise Break;
  );
    with Continue -> ()
  done with Break -> ());
  temp := 1;
  (try while true do
    try
  let result = (5 * !temp) in
  if (result < n) then (
  xmulti := (Obj.magic ((List.append (!xmulti) [(Obj.magic (result) : int)])) : int list);
  temp := (!temp + 1);
  ) else (
  raise Break;
  );
    with Continue -> ()
  done with Break -> ());
  let collection = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < List.length (!zmulti)) do
    try
  let v = (let __l = !zmulti in let __i = !i in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  if not (contains (!collection) (Obj.repr (v))) then (
  collection := (Obj.magic ((List.append (!collection) [(Obj.magic (v) : int)])) : int list);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  i := 0;
  (try while (!i < List.length (!xmulti)) do
    try
  let v = (let __l = !xmulti in let __i = !i in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  if not (contains (!collection) (Obj.repr (v))) then (
  collection := (Obj.magic ((List.append (!collection) [(Obj.magic (v) : int)])) : int list);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let total = ref (0) in
  i := 0;
  (try while (!i < List.length (!collection)) do
    try
  total := (!total + (let __l = !collection in let __i = !i in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!total) : int); raise Return
  with Return -> !__ret)

and test_solution () =
  let __ret = ref (Obj.magic 0) in
  (try
  if (solution (Obj.repr (3)) <> 0) then (
  (failwith ("solution(3) failed"));
  );
  if (solution (Obj.repr (4)) <> 3) then (
  (failwith ("solution(4) failed"));
  );
  if (solution (Obj.repr (10)) <> 23) then (
  (failwith ("solution(10) failed"));
  );
  if (solution (Obj.repr (600)) <> 83700) then (
  (failwith ("solution(600) failed"));
  );
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (test_solution ());
  print_endline (("solution() = " ^ (string_of_int (Obj.magic (solution (Obj.repr (1000))) : int))));
    !__ret
  with Return -> !__ret)


let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  ignore (main ());
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
