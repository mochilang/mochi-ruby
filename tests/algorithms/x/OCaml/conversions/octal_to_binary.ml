(* Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:21 +0700 *)


let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_list (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_list (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec octal_to_binary octal_number =
  let __ret = ref "" in
  (try
  if (String.length (octal_number) = 0) then (
  (failwith ("Empty string was passed to the function"));
  );
  let octal_digits = "01234567" in
  let binary_number = ref ("") in
  let i = ref (0) in
  (try while (!i < String.length (octal_number)) do
    try
  let digit = String.make 1 (String.get (octal_number) !i) in
  let valid = ref (false) in
  let j = ref (0) in
  (try while (!j < String.length (octal_digits)) do
    try
  if (digit = String.make 1 (String.get (octal_digits) !j)) then (
  valid := true;
  raise Break;
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  if not (!valid) then (
  (failwith ("Non-octal value was passed to the function"));
  );
  let value = ref (int_of_string (digit)) in
  let k = ref (0) in
  let binary_digit = ref ("") in
  (try while (!k < 3) do
    try
  binary_digit := ((string_of_int (((!value mod 2 + 2) mod 2))) ^ !binary_digit);
  value := (!value / 2);
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  binary_number := (!binary_number ^ !binary_digit);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!binary_number) : string); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((octal_to_binary ("17")));
  print_endline ((octal_to_binary ("7")));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()