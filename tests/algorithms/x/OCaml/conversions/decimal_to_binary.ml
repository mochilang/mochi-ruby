(* Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:21 +0700 *)


let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_list (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_list (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec decimal_to_binary_iterative num =
  let __ret = ref "" in
  (try
  let num = (Obj.magic num : int) in
  if (num = 0) then (
  __ret := (Obj.magic ("0b0") : string); raise Return
  );
  let negative = ref (false) in
  let n = ref (num) in
  if (!n < 0) then (
  negative := true;
  n := -(!n);
  );
  let result = ref ("") in
  (try while (!n > 0) do
    try
  result := ((string_of_int (((!n mod 2 + 2) mod 2))) ^ !result);
  n := (!n / 2);
    with Continue -> ()
  done with Break -> ());
  if !negative then (
  __ret := (Obj.magic (("-0b" ^ !result)) : string); raise Return
  );
  __ret := (Obj.magic (("0b" ^ !result)) : string); raise Return
  with Return -> !__ret)

and decimal_to_binary_recursive_helper n =
  let __ret = ref "" in
  (try
  let n = (Obj.magic n : int) in
  if (n = 0) then (
  __ret := (Obj.magic ("0") : string); raise Return
  );
  if (n = 1) then (
  __ret := (Obj.magic ("1") : string); raise Return
  );
  let div = (n / 2) in
  let mod_ = ((n mod 2 + 2) mod 2) in
  __ret := (Obj.magic ((decimal_to_binary_recursive_helper (Obj.repr (div)) ^ (string_of_int (mod_)))) : string); raise Return
  with Return -> !__ret)

and decimal_to_binary_recursive num =
  let __ret = ref "" in
  (try
  let num = (Obj.magic num : int) in
  if (num = 0) then (
  __ret := (Obj.magic ("0b0") : string); raise Return
  );
  if (num < 0) then (
  __ret := (Obj.magic (("-0b" ^ decimal_to_binary_recursive_helper (Obj.repr (-(num))))) : string); raise Return
  );
  __ret := (Obj.magic (("0b" ^ decimal_to_binary_recursive_helper (Obj.repr (num)))) : string); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((decimal_to_binary_iterative (Obj.repr (0))));
  print_endline ((decimal_to_binary_iterative (Obj.repr (2))));
  print_endline ((decimal_to_binary_iterative (Obj.repr (7))));
  print_endline ((decimal_to_binary_iterative (Obj.repr (35))));
  print_endline ((decimal_to_binary_iterative (Obj.repr (-(2)))));
  print_endline ((decimal_to_binary_recursive (Obj.repr (0))));
  print_endline ((decimal_to_binary_recursive (Obj.repr (40))));
  print_endline ((decimal_to_binary_recursive (Obj.repr (-(40)))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()