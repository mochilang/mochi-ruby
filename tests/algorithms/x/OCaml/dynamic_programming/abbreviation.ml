(* Generated by Mochi transpiler v0.10.62 on 2025-08-09 15:58 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec index_of s ch =
  let __ret = ref 0 in
  (try
  let i = ref (0) in
  (try while (!i < String.length (s)) do
    try
  if (String.make 1 (String.get (s) !i) = ch) then (
  __ret := (Obj.magic (!i) : int); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (-(1)) : int); raise Return
  with Return -> !__ret)

and ord ch =
  let __ret = ref 0 in
  (try
  let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ" in
  let lower = "abcdefghijklmnopqrstuvwxyz" in
  let idx = ref (index_of (upper) (ch)) in
  if (!idx >= 0) then (
  __ret := (Obj.magic ((65 + !idx)) : int); raise Return
  );
  idx := index_of (lower) (ch);
  if (!idx >= 0) then (
  __ret := (Obj.magic ((97 + !idx)) : int); raise Return
  );
  __ret := (Obj.magic (0) : int); raise Return
  with Return -> !__ret)

and chr n =
  let __ret = ref "" in
  (try
  let n = (Obj.magic n : int) in
  let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ" in
  let lower = "abcdefghijklmnopqrstuvwxyz" in
  if ((n >= 65) && (n < 91)) then (
  __ret := (Obj.magic (String.sub (upper) (n - 65) ((n - 64) - (n - 65))) : string); raise Return
  );
  if ((n >= 97) && (n < 123)) then (
  __ret := (Obj.magic (String.sub (lower) (n - 97) ((n - 96) - (n - 97))) : string); raise Return
  );
  __ret := (Obj.magic ("?") : string); raise Return
  with Return -> !__ret)

and to_upper_char c =
  let __ret = ref "" in
  (try
  let code = ord (c) in
  if ((code >= 97) && (code <= 122)) then (
  __ret := (Obj.magic (chr (Obj.repr ((code - 32)))) : string); raise Return
  );
  __ret := (Obj.magic (c) : string); raise Return
  with Return -> !__ret)

and is_lower c =
  let __ret = ref false in
  (try
  let code = ord (c) in
  __ret := (Obj.magic (((code >= 97) && (code <= 122))) : bool); raise Return
  with Return -> !__ret)

and abbr a b =
  let __ret = ref false in
  (try
  let n = String.length (a) in
  let m = String.length (b) in
  let dp = ref (([] : (bool list) list)) in
  let i = ref (0) in
  (try while (!i <= n) do
    try
  let row = ref (([] : (bool) list)) in
  let j = ref (0) in
  (try while (!j <= m) do
    try
  row := (List.append (!row) [(Obj.magic (false) : bool)]);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  dp := (List.append (!dp) [(Obj.magic (!row) : bool list)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  dp := (List.mapi (fun __i __x -> if __i = 0 then (List.mapi (fun __i __x -> if __i = 0 then true else __x) (List.nth (!dp) (0))) else __x) (!dp));
  i := 0;
  (try while (!i < n) do
    try
  let j = ref (0) in
  (try while (!j <= m) do
    try
  if List.nth (List.nth (!dp) (!i)) (!j) then (
  if ((!j < m) && (to_upper_char (String.make 1 (String.get (a) !i)) = String.make 1 (String.get (b) !j))) then (
  dp := (List.mapi (fun __i __x -> if __i = (!i + 1) then (List.mapi (fun __i __x -> if __i = (!j + 1) then true else __x) (List.nth (!dp) ((!i + 1)))) else __x) (!dp));
  );
  if is_lower (String.make 1 (String.get (a) !i)) then (
  dp := (List.mapi (fun __i __x -> if __i = (!i + 1) then (List.mapi (fun __i __x -> if __i = !j then true else __x) (List.nth (!dp) ((!i + 1)))) else __x) (!dp));
  );
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (List.nth (List.nth (!dp) (n)) (m)) : bool); raise Return
  with Return -> !__ret)

and print_bool b =
  let __ret = ref (Obj.magic 0) in
  (try
  if b then (
  print_endline (string_of_bool true);
  ) else (
  print_endline (string_of_bool false);
  );
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (print_bool (abbr ("daBcd") ("ABC")));
  ignore (print_bool (abbr ("dBcd") ("ABC")));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
