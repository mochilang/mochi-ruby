(* Generated by Mochi transpiler v0.10.61 on 2025-08-09 10:14 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec minimum_subarray_sum target numbers =
  let __ret = ref 0 in
  (try
  let target = (Obj.magic target : int) in
  let n = List.length (numbers) in
  if (n = 0) then (
  __ret := (Obj.magic (0) : int); raise Return
  );
  if (target = 0) then (
  let i = ref (0) in
  (try while (!i < n) do
    try
  if (List.nth (numbers) (!i) = 0) then (
  __ret := (Obj.magic (0) : int); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  );
  let left = ref (0) in
  let right = ref (0) in
  let curr_sum = ref (0) in
  let min_len = ref ((n + 1)) in
  (try while (!right < n) do
    try
  curr_sum := (!curr_sum + List.nth (numbers) (!right));
  (try while ((!curr_sum >= target) && (!left <= !right)) do
    try
  let current_len = ((!right - !left) + 1) in
  if (current_len < !min_len) then (
  min_len := current_len;
  );
  curr_sum := (!curr_sum - List.nth (numbers) (!left));
  left := (!left + 1);
    with Continue -> ()
  done with Break -> ());
  right := (!right + 1);
    with Continue -> ()
  done with Break -> ());
  if (!min_len = (n + 1)) then (
  __ret := (Obj.magic (0) : int); raise Return
  );
  __ret := (Obj.magic (!min_len) : int); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((string_of_int (minimum_subarray_sum (Obj.repr (7)) ([2; 3; 1; 2; 4; 3]))));
  print_endline ((string_of_int (minimum_subarray_sum (Obj.repr (7)) ([2; 3; -(1); 2; 4; -(3)]))));
  print_endline ((string_of_int (minimum_subarray_sum (Obj.repr (11)) ([1; 1; 1; 1; 1; 1; 1; 1]))));
  print_endline ((string_of_int (minimum_subarray_sum (Obj.repr (0)) ([1; 2; 3]))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
