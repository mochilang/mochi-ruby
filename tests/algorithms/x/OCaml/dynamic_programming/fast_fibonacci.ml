(* Generated by Mochi transpiler v0.10.62 on 2025-08-09 15:58 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec _fib n =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let n = (Obj.magic n : int) in
  if (n = 0) then (
  __ret := (Obj.magic ([(__str ("fn"), Obj.repr (0)); (__str ("fn1"), Obj.repr (1))]) : (string * Obj.t) list); raise Return
  );
  let half = _fib (Obj.repr ((n / 2))) in
  let a = (Obj.obj (List.assoc (__str ("fn")) (half) : Obj.t) : int) in
  let b = (Obj.obj (List.assoc (__str ("fn1")) (half) : Obj.t) : int) in
  let c = (a * ((b * 2) - a)) in
  let d = ((a * a) + (b * b)) in
  if (((n mod 2 + 2) mod 2) = 0) then (
  __ret := (Obj.magic ([(__str ("fn"), Obj.repr (c)); (__str ("fn1"), Obj.repr (d))]) : (string * Obj.t) list); raise Return
  );
  __ret := (Obj.magic ([(__str ("fn"), Obj.repr (d)); (__str ("fn1"), Obj.repr ((c + d)))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and fibonacci n =
  let __ret = ref 0 in
  (try
  let n = (Obj.magic n : int) in
  if (n < 0) then (
  (failwith ("Negative arguments are not supported"));
  );
  let res = _fib (Obj.repr (n)) in
  __ret := (Obj.magic ((Obj.obj (List.assoc (__str ("fn")) (res) : Obj.t) : int)) : int); raise Return
  with Return -> !__ret)


let i = ref (0)
let () =
  let mem_start = _mem () in
  let start = _now () in
  (try while (!i < 13) do
    try
  print_endline ((string_of_int (fibonacci (Obj.repr (!i)))));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
