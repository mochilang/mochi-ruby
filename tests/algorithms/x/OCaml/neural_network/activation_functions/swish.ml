(* Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:36 +0700 *)


let rec __is_list v =
  let open Obj in
  let rec loop x =
    let r = repr x in
    if is_int r then (magic r : int) = 0
    else
      match tag r with
      | 0 -> loop (field r 1)
      | _ -> false
  in
  loop v

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec exp_approx x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  let sum = ref (1.0) in
  let term = ref (1.0) in
  let i = ref (1) in
  (try while (!i <= 20) do
    try
  term := ((!term *. x) /. float_of_int (!i));
  sum := (!sum +. !term);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!sum) : float); raise Return
  with Return -> !__ret)

and sigmoid vector =
  let __ret = ref ([] : (float) list) in
  (try
  let vector = (Obj.magic vector : float list) in
  let result = ref (([] : (float) list)) in
  let i = ref (0) in
  (try while (!i < List.length (vector)) do
    try
  let v = (let __l = vector in let __i = !i in if __i < 0 then 0. else match List.nth_opt __l __i with Some v -> (Obj.magic v : float) | None -> 0.) in
  let s = (1.0 /. (1.0 +. exp_approx (Obj.repr ((-.(v)))))) in
  result := (Obj.magic ((List.append (!result) [(Obj.magic (s) : float)])) : float list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : float list); raise Return
  with Return -> !__ret)

and swish vector beta =
  let __ret = ref ([] : (float) list) in
  (try
  let vector = (Obj.magic vector : float list) in
  let beta = (Obj.magic beta : float) in
  let result = ref (([] : (float) list)) in
  let i = ref (0) in
  (try while (!i < List.length (vector)) do
    try
  let v = (let __l = vector in let __i = !i in if __i < 0 then 0. else match List.nth_opt __l __i with Some v -> (Obj.magic v : float) | None -> 0.) in
  let s = (1.0 /. (1.0 +. exp_approx (Obj.repr (((-.(beta)) *. v))))) in
  result := (Obj.magic ((List.append (!result) [(Obj.magic ((v *. s)) : float)])) : float list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : float list); raise Return
  with Return -> !__ret)

and sigmoid_linear_unit vector =
  let __ret = ref ([] : (float) list) in
  (try
  let vector = (Obj.magic vector : float list) in
  __ret := (Obj.magic (swish (vector) (Obj.repr (1.0))) : float list); raise Return
  with Return -> !__ret)

and approx_equal a b eps =
  let __ret = ref false in
  (try
  let a = (Obj.magic a : float) in
  let b = (Obj.magic b : float) in
  let eps = (Obj.magic eps : float) in
  let diff = if (a > b) then (a -. b) else (b -. a) in
  __ret := (Obj.magic ((diff < eps)) : bool); raise Return
  with Return -> !__ret)

and approx_equal_list a b eps =
  let __ret = ref false in
  (try
  let a = (Obj.magic a : float list) in
  let b = (Obj.magic b : float list) in
  let eps = (Obj.magic eps : float) in
  if (List.length (a) <> List.length (b)) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  let i = ref (0) in
  (try while (!i < List.length (a)) do
    try
  if not (approx_equal (Obj.repr ((let __l = a in let __i = !i in if __i < 0 then 0. else match List.nth_opt __l __i with Some v -> (Obj.magic v : float) | None -> 0.))) (Obj.repr ((let __l = b in let __i = !i in if __i < 0 then 0. else match List.nth_opt __l __i with Some v -> (Obj.magic v : float) | None -> 0.))) (Obj.repr (eps))) then (
  __ret := (Obj.magic (false) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (true) : bool); raise Return
  with Return -> !__ret)

and test_swish () =
  let __ret = ref (Obj.magic 0) in
  (try
  let v = ref ([(-.(1.0)); 1.0; 2.0]) in
  let eps = 0.001 in
  if not (approx_equal_list (sigmoid (!v)) ([0.26894142; 0.73105858; 0.88079708]) (Obj.repr (eps))) then (
  (failwith ("sigmoid incorrect"));
  );
  if not (approx_equal_list (sigmoid_linear_unit (!v)) ([(-.(0.26894142)); 0.73105858; 1.76159416]) (Obj.repr (eps))) then (
  (failwith ("sigmoid_linear_unit incorrect"));
  );
  if not (approx_equal_list (swish (!v) (Obj.repr (2.0))) ([(-.(0.11920292)); 0.88079708; 1.96402758]) (Obj.repr (eps))) then (
  (failwith ("swish incorrect"));
  );
  if not (approx_equal_list (swish ([(-.(2.0))]) (Obj.repr (1.0))) ([(-.(0.23840584))]) (Obj.repr (eps))) then (
  (failwith ("swish with parameter 1 incorrect"));
  );
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (test_swish ());
  print_endline ((__str (Obj.magic (sigmoid ([(-.(1.0)); 1.0; 2.0])) : Obj.t)));
  print_endline ((__str (Obj.magic (sigmoid_linear_unit ([(-.(1.0)); 1.0; 2.0])) : Obj.t)));
  print_endline ((__str (Obj.magic (swish ([(-.(1.0)); 1.0; 2.0]) (Obj.repr (2.0))) : Obj.t)));
  print_endline ((__str (Obj.magic (swish ([(-.(2.0))]) (Obj.repr (1.0))) : Obj.t)));
    !__ret
  with Return -> !__ret)


let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  ignore (main ());
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
