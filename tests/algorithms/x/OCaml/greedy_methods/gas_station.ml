(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 08:38 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec get_gas_stations gas_quantities costs =
  let __ret = ref ([] : ((string * Obj.t) list) list) in
  (try
  let gas_quantities = (Obj.magic gas_quantities : int list) in
  let costs = (Obj.magic costs : int list) in
  let stations = ref (([] : ((string * Obj.t) list) list)) in
  let i = ref (0) in
  (try while (!i < List.length (gas_quantities)) do
    try
  stations := (Obj.magic (List.append (!stations) [(Obj.magic ([(__str ("gas_quantity"), Obj.repr ((let __l = gas_quantities in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))); (__str ("cost"), Obj.repr ((let __l = costs in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))))]) : (string * Obj.t) list)]) : (string * Obj.t) list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!stations) : (string * Obj.t) list list); raise Return
  with Return -> !__ret)

and can_complete_journey gas_stations =
  let __ret = ref 0 in
  (try
  let gas_stations = (Obj.magic gas_stations : (string * Obj.t) list list) in
  let total_gas = ref (0) in
  let total_cost = ref (0) in
  let i = ref (0) in
  (try while (!i < List.length (gas_stations)) do
    try
  total_gas := (!total_gas + (Obj.obj (List.assoc (__str ("gas_quantity")) ((let __l = gas_stations in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : Obj.t) : int));
  total_cost := (!total_cost + (Obj.obj (List.assoc (__str ("cost")) ((let __l = gas_stations in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) : Obj.t) : int));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  if (!total_gas < !total_cost) then (
  __ret := (Obj.magic (-(1)) : int); raise Return
  );
  let start = ref (0) in
  let net = ref (0) in
  i := 0;
  (try while (!i < List.length (gas_stations)) do
    try
  let station = ref ((let __l = gas_stations in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) in
  net := ((!net + (Obj.obj (List.assoc (__str ("gas_quantity")) (!station) : Obj.t) : int)) - (Obj.obj (List.assoc (__str ("cost")) (!station) : Obj.t) : int));
  if (!net < 0) then (
  start := (!i + 1);
  net := 0;
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!start) : int); raise Return
  with Return -> !__ret)


let example1 = ref (get_gas_stations ([1; 2; 3; 4; 5]) ([3; 4; 5; 1; 2]))
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((string_of_int (can_complete_journey ((Obj.magic !example1 : (string * Obj.t) list list)))));
  let example2 = ref (get_gas_stations ([2; 3; 4]) ([3; 4; 3])) in
  print_endline ((string_of_int (can_complete_journey ((Obj.magic !example2 : (string * Obj.t) list list)))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
