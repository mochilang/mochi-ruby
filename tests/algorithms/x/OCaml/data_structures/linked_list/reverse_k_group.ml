(* Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:06 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec to_string list =
  let __ret = ref "" in
  (try
  if (List.length ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list)) = 0) then (
  __ret := (Obj.magic ("") : string); raise Return
  );
  let s = ref ((string_of_int (List.nth ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list)) (0)))) in
  let i = ref (1) in
  (try while (!i < List.length ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list))) do
    try
  s := ((!s ^ " -> ") ^ (string_of_int (List.nth ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list)) (!i))));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!s) : string); raise Return
  with Return -> !__ret)

and reverse_k_nodes list k =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let k = (Obj.magic k : int) in
  if (k <= 1) then (
  __ret := (Obj.magic (list) : (string * Obj.t) list); raise Return
  );
  let res = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < List.length ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list))) do
    try
  let j = ref (0) in
  let group = ref (([] : (int) list)) in
  (try while ((!j < k) && ((!i + !j) < List.length ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list)))) do
    try
  group := (List.append (!group) [(Obj.magic (List.nth ((Obj.obj (List.assoc (__str ("data")) (list) : Obj.t) : int list)) ((!i + !j))) : int)]);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  if (List.length (!group) = k) then (
  let g = ref ((k - 1)) in
  (try while (!g >= 0) do
    try
  res := (List.append (!res) [(Obj.magic (List.nth (!group) (!g)) : int)]);
  g := (!g - 1);
    with Continue -> ()
  done with Break -> ());
  ) else (
  let g = ref (0) in
  (try while (!g < List.length (!group)) do
    try
  res := (List.append (!res) [(Obj.magic (List.nth (!group) (!g)) : int)]);
  g := (!g + 1);
    with Continue -> ()
  done with Break -> ());
  );
  i := (!i + k);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ([(__str ("data"), Obj.repr (!res))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let ll = ref ([(__str ("data"), Obj.repr ([1; 2; 3; 4; 5]))]) in
  print_endline (("Original Linked List: " ^ to_string (!ll)));
  let k = ref (2) in
  ll := reverse_k_nodes (!ll) (Obj.repr (!k));
  print_endline (((("After reversing groups of size " ^ (string_of_int (!k))) ^ ": ") ^ to_string (!ll)));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()