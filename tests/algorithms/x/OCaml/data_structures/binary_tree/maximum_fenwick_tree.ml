(* Generated by Mochi transpiler v0.10.59 on 2025-08-07 10:03 +0700 *)


let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_list (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_list (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec zeros n =
  let __ret = ref ([] : (int) list) in
  (try
  let n = (Obj.magic n : int) in
  let res = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  res := (List.append (!res) [(Obj.magic (0) : int)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : int list); raise Return
  with Return -> !__ret)

and update arr idx value =
  let __ret = ref (Obj.magic 0) in
  (try
  let idx = (Obj.magic idx : int) in
  let value = (Obj.magic value : int) in
  arr := (List.mapi (fun __i __x -> if __i = idx then value else __x) (!arr));
    !__ret
  with Return -> !__ret)

and query arr left right =
  let __ret = ref 0 in
  (try
  let left = (Obj.magic left : int) in
  let right = (Obj.magic right : int) in
  let result = ref (0) in
  let i = ref (left) in
  (try while (!i < right) do
    try
  if (List.nth (arr) (!i) > !result) then (
  result := List.nth (arr) (!i);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : int); raise Return
  with Return -> !__ret)


let arr = ref ([0; 0; 0; 0; 0])
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (string_of_int (query (!arr) (Obj.repr (0)) (Obj.repr (5))));
  ignore (update (arr) (Obj.repr (4)) (Obj.repr (100)));
  print_endline (string_of_int (query (!arr) (Obj.repr (0)) (Obj.repr (5))));
  ignore (update (arr) (Obj.repr (4)) (Obj.repr (0)));
  ignore (update (arr) (Obj.repr (2)) (Obj.repr (20)));
  print_endline (string_of_int (query (!arr) (Obj.repr (0)) (Obj.repr (5))));
  ignore (update (arr) (Obj.repr (4)) (Obj.repr (10)));
  print_endline (string_of_int (query (!arr) (Obj.repr (2)) (Obj.repr (5))));
  print_endline (string_of_int (query (!arr) (Obj.repr (1)) (Obj.repr (5))));
  ignore (update (arr) (Obj.repr (2)) (Obj.repr (0)));
  print_endline (string_of_int (query (!arr) (Obj.repr (0)) (Obj.repr (5))));
  arr := zeros (Obj.repr (10000));
  ignore (update (arr) (Obj.repr (255)) (Obj.repr (30)));
  print_endline (string_of_int (query (!arr) (Obj.repr (0)) (Obj.repr (10000))));
  arr := zeros (Obj.repr (6));
  ignore (update (arr) (Obj.repr (5)) (Obj.repr (1)));
  print_endline (string_of_int (query (!arr) (Obj.repr (5)) (Obj.repr (6))));
  arr := zeros (Obj.repr (6));
  ignore (update (arr) (Obj.repr (0)) (Obj.repr (1000)));
  print_endline (string_of_int (query (!arr) (Obj.repr (0)) (Obj.repr (1))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()