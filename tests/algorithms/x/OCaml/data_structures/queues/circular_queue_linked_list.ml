(* Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:06 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec create_queue capacity =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let capacity = (Obj.magic capacity : int) in
  let data = ref (([] : (string) list)) in
  let next = ref (([] : (int) list)) in
  let prev = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < capacity) do
    try
  data := (List.append (!data) [(Obj.magic ("") : string)]);
  next := (List.append (!next) [(Obj.magic ((((!i + 1) mod capacity + capacity) mod capacity)) : int)]);
  prev := (List.append (!prev) [(Obj.magic (((((!i - 1) + capacity) mod capacity + capacity) mod capacity)) : int)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ([(__str ("data"), Obj.repr (!data)); (__str ("next"), Obj.repr (!next)); (__str ("prev"), Obj.repr (!prev)); (__str ("front"), Obj.repr (0)); (__str ("rear"), Obj.repr (0))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and is_empty q =
  let __ret = ref false in
  (try
  __ret := (Obj.magic ((((Obj.obj (List.assoc (__str ("front")) (q) : Obj.t) : int) = (Obj.obj (List.assoc (__str ("rear")) (q) : Obj.t) : int)) && (List.nth ((Obj.obj (List.assoc (__str ("data")) (q) : Obj.t) : string list)) ((Obj.obj (List.assoc (__str ("front")) (q) : Obj.t) : int)) = ""))) : bool); raise Return
  with Return -> !__ret)

and check_can_perform q =
  let __ret = ref (Obj.magic 0) in
  (try
  if is_empty ((Obj.magic q : (string * Obj.t) list)) then (
  (failwith ("Empty Queue"));
  );
    !__ret
  with Return -> !__ret)

and check_is_full q =
  let __ret = ref (Obj.magic 0) in
  (try
  if (List.nth ((Obj.obj (List.assoc (__str ("next")) (q) : Obj.t) : int list)) ((Obj.obj (List.assoc (__str ("rear")) (q) : Obj.t) : int)) = (Obj.obj (List.assoc (__str ("front")) (q) : Obj.t) : int)) then (
  (failwith ("Full Queue"));
  );
    !__ret
  with Return -> !__ret)

and peek q =
  let __ret = ref "" in
  (try
  ignore (check_can_perform ((Obj.magic q : (string * Obj.t) list)));
  __ret := (Obj.magic (List.nth ((Obj.obj (List.assoc (__str ("data")) (q) : Obj.t) : string list)) ((Obj.obj (List.assoc (__str ("front")) (q) : Obj.t) : int))) : string); raise Return
  with Return -> !__ret)

and enqueue q value =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  ignore (check_is_full ((Obj.magic !q : (string * Obj.t) list)));
  if not (is_empty ((Obj.magic !q : (string * Obj.t) list))) then (
  q := ((__str ("rear"), Obj.repr (List.nth ((Obj.obj (List.assoc (__str ("next")) (!q) : Obj.t) : int list)) ((Obj.obj (List.assoc (__str ("rear")) (!q) : Obj.t) : int)))) :: List.remove_assoc (__str ("rear")) (Obj.magic (!q) : (string * Obj.t) list));
  );
  let data = ref ((Obj.obj (List.assoc (__str ("data")) (!q) : Obj.t) : string list)) in
  data := (List.mapi (fun __i __x -> if __i = (Obj.obj (List.assoc (__str ("rear")) (!q) : Obj.t) : int) then value else __x) (!data));
  q := ((__str ("data"), Obj.repr (!data)) :: List.remove_assoc (__str ("data")) (Obj.magic (!q) : (string * Obj.t) list));
  __ret := (Obj.magic (!q) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and dequeue q =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  ignore (check_can_perform ((Obj.magic !q : (string * Obj.t) list)));
  let data = ref ((Obj.obj (List.assoc (__str ("data")) (!q) : Obj.t) : string list)) in
  let val_ = List.nth (!data) ((Obj.obj (List.assoc (__str ("front")) (!q) : Obj.t) : int)) in
  data := (List.mapi (fun __i __x -> if __i = (Obj.obj (List.assoc (__str ("front")) (!q) : Obj.t) : int) then "" else __x) (!data));
  q := ((__str ("data"), Obj.repr (!data)) :: List.remove_assoc (__str ("data")) (Obj.magic (!q) : (string * Obj.t) list));
  if ((Obj.obj (List.assoc (__str ("front")) (!q) : Obj.t) : int) <> (Obj.obj (List.assoc (__str ("rear")) (!q) : Obj.t) : int)) then (
  q := ((__str ("front"), Obj.repr (List.nth ((Obj.obj (List.assoc (__str ("next")) (!q) : Obj.t) : int list)) ((Obj.obj (List.assoc (__str ("front")) (!q) : Obj.t) : int)))) :: List.remove_assoc (__str ("front")) (Obj.magic (!q) : (string * Obj.t) list));
  );
  __ret := (Obj.magic ([(__str ("queue"), Obj.repr (!q)); (__str ("value"), Obj.repr (val_))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let q = ref (create_queue (Obj.repr (3))) in
  print_endline ((string_of_bool (is_empty (!q))));
  q := enqueue (q) ("a");
  q := enqueue (q) ("b");
  print_endline ((peek (!q)));
  let res = ref (dequeue (q)) in
  q := (Obj.obj (List.assoc (__str ("queue")) (!res) : Obj.t) : ( string * Obj.t ) list);
  print_endline (__show (Obj.obj (List.assoc (__str ("value")) (!res) : Obj.t) : string));
  res := dequeue (q);
  q := (Obj.obj (List.assoc (__str ("queue")) (!res) : Obj.t) : ( string * Obj.t ) list);
  print_endline (__show (Obj.obj (List.assoc (__str ("value")) (!res) : Obj.t) : string));
  print_endline ((string_of_bool (is_empty (!q))));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()