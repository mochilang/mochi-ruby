(* Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700 *)


let rec __is_list v =
  let open Obj in
  let rec loop x =
    let r = repr x in
    if is_int r then (magic r : int) = 0
    else
      match tag r with
      | 0 -> loop (field r 1)
      | _ -> false
  in
  loop v

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec get_parent_idx idx =
  let __ret = ref 0 in
  (try
  let idx = (Obj.magic idx : int) in
  __ret := (Obj.magic (((idx - 1) / 2)) : int); raise Return
  with Return -> !__ret)

and get_left_child_idx idx =
  let __ret = ref 0 in
  (try
  let idx = (Obj.magic idx : int) in
  __ret := (Obj.magic (((idx * 2) + 1)) : int); raise Return
  with Return -> !__ret)

and get_right_child_idx idx =
  let __ret = ref 0 in
  (try
  let idx = (Obj.magic idx : int) in
  __ret := (Obj.magic (((idx * 2) + 2)) : int); raise Return
  with Return -> !__ret)

and remove_key m k =
  let __ret = ref ([] : (string * int) list) in
  (try
  let m = (Obj.magic m : (string * int) list) in
  let out = ref ([] : (string * int) list) in
  (try List.iter (fun (key, _) ->
    try
  if (key <> k) then (
  out := ((__str (Obj.repr (key)), (match List.assoc_opt (__str (Obj.repr (key))) (m) with Some v -> (Obj.magic v : int) | None -> 0)) :: List.remove_assoc (__str (Obj.repr (key))) (!out));
  );
    with Continue -> ()) (m) with Break -> ());
  __ret := (Obj.magic (!out) : (string * int) list); raise Return
  with Return -> !__ret)

and slice_without_last xs =
  let __ret = ref ([] : ((string * Obj.t) list) list) in
  (try
  let xs = (Obj.magic xs : (string * Obj.t) list list) in
  let res = ref (([] : ((string * Obj.t) list) list)) in
  let i = ref (0) in
  (try while (!i < (List.length (xs) - 1)) do
    try
  res := (Obj.magic ((List.append (!res) [(Obj.magic ((let __l = xs in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) : (string * Obj.t) list)])) : (string * Obj.t) list list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : (string * Obj.t) list list); raise Return
  with Return -> !__ret)

and sift_down mh idx =
  let __ret = ref (Obj.magic 0) in
  (try
  mh := (Obj.magic !mh : (string * Obj.t) list);
  let idx = (Obj.magic idx : int) in
  let heap = ref ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) in
  let idx_map = ref ((match List.assoc_opt (__str (Obj.repr ("idx_of_element"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let i = ref (idx) in
  (try while true do
    try
  let left = get_left_child_idx (Obj.repr (!i)) in
  let right = get_right_child_idx (Obj.repr (!i)) in
  let smallest = ref (!i) in
  if ((left < List.length (!heap)) && ((match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = left in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0) < (match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = !smallest in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0))) then (
  smallest := left;
  );
  if ((right < List.length (!heap)) && ((match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = right in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0) < (match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = !smallest in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0))) then (
  smallest := right;
  );
  if (!smallest <> !i) then (
  let tmp = ref ((let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  heap := (List.mapi (fun __i __x -> if __i = !i then (let __l = !heap in let __i = !smallest in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) else __x) (!heap));
  heap := (List.mapi (fun __i __x -> if __i = !smallest then !tmp else __x) (!heap));
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> ""))), !i) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !smallest in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> ""))), !smallest) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !smallest in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  i := !smallest;
  ) else (
  raise Break;
  );
    with Continue -> ()
  done with Break -> ());
  mh := ((__str (Obj.repr ("heap")), Obj.repr (Obj.magic (!heap) : (string * Obj.t) list list)) :: List.remove_assoc (__str (Obj.repr ("heap"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("idx_of_element")), Obj.repr (Obj.magic (!idx_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("idx_of_element"))) (Obj.magic (!mh) : (string * Obj.t) list));
    !__ret
  with Return -> !__ret)

and sift_up mh idx =
  let __ret = ref (Obj.magic 0) in
  (try
  mh := (Obj.magic !mh : (string * Obj.t) list);
  let idx = (Obj.magic idx : int) in
  let heap = ref ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) in
  let idx_map = ref ((match List.assoc_opt (__str (Obj.repr ("idx_of_element"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let i = ref (idx) in
  let p = ref (get_parent_idx (Obj.repr (!i))) in
  (try while ((!p >= 0) && ((match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = !p in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0) > (match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0))) do
    try
  let tmp = ref ((let __l = !heap in let __i = !p in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  heap := (List.mapi (fun __i __x -> if __i = !p then (let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) else __x) (!heap));
  heap := (List.mapi (fun __i __x -> if __i = !i then !tmp else __x) (!heap));
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !p in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> ""))), !p) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !p in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> ""))), !i) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) ((let __l = !heap in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  i := !p;
  p := get_parent_idx (Obj.repr (!i));
    with Continue -> ()
  done with Break -> ());
  mh := ((__str (Obj.repr ("heap")), Obj.repr (Obj.magic (!heap) : (string * Obj.t) list list)) :: List.remove_assoc (__str (Obj.repr ("heap"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("idx_of_element")), Obj.repr (Obj.magic (!idx_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("idx_of_element"))) (Obj.magic (!mh) : (string * Obj.t) list));
    !__ret
  with Return -> !__ret)

and new_min_heap array =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let array = (Obj.magic array : (string * Obj.t) list list) in
  let idx_map = ref ([] : (string * int) list) in
  let val_map = ref ([] : (string * int) list) in
  let heap = ref (array) in
  let i = ref (0) in
  (try while (!i < List.length (array)) do
    try
  let n = ref ((let __l = array in let __i = !i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!n) with Some v -> (Obj.magic v : string) | None -> ""))), !i) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!n) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  val_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!n) with Some v -> (Obj.magic v : string) | None -> ""))), (match List.assoc_opt (__str (Obj.repr ("val"))) (!n) with Some v -> (Obj.magic v : int) | None -> 0)) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!n) with Some v -> (Obj.magic v : string) | None -> "")))) (!val_map));
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let mh = ref ([(__str (Obj.repr ("heap")), Obj.repr (!heap)); (__str (Obj.repr ("idx_of_element")), Obj.repr (!idx_map)); (__str (Obj.repr ("heap_dict")), Obj.repr (!val_map))]) in
  let start = ref (get_parent_idx (Obj.repr ((List.length (array) - 1)))) in
  (try while (!start >= 0) do
    try
  ignore (sift_down (mh) (Obj.repr (!start)));
  start := (!start - 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!mh) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and peek mh =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let mh = (Obj.magic mh : (string * Obj.t) list) in
  __ret := (Obj.magic ((let __l = (match List.assoc_opt (__str (Obj.repr ("heap"))) (mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> []) in let __i = 0 in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and remove_min mh =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  mh := (Obj.magic !mh : (string * Obj.t) list);
  let heap = ref ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) in
  let idx_map = ref ((match List.assoc_opt (__str (Obj.repr ("idx_of_element"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let val_map = ref ((match List.assoc_opt (__str (Obj.repr ("heap_dict"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let last_idx = (List.length (!heap) - 1) in
  let top = ref ((let __l = !heap in let __i = 0 in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  let last = ref ((let __l = !heap in let __i = last_idx in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  heap := (List.mapi (fun __i __x -> if __i = 0 then !last else __x) (!heap));
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!last) with Some v -> (Obj.magic v : string) | None -> ""))), 0) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!last) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  heap := slice_without_last (!heap);
  idx_map := (Obj.magic (remove_key ((Obj.magic (!idx_map) : (string * int) list)) ((match List.assoc_opt (__str (Obj.repr ("name"))) (!top) with Some v -> (Obj.magic v : string) | None -> ""))) : (string * int) list);
  val_map := (Obj.magic (remove_key ((Obj.magic (!val_map) : (string * int) list)) ((match List.assoc_opt (__str (Obj.repr ("name"))) (!top) with Some v -> (Obj.magic v : string) | None -> ""))) : (string * int) list);
  mh := ((__str (Obj.repr ("heap")), Obj.repr (Obj.magic (!heap) : (string * Obj.t) list list)) :: List.remove_assoc (__str (Obj.repr ("heap"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("idx_of_element")), Obj.repr (Obj.magic (!idx_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("idx_of_element"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("heap_dict")), Obj.repr (Obj.magic (!val_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("heap_dict"))) (Obj.magic (!mh) : (string * Obj.t) list));
  if (List.length (!heap) > 0) then (
  ignore (sift_down (mh) (Obj.repr (0)));
  );
  __ret := (Obj.magic (!top) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and insert mh node =
  let __ret = ref (Obj.magic 0) in
  (try
  mh := (Obj.magic !mh : (string * Obj.t) list);
  let node = (Obj.magic node : (string * Obj.t) list) in
  let heap = ref ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) in
  let idx_map = ref ((match List.assoc_opt (__str (Obj.repr ("idx_of_element"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let val_map = ref ((match List.assoc_opt (__str (Obj.repr ("heap_dict"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  heap := (Obj.magic ((List.append (!heap) [(Obj.magic (node) : (string * Obj.t) list)])) : (string * Obj.t) list list);
  let idx = (List.length (!heap) - 1) in
  idx_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (node) with Some v -> (Obj.magic v : string) | None -> ""))), idx) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (node) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map));
  val_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (node) with Some v -> (Obj.magic v : string) | None -> ""))), (match List.assoc_opt (__str (Obj.repr ("val"))) (node) with Some v -> (Obj.magic v : int) | None -> 0)) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (node) with Some v -> (Obj.magic v : string) | None -> "")))) (!val_map));
  mh := ((__str (Obj.repr ("heap")), Obj.repr (Obj.magic (!heap) : (string * Obj.t) list list)) :: List.remove_assoc (__str (Obj.repr ("heap"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("idx_of_element")), Obj.repr (Obj.magic (!idx_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("idx_of_element"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("heap_dict")), Obj.repr (Obj.magic (!val_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("heap_dict"))) (Obj.magic (!mh) : (string * Obj.t) list));
  ignore (sift_up (mh) (Obj.repr (idx)));
    !__ret
  with Return -> !__ret)

and is_empty mh =
  let __ret = ref false in
  (try
  let mh = (Obj.magic mh : (string * Obj.t) list) in
  __ret := (Obj.magic ((List.length ((match List.assoc_opt (__str (Obj.repr ("heap"))) (mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) = 0)) : bool); raise Return
  with Return -> !__ret)

and get_value mh key =
  let __ret = ref 0 in
  (try
  let mh = (Obj.magic mh : (string * Obj.t) list) in
  __ret := (Obj.magic ((match List.assoc_opt (__str (Obj.repr (key))) ((match List.assoc_opt (__str (Obj.repr ("heap_dict"))) (mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0)) : int); raise Return
  with Return -> !__ret)

and decrease_key mh node new_value =
  let __ret = ref (Obj.magic 0) in
  (try
  mh := (Obj.magic !mh : (string * Obj.t) list);
  node := (Obj.magic !node : (string * Obj.t) list);
  let new_value = (Obj.magic new_value : int) in
  let heap = ref ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!mh) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) in
  let val_map = ref ((match List.assoc_opt (__str (Obj.repr ("heap_dict"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let idx_map = ref ((match List.assoc_opt (__str (Obj.repr ("idx_of_element"))) (!mh) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  let idx = (match List.assoc_opt (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!node) with Some v -> (Obj.magic v : string) | None -> "")))) (!idx_map) with Some v -> (Obj.magic v : int) | None -> 0) in
  if not (((match List.assoc_opt (__str (Obj.repr ("val"))) ((let __l = !heap in let __i = idx in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) with Some v -> (Obj.magic v : int) | None -> 0) > new_value)) then (
  (failwith ("newValue must be less than current value"));
  );
  node := ((__str (Obj.repr ("val")), Obj.repr (Obj.magic (new_value) : int)) :: List.remove_assoc (__str (Obj.repr ("val"))) (Obj.magic (!node) : (string * Obj.t) list));
  heap := (List.mapi (fun __i __x -> if __i = idx then ((__str (Obj.repr ("val")), Obj.repr (Obj.magic (new_value) : Obj.t)) :: List.remove_assoc (__str (Obj.repr ("val"))) (Obj.magic ((let __l = !heap in let __i = idx in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) : (string * Obj.t) list)) else __x) (!heap));
  val_map := ((__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!node) with Some v -> (Obj.magic v : string) | None -> ""))), new_value) :: List.remove_assoc (__str (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("name"))) (!node) with Some v -> (Obj.magic v : string) | None -> "")))) (!val_map));
  mh := ((__str (Obj.repr ("heap")), Obj.repr (Obj.magic (!heap) : (string * Obj.t) list list)) :: List.remove_assoc (__str (Obj.repr ("heap"))) (Obj.magic (!mh) : (string * Obj.t) list));
  mh := ((__str (Obj.repr ("heap_dict")), Obj.repr (Obj.magic (!val_map) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("heap_dict"))) (Obj.magic (!mh) : (string * Obj.t) list));
  ignore (sift_up (mh) (Obj.repr (idx)));
    !__ret
  with Return -> !__ret)

and node_to_string n =
  let __ret = ref "" in
  (try
  let n = (Obj.magic n : (string * Obj.t) list) in
  __ret := (Obj.magic ((((("Node(" ^ (match List.assoc_opt (__str (Obj.repr ("name"))) (n) with Some v -> (Obj.magic v : string) | None -> "")) ^ ", ") ^ (string_of_int (Obj.magic ((match List.assoc_opt (__str (Obj.repr ("val"))) (n) with Some v -> (Obj.magic v : int) | None -> 0)) : int))) ^ ")")) : string); raise Return
  with Return -> !__ret)


let r = ref ([(__str (Obj.repr ("name")), Obj.repr ("R")); (__str (Obj.repr ("val")), Obj.repr (-(1)))])
let b = ref ([(__str (Obj.repr ("name")), Obj.repr ("B")); (__str (Obj.repr ("val")), Obj.repr (6))])
let a = ref ([(__str (Obj.repr ("name")), Obj.repr ("A")); (__str (Obj.repr ("val")), Obj.repr (3))])
let x = ref ([(__str (Obj.repr ("name")), Obj.repr ("X")); (__str (Obj.repr ("val")), Obj.repr (1))])
let e = ref ([(__str (Obj.repr ("name")), Obj.repr ("E")); (__str (Obj.repr ("val")), Obj.repr (4))])
let my_min_heap = ref (new_min_heap ((Obj.magic ([!r; !b; !a; !x; !e]) : (string * Obj.t) list list)))
let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  print_endline ("Min Heap - before decrease key");
  (try List.iter (fun n ->
    try
  print_endline ((node_to_string ((Obj.magic (n) : (string * Obj.t) list))));
    with Continue -> ()) ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!my_min_heap) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) with Break -> ());
  print_endline ("Min Heap - After decrease key of node [B -> -17]");
  ignore (decrease_key (my_min_heap) (b) (Obj.repr (-(17))));
  (try List.iter (fun n ->
    try
  print_endline ((node_to_string ((Obj.magic (n) : (string * Obj.t) list))));
    with Continue -> ()) ((match List.assoc_opt (__str (Obj.repr ("heap"))) (!my_min_heap) with Some v -> (Obj.magic v : (string * Obj.t) list list) | None -> [])) with Break -> ());
  print_endline ((string_of_int (Obj.magic (get_value (!my_min_heap) ("B")) : int)));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
