(* Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:57 +0700 *)


let rec __is_list v =
  let open Obj in
  let rec loop x =
    let r = repr x in
    if is_int r then (magic r : int) = 0
    else
      match tag r with
      | 0 -> loop (field r 1)
      | _ -> false
  in
  loop v

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec new_heap key =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  __ret := (Obj.magic ([(__str (Obj.repr ("arr")), Obj.repr ([])); (__str (Obj.repr ("pos_map")), Obj.repr ([])); (__str (Obj.repr ("size")), Obj.repr (0)); (__str (Obj.repr ("key")), Obj.repr (key))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and parent i =
  let __ret = ref 0 in
  (try
  let i = (Obj.magic i : int) in
  if (i > 0) then (
  __ret := (Obj.magic (((i - 1) / 2)) : int); raise Return
  );
  __ret := (Obj.magic (-(1)) : int); raise Return
  with Return -> !__ret)

and left i size =
  let __ret = ref 0 in
  (try
  let i = (Obj.magic i : int) in
  let size = (Obj.magic size : int) in
  let l = ((2 * i) + 1) in
  if (l < size) then (
  __ret := (Obj.magic (l) : int); raise Return
  );
  __ret := (Obj.magic (-(1)) : int); raise Return
  with Return -> !__ret)

and right i size =
  let __ret = ref 0 in
  (try
  let i = (Obj.magic i : int) in
  let size = (Obj.magic size : int) in
  let r = ((2 * i) + 2) in
  if (r < size) then (
  __ret := (Obj.magic (r) : int); raise Return
  );
  __ret := (Obj.magic (-(1)) : int); raise Return
  with Return -> !__ret)

and swap h i j =
  let __ret = ref (Obj.magic 0) in
  (try
  h := (Obj.magic !h : (string * Obj.t) list);
  let i = (Obj.magic i : int) in
  let j = (Obj.magic j : int) in
  let arr = ref ((match List.assoc_opt (__str (Obj.repr ("arr"))) (!h) with Some v -> (Obj.magic v : int list list) | None -> [])) in
  let item_i = (let __l = (let __l = !arr in let __i = i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = 0 in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  let item_j = (let __l = (let __l = !arr in let __i = j in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = 0 in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  let pm = ref ((match List.assoc_opt (__str (Obj.repr ("pos_map"))) (!h) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  pm := ((__str (Obj.repr (item_i)), (j + 1)) :: List.remove_assoc (__str (Obj.repr (item_i))) (!pm));
  pm := ((__str (Obj.repr (item_j)), (i + 1)) :: List.remove_assoc (__str (Obj.repr (item_j))) (!pm));
  h := ((__str (Obj.repr ("pos_map")), Obj.repr (Obj.magic (!pm) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("pos_map"))) (Obj.magic (!h) : (string * Obj.t) list));
  let tmp = ref ((let __l = !arr in let __i = i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  arr := (List.mapi (fun __i __x -> if __i = i then (let __l = !arr in let __i = j in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) else __x) (!arr));
  arr := (List.mapi (fun __i __x -> if __i = j then !tmp else __x) (!arr));
  h := ((__str (Obj.repr ("arr")), Obj.repr (Obj.magic (!arr) : int list list)) :: List.remove_assoc (__str (Obj.repr ("arr"))) (Obj.magic (!h) : (string * Obj.t) list));
    !__ret
  with Return -> !__ret)

and cmp h i j =
  let __ret = ref false in
  (try
  let h = (Obj.magic h : (string * Obj.t) list) in
  let i = (Obj.magic i : int) in
  let j = (Obj.magic j : int) in
  let arr = ref ((match List.assoc_opt (__str (Obj.repr ("arr"))) (h) with Some v -> (Obj.magic v : int list list) | None -> [])) in
  __ret := (Obj.magic (((let __l = (let __l = !arr in let __i = i in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = 1 in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) < (let __l = (let __l = !arr in let __i = j in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = 1 in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0))) : bool); raise Return
  with Return -> !__ret)

and get_valid_parent h i =
  let __ret = ref 0 in
  (try
  let h = (Obj.magic h : (string * Obj.t) list) in
  let i = (Obj.magic i : int) in
  let vp = ref (i) in
  let l = left (Obj.repr (i)) (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("size"))) (h) with Some v -> (Obj.magic v : int) | None -> 0))) in
  if ((l <> (0 - 1)) && (cmp ((Obj.magic (h) : (string * Obj.t) list)) (Obj.repr (l)) (Obj.repr (!vp)) = false)) then (
  vp := l;
  );
  let r = right (Obj.repr (i)) (Obj.repr ((match List.assoc_opt (__str (Obj.repr ("size"))) (h) with Some v -> (Obj.magic v : int) | None -> 0))) in
  if ((r <> (0 - 1)) && (cmp ((Obj.magic (h) : (string * Obj.t) list)) (Obj.repr (r)) (Obj.repr (!vp)) = false)) then (
  vp := r;
  );
  __ret := (Obj.magic (!vp) : int); raise Return
  with Return -> !__ret)

and heapify_up h index =
  let __ret = ref (Obj.magic 0) in
  (try
  let h = (Obj.magic h : (string * Obj.t) list) in
  let index = (Obj.magic index : int) in
  let idx = ref (index) in
  let p = ref (parent (Obj.repr (!idx))) in
  (try while ((!p <> (0 - 1)) && (cmp ((Obj.magic (h) : (string * Obj.t) list)) (Obj.repr (!idx)) (Obj.repr (!p)) = false)) do
    try
  ignore (swap (ref (h)) (Obj.repr (!idx)) (Obj.repr (!p)));
  idx := !p;
  p := parent (Obj.repr (!p));
    with Continue -> ()
  done with Break -> ());
    !__ret
  with Return -> !__ret)

and heapify_down h index =
  let __ret = ref (Obj.magic 0) in
  (try
  let h = (Obj.magic h : (string * Obj.t) list) in
  let index = (Obj.magic index : int) in
  let idx = ref (index) in
  let vp = ref (get_valid_parent ((Obj.magic (h) : (string * Obj.t) list)) (Obj.repr (!idx))) in
  (try while (!vp <> !idx) do
    try
  ignore (swap (ref (h)) (Obj.repr (!idx)) (Obj.repr (!vp)));
  idx := !vp;
  vp := get_valid_parent ((Obj.magic (h) : (string * Obj.t) list)) (Obj.repr (!idx));
    with Continue -> ()
  done with Break -> ());
    !__ret
  with Return -> !__ret)

and update_item h item item_value =
  let __ret = ref (Obj.magic 0) in
  (try
  h := (Obj.magic !h : (string * Obj.t) list);
  let item = (Obj.magic item : int) in
  let item_value = (Obj.magic item_value : int) in
  let pm = ref ((match List.assoc_opt (__str (Obj.repr ("pos_map"))) (!h) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  if ((match List.assoc_opt (__str (Obj.repr (item))) (!pm) with Some v -> (Obj.magic v : int) | None -> 0) = 0) then (
  __ret := (); raise Return
  );
  let index = ((match List.assoc_opt (__str (Obj.repr (item))) (!pm) with Some v -> (Obj.magic v : int) | None -> 0) - 1) in
  let arr = ref ((match List.assoc_opt (__str (Obj.repr ("arr"))) (!h) with Some v -> (Obj.magic v : int list list) | None -> [])) in
  arr := (List.mapi (fun __i __x -> if __i = index then [item; ((match List.assoc_opt (__str (Obj.repr ("key"))) (!h) with Some v -> (Obj.magic v : Obj.t -> int) | None -> fun _ -> 0) (Obj.repr (item_value)))] else __x) (!arr));
  h := ((__str (Obj.repr ("arr")), Obj.repr (Obj.magic (!arr) : int list list)) :: List.remove_assoc (__str (Obj.repr ("arr"))) (Obj.magic (!h) : (string * Obj.t) list));
  h := ((__str (Obj.repr ("pos_map")), Obj.repr (Obj.magic (!pm) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("pos_map"))) (Obj.magic (!h) : (string * Obj.t) list));
  ignore (heapify_up ((Obj.magic (!h) : (string * Obj.t) list)) (Obj.repr (index)));
  ignore (heapify_down ((Obj.magic (!h) : (string * Obj.t) list)) (Obj.repr (index)));
    !__ret
  with Return -> !__ret)

and delete_item h item =
  let __ret = ref (Obj.magic 0) in
  (try
  h := (Obj.magic !h : (string * Obj.t) list);
  let item = (Obj.magic item : int) in
  let pm = ref ((match List.assoc_opt (__str (Obj.repr ("pos_map"))) (!h) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  if ((match List.assoc_opt (__str (Obj.repr (item))) (!pm) with Some v -> (Obj.magic v : int) | None -> 0) = 0) then (
  __ret := (); raise Return
  );
  let index = ((match List.assoc_opt (__str (Obj.repr (item))) (!pm) with Some v -> (Obj.magic v : int) | None -> 0) - 1) in
  pm := ((__str (Obj.repr (item)), 0) :: List.remove_assoc (__str (Obj.repr (item))) (!pm));
  let arr = ref ((match List.assoc_opt (__str (Obj.repr ("arr"))) (!h) with Some v -> (Obj.magic v : int list list) | None -> [])) in
  let last_index = ((match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) - 1) in
  if (index <> last_index) then (
  arr := (List.mapi (fun __i __x -> if __i = index then (let __l = !arr in let __i = last_index in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) else __x) (!arr));
  let moved = (let __l = (let __l = !arr in let __i = index in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = 0 in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  pm := ((__str (Obj.repr (moved)), (index + 1)) :: List.remove_assoc (__str (Obj.repr (moved))) (!pm));
  );
  h := ((__str (Obj.repr ("size")), Obj.repr (Obj.magic (((match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) - 1)) : int)) :: List.remove_assoc (__str (Obj.repr ("size"))) (Obj.magic (!h) : (string * Obj.t) list));
  h := ((__str (Obj.repr ("arr")), Obj.repr (Obj.magic (!arr) : int list list)) :: List.remove_assoc (__str (Obj.repr ("arr"))) (Obj.magic (!h) : (string * Obj.t) list));
  h := ((__str (Obj.repr ("pos_map")), Obj.repr (Obj.magic (!pm) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("pos_map"))) (Obj.magic (!h) : (string * Obj.t) list));
  if ((match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) > index) then (
  ignore (heapify_up ((Obj.magic (!h) : (string * Obj.t) list)) (Obj.repr (index)));
  ignore (heapify_down ((Obj.magic (!h) : (string * Obj.t) list)) (Obj.repr (index)));
  );
    !__ret
  with Return -> !__ret)

and insert_item h item item_value =
  let __ret = ref (Obj.magic 0) in
  (try
  h := (Obj.magic !h : (string * Obj.t) list);
  let item = (Obj.magic item : int) in
  let item_value = (Obj.magic item_value : int) in
  let arr = ref ((match List.assoc_opt (__str (Obj.repr ("arr"))) (!h) with Some v -> (Obj.magic v : int list list) | None -> [])) in
  let arr_len = List.length (!arr) in
  if (arr_len = (match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0)) then (
  arr := (Obj.magic ((List.append (!arr) [(Obj.magic ([item; ((match List.assoc_opt (__str (Obj.repr ("key"))) (!h) with Some v -> (Obj.magic v : Obj.t -> int) | None -> fun _ -> 0) (Obj.repr (item_value)))]) : int list)])) : int list list);
  ) else (
  arr := (List.mapi (fun __i __x -> if __i = (match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) then [item; ((match List.assoc_opt (__str (Obj.repr ("key"))) (!h) with Some v -> (Obj.magic v : Obj.t -> int) | None -> fun _ -> 0) (Obj.repr (item_value)))] else __x) (!arr));
  );
  let pm = ref ((match List.assoc_opt (__str (Obj.repr ("pos_map"))) (!h) with Some v -> (Obj.magic v : ( string * int ) list) | None -> [])) in
  pm := ((__str (Obj.repr (item)), ((match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) + 1)) :: List.remove_assoc (__str (Obj.repr (item))) (!pm));
  h := ((__str (Obj.repr ("size")), Obj.repr (Obj.magic (((match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) + 1)) : int)) :: List.remove_assoc (__str (Obj.repr ("size"))) (Obj.magic (!h) : (string * Obj.t) list));
  h := ((__str (Obj.repr ("arr")), Obj.repr (Obj.magic (!arr) : int list list)) :: List.remove_assoc (__str (Obj.repr ("arr"))) (Obj.magic (!h) : (string * Obj.t) list));
  h := ((__str (Obj.repr ("pos_map")), Obj.repr (Obj.magic (!pm) : (string * int) list)) :: List.remove_assoc (__str (Obj.repr ("pos_map"))) (Obj.magic (!h) : (string * Obj.t) list));
  ignore (heapify_up ((Obj.magic (!h) : (string * Obj.t) list)) (Obj.repr (((match List.assoc_opt (__str (Obj.repr ("size"))) (!h) with Some v -> (Obj.magic v : int) | None -> 0) - 1))));
    !__ret
  with Return -> !__ret)

and get_top h =
  let __ret = ref ([] : (int) list) in
  (try
  let h = (Obj.magic h : (string * Obj.t) list) in
  let arr = ref ((match List.assoc_opt (__str (Obj.repr ("arr"))) (h) with Some v -> (Obj.magic v : int list list) | None -> [])) in
  if ((match List.assoc_opt (__str (Obj.repr ("size"))) (h) with Some v -> (Obj.magic v : int) | None -> 0) > 0) then (
  __ret := (Obj.magic ((let __l = !arr in let __i = 0 in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) : int list); raise Return
  );
  __ret := (Obj.magic ([]) : int list); raise Return
  with Return -> !__ret)

and extract_top h =
  let __ret = ref ([] : (int) list) in
  (try
  let h = (Obj.magic h : (string * Obj.t) list) in
  let top = ref (get_top ((Obj.magic (h) : (string * Obj.t) list))) in
  if (List.length (!top) > 0) then (
  ignore (delete_item (ref (h)) (Obj.repr ((let __l = !top in let __i = 0 in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0))));
  );
  __ret := (Obj.magic (!top) : int list); raise Return
  with Return -> !__ret)

and identity x =
  let __ret = ref 0 in
  (try
  let x = (Obj.magic x : int) in
  __ret := (Obj.magic (x) : int); raise Return
  with Return -> !__ret)

and negate x =
  let __ret = ref 0 in
  (try
  let x = (Obj.magic x : int) in
  __ret := (Obj.magic ((0 - x)) : int); raise Return
  with Return -> !__ret)


let h = ref (new_heap (identity))
let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  ignore (insert_item (h) (Obj.repr (5)) (Obj.repr (34)));
  ignore (insert_item (h) (Obj.repr (6)) (Obj.repr (31)));
  ignore (insert_item (h) (Obj.repr (7)) (Obj.repr (37)));
  print_endline ((__str (Obj.magic (get_top (!h)) : Obj.t)));
  print_endline ((__str (Obj.magic (extract_top (!h)) : Obj.t)));
  print_endline ((__str (Obj.magic (extract_top (!h)) : Obj.t)));
  print_endline ((__str (Obj.magic (extract_top (!h)) : Obj.t)));
  h := new_heap (negate);
  ignore (insert_item (h) (Obj.repr (5)) (Obj.repr (34)));
  ignore (insert_item (h) (Obj.repr (6)) (Obj.repr (31)));
  ignore (insert_item (h) (Obj.repr (7)) (Obj.repr (37)));
  print_endline ((__str (Obj.magic (get_top (!h)) : Obj.t)));
  print_endline ((__str (Obj.magic (extract_top (!h)) : Obj.t)));
  print_endline ((__str (Obj.magic (extract_top (!h)) : Obj.t)));
  print_endline ((__str (Obj.magic (extract_top (!h)) : Obj.t)));
  ignore (insert_item (h) (Obj.repr (8)) (Obj.repr (45)));
  ignore (insert_item (h) (Obj.repr (9)) (Obj.repr (40)));
  ignore (insert_item (h) (Obj.repr (10)) (Obj.repr (50)));
  print_endline ((__str (Obj.magic (get_top (!h)) : Obj.t)));
  ignore (update_item (h) (Obj.repr (10)) (Obj.repr (30)));
  print_endline ((__str (Obj.magic (get_top (!h)) : Obj.t)));
  ignore (delete_item (h) (Obj.repr (10)));
  print_endline ((__str (Obj.magic (get_top (!h)) : Obj.t)));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
