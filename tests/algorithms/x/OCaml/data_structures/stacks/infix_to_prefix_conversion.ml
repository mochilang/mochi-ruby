(* Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:06 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _priority = ref ([(__str ("^"), 3); (__str ("*"), 2); (__str ("/"), 2); (__str ("%"), 2); (__str ("+"), 1); (__str ("-"), 1)])
let _letters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
let _digits = "0123456789"
let rec is_alpha ch =
  let __ret = ref false in
  (try
  let i = ref (0) in
  (try while (!i < String.length (_letters)) do
    try
  if (String.make 1 (String.get (_letters) !i) = ch) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and is_digit ch =
  let __ret = ref false in
  (try
  let i = ref (0) in
  (try while (!i < String.length (_digits)) do
    try
  if (String.make 1 (String.get (_digits) !i) = ch) then (
  __ret := (Obj.magic (true) : bool); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (false) : bool); raise Return
  with Return -> !__ret)

and reverse_string s =
  let __ret = ref "" in
  (try
  let out = ref ("") in
  let i = ref ((String.length (s) - 1)) in
  (try while (!i >= 0) do
    try
  out := (!out ^ String.make 1 (String.get (s) !i));
  i := (!i - 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!out) : string); raise Return
  with Return -> !__ret)

and infix_to_postfix infix =
  let __ret = ref "" in
  (try
  let stack = ref (([] : (string) list)) in
  let post = ref (([] : (string) list)) in
  let i = ref (0) in
  (try while (!i < String.length (infix)) do
    try
  let x = String.make 1 (String.get (infix) !i) in
  if (is_alpha (x) || is_digit (x)) then (
  post := (List.append (!post) [(Obj.magic (x) : string)]);
  ) else (
  if (x = "(") then (
  stack := (List.append (!stack) [(Obj.magic (x) : string)]);
  ) else (
  if (x = ")") then (
  if (List.length (!stack) = 0) then (
  (failwith ("list index out of range"));
  );
  (try while (List.nth (!stack) ((List.length (!stack) - 1)) <> "(") do
    try
  post := (List.append (!post) [(Obj.magic (List.nth (!stack) ((List.length (!stack) - 1))) : string)]);
  stack := List.of_seq (Seq.take ((List.length (!stack) - 1) - 0) (Seq.drop 0 (List.to_seq (!stack))));
    with Continue -> ()
  done with Break -> ());
  stack := List.of_seq (Seq.take ((List.length (!stack) - 1) - 0) (Seq.drop 0 (List.to_seq (!stack))));
  ) else (
  if (List.length (!stack) = 0) then (
  stack := (List.append (!stack) [(Obj.magic (x) : string)]);
  ) else (
  (try while (((List.length (!stack) > 0) && (List.nth (!stack) ((List.length (!stack) - 1)) <> "(")) && (List.assoc (__str (x)) (!_priority) <= List.assoc (__str (List.nth (!stack) ((List.length (!stack) - 1)))) (!_priority))) do
    try
  post := (List.append (!post) [(Obj.magic (List.nth (!stack) ((List.length (!stack) - 1))) : string)]);
  stack := List.of_seq (Seq.take ((List.length (!stack) - 1) - 0) (Seq.drop 0 (List.to_seq (!stack))));
    with Continue -> ()
  done with Break -> ());
  stack := (List.append (!stack) [(Obj.magic (x) : string)]);
  );
  );
  );
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  (try while (List.length (!stack) > 0) do
    try
  if (List.nth (!stack) ((List.length (!stack) - 1)) = "(") then (
  (failwith ("invalid expression"));
  );
  post := (List.append (!post) [(Obj.magic (List.nth (!stack) ((List.length (!stack) - 1))) : string)]);
  stack := List.of_seq (Seq.take ((List.length (!stack) - 1) - 0) (Seq.drop 0 (List.to_seq (!stack))));
    with Continue -> ()
  done with Break -> ());
  let res = ref ("") in
  let j = ref (0) in
  (try while (!j < List.length (!post)) do
    try
  res := (!res ^ List.nth (!post) (!j));
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : string); raise Return
  with Return -> !__ret)

and infix_to_prefix infix =
  let __ret = ref "" in
  (try
  let reversed = ref ("") in
  let i = ref ((String.length (infix) - 1)) in
  (try while (!i >= 0) do
    try
  let ch = String.make 1 (String.get (infix) !i) in
  if (ch = "(") then (
  reversed := (!reversed ^ ")");
  ) else (
  if (ch = ")") then (
  reversed := (!reversed ^ "(");
  ) else (
  reversed := (!reversed ^ ch);
  );
  );
  i := (!i - 1);
    with Continue -> ()
  done with Break -> ());
  let postfix = infix_to_postfix (!reversed) in
  let prefix = reverse_string (postfix) in
  __ret := (Obj.magic (prefix) : string); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()