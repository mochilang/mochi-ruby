(* Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:06 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let arr = ref ([(-.(10.0)); (-.(5.0)); 0.0; 5.0; 5.1; 11.0; 13.0; 21.0; 3.0; 4.0; (-.(21.0)); (-.(10.0)); (-.(5.0)); (-.(1.0)); 0.0])
let expected = ref ([(-.(5.0)); 0.0; 5.0; 5.1; 11.0; 13.0; 21.0; (-.(1.0)); 4.0; (-.(1.0)); (-.(10.0)); (-.(5.0)); (-.(1.0)); 0.0; (-.(1.0))])
let rec next_greatest_element_slow xs =
  let __ret = ref ([] : (float) list) in
  (try
  let res = ref (([] : (float) list)) in
  let i = ref (0) in
  (try while (!i < List.length (xs)) do
    try
  let next = ref ((-.(1.0))) in
  let j = ref ((!i + 1)) in
  (try while (!j < List.length (xs)) do
    try
  if (List.nth (xs) (!i) < List.nth (xs) (!j)) then (
  next := List.nth (xs) (!j);
  raise Break;
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  res := (List.append (!res) [(Obj.magic (!next) : float)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list); raise Return
  with Return -> !__ret)

and next_greatest_element_fast xs =
  let __ret = ref ([] : (float) list) in
  (try
  let res = ref (([] : (float) list)) in
  let i = ref (0) in
  (try while (!i < List.length (xs)) do
    try
  let next = ref ((-.(1.0))) in
  let j = ref ((!i + 1)) in
  (try while (!j < List.length (xs)) do
    try
  let inner = List.nth (xs) (!j) in
  if (List.nth (xs) (!i) < inner) then (
  next := inner;
  raise Break;
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  res := (List.append (!res) [(Obj.magic (!next) : float)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list); raise Return
  with Return -> !__ret)

and set_at_float xs idx value =
  let __ret = ref ([] : (float) list) in
  (try
  let idx = (Obj.magic idx : int) in
  let value = (Obj.magic value : float) in
  let i = ref (0) in
  let res = ref (([] : (float) list)) in
  (try while (!i < List.length (xs)) do
    try
  if (!i = idx) then (
  res := (List.append (!res) [(Obj.magic (value) : float)]);
  ) else (
  res := (List.append (!res) [(Obj.magic (List.nth (xs) (!i)) : float)]);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list); raise Return
  with Return -> !__ret)

and next_greatest_element xs =
  let __ret = ref ([] : (float) list) in
  (try
  let res = ref (([] : (float) list)) in
  let k = ref (0) in
  (try while (!k < List.length (xs)) do
    try
  res := (List.append (!res) [(Obj.magic ((-.(1.0))) : float)]);
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  let stack = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < List.length (xs)) do
    try
  (try while ((List.length (!stack) > 0) && (List.nth (xs) (!i) > List.nth (xs) (List.nth (!stack) ((List.length (!stack) - 1))))) do
    try
  let idx = List.nth (!stack) ((List.length (!stack) - 1)) in
  stack := List.of_seq (Seq.take ((List.length (!stack) - 1) - 0) (Seq.drop 0 (List.to_seq (!stack))));
  res := set_at_float (!res) (Obj.repr (idx)) (Obj.repr (List.nth (xs) (!i)));
    with Continue -> ()
  done with Break -> ());
  stack := (List.append (!stack) [(Obj.magic (!i) : int)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!res) : float list); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((__str (next_greatest_element_slow (!arr))));
  print_endline ((__str (next_greatest_element_fast (!arr))));
  print_endline ((__str (next_greatest_element (!arr))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()