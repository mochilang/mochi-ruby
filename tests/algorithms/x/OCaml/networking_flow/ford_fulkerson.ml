(* Generated by Mochi transpiler v0.10.67 on 2025-08-17 13:24 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0 else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _inf = 1000000000
let rec breadth_first_search graph source sink parent =
  let __ret = ref false in
  (try
  let graph = (Obj.magic graph : int list list) in
  let source = (Obj.magic source : int) in
  let sink = (Obj.magic sink : int) in
  parent := (Obj.magic !parent : int list);
  let visited = ref (([] : (bool) list)) in
  let i = ref (0) in
  (try while (!i < List.length (graph)) do
    try
  visited := (Obj.magic ((List.append (!visited) [(Obj.magic (false) : bool)])) : bool list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let queue = ref (([] : (int) list)) in
  queue := (Obj.magic ((List.append (!queue) [(Obj.magic (source) : int)])) : int list);
  visited := (List.mapi (fun __i __x -> if __i = source then true else __x) (!visited));
  let head = ref (0) in
  (try while (!head < List.length (!queue)) do
    try
  let u = (let __l = !queue in let __i = !head in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  head := (!head + 1);
  let row = ref ((let __l = graph in let __i = u in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> [])) in
  let ind = ref (0) in
  (try while (!ind < List.length (!row)) do
    try
  let capacity = (let __l = !row in let __i = !ind in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  if (((let __l = !visited in let __i = !ind in if __i < 0 then false else match List.nth_opt __l __i with Some v -> (Obj.magic v : bool) | None -> false) = false) && (capacity > 0)) then (
  queue := (Obj.magic ((List.append (!queue) [(Obj.magic (!ind) : int)])) : int list);
  visited := (List.mapi (fun __i __x -> if __i = !ind then true else __x) (!visited));
  parent := (List.mapi (fun __i __x -> if __i = !ind then u else __x) (!parent));
  );
  ind := (!ind + 1);
    with Continue -> ()
  done with Break -> ());
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((let __l = !visited in let __i = sink in if __i < 0 then false else match List.nth_opt __l __i with Some v -> (Obj.magic v : bool) | None -> false)) : bool); raise Return
  with Return -> !__ret)

and ford_fulkerson graph source sink =
  let __ret = ref 0 in
  (try
  graph := (Obj.magic !graph : int list list);
  let source = (Obj.magic source : int) in
  let sink = (Obj.magic sink : int) in
  let parent = ref (([] : (int) list)) in
  let i = ref (0) in
  (try while (!i < List.length (!graph)) do
    try
  parent := (Obj.magic ((List.append (!parent) [(Obj.magic (-(1)) : int)])) : int list);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let max_flow = ref (0) in
  (try while breadth_first_search (!graph) (Obj.repr (source)) (Obj.repr (sink)) (parent) do
    try
  let path_flow = ref (_inf) in
  let s = ref (sink) in
  (try while (!s <> source) do
    try
  let prev = (let __l = !parent in let __i = !s in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  let cap = (let __l = (let __l = !graph in let __i = prev in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = !s in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  if (cap < !path_flow) then (
  path_flow := cap;
  );
  s := prev;
    with Continue -> ()
  done with Break -> ());
  max_flow := (!max_flow + !path_flow);
  let v = ref (sink) in
  (try while (!v <> source) do
    try
  let u = (let __l = !parent in let __i = !v in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) in
  graph := (List.mapi (fun __i __x -> if __i = u then (List.mapi (fun __i __x -> if __i = !v then ((let __l = (let __l = !graph in let __i = u in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = !v in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) - !path_flow) else __x) ((let __l = !graph in let __i = u in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []))) else __x) (!graph));
  graph := (List.mapi (fun __i __x -> if __i = !v then (List.mapi (fun __i __x -> if __i = u then ((let __l = (let __l = !graph in let __i = !v in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []) in let __i = u in if __i < 0 then 0 else match List.nth_opt __l __i with Some v -> (Obj.magic v : int) | None -> 0) + !path_flow) else __x) ((let __l = !graph in let __i = !v in if __i < 0 then [] else match List.nth_opt __l __i with Some v -> v | None -> []))) else __x) (!graph));
  v := u;
    with Continue -> ()
  done with Break -> ());
  let j = ref (0) in
  (try while (!j < List.length (!parent)) do
    try
  parent := (List.mapi (fun __i __x -> if __i = !j then -(1) else __x) (!parent));
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!max_flow) : int); raise Return
  with Return -> !__ret)


let graph = ref ([[0; 16; 13; 0; 0; 0]; [0; 0; 10; 12; 0; 0]; [0; 4; 0; 0; 14; 0]; [0; 0; 9; 0; 0; 20]; [0; 0; 0; 7; 0; 4]; [0; 0; 0; 0; 0; 0]])
let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  print_endline ((string_of_int (Obj.magic (ford_fulkerson (graph) (Obj.repr (0)) (Obj.repr (5))) : int)));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
