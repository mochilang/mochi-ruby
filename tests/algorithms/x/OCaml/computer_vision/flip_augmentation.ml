(* Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:21 +0700 *)


let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_list (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_list (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec flip_horizontal_image img =
  let __ret = ref ([] : (int list) list) in
  (try
  let flipped = ref (([] : (int list) list)) in
  let i = ref (0) in
  (try while (!i < List.length (img)) do
    try
  let row = ref (List.nth (img) (!i)) in
  let j = ref ((List.length (!row) - 1)) in
  let new_row = ref (([] : (int) list)) in
  (try while (!j >= 0) do
    try
  new_row := (List.append (!new_row) [(Obj.magic (List.nth (!row) (!j)) : int)]);
  j := (!j - 1);
    with Continue -> ()
  done with Break -> ());
  flipped := (List.append (!flipped) [(Obj.magic (!new_row) : int list)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!flipped) : int list list); raise Return
  with Return -> !__ret)

and flip_vertical_image img =
  let __ret = ref ([] : (int list) list) in
  (try
  let flipped = ref (([] : (int list) list)) in
  let i = ref ((List.length (img) - 1)) in
  (try while (!i >= 0) do
    try
  flipped := (List.append (!flipped) [(Obj.magic (List.nth (img) (!i)) : int list)]);
  i := (!i - 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!flipped) : int list list); raise Return
  with Return -> !__ret)

and flip_horizontal_boxes boxes =
  let __ret = ref ([] : (float list) list) in
  (try
  let result = ref (([] : (float list) list)) in
  let i = ref (0) in
  (try while (!i < List.length (boxes)) do
    try
  let b = ref (List.nth (boxes) (!i)) in
  let x_new = (1.0 -. List.nth (!b) (1)) in
  result := (List.append (!result) [(Obj.magic ([List.nth (!b) (0); x_new; List.nth (!b) (2); List.nth (!b) (3); List.nth (!b) (4)]) : float list)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : float list list); raise Return
  with Return -> !__ret)

and flip_vertical_boxes boxes =
  let __ret = ref ([] : (float list) list) in
  (try
  let result = ref (([] : (float list) list)) in
  let i = ref (0) in
  (try while (!i < List.length (boxes)) do
    try
  let b = ref (List.nth (boxes) (!i)) in
  let y_new = (1.0 -. List.nth (!b) (2)) in
  result := (List.append (!result) [(Obj.magic ([List.nth (!b) (0); List.nth (!b) (1); y_new; List.nth (!b) (3); List.nth (!b) (4)]) : float list)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : float list list); raise Return
  with Return -> !__ret)

and print_image img =
  let __ret = ref (Obj.magic 0) in
  (try
  let i = ref (0) in
  (try while (!i < List.length (img)) do
    try
  let row = ref (List.nth (img) (!i)) in
  let j = ref (0) in
  let line = ref ("") in
  (try while (!j < List.length (!row)) do
    try
  line := ((!line ^ (string_of_int (List.nth (!row) (!j)))) ^ " ");
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline (!line);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
    !__ret
  with Return -> !__ret)


let image = ref ([[1; 2; 3]; [4; 5; 6]; [7; 8; 9]])
let boxes = ref ([[0.0; 0.25; 0.25; 0.5; 0.5]; [1.0; 0.75; 0.75; 0.5; 0.5]])
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ("Original image:");
  ignore (print_image (!image));
  print_endline ((__str (!boxes)));
  print_endline ("Horizontal flip:");
  let h_img = ref (flip_horizontal_image (!image)) in
  let h_boxes = ref (flip_horizontal_boxes (!boxes)) in
  ignore (print_image (!h_img));
  print_endline ((__str (!h_boxes)));
  print_endline ("Vertical flip:");
  let v_img = ref (flip_vertical_image (!image)) in
  let v_boxes = ref (flip_vertical_boxes (!boxes)) in
  ignore (print_image (!v_img));
  print_endline ((__str (!v_boxes)));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()