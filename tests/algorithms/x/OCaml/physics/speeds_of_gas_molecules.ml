(* Generated by Mochi transpiler v0.10.70 on 2025-08-23 14:49 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic r : int) = 0
  else
    match tag r with
    | 0 -> __is_list (field r 1)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _pi = 3.141592653589793
let _r = 8.31446261815324
let rec sqrt x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x <= 0.0) then (
  __ret := (Obj.magic (0.0) : float); raise Return
  );
  let guess = ref (x) in
  let i = ref (0) in
  (try while (!i < 20) do
    try
  guess := ((!guess +. (x /. !guess)) /. 2.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!guess) : float); raise Return
  with Return -> !__ret)

and avg_speed_of_molecule temperature molar_mass =
  let __ret = ref 0.0 in
  (try
  let temperature = (Obj.magic temperature : float) in
  let molar_mass = (Obj.magic molar_mass : float) in
  if (temperature < 0.0) then (
  (failwith ("Absolute temperature cannot be less than 0 K"));
  );
  if (molar_mass <= 0.0) then (
  (failwith ("Molar mass should be greater than 0 kg/mol"));
  );
  let expr = (((8.0 *. _r) *. temperature) /. (_pi *. molar_mass)) in
  let s = sqrt (Obj.repr (expr)) in
  __ret := (Obj.magic (s) : float); raise Return
  with Return -> !__ret)

and mps_speed_of_molecule temperature molar_mass =
  let __ret = ref 0.0 in
  (try
  let temperature = (Obj.magic temperature : float) in
  let molar_mass = (Obj.magic molar_mass : float) in
  if (temperature < 0.0) then (
  (failwith ("Absolute temperature cannot be less than 0 K"));
  );
  if (molar_mass <= 0.0) then (
  (failwith ("Molar mass should be greater than 0 kg/mol"));
  );
  let expr = (((2.0 *. _r) *. temperature) /. molar_mass) in
  let s = sqrt (Obj.repr (expr)) in
  __ret := (Obj.magic (s) : float); raise Return
  with Return -> !__ret)


let () =
  let bench_mem_start = _mem () in
  let bench_start = _now () in
  print_endline ((Printf.sprintf "%.16g" (Obj.magic (avg_speed_of_molecule (Obj.repr (273.0)) (Obj.repr (0.028))) : float)));
  print_endline ((Printf.sprintf "%.16g" (Obj.magic (avg_speed_of_molecule (Obj.repr (300.0)) (Obj.repr (0.032))) : float)));
  print_endline ((Printf.sprintf "%.16g" (Obj.magic (mps_speed_of_molecule (Obj.repr (273.0)) (Obj.repr (0.028))) : float)));
  print_endline ((Printf.sprintf "%.16g" (Obj.magic (mps_speed_of_molecule (Obj.repr (300.0)) (Obj.repr (0.032))) : float)));
  let bench_finish = _now () in
  let bench_mem_end = _mem () in
  let bench_dur = (bench_finish - bench_start) / 1000 in
  let bench_mem_bytes = Stdlib.max 0 (bench_mem_end - bench_mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" bench_dur bench_mem_bytes "main";
  ()
