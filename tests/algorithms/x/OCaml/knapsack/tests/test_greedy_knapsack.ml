(* Generated by Mochi transpiler v0.10.63 on 2025-08-12 08:38 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then (magic v : int) = 0
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __show_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if __is_list v then
    __str_list (Obj.magic v)
  else if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec calc_profit profit weight max_weight =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let profit = (Obj.magic profit : int list) in
  let weight = (Obj.magic weight : int list) in
  let max_weight = (Obj.magic max_weight : int) in
  if (List.length (profit) <> List.length (weight)) then (
  __ret := (Obj.magic ([(__str ("ok"), Obj.repr (false)); (__str ("value"), Obj.repr (0.0)); (__str ("error"), Obj.repr ("The length of profit and weight must be same."))]) : (string * Obj.t) list); raise Return
  );
  if (max_weight <= 0) then (
  __ret := (Obj.magic ([(__str ("ok"), Obj.repr (false)); (__str ("value"), Obj.repr (0.0)); (__str ("error"), Obj.repr ("max_weight must greater than zero."))]) : (string * Obj.t) list); raise Return
  );
  let i = ref (0) in
  (try while (!i < List.length (profit)) do
    try
  if ((let __l = profit in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) < 0) then (
  __ret := (Obj.magic ([(__str ("ok"), Obj.repr (false)); (__str ("value"), Obj.repr (0.0)); (__str ("error"), Obj.repr ("Profit can not be negative."))]) : (string * Obj.t) list); raise Return
  );
  if ((let __l = weight in let __i = !i in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)) < 0) then (
  __ret := (Obj.magic ([(__str ("ok"), Obj.repr (false)); (__str ("value"), Obj.repr (0.0)); (__str ("error"), Obj.repr ("Weight can not be negative."))]) : (string * Obj.t) list); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let used = ref ((Obj.magic [] : bool list)) in
  let j = ref (0) in
  (try while (!j < List.length (profit)) do
    try
  used := (Obj.magic (List.append (!used) [(Obj.magic (false) : bool)]) : bool list);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  let limit = ref (0) in
  let gain = ref (0.0) in
  (try while (!limit < max_weight) do
    try
  let max_ratio = ref ((-.(1.0))) in
  let idx = ref ((0 - 1)) in
  let k = ref (0) in
  (try while (!k < List.length (profit)) do
    try
  if not ((let __l = !used in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) then (
  let ratio = (float_of_int ((let __l = profit in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) /. float_of_int ((let __l = weight in let __i = !k in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) in
  if (ratio > !max_ratio) then (
  max_ratio := ratio;
  idx := !k;
  );
  );
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  if (!idx = (0 - 1)) then (
  raise Break;
  );
  used := (List.mapi (fun __i __x -> if __i = !idx then true else __x) (!used));
  if ((max_weight - !limit) >= (let __l = weight in let __i = !idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))) then (
  limit := (!limit + (let __l = weight in let __i = !idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)));
  gain := (!gain +. float_of_int ((let __l = profit in let __i = !idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i))));
  ) else (
  gain := (!gain +. ((float_of_int ((max_weight - !limit)) /. float_of_int ((let __l = weight in let __i = !idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))) *. float_of_int ((let __l = profit in let __i = !idx in let __len = List.length __l in List.nth __l (if __i >= 0 then __i else __len + __i)))));
  raise Break;
  );
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ([(__str ("ok"), Obj.repr (true)); (__str ("value"), Obj.repr (!gain)); (__str ("error"), Obj.repr (""))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and test_sorted () =
  let __ret = ref false in
  (try
  let profit = ref ([10; 20; 30; 40; 50; 60]) in
  let weight = ref ([2; 4; 6; 8; 10; 12]) in
  let res = calc_profit (!profit) (!weight) (Obj.repr (100)) in
  __ret := (Obj.magic (((Obj.obj (List.assoc (__str ("ok")) (res) : Obj.t) : bool) && ((Obj.obj (List.assoc (__str ("value")) (res) : Obj.t) : float) = 210.0))) : bool); raise Return
  with Return -> !__ret)

and test_negative_max_weight () =
  let __ret = ref false in
  (try
  let profit = ref ([10; 20; 30; 40; 50; 60]) in
  let weight = ref ([2; 4; 6; 8; 10; 12]) in
  let res = calc_profit (!profit) (!weight) (Obj.repr (-(15))) in
  __ret := (Obj.magic ((not ((Obj.obj (List.assoc (__str ("ok")) (res) : Obj.t) : bool)) && ((Obj.obj (List.assoc (__str ("error")) (res) : Obj.t) : string) = "max_weight must greater than zero."))) : bool); raise Return
  with Return -> !__ret)

and test_negative_profit_value () =
  let __ret = ref false in
  (try
  let profit = ref ([10; -(20); 30; 40; 50; 60]) in
  let weight = ref ([2; 4; 6; 8; 10; 12]) in
  let res = calc_profit (!profit) (!weight) (Obj.repr (15)) in
  __ret := (Obj.magic ((not ((Obj.obj (List.assoc (__str ("ok")) (res) : Obj.t) : bool)) && ((Obj.obj (List.assoc (__str ("error")) (res) : Obj.t) : string) = "Profit can not be negative."))) : bool); raise Return
  with Return -> !__ret)

and test_negative_weight_value () =
  let __ret = ref false in
  (try
  let profit = ref ([10; 20; 30; 40; 50; 60]) in
  let weight = ref ([2; -(4); 6; -(8); 10; 12]) in
  let res = calc_profit (!profit) (!weight) (Obj.repr (15)) in
  __ret := (Obj.magic ((not ((Obj.obj (List.assoc (__str ("ok")) (res) : Obj.t) : bool)) && ((Obj.obj (List.assoc (__str ("error")) (res) : Obj.t) : string) = "Weight can not be negative."))) : bool); raise Return
  with Return -> !__ret)

and test_null_max_weight () =
  let __ret = ref false in
  (try
  let profit = ref ([10; 20; 30; 40; 50; 60]) in
  let weight = ref ([2; 4; 6; 8; 10; 12]) in
  let res = calc_profit (!profit) (!weight) (Obj.repr (0)) in
  __ret := (Obj.magic ((not ((Obj.obj (List.assoc (__str ("ok")) (res) : Obj.t) : bool)) && ((Obj.obj (List.assoc (__str ("error")) (res) : Obj.t) : string) = "max_weight must greater than zero."))) : bool); raise Return
  with Return -> !__ret)

and test_unequal_list_length () =
  let __ret = ref false in
  (try
  let profit = ref ([10; 20; 30; 40; 50]) in
  let weight = ref ([2; 4; 6; 8; 10; 12]) in
  let res = calc_profit (!profit) (!weight) (Obj.repr (100)) in
  __ret := (Obj.magic ((not ((Obj.obj (List.assoc (__str ("ok")) (res) : Obj.t) : bool)) && ((Obj.obj (List.assoc (__str ("error")) (res) : Obj.t) : string) = "The length of profit and weight must be same."))) : bool); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (string_of_bool (test_sorted ()));
  print_endline (string_of_bool (test_negative_max_weight ()));
  print_endline (string_of_bool (test_negative_profit_value ()));
  print_endline (string_of_bool (test_negative_weight_value ()));
  print_endline (string_of_bool (test_null_max_weight ()));
  print_endline (string_of_bool (test_unequal_list_length ()));
  print_endline (string_of_bool true);
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
