(* Generated by Mochi transpiler v0.10.61 on 2025-08-09 10:14 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Return

let _pi = 3.141592653589793
let rec ind_reactance inductance frequency reactance =
  let __ret = ref ([] : (string * float) list) in
  (try
  let inductance = (Obj.magic inductance : float) in
  let frequency = (Obj.magic frequency : float) in
  let reactance = (Obj.magic reactance : float) in
  let zero_count = ref (0) in
  if (inductance = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (frequency = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (reactance = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (!zero_count <> 1) then (
  (failwith ("One and only one argument must be 0"));
  );
  if (inductance < 0.0) then (
  (failwith ("Inductance cannot be negative"));
  );
  if (frequency < 0.0) then (
  (failwith ("Frequency cannot be negative"));
  );
  if (reactance < 0.0) then (
  (failwith ("Inductive reactance cannot be negative"));
  );
  if (inductance = 0.0) then (
  __ret := (Obj.magic ([(__str ("inductance"), (reactance /. ((2.0 *. _pi) *. frequency)))]) : (string * float) list); raise Return
  );
  if (frequency = 0.0) then (
  __ret := (Obj.magic ([(__str ("frequency"), (reactance /. ((2.0 *. _pi) *. inductance)))]) : (string * float) list); raise Return
  );
  __ret := (Obj.magic ([(__str ("reactance"), (((2.0 *. _pi) *. frequency) *. inductance))]) : (string * float) list); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (__show (ind_reactance (Obj.repr (0.0)) (Obj.repr (10000.0)) (Obj.repr (50.0))));
  print_endline (__show (ind_reactance (Obj.repr (0.035)) (Obj.repr (0.0)) (Obj.repr (50.0))));
  print_endline (__show (ind_reactance (Obj.repr (0.000035)) (Obj.repr (1000.0)) (Obj.repr (0.0))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
