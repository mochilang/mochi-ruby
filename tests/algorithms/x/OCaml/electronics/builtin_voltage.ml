(* Generated by Mochi transpiler v0.10.61 on 2025-08-09 10:14 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec pow10 n =
  let __ret = ref 0.0 in
  (try
  let n = (Obj.magic n : int) in
  let result = ref (1.0) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  result := (!result *. 10.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!result) : float); raise Return
  with Return -> !__ret)


let _boltzmann = (1.380649 /. pow10 (Obj.repr (23)))
let _electron_volt = (1.602176634 /. pow10 (Obj.repr (19)))
let _temperature = 300.0
let rec ln_series x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  let t = ((x -. 1.0) /. (x +. 1.0)) in
  let term = ref (t) in
  let sum = ref (0.0) in
  let n = ref (1) in
  (try while (!n <= 19) do
    try
  sum := (!sum +. (!term /. float_of_int (!n)));
  term := ((!term *. t) *. t);
  n := (!n + 2);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((2.0 *. !sum)) : float); raise Return
  with Return -> !__ret)

and ln x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  let y = ref (x) in
  let k = ref (0) in
  (try while (!y >= 10.0) do
    try
  y := (!y /. 10.0);
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  (try while (!y < 1.0) do
    try
  y := (!y *. 10.0);
  k := (!k - 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((ln_series (Obj.repr (!y)) +. (float_of_int (!k) *. ln_series (Obj.repr (10.0))))) : float); raise Return
  with Return -> !__ret)

and builtin_voltage donor_conc acceptor_conc intrinsic_conc =
  let __ret = ref 0.0 in
  (try
  let donor_conc = (Obj.magic donor_conc : float) in
  let acceptor_conc = (Obj.magic acceptor_conc : float) in
  let intrinsic_conc = (Obj.magic intrinsic_conc : float) in
  if (donor_conc <= 0.0) then (
  (failwith ("Donor concentration should be positive"));
  );
  if (acceptor_conc <= 0.0) then (
  (failwith ("Acceptor concentration should be positive"));
  );
  if (intrinsic_conc <= 0.0) then (
  (failwith ("Intrinsic concentration should be positive"));
  );
  if (donor_conc <= intrinsic_conc) then (
  (failwith ("Donor concentration should be greater than intrinsic concentration"));
  );
  if (acceptor_conc <= intrinsic_conc) then (
  (failwith ("Acceptor concentration should be greater than intrinsic concentration"));
  );
  __ret := (Obj.magic ((((_boltzmann *. _temperature) *. ln (Obj.repr (((donor_conc *. acceptor_conc) /. (intrinsic_conc *. intrinsic_conc))))) /. _electron_volt)) : float); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline ((Printf.sprintf "%.16g" (builtin_voltage (Obj.repr (pow10 (Obj.repr (17)))) (Obj.repr (pow10 (Obj.repr (17)))) (Obj.repr (pow10 (Obj.repr (10)))))));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
