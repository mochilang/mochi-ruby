(* Generated by Mochi transpiler v0.10.61 on 2025-08-09 10:14 +0700 *)


let rec __is_list v =
  let open Obj in
  let r = repr v in
  if is_int r then false
  else
    match tag r with
    | 0 ->
        let t = field r 1 in
        if is_int t then (magic t : int) = 0
        else __is_list (magic t)
    | _ -> false

let rec __show v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __show_list (Obj.magic v) else __show_tuple (Obj.magic v)
    | 252 -> Printf.sprintf "'%s'" (String.escaped (magic v : string))
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __show_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __show l) ^ "]"
and __show_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __show (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"
and __str v =
  let open Obj in
  let r = repr v in
  if is_int r then
    string_of_int (magic v : int)
  else
    match tag r with
    | 0 -> if __is_list v then __str_list (Obj.magic v) else __str_tuple (Obj.magic v)
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"
and __str_list l =
  match l with
  | [] -> "[]"
  | _ -> "[" ^ String.concat " " (List.map __str l) ^ "]"
and __str_tuple t =
  let open Obj in
  let r = repr t in
  let rec aux i =
    if i >= size r then []
    else __str (Obj.obj (field r i)) :: aux (i + 1)
  in
  "(" ^ String.concat ", " (aux 0) ^ ")"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _coulombs_constant = 8988000000.0
let rec abs x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x < 0.0) then (
  __ret := (Obj.magic ((-.(x))) : float); raise Return
  );
  __ret := (Obj.magic (x) : float); raise Return
  with Return -> !__ret)

and sqrtApprox x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x <= 0.0) then (
  __ret := (Obj.magic (0.0) : float); raise Return
  );
  let guess = ref (x) in
  let i = ref (0) in
  (try while (!i < 20) do
    try
  guess := ((!guess +. (x /. !guess)) /. 2.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!guess) : float); raise Return
  with Return -> !__ret)

and coulombs_law force charge1 charge2 distance =
  let __ret = ref ([] : (string * float) list) in
  (try
  let force = (Obj.magic force : float) in
  let charge1 = (Obj.magic charge1 : float) in
  let charge2 = (Obj.magic charge2 : float) in
  let distance = (Obj.magic distance : float) in
  let charge_product = abs_float (charge1 *. charge2) in
  let zero_count = ref (0) in
  if (force = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (charge1 = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (charge2 = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (distance = 0.0) then (
  zero_count := (!zero_count + 1);
  );
  if (!zero_count <> 1) then (
  (failwith ("One and only one argument must be 0"));
  );
  if (distance < 0.0) then (
  (failwith ("Distance cannot be negative"));
  );
  if (force = 0.0) then (
  let f = ((_coulombs_constant *. charge_product) /. (distance *. distance)) in
  __ret := (Obj.magic ([(__str ("force"), f)]) : (string * float) list); raise Return
  );
  if (charge1 = 0.0) then (
  let c1 = ((abs_float force *. (distance *. distance)) /. (_coulombs_constant *. charge2)) in
  __ret := (Obj.magic ([(__str ("charge1"), c1)]) : (string * float) list); raise Return
  );
  if (charge2 = 0.0) then (
  let c2 = ((abs_float force *. (distance *. distance)) /. (_coulombs_constant *. charge1)) in
  __ret := (Obj.magic ([(__str ("charge2"), c2)]) : (string * float) list); raise Return
  );
  let d = sqrtApprox (Obj.repr (((_coulombs_constant *. charge_product) /. abs_float force))) in
  __ret := (Obj.magic ([(__str ("distance"), d)]) : (string * float) list); raise Return
  with Return -> !__ret)

and print_map m =
  let __ret = ref (Obj.magic 0) in
  (try
  (try List.iter (fun (k, _) ->
    try
  print_endline ((((("{\"" ^ k) ^ "\": ") ^ (Printf.sprintf "%.16g" (List.assoc (__str (k)) (m)))) ^ "}"));
    with Continue -> ()) (m) with Break -> ());
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (print_map ((Obj.magic coulombs_law (Obj.repr (0.0)) (Obj.repr (3.0)) (Obj.repr (5.0)) (Obj.repr (2000.0)) : (string * float) list)));
  ignore (print_map ((Obj.magic coulombs_law (Obj.repr (10.0)) (Obj.repr (3.0)) (Obj.repr (5.0)) (Obj.repr (0.0)) : (string * float) list)));
  ignore (print_map ((Obj.magic coulombs_law (Obj.repr (10.0)) (Obj.repr (0.0)) (Obj.repr (5.0)) (Obj.repr (2000.0)) : (string * float) list)));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()
