-- Generated by Mochi v0.10.72 on 2025-08-25 08:35 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function abs(x)
    if (x < 0.0) then
      return (-x)
    end
    return x
  end
  function to_radians(deg)
    return ((deg * PI) / 180.0)
  end
  function sin_taylor(x)
    local term = x
    local sum = x
    local i = 1
    while (i < 10) do
      local k1 = (2.0 * i)
      local k2 = (k1 + 1.0)
      term = ((((-term) * x) * x) / (k1 * k2))
      sum = (sum + term)
      i = (i + 1)
    end
    return sum
  end
  function cos_taylor(x)
    local term = 1.0
    local sum = 1.0
    local i = 1
    while (i < 10) do
      local k1 = ((2.0 * i) - 1.0)
      local k2 = (2.0 * i)
      term = ((((-term) * x) * x) / (k1 * k2))
      sum = (sum + term)
      i = (i + 1)
    end
    return sum
  end
  function rect(mag, angle)
    local c = cos_taylor(angle)
    local s = sin_taylor(angle)
    return {(mag * c), (mag * s)}
  end
  function multiply(a, b)
    return {((a[0 + 1] * b[0 + 1]) - (a[1 + 1] * b[1 + 1])), ((a[0 + 1] * b[1 + 1]) + (a[1 + 1] * b[0 + 1]))}
  end
  function apparent_power(voltage, current, voltage_angle, current_angle)
    local vrad = to_radians(voltage_angle)
    local irad = to_radians(current_angle)
    local vrect = rect(voltage, vrad)
    local irect = rect(current, irad)
    local result = multiply(vrect, irect)
    return result
  end
  function approx_equal(a, b, eps)
    return ((abs((a[0 + 1] - b[0 + 1])) < eps) and (abs((a[1 + 1] - b[1 + 1])) < eps))
  end
  PI = 3.141592653589793
  local _bench_end = os.clock()
  collectgarbage()
  local _bench_end_mem = collectgarbage('count') * 1024
  local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
  local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
  print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
