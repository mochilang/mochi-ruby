-- Generated by Mochi v0.10.63 on 2025-08-11 16:20 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function solution(length)
    local ways = {}
    local i = 0
    while (i <= length) do
      local row = {}
      row = (function(lst, item)
      lst = lst or {}
      table.insert(lst, item)
      return lst
    end)(row, 0)
    row = (function(lst, item)
    lst = lst or {}
    table.insert(lst, item)
    return lst
  end)(row, 0)
  row = (function(lst, item)
  lst = lst or {}
  table.insert(lst, item)
  return lst
end)(row, 0)
ways = (function(lst, item)
lst = lst or {}
table.insert(lst, item)
return lst
end)(ways, row)
i = (i + 1)
end
local row_length = 0
while (row_length <= length) do
  local tile_length = 2
  while (tile_length <= 4) do
    local tile_start = 0
    while (tile_start <= (row_length - tile_length)) do
      local remaining = ((row_length - tile_start) - tile_length)
      ways[row_length + 1][(tile_length - 2) + 1] = ((ways[row_length + 1][(tile_length - 2) + 1] + ways[remaining + 1][(tile_length - 2) + 1]) + 1)
      tile_start = (tile_start + 1)
    end
    tile_length = (tile_length + 1)
  end
  row_length = (row_length + 1)
end
local total = 0
local j = 0
while (j < 3) do
  total = (total + ways[length + 1][j + 1])
  j = (j + 1)
end
return total
end
print(solution(5))
print(solution(50))
local _bench_end = os.clock()
collectgarbage()
local _bench_end_mem = collectgarbage('count') * 1024
local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
