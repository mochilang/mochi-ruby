-- Generated by Mochi v0.10.61 on 2025-08-08 17:12 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end

local function _equal(a, b)
if a == b then return true end
if type(a) ~= type(b) then return false end
if type(a) ~= 'table' then return a == b end
local count = 0
for k, v in pairs(a) do
  if not _equal(v, b[k]) then return false end
  count = count + 1
end
for k in pairs(b) do
  count = count - 1
end
return count == 0
end

local function _substring(s, i, j)
i = i + 1
if j == nil then j = #s end
local si = utf8.offset(s, i)
if not si then return '' end
local sj = utf8.offset(s, j+1)
if not sj then sj = -1 end
return string.sub(s, si, sj-1)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function find_substring(s, sub, from)
    local i = from
    while (i <= ((function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' then
          local l = utf8.len(v)
          if l then return l end
          return #v
        elseif type(v) == 'table' then
            return #v
          else
            return 0
          end
        end)(s) - (function(v)
        if type(v) == 'table' and v.items ~= nil then
          return #v.items
        elseif type(v) == 'table' and (v[1] == nil) then
            local c = 0
            for _ in pairs(v) do c = c + 1 end
            return c
          elseif type(v) == 'string' then
              local l = utf8.len(v)
              if l then return l end
              return #v
            elseif type(v) == 'table' then
                return #v
              else
                return 0
              end
            end)(sub))) do
              local j = 0
              while ((j < (function(v)
              if type(v) == 'table' and v.items ~= nil then
                return #v.items
              elseif type(v) == 'table' and (v[1] == nil) then
                  local c = 0
                  for _ in pairs(v) do c = c + 1 end
                  return c
                elseif type(v) == 'string' then
                    local l = utf8.len(v)
                    if l then return l end
                    return #v
                  elseif type(v) == 'table' then
                      return #v
                    else
                      return 0
                    end
                  end)(sub)) and (_substring(s, (i + j), ((i + j) + 1)) == _substring(sub, j, (j + 1)))) do
                    j = (j + 1)
                  end
                  if (j == (function(v)
                  if type(v) == 'table' and v.items ~= nil then
                    return #v.items
                  elseif type(v) == 'table' and (v[1] == nil) then
                      local c = 0
                      for _ in pairs(v) do c = c + 1 end
                      return c
                    elseif type(v) == 'string' then
                        local l = utf8.len(v)
                        if l then return l end
                        return #v
                      elseif type(v) == 'table' then
                          return #v
                        else
                          return 0
                        end
                      end)(sub)) then
                        return i
                      end
                      i = (i + 1)
                    end
                    return (-1)
                  end
                  function fetch_pharmacy_and_price_list(drug_name, zip_code)
                    if ((drug_name == "") or (zip_code == "")) then
                      return _nil
                    end
                    local results = {}
                    local pos = 0
                    local block_tag = "<div class=\"grid-x pharmCard\">"
                    local name_tag = "<p class=\"list-title\">"
                    local price_tag = "<span class=\"price price-large\">"
                    while true do
                      local div_idx = find_substring(SAMPLE_HTML, block_tag, pos)
                      if (div_idx < 0) then
                        break
                      end
                      local name_start = find_substring(SAMPLE_HTML, name_tag, div_idx)
                      if (name_start < 0) then
                        break
                      end
                      name_start = (name_start + (function(v)
                      if type(v) == 'table' and v.items ~= nil then
                        return #v.items
                      elseif type(v) == 'table' and (v[1] == nil) then
                          local c = 0
                          for _ in pairs(v) do c = c + 1 end
                          return c
                        elseif type(v) == 'string' then
                            local l = utf8.len(v)
                            if l then return l end
                            return #v
                          elseif type(v) == 'table' then
                              return #v
                            else
                              return 0
                            end
                          end)(name_tag))
                          local name_end = find_substring(SAMPLE_HTML, "</p>", name_start)
                          if (name_end < 0) then
                            break
                          end
                          local name = _substring(SAMPLE_HTML, name_start, name_end)
                          local price_start = find_substring(SAMPLE_HTML, price_tag, name_end)
                          if (price_start < 0) then
                            break
                          end
                          price_start = (price_start + (function(v)
                          if type(v) == 'table' and v.items ~= nil then
                            return #v.items
                          elseif type(v) == 'table' and (v[1] == nil) then
                              local c = 0
                              for _ in pairs(v) do c = c + 1 end
                              return c
                            elseif type(v) == 'string' then
                                local l = utf8.len(v)
                                if l then return l end
                                return #v
                              elseif type(v) == 'table' then
                                  return #v
                                else
                                  return 0
                                end
                              end)(price_tag))
                              local price_end = find_substring(SAMPLE_HTML, "</span>", price_start)
                              if (price_end < 0) then
                                break
                              end
                              local price = _substring(SAMPLE_HTML, price_start, price_end)
                              results = (function(lst, item)
                              lst = lst or {}
                              table.insert(lst, item)
                              return lst
                            end)(results, {__name = "GenType1", __order = {"pharmacy_name", "price"}, pharmacy_name = name, price = price})
                            pos = price_end
                          end
                          return results
                        end
                        SAMPLE_HTML = "<div class=\"grid-x pharmCard\"><p class=\"list-title\">Pharmacy A</p><span class=\"price price-large\">$10.00</span></div><div class=\"grid-x pharmCard\"><p class=\"list-title\">Pharmacy B</p><span class=\"price price-large\">$12.50</span></div>"
                        pharmacy_price_list = fetch_pharmacy_and_price_list("aspirin", "30303")
                        if (not _equal(pharmacy_price_list, _nil)) then
                          i = 0
                          while (i < (function(v)
                          if type(v) == 'table' and v.items ~= nil then
                            return #v.items
                          elseif type(v) == 'table' and (v[1] == nil) then
                              local c = 0
                              for _ in pairs(v) do c = c + 1 end
                              return c
                            elseif type(v) == 'string' then
                                local l = utf8.len(v)
                                if l then return l end
                                return #v
                              elseif type(v) == 'table' then
                                  return #v
                                else
                                  return 0
                                end
                              end)(pharmacy_price_list)) do
                                entry = pharmacy_price_list[i + 1]
                                print((((type(((("Pharmacy: " .. entry.pharmacy_name) .. " Price: ") .. entry.price)) == "table")) and (
                                (function(v)
                                local function encode(x)
                                if type(x) == "table" then
                                  if x.__name and x.__order then
                                    local parts = {x.__name, " {"}
                                    for i, k in ipairs(x.__order) do
                                      if i > 1 then parts[#parts+1] = ", " end
                                      parts[#parts+1] = k .. " = " .. encode(x[k])
                                    end
                                    parts[#parts+1] = "}"
                                    return table.concat(parts)
                                  elseif #x > 0 then
                                      local allTables = true
                                      for _, v in ipairs(x) do
                                        if type(v) ~= "table" then allTables = false break end
                                      end
                                      local parts = {}
                                      if not allTables then parts[#parts+1] = "[" end
                                      for i, val in ipairs(x) do
                                        parts[#parts+1] = encode(val)
                                        if i < #x then parts[#parts+1] = " " end
                                      end
                                      if not allTables then parts[#parts+1] = "]" end
                                      return table.concat(parts)
                                    else
                                      local keys = {}
                                      for k in pairs(x) do if k ~= "__name" and k ~= "__order" then table.insert(keys, k) end end
                                      table.sort(keys, function(a,b) return tostring(a) > tostring(b) end)
                                      local parts = {"{"}
                                      for i, k in ipairs(keys) do
                                        parts[#parts+1] = "'" .. tostring(k) .. "': " .. encode(x[k])
                                        if i < #keys then parts[#parts+1] = ", " end
                                      end
                                      parts[#parts+1] = "}"
                                      return table.concat(parts)
                                    end
                                  elseif type(x) == "string" then
                                      return '"' .. x .. '"'
                                    else
                                      return tostring(x)
                                    end
                                  end
                                  return encode(v)
                                end)(((("Pharmacy: " .. entry.pharmacy_name) .. " Price: ") .. entry.price))) or (((("Pharmacy: " .. entry.pharmacy_name) .. " Price: ") .. entry.price))))
                                i = (i + 1)
                              end
                            else
                              print("No results found")
                            end
                            local _bench_end = os.clock()
                            collectgarbage()
                            local _bench_end_mem = collectgarbage('count') * 1024
                            local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
                            local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
                            print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
                          end;
