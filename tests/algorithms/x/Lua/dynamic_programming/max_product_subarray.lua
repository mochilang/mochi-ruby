-- Generated by Mochi v0.10.72 on 2025-08-25 08:35 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end

local function _len(v)
if type(v) == 'table' and v.items ~= nil then
  return #v.items
elseif type(v) == 'table' and (v[1] == nil or v[0] ~= nil) then
    local c = 0
    for k in pairs(v) do
      if k ~= '__name' and k ~= '__order' then
        c = c + 1
      end
    end
    return c
  elseif type(v) == 'string' then
      local l = utf8.len(v)
      if l then return l end
      return #v
    elseif type(v) == 'table' then
        return #v
      else
        return 0
      end
    end
    do
      collectgarbage()
      local _bench_start_mem = collectgarbage('count') * 1024
      local _bench_start = os.clock()
      function max_product_subarray(numbers)
        if (_len(numbers) == 0) then
          return 0
        end
        local max_till_now = numbers[0 + 1]
        local min_till_now = numbers[0 + 1]
        local max_prod = numbers[0 + 1]
        local i = 1
        while (i < _len(numbers)) do
          local number = numbers[i + 1]
          if (number < 0) then
            local temp = max_till_now
            max_till_now = min_till_now
            min_till_now = temp
          end
          local prod_max = (max_till_now * number)
          if (number > prod_max) then
            max_till_now = number
          else
            max_till_now = prod_max
          end
          local prod_min = (min_till_now * number)
          if (number < prod_min) then
            min_till_now = number
          else
            min_till_now = prod_min
          end
          if (max_till_now > max_prod) then
            max_prod = max_till_now
          end
          i = (i + 1)
        end
        return max_prod
      end
      print(max_product_subarray({2, 3, (-2), 4}))
      print(max_product_subarray({(-2), 0, (-1)}))
      local _bench_end = os.clock()
      collectgarbage()
      local _bench_end_mem = collectgarbage('count') * 1024
      local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
      local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
      print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
    end;
