-- Generated by Mochi v0.10.72 on 2025-08-25 08:35 GMT+7
function input()
  local line = io.read('*l')
  if line == nil then return '' end
  return line
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end

local function _len(v)
if type(v) == 'table' and v.items ~= nil then
  return #v.items
elseif type(v) == 'table' and (v[1] == nil or v[0] ~= nil) then
    local c = 0
    for k in pairs(v) do
      if k ~= '__name' and k ~= '__order' then
        c = c + 1
      end
    end
    return c
  elseif type(v) == 'string' then
      local l = utf8.len(v)
      if l then return l end
      return #v
    elseif type(v) == 'table' then
        return #v
      else
        return 0
      end
    end
    
    local function slice(lst, s, e)
    local len = #lst
    if s < 0 then s = len + s end
    if s < 0 then s = 0 end
    if e == nil then
      e = len
    elseif e < 0 then
        e = len + e
      end
      if e > len then e = len end
      if s > e then return {} end
      local r = {}
      for i = s + 1, e do
        r[#r+1] = lst[i]
      end
      return r
    end
    do
      collectgarbage()
      local _bench_start_mem = collectgarbage('count') * 1024
      local _bench_start = os.clock()
      function longest_subsequence(xs)
        local n = _len(xs)
        if (n <= 1) then
          return xs
        end
        local pivot = xs[0 + 1]
        local is_found = false
        local i = 1
        local longest_subseq = {}
        while ((not is_found) and (i < n)) do
          if (xs[i + 1] < pivot) then
            is_found = true
            local temp_array = slice(xs, i, n)
            temp_array = longest_subsequence(temp_array)
            if (_len(temp_array) > _len(longest_subseq)) then
              longest_subseq = temp_array
            end
          else
            i = (i + 1)
          end
        end
        local filtered = {}
        local j = 1
        while (j < n) do
          if (xs[j + 1] >= pivot) then
            filtered = (function(lst, item)
            local res = {table.unpack(lst or {})}
            res[#res+1] = item
            return res
          end)(filtered, xs[j + 1])
        end
        j = (j + 1)
      end
      local candidate = {}
      candidate = (function(lst, item)
      local res = {table.unpack(lst or {})}
      res[#res+1] = item
      return res
    end)(candidate, pivot)
    candidate = (function(a, b)
    local res = {table.unpack(a or {})}
    for _, v in ipairs(b or {}) do
      res[#res+1] = v
    end
    return res
  end)(candidate, longest_subsequence(filtered))
  if (_len(candidate) > _len(longest_subseq)) then
    return candidate
  else
    return longest_subseq
  end
end
local _bench_end = os.clock()
collectgarbage()
local _bench_end_mem = collectgarbage('count') * 1024
local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
