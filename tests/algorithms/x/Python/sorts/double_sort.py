# Code generated by Mochi transpiler.
# Version 0.10.63, generated on 2025-08-11 16:20 +0700
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


def _str(v):
    if isinstance(v, float):
        if v.is_integer():
            return str(int(v))
        return format(v, ".15g")
    return str(v)


def _set_index(lst, idx, val):
    if lst is None:
        lst = []
    if idx >= len(lst):
        lst.extend([None] * (idx - len(lst) + 1))
    lst[idx] = val
    return lst

def double_sort(collection):
    no_of_elements = len(collection)
    passes = ((no_of_elements - 1) // 2) + 1
    i = 0
    while i < passes:
        j = 0
        while j < no_of_elements - 1:
            if collection[j + 1] < collection[j]:
                tmp = collection[j]
                collection[j] = collection[j + 1]
                collection[j + 1] = tmp
            if collection[no_of_elements - 1 - j] < collection[no_of_elements - 2 - j]:
                tmp2 = collection[no_of_elements - 1 - j]
                collection[no_of_elements - 1 - j] = collection[no_of_elements - 2 - j]
                collection[no_of_elements - 2 - j] = tmp2
            j = j + 1
        i = i + 1
    return collection
print(_str(double_sort([-1, -2, -3, -4, -5, -6, -7])))
print(_str(double_sort([])))
print(_str(double_sort([-1, -2, -3, -4, -5, -6])))
print(_str(double_sort([-3, 10, 16, -42, 29]) == [-42, -3, 10, 16, 29]))

