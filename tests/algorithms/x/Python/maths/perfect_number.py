# Code generated by Mochi transpiler.
# Version 0.10.61, generated on 2025-08-08 16:09 +0700
import json
import resource
import time

import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())


def _str(v):
    if isinstance(v, float) and v.is_integer():
        return str(int(v))
    return str(v)

_bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
_bench_start = _now()
try:
    def perfect(n):
        if n <= 0:
            return False
        total = 0
        divisor = 1
        while divisor <= (n // 2):
            if n % divisor == 0:
                total = total + divisor
            divisor = divisor + 1
        return total == n
    print(_str(perfect(27)))
    print(_str(perfect(28)))
    print(_str(perfect(29)))
    print(_str(perfect(6)))
    print(_str(perfect(12)))
    print(_str(perfect(496)))
    print(_str(perfect(8128)))
    print(_str(perfect(0)))
    print(_str(perfect(-1)))
    print(_str(perfect(33550336)))
    print(_str(perfect(33550337)))
    print(_str(perfect(1)))
finally:
    _bench_end = _now()
    _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": _bench_mem_end*1024, "name": "main"}, indent=2))
