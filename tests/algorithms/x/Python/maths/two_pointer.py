# Code generated by Mochi transpiler.
# Version 0.10.61, generated on 2025-08-08 17:13 +0700
import json
import resource
import time

import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

def two_pointer(nums, target):
    i = 0
    j = len(nums) - 1
    while i < j:
        s = nums[i] + nums[j]
        if s == target:
            return [i, j]
        if s < target:
            i = i + 1
        else:
            j = j - 1
    return []
def test_two_pointer():
    if two_pointer([2, 7, 11, 15], 9) != [0, 1]:
        panic("case1")
    if two_pointer([2, 7, 11, 15], 17) != [0, 3]:
        panic("case2")
    if two_pointer([2, 7, 11, 15], 18) != [1, 2]:
        panic("case3")
    if two_pointer([2, 7, 11, 15], 26) != [2, 3]:
        panic("case4")
    if two_pointer([1, 3, 3], 6) != [1, 2]:
        panic("case5")
    if len(two_pointer([2, 7, 11, 15], 8)) != 0:
        panic("case6")
    if len(two_pointer([0, 3, 6, 9, 12, 15, 18, 21, 24, 27], 19)) != 0:
        panic("case7")
    if len(two_pointer([1, 2, 3], 6)) != 0:
        panic("case8")
def main():
    _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    _bench_start = _now()
    try:
        test_two_pointer()
        print(two_pointer([2, 7, 11, 15], 9))
    finally:
        _bench_end = _now()
        _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
        print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": _bench_mem_end*1024, "name": "main"}, indent=2))
main()
