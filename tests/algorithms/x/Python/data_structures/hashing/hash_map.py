# Code generated by Mochi transpiler.
# Version 0.10.72, generated on 2025-08-24 16:59 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
import dataclasses
import json
import time

try:
    import resource
except Exception:
    resource = None
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
if hasattr(sys, "setrecursionlimit"):
    sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())


def _append(lst, v):
    if lst is None:
        lst = []
    return lst + [v]


import builtins
def _str(v):
    if isinstance(v, float):
        if abs(v - builtins.round(v)) < 1e-9:
            return builtins.str(int(builtins.round(v)))
        return builtins.str(v)
    return builtins.str(v)


def _set_index(lst, idx, val):
    if lst is None:
        lst = []
    if idx >= len(lst):
        lst.extend([None] * (idx - len(lst) + 1))
    lst[idx] = val
    return lst

if resource:
    _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
else:
    _bench_mem_start = 0
_bench_start = _now()
try:
    @dataclass
    class Bucket:
        state: int
        key: int
        val: int
    @dataclass
    class HashMap:
        buckets: List[Bucket]
        len_: int
        cap_num: int
        cap_den: int
        initial_size: int
    def make_buckets(n):
        buckets = []
        i = 0
        while i < n:
            buckets = _append(buckets, Bucket(state=0, key=0, val=0))
            i = i + 1
        return buckets
    def hashmap_new(initial_size):
        return HashMap(buckets=make_buckets(initial_size), len_=0, cap_num=3, cap_den=4, initial_size=initial_size)
    def bucket_index(hm, key):
        ind = key % len(hm.buckets)
        if ind < 0:
            ind = ind + len(hm.buckets)
        return ind
    def next_index(hm, ind):
        return (ind + 1) % len(hm.buckets)
    def try_set(hm, ind, key, val):
        buckets = hm.buckets
        b = buckets[ind]
        if b.state == 0 or b.state == 2:
            buckets[ind] = Bucket(state=1, key=key, val=val)
            hm.buckets = buckets
            hm.len = hm.len_ + 1
            return True
        if b.key == key:
            buckets[ind] = Bucket(state=1, key=key, val=val)
            hm.buckets = buckets
            return True
        return False
    def is_full(hm):
        limit = len(hm.buckets) * hm.cap_num / hm.cap_den
        return hm.len_ >= limit
    def is_sparse(hm):
        if len(hm.buckets) <= hm.initial_size:
            return False
        limit = len(hm.buckets) * hm.cap_num / (2 * hm.cap_den)
        return hm.len_ < limit
    def resize(hm, new_size):
        old = hm.buckets
        hm.buckets = make_buckets(new_size)
        hm.len = 0
        i = 0
        while i < len(old):
            it = old[i]
            if it.state == 1:
                add_item(hm, it.key, it.val)
            i = i + 1
    def size_up(hm):
        resize(hm, len(hm.buckets) * 2)
    def size_down(hm):
        resize(hm, len(hm.buckets) // 2)
    def add_item(hm, key, val):
        ind = bucket_index(hm, key)
        i = 0
        while i < len(hm.buckets):
            if try_set(hm, ind, key, val):
                break
            ind = next_index(hm, ind)
            i = i + 1
    def hashmap_set(hm, key, val):
        if is_full(hm):
            size_up(hm)
        add_item(hm, key, val)
    def hashmap_get(hm, key):
        buckets = hm.buckets
        ind = bucket_index(hm, key)
        i = 0
        while i < len(buckets):
            it = buckets[ind]
            if it.state == 0:
                break
            if it.state == 1 and it.key == key:
                return it.val
            ind = next_index(hm, ind)
            i = i + 1
        return 0
    def hashmap_del(hm, key):
        buckets = hm.buckets
        ind = bucket_index(hm, key)
        i = 0
        while i < len(buckets):
            it = buckets[ind]
            if it.state == 0:
                print("KeyError: " + _str(key))
                return
            if it.state == 1 and it.key == key:
                buckets[ind] = Bucket(state=2, key=0, val=0)
                hm.buckets = buckets
                hm.len = hm.len_ - 1
                break
            ind = next_index(hm, ind)
            i = i + 1
        if is_sparse(hm):
            size_down(hm)
    def hashmap_len(hm):
        return hm.len_
    def hashmap_repr(hm):
        out = "HashMap("
        first = True
        i = 0
        while i < len(hm.buckets):
            b = hm.buckets[i]
            if b.state == 1:
                if not first:
                    out = out + ", "
                else:
                    first = False
                out = out + _str(b.key) + ": " + _str(b.val)
            i = i + 1
        out = out + ")"
        return out
    hm = hashmap_new(5)
    hashmap_set(hm, 1, 10)
    hashmap_set(hm, 2, 20)
    hashmap_set(hm, 3, 30)
    print(hashmap_repr(hm))
    print(_str(hashmap_get(hm, 2)))
    hashmap_del(hm, 1)
    print(hashmap_repr(hm))
    print(_str(hashmap_len(hm)))
finally:
    _bench_end = _now()
    if resource:
        _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    else:
        _bench_mem_end = 0
    print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": (_bench_mem_end - _bench_mem_start)*1024, "name": "main"}, indent=2))

