# Code generated by Mochi transpiler.
# Version 0.10.72, generated on 2025-08-24 22:16 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
import dataclasses
import json
import time

try:
    import resource
except Exception:
    resource = None
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
if hasattr(sys, "setrecursionlimit"):
    sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())


def _append(lst, v):
    if lst is None:
        lst = []
    lst.append(v)
    return lst


import builtins
def _str(v):
    if isinstance(v, float):
        if abs(v - builtins.round(v)) < 1e-9:
            return builtins.str(int(builtins.round(v)))
        return builtins.str(v)
    return builtins.str(v)

@dataclass
class SinglyLinkedList:
    data: List[int]

def empty_list():
    return SinglyLinkedList(data=[])
def length(list_):
    return len(list_.data)
def is_empty(list_):
    return len(list_.data) == 0
def to_string(list_):
    if len(list_.data) == 0:
        return ""
    s = _str(list_.data[0])
    i = 1
    while i < len(list_.data):
        s = s + " -> " + _str(list_.data[i])
        i = i + 1
    return s
def insert_nth(list_, index, value):
    if index < 0 or index > len(list_.data):
        panic("index out of range")
    res = []
    i = 0
    while i < index:
        res = _append(res, list_.data[i])
        i = i + 1
    res = _append(res, value)
    while i < len(list_.data):
        res = _append(res, list_.data[i])
        i = i + 1
    return SinglyLinkedList(data=res)
def insert_head(list_, value):
    return insert_nth(list_, 0, value)
def insert_tail(list_, value):
    return insert_nth(list_, len(list_.data), value)
@dataclass
class DeleteResult:
    list_: SinglyLinkedList
    value: int

def delete_nth(list_, index):
    if index < 0 or index >= len(list_.data):
        panic("index out of range")
    res = []
    val = 0
    i = 0
    while i < len(list_.data):
        if i == index:
            val = list_.data[i]
        else:
            res = _append(res, list_.data[i])
        i = i + 1
    return DeleteResult(list_=SinglyLinkedList(data=res), value=val)
def delete_head(list_):
    return delete_nth(list_, 0)
def delete_tail(list_):
    return delete_nth(list_, len(list_.data) - 1)
def get_item(list_, index):
    if index < 0 or index >= len(list_.data):
        panic("index out of range")
    return list_.data[index]
def set_item(list_, index, value):
    if index < 0 or index >= len(list_.data):
        panic("index out of range")
    res = []
    i = 0
    while i < len(list_.data):
        if i == index:
            res = _append(res, value)
        else:
            res = _append(res, list_.data[i])
        i = i + 1
    return SinglyLinkedList(data=res)
def reverse_list(list_):
    res = []
    i = len(list_.data) - 1
    while i >= 0:
        res = _append(res, list_.data[i])
        i = i - 1
    return SinglyLinkedList(data=res)
def main():
    if resource:
        _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    else:
        _bench_mem_start = 0
    _bench_start = _now()
    try:
        lst = empty_list()
        i = 1
        while i <= 5:
            lst = insert_tail(lst, i)
            i = i + 1
        print(to_string(lst))
        lst = insert_head(lst, 0)
        print(to_string(lst))
        del_ = delete_head(lst)
        lst = del_.list_
        print(_str(del_.value))
        del_ = delete_tail(lst)
        lst = del_.list_
        print(_str(del_.value))
        del_ = delete_nth(lst, 2)
        lst = del_.list_
        print(_str(del_.value))
        lst = set_item(lst, 1, 99)
        print(_str(get_item(lst, 1)))
        lst = reverse_list(lst)
        print(to_string(lst))
    finally:
        _bench_end = _now()
        if resource:
            _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
        else:
            _bench_mem_end = 0
        print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": (_bench_mem_end - _bench_mem_start)*1024, "name": "main"}, indent=2))
main()

