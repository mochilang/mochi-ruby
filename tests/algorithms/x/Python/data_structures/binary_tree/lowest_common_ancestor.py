# Code generated by Mochi transpiler.
# Version 0.10.72, generated on 2025-08-24 16:59 +0700
import json
import time

try:
    import resource
except Exception:
    resource = None
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
if hasattr(sys, "setrecursionlimit"):
    sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())


def _append(lst, v):
    if lst is None:
        lst = []
    return lst + [v]


import builtins
def _str(v):
    if isinstance(v, float):
        if abs(v - builtins.round(v)) < 1e-9:
            return builtins.str(int(builtins.round(v)))
        return builtins.str(v)
    return builtins.str(v)


def _set_index(lst, idx, val):
    if lst is None:
        lst = []
    if idx >= len(lst):
        lst.extend([None] * (idx - len(lst) + 1))
    lst[idx] = val
    return lst

def pow2(exp):
    res = 1
    i = 0
    while i < exp:
        res = res * 2
        i = i + 1
    return res
def create_sparse(max_node, parent):
    j = 1
    while pow2(j) < max_node:
        i = 1
        while i <= max_node:
            parent[j][i] = parent[j - 1][parent[j - 1][i]]
            i = i + 1
        j = j + 1
    return parent
def lowest_common_ancestor(u, v, level, parent):
    if level[u] < level[v]:
        temp = u
        u = v
        v = temp
    i = 18
    while i >= 0:
        if level[u] - pow2(i) >= level[v]:
            u = parent[i][u]
        i = i - 1
    if u == v:
        return u
    i = 18
    while i >= 0:
        pu = parent[i][u]
        pv = parent[i][v]
        if pu != 0 and pu != pv:
            u = pu
            v = pv
        i = i - 1
    return parent[0][u]
def breadth_first_search(level, parent, max_node, graph, root):
    level[root] = 0
    q = []
    q = _append(q, root)
    head = 0
    while head < len(q):
        u = q[head]
        head = head + 1
        adj = graph.get(u, [])
        j = 0
        while j < len(adj):
            v = adj[j]
            if level[v] == 0 - 1:
                level[v] = level[u] + 1
                parent[0][v] = u
                q = _append(q, v)
            j = j + 1
def main():
    if resource:
        _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    else:
        _bench_mem_start = 0
    _bench_start = _now()
    try:
        max_node = 13
        parent = []
        i = 0
        while i < 20:
            row = []
            j = 0
            while j < max_node + 10:
                row = _append(row, 0)
                j = j + 1
            parent = _append(parent, row)
            i = i + 1
        level = []
        i = 0
        while i < max_node + 10:
            level = _append(level, 0 - 1)
            i = i + 1
        graph = {}
        graph[1] = [2, 3, 4]
        graph[2] = [5]
        graph[3] = [6, 7]
        graph[4] = [8]
        graph[5] = [9, 10]
        graph[6] = [11]
        graph[7] = []
        graph[8] = [12, 13]
        graph[9] = []
        graph[10] = []
        graph[11] = []
        graph[12] = []
        graph[13] = []
        breadth_first_search(level, parent, max_node, graph, 1)
        parent = create_sparse(max_node, parent)
        print("LCA of node 1 and 3 is: " + _str(lowest_common_ancestor(1, 3, level, parent)))
        print("LCA of node 5 and 6 is: " + _str(lowest_common_ancestor(5, 6, level, parent)))
        print("LCA of node 7 and 11 is: " + _str(lowest_common_ancestor(7, 11, level, parent)))
        print("LCA of node 6 and 7 is: " + _str(lowest_common_ancestor(6, 7, level, parent)))
        print("LCA of node 4 and 12 is: " + _str(lowest_common_ancestor(4, 12, level, parent)))
        print("LCA of node 8 and 8 is: " + _str(lowest_common_ancestor(8, 8, level, parent)))
    finally:
        _bench_end = _now()
        if resource:
            _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
        else:
            _bench_mem_end = 0
        print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": (_bench_mem_end - _bench_mem_start)*1024, "name": "main"}, indent=2))
main()

