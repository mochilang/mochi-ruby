# Code generated by Mochi transpiler.
# Version 0.10.65, generated on 2025-08-13 16:39 +0700
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def _str(v):
    if isinstance(v, float):
        if v.is_integer():
            return str(int(v))
        return format(v, ".17g")
    return str(v)

def starts_with(s, prefix):
    if len(s) < len(prefix):
        return False
    return s[0:len(prefix)] == prefix
def all_digits(s):
    i = 0
    while i < len(s):
        c = s[i]
        if c < "0" or c > "9":
            return False
        i = i + 1
    return True
def is_sri_lankan_phone_number(phone):
    p = phone
    if starts_with(p, "+94"):
        p = p[3:len(p)]
    else:
        if starts_with(p, "0094"):
            p = p[4:len(p)]
        else:
            if starts_with(p, "94"):
                p = p[2:len(p)]
            else:
                if starts_with(p, "0"):
                    p = p[1:len(p)]
                else:
                    return False
    if len(p) != 9 and len(p) != 10:
        return False
    if p[0] != "7":
        return False
    second = p[1]
    allowed = ["0", "1", "2", "4", "5", "6", "7", "8"]
    if not (second in allowed):
        return False
    idx = 2
    if len(p) == 10:
        sep = p[2]
        if sep != "-" and sep != " ":
            return False
        idx = 3
    if len(p) - idx != 7:
        return False
    rest = p[idx:len(p)]
    return all_digits(rest)
phone = "0094702343221"
print(_str(is_sri_lankan_phone_number(phone)))

