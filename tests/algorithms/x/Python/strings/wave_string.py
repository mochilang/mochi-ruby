# Code generated by Mochi transpiler.
# Version 0.10.65, generated on 2025-08-13 16:39 +0700
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def _append(lst, v):
    if lst is None:
        lst = []
    lst.append(v)
    return lst


def _str(v):
    if isinstance(v, float):
        if v.is_integer():
            return str(int(v))
        return format(v, ".17g")
    return str(v)

lowercase = "abcdefghijklmnopqrstuvwxyz"
uppercase = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
def index_of(s, c):
    i = 0
    while i < len(s):
        if s[i:i + 1] == c:
            return i
        i = i + 1
    return -1
def is_alpha(c):
    return index_of(lowercase, c) >= 0 or index_of(uppercase, c) >= 0
def to_upper(c):
    idx = index_of(lowercase, c)
    if idx >= 0:
        return uppercase[idx:idx + 1]
    return c
def wave(txt):
    result = []
    i = 0
    while i < len(txt):
        ch = txt[i:i + 1]
        if is_alpha(ch):
            prefix = txt[0:i]
            suffix = txt[i + 1:len(txt)]
            result = _append(result, prefix + to_upper(ch) + suffix)
        i = i + 1
    return result
print(_str(wave("cat")))
print(_str(wave("one")))
print(_str(wave("book")))

