# Code generated by Mochi transpiler.
# Version 0.10.65, generated on 2025-08-13 16:39 +0700
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)

def contains(chars, ch):
    i = 0
    while i < len(chars):
        if chars[i] == ch:
            return True
        i = i + 1
    return False
def substring(s, start, end):
    res = ""
    i = start
    while i < end:
        res = res + s[i]
        i = i + 1
    return res
def strip_chars(user_string, characters):
    start = 0
    end = len(user_string)
    while start < end and contains(characters, user_string[start]):
        start = start + 1
    while end > start and contains(characters, user_string[end - 1]):
        end = end - 1
    return user_string[start:end]
def strip(user_string):
    return strip_chars(user_string, " \t\n\r")
def test_strip():
    if strip("   hello   ") != "hello":
        panic("test1 failed")
    if strip_chars("...world...", ".") != "world":
        panic("test2 failed")
    if strip_chars("123hello123", "123") != "hello":
        panic("test3 failed")
    if strip("") != "":
        panic("test4 failed")
def main():
    test_strip()
    print(strip("   hello   "))
    print(strip_chars("...world...", "."))
    print(strip_chars("123hello123", "123"))
    print(strip(""))
main()

