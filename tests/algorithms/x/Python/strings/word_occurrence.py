# Code generated by Mochi transpiler.
# Version 0.10.65, generated on 2025-08-13 16:39 +0700
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def _str(v):
    if isinstance(v, float):
        if v.is_integer():
            return str(int(v))
        return format(v, ".17g")
    return str(v)


def _set_index(lst, idx, val):
    if lst is None:
        lst = []
    if idx >= len(lst):
        lst.extend([None] * (idx - len(lst) + 1))
    lst[idx] = val
    return lst

def word_occurrence(sentence):
    occurrence = {}
    word = ""
    i = 0
    while i < len(sentence):
        ch = sentence[i:i + 1]
        if ch == " ":
            if word != "":
                if word in occurrence:
                    occurrence[word] = occurrence.get(word, None) + 1
                else:
                    occurrence[word] = 1
                word = ""
        else:
            word = word + ch
        i = i + 1
    if word != "":
        if word in occurrence:
            occurrence[word] = occurrence.get(word, None) + 1
        else:
            occurrence[word] = 1
    return occurrence
def main():
    result = word_occurrence("INPUT STRING")
    for w in result:
        print(w + ": " + _str(result.get(w, 0)))
main()

