# Code generated by Mochi transpiler.
# Version 0.10.59, generated on 2025-08-06 22:12 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))

def bin_to_octal(bin_string):
    i = 0
    while i < len(bin_string):
        c = bin_string[i]
        if not (c == "0" or c == "1"):
            panic("Non-binary value was passed to the function")
        i = i + 1
    if len(bin_string) == 0:
        panic("Empty string was passed to the function")
    padded = bin_string
    while len(padded) % 3 != 0:
        padded = "0" + padded
    oct_string = ""
    index = 0
    while index < len(padded):
        group = padded[index:index + 3]
        b0 = (1 if group[0] == "1" else 0)
        b1 = (1 if group[1] == "1" else 0)
        b2 = (1 if group[2] == "1" else 0)
        oct_val = b0 * 4 + b1 * 2 + b2
        oct_string = oct_string + str(oct_val)
        index = index + 3
    return oct_string
print(bin_to_octal("1111"))
print(bin_to_octal("101010101010011"))
