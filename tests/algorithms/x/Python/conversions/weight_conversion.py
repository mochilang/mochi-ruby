# Code generated by Mochi transpiler.
# Version 0.10.71, generated on 2025-08-24 15:35 +0700
import json
import time

try:
    import resource
except Exception:
    resource = None
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
if hasattr(sys, "setrecursionlimit"):
    sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

if resource:
    _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
else:
    _bench_mem_start = 0
_bench_start = _now()
try:
    def pow10(exp):
        result = 1.0
        if exp >= 0:
            i = 0
            while i < exp:
                result = result * 10.0
                i = i + 1
        else:
            i = 0
            while i < (0 - exp):
                result = result / 10.0
                i = i + 1
        return result
    KILOGRAM_CHART = {"kilogram": 1.0, "gram": 1000.0, "milligram": 1e+06, "metric-ton": 0.001, "long-ton": 0.0009842073, "short-ton": 0.0011023122, "pound": 2.2046244202, "stone": 0.1574731728, "ounce": 35.273990723, "carrat": 5000.0, "atomic-mass-unit": 6.022136652 * pow10(26)}
    WEIGHT_TYPE_CHART = {"kilogram": 1.0, "gram": 0.001, "milligram": 1e-06, "metric-ton": 1000.0, "long-ton": 1016.04608, "short-ton": 907.184, "pound": 0.453592, "stone": 6.35029, "ounce": 0.0283495, "carrat": 0.0002, "atomic-mass-unit": 1.660540199 * pow10(-27)}
    def weight_conversion(from_type, to_type, value):
        has_to = to_type in KILOGRAM_CHART
        has_from = from_type in WEIGHT_TYPE_CHART
        if has_to and has_from:
            return value * KILOGRAM_CHART.get(to_type, 0.0) * WEIGHT_TYPE_CHART.get(from_type, 0.0)
        print("Invalid 'from_type' or 'to_type'")
        return 0.0
    print(weight_conversion("kilogram", "gram", 1.0))
    print(weight_conversion("gram", "pound", 3.0))
    print(weight_conversion("ounce", "kilogram", 3.0))
finally:
    _bench_end = _now()
    if resource:
        _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    else:
        _bench_mem_end = 0
    print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": (_bench_mem_end - _bench_mem_start)*1024, "name": "main"}, indent=2))

