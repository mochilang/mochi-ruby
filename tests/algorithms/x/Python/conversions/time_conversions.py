# Code generated by Mochi transpiler.
# Version 0.10.59, generated on 2025-08-06 22:12 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))

time_chart = {"seconds": 1.0, "minutes": 60.0, "hours": 3600.0, "days": 86400.0, "weeks": 604800.0, "months": 2.6298e+06, "years": 3.15576e+07}
time_chart_inverse = {"seconds": 1.0, "minutes": 1.0 / 60.0, "hours": 1.0 / 3600.0, "days": 1.0 / 86400.0, "weeks": 1.0 / 604800.0, "months": 1.0 / 2.6298e+06, "years": 1.0 / 3.15576e+07}
units = ["seconds", "minutes", "hours", "days", "weeks", "months", "years"]
units_str = "seconds, minutes, hours, days, weeks, months, years"
def contains(arr, t):
    i = 0
    while i < len(arr):
        if arr[i] == t:
            return True
        i = i + 1
    return False
def convert_time(time_value, unit_from, unit_to):
    if time_value < 0.0:
        panic("'time_value' must be a non-negative number.")
    from_ = unit_from.lower()
    to = unit_to.lower()
    if (not from_ in units) or (not to in units):
        invalid_unit = from_
        if from_ in units:
            invalid_unit = to
        panic("Invalid unit " + invalid_unit + " is not in " + units_str + ".")
    seconds = time_value * time_chart.get(from_)
    converted = seconds * time_chart_inverse.get(to)
    scaled = converted * 1000.0
    int_part = int(scaled + 0.5)
    return (int_part + 0.0) / 1000.0
print(convert_time(3600.0, "seconds", "hours"))
print(convert_time(360.0, "days", "months"))
print(convert_time(360.0, "months", "years"))
print(convert_time(1.0, "years", "seconds"))
