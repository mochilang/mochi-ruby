# Code generated by Mochi transpiler.
# Version 0.10.59, generated on 2025-08-06 22:12 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))

def round_int(x):
    return int((x + 0.5))
def rgb_to_cmyk(r_input, g_input, b_input):
    if r_input < 0 or r_input >= 256 or g_input < 0 or g_input >= 256 or b_input < 0 or b_input >= 256:
        panic("Expected int of the range 0..255")
    r = (float(r_input)) / 255.0
    g = (float(g_input)) / 255.0
    b = (float(b_input)) / 255.0
    max_val = r
    if g > max_val:
        max_val = g
    if b > max_val:
        max_val = b
    k_float = 1.0 - max_val
    if k_float == 1.0:
        return [0, 0, 0, 100]
    c_float = 100.0 * (1.0 - r - k_float) // (1.0 - k_float)
    m_float = 100.0 * (1.0 - g - k_float) // (1.0 - k_float)
    y_float = 100.0 * (1.0 - b - k_float) // (1.0 - k_float)
    k_percent = 100.0 * k_float
    c = round_int(c_float)
    m = round_int(m_float)
    y = round_int(y_float)
    k = round_int(k_percent)
    return [c, m, y, k]
print(rgb_to_cmyk(255, 255, 255))
print(rgb_to_cmyk(128, 128, 128))
print(rgb_to_cmyk(0, 0, 0))
print(rgb_to_cmyk(255, 0, 0))
print(rgb_to_cmyk(0, 255, 0))
print(rgb_to_cmyk(0, 0, 255))
