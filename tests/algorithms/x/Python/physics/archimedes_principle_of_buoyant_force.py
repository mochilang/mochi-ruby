# Code generated by Mochi transpiler.
# Version 0.10.73, generated on 2025-08-25 23:46 +0700
import json
import time

try:
    import resource
except Exception:
    resource = None
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
if hasattr(sys, "setrecursionlimit"):
    sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


def panic(msg):
    raise RuntimeError(msg)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

if resource:
    _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
else:
    _bench_mem_start = 0
_bench_start = _now()
try:
    G = 9.80665
    def archimedes_principle(fluid_density, volume, gravity):
        if fluid_density <= 0.0:
            panic("Impossible fluid density")
        if volume <= 0.0:
            panic("Impossible object volume")
        if gravity < 0.0:
            panic("Impossible gravity")
        return fluid_density * volume * gravity
    def archimedes_principle_default(fluid_density, volume):
        res = archimedes_principle(fluid_density, volume, G)
        return res
    # test archimedes principle
    assert archimedes_principle(500.0, 4.0, 9.8) == 19600.0
    assert archimedes_principle(997.0, 0.5, 9.8) == 4885.3
    r = archimedes_principle_default(997.0, 0.7)
    expected = archimedes_principle(997.0, 0.7, G)
    assert r == expected
    assert archimedes_principle(997.0, 0.7, 0.0) == 0.0
finally:
    _bench_end = _now()
    if resource:
        _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    else:
        _bench_mem_end = 0
    print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": (_bench_mem_end - _bench_mem_start)*1024, "name": "main"}, indent=2))

