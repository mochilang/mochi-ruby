# Code generated by Mochi transpiler.
# Version 0.10.73, generated on 2025-08-25 23:46 +0700
from __future__ import annotations
from dataclasses import dataclass
from typing import List, Dict
import dataclasses
import json
import time

try:
    import resource
except Exception:
    resource = None
import sys
if hasattr(sys, "set_int_max_str_digits"):
    sys.set_int_max_str_digits(0)
if hasattr(sys, "setrecursionlimit"):
    sys.setrecursionlimit(1000000)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())


def _append(lst, v):
    if lst is None:
        lst = []
    lst.append(v)
    return lst


import builtins
def _str(v):
    if isinstance(v, float):
        if abs(v - builtins.round(v)) < 1e-9:
            return builtins.str(int(builtins.round(v)))
        return builtins.str(v)
    return builtins.str(v)


def _set_index(lst, idx, val):
    if lst is None:
        lst = []
    if idx >= len(lst):
        lst.extend([None] * (idx - len(lst) + 1))
    lst[idx] = val
    return lst

@dataclass
class Node:
    key: int
    value: int
    prev: int
    next_: int

@dataclass
class DoubleLinkedList:
    nodes: List[Node]
    head: int
    tail: int

def new_list():
    nodes = []
    head = Node(key=0, value=0, prev=0 - 1, next_=1)
    tail = Node(key=0, value=0, prev=0, next_=0 - 1)
    nodes = _append(nodes, head)
    nodes = _append(nodes, tail)
    return DoubleLinkedList(nodes=nodes, head=0, tail=1)
def dll_add(lst, idx):
    nodes = lst.nodes
    tail_idx = lst.tail
    tail_node = nodes[tail_idx]
    prev_idx = tail_node.prev
    node = nodes[idx]
    node.prev = prev_idx
    node.next = tail_idx
    nodes[idx] = node
    prev_node = nodes[prev_idx]
    prev_node.next = idx
    nodes[prev_idx] = prev_node
    tail_node.prev = idx
    nodes[tail_idx] = tail_node
    lst.nodes = nodes
    return lst
def dll_remove(lst, idx):
    nodes = lst.nodes
    node = nodes[idx]
    prev_idx = node.prev
    next_idx = node.next_
    if prev_idx == 0 - 1 or next_idx == 0 - 1:
        return lst
    prev_node = nodes[prev_idx]
    prev_node.next = next_idx
    nodes[prev_idx] = prev_node
    next_node = nodes[next_idx]
    next_node.prev = prev_idx
    nodes[next_idx] = next_node
    node.prev = 0 - 1
    node.next = 0 - 1
    nodes[idx] = node
    lst.nodes = nodes
    return lst
@dataclass
class LRUCache:
    list_: DoubleLinkedList
    capacity: int
    num_keys: int
    hits: int
    misses: int
    cache: Dict[str, int]

@dataclass
class GetResult:
    cache: LRUCache
    value: int
    ok: bool

def new_cache(cap):
    empty_map = {}
    return LRUCache(list_=new_list(), capacity=cap, num_keys=0, hits=0, misses=0, cache=empty_map)
def lru_get(c, key):
    cache = c
    key_str = _str(key)
    if key_str in cache.cache:
        idx = cache.cache[key_str]
        if idx != 0 - 1:
            cache.hits = cache.hits + 1
            node = cache.list_.nodes[int(idx)]
            value = node.value
            cache.list = dll_remove(cache.list_, idx)
            cache.list = dll_add(cache.list_, idx)
            return GetResult(cache=cache, value=value, ok=True)
    cache.misses = cache.misses + 1
    return GetResult(cache=cache, value=0, ok=False)
def lru_put(c, key, value):
    cache = c
    key_str = _str(key)
    if not (key_str in cache.cache):
        if cache.num_keys >= cache.capacity:
            head_node = cache.list_.nodes[cache.list_.head]
            first_idx = head_node.next_
            first_node = cache.list_.nodes[first_idx]
            old_key = first_node.key
            cache.list = dll_remove(cache.list_, first_idx)
            mdel = cache.cache
            mdel[_str(old_key)] = 0 - 1
            cache.cache = mdel
            cache.num_keys = cache.num_keys - 1
        nodes = cache.list_.nodes
        new_node = Node(key=key, value=value, prev=0 - 1, next_=0 - 1)
        nodes = _append(nodes, new_node)
        idx = len(nodes) - 1
        cache.list_.nodes = nodes
        cache.list = dll_add(cache.list_, idx)
        m = cache.cache
        m[key_str] = idx
        cache.cache = m
        cache.num_keys = cache.num_keys + 1
    else:
        m = cache.cache
        idx = m[key_str]
        nodes = cache.list_.nodes
        node = nodes[int(idx)]
        node.value = value
        nodes[idx] = node
        cache.list_.nodes = nodes
        cache.list = dll_remove(cache.list_, idx)
        cache.list = dll_add(cache.list_, idx)
        cache.cache = m
    return cache
def cache_info(cache):
    return "CacheInfo(hits=" + _str(cache.hits) + ", misses=" + _str(cache.misses) + ", capacity=" + _str(cache.capacity) + ", current size=" + _str(cache.num_keys) + ")"
def print_result(res):
    if res.ok:
        print(_str(res.value))
    else:
        print("None")
def main():
    if resource:
        _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    else:
        _bench_mem_start = 0
    _bench_start = _now()
    try:
        cache = new_cache(2)
        cache = lru_put(cache, 1, 1)
        cache = lru_put(cache, 2, 2)
        r1 = lru_get(cache, 1)
        cache = r1.cache
        print_result(r1)
        cache = lru_put(cache, 3, 3)
        r2 = lru_get(cache, 2)
        cache = r2.cache
        print_result(r2)
        cache = lru_put(cache, 4, 4)
        r3 = lru_get(cache, 1)
        cache = r3.cache
        print_result(r3)
        r4 = lru_get(cache, 3)
        cache = r4.cache
        print_result(r4)
        r5 = lru_get(cache, 4)
        cache = r5.cache
        print_result(r5)
        print(cache_info(cache))
    finally:
        _bench_end = _now()
        if resource:
            _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
        else:
            _bench_mem_end = 0
        print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": (_bench_mem_end - _bench_mem_start)*1024, "name": "main"}, indent=2))
main()

