#!/usr/bin/env escript
-module(main).
-compile([export_vars, nowarn_export_vars, nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, is_palindrome/1, main/0]).

% Generated by Mochi transpiler v0.10.72 (b9a682dda0)


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> case V of
                true -> 1;
                false -> 0;
                _ -> list_to_integer(V)
            end
        end
    end.


toi(V) ->
    mochi_to_int(V).


mochi_to_float(V) ->
    case erlang:is_float(V) of
        true -> V;
        _ -> float(V)
    end.


-compile({nowarn_unused_function, [mochi_nth/2]}).
-spec mochi_nth(integer(), list()) -> any().
mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


mochi_format(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false ->
            S = lists:flatten(io_lib:format("~.15f", [V])),
            re:replace(S, "\\.?0+$", "", [global, {return, list}])
    end;
mochi_format(V) when is_integer(V) -> integer_to_list(V);
mochi_format(V) when is_binary(V) -> V;
mochi_format(V) when is_list(V) -> mochi_str(V);
mochi_format(V) -> lists:flatten(io_lib:format("~p", [V])).


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_list(V) ->
    case io_lib:printable_list(V) of
        true -> V;
        false -> "[" ++ string:join([mochi_str(E) || E <- V], " ") ++ "]"
    end;
mochi_str(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false -> erlang:float_to_list(V, [short])
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

is_palindrome(Values) ->
    try
        Stack = [],
        Fast = 0,
        Slow = 0,
        N = erlang:length(Values),
        Fun = fun Fun_loop(Fast, N, Slow, Stack, Values) ->
    case ((Fast < N) andalso ((Fast + 1) < N)) of
        true ->
            Stack_2 = lists:append((case Stack of nil -> []; _ -> Stack end), [(case erlang:is_map(Values) of true -> maps:get(Slow, Values, nil); _ -> case Slow < 0 of true -> mochi_nth(erlang:length(Values) + Slow + 1, Values); _ -> mochi_nth(Slow + 1, Values) end end)]),
            Slow_2 = (Slow + 1),
            Fast_2 = (Fast + 2),
            Fun_loop(Fast_2, N, Slow_2, Stack_2, Values);
        _ -> {Fast, N, Slow, Stack, Values}
    end
end,
{Fast_2, N, Slow_2, Stack_2, Values} = Fun(Fast, N, Slow, Stack, Values),
        Slow_4 = (case (Fast_2 == (N - 1)) of
        true -> Slow_3 = (Slow_2 + 1),
            Slow_3;
        _ -> Slow_2
    end),
        I = (erlang:length(Stack_2) - 1),
        Fun_2 = fun Fun_2_loop(Fast_2, I, N, Slow_4, Stack_2, Values) ->
    case (Slow_4 < N) of
        true ->
            case ((case erlang:is_map(Stack_2) of true -> maps:get(I, Stack_2, nil); _ -> case I < 0 of true -> mochi_nth(erlang:length(Stack_2) + I + 1, Stack_2); _ -> mochi_nth(I + 1, Stack_2) end end) /= (case erlang:is_map(Values) of true -> maps:get(Slow_4, Values, nil); _ -> case Slow_4 < 0 of true -> mochi_nth(erlang:length(Values) + Slow_4 + 1, Values); _ -> mochi_nth(Slow_4 + 1, Values) end end)) of
        true -> throw({return, false});
        _ -> ok
    end,
            I_2 = (I - 1),
            Slow_5 = (Slow_4 + 1),
            Fun_2_loop(Fast_2, I_2, N, Slow_5, Stack_2, Values);
        _ -> {Fast_2, I, N, Slow_4, Stack_2, Values}
    end
end,
{Fast_2, I_2, N, Slow_5, Stack_2, Values} = Fun_2(Fast_2, I, N, Slow_4, Stack_2, Values),
        true
    catch {return, RetCatch} -> RetCatch end.

main() ->
    try
        io:format("~s~n", [mochi_format(is_palindrome([]))]),
        io:format("~s~n", [mochi_format(is_palindrome([1]))]),
        io:format("~s~n", [mochi_format(is_palindrome([1, 2]))]),
        io:format("~s~n", [mochi_format(is_palindrome([1, 2, 1]))]),
        io:format("~s~n", [mochi_format(is_palindrome([1, 2, 2, 1]))]),
        nil
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~p~n", [Err])
    end.
