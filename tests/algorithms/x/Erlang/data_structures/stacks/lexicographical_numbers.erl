#!/usr/bin/env escript
-module(main).
-compile([export_vars, nowarn_export_vars, nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, lexical_order/1, join_ints/1]).

% Generated by Mochi transpiler v0.10.72 (b9a682dda0)


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> case V of
                true -> 1;
                false -> 0;
                _ -> list_to_integer(V)
            end
        end
    end.


toi(V) ->
    mochi_to_int(V).


mochi_to_float(V) ->
    case erlang:is_float(V) of
        true -> V;
        _ -> float(V)
    end.


-compile({nowarn_unused_function, [mochi_nth/2]}).
-spec mochi_nth(integer(), list()) -> any().
mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


mochi_format(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false ->
            S = lists:flatten(io_lib:format("~.15f", [V])),
            re:replace(S, "\\.?0+$", "", [global, {return, list}])
    end;
mochi_format(V) when is_integer(V) -> integer_to_list(V);
mochi_format(V) when is_binary(V) -> V;
mochi_format(V) when is_list(V) -> mochi_str(V);
mochi_format(V) -> lists:flatten(io_lib:format("~p", [V])).


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_list(V) ->
    case io_lib:printable_list(V) of
        true -> V;
        false -> "[" ++ string:join([mochi_str(E) || E <- V], " ") ++ "]"
    end;
mochi_str(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false -> erlang:float_to_list(V, [short])
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_idiv(A, B) ->
    Q = A div B,
    R = A rem B,
    case ((R /= 0) andalso ((B < 0 andalso R > 0) orelse (B > 0 andalso R < 0))) of
        true -> Q - 1;
        false -> Q
    end.


mochi_mod(A, B) ->
    A1 = case erlang:is_float(A) of
        true -> trunc(A);
        _ -> A
    end,
    B1 = case erlang:is_float(B) of
        true -> trunc(B);
        _ -> B
    end,
    case B1 of
        0 -> 0;
        _ -> A1 - B1 * mochi_idiv(A1, B1)
    end.

lexical_order(Max_number) ->
    try
        Result = [],
        Stack = [1],
        Fun = fun Fun_loop(Max_number, Result, Stack) ->
    case (erlang:length(Stack) > 0) of
        true ->
            try
                Idx = (erlang:length(Stack) - 1),
                Num = (case erlang:is_map(Stack) of true -> maps:get(Idx, Stack, nil); _ -> case Idx < 0 of true -> mochi_nth(erlang:length(Stack) + Idx + 1, Stack); _ -> mochi_nth(Idx + 1, Stack) end end),
                Stack_2 = lists:sublist(Stack, 0 + 1, (Idx - 0)),
                case (Num > Max_number) of
        true -> throw({continue, Max_number, Result, Stack_2});
        _ -> ok
    end,
                Result_2 = lists:append((case Result of nil -> []; _ -> Result end), [Num]),
                Stack_4 = (case (mochi_mod(Num, 10) /= 9) of
        true -> Stack_3 = lists:append((case Stack_2 of nil -> []; _ -> Stack_2 end), [(Num + 1)]),
            Stack_3;
        _ -> Stack_2
    end),
                Stack_5 = lists:append((case Stack_4 of nil -> []; _ -> Stack_4 end), [(Num * 10)]),
                Fun_loop(Max_number, Result_2, Stack_5)
            catch
                {continue, C0, C1, C2} -> Fun_loop(C0, C1, C2);
                {break, Bs} -> Bs
            end;
        _ -> {Max_number, Result, Stack}
    end
end,
{Max_number, Result_2, Stack_5} = Fun(Max_number, Result, Stack),
        Result_2
    catch {return, RetCatch} -> RetCatch end.

join_ints(Xs) ->
    try
        Res = "",
        I = 0,
        Fun_2 = fun Fun_2_loop(I, Res, Xs) ->
    case (I < erlang:length(Xs)) of
        true ->
            Res_3 = (case (I > 0) of
        true -> Res_2 = (Res ++ " "),
            Res_2;
        _ -> Res
    end),
            Res_4 = (Res_3 ++ mochi_str((case erlang:is_map(Xs) of true -> maps:get(I, Xs, nil); _ -> case I < 0 of true -> mochi_nth(erlang:length(Xs) + I + 1, Xs); _ -> mochi_nth(I + 1, Xs) end end))),
            I_2 = (I + 1),
            Fun_2_loop(I_2, Res_4, Xs);
        _ -> {I, Res, Xs}
    end
end,
{I_2, Res_4, Xs} = Fun_2(I, Res, Xs),
        Res_4
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~s~n", [mochi_format(join_ints(lexical_order(13)))]),
    io:format("~s~n", [mochi_str(lexical_order(1))]),
    io:format("~s~n", [mochi_format(join_ints(lexical_order(20)))]),
    io:format("~s~n", [mochi_format(join_ints(lexical_order(25)))]),
    io:format("~s~n", [mochi_str(lexical_order(12))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~p~n", [Err])
    end.
