#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, runge_kutta/5, test_runge_kutta/0, main/0]).

% Generated by Mochi transpiler v0.10.60 (c3816b866e) on 2025-08-07 15:40 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> list_to_integer(V)
        end
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try A / B catch _:_ -> 0.0 end.

runge_kutta(F, Y0, X0, H, X_end) ->
    try
        Span = mochi_safe_div((X_end - X0), H),
        N = mochi_to_int(Span),
        case (float(N) < Span) of
        true -> N_2 = (N + 1),
            N_3 = N_2;
        _ -> N_3 = N
    end,
        Y = [],
        I = 0,
        Fun = fun Fun_loop(F, H, I, N_3, Span, X0, X_end, Y, Y0) ->
    case (I < (N_3 + 1)) of
        true ->
            Y_2 = lists:append((case Y of nil -> []; _ -> Y end), [0.0]),
            I_2 = (I + 1),
            Fun_loop(F, H, I_2, N_3, Span, X0, X_end, Y_2, Y0);
        _ -> {F, H, I, N_3, Span, X0, X_end, Y, Y0}
    end
end,
{F, H, I_2, N_3, Span, X0, X_end, Y_2, Y0} = Fun(F, H, I, N_3, Span, X0, X_end, Y, Y0),
        Y_3 = lists:sublist(Y_2, 0) ++ [Y0] ++ lists:nthtail(0 + 1, Y_2),
        X = X0,
        K = 0,
        Fun_2 = fun Fun_2_loop(F, H, I_2, K, N_3, Span, X, X0, X_end, Y_3, Y0) ->
    case (K < N_3) of
        true ->
            K1 = F(X, (case erlang:is_map(Y_3) of true -> maps:get(K, Y_3, nil); _ -> case K < 0 of true -> mochi_nth(erlang:length(Y_3) + K + 1, Y_3); _ -> mochi_nth(K + 1, Y_3) end end)),
            K2 = F((X + mochi_safe_mul(0.5, H)), ((case erlang:is_map(Y_3) of true -> maps:get(K, Y_3, nil); _ -> case K < 0 of true -> mochi_nth(erlang:length(Y_3) + K + 1, Y_3); _ -> mochi_nth(K + 1, Y_3) end end) + mochi_safe_mul(mochi_safe_mul(0.5, H), K1))),
            K3 = F((X + mochi_safe_mul(0.5, H)), ((case erlang:is_map(Y_3) of true -> maps:get(K, Y_3, nil); _ -> case K < 0 of true -> mochi_nth(erlang:length(Y_3) + K + 1, Y_3); _ -> mochi_nth(K + 1, Y_3) end end) + mochi_safe_mul(mochi_safe_mul(0.5, H), K2))),
            K4 = F((X + H), ((case erlang:is_map(Y_3) of true -> maps:get(K, Y_3, nil); _ -> case K < 0 of true -> mochi_nth(erlang:length(Y_3) + K + 1, Y_3); _ -> mochi_nth(K + 1, Y_3) end end) + mochi_safe_mul(H, K3))),
            Y_4 = lists:sublist(Y_3, (K + 1)) ++ [((case erlang:is_map(Y_3) of true -> maps:get(K, Y_3, nil); _ -> case K < 0 of true -> mochi_nth(erlang:length(Y_3) + K + 1, Y_3); _ -> mochi_nth(K + 1, Y_3) end end) + mochi_safe_mul(mochi_safe_mul(mochi_safe_div(1.0, 6.0), H), (((K1 + mochi_safe_mul(2.0, K2)) + mochi_safe_mul(2.0, K3)) + K4)))] ++ lists:nthtail((K + 1) + 1, Y_3),
            X_2 = (X + H),
            K_2 = (K + 1),
            Fun_2_loop(F, H, I_2, K_2, N_3, Span, X_2, X0, X_end, Y_4, Y0);
        _ -> {F, H, I_2, K, N_3, Span, X, X0, X_end, Y_3, Y0}
    end
end,
{F, H, I_2, K_2, N_3, Span, X_2, X0, X_end, Y_4, Y0} = Fun_2(F, H, I_2, K, N_3, Span, X, X0, X_end, Y_3, Y0),
        Y_4
    catch {return, Ret} -> Ret end.

test_runge_kutta() ->
    try
        F_2 = fun F_2(X_3, Y_5) ->
    try
        Y_5
    catch {return, Ret} -> Ret end
end,
        Result = runge_kutta(F_2, 1.0, 0.0, 0.01, 5.0),
        Last = (case erlang:is_map(Result) of true -> maps:get((erlang:length(Result) - 1), Result, nil); _ -> case (erlang:length(Result) - 1) < 0 of true -> mochi_nth(erlang:length(Result) + (erlang:length(Result) - 1) + 1, Result); _ -> mochi_nth((erlang:length(Result) - 1) + 1, Result) end end),
        Expected = 148.41315904125113,
        Diff = (Last - 148.41315904125113),
        case (Diff < 0.0) of
        true -> Diff_2 = -Diff,
            Diff_3 = Diff_2;
        _ -> Diff_3 = Diff
    end,
        case (Diff_3 > 0.000001) of
        true -> erlang:error("runge_kutta failed");
        _ -> ok
    end,
        nil
    catch {return, Ret} -> Ret end.

main() ->
    try
        test_runge_kutta(),
        F_3 = fun F_3(X_4, Y_6) ->
    try
        Y_6
    catch {return, Ret} -> Ret end
end,
        R = runge_kutta(F_3, 1.0, 0.0, 0.1, 1.0),
        io:format("~ts~n", [mochi_str((case erlang:is_map(R) of true -> maps:get((erlang:length(R) - 1), R, nil); _ -> case (erlang:length(R) - 1) < 0 of true -> mochi_nth(erlang:length(R) + (erlang:length(R) - 1) + 1, R); _ -> mochi_nth((erlang:length(R) - 1) + 1, R) end end))]),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
