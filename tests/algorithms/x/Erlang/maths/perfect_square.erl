#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, perfect_square/1, perfect_square_binary_search/1]).

% Generated by Mochi transpiler v0.10.60 (c3816b866e) on 2025-08-07 15:40 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

perfect_square(Num) ->
    try
        case (Num < 0) of
        true -> throw({return, false});
        _ -> ok
    end,
        I = 0,
        Fun = fun Fun_loop(I, Num) ->
    case ((I * I) =< Num) of
        true ->
            case ((I * I) == Num) of
        true -> throw({return, true});
        _ -> ok
    end,
            I_2 = (I + 1),
            Fun_loop(I_2, Num);
        _ -> {I, Num}
    end
end,
{I_2, Num} = Fun(I, Num),
        false
    catch {return, Ret} -> Ret end.

perfect_square_binary_search(N) ->
    try
        case (N < 0) of
        true -> throw({return, false});
        _ -> ok
    end,
        Left = 0,
        Right = N,
        Fun_2 = fun Fun_2_loop(Left, N, Right) ->
    case (Left =< Right) of
        true ->
            Mid = ((Left + Right) div 2),
            Sq = (Mid * Mid),
            case (Sq == N) of
        true -> throw({return, true});
        _ -> ok
    end,
            case (Sq > N) of
        true -> Right_2 = (Mid - 1),
            Left_3 = Left,
            Right_3 = Right_2;
        _ -> Left_2 = (Mid + 1),
            Left_3 = Left_2,
            Right_3 = Right
    end,
            Fun_2_loop(Left_3, N, Right_3);
        _ -> {Left, N, Right}
    end
end,
{Left_3, N, Right_3} = Fun_2(Left, N, Right),
        false
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~ts~n", [mochi_str(perfect_square(9))]),
    io:format("~ts~n", [mochi_str(perfect_square(10))]),
    io:format("~ts~n", [mochi_str(perfect_square_binary_search(16))]),
    io:format("~ts~n", [mochi_str(perfect_square_binary_search(10))]),
    io:format("~ts~n", [mochi_str(perfect_square_binary_search(-1))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
