#!/usr/bin/env escript
-module(main).
-compile([export_vars, nowarn_export_vars, nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, totient/1, test_totient/0, main/0]).

% Generated by Mochi transpiler v0.10.66 (f6dfa6fffe)


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> list_to_integer(V)
        end
    end.


toi(V) ->
    mochi_to_int(V).


mochi_to_float(V) ->
    case erlang:is_float(V) of
        true -> V;
        _ -> float(V)
    end.


-compile({nowarn_unused_function, [mochi_nth/2]}).
-spec mochi_nth(integer(), list()) -> any().
mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_float(V) ->
    erlang:float_to_list(V, [short]);
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_mod(A, B) ->
    A1 = case erlang:is_float(A) of
        true -> trunc(A);
        _ -> A
    end,
    B1 = case erlang:is_float(B) of
        true -> trunc(B);
        _ -> B
    end,
    case B1 of
        0 -> 0;
        _ -> ((A1 rem B1) + B1) rem B1
    end.

totient(N) ->
    try
        Is_prime = [],
        Totients = [],
        Primes = [],
        I = 0,
        Fun = fun Fun_loop(I, Is_prime, N, Primes, Totients) ->
    case (I =< N) of
        true ->
            Is_prime_2 = lists:append((case Is_prime of nil -> []; _ -> Is_prime end), [true]),
            Totients_2 = lists:append((case Totients of nil -> []; _ -> Totients end), [(I - 1)]),
            I_2 = (I + 1),
            Fun_loop(I_2, Is_prime_2, N, Primes, Totients_2);
        _ -> {I, Is_prime, N, Primes, Totients}
    end
end,
{I_2, Is_prime_2, N, Primes, Totients_2} = Fun(I, Is_prime, N, Primes, Totients),
        I_3 = 2,
        Fun_3 = fun Fun_3_loop(I_3, Is_prime_2, N, Primes, Totients_2) ->
    case (I_3 =< N) of
        true ->
            try
                Primes_3 = (case (case erlang:is_map(Is_prime_2) of true -> maps:get(I_3, Is_prime_2, nil); _ -> case I_3 < 0 of true -> mochi_nth(erlang:length(Is_prime_2) + I_3 + 1, Is_prime_2); _ -> mochi_nth(I_3 + 1, Is_prime_2) end end) of
        true -> Primes_2 = lists:append((case Primes of nil -> []; _ -> Primes end), [I_3]),
            Primes_2;
        _ -> Primes
    end),
                J = 0,
                Fun_2 = fun Fun_2_loop(I_3, Is_prime_2, J, N, Primes_3, Totients_2) ->
    case (J < erlang:length(Primes_3)) of
        true ->
            try
                P = (case erlang:is_map(Primes_3) of true -> maps:get(J, Primes_3, nil); _ -> case J < 0 of true -> mochi_nth(erlang:length(Primes_3) + J + 1, Primes_3); _ -> mochi_nth(J + 1, Primes_3) end end),
                case ((I_3 * P) >= N) of
        true -> throw({break, {I_3, Is_prime_2, J, N, Primes_3, Totients_2}});
        _ -> ok
    end,
                Is_prime_3 = lists:sublist(Is_prime_2, (I_3 * P)) ++ [false] ++ lists:nthtail((I_3 * P) + 1, Is_prime_2),
                Totients_4 = (case (mochi_mod(I_3, P) == 0) of
        true -> Totients_3 = lists:sublist(Totients_2, (I_3 * P)) ++ [((case erlang:is_map(Totients_2) of true -> maps:get(I_3, Totients_2, nil); _ -> case I_3 < 0 of true -> mochi_nth(erlang:length(Totients_2) + I_3 + 1, Totients_2); _ -> mochi_nth(I_3 + 1, Totients_2) end end) * P)] ++ lists:nthtail((I_3 * P) + 1, Totients_2),
            throw({break, {I_3, Is_prime_3, J, N, Primes_3, Totients_3}}),
            Totients_3;
        _ -> Totients_2
    end),
                Totients_5 = lists:sublist(Totients_4, (I_3 * P)) ++ [((case erlang:is_map(Totients_4) of true -> maps:get(I_3, Totients_4, nil); _ -> case I_3 < 0 of true -> mochi_nth(erlang:length(Totients_4) + I_3 + 1, Totients_4); _ -> mochi_nth(I_3 + 1, Totients_4) end end) * (P - 1))] ++ lists:nthtail((I_3 * P) + 1, Totients_4),
                J_2 = (J + 1),
                Fun_2_loop(I_3, Is_prime_3, J_2, N, Primes_3, Totients_5)
            catch
                {continue, C0, C1, C2, C3, C4, C5} -> Fun_2_loop(C0, C1, C2, C3, C4, C5);
                {break, Bs} -> Bs
            end;
        _ -> {I_3, Is_prime_2, J, N, Primes_3, Totients_2}
    end
end,
{I_3, Is_prime_3, J_2, N, Primes_3, Totients_5} = Fun_2(I_3, Is_prime_2, J, N, Primes_3, Totients_2),
                I_4 = (I_3 + 1),
                Fun_3_loop(I_4, Is_prime_3, N, Primes_3, Totients_5)
            catch
                {continue, C0, C1, C2, C3, C4} -> Fun_3_loop(C0, C1, C2, C3, C4);
                {break, Bs} -> Bs
            end;
        _ -> {I_3, Is_prime_2, N, Primes, Totients_2}
    end
end,
{I_4, Is_prime_3, N, Primes_3, Totients_5} = Fun_3(I_3, Is_prime_2, N, Primes, Totients_2),
        Totients_5
    catch {return, RetCatch} -> RetCatch end.

test_totient() ->
    try
        Expected = [-1, 0, 1, 2, 2, 4, 2, 6, 4, 6, 9],
        Res = totient(10),
        Idx = 0,
        Fun_4 = fun Fun_4_loop(Expected, Idx, Res) ->
    case (Idx < erlang:length(Expected)) of
        true ->
            case ((case erlang:is_map(Res) of true -> maps:get(Idx, Res, nil); _ -> case Idx < 0 of true -> mochi_nth(erlang:length(Res) + Idx + 1, Res); _ -> mochi_nth(Idx + 1, Res) end end) /= (case erlang:is_map(Expected) of true -> maps:get(Idx, Expected, nil); _ -> case Idx < 0 of true -> mochi_nth(erlang:length(Expected) + Idx + 1, Expected); _ -> mochi_nth(Idx + 1, Expected) end end)) of
        true -> erlang:error({panic, ("totient mismatch at " ++ mochi_str(Idx))});
        _ -> ok
    end,
            Idx_2 = (Idx + 1),
            Fun_4_loop(Expected, Idx_2, Res);
        _ -> {Expected, Idx, Res}
    end
end,
{Expected, Idx_2, Res} = Fun_4(Expected, Idx, Res),
        nil
    catch {return, RetCatch} -> RetCatch end.

main() ->
    try
        test_totient(),
        N_2 = 10,
        Res_2 = totient(10),
        I_5 = 1,
        Fun_5 = fun Fun_5_loop(I_5, N_2, Res_2) ->
    case (I_5 < N_2) of
        true ->
            io:format("~ts~n", [mochi_repr((((mochi_str(I_5) ++ " has ") ++ mochi_str((case erlang:is_map(Res_2) of true -> maps:get(I_5, Res_2, nil); _ -> case I_5 < 0 of true -> mochi_nth(erlang:length(Res_2) + I_5 + 1, Res_2); _ -> mochi_nth(I_5 + 1, Res_2) end end))) ++ " relative primes."))]),
            I_6 = (I_5 + 1),
            Fun_5_loop(I_6, N_2, Res_2);
        _ -> {I_5, N_2, Res_2}
    end
end,
{I_6, N_2, Res_2} = Fun_5(I_5, N_2, Res_2),
        nil
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
