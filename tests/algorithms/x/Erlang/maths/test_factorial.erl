#!/usr/bin/env escript
-module(main).
-compile([export_vars, nowarn_export_vars, nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, factorial/1, factorial_recursive/1, test_zero/0, test_positive_integers/0, test_large_number/0, run_tests/0, main/0]).

% Generated by Mochi transpiler v0.10.73 (698bf55243)


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> case V of
                true -> 1;
                false -> 0;
                _ -> list_to_integer(V)
            end
        end
    end.


toi(V) ->
    mochi_to_int(V).


mochi_to_float(V) ->
    case erlang:is_float(V) of
        true -> V;
        _ -> float(V)
    end.


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_list(V) ->
    case io_lib:printable_list(V) of
        true -> V;
        false -> "[" ++ string:join([mochi_str(E) || E <- V], " ") ++ "]"
    end;
mochi_str(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false -> erlang:float_to_list(V, [short])
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

factorial(N) ->
    try
        case (N < 0) of
        true -> erlang:error({panic, "factorial() not defined for negative values"});
        _ -> ok
    end,
        Value = 1,
        I = 1,
        Fun = fun Fun_loop(I, N, Value) ->
    case (I =< N) of
        true ->
            Value_2 = (Value * I),
            I_2 = (I + 1),
            Fun_loop(I_2, N, Value_2);
        _ -> {I, N, Value}
    end
end,
{I_2, N, Value_2} = Fun(I, N, Value),
        Value_2
    catch {return, RetCatch} -> RetCatch end.

factorial_recursive(N_2) ->
    try
        case (N_2 < 0) of
        true -> erlang:error({panic, "factorial() not defined for negative values"});
        _ -> ok
    end,
        case (N_2 =< 1) of
        true -> throw({return, 1});
        _ -> ok
    end,
        (N_2 * factorial_recursive((N_2 - 1)))
    catch {return, RetCatch} -> RetCatch end.

test_zero() ->
    try
        case (factorial(0) /= 1) of
        true -> erlang:error({panic, "factorial(0) failed"});
        _ -> ok
    end,
        case (factorial_recursive(0) /= 1) of
        true -> erlang:error({panic, "factorial_recursive(0) failed"});
        _ -> ok
    end,
        nil
    catch {return, RetCatch} -> RetCatch end.

test_positive_integers() ->
    try
        case (factorial(1) /= 1) of
        true -> erlang:error({panic, "factorial(1) failed"});
        _ -> ok
    end,
        case (factorial_recursive(1) /= 1) of
        true -> erlang:error({panic, "factorial_recursive(1) failed"});
        _ -> ok
    end,
        case (factorial(5) /= 120) of
        true -> erlang:error({panic, "factorial(5) failed"});
        _ -> ok
    end,
        case (factorial_recursive(5) /= 120) of
        true -> erlang:error({panic, "factorial_recursive(5) failed"});
        _ -> ok
    end,
        case (factorial(7) /= 5040) of
        true -> erlang:error({panic, "factorial(7) failed"});
        _ -> ok
    end,
        case (factorial_recursive(7) /= 5040) of
        true -> erlang:error({panic, "factorial_recursive(7) failed"});
        _ -> ok
    end,
        nil
    catch {return, RetCatch} -> RetCatch end.

test_large_number() ->
    try
        case (factorial(10) /= 3628800) of
        true -> erlang:error({panic, "factorial(10) failed"});
        _ -> ok
    end,
        case (factorial_recursive(10) /= 3628800) of
        true -> erlang:error({panic, "factorial_recursive(10) failed"});
        _ -> ok
    end,
        nil
    catch {return, RetCatch} -> RetCatch end.

run_tests() ->
    try
        test_zero(),
        test_positive_integers(),
        test_large_number(),
        nil
    catch {return, RetCatch} -> RetCatch end.

main() ->
    try
        run_tests(),
        io:format("~s~n", [mochi_repr(factorial(6))]),
        nil
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~p~n", [Err])
    end.
