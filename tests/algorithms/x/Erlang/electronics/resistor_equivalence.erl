#!/usr/bin/env escript
-module(main).
-compile([export_vars, nowarn_export_vars, nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, resistor_parallel/1, resistor_series/1, main/0]).

% Generated by Mochi transpiler v0.10.73 (56e75d8a7a)


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> case V of
                true -> 1;
                false -> 0;
                _ -> list_to_integer(V)
            end
        end
    end.


toi(V) ->
    mochi_to_int(V).


mochi_to_float(V) ->
    case erlang:is_float(V) of
        true -> V;
        _ -> float(V)
    end.


-compile({nowarn_unused_function, [mochi_nth/2]}).
-spec mochi_nth(integer(), list()) -> any().
mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


mochi_format(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false ->
            S = lists:flatten(io_lib:format("~.15f", [V])),
            re:replace(S, "\\.?0+$", "", [global, {return, list}])
    end;
mochi_format(V) when is_integer(V) -> integer_to_list(V);
mochi_format(V) when is_binary(V) -> V;
mochi_format(V) when is_list(V) -> mochi_str(V);
mochi_format(V) -> lists:flatten(io_lib:format("~p", [V])).


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_list(V) ->
    case io_lib:printable_list(V) of
        true -> V;
        false -> "[" ++ string:join([mochi_str(E) || E <- V], " ") ++ "]"
    end;
mochi_str(V) when is_float(V) ->
    I = trunc(V),
    case V == I of
        true -> integer_to_list(I);
        false -> erlang:float_to_list(V, [short])
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_safe_div(A, B) ->
    try
        case erlang:is_integer(A) andalso erlang:is_integer(B) of
            true ->
                case B of
                    0 -> 0;
                    _ -> A div B
                end;
            false -> A / B
        end
    catch _:_ -> 0.0 end.

resistor_parallel(Resistors) ->
    try
        Sum = 0.0,
        I = 0,
        Fun = fun Fun_loop(I, Resistors, Sum) ->
    case (I < erlang:length(Resistors)) of
        true ->
            R = (case erlang:is_map(Resistors) of true -> maps:get(I, Resistors, nil); _ -> case I < 0 of true -> mochi_nth(erlang:length(Resistors) + I + 1, Resistors); _ -> mochi_nth(I + 1, Resistors) end end),
            case (R =< 0.0) of
        true -> erlang:error({panic, (("Resistor at index " ++ mochi_str(I)) ++ " has a negative or zero value!")});
        _ -> ok
    end,
            Sum_2 = (Sum + mochi_safe_div(1.0, R)),
            I_2 = (I + 1),
            Fun_loop(I_2, Resistors, Sum_2);
        _ -> {I, Resistors, Sum}
    end
end,
{I_2, Resistors, Sum_2} = Fun(I, Resistors, Sum),
        mochi_safe_div(1.0, Sum_2)
    catch {return, RetCatch} -> RetCatch end.

resistor_series(Resistors_2) ->
    try
        Sum_3 = 0.0,
        I_3 = 0,
        Fun_2 = fun Fun_2_loop(I_3, Resistors_2, Sum_3) ->
    case (I_3 < erlang:length(Resistors_2)) of
        true ->
            R_2 = (case erlang:is_map(Resistors_2) of true -> maps:get(I_3, Resistors_2, nil); _ -> case I_3 < 0 of true -> mochi_nth(erlang:length(Resistors_2) + I_3 + 1, Resistors_2); _ -> mochi_nth(I_3 + 1, Resistors_2) end end),
            case (R_2 < 0.0) of
        true -> erlang:error({panic, (("Resistor at index " ++ mochi_str(I_3)) ++ " has a negative value!")});
        _ -> ok
    end,
            Sum_4 = (Sum_3 + R_2),
            I_4 = (I_3 + 1),
            Fun_2_loop(I_4, Resistors_2, Sum_4);
        _ -> {I_3, Resistors_2, Sum_3}
    end
end,
{I_4, Resistors_2, Sum_4} = Fun_2(I_3, Resistors_2, Sum_3),
        Sum_4
    catch {return, RetCatch} -> RetCatch end.

main() ->
    try
        Resistors_3 = [3.21389, 2.0, 3.0],
        io:format("~s~n", [mochi_format(("Parallel: " ++ mochi_str(resistor_parallel(Resistors_3))))]),
        io:format("~s~n", [mochi_format(("Series: " ++ mochi_str(resistor_series(Resistors_3))))]),
        nil
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~p~n", [Err])
    end.
