#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, capacitor_parallel/1, capacitor_series/1, main/0]).

% Generated by Mochi transpiler v0.10.64 (9e806895eb) on 2025-08-13 16:13 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, [mochi_nth/2]}).
-spec mochi_nth(integer(), list()) -> any().
mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


-compile({nowarn_unused_function, [mochi_safe_mul/2, mochi_safe_div/2]}).
mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try
        case erlang:is_integer(A) andalso erlang:is_integer(B) of
            true ->
                case B of
                    0 -> 0;
                    _ -> A div B
                end;
            false -> A / B
        end
    catch _:_ -> 0.0 end.

capacitor_parallel(Capacitors) ->
    try
        Sum_c = 0.0,
        I = 0,
        Fun = fun Fun_loop(Capacitors, I, Sum_c) ->
    case (I < erlang:length(Capacitors)) of
        true ->
            C = (case erlang:is_map(Capacitors) of true -> maps:get(I, Capacitors, nil); _ -> case I < 0 of true -> mochi_nth(erlang:length(Capacitors) + I + 1, Capacitors); _ -> mochi_nth(I + 1, Capacitors) end end),
            case (C < 0.0) of
        true -> erlang:error({panic, (("Capacitor at index " ++ mochi_str(I)) ++ " has a negative value!")}),
            throw({return, 0.0});
        _ -> ok
    end,
            Sum_c_2 = (Sum_c + C),
            I_2 = (I + 1),
            Fun_loop(Capacitors, I_2, Sum_c_2);
        _ -> {Capacitors, I, Sum_c}
    end
end,
{Capacitors, I_2, Sum_c_2} = Fun(Capacitors, I, Sum_c),
        Sum_c_2
    catch {return, RetCatch} -> RetCatch end.

capacitor_series(Capacitors_2) ->
    try
        First_sum = 0.0,
        I_3 = 0,
        Fun_2 = fun Fun_2_loop(Capacitors_2, First_sum, I_3) ->
    case (I_3 < erlang:length(Capacitors_2)) of
        true ->
            C_2 = (case erlang:is_map(Capacitors_2) of true -> maps:get(I_3, Capacitors_2, nil); _ -> case I_3 < 0 of true -> mochi_nth(erlang:length(Capacitors_2) + I_3 + 1, Capacitors_2); _ -> mochi_nth(I_3 + 1, Capacitors_2) end end),
            case (C_2 =< 0.0) of
        true -> erlang:error({panic, (("Capacitor at index " ++ mochi_str(I_3)) ++ " has a negative or zero value!")}),
            throw({return, 0.0});
        _ -> ok
    end,
            First_sum_2 = (First_sum + mochi_safe_div(1.0, C_2)),
            I_4 = (I_3 + 1),
            Fun_2_loop(Capacitors_2, First_sum_2, I_4);
        _ -> {Capacitors_2, First_sum, I_3}
    end
end,
{Capacitors_2, First_sum_2, I_4} = Fun_2(Capacitors_2, First_sum, I_3),
        mochi_safe_div(1.0, First_sum_2)
    catch {return, RetCatch} -> RetCatch end.

main() ->
    try
        Parallel = capacitor_parallel([5.71389, 12.0, 3.0]),
        Series = capacitor_series([5.71389, 12.0, 3.0]),
        io:format("~ts~n", [mochi_str(Parallel)]),
        io:format("~ts~n", [mochi_str(Series)]),
        nil
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
