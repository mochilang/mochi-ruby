#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars, nowarn_export_vars, nowarn_export_all, nowarn_unused_expr, nowarn_unused_function]).
-export([main/1, electric_conductivity/3]).

% Generated by Mochi transpiler v0.10.64 (9e806895eb) on 2025-08-13 16:13 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false ->
                    case os:getenv("MOCHI_BENCHMARK") of
                        false -> erlang:system_time(nanosecond);
                        _ ->
                            erlang:put(now_seed, 1),
                            mochi_now()
                    end;
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


-compile({nowarn_unused_function, [mochi_nth/2]}).
-spec mochi_nth(integer(), list()) -> any().
mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, [mochi_str/1]}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, [mochi_repr/1]}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


-compile({nowarn_unused_function, [mochi_safe_mul/2, mochi_safe_div/2]}).
mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try
        case erlang:is_integer(A) andalso erlang:is_integer(B) of
            true ->
                case B of
                    0 -> 0;
                    _ -> A div B
                end;
            false -> A / B
        end
    catch _:_ -> 0.0 end.

electric_conductivity(Conductivity, Electron_conc, Mobility) ->
    try
        Zero_count = 0,
        case (Conductivity == 0.0) of
        true -> Zero_count_2 = (Zero_count + 1),
            Zero_count_3 = Zero_count_2;
        _ -> Zero_count_3 = Zero_count
    end,
        case (Electron_conc == 0.0) of
        true -> Zero_count_4 = (Zero_count_3 + 1),
            Zero_count_5 = Zero_count_4;
        _ -> Zero_count_5 = Zero_count_3
    end,
        case (Mobility == 0.0) of
        true -> Zero_count_6 = (Zero_count_5 + 1),
            Zero_count_7 = Zero_count_6;
        _ -> Zero_count_7 = Zero_count_5
    end,
        case (Zero_count_7 /= 1) of
        true -> erlang:error({panic, "You cannot supply more or less than 2 values"});
        _ -> ok
    end,
        case (Conductivity < 0.0) of
        true -> erlang:error({panic, "Conductivity cannot be negative"});
        _ -> ok
    end,
        case (Electron_conc < 0.0) of
        true -> erlang:error({panic, "Electron concentration cannot be negative"});
        _ -> ok
    end,
        case (Mobility < 0.0) of
        true -> erlang:error({panic, "mobility cannot be negative"});
        _ -> ok
    end,
        case (Conductivity == 0.0) of
        true -> throw({return, #{"kind" => "conductivity", "value" => mochi_safe_mul(mochi_safe_mul(Mobility, Electron_conc), 0.00000000000000000016021)}});
        _ -> ok
    end,
        case (Electron_conc == 0.0) of
        true -> throw({return, #{"kind" => "electron_conc", "value" => mochi_safe_div(Conductivity, mochi_safe_mul(Mobility, 0.00000000000000000016021))}});
        _ -> ok
    end,
        #{"kind" => "mobility", "value" => mochi_safe_div(Conductivity, mochi_safe_mul(Electron_conc, 0.00000000000000000016021))}
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('ELECTRON_CHARGE', 0.00000000000000000016021),
    erlang:put('r1', electric_conductivity(25.0, 100.0, 0.0)),
    erlang:put('r2', electric_conductivity(0.0, 1600.0, 200.0)),
    erlang:put('r3', electric_conductivity(1000.0, 0.0, 1200.0)),
    io:format("~ts~n", [mochi_repr(((maps:get("kind", erlang:get('r1'), nil) ++ " ") ++ mochi_str(maps:get("value", erlang:get('r1'), nil))))]),
    io:format("~ts~n", [mochi_repr(((maps:get("kind", erlang:get('r2'), nil) ++ " ") ++ mochi_str(maps:get("value", erlang:get('r2'), nil))))]),
    io:format("~ts~n", [mochi_repr(((maps:get("kind", erlang:get('r3'), nil) ++ " ") ++ mochi_str(maps:get("value", erlang:get('r3'), nil))))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
