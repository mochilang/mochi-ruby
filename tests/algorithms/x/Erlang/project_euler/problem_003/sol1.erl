#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, is_prime/1, solution/1, main/0]).

% Generated by Mochi transpiler v0.10.61 (c04b4c8319) on 2025-08-08 17:05 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try
        case erlang:is_integer(A) andalso erlang:is_integer(B) of
            true ->
                case B of
                    0 -> 0;
                    _ -> A div B
                end;
            false -> A / B
        end
    catch _:_ -> 0.0 end.


mochi_mod(A, B) ->
    A1 = case erlang:is_float(A) of
        true -> trunc(A);
        _ -> A
    end,
    B1 = case erlang:is_float(B) of
        true -> trunc(B);
        _ -> B
    end,
    case B1 of
        0 -> 0;
        _ -> ((A1 rem B1) + B1) rem B1
    end.

is_prime(Number) ->
    try
        case ((Number > 1) andalso (Number < 4)) of
        true -> throw({return, true});
        _ -> ok
    end,
        case (((Number < 2) orelse (mochi_mod(Number, 2) == 0)) orelse (mochi_mod(Number, 3) == 0)) of
        true -> throw({return, false});
        _ -> ok
    end,
        I = 5,
        Fun = fun Fun_loop(I, Number) ->
    case ((I * I) =< Number) of
        true ->
            case ((mochi_mod(Number, I) == 0) orelse (mochi_mod(Number, (I + 2)) == 0)) of
        true -> throw({return, false});
        _ -> ok
    end,
            I_2 = (I + 6),
            Fun_loop(I_2, Number);
        _ -> {I, Number}
    end
end,
{I_2, Number} = Fun(I, Number),
        true
    catch {return, Ret} -> Ret end.

solution(N) ->
    try
        Num = N,
        case (Num =< 0) of
        true -> io:format("~ts~n", [mochi_repr("Parameter n must be greater than or equal to one.")]),
            throw({return, 0});
        _ -> ok
    end,
        case is_prime(Num) of
        true -> throw({return, Num});
        _ -> ok
    end,
        Fun_2 = fun Fun_2_loop(N, Num) ->
    case (mochi_mod(Num, 2) == 0) of
        true ->
            Num_2 = (Num div 2),
            case is_prime(Num_2) of
        true -> throw({return, Num_2});
        _ -> ok
    end,
            Fun_2_loop(N, Num_2);
        _ -> {N, Num}
    end
end,
{N, Num_2} = Fun_2(N, Num),
        Max_number = 1,
        I_3 = 3,
        Fun_3 = fun Fun_3_loop(I_3, Max_number, N, Num_2) ->
    case ((I_3 * I_3) =< Num_2) of
        true ->
            try
                case (mochi_mod(Num_2, I_3) == 0) of
        true -> case is_prime(mochi_safe_div(Num_2, I_3)) of
        true -> Max_number_2 = mochi_safe_div(Num_2, I_3),
            throw({break, I_3, Max_number_2, N, Num_2}),
            Max_number_5 = Max_number_2;
        _ -> case is_prime(I_3) of
        true -> Max_number_3 = I_3,
            Max_number_4 = Max_number_3;
        _ -> Max_number_4 = Max_number
    end,
            Max_number_5 = Max_number_4
    end,
            Max_number_6 = Max_number_5;
        _ -> Max_number_6 = Max_number
    end,
                I_4 = (I_3 + 2),
                Fun_3_loop(I_4, Max_number_6, N, Num_2)
            catch
                {continue, C0, C1, C2, C3} -> Fun_3_loop(C0, C1, C2, C3);
                {break, B0, B1, B2, B3} -> {B0, B1, B2, B3}
            end;
        _ -> {I_3, Max_number, N, Num_2}
    end
end,
{I_4, Max_number_6, N, Num_2} = Fun_3(I_3, Max_number, N, Num_2),
        Max_number_6
    catch {return, Ret} -> Ret end.

main() ->
    try
        Result = solution(600851475143),
        io:format("~ts~n", [mochi_repr(("solution() = " ++ mochi_str(Result)))]),
        nil
    catch {return, Ret} -> Ret end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    main(),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
