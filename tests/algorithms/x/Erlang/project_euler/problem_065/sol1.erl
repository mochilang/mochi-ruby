#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, sum_digits/1, solution/1]).

% Generated by Mochi transpiler v0.10.61 (336645299d) on 2025-08-08 18:06 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_mod(A, B) ->
    A1 = case erlang:is_float(A) of
        true -> trunc(A);
        _ -> A
    end,
    B1 = case erlang:is_float(B) of
        true -> trunc(B);
        _ -> B
    end,
    case B1 of
        0 -> 0;
        _ -> ((A1 rem B1) + B1) rem B1
    end.

sum_digits(Num) ->
    try
        N = Num,
        Digit_sum = 0,
        Fun = fun Fun_loop(Digit_sum, N, Num) ->
    case (N > 0) of
        true ->
            Digit_sum_2 = (Digit_sum + mochi_mod(N, 10)),
            N_2 = (N div 10),
            Fun_loop(Digit_sum_2, N_2, Num);
        _ -> {Digit_sum, N, Num}
    end
end,
{Digit_sum_2, N_2, Num} = Fun(Digit_sum, N, Num),
        Digit_sum_2
    catch {return, Ret} -> Ret end.

solution(Max_n) ->
    try
        Pre_numerator = 1,
        Cur_numerator = 2,
        I = 2,
        Fun_2 = fun Fun_2_loop(Cur_numerator, I, Max_n, Pre_numerator) ->
    case (I =< Max_n) of
        true ->
            Temp = Pre_numerator,
            E_cont = 1,
            case (mochi_mod(I, 3) == 0) of
        true -> E_cont_2 = ((2 * I) div 3),
            E_cont_3 = E_cont_2;
        _ -> E_cont_3 = E_cont
    end,
            Pre_numerator_2 = Cur_numerator,
            Cur_numerator_2 = ((E_cont_3 * Pre_numerator_2) + Temp),
            I_2 = (I + 1),
            Fun_2_loop(Cur_numerator_2, I_2, Max_n, Pre_numerator_2);
        _ -> {Cur_numerator, I, Max_n, Pre_numerator}
    end
end,
{Cur_numerator_2, I_2, Max_n, Pre_numerator_2} = Fun_2(Cur_numerator, I, Max_n, Pre_numerator),
        sum_digits(Cur_numerator_2)
    catch {return, Ret} -> Ret end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~ts~n", [mochi_repr(solution(9))]),
    io:format("~ts~n", [mochi_repr(solution(10))]),
    io:format("~ts~n", [mochi_repr(solution(50))]),
    io:format("~ts~n", [mochi_repr(solution(100))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
