#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, run/1, solution/1]).

% Generated by Mochi transpiler v0.10.61 (336645299d) on 2025-08-08 18:06 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_member(Key, Coll) ->
    case erlang:is_map(Coll) of
        true -> maps:is_key(Key, Coll);
        _ -> case erlang:is_list(Coll) of
            true -> lists:member(Key, Coll);
            _ when is_binary(Coll) -> string:str(Coll, Key) /= 0;
            _ -> false
        end
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).


mochi_not(X) ->
    case X of
        true -> false;
        false -> true;
        nil -> true;
        _ -> false
    end.

run(N) ->
    try
        Limit = 200000,
        Counts = #{},
        P = 2,
        Fun_3 = fun Fun_3_loop(Counts, Limit, N, P) ->
    case (P =< Limit) of
        true ->
            case mochi_not(maps:is_key(P, Counts)) of
        true -> M = P,
            Fun = fun Fun_loop(Counts, Limit, M, N, P) ->
    case (M =< Limit) of
        true ->
            case maps:is_key(M, Counts) of
        true -> Counts_2 = maps:put(M, (maps:get(M, Counts, nil) + 1), Counts),
            Counts_4 = Counts_2;
        _ -> Counts_3 = maps:put(M, 1, Counts),
            Counts_4 = Counts_3
    end,
            M_2 = (M + P),
            Fun_loop(Counts_4, Limit, M_2, N, P);
        _ -> {Counts, Limit, M, N, P}
    end
end,
{Counts_4, Limit, M_2, N, P} = Fun(Counts, Limit, M, N, P),
            Counts_5 = Counts_4,
            Fun_2 = Fun,
            M_3 = M_2;
        _ -> Counts_5 = Counts,
            Fun_2 = nil,
            M_3 = nil
    end,
            P_2 = (P + 1),
            Fun_3_loop(Counts_5, Limit, N, P_2);
        _ -> {Counts, Limit, N, P}
    end
end,
{Counts_5, Limit, N, P_2} = Fun_3(Counts, Limit, N, P),
        Streak = 0,
        Num = 2,
        Fun_7 = fun Fun_7_loop(Counts_5, Limit, N, Num, P_2, Streak) ->
    case (Num =< Limit) of
        true ->
            C = (case mochi_member(Num, Counts_5) of
    true -> (case erlang:is_map(Counts_5) of true -> maps:get(Num, Counts_5, nil); _ -> case Num < 0 of true -> mochi_nth(erlang:length(Counts_5) + Num + 1, Counts_5); _ -> mochi_nth(Num + 1, Counts_5) end end);
    _ -> 0
end),
            case (C == N) of
        true -> Streak_2 = (Streak + 1),
            case (Streak_2 == N) of
        true -> Result = [],
            Start = ((Num - N) + 1),
            J = 0,
            Fun_4 = fun Fun_4_loop(C, Counts_5, J, Limit, N, Num, P_2, Result, Start, Streak_2) ->
    case (J < N) of
        true ->
            Result_2 = lists:append((case Result of nil -> []; _ -> Result end), [(Start + J)]),
            J_2 = (J + 1),
            Fun_4_loop(C, Counts_5, J_2, Limit, N, Num, P_2, Result_2, Start, Streak_2);
        _ -> {C, Counts_5, J, Limit, N, Num, P_2, Result, Start, Streak_2}
    end
end,
{C, Counts_5, J_2, Limit, N, Num, P_2, Result_2, Start, Streak_2} = Fun_4(C, Counts_5, J, Limit, N, Num, P_2, Result, Start, Streak_2),
            throw({return, Result_2}),
            Fun_5 = Fun_4,
            J_3 = J_2,
            Result_3 = Result_2,
            Start_2 = Start;
        _ -> Fun_5 = Fun_3,
            J_3 = nil,
            Result_3 = nil,
            Start_2 = nil
    end,
            Fun_6 = Fun_5,
            J_4 = J_3,
            Result_4 = Result_3,
            Start_3 = Start_2,
            Streak_4 = Streak_2;
        _ -> Streak_3 = 0,
            Fun_6 = Fun_3,
            J_4 = nil,
            Result_4 = nil,
            Start_3 = nil,
            Streak_4 = Streak_3
    end,
            Num_2 = (Num + 1),
            Fun_7_loop(Counts_5, Limit, N, Num_2, P_2, Streak_4);
        _ -> {Counts_5, Limit, N, Num, P_2, Streak}
    end
end,
{Counts_5, Limit, N, Num_2, P_2, Streak_4} = Fun_7(Counts_5, Limit, N, Num, P_2, Streak),
        []
    catch {return, Ret} -> Ret end.

solution(N_2) ->
    try
        Res = run(N_2),
        (case erlang:is_map(Res) of true -> maps:get(0, Res, nil); _ -> case 0 < 0 of true -> mochi_nth(erlang:length(Res) + 0 + 1, Res); _ -> mochi_nth(0 + 1, Res) end end)
    catch {return, Ret} -> Ret end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~ts~n", [mochi_str(solution(4))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
