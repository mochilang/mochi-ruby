#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, index_of/2, is_alpha/1, to_upper/1, wave/1]).

% Generated by Mochi transpiler v0.10.61 (0609bdaff6) on 2025-08-08 20:45 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_nth(I, L) ->
    try lists:nth(I, L) catch _:_ -> nil end.


-compile({nowarn_unused_function, mochi_str/1}).
mochi_str(V) when is_float(V) ->
    S0 = erlang:float_to_list(V, [short]),
    S1 = re:replace(S0, "\.?0+$", "", [{return, list}]),
    case S1 of
        "" -> "0";
        _ -> S1
    end;
mochi_str(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    S1 = lists:flatten(string:replace(S, ",", " ", all)),
    lists:flatten(string:replace(S1, "\"", "", all)).


-compile({nowarn_unused_function, mochi_repr/1}).
mochi_repr(V) ->
    S = lists:flatten(io_lib:format("~p", [V])),
    lists:flatten(string:replace(S, ",", ", ", all)).

index_of(S, C) ->
    try
        I = 0,
        Fun = fun Fun_loop(C, I, S) ->
    case (I < erlang:length(S)) of
        true ->
            case (string:substr(S, I + 1, ((I + 1) - I)) == C) of
        true -> throw({return, I});
        _ -> ok
    end,
            I_2 = (I + 1),
            Fun_loop(C, I_2, S);
        _ -> {C, I, S}
    end
end,
{C, I_2, S} = Fun(C, I, S),
        -1
    catch {return, RetCatch} -> RetCatch end.

is_alpha(C_2) ->
    try
        ((index_of("abcdefghijklmnopqrstuvwxyz", C_2) >= 0) orelse (index_of("ABCDEFGHIJKLMNOPQRSTUVWXYZ", C_2) >= 0))
    catch {return, RetCatch} -> RetCatch end.

to_upper(C_3) ->
    try
        Idx = index_of("abcdefghijklmnopqrstuvwxyz", C_3),
        case (Idx >= 0) of
        true -> throw({return, string:substr("ABCDEFGHIJKLMNOPQRSTUVWXYZ", Idx + 1, ((Idx + 1) - Idx))});
        _ -> ok
    end,
        C_3
    catch {return, RetCatch} -> RetCatch end.

wave(Txt) ->
    try
        Result = [],
        I_3 = 0,
        Fun_2 = fun Fun_2_loop(I_3, Result, Txt) ->
    case (I_3 < erlang:length(Txt)) of
        true ->
            Ch = string:substr(Txt, I_3 + 1, ((I_3 + 1) - I_3)),
            case is_alpha(Ch) of
        true -> Prefix = string:substr(Txt, 0 + 1, (I_3 - 0)),
            Suffix = string:substr(Txt, (I_3 + 1) + 1, (erlang:length(Txt) - (I_3 + 1))),
            Result_2 = lists:append((case Result of nil -> []; _ -> Result end), [((Prefix ++ to_upper(Ch)) ++ Suffix)]),
            Prefix_2 = Prefix,
            Result_3 = Result_2,
            Suffix_2 = Suffix;
        _ -> Prefix_2 = nil,
            Result_3 = Result,
            Suffix_2 = nil
    end,
            I_4 = (I_3 + 1),
            Fun_2_loop(I_4, Result_3, Txt);
        _ -> {I_3, Result, Txt}
    end
end,
{I_4, Result_3, Txt} = Fun_2(I_3, Result, Txt),
        Result_3
    catch {return, RetCatch} -> RetCatch end.

main(_) ->
    try
        __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    erlang:put('lowercase', "abcdefghijklmnopqrstuvwxyz"),
    erlang:put('uppercase', "ABCDEFGHIJKLMNOPQRSTUVWXYZ"),
    io:format("~ts~n", [mochi_str(wave("cat"))]),
    io:format("~ts~n", [mochi_str(wave("one"))]),
    io:format("~ts~n", [mochi_str(wave("book"))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes])
    catch
        _:Err -> io:format("~s~n", [mochi_str(Err)])
    end.
