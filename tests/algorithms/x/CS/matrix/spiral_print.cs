// Generated by Mochi 0.10.73 (commit 698bf55243) on 2025-08-25 22:27 +0700
#pragma warning disable 0169, 0649, 0162
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static bool is_valid_matrix(BigInteger[][] matrix_0) {
        if ((matrix_0.Length == 0)) {
            return false;
        };
        long cols_1 = _idx(matrix_0, (long)(0)).Length;
        foreach (BigInteger[] row_2 in matrix_0) {
            if ((row_2.Length != cols_1)) {
                return false;
            }
        };
        return true;
    }

    public static BigInteger[] spiral_traversal(BigInteger[][] matrix_3) {
        if ((!Program.is_valid_matrix(matrix_3))) {
            return new BigInteger[]{};
        };
        long rows_4 = matrix_3.Length;
        long cols_5 = _idx(matrix_3, (long)(0)).Length;
        BigInteger top_6 = 0;
        BigInteger bottom_7 = (rows_4 - 1);
        BigInteger left_8 = 0;
        BigInteger right_9 = (cols_5 - 1);
        BigInteger[] result_10 = new BigInteger[]{};
        while (((left_8 <= right_9) && (top_6 <= bottom_7))) {
            BigInteger i_11 = left_8;
            while ((i_11 <= right_9)) {
                result_10 = ((Func<BigInteger[]>)(() => { var _tmp = result_10.Cast<BigInteger>().ToList(); _tmp.Add(_idx(_idx(matrix_3, (long)(top_6)), (long)(i_11))); return _tmp.ToArray(); }))();
                i_11 = (i_11 + 1);
            }
            top_6 = (top_6 + 1);
            i_11 = top_6;
            while ((i_11 <= bottom_7)) {
                result_10 = ((Func<BigInteger[]>)(() => { var _tmp = result_10.Cast<BigInteger>().ToList(); _tmp.Add(_idx(_idx(matrix_3, (long)(i_11)), (long)(right_9))); return _tmp.ToArray(); }))();
                i_11 = (i_11 + 1);
            }
            right_9 = (right_9 - 1);
            if ((top_6 <= bottom_7)) {
                i_11 = right_9;
                while ((i_11 >= left_8)) {
                    result_10 = ((Func<BigInteger[]>)(() => { var _tmp = result_10.Cast<BigInteger>().ToList(); _tmp.Add(_idx(_idx(matrix_3, (long)(bottom_7)), (long)(i_11))); return _tmp.ToArray(); }))();
                    i_11 = (i_11 - 1);
                }
                bottom_7 = (bottom_7 - 1);
            }
            if ((left_8 <= right_9)) {
                i_11 = bottom_7;
                while ((i_11 >= top_6)) {
                    result_10 = ((Func<BigInteger[]>)(() => { var _tmp = result_10.Cast<BigInteger>().ToList(); _tmp.Add(_idx(_idx(matrix_3, (long)(i_11)), (long)(left_8))); return _tmp.ToArray(); }))();
                    i_11 = (i_11 - 1);
                }
                left_8 = (left_8 + 1);
            }
        };
        return result_10;
    }

    public static void spiral_print_clockwise(BigInteger[][] matrix_12) {
        foreach (BigInteger value_13 in Program.spiral_traversal(matrix_12)) {
            Console.WriteLine(Program._fmtTop(_fmtStr(value_13)));
        };
    }

    public static void main() {
        BigInteger[][] a_14 = new BigInteger[][]{new BigInteger[]{1, 2, 3, 4}, new BigInteger[]{5, 6, 7, 8}, new BigInteger[]{9, 10, 11, 12}};
        Program.spiral_print_clockwise(a_14);
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.spiral_traversal(a_14))));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
