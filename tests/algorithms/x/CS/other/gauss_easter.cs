// Generated by Mochi 0.10.73 (commit 9a917aab9f) on 2025-08-25 23:45 +0700
#pragma warning disable 0169, 0649, 0162
using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class EasterDate {
    public BigInteger month;
    public BigInteger day;
    public override string ToString() => $"EasterDate {{month = {month}, day = {day}}}";
}
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static long _mod(long a, long b) {
        if (b == 0) return 0;
        var r = a % b;
        if ((r < 0 && b > 0) || (r > 0 && b < 0)) r += b;
        return r;
    }
    static BigInteger _mod(BigInteger a, BigInteger b) {
        if (b == 0) return BigInteger.Zero;
        var r = a % b;
        if ((r < 0 && b > 0) || (r > 0 && b < 0)) r += b;
        return r;
    }
    static long _floordiv(long a, long b) {
        if (b == 0) return 0;
        var q = a / b;
        var r = a % b;
        if ((r > 0 && b < 0) || (r < 0 && b > 0)) q--;
        return q;
    }
    static BigInteger _floordiv(BigInteger a, BigInteger b) {
        if (b == 0) return BigInteger.Zero;
        var q = a / b;
        var r = a % b;
        if ((r > 0 && b < 0) || (r < 0 && b > 0)) q -= 1;
        return q;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    static BigInteger[] years_17 = new BigInteger[]{1994, 2000, 2010, 2021, 2023, 2032, 2100};
    static BigInteger i_18 = 0;
    public static EasterDate gauss_easter(BigInteger year_0) {
        BigInteger metonic_cycle_1 = _mod(year_0, 19);
        BigInteger julian_leap_year_2 = _mod(year_0, 4);
        BigInteger non_leap_year_3 = _mod(year_0, 7);
        BigInteger leap_day_inhibits_4 = (year_0 / 100);
        BigInteger lunar_orbit_correction_5 = ((13 + (8 * leap_day_inhibits_4)) / 25);
        double leap_day_reinstall_number_6 = ((double)(leap_day_inhibits_4) / 4.0);
        double secular_moon_shift_7 = ((((15.0 - (double)(lunar_orbit_correction_5)) + (double)(leap_day_inhibits_4)) - leap_day_reinstall_number_6) % 30.0);
        double century_starting_point_8 = (((4.0 + (double)(leap_day_inhibits_4)) - leap_day_reinstall_number_6) % 7.0);
        double days_to_add_9 = (((19.0 * (double)(metonic_cycle_1)) + secular_moon_shift_7) % 30.0);
        double days_from_phm_to_sunday_10 = (((((2.0 * (double)(julian_leap_year_2)) + (4.0 * (double)(non_leap_year_3))) + (6.0 * days_to_add_9)) + century_starting_point_8) % 7.0);
        if (((days_to_add_9 == 29.0) && (days_from_phm_to_sunday_10 == 6.0))) {
            return new EasterDate{month = 4, day = 19};
        };
        if (((days_to_add_9 == 28.0) && (days_from_phm_to_sunday_10 == 6.0))) {
            return new EasterDate{month = 4, day = 18};
        };
        long offset_11 = (long)((days_to_add_9 + days_from_phm_to_sunday_10));
        BigInteger total_12 = (22 + offset_11);
        if ((total_12 > 31)) {
            return new EasterDate{month = 4, day = (total_12 - 31)};
        };
        return new EasterDate{month = 3, day = total_12};
    }

    public static string format_date(BigInteger year_13, EasterDate d_14) {
        string month_15 = ((d_14.month < 10) ? ("0" + _fmtStr(d_14.month)) : _fmtStr(d_14.month));
        string day_16 = ((d_14.day < 10) ? ("0" + _fmtStr(d_14.day)) : _fmtStr(d_14.day));
        return ((((_fmtStr(year_13) + "-") + month_15) + "-") + day_16);
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            while ((i_18 < years_17.Length)) {
                BigInteger y_19 = _idx(years_17, (long)(i_18));
                EasterDate e_20 = Program.gauss_easter(y_19);
                Console.WriteLine(Program._fmtTop(((("Easter in " + _fmtStr(y_19)) + " is ") + Program.format_date(y_19, e_20))));
                i_18 = (i_18 + 1);
            }
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
