// Generated by Mochi 0.10.59 on 2025-08-06 22:16 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static void panic(string msg_0) {
        Console.WriteLine(Program._fmtTop(msg_0));
    }

    public static string trim_spaces(string s_1) {
        long start_2 = 0;
        long end_3 = (s_1.Length - 1);
        while (((start_2 <= end_3) && (_substr(s_1, start_2, (start_2 + 1)) == " "))) {
            start_2 = (start_2 + 1);
        };
        while (((end_3 >= start_2) && (_substr(s_1, end_3, (end_3 + 1)) == " "))) {
            end_3 = (end_3 - 1);
        };
        if ((start_2 > end_3)) {
            return "";
        };
        return _substr(s_1, start_2, (end_3 + 1));
    }

    public static long char_to_digit(string ch_4) {
        if ((ch_4 == "0")) {
            return 0;
        };
        if ((ch_4 == "1")) {
            return 1;
        };
        if ((ch_4 == "2")) {
            return 2;
        };
        if ((ch_4 == "3")) {
            return 3;
        };
        if ((ch_4 == "4")) {
            return 4;
        };
        if ((ch_4 == "5")) {
            return 5;
        };
        if ((ch_4 == "6")) {
            return 6;
        };
        if ((ch_4 == "7")) {
            return 7;
        };
        Program.panic("Non-octal value was passed to the function");
        return 0;
    }

    public static long oct_to_decimal(string oct_string_5) {
        string s_6 = Program.trim_spaces(oct_string_5);
        if ((s_6.Length == 0)) {
            Program.panic("Empty string was passed to the function");
            return 0;
        };
        bool is_negative_7 = false;
        if ((_substr(s_6, 0, 1) == "-")) {
            is_negative_7 = true;
            s_6 = _substr(s_6, 1, s_6.Length);
        };
        if ((s_6.Length == 0)) {
            Program.panic("Non-octal value was passed to the function");
            return 0;
        };
        long decimal_number_8 = 0;
        long i_9 = 0;
        while ((i_9 < s_6.Length)) {
            string ch_10 = _substr(s_6, i_9, (i_9 + 1));
            long digit_11 = Program.char_to_digit(ch_10);
            decimal_number_8 = ((8 * decimal_number_8) + digit_11);
            i_9 = (i_9 + 1);
        };
        if (is_negative_7) {
            decimal_number_8 = -decimal_number_8;
        };
        return decimal_number_8;
    }

    public static void main() {
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.oct_to_decimal("1"))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.oct_to_decimal("-1"))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.oct_to_decimal("12"))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.oct_to_decimal(" 12   "))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.oct_to_decimal("-45"))));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.oct_to_decimal("0"))));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
