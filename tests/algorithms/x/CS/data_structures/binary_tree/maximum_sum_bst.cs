// Generated by Mochi 0.10.72 (commit bec97b2ce3) on 2025-08-24 16:57 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class Node {
    public BigInteger val;
    public BigInteger left;
    public BigInteger right;
    public override string ToString() => $"Node {{val = {val}, left = {left}, right = {right}}}";
}
class Info {
    public bool is_bst;
    public BigInteger min_val;
    public BigInteger max_val;
    public BigInteger total;
    public BigInteger best;
    public override string ToString() => $"Info {{is_bst = {is_bst}, min_val = {min_val}, max_val = {max_val}, total = {total}, best = {best}}}";
}
#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static BigInteger min_int(BigInteger a_0, BigInteger b_1) {
        if ((a_0 < b_1)) {
            return a_0;
        };
        return b_1;
    }

    public static BigInteger max_int(BigInteger a_2, BigInteger b_3) {
        if ((a_2 > b_3)) {
            return a_2;
        };
        return b_3;
    }

    public static Info solver(Node[] nodes_4, BigInteger idx_5) {
        if ((idx_5 == -1)) {
            return new Info{is_bst = true, min_val = 2147483647, max_val = -2147483648L, total = 0, best = 0};
        };
        Node node_6 = _idx(nodes_4, (long)(idx_5));
        Info left_info_7 = Program.solver(nodes_4, node_6.left);
        Info right_info_8 = Program.solver(nodes_4, node_6.right);
        BigInteger current_best_9 = Program.max_int(left_info_7.best, right_info_8.best);
        if ((((left_info_7.is_bst && right_info_8.is_bst) && (left_info_7.max_val < node_6.val)) && (node_6.val < right_info_8.min_val))) {
            BigInteger sum_val_10 = ((left_info_7.total + right_info_8.total) + node_6.val);
            current_best_9 = Program.max_int(current_best_9, sum_val_10);
            return new Info{is_bst = true, min_val = Program.min_int(left_info_7.min_val, node_6.val), max_val = Program.max_int(right_info_8.max_val, node_6.val), total = sum_val_10, best = current_best_9};
        };
        return new Info{is_bst = false, min_val = 0, max_val = 0, total = 0, best = current_best_9};
    }

    public static BigInteger max_sum_bst(Node[] nodes_11, BigInteger root_12) {
        Info info_13 = Program.solver(nodes_11, root_12);
        return info_13.best;
    }

    public static void main() {
        Node[] t1_nodes_14 = new Node[]{new Node{val = 4, left = 1, right = -1}, new Node{val = 3, left = 2, right = 3}, new Node{val = 1, left = -1, right = -1}, new Node{val = 2, left = -1, right = -1}};
        Console.WriteLine(Program._fmtTop(Program.max_sum_bst(t1_nodes_14, 0)));
        Node[] t2_nodes_15 = new Node[]{new Node{val = -4, left = 1, right = 2}, new Node{val = -2, left = -1, right = -1}, new Node{val = -5, left = -1, right = -1}};
        Console.WriteLine(Program._fmtTop(Program.max_sum_bst(t2_nodes_15, 0)));
        Node[] t3_nodes_16 = new Node[]{new Node{val = 1, left = 1, right = 2}, new Node{val = 4, left = 3, right = 4}, new Node{val = 3, left = 5, right = 6}, new Node{val = 2, left = -1, right = -1}, new Node{val = 4, left = -1, right = -1}, new Node{val = 2, left = -1, right = -1}, new Node{val = 5, left = 7, right = 8}, new Node{val = 4, left = -1, right = -1}, new Node{val = 6, left = -1, right = -1}};
        Console.WriteLine(Program._fmtTop(Program.max_sum_bst(t3_nodes_16, 0)));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
