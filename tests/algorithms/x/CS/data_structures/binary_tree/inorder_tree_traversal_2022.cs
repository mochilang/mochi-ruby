// Generated by Mochi 0.10.72 (commit bec97b2ce3) on 2025-08-24 16:57 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class Node {
    public BigInteger data;
    public BigInteger left;
    public BigInteger right;
    public override string ToString() => $"Node {{data = {data}, left = {left}, right = {right}}}";
}
class TreeState {
    public Node[] nodes;
    public BigInteger root;
    public override string ToString() => $"TreeState {{nodes = {nodes}, root = {root}}}";
}
#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static BigInteger new_node(TreeState state_0, BigInteger value_1) {
        state_0.nodes = ((Func<Node[]>)(() => { var _tmp = state_0.nodes.Cast<Node>().ToList(); _tmp.Add(new Node{data = value_1, left = -1, right = -1}); return _tmp.ToArray(); }))();
        return (state_0.nodes.Length - 1);
    }

    public static void insert(TreeState state_2, BigInteger value_3) {
        if ((state_2.root == -1)) {
            state_2.root = Program.new_node(state_2, value_3);
            return;
        };
        BigInteger current_4 = state_2.root;
        Node[] nodes_5 = state_2.nodes;
        while (true) {
            Node node_6 = _idx(nodes_5, (long)(current_4));
            if ((value_3 < node_6.data)) {
                if ((node_6.left == -1)) {
                    node_6.left = Program.new_node(state_2, value_3);
                    nodes_5[(int)(current_4)] = node_6;
                    state_2.nodes = nodes_5;
                    return;
                }
                current_4 = node_6.left;
            } else {
                if ((node_6.right == -1)) {
                    node_6.right = Program.new_node(state_2, value_3);
                    nodes_5[(int)(current_4)] = node_6;
                    state_2.nodes = nodes_5;
                    return;
                }
                current_4 = node_6.right;
            }
        };
    }

    public static BigInteger[] inorder(TreeState state_7, BigInteger idx_8) {
        if ((idx_8 == -1)) {
            return new BigInteger[]{};
        };
        Node node_9 = _idx(state_7.nodes, (long)(idx_8));
        BigInteger[] result_10 = Program.inorder(state_7, node_9.left);
        result_10 = ((Func<BigInteger[]>)(() => { var _tmp = result_10.Cast<BigInteger>().ToList(); _tmp.Add(node_9.data); return _tmp.ToArray(); }))();
        BigInteger[] right_part_11 = Program.inorder(state_7, node_9.right);
        BigInteger i_12 = 0;
        while ((i_12 < right_part_11.Length)) {
            result_10 = ((Func<BigInteger[]>)(() => { var _tmp = result_10.Cast<BigInteger>().ToList(); _tmp.Add(_idx(right_part_11, (long)(i_12))); return _tmp.ToArray(); }))();
            i_12 = (i_12 + 1);
        };
        return result_10;
    }

    public static TreeState make_tree() {
        TreeState state_13 = new TreeState{nodes = new Node[]{}, root = -1};
        Program.insert(state_13, 15);
        Program.insert(state_13, 10);
        Program.insert(state_13, 25);
        Program.insert(state_13, 6);
        Program.insert(state_13, 14);
        Program.insert(state_13, 20);
        Program.insert(state_13, 60);
        return state_13;
    }

    public static void main() {
        TreeState state_14 = Program.make_tree();
        Console.WriteLine(Program._fmtTop("Printing values of binary search tree in Inorder Traversal."));
        Console.WriteLine(Program._fmtTop(Program.inorder(state_14, state_14.root)));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
