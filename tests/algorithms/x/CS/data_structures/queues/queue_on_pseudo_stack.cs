// Generated by Mochi 0.10.72 (commit b9a682dda0) on 2025-08-24 22:16 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class Queue {
    public BigInteger[] stack;
    public BigInteger length;
    public override string ToString() => $"Queue {{stack = {stack}, length = {length}}}";
}
class GetResult {
    public Queue queue;
    public BigInteger value;
    public override string ToString() => $"GetResult {{queue = {queue}, value = {value}}}";
}
class FrontResult {
    public Queue queue;
    public BigInteger value;
    public override string ToString() => $"FrontResult {{queue = {queue}, value = {value}}}";
}
#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static Queue empty_queue() {
        return new Queue{stack = new BigInteger[]{}, length = 0};
    }

    public static Queue put(Queue q_0, BigInteger item_1) {
        BigInteger[] s_2 = ((Func<BigInteger[]>)(() => { var _tmp = q_0.stack.Cast<BigInteger>().ToList(); _tmp.Add(item_1); return _tmp.ToArray(); }))();
        return new Queue{stack = s_2, length = (q_0.length + 1)};
    }

    public static BigInteger[] drop_first(BigInteger[] xs_3) {
        BigInteger[] res_4 = new BigInteger[]{};
        BigInteger i_5 = 1;
        while ((i_5 < xs_3.Length)) {
            res_4 = ((Func<BigInteger[]>)(() => { var _tmp = res_4.Cast<BigInteger>().ToList(); _tmp.Add(_idx(xs_3, (long)(i_5))); return _tmp.ToArray(); }))();
            i_5 = (i_5 + 1);
        };
        return res_4;
    }

    public static BigInteger[] drop_last(BigInteger[] xs_6) {
        BigInteger[] res_7 = new BigInteger[]{};
        BigInteger i_8 = 0;
        while ((i_8 < (xs_6.Length - 1))) {
            res_7 = ((Func<BigInteger[]>)(() => { var _tmp = res_7.Cast<BigInteger>().ToList(); _tmp.Add(_idx(xs_6, (long)(i_8))); return _tmp.ToArray(); }))();
            i_8 = (i_8 + 1);
        };
        return res_7;
    }

    public static Queue rotate(Queue q_9, BigInteger rotation_10) {
        BigInteger[] s_11 = q_9.stack;
        BigInteger i_12 = 0;
        while (((i_12 < rotation_10) && (s_11.Length > 0))) {
            BigInteger temp_13 = _idx(s_11, (long)(0));
            s_11 = Program.drop_first(s_11);
            s_11 = ((Func<BigInteger[]>)(() => { var _tmp = s_11.Cast<BigInteger>().ToList(); _tmp.Add(temp_13); return _tmp.ToArray(); }))();
            i_12 = (i_12 + 1);
        };
        return new Queue{stack = s_11, length = q_9.length};
    }

    public static GetResult get(Queue q_14) {
        if ((q_14.length == 0)) {
            throw new Exception("queue empty");
        };
        Queue q1_15 = Program.rotate(q_14, 1);
        BigInteger v_16 = _idx(q1_15.stack, (long)((q1_15.length - 1)));
        BigInteger[] s_17 = Program.drop_last(q1_15.stack);
        Queue q2_18 = new Queue{stack = s_17, length = q1_15.length};
        q2_18 = Program.rotate(q2_18, (q2_18.length - 1));
        q2_18 = new Queue{stack = q2_18.stack, length = (q2_18.length - 1)};
        return new GetResult{queue = q2_18, value = v_16};
    }

    public static FrontResult front(Queue q_19) {
        GetResult r_20 = Program.get(q_19);
        Queue q2_21 = Program.put(r_20.queue, r_20.value);
        q2_21 = Program.rotate(q2_21, (q2_21.length - 1));
        return new FrontResult{queue = q2_21, value = r_20.value};
    }

    public static BigInteger size(Queue q_22) {
        return q_22.length;
    }

    public static string to_string(Queue q_23) {
        string s_24 = "<";
        if ((q_23.length > 0)) {
            s_24 = (s_24 + _fmtStr(_idx(q_23.stack, (long)(0))));
            BigInteger i_25 = 1;
            while ((i_25 < q_23.length)) {
                s_24 = ((s_24 + ", ") + _fmtStr(_idx(q_23.stack, (long)(i_25))));
                i_25 = (i_25 + 1);
            }
        };
        s_24 = (s_24 + ">");
        return s_24;
    }

    public static void main() {
        Queue q_26 = Program.empty_queue();
        q_26 = Program.put(q_26, 1);
        q_26 = Program.put(q_26, 2);
        q_26 = Program.put(q_26, 3);
        Console.WriteLine(Program._fmtTop(Program.to_string(q_26)));
        GetResult g_27 = Program.get(q_26);
        q_26 = g_27.queue;
        Console.WriteLine(Program._fmtTop(g_27.value));
        Console.WriteLine(Program._fmtTop(Program.to_string(q_26)));
        FrontResult f_28 = Program.front(q_26);
        q_26 = f_28.queue;
        Console.WriteLine(Program._fmtTop(f_28.value));
        Console.WriteLine(Program._fmtTop(Program.to_string(q_26)));
        Console.WriteLine(Program._fmtTop(Program.size(q_26)));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
