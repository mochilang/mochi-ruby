// Generated by Mochi 0.10.72 (commit b9a682dda0) on 2025-08-24 22:16 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class CircularQueue {
    public string[] data;
    public BigInteger[] next;
    public BigInteger[] prev;
    public BigInteger front;
    public BigInteger rear;
    public override string ToString() => $"CircularQueue {{data = {data}, next = {next}, prev = {prev}, front = {front}, rear = {rear}}}";
}
class DequeueResult {
    public CircularQueue queue;
    public string value;
    public override string ToString() => $"DequeueResult {{queue = {queue}, value = \"{value}\"}}";
}
#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static long _mod(long a, long b) {
        if (b == 0) return 0;
        var r = a % b;
        if ((r < 0 && b > 0) || (r > 0 && b < 0)) r += b;
        return r;
    }
    static BigInteger _mod(BigInteger a, BigInteger b) {
        if (b == 0) return BigInteger.Zero;
        var r = a % b;
        if ((r < 0 && b > 0) || (r > 0 && b < 0)) r += b;
        return r;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static CircularQueue create_queue(BigInteger capacity_0) {
        string[] data_1 = new string[]{};
        BigInteger[] next_2 = new BigInteger[]{};
        BigInteger[] prev_3 = new BigInteger[]{};
        BigInteger i_4 = 0;
        while ((i_4 < capacity_0)) {
            data_1 = ((Func<string[]>)(() => { var _tmp = data_1.Cast<string>().ToList(); _tmp.Add(""); return _tmp.ToArray(); }))();
            next_2 = ((Func<BigInteger[]>)(() => { var _tmp = next_2.Cast<BigInteger>().ToList(); _tmp.Add(_mod((i_4 + 1), capacity_0)); return _tmp.ToArray(); }))();
            prev_3 = ((Func<BigInteger[]>)(() => { var _tmp = prev_3.Cast<BigInteger>().ToList(); _tmp.Add(_mod(((i_4 - 1) + capacity_0), capacity_0)); return _tmp.ToArray(); }))();
            i_4 = (i_4 + 1);
        };
        return new CircularQueue{data = data_1, next = next_2, prev = prev_3, front = 0, rear = 0};
    }

    public static bool is_empty(CircularQueue q_5) {
        return ((q_5.front == q_5.rear) && (_idx(q_5.data, (long)(q_5.front)) == ""));
    }

    public static void check_can_perform(CircularQueue q_6) {
        if (Program.is_empty(q_6)) {
            throw new Exception("Empty Queue");
        };
    }

    public static void check_is_full(CircularQueue q_7) {
        if ((_idx(q_7.next, (long)(q_7.rear)) == q_7.front)) {
            throw new Exception("Full Queue");
        };
    }

    public static string peek(CircularQueue q_8) {
        Program.check_can_perform(q_8);
        return _idx(q_8.data, (long)(q_8.front));
    }

    public static CircularQueue enqueue(CircularQueue q_9, string value_10) {
        Program.check_is_full(q_9);
        if ((!Program.is_empty(q_9))) {
            q_9.rear = _idx(q_9.next, (long)(q_9.rear));
        };
        string[] data_11 = q_9.data;
        data_11[(int)(q_9.rear)] = value_10;
        q_9.data = data_11;
        return q_9;
    }

    public static DequeueResult dequeue(CircularQueue q_12) {
        Program.check_can_perform(q_12);
        string[] data_13 = q_12.data;
        string val_14 = _idx(data_13, (long)(q_12.front));
        data_13[(int)(q_12.front)] = "";
        q_12.data = data_13;
        if ((q_12.front != q_12.rear)) {
            q_12.front = _idx(q_12.next, (long)(q_12.front));
        };
        return new DequeueResult{queue = q_12, value = val_14};
    }

    public static void main() {
        CircularQueue q_15 = Program.create_queue(3);
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.is_empty(q_15))));
        q_15 = Program.enqueue(q_15, "a");
        q_15 = Program.enqueue(q_15, "b");
        Console.WriteLine(Program._fmtTop(Program.peek(q_15)));
        DequeueResult res_16 = Program.dequeue(q_15);
        q_15 = res_16.queue;
        Console.WriteLine(Program._fmtTop(res_16.value));
        res_16 = Program.dequeue(q_15);
        q_15 = res_16.queue;
        Console.WriteLine(Program._fmtTop(res_16.value));
        Console.WriteLine(Program._fmtTop(_fmtStr(Program.is_empty(q_15))));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
