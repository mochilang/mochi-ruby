// Generated by Mochi 0.10.72 (commit ae593555b9) on 2025-08-25 08:33 +0700
#pragma warning disable 0169, 0649, 0162
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static BigInteger[] make_list(BigInteger len_0, BigInteger value_1) {
        BigInteger[] arr_2 = new BigInteger[]{};
        BigInteger i_3 = 0;
        while ((i_3 < len_0)) {
            arr_2 = ((Func<BigInteger[]>)(() => { var _tmp = arr_2.Cast<BigInteger>().ToList(); _tmp.Add(value_1); return _tmp.ToArray(); }))();
            i_3 = (i_3 + 1);
        };
        return arr_2;
    }

    public static BigInteger trapped_rainwater(BigInteger[] heights_4) {
        if ((heights_4.Length == 0)) {
            return 0;
        };
        BigInteger i_5 = 0;
        while ((i_5 < heights_4.Length)) {
            if ((_idx(heights_4, (long)(i_5)) < 0)) {
                throw new Exception("No height can be negative");
            }
            i_5 = (i_5 + 1);
        };
        BigInteger length_6 = heights_4.Length;
        BigInteger[] left_max_7 = Program.make_list(length_6, 0);
        left_max_7[(int)(0)] = _idx(heights_4, (long)(0));
        i_5 = 1;
        while ((i_5 < length_6)) {
            if ((_idx(heights_4, (long)(i_5)) > _idx(left_max_7, (long)((i_5 - 1))))) {
                left_max_7[(int)(i_5)] = _idx(heights_4, (long)(i_5));
            } else {
                left_max_7[(int)(i_5)] = _idx(left_max_7, (long)((i_5 - 1)));
            }
            i_5 = (i_5 + 1);
        };
        BigInteger[] right_max_8 = Program.make_list(length_6, 0);
        BigInteger last_9 = (length_6 - 1);
        right_max_8[(int)(last_9)] = _idx(heights_4, (long)(last_9));
        i_5 = (last_9 - 1);
        while ((i_5 >= 0)) {
            if ((_idx(heights_4, (long)(i_5)) > _idx(right_max_8, (long)((i_5 + 1))))) {
                right_max_8[(int)(i_5)] = _idx(heights_4, (long)(i_5));
            } else {
                right_max_8[(int)(i_5)] = _idx(right_max_8, (long)((i_5 + 1)));
            }
            i_5 = (i_5 - 1);
        };
        BigInteger total_10 = 0;
        i_5 = 0;
        while ((i_5 < length_6)) {
            BigInteger left_11 = _idx(left_max_7, (long)(i_5));
            BigInteger right_12 = _idx(right_max_8, (long)(i_5));
            BigInteger smaller_13 = ((left_11 < right_12) ? left_11 : right_12);
            total_10 = (total_10 + (smaller_13 - _idx(heights_4, (long)(i_5))));
            i_5 = (i_5 + 1);
        };
        return total_10;
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.trapped_rainwater(new BigInteger[]{0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1}))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.trapped_rainwater(new BigInteger[]{7, 1, 5, 3, 6, 4}))));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
