// Generated by Mochi 0.10.67 (commit c7612b9c83) on 2025-08-16 19:42 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;
using System.Globalization;

#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static double abs_val(double num_0) {
        if ((num_0 < 0.0)) {
            return -num_0;
        };
        return num_0;
    }

    public static long abs_min(long[] x_1) {
        if ((x_1.Length == 0)) {
            throw new Exception("abs_min() arg is an empty sequence");
        };
        long j_2 = _idx(x_1, 0);
        long idx_3 = 0;
        while ((idx_3 < x_1.Length)) {
            long i_4 = _idx(x_1, idx_3);
            if ((Program.abs_val(Convert.ToDouble(i_4)) < Program.abs_val(Convert.ToDouble(j_2)))) {
                j_2 = i_4;
            }
            idx_3 = (idx_3 + 1);
        };
        return j_2;
    }

    public static long abs_max(long[] x_5) {
        if ((x_5.Length == 0)) {
            throw new Exception("abs_max() arg is an empty sequence");
        };
        long j_6 = _idx(x_5, 0);
        long idx_7 = 0;
        while ((idx_7 < x_5.Length)) {
            long i_8 = _idx(x_5, idx_7);
            if ((Program.abs_val(Convert.ToDouble(i_8)) > Program.abs_val(Convert.ToDouble(j_6)))) {
                j_6 = i_8;
            }
            idx_7 = (idx_7 + 1);
        };
        return j_6;
    }

    public static long abs_max_sort(long[] x_9) {
        if ((x_9.Length == 0)) {
            throw new Exception("abs_max_sort() arg is an empty sequence");
        };
        long[] arr_10 = new long[]{};
        long i_11 = 0;
        while ((i_11 < x_9.Length)) {
            arr_10 = ((Func<long[]>)(() => { var _tmp = arr_10.Cast<long>().ToList(); _tmp.Add(_idx(x_9, i_11)); return _tmp.ToArray(); }))();
            i_11 = (i_11 + 1);
        };
        long n_12 = arr_10.Length;
        long a_13 = 0;
        while ((a_13 < n_12)) {
            long b_14 = 0;
            while ((b_14 < ((n_12 - a_13) - 1))) {
                if ((Program.abs_val(Convert.ToDouble(_idx(arr_10, b_14))) > Program.abs_val(Convert.ToDouble(_idx(arr_10, (b_14 + 1)))))) {
                    long temp_15 = _idx(arr_10, b_14);
                    arr_10[(int)(b_14)] = _idx(arr_10, (b_14 + 1));
                    arr_10[(int)((b_14 + 1))] = temp_15;
                }
                b_14 = (b_14 + 1);
            }
            a_13 = (a_13 + 1);
        };
        return _idx(arr_10, (n_12 - 1));
    }

    public static void test_abs_val() {
        if ((Program.abs_val(0.0) != 0.0)) {
            throw new Exception("abs_val(0) failed");
        };
        if ((Program.abs_val(34.0) != 34.0)) {
            throw new Exception("abs_val(34) failed");
        };
        if ((Program.abs_val(-100000000000.0) != 100000000000.0)) {
            throw new Exception("abs_val large failed");
        };
        long[] a_16 = new long[]{-3, -1, 2, -11};
        if ((Program.abs_max(a_16) != -11)) {
            throw new Exception("abs_max failed");
        };
        if ((Program.abs_max_sort(a_16) != -11)) {
            throw new Exception("abs_max_sort failed");
        };
        if ((Program.abs_min(a_16) != -1)) {
            throw new Exception("abs_min failed");
        };
    }

    public static void main() {
        Program.test_abs_val();
        Console.WriteLine(Program._fmtTop(Program.abs_val(-34.0)));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }
    }
}
