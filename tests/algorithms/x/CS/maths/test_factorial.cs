// Generated by Mochi 0.10.73 (commit 698bf55243) on 2025-08-25 22:27 +0700
#pragma warning disable 0169, 0649, 0162
using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Numerics;
using System.Collections;
using System.Globalization;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        if (v is BigInteger bi) return bi.ToString(CultureInfo.InvariantCulture);

        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static BigInteger factorial(BigInteger n_0) {
        if ((n_0 < 0)) {
            throw new Exception("factorial() not defined for negative values");
        };
        BigInteger value_1 = 1;
        BigInteger i_2 = 1;
        while ((i_2 <= n_0)) {
            value_1 = (value_1 * i_2);
            i_2 = (i_2 + 1);
        };
        return value_1;
    }

    public static BigInteger factorial_recursive(BigInteger n_3) {
        if ((n_3 < 0)) {
            throw new Exception("factorial() not defined for negative values");
        };
        if ((n_3 <= 1)) {
            return 1;
        };
        return (n_3 * Program.factorial_recursive((n_3 - 1)));
    }

    public static void test_zero() {
        if ((Program.factorial(0) != 1)) {
            throw new Exception("factorial(0) failed");
        };
        if ((Program.factorial_recursive(0) != 1)) {
            throw new Exception("factorial_recursive(0) failed");
        };
    }

    public static void test_positive_integers() {
        if ((Program.factorial(1) != 1)) {
            throw new Exception("factorial(1) failed");
        };
        if ((Program.factorial_recursive(1) != 1)) {
            throw new Exception("factorial_recursive(1) failed");
        };
        if ((Program.factorial(5) != 120)) {
            throw new Exception("factorial(5) failed");
        };
        if ((Program.factorial_recursive(5) != 120)) {
            throw new Exception("factorial_recursive(5) failed");
        };
        if ((Program.factorial(7) != 5040)) {
            throw new Exception("factorial(7) failed");
        };
        if ((Program.factorial_recursive(7) != 5040)) {
            throw new Exception("factorial_recursive(7) failed");
        };
    }

    public static void test_large_number() {
        if ((Program.factorial(10) != 3628800)) {
            throw new Exception("factorial(10) failed");
        };
        if ((Program.factorial_recursive(10) != 3628800)) {
            throw new Exception("factorial_recursive(10) failed");
        };
    }

    public static void run_tests() {
        Program.test_zero();
        Program.test_positive_integers();
        Program.test_large_number();
    }

    public static void main() {
        Program.run_tests();
        Console.WriteLine(Program._fmtTop(Program.factorial(6)));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }

    }
}
