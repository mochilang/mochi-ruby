// Generated by Mochi 0.10.66 (commit 8fdd1fe3d6) on 2025-08-15 10:15 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;
using System.Globalization;

#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static long _mod(long a, long b) {
        if (b == 0) return 0;
        var r = a % b;
        if ((r < 0 && b > 0) || (r > 0 && b < 0)) r += b;
        return r;
    }
    static long _floordiv(long a, long b) {
        if (b == 0) return 0;
        var q = a / b;
        var r = a % b;
        if ((r > 0 && b < 0) || (r < 0 && b > 0)) q--;
        return q;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static bool is_prime(long n_0) {
        if ((n_0 <= 1)) {
            return false;
        };
        if ((n_0 <= 3)) {
            return true;
        };
        if ((_mod(n_0, 2) == 0)) {
            return false;
        };
        long i_1 = 3;
        while (((i_1 * i_1) <= n_0)) {
            if ((_mod(n_0, i_1) == 0)) {
                return false;
            }
            i_1 = (i_1 + 2);
        };
        return true;
    }

    public static bool is_germain_prime(long number_2) {
        if ((number_2 < 1)) {
            throw new Exception("Input value must be a positive integer");
        };
        return (Program.is_prime(number_2) && Program.is_prime(((2 * number_2) + 1)));
    }

    public static bool is_safe_prime(long number_3) {
        if ((number_3 < 1)) {
            throw new Exception("Input value must be a positive integer");
        };
        if ((_mod((number_3 - 1), 2) != 0)) {
            return false;
        };
        return (Program.is_prime(number_3) && Program.is_prime(_floordiv((number_3 - 1), 2)));
    }

    public static void test_is_germain_prime() {
        if ((!Program.is_germain_prime(3))) {
            throw new Exception("is_germain_prime(3) failed");
        };
        if ((!Program.is_germain_prime(11))) {
            throw new Exception("is_germain_prime(11) failed");
        };
        if (Program.is_germain_prime(4)) {
            throw new Exception("is_germain_prime(4) failed");
        };
        if ((!Program.is_germain_prime(23))) {
            throw new Exception("is_germain_prime(23) failed");
        };
        if (Program.is_germain_prime(13)) {
            throw new Exception("is_germain_prime(13) failed");
        };
        if (Program.is_germain_prime(20)) {
            throw new Exception("is_germain_prime(20) failed");
        };
    }

    public static void test_is_safe_prime() {
        if ((!Program.is_safe_prime(5))) {
            throw new Exception("is_safe_prime(5) failed");
        };
        if ((!Program.is_safe_prime(11))) {
            throw new Exception("is_safe_prime(11) failed");
        };
        if (Program.is_safe_prime(1)) {
            throw new Exception("is_safe_prime(1) failed");
        };
        if (Program.is_safe_prime(2)) {
            throw new Exception("is_safe_prime(2) failed");
        };
        if (Program.is_safe_prime(3)) {
            throw new Exception("is_safe_prime(3) failed");
        };
        if ((!Program.is_safe_prime(47))) {
            throw new Exception("is_safe_prime(47) failed");
        };
    }

    public static void main() {
        Program.test_is_germain_prime();
        Program.test_is_safe_prime();
        Console.WriteLine(Program._fmtTop(Program.is_germain_prime(23)));
        Console.WriteLine(Program._fmtTop(Program.is_safe_prime(47)));
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }
    }
}
