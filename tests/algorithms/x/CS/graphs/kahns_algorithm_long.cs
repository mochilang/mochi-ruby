// Generated by Mochi 0.10.66 (commit ce2d80d303) on 2025-08-15 15:17 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;
using System.Globalization;

#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    static long[][] graph_16 = new long[][]{new long[]{2, 3, 4}, new long[]{2, 7}, new long[]{5}, new long[]{5, 7}, new long[]{7}, new long[]{6}, new long[]{7}, new long[]{}};
    public static long longest_distance(long[][] graph_0) {
        long n_1 = graph_0.Length;
        long[] indegree_2 = new long[]{};
        long i_3 = 0;
        while ((i_3 < n_1)) {
            indegree_2 = ((Func<long[]>)(() => { var _tmp = indegree_2.ToList(); _tmp.Add(0); return _tmp.ToArray(); }))();
            i_3 = (i_3 + 1);
        };
        long[] long_dist_4 = new long[]{};
        long j_5 = 0;
        while ((j_5 < n_1)) {
            long_dist_4 = ((Func<long[]>)(() => { var _tmp = long_dist_4.ToList(); _tmp.Add(1); return _tmp.ToArray(); }))();
            j_5 = (j_5 + 1);
        };
        long u_6 = 0;
        while ((u_6 < n_1)) {
            foreach (long v_7 in _idx(graph_0, u_6)) {
                indegree_2[(int)(v_7)] = (_idx(indegree_2, v_7) + 1);
            }
            u_6 = (u_6 + 1);
        };
        long[] queue_8 = new long[]{};
        long head_9 = 0;
        long k_10 = 0;
        while ((k_10 < n_1)) {
            if ((_idx(indegree_2, k_10) == 0)) {
                queue_8 = ((Func<long[]>)(() => { var _tmp = queue_8.ToList(); _tmp.Add(k_10); return _tmp.ToArray(); }))();
            }
            k_10 = (k_10 + 1);
        };
        while ((head_9 < queue_8.Length)) {
            long vertex_11 = _idx(queue_8, head_9);
            head_9 = (head_9 + 1);
            foreach (long x_12 in _idx(graph_0, vertex_11)) {
                indegree_2[(int)(x_12)] = (_idx(indegree_2, x_12) - 1);
                long new_dist_13 = (_idx(long_dist_4, vertex_11) + 1);
                if ((new_dist_13 > _idx(long_dist_4, x_12))) {
                    long_dist_4[(int)(x_12)] = new_dist_13;
                }
                if ((_idx(indegree_2, x_12) == 0)) {
                    queue_8 = ((Func<long[]>)(() => { var _tmp = queue_8.ToList(); _tmp.Add(x_12); return _tmp.ToArray(); }))();
                }
            }
        };
        long max_len_14 = _idx(long_dist_4, 0);
        long m_15 = 1;
        while ((m_15 < n_1)) {
            if ((_idx(long_dist_4, m_15) > max_len_14)) {
                max_len_14 = _idx(long_dist_4, m_15);
            }
            m_15 = (m_15 + 1);
        };
        return max_len_14;
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(Program.longest_distance(graph_16)));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }
    }
}
