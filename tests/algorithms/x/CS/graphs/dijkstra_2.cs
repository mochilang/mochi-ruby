// Generated by Mochi 0.10.65 (commit 0d2a833829) on 2025-08-14 15:42 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;
using System.Globalization;

#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    static double INF_0 = 1000000000.0;
    public static void print_dist(double[] dist_1) {
        Console.WriteLine(Program._fmtTop("Vertex Distance"));
        long i_2 = 0;
        while ((i_2 < dist_1.Length)) {
            if ((_idx(dist_1, i_2) >= INF_0)) {
                Console.WriteLine(string.Join(" ", new string[]{Program._fmtTop(i_2), Program._fmtTop("\tINF")}));
            } else {
                Console.WriteLine(string.Join(" ", new string[]{Program._fmtTop(i_2), Program._fmtTop("\t"), Program._fmtTop((long)(_idx(dist_1, i_2)))}));
            }
            i_2 = (i_2 + 1);
        };
    }

    public static long min_dist(double[] mdist_3, bool[] vset_4) {
        double min_val_5 = INF_0;
        long min_ind_6 = -1;
        long i_7 = 0;
        while ((i_7 < mdist_3.Length)) {
            if (((!_idx(vset_4, i_7)) && (_idx(mdist_3, i_7) < min_val_5))) {
                min_val_5 = _idx(mdist_3, i_7);
                min_ind_6 = i_7;
            }
            i_7 = (i_7 + 1);
        };
        return min_ind_6;
    }

    public static double[] dijkstra(double[][] graph_8, long src_9) {
        long v_10 = graph_8.Length;
        double[] mdist_11 = new double[]{};
        bool[] vset_12 = new bool[]{};
        long i_13 = 0;
        while ((i_13 < v_10)) {
            mdist_11 = ((Func<double[]>)(() => { var _tmp = mdist_11.ToList(); _tmp.Add(INF_0); return _tmp.ToArray(); }))();
            vset_12 = ((Func<bool[]>)(() => { var _tmp = vset_12.ToList(); _tmp.Add(false); return _tmp.ToArray(); }))();
            i_13 = (i_13 + 1);
        };
        mdist_11[(int)(src_9)] = 0.0;
        long count_14 = 0;
        while ((count_14 < (v_10 - 1))) {
            long u_15 = Program.min_dist(mdist_11, vset_12);
            vset_12[(int)(u_15)] = true;
            long i_16 = 0;
            while ((i_16 < v_10)) {
                double alt_17 = (_idx(mdist_11, u_15) + _idx(_idx(graph_8, u_15), i_16));
                if ((((!_idx(vset_12, i_16)) && (_idx(_idx(graph_8, u_15), i_16) < INF_0)) && (alt_17 < _idx(mdist_11, i_16)))) {
                    mdist_11[(int)(i_16)] = alt_17;
                }
                i_16 = (i_16 + 1);
            }
            count_14 = (count_14 + 1);
        };
        return mdist_11;
    }

    public static void main() {
        double[][] graph_18 = new double[][]{new double[]{0.0, 10.0, INF_0, INF_0, 5.0}, new double[]{INF_0, 0.0, 1.0, INF_0, 2.0}, new double[]{INF_0, INF_0, 0.0, 4.0, INF_0}, new double[]{INF_0, INF_0, 6.0, 0.0, INF_0}, new double[]{INF_0, 3.0, 9.0, 2.0, 0.0}};
        double[] dist_19 = Program.dijkstra(graph_18, 0);
        Program.print_dist(dist_19);
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }
    }
}
