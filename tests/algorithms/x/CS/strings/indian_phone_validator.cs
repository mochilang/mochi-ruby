// Generated by Mochi 0.10.61 on 2025-08-08 18:01 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static bool all_digits(string s_0) {
        if ((s_0.Length == 0)) {
            return false;
        };
        long i_1 = 0;
        while ((i_1 < s_0.Length)) {
            string c_2 = s_0.Substring((int)(i_1), 1);
            if (((string.Compare(c_2, "0") < 0) || (string.Compare(c_2, "9") > 0))) {
                return false;
            }
            i_1 = (i_1 + 1);
        };
        return true;
    }

    public static bool indian_phone_validator(string phone_3) {
        string s_4 = phone_3;
        if (((s_4.Length >= 3) && (_substr(s_4, 0, 3) == "+91"))) {
            s_4 = _substr(s_4, 3, s_4.Length);
            if ((s_4.Length > 0)) {
                string c_5 = s_4.Substring((int)(0), 1);
                if (((c_5 == "-") || (c_5 == " "))) {
                    s_4 = _substr(s_4, 1, s_4.Length);
                }
            }
        };
        if (((s_4.Length > 0) && (s_4.Substring((int)(0), 1) == "0"))) {
            s_4 = _substr(s_4, 1, s_4.Length);
        };
        if (((s_4.Length >= 2) && (_substr(s_4, 0, 2) == "91"))) {
            s_4 = _substr(s_4, 2, s_4.Length);
        };
        if ((s_4.Length != 10)) {
            return false;
        };
        string first_6 = s_4.Substring((int)(0), 1);
        if ((!(((first_6 == "7") || (first_6 == "8")) || (first_6 == "9")))) {
            return false;
        };
        if ((!Program.all_digits(s_4))) {
            return false;
        };
        return true;
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.indian_phone_validator("+91123456789"))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.indian_phone_validator("+919876543210"))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.indian_phone_validator("01234567896"))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.indian_phone_validator("919876543218"))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.indian_phone_validator("+91-1234567899"))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.indian_phone_validator("+91-9876543218"))));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
