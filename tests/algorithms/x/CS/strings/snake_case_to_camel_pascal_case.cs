// Generated by Mochi 0.10.61 on 2025-08-08 18:01 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static long _len(object v) {
        if (v is Array a) return a.Length;
        if (v is string s) return s.Length;
        if (v is System.Collections.ICollection c) return c.Count;
        return Convert.ToString(v).Length;
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    public static string[] split(string s_0, string sep_1) {
        string[] res_2 = new string[]{};
        string current_3 = "";
        long i_4 = 0;
        while ((i_4 < s_0.Length)) {
            string ch_5 = _substr(s_0, i_4, (i_4 + 1));
            if ((ch_5 == sep_1)) {
                res_2 = (Enumerable.ToArray(Enumerable.Append<string>(res_2, current_3)));
                current_3 = "";
            } else {
                current_3 = (current_3 + ch_5);
            }
            i_4 = (i_4 + 1);
        };
        res_2 = (Enumerable.ToArray(Enumerable.Append<string>(res_2, current_3)));
        return res_2;
    }

    public static string capitalize(string word_6) {
        if ((word_6.Length == 0)) {
            return "";
        };
        string first_7 = _substr(word_6, 0, 1).ToUpper();
        string rest_8 = _substr(word_6, 1, word_6.Length);
        return (first_7 + rest_8);
    }

    public static string snake_to_camel_case(string input_str_9, bool use_pascal_10) {
        string[] words_11 = Program.split(input_str_9, "_");
        string result_12 = "";
        long index_13 = 0;
        if ((!use_pascal_10)) {
            if ((words_11.Length > 0)) {
                result_12 = words_11[(int)(0)];
                index_13 = 1;
            }
        };
        while ((index_13 < words_11.Length)) {
            string word_14 = words_11[(int)(index_13)];
            result_12 = (result_12 + Program.capitalize(word_14));
            index_13 = (index_13 + 1);
        };
        return result_12;
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(Program.snake_to_camel_case("some_random_string", false)));
            Console.WriteLine(Program._fmtTop(Program.snake_to_camel_case("some_random_string", true)));
            Console.WriteLine(Program._fmtTop(Program.snake_to_camel_case("some_random_string_with_numbers_123", false)));
            Console.WriteLine(Program._fmtTop(Program.snake_to_camel_case("some_random_string_with_numbers_123", true)));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
