// Generated by Mochi 0.10.65 (commit 7675ac6ff5) on 2025-08-14 09:57 +0700
#pragma warning disable 0169, 0649
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Collections;
using System.Globalization;

#pragma warning disable CS0162
class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            } else if (!string.IsNullOrEmpty(Environment.GetEnvironmentVariable("MOCHI_BENCHMARK"))) {
                nowSeed = 1170750916L;
                seededNow = true;
            }
        }
        var r = nowSeed;
        nowSeed++;
        return r;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static T _idx<T>(T[] arr, long i) {
        if (arr == null) return default(T);
        if (i < 0) i += arr.Length;
        if (i < 0 || i >= arr.Length) return default(T);
        return arr[(int)i];
    }
    static string _substr(string s, long start, long end) {
        if (start < 0) start = 0;
        if (end < 0) end = 0;
        if (start > s.Length) start = s.Length;
        if (end > s.Length) end = s.Length;
        if (start > end) start = end;
        return s.Substring((int)start, (int)(end - start));
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(", ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(", ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    static string _fmtStr(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmtStr(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmtStr(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmtStr(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return "\"" + s.Replace("\"", "\\\"") + "\"";
        if (v is bool b) return b ? "true" : "false";
        if (v is double dv) return dv.ToString("R", CultureInfo.InvariantCulture);
        if (v is float fv) return fv.ToString("R", CultureInfo.InvariantCulture);
        return Convert.ToString(v, CultureInfo.InvariantCulture);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is string s) return s;
        return _fmt(v);
    }
    static string __name__ = "__main__";
    public static double[] straight_line_depreciation(long useful_years_0, double purchase_value_1, double residual_value_2) {
        if ((useful_years_0 < 1)) {
            throw new Exception("Useful years cannot be less than 1");
        };
        if ((purchase_value_1 < 0.0)) {
            throw new Exception("Purchase value cannot be less than zero");
        };
        if ((purchase_value_1 < residual_value_2)) {
            throw new Exception("Purchase value cannot be less than residual value");
        };
        double depreciable_cost_3 = (purchase_value_1 - residual_value_2);
        double annual_expense_4 = (depreciable_cost_3 / (1.0 * useful_years_0));
        double[] expenses_5 = new double[]{};
        double accumulated_6 = 0.0;
        long period_7 = 0;
        while ((period_7 < useful_years_0)) {
            if ((period_7 != (useful_years_0 - 1))) {
                accumulated_6 = (accumulated_6 + annual_expense_4);
                expenses_5 = ((Func<double[]>)(() => { var _tmp = expenses_5.ToList(); _tmp.Add(annual_expense_4); return _tmp.ToArray(); }))();
            } else {
                double end_year_expense_8 = (depreciable_cost_3 - accumulated_6);
                expenses_5 = ((Func<double[]>)(() => { var _tmp = expenses_5.ToList(); _tmp.Add(end_year_expense_8); return _tmp.ToArray(); }))();
            }
            period_7 = (period_7 + 1);
        };
        return expenses_5;
    }

    static void Main() {
        _ = __name__;
        {
            var __memStart = _mem();
            var __start = _now();
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.straight_line_depreciation(10, 1100.0, 100.0))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.straight_line_depreciation(6, 1250.0, 50.0))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.straight_line_depreciation(4, 1001.0, 0.0))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.straight_line_depreciation(11, 380.0, 50.0))));
            Console.WriteLine(Program._fmtTop(_fmtStr(Program.straight_line_depreciation(1, 4985.0, 100.0))));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            var __bench = new Dictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}};
            Console.WriteLine(JsonSerializer.Serialize(__bench));
        }
    }
}
