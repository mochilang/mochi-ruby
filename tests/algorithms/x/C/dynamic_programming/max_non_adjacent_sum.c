// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long maximum_non_adjacent_sum(long long * nums, size_t nums_len);
int main(void);

long long maximum_non_adjacent_sum(long long * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        return 0LL;
    }
    long long max_including = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    long long max_excluding = 0LL;
    long long i = 1LL;
    while (i < nums_len) {
        long long num = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        long long new_including = max_excluding + num;
        long long new_excluding = (max_including > max_excluding ? max_including : max_excluding);
        max_including = new_including;
        max_excluding = new_excluding;
        i = i + 1LL;
    }
    if (max_including > max_excluding) {
        return max_including;
    }
    return max_excluding;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(maximum_non_adjacent_sum(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3)));
        puts(str_int(maximum_non_adjacent_sum(({long long *tmp = malloc(7 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 5LL; tmp[2] = 3LL; tmp[3] = 7LL; tmp[4] = 2LL; tmp[5] = 2LL; tmp[6] = 6LL; tmp;}), 7)));
        puts(str_int(maximum_non_adjacent_sum(({double *tmp = malloc(7 * sizeof(double)); tmp[0] = -1.0; tmp[1] = -5.0; tmp[2] = -3.0; tmp[3] = -7.0; tmp[4] = -2.0; tmp[5] = -2.0; tmp[6] = -6.0; tmp;}), 7)));
        puts(str_int(maximum_non_adjacent_sum(({long long *tmp = malloc(7 * sizeof(long long)); tmp[0] = 499LL; tmp[1] = 500LL; tmp[2] = -3.0; tmp[3] = -7.0; tmp[4] = -2.0; tmp[5] = -2.0; tmp[6] = -6.0; tmp;}), 7)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
