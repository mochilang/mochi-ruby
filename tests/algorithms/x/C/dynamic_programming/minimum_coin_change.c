// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long dp_count(long long * s, size_t s_len, long long n);
int main(void);

long long dp_count(long long * s, size_t s_len, long long n) {
    if (n < 0LL) {
        return 0LL;
    }
    long long *table = NULL;
    size_t table_len = 0;
    long long i = 0LL;
    while (i <= n) {
        table = list_append_long_long(table, &table_len, 0LL);
        i = i + 1LL;
    }
    table[(int)(0LL)] = 1LL;
    long long idx = 0LL;
    while (idx < s_len) {
        long long coin_val = s[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? s_len + _mochi_idx : _mochi_idx;})];
        long long j = coin_val;
        while (j <= n) {
            table[(int)(j)] = table[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})] + table[(int)({long long _mochi_idx = j - coin_val; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        idx = idx + 1LL;
    }
    return table[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", dp_count(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3, 4LL));
        printf("%d\n", dp_count(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3, 7LL));
        printf("%d\n", dp_count(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 5LL; tmp[2] = 3LL; tmp[3] = 6LL; tmp;}), 4, 10LL));
        printf("%d\n", dp_count(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 10LL; tmp;}), 1, 99LL));
        printf("%d\n", dp_count(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 5LL; tmp[2] = 6LL; tmp;}), 3, 0LL));
        printf("%d\n", dp_count(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3, -5.0));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
