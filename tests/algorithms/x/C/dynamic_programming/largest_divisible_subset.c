// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_list_len;
size_t largest_divisible_subset_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * sort_list(long long * nums, size_t nums_len);
long long * largest_divisible_subset(long long * items, size_t items_len);
void user_main();
int main(void);

long long * sort_list(long long * nums, size_t nums_len) {
    long long *arr = nums;
    size_t arr_len = nums_len;
    long long i = 1LL;
    while (i < arr_len) {
        long long key = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > key)) {
            arr[(int)(j + 1LL)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        arr[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return sort_list_len = arr_len, arr;
}

long long * largest_divisible_subset(long long * items, size_t items_len) {
    if (items_len == 0LL) {
        {
            long long* __tmp0_data = malloc(0 * sizeof(long long));
            largest_divisible_subset_len = 0;
            return __tmp0_data;
        }
    }
    long long *nums = sort_list(items, items_len);
    size_t nums_len = sort_list_len;
    long long n = nums_len;
    long long *memo = NULL;
    size_t memo_len = 0;
    long long *prev = NULL;
    size_t prev_len = 0;
    long long i = 0LL;
    while (i < n) {
        memo = list_append_long_long(memo, &memo_len, 1LL);
        prev = list_append_long_long(prev, &prev_len, i);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j < i) {
            if (((nums[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] == 0LL) || ((nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] % nums[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]) == 0LL)) && ((memo[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})] + 1LL) > memo[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})])) {
                memo[(int)(i)] = memo[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})] + 1LL;
                prev[(int)(i)] = j;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    long long ans = -1LL;
    long long last_index = -1LL;
    i = 0LL;
    while (i < n) {
        if (memo[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})] > ans) {
            ans = memo[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})];
            last_index = i;
        }
        i = i + 1LL;
    }
    if (last_index == (0LL - 1LL)) {
        {
            long long* __tmp1_data = malloc(0 * sizeof(long long));
            largest_divisible_subset_len = 0;
            return __tmp1_data;
        }
    }
    long long *result = NULL;
    size_t result_len = 0;
    result = list_append_long_long(result, &result_len, nums[(int)({long long _mochi_idx = last_index; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]);
    while (prev[(int)({long long _mochi_idx = last_index; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})] != last_index) {
        last_index = prev[(int)({long long _mochi_idx = last_index; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})];
        result = list_append_long_long(result, &result_len, nums[(int)({long long _mochi_idx = last_index; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]);
    }
    return largest_divisible_subset_len = result_len, result;
}

void user_main() {
    long long *items = NULL;
    size_t items_len = 0;
    items = list_append_long_long(items, &items_len, 1LL);
    items = list_append_long_long(items, &items_len, 16LL);
    items = list_append_long_long(items, &items_len, 7LL);
    items = list_append_long_long(items, &items_len, 8LL);
    items = list_append_long_long(items, &items_len, 4LL);
    long long *subset = largest_divisible_subset(items, items_len);
    size_t subset_len = largest_divisible_subset_len;
    puts(str_concat(str_concat(str_concat(str_concat("The longest divisible subset of ", str_list_int(items, items_len)), " is "), str_list_int(subset, subset_len)), "."));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
