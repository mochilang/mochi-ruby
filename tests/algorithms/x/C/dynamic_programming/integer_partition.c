// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long partition(long long m);
int main(void);

long long partition(long long m) {
    long long **memo = NULL;
    size_t memo_len = 0;
    size_t *memo_lens = NULL;
    size_t memo_lens_len = 0;
    long long i = 0LL;
    while (i < (m + 1LL)) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        memo = list_append_intptr(memo, &memo_len, row);
        memo_lens = list_append_szt(memo_lens, &memo_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < (m + 1LL)) {
        memo[(int)(i)][(int)(0LL)] = 1LL;
        i = i + 1LL;
    }
    long long n = 0LL;
    while (n < (m + 1LL)) {
        long long k = 1LL;
        while (k < m) {
            memo[(int)(n)][(int)(k)] = memo[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? memo_lens[(int)(n)] + _mochi_idx : _mochi_idx;})] + memo[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k - 1LL; _mochi_idx < 0 ? memo_lens[(int)(n)] + _mochi_idx : _mochi_idx;})];
            if ((n - k) > 0LL) {
                memo[(int)(n)][(int)(k)] = memo[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? memo_lens[(int)(n)] + _mochi_idx : _mochi_idx;})] + memo[(int)({long long _mochi_idx = (n - k) - 1LL; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? memo_lens[(int)((n - k) - 1LL)] + _mochi_idx : _mochi_idx;})];
            }
            k = k + 1LL;
        }
        n = n + 1LL;
    }
    return memo[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? memo_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = m - 1LL; _mochi_idx < 0 ? memo_lens[(int)(m)] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", partition(5LL));
        printf("%d\n", partition(7LL));
        printf("%d\n", partition(100LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
