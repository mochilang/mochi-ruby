// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_list_len;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * make_list(long long len, long long value);
long long min_int(long long a, long long b);
long long min_steps_to_one(long long number);
int main(void);

long long * make_list(long long len, long long value) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < len) {
        arr = list_append_long_long(arr, &arr_len, value);
        i = i + 1LL;
    }
    return make_list_len = arr_len, arr;
}

long long min_int(long long a, long long b) {
    if (a < b) {
        return a;
    }
    return b;
}

long long min_steps_to_one(long long number) {
    if (number <= 0LL) {
        return 0LL;
    }
    long long *table = make_list(number + 1LL, number + 1LL);
    size_t table_len = make_list_len;
    table[(int)(1LL)] = 0LL;
    long long i = 1LL;
    while (i < number) {
        table[(int)(i + 1LL)] = min_int(table[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})], table[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})] + 1LL);
        if ((i * 2LL) <= number) {
            table[(int)(i * 2LL)] = min_int(table[(int)({long long _mochi_idx = i * 2LL; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})], table[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})] + 1LL);
        }
        if ((i * 3LL) <= number) {
            table[(int)(i * 3LL)] = min_int(table[(int)({long long _mochi_idx = i * 3LL; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})], table[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})] + 1LL);
        }
        i = i + 1LL;
    }
    return table[(int)({long long _mochi_idx = number; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(min_steps_to_one(10LL)));
        puts(str_int(min_steps_to_one(15LL)));
        puts(str_int(min_steps_to_one(6LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
