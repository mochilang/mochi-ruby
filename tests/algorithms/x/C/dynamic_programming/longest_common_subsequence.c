// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t zeros_matrix_len;
size_t *zeros_matrix_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LcsResult LcsResult;

struct LcsResult {
    long long length;
    const char* sequence;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* a = "AGGTAB";
const char* b = "GXTXAYB";
LcsResult res;

long long * * zeros_matrix(long long rows, long long cols);
LcsResult longest_common_subsequence(const char* x, const char* y);
int main(void);

long long * * zeros_matrix(long long rows, long long cols) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    long long i = 0LL;
    while (i <= rows) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= cols) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
        i = i + 1LL;
    }
    return zeros_matrix_lens = matrix_lens, zeros_matrix_len = matrix_len, matrix;
}

LcsResult longest_common_subsequence(const char* x, const char* y) {
    long long m = strlen(x);
    long long n = strlen(y);
    long long **dp = zeros_matrix(m, n);
    size_t dp_len = zeros_matrix_len;
    size_t *dp_lens = zeros_matrix_lens;
    size_t dp_lens_len = zeros_matrix_len;
    long long i = 1LL;
    while (i <= m) {
        long long j = 1LL;
        while (j <= n) {
            if (strcmp((const char[]){x[i - 1LL], 0}, (const char[]){y[j - 1LL], 0}) == 0) {
                dp[(int)(i)][(int)(j)] = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + 1LL;
            } else {
                if (dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] > dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
                    dp[(int)(i)][(int)(j)] = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                } else {
                    dp[(int)(i)][(int)(j)] = dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    const char* seq = "";
    long long i2 = m;
    long long j2 = n;
    while ((i2 > 0LL) && (j2 > 0LL)) {
        if (strcmp((const char[]){x[i2 - 1LL], 0}, (const char[]){y[j2 - 1LL], 0}) == 0) {
            seq = str_concat((const char[]){x[i2 - 1LL], 0}, seq);
            i2 = i2 - 1LL;
            j2 = j2 - 1LL;
        } else {
            if (dp[(int)({long long _mochi_idx = i2 - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2; _mochi_idx < 0 ? dp_lens[(int)(i2 - 1LL)] + _mochi_idx : _mochi_idx;})] >= dp[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2 - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i2)] + _mochi_idx : _mochi_idx;})]) {
                i2 = i2 - 1LL;
            } else {
                j2 = j2 - 1LL;
            }
        }
    }
    return (LcsResult){.length = dp[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_lens[(int)(m)] + _mochi_idx : _mochi_idx;})], .sequence = seq};
}

int main(void) {
    {
        long long __start = _now();
        res = longest_common_subsequence(a, b);
        puts(str_concat(str_concat(str_concat("len = ", str_int(res.length)), ", sub-sequence = "), res.sequence));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
