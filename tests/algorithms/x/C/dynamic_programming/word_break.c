// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

typedef struct MapSI { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

MapSI build_set(const char* * words, size_t words_len);
long long word_break(const char* s, const char* * words, size_t words_len);
void print_bool(long long b);
int main(void);

MapSI build_set(const char* * words, size_t words_len) {
    static const char* m_keys[16] = {};
    static int m_vals[16] = {};
    size_t m_len = 0;
    MapSI m = { m_keys, m_vals, 0, 16 };
    for (size_t __i = 0; __i < words_len; __i++) {
        const char* w = words[__i];
        map_set_si(&(m), w, 1LL);
    }
    return m;
}

long long word_break(const char* s, const char* * words, size_t words_len) {
    long long n = strlen(s);
    MapSI dict = build_set(words, words_len);
    long long *dp = NULL;
    size_t dp_len = 0;
    long long i = 0LL;
    while (i <= n) {
        dp = list_append_long_long(dp, &dp_len, 0LL);
        i = i + 1LL;
    }
    dp[(int)(0LL)] = 1LL;
    i = 1LL;
    while (i <= n) {
        long long j = 0LL;
        while (j < i) {
            if (dp[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})]) {
                const char* sub = _substring(s, j, i);
                if (contains_str(dict.keys, dict.len, sub)) {
                    dp[(int)(i)] = 1LL;
                    j = i;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return dp[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        print_bool(word_break("applepenapple", ({const char* *tmp = malloc(2 * sizeof(const char*)); tmp[0] = "apple"; tmp[1] = "pen"; tmp;}), 2));
        print_bool(word_break("catsandog", ({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = "cats"; tmp[1] = "dog"; tmp[2] = "sand"; tmp[3] = "and"; tmp[4] = "cat"; tmp;}), 5));
        print_bool(word_break("cars", ({const char* *tmp = malloc(3 * sizeof(const char*)); tmp[0] = "car"; tmp[1] = "ca"; tmp[2] = "rs"; tmp;}), 3));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
