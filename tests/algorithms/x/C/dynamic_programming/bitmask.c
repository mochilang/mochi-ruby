// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_int(const int arr[], size_t len, int val) {
    for (size_t i = 0; i < len; i++) {
        if (arr[i] == val) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long count_assignments(long long person, long long * * task_performed, size_t task_performed_len, size_t* task_performed_lens, size_t task_performed_lens_len, long long * used, size_t used_len);
long long count_no_of_ways(long long * * task_performed, size_t task_performed_len, size_t* task_performed_lens, size_t task_performed_lens_len);
void user_main();
int main(void);

long long count_assignments(long long person, long long * * task_performed, size_t task_performed_len, size_t* task_performed_lens, size_t task_performed_lens_len, long long * used, size_t used_len) {
    if (person == task_performed_len) {
        return 1LL;
    }
    long long total = 0LL;
    long long *tasks = task_performed[(int)({long long _mochi_idx = person; _mochi_idx < 0 ? task_performed_len + _mochi_idx : _mochi_idx;})];
    size_t tasks_len = task_performed_lens[(int)(person)];
    long long i = 0LL;
    while (i < tasks_len) {
        long long t = tasks[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? tasks_len + _mochi_idx : _mochi_idx;})];
        if (!(contains_int(used, used_len, t))) {
            total = total + ({long long* __tmp1 = list_append_int_new(used, used_len, t); size_t __tmp1_len = append_len; count_assignments(person + 1LL, task_performed, task_performed_len, task_performed_lens, task_performed_len, __tmp1, __tmp1_len);});
        }
        i = i + 1LL;
    }
    return total;
}

long long count_no_of_ways(long long * * task_performed, size_t task_performed_len, size_t* task_performed_lens, size_t task_performed_lens_len) {
    return count_assignments(0LL, task_performed, task_performed_len, task_performed_lens, task_performed_len, NULL, 0);
}

void user_main() {
    long long task_performed_0[3] = {1LL, 3LL, 4LL};
    long long task_performed_1[3] = {1LL, 2LL, 5LL};
    long long task_performed_2[2] = {3LL, 4LL};
    long long *task_performed_init[3] = {task_performed_0, task_performed_1, task_performed_2};
    long long **task_performed = task_performed_init;
    size_t task_performed_len = 3;
    size_t task_performed_lens_init[3] = {3, 3, 2};
    size_t *task_performed_lens = task_performed_lens_init;
    size_t task_performed_lens_len = 3;
    puts(str_int(count_no_of_ways(task_performed, task_performed_len, task_performed_lens, task_performed_len)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
