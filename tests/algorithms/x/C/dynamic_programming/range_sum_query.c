// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prefix_sum_len;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Query Query;

struct Query {
    long long left;
    long long right;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *arr1 = NULL;
size_t arr1_len = 0;
Query *queries1 = NULL;
size_t queries1_len = 0;
long long *arr2 = NULL;
size_t arr2_len = 0;
Query *queries2 = NULL;
size_t queries2_len = 0;

long long * prefix_sum(long long * arr, size_t arr_len, Query * queries, size_t queries_len);
int main(void);

long long * prefix_sum(long long * arr, size_t arr_len, Query * queries, size_t queries_len) {
    long long *dp = NULL;
    size_t dp_len = 0;
    long long i = 0LL;
    while (i < arr_len) {
        if (i == 0LL) {
            dp = list_append_long_long(dp, &dp_len, arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        } else {
            dp = list_append_long_long(dp, &dp_len, dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})] + arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    long long j = 0LL;
    while (j < queries_len) {
        Query q = queries[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? queries_len + _mochi_idx : _mochi_idx;})];
        long long sum = dp[(int)({long long _mochi_idx = q.right; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
        if (q.left > 0LL) {
            sum = sum - dp[(int)({long long _mochi_idx = q.left - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
        }
        result = list_append_long_long(result, &result_len, sum);
        j = j + 1LL;
    }
    return prefix_sum_len = result_len, result;
}

int main(void) {
    {
        long long __start = _now();
        arr1 = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 4LL; tmp[2] = 6LL; tmp[3] = 2LL; tmp[4] = 61LL; tmp[5] = 12LL; tmp;});
        arr1_len = 6;
        queries1 = ({Query *tmp = malloc(3 * sizeof(Query)); tmp[0] = (Query){.left = 2LL, .right = 5LL}; tmp[1] = (Query){.left = 1LL, .right = 5LL}; tmp[2] = (Query){.left = 3LL, .right = 4LL}; tmp;});
        queries1_len = 3;
        puts(({long long *__tmp2 = prefix_sum(arr1, arr1_len, queries1, queries1_len); char *__res = str_list_int(__tmp2, prefix_sum_len); __res;}));
        arr2 = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 2LL; tmp[2] = 1LL; tmp[3] = 6LL; tmp[4] = 3LL; tmp;});
        arr2_len = 5;
        queries2 = ({Query *tmp = malloc(3 * sizeof(Query)); tmp[0] = (Query){.left = 3LL, .right = 4LL}; tmp[1] = (Query){.left = 1LL, .right = 3LL}; tmp[2] = (Query){.left = 0LL, .right = 2LL}; tmp;});
        queries2_len = 3;
        puts(({long long *__tmp3 = prefix_sum(arr2, arr2_len, queries2, queries2_len); char *__res = str_list_int(__tmp3, prefix_sum_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
