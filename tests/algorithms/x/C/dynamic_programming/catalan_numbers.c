// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t catalan_numbers_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

void user_panic(const char* msg);
long long * catalan_numbers(long long upper_limit);
int main(void);

void user_panic(const char* msg) {
    puts(msg);
}

long long * catalan_numbers(long long upper_limit) {
    if (upper_limit < 0LL) {
        user_panic("Limit for the Catalan sequence must be >= 0");
        {
            long long* __tmp2_data = malloc(0 * sizeof(long long));
            catalan_numbers_len = 0;
            return __tmp2_data;
        }
    }
    long long *catalans = NULL;
    size_t catalans_len = 0;
    catalans = list_append_long_long(catalans, &catalans_len, 1LL);
    long long n = 1LL;
    while (n <= upper_limit) {
        long long next_val = 0LL;
        long long j = 0LL;
        while (j < n) {
            next_val = next_val + (catalans[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? catalans_len + _mochi_idx : _mochi_idx;})] * catalans[(int)({long long _mochi_idx = (n - j) - 1LL; _mochi_idx < 0 ? catalans_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        catalans = list_append_long_long(catalans, &catalans_len, next_val);
        n = n + 1LL;
    }
    return catalan_numbers_len = catalans_len, catalans;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = catalan_numbers(5LL); char *__res = str_list_int(__tmp3, catalan_numbers_len); __res;}));
        puts(({long long *__tmp4 = catalan_numbers(2LL); char *__res = str_list_int(__tmp4, catalan_numbers_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
