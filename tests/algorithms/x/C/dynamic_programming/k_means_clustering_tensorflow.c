// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t mean_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct KMeansResult KMeansResult;

struct KMeansResult {
    double **centroids;
    size_t centroids_len;
    size_t *centroids_lens;
    size_t centroids_lens_len;
    long long *assignments;
    size_t assignments_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double distance_sq(double * a, size_t a_len, double * b, size_t b_len);
double * mean(double * * vectors, size_t vectors_len, size_t* vectors_lens, size_t vectors_lens_len);
KMeansResult k_means(double * * vectors, size_t vectors_len, size_t* vectors_lens, size_t vectors_lens_len, long long k, long long iterations);
void user_main();
int main(void);

double distance_sq(double * a, size_t a_len, double * b, size_t b_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < a_len) {
        double diff = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})];
        sum = sum + (diff * diff);
        i = i + 1LL;
    }
    return sum;
}

double * mean(double * * vectors, size_t vectors_len, size_t* vectors_lens, size_t vectors_lens_len) {
    long long dim = vectors_lens[(int)(0LL)];
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < dim) {
        double total = 0.0;
        long long j = 0LL;
        while (j < vectors_len) {
            total = total + vectors[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? vectors_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vectors_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        res = list_append_double(res, &res_len, total / vectors_len);
        i = i + 1LL;
    }
    return mean_len = res_len, res;
}

KMeansResult k_means(double * * vectors, size_t vectors_len, size_t* vectors_lens, size_t vectors_lens_len, long long k, long long iterations) {
    double **centroids = NULL;
    size_t centroids_len = 0;
    size_t *centroids_lens = NULL;
    size_t centroids_lens_len = 0;
    long long i = 0LL;
    while (i < k) {
        centroids = list_append_doubleptr(centroids, &centroids_len, vectors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vectors_len + _mochi_idx : _mochi_idx;})]);
        centroids_lens = list_append_szt(centroids_lens, &centroids_lens_len, vectors_lens[(int)(i)]);
        i = i + 1LL;
    }
    long long *assignments = NULL;
    size_t assignments_len = 0;
    long long n = vectors_len;
    i = 0LL;
    while (i < n) {
        assignments = list_append_long_long(assignments, &assignments_len, 0LL);
        i = i + 1LL;
    }
    long long it = 0LL;
    while (it < iterations) {
        long long v = 0LL;
        while (v < n) {
            long long best = 0LL;
            double bestDist = distance_sq(vectors[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? vectors_len + _mochi_idx : _mochi_idx;})], vectors_lens[(int)(v)], centroids[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? centroids_len + _mochi_idx : _mochi_idx;})], centroids_lens[(int)(0LL)]);
            long long c = 1LL;
            while (c < k) {
                double d = distance_sq(vectors[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? vectors_len + _mochi_idx : _mochi_idx;})], vectors_lens[(int)(v)], centroids[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? centroids_len + _mochi_idx : _mochi_idx;})], centroids_lens[(int)(c)]);
                if (d < bestDist) {
                    bestDist = d;
                    best = c;
                }
                c = c + 1LL;
            }
            assignments[(int)(v)] = best;
            v = v + 1LL;
        }
        long long cIdx = 0LL;
        while (cIdx < k) {
            double **cluster = NULL;
            size_t cluster_len = 0;
            size_t *cluster_lens = NULL;
            size_t cluster_lens_len = 0;
            long long v2 = 0LL;
            while (v2 < n) {
                if (assignments[(int)({long long _mochi_idx = v2; _mochi_idx < 0 ? assignments_len + _mochi_idx : _mochi_idx;})] == cIdx) {
                    cluster = list_append_doubleptr(cluster, &cluster_len, vectors[(int)({long long _mochi_idx = v2; _mochi_idx < 0 ? vectors_len + _mochi_idx : _mochi_idx;})]);
                    cluster_lens = list_append_szt(cluster_lens, &cluster_lens_len, vectors_lens[(int)(v2)]);
                }
                v2 = v2 + 1LL;
            }
            if (cluster_len > 0LL) {
                centroids[(int)(cIdx)] = mean(cluster, cluster_len, cluster_lens, cluster_len);
            }
            cIdx = cIdx + 1LL;
        }
        it = it + 1LL;
    }
    return (KMeansResult){.centroids = centroids, .centroids_len = centroids_len, .centroids_lens = centroids_lens, .centroids_lens_len = centroids_len, .assignments = assignments, .assignments_len = assignments_len};
}

void user_main() {
    double vectors_0[2] = {1.0, 2.0};
    double vectors_1[2] = {1.5, 1.8};
    double vectors_2[2] = {5.0, 8.0};
    double vectors_3[2] = {8.0, 8.0};
    double vectors_4[2] = {1.0, 0.6};
    double vectors_5[2] = {9.0, 11.0};
    double *vectors_init[6] = {vectors_0, vectors_1, vectors_2, vectors_3, vectors_4, vectors_5};
    double **vectors = vectors_init;
    size_t vectors_len = 6;
    size_t vectors_lens_init[6] = {2, 2, 2, 2, 2, 2};
    size_t *vectors_lens = vectors_lens_init;
    size_t vectors_lens_len = 6;
    KMeansResult result = k_means(vectors, vectors_len, vectors_lens, vectors_len, 2LL, 5LL);
    puts(str_list_list_double(result.centroids, result.centroids_len, result.centroids_lens));
    puts(str_list_int(result.assignments, result.assignments_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
