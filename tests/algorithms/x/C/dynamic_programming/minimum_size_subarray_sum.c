// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long minimum_subarray_sum(long long target, long long * numbers, size_t numbers_len);
int main(void);

long long minimum_subarray_sum(long long target, long long * numbers, size_t numbers_len) {
    long long n = numbers_len;
    if (n == 0LL) {
        return 0LL;
    }
    if (target == 0LL) {
        long long i = 0LL;
        while (i < n) {
            if (numbers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})] == 0LL) {
                return 0LL;
            }
            i = i + 1LL;
        }
    }
    long long left = 0LL;
    long long right = 0LL;
    long long curr_sum = 0LL;
    long long min_len = n + 1LL;
    while (right < n) {
        curr_sum = curr_sum + numbers[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
        while ((curr_sum >= target) && (left <= right)) {
            long long current_len = (right - left) + 1LL;
            if (current_len < min_len) {
                min_len = current_len;
            }
            curr_sum = curr_sum - numbers[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
            left = left + 1LL;
        }
        right = right + 1LL;
    }
    if (min_len == (n + 1LL)) {
        return 0LL;
    }
    return min_len;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(minimum_subarray_sum(7LL, ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = 1LL; tmp[3] = 2LL; tmp[4] = 4LL; tmp[5] = 3LL; tmp;}), 6)));
        puts(str_int(minimum_subarray_sum(7LL, ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = -1.0; tmp[3] = 2LL; tmp[4] = 4LL; tmp[5] = -3.0; tmp;}), 6)));
        puts(str_int(minimum_subarray_sum(11LL, ({long long *tmp = malloc(8 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = 1LL; tmp[3] = 1LL; tmp[4] = 1LL; tmp[5] = 1LL; tmp[6] = 1LL; tmp[7] = 1LL; tmp;}), 8)));
        puts(str_int(minimum_subarray_sum(0LL, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
