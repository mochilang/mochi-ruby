// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t copy_list_len;
size_t longest_subsequence_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * copy_list(long long * xs, size_t xs_len);
long long * longest_subsequence(long long * arr, size_t arr_len);
void user_main();
int main(void);

long long * copy_list(long long * xs, size_t xs_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return copy_list_len = res_len, res;
}

long long * longest_subsequence(long long * arr, size_t arr_len) {
    long long n = arr_len;
    long long **lis = NULL;
    size_t lis_len = 0;
    size_t *lis_lens = NULL;
    size_t lis_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *single = NULL;
        size_t single_len = 0;
        single = list_append_long_long(single, &single_len, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        lis = list_append_intptr(lis, &lis_len, single);
        lis_lens = list_append_szt(lis_lens, &lis_lens_len, single_len);
        i = i + 1LL;
    }
    i = 1LL;
    while (i < n) {
        long long prev = 0LL;
        while (prev < i) {
            if ((arr[(int)({long long _mochi_idx = prev; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] <= arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) && ((lis_lens[(int)(prev)] + 1LL) > lis_lens[(int)(i)])) {
                long long *temp = copy_list(lis[(int)({long long _mochi_idx = prev; _mochi_idx < 0 ? lis_len + _mochi_idx : _mochi_idx;})], lis_lens[(int)(prev)]);
                size_t temp_len = copy_list_len;
                long long *temp2 = list_append_int_new(temp, temp_len, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
                size_t temp2_len = temp_len + 1;
                lis[(int)(i)] = temp2;
            }
            prev = prev + 1LL;
        }
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    i = 0LL;
    while (i < n) {
        if (lis_lens[(int)(i)] > result_len) {
            result = lis[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lis_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return longest_subsequence_len = result_len, result;
}

void user_main() {
    puts(({long long *__tmp6 = longest_subsequence(({long long *tmp = malloc(9 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 22LL; tmp[2] = 9LL; tmp[3] = 33LL; tmp[4] = 21LL; tmp[5] = 50LL; tmp[6] = 41LL; tmp[7] = 60LL; tmp[8] = 80LL; tmp;}), 9); char *__res = str_list_int(__tmp6, longest_subsequence_len); __res;}));
    puts(({long long *__tmp7 = longest_subsequence(({long long *tmp = malloc(9 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 8LL; tmp[2] = 7LL; tmp[3] = 5LL; tmp[4] = 1LL; tmp[5] = 12LL; tmp[6] = 2LL; tmp[7] = 3LL; tmp[8] = 9LL; tmp;}), 9); char *__res = str_list_int(__tmp7, longest_subsequence_len); __res;}));
    puts(({long long *__tmp8 = longest_subsequence(({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 9LL; tmp[1] = 8LL; tmp[2] = 7LL; tmp[3] = 6LL; tmp[4] = 5LL; tmp[5] = 7LL; tmp;}), 6); char *__res = str_list_int(__tmp8, longest_subsequence_len); __res;}));
    puts(({long long *__tmp9 = longest_subsequence(({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 28LL; tmp[1] = 26LL; tmp[2] = 12LL; tmp[3] = 23LL; tmp[4] = 35LL; tmp[5] = 39LL; tmp;}), 6); char *__res = str_list_int(__tmp9, longest_subsequence_len); __res;}));
    puts(({long long *__tmp10 = longest_subsequence(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = 1LL; tmp;}), 3); char *__res = str_list_int(__tmp10, longest_subsequence_len); __res;}));
    puts(({long long *__tmp11 = longest_subsequence(NULL, 0); char *__res = str_list_int(__tmp11, longest_subsequence_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
