// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t init_f_len;
size_t *init_f_lens;
size_t create_matrix_len;
size_t *create_matrix_lens;
size_t knapsack_len;
size_t *knapsack_lens;
size_t construct_solution_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct KnapsackResult KnapsackResult;

struct KnapsackResult {
    long long value;
    long long *subset;
    size_t subset_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **f = NULL;
size_t f_len = 0;
size_t *f_lens = NULL;
size_t f_lens_len = 0;
long long *val_list = NULL;
size_t val_list_len = 0;
long long *wt_list = NULL;
size_t wt_list_len = 0;
long long n = 4LL;
long long w_cap = 6LL;
long long **dp_table = NULL;
size_t dp_table_len = 0;
size_t *dp_table_lens = NULL;
size_t dp_table_lens_len = 0;
long long optimal_solution = 0;
KnapsackResult example;

long long max_int(long long a, long long b);
long long * * init_f(long long n, long long w);
long long mf_knapsack(long long i, long long * wt, size_t wt_len, long long * val, size_t val_len, long long j);
long long * * create_matrix(long long rows, long long cols);
long long * * knapsack(long long w, long long * wt, size_t wt_len, long long * val, size_t val_len, long long n);
long long * construct_solution(long long * * dp, size_t dp_len, size_t* dp_lens, size_t dp_lens_len, long long * wt, size_t wt_len, long long i, long long j, long long * optimal_set, size_t optimal_set_len);
KnapsackResult knapsack_with_example_solution(long long w, long long * wt, size_t wt_len, long long * val, size_t val_len);
const char* format_set(long long * xs, size_t xs_len);
int main(void);

long long max_int(long long a, long long b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

long long * * init_f(long long n, long long w) {
    long long **table = NULL;
    size_t table_len = 0;
    size_t *table_lens = NULL;
    size_t table_lens_len = 0;
    long long i = 0LL;
    while (i <= n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= w) {
            if ((i == 0LL) || (j == 0LL)) {
                row = list_append_long_long(row, &row_len, 0LL);
            } else {
                row = list_append_long_long(row, &row_len, -1LL);
            }
            j = j + 1LL;
        }
        table = list_append_intptr(table, &table_len, row);
        table_lens = list_append_szt(table_lens, &table_lens_len, row_len);
        i = i + 1LL;
    }
    return init_f_lens = table_lens, init_f_len = table_len, table;
}

long long mf_knapsack(long long i, long long * wt, size_t wt_len, long long * val, size_t val_len, long long j) {
    if (f[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? f_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? f_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] < 0LL) {
        if (j < wt[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? wt_len + _mochi_idx : _mochi_idx;})]) {
            f[(int)(i)][(int)(j)] = mf_knapsack(i - 1LL, wt, wt_len, val, val_len, j);
        } else {
            long long without_item = mf_knapsack(i - 1LL, wt, wt_len, val, val_len, j);
            long long with_item = mf_knapsack(i - 1LL, wt, wt_len, val, val_len, j - wt[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? wt_len + _mochi_idx : _mochi_idx;})]) + val[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? val_len + _mochi_idx : _mochi_idx;})];
            f[(int)(i)][(int)(j)] = max_int(without_item, with_item);
        }
    }
    return f[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? f_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? f_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
}

long long * * create_matrix(long long rows, long long cols) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    long long i = 0LL;
    while (i <= rows) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= cols) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
        i = i + 1LL;
    }
    return create_matrix_lens = matrix_lens, create_matrix_len = matrix_len, matrix;
}

long long * * knapsack(long long w, long long * wt, size_t wt_len, long long * val, size_t val_len, long long n) {
    long long **dp = create_matrix(n, w);
    size_t dp_len = create_matrix_len;
    size_t *dp_lens = create_matrix_lens;
    size_t dp_lens_len = create_matrix_len;
    long long i = 1LL;
    while (i <= n) {
        long long w_ = 1LL;
        while (w_ <= w) {
            if (wt[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? wt_len + _mochi_idx : _mochi_idx;})] <= w_) {
                long long include = val[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? val_len + _mochi_idx : _mochi_idx;})] + dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = w_ - wt[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? wt_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                long long exclude = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = w_; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                dp[(int)(i)][(int)(w_)] = max_int(include, exclude);
            } else {
                dp[(int)(i)][(int)(w_)] = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = w_; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
            }
            w_ = w_ + 1LL;
        }
        i = i + 1LL;
    }
    return knapsack_lens = dp_lens, knapsack_len = dp_len, dp;
}

long long * construct_solution(long long * * dp, size_t dp_len, size_t* dp_lens, size_t dp_lens_len, long long * wt, size_t wt_len, long long i, long long j, long long * optimal_set, size_t optimal_set_len) {
    if ((i > 0LL) && (j > 0LL)) {
        if (dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] == dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
            {
                long long* __ret0 = construct_solution(dp, dp_len, dp_lens, dp_len, wt, wt_len, i - 1LL, j, optimal_set, optimal_set_len);
                construct_solution_len = construct_solution_len;
                return __ret0;
            }
        } else {
            long long *with_prev = construct_solution(dp, dp_len, dp_lens, dp_len, wt, wt_len, i - 1LL, j - wt[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? wt_len + _mochi_idx : _mochi_idx;})], optimal_set, optimal_set_len);
            size_t with_prev_len = construct_solution_len;
            return construct_solution_len = with_prev_len + 1, list_append_int_new(with_prev, with_prev_len, i);
        }
    }
    return construct_solution_len = optimal_set_len, optimal_set;
}

KnapsackResult knapsack_with_example_solution(long long w, long long * wt, size_t wt_len, long long * val, size_t val_len) {
    long long num_items = wt_len;
    long long **dp_table = knapsack(w, wt, wt_len, val, val_len, num_items);
    size_t dp_table_len = knapsack_len;
    size_t *dp_table_lens = knapsack_lens;
    size_t dp_table_lens_len = knapsack_len;
    long long optimal_val = dp_table[(int)({long long _mochi_idx = num_items; _mochi_idx < 0 ? dp_table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = w; _mochi_idx < 0 ? dp_table_lens[(int)(num_items)] + _mochi_idx : _mochi_idx;})];
    long long *subset = construct_solution(dp_table, dp_table_len, dp_table_lens, dp_table_len, wt, wt_len, num_items, w, NULL, 0);
    size_t subset_len = construct_solution_len;
    return (KnapsackResult){.value = optimal_val, .subset = subset, .subset_len = subset_len};
}

const char* format_set(long long * xs, size_t xs_len) {
    const char* res = "{";
    long long i = 0LL;
    while (i < xs_len) {
        res = str_concat(res, str_int(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        if ((i + 1LL) < xs_len) {
            res = str_concat(res, ", ");
        }
        i = i + 1LL;
    }
    res = str_concat(res, "}");
    return res;
}

int main(void) {
    {
        long long __start = _now();
        val_list = malloc(4 * sizeof(long long));
        val_list_len = 4;
        val_list[0] = 3LL;
        val_list[1] = 2LL;
        val_list[2] = 4LL;
        val_list[3] = 4LL;
        wt_list = malloc(4 * sizeof(long long));
        wt_list_len = 4;
        wt_list[0] = 4LL;
        wt_list[1] = 3LL;
        wt_list[2] = 2LL;
        wt_list[3] = 3LL;
        f = init_f(n, w_cap);
        f_len = init_f_len;
        f_lens = init_f_lens;
        dp_table = knapsack(w_cap, wt_list, wt_list_len, val_list, val_list_len, n);
        dp_table_len = knapsack_len;
        dp_table_lens = knapsack_lens;
        optimal_solution = dp_table[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = w_cap; _mochi_idx < 0 ? dp_table_lens[(int)(n)] + _mochi_idx : _mochi_idx;})];
        printf("%d\n", optimal_solution);
        printf("%d\n", mf_knapsack(n, wt_list, wt_list_len, val_list, val_list_len, w_cap));
        example = knapsack_with_example_solution(w_cap, wt_list, wt_list_len, val_list, val_list_len);
        puts(str_concat("optimal_value = ", str_int(example.value)));
        puts(str_concat("An optimal subset corresponding to the optimal value ", format_set(example.subset, example.subset_len)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
