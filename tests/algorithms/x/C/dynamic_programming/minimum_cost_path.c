// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long **m1 = NULL;
size_t m1_len = 0;
size_t *m1_lens = NULL;
size_t m1_lens_len = 0;
long long **m2 = NULL;
size_t m2_len = 0;
size_t *m2_lens = NULL;
size_t m2_lens_len = 0;

long long min_int(long long a, long long b);
long long minimum_cost_path(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
int main(void);

long long min_int(long long a, long long b) {
    if (a < b) {
        return a;
    }
    return b;
}

long long minimum_cost_path(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    long long rows = matrix_len;
    long long cols = matrix_lens[0LL];
    long long j = 1LL;
    while (j < cols) {
        long long *row0 = matrix[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})];
        size_t row0_len = matrix_lens[0LL];
        row0[(int)(j)] = row0[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row0_len + _mochi_idx : _mochi_idx;})] + row0[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? row0_len + _mochi_idx : _mochi_idx;})];
        matrix[(int)(0LL)] = row0;
        j = j + 1LL;
    }
    long long i = 1LL;
    while (i < rows) {
        long long *row = matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = matrix_lens[i];
        row[(int)(0LL)] = row[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})] + matrix[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? matrix_lens[i - 1LL] + _mochi_idx : _mochi_idx;})];
        matrix[(int)(i)] = row;
        i = i + 1LL;
    }
    i = 1LL;
    while (i < rows) {
        long long *row = matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = matrix_lens[i];
        j = 1LL;
        while (j < cols) {
            long long up = matrix[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? matrix_lens[i - 1LL] + _mochi_idx : _mochi_idx;})];
            long long left = row[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})];
            long long best = min_int(up, left);
            row[(int)(j)] = row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})] + best;
            j = j + 1LL;
        }
        matrix[(int)(i)] = row;
        i = i + 1LL;
    }
    return matrix[(int)({long long _mochi_idx = rows - 1LL; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = cols - 1LL; _mochi_idx < 0 ? matrix_lens[rows - 1LL] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        m1 = ({long long** tmp = malloc(3 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp;}); tmp[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 1LL; tmp;}); tmp[2] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 2LL; tmp;}); tmp;});
        m1_len = 3;
        m1_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp;});
        m1_lens_len = 3;
        m2 = ({long long** tmp = malloc(3 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 4LL; tmp;}); tmp[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 3LL; tmp;}); tmp[2] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 2LL; tmp[2] = 1LL; tmp;}); tmp;});
        m2_len = 3;
        m2_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;});
        m2_lens_len = 3;
        puts(str_int(minimum_cost_path(m1, m1_len, m1_lens, m1_len)));
        puts(str_int(minimum_cost_path(m2, m2_len, m2_lens, m2_len)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
