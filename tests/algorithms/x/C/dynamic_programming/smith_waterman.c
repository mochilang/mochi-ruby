// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t smith_waterman_len;
size_t *smith_waterman_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* query = "HEAGAWGHEE";
const char* subject = "PAWHEAE";
long long **score = NULL;
size_t score_len = 0;
size_t *score_lens = NULL;
size_t score_lens_len = 0;

long long score_function(const char* source_char, const char* target_char, long long match_score, long long mismatch_score, long long gap_score);
long long * * smith_waterman(const char* query, const char* subject, long long match_score, long long mismatch_score, long long gap_score);
const char* traceback(long long * * score, size_t score_len, size_t* score_lens, size_t score_lens_len, const char* query, const char* subject, long long match_score, long long mismatch_score, long long gap_score);
int main(void);

long long score_function(const char* source_char, const char* target_char, long long match_score, long long mismatch_score, long long gap_score) {
    if ((strcmp(source_char, "-") == 0) || (strcmp(target_char, "-") == 0)) {
        return gap_score;
    }
    if (strcmp(source_char, target_char) == 0) {
        return match_score;
    }
    return mismatch_score;
}

long long * * smith_waterman(const char* query, const char* subject, long long match_score, long long mismatch_score, long long gap_score) {
    const char* q = str_upper(query);
    const char* s = str_upper(subject);
    long long m = strlen(q);
    long long n = strlen(s);
    long long **score = NULL;
    size_t score_len = 0;
    size_t *score_lens = NULL;
    size_t score_lens_len = 0;
    for (int _ = 0LL; _ < m + 1LL; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < n + 1LL; _2++) {
            row = list_append_long_long(row, &row_len, 0LL);
        }
        score = list_append_long_longptr(score, &score_len, row);
        score_lens = list_append_szt(score_lens, &score_lens_len, row_len);
    }
    for (int i = 1LL; i < m + 1LL; i++) {
        for (int j = 1LL; j < n + 1LL; j++) {
            const char* qc = _substring(q, i - 1LL, i);
            const char* sc = _substring(s, j - 1LL, j);
            long long diag = score[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? score_lens[i - 1LL] + _mochi_idx : _mochi_idx;})] + score_function(qc, sc, match_score, mismatch_score, gap_score);
            long long delete = score[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? score_lens[i - 1LL] + _mochi_idx : _mochi_idx;})] + gap_score;
            long long insert = score[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? score_lens[i] + _mochi_idx : _mochi_idx;})] + gap_score;
            long long max_val = 0LL;
            if (diag > max_val) {
                max_val = diag;
            }
            if (delete > max_val) {
                max_val = delete;
            }
            if (insert > max_val) {
                max_val = insert;
            }
            score[(int)(i)][(int)(j)] = max_val;
        }
    }
    return smith_waterman_lens = score_lens, smith_waterman_len = score_len, score;
}

const char* traceback(long long * * score, size_t score_len, size_t* score_lens, size_t score_lens_len, const char* query, const char* subject, long long match_score, long long mismatch_score, long long gap_score) {
    const char* q = str_upper(query);
    const char* s = str_upper(subject);
    long long max_value = 0LL;
    long long i_max = 0LL;
    long long j_max = 0LL;
    for (int i = 0LL; i < score_len; i++) {
        for (int j = 0LL; j < score_lens[i]; j++) {
            if (score[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? score_lens[i] + _mochi_idx : _mochi_idx;})] > max_value) {
                max_value = score[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? score_lens[i] + _mochi_idx : _mochi_idx;})];
                i_max = i;
                j_max = j;
            }
        }
    }
    long long i = i_max;
    long long j = j_max;
    const char* align1 = "";
    const char* align2 = "";
    long long gap_penalty = score_function("-", "-", match_score, mismatch_score, gap_score);
    if ((i == 0LL) || (j == 0LL)) {
        return "";
    }
    while ((i > 0LL) && (j > 0LL)) {
        const char* qc = _substring(q, i - 1LL, i);
        const char* sc = _substring(s, j - 1LL, j);
        if (score[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? score_lens[i] + _mochi_idx : _mochi_idx;})] == (score[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? score_lens[i - 1LL] + _mochi_idx : _mochi_idx;})] + score_function(qc, sc, match_score, mismatch_score, gap_score))) {
            align1 = str_concat(qc, align1);
            align2 = str_concat(sc, align2);
            i = i - 1LL;
            j = j - 1LL;
        } else {
            if (score[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? score_lens[i] + _mochi_idx : _mochi_idx;})] == (score[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? score_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? score_lens[i - 1LL] + _mochi_idx : _mochi_idx;})] + gap_penalty)) {
                align1 = str_concat(qc, align1);
                align2 = str_concat("-", align2);
                i = i - 1LL;
            } else {
                align1 = str_concat("-", align1);
                align2 = str_concat(sc, align2);
                j = j - 1LL;
            }
        }
    }
    return str_concat(str_concat(align1, "\n"), align2);
}

int main(void) {
    {
        long long __start = _now();
        score = smith_waterman(query, subject, 1LL, -1.0, -2.0);
        score_len = smith_waterman_len;
        score_lens = smith_waterman_lens;
        puts(traceback(score, score_len, score_lens, score_len, query, subject, 1LL, -1.0, -2.0));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
