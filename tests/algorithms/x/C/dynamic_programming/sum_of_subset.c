// Generated by Mochi 0.10.32 on 2025-08-13 15:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_bool_matrix_len;
size_t *create_bool_matrix_lens;

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * create_bool_matrix(long long rows, long long cols);
long long is_sum_subset(long long * arr, long long arr_len, long long required_sum);
int main(void);

long long * * create_bool_matrix(long long rows, long long cols) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    long long i = 0LL;
    while (i <= rows) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= cols) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
        i = i + 1LL;
    }
    return create_bool_matrix_lens = matrix_lens, create_bool_matrix_len = matrix_len, matrix;
}

long long is_sum_subset(long long * arr, long long arr_len, long long required_sum) {
    long long **subset = create_bool_matrix(arr_len, required_sum);
    size_t subset_len = create_bool_matrix_len;
    size_t *subset_lens = create_bool_matrix_lens;
    size_t subset_lens_len = create_bool_matrix_len;
    long long i = 0LL;
    while (i <= arr_len) {
        subset[(int)(i)][(int)(0LL)] = 1LL;
        i = i + 1LL;
    }
    long long j = 1LL;
    while (j <= required_sum) {
        subset[(int)(0LL)][(int)(j)] = 0LL;
        j = j + 1LL;
    }
    i = 1LL;
    while (i <= arr_len) {
        j = 1LL;
        while (j <= required_sum) {
            if (arr[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > j) {
                subset[(int)(i)][(int)(j)] = subset[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? subset_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? subset_lens[i - 1LL] + _mochi_idx : _mochi_idx;})];
            }
            if (arr[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] <= j) {
                subset[(int)(i)][(int)(j)] = subset[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? subset_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? subset_lens[i - 1LL] + _mochi_idx : _mochi_idx;})] || subset[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? subset_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - arr[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? subset_lens[i - 1LL] + _mochi_idx : _mochi_idx;})];
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return subset[(int)({long long _mochi_idx = arr_len; _mochi_idx < 0 ? subset_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = required_sum; _mochi_idx < 0 ? subset_lens[arr_len] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", is_sum_subset(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 4LL; tmp[2] = 6LL; tmp[3] = 8LL; tmp;}), 4, 5LL));
        printf("%d\n", is_sum_subset(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 4LL; tmp[2] = 6LL; tmp[3] = 8LL; tmp;}), 4, 14LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
