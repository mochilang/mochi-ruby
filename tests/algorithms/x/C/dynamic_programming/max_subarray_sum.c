// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double *empty = NULL;
size_t empty_len = 0;

double max_subarray_sum(double * nums, size_t nums_len, long long allow_empty);
int main(void);

double max_subarray_sum(double * nums, size_t nums_len, long long allow_empty) {
    if (nums_len == 0LL) {
        return 0.0;
    }
    double max_sum = 0.0;
    double curr_sum = 0.0;
    if (allow_empty) {
        max_sum = 0.0;
        curr_sum = 0.0;
        long long i = 0LL;
        while (i < nums_len) {
            double num = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
            double temp = curr_sum + num;
            curr_sum = (temp > 0.0 ? temp : 0.0);
            if (curr_sum > max_sum) {
                max_sum = curr_sum;
            }
            i = i + 1LL;
        }
    } else {
        max_sum = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        curr_sum = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        long long i = 1LL;
        while (i < nums_len) {
            double num = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
            double temp = curr_sum + num;
            curr_sum = (temp > num ? temp : num);
            if (curr_sum > max_sum) {
                max_sum = curr_sum;
            }
            i = i + 1LL;
        }
    }
    return max_sum;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(max_subarray_sum(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 8.0; tmp[2] = 9.0; tmp;}), 3, 0LL)));
        puts(str_float(max_subarray_sum(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 0.0; tmp;}), 2, 0LL)));
        puts(str_float(max_subarray_sum(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -1.0; tmp[1] = 0.0; tmp[2] = 1.0; tmp;}), 3, 0LL)));
        puts(str_float(max_subarray_sum(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 2.0; tmp[2] = 3.0; tmp[3] = 4.0; tmp[4] = -2.0; tmp;}), 5, 0LL)));
        puts(str_float(max_subarray_sum(({double *tmp = malloc(9 * sizeof(double)); tmp[0] = -2.0; tmp[1] = 1.0; tmp[2] = -3.0; tmp[3] = 4.0; tmp[4] = -1.0; tmp[5] = 2.0; tmp[6] = 1.0; tmp[7] = -5.0; tmp[8] = 4.0; tmp;}), 9, 0LL)));
        puts(str_float(max_subarray_sum(({double *tmp = malloc(5 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 3.0; tmp[2] = -9.0; tmp[3] = 8.0; tmp[4] = -2.0; tmp;}), 5, 0LL)));
        puts(str_float(max_subarray_sum(({double *tmp = malloc(5 * sizeof(double)); tmp[0] = -2.0; tmp[1] = -3.0; tmp[2] = -1.0; tmp[3] = -4.0; tmp[4] = -6.0; tmp;}), 5, 0LL)));
        puts(str_float(max_subarray_sum(({double *tmp = malloc(5 * sizeof(double)); tmp[0] = -2.0; tmp[1] = -3.0; tmp[2] = -1.0; tmp[3] = -4.0; tmp[4] = -6.0; tmp;}), 5, 1LL)));
        empty = NULL;
        empty_len = 0;
        puts(str_float(max_subarray_sum(empty, empty_len, 0LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
