// Generated by Mochi 0.10.32 on 2025-08-25 19:02 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t longest_subsequence_len;

size_t _slice_int_len;
size_t append_len;
static size_t concat_len;
static long long* concat_long_long(long long *a, size_t a_len, const long long *b, size_t b_len) {
    long long *res = realloc(a, (a_len + b_len) * sizeof(long long));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(long long));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * longest_subsequence(long long * xs, size_t xs_len);
int main(void);

long long * longest_subsequence(long long * xs, size_t xs_len) {
    long long n = xs_len;
    if (n <= 1LL) {
        return longest_subsequence_len = xs_len, xs;
    }
    long long pivot = xs[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
    long long is_found = 0LL;
    long long i = 1LL;
    long long *longest_subseq = NULL;
    size_t longest_subseq_len = 0;
    while (!(is_found) && (i < n)) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] < pivot) {
            is_found = 1LL;
            long long *temp_array = _slice_int(xs, xs_len, i, n, &(_slice_int_len));
            size_t temp_array_len = _slice_int_len;
            temp_array = longest_subsequence(temp_array, temp_array_len);
            temp_array_len = longest_subsequence_len;
            if (temp_array_len > longest_subseq_len) {
                longest_subseq = temp_array;
                longest_subseq_len = temp_array_len;
            }
        } else {
            i = i + 1LL;
        }
    }
    long long *filtered = NULL;
    size_t filtered_len = 0;
    long long j = 1LL;
    while (j < n) {
        if (xs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] >= pivot) {
            filtered = list_append_long_long(filtered, &filtered_len, xs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        j = j + 1LL;
    }
    long long *candidate = NULL;
    size_t candidate_len = 0;
    candidate = list_append_long_long(candidate, &candidate_len, pivot);
    candidate = concat_long_long(candidate, candidate_len, longest_subsequence(filtered, filtered_len), longest_subsequence_len);
    candidate_len = concat_len;
    if (candidate_len > longest_subseq_len) {
        return longest_subsequence_len = candidate_len, candidate;
    } else {
        return longest_subsequence_len = longest_subseq_len, longest_subseq;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
