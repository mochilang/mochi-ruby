// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long recursive_match(const char* text, const char* pattern);
long long dp_match(const char* text, const char* pattern);
void print_bool(long long b);
int main(void);

long long recursive_match(const char* text, const char* pattern) {
    if (strlen(pattern) == 0LL) {
        return strlen(text) == 0LL;
    }
    if (strlen(text) == 0LL) {
        if ((strlen(pattern) >= 2LL) && (strcmp(_substring(pattern, strlen(pattern) - 1LL, strlen(pattern)), "*") == 0)) {
            return recursive_match(text, _substring(pattern, 0LL, strlen(pattern) - 2LL));
        }
        return 0LL;
    }
    const char* last_text = _substring(text, strlen(text) - 1LL, strlen(text));
    const char* last_pattern = _substring(pattern, strlen(pattern) - 1LL, strlen(pattern));
    if ((strcmp(last_text, last_pattern) == 0) || (strcmp(last_pattern, ".") == 0)) {
        return recursive_match(_substring(text, 0LL, strlen(text) - 1LL), _substring(pattern, 0LL, strlen(pattern) - 1LL));
    }
    if (strcmp(last_pattern, "*") == 0) {
        if (recursive_match(_substring(text, 0LL, strlen(text) - 1LL), pattern)) {
            return 1LL;
        }
        return recursive_match(text, _substring(pattern, 0LL, strlen(pattern) - 2LL));
    }
    return 0LL;
}

long long dp_match(const char* text, const char* pattern) {
    long long m = strlen(text);
    long long n = strlen(pattern);
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    long long i = 0LL;
    while (i <= m) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= n) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        dp = list_append_long_longptr(dp, &dp_len, row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, row_len);
        i = i + 1LL;
    }
    dp[(int)(0LL)][(int)(0LL)] = 1LL;
    long long j = 1LL;
    while (j <= n) {
        if ((strcmp(_substring(pattern, j - 1LL, j), "*") == 0) && (j >= 2LL)) {
            if (dp[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 2LL; _mochi_idx < 0 ? dp_lens[0LL] + _mochi_idx : _mochi_idx;})]) {
                dp[(int)(0LL)][(int)(j)] = 1LL;
            }
        }
        j = j + 1LL;
    }
    i = 1LL;
    while (i <= m) {
        j = 1LL;
        while (j <= n) {
            const char* p_char = _substring(pattern, j - 1LL, j);
            const char* t_char = _substring(text, i - 1LL, i);
            if ((strcmp(p_char, ".") == 0) || (strcmp(p_char, t_char) == 0)) {
                if (dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[i - 1LL] + _mochi_idx : _mochi_idx;})]) {
                    dp[(int)(i)][(int)(j)] = 1LL;
                }
            } else {
                if (strcmp(p_char, "*") == 0) {
                    if (j >= 2LL) {
                        if (dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 2LL; _mochi_idx < 0 ? dp_lens[i] + _mochi_idx : _mochi_idx;})]) {
                            dp[(int)(i)][(int)(j)] = 1LL;
                        }
                        const char* prev_p = _substring(pattern, j - 2LL, j - 1LL);
                        if ((strcmp(prev_p, ".") == 0) || (strcmp(prev_p, t_char) == 0)) {
                            if (dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[i - 1LL] + _mochi_idx : _mochi_idx;})]) {
                                dp[(int)(i)][(int)(j)] = 1LL;
                            }
                        }
                    }
                } else {
                    dp[(int)(i)][(int)(j)] = 0LL;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return dp[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_lens[m] + _mochi_idx : _mochi_idx;})];
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        print_bool(recursive_match("abc", "a.c"));
        print_bool(recursive_match("abc", "af*.c"));
        print_bool(recursive_match("abc", "a.c*"));
        print_bool(recursive_match("abc", "a.c*d"));
        print_bool(recursive_match("aa", ".*"));
        print_bool(dp_match("abc", "a.c"));
        print_bool(dp_match("abc", "af*.c"));
        print_bool(dp_match("abc", "a.c*"));
        print_bool(dp_match("abc", "a.c*d"));
        print_bool(dp_match("aa", ".*"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
