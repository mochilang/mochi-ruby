// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t allConstruct_len;
size_t *allConstruct_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long*** list_append_intptrptr(long long ***arr, size_t *len, long long **val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* * * allConstruct(const char* target, const char* * wordBank, size_t wordBank_len);
int main(void);

const char* * * allConstruct(const char* target, const char* * wordBank, size_t wordBank_len) {
    long long tableSize = strlen(target) + 1LL;
    const char* ***table = NULL;
    size_t table_len = 0;
    size_t *table_lens = NULL;
    size_t table_lens_len = 0;
    size_t **table_lens_lens = NULL;
    size_t table_lens_lens_len = 0;
    long long idx = 0LL;
    while (idx < tableSize) {
        const char* **empty = NULL;
        size_t empty_len = 0;
        size_t *empty_lens = NULL;
        size_t empty_lens_len = 0;
        table = list_append_strptr(table, &table_len, empty);
        table_lens = list_append_szt(table_lens, &table_lens_len, empty_len);
        idx = idx + 1LL;
    }
    const char* *base = NULL;
    size_t base_len = 0;
    table[(int)(0LL)] = ({const char*** tmp = malloc(1 * sizeof(const char**)); tmp[0] = base; tmp;});
    long long i = 0LL;
    while (i < tableSize) {
        if (table_lens[(int)(i)] != 0LL) {
            long long w = 0LL;
            while (w < wordBank_len) {
                const char* word = wordBank[(int)({long long _mochi_idx = w; _mochi_idx < 0 ? wordBank_len + _mochi_idx : _mochi_idx;})];
                long long wordLen = strlen(word);
                if (strcmp(_substring(target, i, i + wordLen), word) == 0) {
                    long long k = 0LL;
                    while (k < table_lens[(int)(i)]) {
                        const char* *way = table[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? table_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
                        size_t way_len = 0;
                        const char* *combination = NULL;
                        size_t combination_len = 0;
                        long long m = 0LL;
                        while (m < way_len) {
                            combination = list_append_str(combination, &combination_len, way[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? way_len + _mochi_idx : _mochi_idx;})]);
                            m = m + 1LL;
                        }
                        combination = list_append_str(combination, &combination_len, word);
                        long long nextIndex = i + wordLen;
                        table[(int)(nextIndex)] = list_append_int_new(table[(int)({long long _mochi_idx = nextIndex; _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})], table_lens[(int)(nextIndex)], combination);
                        {size_t __tmp = table_lens[(int)(nextIndex)]; table_lens_lens[(int)(nextIndex)] = list_append_szt(table_lens_lens[(int)(nextIndex)], &__tmp, combination_len); table_lens[(int)(nextIndex)] = __tmp;}
                        k = k + 1LL;
                    }
                }
                w = w + 1LL;
            }
        }
        i = i + 1LL;
    }
    return table[(int)({long long _mochi_idx = strlen(target); _mochi_idx < 0 ? table_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(allConstruct("jwajalapa", ({const char* *tmp = malloc(6 * sizeof(const char*)); tmp[0] = "jwa"; tmp[1] = "j"; tmp[2] = "w"; tmp[3] = "a"; tmp[4] = "la"; tmp[5] = "lapa"; tmp;}), 6)));
        puts(str_int(allConstruct("rajamati", ({const char* *tmp = malloc(7 * sizeof(const char*)); tmp[0] = "s"; tmp[1] = "raj"; tmp[2] = "amat"; tmp[3] = "raja"; tmp[4] = "ma"; tmp[5] = "i"; tmp[6] = "t"; tmp;}), 7)));
        puts(str_int(allConstruct("hexagonosaurus", ({const char* *tmp = malloc(12 * sizeof(const char*)); tmp[0] = "h"; tmp[1] = "ex"; tmp[2] = "hex"; tmp[3] = "ag"; tmp[4] = "ago"; tmp[5] = "ru"; tmp[6] = "auru"; tmp[7] = "rus"; tmp[8] = "go"; tmp[9] = "no"; tmp[10] = "o"; tmp[11] = "s"; tmp;}), 12)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
