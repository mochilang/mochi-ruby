// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_nodes_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    long long key;
    long long freq;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Node * sort_nodes(Node * nodes, size_t nodes_len);
void print_node(Node n);
void print_binary_search_tree(long long * * root, size_t root_len, size_t* root_lens, size_t root_lens_len, long long * keys, size_t keys_len, long long i, long long j, long long parent, long long is_left);
void find_optimal_binary_search_tree(Node * original_nodes, size_t original_nodes_len);
void user_main();
int main(void);

Node * sort_nodes(Node * nodes, size_t nodes_len) {
    Node *arr = nodes;
    size_t arr_len = nodes_len;
    long long i = 1LL;
    while (i < arr_len) {
        Node key_node = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while (j >= 0LL) {
            Node temp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            if (temp.key > key_node.key) {
                arr[(int)(j + 1LL)] = temp;
                j = j - 1LL;
            } else {
                break;
            }
        }
        arr[(int)(j + 1LL)] = key_node;
        i = i + 1LL;
    }
    return sort_nodes_len = arr_len, arr;
}

void print_node(Node n) {
    puts(str_concat(str_concat(str_concat(str_concat("Node(key=", str_int(n.key)), ", freq="), str_int(n.freq)), ")"));
}

void print_binary_search_tree(long long * * root, size_t root_len, size_t* root_lens, size_t root_lens_len, long long * keys, size_t keys_len, long long i, long long j, long long parent, long long is_left) {
    if (((i > j) || (i < 0LL)) || (j > (root_len - 1LL))) {
        return 0;
    }
    long long node = root[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? root_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? root_lens[i] + _mochi_idx : _mochi_idx;})];
    if (parent == -1.0) {
        puts(str_concat(str_int(keys[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})]), " is the root of the binary search tree."));
    } else {
        if (is_left) {
            puts(str_concat(str_concat(str_concat(str_int(keys[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})]), " is the left child of key "), str_int(parent)), "."));
        } else {
            puts(str_concat(str_concat(str_concat(str_int(keys[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})]), " is the right child of key "), str_int(parent)), "."));
        }
    }
    print_binary_search_tree(root, root_len, root_lens, root_len, keys, keys_len, i, node - 1LL, keys[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})], 1LL);
    print_binary_search_tree(root, root_len, root_lens, root_len, keys, keys_len, node + 1LL, j, keys[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})], 0LL);
}

void find_optimal_binary_search_tree(Node * original_nodes, size_t original_nodes_len) {
    Node *nodes = sort_nodes(original_nodes, original_nodes_len);
    size_t nodes_len = sort_nodes_len;
    long long n = nodes_len;
    long long *keys = NULL;
    size_t keys_len = 0;
    long long *freqs = NULL;
    size_t freqs_len = 0;
    long long i = 0LL;
    while (i < n) {
        Node node = nodes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        keys = list_append_long_long(keys, &keys_len, node.key);
        freqs = list_append_long_long(freqs, &freqs_len, node.freq);
        i = i + 1LL;
    }
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    long long **total = NULL;
    size_t total_len = 0;
    size_t *total_lens = NULL;
    size_t total_lens_len = 0;
    long long **root = NULL;
    size_t root_len = 0;
    size_t *root_lens = NULL;
    size_t root_lens_len = 0;
    i = 0LL;
    while (i < n) {
        long long *dp_row = NULL;
        size_t dp_row_len = 0;
        long long *total_row = NULL;
        size_t total_row_len = 0;
        long long *root_row = NULL;
        size_t root_row_len = 0;
        long long j = 0LL;
        while (j < n) {
            if (i == j) {
                dp_row = list_append_long_long(dp_row, &dp_row_len, freqs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? freqs_len + _mochi_idx : _mochi_idx;})]);
                total_row = list_append_long_long(total_row, &total_row_len, freqs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? freqs_len + _mochi_idx : _mochi_idx;})]);
                root_row = list_append_long_long(root_row, &root_row_len, i);
            } else {
                dp_row = list_append_long_long(dp_row, &dp_row_len, 0LL);
                total_row = list_append_long_long(total_row, &total_row_len, 0LL);
                root_row = list_append_long_long(root_row, &root_row_len, 0LL);
            }
            j = j + 1LL;
        }
        dp = list_append_long_longptr(dp, &dp_len, dp_row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, dp_row_len);
        total = list_append_long_longptr(total, &total_len, total_row);
        total_lens = list_append_szt(total_lens, &total_lens_len, total_row_len);
        root = list_append_long_longptr(root, &root_len, root_row);
        root_lens = list_append_szt(root_lens, &root_lens_len, root_row_len);
        i = i + 1LL;
    }
    long long interval_length = 2LL;
    long long INF = 2147483647LL;
    while (interval_length <= n) {
        i = 0LL;
        while (i < ((n - interval_length) + 1LL)) {
            long long j = (i + interval_length) - 1LL;
            dp[(int)(i)][(int)(j)] = INF;
            total[(int)(i)][(int)(j)] = total[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? total_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? total_lens[i] + _mochi_idx : _mochi_idx;})] + freqs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? freqs_len + _mochi_idx : _mochi_idx;})];
            long long r = root[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? root_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? root_lens[i] + _mochi_idx : _mochi_idx;})];
            while (r <= root[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? root_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? root_lens[i + 1LL] + _mochi_idx : _mochi_idx;})]) {
                long long left = (r != i ? dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = r - 1LL; _mochi_idx < 0 ? dp_lens[i] + _mochi_idx : _mochi_idx;})] : 0LL);
                long long right = (r != j ? dp[(int)({long long _mochi_idx = r + 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[r + 1LL] + _mochi_idx : _mochi_idx;})] : 0LL);
                long long cost = (left + total[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? total_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? total_lens[i] + _mochi_idx : _mochi_idx;})]) + right;
                if (dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[i] + _mochi_idx : _mochi_idx;})] > cost) {
                    dp[(int)(i)][(int)(j)] = cost;
                    root[(int)(i)][(int)(j)] = r;
                }
                r = r + 1LL;
            }
            i = i + 1LL;
        }
        interval_length = interval_length + 1LL;
    }
    puts("Binary search tree nodes:");
    i = 0LL;
    while (i < n) {
        print_node(nodes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    puts(str_concat(str_concat("\nThe cost of optimal BST for given tree nodes is ", str_int(dp[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? dp_lens[0LL] + _mochi_idx : _mochi_idx;})])), "."));
    print_binary_search_tree(root, root_len, root_lens, root_len, keys, keys_len, 0LL, n - 1LL, -1.0, 0LL);
}

void user_main() {
    Node *nodes = NULL;
    size_t nodes_len = 0;
    nodes = list_append_Node(nodes, &nodes_len, (Node){.key = 12LL, .freq = 8LL});
    nodes = list_append_Node(nodes, &nodes_len, (Node){.key = 10LL, .freq = 34LL});
    nodes = list_append_Node(nodes, &nodes_len, (Node){.key = 20LL, .freq = 50LL});
    nodes = list_append_Node(nodes, &nodes_len, (Node){.key = 42LL, .freq = 3LL});
    nodes = list_append_Node(nodes, &nodes_len, (Node){.key = 25LL, .freq = 40LL});
    nodes = list_append_Node(nodes, &nodes_len, (Node){.key = 37LL, .freq = 30LL});
    find_optimal_binary_search_tree(nodes, nodes_len);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
