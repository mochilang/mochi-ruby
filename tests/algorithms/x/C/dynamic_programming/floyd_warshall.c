// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Graph Graph;

struct Graph {
    long long n;
    long long **dp;
    size_t dp_len;
    size_t *dp_lens;
    size_t dp_lens_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long INF = 1000000000LL;
Graph graph;

Graph new_graph(long long n);
const char* add_edge(Graph* g, long long u, long long v, long long w);
const char* floyd_warshall(Graph* g);
long long show_min(Graph g, long long u, long long v);
int main(void);

Graph new_graph(long long n) {
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            if (i == j) {
                row = list_append_long_long(row, &row_len, 0LL);
            } else {
                row = list_append_long_long(row, &row_len, INF);
            }
            j = j + 1LL;
        }
        dp = list_append_intptr(dp, &dp_len, row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, row_len);
        i = i + 1LL;
    }
    return (Graph){.n = n, .dp = dp, .dp_len = dp_len, .dp_lens = dp_lens, .dp_lens_len = dp_len};
}

const char* add_edge(Graph* g, long long u, long long v, long long w) {
    long long **dp = g->dp;
    size_t dp_len = g->dp_len;
    size_t *dp_lens = g->dp_lens;
    size_t dp_lens_len = g->dp_len;
    long long *row = dp[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
    size_t row_len = dp_lens[(int)(u)];
    row[(int)(v)] = w;
    dp[(int)(u)] = row;
    g->dp = dp;
    g->dp_len = dp_len;
    g->dp_lens = dp_lens;
    g->dp_lens_len = dp_lens_len;
}

const char* floyd_warshall(Graph* g) {
    long long **dp = g->dp;
    size_t dp_len = g->dp_len;
    size_t *dp_lens = g->dp_lens;
    size_t dp_lens_len = g->dp_len;
    long long k = 0LL;
    while (k < g->n) {
        long long i = 0LL;
        while (i < g->n) {
            long long j = 0LL;
            while (j < g->n) {
                long long alt = dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] + dp[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(k)] + _mochi_idx : _mochi_idx;})];
                long long *row = dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
                size_t row_len = dp_lens[(int)(i)];
                if (alt < row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]) {
                    row[(int)(j)] = alt;
                    dp[(int)(i)] = row;
                }
                j = j + 1LL;
            }
            i = i + 1LL;
        }
        k = k + 1LL;
    }
    g->dp = dp;
    g->dp_len = dp_len;
    g->dp_lens = dp_lens;
    g->dp_lens_len = dp_lens_len;
}

long long show_min(Graph g, long long u, long long v) {
    return g.dp[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? g.dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = v; _mochi_idx < 0 ? g.dp_lens[u] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        graph = new_graph(5LL);
        add_edge(&graph, 0LL, 2LL, 9LL);
        add_edge(&graph, 0LL, 4LL, 10LL);
        add_edge(&graph, 1LL, 3LL, 5LL);
        add_edge(&graph, 2LL, 3LL, 7LL);
        add_edge(&graph, 3LL, 0LL, 10LL);
        add_edge(&graph, 3LL, 1LL, 2LL);
        add_edge(&graph, 3LL, 2LL, 1LL);
        add_edge(&graph, 3LL, 4LL, 6LL);
        add_edge(&graph, 4LL, 1LL, 3LL);
        add_edge(&graph, 4LL, 2LL, 4LL);
        add_edge(&graph, 4LL, 3LL, 9LL);
        floyd_warshall(&graph);
        puts(str_int(show_min(graph, 1LL, 4LL)));
        puts(str_int(show_min(graph, 0LL, 3LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
