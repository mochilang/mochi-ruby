// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_list_len;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * make_list(long long len, long long value);
long long int_sqrt(long long n);
long long minimum_squares_to_represent_a_number(long long number);
int main(void);

long long * make_list(long long len, long long value) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < len) {
        arr = list_append_long_long(arr, &arr_len, value);
        i = i + 1LL;
    }
    return make_list_len = arr_len, arr;
}

long long int_sqrt(long long n) {
    long long r = 0LL;
    while (((r + 1LL) * (r + 1LL)) <= n) {
        r = r + 1LL;
    }
    return r;
}

long long minimum_squares_to_represent_a_number(long long number) {
    if (number < 0LL) {
        panic("the value of input must not be a negative number");
    }
    if (number == 0LL) {
        return 1LL;
    }
    long long *answers = make_list(number + 1LL, -1.0);
    size_t answers_len = make_list_len;
    answers[(int)(0LL)] = 0LL;
    long long i = 1LL;
    while (i <= number) {
        long long answer = i;
        long long root = int_sqrt(i);
        long long j = 1LL;
        while (j <= root) {
            long long current_answer = 1LL + answers[(int)({long long _mochi_idx = i - (j * j); _mochi_idx < 0 ? answers_len + _mochi_idx : _mochi_idx;})];
            if (current_answer < answer) {
                answer = current_answer;
            }
            j = j + 1LL;
        }
        answers[(int)(i)] = answer;
        i = i + 1LL;
    }
    return answers[(int)({long long _mochi_idx = number; _mochi_idx < 0 ? answers_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", minimum_squares_to_represent_a_number(25LL));
        printf("%d\n", minimum_squares_to_represent_a_number(21LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
