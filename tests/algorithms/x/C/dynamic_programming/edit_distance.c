// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long min3(long long a, long long b, long long c);
long long helper_top_down(const char* word1, const char* word2, long long * * dp, size_t dp_len, size_t* dp_lens, size_t dp_lens_len, long long i, long long j);
long long min_dist_top_down(const char* word1, const char* word2);
long long min_dist_bottom_up(const char* word1, const char* word2);
int main(void);

long long min3(long long a, long long b, long long c) {
    long long m = a;
    if (b < m) {
        m = b;
    }
    if (c < m) {
        m = c;
    }
    return m;
}

long long helper_top_down(const char* word1, const char* word2, long long * * dp, size_t dp_len, size_t* dp_lens, size_t dp_lens_len, long long i, long long j) {
    if (i < 0LL) {
        return j + 1LL;
    }
    if (j < 0LL) {
        return i + 1LL;
    }
    if (dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] != -1LL) {
        return dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
    }
    if (strcmp(_substring(word1, i, i + 1LL), _substring(word2, j, j + 1LL)) == 0) {
        dp[(int)(i)][(int)(j)] = helper_top_down(word1, word2, dp, dp_len, dp_lens, dp_len, i - 1LL, j - 1LL);
    } else {
        long long insert = helper_top_down(word1, word2, dp, dp_len, dp_lens, dp_len, i, j - 1LL);
        long long delete = helper_top_down(word1, word2, dp, dp_len, dp_lens, dp_len, i - 1LL, j);
        long long replace = helper_top_down(word1, word2, dp, dp_len, dp_lens, dp_len, i - 1LL, j - 1LL);
        dp[(int)(i)][(int)(j)] = 1LL + min3(insert, delete, replace);
    }
    return dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
}

long long min_dist_top_down(const char* word1, const char* word2) {
    long long m = strlen(word1);
    long long n = strlen(word2);
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    for (int _ = 0LL; _ < m; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < n; _2++) {
            row = list_append_long_long(row, &row_len, -1LL);
        }
        dp = list_append_intptr(dp, &dp_len, row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, row_len);
    }
    return helper_top_down(word1, word2, dp, dp_len, dp_lens, dp_len, m - 1LL, n - 1LL);
}

long long min_dist_bottom_up(const char* word1, const char* word2) {
    long long m = strlen(word1);
    long long n = strlen(word2);
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    for (int _ = 0LL; _ < m + 1LL; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < n + 1LL; _2++) {
            row = list_append_long_long(row, &row_len, 0LL);
        }
        dp = list_append_intptr(dp, &dp_len, row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, row_len);
    }
    for (int i = 0LL; i < m + 1LL; i++) {
        for (int j = 0LL; j < n + 1LL; j++) {
            if (i == 0LL) {
                dp[(int)(i)][(int)(j)] = j;
            } else {
                if (j == 0LL) {
                    dp[(int)(i)][(int)(j)] = i;
                } else {
                    if (strcmp(_substring(word1, i - 1LL, i), _substring(word2, j - 1LL, j)) == 0) {
                        dp[(int)(i)][(int)(j)] = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                    } else {
                        long long insert = dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
                        long long delete = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                        long long replace = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                        dp[(int)(i)][(int)(j)] = 1LL + min3(insert, delete, replace);
                    }
                }
            }
        }
    }
    return dp[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_lens[(int)(m)] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(min_dist_top_down("intention", "execution")));
        puts(str_int(min_dist_top_down("intention", "")));
        puts(str_int(min_dist_top_down("", "")));
        puts(str_int(min_dist_bottom_up("intention", "execution")));
        puts(str_int(min_dist_bottom_up("intention", "")));
        puts(str_int(min_dist_bottom_up("", "")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
