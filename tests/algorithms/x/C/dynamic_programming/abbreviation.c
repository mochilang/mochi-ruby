// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long index_of(const char* s, const char* ch);
long long ord(const char* ch);
const char* chr(long long n);
const char* to_upper_char(const char* c);
long long is_lower(const char* c);
long long abbr(const char* a, const char* b);
void print_bool(long long b);
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long ord(const char* ch) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    long long idx = index_of(upper, ch);
    if (idx >= 0LL) {
        return 65LL + idx;
    }
    idx = index_of(lower, ch);
    if (idx >= 0LL) {
        return 97LL + idx;
    }
    return 0LL;
}

const char* chr(long long n) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    if ((n >= 65LL) && (n < 91LL)) {
        return _substring(upper, n - 65LL, n - 64LL);
    }
    if ((n >= 97LL) && (n < 123LL)) {
        return _substring(lower, n - 97LL, n - 96LL);
    }
    return "?";
}

const char* to_upper_char(const char* c) {
    long long code = ord(c);
    if ((code >= 97LL) && (code <= 122LL)) {
        return chr(code - 32LL);
    }
    return c;
}

long long is_lower(const char* c) {
    long long code = ord(c);
    return (code >= 97LL) && (code <= 122LL);
}

long long abbr(const char* a, const char* b) {
    long long n = strlen(a);
    long long m = strlen(b);
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    long long i = 0LL;
    while (i <= n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= m) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        dp = list_append_intptr(dp, &dp_len, row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, row_len);
        i = i + 1LL;
    }
    dp[(int)(0LL)][(int)(0LL)] = 1LL;
    i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j <= m) {
            if (dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
                if ((j < m) && (strcmp(to_upper_char((const char[]){a[i], 0}), (const char[]){b[j], 0}) == 0)) {
                    dp[(int)(i + 1LL)][(int)(j + 1LL)] = 1LL;
                }
                if (is_lower((const char[]){a[i], 0})) {
                    dp[(int)(i + 1LL)][(int)(j)] = 1LL;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return dp[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = m; _mochi_idx < 0 ? dp_lens[(int)(n)] + _mochi_idx : _mochi_idx;})];
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        print_bool(abbr("daBcd", "ABC"));
        print_bool(abbr("dBcd", "ABC"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
