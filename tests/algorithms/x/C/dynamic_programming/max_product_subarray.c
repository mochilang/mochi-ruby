// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long max_product_subarray(long long * numbers, size_t numbers_len);
int main(void);

long long max_product_subarray(long long * numbers, size_t numbers_len) {
    if (numbers_len == 0LL) {
        return 0LL;
    }
    long long max_till_now = numbers[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
    long long min_till_now = numbers[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
    long long max_prod = numbers[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < numbers_len) {
        long long number = numbers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
        if (number < 0LL) {
            long long temp = max_till_now;
            max_till_now = min_till_now;
            min_till_now = temp;
        }
        long long prod_max = max_till_now * number;
        if (number > prod_max) {
            max_till_now = number;
        } else {
            max_till_now = prod_max;
        }
        long long prod_min = min_till_now * number;
        if (number < prod_min) {
            min_till_now = number;
        } else {
            min_till_now = prod_min;
        }
        if (max_till_now > max_prod) {
            max_prod = max_till_now;
        }
        i = i + 1LL;
    }
    return max_prod;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", max_product_subarray(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = -2.0; tmp[3] = 4LL; tmp;}), 4));
        printf("%d\n", max_product_subarray(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -2.0; tmp[1] = 0LL; tmp[2] = -1.0; tmp;}), 3));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
