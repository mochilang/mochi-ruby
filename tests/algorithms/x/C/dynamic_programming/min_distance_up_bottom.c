// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long min3(long long a, long long b, long long c);
long long helper(const char* word1, const char* word2, long long * * cache, size_t cache_len, size_t* cache_lens, size_t cache_lens_len, long long i, long long j, long long len1, long long len2);
long long min_distance_up_bottom(const char* word1, const char* word2);
int main(void);

long long min3(long long a, long long b, long long c) {
    long long m = a;
    if (b < m) {
        m = b;
    }
    if (c < m) {
        m = c;
    }
    return m;
}

long long helper(const char* word1, const char* word2, long long * * cache, size_t cache_len, size_t* cache_lens, size_t cache_lens_len, long long i, long long j, long long len1, long long len2) {
    if (i >= len1) {
        return len2 - j;
    }
    if (j >= len2) {
        return len1 - i;
    }
    if (cache[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cache_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? cache_lens[i] + _mochi_idx : _mochi_idx;})] != -1LL) {
        return cache[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cache_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? cache_lens[i] + _mochi_idx : _mochi_idx;})];
    }
    long long diff = 0LL;
    if (strcmp(_substring(word1, i, i + 1LL), _substring(word2, j, j + 1LL)) != 0) {
        diff = 1LL;
    }
    long long delete_cost = 1LL + helper(word1, word2, cache, cache_len, cache_lens, cache_len, i + 1LL, j, len1, len2);
    long long insert_cost = 1LL + helper(word1, word2, cache, cache_len, cache_lens, cache_len, i, j + 1LL, len1, len2);
    long long replace_cost = diff + helper(word1, word2, cache, cache_len, cache_lens, cache_len, i + 1LL, j + 1LL, len1, len2);
    cache[(int)(i)][(int)(j)] = min3(delete_cost, insert_cost, replace_cost);
    return cache[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cache_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? cache_lens[i] + _mochi_idx : _mochi_idx;})];
}

long long min_distance_up_bottom(const char* word1, const char* word2) {
    long long len1 = strlen(word1);
    long long len2 = strlen(word2);
    long long **cache = NULL;
    size_t cache_len = 0;
    size_t *cache_lens = NULL;
    size_t cache_lens_len = 0;
    for (int _ = 0LL; _ < len1; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < len2; _2++) {
            row = list_append_long_long(row, &row_len, -1LL);
        }
        cache = list_append_long_longptr(cache, &cache_len, row);
        cache_lens = list_append_szt(cache_lens, &cache_lens_len, row_len);
    }
    return helper(word1, word2, cache, cache_len, cache_lens, cache_len, 0LL, 0LL, len1, len2);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(min_distance_up_bottom("intention", "execution")));
        puts(str_int(min_distance_up_bottom("intention", "")));
        puts(str_int(min_distance_up_bottom("", "")));
        puts(str_int(min_distance_up_bottom("zooicoarchaeologist", "zoologist")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
