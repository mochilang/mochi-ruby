// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long min_partitions(const char* s);
int main(void);

long long min_partitions(const char* s) {
    long long n = strlen(s);
    long long *cut = NULL;
    size_t cut_len = 0;
    long long i = 0LL;
    while (i < n) {
        cut = list_append_long_long(cut, &cut_len, 0LL);
        i = i + 1LL;
    }
    long long **pal = NULL;
    size_t pal_len = 0;
    size_t *pal_lens = NULL;
    size_t pal_lens_len = 0;
    i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        pal = list_append_long_longptr(pal, &pal_len, row);
        pal_lens = list_append_szt(pal_lens, &pal_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        long long mincut = i;
        long long j = 0LL;
        while (j <= i) {
            if ((strcmp((const char[]){s[i], 0}, (const char[]){s[j], 0}) == 0) && (((i - j) < 2LL) || pal[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? pal_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? pal_lens[j + 1LL] + _mochi_idx : _mochi_idx;})])) {
                pal[(int)(j)][(int)(i)] = 1LL;
                if (j == 0LL) {
                    mincut = 0LL;
                } else {
                    long long candidate = cut[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? cut_len + _mochi_idx : _mochi_idx;})] + 1LL;
                    if (candidate < mincut) {
                        mincut = candidate;
                    }
                }
            }
            j = j + 1LL;
        }
        cut[(int)(i)] = mincut;
        i = i + 1LL;
    }
    return cut[(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? cut_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", min_partitions("aab"));
        printf("%d\n", min_partitions("aaa"));
        printf("%d\n", min_partitions("ababbbabbababa"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
