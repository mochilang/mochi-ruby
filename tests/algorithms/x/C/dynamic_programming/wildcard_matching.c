// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_bool_list_len;
size_t make_bool_matrix_len;
size_t *make_bool_matrix_lens;

size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * make_bool_list(long long n);
long long * * make_bool_matrix(long long rows, long long cols);
long long is_match(const char* s, const char* p);
void print_bool(long long b);
int main(void);

long long * make_bool_list(long long n) {
    long long *row = NULL;
    size_t row_len = 0;
    long long i = 0LL;
    while (i < n) {
        row = list_append_long_long(row, &row_len, 0LL);
        i = i + 1LL;
    }
    return make_bool_list_len = row_len, row;
}

long long * * make_bool_matrix(long long rows, long long cols) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        matrix = list_append_intptr(matrix, &matrix_len, make_bool_list(cols));
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, make_bool_list_len);
        i = i + 1LL;
    }
    return make_bool_matrix_lens = matrix_lens, make_bool_matrix_len = matrix_len, matrix;
}

long long is_match(const char* s, const char* p) {
    long long n = strlen(s);
    long long m = strlen(p);
    long long **dp = make_bool_matrix(n + 1LL, m + 1LL);
    size_t dp_len = make_bool_matrix_len;
    size_t *dp_lens = make_bool_matrix_lens;
    size_t dp_lens_len = make_bool_matrix_len;
    dp[(int)(0LL)][(int)(0LL)] = 1LL;
    long long j = 1LL;
    while (j <= m) {
        if (strcmp(_substring(p, j - 1LL, j), "*") == 0) {
            dp[(int)(0LL)][(int)(j)] = dp[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
        }
        j = j + 1LL;
    }
    long long i = 1LL;
    while (i <= n) {
        long long j2 = 1LL;
        while (j2 <= m) {
            const char* pc = _substring(p, j2 - 1LL, j2);
            const char* sc = _substring(s, i - 1LL, i);
            if ((strcmp(pc, sc) == 0) || (strcmp(pc, "?") == 0)) {
                dp[(int)(i)][(int)(j2)] = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2 - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
            } else {
                if (strcmp(pc, "*") == 0) {
                    if (dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] || dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2 - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
                        dp[(int)(i)][(int)(j2)] = 1LL;
                    }
                }
            }
            j2 = j2 + 1LL;
        }
        i = i + 1LL;
    }
    return dp[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = m; _mochi_idx < 0 ? dp_lens[(int)(n)] + _mochi_idx : _mochi_idx;})];
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        print_bool(is_match("abc", "a*c"));
        print_bool(is_match("abc", "a*d"));
        print_bool(is_match("baaabab", "*****ba*****ab"));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
