// Generated by Mochi 0.10.32 on 2025-08-13 07:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_2d_len;
size_t *make_2d_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct MatrixChainResult MatrixChainResult;

struct MatrixChainResult {
    long long **matrix;
    size_t matrix_len;
    size_t *matrix_lens;
    size_t matrix_lens_len;
    long long **solution;
    size_t solution_len;
    size_t *solution_lens;
    size_t solution_lens_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * make_2d(long long n);
MatrixChainResult matrix_chain_order(long long * arr, size_t arr_len);
const char* optimal_parenthesization(long long * * s, size_t s_len, size_t* s_lens, size_t s_lens_len, long long i, long long j);
void user_main();
int main(void);

long long * * make_2d(long long n) {
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        res = list_append_long_longptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return make_2d_lens = res_lens, make_2d_len = res_len, res;
}

MatrixChainResult matrix_chain_order(long long * arr, size_t arr_len) {
    long long n = arr_len;
    long long **m = make_2d(n);
    size_t m_len = make_2d_len;
    size_t *m_lens = make_2d_lens;
    size_t m_lens_len = make_2d_len;
    long long **s = make_2d(n);
    size_t s_len = make_2d_len;
    size_t *s_lens = make_2d_lens;
    size_t s_lens_len = make_2d_len;
    long long chain_length = 2LL;
    while (chain_length < n) {
        long long a = 1LL;
        while (a < ((n - chain_length) + 1LL)) {
            long long b = (a + chain_length) - 1LL;
            m[(int)(a)][(int)(b)] = 1000000000LL;
            long long c = a;
            while (c < b) {
                long long cost = (m[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = c; _mochi_idx < 0 ? m_lens[a] + _mochi_idx : _mochi_idx;})] + m[(int)({long long _mochi_idx = c + 1LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = b; _mochi_idx < 0 ? m_lens[c + 1LL] + _mochi_idx : _mochi_idx;})]) + ((arr[(int)({long long _mochi_idx = a - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] * arr[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) * arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
                if (cost < m[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = b; _mochi_idx < 0 ? m_lens[a] + _mochi_idx : _mochi_idx;})]) {
                    m[(int)(a)][(int)(b)] = cost;
                    s[(int)(a)][(int)(b)] = c;
                }
                c = c + 1LL;
            }
            a = a + 1LL;
        }
        chain_length = chain_length + 1LL;
    }
    return (MatrixChainResult){.matrix = m, .matrix_len = m_len, .matrix_lens = m_lens, .matrix_lens_len = m_len, .solution = s, .solution_len = s_len, .solution_lens = s_lens, .solution_lens_len = s_len};
}

const char* optimal_parenthesization(long long * * s, size_t s_len, size_t* s_lens, size_t s_lens_len, long long i, long long j) {
    if (i == j) {
        return str_concat("A", str_int(i));
    } else {
        const char* left = optimal_parenthesization(s, s_len, s_lens, s_len, i, s[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? s_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? s_lens[i] + _mochi_idx : _mochi_idx;})]);
        const char* right = optimal_parenthesization(s, s_len, s_lens, s_len, s[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? s_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? s_lens[i] + _mochi_idx : _mochi_idx;})] + 1LL, j);
        return str_concat(str_concat(str_concat(str_concat("( ", left), " "), right), " )");
    }
}

void user_main() {
    long long *arr = NULL;
    size_t arr_len = 0;
    arr = list_append_long_long(arr, &arr_len, 30LL);
    arr = list_append_long_long(arr, &arr_len, 35LL);
    arr = list_append_long_long(arr, &arr_len, 15LL);
    arr = list_append_long_long(arr, &arr_len, 5LL);
    arr = list_append_long_long(arr, &arr_len, 10LL);
    arr = list_append_long_long(arr, &arr_len, 20LL);
    arr = list_append_long_long(arr, &arr_len, 25LL);
    long long n = 7LL;
    MatrixChainResult res = matrix_chain_order(arr, arr_len);
    long long **m = res.matrix;
    size_t m_len = res.matrix_len;
    size_t *m_lens = res.matrix_lens;
    size_t m_lens_len = res.matrix_len;
    long long **s = res.solution;
    size_t s_len = res.solution_len;
    size_t *s_lens = res.solution_lens;
    size_t s_lens_len = res.solution_len;
    puts(str_concat("No. of Operation required: ", str_int(m[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? m_lens[1LL] + _mochi_idx : _mochi_idx;})])));
    const char* seq = optimal_parenthesization(s, s_len, s_lens, s_len, 1LL, n - 1LL);
    puts(seq);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
