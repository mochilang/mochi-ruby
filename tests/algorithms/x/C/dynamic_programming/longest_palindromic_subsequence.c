// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* reverse(const char* s);
long long max_int(long long a, long long b);
long long longest_palindromic_subsequence(const char* s);
int main(void);

const char* reverse(const char* s) {
    const char* result = "";
    long long i = strlen(s) - 1LL;
    while (i >= 0LL) {
        result = str_concat(result, _substring(s, i, i + 1LL));
        i = i - 1LL;
    }
    return result;
}

long long max_int(long long a, long long b) {
    if (a > b) {
        return a;
    }
    return b;
}

long long longest_palindromic_subsequence(const char* s) {
    const char* rev = reverse(s);
    long long n = strlen(s);
    long long m = strlen(rev);
    long long **dp = NULL;
    size_t dp_len = 0;
    size_t *dp_lens = NULL;
    size_t dp_lens_len = 0;
    long long i = 0LL;
    while (i <= n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= m) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        dp = list_append_intptr(dp, &dp_len, row);
        dp_lens = list_append_szt(dp_lens, &dp_lens_len, row_len);
        i = i + 1LL;
    }
    i = 1LL;
    while (i <= n) {
        long long j = 1LL;
        while (j <= m) {
            const char* a_char = _substring(s, i - 1LL, i);
            const char* b_char = _substring(rev, j - 1LL, j);
            if (strcmp(a_char, b_char) == 0) {
                dp[(int)(i)][(int)(j)] = 1LL + dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
            } else {
                dp[(int)(i)][(int)(j)] = max_int(dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})], dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return dp[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = m; _mochi_idx < 0 ? dp_lens[(int)(n)] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(longest_palindromic_subsequence("bbbab")));
        puts(str_int(longest_palindromic_subsequence("bbabcbcab")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
