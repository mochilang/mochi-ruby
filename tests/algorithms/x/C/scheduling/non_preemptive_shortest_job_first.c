// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t calculate_waitingtime_len;
size_t calculate_turnaroundtime_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long no_of_processes = 4LL;
long long burst_time_init[4] = {2LL, 5LL, 3LL, 7LL};
long long *burst_time = burst_time_init;
size_t burst_time_len = 4;
long long arrival_time_init[4] = {0LL, 0LL, 0LL, 0LL};
long long *arrival_time = arrival_time_init;
size_t arrival_time_len = 4;
long long *waiting_time = NULL;
size_t waiting_time_len = 0;
long long *turn_around_time = NULL;
size_t turn_around_time_len = 0;
long long i = 0LL;
double avg_wait = 0;
double avg_turn = 0;

long long * calculate_waitingtime(long long * arrival_time, size_t arrival_time_len, long long * burst_time, size_t burst_time_len, long long no_of_processes);
long long * calculate_turnaroundtime(long long * burst_time, size_t burst_time_len, long long no_of_processes, long long * waiting_time, size_t waiting_time_len);
double average(long long * values, size_t values_len);
int main(void);

long long * calculate_waitingtime(long long * arrival_time, size_t arrival_time_len, long long * burst_time, size_t burst_time_len, long long no_of_processes) {
    long long *waiting_time = NULL;
    size_t waiting_time_len = 0;
    long long *remaining_time = NULL;
    size_t remaining_time_len = 0;
    long long i = 0LL;
    while (i < no_of_processes) {
        waiting_time = list_append_long_long(waiting_time, &waiting_time_len, 0LL);
        remaining_time = list_append_long_long(remaining_time, &remaining_time_len, burst_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long completed = 0LL;
    long long total_time = 0LL;
    while (completed != no_of_processes) {
        long long *ready_process = NULL;
        size_t ready_process_len = 0;
        double target_process = -1.0;
        long long j = 0LL;
        while (j < no_of_processes) {
            if ((arrival_time[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arrival_time_len + _mochi_idx : _mochi_idx;})] <= total_time) && (remaining_time[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})] > 0LL)) {
                ready_process = list_append_long_long(ready_process, &ready_process_len, j);
            }
            j = j + 1LL;
        }
        if (ready_process_len > 0LL) {
            target_process = ready_process[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? ready_process_len + _mochi_idx : _mochi_idx;})];
            long long k = 0LL;
            while (k < ready_process_len) {
                long long idx = ready_process[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? ready_process_len + _mochi_idx : _mochi_idx;})];
                if (remaining_time[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})] < remaining_time[(int)({long long _mochi_idx = target_process; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})]) {
                    target_process = idx;
                }
                k = k + 1LL;
            }
            total_time = total_time + burst_time[(int)({long long _mochi_idx = target_process; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})];
            completed = completed + 1LL;
            remaining_time[(int)(target_process)] = 0LL;
            waiting_time[(int)(target_process)] = (total_time - arrival_time[(int)({long long _mochi_idx = target_process; _mochi_idx < 0 ? arrival_time_len + _mochi_idx : _mochi_idx;})]) - burst_time[(int)({long long _mochi_idx = target_process; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})];
        } else {
            total_time = total_time + 1LL;
        }
    }
    return calculate_waitingtime_len = waiting_time_len, waiting_time;
}

long long * calculate_turnaroundtime(long long * burst_time, size_t burst_time_len, long long no_of_processes, long long * waiting_time, size_t waiting_time_len) {
    long long *turn_around_time = NULL;
    size_t turn_around_time_len = 0;
    long long i = 0LL;
    while (i < no_of_processes) {
        turn_around_time = list_append_long_long(turn_around_time, &turn_around_time_len, burst_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})] + waiting_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? waiting_time_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return calculate_turnaroundtime_len = turn_around_time_len, turn_around_time;
}

double average(long long * values, size_t values_len) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < values_len) {
        total = total + values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return (double)(total) / (double)(values_len);
}

int main(void) {
    {
        long long __start = _now();
        puts("[TEST CASE 01]");
        waiting_time = calculate_waitingtime(arrival_time, arrival_time_len, burst_time, burst_time_len, no_of_processes);
        waiting_time_len = calculate_waitingtime_len;
        turn_around_time = calculate_turnaroundtime(burst_time, burst_time_len, no_of_processes, waiting_time, waiting_time_len);
        turn_around_time_len = calculate_turnaroundtime_len;
        puts("PID\tBurst Time\tArrival Time\tWaiting Time\tTurnaround Time");
        while (i < no_of_processes) {
            long long pid = i + 1LL;
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_int(pid), "\t"), str_int(burst_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})])), "\t\t\t"), str_int(arrival_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arrival_time_len + _mochi_idx : _mochi_idx;})])), "\t\t\t\t"), str_int(waiting_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? waiting_time_len + _mochi_idx : _mochi_idx;})])), "\t\t\t\t"), str_int(turn_around_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? turn_around_time_len + _mochi_idx : _mochi_idx;})])));
            i = i + 1LL;
        }
        avg_wait = average(waiting_time, waiting_time_len);
        avg_turn = average(turn_around_time, turn_around_time_len);
        puts(str_concat("\nAverage waiting time = ", str_float(avg_wait)));
        puts(str_concat("Average turnaround time = ", str_float(avg_turn)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
