// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t calculate_waitingtime_len;
size_t calculate_turnaroundtime_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * calculate_waitingtime(long long * arrival_time, size_t arrival_time_len, long long * burst_time, size_t burst_time_len, long long no_of_processes);
long long * calculate_turnaroundtime(long long * burst_time, size_t burst_time_len, long long no_of_processes, long long * waiting_time, size_t waiting_time_len);
double to_float(long long x);
const char* calculate_average_times(long long * waiting_time, size_t waiting_time_len, long long * turn_around_time, size_t turn_around_time_len, long long no_of_processes);
int main(void);

long long * calculate_waitingtime(long long * arrival_time, size_t arrival_time_len, long long * burst_time, size_t burst_time_len, long long no_of_processes) {
    long long *remaining_time = NULL;
    size_t remaining_time_len = 0;
    long long i = 0LL;
    while (i < no_of_processes) {
        remaining_time = list_append_long_long(remaining_time, &remaining_time_len, burst_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long *waiting_time = NULL;
    size_t waiting_time_len = 0;
    i = 0LL;
    while (i < no_of_processes) {
        waiting_time = list_append_long_long(waiting_time, &waiting_time_len, 0LL);
        i = i + 1LL;
    }
    long long complete = 0LL;
    long long increment_time = 0LL;
    long long minm = 1000000000LL;
    long long short_ = 0LL;
    long long check = 0LL;
    while (complete != no_of_processes) {
        long long j = 0LL;
        while (j < no_of_processes) {
            if (((arrival_time[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arrival_time_len + _mochi_idx : _mochi_idx;})] <= increment_time) && (remaining_time[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})] > 0LL)) && (remaining_time[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})] < minm)) {
                minm = remaining_time[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})];
                short_ = j;
                check = 1LL;
            }
            j = j + 1LL;
        }
        if (!(check)) {
            increment_time = increment_time + 1LL;
            continue;
        }
        remaining_time[(int)(short_)] = remaining_time[(int)({long long _mochi_idx = short_; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})] - 1LL;
        minm = remaining_time[(int)({long long _mochi_idx = short_; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})];
        if (minm == 0LL) {
            minm = 1000000000LL;
        }
        if (remaining_time[(int)({long long _mochi_idx = short_; _mochi_idx < 0 ? remaining_time_len + _mochi_idx : _mochi_idx;})] == 0LL) {
            complete = complete + 1LL;
            check = 0LL;
            long long finish_time = increment_time + 1LL;
            long long finar = finish_time - arrival_time[(int)({long long _mochi_idx = short_; _mochi_idx < 0 ? arrival_time_len + _mochi_idx : _mochi_idx;})];
            waiting_time[(int)(short_)] = finar - burst_time[(int)({long long _mochi_idx = short_; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})];
            if (waiting_time[(int)({long long _mochi_idx = short_; _mochi_idx < 0 ? waiting_time_len + _mochi_idx : _mochi_idx;})] < 0LL) {
                waiting_time[(int)(short_)] = 0LL;
            }
        }
        increment_time = increment_time + 1LL;
    }
    return calculate_waitingtime_len = waiting_time_len, waiting_time;
}

long long * calculate_turnaroundtime(long long * burst_time, size_t burst_time_len, long long no_of_processes, long long * waiting_time, size_t waiting_time_len) {
    long long *turn_around_time = NULL;
    size_t turn_around_time_len = 0;
    long long i = 0LL;
    while (i < no_of_processes) {
        turn_around_time = list_append_long_long(turn_around_time, &turn_around_time_len, burst_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_time_len + _mochi_idx : _mochi_idx;})] + waiting_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? waiting_time_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return calculate_turnaroundtime_len = turn_around_time_len, turn_around_time;
}

double to_float(long long x) {
    return x * 1.0;
}

const char* calculate_average_times(long long * waiting_time, size_t waiting_time_len, long long * turn_around_time, size_t turn_around_time_len, long long no_of_processes) {
    long long total_waiting_time = 0LL;
    long long total_turn_around_time = 0LL;
    long long i = 0LL;
    while (i < no_of_processes) {
        total_waiting_time = total_waiting_time + waiting_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? waiting_time_len + _mochi_idx : _mochi_idx;})];
        total_turn_around_time = total_turn_around_time + turn_around_time[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? turn_around_time_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    double avg_wait = to_float(total_waiting_time) / to_float(no_of_processes);
    double avg_turn = to_float(total_turn_around_time) / to_float(no_of_processes);
    puts(str_concat("Average waiting time = ", str_float(avg_wait)));
    puts(str_concat("Average turn around time = ", str_float(avg_turn)));
}

int main(void) {
    {
        long long __start = _now();
        long long* __tmp6 = calculate_waitingtime((long long[]){1LL, 2LL, 3LL, 4LL}, 4, (long long[]){3LL, 3LL, 5LL, 1LL}, 4, 4LL);
        puts(str_list_int(__tmp6, calculate_waitingtime_len));
        long long* __tmp7 = calculate_waitingtime((long long[]){1LL, 2LL, 3LL}, 3, (long long[]){2LL, 5LL, 1LL}, 3, 3LL);
        puts(str_list_int(__tmp7, calculate_waitingtime_len));
        long long* __tmp8 = calculate_waitingtime((long long[]){2LL, 3LL}, 2, (long long[]){5LL, 1LL}, 2, 2LL);
        puts(str_list_int(__tmp8, calculate_waitingtime_len));
        long long* __tmp9 = calculate_turnaroundtime((long long[]){3LL, 3LL, 5LL, 1LL}, 4, 4LL, (long long[]){0LL, 3LL, 5LL, 0LL}, 4);
        puts(str_list_int(__tmp9, calculate_turnaroundtime_len));
        long long* __tmp10 = calculate_turnaroundtime((long long[]){3LL, 3LL}, 2, 2LL, (long long[]){0LL, 3LL}, 2);
        puts(str_list_int(__tmp10, calculate_turnaroundtime_len));
        long long* __tmp11 = calculate_turnaroundtime((long long[]){8LL, 10LL, 1LL}, 3, 3LL, (long long[]){1LL, 0LL, 3LL}, 3);
        puts(str_list_int(__tmp11, calculate_turnaroundtime_len));
        calculate_average_times((long long[]){0LL, 3LL, 5LL, 0LL}, 4, (long long[]){3LL, 6LL, 10LL, 1LL}, 4, 4LL);
        calculate_average_times((long long[]){2LL, 3LL}, 2, (long long[]){3LL, 6LL}, 2, 2LL);
        calculate_average_times((long long[]){10LL, 4LL, 3LL}, 3, (long long[]){2LL, 7LL, 6LL}, 3, 3LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
