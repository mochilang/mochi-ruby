// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_jobs_by_profit_len;
size_t job_sequencing_with_deadlines_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Job Job;

struct Job {
    long long id;
    long long deadline;
    long long profit;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Job* list_append_Job(Job *arr, size_t *len, Job val) {
    arr = realloc(arr, (*len + 1) * sizeof(Job));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Job *jobs1 = NULL;
size_t jobs1_len = 0;
Job *jobs2 = NULL;
size_t jobs2_len = 0;

Job * sort_jobs_by_profit(Job * jobs, size_t jobs_len);
long long max_deadline(Job * jobs, size_t jobs_len);
long long * job_sequencing_with_deadlines(Job * jobs, size_t jobs_len);
int main(void);

Job * sort_jobs_by_profit(Job * jobs, size_t jobs_len) {
    Job *js = jobs;
    size_t js_len = jobs_len;
    long long i = 0LL;
    while (i < js_len) {
        long long j = 0LL;
        while (j < ((js_len - i) - 1LL)) {
            Job a = js[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? js_len + _mochi_idx : _mochi_idx;})];
            Job b = js[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? js_len + _mochi_idx : _mochi_idx;})];
            if (a.profit < b.profit) {
                js[(int)(j)] = b;
                js[(int)(j + 1LL)] = a;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_jobs_by_profit_len = js_len, js;
}

long long max_deadline(Job * jobs, size_t jobs_len) {
    long long max_d = 0LL;
    long long i = 0LL;
    while (i < jobs_len) {
        Job job = jobs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? jobs_len + _mochi_idx : _mochi_idx;})];
        long long d = job.deadline;
        if (d > max_d) {
            max_d = d;
        }
        i = i + 1LL;
    }
    return max_d;
}

long long * job_sequencing_with_deadlines(Job * jobs, size_t jobs_len) {
    Job *js = sort_jobs_by_profit(jobs, jobs_len);
    size_t js_len = sort_jobs_by_profit_len;
    long long max_d = max_deadline(js, js_len);
    long long *time_slots = NULL;
    size_t time_slots_len = 0;
    long long t = 0LL;
    while (t < max_d) {
        time_slots = list_append_long_long(time_slots, &time_slots_len, -1LL);
        t = t + 1LL;
    }
    long long count_ = 0LL;
    long long max_profit = 0LL;
    long long i = 0LL;
    while (i < js_len) {
        Job job = js[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? js_len + _mochi_idx : _mochi_idx;})];
        long long j = job.deadline - 1LL;
        while (j >= 0LL) {
            if (time_slots[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? time_slots_len + _mochi_idx : _mochi_idx;})] == (0LL - 1LL)) {
                time_slots[(int)(j)] = job.id;
                count_ = count_ + 1LL;
                max_profit = max_profit + job.profit;
                break;
            }
            j = j - 1LL;
        }
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    result = list_append_long_long(result, &result_len, count_);
    result = list_append_long_long(result, &result_len, max_profit);
    return job_sequencing_with_deadlines_len = result_len, result;
}

int main(void) {
    {
        long long __start = _now();
        jobs1 = list_append_Job(jobs1, &jobs1_len, (Job){.id = 1LL, .deadline = 4LL, .profit = 20LL});
        jobs1 = list_append_Job(jobs1, &jobs1_len, (Job){.id = 2LL, .deadline = 1LL, .profit = 10LL});
        jobs1 = list_append_Job(jobs1, &jobs1_len, (Job){.id = 3LL, .deadline = 1LL, .profit = 40LL});
        jobs1 = list_append_Job(jobs1, &jobs1_len, (Job){.id = 4LL, .deadline = 1LL, .profit = 30LL});
        puts(({long long *__tmp2 = job_sequencing_with_deadlines(jobs1, jobs1_len); char *__res = str_list_int(__tmp2, job_sequencing_with_deadlines_len); __res;}));
        jobs2 = list_append_Job(jobs2, &jobs2_len, (Job){.id = 1LL, .deadline = 2LL, .profit = 100LL});
        jobs2 = list_append_Job(jobs2, &jobs2_len, (Job){.id = 2LL, .deadline = 1LL, .profit = 19LL});
        jobs2 = list_append_Job(jobs2, &jobs2_len, (Job){.id = 3LL, .deadline = 2LL, .profit = 27LL});
        jobs2 = list_append_Job(jobs2, &jobs2_len, (Job){.id = 4LL, .deadline = 1LL, .profit = 25LL});
        jobs2 = list_append_Job(jobs2, &jobs2_len, (Job){.id = 5LL, .deadline = 1LL, .profit = 15LL});
        puts(({long long *__tmp3 = job_sequencing_with_deadlines(jobs2, jobs2_len); char *__res = str_list_int(__tmp3, job_sequencing_with_deadlines_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
