// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t max_tasks_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * max_tasks(long long * * tasks_info, size_t tasks_info_len, size_t* tasks_info_lens, size_t tasks_info_lens_len);
const char* user_main();
int main(void);

long long * max_tasks(long long * * tasks_info, size_t tasks_info_len, size_t* tasks_info_lens, size_t tasks_info_lens_len) {
    long long *order = NULL;
    size_t order_len = 0;
    long long i = 0LL;
    while (i < tasks_info_len) {
        order = list_append_long_long(order, &order_len, i);
        i = i + 1LL;
    }
    long long n = order_len;
    i = 0LL;
    while (i < n) {
        long long j = i + 1LL;
        while (j < n) {
            if (tasks_info[(int)({long long _mochi_idx = order[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? tasks_info_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? tasks_info_lens[(int)(order[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})])] + _mochi_idx : _mochi_idx;})] > tasks_info[(int)({long long _mochi_idx = order[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? tasks_info_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? tasks_info_lens[(int)(order[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})])] + _mochi_idx : _mochi_idx;})]) {
                long long tmp = order[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})];
                order[(int)(i)] = order[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})];
                order[(int)(j)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    long long pos = 1LL;
    i = 0LL;
    while (i < n) {
        long long id = order[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? order_len + _mochi_idx : _mochi_idx;})];
        long long deadline = tasks_info[(int)({long long _mochi_idx = id; _mochi_idx < 0 ? tasks_info_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? tasks_info_lens[(int)(id)] + _mochi_idx : _mochi_idx;})];
        if (deadline >= pos) {
            result = list_append_long_long(result, &result_len, id);
        }
        i = i + 1LL;
        pos = pos + 1LL;
    }
    return max_tasks_len = result_len, result;
}

const char* user_main() {
    long long ex1_0[2] = {4LL, 20LL};
    long long ex1_1[2] = {1LL, 10LL};
    long long ex1_2[2] = {1LL, 40LL};
    long long ex1_3[2] = {1LL, 30LL};
    long long *ex1_init[4] = {ex1_0, ex1_1, ex1_2, ex1_3};
    long long **ex1 = ex1_init;
    size_t ex1_len = 4;
    size_t ex1_lens_init[4] = {2, 2, 2, 2};
    size_t *ex1_lens = ex1_lens_init;
    size_t ex1_lens_len = 4;
    long long ex2_0[2] = {1LL, 10LL};
    long long ex2_1[2] = {2LL, 20LL};
    long long ex2_2[2] = {3LL, 30LL};
    long long ex2_3[2] = {2LL, 40LL};
    long long *ex2_init[4] = {ex2_0, ex2_1, ex2_2, ex2_3};
    long long **ex2 = ex2_init;
    size_t ex2_len = 4;
    size_t ex2_lens_init[4] = {2, 2, 2, 2};
    size_t *ex2_lens = ex2_lens_init;
    size_t ex2_lens_len = 4;
    puts(({long long *__tmp2 = max_tasks(ex1, ex1_len, ex1_lens, ex1_len); char *__res = str_list_int(__tmp2, max_tasks_len); __res;}));
    puts(({long long *__tmp3 = max_tasks(ex2, ex2_len, ex2_lens, ex2_len); char *__res = str_list_int(__tmp3, max_tasks_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
