// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t calculate_waiting_times_len;
size_t calculate_turn_around_times_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * calculate_waiting_times(long long * burst_times, size_t burst_times_len);
long long * calculate_turn_around_times(long long * burst_times, size_t burst_times_len, long long * waiting_times, size_t waiting_times_len);
double mean(long long * values, size_t values_len);
const char* format_float_5(double x);
void user_main();
int main(void);

long long * calculate_waiting_times(long long * burst_times, size_t burst_times_len) {
    long long quantum = 2LL;
    long long *rem = NULL;
    size_t rem_len = 0;
    long long i = 0LL;
    while (i < burst_times_len) {
        rem = list_append_long_long(rem, &rem_len, burst_times[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_times_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long *waiting = NULL;
    size_t waiting_len = 0;
    i = 0LL;
    while (i < burst_times_len) {
        waiting = list_append_long_long(waiting, &waiting_len, 0LL);
        i = i + 1LL;
    }
    long long t = 0LL;
    while (1) {
        long long done = 1LL;
        long long j = 0LL;
        while (j < burst_times_len) {
            if (rem[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rem_len + _mochi_idx : _mochi_idx;})] > 0LL) {
                done = 0LL;
                if (rem[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rem_len + _mochi_idx : _mochi_idx;})] > quantum) {
                    t = t + quantum;
                    rem[(int)(j)] = rem[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rem_len + _mochi_idx : _mochi_idx;})] - quantum;
                } else {
                    t = t + rem[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rem_len + _mochi_idx : _mochi_idx;})];
                    waiting[(int)(j)] = t - burst_times[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? burst_times_len + _mochi_idx : _mochi_idx;})];
                    rem[(int)(j)] = 0LL;
                }
            }
            j = j + 1LL;
        }
        if (done) {
            return calculate_waiting_times_len = waiting_len, waiting;
        }
    }
    return calculate_waiting_times_len = waiting_len, waiting;
}

long long * calculate_turn_around_times(long long * burst_times, size_t burst_times_len, long long * waiting_times, size_t waiting_times_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < burst_times_len) {
        result = list_append_long_long(result, &result_len, burst_times[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_times_len + _mochi_idx : _mochi_idx;})] + waiting_times[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? waiting_times_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return calculate_turn_around_times_len = result_len, result;
}

double mean(long long * values, size_t values_len) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < values_len) {
        total = total + values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return (double)(total) / (double)(values_len);
}

const char* format_float_5(double x) {
    long long scaled = (int)((x * 100000.0) + 0.5);
    long long int_part = scaled / 100000LL;
    long long frac_part = scaled % 100000LL;
    const char* frac_str = str_int(frac_part);
    while (strlen(frac_str) < 5LL) {
        frac_str = str_concat("0", frac_str);
    }
    return str_concat(str_concat(str_int(int_part), "."), frac_str);
}

void user_main() {
    long long *burst_times = NULL;
    size_t burst_times_len = 0;
    burst_times = list_append_long_long(burst_times, &burst_times_len, 3LL);
    burst_times = list_append_long_long(burst_times, &burst_times_len, 5LL);
    burst_times = list_append_long_long(burst_times, &burst_times_len, 7LL);
    long long *waiting_times = calculate_waiting_times(burst_times, burst_times_len);
    size_t waiting_times_len = calculate_waiting_times_len;
    long long *turn_around_times = calculate_turn_around_times(burst_times, burst_times_len, waiting_times, waiting_times_len);
    size_t turn_around_times_len = calculate_turn_around_times_len;
    puts("Process ID \tBurst Time \tWaiting Time \tTurnaround Time");
    long long i = 0LL;
    while (i < 3LL) {
        const char* line = str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("  ", str_int(i + 1LL)), "\t\t  "), str_int(burst_times[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? burst_times_len + _mochi_idx : _mochi_idx;})])), "\t\t  "), str_int(waiting_times[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? waiting_times_len + _mochi_idx : _mochi_idx;})])), "\t\t  "), str_int(turn_around_times[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? turn_around_times_len + _mochi_idx : _mochi_idx;})]));
        puts(line);
        i = i + 1LL;
    }
    puts("");
    puts(str_concat("Average waiting time = ", format_float_5(mean(waiting_times, waiting_times_len))));
    puts(str_concat("Average turn around time = ", format_float_5(mean(turn_around_times, turn_around_times_len))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
