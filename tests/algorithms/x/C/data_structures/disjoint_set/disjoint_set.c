// Generated by Mochi 0.10.32 on 2025-08-08 19:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct DS DS;
typedef struct FindResult FindResult;

struct DS {
    long long *parent;
    size_t parent_len;
    long long *rank;
    size_t rank_len;
};

struct FindResult {
    DS ds;
    long long root;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

DS ds = (DS){.parent = NULL, .parent_len = 0, .rank = NULL, .rank_len = 0};
long long i = 0LL;

DS make_set(DS ds, long long x);
FindResult find_set(DS ds, long long x);
DS union_set(DS ds, long long x, long long y);
long long same_python_set(long long a, long long b);
int main(void);

DS make_set(DS ds, long long x) {
    long long *p = ds.parent;
    size_t p_len = ds.parent_len;
    long long *r = ds.rank;
    size_t r_len = ds.rank_len;
    p[(int)(x)] = x;
    r[(int)(x)] = 0LL;
    return (DS){.parent = p, .parent_len = p_len, .rank = r, .rank_len = r_len};
}

FindResult find_set(DS ds, long long x) {
    if (ds.parent[(int)(x)] == x) {
        return (FindResult){.ds = ds, .root = x};
    }
    FindResult res = find_set(ds, ds.parent[(int)(x)]);
    long long *p = res.ds.parent;
    size_t p_len = res.ds.parent_len;
    p[(int)(x)] = res.root;
    return (FindResult){.ds = (DS){.parent = p, .parent_len = p_len, .rank = res.ds.rank, .rank_len = res.ds.rank_len}, .root = res.root};
}

DS union_set(DS ds, long long x, long long y) {
    FindResult fx = find_set(ds, x);
    DS ds1 = fx.ds;
    long long x_root = fx.root;
    FindResult fy = find_set(ds1, y);
    DS ds2 = fy.ds;
    long long y_root = fy.root;
    if (x_root == y_root) {
        return ds2;
    }
    long long *p = ds2.parent;
    size_t p_len = ds2.parent_len;
    long long *r = ds2.rank;
    size_t r_len = ds2.rank_len;
    if (r[(int)(x_root)] > r[(int)(y_root)]) {
        p[(int)(y_root)] = x_root;
    } else {
        p[(int)(x_root)] = y_root;
        if (r[(int)(x_root)] == r[(int)(y_root)]) {
            r[(int)(y_root)] = r[(int)(y_root)] + 1LL;
        }
    }
    return (DS){.parent = p, .parent_len = p_len, .rank = r, .rank_len = r_len};
}

long long same_python_set(long long a, long long b) {
    if ((a < 3LL) && (b < 3LL)) {
        return 1LL;
    }
    if ((((a >= 3LL) && (a < 6LL)) && (b >= 3LL)) && (b < 6LL)) {
        return 1LL;
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        while (i < 6LL) {
            ds.parent = list_append_long_long(ds.parent, &ds.parent_len, 0LL);
            ds.rank = list_append_long_long(ds.rank, &ds.rank_len, 0LL);
            ds = make_set(ds, i);
            i = i + 1LL;
        }
        ds = union_set(ds, 0LL, 1LL);
        ds = union_set(ds, 1LL, 2LL);
        ds = union_set(ds, 3LL, 4LL);
        ds = union_set(ds, 3LL, 5LL);
        i = 0LL;
        while (i < 6LL) {
            long long j = 0LL;
            while (j < 6LL) {
                FindResult res_i = find_set(ds, i);
                ds = res_i.ds;
                long long root_i = res_i.root;
                FindResult res_j = find_set(ds, j);
                ds = res_j.ds;
                long long root_j = res_j.root;
                long long same = same_python_set(i, j);
                long long root_same = root_i == root_j;
                if (same) {
                    if (!(root_same)) {
                        panic("nodes should be in same set");
                    }
                } else {
                    if (root_same) {
                        panic("nodes should be in different sets");
                    }
                }
                j = j + 1LL;
            }
            i = i + 1LL;
        }
        i = 0LL;
        while (i < 6LL) {
            FindResult res = find_set(ds, i);
            ds = res.ds;
            puts(str_int(res.root));
            i = i + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
