// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t _slice_str_len;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

static const char* PRIORITY_keys[22] = {"^", "*", "/", "%", "+", "-"};
static int PRIORITY_vals[22] = {3, 2, 2, 2, 1, 1};
size_t PRIORITY_len = 6;
MapSI PRIORITY = { PRIORITY_keys, PRIORITY_vals, 6, 22 };
const char* LETTERS = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* DIGITS = "0123456789";

long long is_alpha(const char* ch);
long long is_digit(const char* ch);
const char* reverse_string(const char* s);
const char* infix_to_postfix(const char* infix);
const char* infix_to_prefix(const char* infix);
int main(void);

long long is_alpha(const char* ch) {
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        if (strcmp((const char[]){LETTERS[i], 0}, ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long is_digit(const char* ch) {
    long long i = 0LL;
    while (i < strlen(DIGITS)) {
        if (strcmp((const char[]){DIGITS[i], 0}, ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* reverse_string(const char* s) {
    const char* out = "";
    long long i = strlen(s) - 1LL;
    while (i >= 0LL) {
        out = str_concat(out, (const char[]){s[i], 0});
        i = i - 1LL;
    }
    return out;
}

const char* infix_to_postfix(const char* infix) {
    const char* *stack = NULL;
    size_t stack_len = 0;
    const char* *post = NULL;
    size_t post_len = 0;
    long long i = 0LL;
    while (i < strlen(infix)) {
        const char* x = (const char[]){infix[i], 0};
        if (is_alpha(x) || is_digit(x)) {
            post = list_append_str(post, &post_len, x);
        } else {
            if (strcmp(x, "(") == 0) {
                stack = list_append_str(stack, &stack_len, x);
            } else {
                if (strcmp(x, ")") == 0) {
                    if (stack_len == 0LL) {
                        panic("list index out of range");
                    }
                    while (strcmp(stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})], "(") != 0) {
                        post = list_append_str(post, &post_len, stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})]);
                        stack = _slice_str(stack, stack_len, 0LL, stack_len - 1LL, &(_slice_str_len));
                        stack_len = _slice_str_len;
                    }
                    stack = _slice_str(stack, stack_len, 0LL, stack_len - 1LL, &(_slice_str_len));
                    stack_len = _slice_str_len;
                } else {
                    if (stack_len == 0LL) {
                        stack = list_append_str(stack, &stack_len, x);
                    } else {
                        while (((stack_len > 0LL) && (strcmp(stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})], "(") != 0)) && (map_get_si(PRIORITY.keys, PRIORITY.vals, PRIORITY.len, x) <= map_get_si(PRIORITY.keys, PRIORITY.vals, PRIORITY.len, stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})]))) {
                            post = list_append_str(post, &post_len, stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})]);
                            stack = _slice_str(stack, stack_len, 0LL, stack_len - 1LL, &(_slice_str_len));
                            stack_len = _slice_str_len;
                        }
                        stack = list_append_str(stack, &stack_len, x);
                    }
                }
            }
        }
        i = i + 1LL;
    }
    while (stack_len > 0LL) {
        if (strcmp(stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})], "(") == 0) {
            panic("invalid expression");
        }
        post = list_append_str(post, &post_len, stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})]);
        stack = _slice_str(stack, stack_len, 0LL, stack_len - 1LL, &(_slice_str_len));
        stack_len = _slice_str_len;
    }
    const char* res = "";
    long long j = 0LL;
    while (j < post_len) {
        res = str_concat(res, post[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? post_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return res;
}

const char* infix_to_prefix(const char* infix) {
    const char* reversed = "";
    long long i = strlen(infix) - 1LL;
    while (i >= 0LL) {
        const char* ch = (const char[]){infix[i], 0};
        if (strcmp(ch, "(") == 0) {
            reversed = str_concat(reversed, ")");
        } else {
            if (strcmp(ch, ")") == 0) {
                reversed = str_concat(reversed, "(");
            } else {
                reversed = str_concat(reversed, ch);
            }
        }
        i = i - 1LL;
    }
    const char* postfix = infix_to_postfix(reversed);
    const char* prefix = reverse_string(postfix);
    return prefix;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
