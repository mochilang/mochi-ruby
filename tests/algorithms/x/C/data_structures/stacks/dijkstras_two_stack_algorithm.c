// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t slice_without_last_int_len;
size_t slice_without_last_string_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* equation = "(5 + ((4 * 2) * (2 + 3)))";

long long is_digit(const char* ch);
long long * slice_without_last_int(long long * xs, size_t xs_len);
const char* * slice_without_last_string(const char* * xs, size_t xs_len);
long long dijkstras_two_stack_algorithm(const char* equation);
int main(void);

long long is_digit(const char* ch) {
    return (((((((((strcmp(ch, "0") == 0) || (strcmp(ch, "1") == 0)) || (strcmp(ch, "2") == 0)) || (strcmp(ch, "3") == 0)) || (strcmp(ch, "4") == 0)) || (strcmp(ch, "5") == 0)) || (strcmp(ch, "6") == 0)) || (strcmp(ch, "7") == 0)) || (strcmp(ch, "8") == 0)) || (strcmp(ch, "9") == 0);
}

long long * slice_without_last_int(long long * xs, size_t xs_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < (xs_len - 1LL)) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return slice_without_last_int_len = res_len, res;
}

const char* * slice_without_last_string(const char* * xs, size_t xs_len) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < (xs_len - 1LL)) {
        res = list_append_str(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return slice_without_last_string_len = res_len, res;
}

long long dijkstras_two_stack_algorithm(const char* equation) {
    long long *operand_stack = NULL;
    size_t operand_stack_len = 0;
    const char* *operator_stack = NULL;
    size_t operator_stack_len = 0;
    long long idx = 0LL;
    while (idx < strlen(equation)) {
        const char* ch = _substring(equation, idx, idx + 1LL);
        if (is_digit(ch)) {
            operand_stack = list_append_long_long(operand_stack, &operand_stack_len, _atoi(ch));
        } else {
            if ((((strcmp(ch, "+") == 0) || (strcmp(ch, "-") == 0)) || (strcmp(ch, "*") == 0)) || (strcmp(ch, "/") == 0)) {
                operator_stack = list_append_str(operator_stack, &operator_stack_len, ch);
            } else {
                if (strcmp(ch, ")") == 0) {
                    const char* opr = operator_stack[(int)({long long _mochi_idx = operator_stack_len - 1LL; _mochi_idx < 0 ? operator_stack_len + _mochi_idx : _mochi_idx;})];
                    operator_stack = slice_without_last_string(operator_stack, operator_stack_len);
                    operator_stack_len = slice_without_last_string_len;
                    long long num1 = operand_stack[(int)({long long _mochi_idx = operand_stack_len - 1LL; _mochi_idx < 0 ? operand_stack_len + _mochi_idx : _mochi_idx;})];
                    operand_stack = slice_without_last_int(operand_stack, operand_stack_len);
                    operand_stack_len = slice_without_last_int_len;
                    long long num2 = operand_stack[(int)({long long _mochi_idx = operand_stack_len - 1LL; _mochi_idx < 0 ? operand_stack_len + _mochi_idx : _mochi_idx;})];
                    operand_stack = slice_without_last_int(operand_stack, operand_stack_len);
                    operand_stack_len = slice_without_last_int_len;
                    long long total;
                    if (strcmp(opr, "+") == 0) {
                        total = num2 + num1;
                    } else {
                        total = (strcmp(opr, "-") == 0 ? num2 - num1 : (strcmp(opr, "*") == 0 ? num2 * num1 : num2 / num1));
                    }
                    operand_stack = list_append_long_long(operand_stack, &operand_stack_len, total);
                }
            }
        }
        idx = idx + 1LL;
    }
    return operand_stack[(int)({long long _mochi_idx = operand_stack_len - 1LL; _mochi_idx < 0 ? operand_stack_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat(str_concat(equation, " = "), str_int(dijkstras_two_stack_algorithm(equation))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
