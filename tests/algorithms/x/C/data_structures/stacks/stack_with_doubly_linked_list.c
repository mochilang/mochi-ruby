// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;
typedef struct Stack Stack;
typedef struct TopResult TopResult;
typedef struct PopResult PopResult;

struct Node {
    long long data;
    long long next;
    long long prev;
};

struct Stack {
    Node *nodes;
    size_t nodes_len;
    long long head;
};

struct TopResult {
    long long value;
    long long ok;
};

struct PopResult {
    Stack stack;
    long long value;
    long long ok;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Stack empty_stack();
Stack push(Stack stack, long long value);
PopResult pop(Stack stack);
TopResult top(Stack stack);
long long size(Stack stack);
long long is_empty(Stack stack);
void print_stack(Stack stack);
void user_main();
int main(void);

Stack empty_stack() {
    return (Stack){.nodes = NULL, .nodes_len = 0, .head = -1LL};
}

Stack push(Stack stack, long long value) {
    Node *nodes = stack.nodes;
    size_t nodes_len = stack.nodes_len;
    long long idx = nodes_len;
    Node new_node = (Node){.data = value, .next = stack.head, .prev = -1LL};
    nodes = list_append_Node(nodes, &nodes_len, new_node);
    if (stack.head != (0LL - 1LL)) {
        Node head_node = nodes[(int)({long long _mochi_idx = stack.head; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        head_node.prev = idx;
        nodes[(int)(stack.head)] = head_node;
    }
    return (Stack){.nodes = nodes, .nodes_len = nodes_len, .head = idx};
}

PopResult pop(Stack stack) {
    if (stack.head == (0LL - 1LL)) {
        return (PopResult){.stack = stack, .value = 0LL, .ok = 0LL};
    }
    Node *nodes = stack.nodes;
    size_t nodes_len = stack.nodes_len;
    Node head_node = nodes[(int)({long long _mochi_idx = stack.head; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long value = head_node.data;
    long long next_idx = head_node.next;
    if (next_idx != (0LL - 1LL)) {
        Node next_node = nodes[(int)({long long _mochi_idx = next_idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        next_node.prev = -1LL;
        nodes[(int)(next_idx)] = next_node;
    }
    Stack new_stack = (Stack){.nodes = nodes, .nodes_len = nodes_len, .head = next_idx};
    return (PopResult){.stack = new_stack, .value = value, .ok = 1LL};
}

TopResult top(Stack stack) {
    if (stack.head == (0LL - 1LL)) {
        return (TopResult){.value = 0LL, .ok = 0LL};
    }
    Node node = stack.nodes[(int)({long long _mochi_idx = stack.head; _mochi_idx < 0 ? stack.nodes_len + _mochi_idx : _mochi_idx;})];
    return (TopResult){.value = node.data, .ok = 1LL};
}

long long size(Stack stack) {
    long long count_ = 0LL;
    long long idx = stack.head;
    while (idx != (0LL - 1LL)) {
        count_ = count_ + 1LL;
        Node node = stack.nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? stack.nodes_len + _mochi_idx : _mochi_idx;})];
        idx = node.next;
    }
    return count_;
}

long long is_empty(Stack stack) {
    return stack.head == (0LL - 1LL);
}

void print_stack(Stack stack) {
    puts("stack elements are:");
    long long idx = stack.head;
    const char* s = "";
    while (idx != (0LL - 1LL)) {
        Node node = stack.nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? stack.nodes_len + _mochi_idx : _mochi_idx;})];
        s = str_concat(str_concat(s, str_int(node.data)), "->");
        idx = node.next;
    }
    if (strlen(s) > 0LL) {
        puts(s);
    }
}

void user_main() {
    Stack stack = empty_stack();
    puts("Stack operations using Doubly LinkedList");
    stack = push(stack, 4LL);
    stack = push(stack, 5LL);
    stack = push(stack, 6LL);
    stack = push(stack, 7LL);
    print_stack(stack);
    TopResult t = top(stack);
    if (t.ok) {
        puts(str_concat("Top element is ", str_int(t.value)));
    } else {
        puts("Top element is None");
    }
    puts(str_concat("Size of the stack is ", str_int(size(stack))));
    PopResult p = pop(stack);
    stack = p.stack;
    p = pop(stack);
    stack = p.stack;
    print_stack(stack);
    puts(str_concat("stack is empty: ", str_int(is_empty(stack))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
