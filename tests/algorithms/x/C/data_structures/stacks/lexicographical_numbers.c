// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t lexical_order_len;

size_t _slice_int_len;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * lexical_order(long long max_number);
const char* join_ints(long long * xs, size_t xs_len);
int main(void);

long long * lexical_order(long long max_number) {
    long long *result = NULL;
    size_t result_len = 0;
    long long *stack = NULL;
    size_t stack_len = 0;
    stack = list_append_long_long(stack, &stack_len, 1LL);
    while (stack_len > 0LL) {
        long long idx = stack_len - 1LL;
        long long num = stack[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
        stack = _slice_int(stack, stack_len, 0LL, idx, &(_slice_int_len));
        stack_len = _slice_int_len;
        if (num > max_number) {
            continue;
        }
        result = list_append_long_long(result, &result_len, num);
        if ((num % 10LL) != 9LL) {
            stack = list_append_long_long(stack, &stack_len, num + 1LL);
        }
        stack = list_append_long_long(stack, &stack_len, num * 10LL);
    }
    return lexical_order_len = result_len, result;
}

const char* join_ints(long long * xs, size_t xs_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < xs_len) {
        if (i > 0LL) {
            res = str_concat(res, " ");
        }
        res = str_concat(res, str_int(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long* __tmp5 = lexical_order(13LL); size_t __tmp5_len = lexical_order_len; join_ints(__tmp5, __tmp5_len);}));
        puts(({long long *__tmp6 = lexical_order(1LL); char *__res = str_list_int(__tmp6, lexical_order_len); __res;}));
        puts(({long long* __tmp7 = lexical_order(20LL); size_t __tmp7_len = lexical_order_len; join_ints(__tmp7, __tmp7_len);}));
        puts(({long long* __tmp8 = lexical_order(25LL); size_t __tmp8_len = lexical_order_len; join_ints(__tmp8, __tmp8_len);}));
        puts(({long long *__tmp9 = lexical_order(12LL); char *__res = str_list_int(__tmp9, lexical_order_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
