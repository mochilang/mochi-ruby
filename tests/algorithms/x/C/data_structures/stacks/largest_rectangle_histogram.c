// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t _slice_int_len;
size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long largest_rectangle_area(long long * heights, size_t heights_len);
int main(void);

long long largest_rectangle_area(long long * heights, size_t heights_len) {
    long long *stack = NULL;
    size_t stack_len = 0;
    long long max_area = 0LL;
    long long *hs = heights;
    size_t hs_len = heights_len;
    hs = list_append_long_long(hs, &hs_len, 0LL);
    long long i = 0LL;
    while (i < hs_len) {
        while ((stack_len > 0LL) && (hs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? hs_len + _mochi_idx : _mochi_idx;})] < hs[(int)({long long _mochi_idx = stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? hs_len + _mochi_idx : _mochi_idx;})])) {
            long long top = stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
            stack = _slice_int(stack, stack_len, 0LL, stack_len - 1LL, &(_slice_int_len));
            stack_len = _slice_int_len;
            long long height = hs[(int)({long long _mochi_idx = top; _mochi_idx < 0 ? hs_len + _mochi_idx : _mochi_idx;})];
            long long width = i;
            if (stack_len > 0LL) {
                width = (i - stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})]) - 1LL;
            }
            long long area = height * width;
            if (area > max_area) {
                max_area = area;
            }
        }
        stack = list_append_long_long(stack, &stack_len, i);
        i = i + 1LL;
    }
    return max_area;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(largest_rectangle_area(({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 5LL; tmp[3] = 6LL; tmp[4] = 2LL; tmp[5] = 3LL; tmp;}), 6)));
        puts(str_int(largest_rectangle_area(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 4LL; tmp;}), 2)));
        puts(str_int(largest_rectangle_area(({long long *tmp = malloc(7 * sizeof(long long)); tmp[0] = 6LL; tmp[1] = 2LL; tmp[2] = 5LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp[5] = 1LL; tmp[6] = 6LL; tmp;}), 7)));
        puts(str_int(largest_rectangle_area(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 1LL; tmp;}), 1)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
