// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t slice_without_last_len;

size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

double * slice_without_last(double * xs, size_t xs_len);
double parse_float(const char* token);
double pow_float(double base, double exp);
double apply_op(double a, double b, const char* op);
double evaluate(const char* * tokens, size_t tokens_len);
int main(void);

double * slice_without_last(double * xs, size_t xs_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < (xs_len - 1LL)) {
        res = list_append_double(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return slice_without_last_len = res_len, res;
}

double parse_float(const char* token) {
    double sign = 1.0;
    long long idx = 0LL;
    if (strlen(token) > 0LL) {
        const char* first = _substring(token, 0LL, 1LL);
        if (strcmp(first, "-") == 0) {
            sign = -1.0;
            idx = 1LL;
        } else {
            if (strcmp(first, "+") == 0) {
                idx = 1LL;
            }
        }
    }
    long long int_part = 0LL;
    while ((idx < strlen(token)) && (strcmp(_substring(token, idx, idx + 1LL), ".") != 0)) {
        int_part = (int_part * 10LL) + _atoi(_substring(token, idx, idx + 1LL));
        idx = idx + 1LL;
    }
    double result = 1.0 * int_part;
    if ((idx < strlen(token)) && (strcmp(_substring(token, idx, idx + 1LL), ".") == 0)) {
        idx = idx + 1LL;
        double place = 0.1;
        while (idx < strlen(token)) {
            long long digit = _atoi(_substring(token, idx, idx + 1LL));
            result = result + (place * (1.0 * digit));
            place = place / 10.0;
            idx = idx + 1LL;
        }
    }
    return sign * result;
}

double pow_float(double base, double exp) {
    double result = 1.0;
    long long i = 0LL;
    long long e = (int)(exp);
    while (i < e) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double apply_op(double a, double b, const char* op) {
    if (strcmp(op, "+") == 0) {
        return a + b;
    }
    if (strcmp(op, "-") == 0) {
        return a - b;
    }
    if (strcmp(op, "*") == 0) {
        return a * b;
    }
    if (strcmp(op, "/") == 0) {
        return a / b;
    }
    if (strcmp(op, "^") == 0) {
        return pow_float(a, b);
    }
    return 0.0;
}

double evaluate(const char* * tokens, size_t tokens_len) {
    if (tokens_len == 0LL) {
        return 0.0;
    }
    double *stack = NULL;
    size_t stack_len = 0;
    for (size_t __i = 0; __i < tokens_len; __i++) {
        const char* token = tokens[__i];
        if (((((strcmp(token, "+") == 0) || (strcmp(token, "-") == 0)) || (strcmp(token, "*") == 0)) || (strcmp(token, "/") == 0)) || (strcmp(token, "^") == 0)) {
            if (((strcmp(token, "+") == 0) || (strcmp(token, "-") == 0)) && (stack_len < 2LL)) {
                double b = stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
                stack = slice_without_last(stack, stack_len);
                stack_len = slice_without_last_len;
                if (strcmp(token, "-") == 0) {
                    stack = list_append_double(stack, &stack_len, 0.0 - b);
                } else {
                    stack = list_append_double(stack, &stack_len, b);
                }
            } else {
                double b = stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
                stack = slice_without_last(stack, stack_len);
                stack_len = slice_without_last_len;
                double a = stack[(int)({long long _mochi_idx = stack_len - 1LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
                stack = slice_without_last(stack, stack_len);
                stack_len = slice_without_last_len;
                double result = apply_op(a, b, token);
                stack = list_append_double(stack, &stack_len, result);
            }
        } else {
            stack = list_append_double(stack, &stack_len, parse_float(token));
        }
    }
    if (stack_len != 1LL) {
        panic("Invalid postfix expression");
    }
    return stack[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(evaluate(({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = "2"; tmp[1] = "1"; tmp[2] = "+"; tmp[3] = "3"; tmp[4] = "*"; tmp;}), 5)));
        puts(str_float(evaluate(({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = "4"; tmp[1] = "13"; tmp[2] = "5"; tmp[3] = "/"; tmp[4] = "+"; tmp;}), 5)));
        puts(str_float(evaluate(({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = "5"; tmp[1] = "6"; tmp[2] = "9"; tmp[3] = "*"; tmp[4] = "+"; tmp;}), 5)));
        puts(str_float(evaluate(({const char* *tmp = malloc(4 * sizeof(const char*)); tmp[0] = "2"; tmp[1] = "-"; tmp[2] = "3"; tmp[3] = "+"; tmp;}), 4)));
        puts(str_float(evaluate(NULL, 0)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
