// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t string_to_grid_len;
size_t *string_to_grid_lens;
size_t find_empty_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* puzzle = "003020600900305001001806400008102900700000008006708200002609500800203009005010300";
long long **grid = NULL;
size_t grid_len = 0;
size_t *grid_lens = NULL;
size_t grid_lens_len = 0;

long long * * string_to_grid(const char* s);
const char* print_grid(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
long long is_safe(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long row, long long column, long long n);
long long * find_empty(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
long long solve(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
int main(void);

long long * * string_to_grid(const char* s) {
    long long **grid = NULL;
    size_t grid_len = 0;
    size_t *grid_lens = NULL;
    size_t grid_lens_len = 0;
    long long i = 0LL;
    while (i < 9LL) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < 9LL) {
            const char* ch = _substring(s, (i * 9LL) + j, ((i * 9LL) + j) + 1LL);
            long long val = 0LL;
            if ((strcmp(ch, "0") != 0) && (strcmp(ch, ".") != 0)) {
                val = _atoi(ch);
            }
            row = list_append_long_long(row, &row_len, val);
            j = j + 1LL;
        }
        grid = list_append_intptr(grid, &grid_len, row);
        grid_lens = list_append_szt(grid_lens, &grid_lens_len, row_len);
        i = i + 1LL;
    }
    return string_to_grid_lens = grid_lens, string_to_grid_len = grid_len, grid;
}

const char* print_grid(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    for (int r = 0LL; r < 9LL; r++) {
        const char* line = "";
        for (int c = 0LL; c < 9LL; c++) {
            line = str_concat(line, str_int(grid[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = c; _mochi_idx < 0 ? grid_lens[(int)(r)] + _mochi_idx : _mochi_idx;})]));
            if (c < 8LL) {
                line = str_concat(line, " ");
            }
        }
        puts(line);
    }
}

long long is_safe(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long row, long long column, long long n) {
    for (int i = 0LL; i < 9LL; i++) {
        if ((grid[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_lens[(int)(row)] + _mochi_idx : _mochi_idx;})] == n) || (grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column; _mochi_idx < 0 ? grid_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] == n)) {
            return 0LL;
        }
    }
    for (int i = 0LL; i < 3LL; i++) {
        for (int j = 0LL; j < 3LL; j++) {
            if (grid[(int)({long long _mochi_idx = (row - (row % 3LL)) + i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = (column - (column % 3LL)) + j; _mochi_idx < 0 ? grid_lens[(int)((row - (row % 3LL)) + i)] + _mochi_idx : _mochi_idx;})] == n) {
                return 0LL;
            }
        }
    }
    return 1LL;
}

long long * find_empty(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    for (int i = 0LL; i < 9LL; i++) {
        for (int j = 0LL; j < 9LL; j++) {
            if (grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? grid_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] == 0LL) {
                {
                    long long* __tmp0_data = malloc(2 * sizeof(long long));
                    __tmp0_data[0] = i;
                    __tmp0_data[1] = j;
                    find_empty_len = 2;
                    return __tmp0_data;
                }
            }
        }
    }
    {
        long long* __tmp1_data = malloc(0 * sizeof(long long));
        find_empty_len = 0;
        return __tmp1_data;
    }
}

long long solve(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long *loc = find_empty(grid, grid_len, grid_lens, grid_len);
    size_t loc_len = find_empty_len;
    if (loc_len == 0LL) {
        return 1LL;
    }
    long long row = loc[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? loc_len + _mochi_idx : _mochi_idx;})];
    long long column = loc[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? loc_len + _mochi_idx : _mochi_idx;})];
    for (int digit = 1LL; digit < 10LL; digit++) {
        if (is_safe(grid, grid_len, grid_lens, grid_len, row, column, digit)) {
            grid[(int)(row)][(int)(column)] = digit;
            if (solve(grid, grid_len, grid_lens, grid_len)) {
                return 1LL;
            }
            grid[(int)(row)][(int)(column)] = 0LL;
        }
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        grid = string_to_grid(puzzle);
        grid_len = string_to_grid_len;
        grid_lens = string_to_grid_lens;
        puts("Original grid:");
        print_grid(grid, grid_len, grid_lens, grid_len);
        if (solve(grid, grid_len, grid_lens, grid_len)) {
            puts("\nSolved grid:");
            print_grid(grid, grid_len, grid_lens, grid_len);
        } else {
            puts("\nNo solution found");
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
