// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t build_sparse_table_len;
size_t *build_sparse_table_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **st1 = NULL;
size_t st1_len = 0;
size_t *st1_lens = NULL;
size_t st1_lens_len = 0;
long long **st2 = NULL;
size_t st2_len = 0;
size_t *st2_lens = NULL;
size_t st2_lens_len = 0;

long long pow2(long long n);
long long int_log2(long long n);
long long * * build_sparse_table(long long * number_list, size_t number_list_len);
long long query(long long * * sparse_table, size_t sparse_table_len, size_t* sparse_table_lens, size_t sparse_table_lens_len, long long left_bound, long long right_bound);
int main(void);

long long pow2(long long n) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < n) {
        result = result * 2LL;
        i = i + 1LL;
    }
    return result;
}

long long int_log2(long long n) {
    long long v = n;
    long long res = 0LL;
    while (v > 1LL) {
        v = v / 2LL;
        res = res + 1LL;
    }
    return res;
}

long long * * build_sparse_table(long long * number_list, size_t number_list_len) {
    if (number_list_len == 0LL) {
        panic("empty number list not allowed");
    }
    long long length = number_list_len;
    long long row = int_log2(length) + 1LL;
    long long **sparse_table = NULL;
    size_t sparse_table_len = 0;
    size_t *sparse_table_lens = NULL;
    size_t sparse_table_lens_len = 0;
    long long j = 0LL;
    while (j < row) {
        long long *inner = NULL;
        size_t inner_len = 0;
        long long i = 0LL;
        while (i < length) {
            inner = list_append_long_long(inner, &inner_len, 0LL);
            i = i + 1LL;
        }
        sparse_table = list_append_intptr(sparse_table, &sparse_table_len, inner);
        sparse_table_lens = list_append_szt(sparse_table_lens, &sparse_table_lens_len, inner_len);
        j = j + 1LL;
    }
    long long i = 0LL;
    while (i < length) {
        sparse_table[(int)(0LL)][(int)(i)] = number_list[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? number_list_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    j = 1LL;
    while (pow2(j) <= length) {
        i = 0LL;
        while (((i + pow2(j)) - 1LL) < length) {
            long long left = sparse_table[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? sparse_table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i + pow2(j - 1LL); _mochi_idx < 0 ? sparse_table_lens[(int)(j - 1LL)] + _mochi_idx : _mochi_idx;})];
            long long right = sparse_table[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? sparse_table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sparse_table_lens[(int)(j - 1LL)] + _mochi_idx : _mochi_idx;})];
            if (left < right) {
                sparse_table[(int)(j)][(int)(i)] = left;
            } else {
                sparse_table[(int)(j)][(int)(i)] = right;
            }
            i = i + 1LL;
        }
        j = j + 1LL;
    }
    return build_sparse_table_lens = sparse_table_lens, build_sparse_table_len = sparse_table_len, sparse_table;
}

long long query(long long * * sparse_table, size_t sparse_table_len, size_t* sparse_table_lens, size_t sparse_table_lens_len, long long left_bound, long long right_bound) {
    if ((left_bound < 0LL) || (right_bound >= sparse_table_lens[(int)(0LL)])) {
        panic("list index out of range");
    }
    long long interval = (right_bound - left_bound) + 1LL;
    long long j = int_log2(interval);
    long long val1 = sparse_table[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? sparse_table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = (right_bound - pow2(j)) + 1LL; _mochi_idx < 0 ? sparse_table_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
    long long val2 = sparse_table[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? sparse_table_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = left_bound; _mochi_idx < 0 ? sparse_table_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
    if (val1 < val2) {
        return val1;
    }
    return val2;
}

int main(void) {
    {
        long long __start = _now();
        st1 = build_sparse_table((long long[]){8LL, 1LL, 0LL, 3LL, 4LL, 9LL, 3LL}, 7);
        st1_len = build_sparse_table_len;
        st1_lens = build_sparse_table_lens;
        puts(str_list_list_int(st1, st1_len, st1_lens));
        st2 = build_sparse_table((long long[]){3LL, 1LL, 9LL}, 3);
        st2_len = build_sparse_table_len;
        st2_lens = build_sparse_table_lens;
        puts(str_list_list_int(st2, st2_len, st2_lens));
        puts(str_int(query(st1, st1_len, st1_lens, st1_len, 0LL, 4LL)));
        puts(str_int(query(st1, st1_len, st1_lens, st1_len, 4LL, 6LL)));
        puts(str_int(query(st2, st2_len, st2_lens, st2_len, 2LL, 2LL)));
        puts(str_int(query(st2, st2_len, st2_lens, st2_len, 0LL, 1LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
