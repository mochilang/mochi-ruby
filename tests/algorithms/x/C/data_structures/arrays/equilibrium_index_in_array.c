// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long arr1_init[7] = {-7LL, 1LL, 5LL, 2LL, -4LL, 3LL, 0LL};
long long *arr1 = arr1_init;
size_t arr1_len = 7;
long long arr2_init[5] = {1LL, 2LL, 3LL, 4LL, 5LL};
long long *arr2 = arr2_init;
size_t arr2_len = 5;
long long arr3_init[5] = {1LL, 1LL, 1LL, 1LL, 1LL};
long long *arr3 = arr3_init;
size_t arr3_len = 5;
long long arr4_init[6] = {2LL, 4LL, 6LL, 8LL, 10LL, 3LL};
long long *arr4 = arr4_init;
size_t arr4_len = 6;

long long equilibrium_index(long long * arr, size_t arr_len);
int main(void);

long long equilibrium_index(long long * arr, size_t arr_len) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < arr_len) {
        total = total + arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    long long left = 0LL;
    i = 0LL;
    while (i < arr_len) {
        total = total - arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (left == total) {
            return i;
        }
        left = left + arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return -1LL;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", equilibrium_index(arr1, arr1_len));
        printf("%d\n", equilibrium_index(arr2, arr2_len));
        printf("%d\n", equilibrium_index(arr3, arr3_len));
        printf("%d\n", equilibrium_index(arr4, arr4_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
