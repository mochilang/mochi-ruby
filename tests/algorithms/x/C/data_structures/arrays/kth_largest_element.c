// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long arr1_init[11] = {3LL, 1LL, 4LL, 1LL, 5LL, 9LL, 2LL, 6LL, 5LL, 3LL, 5LL};
long long *arr1 = arr1_init;
size_t arr1_len = 11;
long long arr2_init[11] = {2LL, 5LL, 6LL, 1LL, 9LL, 3LL, 8LL, 4LL, 7LL, 3LL, 5LL};
long long *arr2 = arr2_init;
size_t arr2_len = 11;

long long partition(long long * arr, size_t arr_len, long long low, long long high);
long long kth_largest_element(long long * arr, size_t arr_len, long long position);
int main(void);

long long partition(long long * arr, size_t arr_len, long long low, long long high) {
    long long pivot = arr[(int)({long long _mochi_idx = high; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    long long i = low - 1LL;
    long long j = low;
    while (j < high) {
        if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] >= pivot) {
            i = i + 1LL;
            long long tmp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(i)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(j)] = tmp;
        }
        j = j + 1LL;
    }
    long long k = i + 1LL;
    long long tmp = arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    arr[(int)(k)] = arr[(int)({long long _mochi_idx = high; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    arr[(int)(high)] = tmp;
    return k;
}

long long kth_largest_element(long long * arr, size_t arr_len, long long position) {
    if (arr_len == 0LL) {
        return -1LL;
    }
    if ((position < 1LL) || (position > arr_len)) {
        return -1LL;
    }
    long long low = 0LL;
    long long high = arr_len - 1LL;
    while (low <= high) {
        if ((low > (arr_len - 1LL)) || (high < 0LL)) {
            return -1LL;
        }
        long long pivot_index = partition(arr, arr_len, low, high);
        if (pivot_index == (position - 1LL)) {
            return arr[(int)({long long _mochi_idx = pivot_index; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        } else {
            if (pivot_index > (position - 1LL)) {
                high = pivot_index - 1LL;
            } else {
                low = pivot_index + 1LL;
            }
        }
    }
    return -1LL;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", kth_largest_element(arr1, arr1_len, 3LL));
        puts("\n");
        printf("%d\n", kth_largest_element(arr2, arr2_len, 1LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
