// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t tail_len;
size_t rotate_left_len;
size_t permute_recursive_len;
size_t *permute_recursive_lens;
size_t swap_len;
size_t permute_backtrack_helper_len;
size_t *permute_backtrack_helper_lens;
size_t permute_backtrack_len;
size_t *permute_backtrack_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * tail(long long * xs, size_t xs_len);
long long * rotate_left(long long * xs, size_t xs_len);
long long * * permute_recursive(long long * nums, size_t nums_len);
long long * swap(long long * xs, size_t xs_len, long long i, long long j);
long long * * permute_backtrack_helper(long long * nums, size_t nums_len, long long start, long long * * output, size_t output_len, size_t* output_lens, size_t output_lens_len);
long long * * permute_backtrack(long long * nums, size_t nums_len);
int main(void);

long long * tail(long long * xs, size_t xs_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 1LL;
    while (i < xs_len) {
        res = list_append_int_new(res, res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        res_len++;
        i = i + 1LL;
    }
    return tail_len = res_len, res;
}

long long * rotate_left(long long * xs, size_t xs_len) {
    if (xs_len == 0LL) {
        return rotate_left_len = xs_len, xs;
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 1LL;
    while (i < xs_len) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
    return rotate_left_len = res_len, res;
}

long long * * permute_recursive(long long * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        long long **base = NULL;
        size_t base_len = 0;
        size_t *base_lens = NULL;
        size_t base_lens_len = 0;
        return permute_recursive_len = base_len + 1, (base = list_append_long_longptr(base, &base_len, NULL), base_lens = list_append_szt(base_lens, &base_lens_len, 0), permute_recursive_lens = base_lens, base);
    }
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long *current = nums;
    size_t current_len = nums_len;
    long long count_ = 0LL;
    while (count_ < nums_len) {
        long long n = current[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})];
        long long *rest = tail(current, current_len);
        size_t rest_len = tail_len;
        long long **perms = permute_recursive(rest, rest_len);
        size_t perms_len = permute_recursive_len;
        size_t *perms_lens = permute_recursive_lens;
        size_t perms_lens_len = permute_recursive_len;
        long long j = 0LL;
        while (j < perms_len) {
            long long *perm = list_append_int_new(perms[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? perms_len + _mochi_idx : _mochi_idx;})], perms_lens[(int)(j)], n);
            size_t perm_len = perms_lens[(int)(j)] + 1;
            result = list_append_intptr(result, &result_len, perm);
            result_lens = list_append_szt(result_lens, &result_lens_len, perm_len);
            j = j + 1LL;
        }
        current = rotate_left(current, current_len);
        current_len = rotate_left_len;
        count_ = count_ + 1LL;
    }
    return permute_recursive_lens = result_lens, permute_recursive_len = result_len, result;
}

long long * swap(long long * xs, size_t xs_len, long long i, long long j) {
    long long *res = NULL;
    size_t res_len = 0;
    long long k = 0LL;
    while (k < xs_len) {
        if (k == i) {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        } else {
            if (k == j) {
                res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
            } else {
                res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
            }
        }
        k = k + 1LL;
    }
    return swap_len = res_len, res;
}

long long * * permute_backtrack_helper(long long * nums, size_t nums_len, long long start, long long * * output, size_t output_len, size_t* output_lens, size_t output_lens_len) {
    if (start == (nums_len - 1LL)) {
        return permute_backtrack_helper_len = output_len + 1, (output = list_append_long_longptr(output, &output_len, nums), output_lens = list_append_szt(output_lens, &output_lens_len, nums_len), permute_backtrack_helper_lens = output_lens, output);
    }
    long long i = start;
    long long **res = output;
    size_t res_len = output_len;
    size_t *res_lens = output_lens;
    size_t res_lens_len = output_len;
    while (i < nums_len) {
        long long *swapped = swap(nums, nums_len, start, i);
        size_t swapped_len = swap_len;
        res = permute_backtrack_helper(swapped, swapped_len, start + 1LL, res, res_len, res_lens, res_len);
        res_len = permute_backtrack_helper_len;
        res_lens = permute_backtrack_helper_lens;
        i = i + 1LL;
    }
    return permute_backtrack_helper_lens = res_lens, permute_backtrack_helper_len = res_len, res;
}

long long * * permute_backtrack(long long * nums, size_t nums_len) {
    long long **output = NULL;
    size_t output_len = 0;
    size_t *output_lens = NULL;
    size_t output_lens_len = 0;
    {
        long long** __ret2 = permute_backtrack_helper(nums, nums_len, 0LL, output, output_len, output_lens, output_len);
        permute_backtrack_lens = permute_backtrack_helper_lens;
permute_backtrack_len = permute_backtrack_helper_len;
        return __ret2;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long **__tmp3 = permute_recursive((long long[]){1LL, 2LL, 3LL}, 3); char *__res = str_list_list_int(__tmp3, permute_recursive_len, permute_recursive_lens); __res;}));
        puts(({long long **__tmp4 = permute_backtrack((long long[]){1LL, 2LL, 3LL}, 3); char *__res = str_list_list_int(__tmp4, permute_backtrack_len, permute_backtrack_lens); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
