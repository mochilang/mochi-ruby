// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_triplet_len;
size_t find_triplets_with_0_sum_len;
size_t *find_triplets_with_0_sum_lens;
size_t find_triplets_with_0_sum_hashing_len;
size_t *find_triplets_with_0_sum_hashing_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * sort_triplet(long long a, long long b, long long c);
long long contains_triplet(long long * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long * target, size_t target_len);
long long contains_int(long long * arr, size_t arr_len, long long value);
long long * * find_triplets_with_0_sum(long long * nums, size_t nums_len);
long long * * find_triplets_with_0_sum_hashing(long long * arr, size_t arr_len);
int main(void);

long long * sort_triplet(long long a, long long b, long long c) {
    long long x = a;
    long long y = b;
    long long z = c;
    if (x > y) {
        long long t = x;
        x = y;
        y = t;
    }
    if (y > z) {
        long long t = y;
        y = z;
        z = t;
    }
    if (x > y) {
        long long t = x;
        x = y;
        y = t;
    }
    {
        long long* __tmp8_data = malloc(3 * sizeof(long long));
        __tmp8_data[0] = x;
        __tmp8_data[1] = y;
        __tmp8_data[2] = z;
        sort_triplet_len = 3;
        return __tmp8_data;
    }
}

long long contains_triplet(long long * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long * target, size_t target_len) {
    for (int i = 0LL; i < arr_len; i++) {
        long long *item = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        size_t item_len = arr_lens[(int)(i)];
        long long same = 1LL;
        for (int j = 0LL; j < target_len; j++) {
            if (item[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? item_len + _mochi_idx : _mochi_idx;})] != target[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? target_len + _mochi_idx : _mochi_idx;})]) {
                same = 0LL;
                break;
            }
        }
        if (same) {
            return 1LL;
        }
    }
    return 0LL;
}

long long contains_int(long long * arr, size_t arr_len, long long value) {
    for (int i = 0LL; i < arr_len; i++) {
        if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == value) {
            return 1LL;
        }
    }
    return 0LL;
}

long long * * find_triplets_with_0_sum(long long * nums, size_t nums_len) {
    long long n = nums_len;
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    for (int i = 0LL; i < n; i++) {
        for (int j = i + 1LL; j < n; j++) {
            for (int k = j + 1LL; k < n; k++) {
                long long a = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
                long long b = nums[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
                long long c = nums[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
                if (((a + b) + c) == 0LL) {
                    long long *trip = sort_triplet(a, b, c);
                    size_t trip_len = sort_triplet_len;
                    if (!(contains_triplet(result, result_len, result_lens, result_len, trip, trip_len))) {
                        result = list_append_intptr(result, &result_len, trip);
                        result_lens = list_append_szt(result_lens, &result_lens_len, trip_len);
                    }
                }
            }
        }
    }
    return find_triplets_with_0_sum_lens = result_lens, find_triplets_with_0_sum_len = result_len, result;
}

long long * * find_triplets_with_0_sum_hashing(long long * arr, size_t arr_len) {
    long long target_sum = 0LL;
    long long **output = NULL;
    size_t output_len = 0;
    size_t *output_lens = NULL;
    size_t output_lens_len = 0;
    for (int i = 0LL; i < arr_len; i++) {
        long long *seen = NULL;
        size_t seen_len = 0;
        long long current_sum = target_sum - arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        for (int j = i + 1LL; j < arr_len; j++) {
            long long other = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            long long required = current_sum - other;
            if (contains_int(seen, seen_len, required)) {
                long long *trip = sort_triplet(arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], other, required);
                size_t trip_len = sort_triplet_len;
                if (!(contains_triplet(output, output_len, output_lens, output_len, trip, trip_len))) {
                    output = list_append_intptr(output, &output_len, trip);
                    output_lens = list_append_szt(output_lens, &output_lens_len, trip_len);
                }
            }
            seen = list_append_long_long(seen, &seen_len, other);
        }
    }
    return find_triplets_with_0_sum_hashing_lens = output_lens, find_triplets_with_0_sum_hashing_len = output_len, output;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long **__tmp9 = find_triplets_with_0_sum((long long[]){-1LL, 0LL, 1LL, 2LL, -1LL, -4LL}, 6); char *__res = str_list_list_int(__tmp9, find_triplets_with_0_sum_len, find_triplets_with_0_sum_lens); __res;}));
        puts(({long long **__tmp10 = find_triplets_with_0_sum(NULL, 0); char *__res = str_list_list_int(__tmp10, find_triplets_with_0_sum_len, find_triplets_with_0_sum_lens); __res;}));
        puts(({long long **__tmp11 = find_triplets_with_0_sum((long long[]){0LL, 0LL, 0LL}, 3); char *__res = str_list_list_int(__tmp11, find_triplets_with_0_sum_len, find_triplets_with_0_sum_lens); __res;}));
        puts(({long long **__tmp12 = find_triplets_with_0_sum((long long[]){1LL, 2LL, 3LL, 0LL, -1LL, -2LL, -3LL}, 7); char *__res = str_list_list_int(__tmp12, find_triplets_with_0_sum_len, find_triplets_with_0_sum_lens); __res;}));
        puts(({long long **__tmp13 = find_triplets_with_0_sum_hashing((long long[]){-1LL, 0LL, 1LL, 2LL, -1LL, -4LL}, 6); char *__res = str_list_list_int(__tmp13, find_triplets_with_0_sum_hashing_len, find_triplets_with_0_sum_hashing_lens); __res;}));
        puts(({long long **__tmp14 = find_triplets_with_0_sum_hashing(NULL, 0); char *__res = str_list_list_int(__tmp14, find_triplets_with_0_sum_hashing_len, find_triplets_with_0_sum_hashing_lens); __res;}));
        puts(({long long **__tmp15 = find_triplets_with_0_sum_hashing((long long[]){0LL, 0LL, 0LL}, 3); char *__res = str_list_list_int(__tmp15, find_triplets_with_0_sum_hashing_len, find_triplets_with_0_sum_hashing_lens); __res;}));
        puts(({long long **__tmp16 = find_triplets_with_0_sum_hashing((long long[]){1LL, 2LL, 3LL, 0LL, -1LL, -2LL, -3LL}, 7); char *__res = str_list_list_int(__tmp16, find_triplets_with_0_sum_hashing_len, find_triplets_with_0_sum_hashing_lens); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
