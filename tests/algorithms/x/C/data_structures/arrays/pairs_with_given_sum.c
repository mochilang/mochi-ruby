// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long pairs_with_sum(long long * arr, size_t arr_len, long long req_sum);
int main(void);

long long pairs_with_sum(long long * arr, size_t arr_len, long long req_sum) {
    long long n = arr_len;
    long long count_ = 0LL;
    long long i = 0LL;
    while (i < n) {
        long long j = i + 1LL;
        while (j < n) {
            if ((arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] + arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) == req_sum) {
                count_ = count_ + 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return count_;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", pairs_with_sum((long long[]){1LL, 5LL, 7LL, 1LL}, 4, 6LL));
        printf("%d\n", pairs_with_sum((long long[]){1LL, 1LL, 1LL, 1LL, 1LL, 1LL, 1LL, 1LL}, 8, 2LL));
        printf("%d\n", pairs_with_sum((long long[]){1LL, 7LL, 6LL, 2LL, 5LL, 4LL, 3LL, 1LL, 9LL, 8LL}, 10, 7LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
