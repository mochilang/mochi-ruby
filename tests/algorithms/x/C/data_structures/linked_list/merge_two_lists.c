// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_list_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct SortedLinkedList SortedLinkedList;

struct SortedLinkedList {
    long long *values;
    size_t values_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * sort_list(long long * nums, size_t nums_len);
SortedLinkedList make_sorted_linked_list(long long * ints, size_t ints_len);
long long len_sll(SortedLinkedList sll);
const char* str_sll(SortedLinkedList sll);
SortedLinkedList merge_lists(SortedLinkedList a, SortedLinkedList b);
void user_main();
int main(void);

long long * sort_list(long long * nums, size_t nums_len) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < nums_len) {
        arr = list_append_long_long(arr, &arr_len, nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long j = 0LL;
    while (j < arr_len) {
        long long k = j + 1LL;
        while (k < arr_len) {
            if (arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(k)] = tmp;
            }
            k = k + 1LL;
        }
        j = j + 1LL;
    }
    return sort_list_len = arr_len, arr;
}

SortedLinkedList make_sorted_linked_list(long long * ints, size_t ints_len) {
    return (SortedLinkedList){.values = sort_list(ints, ints_len), .values_len = sort_list_len};
}

long long len_sll(SortedLinkedList sll) {
    return sll.values_len;
}

const char* str_sll(SortedLinkedList sll) {
    const char* res = "";
    long long i = 0LL;
    while (i < sll.values_len) {
        res = str_concat(res, str_int(sll.values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sll.values_len + _mochi_idx : _mochi_idx;})]));
        if ((i + 1LL) < sll.values_len) {
            res = str_concat(res, " -> ");
        }
        i = i + 1LL;
    }
    return res;
}

SortedLinkedList merge_lists(SortedLinkedList a, SortedLinkedList b) {
    long long *combined = NULL;
    size_t combined_len = 0;
    long long i = 0LL;
    while (i < a.values_len) {
        combined = list_append_long_long(combined, &combined_len, a.values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.values_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < b.values_len) {
        combined = list_append_long_long(combined, &combined_len, b.values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.values_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return make_sorted_linked_list(combined, combined_len);
}

void user_main() {
    long long *test_data_odd = NULL;
    size_t test_data_odd_len = 0;
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, 3LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, 9LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, -11LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, 0LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, 7LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, 5LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, 1LL);
    test_data_odd = list_append_long_long(test_data_odd, &test_data_odd_len, -1LL);
    long long *test_data_even = NULL;
    size_t test_data_even_len = 0;
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 4LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 6LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 2LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 0LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 8LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 10LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, 3LL);
    test_data_even = list_append_long_long(test_data_even, &test_data_even_len, -2LL);
    SortedLinkedList sll_one = make_sorted_linked_list(test_data_odd, test_data_odd_len);
    SortedLinkedList sll_two = make_sorted_linked_list(test_data_even, test_data_even_len);
    SortedLinkedList merged = merge_lists(sll_one, sll_two);
    puts(str_int(len_sll(merged)));
    puts(str_sll(merged));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
