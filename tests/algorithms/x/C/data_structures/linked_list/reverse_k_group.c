// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LinkedList LinkedList;

struct LinkedList {
    long long *data;
    size_t data_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* to_string(LinkedList list);
LinkedList reverse_k_nodes(LinkedList list, long long k);
void user_main();
int main(void);

const char* to_string(LinkedList list) {
    if (list.data_len == 0LL) {
        return "";
    }
    const char* s = str_int(list.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
    long long i = 1LL;
    while (i < list.data_len) {
        s = str_concat(str_concat(s, " -> "), str_int(list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return s;
}

LinkedList reverse_k_nodes(LinkedList list, long long k) {
    if (k <= 1LL) {
        return list;
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < list.data_len) {
        long long j = 0LL;
        long long *group = NULL;
        size_t group_len = 0;
        while ((j < k) && ((i + j) < list.data_len)) {
            group = list_append_long_long(group, &group_len, list.data[(int)({long long _mochi_idx = i + j; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        if (group_len == k) {
            long long g = k - 1LL;
            while (g >= 0LL) {
                res = list_append_long_long(res, &res_len, group[(int)({long long _mochi_idx = g; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})]);
                g = g - 1LL;
            }
        } else {
            long long g = 0LL;
            while (g < group_len) {
                res = list_append_long_long(res, &res_len, group[(int)({long long _mochi_idx = g; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})]);
                g = g + 1LL;
            }
        }
        i = i + k;
    }
    return (LinkedList){.data = res, .data_len = res_len};
}

void user_main() {
    LinkedList ll = (LinkedList){.data = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp;}), .data_len = 5};
    puts(str_concat("Original Linked List: ", to_string(ll)));
    long long k = 2LL;
    ll = reverse_k_nodes(ll, k);
    puts(str_concat(str_concat(str_concat("After reversing groups of size ", str_int(k)), ": "), to_string(ll)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
