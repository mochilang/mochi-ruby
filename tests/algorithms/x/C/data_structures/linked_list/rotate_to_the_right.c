// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t insert_node_len;
size_t rotate_to_the_right_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* list_to_string(long long * xs, size_t xs_len);
long long * insert_node(long long * xs, size_t xs_len, long long data);
long long * rotate_to_the_right(long long * xs, size_t xs_len, long long places);
void user_main();
int main(void);

const char* list_to_string(long long * xs, size_t xs_len) {
    if (xs_len == 0LL) {
        return "";
    }
    const char* s = str_int(xs[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
    long long i = 1LL;
    while (i < xs_len) {
        s = str_concat(str_concat(s, "->"), str_int(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return s;
}

long long * insert_node(long long * xs, size_t xs_len, long long data) {
    return insert_node_len = xs_len + 1, list_append_int_new(xs, xs_len, data);
}

long long * rotate_to_the_right(long long * xs, size_t xs_len, long long places) {
    if (xs_len == 0LL) {
        panic("The linked list is empty.");
    }
    long long n = xs_len;
    long long k = places % n;
    if (k == 0LL) {
        return rotate_to_the_right_len = xs_len, xs;
    }
    long long split = n - k;
    long long *res = NULL;
    size_t res_len = 0;
    long long i = split;
    while (i < n) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long j = 0LL;
    while (j < split) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return rotate_to_the_right_len = res_len, res;
}

void user_main() {
    long long *head = NULL;
    size_t head_len = 0;
    head = insert_node(head, head_len, 5LL);
    head_len = insert_node_len;
    head = insert_node(head, head_len, 1LL);
    head_len = insert_node_len;
    head = insert_node(head, head_len, 2LL);
    head_len = insert_node_len;
    head = insert_node(head, head_len, 4LL);
    head_len = insert_node_len;
    head = insert_node(head, head_len, 3LL);
    head_len = insert_node_len;
    puts(str_concat("Original list: ", list_to_string(head, head_len)));
    long long places = 3LL;
    long long *new_head = rotate_to_the_right(head, head_len, places);
    size_t new_head_len = rotate_to_the_right_len;
    puts(str_concat(str_concat(str_concat("After ", str_int(places)), " iterations: "), list_to_string(new_head, new_head_len)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
