// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_nodes_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    long long data;
    long long next;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long has_loop(Node * nodes, size_t nodes_len, long long head);
Node * make_nodes(long long * values, size_t values_len);
void user_main();
int main(void);

long long has_loop(Node * nodes, size_t nodes_len, long long head) {
    long long slow = head;
    long long fast = head;
    while (fast != (0LL - 1LL)) {
        Node fast_node1 = nodes[(int)({long long _mochi_idx = fast; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        if (fast_node1.next == (0LL - 1LL)) {
            return 0LL;
        }
        Node fast_node2 = nodes[(int)({long long _mochi_idx = fast_node1.next; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        if (fast_node2.next == (0LL - 1LL)) {
            return 0LL;
        }
        Node slow_node = nodes[(int)({long long _mochi_idx = slow; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        slow = slow_node.next;
        fast = fast_node2.next;
        if (slow == fast) {
            return 1LL;
        }
    }
    return 0LL;
}

Node * make_nodes(long long * values, size_t values_len) {
    Node *nodes = NULL;
    size_t nodes_len = 0;
    long long i = 0LL;
    while (i < values_len) {
        long long next_idx;
        if (i == (values_len - 1LL)) {
            next_idx = -1LL;
        } else {
            next_idx = i + 1LL;
        }
        nodes = list_append_Node(nodes, &nodes_len, (Node){.data = values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})], .next = next_idx});
        i = i + 1LL;
    }
    return make_nodes_len = nodes_len, nodes;
}

void user_main() {
    Node *list1 = make_nodes(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp;}), 4);
    size_t list1_len = make_nodes_len;
    puts(str_int(has_loop(list1, list1_len, 0LL)));
    list1[(int)(3LL)].next = 1LL;
    puts(str_int(has_loop(list1, list1_len, 0LL)));
    Node *list2 = make_nodes(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 5LL; tmp[1] = 6LL; tmp[2] = 5LL; tmp[3] = 6LL; tmp;}), 4);
    size_t list2_len = make_nodes_len;
    puts(str_int(has_loop(list2, list2_len, 0LL)));
    Node *list3 = make_nodes(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 1LL; tmp;}), 1);
    size_t list3_len = make_nodes_len;
    puts(str_int(has_loop(list3, list3_len, 0LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
