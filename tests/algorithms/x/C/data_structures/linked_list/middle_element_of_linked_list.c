// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct List List;

struct List {
    long long *data;
    size_t data_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

List empty_list();
List push(List lst, long long value);
long long middle_element(List lst);
void user_main();
int main(void);

List empty_list() {
    return (List){.data = NULL, .data_len = 0};
}

List push(List lst, long long value) {
    long long *res = NULL;
    size_t res_len = 0;
    res = list_append_long_long(res, &res_len, value);
    long long i = 0LL;
    while (i < lst.data_len) {
        res = list_append_long_long(res, &res_len, lst.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (List){.data = res, .data_len = res_len};
}

long long middle_element(List lst) {
    long long n = lst.data_len;
    if (n == 0LL) {
        puts("No element found.");
        return 0LL;
    }
    long long slow = 0LL;
    long long fast = 0LL;
    while ((fast + 1LL) < n) {
        fast = fast + 2LL;
        slow = slow + 1LL;
    }
    return lst.data[(int)({long long _mochi_idx = slow; _mochi_idx < 0 ? lst.data_len + _mochi_idx : _mochi_idx;})];
}

void user_main() {
    List lst = empty_list();
    middle_element(lst);
    lst = push(lst, 5LL);
    printf("%d\n", 5LL);
    lst = push(lst, 6LL);
    printf("%d\n", 6LL);
    lst = push(lst, 8LL);
    printf("%d\n", 8LL);
    lst = push(lst, 8LL);
    printf("%d\n", 8LL);
    lst = push(lst, 10LL);
    printf("%d\n", 10LL);
    lst = push(lst, 12LL);
    printf("%d\n", 12LL);
    lst = push(lst, 17LL);
    printf("%d\n", 17LL);
    lst = push(lst, 7LL);
    printf("%d\n", 7LL);
    lst = push(lst, 3LL);
    printf("%d\n", 3LL);
    lst = push(lst, 20LL);
    printf("%d\n", 20LL);
    lst = push(lst, -20LL);
    printf("%d\n", -20LL);
    printf("%d\n", middle_element(lst));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
