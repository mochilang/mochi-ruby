// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LinkedList LinkedList;

struct LinkedList {
    long long *data;
    size_t data_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

LinkedList empty_list();
LinkedList append_value(LinkedList list, long long value);
LinkedList extend_list(LinkedList list, long long * items, size_t items_len);
const char* to_string(LinkedList list);
LinkedList make_linked_list(long long * items, size_t items_len);
const char* in_reverse(LinkedList list);
void user_main();
int main(void);

LinkedList empty_list() {
    return (LinkedList){.data = NULL, .data_len = 0};
}

LinkedList append_value(LinkedList list, long long value) {
    long long *d = list.data;
    size_t d_len = list.data_len;
    d = list_append_long_long(d, &d_len, value);
    return (LinkedList){.data = d, .data_len = d_len};
}

LinkedList extend_list(LinkedList list, long long * items, size_t items_len) {
    LinkedList result = list;
    long long i = 0LL;
    while (i < items_len) {
        result = append_value(result, items[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? items_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return result;
}

const char* to_string(LinkedList list) {
    if (list.data_len == 0LL) {
        return "";
    }
    const char* s = str_int(list.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
    long long i = 1LL;
    while (i < list.data_len) {
        s = str_concat(str_concat(s, " -> "), str_int(list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return s;
}

LinkedList make_linked_list(long long * items, size_t items_len) {
    if (items_len == 0LL) {
        panic("The Elements List is empty");
    }
    LinkedList ll = empty_list();
    ll = extend_list(ll, items, items_len);
    return ll;
}

const char* in_reverse(LinkedList list) {
    if (list.data_len == 0LL) {
        return "";
    }
    long long i = list.data_len - 1LL;
    const char* s = str_int(list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
    i = i - 1LL;
    while (i >= 0LL) {
        s = str_concat(str_concat(s, " <- "), str_int(list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]));
        i = i - 1LL;
    }
    return s;
}

void user_main() {
    LinkedList linked_list = make_linked_list(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 14LL; tmp[1] = 52LL; tmp[2] = 14LL; tmp[3] = 12LL; tmp[4] = 43LL; tmp;}), 5);
    puts(str_concat("Linked List:  ", to_string(linked_list)));
    puts(str_concat("Reverse List: ", in_reverse(linked_list)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
