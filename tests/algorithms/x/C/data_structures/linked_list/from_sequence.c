// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    long long data;
    long long next;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long NIL = -1LL;
Node *nodes = NULL;
size_t nodes_len = 0;

long long make_linked_list(long long * elements, size_t elements_len);
const char* node_to_string(long long head);
void user_main();
int main(void);

long long make_linked_list(long long * elements, size_t elements_len) {
    if (elements_len == 0LL) {
        panic("The Elements List is empty");
    }
    nodes = malloc(0 * sizeof(Node));
    nodes_len = 0;
    nodes = list_append_Node(nodes, &nodes_len, (Node){.data = elements[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? elements_len + _mochi_idx : _mochi_idx;})], .next = NIL});
    long long head = 0LL;
    long long current = head;
    long long i = 1LL;
    while (i < elements_len) {
        nodes = list_append_Node(nodes, &nodes_len, (Node){.data = elements[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? elements_len + _mochi_idx : _mochi_idx;})], .next = NIL});
        nodes[(int)(current)].next = nodes_len - 1LL;
        current = nodes_len - 1LL;
        i = i + 1LL;
    }
    return head;
}

const char* node_to_string(long long head) {
    const char* s = "";
    long long index = head;
    while (index != NIL) {
        Node node = nodes[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        s = str_concat(str_concat(str_concat(s, "<"), str_int(node.data)), "> ---> ");
        index = node.next;
    }
    s = str_concat(s, "<END>");
    return s;
}

void user_main() {
    long long *list_data = NULL;
    size_t list_data_len = 0;
    list_data = list_append_long_long(list_data, &list_data_len, 1LL);
    list_data = list_append_long_long(list_data, &list_data_len, 3LL);
    list_data = list_append_long_long(list_data, &list_data_len, 5LL);
    list_data = list_append_long_long(list_data, &list_data_len, 32LL);
    list_data = list_append_long_long(list_data, &list_data_len, 44LL);
    list_data = list_append_long_long(list_data, &list_data_len, 12LL);
    list_data = list_append_long_long(list_data, &list_data_len, 43LL);
    puts(str_concat("List: ", str_list_int(list_data, list_data_len)));
    puts("Creating Linked List from List.");
    long long head = make_linked_list(list_data, list_data_len);
    puts("Linked List:");
    puts(node_to_string(head));
}

int main(void) {
    {
        long long __start = _now();
        nodes = malloc(0 * sizeof(Node));
        nodes_len = 0;
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
