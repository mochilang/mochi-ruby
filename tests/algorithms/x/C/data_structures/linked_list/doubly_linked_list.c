// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct DoublyLinkedList DoublyLinkedList;
typedef struct DeleteResult DeleteResult;

struct DoublyLinkedList {
    long long *data;
    size_t data_len;
};

struct DeleteResult {
    DoublyLinkedList list;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

DoublyLinkedList empty_list();
long long length(DoublyLinkedList list);
long long is_empty(DoublyLinkedList list);
const char* to_string(DoublyLinkedList list);
DoublyLinkedList insert_nth(DoublyLinkedList list, long long index, long long value);
DoublyLinkedList insert_head(DoublyLinkedList list, long long value);
DoublyLinkedList insert_tail(DoublyLinkedList list, long long value);
DeleteResult delete_nth(DoublyLinkedList list, long long index);
DeleteResult delete_head(DoublyLinkedList list);
DeleteResult delete_tail(DoublyLinkedList list);
DeleteResult delete_value(DoublyLinkedList list, long long value);
void user_main();
int main(void);

DoublyLinkedList empty_list() {
    return (DoublyLinkedList){.data = NULL, .data_len = 0};
}

long long length(DoublyLinkedList list) {
    return list.data_len;
}

long long is_empty(DoublyLinkedList list) {
    return list.data_len == 0LL;
}

const char* to_string(DoublyLinkedList list) {
    if (list.data_len == 0LL) {
        return "";
    }
    const char* s = str_int(list.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
    long long i = 1LL;
    while (i < list.data_len) {
        s = str_concat(str_concat(s, "->"), str_int(list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return s;
}

DoublyLinkedList insert_nth(DoublyLinkedList list, long long index, long long value) {
    if ((index < 0LL) || (index > list.data_len)) {
        panic("index out of range");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < index) {
        res = list_append_long_long(res, &res_len, list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    res = list_append_long_long(res, &res_len, value);
    while (i < list.data_len) {
        res = list_append_long_long(res, &res_len, list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (DoublyLinkedList){.data = res, .data_len = res_len};
}

DoublyLinkedList insert_head(DoublyLinkedList list, long long value) {
    return insert_nth(list, 0LL, value);
}

DoublyLinkedList insert_tail(DoublyLinkedList list, long long value) {
    return insert_nth(list, list.data_len, value);
}

DeleteResult delete_nth(DoublyLinkedList list, long long index) {
    if ((index < 0LL) || (index >= list.data_len)) {
        panic("index out of range");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    long long removed = 0LL;
    while (i < list.data_len) {
        if (i == index) {
            removed = list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})];
        } else {
            res = list_append_long_long(res, &res_len, list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return (DeleteResult){.list = (DoublyLinkedList){.data = res, .data_len = res_len}, .value = removed};
}

DeleteResult delete_head(DoublyLinkedList list) {
    return delete_nth(list, 0LL);
}

DeleteResult delete_tail(DoublyLinkedList list) {
    return delete_nth(list, list.data_len - 1LL);
}

DeleteResult delete_value(DoublyLinkedList list, long long value) {
    long long idx = 0LL;
    long long found = 0LL;
    while (idx < list.data_len) {
        if (list.data[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})] == value) {
            found = 1LL;
            break;
        }
        idx = idx + 1LL;
    }
    if (!(found)) {
        panic("value not found");
    }
    return delete_nth(list, idx);
}

void user_main() {
    DoublyLinkedList dll = empty_list();
    dll = insert_tail(dll, 1LL);
    dll = insert_tail(dll, 2LL);
    dll = insert_tail(dll, 3LL);
    puts(to_string(dll));
    dll = insert_head(dll, 0LL);
    puts(to_string(dll));
    dll = insert_nth(dll, 2LL, 9LL);
    puts(to_string(dll));
    DeleteResult res = delete_nth(dll, 2LL);
    dll = res.list;
    printf("%d\n", res.value);
    puts(to_string(dll));
    res = delete_tail(dll);
    dll = res.list;
    printf("%d\n", res.value);
    puts(to_string(dll));
    res = delete_value(dll, 1LL);
    dll = res.list;
    printf("%d\n", res.value);
    puts(to_string(dll));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
