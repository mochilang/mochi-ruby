// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static size_t concat_len;
static long long* concat_long_long(long long *a, size_t a_len, const long long *b, size_t b_len) {
    long long *res = realloc(a, (a_len + b_len) * sizeof(long long));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(long long));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LinkedList LinkedList;

struct LinkedList {
    long long *data;
    size_t data_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

LinkedList empty_list();
LinkedList push(LinkedList list, long long value);
LinkedList swap_nodes(LinkedList list, long long v1, long long v2);
const char* to_string(LinkedList list);
void user_main();
int main(void);

LinkedList empty_list() {
    return (LinkedList){.data = NULL, .data_len = 0};
}

LinkedList push(LinkedList list, long long value) {
    long long *res = NULL;
    size_t res_len = 0;
    res = list_append_long_long(res, &res_len, value);
    res = concat_long_long(res, res_len, list.data, list.data_len);
    res_len = concat_len;
    return (LinkedList){.data = res, .data_len = res_len};
}

LinkedList swap_nodes(LinkedList list, long long v1, long long v2) {
    if (v1 == v2) {
        return list;
    }
    long long idx1 = -1LL;
    long long idx2 = -1LL;
    long long i = 0LL;
    while (i < list.data_len) {
        if ((list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})] == v1) && (idx1 == (0LL - 1LL))) {
            idx1 = i;
        }
        if ((list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})] == v2) && (idx2 == (0LL - 1LL))) {
            idx2 = i;
        }
        i = i + 1LL;
    }
    if ((idx1 == (0LL - 1LL)) || (idx2 == (0LL - 1LL))) {
        return list;
    }
    long long *res = list.data;
    size_t res_len = list.data_len;
    long long temp = res[(int)({long long _mochi_idx = idx1; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    res[(int)(idx1)] = res[(int)({long long _mochi_idx = idx2; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    res[(int)(idx2)] = temp;
    return (LinkedList){.data = res, .data_len = res_len};
}

const char* to_string(LinkedList list) {
    return str_list_int(list.data, list.data_len);
}

void user_main() {
    LinkedList ll = empty_list();
    long long i = 5LL;
    while (i > 0LL) {
        ll = push(ll, i);
        i = i - 1LL;
    }
    puts(str_concat("Original Linked List: ", to_string(ll)));
    ll = swap_nodes(ll, 1LL, 4LL);
    puts(str_concat("Modified Linked List: ", to_string(ll)));
    puts("After swapping the nodes whose data is 1 and 4.");
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
