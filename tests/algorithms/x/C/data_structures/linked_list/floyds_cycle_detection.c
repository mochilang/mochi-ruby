// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LinkedList LinkedList;

struct LinkedList {
    long long *next;
    size_t next_len;
    long long head;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long NULL_ = -1LL;

LinkedList empty_list();
LinkedList add_node(LinkedList list, long long value);
LinkedList set_next(LinkedList list, long long index, long long next_index);
long long detect_cycle(LinkedList list);
void user_main();
int main(void);

LinkedList empty_list() {
    return (LinkedList){.next = NULL, .next_len = 0, .head = NULL_};
}

LinkedList add_node(LinkedList list, long long value) {
    long long *nexts = list.next;
    size_t nexts_len = list.next_len;
    long long new_index = nexts_len;
    nexts = list_append_long_long(nexts, &nexts_len, NULL_);
    if (list.head == NULL_) {
        return (LinkedList){.next = nexts, .next_len = nexts_len, .head = new_index};
    }
    long long last = list.head;
    while (nexts[(int)({long long _mochi_idx = last; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})] != NULL_) {
        last = nexts[(int)({long long _mochi_idx = last; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})];
    }
    long long *new_nexts = NULL;
    size_t new_nexts_len = 0;
    long long i = 0LL;
    while (i < nexts_len) {
        if (i == last) {
            new_nexts = list_append_long_long(new_nexts, &new_nexts_len, new_index);
        } else {
            new_nexts = list_append_long_long(new_nexts, &new_nexts_len, nexts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return (LinkedList){.next = new_nexts, .next_len = new_nexts_len, .head = list.head};
}

LinkedList set_next(LinkedList list, long long index, long long next_index) {
    long long *nexts = list.next;
    size_t nexts_len = list.next_len;
    long long *new_nexts = NULL;
    size_t new_nexts_len = 0;
    long long i = 0LL;
    while (i < nexts_len) {
        if (i == index) {
            new_nexts = list_append_long_long(new_nexts, &new_nexts_len, next_index);
        } else {
            new_nexts = list_append_long_long(new_nexts, &new_nexts_len, nexts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return (LinkedList){.next = new_nexts, .next_len = new_nexts_len, .head = list.head};
}

long long detect_cycle(LinkedList list) {
    if (list.head == NULL_) {
        return 0LL;
    }
    long long *nexts = list.next;
    size_t nexts_len = list.next_len;
    long long slow = list.head;
    long long fast = list.head;
    while ((fast != NULL_) && (nexts[(int)({long long _mochi_idx = fast; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})] != NULL_)) {
        slow = nexts[(int)({long long _mochi_idx = slow; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})];
        fast = nexts[(int)({long long _mochi_idx = nexts[(int)({long long _mochi_idx = fast; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? nexts_len + _mochi_idx : _mochi_idx;})];
        if (slow == fast) {
            return 1LL;
        }
    }
    return 0LL;
}

void user_main() {
    LinkedList ll = empty_list();
    ll = add_node(ll, 1LL);
    ll = add_node(ll, 2LL);
    ll = add_node(ll, 3LL);
    ll = add_node(ll, 4LL);
    ll = set_next(ll, 3LL, 1LL);
    printf("%d\n", detect_cycle(ll));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
