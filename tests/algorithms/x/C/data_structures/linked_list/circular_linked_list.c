// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct CircularLinkedList CircularLinkedList;
typedef struct DeleteResult DeleteResult;

struct CircularLinkedList {
    long long *data;
    size_t data_len;
};

struct DeleteResult {
    CircularLinkedList list;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

CircularLinkedList empty_list();
long long length(CircularLinkedList list);
long long is_empty(CircularLinkedList list);
const char* to_string(CircularLinkedList list);
CircularLinkedList insert_nth(CircularLinkedList list, long long index, long long value);
CircularLinkedList insert_head(CircularLinkedList list, long long value);
CircularLinkedList insert_tail(CircularLinkedList list, long long value);
DeleteResult delete_nth(CircularLinkedList list, long long index);
DeleteResult delete_front(CircularLinkedList list);
DeleteResult delete_tail(CircularLinkedList list);
void user_main();
int main(void);

CircularLinkedList empty_list() {
    return (CircularLinkedList){.data = NULL, .data_len = 0};
}

long long length(CircularLinkedList list) {
    return list.data_len;
}

long long is_empty(CircularLinkedList list) {
    return list.data_len == 0LL;
}

const char* to_string(CircularLinkedList list) {
    if (list.data_len == 0LL) {
        return "";
    }
    const char* s = str_int(list.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
    long long i = 1LL;
    while (i < list.data_len) {
        s = str_concat(str_concat(s, "->"), str_int(list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return s;
}

CircularLinkedList insert_nth(CircularLinkedList list, long long index, long long value) {
    if ((index < 0LL) || (index > list.data_len)) {
        panic("index out of range");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < index) {
        res = list_append_long_long(res, &res_len, list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    res = list_append_long_long(res, &res_len, value);
    while (i < list.data_len) {
        res = list_append_long_long(res, &res_len, list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (CircularLinkedList){.data = res, .data_len = res_len};
}

CircularLinkedList insert_head(CircularLinkedList list, long long value) {
    return insert_nth(list, 0LL, value);
}

CircularLinkedList insert_tail(CircularLinkedList list, long long value) {
    return insert_nth(list, list.data_len, value);
}

DeleteResult delete_nth(CircularLinkedList list, long long index) {
    if ((index < 0LL) || (index >= list.data_len)) {
        panic("index out of range");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    long long val = 0LL;
    while (i < list.data_len) {
        if (i == index) {
            val = list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})];
        } else {
            res = list_append_long_long(res, &res_len, list.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list.data_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return (DeleteResult){.list = (CircularLinkedList){.data = res, .data_len = res_len}, .value = val};
}

DeleteResult delete_front(CircularLinkedList list) {
    return delete_nth(list, 0LL);
}

DeleteResult delete_tail(CircularLinkedList list) {
    return delete_nth(list, list.data_len - 1LL);
}

void user_main() {
    CircularLinkedList cll = empty_list();
    long long i = 0LL;
    while (i < 5LL) {
        cll = insert_tail(cll, i + 1LL);
        i = i + 1LL;
    }
    puts(to_string(cll));
    cll = insert_tail(cll, 6LL);
    puts(to_string(cll));
    cll = insert_head(cll, 0LL);
    puts(to_string(cll));
    DeleteResult res = delete_front(cll);
    cll = res.list;
    printf("%d\n", res.value);
    res = delete_tail(cll);
    cll = res.list;
    printf("%d\n", res.value);
    res = delete_nth(cll, 2LL);
    cll = res.list;
    printf("%d\n", res.value);
    puts(to_string(cll));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
