// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_palindrome(long long * values, size_t values_len);
void user_main();
int main(void);

long long is_palindrome(long long * values, size_t values_len) {
    long long *stack = NULL;
    size_t stack_len = 0;
    long long fast = 0LL;
    long long slow = 0LL;
    long long n = values_len;
    while ((fast < n) && ((fast + 1LL) < n)) {
        stack = list_append_long_long(stack, &stack_len, values[(int)({long long _mochi_idx = slow; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]);
        slow = slow + 1LL;
        fast = fast + 2LL;
    }
    if (fast == (n - 1LL)) {
        slow = slow + 1LL;
    }
    long long i = stack_len - 1LL;
    while (slow < n) {
        if (stack[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})] != values[(int)({long long _mochi_idx = slow; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i - 1LL;
        slow = slow + 1LL;
    }
    return 1LL;
}

void user_main() {
    printf("%d\n", is_palindrome(NULL, 0));
    printf("%d\n", is_palindrome(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 1LL; tmp;}), 1));
    printf("%d\n", is_palindrome(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp;}), 2));
    printf("%d\n", is_palindrome(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 1LL; tmp;}), 3));
    printf("%d\n", is_palindrome(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 2LL; tmp[3] = 1LL; tmp;}), 4));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
