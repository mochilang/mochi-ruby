// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Queue Queue;
typedef struct GetResult GetResult;

struct Queue {
    long long *entries;
    size_t entries_len;
};

struct GetResult {
    Queue queue;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Queue q;
GetResult res;
long long front = 0;

Queue new_queue(long long * items, size_t items_len);
long long len_queue(Queue q);
const char* str_queue(Queue q);
Queue put(Queue q, long long item);
GetResult get(Queue q);
Queue rotate(Queue q, long long rotation);
long long get_front(Queue q);
int main(void);

Queue new_queue(long long * items, size_t items_len) {
    return (Queue){.entries = items, .entries_len = items_len};
}

long long len_queue(Queue q) {
    return q.entries_len;
}

const char* str_queue(Queue q) {
    const char* s = "Queue((";
    long long i = 0LL;
    while (i < q.entries_len) {
        s = str_concat(s, str_int(q.entries[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? q.entries_len + _mochi_idx : _mochi_idx;})]));
        if (i < (q.entries_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "))");
    return s;
}

Queue put(Queue q, long long item) {
    long long *e = q.entries;
    size_t e_len = q.entries_len;
    e = list_append_long_long(e, &e_len, item);
    return (Queue){.entries = e, .entries_len = e_len};
}

GetResult get(Queue q) {
    if (q.entries_len == 0LL) {
        panic("Queue is empty");
    }
    long long value = q.entries[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? q.entries_len + _mochi_idx : _mochi_idx;})];
    long long *new_entries = NULL;
    size_t new_entries_len = 0;
    long long i = 1LL;
    while (i < q.entries_len) {
        new_entries = list_append_long_long(new_entries, &new_entries_len, q.entries[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? q.entries_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (GetResult){.queue = (Queue){.entries = new_entries, .entries_len = new_entries_len}, .value = value};
}

Queue rotate(Queue q, long long rotation) {
    long long *e = q.entries;
    size_t e_len = q.entries_len;
    long long r = 0LL;
    while (r < rotation) {
        if (e_len > 0LL) {
            long long first = e[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})];
            long long *rest = NULL;
            size_t rest_len = 0;
            long long i = 1LL;
            while (i < e_len) {
                rest = list_append_long_long(rest, &rest_len, e[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})]);
                i = i + 1LL;
            }
            rest = list_append_long_long(rest, &rest_len, first);
            e = rest;
            e_len = rest_len;
        }
        r = r + 1LL;
    }
    return (Queue){.entries = e, .entries_len = e_len};
}

long long get_front(Queue q) {
    return q.entries[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? q.entries_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        q = new_queue(NULL, 0);
        printf("%d\n", len_queue(q));
        q = put(q, 10LL);
        q = put(q, 20LL);
        q = put(q, 30LL);
        q = put(q, 40LL);
        puts(str_queue(q));
        res = get(q);
        q = res.queue;
        printf("%d\n", res.value);
        puts(str_queue(q));
        q = rotate(q, 2LL);
        puts(str_queue(q));
        front = get_front(q);
        printf("%d\n", front);
        puts(str_queue(q));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
