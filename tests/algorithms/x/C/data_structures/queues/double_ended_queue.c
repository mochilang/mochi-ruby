// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Deque Deque;
typedef struct PopResult PopResult;

struct Deque {
    long long *data;
    size_t data_len;
};

struct PopResult {
    Deque deque;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Deque empty_deque();
Deque push_back(Deque dq, long long value);
Deque push_front(Deque dq, long long value);
Deque extend_back(Deque dq, long long * values, size_t values_len);
Deque extend_front(Deque dq, long long * values, size_t values_len);
PopResult pop_back(Deque dq);
PopResult pop_front(Deque dq);
long long is_empty(Deque dq);
long long length(Deque dq);
const char* to_string(Deque dq);
void user_main();
int main(void);

Deque empty_deque() {
    return (Deque){.data = NULL, .data_len = 0};
}

Deque push_back(Deque dq, long long value) {
    return (Deque){.data = list_append_int_new(dq.data, dq.data_len, value), .data_len = dq.data_len + 1};
}

Deque push_front(Deque dq, long long value) {
    long long *res = NULL;
    size_t res_len = 0;
    res = list_append_long_long(res, &res_len, value);
    long long i = 0LL;
    while (i < dq.data_len) {
        res = list_append_long_long(res, &res_len, dq.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (Deque){.data = res, .data_len = res_len};
}

Deque extend_back(Deque dq, long long * values, size_t values_len) {
    long long *res = dq.data;
    size_t res_len = dq.data_len;
    long long i = 0LL;
    while (i < values_len) {
        res = list_append_long_long(res, &res_len, values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (Deque){.data = res, .data_len = res_len};
}

Deque extend_front(Deque dq, long long * values, size_t values_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = values_len - 1LL;
    while (i >= 0LL) {
        res = list_append_long_long(res, &res_len, values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]);
        i = i - 1LL;
    }
    long long j = 0LL;
    while (j < dq.data_len) {
        res = list_append_long_long(res, &res_len, dq.data[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return (Deque){.data = res, .data_len = res_len};
}

PopResult pop_back(Deque dq) {
    if (dq.data_len == 0LL) {
        panic("pop from empty deque");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < (dq.data_len - 1LL)) {
        res = list_append_long_long(res, &res_len, dq.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (PopResult){.deque = (Deque){.data = res, .data_len = res_len}, .value = dq.data[(int)({long long _mochi_idx = dq.data_len - 1LL; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]};
}

PopResult pop_front(Deque dq) {
    if (dq.data_len == 0LL) {
        panic("popleft from empty deque");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 1LL;
    while (i < dq.data_len) {
        res = list_append_long_long(res, &res_len, dq.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (PopResult){.deque = (Deque){.data = res, .data_len = res_len}, .value = dq.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]};
}

long long is_empty(Deque dq) {
    return dq.data_len == 0LL;
}

long long length(Deque dq) {
    return dq.data_len;
}

const char* to_string(Deque dq) {
    if (dq.data_len == 0LL) {
        return "[]";
    }
    const char* s = str_concat("[", str_int(dq.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]));
    long long i = 1LL;
    while (i < dq.data_len) {
        s = str_concat(str_concat(s, ", "), str_int(dq.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dq.data_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return str_concat(s, "]");
}

void user_main() {
    Deque dq = empty_deque();
    dq = push_back(dq, 2LL);
    dq = push_front(dq, 1LL);
    dq = extend_back(dq, ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp;}), 2);
    dq = extend_front(dq, ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 0LL; tmp;}), 1);
    puts(to_string(dq));
    PopResult r = pop_back(dq);
    dq = r.deque;
    printf("%d\n", r.value);
    r = pop_front(dq);
    dq = r.deque;
    printf("%d\n", r.value);
    puts(to_string(dq));
    printf("%d\n", is_empty(empty_deque()));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
