// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct ElementPriorityQueue ElementPriorityQueue;
typedef struct FixedPriorityQueue FixedPriorityQueue;
typedef struct EPQDequeueResult EPQDequeueResult;
typedef struct FPQDequeueResult FPQDequeueResult;

struct ElementPriorityQueue {
    long long *queue;
    size_t queue_len;
};

struct FixedPriorityQueue {
    long long **queues;
    size_t queues_len;
    size_t *queues_lens;
    size_t queues_lens_len;
};

struct EPQDequeueResult {
    ElementPriorityQueue queue;
    long long value;
};

struct FPQDequeueResult {
    FixedPriorityQueue queue;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

void user_panic(const char* msg);
FixedPriorityQueue fpq_new();
FixedPriorityQueue fpq_enqueue(FixedPriorityQueue fpq, long long priority, long long data);
FPQDequeueResult fpq_dequeue(FixedPriorityQueue fpq);
const char* fpq_to_string(FixedPriorityQueue fpq);
ElementPriorityQueue epq_new();
ElementPriorityQueue epq_enqueue(ElementPriorityQueue epq, long long data);
EPQDequeueResult epq_dequeue(ElementPriorityQueue epq);
const char* epq_to_string(ElementPriorityQueue epq);
void fixed_priority_queue();
void element_priority_queue();
void user_main();
int main(void);

void user_panic(const char* msg) {
    puts(msg);
}

FixedPriorityQueue fpq_new() {
    return (FixedPriorityQueue){.queues = ({long long** tmp = malloc(3 * sizeof(long long*)); tmp[0] = NULL; tmp[1] = NULL; tmp[2] = NULL; tmp;}), .queues_len = 3, .queues_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 0; tmp[1] = 0; tmp[2] = 0; tmp;}), .queues_lens_len = 3};
}

FixedPriorityQueue fpq_enqueue(FixedPriorityQueue fpq, long long priority, long long data) {
    if ((priority < 0LL) || (priority >= fpq.queues_len)) {
        user_panic("Valid priorities are 0, 1, and 2");
        return fpq;
    }
    if (fpq.queues_lens[priority] >= 100LL) {
        user_panic("Maximum queue size is 100");
        return fpq;
    }
    long long **qs = fpq.queues;
    size_t qs_len = fpq.queues_len;
    size_t *qs_lens = fpq.queues_lens;
    size_t qs_lens_len = fpq.queues_len;
    qs[(int)(priority)] = list_append_int_new(qs[(int)({long long _mochi_idx = priority; _mochi_idx < 0 ? qs_len + _mochi_idx : _mochi_idx;})], qs_lens[(int)(priority)], data);
    fpq.queues = qs;
    fpq.queues_len = qs_len;
    fpq.queues_lens = qs_lens;
    fpq.queues_lens_len = qs_lens_len;
    return fpq;
}

FPQDequeueResult fpq_dequeue(FixedPriorityQueue fpq) {
    long long **qs = fpq.queues;
    size_t qs_len = fpq.queues_len;
    size_t *qs_lens = fpq.queues_lens;
    size_t qs_lens_len = fpq.queues_len;
    long long i = 0LL;
    while (i < qs_len) {
        long long *q = qs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? qs_len + _mochi_idx : _mochi_idx;})];
        size_t q_len = qs_lens[(int)(i)];
        if (q_len > 0LL) {
            long long val = q[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})];
            long long *new_q = NULL;
            size_t new_q_len = 0;
            long long j = 1LL;
            while (j < q_len) {
                new_q = list_append_long_long(new_q, &new_q_len, q[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})]);
                j = j + 1LL;
            }
            qs[(int)(i)] = new_q;
            fpq.queues = qs;
            fpq.queues_len = qs_len;
            fpq.queues_lens = qs_lens;
            fpq.queues_lens_len = qs_lens_len;
            return (FPQDequeueResult){.queue = fpq, .value = val};
        }
        i = i + 1LL;
    }
    user_panic("All queues are empty");
    return (FPQDequeueResult){.queue = fpq, .value = 0LL};
}

const char* fpq_to_string(FixedPriorityQueue fpq) {
    const char* *lines = NULL;
    size_t lines_len = 0;
    long long i = 0LL;
    while (i < fpq.queues_len) {
        const char* q_str = "[";
        long long *q = fpq.queues[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? fpq.queues_len + _mochi_idx : _mochi_idx;})];
        size_t q_len = fpq.queues_lens[i];
        long long j = 0LL;
        while (j < q_len) {
            if (j > 0LL) {
                q_str = str_concat(q_str, ", ");
            }
            q_str = str_concat(q_str, str_int(q[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})]));
            j = j + 1LL;
        }
        q_str = str_concat(q_str, "]");
        lines = list_append_str(lines, &lines_len, str_concat(str_concat(str_concat("Priority ", str_int(i)), ": "), q_str));
        i = i + 1LL;
    }
    const char* res = "";
    i = 0LL;
    while (i < lines_len) {
        if (i > 0LL) {
            res = str_concat(res, "\n");
        }
        res = str_concat(res, lines[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lines_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return res;
}

ElementPriorityQueue epq_new() {
    return (ElementPriorityQueue){.queue = NULL, .queue_len = 0};
}

ElementPriorityQueue epq_enqueue(ElementPriorityQueue epq, long long data) {
    if (epq.queue_len >= 100LL) {
        user_panic("Maximum queue size is 100");
        return epq;
    }
    epq.queue = list_append_long_long(epq.queue, &epq.queue_len, data);
    return epq;
}

EPQDequeueResult epq_dequeue(ElementPriorityQueue epq) {
    if (epq.queue_len == 0LL) {
        user_panic("The queue is empty");
        return (EPQDequeueResult){.queue = epq, .value = 0LL};
    }
    long long min_val = epq.queue[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? epq.queue_len + _mochi_idx : _mochi_idx;})];
    long long idx = 0LL;
    long long i = 1LL;
    while (i < epq.queue_len) {
        long long v = epq.queue[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? epq.queue_len + _mochi_idx : _mochi_idx;})];
        if (v < min_val) {
            min_val = v;
            idx = i;
        }
        i = i + 1LL;
    }
    long long *new_q = NULL;
    size_t new_q_len = 0;
    i = 0LL;
    while (i < epq.queue_len) {
        if (i != idx) {
            new_q = list_append_long_long(new_q, &new_q_len, epq.queue[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? epq.queue_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    epq.queue = new_q;
    epq.queue_len = new_q_len;
    return (EPQDequeueResult){.queue = epq, .value = min_val};
}

const char* epq_to_string(ElementPriorityQueue epq) {
    return str_list_int(epq.queue, epq.queue_len);
}

void fixed_priority_queue() {
    FixedPriorityQueue fpq = fpq_new();
    fpq = fpq_enqueue(fpq, 0LL, 10LL);
    fpq = fpq_enqueue(fpq, 1LL, 70LL);
    fpq = fpq_enqueue(fpq, 0LL, 100LL);
    fpq = fpq_enqueue(fpq, 2LL, 1LL);
    fpq = fpq_enqueue(fpq, 2LL, 5LL);
    fpq = fpq_enqueue(fpq, 1LL, 7LL);
    fpq = fpq_enqueue(fpq, 2LL, 4LL);
    fpq = fpq_enqueue(fpq, 1LL, 64LL);
    fpq = fpq_enqueue(fpq, 0LL, 128LL);
    puts(fpq_to_string(fpq));
    FPQDequeueResult res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    puts(fpq_to_string(fpq));
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
    res = fpq_dequeue(fpq);
    fpq = res.queue;
    printf("%d\n", res.value);
}

void element_priority_queue() {
    ElementPriorityQueue epq = epq_new();
    epq = epq_enqueue(epq, 10LL);
    epq = epq_enqueue(epq, 70LL);
    epq = epq_enqueue(epq, 100LL);
    epq = epq_enqueue(epq, 1LL);
    epq = epq_enqueue(epq, 5LL);
    epq = epq_enqueue(epq, 7LL);
    epq = epq_enqueue(epq, 4LL);
    epq = epq_enqueue(epq, 64LL);
    epq = epq_enqueue(epq, 128LL);
    puts(epq_to_string(epq));
    EPQDequeueResult res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    puts(epq_to_string(epq));
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
    res = epq_dequeue(epq);
    epq = res.queue;
    printf("%d\n", res.value);
}

void user_main() {
    fixed_priority_queue();
    element_priority_queue();
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
