// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Queue Queue;
typedef struct GetResult GetResult;

struct Queue {
    long long *stack1;
    size_t stack1_len;
    long long *stack2;
    size_t stack2_len;
};

struct GetResult {
    Queue queue;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Queue q;
GetResult r1;
GetResult r2;
GetResult r3;
GetResult r4;

Queue new_queue(long long * items, size_t items_len);
long long len_queue(Queue q);
const char* str_queue(Queue q);
Queue put(Queue q, long long item);
GetResult get(Queue q);
int main(void);

Queue new_queue(long long * items, size_t items_len) {
    return (Queue){.stack1 = items, .stack1_len = items_len, .stack2 = NULL, .stack2_len = 0};
}

long long len_queue(Queue q) {
    return q.stack1_len + q.stack2_len;
}

const char* str_queue(Queue q) {
    long long *items = NULL;
    size_t items_len = 0;
    long long i = q.stack2_len - 1LL;
    while (i >= 0LL) {
        items = list_append_long_long(items, &items_len, q.stack2[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? q.stack2_len + _mochi_idx : _mochi_idx;})]);
        i = i - 1LL;
    }
    long long j = 0LL;
    while (j < q.stack1_len) {
        items = list_append_long_long(items, &items_len, q.stack1[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? q.stack1_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    const char* s = "Queue((";
    long long k = 0LL;
    while (k < items_len) {
        s = str_concat(s, str_int(items[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? items_len + _mochi_idx : _mochi_idx;})]));
        if (k < (items_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        k = k + 1LL;
    }
    s = str_concat(s, "))");
    return s;
}

Queue put(Queue q, long long item) {
    long long *s1 = q.stack1;
    size_t s1_len = q.stack1_len;
    s1 = list_append_long_long(s1, &s1_len, item);
    return (Queue){.stack1 = s1, .stack1_len = s1_len, .stack2 = q.stack2, .stack2_len = q.stack2_len};
}

GetResult get(Queue q) {
    long long *s1 = q.stack1;
    size_t s1_len = q.stack1_len;
    long long *s2 = q.stack2;
    size_t s2_len = q.stack2_len;
    if (s2_len == 0LL) {
        while (s1_len > 0LL) {
            long long idx = s1_len - 1LL;
            long long v = s1[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? s1_len + _mochi_idx : _mochi_idx;})];
            long long *new_s1 = NULL;
            size_t new_s1_len = 0;
            long long i = 0LL;
            while (i < idx) {
                new_s1 = list_append_long_long(new_s1, &new_s1_len, s1[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? s1_len + _mochi_idx : _mochi_idx;})]);
                i = i + 1LL;
            }
            s1 = new_s1;
            s1_len = new_s1_len;
            s2 = list_append_long_long(s2, &s2_len, v);
        }
    }
    if (s2_len == 0LL) {
        panic("Queue is empty");
    }
    long long idx2 = s2_len - 1LL;
    long long value = s2[(int)({long long _mochi_idx = idx2; _mochi_idx < 0 ? s2_len + _mochi_idx : _mochi_idx;})];
    long long *new_s2 = NULL;
    size_t new_s2_len = 0;
    long long j = 0LL;
    while (j < idx2) {
        new_s2 = list_append_long_long(new_s2, &new_s2_len, s2[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? s2_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    s2 = new_s2;
    s2_len = new_s2_len;
    return (GetResult){.queue = (Queue){.stack1 = s1, .stack1_len = s1_len, .stack2 = s2, .stack2_len = s2_len}, .value = value};
}

int main(void) {
    {
        long long __start = _now();
        q = new_queue(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 20LL; tmp[2] = 30LL; tmp;}), 3);
        r1 = get(q);
        q = r1.queue;
        printf("%d\n", r1.value);
        q = put(q, 40LL);
        r2 = get(q);
        q = r2.queue;
        printf("%d\n", r2.value);
        r3 = get(q);
        q = r3.queue;
        printf("%d\n", r3.value);
        printf("%d\n", len_queue(q));
        r4 = get(q);
        q = r4.queue;
        printf("%d\n", r4.value);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
