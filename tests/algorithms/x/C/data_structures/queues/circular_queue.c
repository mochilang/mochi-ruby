// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct CircularQueue CircularQueue;
typedef struct DequeueResult DequeueResult;

struct CircularQueue {
    long long *data;
    size_t data_len;
    long long front;
    long long rear;
    long long size;
    long long capacity;
};

struct DequeueResult {
    CircularQueue queue;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

CircularQueue create_queue(long long capacity);
long long length(CircularQueue q);
long long is_empty(CircularQueue q);
long long front(CircularQueue q);
CircularQueue enqueue(CircularQueue q, long long value);
DequeueResult dequeue(CircularQueue q);
void user_main();
int main(void);

CircularQueue create_queue(long long capacity) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < capacity) {
        arr = list_append_long_long(arr, &arr_len, 0LL);
        i = i + 1LL;
    }
    return (CircularQueue){.data = arr, .data_len = arr_len, .front = 0LL, .rear = 0LL, .size = 0LL, .capacity = capacity};
}

long long length(CircularQueue q) {
    return q.size;
}

long long is_empty(CircularQueue q) {
    return q.size == 0LL;
}

long long front(CircularQueue q) {
    if (is_empty(q)) {
        return 0LL;
    }
    return q.data[(int)({long long _mochi_idx = q.front; _mochi_idx < 0 ? q.data_len + _mochi_idx : _mochi_idx;})];
}

CircularQueue enqueue(CircularQueue q, long long value) {
    if (q.size >= q.capacity) {
        panic("QUEUE IS FULL");
    }
    long long *arr = q.data;
    size_t arr_len = q.data_len;
    arr[(int)(q.rear)] = value;
    q.data = arr;
    q.data_len = arr_len;
    q.rear = (q.rear + 1LL) % q.capacity;
    q.size = q.size + 1LL;
    return q;
}

DequeueResult dequeue(CircularQueue q) {
    if (q.size == 0LL) {
        panic("UNDERFLOW");
    }
    long long value = q.data[(int)({long long _mochi_idx = q.front; _mochi_idx < 0 ? q.data_len + _mochi_idx : _mochi_idx;})];
    long long *arr2 = q.data;
    size_t arr2_len = q.data_len;
    arr2[(int)(q.front)] = 0LL;
    q.data = arr2;
    q.data_len = arr2_len;
    q.front = (q.front + 1LL) % q.capacity;
    q.size = q.size - 1LL;
    return (DequeueResult){.queue = q, .value = value};
}

void user_main() {
    CircularQueue q = create_queue(5LL);
    printf("%d\n", is_empty(q));
    q = enqueue(q, 10LL);
    printf("%d\n", is_empty(q));
    q = enqueue(q, 20LL);
    q = enqueue(q, 30LL);
    printf("%d\n", front(q));
    DequeueResult r = dequeue(q);
    q = r.queue;
    printf("%d\n", r.value);
    printf("%d\n", front(q));
    printf("%d\n", length(q));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
