// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct SuffixTree SuffixTree;

struct SuffixTree {
    const char* text;
};

const char* text = "banana";
SuffixTree st;
const char* *patterns_exist = NULL;
size_t patterns_exist_len = 0;
long long i = 0LL;
const char* *patterns_none = NULL;
size_t patterns_none_len = 0;
const char* *substrings = NULL;
size_t substrings_len = 0;

SuffixTree suffix_tree_new(const char* text);
long long suffix_tree_search(SuffixTree st, const char* pattern);
int main(void);

SuffixTree suffix_tree_new(const char* text) {
    return (SuffixTree){.text = text};
}

long long suffix_tree_search(SuffixTree st, const char* pattern) {
    if (strlen(pattern) == 0LL) {
        return 1LL;
    }
    long long i = 0LL;
    long long n = strlen(st.text);
    long long m = strlen(pattern);
    while (i <= (n - m)) {
        long long j = 0LL;
        long long found = 1LL;
        while (j < m) {
            if (strcmp((const char[]){st.text[i + j], 0}, (const char[]){pattern[j], 0}) != 0) {
                found = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (found) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        st = suffix_tree_new(text);
        patterns_exist = malloc(3 * sizeof(const char*));
        patterns_exist_len = 3;
        patterns_exist[0] = "ana";
        patterns_exist[1] = "ban";
        patterns_exist[2] = "na";
        while (i < 3LL) {
            puts(str_int(suffix_tree_search(st, patterns_exist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? patterns_exist_len + _mochi_idx : _mochi_idx;})])));
            i = i + 1LL;
        }
        patterns_none = malloc(3 * sizeof(const char*));
        patterns_none_len = 3;
        patterns_none[0] = "xyz";
        patterns_none[1] = "apple";
        patterns_none[2] = "cat";
        i = 0LL;
        while (i < 3LL) {
            puts(str_int(suffix_tree_search(st, patterns_none[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? patterns_none_len + _mochi_idx : _mochi_idx;})])));
            i = i + 1LL;
        }
        puts(str_int(suffix_tree_search(st, "")));
        puts(str_int(suffix_tree_search(st, text)));
        substrings = malloc(4 * sizeof(const char*));
        substrings_len = 4;
        substrings[0] = "ban";
        substrings[1] = "ana";
        substrings[2] = "a";
        substrings[3] = "na";
        i = 0LL;
        while (i < 4LL) {
            puts(str_int(suffix_tree_search(st, substrings[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? substrings_len + _mochi_idx : _mochi_idx;})])));
            i = i + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
