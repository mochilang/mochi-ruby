// Generated by Mochi 0.10.32 on 2025-08-25 08:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t sort_points_len;
size_t *sort_points_lens;

size_t _slice_doubleptr_len;
size_t *_slice_doubleptr_lens;
size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static double** _slice_doubleptr(double **arr, size_t len, size_t *lens, int start, int end, size_t *out_len, size_t **out_lens) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    *out_len = n;
    _slice_doubleptr_len = n;
    *out_lens = lens + start;
    _slice_doubleptr_lens = lens + start;
    return arr + start;
}

typedef struct KDNode KDNode;

struct KDNode {
    double *point;
    size_t point_len;
    long long left;
    long long right;
};

static KDNode* list_append_KDNode(KDNode *arr, size_t *len, KDNode val) {
    arr = realloc(arr, (*len + 1) * sizeof(KDNode));
    arr[*len] = val;
    (*len)++;
    return arr;
}

KDNode *tree = NULL;
size_t tree_len = 0;
double **pts = NULL;
size_t pts_len = 0;
size_t *pts_lens = NULL;
size_t pts_lens_len = 0;
long long root = 0;

double * * sort_points(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long axis);
long long build_kdtree(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long depth);
int main(void);

double * * sort_points(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long axis) {
    double **arr = points;
    size_t arr_len = points_len;
    size_t *arr_lens = points_lens;
    size_t arr_lens_len = points_len;
    long long i = 0LL;
    while (i < arr_len) {
        long long j = 0LL;
        while (j < (arr_len - 1LL)) {
            if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = axis; _mochi_idx < 0 ? arr_lens[(int)(j)] + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = axis; _mochi_idx < 0 ? arr_lens[(int)(j + 1LL)] + _mochi_idx : _mochi_idx;})]) {
                double *tmp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                size_t tmp_len = arr_lens[(int)(j)];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_points_lens = arr_lens, sort_points_len = arr_len, arr;
}

long long build_kdtree(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long depth) {
    if (points_len == 0LL) {
        return -1LL;
    }
    long long k = points_lens[(int)(0LL)];
    long long axis = depth % k;
    double **sorted = sort_points(points, points_len, points_lens, points_len, axis);
    size_t sorted_len = sort_points_len;
    size_t *sorted_lens = sort_points_lens;
    size_t sorted_lens_len = sort_points_len;
    long long median_idx = sorted_len / 2LL;
    double **left_points = _slice_doubleptr(sorted, sorted_len, sorted_lens, 0LL, median_idx, &(_slice_doubleptr_len), &(_slice_doubleptr_lens));
    size_t left_points_len = _slice_doubleptr_len;
    size_t *left_points_lens = _slice_doubleptr_lens;
    size_t left_points_lens_len = _slice_doubleptr_len;
    double **right_points = _slice_doubleptr(sorted, sorted_len, sorted_lens, median_idx + 1LL, sorted_len, &(_slice_doubleptr_len), &(_slice_doubleptr_lens));
    size_t right_points_len = _slice_doubleptr_len;
    size_t *right_points_lens = _slice_doubleptr_lens;
    size_t right_points_lens_len = _slice_doubleptr_len;
    long long idx = tree_len;
    tree = list_append_KDNode(tree, &tree_len, (KDNode){.point = sorted[(int)({long long _mochi_idx = median_idx; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})], .point_len = sorted_lens[(int)(median_idx)], .left = -1LL, .right = -1LL});
    long long left_idx = build_kdtree(left_points, left_points_len, left_points_lens, left_points_len, depth + 1LL);
    long long right_idx = build_kdtree(right_points, right_points_len, right_points_lens, right_points_len, depth + 1LL);
    KDNode node = tree[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? tree_len + _mochi_idx : _mochi_idx;})];
    node.left = left_idx;
    node.right = right_idx;
    tree[(int)(idx)] = node;
    return idx;
}

int main(void) {
    {
        long long __start = _now();
        tree = malloc(0 * sizeof(KDNode));
        tree_len = 0;
        pts = malloc(6 * sizeof(double*));
        pts_len = 6;
        pts[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 3.0; tmp;});
        pts[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 5.0; tmp[1] = 4.0; tmp;});
        pts[2] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 9.0; tmp[1] = 6.0; tmp;});
        pts[3] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 4.0; tmp[1] = 7.0; tmp;});
        pts[4] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 8.0; tmp[1] = 1.0; tmp;});
        pts[5] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 7.0; tmp[1] = 2.0; tmp;});
        pts_lens = ({size_t *tmp = malloc(6 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp[3] = 2; tmp[4] = 2; tmp[5] = 2; tmp;});
        pts_lens_len = 6;
        root = build_kdtree(pts, pts_len, pts_lens, pts_len, 0LL);
        puts(str_int(tree));
        printf("%d\n", root);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
