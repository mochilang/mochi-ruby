// Generated by Mochi 0.10.32 on 2025-08-25 16:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t hypercube_points_len;
size_t *hypercube_points_lens;
size_t build_kdtree_len;
size_t *build_kdtree_lens;

size_t append_len;
typedef struct { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double INF = 1e+09;
long long seed = 1LL;

double rand_float();
double * * hypercube_points(long long num_points, double cube_size, long long num_dimensions);
double * * build_kdtree(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long depth);
double distance_sq(double * a, size_t a_len, double * b, size_t b_len);
MapSD nearest_neighbour_search(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, double * query, size_t query_len);
void test_build_cases();
void test_search();
void test_edge();
void user_main();
int main(void);

double rand_float() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return (double)(seed) / 2.147483648e+09;
}

double * * hypercube_points(long long num_points, double cube_size, long long num_dimensions) {
    double **pts = NULL;
    size_t pts_len = 0;
    size_t *pts_lens = NULL;
    size_t pts_lens_len = 0;
    long long i = 0LL;
    while (i < num_points) {
        double *p = NULL;
        size_t p_len = 0;
        long long j = 0LL;
        while (j < num_dimensions) {
            double v = cube_size * rand_float();
            p = list_append_double(p, &p_len, v);
            j = j + 1LL;
        }
        pts = list_append_doubleptr(pts, &pts_len, p);
        pts_lens = list_append_szt(pts_lens, &pts_lens_len, p_len);
        i = i + 1LL;
    }
    return hypercube_points_lens = pts_lens, hypercube_points_len = pts_len, pts;
}

double * * build_kdtree(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long depth) {
    return build_kdtree_lens = points_lens, build_kdtree_len = points_len, points;
}

double distance_sq(double * a, size_t a_len, double * b, size_t b_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < a_len) {
        double d = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})];
        sum = sum + (d * d);
        i = i + 1LL;
    }
    return sum;
}

MapSD nearest_neighbour_search(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, double * query, size_t query_len) {
    if (points_len == 0LL) {
        {
            const char** __ret0_keys = malloc(3 * sizeof(char*));
            double *__ret0_vals = malloc(3 * sizeof(double));
            __ret0_keys[0] = "index";
            __ret0_vals[0] = -1.0;
            __ret0_keys[1] = "dist";
            __ret0_vals[1] = INF;
            __ret0_keys[2] = "visited";
            __ret0_vals[2] = 0.0;
            MapSD __ret0 = { __ret0_keys, __ret0_vals, 3, 19 };
            return __ret0;
        }
    }
    long long nearest_idx = 0LL;
    double nearest_dist = INF;
    long long visited = 0LL;
    long long i = 0LL;
    while (i < points_len) {
        double d = distance_sq(query, query_len, points[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})], points_lens[(int)(i)]);
        visited = visited + 1LL;
        if (d < nearest_dist) {
            nearest_dist = d;
            nearest_idx = i;
        }
        i = i + 1LL;
    }
    {
        const char** __ret1_keys = malloc(3 * sizeof(char*));
        double *__ret1_vals = malloc(3 * sizeof(double));
        __ret1_keys[0] = "index";
        __ret1_vals[0] = (double)(nearest_idx);
        __ret1_keys[1] = "dist";
        __ret1_vals[1] = nearest_dist;
        __ret1_keys[2] = "visited";
        __ret1_vals[2] = (double)(visited);
        MapSD __ret1 = { __ret1_keys, __ret1_vals, 3, 19 };
        return __ret1;
    }
}

void test_build_cases() {
    double **empty_pts = NULL;
    size_t empty_pts_len = 0;
    size_t *empty_pts_lens = NULL;
    size_t empty_pts_lens_len = 0;
    double **tree0 = build_kdtree(empty_pts, empty_pts_len, empty_pts_lens, empty_pts_len, 0LL);
    size_t tree0_len = build_kdtree_len;
    size_t *tree0_lens = build_kdtree_lens;
    size_t tree0_lens_len = build_kdtree_len;
    if (tree0_len == 0LL) {
        puts("case1 true");
    } else {
        puts("case1 false");
    }
    double **pts1 = hypercube_points(10LL, 10.0, 2LL);
    size_t pts1_len = hypercube_points_len;
    size_t *pts1_lens = hypercube_points_lens;
    size_t pts1_lens_len = hypercube_points_len;
    double **tree1 = build_kdtree(pts1, pts1_len, pts1_lens, pts1_len, 2LL);
    size_t tree1_len = build_kdtree_len;
    size_t *tree1_lens = build_kdtree_lens;
    size_t tree1_lens_len = build_kdtree_len;
    if ((tree1_len > 0LL) && (tree1_lens[(int)(0LL)] == 2LL)) {
        puts("case2 true");
    } else {
        puts("case2 false");
    }
    double **pts2 = hypercube_points(10LL, 10.0, 3LL);
    size_t pts2_len = hypercube_points_len;
    size_t *pts2_lens = hypercube_points_lens;
    size_t pts2_lens_len = hypercube_points_len;
    double **tree2 = build_kdtree(pts2, pts2_len, pts2_lens, pts2_len, -2LL);
    size_t tree2_len = build_kdtree_len;
    size_t *tree2_lens = build_kdtree_lens;
    size_t tree2_lens_len = build_kdtree_len;
    if ((tree2_len > 0LL) && (tree2_lens[(int)(0LL)] == 3LL)) {
        puts("case3 true");
    } else {
        puts("case3 false");
    }
}

void test_search() {
    double **pts = hypercube_points(10LL, 10.0, 2LL);
    size_t pts_len = hypercube_points_len;
    size_t *pts_lens = hypercube_points_lens;
    size_t pts_lens_len = hypercube_points_len;
    double **tree = build_kdtree(pts, pts_len, pts_lens, pts_len, 0LL);
    size_t tree_len = build_kdtree_len;
    size_t *tree_lens = build_kdtree_lens;
    size_t tree_lens_len = build_kdtree_len;
    double *qp = hypercube_points(1LL, 10.0, 2LL)[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? hypercube_points_len + _mochi_idx : _mochi_idx;})];
    size_t qp_len = 0;
    MapSD res = nearest_neighbour_search(tree, tree_len, tree_lens, tree_len, qp, qp_len);
    if (((map_get_sd(res.keys, res.vals, res.len, "index") != -1.0) && (map_get_sd(res.keys, res.vals, res.len, "dist") >= 0.0)) && (map_get_sd(res.keys, res.vals, res.len, "visited") > 0.0)) {
        puts("search true");
    } else {
        puts("search false");
    }
}

void test_edge() {
    double **empty_pts = NULL;
    size_t empty_pts_len = 0;
    size_t *empty_pts_lens = NULL;
    size_t empty_pts_lens_len = 0;
    double **tree = build_kdtree(empty_pts, empty_pts_len, empty_pts_lens, empty_pts_len, 0LL);
    size_t tree_len = build_kdtree_len;
    size_t *tree_lens = build_kdtree_lens;
    size_t tree_lens_len = build_kdtree_len;
    double *query = NULL;
    size_t query_len = 0;
    query = list_append_double(query, &query_len, 0.0);
    query = list_append_double(query, &query_len, 0.0);
    MapSD res = nearest_neighbour_search(tree, tree_len, tree_lens, tree_len, query, query_len);
    if (((map_get_sd(res.keys, res.vals, res.len, "index") == -1.0) && (map_get_sd(res.keys, res.vals, res.len, "dist") > 1e+08)) && (map_get_sd(res.keys, res.vals, res.len, "visited") == 0.0)) {
        puts("edge true");
    } else {
        puts("edge false");
    }
}

void user_main() {
    seed = 1LL;
    test_build_cases();
    test_search();
    test_edge();
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
