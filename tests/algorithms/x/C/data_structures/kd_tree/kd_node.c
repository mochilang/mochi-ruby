// Generated by Mochi 0.10.32 on 2025-08-08 19:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct KDNode KDNode;

struct KDNode {
    double *point;
    size_t point_len;
    long long left;
    long long right;
};

static KDNode* list_append_KDNode(KDNode *arr, size_t *len, KDNode val) {
    arr = realloc(arr, (*len + 1) * sizeof(KDNode));
    arr[*len] = val;
    (*len)++;
    return arr;
}

KDNode *nodes = NULL;
size_t nodes_len = 0;
KDNode root;
KDNode left_child;
KDNode right_child;

KDNode make_kd_node(double * point, size_t point_len, long long left, long long right);
int main(void);

KDNode make_kd_node(double * point, size_t point_len, long long left, long long right) {
    return (KDNode){.point = point, .point_len = point_len, .left = left, .right = right};
}

int main(void) {
    {
        long long __start = _now();
        nodes = list_append_KDNode(nodes, &nodes_len, make_kd_node(({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 3.0; tmp;}), 2, 1LL, 2LL));
        nodes = list_append_KDNode(nodes, &nodes_len, make_kd_node(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 5.0; tmp;}), 2, -1LL, -1LL));
        nodes = list_append_KDNode(nodes, &nodes_len, make_kd_node(({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 4.0; tmp[1] = 2.0; tmp;}), 2, -1LL, -1LL));
        root = nodes[(int)(0LL)];
        left_child = nodes[(int)(1LL)];
        right_child = nodes[(int)(2LL)];
        puts(str_list_double(root.point, root.point_len));
        puts(str_int(root.left));
        puts(str_int(root.right));
        puts(str_list_double(left_child.point, left_child.point_len));
        puts(str_list_double(right_child.point, right_child.point_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
