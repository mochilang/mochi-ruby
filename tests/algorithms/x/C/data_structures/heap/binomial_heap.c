// Generated by Mochi 0.10.32 on 2025-08-24 22:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t _slice_int_len;
size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

typedef struct BinomialHeap BinomialHeap;
typedef struct DeleteResult DeleteResult;

struct BinomialHeap {
    long long *data;
    size_t data_len;
};

struct DeleteResult {
    BinomialHeap heap;
    long long value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

BinomialHeap new_heap();
void swap(long long * data, size_t data_len, long long i, long long j);
void sift_up(long long * data, size_t data_len, long long idx);
void sift_down(long long * data, size_t data_len, long long idx);
BinomialHeap insert(BinomialHeap heap, long long v);
long long peek(BinomialHeap heap);
long long is_empty(BinomialHeap heap);
DeleteResult delete_min(BinomialHeap heap);
void user_main();
int main(void);

BinomialHeap new_heap() {
    return (BinomialHeap){.data = NULL, .data_len = 0};
}

void swap(long long * data, size_t data_len, long long i, long long j) {
    long long tmp = data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})];
    data[(int)(i)] = data[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})];
    data[(int)(j)] = tmp;
}

void sift_up(long long * data, size_t data_len, long long idx) {
    long long i = idx;
    while (i > 0LL) {
        long long parent = (i - 1LL) / 2LL;
        if (data[(int)({long long _mochi_idx = parent; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})] <= data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})]) {
            break;
        }
        swap(data, data_len, parent, i);
        i = parent;
    }
}

void sift_down(long long * data, size_t data_len, long long idx) {
    long long i = idx;
    long long n = data_len;
    while (1) {
        long long left = (2LL * i) + 1LL;
        long long right = left + 1LL;
        long long smallest = i;
        if ((left < n) && (data[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})] < data[(int)({long long _mochi_idx = smallest; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})])) {
            smallest = left;
        }
        if ((right < n) && (data[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})] < data[(int)({long long _mochi_idx = smallest; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})])) {
            smallest = right;
        }
        if (smallest == i) {
            break;
        }
        swap(data, data_len, i, smallest);
        i = smallest;
    }
}

BinomialHeap insert(BinomialHeap heap, long long v) {
    long long *d = heap.data;
    size_t d_len = heap.data_len;
    d = list_append_long_long(d, &d_len, v);
    sift_up(d, d_len, d_len - 1LL);
    return (BinomialHeap){.data = d, .data_len = d_len};
}

long long peek(BinomialHeap heap) {
    return heap.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? heap.data_len + _mochi_idx : _mochi_idx;})];
}

long long is_empty(BinomialHeap heap) {
    return heap.data_len == 0LL;
}

DeleteResult delete_min(BinomialHeap heap) {
    long long *d = heap.data;
    size_t d_len = heap.data_len;
    long long min = d[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? d_len + _mochi_idx : _mochi_idx;})];
    d[(int)(0LL)] = d[(int)({long long _mochi_idx = d_len - 1LL; _mochi_idx < 0 ? d_len + _mochi_idx : _mochi_idx;})];
    d = _slice_int(d, d_len, 0LL, d_len - 1LL, &(_slice_int_len));
    d_len = _slice_int_len;
    if (d_len > 0LL) {
        sift_down(d, d_len, 0LL);
    }
    return (DeleteResult){.heap = (BinomialHeap){.data = d, .data_len = d_len}, .value = min};
}

void user_main() {
    BinomialHeap h = new_heap();
    h = insert(h, 10LL);
    h = insert(h, 3LL);
    h = insert(h, 7LL);
    puts(str_int(peek(h)));
    DeleteResult d1 = delete_min(h);
    h = d1.heap;
    puts(str_int(d1.value));
    DeleteResult d2 = delete_min(h);
    h = d2.heap;
    puts(str_int(d2.value));
    DeleteResult d3 = delete_min(h);
    h = d3.heap;
    puts(str_int(d3.value));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
