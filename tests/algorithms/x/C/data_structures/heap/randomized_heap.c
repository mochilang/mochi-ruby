// Generated by Mochi 0.10.32 on 2025-08-25 07:29 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t to_sorted_list_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static MapSI* list_append_MapSI(MapSI *arr, size_t *len, MapSI val) {
    arr = realloc(arr, (*len + 1) * sizeof(MapSI));
    arr[*len].keys = malloc(val.cap * sizeof(char*));
    arr[*len].vals = malloc(val.cap * sizeof(int));
    memcpy(arr[*len].keys, val.keys, val.len * sizeof(char*));
    memcpy(arr[*len].vals, val.vals, val.len * sizeof(int));
    arr[*len].len = val.len;
    arr[*len].cap = val.cap;
    (*len)++;
    return arr;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long NIL = -1LL;
long long seed = 1LL;
MapSI *nodes = NULL;
size_t nodes_len = 0;
long long root = 0;

void set_seed(long long s);
long long randint(long long a, long long b);
long long rand_bool();
void new_heap();
long long merge(long long r1, long long r2);
void insert(long long value);
long long top();
long long pop();
long long is_empty();
long long * to_sorted_list();
int main(void);

void set_seed(long long s) {
    seed = s;
}

long long randint(long long a, long long b) {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return (seed % ((b - a) + 1LL)) + a;
}

long long rand_bool() {
    return randint(0LL, 1LL) == 1LL;
}

void new_heap() {
    nodes = NULL;
    nodes = malloc(0 * sizeof(MapSI));
    nodes_len = 0;
    root = NIL;
}

long long merge(long long r1, long long r2) {
    if (r1 == NIL) {
        return r2;
    }
    if (r2 == NIL) {
        return r1;
    }
    if (map_get_si(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "value") > map_get_si(nodes[(int)({long long _mochi_idx = r2; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = r2; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = r2; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "value")) {
        long long tmp = r1;
        r1 = r2;
        r2 = tmp;
    }
    if (rand_bool()) {
        long long tmp = map_get_si(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "left");
        map_set_si(&(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})]), "left", map_get_si(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "right"));
        map_set_si(&(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})]), "right", tmp);
    }
    map_set_si(&(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})]), "left", merge(map_get_si(nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = r1; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "left"), r2));
    return r1;
}

void insert(long long value) {
    const char* node_keys[19] = {"value", "left", "right"};
    int node_vals[19] = {value, -1, -1};
    size_t node_len = 3;
    MapSI node = { node_keys, node_vals, 3, 19 };
    nodes = list_append_MapSI(nodes, &nodes_len, node);
    long long idx = nodes_len - 1LL;
    root = merge(root, idx);
}

long long top() {
    if (root == NIL) {
        return 0LL;
    }
    return map_get_si(nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "value");
}

long long pop() {
    long long result = top();
    long long l = map_get_si(nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "left");
    long long r = map_get_si(nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].keys, nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].vals, nodes[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})].len, "right");
    root = merge(l, r);
    return result;
}

long long is_empty() {
    return root == NIL;
}

long long * to_sorted_list() {
    long long *res = NULL;
    size_t res_len = 0;
    while (!(is_empty())) {
        res = list_append_long_long(res, &res_len, pop());
    }
    return to_sorted_list_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        nodes = NULL;
        nodes = malloc(0 * sizeof(MapSI));
        nodes_len = 0;
        root = NIL;
        set_seed(1LL);
        new_heap();
        insert(2LL);
        insert(3LL);
        insert(1LL);
        insert(5LL);
        insert(1LL);
        insert(7LL);
        long long* __tmp2 = to_sorted_list();
        puts(str_list_int(__tmp2, to_sorted_list_len));
        new_heap();
        insert(1LL);
        insert(-1LL);
        insert(0LL);
        long long* __tmp3 = to_sorted_list();
        puts(str_list_int(__tmp3, to_sorted_list_len));
        new_heap();
        insert(3LL);
        insert(1LL);
        insert(3LL);
        insert(7LL);
        printf("%d\n", pop());
        printf("%d\n", pop());
        printf("%d\n", pop());
        printf("%d\n", pop());
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
