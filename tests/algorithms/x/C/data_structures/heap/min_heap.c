// Generated by Mochi 0.10.32 on 2025-08-24 23:54 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t slice_without_last_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct MinHeap MinHeap;
typedef struct Node Node;

struct MinHeap {
    Node *heap;
    size_t heap_len;
    MapSI idx_of_element;
    MapSI heap_dict;
};

struct Node {
    const char* name;
    long long val;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Node r = (Node){.name = "R", .val = -1LL};
Node b = (Node){.name = "B", .val = 6LL};
Node a = (Node){.name = "A", .val = 3LL};
Node x = (Node){.name = "X", .val = 1LL};
Node e = (Node){.name = "E", .val = 4LL};
MinHeap my_min_heap;

long long get_parent_idx(long long idx);
long long get_left_child_idx(long long idx);
long long get_right_child_idx(long long idx);
MapSI remove_key(MapSI m, const char* k);
Node * slice_without_last(Node * xs, size_t xs_len);
void sift_down(MinHeap* mh, long long idx);
void sift_up(MinHeap* mh, long long idx);
MinHeap new_min_heap(Node * array, size_t array_len);
Node peek(MinHeap mh);
Node remove_min(MinHeap* mh);
void insert(MinHeap* mh, Node node);
long long is_empty(MinHeap mh);
long long get_value(MinHeap mh, const char* key);
void decrease_key(MinHeap* mh, Node* node, long long new_value);
const char* node_to_string(Node n);
int main(void);

long long get_parent_idx(long long idx) {
    return (idx - 1LL) / 2LL;
}

long long get_left_child_idx(long long idx) {
    return (idx * 2LL) + 1LL;
}

long long get_right_child_idx(long long idx) {
    return (idx * 2LL) + 2LL;
}

MapSI remove_key(MapSI m, const char* k) {
    static const char* out_keys[16] = {};
    static int out_vals[16] = {};
    size_t out_len = 0;
    MapSI out = { out_keys, out_vals, 0, 16 };
    MapSI __tmp0 = m;
    for (size_t __i = 0; __i < __tmp0.len; __i++) {
        const char* key = __tmp0.keys[__i];
        if (key != k) {
            map_set_si(&(out), key, map_get_si(m.keys, m.vals, m.len, key));
        }
    }
    return out;
}

Node * slice_without_last(Node * xs, size_t xs_len) {
    Node *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < (xs_len - 1LL)) {
        res = list_append_Node(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return slice_without_last_len = res_len, res;
}

void sift_down(MinHeap* mh, long long idx) {
    Node *heap = mh->heap;
    size_t heap_len = mh->heap_len;
    MapSI idx_map = mh->idx_of_element;
    long long i = idx;
    while (1) {
        long long left = get_left_child_idx(i);
        long long right = get_right_child_idx(i);
        long long smallest = i;
        if ((left < heap_len) && (heap[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val < heap[(int)({long long _mochi_idx = smallest; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val)) {
            smallest = left;
        }
        if ((right < heap_len) && (heap[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val < heap[(int)({long long _mochi_idx = smallest; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val)) {
            smallest = right;
        }
        if (smallest != i) {
            Node tmp = heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
            heap[(int)(i)] = heap[(int)({long long _mochi_idx = smallest; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
            heap[(int)(smallest)] = tmp;
            map_set_si(&(idx_map), heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].name, i);
            map_set_si(&(idx_map), heap[(int)({long long _mochi_idx = smallest; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].name, smallest);
            i = smallest;
        } else {
            break;
        }
    }
    mh->heap = heap;
    mh->heap_len = heap_len;
    mh->idx_of_element = idx_map;
}

void sift_up(MinHeap* mh, long long idx) {
    Node *heap = mh->heap;
    size_t heap_len = mh->heap_len;
    MapSI idx_map = mh->idx_of_element;
    long long i = idx;
    long long p = get_parent_idx(i);
    while ((p >= 0LL) && (heap[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val > heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val)) {
        Node tmp = heap[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
        heap[(int)(p)] = heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
        heap[(int)(i)] = tmp;
        map_set_si(&(idx_map), heap[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].name, p);
        map_set_si(&(idx_map), heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].name, i);
        i = p;
        p = get_parent_idx(i);
    }
    mh->heap = heap;
    mh->heap_len = heap_len;
    mh->idx_of_element = idx_map;
}

MinHeap new_min_heap(Node * array, size_t array_len) {
    static const char* idx_map_keys[16] = {};
    static int idx_map_vals[16] = {};
    size_t idx_map_len = 0;
    MapSI idx_map = { idx_map_keys, idx_map_vals, 0, 16 };
    static const char* val_map_keys[16] = {};
    static int val_map_vals[16] = {};
    size_t val_map_len = 0;
    MapSI val_map = { val_map_keys, val_map_vals, 0, 16 };
    Node *heap = array;
    size_t heap_len = array_len;
    long long i = 0LL;
    while (i < array_len) {
        Node n = array[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})];
        map_set_si(&(idx_map), n.name, i);
        map_set_si(&(val_map), n.name, n.val);
        i = i + 1LL;
    }
    MinHeap mh = (MinHeap){.heap = heap, .heap_len = heap_len, .idx_of_element = idx_map, .heap_dict = val_map};
    long long start = get_parent_idx(array_len - 1LL);
    while (start >= 0LL) {
        sift_down(&mh, start);
        start = start - 1LL;
    }
    return mh;
}

Node peek(MinHeap mh) {
    return mh.heap[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? mh.heap_len + _mochi_idx : _mochi_idx;})];
}

Node remove_min(MinHeap* mh) {
    Node *heap = mh->heap;
    size_t heap_len = mh->heap_len;
    MapSI idx_map = mh->idx_of_element;
    MapSI val_map = mh->heap_dict;
    long long last_idx = heap_len - 1LL;
    Node top = heap[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
    Node last = heap[(int)({long long _mochi_idx = last_idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
    heap[(int)(0LL)] = last;
    map_set_si(&(idx_map), last.name, 0LL);
    heap = slice_without_last(heap, heap_len);
    heap_len = slice_without_last_len;
    idx_map = remove_key(idx_map, top.name);
    val_map = remove_key(val_map, top.name);
    mh->heap = heap;
    mh->heap_len = heap_len;
    mh->idx_of_element = idx_map;
    mh->heap_dict = val_map;
    if (heap_len > 0LL) {
        sift_down(mh, 0LL);
    }
    return top;
}

void insert(MinHeap* mh, Node node) {
    Node *heap = mh->heap;
    size_t heap_len = mh->heap_len;
    MapSI idx_map = mh->idx_of_element;
    MapSI val_map = mh->heap_dict;
    heap = list_append_Node(heap, &heap_len, node);
    long long idx = heap_len - 1LL;
    map_set_si(&(idx_map), node.name, idx);
    map_set_si(&(val_map), node.name, node.val);
    mh->heap = heap;
    mh->heap_len = heap_len;
    mh->idx_of_element = idx_map;
    mh->heap_dict = val_map;
    sift_up(mh, idx);
}

long long is_empty(MinHeap mh) {
    return mh.heap_len == 0LL;
}

long long get_value(MinHeap mh, const char* key) {
    return map_get_si(mh.heap_dict.keys, mh.heap_dict.vals, mh.heap_dict.len, key);
}

void decrease_key(MinHeap* mh, Node* node, long long new_value) {
    Node *heap = mh->heap;
    size_t heap_len = mh->heap_len;
    MapSI val_map = mh->heap_dict;
    MapSI idx_map = mh->idx_of_element;
    long long idx = map_get_si(idx_map.keys, idx_map.vals, idx_map.len, node->name);
    if (!(heap[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})].val > new_value)) {
        panic("newValue must be less than current value");
    }
    node->val = new_value;
    heap[(int)(idx)].val = new_value;
    map_set_si(&(val_map), node->name, new_value);
    mh->heap = heap;
    mh->heap_len = heap_len;
    mh->heap_dict = val_map;
    sift_up(mh, idx);
}

const char* node_to_string(Node n) {
    return str_concat(str_concat(str_concat(str_concat("Node(", n.name), ", "), str_int(n.val)), ")");
}

int main(void) {
    {
        long long __start = _now();
        my_min_heap = new_min_heap(({Node *tmp = malloc(5 * sizeof(Node)); tmp[0] = r; tmp[1] = b; tmp[2] = a; tmp[3] = x; tmp[4] = e; tmp;}), 5);
        puts("Min Heap - before decrease key");
        for (size_t __i = 0; __i < my_min_heap.heap_len; __i++) {
            Node n = my_min_heap.heap[__i];
            puts(node_to_string(n));
        }
        puts("Min Heap - After decrease key of node [B -> -17]");
        decrease_key(&my_min_heap, &b, -17LL);
        for (size_t __i = 0; __i < my_min_heap.heap_len; __i++) {
            Node n = my_min_heap.heap[__i];
            puts(node_to_string(n));
        }
        puts(str_int(get_value(my_min_heap, "B")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
