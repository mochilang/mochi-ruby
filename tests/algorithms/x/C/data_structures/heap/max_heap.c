// Generated by Mochi 0.10.32 on 2025-08-24 23:41 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_list_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *heap = NULL;
size_t heap_len = 0;
long long size = 0LL;

void swap_up(long long i);
void insert(long long value);
void swap_down(long long i);
void shrink();
long long pop();
long long * get_list();
long long len();
int main(void);

void swap_up(long long i) {
    long long temp = heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
    long long idx = i;
    while ((idx / 2LL) > 0LL) {
        if (heap[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})] > heap[(int)({long long _mochi_idx = idx / 2LL; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})]) {
            heap[(int)(idx)] = heap[(int)({long long _mochi_idx = idx / 2LL; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
            heap[(int)(idx / 2LL)] = temp;
        }
        idx = idx / 2LL;
    }
}

void insert(long long value) {
    heap = list_append_long_long(heap, &heap_len, value);
    size = size + 1LL;
    swap_up(size);
}

void swap_down(long long i) {
    long long idx = i;
    while (size >= (2LL * idx)) {
        long long bigger_child;
        if (((2LL * idx) + 1LL) > size) {
            bigger_child = 2LL * idx;
        } else {
            bigger_child = (heap[(int)({long long _mochi_idx = 2LL * idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})] > heap[(int)({long long _mochi_idx = (2LL * idx) + 1LL; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})] ? 2LL * idx : (2LL * idx) + 1LL);
        }
        long long temp = heap[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
        if (heap[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})] < heap[(int)({long long _mochi_idx = bigger_child; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})]) {
            heap[(int)(idx)] = heap[(int)({long long _mochi_idx = bigger_child; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
            heap[(int)(bigger_child)] = temp;
        }
        idx = bigger_child;
    }
}

void shrink() {
    long long *new_heap = NULL;
    size_t new_heap_len = 0;
    long long i = 0LL;
    while (i <= size) {
        new_heap = list_append_long_long(new_heap, &new_heap_len, heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    heap = new_heap;
    heap_len = new_heap_len;
}

long long pop() {
    long long max_value = heap[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
    heap[(int)(1LL)] = heap[(int)({long long _mochi_idx = size; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
    size = size - 1LL;
    shrink();
    swap_down(1LL);
    return max_value;
}

long long * get_list() {
    long long *out = NULL;
    size_t out_len = 0;
    long long i = 1LL;
    while (i <= size) {
        out = list_append_long_long(out, &out_len, heap[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return get_list_len = out_len, out;
}

long long len() {
    return size;
}

int main(void) {
    {
        long long __start = _now();
        heap = ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 0LL; tmp;});
        heap = malloc(1 * sizeof(long long));
        heap_len = 1;
        heap[0] = 0LL;
        insert(6LL);
        insert(10LL);
        insert(15LL);
        insert(12LL);
        printf("%d\n", pop());
        printf("%d\n", pop());
        long long* __tmp1 = get_list();
        puts(str_list_int(__tmp1, get_list_len));
        printf("%d\n", len());
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
