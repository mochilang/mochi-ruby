// Generated by Mochi 0.10.32 on 2025-08-24 22:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long isPrime(long long number);
long long nextPrime(long long value, long long factor, long long desc);
int main(void);

long long isPrime(long long number) {
    if (number < 2LL) {
        return 0LL;
    }
    if (number < 4LL) {
        return 1LL;
    }
    if ((number % 2LL) == 0LL) {
        return 0LL;
    }
    long long i = 3LL;
    while ((i * i) <= number) {
        if ((number % i) == 0LL) {
            return 0LL;
        }
        i = i + 2LL;
    }
    return 1LL;
}

long long nextPrime(long long value, long long factor, long long desc) {
    long long v = value * factor;
    long long firstValue = v;
    while (!(isPrime(v))) {
        if (desc) {
            v = v - 1LL;
        } else {
            v = v + 1LL;
        }
    }
    if (v == firstValue) {
        if (desc) {
            return nextPrime(v - 1LL, 1LL, desc);
        } else {
            return nextPrime(v + 1LL, 1LL, desc);
        }
    }
    return v;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", isPrime(0LL));
        printf("%d\n", isPrime(1LL));
        printf("%d\n", isPrime(2LL));
        printf("%d\n", isPrime(3LL));
        printf("%d\n", isPrime(27LL));
        printf("%d\n", isPrime(87LL));
        printf("%d\n", isPrime(563LL));
        printf("%d\n", isPrime(2999LL));
        printf("%d\n", isPrime(67483LL));
        printf("%d\n", nextPrime(14LL, 1LL, 0LL));
        printf("%d\n", nextPrime(14LL, 1LL, 1LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
