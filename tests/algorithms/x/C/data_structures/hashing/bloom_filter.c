// Generated by Mochi 0.10.32 on 2025-08-08 19:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t hash_positions_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Bloom Bloom;

struct Bloom {
    long long size;
    long long *bits;
    size_t bits_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* ascii = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";

long long ord(const char* ch);
Bloom new_bloom(long long size);
long long hash1(const char* value, long long size);
long long hash2(const char* value, long long size);
long long * hash_positions(const char* value, long long size);
Bloom bloom_add(Bloom b, const char* value);
long long bloom_exists(Bloom b, const char* value);
const char* bitstring(Bloom b);
const char* format_hash(Bloom b, const char* value);
double estimated_error_rate(Bloom b);
long long any_in(Bloom b, const char* * items, size_t items_len);
void user_main();
int main(void);

long long ord(const char* ch) {
    long long i = 0LL;
    while (i < strlen(ascii)) {
        if (strcmp(_substring(ascii, i, i + 1LL), ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

Bloom new_bloom(long long size) {
    long long *bits = NULL;
    size_t bits_len = 0;
    long long i = 0LL;
    while (i < size) {
        bits = list_append_long_long(bits, &bits_len, 0LL);
        i = i + 1LL;
    }
    return (Bloom){.size = size, .bits = bits, .bits_len = bits_len};
}

long long hash1(const char* value, long long size) {
    long long h = 0LL;
    long long i = 0LL;
    while (i < strlen(value)) {
        h = ((h * 31LL) + ord(_substring(value, i, i + 1LL))) % size;
        i = i + 1LL;
    }
    return h;
}

long long hash2(const char* value, long long size) {
    long long h = 0LL;
    long long i = 0LL;
    while (i < strlen(value)) {
        h = ((h * 131LL) + ord(_substring(value, i, i + 1LL))) % size;
        i = i + 1LL;
    }
    return h;
}

long long * hash_positions(const char* value, long long size) {
    long long h1 = hash1(value, size);
    long long h2 = hash2(value, size);
    long long *res = NULL;
    size_t res_len = 0;
    res = list_append_long_long(res, &res_len, h1);
    res = list_append_long_long(res, &res_len, h2);
    return hash_positions_len = res_len, res;
}

Bloom bloom_add(Bloom b, const char* value) {
    long long *pos = hash_positions(value, b.size);
    size_t pos_len = hash_positions_len;
    long long *bits = b.bits;
    size_t bits_len = b.bits_len;
    long long i = 0LL;
    while (i < pos_len) {
        long long idx = (b.size - 1LL) - pos[(int)(i)];
        bits[(int)(idx)] = 1LL;
        i = i + 1LL;
    }
    return (Bloom){.size = b.size, .bits = bits, .bits_len = bits_len};
}

long long bloom_exists(Bloom b, const char* value) {
    long long *pos = hash_positions(value, b.size);
    size_t pos_len = hash_positions_len;
    long long i = 0LL;
    while (i < pos_len) {
        long long idx = (b.size - 1LL) - pos[(int)(i)];
        if (b.bits[(int)(idx)] != 1LL) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

const char* bitstring(Bloom b) {
    const char* res = "";
    long long i = 0LL;
    while (i < b.size) {
        res = str_concat(res, str_int(b.bits[(int)(i)]));
        i = i + 1LL;
    }
    return res;
}

const char* format_hash(Bloom b, const char* value) {
    long long *pos = hash_positions(value, b.size);
    size_t pos_len = hash_positions_len;
    long long *bits = NULL;
    size_t bits_len = 0;
    long long i = 0LL;
    while (i < b.size) {
        bits = list_append_long_long(bits, &bits_len, 0LL);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < pos_len) {
        long long idx = (b.size - 1LL) - pos[(int)(i)];
        bits[(int)(idx)] = 1LL;
        i = i + 1LL;
    }
    const char* res = "";
    i = 0LL;
    while (i < b.size) {
        res = str_concat(res, str_int(bits[(int)(i)]));
        i = i + 1LL;
    }
    return res;
}

double estimated_error_rate(Bloom b) {
    long long ones = 0LL;
    long long i = 0LL;
    while (i < b.size) {
        if (b.bits[(int)(i)] == 1LL) {
            ones = ones + 1LL;
        }
        i = i + 1LL;
    }
    double frac = (double)(ones) / (double)(b.size);
    return frac * frac;
}

long long any_in(Bloom b, const char* * items, size_t items_len) {
    long long i = 0LL;
    while (i < items_len) {
        if (bloom_exists(b, items[(int)(i)])) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

void user_main() {
    Bloom bloom = new_bloom(8LL);
    puts(bitstring(bloom));
    printf("%d\n", bloom_exists(bloom, "Titanic"));
    bloom = bloom_add(bloom, "Titanic");
    puts(bitstring(bloom));
    printf("%d\n", bloom_exists(bloom, "Titanic"));
    bloom = bloom_add(bloom, "Avatar");
    printf("%d\n", bloom_exists(bloom, "Avatar"));
    puts(format_hash(bloom, "Avatar"));
    puts(bitstring(bloom));
    const char* *not_present = NULL;
    size_t not_present_len = 0;
    not_present = list_append_str(not_present, &not_present_len, "The Godfather");
    not_present = list_append_str(not_present, &not_present_len, "Interstellar");
    not_present = list_append_str(not_present, &not_present_len, "Parasite");
    not_present = list_append_str(not_present, &not_present_len, "Pulp Fiction");
    long long i = 0LL;
    while (i < 4LL) {
        const char* film = not_present[(int)(i)];
        puts(str_concat(str_concat(film, ":"), format_hash(bloom, film)));
        i = i + 1LL;
    }
    printf("%d\n", any_in(bloom, not_present, not_present_len));
    printf("%d\n", bloom_exists(bloom, "Ratatouille"));
    puts(format_hash(bloom, "Ratatouille"));
    puts(str_float(estimated_error_rate(bloom)));
    bloom = bloom_add(bloom, "The Godfather");
    puts(str_float(estimated_error_rate(bloom)));
    puts(bitstring(bloom));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
