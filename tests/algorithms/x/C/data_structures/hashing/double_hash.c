// Generated by Mochi 0.10.32 on 2025-08-24 16:59 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_table_len;
size_t insert_double_hash_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static char* str_map_ii(MapII m) {
    char *res = strdup("map[");
    for (size_t i = 0; i < m.len; i++) {
        char *k = str_int(m.keys[i]);
        char *v = str_int(m.vals[i]);
        char *kv = str_concat(k, ":");
        kv = str_concat(kv, v);
        res = str_concat(res, kv);
        if (i + 1 < m.len) res = str_concat(res, " ");
        free(k); free(v); free(kv);
    }
    res = str_concat(res, "]");
    return res;
}

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        int *new_keys = malloc(m->cap * sizeof(int));
        int *new_vals = malloc(m->cap * sizeof(int));
        if (m->keys) memcpy(new_keys, m->keys, m->len * sizeof(int));
        if (m->vals) memcpy(new_vals, m->vals, m->len * sizeof(int));
        m->keys = new_keys;
        m->vals = new_vals;
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_prime(long long n);
long long prev_prime(long long n);
long long * create_table(long long size);
long long hash1(long long size, long long key);
long long hash2(long long prime, long long key);
long long * insert_double_hash(long long * values, size_t values_len, long long size, long long prime, long long value);
MapII table_keys(long long * values, size_t values_len);
void run_example(long long size, long long * data, size_t data_len);
int main(void);

long long is_prime(long long n) {
    if (n < 2LL) {
        return 0LL;
    }
    long long i = 2LL;
    while ((i * i) <= n) {
        if ((n % i) == 0LL) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long prev_prime(long long n) {
    long long p = n - 1LL;
    while (p >= 2LL) {
        if (is_prime(p)) {
            return p;
        }
        p = p - 1LL;
    }
    return 1LL;
}

long long * create_table(long long size) {
    long long *vals = NULL;
    size_t vals_len = 0;
    long long i = 0LL;
    while (i < size) {
        vals = list_append_long_long(vals, &vals_len, -1LL);
        i = i + 1LL;
    }
    return create_table_len = vals_len, vals;
}

long long hash1(long long size, long long key) {
    return key % size;
}

long long hash2(long long prime, long long key) {
    return prime - (key % prime);
}

long long * insert_double_hash(long long * values, size_t values_len, long long size, long long prime, long long value) {
    long long *vals = values;
    size_t vals_len = values_len;
    long long idx = hash1(size, value);
    long long step = hash2(prime, value);
    long long count_ = 0LL;
    while ((vals[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? vals_len + _mochi_idx : _mochi_idx;})] != -1LL) && (count_ < size)) {
        idx = (idx + step) % size;
        count_ = count_ + 1LL;
    }
    if (vals[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? vals_len + _mochi_idx : _mochi_idx;})] == -1LL) {
        vals[(int)(idx)] = value;
    }
    return insert_double_hash_len = vals_len, vals;
}

MapII table_keys(long long * values, size_t values_len) {
    MapII res = { NULL, NULL, 0, 0 };
    long long i = 0LL;
    while (i < values_len) {
        if (values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})] != -1LL) {
            map_set_ii(&res, i, values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return res;
}

void run_example(long long size, long long * data, size_t data_len) {
    long long prime = prev_prime(size);
    long long *table = create_table(size);
    size_t table_len = create_table_len;
    long long i = 0LL;
    while (i < data_len) {
        table = insert_double_hash(table, table_len, size, prime, data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})]);
        table_len = insert_double_hash_len;
        i = i + 1LL;
    }
    puts(({MapII __tmp1 = table_keys(table, table_len); char *__res = str_map_ii(__tmp1); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        run_example(3LL, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 20LL; tmp[2] = 30LL; tmp;}), 3);
        run_example(4LL, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 20LL; tmp[2] = 30LL; tmp;}), 3);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
