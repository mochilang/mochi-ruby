// Generated by Mochi 0.10.32 on 2025-08-24 22:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct HashTable HashTable;

struct HashTable {
    long long size_table;
    long long *values;
    size_t values_len;
    double lim_charge;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

HashTable qp;

HashTable create_hash_table(long long size);
long long hash_function(HashTable table, long long key);
double balanced_factor(HashTable table);
long long collision_resolution(HashTable table, long long key);
const char* insert_data(HashTable* table, long long data);
const char* int_to_string(long long n);
const char* keys_to_string(HashTable table);
int main(void);

HashTable create_hash_table(long long size) {
    long long *vals = NULL;
    size_t vals_len = 0;
    long long i = 0LL;
    while (i < size) {
        vals = list_append_long_long(vals, &vals_len, NULL);
        i = i + 1LL;
    }
    return (HashTable){.size_table = size, .values = vals, .values_len = vals_len, .lim_charge = 0.75};
}

long long hash_function(HashTable table, long long key) {
    return key % table.size_table;
}

double balanced_factor(HashTable table) {
    long long count_ = 0LL;
    long long i = 0LL;
    while (i < table.values_len) {
        if (table.values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? table.values_len + _mochi_idx : _mochi_idx;})] != NULL) {
            count_ = count_ + 1LL;
        }
        i = i + 1LL;
    }
    return (double)(count_) / (double)(table.size_table);
}

long long collision_resolution(HashTable table, long long key) {
    long long i = 1LL;
    long long new_key = hash_function(table, key + (i * i));
    while ((table.values[(int)({long long _mochi_idx = new_key; _mochi_idx < 0 ? table.values_len + _mochi_idx : _mochi_idx;})] != NULL) && (table.values[(int)({long long _mochi_idx = new_key; _mochi_idx < 0 ? table.values_len + _mochi_idx : _mochi_idx;})] != key)) {
        i = i + 1LL;
        if (balanced_factor(table) >= table.lim_charge) {
            return table.size_table;
        }
        new_key = hash_function(table, key + (i * i));
    }
    return new_key;
}

const char* insert_data(HashTable* table, long long data) {
    long long key = hash_function(*table, data);
    long long *vals = table->values;
    size_t vals_len = table->values_len;
    if (vals[(int)({long long _mochi_idx = key; _mochi_idx < 0 ? vals_len + _mochi_idx : _mochi_idx;})] == NULL) {
        vals[(int)(key)] = data;
    } else {
        if (vals[(int)({long long _mochi_idx = key; _mochi_idx < 0 ? vals_len + _mochi_idx : _mochi_idx;})] == data) {
            table->values = vals;
            table->values_len = vals_len;
            return 0;
        } else {
            long long new_key = collision_resolution(*table, key);
            if ((new_key < vals_len) && (vals[(int)({long long _mochi_idx = new_key; _mochi_idx < 0 ? vals_len + _mochi_idx : _mochi_idx;})] == NULL)) {
                vals[(int)(new_key)] = data;
            }
        }
    }
    table->values = vals;
    table->values_len = vals_len;
}

const char* int_to_string(long long n) {
    if (n == 0LL) {
        return "0";
    }
    long long num = n;
    long long neg = 0LL;
    if (num < 0LL) {
        neg = 1LL;
        num = -(num);
    }
    const char* res = "";
    while (num > 0LL) {
        long long digit = num % 10LL;
        const char* ch = _substring("0123456789", digit, digit + 1LL);
        res = str_concat(ch, res);
        num = num / 10LL;
    }
    if (neg) {
        res = str_concat("-", res);
    }
    return res;
}

const char* keys_to_string(HashTable table) {
    const char* result = "{";
    long long first = 1LL;
    long long i = 0LL;
    while (i < table.values_len) {
        long long v = table.values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? table.values_len + _mochi_idx : _mochi_idx;})];
        if (v != NULL) {
            if (!(first)) {
                result = str_concat(result, ", ");
            }
            result = str_concat(str_concat(str_concat(result, int_to_string(i)), ": "), int_to_string(v));
            first = 0LL;
        }
        i = i + 1LL;
    }
    result = str_concat(result, "}");
    return result;
}

int main(void) {
    {
        long long __start = _now();
        qp = create_hash_table(8LL);
        insert_data(&qp, 0LL);
        insert_data(&qp, 999LL);
        insert_data(&qp, 111LL);
        puts(keys_to_string(qp));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
