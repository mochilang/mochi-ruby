// Generated by Mochi 0.10.32 on 2025-08-24 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t inorder_len;
size_t floor_ceiling_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static size_t concat_len;
static long long* concat_long_long(long long *a, size_t a_len, const long long *b, size_t b_len) {
    long long *res = realloc(a, (a_len + b_len) * sizeof(long long));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(long long));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    long long key;
    long long left;
    long long right;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Node *tree = NULL;
size_t tree_len = 0;

long long * inorder(Node * nodes, size_t nodes_len, long long idx);
long long * floor_ceiling(Node * nodes, size_t nodes_len, long long idx, long long key);
int main(void);

long long * inorder(Node * nodes, size_t nodes_len, long long idx) {
    if (idx == -1LL) {
        {
            long long* __tmp5_data = malloc(0 * sizeof(long long));
            inorder_len = 0;
            return __tmp5_data;
        }
    }
    Node node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long *result = inorder(nodes, nodes_len, node.left);
    size_t result_len = inorder_len;
    result = list_append_long_long(result, &result_len, node.key);
    result = concat_long_long(result, result_len, inorder(nodes, nodes_len, node.right), inorder_len);
    result_len = concat_len;
    return inorder_len = result_len, result;
}

long long * floor_ceiling(Node * nodes, size_t nodes_len, long long idx, long long key) {
    long long floor_val = NULL;
    long long ceiling_val = NULL;
    long long current = idx;
    while (current != -1LL) {
        Node node = nodes[(int)({long long _mochi_idx = current; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        if (node.key == key) {
            floor_val = node.key;
            ceiling_val = node.key;
            break;
        }
        if (key < node.key) {
            ceiling_val = node.key;
            current = node.left;
        } else {
            floor_val = node.key;
            current = node.right;
        }
    }
    {
        long long* __tmp6_data = malloc(2 * sizeof(long long));
        __tmp6_data[0] = floor_val;
        __tmp6_data[1] = ceiling_val;
        floor_ceiling_len = 2;
        return __tmp6_data;
    }
}

int main(void) {
    {
        long long __start = _now();
        tree = (Node[]){(Node){.key = 10LL, .left = 1LL, .right = 2LL}, (Node){.key = 5LL, .left = 3LL, .right = 4LL}, (Node){.key = 20LL, .left = 5LL, .right = 6LL}, (Node){.key = 3LL, .left = -1LL, .right = -1LL}, (Node){.key = 7LL, .left = -1LL, .right = -1LL}, (Node){.key = 15LL, .left = -1LL, .right = -1LL}, (Node){.key = 25LL, .left = -1LL, .right = -1LL}};
        tree = malloc(7 * sizeof(Node));
        tree_len = 7;
        tree[0] = (Node){.key = 10LL, .left = 1LL, .right = 2LL};
        tree[1] = (Node){.key = 5LL, .left = 3LL, .right = 4LL};
        tree[2] = (Node){.key = 20LL, .left = 5LL, .right = 6LL};
        tree[3] = (Node){.key = 3LL, .left = -1LL, .right = -1LL};
        tree[4] = (Node){.key = 7LL, .left = -1LL, .right = -1LL};
        tree[5] = (Node){.key = 15LL, .left = -1LL, .right = -1LL};
        tree[6] = (Node){.key = 25LL, .left = -1LL, .right = -1LL};
        puts(({long long *__tmp7 = inorder(tree, tree_len, 0LL); char *__res = str_list_int(__tmp7, inorder_len); __res;}));
        puts(({long long *__tmp8 = floor_ceiling(tree, tree_len, 0LL, 8LL); char *__res = str_list_int(__tmp8, floor_ceiling_len); __res;}));
        puts(({long long *__tmp9 = floor_ceiling(tree, tree_len, 0LL, 14LL); char *__res = str_list_int(__tmp9, floor_ceiling_len); __res;}));
        puts(({long long *__tmp10 = floor_ceiling(tree, tree_len, 0LL, -1LL); char *__res = str_list_int(__tmp10, floor_ceiling_len); __res;}));
        puts(({long long *__tmp11 = floor_ceiling(tree, tree_len, 0LL, 30LL); char *__res = str_list_int(__tmp11, floor_ceiling_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
