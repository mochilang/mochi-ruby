// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t inorder_len;
size_t small_tree_len;
size_t medium_tree_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    long long data;
    long long left;
    long long right;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Node *small = NULL;
size_t small_len = 0;
Node *medium = NULL;
size_t medium_len = 0;

long long * inorder(Node * nodes, size_t nodes_len, long long index, long long * acc, size_t acc_len);
long long size(Node * nodes, size_t nodes_len, long long index);
long long depth(Node * nodes, size_t nodes_len, long long index);
long long is_full(Node * nodes, size_t nodes_len, long long index);
Node * small_tree();
Node * medium_tree();
int main(void);

long long * inorder(Node * nodes, size_t nodes_len, long long index, long long * acc, size_t acc_len) {
    if (index == (0LL - 1LL)) {
        return inorder_len = acc_len, acc;
    }
    Node node = nodes[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long *res = inorder(nodes, nodes_len, node.left, acc, acc_len);
    size_t res_len = inorder_len;
    res = list_append_long_long(res, &res_len, node.data);
    res = inorder(nodes, nodes_len, node.right, res, res_len);
    res_len = inorder_len;
    return inorder_len = res_len, res;
}

long long size(Node * nodes, size_t nodes_len, long long index) {
    if (index == (0LL - 1LL)) {
        return 0LL;
    }
    Node node = nodes[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    return (1LL + size(nodes, nodes_len, node.left)) + size(nodes, nodes_len, node.right);
}

long long depth(Node * nodes, size_t nodes_len, long long index) {
    if (index == (0LL - 1LL)) {
        return 0LL;
    }
    Node node = nodes[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long left_depth = depth(nodes, nodes_len, node.left);
    long long right_depth = depth(nodes, nodes_len, node.right);
    if (left_depth > right_depth) {
        return left_depth + 1LL;
    }
    return right_depth + 1LL;
}

long long is_full(Node * nodes, size_t nodes_len, long long index) {
    if (index == (0LL - 1LL)) {
        return 1LL;
    }
    Node node = nodes[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    if ((node.left == (0LL - 1LL)) && (node.right == (0LL - 1LL))) {
        return 1LL;
    }
    if ((node.left != (0LL - 1LL)) && (node.right != (0LL - 1LL))) {
        return is_full(nodes, nodes_len, node.left) && is_full(nodes, nodes_len, node.right);
    }
    return 0LL;
}

Node * small_tree() {
    Node *arr = NULL;
    size_t arr_len = 0;
    arr = list_append_Node(arr, &arr_len, (Node){.data = 2LL, .left = 1LL, .right = 2LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 1LL, .left = -1LL, .right = -1LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 3LL, .left = -1LL, .right = -1LL});
    return small_tree_len = arr_len, arr;
}

Node * medium_tree() {
    Node *arr = NULL;
    size_t arr_len = 0;
    arr = list_append_Node(arr, &arr_len, (Node){.data = 4LL, .left = 1LL, .right = 4LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 2LL, .left = 2LL, .right = 3LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 1LL, .left = -1LL, .right = -1LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 3LL, .left = -1LL, .right = -1LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 5LL, .left = -1LL, .right = 5LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 6LL, .left = -1LL, .right = 6LL});
    arr = list_append_Node(arr, &arr_len, (Node){.data = 7LL, .left = -1LL, .right = -1LL});
    return medium_tree_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        small = small_tree();
        small_len = small_tree_len;
        printf("%d\n", size(small, small_len, 0LL));
        long long* __tmp2 = inorder(small, small_len, 0LL, NULL, 0);
        puts(str_list_int(__tmp2, inorder_len));
        printf("%d\n", depth(small, small_len, 0LL));
        printf("%d\n", is_full(small, small_len, 0LL));
        medium = medium_tree();
        medium_len = medium_tree_len;
        printf("%d\n", size(medium, medium_len, 0LL));
        long long* __tmp3 = inorder(medium, medium_len, 0LL, NULL, 0);
        puts(str_list_int(__tmp3, inorder_len));
        printf("%d\n", depth(medium, medium_len, 0LL));
        printf("%d\n", is_full(medium, medium_len, 0LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
