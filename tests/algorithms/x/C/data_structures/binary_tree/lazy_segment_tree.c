// Generated by Mochi 0.10.32 on 2025-08-24 16:37 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t init_int_array_len;
size_t init_bool_array_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long NEG_INF = -1000000000LL;
long long *A = NULL;
size_t A_len = 0;
long long n = 15LL;
long long *segment_tree = NULL;
size_t segment_tree_len = 0;
long long *lazy = NULL;
size_t lazy_len = 0;
long long *flag = NULL;
size_t flag_len = 0;

long long * init_int_array(long long n);
long long * init_bool_array(long long n);
long long left(long long idx);
long long right(long long idx);
void build(long long * segment_tree, size_t segment_tree_len, long long idx, long long l, long long r, long long * a, size_t a_len);
void update(long long * segment_tree, size_t segment_tree_len, long long * lazy, size_t lazy_len, long long * flag, size_t flag_len, long long idx, long long l, long long r, long long a, long long b, long long val);
long long query(long long * segment_tree, size_t segment_tree_len, long long * lazy, size_t lazy_len, long long * flag, size_t flag_len, long long idx, long long l, long long r, long long a, long long b);
const char* segtree_to_string(long long * segment_tree, size_t segment_tree_len, long long * lazy, size_t lazy_len, long long * flag, size_t flag_len, long long n);
int main(void);

long long * init_int_array(long long n) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < ((4LL * n) + 5LL)) {
        arr = list_append_long_long(arr, &arr_len, 0LL);
        i = i + 1LL;
    }
    return init_int_array_len = arr_len, arr;
}

long long * init_bool_array(long long n) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < ((4LL * n) + 5LL)) {
        arr = list_append_long_long(arr, &arr_len, 0LL);
        i = i + 1LL;
    }
    return init_bool_array_len = arr_len, arr;
}

long long left(long long idx) {
    return idx * 2LL;
}

long long right(long long idx) {
    return (idx * 2LL) + 1LL;
}

void build(long long * segment_tree, size_t segment_tree_len, long long idx, long long l, long long r, long long * a, size_t a_len) {
    if (l == r) {
        segment_tree[(int)(idx)] = a[(int)({long long _mochi_idx = l - 1LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
    } else {
        long long mid = (l + r) / 2LL;
        build(segment_tree, segment_tree_len, left(idx), l, mid, a, a_len);
        build(segment_tree, segment_tree_len, right(idx), mid + 1LL, r, a, a_len);
        long long lv = segment_tree[(int)({long long _mochi_idx = left(idx); _mochi_idx < 0 ? segment_tree_len + _mochi_idx : _mochi_idx;})];
        long long rv = segment_tree[(int)({long long _mochi_idx = right(idx); _mochi_idx < 0 ? segment_tree_len + _mochi_idx : _mochi_idx;})];
        if (lv > rv) {
            segment_tree[(int)(idx)] = lv;
        } else {
            segment_tree[(int)(idx)] = rv;
        }
    }
}

void update(long long * segment_tree, size_t segment_tree_len, long long * lazy, size_t lazy_len, long long * flag, size_t flag_len, long long idx, long long l, long long r, long long a, long long b, long long val) {
    if (flag[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? flag_len + _mochi_idx : _mochi_idx;})]) {
        segment_tree[(int)(idx)] = lazy[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? lazy_len + _mochi_idx : _mochi_idx;})];
        flag[(int)(idx)] = 0LL;
        if (l != r) {
            lazy[(int)(left(idx))] = lazy[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? lazy_len + _mochi_idx : _mochi_idx;})];
            lazy[(int)(right(idx))] = lazy[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? lazy_len + _mochi_idx : _mochi_idx;})];
            flag[(int)(left(idx))] = 1LL;
            flag[(int)(right(idx))] = 1LL;
        }
    }
    if ((r < a) || (l > b)) {
        return 0;
    }
    if ((l >= a) && (r <= b)) {
        segment_tree[(int)(idx)] = val;
        if (l != r) {
            lazy[(int)(left(idx))] = val;
            lazy[(int)(right(idx))] = val;
            flag[(int)(left(idx))] = 1LL;
            flag[(int)(right(idx))] = 1LL;
        }
        return 0;
    }
    long long mid = (l + r) / 2LL;
    update(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, left(idx), l, mid, a, b, val);
    update(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, right(idx), mid + 1LL, r, a, b, val);
    long long lv = segment_tree[(int)({long long _mochi_idx = left(idx); _mochi_idx < 0 ? segment_tree_len + _mochi_idx : _mochi_idx;})];
    long long rv = segment_tree[(int)({long long _mochi_idx = right(idx); _mochi_idx < 0 ? segment_tree_len + _mochi_idx : _mochi_idx;})];
    if (lv > rv) {
        segment_tree[(int)(idx)] = lv;
    } else {
        segment_tree[(int)(idx)] = rv;
    }
}

long long query(long long * segment_tree, size_t segment_tree_len, long long * lazy, size_t lazy_len, long long * flag, size_t flag_len, long long idx, long long l, long long r, long long a, long long b) {
    if (flag[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? flag_len + _mochi_idx : _mochi_idx;})]) {
        segment_tree[(int)(idx)] = lazy[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? lazy_len + _mochi_idx : _mochi_idx;})];
        flag[(int)(idx)] = 0LL;
        if (l != r) {
            lazy[(int)(left(idx))] = lazy[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? lazy_len + _mochi_idx : _mochi_idx;})];
            lazy[(int)(right(idx))] = lazy[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? lazy_len + _mochi_idx : _mochi_idx;})];
            flag[(int)(left(idx))] = 1LL;
            flag[(int)(right(idx))] = 1LL;
        }
    }
    if ((r < a) || (l > b)) {
        return NEG_INF;
    }
    if ((l >= a) && (r <= b)) {
        return segment_tree[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? segment_tree_len + _mochi_idx : _mochi_idx;})];
    }
    long long mid = (l + r) / 2LL;
    long long q1 = query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, left(idx), l, mid, a, b);
    long long q2 = query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, right(idx), mid + 1LL, r, a, b);
    if (q1 > q2) {
        return q1;
    } else {
        return q2;
    }
}

const char* segtree_to_string(long long * segment_tree, size_t segment_tree_len, long long * lazy, size_t lazy_len, long long * flag, size_t flag_len, long long n) {
    const char* res = "[";
    long long i = 1LL;
    while (i <= n) {
        long long v = query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, i, i);
        res = str_concat(res, str_int(v));
        if (i < n) {
            res = str_concat(res, ", ");
        }
        i = i + 1LL;
    }
    res = str_concat(res, "]");
    return res;
}

int main(void) {
    {
        long long __start = _now();
        A = (long long[]){1LL, 2LL, -4LL, 7LL, 3LL, -5LL, 6LL, 11LL, -20LL, 9LL, 14LL, 15LL, 5LL, 2LL, -8LL};
        A = malloc(15 * sizeof(long long));
        A_len = 15;
        A[0] = 1LL;
        A[1] = 2LL;
        A[2] = -4LL;
        A[3] = 7LL;
        A[4] = 3LL;
        A[5] = -5LL;
        A[6] = 6LL;
        A[7] = 11LL;
        A[8] = -20LL;
        A[9] = 9LL;
        A[10] = 14LL;
        A[11] = 15LL;
        A[12] = 5LL;
        A[13] = 2LL;
        A[14] = -8LL;
        segment_tree = init_int_array(n);
        segment_tree_len = init_int_array_len;
        lazy = init_int_array(n);
        lazy_len = init_int_array_len;
        flag = init_bool_array(n);
        flag_len = init_bool_array_len;
        build(segment_tree, segment_tree_len, 1LL, 1LL, n, A, A_len);
        printf("%d\n", query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, 4LL, 6LL));
        printf("%d\n", query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, 7LL, 11LL));
        printf("%d\n", query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, 7LL, 12LL));
        update(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, 1LL, 3LL, 111LL);
        printf("%d\n", query(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, 1LL, 15LL));
        update(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, 1LL, 1LL, n, 7LL, 8LL, 235LL);
        puts(segtree_to_string(segment_tree, segment_tree_len, lazy, lazy_len, flag, flag_len, n));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
