// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t fenwick_get_array_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct FenwickTree FenwickTree;

struct FenwickTree {
    long long size;
    long long *tree;
    size_t tree_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

FenwickTree f_base;
FenwickTree f;
FenwickTree f2;
FenwickTree f3;

FenwickTree fenwick_from_list(long long * arr, size_t arr_len);
FenwickTree fenwick_empty(long long size);
long long * fenwick_get_array(FenwickTree f);
long long bit_and(long long a, long long b);
long long low_bit(long long x);
long long fenwick_next(long long index);
long long fenwick_prev(long long index);
FenwickTree fenwick_add(FenwickTree f, long long index, long long value);
FenwickTree fenwick_update(FenwickTree f, long long index, long long value);
long long fenwick_prefix(FenwickTree f, long long right);
long long fenwick_query(FenwickTree f, long long left, long long right);
long long fenwick_get(FenwickTree f, long long index);
long long fenwick_rank_query(FenwickTree f, long long value);
int main(void);

FenwickTree fenwick_from_list(long long * arr, size_t arr_len) {
    long long size = arr_len;
    long long *tree = NULL;
    size_t tree_len = 0;
    long long i = 0LL;
    while (i < size) {
        tree = list_append_long_long(tree, &tree_len, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    i = 1LL;
    while (i < size) {
        long long j = fenwick_next(i);
        if (j < size) {
            tree[(int)(j)] = tree[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? tree_len + _mochi_idx : _mochi_idx;})] + tree[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? tree_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return (FenwickTree){.size = size, .tree = tree, .tree_len = tree_len};
}

FenwickTree fenwick_empty(long long size) {
    long long *tree = NULL;
    size_t tree_len = 0;
    long long i = 0LL;
    while (i < size) {
        tree = list_append_long_long(tree, &tree_len, 0LL);
        i = i + 1LL;
    }
    return (FenwickTree){.size = size, .tree = tree, .tree_len = tree_len};
}

long long * fenwick_get_array(FenwickTree f) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < f.size) {
        arr = list_append_long_long(arr, &arr_len, f.tree[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? f.tree_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    i = f.size - 1LL;
    while (i > 0LL) {
        long long j = fenwick_next(i);
        if (j < f.size) {
            arr[(int)(j)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] - arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        }
        i = i - 1LL;
    }
    return fenwick_get_array_len = arr_len, arr;
}

long long bit_and(long long a, long long b) {
    long long ua = a;
    long long ub = b;
    long long res = 0LL;
    long long bit = 1LL;
    while ((ua != 0LL) || (ub != 0LL)) {
        if (((ua % 2LL) == 1LL) && ((ub % 2LL) == 1LL)) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long low_bit(long long x) {
    if (x == 0LL) {
        return 0LL;
    }
    return x - bit_and(x, x - 1LL);
}

long long fenwick_next(long long index) {
    return index + low_bit(index);
}

long long fenwick_prev(long long index) {
    return index - low_bit(index);
}

FenwickTree fenwick_add(FenwickTree f, long long index, long long value) {
    long long *tree = f.tree;
    size_t tree_len = f.tree_len;
    if (index == 0LL) {
        tree[(int)(0LL)] = tree[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? tree_len + _mochi_idx : _mochi_idx;})] + value;
        return (FenwickTree){.size = f.size, .tree = tree, .tree_len = tree_len};
    }
    long long i = index;
    while (i < f.size) {
        tree[(int)(i)] = tree[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? tree_len + _mochi_idx : _mochi_idx;})] + value;
        i = fenwick_next(i);
    }
    return (FenwickTree){.size = f.size, .tree = tree, .tree_len = tree_len};
}

FenwickTree fenwick_update(FenwickTree f, long long index, long long value) {
    long long current = fenwick_get(f, index);
    return fenwick_add(f, index, value - current);
}

long long fenwick_prefix(FenwickTree f, long long right) {
    if (right == 0LL) {
        return 0LL;
    }
    long long result = f.tree[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? f.tree_len + _mochi_idx : _mochi_idx;})];
    long long r = right - 1LL;
    while (r > 0LL) {
        result = result + f.tree[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? f.tree_len + _mochi_idx : _mochi_idx;})];
        r = fenwick_prev(r);
    }
    return result;
}

long long fenwick_query(FenwickTree f, long long left, long long right) {
    return fenwick_prefix(f, right) - fenwick_prefix(f, left);
}

long long fenwick_get(FenwickTree f, long long index) {
    return fenwick_query(f, index, index + 1LL);
}

long long fenwick_rank_query(FenwickTree f, long long value) {
    long long v = value - f.tree[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? f.tree_len + _mochi_idx : _mochi_idx;})];
    if (v < 0LL) {
        return -1LL;
    }
    long long j = 1LL;
    while ((j * 2LL) < f.size) {
        j = j * 2LL;
    }
    long long i = 0LL;
    long long jj = j;
    while (jj > 0LL) {
        if (((i + jj) < f.size) && (f.tree[(int)({long long _mochi_idx = i + jj; _mochi_idx < 0 ? f.tree_len + _mochi_idx : _mochi_idx;})] <= v)) {
            v = v - f.tree[(int)({long long _mochi_idx = i + jj; _mochi_idx < 0 ? f.tree_len + _mochi_idx : _mochi_idx;})];
            i = i + jj;
        }
        jj = jj / 2LL;
    }
    return i;
}

int main(void) {
    {
        long long __start = _now();
        f_base = fenwick_from_list((long long[]){1LL, 2LL, 3LL, 4LL, 5LL}, 5);
        long long* __tmp2 = fenwick_get_array(f_base);
        puts(str_list_int(__tmp2, fenwick_get_array_len));
        f = fenwick_from_list((long long[]){1LL, 2LL, 3LL, 4LL, 5LL}, 5);
        f = fenwick_add(f, 0LL, 1LL);
        f = fenwick_add(f, 1LL, 2LL);
        f = fenwick_add(f, 2LL, 3LL);
        f = fenwick_add(f, 3LL, 4LL);
        f = fenwick_add(f, 4LL, 5LL);
        long long* __tmp3 = fenwick_get_array(f);
        puts(str_list_int(__tmp3, fenwick_get_array_len));
        f2 = fenwick_from_list((long long[]){1LL, 2LL, 3LL, 4LL, 5LL}, 5);
        printf("%d\n", fenwick_prefix(f2, 3LL));
        printf("%d\n", fenwick_query(f2, 1LL, 4LL));
        f3 = fenwick_from_list((long long[]){1LL, 2LL, 0LL, 3LL, 0LL, 5LL}, 6);
        printf("%d\n", fenwick_rank_query(f3, 0LL));
        printf("%d\n", fenwick_rank_query(f3, 2LL));
        printf("%d\n", fenwick_rank_query(f3, 1LL));
        printf("%d\n", fenwick_rank_query(f3, 3LL));
        printf("%d\n", fenwick_rank_query(f3, 5LL));
        printf("%d\n", fenwick_rank_query(f3, 6LL));
        printf("%d\n", fenwick_rank_query(f3, 11LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
