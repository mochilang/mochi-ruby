// Generated by Mochi 0.10.32 on 2025-08-08 19:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

const char* input_str = "";
long long node_count = 0;

long long binomial_coefficient(long long n, long long k);
long long catalan_number(long long node_count);
long long factorial(long long n);
long long binary_tree_count(long long node_count);
int main(void);

long long binomial_coefficient(long long n, long long k) {
    long long result = 1LL;
    long long kk = k;
    if (k > (n - k)) {
        kk = n - k;
    }
    for (int i = 0LL; i < kk; i++) {
        result = result * (n - i);
        result = result / (i + 1LL);
    }
    return result;
}

long long catalan_number(long long node_count) {
    return binomial_coefficient(2LL * node_count, node_count) / (node_count + 1LL);
}

long long factorial(long long n) {
    if (n < 0LL) {
        puts("factorial() not defined for negative values");
        return 0LL;
    }
    long long result = 1LL;
    for (int i = 1LL; i < n + 1LL; i++) {
        result = result * i;
    }
    return result;
}

long long binary_tree_count(long long node_count) {
    return catalan_number(node_count) * factorial(node_count);
}

int main(void) {
    {
        long long __start = _now();
        puts("Enter the number of nodes:");
        input_str = _input();
        node_count = _atoi(input_str);
        if (node_count <= 0LL) {
            puts("We need some nodes to work with.");
        } else {
            long long bst = catalan_number(node_count);
            long long bt = binary_tree_count(node_count);
            printf("%s %d %s %d %s %d %s\n", "Given", node_count, "nodes, there are", bt, "binary trees and", bst, "binary search trees.");
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
