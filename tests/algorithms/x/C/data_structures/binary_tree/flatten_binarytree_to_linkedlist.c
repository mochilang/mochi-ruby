// Generated by Mochi 0.10.32 on 2025-08-24 15:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t flatten_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *node_data = NULL;
size_t node_data_len = 0;
long long *left_child = NULL;
size_t left_child_len = 0;
long long *right_child = NULL;
size_t right_child_len = 0;
long long root = 0;
long long *vals = NULL;
size_t vals_len = 0;

long long new_node(long long value);
long long build_tree();
long long * flatten(long long root);
const char* display(long long * values, size_t values_len);
int main(void);

long long new_node(long long value) {
    node_data = list_append_long_long(node_data, &node_data_len, value);
    left_child = list_append_long_long(left_child, &left_child_len, 0LL);
    right_child = list_append_long_long(right_child, &right_child_len, 0LL);
    return node_data_len - 1LL;
}

long long build_tree() {
    long long root = new_node(1LL);
    long long n2 = new_node(2LL);
    long long n5 = new_node(5LL);
    long long n3 = new_node(3LL);
    long long n4 = new_node(4LL);
    long long n6 = new_node(6LL);
    left_child[(int)(root)] = n2;
    right_child[(int)(root)] = n5;
    left_child[(int)(n2)] = n3;
    right_child[(int)(n2)] = n4;
    right_child[(int)(n5)] = n6;
    return root;
}

long long * flatten(long long root) {
    if (root == 0LL) {
        {
            long long* __tmp0_data = malloc(0 * sizeof(long long));
            flatten_len = 0;
            return __tmp0_data;
        }
    }
    long long *res = NULL;
    size_t res_len = 0;
    res = list_append_long_long(res, &res_len, node_data[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? node_data_len + _mochi_idx : _mochi_idx;})]);
    long long *left_vals = flatten(left_child[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? left_child_len + _mochi_idx : _mochi_idx;})]);
    size_t left_vals_len = flatten_len;
    long long *right_vals = flatten(right_child[(int)({long long _mochi_idx = root; _mochi_idx < 0 ? right_child_len + _mochi_idx : _mochi_idx;})]);
    size_t right_vals_len = flatten_len;
    long long i = 0LL;
    while (i < left_vals_len) {
        res = list_append_long_long(res, &res_len, left_vals[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_vals_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < right_vals_len) {
        res = list_append_long_long(res, &res_len, right_vals[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? right_vals_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return flatten_len = res_len, res;
}

const char* display(long long * values, size_t values_len) {
    const char* s = "";
    long long i = 0LL;
    while (i < values_len) {
        if (i == 0LL) {
            s = str_int(values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]);
        } else {
            s = str_concat(str_concat(s, " "), str_int(values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]));
        }
        i = i + 1LL;
    }
    puts(s);
}

int main(void) {
    {
        long long __start = _now();
        node_data = (long long[]){0LL};
        node_data = malloc(1 * sizeof(long long));
        node_data_len = 1;
        node_data[0] = 0LL;
        left_child = (long long[]){0LL};
        left_child = malloc(1 * sizeof(long long));
        left_child_len = 1;
        left_child[0] = 0LL;
        right_child = (long long[]){0LL};
        right_child = malloc(1 * sizeof(long long));
        right_child_len = 1;
        right_child[0] = 0LL;
        puts("Flattened Linked List:");
        root = build_tree();
        vals = flatten(root);
        vals_len = flatten_len;
        display(vals, vals_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
