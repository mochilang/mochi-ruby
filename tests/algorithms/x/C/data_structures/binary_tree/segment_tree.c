// Generated by Mochi 0.10.32 on 2025-08-08 19:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *A = NULL;
size_t A_len = 0;
long long N = 0LL;
long long *st = NULL;
size_t st_len = 0;
long long NEG_INF = -1000000000LL;

long long left_child(long long idx);
long long right_child(long long idx);
const char* build(long long idx, long long left, long long right);
long long update_recursive(long long idx, long long left, long long right, long long a, long long b, long long val);
long long update(long long a, long long b, long long val);
long long query_recursive(long long idx, long long left, long long right, long long a, long long b);
long long query(long long a, long long b);
const char* show_data();
void user_main();
int main(void);

long long left_child(long long idx) {
    return idx * 2LL;
}

long long right_child(long long idx) {
    return (idx * 2LL) + 1LL;
}

const char* build(long long idx, long long left, long long right) {
    if (left == right) {
        st[(int)(idx)] = A[(int)(left)];
    } else {
        long long mid = (left + right) / 2LL;
        build(left_child(idx), left, (long long)mid);
        build(right_child(idx), mid + 1LL, right);
        long long left_val = st[(int)(left_child(idx))];
        long long right_val = st[(int)(right_child(idx))];
        st[(int)(idx)] = (left_val > right_val ? left_val : right_val);
    }
}

long long update_recursive(long long idx, long long left, long long right, long long a, long long b, long long val) {
    if ((right < a) || (left > b)) {
        return 1LL;
    }
    if (left == right) {
        st[(int)(idx)] = val;
        return 1LL;
    }
    long long mid = (left + right) / 2LL;
    update_recursive(left_child(idx), left, (long long)mid, a, b, val);
    update_recursive(right_child(idx), mid + 1LL, right, a, b, val);
    long long left_val = st[(int)(left_child(idx))];
    long long right_val = st[(int)(right_child(idx))];
    st[(int)(idx)] = (left_val > right_val ? left_val : right_val);
    return 1LL;
}

long long update(long long a, long long b, long long val) {
    return update_recursive(1LL, 0LL, N - 1LL, a - 1LL, b - 1LL, val);
}

long long query_recursive(long long idx, long long left, long long right, long long a, long long b) {
    if ((right < a) || (left > b)) {
        return NEG_INF;
    }
    if ((left >= a) && (right <= b)) {
        return st[(int)(idx)];
    }
    long long mid = (left + right) / 2LL;
    long long q1 = query_recursive(left_child(idx), left, mid, a, b);
    long long q2 = query_recursive(right_child(idx), mid + 1LL, right, a, b);
    return (q1 > q2 ? q1 : q2);
}

long long query(long long a, long long b) {
    return query_recursive(1LL, 0LL, N - 1LL, a - 1LL, b - 1LL);
}

const char* show_data() {
    long long i = 0LL;
    long long *show_list = NULL;
    size_t show_list_len = 0;
    while (i < N) {
        show_list = list_append_long_long(show_list, &show_list_len, query(i + 1LL, i + 1LL));
        i = i + 1LL;
    }
    puts(str_list_int(show_list, show_list_len));
}

void user_main() {
    A = ({long long *tmp = malloc(15 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = -4LL; tmp[3] = 7LL; tmp[4] = 3LL; tmp[5] = -5LL; tmp[6] = 6LL; tmp[7] = 11LL; tmp[8] = -20LL; tmp[9] = 9LL; tmp[10] = 14LL; tmp[11] = 15LL; tmp[12] = 5LL; tmp[13] = 2LL; tmp[14] = -8LL; tmp;});
    A_len = 15;
    N = A_len;
    long long i = 0LL;
    while (i < (4LL * N)) {
        st = list_append_long_long(st, &st_len, 0LL);
        i = i + 1LL;
    }
    if (N > 0LL) {
        build(1LL, 0LL, N - 1LL);
    }
    printf("%d\n", query(4LL, 6LL));
    printf("%d\n", query(7LL, 11LL));
    printf("%d\n", query(7LL, 12LL));
    update(1LL, 3LL, 111LL);
    printf("%d\n", query(1LL, 15LL));
    update(7LL, 8LL, 235LL);
    show_data();
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
