// Generated by Mochi 0.10.32 on 2025-08-08 19:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t build_a_tree_len;
size_t build_a_sum_tree_len;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    long long value;
    long long left;
    long long right;
};

long long tree_sum(Node * nodes, size_t nodes_len, long long idx);
long long is_sum_node(Node * nodes, size_t nodes_len, long long idx);
Node * build_a_tree();
Node * build_a_sum_tree();
int main(void);

long long tree_sum(Node * nodes, size_t nodes_len, long long idx) {
    if (idx == -1LL) {
        return 0LL;
    }
    Node node = nodes[(int)(idx)];
    return (node.value + tree_sum(nodes, nodes_len, node.left)) + tree_sum(nodes, nodes_len, node.right);
}

long long is_sum_node(Node * nodes, size_t nodes_len, long long idx) {
    Node node = nodes[(int)(idx)];
    if ((node.left == -1LL) && (node.right == -1LL)) {
        return 1LL;
    }
    long long left_sum = tree_sum(nodes, nodes_len, node.left);
    long long right_sum = tree_sum(nodes, nodes_len, node.right);
    if (node.value != (left_sum + right_sum)) {
        return 0LL;
    }
    long long left_ok = 1LL;
    if (node.left != -1LL) {
        left_ok = is_sum_node(nodes, nodes_len, node.left);
    }
    long long right_ok = 1LL;
    if (node.right != -1LL) {
        right_ok = is_sum_node(nodes, nodes_len, node.right);
    }
    return left_ok && right_ok;
}

Node * build_a_tree() {
    {
        static Node __tmp0_arr[8];
        __tmp0_arr[0] = (Node){.value = 11LL, .left = 1LL, .right = 2LL};
        __tmp0_arr[1] = (Node){.value = 2LL, .left = 3LL, .right = 4LL};
        __tmp0_arr[2] = (Node){.value = 29LL, .left = 5LL, .right = 6LL};
        __tmp0_arr[3] = (Node){.value = 1LL, .left = -1LL, .right = -1LL};
        __tmp0_arr[4] = (Node){.value = 7LL, .left = -1LL, .right = -1LL};
        __tmp0_arr[5] = (Node){.value = 15LL, .left = -1LL, .right = -1LL};
        __tmp0_arr[6] = (Node){.value = 40LL, .left = 7LL, .right = -1LL};
        __tmp0_arr[7] = (Node){.value = 35LL, .left = -1LL, .right = -1LL};
        build_a_tree_len = 8;
        return __tmp0_arr;
    }
}

Node * build_a_sum_tree() {
    {
        static Node __tmp1_arr[6];
        __tmp1_arr[0] = (Node){.value = 26LL, .left = 1LL, .right = 2LL};
        __tmp1_arr[1] = (Node){.value = 10LL, .left = 3LL, .right = 4LL};
        __tmp1_arr[2] = (Node){.value = 3LL, .left = -1LL, .right = 5LL};
        __tmp1_arr[3] = (Node){.value = 4LL, .left = -1LL, .right = -1LL};
        __tmp1_arr[4] = (Node){.value = 6LL, .left = -1LL, .right = -1LL};
        __tmp1_arr[5] = (Node){.value = 3LL, .left = -1LL, .right = -1LL};
        build_a_sum_tree_len = 6;
        return __tmp1_arr;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
