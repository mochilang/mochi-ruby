// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_node_len;
size_t insert_len;
size_t inorder_len;
size_t delete_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* * create_node(long long value);
const char* * insert(const char* * node, size_t node_len, long long value);
long long search(const char* * node, size_t node_len, long long value);
long long * inorder(const char* * node, size_t node_len, long long * acc, size_t acc_len);
long long find_min(const char* * node, size_t node_len);
long long find_max(const char* * node, size_t node_len);
const char* * delete(const char* * node, size_t node_len, long long value);
void user_main();
int main(void);

const char* * create_node(long long value) {
    {
        const char** __tmp2_data = malloc(3 * sizeof(const char*));
        __tmp2_data[0] = value;
        __tmp2_data[1] = NULL;
        __tmp2_data[2] = NULL;
        create_node_len = 3;
        return __tmp2_data;
    }
}

const char* * insert(const char* * node, size_t node_len, long long value) {
    if (node == NULL) {
        {
            const char** __ret3 = create_node(value);
            insert_len = create_node_len;
            return __ret3;
        }
    }
    if (value < node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]) {
        node[(int)(1LL)] = insert(node[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(1LL)], value);
    } else {
        if (value > node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]) {
            node[(int)(2LL)] = insert(node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(2LL)], value);
        }
    }
    return insert_len = node_len, node;
}

long long search(const char* * node, size_t node_len, long long value) {
    if (node == NULL) {
        return 0LL;
    }
    if (value == node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]) {
        return 1LL;
    }
    if (value < node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]) {
        return search(node[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(1LL)], value);
    }
    return search(node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(2LL)], value);
}

long long * inorder(const char* * node, size_t node_len, long long * acc, size_t acc_len) {
    if (node == NULL) {
        return inorder_len = acc_len, acc;
    }
    long long *left_acc = inorder(node[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(1LL)], acc, acc_len);
    size_t left_acc_len = inorder_len;
    long long *with_node = list_append_int_new(left_acc, left_acc_len, node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]);
    size_t with_node_len = left_acc_len + 1;
    {
        long long* __ret4 = inorder(node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(2LL)], with_node, with_node_len);
        inorder_len = inorder_len;
        return __ret4;
    }
}

long long find_min(const char* * node, size_t node_len) {
    const char* *current = node;
    size_t current_len = node_len;
    while (current[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})] != NULL) {
        current = current[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})];
    }
    return current[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})];
}

long long find_max(const char* * node, size_t node_len) {
    const char* *current = node;
    size_t current_len = node_len;
    while (current[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})] != NULL) {
        current = current[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})];
    }
    return current[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? current_len + _mochi_idx : _mochi_idx;})];
}

const char* * delete(const char* * node, size_t node_len, long long value) {
    if (node == NULL) {
        delete_len = 0;
        return NULL;
    }
    if (value < node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]) {
        node[(int)(1LL)] = delete(node[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(1LL)], value);
    } else {
        if (value > node[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})]) {
            node[(int)(2LL)] = delete(node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(2LL)], value);
        } else {
            if (node[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})] == NULL) {
                return node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})];
            }
            if (node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})] == NULL) {
                return node[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})];
            }
            long long min_val = find_min(node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(2LL)]);
            node[(int)(0LL)] = min_val;
            node[(int)(2LL)] = delete(node[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? node_len + _mochi_idx : _mochi_idx;})], node_lens[(int)(2LL)], min_val);
        }
    }
    return delete_len = node_len, node;
}

void user_main() {
    const char* *root = NULL;
    long long *nums = NULL;
    size_t nums_len = 0;
    nums = list_append_long_long(nums, &nums_len, 8LL);
    nums = list_append_long_long(nums, &nums_len, 3LL);
    nums = list_append_long_long(nums, &nums_len, 6LL);
    nums = list_append_long_long(nums, &nums_len, 1LL);
    nums = list_append_long_long(nums, &nums_len, 10LL);
    nums = list_append_long_long(nums, &nums_len, 14LL);
    nums = list_append_long_long(nums, &nums_len, 13LL);
    nums = list_append_long_long(nums, &nums_len, 4LL);
    nums = list_append_long_long(nums, &nums_len, 7LL);
    {
        long long v_arr[] = {8LL, 3LL, 6LL, 1LL, 10LL, 14LL, 13LL, 4LL, 7LL};
        size_t v_len = sizeof(v_arr) / sizeof(v_arr[0]);
        for (size_t __i = 0; __i < v_len; __i++) {
            long long v = v_arr[__i];
            root = insert(root, root_len, v);
            root_len = insert_len;
        }
    }
    puts(({long long *__tmp5 = inorder(root, root_len, NULL, 0); char *__res = str_list_int(__tmp5, inorder_len); __res;}));
    printf("%d\n", search(root, root_len, 6LL));
    printf("%d\n", search(root, root_len, 20LL));
    printf("%d\n", find_min(root, root_len));
    printf("%d\n", find_max(root, root_len));
    root = delete(root, root_len, 6LL);
    root_len = delete_len;
    puts(({long long *__tmp6 = inorder(root, root_len, NULL, 0); char *__res = str_list_int(__tmp6, inorder_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
