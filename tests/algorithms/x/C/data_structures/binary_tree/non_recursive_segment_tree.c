// Generated by Mochi 0.10.32 on 2025-08-24 21:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t build_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *arr1 = NULL;
size_t arr1_len = 0;
long long *st1 = NULL;
size_t st1_len = 0;
long long *arr2 = NULL;
size_t arr2_len = 0;
long long *st2 = NULL;
size_t st2_len = 0;
long long *arr3 = NULL;
size_t arr3_len = 0;
long long *st3 = NULL;
size_t st3_len = 0;
long long *arr4 = NULL;
size_t arr4_len = 0;
long long n4 = 5LL;
long long *st4 = NULL;
size_t st4_len = 0;

long long * build(long long * arr, size_t arr_len, long long(*combine)(long long, long long));
void update(long long * st, size_t st_len, long long n, long long(*combine)(long long, long long), long long p, long long v);
long long query(long long * st, size_t st_len, long long n, long long(*combine)(long long, long long), long long left, long long right);
long long add(long long a, long long b);
long long min_int(long long a, long long b);
long long max_int(long long a, long long b);
int main(void);

long long * build(long long * arr, size_t arr_len, long long(*combine)(long long, long long)) {
    long long n = arr_len;
    long long *st = NULL;
    size_t st_len = 0;
    long long i = 0LL;
    while (i < (2LL * n)) {
        st = list_append_long_long(st, &st_len, 0LL);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        st[(int)(n + i)] = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    i = n - 1LL;
    while (i > 0LL) {
        st[(int)(i)] = combine(st[(int)({long long _mochi_idx = i * 2LL; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})], st[(int)({long long _mochi_idx = (i * 2LL) + 1LL; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})]);
        i = i - 1LL;
    }
    return build_len = st_len, st;
}

void update(long long * st, size_t st_len, long long n, long long(*combine)(long long, long long), long long p, long long v) {
    long long idx = p + n;
    st[(int)(idx)] = v;
    while (idx > 1LL) {
        idx = idx / 2LL;
        st[(int)(idx)] = combine(st[(int)({long long _mochi_idx = idx * 2LL; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})], st[(int)({long long _mochi_idx = (idx * 2LL) + 1LL; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})]);
    }
}

long long query(long long * st, size_t st_len, long long n, long long(*combine)(long long, long long), long long left, long long right) {
    long long l = left + n;
    long long r = right + n;
    long long res = 0LL;
    long long has = 0LL;
    while (l <= r) {
        if ((l % 2LL) == 1LL) {
            if (!(has)) {
                res = st[(int)({long long _mochi_idx = l; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})];
                has = 1LL;
            } else {
                res = combine(res, st[(int)({long long _mochi_idx = l; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})]);
            }
            l = l + 1LL;
        }
        if ((r % 2LL) == 0LL) {
            if (!(has)) {
                res = st[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})];
                has = 1LL;
            } else {
                res = combine(res, st[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? st_len + _mochi_idx : _mochi_idx;})]);
            }
            r = r - 1LL;
        }
        l = l / 2LL;
        r = r / 2LL;
    }
    return res;
}

long long add(long long a, long long b) {
    return a + b;
}

long long min_int(long long a, long long b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}

long long max_int(long long a, long long b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

int main(void) {
    {
        long long __start = _now();
        arr1 = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;});
        arr1 = malloc(3 * sizeof(long long));
        arr1_len = 3;
        arr1[0] = 1LL;
        arr1[1] = 2LL;
        arr1[2] = 3LL;
        st1 = build(arr1, arr1_len, add);
        st1_len = build_len;
        puts(str_int(query(st1, st1_len, 3LL, add, 0LL, 2LL)));
        arr2 = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 1LL; tmp[2] = 2LL; tmp;});
        arr2 = malloc(3 * sizeof(long long));
        arr2_len = 3;
        arr2[0] = 3LL;
        arr2[1] = 1LL;
        arr2[2] = 2LL;
        st2 = build(arr2, arr2_len, min_int);
        st2_len = build_len;
        puts(str_int(query(st2, st2_len, 3LL, min_int, 0LL, 2LL)));
        arr3 = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = 1LL; tmp;});
        arr3 = malloc(3 * sizeof(long long));
        arr3_len = 3;
        arr3[0] = 2LL;
        arr3[1] = 3LL;
        arr3[2] = 1LL;
        st3 = build(arr3, arr3_len, max_int);
        st3_len = build_len;
        puts(str_int(query(st3, st3_len, 3LL, max_int, 0LL, 2LL)));
        arr4 = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 5LL; tmp[2] = 7LL; tmp[3] = -1LL; tmp[4] = 6LL; tmp;});
        arr4 = malloc(5 * sizeof(long long));
        arr4_len = 5;
        arr4[0] = 1LL;
        arr4[1] = 5LL;
        arr4[2] = 7LL;
        arr4[3] = -1LL;
        arr4[4] = 6LL;
        st4 = build(arr4, arr4_len, add);
        st4_len = build_len;
        update(st4, st4_len, n4, add, 1LL, -1LL);
        update(st4, st4_len, n4, add, 2LL, 3LL);
        puts(str_int(query(st4, st4_len, n4, add, 1LL, 2LL)));
        puts(str_int(query(st4, st4_len, n4, add, 1LL, 1LL)));
        update(st4, st4_len, n4, add, 4LL, 1LL);
        puts(str_int(query(st4, st4_len, n4, add, 3LL, 4LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
