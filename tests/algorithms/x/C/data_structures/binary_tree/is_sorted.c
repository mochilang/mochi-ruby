// Generated by Mochi 0.10.32 on 2025-08-24 16:37 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t inorder_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t concat_len;
static double* concat_double(double *a, size_t a_len, const double *b, size_t b_len) {
    double *res = realloc(a, (a_len + b_len) * sizeof(double));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(double));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Tree Tree;

struct Tree {
    double *data;
    size_t data_len;
    long long *left;
    size_t left_len;
    long long *right;
    size_t right_len;
};

long long NONE = -1LL;
Tree tree1;
Tree tree2;
Tree tree3;

double * inorder(Tree tree, long long index);
long long is_sorted(Tree tree, long long index);
int main(void);

double * inorder(Tree tree, long long index) {
    double *res = NULL;
    size_t res_len = 0;
    if (index == NONE) {
        return inorder_len = res_len, res;
    }
    long long left_idx = tree.left[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.left_len + _mochi_idx : _mochi_idx;})];
    if (left_idx != NONE) {
        res = concat_double(res, res_len, inorder(tree, left_idx), inorder_len);
        res_len = concat_len;
    }
    res = list_append_double(res, &res_len, tree.data[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.data_len + _mochi_idx : _mochi_idx;})]);
    long long right_idx = tree.right[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.right_len + _mochi_idx : _mochi_idx;})];
    if (right_idx != NONE) {
        res = concat_double(res, res_len, inorder(tree, right_idx), inorder_len);
        res_len = concat_len;
    }
    return inorder_len = res_len, res;
}

long long is_sorted(Tree tree, long long index) {
    if (index == NONE) {
        return 1LL;
    }
    long long left_idx = tree.left[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.left_len + _mochi_idx : _mochi_idx;})];
    if (left_idx != NONE) {
        if (tree.data[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.data_len + _mochi_idx : _mochi_idx;})] < tree.data[(int)({long long _mochi_idx = left_idx; _mochi_idx < 0 ? tree.data_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        if (!(is_sorted(tree, left_idx))) {
            return 0LL;
        }
    }
    long long right_idx = tree.right[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.right_len + _mochi_idx : _mochi_idx;})];
    if (right_idx != NONE) {
        if (tree.data[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? tree.data_len + _mochi_idx : _mochi_idx;})] > tree.data[(int)({long long _mochi_idx = right_idx; _mochi_idx < 0 ? tree.data_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        if (!(is_sorted(tree, right_idx))) {
            return 0LL;
        }
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        tree1 = (Tree){.data = (double[]){2.1, 2.0, 2.2}, .data_len = 3, .left = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = NONE; tmp[2] = NONE; tmp;}), .left_len = 3, .right = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = NONE; tmp[2] = NONE; tmp;}), .right_len = 3};
        puts(str_concat(str_concat(str_concat("Tree ", ({double *__tmp3 = inorder(tree1, 0LL); char *__res = str_list_double(__tmp3, inorder_len); __res;})), " is sorted: "), str_int(is_sorted(tree1, 0LL))));
        tree2 = (Tree){.data = (double[]){2.1, 2.0, 2.0}, .data_len = 3, .left = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = NONE; tmp[2] = NONE; tmp;}), .left_len = 3, .right = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = NONE; tmp[2] = NONE; tmp;}), .right_len = 3};
        puts(str_concat(str_concat(str_concat("Tree ", ({double *__tmp4 = inorder(tree2, 0LL); char *__res = str_list_double(__tmp4, inorder_len); __res;})), " is sorted: "), str_int(is_sorted(tree2, 0LL))));
        tree3 = (Tree){.data = (double[]){2.1, 2.0, 2.1}, .data_len = 3, .left = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = NONE; tmp[2] = NONE; tmp;}), .left_len = 3, .right = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = NONE; tmp[2] = NONE; tmp;}), .right_len = 3};
        puts(str_concat(str_concat(str_concat("Tree ", ({double *__tmp5 = inorder(tree3, 0LL); char *__res = str_list_double(__tmp5, inorder_len); __res;})), " is sorted: "), str_int(is_sorted(tree3, 0LL))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
