// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct TreeNode TreeNode;

struct TreeNode {
    long long data;
    long long left;
    long long right;
};

static TreeNode* list_append_TreeNode(TreeNode *arr, size_t *len, TreeNode val) {
    arr = realloc(arr, (*len + 1) * sizeof(TreeNode));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long total_moves = 0LL;

long long count_nodes(TreeNode * nodes, size_t nodes_len, long long idx);
long long count_coins(TreeNode * nodes, size_t nodes_len, long long idx);
long long iabs(long long x);
long long dfs(TreeNode * nodes, size_t nodes_len, long long idx);
long long distribute_coins(TreeNode * nodes, size_t nodes_len, long long root);
void user_main();
int main(void);

long long count_nodes(TreeNode * nodes, size_t nodes_len, long long idx) {
    if (idx == 0LL) {
        return 0LL;
    }
    TreeNode node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    return (count_nodes(nodes, nodes_len, node.left) + count_nodes(nodes, nodes_len, node.right)) + 1LL;
}

long long count_coins(TreeNode * nodes, size_t nodes_len, long long idx) {
    if (idx == 0LL) {
        return 0LL;
    }
    TreeNode node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    return (count_coins(nodes, nodes_len, node.left) + count_coins(nodes, nodes_len, node.right)) + node.data;
}

long long iabs(long long x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long dfs(TreeNode * nodes, size_t nodes_len, long long idx) {
    if (idx == 0LL) {
        return 0LL;
    }
    TreeNode node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long left_excess = dfs(nodes, nodes_len, node.left);
    long long right_excess = dfs(nodes, nodes_len, node.right);
    long long abs_left = iabs(left_excess);
    long long abs_right = iabs(right_excess);
    total_moves = (total_moves + abs_left) + abs_right;
    return ((node.data + left_excess) + right_excess) - 1LL;
}

long long distribute_coins(TreeNode * nodes, size_t nodes_len, long long root) {
    if (root == 0LL) {
        return 0LL;
    }
    if (count_nodes(nodes, nodes_len, root) != count_coins(nodes, nodes_len, root)) {
        panic("The nodes number should be same as the number of coins");
    }
    total_moves = 0LL;
    dfs(nodes, nodes_len, root);
    return total_moves;
}

void user_main() {
    TreeNode *example1 = NULL;
    size_t example1_len = 0;
    example1 = list_append_TreeNode(example1, &example1_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    example1 = list_append_TreeNode(example1, &example1_len, (TreeNode){.data = 3LL, .left = 2LL, .right = 3LL});
    example1 = list_append_TreeNode(example1, &example1_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    example1 = list_append_TreeNode(example1, &example1_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    TreeNode *example2 = NULL;
    size_t example2_len = 0;
    example2 = list_append_TreeNode(example2, &example2_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    example2 = list_append_TreeNode(example2, &example2_len, (TreeNode){.data = 0LL, .left = 2LL, .right = 3LL});
    example2 = list_append_TreeNode(example2, &example2_len, (TreeNode){.data = 3LL, .left = 0LL, .right = 0LL});
    example2 = list_append_TreeNode(example2, &example2_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    TreeNode *example3 = NULL;
    size_t example3_len = 0;
    example3 = list_append_TreeNode(example3, &example3_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    example3 = list_append_TreeNode(example3, &example3_len, (TreeNode){.data = 0LL, .left = 2LL, .right = 3LL});
    example3 = list_append_TreeNode(example3, &example3_len, (TreeNode){.data = 0LL, .left = 0LL, .right = 0LL});
    example3 = list_append_TreeNode(example3, &example3_len, (TreeNode){.data = 3LL, .left = 0LL, .right = 0LL});
    printf("%d\n", distribute_coins(example1, example1_len, 1LL));
    printf("%d\n", distribute_coins(example2, example2_len, 1LL));
    printf("%d\n", distribute_coins(example3, example3_len, 1LL));
    printf("%d\n", distribute_coins((TreeNode[]){(TreeNode){.data = 0LL, .left = 0LL, .right = 0LL}}, 1, 0LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
