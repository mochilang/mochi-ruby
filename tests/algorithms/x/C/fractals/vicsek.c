// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t vicsek_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* repeat_char(const char* c, long long count_);
const char* * vicsek(long long order);
const char* print_pattern(const char* * pattern, size_t pattern_len);
const char* user_main();
int main(void);

const char* repeat_char(const char* c, long long count_) {
    const char* s = "";
    long long i = 0LL;
    while (i < count_) {
        s = str_concat(s, c);
        i = i + 1LL;
    }
    return s;
}

const char* * vicsek(long long order) {
    if (order == 0LL) {
        {
            const char** __tmp0_data = malloc(1 * sizeof(const char*));
            __tmp0_data[0] = "#";
            vicsek_len = 1;
            return __tmp0_data;
        }
    }
    const char* *prev = vicsek(order - 1LL);
    size_t prev_len = vicsek_len;
    long long size = prev_len;
    const char* blank = repeat_char(" ", size);
    const char* *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < size) {
        result = list_append_str(result, &result_len, str_concat(str_concat(blank, prev[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})]), blank));
        i = i + 1LL;
    }
    i = 0LL;
    while (i < size) {
        result = list_append_str(result, &result_len, str_concat(str_concat(prev[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})], prev[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})]), prev[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    i = 0LL;
    while (i < size) {
        result = list_append_str(result, &result_len, str_concat(str_concat(blank, prev[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prev_len + _mochi_idx : _mochi_idx;})]), blank));
        i = i + 1LL;
    }
    return vicsek_len = result_len, result;
}

const char* print_pattern(const char* * pattern, size_t pattern_len) {
    long long i = 0LL;
    while (i < pattern_len) {
        puts(pattern[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? pattern_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
}

const char* user_main() {
    long long depth = 3LL;
    const char* *pattern = vicsek(depth);
    size_t pattern_len = vicsek_len;
    print_pattern(pattern, pattern_len);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
