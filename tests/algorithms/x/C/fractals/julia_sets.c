// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t prepare_grid_len;
size_t *prepare_grid_lens;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Anon1 {
    const char* __name;
    const char* im;
    const char* re;
} Anon1;

typedef struct Complex {
    double re;
    double im;
} Complex;

static Complex* list_append_Complex(Complex *arr, size_t *len, Complex val) {
    arr = realloc(arr, (*len + 1) * sizeof(Complex));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Complex** list_append_Complexptr(Complex **arr, size_t *len, Complex *val) {
    arr = realloc(arr, (*len + 1) * sizeof(Complex*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Complex complex_add(Complex a, Complex b);
Complex complex_mul(Complex a, Complex b);
double sqrtApprox(double x);
double complex_abs(Complex a);
double sin_taylor(double x);
double cos_taylor(double x);
double exp_taylor(double x);
Complex complex_exp(Complex z);
Complex eval_quadratic(Complex c, Complex z);
Complex eval_exponential(Complex c, Complex z);
Complex iterate_function(Complex(*eval_function)(Complex, Complex), Complex c, long long nb_iterations, Complex z0, double infinity);
Complex * * prepare_grid(double window_size, long long nb_pixels);
const char* julia_demo();
int main(void);

Complex complex_add(Complex a, Complex b) {
    return (Complex){.re = a.re + b.re, .im = a.im + b.im};
}

Complex complex_mul(Complex a, Complex b) {
    double real = (a.re * b.re) - (a.im * b.im);
    double imag = (a.re * b.im) + (a.im * b.re);
    return (Complex){.re = real, .im = imag};
}

double sqrtApprox(double x) {
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double complex_abs(Complex a) {
    return sqrtApprox((a.re * a.re) + (a.im * a.im));
}

double sin_taylor(double x) {
    double term = x;
    double sum = x;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = 2.0 * (double)(i);
        double k2 = (2.0 * (double)(i)) + 1.0;
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double cos_taylor(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = (2.0 * (double)(i)) - 1.0;
        double k2 = 2.0 * (double)(i);
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double exp_taylor(double x) {
    double term = 1.0;
    double sum = 1.0;
    double i = 1.0;
    while (i < 20.0) {
        term = (term * x) / i;
        sum = sum + term;
        i = i + 1.0;
    }
    return sum;
}

Complex complex_exp(Complex z) {
    double e = exp_taylor(z.re);
    return (Complex){.re = e * cos_taylor(z.im), .im = e * sin_taylor(z.im)};
}

Complex eval_quadratic(Complex c, Complex z) {
    return complex_add(complex_mul(z, z), c);
}

Complex eval_exponential(Complex c, Complex z) {
    return complex_add(complex_exp(z), c);
}

Complex iterate_function(Complex(*eval_function)(Complex, Complex), Complex c, long long nb_iterations, Complex z0, double infinity) {
    Complex z_n = z0;
    long long i = 0LL;
    while (i < nb_iterations) {
        z_n = eval_function(c, z_n);
        if (complex_abs(z_n) > infinity) {
            return z_n;
        }
        i = i + 1LL;
    }
    return z_n;
}

Complex * * prepare_grid(double window_size, long long nb_pixels) {
    Complex **grid = NULL;
    size_t grid_len = 0;
    size_t *grid_lens = NULL;
    size_t grid_lens_len = 0;
    long long i = 0LL;
    while (i < nb_pixels) {
        Complex *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < nb_pixels) {
            double real = -(window_size) + (((2.0 * window_size) * (double)(i)) / (nb_pixels - 1LL));
            double imag = -(window_size) + (((2.0 * window_size) * (double)(j)) / (nb_pixels - 1LL));
            row = list_append_Complex(row, &row_len, (Complex){.re = real, .im = imag});
            j = j + 1LL;
        }
        grid = list_append_Complexptr(grid, &grid_len, row);
        grid_lens = list_append_szt(grid_lens, &grid_lens_len, row_len);
        i = i + 1LL;
    }
    return prepare_grid_lens = grid_lens, prepare_grid_len = grid_len, grid;
}

const char* julia_demo() {
    Complex **grid = prepare_grid(1.0, 5LL);
    size_t grid_len = prepare_grid_len;
    size_t *grid_lens = prepare_grid_lens;
    size_t grid_lens_len = prepare_grid_len;
    Complex c_poly = (Complex){.re = -0.4, .im = 0.6};
    Complex c_exp = (Complex){.re = -2.0, .im = 0.0};
    long long **poly_result = NULL;
    size_t poly_result_len = 0;
    size_t *poly_result_lens = NULL;
    size_t poly_result_lens_len = 0;
    long long **exp_result = NULL;
    size_t exp_result_len = 0;
    size_t *exp_result_lens = NULL;
    size_t exp_result_lens_len = 0;
    long long y = 0LL;
    while (y < grid_len) {
        long long *row_poly = NULL;
        size_t row_poly_len = 0;
        long long *row_exp = NULL;
        size_t row_exp_len = 0;
        long long x = 0LL;
        while (x < grid_lens[(int)(y)]) {
            Complex z0 = grid[(int)({long long _mochi_idx = y; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x; _mochi_idx < 0 ? grid_lens[(int)(y)] + _mochi_idx : _mochi_idx;})];
            Complex z_poly = iterate_function(eval_quadratic, c_poly, 20LL, z0, 4.0);
            Complex z_exp = iterate_function(eval_exponential, c_exp, 10LL, z0, 1e+10);
            row_poly = list_append_long_long(row_poly, &row_poly_len, (complex_abs(z_poly) < 2.0 ? 1LL : 0LL));
            row_exp = list_append_long_long(row_exp, &row_exp_len, (complex_abs(z_exp) < 10000.0 ? 1LL : 0LL));
            x = x + 1LL;
        }
        poly_result = list_append_intptr(poly_result, &poly_result_len, row_poly);
        poly_result_lens = list_append_szt(poly_result_lens, &poly_result_lens_len, row_poly_len);
        exp_result = list_append_intptr(exp_result, &exp_result_len, row_exp);
        exp_result_lens = list_append_szt(exp_result_lens, &exp_result_lens_len, row_exp_len);
        y = y + 1LL;
    }
    puts(str_list_list_int(poly_result, poly_result_len, poly_result_lens));
    puts(str_list_list_int(exp_result, exp_result_len, exp_result_lens));
}

int main(void) {
    {
        long long __start = _now();
        julia_demo();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
