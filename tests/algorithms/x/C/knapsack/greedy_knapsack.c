// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double calc_profit(long long * profit, size_t profit_len, long long * weight, size_t weight_len, long long max_weight);
const char* user_main();
int main(void);

double calc_profit(long long * profit, size_t profit_len, long long * weight, size_t weight_len, long long max_weight) {
    if (profit_len != weight_len) {
        panic("The length of profit and weight must be same.");
    }
    if (max_weight <= 0LL) {
        panic("max_weight must greater than zero.");
    }
    long long i = 0LL;
    while (i < profit_len) {
        if (profit[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})] < 0LL) {
            panic("Profit can not be negative.");
        }
        if (weight[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})] < 0LL) {
            panic("Weight can not be negative.");
        }
        i = i + 1LL;
    }
    long long n = profit_len;
    long long *used = NULL;
    size_t used_len = 0;
    long long j = 0LL;
    while (j < n) {
        used = list_append_long_long(used, &used_len, 0LL);
        j = j + 1LL;
    }
    long long limit = 0LL;
    double gain = 0LL;
    long long count = 0LL;
    while ((limit < max_weight) && (count < n)) {
        double maxRatio = -1LL;
        double maxIndex = -1LL;
        long long k = 0LL;
        while (k < n) {
            if (!(used[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? used_len + _mochi_idx : _mochi_idx;})])) {
                double ratio = (double)(profit[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})]) / (double)(weight[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})]);
                if (ratio > maxRatio) {
                    maxRatio = ratio;
                    maxIndex = k;
                }
            }
            k = k + 1LL;
        }
        if (maxIndex < 0LL) {
            break;
        }
        used[(int)(maxIndex)] = 1LL;
        if ((max_weight - limit) >= weight[(int)({long long _mochi_idx = maxIndex; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})]) {
            limit = limit + weight[(int)({long long _mochi_idx = maxIndex; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})];
            gain = gain + (double)(profit[(int)({long long _mochi_idx = maxIndex; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})]);
        } else {
            gain = gain + (((max_weight - limit) / (double)(weight[(int)({long long _mochi_idx = maxIndex; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})])) * (double)(profit[(int)({long long _mochi_idx = maxIndex; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})]));
            break;
        }
        count = count + 1LL;
    }
    return gain;
}

const char* user_main() {
    printf("%g\n", calc_profit(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 5LL; tmp;}), 3, 15LL));
    printf("%g\n", calc_profit(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 9LL; tmp[2] = 8LL; tmp;}), 3, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 5LL; tmp;}), 3, 25LL));
    printf("%g\n", calc_profit(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 9LL; tmp[2] = 8LL; tmp;}), 3, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 5LL; tmp;}), 3, 5LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
