// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long knapsack(long long capacity, long long * weights, size_t weights_len, long long * values, size_t values_len, long long counter);
long long test_base_case();
long long test_easy_case();
long long test_knapsack();
int main(void);

long long knapsack(long long capacity, long long * weights, size_t weights_len, long long * values, size_t values_len, long long counter) {
    if ((counter == 0LL) || (capacity == 0LL)) {
        return 0LL;
    }
    if (weights[(int)({long long _mochi_idx = counter - 1LL; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})] > capacity) {
        return knapsack(capacity, weights, weights_len, values, values_len, counter - 1LL);
    }
    long long left_capacity = capacity - weights[(int)({long long _mochi_idx = counter - 1LL; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})];
    long long include_val = values[(int)({long long _mochi_idx = counter - 1LL; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})] + knapsack(left_capacity, weights, weights_len, values, values_len, counter - 1LL);
    long long exclude_val = knapsack(capacity, weights, weights_len, values, values_len, counter - 1LL);
    if (include_val > exclude_val) {
        return include_val;
    }
    return exclude_val;
}

long long test_base_case() {
    long long cap = 0LL;
    long long *val = NULL;
    size_t val_len = 0;
    val = list_append_long_long(val, &val_len, 0LL);
    long long *w = NULL;
    size_t w_len = 0;
    w = list_append_long_long(w, &w_len, 0LL);
    long long c = 1LL;
    if (knapsack(cap, w, w_len, val, val_len, c) != 0LL) {
        return 0LL;
    }
    long long *val2 = NULL;
    size_t val2_len = 0;
    val2 = list_append_long_long(val2, &val2_len, 60LL);
    long long *w2 = NULL;
    size_t w2_len = 0;
    w2 = list_append_long_long(w2, &w2_len, 10LL);
    long long c2 = 1LL;
    return knapsack(cap, w2, w2_len, val2, val2_len, c2) == 0LL;
}

long long test_easy_case() {
    long long cap = 3LL;
    long long *val = NULL;
    size_t val_len = 0;
    val = list_append_long_long(val, &val_len, 1LL);
    val = list_append_long_long(val, &val_len, 2LL);
    val = list_append_long_long(val, &val_len, 3LL);
    long long *w = NULL;
    size_t w_len = 0;
    w = list_append_long_long(w, &w_len, 3LL);
    w = list_append_long_long(w, &w_len, 2LL);
    w = list_append_long_long(w, &w_len, 1LL);
    long long c = 3LL;
    return knapsack(cap, w, w_len, val, val_len, c) == 5LL;
}

long long test_knapsack() {
    long long cap = 50LL;
    long long *val = NULL;
    size_t val_len = 0;
    val = list_append_long_long(val, &val_len, 60LL);
    val = list_append_long_long(val, &val_len, 100LL);
    val = list_append_long_long(val, &val_len, 120LL);
    long long *w = NULL;
    size_t w_len = 0;
    w = list_append_long_long(w, &w_len, 10LL);
    w = list_append_long_long(w, &w_len, 20LL);
    w = list_append_long_long(w, &w_len, 30LL);
    long long c = 3LL;
    return knapsack(cap, w, w_len, val, val_len, c) == 220LL;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", test_base_case());
        printf("%d\n", test_easy_case());
        printf("%d\n", test_knapsack());
        printf("%d\n", 1LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
