// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct CalcResult CalcResult;

struct CalcResult {
    long long ok;
    double value;
    const char* error;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

CalcResult calc_profit(long long * profit, size_t profit_len, long long * weight, size_t weight_len, long long max_weight);
long long test_sorted();
long long test_negative_max_weight();
long long test_negative_profit_value();
long long test_negative_weight_value();
long long test_null_max_weight();
long long test_unequal_list_length();
int main(void);

CalcResult calc_profit(long long * profit, size_t profit_len, long long * weight, size_t weight_len, long long max_weight) {
    if (profit_len != weight_len) {
        return (CalcResult){.ok = 0LL, .value = 0LL, .error = "The length of profit and weight must be same."};
    }
    if (max_weight <= 0LL) {
        return (CalcResult){.ok = 0LL, .value = 0LL, .error = "max_weight must greater than zero."};
    }
    long long i = 0LL;
    while (i < profit_len) {
        if (profit[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})] < 0LL) {
            return (CalcResult){.ok = 0LL, .value = 0LL, .error = "Profit can not be negative."};
        }
        if (weight[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})] < 0LL) {
            return (CalcResult){.ok = 0LL, .value = 0LL, .error = "Weight can not be negative."};
        }
        i = i + 1LL;
    }
    long long *used = NULL;
    size_t used_len = 0;
    long long j = 0LL;
    while (j < profit_len) {
        used = list_append_long_long(used, &used_len, 0LL);
        j = j + 1LL;
    }
    long long limit = 0LL;
    double gain = 0LL;
    while (limit < max_weight) {
        double max_ratio = -1LL;
        long long idx = -1LL;
        long long k = 0LL;
        while (k < profit_len) {
            if (!(used[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? used_len + _mochi_idx : _mochi_idx;})])) {
                double ratio = (double)(profit[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})]) / (double)(weight[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})]);
                if (ratio > max_ratio) {
                    max_ratio = ratio;
                    idx = k;
                }
            }
            k = k + 1LL;
        }
        if (idx == (0LL - 1LL)) {
            break;
        }
        used[(int)(idx)] = 1LL;
        if ((max_weight - limit) >= weight[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})]) {
            limit = limit + weight[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})];
            gain = gain + (double)(profit[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})]);
        } else {
            gain = gain + (((max_weight - limit) / (double)(weight[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})])) * (double)(profit[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? profit_len + _mochi_idx : _mochi_idx;})]));
            break;
        }
    }
    return (CalcResult){.ok = 1LL, .value = gain, .error = ""};
}

long long test_sorted() {
    long long *profit = NULL;
    size_t profit_len = 0;
    profit = list_append_long_long(profit, &profit_len, 10LL);
    profit = list_append_long_long(profit, &profit_len, 20LL);
    profit = list_append_long_long(profit, &profit_len, 30LL);
    profit = list_append_long_long(profit, &profit_len, 40LL);
    profit = list_append_long_long(profit, &profit_len, 50LL);
    profit = list_append_long_long(profit, &profit_len, 60LL);
    long long *weight = NULL;
    size_t weight_len = 0;
    weight = list_append_long_long(weight, &weight_len, 2LL);
    weight = list_append_long_long(weight, &weight_len, 4LL);
    weight = list_append_long_long(weight, &weight_len, 6LL);
    weight = list_append_long_long(weight, &weight_len, 8LL);
    weight = list_append_long_long(weight, &weight_len, 10LL);
    weight = list_append_long_long(weight, &weight_len, 12LL);
    CalcResult res = calc_profit(profit, profit_len, weight, weight_len, 100LL);
    return res.ok && (res.value == 210LL);
}

long long test_negative_max_weight() {
    long long *profit = NULL;
    size_t profit_len = 0;
    profit = list_append_long_long(profit, &profit_len, 10LL);
    profit = list_append_long_long(profit, &profit_len, 20LL);
    profit = list_append_long_long(profit, &profit_len, 30LL);
    profit = list_append_long_long(profit, &profit_len, 40LL);
    profit = list_append_long_long(profit, &profit_len, 50LL);
    profit = list_append_long_long(profit, &profit_len, 60LL);
    long long *weight = NULL;
    size_t weight_len = 0;
    weight = list_append_long_long(weight, &weight_len, 2LL);
    weight = list_append_long_long(weight, &weight_len, 4LL);
    weight = list_append_long_long(weight, &weight_len, 6LL);
    weight = list_append_long_long(weight, &weight_len, 8LL);
    weight = list_append_long_long(weight, &weight_len, 10LL);
    weight = list_append_long_long(weight, &weight_len, 12LL);
    CalcResult res = calc_profit(profit, profit_len, weight, weight_len, -15LL);
    return !(res.ok) && (strcmp(res.error, "max_weight must greater than zero.") == 0);
}

long long test_negative_profit_value() {
    long long *profit = NULL;
    size_t profit_len = 0;
    profit = list_append_long_long(profit, &profit_len, 10LL);
    profit = list_append_long_long(profit, &profit_len, -20LL);
    profit = list_append_long_long(profit, &profit_len, 30LL);
    profit = list_append_long_long(profit, &profit_len, 40LL);
    profit = list_append_long_long(profit, &profit_len, 50LL);
    profit = list_append_long_long(profit, &profit_len, 60LL);
    long long *weight = NULL;
    size_t weight_len = 0;
    weight = list_append_long_long(weight, &weight_len, 2LL);
    weight = list_append_long_long(weight, &weight_len, 4LL);
    weight = list_append_long_long(weight, &weight_len, 6LL);
    weight = list_append_long_long(weight, &weight_len, 8LL);
    weight = list_append_long_long(weight, &weight_len, 10LL);
    weight = list_append_long_long(weight, &weight_len, 12LL);
    CalcResult res = calc_profit(profit, profit_len, weight, weight_len, 15LL);
    return !(res.ok) && (strcmp(res.error, "Profit can not be negative.") == 0);
}

long long test_negative_weight_value() {
    long long *profit = NULL;
    size_t profit_len = 0;
    profit = list_append_long_long(profit, &profit_len, 10LL);
    profit = list_append_long_long(profit, &profit_len, 20LL);
    profit = list_append_long_long(profit, &profit_len, 30LL);
    profit = list_append_long_long(profit, &profit_len, 40LL);
    profit = list_append_long_long(profit, &profit_len, 50LL);
    profit = list_append_long_long(profit, &profit_len, 60LL);
    long long *weight = NULL;
    size_t weight_len = 0;
    weight = list_append_long_long(weight, &weight_len, 2LL);
    weight = list_append_long_long(weight, &weight_len, -4LL);
    weight = list_append_long_long(weight, &weight_len, 6LL);
    weight = list_append_long_long(weight, &weight_len, -8LL);
    weight = list_append_long_long(weight, &weight_len, 10LL);
    weight = list_append_long_long(weight, &weight_len, 12LL);
    CalcResult res = calc_profit(profit, profit_len, weight, weight_len, 15LL);
    return !(res.ok) && (strcmp(res.error, "Weight can not be negative.") == 0);
}

long long test_null_max_weight() {
    long long *profit = NULL;
    size_t profit_len = 0;
    profit = list_append_long_long(profit, &profit_len, 10LL);
    profit = list_append_long_long(profit, &profit_len, 20LL);
    profit = list_append_long_long(profit, &profit_len, 30LL);
    profit = list_append_long_long(profit, &profit_len, 40LL);
    profit = list_append_long_long(profit, &profit_len, 50LL);
    profit = list_append_long_long(profit, &profit_len, 60LL);
    long long *weight = NULL;
    size_t weight_len = 0;
    weight = list_append_long_long(weight, &weight_len, 2LL);
    weight = list_append_long_long(weight, &weight_len, 4LL);
    weight = list_append_long_long(weight, &weight_len, 6LL);
    weight = list_append_long_long(weight, &weight_len, 8LL);
    weight = list_append_long_long(weight, &weight_len, 10LL);
    weight = list_append_long_long(weight, &weight_len, 12LL);
    CalcResult res = calc_profit(profit, profit_len, weight, weight_len, 0LL);
    return !(res.ok) && (strcmp(res.error, "max_weight must greater than zero.") == 0);
}

long long test_unequal_list_length() {
    long long *profit = NULL;
    size_t profit_len = 0;
    profit = list_append_long_long(profit, &profit_len, 10LL);
    profit = list_append_long_long(profit, &profit_len, 20LL);
    profit = list_append_long_long(profit, &profit_len, 30LL);
    profit = list_append_long_long(profit, &profit_len, 40LL);
    profit = list_append_long_long(profit, &profit_len, 50LL);
    long long *weight = NULL;
    size_t weight_len = 0;
    weight = list_append_long_long(weight, &weight_len, 2LL);
    weight = list_append_long_long(weight, &weight_len, 4LL);
    weight = list_append_long_long(weight, &weight_len, 6LL);
    weight = list_append_long_long(weight, &weight_len, 8LL);
    weight = list_append_long_long(weight, &weight_len, 10LL);
    weight = list_append_long_long(weight, &weight_len, 12LL);
    CalcResult res = calc_profit(profit, profit_len, weight, weight_len, 100LL);
    return !(res.ok) && (strcmp(res.error, "The length of profit and weight must be same.") == 0);
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", test_sorted());
        printf("%d\n", test_negative_max_weight());
        printf("%d\n", test_negative_profit_value());
        printf("%d\n", test_negative_weight_value());
        printf("%d\n", test_null_max_weight());
        printf("%d\n", test_unequal_list_length());
        printf("%d\n", 1LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
