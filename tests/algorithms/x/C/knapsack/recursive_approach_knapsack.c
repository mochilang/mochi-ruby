// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long knapsack(long long * weights, size_t weights_len, long long * values, size_t values_len, long long number_of_items, long long max_weight, long long index);
void user_main();
int main(void);

long long knapsack(long long * weights, size_t weights_len, long long * values, size_t values_len, long long number_of_items, long long max_weight, long long index) {
    if (index == number_of_items) {
        return 0LL;
    }
    long long ans1 = knapsack(weights, weights_len, values, values_len, number_of_items, max_weight, index + 1LL);
    long long ans2 = 0LL;
    if (weights[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})] <= max_weight) {
        ans2 = values[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})] + knapsack(weights, weights_len, values, values_len, number_of_items, max_weight - weights[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})], index + 1LL);
    }
    if (ans1 > ans2) {
        return ans1;
    }
    return ans2;
}

void user_main() {
    long long *w1 = NULL;
    size_t w1_len = 0;
    w1 = list_append_long_long(w1, &w1_len, 1LL);
    w1 = list_append_long_long(w1, &w1_len, 2LL);
    w1 = list_append_long_long(w1, &w1_len, 4LL);
    w1 = list_append_long_long(w1, &w1_len, 5LL);
    long long *v1 = NULL;
    size_t v1_len = 0;
    v1 = list_append_long_long(v1, &v1_len, 5LL);
    v1 = list_append_long_long(v1, &v1_len, 4LL);
    v1 = list_append_long_long(v1, &v1_len, 8LL);
    v1 = list_append_long_long(v1, &v1_len, 6LL);
    long long r1 = knapsack(w1, w1_len, v1, v1_len, 4LL, 5LL, 0LL);
    puts(str_int(r1));
    long long *w2 = NULL;
    size_t w2_len = 0;
    w2 = list_append_long_long(w2, &w2_len, 3LL);
    w2 = list_append_long_long(w2, &w2_len, 4LL);
    w2 = list_append_long_long(w2, &w2_len, 5LL);
    long long *v2 = NULL;
    size_t v2_len = 0;
    v2 = list_append_long_long(v2, &v2_len, 10LL);
    v2 = list_append_long_long(v2, &v2_len, 9LL);
    v2 = list_append_long_long(v2, &v2_len, 8LL);
    long long r2 = knapsack(w2, w2_len, v2, v2_len, 3LL, 25LL, 0LL);
    puts(str_int(r2));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
