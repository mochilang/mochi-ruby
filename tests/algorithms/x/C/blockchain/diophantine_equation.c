// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t extended_gcd_len;
size_t diophantine_len;
size_t diophantine_all_soln_len;
size_t *diophantine_all_soln_lens;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long *s1 = NULL;
size_t s1_len = 0;
long long **sols = NULL;
size_t sols_len = 0;
size_t *sols_lens = NULL;
size_t sols_lens_len = 0;
long long j = 0LL;

long long gcd(long long a, long long b);
long long * extended_gcd(long long a, long long b);
long long * diophantine(long long a, long long b, long long c);
long long * * diophantine_all_soln(long long a, long long b, long long c, long long n);
int main(void);

long long gcd(long long a, long long b) {
    long long x = (a < 0LL ? -(a) : a);
    long long y = (b < 0LL ? -(b) : b);
    while (y != 0LL) {
        long long t = x % y;
        x = y;
        y = t;
    }
    return x;
}

long long * extended_gcd(long long a, long long b) {
    if (b == 0LL) {
        {
            static long long __tmp3_arr[3];
            __tmp3_arr[0] = a;
            __tmp3_arr[1] = 1LL;
            __tmp3_arr[2] = 0LL;
            extended_gcd_len = 3;
            return __tmp3_arr;
        }
    }
    long long *res = extended_gcd(b, a % b);
    size_t res_len = extended_gcd_len;
    long long d = res[(int)(0LL)];
    long long p = res[(int)(1LL)];
    long long q = res[(int)(2LL)];
    long long x = q;
    long long y = p - (q * (a / b));
    {
        static long long __tmp4_arr[3];
        __tmp4_arr[0] = d;
        __tmp4_arr[1] = x;
        __tmp4_arr[2] = y;
        extended_gcd_len = 3;
        return __tmp4_arr;
    }
}

long long * diophantine(long long a, long long b, long long c) {
    long long d = gcd(a, b);
    if ((c % d) != 0LL) {
        panic("No solution");
    }
    long long *eg = extended_gcd(a, b);
    size_t eg_len = extended_gcd_len;
    long long r = c / d;
    long long x = eg[(int)(1LL)] * r;
    long long y = eg[(int)(2LL)] * r;
    {
        static long long __tmp5_arr[2];
        __tmp5_arr[0] = x;
        __tmp5_arr[1] = y;
        diophantine_len = 2;
        return __tmp5_arr;
    }
}

long long * * diophantine_all_soln(long long a, long long b, long long c, long long n) {
    long long *base = diophantine(a, b, c);
    size_t base_len = diophantine_len;
    long long x0 = base[(int)(0LL)];
    long long y0 = base[(int)(1LL)];
    long long d = gcd(a, b);
    long long p = a / d;
    long long q = b / d;
    long long **sols = NULL;
    size_t sols_len = 0;
    size_t *sols_lens = NULL;
    size_t sols_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long x = x0 + (i * q);
        long long y = y0 - (i * p);
        sols = list_append_intptr(sols, &sols_len, ({int *tmp = malloc(2 * sizeof(int)); tmp[0] = x; tmp[1] = y; tmp;}));
        sols_lens = list_append_szt(sols_lens, &sols_lens_len, 2);
        i = i + 1LL;
    }
    return diophantine_all_soln_lens = sols_lens, diophantine_all_soln_len = sols_len, sols;
}

int main(void) {
    {
        long long __start = _now();
        s1 = diophantine(10LL, 6LL, 14LL);
        s1_len = diophantine_len;
        puts(str_list_int(s1, s1_len));
        sols = diophantine_all_soln(10LL, 6LL, 14LL, 4LL);
        sols_len = diophantine_all_soln_len;
        sols_lens = diophantine_all_soln_lens;
        while (j < sols_len) {
            puts(str_list_int(sols[(int)(j)], sols_lens[j]));
            j = j + 1LL;
        }
        puts(({long long *__tmp6 = diophantine(391LL, 299LL, -69LL); char *__res = str_list_int(__tmp6, diophantine_len); __res;}));
        puts(({long long *__tmp7 = extended_gcd(10LL, 6LL); char *__res = str_list_int(__tmp7, extended_gcd_len); __res;}));
        puts(({long long *__tmp8 = extended_gcd(7LL, 5LL); char *__res = str_list_int(__tmp8, extended_gcd_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
