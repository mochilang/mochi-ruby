// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* ascii = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";

long long ord(const char* ch);
long long bit_and(long long a, long long b);
long long bit_xor(long long a, long long b);
long long bit_not32(long long x);
long long elf_hash(const char* data);
int main(void);

long long ord(const char* ch) {
    long long i = 0LL;
    while (i < strlen(ascii)) {
        if (strcmp(_substring(ascii, i, i + 1LL), ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long bit_and(long long a, long long b) {
    long long ua = a;
    long long ub = b;
    long long res = 0LL;
    long long bit = 1LL;
    while ((ua > 0LL) || (ub > 0LL)) {
        if (((ua % 2LL) == 1LL) && ((ub % 2LL) == 1LL)) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long bit_xor(long long a, long long b) {
    long long ua = a;
    long long ub = b;
    long long res = 0LL;
    long long bit = 1LL;
    while ((ua > 0LL) || (ub > 0LL)) {
        long long abit = ua % 2LL;
        long long bbit = ub % 2LL;
        if (abit != bbit) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long bit_not32(long long x) {
    long long ux = x;
    long long res = 0LL;
    long long bit = 1LL;
    long long count = 0LL;
    while (count < 32LL) {
        if ((ux % 2LL) == 0LL) {
            res = res + bit;
        }
        ux = ux / 2LL;
        bit = bit * 2LL;
        count = count + 1LL;
    }
    return res;
}

long long elf_hash(const char* data) {
    long long hash_ = 0LL;
    long long i = 0LL;
    while (i < strlen(data)) {
        long long c = ord(_substring(data, i, i + 1LL));
        hash_ = (hash_ * 16LL) + c;
        long long x = bit_and(hash_, 4026531840LL);
        if (x != 0LL) {
            hash_ = bit_xor(hash_, x / 16777216LL);
        }
        hash_ = bit_and(hash_, bit_not32(x));
        i = i + 1LL;
    }
    return hash_;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(elf_hash("lorem ipsum")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
