// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_block_words_len;
size_t *get_block_words_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long MOD = 4294967296LL;
const char* ASCII = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";

long long ord(const char* ch);
const char* to_little_endian(const char* s);
const char* int_to_bits(long long n, long long width);
long long bits_to_int(const char* bits);
const char* to_hex(long long n);
const char* reformat_hex(long long i);
const char* preprocess(const char* message);
long long * * get_block_words(const char* bit_string);
long long bit_and(long long a, long long b);
long long bit_or(long long a, long long b);
long long bit_xor(long long a, long long b);
long long not_32(long long i);
long long sum_32(long long a, long long b);
long long lshift(long long num, long long k);
long long rshift(long long num, long long k);
long long left_rotate_32(long long i, long long shift);
const char* md5_me(const char* message);
int main(void);

long long ord(const char* ch) {
    long long i = 0LL;
    while (i < strlen(ASCII)) {
        if (strcmp(_substring(ASCII, i, i + 1LL), ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* to_little_endian(const char* s) {
    if (strlen(s) != 32LL) {
        panic("Input must be of length 32");
    }
    return str_concat(str_concat(str_concat(_substring(s, 24LL, 32LL), _substring(s, 16LL, 24LL)), _substring(s, 8LL, 16LL)), _substring(s, 0LL, 8LL));
}

const char* int_to_bits(long long n, long long width) {
    const char* bits = "";
    long long num = n;
    while (num > 0LL) {
        bits = str_concat(str_int(num % 2LL), bits);
        num = num / 2LL;
    }
    while (strlen(bits) < width) {
        bits = str_concat("0", bits);
    }
    if (strlen(bits) > width) {
        bits = _substring(bits, strlen(bits) - width, strlen(bits));
    }
    return bits;
}

long long bits_to_int(const char* bits) {
    long long num = 0LL;
    long long i = 0LL;
    while (i < strlen(bits)) {
        if (strcmp(_substring(bits, i, i + 1LL), "1") == 0) {
            num = (num * 2LL) + 1LL;
        } else {
            num = num * 2LL;
        }
        i = i + 1LL;
    }
    return num;
}

const char* to_hex(long long n) {
    const char* digits = "0123456789abcdef";
    if (n == 0LL) {
        return "0";
    }
    long long num = n;
    const char* s = "";
    while (num > 0LL) {
        long long d = num % 16LL;
        s = str_concat(_substring(digits, d, d + 1LL), s);
        num = num / 16LL;
    }
    return s;
}

const char* reformat_hex(long long i) {
    if (i < 0LL) {
        panic("Input must be non-negative");
    }
    const char* hex = to_hex(i);
    while (strlen(hex) < 8LL) {
        hex = str_concat("0", hex);
    }
    if (strlen(hex) > 8LL) {
        hex = _substring(hex, strlen(hex) - 8LL, strlen(hex));
    }
    const char* le = "";
    long long j = strlen(hex) - 2LL;
    while (j >= 0LL) {
        le = str_concat(le, _substring(hex, j, j + 2LL));
        j = j - 2LL;
    }
    return le;
}

const char* preprocess(const char* message) {
    const char* bit_string = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* ch = _substring(message, i, i + 1LL);
        bit_string = str_concat(bit_string, int_to_bits(ord(ch), 8LL));
        i = i + 1LL;
    }
    const char* start_len = int_to_bits(strlen(bit_string), 64LL);
    bit_string = str_concat(bit_string, "1");
    while ((strlen(bit_string) % 512LL) != 448LL) {
        bit_string = str_concat(bit_string, "0");
    }
    bit_string = str_concat(str_concat(bit_string, to_little_endian(_substring(start_len, 32LL, 64LL))), to_little_endian(_substring(start_len, 0LL, 32LL)));
    return bit_string;
}

long long * * get_block_words(const char* bit_string) {
    if ((strlen(bit_string) % 512LL) != 0LL) {
        panic("Input must have length that's a multiple of 512");
    }
    long long **blocks = NULL;
    size_t blocks_len = 0;
    size_t *blocks_lens = NULL;
    size_t blocks_lens_len = 0;
    long long pos = 0LL;
    while (pos < strlen(bit_string)) {
        long long *block = NULL;
        size_t block_len = 0;
        long long i = 0LL;
        while (i < 512LL) {
            const char* part = _substring(bit_string, pos + i, (pos + i) + 32LL);
            long long word = bits_to_int(to_little_endian(part));
            block = list_append_long_long(block, &block_len, word);
            i = i + 32LL;
        }
        blocks = list_append_intptr(blocks, &blocks_len, block);
        blocks_lens = list_append_szt(blocks_lens, &blocks_lens_len, block_len);
        pos = pos + 512LL;
    }
    return get_block_words_lens = blocks_lens, get_block_words_len = blocks_len, blocks;
}

long long bit_and(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    long long i = 0LL;
    while (i < 32LL) {
        if (((x % 2LL) == 1LL) && ((y % 2LL) == 1LL)) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long bit_or(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    long long i = 0LL;
    while (i < 32LL) {
        long long abit = x % 2LL;
        long long bbit = y % 2LL;
        if ((abit == 1LL) || (bbit == 1LL)) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long bit_xor(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    long long i = 0LL;
    while (i < 32LL) {
        long long abit = x % 2LL;
        long long bbit = y % 2LL;
        if (((abit + bbit) % 2LL) == 1LL) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long not_32(long long i) {
    if (i < 0LL) {
        panic("Input must be non-negative");
    }
    return 4294967295LL - i;
}

long long sum_32(long long a, long long b) {
    return (a + b) % MOD;
}

long long lshift(long long num, long long k) {
    long long result = num % MOD;
    long long i = 0LL;
    while (i < k) {
        result = (result * 2LL) % MOD;
        i = i + 1LL;
    }
    return result;
}

long long rshift(long long num, long long k) {
    long long result = num;
    long long i = 0LL;
    while (i < k) {
        result = result / 2LL;
        i = i + 1LL;
    }
    return result;
}

long long left_rotate_32(long long i, long long shift) {
    if (i < 0LL) {
        panic("Input must be non-negative");
    }
    if (shift < 0LL) {
        panic("Shift must be non-negative");
    }
    long long left = lshift(i, shift);
    long long right = rshift(i, 32LL - shift);
    return (left + right) % MOD;
}

const char* md5_me(const char* message) {
    const char* bit_string = preprocess(message);
    long long *added_consts = NULL;
    size_t added_consts_len = 0;
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3614090360LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3905402710LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 606105819LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3250441966LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4118548399LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1200080426LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2821735955LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4249261313LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1770035416LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2336552879LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4294925233LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2304563134LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1804603682LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4254626195LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2792965006LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1236535329LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4129170786LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3225465664LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 643717713LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3921069994LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3593408605LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 38016083LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3634488961LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3889429448LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 568446438LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3275163606LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4107603335LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1163531501LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2850285829LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4243563512LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1735328473LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2368359562LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4294588738LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2272392833LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1839030562LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4259657740LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2763975236LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1272893353LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4139469664LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3200236656LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 681279174LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3936430074LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3572445317LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 76029189LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3654602809LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3873151461LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 530742520LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3299628645LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4096336452LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1126891415LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2878612391LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4237533241LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1700485571LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2399980690LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4293915773LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2240044497LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1873313359LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4264355552LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 2734768916LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 1309151649LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 4149444226LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3174756917LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 718787259LL);
    added_consts = list_append_long_long(added_consts, &added_consts_len, 3951481745LL);
    long long *shift_amounts = NULL;
    size_t shift_amounts_len = 0;
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 7LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 12LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 17LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 22LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 7LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 12LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 17LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 22LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 7LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 12LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 17LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 22LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 7LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 12LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 17LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 22LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 5LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 9LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 14LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 20LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 5LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 9LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 14LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 20LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 5LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 9LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 14LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 20LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 5LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 9LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 14LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 20LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 4LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 11LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 16LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 23LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 4LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 11LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 16LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 23LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 4LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 11LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 16LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 23LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 4LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 11LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 16LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 23LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 6LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 10LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 15LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 21LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 6LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 10LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 15LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 21LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 6LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 10LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 15LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 21LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 6LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 10LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 15LL);
    shift_amounts = list_append_long_long(shift_amounts, &shift_amounts_len, 21LL);
    long long a0 = 1732584193LL;
    long long b0 = 4023233417LL;
    long long c0 = 2562383102LL;
    long long d0 = 271733878LL;
    long long **blocks = get_block_words(bit_string);
    size_t blocks_len = get_block_words_len;
    size_t *blocks_lens = get_block_words_lens;
    size_t blocks_lens_len = get_block_words_len;
    long long bi = 0LL;
    while (bi < blocks_len) {
        long long *block = blocks[(int)({long long _mochi_idx = bi; _mochi_idx < 0 ? blocks_len + _mochi_idx : _mochi_idx;})];
        size_t block_len = blocks_lens[bi];
        long long a = a0;
        long long b = b0;
        long long c = c0;
        long long d = d0;
        long long i = 0LL;
        while (i < 64LL) {
            long long f = 0LL;
            long long g = 0LL;
            if (i <= 15LL) {
                f = bit_xor(d, bit_and(b, bit_xor(c, d)));
                g = i;
            } else {
                if (i <= 31LL) {
                    f = bit_xor(c, bit_and(d, bit_xor(b, c)));
                    g = ((5LL * i) + 1LL) % 16LL;
                } else {
                    if (i <= 47LL) {
                        f = bit_xor(bit_xor(b, c), d);
                        g = ((3LL * i) + 5LL) % 16LL;
                    } else {
                        f = bit_xor(c, bit_or(b, not_32(d)));
                        g = (7LL * i) % 16LL;
                    }
                }
            }
            f = sum_32(f, a);
            f = sum_32(f, added_consts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? added_consts_len + _mochi_idx : _mochi_idx;})]);
            f = sum_32(f, block[(int)({long long _mochi_idx = g; _mochi_idx < 0 ? block_len + _mochi_idx : _mochi_idx;})]);
            long long rotated = left_rotate_32(f, shift_amounts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? shift_amounts_len + _mochi_idx : _mochi_idx;})]);
            long long new_b = sum_32(b, rotated);
            a = d;
            d = c;
            c = b;
            b = new_b;
            i = i + 1LL;
        }
        a0 = sum_32(a0, a);
        b0 = sum_32(b0, b);
        c0 = sum_32(c0, c);
        d0 = sum_32(d0, d);
        bi = bi + 1LL;
    }
    const char* digest = str_concat(str_concat(str_concat(reformat_hex(a0), reformat_hex(b0)), reformat_hex(c0)), reformat_hex(d0));
    return digest;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
