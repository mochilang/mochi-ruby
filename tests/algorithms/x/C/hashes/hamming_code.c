// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t string_to_bitlist_len;
size_t hamming_encode_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct DecodeResult DecodeResult;

struct DecodeResult {
    long long *data;
    size_t data_len;
    long long ack;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long index_of(const char* s, const char* ch);
long long ord(const char* ch);
const char* chr(long long n);
const char* text_to_bits(const char* text);
const char* text_from_bits(const char* bits);
const char* bool_to_string(long long b);
long long * string_to_bitlist(const char* s);
const char* bitlist_to_string(long long * bits, size_t bits_len);
long long is_power_of_two(long long x);
long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len);
long long pow2(long long e);
long long has_bit(long long n, long long b);
long long * hamming_encode(long long r, long long * data_bits, size_t data_bits_len);
DecodeResult hamming_decode(long long r, long long * code, size_t code_len);
void user_main();
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long ord(const char* ch) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    long long idx = index_of(upper, ch);
    if (idx >= 0LL) {
        return 65LL + idx;
    }
    idx = index_of(lower, ch);
    if (idx >= 0LL) {
        return 97LL + idx;
    }
    return 0LL;
}

const char* chr(long long n) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    if ((n >= 65LL) && (n < 91LL)) {
        return _substring(upper, n - 65LL, n - 64LL);
    }
    if ((n >= 97LL) && (n < 123LL)) {
        return _substring(lower, n - 97LL, n - 96LL);
    }
    return "?";
}

const char* text_to_bits(const char* text) {
    const char* bits = "";
    long long i = 0LL;
    while (i < strlen(text)) {
        long long code = ord((const char[]){text[i], 0});
        long long j = 7LL;
        while (j >= 0LL) {
            long long p = pow2(j);
            if (((code / p) % 2LL) == 1LL) {
                bits = str_concat(bits, "1");
            } else {
                bits = str_concat(bits, "0");
            }
            j = j - 1LL;
        }
        i = i + 1LL;
    }
    return bits;
}

const char* text_from_bits(const char* bits) {
    const char* text = "";
    long long i = 0LL;
    while (i < strlen(bits)) {
        long long code = 0LL;
        long long j = 0LL;
        while ((j < 8LL) && ((i + j) < strlen(bits))) {
            code = code * 2LL;
            if (strcmp((const char[]){bits[i + j], 0}, "1") == 0) {
                code = code + 1LL;
            }
            j = j + 1LL;
        }
        text = str_concat(text, chr(code));
        i = i + 8LL;
    }
    return text;
}

const char* bool_to_string(long long b) {
    if (b) {
        return "True";
    }
    return "False";
}

long long * string_to_bitlist(const char* s) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, "1") == 0) {
            res = list_append_long_long(res, &res_len, 1LL);
        } else {
            res = list_append_long_long(res, &res_len, 0LL);
        }
        i = i + 1LL;
    }
    return string_to_bitlist_len = res_len, res;
}

const char* bitlist_to_string(long long * bits, size_t bits_len) {
    const char* s = "";
    long long i = 0LL;
    while (i < bits_len) {
        if (bits[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? bits_len + _mochi_idx : _mochi_idx;})] == 1LL) {
            s = str_concat(s, "1");
        } else {
            s = str_concat(s, "0");
        }
        i = i + 1LL;
    }
    return s;
}

long long is_power_of_two(long long x) {
    if (x < 1LL) {
        return 0LL;
    }
    long long p = 1LL;
    while (p < x) {
        p = p * 2LL;
    }
    return p == x;
}

long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long pow2(long long e) {
    long long res = 1LL;
    long long i = 0LL;
    while (i < e) {
        res = res * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long has_bit(long long n, long long b) {
    long long p = pow2(b);
    if (((n / p) % 2LL) == 1LL) {
        return 1LL;
    }
    return 0LL;
}

long long * hamming_encode(long long r, long long * data_bits, size_t data_bits_len) {
    long long total = r + data_bits_len;
    long long *data_ord = NULL;
    size_t data_ord_len = 0;
    long long cont_data = 0LL;
    long long x = 1LL;
    while (x <= total) {
        if (is_power_of_two(x)) {
            data_ord = list_append_long_long(data_ord, &data_ord_len, -1LL);
        } else {
            data_ord = list_append_long_long(data_ord, &data_ord_len, data_bits[(int)({long long _mochi_idx = cont_data; _mochi_idx < 0 ? data_bits_len + _mochi_idx : _mochi_idx;})]);
            cont_data = cont_data + 1LL;
        }
        x = x + 1LL;
    }
    long long *parity = NULL;
    size_t parity_len = 0;
    long long bp = 0LL;
    while (bp < r) {
        long long cont_bo = 0LL;
        long long j = 0LL;
        while (j < data_ord_len) {
            long long bit = data_ord[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? data_ord_len + _mochi_idx : _mochi_idx;})];
            if (bit >= 0LL) {
                long long pos = j + 1LL;
                if (has_bit(pos, bp) && (bit == 1LL)) {
                    cont_bo = cont_bo + 1LL;
                }
            }
            j = j + 1LL;
        }
        parity = list_append_long_long(parity, &parity_len, cont_bo % 2LL);
        bp = bp + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    long long cont_bp = 0LL;
    long long i = 0LL;
    while (i < data_ord_len) {
        if (data_ord[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_ord_len + _mochi_idx : _mochi_idx;})] < 0LL) {
            result = list_append_long_long(result, &result_len, parity[(int)({long long _mochi_idx = cont_bp; _mochi_idx < 0 ? parity_len + _mochi_idx : _mochi_idx;})]);
            cont_bp = cont_bp + 1LL;
        } else {
            result = list_append_long_long(result, &result_len, data_ord[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_ord_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return hamming_encode_len = result_len, result;
}

DecodeResult hamming_decode(long long r, long long * code, size_t code_len) {
    long long *data_output = NULL;
    size_t data_output_len = 0;
    long long *parity_received = NULL;
    size_t parity_received_len = 0;
    long long i = 1LL;
    long long idx = 0LL;
    while (i <= code_len) {
        if (is_power_of_two(i)) {
            parity_received = list_append_long_long(parity_received, &parity_received_len, code[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? code_len + _mochi_idx : _mochi_idx;})]);
        } else {
            data_output = list_append_long_long(data_output, &data_output_len, code[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? code_len + _mochi_idx : _mochi_idx;})]);
        }
        idx = idx + 1LL;
        i = i + 1LL;
    }
    long long *recomputed = hamming_encode(r, data_output, data_output_len);
    size_t recomputed_len = hamming_encode_len;
    long long *parity_calc = NULL;
    size_t parity_calc_len = 0;
    long long j = 0LL;
    while (j < recomputed_len) {
        if (is_power_of_two(j + 1LL)) {
            parity_calc = list_append_long_long(parity_calc, &parity_calc_len, recomputed[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? recomputed_len + _mochi_idx : _mochi_idx;})]);
        }
        j = j + 1LL;
    }
    long long ack = list_eq(parity_received, parity_received_len, parity_calc, parity_calc_len);
    return (DecodeResult){.data = data_output, .data_len = data_output_len, .ack = ack};
}

void user_main() {
    long long sizePari = 4LL;
    long long be = 2LL;
    const char* text = "Message01";
    const char* binary = text_to_bits(text);
    puts(str_concat(str_concat("Text input in binary is '", binary), "'"));
    long long *data_bits = string_to_bitlist(binary);
    size_t data_bits_len = string_to_bitlist_len;
    long long *encoded = hamming_encode(sizePari, data_bits, data_bits_len);
    size_t encoded_len = hamming_encode_len;
    puts(str_concat("Data converted ----------> ", bitlist_to_string(encoded, encoded_len)));
    DecodeResult decoded = hamming_decode(sizePari, encoded, encoded_len);
    puts(str_concat(str_concat(str_concat("Data receive ------------> ", bitlist_to_string(decoded.data, decoded.data_len)), " -- Data integrity: "), bool_to_string(decoded.ack)));
    long long *corrupted = NULL;
    size_t corrupted_len = 0;
    long long i = 0LL;
    while (i < encoded_len) {
        corrupted = list_append_long_long(corrupted, &corrupted_len, encoded[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? encoded_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long pos = be - 1LL;
    if (corrupted[(int)({long long _mochi_idx = pos; _mochi_idx < 0 ? corrupted_len + _mochi_idx : _mochi_idx;})] == 0LL) {
        corrupted[(int)(pos)] = 1LL;
    } else {
        corrupted[(int)(pos)] = 0LL;
    }
    DecodeResult decoded_err = hamming_decode(sizePari, corrupted, corrupted_len);
    puts(str_concat(str_concat(str_concat("Data receive (error) ----> ", bitlist_to_string(decoded_err.data, decoded_err.data_len)), " -- Data integrity: "), bool_to_string(decoded_err.ack)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
