// Generated by Mochi 0.10.32 on 2025-08-08 10:43 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t segment_image_len;
size_t *segment_image_lens;

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * segment_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long * thresholds, size_t thresholds_len);
void user_main();
int main(void);

long long * * segment_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long * thresholds, size_t thresholds_len) {
    long long **segmented = NULL;
    size_t segmented_len = 0;
    size_t *segmented_lens = NULL;
    size_t segmented_lens_len = 0;
    long long i = 0LL;
    while (i < image_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < image_lens[i]) {
            long long pixel = image[(int)(i)][(int)(j)];
            long long label = 0LL;
            long long k = 0LL;
            while (k < thresholds_len) {
                if (pixel > thresholds[(int)(k)]) {
                    label = k + 1LL;
                }
                k = k + 1LL;
            }
            row = list_append_int(row, &row_len, label);
            j = j + 1LL;
        }
        segmented = list_append_long_longptr(segmented, &segmented_len, row);
        segmented_lens = list_append_szt(segmented_lens, &segmented_lens_len, row_len);
        i = i + 1LL;
    }
    return segment_image_lens = segmented_lens, segment_image_len = segmented_len, segmented;
}

void user_main() {
    long long image_0[3] = {80LL, 120LL, 180LL};
    long long image_1[3] = {40LL, 90LL, 150LL};
    long long image_2[3] = {20LL, 60LL, 100LL};
    long long *image_init[3] = {image_0, image_1, image_2};
    long long **image = image_init;
    size_t image_len = 3;
    size_t image_lens_init[3] = {3, 3, 3};
    size_t *image_lens = image_lens_init;
    size_t image_lens_len = 3;
    long long *thresholds = NULL;
    size_t thresholds_len = 0;
    thresholds = list_append_int(thresholds, &thresholds_len, 50LL);
    thresholds = list_append_int(thresholds, &thresholds_len, 100LL);
    thresholds = list_append_int(thresholds, &thresholds_len, 150LL);
    long long **segmented = segment_image(image, image_len, image_lens, image_len, thresholds, thresholds_len);
    size_t segmented_len = segment_image_len;
    size_t *segmented_lens = segment_image_lens;
    size_t segmented_lens_len = segment_image_len;
    puts(str_list_list_int(segmented, segmented_len, segmented_lens));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
