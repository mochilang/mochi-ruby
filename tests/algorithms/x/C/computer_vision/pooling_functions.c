// Generated by Mochi 0.10.32 on 2025-08-08 11:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t maxpooling_len;
size_t *maxpooling_lens;
size_t avgpooling_len;
size_t *avgpooling_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * maxpooling(long long * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long size, long long stride);
long long * * avgpooling(long long * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long size, long long stride);
const char* print_matrix(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
void user_main();
int main(void);

long long * * maxpooling(long long * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long size, long long stride) {
    long long n = arr_len;
    if ((n == 0LL) || (arr_lens[0LL] != n)) {
        panic("The input array is not a square matrix");
    }
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while ((i + size) <= n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while ((j + size) <= n) {
            long long max_val = arr[(int)(i)][(int)(j)];
            long long r = i;
            while (r < (i + size)) {
                long long c = j;
                while (c < (j + size)) {
                    long long val = arr[(int)(r)][(int)(c)];
                    if (val > max_val) {
                        max_val = val;
                    }
                    c = c + 1LL;
                }
                r = r + 1LL;
            }
            row = list_append_long_long(row, &row_len, max_val);
            j = j + stride;
        }
        result = list_append_long_longptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + stride;
    }
    return maxpooling_lens = result_lens, maxpooling_len = result_len, result;
}

long long * * avgpooling(long long * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long size, long long stride) {
    long long n = arr_len;
    if ((n == 0LL) || (arr_lens[0LL] != n)) {
        panic("The input array is not a square matrix");
    }
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while ((i + size) <= n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while ((j + size) <= n) {
            long long sum = 0LL;
            long long r = i;
            while (r < (i + size)) {
                long long c = j;
                while (c < (j + size)) {
                    sum = sum + arr[(int)(r)][(int)(c)];
                    c = c + 1LL;
                }
                r = r + 1LL;
            }
            row = list_append_int(row, &row_len, sum / (size * size));
            j = j + stride;
        }
        result = list_append_long_longptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + stride;
    }
    return avgpooling_lens = result_lens, avgpooling_len = result_len, result;
}

const char* print_matrix(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long i = 0LL;
    while (i < mat_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < mat_lens[i]) {
            line = str_concat(line, str_int(mat[(int)(i)][(int)(j)]));
            if (j < (mat_lens[i] - 1LL)) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

void user_main() {
    long long arr1_0[4] = {1LL, 2LL, 3LL, 4LL};
    long long arr1_1[4] = {5LL, 6LL, 7LL, 8LL};
    long long arr1_2[4] = {9LL, 10LL, 11LL, 12LL};
    long long arr1_3[4] = {13LL, 14LL, 15LL, 16LL};
    long long *arr1_init[4] = {arr1_0, arr1_1, arr1_2, arr1_3};
    long long **arr1 = arr1_init;
    size_t arr1_len = 4;
    size_t arr1_lens_init[4] = {4, 4, 4, 4};
    size_t *arr1_lens = arr1_lens_init;
    size_t arr1_lens_len = 4;
    long long arr2_0[3] = {147LL, 180LL, 122LL};
    long long arr2_1[3] = {241LL, 76LL, 32LL};
    long long arr2_2[3] = {126LL, 13LL, 157LL};
    long long *arr2_init[3] = {arr2_0, arr2_1, arr2_2};
    long long **arr2 = arr2_init;
    size_t arr2_len = 3;
    size_t arr2_lens_init[3] = {3, 3, 3};
    size_t *arr2_lens = arr2_lens_init;
    size_t arr2_lens_len = 3;
    long long** __tmp4 = maxpooling(arr1, arr1_len, arr1_lens, arr1_len, 2LL, 2LL);
    print_matrix(__tmp4, maxpooling_len, maxpooling_lens, maxpooling_len);
    long long** __tmp5 = maxpooling(arr2, arr2_len, arr2_lens, arr2_len, 2LL, 1LL);
    print_matrix(__tmp5, maxpooling_len, maxpooling_lens, maxpooling_len);
    long long** __tmp6 = avgpooling(arr1, arr1_len, arr1_lens, arr1_len, 2LL, 2LL);
    print_matrix(__tmp6, avgpooling_len, avgpooling_lens, avgpooling_len);
    long long** __tmp7 = avgpooling(arr2, arr2_len, arr2_lens, arr2_len, 2LL, 1LL);
    print_matrix(__tmp7, avgpooling_len, avgpooling_lens, avgpooling_len);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
