// Generated by Mochi 0.10.32 on 2025-08-07 17:30 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t flip_horizontal_image_len;
size_t *flip_horizontal_image_lens;
size_t flip_vertical_image_len;
size_t *flip_vertical_image_lens;
size_t flip_horizontal_boxes_len;
size_t *flip_horizontal_boxes_lens;
size_t flip_vertical_boxes_len;
size_t *flip_vertical_boxes_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long image_0[3] = {1LL, 2LL, 3LL};
long long image_1[3] = {4LL, 5LL, 6LL};
long long image_2[3] = {7LL, 8LL, 9LL};
long long *image_init[3] = {image_0, image_1, image_2};
long long **image = image_init;
size_t image_len = 3;
size_t image_lens_init[3] = {3, 3, 3};
size_t *image_lens = image_lens_init;
size_t image_lens_len = 3;
double boxes_0[5] = {0.0, 0.25, 0.25, 0.5, 0.5};
double boxes_1[5] = {1.0, 0.75, 0.75, 0.5, 0.5};
double *boxes_init[2] = {boxes_0, boxes_1};
double **boxes = boxes_init;
size_t boxes_len = 2;
size_t boxes_lens_init[2] = {5, 5};
size_t *boxes_lens = boxes_lens_init;
size_t boxes_lens_len = 2;
long long **h_img = NULL;
size_t h_img_len = 0;
size_t *h_img_lens = NULL;
size_t h_img_lens_len = 0;
double **h_boxes = NULL;
size_t h_boxes_len = 0;
size_t *h_boxes_lens = NULL;
size_t h_boxes_lens_len = 0;
long long **v_img = NULL;
size_t v_img_len = 0;
size_t *v_img_lens = NULL;
size_t v_img_lens_len = 0;
double **v_boxes = NULL;
size_t v_boxes_len = 0;
size_t *v_boxes_lens = NULL;
size_t v_boxes_lens_len = 0;

long long * * flip_horizontal_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
long long * * flip_vertical_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
double * * flip_horizontal_boxes(double * * boxes, size_t boxes_len, size_t* boxes_lens, size_t boxes_lens_len);
double * * flip_vertical_boxes(double * * boxes, size_t boxes_len, size_t* boxes_lens, size_t boxes_lens_len);
void print_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
int main(void);

long long * * flip_horizontal_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long **flipped = NULL;
    size_t flipped_len = 0;
    size_t *flipped_lens = NULL;
    size_t flipped_lens_len = 0;
    long long i = 0LL;
    while (i < img_len) {
        long long *row = img[(int)(i)];
        size_t row_len = img_lens[i];
        long long j = row_len - 1LL;
        long long *new_row = NULL;
        size_t new_row_len = 0;
        while (j >= 0LL) {
            new_row = list_append_long_long(new_row, &new_row_len, row[(int)(j)]);
            j = j - 1LL;
        }
        flipped = list_append_long_longptr(flipped, &flipped_len, new_row);
        flipped_lens = list_append_szt(flipped_lens, &flipped_lens_len, new_row_len);
        i = i + 1LL;
    }
    return flip_horizontal_image_lens = flipped_lens, flip_horizontal_image_len = flipped_len, flipped;
}

long long * * flip_vertical_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long **flipped = NULL;
    size_t flipped_len = 0;
    size_t *flipped_lens = NULL;
    size_t flipped_lens_len = 0;
    long long i = img_len - 1LL;
    while (i >= 0LL) {
        flipped = list_append_long_longptr(flipped, &flipped_len, img[(int)(i)]);
        flipped_lens = list_append_szt(flipped_lens, &flipped_lens_len, img_lens[i]);
        i = i - 1LL;
    }
    return flip_vertical_image_lens = flipped_lens, flip_vertical_image_len = flipped_len, flipped;
}

double * * flip_horizontal_boxes(double * * boxes, size_t boxes_len, size_t* boxes_lens, size_t boxes_lens_len) {
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < boxes_len) {
        double *b = boxes[(int)(i)];
        size_t b_len = boxes_lens[i];
        double x_new = 1.0 - b[(int)(1LL)];
        result = list_append_doubleptr(result, &result_len, ({double *tmp = malloc(5 * sizeof(double)); tmp[0] = b[(int)(0LL)]; tmp[1] = x_new; tmp[2] = b[(int)(2LL)]; tmp[3] = b[(int)(3LL)]; tmp[4] = b[(int)(4LL)]; tmp;}));
        result_lens = list_append_szt(result_lens, &result_lens_len, 5);
        i = i + 1LL;
    }
    return flip_horizontal_boxes_lens = result_lens, flip_horizontal_boxes_len = result_len, result;
}

double * * flip_vertical_boxes(double * * boxes, size_t boxes_len, size_t* boxes_lens, size_t boxes_lens_len) {
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < boxes_len) {
        double *b = boxes[(int)(i)];
        size_t b_len = boxes_lens[i];
        double y_new = 1.0 - b[(int)(2LL)];
        result = list_append_doubleptr(result, &result_len, ({double *tmp = malloc(5 * sizeof(double)); tmp[0] = b[(int)(0LL)]; tmp[1] = b[(int)(1LL)]; tmp[2] = y_new; tmp[3] = b[(int)(3LL)]; tmp[4] = b[(int)(4LL)]; tmp;}));
        result_lens = list_append_szt(result_lens, &result_lens_len, 5);
        i = i + 1LL;
    }
    return flip_vertical_boxes_lens = result_lens, flip_vertical_boxes_len = result_len, result;
}

void print_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long i = 0LL;
    while (i < img_len) {
        long long *row = img[(int)(i)];
        size_t row_len = img_lens[i];
        long long j = 0LL;
        const char* line = "";
        while (j < row_len) {
            line = str_concat(str_concat(line, str_int(row[(int)(j)])), " ");
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts("Original image:");
        print_image(image, image_len, image_lens, image_len);
        puts(str_list_list_double(boxes, boxes_len, boxes_lens));
        puts("Horizontal flip:");
        h_img = flip_horizontal_image(image, image_len, image_lens, image_len);
        h_img_len = flip_horizontal_image_len;
        h_img_lens = flip_horizontal_image_lens;
        h_boxes = flip_horizontal_boxes(boxes, boxes_len, boxes_lens, boxes_len);
        h_boxes_len = flip_horizontal_boxes_len;
        h_boxes_lens = flip_horizontal_boxes_lens;
        print_image(h_img, h_img_len, h_img_lens, h_img_len);
        puts(str_list_list_double(h_boxes, h_boxes_len, h_boxes_lens));
        puts("Vertical flip:");
        v_img = flip_vertical_image(image, image_len, image_lens, image_len);
        v_img_len = flip_vertical_image_len;
        v_img_lens = flip_vertical_image_lens;
        v_boxes = flip_vertical_boxes(boxes, boxes_len, boxes_lens, boxes_len);
        v_boxes_len = flip_vertical_boxes_len;
        v_boxes_lens = flip_vertical_boxes_lens;
        print_image(v_img, v_img_len, v_img_lens, v_img_len);
        puts(str_list_list_double(v_boxes, v_boxes_len, v_boxes_lens));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
