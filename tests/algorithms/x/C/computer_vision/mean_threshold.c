// Generated by Mochi 0.10.32 on 2025-08-08 10:43 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t mean_threshold_len;
size_t *mean_threshold_lens;

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long img_0[3] = {10LL, 200LL, 50LL};
long long img_1[3] = {100LL, 150LL, 30LL};
long long img_2[3] = {90LL, 80LL, 220LL};
long long *img_init[3] = {img_0, img_1, img_2};
long long **img = img_init;
size_t img_len = 3;
size_t img_lens_init[3] = {3, 3, 3};
size_t *img_lens = img_lens_init;
size_t img_lens_len = 3;
long long **result = NULL;
size_t result_len = 0;
size_t *result_lens = NULL;
size_t result_lens_len = 0;

long long * * mean_threshold(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len);
void print_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len);
int main(void);

long long * * mean_threshold(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len) {
    long long height = image_len;
    long long width = image_lens[0LL];
    long long total = 0LL;
    long long i = 0LL;
    while (i < height) {
        long long j = 0LL;
        while (j < width) {
            total = total + image[(int)(i)][(int)(j)];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    long long mean = total / (height * width);
    i = 0LL;
    while (i < height) {
        long long j = 0LL;
        while (j < width) {
            if (image[(int)(i)][(int)(j)] > mean) {
                image[(int)(i)][(int)(j)] = 255LL;
            } else {
                image[(int)(i)][(int)(j)] = 0LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return mean_threshold_lens = image_lens, mean_threshold_len = image_len, image;
}

void print_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len) {
    long long i = 0LL;
    while (i < image_len) {
        puts(str_list_int(image[(int)(i)], image_lens[i]));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        result = mean_threshold(img, img_len, img_lens, img_len);
        result_len = mean_threshold_len;
        result_lens = mean_threshold_lens;
        print_image(result, result_len, result_lens, result_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
