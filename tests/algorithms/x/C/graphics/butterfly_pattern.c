// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* repeat_char(const char* ch, long long count_);
const char* butterfly_pattern(long long n);
int main(void);

const char* repeat_char(const char* ch, long long count_) {
    const char* result = "";
    long long i = 0LL;
    while (i < count_) {
        result = str_concat(result, ch);
        i = i + 1LL;
    }
    return result;
}

const char* butterfly_pattern(long long n) {
    const char* *lines = NULL;
    size_t lines_len = 0;
    long long i = 1LL;
    while (i < n) {
        const char* left = repeat_char("*", i);
        const char* mid = repeat_char(" ", (2LL * (n - i)) - 1LL);
        const char* right = repeat_char("*", i);
        lines = list_append_str(lines, &lines_len, str_concat(str_concat(left, mid), right));
        i = i + 1LL;
    }
    lines = list_append_str(lines, &lines_len, repeat_char("*", (2LL * n) - 1LL));
    long long j = n - 1LL;
    while (j > 0LL) {
        const char* left = repeat_char("*", j);
        const char* mid = repeat_char(" ", (2LL * (n - j)) - 1LL);
        const char* right = repeat_char("*", j);
        lines = list_append_str(lines, &lines_len, str_concat(str_concat(left, mid), right));
        j = j - 1LL;
    }
    const char* out = "";
    long long k = 0LL;
    while (k < lines_len) {
        if (k > 0LL) {
            out = str_concat(out, "\n");
        }
        out = str_concat(out, lines[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? lines_len + _mochi_idx : _mochi_idx;})]);
        k = k + 1LL;
    }
    return out;
}

int main(void) {
    {
        long long __start = _now();
        puts(butterfly_pattern(3LL));
        puts(butterfly_pattern(5LL));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
