// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t convert_to_2d_len;
size_t rotate_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;

double user_floor(double x);
double user_modf(double x, double m);
double sin_taylor(double x);
double cos_taylor(double x);
double * convert_to_2d(double x, double y, double z, double scale, double distance);
double * rotate(double x, double y, double z, const char* axis, double angle);
int main(void);

double user_floor(double x) {
    long long i = (int)(x);
    if ((double)(i) > x) {
        i = i - 1LL;
    }
    return (double)(i);
}

double user_modf(double x, double m) {
    return x - (user_floor(x / m) * m);
}

double sin_taylor(double x) {
    double term = x;
    double sum = x;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = 2.0 * (double)(i);
        double k2 = k1 + 1.0;
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double cos_taylor(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = (2.0 * (double)(i)) - 1.0;
        double k2 = 2.0 * (double)(i);
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double * convert_to_2d(double x, double y, double z, double scale, double distance) {
    double projected_x = ((x * distance) / (z + distance)) * scale;
    double projected_y = ((y * distance) / (z + distance)) * scale;
    {
        double* __tmp2_data = malloc(2 * sizeof(double));
        __tmp2_data[0] = projected_x;
        __tmp2_data[1] = projected_y;
        convert_to_2d_len = 2;
        return __tmp2_data;
    }
}

double * rotate(double x, double y, double z, const char* axis, double angle) {
    angle = ((user_modf(angle, 360.0) / 450.0) * 180.0) / PI;
    angle = user_modf(angle, 2.0 * PI);
    if (angle > PI) {
        angle = angle - (2.0 * PI);
    }
    if (strcmp(axis, "z") == 0) {
        double new_x = (x * cos_taylor(angle)) - (y * sin_taylor(angle));
        double new_y = (y * cos_taylor(angle)) + (x * sin_taylor(angle));
        double new_z = z;
        {
            double* __tmp3_data = malloc(3 * sizeof(double));
            __tmp3_data[0] = new_x;
            __tmp3_data[1] = new_y;
            __tmp3_data[2] = new_z;
            rotate_len = 3;
            return __tmp3_data;
        }
    }
    if (strcmp(axis, "x") == 0) {
        double new_y = (y * cos_taylor(angle)) - (z * sin_taylor(angle));
        double new_z = (z * cos_taylor(angle)) + (y * sin_taylor(angle));
        double new_x = x;
        {
            double* __tmp4_data = malloc(3 * sizeof(double));
            __tmp4_data[0] = new_x;
            __tmp4_data[1] = new_y;
            __tmp4_data[2] = new_z;
            rotate_len = 3;
            return __tmp4_data;
        }
    }
    if (strcmp(axis, "y") == 0) {
        double new_x = (x * cos_taylor(angle)) - (z * sin_taylor(angle));
        double new_z = (z * cos_taylor(angle)) + (x * sin_taylor(angle));
        double new_y = y;
        {
            double* __tmp5_data = malloc(3 * sizeof(double));
            __tmp5_data[0] = new_x;
            __tmp5_data[1] = new_y;
            __tmp5_data[2] = new_z;
            rotate_len = 3;
            return __tmp5_data;
        }
    }
    puts("not a valid axis, choose one of 'x', 'y', 'z'");
    {
        double* __tmp6_data = malloc(3 * sizeof(double));
        __tmp6_data[0] = 0.0;
        __tmp6_data[1] = 0.0;
        __tmp6_data[2] = 0.0;
        rotate_len = 3;
        return __tmp6_data;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(({double *__tmp7 = convert_to_2d(1.0, 2.0, 3.0, 10.0, 10.0); char *__res = str_list_double(__tmp7, convert_to_2d_len); __res;}));
        puts(({double *__tmp8 = rotate(1.0, 2.0, 3.0, "y", 90.0); char *__res = str_list_double(__tmp8, rotate_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
