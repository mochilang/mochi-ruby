// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t basis_function_len;
size_t bezier_point_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long **control = NULL;
size_t control_len = 0;
size_t *control_lens = NULL;
size_t control_lens_len = 0;

double n_choose_k(long long n, long long k);
double pow_float(double base, long long exp);
double * basis_function(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, double t);
double * bezier_point(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, double t);
int main(void);

double n_choose_k(long long n, long long k) {
    if ((k < 0LL) || (k > n)) {
        return 0.0;
    }
    if ((k == 0LL) || (k == n)) {
        return 1.0;
    }
    double result = 1.0;
    long long i = 1LL;
    while (i <= k) {
        result = (result * (1.0 * ((n - k) + i))) / (1.0 * i);
        i = i + 1LL;
    }
    return result;
}

double pow_float(double base, long long exp) {
    double result = 1.0;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double * basis_function(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, double t) {
    long long degree = points_len - 1LL;
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i <= degree) {
        double coef = n_choose_k(degree, i);
        double term = pow_float(1.0 - t, degree - i) * pow_float(t, i);
        res = list_append_double(res, &res_len, coef * term);
        i = i + 1LL;
    }
    return basis_function_len = res_len, res;
}

double * bezier_point(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, double t) {
    double *basis = basis_function(points, points_len, points_lens, points_len, t);
    size_t basis_len = basis_function_len;
    double x = 0.0;
    double y = 0.0;
    long long i = 0LL;
    while (i < points_len) {
        x = x + (basis[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? basis_len + _mochi_idx : _mochi_idx;})] * points[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? points_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
        y = y + (basis[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? basis_len + _mochi_idx : _mochi_idx;})] * points[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? points_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    {
        double* __tmp4_data = malloc(2 * sizeof(double));
        __tmp4_data[0] = x;
        __tmp4_data[1] = y;
        bezier_point_len = 2;
        return __tmp4_data;
    }
}

int main(void) {
    {
        long long __start = _now();
        control = malloc(2 * sizeof(long long*));
        control_len = 2;
        control[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 1.0; tmp[1] = 1.0; tmp;});
        control[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 1.0; tmp[1] = 2.0; tmp;});
        control_lens = ({size_t *tmp = malloc(2 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp;});
        control_lens_len = 2;
        puts(({double *__tmp5 = basis_function(control, control_len, control_lens, control_len, 0.0); char *__res = str_list_double(__tmp5, basis_function_len); __res;}));
        puts(({double *__tmp6 = basis_function(control, control_len, control_lens, control_len, 1.0); char *__res = str_list_double(__tmp6, basis_function_len); __res;}));
        puts(({double *__tmp7 = bezier_point(control, control_len, control_lens, control_len, 0.0); char *__res = str_list_double(__tmp7, bezier_point_len); __res;}));
        puts(({double *__tmp8 = bezier_point(control, control_len, control_lens, control_len, 1.0); char *__res = str_list_double(__tmp8, bezier_point_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
