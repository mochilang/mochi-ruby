// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t zeros3d_len;
size_t *zeros3d_lens;
size_t **zeros3d_lens_lens;
size_t resize_nn_len;
size_t *resize_nn_lens;
size_t **resize_nn_lens_lens;

size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long*** list_append_intptrptr(long long ***arr, size_t *len, long long **val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long*** list_append_long_longptrptr(long long* **arr, size_t *len, long long* *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * * zeros3d(long long h, long long w, long long c);
long long * * * resize_nn(long long * * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long dst_w, long long dst_h);
void user_main();
int main(void);

long long * * * zeros3d(long long h, long long w, long long c) {
    long long ***arr = NULL;
    size_t arr_len = 0;
    size_t *arr_lens = NULL;
    size_t arr_lens_len = 0;
    size_t **arr_lens_lens = NULL;
    size_t arr_lens_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long **row = NULL;
        size_t row_len = 0;
        size_t *row_lens = NULL;
        size_t row_lens_len = 0;
        long long x = 0LL;
        while (x < w) {
            long long *pixel = NULL;
            size_t pixel_len = 0;
            long long k = 0LL;
            while (k < c) {
                pixel = list_append_long_long(pixel, &pixel_len, 0LL);
                k = k + 1LL;
            }
            row = list_append_intptr(row, &row_len, pixel);
            row_lens = list_append_szt(row_lens, &row_lens_len, pixel_len);
            x = x + 1LL;
        }
        arr = list_append_long_longptrptr(arr, &arr_len, row);
        arr_lens = list_append_szt(arr_lens, &arr_lens_len, row_len);
        y = y + 1LL;
    }
    return zeros3d_lens = arr_lens, zeros3d_len = arr_len, arr;
}

long long * * * resize_nn(long long * * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long dst_w, long long dst_h) {
    long long src_h = img_len;
    long long src_w = img_lens[(int)(0LL)];
    long long channels = 0;
    double ratio_x = (double)(src_w) / (double)(dst_w);
    double ratio_y = (double)(src_h) / (double)(dst_h);
    long long ***out = zeros3d(dst_h, dst_w, channels);
    size_t out_len = zeros3d_len;
    size_t *out_lens = zeros3d_lens;
    size_t out_lens_len = zeros3d_len;
    size_t **out_lens_lens = zeros3d_lens_lens;
    size_t out_lens_lens_len = zeros3d_len;
    long long i = 0LL;
    while (i < dst_h) {
        long long j = 0LL;
        while (j < dst_w) {
            long long src_x = ratio_x * (double)(j);
            long long src_y = ratio_y * (double)(i);
            out[(int)(i)][(int)(j)] = img[(int)({long long _mochi_idx = src_y; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = src_x; _mochi_idx < 0 ? img_lens[(int)(src_y)] + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return resize_nn_lens = out_lens, resize_nn_len = out_len, out;
}

void user_main() {
    long long img_0_0[3] = {0LL, 0LL, 0LL};
    long long img_0_1[3] = {255LL, 255LL, 255LL};
    long long *img_0[2] = {img_0_0, img_0_1};
    size_t img_0_lens[2] = {3, 3};
    long long img_1_0[3] = {255LL, 0LL, 0LL};
    long long img_1_1[3] = {0LL, 255LL, 0LL};
    long long *img_1[2] = {img_1_0, img_1_1};
    size_t img_1_lens[2] = {3, 3};
    long long **img_init[2] = {img_0, img_1};
    long long ***img = img_init;
    size_t img_len = 2;
    size_t img_lens_init[2] = {2, 2};
    size_t *img_lens = img_lens_init;
    size_t img_lens_len = 2;
    size_t *img_lens_lens_init[2] = {img_0_lens, img_1_lens};
    size_t **img_lens_lens = img_lens_lens_init;
    size_t img_lens_lens_len = 2;
    long long ***resized = resize_nn(img, img_len, img_lens, img_len, 4LL, 4LL);
    size_t resized_len = resize_nn_len;
    size_t *resized_lens = resize_nn_lens;
    size_t resized_lens_len = resize_nn_len;
    size_t **resized_lens_lens = resize_nn_lens_lens;
    size_t resized_lens_lens_len = resize_nn_len;
    printf("%d\n", resized);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
