// Generated by Mochi 0.10.32 on 2025-08-25 16:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t change_brightness_len;
size_t *change_brightness_lens;

size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **sample = NULL;
size_t sample_len = 0;
size_t *sample_lens = NULL;
size_t sample_lens_len = 0;

long long clamp(long long value);
long long * * change_brightness(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long level);
int main(void);

long long clamp(long long value) {
    if (value < 0LL) {
        return 0LL;
    }
    if (value > 255LL) {
        return 255LL;
    }
    return value;
}

long long * * change_brightness(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long level) {
    if ((level < -255LL) || (level > 255LL)) {
        panic("level must be between -255 and 255");
    }
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < img_len) {
        long long *row_res = NULL;
        size_t row_res_len = 0;
        long long j = 0LL;
        while (j < img_lens[(int)(i)]) {
            row_res = list_append_long_long(row_res, &row_res_len, clamp(img[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? img_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] + level));
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row_res);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_res_len);
        i = i + 1LL;
    }
    return change_brightness_lens = result_lens, change_brightness_len = result_len, result;
}

int main(void) {
    {
        long long __start = _now();
        sample = malloc(2 * sizeof(long long*));
        sample_len = 2;
        sample[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 100LL; tmp[1] = 150LL; tmp;});
        sample[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 200LL; tmp[1] = 250LL; tmp;});
        sample_lens = ({size_t *tmp = malloc(2 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp;});
        sample_lens_len = 2;
        printf("%d\n", change_brightness(sample, sample_len, sample_lens, sample_len, 30LL));
        printf("%d\n", change_brightness(sample, sample_len, sample_lens, sample_len, -60LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
