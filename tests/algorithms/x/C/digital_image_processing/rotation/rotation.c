// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t mat_inverse3_len;
size_t *mat_inverse3_lens;
size_t mat_vec_mul_len;
size_t create_matrix_len;
size_t *create_matrix_lens;
size_t get_rotation_len;
size_t *get_rotation_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **img = NULL;
size_t img_len = 0;
size_t *img_lens = NULL;
size_t img_lens_len = 0;
double **pts1 = NULL;
size_t pts1_len = 0;
size_t *pts1_lens = NULL;
size_t pts1_lens_len = 0;
double **pts2 = NULL;
size_t pts2_len = 0;
size_t *pts2_lens = NULL;
size_t pts2_lens_len = 0;
long long **rotated = NULL;
size_t rotated_len = 0;
size_t *rotated_lens = NULL;
size_t rotated_lens_len = 0;

double * * mat_inverse3(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
double * mat_vec_mul(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, double * v, size_t v_len);
long long * * create_matrix(long long rows, long long cols, long long value);
long long round_to_int(double x);
long long * * get_rotation(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, double * * pt1, size_t pt1_len, size_t* pt1_lens, size_t pt1_lens_len, double * * pt2, size_t pt2_len, size_t* pt2_lens, size_t pt2_lens_len, long long rows, long long cols);
int main(void);

double * * mat_inverse3(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    double a = m[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
    double b = m[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
    double c = m[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
    double d = m[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})];
    double e = m[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})];
    double f = m[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})];
    double g = m[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})];
    double h = m[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})];
    double i = m[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})];
    double det = ((a * ((e * i) - (f * h))) - (b * ((d * i) - (f * g)))) + (c * ((d * h) - (e * g)));
    if (det == 0.0) {
        panic("singular matrix");
    }
    double adj00 = (e * i) - (f * h);
    double adj01 = (c * h) - (b * i);
    double adj02 = (b * f) - (c * e);
    double adj10 = (f * g) - (d * i);
    double adj11 = (a * i) - (c * g);
    double adj12 = (c * d) - (a * f);
    double adj20 = (d * h) - (e * g);
    double adj21 = (b * g) - (a * h);
    double adj22 = (a * e) - (b * d);
    double **inv = NULL;
    size_t inv_len = 0;
    size_t *inv_lens = NULL;
    size_t inv_lens_len = 0;
    inv = list_append_doubleptr(inv, &inv_len, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = adj00 / det; tmp[1] = adj01 / det; tmp[2] = adj02 / det; tmp;}));
    inv_lens = list_append_szt(inv_lens, &inv_lens_len, 3);
    inv = list_append_doubleptr(inv, &inv_len, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = adj10 / det; tmp[1] = adj11 / det; tmp[2] = adj12 / det; tmp;}));
    inv_lens = list_append_szt(inv_lens, &inv_lens_len, 3);
    inv = list_append_doubleptr(inv, &inv_len, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = adj20 / det; tmp[1] = adj21 / det; tmp[2] = adj22 / det; tmp;}));
    inv_lens = list_append_szt(inv_lens, &inv_lens_len, 3);
    return mat_inverse3_lens = inv_lens, mat_inverse3_len = inv_len, inv;
}

double * mat_vec_mul(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, double * v, size_t v_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < 3LL) {
        double val = ((m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * v[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]) + (m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * v[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})])) + (m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? m_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * v[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]);
        res = list_append_double(res, &res_len, val);
        i = i + 1LL;
    }
    return mat_vec_mul_len = res_len, res;
}

long long * * create_matrix(long long rows, long long cols, long long value) {
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long r = 0LL;
    while (r < rows) {
        long long *row = NULL;
        size_t row_len = 0;
        long long c = 0LL;
        while (c < cols) {
            row = list_append_long_long(row, &row_len, value);
            c = c + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        r = r + 1LL;
    }
    return create_matrix_lens = result_lens, create_matrix_len = result_len, result;
}

long long round_to_int(double x) {
    if (x >= 0.0) {
        return (int)(x + 0.5);
    }
    return (int)(x - 0.5);
}

long long * * get_rotation(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, double * * pt1, size_t pt1_len, size_t* pt1_lens, size_t pt1_lens_len, double * * pt2, size_t pt2_len, size_t* pt2_lens, size_t pt2_lens_len, long long rows, long long cols) {
    double src_0[3] = {pt1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt1_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt1_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})], pt1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt1_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt1_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})], 1.0};
    double src_1[3] = {pt1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt1_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt1_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})], pt1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt1_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt1_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})], 1.0};
    double src_2[3] = {pt1[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? pt1_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt1_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})], pt1[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? pt1_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt1_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})], 1.0};
    double *src_init[3] = {src_0, src_1, src_2};
    double **src = src_init;
    size_t src_len = 3;
    size_t src_lens_init[3] = {3, 3, 3};
    size_t *src_lens = src_lens_init;
    size_t src_lens_len = 3;
    double **inv = mat_inverse3(src, src_len, src_lens, src_len);
    size_t inv_len = mat_inverse3_len;
    size_t *inv_lens = mat_inverse3_lens;
    size_t inv_lens_len = mat_inverse3_len;
    double *vecx = NULL;
    size_t vecx_len = 0;
    vecx = list_append_double(vecx, &vecx_len, pt2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt2_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt2_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})]);
    vecx = list_append_double(vecx, &vecx_len, pt2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt2_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt2_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})]);
    vecx = list_append_double(vecx, &vecx_len, pt2[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? pt2_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt2_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})]);
    double *vecy = NULL;
    size_t vecy_len = 0;
    vecy = list_append_double(vecy, &vecy_len, pt2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pt2_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt2_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})]);
    vecy = list_append_double(vecy, &vecy_len, pt2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt2_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt2_lens[(int)(1LL)] + _mochi_idx : _mochi_idx;})]);
    vecy = list_append_double(vecy, &vecy_len, pt2[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? pt2_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pt2_lens[(int)(2LL)] + _mochi_idx : _mochi_idx;})]);
    double *avec = mat_vec_mul(inv, inv_len, inv_lens, inv_len, vecx, vecx_len);
    size_t avec_len = mat_vec_mul_len;
    double *bvec = mat_vec_mul(inv, inv_len, inv_lens, inv_len, vecy, vecy_len);
    size_t bvec_len = mat_vec_mul_len;
    double a0 = avec[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? avec_len + _mochi_idx : _mochi_idx;})];
    double a1 = avec[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? avec_len + _mochi_idx : _mochi_idx;})];
    double a2 = avec[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? avec_len + _mochi_idx : _mochi_idx;})];
    double b0 = bvec[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? bvec_len + _mochi_idx : _mochi_idx;})];
    double b1 = bvec[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? bvec_len + _mochi_idx : _mochi_idx;})];
    double b2 = bvec[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? bvec_len + _mochi_idx : _mochi_idx;})];
    long long **out = create_matrix(rows, cols, 0LL);
    size_t out_len = create_matrix_len;
    size_t *out_lens = create_matrix_lens;
    size_t out_lens_len = create_matrix_len;
    long long y = 0LL;
    while (y < rows) {
        long long x = 0LL;
        while (x < cols) {
            double xf = ((a0 * (1.0 * x)) + (a1 * (1.0 * y))) + a2;
            double yf = ((b0 * (1.0 * x)) + (b1 * (1.0 * y))) + b2;
            long long sx = round_to_int(xf);
            long long sy = round_to_int(yf);
            if ((((sx >= 0LL) && (sx < cols)) && (sy >= 0LL)) && (sy < rows)) {
                out[(int)(sy)][(int)(sx)] = img[(int)({long long _mochi_idx = y; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x; _mochi_idx < 0 ? img_lens[(int)(y)] + _mochi_idx : _mochi_idx;})];
            }
            x = x + 1LL;
        }
        y = y + 1LL;
    }
    return get_rotation_lens = out_lens, get_rotation_len = out_len, out;
}

int main(void) {
    {
        long long __start = _now();
        img = malloc(3 * sizeof(long long*));
        img_len = 3;
        img[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;});
        img[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 5LL; tmp[2] = 6LL; tmp;});
        img[2] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 7LL; tmp[1] = 8LL; tmp[2] = 9LL; tmp;});
        img_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;});
        img_lens_len = 3;
        pts1 = malloc(3 * sizeof(double*));
        pts1_len = 3;
        pts1[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 0.0; tmp[1] = 0.0; tmp;});
        pts1[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 0.0; tmp;});
        pts1[2] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 0.0; tmp[1] = 2.0; tmp;});
        pts1_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp;});
        pts1_lens_len = 3;
        pts2 = malloc(3 * sizeof(double*));
        pts2_len = 3;
        pts2[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 0.0; tmp[1] = 2.0; tmp;});
        pts2[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 0.0; tmp[1] = 0.0; tmp;});
        pts2[2] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp;});
        pts2_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp;});
        pts2_lens_len = 3;
        rotated = get_rotation(img, img_len, img_lens, img_len, pts1, pts1_len, pts1_lens, pts1_len, pts2, pts2_len, pts2_lens, pts2_len, 3LL, 3LL);
        rotated_len = get_rotation_len;
        rotated_lens = get_rotation_lens;
        puts(str_list_list_int(rotated, rotated_len, rotated_lens));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
