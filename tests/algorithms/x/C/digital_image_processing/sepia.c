// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t make_sepia_len;
size_t *make_sepia_lens;
size_t **make_sepia_lens_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long ***image = NULL;
size_t image_len = 0;
size_t *image_lens = NULL;
size_t image_lens_len = 0;
size_t **image_lens_lens = NULL;
size_t image_lens_lens_len = 0;
long long ***sepia = NULL;
size_t sepia_len = 0;
size_t *sepia_lens = NULL;
size_t sepia_lens_len = 0;
size_t **sepia_lens_lens = NULL;
size_t sepia_lens_lens_len = 0;

long long normalize(long long value);
long long to_grayscale(long long blue, long long green, long long red);
long long * * * make_sepia(long long * * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long factor);
int main(void);

long long normalize(long long value) {
    if (value > 255LL) {
        return 255LL;
    }
    return value;
}

long long to_grayscale(long long blue, long long green, long long red) {
    double gs = ((0.2126 * (double)(red)) + (0.587 * (double)(green))) + (0.114 * (double)(blue));
    return (int)(gs);
}

long long * * * make_sepia(long long * * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long factor) {
    long long pixel_h = img_len;
    long long pixel_v = img_lens[(int)(0LL)];
    long long i = 0LL;
    while (i < pixel_h) {
        long long j = 0LL;
        while (j < pixel_v) {
            long long *pixel = img[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? img_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
            size_t pixel_len = 0;
            long long grey = to_grayscale(pixel[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pixel_len + _mochi_idx : _mochi_idx;})], pixel[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pixel_len + _mochi_idx : _mochi_idx;})], pixel[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? pixel_len + _mochi_idx : _mochi_idx;})]);
            img[(int)(i)][(int)(j)] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = normalize(grey); tmp[1] = normalize(grey + factor); tmp[2] = normalize(grey + (2LL * factor)); tmp;});
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return make_sepia_lens = img_lens, make_sepia_len = img_len, img;
}

int main(void) {
    {
        long long __start = _now();
        image = malloc(2 * sizeof(long long**));
        image_len = 2;
        image[0] = ({long long** tmp = malloc(2 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 20LL; tmp[2] = 30LL; tmp;}); tmp[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 40LL; tmp[1] = 50LL; tmp[2] = 60LL; tmp;}); tmp;});
        image[1] = ({long long** tmp = malloc(2 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 70LL; tmp[1] = 80LL; tmp[2] = 90LL; tmp;}); tmp[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 200LL; tmp[1] = 150LL; tmp[2] = 100LL; tmp;}); tmp;});
        image_lens = ({size_t *tmp = malloc(2 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp;});
        image_lens_len = 2;
        sepia = make_sepia(image, image_len, image_lens, image_len, 20LL);
        sepia_len = make_sepia_len;
        sepia_lens = make_sepia_lens;
        puts(str_int(sepia));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
