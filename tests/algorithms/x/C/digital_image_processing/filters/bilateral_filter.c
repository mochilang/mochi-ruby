// Generated by Mochi 0.10.32 on 2025-08-09 16:15 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t vec_gaussian_len;
size_t *vec_gaussian_lens;
size_t get_slice_len;
size_t *get_slice_lens;
size_t get_gauss_kernel_len;
size_t *get_gauss_kernel_lens;
size_t elementwise_sub_len;
size_t *elementwise_sub_lens;
size_t elementwise_mul_len;
size_t *elementwise_mul_lens;

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;
double img_0[3] = {0.2, 0.3, 0.4};
double img_1[3] = {0.3, 0.4, 0.5};
double img_2[3] = {0.4, 0.5, 0.6};
double *img_init[3] = {img_0, img_1, img_2};
double **img = img_init;
size_t img_len = 3;
size_t img_lens_init[3] = {3, 3, 3};
size_t *img_lens = img_lens_init;
size_t img_lens_len = 3;
double result = 0;

double user_abs(double x);
double sqrtApprox(double x);
double expApprox(double x);
double * * vec_gaussian(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len, double variance);
double * * get_slice(double * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long x, long long y, long long kernel_size);
double * * get_gauss_kernel(long long kernel_size, double spatial_variance);
double * * elementwise_sub(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len, double value);
double * * elementwise_mul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
double matrix_sum(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
double bilateral_filter(double * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, double spatial_variance, double intensity_variance, long long kernel_size);
int main(void);

double user_abs(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double expApprox(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long n = 1LL;
    while (n < 10LL) {
        term = (term * x) / (double)(n);
        sum = sum + term;
        n = n + 1LL;
    }
    return sum;
}

double * * vec_gaussian(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len, double variance) {
    long long i = 0LL;
    double **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    while (i < mat_len) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < mat_lens[i]) {
            double v = mat[(int)(i)][(int)(j)];
            double e = -(v * v) / (2.0 * variance);
            row = list_append_double(row, &row_len, expApprox(e));
            j = j + 1LL;
        }
        out = list_append_doubleptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        i = i + 1LL;
    }
    return vec_gaussian_lens = out_lens, vec_gaussian_len = out_len, out;
}

double * * get_slice(double * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long x, long long y, long long kernel_size) {
    long long half = kernel_size / 2LL;
    long long i = x - half;
    double **slice = NULL;
    size_t slice_len = 0;
    size_t *slice_lens = NULL;
    size_t slice_lens_len = 0;
    while (i <= (x + half)) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = y - half;
        while (j <= (y + half)) {
            row = list_append_double(row, &row_len, img[(int)(i)][(int)(j)]);
            j = j + 1LL;
        }
        slice = list_append_doubleptr(slice, &slice_len, row);
        slice_lens = list_append_szt(slice_lens, &slice_lens_len, row_len);
        i = i + 1LL;
    }
    return get_slice_lens = slice_lens, get_slice_len = slice_len, slice;
}

double * * get_gauss_kernel(long long kernel_size, double spatial_variance) {
    double **arr = NULL;
    size_t arr_len = 0;
    size_t *arr_lens = NULL;
    size_t arr_lens_len = 0;
    long long i = 0LL;
    while (i < kernel_size) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < kernel_size) {
            double di = i - (kernel_size / 2LL);
            double dj = j - (kernel_size / 2LL);
            double dist = sqrtApprox((di * di) + (dj * dj));
            row = list_append_double(row, &row_len, dist);
            j = j + 1LL;
        }
        arr = list_append_doubleptr(arr, &arr_len, row);
        arr_lens = list_append_szt(arr_lens, &arr_lens_len, row_len);
        i = i + 1LL;
    }
    {
        double** __ret0 = vec_gaussian(arr, arr_len, arr_lens, arr_len, spatial_variance);
        get_gauss_kernel_lens = vec_gaussian_lens;
get_gauss_kernel_len = vec_gaussian_len;
        return __ret0;
    }
}

double * * elementwise_sub(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len, double value) {
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < mat_len) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < mat_lens[i]) {
            row = list_append_double(row, &row_len, mat[(int)(i)][(int)(j)] - value);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return elementwise_sub_lens = res_lens, elementwise_sub_len = res_len, res;
}

double * * elementwise_mul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < a_len) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < a_lens[i]) {
            row = list_append_double(row, &row_len, a[(int)(i)][(int)(j)] * b[(int)(i)][(int)(j)]);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return elementwise_mul_lens = res_lens, elementwise_mul_len = res_len, res;
}

double matrix_sum(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    double total = 0.0;
    long long i = 0LL;
    while (i < mat_len) {
        long long j = 0LL;
        while (j < mat_lens[i]) {
            total = total + mat[(int)(i)][(int)(j)];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return total;
}

double bilateral_filter(double * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, double spatial_variance, double intensity_variance, long long kernel_size) {
    double **gauss_ker = get_gauss_kernel(kernel_size, spatial_variance);
    size_t gauss_ker_len = get_gauss_kernel_len;
    size_t *gauss_ker_lens = get_gauss_kernel_lens;
    size_t gauss_ker_lens_len = get_gauss_kernel_len;
    double **img_s = img;
    size_t img_s_len = img_len;
    size_t *img_s_lens = img_lens;
    size_t img_s_lens_len = img_len;
    double center = img_s[(int)(kernel_size / 2LL)][(int)(kernel_size / 2LL)];
    double **img_i = elementwise_sub(img_s, img_s_len, img_s_lens, img_s_len, center);
    size_t img_i_len = elementwise_sub_len;
    size_t *img_i_lens = elementwise_sub_lens;
    size_t img_i_lens_len = elementwise_sub_len;
    double **img_ig = vec_gaussian(img_i, img_i_len, img_i_lens, img_i_len, intensity_variance);
    size_t img_ig_len = vec_gaussian_len;
    size_t *img_ig_lens = vec_gaussian_lens;
    size_t img_ig_lens_len = vec_gaussian_len;
    double **weights = elementwise_mul(gauss_ker, gauss_ker_len, gauss_ker_lens, gauss_ker_len, img_ig, img_ig_len, img_ig_lens, img_ig_len);
    size_t weights_len = elementwise_mul_len;
    size_t *weights_lens = elementwise_mul_lens;
    size_t weights_lens_len = elementwise_mul_len;
    double **vals = elementwise_mul(img_s, img_s_len, img_s_lens, img_s_len, weights, weights_len, weights_lens, weights_len);
    size_t vals_len = elementwise_mul_len;
    size_t *vals_lens = elementwise_mul_lens;
    size_t vals_lens_len = elementwise_mul_len;
    double sum_weights = matrix_sum(weights, weights_len, weights_lens, weights_len);
    double val = 0.0;
    if (sum_weights != 0.0) {
        val = matrix_sum(vals, vals_len, vals_lens, vals_len) / sum_weights;
    }
    return val;
}

int main(void) {
    {
        long long __start = _now();
        result = bilateral_filter(img, img_len, img_lens, img_len, 1.0, 1.0, 3LL);
        printf("%g\n", result);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
