// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t gen_gaussian_kernel_len;
size_t *gen_gaussian_kernel_lens;
size_t gaussian_filter_len;
size_t *gaussian_filter_lens;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double PI = 3.141592653589793;
long long **img = NULL;
size_t img_len = 0;
size_t *img_lens = NULL;
size_t img_lens_len = 0;
long long **gaussian3 = NULL;
size_t gaussian3_len = 0;
size_t *gaussian3_lens = NULL;
size_t gaussian3_lens_len = 0;
long long **gaussian5 = NULL;
size_t gaussian5_len = 0;
size_t *gaussian5_lens = NULL;
size_t gaussian5_lens_len = 0;

double expApprox(double x);
double * * gen_gaussian_kernel(long long k_size, double sigma);
long long * * gaussian_filter(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long k_size, double sigma);
void print_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len);
int main(void);

double expApprox(double x) {
    double sum = 1.0;
    double term = 1.0;
    long long n = 1LL;
    while (n < 10LL) {
        term = (term * x) / (double)(n);
        sum = sum + term;
        n = n + 1LL;
    }
    return sum;
}

double * * gen_gaussian_kernel(long long k_size, double sigma) {
    long long center = k_size / 2LL;
    double **kernel = NULL;
    size_t kernel_len = 0;
    size_t *kernel_lens = NULL;
    size_t kernel_lens_len = 0;
    long long i = 0LL;
    while (i < k_size) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < k_size) {
            double x = i - center;
            double y = j - center;
            double exponent = -(((x * x) + (y * y)) / ((2.0 * sigma) * sigma));
            double value = (1.0 / ((2.0 * PI) * sigma)) * expApprox(exponent);
            row = list_append_double(row, &row_len, value);
            j = j + 1LL;
        }
        kernel = list_append_doubleptr(kernel, &kernel_len, row);
        kernel_lens = list_append_szt(kernel_lens, &kernel_lens_len, row_len);
        i = i + 1LL;
    }
    return gen_gaussian_kernel_lens = kernel_lens, gen_gaussian_kernel_len = kernel_len, kernel;
}

long long * * gaussian_filter(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long k_size, double sigma) {
    long long height = image_len;
    long long width = image_lens[(int)(0LL)];
    long long dst_height = (height - k_size) + 1LL;
    long long dst_width = (width - k_size) + 1LL;
    double **kernel = gen_gaussian_kernel(k_size, sigma);
    size_t kernel_len = gen_gaussian_kernel_len;
    size_t *kernel_lens = gen_gaussian_kernel_lens;
    size_t kernel_lens_len = gen_gaussian_kernel_len;
    long long **dst = NULL;
    size_t dst_len = 0;
    size_t *dst_lens = NULL;
    size_t dst_lens_len = 0;
    long long i = 0LL;
    while (i < dst_height) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < dst_width) {
            double sum = 0.0;
            long long ki = 0LL;
            while (ki < k_size) {
                long long kj = 0LL;
                while (kj < k_size) {
                    sum = sum + ((double)(image[(int)({long long _mochi_idx = i + ki; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j + kj; _mochi_idx < 0 ? image_lens[(int)(i + ki)] + _mochi_idx : _mochi_idx;})]) * kernel[(int)({long long _mochi_idx = ki; _mochi_idx < 0 ? kernel_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = kj; _mochi_idx < 0 ? kernel_lens[(int)(ki)] + _mochi_idx : _mochi_idx;})]);
                    kj = kj + 1LL;
                }
                ki = ki + 1LL;
            }
            row = list_append_long_long(row, &row_len, (int)(sum));
            j = j + 1LL;
        }
        dst = list_append_intptr(dst, &dst_len, row);
        dst_lens = list_append_szt(dst_lens, &dst_lens_len, row_len);
        i = i + 1LL;
    }
    return gaussian_filter_lens = dst_lens, gaussian_filter_len = dst_len, dst;
}

void print_image(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len) {
    long long i = 0LL;
    while (i < image_len) {
        puts(str_list_int(image[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})], image_lens[(int)(i)]));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        img = malloc(5 * sizeof(long long*));
        img_len = 5;
        img[0] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 52LL; tmp[1] = 55LL; tmp[2] = 61LL; tmp[3] = 59LL; tmp[4] = 79LL; tmp;});
        img[1] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 62LL; tmp[1] = 59LL; tmp[2] = 55LL; tmp[3] = 104LL; tmp[4] = 94LL; tmp;});
        img[2] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 63LL; tmp[1] = 65LL; tmp[2] = 66LL; tmp[3] = 113LL; tmp[4] = 144LL; tmp;});
        img[3] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 68LL; tmp[1] = 70LL; tmp[2] = 70LL; tmp[3] = 126LL; tmp[4] = 154LL; tmp;});
        img[4] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 70LL; tmp[1] = 72LL; tmp[2] = 69LL; tmp[3] = 128LL; tmp[4] = 155LL; tmp;});
        img_lens = ({size_t *tmp = malloc(5 * sizeof(size_t)); tmp[0] = 5; tmp[1] = 5; tmp[2] = 5; tmp[3] = 5; tmp[4] = 5; tmp;});
        img_lens_len = 5;
        gaussian3 = gaussian_filter(img, img_len, img_lens, img_len, 3LL, 1.0);
        gaussian3_len = gaussian_filter_len;
        gaussian3_lens = gaussian_filter_lens;
        gaussian5 = gaussian_filter(img, img_len, img_lens, img_len, 5LL, 0.8);
        gaussian5_len = gaussian_filter_len;
        gaussian5_lens = gaussian_filter_lens;
        print_image(gaussian3, gaussian3_len, gaussian3_lens, gaussian3_len);
        print_image(gaussian5, gaussian5_len, gaussian5_lens, gaussian5_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
