// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t insertion_sort_len;
size_t median_filter_len;
size_t *median_filter_lens;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * insertion_sort(long long * a, size_t a_len);
long long * * median_filter(long long * * gray_img, size_t gray_img_len, size_t* gray_img_lens, size_t gray_img_lens_len, long long mask);
void user_main();
int main(void);

long long * insertion_sort(long long * a, size_t a_len) {
    long long i = 1LL;
    while (i < a_len) {
        long long key = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (a[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] > key)) {
            a[(int)(j + 1LL)] = a[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        a[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return insertion_sort_len = a_len, a;
}

long long * * median_filter(long long * * gray_img, size_t gray_img_len, size_t* gray_img_lens, size_t gray_img_lens_len, long long mask) {
    long long rows = gray_img_len;
    long long cols = gray_img_lens[(int)(0LL)];
    long long bd = mask / 2LL;
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < cols) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    i = bd;
    while (i < (rows - bd)) {
        long long j = bd;
        while (j < (cols - bd)) {
            long long *kernel = NULL;
            size_t kernel_len = 0;
            long long x = i - bd;
            while (x <= (i + bd)) {
                long long y = j - bd;
                while (y <= (j + bd)) {
                    kernel = list_append_long_long(kernel, &kernel_len, gray_img[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? gray_img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? gray_img_lens[(int)(x)] + _mochi_idx : _mochi_idx;})]);
                    y = y + 1LL;
                }
                x = x + 1LL;
            }
            kernel = insertion_sort(kernel, kernel_len);
            kernel_len = insertion_sort_len;
            long long idx = (mask * mask) / 2LL;
            result[(int)(i)][(int)(j)] = kernel[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? kernel_len + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return median_filter_lens = result_lens, median_filter_len = result_len, result;
}

void user_main() {
    long long img_0[5] = {10LL, 10LL, 10LL, 10LL, 10LL};
    long long img_1[5] = {10LL, 255LL, 10LL, 255LL, 10LL};
    long long img_2[5] = {10LL, 10LL, 10LL, 10LL, 10LL};
    long long img_3[5] = {10LL, 255LL, 10LL, 255LL, 10LL};
    long long img_4[5] = {10LL, 10LL, 10LL, 10LL, 10LL};
    long long *img_init[5] = {img_0, img_1, img_2, img_3, img_4};
    long long **img = img_init;
    size_t img_len = 5;
    size_t img_lens_init[5] = {5, 5, 5, 5, 5};
    size_t *img_lens = img_lens_init;
    size_t img_lens_len = 5;
    long long **filtered = median_filter(img, img_len, img_lens, img_len, 3LL);
    size_t filtered_len = median_filter_len;
    size_t *filtered_lens = median_filter_lens;
    size_t filtered_lens_len = median_filter_len;
    puts(str_list_list_int(filtered, filtered_len, filtered_lens));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
