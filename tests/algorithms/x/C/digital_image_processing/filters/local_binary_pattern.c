// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **image = NULL;
size_t image_len = 0;
size_t *image_lens = NULL;
size_t image_lens_len = 0;
long long i = 0LL;

long long get_neighbors_pixel(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long x, long long y, long long center);
long long local_binary_value(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long x, long long y);
int main(void);

long long get_neighbors_pixel(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long x, long long y, long long center) {
    if ((x < 0LL) || (y < 0LL)) {
        return 0LL;
    }
    if ((x >= image_len) || (y >= image_lens[(int)(0LL)])) {
        return 0LL;
    }
    if (image[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? image_lens[(int)(x)] + _mochi_idx : _mochi_idx;})] >= center) {
        return 1LL;
    }
    return 0LL;
}

long long local_binary_value(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long x, long long y) {
    long long center = image[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? image_lens[(int)(x)] + _mochi_idx : _mochi_idx;})];
    long long *powers = NULL;
    size_t powers_len = 0;
    powers = list_append_long_long(powers, &powers_len, 1LL);
    powers = list_append_long_long(powers, &powers_len, 2LL);
    powers = list_append_long_long(powers, &powers_len, 4LL);
    powers = list_append_long_long(powers, &powers_len, 8LL);
    powers = list_append_long_long(powers, &powers_len, 16LL);
    powers = list_append_long_long(powers, &powers_len, 32LL);
    powers = list_append_long_long(powers, &powers_len, 64LL);
    powers = list_append_long_long(powers, &powers_len, 128LL);
    long long *neighbors = NULL;
    size_t neighbors_len = 0;
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x - 1LL, y + 1LL, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x, y + 1LL, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x - 1LL, y, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x + 1LL, y + 1LL, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x + 1LL, y, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x + 1LL, y - 1LL, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x, y - 1LL, center));
    neighbors = list_append_long_long(neighbors, &neighbors_len, get_neighbors_pixel(image, image_len, image_lens, image_len, x - 1LL, y - 1LL, center));
    long long sum = 0LL;
    long long i = 0LL;
    while (i < 8LL) {
        sum = sum + (neighbors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})] * powers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? powers_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return sum;
}

int main(void) {
    {
        long long __start = _now();
        image = malloc(5 * sizeof(long long*));
        image_len = 5;
        image[0] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 10LL; tmp[2] = 10LL; tmp[3] = 10LL; tmp[4] = 10LL; tmp;});
        image[1] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 20LL; tmp[2] = 30LL; tmp[3] = 20LL; tmp[4] = 10LL; tmp;});
        image[2] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 30LL; tmp[2] = 40LL; tmp[3] = 30LL; tmp[4] = 10LL; tmp;});
        image[3] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 20LL; tmp[2] = 30LL; tmp[3] = 20LL; tmp[4] = 10LL; tmp;});
        image[4] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 10LL; tmp[1] = 10LL; tmp[2] = 10LL; tmp[3] = 10LL; tmp[4] = 10LL; tmp;});
        image_lens = ({size_t *tmp = malloc(5 * sizeof(size_t)); tmp[0] = 5; tmp[1] = 5; tmp[2] = 5; tmp[3] = 5; tmp[4] = 5; tmp;});
        image_lens_len = 5;
        while (i < image_len) {
            long long j = 0LL;
            const char* line = "";
            while (j < image_lens[(int)(0LL)]) {
                long long value = local_binary_value(image, image_len, image_lens, image_len, i, j);
                line = str_concat(line, str_int(value));
                if (j < (image_lens[(int)(0LL)] - 1LL)) {
                    line = str_concat(line, " ");
                }
                j = j + 1LL;
            }
            puts(line);
            i = i + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
