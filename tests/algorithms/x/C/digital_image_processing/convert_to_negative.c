// Generated by Mochi 0.10.32 on 2025-08-25 16:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t convert_to_negative_len;
size_t *convert_to_negative_lens;

size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long*** list_append_intptrptr(long long ***arr, size_t *len, long long **val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long*** list_append_long_longptrptr(long long* **arr, size_t *len, long long* *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * * convert_to_negative(long long * * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
void user_main();
int main(void);

long long * * * convert_to_negative(long long * * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long ***result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    size_t **result_lens_lens = NULL;
    size_t result_lens_lens_len = 0;
    long long i = 0LL;
    while (i < img_len) {
        long long **row = NULL;
        size_t row_len = 0;
        size_t *row_lens = NULL;
        size_t row_lens_len = 0;
        long long j = 0LL;
        while (j < img_lens[(int)(i)]) {
            long long *pixel = img[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? img_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
            size_t pixel_len = 0;
            long long r = 255LL - pixel[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pixel_len + _mochi_idx : _mochi_idx;})];
            long long g = 255LL - pixel[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pixel_len + _mochi_idx : _mochi_idx;})];
            long long b = 255LL - pixel[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? pixel_len + _mochi_idx : _mochi_idx;})];
            row = list_append_intptr(row, &row_len, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = r; tmp[1] = g; tmp[2] = b; tmp;}));
            row_lens = list_append_szt(row_lens, &row_lens_len, 3);
            j = j + 1LL;
        }
        result = list_append_long_longptrptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return convert_to_negative_lens = result_lens, convert_to_negative_len = result_len, result;
}

void user_main() {
    long long image_0_0[3] = {10LL, 20LL, 30LL};
    long long image_0_1[3] = {0LL, 0LL, 0LL};
    long long *image_0[2] = {image_0_0, image_0_1};
    size_t image_0_lens[2] = {3, 3};
    long long image_1_0[3] = {255LL, 255LL, 255LL};
    long long image_1_1[3] = {100LL, 150LL, 200LL};
    long long *image_1[2] = {image_1_0, image_1_1};
    size_t image_1_lens[2] = {3, 3};
    long long **image_init[2] = {image_0, image_1};
    long long ***image = image_init;
    size_t image_len = 2;
    size_t image_lens_init[2] = {2, 2};
    size_t *image_lens = image_lens_init;
    size_t image_lens_len = 2;
    size_t *image_lens_lens_init[2] = {image_0_lens, image_1_lens};
    size_t **image_lens_lens = image_lens_lens_init;
    size_t image_lens_lens_len = 2;
    long long ***neg = NULL;
    size_t neg_len = 0;
    size_t *neg_lens = NULL;
    size_t neg_lens_len = 0;
    size_t **neg_lens_lens = NULL;
    size_t neg_lens_lens_len = 0;
    printf("%d\n", neg);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
