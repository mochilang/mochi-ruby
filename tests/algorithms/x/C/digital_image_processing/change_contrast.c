// Generated by Mochi 0.10.32 on 2025-08-25 16:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t change_contrast_len;
size_t *change_contrast_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **image = NULL;
size_t image_len = 0;
size_t *image_lens = NULL;
size_t image_lens_len = 0;
long long **contrasted = NULL;
size_t contrasted_len = 0;
size_t *contrasted_lens = NULL;
size_t contrasted_lens_len = 0;

long long * * change_contrast(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long level);
void print_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
int main(void);

long long * * change_contrast(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long level) {
    double factor = (259.0 * ((double)(level) + 255.0)) / (255.0 * (259.0 - (double)(level)));
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < img_len) {
        long long *row = img[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = img_lens[(int)(i)];
        long long *new_row = NULL;
        size_t new_row_len = 0;
        long long j = 0LL;
        while (j < row_len) {
            long long c = row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})];
            long long contrasted = 128.0 + (factor * ((double)(c) - 128.0));
            long long clamped;
            if (contrasted < 0LL) {
                clamped = 0LL;
            } else {
                clamped = (contrasted > 255LL ? 255LL : contrasted);
            }
            new_row = list_append_long_long(new_row, &new_row_len, clamped);
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, new_row);
        result_lens = list_append_szt(result_lens, &result_lens_len, new_row_len);
        i = i + 1LL;
    }
    return change_contrast_lens = result_lens, change_contrast_len = result_len, result;
}

void print_image(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long i = 0LL;
    while (i < img_len) {
        long long *row = img[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = img_lens[(int)(i)];
        long long j = 0LL;
        const char* line = "";
        while (j < row_len) {
            line = str_concat(str_concat(line, str_int(row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})])), " ");
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        image = malloc(3 * sizeof(long long*));
        image_len = 3;
        image[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 100LL; tmp[1] = 125LL; tmp[2] = 150LL; tmp;});
        image[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 175LL; tmp[1] = 200LL; tmp[2] = 225LL; tmp;});
        image[2] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 50LL; tmp[1] = 75LL; tmp[2] = 100LL; tmp;});
        image_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;});
        image_lens_len = 3;
        puts("Original image:");
        print_image(image, image_len, image_lens, image_len);
        contrasted = change_contrast(image, image_len, image_lens, image_len, 170LL);
        contrasted_len = change_contrast_len;
        contrasted_lens = change_contrast_lens;
        puts("After contrast:");
        print_image(contrasted, contrasted_len, contrasted_lens, contrasted_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
