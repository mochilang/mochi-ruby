// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t convert_to_negative_len;
size_t *convert_to_negative_lens;
size_t change_contrast_len;
size_t *change_contrast_lens;
size_t gen_gaussian_kernel_len;
size_t *gen_gaussian_kernel_lens;
size_t img_convolve_len;
size_t *img_convolve_lens;
size_t sort_ints_len;
size_t median_filter_len;
size_t *median_filter_lens;
size_t sobel_filter_len;
size_t *sobel_filter_lens;
size_t get_neighbors_pixel_len;
size_t local_binary_pattern_len;
size_t *local_binary_pattern_lens;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **img = NULL;
size_t img_len = 0;
size_t *img_lens = NULL;
size_t img_lens_len = 0;
long long **negative = NULL;
size_t negative_len = 0;
size_t *negative_lens = NULL;
size_t negative_lens_len = 0;
long long **contrast = NULL;
size_t contrast_len = 0;
size_t *contrast_lens = NULL;
size_t contrast_lens_len = 0;
double **kernel = NULL;
size_t kernel_len = 0;
size_t *kernel_lens = NULL;
size_t kernel_lens_len = 0;
double **laplace = NULL;
size_t laplace_len = 0;
size_t *laplace_lens = NULL;
size_t laplace_lens_len = 0;
long long **convolved = NULL;
size_t convolved_len = 0;
size_t *convolved_lens = NULL;
size_t convolved_lens_len = 0;
long long **medianed = NULL;
size_t medianed_len = 0;
size_t *medianed_lens = NULL;
size_t medianed_lens_len = 0;
long long **sobel = NULL;
size_t sobel_len = 0;
size_t *sobel_lens = NULL;
size_t sobel_lens_len = 0;
long long **lbp_img = NULL;
size_t lbp_img_len = 0;
size_t *lbp_img_lens = NULL;
size_t lbp_img_lens_len = 0;

long long clamp_byte(long long x);
long long * * convert_to_negative(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
long long * * change_contrast(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long factor);
double * * gen_gaussian_kernel(long long n, double sigma);
long long * * img_convolve(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, double * * kernel, size_t kernel_len, size_t* kernel_lens, size_t kernel_lens_len);
long long * sort_ints(long long * xs, size_t xs_len);
long long * * median_filter(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long k);
long long iabs(long long x);
long long * * sobel_filter(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
long long * get_neighbors_pixel(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long x, long long y);
long long pow2(long long e);
long long local_binary_value(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long x, long long y);
long long * * local_binary_pattern(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len);
int main(void);

long long clamp_byte(long long x) {
    if (x < 0LL) {
        return 0LL;
    }
    if (x > 255LL) {
        return 255LL;
    }
    return x;
}

long long * * convert_to_negative(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < w) {
            row = list_append_long_long(row, &row_len, 255LL - img[(int)({long long _mochi_idx = y; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x; _mochi_idx < 0 ? img_lens[(int)(y)] + _mochi_idx : _mochi_idx;})]);
            x = x + 1LL;
        }
        out = list_append_intptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        y = y + 1LL;
    }
    return convert_to_negative_lens = out_lens, convert_to_negative_len = out_len, out;
}

long long * * change_contrast(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long factor) {
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < w) {
            long long p = img[(int)({long long _mochi_idx = y; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x; _mochi_idx < 0 ? img_lens[(int)(y)] + _mochi_idx : _mochi_idx;})];
            long long v = (((p - 128LL) * factor) / 100LL) + 128LL;
            v = clamp_byte(v);
            row = list_append_long_long(row, &row_len, v);
            x = x + 1LL;
        }
        out = list_append_intptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        y = y + 1LL;
    }
    return change_contrast_lens = out_lens, change_contrast_len = out_len, out;
}

double * * gen_gaussian_kernel(long long n, double sigma) {
    if (n == 3LL) {
        {
            size_t *__tmp0_lens = malloc(3 * sizeof(size_t));
            __tmp0_lens[0] = 3;
            __tmp0_lens[1] = 3;
            __tmp0_lens[2] = 3;
            double** __tmp0_data = malloc(3 * sizeof(double*));
            __tmp0_data[0] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 1.0 / 16.0; tmp[1] = 2.0 / 16.0; tmp[2] = 1.0 / 16.0; tmp;});
            __tmp0_data[1] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 2.0 / 16.0; tmp[1] = 4.0 / 16.0; tmp[2] = 2.0 / 16.0; tmp;});
            __tmp0_data[2] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 1.0 / 16.0; tmp[1] = 2.0 / 16.0; tmp[2] = 1.0 / 16.0; tmp;});
            gen_gaussian_kernel_lens = __tmp0_lens;
            gen_gaussian_kernel_len = 3;
            return __tmp0_data;
        }
    }
    double **k = NULL;
    size_t k_len = 0;
    size_t *k_lens = NULL;
    size_t k_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, 0.0);
            j = j + 1LL;
        }
        k = list_append_doubleptr(k, &k_len, row);
        k_lens = list_append_szt(k_lens, &k_lens_len, row_len);
        i = i + 1LL;
    }
    return gen_gaussian_kernel_lens = k_lens, gen_gaussian_kernel_len = k_len, k;
}

long long * * img_convolve(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, double * * kernel, size_t kernel_len, size_t* kernel_lens, size_t kernel_lens_len) {
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < w) {
            double acc = 0.0;
            long long ky = 0LL;
            while (ky < kernel_len) {
                long long kx = 0LL;
                while (kx < kernel_lens[(int)(0LL)]) {
                    long long iy = (y + ky) - 1LL;
                    long long ix = (x + kx) - 1LL;
                    long long pixel = 0LL;
                    if ((((iy >= 0LL) && (iy < h)) && (ix >= 0LL)) && (ix < w)) {
                        pixel = img[(int)({long long _mochi_idx = iy; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = ix; _mochi_idx < 0 ? img_lens[(int)(iy)] + _mochi_idx : _mochi_idx;})];
                    }
                    acc = acc + (kernel[(int)({long long _mochi_idx = ky; _mochi_idx < 0 ? kernel_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = kx; _mochi_idx < 0 ? kernel_lens[(int)(ky)] + _mochi_idx : _mochi_idx;})] * (1.0 * pixel));
                    kx = kx + 1LL;
                }
                ky = ky + 1LL;
            }
            row = list_append_long_long(row, &row_len, (int)(acc));
            x = x + 1LL;
        }
        out = list_append_intptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        y = y + 1LL;
    }
    return img_convolve_lens = out_lens, img_convolve_len = out_len, out;
}

long long * sort_ints(long long * xs, size_t xs_len) {
    long long *arr = xs;
    size_t arr_len = xs_len;
    long long i = 0LL;
    while (i < arr_len) {
        long long j = 0LL;
        while (j < ((arr_len - 1LL) - i)) {
            if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_ints_len = arr_len, arr;
}

long long * * median_filter(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long k) {
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long offset = k / 2LL;
    long long **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < w) {
            long long *vals = NULL;
            size_t vals_len = 0;
            long long ky = 0LL;
            while (ky < k) {
                long long kx = 0LL;
                while (kx < k) {
                    long long iy = (y + ky) - offset;
                    long long ix = (x + kx) - offset;
                    long long pixel = 0LL;
                    if ((((iy >= 0LL) && (iy < h)) && (ix >= 0LL)) && (ix < w)) {
                        pixel = img[(int)({long long _mochi_idx = iy; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = ix; _mochi_idx < 0 ? img_lens[(int)(iy)] + _mochi_idx : _mochi_idx;})];
                    }
                    vals = list_append_long_long(vals, &vals_len, pixel);
                    kx = kx + 1LL;
                }
                ky = ky + 1LL;
            }
            long long *sorted = sort_ints(vals, vals_len);
            size_t sorted_len = sort_ints_len;
            row = list_append_long_long(row, &row_len, sorted[(int)({long long _mochi_idx = sorted_len / 2LL; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})]);
            x = x + 1LL;
        }
        out = list_append_intptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        y = y + 1LL;
    }
    return median_filter_lens = out_lens, median_filter_len = out_len, out;
}

long long iabs(long long x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long * * sobel_filter(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long gx_0[3] = {1LL, 0LL, -1LL};
    long long gx_1[3] = {2LL, 0LL, -2LL};
    long long gx_2[3] = {1LL, 0LL, -1LL};
    long long *gx_init[3] = {gx_0, gx_1, gx_2};
    long long **gx = gx_init;
    size_t gx_len = 3;
    size_t gx_lens_init[3] = {3, 3, 3};
    size_t *gx_lens = gx_lens_init;
    size_t gx_lens_len = 3;
    long long gy_0[3] = {1LL, 2LL, 1LL};
    long long gy_1[3] = {0LL, 0LL, 0LL};
    long long gy_2[3] = {-1LL, -2LL, -1LL};
    long long *gy_init[3] = {gy_0, gy_1, gy_2};
    long long **gy = gy_init;
    size_t gy_len = 3;
    size_t gy_lens_init[3] = {3, 3, 3};
    size_t *gy_lens = gy_lens_init;
    size_t gy_lens_len = 3;
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < w) {
            long long sx = 0LL;
            long long sy = 0LL;
            long long ky = 0LL;
            while (ky < 3LL) {
                long long kx = 0LL;
                while (kx < 3LL) {
                    long long iy = (y + ky) - 1LL;
                    long long ix = (x + kx) - 1LL;
                    long long pixel = 0LL;
                    if ((((iy >= 0LL) && (iy < h)) && (ix >= 0LL)) && (ix < w)) {
                        pixel = img[(int)({long long _mochi_idx = iy; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = ix; _mochi_idx < 0 ? img_lens[(int)(iy)] + _mochi_idx : _mochi_idx;})];
                    }
                    sx = sx + (gx[(int)({long long _mochi_idx = ky; _mochi_idx < 0 ? gx_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = kx; _mochi_idx < 0 ? gx_lens[(int)(ky)] + _mochi_idx : _mochi_idx;})] * pixel);
                    sy = sy + (gy[(int)({long long _mochi_idx = ky; _mochi_idx < 0 ? gy_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = kx; _mochi_idx < 0 ? gy_lens[(int)(ky)] + _mochi_idx : _mochi_idx;})] * pixel);
                    kx = kx + 1LL;
                }
                ky = ky + 1LL;
            }
            row = list_append_long_long(row, &row_len, iabs(sx) + iabs(sy));
            x = x + 1LL;
        }
        out = list_append_intptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        y = y + 1LL;
    }
    return sobel_filter_lens = out_lens, sobel_filter_len = out_len, out;
}

long long * get_neighbors_pixel(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long x, long long y) {
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long *neighbors = NULL;
    size_t neighbors_len = 0;
    long long dy = -1LL;
    while (dy <= 1LL) {
        long long dx = -1LL;
        while (dx <= 1LL) {
            if (!((dx == 0LL) && (dy == 0LL))) {
                long long ny = y + dy;
                long long nx = x + dx;
                long long val = 0LL;
                if ((((ny >= 0LL) && (ny < h)) && (nx >= 0LL)) && (nx < w)) {
                    val = img[(int)({long long _mochi_idx = ny; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = nx; _mochi_idx < 0 ? img_lens[(int)(ny)] + _mochi_idx : _mochi_idx;})];
                }
                neighbors = list_append_long_long(neighbors, &neighbors_len, val);
            }
            dx = dx + 1LL;
        }
        dy = dy + 1LL;
    }
    return get_neighbors_pixel_len = neighbors_len, neighbors;
}

long long pow2(long long e) {
    long long r = 1LL;
    long long i = 0LL;
    while (i < e) {
        r = r * 2LL;
        i = i + 1LL;
    }
    return r;
}

long long local_binary_value(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len, long long x, long long y) {
    long long center = img[(int)({long long _mochi_idx = y; _mochi_idx < 0 ? img_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x; _mochi_idx < 0 ? img_lens[(int)(y)] + _mochi_idx : _mochi_idx;})];
    long long *neighbors = get_neighbors_pixel(img, img_len, img_lens, img_len, x, y);
    size_t neighbors_len = get_neighbors_pixel_len;
    long long v = 0LL;
    long long i = 0LL;
    while (i < neighbors_len) {
        if (neighbors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})] >= center) {
            v = v + pow2(i);
        }
        i = i + 1LL;
    }
    return v;
}

long long * * local_binary_pattern(long long * * img, size_t img_len, size_t* img_lens, size_t img_lens_len) {
    long long h = img_len;
    long long w = img_lens[(int)(0LL)];
    long long **out = NULL;
    size_t out_len = 0;
    size_t *out_lens = NULL;
    size_t out_lens_len = 0;
    long long y = 0LL;
    while (y < h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < w) {
            row = list_append_long_long(row, &row_len, local_binary_value(img, img_len, img_lens, img_len, x, y));
            x = x + 1LL;
        }
        out = list_append_intptr(out, &out_len, row);
        out_lens = list_append_szt(out_lens, &out_lens_len, row_len);
        y = y + 1LL;
    }
    return local_binary_pattern_lens = out_lens, local_binary_pattern_len = out_len, out;
}

int main(void) {
    {
        long long __start = _now();
        img = malloc(3 * sizeof(long long*));
        img_len = 3;
        img[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 52LL; tmp[1] = 55LL; tmp[2] = 61LL; tmp;});
        img[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 62LL; tmp[1] = 59LL; tmp[2] = 55LL; tmp;});
        img[2] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 63LL; tmp[1] = 65LL; tmp[2] = 66LL; tmp;});
        img_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;});
        img_lens_len = 3;
        negative = convert_to_negative(img, img_len, img_lens, img_len);
        negative_len = convert_to_negative_len;
        negative_lens = convert_to_negative_lens;
        contrast = change_contrast(img, img_len, img_lens, img_len, 110LL);
        contrast_len = change_contrast_len;
        contrast_lens = change_contrast_lens;
        kernel = gen_gaussian_kernel(3LL, 1.0);
        kernel_len = gen_gaussian_kernel_len;
        kernel_lens = gen_gaussian_kernel_lens;
        laplace = malloc(3 * sizeof(double*));
        laplace_len = 3;
        laplace[0] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 0.25; tmp[1] = 0.5; tmp[2] = 0.25; tmp;});
        laplace[1] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 0.5; tmp[1] = -3.0; tmp[2] = 0.5; tmp;});
        laplace[2] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 0.25; tmp[1] = 0.5; tmp[2] = 0.25; tmp;});
        laplace_lens = ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;});
        laplace_lens_len = 3;
        convolved = img_convolve(img, img_len, img_lens, img_len, laplace, laplace_len, laplace_lens, laplace_len);
        convolved_len = img_convolve_len;
        convolved_lens = img_convolve_lens;
        medianed = median_filter(img, img_len, img_lens, img_len, 3LL);
        medianed_len = median_filter_len;
        medianed_lens = median_filter_lens;
        sobel = sobel_filter(img, img_len, img_lens, img_len);
        sobel_len = sobel_filter_len;
        sobel_lens = sobel_filter_lens;
        lbp_img = local_binary_pattern(img, img_len, img_lens, img_len);
        lbp_img_len = local_binary_pattern_len;
        lbp_img_lens = local_binary_pattern_lens;
        puts(str_list_list_int(negative, negative_len, negative_lens));
        puts(str_list_list_int(contrast, contrast_len, contrast_lens));
        puts(str_list_list_double(kernel, kernel_len, kernel_lens));
        puts(str_list_list_int(convolved, convolved_len, convolved_lens));
        puts(str_list_list_int(medianed, medianed_len, medianed_lens));
        puts(str_list_list_int(sobel, sobel_len, sobel_lens));
        puts(str_list_list_int(lbp_img, lbp_img_len, lbp_img_lens));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
