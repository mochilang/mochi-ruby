// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double ndvi(double red, double nir);
double bndvi(double blue, double nir);
double gndvi(double green, double nir);
double ndre(double redEdge, double nir);
double ccci(double red, double redEdge, double nir);
double cvi(double red, double green, double nir);
double gli(double red, double green, double blue);
double dvi(double red, double nir);
double calc(const char* index, double red, double green, double blue, double redEdge, double nir);
void user_main();
int main(void);

double ndvi(double red, double nir) {
    return (nir - red) / (nir + red);
}

double bndvi(double blue, double nir) {
    return (nir - blue) / (nir + blue);
}

double gndvi(double green, double nir) {
    return (nir - green) / (nir + green);
}

double ndre(double redEdge, double nir) {
    return (nir - redEdge) / (nir + redEdge);
}

double ccci(double red, double redEdge, double nir) {
    return ndre(redEdge, nir) / ndvi(red, nir);
}

double cvi(double red, double green, double nir) {
    return (nir * red) / (green * green);
}

double gli(double red, double green, double blue) {
    return (((2.0 * green) - red) - blue) / (((2.0 * green) + red) + blue);
}

double dvi(double red, double nir) {
    return nir / red;
}

double calc(const char* index, double red, double green, double blue, double redEdge, double nir) {
    if (strcmp(index, "NDVI") == 0) {
        return ndvi(red, nir);
    }
    if (strcmp(index, "BNDVI") == 0) {
        return bndvi(blue, nir);
    }
    if (strcmp(index, "GNDVI") == 0) {
        return gndvi(green, nir);
    }
    if (strcmp(index, "NDRE") == 0) {
        return ndre(redEdge, nir);
    }
    if (strcmp(index, "CCCI") == 0) {
        return ccci(red, redEdge, nir);
    }
    if (strcmp(index, "CVI") == 0) {
        return cvi(red, green, nir);
    }
    if (strcmp(index, "GLI") == 0) {
        return gli(red, green, blue);
    }
    if (strcmp(index, "DVI") == 0) {
        return dvi(red, nir);
    }
    return 0.0;
}

void user_main() {
    double red = 50.0;
    double green = 30.0;
    double blue = 10.0;
    double redEdge = 40.0;
    double nir = 100.0;
    puts(str_concat("NDVI=", str_float(ndvi(red, nir))));
    puts(str_concat("CCCI=", str_float(ccci(red, redEdge, nir))));
    puts(str_concat("CVI=", str_float(cvi(red, green, nir))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
