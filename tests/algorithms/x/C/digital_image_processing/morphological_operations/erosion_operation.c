// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t rgb_to_gray_len;
size_t *rgb_to_gray_lens;
size_t gray_to_binary_len;
size_t *gray_to_binary_lens;
size_t erosion_len;
size_t *erosion_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long ***rgb_img = NULL;
size_t rgb_img_len = 0;
size_t *rgb_img_lens = NULL;
size_t rgb_img_lens_len = 0;
size_t **rgb_img_lens_lens = NULL;
size_t rgb_img_lens_lens_len = 0;
long long **gray_img = NULL;
size_t gray_img_len = 0;
size_t *gray_img_lens = NULL;
size_t gray_img_lens_len = 0;
long long **img1 = NULL;
size_t img1_len = 0;
size_t *img1_lens = NULL;
size_t img1_lens_len = 0;
long long **kernel1 = NULL;
size_t kernel1_len = 0;
size_t *kernel1_lens = NULL;
size_t kernel1_lens_len = 0;
long long **img2 = NULL;
size_t img2_len = 0;
size_t *img2_lens = NULL;
size_t img2_lens_len = 0;
long long **kernel2 = NULL;
size_t kernel2_len = 0;
size_t *kernel2_lens = NULL;
size_t kernel2_lens_len = 0;

double * * rgb_to_gray(long long * * * rgb, size_t rgb_len, size_t* rgb_lens, size_t rgb_lens_len);
long long * * gray_to_binary(double * * gray, size_t gray_len, size_t* gray_lens, size_t gray_lens_len);
long long * * erosion(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long * * kernel, size_t kernel_len, size_t* kernel_lens, size_t kernel_lens_len);
int main(void);

double * * rgb_to_gray(long long * * * rgb, size_t rgb_len, size_t* rgb_lens, size_t rgb_lens_len) {
    double **gray = NULL;
    size_t gray_len = 0;
    size_t *gray_lens = NULL;
    size_t gray_lens_len = 0;
    long long i = 0LL;
    while (i < rgb_len) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < rgb_lens[(int)(i)]) {
            double r = (double)(rgb[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rgb_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rgb_lens[(int)(i)] + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? 0 + _mochi_idx : _mochi_idx;})]);
            double g = (double)(rgb[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rgb_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rgb_lens[(int)(i)] + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? 0 + _mochi_idx : _mochi_idx;})]);
            double b = (double)(rgb[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rgb_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rgb_lens[(int)(i)] + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? 0 + _mochi_idx : _mochi_idx;})]);
            double value = ((0.2989 * r) + (0.587 * g)) + (0.114 * b);
            row = list_append_double(row, &row_len, value);
            j = j + 1LL;
        }
        gray = list_append_doubleptr(gray, &gray_len, row);
        gray_lens = list_append_szt(gray_lens, &gray_lens_len, row_len);
        i = i + 1LL;
    }
    return rgb_to_gray_lens = gray_lens, rgb_to_gray_len = gray_len, gray;
}

long long * * gray_to_binary(double * * gray, size_t gray_len, size_t* gray_lens, size_t gray_lens_len) {
    long long **binary = NULL;
    size_t binary_len = 0;
    size_t *binary_lens = NULL;
    size_t binary_lens_len = 0;
    long long i = 0LL;
    while (i < gray_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < gray_lens[(int)(i)]) {
            row = list_append_long_long(row, &row_len, (gray[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gray_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? gray_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] > 127.0) && (gray[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gray_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? gray_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] <= 255.0));
            j = j + 1LL;
        }
        binary = list_append_intptr(binary, &binary_len, row);
        binary_lens = list_append_szt(binary_lens, &binary_lens_len, row_len);
        i = i + 1LL;
    }
    return gray_to_binary_lens = binary_lens, gray_to_binary_len = binary_len, binary;
}

long long * * erosion(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long * * kernel, size_t kernel_len, size_t* kernel_lens, size_t kernel_lens_len) {
    long long h = image_len;
    long long w = image_lens[(int)(0LL)];
    long long k_h = kernel_len;
    long long k_w = kernel_lens[(int)(0LL)];
    long long pad_y = k_h / 2LL;
    long long pad_x = k_w / 2LL;
    long long **padded = NULL;
    size_t padded_len = 0;
    size_t *padded_lens = NULL;
    size_t padded_lens_len = 0;
    long long y = 0LL;
    while (y < (h + (2LL * pad_y))) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < (w + (2LL * pad_x))) {
            row = list_append_long_long(row, &row_len, 0LL);
            x = x + 1LL;
        }
        padded = list_append_intptr(padded, &padded_len, row);
        padded_lens = list_append_szt(padded_lens, &padded_lens_len, row_len);
        y = y + 1LL;
    }
    y = 0LL;
    while (y < h) {
        long long x = 0LL;
        while (x < w) {
            padded[(int)(pad_y + y)][(int)(pad_x + x)] = image[(int)({long long _mochi_idx = y; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x; _mochi_idx < 0 ? image_lens[(int)(y)] + _mochi_idx : _mochi_idx;})];
            x = x + 1LL;
        }
        y = y + 1LL;
    }
    long long **output = NULL;
    size_t output_len = 0;
    size_t *output_lens = NULL;
    size_t output_lens_len = 0;
    y = 0LL;
    while (y < h) {
        long long *row_out = NULL;
        size_t row_out_len = 0;
        long long x = 0LL;
        while (x < w) {
            long long sum = 0LL;
            long long ky = 0LL;
            while (ky < k_h) {
                long long kx = 0LL;
                while (kx < k_w) {
                    if ((kernel[(int)({long long _mochi_idx = ky; _mochi_idx < 0 ? kernel_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = kx; _mochi_idx < 0 ? kernel_lens[(int)(ky)] + _mochi_idx : _mochi_idx;})] == 1LL) && padded[(int)({long long _mochi_idx = y + ky; _mochi_idx < 0 ? padded_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = x + kx; _mochi_idx < 0 ? padded_lens[(int)(y + ky)] + _mochi_idx : _mochi_idx;})]) {
                        sum = sum + 1LL;
                    }
                    kx = kx + 1LL;
                }
                ky = ky + 1LL;
            }
            row_out = list_append_long_long(row_out, &row_out_len, sum == 5LL);
            x = x + 1LL;
        }
        output = list_append_intptr(output, &output_len, row_out);
        output_lens = list_append_szt(output_lens, &output_lens_len, row_out_len);
        y = y + 1LL;
    }
    return erosion_lens = output_lens, erosion_len = output_len, output;
}

int main(void) {
    {
        long long __start = _now();
        rgb_img = malloc(1 * sizeof(long long**));
        rgb_img_len = 1;
        rgb_img[0] = ({long long** tmp = malloc(1 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 127LL; tmp[1] = 255LL; tmp[2] = 0LL; tmp;}); tmp;});
        rgb_img_lens = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 1; tmp;});
        rgb_img_lens_len = 1;
        puts(({double **__tmp4 = rgb_to_gray(rgb_img, rgb_img_len, rgb_img_lens, rgb_img_len); char *__res = str_list_list_double(__tmp4, rgb_to_gray_len, rgb_to_gray_lens); __res;}));
        gray_img = malloc(1 * sizeof(long long*));
        gray_img_len = 1;
        gray_img[0] = ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 127.0; tmp[1] = 255.0; tmp[2] = 0.0; tmp;});
        gray_img_lens = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 3; tmp;});
        gray_img_lens_len = 1;
        puts(({long long **__tmp5 = gray_to_binary(gray_img, gray_img_len, gray_img_lens, gray_img_len); char *__res = str_list_list_int(__tmp5, gray_to_binary_len, gray_to_binary_lens); __res;}));
        img1 = malloc(1 * sizeof(long long*));
        img1_len = 1;
        img1[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = 0LL; tmp;});
        img1_lens = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 3; tmp;});
        img1_lens_len = 1;
        kernel1 = malloc(1 * sizeof(long long*));
        kernel1_len = 1;
        kernel1[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 1LL; tmp[2] = 0LL; tmp;});
        kernel1_lens = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 3; tmp;});
        kernel1_lens_len = 1;
        puts(({long long **__tmp6 = erosion(img1, img1_len, img1_lens, img1_len, kernel1, kernel1_len, kernel1_lens, kernel1_len); char *__res = str_list_list_int(__tmp6, erosion_len, erosion_lens); __res;}));
        img2 = malloc(1 * sizeof(long long*));
        img2_len = 1;
        img2[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp;});
        img2_lens = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 3; tmp;});
        img2_lens_len = 1;
        kernel2 = malloc(1 * sizeof(long long*));
        kernel2_len = 1;
        kernel2[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = 0LL; tmp;});
        kernel2_lens = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 3; tmp;});
        kernel2_lens_len = 1;
        puts(({long long **__tmp7 = erosion(img2, img2_len, img2_lens, img2_len, kernel2, kernel2_len, kernel2_lens, kernel2_len); char *__res = str_list_list_int(__tmp7, erosion_len, erosion_lens); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
