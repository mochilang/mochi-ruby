// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t rgb_to_gray_len;
size_t *rgb_to_gray_lens;
size_t gray_to_binary_len;
size_t *gray_to_binary_lens;
size_t dilation_len;
size_t *dilation_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double * * rgb_to_gray(long long * * * rgb, size_t rgb_len, size_t* rgb_lens, size_t rgb_lens_len);
long long * * gray_to_binary(double * * gray, size_t gray_len, size_t* gray_lens, size_t gray_lens_len);
long long * * dilation(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long * * kernel, size_t kernel_len, size_t* kernel_lens, size_t kernel_lens_len);
const char* print_float_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
const char* print_int_matrix(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
void user_main();
int main(void);

double * * rgb_to_gray(long long * * * rgb, size_t rgb_len, size_t* rgb_lens, size_t rgb_lens_len) {
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rgb_len) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < rgb_lens[(int)(i)]) {
            long long r = rgb[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rgb_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rgb_lens[(int)(i)] + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? 0 + _mochi_idx : _mochi_idx;})];
            long long g = rgb[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rgb_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rgb_lens[(int)(i)] + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? 0 + _mochi_idx : _mochi_idx;})];
            long long b = rgb[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rgb_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? rgb_lens[(int)(i)] + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? 0 + _mochi_idx : _mochi_idx;})];
            double gray = ((0.2989 * (1.0 * r)) + (0.587 * (1.0 * g))) + (0.114 * (1.0 * b));
            row = list_append_double(row, &row_len, gray);
            j = j + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return rgb_to_gray_lens = result_lens, rgb_to_gray_len = result_len, result;
}

long long * * gray_to_binary(double * * gray, size_t gray_len, size_t* gray_lens, size_t gray_lens_len) {
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < gray_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < gray_lens[(int)(i)]) {
            double v = gray[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gray_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? gray_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
            if ((v > 127.0) && (v <= 255.0)) {
                row = list_append_long_long(row, &row_len, 1LL);
            } else {
                row = list_append_long_long(row, &row_len, 0LL);
            }
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return gray_to_binary_lens = result_lens, gray_to_binary_len = result_len, result;
}

long long * * dilation(long long * * image, size_t image_len, size_t* image_lens, size_t image_lens_len, long long * * kernel, size_t kernel_len, size_t* kernel_lens, size_t kernel_lens_len) {
    long long img_h = image_len;
    long long img_w = image_lens[(int)(0LL)];
    long long k_h = kernel_len;
    long long k_w = kernel_lens[(int)(0LL)];
    long long pad_h = k_h / 2LL;
    long long pad_w = k_w / 2LL;
    long long p_h = img_h + (2LL * pad_h);
    long long p_w = img_w + (2LL * pad_w);
    long long **padded = NULL;
    size_t padded_len = 0;
    size_t *padded_lens = NULL;
    size_t padded_lens_len = 0;
    long long i = 0LL;
    while (i < p_h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < p_w) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        padded = list_append_intptr(padded, &padded_len, row);
        padded_lens = list_append_szt(padded_lens, &padded_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < img_h) {
        long long j = 0LL;
        while (j < img_w) {
            padded[(int)(pad_h + i)][(int)(pad_w + j)] = image[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? image_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? image_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    long long **output = NULL;
    size_t output_len = 0;
    size_t *output_lens = NULL;
    size_t output_lens_len = 0;
    i = 0LL;
    while (i < img_h) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < img_w) {
            long long sum = 0LL;
            long long ky = 0LL;
            while (ky < k_h) {
                long long kx = 0LL;
                while (kx < k_w) {
                    if (kernel[(int)({long long _mochi_idx = ky; _mochi_idx < 0 ? kernel_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = kx; _mochi_idx < 0 ? kernel_lens[(int)(ky)] + _mochi_idx : _mochi_idx;})] == 1LL) {
                        sum = sum + padded[(int)({long long _mochi_idx = i + ky; _mochi_idx < 0 ? padded_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j + kx; _mochi_idx < 0 ? padded_lens[(int)(i + ky)] + _mochi_idx : _mochi_idx;})];
                    }
                    kx = kx + 1LL;
                }
                ky = ky + 1LL;
            }
            if (sum > 0LL) {
                row = list_append_long_long(row, &row_len, 1LL);
            } else {
                row = list_append_long_long(row, &row_len, 0LL);
            }
            j = j + 1LL;
        }
        output = list_append_intptr(output, &output_len, row);
        output_lens = list_append_szt(output_lens, &output_lens_len, row_len);
        i = i + 1LL;
    }
    return dilation_lens = output_lens, dilation_len = output_len, output;
}

const char* print_float_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long i = 0LL;
    while (i < mat_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < mat_lens[(int)(i)]) {
            line = str_concat(line, str_float(mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]));
            if (j < (mat_lens[(int)(i)] - 1LL)) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

const char* print_int_matrix(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long i = 0LL;
    while (i < mat_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < mat_lens[(int)(i)]) {
            line = str_concat(line, str_int(mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]));
            if (j < (mat_lens[(int)(i)] - 1LL)) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

void user_main() {
    long long rgb_example_0_0[3] = {127LL, 255LL, 0LL};
    long long *rgb_example_0[1] = {rgb_example_0_0};
    size_t rgb_example_0_lens[1] = {3};
    long long **rgb_example_init[1] = {rgb_example_0};
    long long ***rgb_example = rgb_example_init;
    size_t rgb_example_len = 1;
    size_t rgb_example_lens_init[1] = {1};
    size_t *rgb_example_lens = rgb_example_lens_init;
    size_t rgb_example_lens_len = 1;
    size_t *rgb_example_lens_lens_init[1] = {rgb_example_0_lens};
    size_t **rgb_example_lens_lens = rgb_example_lens_lens_init;
    size_t rgb_example_lens_lens_len = 1;
    double** __tmp3 = rgb_to_gray(rgb_example, rgb_example_len, rgb_example_lens, rgb_example_len);
    print_float_matrix(__tmp3, rgb_to_gray_len, rgb_to_gray_lens, rgb_to_gray_len);
    double gray_example_0[3] = {26.0, 255.0, 14.0};
    double gray_example_1[3] = {5.0, 147.0, 20.0};
    double gray_example_2[3] = {1.0, 200.0, 0.0};
    double *gray_example_init[3] = {gray_example_0, gray_example_1, gray_example_2};
    double **gray_example = gray_example_init;
    size_t gray_example_len = 3;
    size_t gray_example_lens_init[3] = {3, 3, 3};
    size_t *gray_example_lens = gray_example_lens_init;
    size_t gray_example_lens_len = 3;
    long long** __tmp4 = gray_to_binary(gray_example, gray_example_len, gray_example_lens, gray_example_len);
    print_int_matrix(__tmp4, gray_to_binary_len, gray_to_binary_lens, gray_to_binary_len);
    long long binary_image_0[3] = {0LL, 1LL, 0LL};
    long long binary_image_1[3] = {0LL, 1LL, 0LL};
    long long binary_image_2[3] = {0LL, 1LL, 0LL};
    long long *binary_image_init[3] = {binary_image_0, binary_image_1, binary_image_2};
    long long **binary_image = binary_image_init;
    size_t binary_image_len = 3;
    size_t binary_image_lens_init[3] = {3, 3, 3};
    size_t *binary_image_lens = binary_image_lens_init;
    size_t binary_image_lens_len = 3;
    long long kernel_0[3] = {0LL, 1LL, 0LL};
    long long kernel_1[3] = {1LL, 1LL, 1LL};
    long long kernel_2[3] = {0LL, 1LL, 0LL};
    long long *kernel_init[3] = {kernel_0, kernel_1, kernel_2};
    long long **kernel = kernel_init;
    size_t kernel_len = 3;
    size_t kernel_lens_init[3] = {3, 3, 3};
    size_t *kernel_lens = kernel_lens_init;
    size_t kernel_lens_len = 3;
    long long** __tmp5 = dilation(binary_image, binary_image_len, binary_image_lens, binary_image_len, kernel, kernel_len, kernel_lens, kernel_len);
    print_int_matrix(__tmp5, dilation_len, dilation_lens, dilation_len);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
