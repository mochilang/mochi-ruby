// Generated by Mochi 0.10.32 on 2025-08-07 10:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long gcd(long long a, long long b);
long long find_mod_inverse(long long a, long long m);
int main(void);

long long gcd(long long a, long long b) {
    long long x = (a < 0LL ? -(a) : a);
    long long y = (b < 0LL ? -(b) : b);
    while (y != 0LL) {
        long long t = x % y;
        x = y;
        y = t;
    }
    return x;
}

long long find_mod_inverse(long long a, long long m) {
    if (gcd(a, m) != 1LL) {
        error(str_concat(str_concat(str_concat(str_concat("mod inverse of ", str_int(a)), " and "), str_int(m)), " does not exist"));
    }
    long long u1 = 1LL;
    long long u2 = 0LL;
    long long u3 = a;
    long long v1 = 0LL;
    long long v2 = 1LL;
    long long v3 = m;
    while (v3 != 0LL) {
        long long q = u3 / v3;
        long long t1 = u1 - (q * v1);
        long long t2 = u2 - (q * v2);
        long long t3 = u3 - (q * v3);
        u1 = v1;
        u2 = v2;
        u3 = v3;
        v1 = t1;
        v2 = t2;
        v3 = t3;
    }
    long long res = u1 % m;
    if (res < 0LL) {
        res = res + m;
    }
    return res;
}

int main(void) {
    puts(str_int(find_mod_inverse(3LL, 11LL)));
    puts(str_int(find_mod_inverse(7LL, 26LL)));
    puts(str_int(find_mod_inverse(11LL, 26LL)));
    puts(str_int(find_mod_inverse(17LL, 43LL)));
    return 0;
}
