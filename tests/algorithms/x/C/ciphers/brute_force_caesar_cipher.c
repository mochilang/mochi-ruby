// Generated by Mochi 0.10.32 on 2025-08-07 10:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

long long index_of(const char* s, const char* ch);
const char* decrypt(const char* message);
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* decrypt(const char* message) {
    for (int key = 0LL; key < strlen(LETTERS); key++) {
        const char* translated = "";
        for (int i = 0LL; i < strlen(message); i++) {
            const char* symbol = _substring(message, i, i + 1LL);
            long long idx = index_of(LETTERS, symbol);
            if (idx != (0LL - 1LL)) {
                long long num = idx - key;
                if (num < 0LL) {
                    num = num + strlen(LETTERS);
                }
                translated = str_concat(translated, _substring(LETTERS, num, num + 1LL));
            } else {
                translated = str_concat(translated, symbol);
            }
        }
        puts(str_concat(str_concat(str_concat("Decryption using Key #", str_int(key)), ": "), translated));
    }
}

int main(void) {
    decrypt("TMDETUX PMDVU");
    return 0;
}
