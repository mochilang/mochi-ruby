// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_blocks_from_text_len;
size_t encrypt_message_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long BYTE_SIZE = 256LL;

long long pow_int(long long base, long long exp);
long long mod_pow(long long base, long long exponent, long long modulus);
long long ord(const char* ch);
const char* chr(long long code);
long long * get_blocks_from_text(const char* message, long long block_size);
const char* get_text_from_blocks(long long * block_ints, size_t block_ints_len, long long message_length, long long block_size);
long long * encrypt_message(const char* message, long long n, long long e, long long block_size);
const char* decrypt_message(long long * blocks, size_t blocks_len, long long message_length, long long n, long long d, long long block_size);
void user_main();
int main(void);

long long pow_int(long long base, long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

long long mod_pow(long long base, long long exponent, long long modulus) {
    long long result = 1LL;
    long long b = base % modulus;
    long long e = exponent;
    while (e > 0LL) {
        if ((e % 2LL) == 1LL) {
            result = (result * b) % modulus;
        }
        e = e / 2LL;
        b = (b * b) % modulus;
    }
    return result;
}

long long ord(const char* ch) {
    if (strcmp(ch, " ") == 0) {
        return 32LL;
    }
    if (strcmp(ch, "a") == 0) {
        return 97LL;
    }
    if (strcmp(ch, "b") == 0) {
        return 98LL;
    }
    if (strcmp(ch, "c") == 0) {
        return 99LL;
    }
    if (strcmp(ch, "d") == 0) {
        return 100LL;
    }
    if (strcmp(ch, "e") == 0) {
        return 101LL;
    }
    if (strcmp(ch, "f") == 0) {
        return 102LL;
    }
    if (strcmp(ch, "g") == 0) {
        return 103LL;
    }
    if (strcmp(ch, "h") == 0) {
        return 104LL;
    }
    if (strcmp(ch, "i") == 0) {
        return 105LL;
    }
    if (strcmp(ch, "j") == 0) {
        return 106LL;
    }
    if (strcmp(ch, "k") == 0) {
        return 107LL;
    }
    if (strcmp(ch, "l") == 0) {
        return 108LL;
    }
    if (strcmp(ch, "m") == 0) {
        return 109LL;
    }
    if (strcmp(ch, "n") == 0) {
        return 110LL;
    }
    if (strcmp(ch, "o") == 0) {
        return 111LL;
    }
    if (strcmp(ch, "p") == 0) {
        return 112LL;
    }
    if (strcmp(ch, "q") == 0) {
        return 113LL;
    }
    if (strcmp(ch, "r") == 0) {
        return 114LL;
    }
    if (strcmp(ch, "s") == 0) {
        return 115LL;
    }
    if (strcmp(ch, "t") == 0) {
        return 116LL;
    }
    if (strcmp(ch, "u") == 0) {
        return 117LL;
    }
    if (strcmp(ch, "v") == 0) {
        return 118LL;
    }
    if (strcmp(ch, "w") == 0) {
        return 119LL;
    }
    if (strcmp(ch, "x") == 0) {
        return 120LL;
    }
    if (strcmp(ch, "y") == 0) {
        return 121LL;
    }
    if (strcmp(ch, "z") == 0) {
        return 122LL;
    }
    return 0LL;
}

const char* chr(long long code) {
    if (code == 32LL) {
        return " ";
    }
    if (code == 97LL) {
        return "a";
    }
    if (code == 98LL) {
        return "b";
    }
    if (code == 99LL) {
        return "c";
    }
    if (code == 100LL) {
        return "d";
    }
    if (code == 101LL) {
        return "e";
    }
    if (code == 102LL) {
        return "f";
    }
    if (code == 103LL) {
        return "g";
    }
    if (code == 104LL) {
        return "h";
    }
    if (code == 105LL) {
        return "i";
    }
    if (code == 106LL) {
        return "j";
    }
    if (code == 107LL) {
        return "k";
    }
    if (code == 108LL) {
        return "l";
    }
    if (code == 109LL) {
        return "m";
    }
    if (code == 110LL) {
        return "n";
    }
    if (code == 111LL) {
        return "o";
    }
    if (code == 112LL) {
        return "p";
    }
    if (code == 113LL) {
        return "q";
    }
    if (code == 114LL) {
        return "r";
    }
    if (code == 115LL) {
        return "s";
    }
    if (code == 116LL) {
        return "t";
    }
    if (code == 117LL) {
        return "u";
    }
    if (code == 118LL) {
        return "v";
    }
    if (code == 119LL) {
        return "w";
    }
    if (code == 120LL) {
        return "x";
    }
    if (code == 121LL) {
        return "y";
    }
    if (code == 122LL) {
        return "z";
    }
    return "";
}

long long * get_blocks_from_text(const char* message, long long block_size) {
    long long *block_ints = NULL;
    size_t block_ints_len = 0;
    long long block_start = 0LL;
    while (block_start < strlen(message)) {
        long long block_int = 0LL;
        long long i = block_start;
        while ((i < (block_start + block_size)) && (i < strlen(message))) {
            block_int = block_int + (ord((const char[]){message[i], 0}) * pow_int(BYTE_SIZE, i - block_start));
            i = i + 1LL;
        }
        block_ints = list_append_int(block_ints, &block_ints_len, block_int);
        block_start = block_start + block_size;
    }
    return get_blocks_from_text_len = block_ints_len, block_ints;
}

const char* get_text_from_blocks(long long * block_ints, size_t block_ints_len, long long message_length, long long block_size) {
    const char* message = "";
    for (size_t __i = 0; __i < block_ints_len; __i++) {
        long long block_int = block_ints[__i];
        long long block = block_int;
        long long i = block_size - 1LL;
        const char* block_message = "";
        while (i >= 0LL) {
            if ((strlen(message) + i) < message_length) {
                long long ascii_number = block / pow_int(BYTE_SIZE, i);
                block = block % pow_int(BYTE_SIZE, i);
                block_message = str_concat(chr(ascii_number), block_message);
            }
            i = i - 1LL;
        }
        message = str_concat(message, block_message);
    }
    return message;
}

long long * encrypt_message(const char* message, long long n, long long e, long long block_size) {
    long long *encrypted = NULL;
    size_t encrypted_len = 0;
    long long *blocks = get_blocks_from_text(message, block_size);
    size_t blocks_len = get_blocks_from_text_len;
    for (size_t __i = 0; __i < blocks_len; __i++) {
        long long block = blocks[__i];
        encrypted = list_append_long_long(encrypted, &encrypted_len, mod_pow(block, e, n));
    }
    return encrypt_message_len = encrypted_len, encrypted;
}

const char* decrypt_message(long long * blocks, size_t blocks_len, long long message_length, long long n, long long d, long long block_size) {
    long long *decrypted_blocks = NULL;
    size_t decrypted_blocks_len = 0;
    for (size_t __i = 0; __i < blocks_len; __i++) {
        long long block = blocks[__i];
        decrypted_blocks = list_append_long_long(decrypted_blocks, &decrypted_blocks_len, mod_pow(block, d, n));
    }
    const char* message = "";
    for (size_t __i = 0; __i < decrypted_blocks_len; __i++) {
        long long num = decrypted_blocks[__i];
        message = str_concat(message, chr(num));
    }
    return message;
}

void user_main() {
    const char* message = "hello world";
    long long n = 3233LL;
    long long e = 17LL;
    long long d = 2753LL;
    long long block_size = 1LL;
    long long *encrypted = encrypt_message(message, n, e, block_size);
    size_t encrypted_len = encrypt_message_len;
    puts(str_list_int(encrypted, encrypted_len));
    const char* decrypted = decrypt_message(encrypted, encrypted_len, strlen(message), n, d, block_size);
    puts(decrypted);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
