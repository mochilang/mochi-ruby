// Generated by Mochi 0.10.32 on 2025-08-07 11:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long mod_pow(long long base, long long exp, long long mod);
long long miller_rabin(long long n, long long allow_probable);
int main(void);

long long mod_pow(long long base, long long exp, long long mod) {
    long long result = 1LL;
    long long b = base % mod;
    long long e = exp;
    while (e > 0LL) {
        if ((e % 2LL) == 1LL) {
            result = (result * b) % mod;
        }
        b = (b * b) % mod;
        e = e / 2LL;
    }
    return result;
}

long long miller_rabin(long long n, long long allow_probable) {
    if (n == 2LL) {
        return 1LL;
    }
    if ((n < 2LL) || ((n % 2LL) == 0LL)) {
        return 0LL;
    }
    if (n > 5LL) {
        long long last = n % 10LL;
        if (!((((last == 1LL) || (last == 3LL)) || (last == 7LL)) || (last == 9LL))) {
            return 0LL;
        }
    }
    long long limit = 3825123056546413051LL;
    if ((n > limit) && !(allow_probable)) {
        panic("Warning: upper bound of deterministic test is exceeded. Pass allow_probable=true to allow probabilistic test.");
    }
    long long *bounds = NULL;
    size_t bounds_len = 0;
    bounds = list_append_int(bounds, &bounds_len, 2047LL);
    bounds = list_append_int(bounds, &bounds_len, 1373653LL);
    bounds = list_append_int(bounds, &bounds_len, 25326001LL);
    bounds = list_append_int(bounds, &bounds_len, 3215031751LL);
    bounds = list_append_int(bounds, &bounds_len, 2152302898747LL);
    bounds = list_append_int(bounds, &bounds_len, 3474749660383LL);
    bounds = list_append_int(bounds, &bounds_len, 341550071728321LL);
    bounds = list_append_int(bounds, &bounds_len, limit);
    long long *primes = NULL;
    size_t primes_len = 0;
    primes = list_append_int(primes, &primes_len, 2LL);
    primes = list_append_int(primes, &primes_len, 3LL);
    primes = list_append_int(primes, &primes_len, 5LL);
    primes = list_append_int(primes, &primes_len, 7LL);
    primes = list_append_int(primes, &primes_len, 11LL);
    primes = list_append_int(primes, &primes_len, 13LL);
    primes = list_append_int(primes, &primes_len, 17LL);
    primes = list_append_int(primes, &primes_len, 19LL);
    long long i = 0LL;
    long long plist_len = 8LL;
    while (i < 8LL) {
        if (n < bounds[(int)(i)]) {
            plist_len = i + 1LL;
            i = 8LL;
        } else {
            i = i + 1LL;
        }
    }
    long long d = n - 1LL;
    long long s = 0LL;
    while ((d % 2LL) == 0LL) {
        d = d / 2LL;
        s = s + 1LL;
    }
    long long j = 0LL;
    while (j < plist_len) {
        long long prime = primes[(int)(j)];
        long long x = mod_pow(prime, d, n);
        long long pr = 0LL;
        if ((x == 1LL) || (x == (n - 1LL))) {
            pr = 1LL;
        } else {
            long long r = 1LL;
            while ((r < s) && !(pr)) {
                x = (x * x) % n;
                if (x == (n - 1LL)) {
                    pr = 1LL;
                }
                r = r + 1LL;
            }
        }
        if (!(pr)) {
            return 0LL;
        }
        j = j + 1LL;
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(miller_rabin(561LL, 0LL)));
        puts(str_int(miller_rabin(563LL, 0LL)));
        puts(str_int(miller_rabin(838201LL, 0LL)));
        puts(str_int(miller_rabin(838207LL, 0LL)));
        puts(str_int(miller_rabin(17316001LL, 0LL)));
        puts(str_int(miller_rabin(17316017LL, 0LL)));
        puts(str_int(miller_rabin(3078386641LL, 0LL)));
        puts(str_int(miller_rabin(3078386653LL, 0LL)));
        puts(str_int(miller_rabin(1713045574801LL, 0LL)));
        puts(str_int(miller_rabin(1713045574819LL, 0LL)));
        puts(str_int(miller_rabin(2779799728307LL, 0LL)));
        puts(str_int(miller_rabin(2779799728327LL, 0LL)));
        puts(str_int(miller_rabin(113850023909441LL, 0LL)));
        puts(str_int(miller_rabin(113850023909527LL, 0LL)));
        puts(str_int(miller_rabin(1275041018848804351LL, 0LL)));
        puts(str_int(miller_rabin(1275041018848804391LL, 0LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
