// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { int *keys; const char **vals; size_t len; size_t cap; } MapIS;

static const char* map_get_is(const int keys[], const char *vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return "";
}

static void map_set_is(MapIS *m, int key, const char *val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc(m->keys, m->cap * sizeof(int));
        m->vals = realloc((void*)m->vals, m->cap * sizeof(char*));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

MapIS bf = (MapIS){0};
size_t bf_len = 0;

const char* encrypt(const char* input_string, long long key);
const char* decrypt(const char* input_string, long long key);
MapIS bruteforce(const char* input_string);
int main(void);

const char* encrypt(const char* input_string, long long key) {
    if (key <= 0LL) {
        panic("Height of grid can't be 0 or negative");
    }
    if ((key == 1LL) || (strlen(input_string) <= key)) {
        return input_string;
    }
    long long lowest = key - 1LL;
    const char* **temp_grid = NULL;
    size_t temp_grid_len = 0;
    size_t *temp_grid_lens = NULL;
    size_t temp_grid_lens_len = 0;
    long long i = 0LL;
    while (i < key) {
        temp_grid = list_append_long_longptr(temp_grid, &temp_grid_len, ({long long *tmp = malloc(0 * sizeof(long long)); tmp;}));
        temp_grid_lens = list_append_szt(temp_grid_lens, &temp_grid_lens_len, 0);
        i = i + 1LL;
    }
    long long position = 0LL;
    while (position < strlen(input_string)) {
        long long num = position % (lowest * 2LL);
        long long alt = (lowest * 2LL) - num;
        if (num > alt) {
            num = alt;
        }
        const char* *row = temp_grid[(int)(num)];
        size_t row_len = temp_grid_lens[num];
        row = list_append_str(row, &row_len, _substring(input_string, position, position + 1LL));
        temp_grid[(int)(num)] = row;
        position = position + 1LL;
    }
    const char* output = "";
    i = 0LL;
    while (i < key) {
        const char* *row = temp_grid[(int)(i)];
        size_t row_len = temp_grid_lens[i];
        long long j = 0LL;
        while (j < row_len) {
            output = str_concat(output, row[(int)(j)]);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return output;
}

const char* decrypt(const char* input_string, long long key) {
    if (key <= 0LL) {
        panic("Height of grid can't be 0 or negative");
    }
    if (key == 1LL) {
        return input_string;
    }
    long long lowest = key - 1LL;
    long long *counts = NULL;
    size_t counts_len = 0;
    long long i = 0LL;
    while (i < key) {
        counts = list_append_long_long(counts, &counts_len, 0LL);
        i = i + 1LL;
    }
    long long pos = 0LL;
    while (pos < strlen(input_string)) {
        long long num = pos % (lowest * 2LL);
        long long alt = (lowest * 2LL) - num;
        if (num > alt) {
            num = alt;
        }
        counts[(int)(num)] = counts[(int)(num)] + 1LL;
        pos = pos + 1LL;
    }
    const char* **grid = NULL;
    size_t grid_len = 0;
    size_t *grid_lens = NULL;
    size_t grid_lens_len = 0;
    long long counter = 0LL;
    i = 0LL;
    while (i < key) {
        long long length = counts[(int)(i)];
        const char* slice = _substring(input_string, counter, counter + length);
        const char* *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < strlen(slice)) {
            row = list_append_str(row, &row_len, (const char[]){slice[j], 0});
            j = j + 1LL;
        }
        grid = list_append_strptr(grid, &grid_len, row);
        grid_lens = list_append_szt(grid_lens, &grid_lens_len, row_len);
        counter = counter + length;
        i = i + 1LL;
    }
    long long *indices = NULL;
    size_t indices_len = 0;
    i = 0LL;
    while (i < key) {
        indices = list_append_long_long(indices, &indices_len, 0LL);
        i = i + 1LL;
    }
    const char* output = "";
    pos = 0LL;
    while (pos < strlen(input_string)) {
        long long num = pos % (lowest * 2LL);
        long long alt = (lowest * 2LL) - num;
        if (num > alt) {
            num = alt;
        }
        output = str_concat(output, grid[(int)(num)][(int)(indices[(int)(num)])]);
        indices[(int)(num)] = indices[(int)(num)] + 1LL;
        pos = pos + 1LL;
    }
    return output;
}

MapIS bruteforce(const char* input_string) {
    MapIS results = { NULL, NULL, 0, 0 };
    long long key_guess = 1LL;
    while (key_guess < strlen(input_string)) {
        map_set_is(&results, key_guess, decrypt(input_string, key_guess));
        key_guess = key_guess + 1LL;
    }
    return results;
}

int main(void) {
    {
        long long __start = _now();
        puts(encrypt("Hello World", 4LL));
        puts(decrypt("HWe olordll", 4LL));
        bf = bruteforce("HWe olordll");
        puts(map_get_is(bf.keys, bf.vals, bf.len, 4LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
