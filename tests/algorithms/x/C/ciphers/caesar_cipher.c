// Generated by Mochi 0.10.32 on 2025-08-07 10:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>

size_t brute_force_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* default_alphabet = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";

long long index_of(const char* s, const char* ch);
const char* encrypt(const char* input_string, long long key, const char* alphabet);
const char* decrypt(const char* input_string, long long key, const char* alphabet);
const char* * brute_force(const char* input_string, const char* alphabet);
void user_main();
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* encrypt(const char* input_string, long long key, const char* alphabet) {
    const char* result = "";
    long long i = 0LL;
    long long n = strlen(alphabet);
    while (i < strlen(input_string)) {
        const char* ch = _substring(input_string, i, i + 1LL);
        long long idx = index_of(alphabet, ch);
        if (idx < 0LL) {
            result = str_concat(result, ch);
        } else {
            long long new_key = (idx + key) % n;
            if (new_key < 0LL) {
                new_key = new_key + n;
            }
            result = str_concat(result, _substring(alphabet, new_key, new_key + 1LL));
        }
        i = i + 1LL;
    }
    return result;
}

const char* decrypt(const char* input_string, long long key, const char* alphabet) {
    const char* result = "";
    long long i = 0LL;
    long long n = strlen(alphabet);
    while (i < strlen(input_string)) {
        const char* ch = _substring(input_string, i, i + 1LL);
        long long idx = index_of(alphabet, ch);
        if (idx < 0LL) {
            result = str_concat(result, ch);
        } else {
            long long new_key = (idx - key) % n;
            if (new_key < 0LL) {
                new_key = new_key + n;
            }
            result = str_concat(result, _substring(alphabet, new_key, new_key + 1LL));
        }
        i = i + 1LL;
    }
    return result;
}

const char* * brute_force(const char* input_string, const char* alphabet) {
    const char* *results = NULL;
    size_t results_len = 0;
    long long key = 1LL;
    long long n = strlen(alphabet);
    while (key <= n) {
        const char* message = decrypt(input_string, key, alphabet);
        results = list_append_str(results, &results_len, message);
        key = key + 1LL;
    }
    return brute_force_len = results_len, results;
}

void user_main() {
    const char* alpha = default_alphabet;
    const char* enc = encrypt("The quick brown fox jumps over the lazy dog", 8LL, alpha);
    puts(enc);
    const char* dec = decrypt(enc, 8LL, alpha);
    puts(dec);
    const char* *brute = brute_force("jFyuMy xIH'N vLONy zILwy Gy!", alpha);
    size_t brute_len = brute_force_len;
    puts(brute[(int)(19LL)]);
}

int main(void) {
    user_main();
    return 0;
}
