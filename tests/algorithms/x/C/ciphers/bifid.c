// Generated by Mochi 0.10.32 on 2025-08-07 10:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>

size_t letter_to_numbers_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* SQUARE_0[5] = {"a", "b", "c", "d", "e"};
const char* SQUARE_1[5] = {"f", "g", "h", "i", "k"};
const char* SQUARE_2[5] = {"l", "m", "n", "o", "p"};
const char* SQUARE_3[5] = {"q", "r", "s", "t", "u"};
const char* SQUARE_4[5] = {"v", "w", "x", "y", "z"};
const char* *SQUARE_init[5] = {SQUARE_0, SQUARE_1, SQUARE_2, SQUARE_3, SQUARE_4};
const char* **SQUARE = SQUARE_init;
size_t SQUARE_len = 5;
size_t SQUARE_lens_init[5] = {5, 5, 5, 5, 5};
size_t *SQUARE_lens = SQUARE_lens_init;
size_t SQUARE_lens_len = 5;

long long index_of(const char* s, const char* ch);
const char* to_lower_without_spaces(const char* message, long long replace_j);
long long * letter_to_numbers(const char* letter);
const char* numbers_to_letter(long long row, long long col);
const char* encode(const char* message);
const char* decode(const char* message);
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* to_lower_without_spaces(const char* message, long long replace_j) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* ch = (const char[]){message[i], 0};
        long long pos = index_of(upper, ch);
        if (pos >= 0LL) {
            ch = (const char[]){lower[pos], 0};
        }
        if (strcmp(ch, " ") != 0) {
            if (replace_j && (strcmp(ch, "j") == 0)) {
                ch = "i";
            }
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

long long * letter_to_numbers(const char* letter) {
    long long r = 0LL;
    while (r < SQUARE_len) {
        long long c = 0LL;
        while (c < SQUARE_lens[r]) {
            if (strcmp(SQUARE[(int)(r)][(int)(c)], letter) == 0) {
                {
                    static long long __tmp0_arr[2];
                    __tmp0_arr[0] = r + 1LL;
                    __tmp0_arr[1] = c + 1LL;
                    letter_to_numbers_len = 2;
                    return __tmp0_arr;
                }
            }
            c = c + 1LL;
        }
        r = r + 1LL;
    }
    {
        static long long __tmp1_arr[2];
        __tmp1_arr[0] = 0LL;
        __tmp1_arr[1] = 0LL;
        letter_to_numbers_len = 2;
        return __tmp1_arr;
    }
}

const char* numbers_to_letter(long long row, long long col) {
    return SQUARE[(int)(row - 1LL)][(int)(col - 1LL)];
}

const char* encode(const char* message) {
    const char* clean = to_lower_without_spaces(message, 1LL);
    long long l = strlen(clean);
    long long *rows = NULL;
    size_t rows_len = 0;
    long long *cols = NULL;
    size_t cols_len = 0;
    long long i = 0LL;
    while (i < l) {
        long long *nums = letter_to_numbers((const char[]){clean[i], 0});
        size_t nums_len = letter_to_numbers_len;
        rows = list_append_long_long(rows, &rows_len, nums[(int)(0LL)]);
        cols = list_append_long_long(cols, &cols_len, nums[(int)(1LL)]);
        i = i + 1LL;
    }
    long long *seq = NULL;
    size_t seq_len = 0;
    i = 0LL;
    while (i < l) {
        seq = list_append_long_long(seq, &seq_len, rows[(int)(i)]);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < l) {
        seq = list_append_long_long(seq, &seq_len, cols[(int)(i)]);
        i = i + 1LL;
    }
    const char* encoded = "";
    i = 0LL;
    while (i < l) {
        long long r = seq[(int)(2LL * i)];
        long long c = seq[(int)((2LL * i) + 1LL)];
        encoded = str_concat(encoded, numbers_to_letter(r, c));
        i = i + 1LL;
    }
    return encoded;
}

const char* decode(const char* message) {
    const char* clean = to_lower_without_spaces(message, 0LL);
    long long l = strlen(clean);
    long long *first = NULL;
    size_t first_len = 0;
    long long i = 0LL;
    while (i < l) {
        long long *nums = letter_to_numbers((const char[]){clean[i], 0});
        size_t nums_len = letter_to_numbers_len;
        first = list_append_long_long(first, &first_len, nums[(int)(0LL)]);
        first = list_append_long_long(first, &first_len, nums[(int)(1LL)]);
        i = i + 1LL;
    }
    long long *top = NULL;
    size_t top_len = 0;
    long long *bottom = NULL;
    size_t bottom_len = 0;
    i = 0LL;
    while (i < l) {
        top = list_append_long_long(top, &top_len, first[(int)(i)]);
        bottom = list_append_long_long(bottom, &bottom_len, first[(int)(i + l)]);
        i = i + 1LL;
    }
    const char* decoded = "";
    i = 0LL;
    while (i < l) {
        long long r = top[(int)(i)];
        long long c = bottom[(int)(i)];
        decoded = str_concat(decoded, numbers_to_letter(r, c));
        i = i + 1LL;
    }
    return decoded;
}

int main(void) {
    puts(encode("testmessage"));
    puts(encode("Test Message"));
    puts(encode("test j"));
    puts(encode("test i"));
    puts(decode("qtltbdxrxlk"));
    return 0;
}
