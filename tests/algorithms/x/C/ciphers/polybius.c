// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t letter_to_numbers_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* square_0[5] = {"a", "b", "c", "d", "e"};
const char* square_1[5] = {"f", "g", "h", "i", "k"};
const char* square_2[5] = {"l", "m", "n", "o", "p"};
const char* square_3[5] = {"q", "r", "s", "t", "u"};
const char* square_4[5] = {"v", "w", "x", "y", "z"};
const char* *square_init[5] = {square_0, square_1, square_2, square_3, square_4};
const char* **square = square_init;
size_t square_len = 5;
size_t square_lens_init[5] = {5, 5, 5, 5, 5};
size_t *square_lens = square_lens_init;
size_t square_lens_len = 5;

long long * letter_to_numbers(const char* letter);
const char* numbers_to_letter(long long index1, long long index2);
long long char_to_int(const char* ch);
const char* encode(const char* message);
const char* decode(const char* message);
int main(void);

long long * letter_to_numbers(const char* letter) {
    long long i = 0LL;
    while (i < square_len) {
        long long j = 0LL;
        while (j < square_lens[i]) {
            if (strcmp(square[(int)(i)][(int)(j)], letter) == 0) {
                {
                    static long long __tmp0_arr[2];
                    __tmp0_arr[0] = i + 1LL;
                    __tmp0_arr[1] = j + 1LL;
                    letter_to_numbers_len = 2;
                    return __tmp0_arr;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    {
        static long long __tmp1_arr[2];
        __tmp1_arr[0] = 0LL;
        __tmp1_arr[1] = 0LL;
        letter_to_numbers_len = 2;
        return __tmp1_arr;
    }
}

const char* numbers_to_letter(long long index1, long long index2) {
    return square[(int)(index1 - 1LL)][(int)(index2 - 1LL)];
}

long long char_to_int(const char* ch) {
    if (strcmp(ch, "1") == 0) {
        return 1LL;
    }
    if (strcmp(ch, "2") == 0) {
        return 2LL;
    }
    if (strcmp(ch, "3") == 0) {
        return 3LL;
    }
    if (strcmp(ch, "4") == 0) {
        return 4LL;
    }
    if (strcmp(ch, "5") == 0) {
        return 5LL;
    }
    return 0LL;
}

const char* encode(const char* message) {
    message = str_lower(message);
    const char* encoded = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* ch = (const char[]){message[i], 0};
        if (strcmp(ch, "j") == 0) {
            ch = "i";
        }
        if (strcmp(ch, " ") != 0) {
            long long *nums = letter_to_numbers(ch);
            size_t nums_len = letter_to_numbers_len;
            encoded = str_concat(str_concat(encoded, str_int(nums[(int)(0LL)])), str_int(nums[(int)(1LL)]));
        } else {
            encoded = str_concat(encoded, " ");
        }
        i = i + 1LL;
    }
    return encoded;
}

const char* decode(const char* message) {
    const char* decoded = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        if (strcmp((const char[]){message[i], 0}, " ") == 0) {
            decoded = str_concat(decoded, " ");
            i = i + 1LL;
        } else {
            long long index1 = char_to_int((const char[]){message[i], 0});
            long long index2 = char_to_int((const char[]){message[i + 1LL], 0});
            const char* letter = numbers_to_letter(index1, index2);
            decoded = str_concat(decoded, letter);
            i = i + 2LL;
        }
    }
    return decoded;
}

int main(void) {
    {
        long long __start = _now();
        puts(encode("test message"));
        puts(encode("Test Message"));
        puts(decode("44154344 32154343112215"));
        puts(decode("4415434432154343112215"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
