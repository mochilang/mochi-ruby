// Generated by Mochi 0.10.32 on 2025-08-07 11:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

long long find_char(const char* s, const char* ch);
const char* encrypt_message(const char* key, const char* message);
const char* decrypt_message(const char* key, const char* message);
const char* user_main();
int main(void);

long long find_char(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* encrypt_message(const char* key, const char* message) {
    const char* chars_a = key;
    const char* chars_b = LETTERS;
    const char* translated = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* symbol = (const char[]){message[i], 0};
        const char* upper_sym = str_upper(symbol);
        long long sym_index = find_char(chars_a, upper_sym);
        if (sym_index >= 0LL) {
            const char* sub_char = (const char[]){chars_b[sym_index], 0};
            if (strcmp(symbol, upper_sym) == 0) {
                translated = str_concat(translated, str_upper(sub_char));
            } else {
                translated = str_concat(translated, str_lower(sub_char));
            }
        } else {
            translated = str_concat(translated, symbol);
        }
        i = i + 1LL;
    }
    return translated;
}

const char* decrypt_message(const char* key, const char* message) {
    const char* chars_a = LETTERS;
    const char* chars_b = key;
    const char* translated = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* symbol = (const char[]){message[i], 0};
        const char* upper_sym = str_upper(symbol);
        long long sym_index = find_char(chars_a, upper_sym);
        if (sym_index >= 0LL) {
            const char* sub_char = (const char[]){chars_b[sym_index], 0};
            if (strcmp(symbol, upper_sym) == 0) {
                translated = str_concat(translated, str_upper(sub_char));
            } else {
                translated = str_concat(translated, str_lower(sub_char));
            }
        } else {
            translated = str_concat(translated, symbol);
        }
        i = i + 1LL;
    }
    return translated;
}

const char* user_main() {
    const char* message = "Hello World";
    const char* key = "QWERTYUIOPASDFGHJKLZXCVBNM";
    const char* mode = "decrypt";
    const char* translated = "";
    if (0LL) {
        translated = encrypt_message(key, message);
    } else {
        if (1LL) {
            translated = decrypt_message(key, message);
        }
    }
    puts(str_concat(str_concat(str_concat(str_concat(str_concat("Using the key ", key), ", the "), mode), "ed message is: "), translated));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
