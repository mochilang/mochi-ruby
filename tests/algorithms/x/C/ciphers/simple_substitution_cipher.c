// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(const char *keys[], int vals[], size_t *len, const char *key, int val) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* LOWERCASE = "abcdefghijklmnopqrstuvwxyz";
long long seed = 1LL;
const char* key = "LFWOAYUISVKMNXPBDCRJTQEGHZ";

long long user_rand(long long n);
const char* get_random_key();
long long check_valid_key(const char* key);
long long index_in(const char* s, const char* ch);
const char* char_to_upper(const char* c);
const char* char_to_lower(const char* c);
long long is_upper(const char* c);
const char* translate_message(const char* key, const char* message, const char* mode);
const char* encrypt_message(const char* key, const char* message);
const char* decrypt_message(const char* key, const char* message);
int main(void);

long long user_rand(long long n) {
    seed = ((seed * 1664525LL) + 1013904223LL) % 2147483647LL;
    return seed % n;
}

const char* get_random_key() {
    const char* *chars = NULL;
    size_t chars_len = 0;
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        chars = list_append_str(chars, &chars_len, (const char[]){LETTERS[i], 0});
        i = i + 1LL;
    }
    long long j = chars_len - 1LL;
    while (j > 0LL) {
        long long k = user_rand(j + 1LL);
        const char* tmp = chars[(int)(j)];
        chars[(int)(j)] = chars[(int)(k)];
        chars[(int)(k)] = tmp;
        j = j - 1LL;
    }
    const char* res = "";
    i = 0LL;
    while (i < chars_len) {
        res = str_concat(res, chars[(int)(i)]);
        i = i + 1LL;
    }
    return res;
}

long long check_valid_key(const char* key) {
    if (strlen(key) != strlen(LETTERS)) {
        return 0LL;
    }
    static const char* used_keys[16] = {};
    int used_vals[16] = {};
    size_t used_len = 0;
    MapSI used = { used_keys, used_vals, 0, 16 };
    long long i = 0LL;
    while (i < strlen(key)) {
        const char* ch = (const char[]){key[i], 0};
        if (map_get_si(used.keys, used.vals, used.len, ch)) {
            return 0LL;
        }
        map_set_si(used.keys, used.vals, &used.len, ch, 1LL);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < strlen(LETTERS)) {
        const char* ch = (const char[]){LETTERS[i], 0};
        if (!(map_get_si(used.keys, used.vals, used.len, ch))) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long index_in(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* char_to_upper(const char* c) {
    long long i = 0LL;
    while (i < strlen(LOWERCASE)) {
        if (strcmp(c, (const char[]){LOWERCASE[i], 0}) == 0) {
            return (const char[]){LETTERS[i], 0};
        }
        i = i + 1LL;
    }
    return c;
}

const char* char_to_lower(const char* c) {
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        if (strcmp(c, (const char[]){LETTERS[i], 0}) == 0) {
            return (const char[]){LOWERCASE[i], 0};
        }
        i = i + 1LL;
    }
    return c;
}

long long is_upper(const char* c) {
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        if (strcmp(c, (const char[]){LETTERS[i], 0}) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* translate_message(const char* key, const char* message, const char* mode) {
    const char* chars_a = LETTERS;
    const char* chars_b = key;
    if (strcmp(mode, "decrypt") == 0) {
        const char* tmp = chars_a;
        chars_a = chars_b;
        chars_b = tmp;
    }
    const char* translated = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* symbol = (const char[]){message[i], 0};
        const char* upper_symbol = char_to_upper(symbol);
        long long idx = index_in(chars_a, upper_symbol);
        if (idx >= 0LL) {
            const char* mapped = (const char[]){chars_b[idx], 0};
            if (is_upper(symbol)) {
                translated = str_concat(translated, mapped);
            } else {
                translated = str_concat(translated, char_to_lower(mapped));
            }
        } else {
            translated = str_concat(translated, symbol);
        }
        i = i + 1LL;
    }
    return translated;
}

const char* encrypt_message(const char* key, const char* message) {
    const char* res = translate_message(key, message, "encrypt");
    return res;
}

const char* decrypt_message(const char* key, const char* message) {
    const char* res = translate_message(key, message, "decrypt");
    return res;
}

int main(void) {
    {
        long long __start = _now();
        puts(encrypt_message(key, "Harshil Darji"));
        puts(decrypt_message(key, "Ilcrism Olcvs"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
