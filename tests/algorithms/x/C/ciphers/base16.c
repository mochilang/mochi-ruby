// Generated by Mochi 0.10.32 on 2025-08-07 09:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t base16_decode_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long example1_init[12] = {72LL, 101LL, 108LL, 108LL, 111LL, 32LL, 87LL, 111LL, 114LL, 108LL, 100LL, 33LL};
long long *example1 = example1_init;
size_t example1_len = 12;
long long example2_init[12] = {72LL, 69LL, 76LL, 76LL, 79LL, 32LL, 87LL, 79LL, 82LL, 76LL, 68LL, 33LL};
long long *example2 = example2_init;
size_t example2_len = 12;

const char* base16_encode(long long * data, size_t data_len);
long long * base16_decode(const char* data);
long long _hex_value(const char* digits, const char* ch);
int main(void);

const char* base16_encode(long long * data, size_t data_len) {
    const char* digits = "0123456789ABCDEF";
    const char* res = "";
    long long i = 0LL;
    while (i < data_len) {
        long long b = data[(int)(i)];
        if ((b < 0LL) || (b > 255LL)) {
            panic("byte out of range");
        }
        long long hi = b / 16LL;
        long long lo = b % 16LL;
        res = str_concat(str_concat(res, _substring(digits, hi, hi + 1LL)), _substring(digits, lo, lo + 1LL));
        i = i + 1LL;
    }
    return res;
}

long long * base16_decode(const char* data) {
    const char* digits = "0123456789ABCDEF";
    if ((strlen(data) % 2LL) != 0LL) {
        panic("Base16 encoded data is invalid: Data does not have an even number of hex digits.");
    }
    long long *out = NULL;
    size_t out_len = 0;
    long long i = 0LL;
    while (i < strlen(data)) {
        const char* hi_char = _substring(data, i, i + 1LL);
        const char* lo_char = _substring(data, i + 1LL, i + 2LL);
        long long hi = _hex_value(digits, hi_char);
        long long lo = _hex_value(digits, lo_char);
        if ((hi < 0LL) || (lo < 0LL)) {
            panic("Base16 encoded data is invalid: Data is not uppercase hex or it contains invalid characters.");
        }
        out = list_append_int(out, &out_len, (hi * 16LL) + lo);
        i = i + 2LL;
    }
    return base16_decode_len = out_len, out;
}

long long _hex_value(const char* digits, const char* ch) {
    long long j = 0LL;
    while (j < 16LL) {
        if (strcmp(_substring(digits, j, j + 1LL), ch) == 0) {
            return j;
        }
        j = j + 1LL;
    }
    return -1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(base16_encode(example1, example1_len));
        puts(base16_encode(example2, example2_len));
        puts(base16_encode(NULL, 0));
        puts(({long long *__tmp3 = base16_decode("48656C6C6F20576F726C6421"); char *__res = str_list_int(__tmp3, base16_decode_len); __res;}));
        puts(({long long *__tmp4 = base16_decode("48454C4C4F20574F524C4421"); char *__res = str_list_int(__tmp4, base16_decode_len); __res;}));
        puts(({long long *__tmp5 = base16_decode(""); char *__res = str_list_int(__tmp5, base16_decode_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
