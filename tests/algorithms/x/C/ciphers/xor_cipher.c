// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t encrypt_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* ascii = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";
const char* sample = "hallo welt";
const char* enc = "";
const char* dec = "";

long long xor(long long a, long long b);
long long ord(const char* ch);
const char* chr(long long n);
long long normalize_key(long long key);
const char* * encrypt(const char* content, long long key);
const char* encrypt_string(const char* content, long long key);
int main(void);

long long xor(long long a, long long b) {
    long long res = 0LL;
    long long bit = 1LL;
    long long x = a;
    long long y = b;
    while ((x > 0LL) || (y > 0LL)) {
        long long abit = x % 2LL;
        long long bbit = y % 2LL;
        if (abit != bbit) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long ord(const char* ch) {
    long long i = 0LL;
    while (i < strlen(ascii)) {
        if (strcmp(_substring(ascii, i, i + 1LL), ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* chr(long long n) {
    if ((n >= 32LL) && (n < 127LL)) {
        return _substring(ascii, n - 32LL, n - 31LL);
    }
    return "";
}

long long normalize_key(long long key) {
    long long k = key;
    if (k == 0LL) {
        k = 1LL;
    }
    k = k % 256LL;
    if (k < 0LL) {
        k = k + 256LL;
    }
    return k;
}

const char* * encrypt(const char* content, long long key) {
    long long k = normalize_key(key);
    const char* *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < strlen(content)) {
        long long c = ord(_substring(content, i, i + 1LL));
        long long e = xor(c, k);
        result = list_append_str(result, &result_len, chr(e));
        i = i + 1LL;
    }
    return encrypt_len = result_len, result;
}

const char* encrypt_string(const char* content, long long key) {
    const char* *chars = encrypt(content, key);
    size_t chars_len = encrypt_len;
    const char* out = "";
    for (size_t __i = 0; __i < chars_len; __i++) {
        const char* ch = chars[__i];
        out = str_concat(out, ch);
    }
    return out;
}

int main(void) {
    {
        long long __start = _now();
        enc = encrypt_string(sample, 1LL);
        dec = encrypt_string(enc, 1LL);
        puts(({const char **__tmp1 = encrypt(sample, 1LL); char *__res = str_list_str(__tmp1, encrypt_len); __res;}));
        puts(enc);
        puts(dec);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
