// Generated by Mochi 0.10.32 on 2025-08-07 11:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; void **vals; size_t *lens; size_t len; size_t cap; } MapSL;

static void* map_get_sl(const char *keys[], void *vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static void map_set_sl(const char *keys[], void *vals[], size_t lens[], size_t *len, const char *key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; lens[i] = val_len; return; }
    }
    keys[*len] = key; vals[*len] = val; lens[*len] = val_len; (*len)++;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long seed = 1LL;
const char* ascii_chars = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";
MapSL res = (MapSL){0};
size_t res_len = 0;
const char* *cipher = NULL;
size_t cipher_len = 0;
const char* *key = NULL;
size_t key_len = 0;

void set_seed(long long s);
long long randint(long long a, long long b);
long long ord(const char* ch);
const char* chr(long long code);
MapSL encrypt(const char* text);
const char* decrypt(long long * cipher, size_t cipher_len, long long * key, size_t key_len);
int main(void);

void set_seed(long long s) {
    seed = s;
}

long long randint(long long a, long long b) {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return (seed % ((b - a) + 1LL)) + a;
}

long long ord(const char* ch) {
    long long i = 0LL;
    while (i < strlen(ascii_chars)) {
        if (strcmp((const char[]){ascii_chars[i], 0}, ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* chr(long long code) {
    if ((code < 32LL) || (code > 126LL)) {
        return "";
    }
    return (const char[]){ascii_chars[code - 32LL], 0};
}

MapSL encrypt(const char* text) {
    long long *cipher = NULL;
    size_t cipher_len = 0;
    long long *key = NULL;
    size_t key_len = 0;
    long long i = 0LL;
    while (i < strlen(text)) {
        long long p = ord((const char[]){text[i], 0});
        long long k = randint(1LL, 300LL);
        long long c = (p + k) * k;
        cipher = list_append_int(cipher, &cipher_len, c);
        key = list_append_long_long(key, &key_len, k);
        i = i + 1LL;
    }
    static const char* res_keys[16] = {};
    static void* res_vals[16] = {};
    static size_t res_lens[16] = {};
    size_t res_len = 0;
    MapSL res = { res_keys, res_vals, res_lens, 0, 16 };
    map_set_sl(res.keys, res.vals, res.lens, &res.len, "cipher", cipher, cipher_len);
    map_set_sl(res.keys, res.vals, res.lens, &res.len, "key", key, key_len);
    return res;
}

const char* decrypt(long long * cipher, size_t cipher_len, long long * key, size_t key_len) {
    const char* plain = "";
    long long i = 0LL;
    while (i < key_len) {
        long long p = (cipher[(int)(i)] - (key[(int)(i)] * key[(int)(i)])) / key[(int)(i)];
        plain = str_concat(plain, chr(p));
        i = i + 1LL;
    }
    return plain;
}

int main(void) {
    {
        long long __start = _now();
        set_seed(1LL);
        res = encrypt("Hello");
        cipher = map_get_sl(res.keys, res.vals, res.lens, res.len, "cipher", &cipher_len);
        key = map_get_sl(res.keys, res.vals, res.lens, res.len, "key", &key_len);
        printf("%d\n", cipher);
        printf("%d\n", key);
        puts(decrypt(cipher, cipher_len, key, key_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
