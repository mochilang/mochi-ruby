// Generated by Mochi 0.10.32 on 2025-08-07 11:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t build_plugboard_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* abc = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* low_abc = "abcdefghijklmnopqrstuvwxyz";
const char* rotor1 = "EGZWVONAHDCLFQMSIPJBYUKXTR";
const char* rotor2 = "FOBHMDKEXQNRAULPGSJVTYICZW";
const char* rotor3 = "ZJXESIUQLHAVRMDOYGTNFWPBKC";
const char* rotor4 = "RMDJXFUWGISLHVTCQNKYPBEZOA";
const char* rotor5 = "SGLCPQWZHKXAREONTFBVIYJUDM";
const char* rotor6 = "HVSICLTYKQUBXDWAJZOMFGPREN";
const char* rotor7 = "RZWQHFMVDBKICJLNTUXAGYPSOE";
const char* rotor8 = "LFKIJODBEGAMQPXVUHYSTCZRWN";
const char* rotor9 = "KOAEGVDHXPQZMLFTYWJNBRCIUS";
const char* reflector_pairs_init[13] = {"AN", "BO", "CP", "DQ", "ER", "FS", "GT", "HU", "IV", "JW", "KX", "LY", "MZ"};
const char* *reflector_pairs = reflector_pairs_init;
size_t reflector_pairs_len = 13;

long long list_contains(const char* * xs, size_t xs_len, const char* x);
long long index_in_string(const char* s, const char* ch);
long long contains_char(const char* s, const char* ch);
const char* to_uppercase(const char* s);
const char* plugboard_map(const char* * pb, size_t pb_len, const char* ch);
const char* reflector_map(const char* ch);
long long count_unique(const char* * xs, size_t xs_len);
const char* * build_plugboard(const char* pbstring);
void validator(long long * rotpos, size_t rotpos_len, const char* * rotsel, size_t rotsel_len, const char* pb);
const char* enigma(const char* text, long long * rotor_position, size_t rotor_position_len, const char* * rotor_selection, size_t rotor_selection_len, const char* plugb);
void user_main();
int main(void);

long long list_contains(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)(i)], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long index_in_string(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long contains_char(const char* s, const char* ch) {
    return index_in_string(s, ch) >= 0LL;
}

const char* to_uppercase(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        long long idx = index_in_string(low_abc, ch);
        if (idx >= 0LL) {
            res = str_concat(res, _substring(abc, idx, idx + 1LL));
        } else {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

const char* plugboard_map(const char* * pb, size_t pb_len, const char* ch) {
    long long i = 0LL;
    while (i < pb_len) {
        const char* pair = pb[(int)(i)];
        const char* a = _substring(pair, 0LL, 1LL);
        const char* b = _substring(pair, 1LL, 2LL);
        if (strcmp(ch, a) == 0) {
            return b;
        }
        if (strcmp(ch, b) == 0) {
            return a;
        }
        i = i + 1LL;
    }
    return ch;
}

const char* reflector_map(const char* ch) {
    long long i = 0LL;
    while (i < 13LL) {
        const char* pair = reflector_pairs[(int)(i)];
        const char* a = _substring(pair, 0LL, 1LL);
        const char* b = _substring(pair, 1LL, 2LL);
        if (strcmp(ch, a) == 0) {
            return b;
        }
        if (strcmp(ch, b) == 0) {
            return a;
        }
        i = i + 1LL;
    }
    return ch;
}

long long count_unique(const char* * xs, size_t xs_len) {
    const char* *unique = NULL;
    size_t unique_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        if (!(list_contains(unique, unique_len, xs[(int)(i)]))) {
            unique = list_append_str(unique, &unique_len, xs[(int)(i)]);
        }
        i = i + 1LL;
    }
    return unique_len;
}

const char* * build_plugboard(const char* pbstring) {
    if (strlen(pbstring) == 0LL) {
        {
            static const char* __tmp0_arr[0];
            build_plugboard_len = 0;
            return __tmp0_arr;
        }
    }
    if ((strlen(pbstring) % 2LL) != 0LL) {
        panic(str_concat(str_concat("Odd number of symbols(", str_int(strlen(pbstring))), ")"));
    }
    const char* pbstring_nospace = "";
    long long i = 0LL;
    while (i < strlen(pbstring)) {
        const char* ch = _substring(pbstring, i, i + 1LL);
        if (strcmp(ch, " ") != 0) {
            pbstring_nospace = str_concat(pbstring_nospace, ch);
        }
        i = i + 1LL;
    }
    const char* *seen = NULL;
    size_t seen_len = 0;
    i = 0LL;
    while (i < strlen(pbstring_nospace)) {
        const char* ch = _substring(pbstring_nospace, i, i + 1LL);
        if (!(contains_char(abc, ch))) {
            panic(str_concat(str_concat("'", ch), "' not in list of symbols"));
        }
        if (list_contains(seen, seen_len, ch)) {
            panic(str_concat(str_concat("Duplicate symbol(", ch), ")"));
        }
        seen = list_append_str(seen, &seen_len, ch);
        i = i + 1LL;
    }
    const char* *pb = NULL;
    size_t pb_len = 0;
    i = 0LL;
    while (i < (strlen(pbstring_nospace) - 1LL)) {
        const char* a = _substring(pbstring_nospace, i, i + 1LL);
        const char* b = _substring(pbstring_nospace, i + 1LL, i + 2LL);
        pb = list_append_str(pb, &pb_len, str_concat(a, b));
        i = i + 2LL;
    }
    return build_plugboard_len = pb_len, pb;
}

void validator(long long * rotpos, size_t rotpos_len, const char* * rotsel, size_t rotsel_len, const char* pb) {
    if (count_unique(rotsel, rotsel_len) < 3LL) {
        panic(str_concat(str_concat("Please use 3 unique rotors (not ", str_int(count_unique(rotsel, rotsel_len))), ")"));
    }
    if (rotpos_len != 3LL) {
        panic("Rotor position must have 3 values");
    }
    long long r1 = rotpos[(int)(0LL)];
    long long r2 = rotpos[(int)(1LL)];
    long long r3 = rotpos[(int)(2LL)];
    if (!((0LL < r1) && (r1 <= strlen(abc)))) {
        panic(str_concat(str_concat("First rotor position is not within range of 1..26 (", str_int(r1)), ")"));
    }
    if (!((0LL < r2) && (r2 <= strlen(abc)))) {
        panic(str_concat(str_concat("Second rotor position is not within range of 1..26 (", str_int(r2)), ")"));
    }
    if (!((0LL < r3) && (r3 <= strlen(abc)))) {
        panic(str_concat(str_concat("Third rotor position is not within range of 1..26 (", str_int(r3)), ")"));
    }
}

const char* enigma(const char* text, long long * rotor_position, size_t rotor_position_len, const char* * rotor_selection, size_t rotor_selection_len, const char* plugb) {
    const char* up_text = to_uppercase(text);
    const char* up_pb = to_uppercase(plugb);
    validator(rotor_position, rotor_position_len, rotor_selection, rotor_selection_len, up_pb);
    const char* *plugboard = build_plugboard(up_pb);
    size_t plugboard_len = build_plugboard_len;
    long long rotorpos1 = rotor_position[(int)(0LL)] - 1LL;
    long long rotorpos2 = rotor_position[(int)(1LL)] - 1LL;
    long long rotorpos3 = rotor_position[(int)(2LL)] - 1LL;
    const char* rotor_a = rotor_selection[(int)(0LL)];
    const char* rotor_b = rotor_selection[(int)(1LL)];
    const char* rotor_c = rotor_selection[(int)(2LL)];
    const char* result = "";
    long long i = 0LL;
    while (i < strlen(up_text)) {
        const char* symbol = _substring(up_text, i, i + 1LL);
        if (contains_char(abc, symbol)) {
            symbol = plugboard_map(plugboard, plugboard_len, symbol);
            long long index = index_in_string(abc, symbol) + rotorpos1;
            symbol = _substring(rotor_a, index % strlen(abc), (index % strlen(abc)) + 1LL);
            index = index_in_string(abc, symbol) + rotorpos2;
            symbol = _substring(rotor_b, index % strlen(abc), (index % strlen(abc)) + 1LL);
            index = index_in_string(abc, symbol) + rotorpos3;
            symbol = _substring(rotor_c, index % strlen(abc), (index % strlen(abc)) + 1LL);
            symbol = reflector_map(symbol);
            index = index_in_string(rotor_c, symbol) - rotorpos3;
            if (index < 0LL) {
                index = index + strlen(abc);
            }
            symbol = _substring(abc, index, index + 1LL);
            index = index_in_string(rotor_b, symbol) - rotorpos2;
            if (index < 0LL) {
                index = index + strlen(abc);
            }
            symbol = _substring(abc, index, index + 1LL);
            index = index_in_string(rotor_a, symbol) - rotorpos1;
            if (index < 0LL) {
                index = index + strlen(abc);
            }
            symbol = _substring(abc, index, index + 1LL);
            symbol = plugboard_map(plugboard, plugboard_len, symbol);
            rotorpos1 = rotorpos1 + 1LL;
            if (rotorpos1 >= strlen(abc)) {
                rotorpos1 = 0LL;
                rotorpos2 = rotorpos2 + 1LL;
            }
            if (rotorpos2 >= strlen(abc)) {
                rotorpos2 = 0LL;
                rotorpos3 = rotorpos3 + 1LL;
            }
            if (rotorpos3 >= strlen(abc)) {
                rotorpos3 = 0LL;
            }
        }
        result = str_concat(result, symbol);
        i = i + 1LL;
    }
    return result;
}

void user_main() {
    const char* message = "This is my Python script that emulates the Enigma machine from WWII.";
    long long *rotor_pos = NULL;
    size_t rotor_pos_len = 0;
    rotor_pos = list_append_int(rotor_pos, &rotor_pos_len, 1LL);
    rotor_pos = list_append_int(rotor_pos, &rotor_pos_len, 1LL);
    rotor_pos = list_append_int(rotor_pos, &rotor_pos_len, 1LL);
    const char* pb = "pictures";
    const char* *rotor_sel = NULL;
    size_t rotor_sel_len = 0;
    rotor_sel = list_append_str(rotor_sel, &rotor_sel_len, rotor2);
    rotor_sel = list_append_str(rotor_sel, &rotor_sel_len, rotor4);
    rotor_sel = list_append_str(rotor_sel, &rotor_sel_len, rotor8);
    const char* en = enigma(message, rotor_pos, rotor_pos_len, rotor_sel, rotor_sel_len, pb);
    puts(str_concat("Encrypted message: ", en));
    puts(str_concat("Decrypted message: ", enigma(en, rotor_pos, rotor_pos_len, rotor_sel, rotor_sel_len, pb)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
