// Generated by Mochi 0.10.32 on 2025-08-07 09:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t base64_decode_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* repeat(const char *s, int n) {
    size_t len = strlen(s);
    char *out = malloc(len * n + 1);
    for (int i = 0; i < n; i++) memcpy(out + i * len, s, len);
    out[len * n] = 0;
    return out;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* B64_CHARSET = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";

const char* to_binary(long long n);
const char* zfill(const char* s, long long width);
long long from_binary(const char* s);
const char* user_repeat(const char* ch, long long times);
long long char_index(const char* s, const char* c);
const char* base64_encode(long long * data, size_t data_len);
long long * base64_decode(const char* s);
void user_main();
int main(void);

const char* to_binary(long long n) {
    if (n == 0LL) {
        return "0";
    }
    long long num = n;
    const char* res = "";
    while (num > 0LL) {
        long long bit = num % 2LL;
        res = str_concat(str_int(bit), res);
        num = num / 2LL;
    }
    return res;
}

const char* zfill(const char* s, long long width) {
    const char* res = s;
    long long pad = width - strlen(s);
    while (pad > 0LL) {
        res = str_concat("0", res);
        pad = pad - 1LL;
    }
    return res;
}

long long from_binary(const char* s) {
    long long i = 0LL;
    long long result = 0LL;
    while (i < strlen(s)) {
        result = result * 2LL;
        if (strcmp(_substring(s, i, i + 1LL), "1") == 0) {
            result = result + 1LL;
        }
        i = i + 1LL;
    }
    return result;
}

const char* user_repeat(const char* ch, long long times) {
    const char* res = "";
    long long i = 0LL;
    while (i < times) {
        res = str_concat(res, ch);
        i = i + 1LL;
    }
    return res;
}

long long char_index(const char* s, const char* c) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), c) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* base64_encode(long long * data, size_t data_len) {
    const char* bits = "";
    long long i = 0LL;
    while (i < data_len) {
        bits = str_concat(bits, zfill(to_binary(data[(int)(i)]), 8LL));
        i = i + 1LL;
    }
    long long pad_bits = 0LL;
    if ((strlen(bits) % 6LL) != 0LL) {
        pad_bits = 6LL - (strlen(bits) % 6LL);
        bits = str_concat(bits, repeat("0", pad_bits));
    }
    long long j = 0LL;
    const char* encoded = "";
    while (j < strlen(bits)) {
        const char* chunk = _substring(bits, j, j + 6LL);
        long long idx = from_binary(chunk);
        encoded = str_concat(encoded, _substring(B64_CHARSET, idx, idx + 1LL));
        j = j + 6LL;
    }
    long long pad = pad_bits / 2LL;
    while (pad > 0LL) {
        encoded = str_concat(encoded, "=");
        pad = pad - 1LL;
    }
    return encoded;
}

long long * base64_decode(const char* s) {
    long long padding = 0LL;
    long long end = strlen(s);
    while ((end > 0LL) && (strcmp(_substring(s, end - 1LL, end), "=") == 0)) {
        padding = padding + 1LL;
        end = end - 1LL;
    }
    const char* bits = "";
    long long k = 0LL;
    while (k < end) {
        const char* c = _substring(s, k, k + 1LL);
        long long idx = char_index(B64_CHARSET, c);
        bits = str_concat(bits, zfill(to_binary(idx), 6LL));
        k = k + 1LL;
    }
    if (padding > 0LL) {
        bits = _substring(bits, 0LL, strlen(bits) - (padding * 2LL));
    }
    long long *bytes = NULL;
    size_t bytes_len = 0;
    long long m = 0LL;
    while (m < strlen(bits)) {
        long long byte = from_binary(_substring(bits, m, m + 8LL));
        bytes = list_append_long_long(bytes, &bytes_len, byte);
        m = m + 8LL;
    }
    return base64_decode_len = bytes_len, bytes;
}

void user_main() {
    long long *data = NULL;
    size_t data_len = 0;
    data = list_append_int(data, &data_len, 77LL);
    data = list_append_int(data, &data_len, 111LL);
    data = list_append_int(data, &data_len, 99LL);
    data = list_append_int(data, &data_len, 104LL);
    data = list_append_int(data, &data_len, 105LL);
    const char* encoded = base64_encode(data, data_len);
    puts(encoded);
    long long* __tmp1 = base64_decode(encoded);
    puts(str_list_int(__tmp1, base64_decode_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
