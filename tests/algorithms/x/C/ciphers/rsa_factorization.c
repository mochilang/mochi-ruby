// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t rsa_factor_len;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long gcd(long long a, long long b);
long long pow_mod(long long base, long long exp, long long mod);
long long * rsa_factor(long long d, long long e, long long n);
int main(void);

long long gcd(long long a, long long b) {
    long long x = a;
    long long y = b;
    while (y != 0LL) {
        long long t = x % y;
        x = y;
        y = t;
    }
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long pow_mod(long long base, long long exp, long long mod) {
    long long result = 1LL;
    long long b = base % mod;
    long long e = exp;
    while (e > 0LL) {
        if ((e % 2LL) == 1LL) {
            result = (result * b) % mod;
        }
        e = e / 2LL;
        b = (b * b) % mod;
    }
    return result;
}

long long * rsa_factor(long long d, long long e, long long n) {
    long long k = (d * e) - 1LL;
    long long p = 0LL;
    long long q = 0LL;
    long long g = 2LL;
    while ((p == 0LL) && (g < n)) {
        long long t = k;
        while ((t % 2LL) == 0LL) {
            t = t / 2LL;
            long long x = pow_mod(g, t, n);
            long long y = gcd(x - 1LL, n);
            if ((x > 1LL) && (y > 1LL)) {
                p = y;
                q = n / y;
                break;
            }
        }
        g = g + 1LL;
    }
    if (p > q) {
        {
            static long long __tmp0_arr[2];
            __tmp0_arr[0] = q;
            __tmp0_arr[1] = p;
            rsa_factor_len = 2;
            return __tmp0_arr;
        }
    }
    {
        static long long __tmp1_arr[2];
        __tmp1_arr[0] = p;
        __tmp1_arr[1] = q;
        rsa_factor_len = 2;
        return __tmp1_arr;
    }
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", rsa_factor(3LL, 16971LL, 25777LL));
        printf("%d\n", rsa_factor(7331LL, 11LL, 27233LL));
        printf("%d\n", rsa_factor(4021LL, 13LL, 17711LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
