// Generated by Mochi 0.10.32 on 2025-08-07 11:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t matrix_minor_len;
size_t *matrix_minor_lens;
size_t cofactor_matrix_len;
size_t *cofactor_matrix_lens;
size_t transpose_len;
size_t *transpose_lens;
size_t matrix_mod_len;
size_t *matrix_mod_lens;
size_t scalar_matrix_mult_len;
size_t *scalar_matrix_mult_lens;
size_t adjugate_len;
size_t *adjugate_lens;
size_t multiply_matrix_vector_len;
size_t inverse_key_len;
size_t *inverse_key_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* KEY_STRING = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
long long key_0[2] = {2LL, 5LL};
long long key_1[2] = {1LL, 6LL};
long long *key_init[2] = {key_0, key_1};
long long **key = key_init;
size_t key_len = 2;
size_t key_lens_init[2] = {2, 2};
size_t *key_lens = key_lens_init;
size_t key_lens_len = 2;

long long mod36(long long n);
long long gcd(long long a, long long b);
long long replace_letters(const char* letter);
const char* replace_digits(long long num);
const char* to_upper(const char* c);
const char* process_text(const char* text, long long break_key);
long long * * matrix_minor(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, long long row, long long col);
long long determinant(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
long long * * cofactor_matrix(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
long long * * transpose(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
long long * * matrix_mod(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
long long * * scalar_matrix_mult(long long s, long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
long long * * adjugate(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
long long * multiply_matrix_vector(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, long long * v, size_t v_len);
long long * * inverse_key(long long * * key, size_t key_len, size_t* key_lens, size_t key_lens_len);
const char* hill_encrypt(long long * * key, size_t key_len, size_t* key_lens, size_t key_lens_len, const char* text);
const char* hill_decrypt(long long * * key, size_t key_len, size_t* key_lens, size_t key_lens_len, const char* text);
int main(void);

long long mod36(long long n) {
    long long r = n % 36LL;
    if (r < 0LL) {
        r = r + 36LL;
    }
    return r;
}

long long gcd(long long a, long long b) {
    long long x = a;
    long long y = b;
    while (y != 0LL) {
        long long t = y;
        y = x % y;
        x = t;
    }
    if (x < 0LL) {
        x = -(x);
    }
    return x;
}

long long replace_letters(const char* letter) {
    long long i = 0LL;
    while (i < strlen(KEY_STRING)) {
        if (strcmp((const char[]){KEY_STRING[i], 0}, letter) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* replace_digits(long long num) {
    long long idx = mod36(num);
    return (const char[]){KEY_STRING[idx], 0};
}

const char* to_upper(const char* c) {
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    long long i = 0LL;
    while (i < strlen(lower)) {
        if (strcmp(c, (const char[]){lower[i], 0}) == 0) {
            return (const char[]){upper[i], 0};
        }
        i = i + 1LL;
    }
    return c;
}

const char* process_text(const char* text, long long break_key) {
    const char* *chars = NULL;
    size_t chars_len = 0;
    long long i = 0LL;
    while (i < strlen(text)) {
        const char* c = to_upper((const char[]){text[i], 0});
        long long j = 0LL;
        long long ok = 0LL;
        while (j < strlen(KEY_STRING)) {
            if (strcmp((const char[]){KEY_STRING[j], 0}, c) == 0) {
                ok = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (ok) {
            chars = list_append_str(chars, &chars_len, c);
        }
        i = i + 1LL;
    }
    if (chars_len == 0LL) {
        return "";
    }
    const char* last = chars[(int)(chars_len - 1LL)];
    while ((chars_len % break_key) != 0LL) {
        chars = list_append_str(chars, &chars_len, last);
    }
    const char* res = "";
    long long k = 0LL;
    while (k < chars_len) {
        res = str_concat(res, chars[(int)(k)]);
        k = k + 1LL;
    }
    return res;
}

long long * * matrix_minor(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, long long row, long long col) {
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < m_len) {
        if (i != row) {
            long long *r = NULL;
            size_t r_len = 0;
            long long j = 0LL;
            while (j < m_lens[i]) {
                if (j != col) {
                    r = list_append_long_long(r, &r_len, m[(int)(i)][(int)(j)]);
                }
                j = j + 1LL;
            }
            res = list_append_long_longptr(res, &res_len, r);
            res_lens = list_append_szt(res_lens, &res_lens_len, r_len);
        }
        i = i + 1LL;
    }
    return matrix_minor_lens = res_lens, matrix_minor_len = res_len, res;
}

long long determinant(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    size_t n = m_len;
    if (n == 1LL) {
        return m[(int)(0LL)][(int)(0LL)];
    }
    if (n == 2LL) {
        return (m[(int)(0LL)][(int)(0LL)] * m[(int)(1LL)][(int)(1LL)]) - (m[(int)(0LL)][(int)(1LL)] * m[(int)(1LL)][(int)(0LL)]);
    }
    long long det = 0LL;
    long long col = 0LL;
    while (col < n) {
        long long **minor_mat = matrix_minor(m, m_len, m_lens, m_len, 0LL, col);
        size_t minor_mat_len = matrix_minor_len;
        size_t *minor_mat_lens = matrix_minor_lens;
        size_t minor_mat_lens_len = matrix_minor_len;
        long long sign = 1LL;
        if ((col % 2LL) == 1LL) {
            sign = -1LL;
        }
        det = det + ((sign * m[(int)(0LL)][(int)(col)]) * determinant(minor_mat, minor_mat_len, minor_mat_lens, minor_mat_len));
        col = col + 1LL;
    }
    return det;
}

long long * * cofactor_matrix(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    size_t n = m_len;
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            long long **minor_mat = matrix_minor(m, m_len, m_lens, m_len, i, j);
            size_t minor_mat_len = matrix_minor_len;
            size_t *minor_mat_lens = matrix_minor_lens;
            size_t minor_mat_lens_len = matrix_minor_len;
            long long det_minor = determinant(minor_mat, minor_mat_len, minor_mat_lens, minor_mat_len);
            long long sign = 1LL;
            if (((i + j) % 2LL) == 1LL) {
                sign = -1LL;
            }
            row = list_append_int(row, &row_len, sign * det_minor);
            j = j + 1LL;
        }
        res = list_append_long_longptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return cofactor_matrix_lens = res_lens, cofactor_matrix_len = res_len, res;
}

long long * * transpose(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    size_t rows = m_len;
    long long cols = m_lens[0LL];
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long j = 0LL;
    while (j < cols) {
        long long *row = NULL;
        size_t row_len = 0;
        long long i = 0LL;
        while (i < rows) {
            row = list_append_long_long(row, &row_len, m[(int)(i)][(int)(j)]);
            i = i + 1LL;
        }
        res = list_append_long_longptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        j = j + 1LL;
    }
    return transpose_lens = res_lens, transpose_len = res_len, res;
}

long long * * matrix_mod(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < m_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m_lens[i]) {
            row = list_append_long_long(row, &row_len, mod36(m[(int)(i)][(int)(j)]));
            j = j + 1LL;
        }
        res = list_append_long_longptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return matrix_mod_lens = res_lens, matrix_mod_len = res_len, res;
}

long long * * scalar_matrix_mult(long long s, long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < m_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m_lens[i]) {
            row = list_append_long_long(row, &row_len, mod36(s * m[(int)(i)][(int)(j)]));
            j = j + 1LL;
        }
        res = list_append_long_longptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return scalar_matrix_mult_lens = res_lens, scalar_matrix_mult_len = res_len, res;
}

long long * * adjugate(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long **cof = cofactor_matrix(m, m_len, m_lens, m_len);
    size_t cof_len = cofactor_matrix_len;
    size_t *cof_lens = cofactor_matrix_lens;
    size_t cof_lens_len = cofactor_matrix_len;
    long long n = cof_len;
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_long_long(row, &row_len, cof[(int)(j)][(int)(i)]);
            j = j + 1LL;
        }
        res = list_append_long_longptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return adjugate_lens = res_lens, adjugate_len = res_len, res;
}

long long * multiply_matrix_vector(long long * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, long long * v, size_t v_len) {
    size_t n = m_len;
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long sum = 0LL;
        long long j = 0LL;
        while (j < n) {
            sum = sum + (m[(int)(i)][(int)(j)] * v[(int)(j)]);
            j = j + 1LL;
        }
        res = list_append_long_long(res, &res_len, mod36(sum));
        i = i + 1LL;
    }
    return multiply_matrix_vector_len = res_len, res;
}

long long * * inverse_key(long long * * key, size_t key_len, size_t* key_lens, size_t key_lens_len) {
    long long det_val = determinant(key, key_len, key_lens, key_len);
    long long det_mod = mod36(det_val);
    long long det_inv = 0LL;
    long long i = 0LL;
    while (i < 36LL) {
        if (((det_mod * i) % 36LL) == 1LL) {
            det_inv = i;
            break;
        }
        i = i + 1LL;
    }
    long long **adj = adjugate(key, key_len, key_lens, key_len);
    size_t adj_len = adjugate_len;
    size_t *adj_lens = adjugate_lens;
    size_t adj_lens_len = adjugate_len;
    long long **tmp = scalar_matrix_mult(det_inv, adj, adj_len, adj_lens, adj_len);
    size_t tmp_len = scalar_matrix_mult_len;
    size_t *tmp_lens = scalar_matrix_mult_lens;
    size_t tmp_lens_len = scalar_matrix_mult_len;
    long long **res = matrix_mod(tmp, tmp_len, tmp_lens, tmp_len);
    size_t res_len = matrix_mod_len;
    size_t *res_lens = matrix_mod_lens;
    size_t res_lens_len = matrix_mod_len;
    return inverse_key_lens = res_lens, inverse_key_len = res_len, res;
}

const char* hill_encrypt(long long * * key, size_t key_len, size_t* key_lens, size_t key_lens_len, const char* text) {
    size_t break_key = key_len;
    const char* processed = process_text(text, break_key);
    const char* encrypted = "";
    long long i = 0LL;
    while (i < strlen(processed)) {
        long long *vec = NULL;
        size_t vec_len = 0;
        long long j = 0LL;
        while (j < break_key) {
            vec = list_append_long_long(vec, &vec_len, replace_letters((const char[]){processed[i + j], 0}));
            j = j + 1LL;
        }
        long long *enc_vec = multiply_matrix_vector(key, key_len, key_lens, key_len, vec, vec_len);
        size_t enc_vec_len = multiply_matrix_vector_len;
        long long k = 0LL;
        while (k < break_key) {
            encrypted = str_concat(encrypted, replace_digits(enc_vec[(int)(k)]));
            k = k + 1LL;
        }
        i = i + break_key;
    }
    return encrypted;
}

const char* hill_decrypt(long long * * key, size_t key_len, size_t* key_lens, size_t key_lens_len, const char* text) {
    size_t break_key = key_len;
    long long **decrypt_key = inverse_key(key, key_len, key_lens, key_len);
    size_t decrypt_key_len = inverse_key_len;
    size_t *decrypt_key_lens = inverse_key_lens;
    size_t decrypt_key_lens_len = inverse_key_len;
    const char* processed = process_text(text, break_key);
    const char* decrypted = "";
    long long i = 0LL;
    while (i < strlen(processed)) {
        long long *vec = NULL;
        size_t vec_len = 0;
        long long j = 0LL;
        while (j < break_key) {
            vec = list_append_long_long(vec, &vec_len, replace_letters((const char[]){processed[i + j], 0}));
            j = j + 1LL;
        }
        long long *dec_vec = multiply_matrix_vector(decrypt_key, decrypt_key_len, decrypt_key_lens, decrypt_key_len, vec, vec_len);
        size_t dec_vec_len = multiply_matrix_vector_len;
        long long k = 0LL;
        while (k < break_key) {
            decrypted = str_concat(decrypted, replace_digits(dec_vec[(int)(k)]));
            k = k + 1LL;
        }
        i = i + break_key;
    }
    return decrypted;
}

int main(void) {
    {
        long long __start = _now();
        puts(hill_encrypt(key, key_len, key_lens, key_len, "testing hill cipher"));
        puts(hill_encrypt(key, key_len, key_lens, key_len, "hello"));
        puts(hill_decrypt(key, key_len, key_lens, key_len, "WHXYJOLM9C6XT085LL"));
        puts(hill_decrypt(key, key_len, key_lens, key_len, "85FF00"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
