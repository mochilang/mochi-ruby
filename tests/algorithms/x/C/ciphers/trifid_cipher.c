// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t decrypt_part_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* triagrams_init[27] = {"111", "112", "113", "121", "122", "123", "131", "132", "133", "211", "212", "213", "221", "222", "223", "231", "232", "233", "311", "312", "313", "321", "322", "323", "331", "332", "333"};
const char* *triagrams = triagrams_init;
size_t triagrams_len = 27;

const char* remove_spaces(const char* s);
const char* char_to_trigram(const char* ch, const char* alphabet);
const char* trigram_to_char(const char* tri, const char* alphabet);
const char* encrypt_part(const char* part, const char* alphabet);
const char* encrypt_message(const char* message, const char* alphabet, long long period);
const char* * decrypt_part(const char* part, const char* alphabet);
const char* decrypt_message(const char* message, const char* alphabet, long long period);
void user_main();
int main(void);

const char* remove_spaces(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        if (strcmp(c, " ") != 0) {
            res = str_concat(res, c);
        }
        i = i + 1LL;
    }
    return res;
}

const char* char_to_trigram(const char* ch, const char* alphabet) {
    long long i = 0LL;
    while (i < strlen(alphabet)) {
        if (strcmp(_substring(alphabet, i, i + 1LL), ch) == 0) {
            return triagrams[(int)(i)];
        }
        i = i + 1LL;
    }
    return "";
}

const char* trigram_to_char(const char* tri, const char* alphabet) {
    long long i = 0LL;
    while (i < 27LL) {
        if (strcmp(triagrams[(int)(i)], tri) == 0) {
            return _substring(alphabet, i, i + 1LL);
        }
        i = i + 1LL;
    }
    return "";
}

const char* encrypt_part(const char* part, const char* alphabet) {
    const char* one = "";
    const char* two = "";
    const char* three = "";
    long long i = 0LL;
    while (i < strlen(part)) {
        const char* tri = char_to_trigram(_substring(part, i, i + 1LL), alphabet);
        one = str_concat(one, _substring(tri, 0LL, 1LL));
        two = str_concat(two, _substring(tri, 1LL, 2LL));
        three = str_concat(three, _substring(tri, 2LL, 3LL));
        i = i + 1LL;
    }
    return str_concat(str_concat(one, two), three);
}

const char* encrypt_message(const char* message, const char* alphabet, long long period) {
    const char* msg = remove_spaces(message);
    const char* alpha = remove_spaces(alphabet);
    if (strlen(alpha) != 27LL) {
        return "";
    }
    const char* encrypted_numeric = "";
    long long i = 0LL;
    while (i < strlen(msg)) {
        long long end = i + period;
        if (end > strlen(msg)) {
            end = strlen(msg);
        }
        const char* part = _substring(msg, i, end);
        encrypted_numeric = str_concat(encrypted_numeric, encrypt_part(part, alpha));
        i = i + period;
    }
    const char* encrypted = "";
    long long j = 0LL;
    while (j < strlen(encrypted_numeric)) {
        const char* tri = _substring(encrypted_numeric, j, j + 3LL);
        encrypted = str_concat(encrypted, trigram_to_char(tri, alpha));
        j = j + 3LL;
    }
    return encrypted;
}

const char* * decrypt_part(const char* part, const char* alphabet) {
    const char* converted = "";
    long long i = 0LL;
    while (i < strlen(part)) {
        const char* tri = char_to_trigram(_substring(part, i, i + 1LL), alphabet);
        converted = str_concat(converted, tri);
        i = i + 1LL;
    }
    const char* *result = NULL;
    size_t result_len = 0;
    const char* tmp = "";
    long long j = 0LL;
    while (j < strlen(converted)) {
        tmp = str_concat(tmp, _substring(converted, j, j + 1LL));
        if (strlen(tmp) == strlen(part)) {
            result = list_append_str(result, &result_len, tmp);
            tmp = "";
        }
        j = j + 1LL;
    }
    return decrypt_part_len = result_len, result;
}

const char* decrypt_message(const char* message, const char* alphabet, long long period) {
    const char* msg = remove_spaces(message);
    const char* alpha = remove_spaces(alphabet);
    if (strlen(alpha) != 27LL) {
        return "";
    }
    const char* *decrypted_numeric = NULL;
    size_t decrypted_numeric_len = 0;
    long long i = 0LL;
    while (i < strlen(msg)) {
        long long end = i + period;
        if (end > strlen(msg)) {
            end = strlen(msg);
        }
        const char* part = _substring(msg, i, end);
        const char* *groups = decrypt_part(part, alpha);
        size_t groups_len = decrypt_part_len;
        long long k = 0LL;
        while (k < strlen(groups[(int)(0LL)])) {
            const char* tri = str_concat(str_concat(_substring(groups[(int)(0LL)], k, k + 1LL), _substring(groups[(int)(1LL)], k, k + 1LL)), _substring(groups[(int)(2LL)], k, k + 1LL));
            decrypted_numeric = list_append_str(decrypted_numeric, &decrypted_numeric_len, tri);
            k = k + 1LL;
        }
        i = i + period;
    }
    const char* decrypted = "";
    long long j = 0LL;
    while (j < decrypted_numeric_len) {
        decrypted = str_concat(decrypted, trigram_to_char(decrypted_numeric[(int)(j)], alpha));
        j = j + 1LL;
    }
    return decrypted;
}

void user_main() {
    const char* msg = "DEFEND THE EAST WALL OF THE CASTLE.";
    const char* alphabet = "EPSDUCVWYM.ZLKXNBTFGORIJHAQ";
    const char* encrypted = encrypt_message(msg, alphabet, 5LL);
    const char* decrypted = decrypt_message(encrypted, alphabet, 5LL);
    puts(str_concat("Encrypted: ", encrypted));
    puts(str_concat("Decrypted: ", decrypted));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
