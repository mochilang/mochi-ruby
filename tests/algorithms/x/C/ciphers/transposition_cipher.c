// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* join_strings(const char* * xs, size_t xs_len);
const char* encrypt_message(long long key, const char* message);
const char* decrypt_message(long long key, const char* message);
void user_main();
int main(void);

const char* join_strings(const char* * xs, size_t xs_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < xs_len) {
        res = str_concat(res, xs[(int)(i)]);
        i = i + 1LL;
    }
    return res;
}

const char* encrypt_message(long long key, const char* message) {
    const char* result = "";
    long long col = 0LL;
    while (col < key) {
        long long pointer = col;
        while (pointer < strlen(message)) {
            result = str_concat(result, _substring(message, pointer, pointer + 1LL));
            pointer = pointer + key;
        }
        col = col + 1LL;
    }
    return result;
}

const char* decrypt_message(long long key, const char* message) {
    long long num_cols = ((strlen(message) + key) - 1LL) / key;
    long long num_rows = key;
    long long num_shaded_boxes = (num_cols * num_rows) - strlen(message);
    const char* *plain_text = NULL;
    size_t plain_text_len = 0;
    long long i = 0LL;
    while (i < num_cols) {
        plain_text = list_append_str(plain_text, &plain_text_len, "");
        i = i + 1LL;
    }
    long long col = 0LL;
    long long row = 0LL;
    long long index = 0LL;
    while (index < strlen(message)) {
        plain_text[(int)(col)] = str_concat(plain_text[(int)(col)], _substring(message, index, index + 1LL));
        col = col + 1LL;
        if ((col == num_cols) || ((col == (num_cols - 1LL)) && (row >= (num_rows - num_shaded_boxes)))) {
            col = 0LL;
            row = row + 1LL;
        }
        index = index + 1LL;
    }
    return join_strings(plain_text, plain_text_len);
}

void user_main() {
    puts("Enter message: ");
    const char* message = _input();
    long long max_key = strlen(message) - 1LL;
    puts(str_concat(str_concat("Enter key [2-", str_int(max_key)), "]: "));
    long long key = _atoi(_input());
    puts("Encryption/Decryption [e/d]: ");
    const char* mode = _input();
    const char* text = "";
    const char* first = _substring(mode, 0LL, 1LL);
    if ((strcmp(first, "e") == 0) || (strcmp(first, "E") == 0)) {
        text = encrypt_message(key, message);
    } else {
        if ((strcmp(first, "d") == 0) || (strcmp(first, "D") == 0)) {
            text = decrypt_message(key, message);
        }
    }
    puts(str_concat(str_concat("Output:\n", text), "|"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
