// Generated by Mochi 0.10.32 on 2025-08-07 00:04 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* LOWER = "abcdefghijklmnopqrstuvwxyz";
const char* UPPER = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

const char* to_lowercase(const char* s);
long long char_index(const char* c);
const char* index_char(long long i);
const char* encrypt(const char* plaintext, const char* key);
const char* decrypt(const char* ciphertext, const char* key);
int main(void);

const char* to_lowercase(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        long long j = 0LL;
        long long found = 0LL;
        while (j < 26LL) {
            if (strcmp(c, (const char[]){UPPER[j], 0}) == 0) {
                res = str_concat(res, (const char[]){LOWER[j], 0});
                found = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (!(found)) {
            res = str_concat(res, c);
        }
        i = i + 1LL;
    }
    return res;
}

long long char_index(const char* c) {
    long long i = 0LL;
    while (i < 26LL) {
        if (strcmp(c, (const char[]){LOWER[i], 0}) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* index_char(long long i) {
    return (const char[]){LOWER[i], 0};
}

const char* encrypt(const char* plaintext, const char* key) {
    if (strlen(plaintext) == 0LL) {
        panic("plaintext is empty");
    }
    if (strlen(key) == 0LL) {
        panic("key is empty");
    }
    const char* full_key = str_concat(key, plaintext);
    plaintext = to_lowercase(plaintext);
    full_key = to_lowercase(full_key);
    long long p_i = 0LL;
    long long k_i = 0LL;
    const char* ciphertext = "";
    while (p_i < strlen(plaintext)) {
        const char* p_char = (const char[]){plaintext[p_i], 0};
        long long p_idx = char_index(p_char);
        if (p_idx < 0LL) {
            ciphertext = str_concat(ciphertext, p_char);
            p_i = p_i + 1LL;
        } else {
            const char* k_char = (const char[]){full_key[k_i], 0};
            long long k_idx = char_index(k_char);
            if (k_idx < 0LL) {
                k_i = k_i + 1LL;
            } else {
                long long c_idx = (p_idx + k_idx) % 26LL;
                ciphertext = str_concat(ciphertext, index_char(c_idx));
                k_i = k_i + 1LL;
                p_i = p_i + 1LL;
            }
        }
    }
    return ciphertext;
}

const char* decrypt(const char* ciphertext, const char* key) {
    if (strlen(ciphertext) == 0LL) {
        panic("ciphertext is empty");
    }
    if (strlen(key) == 0LL) {
        panic("key is empty");
    }
    const char* current_key = to_lowercase(key);
    long long c_i = 0LL;
    long long k_i = 0LL;
    const char* plaintext = "";
    while (c_i < strlen(ciphertext)) {
        const char* c_char = (const char[]){ciphertext[c_i], 0};
        long long c_idx = char_index(c_char);
        if (c_idx < 0LL) {
            plaintext = str_concat(plaintext, c_char);
        } else {
            const char* k_char = (const char[]){current_key[k_i], 0};
            long long k_idx = char_index(k_char);
            long long p_idx = ((c_idx - k_idx) + 26LL) % 26LL;
            const char* p_char = index_char(p_idx);
            plaintext = str_concat(plaintext, p_char);
            current_key = str_concat(current_key, p_char);
            k_i = k_i + 1LL;
        }
        c_i = c_i + 1LL;
    }
    return plaintext;
}

int main(void) {
    {
        long long __start = _now();
        puts(encrypt("hello world", "coffee"));
        puts(decrypt("jsqqs avvwo", "coffee"));
        puts(encrypt("coffee is good as python", "TheAlgorithms"));
        puts(decrypt("vvjfpk wj ohvp su ddylsv", "TheAlgorithms"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
