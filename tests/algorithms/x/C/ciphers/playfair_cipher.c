// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t generate_table_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long contains(const char* * xs, size_t xs_len, const char* x);
long long index_of(const char* * xs, size_t xs_len, const char* x);
const char* prepare_input(const char* dirty);
const char* * generate_table(const char* key);
const char* encode(const char* plaintext, const char* key);
const char* decode(const char* cipher, const char* key);
void user_main();
int main(void);

long long contains(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)(i)], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long index_of(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)(i)], x) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* prepare_input(const char* dirty) {
    const char* letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* upper_dirty = str_upper(dirty);
    const char* filtered = "";
    long long i = 0LL;
    while (i < strlen(upper_dirty)) {
        const char* c = _substring(upper_dirty, i, i + 1LL);
        if (strstr(letters, c) != NULL) {
            filtered = str_concat(filtered, c);
        }
        i = i + 1LL;
    }
    if (strlen(filtered) < 2LL) {
        return filtered;
    }
    const char* clean = "";
    i = 0LL;
    while (i < (strlen(filtered) - 1LL)) {
        const char* c1 = _substring(filtered, i, i + 1LL);
        const char* c2 = _substring(filtered, i + 1LL, i + 2LL);
        clean = str_concat(clean, c1);
        if (strcmp(c1, c2) == 0) {
            clean = str_concat(clean, "X");
        }
        i = i + 1LL;
    }
    clean = str_concat(clean, _substring(filtered, strlen(filtered) - 1LL, strlen(filtered)));
    if ((strlen(clean) % 2LL) == 1LL) {
        clean = str_concat(clean, "X");
    }
    return clean;
}

const char* * generate_table(const char* key) {
    const char* alphabet = "ABCDEFGHIKLMNOPQRSTUVWXYZ";
    const char* *table = NULL;
    size_t table_len = 0;
    const char* upper_key = str_upper(key);
    long long i = 0LL;
    while (i < strlen(upper_key)) {
        const char* c = _substring(upper_key, i, i + 1LL);
        if (strstr(alphabet, c) != NULL) {
            if (!(contains(table, table_len, c))) {
                table = list_append_str(table, &table_len, c);
            }
        }
        i = i + 1LL;
    }
    i = 0LL;
    while (i < strlen(alphabet)) {
        const char* c = _substring(alphabet, i, i + 1LL);
        if (!(contains(table, table_len, c))) {
            table = list_append_str(table, &table_len, c);
        }
        i = i + 1LL;
    }
    return generate_table_len = table_len, table;
}

const char* encode(const char* plaintext, const char* key) {
    const char* *table = generate_table(key);
    size_t table_len = generate_table_len;
    const char* text = prepare_input(plaintext);
    const char* cipher = "";
    long long i = 0LL;
    while (i < strlen(text)) {
        const char* c1 = _substring(text, i, i + 1LL);
        const char* c2 = _substring(text, i + 1LL, i + 2LL);
        long long idx1 = index_of(table, table_len, c1);
        long long idx2 = index_of(table, table_len, c2);
        long long row1 = idx1 / 5LL;
        long long col1 = idx1 % 5LL;
        long long row2 = idx2 / 5LL;
        long long col2 = idx2 % 5LL;
        if (row1 == row2) {
            cipher = str_concat(cipher, table[(int)((row1 * 5LL) + ((col1 + 1LL) % 5LL))]);
            cipher = str_concat(cipher, table[(int)((row2 * 5LL) + ((col2 + 1LL) % 5LL))]);
        } else {
            if (col1 == col2) {
                cipher = str_concat(cipher, table[(int)((((row1 + 1LL) % 5LL) * 5LL) + col1)]);
                cipher = str_concat(cipher, table[(int)((((row2 + 1LL) % 5LL) * 5LL) + col2)]);
            } else {
                cipher = str_concat(cipher, table[(int)((row1 * 5LL) + col2)]);
                cipher = str_concat(cipher, table[(int)((row2 * 5LL) + col1)]);
            }
        }
        i = i + 2LL;
    }
    return cipher;
}

const char* decode(const char* cipher, const char* key) {
    const char* *table = generate_table(key);
    size_t table_len = generate_table_len;
    const char* plain = "";
    long long i = 0LL;
    while (i < strlen(cipher)) {
        const char* c1 = _substring(cipher, i, i + 1LL);
        const char* c2 = _substring(cipher, i + 1LL, i + 2LL);
        long long idx1 = index_of(table, table_len, c1);
        long long idx2 = index_of(table, table_len, c2);
        long long row1 = idx1 / 5LL;
        long long col1 = idx1 % 5LL;
        long long row2 = idx2 / 5LL;
        long long col2 = idx2 % 5LL;
        if (row1 == row2) {
            plain = str_concat(plain, table[(int)((row1 * 5LL) + ((col1 + 4LL) % 5LL))]);
            plain = str_concat(plain, table[(int)((row2 * 5LL) + ((col2 + 4LL) % 5LL))]);
        } else {
            if (col1 == col2) {
                plain = str_concat(plain, table[(int)((((row1 + 4LL) % 5LL) * 5LL) + col1)]);
                plain = str_concat(plain, table[(int)((((row2 + 4LL) % 5LL) * 5LL) + col2)]);
            } else {
                plain = str_concat(plain, table[(int)((row1 * 5LL) + col2)]);
                plain = str_concat(plain, table[(int)((row2 * 5LL) + col1)]);
            }
        }
        i = i + 2LL;
    }
    return plain;
}

void user_main() {
    printf("%s %s\n", "Encoded:", encode("BYE AND THANKS", "GREETING"));
    printf("%s %s\n", "Decoded:", decode("CXRBANRLBALQ", "GREETING"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
