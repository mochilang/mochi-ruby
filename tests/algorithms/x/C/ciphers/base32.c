// Generated by Mochi 0.10.32 on 2025-08-07 09:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* repeat(const char *s, int n) {
    size_t len = strlen(s);
    char *out = malloc(len * n + 1);
    for (int i = 0; i < n; i++) memcpy(out + i * len, s, len);
    out[len * n] = 0;
    return out;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* B32_CHARSET = "ABCDEFGHIJKLMNOPQRSTUVWXYZ234567";

long long indexOfChar(const char* s, const char* ch);
long long ord(const char* ch);
const char* chr(long long code);
const char* user_repeat(const char* s, long long n);
const char* to_binary(long long n, long long bits);
long long binary_to_int(const char* bits);
const char* base32_encode(const char* data);
const char* base32_decode(const char* data);
int main(void);

long long indexOfChar(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long ord(const char* ch) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    const char* digits = "0123456789";
    long long idx = indexOfChar(upper, ch);
    if (idx >= 0LL) {
        return 65LL + idx;
    }
    idx = indexOfChar(lower, ch);
    if (idx >= 0LL) {
        return 97LL + idx;
    }
    idx = indexOfChar(digits, ch);
    if (idx >= 0LL) {
        return 48LL + idx;
    }
    if (strcmp(ch, " ") == 0) {
        return 32LL;
    }
    if (strcmp(ch, "!") == 0) {
        return 33LL;
    }
    return 0LL;
}

const char* chr(long long code) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    const char* digits = "0123456789";
    if (code == 32LL) {
        return " ";
    }
    if (code == 33LL) {
        return "!";
    }
    long long idx = code - 65LL;
    if ((idx >= 0LL) && (idx < strlen(upper))) {
        return (const char[]){upper[idx], 0};
    }
    idx = code - 97LL;
    if ((idx >= 0LL) && (idx < strlen(lower))) {
        return (const char[]){lower[idx], 0};
    }
    idx = code - 48LL;
    if ((idx >= 0LL) && (idx < strlen(digits))) {
        return (const char[]){digits[idx], 0};
    }
    return "";
}

const char* user_repeat(const char* s, long long n) {
    const char* out = "";
    long long i = 0LL;
    while (i < n) {
        out = str_concat(out, s);
        i = i + 1LL;
    }
    return out;
}

const char* to_binary(long long n, long long bits) {
    long long v = n;
    const char* out = "";
    long long i = 0LL;
    while (i < bits) {
        out = str_concat(str_int(v % 2LL), out);
        v = v / 2LL;
        i = i + 1LL;
    }
    return out;
}

long long binary_to_int(const char* bits) {
    long long n = 0LL;
    long long i = 0LL;
    while (i < strlen(bits)) {
        n = n * 2LL;
        if (strcmp((const char[]){bits[i], 0}, "1") == 0) {
            n = n + 1LL;
        }
        i = i + 1LL;
    }
    return n;
}

const char* base32_encode(const char* data) {
    const char* binary_data = "";
    long long i = 0LL;
    while (i < strlen(data)) {
        binary_data = str_concat(binary_data, to_binary(ord((const char[]){data[i], 0}), 8LL));
        i = i + 1LL;
    }
    long long remainder = strlen(binary_data) % 5LL;
    if (remainder != 0LL) {
        binary_data = str_concat(binary_data, repeat("0", 5LL - remainder));
    }
    const char* b32_result = "";
    long long j = 0LL;
    while (j < strlen(binary_data)) {
        const char* chunk = _substring(binary_data, j, j + 5LL);
        long long index = binary_to_int(chunk);
        b32_result = str_concat(b32_result, (const char[]){B32_CHARSET[index], 0});
        j = j + 5LL;
    }
    long long rem = strlen(b32_result) % 8LL;
    if (rem != 0LL) {
        b32_result = str_concat(b32_result, repeat("=", 8LL - rem));
    }
    return b32_result;
}

const char* base32_decode(const char* data) {
    const char* clean = "";
    long long i = 0LL;
    while (i < strlen(data)) {
        const char* ch = (const char[]){data[i], 0};
        if (strcmp(ch, "=") != 0) {
            clean = str_concat(clean, ch);
        }
        i = i + 1LL;
    }
    const char* binary_chunks = "";
    i = 0LL;
    while (i < strlen(clean)) {
        long long idx = indexOfChar(B32_CHARSET, (const char[]){clean[i], 0});
        binary_chunks = str_concat(binary_chunks, to_binary(idx, 5LL));
        i = i + 1LL;
    }
    const char* result = "";
    long long j = 0LL;
    while ((j + 8LL) <= strlen(binary_chunks)) {
        const char* byte_bits = _substring(binary_chunks, j, j + 8LL);
        long long code = binary_to_int(byte_bits);
        result = str_concat(result, chr(code));
        j = j + 8LL;
    }
    return result;
}

int main(void) {
    {
        long long __start = _now();
        puts(base32_encode("Hello World!"));
        puts(base32_encode("123456"));
        puts(base32_encode("some long complex string"));
        puts(base32_decode("JBSWY3DPEBLW64TMMQQQ===="));
        puts(base32_decode("GEZDGNBVGY======"));
        puts(base32_decode("ONXW2ZJANRXW4ZZAMNXW24DMMV4CA43UOJUW4ZY="));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
