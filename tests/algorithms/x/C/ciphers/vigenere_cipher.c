// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* LETTERS_LOWER = "abcdefghijklmnopqrstuvwxyz";
const char* key = "HDarji";
const char* message = "This is Harshil Darji from Dharmaj.";
const char* key_up = "";
const char* encrypted = "";
long long key_index = 0LL;
long long i = 0LL;
const char* decrypted = "";

long long find_index(const char* s, const char* ch);
const char* to_upper_char(const char* ch);
const char* to_lower_char(const char* ch);
long long is_upper(const char* ch);
const char* to_upper_string(const char* s);
int main(void);

long long find_index(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* to_upper_char(const char* ch) {
    long long idx = find_index(LETTERS_LOWER, ch);
    if (idx >= 0LL) {
        return (const char[]){LETTERS[idx], 0};
    }
    return ch;
}

const char* to_lower_char(const char* ch) {
    long long idx = find_index(LETTERS, ch);
    if (idx >= 0LL) {
        return (const char[]){LETTERS_LOWER[idx], 0};
    }
    return ch;
}

long long is_upper(const char* ch) {
    return find_index(LETTERS, ch) >= 0LL;
}

const char* to_upper_string(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        res = str_concat(res, to_upper_char((const char[]){s[i], 0}));
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        key_up = to_upper_string(key);
        while (i < strlen(message)) {
            const char* symbol = (const char[]){message[i], 0};
            const char* upper_symbol = to_upper_char(symbol);
            long long num = find_index(LETTERS, upper_symbol);
            if (num >= 0LL) {
                num = num + find_index(LETTERS, (const char[]){key_up[key_index], 0});
                num = num % strlen(LETTERS);
                if (is_upper(symbol)) {
                    encrypted = str_concat(encrypted, (const char[]){LETTERS[num], 0});
                } else {
                    encrypted = str_concat(encrypted, to_lower_char((const char[]){LETTERS[num], 0}));
                }
                key_index = key_index + 1LL;
                if (key_index == strlen(key_up)) {
                    key_index = 0LL;
                }
            } else {
                encrypted = str_concat(encrypted, symbol);
            }
            i = i + 1LL;
        }
        puts(encrypted);
        key_index = 0LL;
        i = 0LL;
        while (i < strlen(encrypted)) {
            const char* symbol = (const char[]){encrypted[i], 0};
            const char* upper_symbol = to_upper_char(symbol);
            long long num = find_index(LETTERS, upper_symbol);
            if (num >= 0LL) {
                num = num - find_index(LETTERS, (const char[]){key_up[key_index], 0});
                num = num % strlen(LETTERS);
                if (is_upper(symbol)) {
                    decrypted = str_concat(decrypted, (const char[]){LETTERS[num], 0});
                } else {
                    decrypted = str_concat(decrypted, to_lower_char((const char[]){LETTERS[num], 0}));
                }
                key_index = key_index + 1LL;
                if (key_index == strlen(key_up)) {
                    key_index = 0LL;
                }
            } else {
                decrypted = str_concat(decrypted, symbol);
            }
            i = i + 1LL;
        }
        puts(decrypted);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
