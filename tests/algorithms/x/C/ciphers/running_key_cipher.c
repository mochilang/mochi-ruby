// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* key = "How does the duck know that? said Victor";
const char* plaintext = "DEFEND THIS";
const char* ciphertext = "";

long long indexOf(const char* s, const char* ch);
long long ord(const char* ch);
const char* chr(long long n);
const char* clean_text(const char* s);
const char* running_key_encrypt(const char* key, const char* plaintext);
const char* running_key_decrypt(const char* key, const char* ciphertext);
int main(void);

long long indexOf(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long ord(const char* ch) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    long long idx = indexOf(upper, ch);
    if (idx >= 0LL) {
        return 65LL + idx;
    }
    idx = indexOf(lower, ch);
    if (idx >= 0LL) {
        return 97LL + idx;
    }
    return 0LL;
}

const char* chr(long long n) {
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    if ((n >= 65LL) && (n < 91LL)) {
        return _substring(upper, n - 65LL, n - 64LL);
    }
    if ((n >= 97LL) && (n < 123LL)) {
        return _substring(lower, n - 97LL, n - 96LL);
    }
    return "?";
}

const char* clean_text(const char* s) {
    const char* out = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        if ((strcmp(ch, "A") >= 0) && (strcmp(ch, "Z") <= 0)) {
            out = str_concat(out, ch);
        } else {
            if ((strcmp(ch, "a") >= 0) && (strcmp(ch, "z") <= 0)) {
                out = str_concat(out, chr(ord(ch) - 32LL));
            }
        }
        i = i + 1LL;
    }
    return out;
}

const char* running_key_encrypt(const char* key, const char* plaintext) {
    const char* pt = clean_text(plaintext);
    const char* k = clean_text(key);
    long long key_len = strlen(k);
    const char* res = "";
    long long ord_a = ord("A");
    long long i = 0LL;
    while (i < strlen(pt)) {
        long long p = ord((const char[]){pt[i], 0}) - ord_a;
        long long kv = ord((const char[]){k[i % key_len], 0}) - ord_a;
        long long c = (p + kv) % 26LL;
        res = str_concat(res, chr(c + ord_a));
        i = i + 1LL;
    }
    return res;
}

const char* running_key_decrypt(const char* key, const char* ciphertext) {
    const char* ct = clean_text(ciphertext);
    const char* k = clean_text(key);
    long long key_len = strlen(k);
    const char* res = "";
    long long ord_a = ord("A");
    long long i = 0LL;
    while (i < strlen(ct)) {
        long long c = ord((const char[]){ct[i], 0}) - ord_a;
        long long kv = ord((const char[]){k[i % key_len], 0}) - ord_a;
        long long p = ((c - kv) + 26LL) % 26LL;
        res = str_concat(res, chr(p + ord_a));
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        ciphertext = running_key_encrypt(key, plaintext);
        puts(ciphertext);
        puts(running_key_decrypt(key, ciphertext));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
