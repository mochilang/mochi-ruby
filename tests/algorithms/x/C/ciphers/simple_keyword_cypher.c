// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_cipher_map_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* *cipher_map = NULL;
size_t cipher_map_len = 0;
const char* encoded = "";

long long index_in_string(const char* s, const char* ch);
long long contains_char(const char* s, const char* ch);
long long is_alpha(const char* ch);
const char* to_upper(const char* s);
const char* remove_duplicates(const char* key);
const char* * create_cipher_map(const char* key);
long long index_in_list(const char* * lst, size_t lst_len, const char* ch);
const char* encipher(const char* message, const char* * cipher, size_t cipher_len);
const char* decipher(const char* message, const char* * cipher, size_t cipher_len);
int main(void);

long long index_in_string(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long contains_char(const char* s, const char* ch) {
    return index_in_string(s, ch) >= 0LL;
}

long long is_alpha(const char* ch) {
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    return contains_char(lower, ch) || contains_char(upper, ch);
}

const char* to_upper(const char* s) {
    const char* lower = "abcdefghijklmnopqrstuvwxyz";
    const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        long long idx = index_in_string(lower, ch);
        if (idx >= 0LL) {
            res = str_concat(res, (const char[]){upper[idx], 0});
        } else {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

const char* remove_duplicates(const char* key) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(key)) {
        const char* ch = (const char[]){key[i], 0};
        if ((strcmp(ch, " ") == 0) || (is_alpha(ch) && (contains_char(res, ch) == 0LL))) {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

const char* * create_cipher_map(const char* key) {
    const char* alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* cleaned = remove_duplicates(to_upper(key));
    const char* *cipher = NULL;
    size_t cipher_len = 0;
    long long i = 0LL;
    while (i < strlen(cleaned)) {
        cipher = list_append_str(cipher, &cipher_len, (const char[]){cleaned[i], 0});
        i = i + 1LL;
    }
    long long offset = strlen(cleaned);
    long long j = cipher_len;
    while (j < 26LL) {
        const char* char_ = (const char[]){alphabet[j - offset], 0};
        while (contains_char(cleaned, char_)) {
            offset = offset - 1LL;
            char_ = (const char[]){alphabet[j - offset], 0};
        }
        cipher = list_append_str(cipher, &cipher_len, char_);
        j = j + 1LL;
    }
    return create_cipher_map_len = cipher_len, cipher;
}

long long index_in_list(const char* * lst, size_t lst_len, const char* ch) {
    long long i = 0LL;
    while (i < lst_len) {
        if (strcmp(lst[(int)(i)], ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* encipher(const char* message, const char* * cipher, size_t cipher_len) {
    const char* alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* msg = to_upper(message);
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(msg)) {
        const char* ch = (const char[]){msg[i], 0};
        long long idx = index_in_string(alphabet, ch);
        if (idx >= 0LL) {
            res = str_concat(res, cipher[(int)(idx)]);
        } else {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

const char* decipher(const char* message, const char* * cipher, size_t cipher_len) {
    const char* alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    const char* msg = to_upper(message);
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(msg)) {
        const char* ch = (const char[]){msg[i], 0};
        long long idx = index_in_list(cipher, cipher_len, ch);
        if (idx >= 0LL) {
            res = str_concat(res, (const char[]){alphabet[idx], 0});
        } else {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        cipher_map = create_cipher_map("Goodbye!!");
        cipher_map_len = create_cipher_map_len;
        encoded = encipher("Hello World!!", cipher_map, cipher_map_len);
        puts(encoded);
        puts(decipher(encoded, cipher_map, cipher_map_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
