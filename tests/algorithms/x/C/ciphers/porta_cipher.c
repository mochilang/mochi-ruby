// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t table_for_len;
size_t generate_table_len;
size_t *generate_table_lens;
size_t get_position_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* UPPER = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* LOWER = "abcdefghijklmnopqrstuvwxyz";
const char* BASE_TOP = "ABCDEFGHIJKLM";
const char* BASE_BOTTOM = "NOPQRSTUVWXYZ";

const char* to_upper(const char* s);
long long char_index(const char* c);
const char* rotate_right(const char* s, long long k);
const char* * table_for(const char* c);
const char* * * generate_table(const char* key);
long long str_index(const char* s, const char* ch);
long long * get_position(const char* * table, size_t table_len, const char* ch);
const char* get_opponent(const char* * table, size_t table_len, const char* ch);
const char* encrypt(const char* key, const char* words);
const char* decrypt(const char* key, const char* words);
void user_main();
int main(void);

const char* to_upper(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        long long j = 0LL;
        long long replaced = 0LL;
        while (j < strlen(LOWER)) {
            if (strcmp(_substring(LOWER, j, j + 1LL), ch) == 0) {
                res = str_concat(res, _substring(UPPER, j, j + 1LL));
                replaced = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (!(replaced)) {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

long long char_index(const char* c) {
    long long i = 0LL;
    while (i < strlen(UPPER)) {
        if (strcmp(_substring(UPPER, i, i + 1LL), c) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* rotate_right(const char* s, long long k) {
    long long n = strlen(s);
    long long shift = k % n;
    return str_concat(_substring(s, n - shift, n), _substring(s, 0LL, n - shift));
}

const char* * table_for(const char* c) {
    long long idx = char_index(c);
    long long shift = idx / 2LL;
    const char* row1 = rotate_right(BASE_BOTTOM, shift);
    const char* *pair = NULL;
    size_t pair_len = 0;
    pair = list_append_str(pair, &pair_len, BASE_TOP);
    pair = list_append_str(pair, &pair_len, row1);
    return table_for_len = pair_len, pair;
}

const char* * * generate_table(const char* key) {
    const char* up = to_upper(key);
    long long i = 0LL;
    const char* **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    while (i < strlen(up)) {
        const char* ch = _substring(up, i, i + 1LL);
        const char* *pair = table_for(ch);
        size_t pair_len = table_for_len;
        result = list_append_strptr(result, &result_len, pair);
        result_lens = list_append_szt(result_lens, &result_lens_len, pair_len);
        i = i + 1LL;
    }
    return generate_table_lens = result_lens, generate_table_len = result_len, result;
}

long long str_index(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long * get_position(const char* * table, size_t table_len, const char* ch) {
    long long row = 0LL;
    if (str_index(table[(int)(0LL)], ch) == (0LL - 1LL)) {
        row = 1LL;
    }
    long long col = str_index(table[(int)(row)], ch);
    {
        static long long __tmp0_arr[2];
        __tmp0_arr[0] = row;
        __tmp0_arr[1] = col;
        get_position_len = 2;
        return __tmp0_arr;
    }
}

const char* get_opponent(const char* * table, size_t table_len, const char* ch) {
    long long *pos = get_position(table, table_len, ch);
    size_t pos_len = get_position_len;
    long long row = pos[(int)(0LL)];
    long long col = pos[(int)(1LL)];
    if (col == (0LL - 1LL)) {
        return ch;
    }
    if (row == 1LL) {
        return _substring(table[(int)(0LL)], col, col + 1LL);
    }
    return _substring(table[(int)(1LL)], col, col + 1LL);
}

const char* encrypt(const char* key, const char* words) {
    const char* **table = generate_table(key);
    size_t table_len = generate_table_len;
    size_t *table_lens = generate_table_lens;
    size_t table_lens_len = generate_table_len;
    const char* up_words = to_upper(words);
    const char* cipher = "";
    long long count = 0LL;
    long long i = 0LL;
    while (i < strlen(up_words)) {
        const char* ch = _substring(up_words, i, i + 1LL);
        cipher = str_concat(cipher, get_opponent(table[(int)(count)], table_lens[count], ch));
        count = (count + 1LL) % table_len;
        i = i + 1LL;
    }
    return cipher;
}

const char* decrypt(const char* key, const char* words) {
    const char* res = encrypt(key, words);
    return res;
}

void user_main() {
    puts(encrypt("marvin", "jessica"));
    puts(decrypt("marvin", "QRACRWU"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
