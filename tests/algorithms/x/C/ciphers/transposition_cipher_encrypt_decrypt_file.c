// Generated by Mochi 0.10.32 on 2025-08-07 16:48 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long key = 6LL;
const char* message = "Harshil Darji";
const char* encrypted = "";
const char* decrypted = "";

const char* encrypt_message(long long key, const char* message);
const char* decrypt_message(long long key, const char* message);
int main(void);

const char* encrypt_message(long long key, const char* message) {
    const char* result = "";
    long long col = 0LL;
    while (col < key) {
        long long pointer = col;
        while (pointer < strlen(message)) {
            result = str_concat(result, (const char[]){message[pointer], 0});
            pointer = pointer + key;
        }
        col = col + 1LL;
    }
    return result;
}

const char* decrypt_message(long long key, const char* message) {
    long long msg_len = strlen(message);
    long long num_cols = msg_len / key;
    if ((msg_len % key) != 0LL) {
        num_cols = num_cols + 1LL;
    }
    long long num_rows = key;
    long long num_shaded_boxes = (num_cols * num_rows) - msg_len;
    const char* *plain = NULL;
    size_t plain_len = 0;
    long long i = 0LL;
    while (i < num_cols) {
        plain = list_append_str(plain, &plain_len, "");
        i = i + 1LL;
    }
    long long col = 0LL;
    long long row = 0LL;
    long long idx = 0LL;
    while (idx < msg_len) {
        const char* ch = (const char[]){message[idx], 0};
        plain[(int)(col)] = str_concat(plain[(int)(col)], ch);
        col = col + 1LL;
        if ((col == num_cols) || ((col == (num_cols - 1LL)) && (row >= (num_rows - num_shaded_boxes)))) {
            col = 0LL;
            row = row + 1LL;
        }
        idx = idx + 1LL;
    }
    const char* result = "";
    i = 0LL;
    while (i < num_cols) {
        result = str_concat(result, plain[(int)(i)]);
        i = i + 1LL;
    }
    return result;
}

int main(void) {
    {
        long long __start = _now();
        encrypted = encrypt_message(key, message);
        puts(encrypted);
        decrypted = decrypt_message(key, encrypted);
        puts(decrypted);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
