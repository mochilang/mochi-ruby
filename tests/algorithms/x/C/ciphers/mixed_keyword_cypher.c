// Generated by Mochi 0.10.32 on 2025-08-07 11:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* UPPER = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* LOWER = "abcdefghijklmnopqrstuvwxyz";

const char* to_upper(const char* s);
long long contains(const char* * xs, size_t xs_len, const char* x);
long long contains_char(const char* s, const char* ch);
const char* get_value(const char* * keys, size_t keys_len, const char* * values, size_t values_len, const char* key);
void print_mapping(const char* * keys, size_t keys_len, const char* * values, size_t values_len);
const char* mixed_keyword(const char* keyword, const char* plaintext, long long verbose);
int main(void);

const char* to_upper(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        long long j = 0LL;
        long long found = 0LL;
        while (j < 26LL) {
            if (strcmp(ch, (const char[]){LOWER[j], 0}) == 0) {
                res = str_concat(res, (const char[]){UPPER[j], 0});
                found = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (found == 0LL) {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

long long contains(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)(i)], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long contains_char(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* get_value(const char* * keys, size_t keys_len, const char* * values, size_t values_len, const char* key) {
    long long i = 0LL;
    while (i < keys_len) {
        if (strcmp(keys[(int)(i)], key) == 0) {
            return values[(int)(i)];
        }
        i = i + 1LL;
    }
    return NULL;
}

void print_mapping(const char* * keys, size_t keys_len, const char* * values, size_t values_len) {
    const char* s = "{";
    long long i = 0LL;
    while (i < keys_len) {
        s = str_concat(str_concat(str_concat(str_concat(str_concat(s, "'"), keys[(int)(i)]), "': '"), values[(int)(i)]), "'");
        if ((i + 1LL) < keys_len) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "}");
    puts(s);
}

const char* mixed_keyword(const char* keyword, const char* plaintext, long long verbose) {
    const char* alphabet = UPPER;
    const char* keyword_u = to_upper(keyword);
    const char* plaintext_u = to_upper(plaintext);
    const char* *unique = NULL;
    size_t unique_len = 0;
    long long i = 0LL;
    while (i < strlen(keyword_u)) {
        const char* ch = (const char[]){keyword_u[i], 0};
        if (contains_char(alphabet, ch) && (contains(unique, unique_len, ch) == 0LL)) {
            unique = list_append_str(unique, &unique_len, ch);
        }
        i = i + 1LL;
    }
    long long num_unique = unique_len;
    const char* *shifted = NULL;
    size_t shifted_len = 0;
    i = 0LL;
    while (i < unique_len) {
        shifted = list_append_str(shifted, &shifted_len, unique[(int)(i)]);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < strlen(alphabet)) {
        const char* ch = (const char[]){alphabet[i], 0};
        if (contains(unique, unique_len, ch) == 0LL) {
            shifted = list_append_str(shifted, &shifted_len, ch);
        }
        i = i + 1LL;
    }
    const char* **modified = NULL;
    size_t modified_len = 0;
    size_t *modified_lens = NULL;
    size_t modified_lens_len = 0;
    long long k = 0LL;
    while (k < shifted_len) {
        const char* *row = NULL;
        size_t row_len = 0;
        long long r = 0LL;
        while ((r < num_unique) && ((k + r) < shifted_len)) {
            row = list_append_str(row, &row_len, shifted[(int)(k + r)]);
            r = r + 1LL;
        }
        modified = list_append_strptr(modified, &modified_len, row);
        modified_lens = list_append_szt(modified_lens, &modified_lens_len, row_len);
        k = k + num_unique;
    }
    const char* *keys = NULL;
    size_t keys_len = 0;
    const char* *values = NULL;
    size_t values_len = 0;
    long long column = 0LL;
    long long letter_index = 0LL;
    while (column < num_unique) {
        long long row_idx = 0LL;
        while (row_idx < modified_len) {
            const char* *row = modified[(int)(row_idx)];
            size_t row_len = modified_lens[row_idx];
            if (row_len <= column) {
                break;
            }
            keys = list_append_str(keys, &keys_len, (const char[]){alphabet[letter_index], 0});
            values = list_append_str(values, &values_len, row[(int)(column)]);
            letter_index = letter_index + 1LL;
            row_idx = row_idx + 1LL;
        }
        column = column + 1LL;
    }
    if (verbose) {
        print_mapping(keys, keys_len, values, values_len);
    }
    const char* result = "";
    i = 0LL;
    while (i < strlen(plaintext_u)) {
        const char* ch = (const char[]){plaintext_u[i], 0};
        const char* mapped = get_value(keys, keys_len, values, values_len, ch);
        if (mapped == NULL) {
            result = str_concat(result, ch);
        } else {
            result = str_concat(result, mapped);
        }
        i = i + 1LL;
    }
    return result;
}

int main(void) {
    {
        long long __start = _now();
        puts(mixed_keyword("college", "UNIVERSITY", 1LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
