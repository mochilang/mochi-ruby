// Generated by Mochi 0.10.32 on 2025-08-06 20:15 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long contains(const char* * words, size_t words_len, const char* target);
long long backtrack(const char* s, const char* * word_dict, size_t word_dict_len, long long start);
long long word_break(const char* s, const char* * word_dict, size_t word_dict_len);
int main(void);

long long contains(const char* * words, size_t words_len, const char* target) {
    for (size_t __i = 0; __i < words_len; __i++) {
        const char* w = words[__i];
        if (strcmp(w, target) == 0) {
            return 1LL;
        }
    }
    return 0LL;
}

long long backtrack(const char* s, const char* * word_dict, size_t word_dict_len, long long start) {
    if (start == strlen(s)) {
        return 1LL;
    }
    long long end = start + 1LL;
    while (end <= strlen(s)) {
        const char* substr = _substring(s, start, end);
        if (strstr(word_dict, substr) != NULL && backtrack(s, word_dict, word_dict_len, end)) {
            return 1LL;
        }
        end = end + 1LL;
    }
    return 0LL;
}

long long word_break(const char* s, const char* * word_dict, size_t word_dict_len) {
    return backtrack(s, word_dict, word_dict_len, 0LL);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(word_break("leetcode", (const char*[]){ "leet", "code" }, 2)));
        puts(str_int(word_break("applepenapple", (const char*[]){ "apple", "pen" }, 2)));
        puts(str_int(word_break("catsandog", (const char*[]){ "cats", "dog", "sand", "and", "cat" }, 5)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
