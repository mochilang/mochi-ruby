// Generated by Mochi 0.10.32 on 2025-08-06 16:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_board_len;
size_t *create_board_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * create_board(long long n);
long long is_safe(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long row, long long column);
const char* row_string(long long * row, size_t row_len);
void printboard(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len);
long long solve(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long row);
long long n_queens(long long n);
int main(void);

long long * * create_board(long long n) {
    long long **board = NULL;
    size_t board_len = 0;
    size_t *board_lens = NULL;
    size_t board_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        board = list_append_long_longptr(board, &board_len, row);
        board_lens = list_append_szt(board_lens, &board_lens_len, row_len);
        i = i + 1LL;
    }
    return create_board_lens = board_lens, create_board_len = board_len, board;
}

long long is_safe(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long row, long long column) {
    size_t n = board_len;
    long long i = 0LL;
    while (i < row) {
        if (board[(int)(i)][(int)(column)] == 1LL) {
            return 0LL;
        }
        i = i + 1LL;
    }
    i = row - 1LL;
    long long j = column - 1LL;
    while ((i >= 0LL) && (j >= 0LL)) {
        if (board[(int)(i)][(int)(j)] == 1LL) {
            return 0LL;
        }
        i = i - 1LL;
        j = j - 1LL;
    }
    i = row - 1LL;
    j = column + 1LL;
    while ((i >= 0LL) && (j < n)) {
        if (board[(int)(i)][(int)(j)] == 1LL) {
            return 0LL;
        }
        i = i - 1LL;
        j = j + 1LL;
    }
    return 1LL;
}

const char* row_string(long long * row, size_t row_len) {
    const char* s = "";
    long long j = 0LL;
    while (j < row_len) {
        if (row[(int)(j)] == 1LL) {
            s = str_concat(s, "Q ");
        } else {
            s = str_concat(s, ". ");
        }
        j = j + 1LL;
    }
    return s;
}

void printboard(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len) {
    long long i = 0LL;
    while (i < board_len) {
        puts(row_string(board[(int)(i)], board_lens[i]));
        i = i + 1LL;
    }
}

long long solve(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long row) {
    if (row >= board_len) {
        printboard(board, board_len, board_lens, board_len);
        puts("");
        return 1LL;
    }
    long long count = 0LL;
    long long i = 0LL;
    while (i < board_len) {
        if (is_safe(board, board_len, board_lens, board_len, row, i)) {
            board[(int)(row)][(int)(i)] = 1LL;
            count = count + solve(board, board_len, board_lens, board_len, row + 1LL);
            board[(int)(row)][(int)(i)] = 0LL;
        }
        i = i + 1LL;
    }
    return count;
}

long long n_queens(long long n) {
    long long **board = create_board(n);
    size_t board_len = create_board_len;
    size_t *board_lens = create_board_lens;
    size_t board_lens_len = create_board_len;
    long long total = solve(board, board_len, board_lens, board_len, 0LL);
    puts(str_concat("The total number of solutions are: ", str_int(total)));
    return total;
}

int main(void) {
    {
        long long __start = _now();
        n_queens(4LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
