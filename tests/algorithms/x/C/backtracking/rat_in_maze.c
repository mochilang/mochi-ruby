// Generated by Mochi 0.10.32 on 2025-08-06 17:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t solve_maze_len;
size_t *solve_maze_lens;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long maze_0[5] = {0LL, 1LL, 0LL, 1LL, 1LL};
long long maze_1[5] = {0LL, 0LL, 0LL, 0LL, 0LL};
long long maze_2[5] = {1LL, 0LL, 1LL, 0LL, 1LL};
long long maze_3[5] = {0LL, 0LL, 1LL, 0LL, 0LL};
long long maze_4[5] = {1LL, 0LL, 0LL, 1LL, 0LL};
long long *maze_init[5] = {maze_0, maze_1, maze_2, maze_3, maze_4};
long long **maze = maze_init;
size_t maze_len = 5;
size_t maze_lens_init[5] = {5, 5, 5, 5, 5};
size_t *maze_lens = maze_lens_init;
size_t maze_lens_len = 5;
long long n = 0;

long long run_maze(long long * * maze, size_t maze_len, size_t* maze_lens, size_t maze_lens_len, long long i, long long j, long long dr, long long dc, long long * * sol, size_t sol_len, size_t* sol_lens, size_t sol_lens_len);
long long * * solve_maze(long long * * maze, size_t maze_len, size_t* maze_lens, size_t maze_lens_len, long long sr, long long sc, long long dr, long long dc);
int main(void);

long long run_maze(long long * * maze, size_t maze_len, size_t* maze_lens, size_t maze_lens_len, long long i, long long j, long long dr, long long dc, long long * * sol, size_t sol_len, size_t* sol_lens, size_t sol_lens_len) {
    size_t size = maze_len;
    if (((i == dr) && (j == dc)) && (maze[(int)(i)][(int)(j)] == 0LL)) {
        sol[(int)(i)][(int)(j)] = 0LL;
        return 1LL;
    }
    long long lower_flag = (i >= 0LL) && (j >= 0LL);
    long long upper_flag = (i < size) && (j < size);
    if (lower_flag && upper_flag) {
        long long block_flag = (sol[(int)(i)][(int)(j)] == 1LL) && (maze[(int)(i)][(int)(j)] == 0LL);
        if (block_flag) {
            sol[(int)(i)][(int)(j)] = 0LL;
            if (((run_maze(maze, maze_len, maze_lens, maze_len, i + 1LL, j, dr, dc, sol, sol_len, sol_lens, sol_len) || run_maze(maze, maze_len, maze_lens, maze_len, i, j + 1LL, dr, dc, sol, sol_len, sol_lens, sol_len)) || run_maze(maze, maze_len, maze_lens, maze_len, i - 1LL, j, dr, dc, sol, sol_len, sol_lens, sol_len)) || run_maze(maze, maze_len, maze_lens, maze_len, i, j - 1LL, dr, dc, sol, sol_len, sol_lens, sol_len)) {
                return 1LL;
            }
            sol[(int)(i)][(int)(j)] = 1LL;
            return 0LL;
        }
    }
    return 0LL;
}

long long * * solve_maze(long long * * maze, size_t maze_len, size_t* maze_lens, size_t maze_lens_len, long long sr, long long sc, long long dr, long long dc) {
    size_t size = maze_len;
    if (!((((((((0LL <= sr) && (sr < size)) && (0LL <= sc)) && (sc < size)) && (0LL <= dr)) && (dr < size)) && (0LL <= dc)) && (dc < size))) {
        panic("Invalid source or destination coordinates");
    }
    long long **sol = NULL;
    size_t sol_len = 0;
    size_t *sol_lens = NULL;
    size_t sol_lens_len = 0;
    long long i = 0LL;
    while (i < size) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < size) {
            row = list_append_long_long(row, &row_len, 1LL);
            j = j + 1LL;
        }
        sol = list_append_long_longptr(sol, &sol_len, row);
        sol_lens = list_append_szt(sol_lens, &sol_lens_len, row_len);
        i = i + 1LL;
    }
    long long solved = run_maze(maze, maze_len, maze_lens, maze_len, sr, sc, dr, dc, sol, sol_len, sol_lens, sol_len);
    if (solved) {
        return solve_maze_lens = sol_lens, solve_maze_len = sol_len, sol;
    } else {
        panic("No solution exists!");
    }
}

int main(void) {
    {
        long long __start = _now();
        n = maze_len - 1LL;
        puts(({long long **__tmp1 = solve_maze(maze, maze_len, maze_lens, maze_len, 0LL, 0LL, n, n); char *__res = str_list_list_int(__tmp1, solve_maze_len, solve_maze_lens); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
