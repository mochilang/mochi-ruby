// Generated by Mochi 0.10.32 on 2025-08-06 16:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t build_board_len;
size_t depth_first_search_len;
size_t *depth_first_search_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* repeat(const char *s, int n) {
    size_t len = strlen(s);
    char *out = malloc(len * n + 1);
    for (int i = 0; i < n; i++) memcpy(out + i * len, s, len);
    out[len * n] = 0;
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    return res;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char[]** list_append_c_char[]ptr(const char[] **arr, size_t *len, const char[] *val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char[]*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long contains(long long * xs, size_t xs_len, long long x);
const char* repeat(const char* s, long long times);
const char* * build_board(long long * pos, size_t pos_len, long long n);
const char* * * depth_first_search(long long * pos, size_t pos_len, long long * dr, size_t dr_len, long long * dl, size_t dl_len, long long n);
long long n_queens_solution(long long n);
int main(void);

long long contains(long long * xs, size_t xs_len, long long x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)(i)] == x) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* repeat(const char* s, long long times) {
    const char* result = "";
    long long i = 0LL;
    while (i < times) {
        result = str_concat(result, s);
        i = i + 1LL;
    }
    return result;
}

const char* * build_board(long long * pos, size_t pos_len, long long n) {
    const char* *board = NULL;
    size_t board_len = 0;
    long long i = 0LL;
    while (i < pos_len) {
        long long col = pos[(int)(i)];
        const char* line = str_concat(str_concat(repeat(". ", col), "Q "), repeat(". ", (n - 1LL) - col));
        board = list_append_str(board, &board_len, line);
        i = i + 1LL;
    }
    return build_board_len = board_len, board;
}

const char* * * depth_first_search(long long * pos, size_t pos_len, long long * dr, size_t dr_len, long long * dl, size_t dl_len, long long n) {
    size_t row = pos_len;
    if (row == n) {
        const char* **single = NULL;
        size_t single_len = 0;
        size_t *single_lens = NULL;
        size_t single_lens_len = 0;
        single = list_append_c_char[]ptr(single, &single_len, build_board(pos, pos_len, n));
        single_lens = list_append_szt(single_lens, &single_lens_len, build_board_len);
        return depth_first_search_lens = single_lens, depth_first_search_len = single_len, single;
    }
    const char* **boards = NULL;
    size_t boards_len = 0;
    size_t *boards_lens = NULL;
    size_t boards_lens_len = 0;
    long long col = 0LL;
    while (col < n) {
        if ((strstr(pos, col) != NULL || strstr(dr, row - col) != NULL) || strstr(dl, row + col) != NULL) {
            col = col + 1LL;
            continue;
        }
        const char* **result = depth_first_search(list_append_int_new(pos, pos_len, col), append_len, list_append_int_new(dr, dr_len, row - col), append_len, list_append_int_new(dl, dl_len, row + col), append_len, n);
        size_t result_len = depth_first_search_len;
        size_t *result_lens = depth_first_search_lens;
        size_t result_lens_len = depth_first_search_len;
        boards = concat(boards, result);
        col = col + 1LL;
    }
    return depth_first_search_lens = boards_lens, depth_first_search_len = boards_len, boards;
}

long long n_queens_solution(long long n) {
    const char[] **boards = depth_first_search(NULL, 0, NULL, 0, NULL, 0, n);
    size_t boards_len = depth_first_search_len;
    size_t *boards_lens = depth_first_search_lens;
    size_t boards_lens_len = depth_first_search_len;
    long long i = 0LL;
    while (i < boards_len) {
        long long j = 0LL;
        while (j < boards_lens[i]) {
            printf("%d\n", boards[(int)(i)][(int)(j)]);
            j = j + 1LL;
        }
        puts("");
        i = i + 1LL;
    }
    printf("%d %s\n", boards_len, "solutions were found.");
    return boards_len;
}

int main(void) {
    {
        long long __start = _now();
        n_queens_solution(4LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
