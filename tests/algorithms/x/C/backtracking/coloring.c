// Generated by Mochi 0.10.32 on 2025-08-06 16:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t color_len;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long graph_0[5] = {0LL, 1LL, 0LL, 0LL, 0LL};
long long graph_1[5] = {1LL, 0LL, 1LL, 0LL, 1LL};
long long graph_2[5] = {0LL, 1LL, 0LL, 1LL, 0LL};
long long graph_3[5] = {0LL, 1LL, 1LL, 0LL, 0LL};
long long graph_4[5] = {0LL, 1LL, 0LL, 0LL, 0LL};
long long *graph_init[5] = {graph_0, graph_1, graph_2, graph_3, graph_4};
long long **graph = graph_init;
size_t graph_len = 5;
size_t graph_lens_init[5] = {5, 5, 5, 5, 5};
size_t *graph_lens = graph_lens_init;
size_t graph_lens_len = 5;

long long valid_coloring(long long * neighbours, size_t neighbours_len, long long * colored_vertices, size_t colored_vertices_len, long long color);
long long util_color(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long max_colors, long long * colored_vertices, size_t colored_vertices_len, long long index);
long long * color(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long max_colors);
int main(void);

long long valid_coloring(long long * neighbours, size_t neighbours_len, long long * colored_vertices, size_t colored_vertices_len, long long color) {
    long long i = 0LL;
    while (i < neighbours_len) {
        if ((neighbours[(int)(i)] == 1LL) && (colored_vertices[(int)(i)] == color)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long util_color(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long max_colors, long long * colored_vertices, size_t colored_vertices_len, long long index) {
    if (index == graph_len) {
        return 1LL;
    }
    long long c = 0LL;
    while (c < max_colors) {
        if (valid_coloring(graph[(int)(index)], graph_lens[index], colored_vertices, colored_vertices_len, c)) {
            colored_vertices[(int)(index)] = c;
            if (util_color(graph, graph_len, graph_lens, graph_len, max_colors, colored_vertices, colored_vertices_len, index + 1LL)) {
                return 1LL;
            }
            colored_vertices[(int)(index)] = -1LL;
        }
        c = c + 1LL;
    }
    return 0LL;
}

long long * color(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long max_colors) {
    long long *colored_vertices = NULL;
    size_t colored_vertices_len = 0;
    long long i = 0LL;
    while (i < graph_len) {
        colored_vertices = list_append_long_long(colored_vertices, &colored_vertices_len, -1LL);
        i = i + 1LL;
    }
    if (util_color(graph, graph_len, graph_lens, graph_len, max_colors, colored_vertices, colored_vertices_len, 0LL)) {
        return color_len = colored_vertices_len, colored_vertices;
    }
    {
        static long long __tmp0_arr[0];
        color_len = 0;
        return __tmp0_arr;
    }
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", color(graph, graph_len, graph_lens, graph_len, 3LL));
        puts("\n");
        printf("%d\n", (color(graph, graph_len, graph_lens, graph_len, 2LL), color_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
