// Generated by Mochi 0.10.32 on 2025-08-06 20:15 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_state_space_tree_len;
size_t *create_state_space_tree_lens;
size_t generate_sum_of_subsets_solutions_len;
size_t *generate_sum_of_subsets_solutions_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static void json_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    printf("[\n");
    for (size_t i = 0; i < len; i++) {
        printf("  [\n");
        for (size_t j = 0; j < lens[i]; j++) {
            printf("    %lld", arr[i][j]);
            if (j + 1 < lens[i]) printf(",\n"); else printf("\n");
        }
        printf("  ]");
        if (i + 1 < len) printf(",\n"); else printf("\n");
    }
    printf("]");
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    return res;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long sum_list(long long * nums, size_t nums_len);
long long * * create_state_space_tree(long long * nums, size_t nums_len, long long max_sum, long long num_index, long long * path, size_t path_len, long long curr_sum, long long remaining_sum);
long long * * generate_sum_of_subsets_solutions(long long * nums, size_t nums_len, long long max_sum);
void user_main();
int main(void);

long long sum_list(long long * nums, size_t nums_len) {
    long long s = 0LL;
    for (size_t __i = 0; __i < nums_len; __i++) {
        long long n = nums[__i];
        s = s + n;
    }
    return s;
}

long long * * create_state_space_tree(long long * nums, size_t nums_len, long long max_sum, long long num_index, long long * path, size_t path_len, long long curr_sum, long long remaining_sum) {
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    if ((curr_sum > max_sum) || ((curr_sum + remaining_sum) < max_sum)) {
        return create_state_space_tree_lens = result_lens, create_state_space_tree_len = result_len, result;
    }
    if (curr_sum == max_sum) {
        result = list_append_long_longptr(result, &result_len, path);
        result_lens = list_append_szt(result_lens, &result_lens_len, path_len);
        return create_state_space_tree_lens = result_lens, create_state_space_tree_len = result_len, result;
    }
    long long index = num_index;
    while (index < nums_len) {
        long long value = nums[(int)(index)];
        long long **subres = create_state_space_tree(nums, nums_len, max_sum, index + 1LL, list_append_int_new(path, path_len, value), path_len + 1, curr_sum + value, remaining_sum - value);
        size_t subres_len = create_state_space_tree_len;
        size_t *subres_lens = create_state_space_tree_lens;
        size_t subres_lens_len = create_state_space_tree_len;
        long long j = 0LL;
        while (j < subres_len) {
            result = list_append_long_longptr(result, &result_len, subres[(int)(j)]);
            result_lens = list_append_szt(result_lens, &result_lens_len, subres_lens[j]);
            j = j + 1LL;
        }
        index = index + 1LL;
    }
    return create_state_space_tree_lens = result_lens, create_state_space_tree_len = result_len, result;
}

long long * * generate_sum_of_subsets_solutions(long long * nums, size_t nums_len, long long max_sum) {
    long long total = sum_list(nums, nums_len);
    {
        long long** __ret1 = create_state_space_tree(nums, nums_len, max_sum, 0LL, NULL, 0, 0LL, total);
        generate_sum_of_subsets_solutions_lens = create_state_space_tree_lens;
generate_sum_of_subsets_solutions_len = create_state_space_tree_len;
        return __ret1;
    }
}

void user_main() {
    long long** __tmp2 = generate_sum_of_subsets_solutions((long long[]){ 3LL, 34LL, 4LL, 12LL, 5LL, 2LL }, 6, 9LL);
    json_list_list_int(__tmp2, generate_sum_of_subsets_solutions_len, generate_sum_of_subsets_solutions_lens);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
