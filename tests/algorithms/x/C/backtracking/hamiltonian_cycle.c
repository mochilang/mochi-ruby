// Generated by Mochi 0.10.32 on 2025-08-06 16:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t hamilton_cycle_len;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long valid_connection(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long next_ver, long long curr_ind, long long * path, size_t path_len);
long long util_hamilton_cycle(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long * path, size_t path_len, long long curr_ind);
long long * hamilton_cycle(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long start_index);
int main(void);

long long valid_connection(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long next_ver, long long curr_ind, long long * path, size_t path_len) {
    if (graph[(int)(path[(int)(curr_ind - 1LL)])][(int)(next_ver)] == 0LL) {
        return 0LL;
    }
    for (size_t __i = 0; __i < path_len; __i++) {
        long long v = path[__i];
        if (strcmp(v, next_ver) == 0) {
            return 0LL;
        }
    }
    return 1LL;
}

long long util_hamilton_cycle(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long * path, size_t path_len, long long curr_ind) {
    if (curr_ind == graph_len) {
        return graph[(int)(path[(int)(curr_ind - 1LL)])][(int)(path[(int)(0LL)])] == 1LL;
    }
    long long next_ver = 0LL;
    while (next_ver < graph_len) {
        if (valid_connection(graph, graph_len, graph_lens, graph_len, next_ver, curr_ind, path, path_len)) {
            path[(int)(curr_ind)] = next_ver;
            if (util_hamilton_cycle(graph, graph_len, graph_lens, graph_len, path, path_len, curr_ind + 1LL)) {
                return 1LL;
            }
            path[(int)(curr_ind)] = -1LL;
        }
        next_ver = next_ver + 1LL;
    }
    return 0LL;
}

long long * hamilton_cycle(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long start_index) {
    long long *path = NULL;
    size_t path_len = 0;
    long long i = 0LL;
    while (i < (graph_len + 1LL)) {
        path[(int)(i)] = -1LL;
        i = i + 1LL;
    }
    path[(int)(0LL)] = start_index;
    long long last = path_len - 1LL;
    path[(int)(last)] = start_index;
    if (util_hamilton_cycle(graph, graph_len, graph_lens, graph_len, path, path_len, 1LL)) {
        return hamilton_cycle_len = path_len, path;
    }
    {
        static long long __tmp0_arr[0];
        hamilton_cycle_len = 0;
        return __tmp0_arr;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
