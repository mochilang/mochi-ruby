// Generated by Mochi 0.10.32 on 2025-08-06 16:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long minimax(long long depth, long long node_index, long long is_max, long long * scores, size_t scores_len, long long height);
long long tree_height(long long n);
void user_main();
int main(void);

long long minimax(long long depth, long long node_index, long long is_max, long long * scores, size_t scores_len, long long height) {
    if (depth < 0LL) {
        panic("Depth cannot be less than 0");
    }
    if (scores_len == 0LL) {
        panic("Scores cannot be empty");
    }
    if (depth == height) {
        return scores[(int)(node_index)];
    }
    if (is_max) {
        long long left = minimax(depth + 1LL, node_index * 2LL, 0LL, scores, scores_len, height);
        long long right = minimax(depth + 1LL, (node_index * 2LL) + 1LL, 0LL, scores, scores_len, height);
        if (left > right) {
            return left;
        } else {
            return right;
        }
    }
    long long left = minimax(depth + 1LL, node_index * 2LL, 1LL, scores, scores_len, height);
    long long right = minimax(depth + 1LL, (node_index * 2LL) + 1LL, 1LL, scores, scores_len, height);
    if (left < right) {
        return left;
    } else {
        return right;
    }
}

long long tree_height(long long n) {
    long long h = 0LL;
    long long v = n;
    while (v > 1LL) {
        v = v / 2LL;
        h = h + 1LL;
    }
    return h;
}

void user_main() {
    long long *scores = NULL;
    size_t scores_len = 0;
    scores = list_append_int(scores, &scores_len, 90LL);
    scores = list_append_int(scores, &scores_len, 23LL);
    scores = list_append_int(scores, &scores_len, 6LL);
    scores = list_append_int(scores, &scores_len, 33LL);
    scores = list_append_int(scores, &scores_len, 21LL);
    scores = list_append_int(scores, &scores_len, 65LL);
    scores = list_append_int(scores, &scores_len, 123LL);
    scores = list_append_int(scores, &scores_len, 34423LL);
    long long height = tree_height(8LL);
    puts(str_concat("Optimal value : ", str_int(minimax(0LL, 0LL, 1LL, scores, scores_len, height))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
