// Generated by Mochi 0.10.32 on 2025-08-06 17:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t find_empty_location_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long initial_grid_0[9] = {3LL, 0LL, 6LL, 5LL, 0LL, 8LL, 4LL, 0LL, 0LL};
long long initial_grid_1[9] = {5LL, 2LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long initial_grid_2[9] = {0LL, 8LL, 7LL, 0LL, 0LL, 0LL, 0LL, 3LL, 1LL};
long long initial_grid_3[9] = {0LL, 0LL, 3LL, 0LL, 1LL, 0LL, 0LL, 8LL, 0LL};
long long initial_grid_4[9] = {9LL, 0LL, 0LL, 8LL, 6LL, 3LL, 0LL, 0LL, 5LL};
long long initial_grid_5[9] = {0LL, 5LL, 0LL, 0LL, 9LL, 0LL, 6LL, 0LL, 0LL};
long long initial_grid_6[9] = {1LL, 3LL, 0LL, 0LL, 0LL, 0LL, 2LL, 5LL, 0LL};
long long initial_grid_7[9] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 7LL, 4LL};
long long initial_grid_8[9] = {0LL, 0LL, 5LL, 2LL, 0LL, 6LL, 3LL, 0LL, 0LL};
long long *initial_grid_init[9] = {initial_grid_0, initial_grid_1, initial_grid_2, initial_grid_3, initial_grid_4, initial_grid_5, initial_grid_6, initial_grid_7, initial_grid_8};
long long **initial_grid = initial_grid_init;
size_t initial_grid_len = 9;
size_t initial_grid_lens_init[9] = {9, 9, 9, 9, 9, 9, 9, 9, 9};
size_t *initial_grid_lens = initial_grid_lens_init;
size_t initial_grid_lens_len = 9;
long long no_solution_0[9] = {5LL, 0LL, 6LL, 5LL, 0LL, 8LL, 4LL, 0LL, 3LL};
long long no_solution_1[9] = {5LL, 2LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 2LL};
long long no_solution_2[9] = {1LL, 8LL, 7LL, 0LL, 0LL, 0LL, 0LL, 3LL, 1LL};
long long no_solution_3[9] = {0LL, 0LL, 3LL, 0LL, 1LL, 0LL, 0LL, 8LL, 0LL};
long long no_solution_4[9] = {9LL, 0LL, 0LL, 8LL, 6LL, 3LL, 0LL, 0LL, 5LL};
long long no_solution_5[9] = {0LL, 5LL, 0LL, 0LL, 9LL, 0LL, 6LL, 0LL, 0LL};
long long no_solution_6[9] = {1LL, 3LL, 0LL, 0LL, 0LL, 0LL, 2LL, 5LL, 0LL};
long long no_solution_7[9] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 7LL, 4LL};
long long no_solution_8[9] = {0LL, 0LL, 5LL, 2LL, 0LL, 6LL, 3LL, 0LL, 0LL};
long long *no_solution_init[9] = {no_solution_0, no_solution_1, no_solution_2, no_solution_3, no_solution_4, no_solution_5, no_solution_6, no_solution_7, no_solution_8};
long long **no_solution = no_solution_init;
size_t no_solution_len = 9;
size_t no_solution_lens_init[9] = {9, 9, 9, 9, 9, 9, 9, 9, 9};
size_t *no_solution_lens = no_solution_lens_init;
size_t no_solution_lens_len = 9;
long long ***examples = NULL;
size_t examples_len = 0;
size_t *examples_lens = NULL;
size_t examples_lens_len = 0;
long long idx = 0LL;

long long is_safe(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long row, long long column, long long n);
long long * find_empty_location(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
long long sudoku(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
const char* print_solution(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
int main(void);

long long is_safe(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long row, long long column, long long n) {
    for (int i = 0LL; i < 9LL; i++) {
        if ((grid[(int)(row)][(int)(i)] == n) || (grid[(int)(i)][(int)(column)] == n)) {
            return 0LL;
        }
    }
    for (int i = 0LL; i < 3LL; i++) {
        for (int j = 0LL; j < 3LL; j++) {
            if (grid[(int)((row - (row % 3LL)) + i)][(int)((column - (column % 3LL)) + j)] == n) {
                return 0LL;
            }
        }
    }
    return 1LL;
}

long long * find_empty_location(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    for (int i = 0LL; i < 9LL; i++) {
        for (int j = 0LL; j < 9LL; j++) {
            if (grid[(int)(i)][(int)(j)] == 0LL) {
                {
                    static long long __tmp0_arr[2];
                    __tmp0_arr[0] = i;
                    __tmp0_arr[1] = j;
                    find_empty_location_len = 2;
                    return __tmp0_arr;
                }
            }
        }
    }
    {
        static long long __tmp1_arr[0];
        find_empty_location_len = 0;
        return __tmp1_arr;
    }
}

long long sudoku(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long *loc = find_empty_location(grid, grid_len, grid_lens, grid_len);
    size_t loc_len = find_empty_location_len;
    if (loc_len == 0LL) {
        return 1LL;
    }
    long long row = loc[(int)(0LL)];
    long long column = loc[(int)(1LL)];
    for (int digit = 1LL; digit < 10LL; digit++) {
        if (is_safe(grid, grid_len, grid_lens, grid_len, row, column, digit)) {
            grid[(int)(row)][(int)(column)] = digit;
            if (sudoku(grid, grid_len, grid_lens, grid_len)) {
                return 1LL;
            }
            grid[(int)(row)][(int)(column)] = 0LL;
        }
    }
    return 0LL;
}

const char* print_solution(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    for (int r = 0LL; r < grid_len; r++) {
        const char* line = "";
        for (int c = 0LL; c < grid_lens[r]; c++) {
            line = str_concat(line, str_int(grid[(int)(r)][(int)(c)]));
            if (c < (grid_lens[r] - 1LL)) {
                line = str_concat(line, " ");
            }
        }
        puts(line);
    }
}

int main(void) {
    {
        long long __start = _now();
        examples = (long long**[]){ initial_grid, no_solution };
        examples_len = 2;
        examples_lens = (size_t[]){initial_grid_len, no_solution_len};
        examples_lens_len = 2;
        while (idx < examples_len) {
            puts("\nExample grid:\n====================");
            print_solution(examples[(int)(idx)], examples_lens[idx], NULL, examples_lens[idx]);
            puts("\nExample grid solution:");
            if (sudoku(examples[(int)(idx)], examples_lens[idx], NULL, examples_lens[idx])) {
                print_solution(examples[(int)(idx)], examples_lens[idx], NULL, examples_lens[idx]);
            } else {
                puts("Cannot find a solution.");
            }
            idx = idx + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
