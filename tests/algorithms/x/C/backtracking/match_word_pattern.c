// Generated by Mochi 0.10.32 on 2025-08-06 16:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static const char** list_append_str_new(const char **arr, size_t len, const char *val) {
    const char **res = malloc((len + 1) * sizeof(const char*));
    if (arr && len) memcpy(res, arr, len * sizeof(const char*));
    res[len] = val;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* get_value(const char* * keys, size_t keys_len, const char* * values, size_t values_len, const char* key);
long long contains_value(const char* * values, size_t values_len, const char* value);
long long backtrack(const char* pattern, const char* input_string, long long pi, long long si, const char* * keys, size_t keys_len, const char* * values, size_t values_len);
long long match_word_pattern(const char* pattern, const char* input_string);
void user_main();
int main(void);

const char* get_value(const char* * keys, size_t keys_len, const char* * values, size_t values_len, const char* key) {
    long long i = 0LL;
    while (i < keys_len) {
        if (strcmp(keys[(int)(i)], key) == 0) {
            return values[(int)(i)];
        }
        i = i + 1LL;
    }
    return NULL;
}

long long contains_value(const char* * values, size_t values_len, const char* value) {
    long long i = 0LL;
    while (i < values_len) {
        if (strcmp(values[(int)(i)], value) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long backtrack(const char* pattern, const char* input_string, long long pi, long long si, const char* * keys, size_t keys_len, const char* * values, size_t values_len) {
    if ((pi == strlen(pattern)) && (si == strlen(input_string))) {
        return 1LL;
    }
    if ((pi == strlen(pattern)) || (si == strlen(input_string))) {
        return 0LL;
    }
    const char* ch = _substring(pattern, pi, pi + 1LL);
    const char *mapped = get_value(keys, keys_len, values, values_len, ch);
    size_t mapped_len = mapped ? strlen(mapped) : 0;
    if (mapped != NULL) {
        if (strcmp(_substring(input_string, si, si + mapped_len), mapped) == 0) {
            return backtrack(pattern, input_string, pi + 1LL, si + mapped_len, keys, keys_len, values, values_len);
        }
        return 0LL;
    }
    long long end = si + 1LL;
    while (end <= strlen(input_string)) {
        const char* substr = _substring(input_string, si, end);
        if (contains_value(values, values_len, substr)) {
            end = end + 1LL;
            continue;
        }
        const char* *new_keys = list_append_str_new(keys, keys_len, ch);
        size_t new_keys_len = keys_len + 1;
        const char* *new_values = list_append_str_new(values, values_len, substr);
        size_t new_values_len = values_len + 1;
        if (backtrack(pattern, input_string, pi + 1LL, end, new_keys, new_keys_len, new_values, new_values_len)) {
            return 1LL;
        }
        end = end + 1LL;
    }
    return 0LL;
}

long long match_word_pattern(const char* pattern, const char* input_string) {
    const char* *keys = NULL;
    size_t keys_len = 0;
    const char* *values = NULL;
    size_t values_len = 0;
    return backtrack(pattern, input_string, 0LL, 0LL, keys, keys_len, values, values_len);
}

void user_main() {
    printf("%d\n", match_word_pattern("aba", "GraphTreesGraph"));
    printf("%d\n", match_word_pattern("xyx", "PythonRubyPython"));
    printf("%d\n", match_word_pattern("GG", "PythonJavaPython"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
