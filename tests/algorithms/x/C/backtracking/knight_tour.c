// Generated by Mochi 0.10.32 on 2025-08-06 16:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_valid_pos_len;
size_t *get_valid_pos_lens;
size_t open_knight_tour_len;
size_t *open_knight_tour_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **board = NULL;
size_t board_len = 0;
size_t *board_lens = NULL;
size_t board_lens_len = 0;

long long * * get_valid_pos(long long * position, size_t position_len, long long n);
long long is_complete(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len);
long long open_knight_tour_helper(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long * pos, size_t pos_len, long long curr);
long long * * open_knight_tour(long long n);
int main(void);

long long * * get_valid_pos(long long * position, size_t position_len, long long n) {
    long long y = position[(int)(0LL)];
    long long x = position[(int)(1LL)];
    int positions_0[2] = {y + 1LL, x + 2LL};
    int positions_1[2] = {y - 1LL, x + 2LL};
    int positions_2[2] = {y + 1LL, x - 2LL};
    int positions_3[2] = {y - 1LL, x - 2LL};
    int positions_4[2] = {y + 2LL, x + 1LL};
    int positions_5[2] = {y + 2LL, x - 1LL};
    int positions_6[2] = {y - 2LL, x + 1LL};
    int positions_7[2] = {y - 2LL, x - 1LL};
    int *positions_init[8] = {positions_0, positions_1, positions_2, positions_3, positions_4, positions_5, positions_6, positions_7};
    int **positions = positions_init;
    size_t positions_len = 8;
    size_t positions_lens_init[8] = {2, 2, 2, 2, 2, 2, 2, 2};
    size_t *positions_lens = positions_lens_init;
    size_t positions_lens_len = 8;
    long long **permissible = NULL;
    size_t permissible_len = 0;
    size_t *permissible_lens = NULL;
    size_t permissible_lens_len = 0;
    for (int idx = 0LL; idx < positions_len; idx++) {
        int *inner = positions[(int)(idx)];
        size_t inner_len = positions_lens[idx];
        long long y_test = inner[(int)(0LL)];
        long long x_test = inner[(int)(1LL)];
        if ((((y_test >= 0LL) && (y_test < n)) && (x_test >= 0LL)) && (x_test < n)) {
            permissible = list_append_intptr(permissible, &permissible_len, inner);
            permissible_lens = list_append_szt(permissible_lens, &permissible_lens_len, inner_len);
        }
    }
    return get_valid_pos_lens = permissible_lens, get_valid_pos_len = permissible_len, permissible;
}

long long is_complete(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len) {
    for (int i = 0LL; i < board_len; i++) {
        long long *row = board[(int)(i)];
        size_t row_len = board_lens[i];
        for (int j = 0LL; j < row_len; j++) {
            if (row[(int)(j)] == 0LL) {
                return 0LL;
            }
        }
    }
    return 1LL;
}

long long open_knight_tour_helper(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long * pos, size_t pos_len, long long curr) {
    if (is_complete(board, board_len, board_lens, board_len)) {
        return 1LL;
    }
    long long **moves = get_valid_pos(pos, pos_len, board_len);
    size_t moves_len = get_valid_pos_len;
    size_t *moves_lens = get_valid_pos_lens;
    size_t moves_lens_len = get_valid_pos_len;
    for (int i = 0LL; i < moves_len; i++) {
        long long *position = moves[(int)(i)];
        size_t position_len = moves_lens[i];
        long long y = position[(int)(0LL)];
        long long x = position[(int)(1LL)];
        if (board[(int)(y)][(int)(x)] == 0LL) {
            board[(int)(y)][(int)(x)] = curr + 1LL;
            if (open_knight_tour_helper(board, board_len, board_lens, board_len, position, position_len, curr + 1LL)) {
                return 1LL;
            }
            board[(int)(y)][(int)(x)] = 0LL;
        }
    }
    return 0LL;
}

long long * * open_knight_tour(long long n) {
    long long **board = NULL;
    size_t board_len = 0;
    size_t *board_lens = NULL;
    size_t board_lens_len = 0;
    for (int i = 0LL; i < n; i++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int j = 0LL; j < n; j++) {
            row = list_append_long_long(row, &row_len, 0LL);
        }
        board = list_append_long_longptr(board, &board_len, row);
        board_lens = list_append_szt(board_lens, &board_lens_len, row_len);
    }
    for (int i = 0LL; i < n; i++) {
        for (int j = 0LL; j < n; j++) {
            board[(int)(i)][(int)(j)] = 1LL;
            if (open_knight_tour_helper(board, board_len, board_lens, board_len, (int[]){ i, j }, 2, 1LL)) {
                return open_knight_tour_lens = board_lens, open_knight_tour_len = board_len, board;
            }
            board[(int)(i)][(int)(j)] = 0LL;
        }
    }
    puts(str_concat("Open Knight Tour cannot be performed on a board of size ", str_int(n)));
    return open_knight_tour_lens = board_lens, open_knight_tour_len = board_len, board;
}

int main(void) {
    {
        long long __start = _now();
        board = open_knight_tour(1LL);
        board_len = open_knight_tour_len;
        board_lens = open_knight_tour_lens;
        printf("%d\n", board[(int)(0LL)][(int)(0LL)]);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
