// Generated by Mochi 0.10.32 on 2025-08-06 20:15 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long contains(long long * xs, size_t xs_len, long long x);
long long get_point_key(long long len_board, long long len_board_column, long long row, long long column);
long long search_from(const char* * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, const char* word, long long row, long long column, long long word_index, long long * visited, size_t visited_len);
long long word_exists(const char* * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, const char* word);
void user_main();
int main(void);

long long contains(long long * xs, size_t xs_len, long long x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)(i)] == x) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long get_point_key(long long len_board, long long len_board_column, long long row, long long column) {
    return ((len_board * len_board_column) * row) + column;
}

long long search_from(const char* * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, const char* word, long long row, long long column, long long word_index, long long * visited, size_t visited_len) {
    if (strcmp(board[(int)(row)][(int)(column)], _substring(word, word_index, word_index + 1LL)) != 0) {
        return 0LL;
    }
    if (word_index == (strlen(word) - 1LL)) {
        return 1LL;
    }
    size_t len_board = board_len;
    long long len_board_column = board_lens[0LL];
    long long *dir_i = NULL;
    size_t dir_i_len = 0;
    dir_i = list_append_int(dir_i, &dir_i_len, 0LL);
    dir_i = list_append_int(dir_i, &dir_i_len, 0LL);
    dir_i = list_append_int(dir_i, &dir_i_len, -1LL);
    dir_i = list_append_int(dir_i, &dir_i_len, 1LL);
    long long *dir_j = NULL;
    size_t dir_j_len = 0;
    dir_j = list_append_int(dir_j, &dir_j_len, 1LL);
    dir_j = list_append_int(dir_j, &dir_j_len, -1LL);
    dir_j = list_append_int(dir_j, &dir_j_len, 0LL);
    dir_j = list_append_int(dir_j, &dir_j_len, 0LL);
    long long k = 0LL;
    while (k < 4LL) {
        long long next_i = row + dir_i[(int)(k)];
        long long next_j = column + dir_j[(int)(k)];
        if (!((((0LL <= next_i) && (next_i < len_board)) && (0LL <= next_j)) && (next_j < len_board_column))) {
            k = k + 1LL;
            continue;
        }
        long long key = get_point_key(len_board, len_board_column, next_i, next_j);
        if (strstr(visited, key) != NULL) {
            k = k + 1LL;
            continue;
        }
        long long *new_visited = list_append_int_new(visited, visited_len, key);
        size_t new_visited_len = visited_len + 1;
        if (search_from(board, board_len, board_lens, board_len, word, next_i, next_j, word_index + 1LL, new_visited, new_visited_len)) {
            return 1LL;
        }
        k = k + 1LL;
    }
    return 0LL;
}

long long word_exists(const char* * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, const char* word) {
    size_t len_board = board_len;
    long long len_board_column = board_lens[0LL];
    long long i = 0LL;
    while (i < len_board) {
        long long j = 0LL;
        while (j < len_board_column) {
            long long key = get_point_key(len_board, len_board_column, i, j);
            long long *visited = NULL;
            size_t visited_len = 0;
            visited = list_append_int(visited, &visited_len, key);
            if (search_from(board, board_len, board_lens, board_len, word, i, j, 0LL, visited, visited_len)) {
                return 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

void user_main() {
    const char* board_0[4] = {"A", "B", "C", "E"};
    const char* board_1[4] = {"S", "F", "C", "S"};
    const char* board_2[4] = {"A", "D", "E", "E"};
    const char* *board_init[3] = {board_0, board_1, board_2};
    const char* **board = board_init;
    size_t board_len = 3;
    size_t board_lens_init[3] = {4, 4, 4};
    size_t *board_lens = board_lens_init;
    size_t board_lens_len = 3;
    printf("%d\n", word_exists(board, board_len, board_lens, board_len, "ABCCED"));
    printf("%d\n", word_exists(board, board_len, board_lens, board_len, "SEE"));
    printf("%d\n", word_exists(board, board_len, board_lens, board_len, "ABCB"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
