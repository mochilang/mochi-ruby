// Generated by Mochi 0.10.32 on 2025-08-12 12:29 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long ans = 0;

long long is_prime(long long number);
long long solution(long long nth);
int main(void);

long long is_prime(long long number) {
    if ((number > 1LL) && (number < 4LL)) {
        return 1LL;
    } else {
        if (((number < 2LL) || ((number % 2LL) == 0LL)) || ((number % 3LL) == 0LL)) {
            return 0LL;
        }
    }
    long long i = 5LL;
    while ((i * i) <= number) {
        if (((number % i) == 0LL) || ((number % (i + 2LL)) == 0LL)) {
            return 0LL;
        }
        i = i + 6LL;
    }
    return 1LL;
}

long long solution(long long nth) {
    if (nth <= 0LL) {
        error("Parameter nth must be greater than or equal to one.");
    }
    long long *primes = NULL;
    size_t primes_len = 0;
    long long num = 2LL;
    while (primes_len < nth) {
        if (is_prime(num)) {
            primes = list_append_long_long(primes, &primes_len, num);
        }
        num = num + 1LL;
    }
    return primes[(int)({long long _mochi_idx = primes_len - 1LL; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    ans = solution(10001LL);
    puts(str_concat("solution(10001) = ", str_int(ans)));
    return 0;
}
