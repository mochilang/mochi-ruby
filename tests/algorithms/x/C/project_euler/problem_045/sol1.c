// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double to_float(long long x);
double sqrt(double x);
long long user_floor(double x);
long long hexagonal_num(long long n);
long long is_pentagonal(long long n);
long long solution(long long start);
void test_hexagonal_num();
void test_is_pentagonal();
void test_solution();
int main(void);

double to_float(long long x) {
    return x * 1.0;
}

double sqrt(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

long long user_floor(double x) {
    long long n = 0LL;
    double y = x;
    while (y >= 1.0) {
        y = y - 1.0;
        n = n + 1LL;
    }
    return n;
}

long long hexagonal_num(long long n) {
    return n * ((2LL * n) - 1LL);
}

long long is_pentagonal(long long n) {
    double root = sqrt(1.0 + (24.0 * to_float(n)));
    double val = (1.0 + root) / 6.0;
    return val == to_float(user_floor(val));
}

long long solution(long long start) {
    long long idx = start;
    long long num = hexagonal_num(idx);
    while (!(is_pentagonal(num))) {
        idx = idx + 1LL;
        num = hexagonal_num(idx);
    }
    return num;
}

void test_hexagonal_num() {
    if (hexagonal_num(143LL) != 40755LL) {
        panic("hexagonal_num(143) failed");
    }
    if (hexagonal_num(21LL) != 861LL) {
        panic("hexagonal_num(21) failed");
    }
    if (hexagonal_num(10LL) != 190LL) {
        panic("hexagonal_num(10) failed");
    }
}

void test_is_pentagonal() {
    if (!(is_pentagonal(330LL))) {
        panic("330 should be pentagonal");
    }
    if (is_pentagonal(7683LL)) {
        panic("7683 should not be pentagonal");
    }
    if (!(is_pentagonal(2380LL))) {
        panic("2380 should be pentagonal");
    }
}

void test_solution() {
    if (solution(144LL) != 1533776805LL) {
        panic("solution failed");
    }
}

int main(void) {
    {
        long long __start = _now();
        test_hexagonal_num();
        test_is_pentagonal();
        test_solution();
        puts(str_concat(str_int(solution(144LL)), " = "));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
