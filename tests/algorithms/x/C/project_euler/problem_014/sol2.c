// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        int *new_keys = malloc(m->cap * sizeof(int));
        int *new_vals = malloc(m->cap * sizeof(int));
        if (m->keys) memcpy(new_keys, m->keys, m->len * sizeof(int));
        if (m->vals) memcpy(new_vals, m->vals, m->len * sizeof(int));
        m->keys = new_keys;
        m->vals = new_vals;
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int collatz_cache_keys[17] = {1LL};
static int collatz_cache_vals[17] = {1LL};
size_t collatz_cache_len = 1;
MapII collatz_cache = { collatz_cache_keys, collatz_cache_vals, 1, 17 };
const char* input_str = "";
long long limit = 0;

long long collatz_length(long long n);
long long solution(long long limit);
int main(void);

long long collatz_length(long long n) {
    long long num = n;
    long long *sequence = NULL;
    size_t sequence_len = 0;
    while (!(contains_map_int(collatz_cache.keys, collatz_cache.len, num))) {
        sequence = list_append_long_long(sequence, &sequence_len, num);
        if ((num % 2LL) == 0LL) {
            num = num / 2LL;
        } else {
            num = (3LL * num) + 1LL;
        }
    }
    long long length = map_get_ii(collatz_cache.keys, collatz_cache.vals, collatz_cache.len, num);
    long long i = sequence_len - 1LL;
    while (i >= 0LL) {
        length = length + 1LL;
        map_set_ii(&collatz_cache, sequence[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sequence_len + _mochi_idx : _mochi_idx;})], length);
        i = i - 1LL;
    }
    return length;
}

long long solution(long long limit) {
    long long max_len = 0LL;
    long long max_start = 1LL;
    long long i = 1LL;
    while (i < limit) {
        long long length = collatz_length(i);
        if (length > max_len) {
            max_len = length;
            max_start = i;
        }
        i = i + 1LL;
    }
    return max_start;
}

int main(void) {
    {
        long long __start = _now();
        input_str = _input();
        limit = _atoi(input_str);
        printf("%d\n", solution(limit));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
