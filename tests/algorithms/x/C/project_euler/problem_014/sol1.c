// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* input_str = "";
long long n = 0;

long long solution(long long n);
int main(void);

long long solution(long long n) {
    long long *counters = NULL;
    size_t counters_len = 0;
    long long i = 0LL;
    while (i <= n) {
        counters = list_append_long_long(counters, &counters_len, 0LL);
        i = i + 1LL;
    }
    counters[(int)(1LL)] = 1LL;
    long long largest_number = 1LL;
    long long pre_counter = 1LL;
    long long start = 2LL;
    while (start < n) {
        long long number = start;
        long long counter = 0LL;
        while (1LL) {
            if ((number < counters_len) && (counters[(int)({long long _mochi_idx = number; _mochi_idx < 0 ? counters_len + _mochi_idx : _mochi_idx;})] != 0LL)) {
                counter = counter + counters[(int)({long long _mochi_idx = number; _mochi_idx < 0 ? counters_len + _mochi_idx : _mochi_idx;})];
                break;
            }
            if ((number % 2LL) == 0LL) {
                number = number / 2LL;
            } else {
                number = (3LL * number) + 1LL;
            }
            counter = counter + 1LL;
        }
        if ((start < counters_len) && (counters[(int)({long long _mochi_idx = start; _mochi_idx < 0 ? counters_len + _mochi_idx : _mochi_idx;})] == 0LL)) {
            counters[(int)(start)] = counter;
        }
        if (counter > pre_counter) {
            largest_number = start;
            pre_counter = counter;
        }
        start = start + 1LL;
    }
    return largest_number;
}

int main(void) {
    {
        long long __start = _now();
        input_str = _input();
        n = _atoi(input_str);
        puts(str_int(solution(n)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
