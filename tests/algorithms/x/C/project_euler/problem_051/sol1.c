// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t digit_replacements_len;
size_t *digit_replacements_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long parse_int(const char* s);
long long * * digit_replacements(long long number);
long long is_prime(long long num);
long long solution(long long family_length);
int main(void);

long long parse_int(const char* s) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        value = (value * 10LL) + s[i] - '0';
        i = i + 1LL;
    }
    return value;
}

long long * * digit_replacements(long long number) {
    const char* num_str = str_int(number);
    long long *counts = NULL;
    size_t counts_len = 0;
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    counts = list_append_long_long(counts, &counts_len, 0LL);
    long long i = 0LL;
    while (i < strlen(num_str)) {
        long long d = num_str[i] - '0';
        counts[(int)(d)] = counts[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] + 1LL;
        i = i + 1LL;
    }
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    const char* digits = "0123456789";
    long long digit = 0LL;
    while (digit < 10LL) {
        if (counts[(int)({long long _mochi_idx = digit; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] > 1LL) {
            long long *family = NULL;
            size_t family_len = 0;
            long long repl = 0LL;
            while (repl < 10LL) {
                const char* new_str = "";
                long long j = 0LL;
                while (j < strlen(num_str)) {
                    const char* c = (const char[]){num_str[j], 0};
                    if (strcmp(c, (const char[]){digits[digit], 0}) == 0) {
                        new_str = str_concat(new_str, (const char[]){digits[repl], 0});
                    } else {
                        new_str = str_concat(new_str, c);
                    }
                    j = j + 1LL;
                }
                family = list_append_long_long(family, &family_len, parse_int(new_str));
                repl = repl + 1LL;
            }
            result = list_append_intptr(result, &result_len, family);
            result_lens = list_append_szt(result_lens, &result_lens_len, family_len);
        }
        digit = digit + 1LL;
    }
    return digit_replacements_lens = result_lens, digit_replacements_len = result_len, result;
}

long long is_prime(long long num) {
    if (num < 2LL) {
        return 0LL;
    }
    if ((num % 2LL) == 0LL) {
        return num == 2LL;
    }
    long long i = 3LL;
    while ((i * i) <= num) {
        if ((num % i) == 0LL) {
            return 0LL;
        }
        i = i + 2LL;
    }
    return 1LL;
}

long long solution(long long family_length) {
    long long candidate = 121313LL;
    if (!(is_prime(candidate))) {
        return -1.0;
    }
    long long **reps = digit_replacements(candidate);
    size_t reps_len = digit_replacements_len;
    size_t *reps_lens = digit_replacements_lens;
    size_t reps_lens_len = digit_replacements_len;
    long long r = 0LL;
    while (r < reps_len) {
        long long *family = reps[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? reps_len + _mochi_idx : _mochi_idx;})];
        size_t family_len = reps_lens[(int)(r)];
        long long count_ = 0LL;
        long long min_prime = 0LL;
        long long first = 1LL;
        long long i = 0LL;
        while (i < family_len) {
            long long num = family[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? family_len + _mochi_idx : _mochi_idx;})];
            if (is_prime(num)) {
                if (first) {
                    min_prime = num;
                    first = 0LL;
                } else {
                    if (num < min_prime) {
                        min_prime = num;
                    }
                }
                count_ = count_ + 1LL;
            }
            i = i + 1LL;
        }
        if (count_ == family_length) {
            return min_prime;
        }
        r = r + 1LL;
    }
    return -1.0;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(8LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
