// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t digits_count_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * digits_count(long long n);
long long equal_lists(long long * a, size_t a_len, long long * b, size_t b_len);
long long solution();
int main(void);

long long * digits_count(long long n) {
    long long *counts = NULL;
    size_t counts_len = 0;
    long long i = 0LL;
    while (i < 10LL) {
        counts = list_append_long_long(counts, &counts_len, 0LL);
        i = i + 1LL;
    }
    long long x = n;
    if (x == 0LL) {
        counts[(int)(0LL)] = counts[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] + 1LL;
    }
    while (x > 0LL) {
        long long d = x % 10LL;
        counts[(int)(d)] = counts[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] + 1LL;
        x = x / 10LL;
    }
    return digits_count_len = counts_len, counts;
}

long long equal_lists(long long * a, size_t a_len, long long * b, size_t b_len) {
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long solution() {
    long long i = 1LL;
    while (1LL) {
        long long *c = digits_count(i);
        size_t c_len = digits_count_len;
        if ((((({long long* __tmp5 = digits_count(2LL * i); size_t __tmp5_len = digits_count_len; equal_lists(c, c_len, __tmp5, __tmp5_len);}) && ({long long* __tmp6 = digits_count(3LL * i); size_t __tmp6_len = digits_count_len; equal_lists(c, c_len, __tmp6, __tmp6_len);})) && ({long long* __tmp7 = digits_count(4LL * i); size_t __tmp7_len = digits_count_len; equal_lists(c, c_len, __tmp7, __tmp7_len);})) && ({long long* __tmp8 = digits_count(5LL * i); size_t __tmp8_len = digits_count_len; equal_lists(c, c_len, __tmp8, __tmp8_len);})) && ({long long* __tmp9 = digits_count(6LL * i); size_t __tmp9_len = digits_count_len; equal_lists(c, c_len, __tmp9, __tmp9_len);})) {
            return i;
        }
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution()));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
