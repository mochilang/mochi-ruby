// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t range_desc_len;
size_t range_asc_len;
size_t concat_lists_len;
size_t swap_len;
size_t generate_gon_ring_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * range_desc(long long start, long long end);
long long * range_asc(long long start, long long end);
long long * concat_lists(long long * a, size_t a_len, long long * b, size_t b_len);
long long * swap(long long * xs, size_t xs_len, long long i, long long j);
long long * generate_gon_ring(long long gon_side, long long * perm, size_t perm_len);
long long min_outer(long long * numbers, size_t numbers_len);
long long is_magic_gon(long long * numbers, size_t numbers_len);
const char* permute_search(long long * nums, size_t nums_len, long long start, long long gon_side, const char* current_max);
const char* solution(long long gon_side);
int main(void);

long long * range_desc(long long start, long long end) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = start;
    while (i >= end) {
        res = list_append_long_long(res, &res_len, i);
        i = i - 1LL;
    }
    return range_desc_len = res_len, res;
}

long long * range_asc(long long start, long long end) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = start;
    while (i <= end) {
        res = list_append_long_long(res, &res_len, i);
        i = i + 1LL;
    }
    return range_asc_len = res_len, res;
}

long long * concat_lists(long long * a, size_t a_len, long long * b, size_t b_len) {
    long long *res = a;
    size_t res_len = a_len;
    long long i = 0LL;
    while (i < b_len) {
        res = list_append_int_new(res, res_len, b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        res_len++;
        i = i + 1LL;
    }
    return concat_lists_len = res_len, res;
}

long long * swap(long long * xs, size_t xs_len, long long i, long long j) {
    long long *res = NULL;
    size_t res_len = 0;
    long long k = 0LL;
    while (k < xs_len) {
        if (k == i) {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        } else {
            if (k == j) {
                res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
            } else {
                res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
            }
        }
        k = k + 1LL;
    }
    return swap_len = res_len, res;
}

long long * generate_gon_ring(long long gon_side, long long * perm, size_t perm_len) {
    long long *result = NULL;
    size_t result_len = 0;
    result = list_append_long_long(result, &result_len, perm[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? perm_len + _mochi_idx : _mochi_idx;})]);
    result = list_append_long_long(result, &result_len, perm[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? perm_len + _mochi_idx : _mochi_idx;})]);
    result = list_append_long_long(result, &result_len, perm[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? perm_len + _mochi_idx : _mochi_idx;})]);
    long long *extended = list_append_int_new(perm, perm_len, perm[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? perm_len + _mochi_idx : _mochi_idx;})]);
    size_t extended_len = perm_len + 1;
    long long magic_number;
    if (gon_side < 5LL) {
        magic_number = 1LL;
    } else {
        magic_number = 2LL;
    }
    long long i = 1LL;
    while (i < ((extended_len / 3LL) + magic_number)) {
        result = list_append_long_long(result, &result_len, extended[(int)({long long _mochi_idx = (2LL * i) + 1LL; _mochi_idx < 0 ? extended_len + _mochi_idx : _mochi_idx;})]);
        result = list_append_long_long(result, &result_len, result[(int)({long long _mochi_idx = (3LL * i) - 1LL; _mochi_idx < 0 ? result_len + _mochi_idx : _mochi_idx;})]);
        result = list_append_long_long(result, &result_len, extended[(int)({long long _mochi_idx = (2LL * i) + 2LL; _mochi_idx < 0 ? extended_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return generate_gon_ring_len = result_len, result;
}

long long min_outer(long long * numbers, size_t numbers_len) {
    long long min_val = numbers[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
    long long i = 3LL;
    while (i < numbers_len) {
        if (numbers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})] < min_val) {
            min_val = numbers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 3LL;
    }
    return min_val;
}

long long is_magic_gon(long long * numbers, size_t numbers_len) {
    if ((numbers_len % 3LL) != 0LL) {
        return 0LL;
    }
    if (min_outer(numbers, numbers_len) != numbers[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})]) {
        return 0LL;
    }
    long long total = (numbers[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})] + numbers[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})]) + numbers[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
    long long i = 3LL;
    while (i < numbers_len) {
        if (((numbers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})] + numbers[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})]) + numbers[(int)({long long _mochi_idx = i + 2LL; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})]) != total) {
            return 0LL;
        }
        i = i + 3LL;
    }
    return 1LL;
}

const char* permute_search(long long * nums, size_t nums_len, long long start, long long gon_side, const char* current_max) {
    if (start == nums_len) {
        long long *ring = generate_gon_ring(gon_side, nums, nums_len);
        size_t ring_len = generate_gon_ring_len;
        if (is_magic_gon(ring, ring_len)) {
            const char* s = "";
            long long k = 0LL;
            while (k < ring_len) {
                s = str_concat(s, str_int(ring[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? ring_len + _mochi_idx : _mochi_idx;})]));
                k = k + 1LL;
            }
            if (strcmp(s, current_max) > 0) {
                return s;
            }
        }
        return current_max;
    }
    const char* res = current_max;
    long long i = start;
    while (i < nums_len) {
        long long *swapped = swap(nums, nums_len, start, i);
        size_t swapped_len = swap_len;
        const char* candidate = permute_search(swapped, swapped_len, start + 1LL, gon_side, res);
        if (strcmp(candidate, res) > 0) {
            res = candidate;
        }
        i = i + 1LL;
    }
    return res;
}

const char* solution(long long gon_side) {
    if ((gon_side < 3LL) || (gon_side > 5LL)) {
        return "";
    }
    long long *small = range_desc(gon_side + 1LL, 1LL);
    size_t small_len = range_desc_len;
    long long *big = range_asc(gon_side + 2LL, gon_side * 2LL);
    size_t big_len = range_asc_len;
    long long *numbers = concat_lists(small, small_len, big, big_len);
    size_t numbers_len = concat_lists_len;
    const char* max_str = permute_search(numbers, numbers_len, 0LL, gon_side, "");
    return max_str;
}

int main(void) {
    {
        long long __start = _now();
        puts(solution(5LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
