// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long is_prime(long long number);
long long solution(long long a_limit, long long b_limit);
int main(void);

long long is_prime(long long number) {
    if ((1LL < number) && (number < 4LL)) {
        return 1LL;
    } else {
        if (((number < 2LL) || ((number % 2LL) == 0LL)) || ((number % 3LL) == 0LL)) {
            return 0LL;
        }
    }
    long long i = 5LL;
    while ((i * i) <= number) {
        if (((number % i) == 0LL) || ((number % (i + 2LL)) == 0LL)) {
            return 0LL;
        }
        i = i + 6LL;
    }
    return 1LL;
}

long long solution(long long a_limit, long long b_limit) {
    long long longest_len = 0LL;
    long long longest_a = 0LL;
    long long longest_b = 0LL;
    long long a = (-1.0 * a_limit) + 1LL;
    while (a < a_limit) {
        long long b = 2LL;
        while (b < b_limit) {
            if (is_prime(b)) {
                long long count_ = 0LL;
                long long n = 0LL;
                while (is_prime(((n * n) + (a * n)) + b)) {
                    count_ = count_ + 1LL;
                    n = n + 1LL;
                }
                if (count_ > longest_len) {
                    longest_len = count_;
                    longest_a = a;
                    longest_b = b;
                }
            }
            b = b + 1LL;
        }
        a = a + 1LL;
    }
    return longest_a * longest_b;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(1000LL, 1000LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
