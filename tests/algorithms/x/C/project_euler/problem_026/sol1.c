// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long contains(long long * lst, size_t lst_len, long long val);
long long solution(long long numerator, long long limit);
void user_main();
int main(void);

long long contains(long long * lst, size_t lst_len, long long val) {
    for (size_t __i = 0; __i < lst_len; __i++) {
        long long x = lst[__i];
        if (x == val) {
            return 1LL;
        }
    }
    return 0LL;
}

long long solution(long long numerator, long long limit) {
    long long the_digit = 1LL;
    long long longest_len = 0LL;
    for (int d = numerator; d < limit; d++) {
        long long *remainders = NULL;
        size_t remainders_len = 0;
        long long rem = numerator;
        long long count_ = 1LL;
        while (count_ <= limit) {
            if (contains(remainders, remainders_len, rem)) {
                if (longest_len < remainders_len) {
                    longest_len = remainders_len;
                    the_digit = d;
                }
            } else {
                remainders = list_append_long_long(remainders, &remainders_len, rem);
                rem = (rem * 10LL) % d;
            }
            count_ = count_ + 1LL;
        }
    }
    return the_digit;
}

void user_main() {
    puts(str_int(solution(1LL, 10LL)));
    puts(str_int(solution(10LL, 100LL)));
    puts(str_int(solution(10LL, 1000LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
