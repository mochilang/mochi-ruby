// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_at_len;
size_t search_len;

size_t _slice_str_len;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str_new(const char **arr, size_t len, const char *val) {
    const char **res = malloc((len + 1) * sizeof(const char*));
    if (arr && len) memcpy(res, arr, len * sizeof(const char*));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* digits_init[9] = {"1", "2", "3", "4", "5", "6", "7", "8", "9"};
const char* *digits = digits_init;
size_t digits_len = 9;
long long *products = NULL;
size_t products_len = 0;
long long total = 0LL;
long long i = 0LL;

const char* join_digits(const char* * xs, size_t xs_len);
long long digits_to_int(const char* * xs, size_t xs_len);
long long contains_int(long long * xs, size_t xs_len, long long value);
const char* * remove_at(const char* * xs, size_t xs_len, long long idx);
long long is_combination_valid(const char* * comb, size_t comb_len);
long long * search(const char* * prefix, size_t prefix_len, const char* * remaining, size_t remaining_len, long long * products, size_t products_len);
int main(void);

const char* join_digits(const char* * xs, size_t xs_len) {
    const char* s = "";
    long long i = 0LL;
    while (i < xs_len) {
        s = str_concat(s, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return s;
}

long long digits_to_int(const char* * xs, size_t xs_len) {
    return _atoi(join_digits(xs, xs_len));
}

long long contains_int(long long * xs, size_t xs_len, long long value) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == value) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * remove_at(const char* * xs, size_t xs_len, long long idx) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        if (i != idx) {
            res = list_append_str(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_at_len = res_len, res;
}

long long is_combination_valid(const char* * comb, size_t comb_len) {
    long long prod = ({const char** __tmp6 = _slice_str(comb, comb_len, 5LL, 9LL, &(_slice_str_len)); size_t __tmp6_len = _slice_str_len; digits_to_int(__tmp6, __tmp6_len);});
    long long mul2 = ({const char** __tmp7 = _slice_str(comb, comb_len, 0LL, 2LL, &(_slice_str_len)); size_t __tmp7_len = _slice_str_len; digits_to_int(__tmp7, __tmp7_len);});
    long long mul3 = ({const char** __tmp8 = _slice_str(comb, comb_len, 2LL, 5LL, &(_slice_str_len)); size_t __tmp8_len = _slice_str_len; digits_to_int(__tmp8, __tmp8_len);});
    if ((mul2 * mul3) == prod) {
        return 1LL;
    }
    long long mul1 = ({const char** __tmp9 = _slice_str(comb, comb_len, 0LL, 1LL, &(_slice_str_len)); size_t __tmp9_len = _slice_str_len; digits_to_int(__tmp9, __tmp9_len);});
    long long mul4 = ({const char** __tmp10 = _slice_str(comb, comb_len, 1LL, 5LL, &(_slice_str_len)); size_t __tmp10_len = _slice_str_len; digits_to_int(__tmp10, __tmp10_len);});
    return (mul1 * mul4) == prod;
}

long long * search(const char* * prefix, size_t prefix_len, const char* * remaining, size_t remaining_len, long long * products, size_t products_len) {
    if (remaining_len == 0LL) {
        if (is_combination_valid(prefix, prefix_len)) {
            long long p = ({const char** __tmp11 = _slice_str(prefix, prefix_len, 5LL, 9LL, &(_slice_str_len)); size_t __tmp11_len = _slice_str_len; digits_to_int(__tmp11, __tmp11_len);});
            if (!(contains_int(products, products_len, p))) {
                products = list_append_long_long(products, &products_len, p);
            }
        }
        return search_len = products_len, products;
    }
    long long i = 0LL;
    while (i < remaining_len) {
        const char* *next_prefix = list_append_str_new(prefix, prefix_len, remaining[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? remaining_len + _mochi_idx : _mochi_idx;})]);
        size_t next_prefix_len = prefix_len + 1;
        const char* *next_remaining = remove_at(remaining, remaining_len, i);
        size_t next_remaining_len = remove_at_len;
        products = search(next_prefix, next_prefix_len, next_remaining, next_remaining_len, products, products_len);
        products_len = search_len;
        i = i + 1LL;
    }
    return search_len = products_len, products;
}

int main(void) {
    {
        long long __start = _now();
        products = search(NULL, 0, digits, digits_len, products, products_len);
        products_len = search_len;
        while (i < products_len) {
            total = total + products[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? products_len + _mochi_idx : _mochi_idx;})];
            i = i + 1LL;
        }
        puts(str_int(total));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
