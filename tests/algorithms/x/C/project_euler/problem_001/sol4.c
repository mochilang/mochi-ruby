// Generated by Mochi 0.10.32 on 2025-08-19 09:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long contains(long long * xs, size_t xs_len, long long value);
long long solution(long long n);
void test_solution();
void user_main();
int main(void);

long long contains(long long * xs, size_t xs_len, long long value) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == value) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long solution(long long n) {
    long long *zmulti = NULL;
    size_t zmulti_len = 0;
    long long *xmulti = NULL;
    size_t xmulti_len = 0;
    long long temp = 1LL;
    while (1LL) {
        long long result = 3LL * temp;
        if (result < n) {
            zmulti = list_append_long_long(zmulti, &zmulti_len, result);
            temp = temp + 1LL;
        } else {
            break;
        }
    }
    temp = 1LL;
    while (1LL) {
        long long result = 5LL * temp;
        if (result < n) {
            xmulti = list_append_long_long(xmulti, &xmulti_len, result);
            temp = temp + 1LL;
        } else {
            break;
        }
    }
    long long *collection = NULL;
    size_t collection_len = 0;
    long long i = 0LL;
    while (i < zmulti_len) {
        long long v = zmulti[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? zmulti_len + _mochi_idx : _mochi_idx;})];
        if (!(contains(collection, collection_len, v))) {
            collection = list_append_long_long(collection, &collection_len, v);
        }
        i = i + 1LL;
    }
    i = 0LL;
    while (i < xmulti_len) {
        long long v = xmulti[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xmulti_len + _mochi_idx : _mochi_idx;})];
        if (!(contains(collection, collection_len, v))) {
            collection = list_append_long_long(collection, &collection_len, v);
        }
        i = i + 1LL;
    }
    long long total = 0LL;
    i = 0LL;
    while (i < collection_len) {
        total = total + collection[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return total;
}

void test_solution() {
    if (solution(3LL) != 0LL) {
        panic("solution(3) failed");
    }
    if (solution(4LL) != 3LL) {
        panic("solution(4) failed");
    }
    if (solution(10LL) != 23LL) {
        panic("solution(10) failed");
    }
    if (solution(600LL) != 83700LL) {
        panic("solution(600) failed");
    }
}

void user_main() {
    test_solution();
    puts(str_concat("solution() = ", str_int(solution(1000LL))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
