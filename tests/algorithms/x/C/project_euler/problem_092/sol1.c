// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long next_number(long long number);
long long chain(long long number);
long long solution(long long limit);
int main(void);

long long next_number(long long number) {
    long long n = number;
    long long total = 0LL;
    while (n > 0LL) {
        long long d = n % 10LL;
        total = total + (d * d);
        n = n / 10LL;
    }
    return total;
}

long long chain(long long number) {
    long long n = number;
    while ((n != 1LL) && (n != 89LL)) {
        n = next_number(n);
    }
    return n == 1LL;
}

long long solution(long long limit) {
    long long count_ = 0LL;
    long long i = 1LL;
    while (i < limit) {
        if (!(chain(i))) {
            count_ = count_ + 1LL;
        }
        i = i + 1LL;
    }
    return count_;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(next_number(44LL)));
        puts(str_int(next_number(10LL)));
        puts(str_int(next_number(32LL)));
        puts(str_int(chain(10LL)));
        puts(str_int(chain(58LL)));
        puts(str_int(chain(1LL)));
        puts(str_int(solution(100LL)));
        puts(str_int(solution(1000LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
