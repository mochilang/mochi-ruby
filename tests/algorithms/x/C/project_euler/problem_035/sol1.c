// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t find_circular_primes_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long LIMIT = 10000LL;
long long *sieve = NULL;
size_t sieve_len = 0;
long long i = 0LL;
long long p = 2LL;

long long is_prime(long long n);
long long contains_an_even_digit(long long n);
long long parse_int(const char* s);
long long * find_circular_primes(long long limit);
long long solution();
int main(void);

long long is_prime(long long n) {
    return sieve[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})];
}

long long contains_an_even_digit(long long n) {
    const char* s = str_int(n);
    long long idx = 0LL;
    while (idx < strlen(s)) {
        const char* c = (const char[]){s[idx], 0};
        if (((((strcmp(c, "0") == 0) || (strcmp(c, "2") == 0)) || (strcmp(c, "4") == 0)) || (strcmp(c, "6") == 0)) || (strcmp(c, "8") == 0)) {
            return 1LL;
        }
        idx = idx + 1LL;
    }
    return 0LL;
}

long long parse_int(const char* s) {
    long long value = 0LL;
    long long k = 0LL;
    while (k < strlen(s)) {
        const char* ch = (const char[]){s[k], 0};
        value = (value * 10LL) + _atoi(ch);
        k = k + 1LL;
    }
    return value;
}

long long * find_circular_primes(long long limit) {
    long long *result = NULL;
    size_t result_len = 0;
    result = list_append_long_long(result, &result_len, 2LL);
    long long num = 3LL;
    while (num <= limit) {
        if (is_prime(num) && (contains_an_even_digit(num) == 0LL)) {
            const char* s = str_int(num);
            long long all_prime = 1LL;
            long long j = 0LL;
            while (j < strlen(s)) {
                const char* rotated_str = str_concat(_substring(s, j, strlen(s)), _substring(s, 0LL, j));
                long long rotated = parse_int(rotated_str);
                if (!(is_prime(rotated))) {
                    all_prime = 0LL;
                    break;
                }
                j = j + 1LL;
            }
            if (all_prime) {
                result = list_append_long_long(result, &result_len, num);
            }
        }
        num = num + 2LL;
    }
    return find_circular_primes_len = result_len, result;
}

long long solution() {
    return find_circular_primes_len;
}

int main(void) {
    {
        long long __start = _now();
        while (i <= LIMIT) {
            sieve = list_append_long_long(sieve, &sieve_len, 1LL);
            i = i + 1LL;
        }
        while ((p * p) <= LIMIT) {
            if (sieve[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
                long long j = p * p;
                while (j <= LIMIT) {
                    sieve[(int)(j)] = 0LL;
                    j = j + p;
                }
            }
            p = p + 1LL;
        }
        puts(str_concat("len(find_circular_primes()) = ", str_int(solution())));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
