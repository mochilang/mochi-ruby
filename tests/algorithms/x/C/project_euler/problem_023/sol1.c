// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long int_sqrt(long long n);
long long solution(long long limit);
int main(void);

long long int_sqrt(long long n) {
    long long x = 1LL;
    while (((x + 1LL) * (x + 1LL)) <= n) {
        x = x + 1LL;
    }
    return x;
}

long long solution(long long limit) {
    long long *sum_divs = NULL;
    size_t sum_divs_len = 0;
    long long i = 0LL;
    while (i <= limit) {
        sum_divs = list_append_long_long(sum_divs, &sum_divs_len, 1LL);
        i = i + 1LL;
    }
    long long sqrt_limit = int_sqrt(limit);
    i = 2LL;
    while (i <= sqrt_limit) {
        sum_divs[(int)(i * i)] = sum_divs[(int)({long long _mochi_idx = i * i; _mochi_idx < 0 ? sum_divs_len + _mochi_idx : _mochi_idx;})] + i;
        long long k = i + 1LL;
        while (k <= (limit / i)) {
            sum_divs[(int)(k * i)] = (sum_divs[(int)({long long _mochi_idx = k * i; _mochi_idx < 0 ? sum_divs_len + _mochi_idx : _mochi_idx;})] + k) + i;
            k = k + 1LL;
        }
        i = i + 1LL;
    }
    long long *is_abundant = NULL;
    size_t is_abundant_len = 0;
    i = 0LL;
    while (i <= limit) {
        is_abundant = list_append_long_long(is_abundant, &is_abundant_len, 0LL);
        i = i + 1LL;
    }
    long long *abundants = NULL;
    size_t abundants_len = 0;
    long long res = 0LL;
    long long n = 1LL;
    while (n <= limit) {
        if (sum_divs[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? sum_divs_len + _mochi_idx : _mochi_idx;})] > n) {
            abundants = list_append_long_long(abundants, &abundants_len, n);
            is_abundant[(int)(n)] = 1LL;
        }
        long long has_pair = 0LL;
        long long j = 0LL;
        while (j < abundants_len) {
            long long a = abundants[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? abundants_len + _mochi_idx : _mochi_idx;})];
            if (a > n) {
                break;
            }
            long long b = n - a;
            if ((b <= limit) && is_abundant[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? is_abundant_len + _mochi_idx : _mochi_idx;})]) {
                has_pair = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (!(has_pair)) {
            res = res + n;
        }
        n = n + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(28123LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
