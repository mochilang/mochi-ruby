// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long solution(long long length);
int main(void);

long long solution(long long length) {
    long long **ways = NULL;
    size_t ways_len = 0;
    size_t *ways_lens = NULL;
    size_t ways_lens_len = 0;
    long long i = 0LL;
    while (i <= length) {
        long long *row = NULL;
        size_t row_len = 0;
        row = list_append_long_long(row, &row_len, 0LL);
        row = list_append_long_long(row, &row_len, 0LL);
        row = list_append_long_long(row, &row_len, 0LL);
        ways = list_append_intptr(ways, &ways_len, row);
        ways_lens = list_append_szt(ways_lens, &ways_lens_len, row_len);
        i = i + 1LL;
    }
    long long row_length = 0LL;
    while (row_length <= length) {
        long long tile_length = 2LL;
        while (tile_length <= 4LL) {
            long long tile_start = 0LL;
            while (tile_start <= (row_length - tile_length)) {
                long long remaining = (row_length - tile_start) - tile_length;
                ways[(int)(row_length)][(int)(tile_length - 2LL)] = (ways[(int)({long long _mochi_idx = row_length; _mochi_idx < 0 ? ways_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = tile_length - 2LL; _mochi_idx < 0 ? ways_lens[(int)(row_length)] + _mochi_idx : _mochi_idx;})] + ways[(int)({long long _mochi_idx = remaining; _mochi_idx < 0 ? ways_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = tile_length - 2LL; _mochi_idx < 0 ? ways_lens[(int)(remaining)] + _mochi_idx : _mochi_idx;})]) + 1LL;
                tile_start = tile_start + 1LL;
            }
            tile_length = tile_length + 1LL;
        }
        row_length = row_length + 1LL;
    }
    long long total = 0LL;
    long long j = 0LL;
    while (j < 3LL) {
        total = total + ways[(int)({long long _mochi_idx = length; _mochi_idx < 0 ? ways_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ways_lens[(int)(length)] + _mochi_idx : _mochi_idx;})];
        j = j + 1LL;
    }
    return total;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", solution(5LL));
        printf("%d\n", solution(50LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
