// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_at_len;

size_t append_len;
static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_substring_divisible(long long * num, size_t num_len);
long long * remove_at(long long * xs, size_t xs_len, long long idx);
long long digits_to_number(long long * xs, size_t xs_len);
long long search(long long * prefix, size_t prefix_len, long long * remaining, size_t remaining_len);
long long solution(long long n);
int main(void);

long long is_substring_divisible(long long * num, size_t num_len) {
    if ((num[(int)({long long _mochi_idx = 3LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})] % 2LL) != 0LL) {
        return 0LL;
    }
    if ((((num[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})] + num[(int)({long long _mochi_idx = 3LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})]) + num[(int)({long long _mochi_idx = 4LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})]) % 3LL) != 0LL) {
        return 0LL;
    }
    if ((num[(int)({long long _mochi_idx = 5LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})] % 5LL) != 0LL) {
        return 0LL;
    }
    long long *primes = NULL;
    size_t primes_len = 0;
    primes = list_append_long_long(primes, &primes_len, 7LL);
    primes = list_append_long_long(primes, &primes_len, 11LL);
    primes = list_append_long_long(primes, &primes_len, 13LL);
    primes = list_append_long_long(primes, &primes_len, 17LL);
    long long i = 0LL;
    while (i < 4LL) {
        long long p = primes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
        long long idx = i + 4LL;
        long long val = ((num[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})] * 100LL) + (num[(int)({long long _mochi_idx = idx + 1LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})] * 10LL)) + num[(int)({long long _mochi_idx = idx + 2LL; _mochi_idx < 0 ? num_len + _mochi_idx : _mochi_idx;})];
        if ((val % p) != 0LL) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long * remove_at(long long * xs, size_t xs_len, long long idx) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        if (i != idx) {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_at_len = res_len, res;
}

long long digits_to_number(long long * xs, size_t xs_len) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < xs_len) {
        value = (value * 10LL) + xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return value;
}

long long search(long long * prefix, size_t prefix_len, long long * remaining, size_t remaining_len) {
    if (remaining_len == 0LL) {
        if (is_substring_divisible(prefix, prefix_len)) {
            return digits_to_number(prefix, prefix_len);
        }
        return 0LL;
    }
    long long total = 0LL;
    long long i = 0LL;
    while (i < remaining_len) {
        long long d = remaining[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? remaining_len + _mochi_idx : _mochi_idx;})];
        long long *next_prefix = list_append_int_new(prefix, prefix_len, d);
        size_t next_prefix_len = prefix_len + 1;
        long long *next_remaining = remove_at(remaining, remaining_len, i);
        size_t next_remaining_len = remove_at_len;
        total = total + search(next_prefix, next_prefix_len, next_remaining, next_remaining_len);
        i = i + 1LL;
    }
    return total;
}

long long solution(long long n) {
    long long *digits = NULL;
    size_t digits_len = 0;
    long long i = 0LL;
    while (i < n) {
        digits = list_append_long_long(digits, &digits_len, i);
        i = i + 1LL;
    }
    return search(NULL, 0, digits, digits_len);
}

int main(void) {
    {
        long long __start = _now();
        printf("%s %d\n", "solution() =", solution(10LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
