// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long DIGIT_FACTORIALS_init[10] = {1LL, 1LL, 2LL, 6LL, 24LL, 120LL, 720LL, 5040LL, 40320LL, 362880LL};
long long *DIGIT_FACTORIALS = DIGIT_FACTORIALS_init;
size_t DIGIT_FACTORIALS_len = 10;

long long sum_of_digit_factorial(long long n);
long long solution();
int main(void);

long long sum_of_digit_factorial(long long n) {
    if (n == 0LL) {
        return 1LL;
    }
    long long total = 0LL;
    long long num = n;
    while (num > 0LL) {
        long long digit = num % 10LL;
        total = total + DIGIT_FACTORIALS[(int)({long long _mochi_idx = digit; _mochi_idx < 0 ? DIGIT_FACTORIALS_len + _mochi_idx : _mochi_idx;})];
        num = num / 10LL;
    }
    return total;
}

long long solution() {
    long long limit = 2540161LL;
    long long total = 0LL;
    long long i = 3LL;
    while (i < limit) {
        if (sum_of_digit_factorial(i) == i) {
            total = total + i;
        }
        i = i + 1LL;
    }
    return total;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution()));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
