// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; void **vals; size_t *lens; size_t len; size_t cap; } MapSL;

static void* map_get_sl(const char *keys[], void *vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static void map_set_sl(const char *keys[], void *vals[], size_t lens[], size_t *len, const char *key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; lens[i] = val_len; return; }
    }
    keys[*len] = key; vals[*len] = val; lens[*len] = val_len; (*len)++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* get_digits(long long num);
long long solution(long long max_base);
int main(void);

const char* get_digits(long long num) {
    long long cube = (num * num) * num;
    const char* s = str_int(cube);
    long long *counts = NULL;
    size_t counts_len = 0;
    long long j = 0LL;
    while (j < 10LL) {
        counts = list_append_long_long(counts, &counts_len, 0LL);
        j = j + 1LL;
    }
    long long i = 0LL;
    while (i < strlen(s)) {
        long long d = _atoi((const char[]){s[i], 0});
        counts[(int)(d)] = counts[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] + 1LL;
        i = i + 1LL;
    }
    const char* result = "";
    long long d = 0LL;
    while (d < 10LL) {
        long long c = counts[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})];
        while (c > 0LL) {
            result = str_concat(result, str_int(d));
            c = c - 1LL;
        }
        d = d + 1LL;
    }
    return result;
}

long long solution(long long max_base) {
    static const char* freqs_keys[16] = {};
    static void* freqs_vals[16] = {};
    static size_t freqs_lens[16] = {};
    size_t freqs_len = 0;
    MapSL freqs = { freqs_keys, freqs_vals, freqs_lens, 0, 16 };
    long long num = 0LL;
    while (1LL) {
        const char* digits = get_digits(num);
        long long *arr = NULL;
        size_t arr_len = 0;
        if (contains_str(freqs.keys, freqs.len, digits)) {
            arr = map_get_sl(freqs.keys, freqs.vals, freqs.lens, freqs.len, digits, &arr_len);
        }
        arr = list_append_long_long(arr, &arr_len, num);
        map_set_sl(freqs.keys, freqs.vals, freqs.lens, &freqs.len, digits, arr, arr_len);
        if (arr_len == max_base) {
            long long base = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            return (base * base) * base;
        }
        num = num + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("solution() = ", str_int(solution(5LL))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
