// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_leap(long long year);
long long count_sundays();
int main(void);

long long is_leap(long long year) {
    if ((((year % 4LL) == 0LL) && ((year % 100LL) != 0LL)) || ((year % 400LL) == 0LL)) {
        return 1LL;
    }
    return 0LL;
}

long long count_sundays() {
    long long *days_per_month = NULL;
    size_t days_per_month_len = 0;
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 28LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 30LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 30LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 30LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 30LL);
    days_per_month = list_append_long_long(days_per_month, &days_per_month_len, 31LL);
    long long day = 6LL;
    long long month = 1LL;
    long long year = 1901LL;
    long long sundays = 0LL;
    while (year < 2001LL) {
        day = day + 7LL;
        if (is_leap(year)) {
            if ((day > days_per_month[(int)({long long _mochi_idx = month - 1LL; _mochi_idx < 0 ? days_per_month_len + _mochi_idx : _mochi_idx;})]) && (month != 2LL)) {
                month = month + 1LL;
                day = day - days_per_month[(int)({long long _mochi_idx = month - 2LL; _mochi_idx < 0 ? days_per_month_len + _mochi_idx : _mochi_idx;})];
            } else {
                if ((day > 29LL) && (month == 2LL)) {
                    month = month + 1LL;
                    day = day - 29LL;
                }
            }
        } else {
            if (day > days_per_month[(int)({long long _mochi_idx = month - 1LL; _mochi_idx < 0 ? days_per_month_len + _mochi_idx : _mochi_idx;})]) {
                month = month + 1LL;
                day = day - days_per_month[(int)({long long _mochi_idx = month - 2LL; _mochi_idx < 0 ? days_per_month_len + _mochi_idx : _mochi_idx;})];
            }
        }
        if (month > 12LL) {
            year = year + 1LL;
            month = 1LL;
        }
        if ((year < 2001LL) && (day == 1LL)) {
            sundays = sundays + 1LL;
        }
    }
    return sundays;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", count_sundays());
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
