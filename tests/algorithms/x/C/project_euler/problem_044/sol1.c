// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long result = 0;

double sqrt(double x);
long long is_pentagonal(long long n);
long long pentagonal(long long k);
long long solution(long long limit);
int main(void);

double sqrt(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

long long is_pentagonal(long long n) {
    double root = sqrt(1.0 + (24.0 * (1.0 * n)));
    double val = (1.0 + root) / 6.0;
    long long val_int = (int)(val);
    return val == (1.0 * val_int);
}

long long pentagonal(long long k) {
    return (k * ((3LL * k) - 1LL)) / 2LL;
}

long long solution(long long limit) {
    long long *pentagonal_nums = NULL;
    size_t pentagonal_nums_len = 0;
    long long i = 1LL;
    while (i < limit) {
        pentagonal_nums = list_append_long_long(pentagonal_nums, &pentagonal_nums_len, pentagonal(i));
        i = i + 1LL;
    }
    long long a_idx = 0LL;
    while (a_idx < pentagonal_nums_len) {
        long long pentagonal_i = pentagonal_nums[(int)({long long _mochi_idx = a_idx; _mochi_idx < 0 ? pentagonal_nums_len + _mochi_idx : _mochi_idx;})];
        long long b_idx = a_idx;
        while (b_idx < pentagonal_nums_len) {
            long long pentagonal_j = pentagonal_nums[(int)({long long _mochi_idx = b_idx; _mochi_idx < 0 ? pentagonal_nums_len + _mochi_idx : _mochi_idx;})];
            long long s = pentagonal_i + pentagonal_j;
            long long d = pentagonal_j - pentagonal_i;
            if (is_pentagonal(s) && is_pentagonal(d)) {
                return d;
            }
            b_idx = b_idx + 1LL;
        }
        a_idx = a_idx + 1LL;
    }
    return -1.0;
}

int main(void) {
    {
        long long __start = _now();
        result = solution(5000LL);
        puts(str_concat("solution() = ", str_int(result)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
