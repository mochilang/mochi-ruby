// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long is_prime(long long number);
long long solution(double ratio);
void test_solution();
void user_main();
int main(void);

long long is_prime(long long number) {
    if ((1LL < number) && (number < 4LL)) {
        return 1LL;
    }
    if (((number < 2LL) || ((number % 2LL) == 0LL)) || ((number % 3LL) == 0LL)) {
        return 0LL;
    }
    long long i = 5LL;
    while ((i * i) <= number) {
        if (((number % i) == 0LL) || ((number % (i + 2LL)) == 0LL)) {
            return 0LL;
        }
        i = i + 6LL;
    }
    return 1LL;
}

long long solution(double ratio) {
    long long j = 3LL;
    long long primes = 3LL;
    while (((double)(primes) / ((2LL * j) - 1LL)) >= ratio) {
        long long i = ((j * j) + j) + 1LL;
        long long limit = (j + 2LL) * (j + 2LL);
        long long step = j + 1LL;
        while (i < limit) {
            if (is_prime(i)) {
                primes = primes + 1LL;
            }
            i = i + step;
        }
        j = j + 2LL;
    }
    return j;
}

void test_solution() {
    if (solution(0.5) != 11LL) {
        panic("solution 0.5 failed");
    }
    if (solution(0.2) != 309LL) {
        panic("solution 0.2 failed");
    }
    if (solution(0.111) != 11317LL) {
        panic("solution 0.111 failed");
    }
}

void user_main() {
    test_solution();
    puts(str_int(solution(0.1)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
