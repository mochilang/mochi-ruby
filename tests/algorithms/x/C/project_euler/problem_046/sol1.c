// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t compute_nums_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long int_sqrt(long long n);
long long is_prime(long long number);
long long * compute_nums(long long n);
long long solution();
int main(void);

long long int_sqrt(long long n) {
    long long r = 0LL;
    while (((r + 1LL) * (r + 1LL)) <= n) {
        r = r + 1LL;
    }
    return r;
}

long long is_prime(long long number) {
    if ((1LL < number) && (number < 4LL)) {
        return 1LL;
    }
    if (((number < 2LL) || ((number % 2LL) == 0LL)) || ((number % 3LL) == 0LL)) {
        return 0LL;
    }
    long long i = 5LL;
    long long limit = int_sqrt(number);
    while (i <= limit) {
        if (((number % i) == 0LL) || ((number % (i + 2LL)) == 0LL)) {
            return 0LL;
        }
        i = i + 6LL;
    }
    return 1LL;
}

long long * compute_nums(long long n) {
    if (n <= 0LL) {
        panic("n must be >= 0");
    }
    long long *list_nums = NULL;
    size_t list_nums_len = 0;
    long long num = 3LL;
    while (list_nums_len < n) {
        if (!(is_prime(num))) {
            long long i = 0LL;
            long long found = 0LL;
            while (((2LL * i) * i) <= num) {
                long long rem = num - ((2LL * i) * i);
                if (is_prime(rem)) {
                    found = 1LL;
                    break;
                }
                i = i + 1LL;
            }
            if (!(found)) {
                list_nums = list_append_long_long(list_nums, &list_nums_len, num);
            }
        }
        num = num + 2LL;
    }
    return compute_nums_len = list_nums_len, list_nums;
}

long long solution() {
    return compute_nums(1LL)[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? compute_nums_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution()));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
