// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t parse_names_len;
size_t insertion_sort_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* * parse_names(const char* line);
const char* * insertion_sort(const char* * arr, size_t arr_len);
long long letter_value(const char* ch);
long long name_score(const char* name);
void user_main();
int main(void);

const char* * parse_names(const char* line) {
    const char* *names = NULL;
    size_t names_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(line)) {
        const char* ch = _substring(line, i, i + 1LL);
        if (strcmp(ch, ",") == 0) {
            names = list_append_str(names, &names_len, current);
            current = "";
        } else {
            if (strcmp(ch, "\"") != 0) {
                current = str_concat(current, ch);
            }
        }
        i = i + 1LL;
    }
    names = list_append_str(names, &names_len, current);
    return parse_names_len = names_len, names;
}

const char* * insertion_sort(const char* * arr, size_t arr_len) {
    const char* *a = arr;
    size_t a_len = arr_len;
    long long i = 1LL;
    while (i < a_len) {
        const char* key = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (strcmp(a[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], key) > 0)) {
            a[(int)(j + 1LL)] = a[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        a[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return insertion_sort_len = a_len, a;
}

long long letter_value(const char* ch) {
    const char* alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    long long idx = 0LL;
    while (idx < strlen(alphabet)) {
        if (strcmp(_substring(alphabet, idx, idx + 1LL), ch) == 0) {
            return idx + 1LL;
        }
        idx = idx + 1LL;
    }
    return 0LL;
}

long long name_score(const char* name) {
    long long score = 0LL;
    long long i = 0LL;
    while (i < strlen(name)) {
        score = score + letter_value(_substring(name, i, i + 1LL));
        i = i + 1LL;
    }
    return score;
}

void user_main() {
    const char* line = _input();
    const char* *names = ({const char** __tmp1 = parse_names(line); size_t __tmp1_len = parse_names_len; insertion_sort(__tmp1, __tmp1_len);});
    size_t names_len = insertion_sort_len;
    long long total = 0LL;
    long long i = 0LL;
    while (i < names_len) {
        total = total + ((i + 1LL) * name_score(names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? names_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    puts(str_int(total));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
