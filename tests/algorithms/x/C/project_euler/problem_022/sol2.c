// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bubble_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Name Name;

struct Name {
    const char* name;
};

Name *rows = NULL;
size_t rows_len = 0;
const char* *names = NULL;
size_t names_len = 0;
long long total = 0LL;
long long i = 0LL;

long long ord_letter(const char* ch);
long long name_value(const char* name);
const char* * bubble_sort(const char* * arr, size_t arr_len);
int main(void);

long long ord_letter(const char* ch) {
    const char* alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    long long i = 0LL;
    while (i < strlen(alphabet)) {
        if (strcmp(_substring(alphabet, i, i + 1LL), ch) == 0) {
            return i + 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long name_value(const char* name) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < strlen(name)) {
        total = total + ord_letter(_substring(name, i, i + 1LL));
        i = i + 1LL;
    }
    return total;
}

const char* * bubble_sort(const char* * arr, size_t arr_len) {
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j < ((n - i) - 1LL)) {
            if (strcmp(arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) > 0) {
                const char* temp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j + 1LL)] = temp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bubble_sort_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        for (size_t __i = 0; __i < rows_len; __i++) {
            Name r = rows[__i];
            names = list_append_str(names, &names_len, r.name);
        }
        names = bubble_sort(names, names_len);
        names_len = bubble_sort_len;
        while (i < names_len) {
            total = total + ((i + 1LL) * name_value(names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? names_len + _mochi_idx : _mochi_idx;})]));
            i = i + 1LL;
        }
        puts(str_int(total));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
