// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long DIGITS_FIFTH_POWER_init[10] = {0LL, 1LL, 32LL, 243LL, 1024LL, 3125LL, 7776LL, 16807LL, 32768LL, 59049LL};
long long *DIGITS_FIFTH_POWER = DIGITS_FIFTH_POWER_init;
size_t DIGITS_FIFTH_POWER_len = 10;

long long digits_fifth_powers_sum(long long number);
long long solution();
int main(void);

long long digits_fifth_powers_sum(long long number) {
    long long total = 0LL;
    long long n = number;
    while (n > 0LL) {
        long long digit = n % 10LL;
        total = total + DIGITS_FIFTH_POWER[(int)({long long _mochi_idx = digit; _mochi_idx < 0 ? DIGITS_FIFTH_POWER_len + _mochi_idx : _mochi_idx;})];
        n = n / 10LL;
    }
    return total;
}

long long solution() {
    long long total = 0LL;
    long long num = 1000LL;
    while (num < 1000000LL) {
        if (num == digits_fifth_powers_sum(num)) {
            total = total + num;
        }
        num = num + 1LL;
    }
    return total;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", solution());
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
