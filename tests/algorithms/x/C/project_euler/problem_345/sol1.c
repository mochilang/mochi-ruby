// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t parse_row_len;
size_t parse_matrix_len;
size_t *parse_matrix_lens;
size_t build_powers_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* MATRIX_2_init[15] = {"7 53 183 439 863 497 383 563 79 973 287 63 343 169 583", "627 343 773 959 943 767 473 103 699 303 957 703 583 639 913", "447 283 463 29 23 487 463 993 119 883 327 493 423 159 743", "217 623 3 399 853 407 103 983 89 463 290 516 212 462 350", "960 376 682 962 300 780 486 502 912 800 250 346 172 812 350", "870 456 192 162 593 473 915 45 989 873 823 965 425 329 803", "973 965 905 919 133 673 665 235 509 613 673 815 165 992 326", "322 148 972 962 286 255 941 541 265 323 925 281 601 95 973", "445 721 11 525 473 65 511 164 138 672 18 428 154 448 848", "414 456 310 312 798 104 566 520 302 248 694 976 430 392 198", "184 829 373 181 631 101 969 613 840 740 778 458 284 760 390", "821 461 843 513 17 901 711 993 293 157 274 94 192 156 574", "34 124 4 878 450 476 712 914 838 669 875 299 823 329 699", "815 559 813 459 522 788 168 586 966 232 308 833 251 631 107", "813 883 451 509 615 77 281 613 459 205 380 274 302 35 805"};
const char* *MATRIX_2 = MATRIX_2_init;
size_t MATRIX_2_len = 15;
long long result = 0;

long long * parse_row(const char* row_str);
long long * * parse_matrix(const char* * matrix_str, size_t matrix_str_len);
long long bitcount(long long x);
long long * build_powers(long long n);
long long solution(const char* * matrix_str, size_t matrix_str_len);
int main(void);

long long * parse_row(const char* row_str) {
    long long *nums = NULL;
    size_t nums_len = 0;
    long long current = 0LL;
    long long has_digit = 0LL;
    long long i = 0LL;
    while (i < strlen(row_str)) {
        const char* ch = _substring(row_str, i, i + 1LL);
        if (strcmp(ch, " ") == 0) {
            if (has_digit) {
                nums = list_append_long_long(nums, &nums_len, current);
                current = 0LL;
                has_digit = 0LL;
            }
        } else {
            current = (current * 10LL) + _atoi(ch);
            has_digit = 1LL;
        }
        i = i + 1LL;
    }
    if (has_digit) {
        nums = list_append_long_long(nums, &nums_len, current);
    }
    return parse_row_len = nums_len, nums;
}

long long * * parse_matrix(const char* * matrix_str, size_t matrix_str_len) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    for (size_t __i = 0; __i < matrix_str_len; __i++) {
        const char* row_str = matrix_str[__i];
        long long *row = parse_row(row_str);
        size_t row_len = parse_row_len;
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
    }
    return parse_matrix_lens = matrix_lens, parse_matrix_len = matrix_len, matrix;
}

long long bitcount(long long x) {
    long long count_ = 0LL;
    long long y = x;
    while (y > 0LL) {
        if ((y % 2LL) == 1LL) {
            count_ = count_ + 1LL;
        }
        y = y / 2LL;
    }
    return count_;
}

long long * build_powers(long long n) {
    long long *powers = NULL;
    size_t powers_len = 0;
    long long i = 0LL;
    long long current = 1LL;
    while (i <= n) {
        powers = list_append_long_long(powers, &powers_len, current);
        current = current * 2LL;
        i = i + 1LL;
    }
    return build_powers_len = powers_len, powers;
}

long long solution(const char* * matrix_str, size_t matrix_str_len) {
    long long **arr = parse_matrix(matrix_str, matrix_str_len);
    size_t arr_len = parse_matrix_len;
    size_t *arr_lens = parse_matrix_lens;
    size_t arr_lens_len = parse_matrix_len;
    long long n = arr_len;
    long long *powers = build_powers(n);
    size_t powers_len = build_powers_len;
    long long size = powers[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? powers_len + _mochi_idx : _mochi_idx;})];
    long long *dp = NULL;
    size_t dp_len = 0;
    long long i = 0LL;
    while (i < size) {
        dp = list_append_long_long(dp, &dp_len, 0LL);
        i = i + 1LL;
    }
    long long mask = 0LL;
    while (mask < size) {
        long long row = bitcount(mask);
        if (row < n) {
            long long col = 0LL;
            while (col < n) {
                if (((mask / powers[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? powers_len + _mochi_idx : _mochi_idx;})]) % 2LL) == 0LL) {
                    long long new_mask = mask + powers[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? powers_len + _mochi_idx : _mochi_idx;})];
                    long long value = dp[(int)({long long _mochi_idx = mask; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})] + arr[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? arr_lens[(int)(row)] + _mochi_idx : _mochi_idx;})];
                    if (value > dp[(int)({long long _mochi_idx = new_mask; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})]) {
                        dp[(int)(new_mask)] = value;
                    }
                }
                col = col + 1LL;
            }
        }
        mask = mask + 1LL;
    }
    return dp[(int)({long long _mochi_idx = size - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        result = solution(MATRIX_2, MATRIX_2_len);
        puts(str_concat("solution() = ", str_int(result)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
