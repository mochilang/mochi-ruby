// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long solution();
void test_solution();
void user_main();
int main(void);

long long solution() {
    long long *targets = NULL;
    size_t targets_len = 0;
    targets = list_append_long_long(targets, &targets_len, 1LL);
    targets = list_append_long_long(targets, &targets_len, 10LL);
    targets = list_append_long_long(targets, &targets_len, 100LL);
    targets = list_append_long_long(targets, &targets_len, 1000LL);
    targets = list_append_long_long(targets, &targets_len, 10000LL);
    targets = list_append_long_long(targets, &targets_len, 100000LL);
    targets = list_append_long_long(targets, &targets_len, 1000000LL);
    long long idx = 0LL;
    long long product = 1LL;
    long long count_ = 0LL;
    long long i = 1LL;
    while (idx < 7LL) {
        const char* s = str_int(i);
        long long j = 0LL;
        while (j < strlen(s)) {
            count_ = count_ + 1LL;
            if (count_ == targets[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? targets_len + _mochi_idx : _mochi_idx;})]) {
                product = product * s[j] - '0';
                idx = idx + 1LL;
                if (idx == 7LL) {
                    break;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return product;
}

void test_solution() {
    if (solution() != 210LL) {
        panic("solution failed");
    }
}

void user_main() {
    test_solution();
    puts(str_int(solution()));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
