// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long solution(long long pence);
int main(void);

long long solution(long long pence) {
    long long *coins = NULL;
    size_t coins_len = 0;
    coins = list_append_long_long(coins, &coins_len, 1LL);
    coins = list_append_long_long(coins, &coins_len, 2LL);
    coins = list_append_long_long(coins, &coins_len, 5LL);
    coins = list_append_long_long(coins, &coins_len, 10LL);
    coins = list_append_long_long(coins, &coins_len, 20LL);
    coins = list_append_long_long(coins, &coins_len, 50LL);
    coins = list_append_long_long(coins, &coins_len, 100LL);
    coins = list_append_long_long(coins, &coins_len, 200LL);
    long long *ways = NULL;
    size_t ways_len = 0;
    long long i = 0LL;
    while (i <= pence) {
        ways = list_append_long_long(ways, &ways_len, 0LL);
        i = i + 1LL;
    }
    ways[(int)(0LL)] = 1LL;
    long long idx = 0LL;
    while (idx < 8LL) {
        long long coin = coins[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? coins_len + _mochi_idx : _mochi_idx;})];
        long long j = coin;
        while (j <= pence) {
            ways[(int)(j)] = ways[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ways_len + _mochi_idx : _mochi_idx;})] + ways[(int)({long long _mochi_idx = j - coin; _mochi_idx < 0 ? ways_len + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        idx = idx + 1LL;
    }
    return ways[(int)({long long _mochi_idx = pence; _mochi_idx < 0 ? ways_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", solution(500LL));
        printf("%d\n", solution(200LL));
        printf("%d\n", solution(50LL));
        printf("%d\n", solution(10LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
