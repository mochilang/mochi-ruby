// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long solution(long long number);
void user_main();
int main(void);

long long solution(long long number) {
    long long *partitions = NULL;
    size_t partitions_len = 0;
    partitions = list_append_long_long(partitions, &partitions_len, 1LL);
    long long i = partitions_len;
    while (1) {
        long long item = 0LL;
        long long j = 1LL;
        while (1) {
            long long sign;
            if ((j % 2LL) == 0LL) {
                sign = -1.0;
            } else {
                sign = 1LL;
            }
            long long index = (((j * j) * 3LL) - j) / 2LL;
            if (index > i) {
                break;
            }
            item = item + (partitions[(int)({long long _mochi_idx = i - index; _mochi_idx < 0 ? partitions_len + _mochi_idx : _mochi_idx;})] * sign);
            item = item % number;
            index = index + j;
            if (index > i) {
                break;
            }
            item = item + (partitions[(int)({long long _mochi_idx = i - index; _mochi_idx < 0 ? partitions_len + _mochi_idx : _mochi_idx;})] * sign);
            item = item % number;
            j = j + 1LL;
        }
        if (item == 0LL) {
            return i;
        }
        partitions = list_append_long_long(partitions, &partitions_len, item);
        i = i + 1LL;
    }
    return 0LL;
}

void user_main() {
    puts(str_int(solution(1LL)));
    puts(str_int(solution(9LL)));
    puts(str_int(solution(1000000LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
