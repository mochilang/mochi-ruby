// Generated by Mochi 0.10.32 on 2025-08-12 12:29 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(int keys[], int vals[], size_t *len, int key, int val) {
    for (size_t i = 0; i < *len; i++) {
        if (keys[i] == key) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long solution(long long n);
int main(void);

long long solution(long long n) {
    static int composite_keys[16] = {};
    static int composite_vals[16] = {};
    size_t composite_len = 0;
    MapII composite = { composite_keys, composite_vals, 0, 16 };
    long long total = 0LL;
    long long num = 2LL;
    while (num < n) {
        if (!(map_get_ii(composite_keys, composite_vals, composite_len, num))) {
            total = total + num;
            long long multiple = num * num;
            while (multiple < n) {
                map_set_ii(composite_keys, composite_vals, &composite_len, multiple, 1LL);
                multiple = multiple + num;
            }
        }
        num = num + 1LL;
    }
    return total;
}

int main(void) {
    puts(str_int(solution(2000000LL)));
    return 0;
}
