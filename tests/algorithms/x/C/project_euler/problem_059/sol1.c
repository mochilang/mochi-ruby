// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t filter_valid_chars_len;
size_t filter_common_word_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* ascii_chars = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";
long long *LOWERCASE_INTS = NULL;
size_t LOWERCASE_INTS_len = 0;
long long i = 97LL;
const char* COMMON_WORDS_init[8] = {"the", "be", "to", "of", "and", "in", "that", "have"};
const char* *COMMON_WORDS = COMMON_WORDS_init;
size_t COMMON_WORDS_len = 8;
long long ciphertext_init[29] = {17LL, 6LL, 1LL, 69LL, 12LL, 1LL, 69LL, 26LL, 11LL, 69LL, 1LL, 2LL, 69LL, 15LL, 10LL, 1LL, 78LL, 13LL, 11LL, 78LL, 16LL, 13LL, 15LL, 16LL, 69LL, 6LL, 5LL, 19LL, 11LL};
long long *ciphertext = ciphertext_init;
size_t ciphertext_len = 29;

long long xor(long long a, long long b);
const char* chr(long long code);
long long ord(const char* ch);
long long is_valid_ascii(long long code);
const char* try_key(long long * ciphertext, size_t ciphertext_len, long long * key, size_t key_len);
const char* * filter_valid_chars(long long * ciphertext, size_t ciphertext_len);
long long contains(const char* s, const char* sub);
const char* * filter_common_word(const char* * possibles, size_t possibles_len, const char* common_word);
long long solution(long long * ciphertext, size_t ciphertext_len);
int main(void);

long long xor(long long a, long long b) {
    long long res = 0LL;
    long long bit = 1LL;
    long long x = a;
    long long y = b;
    while ((x > 0LL) || (y > 0LL)) {
        long long abit = x % 2LL;
        long long bbit = y % 2LL;
        if (abit != bbit) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

const char* chr(long long code) {
    if (code == 10LL) {
        return "\n";
    }
    if (code == 13LL) {
        return "\r";
    }
    if (code == 9LL) {
        return "\t";
    }
    if ((code >= 32LL) && (code < 127LL)) {
        return _substring(ascii_chars, code - 32LL, code - 31LL);
    }
    return "";
}

long long ord(const char* ch) {
    if (strcmp(ch, "\n") == 0) {
        return 10LL;
    }
    if (strcmp(ch, "\r") == 0) {
        return 13LL;
    }
    if (strcmp(ch, "\t") == 0) {
        return 9LL;
    }
    long long i = 0LL;
    while (i < strlen(ascii_chars)) {
        if (strcmp(_substring(ascii_chars, i, i + 1LL), ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long is_valid_ascii(long long code) {
    if ((code >= 32LL) && (code <= 126LL)) {
        return 1LL;
    }
    if (((code == 9LL) || (code == 10LL)) || (code == 13LL)) {
        return 1LL;
    }
    return 0LL;
}

const char* try_key(long long * ciphertext, size_t ciphertext_len, long long * key, size_t key_len) {
    const char* decoded = "";
    long long i = 0LL;
    long long klen = key_len;
    while (i < ciphertext_len) {
        long long decodedchar = xor(ciphertext[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ciphertext_len + _mochi_idx : _mochi_idx;})], key[(int)({long long _mochi_idx = i % klen; _mochi_idx < 0 ? key_len + _mochi_idx : _mochi_idx;})]);
        if (!(is_valid_ascii(decodedchar))) {
            return NULL;
        }
        decoded = str_concat(decoded, chr(decodedchar));
        i = i + 1LL;
    }
    return decoded;
}

const char* * filter_valid_chars(long long * ciphertext, size_t ciphertext_len) {
    const char* *possibles = NULL;
    size_t possibles_len = 0;
    long long i = 0LL;
    while (i < LOWERCASE_INTS_len) {
        long long j = 0LL;
        while (j < LOWERCASE_INTS_len) {
            long long k = 0LL;
            while (k < LOWERCASE_INTS_len) {
                long long *key = NULL;
                size_t key_len = 0;
                key = list_append_long_long(key, &key_len, LOWERCASE_INTS[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? LOWERCASE_INTS_len + _mochi_idx : _mochi_idx;})]);
                key = list_append_long_long(key, &key_len, LOWERCASE_INTS[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? LOWERCASE_INTS_len + _mochi_idx : _mochi_idx;})]);
                key = list_append_long_long(key, &key_len, LOWERCASE_INTS[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? LOWERCASE_INTS_len + _mochi_idx : _mochi_idx;})]);
                const char* decoded = try_key(ciphertext, ciphertext_len, key, key_len);
                if (decoded != NULL) {
                    possibles = list_append_str(possibles, &possibles_len, decoded);
                }
                k = k + 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return filter_valid_chars_len = possibles_len, possibles;
}

long long contains(const char* s, const char* sub) {
    long long n = strlen(s);
    long long m = strlen(sub);
    if (m == 0LL) {
        return 1LL;
    }
    long long i = 0LL;
    while (i <= (n - m)) {
        long long j = 0LL;
        long long is_match = 1LL;
        while (j < m) {
            if (strcmp((const char[]){s[i + j], 0}, (const char[]){sub[j], 0}) != 0) {
                is_match = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (is_match) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * filter_common_word(const char* * possibles, size_t possibles_len, const char* common_word) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < possibles_len) {
        const char* p = possibles[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? possibles_len + _mochi_idx : _mochi_idx;})];
        if (contains(str_lower(p), common_word)) {
            res = list_append_str(res, &res_len, p);
        }
        i = i + 1LL;
    }
    return filter_common_word_len = res_len, res;
}

long long solution(long long * ciphertext, size_t ciphertext_len) {
    const char* *possibles = filter_valid_chars(ciphertext, ciphertext_len);
    size_t possibles_len = filter_valid_chars_len;
    long long i = 0LL;
    while (i < COMMON_WORDS_len) {
        const char* word = COMMON_WORDS[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? COMMON_WORDS_len + _mochi_idx : _mochi_idx;})];
        possibles = filter_common_word(possibles, possibles_len, word);
        possibles_len = filter_common_word_len;
        if (possibles_len == 1LL) {
            break;
        }
        i = i + 1LL;
    }
    const char* decoded_text = possibles[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? possibles_len + _mochi_idx : _mochi_idx;})];
    long long sum = 0LL;
    long long j = 0LL;
    while (j < strlen(decoded_text)) {
        sum = sum + ord(_substring(decoded_text, j, j + 1LL));
        j = j + 1LL;
    }
    return sum;
}

int main(void) {
    {
        long long __start = _now();
        while (i <= 122LL) {
            LOWERCASE_INTS = list_append_long_long(LOWERCASE_INTS, &LOWERCASE_INTS_len, i);
            i = i + 1LL;
        }
        puts(str_int(solution(ciphertext, ciphertext_len)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
