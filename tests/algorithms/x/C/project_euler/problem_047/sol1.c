// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t run_len;

size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        int *new_keys = malloc(m->cap * sizeof(int));
        int *new_vals = malloc(m->cap * sizeof(int));
        if (m->keys) memcpy(new_keys, m->keys, m->len * sizeof(int));
        if (m->vals) memcpy(new_vals, m->vals, m->len * sizeof(int));
        m->keys = new_keys;
        m->vals = new_vals;
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * run(long long n);
long long solution(long long n);
int main(void);

long long * run(long long n) {
    long long limit = 200000LL;
    MapII counts = { NULL, NULL, 0, 0 };
    long long p = 2LL;
    while (p <= limit) {
        if (!(contains_map_int(counts.keys, counts.len, p))) {
            long long m = p;
            while (m <= limit) {
                if (contains_map_int(counts.keys, counts.len, m)) {
                    map_set_ii(&counts, m, map_get_ii(counts.keys, counts.vals, counts.len, m) + 1LL);
                } else {
                    map_set_ii(&counts, m, 1LL);
                }
                m = m + p;
            }
        }
        p = p + 1LL;
    }
    long long streak = 0LL;
    long long num = 2LL;
    while (num <= limit) {
        long long c;
        if (contains_map_int(counts.keys, counts.len, num)) {
            c = map_get_ii(counts.keys, counts.vals, counts.len, num);
        } else {
            c = 0LL;
        }
        if (c == n) {
            streak = streak + 1LL;
            if (streak == n) {
                long long *result = NULL;
                size_t result_len = 0;
                long long start = (num - n) + 1LL;
                long long j = 0LL;
                while (j < n) {
                    result = list_append_long_long(result, &result_len, start + j);
                    j = j + 1LL;
                }
                return run_len = result_len, result;
            }
        } else {
            streak = 0LL;
        }
        num = num + 1LL;
    }
    {
        long long* __tmp0_data = malloc(0 * sizeof(long long));
        run_len = 0;
        return __tmp0_data;
    }
}

long long solution(long long n) {
    long long *res = run(n);
    size_t res_len = run_len;
    return res[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(4LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
