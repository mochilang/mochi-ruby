// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t list_truncated_nums_len;
size_t compute_truncated_primes_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_prime(long long number);
long long * list_truncated_nums(long long n);
long long validate(long long n);
long long * compute_truncated_primes(long long count_);
long long solution();
int main(void);

long long is_prime(long long number) {
    if ((1LL < number) && (number < 4LL)) {
        return 1LL;
    }
    if (((number < 2LL) || ((number % 2LL) == 0LL)) || ((number % 3LL) == 0LL)) {
        return 0LL;
    }
    long long i = 5LL;
    while ((i * i) <= number) {
        if (((number % i) == 0LL) || ((number % (i + 2LL)) == 0LL)) {
            return 0LL;
        }
        i = i + 6LL;
    }
    return 1LL;
}

long long * list_truncated_nums(long long n) {
    const char* str_num = str_int(n);
    long long *list_nums = NULL;
    size_t list_nums_len = 0;
    list_nums = list_append_long_long(list_nums, &list_nums_len, n);
    long long i = 1LL;
    long long length = strlen(str_num);
    while (i < length) {
        long long right = _atoi(_substring(str_num, i, length));
        long long left = _atoi(_substring(str_num, 0LL, length - i));
        list_nums = list_append_long_long(list_nums, &list_nums_len, right);
        list_nums = list_append_long_long(list_nums, &list_nums_len, left);
        i = i + 1LL;
    }
    return list_truncated_nums_len = list_nums_len, list_nums;
}

long long validate(long long n) {
    const char* s = str_int(n);
    long long length = strlen(s);
    if (length > 3LL) {
        long long last3 = _atoi(_substring(s, length - 3LL, length));
        long long first3 = _atoi(_substring(s, 0LL, 3LL));
        if (!(is_prime(last3) && is_prime(first3))) {
            return 0LL;
        }
    }
    return 1LL;
}

long long * compute_truncated_primes(long long count_) {
    long long *list_truncated_primes = NULL;
    size_t list_truncated_primes_len = 0;
    long long num = 13LL;
    while (list_truncated_primes_len != count_) {
        if (validate(num)) {
            long long *list_nums = list_truncated_nums(num);
            size_t list_nums_len = list_truncated_nums_len;
            long long all_prime = 1LL;
            long long j = 0LL;
            while (j < list_nums_len) {
                if (!(is_prime(list_nums[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? list_nums_len + _mochi_idx : _mochi_idx;})]))) {
                    all_prime = 0LL;
                    break;
                }
                j = j + 1LL;
            }
            if (all_prime) {
                list_truncated_primes = list_append_long_long(list_truncated_primes, &list_truncated_primes_len, num);
            }
        }
        num = num + 2LL;
    }
    return compute_truncated_primes_len = list_truncated_primes_len, list_truncated_primes;
}

long long solution() {
    long long *primes = compute_truncated_primes(11LL);
    size_t primes_len = compute_truncated_primes_len;
    long long total = 0LL;
    long long i = 0LL;
    while (i < primes_len) {
        total = total + primes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return total;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("sum(compute_truncated_primes(11)) = ", str_int(solution())));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
