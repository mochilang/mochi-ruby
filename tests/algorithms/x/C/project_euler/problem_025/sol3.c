// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long num_digits(long long x);
long long solution(long long n);
int main(void);

long long num_digits(long long x) {
    long long count_ = 0LL;
    long long n = x;
    while (n > 0LL) {
        count_ = count_ + 1LL;
        n = n / 10LL;
    }
    return count_;
}

long long solution(long long n) {
    long long f1 = 1LL;
    long long f2 = 1LL;
    long long index = 2LL;
    while (1LL) {
        long long f = f1 + f2;
        f1 = f2;
        f2 = f;
        index = index + 1LL;
        if (num_digits(f) == n) {
            break;
        }
    }
    return index;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("solution(1000) = ", str_int(solution(1000LL))));
        puts(str_concat("solution(100) = ", str_int(solution(100LL))));
        puts(str_concat("solution(50) = ", str_int(solution(50LL))));
        puts(str_concat("solution(3) = ", str_int(solution(3LL))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
