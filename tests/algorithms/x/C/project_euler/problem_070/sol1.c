// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_totients_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * get_totients(long long max_one);
long long has_same_digits(long long num1, long long num2);
long long solution(long long max_n);
int main(void);

long long * get_totients(long long max_one) {
    long long *totients = NULL;
    size_t totients_len = 0;
    long long i = 0LL;
    while (i < max_one) {
        totients = list_append_long_long(totients, &totients_len, i);
        i = i + 1LL;
    }
    i = 2LL;
    while (i < max_one) {
        if (totients[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? totients_len + _mochi_idx : _mochi_idx;})] == i) {
            long long x = i;
            while (x < max_one) {
                totients[(int)(x)] = totients[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? totients_len + _mochi_idx : _mochi_idx;})] - (totients[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? totients_len + _mochi_idx : _mochi_idx;})] / i);
                x = x + i;
            }
        }
        i = i + 1LL;
    }
    return get_totients_len = totients_len, totients;
}

long long has_same_digits(long long num1, long long num2) {
    long long *count1 = NULL;
    size_t count1_len = 0;
    long long *count2 = NULL;
    size_t count2_len = 0;
    long long i = 0LL;
    while (i < 10LL) {
        count1 = list_append_long_long(count1, &count1_len, 0LL);
        count2 = list_append_long_long(count2, &count2_len, 0LL);
        i = i + 1LL;
    }
    long long n1 = num1;
    long long n2 = num2;
    if (n1 == 0LL) {
        count1[(int)(0LL)] = count1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? count1_len + _mochi_idx : _mochi_idx;})] + 1LL;
    }
    if (n2 == 0LL) {
        count2[(int)(0LL)] = count2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? count2_len + _mochi_idx : _mochi_idx;})] + 1LL;
    }
    while (n1 > 0LL) {
        long long d1 = n1 % 10LL;
        count1[(int)(d1)] = count1[(int)({long long _mochi_idx = d1; _mochi_idx < 0 ? count1_len + _mochi_idx : _mochi_idx;})] + 1LL;
        n1 = n1 / 10LL;
    }
    while (n2 > 0LL) {
        long long d2 = n2 % 10LL;
        count2[(int)(d2)] = count2[(int)({long long _mochi_idx = d2; _mochi_idx < 0 ? count2_len + _mochi_idx : _mochi_idx;})] + 1LL;
        n2 = n2 / 10LL;
    }
    i = 0LL;
    while (i < 10LL) {
        if (count1[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? count1_len + _mochi_idx : _mochi_idx;})] != count2[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? count2_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long solution(long long max_n) {
    long long min_numerator = 1LL;
    long long min_denominator = 0LL;
    long long *totients = get_totients(max_n + 1LL);
    size_t totients_len = get_totients_len;
    long long i = 2LL;
    while (i <= max_n) {
        long long t = totients[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? totients_len + _mochi_idx : _mochi_idx;})];
        if (((i * min_denominator) < (min_numerator * t)) && has_same_digits(i, t)) {
            min_numerator = i;
            min_denominator = t;
        }
        i = i + 1LL;
    }
    return min_numerator;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(10000LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
