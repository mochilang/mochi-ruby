// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long solution(long long limit);
void user_main();
int main(void);

long long solution(long long limit) {
    long long *phi = NULL;
    size_t phi_len = 0;
    long long i = 0LL;
    while (i <= limit) {
        phi = list_append_long_long(phi, &phi_len, i);
        i = i + 1LL;
    }
    long long n = 2LL;
    while (n <= limit) {
        if (phi[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})] == n) {
            long long k = n;
            while (k <= limit) {
                phi[(int)(k)] = phi[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})] - (phi[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})] / n);
                k = k + n;
            }
        }
        n = n + 1LL;
    }
    long long total = 0LL;
    long long m = 2LL;
    while (m <= limit) {
        total = total + phi[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})];
        m = m + 1LL;
    }
    return total;
}

void user_main() {
    printf("%d\n", solution(8LL));
    printf("%d\n", solution(1000LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
