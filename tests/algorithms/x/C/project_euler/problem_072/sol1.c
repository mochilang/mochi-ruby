// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t totients_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * totients(long long limit);
long long solution(long long limit);
int main(void);

long long * totients(long long limit) {
    long long *is_prime = NULL;
    size_t is_prime_len = 0;
    long long *phi = NULL;
    size_t phi_len = 0;
    long long *primes = NULL;
    size_t primes_len = 0;
    long long i = 0LL;
    while (i <= limit) {
        is_prime = list_append_long_long(is_prime, &is_prime_len, 1LL);
        phi = list_append_long_long(phi, &phi_len, i - 1LL);
        i = i + 1LL;
    }
    i = 2LL;
    while (i <= limit) {
        if (is_prime[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? is_prime_len + _mochi_idx : _mochi_idx;})]) {
            primes = list_append_long_long(primes, &primes_len, i);
        }
        long long j = 0LL;
        while (j < primes_len) {
            long long p = primes[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
            if ((i * p) > limit) {
                break;
            }
            is_prime[(int)(i * p)] = 0LL;
            if ((i % p) == 0LL) {
                phi[(int)(i * p)] = phi[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})] * p;
                break;
            }
            phi[(int)(i * p)] = phi[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})] * (p - 1LL);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return totients_len = phi_len, phi;
}

long long solution(long long limit) {
    long long *phi = totients(limit);
    size_t phi_len = totients_len;
    long long total = 0LL;
    long long k = 2LL;
    while (k <= limit) {
        total = total + phi[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? phi_len + _mochi_idx : _mochi_idx;})];
        k = k + 1LL;
    }
    return total;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(1000000LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
