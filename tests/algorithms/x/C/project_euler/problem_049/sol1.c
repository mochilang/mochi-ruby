// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_int_len;
size_t permutations_of_number_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_prime(long long number);
long long search(long long target, long long * arr, size_t arr_len);
long long * sort_int(long long * xs, size_t xs_len);
long long * permutations_of_number(long long n);
long long abs_int(long long x);
long long contains_int(long long * xs, size_t xs_len, long long v);
long long solution();
int main(void);

long long is_prime(long long number) {
    if ((1LL < number) && (number < 4LL)) {
        return 1LL;
    }
    if (((number < 2LL) || ((number % 2LL) == 0LL)) || ((number % 3LL) == 0LL)) {
        return 0LL;
    }
    long long i = 5LL;
    while ((i * i) <= number) {
        if (((number % i) == 0LL) || ((number % (i + 2LL)) == 0LL)) {
            return 0LL;
        }
        i = i + 6LL;
    }
    return 1LL;
}

long long search(long long target, long long * arr, size_t arr_len) {
    long long left = 0LL;
    long long right = arr_len - 1LL;
    while (left <= right) {
        long long middle = (left + right) / 2LL;
        if (arr[(int)({long long _mochi_idx = middle; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == target) {
            return 1LL;
        }
        if (arr[(int)({long long _mochi_idx = middle; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < target) {
            left = middle + 1LL;
        } else {
            right = middle - 1LL;
        }
    }
    return 0LL;
}

long long * sort_int(long long * xs, size_t xs_len) {
    long long *arr = xs;
    size_t arr_len = xs_len;
    long long i = 0LL;
    while (i < arr_len) {
        long long j = i + 1LL;
        while (j < arr_len) {
            if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_int_len = arr_len, arr;
}

long long * permutations_of_number(long long n) {
    const char* s = str_int(n);
    long long *d = NULL;
    size_t d_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        d = list_append_long_long(d, &d_len, _atoi((const char[]){s[i], 0}));
        i = i + 1LL;
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long a = 0LL;
    while (a < d_len) {
        long long b = 0LL;
        while (b < d_len) {
            if (b != a) {
                long long c = 0LL;
                while (c < d_len) {
                    if ((c != a) && (c != b)) {
                        long long e = 0LL;
                        while (e < d_len) {
                            if (((e != a) && (e != b)) && (e != c)) {
                                long long val = (((d[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? d_len + _mochi_idx : _mochi_idx;})] * 1000LL) + (d[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? d_len + _mochi_idx : _mochi_idx;})] * 100LL)) + (d[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? d_len + _mochi_idx : _mochi_idx;})] * 10LL)) + d[(int)({long long _mochi_idx = e; _mochi_idx < 0 ? d_len + _mochi_idx : _mochi_idx;})];
                                res = list_append_long_long(res, &res_len, val);
                            }
                            e = e + 1LL;
                        }
                    }
                    c = c + 1LL;
                }
            }
            b = b + 1LL;
        }
        a = a + 1LL;
    }
    return permutations_of_number_len = res_len, res;
}

long long abs_int(long long x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long contains_int(long long * xs, size_t xs_len, long long v) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == v) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long solution() {
    long long *prime_list = NULL;
    size_t prime_list_len = 0;
    long long n = 1001LL;
    while (n < 10000LL) {
        if (is_prime(n)) {
            prime_list = list_append_long_long(prime_list, &prime_list_len, n);
        }
        n = n + 2LL;
    }
    long long **candidates = NULL;
    size_t candidates_len = 0;
    size_t *candidates_lens = NULL;
    size_t candidates_lens_len = 0;
    long long i = 0LL;
    while (i < prime_list_len) {
        long long number = prime_list[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prime_list_len + _mochi_idx : _mochi_idx;})];
        long long *tmp = NULL;
        size_t tmp_len = 0;
        long long *perms = permutations_of_number(number);
        size_t perms_len = permutations_of_number_len;
        long long j = 0LL;
        while (j < perms_len) {
            long long prime = perms[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? perms_len + _mochi_idx : _mochi_idx;})];
            if (((prime % 2LL) != 0LL) && search(prime, prime_list, prime_list_len)) {
                tmp = list_append_long_long(tmp, &tmp_len, prime);
            }
            j = j + 1LL;
        }
        tmp = sort_int(tmp, tmp_len);
        tmp_len = sort_int_len;
        if (tmp_len >= 3LL) {
            candidates = list_append_intptr(candidates, &candidates_len, tmp);
            candidates_lens = list_append_szt(candidates_lens, &candidates_lens_len, tmp_len);
        }
        i = i + 1LL;
    }
    long long **passed = NULL;
    size_t passed_len = 0;
    size_t *passed_lens = NULL;
    size_t passed_lens_len = 0;
    i = 0LL;
    while (i < candidates_len) {
        long long *candidate = candidates[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? candidates_len + _mochi_idx : _mochi_idx;})];
        size_t candidate_len = candidates_lens[(int)(i)];
        long long found = 0LL;
        long long a = 0LL;
        while (a < candidate_len) {
            long long b = a + 1LL;
            while (b < candidate_len) {
                long long c = b + 1LL;
                while (c < candidate_len) {
                    long long x = candidate[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? candidate_len + _mochi_idx : _mochi_idx;})];
                    long long y = candidate[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? candidate_len + _mochi_idx : _mochi_idx;})];
                    long long z = candidate[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? candidate_len + _mochi_idx : _mochi_idx;})];
                    if ((((abs_int(x - y) == abs_int(y - z)) && (x != y)) && (x != z)) && (y != z)) {
                        long long *triple = sort_int(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = x; tmp[1] = y; tmp[2] = z; tmp;}), 3);
                        size_t triple_len = sort_int_len;
                        passed = list_append_intptr(passed, &passed_len, triple);
                        passed_lens = list_append_szt(passed_lens, &passed_lens_len, triple_len);
                        found = 1LL;
                        break;
                    }
                    c = c + 1LL;
                }
                if (found) {
                    break;
                }
                b = b + 1LL;
            }
            if (found) {
                break;
            }
            a = a + 1LL;
        }
        i = i + 1LL;
    }
    long long *answer_nums = NULL;
    size_t answer_nums_len = 0;
    i = 0LL;
    while (i < passed_len) {
        long long *seq = passed[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? passed_len + _mochi_idx : _mochi_idx;})];
        size_t seq_len = passed_lens[(int)(i)];
        long long val = _atoi(str_concat(str_concat(str_int(seq[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})]), str_int(seq[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})])), str_int(seq[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})])));
        if (!(contains_int(answer_nums, answer_nums_len, val))) {
            answer_nums = list_append_long_long(answer_nums, &answer_nums_len, val);
        }
        i = i + 1LL;
    }
    long long max_val = answer_nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? answer_nums_len + _mochi_idx : _mochi_idx;})];
    i = 1LL;
    while (i < answer_nums_len) {
        if (answer_nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? answer_nums_len + _mochi_idx : _mochi_idx;})] > max_val) {
            max_val = answer_nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? answer_nums_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return max_val;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", solution());
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
