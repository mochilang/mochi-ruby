// Generated by Mochi 0.10.32 on 2025-08-23 20:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        int *new_keys = malloc(m->cap * sizeof(int));
        int *new_vals = malloc(m->cap * sizeof(int));
        if (m->keys) memcpy(new_keys, m->keys, m->len * sizeof(int));
        if (m->vals) memcpy(new_vals, m->vals, m->len * sizeof(int));
        m->keys = new_keys;
        m->vals = new_vals;
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long DIGIT_FACTORIAL_init[10] = {1LL, 1LL, 2LL, 6LL, 24LL, 120LL, 720LL, 5040LL, 40320LL, 362880LL};
long long *DIGIT_FACTORIAL = DIGIT_FACTORIAL_init;
size_t DIGIT_FACTORIAL_len = 10;

long long digit_factorial_sum(long long number);
long long chain_len(long long n, long long limit);
long long solution(long long chain_length, long long number_limit);
int main(void);

long long digit_factorial_sum(long long number) {
    if (number < 0LL) {
        panic("Parameter number must be greater than or equal to 0");
    }
    if (number == 0LL) {
        return 1LL;
    }
    long long n = number;
    long long total = 0LL;
    while (n > 0LL) {
        long long digit = n % 10LL;
        total = total + DIGIT_FACTORIAL[(int)({long long _mochi_idx = digit; _mochi_idx < 0 ? DIGIT_FACTORIAL_len + _mochi_idx : _mochi_idx;})];
        n = n / 10LL;
    }
    return total;
}

long long chain_len(long long n, long long limit) {
    MapII seen = { NULL, NULL, 0, 0 };
    long long length = 0LL;
    long long cur = n;
    while (((contains_map_int(seen.keys, seen.len, cur)) == 0LL) && (length <= limit)) {
        map_set_ii(&seen, cur, 1LL);
        length = length + 1LL;
        cur = digit_factorial_sum(cur);
    }
    return length;
}

long long solution(long long chain_length, long long number_limit) {
    if ((chain_length <= 0LL) || (number_limit <= 0LL)) {
        panic("Parameters chain_length and number_limit must be greater than 0");
    }
    long long count_ = 0LL;
    long long start = 1LL;
    while (start < number_limit) {
        if (chain_len(start, chain_length) == chain_length) {
            count_ = count_ + 1LL;
        }
        start = start + 1LL;
    }
    return count_;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(solution(60LL, 1000000LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
