// Generated by Mochi 0.10.32 on 2025-08-23 16:07 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        int *new_keys = malloc(m->cap * sizeof(int));
        int *new_vals = malloc(m->cap * sizeof(int));
        if (m->keys) memcpy(new_keys, m->keys, m->len * sizeof(int));
        if (m->vals) memcpy(new_vals, m->vals, m->len * sizeof(int));
        m->keys = new_keys;
        m->vals = new_vals;
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long DIGIT_FACTORIALS_init[10] = {1LL, 1LL, 2LL, 6LL, 24LL, 120LL, 720LL, 5040LL, 40320LL, 362880LL};
long long *DIGIT_FACTORIALS = DIGIT_FACTORIALS_init;
size_t DIGIT_FACTORIALS_len = 10;
static int cache_sum_digit_factorials_keys[17] = {145LL};
static int cache_sum_digit_factorials_vals[17] = {145LL};
size_t cache_sum_digit_factorials_len = 1;
MapII cache_sum_digit_factorials = { cache_sum_digit_factorials_keys, cache_sum_digit_factorials_vals, 1, 17 };
static int chain_length_cache_keys[23] = {145LL, 169LL, 36301LL, 1454LL, 871LL, 45361LL, 872LL};
static int chain_length_cache_vals[23] = {0LL, 3LL, 3LL, 3LL, 2LL, 2LL, 2LL};
size_t chain_length_cache_len = 7;
MapII chain_length_cache = { chain_length_cache_keys, chain_length_cache_vals, 7, 23 };

long long sum_digit_factorials(long long n);
long long chain_length(long long n);
long long solution(long long num_terms, long long max_start);
int main(void);

long long sum_digit_factorials(long long n) {
    if (contains_map_int(cache_sum_digit_factorials.keys, cache_sum_digit_factorials.len, n)) {
        return map_get_ii(cache_sum_digit_factorials.keys, cache_sum_digit_factorials.vals, cache_sum_digit_factorials.len, n);
    }
    long long m = n;
    long long ret = 0LL;
    if (m == 0LL) {
        ret = 1LL;
    }
    while (m > 0LL) {
        long long digit = m % 10LL;
        ret = ret + DIGIT_FACTORIALS[(int)({long long _mochi_idx = digit; _mochi_idx < 0 ? DIGIT_FACTORIALS_len + _mochi_idx : _mochi_idx;})];
        m = m / 10LL;
    }
    map_set_ii(&cache_sum_digit_factorials, n, ret);
    return ret;
}

long long chain_length(long long n) {
    if (contains_map_int(chain_length_cache.keys, chain_length_cache.len, n)) {
        return map_get_ii(chain_length_cache.keys, chain_length_cache.vals, chain_length_cache.len, n);
    }
    long long *chain = NULL;
    size_t chain_len = 0;
    MapII seen = { NULL, NULL, 0, 0 };
    long long current = n;
    while (1) {
        if (contains_map_int(chain_length_cache.keys, chain_length_cache.len, current)) {
            long long known = map_get_ii(chain_length_cache.keys, chain_length_cache.vals, chain_length_cache.len, current);
            long long total = known;
            long long i = chain_len - 1LL;
            while (i >= 0LL) {
                total = total + 1LL;
                map_set_ii(&chain_length_cache, chain[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chain_len + _mochi_idx : _mochi_idx;})], total);
                i = i - 1LL;
            }
            return map_get_ii(chain_length_cache.keys, chain_length_cache.vals, chain_length_cache.len, n);
        }
        if (contains_map_int(seen.keys, seen.len, current)) {
            long long loop_start = map_get_ii(seen.keys, seen.vals, seen.len, current);
            long long loop_len = chain_len - loop_start;
            long long i = chain_len - 1LL;
            long long ahead = 0LL;
            while (i >= 0LL) {
                if (i >= loop_start) {
                    map_set_ii(&chain_length_cache, chain[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chain_len + _mochi_idx : _mochi_idx;})], loop_len);
                } else {
                    map_set_ii(&chain_length_cache, chain[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chain_len + _mochi_idx : _mochi_idx;})], loop_len + (ahead + 1LL));
                }
                ahead = ahead + 1LL;
                i = i - 1LL;
            }
            return map_get_ii(chain_length_cache.keys, chain_length_cache.vals, chain_length_cache.len, n);
        }
        map_set_ii(&seen, current, chain_len);
        chain = list_append_long_long(chain, &chain_len, current);
        current = sum_digit_factorials(current);
    }
}

long long solution(long long num_terms, long long max_start) {
    long long count_ = 0LL;
    long long i = 1LL;
    while (i < max_start) {
        if (chain_length(i) == num_terms) {
            count_ = count_ + 1LL;
        }
        i = i + 1LL;
    }
    return count_;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("solution() = ", str_int(solution(60LL, 1000LL))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
