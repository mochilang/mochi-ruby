// Generated by Mochi 0.10.32 on 2025-08-23 15:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prime_sieve_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        int *new_keys = malloc(m->cap * sizeof(int));
        int *new_vals = malloc(m->cap * sizeof(int));
        if (m->keys) memcpy(new_keys, m->keys, m->len * sizeof(int));
        if (m->vals) memcpy(new_vals, m->vals, m->len * sizeof(int));
        m->keys = new_keys;
        m->vals = new_vals;
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long ans = 0;

long long * prime_sieve(long long limit);
long long solution(long long ceiling);
int main(void);

long long * prime_sieve(long long limit) {
    if (limit <= 2LL) {
        {
            long long* __tmp0_data = malloc(0 * sizeof(long long));
            prime_sieve_len = 0;
            return __tmp0_data;
        }
    }
    long long *is_prime = NULL;
    size_t is_prime_len = 0;
    long long i = 0LL;
    while (i < limit) {
        is_prime = list_append_long_long(is_prime, &is_prime_len, 1LL);
        i = i + 1LL;
    }
    is_prime[(int)(0LL)] = 0LL;
    is_prime[(int)(1LL)] = 0LL;
    long long p = 3LL;
    while ((p * p) < limit) {
        long long index = p * 2LL;
        while (index < limit) {
            is_prime[(int)(index)] = 0LL;
            index = index + p;
        }
        p = p + 2LL;
    }
    long long *primes = NULL;
    size_t primes_len = 0;
    primes = list_append_long_long(primes, &primes_len, 2LL);
    long long n = 3LL;
    while (n < limit) {
        if (is_prime[(int)({long long _mochi_idx = n; _mochi_idx < 0 ? is_prime_len + _mochi_idx : _mochi_idx;})]) {
            primes = list_append_long_long(primes, &primes_len, n);
        }
        n = n + 2LL;
    }
    return prime_sieve_len = primes_len, primes;
}

long long solution(long long ceiling) {
    long long *primes = prime_sieve(ceiling);
    size_t primes_len = prime_sieve_len;
    MapII prime_map = { NULL, NULL, 0, 0 };
    long long i = 0LL;
    while (i < primes_len) {
        map_set_ii(&prime_map, primes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})], 1LL);
        i = i + 1LL;
    }
    long long *prefix = NULL;
    size_t prefix_len = 0;
    prefix = list_append_long_long(prefix, &prefix_len, 0LL);
    i = 0LL;
    while (i < primes_len) {
        prefix = list_append_long_long(prefix, &prefix_len, prefix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prefix_len + _mochi_idx : _mochi_idx;})] + primes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long max_len = 0LL;
    while ((max_len < prefix_len) && (prefix[(int)({long long _mochi_idx = max_len; _mochi_idx < 0 ? prefix_len + _mochi_idx : _mochi_idx;})] < ceiling)) {
        max_len = max_len + 1LL;
    }
    long long L = max_len;
    while (L > 0LL) {
        long long start = 0LL;
        while ((start + L) <= primes_len) {
            long long s = prefix[(int)({long long _mochi_idx = start + L; _mochi_idx < 0 ? prefix_len + _mochi_idx : _mochi_idx;})] - prefix[(int)({long long _mochi_idx = start; _mochi_idx < 0 ? prefix_len + _mochi_idx : _mochi_idx;})];
            if (s >= ceiling) {
                break;
            }
            if (map_get_ii(prime_map.keys, prime_map.vals, prime_map.len, s)) {
                return s;
            }
            start = start + 1LL;
        }
        L = L - 1LL;
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        ans = solution(1000000LL);
        puts(str_concat("solution() = ", str_int(ans)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
