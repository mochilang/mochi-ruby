// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t straight_line_depreciation_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double * straight_line_depreciation(long long useful_years, double purchase_value, double residual_value);
int main(void);

double * straight_line_depreciation(long long useful_years, double purchase_value, double residual_value) {
    if (useful_years < 1LL) {
        panic("Useful years cannot be less than 1");
    }
    if (purchase_value < 0.0) {
        panic("Purchase value cannot be less than zero");
    }
    if (purchase_value < residual_value) {
        panic("Purchase value cannot be less than residual value");
    }
    double depreciable_cost = purchase_value - residual_value;
    double annual_expense = depreciable_cost / (1.0 * useful_years);
    double *expenses = NULL;
    size_t expenses_len = 0;
    double accumulated = 0.0;
    long long period = 0LL;
    while (period < useful_years) {
        if (period != (useful_years - 1LL)) {
            accumulated = accumulated + annual_expense;
            expenses = list_append_double(expenses, &expenses_len, annual_expense);
        } else {
            double end_year_expense = depreciable_cost - accumulated;
            expenses = list_append_double(expenses, &expenses_len, end_year_expense);
        }
        period = period + 1LL;
    }
    return straight_line_depreciation_len = expenses_len, expenses;
}

int main(void) {
    {
        long long __start = _now();
        puts(({double *__tmp5 = straight_line_depreciation(10LL, 1100.0, 100.0); char *__res = str_list_double(__tmp5, straight_line_depreciation_len); __res;}));
        puts(({double *__tmp6 = straight_line_depreciation(6LL, 1250.0, 50.0); char *__res = str_list_double(__tmp6, straight_line_depreciation_len); __res;}));
        puts(({double *__tmp7 = straight_line_depreciation(4LL, 1001.0, 0.0); char *__res = str_list_double(__tmp7, straight_line_depreciation_len); __res;}));
        puts(({double *__tmp8 = straight_line_depreciation(11LL, 380.0, 50.0); char *__res = str_list_double(__tmp8, straight_line_depreciation_len); __res;}));
        puts(({double *__tmp9 = straight_line_depreciation(1LL, 4985.0, 100.0); char *__res = str_list_double(__tmp9, straight_line_depreciation_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
