// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

void user_panic(const char* msg);
double user_powf(double base, double exp);
double simple_interest(double principal, double daily_rate, double days);
double compound_interest(double principal, double nominal_rate, double periods);
double apr_interest(double principal, double apr, double years);
void user_main();
int main(void);

void user_panic(const char* msg) {
    puts(msg);
}

double user_powf(double base, double exp) {
    double result = 1.0;
    long long i = 0LL;
    while (i < (int)(exp)) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double simple_interest(double principal, double daily_rate, double days) {
    if (days <= 0.0) {
        user_panic("days_between_payments must be > 0");
        return 0.0;
    }
    if (daily_rate < 0.0) {
        user_panic("daily_interest_rate must be >= 0");
        return 0.0;
    }
    if (principal <= 0.0) {
        user_panic("principal must be > 0");
        return 0.0;
    }
    return (principal * daily_rate) * days;
}

double compound_interest(double principal, double nominal_rate, double periods) {
    if (periods <= 0.0) {
        user_panic("number_of_compounding_periods must be > 0");
        return 0.0;
    }
    if (nominal_rate < 0.0) {
        user_panic("nominal_annual_interest_rate_percentage must be >= 0");
        return 0.0;
    }
    if (principal <= 0.0) {
        user_panic("principal must be > 0");
        return 0.0;
    }
    return principal * (user_powf(1.0 + nominal_rate, periods) - 1.0);
}

double apr_interest(double principal, double apr, double years) {
    if (years <= 0.0) {
        user_panic("number_of_years must be > 0");
        return 0.0;
    }
    if (apr < 0.0) {
        user_panic("nominal_annual_percentage_rate must be >= 0");
        return 0.0;
    }
    if (principal <= 0.0) {
        user_panic("principal must be > 0");
        return 0.0;
    }
    return compound_interest(principal, apr / 365.0, years * 365.0);
}

void user_main() {
    puts(str_float(simple_interest(18000.0, 0.06, 3.0)));
    puts(str_float(simple_interest(0.5, 0.06, 3.0)));
    puts(str_float(simple_interest(18000.0, 0.01, 10.0)));
    puts(str_float(compound_interest(10000.0, 0.05, 3.0)));
    puts(str_float(compound_interest(10000.0, 0.05, 1.0)));
    puts(str_float(apr_interest(10000.0, 0.05, 3.0)));
    puts(str_float(apr_interest(10000.0, 0.05, 1.0)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
