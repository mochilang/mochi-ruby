// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_gas_stations_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct GasStation GasStation;

struct GasStation {
    long long gas_quantity;
    long long cost;
};

static GasStation* list_append_GasStation(GasStation *arr, size_t *len, GasStation val) {
    arr = realloc(arr, (*len + 1) * sizeof(GasStation));
    arr[*len] = val;
    (*len)++;
    return arr;
}

GasStation *example1 = NULL;
size_t example1_len = 0;
GasStation *example2 = NULL;
size_t example2_len = 0;

GasStation * get_gas_stations(long long * gas_quantities, size_t gas_quantities_len, long long * costs, size_t costs_len);
long long can_complete_journey(GasStation * gas_stations, size_t gas_stations_len);
int main(void);

GasStation * get_gas_stations(long long * gas_quantities, size_t gas_quantities_len, long long * costs, size_t costs_len) {
    GasStation *stations = NULL;
    size_t stations_len = 0;
    long long i = 0LL;
    while (i < gas_quantities_len) {
        stations = list_append_GasStation(stations, &stations_len, (GasStation){.gas_quantity = gas_quantities[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gas_quantities_len + _mochi_idx : _mochi_idx;})], .cost = costs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})]});
        i = i + 1LL;
    }
    return get_gas_stations_len = stations_len, stations;
}

long long can_complete_journey(GasStation * gas_stations, size_t gas_stations_len) {
    long long total_gas = 0LL;
    long long total_cost = 0LL;
    long long i = 0LL;
    while (i < gas_stations_len) {
        total_gas = total_gas + gas_stations[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gas_stations_len + _mochi_idx : _mochi_idx;})].gas_quantity;
        total_cost = total_cost + gas_stations[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gas_stations_len + _mochi_idx : _mochi_idx;})].cost;
        i = i + 1LL;
    }
    if (total_gas < total_cost) {
        return -1LL;
    }
    long long start = 0LL;
    long long net = 0LL;
    i = 0LL;
    while (i < gas_stations_len) {
        GasStation station = gas_stations[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? gas_stations_len + _mochi_idx : _mochi_idx;})];
        net = (net + station.gas_quantity) - station.cost;
        if (net < 0LL) {
            start = i + 1LL;
            net = 0LL;
        }
        i = i + 1LL;
    }
    return start;
}

int main(void) {
    {
        long long __start = _now();
        example1 = get_gas_stations(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp;}), 5, ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 5LL; tmp[3] = 1LL; tmp[4] = 2LL; tmp;}), 5);
        example1_len = get_gas_stations_len;
        puts(str_int(can_complete_journey(example1, example1_len)));
        example2 = get_gas_stations(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = 4LL; tmp;}), 3, ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 3LL; tmp;}), 3);
        example2_len = get_gas_stations_len;
        puts(str_int(can_complete_journey(example2, example2_len)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
