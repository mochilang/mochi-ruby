// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t sort_by_ratio_len;

size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct KnapsackResult KnapsackResult;

struct KnapsackResult {
    double max_value;
    double *fractions;
    size_t fractions_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double v_init[5] = {1LL, 3LL, 5LL, 7LL, 9LL};
double *v = v_init;
size_t v_len = 5;
double w_init[5] = {0.9, 0.7, 0.5, 0.3, 0.1};
double *w = w_init;
size_t w_len = 5;

long long * sort_by_ratio(long long * index, size_t index_len, double * ratio, size_t ratio_len);
KnapsackResult fractional_knapsack(double * value, size_t value_len, double * weight, size_t weight_len, double capacity);
int main(void);

long long * sort_by_ratio(long long * index, size_t index_len, double * ratio, size_t ratio_len) {
    long long i = 1LL;
    while (i < index_len) {
        long long key = index[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? index_len + _mochi_idx : _mochi_idx;})];
        double key_ratio = ratio[(int)({long long _mochi_idx = key; _mochi_idx < 0 ? ratio_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (ratio[(int)({long long _mochi_idx = index[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? index_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? ratio_len + _mochi_idx : _mochi_idx;})] < key_ratio)) {
            index[(int)(j + 1LL)] = index[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? index_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        index[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return sort_by_ratio_len = index_len, index;
}

KnapsackResult fractional_knapsack(double * value, size_t value_len, double * weight, size_t weight_len, double capacity) {
    long long n = value_len;
    long long *index = NULL;
    size_t index_len = 0;
    long long i = 0LL;
    while (i < n) {
        index = list_append_long_long(index, &index_len, i);
        i = i + 1LL;
    }
    double *ratio = NULL;
    size_t ratio_len = 0;
    i = 0LL;
    while (i < n) {
        ratio = list_append_double(ratio, &ratio_len, value[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? value_len + _mochi_idx : _mochi_idx;})] / weight[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    index = sort_by_ratio(index, index_len, ratio, ratio_len);
    index_len = sort_by_ratio_len;
    double *fractions = NULL;
    size_t fractions_len = 0;
    i = 0LL;
    while (i < n) {
        fractions = list_append_double(fractions, &fractions_len, 0LL);
        i = i + 1LL;
    }
    double max_value = 0LL;
    long long idx = 0LL;
    while (idx < index_len) {
        long long item = index[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? index_len + _mochi_idx : _mochi_idx;})];
        if (weight[(int)({long long _mochi_idx = item; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})] <= capacity) {
            fractions[(int)(item)] = 1LL;
            max_value = max_value + value[(int)({long long _mochi_idx = item; _mochi_idx < 0 ? value_len + _mochi_idx : _mochi_idx;})];
            capacity = capacity - weight[(int)({long long _mochi_idx = item; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})];
        } else {
            fractions[(int)(item)] = capacity / weight[(int)({long long _mochi_idx = item; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})];
            max_value = max_value + ((value[(int)({long long _mochi_idx = item; _mochi_idx < 0 ? value_len + _mochi_idx : _mochi_idx;})] * capacity) / weight[(int)({long long _mochi_idx = item; _mochi_idx < 0 ? weight_len + _mochi_idx : _mochi_idx;})]);
            break;
        }
        idx = idx + 1LL;
    }
    return (KnapsackResult){.max_value = max_value, .fractions = fractions, .fractions_len = fractions_len};
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", fractional_knapsack(v, v_len, w, w_len, 5LL));
        printf("%d\n", fractional_knapsack(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 3LL; tmp[2] = 5LL; tmp[3] = 7LL; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 0.9; tmp[1] = 0.7; tmp[2] = 0.5; tmp[3] = 0.3; tmp;}), 4, 30LL));
        printf("%d\n", fractional_knapsack(NULL, 0, NULL, 0, 30LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
