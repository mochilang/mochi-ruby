// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t sort_by_ratio_desc_len;

size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Item Item;

struct Item {
    double value;
    double weight;
};

static Item* list_append_Item(Item *arr, size_t *len, Item val) {
    arr = realloc(arr, (*len + 1) * sizeof(Item));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double vl_init[3] = {60LL, 100LL, 120LL};
double *vl = vl_init;
size_t vl_len = 3;
double wt_init[3] = {10LL, 20LL, 30LL};
double *wt = wt_init;
size_t wt_len = 3;
double result = 0;

Item * sort_by_ratio_desc(Item * arr, size_t arr_len);
double sum_first(double * arr, size_t arr_len, long long k);
double frac_knapsack(double * vl, size_t vl_len, double * wt, size_t wt_len, double w, long long n);
int main(void);

Item * sort_by_ratio_desc(Item * arr, size_t arr_len) {
    long long i = 1LL;
    while (i < arr_len) {
        Item key = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while (j >= 0LL) {
            Item current = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            if ((current.value / current.weight) < (key.value / key.weight)) {
                arr[(int)(j + 1LL)] = current;
                j = j - 1LL;
            } else {
                break;
            }
        }
        arr[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return sort_by_ratio_desc_len = arr_len, arr;
}

double sum_first(double * arr, size_t arr_len, long long k) {
    double s = 0LL;
    long long i = 0LL;
    while ((i < k) && (i < arr_len)) {
        s = s + arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return s;
}

double frac_knapsack(double * vl, size_t vl_len, double * wt, size_t wt_len, double w, long long n) {
    Item *items = NULL;
    size_t items_len = 0;
    long long i = 0LL;
    while ((i < vl_len) && (i < wt_len)) {
        items = list_append_Item(items, &items_len, (Item){.value = vl[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vl_len + _mochi_idx : _mochi_idx;})], .weight = wt[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? wt_len + _mochi_idx : _mochi_idx;})]});
        i = i + 1LL;
    }
    items = sort_by_ratio_desc(items, items_len);
    items_len = sort_by_ratio_desc_len;
    double *values = NULL;
    size_t values_len = 0;
    double *weights = NULL;
    size_t weights_len = 0;
    i = 0LL;
    while (i < items_len) {
        Item itm = items[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? items_len + _mochi_idx : _mochi_idx;})];
        values = list_append_double(values, &values_len, itm.value);
        weights = list_append_double(weights, &weights_len, itm.weight);
        i = i + 1LL;
    }
    double *acc = NULL;
    size_t acc_len = 0;
    double total = 0LL;
    i = 0LL;
    while (i < weights_len) {
        total = total + weights[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})];
        acc = list_append_double(acc, &acc_len, total);
        i = i + 1LL;
    }
    long long k = 0LL;
    while ((k < acc_len) && (w >= acc[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? acc_len + _mochi_idx : _mochi_idx;})])) {
        k = k + 1LL;
    }
    if (k == 0LL) {
        return 0LL;
    }
    if (k >= values_len) {
        return sum_first(values, values_len, values_len);
    }
    if (k != n) {
        return sum_first(values, values_len, k) + (((w - acc[(int)({long long _mochi_idx = k - 1LL; _mochi_idx < 0 ? acc_len + _mochi_idx : _mochi_idx;})]) * values[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]) / weights[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})]);
    }
    return sum_first(values, values_len, k);
}

int main(void) {
    {
        long long __start = _now();
        result = frac_knapsack(vl, vl_len, wt, wt_len, 50LL, 3LL);
        puts(str_float(result));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
