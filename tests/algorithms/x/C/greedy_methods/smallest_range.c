// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t smallest_range_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct HeapItem HeapItem;

struct HeapItem {
    long long value;
    long long list_idx;
    long long elem_idx;
};

static HeapItem* list_append_HeapItem(HeapItem *arr, size_t *len, HeapItem val) {
    arr = realloc(arr, (*len + 1) * sizeof(HeapItem));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long INF = 1000000000LL;

long long * smallest_range(long long * * nums, size_t nums_len, size_t* nums_lens, size_t nums_lens_len);
const char* list_to_string(long long * arr, size_t arr_len);
void user_main();
int main(void);

long long * smallest_range(long long * * nums, size_t nums_len, size_t* nums_lens, size_t nums_lens_len) {
    HeapItem *heap = NULL;
    size_t heap_len = 0;
    long long current_max = -(INF);
    long long i = 0LL;
    while (i < nums_len) {
        long long first_val = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_lens[i] + _mochi_idx : _mochi_idx;})];
        heap = list_append_HeapItem(heap, &heap_len, (HeapItem){.value = first_val, .list_idx = i, .elem_idx = 0LL});
        if (first_val > current_max) {
            current_max = first_val;
        }
        i = i + 1LL;
    }
    long long *best = NULL;
    size_t best_len = 0;
    best = list_append_long_long(best, &best_len, -(INF));
    best = list_append_long_long(best, &best_len, INF);
    while (heap_len > 0LL) {
        long long min_idx = 0LL;
        long long j = 1LL;
        while (j < heap_len) {
            HeapItem hj = heap[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
            HeapItem hmin = heap[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
            if (hj.value < hmin.value) {
                min_idx = j;
            }
            j = j + 1LL;
        }
        HeapItem item = heap[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})];
        HeapItem *new_heap = NULL;
        size_t new_heap_len = 0;
        long long k = 0LL;
        while (k < heap_len) {
            if (k != min_idx) {
                new_heap = list_append_HeapItem(new_heap, &new_heap_len, heap[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? heap_len + _mochi_idx : _mochi_idx;})]);
            }
            k = k + 1LL;
        }
        heap = new_heap;
        heap_len = new_heap_len;
        long long current_min = item.value;
        if ((current_max - current_min) < (best[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? best_len + _mochi_idx : _mochi_idx;})] - best[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? best_len + _mochi_idx : _mochi_idx;})])) {
            best = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = current_min; tmp[1] = current_max; tmp;});
            best_len = 2;
        }
        if (item.elem_idx == (nums_lens[item.list_idx] - 1LL)) {
            break;
        }
        long long next_val = nums[(int)({long long _mochi_idx = item.list_idx; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = item.elem_idx + 1LL; _mochi_idx < 0 ? nums_lens[item.list_idx] + _mochi_idx : _mochi_idx;})];
        heap = list_append_HeapItem(heap, &heap_len, (HeapItem){.value = next_val, .list_idx = item.list_idx, .elem_idx = item.elem_idx + 1LL});
        if (next_val > current_max) {
            current_max = next_val;
        }
    }
    return smallest_range_len = best_len, best;
}

const char* list_to_string(long long * arr, size_t arr_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < arr_len) {
        s = str_concat(s, str_int(arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]));
        if (i < (arr_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    return str_concat(s, "]");
}

void user_main() {
    long long *result1 = smallest_range(({long long** tmp = malloc(3 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 10LL; tmp[2] = 15LL; tmp[3] = 24LL; tmp[4] = 26LL; tmp;}); tmp[1] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 9LL; tmp[2] = 12LL; tmp[3] = 20LL; tmp;}); tmp[2] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 5LL; tmp[1] = 18LL; tmp[2] = 22LL; tmp[3] = 30LL; tmp;}); tmp;}), 3, ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 5; tmp[1] = 4; tmp[2] = 4; tmp;}), 3);
    size_t result1_len = smallest_range_len;
    puts(list_to_string(result1, result1_len));
    long long *result2 = smallest_range(({long long** tmp = malloc(3 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}); tmp[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}); tmp[2] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}); tmp;}), 3, ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;}), 3);
    size_t result2_len = smallest_range_len;
    puts(list_to_string(result2, result2_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
