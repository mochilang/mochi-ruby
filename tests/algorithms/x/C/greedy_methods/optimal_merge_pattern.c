// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_at_len;

size_t append_len;
static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long index_of_min(long long * xs, size_t xs_len);
long long * remove_at(long long * xs, size_t xs_len, long long idx);
long long optimal_merge_pattern(long long * files, size_t files_len);
int main(void);

long long index_of_min(long long * xs, size_t xs_len) {
    long long min_idx = 0LL;
    long long i = 1LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] < xs[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]) {
            min_idx = i;
        }
        i = i + 1LL;
    }
    return min_idx;
}

long long * remove_at(long long * xs, size_t xs_len, long long idx) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        if (i != idx) {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_at_len = res_len, res;
}

long long optimal_merge_pattern(long long * files, size_t files_len) {
    long long *arr = files;
    size_t arr_len = files_len;
    long long optimal_merge_cost = 0LL;
    while (arr_len > 1LL) {
        long long temp = 0LL;
        long long k = 0LL;
        while (k < 2LL) {
            long long min_idx = index_of_min(arr, arr_len);
            temp = temp + arr[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr = remove_at(arr, arr_len, min_idx);
            arr_len = remove_at_len;
            k = k + 1LL;
        }
        arr = list_append_int_new(arr, arr_len, temp);
        arr_len++;
        optimal_merge_cost = optimal_merge_cost + temp;
    }
    return optimal_merge_cost;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", optimal_merge_pattern(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = 4LL; tmp;}), 3));
        printf("%d\n", optimal_merge_pattern(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 5LL; tmp[1] = 10LL; tmp[2] = 20LL; tmp[3] = 30LL; tmp[4] = 30LL; tmp;}), 5));
        printf("%d\n", optimal_merge_pattern(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 8LL; tmp[1] = 8LL; tmp[2] = 8LL; tmp[3] = 8LL; tmp[4] = 8LL; tmp;}), 5));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
