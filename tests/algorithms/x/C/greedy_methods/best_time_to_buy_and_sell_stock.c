// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long max_profit(long long * prices, size_t prices_len);
int main(void);

long long max_profit(long long * prices, size_t prices_len) {
    if (prices_len == 0LL) {
        return 0LL;
    }
    long long min_price = prices[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? prices_len + _mochi_idx : _mochi_idx;})];
    long long max_profit = 0LL;
    long long i = 0LL;
    while (i < prices_len) {
        long long price = prices[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prices_len + _mochi_idx : _mochi_idx;})];
        if (price < min_price) {
            min_price = price;
        }
        long long profit = price - min_price;
        if (profit > max_profit) {
            max_profit = profit;
        }
        i = i + 1LL;
    }
    return max_profit;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", max_profit(({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 7LL; tmp[1] = 1LL; tmp[2] = 5LL; tmp[3] = 3LL; tmp[4] = 6LL; tmp[5] = 4LL; tmp;}), 6));
        printf("%d\n", max_profit(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 7LL; tmp[1] = 6LL; tmp[2] = 4LL; tmp[3] = 3LL; tmp[4] = 1LL; tmp;}), 5));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
