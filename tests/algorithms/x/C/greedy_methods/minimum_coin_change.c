// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t find_minimum_change_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * find_minimum_change(long long * denominations, size_t denominations_len, long long value);
int main(void);

long long * find_minimum_change(long long * denominations, size_t denominations_len, long long value) {
    if (value <= 0LL) {
        {
            long long* __tmp2_data = malloc(0 * sizeof(long long));
            find_minimum_change_len = 0;
            return __tmp2_data;
        }
    }
    long long total = value;
    long long *answer = NULL;
    size_t answer_len = 0;
    long long i = denominations_len - 1LL;
    while (i >= 0LL) {
        long long denom = denominations[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? denominations_len + _mochi_idx : _mochi_idx;})];
        while (total >= denom) {
            total = total - denom;
            answer = list_append_long_long(answer, &answer_len, denom);
        }
        i = i - 1LL;
    }
    return find_minimum_change_len = answer_len, answer;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = find_minimum_change(({long long *tmp = malloc(9 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 5LL; tmp[3] = 10LL; tmp[4] = 20LL; tmp[5] = 50LL; tmp[6] = 100LL; tmp[7] = 500LL; tmp[8] = 2000LL; tmp;}), 9, 987LL); char *__res = str_list_int(__tmp3, find_minimum_change_len); __res;}));
        puts(({long long *__tmp4 = find_minimum_change(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 5LL; tmp[2] = 100LL; tmp[3] = 500LL; tmp[4] = 1000LL; tmp;}), 5, 456LL); char *__res = str_list_int(__tmp4, find_minimum_change_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
