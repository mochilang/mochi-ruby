// Generated by Mochi 0.10.32 on 2025-08-25 22:56 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t mincut_len;
size_t *mincut_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **test_graph = NULL;
size_t test_graph_len = 0;
size_t *test_graph_lens = NULL;
size_t test_graph_lens_len = 0;
long long **result = NULL;
size_t result_len = 0;
size_t *result_lens = NULL;
size_t result_lens_len = 0;

long long bfs(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long s, long long t, long long * parent, size_t parent_len);
long long * * mincut(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long source, long long sink);
int main(void);

long long bfs(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long s, long long t, long long * parent, size_t parent_len) {
    long long *visited = NULL;
    size_t visited_len = 0;
    long long i = 0LL;
    while (i < graph_len) {
        visited = list_append_long_long(visited, &visited_len, 0LL);
        i = i + 1LL;
    }
    long long *queue = NULL;
    size_t queue_len = 0;
    queue = list_append_long_long(queue, &queue_len, s);
    long long head = 0LL;
    visited[(int)(s)] = 1LL;
    while (head < queue_len) {
        long long u = queue[(int)({long long _mochi_idx = head; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        head = head + 1LL;
        long long ind = 0LL;
        while (ind < graph_lens[(int)(u)]) {
            if ((visited[(int)({long long _mochi_idx = ind; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})] == 0LL) && (graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = ind; _mochi_idx < 0 ? graph_lens[(int)(u)] + _mochi_idx : _mochi_idx;})] > 0LL)) {
                queue = list_append_long_long(queue, &queue_len, ind);
                visited[(int)(ind)] = 1LL;
                parent[(int)(ind)] = u;
            }
            ind = ind + 1LL;
        }
    }
    return visited[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})];
}

long long * * mincut(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long source, long long sink) {
    long long **g = graph;
    size_t g_len = graph_len;
    size_t *g_lens = graph_lens;
    size_t g_lens_len = graph_len;
    long long *parent = NULL;
    size_t parent_len = 0;
    long long i = 0LL;
    while (i < g_len) {
        parent = list_append_long_long(parent, &parent_len, -1LL);
        i = i + 1LL;
    }
    long long **temp = NULL;
    size_t temp_len = 0;
    size_t *temp_lens = NULL;
    size_t temp_lens_len = 0;
    i = 0LL;
    while (i < g_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < g_lens[(int)(i)]) {
            row = list_append_long_long(row, &row_len, g[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? g_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? g_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        temp = list_append_intptr(temp, &temp_len, row);
        temp_lens = list_append_szt(temp_lens, &temp_lens_len, row_len);
        i = i + 1LL;
    }
    while (bfs(g, g_len, g_lens, g_len, source, sink, parent, parent_len)) {
        long long path_flow = 1000000000LL;
        long long s = sink;
        while (s != source) {
            long long p = parent[(int)({long long _mochi_idx = s; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})];
            long long cap = g[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? g_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = s; _mochi_idx < 0 ? g_lens[(int)(p)] + _mochi_idx : _mochi_idx;})];
            if (cap < path_flow) {
                path_flow = cap;
            }
            s = p;
        }
        long long v = sink;
        while (v != source) {
            long long u = parent[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})];
            g[(int)(u)][(int)(v)] = g[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? g_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = v; _mochi_idx < 0 ? g_lens[(int)(u)] + _mochi_idx : _mochi_idx;})] - path_flow;
            g[(int)(v)][(int)(u)] = g[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? g_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = u; _mochi_idx < 0 ? g_lens[(int)(v)] + _mochi_idx : _mochi_idx;})] + path_flow;
            v = u;
        }
    }
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    i = 0LL;
    while (i < g_len) {
        long long j = 0LL;
        while (j < g_lens[(int)(0LL)]) {
            if ((g[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? g_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? g_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] == 0LL) && (temp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? temp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? temp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] > 0LL)) {
                res = list_append_intptr(res, &res_len, ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = i; tmp[1] = j; tmp;}));
                res_lens = list_append_szt(res_lens, &res_lens_len, 2);
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return mincut_lens = res_lens, mincut_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        test_graph = malloc(6 * sizeof(long long*));
        test_graph_len = 6;
        test_graph[0] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 16LL; tmp[2] = 13LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp[5] = 0LL; tmp;});
        test_graph[1] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 10LL; tmp[3] = 12LL; tmp[4] = 0LL; tmp[5] = 0LL; tmp;});
        test_graph[2] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 4LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 14LL; tmp[5] = 0LL; tmp;});
        test_graph[3] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 9LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp[5] = 20LL; tmp;});
        test_graph[4] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp[3] = 7LL; tmp[4] = 0LL; tmp[5] = 4LL; tmp;});
        test_graph[5] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp[5] = 0LL; tmp;});
        test_graph_lens = ({size_t *tmp = malloc(6 * sizeof(size_t)); tmp[0] = 6; tmp[1] = 6; tmp[2] = 6; tmp[3] = 6; tmp[4] = 6; tmp[5] = 6; tmp;});
        test_graph_lens_len = 6;
        result = mincut(test_graph, test_graph_len, test_graph_lens, test_graph_len, 0LL, 5LL);
        result_len = mincut_len;
        result_lens = mincut_lens;
        puts(str_list_list_int(result, result_len, result_lens));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
