// Generated by Mochi 0.10.32 on 2025-08-25 22:56 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long INF = 1000000000LL;
long long **graph = NULL;
size_t graph_len = 0;
size_t *graph_lens = NULL;
size_t graph_lens_len = 0;

long long breadth_first_search(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long source, long long sink, long long * parent, size_t parent_len);
long long ford_fulkerson(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long source, long long sink);
int main(void);

long long breadth_first_search(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long source, long long sink, long long * parent, size_t parent_len) {
    long long *visited = NULL;
    size_t visited_len = 0;
    long long i = 0LL;
    while (i < graph_len) {
        visited = list_append_long_long(visited, &visited_len, 0LL);
        i = i + 1LL;
    }
    long long *queue = NULL;
    size_t queue_len = 0;
    queue = list_append_long_long(queue, &queue_len, source);
    visited[(int)(source)] = 1LL;
    long long head = 0LL;
    while (head < queue_len) {
        long long u = queue[(int)({long long _mochi_idx = head; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        head = head + 1LL;
        long long *row = graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = graph_lens[(int)(u)];
        long long ind = 0LL;
        while (ind < row_len) {
            long long capacity = row[(int)({long long _mochi_idx = ind; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})];
            if ((visited[(int)({long long _mochi_idx = ind; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})] == 0LL) && (capacity > 0LL)) {
                queue = list_append_long_long(queue, &queue_len, ind);
                visited[(int)(ind)] = 1LL;
                parent[(int)(ind)] = u;
            }
            ind = ind + 1LL;
        }
    }
    return visited[(int)({long long _mochi_idx = sink; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})];
}

long long ford_fulkerson(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long source, long long sink) {
    long long *parent = NULL;
    size_t parent_len = 0;
    long long i = 0LL;
    while (i < graph_len) {
        parent = list_append_long_long(parent, &parent_len, -1LL);
        i = i + 1LL;
    }
    long long max_flow = 0LL;
    while (breadth_first_search(graph, graph_len, graph_lens, graph_len, source, sink, parent, parent_len)) {
        long long path_flow = INF;
        long long s = sink;
        while (s != source) {
            long long prev = parent[(int)({long long _mochi_idx = s; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})];
            long long cap = graph[(int)({long long _mochi_idx = prev; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = s; _mochi_idx < 0 ? graph_lens[(int)(prev)] + _mochi_idx : _mochi_idx;})];
            if (cap < path_flow) {
                path_flow = cap;
            }
            s = prev;
        }
        max_flow = max_flow + path_flow;
        long long v = sink;
        while (v != source) {
            long long u = parent[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})];
            graph[(int)(u)][(int)(v)] = graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = v; _mochi_idx < 0 ? graph_lens[(int)(u)] + _mochi_idx : _mochi_idx;})] - path_flow;
            graph[(int)(v)][(int)(u)] = graph[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_lens[(int)(v)] + _mochi_idx : _mochi_idx;})] + path_flow;
            v = u;
        }
        long long j = 0LL;
        while (j < parent_len) {
            parent[(int)(j)] = -1LL;
            j = j + 1LL;
        }
    }
    return max_flow;
}

int main(void) {
    {
        long long __start = _now();
        graph = malloc(6 * sizeof(long long*));
        graph_len = 6;
        graph[0] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 16LL; tmp[2] = 13LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp[5] = 0LL; tmp;});
        graph[1] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 10LL; tmp[3] = 12LL; tmp[4] = 0LL; tmp[5] = 0LL; tmp;});
        graph[2] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 4LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 14LL; tmp[5] = 0LL; tmp;});
        graph[3] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 9LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp[5] = 20LL; tmp;});
        graph[4] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp[3] = 7LL; tmp[4] = 0LL; tmp[5] = 4LL; tmp;});
        graph[5] = ({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp[5] = 0LL; tmp;});
        graph_lens = ({size_t *tmp = malloc(6 * sizeof(size_t)); tmp[0] = 6; tmp[1] = 6; tmp[2] = 6; tmp[3] = 6; tmp[4] = 6; tmp[5] = 6; tmp;});
        graph_lens_len = 6;
        puts(str_int(ford_fulkerson(graph, graph_len, graph_lens, graph_len, 0LL, 5LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
