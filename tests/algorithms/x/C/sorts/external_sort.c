// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t subarray_len;
size_t merge_len;
size_t merge_sort_len;
size_t split_into_blocks_len;
size_t *split_into_blocks_lens;
size_t merge_blocks_len;
size_t external_sort_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * subarray(long long * xs, size_t xs_len, long long start, long long end);
long long * merge(long long * left_half, size_t left_half_len, long long * right_half, size_t right_half_len);
long long * merge_sort(long long * array, size_t array_len);
long long * * split_into_blocks(long long * data, size_t data_len, long long block_size);
long long * merge_blocks(long long * * blocks, size_t blocks_len, size_t* blocks_lens, size_t blocks_lens_len);
long long * external_sort(long long * data, size_t data_len, long long block_size);
void user_main();
int main(void);

long long * subarray(long long * xs, size_t xs_len, long long start, long long end) {
    long long *result = NULL;
    size_t result_len = 0;
    long long k = start;
    while (k < end) {
        result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        k = k + 1LL;
    }
    return subarray_len = result_len, result;
}

long long * merge(long long * left_half, size_t left_half_len, long long * right_half, size_t right_half_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    long long j = 0LL;
    while ((i < left_half_len) && (j < right_half_len)) {
        if (left_half[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_half_len + _mochi_idx : _mochi_idx;})] < right_half[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_half_len + _mochi_idx : _mochi_idx;})]) {
            result = list_append_long_long(result, &result_len, left_half[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_half_len + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        } else {
            result = list_append_long_long(result, &result_len, right_half[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_half_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
    }
    while (i < left_half_len) {
        result = list_append_long_long(result, &result_len, left_half[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_half_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    while (j < right_half_len) {
        result = list_append_long_long(result, &result_len, right_half[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_half_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return merge_len = result_len, result;
}

long long * merge_sort(long long * array, size_t array_len) {
    if (array_len <= 1LL) {
        return merge_sort_len = array_len, array;
    }
    long long middle = array_len / 2LL;
    long long *left_half = subarray(array, array_len, 0LL, middle);
    size_t left_half_len = subarray_len;
    long long *right_half = subarray(array, array_len, middle, array_len);
    size_t right_half_len = subarray_len;
    long long *sorted_left = merge_sort(left_half, left_half_len);
    size_t sorted_left_len = merge_sort_len;
    long long *sorted_right = merge_sort(right_half, right_half_len);
    size_t sorted_right_len = merge_sort_len;
    {
        long long* __ret0 = merge(sorted_left, sorted_left_len, sorted_right, sorted_right_len);
        merge_sort_len = merge_len;
        return __ret0;
    }
}

long long * * split_into_blocks(long long * data, size_t data_len, long long block_size) {
    long long **blocks = NULL;
    size_t blocks_len = 0;
    size_t *blocks_lens = NULL;
    size_t blocks_lens_len = 0;
    long long i = 0LL;
    while (i < data_len) {
        long long end;
        if ((i + block_size) < data_len) {
            end = i + block_size;
        } else {
            end = data_len;
        }
        long long *block = subarray(data, data_len, i, end);
        size_t block_len = subarray_len;
        long long *sorted_block = merge_sort(block, block_len);
        size_t sorted_block_len = merge_sort_len;
        blocks = list_append_intptr(blocks, &blocks_len, sorted_block);
        blocks_lens = list_append_szt(blocks_lens, &blocks_lens_len, sorted_block_len);
        i = end;
    }
    return split_into_blocks_lens = blocks_lens, split_into_blocks_len = blocks_len, blocks;
}

long long * merge_blocks(long long * * blocks, size_t blocks_len, size_t* blocks_lens, size_t blocks_lens_len) {
    long long num_blocks = blocks_len;
    long long *indices = NULL;
    size_t indices_len = 0;
    long long i = 0LL;
    while (i < num_blocks) {
        indices = list_append_long_long(indices, &indices_len, 0LL);
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    long long done = 0LL;
    while (!(done)) {
        done = 1LL;
        long long min_val = 0LL;
        long long min_block = -1LL;
        long long j = 0LL;
        while (j < num_blocks) {
            long long idx = indices[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? indices_len + _mochi_idx : _mochi_idx;})];
            if (idx < blocks_lens[(int)(j)]) {
                long long val = blocks[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? blocks_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? blocks_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
                if ((min_block == -1LL) || (val < min_val)) {
                    min_val = val;
                    min_block = j;
                }
                done = 0LL;
            }
            j = j + 1LL;
        }
        if (!(done)) {
            result = list_append_long_long(result, &result_len, min_val);
            indices[(int)(min_block)] = indices[(int)({long long _mochi_idx = min_block; _mochi_idx < 0 ? indices_len + _mochi_idx : _mochi_idx;})] + 1LL;
        }
    }
    return merge_blocks_len = result_len, result;
}

long long * external_sort(long long * data, size_t data_len, long long block_size) {
    long long **blocks = split_into_blocks(data, data_len, block_size);
    size_t blocks_len = split_into_blocks_len;
    size_t *blocks_lens = split_into_blocks_lens;
    size_t blocks_lens_len = split_into_blocks_len;
    {
        long long* __ret1 = merge_blocks(blocks, blocks_len, blocks_lens, blocks_len);
        external_sort_len = merge_blocks_len;
        return __ret1;
    }
}

void user_main() {
    long long *data = NULL;
    size_t data_len = 0;
    data = list_append_long_long(data, &data_len, 7LL);
    data = list_append_long_long(data, &data_len, 1LL);
    data = list_append_long_long(data, &data_len, 5LL);
    data = list_append_long_long(data, &data_len, 3LL);
    data = list_append_long_long(data, &data_len, 9LL);
    data = list_append_long_long(data, &data_len, 2LL);
    data = list_append_long_long(data, &data_len, 6LL);
    data = list_append_long_long(data, &data_len, 4LL);
    data = list_append_long_long(data, &data_len, 8LL);
    data = list_append_long_long(data, &data_len, 0LL);
    long long *sorted_data = external_sort(data, data_len, 3LL);
    size_t sorted_data_len = external_sort_len;
    puts(str_list_int(sorted_data, sorted_data_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
