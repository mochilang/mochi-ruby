// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_buckets_len;
size_t *make_buckets_lens;
size_t radix_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long RADIX = 10LL;

long long * * make_buckets();
long long max_value(long long * xs, size_t xs_len);
long long * radix_sort(long long * list_of_ints, size_t list_of_ints_len);
int main(void);

long long * * make_buckets() {
    long long **buckets = NULL;
    size_t buckets_len = 0;
    size_t *buckets_lens = NULL;
    size_t buckets_lens_len = 0;
    long long i = 0LL;
    while (i < RADIX) {
        buckets = list_append_intptr(buckets, &buckets_len, ({long long* tmp = malloc(0 * sizeof(long long)); tmp;}));
        buckets_lens = list_append_szt(buckets_lens, &buckets_lens_len, 0);
        i = i + 1LL;
    }
    return make_buckets_lens = buckets_lens, make_buckets_len = buckets_len, buckets;
}

long long max_value(long long * xs, size_t xs_len) {
    long long max_val = xs[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] > max_val) {
            max_val = xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return max_val;
}

long long * radix_sort(long long * list_of_ints, size_t list_of_ints_len) {
    long long placement = 1LL;
    long long max_digit = max_value(list_of_ints, list_of_ints_len);
    while (placement <= max_digit) {
        long long **buckets = make_buckets();
        size_t buckets_len = make_buckets_len;
        size_t *buckets_lens = make_buckets_lens;
        size_t buckets_lens_len = make_buckets_len;
        long long i = 0LL;
        while (i < list_of_ints_len) {
            long long value = list_of_ints[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list_of_ints_len + _mochi_idx : _mochi_idx;})];
            long long tmp = (value / placement) % RADIX;
            buckets[(int)(tmp)] = list_append_int_new(buckets[(int)({long long _mochi_idx = tmp; _mochi_idx < 0 ? buckets_len + _mochi_idx : _mochi_idx;})], buckets_lens[(int)(tmp)], value);
            i = i + 1LL;
        }
        long long a = 0LL;
        long long b = 0LL;
        while (b < RADIX) {
            long long *bucket = buckets[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? buckets_len + _mochi_idx : _mochi_idx;})];
            size_t bucket_len = buckets_lens[(int)(b)];
            long long j = 0LL;
            while (j < bucket_len) {
                list_of_ints[(int)(a)] = bucket[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? bucket_len + _mochi_idx : _mochi_idx;})];
                a = a + 1LL;
                j = j + 1LL;
            }
            b = b + 1LL;
        }
        placement = placement * RADIX;
    }
    return radix_sort_len = list_of_ints_len, list_of_ints;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = radix_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5); char *__res = str_list_int(__tmp3, radix_sort_len); __res;}));
        puts(({long long *__tmp4 = radix_sort((long long[]){1LL, 100LL, 10LL, 1000LL}, 4); char *__res = str_list_int(__tmp4, radix_sort_len); __res;}));
        puts(({long long *__tmp5 = radix_sort((long long[]){15LL, 14LL, 13LL, 12LL, 11LL, 10LL, 9LL, 8LL, 7LL, 6LL, 5LL, 4LL, 3LL, 2LL, 1LL, 0LL}, 16); char *__res = str_list_int(__tmp5, radix_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
