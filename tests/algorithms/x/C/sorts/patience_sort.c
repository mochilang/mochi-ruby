// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t reverse_list_len;
size_t patience_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long bisect_left(long long * * stacks, size_t stacks_len, size_t* stacks_lens, size_t stacks_lens_len, long long value);
long long * reverse_list(long long * src, size_t src_len);
long long * patience_sort(long long * collection, size_t collection_len);
int main(void);

long long bisect_left(long long * * stacks, size_t stacks_len, size_t* stacks_lens, size_t stacks_lens_len, long long value) {
    long long low = 0LL;
    long long high = stacks_len;
    while (low < high) {
        long long mid = (low + high) / 2LL;
        long long *stack = stacks[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? stacks_len + _mochi_idx : _mochi_idx;})];
        size_t stack_len = stacks_lens[(int)(mid)];
        long long top_idx = stack_len - 1LL;
        long long top = stack[(int)({long long _mochi_idx = top_idx; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
        if (top < value) {
            low = mid + 1LL;
        } else {
            high = mid;
        }
    }
    return low;
}

long long * reverse_list(long long * src, size_t src_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = src_len - 1LL;
    while (i >= 0LL) {
        res = list_append_long_long(res, &res_len, src[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? src_len + _mochi_idx : _mochi_idx;})]);
        i = i - 1LL;
    }
    return reverse_list_len = res_len, res;
}

long long * patience_sort(long long * collection, size_t collection_len) {
    long long **stacks = NULL;
    size_t stacks_len = 0;
    size_t *stacks_lens = NULL;
    size_t stacks_lens_len = 0;
    long long i = 0LL;
    while (i < collection_len) {
        long long element = collection[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
        long long idx = bisect_left(stacks, stacks_len, stacks_lens, stacks_len, element);
        if (idx != stacks_len) {
            long long *stack = stacks[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? stacks_len + _mochi_idx : _mochi_idx;})];
            size_t stack_len = stacks_lens[(int)(idx)];
            stacks[(int)(idx)] = list_append_int_new(stack, stack_len, element);
        } else {
            long long *new_stack = NULL;
            size_t new_stack_len = 0;
            new_stack = list_append_long_long(new_stack, &new_stack_len, element);
            stacks = list_append_intptr(stacks, &stacks_len, new_stack);
            stacks_lens = list_append_szt(stacks_lens, &stacks_lens_len, new_stack_len);
        }
        i = i + 1LL;
    }
    i = 0LL;
    while (i < stacks_len) {
        stacks[(int)(i)] = reverse_list(stacks[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? stacks_len + _mochi_idx : _mochi_idx;})], stacks_lens[(int)(i)]);
        i = i + 1LL;
    }
    long long *indices = NULL;
    size_t indices_len = 0;
    i = 0LL;
    while (i < stacks_len) {
        indices = list_append_long_long(indices, &indices_len, 0LL);
        i = i + 1LL;
    }
    long long total = 0LL;
    i = 0LL;
    while (i < stacks_len) {
        total = total + stacks_lens[(int)(i)];
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    long long count_ = 0LL;
    while (count_ < total) {
        long long min_val = 0LL;
        long long min_stack = -1LL;
        long long j = 0LL;
        while (j < stacks_len) {
            long long idx = indices[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? indices_len + _mochi_idx : _mochi_idx;})];
            if (idx < stacks_lens[(int)(j)]) {
                long long val = stacks[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? stacks_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? stacks_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
                if (min_stack < 0LL) {
                    min_val = val;
                    min_stack = j;
                } else {
                    if (val < min_val) {
                        min_val = val;
                        min_stack = j;
                    }
                }
            }
            j = j + 1LL;
        }
        result = list_append_long_long(result, &result_len, min_val);
        indices[(int)(min_stack)] = indices[(int)({long long _mochi_idx = min_stack; _mochi_idx < 0 ? indices_len + _mochi_idx : _mochi_idx;})] + 1LL;
        count_ = count_ + 1LL;
    }
    i = 0LL;
    while (i < result_len) {
        collection[(int)(i)] = result[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? result_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return patience_sort_len = collection_len, collection;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = patience_sort((long long[]){1LL, 9LL, 5LL, 21LL, 17LL, 6LL}, 6); char *__res = str_list_int(__tmp3, patience_sort_len); __res;}));
        puts(({long long *__tmp4 = patience_sort(NULL, 0); char *__res = str_list_int(__tmp4, patience_sort_len); __res;}));
        puts(({long long *__tmp5 = patience_sort((long long[]){-3LL, -17LL, -48LL}, 3); char *__res = str_list_int(__tmp5, patience_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
