// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t shell_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * shell_sort(long long * collection, size_t collection_len);
int main(void);

long long * shell_sort(long long * collection, size_t collection_len) {
    long long *arr = collection;
    size_t arr_len = collection_len;
    long long *gaps = NULL;
    size_t gaps_len = 0;
    gaps = list_append_long_long(gaps, &gaps_len, 701LL);
    gaps = list_append_long_long(gaps, &gaps_len, 301LL);
    gaps = list_append_long_long(gaps, &gaps_len, 132LL);
    gaps = list_append_long_long(gaps, &gaps_len, 57LL);
    gaps = list_append_long_long(gaps, &gaps_len, 23LL);
    gaps = list_append_long_long(gaps, &gaps_len, 10LL);
    gaps = list_append_long_long(gaps, &gaps_len, 4LL);
    gaps = list_append_long_long(gaps, &gaps_len, 1LL);
    long long g = 0LL;
    while (g < 8LL) {
        long long gap = gaps[(int)({long long _mochi_idx = g; _mochi_idx < 0 ? gaps_len + _mochi_idx : _mochi_idx;})];
        long long i = gap;
        while (i < arr_len) {
            long long insert_value = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            long long j = i;
            while ((j >= gap) && (arr[(int)({long long _mochi_idx = j - gap; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > insert_value)) {
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j - gap; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                j = j - gap;
            }
            if (j != i) {
                arr[(int)(j)] = insert_value;
            }
            i = i + 1LL;
        }
        g = g + 1LL;
    }
    return shell_sort_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = shell_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5); char *__res = str_list_int(__tmp3, shell_sort_len); __res;}));
        puts(({long long *__tmp4 = shell_sort(NULL, 0); char *__res = str_list_int(__tmp4, shell_sort_len); __res;}));
        puts(({long long *__tmp5 = shell_sort((long long[]){-2LL, -5LL, -45LL}, 3); char *__res = str_list_int(__tmp5, shell_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
