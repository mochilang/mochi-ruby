// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t circle_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long circle_sort_util(long long * collection, size_t collection_len, long long low, long long high);
long long * circle_sort(long long * collection, size_t collection_len);
int main(void);

long long circle_sort_util(long long * collection, size_t collection_len, long long low, long long high) {
    long long swapped = 0LL;
    if (low == high) {
        return swapped;
    }
    long long left = low;
    long long right = high;
    while (left < right) {
        if (collection[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})] > collection[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})]) {
            long long tmp = collection[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
            collection[(int)(left)] = collection[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
            collection[(int)(right)] = tmp;
            swapped = 1LL;
        }
        left = left + 1LL;
        right = right - 1LL;
    }
    if ((left == right) && (collection[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})] > collection[(int)({long long _mochi_idx = right + 1LL; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})])) {
        long long tmp2 = collection[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
        collection[(int)(left)] = collection[(int)({long long _mochi_idx = right + 1LL; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
        collection[(int)(right + 1LL)] = tmp2;
        swapped = 1LL;
    }
    long long mid = low + ((high - low) / 2LL);
    long long left_swap = circle_sort_util(collection, collection_len, low, mid);
    long long right_swap = circle_sort_util(collection, collection_len, mid + 1LL, high);
    if ((swapped || left_swap) || right_swap) {
        return 1LL;
    } else {
        return 0LL;
    }
}

long long * circle_sort(long long * collection, size_t collection_len) {
    if (collection_len < 2LL) {
        return circle_sort_len = collection_len, collection;
    }
    long long is_not_sorted = 1LL;
    while (is_not_sorted) {
        is_not_sorted = circle_sort_util(collection, collection_len, 0LL, collection_len - 1LL);
    }
    return circle_sort_len = collection_len, collection;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = circle_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5); char *__res = str_list_int(__tmp3, circle_sort_len); __res;}));
        puts(({long long *__tmp4 = circle_sort(NULL, 0); char *__res = str_list_int(__tmp4, circle_sort_len); __res;}));
        puts(({long long *__tmp5 = circle_sort((double[]){-2.0, 5LL, 0LL, -45.0}, 4); char *__res = str_list_int(__tmp5, circle_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
