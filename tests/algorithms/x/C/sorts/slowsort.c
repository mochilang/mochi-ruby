// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t slow_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long seq1_init[8] = {1LL, 6LL, 2LL, 5LL, 3LL, 4LL, 4LL, 5LL};
long long *seq1 = seq1_init;
size_t seq1_len = 8;
long long *seq2 = NULL;
size_t seq2_len = 0;
long long seq3_init[1] = {2LL};
long long *seq3 = seq3_init;
size_t seq3_len = 1;
long long seq4_init[4] = {1LL, 2LL, 3LL, 4LL};
long long *seq4 = seq4_init;
size_t seq4_len = 4;
long long seq5_init[4] = {4LL, 3LL, 2LL, 1LL};
long long *seq5 = seq5_init;
size_t seq5_len = 4;
long long seq6_init[10] = {9LL, 8LL, 7LL, 6LL, 5LL, 4LL, 3LL, 2LL, 1LL, 0LL};
long long *seq6 = seq6_init;
size_t seq6_len = 10;
long long seq7_init[10] = {9LL, 8LL, 7LL, 6LL, 5LL, 4LL, 3LL, 2LL, 1LL, 0LL};
long long *seq7 = seq7_init;
size_t seq7_len = 10;
long long seq8_init[10] = {9LL, 8LL, 7LL, 6LL, 5LL, 4LL, 3LL, 2LL, 1LL, 0LL};
long long *seq8 = seq8_init;
size_t seq8_len = 10;

const char* swap(long long * seq, size_t seq_len, long long i, long long j);
const char* slowsort_recursive(long long * seq, size_t seq_len, long long start, long long end_index);
long long * slow_sort(long long * seq, size_t seq_len);
int main(void);

const char* swap(long long * seq, size_t seq_len, long long i, long long j) {
    long long temp = seq[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})];
    seq[(int)(i)] = seq[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})];
    seq[(int)(j)] = temp;
}

const char* slowsort_recursive(long long * seq, size_t seq_len, long long start, long long end_index) {
    if (start >= end_index) {
        return 0;
    }
    long long mid = (start + end_index) / 2LL;
    slowsort_recursive(seq, seq_len, start, mid);
    slowsort_recursive(seq, seq_len, mid + 1LL, end_index);
    if (seq[(int)({long long _mochi_idx = end_index; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})] < seq[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})]) {
        swap(seq, seq_len, end_index, mid);
    }
    slowsort_recursive(seq, seq_len, start, end_index - 1LL);
}

long long * slow_sort(long long * seq, size_t seq_len) {
    if (seq_len > 0LL) {
        slowsort_recursive(seq, seq_len, 0LL, seq_len - 1LL);
    }
    return slow_sort_len = seq_len, seq;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp5 = slow_sort(seq1, seq1_len); char *__res = str_list_int(__tmp5, slow_sort_len); __res;}));
        puts(({long long *__tmp6 = slow_sort(seq2, seq2_len); char *__res = str_list_int(__tmp6, slow_sort_len); __res;}));
        puts(({long long *__tmp7 = slow_sort(seq3, seq3_len); char *__res = str_list_int(__tmp7, slow_sort_len); __res;}));
        puts(({long long *__tmp8 = slow_sort(seq4, seq4_len); char *__res = str_list_int(__tmp8, slow_sort_len); __res;}));
        puts(({long long *__tmp9 = slow_sort(seq5, seq5_len); char *__res = str_list_int(__tmp9, slow_sort_len); __res;}));
        slowsort_recursive(seq6, seq6_len, 2LL, 7LL);
        puts(str_list_int(seq6, seq6_len));
        slowsort_recursive(seq7, seq7_len, 0LL, 4LL);
        puts(str_list_int(seq7, seq7_len));
        slowsort_recursive(seq8, seq8_len, 5LL, 9LL);
        puts(str_list_int(seq8, seq8_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
