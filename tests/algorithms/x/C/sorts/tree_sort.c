// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t inorder_len;
size_t tree_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;
typedef struct TreeState TreeState;

struct Node {
    long long value;
    long long left;
    long long right;
};

struct TreeState {
    Node *nodes;
    size_t nodes_len;
    long long root;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long new_node(TreeState* state, long long value);
void insert(TreeState* state, long long value);
long long * inorder(TreeState state, long long idx);
long long * tree_sort(long long * arr, size_t arr_len);
int main(void);

long long new_node(TreeState* state, long long value) {
    state->nodes = list_append_Node(state->nodes, &state->nodes_len, (Node){.value = value, .left = -1LL, .right = -1LL});
    return state->nodes_len - 1LL;
}

void insert(TreeState* state, long long value) {
    if (state->root == -1LL) {
        state->root = new_node(&state, value);
        return 0;
    }
    long long current = state->root;
    Node *nodes = state->nodes;
    size_t nodes_len = state->nodes_len;
    while (1) {
        Node node = nodes[(int)({long long _mochi_idx = current; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        if (value < node.value) {
            if (node.left == -1LL) {
                long long idx = new_node(&state, value);
                nodes = state->nodes;
                node.left = idx;
                nodes[(int)(current)] = node;
                state->nodes = nodes;
                state->nodes_len = nodes_len;
                return 0;
            }
            current = node.left;
        } else {
            if (value > node.value) {
                if (node.right == -1LL) {
                    long long idx = new_node(&state, value);
                    nodes = state->nodes;
                    node.right = idx;
                    nodes[(int)(current)] = node;
                    state->nodes = nodes;
                    state->nodes_len = nodes_len;
                    return 0;
                }
                current = node.right;
            } else {
                return 0;
            }
        }
    }
}

long long * inorder(TreeState state, long long idx) {
    if (idx == -1LL) {
        {
            long long* __tmp6_data = malloc(0 * sizeof(long long));
            inorder_len = 0;
            return __tmp6_data;
        }
    }
    Node node = state.nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? state.nodes_len + _mochi_idx : _mochi_idx;})];
    long long *result = inorder(state, node.left);
    size_t result_len = inorder_len;
    result = list_append_long_long(result, &result_len, node.value);
    long long *right_part = inorder(state, node.right);
    size_t right_part_len = inorder_len;
    long long i = 0LL;
    while (i < right_part_len) {
        result = list_append_long_long(result, &result_len, right_part[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? right_part_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return inorder_len = result_len, result;
}

long long * tree_sort(long long * arr, size_t arr_len) {
    TreeState state = (TreeState){.nodes = NULL, .nodes_len = 0, .root = -1LL};
    long long i = 0LL;
    while (i < arr_len) {
        insert(&state, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    if (state.root == -1LL) {
        {
            long long* __tmp7_data = malloc(0 * sizeof(long long));
            tree_sort_len = 0;
            return __tmp7_data;
        }
    }
    {
        long long* __ret8 = inorder(state, state.root);
        tree_sort_len = inorder_len;
        return __ret8;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp9 = tree_sort(NULL, 0); char *__res = str_list_int(__tmp9, tree_sort_len); __res;}));
        puts(({long long *__tmp10 = tree_sort((long long[]){1LL}, 1); char *__res = str_list_int(__tmp10, tree_sort_len); __res;}));
        puts(({long long *__tmp11 = tree_sort((long long[]){1LL, 2LL}, 2); char *__res = str_list_int(__tmp11, tree_sort_len); __res;}));
        puts(({long long *__tmp12 = tree_sort((long long[]){5LL, 2LL, 7LL}, 3); char *__res = str_list_int(__tmp12, tree_sort_len); __res;}));
        puts(({long long *__tmp13 = tree_sort((long long[]){5LL, -4LL, 9LL, 2LL, 7LL}, 5); char *__res = str_list_int(__tmp13, tree_sort_len); __res;}));
        puts(({long long *__tmp14 = tree_sort((long long[]){5LL, 6LL, 1LL, -1LL, 4LL, 37LL, 2LL, 7LL}, 8); char *__res = str_list_int(__tmp14, tree_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
