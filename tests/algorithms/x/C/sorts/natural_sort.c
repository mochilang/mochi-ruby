// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t alphanum_key_len;
size_t natural_sort_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* DIGITS = "0123456789";
const char* LOWER = "abcdefghijklmnopqrstuvwxyz";
const char* UPPER = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* example1_init[5] = {"2 ft 7 in", "1 ft 5 in", "10 ft 2 in", "2 ft 11 in", "7 ft 6 in"};
const char* *example1 = example1_init;
size_t example1_len = 5;
const char* example2_init[8] = {"Elm11", "Elm12", "Elm2", "elm0", "elm1", "elm10", "elm13", "elm9"};
const char* *example2 = example2_init;
size_t example2_len = 8;

long long index_of(const char* s, const char* ch);
long long is_digit(const char* ch);
const char* to_lower(const char* ch);
const char* pad_left(const char* s, long long width);
const char* * alphanum_key(const char* s);
long long compare_keys(const char* * a, size_t a_len, const char* * b, size_t b_len);
const char* * natural_sort(const char* * arr, size_t arr_len);
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long is_digit(const char* ch) {
    return index_of(DIGITS, ch) >= 0LL;
}

const char* to_lower(const char* ch) {
    long long idx = index_of(UPPER, ch);
    if (idx >= 0LL) {
        return _substring(LOWER, idx, idx + 1LL);
    }
    return ch;
}

const char* pad_left(const char* s, long long width) {
    const char* res = s;
    while (strlen(res) < width) {
        res = str_concat("0", res);
    }
    return res;
}

const char* * alphanum_key(const char* s) {
    const char* *key = NULL;
    size_t key_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        if (is_digit((const char[]){s[i], 0})) {
            const char* num = "";
            while ((i < strlen(s)) && is_digit((const char[]){s[i], 0})) {
                num = str_concat(num, (const char[]){s[i], 0});
                i = i + 1LL;
            }
            const char* len_str = pad_left(str_int(strlen(num)), 3LL);
            key = list_append_str(key, &key_len, str_concat(str_concat("#", len_str), num));
        } else {
            const char* seg = "";
            while (i < strlen(s)) {
                if (is_digit((const char[]){s[i], 0})) {
                    break;
                }
                seg = str_concat(seg, to_lower((const char[]){s[i], 0}));
                i = i + 1LL;
            }
            key = list_append_str(key, &key_len, seg);
        }
    }
    return alphanum_key_len = key_len, key;
}

long long compare_keys(const char* * a, size_t a_len, const char* * b, size_t b_len) {
    long long i = 0LL;
    while ((i < a_len) && (i < b_len)) {
        if (strcmp(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) < 0) {
            return -1LL;
        }
        if (strcmp(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) > 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    if (a_len < b_len) {
        return -1LL;
    }
    if (a_len > b_len) {
        return 1LL;
    }
    return 0LL;
}

const char* * natural_sort(const char* * arr, size_t arr_len) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* **keys = NULL;
    size_t keys_len = 0;
    size_t *keys_lens = NULL;
    size_t keys_lens_len = 0;
    long long k = 0LL;
    while (k < arr_len) {
        res = list_append_str(res, &res_len, arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        keys = list_append_strptr(keys, &keys_len, alphanum_key(arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]));
        keys_lens = list_append_szt(keys_lens, &keys_lens_len, alphanum_key_len);
        k = k + 1LL;
    }
    long long i = 1LL;
    while (i < res_len) {
        const char* current = res[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
        const char* *current_key = keys[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})];
        size_t current_key_len = keys_lens[(int)(i)];
        long long j = i - 1LL;
        while ((j >= 0LL) && (compare_keys(keys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})], keys_lens[(int)(j)], current_key, current_key_len) > 0LL)) {
            res[(int)(j + 1LL)] = res[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
            keys[(int)(j + 1LL)] = keys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        res[(int)(j + 1LL)] = current;
        keys[(int)(j + 1LL)] = current_key;
        i = i + 1LL;
    }
    return natural_sort_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        puts(({const char **__tmp2 = natural_sort(example1, example1_len); char *__res = str_list_str(__tmp2, natural_sort_len); __res;}));
        puts(({const char **__tmp3 = natural_sort(example2, example2_len); char *__res = str_list_str(__tmp3, natural_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
