// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t subarray_len;
size_t merge_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * subarray(long long * xs, size_t xs_len, long long start, long long end);
long long * merge(long long * arr, size_t arr_len);
int main(void);

long long * subarray(long long * xs, size_t xs_len, long long start, long long end) {
    long long *result = NULL;
    size_t result_len = 0;
    long long k = start;
    while (k < end) {
        result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        k = k + 1LL;
    }
    return subarray_len = result_len, result;
}

long long * merge(long long * arr, size_t arr_len) {
    if (arr_len > 1LL) {
        long long middle_length = arr_len / 2LL;
        long long *left_array = subarray(arr, arr_len, 0LL, middle_length);
        size_t left_array_len = subarray_len;
        long long *right_array = subarray(arr, arr_len, middle_length, arr_len);
        size_t right_array_len = subarray_len;
        long long left_size = left_array_len;
        long long right_size = right_array_len;
        merge(left_array, left_array_len);
        merge(right_array, right_array_len);
        long long left_index = 0LL;
        long long right_index = 0LL;
        long long index = 0LL;
        while ((left_index < left_size) && (right_index < right_size)) {
            if (left_array[(int)({long long _mochi_idx = left_index; _mochi_idx < 0 ? left_array_len + _mochi_idx : _mochi_idx;})] < right_array[(int)({long long _mochi_idx = right_index; _mochi_idx < 0 ? right_array_len + _mochi_idx : _mochi_idx;})]) {
                arr[(int)(index)] = left_array[(int)({long long _mochi_idx = left_index; _mochi_idx < 0 ? left_array_len + _mochi_idx : _mochi_idx;})];
                left_index = left_index + 1LL;
            } else {
                arr[(int)(index)] = right_array[(int)({long long _mochi_idx = right_index; _mochi_idx < 0 ? right_array_len + _mochi_idx : _mochi_idx;})];
                right_index = right_index + 1LL;
            }
            index = index + 1LL;
        }
        while (left_index < left_size) {
            arr[(int)(index)] = left_array[(int)({long long _mochi_idx = left_index; _mochi_idx < 0 ? left_array_len + _mochi_idx : _mochi_idx;})];
            left_index = left_index + 1LL;
            index = index + 1LL;
        }
        while (right_index < right_size) {
            arr[(int)(index)] = right_array[(int)({long long _mochi_idx = right_index; _mochi_idx < 0 ? right_array_len + _mochi_idx : _mochi_idx;})];
            right_index = right_index + 1LL;
            index = index + 1LL;
        }
    }
    return merge_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp5 = merge((long long[]){10LL, 9LL, 8LL, 7LL, 6LL, 5LL, 4LL, 3LL, 2LL, 1LL}, 10); char *__res = str_list_int(__tmp5, merge_len); __res;}));
        puts(({long long *__tmp6 = merge((long long[]){1LL, 2LL, 3LL, 4LL, 5LL, 6LL, 7LL, 8LL, 9LL, 10LL}, 10); char *__res = str_list_int(__tmp6, merge_len); __res;}));
        puts(({long long *__tmp7 = merge((long long[]){10LL, 22LL, 1LL, 2LL, 3LL, 9LL, 15LL, 23LL}, 8); char *__res = str_list_int(__tmp7, merge_len); __res;}));
        puts(({long long *__tmp8 = merge((long long[]){100LL}, 1); char *__res = str_list_int(__tmp8, merge_len); __res;}));
        puts(({long long *__tmp9 = merge(NULL, 0); char *__res = str_list_int(__tmp9, merge_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
