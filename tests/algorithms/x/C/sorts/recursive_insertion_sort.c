// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t insert_next_len;
size_t rec_insertion_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * insert_next(long long * collection, size_t collection_len, long long index);
long long * rec_insertion_sort(long long * collection, size_t collection_len, long long n);
void test_rec_insertion_sort();
void user_main();
int main(void);

long long * insert_next(long long * collection, size_t collection_len, long long index) {
    long long *arr = collection;
    size_t arr_len = collection_len;
    if ((index >= arr_len) || (arr[(int)({long long _mochi_idx = index - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] <= arr[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})])) {
        return insert_next_len = arr_len, arr;
    }
    long long j = index - 1LL;
    long long temp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    arr[(int)(j)] = arr[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    arr[(int)(index)] = temp;
    {
        long long* __ret0 = insert_next(arr, arr_len, index + 1LL);
        insert_next_len = insert_next_len;
        return __ret0;
    }
}

long long * rec_insertion_sort(long long * collection, size_t collection_len, long long n) {
    long long *arr = collection;
    size_t arr_len = collection_len;
    if ((arr_len <= 1LL) || (n <= 1LL)) {
        return rec_insertion_sort_len = arr_len, arr;
    }
    arr = insert_next(arr, arr_len, n - 1LL);
    arr_len = insert_next_len;
    {
        long long* __ret1 = rec_insertion_sort(arr, arr_len, n - 1LL);
        rec_insertion_sort_len = rec_insertion_sort_len;
        return __ret1;
    }
}

void test_rec_insertion_sort() {
    long long *col1 = NULL;
    size_t col1_len = 0;
    col1 = list_append_long_long(col1, &col1_len, 1LL);
    col1 = list_append_long_long(col1, &col1_len, 2LL);
    col1 = list_append_long_long(col1, &col1_len, 1LL);
    col1 = rec_insertion_sort(col1, col1_len, col1_len);
    col1_len = rec_insertion_sort_len;
    if (((col1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? col1_len + _mochi_idx : _mochi_idx;})] != 1LL) || (col1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? col1_len + _mochi_idx : _mochi_idx;})] != 1LL)) || (col1[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? col1_len + _mochi_idx : _mochi_idx;})] != 2LL)) {
        panic("test1 failed");
    }
    long long *col2 = NULL;
    size_t col2_len = 0;
    col2 = list_append_long_long(col2, &col2_len, 2LL);
    col2 = list_append_long_long(col2, &col2_len, 1LL);
    col2 = list_append_long_long(col2, &col2_len, 0LL);
    col2 = list_append_long_long(col2, &col2_len, -1LL);
    col2 = list_append_long_long(col2, &col2_len, -2LL);
    col2 = rec_insertion_sort(col2, col2_len, col2_len);
    col2_len = rec_insertion_sort_len;
    if (col2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? col2_len + _mochi_idx : _mochi_idx;})] != -2LL) {
        panic("test2 failed");
    }
    if (col2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? col2_len + _mochi_idx : _mochi_idx;})] != -1LL) {
        panic("test2 failed");
    }
    if (col2[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? col2_len + _mochi_idx : _mochi_idx;})] != 0LL) {
        panic("test2 failed");
    }
    if (col2[(int)({long long _mochi_idx = 3LL; _mochi_idx < 0 ? col2_len + _mochi_idx : _mochi_idx;})] != 1LL) {
        panic("test2 failed");
    }
    if (col2[(int)({long long _mochi_idx = 4LL; _mochi_idx < 0 ? col2_len + _mochi_idx : _mochi_idx;})] != 2LL) {
        panic("test2 failed");
    }
    long long *col3 = NULL;
    size_t col3_len = 0;
    col3 = list_append_long_long(col3, &col3_len, 1LL);
    col3 = rec_insertion_sort(col3, col3_len, col3_len);
    col3_len = rec_insertion_sort_len;
    if (col3[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? col3_len + _mochi_idx : _mochi_idx;})] != 1LL) {
        panic("test3 failed");
    }
}

void user_main() {
    test_rec_insertion_sort();
    long long *numbers = NULL;
    size_t numbers_len = 0;
    numbers = list_append_long_long(numbers, &numbers_len, 5LL);
    numbers = list_append_long_long(numbers, &numbers_len, 3LL);
    numbers = list_append_long_long(numbers, &numbers_len, 4LL);
    numbers = list_append_long_long(numbers, &numbers_len, 1LL);
    numbers = list_append_long_long(numbers, &numbers_len, 2LL);
    numbers = rec_insertion_sort(numbers, numbers_len, numbers_len);
    numbers_len = rec_insertion_sort_len;
    puts(str_list_int(numbers, numbers_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
