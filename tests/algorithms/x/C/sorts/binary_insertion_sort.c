// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t binary_insertion_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * binary_insertion_sort(long long * arr, size_t arr_len);
void user_main();
int main(void);

long long * binary_insertion_sort(long long * arr, size_t arr_len) {
    long long i = 1LL;
    while (i < arr_len) {
        long long value = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long low = 0LL;
        long long high = i - 1LL;
        while (low <= high) {
            long long mid = (low + high) / 2LL;
            if (value < arr[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                high = mid - 1LL;
            } else {
                low = mid + 1LL;
            }
        }
        long long j = i;
        while (j > low) {
            arr[(int)(j)] = arr[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        arr[(int)(low)] = value;
        i = i + 1LL;
    }
    return binary_insertion_sort_len = arr_len, arr;
}

void user_main() {
    long long *example1 = NULL;
    size_t example1_len = 0;
    example1 = list_append_long_long(example1, &example1_len, 5LL);
    example1 = list_append_long_long(example1, &example1_len, 2LL);
    example1 = list_append_long_long(example1, &example1_len, 4LL);
    example1 = list_append_long_long(example1, &example1_len, 6LL);
    example1 = list_append_long_long(example1, &example1_len, 1LL);
    example1 = list_append_long_long(example1, &example1_len, 3LL);
    puts(({long long *__tmp3 = binary_insertion_sort(example1, example1_len); char *__res = str_list_int(__tmp3, binary_insertion_sort_len); __res;}));
    long long *example2 = NULL;
    size_t example2_len = 0;
    puts(({long long *__tmp4 = binary_insertion_sort(example2, example2_len); char *__res = str_list_int(__tmp4, binary_insertion_sort_len); __res;}));
    long long *example3 = NULL;
    size_t example3_len = 0;
    example3 = list_append_long_long(example3, &example3_len, 4LL);
    example3 = list_append_long_long(example3, &example3_len, 2LL);
    example3 = list_append_long_long(example3, &example3_len, 4LL);
    example3 = list_append_long_long(example3, &example3_len, 1LL);
    example3 = list_append_long_long(example3, &example3_len, 3LL);
    puts(({long long *__tmp5 = binary_insertion_sort(example3, example3_len); char *__res = str_list_int(__tmp5, binary_insertion_sort_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
