// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t pigeonhole_sort_len;

size_t append_len;
static long long list_min(const long long arr[], size_t len) {
    if (len == 0) return 0;
    long long m = arr[0];
    for (size_t i = 1; i < len; i++) {
        if (arr[i] < m) m = arr[i];
    }
    return m;
}

static long long list_max(const long long arr[], size_t len) {
    if (len == 0) return 0;
    long long m = arr[0];
    for (size_t i = 1; i < len; i++) {
        if (arr[i] > m) m = arr[i];
    }
    return m;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long example_init[7] = {8LL, 3LL, 2LL, 7LL, 4LL, 6LL, 8LL};
long long *example = example_init;
size_t example_len = 7;
long long *result = NULL;
size_t result_len = 0;
const char* output = "Sorted order is:";
long long j = 0LL;

long long * pigeonhole_sort(long long * arr, size_t arr_len);
int main(void);

long long * pigeonhole_sort(long long * arr, size_t arr_len) {
    if (arr_len == 0LL) {
        return pigeonhole_sort_len = arr_len, arr;
    }
    long long min_val = (int)(list_min(arr, arr_len));
    long long max_val = (int)(list_max(arr, arr_len));
    long long size = (max_val - min_val) + 1LL;
    long long *holes = NULL;
    size_t holes_len = 0;
    long long i = 0LL;
    while (i < size) {
        holes = list_append_long_long(holes, &holes_len, 0LL);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < arr_len) {
        long long x = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long index = x - min_val;
        holes[(int)(index)] = holes[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? holes_len + _mochi_idx : _mochi_idx;})] + 1LL;
        i = i + 1LL;
    }
    long long sorted_index = 0LL;
    long long count_ = 0LL;
    while (count_ < size) {
        while (holes[(int)({long long _mochi_idx = count_; _mochi_idx < 0 ? holes_len + _mochi_idx : _mochi_idx;})] > 0LL) {
            arr[(int)(sorted_index)] = count_ + min_val;
            holes[(int)(count_)] = holes[(int)({long long _mochi_idx = count_; _mochi_idx < 0 ? holes_len + _mochi_idx : _mochi_idx;})] - 1LL;
            sorted_index = sorted_index + 1LL;
        }
        count_ = count_ + 1LL;
    }
    return pigeonhole_sort_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        result = pigeonhole_sort(example, example_len);
        result_len = pigeonhole_sort_len;
        while (j < result_len) {
            output = str_concat(str_concat(output, " "), str_int(result[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? result_len + _mochi_idx : _mochi_idx;})]));
            j = j + 1LL;
        }
        puts(output);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
