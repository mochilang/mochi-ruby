// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t copy_list_len;
size_t insertion_sort_len;
size_t merge_len;
size_t tim_sort_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long sample_init[12] = {5LL, 9LL, 10LL, 3LL, -4LL, 5LL, 178LL, 92LL, 46LL, -18LL, 0LL, 7LL};
long long *sample = sample_init;
size_t sample_len = 12;
long long *sorted_sample = NULL;
size_t sorted_sample_len = 0;
long long sample2_init[3] = {3LL, 2LL, 1LL};
long long *sample2 = sample2_init;
size_t sample2_len = 3;
long long *sorted_sample2 = NULL;
size_t sorted_sample2_len = 0;

long long * copy_list(long long * xs, size_t xs_len);
long long * insertion_sort(long long * xs, size_t xs_len);
long long * merge(long long * left, size_t left_len, long long * right, size_t right_len);
long long * tim_sort(long long * xs, size_t xs_len);
const char* list_to_string(long long * xs, size_t xs_len);
int main(void);

long long * copy_list(long long * xs, size_t xs_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long k = 0LL;
    while (k < xs_len) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        k = k + 1LL;
    }
    return copy_list_len = res_len, res;
}

long long * insertion_sort(long long * xs, size_t xs_len) {
    long long *arr = copy_list(xs, xs_len);
    size_t arr_len = copy_list_len;
    long long idx = 1LL;
    while (idx < arr_len) {
        long long value = arr[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long jdx = idx - 1LL;
        while ((jdx >= 0LL) && (arr[(int)({long long _mochi_idx = jdx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > value)) {
            arr[(int)(jdx + 1LL)] = arr[(int)({long long _mochi_idx = jdx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            jdx = jdx - 1LL;
        }
        arr[(int)(jdx + 1LL)] = value;
        idx = idx + 1LL;
    }
    return insertion_sort_len = arr_len, arr;
}

long long * merge(long long * left, size_t left_len, long long * right, size_t right_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    long long j = 0LL;
    while ((i < left_len) && (j < right_len)) {
        if (left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})] < right[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]) {
            result = list_append_long_long(result, &result_len, left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        } else {
            result = list_append_long_long(result, &result_len, right[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
    }
    while (i < left_len) {
        result = list_append_long_long(result, &result_len, left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    while (j < right_len) {
        result = list_append_long_long(result, &result_len, right[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return merge_len = result_len, result;
}

long long * tim_sort(long long * xs, size_t xs_len) {
    long long n = xs_len;
    long long **runs = NULL;
    size_t runs_len = 0;
    size_t *runs_lens = NULL;
    size_t runs_lens_len = 0;
    long long **sorted_runs = NULL;
    size_t sorted_runs_len = 0;
    size_t *sorted_runs_lens = NULL;
    size_t sorted_runs_lens_len = 0;
    long long *current = NULL;
    size_t current_len = 0;
    current = list_append_long_long(current, &current_len, xs[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
    long long i = 1LL;
    while (i < n) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] < xs[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]) {
            runs = list_append_intptr(runs, &runs_len, copy_list(current, current_len));
            runs_lens = list_append_szt(runs_lens, &runs_lens_len, copy_list_len);
            current = NULL;
            current_len = 0;
            current = list_append_long_long(current, &current_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        } else {
            current = list_append_long_long(current, &current_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    runs = list_append_intptr(runs, &runs_len, copy_list(current, current_len));
    runs_lens = list_append_szt(runs_lens, &runs_lens_len, copy_list_len);
    long long r = 0LL;
    while (r < runs_len) {
        sorted_runs = list_append_intptr(sorted_runs, &sorted_runs_len, insertion_sort(runs[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? runs_len + _mochi_idx : _mochi_idx;})], runs_lens[(int)(r)]));
        sorted_runs_lens = list_append_szt(sorted_runs_lens, &sorted_runs_lens_len, insertion_sort_len);
        r = r + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    r = 0LL;
    while (r < sorted_runs_len) {
        result = merge(result, result_len, sorted_runs[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? sorted_runs_len + _mochi_idx : _mochi_idx;})], sorted_runs_lens[(int)(r)]);
        result_len = merge_len;
        r = r + 1LL;
    }
    return tim_sort_len = result_len, result;
}

const char* list_to_string(long long * xs, size_t xs_len) {
    const char* s = "[";
    long long k = 0LL;
    while (k < xs_len) {
        s = str_concat(s, str_int(xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        if (k < (xs_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        k = k + 1LL;
    }
    return str_concat(s, "]");
}

int main(void) {
    {
        long long __start = _now();
        sorted_sample = tim_sort(sample, sample_len);
        sorted_sample_len = tim_sort_len;
        puts(list_to_string(sorted_sample, sorted_sample_len));
        sorted_sample2 = tim_sort(sample2, sample2_len);
        sorted_sample2_len = tim_sort_len;
        puts(list_to_string(sorted_sample2, sorted_sample2_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
