// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t cycle_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long * cycle_sort(long long * arr, size_t arr_len);
int main(void);

long long * cycle_sort(long long * arr, size_t arr_len) {
    long long n = arr_len;
    long long cycle_start = 0LL;
    while (cycle_start < (n - 1LL)) {
        long long item = arr[(int)({long long _mochi_idx = cycle_start; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long pos = cycle_start;
        long long i = cycle_start + 1LL;
        while (i < n) {
            if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < item) {
                pos = pos + 1LL;
            }
            i = i + 1LL;
        }
        if (pos == cycle_start) {
            cycle_start = cycle_start + 1LL;
            continue;
        }
        while (item == arr[(int)({long long _mochi_idx = pos; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
            pos = pos + 1LL;
        }
        long long temp = arr[(int)({long long _mochi_idx = pos; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(pos)] = item;
        item = temp;
        while (pos != cycle_start) {
            pos = cycle_start;
            i = cycle_start + 1LL;
            while (i < n) {
                if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < item) {
                    pos = pos + 1LL;
                }
                i = i + 1LL;
            }
            while (item == arr[(int)({long long _mochi_idx = pos; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                pos = pos + 1LL;
            }
            long long temp2 = arr[(int)({long long _mochi_idx = pos; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(pos)] = item;
            item = temp2;
        }
        cycle_start = cycle_start + 1LL;
    }
    return cycle_sort_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = cycle_sort((long long[]){4LL, 3LL, 2LL, 1LL}, 4); char *__res = str_list_int(__tmp3, cycle_sort_len); __res;}));
        puts(({long long *__tmp4 = cycle_sort((double[]){-4.0, 20LL, 0LL, -50.0, 100LL, -1.0}, 6); char *__res = str_list_int(__tmp4, cycle_sort_len); __res;}));
        puts(({long long *__tmp5 = cycle_sort(NULL, 0); char *__res = str_list_int(__tmp5, cycle_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
