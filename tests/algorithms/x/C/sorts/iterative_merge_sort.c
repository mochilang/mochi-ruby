// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t merge_len;
size_t iter_merge_sort_len;

size_t _slice_int_len;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * merge(long long * a, size_t a_len, long long low, long long mid, long long high);
long long * iter_merge_sort(long long * items, size_t items_len);
const char* list_to_string(long long * arr, size_t arr_len);
int main(void);

long long * merge(long long * a, size_t a_len, long long low, long long mid, long long high) {
    long long *left = _slice_int(a, a_len, low, mid, &(_slice_int_len));
    size_t left_len = _slice_int_len;
    long long *right = _slice_int(a, a_len, mid, high + 1LL, &(_slice_int_len));
    size_t right_len = _slice_int_len;
    long long *result = NULL;
    size_t result_len = 0;
    while ((left_len > 0LL) && (right_len > 0LL)) {
        if (left[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})] <= right[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]) {
            result = list_append_long_long(result, &result_len, left[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})]);
            left = _slice_int(left, left_len, 1LL, left_len, &(_slice_int_len));
            left_len = _slice_int_len;
        } else {
            result = list_append_long_long(result, &result_len, right[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]);
            right = _slice_int(right, right_len, 1LL, right_len, &(_slice_int_len));
            right_len = _slice_int_len;
        }
    }
    long long i = 0LL;
    while (i < left_len) {
        result = list_append_long_long(result, &result_len, left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < right_len) {
        result = list_append_long_long(result, &result_len, right[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < result_len) {
        a[(int)(low + i)] = result[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? result_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return merge_len = a_len, a;
}

long long * iter_merge_sort(long long * items, size_t items_len) {
    long long n = items_len;
    if (n <= 1LL) {
        return iter_merge_sort_len = items_len, items;
    }
    long long *arr = _slice_int(items, items_len, 0LL, items_len, &(_slice_int_len));
    size_t arr_len = _slice_int_len;
    long long p = 2LL;
    while (p <= n) {
        long long i = 0LL;
        while (i < n) {
            long long high = (i + p) - 1LL;
            if (high >= n) {
                high = n - 1LL;
            }
            long long low = i;
            long long mid = ((low + high) + 1LL) / 2LL;
            arr = merge(arr, arr_len, low, mid, high);
            arr_len = merge_len;
            i = i + p;
        }
        if ((p * 2LL) >= n) {
            long long mid2 = i - p;
            arr = merge(arr, arr_len, 0LL, mid2, n - 1LL);
            arr_len = merge_len;
            break;
        }
        p = p * 2LL;
    }
    return iter_merge_sort_len = arr_len, arr;
}

const char* list_to_string(long long * arr, size_t arr_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < arr_len) {
        s = str_concat(s, str_int(arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]));
        if (i < (arr_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    return str_concat(s, "]");
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long* __tmp7 = iter_merge_sort((long long[]){5LL, 9LL, 8LL, 7LL, 1LL, 2LL, 7LL}, 7); size_t __tmp7_len = iter_merge_sort_len; list_to_string(__tmp7, __tmp7_len);}));
        puts(({long long* __tmp8 = iter_merge_sort((long long[]){1LL}, 1); size_t __tmp8_len = iter_merge_sort_len; list_to_string(__tmp8, __tmp8_len);}));
        puts(({long long* __tmp9 = iter_merge_sort((long long[]){2LL, 1LL}, 2); size_t __tmp9_len = iter_merge_sort_len; list_to_string(__tmp9, __tmp9_len);}));
        puts(({long long* __tmp10 = iter_merge_sort((long long[]){4LL, 3LL, 2LL, 1LL}, 4); size_t __tmp10_len = iter_merge_sort_len; list_to_string(__tmp10, __tmp10_len);}));
        puts(({long long* __tmp11 = iter_merge_sort((long long[]){5LL, 4LL, 3LL, 2LL, 1LL}, 5); size_t __tmp11_len = iter_merge_sort_len; list_to_string(__tmp11, __tmp11_len);}));
        puts(({long long* __tmp12 = iter_merge_sort((long long[]){-2LL, -9LL, -1LL, -4LL}, 4); size_t __tmp12_len = iter_merge_sort_len; list_to_string(__tmp12, __tmp12_len);}));
        puts(({long long* __tmp13 = iter_merge_sort(NULL, 0); size_t __tmp13_len = iter_merge_sort_len; list_to_string(__tmp13, __tmp13_len);}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
