// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t comb_sort_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long * comb_sort(long long * data, size_t data_len);
void user_main();
int main(void);

long long * comb_sort(long long * data, size_t data_len) {
    double shrink_factor = 1.3;
    long long gap = data_len;
    long long completed = 0LL;
    while (!(completed)) {
        gap = (int)(gap / shrink_factor);
        if (gap <= 1LL) {
            gap = 1LL;
            completed = 1LL;
        }
        long long index = 0LL;
        while ((index + gap) < data_len) {
            if (data[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})] > data[(int)({long long _mochi_idx = index + gap; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = data[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})];
                data[(int)(index)] = data[(int)({long long _mochi_idx = index + gap; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})];
                data[(int)(index + gap)] = tmp;
                completed = 0LL;
            }
            index = index + 1LL;
        }
    }
    return comb_sort_len = data_len, data;
}

void user_main() {
    long long* __tmp3 = comb_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5);
    puts(str_list_int(__tmp3, comb_sort_len));
    long long* __tmp4 = comb_sort(NULL, 0);
    puts(str_list_int(__tmp4, comb_sort_len));
    long long* __tmp5 = comb_sort((double[]){99LL, 45LL, -7.0, 8LL, 2LL, 0LL, -15.0, 3LL}, 8);
    puts(str_list_int(__tmp5, comb_sort_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
