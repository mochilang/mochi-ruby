// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bubble_sort_iterative_len;
size_t bubble_sort_recursive_len;
size_t copy_list_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * bubble_sort_iterative(long long * collection, size_t collection_len);
long long * bubble_sort_recursive(long long * collection, size_t collection_len);
long long * copy_list(long long * xs, size_t xs_len);
long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len);
void test_bubble_sort();
void user_main();
int main(void);

long long * bubble_sort_iterative(long long * collection, size_t collection_len) {
    long long n = collection_len;
    while (n > 0LL) {
        long long swapped = 0LL;
        long long j = 0LL;
        while (j < (n - 1LL)) {
            if (collection[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})] > collection[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})]) {
                long long temp = collection[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
                collection[(int)(j)] = collection[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
                collection[(int)(j + 1LL)] = temp;
                swapped = 1LL;
            }
            j = j + 1LL;
        }
        if (!(swapped)) {
            break;
        }
        n = n - 1LL;
    }
    return bubble_sort_iterative_len = collection_len, collection;
}

long long * bubble_sort_recursive(long long * collection, size_t collection_len) {
    long long n = collection_len;
    long long swapped = 0LL;
    long long i = 0LL;
    while (i < (n - 1LL)) {
        if (collection[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})] > collection[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})]) {
            long long temp = collection[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
            collection[(int)(i)] = collection[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
            collection[(int)(i + 1LL)] = temp;
            swapped = 1LL;
        }
        i = i + 1LL;
    }
    if (swapped) {
        {
            long long* __ret8 = bubble_sort_recursive(collection, collection_len);
            bubble_sort_recursive_len = bubble_sort_recursive_len;
            return __ret8;
        }
    }
    return bubble_sort_recursive_len = collection_len, collection;
}

long long * copy_list(long long * xs, size_t xs_len) {
    long long *out = NULL;
    size_t out_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        out = list_append_long_long(out, &out_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return copy_list_len = out_len, out;
}

long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long k = 0LL;
    while (k < a_len) {
        if (a[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        k = k + 1LL;
    }
    return 1LL;
}

void test_bubble_sort() {
    long long *example = NULL;
    size_t example_len = 0;
    example = list_append_long_long(example, &example_len, 0LL);
    example = list_append_long_long(example, &example_len, 5LL);
    example = list_append_long_long(example, &example_len, 2LL);
    example = list_append_long_long(example, &example_len, 3LL);
    example = list_append_long_long(example, &example_len, 2LL);
    long long *expected = NULL;
    size_t expected_len = 0;
    expected = list_append_long_long(expected, &expected_len, 0LL);
    expected = list_append_long_long(expected, &expected_len, 2LL);
    expected = list_append_long_long(expected, &expected_len, 2LL);
    expected = list_append_long_long(expected, &expected_len, 3LL);
    expected = list_append_long_long(expected, &expected_len, 5LL);
    if (!(({long long* __tmp9 = ({long long* __tmp10 = copy_list(example, example_len); size_t __tmp10_len = copy_list_len; bubble_sort_iterative(__tmp10, __tmp10_len);}); size_t __tmp9_len = bubble_sort_iterative_len; list_eq(__tmp9, __tmp9_len, expected, expected_len);}))) {
        panic("iterative failed");
    }
    if (!(({long long* __tmp11 = ({long long* __tmp12 = copy_list(example, example_len); size_t __tmp12_len = copy_list_len; bubble_sort_recursive(__tmp12, __tmp12_len);}); size_t __tmp11_len = bubble_sort_recursive_len; list_eq(__tmp11, __tmp11_len, expected, expected_len);}))) {
        panic("recursive failed");
    }
    long long *empty = NULL;
    size_t empty_len = 0;
    if (bubble_sort_iterative_len != 0LL) {
        panic("empty iterative failed");
    }
    if (bubble_sort_recursive_len != 0LL) {
        panic("empty recursive failed");
    }
}

void user_main() {
    test_bubble_sort();
    long long *arr = NULL;
    size_t arr_len = 0;
    arr = list_append_long_long(arr, &arr_len, 5LL);
    arr = list_append_long_long(arr, &arr_len, 1LL);
    arr = list_append_long_long(arr, &arr_len, 4LL);
    arr = list_append_long_long(arr, &arr_len, 2LL);
    arr = list_append_long_long(arr, &arr_len, 8LL);
    puts(({long long *__tmp13 = ({long long* __tmp14 = copy_list(arr, arr_len); size_t __tmp14_len = copy_list_len; bubble_sort_iterative(__tmp14, __tmp14_len);}); char *__res = str_list_int(__tmp13, bubble_sort_iterative_len); __res;}));
    puts(({long long *__tmp15 = ({long long* __tmp16 = copy_list(arr, arr_len); size_t __tmp16_len = copy_list_len; bubble_sort_recursive(__tmp16, __tmp16_len);}); char *__res = str_list_int(__tmp15, bubble_sort_recursive_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
