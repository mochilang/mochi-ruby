// Generated by Mochi 0.10.32 on 2025-08-24 08:57 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t heap_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long data_init[11] = {3LL, 7LL, 9LL, 28LL, 123LL, -5LL, 8LL, -30LL, -200LL, 0LL, 4LL};
long long *data = data_init;
size_t data_len = 11;
long long *result = NULL;
size_t result_len = 0;

void heapify(long long * arr, size_t arr_len, long long index, long long heap_size);
long long * heap_sort(long long * arr, size_t arr_len);
int main(void);

void heapify(long long * arr, size_t arr_len, long long index, long long heap_size) {
    long long largest = index;
    long long left_index = (2LL * index) + 1LL;
    long long right_index = (2LL * index) + 2LL;
    if ((left_index < heap_size) && (arr[(int)({long long _mochi_idx = left_index; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = largest; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})])) {
        largest = left_index;
    }
    if ((right_index < heap_size) && (arr[(int)({long long _mochi_idx = right_index; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = largest; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})])) {
        largest = right_index;
    }
    if (largest != index) {
        long long temp = arr[(int)({long long _mochi_idx = largest; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(largest)] = arr[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(index)] = temp;
        heapify(arr, arr_len, largest, heap_size);
    }
}

long long * heap_sort(long long * arr, size_t arr_len) {
    long long n = arr_len;
    long long i = (n / 2LL) - 1LL;
    while (i >= 0LL) {
        heapify(arr, arr_len, i, n);
        i = i - 1LL;
    }
    i = n - 1LL;
    while (i > 0LL) {
        long long temp = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(0LL)] = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(i)] = temp;
        heapify(arr, arr_len, 0LL, i);
        i = i - 1LL;
    }
    return heap_sort_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        result = heap_sort(data, data_len);
        result_len = heap_sort_len;
        puts(str_list_int(result, result_len));
        if (strcmp(str_list_int(result, result_len), str_list_int((long long[]){-200LL, -30LL, -5LL, 0LL, 3LL, 4LL, 7LL, 8LL, 9LL, 28LL, 123LL}, 11)) != 0) {
            panic("Assertion error");
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
