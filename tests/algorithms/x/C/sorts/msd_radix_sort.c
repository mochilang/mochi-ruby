// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t _msd_radix_sort_len;
size_t msd_radix_sort_len;
size_t msd_radix_sort_inplace_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long ex1_init[5] = {40LL, 12LL, 1LL, 100LL, 4LL};
long long *ex1 = ex1_init;
size_t ex1_len = 5;
long long *sorted1 = NULL;
size_t sorted1_len = 0;
long long *ex2 = NULL;
size_t ex2_len = 0;
long long *sorted2 = NULL;
size_t sorted2_len = 0;
long long ex3_init[4] = {123LL, 345LL, 123LL, 80LL};
long long *ex3 = ex3_init;
size_t ex3_len = 4;
long long *sorted3 = NULL;
size_t sorted3_len = 0;
long long ex4_init[5] = {1209LL, 834598LL, 1LL, 540402LL, 45LL};
long long *ex4 = ex4_init;
size_t ex4_len = 5;
long long *sorted4 = NULL;
size_t sorted4_len = 0;

long long get_bit_length(long long n);
long long max_bit_length(long long * nums, size_t nums_len);
long long get_bit(long long num, long long pos);
long long * _msd_radix_sort(long long * nums, size_t nums_len, long long bit_position);
long long * msd_radix_sort(long long * nums, size_t nums_len);
long long * msd_radix_sort_inplace(long long * nums, size_t nums_len);
int main(void);

long long get_bit_length(long long n) {
    if (n == 0LL) {
        return 1LL;
    }
    long long length = 0LL;
    long long num = n;
    while (num > 0LL) {
        length = length + 1LL;
        num = num / 2LL;
    }
    return length;
}

long long max_bit_length(long long * nums, size_t nums_len) {
    long long i = 0LL;
    long long max_len = 0LL;
    while (i < nums_len) {
        long long l = get_bit_length(nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]);
        if (l > max_len) {
            max_len = l;
        }
        i = i + 1LL;
    }
    return max_len;
}

long long get_bit(long long num, long long pos) {
    long long n = num;
    long long i = 0LL;
    while (i < pos) {
        n = n / 2LL;
        i = i + 1LL;
    }
    return n % 2LL;
}

long long * _msd_radix_sort(long long * nums, size_t nums_len, long long bit_position) {
    if ((bit_position == 0LL) || (nums_len <= 1LL)) {
        return _msd_radix_sort_len = nums_len, nums;
    }
    long long *zeros = NULL;
    size_t zeros_len = 0;
    long long *ones = NULL;
    size_t ones_len = 0;
    long long i = 0LL;
    while (i < nums_len) {
        long long num = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (get_bit(num, bit_position - 1LL) == 1LL) {
            ones = list_append_long_long(ones, &ones_len, num);
        } else {
            zeros = list_append_long_long(zeros, &zeros_len, num);
        }
        i = i + 1LL;
    }
    zeros = _msd_radix_sort(zeros, zeros_len, bit_position - 1LL);
    zeros_len = _msd_radix_sort_len;
    ones = _msd_radix_sort(ones, ones_len, bit_position - 1LL);
    ones_len = _msd_radix_sort_len;
    long long *res = zeros;
    size_t res_len = zeros_len;
    i = 0LL;
    while (i < ones_len) {
        res = list_append_int_new(res, res_len, ones[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ones_len + _mochi_idx : _mochi_idx;})]);
        res_len++;
        i = i + 1LL;
    }
    return _msd_radix_sort_len = res_len, res;
}

long long * msd_radix_sort(long long * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        {
            long long* __tmp0_data = malloc(0 * sizeof(long long));
            msd_radix_sort_len = 0;
            return __tmp0_data;
        }
    }
    long long i = 0LL;
    while (i < nums_len) {
        if (nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] < 0LL) {
            panic("All numbers must be positive");
        }
        i = i + 1LL;
    }
    long long bits = max_bit_length(nums, nums_len);
    long long *result = _msd_radix_sort(nums, nums_len, bits);
    size_t result_len = _msd_radix_sort_len;
    return msd_radix_sort_len = result_len, result;
}

long long * msd_radix_sort_inplace(long long * nums, size_t nums_len) {
    {
        long long* __ret1 = msd_radix_sort(nums, nums_len);
        msd_radix_sort_inplace_len = msd_radix_sort_len;
        return __ret1;
    }
}

int main(void) {
    {
        long long __start = _now();
        sorted1 = msd_radix_sort(ex1, ex1_len);
        sorted1_len = msd_radix_sort_len;
        puts(str_list_int(sorted1, sorted1_len));
        sorted2 = msd_radix_sort(ex2, ex2_len);
        sorted2_len = msd_radix_sort_len;
        puts(str_list_int(sorted2, sorted2_len));
        sorted3 = msd_radix_sort(ex3, ex3_len);
        sorted3_len = msd_radix_sort_len;
        puts(str_list_int(sorted3, sorted3_len));
        sorted4 = msd_radix_sort(ex4, ex4_len);
        sorted4_len = msd_radix_sort_len;
        puts(str_list_int(sorted4, sorted4_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
