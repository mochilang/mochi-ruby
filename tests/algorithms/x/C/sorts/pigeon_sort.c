// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_list_len;
size_t pigeon_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * make_list(long long n, long long value);
long long min_value(long long * arr, size_t arr_len);
long long max_value(long long * arr, size_t arr_len);
long long * pigeon_sort(long long * array, size_t array_len);
int main(void);

long long * make_list(long long n, long long value) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < n) {
        result = list_append_long_long(result, &result_len, value);
        i = i + 1LL;
    }
    return make_list_len = result_len, result;
}

long long min_value(long long * arr, size_t arr_len) {
    long long m = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < arr_len) {
        if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < m) {
            m = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return m;
}

long long max_value(long long * arr, size_t arr_len) {
    long long m = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < arr_len) {
        if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > m) {
            m = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return m;
}

long long * pigeon_sort(long long * array, size_t array_len) {
    if (array_len == 0LL) {
        return pigeon_sort_len = array_len, array;
    }
    long long mn = min_value(array, array_len);
    long long mx = max_value(array, array_len);
    long long holes_range = (mx - mn) + 1LL;
    long long *holes = make_list(holes_range, 0LL);
    size_t holes_len = make_list_len;
    long long *holes_repeat = make_list(holes_range, 0LL);
    size_t holes_repeat_len = make_list_len;
    long long i = 0LL;
    while (i < array_len) {
        long long index = array[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] - mn;
        holes[(int)(index)] = array[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})];
        holes_repeat[(int)(index)] = holes_repeat[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? holes_repeat_len + _mochi_idx : _mochi_idx;})] + 1LL;
        i = i + 1LL;
    }
    long long array_index = 0LL;
    long long h = 0LL;
    while (h < holes_range) {
        while (holes_repeat[(int)({long long _mochi_idx = h; _mochi_idx < 0 ? holes_repeat_len + _mochi_idx : _mochi_idx;})] > 0LL) {
            array[(int)(array_index)] = holes[(int)({long long _mochi_idx = h; _mochi_idx < 0 ? holes_len + _mochi_idx : _mochi_idx;})];
            array_index = array_index + 1LL;
            holes_repeat[(int)(h)] = holes_repeat[(int)({long long _mochi_idx = h; _mochi_idx < 0 ? holes_repeat_len + _mochi_idx : _mochi_idx;})] - 1LL;
        }
        h = h + 1LL;
    }
    return pigeon_sort_len = array_len, array;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = pigeon_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5); char *__res = str_list_int(__tmp3, pigeon_sort_len); __res;}));
        puts(({long long *__tmp4 = pigeon_sort(NULL, 0); char *__res = str_list_int(__tmp4, pigeon_sort_len); __res;}));
        puts(({long long *__tmp5 = pigeon_sort((long long[]){-2LL, -5LL, -45LL}, 3); char *__res = str_list_int(__tmp5, pigeon_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
