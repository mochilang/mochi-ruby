// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t merge_len;
size_t strand_sort_rec_len;
size_t strand_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * merge(long long * xs, size_t xs_len, long long * ys, size_t ys_len, long long reverse);
long long * strand_sort_rec(long long * arr, size_t arr_len, long long reverse, long long * solution, size_t solution_len);
long long * strand_sort(long long * arr, size_t arr_len, long long reverse);
int main(void);

long long * merge(long long * xs, size_t xs_len, long long * ys, size_t ys_len, long long reverse) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    long long j = 0LL;
    while ((i < xs_len) && (j < ys_len)) {
        if (reverse) {
            if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] > ys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ys_len + _mochi_idx : _mochi_idx;})]) {
                result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
                i = i + 1LL;
            } else {
                result = list_append_long_long(result, &result_len, ys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ys_len + _mochi_idx : _mochi_idx;})]);
                j = j + 1LL;
            }
        } else {
            if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] < ys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ys_len + _mochi_idx : _mochi_idx;})]) {
                result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
                i = i + 1LL;
            } else {
                result = list_append_long_long(result, &result_len, ys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ys_len + _mochi_idx : _mochi_idx;})]);
                j = j + 1LL;
            }
        }
    }
    while (i < xs_len) {
        result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    while (j < ys_len) {
        result = list_append_long_long(result, &result_len, ys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ys_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return merge_len = result_len, result;
}

long long * strand_sort_rec(long long * arr, size_t arr_len, long long reverse, long long * solution, size_t solution_len) {
    if (arr_len == 0LL) {
        return strand_sort_rec_len = solution_len, solution;
    }
    long long *sublist = NULL;
    size_t sublist_len = 0;
    long long *remaining = NULL;
    size_t remaining_len = 0;
    sublist = list_append_long_long(sublist, &sublist_len, arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
    long long last = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    long long k = 1LL;
    while (k < arr_len) {
        long long item = arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (reverse) {
            if (item < last) {
                sublist = list_append_long_long(sublist, &sublist_len, item);
                last = item;
            } else {
                remaining = list_append_long_long(remaining, &remaining_len, item);
            }
        } else {
            if (item > last) {
                sublist = list_append_long_long(sublist, &sublist_len, item);
                last = item;
            } else {
                remaining = list_append_long_long(remaining, &remaining_len, item);
            }
        }
        k = k + 1LL;
    }
    solution = merge(solution, solution_len, sublist, sublist_len, reverse);
    solution_len = merge_len;
    {
        long long* __ret2 = strand_sort_rec(remaining, remaining_len, reverse, solution, solution_len);
        strand_sort_rec_len = strand_sort_rec_len;
        return __ret2;
    }
}

long long * strand_sort(long long * arr, size_t arr_len, long long reverse) {
    {
        long long* __ret3 = strand_sort_rec(arr, arr_len, reverse, NULL, 0);
        strand_sort_len = strand_sort_rec_len;
        return __ret3;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp4 = strand_sort((long long[]){4LL, 3LL, 5LL, 1LL, 2LL}, 5, 0LL); char *__res = str_list_int(__tmp4, strand_sort_len); __res;}));
        puts(({long long *__tmp5 = strand_sort((long long[]){4LL, 3LL, 5LL, 1LL, 2LL}, 5, 1LL); char *__res = str_list_int(__tmp5, strand_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
