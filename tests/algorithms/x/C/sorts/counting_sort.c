// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t counting_sort_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* ascii_chars = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~";
long long *example1 = NULL;
size_t example1_len = 0;
long long *example2 = NULL;
size_t example2_len = 0;
long long *example3 = NULL;
size_t example3_len = 0;

long long max_val(long long * arr, size_t arr_len);
long long min_val(long long * arr, size_t arr_len);
long long * counting_sort(long long * collection, size_t collection_len);
const char* chr(long long code);
long long ord(const char* ch);
const char* counting_sort_string(const char* s);
int main(void);

long long max_val(long long * arr, size_t arr_len) {
    long long m = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < arr_len) {
        if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > m) {
            m = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return m;
}

long long min_val(long long * arr, size_t arr_len) {
    long long m = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < arr_len) {
        if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < m) {
            m = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return m;
}

long long * counting_sort(long long * collection, size_t collection_len) {
    if (collection_len == 0LL) {
        {
            long long* __tmp0_data = malloc(0 * sizeof(long long));
            counting_sort_len = 0;
            return __tmp0_data;
        }
    }
    long long coll_len = collection_len;
    long long coll_max = max_val(collection, collection_len);
    long long coll_min = min_val(collection, collection_len);
    long long counting_arr_length = (coll_max + 1LL) - coll_min;
    long long *counting_arr = NULL;
    size_t counting_arr_len = 0;
    long long i = 0LL;
    while (i < counting_arr_length) {
        counting_arr = list_append_long_long(counting_arr, &counting_arr_len, 0LL);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < coll_len) {
        long long number = collection[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
        counting_arr[(int)(number - coll_min)] = counting_arr[(int)({long long _mochi_idx = number - coll_min; _mochi_idx < 0 ? counting_arr_len + _mochi_idx : _mochi_idx;})] + 1LL;
        i = i + 1LL;
    }
    i = 1LL;
    while (i < counting_arr_length) {
        counting_arr[(int)(i)] = counting_arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counting_arr_len + _mochi_idx : _mochi_idx;})] + counting_arr[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? counting_arr_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    long long *ordered = NULL;
    size_t ordered_len = 0;
    i = 0LL;
    while (i < coll_len) {
        ordered = list_append_long_long(ordered, &ordered_len, 0LL);
        i = i + 1LL;
    }
    long long idx = coll_len - 1LL;
    while (idx >= 0LL) {
        long long number = collection[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? collection_len + _mochi_idx : _mochi_idx;})];
        long long pos = counting_arr[(int)({long long _mochi_idx = number - coll_min; _mochi_idx < 0 ? counting_arr_len + _mochi_idx : _mochi_idx;})] - 1LL;
        ordered[(int)(pos)] = number;
        counting_arr[(int)(number - coll_min)] = counting_arr[(int)({long long _mochi_idx = number - coll_min; _mochi_idx < 0 ? counting_arr_len + _mochi_idx : _mochi_idx;})] - 1LL;
        idx = idx - 1LL;
    }
    return counting_sort_len = ordered_len, ordered;
}

const char* chr(long long code) {
    if (code == 10LL) {
        return "\n";
    }
    if (code == 13LL) {
        return "\r";
    }
    if (code == 9LL) {
        return "\t";
    }
    if ((code >= 32LL) && (code < 127LL)) {
        return _substring(ascii_chars, code - 32LL, code - 31LL);
    }
    return "";
}

long long ord(const char* ch) {
    if (strcmp(ch, "\n") == 0) {
        return 10LL;
    }
    if (strcmp(ch, "\r") == 0) {
        return 13LL;
    }
    if (strcmp(ch, "\t") == 0) {
        return 9LL;
    }
    long long i = 0LL;
    while (i < strlen(ascii_chars)) {
        if (strcmp(_substring(ascii_chars, i, i + 1LL), ch) == 0) {
            return 32LL + i;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* counting_sort_string(const char* s) {
    long long *codes = NULL;
    size_t codes_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        codes = list_append_long_long(codes, &codes_len, ord(_substring(s, i, i + 1LL)));
        i = i + 1LL;
    }
    long long *sorted_codes = counting_sort(codes, codes_len);
    size_t sorted_codes_len = counting_sort_len;
    const char* res = "";
    i = 0LL;
    while (i < sorted_codes_len) {
        res = str_concat(res, chr(sorted_codes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sorted_codes_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        example1 = counting_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5);
        example1_len = counting_sort_len;
        puts(str_list_int(example1, example1_len));
        example2 = counting_sort(NULL, 0);
        example2_len = counting_sort_len;
        puts(str_list_int(example2, example2_len));
        example3 = counting_sort((double[]){-2.0, -5.0, -45.0}, 3);
        example3_len = counting_sort_len;
        puts(str_list_int(example3, example3_len));
        puts(counting_sort_string("thisisthestring"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
