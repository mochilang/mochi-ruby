// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t odd_even_transposition_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * odd_even_transposition(long long * xs, size_t xs_len);
void user_main();
int main(void);

long long * odd_even_transposition(long long * xs, size_t xs_len) {
    long long *arr = xs;
    size_t arr_len = xs_len;
    long long n = arr_len;
    long long phase = 0LL;
    while (phase < n) {
        long long start;
        if ((phase % 2LL) == 0LL) {
            start = 0LL;
        } else {
            start = 1LL;
        }
        long long i = start;
        while ((i + 1LL) < n) {
            if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i)] = arr[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i + 1LL)] = tmp;
            }
            i = i + 2LL;
        }
        phase = phase + 1LL;
    }
    return odd_even_transposition_len = arr_len, arr;
}

void user_main() {
    long long *data = NULL;
    size_t data_len = 0;
    data = list_append_long_long(data, &data_len, 10LL);
    data = list_append_long_long(data, &data_len, 9LL);
    data = list_append_long_long(data, &data_len, 8LL);
    data = list_append_long_long(data, &data_len, 7LL);
    data = list_append_long_long(data, &data_len, 6LL);
    data = list_append_long_long(data, &data_len, 5LL);
    data = list_append_long_long(data, &data_len, 4LL);
    data = list_append_long_long(data, &data_len, 3LL);
    data = list_append_long_long(data, &data_len, 2LL);
    data = list_append_long_long(data, &data_len, 1LL);
    puts("Initial List");
    puts(str_list_int(data, data_len));
    long long *sorted = odd_even_transposition(data, data_len);
    size_t sorted_len = odd_even_transposition_len;
    puts("Sorted List");
    puts(str_list_int(sorted, sorted_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
