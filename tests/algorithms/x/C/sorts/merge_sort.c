// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t subarray_len;
size_t merge_len;
size_t merge_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * subarray(long long * xs, size_t xs_len, long long start, long long end);
long long * merge(long long * left, size_t left_len, long long * right, size_t right_len);
long long * merge_sort(long long * collection, size_t collection_len);
int main(void);

long long * subarray(long long * xs, size_t xs_len, long long start, long long end) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = start;
    while (i < end) {
        result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return subarray_len = result_len, result;
}

long long * merge(long long * left, size_t left_len, long long * right, size_t right_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    long long j = 0LL;
    while ((i < left_len) && (j < right_len)) {
        if (left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})] <= right[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]) {
            result = list_append_long_long(result, &result_len, left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        } else {
            result = list_append_long_long(result, &result_len, right[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
    }
    while (i < left_len) {
        result = list_append_long_long(result, &result_len, left[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    while (j < right_len) {
        result = list_append_long_long(result, &result_len, right[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return merge_len = result_len, result;
}

long long * merge_sort(long long * collection, size_t collection_len) {
    if (collection_len <= 1LL) {
        return merge_sort_len = collection_len, collection;
    }
    long long mid_index = collection_len / 2LL;
    long long *left = subarray(collection, collection_len, 0LL, mid_index);
    size_t left_len = subarray_len;
    long long *right = subarray(collection, collection_len, mid_index, collection_len);
    size_t right_len = subarray_len;
    long long *sorted_left = merge_sort(left, left_len);
    size_t sorted_left_len = merge_sort_len;
    long long *sorted_right = merge_sort(right, right_len);
    size_t sorted_right_len = merge_sort_len;
    {
        long long* __ret3 = merge(sorted_left, sorted_left_len, sorted_right, sorted_right_len);
        merge_sort_len = merge_len;
        return __ret3;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp4 = merge_sort((long long[]){0LL, 5LL, 3LL, 2LL, 2LL}, 5); char *__res = str_list_int(__tmp4, merge_sort_len); __res;}));
        puts(({long long *__tmp5 = merge_sort(NULL, 0); char *__res = str_list_int(__tmp5, merge_sort_len); __res;}));
        puts(({long long *__tmp6 = merge_sort((long long[]){-2LL, -5LL, -45LL}, 3); char *__res = str_list_int(__tmp6, merge_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
