// Generated by Mochi 0.10.32 on 2025-08-06 21:30 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long bit_and(long long a, long long b);
long long count_bits_kernighan(long long n);
long long count_bits_modulo(long long n);
void user_main();
int main(void);

long long bit_and(long long a, long long b) {
    long long ua = a;
    long long ub = b;
    long long res = 0LL;
    long long bit = 1LL;
    while ((ua > 0LL) || (ub > 0LL)) {
        if (((ua % 2LL) == 1LL) && ((ub % 2LL) == 1LL)) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long count_bits_kernighan(long long n) {
    if (n < 0LL) {
        panic("the value of input must not be negative");
    }
    long long num = n;
    long long result = 0LL;
    while (num != 0LL) {
        num = bit_and(num, num - 1LL);
        result = result + 1LL;
    }
    return result;
}

long long count_bits_modulo(long long n) {
    if (n < 0LL) {
        panic("the value of input must not be negative");
    }
    long long num = n;
    long long result = 0LL;
    while (num != 0LL) {
        if ((num % 2LL) == 1LL) {
            result = result + 1LL;
        }
        num = num / 2LL;
    }
    return result;
}

void user_main() {
    long long *numbers = NULL;
    size_t numbers_len = 0;
    numbers = list_append_int(numbers, &numbers_len, 25LL);
    numbers = list_append_int(numbers, &numbers_len, 37LL);
    numbers = list_append_int(numbers, &numbers_len, 21LL);
    numbers = list_append_int(numbers, &numbers_len, 58LL);
    numbers = list_append_int(numbers, &numbers_len, 0LL);
    numbers = list_append_int(numbers, &numbers_len, 256LL);
    long long i = 0LL;
    while (i < 6LL) {
        puts(str_int(count_bits_kernighan(numbers[(int)(i)])));
        i = i + 1LL;
    }
    i = 0LL;
    while (i < 6LL) {
        puts(str_int(count_bits_modulo(numbers[(int)(i)])));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
