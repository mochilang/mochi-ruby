// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* get_reverse_bit_string(long long number);
const char* reverse_bit(long long number);
int main(void);

const char* get_reverse_bit_string(long long number) {
    const char* bit_string = "";
    long long n = number;
    long long i = 0LL;
    while (i < 32LL) {
        bit_string = str_concat(bit_string, str_int(n % 2LL));
        n = n / 2LL;
        i = i + 1LL;
    }
    return bit_string;
}

const char* reverse_bit(long long number) {
    if (number < 0LL) {
        panic("the value of input must be positive");
    }
    long long n = number;
    long long result = 0LL;
    long long i = 1LL;
    while (i <= 32LL) {
        result = result * 2LL;
        long long end_bit = n % 2LL;
        n = n / 2LL;
        result = result + end_bit;
        i = i + 1LL;
    }
    return get_reverse_bit_string(result);
}

int main(void) {
    {
        long long __start = _now();
        puts(reverse_bit(25LL));
        puts(reverse_bit(37LL));
        puts(reverse_bit(21LL));
        puts(reverse_bit(58LL));
        puts(reverse_bit(0LL));
        puts(reverse_bit(256LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
