// Generated by Mochi 0.10.32 on 2025-08-06 21:30 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t gray_code_len;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *seq2 = NULL;
size_t seq2_len = 0;
long long *seq1 = NULL;
size_t seq1_len = 0;
long long *seq3 = NULL;
size_t seq3_len = 0;

long long pow2(long long exp);
long long * gray_code(long long bit_count);
int main(void);

long long pow2(long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * 2LL;
        i = i + 1LL;
    }
    return result;
}

long long * gray_code(long long bit_count) {
    if (bit_count == 0LL) {
        {
            static long long __tmp0_arr[1];
            __tmp0_arr[0] = 0LL;
            gray_code_len = 1;
            return __tmp0_arr;
        }
    }
    long long *prev = gray_code(bit_count - 1LL);
    size_t prev_len = gray_code_len;
    long long add_val = pow2(bit_count - 1LL);
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < prev_len) {
        res = list_append_long_long(res, &res_len, prev[(int)(i)]);
        i = i + 1LL;
    }
    long long j = prev_len - 1LL;
    while (j >= 0LL) {
        res = list_append_int(res, &res_len, prev[(int)(j)] + add_val);
        j = j - 1LL;
    }
    return gray_code_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        seq2 = gray_code(2LL);
        seq2_len = gray_code_len;
        puts(str_list_int(seq2, seq2_len));
        seq1 = gray_code(1LL);
        seq1_len = gray_code_len;
        puts(str_list_int(seq1, seq1_len));
        seq3 = gray_code(3LL);
        seq3_len = gray_code_len;
        puts(str_list_int(seq3, seq3_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
