// Generated by Mochi 0.10.32 on 2025-08-06 21:30 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long bitwise_xor(long long a, long long b);
long long bitwise_and(long long a, long long b);
long long bitwise_addition_recursive(long long number, long long other_number);
int main(void);

long long bitwise_xor(long long a, long long b) {
    long long result = 0LL;
    long long bit = 1LL;
    long long x = a;
    long long y = b;
    while ((x > 0LL) || (y > 0LL)) {
        long long ax = x % 2LL;
        long long by = y % 2LL;
        if (((ax + by) % 2LL) == 1LL) {
            result = result + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return result;
}

long long bitwise_and(long long a, long long b) {
    long long result = 0LL;
    long long bit = 1LL;
    long long x = a;
    long long y = b;
    while ((x > 0LL) && (y > 0LL)) {
        if (((x % 2LL) == 1LL) && ((y % 2LL) == 1LL)) {
            result = result + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return result;
}

long long bitwise_addition_recursive(long long number, long long other_number) {
    if ((number < 0LL) || (other_number < 0LL)) {
        panic("Both arguments MUST be non-negative!");
    }
    long long bitwise_sum = bitwise_xor(number, other_number);
    long long carry = bitwise_and(number, other_number);
    if (carry == 0LL) {
        return bitwise_sum;
    }
    return bitwise_addition_recursive(bitwise_sum, carry * 2LL);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(bitwise_addition_recursive(4LL, 5LL)));
        puts(str_int(bitwise_addition_recursive(8LL, 9LL)));
        puts(str_int(bitwise_addition_recursive(0LL, 4LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
