// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long pow2(long long exp);
long long is_bit_set(long long number, long long position);
long long set_bit(long long number, long long position);
long long clear_bit(long long number, long long position);
long long flip_bit(long long number, long long position);
long long get_bit(long long number, long long position);
int main(void);

long long pow2(long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * 2LL;
        i = i + 1LL;
    }
    return result;
}

long long is_bit_set(long long number, long long position) {
    long long shifted = number / pow2(position);
    long long remainder = shifted % 2LL;
    return remainder == 1LL;
}

long long set_bit(long long number, long long position) {
    if (is_bit_set(number, position)) {
        return number;
    }
    return number + pow2(position);
}

long long clear_bit(long long number, long long position) {
    if (is_bit_set(number, position)) {
        return number - pow2(position);
    }
    return number;
}

long long flip_bit(long long number, long long position) {
    if (is_bit_set(number, position)) {
        return number - pow2(position);
    }
    return number + pow2(position);
}

long long get_bit(long long number, long long position) {
    if (is_bit_set(number, position)) {
        return 1LL;
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(set_bit(13LL, 1LL)));
        puts(str_int(clear_bit(18LL, 1LL)));
        puts(str_int(flip_bit(5LL, 1LL)));
        puts(str_int(is_bit_set(10LL, 3LL)));
        puts(str_int(get_bit(10LL, 1LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
