// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* pad_left_num(long long n);
const char* to_binary(long long n);
const char* show_bits(long long before, long long after);
long long lshift(long long num, long long k);
long long rshift(long long num, long long k);
long long swap_odd_even_bits(long long num);
void user_main();
int main(void);

const char* pad_left_num(long long n) {
    const char* s = str_int(n);
    while (strlen(s) < 5LL) {
        s = str_concat(" ", s);
    }
    return s;
}

const char* to_binary(long long n) {
    const char* sign = "";
    long long num = n;
    if (num < 0LL) {
        sign = "-";
        num = 0LL - num;
    }
    const char* bits = "";
    while (num > 0LL) {
        bits = str_concat(str_int(num % 2LL), bits);
        num = (num - (num % 2LL)) / 2LL;
    }
    if (strcmp(bits, "") == 0) {
        bits = "0";
    }
    long long min_width = 8LL;
    while (strlen(bits) < (min_width - strlen(sign))) {
        bits = str_concat("0", bits);
    }
    return str_concat(sign, bits);
}

const char* show_bits(long long before, long long after) {
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(pad_left_num(before), ": "), to_binary(before)), "\n"), pad_left_num(after)), ": "), to_binary(after));
}

long long lshift(long long num, long long k) {
    long long result = num;
    long long i = 0LL;
    while (i < k) {
        result = result * 2LL;
        i = i + 1LL;
    }
    return result;
}

long long rshift(long long num, long long k) {
    long long result = num;
    long long i = 0LL;
    while (i < k) {
        result = (result - (result % 2LL)) / 2LL;
        i = i + 1LL;
    }
    return result;
}

long long swap_odd_even_bits(long long num) {
    long long n = num;
    if (n < 0LL) {
        n = n + 4294967296LL;
    }
    long long result = 0LL;
    long long i = 0LL;
    while (i < 32LL) {
        long long bit1 = rshift(n, i) % 2LL;
        long long bit2 = rshift(n, i + 1LL) % 2LL;
        result = (result + lshift(bit1, i + 1LL)) + lshift(bit2, i);
        i = i + 2LL;
    }
    return result;
}

void user_main() {
    long long *nums = NULL;
    size_t nums_len = 0;
    nums = list_append_int(nums, &nums_len, -1LL);
    nums = list_append_int(nums, &nums_len, 0LL);
    nums = list_append_int(nums, &nums_len, 1LL);
    nums = list_append_int(nums, &nums_len, 2LL);
    nums = list_append_int(nums, &nums_len, 3LL);
    nums = list_append_int(nums, &nums_len, 4LL);
    nums = list_append_int(nums, &nums_len, 23LL);
    nums = list_append_int(nums, &nums_len, 24LL);
    long long i = 0LL;
    while (i < 8LL) {
        long long n = nums[(int)(i)];
        puts(show_bits(n, swap_odd_even_bits(n)));
        puts("");
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
