// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;
double AXIS_A = 6.378137e+06;
double AXIS_B = 6.356752314245e+06;
double RADIUS = 6.378137e+06;
double *SAN_FRANCISCO = NULL;
size_t SAN_FRANCISCO_len = 0;
double *YOSEMITE = NULL;
size_t YOSEMITE_len = 0;

double to_radians(double deg);
double sin_taylor(double x);
double cos_taylor(double x);
double tan_approx(double x);
double sqrtApprox(double x);
double atanApprox(double x);
double atan2Approx(double y, double x);
double asinApprox(double x);
double haversine_distance(double lat1, double lon1, double lat2, double lon2);
int main(void);

double to_radians(double deg) {
    return (deg * PI) / 180.0;
}

double sin_taylor(double x) {
    double term = x;
    double sum = x;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = 2.0 * (double)(i);
        double k2 = k1 + 1.0;
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double cos_taylor(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = (2.0 * (double)(i)) - 1.0;
        double k2 = 2.0 * (double)(i);
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double tan_approx(double x) {
    return sin_taylor(x) / cos_taylor(x);
}

double sqrtApprox(double x) {
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double atanApprox(double x) {
    if (x > 1.0) {
        return (PI / 2.0) - (x / ((x * x) + 0.28));
    }
    if (x < -1.0) {
        return (-(PI) / 2.0) - (x / ((x * x) + 0.28));
    }
    return x / (1.0 + ((0.28 * x) * x));
}

double atan2Approx(double y, double x) {
    if (x > 0.0) {
        double val = atanApprox(y / x);
        return val;
    }
    if (x < 0.0) {
        if (y >= 0.0) {
            return atanApprox(y / x) + PI;
        }
        return atanApprox(y / x) - PI;
    }
    if (y > 0.0) {
        return PI / 2.0;
    }
    if (y < 0.0) {
        return -(PI) / 2.0;
    }
    return 0.0;
}

double asinApprox(double x) {
    double denom = sqrtApprox(1.0 - (x * x));
    double res = atan2Approx(x, denom);
    return res;
}

double haversine_distance(double lat1, double lon1, double lat2, double lon2) {
    double flattening = (AXIS_A - AXIS_B) / AXIS_A;
    double phi_1 = atanApprox((1.0 - flattening) * tan_approx(to_radians(lat1)));
    double phi_2 = atanApprox((1.0 - flattening) * tan_approx(to_radians(lat2)));
    double lambda_1 = to_radians(lon1);
    double lambda_2 = to_radians(lon2);
    double sin_sq_phi = sin_taylor((phi_2 - phi_1) / 2.0);
    double sin_sq_lambda = sin_taylor((lambda_2 - lambda_1) / 2.0);
    sin_sq_phi = sin_sq_phi * sin_sq_phi;
    sin_sq_lambda = sin_sq_lambda * sin_sq_lambda;
    double h_value = sqrtApprox(sin_sq_phi + ((cos_taylor(phi_1) * cos_taylor(phi_2)) * sin_sq_lambda));
    return (2.0 * RADIUS) * asinApprox(h_value);
}

int main(void) {
    {
        long long __start = _now();
        SAN_FRANCISCO = malloc(2 * sizeof(double));
        SAN_FRANCISCO_len = 2;
        SAN_FRANCISCO[0] = 37.774856;
        SAN_FRANCISCO[1] = -122.424227;
        YOSEMITE = malloc(2 * sizeof(double));
        YOSEMITE_len = 2;
        YOSEMITE[0] = 37.864742;
        YOSEMITE[1] = -119.537521;
        puts(str_float(haversine_distance(SAN_FRANCISCO[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? SAN_FRANCISCO_len + _mochi_idx : _mochi_idx;})], SAN_FRANCISCO[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? SAN_FRANCISCO_len + _mochi_idx : _mochi_idx;})], YOSEMITE[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? YOSEMITE_len + _mochi_idx : _mochi_idx;})], YOSEMITE[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? YOSEMITE_len + _mochi_idx : _mochi_idx;})])));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
