// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long kmap_0[2] = {0LL, 1LL};
long long kmap_1[2] = {1LL, 1LL};
long long *kmap_init[2] = {kmap_0, kmap_1};
long long **kmap = kmap_init;
size_t kmap_len = 2;
size_t kmap_lens_init[2] = {2, 2};
size_t *kmap_lens = kmap_lens_init;
size_t kmap_lens_len = 2;

const char* row_string(long long * row, size_t row_len);
void print_kmap(long long * * kmap, size_t kmap_len, size_t* kmap_lens, size_t kmap_lens_len);
const char* join_terms(const char* * terms, size_t terms_len);
const char* simplify_kmap(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len);
int main(void);

const char* row_string(long long * row, size_t row_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < row_len) {
        s = str_concat(s, str_int(row[(int)(i)]));
        if (i < (row_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

void print_kmap(long long * * kmap, size_t kmap_len, size_t* kmap_lens, size_t kmap_lens_len) {
    long long i = 0LL;
    while (i < kmap_len) {
        puts(row_string(kmap[(int)(i)], kmap_lens[i]));
        i = i + 1LL;
    }
}

const char* join_terms(const char* * terms, size_t terms_len) {
    if (terms_len == 0LL) {
        return "";
    }
    const char* res = terms[(int)(0LL)];
    long long i = 1LL;
    while (i < terms_len) {
        res = str_concat(str_concat(res, " + "), terms[(int)(i)]);
        i = i + 1LL;
    }
    return res;
}

const char* simplify_kmap(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len) {
    const char* *terms = NULL;
    size_t terms_len = 0;
    long long a = 0LL;
    while (a < board_len) {
        long long *row = board[(int)(a)];
        size_t row_len = board_lens[a];
        long long b = 0LL;
        while (b < row_len) {
            long long item = row[(int)(b)];
            if (item != 0LL) {
                const char* term = str_concat((a != 0LL ? "A" : "A'"), (b != 0LL ? "B" : "B'"));
                terms = list_append_str(terms, &terms_len, term);
            }
            b = b + 1LL;
        }
        a = a + 1LL;
    }
    const char* expr = join_terms(terms, terms_len);
    return expr;
}

int main(void) {
    {
        long long __start = _now();
        print_kmap(kmap, kmap_len, kmap_lens, kmap_len);
        puts("Simplified Expression:");
        puts(simplify_kmap(kmap, kmap_len, kmap_lens, kmap_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
