// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long nor_gate(long long input_1, long long input_2);
const char* center(const char* s, long long width);
const char* make_table_row(long long i, long long j);
const char* truth_table();
int main(void);

long long nor_gate(long long input_1, long long input_2) {
    if ((input_1 == 0LL) && (input_2 == 0LL)) {
        return 1LL;
    }
    return 0LL;
}

const char* center(const char* s, long long width) {
    long long total = width - strlen(s);
    if (total <= 0LL) {
        return s;
    }
    long long left = total / 2LL;
    long long right = total - left;
    const char* res = s;
    long long i = 0LL;
    while (i < left) {
        res = str_concat(" ", res);
        i = i + 1LL;
    }
    long long j = 0LL;
    while (j < right) {
        res = str_concat(res, " ");
        j = j + 1LL;
    }
    return res;
}

const char* make_table_row(long long i, long long j) {
    long long output = nor_gate(i, j);
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("| ", center(str_int(i), 8LL)), " | "), center(str_int(j), 8LL)), " | "), center(str_int(output), 8LL)), " |");
}

const char* truth_table() {
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("Truth Table of NOR Gate:\n", "| Input 1 | Input 2 | Output  |\n"), make_table_row(0LL, 0LL)), "\n"), make_table_row(0LL, 1LL)), "\n"), make_table_row(1LL, 0LL)), "\n"), make_table_row(1LL, 1LL));
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", nor_gate(0LL, 0LL));
        printf("%d\n", nor_gate(0LL, 1LL));
        printf("%d\n", nor_gate(1LL, 0LL));
        printf("%d\n", nor_gate(1LL, 1LL));
        puts(truth_table());
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
