// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t unique_strings_len;
size_t check_len;
size_t decimal_to_binary_len;
size_t selection_len;
size_t prime_implicant_chart_len;
size_t *prime_implicant_chart_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* compare_string(const char* string1, const char* string2);
long long contains_string(const char* * arr, size_t arr_len, const char* value);
const char* * unique_strings(const char* * arr, size_t arr_len);
const char* * check(const char* * binary, size_t binary_len);
const char* * decimal_to_binary(long long no_of_variable, long long * minterms, size_t minterms_len);
long long is_for_table(const char* string1, const char* string2, long long count);
long long count_ones(long long * row, size_t row_len);
const char* * selection(long long * * chart, size_t chart_len, size_t* chart_lens, size_t chart_lens_len, const char* * prime_implicants, size_t prime_implicants_len);
long long count_char(const char* s, const char* ch);
long long * * prime_implicant_chart(const char* * prime_implicants, size_t prime_implicants_len, const char* * binary, size_t binary_len);
void user_main();
int main(void);

const char* compare_string(const char* string1, const char* string2) {
    const char* result = "";
    long long count = 0LL;
    long long i = 0LL;
    while (i < strlen(string1)) {
        const char* c1 = _substring(string1, i, i + 1LL);
        const char* c2 = _substring(string2, i, i + 1LL);
        if (strcmp(c1, c2) != 0) {
            count = count + 1LL;
            result = str_concat(result, "_");
        } else {
            result = str_concat(result, c1);
        }
        i = i + 1LL;
    }
    if (count > 1LL) {
        return "";
    }
    return result;
}

long long contains_string(const char* * arr, size_t arr_len, const char* value) {
    long long i = 0LL;
    while (i < arr_len) {
        if (strcmp(arr[(int)(i)], value) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * unique_strings(const char* * arr, size_t arr_len) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < arr_len) {
        if (!(contains_string(res, res_len, arr[(int)(i)]))) {
            res = list_append_str(res, &res_len, arr[(int)(i)]);
        }
        i = i + 1LL;
    }
    return unique_strings_len = res_len, res;
}

const char* * check(const char* * binary, size_t binary_len) {
    const char* *pi = NULL;
    size_t pi_len = 0;
    const char* *current = binary;
    size_t current_len = binary_len;
    while (1LL) {
        const char* *check1 = NULL;
        size_t check1_len = 0;
        long long i = 0LL;
        while (i < current_len) {
            check1 = list_append_str(check1, &check1_len, "$");
            i = i + 1LL;
        }
        const char* *temp = NULL;
        size_t temp_len = 0;
        i = 0LL;
        while (i < current_len) {
            long long j = i + 1LL;
            while (j < current_len) {
                const char* k = compare_string(current[(int)(i)], current[(int)(j)]);
                if (strcmp(k, "") == 0) {
                    check1[(int)(i)] = "*";
                    check1[(int)(j)] = "*";
                    temp = list_append_str(temp, &temp_len, "X");
                }
                j = j + 1LL;
            }
            i = i + 1LL;
        }
        i = 0LL;
        while (i < current_len) {
            if (strcmp(check1[(int)(i)], "$") == 0) {
                pi = list_append_str(pi, &pi_len, current[(int)(i)]);
            }
            i = i + 1LL;
        }
        if (temp_len == 0LL) {
            return check_len = pi_len, pi;
        }
        current = unique_strings(temp, temp_len);
        current_len = unique_strings_len;
    }
}

const char* * decimal_to_binary(long long no_of_variable, long long * minterms, size_t minterms_len) {
    const char* *temp = NULL;
    size_t temp_len = 0;
    long long idx = 0LL;
    while (idx < minterms_len) {
        long long minterm = minterms[(int)(idx)];
        const char* string = "";
        long long i = 0LL;
        while (i < no_of_variable) {
            string = str_concat(str_int(minterm % 2LL), string);
            minterm = minterm / 2LL;
            i = i + 1LL;
        }
        temp = list_append_str(temp, &temp_len, string);
        idx = idx + 1LL;
    }
    return decimal_to_binary_len = temp_len, temp;
}

long long is_for_table(const char* string1, const char* string2, long long count) {
    long long count_n = 0LL;
    long long i = 0LL;
    while (i < strlen(string1)) {
        const char* c1 = _substring(string1, i, i + 1LL);
        const char* c2 = _substring(string2, i, i + 1LL);
        if (strcmp(c1, c2) != 0) {
            count_n = count_n + 1LL;
        }
        i = i + 1LL;
    }
    return count_n == count;
}

long long count_ones(long long * row, size_t row_len) {
    long long c = 0LL;
    long long j = 0LL;
    while (j < row_len) {
        if (row[(int)(j)] == 1LL) {
            c = c + 1LL;
        }
        j = j + 1LL;
    }
    return c;
}

const char* * selection(long long * * chart, size_t chart_len, size_t* chart_lens, size_t chart_lens_len, const char* * prime_implicants, size_t prime_implicants_len) {
    const char* *temp = NULL;
    size_t temp_len = 0;
    long long *select = NULL;
    size_t select_len = 0;
    long long i = 0LL;
    while (i < chart_len) {
        select = list_append_long_long(select, &select_len, 0LL);
        i = i + 1LL;
    }
    long long col = 0LL;
    while (col < chart_lens[0LL]) {
        long long count = 0LL;
        long long row = 0LL;
        while (row < chart_len) {
            if (chart[(int)(row)][(int)(col)] == 1LL) {
                count = count + 1LL;
            }
            row = row + 1LL;
        }
        if (count == 1LL) {
            long long rem = 0LL;
            row = 0LL;
            while (row < chart_len) {
                if (chart[(int)(row)][(int)(col)] == 1LL) {
                    rem = row;
                }
                row = row + 1LL;
            }
            select[(int)(rem)] = 1LL;
        }
        col = col + 1LL;
    }
    i = 0LL;
    while (i < select_len) {
        if (select[(int)(i)] == 1LL) {
            long long j = 0LL;
            while (j < chart_lens[0LL]) {
                if (chart[(int)(i)][(int)(j)] == 1LL) {
                    long long r = 0LL;
                    while (r < chart_len) {
                        chart[(int)(r)][(int)(j)] = 0LL;
                        r = r + 1LL;
                    }
                }
                j = j + 1LL;
            }
            temp = list_append_str(temp, &temp_len, prime_implicants[(int)(i)]);
        }
        i = i + 1LL;
    }
    while (1LL) {
        long long *counts = NULL;
        size_t counts_len = 0;
        long long r = 0LL;
        while (r < chart_len) {
            counts = list_append_long_long(counts, &counts_len, count_ones(chart[(int)(r)], chart_lens[r]));
            r = r + 1LL;
        }
        long long max_n = counts[(int)(0LL)];
        long long rem = 0LL;
        long long k = 1LL;
        while (k < counts_len) {
            if (counts[(int)(k)] > max_n) {
                max_n = counts[(int)(k)];
                rem = k;
            }
            k = k + 1LL;
        }
        if (max_n == 0LL) {
            return selection_len = temp_len, temp;
        }
        temp = list_append_str(temp, &temp_len, prime_implicants[(int)(rem)]);
        long long j = 0LL;
        while (j < chart_lens[0LL]) {
            if (chart[(int)(rem)][(int)(j)] == 1LL) {
                long long r2 = 0LL;
                while (r2 < chart_len) {
                    chart[(int)(r2)][(int)(j)] = 0LL;
                    r2 = r2 + 1LL;
                }
            }
            j = j + 1LL;
        }
    }
}

long long count_char(const char* s, const char* ch) {
    long long cnt = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            cnt = cnt + 1LL;
        }
        i = i + 1LL;
    }
    return cnt;
}

long long * * prime_implicant_chart(const char* * prime_implicants, size_t prime_implicants_len, const char* * binary, size_t binary_len) {
    long long **chart = NULL;
    size_t chart_len = 0;
    size_t *chart_lens = NULL;
    size_t chart_lens_len = 0;
    long long i = 0LL;
    while (i < prime_implicants_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < binary_len) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        chart = list_append_long_longptr(chart, &chart_len, row);
        chart_lens = list_append_szt(chart_lens, &chart_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < prime_implicants_len) {
        long long count = count_char(prime_implicants[(int)(i)], "_");
        long long j = 0LL;
        while (j < binary_len) {
            if (is_for_table(prime_implicants[(int)(i)], binary[(int)(j)], count)) {
                chart[(int)(i)][(int)(j)] = 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return prime_implicant_chart_lens = chart_lens, prime_implicant_chart_len = chart_len, chart;
}

void user_main() {
    long long no_of_variable = 3LL;
    long long *minterms = NULL;
    size_t minterms_len = 0;
    minterms = list_append_int(minterms, &minterms_len, 1LL);
    minterms = list_append_int(minterms, &minterms_len, 5LL);
    minterms = list_append_int(minterms, &minterms_len, 7LL);
    const char* *binary = decimal_to_binary(no_of_variable, minterms, minterms_len);
    size_t binary_len = decimal_to_binary_len;
    const char* *prime_implicants = check(binary, binary_len);
    size_t prime_implicants_len = check_len;
    puts("Prime Implicants are:");
    puts(str_list_str(prime_implicants, prime_implicants_len));
    long long **chart = prime_implicant_chart(prime_implicants, prime_implicants_len, binary, binary_len);
    size_t chart_len = prime_implicant_chart_len;
    size_t *chart_lens = prime_implicant_chart_lens;
    size_t chart_lens_len = prime_implicant_chart_len;
    const char* *essential_prime_implicants = selection(chart, chart_len, chart_lens, chart_len, prime_implicants, prime_implicants_len);
    size_t essential_prime_implicants_len = selection_len;
    puts("Essential Prime Implicants are:");
    puts(str_list_str(essential_prime_implicants, essential_prime_implicants_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
