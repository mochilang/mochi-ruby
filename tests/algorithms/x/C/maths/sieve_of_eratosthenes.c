// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prime_sieve_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long isqrt(long long n);
long long * prime_sieve(long long num);
int main(void);

long long isqrt(long long n) {
    long long r = 0LL;
    while (((r + 1LL) * (r + 1LL)) <= n) {
        r = r + 1LL;
    }
    return r;
}

long long * prime_sieve(long long num) {
    if (num <= 0LL) {
        panic("Invalid input, please enter a positive integer.");
    }
    long long *sieve = NULL;
    size_t sieve_len = 0;
    long long i = 0LL;
    while (i <= num) {
        sieve = list_append_long_long(sieve, &sieve_len, 1LL);
        i = i + 1LL;
    }
    long long *prime = NULL;
    size_t prime_len = 0;
    long long start = 2LL;
    long long end = isqrt(num);
    while (start <= end) {
        if (sieve[(int)({long long _mochi_idx = start; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
            prime = list_append_long_long(prime, &prime_len, start);
            long long j = start * start;
            while (j <= num) {
                if (sieve[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
                    sieve[(int)(j)] = 0LL;
                }
                j = j + start;
            }
        }
        start = start + 1LL;
    }
    long long k = end + 1LL;
    while (k <= num) {
        if (sieve[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
            prime = list_append_long_long(prime, &prime_len, k);
        }
        k = k + 1LL;
    }
    return prime_sieve_len = prime_len, prime;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp6 = prime_sieve(50LL); char *__res = str_list_int(__tmp6, prime_sieve_len); __res;}));
        puts(({long long *__tmp7 = prime_sieve(25LL); char *__res = str_list_int(__tmp7, prime_sieve_len); __res;}));
        puts(({long long *__tmp8 = prime_sieve(10LL); char *__res = str_list_int(__tmp8, prime_sieve_len); __res;}));
        puts(({long long *__tmp9 = prime_sieve(9LL); char *__res = str_list_int(__tmp9, prime_sieve_len); __res;}));
        puts(({long long *__tmp10 = prime_sieve(2LL); char *__res = str_list_int(__tmp10, prime_sieve_len); __res;}));
        puts(({long long *__tmp11 = prime_sieve(1LL); char *__res = str_list_int(__tmp11, prime_sieve_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
