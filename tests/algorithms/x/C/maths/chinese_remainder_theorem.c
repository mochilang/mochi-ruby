// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct EuclidResult EuclidResult;

struct EuclidResult {
    long long x;
    long long y;
};

EuclidResult e1;
EuclidResult e2;

EuclidResult extended_euclid(long long a, long long b);
long long chinese_remainder_theorem(long long n1, long long r1, long long n2, long long r2);
long long invert_modulo(long long a, long long n);
long long chinese_remainder_theorem2(long long n1, long long r1, long long n2, long long r2);
int main(void);

EuclidResult extended_euclid(long long a, long long b) {
    if (b == 0LL) {
        return (EuclidResult){.x = 1LL, .y = 0LL};
    }
    EuclidResult res = extended_euclid(b, a % b);
    long long k = a / b;
    return (EuclidResult){.x = res.y, .y = res.x - (k * res.y)};
}

long long chinese_remainder_theorem(long long n1, long long r1, long long n2, long long r2) {
    EuclidResult res = extended_euclid(n1, n2);
    long long x = res.x;
    long long y = res.y;
    long long m = n1 * n2;
    long long n = ((r2 * x) * n1) + ((r1 * y) * n2);
    return ((n % m) + m) % m;
}

long long invert_modulo(long long a, long long n) {
    EuclidResult res = extended_euclid(a, n);
    long long b = res.x;
    if (b < 0LL) {
        b = ((b % n) + n) % n;
    }
    return b;
}

long long chinese_remainder_theorem2(long long n1, long long r1, long long n2, long long r2) {
    long long x = invert_modulo(n1, n2);
    long long y = invert_modulo(n2, n1);
    long long m = n1 * n2;
    long long n = ((r2 * x) * n1) + ((r1 * y) * n2);
    return ((n % m) + m) % m;
}

int main(void) {
    {
        long long __start = _now();
        e1 = extended_euclid(10LL, 6LL);
        puts(str_concat(str_concat(str_int(e1.x), ","), str_int(e1.y)));
        e2 = extended_euclid(7LL, 5LL);
        puts(str_concat(str_concat(str_int(e2.x), ","), str_int(e2.y)));
        puts(str_int(chinese_remainder_theorem(5LL, 1LL, 7LL, 3LL)));
        puts(str_int(chinese_remainder_theorem(6LL, 1LL, 4LL, 3LL)));
        puts(str_int(invert_modulo(2LL, 5LL)));
        puts(str_int(invert_modulo(8LL, 7LL)));
        puts(str_int(chinese_remainder_theorem2(5LL, 1LL, 7LL, 3LL)));
        puts(str_int(chinese_remainder_theorem2(6LL, 1LL, 4LL, 3LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
