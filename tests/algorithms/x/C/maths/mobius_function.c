// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t primeFactors_len;

size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc(m->keys, m->cap * sizeof(int));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * primeFactors(long long n);
long long isSquareFree(long long * factors, size_t factors_len);
long long mobius(long long n);
int main(void);

long long * primeFactors(long long n) {
    long long i = 2LL;
    long long *factors = NULL;
    size_t factors_len = 0;
    while ((i * i) <= n) {
        if ((n % i) == 0LL) {
            factors = list_append_long_long(factors, &factors_len, i);
            n = n / i;
        } else {
            i = i + 1LL;
        }
    }
    if (n > 1LL) {
        factors = list_append_long_long(factors, &factors_len, n);
    }
    return primeFactors_len = factors_len, factors;
}

long long isSquareFree(long long * factors, size_t factors_len) {
    MapII seen = { NULL, NULL, 0, 0 };
    for (size_t __i = 0; __i < factors_len; __i++) {
        long long f = factors[__i];
        if (contains_map_int(seen.keys, seen.len, f)) {
            return 0LL;
        }
        map_set_ii(&seen, f, 1LL);
    }
    return 1LL;
}

long long mobius(long long n) {
    long long *factors = primeFactors(n);
    size_t factors_len = primeFactors_len;
    if (isSquareFree(factors, factors_len)) {
        return ((factors_len % 2LL) == 0LL ? 1LL : -1.0);
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", mobius(24LL));
        printf("%d\n", mobius(-1.0));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
