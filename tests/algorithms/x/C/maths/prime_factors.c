// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prime_factors_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * prime_factors(long long n);
long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len);
void test_prime_factors();
void user_main();
int main(void);

long long * prime_factors(long long n) {
    if (n < 2LL) {
        {
            long long* __tmp7_data = malloc(0 * sizeof(long long));
            prime_factors_len = 0;
            return __tmp7_data;
        }
    }
    long long num = n;
    long long i = 2LL;
    long long *factors = NULL;
    size_t factors_len = 0;
    while ((i * i) <= num) {
        if ((num % i) == 0LL) {
            factors = list_append_long_long(factors, &factors_len, i);
            num = num / i;
        } else {
            i = i + 1LL;
        }
    }
    if (num > 1LL) {
        factors = list_append_long_long(factors, &factors_len, num);
    }
    return prime_factors_len = factors_len, factors;
}

long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void test_prime_factors() {
    if (!(({long long* __tmp8 = prime_factors(0LL); list_eq(__tmp8, prime_factors_len, NULL, 0);}))) {
        panic("prime_factors(0) failed");
    }
    if (!(({long long* __tmp9 = prime_factors(100LL); list_eq(__tmp9, prime_factors_len, (long long[]){2LL, 2LL, 5LL, 5LL}, 4);}))) {
        panic("prime_factors(100) failed");
    }
    if (!(({long long* __tmp10 = prime_factors(2560LL); list_eq(__tmp10, prime_factors_len, (long long[]){2LL, 2LL, 2LL, 2LL, 2LL, 2LL, 2LL, 2LL, 2LL, 5LL}, 10);}))) {
        panic("prime_factors(2560) failed");
    }
    if (!(({long long* __tmp11 = prime_factors(97LL); list_eq(__tmp11, prime_factors_len, (long long[]){97LL}, 1);}))) {
        panic("prime_factors(97) failed");
    }
}

void user_main() {
    test_prime_factors();
    puts(({long long *__tmp12 = prime_factors(100LL); char *__res = str_list_int(__tmp12, prime_factors_len); __res;}));
    puts(({long long *__tmp13 = prime_factors(2560LL); char *__res = str_list_int(__tmp13, prime_factors_len); __res;}));
    puts(({long long *__tmp14 = prime_factors(97LL); char *__res = str_list_int(__tmp14, prime_factors_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
