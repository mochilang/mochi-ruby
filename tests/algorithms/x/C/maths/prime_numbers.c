// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t slow_primes_len;
size_t primes_len;
size_t fast_primes_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * slow_primes(long long max_n);
long long * primes(long long max_n);
long long * fast_primes(long long max_n);
int main(void);

long long * slow_primes(long long max_n) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 2LL;
    while (i <= max_n) {
        long long j = 2LL;
        long long is_prime = 1LL;
        while (j < i) {
            if ((i % j) == 0LL) {
                is_prime = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (is_prime) {
            result = list_append_long_long(result, &result_len, i);
        }
        i = i + 1LL;
    }
    return slow_primes_len = result_len, result;
}

long long * primes(long long max_n) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 2LL;
    while (i <= max_n) {
        long long j = 2LL;
        long long is_prime = 1LL;
        while ((j * j) <= i) {
            if ((i % j) == 0LL) {
                is_prime = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (is_prime) {
            result = list_append_long_long(result, &result_len, i);
        }
        i = i + 1LL;
    }
    return primes_len = result_len, result;
}

long long * fast_primes(long long max_n) {
    long long *result = NULL;
    size_t result_len = 0;
    if (max_n >= 2LL) {
        result = list_append_long_long(result, &result_len, 2LL);
    }
    long long i = 3LL;
    while (i <= max_n) {
        long long j = 3LL;
        long long is_prime = 1LL;
        while ((j * j) <= i) {
            if ((i % j) == 0LL) {
                is_prime = 0LL;
                break;
            }
            j = j + 2LL;
        }
        if (is_prime) {
            result = list_append_long_long(result, &result_len, i);
        }
        i = i + 2LL;
    }
    return fast_primes_len = result_len, result;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = slow_primes(25LL); char *__res = str_list_int(__tmp3, slow_primes_len); __res;}));
        puts(({long long *__tmp4 = primes(25LL); char *__res = str_list_int(__tmp4, primes_len); __res;}));
        puts(({long long *__tmp5 = fast_primes(25LL); char *__res = str_list_int(__tmp5, fast_primes_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
