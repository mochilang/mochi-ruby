// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long normalize_index(long long index, long long n);
double find_max_iterative(double * nums, size_t nums_len);
double find_max_recursive(double * nums, size_t nums_len, long long left, long long right);
void test_find_max();
void user_main();
int main(void);

long long normalize_index(long long index, long long n) {
    if (index < 0LL) {
        return n + index;
    }
    return index;
}

double find_max_iterative(double * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("find_max_iterative() arg is an empty sequence");
    }
    double max_num = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    long long i = 0LL;
    while (i < nums_len) {
        double x = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (x > max_num) {
            max_num = x;
        }
        i = i + 1LL;
    }
    return max_num;
}

double find_max_recursive(double * nums, size_t nums_len, long long left, long long right) {
    long long n = nums_len;
    if (n == 0LL) {
        panic("find_max_recursive() arg is an empty sequence");
    }
    if ((((left >= n) || (left < (0LL - n))) || (right >= n)) || (right < (0LL - n))) {
        panic("list index out of range");
    }
    long long l = normalize_index(left, n);
    long long r = normalize_index(right, n);
    if (l == r) {
        return nums[(int)({long long _mochi_idx = l; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    }
    long long mid = (l + r) / 2LL;
    double left_max = find_max_recursive(nums, nums_len, l, mid);
    double right_max = find_max_recursive(nums, nums_len, mid + 1LL, r);
    if (left_max >= right_max) {
        return left_max;
    }
    return right_max;
}

void test_find_max() {
    double *arr = NULL;
    size_t arr_len = 0;
    arr = list_append_double(arr, &arr_len, 2.0);
    arr = list_append_double(arr, &arr_len, 4.0);
    arr = list_append_double(arr, &arr_len, 9.0);
    arr = list_append_double(arr, &arr_len, 7.0);
    arr = list_append_double(arr, &arr_len, 19.0);
    arr = list_append_double(arr, &arr_len, 94.0);
    arr = list_append_double(arr, &arr_len, 5.0);
    if (find_max_iterative(arr, arr_len) != 94.0) {
        panic("find_max_iterative failed");
    }
    if (find_max_recursive(arr, arr_len, 0LL, 6LL) != 94.0) {
        panic("find_max_recursive failed");
    }
    if (find_max_recursive(arr, arr_len, -7LL, -1LL) != 94.0) {
        panic("negative index handling failed");
    }
}

void user_main() {
    test_find_max();
    double *nums = NULL;
    size_t nums_len = 0;
    nums = list_append_double(nums, &nums_len, 2.0);
    nums = list_append_double(nums, &nums_len, 4.0);
    nums = list_append_double(nums, &nums_len, 9.0);
    nums = list_append_double(nums, &nums_len, 7.0);
    nums = list_append_double(nums, &nums_len, 19.0);
    nums = list_append_double(nums, &nums_len, 94.0);
    nums = list_append_double(nums, &nums_len, 5.0);
    printf("%g\n", find_max_iterative(nums, nums_len));
    printf("%g\n", find_max_recursive(nums, nums_len, 0LL, 6LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
