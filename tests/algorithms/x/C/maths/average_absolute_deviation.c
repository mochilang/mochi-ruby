// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double abs_float(double x);
double average_absolute_deviation(long long * nums, size_t nums_len);
int main(void);

double abs_float(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double average_absolute_deviation(long long * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("List is empty");
    }
    long long sum = 0LL;
    for (size_t __i = 0; __i < nums_len; __i++) {
        long long x = nums[__i];
        sum = sum + x;
    }
    double n = (double)(nums_len);
    double mean = (double)(sum) / n;
    double dev_sum = 0.0;
    for (size_t __i = 0; __i < nums_len; __i++) {
        long long x = nums[__i];
        dev_sum = dev_sum + abs_float((double)(x) - mean);
    }
    return dev_sum / n;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(average_absolute_deviation(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 0LL; tmp;}), 1)));
        puts(str_float(average_absolute_deviation(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 1LL; tmp[2] = 3LL; tmp[3] = 2LL; tmp;}), 4)));
        puts(str_float(average_absolute_deviation(({long long *tmp = malloc(8 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 70LL; tmp[2] = 6LL; tmp[3] = 50LL; tmp[4] = 20LL; tmp[5] = 8LL; tmp[6] = 4LL; tmp[7] = 0LL; tmp;}), 8)));
        puts(str_float(average_absolute_deviation(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = -20LL; tmp[1] = 0LL; tmp[2] = 30LL; tmp[3] = 15LL; tmp;}), 4)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
