// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double abs_val(double num);
long long abs_min(long long * x, size_t x_len);
long long abs_max(long long * x, size_t x_len);
long long abs_max_sort(long long * x, size_t x_len);
void test_abs_val();
void user_main();
int main(void);

double abs_val(double num) {
    if (num < 0.0) {
        return -(num);
    }
    return num;
}

long long abs_min(long long * x, size_t x_len) {
    if (x_len == 0LL) {
        panic("abs_min() arg is an empty sequence");
    }
    long long j = x[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})];
    long long idx = 0LL;
    while (idx < x_len) {
        long long i = x[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})];
        if (abs_val((double)(i)) < abs_val((double)(j))) {
            j = i;
        }
        idx = idx + 1LL;
    }
    return j;
}

long long abs_max(long long * x, size_t x_len) {
    if (x_len == 0LL) {
        panic("abs_max() arg is an empty sequence");
    }
    long long j = x[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})];
    long long idx = 0LL;
    while (idx < x_len) {
        long long i = x[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})];
        if (abs_val((double)(i)) > abs_val((double)(j))) {
            j = i;
        }
        idx = idx + 1LL;
    }
    return j;
}

long long abs_max_sort(long long * x, size_t x_len) {
    if (x_len == 0LL) {
        panic("abs_max_sort() arg is an empty sequence");
    }
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < x_len) {
        arr = list_append_long_long(arr, &arr_len, x[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long n = arr_len;
    long long a = 0LL;
    while (a < n) {
        long long b = 0LL;
        while (b < ((n - a) - 1LL)) {
            if (abs_val((double)(arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})])) > abs_val((double)(arr[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]))) {
                long long temp = arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(b)] = arr[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(b + 1LL)] = temp;
            }
            b = b + 1LL;
        }
        a = a + 1LL;
    }
    return arr[(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
}

void test_abs_val() {
    if (abs_val(0.0) != 0.0) {
        panic("abs_val(0) failed");
    }
    if (abs_val(34.0) != 34.0) {
        panic("abs_val(34) failed");
    }
    if (abs_val(-(100000000000.0)) != 100000000000.0) {
        panic("abs_val large failed");
    }
    long long *a = NULL;
    size_t a_len = 0;
    a = list_append_long_long(a, &a_len, -3LL);
    a = list_append_long_long(a, &a_len, -1LL);
    a = list_append_long_long(a, &a_len, 2LL);
    a = list_append_long_long(a, &a_len, -11LL);
    if (abs_max(a, a_len) != -11LL) {
        panic("abs_max failed");
    }
    if (abs_max_sort(a, a_len) != -11LL) {
        panic("abs_max_sort failed");
    }
    if (abs_min(a, a_len) != -1LL) {
        panic("abs_min failed");
    }
}

void user_main() {
    test_abs_val();
    printf("%g\n", abs_val(-(34.0)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
