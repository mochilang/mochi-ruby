// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double mean(double * nums, size_t nums_len);
int main(void);

double mean(double * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("List is empty");
    }
    double total = 0.0;
    long long i = 0LL;
    while (i < nums_len) {
        total = total + nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return total / (double)(nums_len);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(mean(({double *tmp = malloc(7 * sizeof(double)); tmp[0] = 3.0; tmp[1] = 6.0; tmp[2] = 9.0; tmp[3] = 12.0; tmp[4] = 15.0; tmp[5] = 18.0; tmp[6] = 21.0; tmp;}), 7)));
        puts(str_float(mean(({double *tmp = malloc(7 * sizeof(double)); tmp[0] = 5.0; tmp[1] = 10.0; tmp[2] = 15.0; tmp[3] = 20.0; tmp[4] = 25.0; tmp[5] = 30.0; tmp[6] = 35.0; tmp;}), 7)));
        puts(str_float(mean(({long long *tmp = malloc(8 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 2.0; tmp[2] = 3.0; tmp[3] = 4.0; tmp[4] = 5.0; tmp[5] = 6.0; tmp[6] = 7.0; tmp[7] = 8.0; tmp;}), 8)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
