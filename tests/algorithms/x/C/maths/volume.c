// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;
double SQRT5 = 2.23606797749979;

double minf(double a, double b);
double maxf(double a, double b);
double vol_cube(double side_length);
double vol_spherical_cap(double height, double radius);
double vol_sphere(double radius);
double vol_spheres_intersect(double radius_1, double radius_2, double centers_distance);
double vol_spheres_union(double radius_1, double radius_2, double centers_distance);
double vol_cuboid(double width, double height, double length);
double vol_cone(double area_of_base, double height);
double vol_right_circ_cone(double radius, double height);
double vol_prism(double area_of_base, double height);
double vol_pyramid(double area_of_base, double height);
double vol_hemisphere(double radius);
double vol_circular_cylinder(double radius, double height);
double vol_hollow_circular_cylinder(double inner_radius, double outer_radius, double height);
double vol_conical_frustum(double height, double radius_1, double radius_2);
double vol_torus(double torus_radius, double tube_radius);
double vol_icosahedron(double tri_side);
void user_main();
int main(void);

double minf(double a, double b) {
    if (a < b) {
        return a;
    }
    return b;
}

double maxf(double a, double b) {
    if (a > b) {
        return a;
    }
    return b;
}

double vol_cube(double side_length) {
    if (side_length < 0.0) {
        panic("vol_cube() only accepts non-negative values");
    }
    return (side_length * side_length) * side_length;
}

double vol_spherical_cap(double height, double radius) {
    if ((height < 0.0) || (radius < 0.0)) {
        panic("vol_spherical_cap() only accepts non-negative values");
    }
    return ((((1.0 / 3.0) * PI) * height) * height) * ((3.0 * radius) - height);
}

double vol_sphere(double radius) {
    if (radius < 0.0) {
        panic("vol_sphere() only accepts non-negative values");
    }
    return ((((4.0 / 3.0) * PI) * radius) * radius) * radius;
}

double vol_spheres_intersect(double radius_1, double radius_2, double centers_distance) {
    if (((radius_1 < 0.0) || (radius_2 < 0.0)) || (centers_distance < 0.0)) {
        panic("vol_spheres_intersect() only accepts non-negative values");
    }
    if (centers_distance == 0.0) {
        return vol_sphere(minf(radius_1, radius_2));
    }
    double h1 = (((radius_1 - radius_2) + centers_distance) * ((radius_1 + radius_2) - centers_distance)) / (2.0 * centers_distance);
    double h2 = (((radius_2 - radius_1) + centers_distance) * ((radius_2 + radius_1) - centers_distance)) / (2.0 * centers_distance);
    return vol_spherical_cap(h1, radius_2) + vol_spherical_cap(h2, radius_1);
}

double vol_spheres_union(double radius_1, double radius_2, double centers_distance) {
    if (((radius_1 <= 0.0) || (radius_2 <= 0.0)) || (centers_distance < 0.0)) {
        panic("vol_spheres_union() only accepts non-negative values, non-zero radius");
    }
    if (centers_distance == 0.0) {
        return vol_sphere(maxf(radius_1, radius_2));
    }
    return (vol_sphere(radius_1) + vol_sphere(radius_2)) - vol_spheres_intersect(radius_1, radius_2, centers_distance);
}

double vol_cuboid(double width, double height, double length) {
    if (((width < 0.0) || (height < 0.0)) || (length < 0.0)) {
        panic("vol_cuboid() only accepts non-negative values");
    }
    return (width * height) * length;
}

double vol_cone(double area_of_base, double height) {
    if ((height < 0.0) || (area_of_base < 0.0)) {
        panic("vol_cone() only accepts non-negative values");
    }
    return (area_of_base * height) / 3.0;
}

double vol_right_circ_cone(double radius, double height) {
    if ((height < 0.0) || (radius < 0.0)) {
        panic("vol_right_circ_cone() only accepts non-negative values");
    }
    return (((PI * radius) * radius) * height) / 3.0;
}

double vol_prism(double area_of_base, double height) {
    if ((height < 0.0) || (area_of_base < 0.0)) {
        panic("vol_prism() only accepts non-negative values");
    }
    return area_of_base * height;
}

double vol_pyramid(double area_of_base, double height) {
    if ((height < 0.0) || (area_of_base < 0.0)) {
        panic("vol_pyramid() only accepts non-negative values");
    }
    return (area_of_base * height) / 3.0;
}

double vol_hemisphere(double radius) {
    if (radius < 0.0) {
        panic("vol_hemisphere() only accepts non-negative values");
    }
    return ((((radius * radius) * radius) * PI) * 2.0) / 3.0;
}

double vol_circular_cylinder(double radius, double height) {
    if ((height < 0.0) || (radius < 0.0)) {
        panic("vol_circular_cylinder() only accepts non-negative values");
    }
    return ((radius * radius) * height) * PI;
}

double vol_hollow_circular_cylinder(double inner_radius, double outer_radius, double height) {
    if (((inner_radius < 0.0) || (outer_radius < 0.0)) || (height < 0.0)) {
        panic("vol_hollow_circular_cylinder() only accepts non-negative values");
    }
    if (outer_radius <= inner_radius) {
        panic("outer_radius must be greater than inner_radius");
    }
    return (PI * ((outer_radius * outer_radius) - (inner_radius * inner_radius))) * height;
}

double vol_conical_frustum(double height, double radius_1, double radius_2) {
    if (((radius_1 < 0.0) || (radius_2 < 0.0)) || (height < 0.0)) {
        panic("vol_conical_frustum() only accepts non-negative values");
    }
    return (((1.0 / 3.0) * PI) * height) * (((radius_1 * radius_1) + (radius_2 * radius_2)) + (radius_1 * radius_2));
}

double vol_torus(double torus_radius, double tube_radius) {
    if ((torus_radius < 0.0) || (tube_radius < 0.0)) {
        panic("vol_torus() only accepts non-negative values");
    }
    return ((((2.0 * PI) * PI) * torus_radius) * tube_radius) * tube_radius;
}

double vol_icosahedron(double tri_side) {
    if (tri_side < 0.0) {
        panic("vol_icosahedron() only accepts non-negative values");
    }
    return ((((tri_side * tri_side) * tri_side) * (3.0 + SQRT5)) * 5.0) / 12.0;
}

void user_main() {
    puts("Volumes:");
    puts(str_concat("Cube: ", str_float(vol_cube(2.0))));
    puts(str_concat("Cuboid: ", str_float(vol_cuboid(2.0, 2.0, 2.0))));
    puts(str_concat("Cone: ", str_float(vol_cone(2.0, 2.0))));
    puts(str_concat("Right Circular Cone: ", str_float(vol_right_circ_cone(2.0, 2.0))));
    puts(str_concat("Prism: ", str_float(vol_prism(2.0, 2.0))));
    puts(str_concat("Pyramid: ", str_float(vol_pyramid(2.0, 2.0))));
    puts(str_concat("Sphere: ", str_float(vol_sphere(2.0))));
    puts(str_concat("Hemisphere: ", str_float(vol_hemisphere(2.0))));
    puts(str_concat("Circular Cylinder: ", str_float(vol_circular_cylinder(2.0, 2.0))));
    puts(str_concat("Torus: ", str_float(vol_torus(2.0, 2.0))));
    puts(str_concat("Conical Frustum: ", str_float(vol_conical_frustum(2.0, 2.0, 4.0))));
    puts(str_concat("Spherical cap: ", str_float(vol_spherical_cap(1.0, 2.0))));
    puts(str_concat("Spheres intersection: ", str_float(vol_spheres_intersect(2.0, 2.0, 1.0))));
    puts(str_concat("Spheres union: ", str_float(vol_spheres_union(2.0, 2.0, 1.0))));
    puts(str_concat("Hollow Circular Cylinder: ", str_float(vol_hollow_circular_cylinder(1.0, 2.0, 3.0))));
    puts(str_concat("Icosahedron: ", str_float(vol_icosahedron(2.5))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
