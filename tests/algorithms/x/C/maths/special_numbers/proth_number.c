// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long pow2(long long exp);
long long proth(long long number);
void user_main();
int main(void);

long long pow2(long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * 2LL;
        i = i + 1LL;
    }
    return result;
}

long long proth(long long number) {
    if (number < 1LL) {
        panic("Input value must be > 0");
    }
    if (number == 1LL) {
        return 3LL;
    }
    if (number == 2LL) {
        return 5LL;
    }
    long long temp = number / 3LL;
    long long pow = 1LL;
    long long block_index = 1LL;
    while (pow <= temp) {
        pow = pow * 2LL;
        block_index = block_index + 1LL;
    }
    long long *proth_list = NULL;
    size_t proth_list_len = 0;
    proth_list = list_append_long_long(proth_list, &proth_list_len, 3LL);
    proth_list = list_append_long_long(proth_list, &proth_list_len, 5LL);
    long long proth_index = 2LL;
    long long increment = 3LL;
    long long block = 1LL;
    while (block < block_index) {
        long long i = 0LL;
        while (i < increment) {
            long long next_val = pow2(block + 1LL) + proth_list[(int)({long long _mochi_idx = proth_index - 1LL; _mochi_idx < 0 ? proth_list_len + _mochi_idx : _mochi_idx;})];
            proth_list = list_append_long_long(proth_list, &proth_list_len, next_val);
            proth_index = proth_index + 1LL;
            i = i + 1LL;
        }
        increment = increment * 2LL;
        block = block + 1LL;
    }
    return proth_list[(int)({long long _mochi_idx = number - 1LL; _mochi_idx < 0 ? proth_list_len + _mochi_idx : _mochi_idx;})];
}

void user_main() {
    long long n = 1LL;
    while (n <= 10LL) {
        long long value = proth(n);
        puts(str_concat(str_concat(str_concat("The ", str_int(n)), "th Proth number: "), str_int(value)));
        n = n + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
