// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long perfect(long long n);
void user_main();
int main(void);

long long perfect(long long n) {
    if (n <= 0LL) {
        return 0LL;
    }
    long long limit = n / 2LL;
    long long sum = 0LL;
    long long i = 1LL;
    while (i <= limit) {
        if ((n % i) == 0LL) {
            sum = sum + i;
        }
        i = i + 1LL;
    }
    return sum == n;
}

void user_main() {
    long long *numbers = NULL;
    size_t numbers_len = 0;
    numbers = list_append_long_long(numbers, &numbers_len, 6LL);
    numbers = list_append_long_long(numbers, &numbers_len, 28LL);
    numbers = list_append_long_long(numbers, &numbers_len, 29LL);
    numbers = list_append_long_long(numbers, &numbers_len, 12LL);
    numbers = list_append_long_long(numbers, &numbers_len, 496LL);
    numbers = list_append_long_long(numbers, &numbers_len, 8128LL);
    numbers = list_append_long_long(numbers, &numbers_len, 0LL);
    numbers = list_append_long_long(numbers, &numbers_len, -1LL);
    long long idx = 0LL;
    while (idx < 8LL) {
        long long num = numbers[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? numbers_len + _mochi_idx : _mochi_idx;})];
        if (perfect(num)) {
            puts(str_concat(str_int(num), " is a Perfect Number."));
        } else {
            puts(str_concat(str_int(num), " is not a Perfect Number."));
        }
        idx = idx + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
