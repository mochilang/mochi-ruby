// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t harshad_numbers_in_base_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* user_panic(const char* msg);
long long char_to_value(const char* c);
const char* int_to_base(long long number, long long base);
long long base_to_int(const char* num_str, long long base);
const char* sum_of_digits(long long num, long long base);
const char* * harshad_numbers_in_base(long long limit, long long base);
long long is_harshad_number_in_base(long long num, long long base);
void user_main();
int main(void);

const char* user_panic(const char* msg) {
}

long long char_to_value(const char* c) {
    const char* digits = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    long long i = 0LL;
    while (i < strlen(digits)) {
        if (strcmp((const char[]){digits[i], 0}, c) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    user_panic("invalid digit");
}

const char* int_to_base(long long number, long long base) {
    if ((base < 2LL) || (base > 36LL)) {
        user_panic("'base' must be between 2 and 36 inclusive");
    }
    if (number < 0LL) {
        user_panic("number must be a positive integer");
    }
    const char* digits = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    long long n = number;
    const char* result = "";
    while (n > 0LL) {
        long long remainder = n % base;
        result = str_concat((const char[]){digits[remainder], 0}, result);
        n = n / base;
    }
    if (strcmp(result, "") == 0) {
        result = "0";
    }
    return result;
}

long long base_to_int(const char* num_str, long long base) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(num_str)) {
        const char* c = (const char[]){num_str[i], 0};
        value = (value * base) + char_to_value(c);
        i = i + 1LL;
    }
    return value;
}

const char* sum_of_digits(long long num, long long base) {
    if ((base < 2LL) || (base > 36LL)) {
        user_panic("'base' must be between 2 and 36 inclusive");
    }
    const char* num_str = int_to_base(num, base);
    long long total = 0LL;
    long long i = 0LL;
    while (i < strlen(num_str)) {
        const char* c = (const char[]){num_str[i], 0};
        total = total + char_to_value(c);
        i = i + 1LL;
    }
    return int_to_base(total, base);
}

const char* * harshad_numbers_in_base(long long limit, long long base) {
    if ((base < 2LL) || (base > 36LL)) {
        user_panic("'base' must be between 2 and 36 inclusive");
    }
    if (limit < 0LL) {
        {
            const char** __tmp3_data = malloc(0 * sizeof(const char*));
            harshad_numbers_in_base_len = 0;
            return __tmp3_data;
        }
    }
    const char* *numbers = NULL;
    size_t numbers_len = 0;
    long long i = 1LL;
    while (i < limit) {
        const char* s = sum_of_digits(i, base);
        long long divisor = base_to_int(s, base);
        if ((i % divisor) == 0LL) {
            numbers = list_append_str(numbers, &numbers_len, int_to_base(i, base));
        }
        i = i + 1LL;
    }
    return harshad_numbers_in_base_len = numbers_len, numbers;
}

long long is_harshad_number_in_base(long long num, long long base) {
    if ((base < 2LL) || (base > 36LL)) {
        user_panic("'base' must be between 2 and 36 inclusive");
    }
    if (num < 0LL) {
        return 0LL;
    }
    const char* n = int_to_base(num, base);
    const char* d = sum_of_digits(num, base);
    long long n_val = base_to_int(n, base);
    long long d_val = base_to_int(d, base);
    return (n_val % d_val) == 0LL;
}

void user_main() {
    puts(int_to_base(0LL, 21LL));
    puts(int_to_base(23LL, 2LL));
    puts(int_to_base(58LL, 5LL));
    puts(int_to_base(167LL, 16LL));
    puts(sum_of_digits(103LL, 12LL));
    puts(sum_of_digits(1275LL, 4LL));
    puts(sum_of_digits(6645LL, 2LL));
    const char** __tmp4 = harshad_numbers_in_base(15LL, 2LL);
    puts(str_list_str(__tmp4, harshad_numbers_in_base_len));
    const char** __tmp5 = harshad_numbers_in_base(12LL, 34LL);
    puts(str_list_str(__tmp5, harshad_numbers_in_base_len));
    const char** __tmp6 = harshad_numbers_in_base(12LL, 4LL);
    puts(str_list_str(__tmp6, harshad_numbers_in_base_len));
    printf("%d\n", is_harshad_number_in_base(18LL, 10LL));
    printf("%d\n", is_harshad_number_in_base(21LL, 10LL));
    printf("%d\n", is_harshad_number_in_base(-21LL, 5LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
