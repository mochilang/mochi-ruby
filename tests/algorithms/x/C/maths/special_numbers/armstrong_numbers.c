// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long pow_int(long long base, long long exp);
long long armstrong_number(long long n);
long long pluperfect_number(long long n);
long long narcissistic_number(long long n);
int main(void);

long long pow_int(long long base, long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

long long armstrong_number(long long n) {
    if (n < 1LL) {
        return 0LL;
    }
    long long digits = 0LL;
    long long temp = n;
    while (temp > 0LL) {
        temp = temp / 10LL;
        digits = digits + 1LL;
    }
    long long total = 0LL;
    temp = n;
    while (temp > 0LL) {
        long long rem = temp % 10LL;
        total = total + pow_int(rem, digits);
        temp = temp / 10LL;
    }
    return total == n;
}

long long pluperfect_number(long long n) {
    if (n < 1LL) {
        return 0LL;
    }
    long long *digit_histogram = NULL;
    size_t digit_histogram_len = 0;
    long long i = 0LL;
    while (i < 10LL) {
        digit_histogram = list_append_long_long(digit_histogram, &digit_histogram_len, 0LL);
        i = i + 1LL;
    }
    long long digit_total = 0LL;
    long long temp = n;
    while (temp > 0LL) {
        long long rem = temp % 10LL;
        digit_histogram[(int)(rem)] = digit_histogram[(int)({long long _mochi_idx = rem; _mochi_idx < 0 ? digit_histogram_len + _mochi_idx : _mochi_idx;})] + 1LL;
        digit_total = digit_total + 1LL;
        temp = temp / 10LL;
    }
    long long total = 0LL;
    i = 0LL;
    while (i < 10LL) {
        if (digit_histogram[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? digit_histogram_len + _mochi_idx : _mochi_idx;})] > 0LL) {
            total = total + (digit_histogram[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? digit_histogram_len + _mochi_idx : _mochi_idx;})] * pow_int(i, digit_total));
        }
        i = i + 1LL;
    }
    return total == n;
}

long long narcissistic_number(long long n) {
    if (n < 1LL) {
        return 0LL;
    }
    long long digits = 0LL;
    long long temp = n;
    while (temp > 0LL) {
        temp = temp / 10LL;
        digits = digits + 1LL;
    }
    temp = n;
    long long total = 0LL;
    while (temp > 0LL) {
        long long rem = temp % 10LL;
        total = total + pow_int(rem, digits);
        temp = temp / 10LL;
    }
    return total == n;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", armstrong_number(371LL));
        printf("%d\n", armstrong_number(200LL));
        printf("%d\n", pluperfect_number(371LL));
        printf("%d\n", pluperfect_number(200LL));
        printf("%d\n", narcissistic_number(371LL));
        printf("%d\n", narcissistic_number(200LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
