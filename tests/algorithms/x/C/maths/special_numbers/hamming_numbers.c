// Generated by Mochi 0.10.32 on 2025-08-17 13:41 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t hamming_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * hamming(long long n);
int main(void);

long long * hamming(long long n) {
    if (n < 1LL) {
        panic("n_element should be a positive number");
    }
    long long *hamming_list = NULL;
    size_t hamming_list_len = 0;
    hamming_list = list_append_long_long(hamming_list, &hamming_list_len, 1LL);
    long long i = 0LL;
    long long j = 0LL;
    long long k = 0LL;
    long long index = 1LL;
    while (index < n) {
        while ((hamming_list[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})] * 2LL) <= hamming_list[(int)({long long _mochi_idx = hamming_list_len - 1LL; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})]) {
            i = i + 1LL;
        }
        while ((hamming_list[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})] * 3LL) <= hamming_list[(int)({long long _mochi_idx = hamming_list_len - 1LL; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})]) {
            j = j + 1LL;
        }
        while ((hamming_list[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})] * 5LL) <= hamming_list[(int)({long long _mochi_idx = hamming_list_len - 1LL; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})]) {
            k = k + 1LL;
        }
        long long m1 = hamming_list[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})] * 2LL;
        long long m2 = hamming_list[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})] * 3LL;
        long long m3 = hamming_list[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? hamming_list_len + _mochi_idx : _mochi_idx;})] * 5LL;
        long long next = m1;
        if (m2 < next) {
            next = m2;
        }
        if (m3 < next) {
            next = m3;
        }
        hamming_list = list_append_long_long(hamming_list, &hamming_list_len, next);
        index = index + 1LL;
    }
    return hamming_len = hamming_list_len, hamming_list;
}

int main(void) {
    {
        long long __start = _now();
        long long* __tmp3 = hamming(5LL);
        puts(str_list_int(__tmp3, hamming_len));
        long long* __tmp4 = hamming(10LL);
        puts(str_list_int(__tmp4, hamming_len));
        long long* __tmp5 = hamming(15LL);
        puts(str_list_int(__tmp5, hamming_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
