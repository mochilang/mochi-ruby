// Generated by Mochi 0.10.32 on 2025-08-17 20:26 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bubble_sort_len;
size_t factors_len;

size_t append_len;
static int list_eq_int(const long long a[], size_t alen, const long long b[], size_t blen) {
    if (alen != blen) return 0;
    for (size_t i = 0; i < alen; i++) {
        if (a[i] != b[i]) return 0;
    }
    return 1;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * bubble_sort(long long * xs, size_t xs_len);
long long * factors(long long num);
long long sum_list(long long * xs, size_t xs_len);
long long abundant(long long n);
long long semi_perfect(long long number);
long long weird(long long number);
void run_tests();
void user_main();
int main(void);

long long * bubble_sort(long long * xs, size_t xs_len) {
    long long *arr = xs;
    size_t arr_len = xs_len;
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j < ((n - i) - 1LL)) {
            if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bubble_sort_len = arr_len, arr;
}

long long * factors(long long num) {
    long long *values = NULL;
    size_t values_len = 0;
    values = list_append_long_long(values, &values_len, 1LL);
    long long i = 2LL;
    while ((i * i) <= num) {
        if ((num % i) == 0LL) {
            values = list_append_long_long(values, &values_len, i);
            long long d = num / i;
            if (d != i) {
                values = list_append_long_long(values, &values_len, d);
            }
        }
        i = i + 1LL;
    }
    {
        long long* __ret13 = bubble_sort(values, values_len);
        factors_len = bubble_sort_len;
        return __ret13;
    }
}

long long sum_list(long long * xs, size_t xs_len) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < xs_len) {
        total = total + xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return total;
}

long long abundant(long long n) {
    return ({long long* __tmp14 = factors(n); size_t __tmp14_len = factors_len; sum_list(__tmp14, __tmp14_len);}) > n;
}

long long semi_perfect(long long number) {
    if (number <= 0LL) {
        return 1LL;
    }
    long long *values = factors(number);
    size_t values_len = factors_len;
    long long *possible = NULL;
    size_t possible_len = 0;
    long long j = 0LL;
    while (j <= number) {
        possible = list_append_long_long(possible, &possible_len, j == 0LL);
        j = j + 1LL;
    }
    long long idx = 0LL;
    while (idx < values_len) {
        long long v = values[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})];
        long long s = number;
        while (s >= v) {
            if (possible[(int)({long long _mochi_idx = s - v; _mochi_idx < 0 ? possible_len + _mochi_idx : _mochi_idx;})]) {
                possible[(int)(s)] = 1LL;
            }
            s = s - 1LL;
        }
        idx = idx + 1LL;
    }
    return possible[(int)({long long _mochi_idx = number; _mochi_idx < 0 ? possible_len + _mochi_idx : _mochi_idx;})];
}

long long weird(long long number) {
    return abundant(number) && (semi_perfect(number) == 0LL);
}

void run_tests() {
    if (({long long* __tmp15 = factors(12LL); size_t __tmp16 = factors_len; long long* __tmp17 = (long long[]){1LL, 2LL, 3LL, 4LL, 6LL}; size_t __tmp18 = 5; list_eq_int(__tmp15, __tmp16, __tmp17, __tmp18);}) == 0) {
        panic("factors 12 failed");
    }
    if (({long long* __tmp19 = factors(1LL); size_t __tmp20 = factors_len; long long* __tmp21 = (long long[]){1LL}; size_t __tmp22 = 1; list_eq_int(__tmp19, __tmp20, __tmp21, __tmp22);}) == 0) {
        panic("factors 1 failed");
    }
    if (({long long* __tmp23 = factors(100LL); size_t __tmp24 = factors_len; long long* __tmp25 = (long long[]){1LL, 2LL, 4LL, 5LL, 10LL, 20LL, 25LL, 50LL}; size_t __tmp26 = 8; list_eq_int(__tmp23, __tmp24, __tmp25, __tmp26);}) == 0) {
        panic("factors 100 failed");
    }
    if (abundant(0LL) != 1LL) {
        panic("abundant 0 failed");
    }
    if (abundant(1LL) != 0LL) {
        panic("abundant 1 failed");
    }
    if (abundant(12LL) != 1LL) {
        panic("abundant 12 failed");
    }
    if (abundant(13LL) != 0LL) {
        panic("abundant 13 failed");
    }
    if (abundant(20LL) != 1LL) {
        panic("abundant 20 failed");
    }
    if (semi_perfect(0LL) != 1LL) {
        panic("semi_perfect 0 failed");
    }
    if (semi_perfect(1LL) != 1LL) {
        panic("semi_perfect 1 failed");
    }
    if (semi_perfect(12LL) != 1LL) {
        panic("semi_perfect 12 failed");
    }
    if (semi_perfect(13LL) != 0LL) {
        panic("semi_perfect 13 failed");
    }
    if (weird(0LL) != 0LL) {
        panic("weird 0 failed");
    }
    if (weird(70LL) != 1LL) {
        panic("weird 70 failed");
    }
    if (weird(77LL) != 0LL) {
        panic("weird 77 failed");
    }
}

void user_main() {
    run_tests();
    long long *nums = NULL;
    size_t nums_len = 0;
    nums = list_append_long_long(nums, &nums_len, 69LL);
    nums = list_append_long_long(nums, &nums_len, 70LL);
    nums = list_append_long_long(nums, &nums_len, 71LL);
    long long i = 0LL;
    while (i < 3LL) {
        long long n = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (weird(n)) {
            puts(str_concat(str_int(n), " is weird."));
        } else {
            puts(str_concat(str_int(n), " is not weird."));
        }
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
