// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bell_numbers_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long binomial_coefficient(long long total_elements, long long elements_to_choose);
long long * bell_numbers(long long max_set_length);
void user_main();
int main(void);

long long binomial_coefficient(long long total_elements, long long elements_to_choose) {
    if ((elements_to_choose == 0LL) || (elements_to_choose == total_elements)) {
        return 1LL;
    }
    long long k = elements_to_choose;
    if (k > (total_elements - k)) {
        k = total_elements - k;
    }
    long long coefficient = 1LL;
    long long i = 0LL;
    while (i < k) {
        coefficient = coefficient * (total_elements - i);
        coefficient = coefficient / (i + 1LL);
        i = i + 1LL;
    }
    return coefficient;
}

long long * bell_numbers(long long max_set_length) {
    if (max_set_length < 0LL) {
        panic("max_set_length must be non-negative");
    }
    long long *bell = NULL;
    size_t bell_len = 0;
    long long i = 0LL;
    while (i <= max_set_length) {
        bell = list_append_long_long(bell, &bell_len, 0LL);
        i = i + 1LL;
    }
    bell[(int)(0LL)] = 1LL;
    i = 1LL;
    while (i <= max_set_length) {
        long long j = 0LL;
        while (j < i) {
            bell[(int)(i)] = bell[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? bell_len + _mochi_idx : _mochi_idx;})] + (binomial_coefficient(i - 1LL, j) * bell[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? bell_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bell_numbers_len = bell_len, bell;
}

void user_main() {
    puts(({long long *__tmp1 = bell_numbers(5LL); char *__res = str_list_int(__tmp1, bell_numbers_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
