// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long ugly_numbers(long long n);
int main(void);

long long ugly_numbers(long long n) {
    if (n <= 0LL) {
        return 1LL;
    }
    long long *ugly_nums = NULL;
    size_t ugly_nums_len = 0;
    ugly_nums = list_append_long_long(ugly_nums, &ugly_nums_len, 1LL);
    long long i2 = 0LL;
    long long i3 = 0LL;
    long long i5 = 0LL;
    long long next_2 = 2LL;
    long long next_3 = 3LL;
    long long next_5 = 5LL;
    long long count_ = 1LL;
    while (count_ < n) {
        long long next_num;
        if (next_2 < next_3) {
            next_num = (next_2 < next_5 ? next_2 : next_5);
        } else {
            next_num = (next_3 < next_5 ? next_3 : next_5);
        }
        ugly_nums = list_append_long_long(ugly_nums, &ugly_nums_len, next_num);
        if (next_num == next_2) {
            i2 = i2 + 1LL;
            next_2 = ugly_nums[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? ugly_nums_len + _mochi_idx : _mochi_idx;})] * 2LL;
        }
        if (next_num == next_3) {
            i3 = i3 + 1LL;
            next_3 = ugly_nums[(int)({long long _mochi_idx = i3; _mochi_idx < 0 ? ugly_nums_len + _mochi_idx : _mochi_idx;})] * 3LL;
        }
        if (next_num == next_5) {
            i5 = i5 + 1LL;
            next_5 = ugly_nums[(int)({long long _mochi_idx = i5; _mochi_idx < 0 ? ugly_nums_len + _mochi_idx : _mochi_idx;})] * 5LL;
        }
        count_ = count_ + 1LL;
    }
    return ugly_nums[(int)({long long _mochi_idx = ugly_nums_len - 1LL; _mochi_idx < 0 ? ugly_nums_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(ugly_numbers(100LL)));
        puts(str_int(ugly_numbers(0LL)));
        puts(str_int(ugly_numbers(20LL)));
        puts(str_int(ugly_numbers(-5LL)));
        puts(str_int(ugly_numbers(200LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
