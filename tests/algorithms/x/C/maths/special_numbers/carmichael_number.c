// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long abs_int(long long x);
long long gcd(long long a, long long b);
long long power(long long x, long long y, long long m);
long long is_carmichael_number(long long n);
int main(void);

long long abs_int(long long x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long gcd(long long a, long long b) {
    if (a == 0LL) {
        return abs_int(b);
    }
    return gcd(b % a, a);
}

long long power(long long x, long long y, long long m) {
    if (y == 0LL) {
        return 1LL % m;
    }
    long long temp = power(x, y / 2LL, m) % m;
    temp = (temp * temp) % m;
    if ((y % 2LL) == 1LL) {
        temp = (temp * x) % m;
    }
    return temp;
}

long long is_carmichael_number(long long n) {
    if (n <= 0LL) {
        panic("Number must be positive");
    }
    long long b = 2LL;
    while (b < n) {
        if (gcd(b, n) == 1LL) {
            if (power(b, n - 1LL, n) != 1LL) {
                return 0LL;
            }
        }
        b = b + 1LL;
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(power(2LL, 15LL, 3LL)));
        puts(str_int(power(5LL, 1LL, 30LL)));
        puts(str_int(is_carmichael_number(4LL)));
        puts(str_int(is_carmichael_number(561LL)));
        puts(str_int(is_carmichael_number(562LL)));
        puts(str_int(is_carmichael_number(1105LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
