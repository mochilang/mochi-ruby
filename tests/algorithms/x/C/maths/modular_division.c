// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t extended_gcd_len;
size_t extended_euclid_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long mod(long long a, long long n);
long long greatest_common_divisor(long long a, long long b);
long long * extended_gcd(long long a, long long b);
long long * extended_euclid(long long a, long long b);
long long invert_modulo(long long a, long long n);
long long modular_division(long long a, long long b, long long n);
long long modular_division2(long long a, long long b, long long n);
void tests();
void user_main();
int main(void);

long long mod(long long a, long long n) {
    long long r = a % n;
    if (r < 0LL) {
        return r + n;
    }
    return r;
}

long long greatest_common_divisor(long long a, long long b) {
    long long x;
    if (a < 0LL) {
        x = -(a);
    } else {
        x = a;
    }
    long long y;
    if (b < 0LL) {
        y = -(b);
    } else {
        y = b;
    }
    while (y != 0LL) {
        long long t = x % y;
        x = y;
        y = t;
    }
    return x;
}

long long * extended_gcd(long long a, long long b) {
    if (b == 0LL) {
        {
            long long* __tmp0_data = malloc(3 * sizeof(long long));
            __tmp0_data[0] = a;
            __tmp0_data[1] = 1LL;
            __tmp0_data[2] = 0LL;
            extended_gcd_len = 3;
            return __tmp0_data;
        }
    }
    long long *res = extended_gcd(b, a % b);
    size_t res_len = extended_gcd_len;
    long long d = res[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    long long p = res[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    long long q = res[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    long long x = q;
    long long y = p - (q * (a / b));
    {
        long long* __tmp1_data = malloc(3 * sizeof(long long));
        __tmp1_data[0] = d;
        __tmp1_data[1] = x;
        __tmp1_data[2] = y;
        extended_gcd_len = 3;
        return __tmp1_data;
    }
}

long long * extended_euclid(long long a, long long b) {
    if (b == 0LL) {
        {
            long long* __tmp2_data = malloc(2 * sizeof(long long));
            __tmp2_data[0] = 1LL;
            __tmp2_data[1] = 0LL;
            extended_euclid_len = 2;
            return __tmp2_data;
        }
    }
    long long *res = extended_euclid(b, a % b);
    size_t res_len = extended_euclid_len;
    long long x = res[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    long long y = res[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})] - ((a / b) * res[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})]);
    {
        long long* __tmp3_data = malloc(2 * sizeof(long long));
        __tmp3_data[0] = x;
        __tmp3_data[1] = y;
        extended_euclid_len = 2;
        return __tmp3_data;
    }
}

long long invert_modulo(long long a, long long n) {
    long long *res = extended_euclid(a, n);
    size_t res_len = extended_euclid_len;
    long long inv = res[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
    return mod(inv, n);
}

long long modular_division(long long a, long long b, long long n) {
    if (n <= 1LL) {
        panic("n must be > 1");
    }
    if (a <= 0LL) {
        panic("a must be > 0");
    }
    if (greatest_common_divisor(a, n) != 1LL) {
        panic("gcd(a,n) != 1");
    }
    long long *eg = extended_gcd(n, a);
    size_t eg_len = extended_gcd_len;
    long long s = eg[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? eg_len + _mochi_idx : _mochi_idx;})];
    return mod(b * s, n);
}

long long modular_division2(long long a, long long b, long long n) {
    long long s = invert_modulo(a, n);
    return mod(b * s, n);
}

void tests() {
    if (modular_division(4LL, 8LL, 5LL) != 2LL) {
        panic("md1");
    }
    if (modular_division(3LL, 8LL, 5LL) != 1LL) {
        panic("md2");
    }
    if (modular_division(4LL, 11LL, 5LL) != 4LL) {
        panic("md3");
    }
    if (modular_division2(4LL, 8LL, 5LL) != 2LL) {
        panic("md21");
    }
    if (modular_division2(3LL, 8LL, 5LL) != 1LL) {
        panic("md22");
    }
    if (modular_division2(4LL, 11LL, 5LL) != 4LL) {
        panic("md23");
    }
    if (invert_modulo(2LL, 5LL) != 3LL) {
        panic("inv");
    }
    long long *eg = extended_gcd(10LL, 6LL);
    size_t eg_len = extended_gcd_len;
    if (((eg[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? eg_len + _mochi_idx : _mochi_idx;})] != 2LL) || (eg[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? eg_len + _mochi_idx : _mochi_idx;})] != -1.0)) || (eg[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? eg_len + _mochi_idx : _mochi_idx;})] != 2LL)) {
        panic("eg");
    }
    long long *eu = extended_euclid(10LL, 6LL);
    size_t eu_len = extended_euclid_len;
    if ((eu[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? eu_len + _mochi_idx : _mochi_idx;})] != -1.0) || (eu[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? eu_len + _mochi_idx : _mochi_idx;})] != 2LL)) {
        panic("eu");
    }
    if (greatest_common_divisor(121LL, 11LL) != 11LL) {
        panic("gcd");
    }
}

void user_main() {
    tests();
    puts(str_int(modular_division(4LL, 8LL, 5LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
