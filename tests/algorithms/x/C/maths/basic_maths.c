// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prime_factors_len;
size_t unique_len;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long pow_int(long long base, long long exp);
long long * prime_factors(long long n);
long long number_of_divisors(long long n);
long long sum_of_divisors(long long n);
long long contains(long long * arr, size_t arr_len, long long x);
long long * unique(long long * arr, size_t arr_len);
long long euler_phi(long long n);
int main(void);

long long pow_int(long long base, long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

long long * prime_factors(long long n) {
    if (n <= 0LL) {
        panic("Only positive integers have prime factors");
    }
    long long num = n;
    long long *pf = NULL;
    size_t pf_len = 0;
    while ((num % 2LL) == 0LL) {
        pf = list_append_long_long(pf, &pf_len, 2LL);
        num = num / 2LL;
    }
    long long i = 3LL;
    while ((i * i) <= num) {
        while ((num % i) == 0LL) {
            pf = list_append_int(pf, &pf_len, i);
            num = num / i;
        }
        i = i + 2LL;
    }
    if (num > 2LL) {
        pf = list_append_long_long(pf, &pf_len, num);
    }
    return prime_factors_len = pf_len, pf;
}

long long number_of_divisors(long long n) {
    if (n <= 0LL) {
        panic("Only positive numbers are accepted");
    }
    long long num = n;
    long long div = 1LL;
    long long temp = 1LL;
    while ((num % 2LL) == 0LL) {
        temp = temp + 1LL;
        num = num / 2LL;
    }
    div = div * temp;
    long long i = 3LL;
    while ((i * i) <= num) {
        temp = 1LL;
        while ((num % i) == 0LL) {
            temp = temp + 1LL;
            num = num / i;
        }
        div = div * temp;
        i = i + 2LL;
    }
    if (num > 1LL) {
        div = div * 2LL;
    }
    return div;
}

long long sum_of_divisors(long long n) {
    if (n <= 0LL) {
        panic("Only positive numbers are accepted");
    }
    long long num = n;
    long long s = 1LL;
    long long temp = 1LL;
    while ((num % 2LL) == 0LL) {
        temp = temp + 1LL;
        num = num / 2LL;
    }
    if (temp > 1LL) {
        s = s * ((pow_int(2LL, temp) - 1LL) / 1LL);
    }
    long long i = 3LL;
    while ((i * i) <= num) {
        temp = 1LL;
        while ((num % i) == 0LL) {
            temp = temp + 1LL;
            num = num / i;
        }
        if (temp > 1LL) {
            s = s * ((pow_int(i, temp) - 1LL) / (i - 1LL));
        }
        i = i + 2LL;
    }
    return s;
}

long long contains(long long * arr, size_t arr_len, long long x) {
    long long idx = 0LL;
    while (idx < arr_len) {
        if (arr[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == x) {
            return 1LL;
        }
        idx = idx + 1LL;
    }
    return 0LL;
}

long long * unique(long long * arr, size_t arr_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long idx = 0LL;
    while (idx < arr_len) {
        long long v = arr[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (!(contains(result, result_len, v))) {
            result = list_append_long_long(result, &result_len, v);
        }
        idx = idx + 1LL;
    }
    return unique_len = result_len, result;
}

long long euler_phi(long long n) {
    if (n <= 0LL) {
        panic("Only positive numbers are accepted");
    }
    long long s = n;
    long long *factors = ({long long* __tmp2 = prime_factors(n); unique(__tmp2, prime_factors_len);});
    size_t factors_len = unique_len;
    long long idx = 0LL;
    while (idx < factors_len) {
        long long x = factors[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})];
        s = (s / x) * (x - 1LL);
        idx = idx + 1LL;
    }
    return s;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp3 = prime_factors(100LL); char *__res = str_list_int(__tmp3, prime_factors_len); __res;}));
        puts(str_int(number_of_divisors(100LL)));
        puts(str_int(sum_of_divisors(100LL)));
        puts(str_int(euler_phi(100LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
