// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

typedef struct { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_sd(const char *keys[], double vals[], size_t *len, const char *key, double val) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* key(long long x, long long y);
MapSD joint_probability_distribution(long long * x_values, size_t x_values_len, long long * y_values, size_t y_values_len, double * x_probabilities, size_t x_probabilities_len, double * y_probabilities, size_t y_probabilities_len);
double expectation(long long * values, size_t values_len, double * probabilities, size_t probabilities_len);
double variance(long long * values, size_t values_len, double * probabilities, size_t probabilities_len);
double covariance(long long * x_values, size_t x_values_len, long long * y_values, size_t y_values_len, double * x_probabilities, size_t x_probabilities_len, double * y_probabilities, size_t y_probabilities_len);
double sqrtApprox(double x);
double standard_deviation(double v);
void user_main();
int main(void);

const char* key(long long x, long long y) {
    return str_concat(str_concat(str_int(x), ","), str_int(y));
}

MapSD joint_probability_distribution(long long * x_values, size_t x_values_len, long long * y_values, size_t y_values_len, double * x_probabilities, size_t x_probabilities_len, double * y_probabilities, size_t y_probabilities_len) {
    static const char* result_keys[16] = {};
    static double result_vals[16] = {};
    size_t result_len = 0;
    MapSD result = { result_keys, result_vals, 0, 16 };
    long long i = 0LL;
    while (i < x_values_len) {
        long long j = 0LL;
        while (j < y_values_len) {
            const char* k = key(x_values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_values_len + _mochi_idx : _mochi_idx;})], y_values[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? y_values_len + _mochi_idx : _mochi_idx;})]);
            map_set_sd(result.keys, result.vals, &result.len, k, x_probabilities[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_probabilities_len + _mochi_idx : _mochi_idx;})] * y_probabilities[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? y_probabilities_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return result;
}

double expectation(long long * values, size_t values_len, double * probabilities, size_t probabilities_len) {
    double total = 0.0;
    long long i = 0LL;
    while (i < values_len) {
        total = total + ((double)(values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]) * probabilities[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? probabilities_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return total;
}

double variance(long long * values, size_t values_len, double * probabilities, size_t probabilities_len) {
    double mean = expectation(values, values_len, probabilities, probabilities_len);
    double total = 0.0;
    long long i = 0LL;
    while (i < values_len) {
        double diff = (double)(values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]) - mean;
        total = total + ((diff * diff) * probabilities[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? probabilities_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return total;
}

double covariance(long long * x_values, size_t x_values_len, long long * y_values, size_t y_values_len, double * x_probabilities, size_t x_probabilities_len, double * y_probabilities, size_t y_probabilities_len) {
    double mean_x = expectation(x_values, x_values_len, x_probabilities, x_probabilities_len);
    double mean_y = expectation(y_values, y_values_len, y_probabilities, y_probabilities_len);
    double total = 0.0;
    long long i = 0LL;
    while (i < x_values_len) {
        long long j = 0LL;
        while (j < y_values_len) {
            double diff_x = (double)(x_values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_values_len + _mochi_idx : _mochi_idx;})]) - mean_x;
            double diff_y = (double)(y_values[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? y_values_len + _mochi_idx : _mochi_idx;})]) - mean_y;
            total = total + (((diff_x * diff_y) * x_probabilities[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_probabilities_len + _mochi_idx : _mochi_idx;})]) * y_probabilities[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? y_probabilities_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return total;
}

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double standard_deviation(double v) {
    return sqrtApprox(v);
}

void user_main() {
    long long *x_values = NULL;
    size_t x_values_len = 0;
    x_values = list_append_long_long(x_values, &x_values_len, 1LL);
    x_values = list_append_long_long(x_values, &x_values_len, 2LL);
    double *y_values = NULL;
    size_t y_values_len = 0;
    y_values = list_append_double(y_values, &y_values_len, -2.0);
    y_values = list_append_double(y_values, &y_values_len, 5LL);
    y_values = list_append_double(y_values, &y_values_len, 8LL);
    double *x_probabilities = NULL;
    size_t x_probabilities_len = 0;
    x_probabilities = list_append_double(x_probabilities, &x_probabilities_len, 0.7);
    x_probabilities = list_append_double(x_probabilities, &x_probabilities_len, 0.3);
    double *y_probabilities = NULL;
    size_t y_probabilities_len = 0;
    y_probabilities = list_append_double(y_probabilities, &y_probabilities_len, 0.3);
    y_probabilities = list_append_double(y_probabilities, &y_probabilities_len, 0.5);
    y_probabilities = list_append_double(y_probabilities, &y_probabilities_len, 0.2);
    MapSD jpd = joint_probability_distribution(x_values, x_values_len, y_values, y_values_len, x_probabilities, x_probabilities_len, y_probabilities, y_probabilities_len);
    long long i = 0LL;
    while (i < 2LL) {
        long long j = 0LL;
        while (j < 3LL) {
            const char* k = key(x_values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_values_len + _mochi_idx : _mochi_idx;})], y_values[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? y_values_len + _mochi_idx : _mochi_idx;})]);
            double prob = map_get_sd(jpd.keys, jpd.vals, jpd.len, k);
            puts(str_concat(str_concat(k, "="), str_float(prob)));
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    double ex = expectation(x_values, x_values_len, x_probabilities, x_probabilities_len);
    double ey = expectation(y_values, y_values_len, y_probabilities, y_probabilities_len);
    double vx = variance(x_values, x_values_len, x_probabilities, x_probabilities_len);
    double vy = variance(y_values, y_values_len, y_probabilities, y_probabilities_len);
    double cov = covariance(x_values, x_values_len, y_values, y_values_len, x_probabilities, x_probabilities_len, y_probabilities, y_probabilities_len);
    puts(str_concat("Ex=", str_float(ex)));
    puts(str_concat("Ey=", str_float(ey)));
    puts(str_concat("Vx=", str_float(vx)));
    puts(str_concat("Vy=", str_float(vy)));
    puts(str_concat("Cov=", str_float(cov)));
    puts(str_concat("Sx=", str_float(standard_deviation(vx))));
    puts(str_concat("Sy=", str_float(standard_deviation(vy))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
