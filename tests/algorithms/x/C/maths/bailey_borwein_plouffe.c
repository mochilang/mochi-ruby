// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* digits = "";
long long i = 1LL;

long long mod_pow(long long base, long long exponent, long long modulus);
double pow_float(double base, long long exponent);
const char* hex_digit(long long n);
double floor_float(double x);
double subsum(long long digit_pos_to_extract, long long denominator_addend, long long precision);
const char* bailey_borwein_plouffe(long long digit_position, long long precision);
int main(void);

long long mod_pow(long long base, long long exponent, long long modulus) {
    long long result = 1LL;
    long long b = base % modulus;
    long long e = exponent;
    while (e > 0LL) {
        if ((e % 2LL) == 1LL) {
            result = (result * b) % modulus;
        }
        b = (b * b) % modulus;
        e = e / 2LL;
    }
    return result;
}

double pow_float(double base, long long exponent) {
    long long exp = exponent;
    double result = 1.0;
    if (exp < 0LL) {
        exp = -(exp);
    }
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    if (exponent < 0LL) {
        result = 1.0 / result;
    }
    return result;
}

const char* hex_digit(long long n) {
    if (n < 10LL) {
        return str_int(n);
    }
    const char* *letters = NULL;
    size_t letters_len = 0;
    letters = list_append_str(letters, &letters_len, "a");
    letters = list_append_str(letters, &letters_len, "b");
    letters = list_append_str(letters, &letters_len, "c");
    letters = list_append_str(letters, &letters_len, "d");
    letters = list_append_str(letters, &letters_len, "e");
    letters = list_append_str(letters, &letters_len, "f");
    return letters[(int)({long long _mochi_idx = n - 10LL; _mochi_idx < 0 ? letters_len + _mochi_idx : _mochi_idx;})];
}

double floor_float(double x) {
    long long i = (int)(x);
    if ((double)(i) > x) {
        i = i - 1LL;
    }
    return (double)(i);
}

double subsum(long long digit_pos_to_extract, long long denominator_addend, long long precision) {
    double total = 0.0;
    long long sum_index = 0LL;
    while (sum_index < (digit_pos_to_extract + precision)) {
        long long denominator = (8LL * sum_index) + denominator_addend;
        if (sum_index < digit_pos_to_extract) {
            long long exponent = (digit_pos_to_extract - 1LL) - sum_index;
            long long exponential_term = mod_pow(16LL, exponent, denominator);
            total = total + ((double)(exponential_term) / (double)(denominator));
        } else {
            long long exponent = (digit_pos_to_extract - 1LL) - sum_index;
            double exponential_term = pow_float(16.0, exponent);
            total = total + (exponential_term / (double)(denominator));
        }
        sum_index = sum_index + 1LL;
    }
    return total;
}

const char* bailey_borwein_plouffe(long long digit_position, long long precision) {
    if (digit_position <= 0LL) {
        panic("Digit position must be a positive integer");
    }
    if (precision < 0LL) {
        panic("Precision must be a nonnegative integer");
    }
    double sum_result = (((4.0 * subsum(digit_position, 1LL, precision)) - (2.0 * subsum(digit_position, 4LL, precision))) - (1.0 * subsum(digit_position, 5LL, precision))) - (1.0 * subsum(digit_position, 6LL, precision));
    double fraction = sum_result - floor_float(sum_result);
    long long digit = fraction * 16.0;
    const char* hd = hex_digit(digit);
    return hd;
}

int main(void) {
    {
        long long __start = _now();
        while (i <= 10LL) {
            digits = str_concat(digits, bailey_borwein_plouffe(i, 1000LL));
            i = i + 1LL;
        }
        puts(digits);
        puts(bailey_borwein_plouffe(5LL, 10000LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
