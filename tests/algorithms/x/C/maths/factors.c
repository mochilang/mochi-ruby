// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t reverse_len;
size_t factors_of_a_number_len;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t concat_len;
static long long* concat_long_long(long long *a, size_t a_len, const long long *b, size_t b_len) {
    long long *res = realloc(a, (a_len + b_len) * sizeof(long long));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(long long));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * reverse(long long * xs, size_t xs_len);
long long * factors_of_a_number(long long num);
void run_tests();
void user_main();
int main(void);

long long * reverse(long long * xs, size_t xs_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = xs_len - 1LL;
    while (i >= 0LL) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i - 1LL;
    }
    return reverse_len = res_len, res;
}

long long * factors_of_a_number(long long num) {
    long long *facs = NULL;
    size_t facs_len = 0;
    if (num < 1LL) {
        return factors_of_a_number_len = facs_len, facs;
    }
    long long *small = NULL;
    size_t small_len = 0;
    long long *large = NULL;
    size_t large_len = 0;
    long long i = 1LL;
    while ((i * i) <= num) {
        if ((num % i) == 0LL) {
            small = list_append_int(small, &small_len, i);
            long long d = num / i;
            if (d != i) {
                large = list_append_int(large, &large_len, d);
            }
        }
        i = i + 1LL;
    }
    facs = concat_long_long(small, small_len, reverse(large, large_len), reverse_len);
    facs_len = concat_len;
    return factors_of_a_number_len = facs_len, facs;
}

void run_tests() {
    if (factors_of_a_number(1LL) != ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 1LL; tmp;})) {
        panic("case1 failed");
    }
    if (factors_of_a_number(5LL) != ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 5LL; tmp;})) {
        panic("case2 failed");
    }
    if (factors_of_a_number(24LL) != ({long long *tmp = malloc(8 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 6LL; tmp[5] = 8LL; tmp[6] = 12LL; tmp[7] = 24LL; tmp;})) {
        panic("case3 failed");
    }
    if (factors_of_a_number(-24LL) != NULL) {
        panic("case4 failed");
    }
}

void user_main() {
    run_tests();
    puts(({long long *__tmp1 = factors_of_a_number(24LL); char *__res = str_list_int(__tmp1, factors_of_a_number_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
