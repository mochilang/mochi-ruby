// Generated by Mochi 0.10.32 on 2025-08-12 08:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t continued_fraction_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long floor_div(long long a, long long b);
long long * continued_fraction(long long numerator, long long denominator);
const char* list_to_string(long long * lst, size_t lst_len);
int main(void);

long long floor_div(long long a, long long b) {
    long long q = a / b;
    long long r = a % b;
    if ((r != 0LL) && (((a < 0LL) && (b > 0LL)) || ((a > 0LL) && (b < 0LL)))) {
        q = q - 1LL;
    }
    return q;
}

long long * continued_fraction(long long numerator, long long denominator) {
    long long num = numerator;
    long long den = denominator;
    long long *result = NULL;
    size_t result_len = 0;
    while (1LL) {
        long long integer_part = floor_div(num, den);
        result = list_append_long_long(result, &result_len, integer_part);
        num = num - (integer_part * den);
        if (num == 0LL) {
            break;
        }
        long long tmp = num;
        num = den;
        den = tmp;
    }
    return continued_fraction_len = result_len, result;
}

const char* list_to_string(long long * lst, size_t lst_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < lst_len) {
        s = str_concat(s, str_int(lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]));
        if (i < (lst_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    return str_concat(s, "]");
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("Continued Fraction of 0.84375 is: ", ({long long* __tmp1 = continued_fraction(27LL, 32LL); list_to_string(__tmp1, continued_fraction_len);})));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
