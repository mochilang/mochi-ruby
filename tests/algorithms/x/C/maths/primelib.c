// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sieve_er_len;
size_t get_prime_numbers_len;
size_t prime_factorization_len;
size_t goldbach_len;
size_t get_primes_between_len;
size_t get_divisors_len;
size_t simplify_fraction_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long abs_int(long long x);
long long gcd_iter(long long a, long long b);
long long is_prime(long long n);
long long * sieve_er(long long n);
long long * get_prime_numbers(long long n);
long long * prime_factorization(long long number);
long long greatest_prime_factor(long long number);
long long smallest_prime_factor(long long number);
long long kg_v(long long number1, long long number2);
long long is_even(long long number);
long long is_odd(long long number);
long long * goldbach(long long number);
long long get_prime(long long n);
long long * get_primes_between(long long p1, long long p2);
long long * get_divisors(long long n);
long long is_perfect_number(long long number);
long long * simplify_fraction(long long numerator, long long denominator);
long long factorial(long long n);
long long fib(long long n);
int main(void);

long long abs_int(long long x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long gcd_iter(long long a, long long b) {
    long long x = abs_int(a);
    long long y = abs_int(b);
    while (y != 0LL) {
        long long t = y;
        y = x % y;
        x = t;
    }
    return x;
}

long long is_prime(long long n) {
    if (n <= 1LL) {
        return 0LL;
    }
    long long d = 2LL;
    while ((d * d) <= n) {
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 1LL;
    }
    return 1LL;
}

long long * sieve_er(long long n) {
    long long *nums = NULL;
    size_t nums_len = 0;
    long long i = 2LL;
    while (i <= n) {
        nums = list_append_long_long(nums, &nums_len, i);
        i = i + 1LL;
    }
    long long idx = 0LL;
    while (idx < nums_len) {
        long long j = idx + 1LL;
        while (j < nums_len) {
            if (nums[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] != 0LL) {
                if ((nums[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] % nums[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]) == 0LL) {
                    nums[(int)(j)] = 0LL;
                }
            }
            j = j + 1LL;
        }
        idx = idx + 1LL;
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long k = 0LL;
    while (k < nums_len) {
        long long v = nums[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (v != 0LL) {
            res = list_append_long_long(res, &res_len, v);
        }
        k = k + 1LL;
    }
    return sieve_er_len = res_len, res;
}

long long * get_prime_numbers(long long n) {
    long long *ans = NULL;
    size_t ans_len = 0;
    long long num = 2LL;
    while (num <= n) {
        if (is_prime(num)) {
            ans = list_append_long_long(ans, &ans_len, num);
        }
        num = num + 1LL;
    }
    return get_prime_numbers_len = ans_len, ans;
}

long long * prime_factorization(long long number) {
    if (number == 0LL) {
        {
            long long* __tmp7_data = malloc(1 * sizeof(long long));
            __tmp7_data[0] = 0LL;
            prime_factorization_len = 1;
            return __tmp7_data;
        }
    }
    if (number == 1LL) {
        {
            long long* __tmp8_data = malloc(1 * sizeof(long long));
            __tmp8_data[0] = 1LL;
            prime_factorization_len = 1;
            return __tmp8_data;
        }
    }
    long long *ans = NULL;
    size_t ans_len = 0;
    if (is_prime(number)) {
        ans = list_append_long_long(ans, &ans_len, number);
        return prime_factorization_len = ans_len, ans;
    }
    long long quotient = number;
    long long factor = 2LL;
    while (quotient != 1LL) {
        if (is_prime(factor) && ((quotient % factor) == 0LL)) {
            ans = list_append_long_long(ans, &ans_len, factor);
            quotient = quotient / factor;
        } else {
            factor = factor + 1LL;
        }
    }
    return prime_factorization_len = ans_len, ans;
}

long long greatest_prime_factor(long long number) {
    long long *factors = prime_factorization(number);
    size_t factors_len = prime_factorization_len;
    long long m = factors[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < factors_len) {
        if (factors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})] > m) {
            m = factors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return m;
}

long long smallest_prime_factor(long long number) {
    long long *factors = prime_factorization(number);
    size_t factors_len = prime_factorization_len;
    long long m = factors[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < factors_len) {
        if (factors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})] < m) {
            m = factors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? factors_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return m;
}

long long kg_v(long long number1, long long number2) {
    if ((number1 < 1LL) || (number2 < 1LL)) {
        panic("numbers must be positive");
    }
    long long g = gcd_iter(number1, number2);
    return (number1 / g) * number2;
}

long long is_even(long long number) {
    return (number % 2LL) == 0LL;
}

long long is_odd(long long number) {
    return (number % 2LL) != 0LL;
}

long long * goldbach(long long number) {
    if (!(is_even(number)) || (number <= 2LL)) {
        panic("number must be even and > 2");
    }
    long long *primes = get_prime_numbers(number);
    size_t primes_len = get_prime_numbers_len;
    long long i = 0LL;
    while (i < primes_len) {
        long long j = i + 1LL;
        while (j < primes_len) {
            if ((primes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})] + primes[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})]) == number) {
                {
                    long long* __tmp9_data = malloc(2 * sizeof(long long));
                    __tmp9_data[0] = primes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
                    __tmp9_data[1] = primes[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
                    goldbach_len = 2;
                    return __tmp9_data;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    {
        long long* __tmp10_data = malloc(0 * sizeof(long long));
        goldbach_len = 0;
        return __tmp10_data;
    }
}

long long get_prime(long long n) {
    if (n < 0LL) {
        panic("n must be non-negative");
    }
    long long index = 0LL;
    long long ans = 2LL;
    while (index < n) {
        index = index + 1LL;
        ans = ans + 1LL;
        while (!(is_prime(ans))) {
            ans = ans + 1LL;
        }
    }
    return ans;
}

long long * get_primes_between(long long p1, long long p2) {
    long long bad1 = !(is_prime(p1));
    long long bad2 = !(is_prime(p2));
    if ((bad1 || bad2) || (p1 >= p2)) {
        panic("arguments must be prime and p1 < p2");
    }
    long long num = p1 + 1LL;
    while (num < p2) {
        if (is_prime(num)) {
            break;
        }
        num = num + 1LL;
    }
    long long *ans = NULL;
    size_t ans_len = 0;
    while (num < p2) {
        ans = list_append_long_long(ans, &ans_len, num);
        num = num + 1LL;
        while (num < p2) {
            if (is_prime(num)) {
                break;
            }
            num = num + 1LL;
        }
    }
    return get_primes_between_len = ans_len, ans;
}

long long * get_divisors(long long n) {
    if (n < 1LL) {
        panic("n must be >= 1");
    }
    long long *ans = NULL;
    size_t ans_len = 0;
    long long d = 1LL;
    while (d <= n) {
        if ((n % d) == 0LL) {
            ans = list_append_long_long(ans, &ans_len, d);
        }
        d = d + 1LL;
    }
    return get_divisors_len = ans_len, ans;
}

long long is_perfect_number(long long number) {
    if (number <= 1LL) {
        panic("number must be > 1");
    }
    long long *divisors = get_divisors(number);
    size_t divisors_len = get_divisors_len;
    long long sum = 0LL;
    long long i = 0LL;
    while (i < (divisors_len - 1LL)) {
        sum = sum + divisors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? divisors_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return sum == number;
}

long long * simplify_fraction(long long numerator, long long denominator) {
    if (denominator == 0LL) {
        panic("denominator cannot be zero");
    }
    long long g = gcd_iter(abs_int(numerator), abs_int(denominator));
    {
        long long* __tmp11_data = malloc(2 * sizeof(long long));
        __tmp11_data[0] = numerator / g;
        __tmp11_data[1] = denominator / g;
        simplify_fraction_len = 2;
        return __tmp11_data;
    }
}

long long factorial(long long n) {
    if (n < 0LL) {
        panic("n must be >= 0");
    }
    long long ans = 1LL;
    long long i = 1LL;
    while (i <= n) {
        ans = ans * i;
        i = i + 1LL;
    }
    return ans;
}

long long fib(long long n) {
    if (n < 0LL) {
        panic("n must be >= 0");
    }
    if (n <= 1LL) {
        return 1LL;
    }
    long long tmp = 0LL;
    long long fib1 = 1LL;
    long long ans = 1LL;
    long long i = 0LL;
    while (i < (n - 1LL)) {
        tmp = ans;
        ans = ans + fib1;
        fib1 = tmp;
        i = i + 1LL;
    }
    return ans;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(is_prime(97LL)));
        puts(({long long *__tmp12 = sieve_er(20LL); char *__res = str_list_int(__tmp12, sieve_er_len); __res;}));
        puts(({long long *__tmp13 = get_prime_numbers(20LL); char *__res = str_list_int(__tmp13, get_prime_numbers_len); __res;}));
        puts(({long long *__tmp14 = prime_factorization(287LL); char *__res = str_list_int(__tmp14, prime_factorization_len); __res;}));
        puts(str_int(greatest_prime_factor(287LL)));
        puts(str_int(smallest_prime_factor(287LL)));
        puts(str_int(kg_v(8LL, 10LL)));
        puts(({long long *__tmp15 = goldbach(28LL); char *__res = str_list_int(__tmp15, goldbach_len); __res;}));
        puts(str_int(get_prime(8LL)));
        puts(({long long *__tmp16 = get_primes_between(3LL, 23LL); char *__res = str_list_int(__tmp16, get_primes_between_len); __res;}));
        puts(({long long *__tmp17 = get_divisors(28LL); char *__res = str_list_int(__tmp17, get_divisors_len); __res;}));
        puts(str_int(is_perfect_number(28LL)));
        puts(({long long *__tmp18 = simplify_fraction(10LL, 20LL); char *__res = str_list_int(__tmp18, simplify_fraction_len); __res;}));
        puts(str_int(factorial(5LL)));
        puts(str_int(fib(10LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
