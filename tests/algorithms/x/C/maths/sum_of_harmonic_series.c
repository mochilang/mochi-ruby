// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double sum_of_harmonic_progression(double first_term, double common_difference, long long number_of_terms);
double abs_val(double num);
void test_sum_of_harmonic_progression();
void user_main();
int main(void);

double sum_of_harmonic_progression(double first_term, double common_difference, long long number_of_terms) {
    double *arithmetic_progression = NULL;
    size_t arithmetic_progression_len = 0;
    arithmetic_progression = list_append_double(arithmetic_progression, &arithmetic_progression_len, 1.0 / first_term);
    double term = 1.0 / first_term;
    long long i = 0LL;
    while (i < (number_of_terms - 1LL)) {
        term = term + common_difference;
        arithmetic_progression = list_append_double(arithmetic_progression, &arithmetic_progression_len, term);
        i = i + 1LL;
    }
    double total = 0.0;
    long long j = 0LL;
    while (j < arithmetic_progression_len) {
        total = total + (1.0 / arithmetic_progression[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arithmetic_progression_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return total;
}

double abs_val(double num) {
    if (num < 0.0) {
        return -(num);
    }
    return num;
}

void test_sum_of_harmonic_progression() {
    double result1 = sum_of_harmonic_progression(0.5, 2.0, 2LL);
    if (abs_val(result1 - 0.75) > 1e-07) {
        panic("test1 failed");
    }
    double result2 = sum_of_harmonic_progression(0.2, 5.0, 5LL);
    if (abs_val(result2 - 0.45666666666666667) > 1e-07) {
        panic("test2 failed");
    }
}

void user_main() {
    test_sum_of_harmonic_progression();
    printf("%g\n", sum_of_harmonic_progression(0.5, 2.0, 2LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
