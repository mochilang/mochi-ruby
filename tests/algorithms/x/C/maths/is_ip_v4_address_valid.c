// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_by_dot_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* * split_by_dot(const char* s);
long long is_digit_str(const char* s);
long long parse_decimal(const char* s);
long long is_ip_v4_address_valid(const char* ip);
int main(void);

const char* * split_by_dot(const char* s) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char c = (const char[]){s[i], 0};
        if (c == ".") {
            res = list_append_str(res, &res_len, current);
            current = "";
        } else {
            current = str_concat(current, str_int(c));
        }
        i = i + 1LL;
    }
    res = list_append_str(res, &res_len, current);
    return split_by_dot_len = res_len, res;
}

long long is_digit_str(const char* s) {
    if (strlen(s) == 0LL) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < strlen(s)) {
        const char c = (const char[]){s[i], 0};
        if ((c < "0") || (c > "9")) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long parse_decimal(const char* s) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char c = (const char[]){s[i], 0};
        value = (value * 10LL) + (int)(c);
        i = i + 1LL;
    }
    return value;
}

long long is_ip_v4_address_valid(const char* ip) {
    const char* *octets = split_by_dot(ip);
    size_t octets_len = split_by_dot_len;
    if (octets_len != 4LL) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < 4LL) {
        const char* oct = octets[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? octets_len + _mochi_idx : _mochi_idx;})];
        if (!(is_digit_str(oct))) {
            return 0LL;
        }
        long long number = parse_decimal(oct);
        if (strlen(str_int(number)) != strlen(oct)) {
            return 0LL;
        }
        if ((number < 0LL) || (number > 255LL)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(is_ip_v4_address_valid("192.168.0.23")));
        puts(str_int(is_ip_v4_address_valid("192.256.15.8")));
        puts(str_int(is_ip_v4_address_valid("172.100.0.8")));
        puts(str_int(is_ip_v4_address_valid("255.256.0.256")));
        puts(str_int(is_ip_v4_address_valid("1.2.33333333.4")));
        puts(str_int(is_ip_v4_address_valid("1.2.-3.4")));
        puts(str_int(is_ip_v4_address_valid("1.2.3")));
        puts(str_int(is_ip_v4_address_valid("1.2.3.4.5")));
        puts(str_int(is_ip_v4_address_valid("1.2.A.4")));
        puts(str_int(is_ip_v4_address_valid("0.0.0.0")));
        puts(str_int(is_ip_v4_address_valid("1.2.3.")));
        puts(str_int(is_ip_v4_address_valid("1.2.3.05")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
