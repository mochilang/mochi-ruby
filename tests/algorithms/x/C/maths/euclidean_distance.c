// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double sqrtApprox(double x);
double euclidean_distance(double * v1, size_t v1_len, double * v2, size_t v2_len);
double euclidean_distance_no_np(double * v1, size_t v1_len, double * v2, size_t v2_len);
void user_main();
int main(void);

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double euclidean_distance(double * v1, size_t v1_len, double * v2, size_t v2_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < v1_len) {
        double diff = v1[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v1_len + _mochi_idx : _mochi_idx;})] - v2[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v2_len + _mochi_idx : _mochi_idx;})];
        sum = sum + (diff * diff);
        i = i + 1LL;
    }
    return sqrtApprox(sum);
}

double euclidean_distance_no_np(double * v1, size_t v1_len, double * v2, size_t v2_len) {
    return euclidean_distance(v1, v1_len, v2, v2_len);
}

void user_main() {
    puts(str_float(euclidean_distance(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 0.0; tmp;}), 2, ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp;}), 2)));
    puts(str_float(euclidean_distance(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 0.0; tmp[2] = 0.0; tmp;}), 3, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp[2] = 2.0; tmp;}), 3)));
    puts(str_float(euclidean_distance(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 2.0; tmp[2] = 3.0; tmp[3] = 4.0; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 5.0; tmp[1] = 6.0; tmp[2] = 7.0; tmp[3] = 8.0; tmp;}), 4)));
    puts(str_float(euclidean_distance_no_np(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 2.0; tmp[2] = 3.0; tmp[3] = 4.0; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 5.0; tmp[1] = 6.0; tmp[2] = 7.0; tmp[3] = 8.0; tmp;}), 4)));
    puts(str_float(euclidean_distance_no_np(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 0.0; tmp;}), 2, ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp;}), 2)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
