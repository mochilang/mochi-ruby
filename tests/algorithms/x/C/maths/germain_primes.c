// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long is_prime(long long n);
long long is_germain_prime(long long number);
long long is_safe_prime(long long number);
void test_is_germain_prime();
void test_is_safe_prime();
void user_main();
int main(void);

long long is_prime(long long n) {
    if (n <= 1LL) {
        return 0LL;
    }
    if (n <= 3LL) {
        return 1LL;
    }
    if ((n % 2LL) == 0LL) {
        return 0LL;
    }
    long long i = 3LL;
    while ((i * i) <= n) {
        if ((n % i) == 0LL) {
            return 0LL;
        }
        i = i + 2LL;
    }
    return 1LL;
}

long long is_germain_prime(long long number) {
    if (number < 1LL) {
        panic("Input value must be a positive integer");
    }
    return is_prime(number) && is_prime((2LL * number) + 1LL);
}

long long is_safe_prime(long long number) {
    if (number < 1LL) {
        panic("Input value must be a positive integer");
    }
    if (((number - 1LL) % 2LL) != 0LL) {
        return 0LL;
    }
    return is_prime(number) && is_prime((number - 1LL) / 2LL);
}

void test_is_germain_prime() {
    if (!(is_germain_prime(3LL))) {
        panic("is_germain_prime(3) failed");
    }
    if (!(is_germain_prime(11LL))) {
        panic("is_germain_prime(11) failed");
    }
    if (is_germain_prime(4LL)) {
        panic("is_germain_prime(4) failed");
    }
    if (!(is_germain_prime(23LL))) {
        panic("is_germain_prime(23) failed");
    }
    if (is_germain_prime(13LL)) {
        panic("is_germain_prime(13) failed");
    }
    if (is_germain_prime(20LL)) {
        panic("is_germain_prime(20) failed");
    }
}

void test_is_safe_prime() {
    if (!(is_safe_prime(5LL))) {
        panic("is_safe_prime(5) failed");
    }
    if (!(is_safe_prime(11LL))) {
        panic("is_safe_prime(11) failed");
    }
    if (is_safe_prime(1LL)) {
        panic("is_safe_prime(1) failed");
    }
    if (is_safe_prime(2LL)) {
        panic("is_safe_prime(2) failed");
    }
    if (is_safe_prime(3LL)) {
        panic("is_safe_prime(3) failed");
    }
    if (!(is_safe_prime(47LL))) {
        panic("is_safe_prime(47) failed");
    }
}

void user_main() {
    test_is_germain_prime();
    test_is_safe_prime();
    printf("%d\n", is_germain_prime(23LL));
    printf("%d\n", is_safe_prime(47LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
