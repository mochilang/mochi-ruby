// Generated by Mochi 0.10.32 on 2025-08-12 08:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long combinations(long long n, long long k);
int main(void);

long long combinations(long long n, long long k) {
    if ((k < 0LL) || (n < k)) {
        panic("Please enter positive integers for n and k where n >= k");
    }
    long long res = 1LL;
    long long i = 0LL;
    while (i < k) {
        res = res * (n - i);
        res = res / (i + 1LL);
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("The number of five-card hands possible from a standard fifty-two card deck is: ", str_int(combinations(52LL, 5LL))));
        puts("");
        puts(str_concat(str_concat("If a class of 40 students must be arranged into groups of 4 for group projects, there are ", str_int(combinations(40LL, 4LL))), " ways to arrange them."));
        puts("");
        puts(str_concat(str_concat("If 10 teams are competing in a Formula One race, there are ", str_int(combinations(10LL, 3LL))), " ways that first, second and third place can be awarded."));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
