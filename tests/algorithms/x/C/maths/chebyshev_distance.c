// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double user_abs(double x);
double chebyshev_distance(double * point_a, size_t point_a_len, double * point_b, size_t point_b_len);
int main(void);

double user_abs(double x) {
    if (x >= 0.0) {
        return x;
    } else {
        return -(x);
    }
}

double chebyshev_distance(double * point_a, size_t point_a_len, double * point_b, size_t point_b_len) {
    if (point_a_len != point_b_len) {
        panic("Both points must have the same dimension.");
    }
    double max_diff = 0.0;
    long long i = 0LL;
    while (i < point_a_len) {
        double diff = user_abs(point_a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? point_a_len + _mochi_idx : _mochi_idx;})] - point_b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? point_b_len + _mochi_idx : _mochi_idx;})]);
        if (diff > max_diff) {
            max_diff = diff;
        }
        i = i + 1LL;
    }
    return max_diff;
}

int main(void) {
    {
        long long __start = _now();
        printf("%g\n", chebyshev_distance(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 1.0; tmp;}), 2, ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp;}), 2));
        printf("%g\n", chebyshev_distance(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 1.0; tmp[2] = 9.0; tmp;}), 3, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp[2] = -(5.2); tmp;}), 3));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
