// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Coeffs Coeffs;

struct Coeffs {
    long long x;
    long long y;
};

long long abs_val(long long n);
Coeffs extended_euclidean_algorithm(long long a, long long b);
void test_extended_euclidean_algorithm();
void user_main();
int main(void);

long long abs_val(long long n) {
    if (n < 0LL) {
        return -(n);
    }
    return n;
}

Coeffs extended_euclidean_algorithm(long long a, long long b) {
    if (abs_val(a) == 1LL) {
        return (Coeffs){.x = a, .y = 0LL};
    }
    if (abs_val(b) == 1LL) {
        return (Coeffs){.x = 0LL, .y = b};
    }
    long long old_remainder = a;
    long long remainder = b;
    long long old_coeff_a = 1LL;
    long long coeff_a = 0LL;
    long long old_coeff_b = 0LL;
    long long coeff_b = 1LL;
    while (remainder != 0LL) {
        long long quotient = old_remainder / remainder;
        long long temp_remainder = old_remainder - (quotient * remainder);
        old_remainder = remainder;
        remainder = temp_remainder;
        long long temp_a = old_coeff_a - (quotient * coeff_a);
        old_coeff_a = coeff_a;
        coeff_a = temp_a;
        long long temp_b = old_coeff_b - (quotient * coeff_b);
        old_coeff_b = coeff_b;
        coeff_b = temp_b;
    }
    if (a < 0LL) {
        old_coeff_a = -(old_coeff_a);
    }
    if (b < 0LL) {
        old_coeff_b = -(old_coeff_b);
    }
    return (Coeffs){.x = old_coeff_a, .y = old_coeff_b};
}

void test_extended_euclidean_algorithm() {
    Coeffs r1 = extended_euclidean_algorithm(1LL, 24LL);
    if ((r1.x != 1LL) || (r1.y != 0LL)) {
        panic("test1 failed");
    }
    Coeffs r2 = extended_euclidean_algorithm(8LL, 14LL);
    if ((r2.x != 2LL) || (r2.y != -1LL)) {
        panic("test2 failed");
    }
    Coeffs r3 = extended_euclidean_algorithm(240LL, 46LL);
    if ((r3.x != -9LL) || (r3.y != 47LL)) {
        panic("test3 failed");
    }
    Coeffs r4 = extended_euclidean_algorithm(1LL, -4LL);
    if ((r4.x != 1LL) || (r4.y != 0LL)) {
        panic("test4 failed");
    }
    Coeffs r5 = extended_euclidean_algorithm(-2LL, -4LL);
    if ((r5.x != -1LL) || (r5.y != 0LL)) {
        panic("test5 failed");
    }
    Coeffs r6 = extended_euclidean_algorithm(0LL, -4LL);
    if ((r6.x != 0LL) || (r6.y != -1LL)) {
        panic("test6 failed");
    }
    Coeffs r7 = extended_euclidean_algorithm(2LL, 0LL);
    if ((r7.x != 1LL) || (r7.y != 0LL)) {
        panic("test7 failed");
    }
}

void user_main() {
    test_extended_euclidean_algorithm();
    Coeffs res = extended_euclidean_algorithm(240LL, 46LL);
    puts(str_concat(str_concat(str_concat(str_concat("(", str_int(res.x)), ", "), str_int(res.y)), ")"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
