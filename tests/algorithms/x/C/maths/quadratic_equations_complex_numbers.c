// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t quadratic_roots_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Anon1 Anon1;
typedef struct Complex Complex;

struct Anon1 {
    const char* __name;
    const char* im;
    double re;
};

struct Complex {
    double re;
    double im;
};

Complex add(Complex a, Complex b);
Complex sub(Complex a, Complex b);
Complex div_real(Complex a, double r);
double sqrt_newton(double x);
Complex sqrt_to_complex(double d);
Complex * quadratic_roots(double a, double b, double c);
const char* root_str(Complex r);
void user_main();
int main(void);

Complex add(Complex a, Complex b) {
    return (Complex){.re = a.re + b.re, .im = a.im + b.im};
}

Complex sub(Complex a, Complex b) {
    return (Complex){.re = a.re - b.re, .im = a.im - b.im};
}

Complex div_real(Complex a, double r) {
    return (Complex){.re = a.re / r, .im = a.im / r};
}

double sqrt_newton(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

Complex sqrt_to_complex(double d) {
    if (d >= 0.0) {
        return (Complex){.re = sqrt_newton(d), .im = 0.0};
    }
    return (Complex){.re = 0.0, .im = sqrt_newton(-(d))};
}

Complex * quadratic_roots(double a, double b, double c) {
    if (a == 0.0) {
        puts("ValueError: coefficient 'a' must not be zero");
        {
            Complex* __tmp0_data = malloc(0 * sizeof(Complex));
            quadratic_roots_len = 0;
            return __tmp0_data;
        }
    }
    double delta = (b * b) - ((4.0 * a) * c);
    Complex sqrt_d = sqrt_to_complex(delta);
    Complex minus_b = (Complex){.re = -(b), .im = 0.0};
    double two_a = 2.0 * a;
    Complex root1 = div_real(add(minus_b, sqrt_d), two_a);
    Complex root2 = div_real(sub(minus_b, sqrt_d), two_a);
    {
        Complex* __tmp1_data = malloc(2 * sizeof(Complex));
        __tmp1_data[0] = root1;
        __tmp1_data[1] = root2;
        quadratic_roots_len = 2;
        return __tmp1_data;
    }
}

const char* root_str(Complex r) {
    if (r.im == 0.0) {
        return str_float(r.re);
    }
    const char* s = str_float(r.re);
    if (r.im >= 0.0) {
        s = str_concat(str_concat(str_concat(s, "+"), str_float(r.im)), "i");
    } else {
        s = str_concat(str_concat(s, str_float(r.im)), "i");
    }
    return s;
}

void user_main() {
    Complex *roots = quadratic_roots(5.0, 6.0, 1.0);
    size_t roots_len = quadratic_roots_len;
    if (roots_len == 2LL) {
        puts(str_concat(str_concat(str_concat("The solutions are: ", root_str(roots[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? roots_len + _mochi_idx : _mochi_idx;})])), " and "), root_str(roots[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? roots_len + _mochi_idx : _mochi_idx;})])));
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
