// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sieve_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long min_int(long long a, long long b);
long long int_sqrt(long long n);
long long * sieve(long long n);
long long lists_equal(long long * a, size_t a_len, long long * b, size_t b_len);
void test_sieve();
void user_main();
int main(void);

long long min_int(long long a, long long b) {
    if (a < b) {
        return a;
    }
    return b;
}

long long int_sqrt(long long n) {
    long long r = 0LL;
    while (((r + 1LL) * (r + 1LL)) <= n) {
        r = r + 1LL;
    }
    return r;
}

long long * sieve(long long n) {
    if (n <= 0LL) {
        panic("Number must instead be a positive integer");
    }
    long long *in_prime = NULL;
    size_t in_prime_len = 0;
    long long start = 2LL;
    long long end = int_sqrt(n);
    long long *temp = NULL;
    size_t temp_len = 0;
    long long i = 0LL;
    while (i < (end + 1LL)) {
        temp = list_append_long_long(temp, &temp_len, 1LL);
        i = i + 1LL;
    }
    long long *prime = NULL;
    size_t prime_len = 0;
    while (start <= end) {
        if (temp[(int)({long long _mochi_idx = start; _mochi_idx < 0 ? temp_len + _mochi_idx : _mochi_idx;})] == 1LL) {
            in_prime = list_append_long_long(in_prime, &in_prime_len, start);
            long long j = start * start;
            while (j <= end) {
                temp[(int)(j)] = 0LL;
                j = j + start;
            }
        }
        start = start + 1LL;
    }
    i = 0LL;
    while (i < in_prime_len) {
        prime = list_append_long_long(prime, &prime_len, in_prime[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? in_prime_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long low = end + 1LL;
    long long high = min_int(2LL * end, n);
    while (low <= n) {
        long long *tempSeg = NULL;
        size_t tempSeg_len = 0;
        long long size = (high - low) + 1LL;
        long long k = 0LL;
        while (k < size) {
            tempSeg = list_append_long_long(tempSeg, &tempSeg_len, 1LL);
            k = k + 1LL;
        }
        long long idx = 0LL;
        while (idx < in_prime_len) {
            long long each = in_prime[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? in_prime_len + _mochi_idx : _mochi_idx;})];
            long long t = (low / each) * each;
            if (t < low) {
                t = t + each;
            }
            long long j2 = t;
            while (j2 <= high) {
                tempSeg[(int)(j2 - low)] = 0LL;
                j2 = j2 + each;
            }
            idx = idx + 1LL;
        }
        long long j3 = 0LL;
        while (j3 < tempSeg_len) {
            if (tempSeg[(int)({long long _mochi_idx = j3; _mochi_idx < 0 ? tempSeg_len + _mochi_idx : _mochi_idx;})] == 1LL) {
                prime = list_append_long_long(prime, &prime_len, j3 + low);
            }
            j3 = j3 + 1LL;
        }
        low = high + 1LL;
        high = min_int(high + end, n);
    }
    return sieve_len = prime_len, prime;
}

long long lists_equal(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long m = 0LL;
    while (m < a_len) {
        if (a[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        m = m + 1LL;
    }
    return 1LL;
}

void test_sieve() {
    long long *e1 = sieve(8LL);
    size_t e1_len = sieve_len;
    if (!(lists_equal(e1, e1_len, (long long[]){2LL, 3LL, 5LL, 7LL}, 4))) {
        panic("sieve(8) failed");
    }
    long long *e2 = sieve(27LL);
    size_t e2_len = sieve_len;
    if (!(lists_equal(e2, e2_len, (long long[]){2LL, 3LL, 5LL, 7LL, 11LL, 13LL, 17LL, 19LL, 23LL}, 9))) {
        panic("sieve(27) failed");
    }
}

void user_main() {
    test_sieve();
    puts(({long long *__tmp1 = sieve(30LL); char *__res = str_list_int(__tmp1, sieve_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
