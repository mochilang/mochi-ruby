// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double pow_float(double base, long long exponent);
double evaluate_poly(double * poly, size_t poly_len, double x);
double horner(double * poly, size_t poly_len, double x);
void test_polynomial_evaluation();
void user_main();
int main(void);

double pow_float(double base, long long exponent) {
    long long exp = exponent;
    double result = 1.0;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double evaluate_poly(double * poly, size_t poly_len, double x) {
    double total = 0.0;
    long long i = 0LL;
    while (i < poly_len) {
        total = total + (poly[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? poly_len + _mochi_idx : _mochi_idx;})] * pow_float(x, i));
        i = i + 1LL;
    }
    return total;
}

double horner(double * poly, size_t poly_len, double x) {
    double result = 0.0;
    long long i = poly_len - 1LL;
    while (i >= 0LL) {
        result = (result * x) + poly[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? poly_len + _mochi_idx : _mochi_idx;})];
        i = i - 1LL;
    }
    return result;
}

void test_polynomial_evaluation() {
    double *poly = NULL;
    size_t poly_len = 0;
    poly = list_append_double(poly, &poly_len, 0.0);
    poly = list_append_double(poly, &poly_len, 0.0);
    poly = list_append_double(poly, &poly_len, 5.0);
    poly = list_append_double(poly, &poly_len, 9.3);
    poly = list_append_double(poly, &poly_len, 7.0);
    double x = 10.0;
    if (evaluate_poly(poly, poly_len, x) != 79800.0) {
        panic("evaluate_poly failed");
    }
    if (horner(poly, poly_len, x) != 79800.0) {
        panic("horner failed");
    }
}

void user_main() {
    test_polynomial_evaluation();
    double *poly = NULL;
    size_t poly_len = 0;
    poly = list_append_double(poly, &poly_len, 0.0);
    poly = list_append_double(poly, &poly_len, 0.0);
    poly = list_append_double(poly, &poly_len, 5.0);
    poly = list_append_double(poly, &poly_len, 9.3);
    poly = list_append_double(poly, &poly_len, 7.0);
    double x = 10.0;
    printf("%g\n", evaluate_poly(poly, poly_len, x));
    printf("%g\n", horner(poly, poly_len, x));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
