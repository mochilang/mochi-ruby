// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t two_pointer_len;

size_t append_len;
static int list_eq_int(const long long a[], size_t alen, const long long b[], size_t blen) {
    if (alen != blen) return 0;
    for (size_t i = 0; i < alen; i++) {
        if (a[i] != b[i]) return 0;
    }
    return 1;
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long * two_pointer(long long * nums, size_t nums_len, long long target);
void test_two_pointer();
void user_main();
int main(void);

long long * two_pointer(long long * nums, size_t nums_len, long long target) {
    long long i = 0LL;
    long long j = nums_len - 1LL;
    while (i < j) {
        long long s = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] + nums[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (s == target) {
            {
                long long* __tmp21_data = malloc(2 * sizeof(long long));
                __tmp21_data[0] = i;
                __tmp21_data[1] = j;
                two_pointer_len = 2;
                return __tmp21_data;
            }
        }
        if (s < target) {
            i = i + 1LL;
        } else {
            j = j - 1LL;
        }
    }
    {
        long long* __tmp22_data = malloc(0 * sizeof(long long));
        two_pointer_len = 0;
        return __tmp22_data;
    }
}

void test_two_pointer() {
    if (({long long* __tmp23 = two_pointer(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 7LL; tmp[2] = 11LL; tmp[3] = 15LL; tmp;}), 4, 9LL); size_t __tmp24 = two_pointer_len; long long* __tmp25 = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 1LL; tmp;}); size_t __tmp26 = 2; list_eq_int(__tmp23, __tmp24, __tmp25, __tmp26);}) == 0) {
        panic("case1");
    }
    if (({long long* __tmp27 = two_pointer(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 7LL; tmp[2] = 11LL; tmp[3] = 15LL; tmp;}), 4, 17LL); size_t __tmp28 = two_pointer_len; long long* __tmp29 = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 3LL; tmp;}); size_t __tmp30 = 2; list_eq_int(__tmp27, __tmp28, __tmp29, __tmp30);}) == 0) {
        panic("case2");
    }
    if (({long long* __tmp31 = two_pointer(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 7LL; tmp[2] = 11LL; tmp[3] = 15LL; tmp;}), 4, 18LL); size_t __tmp32 = two_pointer_len; long long* __tmp33 = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp;}); size_t __tmp34 = 2; list_eq_int(__tmp31, __tmp32, __tmp33, __tmp34);}) == 0) {
        panic("case3");
    }
    if (({long long* __tmp35 = two_pointer(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 7LL; tmp[2] = 11LL; tmp[3] = 15LL; tmp;}), 4, 26LL); size_t __tmp36 = two_pointer_len; long long* __tmp37 = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp;}); size_t __tmp38 = 2; list_eq_int(__tmp35, __tmp36, __tmp37, __tmp38);}) == 0) {
        panic("case4");
    }
    if (({long long* __tmp39 = two_pointer(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 3LL; tmp[2] = 3LL; tmp;}), 3, 6LL); size_t __tmp40 = two_pointer_len; long long* __tmp41 = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp;}); size_t __tmp42 = 2; list_eq_int(__tmp39, __tmp40, __tmp41, __tmp42);}) == 0) {
        panic("case5");
    }
    if (0LL) {
        panic("case6");
    }
    if (0LL) {
        panic("case7");
    }
    if (0LL) {
        panic("case8");
    }
}

void user_main() {
    test_two_pointer();
    long long* __tmp43 = two_pointer(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 7LL; tmp[2] = 11LL; tmp[3] = 15LL; tmp;}), 4, 9LL);
    puts(str_list_int(__tmp43, two_pointer_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
