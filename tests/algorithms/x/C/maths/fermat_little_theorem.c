// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long p = 701LL;
long long a = 1000000000LL;
long long b = 10LL;
long long left = 0;
long long right_fast = 0;
long long right_naive = 0;

long long binary_exponentiation(long long a, long long n, long long mod);
long long naive_exponent_mod(long long a, long long n, long long mod);
void print_bool(long long b);
int main(void);

long long binary_exponentiation(long long a, long long n, long long mod) {
    if (n == 0LL) {
        return 1LL;
    }
    if ((n % 2LL) == 1LL) {
        return (binary_exponentiation(a, n - 1LL, mod) * a) % mod;
    }
    long long b = binary_exponentiation(a, n / 2LL, mod);
    return (b * b) % mod;
}

long long naive_exponent_mod(long long a, long long n, long long mod) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < n) {
        result = (result * a) % mod;
        i = i + 1LL;
    }
    return result;
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        left = (a / b) % p;
        right_fast = (a * binary_exponentiation(b, p - 2LL, p)) % p;
        print_bool(left == right_fast);
        right_naive = (a * naive_exponent_mod(b, p - 2LL, p)) % p;
        print_bool(left == right_naive);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
