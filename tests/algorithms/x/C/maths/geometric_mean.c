// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double user_abs(double x);
double pow_int(double base, long long exp);
double nth_root(double x, long long n);
double round_nearest(double x);
double compute_geometric_mean(double * nums, size_t nums_len);
void test_compute_geometric_mean();
void user_main();
int main(void);

double user_abs(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double pow_int(double base, long long exp) {
    double result = 1.0;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double nth_root(double x, long long n) {
    if (x == 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        double denom = pow_int(guess, n - 1LL);
        guess = (((n - 1LL) * guess) + (x / denom)) / (double)(n);
        i = i + 1LL;
    }
    return guess;
}

double round_nearest(double x) {
    if (x >= 0.0) {
        long long n = x + 0.5;
        return (double)(n);
    }
    long long n = x - 0.5;
    return (double)(n);
}

double compute_geometric_mean(double * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("no numbers");
    }
    double product = 1.0;
    long long i = 0LL;
    while (i < nums_len) {
        product = product * nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    if ((product < 0.0) && ((nums_len % 2LL) == 0LL)) {
        panic("Cannot Compute Geometric Mean for these numbers.");
    }
    double mean = nth_root(user_abs(product), nums_len);
    if (product < 0.0) {
        mean = -(mean);
    }
    double possible = round_nearest(mean);
    if (pow_int(possible, nums_len) == product) {
        mean = possible;
    }
    return mean;
}

void test_compute_geometric_mean() {
    double eps = 0.0001;
    double m1 = compute_geometric_mean(({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 8.0; tmp;}), 2);
    if (user_abs(m1 - 4.0) > eps) {
        panic("test1 failed");
    }
    double m2 = compute_geometric_mean(({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 5.0; tmp[1] = 125.0; tmp;}), 2);
    if (user_abs(m2 - 25.0) > eps) {
        panic("test2 failed");
    }
    double m3 = compute_geometric_mean(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 0.0; tmp;}), 2);
    if (user_abs(m3 - 0.0) > eps) {
        panic("test3 failed");
    }
    double m4 = compute_geometric_mean(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 5.0; tmp[2] = 25.0; tmp[3] = 5.0; tmp;}), 4);
    if (user_abs(m4 - 5.0) > eps) {
        panic("test4 failed");
    }
    double m5 = compute_geometric_mean(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -(5.0); tmp[1] = 25.0; tmp[2] = 1.0; tmp;}), 3);
    if (user_abs(m5 + 5.0) > eps) {
        panic("test5 failed");
    }
}

void user_main() {
    test_compute_geometric_mean();
    printf("%g\n", compute_geometric_mean(({double *tmp = malloc(2 * sizeof(double)); tmp[0] = -(3.0); tmp[1] = -(27.0); tmp;}), 2));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
