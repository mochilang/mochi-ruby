// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double find_min_iterative(double * nums, size_t nums_len);
double find_min_recursive(double * nums, size_t nums_len, long long left, long long right);
void test_find_min();
void user_main();
int main(void);

double find_min_iterative(double * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("find_min_iterative() arg is an empty sequence");
    }
    double min_num = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    long long i = 0LL;
    while (i < nums_len) {
        double num = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (num < min_num) {
            min_num = num;
        }
        i = i + 1LL;
    }
    return min_num;
}

double find_min_recursive(double * nums, size_t nums_len, long long left, long long right) {
    long long n = nums_len;
    if (n == 0LL) {
        panic("find_min_recursive() arg is an empty sequence");
    }
    if ((((left >= n) || (left < (0LL - n))) || (right >= n)) || (right < (0LL - n))) {
        panic("list index out of range");
    }
    long long l = left;
    long long r = right;
    if (l < 0LL) {
        l = n + l;
    }
    if (r < 0LL) {
        r = n + r;
    }
    if (l == r) {
        return nums[(int)({long long _mochi_idx = l; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    }
    long long mid = (l + r) / 2LL;
    double left_min = find_min_recursive(nums, nums_len, l, mid);
    double right_min = find_min_recursive(nums, nums_len, mid + 1LL, r);
    if (left_min <= right_min) {
        return left_min;
    }
    return right_min;
}

void test_find_min() {
    double *a = NULL;
    size_t a_len = 0;
    a = list_append_double(a, &a_len, 3.0);
    a = list_append_double(a, &a_len, 2.0);
    a = list_append_double(a, &a_len, 1.0);
    if (find_min_iterative(a, a_len) != 1.0) {
        panic("iterative test1 failed");
    }
    if (find_min_recursive(a, a_len, 0LL, 2LL) != 1.0) {
        panic("recursive test1 failed");
    }
    double *b = NULL;
    size_t b_len = 0;
    b = list_append_double(b, &b_len, -(3.0));
    b = list_append_double(b, &b_len, -(2.0));
    b = list_append_double(b, &b_len, -(1.0));
    if (find_min_iterative(b, b_len) != -(3.0)) {
        panic("iterative test2 failed");
    }
    if (find_min_recursive(b, b_len, 0LL, 2LL) != -(3.0)) {
        panic("recursive test2 failed");
    }
    double *c = NULL;
    size_t c_len = 0;
    c = list_append_double(c, &c_len, 3.0);
    c = list_append_double(c, &c_len, -(3.0));
    c = list_append_double(c, &c_len, 0.0);
    if (find_min_iterative(c, c_len) != -(3.0)) {
        panic("iterative test3 failed");
    }
    if (find_min_recursive(c, c_len, 0LL, 2LL) != -(3.0)) {
        panic("recursive test3 failed");
    }
    double *d = NULL;
    size_t d_len = 0;
    d = list_append_double(d, &d_len, 1.0);
    d = list_append_double(d, &d_len, 3.0);
    d = list_append_double(d, &d_len, 5.0);
    d = list_append_double(d, &d_len, 7.0);
    d = list_append_double(d, &d_len, 9.0);
    d = list_append_double(d, &d_len, 2.0);
    d = list_append_double(d, &d_len, 4.0);
    d = list_append_double(d, &d_len, 6.0);
    d = list_append_double(d, &d_len, 8.0);
    d = list_append_double(d, &d_len, 10.0);
    if (find_min_recursive(d, d_len, -10LL, -1LL) != 1.0) {
        panic("negative index test failed");
    }
}

void user_main() {
    test_find_min();
    double *sample = NULL;
    size_t sample_len = 0;
    sample = list_append_double(sample, &sample_len, 0.0);
    sample = list_append_double(sample, &sample_len, 1.0);
    sample = list_append_double(sample, &sample_len, 2.0);
    sample = list_append_double(sample, &sample_len, 3.0);
    sample = list_append_double(sample, &sample_len, 4.0);
    sample = list_append_double(sample, &sample_len, 5.0);
    sample = list_append_double(sample, &sample_len, -(3.0));
    sample = list_append_double(sample, &sample_len, 24.0);
    sample = list_append_double(sample, &sample_len, -(56.0));
    puts(str_float(find_min_iterative(sample, sample_len)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
