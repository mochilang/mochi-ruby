// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long contains(const char* * xs, size_t xs_len, const char* value);
double jaccard_similarity(const char* * set_a, size_t set_a_len, const char* * set_b, size_t set_b_len, long long alternative_union);
void user_main();
int main(void);

long long contains(const char* * xs, size_t xs_len, const char* value) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})], value) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

double jaccard_similarity(const char* * set_a, size_t set_a_len, const char* * set_b, size_t set_b_len, long long alternative_union) {
    long long intersection_len = 0LL;
    long long i = 0LL;
    while (i < set_a_len) {
        if (contains(set_b, set_b_len, set_a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? set_a_len + _mochi_idx : _mochi_idx;})])) {
            intersection_len = intersection_len + 1LL;
        }
        i = i + 1LL;
    }
    long long union_len = 0LL;
    if (alternative_union) {
        union_len = set_a_len + set_b_len;
    } else {
        const char* *union_list = NULL;
        size_t union_list_len = 0;
        i = 0LL;
        while (i < set_a_len) {
            const char* val_a = set_a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? set_a_len + _mochi_idx : _mochi_idx;})];
            if (!(contains(union_list, union_list_len, val_a))) {
                union_list = list_append_str(union_list, &union_list_len, val_a);
            }
            i = i + 1LL;
        }
        i = 0LL;
        while (i < set_b_len) {
            const char* val_b = set_b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? set_b_len + _mochi_idx : _mochi_idx;})];
            if (!(contains(union_list, union_list_len, val_b))) {
                union_list = list_append_str(union_list, &union_list_len, val_b);
            }
            i = i + 1LL;
        }
        union_len = union_list_len;
    }
    return (1.0 * intersection_len) / union_len;
}

void user_main() {
    const char* *set_a = NULL;
    size_t set_a_len = 0;
    set_a = list_append_str(set_a, &set_a_len, "a");
    set_a = list_append_str(set_a, &set_a_len, "b");
    set_a = list_append_str(set_a, &set_a_len, "c");
    set_a = list_append_str(set_a, &set_a_len, "d");
    set_a = list_append_str(set_a, &set_a_len, "e");
    const char* *set_b = NULL;
    size_t set_b_len = 0;
    set_b = list_append_str(set_b, &set_b_len, "c");
    set_b = list_append_str(set_b, &set_b_len, "d");
    set_b = list_append_str(set_b, &set_b_len, "e");
    set_b = list_append_str(set_b, &set_b_len, "f");
    set_b = list_append_str(set_b, &set_b_len, "h");
    set_b = list_append_str(set_b, &set_b_len, "i");
    printf("%g\n", jaccard_similarity(set_a, set_a_len, set_b, set_b_len, 0LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
