// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long MAX = 4294967296LL;
long long HALF = 2147483648LL;

long long to_unsigned(long long n);
long long from_unsigned(long long n);
long long bit_and(long long a, long long b);
long long bit_xor(long long a, long long b);
long long lshift1(long long num);
long long add(long long a, long long b);
int main(void);

long long to_unsigned(long long n) {
    if (n < 0LL) {
        return MAX + n;
    }
    return n;
}

long long from_unsigned(long long n) {
    if (n >= HALF) {
        return n - MAX;
    }
    return n;
}

long long bit_and(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    long long i = 0LL;
    while (i < 32LL) {
        if (((x % 2LL) == 1LL) && ((y % 2LL) == 1LL)) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long bit_xor(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    long long i = 0LL;
    while (i < 32LL) {
        long long abit = x % 2LL;
        long long bbit = y % 2LL;
        if (((abit + bbit) % 2LL) == 1LL) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long lshift1(long long num) {
    return (num * 2LL) % MAX;
}

long long add(long long a, long long b) {
    long long first = to_unsigned(a);
    long long second = to_unsigned(b);
    while (second != 0LL) {
        long long carry = bit_and(first, second);
        first = bit_xor(first, second);
        second = lshift1(carry);
    }
    long long result = from_unsigned(first);
    return result;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(add(3LL, 5LL)));
        puts(str_int(add(13LL, 5LL)));
        puts(str_int(add(-7LL, 2LL)));
        puts(str_int(add(0LL, -7LL)));
        puts(str_int(add(-321LL, 0LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
