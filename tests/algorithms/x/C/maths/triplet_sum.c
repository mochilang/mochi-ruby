// Generated by Mochi 0.10.32 on 2025-08-17 14:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

size_t bubble_sort_len;
size_t sort3_len;
size_t triplet_sum1_len;
size_t triplet_sum2_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * bubble_sort(long long * nums, size_t nums_len);
long long * sort3(long long * xs, size_t xs_len);
long long * triplet_sum1(long long * arr, size_t arr_len, long long target);
long long * triplet_sum2(long long * arr, size_t arr_len, long long target);
long long list_equal(long long * a, size_t a_len, long long * b, size_t b_len);
void test_triplet_sum();
void user_main();
int main(void);

long long * bubble_sort(long long * nums, size_t nums_len) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < nums_len) {
        arr = list_append_long_long(arr, &arr_len, nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long n = arr_len;
    long long a = 0LL;
    while (a < n) {
        long long b = 0LL;
        while (b < ((n - a) - 1LL)) {
            if (arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(b)] = arr[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(b + 1LL)] = tmp;
            }
            b = b + 1LL;
        }
        a = a + 1LL;
    }
    return bubble_sort_len = arr_len, arr;
}

long long * sort3(long long * xs, size_t xs_len) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        arr = list_append_long_long(arr, &arr_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long n = arr_len;
    long long a = 0LL;
    while (a < n) {
        long long b = 0LL;
        while (b < ((n - a) - 1LL)) {
            if (arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(b)] = arr[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(b + 1LL)] = tmp;
            }
            b = b + 1LL;
        }
        a = a + 1LL;
    }
    return sort3_len = arr_len, arr;
}

long long * triplet_sum1(long long * arr, size_t arr_len, long long target) {
    long long i = 0LL;
    while (i < (arr_len - 2LL)) {
        long long j = i + 1LL;
        while (j < (arr_len - 1LL)) {
            long long k = j + 1LL;
            while (k < arr_len) {
                if (((arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] + arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) + arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) == target) {
                    {
                        long long* __ret6 = sort3(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]; tmp[1] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]; tmp[2] = arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]; tmp;}), 3);
                        triplet_sum1_len = sort3_len;
                        return __ret6;
                    }
                }
                k = k + 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    {
        long long* __tmp7_data = malloc(3 * sizeof(long long));
        __tmp7_data[0] = 0LL;
        __tmp7_data[1] = 0LL;
        __tmp7_data[2] = 0LL;
        triplet_sum1_len = 3;
        return __tmp7_data;
    }
}

long long * triplet_sum2(long long * arr, size_t arr_len, long long target) {
    long long *sorted = bubble_sort(arr, arr_len);
    size_t sorted_len = bubble_sort_len;
    long long n = sorted_len;
    long long i = 0LL;
    while (i < (n - 2LL)) {
        long long left = i + 1LL;
        long long right = n - 1LL;
        while (left < right) {
            long long s = (sorted[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})] + sorted[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})]) + sorted[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
            if (s == target) {
                {
                    long long* __tmp8_data = malloc(3 * sizeof(long long));
                    __tmp8_data[0] = sorted[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
                    __tmp8_data[1] = sorted[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
                    __tmp8_data[2] = sorted[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
                    triplet_sum2_len = 3;
                    return __tmp8_data;
                }
            }
            if (s < target) {
                left = left + 1LL;
            } else {
                right = right - 1LL;
            }
        }
        i = i + 1LL;
    }
    {
        long long* __tmp9_data = malloc(3 * sizeof(long long));
        __tmp9_data[0] = 0LL;
        __tmp9_data[1] = 0LL;
        __tmp9_data[2] = 0LL;
        triplet_sum2_len = 3;
        return __tmp9_data;
    }
}

long long list_equal(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void test_triplet_sum() {
    long long *arr1 = NULL;
    size_t arr1_len = 0;
    arr1 = list_append_long_long(arr1, &arr1_len, 13LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 29LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 7LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 23LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 5LL);
    if (!(({long long* __tmp10 = triplet_sum1(arr1, arr1_len, 35LL); size_t __tmp10_len = triplet_sum1_len; list_equal(__tmp10, __tmp10_len, (long long[]){5LL, 7LL, 23LL}, 3);}))) {
        panic("ts1 case1 failed");
    }
    if (!(({long long* __tmp11 = triplet_sum2(arr1, arr1_len, 35LL); size_t __tmp11_len = triplet_sum2_len; list_equal(__tmp11, __tmp11_len, (long long[]){5LL, 7LL, 23LL}, 3);}))) {
        panic("ts2 case1 failed");
    }
    long long *arr2 = NULL;
    size_t arr2_len = 0;
    arr2 = list_append_long_long(arr2, &arr2_len, 37LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 9LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 19LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 50LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 44LL);
    if (!(({long long* __tmp12 = triplet_sum1(arr2, arr2_len, 65LL); size_t __tmp12_len = triplet_sum1_len; list_equal(__tmp12, __tmp12_len, (long long[]){9LL, 19LL, 37LL}, 3);}))) {
        panic("ts1 case2 failed");
    }
    if (!(({long long* __tmp13 = triplet_sum2(arr2, arr2_len, 65LL); size_t __tmp13_len = triplet_sum2_len; list_equal(__tmp13, __tmp13_len, (long long[]){9LL, 19LL, 37LL}, 3);}))) {
        panic("ts2 case2 failed");
    }
    long long *arr3 = NULL;
    size_t arr3_len = 0;
    arr3 = list_append_long_long(arr3, &arr3_len, 6LL);
    arr3 = list_append_long_long(arr3, &arr3_len, 47LL);
    arr3 = list_append_long_long(arr3, &arr3_len, 27LL);
    arr3 = list_append_long_long(arr3, &arr3_len, 1LL);
    arr3 = list_append_long_long(arr3, &arr3_len, 15LL);
    if (!(({long long* __tmp14 = triplet_sum1(arr3, arr3_len, 11LL); size_t __tmp14_len = triplet_sum1_len; list_equal(__tmp14, __tmp14_len, (long long[]){0LL, 0LL, 0LL}, 3);}))) {
        panic("ts1 case3 failed");
    }
    if (!(({long long* __tmp15 = triplet_sum2(arr3, arr3_len, 11LL); size_t __tmp15_len = triplet_sum2_len; list_equal(__tmp15, __tmp15_len, (long long[]){0LL, 0LL, 0LL}, 3);}))) {
        panic("ts2 case3 failed");
    }
}

void user_main() {
    test_triplet_sum();
    long long *sample = NULL;
    size_t sample_len = 0;
    sample = list_append_long_long(sample, &sample_len, 13LL);
    sample = list_append_long_long(sample, &sample_len, 29LL);
    sample = list_append_long_long(sample, &sample_len, 7LL);
    sample = list_append_long_long(sample, &sample_len, 23LL);
    sample = list_append_long_long(sample, &sample_len, 5LL);
    long long *res = triplet_sum2(sample, sample_len, 35LL);
    size_t res_len = triplet_sum2_len;
    puts(str_concat(str_concat(str_concat(str_concat(str_int(res[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})]), " "), str_int(res[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})])), " "), str_int(res[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})])));
}

int main(void) {
    user_main();
    return 0;
}
