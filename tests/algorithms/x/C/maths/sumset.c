// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sumset_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long contains(long long * xs, size_t xs_len, long long value);
long long * sumset(long long * set_a, size_t set_a_len, long long * set_b, size_t set_b_len);
void user_main();
int main(void);

long long contains(long long * xs, size_t xs_len, long long value) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == value) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long * sumset(long long * set_a, size_t set_a_len, long long * set_b, size_t set_b_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < set_a_len) {
        long long j = 0LL;
        while (j < set_b_len) {
            long long s = set_a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? set_a_len + _mochi_idx : _mochi_idx;})] + set_b[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? set_b_len + _mochi_idx : _mochi_idx;})];
            if (!(contains(result, result_len, s))) {
                result = list_append_long_long(result, &result_len, s);
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sumset_len = result_len, result;
}

void user_main() {
    long long *set_a = NULL;
    size_t set_a_len = 0;
    set_a = list_append_long_long(set_a, &set_a_len, 1LL);
    set_a = list_append_long_long(set_a, &set_a_len, 2LL);
    set_a = list_append_long_long(set_a, &set_a_len, 3LL);
    long long *set_b = NULL;
    size_t set_b_len = 0;
    set_b = list_append_long_long(set_b, &set_b_len, 4LL);
    set_b = list_append_long_long(set_b, &set_b_len, 5LL);
    set_b = list_append_long_long(set_b, &set_b_len, 6LL);
    puts(({long long *__tmp2 = sumset(set_a, set_a_len, set_b, set_b_len); char *__res = str_list_int(__tmp2, sumset_len); __res;}));
    long long *set_c = NULL;
    size_t set_c_len = 0;
    set_c = list_append_long_long(set_c, &set_c_len, 4LL);
    set_c = list_append_long_long(set_c, &set_c_len, 5LL);
    set_c = list_append_long_long(set_c, &set_c_len, 6LL);
    set_c = list_append_long_long(set_c, &set_c_len, 7LL);
    puts(({long long *__tmp3 = sumset(set_a, set_a_len, set_c, set_c_len); char *__res = str_list_int(__tmp3, sumset_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
