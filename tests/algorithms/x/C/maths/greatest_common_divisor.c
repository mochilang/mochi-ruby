// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_bool(long long v) {
    return strdup(v ? "true" : "false");
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long abs_int(long long n);
long long greatest_common_divisor(long long a, long long b);
long long gcd_by_iterative(long long x, long long y);
int main(void);

long long abs_int(long long n) {
    if (n < 0LL) {
        return -(n);
    }
    return n;
}

long long greatest_common_divisor(long long a, long long b) {
    long long x = abs_int(a);
    long long y = abs_int(b);
    if (x == 0LL) {
        return y;
    }
    return greatest_common_divisor(y % x, x);
}

long long gcd_by_iterative(long long x, long long y) {
    long long a = abs_int(x);
    long long b = abs_int(y);
    while (b != 0LL) {
        long long temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(greatest_common_divisor(24LL, 40LL)));
        puts(str_int(greatest_common_divisor(1LL, 1LL)));
        puts(str_int(greatest_common_divisor(1LL, 800LL)));
        puts(str_int(greatest_common_divisor(11LL, 37LL)));
        puts(str_int(greatest_common_divisor(3LL, 5LL)));
        puts(str_int(greatest_common_divisor(16LL, 4LL)));
        puts(str_int(greatest_common_divisor(-3LL, 9LL)));
        puts(str_int(greatest_common_divisor(9LL, -3LL)));
        puts(str_int(greatest_common_divisor(3LL, -9LL)));
        puts(str_int(greatest_common_divisor(-3LL, -9LL)));
        puts(str_int(gcd_by_iterative(24LL, 40LL)));
        printf("%d\n", str_bool(greatest_common_divisor(24LL, 40LL) == gcd_by_iterative(24LL, 40LL)));
        puts(str_int(gcd_by_iterative(-3LL, -9LL)));
        puts(str_int(gcd_by_iterative(3LL, -9LL)));
        puts(str_int(gcd_by_iterative(1LL, -800LL)));
        puts(str_int(gcd_by_iterative(11LL, 37LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
