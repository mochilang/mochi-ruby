// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long seed = 1LL;

void set_seed(long long s);
long long randint(long long a, long long b);
long long jacobi_symbol(long long random_a, long long number);
long long pow_mod(long long base, long long exp, long long mod);
long long solovay_strassen(long long number, long long iterations);
void user_main();
int main(void);

void set_seed(long long s) {
    seed = s;
}

long long randint(long long a, long long b) {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return (seed % ((b - a) + 1LL)) + a;
}

long long jacobi_symbol(long long random_a, long long number) {
    if ((random_a == 0LL) || (random_a == 1LL)) {
        return random_a;
    }
    random_a = random_a % number;
    long long t = 1LL;
    while (random_a != 0LL) {
        while ((random_a % 2LL) == 0LL) {
            random_a = random_a / 2LL;
            long long r = number % 8LL;
            if ((r == 3LL) || (r == 5LL)) {
                t = -(t);
            }
        }
        long long temp = random_a;
        random_a = number;
        number = temp;
        if (((random_a % 4LL) == 3LL) && ((number % 4LL) == 3LL)) {
            t = -(t);
        }
        random_a = random_a % number;
    }
    if (number == 1LL) {
        return t;
    }
    return 0LL;
}

long long pow_mod(long long base, long long exp, long long mod) {
    long long result = 1LL;
    long long b = base % mod;
    long long e = exp;
    while (e > 0LL) {
        if ((e % 2LL) == 1LL) {
            result = (result * b) % mod;
        }
        b = (b * b) % mod;
        e = e / 2LL;
    }
    return result;
}

long long solovay_strassen(long long number, long long iterations) {
    if (number <= 1LL) {
        return 0LL;
    }
    if (number <= 3LL) {
        return 1LL;
    }
    long long i = 0LL;
    while (i < iterations) {
        long long a = randint(2LL, number - 2LL);
        long long x = jacobi_symbol(a, number);
        long long y = pow_mod(a, (number - 1LL) / 2LL, number);
        long long mod_x = x % number;
        if (mod_x < 0LL) {
            mod_x = mod_x + number;
        }
        if ((x == 0LL) || (y != mod_x)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void user_main() {
    set_seed(10LL);
    puts(str_int(solovay_strassen(13LL, 5LL)));
    puts(str_int(solovay_strassen(9LL, 10LL)));
    puts(str_int(solovay_strassen(17LL, 15LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
