// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double *nums = NULL;
size_t nums_len = 0;
long long _ = 0;

long long check_polygon(double * nums, size_t nums_len);
int main(void);

long long check_polygon(double * nums, size_t nums_len) {
    if (nums_len < 2LL) {
        error("Monogons and Digons are not polygons in the Euclidean space");
    }
    long long i = 0LL;
    while (i < nums_len) {
        if (nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})] <= 0.0) {
            error("All values must be greater than 0");
        }
        i = i + 1LL;
    }
    double total = 0.0;
    double max_side = 0.0;
    i = 0LL;
    while (i < nums_len) {
        double v = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        total = total + v;
        if (v > max_side) {
            max_side = v;
        }
        i = i + 1LL;
    }
    return max_side < (total - max_side);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(check_polygon(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 6.0; tmp[1] = 10.0; tmp[2] = 5.0; tmp;}), 3)));
        puts(str_int(check_polygon(({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 3.0; tmp[1] = 7.0; tmp[2] = 13.0; tmp[3] = 2.0; tmp;}), 4)));
        puts(str_int(check_polygon(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 4.3; tmp[2] = 5.2; tmp[3] = 12.2; tmp;}), 4)));
        nums = ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 3.0; tmp[1] = 7.0; tmp[2] = 13.0; tmp[3] = 2.0; tmp;});
        nums_len = 4;
        _ = check_polygon(nums, nums_len);
        puts(str_list_double(nums, nums_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
