// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long user_floor(double x);
void test_floor();
void user_main();
int main(void);

long long user_floor(double x) {
    long long i = (int)(x);
    if ((x - (double)(i)) >= 0.0) {
        return i;
    }
    return i - 1LL;
}

void test_floor() {
    double *nums = NULL;
    size_t nums_len = 0;
    nums = list_append_double(nums, &nums_len, 1.0);
    nums = list_append_double(nums, &nums_len, -(1.0));
    nums = list_append_double(nums, &nums_len, 0.0);
    nums = list_append_double(nums, &nums_len, 0.0);
    nums = list_append_double(nums, &nums_len, 1.1);
    nums = list_append_double(nums, &nums_len, -(1.1));
    nums = list_append_double(nums, &nums_len, 1.0);
    nums = list_append_double(nums, &nums_len, -(1.0));
    nums = list_append_double(nums, &nums_len, 1000000000.0);
    long long *expected = NULL;
    size_t expected_len = 0;
    expected = list_append_long_long(expected, &expected_len, 1LL);
    expected = list_append_long_long(expected, &expected_len, -1LL);
    expected = list_append_long_long(expected, &expected_len, 0LL);
    expected = list_append_long_long(expected, &expected_len, 0LL);
    expected = list_append_long_long(expected, &expected_len, 1LL);
    expected = list_append_long_long(expected, &expected_len, -2LL);
    expected = list_append_long_long(expected, &expected_len, 1LL);
    expected = list_append_long_long(expected, &expected_len, -1LL);
    expected = list_append_long_long(expected, &expected_len, 1000000000LL);
    long long idx = 0LL;
    while (idx < 9LL) {
        if (user_floor(nums[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]) != expected[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? expected_len + _mochi_idx : _mochi_idx;})]) {
            panic("floor test failed");
        }
        idx = idx + 1LL;
    }
}

void user_main() {
    test_floor();
    puts(str_int(user_floor(-(1.1))));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
