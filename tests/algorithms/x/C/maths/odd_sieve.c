// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t odd_sieve_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * odd_sieve(long long num);
int main(void);

long long * odd_sieve(long long num) {
    if (num <= 2LL) {
        {
            long long* __tmp4_data = malloc(0 * sizeof(long long));
            odd_sieve_len = 0;
            return __tmp4_data;
        }
    }
    if (num == 3LL) {
        {
            long long* __tmp5_data = malloc(1 * sizeof(long long));
            __tmp5_data[0] = 2LL;
            odd_sieve_len = 1;
            return __tmp5_data;
        }
    }
    long long size = (num / 2LL) - 1LL;
    long long *sieve = NULL;
    size_t sieve_len = 0;
    long long idx = 0LL;
    while (idx < size) {
        sieve = list_append_long_long(sieve, &sieve_len, 1LL);
        idx = idx + 1LL;
    }
    long long i = 3LL;
    while ((i * i) <= num) {
        long long s_idx = (i / 2LL) - 1LL;
        if (sieve[(int)({long long _mochi_idx = s_idx; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
            long long j = i * i;
            while (j < num) {
                long long j_idx = (j / 2LL) - 1LL;
                sieve[(int)(j_idx)] = 0LL;
                j = j + (2LL * i);
            }
        }
        i = i + 2LL;
    }
    long long *primes = NULL;
    size_t primes_len = 0;
    primes = list_append_long_long(primes, &primes_len, 2LL);
    long long n = 3LL;
    long long k = 0LL;
    while (n < num) {
        if (sieve[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
            primes = list_append_long_long(primes, &primes_len, n);
        }
        n = n + 2LL;
        k = k + 1LL;
    }
    return odd_sieve_len = primes_len, primes;
}

int main(void) {
    {
        long long __start = _now();
        long long* __tmp6 = odd_sieve(2LL);
        puts(str_list_int(__tmp6, odd_sieve_len));
        long long* __tmp7 = odd_sieve(3LL);
        puts(str_list_int(__tmp7, odd_sieve_len));
        long long* __tmp8 = odd_sieve(10LL);
        puts(str_list_int(__tmp8, odd_sieve_len));
        long long* __tmp9 = odd_sieve(20LL);
        puts(str_list_int(__tmp9, odd_sieve_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
