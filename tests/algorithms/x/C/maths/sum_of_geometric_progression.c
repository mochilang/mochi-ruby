// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double pow_float(double base, long long exp);
double sum_of_geometric_progression(long long first_term, long long common_ratio, long long num_of_terms);
void test_sum();
void user_main();
int main(void);

double pow_float(double base, long long exp) {
    double result = 1.0;
    long long exponent = exp;
    if (exponent < 0LL) {
        exponent = -(exponent);
        long long i = 0LL;
        while (i < exponent) {
            result = result * base;
            i = i + 1LL;
        }
        return 1.0 / result;
    }
    long long i = 0LL;
    while (i < exponent) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double sum_of_geometric_progression(long long first_term, long long common_ratio, long long num_of_terms) {
    if (common_ratio == 1LL) {
        return num_of_terms * first_term;
    }
    double a = (double)(first_term);
    double r = (double)(common_ratio);
    return (a / (1.0 - r)) * (1.0 - pow_float(r, num_of_terms));
}

void test_sum() {
    if (sum_of_geometric_progression(1LL, 2LL, 10LL) != 1023.0) {
        panic("example1 failed");
    }
    if (sum_of_geometric_progression(1LL, 10LL, 5LL) != 11111.0) {
        panic("example2 failed");
    }
    if (sum_of_geometric_progression(-1LL, 2LL, 10LL) != -1023.0) {
        panic("example3 failed");
    }
}

void user_main() {
    test_sum();
    printf("%g\n", sum_of_geometric_progression(1LL, 2LL, 10LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
