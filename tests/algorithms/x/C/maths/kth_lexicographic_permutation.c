// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_at_len;
size_t kth_permutation_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * remove_at(long long * xs, size_t xs_len, long long idx);
long long * kth_permutation(long long k, long long n);
long long list_equal(long long * a, size_t a_len, long long * b, size_t b_len);
const char* list_to_string(long long * xs, size_t xs_len);
void test_kth_permutation();
void user_main();
int main(void);

long long * remove_at(long long * xs, size_t xs_len, long long idx) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        if (i != idx) {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_at_len = res_len, res;
}

long long * kth_permutation(long long k, long long n) {
    if (n <= 0LL) {
        panic("n must be positive");
    }
    long long *factorials = NULL;
    size_t factorials_len = 0;
    factorials = list_append_long_long(factorials, &factorials_len, 1LL);
    long long i = 2LL;
    while (i < n) {
        factorials = list_append_long_long(factorials, &factorials_len, factorials[(int)({long long _mochi_idx = factorials_len - 1LL; _mochi_idx < 0 ? factorials_len + _mochi_idx : _mochi_idx;})] * i);
        i = i + 1LL;
    }
    long long total = factorials[(int)({long long _mochi_idx = factorials_len - 1LL; _mochi_idx < 0 ? factorials_len + _mochi_idx : _mochi_idx;})] * n;
    if ((k < 0LL) || (k >= total)) {
        panic("k out of bounds");
    }
    long long *elements = NULL;
    size_t elements_len = 0;
    long long e = 0LL;
    while (e < n) {
        elements = list_append_long_long(elements, &elements_len, e);
        e = e + 1LL;
    }
    long long *permutation = NULL;
    size_t permutation_len = 0;
    long long idx = factorials_len - 1LL;
    while (idx >= 0LL) {
        long long factorial = factorials[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? factorials_len + _mochi_idx : _mochi_idx;})];
        long long number = k / factorial;
        k = k % factorial;
        permutation = list_append_long_long(permutation, &permutation_len, elements[(int)({long long _mochi_idx = number; _mochi_idx < 0 ? elements_len + _mochi_idx : _mochi_idx;})]);
        elements = remove_at(elements, elements_len, number);
        elements_len = remove_at_len;
        idx = idx - 1LL;
    }
    permutation = list_append_long_long(permutation, &permutation_len, elements[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? elements_len + _mochi_idx : _mochi_idx;})]);
    return kth_permutation_len = permutation_len, permutation;
}

long long list_equal(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

const char* list_to_string(long long * xs, size_t xs_len) {
    if (xs_len == 0LL) {
        return "[]";
    }
    const char* s = str_concat("[", str_int(xs[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
    long long i = 1LL;
    while (i < xs_len) {
        s = str_concat(str_concat(s, ", "), str_int(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

void test_kth_permutation() {
    long long *expected1 = NULL;
    size_t expected1_len = 0;
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 1LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 2LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 3LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 4LL);
    long long *res1 = kth_permutation(0LL, 5LL);
    size_t res1_len = kth_permutation_len;
    if (!(list_equal(res1, res1_len, expected1, expected1_len))) {
        panic("test case 1 failed");
    }
    long long *expected2 = NULL;
    size_t expected2_len = 0;
    expected2 = list_append_long_long(expected2, &expected2_len, 1LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 3LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 0LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 2LL);
    long long *res2 = kth_permutation(10LL, 4LL);
    size_t res2_len = kth_permutation_len;
    if (!(list_equal(res2, res2_len, expected2, expected2_len))) {
        panic("test case 2 failed");
    }
}

void user_main() {
    test_kth_permutation();
    long long *res = kth_permutation(10LL, 4LL);
    size_t res_len = kth_permutation_len;
    puts(list_to_string(res, res_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
