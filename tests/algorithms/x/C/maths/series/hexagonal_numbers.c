// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t hexagonal_numbers_len;

size_t append_len;
static int list_eq_int(const long long a[], size_t alen, const long long b[], size_t blen) {
    if (alen != blen) return 0;
    for (size_t i = 0; i < alen; i++) {
        if (a[i] != b[i]) return 0;
    }
    return 1;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * hexagonal_numbers(long long length);
void test_hexagonal_numbers();
int main(void);

long long * hexagonal_numbers(long long length) {
    if (length <= 0LL) {
        panic("Length must be a positive integer.");
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long n = 0LL;
    while (n < length) {
        res = list_append_long_long(res, &res_len, n * ((2LL * n) - 1LL));
        n = n + 1LL;
    }
    return hexagonal_numbers_len = res_len, res;
}

void test_hexagonal_numbers() {
    long long *expected5 = NULL;
    size_t expected5_len = 0;
    expected5 = list_append_long_long(expected5, &expected5_len, 0LL);
    expected5 = list_append_long_long(expected5, &expected5_len, 1LL);
    expected5 = list_append_long_long(expected5, &expected5_len, 6LL);
    expected5 = list_append_long_long(expected5, &expected5_len, 15LL);
    expected5 = list_append_long_long(expected5, &expected5_len, 28LL);
    long long *result5 = hexagonal_numbers(5LL);
    size_t result5_len = hexagonal_numbers_len;
    if (list_eq_int(result5, result5_len, expected5, expected5_len) == 0) {
        panic("hexagonal_numbers(5) failed");
    }
    long long *expected10 = NULL;
    size_t expected10_len = 0;
    expected10 = list_append_long_long(expected10, &expected10_len, 0LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 1LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 6LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 15LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 28LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 45LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 66LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 91LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 120LL);
    expected10 = list_append_long_long(expected10, &expected10_len, 153LL);
    long long *result10 = hexagonal_numbers(10LL);
    size_t result10_len = hexagonal_numbers_len;
    if (list_eq_int(result10, result10_len, expected10, expected10_len) == 0) {
        panic("hexagonal_numbers(10) failed");
    }
}

int main(void) {
    {
        long long __start = _now();
        test_hexagonal_numbers();
        puts(({long long *__tmp2 = hexagonal_numbers(5LL); char *__res = str_list_int(__tmp2, hexagonal_numbers_len); __res;}));
        puts(({long long *__tmp3 = hexagonal_numbers(10LL); char *__res = str_list_int(__tmp3, hexagonal_numbers_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
