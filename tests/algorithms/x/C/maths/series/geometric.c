// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long is_geometric_series(double * series, size_t series_len);
double geometric_mean(double * series, size_t series_len);
double pow_float(double base, long long exp);
double nth_root(double value, long long n);
void test_geometric();
void user_main();
int main(void);

long long is_geometric_series(double * series, size_t series_len) {
    if (series_len == 0LL) {
        panic("Input list must be a non empty list");
    }
    if (series_len == 1LL) {
        return 1LL;
    }
    if (series[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})] == 0.0) {
        return 0LL;
    }
    double ratio = series[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})] / series[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})];
    long long i = 0LL;
    while (i < (series_len - 1LL)) {
        if (series[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})] == 0.0) {
            return 0LL;
        }
        if ((series[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})] / series[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})]) != ratio) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

double geometric_mean(double * series, size_t series_len) {
    if (series_len == 0LL) {
        panic("Input list must be a non empty list");
    }
    double product = 1.0;
    long long i = 0LL;
    while (i < series_len) {
        product = product * series[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    long long n = series_len;
    return nth_root(product, n);
}

double pow_float(double base, long long exp) {
    double result = 1.0;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double nth_root(double value, long long n) {
    if (value == 0.0) {
        return 0.0;
    }
    double low = 0.0;
    double high = value;
    if (value < 1.0) {
        high = 1.0;
    }
    double mid = (low + high) / 2.0;
    long long i = 0LL;
    while (i < 40LL) {
        double mp = pow_float(mid, n);
        if (mp > value) {
            high = mid;
        } else {
            low = mid;
        }
        mid = (low + high) / 2.0;
        i = i + 1LL;
    }
    return mid;
}

void test_geometric() {
    double *a = NULL;
    size_t a_len = 0;
    a = list_append_double(a, &a_len, 2.0);
    a = list_append_double(a, &a_len, 4.0);
    a = list_append_double(a, &a_len, 8.0);
    if (!(is_geometric_series(a, a_len))) {
        panic("expected geometric series");
    }
    double *b = NULL;
    size_t b_len = 0;
    b = list_append_double(b, &b_len, 1.0);
    b = list_append_double(b, &b_len, 2.0);
    b = list_append_double(b, &b_len, 3.0);
    if (is_geometric_series(b, b_len)) {
        panic("expected non geometric series");
    }
}

void user_main() {
    test_geometric();
    printf("%g\n", geometric_mean((double[]){2.0, 4.0, 8.0}, 3));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
