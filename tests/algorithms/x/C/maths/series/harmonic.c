// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long is_harmonic_series(double * series, size_t series_len);
double harmonic_mean(double * series, size_t series_len);
int main(void);

long long is_harmonic_series(double * series, size_t series_len) {
    if (series_len == 0LL) {
        panic("Input list must be a non empty list");
    }
    if (series_len == 1LL) {
        if (series[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})] == 0.0) {
            panic("Input series cannot have 0 as an element");
        }
        return 1LL;
    }
    double *rec_series = NULL;
    size_t rec_series_len = 0;
    long long i = 0LL;
    while (i < series_len) {
        double val = series[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})];
        if (val == 0.0) {
            panic("Input series cannot have 0 as an element");
        }
        rec_series = list_append_double(rec_series, &rec_series_len, 1.0 / val);
        i = i + 1LL;
    }
    double common_diff = rec_series[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? rec_series_len + _mochi_idx : _mochi_idx;})] - rec_series[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? rec_series_len + _mochi_idx : _mochi_idx;})];
    long long idx = 2LL;
    while (idx < rec_series_len) {
        if ((rec_series[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? rec_series_len + _mochi_idx : _mochi_idx;})] - rec_series[(int)({long long _mochi_idx = idx - 1LL; _mochi_idx < 0 ? rec_series_len + _mochi_idx : _mochi_idx;})]) != common_diff) {
            return 0LL;
        }
        idx = idx + 1LL;
    }
    return 1LL;
}

double harmonic_mean(double * series, size_t series_len) {
    if (series_len == 0LL) {
        panic("Input list must be a non empty list");
    }
    double total = 0.0;
    long long i = 0LL;
    while (i < series_len) {
        total = total + (1.0 / series[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? series_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (double)(series_len) / total;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", is_harmonic_series((double[]){1.0, 2.0 / 3.0, 1.0 / 2.0, 2.0 / 5.0, 1.0 / 3.0}, 5));
        printf("%d\n", is_harmonic_series((double[]){1.0, 2.0 / 3.0, 2.0 / 5.0, 1.0 / 3.0}, 4));
        printf("%g\n", harmonic_mean((double[]){1.0, 4.0, 4.0}, 3));
        printf("%g\n", harmonic_mean((double[]){3.0, 6.0, 9.0, 12.0}, 4));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
