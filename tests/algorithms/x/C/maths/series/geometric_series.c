// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t geometric_series_len;

size_t append_len;
static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double * geometric_series(double nth_term, double start_term_a, double common_ratio_r);
int main(void);

double * geometric_series(double nth_term, double start_term_a, double common_ratio_r) {
    long long n = (int)(nth_term);
    if (((n <= 0LL) || (start_term_a == 0.0)) || (common_ratio_r == 0.0)) {
        {
            double* __tmp6_data = malloc(0 * sizeof(double));
            geometric_series_len = 0;
            return __tmp6_data;
        }
    }
    double *series = NULL;
    size_t series_len = 0;
    double current = start_term_a;
    long long i = 0LL;
    while (i < n) {
        series = list_append_double(series, &series_len, current);
        current = current * common_ratio_r;
        i = i + 1LL;
    }
    return geometric_series_len = series_len, series;
}

int main(void) {
    {
        long long __start = _now();
        double* __tmp7 = geometric_series(4.0, 2.0, 2.0);
        puts(str_list_double(__tmp7, geometric_series_len));
        double* __tmp8 = geometric_series(4.0, 2.0, -2.0);
        puts(str_list_double(__tmp8, geometric_series_len));
        double* __tmp9 = geometric_series(4.0, -2.0, 2.0);
        puts(str_list_double(__tmp9, geometric_series_len));
        double* __tmp10 = geometric_series(-4.0, 2.0, 2.0);
        puts(str_list_double(__tmp10, geometric_series_len));
        double* __tmp11 = geometric_series(0.0, 100.0, 500.0);
        puts(str_list_double(__tmp11, geometric_series_len));
        double* __tmp12 = geometric_series(1.0, 1.0, 1.0);
        puts(str_list_double(__tmp12, geometric_series_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
