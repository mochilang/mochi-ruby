// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prime_sieve_eratosthenes_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * prime_sieve_eratosthenes(long long num);
long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len);
void test_prime_sieve_eratosthenes();
void user_main();
int main(void);

long long * prime_sieve_eratosthenes(long long num) {
    if (num <= 0LL) {
        panic("Input must be a positive integer");
    }
    long long *primes = NULL;
    size_t primes_len = 0;
    long long i = 0LL;
    while (i <= num) {
        primes = list_append_long_long(primes, &primes_len, 1LL);
        i = i + 1LL;
    }
    long long p = 2LL;
    while ((p * p) <= num) {
        if (primes[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})]) {
            long long j = p * p;
            while (j <= num) {
                primes[(int)(j)] = 0LL;
                j = j + p;
            }
        }
        p = p + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    long long k = 2LL;
    while (k <= num) {
        if (primes[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})]) {
            result = list_append_long_long(result, &result_len, k);
        }
        k = k + 1LL;
    }
    return prime_sieve_eratosthenes_len = result_len, result;
}

long long list_eq(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void test_prime_sieve_eratosthenes() {
    if (!(({long long* __tmp4 = prime_sieve_eratosthenes(10LL); list_eq(__tmp4, prime_sieve_eratosthenes_len, (long long[]){2LL, 3LL, 5LL, 7LL}, 4);}))) {
        panic("test 10 failed");
    }
    if (!(({long long* __tmp5 = prime_sieve_eratosthenes(20LL); list_eq(__tmp5, prime_sieve_eratosthenes_len, (long long[]){2LL, 3LL, 5LL, 7LL, 11LL, 13LL, 17LL, 19LL}, 8);}))) {
        panic("test 20 failed");
    }
    if (!(({long long* __tmp6 = prime_sieve_eratosthenes(2LL); list_eq(__tmp6, prime_sieve_eratosthenes_len, (long long[]){2LL}, 1);}))) {
        panic("test 2 failed");
    }
    if (0LL) {
        panic("test 1 failed");
    }
}

void user_main() {
    test_prime_sieve_eratosthenes();
    puts(({long long *__tmp7 = prime_sieve_eratosthenes(20LL); char *__res = str_list_int(__tmp7, prime_sieve_eratosthenes_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
