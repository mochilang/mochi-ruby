// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double abs_val(double x);
void validate_point(double * p, size_t p_len);
double manhattan_distance(double * a, size_t a_len, double * b, size_t b_len);
double manhattan_distance_one_liner(double * a, size_t a_len, double * b, size_t b_len);
int main(void);

double abs_val(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

void validate_point(double * p, size_t p_len) {
    if (p_len == 0LL) {
        panic("Missing an input");
    }
}

double manhattan_distance(double * a, size_t a_len, double * b, size_t b_len) {
    validate_point(a, a_len);
    validate_point(b, b_len);
    if (a_len != b_len) {
        panic("Both points must be in the same n-dimensional space");
    }
    double total = 0.0;
    long long i = 0LL;
    while (i < a_len) {
        total = total + abs_val(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return total;
}

double manhattan_distance_one_liner(double * a, size_t a_len, double * b, size_t b_len) {
    return manhattan_distance(a, a_len, b, b_len);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(manhattan_distance((long long[]){1.0, 1.0}, 2, (double[]){2.0, 2.0}, 2)));
        puts(str_float(manhattan_distance((double[]){1.5, 1.5}, 2, (double[]){2.0, 2.0}, 2)));
        puts(str_float(manhattan_distance_one_liner((double[]){1.5, 1.5}, 2, (double[]){2.5, 2.0}, 2)));
        puts(str_float(manhattan_distance_one_liner((double[]){-3.0, -3.0, -3.0}, 3, (long long[]){0.0, 0.0, 0.0}, 3)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
