// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long max_sum_sliding_window(long long * arr, size_t arr_len, long long k);
void test_max_sum_sliding_window();
void user_main();
int main(void);

long long max_sum_sliding_window(long long * arr, size_t arr_len, long long k) {
    if ((k < 0LL) || (arr_len < k)) {
        panic("Invalid Input");
    }
    long long idx = 0LL;
    long long current_sum = 0LL;
    while (idx < k) {
        current_sum = current_sum + arr[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        idx = idx + 1LL;
    }
    long long max_sum = current_sum;
    long long i = 0LL;
    while (i < (arr_len - k)) {
        current_sum = (current_sum - arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) + arr[(int)({long long _mochi_idx = i + k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (current_sum > max_sum) {
            max_sum = current_sum;
        }
        i = i + 1LL;
    }
    return max_sum;
}

void test_max_sum_sliding_window() {
    long long *arr1 = NULL;
    size_t arr1_len = 0;
    arr1 = list_append_long_long(arr1, &arr1_len, 1LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 4LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 2LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 10LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 2LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 3LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 1LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 0LL);
    arr1 = list_append_long_long(arr1, &arr1_len, 20LL);
    if (max_sum_sliding_window(arr1, arr1_len, 4LL) != 24LL) {
        panic("test1 failed");
    }
    long long *arr2 = NULL;
    size_t arr2_len = 0;
    arr2 = list_append_long_long(arr2, &arr2_len, 1LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 4LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 2LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 10LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 2LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 13LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 1LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 0LL);
    arr2 = list_append_long_long(arr2, &arr2_len, 2LL);
    if (max_sum_sliding_window(arr2, arr2_len, 4LL) != 27LL) {
        panic("test2 failed");
    }
}

void user_main() {
    test_max_sum_sliding_window();
    long long *sample = NULL;
    size_t sample_len = 0;
    sample = list_append_long_long(sample, &sample_len, 1LL);
    sample = list_append_long_long(sample, &sample_len, 4LL);
    sample = list_append_long_long(sample, &sample_len, 2LL);
    sample = list_append_long_long(sample, &sample_len, 10LL);
    sample = list_append_long_long(sample, &sample_len, 2LL);
    sample = list_append_long_long(sample, &sample_len, 3LL);
    sample = list_append_long_long(sample, &sample_len, 1LL);
    sample = list_append_long_long(sample, &sample_len, 0LL);
    sample = list_append_long_long(sample, &sample_len, 20LL);
    puts(str_int(max_sum_sliding_window(sample, sample_len, 4LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
