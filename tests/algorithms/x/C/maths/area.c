// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;
double TWO_PI = 6.283185307179586;
double TRI_THREE_SIDES = 0;

double _mod(double x, double m);
double sin_approx(double x);
double cos_approx(double x);
double tan_approx(double x);
double sqrt_approx(double x);
double surface_area_cube(double side_length);
double surface_area_cuboid(double length, double breadth, double height);
double surface_area_sphere(double radius);
double surface_area_hemisphere(double radius);
double surface_area_cone(double radius, double height);
double surface_area_conical_frustum(double radius1, double radius2, double height);
double surface_area_cylinder(double radius, double height);
double surface_area_torus(double torus_radius, double tube_radius);
double area_rectangle(double length, double width);
double area_square(double side_length);
double area_triangle(double base, double height);
double area_triangle_three_sides(double side1, double side2, double side3);
double area_parallelogram(double base, double height);
double area_trapezium(double base1, double base2, double height);
double area_circle(double radius);
double area_ellipse(double radius_x, double radius_y);
double area_rhombus(double diagonal1, double diagonal2);
double area_reg_polygon(long long sides, double length);
int main(void);

double _mod(double x, double m) {
    return x - ((double)((int)(x / m)) * m);
}

double sin_approx(double x) {
    double y = _mod(x + PI, TWO_PI) - PI;
    double y2 = y * y;
    double y3 = y2 * y;
    double y5 = y3 * y2;
    double y7 = y5 * y2;
    return ((y - (y3 / 6.0)) + (y5 / 120.0)) - (y7 / 5040.0);
}

double cos_approx(double x) {
    double y = _mod(x + PI, TWO_PI) - PI;
    double y2 = y * y;
    double y4 = y2 * y2;
    double y6 = y4 * y2;
    return ((1.0 - (y2 / 2.0)) + (y4 / 24.0)) - (y6 / 720.0);
}

double tan_approx(double x) {
    return sin_approx(x) / cos_approx(x);
}

double sqrt_approx(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double surface_area_cube(double side_length) {
    if (side_length < 0.0) {
        puts("ValueError: surface_area_cube() only accepts non-negative values");
        return 0.0;
    }
    return (6.0 * side_length) * side_length;
}

double surface_area_cuboid(double length, double breadth, double height) {
    if (((length < 0.0) || (breadth < 0.0)) || (height < 0.0)) {
        puts("ValueError: surface_area_cuboid() only accepts non-negative values");
        return 0.0;
    }
    return 2.0 * (((length * breadth) + (breadth * height)) + (length * height));
}

double surface_area_sphere(double radius) {
    if (radius < 0.0) {
        puts("ValueError: surface_area_sphere() only accepts non-negative values");
        return 0.0;
    }
    return ((4.0 * PI) * radius) * radius;
}

double surface_area_hemisphere(double radius) {
    if (radius < 0.0) {
        puts("ValueError: surface_area_hemisphere() only accepts non-negative values");
        return 0.0;
    }
    return ((3.0 * PI) * radius) * radius;
}

double surface_area_cone(double radius, double height) {
    if ((radius < 0.0) || (height < 0.0)) {
        puts("ValueError: surface_area_cone() only accepts non-negative values");
        return 0.0;
    }
    double slant = sqrt_approx((height * height) + (radius * radius));
    return (PI * radius) * (radius + slant);
}

double surface_area_conical_frustum(double radius1, double radius2, double height) {
    if (((radius1 < 0.0) || (radius2 < 0.0)) || (height < 0.0)) {
        puts("ValueError: surface_area_conical_frustum() only accepts non-negative values");
        return 0.0;
    }
    double slant = sqrt_approx((height * height) + ((radius1 - radius2) * (radius1 - radius2)));
    return PI * (((slant * (radius1 + radius2)) + (radius1 * radius1)) + (radius2 * radius2));
}

double surface_area_cylinder(double radius, double height) {
    if ((radius < 0.0) || (height < 0.0)) {
        puts("ValueError: surface_area_cylinder() only accepts non-negative values");
        return 0.0;
    }
    return ((2.0 * PI) * radius) * (height + radius);
}

double surface_area_torus(double torus_radius, double tube_radius) {
    if ((torus_radius < 0.0) || (tube_radius < 0.0)) {
        puts("ValueError: surface_area_torus() only accepts non-negative values");
        return 0.0;
    }
    if (torus_radius < tube_radius) {
        puts("ValueError: surface_area_torus() does not support spindle or self intersecting tori");
        return 0.0;
    }
    return (((4.0 * PI) * PI) * torus_radius) * tube_radius;
}

double area_rectangle(double length, double width) {
    if ((length < 0.0) || (width < 0.0)) {
        puts("ValueError: area_rectangle() only accepts non-negative values");
        return 0.0;
    }
    return length * width;
}

double area_square(double side_length) {
    if (side_length < 0.0) {
        puts("ValueError: area_square() only accepts non-negative values");
        return 0.0;
    }
    return side_length * side_length;
}

double area_triangle(double base, double height) {
    if ((base < 0.0) || (height < 0.0)) {
        puts("ValueError: area_triangle() only accepts non-negative values");
        return 0.0;
    }
    return (base * height) / 2.0;
}

double area_triangle_three_sides(double side1, double side2, double side3) {
    if (((side1 < 0.0) || (side2 < 0.0)) || (side3 < 0.0)) {
        puts("ValueError: area_triangle_three_sides() only accepts non-negative values");
        return 0.0;
    }
    if ((((side1 + side2) < side3) || ((side1 + side3) < side2)) || ((side2 + side3) < side1)) {
        puts("ValueError: Given three sides do not form a triangle");
        return 0.0;
    }
    double s = ((side1 + side2) + side3) / 2.0;
    double prod = ((s * (s - side1)) * (s - side2)) * (s - side3);
    double res = sqrt_approx(prod);
    return res;
}

double area_parallelogram(double base, double height) {
    if ((base < 0.0) || (height < 0.0)) {
        puts("ValueError: area_parallelogram() only accepts non-negative values");
        return 0.0;
    }
    return base * height;
}

double area_trapezium(double base1, double base2, double height) {
    if (((base1 < 0.0) || (base2 < 0.0)) || (height < 0.0)) {
        puts("ValueError: area_trapezium() only accepts non-negative values");
        return 0.0;
    }
    return (0.5 * (base1 + base2)) * height;
}

double area_circle(double radius) {
    if (radius < 0.0) {
        puts("ValueError: area_circle() only accepts non-negative values");
        return 0.0;
    }
    return (PI * radius) * radius;
}

double area_ellipse(double radius_x, double radius_y) {
    if ((radius_x < 0.0) || (radius_y < 0.0)) {
        puts("ValueError: area_ellipse() only accepts non-negative values");
        return 0.0;
    }
    return (PI * radius_x) * radius_y;
}

double area_rhombus(double diagonal1, double diagonal2) {
    if ((diagonal1 < 0.0) || (diagonal2 < 0.0)) {
        puts("ValueError: area_rhombus() only accepts non-negative values");
        return 0.0;
    }
    return (0.5 * diagonal1) * diagonal2;
}

double area_reg_polygon(long long sides, double length) {
    if (sides < 3LL) {
        puts("ValueError: area_reg_polygon() only accepts integers greater than or equal to three as number of sides");
        return 0.0;
    }
    if (length < 0.0) {
        puts("ValueError: area_reg_polygon() only accepts non-negative values as length of a side");
        return 0.0;
    }
    double n = (double)(sides);
    return ((n * length) * length) / (4.0 * tan_approx(PI / n));
}

int main(void) {
    {
        long long __start = _now();
        puts("[DEMO] Areas of various geometric shapes:");
        puts(str_concat("Rectangle: ", str_float(area_rectangle(10.0, 20.0))));
        puts(str_concat("Square: ", str_float(area_square(10.0))));
        puts(str_concat("Triangle: ", str_float(area_triangle(10.0, 10.0))));
        TRI_THREE_SIDES = area_triangle_three_sides(5.0, 12.0, 13.0);
        puts(str_concat("Triangle Three Sides: ", str_float(TRI_THREE_SIDES)));
        puts(str_concat("Parallelogram: ", str_float(area_parallelogram(10.0, 20.0))));
        puts(str_concat("Rhombus: ", str_float(area_rhombus(10.0, 20.0))));
        puts(str_concat("Trapezium: ", str_float(area_trapezium(10.0, 20.0, 30.0))));
        puts(str_concat("Circle: ", str_float(area_circle(20.0))));
        puts(str_concat("Ellipse: ", str_float(area_ellipse(10.0, 20.0))));
        puts("");
        puts("Surface Areas of various geometric shapes:");
        puts(str_concat("Cube: ", str_float(surface_area_cube(20.0))));
        puts(str_concat("Cuboid: ", str_float(surface_area_cuboid(10.0, 20.0, 30.0))));
        puts(str_concat("Sphere: ", str_float(surface_area_sphere(20.0))));
        puts(str_concat("Hemisphere: ", str_float(surface_area_hemisphere(20.0))));
        puts(str_concat("Cone: ", str_float(surface_area_cone(10.0, 20.0))));
        puts(str_concat("Conical Frustum: ", str_float(surface_area_conical_frustum(10.0, 20.0, 30.0))));
        puts(str_concat("Cylinder: ", str_float(surface_area_cylinder(10.0, 20.0))));
        puts(str_concat("Torus: ", str_float(surface_area_torus(20.0, 10.0))));
        puts(str_concat("Equilateral Triangle: ", str_float(area_reg_polygon(3LL, 10.0))));
        puts(str_concat("Square: ", str_float(area_reg_polygon(4LL, 10.0))));
        puts(str_concat("Regular Pentagon: ", str_float(area_reg_polygon(5LL, 10.0))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
