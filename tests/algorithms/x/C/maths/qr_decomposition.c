// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t identity_matrix_len;
size_t *identity_matrix_lens;
size_t copy_matrix_len;
size_t *copy_matrix_lens;
size_t matmul_len;
size_t *matmul_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct QR QR;

struct QR {
    double **q;
    size_t q_len;
    size_t *q_lens;
    size_t q_lens_len;
    double **r;
    size_t r_len;
    size_t *r_lens;
    size_t r_lens_len;
};

double A_0[3] = {12.0, -51.0, 4.0};
double A_1[3] = {6.0, 167.0, -68.0};
double A_2[3] = {-4.0, 24.0, -41.0};
double *A_init[3] = {A_0, A_1, A_2};
double **A = A_init;
size_t A_len = 3;
size_t A_lens_init[3] = {3, 3, 3};
size_t *A_lens = A_lens_init;
size_t A_lens_len = 3;
QR result;

double sqrt_approx(double x);
double sign(double x);
double vector_norm(double * v, size_t v_len);
double * * identity_matrix(long long n);
double * * copy_matrix(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len);
double * * matmul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
QR qr_decomposition(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len);
void print_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
int main(void);

double sqrt_approx(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double sign(double x) {
    if (x >= 0.0) {
        return 1.0;
    } else {
        return -1.0;
    }
}

double vector_norm(double * v, size_t v_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < v_len) {
        sum = sum + (v[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})] * v[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    double n = sqrt_approx(sum);
    return n;
}

double * * identity_matrix(long long n) {
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            if (i == j) {
                row = list_append_double(row, &row_len, 1.0);
            } else {
                row = list_append_double(row, &row_len, 0.0);
            }
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return identity_matrix_lens = mat_lens, identity_matrix_len = mat_len, mat;
}

double * * copy_matrix(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len) {
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < a_len) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < a_lens[(int)(i)]) {
            row = list_append_double(row, &row_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return copy_matrix_lens = mat_lens, copy_matrix_len = mat_len, mat;
}

double * * matmul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    long long m = a_len;
    long long n = a_lens[(int)(0LL)];
    long long p = b_lens[(int)(0LL)];
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < m) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < p) {
            double sum = 0.0;
            long long k = 0LL;
            while (k < n) {
                sum = sum + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_double(row, &row_len, sum);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return matmul_lens = res_lens, matmul_len = res_len, res;
}

QR qr_decomposition(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len) {
    long long m = a_len;
    long long n = a_lens[(int)(0LL)];
    long long t;
    if (m < n) {
        t = m;
    } else {
        t = n;
    }
    double **q = identity_matrix(m);
    size_t q_len = identity_matrix_len;
    size_t *q_lens = identity_matrix_lens;
    size_t q_lens_len = identity_matrix_len;
    double **r = copy_matrix(a, a_len, a_lens, a_len);
    size_t r_len = copy_matrix_len;
    size_t *r_lens = copy_matrix_lens;
    size_t r_lens_len = copy_matrix_len;
    long long k = 0LL;
    while (k < (t - 1LL)) {
        double *x = NULL;
        size_t x_len = 0;
        long long i = k;
        while (i < m) {
            x = list_append_double(x, &x_len, r[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? r_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? r_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        double *e1 = NULL;
        size_t e1_len = 0;
        i = 0LL;
        while (i < x_len) {
            if (i == 0LL) {
                e1 = list_append_double(e1, &e1_len, 1.0);
            } else {
                e1 = list_append_double(e1, &e1_len, 0.0);
            }
            i = i + 1LL;
        }
        double alpha = vector_norm(x, x_len);
        double s = sign(x[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]) * alpha;
        double *v = NULL;
        size_t v_len = 0;
        i = 0LL;
        while (i < x_len) {
            v = list_append_double(v, &v_len, x[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})] + (s * e1[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? e1_len + _mochi_idx : _mochi_idx;})]));
            i = i + 1LL;
        }
        double vnorm = vector_norm(v, v_len);
        i = 0LL;
        while (i < v_len) {
            v[(int)(i)] = v[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})] / vnorm;
            i = i + 1LL;
        }
        long long size = v_len;
        double **qk_small = NULL;
        size_t qk_small_len = 0;
        size_t *qk_small_lens = NULL;
        size_t qk_small_lens_len = 0;
        i = 0LL;
        while (i < size) {
            double *row = NULL;
            size_t row_len = 0;
            long long j = 0LL;
            while (j < size) {
                long long delta;
                if (i == j) {
                    delta = 1.0;
                } else {
                    delta = 0.0;
                }
                row = list_append_double(row, &row_len, delta - ((2.0 * v[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]) * v[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]));
                j = j + 1LL;
            }
            qk_small = list_append_doubleptr(qk_small, &qk_small_len, row);
            qk_small_lens = list_append_szt(qk_small_lens, &qk_small_lens_len, row_len);
            i = i + 1LL;
        }
        double **qk = identity_matrix(m);
        size_t qk_len = identity_matrix_len;
        size_t *qk_lens = identity_matrix_lens;
        size_t qk_lens_len = identity_matrix_len;
        i = 0LL;
        while (i < size) {
            long long j = 0LL;
            while (j < size) {
                qk[(int)(k + i)][(int)(k + j)] = qk_small[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? qk_small_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? qk_small_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
                j = j + 1LL;
            }
            i = i + 1LL;
        }
        q = matmul(q, q_len, q_lens, q_len, qk, qk_len, qk_lens, qk_len);
        q_len = matmul_len;
        q_lens = matmul_lens;
        r = matmul(qk, qk_len, qk_lens, qk_len, r, r_len, r_lens, r_len);
        r_len = matmul_len;
        r_lens = matmul_lens;
        k = k + 1LL;
    }
    return (QR){.q = q, .q_len = q_len, .q_lens = q_lens, .q_lens_len = q_len, .r = r, .r_len = r_len, .r_lens = r_lens, .r_lens_len = r_len};
}

void print_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long i = 0LL;
    while (i < mat_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < mat_lens[(int)(i)]) {
            line = str_concat(line, str_float(mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]));
            if ((j + 1LL) < mat_lens[(int)(i)]) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        result = qr_decomposition(A, A_len, A_lens, A_len);
        print_matrix(result.q, result.q_len, result.q_lens, result.q_len);
        print_matrix(result.r, result.r_len, result.r_lens, result.r_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
