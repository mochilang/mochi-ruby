// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Point3d Point3d;
typedef struct Vector3d Vector3d;

struct Point3d {
    double x;
    double y;
    double z;
};

struct Vector3d {
    double x;
    double y;
    double z;
};

Vector3d create_vector(Point3d p1, Point3d p2);
Vector3d get_3d_vectors_cross(Vector3d ab, Vector3d ac);
double user_pow10(long long exp);
double round_float(double x, long long digits);
long long is_zero_vector(Vector3d v, long long accuracy);
long long are_collinear(Point3d a, Point3d b, Point3d c, long long accuracy);
void test_are_collinear();
void user_main();
int main(void);

Vector3d create_vector(Point3d p1, Point3d p2) {
    double vx = p2.x - p1.x;
    double vy = p2.y - p1.y;
    double vz = p2.z - p1.z;
    return (Vector3d){.x = vx, .y = vy, .z = vz};
}

Vector3d get_3d_vectors_cross(Vector3d ab, Vector3d ac) {
    double cx = (ab.y * ac.z) - (ab.z * ac.y);
    double cy = (ab.z * ac.x) - (ab.x * ac.z);
    double cz = (ab.x * ac.y) - (ab.y * ac.x);
    return (Vector3d){.x = cx, .y = cy, .z = cz};
}

double user_pow10(long long exp) {
    double result = 1.0;
    long long i = 0LL;
    while (i < exp) {
        result = result * 10.0;
        i = i + 1LL;
    }
    return result;
}

double round_float(double x, long long digits) {
    double factor = user_pow10(digits);
    double v = x * factor;
    if (v >= 0.0) {
        v = v + 0.5;
    } else {
        v = v - 0.5;
    }
    long long t = (int)(v);
    return (double)(t) / factor;
}

long long is_zero_vector(Vector3d v, long long accuracy) {
    return ((round_float(v.x, accuracy) == 0.0) && (round_float(v.y, accuracy) == 0.0)) && (round_float(v.z, accuracy) == 0.0);
}

long long are_collinear(Point3d a, Point3d b, Point3d c, long long accuracy) {
    Vector3d ab = create_vector(a, b);
    Vector3d ac = create_vector(a, c);
    Vector3d cross = get_3d_vectors_cross(ab, ac);
    return is_zero_vector(cross, accuracy);
}

void test_are_collinear() {
    Point3d p1 = (Point3d){.x = 0.0, .y = 0.0, .z = 0.0};
    Point3d p2 = (Point3d){.x = 1.0, .y = 1.0, .z = 1.0};
    Point3d p3 = (Point3d){.x = 2.0, .y = 2.0, .z = 2.0};
    if (!(are_collinear(p1, p2, p3, 10LL))) {
        panic("collinear test failed");
    }
    Point3d q3 = (Point3d){.x = 1.0, .y = 2.0, .z = 3.0};
    if (are_collinear(p1, p2, q3, 10LL)) {
        panic("non-collinear test failed");
    }
}

void user_main() {
    test_are_collinear();
    Point3d a = (Point3d){.x = 4.802293498137402, .y = 3.536233125455244, .z = 0.0};
    Point3d b = (Point3d){.x = -2.186788107953106, .y = -9.24561398001649, .z = 7.141509524846482};
    Point3d c = (Point3d){.x = 1.530169574640268, .y = -2.447927606600034, .z = 3.343487096469054};
    puts(str_int(are_collinear(a, b, c, 10LL)));
    Point3d d = (Point3d){.x = 2.399001826862445, .y = -2.452009976680793, .z = 4.464656666157666};
    Point3d e = (Point3d){.x = -3.682816335934376, .y = 5.753788986533145, .z = 9.490993909044244};
    Point3d f = (Point3d){.x = 1.962903518985307, .y = 3.741415730125627, .z = 7.0};
    puts(str_int(are_collinear(d, e, f, 10LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
