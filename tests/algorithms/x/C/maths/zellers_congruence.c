// Generated by Mochi 0.10.32 on 2025-08-17 20:26 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { int *keys; const char **vals; size_t len; size_t cap; } MapIS;

static const char* map_get_is(const int keys[], const char *vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return "";
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long parse_decimal(const char* s);
const char* zeller_day(const char* date_input);
const char* zeller(const char* date_input);
void test_zeller();
void user_main();
int main(void);

long long parse_decimal(const char* s) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if ((strcmp(c, "0") < 0) || (strcmp(c, "9") > 0)) {
            panic("invalid literal");
        }
        value = (value * 10LL) + _atoi(c);
        i = i + 1LL;
    }
    return value;
}

const char* zeller_day(const char* date_input) {
    static int days_keys[23] = {0LL, 1LL, 2LL, 3LL, 4LL, 5LL, 6LL};
    static const char* days_vals[23] = {"Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"};
    size_t days_len = 7;
    MapIS days = { days_keys, days_vals, 7, 23 };
    if (strlen(date_input) != 10LL) {
        panic("Must be 10 characters long");
    }
    long long m = parse_decimal(_substring(date_input, 0LL, 2LL));
    if ((m <= 0LL) || (m >= 13LL)) {
        panic("Month must be between 1 - 12");
    }
    const char* sep1 = (const char[]){date_input[2LL], 0};
    if ((strcmp(sep1, "-") != 0) && (strcmp(sep1, "/") != 0)) {
        panic("Date separator must be '-' or '/'");
    }
    long long d = parse_decimal(_substring(date_input, 3LL, 5LL));
    if ((d <= 0LL) || (d >= 32LL)) {
        panic("Date must be between 1 - 31");
    }
    const char* sep2 = (const char[]){date_input[5LL], 0};
    if ((strcmp(sep2, "-") != 0) && (strcmp(sep2, "/") != 0)) {
        panic("Date separator must be '-' or '/'");
    }
    long long y = parse_decimal(_substring(date_input, 6LL, 10LL));
    if ((y <= 45LL) || (y >= 8500LL)) {
        panic("Year out of range. There has to be some sort of limit...right?");
    }
    long long year = y;
    long long month = m;
    if (month <= 2LL) {
        year = year - 1LL;
        month = month + 12LL;
    }
    long long c = year / 100LL;
    long long k = year % 100LL;
    long long t = (int)((2.6 * (double)(month)) - 5.39);
    long long u = c / 4LL;
    long long v = k / 4LL;
    long long x = d + k;
    long long z = ((t + u) + v) + x;
    long long w = z - (2LL * c);
    long long f = w % 7LL;
    if (f < 0LL) {
        f = f + 7LL;
    }
    return map_get_is(days.keys, days.vals, days.len, f);
}

const char* zeller(const char* date_input) {
    const char* day = zeller_day(date_input);
    return str_concat(str_concat(str_concat(str_concat("Your date ", date_input), ", is a "), day), "!");
}

void test_zeller() {
    const char* *inputs = NULL;
    size_t inputs_len = 0;
    inputs = list_append_str(inputs, &inputs_len, "01-31-2010");
    inputs = list_append_str(inputs, &inputs_len, "02-01-2010");
    inputs = list_append_str(inputs, &inputs_len, "11-26-2024");
    inputs = list_append_str(inputs, &inputs_len, "07-04-1776");
    const char* *expected = NULL;
    size_t expected_len = 0;
    expected = list_append_str(expected, &expected_len, "Sunday");
    expected = list_append_str(expected, &expected_len, "Monday");
    expected = list_append_str(expected, &expected_len, "Tuesday");
    expected = list_append_str(expected, &expected_len, "Thursday");
    long long i = 0LL;
    while (i < 4LL) {
        const char* res = zeller_day(inputs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? inputs_len + _mochi_idx : _mochi_idx;})]);
        if (strcmp(res, expected[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? expected_len + _mochi_idx : _mochi_idx;})]) != 0) {
            panic("zeller test failed");
        }
        i = i + 1LL;
    }
}

void user_main() {
    test_zeller();
    puts(zeller("01-31-2010"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
