// Generated by Mochi 0.10.32 on 2025-08-17 13:11 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long sock_merchant(long long * colors, size_t colors_len);
void test_sock_merchant();
void user_main();
int main(void);

long long sock_merchant(long long * colors, size_t colors_len) {
    long long *arr = NULL;
    size_t arr_len = 0;
    long long i = 0LL;
    while (i < colors_len) {
        arr = list_append_long_long(arr, &arr_len, colors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? colors_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long n = arr_len;
    long long a = 0LL;
    while (a < n) {
        long long min_idx = a;
        long long b = a + 1LL;
        while (b < n) {
            if (arr[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < arr[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                min_idx = b;
            }
            b = b + 1LL;
        }
        long long temp = arr[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(a)] = arr[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(min_idx)] = temp;
        a = a + 1LL;
    }
    long long pairs = 0LL;
    i = 0LL;
    while (i < n) {
        long long count = 1LL;
        while (((i + 1LL) < n) && (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == arr[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})])) {
            count = count + 1LL;
            i = i + 1LL;
        }
        pairs = pairs + (count / 2LL);
        i = i + 1LL;
    }
    return pairs;
}

void test_sock_merchant() {
    long long *example1 = NULL;
    size_t example1_len = 0;
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 20LL);
    example1 = list_append_long_long(example1, &example1_len, 20LL);
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 30LL);
    example1 = list_append_long_long(example1, &example1_len, 50LL);
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 20LL);
    if (sock_merchant(example1, example1_len) != 3LL) {
        panic("example1 failed");
    }
    long long *example2 = NULL;
    size_t example2_len = 0;
    example2 = list_append_long_long(example2, &example2_len, 1LL);
    example2 = list_append_long_long(example2, &example2_len, 1LL);
    example2 = list_append_long_long(example2, &example2_len, 3LL);
    example2 = list_append_long_long(example2, &example2_len, 3LL);
    if (sock_merchant(example2, example2_len) != 2LL) {
        panic("example2 failed");
    }
}

void user_main() {
    test_sock_merchant();
    long long *example1 = NULL;
    size_t example1_len = 0;
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 20LL);
    example1 = list_append_long_long(example1, &example1_len, 20LL);
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 30LL);
    example1 = list_append_long_long(example1, &example1_len, 50LL);
    example1 = list_append_long_long(example1, &example1_len, 10LL);
    example1 = list_append_long_long(example1, &example1_len, 20LL);
    puts(str_int(sock_merchant(example1, example1_len)));
    long long *example2 = NULL;
    size_t example2_len = 0;
    example2 = list_append_long_long(example2, &example2_len, 1LL);
    example2 = list_append_long_long(example2, &example2_len, 1LL);
    example2 = list_append_long_long(example2, &example2_len, 3LL);
    example2 = list_append_long_long(example2, &example2_len, 3LL);
    puts(str_int(sock_merchant(example2, example2_len)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
