// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_int_len;
size_t sort_string_len;
size_t mode_int_len;
size_t mode_string_len;

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long contains_int(long long * xs, size_t xs_len, long long x);
long long contains_string(const char* * xs, size_t xs_len, const char* x);
long long count_int(long long * xs, size_t xs_len, long long x);
long long count_string(const char* * xs, size_t xs_len, const char* x);
long long * sort_int(long long * xs, size_t xs_len);
const char* * sort_string(const char* * xs, size_t xs_len);
long long * mode_int(long long * lst, size_t lst_len);
const char* * mode_string(const char* * lst, size_t lst_len);
int main(void);

long long contains_int(long long * xs, size_t xs_len, long long x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == x) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long contains_string(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long count_int(long long * xs, size_t xs_len, long long x) {
    long long cnt = 0LL;
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == x) {
            cnt = cnt + 1LL;
        }
        i = i + 1LL;
    }
    return cnt;
}

long long count_string(const char* * xs, size_t xs_len, const char* x) {
    long long cnt = 0LL;
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})], x) == 0) {
            cnt = cnt + 1LL;
        }
        i = i + 1LL;
    }
    return cnt;
}

long long * sort_int(long long * xs, size_t xs_len) {
    long long *arr = xs;
    size_t arr_len = xs_len;
    long long i = 0LL;
    while (i < arr_len) {
        long long j = i + 1LL;
        while (j < arr_len) {
            if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long tmp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_int_len = arr_len, arr;
}

const char* * sort_string(const char* * xs, size_t xs_len) {
    const char* *arr = xs;
    size_t arr_len = xs_len;
    long long i = 0LL;
    while (i < arr_len) {
        long long j = i + 1LL;
        while (j < arr_len) {
            if (strcmp(arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) < 0) {
                const char* tmp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_string_len = arr_len, arr;
}

long long * mode_int(long long * lst, size_t lst_len) {
    if (lst_len == 0LL) {
        {
            long long* __tmp5_data = malloc(0 * sizeof(long long));
            mode_int_len = 0;
            return __tmp5_data;
        }
    }
    long long *counts = NULL;
    size_t counts_len = 0;
    long long i = 0LL;
    while (i < lst_len) {
        counts = list_append_long_long(counts, &counts_len, count_int(lst, lst_len, lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    long long max_count = 0LL;
    i = 0LL;
    while (i < counts_len) {
        if (counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] > max_count) {
            max_count = counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    long long *modes = NULL;
    size_t modes_len = 0;
    i = 0LL;
    while (i < lst_len) {
        if (counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] == max_count) {
            long long v = lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})];
            if (!(contains_int(modes, modes_len, v))) {
                modes = list_append_long_long(modes, &modes_len, v);
            }
        }
        i = i + 1LL;
    }
    {
        long long* __ret6 = sort_int(modes, modes_len);
        mode_int_len = sort_int_len;
        return __ret6;
    }
}

const char* * mode_string(const char* * lst, size_t lst_len) {
    if (lst_len == 0LL) {
        {
            const char** __tmp7_data = malloc(0 * sizeof(const char*));
            mode_string_len = 0;
            return __tmp7_data;
        }
    }
    long long *counts = NULL;
    size_t counts_len = 0;
    long long i = 0LL;
    while (i < lst_len) {
        counts = list_append_long_long(counts, &counts_len, count_string(lst, lst_len, lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    long long max_count = 0LL;
    i = 0LL;
    while (i < counts_len) {
        if (counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] > max_count) {
            max_count = counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    const char* *modes = NULL;
    size_t modes_len = 0;
    i = 0LL;
    while (i < lst_len) {
        if (counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] == max_count) {
            const char* v = lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})];
            if (!(contains_string(modes, modes_len, v))) {
                modes = list_append_str(modes, &modes_len, v);
            }
        }
        i = i + 1LL;
    }
    {
        const char** __ret8 = sort_string(modes, modes_len);
        mode_string_len = sort_string_len;
        return __ret8;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long* __tmp9 = mode_int(({long long *tmp = malloc(14 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = 4LL; tmp[3] = 5LL; tmp[4] = 3LL; tmp[5] = 4LL; tmp[6] = 2LL; tmp[7] = 5LL; tmp[8] = 2LL; tmp[9] = 2LL; tmp[10] = 4LL; tmp[11] = 2LL; tmp[12] = 2LL; tmp[13] = 2LL; tmp;}), 14);
        puts(str_list_int(__tmp9, mode_int_len));
        long long* __tmp10 = mode_int(({long long *tmp = malloc(14 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 5LL; tmp[3] = 3LL; tmp[4] = 4LL; tmp[5] = 2LL; tmp[6] = 5LL; tmp[7] = 2LL; tmp[8] = 2LL; tmp[9] = 4LL; tmp[10] = 4LL; tmp[11] = 2LL; tmp[12] = 2LL; tmp[13] = 2LL; tmp;}), 14);
        puts(str_list_int(__tmp10, mode_int_len));
        long long* __tmp11 = mode_int(({long long *tmp = malloc(16 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp[2] = 5LL; tmp[3] = 3LL; tmp[4] = 4LL; tmp[5] = 2LL; tmp[6] = 5LL; tmp[7] = 2LL; tmp[8] = 2LL; tmp[9] = 4LL; tmp[10] = 4LL; tmp[11] = 4LL; tmp[12] = 2LL; tmp[13] = 2LL; tmp[14] = 4LL; tmp[15] = 2LL; tmp;}), 16);
        puts(str_list_int(__tmp11, mode_int_len));
        const char** __tmp12 = mode_string(({const char* *tmp = malloc(4 * sizeof(const char*)); tmp[0] = "x"; tmp[1] = "y"; tmp[2] = "y"; tmp[3] = "z"; tmp;}), 4);
        puts(str_list_str(__tmp12, mode_string_len));
        const char** __tmp13 = mode_string(({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = "x"; tmp[1] = "x"; tmp[2] = "y"; tmp[3] = "y"; tmp[4] = "z"; tmp;}), 5);
        puts(str_list_str(__tmp13, mode_string_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
