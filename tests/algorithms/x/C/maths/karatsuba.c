// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long int_pow(long long base, long long exp);
long long karatsuba(long long a, long long b);
void user_main();
int main(void);

long long int_pow(long long base, long long exp) {
    long long result = 1LL;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

long long karatsuba(long long a, long long b) {
    if ((strlen(str_int(a)) == 1LL) || (strlen(str_int(b)) == 1LL)) {
        return a * b;
    }
    long long m1 = strlen(str_int(a));
    long long lb = strlen(str_int(b));
    if (lb > m1) {
        m1 = lb;
    }
    long long m2 = m1 / 2LL;
    long long power = int_pow(10LL, m2);
    long long a1 = a / power;
    long long a2 = a % power;
    long long b1 = b / power;
    long long b2 = b % power;
    long long x = karatsuba(a2, b2);
    long long y = karatsuba(a1 + a2, b1 + b2);
    long long z = karatsuba(a1, b1);
    long long result = ((z * int_pow(10LL, 2LL * m2)) + (((y - z) - x) * power)) + x;
    return result;
}

void user_main() {
    puts(str_int(karatsuba(15463LL, 23489LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
