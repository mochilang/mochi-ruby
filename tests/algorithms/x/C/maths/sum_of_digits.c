// Generated by Mochi 0.10.32 on 2025-08-17 13:41 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long abs_int(long long n);
long long sum_of_digits(long long n);
long long sum_of_digits_recursion(long long n);
long long sum_of_digits_compact(long long n);
void test_sum_of_digits();
void user_main();
int main(void);

long long abs_int(long long n) {
    if (n < 0LL) {
        return -(n);
    }
    return n;
}

long long sum_of_digits(long long n) {
    long long m = abs_int(n);
    long long res = 0LL;
    while (m > 0LL) {
        res = res + (m % 10LL);
        m = m / 10LL;
    }
    return res;
}

long long sum_of_digits_recursion(long long n) {
    long long m = abs_int(n);
    if (m < 10LL) {
        return m;
    }
    return (m % 10LL) + sum_of_digits_recursion(m / 10LL);
}

long long sum_of_digits_compact(long long n) {
    const char* s = str_int(abs_int(n));
    long long res = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        res = res + s[i] - '0';
        i = i + 1LL;
    }
    return res;
}

void test_sum_of_digits() {
    if (sum_of_digits(12345LL) != 15LL) {
        panic("sum_of_digits 12345 failed");
    }
    if (sum_of_digits(123LL) != 6LL) {
        panic("sum_of_digits 123 failed");
    }
    if (sum_of_digits(-123.0) != 6LL) {
        panic("sum_of_digits -123 failed");
    }
    if (sum_of_digits(0LL) != 0LL) {
        panic("sum_of_digits 0 failed");
    }
    if (sum_of_digits_recursion(12345LL) != 15LL) {
        panic("recursion 12345 failed");
    }
    if (sum_of_digits_recursion(123LL) != 6LL) {
        panic("recursion 123 failed");
    }
    if (sum_of_digits_recursion(-123.0) != 6LL) {
        panic("recursion -123 failed");
    }
    if (sum_of_digits_recursion(0LL) != 0LL) {
        panic("recursion 0 failed");
    }
    if (sum_of_digits_compact(12345LL) != 15LL) {
        panic("compact 12345 failed");
    }
    if (sum_of_digits_compact(123LL) != 6LL) {
        panic("compact 123 failed");
    }
    if (sum_of_digits_compact(-123.0) != 6LL) {
        panic("compact -123 failed");
    }
    if (sum_of_digits_compact(0LL) != 0LL) {
        panic("compact 0 failed");
    }
}

void user_main() {
    test_sum_of_digits();
    puts(str_int(sum_of_digits(12345LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
