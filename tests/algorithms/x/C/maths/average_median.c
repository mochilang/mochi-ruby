// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t bubble_sort_len;

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long * bubble_sort(long long * nums, size_t nums_len);
double median(long long * nums, size_t nums_len);
int main(void);

long long * bubble_sort(long long * nums, size_t nums_len) {
    long long *arr = nums;
    size_t arr_len = nums_len;
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j < (n - 1LL)) {
            long long a = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            long long b = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            if (a > b) {
                arr[(int)(j)] = b;
                arr[(int)(j + 1LL)] = a;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bubble_sort_len = arr_len, arr;
}

double median(long long * nums, size_t nums_len) {
    long long *sorted_list = bubble_sort(nums, nums_len);
    size_t sorted_list_len = bubble_sort_len;
    long long length = sorted_list_len;
    long long mid_index = length / 2LL;
    if ((length % 2LL) == 0LL) {
        return (sorted_list[(int)({long long _mochi_idx = mid_index; _mochi_idx < 0 ? sorted_list_len + _mochi_idx : _mochi_idx;})] + sorted_list[(int)({long long _mochi_idx = mid_index - 1LL; _mochi_idx < 0 ? sorted_list_len + _mochi_idx : _mochi_idx;})]) / 2.0;
    } else {
        return (double)(sorted_list[(int)({long long _mochi_idx = mid_index; _mochi_idx < 0 ? sorted_list_len + _mochi_idx : _mochi_idx;})]);
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(median(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 0LL; tmp;}), 1)));
        puts(str_float(median(({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 1LL; tmp[2] = 3LL; tmp[3] = 2LL; tmp;}), 4)));
        puts(str_float(median(({long long *tmp = malloc(7 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 70LL; tmp[2] = 6LL; tmp[3] = 50LL; tmp[4] = 20LL; tmp[5] = 8LL; tmp[6] = 4LL; tmp;}), 7)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
