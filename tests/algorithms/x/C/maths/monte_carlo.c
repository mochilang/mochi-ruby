// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;
long long rand_seed = 123456789LL;

double rand_float();
double rand_range(double min_val, double max_val);
double abs_float(double x);
double sqrtApprox(double x);
void pi_estimator(long long iterations);
double area_under_curve_estimator(long long iterations, double(*f)(double), double min_value, double max_value);
void area_under_line_estimator_check(long long iterations, double min_value, double max_value);
double _identity_function(double x);
void pi_estimator_using_area_under_curve(long long iterations);
double _semi_circle(double x);
void user_main();
int main(void);

double rand_float() {
    rand_seed = ((1103515245LL * rand_seed) + 12345LL) % 2147483648LL;
    return (double)(rand_seed) / 2.147483648e+09;
}

double rand_range(double min_val, double max_val) {
    return (rand_float() * (max_val - min_val)) + min_val;
}

double abs_float(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double sqrtApprox(double x) {
    if (x == 0.0) {
        return 0.0;
    }
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

void pi_estimator(long long iterations) {
    double inside = 0.0;
    long long i = 0LL;
    while (i < iterations) {
        double x = rand_range(-1.0, 1.0);
        double y = rand_range(-1.0, 1.0);
        if (((x * x) + (y * y)) <= 1.0) {
            inside = inside + 1.0;
        }
        i = i + 1LL;
    }
    double proportion = inside / (double)(iterations);
    double pi_estimate = proportion * 4.0;
    printf("%s %g\n", "The estimated value of pi is", pi_estimate);
    printf("%s %g\n", "The numpy value of pi is", PI);
    printf("%s %g\n", "The total error is", abs_float(PI - pi_estimate));
}

double area_under_curve_estimator(long long iterations, double(*f)(double), double min_value, double max_value) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < iterations) {
        double x = rand_range(min_value, max_value);
        sum = sum + f(x);
        i = i + 1LL;
    }
    double expected = sum / (double)(iterations);
    return expected * (max_value - min_value);
}

void area_under_line_estimator_check(long long iterations, double min_value, double max_value) {
    double estimated_value = area_under_curve_estimator(iterations, _identity_function, min_value, max_value);
    double expected_value = ((max_value * max_value) - (min_value * min_value)) / 2.0;
    puts("******************");
    printf("%s %g\n", "Estimating area under y=x where x varies from", min_value);
    printf("%s %g\n", "Estimated value is", estimated_value);
    printf("%s %g\n", "Expected value is", expected_value);
    printf("%s %g\n", "Total error is", abs_float(estimated_value - expected_value));
    puts("******************");
}

double _identity_function(double x) {
    return x;
}

void pi_estimator_using_area_under_curve(long long iterations) {
    double estimated_value = area_under_curve_estimator(iterations, _semi_circle, 0.0, 2.0);
    puts("******************");
    puts("Estimating pi using area_under_curve_estimator");
    printf("%s %g\n", "Estimated value is", estimated_value);
    printf("%s %g\n", "Expected value is", PI);
    printf("%s %g\n", "Total error is", abs_float(estimated_value - PI));
    puts("******************");
}

double _semi_circle(double x) {
    double y = 4.0 - (x * x);
    double s = sqrtApprox(y);
    return s;
}

void user_main() {
    pi_estimator(1000LL);
    area_under_line_estimator_check(1000LL, 0.0, 1.0);
    pi_estimator_using_area_under_curve(1000LL);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
