// Generated by Mochi 0.10.32 on 2025-08-17 12:29 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct NevilleResult NevilleResult;

struct NevilleResult {
    double value;
    double **table;
    size_t table_len;
    size_t *table_lens;
    size_t table_lens_len;
};

NevilleResult neville_interpolate(double * x_points, size_t x_points_len, double * y_points, size_t y_points_len, double x0);
void test_neville();
void user_main();
int main(void);

NevilleResult neville_interpolate(double * x_points, size_t x_points_len, double * y_points, size_t y_points_len, double x0) {
    long long n = x_points_len;
    double **q = NULL;
    size_t q_len = 0;
    size_t *q_lens = NULL;
    size_t q_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, 0.0);
            j = j + 1LL;
        }
        q = list_append_doubleptr(q, &q_len, row);
        q_lens = list_append_szt(q_lens, &q_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        q[(int)(i)][(int)(1LL)] = y_points[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y_points_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    long long col = 2LL;
    while (col < n) {
        long long row_idx = col;
        while (row_idx < n) {
            q[(int)(row_idx)][(int)(col)] = (((x0 - x_points[(int)({long long _mochi_idx = (row_idx - col) + 1LL; _mochi_idx < 0 ? x_points_len + _mochi_idx : _mochi_idx;})]) * q[(int)({long long _mochi_idx = row_idx; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col - 1LL; _mochi_idx < 0 ? q_lens[(int)(row_idx)] + _mochi_idx : _mochi_idx;})]) - ((x0 - x_points[(int)({long long _mochi_idx = row_idx; _mochi_idx < 0 ? x_points_len + _mochi_idx : _mochi_idx;})]) * q[(int)({long long _mochi_idx = row_idx - 1LL; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col - 1LL; _mochi_idx < 0 ? q_lens[(int)(row_idx - 1LL)] + _mochi_idx : _mochi_idx;})])) / (x_points[(int)({long long _mochi_idx = row_idx; _mochi_idx < 0 ? x_points_len + _mochi_idx : _mochi_idx;})] - x_points[(int)({long long _mochi_idx = (row_idx - col) + 1LL; _mochi_idx < 0 ? x_points_len + _mochi_idx : _mochi_idx;})]);
            row_idx = row_idx + 1LL;
        }
        col = col + 1LL;
    }
    return (NevilleResult){.value = q[(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? q_lens[(int)(n - 1LL)] + _mochi_idx : _mochi_idx;})], .table = q, .table_len = q_len, .table_lens = q_lens, .table_lens_len = q_len};
}

void test_neville() {
    double *xs = NULL;
    size_t xs_len = 0;
    xs = list_append_double(xs, &xs_len, 1.0);
    xs = list_append_double(xs, &xs_len, 2.0);
    xs = list_append_double(xs, &xs_len, 3.0);
    xs = list_append_double(xs, &xs_len, 4.0);
    xs = list_append_double(xs, &xs_len, 6.0);
    double *ys = NULL;
    size_t ys_len = 0;
    ys = list_append_double(ys, &ys_len, 6.0);
    ys = list_append_double(ys, &ys_len, 7.0);
    ys = list_append_double(ys, &ys_len, 8.0);
    ys = list_append_double(ys, &ys_len, 9.0);
    ys = list_append_double(ys, &ys_len, 11.0);
    NevilleResult r1 = neville_interpolate(xs, xs_len, ys, ys_len, 5.0);
    if (r1.value != 10.0) {
        panic("neville_interpolate at 5 failed");
    }
    NevilleResult r2 = neville_interpolate(xs, xs_len, ys, ys_len, 99.0);
    if (r2.value != 104.0) {
        panic("neville_interpolate at 99 failed");
    }
}

void user_main() {
    test_neville();
    double *xs = NULL;
    size_t xs_len = 0;
    xs = list_append_double(xs, &xs_len, 1.0);
    xs = list_append_double(xs, &xs_len, 2.0);
    xs = list_append_double(xs, &xs_len, 3.0);
    xs = list_append_double(xs, &xs_len, 4.0);
    xs = list_append_double(xs, &xs_len, 6.0);
    double *ys = NULL;
    size_t ys_len = 0;
    ys = list_append_double(ys, &ys_len, 6.0);
    ys = list_append_double(ys, &ys_len, 7.0);
    ys = list_append_double(ys, &ys_len, 8.0);
    ys = list_append_double(ys, &ys_len, 9.0);
    ys = list_append_double(ys, &ys_len, 11.0);
    NevilleResult r = neville_interpolate(xs, xs_len, ys, ys_len, 5.0);
    printf("%g\n", r.value);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
