// Generated by Mochi 0.10.32 on 2025-08-17 12:29 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double x_points_init[4] = {0.0, 1.0, 2.0, 3.0};
double *x_points = x_points_init;
size_t x_points_len = 4;
double y_points_init[4] = {0.0, 1.0, 8.0, 27.0};
double *y_points = y_points_init;
size_t y_points_len = 4;

double ucal(double u, long long p);
double factorial(long long n);
double newton_forward_interpolation(double * x, size_t x_len, double * y0, size_t y0_len, double value);
int main(void);

double ucal(double u, long long p) {
    double temp = u;
    long long i = 1LL;
    while (i < p) {
        temp = temp * (u - (double)(i));
        i = i + 1LL;
    }
    return temp;
}

double factorial(long long n) {
    double result = 1.0;
    long long i = 2LL;
    while (i <= n) {
        result = result * (double)(i);
        i = i + 1LL;
    }
    return result;
}

double newton_forward_interpolation(double * x, size_t x_len, double * y0, size_t y0_len, double value) {
    long long n = x_len;
    double **y = NULL;
    size_t y_len = 0;
    size_t *y_lens = NULL;
    size_t y_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, 0.0);
            j = j + 1LL;
        }
        y = list_append_doubleptr(y, &y_len, row);
        y_lens = list_append_szt(y_lens, &y_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        y[(int)(i)][(int)(0LL)] = y0[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y0_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    long long i1 = 1LL;
    while (i1 < n) {
        long long j1 = 0LL;
        while (j1 < (n - i1)) {
            y[(int)(j1)][(int)(i1)] = y[(int)({long long _mochi_idx = j1 + 1LL; _mochi_idx < 0 ? y_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i1 - 1LL; _mochi_idx < 0 ? y_lens[(int)(j1 + 1LL)] + _mochi_idx : _mochi_idx;})] - y[(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? y_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i1 - 1LL; _mochi_idx < 0 ? y_lens[(int)(j1)] + _mochi_idx : _mochi_idx;})];
            j1 = j1 + 1LL;
        }
        i1 = i1 + 1LL;
    }
    double u = (value - x[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]) / (x[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})] - x[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]);
    double sum = y[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? y_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? y_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
    long long k = 1LL;
    while (k < n) {
        sum = sum + ((ucal(u, k) * y[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? y_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? y_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})]) / factorial(k));
        k = k + 1LL;
    }
    return sum;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(newton_forward_interpolation(x_points, x_points_len, y_points, y_points_len, 1.5)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
