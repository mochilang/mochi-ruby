// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long factorial(long long n);
long long factorial_recursive(long long n);
void test_zero();
void test_positive_integers();
void test_large_number();
void run_tests();
void user_main();
int main(void);

long long factorial(long long n) {
    if (n < 0LL) {
        panic("factorial() not defined for negative values");
    }
    long long value = 1LL;
    long long i = 1LL;
    while (i <= n) {
        value = value * i;
        i = i + 1LL;
    }
    return value;
}

long long factorial_recursive(long long n) {
    if (n < 0LL) {
        panic("factorial() not defined for negative values");
    }
    if (n <= 1LL) {
        return 1LL;
    }
    return n * factorial_recursive(n - 1LL);
}

void test_zero() {
    if (factorial(0LL) != 1LL) {
        panic("factorial(0) failed");
    }
    if (factorial_recursive(0LL) != 1LL) {
        panic("factorial_recursive(0) failed");
    }
}

void test_positive_integers() {
    if (factorial(1LL) != 1LL) {
        panic("factorial(1) failed");
    }
    if (factorial_recursive(1LL) != 1LL) {
        panic("factorial_recursive(1) failed");
    }
    if (factorial(5LL) != 120LL) {
        panic("factorial(5) failed");
    }
    if (factorial_recursive(5LL) != 120LL) {
        panic("factorial_recursive(5) failed");
    }
    if (factorial(7LL) != 5040LL) {
        panic("factorial(7) failed");
    }
    if (factorial_recursive(7LL) != 5040LL) {
        panic("factorial_recursive(7) failed");
    }
}

void test_large_number() {
    if (factorial(10LL) != 3628800LL) {
        panic("factorial(10) failed");
    }
    if (factorial_recursive(10LL) != 3628800LL) {
        panic("factorial_recursive(10) failed");
    }
}

void run_tests() {
    test_zero();
    test_positive_integers();
    test_large_number();
}

void user_main() {
    run_tests();
    printf("%d\n", factorial(6LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
