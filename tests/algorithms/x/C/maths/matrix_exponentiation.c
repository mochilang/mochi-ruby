// Generated by Mochi 0.10.32 on 2025-08-17 12:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t identity_len;
size_t *identity_lens;
size_t matrix_mul_len;
size_t *matrix_mul_lens;
size_t matrix_pow_len;
size_t *matrix_pow_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * identity(long long n);
long long * * matrix_mul(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
long long * * matrix_pow(long long * * base, size_t base_len, size_t* base_lens, size_t base_lens_len, long long exp);
long long fibonacci_with_matrix_exponentiation(long long n, long long f1, long long f2);
long long simple_fibonacci(long long n, long long f1, long long f2);
int main(void);

long long * * identity(long long n) {
    long long i = 0LL;
    long long **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            if (i == j) {
                row = list_append_long_long(row, &row_len, 1LL);
            } else {
                row = list_append_long_long(row, &row_len, 0LL);
            }
            j = j + 1LL;
        }
        mat = list_append_intptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return identity_lens = mat_lens, identity_len = mat_len, mat;
}

long long * * matrix_mul(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    long long n = a_len;
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            long long cell = 0LL;
            long long k = 0LL;
            while (k < n) {
                cell = cell + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_long_long(row, &row_len, cell);
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return matrix_mul_lens = result_lens, matrix_mul_len = result_len, result;
}

long long * * matrix_pow(long long * * base, size_t base_len, size_t* base_lens, size_t base_lens_len, long long exp) {
    long long **result = identity(base_len);
    size_t result_len = identity_len;
    size_t *result_lens = identity_lens;
    size_t result_lens_len = identity_len;
    long long **b = base;
    size_t b_len = base_len;
    size_t *b_lens = base_lens;
    size_t b_lens_len = base_len;
    long long e = exp;
    while (e > 0LL) {
        if ((e % 2LL) == 1LL) {
            result = matrix_mul(result, result_len, result_lens, result_len, b, b_len, b_lens, b_len);
            result_len = matrix_mul_len;
            result_lens = matrix_mul_lens;
        }
        b = matrix_mul(b, b_len, b_lens, b_len, b, b_len, b_lens, b_len);
        b_len = matrix_mul_len;
        b_lens = matrix_mul_lens;
        e = e / 2LL;
    }
    return matrix_pow_lens = result_lens, matrix_pow_len = result_len, result;
}

long long fibonacci_with_matrix_exponentiation(long long n, long long f1, long long f2) {
    if (n == 1LL) {
        return f1;
    }
    if (n == 2LL) {
        return f2;
    }
    long long base_0[2] = {1LL, 1LL};
    long long base_1[2] = {1LL, 0LL};
    long long *base_init[2] = {base_0, base_1};
    long long **base = base_init;
    size_t base_len = 2;
    size_t base_lens_init[2] = {2, 2};
    size_t *base_lens = base_lens_init;
    size_t base_lens_len = 2;
    long long **m = matrix_pow(base, base_len, base_lens, base_len, n - 2LL);
    size_t m_len = matrix_pow_len;
    size_t *m_lens = matrix_pow_lens;
    size_t m_lens_len = matrix_pow_len;
    return (f2 * m[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})]) + (f1 * m[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})]);
}

long long simple_fibonacci(long long n, long long f1, long long f2) {
    if (n == 1LL) {
        return f1;
    }
    if (n == 2LL) {
        return f2;
    }
    long long a = f1;
    long long b = f2;
    long long count = n - 2LL;
    while (count > 0LL) {
        long long tmp = a + b;
        a = b;
        b = tmp;
        count = count - 1LL;
    }
    return b;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(fibonacci_with_matrix_exponentiation(1LL, 5LL, 6LL)));
        puts(str_int(fibonacci_with_matrix_exponentiation(2LL, 10LL, 11LL)));
        puts(str_int(fibonacci_with_matrix_exponentiation(13LL, 0LL, 1LL)));
        puts(str_int(fibonacci_with_matrix_exponentiation(10LL, 5LL, 9LL)));
        puts(str_int(fibonacci_with_matrix_exponentiation(9LL, 2LL, 3LL)));
        puts(str_int(simple_fibonacci(1LL, 5LL, 6LL)));
        puts(str_int(simple_fibonacci(2LL, 10LL, 11LL)));
        puts(str_int(simple_fibonacci(13LL, 0LL, 1LL)));
        puts(str_int(simple_fibonacci(10LL, 5LL, 9LL)));
        puts(str_int(simple_fibonacci(9LL, 2LL, 3LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
