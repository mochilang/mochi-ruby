// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long gcd(long long a, long long b);
long long get_greatest_common_divisor(long long * nums, size_t nums_len);
int main(void);

long long gcd(long long a, long long b) {
    long long x = a;
    long long y = b;
    while (y != 0LL) {
        long long r = x % y;
        x = y;
        y = r;
    }
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

long long get_greatest_common_divisor(long long * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("at least one number is required");
    }
    long long g = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    if (g <= 0LL) {
        panic("numbers must be integer and greater than zero");
    }
    long long i = 1LL;
    while (i < nums_len) {
        long long n = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        if (n <= 0LL) {
            panic("numbers must be integer and greater than zero");
        }
        g = gcd(g, n);
        i = i + 1LL;
    }
    return g;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(get_greatest_common_divisor(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 18LL; tmp[1] = 45LL; tmp;}), 2)));
        puts(str_int(get_greatest_common_divisor(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 23LL; tmp[1] = 37LL; tmp;}), 2)));
        puts(str_int(get_greatest_common_divisor(({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 2520LL; tmp[1] = 8350LL; tmp;}), 2)));
        puts(str_int(get_greatest_common_divisor(({long long *tmp = malloc(10 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp[5] = 6LL; tmp[6] = 7LL; tmp[7] = 8LL; tmp[8] = 9LL; tmp[9] = 10LL; tmp;}), 10)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
