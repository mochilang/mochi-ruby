// Generated by Mochi 0.10.32 on 2025-08-11 22:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t totient_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * totient(long long n);
void test_totient();
void user_main();
int main(void);

long long * totient(long long n) {
    long long *is_prime = NULL;
    size_t is_prime_len = 0;
    long long *totients = NULL;
    size_t totients_len = 0;
    long long *primes = NULL;
    size_t primes_len = 0;
    long long i = 0LL;
    while (i <= n) {
        is_prime = list_append_long_long(is_prime, &is_prime_len, 1LL);
        totients = list_append_int(totients, &totients_len, i - 1LL);
        i = i + 1LL;
    }
    i = 2LL;
    while (i <= n) {
        if (is_prime[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? is_prime_len + _mochi_idx : _mochi_idx;})]) {
            primes = list_append_int(primes, &primes_len, i);
        }
        long long j = 0LL;
        while (j < primes_len) {
            long long p = primes[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? primes_len + _mochi_idx : _mochi_idx;})];
            if ((i * p) >= n) {
                break;
            }
            is_prime[(int)(i * p)] = 0LL;
            if ((i % p) == 0LL) {
                totients[(int)(i * p)] = totients[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? totients_len + _mochi_idx : _mochi_idx;})] * p;
                break;
            }
            totients[(int)(i * p)] = totients[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? totients_len + _mochi_idx : _mochi_idx;})] * (p - 1LL);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return totient_len = totients_len, totients;
}

void test_totient() {
    long long *expected = NULL;
    size_t expected_len = 0;
    expected = list_append_long_long(expected, &expected_len, -1LL);
    expected = list_append_long_long(expected, &expected_len, 0LL);
    expected = list_append_long_long(expected, &expected_len, 1LL);
    expected = list_append_long_long(expected, &expected_len, 2LL);
    expected = list_append_long_long(expected, &expected_len, 2LL);
    expected = list_append_long_long(expected, &expected_len, 4LL);
    expected = list_append_long_long(expected, &expected_len, 2LL);
    expected = list_append_long_long(expected, &expected_len, 6LL);
    expected = list_append_long_long(expected, &expected_len, 4LL);
    expected = list_append_long_long(expected, &expected_len, 6LL);
    expected = list_append_long_long(expected, &expected_len, 9LL);
    long long *res = totient(10LL);
    size_t res_len = totient_len;
    long long idx = 0LL;
    while (idx < 11LL) {
        if (res[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})] != expected[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? expected_len + _mochi_idx : _mochi_idx;})]) {
            panic(str_concat("totient mismatch at ", str_int(idx)));
        }
        idx = idx + 1LL;
    }
}

void user_main() {
    test_totient();
    long long n = 10LL;
    long long *res = totient(n);
    size_t res_len = totient_len;
    long long i = 1LL;
    while (i < n) {
        puts(str_concat(str_concat(str_concat(str_int(i), " has "), str_int(res[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})])), " relative primes."));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
