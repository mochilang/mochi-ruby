// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t subarray_len;
size_t merge_len;
size_t merge_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * subarray(long long * xs, size_t xs_len, long long start, long long end);
long long * merge(long long * left_half, size_t left_half_len, long long * right_half, size_t right_half_len);
long long * merge_sort(long long * array, size_t array_len);
int main(void);

long long * subarray(long long * xs, size_t xs_len, long long start, long long end) {
    long long *result = NULL;
    size_t result_len = 0;
    long long k = start;
    while (k < end) {
        result = list_append_long_long(result, &result_len, xs[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        k = k + 1LL;
    }
    return subarray_len = result_len, result;
}

long long * merge(long long * left_half, size_t left_half_len, long long * right_half, size_t right_half_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    long long j = 0LL;
    while ((i < left_half_len) && (j < right_half_len)) {
        if (left_half[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_half_len + _mochi_idx : _mochi_idx;})] < right_half[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_half_len + _mochi_idx : _mochi_idx;})]) {
            result = list_append_long_long(result, &result_len, left_half[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_half_len + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        } else {
            result = list_append_long_long(result, &result_len, right_half[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_half_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
    }
    while (i < left_half_len) {
        result = list_append_long_long(result, &result_len, left_half[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? left_half_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    while (j < right_half_len) {
        result = list_append_long_long(result, &result_len, right_half[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? right_half_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return merge_len = result_len, result;
}

long long * merge_sort(long long * array, size_t array_len) {
    if (array_len <= 1LL) {
        return merge_sort_len = array_len, array;
    }
    long long middle = array_len / 2LL;
    long long *left_half = subarray(array, array_len, 0LL, middle);
    size_t left_half_len = subarray_len;
    long long *right_half = subarray(array, array_len, middle, array_len);
    size_t right_half_len = subarray_len;
    long long *sorted_left = merge_sort(left_half, left_half_len);
    size_t sorted_left_len = merge_sort_len;
    long long *sorted_right = merge_sort(right_half, right_half_len);
    size_t sorted_right_len = merge_sort_len;
    {
        long long* __ret4 = merge(sorted_left, sorted_left_len, sorted_right, sorted_right_len);
        merge_sort_len = merge_len;
        return __ret4;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp5 = merge_sort(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 5LL; tmp[1] = 3LL; tmp[2] = 1LL; tmp[3] = 4LL; tmp[4] = 2LL; tmp;}), 5); char *__res = str_list_int(__tmp5, merge_sort_len); __res;}));
        puts(({long long *__tmp6 = merge_sort(({long long *tmp = malloc(8 * sizeof(long long)); tmp[0] = -2LL; tmp[1] = 3LL; tmp[2] = -10LL; tmp[3] = 11LL; tmp[4] = 99LL; tmp[5] = 100000LL; tmp[6] = 100LL; tmp[7] = -200LL; tmp;}), 8); char *__res = str_list_int(__tmp6, merge_sort_len); __res;}));
        puts(({long long *__tmp7 = merge_sort(({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = -200LL; tmp;}), 1); char *__res = str_list_int(__tmp7, merge_sort_len); __res;}));
        puts(({long long *__tmp8 = merge_sort(NULL, 0); char *__res = str_list_int(__tmp8, merge_sort_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
