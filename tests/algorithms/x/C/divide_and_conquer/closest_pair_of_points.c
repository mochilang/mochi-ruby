// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t column_based_sort_len;
size_t *column_based_sort_lens;

size_t _slice_doubleptr_len;
size_t *_slice_doubleptr_lens;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static double** _slice_doubleptr(double **arr, size_t len, size_t *lens, int start, int end, size_t *out_len, size_t **out_lens) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    *out_len = n;
    _slice_doubleptr_len = n;
    *out_lens = lens + start;
    _slice_doubleptr_lens = lens + start;
    return arr + start;
}

double **points = NULL;
size_t points_len = 0;
size_t *points_lens = NULL;
size_t points_lens_len = 0;

double user_abs(double x);
double sqrtApprox(double x);
double euclidean_distance_sqr(double * p1, size_t p1_len, double * p2, size_t p2_len);
double * * column_based_sort(double * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long column);
double dis_between_closest_pair(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long count_, double min_dis);
double dis_between_closest_in_strip(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long count_, double min_dis);
double closest_pair_of_points_sqr(double * * px, size_t px_len, size_t* px_lens, size_t px_lens_len, double * * py, size_t py_len, size_t* py_lens, size_t py_lens_len, long long count_);
double closest_pair_of_points(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long count_);
int main(void);

double user_abs(double x) {
    if (x < 0.0) {
        return 0.0 - x;
    }
    return x;
}

double sqrtApprox(double x) {
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double euclidean_distance_sqr(double * p1, size_t p1_len, double * p2, size_t p2_len) {
    double dx = p1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? p1_len + _mochi_idx : _mochi_idx;})] - p2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? p2_len + _mochi_idx : _mochi_idx;})];
    double dy = p1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? p1_len + _mochi_idx : _mochi_idx;})] - p2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? p2_len + _mochi_idx : _mochi_idx;})];
    return (dx * dx) + (dy * dy);
}

double * * column_based_sort(double * * arr, size_t arr_len, size_t* arr_lens, size_t arr_lens_len, long long column) {
    double **points = arr;
    size_t points_len = arr_len;
    size_t *points_lens = arr_lens;
    size_t points_lens_len = arr_len;
    long long i = 0LL;
    while (i < points_len) {
        long long j = 0LL;
        while (j < (points_len - 1LL)) {
            if (points[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column; _mochi_idx < 0 ? points_lens[(int)(j)] + _mochi_idx : _mochi_idx;})] > points[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column; _mochi_idx < 0 ? points_lens[(int)(j + 1LL)] + _mochi_idx : _mochi_idx;})]) {
                double *tmp = points[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})];
                size_t tmp_len = points_lens[(int)(j)];
                points[(int)(j)] = points[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})];
                points[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return column_based_sort_lens = points_lens, column_based_sort_len = points_len, points;
}

double dis_between_closest_pair(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long count_, double min_dis) {
    long long i = 0LL;
    while (i < (count_ - 1LL)) {
        long long j = i + 1LL;
        while (j < count_) {
            double current = euclidean_distance_sqr(points[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})], points_lens[(int)(i)], points[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})], points_lens[(int)(j)]);
            if (current < min_dis) {
                min_dis = current;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return min_dis;
}

double dis_between_closest_in_strip(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long count_, double min_dis) {
    long long i_start = 0LL;
    if (6LL < (count_ - 1LL)) {
        i_start = 6LL;
    } else {
        i_start = count_ - 1LL;
    }
    long long i = i_start;
    while (i < count_) {
        long long j_start = 0LL;
        if ((i - 6LL) > 0LL) {
            j_start = i - 6LL;
        }
        long long j = j_start;
        while (j < i) {
            double current = euclidean_distance_sqr(points[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})], points_lens[(int)(i)], points[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? points_len + _mochi_idx : _mochi_idx;})], points_lens[(int)(j)]);
            if (current < min_dis) {
                min_dis = current;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return min_dis;
}

double closest_pair_of_points_sqr(double * * px, size_t px_len, size_t* px_lens, size_t px_lens_len, double * * py, size_t py_len, size_t* py_lens, size_t py_lens_len, long long count_) {
    if (count_ <= 3LL) {
        return dis_between_closest_pair(px, px_len, px_lens, px_len, count_, 1e+18);
    }
    long long mid = count_ / 2LL;
    double left = ({double** __tmp2 = _slice_doubleptr(py, py_len, py_lens, 0LL, mid, &(_slice_doubleptr_len), &(_slice_doubleptr_lens)); size_t __tmp2_len = _slice_doubleptr_len; size_t* __tmp2_lens = _slice_doubleptr_lens; closest_pair_of_points_sqr(px, px_len, px_lens, px_len, __tmp2, __tmp2_len, __tmp2_lens, __tmp2_len, mid);});
    double right = ({double** __tmp3 = _slice_doubleptr(py, py_len, py_lens, mid, count_, &(_slice_doubleptr_len), &(_slice_doubleptr_lens)); size_t __tmp3_len = _slice_doubleptr_len; size_t* __tmp3_lens = _slice_doubleptr_lens; closest_pair_of_points_sqr(py, py_len, py_lens, py_len, __tmp3, __tmp3_len, __tmp3_lens, __tmp3_len, count_ - mid);});
    double best = left;
    if (right < best) {
        best = right;
    }
    double **strip = NULL;
    size_t strip_len = 0;
    size_t *strip_lens = NULL;
    size_t strip_lens_len = 0;
    long long i = 0LL;
    while (i < px_len) {
        if (user_abs(px[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? px_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? px_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] - px[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? px_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? px_lens[(int)(mid)] + _mochi_idx : _mochi_idx;})]) < best) {
            strip = list_append_doubleptr(strip, &strip_len, px[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? px_len + _mochi_idx : _mochi_idx;})]);
            strip_lens = list_append_szt(strip_lens, &strip_lens_len, px_lens[(int)(i)]);
        }
        i = i + 1LL;
    }
    double strip_best = dis_between_closest_in_strip(strip, strip_len, strip_lens, strip_len, strip_len, best);
    if (strip_best < best) {
        best = strip_best;
    }
    return best;
}

double closest_pair_of_points(double * * points, size_t points_len, size_t* points_lens, size_t points_lens_len, long long count_) {
    double **points_sorted_on_x = column_based_sort(points, points_len, points_lens, points_len, 0LL);
    size_t points_sorted_on_x_len = column_based_sort_len;
    size_t *points_sorted_on_x_lens = column_based_sort_lens;
    size_t points_sorted_on_x_lens_len = column_based_sort_len;
    double **points_sorted_on_y = column_based_sort(points, points_len, points_lens, points_len, 1LL);
    size_t points_sorted_on_y_len = column_based_sort_len;
    size_t *points_sorted_on_y_lens = column_based_sort_lens;
    size_t points_sorted_on_y_lens_len = column_based_sort_len;
    double dist_sqr = closest_pair_of_points_sqr(points_sorted_on_x, points_sorted_on_x_len, points_sorted_on_x_lens, points_sorted_on_x_len, points_sorted_on_y, points_sorted_on_y_len, points_sorted_on_y_lens, points_sorted_on_y_len, count_);
    return sqrtApprox(dist_sqr);
}

int main(void) {
    {
        long long __start = _now();
        points = malloc(6 * sizeof(double*));
        points_len = 6;
        points[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 3.0; tmp;});
        points[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 12.0; tmp[1] = 30.0; tmp;});
        points[2] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 40.0; tmp[1] = 50.0; tmp;});
        points[3] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 5.0; tmp[1] = 1.0; tmp;});
        points[4] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 12.0; tmp[1] = 10.0; tmp;});
        points[5] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 3.0; tmp[1] = 4.0; tmp;});
        points_lens = ({size_t *tmp = malloc(6 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp[3] = 2; tmp[4] = 2; tmp[5] = 2; tmp;});
        points_lens_len = 6;
        puts(str_concat("Distance: ", str_float(closest_pair_of_points(points, points_len, points_lens, points_len, points_len))));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
