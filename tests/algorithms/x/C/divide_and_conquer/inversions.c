// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t slice_list_len;

size_t append_len;
static size_t concat_len;
static long long* concat_long_long(long long *a, size_t a_len, const long long *b, size_t b_len) {
    long long *res = realloc(a, (a_len + b_len) * sizeof(long long));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(long long));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct InvResult InvResult;

struct InvResult {
    long long *arr;
    size_t arr_len;
    long long inv;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *arr_1 = NULL;
size_t arr_1_len = 0;
long long nbf = 0;
long long nrec = 0;
long long nbf2 = 0;
long long nrec2 = 0;
long long nbf3 = 0;
long long nrec3 = 0;

long long * slice_list(long long * arr, size_t arr_len, long long start, long long end);
long long count_inversions_bf(long long * arr, size_t arr_len);
InvResult count_cross_inversions(long long * p, size_t p_len, long long * q, size_t q_len);
InvResult count_inversions_recursive(long long * arr, size_t arr_len);
int main(void);

long long * slice_list(long long * arr, size_t arr_len, long long start, long long end) {
    long long *res = NULL;
    size_t res_len = 0;
    long long k = start;
    while (k < end) {
        res = list_append_long_long(res, &res_len, arr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        k = k + 1LL;
    }
    return slice_list_len = res_len, res;
}

long long count_inversions_bf(long long * arr, size_t arr_len) {
    long long n = arr_len;
    long long inv = 0LL;
    long long i = 0LL;
    while (i < (n - 1LL)) {
        long long j = i + 1LL;
        while (j < n) {
            if (arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                inv = inv + 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return inv;
}

InvResult count_cross_inversions(long long * p, size_t p_len, long long * q, size_t q_len) {
    long long *r = NULL;
    size_t r_len = 0;
    long long i = 0LL;
    long long j = 0LL;
    long long inv = 0LL;
    while ((i < p_len) && (j < q_len)) {
        if (p[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? p_len + _mochi_idx : _mochi_idx;})] > q[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})]) {
            inv = inv + (p_len - i);
            r = list_append_long_long(r, &r_len, q[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        } else {
            r = list_append_long_long(r, &r_len, p[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? p_len + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
    }
    if (i < p_len) {
        r = concat_long_long(r, r_len, slice_list(p, p_len, i, p_len), slice_list_len);
        r_len = concat_len;
    } else {
        r = concat_long_long(r, r_len, slice_list(q, q_len, j, q_len), slice_list_len);
        r_len = concat_len;
    }
    return (InvResult){.arr = r, .arr_len = r_len, .inv = inv};
}

InvResult count_inversions_recursive(long long * arr, size_t arr_len) {
    if (arr_len <= 1LL) {
        return (InvResult){.arr = arr, .arr_len = arr_len, .inv = 0LL};
    }
    long long mid = arr_len / 2LL;
    long long *p = slice_list(arr, arr_len, 0LL, mid);
    size_t p_len = slice_list_len;
    long long *q = slice_list(arr, arr_len, mid, arr_len);
    size_t q_len = slice_list_len;
    InvResult res_p = count_inversions_recursive(p, p_len);
    InvResult res_q = count_inversions_recursive(q, q_len);
    InvResult res_cross = count_cross_inversions(res_p.arr, res_p.arr_len, res_q.arr, res_q.arr_len);
    long long total = (res_p.inv + res_q.inv) + res_cross.inv;
    return (InvResult){.arr = res_cross.arr, .arr_len = res_cross.arr_len, .inv = total};
}

int main(void) {
    {
        long long __start = _now();
        arr_1 = malloc(7 * sizeof(long long));
        arr_1_len = 7;
        arr_1[0] = 10LL;
        arr_1[1] = 2LL;
        arr_1[2] = 1LL;
        arr_1[3] = 5LL;
        arr_1[4] = 5LL;
        arr_1[5] = 2LL;
        arr_1[6] = 11LL;
        nbf = count_inversions_bf(arr_1, arr_1_len);
        nrec = count_inversions_recursive(arr_1, arr_1_len).inv;
        printf("%s %d\n", "number of inversions = ", nbf);
        arr_1 = malloc(7 * sizeof(long long));
        arr_1_len = 7;
        arr_1[0] = 1LL;
        arr_1[1] = 2LL;
        arr_1[2] = 2LL;
        arr_1[3] = 5LL;
        arr_1[4] = 5LL;
        arr_1[5] = 10LL;
        arr_1[6] = 11LL;
        nbf2 = count_inversions_bf(arr_1, arr_1_len);
        nrec2 = count_inversions_recursive(arr_1, arr_1_len).inv;
        printf("%s %d\n", "number of inversions = ", nbf2);
        arr_1 = malloc(0 * sizeof(long long));
        arr_1_len = 0;
        nbf3 = count_inversions_bf(arr_1, arr_1_len);
        nrec3 = count_inversions_recursive(arr_1, arr_1_len).inv;
        printf("%s %d\n", "number of inversions = ", nbf3);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
