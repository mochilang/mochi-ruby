// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long pivot(long long * lst, size_t lst_len);
long long kth_number(long long * lst, size_t lst_len, long long k);
int main(void);

long long pivot(long long * lst, size_t lst_len) {
    return lst[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})];
}

long long kth_number(long long * lst, size_t lst_len, long long k) {
    long long p = pivot(lst, lst_len);
    long long *small = NULL;
    size_t small_len = 0;
    long long *big = NULL;
    size_t big_len = 0;
    long long i = 0LL;
    while (i < lst_len) {
        long long e = lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})];
        if (e < p) {
            small = list_append_long_long(small, &small_len, e);
        } else {
            if (e > p) {
                big = list_append_long_long(big, &big_len, e);
            }
        }
        i = i + 1LL;
    }
    if (small_len == (k - 1LL)) {
        return p;
    } else {
        if (small_len < (k - 1LL)) {
            return kth_number(big, big_len, (k - small_len) - 1LL);
        } else {
            return kth_number(small, small_len, k);
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(kth_number(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp;}), 5, 3LL)));
        puts(str_int(kth_number(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp;}), 5, 1LL)));
        puts(str_int(kth_number(({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 3LL; tmp[3] = 4LL; tmp[4] = 5LL; tmp;}), 5, 5LL)));
        puts(str_int(kth_number(({long long *tmp = malloc(6 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 2LL; tmp[2] = 5LL; tmp[3] = 6LL; tmp[4] = 7LL; tmp[5] = 8LL; tmp;}), 6, 2LL)));
        puts(str_int(kth_number(({long long *tmp = malloc(10 * sizeof(long long)); tmp[0] = 25LL; tmp[1] = 21LL; tmp[2] = 98LL; tmp[3] = 100LL; tmp[4] = 76LL; tmp[5] = 22LL; tmp[6] = 43LL; tmp[7] = 60LL; tmp[8] = 89LL; tmp[9] = 87LL; tmp;}), 10, 4LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
