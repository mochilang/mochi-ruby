// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sortPoints_len;
size_t convex_hull_len;

size_t _slice_Point_len;
size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Point Point;

struct Point {
    long long x;
    long long y;
};

static struct Point* _slice_Point(struct Point *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    *out_len = n;
    _slice_Point_len = n;
    return arr + start;
}

static Point* list_append_Point(Point *arr, size_t *len, Point val) {
    arr = realloc(arr, (*len + 1) * sizeof(Point));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long cross(Point o, Point a, Point b);
Point * sortPoints(Point * ps, size_t ps_len);
Point * convex_hull(Point * ps, size_t ps_len);
int main(void);

long long cross(Point o, Point a, Point b) {
    return ((a.x - o.x) * (b.y - o.y)) - ((a.y - o.y) * (b.x - o.x));
}

Point * sortPoints(Point * ps, size_t ps_len) {
    Point *arr = ps;
    size_t arr_len = ps_len;
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j < (n - 1LL)) {
            Point p = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            Point q = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            if ((p.x > q.x) || ((p.x == q.x) && (p.y > q.y))) {
                arr[(int)(j)] = q;
                arr[(int)(j + 1LL)] = p;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sortPoints_len = arr_len, arr;
}

Point * convex_hull(Point * ps, size_t ps_len) {
    ps = sortPoints(ps, ps_len);
    ps_len = sortPoints_len;
    Point *lower = NULL;
    size_t lower_len = 0;
    for (size_t __i = 0; __i < ps_len; __i++) {
        Point p = ps[__i];
        while ((lower_len >= 2LL) && (cross(lower[(int)({long long _mochi_idx = lower_len - 2LL; _mochi_idx < 0 ? lower_len + _mochi_idx : _mochi_idx;})], lower[(int)({long long _mochi_idx = lower_len - 1LL; _mochi_idx < 0 ? lower_len + _mochi_idx : _mochi_idx;})], p) <= 0LL)) {
            lower = _slice_Point(lower, lower_len, 0LL, lower_len - 1LL, &(_slice_Point_len));
            lower_len = _slice_Point_len;
        }
        lower = list_append_Point(lower, &lower_len, p);
    }
    Point *upper = NULL;
    size_t upper_len = 0;
    long long i = ps_len - 1LL;
    while (i >= 0LL) {
        Point p = ps[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ps_len + _mochi_idx : _mochi_idx;})];
        while ((upper_len >= 2LL) && (cross(upper[(int)({long long _mochi_idx = upper_len - 2LL; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})], upper[(int)({long long _mochi_idx = upper_len - 1LL; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})], p) <= 0LL)) {
            upper = _slice_Point(upper, upper_len, 0LL, upper_len - 1LL, &(_slice_Point_len));
            upper_len = _slice_Point_len;
        }
        upper = list_append_Point(upper, &upper_len, p);
        i = i - 1LL;
    }
    Point *hull = _slice_Point(lower, lower_len, 0LL, lower_len - 1LL, &(_slice_Point_len));
    size_t hull_len = _slice_Point_len;
    long long j = 0LL;
    while (j < (upper_len - 1LL)) {
        hull = list_append_Point(hull, &hull_len, upper[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    return convex_hull_len = hull_len, hull;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
