// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t copy_list_len;
size_t heaps_len;
size_t *heaps_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long* list_append_long_long_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    return res;
}

long long * copy_list(long long * arr, size_t arr_len);
long long * * heaps(long long * arr, size_t arr_len);
int main(void);

long long * copy_list(long long * arr, size_t arr_len) {
    long long *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < arr_len) {
        result = list_append_long_long(result, &result_len, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return copy_list_len = result_len, result;
}

long long * * heaps(long long * arr, size_t arr_len) {
    if (arr_len <= 1LL) {
        long long **single = NULL;
        size_t single_len = 0;
        size_t *single_lens = NULL;
        size_t single_lens_len = 0;
        return heaps_len = single_len + 1, list_append_long_long_new(single, single_len, copy_list(arr, arr_len));
    }
    long long n = arr_len;
    long long *c = NULL;
    size_t c_len = 0;
    long long i = 0LL;
    while (i < n) {
        c = list_append_long_long(c, &c_len, 0LL);
        i = i + 1LL;
    }
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    res = list_append_intptr(res, &res_len, copy_list(arr, arr_len));
    res_lens = list_append_szt(res_lens, &res_lens_len, copy_list_len);
    i = 0LL;
    while (i < n) {
        if (c[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c_len + _mochi_idx : _mochi_idx;})] < i) {
            if ((i % 2LL) == 0LL) {
                long long temp = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(0LL)] = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i)] = temp;
            } else {
                long long temp = arr[(int)({long long _mochi_idx = c[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(c[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c_len + _mochi_idx : _mochi_idx;})])] = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(i)] = temp;
            }
            res = list_append_intptr(res, &res_len, copy_list(arr, arr_len));
            res_lens = list_append_szt(res_lens, &res_lens_len, copy_list_len);
            c[(int)(i)] = c[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c_len + _mochi_idx : _mochi_idx;})] + 1LL;
            i = 0LL;
        } else {
            c[(int)(i)] = 0LL;
            i = i + 1LL;
        }
    }
    return heaps_lens = res_lens, heaps_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long **__tmp1 = heaps(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3); char *__res = str_list_list_int(__tmp1, heaps_len, heaps_lens); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
