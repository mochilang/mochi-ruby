// Generated by Mochi 0.10.32 on 2025-08-25 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t permute_len;
size_t *permute_lens;
size_t heaps_len;
size_t *heaps_lens;

size_t _slice_int_len;
size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    return res;
}

long long * * permute(long long k, long long * arr, size_t arr_len, long long * * res, size_t res_len, size_t* res_lens, size_t res_lens_len);
long long * * heaps(long long * arr, size_t arr_len);
void user_main();
int main(void);

long long * * permute(long long k, long long * arr, size_t arr_len, long long * * res, size_t res_len, size_t* res_lens, size_t res_lens_len) {
    if (k == 1LL) {
        long long *copy = _slice_int(arr, arr_len, 0LL, arr_len, &(_slice_int_len));
        size_t copy_len = _slice_int_len;
        return permute_len = res_len + 1, list_append_long_long_new(res, res_len, copy);
    }
    res = permute(k - 1LL, arr, arr_len, res, res_len, res_lens, res_len);
    res_len = permute_len;
    res_lens = permute_lens;
    long long i = 0LL;
    while (i < (k - 1LL)) {
        if ((k % 2LL) == 0LL) {
            long long temp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(i)] = arr[(int)({long long _mochi_idx = k - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(k - 1LL)] = temp;
        } else {
            long long temp = arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(0LL)] = arr[(int)({long long _mochi_idx = k - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            arr[(int)(k - 1LL)] = temp;
        }
        res = permute(k - 1LL, arr, arr_len, res, res_len, res_lens, res_len);
        res_len = permute_len;
        res_lens = permute_lens;
        i = i + 1LL;
    }
    return permute_lens = res_lens, permute_len = res_len, res;
}

long long * * heaps(long long * arr, size_t arr_len) {
    if (arr_len <= 1LL) {
        {
            size_t *__tmp0_lens = malloc(1 * sizeof(size_t));
            __tmp0_lens[0] = _slice_int_len;
            long long** __tmp0_data = malloc(1 * sizeof(long long*));
            __tmp0_data[0] = _slice_int(arr, arr_len, 0LL, arr_len, &(_slice_int_len));
            heaps_lens = __tmp0_lens;
            heaps_len = 1;
            return __tmp0_data;
        }
    }
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    res = permute(arr_len, arr, arr_len, res, res_len, res_lens, res_len);
    res_len = permute_len;
    res_lens = permute_lens;
    return heaps_lens = res_lens, heaps_len = res_len, res;
}

void user_main() {
    long long **perms = heaps(({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), 3);
    size_t perms_len = heaps_len;
    size_t *perms_lens = heaps_lens;
    size_t perms_lens_len = heaps_len;
    puts(str_list_list_int(perms, perms_len, perms_lens));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
