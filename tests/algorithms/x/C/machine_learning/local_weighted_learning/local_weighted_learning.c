// Generated by Mochi 0.10.32 on 2025-08-16 20:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

size_t transpose_len;
size_t *transpose_lens;
size_t matMul_len;
size_t *matMul_lens;
size_t matInv_len;
size_t *matInv_lens;
size_t weight_matrix_len;
size_t *weight_matrix_lens;
size_t local_weight_len;
size_t *local_weight_lens;
size_t local_weight_regression_len;

size_t append_len;
static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double x_train_0[2] = {16.99, 10.34};
double x_train_1[2] = {21.01, 23.68};
double x_train_2[2] = {24.59, 25.69};
double *x_train_init[3] = {x_train_0, x_train_1, x_train_2};
double **x_train = x_train_init;
size_t x_train_len = 3;
size_t x_train_lens_init[3] = {2, 2, 2};
size_t *x_train_lens = x_train_lens_init;
size_t x_train_lens_len = 3;
double y_train_init[3] = {1.01, 1.66, 3.5};
double *y_train = y_train_init;
size_t y_train_len = 3;
double *preds = NULL;
size_t preds_len = 0;

double expApprox(double x);
double * * transpose(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
double * * matMul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
double * * matInv(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
double * * weight_matrix(double * point, size_t point_len, double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double tau);
double * * local_weight(double * point, size_t point_len, double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double * y_train, size_t y_train_len, double tau);
double * local_weight_regression(double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double * y_train, size_t y_train_len, double tau);
int main(void);

double expApprox(double x) {
    if (x < 0.0) {
        return 1.0 / expApprox(-(x));
    }
    if (x > 1.0) {
        double half = expApprox(x / 2.0);
        return half * half;
    }
    double sum = 1.0;
    double term = 1.0;
    long long n = 1LL;
    while (n < 20LL) {
        term = (term * x) / (double)(n);
        sum = sum + term;
        n = n + 1LL;
    }
    return sum;
}

double * * transpose(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long rows = mat_len;
    long long cols = mat_lens[(int)(0LL)];
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < cols) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < rows) {
            row = list_append_double(row, &row_len, mat[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_lens[(int)(j)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return transpose_lens = res_lens, transpose_len = res_len, res;
}

double * * matMul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    long long a_rows = a_len;
    long long a_cols = a_lens[(int)(0LL)];
    long long b_cols = b_lens[(int)(0LL)];
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < a_rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < b_cols) {
            double sum = 0.0;
            long long k = 0LL;
            while (k < a_cols) {
                sum = sum + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_double(row, &row_len, sum);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return matMul_lens = res_lens, matMul_len = res_len, res;
}

double * * matInv(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long n = mat_len;
    double **aug = NULL;
    size_t aug_len = 0;
    size_t *aug_lens = NULL;
    size_t aug_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        j = 0LL;
        while (j < n) {
            if (i == j) {
                row = list_append_double(row, &row_len, 1.0);
            } else {
                row = list_append_double(row, &row_len, 0.0);
            }
            j = j + 1LL;
        }
        aug = list_append_doubleptr(aug, &aug_len, row);
        aug_lens = list_append_szt(aug_lens, &aug_lens_len, row_len);
        i = i + 1LL;
    }
    long long col = 0LL;
    while (col < n) {
        double pivot = aug[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? aug_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? aug_lens[(int)(col)] + _mochi_idx : _mochi_idx;})];
        if (pivot == 0.0) {
            panic("Matrix is singular");
        }
        long long j = 0LL;
        while (j < (2LL * n)) {
            aug[(int)(col)][(int)(j)] = aug[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? aug_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? aug_lens[(int)(col)] + _mochi_idx : _mochi_idx;})] / pivot;
            j = j + 1LL;
        }
        long long r = 0LL;
        while (r < n) {
            if (r != col) {
                double factor = aug[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? aug_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? aug_lens[(int)(r)] + _mochi_idx : _mochi_idx;})];
                j = 0LL;
                while (j < (2LL * n)) {
                    aug[(int)(r)][(int)(j)] = aug[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? aug_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? aug_lens[(int)(r)] + _mochi_idx : _mochi_idx;})] - (factor * aug[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? aug_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? aug_lens[(int)(col)] + _mochi_idx : _mochi_idx;})]);
                    j = j + 1LL;
                }
            }
            r = r + 1LL;
        }
        col = col + 1LL;
    }
    double **inv = NULL;
    size_t inv_len = 0;
    size_t *inv_lens = NULL;
    size_t inv_lens_len = 0;
    i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, aug[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? aug_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j + n; _mochi_idx < 0 ? aug_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        inv = list_append_doubleptr(inv, &inv_len, row);
        inv_lens = list_append_szt(inv_lens, &inv_lens_len, row_len);
        i = i + 1LL;
    }
    return matInv_lens = inv_lens, matInv_len = inv_len, inv;
}

double * * weight_matrix(double * point, size_t point_len, double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double tau) {
    long long m = x_train_len;
    double **weights = NULL;
    size_t weights_len = 0;
    size_t *weights_lens = NULL;
    size_t weights_lens_len = 0;
    long long i = 0LL;
    while (i < m) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m) {
            if (i == j) {
                row = list_append_double(row, &row_len, 1.0);
            } else {
                row = list_append_double(row, &row_len, 0.0);
            }
            j = j + 1LL;
        }
        weights = list_append_doubleptr(weights, &weights_len, row);
        weights_lens = list_append_szt(weights_lens, &weights_lens_len, row_len);
        i = i + 1LL;
    }
    long long j = 0LL;
    while (j < m) {
        double diff_sq = 0.0;
        long long k = 0LL;
        while (k < point_len) {
            double diff = point[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? point_len + _mochi_idx : _mochi_idx;})] - x_train[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? x_train_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? x_train_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
            diff_sq = diff_sq + (diff * diff);
            k = k + 1LL;
        }
        weights[(int)(j)][(int)(j)] = expApprox(-(diff_sq) / ((2.0 * tau) * tau));
        j = j + 1LL;
    }
    return weight_matrix_lens = weights_lens, weight_matrix_len = weights_len, weights;
}

double * * local_weight(double * point, size_t point_len, double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double * y_train, size_t y_train_len, double tau) {
    double **w = weight_matrix(point, point_len, x_train, x_train_len, x_train_lens, x_train_len, tau);
    size_t w_len = weight_matrix_len;
    size_t *w_lens = weight_matrix_lens;
    size_t w_lens_len = weight_matrix_len;
    double **x_t = transpose(x_train, x_train_len, x_train_lens, x_train_len);
    size_t x_t_len = transpose_len;
    size_t *x_t_lens = transpose_lens;
    size_t x_t_lens_len = transpose_len;
    double **x_t_w = matMul(x_t, x_t_len, x_t_lens, x_t_len, w, w_len, w_lens, w_len);
    size_t x_t_w_len = matMul_len;
    size_t *x_t_w_lens = matMul_lens;
    size_t x_t_w_lens_len = matMul_len;
    double **x_t_w_x = matMul(x_t_w, x_t_w_len, x_t_w_lens, x_t_w_len, x_train, x_train_len, x_train_lens, x_train_len);
    size_t x_t_w_x_len = matMul_len;
    size_t *x_t_w_x_lens = matMul_lens;
    size_t x_t_w_x_lens_len = matMul_len;
    double **inv_part = matInv(x_t_w_x, x_t_w_x_len, x_t_w_x_lens, x_t_w_x_len);
    size_t inv_part_len = matInv_len;
    size_t *inv_part_lens = matInv_lens;
    size_t inv_part_lens_len = matInv_len;
    double **y_col = NULL;
    size_t y_col_len = 0;
    size_t *y_col_lens = NULL;
    size_t y_col_lens_len = 0;
    long long i = 0LL;
    while (i < y_train_len) {
        y_col = list_append_doubleptr(y_col, &y_col_len, ({double* tmp = malloc(1 * sizeof(double)); tmp[0] = y_train[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y_train_len + _mochi_idx : _mochi_idx;})]; tmp;}));
        y_col_lens = list_append_szt(y_col_lens, &y_col_lens_len, 1);
        i = i + 1LL;
    }
    double **x_t_w_y = matMul(x_t_w, x_t_w_len, x_t_w_lens, x_t_w_len, y_col, y_col_len, y_col_lens, y_col_len);
    size_t x_t_w_y_len = matMul_len;
    size_t *x_t_w_y_lens = matMul_lens;
    size_t x_t_w_y_lens_len = matMul_len;
    {
        double** __ret0 = matMul(inv_part, inv_part_len, inv_part_lens, inv_part_len, x_t_w_y, x_t_w_y_len, x_t_w_y_lens, x_t_w_y_len);
        local_weight_lens = matMul_lens;
local_weight_len = matMul_len;
        return __ret0;
    }
}

double * local_weight_regression(double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double * y_train, size_t y_train_len, double tau) {
    long long m = x_train_len;
    double *preds = NULL;
    size_t preds_len = 0;
    long long i = 0LL;
    while (i < m) {
        double **theta = local_weight(x_train[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_train_len + _mochi_idx : _mochi_idx;})], x_train_lens[(int)(i)], x_train, x_train_len, x_train_lens, x_train_len, y_train, y_train_len, tau);
        size_t theta_len = local_weight_len;
        size_t *theta_lens = local_weight_lens;
        size_t theta_lens_len = local_weight_len;
        double *weights_vec = NULL;
        size_t weights_vec_len = 0;
        long long k = 0LL;
        while (k < theta_len) {
            weights_vec = list_append_double(weights_vec, &weights_vec_len, theta[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? theta_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? theta_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
            k = k + 1LL;
        }
        double pred = 0.0;
        long long j = 0LL;
        while (j < x_train_lens[(int)(i)]) {
            pred = pred + (x_train[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_train_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? x_train_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * weights_vec[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? weights_vec_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        preds = list_append_double(preds, &preds_len, pred);
        i = i + 1LL;
    }
    return local_weight_regression_len = preds_len, preds;
}

int main(void) {
    preds = local_weight_regression(x_train, x_train_len, x_train_lens, x_train_len, y_train, y_train_len, 0.6);
    preds_len = local_weight_regression_len;
    puts(str_list_double(preds, preds_len));
    return 0;
}
