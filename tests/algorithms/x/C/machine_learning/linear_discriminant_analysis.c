// Generated by Mochi 0.10.32 on 2025-08-16 20:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t gaussian_distribution_len;
size_t y_generator_len;
size_t predict_y_values_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double PI = 3.141592653589793;
double TWO_PI = 6.283185307179586;
long long seed = 1LL;

long long user_rand();
double user_random();
double _mod(double x, double m);
double cos(double x);
double sqrtApprox(double x);
double user_ln(double x);
double * gaussian_distribution(double mean, double std_dev, long long instance_count);
long long * y_generator(long long class_count, long long * instance_count, size_t instance_count_len);
double calculate_mean(long long instance_count, double * items, size_t items_len);
double calculate_probabilities(long long instance_count, long long total_count);
double calculate_variance(double * * items, size_t items_len, size_t* items_lens, size_t items_lens_len, double * means, size_t means_len, long long total_count);
long long * predict_y_values(double * * x_items, size_t x_items_len, size_t* x_items_lens, size_t x_items_lens_len, double * means, size_t means_len, double variance, double * probabilities, size_t probabilities_len);
double accuracy(long long * actual_y, size_t actual_y_len, long long * predicted_y, size_t predicted_y_len);
void user_main();
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed;
}

double user_random() {
    return (double)(user_rand()) / 2.147483648e+09;
}

double _mod(double x, double m) {
    return x - ((double)((int)(x / m)) * m);
}

double cos(double x) {
    double y = _mod(x + PI, TWO_PI) - PI;
    double y2 = y * y;
    double y4 = y2 * y2;
    double y6 = y4 * y2;
    return ((1.0 - (y2 / 2.0)) + (y4 / 24.0)) - (y6 / 720.0);
}

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double user_ln(double x) {
    double t = (x - 1.0) / (x + 1.0);
    double term = t;
    double sum = 0.0;
    long long n = 1LL;
    while (n <= 19LL) {
        sum = sum + (term / (double)(n));
        term = (term * t) * t;
        n = n + 2LL;
    }
    return 2.0 * sum;
}

double * gaussian_distribution(double mean, double std_dev, long long instance_count) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < instance_count) {
        double u1 = user_random();
        double u2 = user_random();
        double r = sqrtApprox(-2.0 * log(u1));
        double theta = TWO_PI * u2;
        double z = r * cos(theta);
        res = list_append_double(res, &res_len, mean + (z * std_dev));
        i = i + 1LL;
    }
    return gaussian_distribution_len = res_len, res;
}

long long * y_generator(long long class_count, long long * instance_count, size_t instance_count_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long k = 0LL;
    while (k < class_count) {
        long long i = 0LL;
        while (i < instance_count[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? instance_count_len + _mochi_idx : _mochi_idx;})]) {
            res = list_append_long_long(res, &res_len, k);
            i = i + 1LL;
        }
        k = k + 1LL;
    }
    return y_generator_len = res_len, res;
}

double calculate_mean(long long instance_count, double * items, size_t items_len) {
    double total = 0.0;
    long long i = 0LL;
    while (i < instance_count) {
        total = total + items[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? items_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return total / (double)(instance_count);
}

double calculate_probabilities(long long instance_count, long long total_count) {
    return (double)(instance_count) / (double)(total_count);
}

double calculate_variance(double * * items, size_t items_len, size_t* items_lens, size_t items_lens_len, double * means, size_t means_len, long long total_count) {
    double *squared_diff = NULL;
    size_t squared_diff_len = 0;
    long long i = 0LL;
    while (i < items_len) {
        long long j = 0LL;
        while (j < items_lens[(int)(i)]) {
            double diff = items[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? items_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? items_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] - means[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? means_len + _mochi_idx : _mochi_idx;})];
            squared_diff = list_append_double(squared_diff, &squared_diff_len, diff * diff);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    double sum_sq = 0.0;
    long long k = 0LL;
    while (k < squared_diff_len) {
        sum_sq = sum_sq + squared_diff[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? squared_diff_len + _mochi_idx : _mochi_idx;})];
        k = k + 1LL;
    }
    long long n_classes = means_len;
    return (1.0 / (total_count - n_classes)) * sum_sq;
}

long long * predict_y_values(double * * x_items, size_t x_items_len, size_t* x_items_lens, size_t x_items_lens_len, double * means, size_t means_len, double variance, double * probabilities, size_t probabilities_len) {
    long long *results = NULL;
    size_t results_len = 0;
    long long i = 0LL;
    while (i < x_items_len) {
        long long j = 0LL;
        while (j < x_items_lens[(int)(i)]) {
            double *temp = NULL;
            size_t temp_len = 0;
            long long k = 0LL;
            while (k < x_items_len) {
                double discr = ((x_items[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_items_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? x_items_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * (means[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? means_len + _mochi_idx : _mochi_idx;})] / variance)) - ((means[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? means_len + _mochi_idx : _mochi_idx;})] * means[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? means_len + _mochi_idx : _mochi_idx;})]) / (2.0 * variance))) + log(probabilities[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? probabilities_len + _mochi_idx : _mochi_idx;})]);
                temp = list_append_double(temp, &temp_len, discr);
                k = k + 1LL;
            }
            long long max_idx = 0LL;
            double max_val = temp[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? temp_len + _mochi_idx : _mochi_idx;})];
            long long t = 1LL;
            while (t < temp_len) {
                if (temp[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? temp_len + _mochi_idx : _mochi_idx;})] > max_val) {
                    max_val = temp[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? temp_len + _mochi_idx : _mochi_idx;})];
                    max_idx = t;
                }
                t = t + 1LL;
            }
            results = list_append_long_long(results, &results_len, max_idx);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return predict_y_values_len = results_len, results;
}

double accuracy(long long * actual_y, size_t actual_y_len, long long * predicted_y, size_t predicted_y_len) {
    long long correct = 0LL;
    long long i = 0LL;
    while (i < actual_y_len) {
        if (actual_y[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? actual_y_len + _mochi_idx : _mochi_idx;})] == predicted_y[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? predicted_y_len + _mochi_idx : _mochi_idx;})]) {
            correct = correct + 1LL;
        }
        i = i + 1LL;
    }
    return ((double)(correct) / (double)(actual_y_len)) * 100.0;
}

void user_main() {
    seed = 1LL;
    long long *counts = NULL;
    size_t counts_len = 0;
    counts = list_append_long_long(counts, &counts_len, 20LL);
    counts = list_append_long_long(counts, &counts_len, 20LL);
    counts = list_append_long_long(counts, &counts_len, 20LL);
    double *means = NULL;
    size_t means_len = 0;
    means = list_append_double(means, &means_len, 5.0);
    means = list_append_double(means, &means_len, 10.0);
    means = list_append_double(means, &means_len, 15.0);
    long long std_dev = 1.0;
    double **x = NULL;
    size_t x_len = 0;
    size_t *x_lens = NULL;
    size_t x_lens_len = 0;
    long long i = 0LL;
    while (i < 3LL) {
        x = list_append_doubleptr(x, &x_len, gaussian_distribution(means[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? means_len + _mochi_idx : _mochi_idx;})], std_dev, counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})]));
        x_lens = list_append_szt(x_lens, &x_lens_len, gaussian_distribution_len);
        i = i + 1LL;
    }
    long long *y = y_generator(3LL, counts, counts_len);
    size_t y_len = y_generator_len;
    double *actual_means = NULL;
    size_t actual_means_len = 0;
    i = 0LL;
    while (i < 3LL) {
        actual_means = list_append_double(actual_means, &actual_means_len, calculate_mean(counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})], x[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})], x_lens[(int)(i)]));
        i = i + 1LL;
    }
    long long total_count = 0LL;
    i = 0LL;
    while (i < 3LL) {
        total_count = total_count + counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    double *probabilities = NULL;
    size_t probabilities_len = 0;
    i = 0LL;
    while (i < 3LL) {
        probabilities = list_append_double(probabilities, &probabilities_len, calculate_probabilities(counts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})], total_count));
        i = i + 1LL;
    }
    double variance = calculate_variance(x, x_len, x_lens, x_len, actual_means, actual_means_len, total_count);
    long long *predicted = predict_y_values(x, x_len, x_lens, x_len, actual_means, actual_means_len, variance, probabilities, probabilities_len);
    size_t predicted_len = predict_y_values_len;
    puts(str_list_int(predicted, predicted_len));
    printf("%g\n", accuracy(y, y_len, predicted, predicted_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
