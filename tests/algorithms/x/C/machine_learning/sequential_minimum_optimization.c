// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t smo_train_len;
size_t *smo_train_lens;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double **samples = NULL;
size_t samples_len = 0;
size_t *samples_lens = NULL;
size_t samples_lens_len = 0;
long long *labels = NULL;
size_t labels_len = 0;
double **model = NULL;
size_t model_len = 0;
size_t *model_lens = NULL;
size_t model_lens_len = 0;

double dot(double * a, size_t a_len, double * b, size_t b_len);
double maxf(double a, double b);
double minf(double a, double b);
double absf(double x);
double predict_raw(double * * samples, size_t samples_len, size_t* samples_lens, size_t samples_lens_len, double * labels, size_t labels_len, double * alphas, size_t alphas_len, double b, double * x, size_t x_len);
double * * smo_train(double * * samples, size_t samples_len, size_t* samples_lens, size_t samples_lens_len, double * labels, size_t labels_len, double c, double tol, long long max_passes);
double predict(double * * samples, size_t samples_len, size_t* samples_lens, size_t samples_lens_len, double * labels, size_t labels_len, double * * model, size_t model_len, size_t* model_lens, size_t model_lens_len, double * x, size_t x_len);
int main(void);

double dot(double * a, size_t a_len, double * b, size_t b_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < a_len) {
        sum = sum + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return sum;
}

double maxf(double a, double b) {
    if (a > b) {
        return a;
    }
    return b;
}

double minf(double a, double b) {
    if (a < b) {
        return a;
    }
    return b;
}

double absf(double x) {
    if (x >= 0.0) {
        return x;
    }
    return 0.0 - x;
}

double predict_raw(double * * samples, size_t samples_len, size_t* samples_lens, size_t samples_lens_len, double * labels, size_t labels_len, double * alphas, size_t alphas_len, double b, double * x, size_t x_len) {
    double res = 0.0;
    long long i = 0LL;
    while (i < samples_len) {
        res = res + ((alphas[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] * labels[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})]) * dot(samples[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i], x, x_len));
        i = i + 1LL;
    }
    return res + b;
}

double * * smo_train(double * * samples, size_t samples_len, size_t* samples_lens, size_t samples_lens_len, double * labels, size_t labels_len, double c, double tol, long long max_passes) {
    long long m = samples_len;
    double *alphas = NULL;
    size_t alphas_len = 0;
    long long i = 0LL;
    while (i < m) {
        alphas = list_append_long_long(alphas, &alphas_len, 0.0);
        i = i + 1LL;
    }
    double b = 0.0;
    long long passes = 0LL;
    while (passes < max_passes) {
        long long num_changed = 0LL;
        long long i1 = 0LL;
        while (i1 < m) {
            double Ei = predict_raw(samples, samples_len, samples_lens, samples_len, labels, labels_len, alphas, alphas_len, b, samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1]) - labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})];
            if ((((labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * Ei) < (0.0 - tol)) && (alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] < c)) || (((labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * Ei) > tol) && (alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] > 0.0))) {
                long long i2 = (i1 + 1LL) % m;
                double Ej = predict_raw(samples, samples_len, samples_lens, samples_len, labels, labels_len, alphas, alphas_len, b, samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2]) - labels[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})];
                double alpha1_old = alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})];
                double alpha2_old = alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})];
                double L = 0.0;
                double H = 0.0;
                if (labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] != labels[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})]) {
                    L = maxf(0.0, alpha2_old - alpha1_old);
                    H = minf(c, (c + alpha2_old) - alpha1_old);
                } else {
                    L = maxf(0.0, (alpha2_old + alpha1_old) - c);
                    H = minf(c, alpha2_old + alpha1_old);
                }
                if (L == H) {
                    i1 = i1 + 1LL;
                    continue;
                }
                double eta = ((2.0 * dot(samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1], samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2])) - dot(samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1], samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1])) - dot(samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2], samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2]);
                if (eta >= 0.0) {
                    i1 = i1 + 1LL;
                    continue;
                }
                alphas[(int)(i2)] = alpha2_old - ((labels[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * (Ei - Ej)) / eta);
                if (alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] > H) {
                    alphas[(int)(i2)] = H;
                }
                if (alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] < L) {
                    alphas[(int)(i2)] = L;
                }
                if (absf(alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] - alpha2_old) < 1e-05) {
                    i1 = i1 + 1LL;
                    continue;
                }
                alphas[(int)(i1)] = alpha1_old + ((labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * labels[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})]) * (alpha2_old - alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})]));
                double b1 = ((b - Ei) - ((labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * (alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] - alpha1_old)) * dot(samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1], samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1]))) - ((labels[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * (alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] - alpha2_old)) * dot(samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1], samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2]));
                double b2 = ((b - Ej) - ((labels[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * (alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] - alpha1_old)) * dot(samples[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i1], samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2]))) - ((labels[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? labels_len + _mochi_idx : _mochi_idx;})] * (alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] - alpha2_old)) * dot(samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2], samples[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? samples_len + _mochi_idx : _mochi_idx;})], samples_lens[i2]));
                if ((alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] > 0.0) && (alphas[(int)({long long _mochi_idx = i1; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] < c)) {
                    b = b1;
                } else {
                    if ((alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] > 0.0) && (alphas[(int)({long long _mochi_idx = i2; _mochi_idx < 0 ? alphas_len + _mochi_idx : _mochi_idx;})] < c)) {
                        b = b2;
                    } else {
                        b = (b1 + b2) / 2.0;
                    }
                }
                num_changed = num_changed + 1LL;
            }
            i1 = i1 + 1LL;
        }
        if (num_changed == 0LL) {
            passes = passes + 1LL;
        } else {
            passes = 0LL;
        }
    }
    {
        size_t *__tmp0_lens = malloc(2 * sizeof(size_t));
        __tmp0_lens[0] = alphas_len;
        __tmp0_lens[1] = 1;
        double** __tmp0_data = malloc(2 * sizeof(double*));
        __tmp0_data[0] = alphas;
        __tmp0_data[1] = ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = b; tmp;});
        smo_train_lens = __tmp0_lens;
        smo_train_len = 2;
        return __tmp0_data;
    }
}

double predict(double * * samples, size_t samples_len, size_t* samples_lens, size_t samples_lens_len, double * labels, size_t labels_len, double * * model, size_t model_len, size_t* model_lens, size_t model_lens_len, double * x, size_t x_len) {
    double *alphas = model[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? model_len + _mochi_idx : _mochi_idx;})];
    size_t alphas_len = model_lens[0LL];
    double b = model[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? model_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? model_lens[1LL] + _mochi_idx : _mochi_idx;})];
    double val = predict_raw(samples, samples_len, samples_lens, samples_len, labels, labels_len, alphas, alphas_len, b, x, x_len);
    if (val >= 0.0) {
        return 1.0;
    }
    return -(1.0);
}

int main(void) {
    {
        long long __start = _now();
        samples = ({double** tmp = malloc(4 * sizeof(double*)); tmp[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp;}); tmp[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 1.5; tmp[1] = 1.5; tmp;}); tmp[2] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 0.0; tmp;}); tmp[3] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 0.5; tmp[1] = 0.0; tmp;}); tmp;});
        samples_len = 4;
        samples_lens = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp[3] = 2; tmp;});
        samples_lens_len = 4;
        labels = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 1.0; tmp[2] = -(1.0); tmp[3] = -(1.0); tmp;});
        labels_len = 4;
        model = smo_train(samples, samples_len, samples_lens, samples_len, labels, labels_len, 1.0, 0.001, 10LL);
        model_len = smo_train_len;
        model_lens = smo_train_lens;
        printf("%g\n", predict(samples, samples_len, samples_lens, samples_len, labels, labels_len, model, model_len, model_lens, model_len, ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 1.5; tmp[1] = 1.0; tmp;}), 2));
        printf("%g\n", predict(samples, samples_len, samples_lens, samples_len, labels, labels_len, model, model_len, model_lens, model_len, ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 0.2; tmp[1] = 0.1; tmp;}), 2));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
