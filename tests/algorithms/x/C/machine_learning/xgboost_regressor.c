// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t xgboost_len;

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Dataset Dataset;
typedef struct Tree Tree;

struct Dataset {
    double **data;
    size_t data_len;
    size_t *data_lens;
    size_t data_lens_len;
    double *target;
    size_t target_len;
};

struct Tree {
    double threshold;
    double left_value;
    double right_value;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Tree* list_append_Tree(Tree *arr, size_t *len, Tree val) {
    arr = realloc(arr, (*len + 1) * sizeof(Tree));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Dataset data_handling(Dataset dataset);
double * xgboost(double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double * target, size_t target_len, double * * test_features, size_t test_features_len, size_t* test_features_lens, size_t test_features_lens_len);
double mean_absolute_error(double * y_true, size_t y_true_len, double * y_pred, size_t y_pred_len);
double mean_squared_error(double * y_true, size_t y_true_len, double * y_pred, size_t y_pred_len);
const char* user_main();
int main(void);

Dataset data_handling(Dataset dataset) {
    return dataset;
}

double * xgboost(double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double * target, size_t target_len, double * * test_features, size_t test_features_len, size_t* test_features_lens, size_t test_features_lens_len) {
    double learning_rate = 0.5;
    long long n_estimators = 3LL;
    Tree *trees = NULL;
    size_t trees_len = 0;
    double *predictions = NULL;
    size_t predictions_len = 0;
    long long i = 0LL;
    while (i < target_len) {
        predictions = list_append_long_long(predictions, &predictions_len, 0.0);
        i = i + 1LL;
    }
    long long est = 0LL;
    while (est < n_estimators) {
        double *residuals = NULL;
        size_t residuals_len = 0;
        long long j = 0LL;
        while (j < target_len) {
            residuals = list_append_double(residuals, &residuals_len, target[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? target_len + _mochi_idx : _mochi_idx;})] - predictions[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? predictions_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        double sum_feat = 0.0;
        j = 0LL;
        while (j < features_len) {
            sum_feat = sum_feat + features[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? features_lens[j] + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        double threshold = sum_feat / (double)(features_len);
        double left_sum = 0.0;
        long long left_count = 0LL;
        double right_sum = 0.0;
        long long right_count = 0LL;
        j = 0LL;
        while (j < features_len) {
            if (features[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? features_lens[j] + _mochi_idx : _mochi_idx;})] <= threshold) {
                left_sum = left_sum + residuals[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? residuals_len + _mochi_idx : _mochi_idx;})];
                left_count = left_count + 1LL;
            } else {
                right_sum = right_sum + residuals[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? residuals_len + _mochi_idx : _mochi_idx;})];
                right_count = right_count + 1LL;
            }
            j = j + 1LL;
        }
        double left_value = 0.0;
        if (left_count > 0LL) {
            left_value = left_sum / (double)(left_count);
        }
        double right_value = 0.0;
        if (right_count > 0LL) {
            right_value = right_sum / (double)(right_count);
        }
        j = 0LL;
        while (j < features_len) {
            if (features[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? features_lens[j] + _mochi_idx : _mochi_idx;})] <= threshold) {
                predictions[(int)(j)] = predictions[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? predictions_len + _mochi_idx : _mochi_idx;})] + (learning_rate * left_value);
            } else {
                predictions[(int)(j)] = predictions[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? predictions_len + _mochi_idx : _mochi_idx;})] + (learning_rate * right_value);
            }
            j = j + 1LL;
        }
        trees = list_append_Tree(trees, &trees_len, (Tree){.threshold = threshold, .left_value = left_value, .right_value = right_value});
        est = est + 1LL;
    }
    double *preds = NULL;
    size_t preds_len = 0;
    long long t = 0LL;
    while (t < test_features_len) {
        double pred = 0.0;
        long long k = 0LL;
        while (k < trees_len) {
            if (test_features[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? test_features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? test_features_lens[t] + _mochi_idx : _mochi_idx;})] <= trees[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? trees_len + _mochi_idx : _mochi_idx;})].threshold) {
                pred = pred + (learning_rate * trees[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? trees_len + _mochi_idx : _mochi_idx;})].left_value);
            } else {
                pred = pred + (learning_rate * trees[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? trees_len + _mochi_idx : _mochi_idx;})].right_value);
            }
            k = k + 1LL;
        }
        preds = list_append_double(preds, &preds_len, pred);
        t = t + 1LL;
    }
    return xgboost_len = preds_len, preds;
}

double mean_absolute_error(double * y_true, size_t y_true_len, double * y_pred, size_t y_pred_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < y_true_len) {
        double diff = y_true[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y_true_len + _mochi_idx : _mochi_idx;})] - y_pred[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y_pred_len + _mochi_idx : _mochi_idx;})];
        if (diff < 0.0) {
            diff = -(diff);
        }
        sum = sum + diff;
        i = i + 1LL;
    }
    return sum / (double)(y_true_len);
}

double mean_squared_error(double * y_true, size_t y_true_len, double * y_pred, size_t y_pred_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < y_true_len) {
        double diff = y_true[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y_true_len + _mochi_idx : _mochi_idx;})] - y_pred[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? y_pred_len + _mochi_idx : _mochi_idx;})];
        sum = sum + (diff * diff);
        i = i + 1LL;
    }
    return sum / (double)(y_true_len);
}

const char* user_main() {
    Dataset california = (Dataset){.data = ({long long** tmp = malloc(4 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 1.0; tmp;}); tmp[1] = ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = 2.0; tmp;}); tmp[2] = ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = 3.0; tmp;}); tmp[3] = ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = 4.0; tmp;}); tmp;}), .data_len = 4, .data_lens = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 1; tmp[1] = 1; tmp[2] = 1; tmp[3] = 1; tmp;}), .data_lens_len = 4, .target = ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 3.0; tmp[2] = 4.0; tmp[3] = 5.0; tmp;}), .target_len = 4};
    Dataset ds = data_handling(california);
    double **x_train = ds.data;
    size_t x_train_len = ds.data_len;
    size_t *x_train_lens = ds.data_lens;
    size_t x_train_lens_len = ds.data_len;
    double *y_train = ds.target;
    size_t y_train_len = ds.target_len;
    double x_test_0[1] = {1.5};
    double x_test_1[1] = {3.5};
    double *x_test_init[2] = {x_test_0, x_test_1};
    double **x_test = x_test_init;
    size_t x_test_len = 2;
    size_t x_test_lens_init[2] = {1, 1};
    size_t *x_test_lens = x_test_lens_init;
    size_t x_test_lens_len = 2;
    double *y_test = NULL;
    size_t y_test_len = 0;
    y_test = list_append_double(y_test, &y_test_len, 2.5);
    y_test = list_append_double(y_test, &y_test_len, 4.5);
    double *predictions = xgboost(x_train, x_train_len, x_train_lens, x_train_len, y_train, y_train_len, x_test, x_test_len, x_test_lens, x_test_len);
    size_t predictions_len = xgboost_len;
    puts("Predictions:");
    puts(str_list_double(predictions, predictions_len));
    puts("Mean Absolute Error:");
    printf("%g\n", mean_absolute_error(y_test, y_test_len, predictions, predictions_len));
    puts("Mean Square Error:");
    printf("%g\n", mean_squared_error(y_test, y_test_len, predictions, predictions_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
