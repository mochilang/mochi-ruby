// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t update_len;
size_t *update_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long get_winner(double * * weights, size_t weights_len, size_t* weights_lens, size_t weights_lens_len, long long * sample, size_t sample_len);
double * * update(double * * weights, size_t weights_len, size_t* weights_lens, size_t weights_lens_len, long long * sample, size_t sample_len, long long j, double alpha);
const char* list_to_string(double * xs, size_t xs_len);
const char* matrix_to_string(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
void user_main();
int main(void);

long long get_winner(double * * weights, size_t weights_len, size_t* weights_lens, size_t weights_lens_len, long long * sample, size_t sample_len) {
    double d0 = 0.0;
    double d1 = 0.0;
    for (int i = 0LL; i < sample_len; i++) {
        double diff0 = sample[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sample_len + _mochi_idx : _mochi_idx;})] - weights[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_lens[0LL] + _mochi_idx : _mochi_idx;})];
        double diff1 = sample[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sample_len + _mochi_idx : _mochi_idx;})] - weights[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_lens[1LL] + _mochi_idx : _mochi_idx;})];
        d0 = d0 + (diff0 * diff0);
        d1 = d1 + (diff1 * diff1);
        return (d0 > d1 ? 0LL : 1LL);
    }
    return 0LL;
}

double * * update(double * * weights, size_t weights_len, size_t* weights_lens, size_t weights_lens_len, long long * sample, size_t sample_len, long long j, double alpha) {
    for (int i = 0LL; i < weights_len; i++) {
        weights[(int)(j)][(int)(i)] = weights[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_lens[j] + _mochi_idx : _mochi_idx;})] + (alpha * (sample[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sample_len + _mochi_idx : _mochi_idx;})] - weights[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_lens[j] + _mochi_idx : _mochi_idx;})]));
    }
    return update_lens = weights_lens, update_len = weights_len, weights;
}

const char* list_to_string(double * xs, size_t xs_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < xs_len) {
        s = str_concat(s, str_float(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        if (i < (xs_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

const char* matrix_to_string(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < m_len) {
        s = str_concat(s, list_to_string(m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})], m_lens[i]));
        if (i < (m_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

void user_main() {
    long long training_samples_0[4] = {1LL, 1LL, 0LL, 0LL};
    long long training_samples_1[4] = {0LL, 0LL, 0LL, 1LL};
    long long training_samples_2[4] = {1LL, 0LL, 0LL, 0LL};
    long long training_samples_3[4] = {0LL, 0LL, 1LL, 1LL};
    long long *training_samples_init[4] = {training_samples_0, training_samples_1, training_samples_2, training_samples_3};
    long long **training_samples = training_samples_init;
    size_t training_samples_len = 4;
    size_t training_samples_lens_init[4] = {4, 4, 4, 4};
    size_t *training_samples_lens = training_samples_lens_init;
    size_t training_samples_lens_len = 4;
    double weights_0[4] = {0.2, 0.6, 0.5, 0.9};
    double weights_1[4] = {0.8, 0.4, 0.7, 0.3};
    double *weights_init[2] = {weights_0, weights_1};
    double **weights = weights_init;
    size_t weights_len = 2;
    size_t weights_lens_init[2] = {4, 4};
    size_t *weights_lens = weights_lens_init;
    size_t weights_lens_len = 2;
    long long epochs = 3LL;
    double alpha = 0.5;
    for (int _ = 0LL; _ < epochs; _++) {
        for (int j = 0LL; j < training_samples_len; j++) {
            long long *sample = training_samples[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? training_samples_len + _mochi_idx : _mochi_idx;})];
            size_t sample_len = training_samples_lens[j];
            long long winner = get_winner(weights, weights_len, weights_lens, weights_len, sample, sample_len);
            weights = update(weights, weights_len, weights_lens, weights_len, sample, sample_len, winner, alpha);
            weights_len = update_len;
            weights_lens = update_lens;
        }
    }
    long long *sample = NULL;
    size_t sample_len = 0;
    sample = list_append_long_long(sample, &sample_len, 0LL);
    sample = list_append_long_long(sample, &sample_len, 0LL);
    sample = list_append_long_long(sample, &sample_len, 0LL);
    sample = list_append_long_long(sample, &sample_len, 1LL);
    long long winner = get_winner(weights, weights_len, weights_lens, weights_len, sample, sample_len);
    puts(str_concat("Clusters that the test sample belongs to : ", str_int(winner)));
    puts(str_concat("Weights that have been trained : ", matrix_to_string(weights, weights_len, weights_lens, weights_len)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
