// Generated by Mochi 0.10.32 on 2025-08-16 20:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t gradient_len;
size_t predict_raw_len;
size_t predict_len;
size_t fit_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Stump Stump;

struct Stump {
    long long feature;
    double threshold;
    double left;
    double right;
};

static Stump* list_append_Stump(Stump *arr, size_t *len, Stump val) {
    arr = realloc(arr, (*len + 1) * sizeof(Stump));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double features_0[1] = {1.0};
double features_1[1] = {2.0};
double features_2[1] = {3.0};
double features_3[1] = {4.0};
double *features_init[4] = {features_0, features_1, features_2, features_3};
double **features = features_init;
size_t features_len = 4;
size_t features_lens_init[4] = {1, 1, 1, 1};
size_t *features_lens = features_lens_init;
size_t features_lens_len = 4;
double target_init[4] = {-1.0, -1.0, 1.0, 1.0};
double *target = target_init;
size_t target_len = 4;
Stump *models = NULL;
size_t models_len = 0;
double *predictions = NULL;
size_t predictions_len = 0;
double acc = 0;

double exp_approx(double x);
double signf(double x);
double * gradient(double * target, size_t target_len, double * preds, size_t preds_len);
double * predict_raw(Stump * models, size_t models_len, double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double learning_rate);
double * predict(Stump * models, size_t models_len, double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double learning_rate);
Stump train_stump(double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double * residuals, size_t residuals_len);
Stump * fit(long long n_estimators, double learning_rate, double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double * target, size_t target_len);
double accuracy(double * preds, size_t preds_len, double * target, size_t target_len);
int main(void);

double exp_approx(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long i = 1LL;
    while (i < 10LL) {
        term = (term * x) / (double)(i);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double signf(double x) {
    if (x >= 0.0) {
        return 1.0;
    }
    return -1.0;
}

double * gradient(double * target, size_t target_len, double * preds, size_t preds_len) {
    long long n = target_len;
    double *residuals = NULL;
    size_t residuals_len = 0;
    long long i = 0LL;
    while (i < n) {
        double t = target[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? target_len + _mochi_idx : _mochi_idx;})];
        double y = preds[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? preds_len + _mochi_idx : _mochi_idx;})];
        double exp_val = exp_approx(t * y);
        double res = -(t) / (1.0 + exp_val);
        residuals = list_append_double(residuals, &residuals_len, res);
        i = i + 1LL;
    }
    return gradient_len = residuals_len, residuals;
}

double * predict_raw(Stump * models, size_t models_len, double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double learning_rate) {
    long long n = features_len;
    double *preds = NULL;
    size_t preds_len = 0;
    long long i = 0LL;
    while (i < n) {
        preds = list_append_double(preds, &preds_len, 0.0);
        i = i + 1LL;
    }
    long long m = 0LL;
    while (m < models_len) {
        Stump stump = models[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? models_len + _mochi_idx : _mochi_idx;})];
        i = 0LL;
        while (i < n) {
            double value = features[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = stump.feature; _mochi_idx < 0 ? features_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
            if (value <= stump.threshold) {
                preds[(int)(i)] = preds[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? preds_len + _mochi_idx : _mochi_idx;})] + (learning_rate * stump.left);
            } else {
                preds[(int)(i)] = preds[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? preds_len + _mochi_idx : _mochi_idx;})] + (learning_rate * stump.right);
            }
            i = i + 1LL;
        }
        m = m + 1LL;
    }
    return predict_raw_len = preds_len, preds;
}

double * predict(Stump * models, size_t models_len, double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double learning_rate) {
    double *raw = predict_raw(models, models_len, features, features_len, features_lens, features_len, learning_rate);
    size_t raw_len = predict_raw_len;
    double *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < raw_len) {
        result = list_append_double(result, &result_len, signf(raw[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? raw_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return predict_len = result_len, result;
}

Stump train_stump(double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double * residuals, size_t residuals_len) {
    long long n_samples = features_len;
    long long n_features = features_lens[(int)(0LL)];
    long long best_feature = 0LL;
    double best_threshold = 0.0;
    double best_error = 1e+09;
    double best_left = 0.0;
    double best_right = 0.0;
    long long j = 0LL;
    while (j < n_features) {
        long long t_index = 0LL;
        while (t_index < n_samples) {
            double t = features[(int)({long long _mochi_idx = t_index; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? features_lens[(int)(t_index)] + _mochi_idx : _mochi_idx;})];
            double sum_left = 0.0;
            long long count_left = 0LL;
            double sum_right = 0.0;
            long long count_right = 0LL;
            long long i = 0LL;
            while (i < n_samples) {
                if (features[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? features_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] <= t) {
                    sum_left = sum_left + residuals[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? residuals_len + _mochi_idx : _mochi_idx;})];
                    count_left = count_left + 1LL;
                } else {
                    sum_right = sum_right + residuals[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? residuals_len + _mochi_idx : _mochi_idx;})];
                    count_right = count_right + 1LL;
                }
                i = i + 1LL;
            }
            double left_val = 0.0;
            if (count_left != 0LL) {
                left_val = sum_left / (double)(count_left);
            }
            double right_val = 0.0;
            if (count_right != 0LL) {
                right_val = sum_right / (double)(count_right);
            }
            double error = 0.0;
            i = 0LL;
            while (i < n_samples) {
                double pred;
                if (features[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? features_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? features_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] <= t) {
                    pred = left_val;
                } else {
                    pred = right_val;
                }
                double diff = residuals[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? residuals_len + _mochi_idx : _mochi_idx;})] - pred;
                error = error + (diff * diff);
                i = i + 1LL;
            }
            if (error < best_error) {
                best_error = error;
                best_feature = j;
                best_threshold = t;
                best_left = left_val;
                best_right = right_val;
            }
            t_index = t_index + 1LL;
        }
        j = j + 1LL;
    }
    return (Stump){.feature = best_feature, .threshold = best_threshold, .left = best_left, .right = best_right};
}

Stump * fit(long long n_estimators, double learning_rate, double * * features, size_t features_len, size_t* features_lens, size_t features_lens_len, double * target, size_t target_len) {
    Stump *models = NULL;
    size_t models_len = 0;
    long long m = 0LL;
    while (m < n_estimators) {
        double *preds = predict_raw(models, models_len, features, features_len, features_lens, features_len, learning_rate);
        size_t preds_len = predict_raw_len;
        double *grad = gradient(target, target_len, preds, preds_len);
        size_t grad_len = gradient_len;
        double *residuals = NULL;
        size_t residuals_len = 0;
        long long i = 0LL;
        while (i < grad_len) {
            residuals = list_append_double(residuals, &residuals_len, -(grad[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grad_len + _mochi_idx : _mochi_idx;})]));
            i = i + 1LL;
        }
        Stump stump = train_stump(features, features_len, features_lens, features_len, residuals, residuals_len);
        models = list_append_Stump(models, &models_len, stump);
        m = m + 1LL;
    }
    return fit_len = models_len, models;
}

double accuracy(double * preds, size_t preds_len, double * target, size_t target_len) {
    long long n = target_len;
    long long correct = 0LL;
    long long i = 0LL;
    while (i < n) {
        if (preds[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? preds_len + _mochi_idx : _mochi_idx;})] == target[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? target_len + _mochi_idx : _mochi_idx;})]) {
            correct = correct + 1LL;
        }
        i = i + 1LL;
    }
    return (double)(correct) / (double)(n);
}

int main(void) {
    {
        long long __start = _now();
        models = fit(5LL, 0.5, features, features_len, features_lens, features_len, target, target_len);
        models_len = fit_len;
        predictions = predict(models, models_len, features, features_len, features_lens, features_len, 0.5);
        predictions_len = predict_len;
        acc = accuracy(predictions, predictions_len, target, target_len);
        puts(str_concat("Accuracy: ", str_float(acc)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
