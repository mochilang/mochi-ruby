// Generated by Mochi 0.10.32 on 2025-08-11 21:16 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct SVC SVC;

struct SVC {
    double *weights;
    size_t weights_len;
    double bias;
    double lr;
    double lambda;
    long long epochs;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double **xs = NULL;
size_t xs_len = 0;
size_t *xs_lens = NULL;
size_t xs_lens_len = 0;
long long *ys = NULL;
size_t ys_len = 0;
SVC base;
SVC model;

double dot(double * a, size_t a_len, double * b, size_t b_len);
SVC new_svc(double lr, double lambda, long long epochs);
SVC fit(SVC model, double * * xs, size_t xs_len, size_t* xs_lens, size_t xs_lens_len, long long * ys, size_t ys_len);
long long predict(SVC model, double * x, size_t x_len);
int main(void);

double dot(double * a, size_t a_len, double * b, size_t b_len) {
    double s = 0.0;
    long long i = 0LL;
    while (i < a_len) {
        s = s + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return s;
}

SVC new_svc(double lr, double lambda, long long epochs) {
    return (SVC){.weights = NULL, .weights_len = 0, .bias = 0.0, .lr = lr, .lambda = lambda, .epochs = epochs};
}

SVC fit(SVC model, double * * xs, size_t xs_len, size_t* xs_lens, size_t xs_lens_len, long long * ys, size_t ys_len) {
    long long n_features = xs_lens[0LL];
    double *w = NULL;
    size_t w_len = 0;
    long long i = 0LL;
    while (i < n_features) {
        w = list_append_long_long(w, &w_len, 0.0);
        i = i + 1LL;
    }
    double b = 0.0;
    long long epoch = 0LL;
    while (epoch < model.epochs) {
        long long j = 0LL;
        while (j < xs_len) {
            double *x = xs[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
            size_t x_len = xs_lens[j];
            double y = (double)(ys[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ys_len + _mochi_idx : _mochi_idx;})]);
            double prod = dot(w, w_len, x, x_len) + b;
            if ((y * prod) < 1.0) {
                long long k = 0LL;
                while (k < w_len) {
                    w[(int)(k)] = w[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? w_len + _mochi_idx : _mochi_idx;})] + (model.lr * ((y * x[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]) - ((2.0 * model.lambda) * w[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? w_len + _mochi_idx : _mochi_idx;})])));
                    k = k + 1LL;
                }
                b = b + (model.lr * y);
            } else {
                long long k = 0LL;
                while (k < w_len) {
                    w[(int)(k)] = w[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? w_len + _mochi_idx : _mochi_idx;})] - (model.lr * ((2.0 * model.lambda) * w[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? w_len + _mochi_idx : _mochi_idx;})]));
                    k = k + 1LL;
                }
            }
            j = j + 1LL;
        }
        epoch = epoch + 1LL;
    }
    return (SVC){.weights = w, .weights_len = w_len, .bias = b, .lr = model.lr, .lambda = model.lambda, .epochs = model.epochs};
}

long long predict(SVC model, double * x, size_t x_len) {
    double s = dot(model.weights, model.weights_len, x, x_len) + model.bias;
    if (s >= 0.0) {
        return 1LL;
    } else {
        return -1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        xs = ({long long** tmp = malloc(4 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 1.0; tmp;}); tmp[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 2.0; tmp;}); tmp[2] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 1.0; tmp;}); tmp[3] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 2.0; tmp;}); tmp;});
        xs_len = 4;
        xs_lens = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp[3] = 2; tmp;});
        xs_lens_len = 4;
        ys = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = -1LL; tmp[3] = -1LL; tmp;});
        ys_len = 4;
        base = new_svc(0.01, 0.01, 1000LL);
        model = fit(base, xs, xs_len, xs_lens, xs_len, ys, ys_len);
        printf("%d\n", predict(model, ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0.0; tmp[1] = 1.0; tmp;}), 2));
        printf("%d\n", predict(model, ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1.0; tmp[1] = 1.0; tmp;}), 2));
        printf("%d\n", predict(model, ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 2.0; tmp;}), 2));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
