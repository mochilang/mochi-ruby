// Generated by Mochi 0.10.32 on 2025-08-16 13:01 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t transpose_len;
size_t *transpose_lens;
size_t matmul_len;
size_t *matmul_lens;
size_t matvec_len;
size_t identity_len;
size_t *identity_lens;
size_t invert_len;
size_t *invert_lens;
size_t normal_equation_len;
size_t set_at_float_len;
size_t sort_float_len;

size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double int_to_float(long long x);
double abs_float(double x);
double exp_approx(double x);
long long floor_int(double x);
double dot(double * a, size_t a_len, double * b, size_t b_len);
double * * transpose(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
double * * matmul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
double * matvec(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * b, size_t b_len);
double * * identity(long long n);
double * * invert(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
double * normal_equation(double * * X, size_t X_len, size_t* X_lens, size_t X_lens_len, double * y, size_t y_len);
double linear_regression_prediction(double * train_dt, size_t train_dt_len, double * train_usr, size_t train_usr_len, double * train_mtch, size_t train_mtch_len, double * test_dt, size_t test_dt_len, double * test_mtch, size_t test_mtch_len);
double sarimax_predictor(double * train_user, size_t train_user_len, double * train_match, size_t train_match_len, double * test_match, size_t test_match_len);
double rbf_kernel(double * a, size_t a_len, double * b, size_t b_len, double gamma);
double support_vector_regressor(double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double * * x_test, size_t x_test_len, size_t* x_test_lens, size_t x_test_lens_len, double * train_user, size_t train_user_len);
double * set_at_float(double * xs, size_t xs_len, long long idx, double value);
double * sort_float(double * xs, size_t xs_len);
double percentile(double * data, size_t data_len, double q);
double interquartile_range_checker(double * train_user, size_t train_user_len);
long long data_safety_checker(double * list_vote, size_t list_vote_len, double actual_result);
const char* user_main();
int main(void);

double int_to_float(long long x) {
    return x * 1LL;
}

double abs_float(double x) {
    if (x < 0LL) {
        return 0LL - x;
    }
    return x;
}

double exp_approx(double x) {
    double term = 1LL;
    double sum = 1LL;
    long long i = 1LL;
    while (i < 10LL) {
        term = (term * x) / int_to_float(i);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

long long floor_int(double x) {
    long long i = 0LL;
    while (int_to_float(i + 1LL) <= x) {
        i = i + 1LL;
    }
    return i;
}

double dot(double * a, size_t a_len, double * b, size_t b_len) {
    double s = 0LL;
    long long i = 0LL;
    while (i < a_len) {
        s = s + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return s;
}

double * * transpose(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long rows = m_len;
    long long cols = m_lens[(int)(0LL)];
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long j = 0LL;
    while (j < cols) {
        double *row = NULL;
        size_t row_len = 0;
        long long i = 0LL;
        while (i < rows) {
            row = list_append_double(row, &row_len, m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        j = j + 1LL;
    }
    return transpose_lens = res_lens, transpose_len = res_len, res;
}

double * * matmul(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    long long n = a_len;
    long long m = b_lens[(int)(0LL)];
    long long p = b_len;
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m) {
            double s = 0LL;
            long long k = 0LL;
            while (k < p) {
                s = s + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_double(row, &row_len, s);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return matmul_lens = res_lens, matmul_len = res_len, res;
}

double * matvec(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * b, size_t b_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < a_len) {
        res = list_append_double(res, &res_len, dot(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], a_lens[(int)(i)], b, b_len));
        i = i + 1LL;
    }
    return matvec_len = res_len, res;
}

double * * identity(long long n) {
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, (i == j ? 1LL : 0LL));
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return identity_lens = res_lens, identity_len = res_len, res;
}

double * * invert(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long n = mat_len;
    double **a = mat;
    size_t a_len = mat_len;
    size_t *a_lens = mat_lens;
    size_t a_lens_len = mat_len;
    double **inv = identity(n);
    size_t inv_len = identity_len;
    size_t *inv_lens = identity_lens;
    size_t inv_lens_len = identity_len;
    long long i = 0LL;
    while (i < n) {
        double pivot = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
        long long j = 0LL;
        while (j < n) {
            a[(int)(i)][(int)(j)] = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] / pivot;
            inv[(int)(i)][(int)(j)] = inv[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? inv_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? inv_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] / pivot;
            j = j + 1LL;
        }
        long long k = 0LL;
        while (k < n) {
            if (k != i) {
                double factor = a[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_lens[(int)(k)] + _mochi_idx : _mochi_idx;})];
                j = 0LL;
                while (j < n) {
                    a[(int)(k)][(int)(j)] = a[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(k)] + _mochi_idx : _mochi_idx;})] - (factor * a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
                    inv[(int)(k)][(int)(j)] = inv[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? inv_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? inv_lens[(int)(k)] + _mochi_idx : _mochi_idx;})] - (factor * inv[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? inv_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? inv_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
                    j = j + 1LL;
                }
            }
            k = k + 1LL;
        }
        i = i + 1LL;
    }
    return invert_lens = inv_lens, invert_len = inv_len, inv;
}

double * normal_equation(double * * X, size_t X_len, size_t* X_lens, size_t X_lens_len, double * y, size_t y_len) {
    double **Xt = transpose(X, X_len, X_lens, X_len);
    size_t Xt_len = transpose_len;
    size_t *Xt_lens = transpose_lens;
    size_t Xt_lens_len = transpose_len;
    double **XtX = matmul(Xt, Xt_len, Xt_lens, Xt_len, X, X_len, X_lens, X_len);
    size_t XtX_len = matmul_len;
    size_t *XtX_lens = matmul_lens;
    size_t XtX_lens_len = matmul_len;
    double **XtX_inv = invert(XtX, XtX_len, XtX_lens, XtX_len);
    size_t XtX_inv_len = invert_len;
    size_t *XtX_inv_lens = invert_lens;
    size_t XtX_inv_lens_len = invert_len;
    double *Xty = matvec(Xt, Xt_len, Xt_lens, Xt_len, y, y_len);
    size_t Xty_len = matvec_len;
    {
        double* __ret0 = matvec(XtX_inv, XtX_inv_len, XtX_inv_lens, XtX_inv_len, Xty, Xty_len);
        normal_equation_len = matvec_len;
        return __ret0;
    }
}

double linear_regression_prediction(double * train_dt, size_t train_dt_len, double * train_usr, size_t train_usr_len, double * train_mtch, size_t train_mtch_len, double * test_dt, size_t test_dt_len, double * test_mtch, size_t test_mtch_len) {
    double **X = NULL;
    size_t X_len = 0;
    size_t *X_lens = NULL;
    size_t X_lens_len = 0;
    long long i = 0LL;
    while (i < train_dt_len) {
        X = list_append_doubleptr(X, &X_len, ({double* tmp = malloc(3 * sizeof(double)); tmp[0] = 1LL; tmp[1] = train_dt[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? train_dt_len + _mochi_idx : _mochi_idx;})]; tmp[2] = train_mtch[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? train_mtch_len + _mochi_idx : _mochi_idx;})]; tmp;}));
        X_lens = list_append_szt(X_lens, &X_lens_len, 3);
        i = i + 1LL;
    }
    double *beta = normal_equation(X, X_len, X_lens, X_len, train_usr, train_usr_len);
    size_t beta_len = normal_equation_len;
    return abs_float((beta[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? beta_len + _mochi_idx : _mochi_idx;})] + (test_dt[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? test_dt_len + _mochi_idx : _mochi_idx;})] * beta[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? beta_len + _mochi_idx : _mochi_idx;})])) + (test_mtch[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? test_mtch_len + _mochi_idx : _mochi_idx;})] * beta[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? beta_len + _mochi_idx : _mochi_idx;})]));
}

double sarimax_predictor(double * train_user, size_t train_user_len, double * train_match, size_t train_match_len, double * test_match, size_t test_match_len) {
    long long n = train_user_len;
    double **X = NULL;
    size_t X_len = 0;
    size_t *X_lens = NULL;
    size_t X_lens_len = 0;
    double *y = NULL;
    size_t y_len = 0;
    long long i = 1LL;
    while (i < n) {
        X = list_append_doubleptr(X, &X_len, ({double* tmp = malloc(3 * sizeof(double)); tmp[0] = 1LL; tmp[1] = train_user[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? train_user_len + _mochi_idx : _mochi_idx;})]; tmp[2] = train_match[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? train_match_len + _mochi_idx : _mochi_idx;})]; tmp;}));
        X_lens = list_append_szt(X_lens, &X_lens_len, 3);
        y = list_append_double(y, &y_len, train_user[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? train_user_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    double *beta = normal_equation(X, X_len, X_lens, X_len, y, y_len);
    size_t beta_len = normal_equation_len;
    return (beta[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? beta_len + _mochi_idx : _mochi_idx;})] + (beta[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? beta_len + _mochi_idx : _mochi_idx;})] * train_user[(int)({long long _mochi_idx = n - 1LL; _mochi_idx < 0 ? train_user_len + _mochi_idx : _mochi_idx;})])) + (beta[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? beta_len + _mochi_idx : _mochi_idx;})] * test_match[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? test_match_len + _mochi_idx : _mochi_idx;})]);
}

double rbf_kernel(double * a, size_t a_len, double * b, size_t b_len, double gamma) {
    double sum = 0LL;
    long long i = 0LL;
    while (i < a_len) {
        double diff = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})];
        sum = sum + (diff * diff);
        i = i + 1LL;
    }
    return exp_approx(-(gamma) * sum);
}

double support_vector_regressor(double * * x_train, size_t x_train_len, size_t* x_train_lens, size_t x_train_lens_len, double * * x_test, size_t x_test_len, size_t* x_test_lens, size_t x_test_lens_len, double * train_user, size_t train_user_len) {
    double gamma = 0.1;
    double *weights = NULL;
    size_t weights_len = 0;
    long long i = 0LL;
    while (i < x_train_len) {
        weights = list_append_double(weights, &weights_len, rbf_kernel(x_train[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? x_train_len + _mochi_idx : _mochi_idx;})], x_train_lens[(int)(i)], x_test[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_test_len + _mochi_idx : _mochi_idx;})], x_test_lens[(int)(0LL)], gamma));
        i = i + 1LL;
    }
    double num = 0LL;
    double den = 0LL;
    i = 0LL;
    while (i < train_user_len) {
        num = num + (weights[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})] * train_user[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? train_user_len + _mochi_idx : _mochi_idx;})]);
        den = den + weights[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})];
        i = i + 1LL;
    }
    return num / den;
}

double * set_at_float(double * xs, size_t xs_len, long long idx, double value) {
    long long i = 0LL;
    double *res = NULL;
    size_t res_len = 0;
    while (i < xs_len) {
        if (i == idx) {
            res = list_append_double(res, &res_len, value);
        } else {
            res = list_append_double(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return set_at_float_len = res_len, res;
}

double * sort_float(double * xs, size_t xs_len) {
    double *res = xs;
    size_t res_len = xs_len;
    long long i = 1LL;
    while (i < res_len) {
        double key = res[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (res[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})] > key)) {
            res = set_at_float(res, res_len, j + 1LL, res[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})]);
            res_len = set_at_float_len;
            j = j - 1LL;
        }
        res = set_at_float(res, res_len, j + 1LL, key);
        res_len = set_at_float_len;
        i = i + 1LL;
    }
    return sort_float_len = res_len, res;
}

double percentile(double * data, size_t data_len, double q) {
    double *sorted = sort_float(data, data_len);
    size_t sorted_len = sort_float_len;
    long long n = sorted_len;
    double pos = (q / 100LL) * int_to_float(n - 1LL);
    long long idx = floor_int(pos);
    double frac = pos - int_to_float(idx);
    if ((idx + 1LL) < n) {
        return (sorted[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})] * (1LL - frac)) + (sorted[(int)({long long _mochi_idx = idx + 1LL; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})] * frac);
    }
    return sorted[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
}

double interquartile_range_checker(double * train_user, size_t train_user_len) {
    double q1 = percentile(train_user, train_user_len, 25LL);
    double q3 = percentile(train_user, train_user_len, 75LL);
    double iqr = q3 - q1;
    return q1 - (iqr * 0.1);
}

long long data_safety_checker(double * list_vote, size_t list_vote_len, double actual_result) {
    long long safe = 0LL;
    long long not_safe = 0LL;
    long long i = 0LL;
    while (i < list_vote_len) {
        double v = list_vote[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list_vote_len + _mochi_idx : _mochi_idx;})];
        if (v > actual_result) {
            safe = not_safe + 1LL;
        } else {
            if (abs_float(abs_float(v) - abs_float(actual_result)) <= 0.1) {
                safe = safe + 1LL;
            } else {
                not_safe = not_safe + 1LL;
            }
        }
        i = i + 1LL;
    }
    return safe > not_safe;
}

const char* user_main() {
    double *vote = NULL;
    size_t vote_len = 0;
    vote = list_append_double(vote, &vote_len, linear_regression_prediction(({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 2LL; tmp[1] = 3LL; tmp[2] = 4LL; tmp[3] = 5LL; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 5LL; tmp[1] = 3LL; tmp[2] = 4LL; tmp[3] = 6LL; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 3LL; tmp[1] = 1LL; tmp[2] = 2LL; tmp[3] = 4LL; tmp;}), 4, ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = 2LL; tmp;}), 1, ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = 2LL; tmp;}), 1));
    vote = list_append_double(vote, &vote_len, sarimax_predictor(({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 4LL; tmp[1] = 2LL; tmp[2] = 6LL; tmp[3] = 8LL; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 3LL; tmp[1] = 1LL; tmp[2] = 2LL; tmp[3] = 4LL; tmp;}), 4, ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = 2LL; tmp;}), 1));
    vote = list_append_double(vote, &vote_len, support_vector_regressor(({double** tmp = malloc(3 * sizeof(double*)); tmp[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 5LL; tmp[1] = 2LL; tmp;}); tmp[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 5LL; tmp;}); tmp[2] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 6LL; tmp[1] = 2LL; tmp;}); tmp;}), 3, ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp;}), 3, ({double** tmp = malloc(1 * sizeof(double*)); tmp[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 3LL; tmp[1] = 2LL; tmp;}); tmp;}), 1, ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 2; tmp;}), 1, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 2LL; tmp[1] = 1LL; tmp[2] = 4LL; tmp;}), 3));
    printf("%g\n", vote[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? vote_len + _mochi_idx : _mochi_idx;})]);
    printf("%g\n", vote[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? vote_len + _mochi_idx : _mochi_idx;})]);
    printf("%g\n", vote[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? vote_len + _mochi_idx : _mochi_idx;})]);
    printf("%d\n", data_safety_checker(vote, vote_len, 5LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
