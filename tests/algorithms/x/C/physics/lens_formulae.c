// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double focal_length_of_lens(double object_distance_from_lens, double image_distance_from_lens);
double object_distance(double focal_length_of_lens, double image_distance_from_lens);
double image_distance(double focal_length_of_lens, double object_distance_from_lens);
int main(void);

double focal_length_of_lens(double object_distance_from_lens, double image_distance_from_lens) {
    if ((object_distance_from_lens == 0.0) || (image_distance_from_lens == 0.0)) {
        panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
    }
    return 1.0 / ((1.0 / image_distance_from_lens) - (1.0 / object_distance_from_lens));
}

double object_distance(double focal_length_of_lens, double image_distance_from_lens) {
    if ((image_distance_from_lens == 0.0) || (focal_length_of_lens == 0.0)) {
        panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
    }
    return 1.0 / ((1.0 / image_distance_from_lens) - (1.0 / focal_length_of_lens));
}

double image_distance(double focal_length_of_lens, double object_distance_from_lens) {
    if ((object_distance_from_lens == 0.0) || (focal_length_of_lens == 0.0)) {
        panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
    }
    return 1.0 / ((1.0 / object_distance_from_lens) + (1.0 / focal_length_of_lens));
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(focal_length_of_lens(10.0, 4.0)));
        puts(str_float(focal_length_of_lens(2.7, 5.8)));
        puts(str_float(object_distance(10.0, 40.0)));
        puts(str_float(object_distance(6.2, 1.5)));
        puts(str_float(image_distance(50.0, 40.0)));
        puts(str_float(image_distance(5.3, 7.9)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
