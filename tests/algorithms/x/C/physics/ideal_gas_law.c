// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double UNIVERSAL_GAS_CONSTANT = 8.314462;

double pressure_of_gas_system(double moles, double kelvin, double volume);
double volume_of_gas_system(double moles, double kelvin, double pressure);
double temperature_of_gas_system(double moles, double volume, double pressure);
double moles_of_gas_system(double kelvin, double volume, double pressure);
int main(void);

double pressure_of_gas_system(double moles, double kelvin, double volume) {
    if (((moles < 0LL) || (kelvin < 0LL)) || (volume < 0LL)) {
        panic("Invalid inputs. Enter positive value.");
    }
    return ((moles * kelvin) * UNIVERSAL_GAS_CONSTANT) / volume;
}

double volume_of_gas_system(double moles, double kelvin, double pressure) {
    if (((moles < 0LL) || (kelvin < 0LL)) || (pressure < 0LL)) {
        panic("Invalid inputs. Enter positive value.");
    }
    return ((moles * kelvin) * UNIVERSAL_GAS_CONSTANT) / pressure;
}

double temperature_of_gas_system(double moles, double volume, double pressure) {
    if (((moles < 0LL) || (volume < 0LL)) || (pressure < 0LL)) {
        panic("Invalid inputs. Enter positive value.");
    }
    return (pressure * volume) / (moles * UNIVERSAL_GAS_CONSTANT);
}

double moles_of_gas_system(double kelvin, double volume, double pressure) {
    if (((kelvin < 0LL) || (volume < 0LL)) || (pressure < 0LL)) {
        panic("Invalid inputs. Enter positive value.");
    }
    return (pressure * volume) / (kelvin * UNIVERSAL_GAS_CONSTANT);
}

int main(void) {
    {
        long long __start = _now();
        printf("%g\n", pressure_of_gas_system(2.0, 100.0, 5.0));
        printf("%g\n", volume_of_gas_system(0.5, 273.0, 0.004));
        printf("%g\n", temperature_of_gas_system(2.0, 100.0, 5.0));
        printf("%g\n", moles_of_gas_system(100.0, 5.0, 10.0));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
