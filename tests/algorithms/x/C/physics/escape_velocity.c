// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double user_pow10(long long n);
double sqrt_newton(double n);
double round3(double x);
double escape_velocity(double mass, double radius);
int main(void);

double user_pow10(long long n) {
    double p = 1.0;
    long long k = 0LL;
    if (n >= 0LL) {
        while (k < n) {
            p = p * 10.0;
            k = k + 1LL;
        }
    } else {
        long long m = -(n);
        while (k < m) {
            p = p / 10.0;
            k = k + 1LL;
        }
    }
    return p;
}

double sqrt_newton(double n) {
    if (n == 0.0) {
        return 0.0;
    }
    double x = n;
    long long j = 0LL;
    while (j < 20LL) {
        x = (x + (n / x)) / 2.0;
        j = j + 1LL;
    }
    return x;
}

double round3(double x) {
    double y = (x * 1000.0) + 0.5;
    long long yi = (int)(y);
    if ((double)(yi) > y) {
        yi = yi - 1LL;
    }
    return (double)(yi) / 1000.0;
}

double escape_velocity(double mass, double radius) {
    if (radius == 0.0) {
        panic("Radius cannot be zero.");
    }
    double G = 6.6743 * user_pow10(-11.0);
    double velocity = sqrt_newton(((2.0 * G) * mass) / radius);
    return round3(velocity);
}

int main(void) {
    {
        long long __start = _now();
        printf("%g\n", escape_velocity(5.972 * 1e+24, 6.371 * 1e+06));
        printf("%g\n", escape_velocity(7.348 * 1e+22, 1.737 * 1e+06));
        printf("%g\n", escape_velocity(1.898 * 1e+27, 6.9911 * 1e+07));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
