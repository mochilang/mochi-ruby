// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double to_float(long long x);
double round6(double x);
double sqrtApprox(double x);
long long validate(double * values, size_t values_len);
double effusion_ratio(double m1, double m2);
double first_effusion_rate(double rate, double m1, double m2);
double second_effusion_rate(double rate, double m1, double m2);
double first_molar_mass(double mass, double r1, double r2);
double second_molar_mass(double mass, double r1, double r2);
int main(void);

double to_float(long long x) {
    return x * 1.0;
}

double round6(double x) {
    double factor = 1e+06;
    return to_float((int)((x * factor) + 0.5)) / factor;
}

double sqrtApprox(double x) {
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

long long validate(double * values, size_t values_len) {
    if (values_len == 0LL) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < values_len) {
        if (values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})] <= 0.0) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

double effusion_ratio(double m1, double m2) {
    if (!(validate(({double *tmp = malloc(2 * sizeof(double)); tmp[0] = m1; tmp[1] = m2; tmp;}), 2))) {
        puts("ValueError: Molar mass values must greater than 0.");
        return 0.0;
    }
    return round6(sqrtApprox(m2 / m1));
}

double first_effusion_rate(double rate, double m1, double m2) {
    if (!(validate(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = rate; tmp[1] = m1; tmp[2] = m2; tmp;}), 3))) {
        puts("ValueError: Molar mass and effusion rate values must greater than 0.");
        return 0.0;
    }
    return round6(rate * sqrtApprox(m2 / m1));
}

double second_effusion_rate(double rate, double m1, double m2) {
    if (!(validate(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = rate; tmp[1] = m1; tmp[2] = m2; tmp;}), 3))) {
        puts("ValueError: Molar mass and effusion rate values must greater than 0.");
        return 0.0;
    }
    return round6(rate / sqrtApprox(m2 / m1));
}

double first_molar_mass(double mass, double r1, double r2) {
    if (!(validate(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = mass; tmp[1] = r1; tmp[2] = r2; tmp;}), 3))) {
        puts("ValueError: Molar mass and effusion rate values must greater than 0.");
        return 0.0;
    }
    double ratio = r1 / r2;
    return round6(mass / (ratio * ratio));
}

double second_molar_mass(double mass, double r1, double r2) {
    if (!(validate(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = mass; tmp[1] = r1; tmp[2] = r2; tmp;}), 3))) {
        puts("ValueError: Molar mass and effusion rate values must greater than 0.");
        return 0.0;
    }
    double ratio = r1 / r2;
    return round6((ratio * ratio) / mass);
}

int main(void) {
    {
        long long __start = _now();
        printf("%g\n", effusion_ratio(2.016, 4.002));
        printf("%g\n", first_effusion_rate(1.0, 2.016, 4.002));
        printf("%g\n", second_effusion_rate(1.0, 2.016, 4.002));
        printf("%g\n", first_molar_mass(2.0, 1.408943, 0.709752));
        printf("%g\n", second_molar_mass(2.0, 1.408943, 0.709752));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
