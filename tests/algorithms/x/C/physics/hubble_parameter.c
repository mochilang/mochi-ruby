// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double user_pow(double base, long long exp);
double sqrt_approx(double x);
double hubble_parameter(double hubble_constant, double radiation_density, double matter_density, double dark_energy, double redshift);
void test_hubble_parameter();
void user_main();
int main(void);

double user_pow(double base, long long exp) {
    double result = 1.0;
    long long i = 0LL;
    while (i < exp) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

double sqrt_approx(double x) {
    if (x == 0.0) {
        return 0.0;
    }
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double hubble_parameter(double hubble_constant, double radiation_density, double matter_density, double dark_energy, double redshift) {
    double *parameters = NULL;
    size_t parameters_len = 0;
    parameters = list_append_double(parameters, &parameters_len, redshift);
    parameters = list_append_double(parameters, &parameters_len, radiation_density);
    parameters = list_append_double(parameters, &parameters_len, matter_density);
    parameters = list_append_double(parameters, &parameters_len, dark_energy);
    long long i = 0LL;
    while (i < 4LL) {
        if (parameters[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parameters_len + _mochi_idx : _mochi_idx;})] < 0.0) {
            panic("All input parameters must be positive");
        }
        i = i + 1LL;
    }
    i = 1LL;
    while (i < 4LL) {
        if (parameters[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parameters_len + _mochi_idx : _mochi_idx;})] > 1.0) {
            panic("Relative densities cannot be greater than one");
        }
        i = i + 1LL;
    }
    double curvature = 1.0 - ((matter_density + radiation_density) + dark_energy);
    double zp1 = redshift + 1.0;
    double e2 = (((radiation_density * user_pow(zp1, 4LL)) + (matter_density * user_pow(zp1, 3LL))) + (curvature * user_pow(zp1, 2LL))) + dark_energy;
    return hubble_constant * sqrt_approx(e2);
}

void test_hubble_parameter() {
    double h = hubble_parameter(68.3, 0.0001, 0.3, 0.7, 0.0);
    if ((h < 68.2999) || (h > 68.3001)) {
        panic("hubble_parameter test failed");
    }
}

void user_main() {
    test_hubble_parameter();
    printf("%g\n", hubble_parameter(68.3, 0.0001, 0.3, 0.7, 0.0));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
