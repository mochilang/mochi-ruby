// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Coord3D Coord3D;
typedef struct Particle Particle;

struct Coord3D {
    double x;
    double y;
    double z;
};

struct Particle {
    double x;
    double y;
    double z;
    double mass;
};

Coord3D r1;
Coord3D r2;

double round2(double x);
Coord3D center_of_mass(Particle * ps, size_t ps_len);
const char* coord_to_string(Coord3D c);
int main(void);

double round2(double x) {
    double scaled = x * 100.0;
    double rounded = scaled + 0.5;
    return rounded / 100.0;
}

Coord3D center_of_mass(Particle * ps, size_t ps_len) {
    if (ps_len == 0LL) {
        panic("No particles provided");
    }
    long long i = 0LL;
    double total_mass = 0.0;
    while (i < ps_len) {
        Particle p = ps[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ps_len + _mochi_idx : _mochi_idx;})];
        if (p.mass <= 0.0) {
            panic("Mass of all particles must be greater than 0");
        }
        total_mass = total_mass + p.mass;
        i = i + 1LL;
    }
    double sum_x = 0.0;
    double sum_y = 0.0;
    double sum_z = 0.0;
    i = 0LL;
    while (i < ps_len) {
        Particle p = ps[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ps_len + _mochi_idx : _mochi_idx;})];
        sum_x = sum_x + (p.x * p.mass);
        sum_y = sum_y + (p.y * p.mass);
        sum_z = sum_z + (p.z * p.mass);
        i = i + 1LL;
    }
    double cm_x = round2(sum_x / total_mass);
    double cm_y = round2(sum_y / total_mass);
    double cm_z = round2(sum_z / total_mass);
    return (Coord3D){.x = cm_x, .y = cm_y, .z = cm_z};
}

const char* coord_to_string(Coord3D c) {
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("Coord3D(x=", str_float(c.x)), ", y="), str_float(c.y)), ", z="), str_float(c.z)), ")");
}

int main(void) {
    {
        long long __start = _now();
        r1 = center_of_mass((Particle[]){(Particle){.x = 1.5, .y = 4.0, .z = 3.4, .mass = 4.0}, (Particle){.x = 5.0, .y = 6.8, .z = 7.0, .mass = 8.1}, (Particle){.x = 9.4, .y = 10.1, .z = 11.6, .mass = 12.0}}, 3);
        puts(coord_to_string(r1));
        r2 = center_of_mass((Particle[]){(Particle){.x = 1.0, .y = 2.0, .z = 3.0, .mass = 4.0}, (Particle){.x = 5.0, .y = 6.0, .z = 7.0, .mass = 8.0}, (Particle){.x = 9.0, .y = 10.0, .z = 11.0, .mass = 12.0}}, 3);
        puts(coord_to_string(r2));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
