// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double to_float(long long x);
double user_ln(double x);
double user_exp(double x);
double pow_float(double base, double exponent);
double get_altitude_at_pressure(double pressure);
int main(void);

double to_float(long long x) {
    return x * 1.0;
}

double user_ln(double x) {
    if (x <= 0.0) {
        panic("ln domain error");
    }
    double y = (x - 1.0) / (x + 1.0);
    double y2_ = y * y;
    double term = y;
    double sum = 0.0;
    long long k = 0LL;
    while (k < 10LL) {
        double denom = to_float((2LL * k) + 1LL);
        sum = sum + (term / denom);
        term = term * y2_;
        k = k + 1LL;
    }
    return 2.0 * sum;
}

double user_exp(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long n = 1LL;
    while (n < 20LL) {
        term = (term * x) / to_float(n);
        sum = sum + term;
        n = n + 1LL;
    }
    return sum;
}

double pow_float(double base, double exponent) {
    return exp(exponent * log(base));
}

double get_altitude_at_pressure(double pressure) {
    if (pressure > 101325.0) {
        panic("Value Higher than Pressure at Sea Level !");
    }
    if (pressure < 0.0) {
        panic("Atmospheric Pressure can not be negative !");
    }
    double ratio = pressure / 101325.0;
    return 44330.0 * (1.0 - pow_float(ratio, 1.0 / 5.5255));
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(get_altitude_at_pressure(100000.0)));
        puts(str_float(get_altitude_at_pressure(101325.0)));
        puts(str_float(get_altitude_at_pressure(80000.0)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
