// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

typedef struct { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static char* str_map_sd(MapSD m) {
    char *res = strdup("map[");
    for (size_t i = 0; i < m.len; i++) {
        char *k = strdup(m.keys[i]);
        char *v = str_float(m.vals[i]);
        char *kv = str_concat(k, ":");
        kv = str_concat(kv, v);
        res = str_concat(res, kv);
        if (i + 1 < m.len) res = str_concat(res, " ");
        free(k); free(v); free(kv);
    }
    res = str_concat(res, "]");
    return res;
}

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;
double REDUCED_PLANCK_CONSTANT = 1.054571817e-34;
double SPEED_OF_LIGHT = 3e+08;

double sqrtApprox(double x);
MapSD casimir_force(double force, double area, double distance);
void user_main();
int main(void);

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 100LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

MapSD casimir_force(double force, double area, double distance) {
    long long zero_count = 0LL;
    if (force == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (area == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (distance == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (zero_count != 1LL) {
        panic("One and only one argument must be 0");
    }
    if (force < 0.0) {
        panic("Magnitude of force can not be negative");
    }
    if (distance < 0.0) {
        panic("Distance can not be negative");
    }
    if (area < 0.0) {
        panic("Area can not be negative");
    }
    if (force == 0.0) {
        double num = (((REDUCED_PLANCK_CONSTANT * SPEED_OF_LIGHT) * PI) * PI) * area;
        double den = (((240.0 * distance) * distance) * distance) * distance;
        double f = num / den;
        {
            const char** __ret3_keys = malloc(1 * sizeof(char*));
            double *__ret3_vals = malloc(1 * sizeof(double));
            __ret3_keys[0] = "force";
            __ret3_vals[0] = f;
            MapSD __ret3 = { __ret3_keys, __ret3_vals, 1, 17 };
            return __ret3;
        }
    }
    if (area == 0.0) {
        double num = ((((240.0 * force) * distance) * distance) * distance) * distance;
        double den = ((REDUCED_PLANCK_CONSTANT * SPEED_OF_LIGHT) * PI) * PI;
        double a = num / den;
        {
            const char** __ret4_keys = malloc(1 * sizeof(char*));
            double *__ret4_vals = malloc(1 * sizeof(double));
            __ret4_keys[0] = "area";
            __ret4_vals[0] = a;
            MapSD __ret4 = { __ret4_keys, __ret4_vals, 1, 17 };
            return __ret4;
        }
    }
    double num = (((REDUCED_PLANCK_CONSTANT * SPEED_OF_LIGHT) * PI) * PI) * area;
    double den = 240.0 * force;
    double inner = num / den;
    double d = sqrtApprox(sqrtApprox(inner));
    {
        const char** __ret5_keys = malloc(1 * sizeof(char*));
        double *__ret5_vals = malloc(1 * sizeof(double));
        __ret5_keys[0] = "distance";
        __ret5_vals[0] = d;
        MapSD __ret5 = { __ret5_keys, __ret5_vals, 1, 17 };
        return __ret5;
    }
}

void user_main() {
    puts(({MapSD __tmp6 = casimir_force(0.0, 4.0, 0.03); char *__res = str_map_sd(__tmp6); __res;}));
    puts(({MapSD __tmp7 = casimir_force(2.635e-10, 0.0023, 0.0); char *__res = str_map_sd(__tmp7); __res;}));
    puts(({MapSD __tmp8 = casimir_force(2.737e-18, 0.0, 0.0023746); char *__res = str_map_sd(__tmp8); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
