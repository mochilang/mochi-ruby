// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double user_abs(double x);
double sqrtApprox(double x);
double user_ln(double x);
double log10(double x);
double peak_signal_to_noise_ratio(long long * * original, size_t original_len, size_t* original_lens, size_t original_lens_len, long long * * contrast, size_t contrast_len, size_t* contrast_lens, size_t contrast_lens_len);
int main(void);

double user_abs(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double user_ln(double x) {
    double t = (x - 1.0) / (x + 1.0);
    double term = t;
    double sum = 0.0;
    long long n = 1LL;
    while (n <= 19LL) {
        sum = sum + (term / (double)(n));
        term = (term * t) * t;
        n = n + 2LL;
    }
    return 2.0 * sum;
}

double log10(double x) {
    return log(x) / log(10.0);
}

double peak_signal_to_noise_ratio(long long * * original, size_t original_len, size_t* original_lens, size_t original_lens_len, long long * * contrast, size_t contrast_len, size_t* contrast_lens, size_t contrast_lens_len) {
    double mse = 0.0;
    long long i = 0LL;
    while (i < original_len) {
        long long j = 0LL;
        while (j < original_lens[(int)(i)]) {
            double diff = original[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? original_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? original_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] - contrast[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? contrast_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? contrast_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
            mse = mse + (diff * diff);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    double size = original_len * original_lens[(int)(0LL)];
    mse = mse / size;
    if (mse == 0.0) {
        return 100.0;
    }
    double PIXEL_MAX = 255.0;
    return 20.0 * log10(PIXEL_MAX / sqrtApprox(mse));
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
