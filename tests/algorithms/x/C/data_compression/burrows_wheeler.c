// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t all_rotations_len;
size_t sort_strings_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct BWTResult BWTResult;

struct BWTResult {
    const char* bwt_string;
    long long idx_original_string;
};

const char* s = "^BANANA";
BWTResult result;

const char* * all_rotations(const char* s);
const char* * sort_strings(const char* * arr, size_t arr_len);
const char* join_strings(const char* * arr, size_t arr_len);
BWTResult bwt_transform(const char* s);
long long index_of(const char* * arr, size_t arr_len, const char* target);
const char* reverse_bwt(const char* bwt_string, long long idx_original_string);
int main(void);

const char* * all_rotations(const char* s) {
    long long n = strlen(s);
    const char* *rotations = NULL;
    size_t rotations_len = 0;
    long long i = 0LL;
    while (i < n) {
        const char* rotation = str_concat(_substring(s, i, n), _substring(s, 0LL, i));
        rotations = list_append_str(rotations, &rotations_len, rotation);
        i = i + 1LL;
    }
    return all_rotations_len = rotations_len, rotations;
}

const char* * sort_strings(const char* * arr, size_t arr_len) {
    long long n = arr_len;
    long long i = 1LL;
    while (i < n) {
        const char* key = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (strcmp(arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], key) > 0)) {
            arr[(int)(j + 1LL)] = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        arr[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return sort_strings_len = arr_len, arr;
}

const char* join_strings(const char* * arr, size_t arr_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < arr_len) {
        res = str_concat(res, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return res;
}

BWTResult bwt_transform(const char* s) {
    if (strcmp(s, "") == 0) {
        panic("input string must not be empty");
    }
    const char* *rotations = all_rotations(s);
    size_t rotations_len = all_rotations_len;
    rotations = sort_strings(rotations, rotations_len);
    rotations_len = sort_strings_len;
    const char* *last_col = NULL;
    size_t last_col_len = 0;
    long long i = 0LL;
    while (i < rotations_len) {
        const char* word = rotations[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? rotations_len + _mochi_idx : _mochi_idx;})];
        last_col = list_append_str(last_col, &last_col_len, _substring(word, strlen(word) - 1LL, strlen(word)));
        i = i + 1LL;
    }
    const char* bwt_string = join_strings(last_col, last_col_len);
    long long idx = index_of(rotations, rotations_len, s);
    return (BWTResult){.bwt_string = bwt_string, .idx_original_string = idx};
}

long long index_of(const char* * arr, size_t arr_len, const char* target) {
    long long i = 0LL;
    while (i < arr_len) {
        if (strcmp(arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], target) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* reverse_bwt(const char* bwt_string, long long idx_original_string) {
    if (strcmp(bwt_string, "") == 0) {
        panic("bwt string must not be empty");
    }
    long long n = strlen(bwt_string);
    if ((idx_original_string < 0LL) || (idx_original_string >= n)) {
        panic("index out of range");
    }
    const char* *ordered_rotations = NULL;
    size_t ordered_rotations_len = 0;
    long long i = 0LL;
    while (i < n) {
        ordered_rotations = list_append_str(ordered_rotations, &ordered_rotations_len, "");
        i = i + 1LL;
    }
    long long iter = 0LL;
    while (iter < n) {
        long long j = 0LL;
        while (j < n) {
            const char* ch = _substring(bwt_string, j, j + 1LL);
            ordered_rotations[(int)(j)] = str_concat(ch, ordered_rotations[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ordered_rotations_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        ordered_rotations = sort_strings(ordered_rotations, ordered_rotations_len);
        ordered_rotations_len = sort_strings_len;
        iter = iter + 1LL;
    }
    return ordered_rotations[(int)({long long _mochi_idx = idx_original_string; _mochi_idx < 0 ? ordered_rotations_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        result = bwt_transform(s);
        puts(result.bwt_string);
        printf("%d\n", result.idx_original_string);
        puts(reverse_bwt(result.bwt_string, result.idx_original_string));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
