// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t find_all_len;
size_t world_covid19_stats_len;
size_t *world_covid19_stats_lens;

size_t append_len;
static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* sample_html = "<h1>Coronavirus Cases:</h1><div class=\"maincounter-number\"><span>100</span></div><h1>Deaths:</h1><div class=\"maincounter-number\"><span>10</span></div><h1>Recovered:</h1><div class=\"maincounter-number\"><span>50</span></div><span class=\"panel-title\">Active Cases</span><div class=\"number-table-main\">20</div><span class=\"panel-title\">Closed Cases</span><div class=\"number-table-main\">80</div>";
const char* **stats = NULL;
size_t stats_len = 0;
size_t *stats_lens = NULL;
size_t stats_lens_len = 0;
long long i = 0LL;

long long index_of(const char* s, const char* sub, long long start);
const char* * find_all(const char* html, const char* open, const char* close);
const char* * * world_covid19_stats(const char* html);
int main(void);

long long index_of(const char* s, const char* sub, long long start) {
    long long n = strlen(s);
    long long m = strlen(sub);
    long long i = start;
    while (i <= (n - m)) {
        long long j = 0LL;
        while ((j < m) && (strcmp(_substring(s, i + j, (i + j) + 1LL), _substring(sub, j, j + 1LL)) == 0)) {
            j = j + 1LL;
        }
        if (j == m) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* * find_all(const char* html, const char* open, const char* close) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long pos = 0LL;
    long long ol = strlen(open);
    long long cl = strlen(close);
    while (1) {
        long long start = index_of(html, open, pos);
        if (start < 0LL) {
            break;
        }
        long long begin = start + ol;
        long long end = index_of(html, close, begin);
        if (end < 0LL) {
            break;
        }
        res = list_append_str(res, &res_len, _substring(html, begin, end));
        pos = end + cl;
    }
    return find_all_len = res_len, res;
}

const char* * * world_covid19_stats(const char* html) {
    const char* *keys = find_all(html, "<h1>", "</h1>");
    size_t keys_len = find_all_len;
    const char* *values = find_all(html, "<div class=\"maincounter-number\"><span>", "</span></div>");
    size_t values_len = find_all_len;
    const char* *extra_keys = find_all(html, "<span class=\"panel-title\">", "</span>");
    size_t extra_keys_len = find_all_len;
    for (size_t __i = 0; __i < extra_keys_len; __i++) {
        const char* k = extra_keys[__i];
        keys = list_append_str(keys, &keys_len, k);
    }
    const char* *extra_vals = find_all(html, "<div class=\"number-table-main\">", "</div>");
    size_t extra_vals_len = find_all_len;
    for (size_t __i = 0; __i < extra_vals_len; __i++) {
        const char* v = extra_vals[__i];
        values = list_append_str(values, &values_len, v);
    }
    const char* **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while ((i < keys_len) && (i < values_len)) {
        res = list_append_strptr(res, &res_len, ({const char** tmp = malloc(2 * sizeof(const char*)); tmp[0] = keys[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? keys_len + _mochi_idx : _mochi_idx;})]; tmp[1] = values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? values_len + _mochi_idx : _mochi_idx;})]; tmp;}));
        res_lens = list_append_szt(res_lens, &res_lens_len, 2);
        i = i + 1LL;
    }
    return world_covid19_stats_lens = res_lens, world_covid19_stats_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        stats = world_covid19_stats(sample_html);
        stats_len = world_covid19_stats_len;
        stats_lens = world_covid19_stats_lens;
        puts("COVID-19 Status of the World\n");
        while (i < stats_len) {
            puts(stats[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? stats_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? stats_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            puts(stats[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? stats_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? stats_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
