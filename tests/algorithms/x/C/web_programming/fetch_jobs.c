// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t splitBy_len;
size_t fetch_jobs_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Anon1 Anon1;
typedef struct Job Job;

struct Anon1 {
    const char* __name;
    const char* company;
    const char* title;
};

struct Job {
    const char* title;
    const char* company;
};

static Job* list_append_Job(Job *arr, size_t *len, Job val) {
    arr = realloc(arr, (*len + 1) * sizeof(Job));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* SAMPLE_HTML = "<div data-tn-component=\"organicJob\"><a data-tn-element=\"jobTitle\">Android Developer</a><span class=\"company\">ABC Corp</span></div><div data-tn-component=\"organicJob\"><a data-tn-element=\"jobTitle\">iOS Engineer</a><span class=\"company\">XYZ Ltd</span></div>";

long long indexOf(const char* s, const char* sub, long long start);
const char* * splitBy(const char* s, const char* sep);
const char* between(const char* s, const char* startTag, const char* endTag);
const char* intToString(long long n);
Job * fetch_jobs(const char* location);
void user_main();
int main(void);

long long indexOf(const char* s, const char* sub, long long start) {
    long long n = strlen(s);
    long long m = strlen(sub);
    long long i = start;
    while (i <= (n - m)) {
        long long j = 0LL;
        long long ok = 1LL;
        while (j < m) {
            if (strcmp(_substring(s, i + j, (i + j) + 1LL), _substring(sub, j, j + 1LL)) != 0) {
                ok = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (ok) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* * splitBy(const char* s, const char* sep) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long start = 0LL;
    long long sepLen = strlen(sep);
    long long idx = indexOf(s, sep, 0LL);
    while (idx != -1LL) {
        res = list_append_str(res, &res_len, _substring(s, start, idx));
        start = idx + sepLen;
        idx = indexOf(s, sep, start);
    }
    res = list_append_str(res, &res_len, _substring(s, start, strlen(s)));
    return splitBy_len = res_len, res;
}

const char* between(const char* s, const char* startTag, const char* endTag) {
    long long startIdx = indexOf(s, startTag, 0LL);
    if (startIdx == -1LL) {
        return "";
    }
    startIdx = startIdx + strlen(startTag);
    long long endIdx = indexOf(s, endTag, startIdx);
    if (endIdx == -1LL) {
        return "";
    }
    return _substring(s, startIdx, endIdx);
}

const char* intToString(long long n) {
    if (n == 0LL) {
        return "0";
    }
    long long num = n;
    const char* digits = "";
    while (num > 0LL) {
        long long d = num % 10LL;
        digits = str_concat(_substring("0123456789", d, d + 1LL), digits);
        num = num / 10LL;
    }
    return digits;
}

Job * fetch_jobs(const char* location) {
    const char* *blocks = splitBy(SAMPLE_HTML, "data-tn-component=\"organicJob\"");
    size_t blocks_len = splitBy_len;
    Job *jobs = NULL;
    size_t jobs_len = 0;
    long long i = 1LL;
    while (i < blocks_len) {
        const char* block = blocks[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? blocks_len + _mochi_idx : _mochi_idx;})];
        const char* title = between(block, "data-tn-element=\"jobTitle\">", "</a>");
        const char* company = between(block, "class=\"company\">", "</span>");
        jobs = list_append_Job(jobs, &jobs_len, (Job){.title = title, .company = company});
        i = i + 1LL;
    }
    return fetch_jobs_len = jobs_len, jobs;
}

void user_main() {
    Job *jobs = fetch_jobs("Bangalore");
    size_t jobs_len = fetch_jobs_len;
    long long i = 0LL;
    while (i < jobs_len) {
        Job j = jobs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? jobs_len + _mochi_idx : _mochi_idx;})];
        long long idx = i + 1LL;
        puts(str_concat(str_concat(str_concat(str_concat(str_concat("Job ", intToString(idx)), " is "), j.title), " at "), j.company));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
