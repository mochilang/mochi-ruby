// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_len;
size_t bubble_sort_len;
size_t extract_links_len;
size_t extract_emails_len;
size_t emails_from_url_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Page Page;

struct Page {
    const char* url;
    const char* html;
};

Page pages_init[2] = {(Page){.url = "https://example.com", .html = "<html><body><a href=\"/contact\">Contact</a></body></html>"}, (Page){.url = "https://example.com/contact", .html = "<html>Contact us at info@example.com or support@example.com</html>"}};
Page *pages = pages_init;
size_t pages_len = 2;
const char* *emails = NULL;
size_t emails_len = 0;
long long k = 0LL;

long long index_of(const char* s, const char* ch);
long long index_of_substring(const char* s, const char* sub);
const char* * split(const char* s, const char* sep);
const char* get_sub_domain_name(const char* url);
const char* get_domain_name(const char* url);
long long is_alnum(const char* ch);
long long contains(const char* * xs, size_t xs_len, const char* x);
const char* * bubble_sort(const char* * xs, size_t xs_len);
const char* * extract_links(const char* domain, const char* html);
const char* * extract_emails(const char* domain, const char* text);
const char* find_page(Page * pages, size_t pages_len, const char* url);
const char* * emails_from_url(const char* url, Page * pages, size_t pages_len);
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long index_of_substring(const char* s, const char* sub) {
    long long n = strlen(s);
    long long m = strlen(sub);
    if (m == 0LL) {
        return 0LL;
    }
    long long i = 0LL;
    while (i <= (n - m)) {
        long long j = 0LL;
        long long is_match = 1LL;
        while (j < m) {
            if (strcmp((const char[]){s[i + j], 0}, (const char[]){sub[j], 0}) != 0) {
                is_match = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (is_match) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* * split(const char* s, const char* sep) {
    const char* *parts = NULL;
    size_t parts_len = 0;
    long long last = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        if (strcmp(ch, sep) == 0) {
            parts = list_append_str(parts, &parts_len, _substring(s, last, i));
            last = i + 1LL;
        }
        if ((i + 1LL) == strlen(s)) {
            parts = list_append_str(parts, &parts_len, _substring(s, last, i + 1LL));
        }
        i = i + 1LL;
    }
    return split_len = parts_len, parts;
}

const char* get_sub_domain_name(const char* url) {
    long long proto_pos = index_of_substring(url, "://");
    long long start = 0LL;
    if (proto_pos >= 0LL) {
        start = proto_pos + 3LL;
    }
    long long i = start;
    while (i < strlen(url)) {
        if (strcmp((const char[]){url[i], 0}, "/") == 0) {
            break;
        }
        i = i + 1LL;
    }
    return _substring(url, start, i);
}

const char* get_domain_name(const char* url) {
    const char* sub = get_sub_domain_name(url);
    const char* *parts = split(sub, ".");
    size_t parts_len = split_len;
    if (parts_len >= 2LL) {
        return str_concat(str_concat(parts[(int)({long long _mochi_idx = parts_len - 2LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})], "."), parts[(int)({long long _mochi_idx = parts_len - 1LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})]);
    }
    return sub;
}

long long is_alnum(const char* ch) {
    const char* chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";
    return index_of(chars, ch) >= 0LL;
}

long long contains(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * bubble_sort(const char* * xs, size_t xs_len) {
    const char* *arr = xs;
    size_t arr_len = xs_len;
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while ((j + 1LL) < (n - i)) {
            if (strcmp(arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) > 0) {
                const char* tmp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bubble_sort_len = arr_len, arr;
}

const char* * extract_links(const char* domain, const char* html) {
    const char* *links = NULL;
    size_t links_len = 0;
    long long pos = index_of_substring(html, "href=");
    while (pos >= 0LL) {
        long long start_quote = index_of(_substring(html, pos + 5LL, strlen(html)), "\"");
        if (start_quote < 0LL) {
            break;
        }
        long long rest = ((pos + 5LL) + start_quote) + 1LL;
        long long end_quote = index_of(_substring(html, rest, strlen(html)), "\"");
        if (end_quote < 0LL) {
            break;
        }
        const char* link = _substring(html, rest, rest + end_quote);
        if (!(contains(links, links_len, link))) {
            const char* absolute = link;
            if (!((index_of_substring(link, "http://") == 0LL) || (index_of_substring(link, "https://") == 0LL))) {
                if (index_of_substring(link, "/") == 0LL) {
                    absolute = str_concat(str_concat("https://", domain), link);
                } else {
                    absolute = str_concat(str_concat(str_concat("https://", domain), "/"), link);
                }
            }
            links = list_append_str(links, &links_len, absolute);
        }
        pos = index_of_substring(_substring(html, rest + end_quote, strlen(html)), "href=");
        if (pos >= 0LL) {
            pos = (pos + rest) + end_quote;
        }
    }
    return extract_links_len = links_len, links;
}

const char* * extract_emails(const char* domain, const char* text) {
    const char* *emails = NULL;
    size_t emails_len = 0;
    long long i = 0LL;
    while (i < strlen(text)) {
        if (strcmp((const char[]){text[i], 0}, "@") == 0) {
            if (strcmp(_substring(text, i + 1LL, (i + 1LL) + strlen(domain)), domain) == 0) {
                long long j = i - 1LL;
                while ((j >= 0LL) && is_alnum((const char[]){text[j], 0})) {
                    j = j - 1LL;
                }
                const char* local = _substring(text, j + 1LL, i);
                if (strlen(local) > 0LL) {
                    const char* email = str_concat(str_concat(local, "@"), domain);
                    if (!(contains(emails, emails_len, email))) {
                        emails = list_append_str(emails, &emails_len, email);
                    }
                }
            }
        }
        i = i + 1LL;
    }
    return extract_emails_len = emails_len, emails;
}

const char* find_page(Page * pages, size_t pages_len, const char* url) {
    long long i = 0LL;
    while (i < pages_len) {
        Page p = pages[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? pages_len + _mochi_idx : _mochi_idx;})];
        if (strcmp(p.url, url) == 0) {
            return p.html;
        }
        i = i + 1LL;
    }
    return "";
}

const char* * emails_from_url(const char* url, Page * pages, size_t pages_len) {
    const char* domain = get_domain_name(url);
    const char* base_html = find_page(pages, pages_len, url);
    const char* *links = extract_links(domain, base_html);
    size_t links_len = extract_links_len;
    const char* *found = NULL;
    size_t found_len = 0;
    long long i = 0LL;
    while (i < links_len) {
        const char* html = find_page(pages, pages_len, links[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? links_len + _mochi_idx : _mochi_idx;})]);
        const char* *emails = extract_emails(domain, html);
        size_t emails_len = extract_emails_len;
        long long j = 0LL;
        while (j < emails_len) {
            if (!(contains(found, found_len, emails[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? emails_len + _mochi_idx : _mochi_idx;})]))) {
                found = list_append_str(found, &found_len, emails[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? emails_len + _mochi_idx : _mochi_idx;})]);
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    const char* *sorted = bubble_sort(found, found_len);
    size_t sorted_len = bubble_sort_len;
    return emails_from_url_len = sorted_len, sorted;
}

int main(void) {
    {
        long long __start = _now();
        emails = emails_from_url("https://example.com", pages, pages_len);
        emails_len = emails_from_url_len;
        puts(str_concat(str_int(emails_len), " emails found:"));
        while (k < emails_len) {
            puts(emails[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? emails_len + _mochi_idx : _mochi_idx;})]);
            k = k + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
