// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t extract_numbers_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct CovidData CovidData;

struct CovidData {
    long long cases;
    long long deaths;
    long long recovered;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long parse_int(const char* s);
long long find(const char* haystack, const char* needle, long long start);
long long * extract_numbers(const char* html);
CovidData covid_stats(const char* html);
void user_main();
int main(void);

long long parse_int(const char* s) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (strcmp(ch, ",") == 0) {
            i = i + 1LL;
            continue;
        }
        value = (value * 10LL) + _atoi(ch);
        i = i + 1LL;
    }
    return value;
}

long long find(const char* haystack, const char* needle, long long start) {
    long long nlen = strlen(needle);
    long long i = start;
    while (i <= (strlen(haystack) - nlen)) {
        long long j = 0LL;
        long long matched = 1LL;
        while (j < nlen) {
            if (strcmp(_substring(haystack, i + j, (i + j) + 1LL), _substring(needle, j, j + 1LL)) != 0) {
                matched = 0LL;
                break;
            }
            j = j + 1LL;
        }
        if (matched) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long * extract_numbers(const char* html) {
    long long *nums = NULL;
    size_t nums_len = 0;
    long long pos = 0LL;
    const char* start_tag = "<span>";
    const char* end_tag = "</span>";
    while (1) {
        long long s = find(html, start_tag, pos);
        if (s == (0LL - 1LL)) {
            break;
        }
        long long content_start = s + strlen(start_tag);
        long long e = find(html, end_tag, content_start);
        if (e == (0LL - 1LL)) {
            break;
        }
        const char* num_str = _substring(html, content_start, e);
        nums = list_append_long_long(nums, &nums_len, parse_int(num_str));
        pos = e + strlen(end_tag);
    }
    return extract_numbers_len = nums_len, nums;
}

CovidData covid_stats(const char* html) {
    long long *nums = extract_numbers(html);
    size_t nums_len = extract_numbers_len;
    return (CovidData){.cases = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})], .deaths = nums[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})], .recovered = nums[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})]};
}

void user_main() {
    const char* sample_html = "<div class=\"maincounter-number\"><span>123456</span></div><div class=\"maincounter-number\"><span>7890</span></div><div class=\"maincounter-number\"><span>101112</span></div>";
    CovidData stats = covid_stats(sample_html);
    puts(str_concat("Total COVID-19 cases in the world: ", str_int(stats.cases)));
    puts(str_concat("Total deaths due to COVID-19 in the world: ", str_int(stats.deaths)));
    puts(str_concat("Total COVID-19 patients recovered in the world: ", str_int(stats.recovered)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
