// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct GitHubUser GitHubUser;

struct GitHubUser {
    const char* login;
    long long id;
};

const char* AUTHENTICATED_USER_ENDPOINT = "https://api.github.com/user";

const char* get_header(const char* * * hs, size_t hs_len, size_t* hs_lens, size_t hs_lens_len, const char* key);
GitHubUser mock_response(const char* url, const char* * * headers, size_t headers_len, size_t* headers_lens, size_t headers_lens_len);
GitHubUser fetch_github_info(const char* auth_token);
void test_fetch_github_info();
void user_main();
int main(void);

const char* get_header(const char* * * hs, size_t hs_len, size_t* hs_lens, size_t hs_lens_len, const char* key) {
    long long i = 0LL;
    while (i < hs_len) {
        const char* *pair = hs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? hs_len + _mochi_idx : _mochi_idx;})];
        size_t pair_len = hs_lens[(int)(i)];
        if (strcmp(pair[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? pair_len + _mochi_idx : _mochi_idx;})], key) == 0) {
            return pair[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? pair_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return "";
}

GitHubUser mock_response(const char* url, const char* * * headers, size_t headers_len, size_t* headers_lens, size_t headers_lens_len) {
    if (strcmp(url, AUTHENTICATED_USER_ENDPOINT) != 0) {
        panic("wrong url");
    }
    const char* auth = get_header(headers, headers_len, headers_lens, headers_len, "Authorization");
    if (strlen(auth) == 0LL) {
        panic("missing Authorization");
    }
    if (strcmp(_substring(auth, 0LL, 6LL), "token ") != 0) {
        panic("bad token prefix");
    }
    const char* accept = get_header(headers, headers_len, headers_lens, headers_len, "Accept");
    if (strlen(accept) == 0LL) {
        panic("missing Accept");
    }
    return (GitHubUser){.login = "test", .id = 1LL};
}

GitHubUser fetch_github_info(const char* auth_token) {
    const char* headers_0[2] = {"Authorization", str_concat("token ", auth_token)};
    const char* headers_1[2] = {"Accept", "application/vnd.github.v3+json"};
    const char* *headers_init[2] = {headers_0, headers_1};
    const char* **headers = headers_init;
    size_t headers_len = 2;
    size_t headers_lens_init[2] = {2, 2};
    size_t *headers_lens = headers_lens_init;
    size_t headers_lens_len = 2;
    return mock_response(AUTHENTICATED_USER_ENDPOINT, headers, headers_len, headers_lens, headers_len);
}

void test_fetch_github_info() {
    GitHubUser result = fetch_github_info("token");
    if (strcmp(result.login, "test") != 0) {
        panic("login mismatch");
    }
    if (result.id != 1LL) {
        panic("id mismatch");
    }
    puts(result.login);
    puts(str_int(result.id));
}

void user_main() {
    test_fetch_github_info();
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
