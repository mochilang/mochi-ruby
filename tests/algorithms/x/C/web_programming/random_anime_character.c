// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Character Character;

struct Character {
    const char* title;
    const char* description;
    const char* image_file;
};

long long seed = 123456789LL;
Character characters_init[3] = {(Character){.title = "Naruto Uzumaki", .description = "A spirited ninja of the Hidden Leaf Village.", .image_file = "naruto.png"}, (Character){.title = "Sailor Moon", .description = "A magical girl who fights for love and justice.", .image_file = "sailor_moon.png"}, (Character){.title = "Spike Spiegel", .description = "A bounty hunter with a laid-back attitude.", .image_file = "spike_spiegel.png"}};
Character *characters = characters_init;
size_t characters_len = 3;
Character c;

long long user_rand();
long long random_int(long long a, long long b);
const char* save_image(const char* _name);
Character random_anime_character();
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed;
}

long long random_int(long long a, long long b) {
    return a + (user_rand() % (b - a));
}

const char* save_image(const char* _name) {
}

Character random_anime_character() {
    long long idx = random_int(0LL, 3LL);
    Character ch = characters[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? characters_len + _mochi_idx : _mochi_idx;})];
    save_image(ch.image_file);
    return ch;
}

int main(void) {
    {
        long long __start = _now();
        c = random_anime_character();
        puts(c.title);
        puts("");
        puts(c.description);
        puts("");
        puts(str_concat("Image saved : ", c.image_file));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
