// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t get_forbes_real_time_billionaires_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

typedef struct { const char **keys; const char **vals; size_t len; size_t cap; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Person Person;
typedef struct PersonsWrapper PersonsWrapper;
typedef struct Response Response;

struct Person {
    double finalWorth;
    const char* personName;
    const char* source;
    const char* countryOfCitizenship;
    const char* gender;
    double birthDate;
};

struct PersonsWrapper {
    Person *personsLists;
    size_t personsLists_len;
    long long count;
};

struct Response {
    PersonsWrapper personList;
};

static MapSS* list_append_MapSS(MapSS *arr, size_t *len, MapSS val) {
    arr = realloc(arr, (*len + 1) * sizeof(MapSS));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long LIMIT = 10LL;
double TODAY_MS = 1.7050176e+12;
const char* API_URL = "";

double round1(double value);
long long years_old(double birth_ms, double today_ms);
MapSS * get_forbes_real_time_billionaires();
void display_billionaires(MapSS * list, size_t list_len);
int main(void);

double round1(double value) {
    if (value >= 0.0) {
        long long scaled = (value * 10.0) + 0.5;
        return (double)(scaled) / 10.0;
    }
    long long scaled = (value * 10.0) - 0.5;
    return (double)(scaled) / 10.0;
}

long long years_old(double birth_ms, double today_ms) {
    double ms_per_year = 3.15576e+10;
    return (today_ms - birth_ms) / ms_per_year;
}

MapSS * get_forbes_real_time_billionaires() {
    Response response;
    MapSS *out = NULL;
    size_t out_len = 0;
    {
        Person* __tmp0 = response.personList.personsLists;
        size_t __tmp0_len = response.personList.personsLists_len;
        for (size_t __i = 0; __i < __tmp0_len; __i++) {
            Person person = __tmp0[__i];
            double worth_billion = round1(person.finalWorth / 1000.0);
            long long age_years = years_old(person.birthDate, TODAY_MS);
            const char* entry_keys[22] = {"Name", "Source", "Country", "Gender", "Worth ($)", "Age"};
            const char* entry_vals[22] = {person.personName, person.source, person.countryOfCitizenship, person.gender, str_concat(str_float(worth_billion), " Billion"), str_int(age_years)};
            size_t entry_len = 6;
            MapSS entry = { entry_keys, entry_vals, 6, 22 };
            out = list_append_MapSS(out, &out_len, entry);
        }
    }
    return get_forbes_real_time_billionaires_len = out_len, out;
}

void display_billionaires(MapSS * list, size_t list_len) {
    for (size_t __i = 0; __i < list_len; __i++) {
        int b = list_keys[__i];
        puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(b[(int)({long long _mochi_idx = "Name"; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})], " | "), b[(int)({long long _mochi_idx = "Source"; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]), " | "), b[(int)({long long _mochi_idx = "Country"; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]), " | "), b[(int)({long long _mochi_idx = "Gender"; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]), " | "), b[(int)({long long _mochi_idx = "Worth ($)"; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]), " | "), b[(int)({long long _mochi_idx = "Age"; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]));
    }
}

int main(void) {
    {
        long long __start = _now();
        API_URL = str_concat("https://www.forbes.com/forbesapi/person/rtb/0/position/true.json?fields=personName,gender,source,countryOfCitizenship,birthDate,finalWorth&limit=", str_int(LIMIT));
        MapSS* __tmp2 = get_forbes_real_time_billionaires();
        display_billionaires(__tmp2, get_forbes_real_time_billionaires_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
