// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; const char **vals; size_t len; size_t cap; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long find(const char* text, const char* pattern, long long start);
const char* stock_price(const char* symbol);
int main(void);

long long find(const char* text, const char* pattern, long long start) {
    long long i = start;
    long long limit = strlen(text) - strlen(pattern);
    while (i <= limit) {
        if (strcmp(_substring(text, i, i + strlen(pattern)), pattern) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* stock_price(const char* symbol) {
    static const char* pages_keys[22] = {"AAPL", "AMZN", "IBM", "GOOG", "MSFT", "ORCL"};
    static const char* pages_vals[22] = {"<span data-testid=\"qsp-price\">228.43</span>", "<span data-testid=\"qsp-price\">201.85</span>", "<span data-testid=\"qsp-price\">210.30</span>", "<span data-testid=\"qsp-price\">177.86</span>", "<span data-testid=\"qsp-price\">414.82</span>", "<span data-testid=\"qsp-price\">188.87</span>"};
    size_t pages_len = 6;
    MapSS pages = { pages_keys, pages_vals, 6, 22 };
    if (contains_str(pages.keys, pages.len, symbol)) {
        const char* html = map_get_ss(pages.keys, pages.vals, pages.len, symbol);
        const char* marker = "<span data-testid=\"qsp-price\">";
        long long start_idx = find(html, marker, 0LL);
        if (start_idx != -1LL) {
            long long price_start = start_idx + strlen(marker);
            long long end_idx = find(html, "</span>", price_start);
            if (end_idx != -1LL) {
                return _substring(html, price_start, end_idx);
            }
        }
    }
    return "No <fin-streamer> tag with the specified data-testid attribute found.";
}

int main(void) {
    {
        long long __start = _now();
        {
            const char* symbol_arr[] = {"AAPL", "AMZN", "IBM", "GOOG", "MSFT", "ORCL"};
            size_t symbol_len = sizeof(symbol_arr) / sizeof(symbol_arr[0]);
            for (size_t __i = 0; __i < symbol_len; __i++) {
                const char* symbol = symbol_arr[__i];
                puts(str_concat(str_concat(str_concat("Current ", symbol), " stock price is "), stock_price(symbol)));
            }
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
