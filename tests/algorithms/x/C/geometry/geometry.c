// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Angle Angle;
typedef struct Circle Circle;
typedef struct Ellipse Ellipse;
typedef struct Polygon Polygon;
typedef struct Side Side;
typedef struct Rectangle Rectangle;
typedef struct Square Square;

struct Angle {
    double degrees;
};

struct Circle {
    double radius;
};

struct Ellipse {
    double major;
    double minor;
};

struct Polygon {
    Side *sides;
    size_t sides_len;
};

struct Side {
    double length;
    Angle angle;
    long long next;
};

struct Rectangle {
    Side short_side;
    Side long_side;
    Polygon poly;
};

struct Square {
    Side side;
    Rectangle rect;
};

static Side* list_append_Side(Side *arr, size_t *len, Side val) {
    arr = realloc(arr, (*len + 1) * sizeof(Side));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double PI = 3.141592653589793;

Angle make_angle(double deg);
Side make_side(double length, Angle angle);
double ellipse_area(Ellipse e);
double ellipse_perimeter(Ellipse e);
double circle_area(Circle c);
double circle_perimeter(Circle c);
double circle_diameter(Circle c);
double circle_max_parts(double num_cuts);
Polygon make_polygon();
void polygon_add_side(Polygon* p, Side s);
Side polygon_get_side(Polygon p, long long index);
void polygon_set_side(Polygon* p, long long index, Side s);
Rectangle make_rectangle(double short_len, double long_len);
double rectangle_perimeter(Rectangle r);
double rectangle_area(Rectangle r);
Square make_square(double side_len);
double square_perimeter(Square s);
double square_area(Square s);
void user_main();
int main(void);

Angle make_angle(double deg) {
    if ((deg < 0.0) || (deg > 360.0)) {
        panic("degrees must be between 0 and 360");
    }
    return (Angle){.degrees = deg};
}

Side make_side(double length, Angle angle) {
    if (length <= 0.0) {
        panic("length must be positive");
    }
    return (Side){.length = length, .angle = angle, .next = -1LL};
}

double ellipse_area(Ellipse e) {
    return (PI * e.major) * e.minor;
}

double ellipse_perimeter(Ellipse e) {
    return PI * (e.major + e.minor);
}

double circle_area(Circle c) {
    Ellipse e = (Ellipse){.major = c.radius, .minor = c.radius};
    double area = ellipse_area(e);
    return area;
}

double circle_perimeter(Circle c) {
    Ellipse e = (Ellipse){.major = c.radius, .minor = c.radius};
    double per = ellipse_perimeter(e);
    return per;
}

double circle_diameter(Circle c) {
    return c.radius * 2.0;
}

double circle_max_parts(double num_cuts) {
    if (num_cuts < 0.0) {
        panic("num_cuts must be positive");
    }
    return ((num_cuts + 2.0) + (num_cuts * num_cuts)) * 0.5;
}

Polygon make_polygon() {
    Side *s = NULL;
    size_t s_len = 0;
    return (Polygon){.sides = s, .sides_len = s_len};
}

void polygon_add_side(Polygon* p, Side s) {
    p->sides = list_append_Side(p->sides, &p->sides_len, s);
}

Side polygon_get_side(Polygon p, long long index) {
    return p.sides[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? p.sides_len + _mochi_idx : _mochi_idx;})];
}

void polygon_set_side(Polygon* p, long long index, Side s) {
    Side *tmp = p->sides;
    size_t tmp_len = p->sides_len;
    tmp[(int)(index)] = s;
    p->sides = tmp;
    p->sides_len = tmp_len;
}

Rectangle make_rectangle(double short_len, double long_len) {
    if ((short_len <= 0.0) || (long_len <= 0.0)) {
        panic("length must be positive");
    }
    Side short_ = make_side(short_len, make_angle(90.0));
    Side long_ = make_side(long_len, make_angle(90.0));
    Polygon p = make_polygon();
    polygon_add_side(&p, short_);
    polygon_add_side(&p, long_);
    return (Rectangle){.short_side = short_, .long_side = long_, .poly = p};
}

double rectangle_perimeter(Rectangle r) {
    return (r.short_side.length + r.long_side.length) * 2.0;
}

double rectangle_area(Rectangle r) {
    return r.short_side.length * r.long_side.length;
}

Square make_square(double side_len) {
    Rectangle rect = make_rectangle(side_len, side_len);
    return (Square){.side = rect.short_side, .rect = rect};
}

double square_perimeter(Square s) {
    double p = rectangle_perimeter(s.rect);
    return p;
}

double square_area(Square s) {
    double a = rectangle_area(s.rect);
    return a;
}

void user_main() {
    Angle a = make_angle(90.0);
    printf("%g\n", a.degrees);
    Side s = make_side(5.0, a);
    printf("%g\n", s.length);
    Ellipse e = (Ellipse){.major = 5.0, .minor = 10.0};
    printf("%g\n", ellipse_area(e));
    printf("%g\n", ellipse_perimeter(e));
    Circle c = (Circle){.radius = 5.0};
    printf("%g\n", circle_area(c));
    printf("%g\n", circle_perimeter(c));
    printf("%g\n", circle_diameter(c));
    printf("%g\n", circle_max_parts(7.0));
    Rectangle r = make_rectangle(5.0, 10.0);
    printf("%g\n", rectangle_perimeter(r));
    printf("%g\n", rectangle_area(r));
    Square q = make_square(5.0);
    printf("%g\n", square_perimeter(q));
    printf("%g\n", square_area(q));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
