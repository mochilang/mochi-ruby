// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t guess_the_number_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long get_avg(long long number_1, long long number_2);
long long * guess_the_number(long long lower, long long higher, long long to_guess);
const char* _answer_0(long long to_guess, long long number);
int main(void);

long long get_avg(long long number_1, long long number_2) {
    return (number_1 + number_2) / 2LL;
}

long long * guess_the_number(long long lower, long long higher, long long to_guess) {
    if (lower > higher) {
        panic("argument value for lower and higher must be(lower > higher)");
    }
    if (!((lower < to_guess) && (to_guess < higher))) {
        panic("guess value must be within the range of lower and higher value");
    }
    puts("started...");
    long long last_lowest = lower;
    long long last_highest = higher;
    long long *last_numbers = NULL;
    size_t last_numbers_len = 0;
    while (1LL) {
        long long number = get_avg(last_lowest, last_highest);
        last_numbers = list_append_long_long(last_numbers, &last_numbers_len, number);
        const char* resp = _answer_0(to_guess, number);
        if (strcmp(resp, "low") == 0) {
            last_lowest = number;
        } else {
            if (strcmp(resp, "high") == 0) {
                last_highest = number;
            } else {
                break;
            }
        }
    }
    puts(str_concat("guess the number : ", str_int(last_numbers[(int)({long long _mochi_idx = last_numbers_len - 1LL; _mochi_idx < 0 ? last_numbers_len + _mochi_idx : _mochi_idx;})])));
    puts(str_concat("details : ", str_list_int(last_numbers, last_numbers_len)));
    return guess_the_number_len = last_numbers_len, last_numbers;
}

const char* _answer_0(long long to_guess, long long number) {
    if (number > to_guess) {
        return "high";
    } else {
        if (number < to_guess) {
            return "low";
        } else {
            return "same";
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        guess_the_number(10LL, 1000LL, 17LL);
        guess_the_number(-10000.0, 10000LL, 7LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
