// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
typedef struct { int *keys; const char **vals; size_t len; size_t cap; } MapIS;

static const char* map_get_is(const int keys[], const char *vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return "";
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long DOOMSDAY_LEAP_init[12] = {4LL, 1LL, 7LL, 4LL, 2LL, 6LL, 4LL, 1LL, 5LL, 3LL, 7LL, 5LL};
long long *DOOMSDAY_LEAP = DOOMSDAY_LEAP_init;
size_t DOOMSDAY_LEAP_len = 12;
long long DOOMSDAY_NOT_LEAP_init[12] = {3LL, 7LL, 7LL, 4LL, 2LL, 6LL, 4LL, 1LL, 5LL, 3LL, 7LL, 5LL};
long long *DOOMSDAY_NOT_LEAP = DOOMSDAY_NOT_LEAP_init;
size_t DOOMSDAY_NOT_LEAP_len = 12;
static int WEEK_DAY_NAMES_keys[23] = {0LL, 1LL, 2LL, 3LL, 4LL, 5LL, 6LL};
static const char* WEEK_DAY_NAMES_vals[23] = {"Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"};
size_t WEEK_DAY_NAMES_len = 7;
MapIS WEEK_DAY_NAMES = { WEEK_DAY_NAMES_keys, WEEK_DAY_NAMES_vals, 7, 23 };

const char* get_week_day(long long year, long long month, long long day);
int main(void);

const char* get_week_day(long long year, long long month, long long day) {
    if (year < 100LL) {
        panic("year should be in YYYY format");
    }
    if ((month < 1LL) || (month > 12LL)) {
        panic("month should be between 1 to 12");
    }
    if ((day < 1LL) || (day > 31LL)) {
        panic("day should be between 1 to 31");
    }
    long long century = year / 100LL;
    long long century_anchor = ((5LL * (century % 4LL)) + 2LL) % 7LL;
    long long centurian = year % 100LL;
    long long centurian_m = centurian % 12LL;
    long long dooms_day = ((((centurian / 12LL) + centurian_m) + (centurian_m / 4LL)) + century_anchor) % 7LL;
    long long day_anchor;
    if (((year % 4LL) != 0LL) || ((centurian == 0LL) && ((year % 400LL) != 0LL))) {
        day_anchor = DOOMSDAY_NOT_LEAP[(int)({long long _mochi_idx = month - 1LL; _mochi_idx < 0 ? DOOMSDAY_NOT_LEAP_len + _mochi_idx : _mochi_idx;})];
    } else {
        day_anchor = DOOMSDAY_LEAP[(int)({long long _mochi_idx = month - 1LL; _mochi_idx < 0 ? DOOMSDAY_LEAP_len + _mochi_idx : _mochi_idx;})];
    }
    long long week_day = ((dooms_day + day) - day_anchor) % 7LL;
    if (week_day < 0LL) {
        week_day = week_day + 7LL;
    }
    return map_get_is(WEEK_DAY_NAMES.keys, WEEK_DAY_NAMES.vals, WEEK_DAY_NAMES.len, week_day);
}

int main(void) {
    {
        long long __start = _now();
        puts(get_week_day(2020LL, 10LL, 24LL));
        puts(get_week_day(2017LL, 10LL, 24LL));
        puts(get_week_day(2019LL, 5LL, 3LL));
        puts(get_week_day(1970LL, 9LL, 16LL));
        puts(get_week_day(1870LL, 8LL, 13LL));
        puts(get_week_day(2040LL, 3LL, 14LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
