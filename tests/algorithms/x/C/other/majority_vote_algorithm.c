// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t majority_vote_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long index_of(long long * xs, size_t xs_len, long long x);
long long * majority_vote(long long * votes, size_t votes_len, long long votes_needed_to_win);
void user_main();
int main(void);

long long index_of(long long * xs, size_t xs_len, long long x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == x) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long * majority_vote(long long * votes, size_t votes_len, long long votes_needed_to_win) {
    if (votes_needed_to_win < 2LL) {
        {
            long long* __tmp3_data = malloc(0 * sizeof(long long));
            majority_vote_len = 0;
            return __tmp3_data;
        }
    }
    long long *candidates = NULL;
    size_t candidates_len = 0;
    long long *counts = NULL;
    size_t counts_len = 0;
    long long i = 0LL;
    while (i < votes_len) {
        long long v = votes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? votes_len + _mochi_idx : _mochi_idx;})];
        long long idx = index_of(candidates, candidates_len, v);
        if (idx != (0LL - 1LL)) {
            counts[(int)(idx)] = counts[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] + 1LL;
        } else {
            if (candidates_len < (votes_needed_to_win - 1LL)) {
                candidates = list_append_long_long(candidates, &candidates_len, v);
                counts = list_append_long_long(counts, &counts_len, 1LL);
            } else {
                long long j = 0LL;
                while (j < counts_len) {
                    counts[(int)(j)] = counts[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] - 1LL;
                    j = j + 1LL;
                }
                long long *new_candidates = NULL;
                size_t new_candidates_len = 0;
                long long *new_counts = NULL;
                size_t new_counts_len = 0;
                j = 0LL;
                while (j < candidates_len) {
                    if (counts[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})] > 0LL) {
                        new_candidates = list_append_long_long(new_candidates, &new_candidates_len, candidates[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? candidates_len + _mochi_idx : _mochi_idx;})]);
                        new_counts = list_append_long_long(new_counts, &new_counts_len, counts[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? counts_len + _mochi_idx : _mochi_idx;})]);
                    }
                    j = j + 1LL;
                }
                candidates = new_candidates;
                candidates_len = new_candidates_len;
                counts = new_counts;
                counts_len = new_counts_len;
            }
        }
        i = i + 1LL;
    }
    long long *final_counts = NULL;
    size_t final_counts_len = 0;
    long long j = 0LL;
    while (j < candidates_len) {
        final_counts = list_append_long_long(final_counts, &final_counts_len, 0LL);
        j = j + 1LL;
    }
    i = 0LL;
    while (i < votes_len) {
        long long v = votes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? votes_len + _mochi_idx : _mochi_idx;})];
        long long idx = index_of(candidates, candidates_len, v);
        if (idx != (0LL - 1LL)) {
            final_counts[(int)(idx)] = final_counts[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? final_counts_len + _mochi_idx : _mochi_idx;})] + 1LL;
        }
        i = i + 1LL;
    }
    long long *result = NULL;
    size_t result_len = 0;
    j = 0LL;
    while (j < candidates_len) {
        if ((final_counts[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? final_counts_len + _mochi_idx : _mochi_idx;})] * votes_needed_to_win) > votes_len) {
            result = list_append_long_long(result, &result_len, candidates[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? candidates_len + _mochi_idx : _mochi_idx;})]);
        }
        j = j + 1LL;
    }
    return majority_vote_len = result_len, result;
}

void user_main() {
    long long *votes = NULL;
    size_t votes_len = 0;
    votes = list_append_long_long(votes, &votes_len, 1LL);
    votes = list_append_long_long(votes, &votes_len, 2LL);
    votes = list_append_long_long(votes, &votes_len, 2LL);
    votes = list_append_long_long(votes, &votes_len, 3LL);
    votes = list_append_long_long(votes, &votes_len, 1LL);
    votes = list_append_long_long(votes, &votes_len, 3LL);
    votes = list_append_long_long(votes, &votes_len, 2LL);
    puts(({long long *__tmp4 = majority_vote(votes, votes_len, 3LL); char *__res = str_list_int(__tmp4, majority_vote_len); __res;}));
    puts(({long long *__tmp5 = majority_vote(votes, votes_len, 2LL); char *__res = str_list_int(__tmp5, majority_vote_len); __res;}));
    puts(({long long *__tmp6 = majority_vote(votes, votes_len, 4LL); char *__res = str_list_int(__tmp6, majority_vote_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
