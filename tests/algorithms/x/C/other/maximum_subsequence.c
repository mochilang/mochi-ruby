// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long max_int(long long a, long long b);
long long max_subsequence_sum(long long * nums, size_t nums_len);
int main(void);

long long max_int(long long a, long long b) {
    if (a >= b) {
        return a;
    } else {
        return b;
    }
}

long long max_subsequence_sum(long long * nums, size_t nums_len) {
    if (nums_len == 0LL) {
        panic("input sequence should not be empty");
    }
    long long ans = nums[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
    long long i = 1LL;
    while (i < nums_len) {
        long long num = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        long long extended = ans + num;
        ans = max_int(max_int(ans, extended), num);
        i = i + 1LL;
    }
    return ans;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", max_subsequence_sum((double[]){1LL, 2LL, 3LL, 4LL, -2.0}, 5));
        printf("%d\n", max_subsequence_sum((double[]){-2.0, -3.0, -1.0, -4.0, -6.0}, 5));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
