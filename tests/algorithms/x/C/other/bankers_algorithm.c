// Generated by Mochi 0.10.32 on 2025-08-22 23:02 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t processes_resource_summation_len;
size_t available_resources_len;
size_t need_len;
size_t *need_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct State State;

struct State {
    long long *claim;
    size_t claim_len;
    long long **alloc;
    size_t alloc_len;
    size_t *alloc_lens;
    size_t alloc_lens_len;
    long long **max;
    size_t max_len;
    size_t *max_lens;
    size_t max_lens_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long claim_vector_init[4] = {8LL, 5LL, 9LL, 7LL};
long long *claim_vector = claim_vector_init;
size_t claim_vector_len = 4;
long long allocated_resources_table_0[4] = {2LL, 0LL, 1LL, 1LL};
long long allocated_resources_table_1[4] = {0LL, 1LL, 2LL, 1LL};
long long allocated_resources_table_2[4] = {4LL, 0LL, 0LL, 3LL};
long long allocated_resources_table_3[4] = {0LL, 2LL, 1LL, 0LL};
long long allocated_resources_table_4[4] = {1LL, 0LL, 3LL, 0LL};
long long *allocated_resources_table_init[5] = {allocated_resources_table_0, allocated_resources_table_1, allocated_resources_table_2, allocated_resources_table_3, allocated_resources_table_4};
long long **allocated_resources_table = allocated_resources_table_init;
size_t allocated_resources_table_len = 5;
size_t allocated_resources_table_lens_init[5] = {4, 4, 4, 4, 4};
size_t *allocated_resources_table_lens = allocated_resources_table_lens_init;
size_t allocated_resources_table_lens_len = 5;
long long maximum_claim_table_0[4] = {3LL, 2LL, 1LL, 4LL};
long long maximum_claim_table_1[4] = {0LL, 2LL, 5LL, 2LL};
long long maximum_claim_table_2[4] = {5LL, 1LL, 0LL, 5LL};
long long maximum_claim_table_3[4] = {1LL, 5LL, 3LL, 0LL};
long long maximum_claim_table_4[4] = {3LL, 0LL, 3LL, 3LL};
long long *maximum_claim_table_init[5] = {maximum_claim_table_0, maximum_claim_table_1, maximum_claim_table_2, maximum_claim_table_3, maximum_claim_table_4};
long long **maximum_claim_table = maximum_claim_table_init;
size_t maximum_claim_table_len = 5;
size_t maximum_claim_table_lens_init[5] = {4, 4, 4, 4, 4};
size_t *maximum_claim_table_lens = maximum_claim_table_lens_init;
size_t maximum_claim_table_lens_len = 5;

long long * processes_resource_summation(long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len);
long long * available_resources(long long * claim, size_t claim_len, long long * alloc_sum, size_t alloc_sum_len);
long long * * need(long long * * max, size_t max_len, size_t* max_lens, size_t max_lens_len, long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len);
const char* pretty_print(long long * claim, size_t claim_len, long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len, long long * * max, size_t max_len, size_t* max_lens, size_t max_lens_len);
const char* bankers_algorithm(long long * claim, size_t claim_len, long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len, long long * * max, size_t max_len, size_t* max_lens, size_t max_lens_len);
int main(void);

long long * processes_resource_summation(long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len) {
    long long resources = alloc_lens[(int)(0LL)];
    long long *sums = NULL;
    size_t sums_len = 0;
    long long i = 0LL;
    while (i < resources) {
        long long total = 0LL;
        long long j = 0LL;
        while (j < alloc_len) {
            total = total + alloc[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? alloc_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? alloc_lens[(int)(j)] + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        sums = list_append_long_long(sums, &sums_len, total);
        i = i + 1LL;
    }
    return processes_resource_summation_len = sums_len, sums;
}

long long * available_resources(long long * claim, size_t claim_len, long long * alloc_sum, size_t alloc_sum_len) {
    long long *avail = NULL;
    size_t avail_len = 0;
    long long i = 0LL;
    while (i < claim_len) {
        avail = list_append_long_long(avail, &avail_len, claim[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? claim_len + _mochi_idx : _mochi_idx;})] - alloc_sum[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? alloc_sum_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return available_resources_len = avail_len, avail;
}

long long * * need(long long * * max, size_t max_len, size_t* max_lens, size_t max_lens_len, long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len) {
    long long **needs = NULL;
    size_t needs_len = 0;
    size_t *needs_lens = NULL;
    size_t needs_lens_len = 0;
    long long i = 0LL;
    while (i < max_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < max_lens[(int)(0LL)]) {
            row = list_append_long_long(row, &row_len, max[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? max_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? max_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] - alloc[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? alloc_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? alloc_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        needs = list_append_intptr(needs, &needs_len, row);
        needs_lens = list_append_szt(needs_lens, &needs_lens_len, row_len);
        i = i + 1LL;
    }
    return need_lens = needs_lens, need_len = needs_len, needs;
}

const char* pretty_print(long long * claim, size_t claim_len, long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len, long long * * max, size_t max_len, size_t* max_lens, size_t max_lens_len) {
    puts("         Allocated Resource Table");
    long long i = 0LL;
    while (i < alloc_len) {
        long long *row = alloc[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? alloc_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = alloc_lens[(int)(i)];
        const char* line = str_concat(str_concat("P", str_int(i + 1LL)), "       ");
        long long j = 0LL;
        while (j < row_len) {
            line = str_concat(line, str_int(row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]));
            if (j < (row_len - 1LL)) {
                line = str_concat(line, "        ");
            }
            j = j + 1LL;
        }
        puts(line);
        puts("");
        i = i + 1LL;
    }
    puts("         System Resource Table");
    i = 0LL;
    while (i < max_len) {
        long long *row = max[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? max_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = max_lens[(int)(i)];
        const char* line = str_concat(str_concat("P", str_int(i + 1LL)), "       ");
        long long j = 0LL;
        while (j < row_len) {
            line = str_concat(line, str_int(row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]));
            if (j < (row_len - 1LL)) {
                line = str_concat(line, "        ");
            }
            j = j + 1LL;
        }
        puts(line);
        puts("");
        i = i + 1LL;
    }
    const char* usage = "";
    i = 0LL;
    while (i < claim_len) {
        if (i > 0LL) {
            usage = str_concat(usage, " ");
        }
        usage = str_concat(usage, str_int(claim[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? claim_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    long long *alloc_sum = processes_resource_summation(alloc, alloc_len, alloc_lens, alloc_len);
    size_t alloc_sum_len = processes_resource_summation_len;
    long long *avail = available_resources(claim, claim_len, alloc_sum, alloc_sum_len);
    size_t avail_len = available_resources_len;
    const char* avail_str = "";
    i = 0LL;
    while (i < avail_len) {
        if (i > 0LL) {
            avail_str = str_concat(avail_str, " ");
        }
        avail_str = str_concat(avail_str, str_int(avail[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? avail_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    puts(str_concat("Current Usage by Active Processes: ", usage));
    puts(str_concat("Initial Available Resources:       ", avail_str));
}

const char* bankers_algorithm(long long * claim, size_t claim_len, long long * * alloc, size_t alloc_len, size_t* alloc_lens, size_t alloc_lens_len, long long * * max, size_t max_len, size_t* max_lens, size_t max_lens_len) {
    long long **need_list = need(max, max_len, max_lens, max_len, alloc, alloc_len, alloc_lens, alloc_len);
    size_t need_list_len = need_len;
    size_t *need_list_lens = need_lens;
    size_t need_list_lens_len = need_len;
    long long *alloc_sum = processes_resource_summation(alloc, alloc_len, alloc_lens, alloc_len);
    size_t alloc_sum_len = processes_resource_summation_len;
    long long *avail = available_resources(claim, claim_len, alloc_sum, alloc_sum_len);
    size_t avail_len = available_resources_len;
    puts("__________________________________________________");
    puts("");
    long long *finished = NULL;
    size_t finished_len = 0;
    long long i = 0LL;
    while (i < need_list_len) {
        finished = list_append_long_long(finished, &finished_len, 0LL);
        i = i + 1LL;
    }
    long long remaining = need_list_len;
    while (remaining > 0LL) {
        long long safe = 0LL;
        long long p = 0LL;
        while (p < need_list_len) {
            if (!(finished[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? finished_len + _mochi_idx : _mochi_idx;})])) {
                long long exec = 1LL;
                long long r = 0LL;
                while (r < avail_len) {
                    if (need_list[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? need_list_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = r; _mochi_idx < 0 ? need_list_lens[(int)(p)] + _mochi_idx : _mochi_idx;})] > avail[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? avail_len + _mochi_idx : _mochi_idx;})]) {
                        exec = 0LL;
                        break;
                    }
                    r = r + 1LL;
                }
                if (exec) {
                    safe = 1LL;
                    puts(str_concat(str_concat("Process ", str_int(p + 1LL)), " is executing."));
                    r = 0LL;
                    while (r < avail_len) {
                        avail[(int)(r)] = avail[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? avail_len + _mochi_idx : _mochi_idx;})] + alloc[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? alloc_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = r; _mochi_idx < 0 ? alloc_lens[(int)(p)] + _mochi_idx : _mochi_idx;})];
                        r = r + 1LL;
                    }
                    const char* avail_str = "";
                    r = 0LL;
                    while (r < avail_len) {
                        if (r > 0LL) {
                            avail_str = str_concat(avail_str, " ");
                        }
                        avail_str = str_concat(avail_str, str_int(avail[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? avail_len + _mochi_idx : _mochi_idx;})]));
                        r = r + 1LL;
                    }
                    puts(str_concat("Updated available resource stack for processes: ", avail_str));
                    puts("The process is in a safe state.");
                    puts("");
                    finished[(int)(p)] = 1LL;
                    remaining = remaining - 1LL;
                }
            }
            p = p + 1LL;
        }
        if (!(safe)) {
            puts("System in unsafe state. Aborting...");
            puts("");
            break;
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        pretty_print(claim_vector, claim_vector_len, allocated_resources_table, allocated_resources_table_len, allocated_resources_table_lens, allocated_resources_table_len, maximum_claim_table, maximum_claim_table_len, maximum_claim_table_lens, maximum_claim_table_len);
        bankers_algorithm(claim_vector, claim_vector_len, allocated_resources_table, allocated_resources_table_len, allocated_resources_table_lens, allocated_resources_table_len, maximum_claim_table, maximum_claim_table_len, maximum_claim_table_lens, maximum_claim_table_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
