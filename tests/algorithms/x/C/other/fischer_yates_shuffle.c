// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t fisher_yates_shuffle_int_len;
size_t fisher_yates_shuffle_str_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long seed = 1LL;
long long integers_init[8] = {0LL, 1LL, 2LL, 3LL, 4LL, 5LL, 6LL, 7LL};
long long *integers = integers_init;
size_t integers_len = 8;
const char* strings_init[4] = {"python", "says", "hello", "!"};
const char* *strings = strings_init;
size_t strings_len = 4;

long long user_rand();
long long randint(long long a, long long b);
long long * fisher_yates_shuffle_int(long long * data, size_t data_len);
const char* * fisher_yates_shuffle_str(const char* * data, size_t data_len);
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed / 65536LL;
}

long long randint(long long a, long long b) {
    long long r = user_rand();
    return a + (r % ((b - a) + 1LL));
}

long long * fisher_yates_shuffle_int(long long * data, size_t data_len) {
    long long *res = data;
    size_t res_len = data_len;
    long long i = 0LL;
    while (i < res_len) {
        long long a = randint(0LL, res_len - 1LL);
        long long b = randint(0LL, res_len - 1LL);
        long long temp = res[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
        res[(int)(a)] = res[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
        res[(int)(b)] = temp;
        i = i + 1LL;
    }
    return fisher_yates_shuffle_int_len = res_len, res;
}

const char* * fisher_yates_shuffle_str(const char* * data, size_t data_len) {
    const char* *res = data;
    size_t res_len = data_len;
    long long i = 0LL;
    while (i < res_len) {
        long long a = randint(0LL, res_len - 1LL);
        long long b = randint(0LL, res_len - 1LL);
        const char* temp = res[(int)({long long _mochi_idx = a; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
        res[(int)(a)] = res[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
        res[(int)(b)] = temp;
        i = i + 1LL;
    }
    return fisher_yates_shuffle_str_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        puts("Fisher-Yates Shuffle:");
        puts(str_concat(str_concat(str_concat("List ", str_list_int(integers, integers_len)), " "), str_list_str(strings, strings_len)));
        puts(str_concat(str_concat(str_concat("FY Shuffle ", ({long long *__tmp2 = fisher_yates_shuffle_int(integers, integers_len); char *__res = str_list_int(__tmp2, fisher_yates_shuffle_int_len); __res;})), " "), ({const char **__tmp3 = fisher_yates_shuffle_str(strings, strings_len); char *__res = str_list_str(__tmp3, fisher_yates_shuffle_str_len); __res;})));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
