// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_element_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t concat_len;
static long long* concat_long_long(long long *a, size_t a_len, const long long *b, size_t b_len) {
    long long *res = realloc(a, (a_len + b_len) * sizeof(long long));
    if (b_len > 0) memcpy(res + a_len, b, b_len * sizeof(long long));
    concat_len = a_len + b_len;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LRUCache LRUCache;

struct LRUCache {
    long long max_capacity;
    const char* *store;
    size_t store_len;
};

LRUCache lru;
const char* r = "";

LRUCache new_cache(long long n);
const char* * remove_element(const char* * xs, size_t xs_len, const char* x);
LRUCache refer(LRUCache cache, const char* x);
void display(LRUCache cache);
const char* repr_item(const char* s);
const char* cache_repr(LRUCache cache);
int main(void);

LRUCache new_cache(long long n) {
    if (n < 0LL) {
        panic("n should be an integer greater than 0.");
    }
    long long cap;
    if (n == 0LL) {
        cap = 2147483647LL;
    } else {
        cap = n;
    }
    return (LRUCache){.max_capacity = cap, .store = NULL, .store_len = 0};
}

const char* * remove_element(const char* * xs, size_t xs_len, const char* x) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long removed = 0LL;
    long long i = 0LL;
    while (i < xs_len) {
        const char* v = xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
        if ((removed == 0LL) && (strcmp(v, x) == 0)) {
            removed = 1LL;
        } else {
            res = list_append_str(res, &res_len, v);
        }
        i = i + 1LL;
    }
    return remove_element_len = res_len, res;
}

LRUCache refer(LRUCache cache, const char* x) {
    const char* *store = cache.store;
    size_t store_len = cache.store_len;
    long long exists = 0LL;
    long long i = 0LL;
    while (i < store_len) {
        if (strcmp(store[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? store_len + _mochi_idx : _mochi_idx;})], x) == 0) {
            exists = 1LL;
        }
        i = i + 1LL;
    }
    if (exists) {
        store = remove_element(store, store_len, x);
        store_len = remove_element_len;
    } else {
        if (store_len == cache.max_capacity) {
            const char* *new_store = NULL;
            size_t new_store_len = 0;
            long long j = 0LL;
            while (j < (store_len - 1LL)) {
                new_store = list_append_str(new_store, &new_store_len, store[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? store_len + _mochi_idx : _mochi_idx;})]);
                j = j + 1LL;
            }
            store = new_store;
            store_len = new_store_len;
        }
    }
    store = ({const char* *__tmp1 = malloc((store_len + 1) * sizeof(const char*)); __tmp1[0] = x; if (store_len > 0) memcpy(__tmp1 + 1, store, store_len * sizeof(const char*)); store_len = store_len + 1; concat_len = store_len; __tmp1;});
    return (LRUCache){.max_capacity = cache.max_capacity, .store = store, .store_len = store_len};
}

void display(LRUCache cache) {
    long long i = 0LL;
    while (i < cache.store_len) {
        puts(cache.store[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cache.store_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
}

const char* repr_item(const char* s) {
    long long all_digits = 1LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        if ((strcmp(ch, "0") < 0) || (strcmp(ch, "9") > 0)) {
            all_digits = 0LL;
        }
        i = i + 1LL;
    }
    if (all_digits) {
        return s;
    }
    return str_concat(str_concat("'", s), "'");
}

const char* cache_repr(LRUCache cache) {
    const char* res = str_concat(str_concat("LRUCache(", str_int(cache.max_capacity)), ") => [");
    long long i = 0LL;
    while (i < cache.store_len) {
        res = str_concat(res, repr_item(cache.store[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cache.store_len + _mochi_idx : _mochi_idx;})]));
        if (i < (cache.store_len - 1LL)) {
            res = str_concat(res, ", ");
        }
        i = i + 1LL;
    }
    res = str_concat(res, "]");
    return res;
}

int main(void) {
    {
        long long __start = _now();
        lru = new_cache(4LL);
        lru = refer(lru, "A");
        lru = refer(lru, "2");
        lru = refer(lru, "3");
        lru = refer(lru, "A");
        lru = refer(lru, "4");
        lru = refer(lru, "5");
        r = cache_repr(lru);
        puts(r);
        if (strcmp(r, "LRUCache(4) => [5, 4, 'A', 3]") != 0) {
            panic("Assertion error");
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
