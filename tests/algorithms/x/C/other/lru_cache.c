// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct DoubleLinkedList DoubleLinkedList;
typedef struct LRUCache LRUCache;
typedef struct Node Node;
typedef struct GetResult GetResult;

struct DoubleLinkedList {
    Node *nodes;
    size_t nodes_len;
    long long head;
    long long tail;
};

struct LRUCache {
    DoubleLinkedList list;
    long long capacity;
    long long num_keys;
    long long hits;
    long long misses;
    MapSI cache;
};

struct Node {
    long long key;
    long long value;
    long long prev;
    long long next;
};

struct GetResult {
    LRUCache cache;
    long long value;
    long long ok;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

DoubleLinkedList new_list();
DoubleLinkedList dll_add(DoubleLinkedList lst, long long idx);
DoubleLinkedList dll_remove(DoubleLinkedList lst, long long idx);
LRUCache new_cache(long long cap);
GetResult lru_get(LRUCache c, long long key);
LRUCache lru_put(LRUCache c, long long key, long long value);
const char* cache_info(LRUCache cache);
void print_result(GetResult res);
void user_main();
int main(void);

DoubleLinkedList new_list() {
    Node *nodes = NULL;
    size_t nodes_len = 0;
    Node head = (Node){.key = 0LL, .value = 0LL, .prev = -1LL, .next = 1LL};
    Node tail = (Node){.key = 0LL, .value = 0LL, .prev = 0LL, .next = -1LL};
    nodes = list_append_Node(nodes, &nodes_len, head);
    nodes = list_append_Node(nodes, &nodes_len, tail);
    return (DoubleLinkedList){.nodes = nodes, .nodes_len = nodes_len, .head = 0LL, .tail = 1LL};
}

DoubleLinkedList dll_add(DoubleLinkedList lst, long long idx) {
    Node *nodes = lst.nodes;
    size_t nodes_len = lst.nodes_len;
    long long tail_idx = lst.tail;
    Node tail_node = nodes[(int)({long long _mochi_idx = tail_idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long prev_idx = tail_node.prev;
    Node node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    node.prev = prev_idx;
    node.next = tail_idx;
    nodes[(int)(idx)] = node;
    Node prev_node = nodes[(int)({long long _mochi_idx = prev_idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    prev_node.next = idx;
    nodes[(int)(prev_idx)] = prev_node;
    tail_node.prev = idx;
    nodes[(int)(tail_idx)] = tail_node;
    lst.nodes = nodes;
    lst.nodes_len = nodes_len;
    return lst;
}

DoubleLinkedList dll_remove(DoubleLinkedList lst, long long idx) {
    Node *nodes = lst.nodes;
    size_t nodes_len = lst.nodes_len;
    Node node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    long long prev_idx = node.prev;
    long long next_idx = node.next;
    if ((prev_idx == (0LL - 1LL)) || (next_idx == (0LL - 1LL))) {
        return lst;
    }
    Node prev_node = nodes[(int)({long long _mochi_idx = prev_idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    prev_node.next = next_idx;
    nodes[(int)(prev_idx)] = prev_node;
    Node next_node = nodes[(int)({long long _mochi_idx = next_idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
    next_node.prev = prev_idx;
    nodes[(int)(next_idx)] = next_node;
    node.prev = -1LL;
    node.next = -1LL;
    nodes[(int)(idx)] = node;
    lst.nodes = nodes;
    lst.nodes_len = nodes_len;
    return lst;
}

LRUCache new_cache(long long cap) {
    static const char* empty_map_keys[16] = {};
    static int empty_map_vals[16] = {};
    size_t empty_map_len = 0;
    MapSI empty_map = { empty_map_keys, empty_map_vals, 0, 16 };
    return (LRUCache){.list = new_list(), .capacity = cap, .num_keys = 0LL, .hits = 0LL, .misses = 0LL, .cache = empty_map};
}

GetResult lru_get(LRUCache c, long long key) {
    LRUCache cache = c;
    const char* key_str = str_int(key);
    if (contains_str(cache.cache.keys, cache.cache.len, key_str)) {
        long long idx = map_get_si(cache.cache.keys, cache.cache.vals, cache.cache.len, key_str);
        if (idx != (0LL - 1LL)) {
            cache.hits = cache.hits + 1LL;
            Node node = cache.list.nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? cache.list.nodes_len + _mochi_idx : _mochi_idx;})];
            long long value = node.value;
            cache.list = dll_remove(cache.list, idx);
            cache.list = dll_add(cache.list, idx);
            return (GetResult){.cache = cache, .value = value, .ok = 1LL};
        }
    }
    cache.misses = cache.misses + 1LL;
    return (GetResult){.cache = cache, .value = 0LL, .ok = 0LL};
}

LRUCache lru_put(LRUCache c, long long key, long long value) {
    LRUCache cache = c;
    const char* key_str = str_int(key);
    if (!(contains_str(cache.cache.keys, cache.cache.len, key_str))) {
        if (cache.num_keys >= cache.capacity) {
            Node head_node = cache.list.nodes[(int)({long long _mochi_idx = cache.list.head; _mochi_idx < 0 ? cache.list.nodes_len + _mochi_idx : _mochi_idx;})];
            long long first_idx = head_node.next;
            Node first_node = cache.list.nodes[(int)({long long _mochi_idx = first_idx; _mochi_idx < 0 ? cache.list.nodes_len + _mochi_idx : _mochi_idx;})];
            long long old_key = first_node.key;
            cache.list = dll_remove(cache.list, first_idx);
            MapSI mdel = cache.cache;
            map_set_si(&mdel, str_int(old_key), -1LL);
            cache.cache = mdel;
            cache.num_keys = cache.num_keys - 1LL;
        }
        Node *nodes = cache.list.nodes;
        size_t nodes_len = cache.list.nodes_len;
        Node new_node = (Node){.key = key, .value = value, .prev = -1LL, .next = -1LL};
        nodes = list_append_Node(nodes, &nodes_len, new_node);
        long long idx = nodes_len - 1LL;
        cache.list.nodes = nodes;
        cache.list = dll_add(cache.list, idx);
        MapSI m = cache.cache;
        map_set_si(&m, key_str, idx);
        cache.cache = m;
        cache.num_keys = cache.num_keys + 1LL;
    } else {
        MapSI m = cache.cache;
        long long idx = map_get_si(m.keys, m.vals, m.len, key_str);
        Node *nodes = cache.list.nodes;
        size_t nodes_len = cache.list.nodes_len;
        Node node = nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
        node.value = value;
        nodes[(int)(idx)] = node;
        cache.list.nodes = nodes;
        cache.list = dll_remove(cache.list, idx);
        cache.list = dll_add(cache.list, idx);
        cache.cache = m;
    }
    return cache;
}

const char* cache_info(LRUCache cache) {
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("CacheInfo(hits=", str_int(cache.hits)), ", misses="), str_int(cache.misses)), ", capacity="), str_int(cache.capacity)), ", current size="), str_int(cache.num_keys)), ")");
}

void print_result(GetResult res) {
    if (res.ok) {
        puts(str_int(res.value));
    } else {
        puts("None");
    }
}

void user_main() {
    LRUCache cache = new_cache(2LL);
    cache = lru_put(cache, 1LL, 1LL);
    cache = lru_put(cache, 2LL, 2LL);
    GetResult r1 = lru_get(cache, 1LL);
    cache = r1.cache;
    print_result(r1);
    cache = lru_put(cache, 3LL, 3LL);
    GetResult r2 = lru_get(cache, 2LL);
    cache = r2.cache;
    print_result(r2);
    cache = lru_put(cache, 4LL, 4LL);
    GetResult r3 = lru_get(cache, 1LL);
    cache = r3.cache;
    print_result(r3);
    GetResult r4 = lru_get(cache, 3LL);
    cache = r4.cache;
    print_result(r4);
    GetResult r5 = lru_get(cache, 4LL);
    cache = r5.cache;
    print_result(r5);
    puts(cache_info(cache));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
