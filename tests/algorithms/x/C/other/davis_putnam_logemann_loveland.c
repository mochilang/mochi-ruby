// Generated by Mochi 0.10.32 on 2025-08-23 01:15 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_symbol_len;

size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Clause Clause;
typedef struct DPLLResult DPLLResult;
typedef struct EvalResult EvalResult;
typedef struct Formula Formula;

struct Clause {
    MapSI literals;
    const char* *names;
    size_t names_len;
};

struct DPLLResult {
    long long sat;
    MapSI model;
};

struct EvalResult {
    long long value;
    Clause clause;
};

struct Formula {
    Clause *clauses;
    size_t clauses_len;
};

Clause clause1;
Clause clause2;
Formula formula;
const char* formula_str = "";
Clause *clauses = NULL;
size_t clauses_len = 0;
const char* symbols_init[4] = {"A4", "A3", "A5", "A1"};
const char* *symbols = symbols_init;
size_t symbols_len = 4;
static const char* model_keys[16] = {};
static int model_vals[16] = {};
size_t model_len = 0;
MapSI model = { model_keys, model_vals, 0, 16 };
DPLLResult result;

Clause new_clause(const char* * lits, size_t lits_len);
Clause assign_clause(Clause c, MapSI model);
EvalResult evaluate_clause(Clause c, MapSI model);
Formula new_formula(Clause * cs, size_t cs_len);
const char* * remove_symbol(const char* * symbols, size_t symbols_len, const char* s);
DPLLResult dpll_algorithm(Clause * clauses, size_t clauses_len, const char* * symbols, size_t symbols_len, MapSI model);
const char* str_clause(Clause c);
const char* str_formula(Formula f);
int main(void);

Clause new_clause(const char* * lits, size_t lits_len) {
    static const char* m_keys[16] = {};
    static int m_vals[16] = {};
    size_t m_len = 0;
    MapSI m = { m_keys, m_vals, 0, 16 };
    const char* *names = NULL;
    size_t names_len = 0;
    long long i = 0LL;
    while (i < lits_len) {
        const char* lit = lits[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lits_len + _mochi_idx : _mochi_idx;})];
        map_set_si(&m, lit, -1LL);
        names = list_append_str(names, &names_len, lit);
        i = i + 1LL;
    }
    return (Clause){.literals = m, .names = names, .names_len = names_len};
}

Clause assign_clause(Clause c, MapSI model) {
    MapSI lits = c.literals;
    long long i = 0LL;
    while (i < c.names_len) {
        const char* lit = c.names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c.names_len + _mochi_idx : _mochi_idx;})];
        const char* symbol = _substring(lit, 0LL, 2LL);
        if (contains_str(model.keys, model.len, symbol)) {
            long long value = map_get_si(model.keys, model.vals, model.len, symbol);
            if ((strcmp(_substring(lit, strlen(lit) - 1LL, strlen(lit)), "'") == 0) && (value != (0LL - 1LL))) {
                value = 1LL - value;
            }
            map_set_si(&lits, lit, value);
        }
        i = i + 1LL;
    }
    c.literals = lits;
    return c;
}

EvalResult evaluate_clause(Clause c, MapSI model) {
    long long i = 0LL;
    while (i < c.names_len) {
        const char* lit = c.names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c.names_len + _mochi_idx : _mochi_idx;})];
        const char* sym;
        if (strcmp(_substring(lit, strlen(lit) - 1LL, strlen(lit)), "'") == 0) {
            sym = _substring(lit, 0LL, 2LL);
        } else {
            sym = str_concat(lit, "'");
        }
        if (contains_str(c.literals.keys, c.literals.len, sym)) {
            return (EvalResult){.value = 1LL, .clause = c};
        }
        i = i + 1LL;
    }
    c = assign_clause(c, model);
    i = 0LL;
    while (i < c.names_len) {
        const char* lit = c.names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c.names_len + _mochi_idx : _mochi_idx;})];
        long long value = map_get_si(c.literals.keys, c.literals.vals, c.literals.len, lit);
        if (value == 1LL) {
            return (EvalResult){.value = 1LL, .clause = c};
        }
        if (value == (0LL - 1LL)) {
            return (EvalResult){.value = -1LL, .clause = c};
        }
        i = i + 1LL;
    }
    long long any_true = 0LL;
    i = 0LL;
    while (i < c.names_len) {
        const char* lit = c.names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c.names_len + _mochi_idx : _mochi_idx;})];
        if (map_get_si(c.literals.keys, c.literals.vals, c.literals.len, lit) == 1LL) {
            any_true = 1LL;
        }
        i = i + 1LL;
    }
    return (EvalResult){.value = any_true, .clause = c};
}

Formula new_formula(Clause * cs, size_t cs_len) {
    return (Formula){.clauses = cs, .clauses_len = cs_len};
}

const char* * remove_symbol(const char* * symbols, size_t symbols_len, const char* s) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < symbols_len) {
        if (strcmp(symbols[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? symbols_len + _mochi_idx : _mochi_idx;})], s) != 0) {
            res = list_append_str(res, &res_len, symbols[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? symbols_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_symbol_len = res_len, res;
}

DPLLResult dpll_algorithm(Clause * clauses, size_t clauses_len, const char* * symbols, size_t symbols_len, MapSI model) {
    long long all_true = 1LL;
    long long i = 0LL;
    while (i < clauses_len) {
        EvalResult ev = evaluate_clause(clauses[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? clauses_len + _mochi_idx : _mochi_idx;})], model);
        clauses[(int)(i)] = ev.clause;
        if (ev.value == 0LL) {
            return (DPLLResult){.sat = 0LL, .model = (MapSI){0}};
        } else {
            if (ev.value == (0LL - 1LL)) {
                all_true = 0LL;
            }
        }
        i = i + 1LL;
    }
    if (all_true) {
        return (DPLLResult){.sat = 1LL, .model = model};
    }
    const char* p = symbols[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? symbols_len + _mochi_idx : _mochi_idx;})];
    const char* *rest = remove_symbol(symbols, symbols_len, p);
    size_t rest_len = remove_symbol_len;
    MapSI tmp1 = model;
    MapSI tmp2 = model;
    map_set_si(&tmp1, p, 1LL);
    map_set_si(&tmp2, p, 0LL);
    DPLLResult res1 = dpll_algorithm(clauses, clauses_len, rest, rest_len, tmp1);
    if (res1.sat) {
        return res1;
    }
    return dpll_algorithm(clauses, clauses_len, rest, rest_len, tmp2);
}

const char* str_clause(Clause c) {
    const char* line = "{";
    long long first = 1LL;
    long long i = 0LL;
    while (i < c.names_len) {
        const char* lit = c.names[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? c.names_len + _mochi_idx : _mochi_idx;})];
        if (first) {
            first = 0LL;
        } else {
            line = str_concat(line, " , ");
        }
        line = str_concat(line, lit);
        i = i + 1LL;
    }
    line = str_concat(line, "}");
    return line;
}

const char* str_formula(Formula f) {
    const char* line = "{";
    long long i = 0LL;
    while (i < f.clauses_len) {
        line = str_concat(line, str_clause(f.clauses[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? f.clauses_len + _mochi_idx : _mochi_idx;})]));
        if (i < (f.clauses_len - 1LL)) {
            line = str_concat(line, " , ");
        }
        i = i + 1LL;
    }
    line = str_concat(line, "}");
    return line;
}

int main(void) {
    {
        long long __start = _now();
        clause1 = new_clause((const char*[]){"A4", "A3", "A5'", "A1", "A3'"}, 5);
        clause2 = new_clause((const char*[]){"A4"}, 1);
        formula = new_formula(({Clause *tmp = malloc(2 * sizeof(Clause)); tmp[0] = clause1; tmp[1] = clause2; tmp;}), 2);
        formula_str = str_formula(formula);
        clauses = ({Clause *tmp = malloc(2 * sizeof(Clause)); tmp[0] = clause1; tmp[1] = clause2; tmp;});
        clauses_len = 2;
        result = dpll_algorithm(clauses, clauses_len, symbols, symbols_len, model);
        if (result.sat) {
            puts(str_concat(str_concat("The formula ", formula_str), " is satisfiable."));
        } else {
            puts(str_concat(str_concat("The formula ", formula_str), " is not satisfiable."));
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
