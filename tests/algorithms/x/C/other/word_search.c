// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t shuffle_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct WordSearch WordSearch;

struct WordSearch {
    const char* *words;
    size_t words_len;
    long long width;
    long long height;
    const char* **board;
    size_t board_len;
    size_t *board_lens;
    size_t board_lens_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long seed = 123456789LL;

long long user_rand();
long long rand_range(long long max);
long long * shuffle(long long * list_int, size_t list_int_len);
const char* rand_letter();
WordSearch make_word_search(const char* * words, size_t words_len, long long width, long long height);
long long insert_dir(WordSearch ws, const char* word, long long dr, long long dc, long long * rows, size_t rows_len, long long * cols, size_t cols_len);
void generate_board(WordSearch ws);
const char* visualise(WordSearch ws, long long add_fake_chars);
void user_main();
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed;
}

long long rand_range(long long max) {
    return user_rand() % max;
}

long long * shuffle(long long * list_int, size_t list_int_len) {
    long long i = list_int_len - 1LL;
    while (i > 0LL) {
        long long j = rand_range(i + 1LL);
        long long tmp = list_int[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? list_int_len + _mochi_idx : _mochi_idx;})];
        list_int[(int)(i)] = list_int[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? list_int_len + _mochi_idx : _mochi_idx;})];
        list_int[(int)(j)] = tmp;
        i = i - 1LL;
    }
    return shuffle_len = list_int_len, list_int;
}

const char* rand_letter() {
    const char* letters = "abcdefghijklmnopqrstuvwxyz";
    long long i = rand_range(26LL);
    return _substring(letters, i, i + 1LL);
}

WordSearch make_word_search(const char* * words, size_t words_len, long long width, long long height) {
    const char* **board = NULL;
    size_t board_len = 0;
    size_t *board_lens = NULL;
    size_t board_lens_len = 0;
    long long r = 0LL;
    while (r < height) {
        const char* *row = NULL;
        size_t row_len = 0;
        long long c = 0LL;
        while (c < width) {
            row = list_append_str(row, &row_len, "");
            c = c + 1LL;
        }
        board = list_append_strptr(board, &board_len, row);
        board_lens = list_append_szt(board_lens, &board_lens_len, row_len);
        r = r + 1LL;
    }
    return (WordSearch){.words = words, .words_len = words_len, .width = width, .height = height, .board = board, .board_len = board_len, .board_lens = board_lens, .board_lens_len = board_len};
}

long long insert_dir(WordSearch ws, const char* word, long long dr, long long dc, long long * rows, size_t rows_len, long long * cols, size_t cols_len) {
    long long word_len = strlen(word);
    long long ri = 0LL;
    while (ri < rows_len) {
        long long row = rows[(int)({long long _mochi_idx = ri; _mochi_idx < 0 ? rows_len + _mochi_idx : _mochi_idx;})];
        long long ci = 0LL;
        while (ci < cols_len) {
            long long col = cols[(int)({long long _mochi_idx = ci; _mochi_idx < 0 ? cols_len + _mochi_idx : _mochi_idx;})];
            long long end_r = row + (dr * (word_len - 1LL));
            long long end_c = col + (dc * (word_len - 1LL));
            if ((((end_r < 0LL) || (end_r >= ws.height)) || (end_c < 0LL)) || (end_c >= ws.width)) {
                ci = ci + 1LL;
                continue;
            }
            long long k = 0LL;
            long long ok = 1LL;
            while (k < word_len) {
                long long rr = row + (dr * k);
                long long cc = col + (dc * k);
                if (strcmp(ws.board[(int)({long long _mochi_idx = rr; _mochi_idx < 0 ? ws.board_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = cc; _mochi_idx < 0 ? ws.board_lens[rr] + _mochi_idx : _mochi_idx;})], "") != 0) {
                    ok = 0LL;
                    break;
                }
                k = k + 1LL;
            }
            if (ok) {
                k = 0LL;
                while (k < word_len) {
                    long long rr2 = row + (dr * k);
                    long long cc2 = col + (dc * k);
                    const char* *row_list = ws.board[(int)({long long _mochi_idx = rr2; _mochi_idx < 0 ? ws.board_len + _mochi_idx : _mochi_idx;})];
                    size_t row_list_len = ws.board_lens[rr2];
                    row_list[(int)(cc2)] = _substring(word, k, k + 1LL);
                    k = k + 1LL;
                }
                return 1LL;
            }
            ci = ci + 1LL;
        }
        ri = ri + 1LL;
    }
    return 0LL;
}

void generate_board(WordSearch ws) {
    double *dirs_r = NULL;
    size_t dirs_r_len = 0;
    dirs_r = list_append_double(dirs_r, &dirs_r_len, -1.0);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, -1.0);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, 0LL);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, 1LL);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, 1LL);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, 1LL);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, 0LL);
    dirs_r = list_append_double(dirs_r, &dirs_r_len, -1.0);
    long long *dirs_c = NULL;
    size_t dirs_c_len = 0;
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, 0LL);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, 1LL);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, 1LL);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, 1LL);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, 0LL);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, -1.0);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, -1.0);
    dirs_c = list_append_long_long(dirs_c, &dirs_c_len, -1.0);
    long long i = 0LL;
    while (i < ws.words_len) {
        const char* word = ws.words[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ws.words_len + _mochi_idx : _mochi_idx;})];
        long long *rows = NULL;
        size_t rows_len = 0;
        long long r = 0LL;
        while (r < ws.height) {
            rows = list_append_long_long(rows, &rows_len, r);
            r = r + 1LL;
        }
        long long *cols = NULL;
        size_t cols_len = 0;
        long long c = 0LL;
        while (c < ws.width) {
            cols = list_append_long_long(cols, &cols_len, c);
            c = c + 1LL;
        }
        rows = shuffle(rows, rows_len);
        rows_len = shuffle_len;
        cols = shuffle(cols, cols_len);
        cols_len = shuffle_len;
        long long d = rand_range(8LL);
        insert_dir(ws, word, dirs_r[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? dirs_r_len + _mochi_idx : _mochi_idx;})], dirs_c[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? dirs_c_len + _mochi_idx : _mochi_idx;})], rows, rows_len, cols, cols_len);
        i = i + 1LL;
    }
}

const char* visualise(WordSearch ws, long long add_fake_chars) {
    const char* result = "";
    long long r = 0LL;
    while (r < ws.height) {
        long long c = 0LL;
        while (c < ws.width) {
            const char* ch = ws.board[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? ws.board_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = c; _mochi_idx < 0 ? ws.board_lens[r] + _mochi_idx : _mochi_idx;})];
            if (strcmp(ch, "") == 0) {
                if (add_fake_chars) {
                    ch = rand_letter();
                } else {
                    ch = "#";
                }
            }
            result = str_concat(str_concat(result, ch), " ");
            c = c + 1LL;
        }
        result = str_concat(result, "\n");
        r = r + 1LL;
    }
    return result;
}

void user_main() {
    const char* *words = NULL;
    size_t words_len = 0;
    words = list_append_str(words, &words_len, "cat");
    words = list_append_str(words, &words_len, "dog");
    words = list_append_str(words, &words_len, "snake");
    words = list_append_str(words, &words_len, "fish");
    WordSearch ws = make_word_search(words, words_len, 10LL, 10LL);
    generate_board(ws);
    puts(visualise(ws, 1LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
