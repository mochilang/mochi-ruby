// Generated by Mochi 0.10.32 on 2025-08-23 13:39 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t get_data_len;
size_t *get_data_lens;
size_t calculate_each_score_len;
size_t *calculate_each_score_lens;
size_t generate_final_scores_len;
size_t procentual_proximity_len;
size_t *procentual_proximity_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double* list_append_double_new(const double *arr, size_t len, double val) {
    double *res = malloc((len + 1) * sizeof(double));
    if (arr && len) memcpy(res, arr, len * sizeof(double));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double **vehicles = NULL;
size_t vehicles_len = 0;
size_t *vehicles_lens = NULL;
size_t vehicles_lens_len = 0;
long long weights_init[3] = {0LL, 0LL, 1LL};
long long *weights = weights_init;
size_t weights_len = 3;
double **result = NULL;
size_t result_len = 0;
size_t *result_lens = NULL;
size_t result_lens_len = 0;

double * * get_data(double * * source_data, size_t source_data_len, size_t* source_data_lens, size_t source_data_lens_len);
double * * calculate_each_score(double * * data_lists, size_t data_lists_len, size_t* data_lists_lens, size_t data_lists_lens_len, long long * weights, size_t weights_len);
double * generate_final_scores(double * * score_lists, size_t score_lists_len, size_t* score_lists_lens, size_t score_lists_lens_len);
double * * procentual_proximity(double * * source_data, size_t source_data_len, size_t* source_data_lens, size_t source_data_lens_len, long long * weights, size_t weights_len);
int main(void);

double * * get_data(double * * source_data, size_t source_data_len, size_t* source_data_lens, size_t source_data_lens_len) {
    double **data_lists = NULL;
    size_t data_lists_len = 0;
    size_t *data_lists_lens = NULL;
    size_t data_lists_lens_len = 0;
    long long i = 0LL;
    while (i < source_data_len) {
        double *row = source_data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? source_data_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = source_data_lens[(int)(i)];
        long long j = 0LL;
        while (j < row_len) {
            if (data_lists_len < (j + 1LL)) {
                double *empty = NULL;
                size_t empty_len = 0;
                data_lists = list_append_doubleptr(data_lists, &data_lists_len, empty);
                data_lists_lens = list_append_szt(data_lists_lens, &data_lists_lens_len, empty_len);
            }
            data_lists[(int)(j)] = list_append_double_new(data_lists[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? data_lists_len + _mochi_idx : _mochi_idx;})], data_lists_lens[(int)(j)], row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return get_data_lens = data_lists_lens, get_data_len = data_lists_len, data_lists;
}

double * * calculate_each_score(double * * data_lists, size_t data_lists_len, size_t* data_lists_lens, size_t data_lists_lens_len, long long * weights, size_t weights_len) {
    double **score_lists = NULL;
    size_t score_lists_len = 0;
    size_t *score_lists_lens = NULL;
    size_t score_lists_lens_len = 0;
    long long i = 0LL;
    while (i < data_lists_len) {
        double *dlist = data_lists[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_lists_len + _mochi_idx : _mochi_idx;})];
        size_t dlist_len = data_lists_lens[(int)(i)];
        long long weight = weights[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? weights_len + _mochi_idx : _mochi_idx;})];
        double mind = dlist[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dlist_len + _mochi_idx : _mochi_idx;})];
        double maxd = dlist[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dlist_len + _mochi_idx : _mochi_idx;})];
        long long j = 1LL;
        while (j < dlist_len) {
            double val = dlist[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dlist_len + _mochi_idx : _mochi_idx;})];
            if (val < mind) {
                mind = val;
            }
            if (val > maxd) {
                maxd = val;
            }
            j = j + 1LL;
        }
        double *score = NULL;
        size_t score_len = 0;
        j = 0LL;
        if (weight == 0LL) {
            while (j < dlist_len) {
                double item = dlist[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dlist_len + _mochi_idx : _mochi_idx;})];
                if ((maxd - mind) == 0.0) {
                    score = list_append_double(score, &score_len, 1.0);
                } else {
                    score = list_append_double(score, &score_len, 1.0 - ((item - mind) / (maxd - mind)));
                }
                j = j + 1LL;
            }
        } else {
            while (j < dlist_len) {
                double item = dlist[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dlist_len + _mochi_idx : _mochi_idx;})];
                if ((maxd - mind) == 0.0) {
                    score = list_append_double(score, &score_len, 0.0);
                } else {
                    score = list_append_double(score, &score_len, (item - mind) / (maxd - mind));
                }
                j = j + 1LL;
            }
        }
        score_lists = list_append_doubleptr(score_lists, &score_lists_len, score);
        score_lists_lens = list_append_szt(score_lists_lens, &score_lists_lens_len, score_len);
        i = i + 1LL;
    }
    return calculate_each_score_lens = score_lists_lens, calculate_each_score_len = score_lists_len, score_lists;
}

double * generate_final_scores(double * * score_lists, size_t score_lists_len, size_t* score_lists_lens, size_t score_lists_lens_len) {
    long long count_ = score_lists_lens[(int)(0LL)];
    double *final_scores = NULL;
    size_t final_scores_len = 0;
    long long i = 0LL;
    while (i < count_) {
        final_scores = list_append_double(final_scores, &final_scores_len, 0.0);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < score_lists_len) {
        double *slist = score_lists[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? score_lists_len + _mochi_idx : _mochi_idx;})];
        size_t slist_len = score_lists_lens[(int)(i)];
        long long j = 0LL;
        while (j < slist_len) {
            final_scores[(int)(j)] = final_scores[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? final_scores_len + _mochi_idx : _mochi_idx;})] + slist[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? slist_len + _mochi_idx : _mochi_idx;})];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return generate_final_scores_len = final_scores_len, final_scores;
}

double * * procentual_proximity(double * * source_data, size_t source_data_len, size_t* source_data_lens, size_t source_data_lens_len, long long * weights, size_t weights_len) {
    double **data_lists = get_data(source_data, source_data_len, source_data_lens, source_data_len);
    size_t data_lists_len = get_data_len;
    size_t *data_lists_lens = get_data_lens;
    size_t data_lists_lens_len = get_data_len;
    double **score_lists = calculate_each_score(data_lists, data_lists_len, data_lists_lens, data_lists_len, weights, weights_len);
    size_t score_lists_len = calculate_each_score_len;
    size_t *score_lists_lens = calculate_each_score_lens;
    size_t score_lists_lens_len = calculate_each_score_len;
    double *final_scores = generate_final_scores(score_lists, score_lists_len, score_lists_lens, score_lists_len);
    size_t final_scores_len = generate_final_scores_len;
    long long i = 0LL;
    while (i < final_scores_len) {
        source_data[(int)(i)] = list_append_double_new(source_data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? source_data_len + _mochi_idx : _mochi_idx;})], source_data_lens[(int)(i)], final_scores[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? final_scores_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return procentual_proximity_lens = source_data_lens, procentual_proximity_len = source_data_len, source_data;
}

int main(void) {
    {
        long long __start = _now();
        vehicles = list_append_doubleptr(vehicles, &vehicles_len, ({double* tmp = malloc(3 * sizeof(double)); tmp[0] = 20.0; tmp[1] = 60.0; tmp[2] = 2012.0; tmp;}));
        vehicles_lens = list_append_szt(vehicles_lens, &vehicles_lens_len, 3);
        vehicles = list_append_doubleptr(vehicles, &vehicles_len, ({double* tmp = malloc(3 * sizeof(double)); tmp[0] = 23.0; tmp[1] = 90.0; tmp[2] = 2015.0; tmp;}));
        vehicles_lens = list_append_szt(vehicles_lens, &vehicles_lens_len, 3);
        vehicles = list_append_doubleptr(vehicles, &vehicles_len, ({double* tmp = malloc(3 * sizeof(double)); tmp[0] = 22.0; tmp[1] = 50.0; tmp[2] = 2011.0; tmp;}));
        vehicles_lens = list_append_szt(vehicles_lens, &vehicles_lens_len, 3);
        result = procentual_proximity(vehicles, vehicles_len, vehicles_lens, vehicles_len, weights, weights_len);
        result_len = procentual_proximity_len;
        result_lens = procentual_proximity_lens;
        puts(str_list_list_double(result, result_len, result_lens));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
