// Generated by Mochi 0.10.32 on 2025-08-26 00:42 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t alternative_list_arrange_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Anon1 Anon1;
typedef struct Int Int;
typedef struct Str Str;

struct Anon1 {
    const char* __name;
    const char* value;
};

struct Int {
    long long value;
};

struct Str {
    const char* value;
};

const char* *example1 = NULL;
size_t example1_len = 0;
const char* *example2 = NULL;
size_t example2_len = 0;
const char* *example3 = NULL;
size_t example3_len = 0;
const char* *example4 = NULL;
size_t example4_len = 0;

const char* from_int(long long x);
const char* from_string(const char* s);
const char* item_to_string(const char* it);
const char* * alternative_list_arrange(const char* * first, size_t first_len, const char* * second, size_t second_len);
const char* list_to_string(const char* * xs, size_t xs_len);
int main(void);

const char* from_int(long long x) {
    return str_int(x);
}

const char* from_string(const char* s) {
    return s;
}

const char* item_to_string(const char* it) {
    return it;
}

const char* * alternative_list_arrange(const char* * first, size_t first_len, const char* * second, size_t second_len) {
    long long len1 = first_len;
    long long len2 = second_len;
    long long abs_len;
    if (len1 > len2) {
        abs_len = len1;
    } else {
        abs_len = len2;
    }
    const char* *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < abs_len) {
        if (i < len1) {
            result = list_append_str(result, &result_len, first[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? first_len + _mochi_idx : _mochi_idx;})]);
        }
        if (i < len2) {
            result = list_append_str(result, &result_len, second[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? second_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return alternative_list_arrange_len = result_len, result;
}

const char* list_to_string(const char* * xs, size_t xs_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < xs_len) {
        s = str_concat(s, item_to_string(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        if (i < (xs_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

int main(void) {
    {
        long long __start = _now();
        example1 = alternative_list_arrange(({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = from_int(1LL); tmp[1] = from_int(2LL); tmp[2] = from_int(3LL); tmp[3] = from_int(4LL); tmp[4] = from_int(5LL); tmp;}), 5, ({const char* *tmp = malloc(3 * sizeof(const char*)); tmp[0] = from_string("A"); tmp[1] = from_string("B"); tmp[2] = from_string("C"); tmp;}), 3);
        example1_len = alternative_list_arrange_len;
        puts(list_to_string(example1, example1_len));
        example2 = alternative_list_arrange(({const char* *tmp = malloc(3 * sizeof(const char*)); tmp[0] = from_string("A"); tmp[1] = from_string("B"); tmp[2] = from_string("C"); tmp;}), 3, ({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = from_int(1LL); tmp[1] = from_int(2LL); tmp[2] = from_int(3LL); tmp[3] = from_int(4LL); tmp[4] = from_int(5LL); tmp;}), 5);
        example2_len = alternative_list_arrange_len;
        puts(list_to_string(example2, example2_len));
        example3 = alternative_list_arrange(({const char* *tmp = malloc(3 * sizeof(const char*)); tmp[0] = from_string("X"); tmp[1] = from_string("Y"); tmp[2] = from_string("Z"); tmp;}), 3, ({const char* *tmp = malloc(4 * sizeof(const char*)); tmp[0] = from_int(9LL); tmp[1] = from_int(8LL); tmp[2] = from_int(7LL); tmp[3] = from_int(6LL); tmp;}), 4);
        example3_len = alternative_list_arrange_len;
        puts(list_to_string(example3, example3_len));
        example4 = alternative_list_arrange(({const char* *tmp = malloc(5 * sizeof(const char*)); tmp[0] = from_int(1LL); tmp[1] = from_int(2LL); tmp[2] = from_int(3LL); tmp[3] = from_int(4LL); tmp[4] = from_int(5LL); tmp;}), 5, NULL, 0);
        example4_len = alternative_list_arrange_len;
        puts(list_to_string(example4, example4_len));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
