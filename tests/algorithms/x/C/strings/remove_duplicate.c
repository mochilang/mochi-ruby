// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_ws_len;
size_t unique_len;
size_t insertion_sort_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* * split_ws(const char* s);
long long contains(const char* * xs, size_t xs_len, const char* x);
const char* * unique(const char* * xs, size_t xs_len);
const char* * insertion_sort(const char* * arr, size_t arr_len);
const char* join_with_space(const char* * xs, size_t xs_len);
const char* remove_duplicates(const char* sentence);
int main(void);

const char* * split_ws(const char* s) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* word = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (strcmp(ch, " ") == 0) {
            if (strcmp(word, "") != 0) {
                res = list_append_str(res, &res_len, word);
                word = "";
            }
        } else {
            word = str_concat(word, ch);
        }
        i = i + 1LL;
    }
    if (strcmp(word, "") != 0) {
        res = list_append_str(res, &res_len, word);
    }
    return split_ws_len = res_len, res;
}

long long contains(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * unique(const char* * xs, size_t xs_len) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        const char* w = xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})];
        if (!(contains(res, res_len, w))) {
            res = list_append_str(res, &res_len, w);
        }
        i = i + 1LL;
    }
    return unique_len = res_len, res;
}

const char* * insertion_sort(const char* * arr, size_t arr_len) {
    const char* *a = arr;
    size_t a_len = arr_len;
    long long i = 1LL;
    while (i < a_len) {
        const char* key = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while ((j >= 0LL) && (strcmp(a[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], key) > 0)) {
            a[(int)(j + 1LL)] = a[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
            j = j - 1LL;
        }
        a[(int)(j + 1LL)] = key;
        i = i + 1LL;
    }
    return insertion_sort_len = a_len, a;
}

const char* join_with_space(const char* * xs, size_t xs_len) {
    const char* s = "";
    long long i = 0LL;
    while (i < xs_len) {
        if (i > 0LL) {
            s = str_concat(s, " ");
        }
        s = str_concat(s, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return s;
}

const char* remove_duplicates(const char* sentence) {
    const char* *words = split_ws(sentence);
    size_t words_len = split_ws_len;
    const char* *uniq = unique(words, words_len);
    size_t uniq_len = unique_len;
    const char* *sorted_words = insertion_sort(uniq, uniq_len);
    size_t sorted_words_len = insertion_sort_len;
    return join_with_space(sorted_words, sorted_words_len);
}

int main(void) {
    {
        long long __start = _now();
        puts(remove_duplicates("Python is great and Java is also great"));
        puts(remove_duplicates("Python   is      great and Java is also great"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
