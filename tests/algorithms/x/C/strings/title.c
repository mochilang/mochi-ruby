// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_words_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* lower = "abcdefghijklmnopqrstuvwxyz";
const char* upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

long long index_of(const char* s, const char* ch);
const char* to_title_case(const char* word);
const char* * split_words(const char* s);
const char* sentence_to_title_case(const char* sentence);
int main(void);

long long index_of(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp((const char[]){s[i], 0}, ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* to_title_case(const char* word) {
    if (strlen(word) == 0LL) {
        return "";
    }
    const char* first = _substring(word, 0LL, 1LL);
    long long idx = index_of(lower, first);
    const char* result;
    if (idx >= 0LL) {
        result = _substring(upper, idx, idx + 1LL);
    } else {
        result = first;
    }
    long long i = 1LL;
    while (i < strlen(word)) {
        const char* ch = _substring(word, i, i + 1LL);
        long long uidx = index_of(upper, ch);
        if (uidx >= 0LL) {
            result = str_concat(result, _substring(lower, uidx, uidx + 1LL));
        } else {
            result = str_concat(result, ch);
        }
        i = i + 1LL;
    }
    return result;
}

const char* * split_words(const char* s) {
    const char* *words = NULL;
    size_t words_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        if (strcmp(ch, " ") == 0) {
            if (strlen(current) > 0LL) {
                words = list_append_str(words, &words_len, current);
                current = "";
            }
        } else {
            current = str_concat(current, ch);
        }
        i = i + 1LL;
    }
    if (strlen(current) > 0LL) {
        words = list_append_str(words, &words_len, current);
    }
    return split_words_len = words_len, words;
}

const char* sentence_to_title_case(const char* sentence) {
    const char* *words = split_words(sentence);
    size_t words_len = split_words_len;
    const char* res = "";
    long long i = 0LL;
    while (i < words_len) {
        res = str_concat(res, to_title_case(words[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? words_len + _mochi_idx : _mochi_idx;})]));
        if ((i + 1LL) < words_len) {
            res = str_concat(res, " ");
        }
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        puts(to_title_case("Aakash"));
        puts(to_title_case("aakash"));
        puts(to_title_case("AAKASH"));
        puts(to_title_case("aAkAsH"));
        puts(sentence_to_title_case("Aakash Giri"));
        puts(sentence_to_title_case("aakash giri"));
        puts(sentence_to_title_case("AAKASH GIRI"));
        puts(sentence_to_title_case("aAkAsH gIrI"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
