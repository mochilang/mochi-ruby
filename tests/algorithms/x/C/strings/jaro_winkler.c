// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t repeat_bool_len;
size_t set_bool_len;

size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long min_int(long long a, long long b);
long long max_int(long long a, long long b);
long long * repeat_bool(long long n, long long value);
long long * set_bool(long long * xs, size_t xs_len, long long idx, long long value);
double jaro_winkler(const char* s1, const char* s2);
int main(void);

long long min_int(long long a, long long b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}

long long max_int(long long a, long long b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

long long * repeat_bool(long long n, long long value) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        res = list_append_long_long(res, &res_len, value);
        i = i + 1LL;
    }
    return repeat_bool_len = res_len, res;
}

long long * set_bool(long long * xs, size_t xs_len, long long idx, long long value) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < xs_len) {
        if (i == idx) {
            res = list_append_long_long(res, &res_len, value);
        } else {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return set_bool_len = res_len, res;
}

double jaro_winkler(const char* s1, const char* s2) {
    long long len1 = strlen(s1);
    long long len2 = strlen(s2);
    long long limit = min_int(len1, len2) / 2LL;
    long long *match1 = repeat_bool(len1, 0LL);
    size_t match1_len = repeat_bool_len;
    long long *match2 = repeat_bool(len2, 0LL);
    size_t match2_len = repeat_bool_len;
    long long matches = 0LL;
    long long i = 0LL;
    while (i < len1) {
        long long start = max_int(0LL, i - limit);
        long long end = min_int((i + limit) + 1LL, len2);
        long long j = start;
        while (j < end) {
            if (!(match2[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? match2_len + _mochi_idx : _mochi_idx;})]) && (strcmp(_substring(s1, i, i + 1LL), _substring(s2, j, j + 1LL)) == 0)) {
                match1 = set_bool(match1, match1_len, i, 1LL);
                match1_len = set_bool_len;
                match2 = set_bool(match2, match2_len, j, 1LL);
                match2_len = set_bool_len;
                matches = matches + 1LL;
                break;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    if (matches == 0LL) {
        return 0.0;
    }
    long long transpositions = 0LL;
    long long k = 0LL;
    i = 0LL;
    while (i < len1) {
        if (match1[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? match1_len + _mochi_idx : _mochi_idx;})]) {
            while (!(match2[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? match2_len + _mochi_idx : _mochi_idx;})])) {
                k = k + 1LL;
            }
            if (strcmp(_substring(s1, i, i + 1LL), _substring(s2, k, k + 1LL)) != 0) {
                transpositions = transpositions + 1LL;
            }
            k = k + 1LL;
        }
        i = i + 1LL;
    }
    double m = (double)(matches);
    double jaro = (((m / (double)(len1)) + (m / (double)(len2))) + ((m - ((double)(transpositions) / 2.0)) / m)) / 3.0;
    long long prefix_len = 0LL;
    i = 0LL;
    while (((i < 4LL) && (i < len1)) && (i < len2)) {
        if (strcmp(_substring(s1, i, i + 1LL), _substring(s2, i, i + 1LL)) == 0) {
            prefix_len = prefix_len + 1LL;
        } else {
            break;
        }
        i = i + 1LL;
    }
    return jaro + ((0.1 * (double)(prefix_len)) * (1.0 - jaro));
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(jaro_winkler("hello", "world")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
