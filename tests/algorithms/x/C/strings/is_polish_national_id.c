// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long parse_int(const char* s);
long long is_polish_national_id(const char* id);
int main(void);

long long parse_int(const char* s) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        value = (value * 10LL) + _atoi(c);
        i = i + 1LL;
    }
    return value;
}

long long is_polish_national_id(const char* id) {
    if (strlen(id) == 0LL) {
        return 0LL;
    }
    if (strcmp(_substring(id, 0LL, 1LL), "-") == 0) {
        return 0LL;
    }
    long long input_int = parse_int(id);
    if ((input_int < 10100000LL) || (input_int > 99923199999LL)) {
        return 0LL;
    }
    long long month = parse_int(_substring(id, 2LL, 4LL));
    if (!((((((month >= 1LL) && (month <= 12LL)) || ((month >= 21LL) && (month <= 32LL))) || ((month >= 41LL) && (month <= 52LL))) || ((month >= 61LL) && (month <= 72LL))) || ((month >= 81LL) && (month <= 92LL)))) {
        return 0LL;
    }
    long long day = parse_int(_substring(id, 4LL, 6LL));
    if ((day < 1LL) || (day > 31LL)) {
        return 0LL;
    }
    long long *multipliers = NULL;
    size_t multipliers_len = 0;
    multipliers = list_append_long_long(multipliers, &multipliers_len, 1LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 3LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 7LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 9LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 1LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 3LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 7LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 9LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 1LL);
    multipliers = list_append_long_long(multipliers, &multipliers_len, 3LL);
    long long subtotal = 0LL;
    long long i = 0LL;
    while (i < 10LL) {
        long long digit = parse_int(_substring(id, i, i + 1LL));
        subtotal = subtotal + ((digit * multipliers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? multipliers_len + _mochi_idx : _mochi_idx;})]) % 10LL);
        i = i + 1LL;
    }
    long long checksum = 10LL - (subtotal % 10LL);
    return checksum == (input_int % 10LL);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(is_polish_national_id("02070803628")));
        puts(str_int(is_polish_national_id("02150803629")));
        puts(str_int(is_polish_national_id("02075503622")));
        puts(str_int(is_polish_national_id("-99012212349")));
        puts(str_int(is_polish_national_id("990122123499999")));
        puts(str_int(is_polish_national_id("02070803621")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
