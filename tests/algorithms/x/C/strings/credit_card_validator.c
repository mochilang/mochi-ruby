// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static int _char_at(const char *s, int idx) {
    int len = (int)strlen(s);
    if (idx < 0 || idx >= len) return 0;
    return (unsigned char)s[idx];
}

long long validate_initial_digits(const char* cc);
long long luhn_validation(const char* cc);
long long is_digit_string(const char* s);
long long validate_credit_card_number(const char* cc);
void user_main();
int main(void);

long long validate_initial_digits(const char* cc) {
    return (((((strcmp(_substring(cc, 0LL, 2LL), "34") == 0) || (strcmp(_substring(cc, 0LL, 2LL), "35") == 0)) || (strcmp(_substring(cc, 0LL, 2LL), "37") == 0)) || (strcmp(_substring(cc, 0LL, 1LL), "4") == 0)) || (strcmp(_substring(cc, 0LL, 1LL), "5") == 0)) || (strcmp(_substring(cc, 0LL, 1LL), "6") == 0);
}

long long luhn_validation(const char* cc) {
    long long sum = 0LL;
    long long double_digit = 0LL;
    long long i = strlen(cc) - 1LL;
    while (i >= 0LL) {
        long long n = (int)(_char_at(_substring(cc, i, i + 1LL), i));
        if (double_digit) {
            n = n * 2LL;
            if (n > 9LL) {
                n = n - 9LL;
            }
        }
        sum = sum + n;
        double_digit = !(double_digit);
        i = i - 1LL;
    }
    return (sum % 10LL) == 0LL;
}

long long is_digit_string(const char* s) {
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        if ((strcmp(c, "0") < 0) || (strcmp(c, "9") > 0)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long validate_credit_card_number(const char* cc) {
    const char* error_message = str_concat(cc, " is an invalid credit card number because");
    if (!(is_digit_string(cc))) {
        puts(str_concat(error_message, " it has nonnumerical characters."));
        return 0LL;
    }
    if (!((strlen(cc) >= 13LL) && (strlen(cc) <= 16LL))) {
        puts(str_concat(error_message, " of its length."));
        return 0LL;
    }
    if (!(validate_initial_digits(cc))) {
        puts(str_concat(error_message, " of its first two digits."));
        return 0LL;
    }
    if (!(luhn_validation(cc))) {
        puts(str_concat(error_message, " it fails the Luhn check."));
        return 0LL;
    }
    puts(str_concat(cc, " is a valid credit card number."));
    return 1LL;
}

void user_main() {
    validate_credit_card_number("4111111111111111");
    validate_credit_card_number("32323");
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
