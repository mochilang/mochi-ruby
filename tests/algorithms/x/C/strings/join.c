// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* join(const char* separator, const char* * separated, size_t separated_len);
void user_main();
int main(void);

const char* join(const char* separator, const char* * separated, size_t separated_len) {
    const char* joined = "";
    long long last_index = separated_len - 1LL;
    long long i = 0LL;
    while (i < separated_len) {
        joined = str_concat(joined, separated[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? separated_len + _mochi_idx : _mochi_idx;})]);
        if (i < last_index) {
            joined = str_concat(joined, separator);
        }
        i = i + 1LL;
    }
    return joined;
}

void user_main() {
    puts(join("", (const char*[]){"a", "b", "c", "d"}, 4));
    puts(join("#", (const char*[]){"a", "b", "c", "d"}, 4));
    puts(join("#", (const char*[]){"a"}, 1));
    puts(join(" ", (const char*[]){"You", "are", "amazing!"}, 3));
    puts(join(",", (const char*[]){"", "", ""}, 3));
    puts(join("-", (const char*[]){"apple", "banana", "cherry"}, 3));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
