// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prefix_function_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * prefix_function(const char* s);
long long longest_prefix(const char* s);
long long list_eq_int(long long * a, size_t a_len, long long * b, size_t b_len);
void test_prefix_function();
void test_longest_prefix();
void user_main();
int main(void);

long long * prefix_function(const char* s) {
    long long *pi = NULL;
    size_t pi_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        pi = list_append_long_long(pi, &pi_len, 0LL);
        i = i + 1LL;
    }
    i = 1LL;
    while (i < strlen(s)) {
        long long j = pi[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? pi_len + _mochi_idx : _mochi_idx;})];
        while ((j > 0LL) && (strcmp((const char[]){s[i], 0}, (const char[]){s[j], 0}) != 0)) {
            j = pi[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? pi_len + _mochi_idx : _mochi_idx;})];
        }
        if (strcmp((const char[]){s[i], 0}, (const char[]){s[j], 0}) == 0) {
            j = j + 1LL;
        }
        pi[(int)(i)] = j;
        i = i + 1LL;
    }
    return prefix_function_len = pi_len, pi;
}

long long longest_prefix(const char* s) {
    long long *pi = prefix_function(s);
    size_t pi_len = prefix_function_len;
    long long max_val = 0LL;
    long long i = 0LL;
    while (i < pi_len) {
        if (pi[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? pi_len + _mochi_idx : _mochi_idx;})] > max_val) {
            max_val = pi[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? pi_len + _mochi_idx : _mochi_idx;})];
        }
        i = i + 1LL;
    }
    return max_val;
}

long long list_eq_int(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void test_prefix_function() {
    const char* s1 = "aabcdaabc";
    long long *expected1 = NULL;
    size_t expected1_len = 0;
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 1LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 1LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 2LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 3LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 4LL);
    long long *r1 = prefix_function(s1);
    size_t r1_len = prefix_function_len;
    if (!(list_eq_int(r1, r1_len, expected1, expected1_len))) {
        panic("prefix_function aabcdaabc failed");
    }
    const char* s2 = "asdasdad";
    long long *expected2 = NULL;
    size_t expected2_len = 0;
    expected2 = list_append_long_long(expected2, &expected2_len, 0LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 0LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 0LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 1LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 2LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 3LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 4LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 0LL);
    long long *r2 = prefix_function(s2);
    size_t r2_len = prefix_function_len;
    if (!(list_eq_int(r2, r2_len, expected2, expected2_len))) {
        panic("prefix_function asdasdad failed");
    }
}

void test_longest_prefix() {
    if (longest_prefix("aabcdaabc") != 4LL) {
        panic("longest_prefix example1 failed");
    }
    if (longest_prefix("asdasdad") != 4LL) {
        panic("longest_prefix example2 failed");
    }
    if (longest_prefix("abcab") != 2LL) {
        panic("longest_prefix example3 failed");
    }
}

void user_main() {
    test_prefix_function();
    test_longest_prefix();
    long long *r1 = prefix_function("aabcdaabc");
    size_t r1_len = prefix_function_len;
    long long *r2 = prefix_function("asdasdad");
    size_t r2_len = prefix_function_len;
    puts(str_list_int(r1, r1_len));
    puts(str_list_int(r2, r2_len));
    puts(str_int(longest_prefix("aabcdaabc")));
    puts(str_int(longest_prefix("abcab")));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
