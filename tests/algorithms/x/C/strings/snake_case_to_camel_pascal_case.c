// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t split_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* * split(const char* s, const char* sep);
const char* capitalize(const char* word);
const char* snake_to_camel_case(const char* input_str, long long use_pascal);
int main(void);

const char* * split(const char* s, const char* sep) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (strcmp(ch, sep) == 0) {
            res = list_append_str(res, &res_len, current);
            current = "";
        } else {
            current = str_concat(current, ch);
        }
        i = i + 1LL;
    }
    res = list_append_str(res, &res_len, current);
    return split_len = res_len, res;
}

const char* capitalize(const char* word) {
    if (strlen(word) == 0LL) {
        return "";
    }
    const char* first = str_upper(_substring(word, 0LL, 1LL));
    const char* rest = _substring(word, 1LL, strlen(word));
    return str_concat(first, rest);
}

const char* snake_to_camel_case(const char* input_str, long long use_pascal) {
    const char* *words = split(input_str, "_");
    size_t words_len = split_len;
    const char* result = "";
    long long index = 0LL;
    if (!(use_pascal)) {
        if (words_len > 0LL) {
            result = words[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? words_len + _mochi_idx : _mochi_idx;})];
            index = 1LL;
        }
    }
    while (index < words_len) {
        const char* word = words[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? words_len + _mochi_idx : _mochi_idx;})];
        result = str_concat(result, capitalize(word));
        index = index + 1LL;
    }
    return result;
}

int main(void) {
    {
        long long __start = _now();
        puts(snake_to_camel_case("some_random_string", 0LL));
        puts(snake_to_camel_case("some_random_string", 1LL));
        puts(snake_to_camel_case("some_random_string_with_numbers_123", 0LL));
        puts(snake_to_camel_case("some_random_string_with_numbers_123", 1LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
