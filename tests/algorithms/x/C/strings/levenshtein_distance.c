// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t range_list_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * range_list(long long n);
long long min3(long long a, long long b, long long c);
long long levenshtein_distance(const char* first_word, const char* second_word);
long long levenshtein_distance_optimized(const char* first_word, const char* second_word);
void user_main();
int main(void);

long long * range_list(long long n) {
    long long *lst = NULL;
    size_t lst_len = 0;
    long long i = 0LL;
    while (i < n) {
        lst = list_append_long_long(lst, &lst_len, i);
        i = i + 1LL;
    }
    return range_list_len = lst_len, lst;
}

long long min3(long long a, long long b, long long c) {
    long long m = a;
    if (b < m) {
        m = b;
    }
    if (c < m) {
        m = c;
    }
    return m;
}

long long levenshtein_distance(const char* first_word, const char* second_word) {
    if (strlen(first_word) < strlen(second_word)) {
        return levenshtein_distance(second_word, first_word);
    }
    if (strlen(second_word) == 0LL) {
        return strlen(first_word);
    }
    long long *previous_row = range_list(strlen(second_word) + 1LL);
    size_t previous_row_len = range_list_len;
    long long i = 0LL;
    while (i < strlen(first_word)) {
        const char* c1 = (const char[]){first_word[i], 0};
        long long *current_row = NULL;
        size_t current_row_len = 0;
        current_row = list_append_long_long(current_row, &current_row_len, i + 1LL);
        long long j = 0LL;
        while (j < strlen(second_word)) {
            const char* c2 = (const char[]){second_word[j], 0};
            long long insertions = previous_row[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? previous_row_len + _mochi_idx : _mochi_idx;})] + 1LL;
            long long deletions = current_row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? current_row_len + _mochi_idx : _mochi_idx;})] + 1LL;
            long long substitutions = previous_row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? previous_row_len + _mochi_idx : _mochi_idx;})] + (strcmp(c1, c2) == 0 ? 0LL : 1LL);
            long long min_val = min3(insertions, deletions, substitutions);
            current_row = list_append_long_long(current_row, &current_row_len, min_val);
            j = j + 1LL;
        }
        previous_row = current_row;
        previous_row_len = current_row_len;
        i = i + 1LL;
    }
    return previous_row[(int)({long long _mochi_idx = previous_row_len - 1LL; _mochi_idx < 0 ? previous_row_len + _mochi_idx : _mochi_idx;})];
}

long long levenshtein_distance_optimized(const char* first_word, const char* second_word) {
    if (strlen(first_word) < strlen(second_word)) {
        return levenshtein_distance_optimized(second_word, first_word);
    }
    if (strlen(second_word) == 0LL) {
        return strlen(first_word);
    }
    long long *previous_row = range_list(strlen(second_word) + 1LL);
    size_t previous_row_len = range_list_len;
    long long i = 0LL;
    while (i < strlen(first_word)) {
        const char* c1 = (const char[]){first_word[i], 0};
        long long *current_row = NULL;
        size_t current_row_len = 0;
        current_row = list_append_long_long(current_row, &current_row_len, i + 1LL);
        long long k = 0LL;
        while (k < strlen(second_word)) {
            current_row = list_append_long_long(current_row, &current_row_len, 0LL);
            k = k + 1LL;
        }
        long long j = 0LL;
        while (j < strlen(second_word)) {
            const char* c2 = (const char[]){second_word[j], 0};
            long long insertions = previous_row[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? previous_row_len + _mochi_idx : _mochi_idx;})] + 1LL;
            long long deletions = current_row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? current_row_len + _mochi_idx : _mochi_idx;})] + 1LL;
            long long substitutions = previous_row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? previous_row_len + _mochi_idx : _mochi_idx;})] + (strcmp(c1, c2) == 0 ? 0LL : 1LL);
            long long min_val = min3(insertions, deletions, substitutions);
            current_row[(int)(j + 1LL)] = min_val;
            j = j + 1LL;
        }
        previous_row = current_row;
        previous_row_len = current_row_len;
        i = i + 1LL;
    }
    return previous_row[(int)({long long _mochi_idx = previous_row_len - 1LL; _mochi_idx < 0 ? previous_row_len + _mochi_idx : _mochi_idx;})];
}

void user_main() {
    const char* a = "kitten";
    const char* b = "sitting";
    puts(str_int(levenshtein_distance(a, b)));
    puts(str_int(levenshtein_distance_optimized(a, b)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
