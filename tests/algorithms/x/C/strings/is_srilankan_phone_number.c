// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* phone = "0094702343221";

long long starts_with(const char* s, const char* prefix);
long long all_digits(const char* s);
long long is_sri_lankan_phone_number(const char* phone);
int main(void);

long long starts_with(const char* s, const char* prefix) {
    if (strlen(s) < strlen(prefix)) {
        return 0LL;
    }
    return strcmp(_substring(s, 0LL, strlen(prefix)), prefix) == 0;
}

long long all_digits(const char* s) {
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if ((strcmp(c, "0") < 0) || (strcmp(c, "9") > 0)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long is_sri_lankan_phone_number(const char* phone) {
    const char* p = phone;
    if (starts_with(p, "+94")) {
        p = _substring(p, 3LL, strlen(p));
    } else {
        if (starts_with(p, "0094")) {
            p = _substring(p, 4LL, strlen(p));
        } else {
            if (starts_with(p, "94")) {
                p = _substring(p, 2LL, strlen(p));
            } else {
                if (starts_with(p, "0")) {
                    p = _substring(p, 1LL, strlen(p));
                } else {
                    return 0LL;
                }
            }
        }
    }
    if ((strlen(p) != 9LL) && (strlen(p) != 10LL)) {
        return 0LL;
    }
    if (strcmp((const char[]){p[0LL], 0}, "7") != 0) {
        return 0LL;
    }
    const char* second = (const char[]){p[1LL], 0};
    const char* *allowed = NULL;
    size_t allowed_len = 0;
    allowed = list_append_str(allowed, &allowed_len, "0");
    allowed = list_append_str(allowed, &allowed_len, "1");
    allowed = list_append_str(allowed, &allowed_len, "2");
    allowed = list_append_str(allowed, &allowed_len, "4");
    allowed = list_append_str(allowed, &allowed_len, "5");
    allowed = list_append_str(allowed, &allowed_len, "6");
    allowed = list_append_str(allowed, &allowed_len, "7");
    allowed = list_append_str(allowed, &allowed_len, "8");
    if (!(contains_str(allowed, allowed_len, second))) {
        return 0LL;
    }
    long long idx = 2LL;
    if (strlen(p) == 10LL) {
        const char* sep = (const char[]){p[2LL], 0};
        if ((strcmp(sep, "-") != 0) && (strcmp(sep, " ") != 0)) {
            return 0LL;
        }
        idx = 3LL;
    }
    if ((strlen(p) - idx) != 7LL) {
        return 0LL;
    }
    const char* rest = _substring(p, idx, strlen(p));
    return all_digits(rest);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(is_sri_lankan_phone_number(phone)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
