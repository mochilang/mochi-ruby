// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* ETAOIN = "ETAOINSHRDLCUMWFGYPBVKJXQZ";
const char* LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

long long etaoin_index(const char* letter);
MapSI get_letter_count(const char* message);
const char* get_frequency_order(const char* message);
long long english_freq_match_score(const char* message);
void user_main();
int main(void);

long long etaoin_index(const char* letter) {
    long long i = 0LL;
    while (i < strlen(ETAOIN)) {
        if (strcmp(_substring(ETAOIN, i, i + 1LL), letter) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return strlen(ETAOIN);
}

MapSI get_letter_count(const char* message) {
    static const char* letter_count_keys[16] = {};
    static int letter_count_vals[16] = {};
    size_t letter_count_len = 0;
    MapSI letter_count = { letter_count_keys, letter_count_vals, 0, 16 };
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        const char* c = _substring(LETTERS, i, i + 1LL);
        map_set_si(&letter_count, c, 0LL);
        i = i + 1LL;
    }
    const char* msg = str_upper(message);
    long long j = 0LL;
    while (j < strlen(msg)) {
        const char* ch = _substring(msg, j, j + 1LL);
        if (strstr(LETTERS, ch) != NULL) {
            map_set_si(&letter_count, ch, map_get_si(letter_count.keys, letter_count.vals, letter_count.len, ch) + 1LL);
        }
        j = j + 1LL;
    }
    return letter_count;
}

const char* get_frequency_order(const char* message) {
    MapSI letter_to_freq = get_letter_count(message);
    long long max_freq = 0LL;
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        const char* letter = _substring(LETTERS, i, i + 1LL);
        long long f = map_get_si(letter_to_freq.keys, letter_to_freq.vals, letter_to_freq.len, letter);
        if (f > max_freq) {
            max_freq = f;
        }
        i = i + 1LL;
    }
    const char* result = "";
    long long freq = max_freq;
    while (freq >= 0LL) {
        const char* *group = NULL;
        size_t group_len = 0;
        long long j = 0LL;
        while (j < strlen(LETTERS)) {
            const char* letter = _substring(LETTERS, j, j + 1LL);
            if (map_get_si(letter_to_freq.keys, letter_to_freq.vals, letter_to_freq.len, letter) == freq) {
                group = list_append_str(group, &group_len, letter);
            }
            j = j + 1LL;
        }
        long long g_len = group_len;
        long long a = 0LL;
        while (a < g_len) {
            long long b = 0LL;
            while (b < ((g_len - a) - 1LL)) {
                const char* g1 = group[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})];
                const char* g2 = group[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})];
                long long idx1 = etaoin_index(g1);
                long long idx2 = etaoin_index(g2);
                if (idx1 < idx2) {
                    const char* tmp = group[(int)({long long _mochi_idx = b; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})];
                    group[(int)(b)] = group[(int)({long long _mochi_idx = b + 1LL; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})];
                    group[(int)(b + 1LL)] = tmp;
                }
                b = b + 1LL;
            }
            a = a + 1LL;
        }
        long long g = 0LL;
        while (g < group_len) {
            result = str_concat(result, group[(int)({long long _mochi_idx = g; _mochi_idx < 0 ? group_len + _mochi_idx : _mochi_idx;})]);
            g = g + 1LL;
        }
        freq = freq - 1LL;
    }
    return result;
}

long long english_freq_match_score(const char* message) {
    const char* freq_order = get_frequency_order(message);
    const char* top = _substring(freq_order, 0LL, 6LL);
    const char* bottom = _substring(freq_order, strlen(freq_order) - 6LL, strlen(freq_order));
    long long score = 0LL;
    long long i = 0LL;
    while (i < 6LL) {
        const char* c = _substring(ETAOIN, i, i + 1LL);
        if (strstr(top, c) != NULL) {
            score = score + 1LL;
        }
        i = i + 1LL;
    }
    long long j = strlen(ETAOIN) - 6LL;
    while (j < strlen(ETAOIN)) {
        const char* c = _substring(ETAOIN, j, j + 1LL);
        if (strstr(bottom, c) != NULL) {
            score = score + 1LL;
        }
        j = j + 1LL;
    }
    return score;
}

void user_main() {
    puts(get_frequency_order("Hello World"));
    printf("%d\n", english_freq_match_score("Hello World"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
