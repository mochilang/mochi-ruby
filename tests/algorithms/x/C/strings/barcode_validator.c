// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *res = NULL;
size_t res_len = 0;
long long x = 0LL;

long long has_alpha(const char* s);
long long parse_decimal(const char* s);
long long get_barcode(const char* barcode);
long long get_check_digit(long long barcode);
long long is_valid(long long barcode);
int main(void);

long long has_alpha(const char* s) {
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if (((strcmp(c, "a") >= 0) && (strcmp(c, "z") <= 0)) || ((strcmp(c, "A") >= 0) && (strcmp(c, "Z") <= 0))) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long parse_decimal(const char* s) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if ((strcmp(c, "0") < 0) || (strcmp(c, "9") > 0)) {
            panic("Non-digit character encountered");
        }
        value = (value * 10LL) + _atoi(c);
        i = i + 1LL;
    }
    return value;
}

long long get_barcode(const char* barcode) {
    if (has_alpha(barcode)) {
        panic(str_concat(str_concat("Barcode '", barcode), "' has alphabetic characters."));
    }
    if ((strlen(barcode) > 0LL) && (strcmp((const char[]){barcode[0LL], 0}, "-") == 0)) {
        panic("The entered barcode has a negative value. Try again.");
    }
    return parse_decimal(barcode);
}

long long get_check_digit(long long barcode) {
    long long num = barcode / 10LL;
    long long s = 0LL;
    long long position = 0LL;
    while (num != 0LL) {
        long long mult;
        if ((position % 2LL) == 0LL) {
            mult = 3LL;
        } else {
            mult = 1LL;
        }
        s = s + (mult * (num % 10LL));
        num = num / 10LL;
        position = position + 1LL;
    }
    return (10LL - (s % 10LL)) % 10LL;
}

long long is_valid(long long barcode) {
    return (strlen(str_int(barcode)) == 13LL) && (get_check_digit(barcode) == (barcode % 10LL));
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(get_check_digit(8718452538119LL)));
        puts(str_int(get_check_digit(87184523LL)));
        puts(str_int(get_check_digit(87193425381086LL)));
        while (x < 100LL) {
            res = list_append_long_long(res, &res_len, get_check_digit(x));
            x = x + 10LL;
        }
        puts(str_list_int(res, res_len));
        puts(str_int(is_valid(8718452538119LL)));
        puts(str_int(is_valid(87184525LL)));
        puts(str_int(is_valid(87193425381089LL)));
        puts(str_int(is_valid(0LL)));
        puts(str_int(get_barcode("8718452538119")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
