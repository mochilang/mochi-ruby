// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_words_len;
size_t text_justification_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* repeat_str(const char* s, long long count_);
const char* * split_words(const char* s);
const char* justify_line(const char* * line, size_t line_len, long long width, long long max_width);
const char* * text_justification(const char* word, long long max_width);
int main(void);

const char* repeat_str(const char* s, long long count_) {
    const char* res = "";
    long long i = 0LL;
    while (i < count_) {
        res = str_concat(res, s);
        i = i + 1LL;
    }
    return res;
}

const char* * split_words(const char* s) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (strcmp(ch, " ") == 0) {
            if (strcmp(current, "") != 0) {
                res = list_append_str(res, &res_len, current);
                current = "";
            }
        } else {
            current = str_concat(current, ch);
        }
        i = i + 1LL;
    }
    if (strcmp(current, "") != 0) {
        res = list_append_str(res, &res_len, current);
    }
    return split_words_len = res_len, res;
}

const char* justify_line(const char* * line, size_t line_len, long long width, long long max_width) {
    long long overall_spaces_count = max_width - width;
    long long words_count = line_len;
    if (words_count == 1LL) {
        return str_concat(line[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? line_len + _mochi_idx : _mochi_idx;})], repeat_str(" ", overall_spaces_count));
    }
    long long spaces_to_insert_between_words = words_count - 1LL;
    long long *num_spaces_between_words_list = NULL;
    size_t num_spaces_between_words_list_len = 0;
    long long base = overall_spaces_count / spaces_to_insert_between_words;
    long long extra = overall_spaces_count % spaces_to_insert_between_words;
    long long i = 0LL;
    while (i < spaces_to_insert_between_words) {
        long long spaces = base;
        if (i < extra) {
            spaces = spaces + 1LL;
        }
        num_spaces_between_words_list = list_append_long_long(num_spaces_between_words_list, &num_spaces_between_words_list_len, spaces);
        i = i + 1LL;
    }
    const char* aligned = "";
    i = 0LL;
    while (i < spaces_to_insert_between_words) {
        aligned = str_concat(str_concat(aligned, line[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? line_len + _mochi_idx : _mochi_idx;})]), repeat_str(" ", num_spaces_between_words_list[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? num_spaces_between_words_list_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    aligned = str_concat(aligned, line[(int)({long long _mochi_idx = spaces_to_insert_between_words; _mochi_idx < 0 ? line_len + _mochi_idx : _mochi_idx;})]);
    return aligned;
}

const char* * text_justification(const char* word, long long max_width) {
    const char* *words = split_words(word);
    size_t words_len = split_words_len;
    const char* *answer = NULL;
    size_t answer_len = 0;
    const char* *line = NULL;
    size_t line_len = 0;
    long long width = 0LL;
    long long idx = 0LL;
    while (idx < words_len) {
        const char* w = words[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? words_len + _mochi_idx : _mochi_idx;})];
        if (((width + strlen(w)) + line_len) <= max_width) {
            line = list_append_str(line, &line_len, w);
            width = width + strlen(w);
        } else {
            answer = list_append_str(answer, &answer_len, justify_line(line, line_len, width, max_width));
            line = ({const char* *tmp = malloc(1 * sizeof(const char*)); tmp[0] = w; tmp;});
            line_len = 1;
            width = strlen(w);
        }
        idx = idx + 1LL;
    }
    long long remaining_spaces = (max_width - width) - line_len;
    const char* last_line = "";
    long long j = 0LL;
    while (j < line_len) {
        if (j > 0LL) {
            last_line = str_concat(last_line, " ");
        }
        last_line = str_concat(last_line, line[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? line_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    last_line = str_concat(last_line, repeat_str(" ", remaining_spaces + 1LL));
    answer = list_append_str(answer, &answer_len, last_line);
    return text_justification_len = answer_len, answer;
}

int main(void) {
    {
        long long __start = _now();
        puts(({const char **__tmp2 = text_justification("This is an example of text justification.", 16LL); char *__res = str_list_str(__tmp2, text_justification_len); __res;}));
        puts(({const char **__tmp3 = text_justification("Two roads diverged in a yellow wood", 16LL); char *__res = str_list_str(__tmp3, text_justification_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
