// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* palindromic_string(const char* input_string);
void user_main();
int main(void);

const char* palindromic_string(const char* input_string) {
    long long max_length = 0LL;
    const char* new_input_string = "";
    const char* output_string = "";
    long long n = strlen(input_string);
    long long i = 0LL;
    while (i < (n - 1LL)) {
        new_input_string = str_concat(str_concat(new_input_string, _substring(input_string, i, i + 1LL)), "|");
        i = i + 1LL;
    }
    new_input_string = str_concat(new_input_string, _substring(input_string, n - 1LL, n));
    long long left = 0LL;
    long long right = 0LL;
    long long *length = NULL;
    size_t length_len = 0;
    i = 0LL;
    long long m = strlen(new_input_string);
    while (i < m) {
        length = list_append_long_long(length, &length_len, 1LL);
        i = i + 1LL;
    }
    long long start = 0LL;
    long long j = 0LL;
    while (j < m) {
        long long k = 1LL;
        if (j <= right) {
            long long mirror = (left + right) - j;
            k = length[(int)({long long _mochi_idx = mirror; _mochi_idx < 0 ? length_len + _mochi_idx : _mochi_idx;})] / 2LL;
            long long diff = (right - j) + 1LL;
            if (diff < k) {
                k = diff;
            }
            if (k < 1LL) {
                k = 1LL;
            }
        }
        while ((((j - k) >= 0LL) && ((j + k) < m)) && (strcmp(_substring(new_input_string, j + k, (j + k) + 1LL), _substring(new_input_string, j - k, (j - k) + 1LL)) == 0)) {
            k = k + 1LL;
        }
        length[(int)(j)] = (2LL * k) - 1LL;
        if (((j + k) - 1LL) > right) {
            left = (j - k) + 1LL;
            right = (j + k) - 1LL;
        }
        if (length[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? length_len + _mochi_idx : _mochi_idx;})] > max_length) {
            max_length = length[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? length_len + _mochi_idx : _mochi_idx;})];
            start = j;
        }
        j = j + 1LL;
    }
    const char* s = _substring(new_input_string, start - (max_length / 2LL), (start + (max_length / 2LL)) + 1LL);
    long long idx = 0LL;
    while (idx < strlen(s)) {
        const char* ch = _substring(s, idx, idx + 1LL);
        if (strcmp(ch, "|") != 0) {
            output_string = str_concat(output_string, ch);
        }
        idx = idx + 1LL;
    }
    return output_string;
}

void user_main() {
    puts(palindromic_string("abbbaba"));
    puts(palindromic_string("ababa"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
