// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* s1 = "The quick brown fox jumps over the lazy dog";
const char* s2 = "My name is Unknown";

long long is_pangram(const char* input_str);
long long is_pangram_faster(const char* input_str);
long long is_pangram_fastest(const char* input_str);
int main(void);

long long is_pangram(const char* input_str) {
    const char* *letters = NULL;
    size_t letters_len = 0;
    long long i = 0LL;
    while (i < strlen(input_str)) {
        const char* c = str_lower((const char[]){input_str[i], 0});
        long long is_new = !(contains_str(letters, letters_len, c));
        if ((((strcmp(c, " ") != 0) && (strcmp("a", c) <= 0)) && (strcmp(c, "z") <= 0)) && is_new) {
            letters = list_append_str(letters, &letters_len, c);
        }
        i = i + 1LL;
    }
    return letters_len == 26LL;
}

long long is_pangram_faster(const char* input_str) {
    const char* alphabet = "abcdefghijklmnopqrstuvwxyz";
    long long *flag = NULL;
    size_t flag_len = 0;
    long long i = 0LL;
    while (i < 26LL) {
        flag = list_append_long_long(flag, &flag_len, 0LL);
        i = i + 1LL;
    }
    long long j = 0LL;
    while (j < strlen(input_str)) {
        const char* c = str_lower((const char[]){input_str[j], 0});
        long long k = 0LL;
        while (k < 26LL) {
            if (strcmp((const char[]){alphabet[k], 0}, c) == 0) {
                flag[(int)(k)] = 1LL;
                break;
            }
            k = k + 1LL;
        }
        j = j + 1LL;
    }
    long long t = 0LL;
    while (t < 26LL) {
        if (!(flag[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? flag_len + _mochi_idx : _mochi_idx;})])) {
            return 0LL;
        }
        t = t + 1LL;
    }
    return 1LL;
}

long long is_pangram_fastest(const char* input_str) {
    const char* s = str_lower(input_str);
    const char* alphabet = "abcdefghijklmnopqrstuvwxyz";
    long long i = 0LL;
    while (i < strlen(alphabet)) {
        const char* letter = (const char[]){alphabet[i], 0};
        if (!(strstr(s, letter) != NULL)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(is_pangram(s1)));
        puts(str_int(is_pangram(s2)));
        puts(str_int(is_pangram_faster(s1)));
        puts(str_int(is_pangram_faster(s2)));
        puts(str_int(is_pangram_fastest(s1)));
        puts(str_int(is_pangram_fastest(s2)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
