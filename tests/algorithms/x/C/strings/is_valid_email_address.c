// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long MAX_LOCAL_PART_OCTETS = 64LL;
long long MAX_DOMAIN_OCTETS = 255LL;
const char* ASCII_LETTERS = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* DIGITS = "0123456789";
const char* LOCAL_EXTRA = ".(!#$%&'*+-/=?^_`{|}~)";
const char* DOMAIN_EXTRA = ".-";
const char* email_tests_init[19] = {"simple@example.com", "very.common@example.com", "disposable.style.email.with+symbol@example.com", "other-email-with-hyphen@and.subdomains.example.com", "fully-qualified-domain@example.com", "user.name+tag+sorting@example.com", "x@example.com", "example-indeed@strange-example.com", "test/test@test.com", "123456789012345678901234567890123456789012345678901234567890123@example.com", "admin@mailserver1", "example@s.example", "Abc.example.com", "A@b@c@example.com", "abc@example..com", "a(c)d,e:f;g<h>i[j\\k]l@example.com", "12345678901234567890123456789012345678901234567890123456789012345@example.com", "i.like.underscores@but_its_not_allowed_in_this_part", ""};
const char* *email_tests = email_tests_init;
size_t email_tests_len = 19;
long long idx = 0LL;

long long count_char(const char* s, const char* target);
long long char_in(const char* c, const char* allowed);
long long starts_with_char(const char* s, const char* c);
long long ends_with_char(const char* s, const char* c);
long long contains_double_dot(const char* s);
long long is_valid_email_address(const char* email);
int main(void);

long long count_char(const char* s, const char* target) {
    long long cnt = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), target) == 0) {
            cnt = cnt + 1LL;
        }
        i = i + 1LL;
    }
    return cnt;
}

long long char_in(const char* c, const char* allowed) {
    long long i = 0LL;
    while (i < strlen(allowed)) {
        if (strcmp(_substring(allowed, i, i + 1LL), c) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long starts_with_char(const char* s, const char* c) {
    return (strlen(s) > 0LL) && (strcmp(_substring(s, 0LL, 1LL), c) == 0);
}

long long ends_with_char(const char* s, const char* c) {
    return (strlen(s) > 0LL) && (strcmp(_substring(s, strlen(s) - 1LL, strlen(s)), c) == 0);
}

long long contains_double_dot(const char* s) {
    if (strlen(s) < 2LL) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < (strlen(s) - 1LL)) {
        if (strcmp(_substring(s, i, i + 2LL), "..") == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long is_valid_email_address(const char* email) {
    if (count_char(email, "@") != 1LL) {
        return 0LL;
    }
    long long at_idx = 0LL;
    long long i = 0LL;
    while (i < strlen(email)) {
        if (strcmp(_substring(email, i, i + 1LL), "@") == 0) {
            at_idx = i;
            break;
        }
        i = i + 1LL;
    }
    const char* local_part = _substring(email, 0LL, at_idx);
    const char* domain = _substring(email, at_idx + 1LL, strlen(email));
    if ((strlen(local_part) > MAX_LOCAL_PART_OCTETS) || (strlen(domain) > MAX_DOMAIN_OCTETS)) {
        return 0LL;
    }
    i = 0LL;
    while (i < strlen(local_part)) {
        const char* ch = _substring(local_part, i, i + 1LL);
        if (!(char_in(ch, "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789.(!#$%&'*+-/=?^_`{|}~)"))) {
            return 0LL;
        }
        i = i + 1LL;
    }
    if ((starts_with_char(local_part, ".") || ends_with_char(local_part, ".")) || contains_double_dot(local_part)) {
        return 0LL;
    }
    i = 0LL;
    while (i < strlen(domain)) {
        const char* ch = _substring(domain, i, i + 1LL);
        if (!(char_in(ch, "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789.-"))) {
            return 0LL;
        }
        i = i + 1LL;
    }
    if (starts_with_char(domain, "-") || ends_with_char(domain, ".")) {
        return 0LL;
    }
    if ((starts_with_char(domain, ".") || ends_with_char(domain, ".")) || contains_double_dot(domain)) {
        return 0LL;
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        while (idx < 19LL) {
            const char* email = email_tests[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? email_tests_len + _mochi_idx : _mochi_idx;})];
            puts(str_int(is_valid_email_address(email)));
            idx = idx + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
