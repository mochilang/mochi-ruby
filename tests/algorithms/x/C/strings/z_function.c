// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t z_function_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * z_function(const char* s);
long long go_next(long long i, long long * z, size_t z_len, const char* s);
long long find_pattern(const char* pattern, const char* input_str);
long long list_eq_int(long long * a, size_t a_len, long long * b, size_t b_len);
void test_z_function();
void test_find_pattern();
void user_main();
int main(void);

long long * z_function(const char* s) {
    long long *z = NULL;
    size_t z_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        z = list_append_long_long(z, &z_len, 0LL);
        i = i + 1LL;
    }
    long long l = 0LL;
    long long r = 0LL;
    i = 1LL;
    while (i < strlen(s)) {
        if (i <= r) {
            long long min_edge = (r - i) + 1LL;
            long long zi = z[(int)({long long _mochi_idx = i - l; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})];
            if (zi < min_edge) {
                min_edge = zi;
            }
            z[(int)(i)] = min_edge;
        }
        while (go_next(i, z, z_len, s)) {
            z[(int)(i)] = z[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})] + 1LL;
        }
        if (((i + z[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})]) - 1LL) > r) {
            l = i;
            r = (i + z[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})]) - 1LL;
        }
        i = i + 1LL;
    }
    return z_function_len = z_len, z;
}

long long go_next(long long i, long long * z, size_t z_len, const char* s) {
    return ((i + z[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})]) < strlen(s)) && (strcmp((const char[]){s[z[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})]], 0}, (const char[]){s[i + z[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_len + _mochi_idx : _mochi_idx;})]], 0}) == 0);
}

long long find_pattern(const char* pattern, const char* input_str) {
    long long answer = 0LL;
    long long *z_res = z_function(str_concat(pattern, input_str));
    size_t z_res_len = z_function_len;
    long long i = 0LL;
    while (i < z_res_len) {
        if (z_res[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? z_res_len + _mochi_idx : _mochi_idx;})] >= strlen(pattern)) {
            answer = answer + 1LL;
        }
        i = i + 1LL;
    }
    return answer;
}

long long list_eq_int(long long * a, size_t a_len, long long * b, size_t b_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] != b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void test_z_function() {
    const char* s1 = "abracadabra";
    long long *expected1 = NULL;
    size_t expected1_len = 0;
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 1LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 1LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 4LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 0LL);
    expected1 = list_append_long_long(expected1, &expected1_len, 1LL);
    long long *r1 = z_function(s1);
    size_t r1_len = z_function_len;
    if (!(list_eq_int(r1, r1_len, expected1, expected1_len))) {
        panic("z_function abracadabra failed");
    }
    const char* s2 = "aaaa";
    long long *expected2 = NULL;
    size_t expected2_len = 0;
    expected2 = list_append_long_long(expected2, &expected2_len, 0LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 3LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 2LL);
    expected2 = list_append_long_long(expected2, &expected2_len, 1LL);
    long long *r2 = z_function(s2);
    size_t r2_len = z_function_len;
    if (!(list_eq_int(r2, r2_len, expected2, expected2_len))) {
        panic("z_function aaaa failed");
    }
    const char* s3 = "zxxzxxz";
    long long *expected3 = NULL;
    size_t expected3_len = 0;
    expected3 = list_append_long_long(expected3, &expected3_len, 0LL);
    expected3 = list_append_long_long(expected3, &expected3_len, 0LL);
    expected3 = list_append_long_long(expected3, &expected3_len, 0LL);
    expected3 = list_append_long_long(expected3, &expected3_len, 4LL);
    expected3 = list_append_long_long(expected3, &expected3_len, 0LL);
    expected3 = list_append_long_long(expected3, &expected3_len, 0LL);
    expected3 = list_append_long_long(expected3, &expected3_len, 1LL);
    long long *r3 = z_function(s3);
    size_t r3_len = z_function_len;
    if (!(list_eq_int(r3, r3_len, expected3, expected3_len))) {
        panic("z_function zxxzxxz failed");
    }
}

void test_find_pattern() {
    if (find_pattern("abr", "abracadabra") != 2LL) {
        panic("find_pattern abr failed");
    }
    if (find_pattern("a", "aaaa") != 4LL) {
        panic("find_pattern aaaa failed");
    }
    if (find_pattern("xz", "zxxzxxz") != 2LL) {
        panic("find_pattern xz failed");
    }
}

void user_main() {
    test_z_function();
    test_find_pattern();
    long long *r1 = z_function("abracadabra");
    size_t r1_len = z_function_len;
    long long *r2 = z_function("aaaa");
    size_t r2_len = z_function_len;
    long long *r3 = z_function("zxxzxxz");
    size_t r3_len = z_function_len;
    puts(str_list_int(r1, r1_len));
    puts(str_list_int(r2, r2_len));
    puts(str_list_int(r3, r3_len));
    puts(str_int(find_pattern("abr", "abracadabra")));
    puts(str_int(find_pattern("a", "aaaa")));
    puts(str_int(find_pattern("xz", "zxxzxxz")));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
