// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* strip_and_remove_spaces(const char* s);
long long check_anagrams(const char* a, const char* b);
void print_bool(long long b);
int main(void);

const char* strip_and_remove_spaces(const char* s) {
    long long start = 0LL;
    long long end = strlen(s) - 1LL;
    while ((start < strlen(s)) && (strcmp((const char[]){s[start], 0}, " ") == 0)) {
        start = start + 1LL;
    }
    while ((end >= start) && (strcmp((const char[]){s[end], 0}, " ") == 0)) {
        end = end - 1LL;
    }
    const char* res = "";
    long long i = start;
    while (i <= end) {
        const char* ch = (const char[]){s[i], 0};
        if (strcmp(ch, " ") != 0) {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

long long check_anagrams(const char* a, const char* b) {
    const char* s1 = str_lower(a);
    const char* s2 = str_lower(b);
    s1 = strip_and_remove_spaces(s1);
    s2 = strip_and_remove_spaces(s2);
    if (strlen(s1) != strlen(s2)) {
        return 0LL;
    }
    static const char* count__keys[16] = {};
    static int count__vals[16] = {};
    size_t count__len = 0;
    MapSI count_ = { count__keys, count__vals, 0, 16 };
    long long i = 0LL;
    while (i < strlen(s1)) {
        const char* c1 = (const char[]){s1[i], 0};
        const char* c2 = (const char[]){s2[i], 0};
        if (c1 in count_) {
            map_set_si(&count_, c1, map_get_si(count_.keys, count_.vals, count_.len, c1) + 1LL);
        } else {
            map_set_si(&count_, c1, 1LL);
        }
        if (c2 in count_) {
            map_set_si(&count_, c2, map_get_si(count_.keys, count_.vals, count_.len, c2) - 1LL);
        } else {
            map_set_si(&count_, c2, -1LL);
        }
        i = i + 1LL;
    }
    MapSI __tmp0 = count_;
    for (size_t __i = 0; __i < __tmp0.len; __i++) {
        const char* ch = __tmp0.keys[__i];
        if (map_get_si(count_.keys, count_.vals, count_.len, ch) != 0LL) {
            return 0LL;
        }
    }
    return 1LL;
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        print_bool(check_anagrams("Silent", "Listen"));
        print_bool(check_anagrams("This is a string", "Is this a string"));
        print_bool(check_anagrams("This is    a      string", "Is     this a string"));
        print_bool(check_anagrams("There", "Their"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
