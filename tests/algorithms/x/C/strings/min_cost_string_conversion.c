// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t string_to_chars_len;
size_t insert_at_len;
size_t remove_at_len;
size_t make_matrix_int_len;
size_t *make_matrix_int_lens;
size_t make_matrix_string_len;
size_t *make_matrix_string_lens;
size_t assemble_transformation_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct TransformTables TransformTables;

struct TransformTables {
    long long **costs;
    size_t costs_len;
    size_t *costs_lens;
    size_t costs_lens_len;
    const char* **ops;
    size_t ops_len;
    size_t *ops_lens;
    size_t ops_lens_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* * string_to_chars(const char* s);
const char* join_chars(const char* * chars, size_t chars_len);
const char* * insert_at(const char* * chars, size_t chars_len, long long index, const char* ch);
const char* * remove_at(const char* * chars, size_t chars_len, long long index);
long long * * make_matrix_int(long long rows, long long cols, long long init);
const char* * * make_matrix_string(long long rows, long long cols, const char* init);
TransformTables compute_transform_tables(const char* source_string, const char* destination_string, long long copy_cost, long long replace_cost, long long delete_cost, long long insert_cost);
const char* * assemble_transformation(const char* * * ops, size_t ops_len, size_t* ops_lens, size_t ops_lens_len, long long i, long long j);
void user_main();
int main(void);

const char* * string_to_chars(const char* s) {
    const char* *chars = NULL;
    size_t chars_len = 0;
    long long i = 0LL;
    while (i < strlen(s)) {
        chars = list_append_str(chars, &chars_len, _substring(s, i, i + 1LL));
        i = i + 1LL;
    }
    return string_to_chars_len = chars_len, chars;
}

const char* join_chars(const char* * chars, size_t chars_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < chars_len) {
        res = str_concat(res, chars[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chars_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return res;
}

const char* * insert_at(const char* * chars, size_t chars_len, long long index, const char* ch) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < index) {
        res = list_append_str(res, &res_len, chars[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chars_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    res = list_append_str(res, &res_len, ch);
    while (i < chars_len) {
        res = list_append_str(res, &res_len, chars[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chars_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return insert_at_len = res_len, res;
}

const char* * remove_at(const char* * chars, size_t chars_len, long long index) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < chars_len) {
        if (i != index) {
            res = list_append_str(res, &res_len, chars[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? chars_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_at_len = res_len, res;
}

long long * * make_matrix_int(long long rows, long long cols, long long init) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    for (int _ = 0LL; _ < rows; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < cols; _2++) {
            row = list_append_long_long(row, &row_len, init);
        }
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
    }
    return make_matrix_int_lens = matrix_lens, make_matrix_int_len = matrix_len, matrix;
}

const char* * * make_matrix_string(long long rows, long long cols, const char* init) {
    const char* **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    for (int _ = 0LL; _ < rows; _++) {
        const char* *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < cols; _2++) {
            row = list_append_str(row, &row_len, init);
        }
        matrix = list_append_strptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
    }
    return make_matrix_string_lens = matrix_lens, make_matrix_string_len = matrix_len, matrix;
}

TransformTables compute_transform_tables(const char* source_string, const char* destination_string, long long copy_cost, long long replace_cost, long long delete_cost, long long insert_cost) {
    const char* *source_seq = string_to_chars(source_string);
    size_t source_seq_len = string_to_chars_len;
    const char* *dest_seq = string_to_chars(destination_string);
    size_t dest_seq_len = string_to_chars_len;
    long long m = source_seq_len;
    long long n = dest_seq_len;
    long long **costs = make_matrix_int(m + 1LL, n + 1LL, 0LL);
    size_t costs_len = make_matrix_int_len;
    size_t *costs_lens = make_matrix_int_lens;
    size_t costs_lens_len = make_matrix_int_len;
    const char* **ops = make_matrix_string(m + 1LL, n + 1LL, "0");
    size_t ops_len = make_matrix_string_len;
    size_t *ops_lens = make_matrix_string_lens;
    size_t ops_lens_len = make_matrix_string_len;
    long long i = 1LL;
    while (i <= m) {
        costs[(int)(i)][(int)(0LL)] = i * delete_cost;
        ops[(int)(i)][(int)(0LL)] = str_concat("D", source_seq[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? source_seq_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    long long j = 1LL;
    while (j <= n) {
        costs[(int)(0LL)][(int)(j)] = j * insert_cost;
        ops[(int)(0LL)][(int)(j)] = str_concat("I", dest_seq[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dest_seq_len + _mochi_idx : _mochi_idx;})]);
        j = j + 1LL;
    }
    i = 1LL;
    while (i <= m) {
        j = 1LL;
        while (j <= n) {
            if (strcmp(source_seq[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? source_seq_len + _mochi_idx : _mochi_idx;})], dest_seq[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dest_seq_len + _mochi_idx : _mochi_idx;})]) == 0) {
                costs[(int)(i)][(int)(j)] = costs[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? costs_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + copy_cost;
                ops[(int)(i)][(int)(j)] = str_concat("C", source_seq[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? source_seq_len + _mochi_idx : _mochi_idx;})]);
            } else {
                costs[(int)(i)][(int)(j)] = costs[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? costs_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + replace_cost;
                ops[(int)(i)][(int)(j)] = str_concat(str_concat("R", source_seq[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? source_seq_len + _mochi_idx : _mochi_idx;})]), dest_seq[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dest_seq_len + _mochi_idx : _mochi_idx;})]);
            }
            if ((costs[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? costs_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + delete_cost) < costs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? costs_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
                costs[(int)(i)][(int)(j)] = costs[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? costs_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + delete_cost;
                ops[(int)(i)][(int)(j)] = str_concat("D", source_seq[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? source_seq_len + _mochi_idx : _mochi_idx;})]);
            }
            if ((costs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? costs_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] + insert_cost) < costs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? costs_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
                costs[(int)(i)][(int)(j)] = costs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? costs_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? costs_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] + insert_cost;
                ops[(int)(i)][(int)(j)] = str_concat("I", dest_seq[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dest_seq_len + _mochi_idx : _mochi_idx;})]);
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return (TransformTables){.costs = costs, .costs_len = costs_len, .costs_lens = costs_lens, .costs_lens_len = costs_len, .ops = ops, .ops_len = ops_len, .ops_lens = ops_lens, .ops_lens_len = ops_len};
}

const char* * assemble_transformation(const char* * * ops, size_t ops_len, size_t* ops_lens, size_t ops_lens_len, long long i, long long j) {
    if ((i == 0LL) && (j == 0LL)) {
        {
            const char** __tmp0_data = malloc(0 * sizeof(const char*));
            assemble_transformation_len = 0;
            return __tmp0_data;
        }
    }
    const char* op = ops[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ops_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ops_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
    const char* kind = _substring(op, 0LL, 1LL);
    if ((strcmp(kind, "C") == 0) || (strcmp(kind, "R") == 0)) {
        const char* *seq = assemble_transformation(ops, ops_len, ops_lens, ops_len, i - 1LL, j - 1LL);
        size_t seq_len = assemble_transformation_len;
        seq = list_append_str(seq, &seq_len, op);
        return assemble_transformation_len = seq_len, seq;
    } else {
        if (strcmp(kind, "D") == 0) {
            const char* *seq = assemble_transformation(ops, ops_len, ops_lens, ops_len, i - 1LL, j);
            size_t seq_len = assemble_transformation_len;
            seq = list_append_str(seq, &seq_len, op);
            return assemble_transformation_len = seq_len, seq;
        } else {
            const char* *seq = assemble_transformation(ops, ops_len, ops_lens, ops_len, i, j - 1LL);
            size_t seq_len = assemble_transformation_len;
            seq = list_append_str(seq, &seq_len, op);
            return assemble_transformation_len = seq_len, seq;
        }
    }
}

void user_main() {
    long long copy_cost = -1LL;
    long long replace_cost = 1LL;
    long long delete_cost = 2LL;
    long long insert_cost = 2LL;
    const char* src = "Python";
    const char* dst = "Algorithms";
    TransformTables tables = compute_transform_tables(src, dst, copy_cost, replace_cost, delete_cost, insert_cost);
    const char* **operations = tables.ops;
    size_t operations_len = tables.ops_len;
    size_t *operations_lens = tables.ops_lens;
    size_t operations_lens_len = tables.ops_len;
    long long m = operations_len;
    long long n = operations_lens[(int)(0LL)];
    const char* *sequence = assemble_transformation(operations, operations_len, operations_lens, operations_len, m - 1LL, n - 1LL);
    size_t sequence_len = assemble_transformation_len;
    const char* *string_list = string_to_chars(src);
    size_t string_list_len = string_to_chars_len;
    long long idx = 0LL;
    long long cost = 0LL;
    long long k = 0LL;
    while (k < sequence_len) {
        puts(join_chars(string_list, string_list_len));
        const char* op = sequence[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? sequence_len + _mochi_idx : _mochi_idx;})];
        const char* kind = _substring(op, 0LL, 1LL);
        if (strcmp(kind, "C") == 0) {
            cost = cost + copy_cost;
        } else {
            if (strcmp(kind, "R") == 0) {
                string_list[(int)(idx)] = _substring(op, 2LL, 3LL);
                cost = cost + replace_cost;
            } else {
                if (strcmp(kind, "D") == 0) {
                    string_list = remove_at(string_list, string_list_len, idx);
                    string_list_len = remove_at_len;
                    cost = cost + delete_cost;
                } else {
                    string_list = insert_at(string_list, string_list_len, idx, _substring(op, 1LL, 2LL));
                    string_list_len = insert_at_len;
                    cost = cost + insert_cost;
                }
            }
        }
        idx = idx + 1LL;
        k = k + 1LL;
    }
    puts(join_chars(string_list, string_list_len));
    puts(str_concat("Cost: ", str_int(cost)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
