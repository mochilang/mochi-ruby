// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t split_spaces_len;

size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* LETTERS_AND_SPACE = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz \t\n";
const char* LOWER = "abcdefghijklmnopqrstuvwxyz";
const char* UPPER = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
MapSI ENGLISH_WORDS = (MapSI){0};

const char* to_upper(const char* s);
long long char_in(const char* chars, const char* c);
const char* remove_non_letters(const char* message);
const char* * split_spaces(const char* text);
MapSI load_dictionary();
double get_english_count(const char* message);
long long is_english(const char* message, long long word_percentage, long long letter_percentage);
int main(void);

const char* to_upper(const char* s) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        long long j = 0LL;
        const char* up = c;
        while (j < strlen(LOWER)) {
            if (strcmp(c, _substring(LOWER, j, j + 1LL)) == 0) {
                up = _substring(UPPER, j, j + 1LL);
                break;
            }
            j = j + 1LL;
        }
        res = str_concat(res, up);
        i = i + 1LL;
    }
    return res;
}

long long char_in(const char* chars, const char* c) {
    long long i = 0LL;
    while (i < strlen(chars)) {
        if (strcmp(_substring(chars, i, i + 1LL), c) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* remove_non_letters(const char* message) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(message)) {
        const char* ch = _substring(message, i, i + 1LL);
        if (char_in(LETTERS_AND_SPACE, ch)) {
            res = str_concat(res, ch);
        }
        i = i + 1LL;
    }
    return res;
}

const char* * split_spaces(const char* text) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(text)) {
        const char* ch = _substring(text, i, i + 1LL);
        if (strcmp(ch, " ") == 0) {
            res = list_append_str(res, &res_len, current);
            current = "";
        } else {
            current = str_concat(current, ch);
        }
        i = i + 1LL;
    }
    res = list_append_str(res, &res_len, current);
    return split_spaces_len = res_len, res;
}

MapSI load_dictionary() {
    const char* *words = NULL;
    size_t words_len = 0;
    words = list_append_str(words, &words_len, "HELLO");
    words = list_append_str(words, &words_len, "WORLD");
    words = list_append_str(words, &words_len, "HOW");
    words = list_append_str(words, &words_len, "ARE");
    words = list_append_str(words, &words_len, "YOU");
    words = list_append_str(words, &words_len, "THE");
    words = list_append_str(words, &words_len, "QUICK");
    words = list_append_str(words, &words_len, "BROWN");
    words = list_append_str(words, &words_len, "FOX");
    words = list_append_str(words, &words_len, "JUMPS");
    words = list_append_str(words, &words_len, "OVER");
    words = list_append_str(words, &words_len, "LAZY");
    words = list_append_str(words, &words_len, "DOG");
    static const char* dict_keys[16] = {};
    static int dict_vals[16] = {};
    size_t dict_len = 0;
    MapSI dict = { dict_keys, dict_vals, 0, 16 };
    {
        const char* w_arr[] = {"HELLO", "WORLD", "HOW", "ARE", "YOU", "THE", "QUICK", "BROWN", "FOX", "JUMPS", "OVER", "LAZY", "DOG"};
        size_t w_len = sizeof(w_arr) / sizeof(w_arr[0]);
        for (size_t __i = 0; __i < w_len; __i++) {
            const char* w = w_arr[__i];
            map_set_si(&dict, w, 1LL);
        }
    }
    return dict;
}

double get_english_count(const char* message) {
    const char* upper = to_upper(message);
    const char* cleaned = remove_non_letters(upper);
    const char* *possible = split_spaces(cleaned);
    size_t possible_len = split_spaces_len;
    long long matches = 0LL;
    long long total = 0LL;
    for (size_t __i = 0; __i < possible_len; __i++) {
        const char* w = possible[__i];
        if (strcmp(w, "") != 0) {
            total = total + 1LL;
            if (contains_str(ENGLISH_WORDS.keys, ENGLISH_WORDS.len, w)) {
                matches = matches + 1LL;
            }
        }
    }
    if (total == 0LL) {
        return 0.0;
    }
    return (double)(matches) / (double)(total);
}

long long is_english(const char* message, long long word_percentage, long long letter_percentage) {
    long long words_match = (get_english_count(message) * 100.0) >= (double)(word_percentage);
    long long num_letters = strlen(remove_non_letters(message));
    long long letters_pct;
    if (strlen(message) == 0LL) {
        letters_pct = 0.0;
    } else {
        letters_pct = ((double)(num_letters) / (double)(strlen(message))) * 100.0;
    }
    long long letters_match = letters_pct >= (double)(letter_percentage);
    return words_match && letters_match;
}

int main(void) {
    {
        long long __start = _now();
        ENGLISH_WORDS = load_dictionary();
        puts(str_int(is_english("Hello World", 20LL, 85LL)));
        puts(str_int(is_english("llold HorWd", 20LL, 85LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
