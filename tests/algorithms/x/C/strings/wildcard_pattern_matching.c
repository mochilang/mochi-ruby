// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_matrix_bool_len;
size_t *make_matrix_bool_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * make_matrix_bool(long long rows, long long cols, long long init);
long long match_pattern(const char* input_string, const char* pattern);
void user_main();
int main(void);

long long * * make_matrix_bool(long long rows, long long cols, long long init) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    for (int _ = 0LL; _ < rows; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < cols; _2++) {
            row = list_append_long_long(row, &row_len, init);
        }
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
    }
    return make_matrix_bool_lens = matrix_lens, make_matrix_bool_len = matrix_len, matrix;
}

long long match_pattern(const char* input_string, const char* pattern) {
    long long len_string = strlen(input_string) + 1LL;
    long long len_pattern = strlen(pattern) + 1LL;
    long long **dp = make_matrix_bool(len_string, len_pattern, 0LL);
    size_t dp_len = make_matrix_bool_len;
    size_t *dp_lens = make_matrix_bool_lens;
    size_t dp_lens_len = make_matrix_bool_len;
    long long *row0 = dp[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
    size_t row0_len = dp_lens[(int)(0LL)];
    row0[(int)(0LL)] = 1LL;
    dp[(int)(0LL)] = row0;
    long long j = 1LL;
    while (j < len_pattern) {
        row0 = dp[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
        if (strcmp(_substring(pattern, j - 1LL, j), "*") == 0) {
            row0[(int)(j)] = row0[(int)({long long _mochi_idx = j - 2LL; _mochi_idx < 0 ? row0_len + _mochi_idx : _mochi_idx;})];
        } else {
            row0[(int)(j)] = 0LL;
        }
        dp[(int)(0LL)] = row0;
        j = j + 1LL;
    }
    long long i = 1LL;
    while (i < len_string) {
        long long *row = dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = dp_lens[(int)(i)];
        long long j2 = 1LL;
        while (j2 < len_pattern) {
            const char* s_char = _substring(input_string, i - 1LL, i);
            const char* p_char = _substring(pattern, j2 - 1LL, j2);
            if ((strcmp(s_char, p_char) == 0) || (strcmp(p_char, ".") == 0)) {
                row[(int)(j2)] = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2 - 1LL; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
            } else {
                if (strcmp(p_char, "*") == 0) {
                    long long val = dp[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2 - 2LL; _mochi_idx < 0 ? dp_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
                    const char* prev_p = _substring(pattern, j2 - 2LL, j2 - 1LL);
                    if (!(val) && ((strcmp(prev_p, s_char) == 0) || (strcmp(prev_p, ".") == 0))) {
                        val = dp[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2; _mochi_idx < 0 ? dp_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})];
                    }
                    row[(int)(j2)] = val;
                } else {
                    row[(int)(j2)] = 0LL;
                }
            }
            j2 = j2 + 1LL;
        }
        dp[(int)(i)] = row;
        i = i + 1LL;
    }
    return dp[(int)({long long _mochi_idx = len_string - 1LL; _mochi_idx < 0 ? dp_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = len_pattern - 1LL; _mochi_idx < 0 ? dp_lens[(int)(len_string - 1LL)] + _mochi_idx : _mochi_idx;})];
}

void user_main() {
    if (!(match_pattern("aab", "c*a*b"))) {
        panic("case1 failed");
    }
    if (match_pattern("dabc", "*abc")) {
        panic("case2 failed");
    }
    if (match_pattern("aaa", "aa")) {
        panic("case3 failed");
    }
    if (!(match_pattern("aaa", "a.a"))) {
        panic("case4 failed");
    }
    if (match_pattern("aaab", "aa*")) {
        panic("case5 failed");
    }
    if (!(match_pattern("aaab", ".*"))) {
        panic("case6 failed");
    }
    if (match_pattern("a", "bbbb")) {
        panic("case7 failed");
    }
    if (match_pattern("", "bbbb")) {
        panic("case8 failed");
    }
    if (match_pattern("a", "")) {
        panic("case9 failed");
    }
    if (!(match_pattern("", ""))) {
        panic("case10 failed");
    }
    puts(str_int(match_pattern("aab", "c*a*b")));
    puts(str_int(match_pattern("dabc", "*abc")));
    puts(str_int(match_pattern("aaa", "aa")));
    puts(str_int(match_pattern("aaa", "a.a")));
    puts(str_int(match_pattern("aaab", "aa*")));
    puts(str_int(match_pattern("aaab", ".*")));
    puts(str_int(match_pattern("a", "bbbb")));
    puts(str_int(match_pattern("", "bbbb")));
    puts(str_int(match_pattern("a", "")));
    puts(str_int(match_pattern("", "")));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
