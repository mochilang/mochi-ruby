// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long all_digits(const char* s);
long long indian_phone_validator(const char* phone);
int main(void);

long long all_digits(const char* s) {
    if (strlen(s) == 0LL) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if ((strcmp(c, "0") < 0) || (strcmp(c, "9") > 0)) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long indian_phone_validator(const char* phone) {
    const char* s = phone;
    if ((strlen(s) >= 3LL) && (strcmp(_substring(s, 0LL, 3LL), "+91") == 0)) {
        s = _substring(s, 3LL, strlen(s));
        if (strlen(s) > 0LL) {
            const char* c = (const char[]){s[0LL], 0};
            if ((strcmp(c, "-") == 0) || (strcmp(c, " ") == 0)) {
                s = _substring(s, 1LL, strlen(s));
            }
        }
    }
    if ((strlen(s) > 0LL) && (strcmp((const char[]){s[0LL], 0}, "0") == 0)) {
        s = _substring(s, 1LL, strlen(s));
    }
    if ((strlen(s) >= 2LL) && (strcmp(_substring(s, 0LL, 2LL), "91") == 0)) {
        s = _substring(s, 2LL, strlen(s));
    }
    if (strlen(s) != 10LL) {
        return 0LL;
    }
    const char* first = (const char[]){s[0LL], 0};
    if (!(((strcmp(first, "7") == 0) || (strcmp(first, "8") == 0)) || (strcmp(first, "9") == 0))) {
        return 0LL;
    }
    if (!(all_digits(s))) {
        return 0LL;
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(indian_phone_validator("+91123456789")));
        puts(str_int(indian_phone_validator("+919876543210")));
        puts(str_int(indian_phone_validator("01234567896")));
        puts(str_int(indian_phone_validator("919876543218")));
        puts(str_int(indian_phone_validator("+91-1234567899")));
        puts(str_int(indian_phone_validator("+91-9876543218")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
