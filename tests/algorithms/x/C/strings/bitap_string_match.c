// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long pow2(long long n);
long long bit_and(long long a, long long b);
long long bit_or(long long a, long long b);
long long char_to_index(const char* ch);
long long bitap_string_match(const char* text, const char* pattern);
void user_main();
int main(void);

long long pow2(long long n) {
    long long res = 1LL;
    long long i = 0LL;
    while (i < n) {
        res = res * 2LL;
        i = i + 1LL;
    }
    return res;
}

long long bit_and(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    while ((x > 0LL) || (y > 0LL)) {
        if (((x % 2LL) == 1LL) && ((y % 2LL) == 1LL)) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long bit_or(long long a, long long b) {
    long long x = a;
    long long y = b;
    long long res = 0LL;
    long long bit = 1LL;
    while ((x > 0LL) || (y > 0LL)) {
        if (((x % 2LL) == 1LL) || ((y % 2LL) == 1LL)) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

long long char_to_index(const char* ch) {
    const char* letters = "abcdefghijklmnopqrstuvwxyz";
    long long i = 0LL;
    while (i < strlen(letters)) {
        if (strcmp(_substring(letters, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return 26LL;
}

long long bitap_string_match(const char* text, const char* pattern) {
    if (strcmp(pattern, "") == 0) {
        return 0LL;
    }
    long long m = strlen(pattern);
    if (m > strlen(text)) {
        return -1LL;
    }
    long long limit = pow2(m + 1LL);
    long long all_ones = limit - 1LL;
    long long *pattern_mask = NULL;
    size_t pattern_mask_len = 0;
    long long i = 0LL;
    while (i < 27LL) {
        pattern_mask = list_append_long_long(pattern_mask, &pattern_mask_len, all_ones);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < m) {
        const char* ch = _substring(pattern, i, i + 1LL);
        long long idx = char_to_index(ch);
        pattern_mask[(int)(idx)] = bit_and(pattern_mask[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? pattern_mask_len + _mochi_idx : _mochi_idx;})], all_ones - pow2(i));
        i = i + 1LL;
    }
    long long state = all_ones - 1LL;
    i = 0LL;
    while (i < strlen(text)) {
        const char* ch = _substring(text, i, i + 1LL);
        long long idx = char_to_index(ch);
        state = bit_or(state, pattern_mask[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? pattern_mask_len + _mochi_idx : _mochi_idx;})]);
        state = (state * 2LL) % limit;
        if (bit_and(state, pow2(m)) == 0LL) {
            return (i - m) + 1LL;
        }
        i = i + 1LL;
    }
    return -1LL;
}

void user_main() {
    puts(str_int(bitap_string_match("abdabababc", "ababc")));
    puts(str_int(bitap_string_match("abdabababc", "")));
    puts(str_int(bitap_string_match("abdabababc", "c")));
    puts(str_int(bitap_string_match("abdabababc", "fofosdfo")));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
