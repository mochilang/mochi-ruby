// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* LOWER = "abcdefghijklmnopqrstuvwxyz";
const char* UPPER = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* DIGITS = "0123456789";

long long is_lower(const char* ch);
long long is_upper(const char* ch);
long long is_digit(const char* ch);
long long is_alpha(const char* ch);
long long is_alnum(const char* ch);
const char* to_lower(const char* ch);
const char* camel_to_snake_case(const char* input_str);
void user_main();
int main(void);

long long is_lower(const char* ch) {
    long long i = 0LL;
    while (i < strlen(LOWER)) {
        if (strcmp((const char[]){LOWER[i], 0}, ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long is_upper(const char* ch) {
    long long i = 0LL;
    while (i < strlen(UPPER)) {
        if (strcmp((const char[]){UPPER[i], 0}, ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long is_digit(const char* ch) {
    long long i = 0LL;
    while (i < strlen(DIGITS)) {
        if (strcmp((const char[]){DIGITS[i], 0}, ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long is_alpha(const char* ch) {
    if (is_lower(ch)) {
        return 1LL;
    }
    if (is_upper(ch)) {
        return 1LL;
    }
    return 0LL;
}

long long is_alnum(const char* ch) {
    if (is_alpha(ch)) {
        return 1LL;
    }
    if (is_digit(ch)) {
        return 1LL;
    }
    return 0LL;
}

const char* to_lower(const char* ch) {
    long long i = 0LL;
    while (i < strlen(UPPER)) {
        if (strcmp((const char[]){UPPER[i], 0}, ch) == 0) {
            return (const char[]){LOWER[i], 0};
        }
        i = i + 1LL;
    }
    return ch;
}

const char* camel_to_snake_case(const char* input_str) {
    const char* snake_str = "";
    long long i = 0LL;
    long long prev_is_digit = 0LL;
    long long prev_is_alpha = 0LL;
    while (i < strlen(input_str)) {
        const char* ch = (const char[]){input_str[i], 0};
        if (is_upper(ch)) {
            snake_str = str_concat(str_concat(snake_str, "_"), to_lower(ch));
        } else {
            if (prev_is_digit && is_lower(ch)) {
                snake_str = str_concat(str_concat(snake_str, "_"), ch);
            } else {
                if (prev_is_alpha && is_digit(ch)) {
                    snake_str = str_concat(str_concat(snake_str, "_"), ch);
                } else {
                    if (!(is_alnum(ch))) {
                        snake_str = str_concat(snake_str, "_");
                    } else {
                        snake_str = str_concat(snake_str, ch);
                    }
                }
            }
        }
        prev_is_digit = is_digit(ch);
        prev_is_alpha = is_alpha(ch);
        i = i + 1LL;
    }
    if ((strlen(snake_str) > 0LL) && (strcmp((const char[]){snake_str[0LL], 0}, "_") == 0)) {
        snake_str = _substring(snake_str, 1LL, strlen(snake_str));
    }
    return snake_str;
}

void user_main() {
    puts(camel_to_snake_case("someRandomString"));
    puts(camel_to_snake_case("SomeRandomStr#ng"));
    puts(camel_to_snake_case("123SomeRandom123String123"));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
