// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Case Case;

struct Case {
    const char* text;
    long long expected;
};

Case test_data_init[8] = {(Case){.text = "MALAYALAM", .expected = 1LL}, (Case){.text = "String", .expected = 0LL}, (Case){.text = "rotor", .expected = 1LL}, (Case){.text = "level", .expected = 1LL}, (Case){.text = "A", .expected = 1LL}, (Case){.text = "BB", .expected = 1LL}, (Case){.text = "ABC", .expected = 0LL}, (Case){.text = "amanaplanacanalpanama", .expected = 1LL}};
Case *test_data = test_data_init;
size_t test_data_len = 8;

const char* reverse(const char* s);
long long is_palindrome(const char* s);
long long is_palindrome_traversal(const char* s);
long long is_palindrome_recursive(const char* s);
long long is_palindrome_slice(const char* s);
void user_main();
int main(void);

const char* reverse(const char* s) {
    const char* res = "";
    long long i = strlen(s) - 1LL;
    while (i >= 0LL) {
        res = str_concat(res, (const char[]){s[i], 0});
        i = i - 1LL;
    }
    return res;
}

long long is_palindrome(const char* s) {
    long long start_i = 0LL;
    long long end_i = strlen(s) - 1LL;
    while (start_i < end_i) {
        if (strcmp((const char[]){s[start_i], 0}, (const char[]){s[end_i], 0}) == 0) {
            start_i = start_i + 1LL;
            end_i = end_i - 1LL;
        } else {
            return 0LL;
        }
    }
    return 1LL;
}

long long is_palindrome_traversal(const char* s) {
    long long end = strlen(s) / 2LL;
    long long n = strlen(s);
    long long i = 0LL;
    while (i < end) {
        if (strcmp((const char[]){s[i], 0}, (const char[]){s[(n - i) - 1LL], 0}) != 0) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long is_palindrome_recursive(const char* s) {
    if (strlen(s) <= 1LL) {
        return 1LL;
    }
    if (strcmp((const char[]){s[0LL], 0}, (const char[]){s[strlen(s) - 1LL], 0}) == 0) {
        return is_palindrome_recursive(_substring(s, 1LL, strlen(s) - 1LL));
    }
    return 0LL;
}

long long is_palindrome_slice(const char* s) {
    return strcmp(s, reverse(s)) == 0;
}

void user_main() {
    {
        Case t_arr[] = {(Case){.text = "MALAYALAM", .expected = 1LL}, (Case){.text = "String", .expected = 0LL}, (Case){.text = "rotor", .expected = 1LL}, (Case){.text = "level", .expected = 1LL}, (Case){.text = "A", .expected = 1LL}, (Case){.text = "BB", .expected = 1LL}, (Case){.text = "ABC", .expected = 0LL}, (Case){.text = "amanaplanacanalpanama", .expected = 1LL}};
        size_t t_len = sizeof(t_arr) / sizeof(t_arr[0]);
        for (size_t __i = 0; __i < t_len; __i++) {
            Case t = t_arr[__i];
            const char* s = t.text;
            long long expected = t.expected;
            long long r1 = is_palindrome(s);
            long long r2 = is_palindrome_traversal(s);
            long long r3 = is_palindrome_recursive(s);
            long long r4 = is_palindrome_slice(s);
            if ((((r1 != expected) || (r2 != expected)) || (r3 != expected)) || (r4 != expected)) {
                panic("algorithm mismatch");
            }
            puts(str_concat(str_concat(s, " "), str_int(expected)));
        }
    }
    puts("a man a plan a canal panama");
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
