// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const char* LOWERCASE = "abcdefghijklmnopqrstuvwxyz";

const char* char_to_lower(const char* c);
const char* normalize(const char* input_str);
long long can_string_be_rearranged_as_palindrome_counter(const char* input_str);
long long can_string_be_rearranged_as_palindrome(const char* input_str);
int main(void);

const char* char_to_lower(const char* c) {
    long long i = 0LL;
    while (i < strlen(LETTERS)) {
        if (strcmp(c, _substring(LETTERS, i, i + 1LL)) == 0) {
            return _substring(LOWERCASE, i, i + 1LL);
        }
        i = i + 1LL;
    }
    return c;
}

const char* normalize(const char* input_str) {
    const char* res = "";
    long long i = 0LL;
    while (i < strlen(input_str)) {
        const char* ch = _substring(input_str, i, i + 1LL);
        const char* lc = char_to_lower(ch);
        if ((strcmp(lc, "a") >= 0) && (strcmp(lc, "z") <= 0)) {
            res = str_concat(res, lc);
        }
        i = i + 1LL;
    }
    return res;
}

long long can_string_be_rearranged_as_palindrome_counter(const char* input_str) {
    const char* s = normalize(input_str);
    static const char* freq_keys[16] = {};
    static int freq_vals[16] = {};
    size_t freq_len = 0;
    MapSI freq = { freq_keys, freq_vals, 0, 16 };
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (contains_str(freq.keys, freq.len, ch)) {
            map_set_si(&freq, ch, map_get_si(freq.keys, freq.vals, freq.len, ch) + 1LL);
        } else {
            map_set_si(&freq, ch, 1LL);
        }
        i = i + 1LL;
    }
    long long odd = 0LL;
    MapSI __tmp0 = freq;
    for (size_t __i = 0; __i < __tmp0.len; __i++) {
        const char* key = __tmp0.keys[__i];
        if ((map_get_si(freq.keys, freq.vals, freq.len, key) % 2LL) != 0LL) {
            odd = odd + 1LL;
        }
    }
    return odd < 2LL;
}

long long can_string_be_rearranged_as_palindrome(const char* input_str) {
    const char* s = normalize(input_str);
    if (strlen(s) == 0LL) {
        return 1LL;
    }
    static const char* character_freq_dict_keys[16] = {};
    static int character_freq_dict_vals[16] = {};
    size_t character_freq_dict_len = 0;
    MapSI character_freq_dict = { character_freq_dict_keys, character_freq_dict_vals, 0, 16 };
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* character = _substring(s, i, i + 1LL);
        if (contains_str(character_freq_dict.keys, character_freq_dict.len, character)) {
            map_set_si(&character_freq_dict, character, map_get_si(character_freq_dict.keys, character_freq_dict.vals, character_freq_dict.len, character) + 1LL);
        } else {
            map_set_si(&character_freq_dict, character, 1LL);
        }
        i = i + 1LL;
    }
    long long odd_char = 0LL;
    MapSI __tmp1 = character_freq_dict;
    for (size_t __i = 0; __i < __tmp1.len; __i++) {
        const char* character_key = __tmp1.keys[__i];
        long long character_count = map_get_si(character_freq_dict.keys, character_freq_dict.vals, character_freq_dict.len, character_key);
        if ((character_count % 2LL) != 0LL) {
            odd_char = odd_char + 1LL;
        }
    }
    return !(odd_char > 1LL);
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", can_string_be_rearranged_as_palindrome_counter("Momo"));
        printf("%d\n", can_string_be_rearranged_as_palindrome_counter("Mother"));
        printf("%d\n", can_string_be_rearranged_as_palindrome("Momo"));
        printf("%d\n", can_string_be_rearranged_as_palindrome("Mother"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
