// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t split_words_len;
size_t split_input_len;
size_t *split_input_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* * split_words(const char* s);
long long is_alnum(const char* c);
const char* * * split_input(const char* text);
const char* capitalize(const char* word);
const char* to_simple_case(const char* text);
const char* to_complex_case(const char* text, long long upper_flag, const char* sep);
const char* to_pascal_case(const char* text);
const char* to_camel_case(const char* text);
const char* to_snake_case(const char* text, long long upper_flag);
const char* to_kebab_case(const char* text, long long upper_flag);
int main(void);

const char* * split_words(const char* s) {
    const char* *words = NULL;
    size_t words_len = 0;
    const char* current = "";
    for (size_t __i = 0; __i < strlen(s); __i++) {
        char ch[2] = { s[__i], 0 };
        if (ch == " ") {
            if (strcmp(current, "") != 0) {
                words = list_append_str(words, &words_len, current);
                current = "";
            }
        } else {
            current = str_concat(current, str_int(ch));
        }
    }
    if (strcmp(current, "") != 0) {
        words = list_append_str(words, &words_len, current);
    }
    return split_words_len = words_len, words;
}

long long is_alnum(const char* c) {
    return (((strstr("0123456789", c) != NULL) || (strstr("abcdefghijklmnopqrstuvwxyz", c) != NULL)) || (strstr("ABCDEFGHIJKLMNOPQRSTUVWXYZ", c) != NULL)) || (strcmp(c, " ") == 0);
}

const char* * * split_input(const char* text) {
    const char* **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    const char* current = "";
    for (size_t __i = 0; __i < strlen(text); __i++) {
        char ch[2] = { text[__i], 0 };
        if (is_alnum(ch)) {
            current = str_concat(current, str_int(ch));
        } else {
            if (strcmp(current, "") != 0) {
                result = list_append_strptr(result, &result_len, split_words(current));
                result_lens = list_append_szt(result_lens, &result_lens_len, split_words_len);
                current = "";
            }
        }
    }
    if (strcmp(current, "") != 0) {
        result = list_append_strptr(result, &result_len, split_words(current));
        result_lens = list_append_szt(result_lens, &result_lens_len, split_words_len);
    }
    return split_input_lens = result_lens, split_input_len = result_len, result;
}

const char* capitalize(const char* word) {
    if (strlen(word) == 0LL) {
        return "";
    }
    if (strlen(word) == 1LL) {
        return str_upper(word);
    }
    return str_concat(str_upper(_substring(word, 0LL, 1LL)), str_lower(_substring(word, 1LL, strlen(word))));
}

const char* to_simple_case(const char* text) {
    const char* **parts = split_input(text);
    size_t parts_len = split_input_len;
    size_t *parts_lens = split_input_lens;
    size_t parts_lens_len = split_input_len;
    const char* res = "";
    for (size_t __i = 0; __i < parts_len; __i++) {
        const char* *sub = parts[__i];
        size_t sub_len = parts_lens[__i];
        for (size_t __i = 0; __i < sub_len; __i++) {
            const char* w = sub[__i];
            res = str_concat(res, capitalize(w));
        }
    }
    return res;
}

const char* to_complex_case(const char* text, long long upper_flag, const char* sep) {
    const char* **parts = split_input(text);
    size_t parts_len = split_input_len;
    size_t *parts_lens = split_input_lens;
    size_t parts_lens_len = split_input_len;
    const char* res = "";
    for (size_t __i = 0; __i < parts_len; __i++) {
        const char* *sub = parts[__i];
        size_t sub_len = parts_lens[__i];
        long long first = 1LL;
        for (size_t __i = 0; __i < sub_len; __i++) {
            const char* w = sub[__i];
            const char* word;
            if (upper_flag) {
                word = str_upper(w);
            } else {
                word = str_lower(w);
            }
            if (first) {
                res = str_concat(res, word);
                first = 0LL;
            } else {
                res = str_concat(str_concat(res, sep), word);
            }
        }
    }
    return res;
}

const char* to_pascal_case(const char* text) {
    return to_simple_case(text);
}

const char* to_camel_case(const char* text) {
    const char* s = to_simple_case(text);
    if (strlen(s) == 0LL) {
        return "";
    }
    return str_concat(str_lower(_substring(s, 0LL, 1LL)), _substring(s, 1LL, strlen(s)));
}

const char* to_snake_case(const char* text, long long upper_flag) {
    return to_complex_case(text, upper_flag, "_");
}

const char* to_kebab_case(const char* text, long long upper_flag) {
    return to_complex_case(text, upper_flag, "-");
}

int main(void) {
    {
        long long __start = _now();
        puts(to_pascal_case("one two 31235three4four"));
        puts(to_camel_case("one two 31235three4four"));
        puts(to_snake_case("one two 31235three4four", 1LL));
        puts(to_snake_case("one two 31235three4four", 0LL));
        puts(to_kebab_case("one two 31235three4four", 1LL));
        puts(to_kebab_case("one two 31235three4four", 0LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
