// Generated by Mochi 0.10.32 on 2025-08-24 09:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* * split(const char* s, const char* sep);
const char* join_with_space(const char* * xs, size_t xs_len);
const char* reverse_str(const char* s);
const char* reverse_letters(const char* sentence, long long length);
void test_reverse_letters();
void user_main();
int main(void);

const char* * split(const char* s, const char* sep) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = (const char[]){s[i], 0};
        if (strcmp(ch, sep) == 0) {
            res = list_append_str(res, &res_len, current);
            current = "";
        } else {
            current = str_concat(current, ch);
        }
        i = i + 1LL;
    }
    res = list_append_str(res, &res_len, current);
    return split_len = res_len, res;
}

const char* join_with_space(const char* * xs, size_t xs_len) {
    const char* s = "";
    long long i = 0LL;
    while (i < xs_len) {
        s = str_concat(s, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        if ((i + 1LL) < xs_len) {
            s = str_concat(s, " ");
        }
        i = i + 1LL;
    }
    return s;
}

const char* reverse_str(const char* s) {
    const char* res = "";
    long long i = strlen(s) - 1LL;
    while (i >= 0LL) {
        res = str_concat(res, (const char[]){s[i], 0});
        i = i - 1LL;
    }
    return res;
}

const char* reverse_letters(const char* sentence, long long length) {
    const char* *words = split(sentence, " ");
    size_t words_len = split_len;
    const char* *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < words_len) {
        const char* word = words[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? words_len + _mochi_idx : _mochi_idx;})];
        if (strlen(word) > length) {
            result = list_append_str(result, &result_len, reverse_str(word));
        } else {
            result = list_append_str(result, &result_len, word);
        }
        i = i + 1LL;
    }
    return join_with_space(result, result_len);
}

void test_reverse_letters() {
    if (strcmp(reverse_letters("Hey wollef sroirraw", 3LL), "Hey fellow warriors") != 0) {
        panic("test1 failed");
    }
    if (strcmp(reverse_letters("nohtyP is nohtyP", 2LL), "Python is Python") != 0) {
        panic("test2 failed");
    }
    if (strcmp(reverse_letters("1 12 123 1234 54321 654321", 0LL), "1 21 321 4321 12345 123456") != 0) {
        panic("test3 failed");
    }
    if (strcmp(reverse_letters("racecar", 0LL), "racecar") != 0) {
        panic("test4 failed");
    }
}

void user_main() {
    test_reverse_letters();
    puts(reverse_letters("Hey wollef sroirraw", 3LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
