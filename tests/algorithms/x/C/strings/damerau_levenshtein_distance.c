// Generated by Mochi 0.10.32 on 2025-08-24 09:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long damerau_levenshtein_distance(const char* first_string, const char* second_string);
int main(void);

long long damerau_levenshtein_distance(const char* first_string, const char* second_string) {
    long long len1 = strlen(first_string);
    long long len2 = strlen(second_string);
    long long **dp_matrix = NULL;
    size_t dp_matrix_len = 0;
    size_t *dp_matrix_lens = NULL;
    size_t dp_matrix_lens_len = 0;
    for (int _ = 0LL; _ < len1 + 1LL; _++) {
        long long *row = NULL;
        size_t row_len = 0;
        for (int _2 = 0LL; _2 < len2 + 1LL; _2++) {
            row = list_append_long_long(row, &row_len, 0LL);
        }
        dp_matrix = list_append_intptr(dp_matrix, &dp_matrix_len, row);
        dp_matrix_lens = list_append_szt(dp_matrix_lens, &dp_matrix_lens_len, row_len);
    }
    for (int i = 0LL; i < len1 + 1LL; i++) {
        long long *row = dp_matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = dp_matrix_lens[(int)(i)];
        row[(int)(0LL)] = i;
        dp_matrix[(int)(i)] = row;
    }
    long long *first_row = dp_matrix[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})];
    size_t first_row_len = dp_matrix_lens[(int)(0LL)];
    for (int j = 0LL; j < len2 + 1LL; j++) {
        first_row[(int)(j)] = j;
    }
    dp_matrix[(int)(0LL)] = first_row;
    for (int i = 1LL; i < len1 + 1LL; i++) {
        long long *row = dp_matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = dp_matrix_lens[(int)(i)];
        const char* first_char = _substring(first_string, i - 1LL, i);
        for (int j = 1LL; j < len2 + 1LL; j++) {
            const char* second_char = _substring(second_string, j - 1LL, j);
            long long cost;
            if (strcmp(first_char, second_char) == 0) {
                cost = 0LL;
            } else {
                cost = 1LL;
            }
            long long value = dp_matrix[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dp_matrix_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + 1LL;
            long long insertion = row[(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})] + 1LL;
            if (insertion < value) {
                value = insertion;
            }
            long long substitution = dp_matrix[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 1LL; _mochi_idx < 0 ? dp_matrix_lens[(int)(i - 1LL)] + _mochi_idx : _mochi_idx;})] + cost;
            if (substitution < value) {
                value = substitution;
            }
            row[(int)(j)] = value;
            if ((((i > 1LL) && (j > 1LL)) && (strcmp(_substring(first_string, i - 1LL, i), _substring(second_string, j - 2LL, j - 1LL)) == 0)) && (strcmp(_substring(first_string, i - 2LL, i - 1LL), _substring(second_string, j - 1LL, j)) == 0)) {
                long long transposition = dp_matrix[(int)({long long _mochi_idx = i - 2LL; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j - 2LL; _mochi_idx < 0 ? dp_matrix_lens[(int)(i - 2LL)] + _mochi_idx : _mochi_idx;})] + cost;
                if (transposition < row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]) {
                    row[(int)(j)] = transposition;
                }
            }
        }
        dp_matrix[(int)(i)] = row;
    }
    return dp_matrix[(int)({long long _mochi_idx = len1; _mochi_idx < 0 ? dp_matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = len2; _mochi_idx < 0 ? dp_matrix_lens[(int)(len1)] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(damerau_levenshtein_distance("cat", "cut")));
        puts(str_int(damerau_levenshtein_distance("kitten", "sitting")));
        puts(str_int(damerau_levenshtein_distance("hello", "world")));
        puts(str_int(damerau_levenshtein_distance("book", "back")));
        puts(str_int(damerau_levenshtein_distance("container", "containment")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
