// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t construct_highway_len;
size_t *construct_highway_lens;
size_t update_len;
size_t simulate_len;
size_t *simulate_lens;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long seed = 1LL;
long long NEG_ONE = -1LL;

long long user_rand();
long long randint(long long a, long long b);
double user_random();
long long * * construct_highway(long long number_of_cells, long long frequency, long long initial_speed, long long random_frequency, long long random_speed, long long max_speed);
long long get_distance(long long * highway_now, size_t highway_now_len, long long car_index);
long long * update(long long * highway_now, size_t highway_now_len, double probability, long long max_speed);
long long * * simulate(long long * * highway, size_t highway_len, size_t* highway_lens, size_t highway_lens_len, long long number_of_update, double probability, long long max_speed);
void user_main();
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed;
}

long long randint(long long a, long long b) {
    long long r = user_rand();
    return a + (r % ((b - a) + 1LL));
}

double user_random() {
    return (1.0 * user_rand()) / 2147483648.0;
}

long long * * construct_highway(long long number_of_cells, long long frequency, long long initial_speed, long long random_frequency, long long random_speed, long long max_speed) {
    long long *row = NULL;
    size_t row_len = 0;
    long long i = 0LL;
    while (i < number_of_cells) {
        row = list_append_long_long(row, &row_len, -1LL);
        i = i + 1LL;
    }
    long long **highway = NULL;
    size_t highway_len = 0;
    size_t *highway_lens = NULL;
    size_t highway_lens_len = 0;
    highway = list_append_long_longptr(highway, &highway_len, row);
    highway_lens = list_append_szt(highway_lens, &highway_lens_len, row_len);
    i = 0LL;
    if (initial_speed < 0LL) {
        initial_speed = 0LL;
    }
    while (i < number_of_cells) {
        long long speed = initial_speed;
        if (random_speed) {
            speed = randint(0LL, max_speed);
        }
        highway[(int)(0LL)][(int)(i)] = speed;
        long long step = frequency;
        if (random_frequency) {
            step = randint(1LL, max_speed * 2LL);
        }
        i = i + step;
    }
    return construct_highway_lens = highway_lens, construct_highway_len = highway_len, highway;
}

long long get_distance(long long * highway_now, size_t highway_now_len, long long car_index) {
    long long distance = 0LL;
    long long i = car_index + 1LL;
    while (i < highway_now_len) {
        if (highway_now[(int)(i)] > NEG_ONE) {
            return distance;
        }
        distance = distance + 1LL;
        i = i + 1LL;
    }
    return distance + get_distance(highway_now, highway_now_len, -1LL);
}

long long * update(long long * highway_now, size_t highway_now_len, double probability, long long max_speed) {
    size_t number_of_cells = highway_now_len;
    long long *next_highway = NULL;
    size_t next_highway_len = 0;
    long long i = 0LL;
    while (i < number_of_cells) {
        next_highway = list_append_long_long(next_highway, &next_highway_len, -1LL);
        i = i + 1LL;
    }
    long long car_index = 0LL;
    while (car_index < number_of_cells) {
        long long speed = highway_now[(int)(car_index)];
        if (speed > NEG_ONE) {
            long long new_speed = speed + 1LL;
            if (new_speed > max_speed) {
                new_speed = max_speed;
            }
            long long dn = get_distance(highway_now, highway_now_len, car_index) - 1LL;
            if (new_speed > dn) {
                new_speed = dn;
            }
            if (user_random() < probability) {
                new_speed = new_speed - 1LL;
                if (new_speed < 0LL) {
                    new_speed = 0LL;
                }
            }
            next_highway[(int)(car_index)] = new_speed;
        }
        car_index = car_index + 1LL;
    }
    return update_len = next_highway_len, next_highway;
}

long long * * simulate(long long * * highway, size_t highway_len, size_t* highway_lens, size_t highway_lens_len, long long number_of_update, double probability, long long max_speed) {
    long long number_of_cells = highway_lens[0LL];
    long long i = 0LL;
    while (i < number_of_update) {
        long long *next_speeds = update(highway[(int)(i)], highway_lens[i], probability, max_speed);
        size_t next_speeds_len = update_len;
        long long *real_next = NULL;
        size_t real_next_len = 0;
        long long j = 0LL;
        while (j < number_of_cells) {
            real_next = list_append_long_long(real_next, &real_next_len, -1LL);
            j = j + 1LL;
        }
        long long k = 0LL;
        while (k < number_of_cells) {
            long long speed = next_speeds[(int)(k)];
            if (speed > NEG_ONE) {
                long long index = (k + speed) % number_of_cells;
                real_next[(int)(index)] = speed;
            }
            k = k + 1LL;
        }
        highway = list_append_long_longptr(highway, &highway_len, real_next);
        highway_lens = list_append_szt(highway_lens, &highway_lens_len, real_next_len);
        i = i + 1LL;
    }
    return simulate_lens = highway_lens, simulate_len = highway_len, highway;
}

void user_main() {
    long long **ex1 = ({long long** __tmp2 = construct_highway(6LL, 3LL, 0LL, 0LL, 0LL, 2LL); simulate(__tmp2, construct_highway_len, construct_highway_lens, construct_highway_len, 2LL, 0.0, 2LL);});
    size_t ex1_len = simulate_len;
    size_t *ex1_lens = simulate_lens;
    size_t ex1_lens_len = simulate_len;
    puts(str_list_list_int(ex1, ex1_len, ex1_lens));
    long long **ex2 = ({long long** __tmp3 = construct_highway(5LL, 2LL, -2LL, 0LL, 0LL, 2LL); simulate(__tmp3, construct_highway_len, construct_highway_lens, construct_highway_len, 3LL, 0.0, 2LL);});
    size_t ex2_len = simulate_len;
    size_t *ex2_lens = simulate_lens;
    size_t ex2_lens_len = simulate_len;
    puts(str_list_list_int(ex2, ex2_len, ex2_lens));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
