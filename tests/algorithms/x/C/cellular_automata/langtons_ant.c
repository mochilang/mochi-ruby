// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t create_board_len;
size_t *create_board_lens;
size_t move_ant_len;
size_t langtons_ant_len;
size_t *langtons_ant_lens;

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * create_board(long long width, long long height);
long long * move_ant(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long x, long long y, long long direction);
long long * * langtons_ant(long long width, long long height, long long steps);
int main(void);

long long * * create_board(long long width, long long height) {
    long long **board = NULL;
    size_t board_len = 0;
    size_t *board_lens = NULL;
    size_t board_lens_len = 0;
    long long i = 0LL;
    while (i < height) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < width) {
            row = list_append_long_long(row, &row_len, 1LL);
            j = j + 1LL;
        }
        board = list_append_long_longptr(board, &board_len, row);
        board_lens = list_append_szt(board_lens, &board_lens_len, row_len);
        i = i + 1LL;
    }
    return create_board_lens = board_lens, create_board_len = board_len, board;
}

long long * move_ant(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long x, long long y, long long direction) {
    if (board[(int)(x)][(int)(y)]) {
        direction = (direction + 1LL) % 4LL;
    } else {
        direction = (direction + 3LL) % 4LL;
    }
    long long old_x = x;
    long long old_y = y;
    if (direction == 0LL) {
        x = x - 1LL;
    } else {
        if (direction == 1LL) {
            y = y + 1LL;
        } else {
            if (direction == 2LL) {
                x = x + 1LL;
            } else {
                y = y - 1LL;
            }
        }
    }
    board[(int)(old_x)][(int)(old_y)] = !(board[(int)(old_x)][(int)(old_y)]);
    {
        static long long __tmp0_arr[3];
        __tmp0_arr[0] = x;
        __tmp0_arr[1] = y;
        __tmp0_arr[2] = direction;
        move_ant_len = 3;
        return __tmp0_arr;
    }
}

long long * * langtons_ant(long long width, long long height, long long steps) {
    long long **board = create_board(width, height);
    size_t board_len = create_board_len;
    size_t *board_lens = create_board_lens;
    size_t board_lens_len = create_board_len;
    long long x = width / 2LL;
    long long y = height / 2LL;
    long long dir = 3LL;
    long long s = 0LL;
    while (s < steps) {
        long long *state = move_ant(board, board_len, board_lens, board_len, x, y, dir);
        size_t state_len = move_ant_len;
        x = state[(int)(0LL)];
        y = state[(int)(1LL)];
        dir = state[(int)(2LL)];
        s = s + 1LL;
    }
    return langtons_ant_lens = board_lens, langtons_ant_len = board_len, board;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
