// Generated by Mochi 0.10.32 on 2025-08-06 22:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t step_len;
size_t *step_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_long_longptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long glider_0[5] = {0LL, 1LL, 0LL, 0LL, 0LL};
long long glider_1[5] = {0LL, 0LL, 1LL, 0LL, 0LL};
long long glider_2[5] = {1LL, 1LL, 1LL, 0LL, 0LL};
long long glider_3[5] = {0LL, 0LL, 0LL, 0LL, 0LL};
long long glider_4[5] = {0LL, 0LL, 0LL, 0LL, 0LL};
long long *glider_init[5] = {glider_0, glider_1, glider_2, glider_3, glider_4};
long long **glider = glider_init;
size_t glider_len = 5;
size_t glider_lens_init[5] = {5, 5, 5, 5, 5};
size_t *glider_lens = glider_lens_init;
size_t glider_lens_len = 5;
long long **board = NULL;
size_t board_len = 0;
size_t *board_lens = NULL;
size_t board_lens_len = 0;
long long i = 0LL;

long long count_alive_neighbours(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long row, long long col);
long long next_state(long long current, long long alive);
long long * * step(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len);
void show(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len);
int main(void);

long long count_alive_neighbours(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len, long long row, long long col) {
    size_t size = board_len;
    long long alive = 0LL;
    long long dr = -1LL;
    while (dr < 2LL) {
        long long dc = -1LL;
        while (dc < 2LL) {
            long long nr = row + dr;
            long long nc = col + dc;
            if ((((!((dr == 0LL) && (dc == 0LL)) && (nr >= 0LL)) && (nr < size)) && (nc >= 0LL)) && (nc < size)) {
                if (board[(int)(nr)][(int)(nc)]) {
                    alive = alive + 1LL;
                }
            }
            dc = dc + 1LL;
        }
        dr = dr + 1LL;
    }
    return alive;
}

long long next_state(long long current, long long alive) {
    long long state = current;
    if (current) {
        if (alive < 2LL) {
            state = 0LL;
        } else {
            if ((alive == 2LL) || (alive == 3LL)) {
                state = 1LL;
            } else {
                state = 0LL;
            }
        }
    } else {
        if (alive == 3LL) {
            state = 1LL;
        }
    }
    return state;
}

long long * * step(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len) {
    size_t size = board_len;
    long long **new_board = NULL;
    size_t new_board_len = 0;
    size_t *new_board_lens = NULL;
    size_t new_board_lens_len = 0;
    long long r = 0LL;
    while (r < size) {
        long long *new_row = NULL;
        size_t new_row_len = 0;
        long long c = 0LL;
        while (c < size) {
            long long alive = count_alive_neighbours(board, board_len, board_lens, board_len, r, c);
            long long cell = board[(int)(r)][(int)(c)];
            long long updated = next_state(cell, alive);
            new_row = list_append_long_long(new_row, &new_row_len, updated);
            c = c + 1LL;
        }
        new_board = list_append_long_longptr(new_board, &new_board_len, new_row);
        new_board_lens = list_append_szt(new_board_lens, &new_board_lens_len, new_row_len);
        r = r + 1LL;
    }
    return step_lens = new_board_lens, step_len = new_board_len, new_board;
}

void show(long long * * board, size_t board_len, size_t* board_lens, size_t board_lens_len) {
    long long r = 0LL;
    while (r < board_len) {
        const char* line = "";
        long long c = 0LL;
        while (c < board_lens[r]) {
            if (board[(int)(r)][(int)(c)]) {
                line = str_concat(line, "#");
            } else {
                line = str_concat(line, ".");
            }
            c = c + 1LL;
        }
        puts(line);
        r = r + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        board = glider;
        puts("Initial");
        show(board, board_len, board_lens, board_len);
        while (i < 4LL) {
            board = step(board, board_len, board_lens, board_len);
            board_len = step_len;
            board_lens = step_lens;
            puts(str_concat("\nStep ", str_int(i + 1LL)));
            show(board, board_len, board_lens, board_len);
            i = i + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
