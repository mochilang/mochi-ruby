// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_last_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * remove_last(long long * xs, size_t xs_len);
long long sentinel_linear_search(long long * sequence, size_t sequence_len, long long target);
int main(void);

long long * remove_last(long long * xs, size_t xs_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < (xs_len - 1LL)) {
        res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return remove_last_len = res_len, res;
}

long long sentinel_linear_search(long long * sequence, size_t sequence_len, long long target) {
    long long *seq = sequence;
    size_t seq_len = sequence_len;
    seq = list_append_int_new(seq, seq_len, target);
    seq_len++;
    long long index = 0LL;
    while (seq[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? seq_len + _mochi_idx : _mochi_idx;})] != target) {
        index = index + 1LL;
    }
    seq = remove_last(seq, seq_len);
    seq_len = remove_last_len;
    if (index == seq_len) {
        return -1.0;
    }
    return index;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(sentinel_linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 0LL)));
        puts(str_int(sentinel_linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 15LL)));
        puts(str_int(sentinel_linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 5LL)));
        puts(str_int(sentinel_linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 6LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
