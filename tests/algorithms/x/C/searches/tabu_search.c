// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t copy_path_len;
size_t find_neighborhood_len;

size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

typedef struct { const char **keys; MapSI *vals; size_t len; size_t cap; } MapSMI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static MapSI map_get_smi(const char *keys[], const MapSI vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return (MapSI){0};
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Solution Solution;
typedef struct Swap Swap;

struct Solution {
    const char* *path;
    size_t path_len;
    long long cost;
};

struct Swap {
    const char* a;
    const char* b;
};

static Solution* list_append_Solution(Solution *arr, size_t *len, Solution val) {
    arr = realloc(arr, (*len + 1) * sizeof(Solution));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Swap* list_append_Swap(Swap *arr, size_t *len, Swap val) {
    arr = realloc(arr, (*len + 1) * sizeof(Swap));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char* graph_keys[21] = {"a", "b", "c", "d", "e"};
static const char* graph_vals_0_keys[4] = {"b", "c", "d", "e"};
static int graph_vals_0_vals[4] = {20LL, 18LL, 22LL, 26LL};
static const char* graph_vals_1_keys[4] = {"a", "c", "d", "e"};
static int graph_vals_1_vals[4] = {20LL, 10LL, 11LL, 12LL};
static const char* graph_vals_2_keys[4] = {"a", "b", "d", "e"};
static int graph_vals_2_vals[4] = {18LL, 10LL, 23LL, 24LL};
static const char* graph_vals_3_keys[4] = {"a", "b", "c", "e"};
static int graph_vals_3_vals[4] = {22LL, 11LL, 23LL, 40LL};
static const char* graph_vals_4_keys[4] = {"a", "b", "c", "d"};
static int graph_vals_4_vals[4] = {26LL, 12LL, 24LL, 40LL};
static MapSI graph_vals[21] = {{ graph_vals_0_keys, graph_vals_0_vals, 4, 20 }, { graph_vals_1_keys, graph_vals_1_vals, 4, 20 }, { graph_vals_2_keys, graph_vals_2_vals, 4, 20 }, { graph_vals_3_keys, graph_vals_3_vals, 4, 20 }, { graph_vals_4_keys, graph_vals_4_vals, 4, 20 }};
size_t graph_len = 5;
MapSMI graph = { graph_keys, graph_vals, 5, 21 };
Solution first;
Solution best;

long long path_cost(const char* * path, size_t path_len, MapSMI graph);
Solution generate_first_solution(MapSMI graph, const char* start);
const char* * copy_path(const char* * path, size_t path_len);
Solution * find_neighborhood(Solution sol, MapSMI graph);
Swap find_swap(const char* * a, size_t a_len, const char* * b, size_t b_len);
Solution tabu_search(Solution first, MapSMI graph, long long iters, long long size);
int main(void);

long long path_cost(const char* * path, size_t path_len, MapSMI graph) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < (path_len - 1LL)) {
        const char* u = path[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? path_len + _mochi_idx : _mochi_idx;})];
        const char* v = path[(int)({long long _mochi_idx = i + 1LL; _mochi_idx < 0 ? path_len + _mochi_idx : _mochi_idx;})];
        total = total + map_get_si(map_get_smi(graph.keys, graph.vals, graph.len, u).keys, map_get_smi(graph.keys, graph.vals, graph.len, u).vals, map_get_smi(graph.keys, graph.vals, graph.len, u).len, v);
        i = i + 1LL;
    }
    return total;
}

Solution generate_first_solution(MapSMI graph, const char* start) {
    const char* *path = NULL;
    size_t path_len = 0;
    const char* visiting = start;
    long long total = 0LL;
    while (path_len < graph.len) {
        path = list_append_str(path, &path_len, visiting);
        const char* best_node = "";
        long long best_cost = 1000000LL;
        MapSI __tmp0 = map_get_smi(graph.keys, graph.vals, graph.len, visiting);
        for (size_t __i = 0; __i < __tmp0.len; __i++) {
            const char* n = __tmp0.keys[__i];
            if (!(contains_str(path, path_len, n)) && (map_get_si(map_get_smi(graph.keys, graph.vals, graph.len, visiting).keys, map_get_smi(graph.keys, graph.vals, graph.len, visiting).vals, map_get_smi(graph.keys, graph.vals, graph.len, visiting).len, n) < best_cost)) {
                best_cost = map_get_si(map_get_smi(graph.keys, graph.vals, graph.len, visiting).keys, map_get_smi(graph.keys, graph.vals, graph.len, visiting).vals, map_get_smi(graph.keys, graph.vals, graph.len, visiting).len, n);
                best_node = n;
            }
        }
        if (strcmp(best_node, "") == 0) {
            break;
        }
        total = total + best_cost;
        visiting = best_node;
    }
    path = list_append_str(path, &path_len, start);
    total = total + map_get_si(map_get_smi(graph.keys, graph.vals, graph.len, visiting).keys, map_get_smi(graph.keys, graph.vals, graph.len, visiting).vals, map_get_smi(graph.keys, graph.vals, graph.len, visiting).len, start);
    return (Solution){.path = path, .path_len = path_len, .cost = total};
}

const char* * copy_path(const char* * path, size_t path_len) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < path_len) {
        res = list_append_str(res, &res_len, path[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? path_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return copy_path_len = res_len, res;
}

Solution * find_neighborhood(Solution sol, MapSMI graph) {
    Solution *neighbors = NULL;
    size_t neighbors_len = 0;
    long long i = 1LL;
    while (i < (sol.path_len - 1LL)) {
        long long j = 1LL;
        while (j < (sol.path_len - 1LL)) {
            if (i != j) {
                const char* *new_path = copy_path(sol.path, sol.path_len);
                size_t new_path_len = copy_path_len;
                const char* tmp = new_path[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? new_path_len + _mochi_idx : _mochi_idx;})];
                new_path[(int)(i)] = new_path[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? new_path_len + _mochi_idx : _mochi_idx;})];
                new_path[(int)(j)] = tmp;
                long long cost = path_cost(new_path, new_path_len, graph);
                neighbors = list_append_Solution(neighbors, &neighbors_len, (Solution){.path = new_path, .path_len = new_path_len, .cost = cost});
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return find_neighborhood_len = neighbors_len, neighbors;
}

Swap find_swap(const char* * a, size_t a_len, const char* * b, size_t b_len) {
    long long i = 0LL;
    while (i < a_len) {
        if (strcmp(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) != 0) {
            return (Swap){.a = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], .b = b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]};
        }
        i = i + 1LL;
    }
    return (Swap){.a = "", .b = ""};
}

Solution tabu_search(Solution first, MapSMI graph, long long iters, long long size) {
    Solution solution = first;
    Solution best = first;
    Swap *tabu = NULL;
    size_t tabu_len = 0;
    long long count_ = 0LL;
    while (count_ < iters) {
        Solution *neighborhood = find_neighborhood(solution, graph);
        size_t neighborhood_len = find_neighborhood_len;
        if (neighborhood_len == 0LL) {
            break;
        }
        Solution best_neighbor = neighborhood[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? neighborhood_len + _mochi_idx : _mochi_idx;})];
        Swap best_move = find_swap(solution.path, solution.path_len, best_neighbor.path, best_neighbor.path_len);
        long long i = 1LL;
        while (i < neighborhood_len) {
            Solution cand = neighborhood[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighborhood_len + _mochi_idx : _mochi_idx;})];
            Swap move = find_swap(solution.path, solution.path_len, cand.path, cand.path_len);
            long long forbidden = 0LL;
            long long t = 0LL;
            while (t < tabu_len) {
                if (((strcmp(tabu[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? tabu_len + _mochi_idx : _mochi_idx;})].a, move.a) == 0) && (strcmp(tabu[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? tabu_len + _mochi_idx : _mochi_idx;})].b, move.b) == 0)) || ((strcmp(tabu[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? tabu_len + _mochi_idx : _mochi_idx;})].a, move.b) == 0) && (strcmp(tabu[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? tabu_len + _mochi_idx : _mochi_idx;})].b, move.a) == 0))) {
                    forbidden = 1LL;
                }
                t = t + 1LL;
            }
            if ((forbidden == 0LL) && (cand.cost < best_neighbor.cost)) {
                best_neighbor = cand;
                best_move = move;
            }
            i = i + 1LL;
        }
        solution = best_neighbor;
        tabu = list_append_Swap(tabu, &tabu_len, best_move);
        if (tabu_len > size) {
            Swap *new_tab = NULL;
            size_t new_tab_len = 0;
            long long j = 1LL;
            while (j < tabu_len) {
                new_tab = list_append_Swap(new_tab, &new_tab_len, tabu[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? tabu_len + _mochi_idx : _mochi_idx;})]);
                j = j + 1LL;
            }
            tabu = new_tab;
            tabu_len = new_tab_len;
        }
        if (solution.cost < best.cost) {
            best = solution;
        }
        count_ = count_ + 1LL;
    }
    return best;
}

int main(void) {
    {
        long long __start = _now();
        first = generate_first_solution(graph, "a");
        best = tabu_search(first, graph, 4LL, 3LL);
        puts(str_list_str(best.path, best.path_len));
        puts(str_int(best.cost));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
