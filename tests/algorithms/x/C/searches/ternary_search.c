// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long precision = 10LL;

long long lin_search(long long left, long long right, long long * array, size_t array_len, long long target);
long long ite_ternary_search(long long * array, size_t array_len, long long target);
long long rec_ternary_search(long long left, long long right, long long * array, size_t array_len, long long target);
void user_main();
int main(void);

long long lin_search(long long left, long long right, long long * array, size_t array_len, long long target) {
    long long i = left;
    while (i < right) {
        if (array[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == target) {
            return i;
        }
        i = i + 1LL;
    }
    return -1.0;
}

long long ite_ternary_search(long long * array, size_t array_len, long long target) {
    long long left = 0LL;
    long long right = array_len - 1LL;
    while (left <= right) {
        if ((right - left) < precision) {
            long long idx = lin_search(left, right + 1LL, array, array_len, target);
            return idx;
        }
        long long one_third = left + ((right - left) / 3LL);
        long long two_third = right - ((right - left) / 3LL);
        if (array[(int)({long long _mochi_idx = one_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == target) {
            return one_third;
        }
        if (array[(int)({long long _mochi_idx = two_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == target) {
            return two_third;
        }
        if (target < array[(int)({long long _mochi_idx = one_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})]) {
            right = one_third - 1LL;
        } else {
            if (array[(int)({long long _mochi_idx = two_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] < target) {
                left = two_third + 1LL;
            } else {
                left = one_third + 1LL;
                right = two_third - 1LL;
            }
        }
    }
    return -1.0;
}

long long rec_ternary_search(long long left, long long right, long long * array, size_t array_len, long long target) {
    if (left <= right) {
        if ((right - left) < precision) {
            long long idx = lin_search(left, right + 1LL, array, array_len, target);
            return idx;
        }
        long long one_third = left + ((right - left) / 3LL);
        long long two_third = right - ((right - left) / 3LL);
        if (array[(int)({long long _mochi_idx = one_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == target) {
            return one_third;
        }
        if (array[(int)({long long _mochi_idx = two_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == target) {
            return two_third;
        }
        if (target < array[(int)({long long _mochi_idx = one_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})]) {
            return rec_ternary_search(left, one_third - 1LL, array, array_len, target);
        }
        if (array[(int)({long long _mochi_idx = two_third; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] < target) {
            return rec_ternary_search(two_third + 1LL, right, array, array_len, target);
        }
        return rec_ternary_search(one_third + 1LL, two_third - 1LL, array, array_len, target);
    }
    return -1.0;
}

void user_main() {
    long long *test_list = NULL;
    size_t test_list_len = 0;
    test_list = list_append_long_long(test_list, &test_list_len, 0LL);
    test_list = list_append_long_long(test_list, &test_list_len, 1LL);
    test_list = list_append_long_long(test_list, &test_list_len, 2LL);
    test_list = list_append_long_long(test_list, &test_list_len, 8LL);
    test_list = list_append_long_long(test_list, &test_list_len, 13LL);
    test_list = list_append_long_long(test_list, &test_list_len, 17LL);
    test_list = list_append_long_long(test_list, &test_list_len, 19LL);
    test_list = list_append_long_long(test_list, &test_list_len, 32LL);
    test_list = list_append_long_long(test_list, &test_list_len, 42LL);
    puts(str_int(ite_ternary_search(test_list, test_list_len, 3LL)));
    puts(str_int(ite_ternary_search(test_list, test_list_len, 13LL)));
    puts(str_int(rec_ternary_search(0LL, 8LL, test_list, test_list_len, 3LL)));
    puts(str_int(rec_ternary_search(0LL, 8LL, test_list, test_list_len, 13LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
