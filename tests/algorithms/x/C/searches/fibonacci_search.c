// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long example1_init[4] = {4LL, 5LL, 6LL, 7LL};
long long *example1 = example1_init;
size_t example1_len = 4;
long long example2_init[2] = {-18.0, 2LL};
long long *example2 = example2_init;
size_t example2_len = 2;
long long example3_init[7] = {0LL, 5LL, 10LL, 15LL, 20LL, 25LL, 30LL};
long long *example3 = example3_init;
size_t example3_len = 7;

long long fibonacci(long long k);
long long min_int(long long a, long long b);
long long fibonacci_search(long long * arr, size_t arr_len, long long val);
int main(void);

long long fibonacci(long long k) {
    if (k < 0LL) {
        panic("k must be >= 0");
    }
    long long a = 0LL;
    long long b = 1LL;
    long long i = 0LL;
    while (i < k) {
        long long tmp = a + b;
        a = b;
        b = tmp;
        i = i + 1LL;
    }
    return a;
}

long long min_int(long long a, long long b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}

long long fibonacci_search(long long * arr, size_t arr_len, long long val) {
    long long n = arr_len;
    long long m = 0LL;
    while (fibonacci(m) < n) {
        m = m + 1LL;
    }
    long long offset = 0LL;
    while (m > 0LL) {
        long long i = min_int(offset + fibonacci(m - 1LL), n - 1LL);
        long long item = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (item == val) {
            return i;
        } else {
            if (val < item) {
                m = m - 1LL;
            } else {
                offset = offset + fibonacci(m - 1LL);
                m = m - 2LL;
            }
        }
    }
    return -1.0;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(fibonacci_search(example1, example1_len, 4LL)));
        puts(str_int(fibonacci_search(example1, example1_len, -10.0)));
        puts(str_int(fibonacci_search(example2, example2_len, -18.0)));
        puts(str_int(fibonacci_search(example3, example3_len, 15LL)));
        puts(str_int(fibonacci_search(example3, example3_len, 17LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
