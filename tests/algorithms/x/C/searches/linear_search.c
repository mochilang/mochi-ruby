// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long linear_search(long long * sequence, size_t sequence_len, long long target);
long long rec_linear_search(long long * sequence, size_t sequence_len, long long low, long long high, long long target);
int main(void);

long long linear_search(long long * sequence, size_t sequence_len, long long target) {
    long long i = 0LL;
    while (i < sequence_len) {
        if (sequence[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sequence_len + _mochi_idx : _mochi_idx;})] == target) {
            return i;
        }
        i = i + 1LL;
    }
    return -1.0;
}

long long rec_linear_search(long long * sequence, size_t sequence_len, long long low, long long high, long long target) {
    if (!((((0LL <= high) && (high < sequence_len)) && (0LL <= low)) && (low < sequence_len))) {
        panic("Invalid upper or lower bound!");
    }
    if (high < low) {
        return -1.0;
    }
    if (sequence[(int)({long long _mochi_idx = low; _mochi_idx < 0 ? sequence_len + _mochi_idx : _mochi_idx;})] == target) {
        return low;
    }
    if (sequence[(int)({long long _mochi_idx = high; _mochi_idx < 0 ? sequence_len + _mochi_idx : _mochi_idx;})] == target) {
        return high;
    }
    return rec_linear_search(sequence, sequence_len, low + 1LL, high - 1LL, target);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 0LL)));
        puts(str_int(linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 15LL)));
        puts(str_int(linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 5LL)));
        puts(str_int(linear_search((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 6LL)));
        puts(str_int(rec_linear_search((long long[]){0LL, 30LL, 500LL, 100LL, 700LL}, 5, 0LL, 4LL, 0LL)));
        puts(str_int(rec_linear_search((long long[]){0LL, 30LL, 500LL, 100LL, 700LL}, 5, 0LL, 4LL, 700LL)));
        puts(str_int(rec_linear_search((long long[]){0LL, 30LL, 500LL, 100LL, 700LL}, 5, 0LL, 4LL, 30LL)));
        puts(str_int(rec_linear_search((long long[]){0LL, 30LL, 500LL, 100LL, 700LL}, 5, 0LL, 4LL, -6.0)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
