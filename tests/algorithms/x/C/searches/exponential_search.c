// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long is_sorted(long long * xs, size_t xs_len);
long long exponential_search(long long * arr, size_t arr_len, long long item);
int main(void);

long long is_sorted(long long * xs, size_t xs_len) {
    long long i = 1LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] > xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long exponential_search(long long * arr, size_t arr_len, long long item) {
    if (!(is_sorted(arr, arr_len))) {
        panic("sorted_collection must be sorted in ascending order");
    }
    if (arr_len == 0LL) {
        return -1.0;
    }
    if (arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == item) {
        return 0LL;
    }
    long long bound = 1LL;
    while ((bound < arr_len) && (arr[(int)({long long _mochi_idx = bound; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < item)) {
        bound = bound * 2LL;
    }
    long long left = bound / 2LL;
    long long right = bound;
    if (right >= arr_len) {
        right = arr_len - 1LL;
    }
    while (left <= right) {
        long long mid = left + ((right - left) / 2LL);
        if (arr[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == item) {
            return mid;
        }
        if (arr[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > item) {
            right = mid - 1LL;
        } else {
            left = mid + 1LL;
        }
    }
    return -1.0;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
