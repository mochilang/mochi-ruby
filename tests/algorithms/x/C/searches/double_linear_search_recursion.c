// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long search(long long * list_data, size_t list_data_len, long long key, long long left, long long right);
const char* user_main();
int main(void);

long long search(long long * list_data, size_t list_data_len, long long key, long long left, long long right) {
    long long r = right;
    if (r == 0LL) {
        r = list_data_len - 1LL;
    }
    if (left > r) {
        return -1.0;
    } else {
        if (list_data[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? list_data_len + _mochi_idx : _mochi_idx;})] == key) {
            return left;
        } else {
            if (list_data[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? list_data_len + _mochi_idx : _mochi_idx;})] == key) {
                return r;
            } else {
                return search(list_data, list_data_len, key, left + 1LL, r - 1LL);
            }
        }
    }
}

const char* user_main() {
    printf("%d\n", search((long long[]){0LL, 1LL, 2LL, 3LL, 4LL, 5LL, 6LL, 7LL, 8LL, 9LL, 10LL}, 11, 5LL, 0LL, 0LL));
    printf("%d\n", search((long long[]){1LL, 2LL, 4LL, 5LL, 3LL}, 5, 4LL, 0LL, 0LL));
    printf("%d\n", search((long long[]){1LL, 2LL, 4LL, 5LL, 3LL}, 5, 6LL, 0LL, 0LL));
    printf("%d\n", search((long long[]){5LL}, 1, 5LL, 0LL, 0LL));
    printf("%d\n", search(NULL, 0, 1LL, 0LL, 0LL));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
