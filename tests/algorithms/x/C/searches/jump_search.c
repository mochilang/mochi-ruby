// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long int_sqrt(long long n);
long long jump_search(long long * arr, size_t arr_len, long long item);
void user_main();
int main(void);

long long int_sqrt(long long n) {
    long long x = 0LL;
    while (((x + 1LL) * (x + 1LL)) <= n) {
        x = x + 1LL;
    }
    return x;
}

long long jump_search(long long * arr, size_t arr_len, long long item) {
    long long arr_size = arr_len;
    long long block_size = int_sqrt(arr_size);
    long long prev = 0LL;
    long long step = block_size;
    while ((step < arr_size) && (arr[(int)({long long _mochi_idx = step - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < item)) {
        prev = step;
        step = step + block_size;
        if (prev >= arr_size) {
            return -1.0;
        }
    }
    while ((prev < arr_size) && (arr[(int)({long long _mochi_idx = prev; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < item)) {
        prev = prev + 1LL;
        if (prev == step) {
            return -1.0;
        }
    }
    if ((prev < arr_size) && (arr[(int)({long long _mochi_idx = prev; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == item)) {
        return prev;
    }
    return -1.0;
}

void user_main() {
    puts(str_int(jump_search((long long[]){0LL, 1LL, 2LL, 3LL, 4LL, 5LL}, 6, 3LL)));
    puts(str_int(jump_search((double[]){-5.0, -2.0, -1.0}, 3, -1.0)));
    puts(str_int(jump_search((long long[]){0LL, 5LL, 10LL, 20LL}, 4, 8LL)));
    puts(str_int(jump_search((long long[]){0LL, 1LL, 1LL, 2LL, 3LL, 5LL, 8LL, 13LL, 21LL, 34LL, 55LL, 89LL, 144LL, 233LL, 377LL, 610LL}, 16, 55LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
