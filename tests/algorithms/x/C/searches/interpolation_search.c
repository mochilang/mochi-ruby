// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long interpolation_search(long long * arr, size_t arr_len, long long item);
long long interpolation_search_recursive(long long * arr, size_t arr_len, long long item, long long left, long long right);
long long interpolation_search_by_recursion(long long * arr, size_t arr_len, long long item);
int main(void);

long long interpolation_search(long long * arr, size_t arr_len, long long item) {
    long long left = 0LL;
    long long right = arr_len - 1LL;
    while (left <= right) {
        if (arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == arr[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
            if (arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == item) {
                return left;
            }
            return -1.0;
        }
        long long point = left + (((item - arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) * (right - left)) / (arr[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] - arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]));
        if ((point < 0LL) || (point >= arr_len)) {
            return -1.0;
        }
        long long current = arr[(int)({long long _mochi_idx = point; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (current == item) {
            return point;
        }
        if (point < left) {
            right = left;
            left = point;
        } else {
            if (point > right) {
                left = right;
                right = point;
            } else {
                if (item < current) {
                    right = point - 1LL;
                } else {
                    left = point + 1LL;
                }
            }
        }
    }
    return -1.0;
}

long long interpolation_search_recursive(long long * arr, size_t arr_len, long long item, long long left, long long right) {
    if (left > right) {
        return -1.0;
    }
    if (arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == arr[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
        if (arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == item) {
            return left;
        }
        return -1.0;
    }
    long long point = left + (((item - arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) * (right - left)) / (arr[(int)({long long _mochi_idx = right; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] - arr[(int)({long long _mochi_idx = left; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]));
    if ((point < 0LL) || (point >= arr_len)) {
        return -1.0;
    }
    if (arr[(int)({long long _mochi_idx = point; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == item) {
        return point;
    }
    if (point < left) {
        return interpolation_search_recursive(arr, arr_len, item, point, left);
    }
    if (point > right) {
        return interpolation_search_recursive(arr, arr_len, item, right, left);
    }
    if (arr[(int)({long long _mochi_idx = point; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > item) {
        return interpolation_search_recursive(arr, arr_len, item, left, point - 1LL);
    }
    return interpolation_search_recursive(arr, arr_len, item, point + 1LL, right);
}

long long interpolation_search_by_recursion(long long * arr, size_t arr_len, long long item) {
    return interpolation_search_recursive(arr, arr_len, item, 0LL, arr_len - 1LL);
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(interpolation_search((long long[]){1LL, 2LL, 3LL, 4LL, 5LL}, 5, 2LL)));
        puts(str_int(interpolation_search((long long[]){1LL, 2LL, 3LL, 4LL, 5LL}, 5, 6LL)));
        puts(str_int(interpolation_search_by_recursion((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 5LL)));
        puts(str_int(interpolation_search_by_recursion((long long[]){0LL, 5LL, 7LL, 10LL, 15LL}, 5, 100LL)));
        puts(str_int(interpolation_search_by_recursion((long long[]){5LL, 5LL, 5LL, 5LL, 5LL}, 5, 3LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
