// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t partition_len;
size_t *partition_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * partition(long long * data, size_t data_len, long long pivot);
long long quick_select(long long * items, size_t items_len, long long index);
double median(long long * items, size_t items_len);
int main(void);

long long * * partition(long long * data, size_t data_len, long long pivot) {
    long long *less = NULL;
    size_t less_len = 0;
    long long *equal = NULL;
    size_t equal_len = 0;
    long long *greater = NULL;
    size_t greater_len = 0;
    for (int i = 0LL; i < data_len; i++) {
        long long v = data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? data_len + _mochi_idx : _mochi_idx;})];
        if (v < pivot) {
            less = list_append_long_long(less, &less_len, v);
        } else {
            if (v > pivot) {
                greater = list_append_long_long(greater, &greater_len, v);
            } else {
                equal = list_append_long_long(equal, &equal_len, v);
            }
        }
    }
    {
        size_t *__tmp0_lens = malloc(3 * sizeof(size_t));
        __tmp0_lens[0] = less_len;
        __tmp0_lens[1] = equal_len;
        __tmp0_lens[2] = greater_len;
        long long** __tmp0_data = malloc(3 * sizeof(long long*));
        __tmp0_data[0] = less;
        __tmp0_data[1] = equal;
        __tmp0_data[2] = greater;
        partition_lens = __tmp0_lens;
        partition_len = 3;
        return __tmp0_data;
    }
}

long long quick_select(long long * items, size_t items_len, long long index) {
    if ((index < 0LL) || (index >= items_len)) {
        return -1.0;
    }
    long long pivot = items[(int)({long long _mochi_idx = items_len / 2LL; _mochi_idx < 0 ? items_len + _mochi_idx : _mochi_idx;})];
    long long **parts = partition(items, items_len, pivot);
    size_t parts_len = partition_len;
    size_t *parts_lens = partition_lens;
    size_t parts_lens_len = partition_len;
    long long *smaller = parts[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
    size_t smaller_len = parts_lens[(int)(0LL)];
    long long *equal = parts[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
    size_t equal_len = parts_lens[(int)(1LL)];
    long long *larger = parts[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
    size_t larger_len = parts_lens[(int)(2LL)];
    long long count_ = equal_len;
    long long m = smaller_len;
    if ((m <= index) && (index < (m + count_))) {
        return pivot;
    } else {
        if (index < m) {
            return quick_select(smaller, smaller_len, index);
        } else {
            return quick_select(larger, larger_len, index - (m + count_));
        }
    }
}

double median(long long * items, size_t items_len) {
    long long n = items_len;
    long long mid = n / 2LL;
    if ((n % 2LL) != 0LL) {
        return 1.0 * quick_select(items, items_len, mid);
    } else {
        long long low = quick_select(items, items_len, mid - 1LL);
        long long high = quick_select(items, items_len, mid);
        return (1.0 * (low + high)) / 2.0;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(quick_select((long long[]){2LL, 4LL, 5LL, 7LL, 899LL, 54LL, 32LL}, 7, 5LL)));
        puts(str_int(quick_select((long long[]){2LL, 4LL, 5LL, 7LL, 899LL, 54LL, 32LL}, 7, 1LL)));
        puts(str_int(quick_select((long long[]){5LL, 4LL, 3LL, 2LL}, 4, 2LL)));
        puts(str_int(quick_select((long long[]){3LL, 5LL, 7LL, 10LL, 2LL, 12LL}, 6, 3LL)));
        puts(str_float(median((long long[]){3LL, 2LL, 2LL, 9LL, 9LL}, 5)));
        puts(str_float(median((long long[]){2LL, 2LL, 9LL, 9LL, 9LL, 3LL}, 6)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
