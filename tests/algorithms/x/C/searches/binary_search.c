// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_sorted(long long * arr, size_t arr_len);
long long binary_search(long long * sorted_collection, size_t sorted_collection_len, long long item);
long long binary_search_by_recursion(long long * sorted_collection, size_t sorted_collection_len, long long item, long long left, long long right);
long long exponential_search(long long * sorted_collection, size_t sorted_collection_len, long long item);
void user_main();
int main(void);

long long is_sorted(long long * arr, size_t arr_len) {
    long long i = 1LL;
    while (i < arr_len) {
        if (arr[(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long binary_search(long long * sorted_collection, size_t sorted_collection_len, long long item) {
    if (!(is_sorted(sorted_collection, sorted_collection_len))) {
        return -1.0;
    }
    long long left = 0LL;
    long long right = sorted_collection_len - 1LL;
    while (left <= right) {
        long long midpoint = left + ((right - left) / 2LL);
        long long current_item = sorted_collection[(int)({long long _mochi_idx = midpoint; _mochi_idx < 0 ? sorted_collection_len + _mochi_idx : _mochi_idx;})];
        if (current_item == item) {
            return midpoint;
        }
        if (item < current_item) {
            right = midpoint - 1LL;
        } else {
            left = midpoint + 1LL;
        }
    }
    return -1.0;
}

long long binary_search_by_recursion(long long * sorted_collection, size_t sorted_collection_len, long long item, long long left, long long right) {
    if (right < left) {
        return -1.0;
    }
    long long midpoint = left + ((right - left) / 2LL);
    if (sorted_collection[(int)({long long _mochi_idx = midpoint; _mochi_idx < 0 ? sorted_collection_len + _mochi_idx : _mochi_idx;})] == item) {
        return midpoint;
    }
    if (sorted_collection[(int)({long long _mochi_idx = midpoint; _mochi_idx < 0 ? sorted_collection_len + _mochi_idx : _mochi_idx;})] > item) {
        return binary_search_by_recursion(sorted_collection, sorted_collection_len, item, left, midpoint - 1LL);
    }
    return binary_search_by_recursion(sorted_collection, sorted_collection_len, item, midpoint + 1LL, right);
}

long long exponential_search(long long * sorted_collection, size_t sorted_collection_len, long long item) {
    if (!(is_sorted(sorted_collection, sorted_collection_len))) {
        return -1.0;
    }
    if (sorted_collection_len == 0LL) {
        return -1.0;
    }
    long long bound = 1LL;
    while ((bound < sorted_collection_len) && (sorted_collection[(int)({long long _mochi_idx = bound; _mochi_idx < 0 ? sorted_collection_len + _mochi_idx : _mochi_idx;})] < item)) {
        bound = bound * 2LL;
    }
    long long left = bound / 2LL;
    long long right = 0;
    return binary_search_by_recursion(sorted_collection, sorted_collection_len, item, left, right);
}

void user_main() {
    long long *data = NULL;
    size_t data_len = 0;
    data = list_append_long_long(data, &data_len, 0LL);
    data = list_append_long_long(data, &data_len, 5LL);
    data = list_append_long_long(data, &data_len, 7LL);
    data = list_append_long_long(data, &data_len, 10LL);
    data = list_append_long_long(data, &data_len, 15LL);
    puts(str_int(binary_search(data, data_len, 0LL)));
    puts(str_int(binary_search(data, data_len, 15LL)));
    puts(str_int(binary_search(data, data_len, 5LL)));
    puts(str_int(binary_search(data, data_len, 6LL)));
    puts(str_int(binary_search_by_recursion(data, data_len, 0LL, 0LL, 4LL)));
    puts(str_int(binary_search_by_recursion(data, data_len, 15LL, 0LL, 4LL)));
    puts(str_int(binary_search_by_recursion(data, data_len, 5LL, 0LL, 4LL)));
    puts(str_int(binary_search_by_recursion(data, data_len, 6LL, 0LL, 4LL)));
    puts(str_int(exponential_search(data, data_len, 0LL)));
    puts(str_int(exponential_search(data, data_len, 15LL)));
    puts(str_int(exponential_search(data, data_len, 5LL)));
    puts(str_int(exponential_search(data, data_len, 6LL)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
