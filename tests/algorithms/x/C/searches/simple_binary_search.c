// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long arr1_init[9] = {0LL, 1LL, 2LL, 8LL, 13LL, 17LL, 19LL, 32LL, 42LL};
long long *arr1 = arr1_init;
size_t arr1_len = 9;
long long arr2_init[5] = {4LL, 4LL, 5LL, 6LL, 7LL};
long long *arr2 = arr2_init;
size_t arr2_len = 5;
long long arr3_init[2] = {-18.0, 2LL};
long long *arr3 = arr3_init;
size_t arr3_len = 2;
long long arr4_init[1] = {5LL};
long long *arr4 = arr4_init;
size_t arr4_len = 1;
long long *arr5 = NULL;
size_t arr5_len = 0;

long long binary_search(long long * arr, size_t arr_len, long long item);
int main(void);

long long binary_search(long long * arr, size_t arr_len, long long item) {
    long long low = 0LL;
    long long high = arr_len - 1LL;
    while (low <= high) {
        long long mid = (low + high) / 2LL;
        long long val = arr[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (val == item) {
            return 1LL;
        }
        if (item < val) {
            high = mid - 1LL;
        } else {
            low = mid + 1LL;
        }
    }
    return 0LL;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", binary_search(arr1, arr1_len, 3LL));
        printf("%d\n", binary_search(arr1, arr1_len, 13LL));
        printf("%d\n", binary_search(arr2, arr2_len, 4LL));
        printf("%d\n", binary_search(arr2, arr2_len, -10.0));
        printf("%d\n", binary_search(arr3, arr3_len, -18.0));
        printf("%d\n", binary_search(arr4, arr4_len, 5LL));
        printf("%d\n", binary_search(arr5, arr5_len, 1LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
