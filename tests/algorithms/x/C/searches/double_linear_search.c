// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t build_range_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long double_linear_search(long long * array, size_t array_len, long long search_item);
void user_main();
long long * build_range(long long n);
int main(void);

long long double_linear_search(long long * array, size_t array_len, long long search_item) {
    long long start_ind = 0LL;
    long long end_ind = array_len - 1LL;
    while (start_ind <= end_ind) {
        if (array[(int)({long long _mochi_idx = start_ind; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == search_item) {
            return start_ind;
        }
        if (array[(int)({long long _mochi_idx = end_ind; _mochi_idx < 0 ? array_len + _mochi_idx : _mochi_idx;})] == search_item) {
            return end_ind;
        }
        start_ind = start_ind + 1LL;
        end_ind = end_ind - 1LL;
    }
    return -1.0;
}

void user_main() {
    long long *data = build_range(100LL);
    size_t data_len = build_range_len;
    puts(str_int(double_linear_search(data, data_len, 40LL)));
}

long long * build_range(long long n) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        res = list_append_long_long(res, &res_len, i);
        i = i + 1LL;
    }
    return build_range_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
