// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t rect_len;
size_t multiply_len;
size_t apparent_power_len;

size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;

double user_abs(double x);
double to_radians(double deg);
double sin_taylor(double x);
double cos_taylor(double x);
double * rect(double mag, double angle);
double * multiply(double * a, size_t a_len, double * b, size_t b_len);
double * apparent_power(double voltage, double current, double voltage_angle, double current_angle);
long long approx_equal(double * a, size_t a_len, double * b, size_t b_len, double eps);
int main(void);

double user_abs(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double to_radians(double deg) {
    return (deg * PI) / 180.0;
}

double sin_taylor(double x) {
    double term = x;
    double sum = x;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = 2.0 * (double)(i);
        double k2 = k1 + 1.0;
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double cos_taylor(double x) {
    double term = 1.0;
    double sum = 1.0;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = (2.0 * (double)(i)) - 1.0;
        double k2 = 2.0 * (double)(i);
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double * rect(double mag, double angle) {
    double c = cos_taylor(angle);
    double s = sin_taylor(angle);
    {
        double* __tmp0_data = malloc(2 * sizeof(double));
        __tmp0_data[0] = mag * c;
        __tmp0_data[1] = mag * s;
        rect_len = 2;
        return __tmp0_data;
    }
}

double * multiply(double * a, size_t a_len, double * b, size_t b_len) {
    {
        double* __tmp1_data = malloc(2 * sizeof(double));
        __tmp1_data[0] = (a[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) - (a[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        __tmp1_data[1] = (a[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) + (a[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        multiply_len = 2;
        return __tmp1_data;
    }
}

double * apparent_power(double voltage, double current, double voltage_angle, double current_angle) {
    double vrad = to_radians(voltage_angle);
    double irad = to_radians(current_angle);
    double *vrect = rect(voltage, vrad);
    size_t vrect_len = rect_len;
    double *irect = rect(current, irad);
    size_t irect_len = rect_len;
    double *result = multiply(vrect, vrect_len, irect, irect_len);
    size_t result_len = multiply_len;
    return apparent_power_len = result_len, result;
}

long long approx_equal(double * a, size_t a_len, double * b, size_t b_len, double eps) {
    return (user_abs(a[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) < eps) && (user_abs(a[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]) < eps);
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
