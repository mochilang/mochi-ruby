// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double sqrt(double x);
double real_power(double apparent_power, double power_factor);
double reactive_power(double apparent_power, double power_factor);
int main(void);

double sqrt(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 10LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double real_power(double apparent_power, double power_factor) {
    if ((power_factor < (0.0 - 1.0)) || (power_factor > 1.0)) {
        panic("power_factor must be a valid float value between -1 and 1.");
    }
    return apparent_power * power_factor;
}

double reactive_power(double apparent_power, double power_factor) {
    if ((power_factor < (0.0 - 1.0)) || (power_factor > 1.0)) {
        panic("power_factor must be a valid float value between -1 and 1.");
    }
    return apparent_power * sqrt(1.0 - (power_factor * power_factor));
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(real_power(100.0, 0.9)));
        puts(str_float(real_power(0.0, 0.8)));
        puts(str_float(real_power(100.0, -0.9)));
        puts(str_float(reactive_power(100.0, 0.9)));
        puts(str_float(reactive_power(0.0, 0.8)));
        puts(str_float(reactive_power(100.0, -0.9)));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
