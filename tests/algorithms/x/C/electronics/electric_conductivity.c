// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Result {
    const char* kind;
    double value;
} Result;

double ELECTRON_CHARGE = 1.6021e-19;
Result r1;
Result r2;
Result r3;

Result electric_conductivity(double conductivity, double electron_conc, double mobility);
int main(void);

Result electric_conductivity(double conductivity, double electron_conc, double mobility) {
    long long zero_count = 0LL;
    if (conductivity == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (electron_conc == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (mobility == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (zero_count != 1LL) {
        panic("You cannot supply more or less than 2 values");
    }
    if (conductivity < 0.0) {
        panic("Conductivity cannot be negative");
    }
    if (electron_conc < 0.0) {
        panic("Electron concentration cannot be negative");
    }
    if (mobility < 0.0) {
        panic("mobility cannot be negative");
    }
    if (conductivity == 0.0) {
        return (Result){.kind = "conductivity", .value = (mobility * electron_conc) * ELECTRON_CHARGE};
    }
    if (electron_conc == 0.0) {
        return (Result){.kind = "electron_conc", .value = conductivity / (mobility * ELECTRON_CHARGE)};
    }
    return (Result){.kind = "mobility", .value = conductivity / (electron_conc * ELECTRON_CHARGE)};
}

int main(void) {
    {
        long long __start = _now();
        r1 = electric_conductivity(25.0, 100.0, 0.0);
        r2 = electric_conductivity(0.0, 1600.0, 200.0);
        r3 = electric_conductivity(1000.0, 0.0, 1200.0);
        puts(str_concat(str_concat(r1.kind, " "), str_float(r1.value)));
        puts(str_concat(str_concat(r2.kind, " "), str_float(r2.value)));
        puts(str_concat(str_concat(r3.kind, " "), str_float(r3.value)));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
