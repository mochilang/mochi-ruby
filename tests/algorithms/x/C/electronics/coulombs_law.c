// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

typedef struct MapSD { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double COULOMBS_CONSTANT = 8.988e+09;

double user_abs(double x);
double sqrtApprox(double x);
MapSD coulombs_law(double force, double charge1, double charge2, double distance);
void print_map(MapSD m);
int main(void);

double user_abs(double x) {
    if (x < 0.0) {
        return -(x);
    }
    return x;
}

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

MapSD coulombs_law(double force, double charge1, double charge2, double distance) {
    double charge_product = user_abs(charge1 * charge2);
    long long zero_count = 0LL;
    if (force == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (charge1 == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (charge2 == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (distance == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (zero_count != 1LL) {
        panic("One and only one argument must be 0");
    }
    if (distance < 0.0) {
        panic("Distance cannot be negative");
    }
    if (force == 0.0) {
        double f = (COULOMBS_CONSTANT * charge_product) / (distance * distance);
        {
            const char** __ret0_keys = malloc(1 * sizeof(char*));
            double *__ret0_vals = malloc(1 * sizeof(double));
            __ret0_keys[0] = "force";
            __ret0_vals[0] = f;
            MapSD __ret0 = { __ret0_keys, __ret0_vals, 1, 17 };
            return __ret0;
        }
    }
    if (charge1 == 0.0) {
        double c1 = (user_abs(force) * (distance * distance)) / (COULOMBS_CONSTANT * charge2);
        {
            const char** __ret1_keys = malloc(1 * sizeof(char*));
            double *__ret1_vals = malloc(1 * sizeof(double));
            __ret1_keys[0] = "charge1";
            __ret1_vals[0] = c1;
            MapSD __ret1 = { __ret1_keys, __ret1_vals, 1, 17 };
            return __ret1;
        }
    }
    if (charge2 == 0.0) {
        double c2 = (user_abs(force) * (distance * distance)) / (COULOMBS_CONSTANT * charge1);
        {
            const char** __ret2_keys = malloc(1 * sizeof(char*));
            double *__ret2_vals = malloc(1 * sizeof(double));
            __ret2_keys[0] = "charge2";
            __ret2_vals[0] = c2;
            MapSD __ret2 = { __ret2_keys, __ret2_vals, 1, 17 };
            return __ret2;
        }
    }
    double d = sqrtApprox((COULOMBS_CONSTANT * charge_product) / user_abs(force));
    {
        const char** __ret3_keys = malloc(1 * sizeof(char*));
        double *__ret3_vals = malloc(1 * sizeof(double));
        __ret3_keys[0] = "distance";
        __ret3_vals[0] = d;
        MapSD __ret3 = { __ret3_keys, __ret3_vals, 1, 17 };
        return __ret3;
    }
}

void print_map(MapSD m) {
    for (size_t __i = 0; __i < m.len; __i++) {
        const char* k = m.keys[__i];
        puts(str_concat(str_concat(str_concat(str_concat("{\"", k), "\": "), str_float(map_get_sd(m.keys, m.vals, m.len, k))), "}"));
    }
}

int main(void) {
    {
        long long __start = _now();
        print_map(coulombs_law(0.0, 3.0, 5.0, 2000.0));
        print_map(coulombs_law(10.0, 3.0, 5.0, 0.0));
        print_map(coulombs_law(10.0, 0.0, 5.0, 2000.0));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
