// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t pad_len;
size_t circular_convolution_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double *example1 = NULL;
size_t example1_len = 0;
double *example2 = NULL;
size_t example2_len = 0;
double *example3 = NULL;
size_t example3_len = 0;
double *example4 = NULL;
size_t example4_len = 0;

double user_floor(double x);
double user_pow10(long long n);
double roundn(double x, long long n);
double * pad(double * signal, size_t signal_len, long long target);
double * circular_convolution(double * a, size_t a_len, double * b, size_t b_len);
int main(void);

double user_floor(double x) {
    long long i = (int)(x);
    if ((double)(i) > x) {
        i = i - 1LL;
    }
    return (double)(i);
}

double user_pow10(long long n) {
    double p = 1.0;
    long long i = 0LL;
    while (i < n) {
        p = p * 10.0;
        i = i + 1LL;
    }
    return p;
}

double roundn(double x, long long n) {
    double m = user_pow10(n);
    return user_floor((x * m) + 0.5) / m;
}

double * pad(double * signal, size_t signal_len, long long target) {
    double *s = signal;
    size_t s_len = signal_len;
    while (s_len < target) {
        s = list_append_double(s, &s_len, 0.0);
    }
    return pad_len = s_len, s;
}

double * circular_convolution(double * a, size_t a_len, double * b, size_t b_len) {
    long long n1 = a_len;
    long long n2 = b_len;
    long long n;
    if (n1 > n2) {
        n = n1;
    } else {
        n = n2;
    }
    double *x = pad(a, a_len, n);
    size_t x_len = pad_len;
    double *y = pad(b, b_len, n);
    size_t y_len = pad_len;
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        double sum = 0.0;
        long long k = 0LL;
        while (k < n) {
            long long j = (i - k) % n;
            long long idx;
            if (j < 0LL) {
                idx = j + n;
            } else {
                idx = j;
            }
            sum = sum + (x[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})] * y[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? y_len + _mochi_idx : _mochi_idx;})]);
            k = k + 1LL;
        }
        res = list_append_double(res, &res_len, roundn(sum, 2LL));
        i = i + 1LL;
    }
    return circular_convolution_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        example1 = circular_convolution(({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 2.0; tmp[1] = 1.0; tmp[2] = 2.0; tmp[3] = -1.0; tmp;}), 4, ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = 1.0; tmp[1] = 2.0; tmp[2] = 3.0; tmp[3] = 4.0; tmp;}), 4);
        example1_len = circular_convolution_len;
        puts(str_list_double(example1, example1_len));
        example2 = circular_convolution(({double *tmp = malloc(8 * sizeof(double)); tmp[0] = 0.2; tmp[1] = 0.4; tmp[2] = 0.6; tmp[3] = 0.8; tmp[4] = 1.0; tmp[5] = 1.2; tmp[6] = 1.4; tmp[7] = 1.6; tmp;}), 8, ({double *tmp = malloc(8 * sizeof(double)); tmp[0] = 0.1; tmp[1] = 0.3; tmp[2] = 0.5; tmp[3] = 0.7; tmp[4] = 0.9; tmp[5] = 1.1; tmp[6] = 1.3; tmp[7] = 1.5; tmp;}), 8);
        example2_len = circular_convolution_len;
        puts(str_list_double(example2, example2_len));
        example3 = circular_convolution(({double *tmp = malloc(4 * sizeof(double)); tmp[0] = -1.0; tmp[1] = 1.0; tmp[2] = 2.0; tmp[3] = -2.0; tmp;}), 4, ({double *tmp = malloc(5 * sizeof(double)); tmp[0] = 0.5; tmp[1] = 1.0; tmp[2] = -1.0; tmp[3] = 2.0; tmp[4] = 0.75; tmp;}), 5);
        example3_len = circular_convolution_len;
        puts(str_list_double(example3, example3_len));
        example4 = circular_convolution(({double *tmp = malloc(5 * sizeof(double)); tmp[0] = 1.0; tmp[1] = -1.0; tmp[2] = 2.0; tmp[3] = 3.0; tmp[4] = -1.0; tmp;}), 5, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 1.0; tmp[1] = 2.0; tmp[2] = 3.0; tmp;}), 3);
        example4_len = circular_convolution_len;
        puts(str_list_double(example4, example4_len));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
