// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

typedef struct MapSD { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static void json_map_sd(MapSD m) {
    printf("{");
    for (size_t i = 0; i < m.len; i++) {
        if (i) printf(", " );
        printf("\"%s\": %s", m.keys[i], str_float(m.vals[i]));
    }
    printf("}\n");
}

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

MapSD ohms_law(double voltage, double current, double resistance);
int main(void);

MapSD ohms_law(double voltage, double current, double resistance) {
    long long zeros = 0LL;
    if (voltage == 0.0) {
        zeros = zeros + 1LL;
    }
    if (current == 0.0) {
        zeros = zeros + 1LL;
    }
    if (resistance == 0.0) {
        zeros = zeros + 1LL;
    }
    if (zeros != 1LL) {
        puts("One and only one argument must be 0");
        {
            const char** __ret0_keys = malloc(0 * sizeof(char*));
            double *__ret0_vals = malloc(0 * sizeof(double));
            MapSD __ret0 = { __ret0_keys, __ret0_vals, 0, 16 };
            return __ret0;
        }
    }
    if (resistance < 0.0) {
        puts("Resistance cannot be negative");
        {
            const char** __ret1_keys = malloc(0 * sizeof(char*));
            double *__ret1_vals = malloc(0 * sizeof(double));
            MapSD __ret1 = { __ret1_keys, __ret1_vals, 0, 16 };
            return __ret1;
        }
    }
    if (voltage == 0.0) {
        {
            const char** __ret2_keys = malloc(1 * sizeof(char*));
            double *__ret2_vals = malloc(1 * sizeof(double));
            __ret2_keys[0] = "voltage";
            __ret2_vals[0] = current * resistance;
            MapSD __ret2 = { __ret2_keys, __ret2_vals, 1, 17 };
            return __ret2;
        }
    }
    if (current == 0.0) {
        {
            const char** __ret3_keys = malloc(1 * sizeof(char*));
            double *__ret3_vals = malloc(1 * sizeof(double));
            __ret3_keys[0] = "current";
            __ret3_vals[0] = voltage / resistance;
            MapSD __ret3 = { __ret3_keys, __ret3_vals, 1, 17 };
            return __ret3;
        }
    }
    {
        const char** __ret4_keys = malloc(1 * sizeof(char*));
        double *__ret4_vals = malloc(1 * sizeof(double));
        __ret4_keys[0] = "resistance";
        __ret4_vals[0] = voltage / current;
        MapSD __ret4 = { __ret4_keys, __ret4_vals, 1, 17 };
        return __ret4;
    }
}

int main(void) {
    {
        long long __start = _now();
        json_map_sd(ohms_law(10.0, 0.0, 5.0));
        json_map_sd(ohms_law(-10.0, 1.0, 0.0));
        json_map_sd(ohms_law(0.0, -1.5, 2.0));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
