// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
typedef struct MapSD { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double sqrtApprox(double x);
MapSD electrical_impedance(double resistance, double reactance, double impedance);
int main(void);

double sqrtApprox(double x) {
    if (x <= 0.0) {
        return 0.0;
    }
    double guess = x / 2.0;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

MapSD electrical_impedance(double resistance, double reactance, double impedance) {
    long long zero_count = 0LL;
    if (resistance == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (reactance == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (impedance == 0.0) {
        zero_count = zero_count + 1LL;
    }
    if (zero_count != 1LL) {
        panic("One and only one argument must be 0");
    }
    if (resistance == 0.0) {
        double value = sqrtApprox((impedance * impedance) - (reactance * reactance));
        {
            const char** __ret0_keys = malloc(1 * sizeof(char*));
            double *__ret0_vals = malloc(1 * sizeof(double));
            __ret0_keys[0] = "resistance";
            __ret0_vals[0] = value;
            MapSD __ret0 = { __ret0_keys, __ret0_vals, 1, 17 };
            return __ret0;
        }
    } else {
        if (reactance == 0.0) {
            double value = sqrtApprox((impedance * impedance) - (resistance * resistance));
            {
                const char** __ret1_keys = malloc(1 * sizeof(char*));
                double *__ret1_vals = malloc(1 * sizeof(double));
                __ret1_keys[0] = "reactance";
                __ret1_vals[0] = value;
                MapSD __ret1 = { __ret1_keys, __ret1_vals, 1, 17 };
                return __ret1;
            }
        } else {
            if (impedance == 0.0) {
                double value = sqrtApprox((resistance * resistance) + (reactance * reactance));
                {
                    const char** __ret2_keys = malloc(1 * sizeof(char*));
                    double *__ret2_vals = malloc(1 * sizeof(double));
                    __ret2_keys[0] = "impedance";
                    __ret2_vals[0] = value;
                    MapSD __ret2 = { __ret2_keys, __ret2_vals, 1, 17 };
                    return __ret2;
                }
            } else {
                panic("Exactly one argument must be 0");
            }
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", electrical_impedance(3.0, 4.0, 0.0));
        printf("%d\n", electrical_impedance(0.0, 4.0, 5.0));
        printf("%d\n", electrical_impedance(3.0, 0.0, 5.0));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
