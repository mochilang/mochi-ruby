// Generated by Mochi 0.10.32 on 2025-08-25 22:56 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t sigmoid_len;
size_t swish_len;
size_t sigmoid_linear_unit_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double exp_approx(double x);
double * sigmoid(double * vector, size_t vector_len);
double * swish(double * vector, size_t vector_len, double beta);
double * sigmoid_linear_unit(double * vector, size_t vector_len);
long long approx_equal(double a, double b, double eps);
long long approx_equal_list(double * a, size_t a_len, double * b, size_t b_len, double eps);
void test_swish();
void user_main();
int main(void);

double exp_approx(double x) {
    double sum = 1.0;
    double term = 1.0;
    long long i = 1LL;
    while (i <= 20LL) {
        term = (term * x) / (double)(i);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double * sigmoid(double * vector, size_t vector_len) {
    double *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < vector_len) {
        double v = vector[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vector_len + _mochi_idx : _mochi_idx;})];
        double s = 1.0 / (1.0 + exp_approx(-(v)));
        result = list_append_double(result, &result_len, s);
        i = i + 1LL;
    }
    return sigmoid_len = result_len, result;
}

double * swish(double * vector, size_t vector_len, double beta) {
    double *result = NULL;
    size_t result_len = 0;
    long long i = 0LL;
    while (i < vector_len) {
        double v = vector[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vector_len + _mochi_idx : _mochi_idx;})];
        double s = 1.0 / (1.0 + exp_approx(-(beta) * v));
        result = list_append_double(result, &result_len, v * s);
        i = i + 1LL;
    }
    return swish_len = result_len, result;
}

double * sigmoid_linear_unit(double * vector, size_t vector_len) {
    {
        double* __ret8 = swish(vector, vector_len, 1.0);
        sigmoid_linear_unit_len = swish_len;
        return __ret8;
    }
}

long long approx_equal(double a, double b, double eps) {
    double diff;
    if (a > b) {
        diff = a - b;
    } else {
        diff = b - a;
    }
    return diff < eps;
}

long long approx_equal_list(double * a, size_t a_len, double * b, size_t b_len, double eps) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        if (!(approx_equal(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})], eps))) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void test_swish() {
    double *v = NULL;
    size_t v_len = 0;
    v = list_append_double(v, &v_len, -1.0);
    v = list_append_double(v, &v_len, 1.0);
    v = list_append_double(v, &v_len, 2.0);
    double eps = 0.001;
    if (!(({double* __tmp9 = sigmoid(v, v_len); size_t __tmp9_len = sigmoid_len; approx_equal_list(__tmp9, __tmp9_len, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = 0.26894142; tmp[1] = 0.73105858; tmp[2] = 0.88079708; tmp;}), 3, eps);}))) {
        panic("sigmoid incorrect");
    }
    if (!(({double* __tmp10 = sigmoid_linear_unit(v, v_len); size_t __tmp10_len = sigmoid_linear_unit_len; approx_equal_list(__tmp10, __tmp10_len, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -0.26894142; tmp[1] = 0.73105858; tmp[2] = 1.76159416; tmp;}), 3, eps);}))) {
        panic("sigmoid_linear_unit incorrect");
    }
    if (!(({double* __tmp11 = swish(v, v_len, 2.0); size_t __tmp11_len = swish_len; approx_equal_list(__tmp11, __tmp11_len, ({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -0.11920292; tmp[1] = 0.88079708; tmp[2] = 1.96402758; tmp;}), 3, eps);}))) {
        panic("swish incorrect");
    }
    if (!(({double* __tmp12 = swish(({double *tmp = malloc(1 * sizeof(double)); tmp[0] = -2.0; tmp;}), 1, 1.0); size_t __tmp12_len = swish_len; approx_equal_list(__tmp12, __tmp12_len, ({double *tmp = malloc(1 * sizeof(double)); tmp[0] = -0.23840584; tmp;}), 1, eps);}))) {
        panic("swish with parameter 1 incorrect");
    }
}

void user_main() {
    test_swish();
    puts(({double *__tmp13 = sigmoid(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -1.0; tmp[1] = 1.0; tmp[2] = 2.0; tmp;}), 3); char *__res = str_list_double(__tmp13, sigmoid_len); __res;}));
    puts(({double *__tmp14 = sigmoid_linear_unit(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -1.0; tmp[1] = 1.0; tmp[2] = 2.0; tmp;}), 3); char *__res = str_list_double(__tmp14, sigmoid_linear_unit_len); __res;}));
    puts(({double *__tmp15 = swish(({double *tmp = malloc(3 * sizeof(double)); tmp[0] = -1.0; tmp[1] = 1.0; tmp[2] = 2.0; tmp;}), 3, 2.0); char *__res = str_list_double(__tmp15, swish_len); __res;}));
    puts(({double *__tmp16 = swish(({double *tmp = malloc(1 * sizeof(double)); tmp[0] = -2.0; tmp;}), 1, 1.0); char *__res = str_list_double(__tmp16, swish_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
