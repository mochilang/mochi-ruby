// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t cramers_rule_2x2_len;

size_t append_len;
static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double * cramers_rule_2x2(double * eq1, size_t eq1_len, double * eq2, size_t eq2_len);
void test_cramers_rule_2x2();
void user_main();
int main(void);

double * cramers_rule_2x2(double * eq1, size_t eq1_len, double * eq2, size_t eq2_len) {
    if ((eq1_len != 3LL) || (eq2_len != 3LL)) {
        panic("Please enter a valid equation.");
    }
    if ((((eq1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? eq1_len + _mochi_idx : _mochi_idx;})] == 0.0) && (eq1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? eq1_len + _mochi_idx : _mochi_idx;})] == 0.0)) && (eq2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? eq2_len + _mochi_idx : _mochi_idx;})] == 0.0)) && (eq2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? eq2_len + _mochi_idx : _mochi_idx;})] == 0.0)) {
        panic("Both a & b of two equations can't be zero.");
    }
    double a1 = eq1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? eq1_len + _mochi_idx : _mochi_idx;})];
    double b1 = eq1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? eq1_len + _mochi_idx : _mochi_idx;})];
    double c1 = eq1[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? eq1_len + _mochi_idx : _mochi_idx;})];
    double a2 = eq2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? eq2_len + _mochi_idx : _mochi_idx;})];
    double b2 = eq2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? eq2_len + _mochi_idx : _mochi_idx;})];
    double c2 = eq2[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? eq2_len + _mochi_idx : _mochi_idx;})];
    double determinant = (a1 * b2) - (a2 * b1);
    double determinant_x = (c1 * b2) - (c2 * b1);
    double determinant_y = (a1 * c2) - (a2 * c1);
    if (determinant == 0.0) {
        if ((determinant_x == 0.0) && (determinant_y == 0.0)) {
            panic("Infinite solutions. (Consistent system)");
        }
        panic("No solution. (Inconsistent system)");
    }
    if ((determinant_x == 0.0) && (determinant_y == 0.0)) {
        {
            double* __tmp1_data = malloc(2 * sizeof(double));
            __tmp1_data[0] = 0.0;
            __tmp1_data[1] = 0.0;
            cramers_rule_2x2_len = 2;
            return __tmp1_data;
        }
    }
    double x = determinant_x / determinant;
    double y = determinant_y / determinant;
    {
        double* __tmp2_data = malloc(2 * sizeof(double));
        __tmp2_data[0] = x;
        __tmp2_data[1] = y;
        cramers_rule_2x2_len = 2;
        return __tmp2_data;
    }
}

void test_cramers_rule_2x2() {
    double *r1 = cramers_rule_2x2((double[]){2.0, 3.0, 0.0}, 3, (double[]){5.0, 1.0, 0.0}, 3);
    size_t r1_len = cramers_rule_2x2_len;
    if ((r1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? r1_len + _mochi_idx : _mochi_idx;})] != 0.0) || (r1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? r1_len + _mochi_idx : _mochi_idx;})] != 0.0)) {
        panic("Test1 failed");
    }
    double *r2 = cramers_rule_2x2((double[]){0.0, 4.0, 50.0}, 3, (double[]){2.0, 0.0, 26.0}, 3);
    size_t r2_len = cramers_rule_2x2_len;
    if ((r2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? r2_len + _mochi_idx : _mochi_idx;})] != 13.0) || (r2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? r2_len + _mochi_idx : _mochi_idx;})] != 12.5)) {
        panic("Test2 failed");
    }
}

void user_main() {
    test_cramers_rule_2x2();
    double* __tmp3 = cramers_rule_2x2((double[]){11.0, 2.0, 30.0}, 3, (double[]){1.0, 0.0, 4.0}, 3);
    puts(str_list_double(__tmp3, cramers_rule_2x2_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
