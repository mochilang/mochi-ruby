// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bubble_sort_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long matrix1_0[3] = {1LL, 3LL, 5LL};
long long matrix1_1[3] = {2LL, 6LL, 9LL};
long long matrix1_2[3] = {3LL, 6LL, 9LL};
long long *matrix1_init[3] = {matrix1_0, matrix1_1, matrix1_2};
long long **matrix1 = matrix1_init;
size_t matrix1_len = 3;
size_t matrix1_lens_init[3] = {3, 3, 3};
size_t *matrix1_lens = matrix1_lens_init;
size_t matrix1_lens_len = 3;
long long matrix2_0[3] = {1LL, 2LL, 3LL};
long long matrix2_1[3] = {4LL, 5LL, 6LL};
long long *matrix2_init[2] = {matrix2_0, matrix2_1};
long long **matrix2 = matrix2_init;
size_t matrix2_len = 2;
size_t matrix2_lens_init[2] = {3, 3};
size_t *matrix2_lens = matrix2_lens_init;
size_t matrix2_lens_len = 2;

long long * bubble_sort(long long * a, size_t a_len);
long long median(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
int main(void);

long long * bubble_sort(long long * a, size_t a_len) {
    long long *arr = a;
    size_t arr_len = a_len;
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while ((j + 1LL) < (n - i)) {
            if (arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] > arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) {
                long long temp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j)] = arr[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
                arr[(int)(j + 1LL)] = temp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bubble_sort_len = arr_len, arr;
}

long long median(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    long long *linear = NULL;
    size_t linear_len = 0;
    long long i = 0LL;
    while (i < matrix_len) {
        long long *row = matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = matrix_lens[(int)(i)];
        long long j = 0LL;
        while (j < row_len) {
            linear = list_append_long_long(linear, &linear_len, row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    long long *sorted = bubble_sort(linear, linear_len);
    size_t sorted_len = bubble_sort_len;
    long long mid = (sorted_len - 1LL) / 2LL;
    return sorted[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(median(matrix1, matrix1_len, matrix1_lens, matrix1_len)));
        puts(str_int(median(matrix2, matrix2_len, matrix2_lens, matrix2_len)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
