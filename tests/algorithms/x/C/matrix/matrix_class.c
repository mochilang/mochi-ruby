// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t matrix_columns_len;
size_t *matrix_columns_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double* list_append_double_new(const double *arr, size_t len, double val) {
    double *res = malloc((len + 1) * sizeof(double));
    if (arr && len) memcpy(res, arr, len * sizeof(double));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Matrix Matrix;

struct Matrix {
    double **data;
    size_t data_len;
    size_t *data_lens;
    size_t data_lens_len;
    long long rows;
    long long cols;
};

Matrix make_matrix(double * * values, size_t values_len, size_t* values_lens, size_t values_lens_len);
double * * matrix_columns(Matrix m);
Matrix matrix_identity(Matrix m);
double matrix_minor(Matrix m, long long r, long long c);
double matrix_cofactor(Matrix m, long long r, long long c);
Matrix matrix_minors(Matrix m);
Matrix matrix_cofactors(Matrix m);
double matrix_determinant(Matrix m);
long long matrix_is_invertible(Matrix m);
Matrix matrix_adjugate(Matrix m);
Matrix matrix_inverse(Matrix m);
Matrix matrix_add_row(Matrix m, double * row, size_t row_len);
Matrix matrix_add_column(Matrix m, double * col, size_t col_len);
Matrix matrix_mul_scalar(Matrix m, double s);
Matrix matrix_neg(Matrix m);
Matrix matrix_add(Matrix a, Matrix b);
Matrix matrix_sub(Matrix a, Matrix b);
double matrix_dot(double * row, size_t row_len, double * col, size_t col_len);
Matrix matrix_mul(Matrix a, Matrix b);
Matrix matrix_pow(Matrix m, long long p);
const char* matrix_to_string(Matrix m);
void user_main();
int main(void);

Matrix make_matrix(double * * values, size_t values_len, size_t* values_lens, size_t values_lens_len) {
    long long r = values_len;
    if (r == 0LL) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
    }
    long long c = values_lens[(int)(0LL)];
    long long i = 0LL;
    while (i < r) {
        if (values_lens[(int)(i)] != c) {
            return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
        }
        i = i + 1LL;
    }
    return (Matrix){.data = values, .data_len = values_len, .data_lens = values_lens, .data_lens_len = values_len, .rows = r, .cols = c};
}

double * * matrix_columns(Matrix m) {
    double **cols = NULL;
    size_t cols_len = 0;
    size_t *cols_lens = NULL;
    size_t cols_lens_len = 0;
    long long j = 0LL;
    while (j < m.cols) {
        double *col = NULL;
        size_t col_len = 0;
        long long i = 0LL;
        while (i < m.rows) {
            col = list_append_double(col, &col_len, m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        cols = list_append_doubleptr(cols, &cols_len, col);
        cols_lens = list_append_szt(cols_lens, &cols_lens_len, col_len);
        j = j + 1LL;
    }
    return matrix_columns_lens = cols_lens, matrix_columns_len = cols_len, cols;
}

Matrix matrix_identity(Matrix m) {
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m.cols) {
            long long v;
            if (i == j) {
                v = 1.0;
            } else {
                v = 0.0;
            }
            row = list_append_double(row, &row_len, v);
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = m.rows, .cols = m.cols};
}

double matrix_minor(Matrix m, long long r, long long c) {
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        if (i != r) {
            double *row = NULL;
            size_t row_len = 0;
            long long j = 0LL;
            while (j < m.cols) {
                if (j != c) {
                    row = list_append_double(row, &row_len, m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})]);
                }
                j = j + 1LL;
            }
            vals = list_append_doubleptr(vals, &vals_len, row);
            vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        }
        i = i + 1LL;
    }
    Matrix sub = (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = m.rows - 1LL, .cols = m.cols - 1LL};
    return matrix_determinant(sub);
}

double matrix_cofactor(Matrix m, long long r, long long c) {
    double minor = matrix_minor(m, r, c);
    if (((r + c) % 2LL) == 0LL) {
        return minor;
    }
    return -1.0 * minor;
}

Matrix matrix_minors(Matrix m) {
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m.cols) {
            row = list_append_double(row, &row_len, matrix_minor(m, i, j));
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = m.rows, .cols = m.cols};
}

Matrix matrix_cofactors(Matrix m) {
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m.cols) {
            row = list_append_double(row, &row_len, matrix_cofactor(m, i, j));
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = m.rows, .cols = m.cols};
}

double matrix_determinant(Matrix m) {
    if (m.rows != m.cols) {
        return 0.0;
    }
    if (m.rows == 0LL) {
        return 0.0;
    }
    if (m.rows == 1LL) {
        return m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})];
    }
    if (m.rows == 2LL) {
        return (m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})] * m.data[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_lens[1LL] + _mochi_idx : _mochi_idx;})]) - (m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})] * m.data[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_lens[1LL] + _mochi_idx : _mochi_idx;})]);
    }
    double sum = 0.0;
    long long j = 0LL;
    while (j < m.cols) {
        sum = sum + (m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})] * matrix_cofactor(m, 0LL, j));
        j = j + 1LL;
    }
    return sum;
}

long long matrix_is_invertible(Matrix m) {
    return matrix_determinant(m) != 0.0;
}

Matrix matrix_adjugate(Matrix m) {
    Matrix cof = matrix_cofactors(m);
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m.cols) {
            row = list_append_double(row, &row_len, cof.data[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? cof.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cof.data_lens[j] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = m.rows, .cols = m.cols};
}

Matrix matrix_inverse(Matrix m) {
    double det = matrix_determinant(m);
    if (det == 0.0) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
    }
    Matrix adj = matrix_adjugate(m);
    return matrix_mul_scalar(adj, 1.0 / det);
}

Matrix matrix_add_row(Matrix m, double * row, size_t row_len) {
    double **newData = m.data;
    size_t newData_len = m.data_len;
    size_t *newData_lens = m.data_lens;
    size_t newData_lens_len = m.data_len;
    newData = list_append_doubleptr(newData, &newData_len, row);
    newData_lens = list_append_szt(newData_lens, &newData_lens_len, row_len);
    return (Matrix){.data = newData, .data_len = newData_len, .data_lens = newData_lens, .data_lens_len = newData_len, .rows = m.rows + 1LL, .cols = m.cols};
}

Matrix matrix_add_column(Matrix m, double * col, size_t col_len) {
    double **newData = NULL;
    size_t newData_len = 0;
    size_t *newData_lens = NULL;
    size_t newData_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        newData = list_append_doubleptr(newData, &newData_len, list_append_double_new(m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})], m.data_lens[(int)(i)], col[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? col_len + _mochi_idx : _mochi_idx;})]));
        newData_lens = list_append_szt(newData_lens, &newData_lens_len, m.data_lens[i] + 1);
        i = i + 1LL;
    }
    return (Matrix){.data = newData, .data_len = newData_len, .data_lens = newData_lens, .data_lens_len = newData_len, .rows = m.rows, .cols = m.cols + 1LL};
}

Matrix matrix_mul_scalar(Matrix m, double s) {
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < m.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m.cols) {
            row = list_append_double(row, &row_len, m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})] * s);
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = m.rows, .cols = m.cols};
}

Matrix matrix_neg(Matrix m) {
    return matrix_mul_scalar(m, -1.0);
}

Matrix matrix_add(Matrix a, Matrix b) {
    if ((a.rows != b.rows) || (a.cols != b.cols)) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
    }
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < a.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < a.cols) {
            row = list_append_double(row, &row_len, a.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a.data_lens[i] + _mochi_idx : _mochi_idx;})] + b.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b.data_lens[i] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = a.rows, .cols = a.cols};
}

Matrix matrix_sub(Matrix a, Matrix b) {
    if ((a.rows != b.rows) || (a.cols != b.cols)) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
    }
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < a.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < a.cols) {
            row = list_append_double(row, &row_len, a.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a.data_lens[i] + _mochi_idx : _mochi_idx;})] - b.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b.data_lens[i] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = a.rows, .cols = a.cols};
}

double matrix_dot(double * row, size_t row_len, double * col, size_t col_len) {
    double sum = 0.0;
    long long i = 0LL;
    while (i < row_len) {
        sum = sum + (row[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})] * col[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? col_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return sum;
}

Matrix matrix_mul(Matrix a, Matrix b) {
    if (a.cols != b.rows) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
    }
    double **bcols = matrix_columns(b);
    size_t bcols_len = matrix_columns_len;
    size_t *bcols_lens = matrix_columns_lens;
    size_t bcols_lens_len = matrix_columns_len;
    double **vals = NULL;
    size_t vals_len = 0;
    size_t *vals_lens = NULL;
    size_t vals_lens_len = 0;
    long long i = 0LL;
    while (i < a.rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < b.cols) {
            row = list_append_double(row, &row_len, matrix_dot(a.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.data_len + _mochi_idx : _mochi_idx;})], a.data_lens[i], bcols[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? bcols_len + _mochi_idx : _mochi_idx;})], bcols_lens[(int)(j)]));
            j = j + 1LL;
        }
        vals = list_append_doubleptr(vals, &vals_len, row);
        vals_lens = list_append_szt(vals_lens, &vals_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = vals, .data_len = vals_len, .data_lens = vals_lens, .data_lens_len = vals_len, .rows = a.rows, .cols = b.cols};
}

Matrix matrix_pow(Matrix m, long long p) {
    if (p == 0LL) {
        return matrix_identity(m);
    }
    if (p < 0LL) {
        if (matrix_is_invertible(m)) {
            return matrix_pow(matrix_inverse(m), -(p));
        }
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .rows = 0LL, .cols = 0LL};
    }
    Matrix result = m;
    long long i = 1LL;
    while (i < p) {
        result = matrix_mul(result, m);
        i = i + 1LL;
    }
    return result;
}

const char* matrix_to_string(Matrix m) {
    if (m.rows == 0LL) {
        return "[]";
    }
    const char* s = "[";
    long long i = 0LL;
    while (i < m.rows) {
        s = str_concat(s, "[");
        long long j = 0LL;
        while (j < m.cols) {
            s = str_concat(s, str_float(m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})]));
            if (j < (m.cols - 1LL)) {
                s = str_concat(s, " ");
            }
            j = j + 1LL;
        }
        s = str_concat(s, "]");
        if (i < (m.rows - 1LL)) {
            s = str_concat(s, "\n ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

void user_main() {
    Matrix m = make_matrix(({int** tmp = malloc(3 * sizeof(int*)); tmp[0] = (double[]){1.0, 2.0, 3.0}; tmp[1] = (double[]){4.0, 5.0, 6.0}; tmp[2] = (double[]){7.0, 8.0, 9.0}; tmp;}), 3, ({size_t *tmp = malloc(3 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp[2] = 3; tmp;}), 3);
    puts(matrix_to_string(m));
    puts(({double **__tmp1 = matrix_columns(m); char *__res = str_list_list_double(__tmp1, matrix_columns_len, matrix_columns_lens); __res;}));
    puts(str_concat(str_concat(str_int(m.rows), ","), str_int(m.cols)));
    puts(str_int(matrix_is_invertible(m)));
    puts(matrix_to_string(matrix_identity(m)));
    puts(str_float(matrix_determinant(m)));
    puts(matrix_to_string(matrix_minors(m)));
    puts(matrix_to_string(matrix_cofactors(m)));
    puts(matrix_to_string(matrix_adjugate(m)));
    Matrix m2 = matrix_mul_scalar(m, 3.0);
    puts(matrix_to_string(m2));
    puts(matrix_to_string(matrix_add(m, m2)));
    puts(matrix_to_string(matrix_sub(m, m2)));
    puts(matrix_to_string(matrix_pow(m, 3LL)));
    Matrix m3 = matrix_add_row(m, (double[]){10.0, 11.0, 12.0}, 3);
    puts(matrix_to_string(m3));
    Matrix m4 = matrix_add_column(m2, (double[]){8.0, 16.0, 32.0}, 3);
    puts(matrix_to_string(matrix_mul(m3, m4)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
