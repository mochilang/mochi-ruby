// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t mat_bin_search_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long binary_search(long long * arr, size_t arr_len, long long lower_bound, long long upper_bound, long long value);
long long * mat_bin_search(long long value, long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
void user_main();
int main(void);

long long binary_search(long long * arr, size_t arr_len, long long lower_bound, long long upper_bound, long long value) {
    long long r = (lower_bound + upper_bound) / 2LL;
    if (arr[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] == value) {
        return r;
    }
    if (lower_bound >= upper_bound) {
        return -1LL;
    }
    if (arr[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < value) {
        return binary_search(arr, arr_len, r + 1LL, upper_bound, value);
    }
    return binary_search(arr, arr_len, lower_bound, r - 1LL, value);
}

long long * mat_bin_search(long long value, long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    long long index = 0LL;
    if (matrix[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? matrix_lens[(int)(index)] + _mochi_idx : _mochi_idx;})] == value) {
        {
            long long* __tmp2_data = malloc(2 * sizeof(long long));
            __tmp2_data[0] = index;
            __tmp2_data[1] = 0LL;
            mat_bin_search_len = 2;
            return __tmp2_data;
        }
    }
    while ((index < matrix_len) && (matrix[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? matrix_lens[(int)(index)] + _mochi_idx : _mochi_idx;})] < value)) {
        long long r = binary_search(matrix[(int)({long long _mochi_idx = index; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})], matrix_lens[(int)(index)], 0LL, matrix_lens[(int)(index)] - 1LL, value);
        if (r != -1LL) {
            {
                long long* __tmp3_data = malloc(2 * sizeof(long long));
                __tmp3_data[0] = index;
                __tmp3_data[1] = r;
                mat_bin_search_len = 2;
                return __tmp3_data;
            }
        }
        index = index + 1LL;
    }
    {
        long long* __tmp4_data = malloc(2 * sizeof(long long));
        __tmp4_data[0] = -1LL;
        __tmp4_data[1] = -1LL;
        mat_bin_search_len = 2;
        return __tmp4_data;
    }
}

void user_main() {
    long long *row = NULL;
    size_t row_len = 0;
    row = list_append_long_long(row, &row_len, 1LL);
    row = list_append_long_long(row, &row_len, 4LL);
    row = list_append_long_long(row, &row_len, 7LL);
    row = list_append_long_long(row, &row_len, 11LL);
    row = list_append_long_long(row, &row_len, 15LL);
    puts(str_int(binary_search(row, row_len, 0LL, 4LL, 1LL)));
    puts(str_int(binary_search(row, row_len, 0LL, 4LL, 23LL)));
    long long matrix_0[5] = {1LL, 4LL, 7LL, 11LL, 15LL};
    long long matrix_1[5] = {2LL, 5LL, 8LL, 12LL, 19LL};
    long long matrix_2[5] = {3LL, 6LL, 9LL, 16LL, 22LL};
    long long matrix_3[5] = {10LL, 13LL, 14LL, 17LL, 24LL};
    long long matrix_4[5] = {18LL, 21LL, 23LL, 26LL, 30LL};
    long long *matrix_init[5] = {matrix_0, matrix_1, matrix_2, matrix_3, matrix_4};
    long long **matrix = matrix_init;
    size_t matrix_len = 5;
    size_t matrix_lens_init[5] = {5, 5, 5, 5, 5};
    size_t *matrix_lens = matrix_lens_init;
    size_t matrix_lens_len = 5;
    puts(({long long *__tmp5 = mat_bin_search(1LL, matrix, matrix_len, matrix_lens, matrix_len); char *__res = str_list_int(__tmp5, mat_bin_search_len); __res;}));
    puts(({long long *__tmp6 = mat_bin_search(34LL, matrix, matrix_len, matrix_lens, matrix_len); char *__res = str_list_int(__tmp6, mat_bin_search_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
