// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t add_len;
size_t *add_lens;
size_t subtract_len;
size_t *subtract_lens;
size_t scalar_multiply_len;
size_t *scalar_multiply_lens;
size_t multiply_len;
size_t *multiply_lens;
size_t identity_len;
size_t *identity_lens;
size_t transpose_len;
size_t *transpose_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

void check_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
double * * add(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
double * * subtract(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
double * * scalar_multiply(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double s);
double * * multiply(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
double * * identity(long long n);
double * * transpose(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len);
void user_main();
int main(void);

void check_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    if ((mat_len < 2LL) || (mat_lens[(int)(0LL)] < 2LL)) {
        panic("Expected a matrix with at least 2x2 dimensions");
    }
}

double * * add(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    check_matrix(a, a_len, a_lens, a_len);
    check_matrix(b, b_len, b_lens, b_len);
    if ((a_len != b_len) || (a_lens[(int)(0LL)] != b_lens[(int)(0LL)])) {
        panic("Matrices must have the same dimensions");
    }
    long long rows = a_len;
    long long cols = a_lens[(int)(0LL)];
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < cols) {
            row = list_append_double(row, &row_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] + b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return add_lens = result_lens, add_len = result_len, result;
}

double * * subtract(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    check_matrix(a, a_len, a_lens, a_len);
    check_matrix(b, b_len, b_lens, b_len);
    if ((a_len != b_len) || (a_lens[(int)(0LL)] != b_lens[(int)(0LL)])) {
        panic("Matrices must have the same dimensions");
    }
    long long rows = a_len;
    long long cols = a_lens[(int)(0LL)];
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < cols) {
            row = list_append_double(row, &row_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return subtract_lens = result_lens, subtract_len = result_len, result;
}

double * * scalar_multiply(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double s) {
    check_matrix(a, a_len, a_lens, a_len);
    long long rows = a_len;
    long long cols = a_lens[(int)(0LL)];
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < cols) {
            row = list_append_double(row, &row_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * s);
            j = j + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return scalar_multiply_lens = result_lens, scalar_multiply_len = result_len, result;
}

double * * multiply(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, double * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    check_matrix(a, a_len, a_lens, a_len);
    check_matrix(b, b_len, b_lens, b_len);
    if (a_lens[(int)(0LL)] != b_len) {
        panic("Invalid dimensions for matrix multiplication");
    }
    long long rows = a_len;
    long long cols = b_lens[(int)(0LL)];
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < cols) {
            double sum = 0.0;
            long long k = 0LL;
            while (k < b_len) {
                sum = sum + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_double(row, &row_len, sum);
            j = j + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return multiply_lens = result_lens, multiply_len = result_len, result;
}

double * * identity(long long n) {
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            if (i == j) {
                row = list_append_double(row, &row_len, 1.0);
            } else {
                row = list_append_double(row, &row_len, 0.0);
            }
            j = j + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return identity_lens = result_lens, identity_len = result_len, result;
}

double * * transpose(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len) {
    check_matrix(a, a_len, a_lens, a_len);
    long long rows = a_len;
    long long cols = a_lens[(int)(0LL)];
    double **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long j = 0LL;
    while (j < cols) {
        double *row = NULL;
        size_t row_len = 0;
        long long i = 0LL;
        while (i < rows) {
            row = list_append_double(row, &row_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        result = list_append_doubleptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        j = j + 1LL;
    }
    return transpose_lens = result_lens, transpose_len = result_len, result;
}

void user_main() {
    double mat_a_0[2] = {12.0, 10.0};
    double mat_a_1[2] = {3.0, 9.0};
    double *mat_a_init[2] = {mat_a_0, mat_a_1};
    double **mat_a = mat_a_init;
    size_t mat_a_len = 2;
    size_t mat_a_lens_init[2] = {2, 2};
    size_t *mat_a_lens = mat_a_lens_init;
    size_t mat_a_lens_len = 2;
    double mat_b_0[2] = {3.0, 4.0};
    double mat_b_1[2] = {7.0, 4.0};
    double *mat_b_init[2] = {mat_b_0, mat_b_1};
    double **mat_b = mat_b_init;
    size_t mat_b_len = 2;
    size_t mat_b_lens_init[2] = {2, 2};
    size_t *mat_b_lens = mat_b_lens_init;
    size_t mat_b_lens_len = 2;
    double mat_c_0[3] = {3.0, 0.0, 2.0};
    double mat_c_1[3] = {2.0, 0.0, -2.0};
    double mat_c_2[3] = {0.0, 1.0, 1.0};
    double *mat_c_init[3] = {mat_c_0, mat_c_1, mat_c_2};
    double **mat_c = mat_c_init;
    size_t mat_c_len = 3;
    size_t mat_c_lens_init[3] = {3, 3, 3};
    size_t *mat_c_lens = mat_c_lens_init;
    size_t mat_c_lens_len = 3;
    puts(({double **__tmp6 = add(mat_a, mat_a_len, mat_a_lens, mat_a_len, mat_b, mat_b_len, mat_b_lens, mat_b_len); char *__res = str_list_list_double(__tmp6, add_len, add_lens); __res;}));
    puts(({double **__tmp7 = subtract(mat_a, mat_a_len, mat_a_lens, mat_a_len, mat_b, mat_b_len, mat_b_lens, mat_b_len); char *__res = str_list_list_double(__tmp7, subtract_len, subtract_lens); __res;}));
    puts(({double **__tmp8 = multiply(mat_a, mat_a_len, mat_a_lens, mat_a_len, mat_b, mat_b_len, mat_b_lens, mat_b_len); char *__res = str_list_list_double(__tmp8, multiply_len, multiply_lens); __res;}));
    puts(({double **__tmp9 = scalar_multiply(mat_a, mat_a_len, mat_a_lens, mat_a_len, 3.5); char *__res = str_list_list_double(__tmp9, scalar_multiply_len, scalar_multiply_lens); __res;}));
    puts(({double **__tmp10 = identity(5LL); char *__res = str_list_list_double(__tmp10, identity_len, identity_lens); __res;}));
    puts(({double **__tmp11 = transpose(mat_c, mat_c_len, mat_c_lens, mat_c_len); char *__res = str_list_list_double(__tmp11, transpose_len, transpose_lens); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
