// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t generate_large_matrix_len;
size_t *generate_large_matrix_lens;

size_t _slice_int_len;
size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **grid = NULL;
size_t grid_len = 0;
size_t *grid_lens = NULL;
size_t grid_lens_len = 0;
long long ***test_grids = NULL;
size_t test_grids_len = 0;
size_t *test_grids_lens = NULL;
size_t test_grids_lens_len = 0;
size_t **test_grids_lens_lens = NULL;
size_t test_grids_lens_lens_len = 0;
long long *results_bin = NULL;
size_t results_bin_len = 0;
long long i = 0LL;
long long *results_brute = NULL;
size_t results_brute_len = 0;
long long *results_break = NULL;
size_t results_break_len = 0;

long long * * generate_large_matrix();
long long find_negative_index(long long * arr, size_t arr_len);
long long count_negatives_binary_search(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
long long count_negatives_brute_force(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
long long count_negatives_brute_force_with_break(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
int main(void);

long long * * generate_large_matrix() {
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < 1000LL) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 1000LL - i;
        while (j > (-1000LL - i)) {
            row = list_append_long_long(row, &row_len, j);
            j = j - 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return generate_large_matrix_lens = result_lens, generate_large_matrix_len = result_len, result;
}

long long find_negative_index(long long * arr, size_t arr_len) {
    long long left = 0LL;
    long long right = arr_len - 1LL;
    if (arr_len == 0LL) {
        return 0LL;
    }
    if (arr[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] < 0LL) {
        return 0LL;
    }
    while (left <= right) {
        long long mid = (left + right) / 2LL;
        long long num = arr[(int)({long long _mochi_idx = mid; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        if (num < 0LL) {
            if (mid == 0LL) {
                return 0LL;
            }
            if (arr[(int)({long long _mochi_idx = mid - 1LL; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})] >= 0LL) {
                return mid;
            }
            right = mid - 1LL;
        } else {
            left = mid + 1LL;
        }
    }
    return arr_len;
}

long long count_negatives_binary_search(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long total = 0LL;
    long long bound = grid_lens[(int)(0LL)];
    long long i = 0LL;
    while (i < grid_len) {
        long long *row = grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = grid_lens[(int)(i)];
        long long idx = ({long long* __tmp1 = _slice_int(row, row_len, 0LL, bound, &(_slice_int_len)); size_t __tmp1_len = _slice_int_len; find_negative_index(__tmp1, __tmp1_len);});
        bound = idx;
        total = total + idx;
        i = i + 1LL;
    }
    return (grid_len * grid_lens[(int)(0LL)]) - total;
}

long long count_negatives_brute_force(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long count_ = 0LL;
    long long i = 0LL;
    while (i < grid_len) {
        long long *row = grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = grid_lens[(int)(i)];
        long long j = 0LL;
        while (j < row_len) {
            if (row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})] < 0LL) {
                count_ = count_ + 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return count_;
}

long long count_negatives_brute_force_with_break(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < grid_len) {
        long long *row = grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = grid_lens[(int)(i)];
        long long j = 0LL;
        while (j < row_len) {
            long long number = row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})];
            if (number < 0LL) {
                total = total + (row_len - j);
                break;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return total;
}

int main(void) {
    {
        long long __start = _now();
        grid = generate_large_matrix();
        grid_len = generate_large_matrix_len;
        grid_lens = generate_large_matrix_lens;
        test_grids = malloc(5 * sizeof(long long**));
        test_grids_len = 5;
        test_grids[0] = ({long long** tmp = malloc(4 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 3LL; tmp[2] = 2LL; tmp[3] = -1LL; tmp;}); tmp[1] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 2LL; tmp[2] = 1LL; tmp[3] = -1LL; tmp;}); tmp[2] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = -1LL; tmp[3] = -2LL; tmp;}); tmp[3] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = -1LL; tmp[1] = -1LL; tmp[2] = -2LL; tmp[3] = -3LL; tmp;}); tmp;});
        test_grids[1] = ({long long** tmp = malloc(2 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 2LL; tmp;}); tmp[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 0LL; tmp;}); tmp;});
        test_grids[2] = ({long long** tmp = malloc(1 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 7LL; tmp[1] = 7LL; tmp[2] = 6LL; tmp;}); tmp;});
        test_grids[3] = ({long long** tmp = malloc(2 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 7LL; tmp[1] = 7LL; tmp[2] = 6LL; tmp;}); tmp[1] = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = -1LL; tmp[1] = -2LL; tmp[2] = -3LL; tmp;}); tmp;});
        test_grids[4] = grid;
        test_grids_lens = ({size_t *tmp = malloc(5 * sizeof(size_t)); tmp[0] = 4; tmp[1] = 2; tmp[2] = 1; tmp[3] = 2; tmp[4] = grid_len; tmp;});
        test_grids_lens_len = 5;
        test_grids_lens_lens = ({size_t **tmp = malloc(5 * sizeof(size_t*)); tmp[0] = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 4; tmp[1] = 4; tmp[2] = 4; tmp[3] = 4; tmp;}); tmp[1] = ({size_t *tmp = malloc(2 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp;}); tmp[2] = ({size_t *tmp = malloc(1 * sizeof(size_t)); tmp[0] = 3; tmp;}); tmp[3] = ({size_t *tmp = malloc(2 * sizeof(size_t)); tmp[0] = 3; tmp[1] = 3; tmp;}); tmp[4] = grid_lens; tmp;});
        test_grids_lens_lens_len = 5;
        results_bin = malloc(0 * sizeof(long long));
        results_bin_len = 0;
        while (i < test_grids_len) {
            results_bin = list_append_long_long(results_bin, &results_bin_len, count_negatives_binary_search(test_grids[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? test_grids_len + _mochi_idx : _mochi_idx;})], test_grids_lens[(int)(i)], test_grids_lens_lens[(int)(i)], test_grids_lens[(int)(i)]));
            i = i + 1LL;
        }
        puts(str_list_int(results_bin, results_bin_len));
        results_brute = malloc(0 * sizeof(long long));
        results_brute_len = 0;
        i = 0LL;
        while (i < test_grids_len) {
            results_brute = list_append_long_long(results_brute, &results_brute_len, count_negatives_brute_force(test_grids[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? test_grids_len + _mochi_idx : _mochi_idx;})], test_grids_lens[(int)(i)], test_grids_lens_lens[(int)(i)], test_grids_lens[(int)(i)]));
            i = i + 1LL;
        }
        puts(str_list_int(results_brute, results_brute_len));
        results_break = malloc(0 * sizeof(long long));
        results_break_len = 0;
        i = 0LL;
        while (i < test_grids_len) {
            results_break = list_append_long_long(results_break, &results_break_len, count_negatives_brute_force_with_break(test_grids[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? test_grids_len + _mochi_idx : _mochi_idx;})], test_grids_lens[(int)(i)], test_grids_lens_lens[(int)(i)], test_grids_lens[(int)(i)]));
            i = i + 1LL;
        }
        puts(str_list_int(results_break, results_break_len));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
