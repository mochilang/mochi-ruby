// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t unique_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * unique(long long * nums, size_t nums_len);
long long array_equalization(long long * vector, size_t vector_len, long long step_size);
int main(void);

long long * unique(long long * nums, size_t nums_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < nums_len) {
        long long v = nums[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
        long long found = 0LL;
        long long j = 0LL;
        while (j < res_len) {
            if (res[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})] == v) {
                found = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (!(found)) {
            res = list_append_long_long(res, &res_len, v);
        }
        i = i + 1LL;
    }
    return unique_len = res_len, res;
}

long long array_equalization(long long * vector, size_t vector_len, long long step_size) {
    if (step_size <= 0LL) {
        error("Step size must be positive and non-zero.");
    }
    long long *elems = unique(vector, vector_len);
    size_t elems_len = unique_len;
    long long min_updates = vector_len;
    long long i = 0LL;
    while (i < elems_len) {
        long long target = elems[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? elems_len + _mochi_idx : _mochi_idx;})];
        long long idx = 0LL;
        long long updates = 0LL;
        while (idx < vector_len) {
            if (vector[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? vector_len + _mochi_idx : _mochi_idx;})] != target) {
                updates = updates + 1LL;
                idx = idx + step_size;
            } else {
                idx = idx + 1LL;
            }
        }
        if (updates < min_updates) {
            min_updates = updates;
        }
        i = i + 1LL;
    }
    return min_updates;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(array_equalization((long long[]){1LL, 1LL, 6LL, 2LL, 4LL, 6LL, 5LL, 1LL, 7LL, 2LL, 2LL, 1LL, 7LL, 2LL, 2LL}, 15, 4LL)));
        puts(str_int(array_equalization((long long[]){22LL, 81LL, 88LL, 71LL, 22LL, 81LL, 632LL, 81LL, 81LL, 22LL, 92LL}, 11, 2LL)));
        puts(str_int(array_equalization((long long[]){0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL}, 12, 5LL)));
        puts(str_int(array_equalization((long long[]){22LL, 22LL, 22LL, 33LL, 33LL, 33LL}, 6, 2LL)));
        puts(str_int(array_equalization((long long[]){1LL, 2LL, 3LL}, 3, 2147483647LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
