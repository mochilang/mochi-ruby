// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

void search_in_sorted_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len, long long m, long long n, double key);
void user_main();
int main(void);

void search_in_sorted_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len, long long m, long long n, double key) {
    long long i = m - 1LL;
    long long j = 0LL;
    while ((i >= 0LL) && (j < n)) {
        if (key == mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
            puts(str_concat(str_concat(str_concat(str_concat(str_concat("Key ", str_float(key)), " found at row- "), str_int(i + 1LL)), " column- "), str_int(j + 1LL)));
            return 0;
        }
        if (key < mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) {
            i = i - 1LL;
        } else {
            j = j + 1LL;
        }
    }
    puts(str_concat(str_concat("Key ", str_float(key)), " not found"));
}

void user_main() {
    double mat_0[3] = {2.0, 5.0, 7.0};
    double mat_1[3] = {4.0, 8.0, 13.0};
    double mat_2[3] = {9.0, 11.0, 15.0};
    double mat_3[3] = {12.0, 17.0, 20.0};
    double *mat_init[4] = {mat_0, mat_1, mat_2, mat_3};
    double **mat = mat_init;
    size_t mat_len = 4;
    size_t mat_lens_init[4] = {3, 3, 3, 3};
    size_t *mat_lens = mat_lens_init;
    size_t mat_lens_len = 4;
    search_in_sorted_matrix(mat, mat_len, mat_lens, mat_len, mat_len, mat_lens[(int)(0LL)], 5.0);
    search_in_sorted_matrix(mat, mat_len, mat_lens, mat_len, mat_len, mat_lens[(int)(0LL)], 21.0);
    double mat2_0[3] = {2.1, 5.0, 7.0};
    double mat2_1[3] = {4.0, 8.0, 13.0};
    double mat2_2[3] = {9.0, 11.0, 15.0};
    double mat2_3[3] = {12.0, 17.0, 20.0};
    double *mat2_init[4] = {mat2_0, mat2_1, mat2_2, mat2_3};
    double **mat2 = mat2_init;
    size_t mat2_len = 4;
    size_t mat2_lens_init[4] = {3, 3, 3, 3};
    size_t *mat2_lens = mat2_lens_init;
    size_t mat2_lens_len = 4;
    search_in_sorted_matrix(mat2, mat2_len, mat2_lens, mat2_len, mat2_len, mat2_lens[(int)(0LL)], 2.1);
    search_in_sorted_matrix(mat2, mat2_len, mat2_lens, mat2_len, mat2_len, mat2_lens[(int)(0LL)], 2.2);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
