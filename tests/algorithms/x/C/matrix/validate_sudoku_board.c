// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str_new(const char **arr, size_t len, const char *val) {
    const char **res = malloc((len + 1) * sizeof(const char*));
    if (arr && len) memcpy(res, arr, len * sizeof(const char*));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long NUM_SQUARES = 9LL;
const char* EMPTY_CELL = ".";
const char* valid_board_0[9] = {"5", "3", ".", ".", "7", ".", ".", ".", "."};
const char* valid_board_1[9] = {"6", ".", ".", "1", "9", "5", ".", ".", "."};
const char* valid_board_2[9] = {".", "9", "8", ".", ".", ".", ".", "6", "."};
const char* valid_board_3[9] = {"8", ".", ".", ".", "6", ".", ".", ".", "3"};
const char* valid_board_4[9] = {"4", ".", ".", "8", ".", "3", ".", ".", "1"};
const char* valid_board_5[9] = {"7", ".", ".", ".", "2", ".", ".", ".", "6"};
const char* valid_board_6[9] = {".", "6", ".", ".", ".", ".", "2", "8", "."};
const char* valid_board_7[9] = {".", ".", ".", "4", "1", "9", ".", ".", "5"};
const char* valid_board_8[9] = {".", ".", ".", ".", "8", ".", ".", "7", "9"};
const char* *valid_board_init[9] = {valid_board_0, valid_board_1, valid_board_2, valid_board_3, valid_board_4, valid_board_5, valid_board_6, valid_board_7, valid_board_8};
const char* **valid_board = valid_board_init;
size_t valid_board_len = 9;
size_t valid_board_lens_init[9] = {9, 9, 9, 9, 9, 9, 9, 9, 9};
size_t *valid_board_lens = valid_board_lens_init;
size_t valid_board_lens_len = 9;
const char* invalid_board_0[9] = {"8", "3", ".", ".", "7", ".", ".", ".", "."};
const char* invalid_board_1[9] = {"6", ".", ".", "1", "9", "5", ".", ".", "."};
const char* invalid_board_2[9] = {".", "9", "8", ".", ".", ".", ".", "6", "."};
const char* invalid_board_3[9] = {"8", ".", ".", ".", "6", ".", ".", ".", "3"};
const char* invalid_board_4[9] = {"4", ".", ".", "8", ".", "3", ".", ".", "1"};
const char* invalid_board_5[9] = {"7", ".", ".", ".", "2", ".", ".", ".", "6"};
const char* invalid_board_6[9] = {".", "6", ".", ".", ".", ".", "2", "8", "."};
const char* invalid_board_7[9] = {".", ".", ".", "4", "1", "9", ".", ".", "5"};
const char* invalid_board_8[9] = {".", ".", ".", ".", "8", ".", ".", "7", "9"};
const char* *invalid_board_init[9] = {invalid_board_0, invalid_board_1, invalid_board_2, invalid_board_3, invalid_board_4, invalid_board_5, invalid_board_6, invalid_board_7, invalid_board_8};
const char* **invalid_board = invalid_board_init;
size_t invalid_board_len = 9;
size_t invalid_board_lens_init[9] = {9, 9, 9, 9, 9, 9, 9, 9, 9};
size_t *invalid_board_lens = invalid_board_lens_init;
size_t invalid_board_lens_len = 9;

long long is_valid_sudoku_board(const char* * * board, size_t board_len, size_t* board_lens, size_t board_lens_len);
int main(void);

long long is_valid_sudoku_board(const char* * * board, size_t board_len, size_t* board_lens, size_t board_lens_len) {
    if (board_len != NUM_SQUARES) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < NUM_SQUARES) {
        if (board_lens[(int)(i)] != NUM_SQUARES) {
            return 0LL;
        }
        i = i + 1LL;
    }
    const char* **rows = NULL;
    size_t rows_len = 0;
    size_t *rows_lens = NULL;
    size_t rows_lens_len = 0;
    const char* **cols = NULL;
    size_t cols_len = 0;
    size_t *cols_lens = NULL;
    size_t cols_lens_len = 0;
    const char* **boxes = NULL;
    size_t boxes_len = 0;
    size_t *boxes_lens = NULL;
    size_t boxes_lens_len = 0;
    i = 0LL;
    while (i < NUM_SQUARES) {
        rows = list_append_strptr(rows, &rows_len, ({const char** tmp = malloc(0 * sizeof(const char*)); tmp;}));
        rows_lens = list_append_szt(rows_lens, &rows_lens_len, 0);
        cols = list_append_strptr(cols, &cols_len, ({const char** tmp = malloc(0 * sizeof(const char*)); tmp;}));
        cols_lens = list_append_szt(cols_lens, &cols_lens_len, 0);
        boxes = list_append_strptr(boxes, &boxes_len, ({const char** tmp = malloc(0 * sizeof(const char*)); tmp;}));
        boxes_lens = list_append_szt(boxes_lens, &boxes_lens_len, 0);
        i = i + 1LL;
    }
    for (int r = 0LL; r < NUM_SQUARES; r++) {
        for (int c = 0LL; c < NUM_SQUARES; c++) {
            const char* value = board[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? board_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = c; _mochi_idx < 0 ? board_lens[(int)(r)] + _mochi_idx : _mochi_idx;})];
            if (strcmp(value, EMPTY_CELL) == 0) {
                continue;
            }
            long long box = ((int)(r / 3LL) * 3LL) + (int)(c / 3LL);
            if (((contains_str(rows[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? rows_len + _mochi_idx : _mochi_idx;})], rows_lens[(int)(r)], value)) || (contains_str(cols[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? cols_len + _mochi_idx : _mochi_idx;})], cols_lens[(int)(c)], value))) || (contains_str(boxes[(int)({long long _mochi_idx = box; _mochi_idx < 0 ? boxes_len + _mochi_idx : _mochi_idx;})], boxes_lens[(int)(box)], value))) {
                return 0LL;
            }
            rows[(int)(r)] = list_append_str_new(rows[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? rows_len + _mochi_idx : _mochi_idx;})], rows_lens[(int)(r)], value);
            cols[(int)(c)] = list_append_str_new(cols[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? cols_len + _mochi_idx : _mochi_idx;})], cols_lens[(int)(c)], value);
            boxes[(int)(box)] = list_append_str_new(boxes[(int)({long long _mochi_idx = box; _mochi_idx < 0 ? boxes_len + _mochi_idx : _mochi_idx;})], boxes_lens[(int)(box)], value);
        }
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", is_valid_sudoku_board(valid_board, valid_board_len, valid_board_lens, valid_board_len));
        printf("%d\n", is_valid_sudoku_board(invalid_board, invalid_board_len, invalid_board_lens, invalid_board_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
