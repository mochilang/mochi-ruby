// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t matrix_multiply_len;
size_t *matrix_multiply_lens;
size_t matrix_multiply_recursive_len;
size_t *matrix_multiply_recursive_lens;

size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long matrix_1_to_4_0[2] = {1LL, 2LL};
long long matrix_1_to_4_1[2] = {3LL, 4LL};
long long *matrix_1_to_4_init[2] = {matrix_1_to_4_0, matrix_1_to_4_1};
long long **matrix_1_to_4 = matrix_1_to_4_init;
size_t matrix_1_to_4_len = 2;
size_t matrix_1_to_4_lens_init[2] = {2, 2};
size_t *matrix_1_to_4_lens = matrix_1_to_4_lens_init;
size_t matrix_1_to_4_lens_len = 2;
long long matrix_5_to_8_0[2] = {5LL, 6LL};
long long matrix_5_to_8_1[2] = {7LL, 8LL};
long long *matrix_5_to_8_init[2] = {matrix_5_to_8_0, matrix_5_to_8_1};
long long **matrix_5_to_8 = matrix_5_to_8_init;
size_t matrix_5_to_8_len = 2;
size_t matrix_5_to_8_lens_init[2] = {2, 2};
size_t *matrix_5_to_8_lens = matrix_5_to_8_lens_init;
size_t matrix_5_to_8_lens_len = 2;
long long matrix_count_up_0[4] = {1LL, 2LL, 3LL, 4LL};
long long matrix_count_up_1[4] = {5LL, 6LL, 7LL, 8LL};
long long matrix_count_up_2[4] = {9LL, 10LL, 11LL, 12LL};
long long matrix_count_up_3[4] = {13LL, 14LL, 15LL, 16LL};
long long *matrix_count_up_init[4] = {matrix_count_up_0, matrix_count_up_1, matrix_count_up_2, matrix_count_up_3};
long long **matrix_count_up = matrix_count_up_init;
size_t matrix_count_up_len = 4;
size_t matrix_count_up_lens_init[4] = {4, 4, 4, 4};
size_t *matrix_count_up_lens = matrix_count_up_lens_init;
size_t matrix_count_up_lens_len = 4;
long long matrix_unordered_0[4] = {5LL, 8LL, 1LL, 2LL};
long long matrix_unordered_1[4] = {6LL, 7LL, 3LL, 0LL};
long long matrix_unordered_2[4] = {4LL, 5LL, 9LL, 1LL};
long long matrix_unordered_3[4] = {2LL, 6LL, 10LL, 14LL};
long long *matrix_unordered_init[4] = {matrix_unordered_0, matrix_unordered_1, matrix_unordered_2, matrix_unordered_3};
long long **matrix_unordered = matrix_unordered_init;
size_t matrix_unordered_len = 4;
size_t matrix_unordered_lens_init[4] = {4, 4, 4, 4};
size_t *matrix_unordered_lens = matrix_unordered_lens_init;
size_t matrix_unordered_lens_len = 4;

long long is_square(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
long long * * matrix_multiply(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
void multiply(long long i, long long j, long long k, long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len, long long * * result, size_t result_len, size_t* result_lens, size_t result_lens_len, long long n, long long m);
long long * * matrix_multiply_recursive(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
int main(void);

long long is_square(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    long long n = matrix_len;
    long long i = 0LL;
    while (i < n) {
        if (matrix_lens[(int)(i)] != n) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

long long * * matrix_multiply(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    long long rows = a_len;
    long long cols = b_lens[(int)(0LL)];
    long long inner = b_len;
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < cols) {
            long long sum = 0LL;
            long long k = 0LL;
            while (k < inner) {
                sum = sum + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_long_long(row, &row_len, sum);
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return matrix_multiply_lens = result_lens, matrix_multiply_len = result_len, result;
}

void multiply(long long i, long long j, long long k, long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len, long long * * result, size_t result_len, size_t* result_lens, size_t result_lens_len, long long n, long long m) {
    if (i >= n) {
        return 0;
    }
    if (j >= m) {
        multiply(i + 1LL, 0LL, 0LL, a, a_len, a_lens, a_len, b, b_len, b_lens, b_len, result, result_len, result_lens, result_len, n, m);
        return 0;
    }
    if (k >= b_len) {
        multiply(i, j + 1LL, 0LL, a, a_len, a_lens, a_len, b, b_len, b_lens, b_len, result, result_len, result_lens, result_len, n, m);
        return 0;
    }
    result[(int)(i)][(int)(j)] = result[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? result_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? result_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
    multiply(i, j, k + 1LL, a, a_len, a_lens, a_len, b, b_len, b_lens, b_len, result, result_len, result_lens, result_len, n, m);
}

long long * * matrix_multiply_recursive(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    if ((a_len == 0LL) || (b_len == 0LL)) {
        {
            size_t *__tmp0_lens = malloc(0 * sizeof(size_t));
            long long** __tmp0_data = malloc(0 * sizeof(long long*));
            matrix_multiply_recursive_lens = __tmp0_lens;
            matrix_multiply_recursive_len = 0;
            return __tmp0_data;
        }
    }
    if (((a_len != b_len) || !(is_square(a, a_len, a_lens, a_len))) || !(is_square(b, b_len, b_lens, b_len))) {
        panic("Invalid matrix dimensions");
    }
    long long n = a_len;
    long long m = b_lens[(int)(0LL)];
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    multiply(0LL, 0LL, 0LL, a, a_len, a_lens, a_len, b, b_len, b_lens, b_len, result, result_len, result_lens, result_len, n, m);
    return matrix_multiply_recursive_lens = result_lens, matrix_multiply_recursive_len = result_len, result;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", matrix_multiply_recursive(matrix_1_to_4, matrix_1_to_4_len, matrix_1_to_4_lens, matrix_1_to_4_len, matrix_5_to_8, matrix_5_to_8_len, matrix_5_to_8_lens, matrix_5_to_8_len));
        printf("%d\n", matrix_multiply_recursive(matrix_count_up, matrix_count_up_len, matrix_count_up_lens, matrix_count_up_len, matrix_unordered, matrix_unordered_len, matrix_unordered_lens, matrix_unordered_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
