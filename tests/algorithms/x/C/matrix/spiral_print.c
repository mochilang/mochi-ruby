// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t spiral_traversal_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long is_valid_matrix(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
long long * spiral_traversal(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
void spiral_print_clockwise(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
void user_main();
int main(void);

long long is_valid_matrix(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    if (matrix_len == 0LL) {
        return 0LL;
    }
    long long cols = matrix_lens[(int)(0LL)];
    for (size_t __i = 0; __i < matrix_len; __i++) {
        long long *row = matrix[__i];
        size_t row_len = matrix_lens[__i];
        if (row_len != cols) {
            return 0LL;
        }
    }
    return 1LL;
}

long long * spiral_traversal(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    if (!(is_valid_matrix(matrix, matrix_len, matrix_lens, matrix_len))) {
        {
            long long* __tmp2_data = malloc(0 * sizeof(long long));
            spiral_traversal_len = 0;
            return __tmp2_data;
        }
    }
    long long rows = matrix_len;
    long long cols = matrix_lens[(int)(0LL)];
    long long top = 0LL;
    long long bottom = rows - 1LL;
    long long left = 0LL;
    long long right = cols - 1LL;
    long long *result = NULL;
    size_t result_len = 0;
    while ((left <= right) && (top <= bottom)) {
        long long i = left;
        while (i <= right) {
            result = list_append_long_long(result, &result_len, matrix[(int)({long long _mochi_idx = top; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_lens[(int)(top)] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        top = top + 1LL;
        i = top;
        while (i <= bottom) {
            result = list_append_long_long(result, &result_len, matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = right; _mochi_idx < 0 ? matrix_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            i = i + 1LL;
        }
        right = right - 1LL;
        if (top <= bottom) {
            i = right;
            while (i >= left) {
                result = list_append_long_long(result, &result_len, matrix[(int)({long long _mochi_idx = bottom; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_lens[(int)(bottom)] + _mochi_idx : _mochi_idx;})]);
                i = i - 1LL;
            }
            bottom = bottom - 1LL;
        }
        if (left <= right) {
            i = bottom;
            while (i >= top) {
                result = list_append_long_long(result, &result_len, matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = left; _mochi_idx < 0 ? matrix_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
                i = i - 1LL;
            }
            left = left + 1LL;
        }
    }
    return spiral_traversal_len = result_len, result;
}

void spiral_print_clockwise(long long * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    {
        long long* __tmp0 = spiral_traversal(matrix, matrix_len, matrix_lens, matrix_len);
        size_t __tmp0_len = spiral_traversal_len;
        for (size_t __i = 0; __i < __tmp0_len; __i++) {
            long long value = __tmp0[__i];
            puts(str_int(value));
        }
    }
}

void user_main() {
    long long a_0[4] = {1LL, 2LL, 3LL, 4LL};
    long long a_1[4] = {5LL, 6LL, 7LL, 8LL};
    long long a_2[4] = {9LL, 10LL, 11LL, 12LL};
    long long *a_init[3] = {a_0, a_1, a_2};
    long long **a = a_init;
    size_t a_len = 3;
    size_t a_lens_init[3] = {4, 4, 4};
    size_t *a_lens = a_lens_init;
    size_t a_lens_len = 3;
    spiral_print_clockwise(a, a_len, a_lens, a_len);
    puts(({long long *__tmp3 = spiral_traversal(a, a_len, a_lens, a_len); char *__res = str_list_int(__tmp3, spiral_traversal_len); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
