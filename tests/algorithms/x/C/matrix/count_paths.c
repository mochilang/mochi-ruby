// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long depth_first_search(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long row, long long col, long long * * visit, size_t visit_len, size_t* visit_lens, size_t visit_lens_len);
long long count_paths(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
void user_main();
int main(void);

long long depth_first_search(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long row, long long col, long long * * visit, size_t visit_len, size_t* visit_lens, size_t visit_lens_len) {
    long long row_length = grid_len;
    long long col_length = grid_lens[(int)(0LL)];
    if ((((row < 0LL) || (col < 0LL)) || (row == row_length)) || (col == col_length)) {
        return 0LL;
    }
    if (visit[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? visit_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? visit_lens[(int)(row)] + _mochi_idx : _mochi_idx;})]) {
        return 0LL;
    }
    if (grid[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? grid_lens[(int)(row)] + _mochi_idx : _mochi_idx;})] == 1LL) {
        return 0LL;
    }
    if ((row == (row_length - 1LL)) && (col == (col_length - 1LL))) {
        return 1LL;
    }
    visit[(int)(row)][(int)(col)] = 1LL;
    long long count_ = 0LL;
    count_ = count_ + depth_first_search(grid, grid_len, grid_lens, grid_len, row + 1LL, col, visit, visit_len, visit_lens, visit_len);
    count_ = count_ + depth_first_search(grid, grid_len, grid_lens, grid_len, row - 1LL, col, visit, visit_len, visit_lens, visit_len);
    count_ = count_ + depth_first_search(grid, grid_len, grid_lens, grid_len, row, col + 1LL, visit, visit_len, visit_lens, visit_len);
    count_ = count_ + depth_first_search(grid, grid_len, grid_lens, grid_len, row, col - 1LL, visit, visit_len, visit_lens, visit_len);
    visit[(int)(row)][(int)(col)] = 0LL;
    return count_;
}

long long count_paths(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long rows = grid_len;
    long long cols = grid_lens[(int)(0LL)];
    long long **visit = NULL;
    size_t visit_len = 0;
    size_t *visit_lens = NULL;
    size_t visit_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        long long *row_visit = NULL;
        size_t row_visit_len = 0;
        long long j = 0LL;
        while (j < cols) {
            row_visit = list_append_long_long(row_visit, &row_visit_len, 0LL);
            j = j + 1LL;
        }
        visit = list_append_intptr(visit, &visit_len, row_visit);
        visit_lens = list_append_szt(visit_lens, &visit_lens_len, row_visit_len);
        i = i + 1LL;
    }
    return depth_first_search(grid, grid_len, grid_lens, grid_len, 0LL, 0LL, visit, visit_len, visit_lens, visit_len);
}

void user_main() {
    long long grid1_0[4] = {0LL, 0LL, 0LL, 0LL};
    long long grid1_1[4] = {1LL, 1LL, 0LL, 0LL};
    long long grid1_2[4] = {0LL, 0LL, 0LL, 1LL};
    long long grid1_3[4] = {0LL, 1LL, 0LL, 0LL};
    long long *grid1_init[4] = {grid1_0, grid1_1, grid1_2, grid1_3};
    long long **grid1 = grid1_init;
    size_t grid1_len = 4;
    size_t grid1_lens_init[4] = {4, 4, 4, 4};
    size_t *grid1_lens = grid1_lens_init;
    size_t grid1_lens_len = 4;
    puts(str_int(count_paths(grid1, grid1_len, grid1_lens, grid1_len)));
    long long grid2_0[5] = {0LL, 0LL, 0LL, 0LL, 0LL};
    long long grid2_1[5] = {0LL, 1LL, 1LL, 1LL, 0LL};
    long long grid2_2[5] = {0LL, 1LL, 1LL, 1LL, 0LL};
    long long grid2_3[5] = {0LL, 0LL, 0LL, 0LL, 0LL};
    long long *grid2_init[4] = {grid2_0, grid2_1, grid2_2, grid2_3};
    long long **grid2 = grid2_init;
    size_t grid2_len = 4;
    size_t grid2_lens_init[4] = {5, 5, 5, 5};
    size_t *grid2_lens = grid2_lens_init;
    size_t grid2_lens_len = 4;
    puts(str_int(count_paths(grid2, grid2_len, grid2_lens, grid2_len)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
