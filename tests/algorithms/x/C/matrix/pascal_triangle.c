// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t populate_current_row_len;
size_t generate_pascal_triangle_len;
size_t *generate_pascal_triangle_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * populate_current_row(long long * * triangle, size_t triangle_len, size_t* triangle_lens, size_t triangle_lens_len, long long current_row_idx);
long long * * generate_pascal_triangle(long long num_rows);
const char* row_to_string(long long * row, size_t row_len, long long total_rows, long long row_idx);
void print_pascal_triangle(long long num_rows);
void user_main();
int main(void);

long long * populate_current_row(long long * * triangle, size_t triangle_len, size_t* triangle_lens, size_t triangle_lens_len, long long current_row_idx) {
    long long *row = NULL;
    size_t row_len = 0;
    long long i = 0LL;
    while (i <= current_row_idx) {
        if ((i == 0LL) || (i == current_row_idx)) {
            row = list_append_long_long(row, &row_len, 1LL);
        } else {
            long long left = triangle[(int)({long long _mochi_idx = current_row_idx - 1LL; _mochi_idx < 0 ? triangle_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i - 1LL; _mochi_idx < 0 ? triangle_lens[(int)(current_row_idx - 1LL)] + _mochi_idx : _mochi_idx;})];
            long long right = triangle[(int)({long long _mochi_idx = current_row_idx - 1LL; _mochi_idx < 0 ? triangle_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? triangle_lens[(int)(current_row_idx - 1LL)] + _mochi_idx : _mochi_idx;})];
            row = list_append_long_long(row, &row_len, left + right);
        }
        i = i + 1LL;
    }
    return populate_current_row_len = row_len, row;
}

long long * * generate_pascal_triangle(long long num_rows) {
    if (num_rows <= 0LL) {
        {
            size_t *__tmp1_lens = malloc(0 * sizeof(size_t));
            long long** __tmp1_data = malloc(0 * sizeof(long long*));
            generate_pascal_triangle_lens = __tmp1_lens;
            generate_pascal_triangle_len = 0;
            return __tmp1_data;
        }
    }
    long long **triangle = NULL;
    size_t triangle_len = 0;
    size_t *triangle_lens = NULL;
    size_t triangle_lens_len = 0;
    long long row_idx = 0LL;
    while (row_idx < num_rows) {
        long long *row = populate_current_row(triangle, triangle_len, triangle_lens, triangle_len, row_idx);
        size_t row_len = populate_current_row_len;
        triangle = list_append_intptr(triangle, &triangle_len, row);
        triangle_lens = list_append_szt(triangle_lens, &triangle_lens_len, row_len);
        row_idx = row_idx + 1LL;
    }
    return generate_pascal_triangle_lens = triangle_lens, generate_pascal_triangle_len = triangle_len, triangle;
}

const char* row_to_string(long long * row, size_t row_len, long long total_rows, long long row_idx) {
    const char* line = "";
    long long spaces = (total_rows - row_idx) - 1LL;
    long long s = 0LL;
    while (s < spaces) {
        line = str_concat(line, " ");
        s = s + 1LL;
    }
    long long c = 0LL;
    while (c <= row_idx) {
        line = str_concat(line, str_int(row[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]));
        if (c != row_idx) {
            line = str_concat(line, " ");
        }
        c = c + 1LL;
    }
    return line;
}

void print_pascal_triangle(long long num_rows) {
    long long **triangle = generate_pascal_triangle(num_rows);
    size_t triangle_len = generate_pascal_triangle_len;
    size_t *triangle_lens = generate_pascal_triangle_lens;
    size_t triangle_lens_len = generate_pascal_triangle_len;
    long long r = 0LL;
    while (r < num_rows) {
        const char* line = row_to_string(triangle[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? triangle_len + _mochi_idx : _mochi_idx;})], triangle_lens[(int)(r)], num_rows, r);
        puts(line);
        r = r + 1LL;
    }
}

void user_main() {
    print_pascal_triangle(5LL);
    puts(({long long **__tmp2 = generate_pascal_triangle(5LL); char *__res = str_list_list_int(__tmp2, generate_pascal_triangle_len, generate_pascal_triangle_lens); __res;}));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
