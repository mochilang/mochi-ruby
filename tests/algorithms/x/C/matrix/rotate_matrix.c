// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_matrix_len;
size_t *make_matrix_lens;
size_t transpose_len;
size_t *transpose_lens;
size_t reverse_row_len;
size_t *reverse_row_lens;
size_t reverse_column_len;
size_t *reverse_column_lens;
size_t rotate_90_len;
size_t *rotate_90_lens;
size_t rotate_180_len;
size_t *rotate_180_lens;
size_t rotate_270_len;
size_t *rotate_270_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **mat = NULL;
size_t mat_len = 0;
size_t *mat_lens = NULL;
size_t mat_lens_len = 0;
long long **r90 = NULL;
size_t r90_len = 0;
size_t *r90_lens = NULL;
size_t r90_lens_len = 0;
long long **r180 = NULL;
size_t r180_len = 0;
size_t *r180_lens = NULL;
size_t r180_lens_len = 0;
long long **r270 = NULL;
size_t r270_len = 0;
size_t *r270_lens = NULL;
size_t r270_lens_len = 0;

long long abs_int(long long n);
long long * * make_matrix(long long row_size);
long long * * transpose(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
long long * * reverse_row(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
long long * * reverse_column(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
long long * * rotate_90(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
long long * * rotate_180(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
long long * * rotate_270(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
const char* row_to_string(long long * row, size_t row_len);
void print_matrix(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
int main(void);

long long abs_int(long long n) {
    if (n < 0LL) {
        return -(n);
    }
    return n;
}

long long * * make_matrix(long long row_size) {
    long long size = abs_int(row_size);
    if (size == 0LL) {
        size = 4LL;
    }
    long long **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long y = 0LL;
    while (y < size) {
        long long *row = NULL;
        size_t row_len = 0;
        long long x = 0LL;
        while (x < size) {
            row = list_append_long_long(row, &row_len, (1LL + x) + (y * size));
            x = x + 1LL;
        }
        mat = list_append_intptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        y = y + 1LL;
    }
    return make_matrix_lens = mat_lens, make_matrix_len = mat_len, mat;
}

long long * * transpose(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long n = mat_len;
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_long_long(row, &row_len, mat[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_lens[(int)(j)] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return transpose_lens = result_lens, transpose_len = result_len, result;
}

long long * * reverse_row(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = mat_len - 1LL;
    while (i >= 0LL) {
        result = list_append_intptr(result, &result_len, mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})]);
        result_lens = list_append_szt(result_lens, &result_lens_len, mat_lens[(int)(i)]);
        i = i - 1LL;
    }
    return reverse_row_lens = result_lens, reverse_row_len = result_len, result;
}

long long * * reverse_column(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long **result = NULL;
    size_t result_len = 0;
    size_t *result_lens = NULL;
    size_t result_lens_len = 0;
    long long i = 0LL;
    while (i < mat_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = mat_lens[(int)(i)] - 1LL;
        while (j >= 0LL) {
            row = list_append_long_long(row, &row_len, mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]);
            j = j - 1LL;
        }
        result = list_append_intptr(result, &result_len, row);
        result_lens = list_append_szt(result_lens, &result_lens_len, row_len);
        i = i + 1LL;
    }
    return reverse_column_lens = result_lens, reverse_column_len = result_len, result;
}

long long * * rotate_90(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long **t = transpose(mat, mat_len, mat_lens, mat_len);
    size_t t_len = transpose_len;
    size_t *t_lens = transpose_lens;
    size_t t_lens_len = transpose_len;
    long long **rr = reverse_row(t, t_len, t_lens, t_len);
    size_t rr_len = reverse_row_len;
    size_t *rr_lens = reverse_row_lens;
    size_t rr_lens_len = reverse_row_len;
    return rotate_90_lens = rr_lens, rotate_90_len = rr_len, rr;
}

long long * * rotate_180(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long **rc = reverse_column(mat, mat_len, mat_lens, mat_len);
    size_t rc_len = reverse_column_len;
    size_t *rc_lens = reverse_column_lens;
    size_t rc_lens_len = reverse_column_len;
    long long **rr = reverse_row(rc, rc_len, rc_lens, rc_len);
    size_t rr_len = reverse_row_len;
    size_t *rr_lens = reverse_row_lens;
    size_t rr_lens_len = reverse_row_len;
    return rotate_180_lens = rr_lens, rotate_180_len = rr_len, rr;
}

long long * * rotate_270(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long **t = transpose(mat, mat_len, mat_lens, mat_len);
    size_t t_len = transpose_len;
    size_t *t_lens = transpose_lens;
    size_t t_lens_len = transpose_len;
    long long **rc = reverse_column(t, t_len, t_lens, t_len);
    size_t rc_len = reverse_column_len;
    size_t *rc_lens = reverse_column_lens;
    size_t rc_lens_len = reverse_column_len;
    return rotate_270_lens = rc_lens, rotate_270_len = rc_len, rc;
}

const char* row_to_string(long long * row, size_t row_len) {
    const char* line = "";
    long long i = 0LL;
    while (i < row_len) {
        if (i == 0LL) {
            line = str_int(row[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]);
        } else {
            line = str_concat(str_concat(line, " "), str_int(row[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]));
        }
        i = i + 1LL;
    }
    return line;
}

void print_matrix(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long i = 0LL;
    while (i < mat_len) {
        puts(row_to_string(mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})], mat_lens[(int)(i)]));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        mat = make_matrix(4LL);
        mat_len = make_matrix_len;
        mat_lens = make_matrix_lens;
        puts("\norigin:\n");
        print_matrix(mat, mat_len, mat_lens, mat_len);
        puts("\nrotate 90 counterclockwise:\n");
        r90 = rotate_90(mat, mat_len, mat_lens, mat_len);
        r90_len = rotate_90_len;
        r90_lens = rotate_90_lens;
        print_matrix(r90, r90_len, r90_lens, r90_len);
        mat = make_matrix(4LL);
        mat_len = make_matrix_len;
        mat_lens = make_matrix_lens;
        puts("\norigin:\n");
        print_matrix(mat, mat_len, mat_lens, mat_len);
        puts("\nrotate 180:\n");
        r180 = rotate_180(mat, mat_len, mat_lens, mat_len);
        r180_len = rotate_180_len;
        r180_lens = rotate_180_lens;
        print_matrix(r180, r180_len, r180_lens, r180_len);
        mat = make_matrix(4LL);
        mat_len = make_matrix_len;
        mat_lens = make_matrix_lens;
        puts("\norigin:\n");
        print_matrix(mat, mat_len, mat_lens, mat_len);
        puts("\nrotate 270 counterclockwise:\n");
        r270 = rotate_270(mat, mat_len, mat_lens, mat_len);
        r270_len = rotate_270_len;
        r270_lens = rotate_270_lens;
        print_matrix(r270, r270_len, r270_lens, r270_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
