// Generated by Mochi 0.10.32 on 2025-08-25 22:27 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **grid = NULL;
size_t grid_len = 0;
size_t *grid_lens = NULL;
size_t grid_lens_len = 0;

long long is_safe(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long * * visited, size_t visited_len, size_t* visited_lens, size_t visited_lens_len, long long row, long long col);
void dfs(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long * * visited, size_t visited_len, size_t* visited_lens, size_t visited_lens_len, long long row, long long col);
long long count_islands(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
int main(void);

long long is_safe(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long * * visited, size_t visited_len, size_t* visited_lens, size_t visited_lens_len, long long row, long long col) {
    long long rows = grid_len;
    long long cols = grid_lens[(int)(0LL)];
    long long within_bounds = (((row >= 0LL) && (row < rows)) && (col >= 0LL)) && (col < cols);
    if (!(within_bounds)) {
        return 0LL;
    }
    long long visited_cell = visited[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? visited_lens[(int)(row)] + _mochi_idx : _mochi_idx;})];
    long long not_visited = visited_cell == 0LL;
    return not_visited && (grid[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? grid_lens[(int)(row)] + _mochi_idx : _mochi_idx;})] == 1LL);
}

void dfs(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long * * visited, size_t visited_len, size_t* visited_lens, size_t visited_lens_len, long long row, long long col) {
    long long *row_nbr = NULL;
    size_t row_nbr_len = 0;
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, -1LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, -1LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, -1LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, 0LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, 0LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, 1LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, 1LL);
    row_nbr = list_append_long_long(row_nbr, &row_nbr_len, 1LL);
    long long *col_nbr = NULL;
    size_t col_nbr_len = 0;
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, -1LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, 0LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, 1LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, -1LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, 1LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, -1LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, 0LL);
    col_nbr = list_append_long_long(col_nbr, &col_nbr_len, 1LL);
    visited[(int)(row)][(int)(col)] = 1LL;
    long long k = 0LL;
    while (k < 8LL) {
        long long new_row = row + row_nbr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? row_nbr_len + _mochi_idx : _mochi_idx;})];
        long long new_col = col + col_nbr[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? col_nbr_len + _mochi_idx : _mochi_idx;})];
        if (is_safe(grid, grid_len, grid_lens, grid_len, visited, visited_len, visited_lens, visited_len, new_row, new_col)) {
            dfs(grid, grid_len, grid_lens, grid_len, visited, visited_len, visited_lens, visited_len, new_row, new_col);
        }
        k = k + 1LL;
    }
}

long long count_islands(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long rows = grid_len;
    long long cols = grid_lens[(int)(0LL)];
    long long **visited = NULL;
    size_t visited_len = 0;
    size_t *visited_lens = NULL;
    size_t visited_lens_len = 0;
    long long i = 0LL;
    while (i < rows) {
        long long *row_list = NULL;
        size_t row_list_len = 0;
        long long j = 0LL;
        while (j < cols) {
            row_list = list_append_long_long(row_list, &row_list_len, 0LL);
            j = j + 1LL;
        }
        visited = list_append_intptr(visited, &visited_len, row_list);
        visited_lens = list_append_szt(visited_lens, &visited_lens_len, row_list_len);
        i = i + 1LL;
    }
    long long count_ = 0LL;
    i = 0LL;
    while (i < rows) {
        long long j = 0LL;
        while (j < cols) {
            if (!(visited[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? visited_lens[(int)(i)] + _mochi_idx : _mochi_idx;})]) && (grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? grid_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] == 1LL)) {
                dfs(grid, grid_len, grid_lens, grid_len, visited, visited_len, visited_lens, visited_len, i, j);
                count_ = count_ + 1LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return count_;
}

int main(void) {
    {
        long long __start = _now();
        grid = malloc(5 * sizeof(long long*));
        grid_len = 5;
        grid[0] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 1LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp;});
        grid[1] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 1LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 1LL; tmp;});
        grid[2] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp[3] = 1LL; tmp[4] = 1LL; tmp;});
        grid[3] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 0LL; tmp[2] = 0LL; tmp[3] = 0LL; tmp[4] = 0LL; tmp;});
        grid[4] = ({long long *tmp = malloc(5 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 0LL; tmp[2] = 1LL; tmp[3] = 0LL; tmp[4] = 1LL; tmp;});
        grid_lens = ({size_t *tmp = malloc(5 * sizeof(size_t)); tmp[0] = 5; tmp[1] = 5; tmp[2] = 5; tmp[3] = 5; tmp[4] = 5; tmp;});
        grid_lens_len = 5;
        printf("%d\n", count_islands(grid, grid_len, grid_lens, grid_len));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
