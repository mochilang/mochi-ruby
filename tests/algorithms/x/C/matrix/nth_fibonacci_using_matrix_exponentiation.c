// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t multiply_len;
size_t *multiply_lens;
size_t identity_len;
size_t *identity_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * multiply(long long * * matrix_a, size_t matrix_a_len, size_t* matrix_a_lens, size_t matrix_a_lens_len, long long * * matrix_b, size_t matrix_b_len, size_t* matrix_b_lens, size_t matrix_b_lens_len);
long long * * identity(long long n);
long long nth_fibonacci_matrix(long long n);
long long nth_fibonacci_bruteforce(long long n);
long long parse_number(const char* s);
void user_main();
int main(void);

long long * * multiply(long long * * matrix_a, size_t matrix_a_len, size_t* matrix_a_lens, size_t matrix_a_lens_len, long long * * matrix_b, size_t matrix_b_len, size_t* matrix_b_lens, size_t matrix_b_lens_len) {
    long long n = matrix_a_len;
    long long **matrix_c = NULL;
    size_t matrix_c_len = 0;
    size_t *matrix_c_lens = NULL;
    size_t matrix_c_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            long long val = 0LL;
            long long k = 0LL;
            while (k < n) {
                val = val + (matrix_a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_a_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? matrix_a_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] * matrix_b[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? matrix_b_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? matrix_b_lens[(int)(k)] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            row = list_append_long_long(row, &row_len, val);
            j = j + 1LL;
        }
        matrix_c = list_append_intptr(matrix_c, &matrix_c_len, row);
        matrix_c_lens = list_append_szt(matrix_c_lens, &matrix_c_lens_len, row_len);
        i = i + 1LL;
    }
    return multiply_lens = matrix_c_lens, multiply_len = matrix_c_len, matrix_c;
}

long long * * identity(long long n) {
    long long **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            if (i == j) {
                row = list_append_long_long(row, &row_len, 1LL);
            } else {
                row = list_append_long_long(row, &row_len, 0LL);
            }
            j = j + 1LL;
        }
        res = list_append_intptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return identity_lens = res_lens, identity_len = res_len, res;
}

long long nth_fibonacci_matrix(long long n) {
    if (n <= 1LL) {
        return n;
    }
    long long **res_matrix = identity(2LL);
    size_t res_matrix_len = identity_len;
    size_t *res_matrix_lens = identity_lens;
    size_t res_matrix_lens_len = identity_len;
    long long fib_matrix_0[2] = {1LL, 1LL};
    long long fib_matrix_1[2] = {1LL, 0LL};
    long long *fib_matrix_init[2] = {fib_matrix_0, fib_matrix_1};
    long long **fib_matrix = fib_matrix_init;
    size_t fib_matrix_len = 2;
    size_t fib_matrix_lens_init[2] = {2, 2};
    size_t *fib_matrix_lens = fib_matrix_lens_init;
    size_t fib_matrix_lens_len = 2;
    long long m = n - 1LL;
    while (m > 0LL) {
        if ((m % 2LL) == 1LL) {
            res_matrix = multiply(res_matrix, res_matrix_len, res_matrix_lens, res_matrix_len, fib_matrix, fib_matrix_len, fib_matrix_lens, fib_matrix_len);
            res_matrix_len = multiply_len;
            res_matrix_lens = multiply_lens;
        }
        fib_matrix = multiply(fib_matrix, fib_matrix_len, fib_matrix_lens, fib_matrix_len, fib_matrix, fib_matrix_len, fib_matrix_lens, fib_matrix_len);
        fib_matrix_len = multiply_len;
        fib_matrix_lens = multiply_lens;
        m = m / 2LL;
    }
    return res_matrix[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_matrix_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
}

long long nth_fibonacci_bruteforce(long long n) {
    if (n <= 1LL) {
        return n;
    }
    long long fib0 = 0LL;
    long long fib1 = 1LL;
    long long i = 2LL;
    while (i <= n) {
        long long next = fib0 + fib1;
        fib0 = fib1;
        fib1 = next;
        i = i + 1LL;
    }
    return fib1;
}

long long parse_number(const char* s) {
    long long result = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if ((strcmp(ch, "0") >= 0) && (strcmp(ch, "9") <= 0)) {
            result = (result * 10LL) + _atoi(ch);
        }
        i = i + 1LL;
    }
    return result;
}

void user_main() {
    const char* *ordinals = NULL;
    size_t ordinals_len = 0;
    ordinals = list_append_str(ordinals, &ordinals_len, "0th");
    ordinals = list_append_str(ordinals, &ordinals_len, "1st");
    ordinals = list_append_str(ordinals, &ordinals_len, "2nd");
    ordinals = list_append_str(ordinals, &ordinals_len, "3rd");
    ordinals = list_append_str(ordinals, &ordinals_len, "10th");
    ordinals = list_append_str(ordinals, &ordinals_len, "100th");
    ordinals = list_append_str(ordinals, &ordinals_len, "1000th");
    long long i = 0LL;
    while (i < 7LL) {
        const char* ordinal = ordinals[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ordinals_len + _mochi_idx : _mochi_idx;})];
        long long n = parse_number(ordinal);
        const char* msg = str_concat(str_concat(str_concat(str_concat(ordinal, " fibonacci number using matrix exponentiation is "), str_int(nth_fibonacci_matrix(n))), " and using bruteforce is "), str_int(nth_fibonacci_bruteforce(n)));
        puts(msg);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
