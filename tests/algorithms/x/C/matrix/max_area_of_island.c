// Generated by Mochi 0.10.32 on 2025-08-19 17:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long matrix_0[13] = {0LL, 0LL, 1LL, 0LL, 0LL, 0LL, 0LL, 1LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long matrix_1[13] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 1LL, 1LL, 1LL, 0LL, 0LL, 0LL};
long long matrix_2[13] = {0LL, 1LL, 1LL, 0LL, 1LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long matrix_3[13] = {0LL, 1LL, 0LL, 0LL, 1LL, 1LL, 0LL, 0LL, 1LL, 0LL, 1LL, 0LL, 0LL};
long long matrix_4[13] = {0LL, 1LL, 0LL, 0LL, 1LL, 1LL, 0LL, 0LL, 1LL, 1LL, 1LL, 0LL, 0LL};
long long matrix_5[13] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 1LL, 0LL, 0LL};
long long matrix_6[13] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 1LL, 1LL, 1LL, 0LL, 0LL, 0LL};
long long matrix_7[13] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 1LL, 1LL, 0LL, 0LL, 0LL, 0LL};
long long *matrix_init[8] = {matrix_0, matrix_1, matrix_2, matrix_3, matrix_4, matrix_5, matrix_6, matrix_7};
long long **matrix = matrix_init;
size_t matrix_len = 8;
size_t matrix_lens_init[8] = {13, 13, 13, 13, 13, 13, 13, 13};
size_t *matrix_lens = matrix_lens_init;
size_t matrix_lens_len = 8;

const char* encode(long long row, long long col);
long long is_safe(long long row, long long col, long long rows, long long cols);
long long has(MapSI seen, const char* key);
long long depth_first_search(long long row, long long col, MapSI seen, long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
long long find_max_area(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
int main(void);

const char* encode(long long row, long long col) {
    return str_concat(str_concat(str_int(row), ","), str_int(col));
}

long long is_safe(long long row, long long col, long long rows, long long cols) {
    return (((row >= 0LL) && (row < rows)) && (col >= 0LL)) && (col < cols);
}

long long has(MapSI seen, const char* key) {
    return contains_str(seen.keys, seen.len, key);
}

long long depth_first_search(long long row, long long col, MapSI seen, long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long rows = mat_len;
    long long cols = mat_lens[(int)(0LL)];
    const char* key = encode(row, col);
    if ((is_safe(row, col, rows, cols) && !(has(seen, key))) && (mat[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? mat_lens[(int)(row)] + _mochi_idx : _mochi_idx;})] == 1LL)) {
        map_set_si(&seen, key, 1LL);
        return (((1LL + depth_first_search(row + 1LL, col, seen, mat, mat_len, mat_lens, mat_len)) + depth_first_search(row - 1LL, col, seen, mat, mat_len, mat_lens, mat_len)) + depth_first_search(row, col + 1LL, seen, mat, mat_len, mat_lens, mat_len)) + depth_first_search(row, col - 1LL, seen, mat, mat_len, mat_lens, mat_len);
    } else {
        return 0LL;
    }
}

long long find_max_area(long long * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    static const char* seen_keys[16] = {};
    static int seen_vals[16] = {};
    size_t seen_len = 0;
    MapSI seen = { seen_keys, seen_vals, 0, 16 };
    long long rows = mat_len;
    long long max_area = 0LL;
    long long r = 0LL;
    while (r < rows) {
        long long *line = mat[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})];
        size_t line_len = mat_lens[(int)(r)];
        long long cols = line_len;
        long long c = 0LL;
        while (c < cols) {
            if (line[(int)({long long _mochi_idx = c; _mochi_idx < 0 ? line_len + _mochi_idx : _mochi_idx;})] == 1LL) {
                const char* key = encode(r, c);
                if (!(contains_str(seen.keys, seen.len, key))) {
                    long long area = depth_first_search(r, c, seen, mat, mat_len, mat_lens, mat_len);
                    if (area > max_area) {
                        max_area = area;
                    }
                }
            }
            c = c + 1LL;
        }
        r = r + 1LL;
    }
    return max_area;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", find_max_area(matrix, matrix_len, matrix_lens, matrix_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
