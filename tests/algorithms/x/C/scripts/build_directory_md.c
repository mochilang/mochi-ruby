// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>

size_t split_len;
size_t sort_strings_len;
size_t good_file_paths_len;

size_t _slice_str_len;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

static char* repeat(const char *s, int n) {
    size_t len = strlen(s);
    char *out = malloc(len * n + 1);
    for (int i = 0; i < n; i++) memcpy(out + i * len, s, len);
    out[len * n] = 0;
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

const char* sample_init[4] = {"data_structures/linked_list.py", "data_structures/binary_tree.py", "math/number_theory/prime_check.py", "math/number_theory/greatest_common_divisor.ipynb"};
const char* *sample = sample_init;
size_t sample_len = 4;

const char* * split(const char* s, const char* sep);
const char* join(const char* * xs, size_t xs_len, const char* sep);
const char* user_repeat(const char* s, long long n);
const char* replace_char(const char* s, const char* old, const char* new);
long long contains(const char* s, const char* sub);
const char* file_extension(const char* name);
const char* remove_extension(const char* name);
const char* title_case(const char* s);
long long count_char(const char* s, const char* ch);
const char* md_prefix(long long level);
const char* print_path(const char* old_path, const char* new_path);
const char* * sort_strings(const char* * xs, size_t xs_len);
const char* * good_file_paths(const char* * paths, size_t paths_len);
void print_directory_md(const char* * paths, size_t paths_len);
int main(void);

const char* * split(const char* s, const char* sep) {
    const char* *parts = NULL;
    size_t parts_len = 0;
    const char* cur = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        if (((strlen(sep) > 0LL) && ((i + strlen(sep)) <= strlen(s))) && (strcmp(_substring(s, i, i + strlen(sep)), sep) == 0)) {
            parts = list_append_str(parts, &parts_len, cur);
            cur = "";
            i = i + strlen(sep);
        } else {
            cur = str_concat(cur, _substring(s, i, i + 1LL));
            i = i + 1LL;
        }
    }
    parts = list_append_str(parts, &parts_len, cur);
    return split_len = parts_len, parts;
}

const char* join(const char* * xs, size_t xs_len, const char* sep) {
    const char* res = "";
    long long i = 0LL;
    while (i < xs_len) {
        if (i > 0LL) {
            res = str_concat(res, sep);
        }
        res = str_concat(res, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return res;
}

const char* user_repeat(const char* s, long long n) {
    const char* out = "";
    long long i = 0LL;
    while (i < n) {
        out = str_concat(out, s);
        i = i + 1LL;
    }
    return out;
}

const char* replace_char(const char* s, const char* old, const char* new) {
    const char* out = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        if (strcmp(c, old) == 0) {
            out = str_concat(out, new);
        } else {
            out = str_concat(out, c);
        }
        i = i + 1LL;
    }
    return out;
}

long long contains(const char* s, const char* sub) {
    if (strlen(sub) == 0LL) {
        return 1LL;
    }
    long long i = 0LL;
    while ((i + strlen(sub)) <= strlen(s)) {
        if (strcmp(_substring(s, i, i + strlen(sub)), sub) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* file_extension(const char* name) {
    long long i = strlen(name) - 1LL;
    while (i >= 0LL) {
        if (strcmp(_substring(name, i, i + 1LL), ".") == 0) {
            return _substring(name, i, strlen(name));
        }
        i = i - 1LL;
    }
    return "";
}

const char* remove_extension(const char* name) {
    long long i = strlen(name) - 1LL;
    while (i >= 0LL) {
        if (strcmp(_substring(name, i, i + 1LL), ".") == 0) {
            return _substring(name, 0LL, i);
        }
        i = i - 1LL;
    }
    return name;
}

const char* title_case(const char* s) {
    const char* out = "";
    long long cap = 1LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        if (strcmp(c, " ") == 0) {
            out = str_concat(out, c);
            cap = 1LL;
        } else {
            if (cap) {
                out = str_concat(out, str_upper(c));
                cap = 0LL;
            } else {
                out = str_concat(out, str_lower(c));
            }
        }
        i = i + 1LL;
    }
    return out;
}

long long count_char(const char* s, const char* ch) {
    long long cnt = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            cnt = cnt + 1LL;
        }
        i = i + 1LL;
    }
    return cnt;
}

const char* md_prefix(long long level) {
    if (level == 0LL) {
        return "\n##";
    }
    return str_concat(repeat("  ", level), "*");
}

const char* print_path(const char* old_path, const char* new_path) {
    const char* *old_parts = split(old_path, "/");
    size_t old_parts_len = split_len;
    const char* *new_parts = split(new_path, "/");
    size_t new_parts_len = split_len;
    long long i = 0LL;
    while (i < new_parts_len) {
        if (((i >= old_parts_len) || (strcmp(old_parts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? old_parts_len + _mochi_idx : _mochi_idx;})], new_parts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? new_parts_len + _mochi_idx : _mochi_idx;})]) != 0)) && (strcmp(new_parts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? new_parts_len + _mochi_idx : _mochi_idx;})], "") != 0)) {
            const char* title = title_case(replace_char(new_parts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? new_parts_len + _mochi_idx : _mochi_idx;})], "_", " "));
            puts(str_concat(str_concat(md_prefix(i), " "), title));
        }
        i = i + 1LL;
    }
    return new_path;
}

const char* * sort_strings(const char* * xs, size_t xs_len) {
    const char* *arr = xs;
    size_t arr_len = xs_len;
    long long i = 0LL;
    while (i < arr_len) {
        long long min_idx = i;
        long long j = i + 1LL;
        while (j < arr_len) {
            if (strcmp(arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], arr[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]) < 0) {
                min_idx = j;
            }
            j = j + 1LL;
        }
        const char* tmp = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(i)] = arr[(int)({long long _mochi_idx = min_idx; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        arr[(int)(min_idx)] = tmp;
        i = i + 1LL;
    }
    return sort_strings_len = arr_len, arr;
}

const char* * good_file_paths(const char* * paths, size_t paths_len) {
    const char* *res = NULL;
    size_t res_len = 0;
    for (size_t __i = 0; __i < paths_len; __i++) {
        const char* p = paths[__i];
        const char* *parts = split(p, "/");
        size_t parts_len = split_len;
        long long skip = 0LL;
        long long k = 0LL;
        while (k < (parts_len - 1LL)) {
            const char* part = parts[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
            if ((((strcmp(part, "scripts") == 0) || (strcmp(_substring(part, 0LL, 1LL), ".") == 0)) || (strcmp(_substring(part, 0LL, 1LL), "_") == 0)) || contains(part, "venv")) {
                skip = 1LL;
            }
            k = k + 1LL;
        }
        if (skip) {
            continue;
        }
        const char* filename = parts[(int)({long long _mochi_idx = parts_len - 1LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
        if (strcmp(filename, "__init__.py") == 0) {
            continue;
        }
        const char* ext = file_extension(filename);
        if ((strcmp(ext, ".py") == 0) || (strcmp(ext, ".ipynb") == 0)) {
            res = list_append_str(res, &res_len, p);
        }
    }
    return good_file_paths_len = res_len, res;
}

void print_directory_md(const char* * paths, size_t paths_len) {
    const char* *files = ({const char** __tmp2 = good_file_paths(paths, paths_len); size_t __tmp2_len = good_file_paths_len; sort_strings(__tmp2, __tmp2_len);});
    size_t files_len = sort_strings_len;
    const char* old_path = "";
    long long i = 0LL;
    while (i < files_len) {
        const char* fp = files[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? files_len + _mochi_idx : _mochi_idx;})];
        const char* *parts = split(fp, "/");
        size_t parts_len = split_len;
        const char* filename = parts[(int)({long long _mochi_idx = parts_len - 1LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
        const char* filepath = "";
        if (parts_len > 1LL) {
            filepath = ({const char** __tmp3 = _slice_str(parts, parts_len, 0LL, parts_len - 1LL, &(_slice_str_len)); size_t __tmp3_len = _slice_str_len; join(__tmp3, __tmp3_len, "/");});
        }
        if (strcmp(filepath, old_path) != 0) {
            old_path = print_path(old_path, filepath);
        }
        long long indent = 0LL;
        if (strlen(filepath) > 0LL) {
            indent = count_char(filepath, "/") + 1LL;
        }
        const char* url = replace_char(fp, " ", "%20");
        const char* name = title_case(replace_char(remove_extension(filename), "_", " "));
        puts(str_concat(str_concat(str_concat(str_concat(str_concat(md_prefix(indent), " ["), name), "]("), url), ")"));
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        print_directory_md(sample, sample_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
