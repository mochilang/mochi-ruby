// Generated by Mochi 0.10.32 on 2025-08-23 21:18 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <ctype.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long indexOf(const char* s, const char* sub);
long long contains(const char* s, const char* sub);
long long validate(const char* * files, size_t files_len);
void user_main();
int main(void);

long long indexOf(const char* s, const char* sub) {
    long long n = strlen(s);
    long long m = strlen(sub);
    long long i = 0LL;
    while (i <= (n - m)) {
        if (strcmp(_substring(s, i, i + m), sub) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1.0;
}

long long contains(const char* s, const char* sub) {
    return indexOf(s, sub) >= 0LL;
}

long long validate(const char* * files, size_t files_len) {
    const char* *upper = NULL;
    size_t upper_len = 0;
    const char* *space = NULL;
    size_t space_len = 0;
    const char* *hyphen = NULL;
    size_t hyphen_len = 0;
    const char* *nodir = NULL;
    size_t nodir_len = 0;
    for (size_t __i = 0; __i < files_len; __i++) {
        const char* f = files[__i];
        if (strcmp(f, str_lower(f)) != 0) {
            upper = list_append_str(upper, &upper_len, f);
        }
        if (contains(f, " ")) {
            space = list_append_str(space, &space_len, f);
        }
        if (contains(f, "-") && (contains(f, "/site-packages/") == 0LL)) {
            hyphen = list_append_str(hyphen, &hyphen_len, f);
        }
        if (!(contains(f, "/"))) {
            nodir = list_append_str(nodir, &nodir_len, f);
        }
    }
    if (upper_len > 0LL) {
        puts(str_concat(str_int(upper_len), " files contain uppercase characters:"));
        for (size_t __i = 0; __i < upper_len; __i++) {
            const char* f = upper[__i];
            puts(f);
        }
        puts("");
    }
    if (space_len > 0LL) {
        puts(str_concat(str_int(space_len), " files contain space characters:"));
        for (size_t __i = 0; __i < space_len; __i++) {
            const char* f = space[__i];
            puts(f);
        }
        puts("");
    }
    if (hyphen_len > 0LL) {
        puts(str_concat(str_int(hyphen_len), " files contain hyphen characters:"));
        for (size_t __i = 0; __i < hyphen_len; __i++) {
            const char* f = hyphen[__i];
            puts(f);
        }
        puts("");
    }
    if (nodir_len > 0LL) {
        puts(str_concat(str_int(nodir_len), " files are not in a directory:"));
        for (size_t __i = 0; __i < nodir_len; __i++) {
            const char* f = nodir[__i];
            puts(f);
        }
        puts("");
    }
    return ((upper_len + space_len) + hyphen_len) + nodir_len;
}

void user_main() {
    const char* *files = NULL;
    size_t files_len = 0;
    files = list_append_str(files, &files_len, "scripts/Validate_filenames.py");
    files = list_append_str(files, &files_len, "good/file.txt");
    files = list_append_str(files, &files_len, "bad file.txt");
    files = list_append_str(files, &files_len, "/site-packages/pkg-name.py");
    files = list_append_str(files, &files_len, "nopath");
    files = list_append_str(files, &files_len, "src/hyphen-name.py");
    long long bad = validate(files, files_len);
    puts(str_int(bad));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
