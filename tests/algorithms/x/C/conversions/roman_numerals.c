// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long roman_values_init[13] = {1000LL, 900LL, 500LL, 400LL, 100LL, 90LL, 50LL, 40LL, 10LL, 9LL, 5LL, 4LL, 1LL};
long long *roman_values = roman_values_init;
size_t roman_values_len = 13;
const char* roman_symbols_init[13] = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};
const char* *roman_symbols = roman_symbols_init;
size_t roman_symbols_len = 13;

long long char_value(const char* c);
long long roman_to_int(const char* roman);
const char* int_to_roman(long long number);
int main(void);

long long char_value(const char* c) {
    if (strcmp(c, "I") == 0) {
        return 1LL;
    }
    if (strcmp(c, "V") == 0) {
        return 5LL;
    }
    if (strcmp(c, "X") == 0) {
        return 10LL;
    }
    if (strcmp(c, "L") == 0) {
        return 50LL;
    }
    if (strcmp(c, "C") == 0) {
        return 100LL;
    }
    if (strcmp(c, "D") == 0) {
        return 500LL;
    }
    if (strcmp(c, "M") == 0) {
        return 1000LL;
    }
    return 0LL;
}

long long roman_to_int(const char* roman) {
    long long total = 0LL;
    long long i = 0LL;
    while (i < strlen(roman)) {
        if (((i + 1LL) < strlen(roman)) && (char_value((const char[]){roman[i], 0}) < char_value((const char[]){roman[i + 1LL], 0}))) {
            total = (total + char_value((const char[]){roman[i + 1LL], 0})) - char_value((const char[]){roman[i], 0});
            i = i + 2LL;
        } else {
            total = total + char_value((const char[]){roman[i], 0});
            i = i + 1LL;
        }
    }
    return total;
}

const char* int_to_roman(long long number) {
    long long num = number;
    const char* res = "";
    long long i = 0LL;
    while (i < 13LL) {
        long long value = roman_values[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? roman_values_len + _mochi_idx : _mochi_idx;})];
        const char* symbol = roman_symbols[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? roman_symbols_len + _mochi_idx : _mochi_idx;})];
        long long factor = num / value;
        num = num % value;
        long long j = 0LL;
        while (j < factor) {
            res = str_concat(res, symbol);
            j = j + 1LL;
        }
        if (num == 0LL) {
            break;
        }
        i = i + 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
