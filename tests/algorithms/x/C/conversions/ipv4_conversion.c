// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_by_dot_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

const char* hex_digits = "0123456789abcdef";

const char* * split_by_dot(const char* s);
long long parse_decimal(const char* s);
const char* to_hex2(long long n);
long long ipv4_to_decimal(const char* ipv4_address);
long long alt_ipv4_to_decimal(const char* ipv4_address);
const char* decimal_to_ipv4(long long decimal_ipv4);
int main(void);

const char* * split_by_dot(const char* s) {
    const char* *res = NULL;
    size_t res_len = 0;
    const char* current = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if (strcmp(c, ".") == 0) {
            res = list_append_str(res, &res_len, current);
            current = "";
        } else {
            current = str_concat(current, c);
        }
        i = i + 1LL;
    }
    res = list_append_str(res, &res_len, current);
    return split_by_dot_len = res_len, res;
}

long long parse_decimal(const char* s) {
    if (strlen(s) == 0LL) {
        panic("Invalid IPv4 address format");
    }
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if ((strcmp(c, "0") < 0) || (strcmp(c, "9") > 0)) {
            panic("Invalid IPv4 address format");
        }
        value = (value * 10LL) + _atoi(c);
        i = i + 1LL;
    }
    return value;
}

const char* to_hex2(long long n) {
    long long x = n;
    const char* res = "";
    while (x > 0LL) {
        long long d = x % 16LL;
        res = str_concat((const char[]){hex_digits[d], 0}, res);
        x = x / 16LL;
    }
    while (strlen(res) < 2LL) {
        res = str_concat("0", res);
    }
    return res;
}

long long ipv4_to_decimal(const char* ipv4_address) {
    const char* *parts = split_by_dot(ipv4_address);
    size_t parts_len = split_by_dot_len;
    if (parts_len != 4LL) {
        panic("Invalid IPv4 address format");
    }
    long long result = 0LL;
    long long i = 0LL;
    while (i < 4LL) {
        long long oct = parse_decimal(parts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})]);
        if ((oct < 0LL) || (oct > 255LL)) {
            panic(str_concat("Invalid IPv4 octet ", str_int(oct)));
        }
        result = (result * 256LL) + oct;
        i = i + 1LL;
    }
    return result;
}

long long alt_ipv4_to_decimal(const char* ipv4_address) {
    const char* *parts = split_by_dot(ipv4_address);
    size_t parts_len = split_by_dot_len;
    if (parts_len != 4LL) {
        panic("Invalid IPv4 address format");
    }
    const char* hex_str = "";
    long long i = 0LL;
    while (i < 4LL) {
        long long oct = parse_decimal(parts[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})]);
        if ((oct < 0LL) || (oct > 255LL)) {
            panic(str_concat("Invalid IPv4 octet ", str_int(oct)));
        }
        hex_str = str_concat(hex_str, to_hex2(oct));
        i = i + 1LL;
    }
    long long value = 0LL;
    long long k = 0LL;
    while (k < strlen(hex_str)) {
        const char* c = (const char[]){hex_str[k], 0};
        long long digit = -1LL;
        long long j = 0LL;
        while (j < strlen(hex_digits)) {
            if (strcmp((const char[]){hex_digits[j], 0}, c) == 0) {
                digit = j;
            }
            j = j + 1LL;
        }
        if (digit < 0LL) {
            panic("Invalid hex digit");
        }
        value = (value * 16LL) + digit;
        k = k + 1LL;
    }
    return value;
}

const char* decimal_to_ipv4(long long decimal_ipv4) {
    if ((decimal_ipv4 < 0LL) || (decimal_ipv4 > 4294967295LL)) {
        panic("Invalid decimal IPv4 address");
    }
    long long n = decimal_ipv4;
    const char* *parts = NULL;
    size_t parts_len = 0;
    long long i = 0LL;
    while (i < 4LL) {
        long long octet = n % 256LL;
        parts = list_append_str(parts, &parts_len, str_int(octet));
        n = n / 256LL;
        i = i + 1LL;
    }
    const char* res = "";
    long long j = parts_len - 1LL;
    while (j >= 0LL) {
        res = str_concat(res, parts[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})]);
        if (j > 0LL) {
            res = str_concat(res, ".");
        }
        j = j - 1LL;
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", ipv4_to_decimal("192.168.0.1"));
        printf("%d\n", ipv4_to_decimal("10.0.0.255"));
        printf("%d\n", alt_ipv4_to_decimal("192.168.0.1"));
        printf("%d\n", alt_ipv4_to_decimal("10.0.0.255"));
        puts(decimal_to_ipv4(3232235521LL));
        puts(decimal_to_ipv4(167772415LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
