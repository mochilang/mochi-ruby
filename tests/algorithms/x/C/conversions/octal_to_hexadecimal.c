// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* nums_init[5] = {"030", "100", "247", "235", "007"};
const char* *nums = nums_init;
size_t nums_len = 5;
long long t = 0LL;

const char* octal_to_hex(const char* octal);
int main(void);

const char* octal_to_hex(const char* octal) {
    const char* s = octal;
    if (((strlen(s) >= 2LL) && (strcmp((const char[]){s[0LL], 0}, "0") == 0)) && (strcmp((const char[]){s[1LL], 0}, "o") == 0)) {
        s = _substring(s, 2LL, strlen(s));
    }
    if (strlen(s) == 0LL) {
        panic("Empty string was passed to the function");
    }
    long long j = 0LL;
    while (j < strlen(s)) {
        const char* c = (const char[]){s[j], 0};
        if ((((((((strcmp(c, "0") != 0) && (strcmp(c, "1") != 0)) && (strcmp(c, "2") != 0)) && (strcmp(c, "3") != 0)) && (strcmp(c, "4") != 0)) && (strcmp(c, "5") != 0)) && (strcmp(c, "6") != 0)) && (strcmp(c, "7") != 0)) {
            panic("Not a Valid Octal Number");
        }
        j = j + 1LL;
    }
    long long decimal = 0LL;
    long long k = 0LL;
    while (k < strlen(s)) {
        long long d = s[k] - '0';
        decimal = (decimal * 8LL) + d;
        k = k + 1LL;
    }
    const char* hex_chars = "0123456789ABCDEF";
    if (decimal == 0LL) {
        return "0x";
    }
    const char* hex = "";
    while (decimal > 0LL) {
        long long idx = decimal % 16LL;
        hex = str_concat((const char[]){hex_chars[idx], 0}, hex);
        decimal = decimal / 16LL;
    }
    return str_concat("0x", hex);
}

int main(void) {
    {
        long long __start = _now();
        while (t < 5LL) {
            const char* num = nums[(int)({long long _mochi_idx = t; _mochi_idx < 0 ? nums_len + _mochi_idx : _mochi_idx;})];
            puts(octal_to_hex(num));
            t = t + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
