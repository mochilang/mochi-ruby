// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* bin_to_octal(const char* bin_string);
int main(void);

const char* bin_to_octal(const char* bin_string) {
    long long i = 0LL;
    while (i < strlen(bin_string)) {
        const char* c = (const char[]){bin_string[i], 0};
        if (!((strcmp(c, "0") == 0) || (strcmp(c, "1") == 0))) {
            panic("Non-binary value was passed to the function");
        }
        i = i + 1LL;
    }
    if (strlen(bin_string) == 0LL) {
        panic("Empty string was passed to the function");
    }
    const char* padded = bin_string;
    while ((strlen(padded) % 3LL) != 0LL) {
        padded = str_concat("0", padded);
    }
    const char* oct_string = "";
    long long index = 0LL;
    while (index < strlen(padded)) {
        const char* group = _substring(padded, index, index + 3LL);
        long long b0;
        if (strcmp((const char[]){group[0LL], 0}, "1") == 0) {
            b0 = 1LL;
        } else {
            b0 = 0LL;
        }
        long long b1;
        if (strcmp((const char[]){group[1LL], 0}, "1") == 0) {
            b1 = 1LL;
        } else {
            b1 = 0LL;
        }
        long long b2;
        if (strcmp((const char[]){group[2LL], 0}, "1") == 0) {
            b2 = 1LL;
        } else {
            b2 = 0LL;
        }
        long long oct_val = ((b0 * 4LL) + (b1 * 2LL)) + b2;
        oct_string = str_concat(oct_string, str_int(oct_val));
        index = index + 3LL;
    }
    return oct_string;
}

int main(void) {
    {
        long long __start = _now();
        puts(bin_to_octal("1111"));
        puts(bin_to_octal("101010101010011"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
