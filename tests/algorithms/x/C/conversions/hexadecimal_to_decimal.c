// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* strip(const char* s);
long long hex_digit_value(const char* c);
long long hex_to_decimal(const char* hex_string);
void user_main();
int main(void);

const char* strip(const char* s) {
    long long start = 0LL;
    long long end = strlen(s);
    while ((start < end) && (strcmp(_substring(s, start, start + 1LL), " ") == 0)) {
        start = start + 1LL;
    }
    while ((end > start) && (strcmp(_substring(s, end - 1LL, end), " ") == 0)) {
        end = end - 1LL;
    }
    return _substring(s, start, end);
}

long long hex_digit_value(const char* c) {
    if (strcmp(c, "0") == 0) {
        return 0LL;
    }
    if (strcmp(c, "1") == 0) {
        return 1LL;
    }
    if (strcmp(c, "2") == 0) {
        return 2LL;
    }
    if (strcmp(c, "3") == 0) {
        return 3LL;
    }
    if (strcmp(c, "4") == 0) {
        return 4LL;
    }
    if (strcmp(c, "5") == 0) {
        return 5LL;
    }
    if (strcmp(c, "6") == 0) {
        return 6LL;
    }
    if (strcmp(c, "7") == 0) {
        return 7LL;
    }
    if (strcmp(c, "8") == 0) {
        return 8LL;
    }
    if (strcmp(c, "9") == 0) {
        return 9LL;
    }
    if ((strcmp(c, "a") == 0) || (strcmp(c, "A") == 0)) {
        return 10LL;
    }
    if ((strcmp(c, "b") == 0) || (strcmp(c, "B") == 0)) {
        return 11LL;
    }
    if ((strcmp(c, "c") == 0) || (strcmp(c, "C") == 0)) {
        return 12LL;
    }
    if ((strcmp(c, "d") == 0) || (strcmp(c, "D") == 0)) {
        return 13LL;
    }
    if ((strcmp(c, "e") == 0) || (strcmp(c, "E") == 0)) {
        return 14LL;
    }
    if ((strcmp(c, "f") == 0) || (strcmp(c, "F") == 0)) {
        return 15LL;
    }
    puts("Non-hexadecimal value was passed to the function");
    return 0LL;
}

long long hex_to_decimal(const char* hex_string) {
    const char* s = strip(hex_string);
    if (strlen(s) == 0LL) {
        puts("Empty string was passed to the function");
        return 0LL;
    }
    long long is_negative = 0LL;
    if (strcmp(_substring(s, 0LL, 1LL), "-") == 0) {
        is_negative = 1LL;
        s = _substring(s, 1LL, strlen(s));
    }
    long long decimal_number = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        long long value = hex_digit_value(c);
        decimal_number = (16LL * decimal_number) + value;
        i = i + 1LL;
    }
    if (is_negative) {
        return -(decimal_number);
    }
    return decimal_number;
}

void user_main() {
    puts(str_int(hex_to_decimal("a")));
    puts(str_int(hex_to_decimal("12f")));
    puts(str_int(hex_to_decimal("   12f   ")));
    puts(str_int(hex_to_decimal("FfFf")));
    puts(str_int(hex_to_decimal("-Ff")));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
