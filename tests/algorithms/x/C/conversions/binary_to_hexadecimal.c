// Generated by Mochi 0.10.32 on 2025-08-24 10:20 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* strip_spaces(const char* s);
const char* repeat_char(const char* ch, long long count_);
const char* slice(const char* s, long long start, long long end);
long long bits_to_int(const char* bits);
const char* bin_to_hexadecimal(const char* binary_str);
int main(void);

const char* strip_spaces(const char* s) {
    long long start = 0LL;
    long long end = strlen(s) - 1LL;
    while ((start < strlen(s)) && (strcmp((const char[]){s[start], 0}, " ") == 0)) {
        start = start + 1LL;
    }
    while ((end >= start) && (strcmp((const char[]){s[end], 0}, " ") == 0)) {
        end = end - 1LL;
    }
    const char* res = "";
    long long i = start;
    while (i <= end) {
        res = str_concat(res, (const char[]){s[i], 0});
        i = i + 1LL;
    }
    return res;
}

const char* repeat_char(const char* ch, long long count_) {
    const char* res = "";
    long long i = 0LL;
    while (i < count_) {
        res = str_concat(res, ch);
        i = i + 1LL;
    }
    return res;
}

const char* slice(const char* s, long long start, long long end) {
    const char* res = "";
    long long i = start;
    while (i < end) {
        res = str_concat(res, (const char[]){s[i], 0});
        i = i + 1LL;
    }
    return res;
}

long long bits_to_int(const char* bits) {
    long long value = 0LL;
    long long i = 0LL;
    while (i < strlen(bits)) {
        value = value * 2LL;
        if (strcmp((const char[]){bits[i], 0}, "1") == 0) {
            value = value + 1LL;
        }
        i = i + 1LL;
    }
    return value;
}

const char* bin_to_hexadecimal(const char* binary_str) {
    const char* s = strip_spaces(binary_str);
    if (strlen(s) == 0LL) {
        panic("Empty string was passed to the function");
    }
    long long is_negative = 0LL;
    if (strcmp((const char[]){s[0LL], 0}, "-") == 0) {
        is_negative = 1LL;
        s = slice(s, 1LL, strlen(s));
    }
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* c = (const char[]){s[i], 0};
        if ((strcmp(c, "0") != 0) && (strcmp(c, "1") != 0)) {
            panic("Non-binary value was passed to the function");
        }
        i = i + 1LL;
    }
    long long groups = (strlen(s) / 4LL) + 1LL;
    long long pad_len = (groups * 4LL) - strlen(s);
    s = str_concat(repeat_char("0", pad_len), s);
    const char* digits = "0123456789abcdef";
    const char* res = "0x";
    long long j = 0LL;
    while (j < strlen(s)) {
        const char* chunk = slice(s, j, j + 4LL);
        long long val = bits_to_int(chunk);
        res = str_concat(res, (const char[]){digits[val], 0});
        j = j + 4LL;
    }
    if (is_negative) {
        return str_concat("-", res);
    }
    return res;
}

int main(void) {
    {
        long long __start = _now();
        puts(bin_to_hexadecimal("101011111"));
        puts(bin_to_hexadecimal(" 1010   "));
        puts(bin_to_hexadecimal("-11101"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
