// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

void user_panic(const char* msg);
const char* trim_spaces(const char* s);
long long char_to_digit(const char* ch);
long long oct_to_decimal(const char* oct_string);
void user_main();
int main(void);

void user_panic(const char* msg) {
    puts(msg);
}

const char* trim_spaces(const char* s) {
    long long start = 0LL;
    long long end = strlen(s) - 1LL;
    while ((start <= end) && (strcmp(_substring(s, start, start + 1LL), " ") == 0)) {
        start = start + 1LL;
    }
    while ((end >= start) && (strcmp(_substring(s, end, end + 1LL), " ") == 0)) {
        end = end - 1LL;
    }
    if (start > end) {
        return "";
    }
    return _substring(s, start, end + 1LL);
}

long long char_to_digit(const char* ch) {
    if (strcmp(ch, "0") == 0) {
        return 0LL;
    }
    if (strcmp(ch, "1") == 0) {
        return 1LL;
    }
    if (strcmp(ch, "2") == 0) {
        return 2LL;
    }
    if (strcmp(ch, "3") == 0) {
        return 3LL;
    }
    if (strcmp(ch, "4") == 0) {
        return 4LL;
    }
    if (strcmp(ch, "5") == 0) {
        return 5LL;
    }
    if (strcmp(ch, "6") == 0) {
        return 6LL;
    }
    if (strcmp(ch, "7") == 0) {
        return 7LL;
    }
    user_panic("Non-octal value was passed to the function");
    return 0LL;
}

long long oct_to_decimal(const char* oct_string) {
    const char* s = trim_spaces(oct_string);
    if (strlen(s) == 0LL) {
        user_panic("Empty string was passed to the function");
        return 0LL;
    }
    long long is_negative = 0LL;
    if (strcmp(_substring(s, 0LL, 1LL), "-") == 0) {
        is_negative = 1LL;
        s = _substring(s, 1LL, strlen(s));
    }
    if (strlen(s) == 0LL) {
        user_panic("Non-octal value was passed to the function");
        return 0LL;
    }
    long long decimal_number = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        long long digit = char_to_digit(ch);
        decimal_number = (8LL * decimal_number) + digit;
        i = i + 1LL;
    }
    if (is_negative) {
        decimal_number = -(decimal_number);
    }
    return decimal_number;
}

void user_main() {
    puts(str_int(oct_to_decimal("1")));
    puts(str_int(oct_to_decimal("-1")));
    puts(str_int(oct_to_decimal("12")));
    puts(str_int(oct_to_decimal(" 12   ")));
    puts(str_int(oct_to_decimal("-45")));
    puts(str_int(oct_to_decimal("0")));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
