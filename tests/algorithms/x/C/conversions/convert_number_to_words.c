// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* ones_init[10] = {"zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
const char* *ones = ones_init;
size_t ones_len = 10;
const char* teens_init[10] = {"ten", "eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"};
const char* *teens = teens_init;
size_t teens_len = 10;
const char* tens_init[10] = {"", "", "twenty", "thirty", "forty", "fifty", "sixty", "seventy", "eighty", "ninety"};
const char* *tens = tens_init;
size_t tens_len = 10;
long long short_powers_init[6] = {15LL, 12LL, 9LL, 6LL, 3LL, 2LL};
long long *short_powers = short_powers_init;
size_t short_powers_len = 6;
const char* short_units_init[6] = {"quadrillion", "trillion", "billion", "million", "thousand", "hundred"};
const char* *short_units = short_units_init;
size_t short_units_len = 6;
long long long_powers_init[5] = {15LL, 9LL, 6LL, 3LL, 2LL};
long long *long_powers = long_powers_init;
size_t long_powers_len = 5;
const char* long_units_init[5] = {"billiard", "milliard", "million", "thousand", "hundred"};
const char* *long_units = long_units_init;
size_t long_units_len = 5;
long long indian_powers_init[6] = {14LL, 12LL, 7LL, 5LL, 3LL, 2LL};
long long *indian_powers = indian_powers_init;
size_t indian_powers_len = 6;
const char* indian_units_init[6] = {"crore crore", "lakh crore", "crore", "lakh", "thousand", "hundred"};
const char* *indian_units = indian_units_init;
size_t indian_units_len = 6;

long long user_pow10(long long exp);
long long max_value(const char* system);
const char* join_words(const char* * words, size_t words_len);
const char* convert_small_number(long long num);
const char* convert_number(long long num, const char* system);
int main(void);

long long user_pow10(long long exp) {
    long long res = 1LL;
    long long i = 0LL;
    while (i < exp) {
        res = res * 10LL;
        i = i + 1LL;
    }
    return res;
}

long long max_value(const char* system) {
    if (strcmp(system, "short") == 0) {
        return 1e+18 - 1LL;
    }
    if (strcmp(system, "long") == 0) {
        return 1e+21 - 1LL;
    }
    if (strcmp(system, "indian") == 0) {
        return 1e+19 - 1LL;
    }
    return 0LL;
}

const char* join_words(const char* * words, size_t words_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < words_len) {
        if (i > 0LL) {
            res = str_concat(res, " ");
        }
        res = str_concat(res, words[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? words_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return res;
}

const char* convert_small_number(long long num) {
    if (num < 0LL) {
        return "";
    }
    if (num >= 100LL) {
        return "";
    }
    long long tens_digit = num / 10LL;
    long long ones_digit = num % 10LL;
    if (tens_digit == 0LL) {
        return ones[(int)({long long _mochi_idx = ones_digit; _mochi_idx < 0 ? ones_len + _mochi_idx : _mochi_idx;})];
    }
    if (tens_digit == 1LL) {
        return teens[(int)({long long _mochi_idx = ones_digit; _mochi_idx < 0 ? teens_len + _mochi_idx : _mochi_idx;})];
    }
    const char* hyphen;
    if (ones_digit > 0LL) {
        hyphen = "-";
    } else {
        hyphen = "";
    }
    const char* tail;
    if (ones_digit > 0LL) {
        tail = ones[(int)({long long _mochi_idx = ones_digit; _mochi_idx < 0 ? ones_len + _mochi_idx : _mochi_idx;})];
    } else {
        tail = "";
    }
    return str_concat(str_concat(tens[(int)({long long _mochi_idx = tens_digit; _mochi_idx < 0 ? tens_len + _mochi_idx : _mochi_idx;})], hyphen), tail);
}

const char* convert_number(long long num, const char* system) {
    const char* *word_groups = NULL;
    size_t word_groups_len = 0;
    long long n = num;
    if (n < 0LL) {
        word_groups = list_append_str(word_groups, &word_groups_len, "negative");
        n = -(n);
    }
    if (n > max_value(system)) {
        return "";
    }
    long long *powers = NULL;
    size_t powers_len = 0;
    const char* *units = NULL;
    size_t units_len = 0;
    if (strcmp(system, "short") == 0) {
        powers = short_powers;
        powers_len = short_powers_len;
        units = short_units;
        units_len = short_units_len;
    } else {
        if (strcmp(system, "long") == 0) {
            powers = long_powers;
            powers_len = long_powers_len;
            units = long_units;
            units_len = long_units_len;
        } else {
            if (strcmp(system, "indian") == 0) {
                powers = indian_powers;
                powers_len = indian_powers_len;
                units = indian_units;
                units_len = indian_units_len;
            } else {
                return "";
            }
        }
    }
    long long i = 0LL;
    while (i < powers_len) {
        long long power = powers[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? powers_len + _mochi_idx : _mochi_idx;})];
        const char* unit = units[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? units_len + _mochi_idx : _mochi_idx;})];
        long long divisor = user_pow10(power);
        long long digit_group = n / divisor;
        n = n % divisor;
        if (digit_group > 0LL) {
            const char* word_group;
            if (digit_group >= 100LL) {
                word_group = convert_number(digit_group, system);
            } else {
                word_group = convert_small_number(digit_group);
            }
            word_groups = list_append_str(word_groups, &word_groups_len, str_concat(str_concat(word_group, " "), unit));
        }
        i = i + 1LL;
    }
    if ((n > 0LL) || (word_groups_len == 0LL)) {
        word_groups = list_append_str(word_groups, &word_groups_len, convert_small_number(n));
    }
    const char* joined = join_words(word_groups, word_groups_len);
    return joined;
}

int main(void) {
    {
        long long __start = _now();
        puts(convert_number(123456789012345LL, "short"));
        puts(convert_number(123456789012345LL, "long"));
        puts(convert_number(123456789012345LL, "indian"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
