// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

const char* octal_to_binary(const char* octal_number);
int main(void);

const char* octal_to_binary(const char* octal_number) {
    if (strlen(octal_number) == 0LL) {
        panic("Empty string was passed to the function");
    }
    const char* octal_digits = "01234567";
    const char* binary_number = "";
    long long i = 0LL;
    while (i < strlen(octal_number)) {
        const char* digit = (const char[]){octal_number[i], 0};
        long long valid = 0LL;
        long long j = 0LL;
        while (j < strlen(octal_digits)) {
            if (strcmp(digit, (const char[]){octal_digits[j], 0}) == 0) {
                valid = 1LL;
                break;
            }
            j = j + 1LL;
        }
        if (!(valid)) {
            panic("Non-octal value was passed to the function");
        }
        long long value = _atoi(digit);
        long long k = 0LL;
        const char* binary_digit = "";
        while (k < 3LL) {
            binary_digit = str_concat(str_int(value % 2LL), binary_digit);
            value = value / 2LL;
            k = k + 1LL;
        }
        binary_number = str_concat(binary_number, binary_digit);
        i = i + 1LL;
    }
    return binary_number;
}

int main(void) {
    {
        long long __start = _now();
        puts(octal_to_binary("17"));
        puts(octal_to_binary("7"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
