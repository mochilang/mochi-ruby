// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Prefix Prefix;

struct Prefix {
    const char* name;
    long long exp;
};

Prefix si_positive_init[10] = {(Prefix){.name = "yotta", .exp = 24LL}, (Prefix){.name = "zetta", .exp = 21LL}, (Prefix){.name = "exa", .exp = 18LL}, (Prefix){.name = "peta", .exp = 15LL}, (Prefix){.name = "tera", .exp = 12LL}, (Prefix){.name = "giga", .exp = 9LL}, (Prefix){.name = "mega", .exp = 6LL}, (Prefix){.name = "kilo", .exp = 3LL}, (Prefix){.name = "hecto", .exp = 2LL}, (Prefix){.name = "deca", .exp = 1LL}};
Prefix *si_positive = si_positive_init;
size_t si_positive_len = 10;
Prefix si_negative_init[10] = {(Prefix){.name = "deci", .exp = -1LL}, (Prefix){.name = "centi", .exp = -2LL}, (Prefix){.name = "milli", .exp = -3LL}, (Prefix){.name = "micro", .exp = -6LL}, (Prefix){.name = "nano", .exp = -9LL}, (Prefix){.name = "pico", .exp = -12LL}, (Prefix){.name = "femto", .exp = -15LL}, (Prefix){.name = "atto", .exp = -18LL}, (Prefix){.name = "zepto", .exp = -21LL}, (Prefix){.name = "yocto", .exp = -24LL}};
Prefix *si_negative = si_negative_init;
size_t si_negative_len = 10;
Prefix binary_prefixes_init[8] = {(Prefix){.name = "yotta", .exp = 80LL}, (Prefix){.name = "zetta", .exp = 70LL}, (Prefix){.name = "exa", .exp = 60LL}, (Prefix){.name = "peta", .exp = 50LL}, (Prefix){.name = "tera", .exp = 40LL}, (Prefix){.name = "giga", .exp = 30LL}, (Prefix){.name = "mega", .exp = 20LL}, (Prefix){.name = "kilo", .exp = 10LL}};
Prefix *binary_prefixes = binary_prefixes_init;
size_t binary_prefixes_len = 8;

double user_pow(double base, long long exp);
const char* add_si_prefix(double value);
const char* add_binary_prefix(double value);
int main(void);

double user_pow(double base, long long exp) {
    double result = 1.0;
    long long e = exp;
    if (e < 0LL) {
        e = -(e);
        long long i = 0LL;
        while (i < e) {
            result = result * base;
            i = i + 1LL;
        }
        return 1.0 / result;
    }
    long long i = 0LL;
    while (i < e) {
        result = result * base;
        i = i + 1LL;
    }
    return result;
}

const char* add_si_prefix(double value) {
    Prefix *prefixes = NULL;
    size_t prefixes_len = 0;
    if (value > 0.0) {
        prefixes = si_positive;
        prefixes_len = si_positive_len;
    } else {
        prefixes = si_negative;
        prefixes_len = si_negative_len;
    }
    long long i = 0LL;
    while (i < prefixes_len) {
        Prefix p = prefixes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? prefixes_len + _mochi_idx : _mochi_idx;})];
        double num = value / user_pow(10.0, p.exp);
        if (num > 1.0) {
            return str_concat(str_concat(str_float(num), " "), p.name);
        }
        i = i + 1LL;
    }
    return str_float(value);
}

const char* add_binary_prefix(double value) {
    long long i = 0LL;
    while (i < 8LL) {
        Prefix p = binary_prefixes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? binary_prefixes_len + _mochi_idx : _mochi_idx;})];
        double num = value / user_pow(2.0, p.exp);
        if (num > 1.0) {
            return str_concat(str_concat(str_float(num), " "), p.name);
        }
        i = i + 1LL;
    }
    return str_float(value);
}

int main(void) {
    {
        long long __start = _now();
        puts(add_si_prefix(10000.0));
        puts(add_si_prefix(0.005));
        puts(add_binary_prefix(65536.0));
        puts(add_binary_prefix(512.0));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
