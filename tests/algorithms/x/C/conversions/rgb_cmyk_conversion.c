// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t rgb_to_cmyk_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long round_int(double x);
long long * rgb_to_cmyk(long long r_input, long long g_input, long long b_input);
int main(void);

long long round_int(double x) {
    return x + 0.5;
}

long long * rgb_to_cmyk(long long r_input, long long g_input, long long b_input) {
    if ((((((r_input < 0LL) || (r_input >= 256LL)) || (g_input < 0LL)) || (g_input >= 256LL)) || (b_input < 0LL)) || (b_input >= 256LL)) {
        panic("Expected int of the range 0..255");
    }
    double r = (double)(r_input) / 255.0;
    double g = (double)(g_input) / 255.0;
    double b = (double)(b_input) / 255.0;
    double max_val = r;
    if (g > max_val) {
        max_val = g;
    }
    if (b > max_val) {
        max_val = b;
    }
    double k_float = 1.0 - max_val;
    if (k_float == 1.0) {
        {
            long long* __tmp6_data = malloc(4 * sizeof(long long));
            __tmp6_data[0] = 0LL;
            __tmp6_data[1] = 0LL;
            __tmp6_data[2] = 0LL;
            __tmp6_data[3] = 100LL;
            rgb_to_cmyk_len = 4;
            return __tmp6_data;
        }
    }
    double c_float = (100.0 * ((1.0 - r) - k_float)) / (1.0 - k_float);
    double m_float = (100.0 * ((1.0 - g) - k_float)) / (1.0 - k_float);
    double y_float = (100.0 * ((1.0 - b) - k_float)) / (1.0 - k_float);
    double k_percent = 100.0 * k_float;
    long long c = round_int(c_float);
    long long m = round_int(m_float);
    long long y = round_int(y_float);
    long long k = round_int(k_percent);
    {
        long long* __tmp7_data = malloc(4 * sizeof(long long));
        __tmp7_data[0] = c;
        __tmp7_data[1] = m;
        __tmp7_data[2] = y;
        __tmp7_data[3] = k;
        rgb_to_cmyk_len = 4;
        return __tmp7_data;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long* __tmp8 = rgb_to_cmyk(255LL, 255LL, 255LL);
        puts(str_list_int(__tmp8, rgb_to_cmyk_len));
        long long* __tmp9 = rgb_to_cmyk(128LL, 128LL, 128LL);
        puts(str_list_int(__tmp9, rgb_to_cmyk_len));
        long long* __tmp10 = rgb_to_cmyk(0LL, 0LL, 0LL);
        puts(str_list_int(__tmp10, rgb_to_cmyk_len));
        long long* __tmp11 = rgb_to_cmyk(255LL, 0LL, 0LL);
        puts(str_list_int(__tmp11, rgb_to_cmyk_len));
        long long* __tmp12 = rgb_to_cmyk(0LL, 255LL, 0LL);
        puts(str_list_int(__tmp12, rgb_to_cmyk_len));
        long long* __tmp13 = rgb_to_cmyk(0LL, 0LL, 255LL);
        puts(str_list_int(__tmp13, rgb_to_cmyk_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
