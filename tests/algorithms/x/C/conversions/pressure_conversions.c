// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct FromTo FromTo;
typedef struct { const char **keys; FromTo *vals; size_t len; size_t cap; } MapSFromTo;

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}


struct FromTo {
    double from_factor;
    double to_factor;
};

static FromTo map_get_s_fromto(const char *keys[], const FromTo vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return (FromTo){0};
}

static const char* PRESSURE_CONVERSION_keys[24] = {"atm", "pascal", "bar", "kilopascal", "megapascal", "psi", "inHg", "torr"};
static FromTo PRESSURE_CONVERSION_vals[24] = {(FromTo){.from_factor = 1.0, .to_factor = 1.0}, (FromTo){.from_factor = 9.8e-06, .to_factor = 101325.0}, (FromTo){.from_factor = 0.986923, .to_factor = 1.01325}, (FromTo){.from_factor = 0.00986923, .to_factor = 101.325}, (FromTo){.from_factor = 9.86923, .to_factor = 0.101325}, (FromTo){.from_factor = 0.068046, .to_factor = 14.6959}, (FromTo){.from_factor = 0.0334211, .to_factor = 29.9213}, (FromTo){.from_factor = 0.00131579, .to_factor = 760.0}};
size_t PRESSURE_CONVERSION_len = 8;
MapSFromTo PRESSURE_CONVERSION = { PRESSURE_CONVERSION_keys, PRESSURE_CONVERSION_vals, 8, 24 };

double pressure_conversion(double value, const char* from_type, const char* to_type);
int main(void);

double pressure_conversion(double value, const char* from_type, const char* to_type) {
    if (!(contains_str(PRESSURE_CONVERSION.keys, PRESSURE_CONVERSION.len, from_type))) {
        const char* keys = "atm, bar, inHg, kilopascal, megapascal, pascal, psi, torr";
        panic(str_concat(str_concat(str_concat("Invalid 'from_type' value: '", from_type), "'  Supported values are:\n"), keys));
    }
    if (!(contains_str(PRESSURE_CONVERSION.keys, PRESSURE_CONVERSION.len, to_type))) {
        const char* keys = "atm, bar, inHg, kilopascal, megapascal, pascal, psi, torr";
        panic(str_concat(str_concat(str_concat("Invalid 'to_type' value: '", to_type), ".  Supported values are:\n"), keys));
    }
    FromTo from = map_get_s_fromto(PRESSURE_CONVERSION.keys, PRESSURE_CONVERSION.vals, PRESSURE_CONVERSION.len, from_type);
    FromTo to = map_get_s_fromto(PRESSURE_CONVERSION.keys, PRESSURE_CONVERSION.vals, PRESSURE_CONVERSION.len, to_type);
    return (value * from.from_factor) * to.to_factor;
}

int main(void) {
    {
        long long __start = _now();
        printf("%g\n", pressure_conversion(4.0, "atm", "pascal"));
        printf("%g\n", pressure_conversion(1.0, "pascal", "psi"));
        printf("%g\n", pressure_conversion(1.0, "bar", "atm"));
        printf("%g\n", pressure_conversion(3.0, "kilopascal", "bar"));
        printf("%g\n", pressure_conversion(2.0, "megapascal", "psi"));
        printf("%g\n", pressure_conversion(4.0, "psi", "torr"));
        printf("%g\n", pressure_conversion(1.0, "inHg", "atm"));
        printf("%g\n", pressure_conversion(1.0, "torr", "psi"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
