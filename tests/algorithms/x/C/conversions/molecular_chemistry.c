// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long round_to_int(double x);
long long molarity_to_normality(double nfactor, double moles, double volume);
long long moles_to_pressure(double volume, double moles, double temperature);
long long moles_to_volume(double pressure, double moles, double temperature);
long long pressure_and_volume_to_temperature(double pressure, double moles, double volume);
int main(void);

long long round_to_int(double x) {
    if (x >= 0.0) {
        return (int)(x + 0.5);
    }
    return (int)(x - 0.5);
}

long long molarity_to_normality(double nfactor, double moles, double volume) {
    return round_to_int((moles / volume) * nfactor);
}

long long moles_to_pressure(double volume, double moles, double temperature) {
    return round_to_int(((moles * 0.0821) * temperature) / volume);
}

long long moles_to_volume(double pressure, double moles, double temperature) {
    return round_to_int(((moles * 0.0821) * temperature) / pressure);
}

long long pressure_and_volume_to_temperature(double pressure, double moles, double volume) {
    return round_to_int((pressure * volume) / (0.0821 * moles));
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(molarity_to_normality(2.0, 3.1, 0.31)));
        puts(str_int(molarity_to_normality(4.0, 11.4, 5.7)));
        puts(str_int(moles_to_pressure(0.82, 3.0, 300.0)));
        puts(str_int(moles_to_pressure(8.2, 5.0, 200.0)));
        puts(str_int(moles_to_volume(0.82, 3.0, 300.0)));
        puts(str_int(moles_to_volume(8.2, 5.0, 200.0)));
        puts(str_int(pressure_and_volume_to_temperature(0.82, 1.0, 2.0)));
        puts(str_int(pressure_and_volume_to_temperature(8.2, 5.0, 3.0)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
