// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

const char* units_init[4] = {"km/h", "m/s", "mph", "knot"};
const char* *units = units_init;
size_t units_len = 4;
double speed_chart_init[4] = {1.0, 3.6, 1.609344, 1.852};
double *speed_chart = speed_chart_init;
size_t speed_chart_len = 4;
double speed_chart_inverse_init[4] = {1.0, 0.277777778, 0.621371192, 0.539956803};
double *speed_chart_inverse = speed_chart_inverse_init;
size_t speed_chart_inverse_len = 4;

long long index_of(const char* * arr, size_t arr_len, const char* value);
const char* units_string(const char* * arr, size_t arr_len);
double round3(double x);
double convert_speed(double speed, const char* unit_from, const char* unit_to);
int main(void);

long long index_of(const char* * arr, size_t arr_len, const char* value) {
    long long i = 0LL;
    while (i < arr_len) {
        if (strcmp(arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})], value) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* units_string(const char* * arr, size_t arr_len) {
    const char* s = "";
    long long i = 0LL;
    while (i < arr_len) {
        if (i > 0LL) {
            s = str_concat(s, ", ");
        }
        s = str_concat(s, arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return s;
}

double round3(double x) {
    double y = (x * 1000.0) + 0.5;
    long long z = (int)(y);
    double zf = (double)(z);
    return zf / 1000.0;
}

double convert_speed(double speed, const char* unit_from, const char* unit_to) {
    long long from_index = index_of(units, units_len, unit_from);
    long long to_index = index_of(units, units_len, unit_to);
    if ((from_index < 0LL) || (to_index < 0LL)) {
        const char* msg = str_concat(str_concat(str_concat(str_concat(str_concat("Incorrect 'from_type' or 'to_type' value: ", unit_from), ", "), unit_to), "\nValid values are: "), units_string(units, units_len));
        panic(msg);
    }
    double result = (speed * speed_chart[(int)({long long _mochi_idx = from_index; _mochi_idx < 0 ? speed_chart_len + _mochi_idx : _mochi_idx;})]) * speed_chart_inverse[(int)({long long _mochi_idx = to_index; _mochi_idx < 0 ? speed_chart_inverse_len + _mochi_idx : _mochi_idx;})];
    double r = round3(result);
    return r;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_float(convert_speed(100.0, "km/h", "m/s")));
        puts(str_float(convert_speed(100.0, "km/h", "mph")));
        puts(str_float(convert_speed(100.0, "km/h", "knot")));
        puts(str_float(convert_speed(100.0, "m/s", "km/h")));
        puts(str_float(convert_speed(100.0, "m/s", "mph")));
        puts(str_float(convert_speed(100.0, "m/s", "knot")));
        puts(str_float(convert_speed(100.0, "mph", "km/h")));
        puts(str_float(convert_speed(100.0, "mph", "m/s")));
        puts(str_float(convert_speed(100.0, "mph", "knot")));
        puts(str_float(convert_speed(100.0, "knot", "km/h")));
        puts(str_float(convert_speed(100.0, "knot", "m/s")));
        puts(str_float(convert_speed(100.0, "knot", "mph")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
