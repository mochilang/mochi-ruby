// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

void user_panic(const char* msg);
const char* trim_spaces(const char* s);
long long hex_digit_value(const char* ch);
long long hex_to_bin(const char* hex_num);
int main(void);

void user_panic(const char* msg) {
    puts(msg);
}

const char* trim_spaces(const char* s) {
    long long start = 0LL;
    long long end = strlen(s);
    while ((start < end) && (strcmp(_substring(s, start, start + 1LL), " ") == 0)) {
        start = start + 1LL;
    }
    while ((end > start) && (strcmp(_substring(s, end - 1LL, end), " ") == 0)) {
        end = end - 1LL;
    }
    return _substring(s, start, end);
}

long long hex_digit_value(const char* ch) {
    if (strcmp(ch, "0") == 0) {
        return 0LL;
    }
    if (strcmp(ch, "1") == 0) {
        return 1LL;
    }
    if (strcmp(ch, "2") == 0) {
        return 2LL;
    }
    if (strcmp(ch, "3") == 0) {
        return 3LL;
    }
    if (strcmp(ch, "4") == 0) {
        return 4LL;
    }
    if (strcmp(ch, "5") == 0) {
        return 5LL;
    }
    if (strcmp(ch, "6") == 0) {
        return 6LL;
    }
    if (strcmp(ch, "7") == 0) {
        return 7LL;
    }
    if (strcmp(ch, "8") == 0) {
        return 8LL;
    }
    if (strcmp(ch, "9") == 0) {
        return 9LL;
    }
    if ((strcmp(ch, "a") == 0) || (strcmp(ch, "A") == 0)) {
        return 10LL;
    }
    if ((strcmp(ch, "b") == 0) || (strcmp(ch, "B") == 0)) {
        return 11LL;
    }
    if ((strcmp(ch, "c") == 0) || (strcmp(ch, "C") == 0)) {
        return 12LL;
    }
    if ((strcmp(ch, "d") == 0) || (strcmp(ch, "D") == 0)) {
        return 13LL;
    }
    if ((strcmp(ch, "e") == 0) || (strcmp(ch, "E") == 0)) {
        return 14LL;
    }
    if ((strcmp(ch, "f") == 0) || (strcmp(ch, "F") == 0)) {
        return 15LL;
    }
    user_panic("Invalid value was passed to the function");
}

long long hex_to_bin(const char* hex_num) {
    const char* trimmed = trim_spaces(hex_num);
    if (strlen(trimmed) == 0LL) {
        user_panic("No value was passed to the function");
    }
    const char* s = trimmed;
    long long is_negative = 0LL;
    if (strcmp(_substring(s, 0LL, 1LL), "-") == 0) {
        is_negative = 1LL;
        s = _substring(s, 1LL, strlen(s));
    }
    long long int_num = 0LL;
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        long long val = hex_digit_value(ch);
        int_num = (int_num * 16LL) + val;
        i = i + 1LL;
    }
    const char* bin_str = "";
    long long n = int_num;
    if (n == 0LL) {
        bin_str = "0";
    }
    while (n > 0LL) {
        bin_str = str_concat(str_int(n % 2LL), bin_str);
        n = n / 2LL;
    }
    long long result = _atoi(bin_str);
    if (is_negative) {
        result = -(result);
    }
    return result;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(hex_to_bin("AC")));
        puts(str_int(hex_to_bin("9A4")));
        puts(str_int(hex_to_bin("   12f   ")));
        puts(str_int(hex_to_bin("FfFf")));
        puts(str_int(hex_to_bin("-fFfF")));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
