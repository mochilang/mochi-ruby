// Generated by Mochi 0.10.32 on 2025-08-24 14:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double user_floor(double x);
double user_pow10(long long n);
double round_to(double x, long long ndigits);
double celsius_to_fahrenheit(double c, long long ndigits);
double celsius_to_kelvin(double c, long long ndigits);
double celsius_to_rankine(double c, long long ndigits);
double fahrenheit_to_celsius(double f, long long ndigits);
double fahrenheit_to_kelvin(double f, long long ndigits);
double fahrenheit_to_rankine(double f, long long ndigits);
double kelvin_to_celsius(double k, long long ndigits);
double kelvin_to_fahrenheit(double k, long long ndigits);
double kelvin_to_rankine(double k, long long ndigits);
double rankine_to_celsius(double r, long long ndigits);
double rankine_to_fahrenheit(double r, long long ndigits);
double rankine_to_kelvin(double r, long long ndigits);
double reaumur_to_kelvin(double r, long long ndigits);
double reaumur_to_fahrenheit(double r, long long ndigits);
double reaumur_to_celsius(double r, long long ndigits);
double reaumur_to_rankine(double r, long long ndigits);
int main(void);

double user_floor(double x) {
    long long i = (int)(x);
    if ((double)(i) > x) {
        i = i - 1LL;
    }
    return (double)(i);
}

double user_pow10(long long n) {
    double p = 1.0;
    long long i = 0LL;
    while (i < n) {
        p = p * 10.0;
        i = i + 1LL;
    }
    return p;
}

double round_to(double x, long long ndigits) {
    double m = user_pow10(ndigits);
    return user_floor((x * m) + 0.5) / m;
}

double celsius_to_fahrenheit(double c, long long ndigits) {
    return round_to(((c * 9.0) / 5.0) + 32.0, ndigits);
}

double celsius_to_kelvin(double c, long long ndigits) {
    return round_to(c + 273.15, ndigits);
}

double celsius_to_rankine(double c, long long ndigits) {
    return round_to(((c * 9.0) / 5.0) + 491.67, ndigits);
}

double fahrenheit_to_celsius(double f, long long ndigits) {
    return round_to(((f - 32.0) * 5.0) / 9.0, ndigits);
}

double fahrenheit_to_kelvin(double f, long long ndigits) {
    return round_to((((f - 32.0) * 5.0) / 9.0) + 273.15, ndigits);
}

double fahrenheit_to_rankine(double f, long long ndigits) {
    return round_to(f + 459.67, ndigits);
}

double kelvin_to_celsius(double k, long long ndigits) {
    return round_to(k - 273.15, ndigits);
}

double kelvin_to_fahrenheit(double k, long long ndigits) {
    return round_to((((k - 273.15) * 9.0) / 5.0) + 32.0, ndigits);
}

double kelvin_to_rankine(double k, long long ndigits) {
    return round_to((k * 9.0) / 5.0, ndigits);
}

double rankine_to_celsius(double r, long long ndigits) {
    return round_to(((r - 491.67) * 5.0) / 9.0, ndigits);
}

double rankine_to_fahrenheit(double r, long long ndigits) {
    return round_to(r - 459.67, ndigits);
}

double rankine_to_kelvin(double r, long long ndigits) {
    return round_to((r * 5.0) / 9.0, ndigits);
}

double reaumur_to_kelvin(double r, long long ndigits) {
    return round_to((r * 1.25) + 273.15, ndigits);
}

double reaumur_to_fahrenheit(double r, long long ndigits) {
    return round_to((r * 2.25) + 32.0, ndigits);
}

double reaumur_to_celsius(double r, long long ndigits) {
    return round_to(r * 1.25, ndigits);
}

double reaumur_to_rankine(double r, long long ndigits) {
    return round_to(((r * 2.25) + 32.0) + 459.67, ndigits);
}

int main(void) {
    {
        long long __start = _now();
        printf("%g\n", celsius_to_fahrenheit(0.0, 2LL));
        printf("%g\n", celsius_to_kelvin(0.0, 2LL));
        printf("%g\n", celsius_to_rankine(0.0, 2LL));
        printf("%g\n", fahrenheit_to_celsius(32.0, 2LL));
        printf("%g\n", fahrenheit_to_kelvin(32.0, 2LL));
        printf("%g\n", fahrenheit_to_rankine(32.0, 2LL));
        printf("%g\n", kelvin_to_celsius(273.15, 2LL));
        printf("%g\n", kelvin_to_fahrenheit(273.15, 2LL));
        printf("%g\n", kelvin_to_rankine(273.15, 2LL));
        printf("%g\n", rankine_to_celsius(491.67, 2LL));
        printf("%g\n", rankine_to_fahrenheit(491.67, 2LL));
        printf("%g\n", rankine_to_kelvin(491.67, 2LL));
        printf("%g\n", reaumur_to_kelvin(80.0, 2LL));
        printf("%g\n", reaumur_to_fahrenheit(80.0, 2LL));
        printf("%g\n", reaumur_to_celsius(80.0, 2LL));
        printf("%g\n", reaumur_to_rankine(80.0, 2LL));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
