// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t conj_vector_len;
size_t vec_mat_mul_len;

size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Complex Complex;

struct Complex {
    double re;
    double im;
};

static Complex* list_append_Complex(Complex *arr, size_t *len, Complex val) {
    arr = realloc(arr, (*len + 1) * sizeof(Complex));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Complex a_0[3] = {(Complex){.re = 2LL, .im = 0LL}, (Complex){.re = 2LL, .im = 1LL}, (Complex){.re = 4LL, .im = 0LL}};
Complex a_1[3] = {(Complex){.re = 2LL, .im = -1LL}, (Complex){.re = 3LL, .im = 0LL}, (Complex){.re = 0LL, .im = 1LL}};
Complex a_2[3] = {(Complex){.re = 4LL, .im = 0LL}, (Complex){.re = 0LL, .im = -1LL}, (Complex){.re = 1LL, .im = 0LL}};
Complex *a_init[3] = {a_0, a_1, a_2};
Complex **a = a_init;
size_t a_len = 3;
size_t a_lens_init[3] = {3, 3, 3};
size_t *a_lens = a_lens_init;
size_t a_lens_len = 3;
Complex v_init[3] = {(Complex){.re = 1LL, .im = 0LL}, (Complex){.re = 2LL, .im = 0LL}, (Complex){.re = 3LL, .im = 0LL}};
Complex *v = v_init;
size_t v_len = 3;
Complex b_0[3] = {(Complex){.re = 1LL, .im = 0LL}, (Complex){.re = 2LL, .im = 0LL}, (Complex){.re = 4LL, .im = 0LL}};
Complex b_1[3] = {(Complex){.re = 2LL, .im = 0LL}, (Complex){.re = 3LL, .im = 0LL}, (Complex){.re = -1LL, .im = 0LL}};
Complex b_2[3] = {(Complex){.re = 4LL, .im = 0LL}, (Complex){.re = -1LL, .im = 0LL}, (Complex){.re = 1LL, .im = 0LL}};
Complex *b_init[3] = {b_0, b_1, b_2};
Complex **b = b_init;
size_t b_len = 3;
size_t b_lens_init[3] = {3, 3, 3};
size_t *b_lens = b_lens_init;
size_t b_lens_len = 3;

Complex complex_conj(Complex z);
long long complex_eq(Complex a, Complex b);
Complex complex_add(Complex a, Complex b);
Complex complex_mul(Complex a, Complex b);
Complex * conj_vector(Complex * v, size_t v_len);
Complex * vec_mat_mul(Complex * v, size_t v_len, Complex * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
Complex dot(Complex * a, size_t a_len, Complex * b, size_t b_len);
long long is_hermitian(Complex * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
double rayleigh_quotient(Complex * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, Complex * v, size_t v_len);
int main(void);

Complex complex_conj(Complex z) {
    return (Complex){.re = z.re, .im = -(z.im)};
}

long long complex_eq(Complex a, Complex b) {
    return (a.re == b.re) && (a.im == b.im);
}

Complex complex_add(Complex a, Complex b) {
    return (Complex){.re = a.re + b.re, .im = a.im + b.im};
}

Complex complex_mul(Complex a, Complex b) {
    double real = (a.re * b.re) - (a.im * b.im);
    double imag = (a.re * b.im) + (a.im * b.re);
    return (Complex){.re = real, .im = imag};
}

Complex * conj_vector(Complex * v, size_t v_len) {
    Complex *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < v_len) {
        res = list_append_Complex(res, &res_len, complex_conj(v[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return conj_vector_len = res_len, res;
}

Complex * vec_mat_mul(Complex * v, size_t v_len, Complex * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    Complex *result = NULL;
    size_t result_len = 0;
    long long col = 0LL;
    while (col < m_lens[0LL]) {
        Complex sum = (Complex){.re = 0LL, .im = 0LL};
        long long row = 0LL;
        while (row < v_len) {
            sum = complex_add(sum, complex_mul(v[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})], m[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = col; _mochi_idx < 0 ? m_lens[row] + _mochi_idx : _mochi_idx;})]));
            row = row + 1LL;
        }
        result = list_append_Complex(result, &result_len, sum);
        col = col + 1LL;
    }
    return vec_mat_mul_len = result_len, result;
}

Complex dot(Complex * a, size_t a_len, Complex * b, size_t b_len) {
    Complex sum = (Complex){.re = 0LL, .im = 0LL};
    long long i = 0LL;
    while (i < a_len) {
        sum = complex_add(sum, complex_mul(a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})], b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]));
        i = i + 1LL;
    }
    return sum;
}

long long is_hermitian(Complex * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long i = 0LL;
    while (i < m_len) {
        long long j = 0LL;
        while (j < m_len) {
            if (!(complex_eq(m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m_lens[i] + _mochi_idx : _mochi_idx;})], complex_conj(m[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_lens[j] + _mochi_idx : _mochi_idx;})])))) {
                return 0LL;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

double rayleigh_quotient(Complex * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, Complex * v, size_t v_len) {
    Complex *v_star = conj_vector(v, v_len);
    size_t v_star_len = conj_vector_len;
    Complex *v_star_dot = vec_mat_mul(v_star, v_star_len, a, a_len, a_lens, a_len);
    size_t v_star_dot_len = vec_mat_mul_len;
    Complex num = dot(v_star_dot, v_star_dot_len, v, v_len);
    Complex den = dot(v_star, v_star_len, v, v_len);
    return num.re / den.re;
}

int main(void) {
    {
        long long __start = _now();
        if (is_hermitian(a, a_len, a_lens, a_len)) {
            double r1 = rayleigh_quotient(a, a_len, a_lens, a_len, v, v_len);
            printf("%g\n", r1);
            puts("\n");
        }
        if (is_hermitian(b, b_len, b_lens, b_len)) {
            double r2 = rayleigh_quotient(b, b_len, b_lens, b_len, v, v_len);
            printf("%g\n", r2);
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
