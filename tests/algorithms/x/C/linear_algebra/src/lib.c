// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Matrix Matrix;
typedef struct Vector Vector;

struct Matrix {
    double **data;
    size_t data_len;
    size_t *data_lens;
    size_t data_lens_len;
    long long width;
    long long height;
};

struct Vector {
    double *components;
    size_t components_len;
};

double PI = 3.141592653589793;
long long seed = 123456789LL;

long long user_rand();
long long random_int(long long a, long long b);
double sqrtApprox(double x);
double arcsin_taylor(double x);
double acos_taylor(double x);
long long vector_len(Vector v);
const char* vector_to_string(Vector v);
Vector vector_add(Vector a, Vector b);
Vector vector_sub(Vector a, Vector b);
long long vector_eq(Vector a, Vector b);
Vector vector_mul_scalar(Vector v, double s);
double vector_dot(Vector a, Vector b);
Vector vector_copy(Vector v);
double vector_component(Vector v, long long idx);
Vector vector_change_component(Vector v, long long pos, double value);
double vector_euclidean_length(Vector v);
double vector_angle(Vector a, Vector b, long long deg);
Vector zero_vector(long long d);
Vector unit_basis_vector(long long d, long long pos);
Vector axpy(double s, Vector x, Vector y);
Vector random_vector(long long n, long long a, long long b);
const char* matrix_to_string(Matrix m);
Matrix matrix_add(Matrix a, Matrix b);
Matrix matrix_sub(Matrix a, Matrix b);
Vector matrix_mul_vector(Matrix m, Vector v);
Matrix matrix_mul_scalar(Matrix m, double s);
double matrix_component(Matrix m, long long x, long long y);
Matrix matrix_change_component(Matrix m, long long x, long long y, double value);
double matrix_minor(Matrix m, long long x, long long y);
double matrix_cofactor(Matrix m, long long x, long long y);
double matrix_determinant(Matrix m);
Matrix square_zero_matrix(long long n);
Matrix random_matrix(long long w, long long h, long long a, long long b);
void user_main();
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed;
}

long long random_int(long long a, long long b) {
    long long r = user_rand() % ((b - a) + 1LL);
    return a + r;
}

double sqrtApprox(double x) {
    if (x <= 0LL) {
        return 0LL;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2LL;
        i = i + 1LL;
    }
    return guess;
}

double arcsin_taylor(double x) {
    double term = x;
    double sum = x;
    long long n = 1LL;
    while (n < 10LL) {
        double num = (((((2LL * (double)(n)) - 1LL) * ((2LL * (double)(n)) - 1LL)) * x) * x) * term;
        double den = (2LL * (double)(n)) * ((2LL * (double)(n)) + 1LL);
        term = num / den;
        sum = sum + term;
        n = n + 1LL;
    }
    return sum;
}

double acos_taylor(double x) {
    return (PI / 2LL) - arcsin_taylor(x);
}

long long vector_len(Vector v) {
    return v.components_len;
}

const char* vector_to_string(Vector v) {
    const char* s = "(";
    long long i = 0LL;
    while (i < v.components_len) {
        s = str_concat(s, str_float(v.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})]));
        if (i < (v.components_len - 1LL)) {
            s = str_concat(s, ",");
        }
        i = i + 1LL;
    }
    s = str_concat(s, ")");
    return s;
}

Vector vector_add(Vector a, Vector b) {
    long long size = vector_len(a);
    if (size != vector_len(b)) {
        return (Vector){.components = NULL, .components_len = 0};
    }
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < size) {
        res = list_append_double(res, &res_len, a.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.components_len + _mochi_idx : _mochi_idx;})] + b.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.components_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

Vector vector_sub(Vector a, Vector b) {
    long long size = vector_len(a);
    if (size != vector_len(b)) {
        return (Vector){.components = NULL, .components_len = 0};
    }
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < size) {
        res = list_append_double(res, &res_len, a.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.components_len + _mochi_idx : _mochi_idx;})] - b.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.components_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

long long vector_eq(Vector a, Vector b) {
    if (vector_len(a) != vector_len(b)) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < vector_len(a)) {
        if (a.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.components_len + _mochi_idx : _mochi_idx;})] != b.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.components_len + _mochi_idx : _mochi_idx;})]) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

Vector vector_mul_scalar(Vector v, double s) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < vector_len(v)) {
        res = list_append_double(res, &res_len, v.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})] * s);
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

double vector_dot(Vector a, Vector b) {
    long long size = vector_len(a);
    if (size != vector_len(b)) {
        return 0LL;
    }
    double sum = 0LL;
    long long i = 0LL;
    while (i < size) {
        sum = sum + (a.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.components_len + _mochi_idx : _mochi_idx;})] * b.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.components_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return sum;
}

Vector vector_copy(Vector v) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < vector_len(v)) {
        res = list_append_double(res, &res_len, v.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

double vector_component(Vector v, long long idx) {
    return v.components[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})];
}

Vector vector_change_component(Vector v, long long pos, double value) {
    double *comps = v.components;
    size_t comps_len = v.components_len;
    comps[(int)(pos)] = value;
    return (Vector){.components = comps, .components_len = comps_len};
}

double vector_euclidean_length(Vector v) {
    double sum = 0LL;
    long long i = 0LL;
    while (i < v.components_len) {
        sum = sum + (v.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})] * v.components[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    double result = sqrtApprox(sum);
    return result;
}

double vector_angle(Vector a, Vector b, long long deg) {
    double num = vector_dot(a, b);
    double den = vector_euclidean_length(a) * vector_euclidean_length(b);
    double ang = acos_taylor(num / den);
    if (deg) {
        ang = (ang * 180LL) / PI;
    }
    return ang;
}

Vector zero_vector(long long d) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < d) {
        res = list_append_double(res, &res_len, 0LL);
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

Vector unit_basis_vector(long long d, long long pos) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < d) {
        if (i == pos) {
            res = list_append_double(res, &res_len, 1LL);
        } else {
            res = list_append_double(res, &res_len, 0LL);
        }
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

Vector axpy(double s, Vector x, Vector y) {
    return vector_add(vector_mul_scalar(x, s), y);
}

Vector random_vector(long long n, long long a, long long b) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        res = list_append_double(res, &res_len, (double)(random_int(a, b)));
        i = i + 1LL;
    }
    return (Vector){.components = res, .components_len = res_len};
}

const char* matrix_to_string(Matrix m) {
    const char* ans = "";
    long long i = 0LL;
    while (i < m.height) {
        ans = str_concat(ans, "|");
        long long j = 0LL;
        while (j < m.width) {
            ans = str_concat(ans, str_float(m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})]));
            if (j < (m.width - 1LL)) {
                ans = str_concat(ans, ",");
            }
            j = j + 1LL;
        }
        ans = str_concat(ans, "|\n");
        i = i + 1LL;
    }
    return ans;
}

Matrix matrix_add(Matrix a, Matrix b) {
    if ((a.width != b.width) || (a.height != b.height)) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .width = 0LL, .height = 0LL};
    }
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < a.height) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < a.width) {
            row = list_append_double(row, &row_len, a.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a.data_lens[i] + _mochi_idx : _mochi_idx;})] + b.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b.data_lens[i] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = mat, .data_len = mat_len, .data_lens = mat_lens, .data_lens_len = mat_len, .width = a.width, .height = a.height};
}

Matrix matrix_sub(Matrix a, Matrix b) {
    if ((a.width != b.width) || (a.height != b.height)) {
        return (Matrix){.data = NULL, .data_len = 0, .data_lens = NULL, .data_lens_len = 0, .width = 0LL, .height = 0LL};
    }
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < a.height) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < a.width) {
            row = list_append_double(row, &row_len, a.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? a.data_lens[i] + _mochi_idx : _mochi_idx;})] - b.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? b.data_lens[i] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = mat, .data_len = mat_len, .data_lens = mat_lens, .data_lens_len = mat_len, .width = a.width, .height = a.height};
}

Vector matrix_mul_vector(Matrix m, Vector v) {
    if (v.components_len != m.width) {
        return (Vector){.components = NULL, .components_len = 0};
    }
    Vector res = zero_vector(m.height);
    long long i = 0LL;
    while (i < m.height) {
        double sum = 0LL;
        long long j = 0LL;
        while (j < m.width) {
            sum = sum + (m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})] * v.components[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? v.components_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        res = vector_change_component(res, i, sum);
        i = i + 1LL;
    }
    return res;
}

Matrix matrix_mul_scalar(Matrix m, double s) {
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < m.height) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < m.width) {
            row = list_append_double(row, &row_len, m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})] * s);
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = mat, .data_len = mat_len, .data_lens = mat_lens, .data_lens_len = mat_len, .width = m.width, .height = m.height};
}

double matrix_component(Matrix m, long long x, long long y) {
    return m.data[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? m.data_lens[x] + _mochi_idx : _mochi_idx;})];
}

Matrix matrix_change_component(Matrix m, long long x, long long y, double value) {
    double **data = m.data;
    size_t data_len = m.data_len;
    size_t *data_lens = m.data_lens;
    size_t data_lens_len = m.data_len;
    data[(int)(x)][(int)(y)] = value;
    return (Matrix){.data = data, .data_len = data_len, .data_lens = data_lens, .data_lens_len = data_len, .width = m.width, .height = m.height};
}

double matrix_minor(Matrix m, long long x, long long y) {
    if (m.height != m.width) {
        return 0LL;
    }
    double **minor = NULL;
    size_t minor_len = 0;
    size_t *minor_lens = NULL;
    size_t minor_lens_len = 0;
    long long i = 0LL;
    while (i < m.height) {
        if (i != x) {
            double *row = NULL;
            size_t row_len = 0;
            long long j = 0LL;
            while (j < m.width) {
                if (j != y) {
                    row = list_append_double(row, &row_len, m.data[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m.data_lens[i] + _mochi_idx : _mochi_idx;})]);
                }
                j = j + 1LL;
            }
            minor = list_append_doubleptr(minor, &minor_len, row);
            minor_lens = list_append_szt(minor_lens, &minor_lens_len, row_len);
        }
        i = i + 1LL;
    }
    Matrix sub = (Matrix){.data = minor, .data_len = minor_len, .data_lens = minor_lens, .data_lens_len = minor_len, .width = m.width - 1LL, .height = m.height - 1LL};
    return matrix_determinant(sub);
}

double matrix_cofactor(Matrix m, long long x, long long y) {
    long long sign;
    if (((x + y) % 2LL) == 0LL) {
        sign = 1LL;
    } else {
        sign = -1LL;
    }
    return sign * matrix_minor(m, x, y);
}

double matrix_determinant(Matrix m) {
    if (m.height != m.width) {
        return 0LL;
    }
    if (m.height == 0LL) {
        return 0LL;
    }
    if (m.height == 1LL) {
        return m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})];
    }
    if (m.height == 2LL) {
        return (m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})] * m.data[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_lens[1LL] + _mochi_idx : _mochi_idx;})]) - (m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})] * m.data[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_lens[1LL] + _mochi_idx : _mochi_idx;})]);
    }
    double sum = 0LL;
    long long y = 0LL;
    while (y < m.width) {
        sum = sum + (m.data[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? m.data_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? m.data_lens[0LL] + _mochi_idx : _mochi_idx;})] * matrix_cofactor(m, 0LL, y));
        y = y + 1LL;
    }
    return sum;
}

Matrix square_zero_matrix(long long n) {
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, 0LL);
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = mat, .data_len = mat_len, .data_lens = mat_lens, .data_lens_len = mat_len, .width = n, .height = n};
}

Matrix random_matrix(long long w, long long h, long long a, long long b) {
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long i = 0LL;
    while (i < h) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < w) {
            row = list_append_double(row, &row_len, (double)(random_int(a, b)));
            j = j + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        i = i + 1LL;
    }
    return (Matrix){.data = mat, .data_len = mat_len, .data_lens = mat_lens, .data_lens_len = mat_len, .width = w, .height = h};
}

void user_main() {
    Vector v1 = (Vector){.components = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp[2] = 3LL; tmp;}), .components_len = 3};
    Vector v2 = (Vector){.components = ({long long *tmp = malloc(3 * sizeof(long long)); tmp[0] = 4LL; tmp[1] = 5LL; tmp[2] = 6LL; tmp;}), .components_len = 3};
    puts(vector_to_string(vector_add(v1, v2)));
    puts(str_float(vector_dot(v1, v2)));
    puts(str_float(vector_euclidean_length(v1)));
    Matrix m = (Matrix){.data = ({long long** tmp = malloc(2 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp;}); tmp[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 3LL; tmp[1] = 4LL; tmp;}); tmp;}), .data_len = 2, .data_lens = ({size_t *tmp = malloc(2 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp;}), .data_lens_len = 2, .width = 2LL, .height = 2LL};
    puts(str_float(matrix_determinant(m)));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
