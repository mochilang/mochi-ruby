// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t zeros_len;
size_t mat_vec_mul_len;
size_t vec_add_len;
size_t vec_sub_len;
size_t scalar_mul_len;
size_t conjugate_gradient_len;

size_t append_len;
static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double A_0[3] = {8.73256573, -5.02034289, -2.68709226};
double A_1[3] = {-5.02034289, 3.78188322, 0.91980451};
double A_2[3] = {-2.68709226, 0.91980451, 1.94746467};
double *A_init[3] = {A_0, A_1, A_2};
double **A = A_init;
size_t A_len = 3;
size_t A_lens_init[3] = {3, 3, 3};
size_t *A_lens = A_lens_init;
size_t A_lens_len = 3;
double b_init[3] = {-5.80872761, 3.23807431, 1.95381422};
double *b = b_init;
size_t b_len = 3;
double *x = NULL;
size_t x_len = 0;

double * zeros(long long n);
double dot(double * a, size_t a_len, double * b, size_t b_len);
double * mat_vec_mul(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, double * v, size_t v_len);
double * vec_add(double * a, size_t a_len, double * b, size_t b_len);
double * vec_sub(double * a, size_t a_len, double * b, size_t b_len);
double * scalar_mul(double s, double * v, size_t v_len);
double sqrtApprox(double x);
double norm(double * v, size_t v_len);
double * conjugate_gradient(double * * A, size_t A_len, size_t* A_lens, size_t A_lens_len, double * b, size_t b_len, long long max_iterations, double tol);
int main(void);

double * zeros(long long n) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < n) {
        res = list_append_double(res, &res_len, 0LL);
        i = i + 1LL;
    }
    return zeros_len = res_len, res;
}

double dot(double * a, size_t a_len, double * b, size_t b_len) {
    double sum = 0LL;
    long long i = 0LL;
    while (i < a_len) {
        sum = sum + (a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] * b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return sum;
}

double * mat_vec_mul(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len, double * v, size_t v_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < m_len) {
        double s = 0LL;
        long long j = 0LL;
        while (j < m_lens[i]) {
            s = s + (m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? m_lens[i] + _mochi_idx : _mochi_idx;})] * v[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        res = list_append_double(res, &res_len, s);
        i = i + 1LL;
    }
    return mat_vec_mul_len = res_len, res;
}

double * vec_add(double * a, size_t a_len, double * b, size_t b_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < a_len) {
        res = list_append_double(res, &res_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] + b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return vec_add_len = res_len, res;
}

double * vec_sub(double * a, size_t a_len, double * b, size_t b_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < a_len) {
        res = list_append_double(res, &res_len, a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})] - b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return vec_sub_len = res_len, res;
}

double * scalar_mul(double s, double * v, size_t v_len) {
    double *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < v_len) {
        res = list_append_double(res, &res_len, s * v[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? v_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return scalar_mul_len = res_len, res;
}

double sqrtApprox(double x) {
    if (x <= 0LL) {
        return 0LL;
    }
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2LL;
        i = i + 1LL;
    }
    return guess;
}

double norm(double * v, size_t v_len) {
    return sqrtApprox(dot(v, v_len, v, v_len));
}

double * conjugate_gradient(double * * A, size_t A_len, size_t* A_lens, size_t A_lens_len, double * b, size_t b_len, long long max_iterations, double tol) {
    long long n = b_len;
    double *x = zeros(n);
    size_t x_len = zeros_len;
    double *r = vec_sub(b, b_len, mat_vec_mul(A, A_len, A_lens, A_len, x, x_len), mat_vec_mul_len);
    size_t r_len = vec_sub_len;
    double *p = r;
    size_t p_len = r_len;
    double rs_old = dot(r, r_len, r, r_len);
    long long i = 0LL;
    while (i < max_iterations) {
        double *Ap = mat_vec_mul(A, A_len, A_lens, A_len, p, p_len);
        size_t Ap_len = mat_vec_mul_len;
        double alpha = rs_old / dot(p, p_len, Ap, Ap_len);
        x = vec_add(x, x_len, scalar_mul(alpha, p, p_len), scalar_mul_len);
        x_len = vec_add_len;
        r = vec_sub(r, r_len, scalar_mul(alpha, Ap, Ap_len), scalar_mul_len);
        r_len = vec_sub_len;
        double rs_new = dot(r, r_len, r, r_len);
        if (sqrtApprox(rs_new) < tol) {
            break;
        }
        double beta = rs_new / rs_old;
        p = vec_add(r, r_len, scalar_mul(beta, p, p_len), scalar_mul_len);
        p_len = vec_add_len;
        rs_old = rs_new;
        i = i + 1LL;
    }
    return conjugate_gradient_len = x_len, x;
}

int main(void) {
    {
        long long __start = _now();
        x = conjugate_gradient(A, A_len, A_lens, A_len, b, b_len, 1000LL, 1e-08);
        x_len = conjugate_gradient_len;
        puts(str_float(x[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]));
        puts(str_float(x[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]));
        puts(str_float(x[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
