// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t scaling_len;
size_t *scaling_lens;
size_t rotation_len;
size_t *rotation_lens;
size_t projection_len;
size_t *projection_lens;
size_t reflection_len;
size_t *reflection_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double PI = 3.141592653589793;

double user_floor(double x);
double user_modf(double x, double m);
double sin_taylor(double angle);
double cos_taylor(double angle);
const char* matrix_to_string(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
double * * scaling(double f);
double * * rotation(double angle);
double * * projection(double angle);
double * * reflection(double angle);
int main(void);

double user_floor(double x) {
    long long i = (int)(x);
    if ((double)(i) > x) {
        i = i - 1LL;
    }
    return (double)(i);
}

double user_modf(double x, double m) {
    return x - (user_floor(x / m) * m);
}

double sin_taylor(double angle) {
    double x = user_modf(angle, 2LL * PI);
    if (x > PI) {
        x = x - (2LL * PI);
    }
    double term = x;
    double sum = x;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = 2LL * (double)(i);
        double k2 = k1 + 1LL;
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

double cos_taylor(double angle) {
    double x = user_modf(angle, 2LL * PI);
    if (x > PI) {
        x = x - (2LL * PI);
    }
    double term = 1LL;
    double sum = 1LL;
    long long i = 1LL;
    while (i < 10LL) {
        double k1 = (2LL * (double)(i)) - 1LL;
        double k2 = 2LL * (double)(i);
        term = ((-(term) * x) * x) / (k1 * k2);
        sum = sum + term;
        i = i + 1LL;
    }
    return sum;
}

const char* matrix_to_string(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < m_len) {
        double *row = m[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? m_len + _mochi_idx : _mochi_idx;})];
        size_t row_len = m_lens[i];
        s = str_concat(s, "[");
        long long j = 0LL;
        while (j < row_len) {
            s = str_concat(s, str_float(row[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})]));
            if (j < (row_len - 1LL)) {
                s = str_concat(s, ", ");
            }
            j = j + 1LL;
        }
        s = str_concat(s, "]");
        if (i < (m_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

double * * scaling(double f) {
    {
        size_t *__tmp4_lens = malloc(2 * sizeof(size_t));
        __tmp4_lens[0] = 2;
        __tmp4_lens[1] = 2;
        double** __tmp4_data = malloc(2 * sizeof(double*));
        __tmp4_data[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = f; tmp[1] = 0LL; tmp;});
        __tmp4_data[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = f; tmp;});
        scaling_lens = __tmp4_lens;
        scaling_len = 2;
        return __tmp4_data;
    }
}

double * * rotation(double angle) {
    double c = cos_taylor(angle);
    double s = sin_taylor(angle);
    {
        size_t *__tmp5_lens = malloc(2 * sizeof(size_t));
        __tmp5_lens[0] = 2;
        __tmp5_lens[1] = 2;
        double** __tmp5_data = malloc(2 * sizeof(double*));
        __tmp5_data[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = c; tmp[1] = -(s); tmp;});
        __tmp5_data[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = s; tmp[1] = c; tmp;});
        rotation_lens = __tmp5_lens;
        rotation_len = 2;
        return __tmp5_data;
    }
}

double * * projection(double angle) {
    double c = cos_taylor(angle);
    double s = sin_taylor(angle);
    double cs = c * s;
    {
        size_t *__tmp6_lens = malloc(2 * sizeof(size_t));
        __tmp6_lens[0] = 2;
        __tmp6_lens[1] = 2;
        double** __tmp6_data = malloc(2 * sizeof(double*));
        __tmp6_data[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = c * c; tmp[1] = cs; tmp;});
        __tmp6_data[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = cs; tmp[1] = s * s; tmp;});
        projection_lens = __tmp6_lens;
        projection_len = 2;
        return __tmp6_data;
    }
}

double * * reflection(double angle) {
    double c = cos_taylor(angle);
    double s = sin_taylor(angle);
    double cs = c * s;
    {
        size_t *__tmp7_lens = malloc(2 * sizeof(size_t));
        __tmp7_lens[0] = 2;
        __tmp7_lens[1] = 2;
        double** __tmp7_data = malloc(2 * sizeof(double*));
        __tmp7_data[0] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = (2LL * c) - 1LL; tmp[1] = 2LL * cs; tmp;});
        __tmp7_data[1] = ({double *tmp = malloc(2 * sizeof(double)); tmp[0] = 2LL * cs; tmp[1] = (2LL * s) - 1LL; tmp;});
        reflection_lens = __tmp7_lens;
        reflection_len = 2;
        return __tmp7_data;
    }
}

int main(void) {
    {
        long long __start = _now();
        puts(str_concat("    scaling(5) = ", ({double** __tmp8 = scaling(5LL); matrix_to_string(__tmp8, scaling_len, scaling_lens, scaling_len);})));
        puts(str_concat("  rotation(45) = ", ({double** __tmp9 = rotation(45LL); matrix_to_string(__tmp9, rotation_len, rotation_lens, rotation_len);})));
        puts(str_concat("projection(45) = ", ({double** __tmp10 = projection(45LL); matrix_to_string(__tmp10, projection_len, projection_lens, projection_len);})));
        puts(str_concat("reflection(45) = ", ({double** __tmp11 = reflection(45LL); matrix_to_string(__tmp11, reflection_len, reflection_lens, reflection_len);})));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
