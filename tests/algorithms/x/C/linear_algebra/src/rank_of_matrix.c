// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long rank_of_matrix(double * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
int main(void);

long long rank_of_matrix(double * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    long long rows = matrix_len;
    if (rows == 0LL) {
        return 0LL;
    }
    long long columns;
    if (matrix_lens[0LL] > 0LL) {
        columns = matrix_lens[0LL];
    } else {
        columns = 0LL;
    }
    long long rank;
    if (rows < columns) {
        rank = rows;
    } else {
        rank = columns;
    }
    long long row = 0LL;
    while (row < rank) {
        if (matrix[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_lens[row] + _mochi_idx : _mochi_idx;})] != 0LL) {
            long long col = row + 1LL;
            while (col < rows) {
                double mult = matrix[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_lens[col] + _mochi_idx : _mochi_idx;})] / matrix[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_lens[row] + _mochi_idx : _mochi_idx;})];
                long long i = row;
                while (i < columns) {
                    matrix[(int)(col)][(int)(i)] = matrix[(int)({long long _mochi_idx = col; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_lens[col] + _mochi_idx : _mochi_idx;})] - (mult * matrix[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_lens[row] + _mochi_idx : _mochi_idx;})]);
                    i = i + 1LL;
                }
                col = col + 1LL;
            }
        } else {
            long long reduce = 1LL;
            long long i = row + 1LL;
            while (i < rows) {
                if (matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_lens[i] + _mochi_idx : _mochi_idx;})] != 0LL) {
                    double *temp = matrix[(int)({long long _mochi_idx = row; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})];
                    size_t temp_len = matrix_lens[row];
                    matrix[(int)(row)] = matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})];
                    matrix[(int)(i)] = temp;
                    reduce = 0LL;
                    break;
                }
                i = i + 1LL;
            }
            if (reduce) {
                rank = rank - 1LL;
                long long j = 0LL;
                while (j < rows) {
                    matrix[(int)(j)][(int)(row)] = matrix[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = rank; _mochi_idx < 0 ? matrix_lens[j] + _mochi_idx : _mochi_idx;})];
                    j = j + 1LL;
                }
            }
            row = row - 1LL;
        }
        row = row + 1LL;
    }
    return rank;
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
