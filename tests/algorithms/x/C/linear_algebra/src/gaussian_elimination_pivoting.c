// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t copy_matrix_len;
size_t *copy_matrix_lens;
size_t solve_linear_system_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_double(double **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_double(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double example_matrix_0[5] = {5LL, -5LL, -3LL, 4LL, -11LL};
double example_matrix_1[5] = {1LL, -4LL, 6LL, -4LL, -10LL};
double example_matrix_2[5] = {-2LL, -5LL, 4LL, -5LL, -12LL};
double example_matrix_3[5] = {-3LL, -3LL, 5LL, -5LL, 8LL};
double *example_matrix_init[4] = {example_matrix_0, example_matrix_1, example_matrix_2, example_matrix_3};
double **example_matrix = example_matrix_init;
size_t example_matrix_len = 4;
size_t example_matrix_lens_init[4] = {5, 5, 5, 5};
size_t *example_matrix_lens = example_matrix_lens_init;
size_t example_matrix_lens_len = 4;
double *solution = NULL;
size_t solution_len = 0;

void user_panic(const char* msg);
double abs_float(double x);
double * * copy_matrix(double * * src, size_t src_len, size_t* src_lens, size_t src_lens_len);
double * solve_linear_system(double * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
int main(void);

void user_panic(const char* msg) {
    puts(msg);
}

double abs_float(double x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

double * * copy_matrix(double * * src, size_t src_len, size_t* src_lens, size_t src_lens_len) {
    double **res = NULL;
    size_t res_len = 0;
    size_t *res_lens = NULL;
    size_t res_lens_len = 0;
    long long i = 0LL;
    while (i < src_len) {
        double *row_src = src[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? src_len + _mochi_idx : _mochi_idx;})];
        size_t row_src_len = src_lens[i];
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < row_src_len) {
            row = list_append_double(row, &row_len, row_src[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? row_src_len + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        res = list_append_doubleptr(res, &res_len, row);
        res_lens = list_append_szt(res_lens, &res_lens_len, row_len);
        i = i + 1LL;
    }
    return copy_matrix_lens = res_lens, copy_matrix_len = res_len, res;
}

double * solve_linear_system(double * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    double **ab = copy_matrix(matrix, matrix_len, matrix_lens, matrix_len);
    size_t ab_len = copy_matrix_len;
    size_t *ab_lens = copy_matrix_lens;
    size_t ab_lens_len = copy_matrix_len;
    long long num_rows = ab_len;
    long long num_cols = ab_lens[0LL] - 1LL;
    if (num_rows != num_cols) {
        user_panic("Matrix is not square");
        {
            double* __tmp0_data = malloc(0 * sizeof(double));
            solve_linear_system_len = 0;
            return __tmp0_data;
        }
    }
    long long column_num = 0LL;
    while (column_num < num_rows) {
        long long i = column_num;
        while (i < num_cols) {
            if (abs_float(ab[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_lens[i] + _mochi_idx : _mochi_idx;})]) > abs_float(ab[(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_lens[column_num] + _mochi_idx : _mochi_idx;})])) {
                double *temp = ab[(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})];
                size_t temp_len = ab_lens[column_num];
                ab[(int)(column_num)] = ab[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})];
                ab[(int)(i)] = temp;
            }
            i = i + 1LL;
        }
        if (abs_float(ab[(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_lens[column_num] + _mochi_idx : _mochi_idx;})]) < 1e-08) {
            user_panic("Matrix is singular");
            {
                double* __tmp1_data = malloc(0 * sizeof(double));
                solve_linear_system_len = 0;
                return __tmp1_data;
            }
        }
        if (column_num != 0LL) {
            i = column_num;
            while (i < num_rows) {
                double factor = ab[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num - 1LL; _mochi_idx < 0 ? ab_lens[i] + _mochi_idx : _mochi_idx;})] / ab[(int)({long long _mochi_idx = column_num - 1LL; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num - 1LL; _mochi_idx < 0 ? ab_lens[column_num - 1LL] + _mochi_idx : _mochi_idx;})];
                long long j = 0LL;
                while (j < ab_lens[i]) {
                    ab[(int)(i)][(int)(j)] = ab[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ab_lens[i] + _mochi_idx : _mochi_idx;})] - (factor * ab[(int)({long long _mochi_idx = column_num - 1LL; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? ab_lens[column_num - 1LL] + _mochi_idx : _mochi_idx;})]);
                    j = j + 1LL;
                }
                i = i + 1LL;
            }
        }
        column_num = column_num + 1LL;
    }
    double *x_lst = NULL;
    size_t x_lst_len = 0;
    long long t = 0LL;
    while (t < num_rows) {
        x_lst = list_append_double(x_lst, &x_lst_len, 0LL);
        t = t + 1LL;
    }
    column_num = num_rows - 1LL;
    while (column_num >= 0LL) {
        double x = ab[(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = num_cols; _mochi_idx < 0 ? ab_lens[column_num] + _mochi_idx : _mochi_idx;})] / ab[(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_lens[column_num] + _mochi_idx : _mochi_idx;})];
        x_lst[(int)(column_num)] = x;
        long long i = column_num - 1LL;
        while (i >= 0LL) {
            ab[(int)(i)][(int)(num_cols)] = ab[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = num_cols; _mochi_idx < 0 ? ab_lens[i] + _mochi_idx : _mochi_idx;})] - (ab[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? ab_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = column_num; _mochi_idx < 0 ? ab_lens[i] + _mochi_idx : _mochi_idx;})] * x);
            i = i - 1LL;
        }
        column_num = column_num - 1LL;
    }
    return solve_linear_system_len = x_lst_len, x_lst;
}

int main(void) {
    {
        long long __start = _now();
        puts("Matrix:");
        puts(str_list_list_double(example_matrix, example_matrix_len, example_matrix_lens));
        solution = solve_linear_system(example_matrix, example_matrix_len, example_matrix_lens, example_matrix_len);
        solution_len = solve_linear_system_len;
        puts(str_list_double(solution, solution_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
