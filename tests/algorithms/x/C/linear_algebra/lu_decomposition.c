// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct LU LU;

struct LU {
    double **lower;
    size_t lower_len;
    size_t *lower_lens;
    size_t lower_lens_len;
    double **upper;
    size_t upper_len;
    size_t *upper_lens;
    size_t upper_lens_len;
};

double matrix_0[3] = {2LL, -2LL, 1LL};
double matrix_1[3] = {0LL, 1LL, 2LL};
double matrix_2[3] = {5LL, 3LL, 1LL};
double *matrix_init[3] = {matrix_0, matrix_1, matrix_2};
double **matrix = matrix_init;
size_t matrix_len = 3;
size_t matrix_lens_init[3] = {3, 3, 3};
size_t *matrix_lens = matrix_lens_init;
size_t matrix_lens_len = 3;
LU result;

LU lu_decomposition(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
void print_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len);
int main(void);

LU lu_decomposition(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long n = mat_len;
    if (n == 0LL) {
        return (LU){.lower = NULL, .lower_len = 0, .lower_lens = NULL, .lower_lens_len = 0, .upper = NULL, .upper_len = 0, .upper_lens = NULL, .upper_lens_len = 0};
    }
    long long m = mat_lens[0LL];
    if (n != m) {
        panic("Matrix must be square");
    }
    double **lower = NULL;
    size_t lower_len = 0;
    size_t *lower_lens = NULL;
    size_t lower_lens_len = 0;
    double **upper = NULL;
    size_t upper_len = 0;
    size_t *upper_lens = NULL;
    size_t upper_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *lrow = NULL;
        size_t lrow_len = 0;
        double *urow = NULL;
        size_t urow_len = 0;
        long long j = 0LL;
        while (j < n) {
            lrow = list_append_double(lrow, &lrow_len, 0LL);
            urow = list_append_double(urow, &urow_len, 0LL);
            j = j + 1LL;
        }
        lower = list_append_doubleptr(lower, &lower_len, lrow);
        lower_lens = list_append_szt(lower_lens, &lower_lens_len, lrow_len);
        upper = list_append_doubleptr(upper, &upper_len, urow);
        upper_lens = list_append_szt(upper_lens, &upper_lens_len, urow_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        long long j1 = 0LL;
        while (j1 < i) {
            double total = 0LL;
            long long k = 0LL;
            while (k < i) {
                total = total + (lower[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lower_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? lower_lens[i] + _mochi_idx : _mochi_idx;})] * upper[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? upper_lens[k] + _mochi_idx : _mochi_idx;})]);
                k = k + 1LL;
            }
            if (upper[(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? upper_lens[j1] + _mochi_idx : _mochi_idx;})] == 0LL) {
                panic("No LU decomposition exists");
            }
            lower[(int)(i)][(int)(j1)] = (mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? mat_lens[i] + _mochi_idx : _mochi_idx;})] - total) / upper[(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j1; _mochi_idx < 0 ? upper_lens[j1] + _mochi_idx : _mochi_idx;})];
            j1 = j1 + 1LL;
        }
        lower[(int)(i)][(int)(i)] = 1LL;
        long long j2 = i;
        while (j2 < n) {
            double total2 = 0LL;
            long long k2 = 0LL;
            while (k2 < i) {
                total2 = total2 + (lower[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lower_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k2; _mochi_idx < 0 ? lower_lens[i] + _mochi_idx : _mochi_idx;})] * upper[(int)({long long _mochi_idx = k2; _mochi_idx < 0 ? upper_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2; _mochi_idx < 0 ? upper_lens[k2] + _mochi_idx : _mochi_idx;})]);
                k2 = k2 + 1LL;
            }
            upper[(int)(i)][(int)(j2)] = mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j2; _mochi_idx < 0 ? mat_lens[i] + _mochi_idx : _mochi_idx;})] - total2;
            j2 = j2 + 1LL;
        }
        i = i + 1LL;
    }
    return (LU){.lower = lower, .lower_len = lower_len, .lower_lens = lower_lens, .lower_lens_len = lower_len, .upper = upper, .upper_len = upper_len, .upper_lens = upper_lens, .upper_lens_len = upper_len};
}

void print_matrix(double * * mat, size_t mat_len, size_t* mat_lens, size_t mat_lens_len) {
    long long i = 0LL;
    while (i < mat_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < mat_lens[i]) {
            line = str_concat(line, str_float(mat[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mat_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? mat_lens[i] + _mochi_idx : _mochi_idx;})]));
            if ((j + 1LL) < mat_lens[i]) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        result = lu_decomposition(matrix, matrix_len, matrix_lens, matrix_len);
        print_matrix(result.lower, result.lower_len, result.lower_lens, result.lower_len);
        print_matrix(result.upper, result.upper_len, result.upper_lens, result.upper_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
