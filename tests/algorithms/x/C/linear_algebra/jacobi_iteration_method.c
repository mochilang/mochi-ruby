// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t jacobi_iteration_method_len;

size_t append_len;
static char* str_list_double(const double *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[64];
        snprintf(tmp, sizeof(tmp), "%g", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long coefficient_0[3] = {4LL, 1LL, 1LL};
long long coefficient_1[3] = {1LL, 5LL, 2LL};
long long coefficient_2[3] = {1LL, 2LL, 4LL};
long long *coefficient_init[3] = {coefficient_0, coefficient_1, coefficient_2};
long long **coefficient = coefficient_init;
size_t coefficient_len = 3;
size_t coefficient_lens_init[3] = {3, 3, 3};
size_t *coefficient_lens = coefficient_lens_init;
size_t coefficient_lens_len = 3;
long long constant_init[3] = {2LL, -6LL, -4LL};
long long *constant = constant_init;
size_t constant_len = 3;
double init_val_init[3] = {0.5, -0.5, -0.5};
double *init_val = init_val_init;
size_t init_val_len = 3;
long long iterations = 3LL;
double *result = NULL;
size_t result_len = 0;

double absf(double x);
long long strictly_diagonally_dominant(double * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len);
double * jacobi_iteration_method(double * * coefficient, size_t coefficient_len, size_t* coefficient_lens, size_t coefficient_lens_len, double * constant, size_t constant_len, double * init_val, size_t init_val_len, long long iterations);
int main(void);

double absf(double x) {
    return (x < 0LL ? -(x) : x);
}

long long strictly_diagonally_dominant(double * * matrix, size_t matrix_len, size_t* matrix_lens, size_t matrix_lens_len) {
    long long n = matrix_len;
    long long i = 0LL;
    while (i < n) {
        double sum = 0LL;
        long long j = 0LL;
        while (j < n) {
            if (i != j) {
                sum = sum + absf(matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? matrix_lens[i] + _mochi_idx : _mochi_idx;})]);
            }
            j = j + 1LL;
        }
        if (absf(matrix[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? matrix_lens[i] + _mochi_idx : _mochi_idx;})]) <= sum) {
            panic("Coefficient matrix is not strictly diagonally dominant");
        }
        i = i + 1LL;
    }
    return 1LL;
}

double * jacobi_iteration_method(double * * coefficient, size_t coefficient_len, size_t* coefficient_lens, size_t coefficient_lens_len, double * constant, size_t constant_len, double * init_val, size_t init_val_len, long long iterations) {
    long long n = coefficient_len;
    if (n == 0LL) {
        panic("Coefficient matrix cannot be empty");
    }
    if (constant_len != n) {
        panic("Constant vector length must equal number of rows in coefficient matrix");
    }
    if (init_val_len != n) {
        panic("Initial values count must match matrix size");
    }
    long long r = 0LL;
    while (r < n) {
        if (coefficient_lens[r] != n) {
            panic("Coefficient matrix must be square");
        }
        r = r + 1LL;
    }
    if (iterations <= 0LL) {
        panic("Iterations must be at least 1");
    }
    strictly_diagonally_dominant(coefficient, coefficient_len, coefficient_lens, coefficient_len);
    double *x = init_val;
    size_t x_len = init_val_len;
    long long k = 0LL;
    while (k < iterations) {
        double *new_x = NULL;
        size_t new_x_len = 0;
        long long i = 0LL;
        while (i < n) {
            double sum = 0LL;
            long long j = 0LL;
            while (j < n) {
                if (i != j) {
                    sum = sum + (coefficient[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? coefficient_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? coefficient_lens[i] + _mochi_idx : _mochi_idx;})] * x[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? x_len + _mochi_idx : _mochi_idx;})]);
                }
                j = j + 1LL;
            }
            double value = (constant[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? constant_len + _mochi_idx : _mochi_idx;})] - sum) / coefficient[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? coefficient_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? coefficient_lens[i] + _mochi_idx : _mochi_idx;})];
            new_x = list_append_double(new_x, &new_x_len, value);
            i = i + 1LL;
        }
        x = new_x;
        x_len = new_x_len;
        k = k + 1LL;
    }
    return jacobi_iteration_method_len = x_len, x;
}

int main(void) {
    {
        long long __start = _now();
        result = jacobi_iteration_method(coefficient, coefficient_len, coefficient_lens, coefficient_len, constant, constant_len, init_val, init_val_len, iterations);
        result_len = jacobi_iteration_method_len;
        puts(str_list_double(result, result_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
