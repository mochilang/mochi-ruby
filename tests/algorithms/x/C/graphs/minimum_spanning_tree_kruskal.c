// Generated by Mochi 0.10.32 on 2025-08-16 10:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_edges_len;
size_t *sort_edges_lens;
size_t kruskal_len;
size_t *kruskal_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long edges1_0[3] = {0LL, 1LL, 3LL};
long long edges1_1[3] = {1LL, 2LL, 5LL};
long long edges1_2[3] = {2LL, 3LL, 1LL};
long long *edges1_init[3] = {edges1_0, edges1_1, edges1_2};
long long **edges1 = edges1_init;
size_t edges1_len = 3;
size_t edges1_lens_init[3] = {3, 3, 3};
size_t *edges1_lens = edges1_lens_init;
size_t edges1_lens_len = 3;
long long edges2_0[3] = {0LL, 1LL, 3LL};
long long edges2_1[3] = {1LL, 2LL, 5LL};
long long edges2_2[3] = {2LL, 3LL, 1LL};
long long edges2_3[3] = {0LL, 2LL, 1LL};
long long edges2_4[3] = {0LL, 3LL, 2LL};
long long *edges2_init[5] = {edges2_0, edges2_1, edges2_2, edges2_3, edges2_4};
long long **edges2 = edges2_init;
size_t edges2_len = 5;
size_t edges2_lens_init[5] = {3, 3, 3, 3, 3};
size_t *edges2_lens = edges2_lens_init;
size_t edges2_lens_len = 5;
long long edges3_0[3] = {0LL, 1LL, 3LL};
long long edges3_1[3] = {1LL, 2LL, 5LL};
long long edges3_2[3] = {2LL, 3LL, 1LL};
long long edges3_3[3] = {0LL, 2LL, 1LL};
long long edges3_4[3] = {0LL, 3LL, 2LL};
long long edges3_5[3] = {2LL, 1LL, 1LL};
long long *edges3_init[6] = {edges3_0, edges3_1, edges3_2, edges3_3, edges3_4, edges3_5};
long long **edges3 = edges3_init;
size_t edges3_len = 6;
size_t edges3_lens_init[6] = {3, 3, 3, 3, 3, 3};
size_t *edges3_lens = edges3_lens_init;
size_t edges3_lens_len = 6;

long long * * sort_edges(long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len);
long long find_parent(long long * parent, size_t parent_len, long long i);
long long * * kruskal(long long num_nodes, long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len);
const char* edges_to_string(long long * * es, size_t es_len, size_t* es_lens, size_t es_lens_len);
int main(void);

long long * * sort_edges(long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len) {
    long long **es = edges;
    size_t es_len = edges_len;
    size_t *es_lens = edges_lens;
    size_t es_lens_len = edges_len;
    long long i = 0LL;
    while (i < es_len) {
        long long j = 0LL;
        while (j < ((es_len - i) - 1LL)) {
            if (es[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? es_lens[j] + _mochi_idx : _mochi_idx;})] > es[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? es_lens[j + 1LL] + _mochi_idx : _mochi_idx;})]) {
                long long *temp = es[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})];
                size_t temp_len = es_lens[j];
                es[(int)(j)] = es[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})];
                es[(int)(j + 1LL)] = temp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_edges_lens = es_lens, sort_edges_len = es_len, es;
}

long long find_parent(long long * parent, size_t parent_len, long long i) {
    if (parent[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})] != i) {
        parent[(int)(i)] = find_parent(parent, parent_len, parent[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})]);
    }
    return parent[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})];
}

long long * * kruskal(long long num_nodes, long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len) {
    long long **es = sort_edges(edges, edges_len, edges_lens, edges_len);
    size_t es_len = sort_edges_len;
    size_t *es_lens = sort_edges_lens;
    size_t es_lens_len = sort_edges_len;
    long long *parent = NULL;
    size_t parent_len = 0;
    long long i = 0LL;
    while (i < num_nodes) {
        parent = list_append_long_long(parent, &parent_len, i);
        i = i + 1LL;
    }
    long long **mst = NULL;
    size_t mst_len = 0;
    size_t *mst_lens = NULL;
    size_t mst_lens_len = 0;
    long long idx = 0LL;
    while (idx < es_len) {
        long long *e = es[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})];
        size_t e_len = es_lens[idx];
        long long pa = find_parent(parent, parent_len, e[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})]);
        long long pb = find_parent(parent, parent_len, e[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})]);
        if (pa != pb) {
            mst = list_append_intptr(mst, &mst_len, e);
            mst_lens = list_append_szt(mst_lens, &mst_lens_len, e_len);
            parent[(int)(pa)] = pb;
        }
        idx = idx + 1LL;
    }
    return kruskal_lens = mst_lens, kruskal_len = mst_len, mst;
}

const char* edges_to_string(long long * * es, size_t es_len, size_t* es_lens, size_t es_lens_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < es_len) {
        long long *e = es[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})];
        size_t e_len = es_lens[i];
        s = str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(s, "("), str_int(e[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})])), ", "), str_int(e[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})])), ", "), str_int(e[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})])), ")");
        if (i < (es_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long** __tmp3 = kruskal(4LL, edges1, edges1_len, edges1_lens, edges1_len); edges_to_string(__tmp3, kruskal_len, kruskal_lens, kruskal_len);}));
        puts(({long long** __tmp4 = kruskal(4LL, edges2, edges2_len, edges2_lens, edges2_len); edges_to_string(__tmp4, kruskal_len, kruskal_lens, kruskal_len);}));
        puts(({long long** __tmp5 = kruskal(4LL, edges3, edges3_len, edges3_lens, edges3_len); edges_to_string(__tmp5, kruskal_len, kruskal_lens, kruskal_len);}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
