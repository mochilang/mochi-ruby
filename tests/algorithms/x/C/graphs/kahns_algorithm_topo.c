// Generated by Mochi 0.10.32 on 2025-08-15 22:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t topological_sort_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static size_t map_len_il(const int keys[], void *const vals[], const size_t lens[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (vals[i] == NULL) return 0; return lens[i]; }
    }
    return 0;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * topological_sort(MapIL graph);
void user_main();
int main(void);

long long * topological_sort(MapIL graph) {
    long long *indegree = NULL;
    size_t indegree_len = 0;
    long long i = 0LL;
    while (i < graph.len) {
        indegree = list_append_long_long(indegree, &indegree_len, 0LL);
        i = i + 1LL;
    }
    long long **graph_vals = (long long**)graph.vals;
    size_t *graph_vals_lens = graph.lens;
    for (size_t __i = 0; __i < graph.len; __i++) {
        long long *edges = graph_vals[__i];
        size_t edges_len = graph_vals_lens[__i];
        long long j = 0LL;
        while (j < edges_len) {
            long long v = edges[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? edges_len + _mochi_idx : _mochi_idx;})];
            indegree[(int)(v)] = indegree[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] + 1LL;
            j = j + 1LL;
        }
    }
    long long *queue = NULL;
    size_t queue_len = 0;
    i = 0LL;
    while (i < indegree_len) {
        if (indegree[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] == 0LL) {
            queue = list_append_long_long(queue, &queue_len, i);
        }
        i = i + 1LL;
    }
    long long *order = NULL;
    size_t order_len = 0;
    long long head = 0LL;
    long long processed = 0LL;
    while (head < queue_len) {
        long long v = queue[(int)({long long _mochi_idx = head; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        head = head + 1LL;
        processed = processed + 1LL;
        order = list_append_long_long(order, &order_len, v);
        long long *neighbors = map_get_il(graph.keys, graph.vals, graph.lens, graph.len, v, NULL);
        size_t neighbors_len = map_len_il(graph.keys, graph.vals, graph.lens, graph.len, v);
        long long k = 0LL;
        while (k < neighbors_len) {
            long long nb = neighbors[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})];
            indegree[(int)(nb)] = indegree[(int)({long long _mochi_idx = nb; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] - 1LL;
            if (indegree[(int)({long long _mochi_idx = nb; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] == 0LL) {
                queue = list_append_long_long(queue, &queue_len, nb);
            }
            k = k + 1LL;
        }
    }
    if (processed != graph.len) {
        topological_sort_len = 0;
        return NULL;
    }
    return topological_sort_len = order_len, order;
}

void user_main() {
    static int graph_keys[22] = {0LL, 1LL, 2LL, 3LL, 4LL, 5LL};
    static long long graph_vals_0[2] = {1LL, 2LL};
    static long long graph_vals_1[1] = {3LL};
    static long long graph_vals_2[1] = {3LL};
    static long long graph_vals_3[2] = {4LL, 5LL};
    static long long graph_vals_4[0] = {};
    static long long graph_vals_5[0] = {};
    static void* graph_vals[22] = {graph_vals_0, graph_vals_1, graph_vals_2, graph_vals_3, graph_vals_4, graph_vals_5};
    static size_t graph_lens[22] = {2, 1, 1, 2, 0, 0};
    size_t graph_len = 6;
    MapIL graph = { graph_keys, graph_vals, graph_lens, 6, 22 };
    long long* __tmp2 = topological_sort(graph);
    puts(str_list_int(__tmp2, topological_sort_len));
    static int cyclic_keys[19] = {0LL, 1LL, 2LL};
    static long long cyclic_vals_0[1] = {1LL};
    static long long cyclic_vals_1[1] = {2LL};
    static long long cyclic_vals_2[1] = {0LL};
    static void* cyclic_vals[19] = {cyclic_vals_0, cyclic_vals_1, cyclic_vals_2};
    static size_t cyclic_lens[19] = {1, 1, 1};
    size_t cyclic_len = 3;
    MapIL cyclic = { cyclic_keys, cyclic_vals, cyclic_lens, 3, 19 };
    long long* __tmp3 = topological_sort(cyclic);
    puts(str_list_int(__tmp3, topological_sort_len));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
