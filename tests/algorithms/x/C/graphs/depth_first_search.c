// Generated by Mochi 0.10.32 on 2025-08-14 20:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t depth_first_search_len;

size_t _slice_str_len;
size_t append_len;
static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { const char **keys; void **vals; size_t *lens; size_t len; size_t cap; } MapSL;

static void* map_get_sl(const char *keys[], void *vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static size_t map_len_sl(const char *keys[], const size_t lens[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return lens[i];
    }
    return 0;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

static const char* G_keys[23] = {"A", "B", "C", "D", "E", "F", "G"};
static const char* G_vals_0[3] = {"B", "C", "D"};
static const char* G_vals_1[3] = {"A", "D", "E"};
static const char* G_vals_2[2] = {"A", "F"};
static const char* G_vals_3[2] = {"B", "D"};
static const char* G_vals_4[2] = {"B", "F"};
static const char* G_vals_5[3] = {"C", "E", "G"};
static const char* G_vals_6[1] = {"F"};
void* G_vals[23] = {G_vals_0, G_vals_1, G_vals_2, G_vals_3, G_vals_4, G_vals_5, G_vals_6};
static size_t G_lens[23] = {3, 3, 2, 2, 2, 3, 1};
size_t G_len = 7;
MapSL G = { G_keys, G_vals, G_lens, 7, 23 };
const char* *result = NULL;
size_t result_len = 0;

long long contains(const char* * lst, size_t lst_len, const char* v);
const char* * depth_first_search(MapSL graph, const char* start);
int main(void);

long long contains(const char* * lst, size_t lst_len, const char* v) {
    long long i = 0LL;
    while (i < lst_len) {
        if (strcmp(lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})], v) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * depth_first_search(MapSL graph, const char* start) {
    const char* *explored = NULL;
    size_t explored_len = 0;
    const char* *stack = NULL;
    size_t stack_len = 0;
    stack = list_append_str(stack, &stack_len, start);
    explored = list_append_str(explored, &explored_len, start);
    while (stack_len > 0LL) {
        long long idx = stack_len - 1LL;
        const char* v = stack[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? stack_len + _mochi_idx : _mochi_idx;})];
        stack = _slice_str(stack, stack_len, 0LL, idx, &(_slice_str_len));
        stack_len = _slice_str_len;
        const char* *neighbors = map_get_sl(graph.keys, graph.vals, graph.lens, graph.len, v, NULL);
        size_t neighbors_len = map_len_sl(graph.keys, graph.lens, graph.len, v);
        long long i = neighbors_len - 1LL;
        while (i >= 0LL) {
            const char* adj = neighbors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})];
            if (!(contains(explored, explored_len, adj))) {
                explored = list_append_str(explored, &explored_len, adj);
                stack = list_append_str(stack, &stack_len, adj);
            }
            i = i - 1LL;
        }
    }
    return depth_first_search_len = explored_len, explored;
}

int main(void) {
    {
        long long __start = _now();
        result = depth_first_search(G, "A");
        result_len = depth_first_search_len;
        puts(str_list_str(result, result_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
