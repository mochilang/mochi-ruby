// Generated by Mochi 0.10.32 on 2025-08-15 19:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_from_list_len;

size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; void **vals; size_t *lens; size_t len; size_t cap; } MapSL;

static void* map_get_sl(const char *keys[], void *vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static size_t map_len_sl(const char *keys[], const size_t lens[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return lens[i];
    }
    return 0;
}

static void map_set_sl(const char *keys[], void *vals[], size_t lens[], size_t *len, const char *key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; lens[i] = val_len; return; }
    }
    keys[*len] = key; vals[*len] = val; lens[*len] = val_len; (*len)++;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Graph Graph;

struct Graph {
    MapSL adj;
    long long directed;
};

Graph create_graph(const char* * vertices, size_t vertices_len, const char* * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len, long long directed);
Graph add_vertex(Graph graph, const char* v);
const char* * remove_from_list(const char* * lst, size_t lst_len, const char* value);
MapSL remove_key(MapSL m, const char* key);
Graph add_edge(Graph graph, const char* s, const char* d);
Graph remove_edge(Graph graph, const char* s, const char* d);
Graph remove_vertex(Graph graph, const char* v);
long long contains_vertex(Graph graph, const char* v);
long long contains_edge(Graph graph, const char* s, const char* d);
Graph clear_graph(Graph graph);
const char* to_string(Graph graph);
void user_main();
int main(void);

Graph create_graph(const char* * vertices, size_t vertices_len, const char* * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len, long long directed) {
    static const char* adj_keys[16] = {};
    static void* adj_vals[16] = {};
    static size_t adj_lens[16] = {};
    size_t adj_len = 0;
    MapSL adj = { adj_keys, adj_vals, adj_lens, 0, 16 };
    for (size_t __i = 0; __i < vertices_len; __i++) {
        const char* v = vertices[__i];
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, v, NULL, 0);
    }
    for (size_t __i = 0; __i < edges_len; __i++) {
        const char* *e = edges[__i];
        size_t e_len = edges_lens[__i];
        const char* s = e[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})];
        const char* d = e[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})];
        if (!(contains_str(adj.keys, adj.len, s))) {
            map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, s, NULL, 0);
        }
        if (!(contains_str(adj.keys, adj.len, d))) {
            map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, d, NULL, 0);
        }
        {
        const char** __tmp0 = map_get_sl(adj.keys, adj.vals, adj.lens, adj.len, s, NULL);
        size_t __tmp1 = map_len_sl(adj.keys, adj.lens, adj.len, s);
        __tmp0 = list_append_str(__tmp0, &__tmp1, d);
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, s, __tmp0, __tmp1);
        }
        if (!(directed)) {
            {
            const char** __tmp2 = map_get_sl(adj.keys, adj.vals, adj.lens, adj.len, d, NULL);
            size_t __tmp3 = map_len_sl(adj.keys, adj.lens, adj.len, d);
            __tmp2 = list_append_str(__tmp2, &__tmp3, s);
            map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, d, __tmp2, __tmp3);
            }
        }
    }
    return (Graph){.adj = adj, .directed = directed};
}

Graph add_vertex(Graph graph, const char* v) {
    if (contains_str(graph.adj.keys, graph.adj.len, v)) {
        panic("vertex exists");
    }
    static const char* adj_keys[16] = {};
    static void* adj_vals[16] = {};
    static size_t adj_lens[16] = {};
    size_t adj_len = 0;
    MapSL adj = { adj_keys, adj_vals, adj_lens, 0, 16 };
    for (size_t __i = 0; __i < graph.adj.len; __i++) {
        const char* k = graph.adj.keys[__i];
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, k, map_get_sl(graph.adj.keys, graph.adj.vals, graph.adj.lens, graph.adj.len, k, NULL), map_len_sl(graph.adj.keys, graph.adj.lens, graph.adj.len, k));
    }
    map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, v, NULL, 0);
    return (Graph){.adj = adj, .directed = graph.directed};
}

const char* * remove_from_list(const char* * lst, size_t lst_len, const char* value) {
    const char* *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < lst_len) {
        if (strcmp(lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})], value) != 0) {
            res = list_append_str(res, &res_len, lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_from_list_len = res_len, res;
}

MapSL remove_key(MapSL m, const char* key) {
    static const char* res_keys[16] = {};
    static void* res_vals[16] = {};
    static size_t res_lens[16] = {};
    size_t res_len = 0;
    MapSL res = { res_keys, res_vals, res_lens, 0, 16 };
    for (size_t __i = 0; __i < m.len; __i++) {
        const char* k = m.keys[__i];
        if (strcmp(k, key) != 0) {
            map_set_sl(res.keys, res.vals, res.lens, &res.len, k, map_get_sl(m.keys, m.vals, m.lens, m.len, k, NULL), map_len_sl(m.keys, m.lens, m.len, k));
        }
    }
    return res;
}

Graph add_edge(Graph graph, const char* s, const char* d) {
    if (!(contains_str(graph.adj.keys, graph.adj.len, s)) || !(contains_str(graph.adj.keys, graph.adj.len, d))) {
        panic("vertex missing");
    }
    if (contains_edge(graph, s, d)) {
        panic("edge exists");
    }
    static const char* adj_keys[16] = {};
    static void* adj_vals[16] = {};
    static size_t adj_lens[16] = {};
    size_t adj_len = 0;
    MapSL adj = { adj_keys, adj_vals, adj_lens, 0, 16 };
    for (size_t __i = 0; __i < graph.adj.len; __i++) {
        const char* k = graph.adj.keys[__i];
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, k, map_get_sl(graph.adj.keys, graph.adj.vals, graph.adj.lens, graph.adj.len, k, NULL), map_len_sl(graph.adj.keys, graph.adj.lens, graph.adj.len, k));
    }
    const char* *list_s = map_get_sl(adj.keys, adj.vals, adj.lens, adj.len, s, NULL);
    size_t list_s_len = map_len_sl(adj.keys, adj.lens, adj.len, s);
    list_s = list_append_str(list_s, &list_s_len, d);
    map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, s, list_s, list_s_len);
    if (!(graph.directed)) {
        const char* *list_d = map_get_sl(adj.keys, adj.vals, adj.lens, adj.len, d, NULL);
        size_t list_d_len = map_len_sl(adj.keys, adj.lens, adj.len, d);
        list_d = list_append_str(list_d, &list_d_len, s);
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, d, list_d, list_d_len);
    }
    return (Graph){.adj = adj, .directed = graph.directed};
}

Graph remove_edge(Graph graph, const char* s, const char* d) {
    if (!(contains_str(graph.adj.keys, graph.adj.len, s)) || !(contains_str(graph.adj.keys, graph.adj.len, d))) {
        panic("vertex missing");
    }
    if (!(contains_edge(graph, s, d))) {
        panic("edge missing");
    }
    static const char* adj_keys[16] = {};
    static void* adj_vals[16] = {};
    static size_t adj_lens[16] = {};
    size_t adj_len = 0;
    MapSL adj = { adj_keys, adj_vals, adj_lens, 0, 16 };
    for (size_t __i = 0; __i < graph.adj.len; __i++) {
        const char* k = graph.adj.keys[__i];
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, k, map_get_sl(graph.adj.keys, graph.adj.vals, graph.adj.lens, graph.adj.len, k, NULL), map_len_sl(graph.adj.keys, graph.adj.lens, graph.adj.len, k));
    }
    map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, s, remove_from_list(map_get_sl(adj.keys, adj.vals, adj.lens, adj.len, s, NULL), map_len_sl(adj.keys, adj.lens, adj.len, s), d), remove_from_list_len);
    if (!(graph.directed)) {
        map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, d, remove_from_list(map_get_sl(adj.keys, adj.vals, adj.lens, adj.len, d, NULL), map_len_sl(adj.keys, adj.lens, adj.len, d), s), remove_from_list_len);
    }
    return (Graph){.adj = adj, .directed = graph.directed};
}

Graph remove_vertex(Graph graph, const char* v) {
    if (!(contains_str(graph.adj.keys, graph.adj.len, v))) {
        panic("vertex missing");
    }
    static const char* adj_keys[16] = {};
    static void* adj_vals[16] = {};
    static size_t adj_lens[16] = {};
    size_t adj_len = 0;
    MapSL adj = { adj_keys, adj_vals, adj_lens, 0, 16 };
    for (size_t __i = 0; __i < graph.adj.len; __i++) {
        const char* k = graph.adj.keys[__i];
        if (strcmp(k, v) != 0) {
            map_set_sl(adj.keys, adj.vals, adj.lens, &adj.len, k, remove_from_list(map_get_sl(graph.adj.keys, graph.adj.vals, graph.adj.lens, graph.adj.len, k, NULL), map_len_sl(graph.adj.keys, graph.adj.lens, graph.adj.len, k), v), remove_from_list_len);
        }
    }
    return (Graph){.adj = adj, .directed = graph.directed};
}

long long contains_vertex(Graph graph, const char* v) {
    return contains_str(graph.adj.keys, graph.adj.len, v);
}

long long contains_edge(Graph graph, const char* s, const char* d) {
    if (!(contains_str(graph.adj.keys, graph.adj.len, s)) || !(contains_str(graph.adj.keys, graph.adj.len, d))) {
        panic("vertex missing");
    }
    {
        size_t __tmp4_len = 0;
        const char** __tmp4 = map_get_sl(graph.adj.keys, graph.adj.vals, graph.adj.lens, graph.adj.len, s, &__tmp4_len);
        for (size_t __i = 0; __i < __tmp4_len; __i++) {
            const char* x = __tmp4[__i];
            if (strcmp(x, d) == 0) {
                return 1LL;
            }
        }
    }
    return 0LL;
}

Graph clear_graph(Graph graph) {
    return (Graph){.adj = (MapSL){0}, .directed = graph.directed};
}

const char* to_string(Graph graph) {
    return "map";
}

void user_main() {
    const char* *vertices = NULL;
    size_t vertices_len = 0;
    vertices = list_append_str(vertices, &vertices_len, "1");
    vertices = list_append_str(vertices, &vertices_len, "2");
    vertices = list_append_str(vertices, &vertices_len, "3");
    vertices = list_append_str(vertices, &vertices_len, "4");
    const char* edges_0[2] = {"1", "2"};
    const char* edges_1[2] = {"2", "3"};
    const char* edges_2[2] = {"3", "4"};
    const char* *edges_init[3] = {edges_0, edges_1, edges_2};
    const char* **edges = edges_init;
    size_t edges_len = 3;
    size_t edges_lens_init[3] = {2, 2, 2};
    size_t *edges_lens = edges_lens_init;
    size_t edges_lens_len = 3;
    Graph g = create_graph(vertices, vertices_len, edges, edges_len, edges_lens, edges_len, 0LL);
    puts(to_string(g));
    g = add_vertex(g, "5");
    g = add_edge(g, "4", "5");
    puts(str_int(contains_edge(g, "4", "5")));
    g = remove_edge(g, "1", "2");
    g = remove_vertex(g, "3");
    puts(to_string(g));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
