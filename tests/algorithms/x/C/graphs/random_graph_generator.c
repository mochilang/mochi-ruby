// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static size_t map_len_il(const int keys[], void *const vals[], const size_t lens[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (vals[i] == NULL) return 0; return lens[i]; }
    }
    return 0;
}

static void map_set_il(int **keys, void ***vals, size_t **lens, size_t *len, size_t *cap, int key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if ((*keys)[i] == key) { (*vals)[i] = val; (*lens)[i] = val_len; return; }
    }
    if (*len >= *cap) {
        *cap = *cap ? *cap * 2 : 16;
        *keys = realloc(*keys, *cap * sizeof(int));
        *vals = realloc(*vals, *cap * sizeof(void*));
        *lens = realloc(*lens, *cap * sizeof(size_t));
    }
    (*keys)[*len] = key; (*vals)[*len] = val; (*lens)[*len] = val_len; (*len)++;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long seed = 1LL;

long long user_rand();
double user_random();
MapIL complete_graph(long long vertices_number);
MapIL random_graph(long long vertices_number, double probability, long long directed);
void user_main();
int main(void);

long long user_rand() {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed;
}

double user_random() {
    return (1LL * user_rand()) / 2147483648LL;
}

MapIL complete_graph(long long vertices_number) {
    static int graph_keys[16] = {};
    static void* graph_vals[16] = {};
    static size_t graph_lens[16] = {};
    size_t graph_len = 0;
    MapIL graph = { graph_keys, graph_vals, graph_lens, 0, 16 };
    long long i = 0LL;
    while (i < vertices_number) {
        long long *neighbors = NULL;
        size_t neighbors_len = 0;
        long long j = 0LL;
        while (j < vertices_number) {
            if (j != i) {
                neighbors = list_append_long_long(neighbors, &neighbors_len, j);
            }
            j = j + 1LL;
        }
        map_set_il(&graph.keys, &graph.vals, &graph.lens, &graph.len, &graph.cap, i, neighbors, neighbors_len);
        i = i + 1LL;
    }
    return graph;
}

MapIL random_graph(long long vertices_number, double probability, long long directed) {
    static int graph_keys[16] = {};
    static void* graph_vals[16] = {};
    static size_t graph_lens[16] = {};
    size_t graph_len = 0;
    MapIL graph = { graph_keys, graph_vals, graph_lens, 0, 16 };
    long long i = 0LL;
    while (i < vertices_number) {
        map_set_il(&graph.keys, &graph.vals, &graph.lens, &graph.len, &graph.cap, i, NULL, 0);
        i = i + 1LL;
    }
    if (probability >= 1LL) {
        return complete_graph(vertices_number);
    }
    if (probability <= 0LL) {
        return graph;
    }
    i = 0LL;
    while (i < vertices_number) {
        long long j = i + 1LL;
        while (j < vertices_number) {
            if (user_random() < probability) {
                map_set_il(&graph.keys, &graph.vals, &graph.lens, &graph.len, &graph.cap, i, list_append_int_new(map_get_il(graph.keys, graph.vals, graph.lens, graph.len, i, NULL), graph.lens[(int)(i)], j), map_len_il(graph.keys, graph.vals, graph.lens, graph.len, i) + 1);
                if (!(directed)) {
                    map_set_il(&graph.keys, &graph.vals, &graph.lens, &graph.len, &graph.cap, j, list_append_int_new(map_get_il(graph.keys, graph.vals, graph.lens, graph.len, j, NULL), graph.lens[(int)(j)], i), map_len_il(graph.keys, graph.vals, graph.lens, graph.len, j) + 1);
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return graph;
}

void user_main() {
    seed = 1LL;
    MapIL g1 = random_graph(4LL, 0.5, 0LL);
    printf("%d\n", g1);
    seed = 1LL;
    MapIL g2 = random_graph(4LL, 0.5, 1LL);
    printf("%d\n", g2);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
