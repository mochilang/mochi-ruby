// Generated by Mochi 0.10.32 on 2025-08-14 09:53 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **g1 = NULL;
size_t g1_len = 0;
size_t *g1_lens = NULL;
size_t g1_lens_len = 0;
long long **g2 = NULL;
size_t g2_len = 0;
size_t *g2_lens = NULL;
size_t g2_lens_len = 0;

long long depth_first_search(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long vertex, long long * visited, size_t visited_len, long long * rec_stk, size_t rec_stk_len);
long long check_cycle(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len);
void print_bool(long long b);
int main(void);

long long depth_first_search(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long vertex, long long * visited, size_t visited_len, long long * rec_stk, size_t rec_stk_len) {
    visited[(int)(vertex)] = 1LL;
    rec_stk[(int)(vertex)] = 1LL;
    {
        long long* __tmp0 = graph[(int)({long long _mochi_idx = vertex; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
        size_t __tmp0_len = graph_lens[vertex];
        for (size_t __i = 0; __i < __tmp0_len; __i++) {
            long long node = __tmp0[__i];
            if (!(visited[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
                if (depth_first_search(graph, graph_len, graph_lens, graph_len, node, visited, visited_len, rec_stk, rec_stk_len)) {
                    return 1LL;
                }
            } else {
                if (rec_stk[(int)({long long _mochi_idx = node; _mochi_idx < 0 ? rec_stk_len + _mochi_idx : _mochi_idx;})]) {
                    return 1LL;
                }
            }
        }
    }
    rec_stk[(int)(vertex)] = 0LL;
    return 0LL;
}

long long check_cycle(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len) {
    long long n = graph_len;
    long long *visited = NULL;
    size_t visited_len = 0;
    long long *rec_stk = NULL;
    size_t rec_stk_len = 0;
    long long i = 0LL;
    while (i < n) {
        visited = list_append_long_long(visited, &visited_len, 0LL);
        rec_stk = list_append_long_long(rec_stk, &rec_stk_len, 0LL);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        if (!(visited[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
            if (depth_first_search(graph, graph_len, graph_lens, graph_len, i, visited, visited_len, rec_stk, rec_stk_len)) {
                return 1LL;
            }
        }
        i = i + 1LL;
    }
    return 0LL;
}

void print_bool(long long b) {
    if (b) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        g1 = ({long long** tmp = malloc(6 * sizeof(long long*)); tmp[0] = NULL; tmp[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 3LL; tmp;}); tmp[2] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 4LL; tmp;}); tmp[3] = ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 5LL; tmp;}); tmp[4] = ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 5LL; tmp;}); tmp[5] = NULL; tmp;});
        g1_len = 6;
        g1_lens = ({size_t *tmp = malloc(6 * sizeof(size_t)); tmp[0] = 0; tmp[1] = 2; tmp[2] = 2; tmp[3] = 1; tmp[4] = 1; tmp[5] = 0; tmp;});
        g1_lens_len = 6;
        print_bool(check_cycle(g1, g1_len, g1_lens, g1_len));
        g2 = ({long long** tmp = malloc(4 * sizeof(long long*)); tmp[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 2LL; tmp;}); tmp[1] = ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 2LL; tmp;}); tmp[2] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 3LL; tmp;}); tmp[3] = ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = 3LL; tmp;}); tmp;});
        g2_len = 4;
        g2_lens = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 1; tmp[2] = 2; tmp[3] = 1; tmp;});
        g2_lens_len = 4;
        print_bool(check_cycle(g2, g2_len, g2_lens, g2_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
