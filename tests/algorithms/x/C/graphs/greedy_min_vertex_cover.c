// Generated by Mochi 0.10.32 on 2025-08-15 22:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_value_len;
size_t greedy_min_vertex_cover_len;

size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static size_t map_len_il(const int keys[], void *const vals[], const size_t lens[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (vals[i] == NULL) return 0; return lens[i]; }
    }
    return 0;
}

static void map_set_il(int **keys, void ***vals, size_t **lens, size_t *len, size_t *cap, int key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if ((*keys)[i] == key) { (*vals)[i] = val; (*lens)[i] = val_len; return; }
    }
    if (*len >= *cap) {
        *cap = *cap ? *cap * 2 : 16;
        *keys = realloc(*keys, *cap * sizeof(int));
        *vals = realloc(*vals, *cap * sizeof(void*));
        *lens = realloc(*lens, *cap * sizeof(size_t));
    }
    (*keys)[*len] = key; (*vals)[*len] = val; (*lens)[*len] = val_len; (*len)++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int graph_keys[21] = {0LL, 1LL, 2LL, 3LL, 4LL};
static long long graph_vals_0[2] = {1LL, 3LL};
static long long graph_vals_1[2] = {0LL, 3LL};
static long long graph_vals_2[3] = {0LL, 3LL, 4LL};
static long long graph_vals_3[3] = {0LL, 1LL, 2LL};
static long long graph_vals_4[2] = {2LL, 3LL};
static void* graph_vals[21] = {graph_vals_0, graph_vals_1, graph_vals_2, graph_vals_3, graph_vals_4};
static size_t graph_lens[21] = {2, 2, 3, 3, 2};
size_t graph_len = 5;
MapIL graph = { graph_keys, graph_vals, graph_lens, 5, 21 };

long long * remove_value(long long * lst, size_t lst_len, long long val);
long long * greedy_min_vertex_cover(MapIL graph);
int main(void);

long long * remove_value(long long * lst, size_t lst_len, long long val) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < lst_len) {
        if (lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})] != val) {
            res = list_append_long_long(res, &res_len, lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_value_len = res_len, res;
}

long long * greedy_min_vertex_cover(MapIL graph) {
    MapIL g = graph;
    long long *cover = NULL;
    size_t cover_len = 0;
    while (1LL) {
        long long max_v = 0LL;
        long long max_deg = 0LL;
        for (size_t __i = 0; __i < g.len; __i++) {
            int v = g.keys[__i];
            long long key = (int)(v);
            long long deg = map_len_il(g.keys, g.vals, g.lens, g.len, key);
            if (deg > max_deg) {
                max_deg = deg;
                max_v = key;
            }
        }
        if (max_deg == 0LL) {
            break;
        }
        cover = list_append_long_long(cover, &cover_len, max_v);
        long long *neighbors = map_get_il(g.keys, g.vals, g.lens, g.len, max_v, NULL);
        size_t neighbors_len = map_len_il(g.keys, g.vals, g.lens, g.len, max_v);
        long long i = 0LL;
        while (i < neighbors_len) {
            long long n = neighbors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})];
            map_set_il(&g.keys, &g.vals, &g.lens, &g.len, &g.cap, n, remove_value(map_get_il(g.keys, g.vals, g.lens, g.len, n, NULL), map_len_il(g.keys, g.vals, g.lens, g.len, n), max_v), remove_value_len);
            i = i + 1LL;
        }
        map_set_il(&g.keys, &g.vals, &g.lens, &g.len, &g.cap, max_v, NULL, 0);
    }
    return greedy_min_vertex_cover_len = cover_len, cover;
}

int main(void) {
    {
        long long __start = _now();
        long long* __tmp1 = greedy_min_vertex_cover(graph);
        puts(str_list_int(__tmp1, greedy_min_vertex_cover_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
