// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t articulation_points_len;

size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long dfs_skip(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long * visited, size_t visited_len, long long skip, long long at);
long long * articulation_points(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len);
void user_main();
int main(void);

long long dfs_skip(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long * visited, size_t visited_len, long long skip, long long at) {
    visited[(int)(at)] = 1LL;
    long long count_ = 1LL;
    {
        long long* __tmp0 = graph[(int)({long long _mochi_idx = at; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
        size_t __tmp0_len = graph_lens[(int)(at)];
        for (size_t __i = 0; __i < __tmp0_len; __i++) {
            long long to = __tmp0[__i];
            if (to == skip) {
                continue;
            }
            if (visited[(int)({long long _mochi_idx = to; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})] == 0LL) {
                count_ = count_ + dfs_skip(graph, graph_len, graph_lens, graph_len, visited, visited_len, skip, to);
            }
        }
    }
    return count_;
}

long long * articulation_points(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len) {
    long long n = graph_len;
    long long *result = NULL;
    size_t result_len = 0;
    long long v = 0LL;
    while (v < n) {
        long long *visited = NULL;
        size_t visited_len = 0;
        long long i = 0LL;
        while (i < n) {
            visited = list_append_long_long(visited, &visited_len, 0LL);
            i = i + 1LL;
        }
        long long start = 0LL;
        while ((start == v) && (start < n)) {
            start = start + 1LL;
        }
        long long reach = dfs_skip(graph, graph_len, graph_lens, graph_len, visited, visited_len, v, start);
        if (reach < (n - 1LL)) {
            result = list_append_long_long(result, &result_len, v);
            printf("%d\n", v);
        }
        v = v + 1LL;
    }
    return articulation_points_len = result_len, result;
}

void user_main() {
    long long graph_0[2] = {1LL, 2LL};
    long long graph_1[2] = {0LL, 2LL};
    long long graph_2[4] = {0LL, 1LL, 3LL, 5LL};
    long long graph_3[2] = {2LL, 4LL};
    long long graph_4[1] = {3LL};
    long long graph_5[3] = {2LL, 6LL, 8LL};
    long long graph_6[2] = {5LL, 7LL};
    long long graph_7[2] = {6LL, 8LL};
    long long graph_8[2] = {5LL, 7LL};
    long long *graph_init[9] = {graph_0, graph_1, graph_2, graph_3, graph_4, graph_5, graph_6, graph_7, graph_8};
    long long **graph = graph_init;
    size_t graph_len = 9;
    size_t graph_lens_init[9] = {2, 2, 4, 2, 1, 3, 2, 2, 2};
    size_t *graph_lens = graph_lens_init;
    size_t graph_lens_len = 9;
    articulation_points(graph, graph_len, graph_lens, graph_len);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
