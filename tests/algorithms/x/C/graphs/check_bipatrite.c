// Generated by Mochi 0.10.32 on 2025-08-14 17:45 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t _slice_int_len;
size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc(m->keys, m->cap * sizeof(int));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int graph_keys[20] = {0LL, 1LL, 2LL, 3LL};
static long long graph_vals_0[2] = {1LL, 3LL};
static long long graph_vals_1[2] = {0LL, 2LL};
static long long graph_vals_2[2] = {1LL, 3LL};
static long long graph_vals_3[2] = {0LL, 2LL};
void* graph_vals[20] = {graph_vals_0, graph_vals_1, graph_vals_2, graph_vals_3};
static size_t graph_lens[20] = {2, 2, 2, 2};
size_t graph_len = 4;
MapIL graph = { graph_keys, graph_vals, graph_lens, 4, 20 };

long long is_bipartite_bfs(MapIL graph);
int main(void);

long long is_bipartite_bfs(MapIL graph) {
    static int visited_keys[16] = {};
    static int visited_vals[16] = {};
    size_t visited_len = 0;
    MapII visited = { visited_keys, visited_vals, 0, 16 };
    for (size_t __i = 0; __i < graph.len; __i++) {
        int node = graph.keys[__i];
        if (!(contains_map_int(visited.keys, visited.len, node))) {
            long long *queue = NULL;
            size_t queue_len = 0;
            queue = list_append_long_long(queue, &queue_len, node);
            map_set_ii(&visited, node, 0LL);
            while (queue_len > 0LL) {
                long long curr = queue[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
                queue = _slice_int(queue, queue_len, 1LL, queue_len, &(_slice_int_len));
                queue_len = _slice_int_len;
                {
                    long long* __tmp0 = map_get_il(graph.keys, graph.vals, graph.lens, graph.len, curr, NULL);
                    size_t __tmp0_len = graph.lens[curr];
                    for (size_t __i = 0; __i < __tmp0_len; __i++) {
                        long long neighbor = __tmp0[__i];
                        if (!(contains_map_int(visited.keys, visited.len, neighbor))) {
                            map_set_ii(&visited, neighbor, 1LL - map_get_ii(visited.keys, visited.vals, visited.len, curr));
                            queue = list_append_long_long(queue, &queue_len, neighbor);
                        } else {
                            if (map_get_ii(visited.keys, visited.vals, visited.len, neighbor) == map_get_ii(visited.keys, visited.vals, visited.len, curr)) {
                                return 0LL;
                            }
                        }
                    }
                }
            }
        }
    }
    return 1LL;
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(is_bipartite_bfs(graph)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
