// Generated by Mochi 0.10.32 on 2025-08-14 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; void **vals; size_t *lens; size_t len; size_t cap; } MapSL;

typedef struct { const char **keys; const char **vals; size_t len; size_t cap; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

static void map_set_ss(MapSS *m, const char *key, const char *val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc((void*)m->vals, m->cap * sizeof(char*));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static void* map_get_sl(const char *keys[], void *vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Graph Graph;

struct Graph {
    MapSL graph;
    MapSS parent;
    const char* source;
};

static const char* graph_keys[23] = {"A", "B", "C", "D", "E", "F", "G"};
static const char* graph_vals_0[3] = {"B", "C", "E"};
static const char* graph_vals_1[3] = {"A", "D", "E"};
static const char* graph_vals_2[3] = {"A", "F", "G"};
static const char* graph_vals_3[1] = {"B"};
static const char* graph_vals_4[3] = {"A", "B", "D"};
static const char* graph_vals_5[1] = {"C"};
static const char* graph_vals_6[1] = {"C"};
static void* graph_vals[23] = {graph_vals_0, graph_vals_1, graph_vals_2, graph_vals_3, graph_vals_4, graph_vals_5, graph_vals_6};
static size_t graph_lens[23] = {3, 3, 3, 1, 3, 1, 1};
size_t graph_len = 7;
MapSL graph = { graph_keys, graph_vals, graph_lens, 7, 23 };
Graph g;

Graph newGraph(MapSL g, const char* s);
Graph breath_first_search(Graph g);
const char* shortest_path(Graph g, const char* target);
int main(void);

Graph newGraph(MapSL g, const char* s) {
    return (Graph){.graph = g, .parent = (MapSS){0}, .source = s};
}

Graph breath_first_search(Graph g) {
    MapSS parent = g.parent;
    map_set_ss(&parent, g.source, g.source);
    const char* *queue = NULL;
    size_t queue_len = 0;
    long long idx = 0LL;
    while (idx < queue_len) {
        const char* vertex = queue[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        {
            size_t __tmp0_len = 0;
            const char** __tmp0 = map_get_sl(g.graph.keys, g.graph.vals, g.graph.lens, g.graph.len, vertex, &__tmp0_len);
            for (size_t __i = 0; __i < __tmp0_len; __i++) {
                const char* adj = __tmp0[__i];
                if (!(contains_str(parent.keys, parent.len, adj))) {
                    map_set_ss(&parent, adj, vertex);
                    queue = list_append_str(queue, &queue_len, adj);
                }
            }
        }
        idx = idx + 1LL;
    }
    g.parent = parent;
    return g;
}

const char* shortest_path(Graph g, const char* target) {
    if (strcmp(target, g.source) == 0) {
        return g.source;
    }
    if (!(contains_str(g.parent.keys, g.parent.len, target))) {
        return str_concat(str_concat(str_concat("No path from vertex: ", g.source), " to vertex: "), target);
    }
    const char* p = map_get_ss(g.parent.keys, g.parent.vals, g.parent.len, target);
    return str_concat(str_concat(shortest_path(g, p), "->"), target);
}

int main(void) {
    {
        long long __start = _now();
        g = newGraph(graph, "G");
        g = breath_first_search(g);
        puts(shortest_path(g, "D"));
        puts(shortest_path(g, "G"));
        puts(shortest_path(g, "Foo"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
