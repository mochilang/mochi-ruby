// Generated by Mochi 0.10.32 on 2025-08-16 10:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t prim_mst_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Edge Edge;
typedef struct Pair Pair;

struct Edge {
    long long to;
    long long weight;
};

struct Pair {
    long long u;
    long long v;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Pair* list_append_Pair(Pair *arr, size_t *len, Pair val) {
    arr = realloc(arr, (*len + 1) * sizeof(Pair));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long INF = 1000000000LL;
Edge adjacency_list_0[2] = {(Edge){.to = 1LL, .weight = 1LL}, (Edge){.to = 3LL, .weight = 3LL}};
Edge adjacency_list_1[4] = {(Edge){.to = 0LL, .weight = 1LL}, (Edge){.to = 2LL, .weight = 6LL}, (Edge){.to = 3LL, .weight = 5LL}, (Edge){.to = 4LL, .weight = 1LL}};
Edge adjacency_list_2[3] = {(Edge){.to = 1LL, .weight = 6LL}, (Edge){.to = 4LL, .weight = 5LL}, (Edge){.to = 5LL, .weight = 2LL}};
Edge adjacency_list_3[3] = {(Edge){.to = 0LL, .weight = 3LL}, (Edge){.to = 1LL, .weight = 5LL}, (Edge){.to = 4LL, .weight = 1LL}};
Edge adjacency_list_4[4] = {(Edge){.to = 1LL, .weight = 1LL}, (Edge){.to = 2LL, .weight = 5LL}, (Edge){.to = 3LL, .weight = 1LL}, (Edge){.to = 5LL, .weight = 4LL}};
Edge adjacency_list_5[2] = {(Edge){.to = 2LL, .weight = 2LL}, (Edge){.to = 4LL, .weight = 4LL}};
Edge *adjacency_list_init[6] = {adjacency_list_0, adjacency_list_1, adjacency_list_2, adjacency_list_3, adjacency_list_4, adjacency_list_5};
Edge **adjacency_list = adjacency_list_init;
size_t adjacency_list_len = 6;
size_t adjacency_list_lens_init[6] = {2, 4, 3, 3, 4, 2};
size_t *adjacency_list_lens = adjacency_list_lens_init;
size_t adjacency_list_lens_len = 6;
Pair *mst_edges = NULL;
size_t mst_edges_len = 0;

const char* pairs_to_string(Pair * edges, size_t edges_len);
Pair * prim_mst(Edge * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len);
int main(void);

const char* pairs_to_string(Pair * edges, size_t edges_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < edges_len) {
        Pair e = edges[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? edges_len + _mochi_idx : _mochi_idx;})];
        s = str_concat(str_concat(str_concat(str_concat(str_concat(s, "("), str_int(e.u)), ", "), str_int(e.v)), ")");
        if (i < (edges_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    return str_concat(s, "]");
}

Pair * prim_mst(Edge * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len) {
    long long n = graph_len;
    long long *visited = NULL;
    size_t visited_len = 0;
    long long *dist = NULL;
    size_t dist_len = 0;
    long long *parent = NULL;
    size_t parent_len = 0;
    long long i = 0LL;
    while (i < n) {
        visited = list_append_long_long(visited, &visited_len, 0LL);
        dist = list_append_long_long(dist, &dist_len, INF);
        parent = list_append_long_long(parent, &parent_len, -1LL);
        i = i + 1LL;
    }
    dist[(int)(0LL)] = 0LL;
    Pair *result = NULL;
    size_t result_len = 0;
    long long count = 0LL;
    while (count < n) {
        long long min_val = INF;
        long long u = 0LL;
        long long v = 0LL;
        while (v < n) {
            if ((visited[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})] == 0LL) && (dist[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})] < min_val)) {
                min_val = dist[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})];
                u = v;
            }
            v = v + 1LL;
        }
        if (min_val == INF) {
            break;
        }
        visited[(int)(u)] = 1LL;
        if (u != 0LL) {
            result = list_append_Pair(result, &result_len, (Pair){.u = parent[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})], .v = u});
        }
        {
            Edge* __tmp0 = graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
            size_t __tmp0_len = graph_lens[u];
            for (size_t __i = 0; __i < __tmp0_len; __i++) {
                Edge e = __tmp0[__i];
                if ((visited[(int)({long long _mochi_idx = e.to; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})] == 0LL) && (e.weight < dist[(int)({long long _mochi_idx = e.to; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})])) {
                    dist[(int)(e.to)] = e.weight;
                    parent[(int)(e.to)] = u;
                }
            }
        }
        count = count + 1LL;
    }
    return prim_mst_len = result_len, result;
}

int main(void) {
    {
        long long __start = _now();
        mst_edges = prim_mst(adjacency_list, adjacency_list_len, adjacency_list_lens, adjacency_list_len);
        mst_edges_len = prim_mst_len;
        puts(pairs_to_string(mst_edges, mst_edges_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
