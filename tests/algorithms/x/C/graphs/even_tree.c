// Generated by Mochi 0.10.32 on 2025-08-15 06:45 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t dfs_len;

size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

typedef struct { int *keys; int *vals; size_t len; size_t cap; } MapII;

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(MapII *m, int key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (m->keys[i] == key) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc(m->keys, m->cap * sizeof(int));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static void map_set_il(int **keys, void ***vals, size_t **lens, size_t *len, size_t *cap, int key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if ((*keys)[i] == key) { (*vals)[i] = val; (*lens)[i] = val_len; return; }
    }
    if (*len >= *cap) {
        *cap = *cap ? *cap * 2 : 16;
        *keys = realloc(*keys, *cap * sizeof(int));
        *vals = realloc(*vals, *cap * sizeof(void*));
        *lens = realloc(*lens, *cap * sizeof(size_t));
    }
    (*keys)[*len] = key; (*vals)[*len] = val; (*lens)[*len] = val_len; (*len)++;
}

static long long* list_append_int_new(const long long *arr, size_t len, long long val) {
    long long *res = malloc((len + 1) * sizeof(long long));
    if (arr && len) memcpy(res, arr, len * sizeof(long long));
    res[len] = val;
    append_len = len + 1;
    return res;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int tree_keys[16] = {};
void* tree_vals[16] = {};
static size_t tree_lens[16] = {};
size_t tree_len = 0;
MapIL tree = { tree_keys, tree_vals, tree_lens, 0, 16 };

long long * dfs(long long start, MapII visited);
long long even_tree();
void user_main();
int main(void);

long long * dfs(long long start, MapII visited) {
    long long size = 1LL;
    long long cuts = 0LL;
    map_set_ii(&visited, start, 1LL);
    {
        size_t __tmp0_len = 0;
        long long* __tmp0 = map_get_il(tree.keys, tree.vals, tree.lens, tree.len, start, &__tmp0_len);
        for (size_t __i = 0; __i < __tmp0_len; __i++) {
            long long v = __tmp0[__i];
            if (!(contains_map_int(visited.keys, visited.len, v))) {
                long long *res = dfs(v, visited);
                size_t res_len = dfs_len;
                size = size + res[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
                cuts = cuts + res[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})];
            }
        }
    }
    if ((size % 2LL) == 0LL) {
        cuts = cuts + 1LL;
    }
    {
        long long* __tmp1_data = malloc(2 * sizeof(long long));
        __tmp1_data[0] = size;
        __tmp1_data[1] = cuts;
        dfs_len = 2;
        return __tmp1_data;
    }
}

long long even_tree() {
    static int visited_keys[16] = {};
    static int visited_vals[16] = {};
    size_t visited_len = 0;
    MapII visited = { visited_keys, visited_vals, 0, 16 };
    long long *res = dfs(1LL, visited);
    size_t res_len = dfs_len;
    return res[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? res_len + _mochi_idx : _mochi_idx;})] - 1LL;
}

void user_main() {
    long long edges_0[2] = {2LL, 1LL};
    long long edges_1[2] = {3LL, 1LL};
    long long edges_2[2] = {4LL, 3LL};
    long long edges_3[2] = {5LL, 2LL};
    long long edges_4[2] = {6LL, 1LL};
    long long edges_5[2] = {7LL, 2LL};
    long long edges_6[2] = {8LL, 6LL};
    long long edges_7[2] = {9LL, 8LL};
    long long edges_8[2] = {10LL, 8LL};
    long long *edges_init[9] = {edges_0, edges_1, edges_2, edges_3, edges_4, edges_5, edges_6, edges_7, edges_8};
    long long **edges = edges_init;
    size_t edges_len = 9;
    size_t edges_lens_init[9] = {2, 2, 2, 2, 2, 2, 2, 2, 2};
    size_t *edges_lens = edges_lens_init;
    size_t edges_lens_len = 9;
    long long i = 0LL;
    while (i < edges_len) {
        long long u = edges[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? edges_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? edges_lens[i] + _mochi_idx : _mochi_idx;})];
        long long v = edges[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? edges_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? edges_lens[i] + _mochi_idx : _mochi_idx;})];
        if (!(contains_map_int(tree.keys, tree.len, u))) {
            map_set_il(&tree.keys, &tree.vals, &tree.lens, &tree.len, &tree.cap, u, NULL, 0);
        }
        if (!(contains_map_int(tree.keys, tree.len, v))) {
            map_set_il(&tree.keys, &tree.vals, &tree.lens, &tree.len, &tree.cap, v, NULL, 0);
        }
        map_set_il(&tree.keys, &tree.vals, &tree.lens, &tree.len, &tree.cap, u, list_append_int_new(map_get_il(tree.keys, tree.vals, tree.lens, tree.len, u, NULL), tree.lens[(int)(u)], v), tree.lens[u] + 1);
        map_set_il(&tree.keys, &tree.vals, &tree.lens, &tree.len, &tree.cap, v, list_append_int_new(map_get_il(tree.keys, tree.vals, tree.lens, tree.len, v, NULL), tree.lens[(int)(v)], u), tree.lens[v] + 1);
        i = i + 1LL;
    }
    puts(str_int(even_tree()));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
