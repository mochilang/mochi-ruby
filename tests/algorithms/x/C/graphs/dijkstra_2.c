// Generated by Mochi 0.10.32 on 2025-08-14 09:53 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t dijkstra_len;

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

double INF = 1000000000.0;

void print_dist(double * dist, size_t dist_len);
long long min_dist(double * mdist, size_t mdist_len, long long * vset, size_t vset_len);
double * dijkstra(double * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long src);
void user_main();
int main(void);

void print_dist(double * dist, size_t dist_len) {
    puts("Vertex Distance");
    long long i = 0LL;
    while (i < dist_len) {
        if (dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})] >= INF) {
            printf("%d %s\n", i, "\tINF");
        } else {
            printf("%d %s %g\n", i, "\t", (int)(dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]));
        }
        i = i + 1LL;
    }
}

long long min_dist(double * mdist, size_t mdist_len, long long * vset, size_t vset_len) {
    double min_val = INF;
    double min_ind = -1.0;
    long long i = 0LL;
    while (i < mdist_len) {
        if (!(vset[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vset_len + _mochi_idx : _mochi_idx;})]) && (mdist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mdist_len + _mochi_idx : _mochi_idx;})] < min_val)) {
            min_val = mdist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mdist_len + _mochi_idx : _mochi_idx;})];
            min_ind = i;
        }
        i = i + 1LL;
    }
    return min_ind;
}

double * dijkstra(double * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long src) {
    long long v = graph_len;
    double *mdist = NULL;
    size_t mdist_len = 0;
    long long *vset = NULL;
    size_t vset_len = 0;
    long long i = 0LL;
    while (i < v) {
        mdist = list_append_double(mdist, &mdist_len, INF);
        vset = list_append_long_long(vset, &vset_len, 0LL);
        i = i + 1LL;
    }
    mdist[(int)(src)] = 0.0;
    long long count = 0LL;
    while (count < (v - 1LL)) {
        long long u = min_dist(mdist, mdist_len, vset, vset_len);
        vset[(int)(u)] = 1LL;
        long long i = 0LL;
        while (i < v) {
            double alt = mdist[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? mdist_len + _mochi_idx : _mochi_idx;})] + graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? graph_lens[u] + _mochi_idx : _mochi_idx;})];
            if ((!(vset[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? vset_len + _mochi_idx : _mochi_idx;})]) && (graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? graph_lens[u] + _mochi_idx : _mochi_idx;})] < INF)) && (alt < mdist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? mdist_len + _mochi_idx : _mochi_idx;})])) {
                mdist[(int)(i)] = alt;
            }
            i = i + 1LL;
        }
        count = count + 1LL;
    }
    return dijkstra_len = mdist_len, mdist;
}

void user_main() {
    double graph_0[5] = {0.0, 10.0, INF, INF, 5.0};
    double graph_1[5] = {INF, 0.0, 1.0, INF, 2.0};
    double graph_2[5] = {INF, INF, 0.0, 4.0, INF};
    double graph_3[5] = {INF, INF, 6.0, 0.0, INF};
    double graph_4[5] = {INF, 3.0, 9.0, 2.0, 0.0};
    double *graph_init[5] = {graph_0, graph_1, graph_2, graph_3, graph_4};
    double **graph = graph_init;
    size_t graph_len = 5;
    size_t graph_lens_init[5] = {5, 5, 5, 5, 5};
    size_t *graph_lens = graph_lens_init;
    size_t graph_lens_len = 5;
    double *dist = dijkstra(graph, graph_len, graph_lens, graph_len, 0LL);
    size_t dist_len = dijkstra_len;
    print_dist(dist, dist_len);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
