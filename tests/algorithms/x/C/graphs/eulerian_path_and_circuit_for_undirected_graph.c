// Generated by Mochi 0.10.32 on 2025-08-15 06:45 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t make_matrix_len;
size_t *make_matrix_lens;
size_t dfs_len;

size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct CheckResult CheckResult;

struct CheckResult {
    long long status;
    long long odd_node;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int g1_keys[21] = {1LL, 2LL, 3LL, 4LL, 5LL};
static long long g1_vals_0[3] = {2LL, 3LL, 4LL};
static long long g1_vals_1[2] = {1LL, 3LL};
static long long g1_vals_2[2] = {1LL, 2LL};
static long long g1_vals_3[2] = {1LL, 5LL};
static long long g1_vals_4[1] = {4LL};
void* g1_vals[21] = {g1_vals_0, g1_vals_1, g1_vals_2, g1_vals_3, g1_vals_4};
static size_t g1_lens[21] = {3, 2, 2, 2, 1};
size_t g1_len = 5;
MapIL g1 = { g1_keys, g1_vals, g1_lens, 5, 21 };
static int g2_keys[21] = {1LL, 2LL, 3LL, 4LL, 5LL};
static long long g2_vals_0[4] = {2LL, 3LL, 4LL, 5LL};
static long long g2_vals_1[2] = {1LL, 3LL};
static long long g2_vals_2[2] = {1LL, 2LL};
static long long g2_vals_3[2] = {1LL, 5LL};
static long long g2_vals_4[2] = {1LL, 4LL};
void* g2_vals[21] = {g2_vals_0, g2_vals_1, g2_vals_2, g2_vals_3, g2_vals_4};
static size_t g2_lens[21] = {4, 2, 2, 2, 2};
size_t g2_len = 5;
MapIL g2 = { g2_keys, g2_vals, g2_lens, 5, 21 };
static int g3_keys[21] = {1LL, 2LL, 3LL, 4LL, 5LL};
static long long g3_vals_0[3] = {2LL, 3LL, 4LL};
static long long g3_vals_1[3] = {1LL, 3LL, 4LL};
static long long g3_vals_2[2] = {1LL, 2LL};
static long long g3_vals_3[3] = {1LL, 2LL, 5LL};
static long long g3_vals_4[1] = {4LL};
void* g3_vals[21] = {g3_vals_0, g3_vals_1, g3_vals_2, g3_vals_3, g3_vals_4};
static size_t g3_lens[21] = {3, 3, 2, 3, 1};
size_t g3_len = 5;
MapIL g3 = { g3_keys, g3_vals, g3_lens, 5, 21 };
static int g4_keys[19] = {1LL, 2LL, 3LL};
static long long g4_vals_0[2] = {2LL, 3LL};
static long long g4_vals_1[2] = {1LL, 3LL};
static long long g4_vals_2[2] = {1LL, 2LL};
void* g4_vals[19] = {g4_vals_0, g4_vals_1, g4_vals_2};
static size_t g4_lens[19] = {2, 2, 2};
size_t g4_len = 3;
MapIL g4 = { g4_keys, g4_vals, g4_lens, 3, 19 };
static int g5_keys[18] = {1LL, 2LL};
static long long g5_vals_0[0] = {};
static long long g5_vals_1[0] = {};
void* g5_vals[18] = {g5_vals_0, g5_vals_1};
static size_t g5_lens[18] = {0, 0};
size_t g5_len = 2;
MapIL g5 = { g5_keys, g5_vals, g5_lens, 2, 18 };
long long max_node = 10LL;

long long * * make_matrix(long long n);
long long * dfs(long long u, MapIL graph, long long * * visited_edge, size_t visited_edge_len, size_t* visited_edge_lens, size_t visited_edge_lens_len, long long * path, size_t path_len);
CheckResult check_circuit_or_path(MapIL graph, long long max_node);
void check_euler(MapIL graph, long long max_node);
int main(void);

long long * * make_matrix(long long n) {
    long long **matrix = NULL;
    size_t matrix_len = 0;
    size_t *matrix_lens = NULL;
    size_t matrix_lens_len = 0;
    long long i = 0LL;
    while (i <= n) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j <= n) {
            row = list_append_long_long(row, &row_len, 0LL);
            j = j + 1LL;
        }
        matrix = list_append_intptr(matrix, &matrix_len, row);
        matrix_lens = list_append_szt(matrix_lens, &matrix_lens_len, row_len);
        i = i + 1LL;
    }
    return make_matrix_lens = matrix_lens, make_matrix_len = matrix_len, matrix;
}

long long * dfs(long long u, MapIL graph, long long * * visited_edge, size_t visited_edge_len, size_t* visited_edge_lens, size_t visited_edge_lens_len, long long * path, size_t path_len) {
    path = list_append_long_long(path, &path_len, u);
    if (contains_map_int(graph.keys, graph.len, u)) {
        long long *neighbors = map_get_il(graph.keys, graph.vals, graph.lens, graph.len, u, NULL);
        size_t neighbors_len = graph.lens[u];
        long long i = 0LL;
        while (i < neighbors_len) {
            long long v = neighbors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})];
            if (visited_edge[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? visited_edge_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = v; _mochi_idx < 0 ? visited_edge_lens[u] + _mochi_idx : _mochi_idx;})] == 0LL) {
                visited_edge[(int)(u)][(int)(v)] = 1LL;
                visited_edge[(int)(v)][(int)(u)] = 1LL;
                path = dfs(v, graph, visited_edge, visited_edge_len, visited_edge_lens, visited_edge_len, path, path_len);
                path_len = dfs_len;
            }
            i = i + 1LL;
        }
    }
    return dfs_len = path_len, path;
}

CheckResult check_circuit_or_path(MapIL graph, long long max_node) {
    long long odd_degree_nodes = 0LL;
    double odd_node = -1LL;
    long long i = 0LL;
    while (i < max_node) {
        if (contains_map_int(graph.keys, graph.len, i)) {
            if ((graph.lens[i] % 2LL) == 1LL) {
                odd_degree_nodes = odd_degree_nodes + 1LL;
                odd_node = i;
            }
        }
        i = i + 1LL;
    }
    if (odd_degree_nodes == 0LL) {
        return (CheckResult){.status = 1LL, .odd_node = odd_node};
    }
    if (odd_degree_nodes == 2LL) {
        return (CheckResult){.status = 2LL, .odd_node = odd_node};
    }
    return (CheckResult){.status = 3LL, .odd_node = odd_node};
}

void check_euler(MapIL graph, long long max_node) {
    long long **visited_edge = make_matrix(max_node);
    size_t visited_edge_len = make_matrix_len;
    size_t *visited_edge_lens = make_matrix_lens;
    size_t visited_edge_lens_len = make_matrix_len;
    CheckResult res = check_circuit_or_path(graph, max_node);
    if (res.status == 3LL) {
        puts("graph is not Eulerian");
        puts("no path");
        return 0;
    }
    long long start_node = 1LL;
    if (res.status == 2LL) {
        start_node = res.odd_node;
        puts("graph has a Euler path");
    }
    if (res.status == 1LL) {
        puts("graph has a Euler cycle");
    }
    long long *path = dfs(start_node, graph, visited_edge, visited_edge_len, visited_edge_lens, visited_edge_len, NULL, 0);
    size_t path_len = dfs_len;
    puts(str_list_int(path, path_len));
}

int main(void) {
    {
        long long __start = _now();
        check_euler(g1, max_node);
        check_euler(g2, max_node);
        check_euler(g3, max_node);
        check_euler(g4, max_node);
        check_euler(g5, max_node);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
