// Generated by Mochi 0.10.32 on 2025-08-15 21:09 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t clone_path_len;
size_t sort_nodes_len;
size_t get_successors_len;
size_t greedy_best_first_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;
typedef struct Pos Pos;

struct Node {
    long long pos_x;
    long long pos_y;
    long long goal_x;
    long long goal_y;
    long long g_cost;
    long long f_cost;
    Pos *path;
    size_t path_len;
};

struct Pos {
    long long y;
    long long x;
};

static Pos* list_append_Pos(Pos *arr, size_t *len, Pos val) {
    arr = realloc(arr, (*len + 1) * sizeof(Pos));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Pos delta_init[4] = {(Pos){.y = -1LL, .x = 0LL}, (Pos){.y = 0LL, .x = -1LL}, (Pos){.y = 1LL, .x = 0LL}, (Pos){.y = 0LL, .x = 1LL}};
Pos *delta = delta_init;
size_t delta_len = 4;
long long TEST_GRIDS_0_0[7] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_0_1[7] = {0LL, 1LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_0_2[7] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_0_3[7] = {0LL, 0LL, 1LL, 0LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_0_4[7] = {1LL, 0LL, 1LL, 0LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_0_5[7] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_0_6[7] = {0LL, 0LL, 0LL, 0LL, 1LL, 0LL, 0LL};
long long *TEST_GRIDS_0[7] = {TEST_GRIDS_0_0, TEST_GRIDS_0_1, TEST_GRIDS_0_2, TEST_GRIDS_0_3, TEST_GRIDS_0_4, TEST_GRIDS_0_5, TEST_GRIDS_0_6};
size_t TEST_GRIDS_0_lens[7] = {7, 7, 7, 7, 7, 7, 7};
long long TEST_GRIDS_1_0[7] = {0LL, 0LL, 0LL, 1LL, 1LL, 0LL, 0LL};
long long TEST_GRIDS_1_1[7] = {0LL, 0LL, 0LL, 0LL, 1LL, 0LL, 1LL};
long long TEST_GRIDS_1_2[7] = {0LL, 0LL, 0LL, 1LL, 1LL, 0LL, 0LL};
long long TEST_GRIDS_1_3[7] = {0LL, 1LL, 0LL, 0LL, 1LL, 0LL, 0LL};
long long TEST_GRIDS_1_4[7] = {1LL, 0LL, 0LL, 1LL, 1LL, 0LL, 1LL};
long long TEST_GRIDS_1_5[7] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
long long *TEST_GRIDS_1[6] = {TEST_GRIDS_1_0, TEST_GRIDS_1_1, TEST_GRIDS_1_2, TEST_GRIDS_1_3, TEST_GRIDS_1_4, TEST_GRIDS_1_5};
size_t TEST_GRIDS_1_lens[6] = {7, 7, 7, 7, 7, 7};
long long TEST_GRIDS_2_0[5] = {0LL, 0LL, 1LL, 0LL, 0LL};
long long TEST_GRIDS_2_1[5] = {0LL, 1LL, 0LL, 0LL, 0LL};
long long TEST_GRIDS_2_2[5] = {0LL, 0LL, 1LL, 0LL, 1LL};
long long TEST_GRIDS_2_3[5] = {1LL, 0LL, 0LL, 1LL, 1LL};
long long TEST_GRIDS_2_4[5] = {0LL, 0LL, 0LL, 0LL, 0LL};
long long *TEST_GRIDS_2[5] = {TEST_GRIDS_2_0, TEST_GRIDS_2_1, TEST_GRIDS_2_2, TEST_GRIDS_2_3, TEST_GRIDS_2_4};
size_t TEST_GRIDS_2_lens[5] = {5, 5, 5, 5, 5};
long long **TEST_GRIDS_init[3] = {TEST_GRIDS_0, TEST_GRIDS_1, TEST_GRIDS_2};
long long ***TEST_GRIDS = TEST_GRIDS_init;
size_t TEST_GRIDS_len = 3;
size_t TEST_GRIDS_lens_init[3] = {7, 6, 5};
size_t *TEST_GRIDS_lens = TEST_GRIDS_lens_init;
size_t TEST_GRIDS_lens_len = 3;
size_t *TEST_GRIDS_lens_lens_init[3] = {TEST_GRIDS_0_lens, TEST_GRIDS_1_lens, TEST_GRIDS_2_lens};
size_t **TEST_GRIDS_lens_lens = TEST_GRIDS_lens_lens_init;
size_t TEST_GRIDS_lens_lens_len = 3;

long long user_abs(long long x);
long long manhattan(long long x1, long long y1, long long x2, long long y2);
Pos * clone_path(Pos * p, size_t p_len);
Node make_node(long long pos_x, long long pos_y, long long goal_x, long long goal_y, long long g_cost, Pos * path, size_t path_len);
long long node_equal(Node a, Node b);
long long contains(Node * nodes, size_t nodes_len, Node node);
Node * sort_nodes(Node * nodes, size_t nodes_len);
Node * get_successors(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, Node parent, Pos target);
Pos * greedy_best_first(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, Pos init, Pos goal);
void print_grid(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
void user_main();
int main(void);

long long user_abs(long long x) {
    if (x < 0LL) {
        return 0LL - x;
    }
    return x;
}

long long manhattan(long long x1, long long y1, long long x2, long long y2) {
    return user_abs(x1 - x2) + user_abs(y1 - y2);
}

Pos * clone_path(Pos * p, size_t p_len) {
    Pos *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < p_len) {
        res = list_append_Pos(res, &res_len, p[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? p_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return clone_path_len = res_len, res;
}

Node make_node(long long pos_x, long long pos_y, long long goal_x, long long goal_y, long long g_cost, Pos * path, size_t path_len) {
    long long f = manhattan(pos_x, pos_y, goal_x, goal_y);
    return (Node){.pos_x = pos_x, .pos_y = pos_y, .goal_x = goal_x, .goal_y = goal_y, .g_cost = g_cost, .f_cost = f, .path = path, .path_len = path_len};
}

long long node_equal(Node a, Node b) {
    return (a.pos_x == b.pos_x) && (a.pos_y == b.pos_y);
}

long long contains(Node * nodes, size_t nodes_len, Node node) {
    long long i = 0LL;
    while (i < nodes_len) {
        if (node_equal(nodes[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})], node)) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

Node * sort_nodes(Node * nodes, size_t nodes_len) {
    Node *arr = nodes;
    size_t arr_len = nodes_len;
    long long i = 1LL;
    while (i < arr_len) {
        Node key_node = arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
        long long j = i - 1LL;
        while (j >= 0LL) {
            Node temp = arr[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})];
            if (temp.f_cost > key_node.f_cost) {
                arr[(int)(j + 1LL)] = temp;
                j = j - 1LL;
            } else {
                break;
            }
        }
        arr[(int)(j + 1LL)] = key_node;
        i = i + 1LL;
    }
    return sort_nodes_len = arr_len, arr;
}

Node * get_successors(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, Node parent, Pos target) {
    Node *res = NULL;
    size_t res_len = 0;
    long long i = 0LL;
    while (i < 4LL) {
        Pos d = delta[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? delta_len + _mochi_idx : _mochi_idx;})];
        long long pos_x = parent.pos_x + d.x;
        long long pos_y = parent.pos_y + d.y;
        if (((((pos_x >= 0LL) && (pos_x < grid_lens[0LL])) && (pos_y >= 0LL)) && (pos_y < grid_len)) && (grid[(int)({long long _mochi_idx = pos_y; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = pos_x; _mochi_idx < 0 ? grid_lens[pos_y] + _mochi_idx : _mochi_idx;})] == 0LL)) {
            Pos *new_path = clone_path(parent.path, parent.path_len);
            size_t new_path_len = clone_path_len;
            new_path = list_append_Pos(new_path, &new_path_len, (Pos){.y = pos_y, .x = pos_x});
            res = list_append_Node(res, &res_len, make_node(pos_x, pos_y, target.x, target.y, parent.g_cost + 1LL, new_path, new_path_len));
        }
        i = i + 1LL;
    }
    return get_successors_len = res_len, res;
}

Pos * greedy_best_first(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, Pos init, Pos goal) {
    Pos *start_path = NULL;
    size_t start_path_len = 0;
    start_path = list_append_Pos(start_path, &start_path_len, init);
    Node start = make_node(init.x, init.y, goal.x, goal.y, 0LL, start_path, start_path_len);
    Node *open_nodes = NULL;
    size_t open_nodes_len = 0;
    open_nodes = list_append_Node(open_nodes, &open_nodes_len, start);
    Node *closed_nodes = NULL;
    size_t closed_nodes_len = 0;
    while (open_nodes_len > 0LL) {
        open_nodes = sort_nodes(open_nodes, open_nodes_len);
        open_nodes_len = sort_nodes_len;
        Node current = open_nodes[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? open_nodes_len + _mochi_idx : _mochi_idx;})];
        Node *new_open = NULL;
        size_t new_open_len = 0;
        long long idx = 1LL;
        while (idx < open_nodes_len) {
            new_open = list_append_Node(new_open, &new_open_len, open_nodes[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? open_nodes_len + _mochi_idx : _mochi_idx;})]);
            idx = idx + 1LL;
        }
        open_nodes = new_open;
        open_nodes_len = new_open_len;
        if ((current.pos_x == goal.x) && (current.pos_y == goal.y)) {
            return greedy_best_first_len = current.path_len, current.path;
        }
        closed_nodes = list_append_Node(closed_nodes, &closed_nodes_len, current);
        Node *successors = get_successors(grid, grid_len, grid_lens, grid_len, current, goal);
        size_t successors_len = get_successors_len;
        long long i = 0LL;
        while (i < successors_len) {
            Node child = successors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? successors_len + _mochi_idx : _mochi_idx;})];
            if (!(contains(closed_nodes, closed_nodes_len, child)) && !(contains(open_nodes, open_nodes_len, child))) {
                open_nodes = list_append_Node(open_nodes, &open_nodes_len, child);
            }
            i = i + 1LL;
        }
    }
    Pos *r = NULL;
    size_t r_len = 0;
    r = list_append_Pos(r, &r_len, init);
    return greedy_best_first_len = r_len, r;
}

void print_grid(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    long long i = 0LL;
    while (i < grid_len) {
        puts(str_list_int(grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})], grid_lens[i]));
        i = i + 1LL;
    }
}

void user_main() {
    long long idx = 0LL;
    while (idx < TEST_GRIDS_len) {
        puts(str_concat(str_concat("==grid-", str_int(idx + 1LL)), "=="));
        long long **grid = TEST_GRIDS[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? TEST_GRIDS_len + _mochi_idx : _mochi_idx;})];
        size_t grid_len = TEST_GRIDS_lens[idx];
        size_t *grid_lens = TEST_GRIDS_lens_lens[(int)(idx)];
        size_t grid_lens_len = grid_len;
        Pos init = (Pos){.y = 0LL, .x = 0LL};
        Pos goal = (Pos){.y = grid_len - 1LL, .x = grid_lens[0LL] - 1LL};
        print_grid(grid, grid_len, grid_lens, grid_len);
        puts("------");
        Pos *path = greedy_best_first(grid, grid_len, grid_lens, grid_len, init, goal);
        size_t path_len = greedy_best_first_len;
        long long j = 0LL;
        while (j < path_len) {
            Pos p = path[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? path_len + _mochi_idx : _mochi_idx;})];
            grid[(int)(p.y)][(int)(p.x)] = 2LL;
            j = j + 1LL;
        }
        print_grid(grid, grid_len, grid_lens, grid_len);
        idx = idx + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
