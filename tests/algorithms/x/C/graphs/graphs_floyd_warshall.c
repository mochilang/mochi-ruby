// Generated by Mochi 0.10.32 on 2025-08-15 21:09 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t floyd_warshall_len;
size_t *floyd_warshall_lens;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

double INF = 1000000000LL;
long long **graph = NULL;
size_t graph_len = 0;
size_t *graph_lens = NULL;
size_t graph_lens_len = 0;
double **result = NULL;
size_t result_len = 0;
size_t *result_lens = NULL;
size_t result_lens_len = 0;

double * * floyd_warshall(double * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len);
const char* print_dist(double * * dist, size_t dist_len, size_t* dist_lens, size_t dist_lens_len);
int main(void);

double * * floyd_warshall(double * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len) {
    long long v = graph_len;
    double **dist = NULL;
    size_t dist_len = 0;
    size_t *dist_lens = NULL;
    size_t dist_lens_len = 0;
    long long i = 0LL;
    while (i < v) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < v) {
            row = list_append_double(row, &row_len, graph[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? graph_lens[i] + _mochi_idx : _mochi_idx;})]);
            j = j + 1LL;
        }
        dist = list_append_doubleptr(dist, &dist_len, row);
        dist_lens = list_append_szt(dist_lens, &dist_lens_len, row_len);
        i = i + 1LL;
    }
    long long k = 0LL;
    while (k < v) {
        long long i = 0LL;
        while (i < v) {
            long long j = 0LL;
            while (j < v) {
                if (((dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dist_lens[i] + _mochi_idx : _mochi_idx;})] < INF) && (dist[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dist_lens[k] + _mochi_idx : _mochi_idx;})] < INF)) && ((dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dist_lens[i] + _mochi_idx : _mochi_idx;})] + dist[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dist_lens[k] + _mochi_idx : _mochi_idx;})]) < dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dist_lens[i] + _mochi_idx : _mochi_idx;})])) {
                    dist[(int)(i)][(int)(j)] = dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dist_lens[i] + _mochi_idx : _mochi_idx;})] + dist[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dist_lens[k] + _mochi_idx : _mochi_idx;})];
                }
                j = j + 1LL;
            }
            i = i + 1LL;
        }
        k = k + 1LL;
    }
    return floyd_warshall_lens = dist_lens, floyd_warshall_len = dist_len, dist;
}

const char* print_dist(double * * dist, size_t dist_len, size_t* dist_lens, size_t dist_lens_len) {
    puts("\nThe shortest path matrix using Floyd Warshall algorithm\n");
    long long i = 0LL;
    while (i < dist_len) {
        long long j = 0LL;
        const char* line = "";
        while (j < dist_lens[i]) {
            if (dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dist_lens[i] + _mochi_idx : _mochi_idx;})] >= (INF / 2LL)) {
                line = str_concat(line, "INF\t");
            } else {
                line = str_concat(str_concat(line, str_int((int)(dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? dist_lens[i] + _mochi_idx : _mochi_idx;})]))), "\t");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        graph = ({int** tmp = malloc(4 * sizeof(int*)); tmp[0] = ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 5LL; tmp[2] = INF; tmp[3] = 10LL; tmp;}); tmp[1] = ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = INF; tmp[1] = 0LL; tmp[2] = 3LL; tmp[3] = INF; tmp;}); tmp[2] = ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = INF; tmp[1] = INF; tmp[2] = 0LL; tmp[3] = 1LL; tmp;}); tmp[3] = ({double *tmp = malloc(4 * sizeof(double)); tmp[0] = INF; tmp[1] = INF; tmp[2] = INF; tmp[3] = 0LL; tmp;}); tmp;});
        graph_len = 4;
        graph_lens = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 4; tmp[1] = 4; tmp[2] = 4; tmp[3] = 4; tmp;});
        graph_lens_len = 4;
        result = floyd_warshall(graph, graph_len, graph_lens, graph_len);
        result_len = floyd_warshall_len;
        result_lens = floyd_warshall_lens;
        print_dist(result, result_len, result_lens, result_len);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
