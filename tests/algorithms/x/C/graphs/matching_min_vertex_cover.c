// Generated by Mochi 0.10.32 on 2025-08-16 00:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t get_edges_len;
size_t *get_edges_lens;
size_t matching_min_vertex_cover_len;

size_t _slice_intptr_len;
size_t *_slice_intptr_lens;
size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long** _slice_intptr(long long **arr, size_t len, size_t *lens, int start, int end, size_t *out_len, size_t **out_lens) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    *out_len = n;
    _slice_intptr_len = n;
    *out_lens = lens + start;
    _slice_intptr_lens = lens + start;
    return arr + start;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int graph_keys[21] = {0LL, 1LL, 2LL, 3LL, 4LL};
static long long graph_vals_0[2] = {1LL, 3LL};
static long long graph_vals_1[2] = {0LL, 3LL};
static long long graph_vals_2[3] = {0LL, 3LL, 4LL};
static long long graph_vals_3[3] = {0LL, 1LL, 2LL};
static long long graph_vals_4[2] = {2LL, 3LL};
static void* graph_vals[21] = {graph_vals_0, graph_vals_1, graph_vals_2, graph_vals_3, graph_vals_4};
static size_t graph_lens[21] = {2, 2, 3, 3, 2};
size_t graph_len = 5;
MapIL graph = { graph_keys, graph_vals, graph_lens, 5, 21 };
long long *cover = NULL;
size_t cover_len = 0;

long long contains(long long * xs, size_t xs_len, long long v);
long long * * get_edges(MapIL graph);
long long * matching_min_vertex_cover(MapIL graph);
int main(void);

long long contains(long long * xs, size_t xs_len, long long v) {
    for (size_t __i = 0; __i < xs_len; __i++) {
        long long x = xs[__i];
        if (x == v) {
            return 1LL;
        }
    }
    return 0LL;
}

long long * * get_edges(MapIL graph) {
    long long n = graph.len;
    long long **edges = NULL;
    size_t edges_len = 0;
    size_t *edges_lens = NULL;
    size_t edges_lens_len = 0;
    for (int i = 0LL; i < n; i++) {
        {
            size_t __tmp0_len = 0;
            long long* __tmp0 = map_get_il(graph.keys, graph.vals, graph.lens, graph.len, i, &__tmp0_len);
            for (size_t __i = 0; __i < __tmp0_len; __i++) {
                long long j = __tmp0[__i];
                edges = list_append_intptr(edges, &edges_len, ({long long* tmp = malloc(2 * sizeof(long long)); tmp[0] = i; tmp[1] = j; tmp;}));
                edges_lens = list_append_szt(edges_lens, &edges_lens_len, 2);
            }
        }
    }
    return get_edges_lens = edges_lens, get_edges_len = edges_len, edges;
}

long long * matching_min_vertex_cover(MapIL graph) {
    long long *chosen = NULL;
    size_t chosen_len = 0;
    long long **edges = get_edges(graph);
    size_t edges_len = get_edges_len;
    size_t *edges_lens = get_edges_lens;
    size_t edges_lens_len = get_edges_len;
    while (edges_len > 0LL) {
        long long idx = edges_len - 1LL;
        long long *e = edges[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? edges_len + _mochi_idx : _mochi_idx;})];
        size_t e_len = edges_lens[idx];
        edges = _slice_intptr(edges, edges_len, edges_lens, 0LL, idx, &(_slice_intptr_len), &(_slice_intptr_lens));
        edges_len = _slice_intptr_len;
        edges_lens = _slice_intptr_lens;
        edges_lens_len = _slice_intptr_len;
        long long u = e[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})];
        long long v = e[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? e_len + _mochi_idx : _mochi_idx;})];
        if (!(contains(chosen, chosen_len, u))) {
            chosen = list_append_long_long(chosen, &chosen_len, u);
        }
        if (!(contains(chosen, chosen_len, v))) {
            chosen = list_append_long_long(chosen, &chosen_len, v);
        }
        long long **filtered = NULL;
        size_t filtered_len = 0;
        size_t *filtered_lens = NULL;
        size_t filtered_lens_len = 0;
        for (size_t __i = 0; __i < edges_len; __i++) {
            long long *edge = edges[__i];
            size_t edge_len = edges_lens[__i];
            long long a = edge[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? edge_len + _mochi_idx : _mochi_idx;})];
            long long b = edge[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? edge_len + _mochi_idx : _mochi_idx;})];
            if ((((a != u) && (b != u)) && (a != v)) && (b != v)) {
                filtered = list_append_intptr(filtered, &filtered_len, edge);
                filtered_lens = list_append_szt(filtered_lens, &filtered_lens_len, edge_len);
            }
        }
        edges = filtered;
        edges_len = filtered_len;
        edges_lens = filtered_lens;
        edges_lens_len = filtered_lens_len;
    }
    return matching_min_vertex_cover_len = chosen_len, chosen;
}

int main(void) {
    {
        long long __start = _now();
        cover = matching_min_vertex_cover(graph);
        cover_len = matching_min_vertex_cover_len;
        puts(str_list_int(cover, cover_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
