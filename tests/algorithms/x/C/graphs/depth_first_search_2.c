// Generated by Mochi 0.10.32 on 2025-08-14 20:55 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t dfs_recursive_len;
size_t dfs_len;

size_t append_len;
static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

typedef struct { int *keys; void **vals; size_t *lens; size_t len; size_t cap; } MapIL;

static char* str_map_il(MapIL m) {
    char *res = strdup("map[");
    for (size_t i = 0; i < m.len; i++) {
        char *k = str_int(m.keys[i]);
        char *v = str_list_int(m.vals[i], m.lens[i]);
        char *kv = str_concat(k, ":");
        kv = str_concat(kv, "[");
        kv = str_concat(kv, v);
        kv = str_concat(kv, "]");
        res = str_concat(res, kv);
        if (i + 1 < m.len) res = str_concat(res, " ");
        free(k); free(v); free(kv);
    }
    res = str_concat(res, "]");
    return res;
}

static void* map_get_il(const int keys[], void *vals[], const size_t lens[], size_t len, int key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static void map_set_il(int **keys, void ***vals, size_t **lens, size_t *len, size_t *cap, int key, void *val, size_t val_len) {
    for (size_t i = 0; i < *len; i++) {
        if ((*keys)[i] == key) { (*vals)[i] = val; (*lens)[i] = val_len; return; }
    }
    if (*len >= *cap) {
        *cap = *cap ? *cap * 2 : 16;
        *keys = realloc(*keys, *cap * sizeof(int));
        *vals = realloc(*vals, *cap * sizeof(void*));
        *lens = realloc(*lens, *cap * sizeof(size_t));
    }
    (*keys)[*len] = key; (*vals)[*len] = val; (*lens)[*len] = val_len; (*len)++;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Anon1 Anon1;
typedef struct Graph Graph;

struct Anon1 {
};

struct Graph {
    MapIL vertex;
    long long size;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Graph g;

Graph add_edge(Graph g, long long from_vertex, long long to_vertex);
const char* list_to_string(long long * lst, size_t lst_len);
const char* list_to_arrow(long long * lst, size_t lst_len);
const char* print_graph(Graph g);
long long * dfs_recursive(Graph g, long long start_vertex, long long * visited, size_t visited_len, long long * order, size_t order_len);
long long * dfs(Graph g);
int main(void);

Graph add_edge(Graph g, long long from_vertex, long long to_vertex) {
    MapIL v = g.vertex;
    if (contains_map_int(v.keys, v.len, from_vertex)) {
        long long *lst = map_get_il(v.keys, v.vals, v.lens, v.len, from_vertex, NULL);
        size_t lst_len = v.lens[from_vertex];
        lst = list_append_long_long(lst, &lst_len, to_vertex);
        map_set_il(&v.keys, &v.vals, &v.lens, &v.len, &v.cap, from_vertex, lst, lst_len);
    } else {
        map_set_il(&v.keys, &v.vals, &v.lens, &v.len, &v.cap, from_vertex, ({long long *tmp = malloc(1 * sizeof(long long)); tmp[0] = to_vertex; tmp;}), 1);
    }
    g.vertex = v;
    if ((from_vertex + 1LL) > g.size) {
        g.size = from_vertex + 1LL;
    }
    if ((to_vertex + 1LL) > g.size) {
        g.size = to_vertex + 1LL;
    }
    return g;
}

const char* list_to_string(long long * lst, size_t lst_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < lst_len) {
        res = str_concat(res, str_int(lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]));
        if (i < (lst_len - 1LL)) {
            res = str_concat(res, " ");
        }
        i = i + 1LL;
    }
    return res;
}

const char* list_to_arrow(long long * lst, size_t lst_len) {
    const char* res = "";
    long long i = 0LL;
    while (i < lst_len) {
        res = str_concat(res, str_int(lst[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? lst_len + _mochi_idx : _mochi_idx;})]));
        if (i < (lst_len - 1LL)) {
            res = str_concat(res, " -> ");
        }
        i = i + 1LL;
    }
    return res;
}

const char* print_graph(Graph g) {
    puts(str_map_il(g.vertex));
    long long i = 0LL;
    while (i < g.size) {
        long long *edges = NULL;
        size_t edges_len = 0;
        if (contains_map_int(g.vertex.keys, g.vertex.len, i)) {
            edges = map_get_il(g.vertex.keys, g.vertex.vals, g.vertex.lens, g.vertex.len, i, NULL);
        }
        const char* line = str_concat(str_concat(str_int(i), "  ->  "), list_to_arrow(edges, edges_len));
        puts(line);
        i = i + 1LL;
    }
}

long long * dfs_recursive(Graph g, long long start_vertex, long long * visited, size_t visited_len, long long * order, size_t order_len) {
    visited[(int)(start_vertex)] = 1LL;
    order = list_append_long_long(order, &order_len, start_vertex);
    if (contains_map_int(g.vertex.keys, g.vertex.len, start_vertex)) {
        long long *neighbors = map_get_il(g.vertex.keys, g.vertex.vals, g.vertex.lens, g.vertex.len, start_vertex, NULL);
        size_t neighbors_len = g.vertex.lens[start_vertex];
        long long i = 0LL;
        while (i < neighbors_len) {
            long long nb = neighbors[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbors_len + _mochi_idx : _mochi_idx;})];
            if (!(visited[(int)({long long _mochi_idx = nb; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
                order = dfs_recursive(g, nb, visited, visited_len, order, order_len);
                order_len = dfs_recursive_len;
            }
            i = i + 1LL;
        }
    }
    return dfs_recursive_len = order_len, order;
}

long long * dfs(Graph g) {
    long long n = g.size;
    long long *visited = NULL;
    size_t visited_len = 0;
    long long i = 0LL;
    while (i < n) {
        visited = list_append_long_long(visited, &visited_len, 0LL);
        i = i + 1LL;
    }
    long long *order = NULL;
    size_t order_len = 0;
    i = 0LL;
    while (i < n) {
        if (!(visited[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
            order = dfs_recursive(g, i, visited, visited_len, order, order_len);
            order_len = dfs_recursive_len;
        }
        i = i + 1LL;
    }
    return dfs_len = order_len, order;
}

int main(void) {
    {
        long long __start = _now();
        g = (Graph){.vertex = (MapIL){0}, .size = 0LL};
        g = add_edge(g, 0LL, 1LL);
        g = add_edge(g, 0LL, 2LL);
        g = add_edge(g, 1LL, 2LL);
        g = add_edge(g, 2LL, 0LL);
        g = add_edge(g, 2LL, 3LL);
        g = add_edge(g, 3LL, 3LL);
        print_graph(g);
        puts("DFS:");
        puts(({long long* __tmp1 = dfs(g); list_to_string(__tmp1, dfs_len);}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
