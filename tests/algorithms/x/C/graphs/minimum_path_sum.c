// Generated by Mochi 0.10.32 on 2025-08-16 10:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t fill_row_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

long long grid1_0[3] = {1LL, 3LL, 1LL};
long long grid1_1[3] = {1LL, 5LL, 1LL};
long long grid1_2[3] = {4LL, 2LL, 1LL};
long long *grid1_init[3] = {grid1_0, grid1_1, grid1_2};
long long **grid1 = grid1_init;
size_t grid1_len = 3;
size_t grid1_lens_init[3] = {3, 3, 3};
size_t *grid1_lens = grid1_lens_init;
size_t grid1_lens_len = 3;
long long grid2_0[5] = {1LL, 0LL, 5LL, 6LL, 7LL};
long long grid2_1[5] = {8LL, 9LL, 0LL, 4LL, 2LL};
long long grid2_2[5] = {4LL, 4LL, 4LL, 5LL, 1LL};
long long grid2_3[5] = {9LL, 6LL, 3LL, 1LL, 0LL};
long long grid2_4[5] = {8LL, 4LL, 3LL, 2LL, 7LL};
long long *grid2_init[5] = {grid2_0, grid2_1, grid2_2, grid2_3, grid2_4};
long long **grid2 = grid2_init;
size_t grid2_len = 5;
size_t grid2_lens_init[5] = {5, 5, 5, 5, 5};
size_t *grid2_lens = grid2_lens_init;
size_t grid2_lens_len = 5;

long long * fill_row(long long * current_row, size_t current_row_len, long long * row_above, size_t row_above_len);
long long min_path_sum(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len);
int main(void);

long long * fill_row(long long * current_row, size_t current_row_len, long long * row_above, size_t row_above_len) {
    current_row[(int)(0LL)] = current_row[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? current_row_len + _mochi_idx : _mochi_idx;})] + row_above[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? row_above_len + _mochi_idx : _mochi_idx;})];
    long long cell_n = 1LL;
    while (cell_n < current_row_len) {
        long long left = current_row[(int)({long long _mochi_idx = cell_n - 1LL; _mochi_idx < 0 ? current_row_len + _mochi_idx : _mochi_idx;})];
        long long up = row_above[(int)({long long _mochi_idx = cell_n; _mochi_idx < 0 ? row_above_len + _mochi_idx : _mochi_idx;})];
        if (left < up) {
            current_row[(int)(cell_n)] = current_row[(int)({long long _mochi_idx = cell_n; _mochi_idx < 0 ? current_row_len + _mochi_idx : _mochi_idx;})] + left;
        } else {
            current_row[(int)(cell_n)] = current_row[(int)({long long _mochi_idx = cell_n; _mochi_idx < 0 ? current_row_len + _mochi_idx : _mochi_idx;})] + up;
        }
        cell_n = cell_n + 1LL;
    }
    return fill_row_len = current_row_len, current_row;
}

long long min_path_sum(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len) {
    if ((grid_len == 0LL) || (grid_lens[0LL] == 0LL)) {
        error("The grid does not contain the appropriate information");
    }
    long long cell_n = 1LL;
    while (cell_n < grid_lens[0LL]) {
        grid[(int)(0LL)][(int)(cell_n)] = grid[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = cell_n; _mochi_idx < 0 ? grid_lens[0LL] + _mochi_idx : _mochi_idx;})] + grid[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = cell_n - 1LL; _mochi_idx < 0 ? grid_lens[0LL] + _mochi_idx : _mochi_idx;})];
        cell_n = cell_n + 1LL;
    }
    long long *row_above = grid[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})];
    size_t row_above_len = grid_lens[0LL];
    long long row_n = 1LL;
    while (row_n < grid_len) {
        long long *current_row = grid[(int)({long long _mochi_idx = row_n; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})];
        size_t current_row_len = grid_lens[row_n];
        grid[(int)(row_n)] = fill_row(current_row, current_row_len, row_above, row_above_len);
        row_above = grid[(int)({long long _mochi_idx = row_n; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})];
        row_n = row_n + 1LL;
    }
    return grid[(int)({long long _mochi_idx = grid_len - 1LL; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = grid_lens[0LL] - 1LL; _mochi_idx < 0 ? grid_lens[grid_len - 1LL] + _mochi_idx : _mochi_idx;})];
}

int main(void) {
    {
        long long __start = _now();
        puts(str_int(min_path_sum(grid1, grid1_len, grid1_lens, grid1_len)));
        puts(str_int(min_path_sum(grid2, grid2_len, grid2_lens, grid2_len)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
