// Generated by Mochi 0.10.32 on 2025-08-14 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bfs_shortest_path_len;

size_t _slice_str_len;
size_t _slice_strptr_len;
size_t *_slice_strptr_lens;
size_t append_len;
typedef struct { const char **keys; void **vals; size_t *lens; size_t len; size_t cap; } MapSL;

typedef struct { const char **keys; int *vals; size_t len; size_t cap; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(MapSI *m, const char *key, int val) {
    for (size_t i = 0; i < m->len; i++) {
        if (strcmp(m->keys[i], key) == 0) { m->vals[i] = val; return; }
    }
    if (m->len >= m->cap) {
        m->cap = m->cap ? m->cap * 2 : 16;
        m->keys = realloc((void*)m->keys, m->cap * sizeof(char*));
        m->vals = realloc(m->vals, m->cap * sizeof(int));
    }
    m->keys[m->len] = key; m->vals[m->len] = val; m->len++;
}

static void* map_get_sl(const char *keys[], void *vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { if (out_len) *out_len = lens[i]; return vals[i]; }
    }
    if (out_len) *out_len = 0;
    return NULL;
}

static size_t map_len_sl(const char *keys[], const size_t lens[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return lens[i];
    }
    return 0;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str_new(const char **arr, size_t len, const char *val) {
    const char **res = malloc((len + 1) * sizeof(const char*));
    if (arr && len) memcpy(res, arr, len * sizeof(const char*));
    res[len] = val;
    append_len = len + 1;
    return res;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

static const char*** _slice_strptr(const char ***arr, size_t len, size_t *lens, int start, int end, size_t *out_len, size_t **out_lens) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    *out_len = n;
    _slice_strptr_len = n;
    *out_lens = lens + start;
    _slice_strptr_lens = lens + start;
    return arr + start;
}

static const char* demo_graph_keys[23] = {"A", "B", "C", "D", "E", "F", "G"};
static const char* demo_graph_vals_0[3] = {"B", "C", "E"};
static const char* demo_graph_vals_1[3] = {"A", "D", "E"};
static const char* demo_graph_vals_2[3] = {"A", "F", "G"};
static const char* demo_graph_vals_3[1] = {"B"};
static const char* demo_graph_vals_4[3] = {"A", "B", "D"};
static const char* demo_graph_vals_5[1] = {"C"};
static const char* demo_graph_vals_6[1] = {"C"};
static void* demo_graph_vals[23] = {demo_graph_vals_0, demo_graph_vals_1, demo_graph_vals_2, demo_graph_vals_3, demo_graph_vals_4, demo_graph_vals_5, demo_graph_vals_6};
static size_t demo_graph_lens[23] = {3, 3, 3, 1, 3, 1, 1};
size_t demo_graph_len = 7;
MapSL demo_graph = { demo_graph_keys, demo_graph_vals, demo_graph_lens, 7, 23 };

long long contains(const char* * xs, size_t xs_len, const char* x);
long long contains_key(MapSL m, const char* key);
const char* * bfs_shortest_path(MapSL graph, const char* start, const char* goal);
long long bfs_shortest_path_distance(MapSL graph, const char* start, const char* target);
int main(void);

long long contains(const char* * xs, size_t xs_len, const char* x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (strcmp(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})], x) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long contains_key(MapSL m, const char* key) {
    for (size_t __i = 0; __i < m.len; __i++) {
        const char* k = m.keys[__i];
        if (strcmp(k, key) == 0) {
            return 1LL;
        }
    }
    return 0LL;
}

const char* * bfs_shortest_path(MapSL graph, const char* start, const char* goal) {
    const char* *explored = NULL;
    size_t explored_len = 0;
    const char* queue_0[1] = {start};
    const char* *queue_init[1] = {queue_0};
    const char* **queue = queue_init;
    size_t queue_len = 1;
    size_t queue_lens_init[1] = {1};
    size_t *queue_lens = queue_lens_init;
    size_t queue_lens_len = 1;
    if (strcmp(start, goal) == 0) {
        {
            const char** __tmp0_data = malloc(1 * sizeof(const char*));
            __tmp0_data[0] = start;
            bfs_shortest_path_len = 1;
            return __tmp0_data;
        }
    }
    while (queue_len > 0LL) {
        const char* *path = queue[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        size_t path_len = queue_lens[0LL];
        queue = _slice_strptr(queue, queue_len, queue_lens, 1LL, queue_len, &(_slice_strptr_len), &(_slice_strptr_lens));
        queue_len = _slice_strptr_len;
        queue_lens = _slice_strptr_lens;
        queue_lens_len = _slice_strptr_len;
        const char* node = path[(int)({long long _mochi_idx = path_len - 1LL; _mochi_idx < 0 ? path_len + _mochi_idx : _mochi_idx;})];
        if (!(contains(explored, explored_len, node))) {
            const char* *neighbours = map_get_sl(graph.keys, graph.vals, graph.lens, graph.len, node, NULL);
            size_t neighbours_len = map_len_sl(graph.keys, graph.lens, graph.len, node);
            long long i = 0LL;
            while (i < neighbours_len) {
                const char* neighbour = neighbours[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? neighbours_len + _mochi_idx : _mochi_idx;})];
                const char* *new_path = path;
                size_t new_path_len = path_len;
                new_path = list_append_str_new(new_path, new_path_len, neighbour);
                new_path_len++;
                queue = list_append_strptr(queue, &queue_len, new_path);
                queue_lens = list_append_szt(queue_lens, &queue_lens_len, new_path_len);
                if (strcmp(neighbour, goal) == 0) {
                    return bfs_shortest_path_len = new_path_len, new_path;
                }
                i = i + 1LL;
            }
            explored = list_append_str(explored, &explored_len, node);
        }
    }
    {
        const char** __tmp1_data = malloc(0 * sizeof(const char*));
        bfs_shortest_path_len = 0;
        return __tmp1_data;
    }
}

long long bfs_shortest_path_distance(MapSL graph, const char* start, const char* target) {
    if ((contains_key(graph, start) == 0LL) || (contains_key(graph, target) == 0LL)) {
        return -1LL;
    }
    if (strcmp(start, target) == 0) {
        return 0LL;
    }
    const char* *queue = NULL;
    size_t queue_len = 0;
    queue = list_append_str(queue, &queue_len, start);
    const char* *visited = NULL;
    size_t visited_len = 0;
    visited = list_append_str(visited, &visited_len, start);
    static const char* dist_keys[16] = {};
    static int dist_vals[16] = {};
    size_t dist_len = 0;
    MapSI dist = { dist_keys, dist_vals, 0, 16 };
    map_set_si(&dist, start, 0LL);
    map_set_si(&dist, target, -1LL);
    while (queue_len > 0LL) {
        const char* node = queue[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        queue = _slice_str(queue, queue_len, 1LL, queue_len, &(_slice_str_len));
        queue_len = _slice_str_len;
        if (strcmp(node, target) == 0) {
            if ((map_get_si(dist.keys, dist.vals, dist.len, target) == -1LL) || (map_get_si(dist.keys, dist.vals, dist.len, node) < map_get_si(dist.keys, dist.vals, dist.len, target))) {
                map_set_si(&dist, target, map_get_si(dist.keys, dist.vals, dist.len, node));
            }
        }
        const char* *adj = map_get_sl(graph.keys, graph.vals, graph.lens, graph.len, node, NULL);
        size_t adj_len = map_len_sl(graph.keys, graph.lens, graph.len, node);
        long long i = 0LL;
        while (i < adj_len) {
            const char* next = adj[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? adj_len + _mochi_idx : _mochi_idx;})];
            if (!(contains(visited, visited_len, next))) {
                visited = list_append_str(visited, &visited_len, next);
                queue = list_append_str(queue, &queue_len, next);
                map_set_si(&dist, next, map_get_si(dist.keys, dist.vals, dist.len, node) + 1LL);
            }
            i = i + 1LL;
        }
    }
    return map_get_si(dist.keys, dist.vals, dist.len, target);
}

int main(void) {
    {
        long long __start = _now();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
