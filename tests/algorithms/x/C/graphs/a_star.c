// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct SearchResult SearchResult;

struct SearchResult {
    long long **path;
    size_t path_len;
    size_t *path_lens;
    size_t path_lens_len;
    long long **action;
    size_t action_len;
    size_t *action_lens;
    size_t action_lens_len;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long **DIRECTIONS = NULL;
size_t DIRECTIONS_len = 0;
size_t *DIRECTIONS_lens = NULL;
size_t DIRECTIONS_lens_len = 0;

long long iabs(long long x);
SearchResult search(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long * init, size_t init_len, long long * goal, size_t goal_len, long long cost, long long * * heuristic, size_t heuristic_len, size_t* heuristic_lens, size_t heuristic_lens_len);
void user_main();
int main(void);

long long iabs(long long x) {
    if (x < 0LL) {
        return -(x);
    }
    return x;
}

SearchResult search(long long * * grid, size_t grid_len, size_t* grid_lens, size_t grid_lens_len, long long * init, size_t init_len, long long * goal, size_t goal_len, long long cost, long long * * heuristic, size_t heuristic_len, size_t* heuristic_lens, size_t heuristic_lens_len) {
    long long **closed = NULL;
    size_t closed_len = 0;
    size_t *closed_lens = NULL;
    size_t closed_lens_len = 0;
    long long r = 0LL;
    while (r < grid_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long c = 0LL;
        while (c < grid_lens[(int)(0LL)]) {
            row = list_append_long_long(row, &row_len, 0LL);
            c = c + 1LL;
        }
        closed = list_append_intptr(closed, &closed_len, row);
        closed_lens = list_append_szt(closed_lens, &closed_lens_len, row_len);
        r = r + 1LL;
    }
    closed[(int)(init[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? init_len + _mochi_idx : _mochi_idx;})])][(int)(init[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? init_len + _mochi_idx : _mochi_idx;})])] = 1LL;
    long long **action = NULL;
    size_t action_len = 0;
    size_t *action_lens = NULL;
    size_t action_lens_len = 0;
    r = 0LL;
    while (r < grid_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long c = 0LL;
        while (c < grid_lens[(int)(0LL)]) {
            row = list_append_long_long(row, &row_len, 0LL);
            c = c + 1LL;
        }
        action = list_append_intptr(action, &action_len, row);
        action_lens = list_append_szt(action_lens, &action_lens_len, row_len);
        r = r + 1LL;
    }
    long long x = init[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? init_len + _mochi_idx : _mochi_idx;})];
    long long y = init[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? init_len + _mochi_idx : _mochi_idx;})];
    long long g = 0LL;
    long long f = g + heuristic[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? heuristic_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? heuristic_lens[(int)(x)] + _mochi_idx : _mochi_idx;})];
    long long cell_0[4] = {f, g, x, y};
    long long *cell_init[1] = {cell_0};
    long long **cell = cell_init;
    size_t cell_len = 1;
    size_t cell_lens_init[1] = {4};
    size_t *cell_lens = cell_lens_init;
    size_t cell_lens_len = 1;
    long long found = 0LL;
    long long resign = 0LL;
    while (!(found) && !(resign)) {
        if (cell_len == 0LL) {
            panic("Algorithm is unable to find solution");
        } else {
            long long best_i = 0LL;
            long long best_f = cell[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? cell_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? cell_lens[(int)(0LL)] + _mochi_idx : _mochi_idx;})];
            long long i = 1LL;
            while (i < cell_len) {
                if (cell[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cell_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? cell_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] < best_f) {
                    best_f = cell[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cell_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? cell_lens[(int)(i)] + _mochi_idx : _mochi_idx;})];
                    best_i = i;
                }
                i = i + 1LL;
            }
            long long *next_cell = cell[(int)({long long _mochi_idx = best_i; _mochi_idx < 0 ? cell_len + _mochi_idx : _mochi_idx;})];
            size_t next_cell_len = cell_lens[(int)(best_i)];
            long long **new_cell = NULL;
            size_t new_cell_len = 0;
            size_t *new_cell_lens = NULL;
            size_t new_cell_lens_len = 0;
            i = 0LL;
            while (i < cell_len) {
                if (i != best_i) {
                    new_cell = list_append_intptr(new_cell, &new_cell_len, cell[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? cell_len + _mochi_idx : _mochi_idx;})]);
                    new_cell_lens = list_append_szt(new_cell_lens, &new_cell_lens_len, cell_lens[(int)(i)]);
                }
                i = i + 1LL;
            }
            cell = new_cell;
            cell_len = new_cell_len;
            cell_lens = new_cell_lens;
            cell_lens_len = new_cell_lens_len;
            x = next_cell[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? next_cell_len + _mochi_idx : _mochi_idx;})];
            y = next_cell[(int)({long long _mochi_idx = 3LL; _mochi_idx < 0 ? next_cell_len + _mochi_idx : _mochi_idx;})];
            g = next_cell[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? next_cell_len + _mochi_idx : _mochi_idx;})];
            if ((x == goal[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? goal_len + _mochi_idx : _mochi_idx;})]) && (y == goal[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? goal_len + _mochi_idx : _mochi_idx;})])) {
                found = 1LL;
            } else {
                long long d = 0LL;
                while (d < DIRECTIONS_len) {
                    long long x2 = x + DIRECTIONS[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? DIRECTIONS_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? DIRECTIONS_lens[(int)(d)] + _mochi_idx : _mochi_idx;})];
                    long long y2_ = y + DIRECTIONS[(int)({long long _mochi_idx = d; _mochi_idx < 0 ? DIRECTIONS_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? DIRECTIONS_lens[(int)(d)] + _mochi_idx : _mochi_idx;})];
                    if ((((((x2 >= 0LL) && (x2 < grid_len)) && (y2_ >= 0LL)) && (y2_ < grid_lens[(int)(0LL)])) && (closed[(int)({long long _mochi_idx = x2; _mochi_idx < 0 ? closed_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y2_; _mochi_idx < 0 ? closed_lens[(int)(x2)] + _mochi_idx : _mochi_idx;})] == 0LL)) && (grid[(int)({long long _mochi_idx = x2; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y2_; _mochi_idx < 0 ? grid_lens[(int)(x2)] + _mochi_idx : _mochi_idx;})] == 0LL)) {
                        long long g2 = g + cost;
                        long long f2 = g2 + heuristic[(int)({long long _mochi_idx = x2; _mochi_idx < 0 ? heuristic_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y2_; _mochi_idx < 0 ? heuristic_lens[(int)(x2)] + _mochi_idx : _mochi_idx;})];
                        cell = list_append_intptr(cell, &cell_len, ({long long *tmp = malloc(4 * sizeof(long long)); tmp[0] = f2; tmp[1] = g2; tmp[2] = x2; tmp[3] = y2_; tmp;}));
                        cell_lens = list_append_szt(cell_lens, &cell_lens_len, 4);
                        closed[(int)(x2)][(int)(y2_)] = 1LL;
                        action[(int)(x2)][(int)(y2_)] = d;
                    }
                    d = d + 1LL;
                }
            }
        }
    }
    long long **invpath = NULL;
    size_t invpath_len = 0;
    size_t *invpath_lens = NULL;
    size_t invpath_lens_len = 0;
    x = goal[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? goal_len + _mochi_idx : _mochi_idx;})];
    y = goal[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? goal_len + _mochi_idx : _mochi_idx;})];
    invpath = list_append_intptr(invpath, &invpath_len, ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = x; tmp[1] = y; tmp;}));
    invpath_lens = list_append_szt(invpath_lens, &invpath_lens_len, 2);
    while ((x != init[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? init_len + _mochi_idx : _mochi_idx;})]) || (y != init[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? init_len + _mochi_idx : _mochi_idx;})])) {
        long long dir = action[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? action_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = y; _mochi_idx < 0 ? action_lens[(int)(x)] + _mochi_idx : _mochi_idx;})];
        long long x2 = x - DIRECTIONS[(int)({long long _mochi_idx = dir; _mochi_idx < 0 ? DIRECTIONS_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? DIRECTIONS_lens[(int)(dir)] + _mochi_idx : _mochi_idx;})];
        long long y2_ = y - DIRECTIONS[(int)({long long _mochi_idx = dir; _mochi_idx < 0 ? DIRECTIONS_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? DIRECTIONS_lens[(int)(dir)] + _mochi_idx : _mochi_idx;})];
        x = x2;
        y = y2_;
        invpath = list_append_intptr(invpath, &invpath_len, ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = x; tmp[1] = y; tmp;}));
        invpath_lens = list_append_szt(invpath_lens, &invpath_lens_len, 2);
    }
    long long **path = NULL;
    size_t path_len = 0;
    size_t *path_lens = NULL;
    size_t path_lens_len = 0;
    long long idx = invpath_len - 1LL;
    while (idx >= 0LL) {
        path = list_append_intptr(path, &path_len, invpath[(int)({long long _mochi_idx = idx; _mochi_idx < 0 ? invpath_len + _mochi_idx : _mochi_idx;})]);
        path_lens = list_append_szt(path_lens, &path_lens_len, invpath_lens[(int)(idx)]);
        idx = idx - 1LL;
    }
    return (SearchResult){.path = path, .path_len = path_len, .path_lens = path_lens, .path_lens_len = path_len, .action = action, .action_len = action_len, .action_lens = action_lens, .action_lens_len = action_len};
}

void user_main() {
    long long grid_0[6] = {0LL, 1LL, 0LL, 0LL, 0LL, 0LL};
    long long grid_1[6] = {0LL, 1LL, 0LL, 0LL, 0LL, 0LL};
    long long grid_2[6] = {0LL, 1LL, 0LL, 0LL, 0LL, 0LL};
    long long grid_3[6] = {0LL, 1LL, 0LL, 0LL, 1LL, 0LL};
    long long grid_4[6] = {0LL, 0LL, 0LL, 0LL, 1LL, 0LL};
    long long *grid_init[5] = {grid_0, grid_1, grid_2, grid_3, grid_4};
    long long **grid = grid_init;
    size_t grid_len = 5;
    size_t grid_lens_init[5] = {6, 6, 6, 6, 6};
    size_t *grid_lens = grid_lens_init;
    size_t grid_lens_len = 5;
    long long *init = NULL;
    size_t init_len = 0;
    init = list_append_long_long(init, &init_len, 0LL);
    init = list_append_long_long(init, &init_len, 0LL);
    long long *goal = NULL;
    size_t goal_len = 0;
    goal = list_append_long_long(goal, &goal_len, grid_len - 1LL);
    goal = list_append_long_long(goal, &goal_len, grid_lens[(int)(0LL)] - 1LL);
    long long cost = 1LL;
    long long **heuristic = NULL;
    size_t heuristic_len = 0;
    size_t *heuristic_lens = NULL;
    size_t heuristic_lens_len = 0;
    long long i = 0LL;
    while (i < grid_len) {
        long long *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < grid_lens[(int)(0LL)]) {
            long long h = iabs(i - goal[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? goal_len + _mochi_idx : _mochi_idx;})]) + iabs(j - goal[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? goal_len + _mochi_idx : _mochi_idx;})]);
            if (grid[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? grid_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? grid_lens[(int)(i)] + _mochi_idx : _mochi_idx;})] == 1LL) {
                row = list_append_long_long(row, &row_len, 99LL);
            } else {
                row = list_append_long_long(row, &row_len, h);
            }
            j = j + 1LL;
        }
        heuristic = list_append_intptr(heuristic, &heuristic_len, row);
        heuristic_lens = list_append_szt(heuristic_lens, &heuristic_lens_len, row_len);
        i = i + 1LL;
    }
    SearchResult result = search(grid, grid_len, grid_lens, grid_len, init, init_len, goal, goal_len, cost, heuristic, heuristic_len, heuristic_lens, heuristic_len);
    puts("ACTION MAP");
    long long rr = 0LL;
    while (rr < result.action_len) {
        puts(str_list_int(result.action[(int)({long long _mochi_idx = rr; _mochi_idx < 0 ? result.action_len + _mochi_idx : _mochi_idx;})], result.action_lens[rr]));
        rr = rr + 1LL;
    }
    long long p = 0LL;
    while (p < result.path_len) {
        puts(str_list_int(result.path[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? result.path_len + _mochi_idx : _mochi_idx;})], result.path_lens[p]));
        p = p + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        DIRECTIONS = malloc(4 * sizeof(long long*));
        DIRECTIONS_len = 4;
        DIRECTIONS[0] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = -1LL; tmp[1] = 0LL; tmp;});
        DIRECTIONS[1] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = -1LL; tmp;});
        DIRECTIONS[2] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 1LL; tmp[1] = 0LL; tmp;});
        DIRECTIONS[3] = ({long long *tmp = malloc(2 * sizeof(long long)); tmp[0] = 0LL; tmp[1] = 1LL; tmp;});
        DIRECTIONS_lens = ({size_t *tmp = malloc(4 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 2; tmp[3] = 2; tmp;});
        DIRECTIONS_lens_len = 4;
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
