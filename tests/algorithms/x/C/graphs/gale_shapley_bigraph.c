// Generated by Mochi 0.10.32 on 2025-08-15 19:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t remove_item_len;
size_t stable_matching_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long donor_pref_0[4] = {0LL, 1LL, 3LL, 2LL};
long long donor_pref_1[4] = {0LL, 2LL, 3LL, 1LL};
long long donor_pref_2[4] = {1LL, 0LL, 2LL, 3LL};
long long donor_pref_3[4] = {0LL, 3LL, 1LL, 2LL};
long long *donor_pref_init[4] = {donor_pref_0, donor_pref_1, donor_pref_2, donor_pref_3};
long long **donor_pref = donor_pref_init;
size_t donor_pref_len = 4;
size_t donor_pref_lens_init[4] = {4, 4, 4, 4};
size_t *donor_pref_lens = donor_pref_lens_init;
size_t donor_pref_lens_len = 4;
long long recipient_pref_0[4] = {3LL, 1LL, 2LL, 0LL};
long long recipient_pref_1[4] = {3LL, 1LL, 0LL, 2LL};
long long recipient_pref_2[4] = {0LL, 3LL, 1LL, 2LL};
long long recipient_pref_3[4] = {1LL, 0LL, 3LL, 2LL};
long long *recipient_pref_init[4] = {recipient_pref_0, recipient_pref_1, recipient_pref_2, recipient_pref_3};
long long **recipient_pref = recipient_pref_init;
size_t recipient_pref_len = 4;
size_t recipient_pref_lens_init[4] = {4, 4, 4, 4};
size_t *recipient_pref_lens = recipient_pref_lens_init;
size_t recipient_pref_lens_len = 4;

long long index_of(long long * xs, size_t xs_len, long long x);
long long * remove_item(long long * xs, size_t xs_len, long long x);
long long * stable_matching(long long * * donor_pref, size_t donor_pref_len, size_t* donor_pref_lens, size_t donor_pref_lens_len, long long * * recipient_pref, size_t recipient_pref_len, size_t* recipient_pref_lens, size_t recipient_pref_lens_len);
int main(void);

long long index_of(long long * xs, size_t xs_len, long long x) {
    long long i = 0LL;
    while (i < xs_len) {
        if (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == x) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long * remove_item(long long * xs, size_t xs_len, long long x) {
    long long *res = NULL;
    size_t res_len = 0;
    long long removed = 0LL;
    long long i = 0LL;
    while (i < xs_len) {
        if (!(removed) && (xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})] == x)) {
            removed = 1LL;
        } else {
            res = list_append_long_long(res, &res_len, xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]);
        }
        i = i + 1LL;
    }
    return remove_item_len = res_len, res;
}

long long * stable_matching(long long * * donor_pref, size_t donor_pref_len, size_t* donor_pref_lens, size_t donor_pref_lens_len, long long * * recipient_pref, size_t recipient_pref_len, size_t* recipient_pref_lens, size_t recipient_pref_lens_len) {
    if (donor_pref_len != recipient_pref_len) {
        panic("unequal groups");
    }
    long long n = donor_pref_len;
    long long *unmatched = NULL;
    size_t unmatched_len = 0;
    long long i = 0LL;
    while (i < n) {
        unmatched = list_append_long_long(unmatched, &unmatched_len, i);
        i = i + 1LL;
    }
    long long *donor_record = NULL;
    size_t donor_record_len = 0;
    i = 0LL;
    while (i < n) {
        donor_record = list_append_long_long(donor_record, &donor_record_len, -1LL);
        i = i + 1LL;
    }
    long long *rec_record = NULL;
    size_t rec_record_len = 0;
    i = 0LL;
    while (i < n) {
        rec_record = list_append_long_long(rec_record, &rec_record_len, -1LL);
        i = i + 1LL;
    }
    long long *num_donations = NULL;
    size_t num_donations_len = 0;
    i = 0LL;
    while (i < n) {
        num_donations = list_append_long_long(num_donations, &num_donations_len, 0LL);
        i = i + 1LL;
    }
    while (unmatched_len > 0LL) {
        long long donor = unmatched[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? unmatched_len + _mochi_idx : _mochi_idx;})];
        long long *donor_preference = donor_pref[(int)({long long _mochi_idx = donor; _mochi_idx < 0 ? donor_pref_len + _mochi_idx : _mochi_idx;})];
        size_t donor_preference_len = donor_pref_lens[donor];
        long long recipient = donor_preference[(int)({long long _mochi_idx = num_donations[(int)({long long _mochi_idx = donor; _mochi_idx < 0 ? num_donations_len + _mochi_idx : _mochi_idx;})]; _mochi_idx < 0 ? donor_preference_len + _mochi_idx : _mochi_idx;})];
        num_donations[(int)(donor)] = num_donations[(int)({long long _mochi_idx = donor; _mochi_idx < 0 ? num_donations_len + _mochi_idx : _mochi_idx;})] + 1LL;
        long long *rec_preference = recipient_pref[(int)({long long _mochi_idx = recipient; _mochi_idx < 0 ? recipient_pref_len + _mochi_idx : _mochi_idx;})];
        size_t rec_preference_len = recipient_pref_lens[recipient];
        long long prev_donor = rec_record[(int)({long long _mochi_idx = recipient; _mochi_idx < 0 ? rec_record_len + _mochi_idx : _mochi_idx;})];
        if (prev_donor != (0LL - 1LL)) {
            long long prev_index = index_of(rec_preference, rec_preference_len, prev_donor);
            long long new_index = index_of(rec_preference, rec_preference_len, donor);
            if (prev_index > new_index) {
                rec_record[(int)(recipient)] = donor;
                donor_record[(int)(donor)] = recipient;
                unmatched = list_append_long_long(unmatched, &unmatched_len, prev_donor);
                unmatched = remove_item(unmatched, unmatched_len, donor);
                unmatched_len = remove_item_len;
            }
        } else {
            rec_record[(int)(recipient)] = donor;
            donor_record[(int)(donor)] = recipient;
            unmatched = remove_item(unmatched, unmatched_len, donor);
            unmatched_len = remove_item_len;
        }
    }
    return stable_matching_len = donor_record_len, donor_record;
}

int main(void) {
    {
        long long __start = _now();
        puts(({long long *__tmp1 = stable_matching(donor_pref, donor_pref_len, donor_pref_lens, donor_pref_len, recipient_pref, recipient_pref_len, recipient_pref_lens, recipient_pref_len); char *__res = str_list_int(__tmp1, stable_matching_len); __res;}));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
