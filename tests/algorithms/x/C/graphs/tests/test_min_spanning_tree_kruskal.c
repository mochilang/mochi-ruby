// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sort_edges_len;
size_t *sort_edges_lens;
size_t kruskal_len;
size_t *kruskal_lens;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_int(const long long *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%lld", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static char* str_list_list_int(long long **arr, size_t len, const size_t lens[]) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += lens[i] * 32 + 3;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char *inner = str_list_int(arr[i], lens[i]);
        size_t n = strlen(inner);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, inner, n);
        pos += n;
        free(inner);
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * * sort_edges(long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len);
long long find(long long * parent, size_t parent_len, long long x);
long long * * kruskal(long long n, long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len);
long long edges_equal(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len);
void user_main();
int main(void);

long long * * sort_edges(long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len) {
    long long **es = edges;
    size_t es_len = edges_len;
    size_t *es_lens = edges_lens;
    size_t es_lens_len = edges_len;
    long long i = 0LL;
    while (i < es_len) {
        long long j = 0LL;
        while (j < ((es_len - i) - 1LL)) {
            if (es[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? es_lens[j] + _mochi_idx : _mochi_idx;})] > es[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? es_lens[j + 1LL] + _mochi_idx : _mochi_idx;})]) {
                long long *tmp = es[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})];
                size_t tmp_len = es_lens[j];
                es[(int)(j)] = es[(int)({long long _mochi_idx = j + 1LL; _mochi_idx < 0 ? es_len + _mochi_idx : _mochi_idx;})];
                es[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sort_edges_lens = es_lens, sort_edges_len = es_len, es;
}

long long find(long long * parent, size_t parent_len, long long x) {
    long long r = x;
    while (parent[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})] != r) {
        r = parent[(int)({long long _mochi_idx = r; _mochi_idx < 0 ? parent_len + _mochi_idx : _mochi_idx;})];
    }
    return r;
}

long long * * kruskal(long long n, long long * * edges, size_t edges_len, size_t* edges_lens, size_t edges_lens_len) {
    long long *parent = NULL;
    size_t parent_len = 0;
    long long i = 0LL;
    while (i < n) {
        parent = list_append_long_long(parent, &parent_len, i);
        i = i + 1LL;
    }
    long long **sorted = sort_edges(edges, edges_len, edges_lens, edges_len);
    size_t sorted_len = sort_edges_len;
    size_t *sorted_lens = sort_edges_lens;
    size_t sorted_lens_len = sort_edges_len;
    long long **mst = NULL;
    size_t mst_len = 0;
    size_t *mst_lens = NULL;
    size_t mst_lens_len = 0;
    long long e = 0LL;
    while (e < sorted_len) {
        if (mst_len == (n - 1LL)) {
            break;
        }
        long long *edge = sorted[(int)({long long _mochi_idx = e; _mochi_idx < 0 ? sorted_len + _mochi_idx : _mochi_idx;})];
        size_t edge_len = sorted_lens[e];
        e = e + 1LL;
        long long u = edge[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? edge_len + _mochi_idx : _mochi_idx;})];
        long long v = edge[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? edge_len + _mochi_idx : _mochi_idx;})];
        long long w = edge[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? edge_len + _mochi_idx : _mochi_idx;})];
        long long ru = find(parent, parent_len, u);
        long long rv = find(parent, parent_len, v);
        if (ru != rv) {
            parent[(int)(ru)] = rv;
            mst = list_append_intptr(mst, &mst_len, ({long long* tmp = malloc(3 * sizeof(long long)); tmp[0] = u; tmp[1] = v; tmp[2] = w; tmp;}));
            mst_lens = list_append_szt(mst_lens, &mst_lens_len, 3);
        }
    }
    return kruskal_lens = mst_lens, kruskal_len = mst_len, mst;
}

long long edges_equal(long long * * a, size_t a_len, size_t* a_lens, size_t a_lens_len, long long * * b, size_t b_len, size_t* b_lens, size_t b_lens_len) {
    if (a_len != b_len) {
        return 0LL;
    }
    long long i = 0LL;
    while (i < a_len) {
        long long *e1 = a[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? a_len + _mochi_idx : _mochi_idx;})];
        size_t e1_len = a_lens[i];
        long long *e2 = b[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? b_len + _mochi_idx : _mochi_idx;})];
        size_t e2_len = b_lens[i];
        if (((e1[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e1_len + _mochi_idx : _mochi_idx;})] != e2[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? e2_len + _mochi_idx : _mochi_idx;})]) || (e1[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? e1_len + _mochi_idx : _mochi_idx;})] != e2[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? e2_len + _mochi_idx : _mochi_idx;})])) || (e1[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? e1_len + _mochi_idx : _mochi_idx;})] != e2[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? e2_len + _mochi_idx : _mochi_idx;})])) {
            return 0LL;
        }
        i = i + 1LL;
    }
    return 1LL;
}

void user_main() {
    long long num_nodes = 9LL;
    long long edges_0[3] = {0LL, 1LL, 4LL};
    long long edges_1[3] = {0LL, 7LL, 8LL};
    long long edges_2[3] = {1LL, 2LL, 8LL};
    long long edges_3[3] = {7LL, 8LL, 7LL};
    long long edges_4[3] = {7LL, 6LL, 1LL};
    long long edges_5[3] = {2LL, 8LL, 2LL};
    long long edges_6[3] = {8LL, 6LL, 6LL};
    long long edges_7[3] = {2LL, 3LL, 7LL};
    long long edges_8[3] = {2LL, 5LL, 4LL};
    long long edges_9[3] = {6LL, 5LL, 2LL};
    long long edges_10[3] = {3LL, 5LL, 14LL};
    long long edges_11[3] = {3LL, 4LL, 9LL};
    long long edges_12[3] = {5LL, 4LL, 10LL};
    long long edges_13[3] = {1LL, 7LL, 11LL};
    long long *edges_init[14] = {edges_0, edges_1, edges_2, edges_3, edges_4, edges_5, edges_6, edges_7, edges_8, edges_9, edges_10, edges_11, edges_12, edges_13};
    long long **edges = edges_init;
    size_t edges_len = 14;
    size_t edges_lens_init[14] = {3, 3, 3, 3, 3, 3, 3, 3, 3, 3, 3, 3, 3, 3};
    size_t *edges_lens = edges_lens_init;
    size_t edges_lens_len = 14;
    long long expected_0[3] = {7LL, 6LL, 1LL};
    long long expected_1[3] = {2LL, 8LL, 2LL};
    long long expected_2[3] = {6LL, 5LL, 2LL};
    long long expected_3[3] = {0LL, 1LL, 4LL};
    long long expected_4[3] = {2LL, 5LL, 4LL};
    long long expected_5[3] = {2LL, 3LL, 7LL};
    long long expected_6[3] = {0LL, 7LL, 8LL};
    long long expected_7[3] = {3LL, 4LL, 9LL};
    long long *expected_init[8] = {expected_0, expected_1, expected_2, expected_3, expected_4, expected_5, expected_6, expected_7};
    long long **expected = expected_init;
    size_t expected_len = 8;
    size_t expected_lens_init[8] = {3, 3, 3, 3, 3, 3, 3, 3};
    size_t *expected_lens = expected_lens_init;
    size_t expected_lens_len = 8;
    long long **result = kruskal(num_nodes, edges, edges_len, edges_lens, edges_len);
    size_t result_len = kruskal_len;
    size_t *result_lens = kruskal_lens;
    size_t result_lens_len = kruskal_len;
    long long **sorted_result = sort_edges(result, result_len, result_lens, result_len);
    size_t sorted_result_len = sort_edges_len;
    size_t *sorted_result_lens = sort_edges_lens;
    size_t sorted_result_lens_len = sort_edges_len;
    long long **sorted_expected = sort_edges(expected, expected_len, expected_lens, expected_len);
    size_t sorted_expected_len = sort_edges_len;
    size_t *sorted_expected_lens = sort_edges_lens;
    size_t sorted_expected_lens_len = sort_edges_len;
    puts(str_list_list_int(sorted_result, sorted_result_len, sorted_result_lens));
    if (edges_equal(sorted_expected, sorted_expected_len, sorted_expected_lens, sorted_expected_len, sorted_result, sorted_result_len, sorted_result_lens, sorted_result_len)) {
        printf("%d\n", 1LL);
    } else {
        printf("%d\n", 0LL);
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
