// Generated by Mochi 0.10.32 on 2025-08-25 21:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t bellman_ford_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%f", v);
    char *p = buf + strlen(buf) - 1;
    while (p > buf && *p == '0') *p-- = '\0';
    if (p > buf && *p == '.') *p = '\0';
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Edge Edge;

struct Edge {
    long long src;
    long long dst;
    long long weight;
};

double INF = 1e+09;
Edge *edges = NULL;
size_t edges_len = 0;
double *distances = NULL;
size_t distances_len = 0;

const char* list_to_string(double * arr, size_t arr_len);
long long check_negative_cycle(Edge * graph, size_t graph_len, double * distance, size_t distance_len, long long edge_count);
double * bellman_ford(Edge * graph, size_t graph_len, long long vertex_count, long long edge_count, long long src);
int main(void);

const char* list_to_string(double * arr, size_t arr_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < arr_len) {
        s = str_concat(s, str_float(arr[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? arr_len + _mochi_idx : _mochi_idx;})]));
        if (i < (arr_len - 1LL)) {
            s = str_concat(s, ", ");
        }
        i = i + 1LL;
    }
    return str_concat(s, "]");
}

long long check_negative_cycle(Edge * graph, size_t graph_len, double * distance, size_t distance_len, long long edge_count) {
    long long j = 0LL;
    while (j < edge_count) {
        Edge e = graph[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
        long long u = e.src;
        long long v = e.dst;
        double w = (double)(e.weight);
        if ((distance[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})] < INF) && ((distance[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})] + w) < distance[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})])) {
            return 1LL;
        }
        j = j + 1LL;
    }
    return 0LL;
}

double * bellman_ford(Edge * graph, size_t graph_len, long long vertex_count, long long edge_count, long long src) {
    double *distance = NULL;
    size_t distance_len = 0;
    long long i = 0LL;
    while (i < vertex_count) {
        distance = list_append_double(distance, &distance_len, INF);
        i = i + 1LL;
    }
    distance[(int)(src)] = 0.0;
    long long k = 0LL;
    while (k < (vertex_count - 1LL)) {
        long long j = 0LL;
        while (j < edge_count) {
            Edge e = graph[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
            long long u = e.src;
            long long v = e.dst;
            double w = (double)(e.weight);
            if ((distance[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})] < INF) && ((distance[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})] + w) < distance[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})])) {
                distance[(int)(v)] = distance[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? distance_len + _mochi_idx : _mochi_idx;})] + w;
            }
            j = j + 1LL;
        }
        k = k + 1LL;
    }
    if (check_negative_cycle(graph, graph_len, distance, distance_len, edge_count)) {
        panic("Negative cycle found");
    }
    return bellman_ford_len = distance_len, distance;
}

int main(void) {
    {
        long long __start = _now();
        edges = malloc(4 * sizeof(Edge));
        edges_len = 4;
        edges[0] = (Edge){.src = 2LL, .dst = 1LL, .weight = -10LL};
        edges[1] = (Edge){.src = 3LL, .dst = 2LL, .weight = 3LL};
        edges[2] = (Edge){.src = 0LL, .dst = 3LL, .weight = 5LL};
        edges[3] = (Edge){.src = 0LL, .dst = 1LL, .weight = 4LL};
        distances = bellman_ford(edges, edges_len, 4LL, 4LL, 0LL);
        distances_len = bellman_ford_len;
        puts(list_to_string(distances, distances_len));
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
