// Generated by Mochi 0.10.32 on 2025-08-14 09:53 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t make_int_list_len;
size_t make_bool_list_len;
size_t dijkstra_len;

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Edge Edge;

struct Edge {
    long long node;
    long long weight;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Edge **graph = NULL;
size_t graph_len = 0;
size_t *graph_lens = NULL;
size_t graph_lens_len = 0;
long long *dist = NULL;
size_t dist_len = 0;

long long * make_int_list(long long n, long long value);
long long * make_bool_list(long long n);
long long * dijkstra(Edge * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long src);
int main(void);

long long * make_int_list(long long n, long long value) {
    long long *lst = NULL;
    size_t lst_len = 0;
    long long i = 0LL;
    while (i < n) {
        lst = list_append_long_long(lst, &lst_len, value);
        i = i + 1LL;
    }
    return make_int_list_len = lst_len, lst;
}

long long * make_bool_list(long long n) {
    long long *lst = NULL;
    size_t lst_len = 0;
    long long i = 0LL;
    while (i < n) {
        lst = list_append_long_long(lst, &lst_len, 0LL);
        i = i + 1LL;
    }
    return make_bool_list_len = lst_len, lst;
}

long long * dijkstra(Edge * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long src) {
    long long n = graph_len;
    long long *dist = make_int_list(n, 1000000000LL);
    size_t dist_len = make_int_list_len;
    long long *visited = make_bool_list(n);
    size_t visited_len = make_bool_list_len;
    dist[(int)(src)] = 0LL;
    long long count = 0LL;
    while (count < n) {
        long long u = -1.0;
        long long min_dist = 1000000000LL;
        long long i = 0LL;
        while (i < n) {
            if (!(visited[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})]) && (dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})] < min_dist)) {
                min_dist = dist[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})];
                u = i;
            }
            i = i + 1LL;
        }
        if (u < 0LL) {
            break;
        }
        visited[(int)(u)] = 1LL;
        long long j = 0LL;
        while (j < graph_lens[u]) {
            Edge e = graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = j; _mochi_idx < 0 ? graph_lens[u] + _mochi_idx : _mochi_idx;})];
            long long v = e.node;
            long long w = e.weight;
            if (!(visited[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
                long long new_dist = dist[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})] + w;
                if (new_dist < dist[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]) {
                    dist[(int)(v)] = new_dist;
                }
            }
            j = j + 1LL;
        }
        count = count + 1LL;
    }
    return dijkstra_len = dist_len, dist;
}

int main(void) {
    {
        long long __start = _now();
        graph = ({Edge** tmp = malloc(5 * sizeof(Edge*)); tmp[0] = ({Edge *tmp = malloc(2 * sizeof(Edge)); tmp[0] = (Edge){.node = 1LL, .weight = 10LL}; tmp[1] = (Edge){.node = 3LL, .weight = 5LL}; tmp;}); tmp[1] = ({Edge *tmp = malloc(2 * sizeof(Edge)); tmp[0] = (Edge){.node = 2LL, .weight = 1LL}; tmp[1] = (Edge){.node = 3LL, .weight = 2LL}; tmp;}); tmp[2] = ({Edge *tmp = malloc(1 * sizeof(Edge)); tmp[0] = (Edge){.node = 4LL, .weight = 4LL}; tmp;}); tmp[3] = ({Edge *tmp = malloc(3 * sizeof(Edge)); tmp[0] = (Edge){.node = 1LL, .weight = 3LL}; tmp[1] = (Edge){.node = 2LL, .weight = 9LL}; tmp[2] = (Edge){.node = 4LL, .weight = 2LL}; tmp;}); tmp[4] = ({Edge *tmp = malloc(2 * sizeof(Edge)); tmp[0] = (Edge){.node = 0LL, .weight = 7LL}; tmp[1] = (Edge){.node = 2LL, .weight = 6LL}; tmp;}); tmp;});
        graph_len = 5;
        graph_lens = ({size_t *tmp = malloc(5 * sizeof(size_t)); tmp[0] = 2; tmp[1] = 2; tmp[2] = 1; tmp[3] = 3; tmp[4] = 2; tmp;});
        graph_lens_len = 5;
        dist = dijkstra(graph, graph_len, graph_lens, graph_len, 0LL);
        dist_len = dijkstra_len;
        puts(str_int(dist[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]));
        puts(str_int(dist[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]));
        puts(str_int(dist[(int)({long long _mochi_idx = 2LL; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]));
        puts(str_int(dist[(int)({long long _mochi_idx = 3LL; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]));
        puts(str_int(dist[(int)({long long _mochi_idx = 4LL; _mochi_idx < 0 ? dist_len + _mochi_idx : _mochi_idx;})]));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
