// Generated by Mochi 0.10.32 on 2025-08-14 17:21 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t push_front_len;
size_t pop_front_len;

size_t append_len;
static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long** list_append_intptr(long long **arr, size_t *len, long long *val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct AdjacencyList AdjacencyList;
typedef struct Edge Edge;

struct AdjacencyList {
    Edge **graph;
    size_t graph_len;
    size_t *graph_lens;
    size_t graph_lens_len;
    long long size;
};

struct Edge {
    long long destination_vertex;
    long long weight;
};

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Edge** list_append_Edgeptr(Edge **arr, size_t *len, Edge *val) {
    arr = realloc(arr, (*len + 1) * sizeof(Edge*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Edge* list_append_Edge_new(const Edge *arr, size_t len, Edge val) {
    Edge *res = malloc((len + 1) * sizeof(Edge));
    if (arr && len) memcpy(res, arr, len * sizeof(Edge));
    res[len] = val;
    return res;
}

AdjacencyList g;

AdjacencyList new_adjacency_list(long long size);
void add_edge(AdjacencyList* al, long long from_vertex, long long to_vertex, long long weight);
long long * push_front(long long * q, size_t q_len, long long v);
long long * pop_front(long long * q, size_t q_len);
long long front(long long * q, size_t q_len);
long long get_shortest_path(AdjacencyList al, long long start_vertex, long long finish_vertex);
int main(void);

AdjacencyList new_adjacency_list(long long size) {
    Edge **g = NULL;
    size_t g_len = 0;
    size_t *g_lens = NULL;
    size_t g_lens_len = 0;
    long long i = 0LL;
    while (i < size) {
        g = list_append_Edgeptr(g, &g_len, ({Edge* tmp = malloc(0 * sizeof(Edge)); tmp;}));
        g_lens = list_append_szt(g_lens, &g_lens_len, 0);
        i = i + 1LL;
    }
    return (AdjacencyList){.graph = g, .graph_len = g_len, .graph_lens = g_lens, .graph_lens_len = g_len, .size = size};
}

void add_edge(AdjacencyList* al, long long from_vertex, long long to_vertex, long long weight) {
    if (!((weight == 0LL) || (weight == 1LL))) {
        panic("Edge weight must be either 0 or 1.");
    }
    if ((to_vertex < 0LL) || (to_vertex >= al->size)) {
        panic("Vertex indexes must be in [0; size).");
    }
    Edge **g = al->graph;
    size_t g_len = al->graph_len;
    size_t *g_lens = al->graph_lens;
    size_t g_lens_len = al->graph_len;
    Edge *edges = g[(int)({long long _mochi_idx = from_vertex; _mochi_idx < 0 ? g_len + _mochi_idx : _mochi_idx;})];
    size_t edges_len = g_lens[from_vertex];
    g[(int)(from_vertex)] = list_append_Edge_new(edges, edges_len, (Edge){.destination_vertex = to_vertex, .weight = weight});
    al->graph = g;
}

long long * push_front(long long * q, size_t q_len, long long v) {
    long long *res = NULL;
    size_t res_len = 0;
    res = list_append_long_long(res, &res_len, v);
    long long i = 0LL;
    while (i < q_len) {
        res = list_append_long_long(res, &res_len, q[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return push_front_len = res_len, res;
}

long long * pop_front(long long * q, size_t q_len) {
    long long *res = NULL;
    size_t res_len = 0;
    long long i = 1LL;
    while (i < q_len) {
        res = list_append_long_long(res, &res_len, q[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})]);
        i = i + 1LL;
    }
    return pop_front_len = res_len, res;
}

long long front(long long * q, size_t q_len) {
    return q[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? q_len + _mochi_idx : _mochi_idx;})];
}

long long get_shortest_path(AdjacencyList al, long long start_vertex, long long finish_vertex) {
    long long *queue = NULL;
    size_t queue_len = 0;
    queue = list_append_long_long(queue, &queue_len, start_vertex);
    long long *distances = NULL;
    size_t distances_len = 0;
    long long i = 0LL;
    while (i < al.size) {
        distances = list_append_long_long(distances, &distances_len, -1LL);
        i = i + 1LL;
    }
    distances[(int)(start_vertex)] = 0LL;
    while (queue_len > 0LL) {
        long long current_vertex = front(queue, queue_len);
        queue = pop_front(queue, queue_len);
        queue_len = pop_front_len;
        long long current_distance = distances[(int)({long long _mochi_idx = current_vertex; _mochi_idx < 0 ? distances_len + _mochi_idx : _mochi_idx;})];
        Edge *edges = al.graph[(int)({long long _mochi_idx = current_vertex; _mochi_idx < 0 ? al.graph_len + _mochi_idx : _mochi_idx;})];
        size_t edges_len = al.graph_lens[current_vertex];
        long long j = 0LL;
        while (j < edges_len) {
            Edge edge = edges[(int)({long long _mochi_idx = j; _mochi_idx < 0 ? edges_len + _mochi_idx : _mochi_idx;})];
            long long new_distance = current_distance + edge.weight;
            long long dest = edge.destination_vertex;
            long long dest_distance = distances[(int)({long long _mochi_idx = dest; _mochi_idx < 0 ? distances_len + _mochi_idx : _mochi_idx;})];
            if ((dest_distance >= 0LL) && (new_distance >= dest_distance)) {
                j = j + 1LL;
                continue;
            }
            distances[(int)(dest)] = new_distance;
            if (edge.weight == 0LL) {
                queue = push_front(queue, queue_len, dest);
                queue_len = push_front_len;
            } else {
                queue = list_append_long_long(queue, &queue_len, dest);
            }
            j = j + 1LL;
        }
    }
    long long result = distances[(int)({long long _mochi_idx = finish_vertex; _mochi_idx < 0 ? distances_len + _mochi_idx : _mochi_idx;})];
    if (result < 0LL) {
        panic("No path from start_vertex to finish_vertex.");
    }
    return result;
}

int main(void) {
    {
        long long __start = _now();
        g = new_adjacency_list(11LL);
        add_edge(&g, 0LL, 1LL, 0LL);
        add_edge(&g, 0LL, 3LL, 1LL);
        add_edge(&g, 1LL, 2LL, 0LL);
        add_edge(&g, 2LL, 3LL, 0LL);
        add_edge(&g, 4LL, 2LL, 1LL);
        add_edge(&g, 4LL, 5LL, 1LL);
        add_edge(&g, 4LL, 6LL, 1LL);
        add_edge(&g, 5LL, 9LL, 0LL);
        add_edge(&g, 6LL, 7LL, 1LL);
        add_edge(&g, 7LL, 8LL, 1LL);
        add_edge(&g, 8LL, 10LL, 1LL);
        add_edge(&g, 9LL, 7LL, 0LL);
        add_edge(&g, 9LL, 10LL, 1LL);
        puts(str_int(get_shortest_path(g, 0LL, 3LL)));
        puts(str_int(get_shortest_path(g, 4LL, 10LL)));
        puts(str_int(get_shortest_path(g, 4LL, 8LL)));
        puts(str_int(get_shortest_path(g, 0LL, 1LL)));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
