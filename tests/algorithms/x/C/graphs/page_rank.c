// Generated by Mochi 0.10.32 on 2025-08-16 11:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>


size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; double *vals; size_t len; size_t cap; } MapSD;

static double map_get_sd(const char *keys[], const double vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

typedef struct Node Node;

struct Node {
    const char* name;
    const char* *inbound;
    size_t inbound_len;
    const char* *outbound;
    size_t outbound_len;
};

static Node* list_append_Node(Node *arr, size_t *len, Node val) {
    arr = realloc(arr, (*len + 1) * sizeof(Node));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* names_init[3] = {"A", "B", "C"};
const char* *names = names_init;
size_t names_len = 3;
long long graph_0[3] = {0LL, 1LL, 1LL};
long long graph_1[3] = {0LL, 0LL, 1LL};
long long graph_2[3] = {1LL, 0LL, 0LL};
long long *graph_init[3] = {graph_0, graph_1, graph_2};
long long **graph = graph_init;
size_t graph_len = 3;
size_t graph_lens_init[3] = {3, 3, 3};
size_t *graph_lens = graph_lens_init;
size_t graph_lens_len = 3;
Node *nodes = NULL;
size_t nodes_len = 0;
long long ri = 0LL;

const char* node_to_string(Node n);
MapSD page_rank(Node * nodes, size_t nodes_len, long long limit, double d);
int main(void);

const char* node_to_string(Node n) {
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("<node=", n.name), " inbound="), str_int(n.inbound)), " outbound="), str_int(n.outbound)), ">");
}

MapSD page_rank(Node * nodes, size_t nodes_len, long long limit, double d) {
    static const char* ranks_keys[16] = {};
    static double ranks_vals[16] = {};
    size_t ranks_len = 0;
    for (size_t __i = 0; __i < nodes_len; __i++) {
        Node n = nodes[__i];
        ranks[(int)(n.name)] = 1LL;
    }
    static const char* outbounds_keys[16] = {};
    static double outbounds_vals[16] = {};
    size_t outbounds_len = 0;
    for (size_t __i = 0; __i < nodes_len; __i++) {
        Node n = nodes[__i];
        outbounds[(int)(n.name)] = 1LL * n.outbound_len;
    }
    long long i = 0LL;
    while (i < limit) {
        puts(str_concat(str_concat("======= Iteration ", str_int(i + 1LL)), " ======="));
        for (size_t __i = 0; __i < nodes_len; __i++) {
            Node n = nodes[__i];
            double sum_val = 0LL;
            for (size_t __i = 0; __i < n.inbound_len; __i++) {
                const char* ib = n.inbound[__i];
                sum_val = sum_val + (map_get_sd(ranks_keys, ranks_vals, ranks_len, ib) / map_get_sd(outbounds_keys, outbounds_vals, outbounds_len, ib));
            }
            ranks[(int)(n.name)] = (1LL - d) + (d * sum_val);
        }
        printf("%d\n", ranks);
        i = i + 1LL;
    }
    return ranks;
}

int main(void) {
    {
        long long __start = _now();
        {
            const char* name_arr[] = {"A", "B", "C"};
            size_t name_len = sizeof(name_arr) / sizeof(name_arr[0]);
            for (size_t __i = 0; __i < name_len; __i++) {
                const char* name = name_arr[__i];
                nodes = list_append_Node(nodes, &nodes_len, (Node){.name = name, .inbound = NULL, .inbound_len = 0, .outbound = NULL, .outbound_len = 0});
            }
        }
        while (ri < graph_len) {
            long long *row = graph[(int)({long long _mochi_idx = ri; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
            size_t row_len = graph_lens[ri];
            long long ci = 0LL;
            while (ci < row_len) {
                if (row[(int)({long long _mochi_idx = ci; _mochi_idx < 0 ? row_len + _mochi_idx : _mochi_idx;})] == 1LL) {
                    Node n_in = nodes[(int)({long long _mochi_idx = ci; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
                    n_in.inbound = list_append_str(n_in.inbound, &n_in.inbound_len, names[(int)({long long _mochi_idx = ri; _mochi_idx < 0 ? names_len + _mochi_idx : _mochi_idx;})]);
                    nodes[(int)(ci)] = n_in;
                    Node n_out = nodes[(int)({long long _mochi_idx = ri; _mochi_idx < 0 ? nodes_len + _mochi_idx : _mochi_idx;})];
                    n_out.outbound = list_append_str(n_out.outbound, &n_out.outbound_len, names[(int)({long long _mochi_idx = ci; _mochi_idx < 0 ? names_len + _mochi_idx : _mochi_idx;})]);
                    nodes[(int)(ri)] = n_out;
                }
                ci = ci + 1LL;
            }
            ri = ri + 1LL;
        }
        puts("======= Nodes =======");
        for (size_t __i = 0; __i < nodes_len; __i++) {
            Node n = nodes[__i];
            printf("%d\n", n);
        }
        page_rank(nodes, nodes_len, 3LL, 0.85);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
