// Generated by Mochi 0.10.32 on 2025-08-15 22:19 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


size_t append_len;
#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long graph_0[3] = {2LL, 3LL, 4LL};
long long graph_1[2] = {2LL, 7LL};
long long graph_2[1] = {5LL};
long long graph_3[2] = {5LL, 7LL};
long long graph_4[1] = {7LL};
long long graph_5[1] = {6LL};
long long graph_6[1] = {7LL};
long long graph_7[0] = {};
long long *graph_init[8] = {graph_0, graph_1, graph_2, graph_3, graph_4, graph_5, graph_6, graph_7};
long long **graph = graph_init;
size_t graph_len = 8;
size_t graph_lens_init[8] = {3, 2, 1, 2, 1, 1, 1, 0};
size_t *graph_lens = graph_lens_init;
size_t graph_lens_len = 8;

long long longest_distance(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len);
int main(void);

long long longest_distance(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len) {
    long long n = graph_len;
    long long *indegree = NULL;
    size_t indegree_len = 0;
    long long i = 0LL;
    while (i < n) {
        indegree = list_append_long_long(indegree, &indegree_len, 0LL);
        i = i + 1LL;
    }
    long long *long_dist = NULL;
    size_t long_dist_len = 0;
    long long j = 0LL;
    while (j < n) {
        long_dist = list_append_long_long(long_dist, &long_dist_len, 1LL);
        j = j + 1LL;
    }
    long long u = 0LL;
    while (u < n) {
        {
            long long* __tmp0 = graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
            size_t __tmp0_len = graph_lens[u];
            for (size_t __i = 0; __i < __tmp0_len; __i++) {
                long long v = __tmp0[__i];
                indegree[(int)(v)] = indegree[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] + 1LL;
            }
        }
        u = u + 1LL;
    }
    long long *queue = NULL;
    size_t queue_len = 0;
    long long head = 0LL;
    long long k = 0LL;
    while (k < n) {
        if (indegree[(int)({long long _mochi_idx = k; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] == 0LL) {
            queue = list_append_long_long(queue, &queue_len, k);
        }
        k = k + 1LL;
    }
    while (head < queue_len) {
        long long vertex = queue[(int)({long long _mochi_idx = head; _mochi_idx < 0 ? queue_len + _mochi_idx : _mochi_idx;})];
        head = head + 1LL;
        {
            long long* __tmp1 = graph[(int)({long long _mochi_idx = vertex; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})];
            size_t __tmp1_len = graph_lens[vertex];
            for (size_t __i = 0; __i < __tmp1_len; __i++) {
                long long x = __tmp1[__i];
                indegree[(int)(x)] = indegree[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] - 1LL;
                long long new_dist = long_dist[(int)({long long _mochi_idx = vertex; _mochi_idx < 0 ? long_dist_len + _mochi_idx : _mochi_idx;})] + 1LL;
                if (new_dist > long_dist[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? long_dist_len + _mochi_idx : _mochi_idx;})]) {
                    long_dist[(int)(x)] = new_dist;
                }
                if (indegree[(int)({long long _mochi_idx = x; _mochi_idx < 0 ? indegree_len + _mochi_idx : _mochi_idx;})] == 0LL) {
                    queue = list_append_long_long(queue, &queue_len, x);
                }
            }
        }
    }
    long long max_len = long_dist[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? long_dist_len + _mochi_idx : _mochi_idx;})];
    long long m = 1LL;
    while (m < n) {
        if (long_dist[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? long_dist_len + _mochi_idx : _mochi_idx;})] > max_len) {
            max_len = long_dist[(int)({long long _mochi_idx = m; _mochi_idx < 0 ? long_dist_len + _mochi_idx : _mochi_idx;})];
        }
        m = m + 1LL;
    }
    return max_len;
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", longest_distance(graph, graph_len, graph_lens, graph_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
