// Generated by Mochi 0.10.32 on 2025-08-15 19:31 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t depth_first_search_len;
size_t topological_sort_len;

size_t _slice_int_len;
size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { int *keys; const char **vals; size_t len; size_t cap; } MapIS;

static const char* map_get_is(const int keys[], const char *vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return "";
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long * depth_first_search(long long u, long long * visited, size_t visited_len, long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long * stack, size_t stack_len);
long long * topological_sort(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len);
void print_stack(long long * stack, size_t stack_len, MapIS clothes);
const char* format_list(long long * xs, size_t xs_len);
void user_main();
int main(void);

long long * depth_first_search(long long u, long long * visited, size_t visited_len, long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len, long long * stack, size_t stack_len) {
    visited[(int)(u)] = 1LL;
    long long i = 0LL;
    while (i < graph_lens[u]) {
        long long v = graph[(int)({long long _mochi_idx = u; _mochi_idx < 0 ? graph_len + _mochi_idx : _mochi_idx;})][(int)({long long _mochi_idx = i; _mochi_idx < 0 ? graph_lens[u] + _mochi_idx : _mochi_idx;})];
        if (!(visited[(int)({long long _mochi_idx = v; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
            stack = depth_first_search(v, visited, visited_len, graph, graph_len, graph_lens, graph_len, stack, stack_len);
            stack_len = depth_first_search_len;
        }
        i = i + 1LL;
    }
    stack = list_append_long_long(stack, &stack_len, u);
    return depth_first_search_len = stack_len, stack;
}

long long * topological_sort(long long * * graph, size_t graph_len, size_t* graph_lens, size_t graph_lens_len) {
    long long *visited = NULL;
    size_t visited_len = 0;
    long long i = 0LL;
    while (i < graph_len) {
        visited = list_append_long_long(visited, &visited_len, 0LL);
        i = i + 1LL;
    }
    long long *stack = NULL;
    size_t stack_len = 0;
    i = 0LL;
    while (i < graph_len) {
        if (!(visited[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? visited_len + _mochi_idx : _mochi_idx;})])) {
            stack = depth_first_search(i, visited, visited_len, graph, graph_len, graph_lens, graph_len, stack, stack_len);
            stack_len = depth_first_search_len;
        }
        i = i + 1LL;
    }
    return topological_sort_len = stack_len, stack;
}

void print_stack(long long * stack, size_t stack_len, MapIS clothes) {
    long long order = 1LL;
    long long *s = stack;
    size_t s_len = stack_len;
    while (s_len > 0LL) {
        long long idx = s[(int)({long long _mochi_idx = s_len - 1LL; _mochi_idx < 0 ? s_len + _mochi_idx : _mochi_idx;})];
        s = _slice_int(s, s_len, 0LL, s_len - 1LL, &(_slice_int_len));
        s_len = _slice_int_len;
        puts(str_concat(str_concat(str_int(order), " "), map_get_is(clothes.keys, clothes.vals, clothes.len, idx)));
        order = order + 1LL;
    }
}

const char* format_list(long long * xs, size_t xs_len) {
    const char* res = "[";
    long long i = 0LL;
    while (i < xs_len) {
        res = str_concat(res, str_int(xs[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? xs_len + _mochi_idx : _mochi_idx;})]));
        if (i < (xs_len - 1LL)) {
            res = str_concat(res, ", ");
        }
        i = i + 1LL;
    }
    res = str_concat(res, "]");
    return res;
}

void user_main() {
    static int clothes_keys[25] = {0LL, 1LL, 2LL, 3LL, 4LL, 5LL, 6LL, 7LL, 8LL};
    static const char* clothes_vals[25] = {"underwear", "pants", "belt", "suit", "shoe", "socks", "shirt", "tie", "watch"};
    size_t clothes_len = 9;
    MapIS clothes = { clothes_keys, clothes_vals, 9, 25 };
    long long graph_0[2] = {1LL, 4LL};
    long long graph_1[2] = {2LL, 4LL};
    long long graph_2[1] = {3LL};
    long long graph_3[0] = {};
    long long graph_4[0] = {};
    long long graph_5[1] = {4LL};
    long long graph_6[2] = {2LL, 7LL};
    long long graph_7[1] = {3LL};
    long long graph_8[0] = {};
    long long *graph_init[9] = {graph_0, graph_1, graph_2, graph_3, graph_4, graph_5, graph_6, graph_7, graph_8};
    long long **graph = graph_init;
    size_t graph_len = 9;
    size_t graph_lens_init[9] = {2, 2, 1, 0, 0, 1, 2, 1, 0};
    size_t *graph_lens = graph_lens_init;
    size_t graph_lens_len = 9;
    long long *stack = topological_sort(graph, graph_len, graph_lens, graph_len);
    size_t stack_len = topological_sort_len;
    puts(format_list(stack, stack_len));
    print_stack(stack, stack_len, clothes);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
