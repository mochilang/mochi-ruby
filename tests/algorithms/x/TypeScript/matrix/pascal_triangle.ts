// Generated by Mochi v0.10.67 on 2025-08-17 13:46:03 GMT+7

function populate_current_row(triangle: number[][], current_row_idx: number): number[] {
  let row: number[] = [];
  let i: number = 0;
  while ((i <= current_row_idx)) {
    if (((i === 0) || (i === current_row_idx))) {
      row.push(1);
    } else {
      let left: number = triangle[(()=>{const _mochi_idx = Math.trunc((current_row_idx - 1)); return _mochi_idx < 0 ? triangle.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc((i - 1)); return _mochi_idx < 0 ? triangle[(()=>{const _mochi_idx = Math.trunc((current_row_idx - 1)); return _mochi_idx < 0 ? triangle.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
      let right: number = triangle[(()=>{const _mochi_idx = Math.trunc((current_row_idx - 1)); return _mochi_idx < 0 ? triangle.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? triangle[(()=>{const _mochi_idx = Math.trunc((current_row_idx - 1)); return _mochi_idx < 0 ? triangle.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
      row.push((left + right));
    }
    i = (i + 1);
  }
  return row;
}
function generate_pascal_triangle(num_rows: number): number[][] {
  if ((num_rows <= 0)) {
    return [];
  }
  let triangle: number[][] = [];
  let row_idx: number = 0;
  while ((row_idx < num_rows)) {
    let row: number[] = populate_current_row(triangle, row_idx);
    triangle.push(row);
    row_idx = (row_idx + 1);
  }
  return triangle;
}
function row_to_string(row: number[], total_rows: number, row_idx: number): string {
  let line: string = "";
  let spaces: bigint = ((total_rows - row_idx) - 1);
  let s: number = 0;
  while ((s < spaces)) {
    line = (line + " ");
    s = (s + 1);
  }
  let c: number = 0;
  while ((c <= row_idx)) {
    line = (line + _str(row[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? row.length + _mochi_idx : _mochi_idx;})()]));
    if ((c !== row_idx)) {
      line = (line + " ");
    }
    c = (c + 1);
  }
  return line;
}
function print_pascal_triangle(num_rows: number) {
  let triangle: number[][] = generate_pascal_triangle(num_rows);
  let r: number = 0;
  while ((r < num_rows)) {
    let line: string = row_to_string(triangle[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? triangle.length + _mochi_idx : _mochi_idx;})()], num_rows, r);
    console.log(_str(line));
    r = (r + 1);
  }
}
function main() {
  print_pascal_triangle(5);
  console.log(_str(generate_pascal_triangle(5)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

