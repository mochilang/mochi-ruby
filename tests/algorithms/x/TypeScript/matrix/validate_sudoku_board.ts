// Generated by Mochi v0.10.67 on 2025-08-17 13:46:11 GMT+7

let NUM_SQUARES: number = 9;
let EMPTY_CELL: string = ".";
function is_valid_sudoku_board(board: string[][]): boolean {
  if ((_len(board) !== NUM_SQUARES)) {
    return false;
  }
  let i: number = 0;
  while ((i < NUM_SQUARES)) {
    if ((_len(board[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? board.length + _mochi_idx : _mochi_idx;})()]) !== NUM_SQUARES)) {
      return false;
    }
    i = (i + 1);
  }
  let rows: string[][] = [];
  let cols: string[][] = [];
  let boxes: string[][] = [];
  i = 0;
  while ((i < NUM_SQUARES)) {
    rows.push([]);
    cols.push([]);
    boxes.push([]);
    i = (i + 1);
  }
  for (let r = 0; r < NUM_SQUARES; r++) {
    for (let c = 0; c < NUM_SQUARES; c++) {
      let value: string = board[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? board.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? board[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? board.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
      if ((value === EMPTY_CELL)) {
        continue
      }
      let box: number = ((Math.trunc(Math.trunc(r / 3)) * 3) + Math.trunc(Math.trunc(c / 3)));
      if ((((Array.isArray(rows[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? rows.length + _mochi_idx : _mochi_idx;})()]) ? rows[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? rows.length + _mochi_idx : _mochi_idx;})()].includes(value) : (value in rows[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? rows.length + _mochi_idx : _mochi_idx;})()])) || (Array.isArray(cols[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? cols.length + _mochi_idx : _mochi_idx;})()]) ? cols[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? cols.length + _mochi_idx : _mochi_idx;})()].includes(value) : (value in cols[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? cols.length + _mochi_idx : _mochi_idx;})()]))) || (Array.isArray(boxes[(()=>{const _mochi_idx = box; return _mochi_idx < 0 ? boxes.length + _mochi_idx : _mochi_idx;})()]) ? boxes[(()=>{const _mochi_idx = box; return _mochi_idx < 0 ? boxes.length + _mochi_idx : _mochi_idx;})()].includes(value) : (value in boxes[(()=>{const _mochi_idx = box; return _mochi_idx < 0 ? boxes.length + _mochi_idx : _mochi_idx;})()])))) {
        return false;
      }
      rows[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? rows.length + _mochi_idx : _mochi_idx;})()].push(value);
      cols[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? cols.length + _mochi_idx : _mochi_idx;})()].push(value);
      boxes[(()=>{const _mochi_idx = box; return _mochi_idx < 0 ? boxes.length + _mochi_idx : _mochi_idx;})()].push(value);
    }
  }
  return true;
}
let valid_board: string[][] = [["5", "3", ".", ".", "7", ".", ".", ".", "."], ["6", ".", ".", "1", "9", "5", ".", ".", "."], [".", "9", "8", ".", ".", ".", ".", "6", "."], ["8", ".", ".", ".", "6", ".", ".", ".", "3"], ["4", ".", ".", "8", ".", "3", ".", ".", "1"], ["7", ".", ".", ".", "2", ".", ".", ".", "6"], [".", "6", ".", ".", ".", ".", "2", "8", "."], [".", ".", ".", "4", "1", "9", ".", ".", "5"], [".", ".", ".", ".", "8", ".", ".", "7", "9"]];
let invalid_board: string[][] = [["8", "3", ".", ".", "7", ".", ".", ".", "."], ["6", ".", ".", "1", "9", "5", ".", ".", "."], [".", "9", "8", ".", ".", ".", ".", "6", "."], ["8", ".", ".", ".", "6", ".", ".", ".", "3"], ["4", ".", ".", "8", ".", "3", ".", ".", "1"], ["7", ".", ".", ".", "2", ".", ".", ".", "6"], [".", "6", ".", ".", ".", ".", "2", "8", "."], [".", ".", ".", "4", "1", "9", ".", ".", "5"], [".", ".", ".", ".", "8", ".", ".", "7", "9"]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(is_valid_sudoku_board(valid_board)));
  console.log(_str(is_valid_sudoku_board(invalid_board)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

