// Generated by Mochi v0.10.67 on 2025-08-17 13:45:57 GMT+7

function add(matrices: number[][][]): number[][] {
  let rows: number = _len(matrices[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()]);
  let cols: number = _len(matrices[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrices[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
  let r: number = 0;
  let result: number[][] = [];
  while ((r < rows)) {
    let row: number[] = [];
    let c: number = 0;
    while ((c < cols)) {
      let sum: number = 0.0;
      let m: number = 0;
      while ((m < _len(matrices))) {
        sum = (sum + matrices[(()=>{const _mochi_idx = m; return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? matrices[(()=>{const _mochi_idx = m; return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? matrices[(()=>{const _mochi_idx = m; return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? matrices[(()=>{const _mochi_idx = m; return _mochi_idx < 0 ? matrices.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
        m = (m + 1);
      }
      row.push(sum);
      c = (c + 1);
    }
    result.push(row);
    r = (r + 1);
  }
  return result;
}
function subtract(a: number[][], b: number[][]): number[][] {
  let rows: number = _len(a);
  let cols: number = _len(a[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()]);
  let r: number = 0;
  let result: number[][] = [];
  while ((r < rows)) {
    let row: number[] = [];
    let c: number = 0;
    while ((c < cols)) {
      row.push((a[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? a[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] - b[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? b[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]));
      c = (c + 1);
    }
    result.push(row);
    r = (r + 1);
  }
  return result;
}
function scalar_multiply(matrix: number[][], n: number): number[][] {
  let result: number[][] = [];
  let i: number = 0;
  while ((i < _len(matrix))) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < _len(matrix[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()]))) {
      row.push((matrix[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * n));
      j = (j + 1);
    }
    result.push(row);
    i = (i + 1);
  }
  return result;
}
function multiply(a: number[][], b: number[][]): number[][] {
  let rowsA: number = _len(a);
  let colsA: number = _len(a[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()]);
  let rowsB: number = _len(b);
  let colsB: number = _len(b[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()]);
  let result: number[][] = [];
  let i: number = 0;
  while ((i < rowsA)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < colsB)) {
      let sum: number = 0.0;
      let k: number = 0;
      while ((k < colsA)) {
        sum = (sum + (a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * b[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? b[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]));
        k = (k + 1);
      }
      row.push(sum);
      j = (j + 1);
    }
    result.push(row);
    i = (i + 1);
  }
  return result;
}
function identity(n: number): number[][] {
  let result: number[][] = [];
  let i: number = 0;
  while ((i < n)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < n)) {
      if ((i === j)) {
        row.push(1.0);
      } else {
        row.push(0.0);
      }
      j = (j + 1);
    }
    result.push(row);
    i = (i + 1);
  }
  return result;
}
function transpose(matrix: number[][]): number[][] {
  let rows: number = _len(matrix);
  let cols: number = _len(matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()]);
  let result: number[][] = [];
  let c: number = 0;
  while ((c < cols)) {
    let row: number[] = [];
    let r: number = 0;
    while ((r < rows)) {
      row.push(matrix[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
      r = (r + 1);
    }
    result.push(row);
    c = (c + 1);
  }
  return result;
}
function minor(matrix: number[][], row: number, column: number): number[][] {
  let result: number[][] = [];
  let i: number = 0;
  while ((i < _len(matrix))) {
    if ((i !== row)) {
      let new_row: number[] = [];
      let j: number = 0;
      while ((j < _len(matrix[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()]))) {
        if ((j !== column)) {
          new_row.push(matrix[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
        }
        j = (j + 1);
      }
      result.push(new_row);
    }
    i = (i + 1);
  }
  return result;
}
function determinant(matrix: number[][]): number {
  if ((_len(matrix) === 1)) {
    return matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
  }
  let det: number = 0.0;
  let c: number = 0;
  while ((c < _len(matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()]))) {
    let sub: number[][] = minor(matrix, 0, c);
    let sign: number = (((c % 2) === 0) ? 1.0 : -1.0);
    det = (det + ((matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * determinant(sub)) * sign));
    c = (c + 1);
  }
  return det;
}
function inverse(matrix: number[][]): number[][] {
  let det: number = determinant(matrix);
  if ((det === 0.0)) {
    return [];
  }
  let size: number = _len(matrix);
  let matrix_minor: number[][] = [];
  let i: number = 0;
  while ((i < size)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < size)) {
      let m: number[][] = minor(matrix, i, j);
      row.push(determinant(m));
      j = (j + 1);
    }
    matrix_minor.push(row);
    i = (i + 1);
  }
  let cofactors: number[][] = [];
  i = 0;
  while ((i < size)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < size)) {
      let sign: number = ((((i + j) % 2) === 0) ? 1.0 : -1.0);
      row.push((matrix_minor[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix_minor.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? matrix_minor[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix_minor.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * sign));
      j = (j + 1);
    }
    cofactors.push(row);
    i = (i + 1);
  }
  let adjugate: number[][] = transpose(cofactors);
  return scalar_multiply(adjugate, (1.0 / det));
}
function main() {
  let matrix_a: number[][] = [[12.0, 10.0], [3.0, 9.0]];
  let matrix_b: number[][] = [[3.0, 4.0], [7.0, 4.0]];
  let matrix_c: number[][] = [[11.0, 12.0, 13.0, 14.0], [21.0, 22.0, 23.0, 24.0], [31.0, 32.0, 33.0, 34.0], [41.0, 42.0, 43.0, 44.0]];
  let matrix_d: number[][] = [[3.0, 0.0, 2.0], [2.0, 0.0, -2.0], [0.0, 1.0, 1.0]];
  console.log(_str((("Add Operation, add(matrix_a, matrix_b) = " + _str(add([matrix_a, matrix_b]))) + " \n")));
  console.log(_str((("Multiply Operation, multiply(matrix_a, matrix_b) = " + _str(multiply(matrix_a, matrix_b))) + " \n")));
  console.log(_str((("Identity: " + _str(identity(5))) + "\n")));
  console.log(_str((((("Minor of " + _str(matrix_c)) + " = ") + _str(minor(matrix_c, 1, 2))) + " \n")));
  console.log(_str((((("Determinant of " + _str(matrix_b)) + " = ") + _str(determinant(matrix_b))) + " \n")));
  console.log(_str((((("Inverse of " + _str(matrix_d)) + " = ") + _str(inverse(matrix_d))) + "\n")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

