// Generated by Mochi v0.10.67 on 2025-08-17 13:46:31 GMT+7

function exp_approx(x: number): number {
  let sum: number = 1.0;
  let term: number = 1.0;
  let i: number = 1;
  let absx: number = ((x < 0.0) ? -x : x);
  while ((i <= 20)) {
    term = ((term * absx) / i);
    sum = (sum + term);
    i = (i + 1);
  }
  if ((x < 0.0)) {
    return (1.0 / sum);
  }
  return sum;
}
function exponential_linear_unit(vector: number[], alpha: number): number[] {
  let result: number[] = [];
  let i: number = 0;
  while ((i < _len(vector))) {
    let v: number = vector[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? vector.length + _mochi_idx : _mochi_idx;})()];
    if ((v > 0.0)) {
      result.push(v);
    } else {
      let neg: number = (alpha * (exp_approx(v) - 1.0));
      result.push(neg);
    }
    i = (i + 1);
  }
  return result;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(exponential_linear_unit([2.3, 0.6, -2.0, -3.8], 0.3)));
  console.log(_str(exponential_linear_unit([-9.2, -0.3, 0.45, -4.56], 0.067)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

