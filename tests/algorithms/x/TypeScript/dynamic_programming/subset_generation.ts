// Generated by Mochi v0.10.64 on 2025-08-13 07:27:08 GMT+7

function copy_list(src: number[]): number[] {
  let result: number[] = [];
  let i: number = 0;
  while ((i < _len(src))) {
    result.push(src[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? src.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return result;
}
function subset_combinations(elements: number[], n: number): number[][] {
  let r: number = _len(elements);
  if ((n > r)) {
    return [];
  }
  let dp: number[][][] = [];
  let i: number = 0;
  while ((i <= r)) {
    dp.push([]);
    i = (i + 1);
  }
  dp[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()] = [...dp[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()], []];
  i = 1;
  while ((i <= r)) {
    let j: number = i;
    while ((j > 0)) {
      let prevs: number[][] = dp[(()=>{const _mochi_idx = Math.trunc((j - 1)); return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()];
      let k: number = 0;
      while ((k < _len(prevs))) {
        let prev: number[] = prevs[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? prevs.length + _mochi_idx : _mochi_idx;})()];
        let comb: number[] = copy_list(prev);
        comb.push(elements[(()=>{const _mochi_idx = Math.trunc((i - 1)); return _mochi_idx < 0 ? elements.length + _mochi_idx : _mochi_idx;})()]);
        dp[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()].push(comb);
        k = (k + 1);
      }
      j = (j - 1);
    }
    i = (i + 1);
  }
  return dp[(()=>{const _mochi_idx = n; return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()];
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(subset_combinations([10, 20, 30, 40], 2)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

