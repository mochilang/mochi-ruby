// Generated by Mochi v0.10.64 on 2025-08-13 07:26:46 GMT+7

function max_product_subarray(numbers: number[]): number {
  if ((_len(numbers) === 0)) {
    return 0;
  }
  let max_till_now: number = numbers[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()];
  let min_till_now: number = numbers[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()];
  let max_prod: number = numbers[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()];
  let i: number = 1;
  while ((i < _len(numbers))) {
    let _number: number = numbers[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()];
    if ((_number < 0)) {
      let temp: number = max_till_now;
      max_till_now = min_till_now;
      min_till_now = temp;
    }
    let prod_max: number = (max_till_now * _number);
    if ((_number > prod_max)) {
      max_till_now = _number;
    } else {
      max_till_now = prod_max;
    }
    let prod_min: number = (min_till_now * _number);
    if ((_number < prod_min)) {
      min_till_now = _number;
    } else {
      min_till_now = prod_min;
    }
    if ((max_till_now > max_prod)) {
      max_prod = max_till_now;
    }
    i = (i + 1);
  }
  return max_prod;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(max_product_subarray([2, 3, -2, 4])));
  console.log(_str(max_product_subarray([-2, 0, -1])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

