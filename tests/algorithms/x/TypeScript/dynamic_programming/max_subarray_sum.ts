// Generated by Mochi v0.10.64 on 2025-08-13 07:26:48 GMT+7

function max_subarray_sum(nums: number[], allow_empty: boolean): number {
  if ((_len(nums) === 0)) {
    return 0.0;
  }
  let max_sum: number = 0.0;
  let curr_sum: number = 0.0;
  if (allow_empty) {
    max_sum = 0.0;
    curr_sum = 0.0;
    let i: number = 0;
    while ((i < _len(nums))) {
      let num = nums[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()];
      let temp = (curr_sum + num);
      curr_sum = ((temp > 0.0) ? temp : 0.0);
      if ((curr_sum > max_sum)) {
        max_sum = curr_sum;
      }
      i = (i + 1);
    }
  } else {
    max_sum = nums[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()];
    curr_sum = nums[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()];
    let i: number = 1;
    while ((i < _len(nums))) {
      let num = nums[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()];
      let temp = (curr_sum + num);
      curr_sum = ((temp > num) ? temp : num);
      if ((curr_sum > max_sum)) {
        max_sum = curr_sum;
      }
      i = (i + 1);
    }
  }
  return max_sum;
}
let empty: number[] = [];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(max_subarray_sum([2.0, 8.0, 9.0], false)));
  console.log(_str(max_subarray_sum([0.0, 0.0], false)));
  console.log(_str(max_subarray_sum([-1.0, 0.0, 1.0], false)));
  console.log(_str(max_subarray_sum([1.0, 2.0, 3.0, 4.0, -2.0], false)));
  console.log(_str(max_subarray_sum([-2.0, 1.0, -3.0, 4.0, -1.0, 2.0, 1.0, -5.0, 4.0], false)));
  console.log(_str(max_subarray_sum([2.0, 3.0, -9.0, 8.0, -2.0], false)));
  console.log(_str(max_subarray_sum([-2.0, -3.0, -1.0, -4.0, -6.0], false)));
  console.log(_str(max_subarray_sum([-2.0, -3.0, -1.0, -4.0, -6.0], true)));
  console.log(_str(max_subarray_sum(empty, false)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

