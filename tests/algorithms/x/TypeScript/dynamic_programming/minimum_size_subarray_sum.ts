// Generated by Mochi v0.10.64 on 2025-08-13 07:26:55 GMT+7

function minimum_subarray_sum(target: number, numbers: number[]): number {
  let n: number = _len(numbers);
  if ((n === 0)) {
    return 0;
  }
  if ((target === 0)) {
    let i: number = 0;
    while ((i < n)) {
      if ((numbers[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()] === 0)) {
        return 0;
      }
      i = (i + 1);
    }
  }
  let left: number = 0;
  let right: number = 0;
  let curr_sum: number = 0;
  let min_len: number = (n + 1);
  while ((right < n)) {
    curr_sum = (curr_sum + numbers[(()=>{const _mochi_idx = right; return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()]);
    while (((curr_sum >= target) && (left <= right))) {
      let current_len: number = ((right - left) + 1);
      if ((current_len < min_len)) {
        min_len = current_len;
      }
      curr_sum = (curr_sum - numbers[(()=>{const _mochi_idx = left; return _mochi_idx < 0 ? numbers.length + _mochi_idx : _mochi_idx;})()]);
      left = (left + 1);
    }
    right = (right + 1);
  }
  if ((min_len === (n + 1))) {
    return 0;
  }
  return min_len;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(minimum_subarray_sum(7, [2, 3, 1, 2, 4, 3])));
  console.log(_str(minimum_subarray_sum(7, [2, 3, -1, 2, 4, -3])));
  console.log(_str(minimum_subarray_sum(11, [1, 1, 1, 1, 1, 1, 1, 1])));
  console.log(_str(minimum_subarray_sum(0, [1, 2, 3])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

