// Generated by Mochi v0.10.64 on 2025-08-13 07:26:33 GMT+7

function sort_list(nums: number[]): number[] {
  let arr: number[] = nums;
  let i: number = 1;
  while ((i < _len(arr))) {
    let key: number = arr[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
    let j: number = (i - 1);
    while (((j >= 0) && (arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] > key))) {
      arr[(()=>{const _mochi_idx = (j + 1); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
      j = (j - 1);
    }
    arr[(()=>{const _mochi_idx = (j + 1); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = key;
    i = (i + 1);
  }
  return arr;
}
function largest_divisible_subset(items: number[]): number[] {
  if ((_len(items) === 0)) {
    return [];
  }
  let nums: number[] = sort_list(items);
  let n: number = _len(nums);
  let memo: number[] = [];
  let prev: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    memo.push(1);
    prev.push(i);
    i = (i + 1);
  }
  i = 0;
  while ((i < n)) {
    let j: number = 0;
    while ((j < i)) {
      if ((((nums[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()] === 0) || ((nums[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()] % nums[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()]) === 0)) && ((memo[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? memo.length + _mochi_idx : _mochi_idx;})()] + 1) > memo[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? memo.length + _mochi_idx : _mochi_idx;})()]))) {
        memo[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? memo.length + _mochi_idx : _mochi_idx;})()] = (memo[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? memo.length + _mochi_idx : _mochi_idx;})()] + 1);
        prev[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? prev.length + _mochi_idx : _mochi_idx;})()] = j;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  let ans: number = (0 - 1);
  let last_index: number = (0 - 1);
  i = 0;
  while ((i < n)) {
    if ((memo[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? memo.length + _mochi_idx : _mochi_idx;})()] > ans)) {
      ans = memo[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? memo.length + _mochi_idx : _mochi_idx;})()];
      last_index = i;
    }
    i = (i + 1);
  }
  if ((last_index === (0 - 1))) {
    return [];
  }
  let result: number[] = [nums[(()=>{const _mochi_idx = last_index; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()]];
  while ((prev[(()=>{const _mochi_idx = last_index; return _mochi_idx < 0 ? prev.length + _mochi_idx : _mochi_idx;})()] !== last_index)) {
    last_index = prev[(()=>{const _mochi_idx = last_index; return _mochi_idx < 0 ? prev.length + _mochi_idx : _mochi_idx;})()];
    result.push(nums[(()=>{const _mochi_idx = last_index; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()]);
  }
  return result;
}
function main() {
  let items: number[] = [1, 16, 7, 8, 4];
  let subset: number[] = largest_divisible_subset(items);
  console.log(_str((((("The longest divisible subset of " + _str(items)) + " is ") + _str(subset)) + ".")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

