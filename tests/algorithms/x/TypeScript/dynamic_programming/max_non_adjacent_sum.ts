// Generated by Mochi v0.10.64 on 2025-08-13 07:26:45 GMT+7

function maximum_non_adjacent_sum(nums: number[]): number {
  if ((_len(nums) === 0)) {
    return 0;
  }
  let max_including: number = nums[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()];
  let max_excluding: number = 0;
  let i: number = 1;
  while ((i < _len(nums))) {
    let num = nums[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? nums.length + _mochi_idx : _mochi_idx;})()];
    let new_including = (max_excluding + num);
    let new_excluding: number = ((max_including > max_excluding) ? max_including : max_excluding);
    max_including = new_including;
    max_excluding = new_excluding;
    i = (i + 1);
  }
  if ((max_including > max_excluding)) {
    return max_including;
  }
  return max_excluding;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(maximum_non_adjacent_sum([1, 2, 3])));
  console.log(_str(maximum_non_adjacent_sum([1, 5, 3, 7, 2, 2, 6])));
  console.log(_str(maximum_non_adjacent_sum([-1, -5, -3, -7, -2, -2, -6])));
  console.log(_str(maximum_non_adjacent_sum([499, 500, -3, -7, -2, -2, -6])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

