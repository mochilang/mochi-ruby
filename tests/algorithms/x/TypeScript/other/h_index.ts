// Generated by Mochi v0.10.73 on 2025-08-26 08:47:36 GMT+7

function subarray(xs: number[], start: number, end: number): number[] {
  let result: number[] = [];
  let k: number = start;
  while ((k < end)) {
    result.push(xs[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? xs.length + _mochi_idx : _mochi_idx;})()]);
    k = (k + 1);
  }
  return result;
}
function merge(left_half: number[], right_half: number[]): number[] {
  let result: number[] = [];
  let i: number = 0;
  let j: number = 0;
  while (((i < _len(left_half)) && (j < _len(right_half)))) {
    if ((left_half[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? left_half.length + _mochi_idx : _mochi_idx;})()] < right_half[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? right_half.length + _mochi_idx : _mochi_idx;})()])) {
      result.push(left_half[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? left_half.length + _mochi_idx : _mochi_idx;})()]);
      i = (i + 1);
    } else {
      result.push(right_half[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? right_half.length + _mochi_idx : _mochi_idx;})()]);
      j = (j + 1);
    }
  }
  while ((i < _len(left_half))) {
    result.push(left_half[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? left_half.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  while ((j < _len(right_half))) {
    result.push(right_half[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? right_half.length + _mochi_idx : _mochi_idx;})()]);
    j = (j + 1);
  }
  return result;
}
function merge_sort(array: number[]): number[] {
  if ((_len(array) <= 1)) {
    return array;
  }
  let middle: number = Math.trunc(_len(array) / 2);
  let left_half: number[] = subarray(array, 0, middle);
  let right_half: number[] = subarray(array, middle, _len(array));
  let sorted_left: number[] = merge_sort(left_half);
  let sorted_right: number[] = merge_sort(right_half);
  return merge(sorted_left, sorted_right);
}
function h_index(citations: number[]): number {
  let idx: number = 0;
  while ((idx < _len(citations))) {
    if ((citations[(()=>{const _mochi_idx = idx; return _mochi_idx < 0 ? citations.length + _mochi_idx : _mochi_idx;})()] < 0)) {
      _panic("The citations should be a list of non negative integers.");
    }
    idx = (idx + 1);
  }
  let sorted: number[] = merge_sort(citations);
  let n: number = _len(sorted);
  let i: number = 0;
  while ((i < n)) {
    if ((sorted[(()=>{const _mochi_idx = Math.trunc(((n - 1) - i)); return _mochi_idx < 0 ? sorted.length + _mochi_idx : _mochi_idx;})()] <= i)) {
      return i;
    }
    i = (i + 1);
  }
  return n;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(h_index([3, 0, 6, 1, 5])));
  console.log(_str(h_index([1, 3, 1])));
  console.log(_str(h_index([1, 2, 3])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

