// Generated by Mochi v0.10.73 on 2025-08-26 08:47:51 GMT+7

function get_data(source_data: number[][]): number[][] {
  let data_lists: number[][] = [];
  let i: number = 0;
  while ((i < _len(source_data))) {
    let row: number[] = source_data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? source_data.length + _mochi_idx : _mochi_idx;})()];
    let j: number = 0;
    while ((j < _len(row))) {
      if ((_len(data_lists) < (j + 1))) {
        let empty: number[] = [];
        data_lists.push(empty);
      }
      data_lists[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? data_lists.length + _mochi_idx : _mochi_idx;})()].push(row[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? row.length + _mochi_idx : _mochi_idx;})()]);
      j = (j + 1);
    }
    i = (i + 1);
  }
  return data_lists;
}
function calculate_each_score(data_lists: number[][], weights: number[]): number[][] {
  let score_lists: number[][] = [];
  let i: number = 0;
  while ((i < _len(data_lists))) {
    let dlist: number[] = data_lists[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data_lists.length + _mochi_idx : _mochi_idx;})()];
    let weight: number = weights[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? weights.length + _mochi_idx : _mochi_idx;})()];
    let mind: number = dlist[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? dlist.length + _mochi_idx : _mochi_idx;})()];
    let maxd: number = dlist[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? dlist.length + _mochi_idx : _mochi_idx;})()];
    let j: number = 1;
    while ((j < _len(dlist))) {
      let val: number = dlist[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? dlist.length + _mochi_idx : _mochi_idx;})()];
      if ((val < mind)) {
        mind = val;
      }
      if ((val > maxd)) {
        maxd = val;
      }
      j = (j + 1);
    }
    let score: number[] = [];
    j = 0;
    if ((weight === 0)) {
      while ((j < _len(dlist))) {
        let item: number = dlist[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? dlist.length + _mochi_idx : _mochi_idx;})()];
        if (_equal((maxd - mind), 0.0)) {
          score.push(1.0);
        } else {
          score.push((1.0 - ((item - mind) / (maxd - mind))));
        }
        j = (j + 1);
      }
    } else {
      while ((j < _len(dlist))) {
        let item: number = dlist[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? dlist.length + _mochi_idx : _mochi_idx;})()];
        if (_equal((maxd - mind), 0.0)) {
          score.push(0.0);
        } else {
          score.push(((item - mind) / (maxd - mind)));
        }
        j = (j + 1);
      }
    }
    score_lists.push(score);
    i = (i + 1);
  }
  return score_lists;
}
function generate_final_scores(score_lists: number[][]): number[] {
  let count = _len(score_lists[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? score_lists.length + _mochi_idx : _mochi_idx;})()]);
  let final_scores: number[] = [];
  let i: number = 0;
  while ((i < count)) {
    final_scores.push(0.0);
    i = (i + 1);
  }
  i = 0;
  while ((i < _len(score_lists))) {
    let slist: number[] = score_lists[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? score_lists.length + _mochi_idx : _mochi_idx;})()];
    let j: number = 0;
    while ((j < _len(slist))) {
      final_scores[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? final_scores.length + _mochi_idx : _mochi_idx;})()] = (final_scores[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? final_scores.length + _mochi_idx : _mochi_idx;})()] + slist[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? slist.length + _mochi_idx : _mochi_idx;})()]);
      j = (j + 1);
    }
    i = (i + 1);
  }
  return final_scores;
}
function procentual_proximity(source_data: number[][], weights: number[]): number[][] {
  let data_lists: number[][] = get_data(source_data);
  let score_lists: number[][] = calculate_each_score(data_lists, weights);
  let final_scores: number[] = generate_final_scores(score_lists);
  let i: number = 0;
  while ((i < _len(final_scores))) {
    source_data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? source_data.length + _mochi_idx : _mochi_idx;})()].push(final_scores[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? final_scores.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return source_data;
}
let vehicles: number[][] = [];
let weights: number[] = [0, 0, 1];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _equal(a: unknown, b: unknown): boolean {
  if (typeof a === 'number' && typeof b === 'number') {
    return Math.abs(a - b) <= 1e-6 * Math.max(Math.abs(a), Math.abs(b));
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === 'object' && typeof b === 'object') {
    const ak = Object.keys(a); const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) { if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) return false; }
    return true;
  }
  return a === b;
}
let result: number[][]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  vehicles.push([20.0, 60.0, 2012.0]);
  vehicles.push([23.0, 90.0, 2015.0]);
  vehicles.push([22.0, 50.0, 2011.0]);
result = procentual_proximity(vehicles, weights)
  console.log(_str(result));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

