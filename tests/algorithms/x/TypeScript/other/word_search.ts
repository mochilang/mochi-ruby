// Generated by Mochi v0.10.73 on 2025-08-26 08:47:55 GMT+7

export interface WordSearch { words: string[]; width: number; height: number; board: string[][] }
let seed: number = 123456789;
function rand(): number {
  seed = Number((((BigInt(seed) * 1103515245n) + 12345n) % 2147483648n));
  return seed;
}
function rand_range(max: number): number {
  return (rand() % max);
}
function shuffle(list_int: number[]): number[] {
  let i: number = (_len(list_int) - 1);
  while ((i > 0)) {
    let j: number = rand_range((i + 1));
    let tmp: number = list_int[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list_int.length + _mochi_idx : _mochi_idx;})()];
    list_int[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list_int.length + _mochi_idx : _mochi_idx;})()] = list_int[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? list_int.length + _mochi_idx : _mochi_idx;})()];
    list_int[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? list_int.length + _mochi_idx : _mochi_idx;})()] = tmp;
    i = (i - 1);
  }
  return list_int;
}
function rand_letter(): string {
  let letters: string = "abcdefghijklmnopqrstuvwxyz";
  let i: number = rand_range(26);
  return letters.slice(i, (i + 1));
}
function make_word_search(words: string[], width: number, height: number): WordSearch {
  let board: string[][] = [];
  let r: number = 0;
  while ((r < height)) {
    let row: string[] = [];
    let c: number = 0;
    while ((c < width)) {
      row.push("");
      c = (c + 1);
    }
    board.push(row);
    r = (r + 1);
  }
  return {words, width, height, board};
}
function insert_dir(ws: WordSearch, word: string, dr: number, dc: number, rows: number[], cols: number[]): boolean {
  let word_len: number = _len(word);
  let ri: number = 0;
  while ((ri < _len(rows))) {
    let row: number = rows[(()=>{const _mochi_idx = ri; return _mochi_idx < 0 ? rows.length + _mochi_idx : _mochi_idx;})()];
    let ci: number = 0;
    while ((ci < _len(cols))) {
      let col: number = cols[(()=>{const _mochi_idx = ci; return _mochi_idx < 0 ? cols.length + _mochi_idx : _mochi_idx;})()];
      let end_r: number = (row + (dr * (word_len - 1)));
      let end_c: number = (col + (dc * (word_len - 1)));
      if (((((end_r < 0) || (end_r >= ws.height)) || (end_c < 0)) || (end_c >= ws.width))) {
        ci = (ci + 1);
        continue
      }
      let k: number = 0;
      let ok: boolean = true;
      while ((k < word_len)) {
        let rr: number = (row + (dr * k));
        let cc: number = (col + (dc * k));
        if ((ws.board[(()=>{const _mochi_idx = rr; return _mochi_idx < 0 ? ws.board.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = cc; return _mochi_idx < 0 ? ws.board[(()=>{const _mochi_idx = rr; return _mochi_idx < 0 ? ws.board.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] !== "")) {
          ok = false;
          break
        }
        k = (k + 1);
      }
      if (ok) {
        k = 0;
        while ((k < word_len)) {
          let rr2: number = (row + (dr * k));
          let cc2: number = (col + (dc * k));
          let row_list: string[] = ws.board[(()=>{const _mochi_idx = rr2; return _mochi_idx < 0 ? ws.board.length + _mochi_idx : _mochi_idx;})()];
          row_list[(()=>{const _mochi_idx = cc2; return _mochi_idx < 0 ? row_list.length + _mochi_idx : _mochi_idx;})()] = word.slice(k, (k + 1));
          k = (k + 1);
        }
        return true;
      }
      ci = (ci + 1);
    }
    ri = (ri + 1);
  }
  return false;
}
function generate_board(ws: WordSearch) {
  let dirs_r: number[] = [-1, -1, 0, 1, 1, 1, 0, -1];
  let dirs_c: number[] = [0, 1, 1, 1, 0, -1, -1, -1];
  let i: number = 0;
  while ((i < _len(ws.words))) {
    let word: string = ws.words[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? ws.words.length + _mochi_idx : _mochi_idx;})()];
    let rows: number[] = [];
    let r: number = 0;
    while ((r < ws.height)) {
      rows.push(r);
      r = (r + 1);
    }
    let cols: number[] = [];
    let c: number = 0;
    while ((c < ws.width)) {
      cols.push(c);
      c = (c + 1);
    }
    rows = shuffle(rows);
    cols = shuffle(cols);
    let d: number = rand_range(8);
    insert_dir(ws, word, dirs_r[(()=>{const _mochi_idx = d; return _mochi_idx < 0 ? dirs_r.length + _mochi_idx : _mochi_idx;})()], dirs_c[(()=>{const _mochi_idx = d; return _mochi_idx < 0 ? dirs_c.length + _mochi_idx : _mochi_idx;})()], rows, cols);
    i = (i + 1);
  }
}
function visualise(ws: WordSearch, add_fake_chars: boolean): string {
  let result: string = "";
  let r: number = 0;
  while ((r < ws.height)) {
    let c: number = 0;
    while ((c < ws.width)) {
      let ch: string = ws.board[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? ws.board.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? ws.board[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? ws.board.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
      if ((ch === "")) {
        if (add_fake_chars) {
          ch = rand_letter();
        } else {
          ch = "#";
        }
      }
      result = ((result + ch) + " ");
      c = (c + 1);
    }
    result = (result + "\n");
    r = (r + 1);
  }
  return result;
}
function main() {
  let words: string[] = ["cat", "dog", "snake", "fish"];
  let ws: WordSearch = make_word_search(words, 10, 10);
  generate_board(ws);
  console.log(_str(visualise(ws, true)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

