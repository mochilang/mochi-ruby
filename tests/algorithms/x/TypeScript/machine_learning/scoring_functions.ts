// Generated by Mochi v0.10.63 on 2025-08-11 18:39:30 GMT+7

function absf(x: number): number {
  if ((x < 0.0)) {
    return (0.0 - x);
  }
  return x;
}
function sqrtApprox(x: number): number {
  if ((x <= 0.0)) {
    return 0.0;
  }
  let guess: number = x;
  let i: number = 0;
  while ((i < 20)) {
    guess = ((guess + (x / guess)) / 2.0);
    i = (i + 1);
  }
  return guess;
}
function ln_series(x: number): number {
  let t: number = ((x - 1.0) / (x + 1.0));
  let term: number = t;
  let sum: number = 0.0;
  let n: number = 1;
  while ((n <= 19)) {
    sum = (sum + (term / n));
    term = ((term * t) * t);
    n = (n + 2);
  }
  return (2.0 * sum);
}
function ln(x: number): number {
  let y: number = x;
  let k: number = 0;
  while ((y >= 10.0)) {
    y = (y / 10.0);
    k = (k + 1);
  }
  while ((y < 1.0)) {
    y = (y * 10.0);
    k = (k - 1);
  }
  return (ln_series(y) + (k * ln_series(10.0)));
}
function mae(predict: number[], actual: number[]): number {
  let sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(predict))) {
    let diff: number = (predict[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? predict.length + _mochi_idx : _mochi_idx;})()] - actual[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual.length + _mochi_idx : _mochi_idx;})()]);
    sum = (sum + absf(diff));
    i = (i + 1);
  }
  return (sum / _len(predict));
}
function mse(predict: number[], actual: number[]): number {
  let sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(predict))) {
    let diff: number = (predict[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? predict.length + _mochi_idx : _mochi_idx;})()] - actual[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual.length + _mochi_idx : _mochi_idx;})()]);
    sum = (sum + (diff * diff));
    i = (i + 1);
  }
  return (sum / _len(predict));
}
function rmse(predict: number[], actual: number[]): number {
  return sqrtApprox(mse(predict, actual));
}
function rmsle(predict: number[], actual: number[]): number {
  let sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(predict))) {
    let lp: number = ln((predict[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? predict.length + _mochi_idx : _mochi_idx;})()] + 1.0));
    let la: number = ln((actual[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual.length + _mochi_idx : _mochi_idx;})()] + 1.0));
    let diff: number = (lp - la);
    sum = (sum + (diff * diff));
    i = (i + 1);
  }
  return sqrtApprox((sum / _len(predict)));
}
function mbd(predict: number[], actual: number[]): number {
  let diff_sum: number = 0.0;
  let actual_sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(predict))) {
    diff_sum = (diff_sum + (predict[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? predict.length + _mochi_idx : _mochi_idx;})()] - actual[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual.length + _mochi_idx : _mochi_idx;})()]));
    actual_sum = (actual_sum + actual[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  let n: number = _len(predict);
  let numerator: number = (diff_sum / n);
  let denominator: number = (actual_sum / n);
  return ((numerator / denominator) * 100.0);
}
function manual_accuracy(predict: number[], actual: number[]): number {
  let correct: number = 0;
  let i: number = 0;
  while ((i < _len(predict))) {
    if ((predict[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? predict.length + _mochi_idx : _mochi_idx;})()] == actual[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual.length + _mochi_idx : _mochi_idx;})()])) {
      correct = (correct + 1);
    }
    i = (i + 1);
  }
  return (correct / _len(predict));
}
function main() {
  let actual: number[] = [1.0, 2.0, 3.0];
  let predict: number[] = [1.0, 4.0, 3.0];
  console.log(_str(_str(mae(predict, actual))));
  console.log(_str(_str(mse(predict, actual))));
  console.log(_str(_str(rmse(predict, actual))));
  console.log(_str(_str(rmsle([10.0, 2.0, 30.0], [10.0, 10.0, 30.0]))));
  console.log(_str(_str(mbd([2.0, 3.0, 4.0], [1.0, 2.0, 3.0]))));
  console.log(_str(_str(mbd([0.0, 1.0, 1.0], [1.0, 2.0, 3.0]))));
  console.log(_str(_str(manual_accuracy(predict, actual))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

