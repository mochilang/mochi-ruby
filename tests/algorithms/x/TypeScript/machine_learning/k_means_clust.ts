// Generated by Mochi v0.10.63 on 2025-08-11 18:38:07 GMT+7

export interface KMeansResult { centroids: number[][]; assignments: number[]; heterogeneity: number[] }
function distance_sq(a: number[], b: number[]): number {
  let sum: number = 0.0;
  for (let i = 0; i < _len(a); i++) {
    let diff: number = (a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()] - b[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()]);
    sum = (sum + (diff * diff));
  }
  return sum;
}
function assign_clusters(data: number[][], centroids: number[][]): number[] {
  let assignments: number[] = [];
  for (let i = 0; i < _len(data); i++) {
    let best_idx: number = 0;
    let best: number = distance_sq(data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()], centroids[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? centroids.length + _mochi_idx : _mochi_idx;})()]);
    for (let j = 1; j < _len(centroids); j++) {
      let dist: number = distance_sq(data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()], centroids[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? centroids.length + _mochi_idx : _mochi_idx;})()]);
      if ((dist < best)) {
        best = dist;
        best_idx = j;
      }
    }
    assignments.push(best_idx);
  }
  return assignments;
}
function revise_centroids(data: number[][], k: number, assignment: number[]): number[][] {
  let dim: number = _len(data[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()]);
  let sums: number[][] = [];
  let counts: number[] = [];
  for (let i = 0; i < k; i++) {
    let row: number[] = [];
    for (let j = 0; j < dim; j++) {
      row.push(0.0);
    }
    sums.push(row);
    counts.push(0);
  }
  for (let i = 0; i < _len(data); i++) {
    let c: number = assignment[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? assignment.length + _mochi_idx : _mochi_idx;})()];
    counts[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] = (counts[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] + 1);
    for (let j = 0; j < dim; j++) {
      sums[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? sums.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? sums[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? sums.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] = (sums[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? sums.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? sums[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? sums.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] + data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
    }
  }
  let centroids: number[][] = [];
  for (let i = 0; i < k; i++) {
    let row: number[] = [];
    if ((counts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] > 0)) {
      for (let j = 0; j < dim; j++) {
        row.push((sums[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? sums.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? sums[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? sums.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] / counts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()]));
      }
    } else {
      for (let j = 0; j < dim; j++) {
        row.push(0.0);
      }
    }
    centroids.push(row);
  }
  return centroids;
}
function compute_heterogeneity(data: number[][], centroids: number[][], assignment: number[]): number {
  let total: number = 0.0;
  for (let i = 0; i < _len(data); i++) {
    let c: number = assignment[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? assignment.length + _mochi_idx : _mochi_idx;})()];
    total = (total + distance_sq(data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()], centroids[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? centroids.length + _mochi_idx : _mochi_idx;})()]));
  }
  return total;
}
function lists_equal(a: number[], b: number[]): boolean {
  if ((_len(a) != _len(b))) {
    return false;
  }
  for (let i = 0; i < _len(a); i++) {
    if ((a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()] != b[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()])) {
      return false;
    }
  }
  return true;
}
function kmeans(data: number[][], k: number, initial_centroids: number[][], max_iter: number): KMeansResult {
  let centroids: number[][] = initial_centroids;
  let assignment: number[] = [];
  let prev: number[] = [];
  let heterogeneity: number[] = [];
  let iter: number = 0;
  while ((iter < max_iter)) {
    assignment = assign_clusters(data, centroids);
    centroids = revise_centroids(data, k, assignment);
    let h: number = compute_heterogeneity(data, centroids, assignment);
    heterogeneity.push(h);
    if (((iter > 0) && lists_equal(prev, assignment))) {
      break
    }
    prev = assignment;
    iter = (iter + 1);
  }
  return {centroids, "assignments": assignment, heterogeneity};
}
let data: number[][] = [[1.0, 2.0], [1.5, 1.8], [5.0, 8.0], [8.0, 8.0], [1.0, 0.6], [9.0, 11.0]];
let k: number = 3;
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let initial_centroids: number[][]
let result: KMeansResult
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
initial_centroids = [data[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()], data[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()], data[(()=>{const _mochi_idx = Math.trunc(5); return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()]]
result = kmeans(data, k, initial_centroids, 10)
  console.log(_str(_str(result.centroids)));
  console.log(_str(_str(result.assignments)));
  console.log(_str(_str(result.heterogeneity)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

