// Generated by Mochi v0.10.63 on 2025-08-11 18:38:15 GMT+7

let PI: number = 3.141592653589793;
let TWO_PI: number = 6.283185307179586;
let seed: number = 1;
function rand(): number {
  seed = Number((((BigInt(seed) * 1103515245n) + 12345n) % 2147483648n));
  return seed;
}
function random(): number {
  return (rand() / 2147483648.0);
}
function _mod(x: number, m: number): number {
  return (x - (Math.trunc((x / m)) * m));
}
function cos(x: number): number {
  let y: number = (_mod((x + PI), TWO_PI) - PI);
  let y2: number = (y * y);
  let y4: number = (y2 * y2);
  let y6: number = (y4 * y2);
  return (((1.0 - (y2 / 2.0)) + (y4 / 24.0)) - (y6 / 720.0));
}
function sqrtApprox(x: number): number {
  if ((x <= 0.0)) {
    return 0.0;
  }
  let guess: number = x;
  let i: number = 0;
  while ((i < 10)) {
    guess = ((guess + (x / guess)) / 2.0);
    i = (i + 1);
  }
  return guess;
}
function ln(x: number): number {
  let t: number = ((x - 1.0) / (x + 1.0));
  let term: number = t;
  let sum: number = 0.0;
  let n: number = 1;
  while ((n <= 19)) {
    sum = (sum + (term / n));
    term = ((term * t) * t);
    n = (n + 2);
  }
  return (2.0 * sum);
}
function gaussian_distribution(mean: number, std_dev: number, instance_count: number): number[] {
  let res: number[] = [];
  let i: number = 0;
  while ((i < instance_count)) {
    let u1: number = random();
    let u2: number = random();
    let r: number = sqrtApprox((-2.0 * ln(u1)));
    let theta: number = (TWO_PI * u2);
    let z: number = (r * cos(theta));
    res.push((mean + (z * std_dev)));
    i = (i + 1);
  }
  return res;
}
function y_generator(class_count: number, instance_count: number[]): number[] {
  let res: number[] = [];
  let k: number = 0;
  while ((k < class_count)) {
    let i: number = 0;
    while ((i < instance_count[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? instance_count.length + _mochi_idx : _mochi_idx;})()])) {
      res.push(k);
      i = (i + 1);
    }
    k = (k + 1);
  }
  return res;
}
function calculate_mean(instance_count: number, items: number[]): number {
  let total: number = 0.0;
  let i: number = 0;
  while ((i < instance_count)) {
    total = (total + items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? items.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return (total / instance_count);
}
function calculate_probabilities(instance_count: number, total_count: number): number {
  return (instance_count / total_count);
}
function calculate_variance(items: number[][], means: number[], total_count: number): number {
  let squared_diff: number[] = [];
  let i: number = 0;
  while ((i < _len(items))) {
    let j: number = 0;
    while ((j < _len(items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? items.length + _mochi_idx : _mochi_idx;})()]))) {
      let diff: number = (items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? items.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? items.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] - means[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? means.length + _mochi_idx : _mochi_idx;})()]);
      squared_diff.push((diff * diff));
      j = (j + 1);
    }
    i = (i + 1);
  }
  let sum_sq: number = 0.0;
  let k: number = 0;
  while ((k < _len(squared_diff))) {
    sum_sq = (sum_sq + squared_diff[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? squared_diff.length + _mochi_idx : _mochi_idx;})()]);
    k = (k + 1);
  }
  let n_classes: number = _len(means);
  return ((1.0 / (total_count - n_classes)) * sum_sq);
}
function predict_y_values(x_items: number[][], means: number[], variance: number, probabilities: number[]): number[] {
  let results: number[] = [];
  let i: number = 0;
  while ((i < _len(x_items))) {
    let j: number = 0;
    while ((j < _len(x_items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? x_items.length + _mochi_idx : _mochi_idx;})()]))) {
      let temp: number[] = [];
      let k: number = 0;
      while ((k < _len(x_items))) {
        let discr: number = (((x_items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? x_items.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? x_items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? x_items.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * (means[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? means.length + _mochi_idx : _mochi_idx;})()] / variance)) - ((means[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? means.length + _mochi_idx : _mochi_idx;})()] * means[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? means.length + _mochi_idx : _mochi_idx;})()]) / (2.0 * variance))) + ln(probabilities[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? probabilities.length + _mochi_idx : _mochi_idx;})()]));
        temp.push(discr);
        k = (k + 1);
      }
      let max_idx: number = 0;
      let max_val: number = temp[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? temp.length + _mochi_idx : _mochi_idx;})()];
      let t: number = 1;
      while ((t < _len(temp))) {
        if ((temp[(()=>{const _mochi_idx = t; return _mochi_idx < 0 ? temp.length + _mochi_idx : _mochi_idx;})()] > max_val)) {
          max_val = temp[(()=>{const _mochi_idx = t; return _mochi_idx < 0 ? temp.length + _mochi_idx : _mochi_idx;})()];
          max_idx = t;
        }
        t = (t + 1);
      }
      results.push(max_idx);
      j = (j + 1);
    }
    i = (i + 1);
  }
  return results;
}
function accuracy(actual_y: number[], predicted_y: number[]): number {
  let correct: number = 0;
  let i: number = 0;
  while ((i < _len(actual_y))) {
    if ((actual_y[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? actual_y.length + _mochi_idx : _mochi_idx;})()] == predicted_y[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? predicted_y.length + _mochi_idx : _mochi_idx;})()])) {
      correct = (correct + 1);
    }
    i = (i + 1);
  }
  return ((correct / _len(actual_y)) * 100.0);
}
function main() {
  seed = 1;
  let counts: number[] = [20, 20, 20];
  let means: number[] = [5.0, 10.0, 15.0];
  let std_dev: number = 1.0;
  let x: number[][] = [];
  let i: number = 0;
  while ((i < _len(counts))) {
    x.push(gaussian_distribution(means[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? means.length + _mochi_idx : _mochi_idx;})()], std_dev, counts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()]));
    i = (i + 1);
  }
  let y: number[] = y_generator(_len(counts), counts);
  let actual_means: number[] = [];
  i = 0;
  while ((i < _len(counts))) {
    actual_means.push(calculate_mean(counts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()], x[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? x.length + _mochi_idx : _mochi_idx;})()]));
    i = (i + 1);
  }
  let total_count: number = 0;
  i = 0;
  while ((i < _len(counts))) {
    total_count = (total_count + counts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  let probabilities: number[] = [];
  i = 0;
  while ((i < _len(counts))) {
    probabilities.push(calculate_probabilities(counts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()], total_count));
    i = (i + 1);
  }
  let variance: number = calculate_variance(x, actual_means, total_count);
  let predicted: number[] = predict_y_values(x, actual_means, variance, probabilities);
  console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(predicted)));
  console.log(_str(accuracy(y, predicted)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

