// Generated by Mochi v0.10.63 on 2025-08-11 18:39:23 GMT+7

export interface PCAResult { transformed: number[][]; variance_ratio: number[] }
function sqrt(x: number): number {
  let guess: number = ((x > 1.0) ? (x / 2.0) : 1.0);
  let i: number = 0;
  while ((i < 20)) {
    guess = (0.5 * (guess + (x / guess)));
    i = (i + 1);
  }
  return guess;
}
function mean(xs: number[]): number {
  let sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(xs))) {
    sum = (sum + xs[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? xs.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return (sum / _len(xs));
}
function standardize(data: number[][]): number[][] {
  let n_samples: number = _len(data);
  let n_features: number = _len(data[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()]);
  let means: number[] = [];
  let stds: number[] = [];
  let j: number = 0;
  while ((j < n_features)) {
    let column: number[] = [];
    let i: number = 0;
    while ((i < n_samples)) {
      column.push(data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
      i = (i + 1);
    }
    let m: number = mean(column);
    means.push(m);
    let variance: number = 0.0;
    let k: number = 0;
    while ((k < n_samples)) {
      let diff: number = (column[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? column.length + _mochi_idx : _mochi_idx;})()] - m);
      variance = (variance + (diff * diff));
      k = (k + 1);
    }
    stds.push(sqrt((variance / (n_samples - 1))));
    j = (j + 1);
  }
  let standardized: number[][] = [];
  let r: number = 0;
  while ((r < n_samples)) {
    let row: number[] = [];
    let c: number = 0;
    while ((c < n_features)) {
      row.push(((data[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? data[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] - means[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? means.length + _mochi_idx : _mochi_idx;})()]) / stds[(()=>{const _mochi_idx = c; return _mochi_idx < 0 ? stds.length + _mochi_idx : _mochi_idx;})()]));
      c = (c + 1);
    }
    standardized.push(row);
    r = (r + 1);
  }
  return standardized;
}
function covariance_matrix(data: number[][]): number[][] {
  let n_samples: number = _len(data);
  let n_features: number = _len(data[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()]);
  let cov: number[][] = [];
  let i: number = 0;
  while ((i < n_features)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < n_features)) {
      let sum: number = 0.0;
      let k: number = 0;
      while ((k < n_samples)) {
        sum = (sum + (data[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * data[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? data[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]));
        k = (k + 1);
      }
      row.push((sum / (n_samples - 1)));
      j = (j + 1);
    }
    cov.push(row);
    i = (i + 1);
  }
  return cov;
}
function normalize(vec: number[]): number[] {
  let sum: number = 0.0;
  let i: number = 0;
  while ((i < _len(vec))) {
    sum = (sum + (vec[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? vec.length + _mochi_idx : _mochi_idx;})()] * vec[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? vec.length + _mochi_idx : _mochi_idx;})()]));
    i = (i + 1);
  }
  let n = sqrt(sum);
  let res: number[] = [];
  let j: number = 0;
  while ((j < _len(vec))) {
    res.push((vec[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? vec.length + _mochi_idx : _mochi_idx;})()] / n));
    j = (j + 1);
  }
  return res;
}
export interface Eigen { values: number[]; vectors: number[][] }
function eigen_decomposition_2x2(matrix: number[][]): Eigen {
  let a: number = matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
  let b: number = matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
  let c: number = matrix[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
  let diff: number = (a - c);
  let discriminant: number = sqrt(((diff * diff) + ((4.0 * b) * b)));
  let lambda1: number = (((a + c) + discriminant) / 2.0);
  let lambda2: number = (((a + c) - discriminant) / 2.0);
  let v1: number[] = [];
  let v2: number[] = [];
  if ((b != 0.0)) {
    v1 = normalize([(lambda1 - c), b]);
    v2 = normalize([(lambda2 - c), b]);
  } else {
    v1 = [1.0, 0.0];
    v2 = [0.0, 1.0];
  }
  let eigenvalues: number[] = [lambda1, lambda2];
  let eigenvectors: number[][] = [v1, v2];
  if ((eigenvalues[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()] < eigenvalues[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()])) {
    let tmp_val: number = eigenvalues[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()];
    eigenvalues[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()] = eigenvalues[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()];
    eigenvalues[(()=>{const _mochi_idx = 1; return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()] = tmp_val;
    let tmp_vec: number[] = eigenvectors[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? eigenvectors.length + _mochi_idx : _mochi_idx;})()];
    eigenvectors[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? eigenvectors.length + _mochi_idx : _mochi_idx;})()] = eigenvectors[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? eigenvectors.length + _mochi_idx : _mochi_idx;})()];
    eigenvectors[(()=>{const _mochi_idx = 1; return _mochi_idx < 0 ? eigenvectors.length + _mochi_idx : _mochi_idx;})()] = tmp_vec;
  }
  return {"values": eigenvalues, "vectors": eigenvectors};
}
function transpose(matrix: number[][]): number[][] {
  let rows: number = _len(matrix);
  let cols: number = _len(matrix[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()]);
  let trans: number[][] = [];
  let i: number = 0;
  while ((i < cols)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < rows)) {
      row.push(matrix[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? matrix[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? matrix.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
      j = (j + 1);
    }
    trans.push(row);
    i = (i + 1);
  }
  return trans;
}
function matrix_multiply(a: number[][], b: number[][]): number[][] {
  let rows_a: number = _len(a);
  let cols_a: number = _len(a[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()]);
  let rows_b: number = _len(b);
  let cols_b: number = _len(b[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()]);
  if ((cols_a != rows_b)) {
    _panic("Incompatible matrices");
  }
  let result: number[][] = [];
  let i: number = 0;
  while ((i < rows_a)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < cols_b)) {
      let sum: number = 0.0;
      let k: number = 0;
      while ((k < cols_a)) {
        sum = (sum + (a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] * b[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? b[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]));
        k = (k + 1);
      }
      row.push(sum);
      j = (j + 1);
    }
    result.push(row);
    i = (i + 1);
  }
  return result;
}
function apply_pca(data: number[][], n_components: number): PCAResult {
  let standardized: number[][] = standardize(data);
  let cov: number[][] = covariance_matrix(standardized);
  let eig: Eigen = eigen_decomposition_2x2(cov);
  let eigenvalues: number[] = eig.values;
  let eigenvectors: number[][] = eig.vectors;
  let components: number[][] = transpose(eigenvectors);
  let transformed: number[][] = matrix_multiply(standardized, components);
  let total: number = (eigenvalues[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()] + eigenvalues[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()]);
  let ratios: number[] = [];
  let i: number = 0;
  while ((i < n_components)) {
    ratios.push((eigenvalues[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? eigenvalues.length + _mochi_idx : _mochi_idx;})()] / total));
    i = (i + 1);
  }
  return {transformed, "variance_ratio": ratios};
}
let data: number[][] = [[2.5, 2.4], [0.5, 0.7], [2.2, 2.9], [1.9, 2.2], [3.1, 3.0], [2.3, 2.7], [2.0, 1.6], [1.0, 1.1], [1.5, 1.6], [1.1, 0.9]];
let idx: number = 0;
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
let result: PCAResult
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
result = apply_pca(data, 2)
  console.log(_str("Transformed Data (first 5 rows):"));
  while ((idx < 5)) {
    console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(result.transformed[(()=>{const _mochi_idx = idx; return _mochi_idx < 0 ? result.transformed.length + _mochi_idx : _mochi_idx;})()])));
    idx = (idx + 1);
  }
  console.log(_str("Explained Variance Ratio:"));
  console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(result.variance_ratio)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

