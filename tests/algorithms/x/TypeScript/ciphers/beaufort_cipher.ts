// Generated by Mochi v0.10.59 on 2025-08-06 20:07:25 GMT+7

let ALPHABET: string = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
function index_of(ch: string): number {
  for (let i = 0; i < _len(ALPHABET); i++) {
    if ((ALPHABET[i] == ch)) {
      return i;
    }
  }
  return -1;
}
function generate_key(message: string, key: string): string {
  let key_new: string = key;
  let i: number = 0;
  while ((_len(key_new) < _len(message))) {
    key_new = (key_new + key[i]);
    i = (i + 1);
    if ((i == _len(key))) {
      i = 0;
    }
  }
  return key_new;
}
function cipher_text(message: string, key_new: string): string {
  let res: string = "";
  let i: number = 0;
  for (let idx = 0; idx < _len(message); idx++) {
    let ch: string = message[idx];
    if ((ch == " ")) {
      res = (res + " ");
    } else {
      let x: number = (((index_of(ch) - index_of(key_new[i])) + 26) % 26);
      i = (i + 1);
      res = (res + ALPHABET[x]);
    }
  }
  return res;
}
function original_text(cipher: string, key_new: string): string {
  let res: string = "";
  let i: number = 0;
  for (let idx = 0; idx < _len(cipher); idx++) {
    let ch: string = cipher[idx];
    if ((ch == " ")) {
      res = (res + " ");
    } else {
      let x: number = (((index_of(ch) + index_of(key_new[i])) + 26) % 26);
      i = (i + 1);
      res = (res + ALPHABET[x]);
    }
  }
  return res;
}
let message: string = "THE GERMAN ATTACK";
let key: string = "SECRET";
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let key_new: string
let encrypted: string
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
key_new = generate_key(message, key)
encrypted = cipher_text(message, key_new)
  console.log(_str(("Encrypted Text = " + encrypted)));
  console.log(_str(("Original Text = " + original_text(encrypted, key_new))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

