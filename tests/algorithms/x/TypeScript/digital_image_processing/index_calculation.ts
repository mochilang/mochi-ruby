// Generated by Mochi v0.10.61 on 2025-08-08 17:05:42 GMT+7

function ndvi(red: number, nir: number): number {
  return ((nir - red) / (nir + red));
}
function bndvi(blue: number, nir: number): number {
  return ((nir - blue) / (nir + blue));
}
function gndvi(green: number, nir: number): number {
  return ((nir - green) / (nir + green));
}
function ndre(redEdge: number, nir: number): number {
  return ((nir - redEdge) / (nir + redEdge));
}
function ccci(red: number, redEdge: number, nir: number): number {
  return (ndre(redEdge, nir) / ndvi(red, nir));
}
function cvi(red: number, green: number, nir: number): number {
  return ((nir * red) / (green * green));
}
function gli(red: number, green: number, blue: number): number {
  return ((((2.0 * green) - red) - blue) / (((2.0 * green) + red) + blue));
}
function dvi(red: number, nir: number): number {
  return (nir / red);
}
function calc(index: string, red: number, green: number, blue: number, redEdge: number, nir: number): number {
  if ((index == "NDVI")) {
    return ndvi(red, nir);
  }
  if ((index == "BNDVI")) {
    return bndvi(blue, nir);
  }
  if ((index == "GNDVI")) {
    return gndvi(green, nir);
  }
  if ((index == "NDRE")) {
    return ndre(redEdge, nir);
  }
  if ((index == "CCCI")) {
    return ccci(red, redEdge, nir);
  }
  if ((index == "CVI")) {
    return cvi(red, green, nir);
  }
  if ((index == "GLI")) {
    return gli(red, green, blue);
  }
  if ((index == "DVI")) {
    return dvi(red, nir);
  }
  return 0.0;
}
function main() {
  let red: number = 50.0;
  let green: number = 30.0;
  let blue: number = 10.0;
  let redEdge: number = 40.0;
  let nir: number = 100.0;
  console.log(_str(("NDVI=" + _str(ndvi(red, nir)))));
  console.log(_str(("CCCI=" + _str(ccci(red, redEdge, nir)))));
  console.log(_str(("CVI=" + _str(cvi(red, green, nir)))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

