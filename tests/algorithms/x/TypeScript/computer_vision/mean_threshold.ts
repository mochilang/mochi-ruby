// Generated by Mochi v0.10.59 on 2025-08-06 20:30:17 GMT+7

function mean_threshold(image: number[][]): number[][] {
  let height: number = _len(image);
  let width: number = _len(image[Math.trunc(0)]);
  let total: number = 0;
  let i: number = 0;
  while ((i < height)) {
    let j: number = 0;
    while ((j < width)) {
      total = (total + image[i][j]);
      j = (j + 1);
    }
    i = (i + 1);
  }
  let mean: number = Math.trunc(total / (height * width));
  i = 0;
  while ((i < height)) {
    let j: number = 0;
    while ((j < width)) {
      if ((image[i][j] > mean)) {
        image[i][j] = 255;
      } else {
        image[i][j] = 0;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return image;
}
function print_image(image: number[][]) {
  let i: number = 0;
  while ((i < _len(image))) {
    console.log(_str("[" + (image[i]).join(' ') + "]"));
    i = (i + 1);
  }
}
let img: number[][] = [[10, 200, 50], [100, 150, 30], [90, 80, 220]];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let result: number[][]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
result = mean_threshold(img)
  print_image(result);
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

