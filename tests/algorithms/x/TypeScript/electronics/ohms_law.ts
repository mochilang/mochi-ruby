// Generated by Mochi v0.10.65 on 2025-08-14 10:03:16 GMT+7

function ohms_law(voltage: number, current: number, resistance: number): Record<string, number> {
  let zeros: number = 0;
  if ((voltage === 0.0)) {
    zeros = (zeros + 1);
  }
  if ((current === 0.0)) {
    zeros = (zeros + 1);
  }
  if ((resistance === 0.0)) {
    zeros = (zeros + 1);
  }
  if ((zeros !== 1)) {
    console.log(_str("One and only one argument must be 0"));
    return {};
  }
  if ((resistance < 0.0)) {
    console.log(_str("Resistance cannot be negative"));
    return {};
  }
  if ((voltage === 0.0)) {
    return {voltage: (current * resistance)};
  }
  if ((current === 0.0)) {
    return {current: (voltage / resistance)};
  }
  return {resistance: (voltage / current)};
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(JSON.stringify(ohms_law(10.0, 0.0, 5.0), null, 2));
  console.log(JSON.stringify(ohms_law(-10.0, 1.0, 0.0), null, 2));
  console.log(JSON.stringify(ohms_law(0.0, -1.5, 2.0), null, 2));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

