// Generated by Mochi v0.10.65 on 2025-08-14 10:03:11 GMT+7

let ELECTRON_CHARGE: number = 1.6021e-19;
export interface Result { kind: string; value: number }
function electric_conductivity(conductivity: number, electron_conc: number, mobility: number): Result {
  let zero_count: number = 0;
  if ((conductivity === 0.0)) {
    zero_count = (zero_count + 1);
  }
  if ((electron_conc === 0.0)) {
    zero_count = (zero_count + 1);
  }
  if ((mobility === 0.0)) {
    zero_count = (zero_count + 1);
  }
  if ((zero_count !== 1)) {
    _panic("You cannot supply more or less than 2 values");
  }
  if ((conductivity < 0.0)) {
    _panic("Conductivity cannot be negative");
  }
  if ((electron_conc < 0.0)) {
    _panic("Electron concentration cannot be negative");
  }
  if ((mobility < 0.0)) {
    _panic("mobility cannot be negative");
  }
  if ((conductivity === 0.0)) {
    return {kind: "conductivity", value: ((mobility * electron_conc) * ELECTRON_CHARGE)};
  }
  if ((electron_conc === 0.0)) {
    return {kind: "electron_conc", value: (conductivity / (mobility * ELECTRON_CHARGE))};
  }
  return {kind: "mobility", value: (conductivity / (electron_conc * ELECTRON_CHARGE))};
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
let r1: Result
let r2: Result
let r3: Result
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
r1 = electric_conductivity(25.0, 100.0, 0.0)
r2 = electric_conductivity(0.0, 1600.0, 200.0)
r3 = electric_conductivity(1000.0, 0.0, 1200.0)
  console.log(_str(((r1.kind + " ") + _str(r1.value))));
  console.log(_str(((r2.kind + " ") + _str(r2.value))));
  console.log(_str(((r3.kind + " ") + _str(r3.value))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

