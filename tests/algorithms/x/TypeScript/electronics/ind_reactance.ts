// Generated by Mochi v0.10.65 on 2025-08-14 10:03:15 GMT+7

let PI: number = 3.141592653589793;
function ind_reactance(inductance: number, frequency: number, reactance: number): Record<string, number> {
  let zero_count: number = 0;
  if ((inductance === 0.0)) {
    zero_count = (zero_count + 1);
  }
  if ((frequency === 0.0)) {
    zero_count = (zero_count + 1);
  }
  if ((reactance === 0.0)) {
    zero_count = (zero_count + 1);
  }
  if ((zero_count !== 1)) {
    _panic("One and only one argument must be 0");
  }
  if ((inductance < 0.0)) {
    _panic("Inductance cannot be negative");
  }
  if ((frequency < 0.0)) {
    _panic("Frequency cannot be negative");
  }
  if ((reactance < 0.0)) {
    _panic("Inductive reactance cannot be negative");
  }
  if ((inductance === 0.0)) {
    return {inductance: (reactance / ((2.0 * PI) * frequency))};
  }
  if ((frequency === 0.0)) {
    return {frequency: (reactance / ((2.0 * PI) * inductance))};
  }
  return {reactance: (((2.0 * PI) * frequency) * inductance)};
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(ind_reactance(0.0, 10000.0, 50.0)));
  console.log(_str(ind_reactance(0.035, 0.0, 50.0)));
  console.log(_str(ind_reactance(3.5e-05, 1000.0, 0.0)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

