// Generated by Mochi v0.10.72 on 2025-08-25 00:09:46 GMT+7

function panic(msg: string) {
  console.log(_str(msg));
}
export interface FixedPriorityQueue { queues: number[][] }
function fpq_new(): FixedPriorityQueue {
  return {queues: [[], [], []]};
}
function fpq_enqueue(fpq: FixedPriorityQueue, priority: number, data: number): FixedPriorityQueue {
  if (((priority < 0) || (priority >= _len(fpq.queues)))) {
    panic("Valid priorities are 0, 1, and 2");
    return fpq;
  }
  if ((_len(fpq.queues[(()=>{const _mochi_idx = priority; return _mochi_idx < 0 ? fpq.queues.length + _mochi_idx : _mochi_idx;})()]) >= 100)) {
    panic("Maximum queue size is 100");
    return fpq;
  }
  let qs: number[][] = fpq.queues;
  qs[(()=>{const _mochi_idx = priority; return _mochi_idx < 0 ? qs.length + _mochi_idx : _mochi_idx;})()].push(data);
  fpq.queues = qs;
  return fpq;
}
export interface FPQDequeueResult { queue: FixedPriorityQueue; value: number }
function fpq_dequeue(fpq: FixedPriorityQueue): FPQDequeueResult {
  let qs: number[][] = fpq.queues;
  let i: number = 0;
  while ((i < _len(qs))) {
    let q: number[] = qs[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? qs.length + _mochi_idx : _mochi_idx;})()];
    if ((_len(q) > 0)) {
      let val: number = q[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? q.length + _mochi_idx : _mochi_idx;})()];
      let new_q: number[] = [];
      let j: number = 1;
      while ((j < _len(q))) {
        new_q.push(q[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? q.length + _mochi_idx : _mochi_idx;})()]);
        j = (j + 1);
      }
      qs[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? qs.length + _mochi_idx : _mochi_idx;})()] = new_q;
      fpq.queues = qs;
      return {queue: fpq, value: val};
    }
    i = (i + 1);
  }
  panic("All queues are empty");
  return {queue: fpq, value: 0};
}
function fpq_to_string(fpq: FixedPriorityQueue): string {
  let lines: string[] = [];
  let i: number = 0;
  while ((i < _len(fpq.queues))) {
    let q_str: string = "[";
    let q: number[] = fpq.queues[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? fpq.queues.length + _mochi_idx : _mochi_idx;})()];
    let j: number = 0;
    while ((j < _len(q))) {
      if ((j > 0)) {
        q_str = (q_str + ", ");
      }
      q_str = (q_str + _str(q[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? q.length + _mochi_idx : _mochi_idx;})()]));
      j = (j + 1);
    }
    q_str = (q_str + "]");
    lines.push(((("Priority " + _str(i)) + ": ") + q_str));
    i = (i + 1);
  }
  let res: string = "";
  i = 0;
  while ((i < _len(lines))) {
    if ((i > 0)) {
      res = (res + "\n");
    }
    res = (res + lines[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? lines.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return res;
}
export interface ElementPriorityQueue { queue: number[] }
function epq_new(): ElementPriorityQueue {
  return {queue: []};
}
function epq_enqueue(epq: ElementPriorityQueue, data: number): ElementPriorityQueue {
  if ((_len(epq.queue) >= 100)) {
    panic("Maximum queue size is 100");
    return epq;
  }
  epq.queue.push(data);
  return epq;
}
export interface EPQDequeueResult { queue: ElementPriorityQueue; value: number }
function epq_dequeue(epq: ElementPriorityQueue): EPQDequeueResult {
  if ((_len(epq.queue) === 0)) {
    panic("The queue is empty");
    return {queue: epq, value: 0};
  }
  let min_val: number = epq.queue[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? epq.queue.length + _mochi_idx : _mochi_idx;})()];
  let idx: number = 0;
  let i: number = 1;
  while ((i < _len(epq.queue))) {
    let v: number = epq.queue[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? epq.queue.length + _mochi_idx : _mochi_idx;})()];
    if ((v < min_val)) {
      min_val = v;
      idx = i;
    }
    i = (i + 1);
  }
  let new_q: number[] = [];
  i = 0;
  while ((i < _len(epq.queue))) {
    if ((i !== idx)) {
      new_q.push(epq.queue[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? epq.queue.length + _mochi_idx : _mochi_idx;})()]);
    }
    i = (i + 1);
  }
  epq.queue = new_q;
  return {queue: epq, value: min_val};
}
function epq_to_string(epq: ElementPriorityQueue): string {
  return _str(epq.queue);
}
function fixed_priority_queue() {
  let fpq: FixedPriorityQueue = fpq_new();
  fpq = fpq_enqueue(fpq, 0, 10);
  fpq = fpq_enqueue(fpq, 1, 70);
  fpq = fpq_enqueue(fpq, 0, 100);
  fpq = fpq_enqueue(fpq, 2, 1);
  fpq = fpq_enqueue(fpq, 2, 5);
  fpq = fpq_enqueue(fpq, 1, 7);
  fpq = fpq_enqueue(fpq, 2, 4);
  fpq = fpq_enqueue(fpq, 1, 64);
  fpq = fpq_enqueue(fpq, 0, 128);
  console.log(_str(fpq_to_string(fpq)));
  let res: FPQDequeueResult = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  console.log(_str(fpq_to_string(fpq)));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
  res = fpq_dequeue(fpq);
  fpq = res.queue;
  console.log(_str(res.value));
}
function element_priority_queue() {
  let epq: ElementPriorityQueue = epq_new();
  epq = epq_enqueue(epq, 10);
  epq = epq_enqueue(epq, 70);
  epq = epq_enqueue(epq, 100);
  epq = epq_enqueue(epq, 1);
  epq = epq_enqueue(epq, 5);
  epq = epq_enqueue(epq, 7);
  epq = epq_enqueue(epq, 4);
  epq = epq_enqueue(epq, 64);
  epq = epq_enqueue(epq, 128);
  console.log(_str(epq_to_string(epq)));
  let res: EPQDequeueResult = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  console.log(_str(epq_to_string(epq)));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
  res = epq_dequeue(epq);
  epq = res.queue;
  console.log(_str(res.value));
}
function main() {
  fixed_priority_queue();
  element_priority_queue();
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

