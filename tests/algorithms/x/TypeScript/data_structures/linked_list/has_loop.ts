// Generated by Mochi v0.10.72 on 2025-08-25 00:09:25 GMT+7

export interface Node { data: number; next: number }
function has_loop(nodes: Node[], head: number): boolean {
  let slow: number = head;
  let fast: number = head;
  while ((fast !== (0 - 1))) {
    let fast_node1: Node = nodes[(()=>{const _mochi_idx = fast; return _mochi_idx < 0 ? nodes.length + _mochi_idx : _mochi_idx;})()];
    if ((fast_node1.next === (0 - 1))) {
      return false;
    }
    let fast_node2: Node = nodes[(()=>{const _mochi_idx = Math.trunc(fast_node1.next); return _mochi_idx < 0 ? nodes.length + _mochi_idx : _mochi_idx;})()];
    if ((fast_node2.next === (0 - 1))) {
      return false;
    }
    let slow_node: Node = nodes[(()=>{const _mochi_idx = slow; return _mochi_idx < 0 ? nodes.length + _mochi_idx : _mochi_idx;})()];
    slow = slow_node.next;
    fast = fast_node2.next;
    if ((slow === fast)) {
      return true;
    }
  }
  return false;
}
function make_nodes(values: number[]): Node[] {
  let nodes: Node[] = [];
  let i: number = 0;
  while ((i < _len(values))) {
    let next_idx = (_equal(i, (_len(values) - 1)) ? (0 - 1) : (i + 1));
    nodes.push({data: values[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? values.length + _mochi_idx : _mochi_idx;})()], next: next_idx});
    i = (i + 1);
  }
  return nodes;
}
function main() {
  let list1: Node[] = make_nodes([1, 2, 3, 4]);
  console.log(_str(has_loop(list1, 0)));
  list1[(()=>{const _mochi_idx = 3; return _mochi_idx < 0 ? list1.length + _mochi_idx : _mochi_idx;})()].next = 1;
  console.log(_str(has_loop(list1, 0)));
  let list2: Node[] = make_nodes([5, 6, 5, 6]);
  console.log(_str(has_loop(list2, 0)));
  let list3: Node[] = make_nodes([1]);
  console.log(_str(has_loop(list3, 0)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _equal(a: unknown, b: unknown): boolean {
  if (typeof a === 'number' && typeof b === 'number') {
    return Math.abs(a - b) < 1e-6;
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === 'object' && typeof b === 'object') {
    const ak = Object.keys(a); const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) { if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) return false; }
    return true;
  }
  return a === b;
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

