// Generated by Mochi v0.10.72 on 2025-08-25 00:09:16 GMT+7

export interface CircularLinkedList { data: number[] }
function empty_list(): CircularLinkedList {
  return {data: []};
}
function length(list: CircularLinkedList): number {
  return _len(list.data);
}
function is_empty(list: CircularLinkedList): boolean {
  return (_len(list.data) === 0);
}
function to_string(list: CircularLinkedList): string {
  if ((_len(list.data) === 0)) {
    return "";
  }
  let s: string = _str(list.data[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? list.data.length + _mochi_idx : _mochi_idx;})()]);
  let i: number = 1;
  while ((i < _len(list.data))) {
    s = ((s + "->") + _str(list.data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list.data.length + _mochi_idx : _mochi_idx;})()]));
    i = (i + 1);
  }
  return s;
}
function insert_nth(list: CircularLinkedList, index: number, value: number): CircularLinkedList {
  if (((index < 0) || (index > _len(list.data)))) {
    _panic("index out of range");
  }
  let res: number[] = [];
  let i: number = 0;
  while ((i < index)) {
    res.push(list.data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list.data.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  res.push(value);
  while ((i < _len(list.data))) {
    res.push(list.data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list.data.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return {data: res};
}
function insert_head(list: CircularLinkedList, value: number): CircularLinkedList {
  return insert_nth(list, 0, value);
}
function insert_tail(list: CircularLinkedList, value: number): CircularLinkedList {
  return insert_nth(list, _len(list.data), value);
}
export interface DeleteResult { list: CircularLinkedList; value: number }
function delete_nth(list: CircularLinkedList, index: number): DeleteResult {
  if (((index < 0) || (index >= _len(list.data)))) {
    _panic("index out of range");
  }
  let res: number[] = [];
  let i: number = 0;
  let val: number = 0;
  while ((i < _len(list.data))) {
    if ((i === index)) {
      val = list.data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list.data.length + _mochi_idx : _mochi_idx;})()];
    } else {
      res.push(list.data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? list.data.length + _mochi_idx : _mochi_idx;})()]);
    }
    i = (i + 1);
  }
  return {list: {data: res}, value: val};
}
function delete_front(list: CircularLinkedList): DeleteResult {
  return delete_nth(list, 0);
}
function delete_tail(list: CircularLinkedList): DeleteResult {
  return delete_nth(list, (_len(list.data) - 1));
}
function main() {
  let cll: CircularLinkedList = empty_list();
  let i: number = 0;
  while ((i < 5)) {
    cll = insert_tail(cll, (i + 1));
    i = (i + 1);
  }
  console.log(_str(to_string(cll)));
  cll = insert_tail(cll, 6);
  console.log(_str(to_string(cll)));
  cll = insert_head(cll, 0);
  console.log(_str(to_string(cll)));
  let res: DeleteResult = delete_front(cll);
  cll = res.list;
  console.log(_str(res.value));
  res = delete_tail(cll);
  cll = res.list;
  console.log(_str(res.value));
  res = delete_nth(cll, 2);
  cll = res.list;
  console.log(_str(res.value));
  console.log(_str(to_string(cll)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

