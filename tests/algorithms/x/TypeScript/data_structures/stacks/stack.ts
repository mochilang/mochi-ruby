// Generated by Mochi v0.10.72 on 2025-08-25 00:10:06 GMT+7

export interface Stack { items: number[]; limit: number }
function make_stack(limit: number): Stack {
  return {items: [], limit};
}
function is_empty(s: Stack): boolean {
  return (_len(s.items) === 0);
}
function size(s: Stack): number {
  return _len(s.items);
}
function is_full(s: Stack): boolean {
  return (_len(s.items) >= s.limit);
}
function push(s: Stack, item: number) {
  if (is_full(s)) {
    _panic("stack overflow");
  }
  s.items.push(item);
}
function pop(s: Stack): number {
  if (is_empty(s)) {
    _panic("stack underflow");
  }
  let n: number = _len(s.items);
  let val: number = s.items[(()=>{const _mochi_idx = Math.trunc((n - 1)); return _mochi_idx < 0 ? s.items.length + _mochi_idx : _mochi_idx;})()];
  s.items = s.items.slice(0, (n - 1));
  return val;
}
function peek(s: Stack): number {
  if (is_empty(s)) {
    _panic("peek from empty stack");
  }
  return s.items[(()=>{const _mochi_idx = Math.trunc((_len(s.items) - 1)); return _mochi_idx < 0 ? s.items.length + _mochi_idx : _mochi_idx;})()];
}
function contains(s: Stack, item: number): boolean {
  let i: number = 0;
  while ((i < _len(s.items))) {
    if ((s.items[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? s.items.length + _mochi_idx : _mochi_idx;})()] === item)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function stack_repr(s: Stack): string {
  return _str(s.items);
}
function main() {
  let s: Stack = make_stack(5);
  console.log(_str(is_empty(s)));
  push(s, 0);
  push(s, 1);
  push(s, 2);
  console.log(_str(peek(s)));
  console.log(_str(size(s)));
  console.log(_str(is_full(s)));
  push(s, 3);
  push(s, 4);
  console.log(_str(is_full(s)));
  console.log(_str(stack_repr(s)));
  console.log(_str(pop(s)));
  console.log(_str(peek(s)));
  console.log(_str(contains(s, 1)));
  console.log(_str(contains(s, 9)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

