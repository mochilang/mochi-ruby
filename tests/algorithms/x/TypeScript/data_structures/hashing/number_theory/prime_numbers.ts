// Generated by Mochi v0.10.60 on 2025-08-08 10:44:58 GMT+7

function isPrime(_number: number): boolean {
  if ((_number < 2)) {
    return false;
  }
  if ((_number < 4)) {
    return true;
  }
  if (((_number % 2) == 0)) {
    return false;
  }
  let i: number = 3;
  while (((i * i) <= _number)) {
    if (((_number % i) == 0)) {
      return false;
    }
    i = (i + 2);
  }
  return true;
}
function nextPrime(value: number, factor: number, desc: boolean): number {
  let v: number = (value * factor);
  let firstValue: number = v;
  while (!isPrime(v)) {
    if (desc) {
      v = (v - 1);
    } else {
      v = (v + 1);
    }
  }
  if ((v == firstValue)) {
    if (desc) {
      return nextPrime((v - 1), 1, desc);
    } else {
      return nextPrime((v + 1), 1, desc);
    }
  }
  return v;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(isPrime(0)));
  console.log(_str(isPrime(1)));
  console.log(_str(isPrime(2)));
  console.log(_str(isPrime(3)));
  console.log(_str(isPrime(27)));
  console.log(_str(isPrime(87)));
  console.log(_str(isPrime(563)));
  console.log(_str(isPrime(2999)));
  console.log(_str(isPrime(67483)));
  console.log(_str(nextPrime(14, 1, false)));
  console.log(_str(nextPrime(14, 1, true)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

