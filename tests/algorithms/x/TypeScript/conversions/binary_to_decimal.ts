// Generated by Mochi v0.10.59 on 2025-08-06 20:39:01 GMT+7

function trim(s: string): string {
  let start: number = 0;
  while ((start < _len(s))) {
    let ch: string = s.slice(start, (start + 1));
    if (((((ch != " ") && (ch != "\n")) && (ch != "\t")) && (ch != "\r"))) {
      break
    }
    start = (start + 1);
  }
  let end: number = _len(s);
  while ((end > start)) {
    let ch: string = s.slice((end - 1), end);
    if (((((ch != " ") && (ch != "\n")) && (ch != "\t")) && (ch != "\r"))) {
      break
    }
    end = (end - 1);
  }
  return (s).substring(start, end);
}
function bin_to_decimal(bin_string: string): number {
  let trimmed: string = trim(bin_string);
  if ((trimmed == "")) {
    panic("Empty string was passed to the function");
  }
  let is_negative: boolean = false;
  let s: string = trimmed;
  if ((s.slice(0, 1) == "-")) {
    is_negative = true;
    s = (s).substring(1, _len(s));
  }
  let i: number = 0;
  while ((i < _len(s))) {
    let c: string = s.slice(i, (i + 1));
    if (((c != "0") && (c != "1"))) {
      panic("Non-binary value was passed to the function");
    }
    i = (i + 1);
  }
  let decimal_number: number = 0;
  i = 0;
  while ((i < _len(s))) {
    let c: string = s.slice(i, (i + 1));
    let digit: number = Math.trunc(c);
    decimal_number = ((2 * decimal_number) + digit);
    i = (i + 1);
  }
  if (is_negative) {
    return -decimal_number;
  }
  return decimal_number;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(bin_to_decimal("101"))));
  console.log(_str(_str(bin_to_decimal(" 1010   "))));
  console.log(_str(_str(bin_to_decimal("-11101"))));
  console.log(_str(_str(bin_to_decimal("0"))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

