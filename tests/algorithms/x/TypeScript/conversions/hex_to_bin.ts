// Generated by Mochi v0.10.59 on 2025-08-06 20:45:54 GMT+7

function panic(msg: string) {
  console.log(_str(msg));
}
function trim_spaces(s: string): string {
  let start: number = 0;
  let end: number = _len(s);
  while (((start < end) && ((s).substring(start, (start + 1)) == " "))) {
    start = (start + 1);
  }
  while (((end > start) && ((s).substring((end - 1), end) == " "))) {
    end = (end - 1);
  }
  return (s).substring(start, end);
}
function hex_digit_value(ch: string): number {
  if ((ch == "0")) {
    return 0;
  }
  if ((ch == "1")) {
    return 1;
  }
  if ((ch == "2")) {
    return 2;
  }
  if ((ch == "3")) {
    return 3;
  }
  if ((ch == "4")) {
    return 4;
  }
  if ((ch == "5")) {
    return 5;
  }
  if ((ch == "6")) {
    return 6;
  }
  if ((ch == "7")) {
    return 7;
  }
  if ((ch == "8")) {
    return 8;
  }
  if ((ch == "9")) {
    return 9;
  }
  if (((ch == "a") || (ch == "A"))) {
    return 10;
  }
  if (((ch == "b") || (ch == "B"))) {
    return 11;
  }
  if (((ch == "c") || (ch == "C"))) {
    return 12;
  }
  if (((ch == "d") || (ch == "D"))) {
    return 13;
  }
  if (((ch == "e") || (ch == "E"))) {
    return 14;
  }
  if (((ch == "f") || (ch == "F"))) {
    return 15;
  }
  panic("Invalid value was passed to the function");
}
function hex_to_bin(hex_num: string): number {
  let trimmed: string = trim_spaces(hex_num);
  if ((_len(trimmed) == 0)) {
    panic("No value was passed to the function");
  }
  let s: string = trimmed;
  let is_negative: boolean = false;
  if (((s).substring(0, 1) == "-")) {
    is_negative = true;
    s = (s).substring(1, _len(s));
  }
  let int_num: number = 0;
  let i: number = 0;
  while ((i < _len(s))) {
    let ch: string = (s).substring(i, (i + 1));
    let val: number = hex_digit_value(ch);
    int_num = ((int_num * 16) + val);
    i = (i + 1);
  }
  let bin_str: string = "";
  let n: number = int_num;
  if ((n == 0)) {
    bin_str = "0";
  }
  while ((n > 0)) {
    bin_str = (_str((n % 2)) + bin_str);
    n = Math.trunc(n / 2);
  }
  let result: number = Math.trunc(bin_str);
  if (is_negative) {
    result = -result;
  }
  return result;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(hex_to_bin("AC"))));
  console.log(_str(_str(hex_to_bin("9A4"))));
  console.log(_str(_str(hex_to_bin("   12f   "))));
  console.log(_str(_str(hex_to_bin("FfFf"))));
  console.log(_str(_str(hex_to_bin("-fFfF"))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

