// Generated by Mochi v0.10.70 on 2025-08-22 23:16:00 GMT+7

function max_tasks(tasks_info: number[][]): number[] {
  let order: number[] = [];
  let i: number = 0;
  while ((i < _len(tasks_info))) {
    order.push(i);
    i = (i + 1);
  }
  let n: number = _len(order);
  i = 0;
  while ((i < n)) {
    let j: bigint = (i + 1);
    while ((j < n)) {
      if ((tasks_info[(()=>{const _mochi_idx = Math.trunc(order[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()]); return _mochi_idx < 0 ? tasks_info.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? tasks_info[(()=>{const _mochi_idx = Math.trunc(order[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()]); return _mochi_idx < 0 ? tasks_info.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()] > tasks_info[(()=>{const _mochi_idx = Math.trunc(order[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()]); return _mochi_idx < 0 ? tasks_info.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? tasks_info[(()=>{const _mochi_idx = Math.trunc(order[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()]); return _mochi_idx < 0 ? tasks_info.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()])) {
        let tmp: number = order[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()];
        order[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()] = order[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()];
        order[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()] = tmp;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  let result: number[] = [];
  let pos: number = 1;
  i = 0;
  while ((i < n)) {
    let id: number = order[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? order.length + _mochi_idx : _mochi_idx;})()];
    let deadline: number = tasks_info[(()=>{const _mochi_idx = id; return _mochi_idx < 0 ? tasks_info.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? tasks_info[(()=>{const _mochi_idx = id; return _mochi_idx < 0 ? tasks_info.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()];
    if ((deadline >= pos)) {
      result.push(id);
    }
    i = (i + 1);
    pos = (pos + 1);
  }
  return result;
}
function main() {
  let ex1: number[][] = [[4, 20], [1, 10], [1, 40], [1, 30]];
  let ex2: number[][] = [[1, 10], [2, 20], [3, 30], [2, 40]];
  console.log(_str(max_tasks(ex1)));
  console.log(_str(max_tasks(ex2)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

