// Generated by Mochi v0.10.70 on 2025-08-22 23:16:36 GMT+7

function partition(data: number[], pivot: number): number[][] {
  let less: number[] = [];
  let equal: number[] = [];
  let greater: number[] = [];
  for (let i = 0; i < _len(data); i++) {
    let v: number = data[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? data.length + _mochi_idx : _mochi_idx;})()];
    if ((v < pivot)) {
      less.push(v);
    } else {
      if ((v > pivot)) {
        greater.push(v);
      } else {
        equal.push(v);
      }
    }
  }
  return [less, equal, greater];
}
function quick_select(items: number[], index: number): number {
  if (((index < 0) || (index >= _len(items)))) {
    return -1;
  }
  let pivot: number = items[(()=>{const _mochi_idx = Math.trunc(Math.trunc(_len(items) / 2)); return _mochi_idx < 0 ? items.length + _mochi_idx : _mochi_idx;})()];
  let parts: number[][] = partition(items, pivot);
  let smaller: number[] = parts[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? parts.length + _mochi_idx : _mochi_idx;})()];
  let equal: number[] = parts[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? parts.length + _mochi_idx : _mochi_idx;})()];
  let larger: number[] = parts[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? parts.length + _mochi_idx : _mochi_idx;})()];
  let count = _len(equal);
  let m: number = _len(smaller);
  if (((m <= index) && (index < (m + count)))) {
    return pivot;
  } else {
    if ((index < m)) {
      return quick_select(smaller, index);
    } else {
      return quick_select(larger, (index - (m + count)));
    }
  }
}
function median(items: number[]): number {
  let n: number = _len(items);
  let mid: number = Math.trunc(n / 2);
  if (((n % 2) !== 0)) {
    return (1.0 * quick_select(items, mid));
  } else {
    let low: number = quick_select(items, (mid - 1));
    let high: number = quick_select(items, mid);
    return ((1.0 * (low + high)) / 2.0);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(quick_select([2, 4, 5, 7, 899, 54, 32], 5)));
  console.log(_str(quick_select([2, 4, 5, 7, 899, 54, 32], 1)));
  console.log(_str(quick_select([5, 4, 3, 2], 2)));
  console.log(_str(quick_select([3, 5, 7, 10, 2, 12], 3)));
  console.log(_str(median([3, 2, 2, 9, 9])));
  console.log(_str(median([2, 2, 9, 9, 9, 3])));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

