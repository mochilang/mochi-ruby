// Generated by Mochi v0.10.59 on 2025-08-07 09:47:32 GMT+7

function find_index(xs: string[], x: string): number {
  let i: number = 0;
  while ((i < _len(xs))) {
    if ((xs[(i) < 0 ? xs.length + (i) : i] == x)) {
      return i;
    }
    i = (i + 1);
  }
  return -1;
}
function get_word_pattern(word: string): string {
  let w: string = word.toUpperCase();
  let letters: string[] = [];
  let numbers: string[] = [];
  let next_num: number = 0;
  let res: string = "";
  let i: number = 0;
  while ((i < _len(w))) {
    let ch: string = w[(i) < 0 ? w.length + (i) : i];
    let idx: number = find_index(letters, ch);
    let num_str: string = "";
    if ((idx >= 0)) {
      num_str = numbers[(idx) < 0 ? numbers.length + (idx) : idx];
    } else {
      num_str = _str(next_num);
      letters.push(ch);
      numbers.push(num_str);
      next_num = (next_num + 1);
    }
    if ((i > 0)) {
      res = (res + ".");
    }
    res = (res + num_str);
    i = (i + 1);
  }
  return res;
}
function main() {
  console.log(_str(get_word_pattern("")));
  console.log(_str(get_word_pattern(" ")));
  console.log(_str(get_word_pattern("pattern")));
  console.log(_str(get_word_pattern("word pattern")));
  console.log(_str(get_word_pattern("get word pattern")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

