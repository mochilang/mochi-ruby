// Generated by Mochi v0.10.70 on 2025-08-23 13:54:47 GMT+7

function get_failure_array(pattern: string): number[] {
  let failure: number[] = [0];
  let i: number = 0;
  let j: number = 1;
  while ((j < _len(pattern))) {
    if (((pattern).substring(i, (i + 1)) === (pattern).substring(j, (j + 1)))) {
      i = (i + 1);
    } else {
      if ((i > 0)) {
        i = failure[(()=>{const _mochi_idx = Math.trunc((i - 1)); return _mochi_idx < 0 ? failure.length + _mochi_idx : _mochi_idx;})()];
        continue
      }
    }
    j = (j + 1);
    failure.push(i);
  }
  return failure;
}
function knuth_morris_pratt(text: string, pattern: string): number {
  let failure: number[] = get_failure_array(pattern);
  let i: number = 0;
  let j: number = 0;
  while ((i < _len(text))) {
    if (((pattern).substring(j, (j + 1)) === (text).substring(i, (i + 1)))) {
      if ((j === (_len(pattern) - 1))) {
        return (i - j);
      }
      j = (j + 1);
    } else {
      if ((j > 0)) {
        j = failure[(()=>{const _mochi_idx = Math.trunc((j - 1)); return _mochi_idx < 0 ? failure.length + _mochi_idx : _mochi_idx;})()];
        continue
      }
    }
    i = (i + 1);
  }
  return -1;
}
let text: string = "abcxabcdabxabcdabcdabcy";
let pattern: string = "abcdabcy";
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(knuth_morris_pratt(text, pattern)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

