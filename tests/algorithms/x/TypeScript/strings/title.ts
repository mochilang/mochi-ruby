// Generated by Mochi v0.10.70 on 2025-08-23 13:55:06 GMT+7

let lower: string = "abcdefghijklmnopqrstuvwxyz";
let upper: string = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
function index_of(s: string, ch: string): number {
  let i: number = 0;
  while ((i < _len(s))) {
    if ((s[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()] === ch)) {
      return i;
    }
    i = (i + 1);
  }
  return -1;
}
function to_title_case(word: string): string {
  if ((_len(word) === 0)) {
    return "";
  }
  let first = word.slice(0, 1);
  let idx = index_of(lower, first);
  let result = ((idx >= 0) ? upper.slice(idx, (idx + 1)) : first);
  let i: number = 1;
  while ((i < _len(word))) {
    let ch: string = word.slice(i, (i + 1));
    let uidx = index_of(upper, ch);
    if ((uidx >= 0)) {
      result = (result + lower.slice(uidx, (uidx + 1)));
    } else {
      result = (result + ch);
    }
    i = (i + 1);
  }
  return result;
}
function split_words(s: string): string[] {
  let words: string[] = [];
  let current: string = "";
  let i: number = 0;
  while ((i < _len(s))) {
    let ch: string = s[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()];
    if ((ch === " ")) {
      if ((_len(current) > 0)) {
        words.push(current);
        current = "";
      }
    } else {
      current = (current + ch);
    }
    i = (i + 1);
  }
  if ((_len(current) > 0)) {
    words.push(current);
  }
  return words;
}
function sentence_to_title_case(sentence: string): string {
  let words: string[] = split_words(sentence);
  let res: string = "";
  let i: number = 0;
  while ((i < _len(words))) {
    res = (res + to_title_case(words[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? words.length + _mochi_idx : _mochi_idx;})()]));
    if (((i + 1) < _len(words))) {
      res = (res + " ");
    }
    i = (i + 1);
  }
  return res;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(to_title_case("Aakash")));
  console.log(_str(to_title_case("aakash")));
  console.log(_str(to_title_case("AAKASH")));
  console.log(_str(to_title_case("aAkAsH")));
  console.log(_str(sentence_to_title_case("Aakash Giri")));
  console.log(_str(sentence_to_title_case("aakash giri")));
  console.log(_str(sentence_to_title_case("AAKASH GIRI")));
  console.log(_str(sentence_to_title_case("aAkAsH gIrI")));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

