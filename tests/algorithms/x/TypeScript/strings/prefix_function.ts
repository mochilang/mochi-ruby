// Generated by Mochi v0.10.70 on 2025-08-23 13:54:56 GMT+7

function prefix_function(s: string): number[] {
  let pi: number[] = [];
  let i: number = 0;
  while ((i < _len(s))) {
    pi.push(0);
    i = (i + 1);
  }
  i = 1;
  while ((i < _len(s))) {
    let j: number = pi[(()=>{const _mochi_idx = Math.trunc((i - 1)); return _mochi_idx < 0 ? pi.length + _mochi_idx : _mochi_idx;})()];
    while (((j > 0) && (s[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()] !== s[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()]))) {
      j = pi[(()=>{const _mochi_idx = Math.trunc((j - 1)); return _mochi_idx < 0 ? pi.length + _mochi_idx : _mochi_idx;})()];
    }
    if ((s[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()] === s[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()])) {
      j = (j + 1);
    }
    pi[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? pi.length + _mochi_idx : _mochi_idx;})()] = j;
    i = (i + 1);
  }
  return pi;
}
function longest_prefix(s: string): number {
  let pi: number[] = prefix_function(s);
  let max_val: number = 0;
  let i: number = 0;
  while ((i < _len(pi))) {
    if ((pi[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? pi.length + _mochi_idx : _mochi_idx;})()] > max_val)) {
      max_val = pi[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? pi.length + _mochi_idx : _mochi_idx;})()];
    }
    i = (i + 1);
  }
  return max_val;
}
function list_eq_int(a: number[], b: number[]): boolean {
  if ((_len(a) !== _len(b))) {
    return false;
  }
  let i: number = 0;
  while ((i < _len(a))) {
    if ((a[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.length + _mochi_idx : _mochi_idx;})()] !== b[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? b.length + _mochi_idx : _mochi_idx;})()])) {
      return false;
    }
    i = (i + 1);
  }
  return true;
}
function test_prefix_function() {
  let s1: string = "aabcdaabc";
  let expected1: number[] = [0, 1, 0, 0, 0, 1, 2, 3, 4];
  let r1: number[] = prefix_function(s1);
  if (!list_eq_int(r1, expected1)) {
    _panic("prefix_function aabcdaabc failed");
  }
  let s2: string = "asdasdad";
  let expected2: number[] = [0, 0, 0, 1, 2, 3, 4, 0];
  let r2: number[] = prefix_function(s2);
  if (!list_eq_int(r2, expected2)) {
    _panic("prefix_function asdasdad failed");
  }
}
function test_longest_prefix() {
  if ((longest_prefix("aabcdaabc") !== 4)) {
    _panic("longest_prefix example1 failed");
  }
  if ((longest_prefix("asdasdad") !== 4)) {
    _panic("longest_prefix example2 failed");
  }
  if ((longest_prefix("abcab") !== 2)) {
    _panic("longest_prefix example3 failed");
  }
}
function main() {
  test_prefix_function();
  test_longest_prefix();
  let r1: number[] = prefix_function("aabcdaabc");
  let r2: number[] = prefix_function("asdasdad");
  console.log(_str(r1));
  console.log(_str(r2));
  console.log(_str(longest_prefix("aabcdaabc")));
  console.log(_str(longest_prefix("abcab")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

