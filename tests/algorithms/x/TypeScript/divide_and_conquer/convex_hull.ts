// Generated by Mochi v0.10.61 on 2025-08-08 17:05:54 GMT+7

export interface Point { x: number; y: number }
function cross(o: Point, a: Point, b: Point): number {
  return (((a.x - o.x) * (b.y - o.y)) - ((a.y - o.y) * (b.x - o.x)));
}
function sortPoints(ps: Point[]): Point[] {
  let arr: Point[] = ps;
  let n: number = _len(arr);
  let i: number = 0;
  while ((i < n)) {
    let j: number = 0;
    while ((j < (n - 1))) {
      let p: Point = arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
      let q: Point = arr[(()=>{const _mochi_idx = Math.trunc((j + 1)); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
      if (((p.x > q.x) || ((p.x == q.x) && (p.y > q.y)))) {
        arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = q;
        arr[(()=>{const _mochi_idx = (j + 1); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = p;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return arr;
}
function convex_hull(ps: Point[]): Point[] {
  ps = sortPoints(ps);
  let lower: Point[] = [];
  for (const p of ps) {
    while (((_len(lower) >= 2) && (cross(lower[(()=>{const _mochi_idx = Math.trunc((_len(lower) - 2)); return _mochi_idx < 0 ? lower.length + _mochi_idx : _mochi_idx;})()], lower[(()=>{const _mochi_idx = Math.trunc((_len(lower) - 1)); return _mochi_idx < 0 ? lower.length + _mochi_idx : _mochi_idx;})()], p) <= 0))) {
      lower = lower.slice(0, (_len(lower) - 1));
    }
    lower.push(p);
  }
  let upper: Point[] = [];
  let i: number = (_len(ps) - 1);
  while ((i >= 0)) {
    let p: Point = ps[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? ps.length + _mochi_idx : _mochi_idx;})()];
    while (((_len(upper) >= 2) && (cross(upper[(()=>{const _mochi_idx = Math.trunc((_len(upper) - 2)); return _mochi_idx < 0 ? upper.length + _mochi_idx : _mochi_idx;})()], upper[(()=>{const _mochi_idx = Math.trunc((_len(upper) - 1)); return _mochi_idx < 0 ? upper.length + _mochi_idx : _mochi_idx;})()], p) <= 0))) {
      upper = upper.slice(0, (_len(upper) - 1));
    }
    upper.push(p);
    i = (i - 1);
  }
  let hull = lower.slice(0, (_len(lower) - 1));
  let j: number = 0;
  while ((j < (_len(upper) - 1))) {
    hull.push(upper[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? upper.length + _mochi_idx : _mochi_idx;})()]);
    j = (j + 1);
  }
  return hull;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

