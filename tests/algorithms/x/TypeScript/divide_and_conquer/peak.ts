// Generated by Mochi v0.10.61 on 2025-08-08 17:06:06 GMT+7

function peak(lst: number[]): number {
  let low: number = 0;
  let high: number = (_len(lst) - 1);
  while ((low < high)) {
    let mid: number = Math.trunc((low + high) / 2);
    if ((lst[(()=>{const _mochi_idx = mid; return _mochi_idx < 0 ? lst.length + _mochi_idx : _mochi_idx;})()] < lst[(()=>{const _mochi_idx = Math.trunc((mid + 1)); return _mochi_idx < 0 ? lst.length + _mochi_idx : _mochi_idx;})()])) {
      low = (mid + 1);
    } else {
      high = mid;
    }
  }
  return lst[(()=>{const _mochi_idx = low; return _mochi_idx < 0 ? lst.length + _mochi_idx : _mochi_idx;})()];
}
function main() {
  console.log(_str(_str(peak([1, 2, 3, 4, 5, 4, 3, 2, 1]))));
  console.log(_str(_str(peak([1, 10, 9, 8, 7, 6, 5, 4]))));
  console.log(_str(_str(peak([1, 9, 8, 7]))));
  console.log(_str(_str(peak([1, 2, 3, 4, 5, 6, 7, 0]))));
  console.log(_str(_str(peak([1, 2, 3, 4, 3, 2, 1, 0, -1, -2]))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

