// Generated by Mochi v0.10.59 on 2025-08-07 08:55:27 GMT+7

function subarray(xs: number[], start: number, end: number): number[] {
  let result: number[] = [];
  let i: number = start;
  while ((i < end)) {
    result.push(xs[(i) < 0 ? xs.length + (i) : i]);
    i = (i + 1);
  }
  return result;
}
function merge(left: number[], right: number[]): number[] {
  let result: number[] = [];
  let i: number = 0;
  let j: number = 0;
  while (((i < _len(left)) && (j < _len(right)))) {
    if ((left[(i) < 0 ? left.length + (i) : i] <= right[(j) < 0 ? right.length + (j) : j])) {
      result.push(left[(i) < 0 ? left.length + (i) : i]);
      i = (i + 1);
    } else {
      result.push(right[(j) < 0 ? right.length + (j) : j]);
      j = (j + 1);
    }
  }
  while ((i < _len(left))) {
    result.push(left[(i) < 0 ? left.length + (i) : i]);
    i = (i + 1);
  }
  while ((j < _len(right))) {
    result.push(right[(j) < 0 ? right.length + (j) : j]);
    j = (j + 1);
  }
  return result;
}
function merge_sort(collection: number[]): number[] {
  if ((_len(collection) <= 1)) {
    return collection;
  }
  let mid_index: number = Math.trunc(_len(collection) / 2);
  let left: number[] = subarray(collection, 0, mid_index);
  let right: number[] = subarray(collection, mid_index, _len(collection));
  let sorted_left: number[] = merge_sort(left);
  let sorted_right: number[] = merge_sort(right);
  return merge(sorted_left, sorted_right);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(merge_sort([0, 5, 3, 2, 2]))));
  console.log(_str(_str(merge_sort([]))));
  console.log(_str(_str(merge_sort([-2, -5, -45]))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

