// Generated by Mochi v0.10.59 on 2025-08-07 08:55:07 GMT+7

function binary_insertion_sort(arr: number[]): number[] {
  let i: number = 1;
  while ((i < _len(arr))) {
    let value: number = arr[(i) < 0 ? arr.length + (i) : i];
    let low: number = 0;
    let high: number = (i - 1);
    while ((low <= high)) {
      let mid: number = Math.trunc((low + high) / 2);
      if ((value < arr[(mid) < 0 ? arr.length + (mid) : mid])) {
        high = (mid - 1);
      } else {
        low = (mid + 1);
      }
    }
    let j: number = i;
    while ((j > low)) {
      arr[(j) < 0 ? arr.length + (j) : j] = arr[(Math.trunc((j - 1))) < 0 ? arr.length + (Math.trunc((j - 1))) : Math.trunc((j - 1))];
      j = (j - 1);
    }
    arr[(low) < 0 ? arr.length + (low) : low] = value;
    i = (i + 1);
  }
  return arr;
}
function main() {
  let example1: number[] = [5, 2, 4, 6, 1, 3];
  console.log(_str(_str(binary_insertion_sort(example1))));
  let example2: number[] = [];
  console.log(_str(_str(binary_insertion_sort(example2))));
  let example3: number[] = [4, 2, 4, 1, 3];
  console.log(_str(_str(binary_insertion_sort(example3))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

