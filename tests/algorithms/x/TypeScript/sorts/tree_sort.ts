// Generated by Mochi v0.10.59 on 2025-08-07 08:55:52 GMT+7

export interface Node { value: number; left: number; right: number }
export interface TreeState { nodes: Node[]; root: number }
function new_node(state: TreeState, value: number): number {
  state.nodes = [...state.nodes, {value, "left": -1, "right": -1}];
  return (_len(state.nodes) - 1);
}
function insert(state: TreeState, value: number) {
  if ((state.root == -1)) {
    state.root = new_node(state, value);
    return;
  }
  let current: number = state.root;
  let nodes: Node[] = state.nodes;
  while (true) {
    let node: Node = nodes[(current) < 0 ? nodes.length + (current) : current];
    if ((value < node.value)) {
      if ((node.left == -1)) {
        let idx: number = new_node(state, value);
        nodes = state.nodes;
        node.left = idx;
        nodes[(current) < 0 ? nodes.length + (current) : current] = node;
        state.nodes = nodes;
        return;
      }
      current = node.left;
    } else {
      if ((value > node.value)) {
        if ((node.right == -1)) {
          let idx: number = new_node(state, value);
          nodes = state.nodes;
          node.right = idx;
          nodes[(current) < 0 ? nodes.length + (current) : current] = node;
          state.nodes = nodes;
          return;
        }
        current = node.right;
      } else {
        return;
      }
    }
  }
}
function inorder(state: TreeState, idx: number): number[] {
  if ((idx == -1)) {
    return [];
  }
  let node: Node = state.nodes[(idx) < 0 ? state.nodes.length + (idx) : idx];
  let result: number[] = inorder(state, node.left);
  result.push(node.value);
  let right_part: number[] = inorder(state, node.right);
  let i: number = 0;
  while ((i < _len(right_part))) {
    result.push(right_part[(i) < 0 ? right_part.length + (i) : i]);
    i = (i + 1);
  }
  return result;
}
function tree_sort(arr: number[]): number[] {
  let state: TreeState = {"nodes": [], "root": -1};
  let i: number = 0;
  while ((i < _len(arr))) {
    insert(state, arr[(i) < 0 ? arr.length + (i) : i]);
    i = (i + 1);
  }
  if ((state.root == -1)) {
    return [];
  }
  return inorder(state, state.root);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(tree_sort([]))));
  console.log(_str(_str(tree_sort([1]))));
  console.log(_str(_str(tree_sort([1, 2]))));
  console.log(_str(_str(tree_sort([5, 2, 7]))));
  console.log(_str(_str(tree_sort([5, -4, 9, 2, 7]))));
  console.log(_str(_str(tree_sort([5, 6, 1, -1, 4, 37, 2, 7]))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

