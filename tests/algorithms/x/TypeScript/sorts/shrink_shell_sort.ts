// Generated by Mochi v0.10.59 on 2025-08-07 08:55:46 GMT+7

function shell_sort(collection: number[]): number[] {
  let gap: number = _len(collection);
  let ten: number = 10;
  let thirteen: number = 13;
  while ((gap > 1)) {
    gap = Math.trunc((gap * ten) / thirteen);
    let i: number = gap;
    while ((i < _len(collection))) {
      let temp: number = collection[(i) < 0 ? collection.length + (i) : i];
      let j: number = i;
      while (((j >= gap) && (collection[(Math.trunc((j - gap))) < 0 ? collection.length + (Math.trunc((j - gap))) : Math.trunc((j - gap))] > temp))) {
        collection[(j) < 0 ? collection.length + (j) : j] = collection[(Math.trunc((j - gap))) < 0 ? collection.length + (Math.trunc((j - gap))) : Math.trunc((j - gap))];
        j = (j - gap);
      }
      collection[(j) < 0 ? collection.length + (j) : j] = temp;
      i = (i + 1);
    }
  }
  return collection;
}
function main() {
  console.log(_str(_str(shell_sort([3, 2, 1]))));
  console.log(_str(_str(shell_sort([]))));
  console.log(_str(_str(shell_sort([1]))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

