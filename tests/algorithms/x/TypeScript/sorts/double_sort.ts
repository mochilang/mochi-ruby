// Generated by Mochi v0.10.59 on 2025-08-07 08:55:17 GMT+7

function double_sort(collection: number[]): number[] {
  let no_of_elements: number = _len(collection);
  let passes: number = (Math.trunc((no_of_elements - 1) / 2) + 1);
  let i: number = 0;
  while ((i < passes)) {
    let j: number = 0;
    while ((j < (no_of_elements - 1))) {
      if ((collection[(Math.trunc((j + 1))) < 0 ? collection.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))] < collection[(j) < 0 ? collection.length + (j) : j])) {
        let tmp: number = collection[(j) < 0 ? collection.length + (j) : j];
        collection[(j) < 0 ? collection.length + (j) : j] = collection[(Math.trunc((j + 1))) < 0 ? collection.length + (Math.trunc((j + 1))) : Math.trunc((j + 1))];
        collection[((j + 1)) < 0 ? collection.length + ((j + 1)) : (j + 1)] = tmp;
      }
      if ((collection[(Math.trunc(((no_of_elements - 1) - j))) < 0 ? collection.length + (Math.trunc(((no_of_elements - 1) - j))) : Math.trunc(((no_of_elements - 1) - j))] < collection[(Math.trunc(((no_of_elements - 2) - j))) < 0 ? collection.length + (Math.trunc(((no_of_elements - 2) - j))) : Math.trunc(((no_of_elements - 2) - j))])) {
        let tmp2: number = collection[(Math.trunc(((no_of_elements - 1) - j))) < 0 ? collection.length + (Math.trunc(((no_of_elements - 1) - j))) : Math.trunc(((no_of_elements - 1) - j))];
        collection[(((no_of_elements - 1) - j)) < 0 ? collection.length + (((no_of_elements - 1) - j)) : ((no_of_elements - 1) - j)] = collection[(Math.trunc(((no_of_elements - 2) - j))) < 0 ? collection.length + (Math.trunc(((no_of_elements - 2) - j))) : Math.trunc(((no_of_elements - 2) - j))];
        collection[(((no_of_elements - 2) - j)) < 0 ? collection.length + (((no_of_elements - 2) - j)) : ((no_of_elements - 2) - j)] = tmp2;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return collection;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(double_sort([-1, -2, -3, -4, -5, -6, -7]))));
  console.log(_str(_str(double_sort([]))));
  console.log(_str(_str(double_sort([-1, -2, -3, -4, -5, -6]))));
  console.log(_str(_str((double_sort([-3, 10, 16, -42, 29]) == [-42, -3, 10, 16, 29]))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

