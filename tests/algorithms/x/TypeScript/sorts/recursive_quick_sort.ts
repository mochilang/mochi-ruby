// Generated by Mochi v0.10.59 on 2025-08-07 08:55:43 GMT+7

function concat(a: number[], b: number[]): number[] {
  let result: number[] = [];
  for (const x of a) {
    result.push(x);
  }
  for (const x of b) {
    result.push(x);
  }
  return result;
}
function quick_sort(data: number[]): number[] {
  if ((_len(data) <= 1)) {
    return data;
  }
  let pivot: number = data[(Math.trunc(0)) < 0 ? data.length + (Math.trunc(0)) : Math.trunc(0)];
  let left: number[] = [];
  let right: number[] = [];
  let i: number = 1;
  while ((i < _len(data))) {
    let e: number = data[(i) < 0 ? data.length + (i) : i];
    if ((e <= pivot)) {
      left.push(e);
    } else {
      right.push(e);
    }
    i = (i + 1);
  }
  let sorted_left: number[] = quick_sort(left);
  let sorted_right: number[] = quick_sort(right);
  let left_pivot: any[] = [...sorted_left, pivot];
  return concat(left_pivot, sorted_right);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(quick_sort([2, 1, 0]))));
  console.log(_str(_str(quick_sort([3, 5, 2, 4, 1]))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

