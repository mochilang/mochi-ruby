// Generated by Mochi v0.10.59 on 2025-08-07 08:55:09 GMT+7

let seed: number = 1;
function rand(): number {
  seed = (((seed * 1103515245) + 12345) % 2147483648);
  return seed;
}
function rand_range(max: number): number {
  return (rand() % max);
}
function shuffle(list_int: number[]): number[] {
  let i: number = (_len(list_int) - 1);
  while ((i > 0)) {
    let j: number = rand_range((i + 1));
    let tmp: number = list_int[(i) < 0 ? list_int.length + (i) : i];
    list_int[(i) < 0 ? list_int.length + (i) : i] = list_int[(j) < 0 ? list_int.length + (j) : j];
    list_int[(j) < 0 ? list_int.length + (j) : j] = tmp;
    i = (i - 1);
  }
  return list_int;
}
function is_sorted(list_int: number[]): boolean {
  let i: number = 0;
  while ((i < (_len(list_int) - 1))) {
    if ((list_int[(i) < 0 ? list_int.length + (i) : i] > list_int[(Math.trunc((i + 1))) < 0 ? list_int.length + (Math.trunc((i + 1))) : Math.trunc((i + 1))])) {
      return false;
    }
    i = (i + 1);
  }
  return true;
}
function bogo_sort(list_int: number[]): number[] {
  let res: number[] = list_int;
  while (!is_sorted(res)) {
    res = shuffle(res);
  }
  return res;
}
let data: number[] = [3, 2, 1];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(bogo_sort(data))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

