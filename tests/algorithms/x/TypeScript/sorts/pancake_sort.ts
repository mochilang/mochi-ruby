// Generated by Mochi v0.10.59 on 2025-08-07 08:55:34 GMT+7

function flip(arr: number[], k: number): number[] {
  let start: number = 0;
  let end: number = k;
  while ((start < end)) {
    let temp: number = arr[(start) < 0 ? arr.length + (start) : start];
    arr[(start) < 0 ? arr.length + (start) : start] = arr[(end) < 0 ? arr.length + (end) : end];
    arr[(end) < 0 ? arr.length + (end) : end] = temp;
    start = (start + 1);
    end = (end - 1);
  }
  return arr;
}
function find_max_index(arr: number[], n: number): number {
  let mi: number = 0;
  let i: number = 1;
  while ((i < n)) {
    if ((arr[(i) < 0 ? arr.length + (i) : i] > arr[(mi) < 0 ? arr.length + (mi) : mi])) {
      mi = i;
    }
    i = (i + 1);
  }
  return mi;
}
function pancake_sort(arr: number[]): number[] {
  let cur: number = _len(arr);
  while ((cur > 1)) {
    let mi: number = find_max_index(arr, cur);
    arr = flip(arr, mi);
    arr = flip(arr, (cur - 1));
    cur = (cur - 1);
  }
  return arr;
}
function main() {
  let data: number[] = [3, 6, 1, 10, 2];
  let sorted: number[] = pancake_sort(data);
  console.log(_str(_str(sorted)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

