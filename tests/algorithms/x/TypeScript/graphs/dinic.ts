// Generated by Mochi v0.10.66 on 2025-08-15 10:28:25 GMT+7

let INF: number = 1000000000;
export interface Dinic { n: number; lvl: number[]; ptr: number[]; q: number[]; adj: number[][][] }
function pow2(k: number): number {
  let res: number = 1;
  let i: number = 0;
  while ((i < k)) {
    res = (res * 2);
    i = (i + 1);
  }
  return res;
}
function min2(a: number, b: number): number {
  if ((a < b)) {
    return a;
  }
  return b;
}
function new_dinic(n: number): Dinic {
  let lvl: number[] = [];
  let ptr: number[] = [];
  let q: number[] = [];
  let adj: number[][][] = [];
  let i: number = 0;
  while ((i < n)) {
    lvl.push(0);
    ptr.push(0);
    q.push(0);
    let edges: number[][] = [];
    adj.push(edges);
    i = (i + 1);
  }
  return {n, lvl, ptr, q, adj};
}
function add_edge(g: Dinic, a: number, b: number, c: number, rcap: number) {
  let adj: number[][][] = g.adj;
  let list_a: number[][] = adj[(()=>{const _mochi_idx = a; return _mochi_idx < 0 ? adj.length + _mochi_idx : _mochi_idx;})()];
  let list_b: number[][] = adj[(()=>{const _mochi_idx = b; return _mochi_idx < 0 ? adj.length + _mochi_idx : _mochi_idx;})()];
  let e1: number[] = [b, _len(list_b), c, 0];
  let e2: number[] = [a, _len(list_a), rcap, 0];
  list_a.push(e1);
  list_b.push(e2);
  adj[(()=>{const _mochi_idx = a; return _mochi_idx < 0 ? adj.length + _mochi_idx : _mochi_idx;})()] = list_a;
  adj[(()=>{const _mochi_idx = b; return _mochi_idx < 0 ? adj.length + _mochi_idx : _mochi_idx;})()] = list_b;
  g.adj = adj;
}
function dfs(g: Dinic, v: number, sink: number, flow: number): number {
  if (((v === sink) || (flow === 0))) {
    return flow;
  }
  let ptr: number[] = g.ptr;
  let i: number = ptr[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? ptr.length + _mochi_idx : _mochi_idx;})()];
  let adj_all: number[][][] = g.adj;
  let adj_v: number[][] = adj_all[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? adj_all.length + _mochi_idx : _mochi_idx;})()];
  while ((i < _len(adj_v))) {
    let e: number[] = adj_v[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? adj_v.length + _mochi_idx : _mochi_idx;})()];
    let to: number = e[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()];
    if ((g.lvl[(()=>{const _mochi_idx = to; return _mochi_idx < 0 ? g.lvl.length + _mochi_idx : _mochi_idx;})()] === (g.lvl[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? g.lvl.length + _mochi_idx : _mochi_idx;})()] + 1))) {
      let avail: number = (e[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()] - e[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()]);
      let pushed: number = dfs(g, to, sink, min2(flow, avail));
      if ((pushed > 0)) {
        e[(()=>{const _mochi_idx = 3; return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()] = (e[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()] + pushed);
        adj_v[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? adj_v.length + _mochi_idx : _mochi_idx;})()] = e;
        let adj_to: number[][] = adj_all[(()=>{const _mochi_idx = to; return _mochi_idx < 0 ? adj_all.length + _mochi_idx : _mochi_idx;})()];
        let back: number[] = adj_to[(()=>{const _mochi_idx = Math.trunc(e[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()]); return _mochi_idx < 0 ? adj_to.length + _mochi_idx : _mochi_idx;})()];
        back[(()=>{const _mochi_idx = 3; return _mochi_idx < 0 ? back.length + _mochi_idx : _mochi_idx;})()] = (back[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? back.length + _mochi_idx : _mochi_idx;})()] - pushed);
        adj_to[(()=>{const _mochi_idx = e[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()]; return _mochi_idx < 0 ? adj_to.length + _mochi_idx : _mochi_idx;})()] = back;
        adj_all[(()=>{const _mochi_idx = to; return _mochi_idx < 0 ? adj_all.length + _mochi_idx : _mochi_idx;})()] = adj_to;
        adj_all[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? adj_all.length + _mochi_idx : _mochi_idx;})()] = adj_v;
        g.adj = adj_all;
        return pushed;
      }
    }
    i = (i + 1);
    ptr[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? ptr.length + _mochi_idx : _mochi_idx;})()] = i;
  }
  g.ptr = ptr;
  adj_all[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? adj_all.length + _mochi_idx : _mochi_idx;})()] = adj_v;
  g.adj = adj_all;
  return 0;
}
function max_flow(g: Dinic, source: number, sink: number): number {
  let flow: number = 0;
  let l: number = 0;
  while ((l < 31)) {
    let threshold: number = pow2((30 - l));
    while (true) {
      let lvl: number[] = [];
      let ptr: number[] = [];
      let i: number = 0;
      while ((i < g.n)) {
        lvl.push(0);
        ptr.push(0);
        i = (i + 1);
      }
      g.lvl = lvl;
      g.ptr = ptr;
      let qi: number = 0;
      let qe: number = 1;
      lvl[(()=>{const _mochi_idx = source; return _mochi_idx < 0 ? lvl.length + _mochi_idx : _mochi_idx;})()] = 1;
      g.lvl = lvl;
      let q: number[] = g.q;
      q[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? q.length + _mochi_idx : _mochi_idx;})()] = source;
      while (((qi < qe) && (g.lvl[(()=>{const _mochi_idx = sink; return _mochi_idx < 0 ? g.lvl.length + _mochi_idx : _mochi_idx;})()] === 0))) {
        let v: number = q[(()=>{const _mochi_idx = qi; return _mochi_idx < 0 ? q.length + _mochi_idx : _mochi_idx;})()];
        qi = (qi + 1);
        let edges: number[][] = g.adj[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? g.adj.length + _mochi_idx : _mochi_idx;})()];
        let j: number = 0;
        while ((j < _len(edges))) {
          let e: number[] = edges[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? edges.length + _mochi_idx : _mochi_idx;})()];
          let to: number = e[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()];
          let residual: number = (e[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()] - e[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? e.length + _mochi_idx : _mochi_idx;})()]);
          let lvl_inner: number[] = g.lvl;
          if (((lvl_inner[(()=>{const _mochi_idx = to; return _mochi_idx < 0 ? lvl_inner.length + _mochi_idx : _mochi_idx;})()] === 0) && (residual >= threshold))) {
            q[(()=>{const _mochi_idx = qe; return _mochi_idx < 0 ? q.length + _mochi_idx : _mochi_idx;})()] = to;
            qe = (qe + 1);
            lvl_inner[(()=>{const _mochi_idx = to; return _mochi_idx < 0 ? lvl_inner.length + _mochi_idx : _mochi_idx;})()] = (lvl_inner[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? lvl_inner.length + _mochi_idx : _mochi_idx;})()] + 1);
            g.lvl = lvl_inner;
          }
          j = (j + 1);
        }
      }
      let p: number = dfs(g, source, sink, INF);
      while ((p > 0)) {
        flow = (flow + p);
        p = dfs(g, source, sink, INF);
      }
      if ((g.lvl[(()=>{const _mochi_idx = sink; return _mochi_idx < 0 ? g.lvl.length + _mochi_idx : _mochi_idx;})()] === 0)) {
        break
      }
    }
    l = (l + 1);
  }
  return flow;
}
let source: number = 0;
let sink: number = 9;
let v: number = 1;
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let graph: Dinic
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
graph = new_dinic(10)
  while ((v < 5)) {
    add_edge(graph, source, v, 1, 0);
    v = (v + 1);
  }
  v = 5;
  while ((v < 9)) {
    add_edge(graph, v, sink, 1, 0);
    v = (v + 1);
  }
  v = 1;
  while ((v < 5)) {
    add_edge(graph, v, (v + 4), 1, 0);
    v = (v + 1);
  }
  console.log(_str(max_flow(graph, source, sink)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

