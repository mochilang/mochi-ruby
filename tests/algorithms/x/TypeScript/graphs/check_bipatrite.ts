// Generated by Mochi v0.10.61 on 2025-08-08 19:18:00 GMT+7

function is_bipartite_bfs(graph: Record<number, number[]>): boolean {
  let visited: Record<number, number> = {};
  for (const node in graph) {
    if (!(node in visited)) {
      let queue: number[] = [];
      queue.push(node);
      visited[(()=>{const _mochi_idx = node; return _mochi_idx < 0 ? visited.length + _mochi_idx : _mochi_idx;})()] = 0;
      while ((_len(queue) > 0)) {
        let curr: number = queue[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? queue.length + _mochi_idx : _mochi_idx;})()];
        queue = queue.slice(1, _len(queue));
        for (const neighbor of graph[curr]) {
          if (!(neighbor in visited)) {
            visited[(()=>{const _mochi_idx = neighbor; return _mochi_idx < 0 ? visited.length + _mochi_idx : _mochi_idx;})()] = (1 - visited[curr]);
            queue.push(neighbor);
          } else {
            if ((visited[neighbor] == visited[curr])) {
              return false;
            }
          }
        }
      }
    }
  }
  return true;
}
let graph: Record<number, number[]> = {[0]: [1, 3], [1]: [0, 2], [2]: [1, 3], [3]: [0, 2]};
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(is_bipartite_bfs(graph))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

