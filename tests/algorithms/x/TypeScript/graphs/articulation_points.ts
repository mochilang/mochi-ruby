// Generated by Mochi v0.10.65 on 2025-08-14 10:03:50 GMT+7

function dfs_skip(graph: number[][], visited: boolean[], skip: number, at: number): number {
  visited[(()=>{const _mochi_idx = at; return _mochi_idx < 0 ? visited.length + _mochi_idx : _mochi_idx;})()] = true;
  let count: number = 1;
  for (const to of graph[(()=>{const _mochi_idx = at; return _mochi_idx < 0 ? graph.length + _mochi_idx : _mochi_idx;})()]) {
    if ((to === skip)) {
      continue
    }
    if ((visited[(()=>{const _mochi_idx = to; return _mochi_idx < 0 ? visited.length + _mochi_idx : _mochi_idx;})()] === false)) {
      count = (count + dfs_skip(graph, visited, skip, to));
    }
  }
  return count;
}
function articulation_points(graph: number[][]): number[] {
  let n: number = _len(graph);
  let result: number[] = [];
  let v: number = 0;
  while ((v < n)) {
    let visited: boolean[] = [];
    let i: number = 0;
    while ((i < n)) {
      visited.push(false);
      i = (i + 1);
    }
    let start: number = 0;
    while (((start === v) && (start < n))) {
      start = (start + 1);
    }
    let reach: number = dfs_skip(graph, visited, v, start);
    if ((reach < (n - 1))) {
      result.push(v);
      console.log(_str(v));
    }
    v = (v + 1);
  }
  return result;
}
function main() {
  let graph: number[][] = [[1, 2], [0, 2], [0, 1, 3, 5], [2, 4], [3], [2, 6, 8], [5, 7], [6, 8], [5, 7]];
  articulation_points(graph);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

