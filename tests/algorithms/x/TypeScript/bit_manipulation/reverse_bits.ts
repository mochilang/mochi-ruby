// Generated by Mochi v0.10.59 on 2025-08-06 19:19:53 GMT+7

function get_reverse_bit_string(_number: number): string {
  let bit_string: string = "";
  let n: number = _number;
  let i: number = 0;
  while ((i < 32)) {
    bit_string = (bit_string + _str((n % 2)));
    n = Math.trunc(n / 2);
    i = (i + 1);
  }
  return bit_string;
}
function reverse_bit(_number: number): string {
  if ((_number < 0)) {
    panic("the value of input must be positive");
  }
  let n: number = _number;
  let result: number = 0;
  let i: number = 1;
  while ((i <= 32)) {
    result = (result * 2);
    let end_bit: number = (n % 2);
    n = Math.trunc(n / 2);
    result = (result + end_bit);
    i = (i + 1);
  }
  return get_reverse_bit_string(result);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(reverse_bit(25)));
  console.log(_str(reverse_bit(37)));
  console.log(_str(reverse_bit(21)));
  console.log(_str(reverse_bit(58)));
  console.log(_str(reverse_bit(0)));
  console.log(_str(reverse_bit(256)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

