// Generated by Mochi v0.10.59 on 2025-08-06 17:46:32 GMT+7

function to_binary4(n: number): string {
  let result: string = "";
  let x: number = n;
  while ((x > 0)) {
    result = (_str((x % 2)) + result);
    x = Math.trunc(x / 2);
  }
  while ((_len(result) < 4)) {
    result = ("0" + result);
  }
  return result;
}
function binary_coded_decimal(_number: number): string {
  let n: number = _number;
  if ((n < 0)) {
    n = 0;
  }
  let digits: string = _str(n);
  let out: string = "0b";
  let i: number = 0;
  while ((i < _len(digits))) {
    let d: string = digits[i];
    let d_int: number = Math.trunc(d);
    out = (out + to_binary4(d_int));
    i = (i + 1);
  }
  return out;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(binary_coded_decimal(-2)));
  console.log(_str(binary_coded_decimal(-1)));
  console.log(_str(binary_coded_decimal(0)));
  console.log(_str(binary_coded_decimal(3)));
  console.log(_str(binary_coded_decimal(2)));
  console.log(_str(binary_coded_decimal(12)));
  console.log(_str(binary_coded_decimal(987)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

