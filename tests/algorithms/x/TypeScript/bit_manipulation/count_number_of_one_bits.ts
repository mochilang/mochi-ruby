// Generated by Mochi v0.10.59 on 2025-08-06 17:46:45 GMT+7

function bit_and(a: number, b: number): number {
  let ua: number = a;
  let ub: number = b;
  let res: number = 0;
  let bit: number = 1;
  while (((ua > 0) || (ub > 0))) {
    if ((((ua % 2) == 1) && ((ub % 2) == 1))) {
      res = (res + bit);
    }
    ua = Math.trunc(Math.trunc(ua / 2));
    ub = Math.trunc(Math.trunc(ub / 2));
    bit = (bit * 2);
  }
  return res;
}
function count_bits_kernighan(n: number): number {
  if ((n < 0)) {
    panic("the value of input must not be negative");
  }
  let num: number = n;
  let result: number = 0;
  while ((num != 0)) {
    num = bit_and(num, (num - 1));
    result = (result + 1);
  }
  return result;
}
function count_bits_modulo(n: number): number {
  if ((n < 0)) {
    panic("the value of input must not be negative");
  }
  let num: number = n;
  let result: number = 0;
  while ((num != 0)) {
    if (((num % 2) == 1)) {
      result = (result + 1);
    }
    num = Math.trunc(Math.trunc(num / 2));
  }
  return result;
}
function main() {
  let numbers: number[] = [25, 37, 21, 58, 0, 256];
  let i: number = 0;
  while ((i < _len(numbers))) {
    console.log(_str(_str(count_bits_kernighan(numbers[i]))));
    i = (i + 1);
  }
  i = 0;
  while ((i < _len(numbers))) {
    console.log(_str(_str(count_bits_modulo(numbers[i]))));
    i = (i + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

