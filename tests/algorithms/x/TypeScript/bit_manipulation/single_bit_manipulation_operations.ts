// Generated by Mochi v0.10.59 on 2025-08-06 19:19:54 GMT+7

function pow2(exp: number): number {
  let result: number = 1;
  let i: number = 0;
  while ((i < exp)) {
    result = (result * 2);
    i = (i + 1);
  }
  return result;
}
function is_bit_set(_number: number, position: number): boolean {
  let shifted: number = Math.trunc(_number / pow2(position));
  let remainder: number = (shifted % 2);
  return (remainder == 1);
}
function set_bit(_number: number, position: number): number {
  if (is_bit_set(_number, position)) {
    return _number;
  }
  return (_number + pow2(position));
}
function clear_bit(_number: number, position: number): number {
  if (is_bit_set(_number, position)) {
    return (_number - pow2(position));
  }
  return _number;
}
function flip_bit(_number: number, position: number): number {
  if (is_bit_set(_number, position)) {
    return (_number - pow2(position));
  }
  return (_number + pow2(position));
}
function get_bit(_number: number, position: number): number {
  if (is_bit_set(_number, position)) {
    return 1;
  }
  return 0;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(set_bit(13, 1))));
  console.log(_str(_str(clear_bit(18, 1))));
  console.log(_str(_str(flip_bit(5, 1))));
  console.log(_str(_str(is_bit_set(10, 3))));
  console.log(_str(_str(get_bit(10, 1))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

