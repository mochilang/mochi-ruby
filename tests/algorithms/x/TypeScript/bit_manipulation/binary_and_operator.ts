// Generated by Mochi v0.10.59 on 2025-08-06 16:10:53 GMT+7

function to_binary(n: number): string {
  if ((n == 0)) {
    return "0";
  }
  let num: number = n;
  let res: string = "";
  while ((num > 0)) {
    let bit = (num % 2);
    res = (_str(bit) + res);
    num = Math.trunc(num / 2);
  }
  return res;
}
function zfill(s: string, width: number): string {
  let res: string = s;
  let pad: number = (width - Number(Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length));
  while ((pad > 0)) {
    res = ("0" + res);
    pad = (pad - 1);
  }
  return res;
}
function binary_and(a: number, b: number): string {
  if (((a < 0) || (b < 0))) {
    panic("the value of both inputs must be positive");
  }
  let a_bin: string = to_binary(a);
  let b_bin: string = to_binary(b);
  let max_len: number = Number(Array.isArray(a_bin) || typeof a_bin === 'string' ? a_bin.length : Object.keys(a_bin ?? {}).length);
  if ((Number(Array.isArray(b_bin) || typeof b_bin === 'string' ? b_bin.length : Object.keys(b_bin ?? {}).length) > max_len)) {
    max_len = Number(Array.isArray(b_bin) || typeof b_bin === 'string' ? b_bin.length : Object.keys(b_bin ?? {}).length);
  }
  let a_pad: string = zfill(a_bin, max_len);
  let b_pad: string = zfill(b_bin, max_len);
  let i: number = 0;
  let res: string = "";
  while ((i < max_len)) {
    if (((a_pad[i] == "1") && (b_pad[i] == "1"))) {
      res = (res + "1");
    } else {
      res = (res + "0");
    }
    i = (i + 1);
  }
  return ("0b" + res);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(binary_and(25, 32)));
  console.log(_str(binary_and(37, 50)));
  console.log(_str(binary_and(21, 30)));
  console.log(_str(binary_and(58, 73)));
  console.log(_str(binary_and(0, 255)));
  console.log(_str(binary_and(256, 256)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

