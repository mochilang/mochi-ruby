// Generated by Mochi v0.10.59 on 2025-08-07 09:47:34 GMT+7

export interface Intensity { forecast: number; actual: number; index: string }
export interface Entry { from: string; to: string; intensity: Intensity }
export interface Response { data: Entry[] }
let BASE_URL: string = "https://api.carbonintensity.org.uk/intensity";
async function fetch_last_half_hour(): number {
  let resp: Response = await _fetch(BASE_URL, {timeout: 10.0});
  let entry: Entry = resp.data[(Math.trunc(0)) < 0 ? resp.data.length + (Math.trunc(0)) : Math.trunc(0)];
  return entry.intensity.actual;
}
async function fetch_from_to(start: string, end: string): Entry[] {
  let url: string = ((((BASE_URL + "/") + start) + "/") + end);
  let resp: Response = await _fetch(url, {timeout: 10.0});
  return resp.data;
}
async function main() {
  let entries: Entry[] = fetch_from_to("2020-10-01", "2020-10-03");
  let i: number = 0;
  while ((i < _len(entries))) {
    let e: Entry = entries[(i) < 0 ? entries.length + (i) : i];
    console.log(_str("from") + " " + _str(e.from) + " " + _str("to") + " " + _str(e.to) + " " + _str(":") + " " + _str(e.intensity.actual));
    i = (i + 1);
  }
  let last: number = fetch_last_half_hour();
  console.log(_str("fetch_last_half_hour() =") + " " + _str(last));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
async function _fetch(url: string, opts?: any): Promise<any> {
  const init: RequestInit = { method: opts?.method ?? 'GET' };
  if (opts?.headers) init.headers = opts.headers;
  if (opts && 'body' in opts) init.body = JSON.stringify(opts.body);
  if (opts?.query) {
    const qs = new URLSearchParams();
    for (const [k, v] of Object.entries(opts.query)) qs.set(k, String(v));
    const sep = url.includes('?') ? '&' : '?';
    url = url + sep + qs.toString();
  }
  const resp = await fetch(url, init);
  const text = await resp.text();
  try { return JSON.parse(text); } catch { return text; }
}
(async () => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

