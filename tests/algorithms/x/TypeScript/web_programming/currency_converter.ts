// Generated by Mochi v0.10.59 on 2025-08-07 09:47:43 GMT+7

export interface Rate { code: string; rate: number }
let rates: Rate[] = [{"code": "USD", "rate": 1.0}, {"code": "EUR", "rate": 0.9}, {"code": "INR", "rate": 83.0}, {"code": "JPY", "rate": 156.0}, {"code": "GBP", "rate": 0.78}];
function rate_of(code: string): number {
  for (const r of rates) {
    if ((r.code == code)) {
      return r.rate;
    }
  }
  return 0.0;
}
function convert_currency(from_: string, to: string, amount: number): number {
  let from_rate: number = rate_of(from_);
  let to_rate: number = rate_of(to);
  if (((from_rate == 0.0) || (to_rate == 0.0))) {
    return 0.0;
  }
  let usd: number = (amount / from_rate);
  return (usd * to_rate);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let result: number
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
result = convert_currency("USD", "INR", 10.0)
  console.log(_str(_str(result)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

