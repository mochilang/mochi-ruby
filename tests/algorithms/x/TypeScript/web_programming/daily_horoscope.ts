// Generated by Mochi v0.10.59 on 2025-08-07 09:47:47 GMT+7

let days: string[] = ["yesterday", "today", "tomorrow"];
let zodiac_names: string[] = ["Aries", "Taurus", "Gemini", "Cancer", "Leo", "Virgo", "Libra", "Scorpio", "Sagittarius", "Capricorn", "Aquarius", "Pisces"];
let day_messages: string[] = ["Reflect on what has passed and learn from it.", "Focus on the present and take decisive action.", "Prepare for future opportunities with optimism."];
function horoscope(zodiac_sign: number, day: string): string {
  let day_index: number = (0 - 1);
  let i: number = 0;
  while ((i < _len(days))) {
    if ((day == days[(i) < 0 ? days.length + (i) : i])) {
      day_index = i;
      break
    }
    i = (i + 1);
  }
  let sign_index: number = (zodiac_sign - 1);
  if ((day_index == (0 - 1))) {
    return "Invalid zodiac sign or day";
  }
  if (((sign_index < 0) || (sign_index >= _len(zodiac_names)))) {
    return "Invalid zodiac sign or day";
  }
  return ((zodiac_names[(sign_index) < 0 ? zodiac_names.length + (sign_index) : sign_index] + ": ") + day_messages[(day_index) < 0 ? day_messages.length + (day_index) : day_index]);
}
function main() {
  let result: string = horoscope(1, "today");
  console.log(_str(result));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

