// Generated by Mochi v0.10.71 on 2025-08-23 15:51:02 GMT+7

function solution(n: number): number {
  let ones_counts: number[] = [0, 3, 3, 5, 4, 4, 3, 5, 5, 4, 3, 6, 6, 8, 8, 7, 7, 9, 8, 8];
  let tens_counts: number[] = [0, 0, 6, 6, 5, 5, 5, 7, 6, 6];
  let count: number = 0;
  let i: number = 1;
  while ((i <= n)) {
    if ((i < 1000)) {
      if ((i >= 100)) {
        count = ((count + ones_counts[(()=>{const _mochi_idx = Math.trunc(Math.trunc(i / 100)); return _mochi_idx < 0 ? ones_counts.length + _mochi_idx : _mochi_idx;})()]) + 7);
        if (((i % 100) !== 0)) {
          count = (count + 3);
        }
      }
      let remainder: bigint = (i % 100);
      if (((remainder > 0) && (remainder < 20))) {
        count = (count + ones_counts[(()=>{const _mochi_idx = remainder; return _mochi_idx < 0 ? ones_counts.length + _mochi_idx : _mochi_idx;})()]);
      } else {
        count = (count + ones_counts[(()=>{const _mochi_idx = Math.trunc((i % 10)); return _mochi_idx < 0 ? ones_counts.length + _mochi_idx : _mochi_idx;})()]);
        count = (count + tens_counts[(()=>{const _mochi_idx = Math.trunc(Math.trunc((remainder - (i % 10)) / 10)); return _mochi_idx < 0 ? tens_counts.length + _mochi_idx : _mochi_idx;})()]);
      }
    } else {
      count = ((count + ones_counts[(()=>{const _mochi_idx = Math.trunc(Math.trunc(i / 1000)); return _mochi_idx < 0 ? ones_counts.length + _mochi_idx : _mochi_idx;})()]) + 8);
    }
    i = (i + 1);
  }
  return count;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(solution(1000)));
  console.log(_str(solution(5)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

