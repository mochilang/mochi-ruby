// Generated by Mochi v0.10.70 on 2025-08-22 23:22:19 GMT+7

function get_digits(num: number): string {
  let cube = ((num * num) * num);
  let s: string = _str(cube);
  let counts: number[] = [];
  let j: number = 0;
  while ((j < 10)) {
    counts.push(0);
    j = (j + 1);
  }
  let i: number = 0;
  while ((i < _len(s))) {
    let d: number = Math.trunc(s[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? s.length + _mochi_idx : _mochi_idx;})()]);
    counts[(()=>{const _mochi_idx = d; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] = (counts[(()=>{const _mochi_idx = d; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] + 1);
    i = (i + 1);
  }
  let result: string = "";
  let d: number = 0;
  while ((d < 10)) {
    let c: number = counts[(()=>{const _mochi_idx = d; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()];
    while ((c > 0)) {
      result = (result + _str(d));
      c = (c - 1);
    }
    d = (d + 1);
  }
  return result;
}
function solution(max_base: number): number {
  let freqs: Record<string, number[]> = {};
  let num: number = 0;
  while (true) {
    let digits = get_digits(num);
    let arr: number[] = [];
    if ((digits in freqs)) {
      arr = freqs[digits];
    }
    arr.push(num);
    freqs[(()=>{const _mochi_idx = digits; return _mochi_idx < 0 ? freqs.length + _mochi_idx : _mochi_idx;})()] = arr;
    if ((_len(arr) === max_base)) {
      let base: number = arr[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
      return ((base * base) * base);
    }
    num = (num + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(("solution() = " + _str(solution(5)))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

