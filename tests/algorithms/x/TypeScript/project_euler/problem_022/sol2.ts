// Generated by Mochi v0.10.71 on 2025-08-23 15:51:15 GMT+7

export interface Name { name: string }
function ord_letter(ch: string): number {
  let alphabet: string = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  let i: number = 0;
  while ((i < _len(alphabet))) {
    if ((alphabet.slice(i, (i + 1)) === ch)) {
      return (i + 1);
    }
    i = (i + 1);
  }
  return 0;
}
function name_value(name: string): number {
  let total: number = 0;
  let i: number = 0;
  while ((i < _len(name))) {
    total = (total + ord_letter(name.slice(i, (i + 1))));
    i = (i + 1);
  }
  return total;
}
function bubble_sort(arr: string[]): string[] {
  let n: number = _len(arr);
  let i: number = 0;
  while ((i < n)) {
    let j: number = 0;
    while ((j < ((n - i) - 1))) {
      if ((arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] > arr[(()=>{const _mochi_idx = Math.trunc((j + 1)); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()])) {
        let temp: string = arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
        arr[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = arr[(()=>{const _mochi_idx = Math.trunc((j + 1)); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
        arr[(()=>{const _mochi_idx = (j + 1); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = temp;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return arr;
}
let names: string[] = [];
let total: number = 0;
let i: number = 0;
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
let rows: Name[]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
rows = Deno.readTextFileSync(new URL('../../../../../..', import.meta.url).pathname + "tests/github/TheAlgorithms/Mochi/project_euler/problem_022/p022_names.jsonl").trim().split(/\r?\n/).map(l=>JSON.parse(l))
  for (const r of rows) {
    names.push(r.name);
  }
  names = bubble_sort(names);
  while ((i < _len(names))) {
    total = (total + ((i + 1) * name_value(names[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? names.length + _mochi_idx : _mochi_idx;})()])));
    i = (i + 1);
  }
  console.log(_str(total));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

