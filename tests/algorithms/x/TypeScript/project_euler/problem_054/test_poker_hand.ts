// Generated by Mochi v0.10.70 on 2025-08-22 15:35:35 GMT+7

function split(s: string, sep: string): string[] {
  let parts: string[] = [];
  let cur: string = "";
  let i: number = 0;
  while ((i < _len(s))) {
    if ((((_len(sep) > 0) && ((i + _len(sep)) <= _len(s))) && ((s).substring(i, (i + _len(sep))) === sep))) {
      parts.push(cur);
      cur = "";
      i = (i + _len(sep));
    } else {
      cur = (cur + s.slice(i, (i + 1)));
      i = (i + 1);
    }
  }
  parts.push(cur);
  return parts;
}
function card_value(ch: string): number {
  if ((ch === "A")) {
    return 14;
  } else {
    if ((ch === "K")) {
      return 13;
    } else {
      if ((ch === "Q")) {
        return 12;
      } else {
        if ((ch === "J")) {
          return 11;
        } else {
          if ((ch === "T")) {
            return 10;
          } else {
            if ((ch === "9")) {
              return 9;
            } else {
              if ((ch === "8")) {
                return 8;
              } else {
                if ((ch === "7")) {
                  return 7;
                } else {
                  if ((ch === "6")) {
                    return 6;
                  } else {
                    if ((ch === "5")) {
                      return 5;
                    } else {
                      if ((ch === "4")) {
                        return 4;
                      } else {
                        if ((ch === "3")) {
                          return 3;
                        } else {
                          return 2;
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
export interface Hand { rank: number; values: number[] }
function parse_hand(hand: string): Hand {
  let counts: number[] = [];
  let i: number = 0;
  while ((i <= 14)) {
    counts.push(0);
    i = (i + 1);
  }
  let suits: string[] = [];
  for (const card of split(hand, " ")) {
    let v: number = card_value(card.slice(0, 1));
    counts[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] = (counts[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] + 1);
    suits.push(card.slice(1, 2));
  }
  let vals: number[] = [];
  let v: number = 14;
  while ((v >= 2)) {
    let c: number = counts[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()];
    let k: number = 0;
    while ((k < c)) {
      vals.push(v);
      k = (k + 1);
    }
    v = (v - 1);
  }
  let is_straight: boolean = false;
  if (((((((_len(vals) === 5) && (vals[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 14)) && (vals[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 5)) && (vals[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 4)) && (vals[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 3)) && (vals[(()=>{const _mochi_idx = Math.trunc(4); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 2))) {
    is_straight = true;
    vals[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] = 5;
    vals[(()=>{const _mochi_idx = 1; return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] = 4;
    vals[(()=>{const _mochi_idx = 2; return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] = 3;
    vals[(()=>{const _mochi_idx = 3; return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] = 2;
    vals[(()=>{const _mochi_idx = 4; return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] = 14;
  } else {
    is_straight = true;
    let j: number = 0;
    while ((j < 4)) {
      if (((vals[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] - vals[(()=>{const _mochi_idx = Math.trunc((j + 1)); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()]) !== 1)) {
        is_straight = false;
      }
      j = (j + 1);
    }
  }
  let is_flush: boolean = true;
  let s0: string = suits[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? suits.length + _mochi_idx : _mochi_idx;})()];
  let t: number = 1;
  while ((t < _len(suits))) {
    if ((suits[(()=>{const _mochi_idx = t; return _mochi_idx < 0 ? suits.length + _mochi_idx : _mochi_idx;})()] !== s0)) {
      is_flush = false;
    }
    t = (t + 1);
  }
  let four_val: number = 0;
  let three_val: number = 0;
  let pair_vals: number[] = [];
  v = 14;
  while ((v >= 2)) {
    if ((counts[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] === 4)) {
      four_val = v;
    } else {
      if ((counts[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] === 3)) {
        three_val = v;
      } else {
        if ((counts[(()=>{const _mochi_idx = v; return _mochi_idx < 0 ? counts.length + _mochi_idx : _mochi_idx;})()] === 2)) {
          pair_vals.push(v);
        }
      }
    }
    v = (v - 1);
  }
  let rank: number = 1;
  if ((((is_flush && is_straight) && (vals[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 14)) && (vals[(()=>{const _mochi_idx = Math.trunc(4); return _mochi_idx < 0 ? vals.length + _mochi_idx : _mochi_idx;})()] === 10))) {
    rank = 10;
  } else {
    if ((is_flush && is_straight)) {
      rank = 9;
    } else {
      if ((four_val !== 0)) {
        rank = 8;
      } else {
        if (((three_val !== 0) && (_len(pair_vals) === 1))) {
          rank = 7;
        } else {
          if (is_flush) {
            rank = 6;
          } else {
            if (is_straight) {
              rank = 5;
            } else {
              if ((three_val !== 0)) {
                rank = 4;
              } else {
                if ((_len(pair_vals) === 2)) {
                  rank = 3;
                } else {
                  if ((_len(pair_vals) === 1)) {
                    rank = 2;
                  } else {
                    rank = 1;
                  }
                }
              }
            }
          }
        }
      }
    }
  }
  return {rank, values: vals};
}
function compare(a: Hand, b: Hand): string {
  if ((a.rank > b.rank)) {
    return "Win";
  }
  if ((a.rank < b.rank)) {
    return "Loss";
  }
  let i: number = 0;
  while ((i < _len(a.values))) {
    if ((a.values[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.values.length + _mochi_idx : _mochi_idx;})()] > b.values[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? b.values.length + _mochi_idx : _mochi_idx;})()])) {
      return "Win";
    }
    if ((a.values[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? a.values.length + _mochi_idx : _mochi_idx;})()] < b.values[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? b.values.length + _mochi_idx : _mochi_idx;})()])) {
      return "Loss";
    }
    i = (i + 1);
  }
  return "Tie";
}
function main() {
  let tests: string[][] = [["2H 3H 4H 5H 6H", "KS AS TS QS JS", "Loss"], ["2H 3H 4H 5H 6H", "AS AD AC AH JD", "Win"], ["AS AH 2H AD AC", "JS JD JC JH 3D", "Win"], ["2S AH 2H AS AC", "JS JD JC JH AD", "Loss"], ["2S AH 2H AS AC", "2H 3H 5H 6H 7H", "Win"]];
  for (const t of tests) {
    let res: string = compare(parse_hand(t[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? t.length + _mochi_idx : _mochi_idx;})()]), parse_hand(t[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? t.length + _mochi_idx : _mochi_idx;})()]));
    console.log(_str(((res + " expected ") + t[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? t.length + _mochi_idx : _mochi_idx;})()])));
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

