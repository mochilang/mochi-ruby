// Generated by Mochi v0.10.70 on 2025-08-22 23:15:57 GMT+7

function parse_row(row_str: string): number[] {
  let nums: number[] = [];
  let current: number = 0;
  let has_digit: boolean = false;
  let i: number = 0;
  while ((i < _len(row_str))) {
    let ch: string = (row_str).substring(i, (i + 1));
    if ((ch === " ")) {
      if (has_digit) {
        nums.push(current);
        current = 0;
        has_digit = false;
      }
    } else {
      current = ((current * 10) + Math.trunc(ch));
      has_digit = true;
    }
    i = (i + 1);
  }
  if (has_digit) {
    nums.push(current);
  }
  return nums;
}
function parse_matrix(matrix_str: string[]): number[][] {
  let matrix: number[][] = [];
  for (const row_str of matrix_str) {
    let row: number[] = parse_row(row_str);
    matrix.push(row);
  }
  return matrix;
}
function bitcount(x: number): number {
  let count: number = 0;
  let y: number = x;
  while ((y > 0)) {
    if (((y % 2) === 1)) {
      count = (count + 1);
    }
    y = Math.trunc(y / 2);
  }
  return count;
}
function build_powers(n: number): number[] {
  let powers: number[] = [];
  let i: number = 0;
  let current: number = 1;
  while ((i <= n)) {
    powers.push(current);
    current = (current * 2);
    i = (i + 1);
  }
  return powers;
}
function solution(matrix_str: string[]): number {
  let arr: number[][] = parse_matrix(matrix_str);
  let n: number = _len(arr);
  let powers: number[] = build_powers(n);
  let size: number = powers[(()=>{const _mochi_idx = n; return _mochi_idx < 0 ? powers.length + _mochi_idx : _mochi_idx;})()];
  let dp: number[] = [];
  let i: number = 0;
  while ((i < size)) {
    dp.push(0);
    i = (i + 1);
  }
  let mask: number = 0;
  while ((mask < size)) {
    let row: number = bitcount(mask);
    if ((row < n)) {
      let col: number = 0;
      while ((col < n)) {
        if (((Math.trunc(mask / powers[(()=>{const _mochi_idx = col; return _mochi_idx < 0 ? powers.length + _mochi_idx : _mochi_idx;})()]) % 2) === 0)) {
          let new_mask: bigint = (mask + powers[(()=>{const _mochi_idx = col; return _mochi_idx < 0 ? powers.length + _mochi_idx : _mochi_idx;})()]);
          let value: bigint = (dp[(()=>{const _mochi_idx = mask; return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()] + arr[(()=>{const _mochi_idx = row; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()][(()=>{const _mochi_idx = col; return _mochi_idx < 0 ? arr[(()=>{const _mochi_idx = row; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()].length + _mochi_idx : _mochi_idx;})()]);
          if ((value > dp[(()=>{const _mochi_idx = new_mask; return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()])) {
            dp[(()=>{const _mochi_idx = new_mask; return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()] = value;
          }
        }
        col = (col + 1);
      }
    }
    mask = (mask + 1);
  }
  return dp[(()=>{const _mochi_idx = Math.trunc((size - 1)); return _mochi_idx < 0 ? dp.length + _mochi_idx : _mochi_idx;})()];
}
let MATRIX_2: string[] = ["7 53 183 439 863 497 383 563 79 973 287 63 343 169 583", "627 343 773 959 943 767 473 103 699 303 957 703 583 639 913", "447 283 463 29 23 487 463 993 119 883 327 493 423 159 743", "217 623 3 399 853 407 103 983 89 463 290 516 212 462 350", "960 376 682 962 300 780 486 502 912 800 250 346 172 812 350", "870 456 192 162 593 473 915 45 989 873 823 965 425 329 803", "973 965 905 919 133 673 665 235 509 613 673 815 165 992 326", "322 148 972 962 286 255 941 541 265 323 925 281 601 95 973", "445 721 11 525 473 65 511 164 138 672 18 428 154 448 848", "414 456 310 312 798 104 566 520 302 248 694 976 430 392 198", "184 829 373 181 631 101 969 613 840 740 778 458 284 760 390", "821 461 843 513 17 901 711 993 293 157 274 94 192 156 574", "34 124 4 878 450 476 712 914 838 669 875 299 823 329 699", "815 559 813 459 522 788 168 586 966 232 308 833 251 631 107", "813 883 451 509 615 77 281 613 459 205 380 274 302 35 805"];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
let result: number
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
result = solution(MATRIX_2)
  console.log(_str(("solution() = " + _str(result))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

