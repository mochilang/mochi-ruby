// Generated by Mochi v0.10.71 on 2025-08-23 15:52:56 GMT+7

function is_9_pandigital(n: number): boolean {
  let digits: number[] = [];
  let i: number = 0;
  while ((i < 10)) {
    digits.push(0);
    i = (i + 1);
  }
  let count: number = 0;
  let x: number = n;
  while ((x > 0)) {
    let digit: bigint = (x % 10);
    if ((digit === 0)) {
      return false;
    }
    if ((digits[(()=>{const _mochi_idx = digit; return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) {
      return false;
    }
    digits[(()=>{const _mochi_idx = digit; return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] = 1;
    x = Math.trunc(x / 10);
    count = (count + 1);
  }
  return ((((((((((count === 9) && (digits[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(4); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(5); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(6); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(7); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(8); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1)) && (digits[(()=>{const _mochi_idx = Math.trunc(9); return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === 1));
}
function solution(): number {
  let base_num: number = 9999;
  while ((base_num >= 5000)) {
    let candidate: bigint = (100002 * base_num);
    if (is_9_pandigital(candidate)) {
      return candidate;
    }
    base_num = (base_num - 1);
  }
  base_num = 333;
  while ((base_num >= 100)) {
    let candidate: bigint = (1002003 * base_num);
    if (is_9_pandigital(candidate)) {
      return candidate;
    }
    base_num = (base_num - 1);
  }
  return 0;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(("solution() = " + _str(solution()))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

