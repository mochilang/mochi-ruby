// Generated by Mochi v0.10.71 on 2025-08-23 15:53:04 GMT+7

function is_substring_divisible(num: number[]): boolean {
  if (!_equal((num[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()] % 2), 0)) {
    return false;
  }
  if (!_equal((((num[(()=>{const _mochi_idx = Math.trunc(2); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()] + num[(()=>{const _mochi_idx = Math.trunc(3); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()]) + num[(()=>{const _mochi_idx = Math.trunc(4); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()]) % 3), 0)) {
    return false;
  }
  if (!_equal((num[(()=>{const _mochi_idx = Math.trunc(5); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()] % 5), 0)) {
    return false;
  }
  let primes: number[] = [7, 11, 13, 17];
  let i: number = 0;
  while ((i < _len(primes))) {
    let p: number = primes[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? primes.length + _mochi_idx : _mochi_idx;})()];
    let idx: bigint = (i + 4);
    let val = (((num[(()=>{const _mochi_idx = idx; return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()] * 100) + (num[(()=>{const _mochi_idx = Math.trunc((idx + 1)); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()] * 10)) + num[(()=>{const _mochi_idx = Math.trunc((idx + 2)); return _mochi_idx < 0 ? num.length + _mochi_idx : _mochi_idx;})()]);
    if (!_equal((val % p), 0)) {
      return false;
    }
    i = (i + 1);
  }
  return true;
}
function remove_at(xs: number[], idx: number): number[] {
  let res: number[] = [];
  let i: number = 0;
  while ((i < _len(xs))) {
    if ((i !== idx)) {
      res.push(xs[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? xs.length + _mochi_idx : _mochi_idx;})()]);
    }
    i = (i + 1);
  }
  return res;
}
function digits_to_number(xs: number[]): number {
  let value: number = 0;
  let i: number = 0;
  while ((i < _len(xs))) {
    value = ((value * 10) + xs[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? xs.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  return value;
}
function search(prefix: number[], remaining: number[]): number {
  if ((_len(remaining) === 0)) {
    if (is_substring_divisible(prefix)) {
      return digits_to_number(prefix);
    }
    return 0;
  }
  let total: number = 0;
  let i: number = 0;
  while ((i < _len(remaining))) {
    let d: number = remaining[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? remaining.length + _mochi_idx : _mochi_idx;})()];
    let next_prefix: any[] = [...prefix, d];
    let next_remaining: number[] = remove_at(remaining, i);
    total = (total + search(next_prefix, next_remaining));
    i = (i + 1);
  }
  return total;
}
function solution(n: number): number {
  let digits: number[] = [];
  let i: number = 0;
  while ((i < n)) {
    digits.push(i);
    i = (i + 1);
  }
  return search([], digits);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _equal(a: unknown, b: unknown): boolean {
  if (typeof a === 'number' && typeof b === 'number') {
    return Math.abs(a - b) < 1e-6;
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === 'object' && typeof b === 'object') {
    const ak = Object.keys(a); const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) { if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) return false; }
    return true;
  }
  return a === b;
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str("solution() =") + " " + _str(solution(10)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

