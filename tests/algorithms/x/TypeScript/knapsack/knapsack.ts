// Generated by Mochi v0.10.63 on 2025-08-12 08:45:23 GMT+7

function knapsack(capacity: number, weights: number[], values: number[], counter: number): number {
  if (((counter == 0) || (capacity == 0))) {
    return 0;
  }
  if ((weights[(()=>{const _mochi_idx = Math.trunc((counter - 1)); return _mochi_idx < 0 ? weights.length + _mochi_idx : _mochi_idx;})()] > capacity)) {
    return knapsack(capacity, weights, values, (counter - 1));
  } else {
    let left_capacity: number = (capacity - weights[(()=>{const _mochi_idx = Math.trunc((counter - 1)); return _mochi_idx < 0 ? weights.length + _mochi_idx : _mochi_idx;})()]);
    let new_value_included = (values[(()=>{const _mochi_idx = Math.trunc((counter - 1)); return _mochi_idx < 0 ? values.length + _mochi_idx : _mochi_idx;})()] + knapsack(left_capacity, weights, values, (counter - 1)));
    let without_new_value = knapsack(capacity, weights, values, (counter - 1));
    if ((new_value_included > without_new_value)) {
      return new_value_included;
    } else {
      return without_new_value;
    }
  }
}
function main() {
  let weights: number[] = [10, 20, 30];
  let values: number[] = [60, 100, 120];
  let cap: number = 50;
  let count = _len(values);
  let result = knapsack(cap, weights, values, count);
  console.log(_str(result));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

