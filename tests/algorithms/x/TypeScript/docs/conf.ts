// Generated by Mochi v0.10.62 on 2025-08-09 16:03:22 GMT+7

function parse_project_name(toml: string): string {
  let i: number = 0;
  let name: string = "";
  let n: number = _len(toml);
  while (((i + 4) < n)) {
    if (((((toml[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()] == "n") && (toml[(()=>{const _mochi_idx = Math.trunc((i + 1)); return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()] == "a")) && (toml[(()=>{const _mochi_idx = Math.trunc((i + 2)); return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()] == "m")) && (toml[(()=>{const _mochi_idx = Math.trunc((i + 3)); return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()] == "e"))) {
      i = (i + 4);
      while (((i < n) && (toml[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()] != "\""))) {
        i = (i + 1);
      }
      i = (i + 1);
      while (((i < n) && (toml[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()] != "\""))) {
        name = (name + toml[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? toml.length + _mochi_idx : _mochi_idx;})()]);
        i = (i + 1);
      }
      return name;
    }
    i = (i + 1);
  }
  return name;
}
let pyproject: string = "[project]\nname = \"thealgorithms-python\"";
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let project: string
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
project = parse_project_name(pyproject)
  console.log(_str(project));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

