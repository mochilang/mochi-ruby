// Generated by Mochi v0.10.66 on 2025-08-15 10:29:41 GMT+7

let MOD_ADLER: number = 65521;
function ord(ch: string): number {
  let lower = "abcdefghijklmnopqrstuvwxyz";
  let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  let digits: string = "0123456789";
  let i: number = 0;
  while ((i < _len(lower))) {
    if ((lower[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? lower.length + _mochi_idx : _mochi_idx;})()] === ch)) {
      return (97 + i);
    }
    i = (i + 1);
  }
  i = 0;
  while ((i < _len(upper))) {
    if ((upper[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? upper.length + _mochi_idx : _mochi_idx;})()] === ch)) {
      return (65 + i);
    }
    i = (i + 1);
  }
  i = 0;
  while ((i < _len(digits))) {
    if ((digits[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? digits.length + _mochi_idx : _mochi_idx;})()] === ch)) {
      return (48 + i);
    }
    i = (i + 1);
  }
  if ((ch === " ")) {
    return 32;
  }
  return 0;
}
function adler32(plain_text: string): number {
  let a: number = 1;
  let b: number = 0;
  let i: number = 0;
  while ((i < _len(plain_text))) {
    let code: number = ord(plain_text[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? plain_text.length + _mochi_idx : _mochi_idx;})()]);
    a = ((a + code) % MOD_ADLER);
    b = ((b + a) % MOD_ADLER);
    i = (i + 1);
  }
  return ((b * 65536) + a);
}
function main() {
  console.log(_str(adler32("Algorithms")));
  console.log(_str(adler32("go adler em all")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

