// Generated by Mochi v0.10.67 on 2025-08-17 12:23:00 GMT+7

function sock_merchant(colors: number[]): number {
  let arr: number[] = [];
  let i: number = 0;
  while ((i < _len(colors))) {
    arr.push(colors[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? colors.length + _mochi_idx : _mochi_idx;})()]);
    i = (i + 1);
  }
  let n: number = _len(arr);
  let a: number = 0;
  while ((a < n)) {
    let min_idx: number = a;
    let b: bigint = (a + 1);
    while ((b < n)) {
      if ((arr[(()=>{const _mochi_idx = b; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] < arr[(()=>{const _mochi_idx = min_idx; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()])) {
        min_idx = b;
      }
      b = (b + 1);
    }
    let temp: number = arr[(()=>{const _mochi_idx = a; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
    arr[(()=>{const _mochi_idx = a; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = arr[(()=>{const _mochi_idx = min_idx; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()];
    arr[(()=>{const _mochi_idx = min_idx; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] = temp;
    a = (a + 1);
  }
  let pairs: number = 0;
  i = 0;
  while ((i < n)) {
    let count: number = 1;
    while ((((i + 1) < n) && (arr[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()] === arr[(()=>{const _mochi_idx = Math.trunc((i + 1)); return _mochi_idx < 0 ? arr.length + _mochi_idx : _mochi_idx;})()]))) {
      count = (count + 1);
      i = (i + 1);
    }
    pairs = (pairs + Math.trunc(count / 2));
    i = (i + 1);
  }
  return pairs;
}
function test_sock_merchant() {
  let example1: number[] = [10, 20, 20, 10, 10, 30, 50, 10, 20];
  if ((sock_merchant(example1) !== 3)) {
    _panic("example1 failed");
  }
  let example2: number[] = [1, 1, 3, 3];
  if ((sock_merchant(example2) !== 2)) {
    _panic("example2 failed");
  }
}
function main() {
  test_sock_merchant();
  let example1: number[] = [10, 20, 20, 10, 10, 30, 50, 10, 20];
  console.log(_str(sock_merchant(example1)));
  let example2: number[] = [1, 1, 3, 3];
  console.log(_str(sock_merchant(example2)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

