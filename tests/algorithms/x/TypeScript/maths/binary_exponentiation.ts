// Generated by Mochi v0.10.66 on 2025-08-16 14:47:59 GMT+7

function binary_exp_recursive(base: number, exponent: number): number {
  if ((exponent < 0)) {
    _panic("exponent must be non-negative");
  }
  if ((exponent === 0)) {
    return 1.0;
  }
  if (((exponent % 2) === 1)) {
    return (binary_exp_recursive(base, (exponent - 1)) * base);
  }
  let half: number = binary_exp_recursive(base, Math.trunc(exponent / 2));
  return (half * half);
}
function binary_exp_iterative(base: number, exponent: number): number {
  if ((exponent < 0)) {
    _panic("exponent must be non-negative");
  }
  let result: number = 1.0;
  let b: number = base;
  let e: number = exponent;
  while ((e > 0)) {
    if (((e % 2) === 1)) {
      result = (result * b);
    }
    b = (b * b);
    e = Math.trunc(e / 2);
  }
  return result;
}
function binary_exp_mod_recursive(base: number, exponent: number, modulus: number): number {
  if ((exponent < 0)) {
    _panic("exponent must be non-negative");
  }
  if ((modulus <= 0)) {
    _panic("modulus must be positive");
  }
  if ((exponent === 0)) {
    return (1 % modulus);
  }
  if (((exponent % 2) === 1)) {
    return ((binary_exp_mod_recursive(base, (exponent - 1), modulus) * (base % modulus)) % modulus);
  }
  let r: number = binary_exp_mod_recursive(base, Math.trunc(exponent / 2), modulus);
  return ((r * r) % modulus);
}
function binary_exp_mod_iterative(base: number, exponent: number, modulus: number): number {
  if ((exponent < 0)) {
    _panic("exponent must be non-negative");
  }
  if ((modulus <= 0)) {
    _panic("modulus must be positive");
  }
  let result: number = (1 % modulus);
  let b: bigint = (base % modulus);
  let e: number = exponent;
  while ((e > 0)) {
    if (((e % 2) === 1)) {
      result = ((result * b) % modulus);
    }
    b = ((b * b) % modulus);
    e = Math.trunc(e / 2);
  }
  return result;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(binary_exp_recursive(3.0, 5)));
  console.log(_str(binary_exp_iterative(1.5, 4)));
  console.log(_str(binary_exp_mod_recursive(3, 4, 5)));
  console.log(_str(binary_exp_mod_iterative(11, 13, 7)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

