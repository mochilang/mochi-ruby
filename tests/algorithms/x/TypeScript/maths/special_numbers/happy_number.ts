// Generated by Mochi v0.10.67 on 2025-08-17 12:23:10 GMT+7

function is_happy_number(num: number): boolean {
  if ((num <= 0)) {
    _panic("num must be a positive integer");
  }
  let seen: number[] = [];
  let n = num;
  while ((n !== 1)) {
    let i: number = 0;
    while ((i < _len(seen))) {
      if ((seen[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? seen.length + _mochi_idx : _mochi_idx;})()] === n)) {
        return false;
      }
      i = (i + 1);
    }
    seen.push(n);
    let total: number = 0;
    let temp = n;
    while ((temp > 0)) {
      let digit = (temp % 10);
      total = (total + (digit * digit));
      temp = Math.trunc(temp / 10);
    }
    n = total;
  }
  return true;
}
function test_is_happy_number() {
  if (!is_happy_number(19)) {
    _panic("19 should be happy");
  }
  if (is_happy_number(2)) {
    _panic("2 should be unhappy");
  }
  if (!is_happy_number(23)) {
    _panic("23 should be happy");
  }
  if (!is_happy_number(1)) {
    _panic("1 should be happy");
  }
}
function main() {
  test_is_happy_number();
  console.log(_str(is_happy_number(19)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

