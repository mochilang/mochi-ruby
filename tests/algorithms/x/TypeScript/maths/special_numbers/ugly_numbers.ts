// Generated by Mochi v0.10.67 on 2025-08-17 12:24:45 GMT+7

function ugly_numbers(n: number): number {
  if ((n <= 0)) {
    return 1;
  }
  let ugly_nums: number[] = [];
  ugly_nums.push(1);
  let i2: number = 0;
  let i3: number = 0;
  let i5: number = 0;
  let next_2: number = 2;
  let next_3: number = 3;
  let next_5: number = 5;
  let count: number = 1;
  while ((count < n)) {
    let next_num: number = ((next_2 < next_3) ? ((next_2 < next_5) ? next_2 : next_5) : ((next_3 < next_5) ? next_3 : next_5));
    ugly_nums.push(next_num);
    if ((next_num === next_2)) {
      i2 = (i2 + 1);
      next_2 = (ugly_nums[(()=>{const _mochi_idx = i2; return _mochi_idx < 0 ? ugly_nums.length + _mochi_idx : _mochi_idx;})()] * 2);
    }
    if ((next_num === next_3)) {
      i3 = (i3 + 1);
      next_3 = (ugly_nums[(()=>{const _mochi_idx = i3; return _mochi_idx < 0 ? ugly_nums.length + _mochi_idx : _mochi_idx;})()] * 3);
    }
    if ((next_num === next_5)) {
      i5 = (i5 + 1);
      next_5 = (ugly_nums[(()=>{const _mochi_idx = i5; return _mochi_idx < 0 ? ugly_nums.length + _mochi_idx : _mochi_idx;})()] * 5);
    }
    count = (count + 1);
  }
  return ugly_nums[(()=>{const _mochi_idx = Math.trunc((_len(ugly_nums) - 1)); return _mochi_idx < 0 ? ugly_nums.length + _mochi_idx : _mochi_idx;})()];
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(ugly_numbers(100)));
  console.log(_str(ugly_numbers(0)));
  console.log(_str(ugly_numbers(20)));
  console.log(_str(ugly_numbers(-5)));
  console.log(_str(ugly_numbers(200)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

