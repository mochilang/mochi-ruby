// Generated by Mochi v0.10.66 on 2025-08-16 14:48:01 GMT+7

function binomial_coefficient(n: number, r: number): number {
  if (((n < 0) || (r < 0))) {
    _panic("n and r must be non-negative integers");
  }
  if (((n === 0) || (r === 0))) {
    return 1;
  }
  let c: number[] = [];
  for (let _ = 0; _ < (r + 1); _++) {
    c.push(0);
  }
  c[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? c.length + _mochi_idx : _mochi_idx;})()] = 1;
  let i: number = 1;
  while ((i <= n)) {
    let j: number = ((i < r) ? i : r);
    while ((j > 0)) {
      c[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? c.length + _mochi_idx : _mochi_idx;})()] = (c[(()=>{const _mochi_idx = j; return _mochi_idx < 0 ? c.length + _mochi_idx : _mochi_idx;})()] + c[(()=>{const _mochi_idx = Math.trunc((j - 1)); return _mochi_idx < 0 ? c.length + _mochi_idx : _mochi_idx;})()]);
      j = (j - 1);
    }
    i = (i + 1);
  }
  return c[(()=>{const _mochi_idx = r; return _mochi_idx < 0 ? c.length + _mochi_idx : _mochi_idx;})()];
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(binomial_coefficient(10, 5)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

