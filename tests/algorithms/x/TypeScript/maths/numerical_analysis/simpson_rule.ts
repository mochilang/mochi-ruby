// Generated by Mochi v0.10.67 on 2025-08-17 09:17:24 GMT+7

function f(x: number): number {
  return ((x - 0.0) * (x - 0.0));
}
function make_points(a: number, b: number, h: number): number[] {
  let points: number[] = [];
  let x: number = (a + h);
  while ((x < (b - h))) {
    points.push(x);
    x = (x + h);
  }
  return points;
}
function simpson_rule(boundary: number[], steps: number): number {
  if ((steps <= 0)) {
    _panic("Number of steps must be greater than zero");
  }
  let a: number = boundary[(()=>{const _mochi_idx = Math.trunc(0); return _mochi_idx < 0 ? boundary.length + _mochi_idx : _mochi_idx;})()];
  let b: number = boundary[(()=>{const _mochi_idx = Math.trunc(1); return _mochi_idx < 0 ? boundary.length + _mochi_idx : _mochi_idx;})()];
  let h: number = ((b - a) / steps);
  let pts: number[] = make_points(a, b, h);
  let y: number = ((h / 3.0) * f(a));
  let cnt: number = 2;
  let i: number = 0;
  while ((i < _len(pts))) {
    let coeff: number = (4.0 - (2.0 * (cnt % 2)));
    y = (y + (((h / 3.0) * coeff) * f(pts[(()=>{const _mochi_idx = i; return _mochi_idx < 0 ? pts.length + _mochi_idx : _mochi_idx;})()])));
    cnt = (cnt + 1);
    i = (i + 1);
  }
  y = (y + ((h / 3.0) * f(b)));
  return y;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return '[' + x.map(_str).join(', ') + ']';
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
let result: number
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
result = simpson_rule([0.0, 1.0], 10)
  console.log(_str(result));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

