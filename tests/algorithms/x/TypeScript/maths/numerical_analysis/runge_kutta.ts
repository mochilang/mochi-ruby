// Generated by Mochi v0.10.67 on 2025-08-17 09:17:18 GMT+7

function runge_kutta(f, y0: number, x0: number, h: number, x_end: number): number[] {
  let span: number = ((x_end - x0) / h);
  let n: number = Math.trunc(span);
  if ((Number(n) < span)) {
    n = (n + 1);
  }
  let y: number[] = [];
  let i: number = 0;
  while ((i < (n + 1))) {
    y.push(0.0);
    i = (i + 1);
  }
  y[(()=>{const _mochi_idx = 0; return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()] = y0;
  let x: number = x0;
  let k: number = 0;
  while ((k < n)) {
    let k1: number = f(x, y[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()]);
    let k2: number = f((x + (0.5 * h)), (y[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()] + ((0.5 * h) * k1)));
    let k3: number = f((x + (0.5 * h)), (y[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()] + ((0.5 * h) * k2)));
    let k4: number = f((x + h), (y[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()] + (h * k3)));
    y[(()=>{const _mochi_idx = (k + 1); return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()] = (y[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? y.length + _mochi_idx : _mochi_idx;})()] + (((1.0 / 6.0) * h) * (((k1 + (2.0 * k2)) + (2.0 * k3)) + k4)));
    x = (x + h);
    k = (k + 1);
  }
  return y;
}
function test_runge_kutta() {
  function f(x: number, y: number): number {
    return y;
  }
  let result = runge_kutta(f, 1.0, 0.0, 0.01, 5.0);
  let last = result[(()=>{const _mochi_idx = Math.trunc((_len(result) - 1)); return _mochi_idx < 0 ? result.length + _mochi_idx : _mochi_idx;})()];
  let expected: number = 148.41315904125113;
  let diff = (last - expected);
  if ((diff < 0.0)) {
    diff = -diff;
  }
  if ((diff > 1e-06)) {
    _panic("runge_kutta failed");
  }
}
function main() {
  test_runge_kutta();
  function f(x: number, y: number): number {
    return y;
  }
  let r = runge_kutta(f, 1.0, 0.0, 0.1, 1.0);
  console.log(_str(r[(()=>{const _mochi_idx = Math.trunc((_len(r) - 1)); return _mochi_idx < 0 ? r.length + _mochi_idx : _mochi_idx;})()]));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _len(x: any): number { return Array.isArray(x) || typeof x === 'string' ? x.length : Object.keys(x ?? {}).length; }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return '[' + x.map(_str).join(', ') + ']';
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

