// Generated by Mochi v0.10.67 on 2025-08-17 12:22:37 GMT+7

function slow_primes(max_n: number): number[] {
  let result: number[] = [];
  let i: number = 2;
  while ((i <= max_n)) {
    let j: number = 2;
    let is_prime: boolean = true;
    while ((j < i)) {
      if (((i % j) === 0)) {
        is_prime = false;
        break
      }
      j = (j + 1);
    }
    if (is_prime) {
      result.push(i);
    }
    i = (i + 1);
  }
  return result;
}
function primes(max_n: number): number[] {
  let result: number[] = [];
  let i: number = 2;
  while ((i <= max_n)) {
    let j: number = 2;
    let is_prime: boolean = true;
    while (((j * j) <= i)) {
      if (((i % j) === 0)) {
        is_prime = false;
        break
      }
      j = (j + 1);
    }
    if (is_prime) {
      result.push(i);
    }
    i = (i + 1);
  }
  return result;
}
function fast_primes(max_n: number): number[] {
  let result: number[] = [];
  if ((max_n >= 2)) {
    result.push(2);
  }
  let i: number = 3;
  while ((i <= max_n)) {
    let j: number = 3;
    let is_prime: boolean = true;
    while (((j * j) <= i)) {
      if (((i % j) === 0)) {
        is_prime = false;
        break
      }
      j = (j + 2);
    }
    if (is_prime) {
      result.push(i);
    }
    i = (i + 2);
  }
  return result;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(slow_primes(25)));
  console.log(_str(primes(25)));
  console.log(_str(fast_primes(25)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

