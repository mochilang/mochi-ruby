// Generated by Mochi v0.10.67 on 2025-08-17 09:17:27 GMT+7

function odd_sieve(num: number): number[] {
  if ((num <= 2)) {
    return [];
  }
  if ((num === 3)) {
    return [2];
  }
  let size = (Math.trunc(num / 2) - 1);
  let sieve: boolean[] = [];
  let idx: number = 0;
  while ((idx < size)) {
    sieve.push(true);
    idx = (idx + 1);
  }
  let i: number = 3;
  while (((i * i) <= num)) {
    let s_idx: bigint = (Math.trunc(i / 2) - 1);
    if (sieve[(()=>{const _mochi_idx = s_idx; return _mochi_idx < 0 ? sieve.length + _mochi_idx : _mochi_idx;})()]) {
      let j: bigint = (i * i);
      while ((j < num)) {
        let j_idx: bigint = (Math.trunc(j / 2) - 1);
        sieve[(()=>{const _mochi_idx = j_idx; return _mochi_idx < 0 ? sieve.length + _mochi_idx : _mochi_idx;})()] = false;
        j = (j + (2 * i));
      }
    }
    i = (i + 2);
  }
  let primes: number[] = [2];
  let n: number = 3;
  let k: number = 0;
  while ((n < num)) {
    if (sieve[(()=>{const _mochi_idx = k; return _mochi_idx < 0 ? sieve.length + _mochi_idx : _mochi_idx;})()]) {
      primes.push(n);
    }
    n = (n + 2);
    k = (k + 1);
  }
  return primes;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return '[' + x.map(_str).join(', ') + ']';
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(odd_sieve(2))));
  console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(odd_sieve(3))));
  console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(odd_sieve(10))));
  console.log(_str(((v)=>v==null?"nil":"[" + v.join(' ') + "]")(odd_sieve(20))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

