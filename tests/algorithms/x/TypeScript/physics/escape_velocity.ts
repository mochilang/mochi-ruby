// Generated by Mochi v0.10.73 on 2025-08-26 08:48:08 GMT+7

function pow10(n: number): number {
  let p: number = 1.0;
  let k: number = 0;
  if ((n >= 0)) {
    while ((k < n)) {
      p = (p * 10.0);
      k = (k + 1);
    }
  } else {
    let m: number = -n;
    while ((k < m)) {
      p = (p / 10.0);
      k = (k + 1);
    }
  }
  return p;
}
function sqrt_newton(n: number): number {
  if (_equal(n, 0.0)) {
    return 0.0;
  }
  let x: number = n;
  let j: number = 0;
  while ((j < 20)) {
    x = ((x + (n / x)) / 2.0);
    j = (j + 1);
  }
  return x;
}
function round3(x: number): number {
  let y: number = ((x * 1000.0) + 0.5);
  let yi: number = Math.trunc(y);
  if ((yi > y)) {
    yi = (yi - 1);
  }
  return (yi / 1000.0);
}
function escape_velocity(mass: number, radius: number): number {
  if (_equal(radius, 0.0)) {
    _panic("Radius cannot be zero.");
  }
  let G: number = (6.6743 * pow10(-11));
  let velocity: number = sqrt_newton((((2.0 * G) * mass) / radius));
  return round3(velocity);
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _equal(a: unknown, b: unknown): boolean {
  if (typeof a === 'number' && typeof b === 'number') {
    return Math.abs(a - b) <= 1e-6 * Math.max(Math.abs(a), Math.abs(b));
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === 'object' && typeof b === 'object') {
    const ak = Object.keys(a); const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) { if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) return false; }
    return true;
  }
  return a === b;
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(escape_velocity(5971999999999999253413888.0, 6.370999999999999e+06)));
  console.log(_str(escape_velocity(73479999999999989514240.0, 1.7369999999999998e+06)));
  console.log(_str(escape_velocity(1897999999999999675025326080.0, 6.991099999999999e+07)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

