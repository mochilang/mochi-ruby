// Generated by Mochi v0.10.73 on 2025-08-26 08:48:05 GMT+7

function format2(x: number): string {
  let sign: string = ((x < 0.0) ? "-" : "");
  let y: number = ((x < 0.0) ? -x : x);
  let m: number = 100.0;
  let scaled: number = (y * m);
  let i: number = Math.trunc(scaled);
  if (((scaled - i) >= 0.5)) {
    i = (i + 1);
  }
  let int_part: number = Math.trunc(i / 100);
  let frac_part: number = (i % 100);
  let frac_str: string = _str(frac_part);
  if ((frac_part < 10)) {
    frac_str = ("0" + frac_str);
  }
  return (((sign + _str(int_part)) + ".") + frac_str);
}
let K: number = 8.9875517923e+09;
function coulombs_law(q1: number, q2: number, radius: number): number {
  if ((radius <= 0.0)) {
    _panic("radius must be positive");
  }
  let force: number = (((K * q1) * q2) / (radius * radius));
  return force;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(format2(coulombs_law(15.5, 20.0, 15.0))));
  console.log(_str(format2(coulombs_law(1.0, 15.0, 5.0))));
  console.log(_str(format2(coulombs_law(20.0, -50.0, 15.0))));
  console.log(_str(format2(coulombs_law(-5.0, -8.0, 10.0))));
  console.log(_str(format2(coulombs_law(50.0, 100.0, 50.0))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

