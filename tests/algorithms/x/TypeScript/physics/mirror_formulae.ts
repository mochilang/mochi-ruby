// Generated by Mochi v0.10.69 on 2025-08-22 13:11:38 GMT+7

function abs_float(x: number): number {
  if ((x < 0.0)) {
    return -x;
  }
  return x;
}
function isclose(a: number, b: number, tolerance: number): boolean {
  return (abs_float((a - b)) < tolerance);
}
function focal_length(distance_of_object: number, distance_of_image: number): number {
  if (((distance_of_object === 0.0) || (distance_of_image === 0.0))) {
    _panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
  }
  return (1.0 / ((1.0 / distance_of_object) + (1.0 / distance_of_image)));
}
function object_distance(focal_length: number, distance_of_image: number): number {
  if (((distance_of_image === 0.0) || (focal_length === 0.0))) {
    _panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
  }
  return (1.0 / ((1.0 / focal_length) - (1.0 / distance_of_image)));
}
function image_distance(focal_length: number, distance_of_object: number): number {
  if (((distance_of_object === 0.0) || (focal_length === 0.0))) {
    _panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
  }
  return (1.0 / ((1.0 / focal_length) - (1.0 / distance_of_object)));
}
function test_focal_length() {
  let f1: number = focal_length(10.0, 20.0);
  if (!isclose(f1, 6.66666666666666, 1e-08)) {
    _panic("focal_length test1 failed");
  }
  let f2: number = focal_length(9.5, 6.7);
  if (!isclose(f2, 3.929012346, 1e-08)) {
    _panic("focal_length test2 failed");
  }
}
function test_object_distance() {
  let u1: number = object_distance(30.0, 20.0);
  if (!isclose(u1, -60.0, 1e-08)) {
    _panic("object_distance test1 failed");
  }
  let u2: number = object_distance(10.5, 11.7);
  if (!isclose(u2, 102.375, 1e-08)) {
    _panic("object_distance test2 failed");
  }
}
function test_image_distance() {
  let v1: number = image_distance(10.0, 40.0);
  if (!isclose(v1, 13.33333333, 1e-08)) {
    _panic("image_distance test1 failed");
  }
  let v2: number = image_distance(1.5, 6.7);
  if (!isclose(v2, 1.932692308, 1e-08)) {
    _panic("image_distance test2 failed");
  }
}
function main() {
  test_focal_length();
  test_object_distance();
  test_image_distance();
  console.log(_str(focal_length(10.0, 20.0)));
  console.log(_str(object_distance(30.0, 20.0)));
  console.log(_str(image_distance(10.0, 40.0)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

