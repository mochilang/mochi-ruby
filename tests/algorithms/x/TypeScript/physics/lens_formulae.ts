// Generated by Mochi v0.10.73 on 2025-08-26 08:48:18 GMT+7

function focal_length_of_lens(object_distance_from_lens: number, image_distance_from_lens: number): number {
  if ((_equal(object_distance_from_lens, 0.0) || _equal(image_distance_from_lens, 0.0))) {
    _panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
  }
  return (1.0 / ((1.0 / image_distance_from_lens) - (1.0 / object_distance_from_lens)));
}
function object_distance(focal_length_of_lens: number, image_distance_from_lens: number): number {
  if ((_equal(image_distance_from_lens, 0.0) || _equal(focal_length_of_lens, 0.0))) {
    _panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
  }
  return (1.0 / ((1.0 / image_distance_from_lens) - (1.0 / focal_length_of_lens)));
}
function image_distance(focal_length_of_lens: number, object_distance_from_lens: number): number {
  if ((_equal(object_distance_from_lens, 0.0) || _equal(focal_length_of_lens, 0.0))) {
    _panic("Invalid inputs. Enter non zero values with respect to the sign convention.");
  }
  return (1.0 / ((1.0 / object_distance_from_lens) + (1.0 / focal_length_of_lens)));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _equal(a: unknown, b: unknown): boolean {
  if (typeof a === 'number' && typeof b === 'number') {
    return Math.abs(a - b) <= 1e-6 * Math.max(Math.abs(a), Math.abs(b));
  }
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === 'object' && typeof b === 'object') {
    const ak = Object.keys(a); const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) { if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) return false; }
    return true;
  }
  return a === b;
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(focal_length_of_lens(10.0, 4.0)));
  console.log(_str(focal_length_of_lens(2.7, 5.8)));
  console.log(_str(object_distance(10.0, 40.0)));
  console.log(_str(object_distance(6.2, 1.5)));
  console.log(_str(image_distance(50.0, 40.0)));
  console.log(_str(image_distance(5.3, 7.9)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

