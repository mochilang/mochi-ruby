// Generated by Mochi v0.10.70 on 2025-08-23 14:54:44 GMT+7

let PI: number = 3.141592653589793;
let R: number = 8.31446261815324;
function sqrt(x: number): number {
  if ((x <= 0.0)) {
    return 0.0;
  }
  let guess: number = x;
  let i: number = 0;
  while ((i < 20)) {
    guess = ((guess + (x / guess)) / 2.0);
    i = (i + 1);
  }
  return guess;
}
function avg_speed_of_molecule(temperature: number, molar_mass: number): number {
  if ((temperature < 0.0)) {
    _panic("Absolute temperature cannot be less than 0 K");
  }
  if ((molar_mass <= 0.0)) {
    _panic("Molar mass should be greater than 0 kg/mol");
  }
  let expr: number = (((8.0 * R) * temperature) / (PI * molar_mass));
  let s: number = sqrt(expr);
  return s;
}
function mps_speed_of_molecule(temperature: number, molar_mass: number): number {
  if ((temperature < 0.0)) {
    _panic("Absolute temperature cannot be less than 0 K");
  }
  if ((molar_mass <= 0.0)) {
    _panic("Molar mass should be greater than 0 kg/mol");
  }
  let expr: number = (((2.0 * R) * temperature) / molar_mass);
  let s: number = sqrt(expr);
  return s;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(avg_speed_of_molecule(273.0, 0.028)));
  console.log(_str(avg_speed_of_molecule(300.0, 0.032)));
  console.log(_str(mps_speed_of_molecule(273.0, 0.028)));
  console.log(_str(mps_speed_of_molecule(300.0, 0.032)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

