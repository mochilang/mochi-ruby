// Generated by Mochi v0.10.73 on 2025-08-26 08:47:56 GMT+7

function to_float(x: number): number {
  return (x * 1.0);
}
function ln(x: number): number {
  if ((x <= 0.0)) {
    _panic("ln domain error");
  }
  let y: number = ((x - 1.0) / (x + 1.0));
  let y2: number = (y * y);
  let term: number = y;
  let sum: number = 0.0;
  let k: number = 0;
  while ((k < 10)) {
    let denom = Number(((2 * k) + 1));
    sum = (sum + (term / denom));
    term = (term * y2);
    k = (k + 1);
  }
  return (2.0 * sum);
}
function exp(x: number): number {
  let term: number = 1.0;
  let sum: number = 1.0;
  let n: number = 1;
  while ((n < 20)) {
    term = ((term * x) / Number(n));
    sum = (sum + term);
    n = (n + 1);
  }
  return sum;
}
function pow_float(base: number, exponent: number): number {
  return exp((exponent * ln(base)));
}
function get_altitude_at_pressure(pressure: number): number {
  if ((pressure > 101325.0)) {
    _panic("Value Higher than Pressure at Sea Level !");
  }
  if ((pressure < 0.0)) {
    _panic("Atmospheric Pressure can not be negative !");
  }
  let ratio: number = (pressure / 101325.0);
  return (44330.0 * (1.0 - pow_float(ratio, (1.0 / 5.5255))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(get_altitude_at_pressure(100000.0)));
  console.log(_str(get_altitude_at_pressure(101325.0)));
  console.log(_str(get_altitude_at_pressure(80000.0)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

