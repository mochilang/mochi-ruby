// Generated by Mochi v0.10.73 on 2025-08-26 08:48:14 GMT+7

let UNIVERSAL_GAS_CONSTANT: number = 8.314462;
function pressure_of_gas_system(moles: number, kelvin: number, volume: number): number {
  if ((((moles < 0) || (kelvin < 0)) || (volume < 0))) {
    _panic("Invalid inputs. Enter positive value.");
  }
  return (((moles * kelvin) * UNIVERSAL_GAS_CONSTANT) / volume);
}
function volume_of_gas_system(moles: number, kelvin: number, pressure: number): number {
  if ((((moles < 0) || (kelvin < 0)) || (pressure < 0))) {
    _panic("Invalid inputs. Enter positive value.");
  }
  return (((moles * kelvin) * UNIVERSAL_GAS_CONSTANT) / pressure);
}
function temperature_of_gas_system(moles: number, volume: number, pressure: number): number {
  if ((((moles < 0) || (volume < 0)) || (pressure < 0))) {
    _panic("Invalid inputs. Enter positive value.");
  }
  return ((pressure * volume) / (moles * UNIVERSAL_GAS_CONSTANT));
}
function moles_of_gas_system(kelvin: number, volume: number, pressure: number): number {
  if ((((kelvin < 0) || (volume < 0)) || (pressure < 0))) {
    _panic("Invalid inputs. Enter positive value.");
  }
  return ((pressure * volume) / (kelvin * UNIVERSAL_GAS_CONSTANT));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = Number((BigInt(_nowSeed) * 1664525n + 1013904223n) % 2147483647n);
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
if (Array.isArray(x)) {
return x.map(_str).join(',');
}
if (x && typeof x === 'object') {
try {
const keys = Object.keys(x).sort();
const obj: any = {};
for (const k of keys) obj[k] = (x as any)[k];
return JSON.stringify(obj);
} catch {
return String(x);
}
}
return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(pressure_of_gas_system(2.0, 100.0, 5.0)));
  console.log(_str(volume_of_gas_system(0.5, 273.0, 0.004)));
  console.log(_str(temperature_of_gas_system(2.0, 100.0, 5.0)));
  console.log(_str(moles_of_gas_system(100.0, 5.0, 10.0)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

