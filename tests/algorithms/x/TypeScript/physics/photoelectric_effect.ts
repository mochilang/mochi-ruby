// Generated by Mochi v0.10.69 on 2025-08-19 16:45:28 GMT+7

function pow10(exp: number): number {
  let result: number = 1.0;
  let i: number = 0;
  while ((i < exp)) {
    result = (result * 10.0);
    i = (i + 1);
  }
  return result;
}
function maximum_kinetic_energy(frequency: number, work_function: number, in_ev: boolean): number {
  if ((frequency < 0.0)) {
    _panic("Frequency can't be negative.");
  }
  let energy: number = (in_ev ? ((PLANCK_CONSTANT_EVS * frequency) - work_function) : ((PLANCK_CONSTANT_JS * frequency) - work_function));
  if ((energy > 0.0)) {
    return energy;
  }
  return 0.0;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  } else {
    _nowSeed = 1;
    _nowSeeded = true;
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  if (typeof performance !== 'undefined' && (performance as any).memory) {
    return (performance as any).memory.usedJSHeapSize;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  if (Array.isArray(x)) {
    return x.map(_str).join(',');
  }
  if (x && typeof x === 'object') {
    try { return JSON.stringify(x); } catch { return String(x); }
  }
  return String(x);
}
function _panic(msg: any): never { throw new Error(String(msg)); }
let PLANCK_CONSTANT_JS: number
let PLANCK_CONSTANT_EVS: number
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
PLANCK_CONSTANT_JS = (6.6261 / pow10(34))
PLANCK_CONSTANT_EVS = (4.1357 / pow10(15))
  console.log(_str(maximum_kinetic_energy(1000000.0, 2.0, false)));
  console.log(_str(maximum_kinetic_energy(1000000.0, 2.0, true)));
  console.log(_str(maximum_kinetic_energy(10000000000000000.0, 2.0, true)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log('\n' + JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

