// Generated by Mochi Zig transpiler on 2025-08-07 14:57 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const plaintext_var: []const u8 = "HELLO";
const key_var: []const u8 = "KEY";
var encrypted_var: []const u8 = &[_]u8{};
var decrypted_var: []const u8 = &[_]u8{};

fn indexOf(s: []const u8, ch: []const u8) i64 {
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(s.len))) {
        if (std.mem.eql(u8, s[@as(usize, @intCast(i))..@min(@as(usize, @intCast(i + 1)), @as(usize, @intCast(s.len)))], ch)) {
            return i;
        }
        i = i + 1;
    }
    return 0 - 1;
}

fn ord(ch: []const u8) i64 {
    var upper: []const u8 = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    upper = upper;
    var idx: i64 = indexOf(upper, ch);
    idx = idx;
    if (idx >= 0) {
        return 65 + idx;
    }
    return 0;
}

fn chr(n: i64) []const u8 {
    var upper: []const u8 = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
    upper = upper;
    if (n >= 65 and n < 91) {
        return upper[@as(usize, @intCast(n - 65))..@min(@as(usize, @intCast(n - 64)), @as(usize, @intCast(upper.len)))];
    }
    return "?";
}

fn vernam_encrypt(plaintext_param: []const u8, key_param: []const u8) []const u8 {
    var ciphertext: []const u8 = "";
    ciphertext = ciphertext;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(plaintext_param.len))) {
        var p: i64 = ord(plaintext_param[@as(usize, @intCast(i))..@min(@as(usize, @intCast(i + 1)), @as(usize, @intCast(plaintext_param.len)))]) - 65;
        p = p;
        var k: i64 = ord(key_param[@as(usize, @intCast(@mod(i, @as(i64, @intCast(key_param.len)))))..@min(@as(usize, @intCast(@mod(i, @as(i64, @intCast(key_param.len))) + 1)), @as(usize, @intCast(key_param.len)))]) - 65;
        k = k;
        var ct: i64 = p + k;
        ct = ct;
        while (ct > 25) {
            ct = ct - 26;
        }
        ciphertext = _concat_string(ciphertext, chr(ct + 65));
        i = i + 1;
    }
    return ciphertext;
}

fn vernam_decrypt(ciphertext: []const u8, key_param: []const u8) []const u8 {
    var decrypted_var_1: []const u8 = "";
    decrypted_var_1 = decrypted_var_1;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(ciphertext.len))) {
        var c: i64 = ord(ciphertext[@as(usize, @intCast(i))..@min(@as(usize, @intCast(i + 1)), @as(usize, @intCast(ciphertext.len)))]);
        c = c;
        var k: i64 = ord(key_param[@as(usize, @intCast(@mod(i, @as(i64, @intCast(key_param.len)))))..@min(@as(usize, @intCast(@mod(i, @as(i64, @intCast(key_param.len))) + 1)), @as(usize, @intCast(key_param.len)))]);
        k = k;
        var val: i64 = c - k;
        val = val;
        while (val < 0) {
            val = val + 26;
        }
        decrypted_var_1 = _concat_string(decrypted_var_1, chr(val + 65));
        i = i + 1;
    }
    return decrypted_var_1;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        encrypted_var = vernam_encrypt(plaintext_var, key_var);
        decrypted_var = vernam_decrypt(encrypted_var, key_var);
        std.debug.print("{s}\n", .{_concat_string("Plaintext: ", plaintext_var)});
        std.debug.print("{s}\n", .{_concat_string("Encrypted: ", encrypted_var)});
        std.debug.print("{s}\n", .{_concat_string("Decrypted: ", decrypted_var)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
