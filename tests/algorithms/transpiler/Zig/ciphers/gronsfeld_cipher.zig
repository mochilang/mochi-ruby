// Generated by Mochi Zig transpiler on 2025-08-07 14:57 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const ASCII_UPPERCASE_var: []const u8 = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
const ASCII_LOWERCASE_var: []const u8 = "abcdefghijklmnopqrstuvwxyz";
var NEG_ONE_var: i64 = 0;

fn index_of(s: []const u8, ch: []const u8) i64 {
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(s.len))) {
        if (std.mem.eql(u8, s[@as(usize, @intCast(i))..@min(@as(usize, @intCast(i + 1)), @as(usize, @intCast(s.len)))], ch)) {
            return i;
        }
        i = i + 1;
    }
    return NEG_ONE_var;
}

fn to_uppercase(s: []const u8) []const u8 {
    var result: []const u8 = "";
    result = result;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(s.len))) {
        var ch: []const u8 = s[@as(usize, @intCast(i))..@min(@as(usize, @intCast(i + 1)), @as(usize, @intCast(s.len)))];
        ch = ch;
        var idx: i64 = index_of(ASCII_LOWERCASE_var, ch);
        idx = idx;
        if (idx == NEG_ONE_var) {
            result = _concat_string(result, ch);
        } else {
            result = _concat_string(result, ASCII_UPPERCASE_var[@as(usize, @intCast(idx))..@min(@as(usize, @intCast(idx + 1)), @as(usize, @intCast(ASCII_UPPERCASE_var.len)))]);
        }
        i = i + 1;
    }
    return result;
}

fn gronsfeld(text: []const u8, key: []const u8) []const u8 {
    var ascii_len: i64 = @as(i64, @intCast(ASCII_UPPERCASE_var.len));
    ascii_len = ascii_len;
    var key_len: i64 = @as(i64, @intCast(key.len));
    key_len = key_len;
    if (key_len == 0) {
        @panic("integer modulo by zero");
    }
    var upper_text: []const u8 = to_uppercase(text);
    upper_text = upper_text;
    var encrypted: []const u8 = "";
    encrypted = encrypted;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(upper_text.len))) {
        var ch: []const u8 = upper_text[@as(usize, @intCast(i))..@min(@as(usize, @intCast(i + 1)), @as(usize, @intCast(upper_text.len)))];
        ch = ch;
        var idx: i64 = index_of(ASCII_UPPERCASE_var, ch);
        idx = idx;
        if (idx == NEG_ONE_var) {
            encrypted = _concat_string(encrypted, ch);
        } else {
            var key_idx: i64 = @mod(i, key_len);
            key_idx = key_idx;
            var shift: i64 = std.fmt.parseInt(i64, key[@as(usize, @intCast(key_idx))..@min(@as(usize, @intCast(key_idx + 1)), @as(usize, @intCast(key.len)))], 10) catch 0;
            shift = shift;
            var new_position: i64 = @mod(idx + shift, ascii_len);
            new_position = new_position;
            encrypted = _concat_string(encrypted, ASCII_UPPERCASE_var[@as(usize, @intCast(new_position))..@min(@as(usize, @intCast(new_position + 1)), @as(usize, @intCast(ASCII_UPPERCASE_var.len)))]);
        }
        i = i + 1;
    }
    return encrypted;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        NEG_ONE_var = 0 - 1;
        std.debug.print("{s}\n", .{gronsfeld("hello", "412")});
        std.debug.print("{s}\n", .{gronsfeld("hello", "123")});
        std.debug.print("{s}\n", .{gronsfeld("", "123")});
        std.debug.print("{s}\n", .{gronsfeld("yes, ¥€$ - _!@#%?", "0")});
        std.debug.print("{s}\n", .{gronsfeld("yes, ¥€$ - _!@#%?", "01")});
        std.debug.print("{s}\n", .{gronsfeld("yes, ¥€$ - _!@#%?", "012")});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
