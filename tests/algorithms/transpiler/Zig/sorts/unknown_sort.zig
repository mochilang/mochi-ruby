// Generated by Mochi Zig transpiler on 2025-08-24 08:57 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn list_min(xs: []i64) i64 {
    var i: i64 = 1;
    i = i;
    var m: i64 = xs[_idx(xs.len, 0)];
    m = m;
    while (i < @as(i64, @intCast(xs.len))) {
        if (xs[_idx(xs.len, i)] < m) {
            m = xs[_idx(xs.len, i)];
        }
        i = i +% 1;
    }
    return m;
}

fn list_max(xs_1: []i64) i64 {
    var i_1: i64 = 1;
    i_1 = i_1;
    var m_1: i64 = xs_1[_idx(xs_1.len, 0)];
    m_1 = m_1;
    while (i_1 < @as(i64, @intCast(xs_1.len))) {
        if (xs_1[_idx(xs_1.len, i_1)] > m_1) {
            m_1 = xs_1[_idx(xs_1.len, i_1)];
        }
        i_1 = i_1 +% 1;
    }
    return m_1;
}

fn remove_once(xs_2: []i64, value: i64) []i64 {
    var res: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    res = res;
    var removed: bool = false;
    removed = removed;
    var i_2: i64 = 0;
    i_2 = i_2;
    while (i_2 < @as(i64, @intCast(xs_2.len))) {
        if (!(removed) and xs_2[_idx(xs_2.len, i_2)] == value) {
            removed = true;
        } else {
            res = blk0: { var _tmp = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, res)) catch |err| handleError(err); _tmp.append(xs_2[_idx(xs_2.len, i_2)]) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
        i_2 = i_2 +% 1;
    }
    return res;
}

fn reverse_list(xs_3: []i64) []i64 {
    var res_1: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    res_1 = res_1;
    var i_3: i64 = @as(i64, @intCast(xs_3.len)) -% 1;
    i_3 = i_3;
    while (i_3 >= 0) {
        res_1 = blk1: { var _tmp_1 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_1.appendSlice(@as([]const i64, res_1)) catch |err| handleError(err); _tmp_1.append(xs_3[_idx(xs_3.len, i_3)]) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
        i_3 = i_3 -% 1;
    }
    return res_1;
}

fn merge_sort(collection: []i64) []i64 {
    var start: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    start = start;
    var end: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    end = end;
    var coll: []i64 = blk2: { const tmp = std.heap.page_allocator.alloc(i64, collection.len) catch unreachable; @memcpy(tmp, collection); break :blk2 tmp; };
    coll = coll;
    while (@as(i64, @intCast(coll.len)) > 1) {
        const mn: i64 = list_min(coll);
        const mx: i64 = list_max(coll);
        start = blk3: { var _tmp_2 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_2.appendSlice(@as([]const i64, start)) catch |err| handleError(err); _tmp_2.append(mn) catch |err| handleError(err); break :blk3 (_tmp_2.toOwnedSlice() catch |err| handleError(err)); };
        end = blk4: { var _tmp_3 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_3.appendSlice(@as([]const i64, end)) catch |err| handleError(err); _tmp_3.append(mx) catch |err| handleError(err); break :blk4 (_tmp_3.toOwnedSlice() catch |err| handleError(err)); };
        coll = remove_once(coll, mn);
        coll = remove_once(coll, mx);
    }
    end = reverse_list(end);
    return blk5: { var _tmp_4 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_4.appendSlice(@as([]const i64, blk6: { var _tmp_5 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_5.appendSlice(@as([]const i64, start)) catch |err| handleError(err); _tmp_5.appendSlice(@as([]const i64, coll)) catch |err| handleError(err); break :blk6 (_tmp_5.toOwnedSlice() catch |err| handleError(err)); })) catch |err| handleError(err); _tmp_4.appendSlice(@as([]const i64, end)) catch |err| handleError(err); break :blk5 (_tmp_4.toOwnedSlice() catch |err| handleError(err)); };
}

fn test_merge_sort() void {
    if (!std.mem.eql(i64, merge_sort(blk7: { var _tmp_6 = [5]i64{0, 5, 3, 2, 2}; break :blk7 _tmp_6[0..]; }), @constCast(([5]i64{0, 2, 2, 3, 5})[0..5]))) {
        @panic("case1 failed");
    }
    if (!std.mem.eql(i64, merge_sort(@constCast((&[_]i64{})[0..0])), @constCast((&[_]i64{})[0..0]))) {
        @panic("case2 failed");
    }
    if (!std.mem.eql(i64, merge_sort(blk8: { var _tmp_7 = [3]i64{0 -% 2, 0 -% 5, 0 -% 45}; break :blk8 _tmp_7[0..]; }), @constCast(blk9: { var _tmp_8 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_8.append(0 -% 45) catch unreachable; _tmp_8.append(0 -% 5) catch unreachable; _tmp_8.append(0 -% 2) catch unreachable; break :blk9 (_tmp_8.toOwnedSlice() catch unreachable); }))) {
        @panic("case3 failed");
    }
}

fn mochi_main() void {
    test_merge_sort();
    std.debug.print("{s}\n", .{_str(merge_sort(blk10: { var _tmp_9 = [5]i64{0, 5, 3, 2, 2}; break :blk10 _tmp_9[0..]; }))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
