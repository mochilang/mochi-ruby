// Generated by Mochi Zig transpiler on 2025-08-13 07:12 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const LcsResult = struct {
    length: i64,
    sequence: []const u8,
};

const a_var: []const u8 = "AGGTAB";
const b_var: []const u8 = "GXTXAYB";
var res_var: LcsResult = undefined;

fn zeros_matrix(rows_param: i64, cols_param: i64) [][]i64 {
    var rows_var: i64 = rows_param;
    rows_var = rows_var;
    var cols_var: i64 = cols_param;
    cols_var = cols_var;
    var matrix: [][]i64 = std.heap.page_allocator.alloc([]i64, 0) catch unreachable;
    matrix = matrix;
    var i: i64 = 0;
    i = i;
    while (i <= rows_var) {
        var row: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
        row = row;
        var j: i64 = 0;
        j = j;
        while (j <= cols_var) {
            row = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, row)) catch |err| handleError(err); _tmp.append(0) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
            j = j + 1;
        }
        matrix = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, matrix)) catch |err| handleError(err); _tmp.append(row) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    return matrix;
}

fn longest_common_subsequence(x_param: []const u8, y_param: []const u8) LcsResult {
    var x_var: []const u8 = x_param;
    x_var = x_var;
    var y_var: []const u8 = y_param;
    y_var = y_var;
    const m: i64 = @as(i64, @intCast(x_var.len));
    const n: i64 = @as(i64, @intCast(y_var.len));
    var dp: [][]i64 = zeros_matrix(m, n);
    dp = dp;
    var i: i64 = 1;
    i = i;
    while (i <= m) {
        var j: i64 = 1;
        j = j;
        while (j <= n) {
            if (std.mem.eql(u8, x_var[_idx(x_var.len, i - 1).._idx(x_var.len, i - 1) + 1], y_var[_idx(y_var.len, j - 1).._idx(y_var.len, j - 1) + 1])) {
                dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = dp[_idx(dp.len, i - 1)][_idx(dp[_idx(dp.len, i - 1)].len, j - 1)] + 1;
            } else {
                if (dp[_idx(dp.len, i - 1)][_idx(dp[_idx(dp.len, i - 1)].len, j)] > dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j - 1)]) {
                    dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = dp[_idx(dp.len, i - 1)][_idx(dp[_idx(dp.len, i - 1)].len, j)];
                } else {
                    dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j - 1)];
                }
            }
            j = j + 1;
        }
        i = i + 1;
    }
    var seq: []const u8 = "";
    seq = seq;
    var _i2: i64 = m;
    _i2 = _i2;
    var j2: i64 = n;
    j2 = j2;
    while (_i2 > 0 and j2 > 0) {
        if (std.mem.eql(u8, x_var[_idx(x_var.len, _i2 - 1).._idx(x_var.len, _i2 - 1) + 1], y_var[_idx(y_var.len, j2 - 1).._idx(y_var.len, j2 - 1) + 1])) {
            seq = _concat_string(x_var[_idx(x_var.len, _i2 - 1).._idx(x_var.len, _i2 - 1) + 1], seq);
            _i2 = _i2 - 1;
            j2 = j2 - 1;
        } else {
            if (dp[_idx(dp.len, _i2 - 1)][_idx(dp[_idx(dp.len, _i2 - 1)].len, j2)] >= dp[_idx(dp.len, _i2)][_idx(dp[_idx(dp.len, _i2)].len, j2 - 1)]) {
                _i2 = _i2 - 1;
            } else {
                j2 = j2 - 1;
            }
        }
    }
    return .{ .length = dp[_idx(dp.len, m)][_idx(dp[_idx(dp.len, m)].len, n)], .sequence = seq };
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        res_var = longest_common_subsequence(a_var, b_var);
        std.debug.print("{s}\n", .{_concat_string(_concat_string(_concat_string("len = ", _str(res_var.length)), ", sub-sequence = "), res_var.sequence)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
