// Generated by Mochi Zig transpiler on 2025-08-13 07:12 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

var empty_var: []f64 = &[_]f64{};

fn max_subarray_sum(nums_param: []f64, allow_empty_param: bool) f64 {
    var nums_var: []f64 = nums_param;
    nums_var = nums_var;
    var allow_empty_var: bool = allow_empty_param;
    allow_empty_var = allow_empty_var;
    if (@as(i64, @intCast(nums_var.len)) == 0) {
        return 0.0;
    }
    var max_sum: f64 = 0.0;
    max_sum = max_sum;
    var curr_sum: f64 = 0.0;
    curr_sum = curr_sum;
    if (allow_empty_var) {
        max_sum = 0.0;
        curr_sum = 0.0;
        var i: i64 = 0;
        i = i;
        while (i < @as(i64, @intCast(nums_var.len))) {
            const num: f64 = nums_var[_idx(nums_var.len, i)];
            const temp: f64 = curr_sum + num;
            curr_sum = if (temp > 0.0) temp else 0.0;
            if (curr_sum > max_sum) {
                max_sum = curr_sum;
            }
            i = i + 1;
        }
    } else {
        max_sum = nums_var[_idx(nums_var.len, 0)];
        curr_sum = nums_var[_idx(nums_var.len, 0)];
        var i: i64 = 1;
        i = i;
        while (i < @as(i64, @intCast(nums_var.len))) {
            const num: f64 = nums_var[_idx(nums_var.len, i)];
            const temp: f64 = curr_sum + num;
            curr_sum = if (temp > num) temp else num;
            if (curr_sum > max_sum) {
                max_sum = curr_sum;
            }
            i = i + 1;
        }
    }
    return max_sum;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        empty_var = std.heap.page_allocator.alloc(f64, 0) catch unreachable;
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([3]f64{2.0, 8.0, 9.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([2]f64{0.0, 0.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([3]f64{@as(f64, @floatFromInt(0)) - 1.0, 0.0, 1.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([5]f64{1.0, 2.0, 3.0, 4.0, @as(f64, @floatFromInt(0)) - 2.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([9]f64{@as(f64, @floatFromInt(0)) - 2.0, 1.0, @as(f64, @floatFromInt(0)) - 3.0, 4.0, @as(f64, @floatFromInt(0)) - 1.0, 2.0, 1.0, @as(f64, @floatFromInt(0)) - 5.0, 4.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([5]f64{2.0, 3.0, @as(f64, @floatFromInt(0)) - 9.0, 8.0, @as(f64, @floatFromInt(0)) - 2.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([5]f64{@as(f64, @floatFromInt(0)) - 2.0, @as(f64, @floatFromInt(0)) - 3.0, @as(f64, @floatFromInt(0)) - 1.0, @as(f64, @floatFromInt(0)) - 4.0, @as(f64, @floatFromInt(0)) - 6.0}))[0..], false))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(@constCast(&([5]f64{@as(f64, @floatFromInt(0)) - 2.0, @as(f64, @floatFromInt(0)) - 3.0, @as(f64, @floatFromInt(0)) - 1.0, @as(f64, @floatFromInt(0)) - 4.0, @as(f64, @floatFromInt(0)) - 6.0}))[0..], true))});
        std.debug.print("{s}\n", .{_str(max_subarray_sum(empty_var, false))});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
