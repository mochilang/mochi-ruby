// Generated by Mochi Zig transpiler on 2025-08-13 07:12 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn make_list(len_param: i64, value_param: i64) []i64 {
    var len_var: i64 = len_param;
    len_var = len_var;
    var value_var: i64 = value_param;
    value_var = value_var;
    var arr: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    arr = arr;
    var i: i64 = 0;
    i = i;
    while (i < len_var) {
        arr = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, arr)) catch |err| handleError(err); _tmp.append(value_var) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    return arr;
}

fn trapped_rainwater(heights_param: []i64) i64 {
    var heights_var: []i64 = heights_param;
    heights_var = heights_var;
    if (@as(i64, @intCast(heights_var.len)) == 0) {
        return 0;
    }
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(heights_var.len))) {
        if (heights_var[_idx(heights_var.len, i)] < 0) {
            @panic("No height can be negative");
        }
        i = i + 1;
    }
    const length: i64 = @as(i64, @intCast(heights_var.len));
    var left_max: []i64 = make_list(length, 0);
    left_max = left_max;
    left_max[_idx(left_max.len, 0)] = heights_var[_idx(heights_var.len, 0)];
    i = 1;
    while (i < length) {
        if (heights_var[_idx(heights_var.len, i)] > left_max[_idx(left_max.len, i - 1)]) {
            left_max[_idx(left_max.len, i)] = heights_var[_idx(heights_var.len, i)];
        } else {
            left_max[_idx(left_max.len, i)] = left_max[_idx(left_max.len, i - 1)];
        }
        i = i + 1;
    }
    var right_max: []i64 = make_list(length, 0);
    right_max = right_max;
    const last: i64 = length - 1;
    right_max[_idx(right_max.len, last)] = heights_var[_idx(heights_var.len, last)];
    i = last - 1;
    while (i >= 0) {
        if (heights_var[_idx(heights_var.len, i)] > right_max[_idx(right_max.len, i + 1)]) {
            right_max[_idx(right_max.len, i)] = heights_var[_idx(heights_var.len, i)];
        } else {
            right_max[_idx(right_max.len, i)] = right_max[_idx(right_max.len, i + 1)];
        }
        i = i - 1;
    }
    var total: i64 = 0;
    total = total;
    i = 0;
    while (i < length) {
        const left: i64 = left_max[_idx(left_max.len, i)];
        const right: i64 = right_max[_idx(right_max.len, i)];
        const smaller: i64 = if (left < right) left else right;
        total = total + (smaller - heights_var[_idx(heights_var.len, i)]);
        i = i + 1;
    }
    return total;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        std.debug.print("{s}\n", .{_str(trapped_rainwater(@constCast(&([12]i64{0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1}))[0..]))});
        std.debug.print("{s}\n", .{_str(trapped_rainwater(@constCast(&([6]i64{7, 1, 5, 3, 6, 4}))[0..]))});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
