// Generated by Mochi Zig transpiler on 2025-08-13 07:12 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn recursive_match(text_param: []const u8, pattern_param: []const u8) bool {
    var text_var: []const u8 = text_param;
    text_var = text_var;
    var pattern_var: []const u8 = pattern_param;
    pattern_var = pattern_var;
    if (@as(i64, @intCast(pattern_var.len)) == 0) {
        return @as(i64, @intCast(text_var.len)) == 0;
    }
    if (@as(i64, @intCast(text_var.len)) == 0) {
        if (@as(i64, @intCast(pattern_var.len)) >= 2 and std.mem.eql(u8, pattern_var[@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)) - 1))..@min(@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)))), @as(usize, @intCast(pattern_var.len)))], "*")) {
            return recursive_match(text_var, pattern_var[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)) - 2)), @as(usize, @intCast(pattern_var.len)))]);
        }
        return false;
    }
    const last_text: []const u8 = text_var[@as(usize, @intCast(@as(i64, @intCast(text_var.len)) - 1))..@min(@as(usize, @intCast(@as(i64, @intCast(text_var.len)))), @as(usize, @intCast(text_var.len)))];
    const last_pattern: []const u8 = pattern_var[@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)) - 1))..@min(@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)))), @as(usize, @intCast(pattern_var.len)))];
    if (std.mem.eql(u8, last_text, last_pattern) or std.mem.eql(u8, last_pattern, ".")) {
        return recursive_match(text_var[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(text_var.len)) - 1)), @as(usize, @intCast(text_var.len)))], pattern_var[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)) - 1)), @as(usize, @intCast(pattern_var.len)))]);
    }
    if (std.mem.eql(u8, last_pattern, "*")) {
        if (recursive_match(text_var[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(text_var.len)) - 1)), @as(usize, @intCast(text_var.len)))], pattern_var)) {
            return true;
        }
        return recursive_match(text_var, pattern_var[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(pattern_var.len)) - 2)), @as(usize, @intCast(pattern_var.len)))]);
    }
    return false;
}

fn dp_match(text_param_1: []const u8, pattern_param_1: []const u8) bool {
    var text_var: []const u8 = text_param_1;
    text_var = text_var;
    var pattern_var: []const u8 = pattern_param_1;
    pattern_var = pattern_var;
    const m: i64 = @as(i64, @intCast(text_var.len));
    const n: i64 = @as(i64, @intCast(pattern_var.len));
    var dp: [][]bool = std.heap.page_allocator.alloc([]bool, 0) catch unreachable;
    dp = dp;
    var i: i64 = 0;
    i = i;
    while (i <= m) {
        var row: []bool = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
        row = row;
        var j: i64 = 0;
        j = j;
        while (j <= n) {
            row = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, row)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
            j = j + 1;
        }
        dp = blk: { var _tmp = std.ArrayList([]bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []bool, dp)) catch |err| handleError(err); _tmp.append(row) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    dp[_idx(dp.len, 0)][_idx(dp[_idx(dp.len, 0)].len, 0)] = true;
    var j: i64 = 1;
    j = j;
    while (j <= n) {
        if (std.mem.eql(u8, pattern_var[@as(usize, @intCast(j - 1))..@min(@as(usize, @intCast(j)), @as(usize, @intCast(pattern_var.len)))], "*") and j >= 2) {
            if (dp[_idx(dp.len, 0)][_idx(dp[_idx(dp.len, 0)].len, j - 2)]) {
                dp[_idx(dp.len, 0)][_idx(dp[_idx(dp.len, 0)].len, j)] = true;
            }
        }
        j = j + 1;
    }
    i = 1;
    while (i <= m) {
        j = 1;
        while (j <= n) {
            const p_char: []const u8 = pattern_var[@as(usize, @intCast(j - 1))..@min(@as(usize, @intCast(j)), @as(usize, @intCast(pattern_var.len)))];
            const t_char: []const u8 = text_var[@as(usize, @intCast(i - 1))..@min(@as(usize, @intCast(i)), @as(usize, @intCast(text_var.len)))];
            if (std.mem.eql(u8, p_char, ".") or std.mem.eql(u8, p_char, t_char)) {
                if (dp[_idx(dp.len, i - 1)][_idx(dp[_idx(dp.len, i - 1)].len, j - 1)]) {
                    dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = true;
                }
            } else {
                if (std.mem.eql(u8, p_char, "*")) {
                    if (j >= 2) {
                        if (dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j - 2)]) {
                            dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = true;
                        }
                        const prev_p: []const u8 = pattern_var[@as(usize, @intCast(j - 2))..@min(@as(usize, @intCast(j - 1)), @as(usize, @intCast(pattern_var.len)))];
                        if (std.mem.eql(u8, prev_p, ".") or std.mem.eql(u8, prev_p, t_char)) {
                            if (dp[_idx(dp.len, i - 1)][_idx(dp[_idx(dp.len, i - 1)].len, j)]) {
                                dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = true;
                            }
                        }
                    }
                } else {
                    dp[_idx(dp.len, i)][_idx(dp[_idx(dp.len, i)].len, j)] = false;
                }
            }
            j = j + 1;
        }
        i = i + 1;
    }
    return dp[_idx(dp.len, m)][_idx(dp[_idx(dp.len, m)].len, n)];
}

fn print_bool(b_param: bool) void {
    var b_var: bool = b_param;
    b_var = b_var;
    if (b_var) {
        std.debug.print("{s}\n", .{_str(true)});
    } else {
        std.debug.print("{s}\n", .{_str(false)});
    }
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        print_bool(recursive_match("abc", "a.c"));
        print_bool(recursive_match("abc", "af*.c"));
        print_bool(recursive_match("abc", "a.c*"));
        print_bool(recursive_match("abc", "a.c*d"));
        print_bool(recursive_match("aa", ".*"));
        print_bool(dp_match("abc", "a.c"));
        print_bool(dp_match("abc", "af*.c"));
        print_bool(dp_match("abc", "a.c*"));
        print_bool(dp_match("abc", "a.c*d"));
        print_bool(dp_match("aa", ".*"));
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
