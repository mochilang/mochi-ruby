// Generated by Mochi Zig transpiler on 2025-08-16 13:51 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn topology_sort(graph: [][]i64, vert: i64, visited_param: []bool) []i64 {
    var visited_var: []bool = visited_param;
    visited_var = visited_var;
    visited_var[_idx(visited_var.len, vert)] = true;
    var order: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    order = order;
    for (graph[_idx(graph.len, vert)]) |__it0| {
        const neighbour_3 = __it0;
        if (!(visited_var[_idx(visited_var.len, neighbour_3)])) {
            order = blk0: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, order)) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, topology_sort(graph, neighbour_3, visited_var))) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    order = blk1: { var _tmp_1 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_1.appendSlice(@as([]const i64, order)) catch |err| handleError(err); _tmp_1.append(vert) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
    return order;
}

fn find_component(graph_1: [][]i64, vert_1: i64, visited_param_1: []bool) []i64 {
    var visited_var_1: []bool = visited_param_1;
    visited_var_1 = visited_var_1;
    visited_var_1[_idx(visited_var_1.len, vert_1)] = true;
    var comp: []i64 = blk2: { var _tmp_2 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_2.append(vert_1) catch unreachable; break :blk2 (_tmp_2.toOwnedSlice() catch unreachable); };
    comp = comp;
    for (graph_1[_idx(graph_1.len, vert_1)]) |__it1| {
        const neighbour_1 = __it1;
        if (!(visited_var_1[_idx(visited_var_1.len, neighbour_1)])) {
            comp = blk3: { var _tmp_3 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_3.appendSlice(@as([]const i64, comp)) catch |err| handleError(err); _tmp_3.appendSlice(@as([]const i64, find_component(graph_1, neighbour_1, visited_var_1))) catch |err| handleError(err); break :blk3 (_tmp_3.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    return comp;
}

fn strongly_connected_components(graph_2: [][]i64) [][]i64 {
    const n: i64 = @as(i64, @intCast(graph_2.len));
    var visited: []bool = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
    visited = visited;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |_| {
        visited = blk4: { var _tmp_4 = std.ArrayList(bool).init(std.heap.page_allocator); _tmp_4.appendSlice(@as([]const bool, visited)) catch |err| handleError(err); _tmp_4.append(false) catch |err| handleError(err); break :blk4 (_tmp_4.toOwnedSlice() catch |err| handleError(err)); };
    }
    var reversed: [][]i64 = std.heap.page_allocator.alloc([]i64, 0) catch unreachable;
    reversed = reversed;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |_| {
        reversed = blk5: { var _tmp_5 = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp_5.appendSlice(@as([]const []i64, reversed)) catch |err| handleError(err); _tmp_5.append(@constCast((&[_]i64{})[0..0])) catch |err| handleError(err); break :blk5 (_tmp_5.toOwnedSlice() catch |err| handleError(err)); };
    }
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |__it4| {
        const i_3: i64 = @as(i64, @intCast(__it4));
        for (graph_2[_idx(graph_2.len, i_3)]) |__it5| {
            const neighbour_2 = __it5;
            reversed[_idx(reversed.len, neighbour_2)] = blk6: { var _tmp_6 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_6.appendSlice(@as([]const i64, reversed[_idx(reversed.len, neighbour_2)])) catch |err| handleError(err); _tmp_6.append(i_3) catch |err| handleError(err); break :blk6 (_tmp_6.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    var order_1: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    order_1 = order_1;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |__it6| {
        const i_1: i64 = @as(i64, @intCast(__it6));
        if (!(visited[_idx(visited.len, i_1)])) {
            order_1 = blk7: { var _tmp_7 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_7.appendSlice(@as([]const i64, order_1)) catch |err| handleError(err); _tmp_7.appendSlice(@as([]const i64, topology_sort(graph_2, i_1, visited))) catch |err| handleError(err); break :blk7 (_tmp_7.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    visited = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
    for (@as(usize, @intCast(0))..@as(usize, @intCast(n))) |_| {
        visited = blk8: { var _tmp_8 = std.ArrayList(bool).init(std.heap.page_allocator); _tmp_8.appendSlice(@as([]const bool, visited)) catch |err| handleError(err); _tmp_8.append(false) catch |err| handleError(err); break :blk8 (_tmp_8.toOwnedSlice() catch |err| handleError(err)); };
    }
    var components: [][]i64 = std.heap.page_allocator.alloc([]i64, 0) catch unreachable;
    components = components;
    var i_2: i64 = 0;
    i_2 = i_2;
    while (i_2 < n) {
        const v: i64 = order_1[_idx(order_1.len, n -% i_2 -% 1)];
        if (!(visited[_idx(visited.len, v)])) {
            const comp_1: []i64 = find_component(reversed, v, visited);
            components = blk9: { var _tmp_9 = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp_9.appendSlice(@as([]const []i64, components)) catch |err| handleError(err); _tmp_9.append(comp_1) catch |err| handleError(err); break :blk9 (_tmp_9.toOwnedSlice() catch |err| handleError(err)); };
        }
        i_2 = i_2 +% 1;
    }
    return components;
}

fn mochi_main() void {
    const test_graph_1: [][]i64 = @constCast(([5][]i64{@constCast(([2]i64{2, 3})[0..2]), @constCast(([1]i64{0})[0..1]), @constCast(([1]i64{1})[0..1]), @constCast(([1]i64{4})[0..1]), @constCast((&[_]i64{})[0..0])})[0..5]);
    const test_graph_2: [][]i64 = @constCast(([6][]i64{@constCast(([3]i64{1, 2, 3})[0..3]), @constCast(([1]i64{2})[0..1]), @constCast(([1]i64{0})[0..1]), @constCast(([1]i64{4})[0..1]), @constCast(([1]i64{5})[0..1]), @constCast(([1]i64{3})[0..1])})[0..6]);
    std.debug.print("{s}\n", .{_str(strongly_connected_components(test_graph_1))});
    std.debug.print("{s}\n", .{_str(strongly_connected_components(test_graph_2))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
