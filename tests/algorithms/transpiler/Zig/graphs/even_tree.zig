// Generated by Mochi Zig transpiler on 2025-08-14 22:13 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

var tree_var_1: std.AutoHashMap(i64,[]i64) = std.AutoHashMap(i64,[]i64).init(std.heap.page_allocator);

fn dfs(start: i64, visited_param: *std.AutoHashMap(i64,bool)) []i64 {
    var size: i64 = 1;
    size = size;
    var cuts: i64 = 0;
    cuts = cuts;
    visited_param.put(start, true) catch unreachable;
    for (tree_var_1.get(start).?) |__it0| {
        const v = __it0;
        if (!(visited_param.contains(v))) {
            const res: []i64 = dfs(v, visited_param);
            size = size + res[_idx(res.len, 0)];
            cuts = cuts + res[_idx(res.len, 1)];
        }
    }
    if (@mod(size, 2) == 0) {
        cuts = cuts + 1;
    }
    return blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.append(size) catch |err| handleError(err); _tmp.append(cuts) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
}

fn even_tree() i64 {
    const visited: std.AutoHashMap(i64,bool) = std.AutoHashMap(i64, bool).init(std.heap.page_allocator);
    const res: []i64 = dfs(1, @constCast(&visited));
    return res[_idx(res.len, 1)] - 1;
}

fn mochi_main() void {
    const edges: [][]i64 = @constCast(([9][]i64{@constCast(([2]i64{2, 1})[0..2]), @constCast(([2]i64{3, 1})[0..2]), @constCast(([2]i64{4, 3})[0..2]), @constCast(([2]i64{5, 2})[0..2]), @constCast(([2]i64{6, 1})[0..2]), @constCast(([2]i64{7, 2})[0..2]), @constCast(([2]i64{8, 6})[0..2]), @constCast(([2]i64{9, 8})[0..2]), @constCast(([2]i64{10, 8})[0..2])})[0..9]);
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(edges.len))) {
        const u: i64 = edges[_idx(edges.len, i)][_idx(edges[_idx(edges.len, i)].len, 0)];
        const v: i64 = edges[_idx(edges.len, i)][_idx(edges[_idx(edges.len, i)].len, 1)];
        if (!(tree_var_1.contains(u))) {
            tree_var_1.put(u, @constCast((&[_]i64{}))[0..]) catch unreachable;
        }
        if (!(tree_var_1.contains(v))) {
            tree_var_1.put(v, @constCast((&[_]i64{}))[0..]) catch unreachable;
        }
        tree_var_1.put(u, blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, tree_var_1.get(u).?)) catch |err| handleError(err); _tmp.append(v) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); }) catch unreachable;
        tree_var_1.put(v, blk: { var _tmp_1 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_1.appendSlice(@as([]const i64, tree_var_1.get(v).?)) catch |err| handleError(err); _tmp_1.append(u) catch |err| handleError(err); break :blk (_tmp_1.toOwnedSlice() catch |err| handleError(err)); }) catch unreachable;
        i = i + 1;
    }
    std.debug.print("{s}\n", .{_str(even_tree())});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        tree_var_1 = std.AutoHashMap(i64, []i64).init(std.heap.page_allocator);
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .pointer => |p| {
        if (p.size == .slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .@"struct" => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.heap.pageSize()));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
