// Generated by Mochi Zig transpiler on 2025-08-14 22:13 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const Edge = struct {
    node: i64,
    weight: i64,
};

var graph_var: [][]Edge = &[_][]Edge{};
var dist_var: []i64 = &[_]i64{};

fn make_int_list(n: i64, value: i64) []i64 {
    var lst: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    lst = lst;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        lst = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, lst)) catch |err| handleError(err); _tmp.append(value) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    return lst;
}

fn make_bool_list(n: i64) []bool {
    var lst: []bool = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
    lst = lst;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        lst = blk: { var _tmp_1 = std.ArrayList(bool).init(std.heap.page_allocator); _tmp_1.appendSlice(@as([]const bool, lst)) catch |err| handleError(err); _tmp_1.append(false) catch |err| handleError(err); break :blk (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    return lst;
}

fn dijkstra(graph_param: [][]Edge, src: i64) []i64 {
    const n: i64 = @as(i64, @intCast(graph_param.len));
    var dist_var_1: []i64 = make_int_list(n, 1000000000);
    dist_var_1 = dist_var_1;
    var visited: []bool = make_bool_list(n);
    visited = visited;
    dist_var_1[_idx(dist_var_1.len, src)] = 0;
    var count: i64 = 0;
    count = count;
    while (count < n) {
        var u: i64 = 0 - 1;
        u = u;
        var min_dist: i64 = 1000000000;
        min_dist = min_dist;
        var i: i64 = 0;
        i = i;
        while (i < n) {
            if (!(visited[_idx(visited.len, i)]) and dist_var_1[_idx(dist_var_1.len, i)] < min_dist) {
                min_dist = dist_var_1[_idx(dist_var_1.len, i)];
                u = i;
            }
            i = i + 1;
        }
        if (u < 0) {
            break;
        }
        visited[_idx(visited.len, u)] = true;
        var j: i64 = 0;
        j = j;
        while (j < @as(i64, @intCast(graph_param[_idx(graph_param.len, u)].len))) {
            const e: Edge = graph_param[_idx(graph_param.len, u)][_idx(graph_param[_idx(graph_param.len, u)].len, j)];
            const v: i64 = e.node;
            const w: i64 = e.weight;
            if (!(visited[_idx(visited.len, v)])) {
                const new_dist: i64 = dist_var_1[_idx(dist_var_1.len, u)] + w;
                if (new_dist < dist_var_1[_idx(dist_var_1.len, v)]) {
                    dist_var_1[_idx(dist_var_1.len, v)] = new_dist;
                }
            }
            j = j + 1;
        }
        count = count + 1;
    }
    return dist_var_1;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        graph_var = std.heap.page_allocator.dupe([]Edge, blk0: { var _tmp_2 = std.ArrayList([]Edge).init(std.heap.page_allocator); _tmp_2.append(@constCast(blk1: { var _tmp_3 = std.ArrayList(Edge).init(std.heap.page_allocator); _tmp_3.append(.{ .node = 1, .weight = 10 }) catch unreachable; _tmp_3.append(.{ .node = 3, .weight = 5 }) catch unreachable; break :blk1 (_tmp_3.toOwnedSlice() catch unreachable); })) catch unreachable; _tmp_2.append(@constCast(blk2: { var _tmp_4 = std.ArrayList(Edge).init(std.heap.page_allocator); _tmp_4.append(.{ .node = 2, .weight = 1 }) catch unreachable; _tmp_4.append(.{ .node = 3, .weight = 2 }) catch unreachable; break :blk2 (_tmp_4.toOwnedSlice() catch unreachable); })) catch unreachable; _tmp_2.append(@constCast(blk3: { var _tmp_5 = std.ArrayList(Edge).init(std.heap.page_allocator); _tmp_5.append(.{ .node = 4, .weight = 4 }) catch unreachable; break :blk3 (_tmp_5.toOwnedSlice() catch unreachable); })) catch unreachable; _tmp_2.append(@constCast(blk4: { var _tmp_6 = std.ArrayList(Edge).init(std.heap.page_allocator); _tmp_6.append(.{ .node = 1, .weight = 3 }) catch unreachable; _tmp_6.append(.{ .node = 2, .weight = 9 }) catch unreachable; _tmp_6.append(.{ .node = 4, .weight = 2 }) catch unreachable; break :blk4 (_tmp_6.toOwnedSlice() catch unreachable); })) catch unreachable; _tmp_2.append(@constCast(blk5: { var _tmp_7 = std.ArrayList(Edge).init(std.heap.page_allocator); _tmp_7.append(.{ .node = 0, .weight = 7 }) catch unreachable; _tmp_7.append(.{ .node = 2, .weight = 6 }) catch unreachable; break :blk5 (_tmp_7.toOwnedSlice() catch unreachable); })) catch unreachable; break :blk0 (_tmp_2.toOwnedSlice() catch unreachable); }) catch unreachable;
        dist_var = dijkstra(graph_var, 0);
        std.debug.print("{s}\n", .{_str(dist_var[_idx(dist_var.len, 0)])});
        std.debug.print("{s}\n", .{_str(dist_var[_idx(dist_var.len, 1)])});
        std.debug.print("{s}\n", .{_str(dist_var[_idx(dist_var.len, 2)])});
        std.debug.print("{s}\n", .{_str(dist_var[_idx(dist_var.len, 3)])});
        std.debug.print("{s}\n", .{_str(dist_var[_idx(dist_var.len, 4)])});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .pointer => |p| {
        if (p.size == .slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .@"struct" => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.heap.pageSize()));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
