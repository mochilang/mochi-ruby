// Generated by Mochi Zig transpiler on 2025-08-15 10:23 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn depth_first_search(u: i64, visited_param: []bool, graph: [][]i64, stack_param: []i64) []i64 {
    var visited_var: []bool = visited_param;
    visited_var = visited_var;
    var stack_var: []i64 = stack_param;
    stack_var = stack_var;
    visited_var[_idx(visited_var.len, u)] = true;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(graph[_idx(graph.len, u)].len))) {
        const v: i64 = graph[_idx(graph.len, u)][_idx(graph[_idx(graph.len, u)].len, i)];
        if (!(visited_var[_idx(visited_var.len, v)])) {
            stack_var = depth_first_search(v, visited_var, graph, stack_var);
        }
        i = i + 1;
    }
    stack_var = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, stack_var)) catch |err| handleError(err); _tmp.append(u) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    return stack_var;
}

fn topological_sort(graph_1: [][]i64) []i64 {
    var visited: []bool = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
    visited = visited;
    var i_1: i64 = 0;
    i_1 = i_1;
    while (i_1 < @as(i64, @intCast(graph_1.len))) {
        visited = blk: { var _tmp_1 = std.ArrayList(bool).init(std.heap.page_allocator); _tmp_1.appendSlice(@as([]const bool, visited)) catch |err| handleError(err); _tmp_1.append(false) catch |err| handleError(err); break :blk (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
        i_1 = i_1 + 1;
    }
    var stack: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    stack = stack;
    i_1 = 0;
    while (i_1 < @as(i64, @intCast(graph_1.len))) {
        if (!(visited[_idx(visited.len, i_1)])) {
            stack = depth_first_search(i_1, visited, graph_1, stack);
        }
        i_1 = i_1 + 1;
    }
    return stack;
}

fn print_stack(stack_1: []i64, clothes: *const std.AutoHashMap(i64,[]const u8)) void {
    var order: i64 = 1;
    order = order;
    var s: []i64 = blk: { const tmp = std.heap.page_allocator.alloc(i64, stack_1.len) catch unreachable; @memcpy(tmp, stack_1); break :blk tmp; };
    s = s;
    while (@as(i64, @intCast(s.len)) > 0) {
        const idx: i64 = s[_idx(s.len, @as(i64, @intCast(s.len)) - 1)];
        s = s[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(s.len)) - 1)), @as(usize, @intCast(s.len)))];
        std.debug.print("{s}\n", .{_concat_string(_concat_string(_str(order), " "), clothes.get(idx).?)});
        order = order + 1;
    }
}

fn format_list(xs: []i64) []const u8 {
    var res: []const u8 = "[";
    res = res;
    var i_2: i64 = 0;
    i_2 = i_2;
    while (i_2 < @as(i64, @intCast(xs.len))) {
        res = _concat_string(res, _str(xs[_idx(xs.len, i_2)]));
        if (i_2 < @as(i64, @intCast(xs.len)) - 1) {
            res = _concat_string(res, ", ");
        }
        i_2 = i_2 + 1;
    }
    res = _concat_string(res, "]");
    return res;
}

fn mochi_main() void {
    const clothes_1: std.AutoHashMap(i64,[]const u8) = blk0: { var m = std.AutoHashMap(i64, []const u8).init(std.heap.page_allocator); m.put(0, "underwear") catch unreachable; m.put(1, "pants") catch unreachable; m.put(2, "belt") catch unreachable; m.put(3, "suit") catch unreachable; m.put(4, "shoe") catch unreachable; m.put(5, "socks") catch unreachable; m.put(6, "shirt") catch unreachable; m.put(7, "tie") catch unreachable; m.put(8, "watch") catch unreachable; break :blk0 m; };
    const graph_2: [][]i64 = @constCast(([9][]i64{@constCast(([2]i64{1, 4})[0..2]), @constCast(([2]i64{2, 4})[0..2]), @constCast(([1]i64{3})[0..1]), @constCast((&[_]i64{})[0..0]), @constCast((&[_]i64{})[0..0]), @constCast(([1]i64{4})[0..1]), @constCast(([2]i64{2, 7})[0..2]), @constCast(([1]i64{3})[0..1]), @constCast((&[_]i64{})[0..0])})[0..9]);
    const stack_2: []i64 = topological_sort(graph_2);
    std.debug.print("{s}\n", .{format_list(stack_2)});
    print_stack(stack_2, @constCast(&clothes_1));
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .pointer => |p| {
        if (p.size == .slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .@"struct" => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.heap.pageSize()));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
