// Generated by Mochi Zig transpiler on 2025-08-16 13:51 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn tarjan(g: [][]i64) [][]i64 {
    const n: i64 = @as(i64, @intCast(g.len));
    var stack: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    stack = stack;
    var on_stack: []bool = std.heap.page_allocator.alloc(bool, 0) catch unreachable;
    on_stack = on_stack;
    var index_of: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    index_of = index_of;
    var lowlink_of: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    lowlink_of = lowlink_of;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        on_stack = blk0: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, on_stack)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        index_of = blk1: { var _tmp_1 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_1.appendSlice(@as([]const i64, index_of)) catch |err| handleError(err); _tmp_1.append(-1) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
        lowlink_of = blk2: { var _tmp_2 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_2.appendSlice(@as([]const i64, lowlink_of)) catch |err| handleError(err); _tmp_2.append(-1) catch |err| handleError(err); break :blk2 (_tmp_2.toOwnedSlice() catch |err| handleError(err)); };
        i = i +% 1;
    }
    var components: [][]i64 = std.heap.page_allocator.alloc([]i64, 0) catch unreachable;
    components = components;
    var v_1: i64 = 0;
    v_1 = v_1;
    while (v_1 < n) {
        if (index_of[_idx(index_of.len, v_1)] == -1) {
            _ = strong_connect(stack, on_stack, index_of, lowlink_of, components, g, v_1, 0);
        }
        v_1 = v_1 +% 1;
    }
    return components;
}

fn create_graph(n_1: i64, edges: [][]i64) [][]i64 {
    var g_1: [][]i64 = std.heap.page_allocator.alloc([]i64, 0) catch unreachable;
    g_1 = g_1;
    var i_1: i64 = 0;
    i_1 = i_1;
    while (i_1 < n_1) {
        g_1 = blk3: { var _tmp_3 = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp_3.appendSlice(@as([]const []i64, g_1)) catch |err| handleError(err); _tmp_3.append(@constCast((&[_]i64{})[0..0])) catch |err| handleError(err); break :blk3 (_tmp_3.toOwnedSlice() catch |err| handleError(err)); };
        i_1 = i_1 +% 1;
    }
    for (edges) |__it0| {
        const e_1 = __it0;
        const u: i64 = e_1[_idx(e_1.len, 0)];
        const v_2: i64 = e_1[_idx(e_1.len, 1)];
        g_1[_idx(g_1.len, u)] = blk4: { var _tmp_4 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_4.appendSlice(@as([]const i64, g_1[_idx(g_1.len, u)])) catch |err| handleError(err); _tmp_4.append(v_2) catch |err| handleError(err); break :blk4 (_tmp_4.toOwnedSlice() catch |err| handleError(err)); };
    }
    return g_1;
}

fn mochi_main() void {
    const n_vertices: i64 = 7;
    const source: []i64 = @constCast(([9]i64{0, 0, 1, 2, 3, 3, 4, 4, 6})[0..9]);
    const target: []i64 = @constCast(([9]i64{1, 3, 2, 0, 1, 4, 5, 6, 5})[0..9]);
    var edges_1: [][]i64 = std.heap.page_allocator.alloc([]i64, 0) catch unreachable;
    edges_1 = edges_1;
    var i_2: i64 = 0;
    i_2 = i_2;
    while (i_2 < @as(i64, @intCast(source.len))) {
        edges_1 = blk5: { var _tmp_5 = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp_5.appendSlice(@as([]const []i64, edges_1)) catch |err| handleError(err); _tmp_5.append(@constCast(blk6: { var _tmp_6 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_6.append(source[_idx(source.len, i_2)]) catch unreachable; _tmp_6.append(target[_idx(target.len, i_2)]) catch unreachable; break :blk6 (_tmp_6.toOwnedSlice() catch unreachable); })) catch |err| handleError(err); break :blk5 (_tmp_5.toOwnedSlice() catch |err| handleError(err)); };
        i_2 = i_2 +% 1;
    }
    const g_2: [][]i64 = create_graph(n_vertices, edges_1);
    std.debug.print("{s}\n", .{_str(tarjan(g_2))});
}

fn strong_connect(stack: []i64, on_stack: []bool, index_of: []i64, lowlink_of: []i64, components: [][]i64, g: [][]i64, v: i64, index: i64) i64 {
    index_of[_idx(index_of.len, v)] = index;
    lowlink_of[_idx(lowlink_of.len, v)] = index;
    var current_index: i64 = index +% 1;
    current_index = current_index;
    stack = blk7: { var _tmp_7 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_7.appendSlice(@as([]const i64, stack)) catch |err| handleError(err); _tmp_7.append(v) catch |err| handleError(err); break :blk7 (_tmp_7.toOwnedSlice() catch |err| handleError(err)); };
    on_stack[_idx(on_stack.len, v)] = true;
    for (g[_idx(g.len, v)]) |__it1| {
        const w_2 = __it1;
        if (index_of[_idx(index_of.len, w_2)] == -1) {
            current_index = strong_connect(w_2, current_index);
            if (lowlink_of[_idx(lowlink_of.len, w_2)] < lowlink_of[_idx(lowlink_of.len, v)]) {
                lowlink_of[_idx(lowlink_of.len, v)] = lowlink_of[_idx(lowlink_of.len, w_2)];
            }
        } else {
            if (on_stack[_idx(on_stack.len, w_2)]) {
                if (lowlink_of[_idx(lowlink_of.len, w_2)] < lowlink_of[_idx(lowlink_of.len, v)]) {
                    lowlink_of[_idx(lowlink_of.len, v)] = lowlink_of[_idx(lowlink_of.len, w_2)];
                }
            }
        }
    }
    if (lowlink_of[_idx(lowlink_of.len, v)] == index_of[_idx(index_of.len, v)]) {
        var component: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
        component = component;
        var w_1: i64 = stack[_idx(stack.len, @as(i64, @intCast(stack.len)) -% 1)];
        w_1 = w_1;
        stack = stack[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(stack.len)) -% 1)), @as(usize, @intCast(stack.len)))];
        on_stack[_idx(on_stack.len, w_1)] = false;
        component = blk8: { var _tmp_8 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_8.appendSlice(@as([]const i64, component)) catch |err| handleError(err); _tmp_8.append(w_1) catch |err| handleError(err); break :blk8 (_tmp_8.toOwnedSlice() catch |err| handleError(err)); };
        while (w_1 != v) {
            w_1 = stack[_idx(stack.len, @as(i64, @intCast(stack.len)) -% 1)];
            stack = stack[@as(usize, @intCast(0))..@min(@as(usize, @intCast(@as(i64, @intCast(stack.len)) -% 1)), @as(usize, @intCast(stack.len)))];
            on_stack[_idx(on_stack.len, w_1)] = false;
            component = blk9: { var _tmp_9 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_9.appendSlice(@as([]const i64, component)) catch |err| handleError(err); _tmp_9.append(w_1) catch |err| handleError(err); break :blk9 (_tmp_9.toOwnedSlice() catch |err| handleError(err)); };
        }
        components = blk10: { var _tmp_10 = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp_10.appendSlice(@as([]const []i64, components)) catch |err| handleError(err); _tmp_10.append(component) catch |err| handleError(err); break :blk10 (_tmp_10.toOwnedSlice() catch |err| handleError(err)); };
    }
    return current_index;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
