// Generated by Mochi Zig transpiler on 2025-08-14 16:41 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const FuzzySet = struct {
    name: []const u8,
    left_boundary: f64,
    peak: f64,
    right_boundary: f64,
};

var sheru_var: FuzzySet = undefined;
var siya_var: FuzzySet = undefined;
var sheru_comp_var: FuzzySet = undefined;
var inter_var: FuzzySet = undefined;
var uni_var: FuzzySet = undefined;

fn stringify(fs: FuzzySet) []const u8 {
    return _concat_string(_concat_string(_concat_string(_concat_string(_concat_string(_concat_string(_concat_string(fs.name, ": ["), _str(fs.left_boundary)), ", "), _str(fs.peak)), ", "), _str(fs.right_boundary)), "]");
}

fn max2(a: f64, b: f64) f64 {
    if (a > b) {
        return a;
    }
    return b;
}

fn min2(a: f64, b: f64) f64 {
    if (a < b) {
        return a;
    }
    return b;
}

fn complement(fs: FuzzySet) FuzzySet {
    return .{ .name = _concat_string("¬", fs.name), .left_boundary = 1.0 - fs.right_boundary, .peak = 1.0 - fs.left_boundary, .right_boundary = 1.0 - fs.peak };
}

fn intersection(a: FuzzySet, b: FuzzySet) FuzzySet {
    return .{ .name = _concat_string(_concat_string(a.name, " ∩ "), b.name), .left_boundary = max2(a.left_boundary, b.left_boundary), .peak = min2(a.right_boundary, b.right_boundary), .right_boundary = (a.peak + b.peak) / 2.0 };
}

fn union_(a: FuzzySet, b: FuzzySet) FuzzySet {
    return .{ .name = _concat_string(_concat_string(a.name, " U "), b.name), .left_boundary = min2(a.left_boundary, b.left_boundary), .peak = max2(a.right_boundary, b.right_boundary), .right_boundary = (a.peak + b.peak) / 2.0 };
}

fn membership(fs: FuzzySet, x: f64) f64 {
    if (x <= fs.left_boundary or x >= fs.right_boundary) {
        return 0.0;
    }
    if (fs.left_boundary < x and x <= fs.peak) {
        return (x - fs.left_boundary) / (fs.peak - fs.left_boundary);
    }
    if (fs.peak < x and x < fs.right_boundary) {
        return (fs.right_boundary - x) / (fs.right_boundary - fs.peak);
    }
    return 0.0;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        sheru_var = .{ .name = "Sheru", .left_boundary = 0.4, .peak = 1.0, .right_boundary = 0.6 };
        siya_var = .{ .name = "Siya", .left_boundary = 0.5, .peak = 1.0, .right_boundary = 0.7 };
        sheru_comp_var = complement(sheru_var);
        inter_var = intersection(siya_var, sheru_var);
        uni_var = union_(siya_var, sheru_var);
        std.debug.print("{s}\n", .{stringify(sheru_var)});
        std.debug.print("{s}\n", .{stringify(siya_var)});
        std.debug.print("{s}\n", .{stringify(sheru_comp_var)});
        std.debug.print("{s}\n", .{stringify(inter_var)});
        std.debug.print("{s}\n", .{_concat_string("Sheru membership 0.5: ", _str(membership(sheru_var, 0.5)))});
        std.debug.print("{s}\n", .{_concat_string("Sheru membership 0.6: ", _str(membership(sheru_var, 0.6)))});
        std.debug.print("{s}\n", .{stringify(uni_var)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
