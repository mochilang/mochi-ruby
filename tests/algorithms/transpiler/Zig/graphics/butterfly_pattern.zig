// Generated by Mochi Zig transpiler on 2025-08-14 16:41 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn repeat_char(ch: []const u8, count: i64) []const u8 {
    var result: []const u8 = "";
    result = result;
    var i: i64 = 0;
    i = i;
    while (i < count) {
        result = _concat_string(result, ch);
        i = i + 1;
    }
    return result;
}

fn butterfly_pattern(n: i64) []const u8 {
    var lines: [][]const u8 = std.heap.page_allocator.alloc([]const u8, 0) catch unreachable;
    lines = lines;
    var i: i64 = 1;
    i = i;
    while (i < n) {
        const left: []const u8 = repeat_char("*", i);
        const mid: []const u8 = repeat_char(" ", 2 * (n - i) - 1);
        const right: []const u8 = repeat_char("*", i);
        lines = blk: { var _tmp = std.ArrayList([]const u8).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []const u8, lines)) catch |err| handleError(err); _tmp.append(@constCast(_concat_string(_concat_string(left, mid), right))) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    lines = blk: { var _tmp = std.ArrayList([]const u8).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []const u8, lines)) catch |err| handleError(err); _tmp.append(@constCast(repeat_char("*", 2 * n - 1))) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    var j: i64 = n - 1;
    j = j;
    while (j > 0) {
        const left: []const u8 = repeat_char("*", j);
        const mid: []const u8 = repeat_char(" ", 2 * (n - j) - 1);
        const right: []const u8 = repeat_char("*", j);
        lines = blk: { var _tmp = std.ArrayList([]const u8).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []const u8, lines)) catch |err| handleError(err); _tmp.append(@constCast(_concat_string(_concat_string(left, mid), right))) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        j = j - 1;
    }
    var out: []const u8 = "";
    out = out;
    var k: i64 = 0;
    k = k;
    while (k < @as(i64, @intCast(lines.len))) {
        if (k > 0) {
            out = _concat_string(out, "\n");
        }
        out = _concat_string(out, lines[_idx(lines.len, k)]);
        k = k + 1;
    }
    return out;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        std.debug.print("{s}\n", .{butterfly_pattern(3)});
        std.debug.print("{s}\n", .{butterfly_pattern(5)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
