// Generated by Mochi Zig transpiler on 2025-08-14 16:41 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn expApprox(x: f64) f64 {
    if (x < 0.0) {
        return 1.0 / expApprox(@as(f64, @floatFromInt(0)) - x);
    }
    if (x > 1.0) {
        const half: f64 = expApprox(x / 2.0);
        return half * half;
    }
    var sum: f64 = 1.0;
    sum = sum;
    var term: f64 = 1.0;
    term = term;
    var n: i64 = 1;
    n = n;
    while (n < 20) {
        term = term * x / @as(f64, @floatFromInt(n));
        sum = sum + term;
        n = n + 1;
    }
    return sum;
}

fn floor(x: f64) f64 {
    var i: i64 = @as(i64, @intFromFloat(x));
    i = i;
    if (@as(f64, @floatFromInt(i)) > x) {
        i = i - 1;
    }
    return @as(f64, @floatFromInt(i));
}

fn pow10(n: i64) f64 {
    var result: f64 = 1.0;
    result = result;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        result = result * 10.0;
        i = i + 1;
    }
    return result;
}

fn round(x: f64, n: i64) f64 {
    const m: f64 = pow10(n);
    return std.math.floor(x * m + 0.5) / m;
}

fn charging_inductor(source_voltage: f64, resistance: f64, inductance: f64, time: f64) f64 {
    if (source_voltage <= 0.0) {
        @panic("Source voltage must be positive.");
    }
    if (resistance <= 0.0) {
        @panic("Resistance must be positive.");
    }
    if (inductance <= 0.0) {
        @panic("Inductance must be positive.");
    }
    const exponent: f64 = (@as(f64, @floatFromInt(0)) - time) * resistance / inductance;
    const current: f64 = source_voltage / resistance * (1.0 - expApprox(exponent));
    return round(current, 3);
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        std.debug.print("{d}\n", .{charging_inductor(5.8, 1.5, 2.3, 2.0)});
        std.debug.print("{d}\n", .{charging_inductor(8.0, 5.0, 3.0, 2.0)});
        std.debug.print("{d}\n", .{charging_inductor(8.0, 5.0 * pow10(2), 3.0, 2.0)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
