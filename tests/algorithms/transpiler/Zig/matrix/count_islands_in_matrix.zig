// Generated by Mochi Zig transpiler on 2025-08-12 13:41 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

var grid_var: [][]i64 = &[_][]i64{};

fn is_safe(grid_param: [][]i64, visited_param: [][]bool, row_param: i64, col_param: i64) bool {
    const grid_var_1: [][]i64 = grid_param;
    var visited_var: [][]bool = visited_param;
    visited_var = visited_var;
    var row_var: i64 = row_param;
    row_var = row_var;
    var col_var: i64 = col_param;
    col_var = col_var;
    const rows: i64 = @as(i64, @intCast(grid_var_1.len));
    const cols: i64 = @as(i64, @intCast(grid_var_1[_idx(grid_var_1.len, 0)].len));
    const visited_cell: bool = visited_var[_idx(visited_var.len, row_var)][_idx(visited_var[_idx(visited_var.len, row_var)].len, col_var)];
    const within_bounds: bool = row_var >= 0 and row_var < rows and col_var >= 0 and col_var < cols;
    const not_visited: bool = visited_cell == false;
    return within_bounds and not_visited and grid_var_1[_idx(grid_var_1.len, row_var)][_idx(grid_var_1[_idx(grid_var_1.len, row_var)].len, col_var)] == 1;
}

fn dfs(grid_param_1: [][]i64, visited_param_1: [][]bool, row_param_1: i64, col_param_1: i64) void {
    const grid_var_2: [][]i64 = grid_param_1;
    var visited_var: [][]bool = visited_param_1;
    visited_var = visited_var;
    var row_var: i64 = row_param_1;
    row_var = row_var;
    var col_var: i64 = col_param_1;
    col_var = col_var;
    const row_nbr: []i64 = @constCast(&([8]i64{0 - 1, 0 - 1, 0 - 1, 0, 0, 1, 1, 1}))[0..];
    const col_nbr: []i64 = @constCast(&([8]i64{0 - 1, 0, 1, 0 - 1, 1, 0 - 1, 0, 1}))[0..];
    visited_var[_idx(visited_var.len, row_var)][_idx(visited_var[_idx(visited_var.len, row_var)].len, col_var)] = true;
    var k: i64 = 0;
    k = k;
    while (k < 8) {
        const new_row: i64 = row_var + row_nbr[_idx(row_nbr.len, k)];
        const new_col: i64 = col_var + col_nbr[_idx(col_nbr.len, k)];
        if (is_safe(grid_var_2, visited_var, new_row, new_col)) {
            dfs(grid_var_2, visited_var, new_row, new_col);
        }
        k = k + 1;
    }
}

fn count_islands(grid_param_2: [][]i64) i64 {
    const grid_var_3: [][]i64 = grid_param_2;
    const rows: i64 = @as(i64, @intCast(grid_var_3.len));
    const cols: i64 = @as(i64, @intCast(grid_var_3[_idx(grid_var_3.len, 0)].len));
    var visited: [][]bool = @constCast(&[_][]bool{})[0..];
    visited = visited;
    var i: i64 = 0;
    i = i;
    while (i < rows) {
        var row_list: []bool = @constCast(&[_]bool{})[0..];
        row_list = row_list;
        var j: i64 = 0;
        j = j;
        while (j < cols) {
            row_list = blk: { var _tmp = std.ArrayList(bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const bool, row_list)) catch |err| handleError(err); _tmp.append(false) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
            j = j + 1;
        }
        visited = blk: { var _tmp = std.ArrayList([]bool).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []bool, visited)) catch |err| handleError(err); _tmp.append(row_list) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    var count: i64 = 0;
    count = count;
    i = 0;
    while (i < rows) {
        var j: i64 = 0;
        j = j;
        while (j < cols) {
            if (!(visited[_idx(visited.len, i)][_idx(visited[_idx(visited.len, i)].len, j)]) and grid_var_3[_idx(grid_var_3.len, i)][_idx(grid_var_3[_idx(grid_var_3.len, i)].len, j)] == 1) {
                dfs(grid_var_3, visited, i, j);
                count = count + 1;
            }
            j = j + 1;
        }
        i = i + 1;
    }
    return count;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        grid_var = std.heap.page_allocator.dupe([]i64, (&([5][]i64{@constCast(&([5]i64{1, 1, 0, 0, 0}))[0..], @constCast(&([5]i64{0, 1, 0, 0, 1}))[0..], @constCast(&([5]i64{1, 0, 0, 1, 1}))[0..], @constCast(&([5]i64{0, 0, 0, 0, 0}))[0..], @constCast(&([5]i64{1, 0, 1, 0, 1}))[0..]}))[0..]) catch unreachable;
        std.debug.print("{s}\n", .{_str(count_islands(grid_var))});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .pointer => |p| {
        if (p.size == .slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .@"struct" => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.heap.pageSize()));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
