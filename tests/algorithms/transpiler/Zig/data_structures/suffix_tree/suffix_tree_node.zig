// Generated by Mochi Zig transpiler on 2025-08-24 22:16 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const SuffixTreeNode = struct {
    children: std.StringHashMap(i64),
    is_end_of_string: bool,
    start: i64,
    end: i64,
    suffix_link: i64,
};

var root_var: SuffixTreeNode = undefined;
var leaf_var: SuffixTreeNode = undefined;
var nodes_var_1: []SuffixTreeNode = &[_]SuffixTreeNode{};
var root_check_var_1: SuffixTreeNode = undefined;
var leaf_check_var_1: SuffixTreeNode = undefined;

fn new_suffix_tree_node(children: *const std.StringHashMap(i64), is_end_of_string: bool, start: i64, end: i64, suffix_link: i64) SuffixTreeNode {
    return .{ .children = (children).*, .is_end_of_string = is_end_of_string, .start = start, .end = end, .suffix_link = suffix_link };
}

fn empty_suffix_tree_node() SuffixTreeNode {
    return new_suffix_tree_node(&std.StringHashMap(i64).init(std.heap.page_allocator), false, -1, -1, -1);
}

fn has_key(m: *const std.StringHashMap(i64), k: []const u8) bool {
    var __mapit1 = m.keyIterator();
    while (__mapit1.next()) |__it0| {
        const key = __it0.*;
        if (std.mem.eql(u8, key, k)) {
            return true;
        }
    }
    return false;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        root_var = new_suffix_tree_node(&blk0: { var m_1 = std.StringHashMap(i64).init(std.heap.page_allocator); m_1.put("a", 1) catch unreachable; break :blk0 m_1; }, false, -1, -1, -1);
        leaf_var = new_suffix_tree_node(&std.StringHashMap(i64).init(std.heap.page_allocator), true, 0, 2, 0);
        nodes_var_1 = std.heap.page_allocator.dupe(SuffixTreeNode, blk1: { var _tmp = std.ArrayList(SuffixTreeNode).init(std.heap.page_allocator); _tmp.append(root_var) catch unreachable; _tmp.append(leaf_var) catch unreachable; break :blk1 (_tmp.toOwnedSlice() catch unreachable); }) catch unreachable;
        root_check_var_1 = nodes_var_1[_idx(nodes_var_1.len, 0)];
        leaf_check_var_1 = nodes_var_1[_idx(nodes_var_1.len, 1)];
        std.debug.print("{s}\n", .{_str(has_key(&root_check_var_1.children, "a"))});
        std.debug.print("{s}\n", .{_str(leaf_check_var_1.is_end_of_string)});
        std.debug.print("{s}\n", .{_str(leaf_check_var_1.start)});
        std.debug.print("{s}\n", .{_str(leaf_check_var_1.end)});
        std.debug.print("{s}\n", .{_str(leaf_check_var_1.suffix_link)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
