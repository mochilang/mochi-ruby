// Generated by Mochi Zig transpiler on 2025-08-07 08:09 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

var arr1_var: []i64 = &[_]i64{};
var arr2_var: []i64 = &[_]i64{};

fn partition(arr: []i64, low: i64, high: i64) i64 {
    const pivot: i64 = arr[@as(usize, @intCast(high))];
    var i: i64 = low - 1;
    var j: i64 = low;
    while (j < high) {
        if (arr[@as(usize, @intCast(j))] >= pivot) {
            i = i + 1;
            const tmp: i64 = arr[@as(usize, @intCast(i))];
            arr[@as(usize, @intCast(i))] = arr[@as(usize, @intCast(j))];
            arr[@as(usize, @intCast(j))] = tmp;
        }
        j = j + 1;
    }
    const k: i64 = i + 1;
    const tmp: i64 = arr[@as(usize, @intCast(k))];
    arr[@as(usize, @intCast(k))] = arr[@as(usize, @intCast(high))];
    arr[@as(usize, @intCast(high))] = tmp;
    return k;
}

fn kth_largest_element(arr: []i64, position: i64) i64 {
    if (@as(i64, @intCast(arr.len)) == 0) {
        return 0 - 1;
    }
    if (position < 1 or position > @as(i64, @intCast(arr.len))) {
        return 0 - 1;
    }
    var low: i64 = 0;
    var high: i64 = @as(i64, @intCast(arr.len)) - 1;
    while (low <= high) {
        if (low > @as(i64, @intCast(arr.len)) - 1 or high < 0) {
            return 0 - 1;
        }
        const pivot_index: i64 = partition(arr, low, high);
        if (pivot_index == position - 1) {
            return arr[@as(usize, @intCast(pivot_index))];
        } else {
            if (pivot_index > position - 1) {
                high = pivot_index - 1;
            } else {
                low = pivot_index + 1;
            }
        }
    }
    return 0 - 1;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        arr1_var = blk0: { var _tmp0 = std.heap.page_allocator.alloc(i64, 11) catch unreachable; _tmp0[0] = 3; _tmp0[1] = 1; _tmp0[2] = 4; _tmp0[3] = 1; _tmp0[4] = 5; _tmp0[5] = 9; _tmp0[6] = 2; _tmp0[7] = 6; _tmp0[8] = 5; _tmp0[9] = 3; _tmp0[10] = 5; break :blk0 _tmp0; };
        arr2_var = blk1: { var _tmp1 = std.heap.page_allocator.alloc(i64, 11) catch unreachable; _tmp1[0] = 2; _tmp1[1] = 5; _tmp1[2] = 6; _tmp1[3] = 1; _tmp1[4] = 9; _tmp1[5] = 3; _tmp1[6] = 8; _tmp1[7] = 4; _tmp1[8] = 7; _tmp1[9] = 3; _tmp1[10] = 5; break :blk1 _tmp1; };
        std.debug.print("{s}\n", .{_str(kth_largest_element(arr1_var, 3))});
        std.debug.print("\n", .{});
        std.debug.print("{s}\n", .{_str(kth_largest_element(arr2_var, 1))});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    if (info == .Pointer and info.Pointer.size == .Slice) {
        var out = std.ArrayList(u8).init(std.heap.page_allocator);
        defer out.deinit();
        out.append('[') catch unreachable;
        for (v, 0..) |e, i| {
            if (i != 0) { out.append(' ') catch unreachable; }
            const s = _str(e);
            out.appendSlice(s) catch unreachable;
        }
        out.append(']') catch unreachable;
        return out.toOwnedSlice() catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
