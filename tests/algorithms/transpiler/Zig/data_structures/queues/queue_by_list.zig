// Generated by Mochi Zig transpiler on 2025-08-24 23:17 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const Queue = struct {
    entries: []i64,
};

const GetResult = struct {
    queue: Queue,
    value: i64,
};

var q_var_1: Queue = undefined;
var res_var: GetResult = undefined;
var front_var: i64 = 0;

fn new_queue(items: []i64) Queue {
    return .{ .entries = items };
}

fn len_queue(q_param: *const Queue) i64 {
    return @as(i64, @intCast(q_param.entries.len));
}

fn str_queue(q_param_1: *const Queue) []const u8 {
    var s: []const u8 = "Queue((";
    s = s;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(q_param_1.entries.len))) {
        s = _concat_string(s, _str(q_param_1.entries[_idx(q_param_1.entries.len, i)]));
        if (i < @as(i64, @intCast(q_param_1.entries.len)) -% 1) {
            s = _concat_string(s, ", ");
        }
        i = i +% 1;
    }
    s = _concat_string(s, "))");
    return s;
}

fn put(q_param_2: *const Queue, item: i64) Queue {
    var e: []i64 = q_param_2.entries;
    e = e;
    e = blk0: { var _tmp = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, e)) catch |err| handleError(err); _tmp.append(item) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
    return .{ .entries = e };
}

fn get(q_param_3: *const Queue) GetResult {
    if (@as(i64, @intCast(q_param_3.entries.len)) == 0) {
        @panic("Queue is empty");
    }
    const value: i64 = q_param_3.entries[_idx(q_param_3.entries.len, 0)];
    var new_entries: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    new_entries = new_entries;
    var i_1: i64 = 1;
    i_1 = i_1;
    while (i_1 < @as(i64, @intCast(q_param_3.entries.len))) {
        new_entries = blk1: { var _tmp_1 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_1.appendSlice(@as([]const i64, new_entries)) catch |err| handleError(err); _tmp_1.append(q_param_3.entries[_idx(q_param_3.entries.len, i_1)]) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
        i_1 = i_1 +% 1;
    }
    return .{ .queue = .{ .entries = new_entries }, .value = value };
}

fn rotate(q_param_4: *const Queue, rotation: i64) Queue {
    var e_1: []i64 = q_param_4.entries;
    e_1 = e_1;
    var r: i64 = 0;
    r = r;
    while (r < rotation) {
        if (@as(i64, @intCast(e_1.len)) > 0) {
            const first: i64 = e_1[_idx(e_1.len, 0)];
            var rest: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
            rest = rest;
            var i_2: i64 = 1;
            i_2 = i_2;
            while (i_2 < @as(i64, @intCast(e_1.len))) {
                rest = blk2: { var _tmp_2 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_2.appendSlice(@as([]const i64, rest)) catch |err| handleError(err); _tmp_2.append(e_1[_idx(e_1.len, i_2)]) catch |err| handleError(err); break :blk2 (_tmp_2.toOwnedSlice() catch |err| handleError(err)); };
                i_2 = i_2 +% 1;
            }
            rest = blk3: { var _tmp_3 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_3.appendSlice(@as([]const i64, rest)) catch |err| handleError(err); _tmp_3.append(first) catch |err| handleError(err); break :blk3 (_tmp_3.toOwnedSlice() catch |err| handleError(err)); };
            e_1 = rest;
        }
        r = r +% 1;
    }
    return .{ .entries = e_1 };
}

fn get_front(q_param_5: *const Queue) i64 {
    return q_param_5.entries[_idx(q_param_5.entries.len, 0)];
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        q_var_1 = new_queue(@constCast((&[_]i64{})[0..0]));
        q_var_1 = put(&q_var_1, 10);
        q_var_1 = put(&q_var_1, 20);
        q_var_1 = put(&q_var_1, 30);
        q_var_1 = put(&q_var_1, 40);
        res_var = get(&q_var_1);
        q_var_1 = res_var.queue;
        q_var_1 = rotate(&q_var_1, 2);
        front_var = get_front(&q_var_1);
        std.debug.print("{s}\n", .{_str(len_queue(&q_var_1))});
        std.debug.print("{s}\n", .{str_queue(&q_var_1)});
        std.debug.print("{s}\n", .{_str(res_var.value)});
        std.debug.print("{s}\n", .{str_queue(&q_var_1)});
        std.debug.print("{s}\n", .{str_queue(&q_var_1)});
        std.debug.print("{s}\n", .{_str(front_var)});
        std.debug.print("{s}\n", .{str_queue(&q_var_1)});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
