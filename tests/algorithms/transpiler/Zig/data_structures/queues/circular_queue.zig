// Generated by Mochi Zig transpiler on 2025-08-24 23:17 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const CircularQueue = struct {
    data: []i64,
    front: i64,
    rear: i64,
    size: i64,
    capacity: i64,
};

const DequeueResult = struct {
    queue: CircularQueue,
    value: i64,
};

fn create_queue(capacity: i64) CircularQueue {
    var arr: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    arr = arr;
    var i: i64 = 0;
    i = i;
    while (i < capacity) {
        arr = blk0: { var _tmp = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, arr)) catch |err| handleError(err); _tmp.append(0) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i +% 1;
    }
    return .{ .data = arr, .front = 0, .rear = 0, .size = 0, .capacity = capacity };
}

fn length(q: *const CircularQueue) i64 {
    return q.size;
}

fn is_empty(q_1: *const CircularQueue) bool {
    return q_1.size == 0;
}

fn front(q_2: *const CircularQueue) i64 {
    if (is_empty(q_2)) {
        return 0;
    }
    return q_2.data[_idx(q_2.data.len, q_2.front)];
}

fn enqueue(q_param: *CircularQueue, value: i64) CircularQueue {
    if (q_param.size >= q_param.capacity) {
        @panic("QUEUE IS FULL");
    }
    var arr_1: []i64 = q_param.data;
    arr_1 = arr_1;
    arr_1[_idx(arr_1.len, q_param.rear)] = value;
    q_param.data = arr_1;
    q_param.rear = @mod(q_param.rear +% 1, q_param.capacity);
    q_param.size = q_param.size +% 1;
    return q_param.*;
}

fn dequeue(q_param_1: *CircularQueue) DequeueResult {
    if (q_param_1.size == 0) {
        @panic("UNDERFLOW");
    }
    const value_1: i64 = q_param_1.data[_idx(q_param_1.data.len, q_param_1.front)];
    var arr2: []i64 = q_param_1.data;
    arr2 = arr2;
    arr2[_idx(arr2.len, q_param_1.front)] = 0;
    q_param_1.data = arr2;
    q_param_1.front = @mod(q_param_1.front +% 1, q_param_1.capacity);
    q_param_1.size = q_param_1.size -% 1;
    return .{ .queue = (q_param_1).*, .value = value_1 };
}

fn mochi_main() void {
    var q_3: CircularQueue = create_queue(5);
    q_3 = q_3;
    std.debug.print("{s}\n", .{_str(is_empty(&q_3))});
    q_3 = enqueue(&q_3, 10);
    std.debug.print("{s}\n", .{_str(is_empty(&q_3))});
    q_3 = enqueue(&q_3, 20);
    q_3 = enqueue(&q_3, 30);
    std.debug.print("{s}\n", .{_str(front(&q_3))});
    const r: DequeueResult = dequeue(&q_3);
    q_3 = r.queue;
    std.debug.print("{s}\n", .{_str(r.value)});
    std.debug.print("{s}\n", .{_str(front(&q_3))});
    std.debug.print("{s}\n", .{_str(length(&q_3))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
