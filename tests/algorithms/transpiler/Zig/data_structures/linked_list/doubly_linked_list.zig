// Generated by Mochi Zig transpiler on 2025-08-24 23:17 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const DoublyLinkedList = struct {
    data: []i64,
};

const DeleteResult = struct {
    list: DoublyLinkedList,
    value: i64,
};

fn empty_list() DoublyLinkedList {
    return .{ .data = (&[_]i64{})[0..0] };
}

fn length(list: *const DoublyLinkedList) i64 {
    return @as(i64, @intCast(list.data.len));
}

fn is_empty(list_1: *const DoublyLinkedList) bool {
    return @as(i64, @intCast(list_1.data.len)) == 0;
}

fn to_string(list_2: *const DoublyLinkedList) []const u8 {
    if (@as(i64, @intCast(list_2.data.len)) == 0) {
        return "";
    }
    var s: []const u8 = _str(list_2.data[_idx(list_2.data.len, 0)]);
    s = s;
    var i: i64 = 1;
    i = i;
    while (i < @as(i64, @intCast(list_2.data.len))) {
        s = _concat_string(_concat_string(s, "->"), _str(list_2.data[_idx(list_2.data.len, i)]));
        i = i +% 1;
    }
    return s;
}

fn insert_nth(list_3: *const DoublyLinkedList, index: i64, value: i64) DoublyLinkedList {
    if (index < 0 or index > @as(i64, @intCast(list_3.data.len))) {
        @panic("index out of range");
    }
    var res: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    res = res;
    var i_1: i64 = 0;
    i_1 = i_1;
    while (i_1 < index) {
        res = blk0: { var _tmp = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, res)) catch |err| handleError(err); _tmp.append(list_3.data[_idx(list_3.data.len, i_1)]) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i_1 = i_1 +% 1;
    }
    res = blk1: { var _tmp_1 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_1.appendSlice(@as([]const i64, res)) catch |err| handleError(err); _tmp_1.append(value) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
    while (i_1 < @as(i64, @intCast(list_3.data.len))) {
        res = blk2: { var _tmp_2 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_2.appendSlice(@as([]const i64, res)) catch |err| handleError(err); _tmp_2.append(list_3.data[_idx(list_3.data.len, i_1)]) catch |err| handleError(err); break :blk2 (_tmp_2.toOwnedSlice() catch |err| handleError(err)); };
        i_1 = i_1 +% 1;
    }
    return .{ .data = res };
}

fn insert_head(list_4: *const DoublyLinkedList, value_1: i64) DoublyLinkedList {
    return insert_nth(list_4, 0, value_1);
}

fn insert_tail(list_5: *const DoublyLinkedList, value_2: i64) DoublyLinkedList {
    return insert_nth(list_5, @as(i64, @intCast(list_5.data.len)), value_2);
}

fn delete_nth(list_6: *const DoublyLinkedList, index_1: i64) DeleteResult {
    if (index_1 < 0 or index_1 >= @as(i64, @intCast(list_6.data.len))) {
        @panic("index out of range");
    }
    var res_1: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    res_1 = res_1;
    var i_2: i64 = 0;
    i_2 = i_2;
    var removed: i64 = 0;
    removed = removed;
    while (i_2 < @as(i64, @intCast(list_6.data.len))) {
        if (i_2 == index_1) {
            removed = list_6.data[_idx(list_6.data.len, i_2)];
        } else {
            res_1 = blk3: { var _tmp_3 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_3.appendSlice(@as([]const i64, res_1)) catch |err| handleError(err); _tmp_3.append(list_6.data[_idx(list_6.data.len, i_2)]) catch |err| handleError(err); break :blk3 (_tmp_3.toOwnedSlice() catch |err| handleError(err)); };
        }
        i_2 = i_2 +% 1;
    }
    return .{ .list = .{ .data = res_1 }, .value = removed };
}

fn delete_head(list_7: *const DoublyLinkedList) DeleteResult {
    return delete_nth(list_7, 0);
}

fn delete_tail(list_8: *const DoublyLinkedList) DeleteResult {
    return delete_nth(list_8, @as(i64, @intCast(list_8.data.len)) -% 1);
}

fn delete_value(list_9: *const DoublyLinkedList, value_3: i64) DeleteResult {
    var idx: i64 = 0;
    idx = idx;
    var found: bool = false;
    found = found;
    while (idx < @as(i64, @intCast(list_9.data.len))) {
        if (list_9.data[_idx(list_9.data.len, idx)] == value_3) {
            found = true;
            break;
        }
        idx = idx +% 1;
    }
    if (!(found)) {
        @panic("value not found");
    }
    return delete_nth(list_9, idx);
}

fn mochi_main() void {
    var dll: DoublyLinkedList = empty_list();
    dll = dll;
    dll = insert_tail(&dll, 1);
    dll = insert_tail(&dll, 2);
    dll = insert_tail(&dll, 3);
    std.debug.print("{s}\n", .{to_string(&dll)});
    dll = insert_head(&dll, 0);
    std.debug.print("{s}\n", .{to_string(&dll)});
    dll = insert_nth(&dll, 2, 9);
    std.debug.print("{s}\n", .{to_string(&dll)});
    var res_2: DeleteResult = delete_nth(&dll, 2);
    res_2 = res_2;
    dll = res_2.list;
    std.debug.print("{s}\n", .{_str(res_2.value)});
    std.debug.print("{s}\n", .{to_string(&dll)});
    res_2 = delete_tail(&dll);
    dll = res_2.list;
    std.debug.print("{s}\n", .{_str(res_2.value)});
    std.debug.print("{s}\n", .{to_string(&dll)});
    res_2 = delete_value(&dll, 1);
    dll = res_2.list;
    std.debug.print("{s}\n", .{_str(res_2.value)});
    std.debug.print("{s}\n", .{to_string(&dll)});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
