// Generated by Mochi Zig transpiler on 2025-08-24 23:17 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const SortedLinkedList = struct {
    values: []i64,
};

fn sort_list(nums: []i64) []i64 {
    var arr: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    arr = arr;
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(nums.len))) {
        arr = blk0: { var _tmp = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp.appendSlice(@as([]const i64, arr)) catch |err| handleError(err); _tmp.append(nums[_idx(nums.len, i)]) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i +% 1;
    }
    var j: i64 = 0;
    j = j;
    while (j < @as(i64, @intCast(arr.len))) {
        var k: i64 = j +% 1;
        k = k;
        while (k < @as(i64, @intCast(arr.len))) {
            if (arr[_idx(arr.len, k)] < arr[_idx(arr.len, j)]) {
                const tmp: i64 = arr[_idx(arr.len, j)];
                arr[_idx(arr.len, j)] = arr[_idx(arr.len, k)];
                arr[_idx(arr.len, k)] = tmp;
            }
            k = k +% 1;
        }
        j = j +% 1;
    }
    return arr;
}

fn make_sorted_linked_list(ints: []i64) SortedLinkedList {
    return .{ .values = sort_list(ints) };
}

fn len_sll(sll: *const SortedLinkedList) i64 {
    return @as(i64, @intCast(sll.values.len));
}

fn str_sll(sll_1: *const SortedLinkedList) []const u8 {
    var res: []const u8 = "";
    res = res;
    var i_1: i64 = 0;
    i_1 = i_1;
    while (i_1 < @as(i64, @intCast(sll_1.values.len))) {
        res = _concat_string(res, _str(sll_1.values[_idx(sll_1.values.len, i_1)]));
        if (i_1 +% 1 < @as(i64, @intCast(sll_1.values.len))) {
            res = _concat_string(res, " -> ");
        }
        i_1 = i_1 +% 1;
    }
    return res;
}

fn merge_lists(a: *const SortedLinkedList, b: *const SortedLinkedList) SortedLinkedList {
    var combined: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    combined = combined;
    var i_2: i64 = 0;
    i_2 = i_2;
    while (i_2 < @as(i64, @intCast(a.values.len))) {
        combined = blk1: { var _tmp_1 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_1.appendSlice(@as([]const i64, combined)) catch |err| handleError(err); _tmp_1.append(a.values[_idx(a.values.len, i_2)]) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
        i_2 = i_2 +% 1;
    }
    i_2 = 0;
    while (i_2 < @as(i64, @intCast(b.values.len))) {
        combined = blk2: { var _tmp_2 = std.ArrayList(i64).initCapacity(std.heap.page_allocator, 0) catch |err| handleError(err); _tmp_2.appendSlice(@as([]const i64, combined)) catch |err| handleError(err); _tmp_2.append(b.values[_idx(b.values.len, i_2)]) catch |err| handleError(err); break :blk2 (_tmp_2.toOwnedSlice() catch |err| handleError(err)); };
        i_2 = i_2 +% 1;
    }
    return make_sorted_linked_list(combined);
}

fn mochi_main() void {
    const test_data_odd: []i64 = blk3: { var _tmp_3 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_3.append(3) catch unreachable; _tmp_3.append(9) catch unreachable; _tmp_3.append(0 -% 11) catch unreachable; _tmp_3.append(0) catch unreachable; _tmp_3.append(7) catch unreachable; _tmp_3.append(5) catch unreachable; _tmp_3.append(1) catch unreachable; _tmp_3.append(0 -% 1) catch unreachable; break :blk3 (_tmp_3.toOwnedSlice() catch unreachable); };
    const test_data_even: []i64 = blk4: { var _tmp_4 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_4.append(4) catch unreachable; _tmp_4.append(6) catch unreachable; _tmp_4.append(2) catch unreachable; _tmp_4.append(0) catch unreachable; _tmp_4.append(8) catch unreachable; _tmp_4.append(10) catch unreachable; _tmp_4.append(3) catch unreachable; _tmp_4.append(0 -% 2) catch unreachable; break :blk4 (_tmp_4.toOwnedSlice() catch unreachable); };
    const sll_one: SortedLinkedList = make_sorted_linked_list(test_data_odd);
    const sll_two: SortedLinkedList = make_sorted_linked_list(test_data_even);
    const merged: SortedLinkedList = merge_lists(@constCast(&sll_one), @constCast(&sll_two));
    std.debug.print("{s}\n", .{_str(len_sll(@constCast(&merged)))});
    std.debug.print("{s}\n", .{str_sll(@constCast(&merged))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
