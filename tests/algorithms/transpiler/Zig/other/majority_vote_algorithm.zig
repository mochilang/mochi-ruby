// Generated by Mochi Zig transpiler on 2025-08-19 09:22 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn index_of(xs: []i64, x: i64) i64 {
    var i: i64 = 0;
    i = i;
    while (i < @as(i64, @intCast(xs.len))) {
        if (xs[_idx(xs.len, i)] == x) {
            return i;
        }
        i = i +% 1;
    }
    return -1;
}

fn majority_vote(votes: []i64, votes_needed_to_win: i64) []i64 {
    if (votes_needed_to_win < 2) {
        return (&[_]i64{})[0..0];
    }
    var candidates: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    candidates = candidates;
    var counts: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    counts = counts;
    var i_1: i64 = 0;
    i_1 = i_1;
    while (i_1 < @as(i64, @intCast(votes.len))) {
        const v: i64 = votes[_idx(votes.len, i_1)];
        const idx: i64 = index_of(candidates, v);
        if (idx != -1) {
            counts[_idx(counts.len, idx)] = counts[_idx(counts.len, idx)] +% 1;
        } else {
            if (@as(i64, @intCast(candidates.len)) < votes_needed_to_win -% 1) {
                candidates = blk0: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, candidates)) catch |err| handleError(err); _tmp.append(v) catch |err| handleError(err); break :blk0 (_tmp.toOwnedSlice() catch |err| handleError(err)); };
                counts = blk1: { var _tmp_1 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_1.appendSlice(@as([]const i64, counts)) catch |err| handleError(err); _tmp_1.append(1) catch |err| handleError(err); break :blk1 (_tmp_1.toOwnedSlice() catch |err| handleError(err)); };
            } else {
                var j: i64 = 0;
                j = j;
                while (j < @as(i64, @intCast(counts.len))) {
                    counts[_idx(counts.len, j)] = counts[_idx(counts.len, j)] -% 1;
                    j = j +% 1;
                }
                var new_candidates: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
                new_candidates = new_candidates;
                var new_counts: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
                new_counts = new_counts;
                j = 0;
                while (j < @as(i64, @intCast(candidates.len))) {
                    if (counts[_idx(counts.len, j)] > 0) {
                        new_candidates = blk2: { var _tmp_2 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_2.appendSlice(@as([]const i64, new_candidates)) catch |err| handleError(err); _tmp_2.append(candidates[_idx(candidates.len, j)]) catch |err| handleError(err); break :blk2 (_tmp_2.toOwnedSlice() catch |err| handleError(err)); };
                        new_counts = blk3: { var _tmp_3 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_3.appendSlice(@as([]const i64, new_counts)) catch |err| handleError(err); _tmp_3.append(counts[_idx(counts.len, j)]) catch |err| handleError(err); break :blk3 (_tmp_3.toOwnedSlice() catch |err| handleError(err)); };
                    }
                    j = j +% 1;
                }
                candidates = new_candidates;
                counts = new_counts;
            }
        }
        i_1 = i_1 +% 1;
    }
    var final_counts: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    final_counts = final_counts;
    var j_1: i64 = 0;
    j_1 = j_1;
    while (j_1 < @as(i64, @intCast(candidates.len))) {
        final_counts = blk4: { var _tmp_4 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_4.appendSlice(@as([]const i64, final_counts)) catch |err| handleError(err); _tmp_4.append(0) catch |err| handleError(err); break :blk4 (_tmp_4.toOwnedSlice() catch |err| handleError(err)); };
        j_1 = j_1 +% 1;
    }
    i_1 = 0;
    while (i_1 < @as(i64, @intCast(votes.len))) {
        const v_1: i64 = votes[_idx(votes.len, i_1)];
        const idx_1: i64 = index_of(candidates, v_1);
        if (idx_1 != -1) {
            final_counts[_idx(final_counts.len, idx_1)] = final_counts[_idx(final_counts.len, idx_1)] +% 1;
        }
        i_1 = i_1 +% 1;
    }
    var result: []i64 = std.heap.page_allocator.alloc(i64, 0) catch unreachable;
    result = result;
    j_1 = 0;
    while (j_1 < @as(i64, @intCast(candidates.len))) {
        if (final_counts[_idx(final_counts.len, j_1)] *% votes_needed_to_win > @as(i64, @intCast(votes.len))) {
            result = blk5: { var _tmp_5 = std.ArrayList(i64).init(std.heap.page_allocator); _tmp_5.appendSlice(@as([]const i64, result)) catch |err| handleError(err); _tmp_5.append(candidates[_idx(candidates.len, j_1)]) catch |err| handleError(err); break :blk5 (_tmp_5.toOwnedSlice() catch |err| handleError(err)); };
        }
        j_1 = j_1 +% 1;
    }
    return result;
}

fn mochi_main() void {
    const votes_1: []i64 = @constCast(([7]i64{1, 2, 2, 3, 1, 3, 2})[0..7]);
    std.debug.print("{s}\n", .{_str(majority_vote(votes_1, 3))});
    std.debug.print("{s}\n", .{_str(majority_vote(votes_1, 2))});
    std.debug.print("{s}\n", .{_str(majority_vote(votes_1, 4))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
