// Generated by Mochi Zig transpiler on 2025-08-06 18:11 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn valid_connection(graph: [][]i64, next_ver: i64, curr_ind: i64, path: []i64) bool {
    if (graph[@intCast(path[@intCast(curr_ind - 1)])][@intCast(next_ver)] == 0) {
        return false;
    }
    for (path) |__it0| {
        const v = __it0;
        if (v == next_ver) {
            return false;
        }
    }
    return true;
}

fn util_hamilton_cycle(graph_1: [][]i64, path_1: []i64, curr_ind_1: i64) bool {
    if (curr_ind_1 == @as(i64, @intCast(graph_1.len))) {
        return graph_1[@intCast(path_1[@intCast(curr_ind_1 - 1)])][@intCast(path_1[@intCast(0)])] == 1;
    }
    var next_ver_1: i64 = 0;
    while (next_ver_1 < @as(i64, @intCast(graph_1.len))) {
        if (valid_connection(graph_1, next_ver_1, curr_ind_1, path_1)) {
            path_1[@intCast(curr_ind_1)] = next_ver_1;
            if (util_hamilton_cycle(graph_1, path_1, curr_ind_1 + 1)) {
                return true;
            }
            path_1[@intCast(curr_ind_1)] = 0 - 1;
        }
        next_ver_1 = next_ver_1 + 1;
    }
    return false;
}

fn hamilton_cycle(graph_2: [][]i64, start_index: i64) []i64 {
    var path_2: []i64 = 0;
    var i: i64 = 0;
    while (i < @as(i64, @intCast(graph_2.len)) + 1) {
        path_2[@intCast(i)] = 0 - 1;
        i = i + 1;
    }
    path_2[@intCast(0)] = start_index;
    const last: i64 = @as(i64, @intCast(path_2.len)) - 1;
    path_2[@intCast(last)] = start_index;
    if (util_hamilton_cycle(graph_2, path_2, 1)) {
        return path_2;
    }
    return &[_]i64{};
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}
