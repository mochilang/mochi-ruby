// Generated by Mochi Zig transpiler on 2025-08-06 20:30 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn create_board(n: i64) [][]i64 {
    var board: [][]i64 = &[_][]i64{};
    var i: i64 = 0;
    while (i < n) {
        var row: []i64 = &[_]i64{};
        var j: i64 = 0;
        while (j < n) {
            row = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const i64, row)) catch |err| handleError(err); _tmp.append(0) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
            j = j + 1;
        }
        board = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, board)) catch |err| handleError(err); _tmp.append(row) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        i = i + 1;
    }
    return board;
}

fn is_safe(board: [][]i64, row: i64, column: i64) bool {
    const n_1: i64 = @as(i64, @intCast(board.len));
    var i: i64 = 0;
    while (i < row) {
        if (board[@as(usize, @intCast(i))][@as(usize, @intCast(column))] == 1) {
            return false;
        }
        i = i + 1;
    }
    i = row - 1;
    var j: i64 = column - 1;
    while (i >= 0 and j >= 0) {
        if (board[@as(usize, @intCast(i))][@as(usize, @intCast(j))] == 1) {
            return false;
        }
        i = i - 1;
        j = j - 1;
    }
    i = row - 1;
    j = column + 1;
    while (i >= 0 and j < n_1) {
        if (board[@as(usize, @intCast(i))][@as(usize, @intCast(j))] == 1) {
            return false;
        }
        i = i - 1;
        j = j + 1;
    }
    return true;
}

fn row_string(row_1: []i64) []const u8 {
    var s: []const u8 = "";
    var j: i64 = 0;
    while (j < @as(i64, @intCast(row_1.len))) {
        if (row_1[@as(usize, @intCast(j))] == 1) {
            s = _concat_string(s, "Q ");
        } else {
            s = _concat_string(s, ". ");
        }
        j = j + 1;
    }
    return s;
}

fn printboard(board_1: [][]i64) void {
    var i: i64 = 0;
    while (i < @as(i64, @intCast(board_1.len))) {
        std.debug.print("{s}\n", .{row_string(board_1[@as(usize, @intCast(i))])});
        i = i + 1;
    }
}

fn solve(board_2: [][]i64, row_2: i64) i64 {
    if (row_2 >= @as(i64, @intCast(board_2.len))) {
        printboard(board_2);
        std.debug.print("{s}\n", .{""});
        return 1;
    }
    var count: i64 = 0;
    var i: i64 = 0;
    while (i < @as(i64, @intCast(board_2.len))) {
        if (is_safe(board_2, row_2, i)) {
            board_2[@as(usize, @intCast(row_2))][@as(usize, @intCast(i))] = 1;
            count = count + solve(board_2, row_2 + 1);
            board_2[@as(usize, @intCast(row_2))][@as(usize, @intCast(i))] = 0;
        }
        i = i + 1;
    }
    return count;
}

fn n_queens(n_1: i64) i64 {
    const board_3: [][]i64 = create_board(n_1);
    const total: i64 = solve(board_3, 0);
    std.debug.print("{s}\n", .{_concat_string("The total number of solutions are: ", _str(total))});
    return total;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        _ = n_queens(4);
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    if (info == .Pointer and info.Pointer.size == .Slice) {
        var out = std.ArrayList(u8).init(std.heap.page_allocator);
        defer out.deinit();
        out.append('[') catch unreachable;
        for (v, 0..) |e, i| {
            if (i != 0) { out.append(' ') catch unreachable; }
            const s = _str(e);
            out.appendSlice(s) catch unreachable;
        }
        out.append(']') catch unreachable;
        return out.toOwnedSlice() catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
