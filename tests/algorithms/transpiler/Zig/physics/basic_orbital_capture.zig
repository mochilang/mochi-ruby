// Generated by Mochi Zig transpiler on 2025-08-19 09:22 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const G_var: f64 = 6.67429999999999938e-11;
const C_var: f64 = 299792458.0;
const PI_var: f64 = 3.14159265358979312;

fn pow10(n: i64) f64 {
    var result: f64 = 1.0;
    result = result;
    var i: i64 = 0;
    i = i;
    while (i < n) {
        result = result * 10.0;
        i = i +% 1;
    }
    return result;
}

fn sqrt(x: f64) f64 {
    if (x <= 0.0) {
        return 0.0;
    }
    var guess: f64 = x;
    guess = guess;
    var i_1: i64 = 0;
    i_1 = i_1;
    while (i_1 < 20) {
        guess = (guess + x / guess) / 2.0;
        i_1 = i_1 +% 1;
    }
    return guess;
}

fn abs(x_1: f64) f64 {
    if (x_1 < 0.0) {
        return @as(f64, @floatFromInt(0)) - x_1;
    }
    return x_1;
}

fn capture_radii(target_body_radius: f64, target_body_mass: f64, projectile_velocity: f64) f64 {
    if (target_body_mass < 0.0) {
        @panic("Mass cannot be less than 0");
    }
    if (target_body_radius < 0.0) {
        @panic("Radius cannot be less than 0");
    }
    if (projectile_velocity > C_var) {
        @panic("Cannot go beyond speed of light");
    }
    const escape_velocity_squared: f64 = 2.0 * G_var * target_body_mass / target_body_radius;
    const denom: f64 = projectile_velocity * projectile_velocity;
    const capture_radius: f64 = target_body_radius * sqrt(1.0 + escape_velocity_squared / denom);
    return capture_radius;
}

fn capture_area(capture_radius_1: f64) f64 {
    if (capture_radius_1 < 0.0) {
        @panic("Cannot have a capture radius less than 0");
    }
    const sigma: f64 = PI_var * capture_radius_1 * capture_radius_1;
    return sigma;
}

fn run_tests() void {
    const r: f64 = capture_radii(6.95699999999999985 * pow10(8), 1.98999999999999999 * pow10(30), 25000.0);
    if (abs(r - 1.72095906914371399 * pow10(10)) > 1.0) {
        @panic("capture_radii failed");
    }
    const a: f64 = capture_area(r);
    if (abs(a - 9.30445533180181172 * pow10(20)) > 1.0) {
        @panic("capture_area failed");
    }
}

fn mochi_main() void {
    run_tests();
    const r_1: f64 = capture_radii(6.95699999999999985 * pow10(8), 1.98999999999999999 * pow10(30), 25000.0);
    std.debug.print("{s}\n", .{_str(r_1)});
    std.debug.print("{s}\n", .{_str(capture_area(r_1))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _idx(len: usize, i: i64) usize {
    return if (i < 0 or i >= @as(i64, @intCast(len))) 0 else @as(usize, @intCast(i));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    .Struct => |_| {
        if (@hasDecl(@TypeOf(v), "iterator")) {
            const KVPair = struct{ ks: []const u8, vs: []const u8 };
            var pairs = std.ArrayList(KVPair).init(std.heap.page_allocator);
            defer pairs.deinit();
            var it = v.iterator();
            while (it.next()) |e| {
                const ks = _str(e.key_ptr.*);
                const vs = _str(e.value_ptr.*);
                pairs.append(.{ .ks = ks, .vs = vs }) catch unreachable;
            }
            std.sort.heap(KVPair, pairs.items, {}, struct {
                pub fn less(_: void, a: KVPair, b: KVPair) bool {
                    return std.mem.lessThan(u8, a.ks, b.ks);
                }
            }.less);
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.appendSlice("map[") catch unreachable;
            for (pairs.items, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                out.appendSlice(e.ks) catch unreachable;
                out.append(':') catch unreachable;
                out.appendSlice(e.vs) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * @as(i64, @intCast(std.mem.page_size));
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
