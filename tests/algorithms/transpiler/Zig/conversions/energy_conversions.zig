// Generated by Mochi Zig transpiler on 2025-08-07 14:57 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

var ENERGY_CONVERSION_var: std.StringHashMap(f64) = std.StringHashMap(f64).init(std.heap.page_allocator);

fn energy_conversion(from_type: []const u8, to_type: []const u8, value: f64) f64 {
    if (ENERGY_CONVERSION_var.contains(from_type) == false or ENERGY_CONVERSION_var.contains(to_type) == false) {
        @panic("Incorrect 'from_type' or 'to_type'");
    }
    return value * ENERGY_CONVERSION_var.get(from_type).? / ENERGY_CONVERSION_var.get(to_type).?;
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        ENERGY_CONVERSION_var = blk: { var m = std.StringHashMap(f64).init(std.heap.page_allocator); m.put("joule", 1.0) catch unreachable; m.put("kilojoule", 1000.0) catch unreachable; m.put("megajoule", 1000000.0) catch unreachable; m.put("gigajoule", 1000000000.0) catch unreachable; m.put("wattsecond", 1.0) catch unreachable; m.put("watthour", 3600.0) catch unreachable; m.put("kilowatthour", 3600000.0) catch unreachable; m.put("newtonmeter", 1.0) catch unreachable; m.put("calorie_nutr", 4186.8) catch unreachable; m.put("kilocalorie_nutr", 4186800.0) catch unreachable; m.put("electronvolt", 0.0000000000000000001602176634) catch unreachable; m.put("britishthermalunit_it", 1055.05585) catch unreachable; m.put("footpound", 1.355818) catch unreachable; break :blk m; };
        std.debug.print("{s}\n", .{_str(energy_conversion("joule", "kilojoule", 1.0))});
        std.debug.print("{s}\n", .{_str(energy_conversion("kilowatthour", "joule", 10.0))});
        std.debug.print("{s}\n", .{_str(energy_conversion("britishthermalunit_it", "footpound", 1.0))});
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    const info = @typeInfo(@TypeOf(v));
    switch (info) {
    .Pointer => |p| {
        if (p.size == .Slice) {
            var out = std.ArrayList(u8).init(std.heap.page_allocator);
            defer out.deinit();
            out.append('[') catch unreachable;
            for (v, 0..) |e, i| {
                if (i != 0) { out.append(' ') catch unreachable; }
                const s = _str(e);
                out.appendSlice(s) catch unreachable;
            }
            out.append(']') catch unreachable;
            return out.toOwnedSlice() catch unreachable;
        }
    },
    else => {},
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
