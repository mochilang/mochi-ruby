//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 15:01:49 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _floatEqual(a, b float64) bool {
	if a > b {
		return a-b < 1e-6
	}
	return b-a < 1e-6
}

var __name__ string = "__main__"

func sqrtApprox(x float64) float64 {
	if _floatEqual(x, 0.0) {
		return 0.0
	}
	var guess float64 = (float64(x) / float64(2.0))
	_ = guess
	var i int = 0
	_ = i
	for i < 20 {
		guess = (float64((guess + (float64(x) / float64(guess)))) / float64(2.0))
		i = (i + 1)
	}
	return guess
}

func speed_of_sound_in_a_fluid(density float64, bulk_modulus float64) float64 {
	if density <= 0.0 {
		panic("Impossible fluid density")
	}
	if bulk_modulus <= 0.0 {
		panic("Impossible bulk modulus")
	}
	return sqrtApprox((float64(bulk_modulus) / float64(density)))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(speed_of_sound_in_a_fluid(998.0, 2150000000.0)))
		fmt.Println(fmt.Sprint(speed_of_sound_in_a_fluid(13600.0, 28500000000.0)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
