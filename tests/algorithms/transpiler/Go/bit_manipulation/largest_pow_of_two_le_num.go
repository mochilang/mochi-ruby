//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:55:55 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func largest_pow_of_two_le_num(n int) int {
	if n <= 0 {
		return 0
	}
	var res int = 1
	_ = res
	for (res * 2) <= n {
		res = (res * 2)
	}
	return res
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(0)))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(1)))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num((0 - 1))))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(3)))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(15)))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(99)))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(178)))
		fmt.Println(fmt.Sprint(largest_pow_of_two_le_num(999999)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
