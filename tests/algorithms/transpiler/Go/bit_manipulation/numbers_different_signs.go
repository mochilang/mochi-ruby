//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:55:59 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func different_signs(num1 int, num2 int) bool {
	var sign1 bool = (num1 < 0)
	_ = sign1
	_ = sign1
	var sign2 bool = (num2 < 0)
	_ = sign2
	_ = sign2
	return (sign1 != sign2)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(different_signs(1, (0 - 1))))
		fmt.Println(fmt.Sprint(different_signs(1, 1)))
		fmt.Println(fmt.Sprint(different_signs(1000000000000000000, (0 - 1000000000000000000))))
		fmt.Println(fmt.Sprint(different_signs((0 - 1000000000000000000), 1000000000000000000)))
		fmt.Println(fmt.Sprint(different_signs(50, 278)))
		fmt.Println(fmt.Sprint(different_signs(0, 2)))
		fmt.Println(fmt.Sprint(different_signs(2, 0)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
