//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:54:22 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func int_to_binary(n int) string {
	if n == 0 {
		return "0"
	}
	var res string = ""
	_ = res
	var num int = n
	_ = num
	for num > 0 {
		res = (fmt.Sprint((num % 2)) + res)
		num = (num / 2)
	}
	return res
}

func pad_left(s string, width int) string {
	var res string = s
	_ = res
	for len(res) < width {
		res = ("0" + res)
	}
	return res
}

func binary_xor(a int, b int) string {
	if (a < 0) || (b < 0) {
		panic("the value of both inputs must be positive")
	}
	var a_bin string = int_to_binary(a)
	_ = a_bin
	var b_bin string = int_to_binary(b)
	_ = b_bin
	var max_len int = func() int {
		if len(a_bin) > len(b_bin) {
			return len(a_bin)
		} else {
			return len(b_bin)
		}
	}()
	_ = max_len
	var a_pad string = pad_left(a_bin, max_len)
	_ = a_pad
	_ = a_pad
	var b_pad string = pad_left(b_bin, max_len)
	_ = b_pad
	_ = b_pad
	var i int = 0
	_ = i
	var result string = ""
	_ = result
	for i < max_len {
		if string([]rune(a_pad)[i]) != string([]rune(b_pad)[i]) {
			result = (result + "1")
		} else {
			result = (result + "0")
		}
		i = (i + 1)
	}
	return ("0b" + result)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(binary_xor(25, 32))
		fmt.Println(binary_xor(37, 50))
		fmt.Println(binary_xor(21, 30))
		fmt.Println(binary_xor(58, 73))
		fmt.Println(binary_xor(0, 255))
		fmt.Println(binary_xor(256, 256))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
