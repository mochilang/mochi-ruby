//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:54:32 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func find_previous_power_of_two(number int) int {
	if number < 0 {
		panic("Input must be a non-negative integer")
	}
	if number == 0 {
		return 0
	}
	var power int = 1
	_ = power
	for power <= number {
		power = (power * 2)
	}
	if number > 1 {
		return (power / 2)
	} else {
		return 1
	}
}

func mochiMain() {
	var results []int = []int{}
	_ = results
	var i int = 0
	_ = i
	for i < 18 {
		results = append(results, find_previous_power_of_two(i))
		i = (i + 1)
	}
	fmt.Println(fmt.Sprint(results))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
