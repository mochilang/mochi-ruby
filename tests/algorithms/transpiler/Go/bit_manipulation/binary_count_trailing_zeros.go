//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:54:14 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func binary_count_trailing_zeros(a int) int {
	if a < 0 {
		panic("Input value must be a non-negative integer")
	}
	if a == 0 {
		return 0
	}
	var n int = a
	_ = n
	var count int = 0
	_ = count
	for (n % 2) == 0 {
		count = (count + 1)
		n = (n / 2)
	}
	return count
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(binary_count_trailing_zeros(25)))
		fmt.Println(fmt.Sprint(binary_count_trailing_zeros(36)))
		fmt.Println(fmt.Sprint(binary_count_trailing_zeros(16)))
		fmt.Println(fmt.Sprint(binary_count_trailing_zeros(58)))
		fmt.Println(fmt.Sprint(binary_count_trailing_zeros(4294967296)))
		fmt.Println(fmt.Sprint(binary_count_trailing_zeros(0)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
