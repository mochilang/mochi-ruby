//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:54:12 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func binary_count_setbits(n int) int {
	if n < 0 {
		panic("Input value must be a non-negative integer")
	}
	var count int = 0
	_ = count
	var value int = n
	_ = value
	for value > 0 {
		count = (count + (value % 2))
		value = (value / 2)
	}
	return count
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(binary_count_setbits(25)))
		fmt.Println(fmt.Sprint(binary_count_setbits(36)))
		fmt.Println(fmt.Sprint(binary_count_setbits(16)))
		fmt.Println(fmt.Sprint(binary_count_setbits(58)))
		fmt.Println(fmt.Sprint(binary_count_setbits(0)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
