//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 16:54:38 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func highest_set_bit_position(number int) int {
	if number < 0 {
		panic("number must be non-negative")
	}
	var position int = 0
	_ = position
	var n int = number
	_ = n
	for n > 0 {
		position = (position + 1)
		n = (n / 2)
	}
	return position
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(highest_set_bit_position(25)))
		fmt.Println(fmt.Sprint(highest_set_bit_position(37)))
		fmt.Println(fmt.Sprint(highest_set_bit_position(1)))
		fmt.Println(fmt.Sprint(highest_set_bit_position(4)))
		fmt.Println(fmt.Sprint(highest_set_bit_position(0)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
