//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-16 09:44:15 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

func bfs(graph [][]int, s int, t int, parent []int) bool {
	var visited []bool = []bool{}
	_ = visited
	var i int = 0
	_ = i
	for i < len(graph) {
		visited = append(visited, false)
		i = (i + 1)
	}
	var queue []int = []int{func(v any) int {
		if vv, ok := v.(int); ok {
			return vv
		}
		return 0
	}(s)}
	_ = queue
	var head int = 0
	_ = head
	_setIndex(visited, s, true)
	for head < len(queue) {
		var u int = _index(queue, head)
		_ = u
		head = (head + 1)
		var ind int = 0
		_ = ind
		for ind < len(_index(graph, u)) {
			if (_index(visited, ind) == false) && (_index(_index(graph, u), ind) > 0) {
				queue = append(queue, ind)
				_setIndex(visited, ind, true)
				_setIndex(parent, ind, u)
			}
			ind = (ind + 1)
		}
	}
	return _index(visited, t)
}

func mincut(graph [][]int, source int, sink int) [][]int {
	var g [][]int = graph
	_ = g
	var parent []int = []int{}
	_ = parent
	var i int = 0
	_ = i
	for i < len(g) {
		parent = append(parent, (0 - 1))
		i = (i + 1)
	}
	var temp [][]int = [][]int{}
	_ = temp
	i = 0
	for i < len(g) {
		var row []int = []int{}
		_ = row
		var j int = 0
		_ = j
		for j < len(_index(g, i)) {
			row = append(row, _index(_index(g, i), j))
			j = (j + 1)
		}
		temp = append(temp, row)
		i = (i + 1)
	}
	for bfs(func(v any) [][]int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([][]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return [][]int{}
			}
			out := make([][]int, len(arr))
			for i, x := range arr {
				out[i] = func(v any) []int {
					if v == nil {
						return nil
					}
					if vv, ok := v.([]int); ok {
						return vv
					}
					if arr, ok := v.([]any); ok {
						if len(arr) == 0 {
							return []int{}
						}
						out := make([]int, len(arr))
						for i, x := range arr {
							out[i] = x.(int)
						}
						return out
					}
					return v.([]int)
				}(x)
			}
			return out
		}
		return v.([][]int)
	}(g), source, sink, func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(parent)) {
		var path_flow int = 1000000000
		_ = path_flow
		var s int = sink
		_ = s
		for s != source {
			var p int = _index(parent, s)
			_ = p
			var cap int = _index(_index(g, p), s)
			_ = cap
			if cap < path_flow {
				path_flow = cap
			}
			s = p
		}
		var v int = sink
		_ = v
		for v != source {
			var u int = _index(parent, v)
			_ = u
			_setIndex(_index(g, u), v, (_index(_index(g, u), v) - path_flow))
			_setIndex(_index(g, v), u, (_index(_index(g, v), u) + path_flow))
			v = u
		}
	}
	var res [][]int = [][]int{}
	_ = res
	i = 0
	for i < len(g) {
		var j int = 0
		_ = j
		for j < len(_index(g, 0)) {
			if (_index(_index(g, i), j) == 0) && (_index(_index(temp, i), j) > 0) {
				res = append(res, []int{func(v any) int {
					if vv, ok := v.(int); ok {
						return vv
					}
					return 0
				}(i), func(v any) int {
					if vv, ok := v.(int); ok {
						return vv
					}
					return 0
				}(j)})
			}
			j = (j + 1)
		}
		i = (i + 1)
	}
	return res
}

var test_graph [][]int

var result [][]int

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		test_graph = [][]int{[]int{0, 16, 13, 0, 0, 0}, []int{0, 0, 10, 12, 0, 0}, []int{0, 4, 0, 0, 14, 0}, []int{0, 0, 9, 0, 0, 20}, []int{0, 0, 0, 7, 0, 4}, []int{0, 0, 0, 0, 0, 0}}
		result = mincut(func(v any) [][]int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([][]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return [][]int{}
				}
				out := make([][]int, len(arr))
				for i, x := range arr {
					out[i] = func(v any) []int {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]int); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []int{}
							}
							out := make([]int, len(arr))
							for i, x := range arr {
								out[i] = x.(int)
							}
							return out
						}
						return v.([]int)
					}(x)
				}
				return out
			}
			return v.([][]int)
		}(test_graph), 0, 5)
		fmt.Println(fmt.Sprint(result))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
