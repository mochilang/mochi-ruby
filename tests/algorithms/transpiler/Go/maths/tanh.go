//go:build ignore

// Generated by Mochi v0.10.67 on 2025-08-17 12:39:59 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func expApprox(x float64) float64 {
	var neg bool = false
	_ = neg
	var y float64 = x
	_ = y
	if x < 0.0 {
		neg = true
		y = (0 - x)
	}
	var term float64 = 1.0
	_ = term
	var sum float64 = 1.0
	_ = sum
	var n int = 1
	_ = n
	for n < 30 {
		term = (float64((term * y)) / float64(n))
		sum = (sum + term)
		n = (n + 1)
	}
	if neg {
		return (float64(1.0) / float64(sum))
	}
	return sum
}

func tangent_hyperbolic(vector []float64) []float64 {
	var result []float64 = []float64{}
	_ = result
	var i int = 0
	_ = i
	for i < len(vector) {
		var x float64 = _index(vector, i)
		_ = x
		var t float64 = ((float64(2.0) / float64((1.0 + expApprox(((0 - 2.0) * x))))) - 1.0)
		_ = t
		result = append(result, t)
		i = (i + 1)
	}
	return result
}

func mochiMain() {
	var v1 []float64 = []float64{1.0, 5.0, 6.0, (0 - 0.67)}
	_ = v1
	var v2 []float64 = []float64{8.0, 10.0, 2.0, (0 - 0.98), 13.0}
	_ = v2
	fmt.Println(fmt.Sprint(tangent_hyperbolic(func(v any) []float64 {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]float64); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []float64{}
			}
			out := make([]float64, len(arr))
			for i, x := range arr {
				out[i] = x.(float64)
			}
			return out
		}
		return v.([]float64)
	}(v1))))
	fmt.Println(fmt.Sprint(tangent_hyperbolic(func(v any) []float64 {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]float64); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []float64{}
			}
			out := make([]float64, len(arr))
			for i, x := range arr {
				out[i] = x.(float64)
			}
			return out
		}
		return v.([]float64)
	}(v2))))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
