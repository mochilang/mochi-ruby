//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-16 12:54:32 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func primeFactors(n int) []int {
	var i int = 2
	_ = i
	var factors []int = []int{}
	_ = factors
	for (i * i) <= n {
		if _modi(n, i) == 0 {
			factors = append(factors, i)
			n = (n / i)
		} else {
			i = (i + 1)
		}
	}
	if n > 1 {
		factors = append(factors, n)
	}
	return factors
}

func isSquareFree(factors []int) bool {
	var seen map[int]bool = map[int]bool{}
	_ = seen
	for _, f := range factors {
		_ = f
		if func() bool { _, ok := seen[f]; return ok }() {
			return false
		}
		seen[f] = true
	}
	return true
}

func mobius(n int) int {
	var factors []int = primeFactors(n)
	_ = factors
	if isSquareFree(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(factors)) {
		return func() int {
			if _modi(len(factors), 2) == 0 {
				return 1
			} else {
				return (0 - 1)
			}
		}()
	}
	return 0
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(mobius(24))
		fmt.Println(mobius((0 - 1)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
