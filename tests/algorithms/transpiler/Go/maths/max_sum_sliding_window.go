//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-16 12:53:16 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func max_sum_sliding_window(arr []int, k int) int {
	if (k < 0) || (len(arr) < k) {
		panic("Invalid Input")
	}
	var idx int = 0
	_ = idx
	var current_sum int = 0
	_ = current_sum
	for idx < k {
		current_sum = (current_sum + _index(arr, idx))
		idx = (idx + 1)
	}
	var max_sum int = current_sum
	_ = max_sum
	var i int = 0
	_ = i
	for i < (len(arr) - k) {
		current_sum = ((current_sum - _index(arr, i)) + _index(arr, (i+k)))
		if current_sum > max_sum {
			max_sum = current_sum
		}
		i = (i + 1)
	}
	return max_sum
}

func test_max_sum_sliding_window() {
	var arr1 []int = []int{1, 4, 2, 10, 2, 3, 1, 0, 20}
	_ = arr1
	if max_sum_sliding_window(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(arr1), 4) != 24 {
		panic("test1 failed")
	}
	var arr2 []int = []int{1, 4, 2, 10, 2, 13, 1, 0, 2}
	_ = arr2
	if max_sum_sliding_window(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(arr2), 4) != 27 {
		panic("test2 failed")
	}
}

func mochiMain() {
	test_max_sum_sliding_window()
	var sample []int = []int{1, 4, 2, 10, 2, 3, 1, 0, 20}
	_ = sample
	fmt.Println(fmt.Sprint(max_sum_sliding_window(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(sample), 4)))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
