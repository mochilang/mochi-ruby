//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-16 13:04:21 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

func isqrt(n int) int {
	var r int = 0
	_ = r
	for ((r + 1) * (r + 1)) <= n {
		r = (r + 1)
	}
	return r
}

func prime_sieve(num int) []int {
	if num <= 0 {
		panic("Invalid input, please enter a positive integer.")
	}
	var sieve []bool = []bool{}
	_ = sieve
	var i int = 0
	_ = i
	for i <= num {
		sieve = append(sieve, true)
		i = (i + 1)
	}
	var prime []int = []int{}
	_ = prime
	var start int = 2
	_ = start
	var end int = isqrt(num)
	_ = end
	for start <= end {
		if _index(sieve, start) {
			prime = append(prime, start)
			var j int = (start * start)
			_ = j
			for j <= num {
				if _index(sieve, j) {
					_setIndex(sieve, j, false)
				}
				j = (j + start)
			}
		}
		start = (start + 1)
	}
	var k int = (end + 1)
	_ = k
	for k <= num {
		if _index(sieve, k) {
			prime = append(prime, k)
		}
		k = (k + 1)
	}
	return prime
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(prime_sieve(50)))
		fmt.Println(fmt.Sprint(prime_sieve(25)))
		fmt.Println(fmt.Sprint(prime_sieve(10)))
		fmt.Println(fmt.Sprint(prime_sieve(9)))
		fmt.Println(fmt.Sprint(prime_sieve(2)))
		fmt.Println(fmt.Sprint(prime_sieve(1)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
