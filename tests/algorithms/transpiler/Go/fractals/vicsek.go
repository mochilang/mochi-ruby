//go:build ignore

// Generated by Mochi v0.10.65 on 2025-08-14 10:05:04 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	return s[idx]
}

func repeat_char(c string, count int) string {
	var s string = ""
	_ = s
	var i int = 0
	_ = i
	for i < count {
		s = (s + c)
		i = (i + 1)
	}
	return s
}

func vicsek(order int) []string {
	if order == 0 {
		return []string{"#"}
	}
	var prev []string = vicsek((order - 1))
	_ = prev
	var size int = len(prev)
	_ = size
	var blank string = repeat_char(" ", size)
	_ = blank
	var result []string = []string{}
	_ = result
	var i int = 0
	_ = i
	for i < size {
		result = append(result, ((blank + _index(prev, i)) + blank))
		i = (i + 1)
	}
	i = 0
	for i < size {
		result = append(result, ((_index(prev, i) + _index(prev, i)) + _index(prev, i)))
		i = (i + 1)
	}
	i = 0
	for i < size {
		result = append(result, ((blank + _index(prev, i)) + blank))
		i = (i + 1)
	}
	return result
}

func print_pattern(pattern []string) {
	var i int = 0
	_ = i
	for i < len(pattern) {
		fmt.Println(_index(pattern, i))
		i = (i + 1)
	}
}

func mochiMain() {
	var depth int = 3
	_ = depth
	var pattern []string = vicsek(depth)
	_ = pattern
	print_pattern(func(v any) []string {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]string); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []string{}
			}
			out := make([]string, len(arr))
			for i, x := range arr {
				out[i] = fmt.Sprint(x)
			}
			return out
		}
		return v.([]string)
	}(pattern))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
