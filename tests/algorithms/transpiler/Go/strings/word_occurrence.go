//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 14:34:44 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"sort"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

func word_occurrence(sentence string) map[string]int {
	var occurrence map[string]int = map[string]int{}
	_ = occurrence
	var word string = ""
	_ = word
	var i int = 0
	_ = i
	for i < len(sentence) {
		var ch string = _substr(sentence, i, (i + 1))
		_ = ch
		if ch == " " {
			if word != "" {
				if func() bool { _, ok := occurrence[word]; return ok }() {
					occurrence[word] = (occurrence[word] + 1)
				} else {
					occurrence[word] = 1
				}
				word = ""
			}
		} else {
			word = (word + ch)
		}
		i = (i + 1)
	}
	if word != "" {
		if func() bool { _, ok := occurrence[word]; return ok }() {
			occurrence[word] = (occurrence[word] + 1)
		} else {
			occurrence[word] = 1
		}
	}
	return occurrence
}

func mochiMain() {
	var result map[string]int = word_occurrence("INPUT STRING")
	_ = result
	for _, w := range func() []string {
		keys := make([]string, 0, len(result))
		for k := range result {
			keys = append(keys, k)
		}
		sort.Slice(keys, func(i, j int) bool { return fmt.Sprint(keys[i]) < fmt.Sprint(keys[j]) })
		return keys
	}() {
		_ = w
		fmt.Println(((w + ": ") + fmt.Sprint(result[w])))
	}
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
