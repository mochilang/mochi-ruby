//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 13:50:25 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"strings"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func find_index(xs []string, x string) int {
	var i int = 0
	_ = i
	for i < len(xs) {
		if _index(xs, i) == x {
			return i
		}
		i = (i + 1)
	}
	return (0 - 1)
}

func get_word_pattern(word string) string {
	var w string = strings.ToUpper(word)
	_ = w
	var letters []string = []string{}
	_ = letters
	var numbers []string = []string{}
	_ = numbers
	var next_num int = 0
	_ = next_num
	var res string = ""
	_ = res
	var i int = 0
	_ = i
	for i < len(w) {
		var ch string = string(_index([]rune(w), i))
		_ = ch
		var idx int = find_index(func(v any) []string {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]string); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []string{}
				}
				out := make([]string, len(arr))
				for i, x := range arr {
					out[i] = fmt.Sprint(x)
				}
				return out
			}
			return v.([]string)
		}(letters), ch)
		_ = idx
		var num_str string
		_ = num_str
		if idx >= 0 {
			num_str = _index(numbers, idx)
		} else {
			num_str = fmt.Sprint(next_num)
			letters = append(letters, ch)
			numbers = append(numbers, num_str)
			next_num = (next_num + 1)
		}
		if i > 0 {
			res = (res + ".")
		}
		res = (res + num_str)
		i = (i + 1)
	}
	return res
}

func mochiMain() {
	fmt.Println(get_word_pattern(""))
	fmt.Println(get_word_pattern(" "))
	fmt.Println(get_word_pattern("pattern"))
	fmt.Println(get_word_pattern("word pattern"))
	fmt.Println(get_word_pattern("get word pattern"))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
