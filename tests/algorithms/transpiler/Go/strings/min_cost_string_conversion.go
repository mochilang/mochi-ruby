//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 13:50:16 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

type TransformTables struct {
	Costs [][]int    `json:"costs"`
	Ops   [][]string `json:"ops"`
}

func string_to_chars(s string) []string {
	var chars []string = []string{}
	_ = chars
	var i int = 0
	_ = i
	for i < len(s) {
		chars = append(chars, _substr(s, i, (i+1)))
		i = (i + 1)
	}
	return chars
}

func join_chars(chars []string) string {
	var res string = ""
	_ = res
	var i int = 0
	_ = i
	for i < len(chars) {
		res = (res + _index(chars, i))
		i = (i + 1)
	}
	return res
}

func insert_at(chars []string, index int, ch string) []string {
	var res []string = []string{}
	_ = res
	var i int = 0
	_ = i
	for i < index {
		res = append(res, _index(chars, i))
		i = (i + 1)
	}
	res = append(res, ch)
	for i < len(chars) {
		res = append(res, _index(chars, i))
		i = (i + 1)
	}
	return res
}

func remove_at(chars []string, index int) []string {
	var res []string = []string{}
	_ = res
	var i int = 0
	_ = i
	for i < len(chars) {
		if i != index {
			res = append(res, _index(chars, i))
		}
		i = (i + 1)
	}
	return res
}

func make_matrix_int(rows int, cols int, init int) [][]int {
	var matrix [][]int = [][]int{}
	_ = matrix
	for _i := 0; _i < rows; _i++ {
		var row []int = []int{}
		_ = row
		for _2 := 0; _2 < cols; _2++ {
			row = append(row, init)
		}
		matrix = append(matrix, row)
	}
	return matrix
}

func make_matrix_string(rows int, cols int, init string) [][]string {
	var matrix [][]string = [][]string{}
	_ = matrix
	for _i := 0; _i < rows; _i++ {
		var row []string = []string{}
		_ = row
		for _2 := 0; _2 < cols; _2++ {
			row = append(row, init)
		}
		matrix = append(matrix, row)
	}
	return matrix
}

func compute_transform_tables(source_string string, destination_string string, copy_cost int, replace_cost int, delete_cost int, insert_cost int) TransformTables {
	var source_seq []string = string_to_chars(source_string)
	_ = source_seq
	var dest_seq []string = string_to_chars(destination_string)
	_ = dest_seq
	var m int = len(source_seq)
	_ = m
	var n int = len(dest_seq)
	_ = n
	var costs [][]int = make_matrix_int((m + 1), (n + 1), 0)
	_ = costs
	var ops [][]string = make_matrix_string((m + 1), (n + 1), "0")
	_ = ops
	var i int = 1
	_ = i
	for i <= m {
		_setIndex(_index(costs, i), 0, (i * delete_cost))
		_setIndex(_index(ops, i), 0, ("D" + _index(source_seq, (i-1))))
		i = (i + 1)
	}
	var j int = 1
	_ = j
	for j <= n {
		_setIndex(_index(costs, 0), j, (j * insert_cost))
		_setIndex(_index(ops, 0), j, ("I" + _index(dest_seq, (j-1))))
		j = (j + 1)
	}
	i = 1
	for i <= m {
		j = 1
		for j <= n {
			if _index(source_seq, (i-1)) == _index(dest_seq, (j-1)) {
				_setIndex(_index(costs, i), j, (_index(_index(costs, (i-1)), (j-1)) + copy_cost))
				_setIndex(_index(ops, i), j, ("C" + _index(source_seq, (i-1))))
			} else {
				_setIndex(_index(costs, i), j, (_index(_index(costs, (i-1)), (j-1)) + replace_cost))
				_setIndex(_index(ops, i), j, (("R" + _index(source_seq, (i-1))) + _index(dest_seq, (j-1))))
			}
			if (_index(_index(costs, (i-1)), j) + delete_cost) < _index(_index(costs, i), j) {
				_setIndex(_index(costs, i), j, (_index(_index(costs, (i-1)), j) + delete_cost))
				_setIndex(_index(ops, i), j, ("D" + _index(source_seq, (i-1))))
			}
			if (_index(_index(costs, i), (j-1)) + insert_cost) < _index(_index(costs, i), j) {
				_setIndex(_index(costs, i), j, (_index(_index(costs, i), (j-1)) + insert_cost))
				_setIndex(_index(ops, i), j, ("I" + _index(dest_seq, (j-1))))
			}
			j = (j + 1)
		}
		i = (i + 1)
	}
	return TransformTables{
		Costs: costs,
		Ops:   ops,
	}
}

func assemble_transformation(ops [][]string, i int, j int) []string {
	if (i == 0) && (j == 0) {
		return func(v any) []string {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]string); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []string{}
				}
				out := make([]string, len(arr))
				for i, x := range arr {
					out[i] = fmt.Sprint(x)
				}
				return out
			}
			return v.([]string)
		}([]string{})
	}
	var op string = _index(_index(ops, i), j)
	_ = op
	var kind string = _substr(op, 0, 1)
	_ = kind
	if (kind == "C") || (kind == "R") {
		var seq []string = assemble_transformation(func(v any) [][]string {
			if v == nil {
				return nil
			}
			if vv, ok := v.([][]string); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return [][]string{}
				}
				out := make([][]string, len(arr))
				for i, x := range arr {
					out[i] = func(v any) []string {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]string); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []string{}
							}
							out := make([]string, len(arr))
							for i, x := range arr {
								out[i] = x.(string)
							}
							return out
						}
						return v.([]string)
					}(x)
				}
				return out
			}
			return v.([][]string)
		}(ops), (i - 1), (j - 1))
		_ = seq
		seq = append(seq, op)
		return seq
	} else {
		if kind == "D" {
			var seq []string = assemble_transformation(func(v any) [][]string {
				if v == nil {
					return nil
				}
				if vv, ok := v.([][]string); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return [][]string{}
					}
					out := make([][]string, len(arr))
					for i, x := range arr {
						out[i] = func(v any) []string {
							if v == nil {
								return nil
							}
							if vv, ok := v.([]string); ok {
								return vv
							}
							if arr, ok := v.([]any); ok {
								if len(arr) == 0 {
									return []string{}
								}
								out := make([]string, len(arr))
								for i, x := range arr {
									out[i] = x.(string)
								}
								return out
							}
							return v.([]string)
						}(x)
					}
					return out
				}
				return v.([][]string)
			}(ops), (i - 1), j)
			_ = seq
			seq = append(seq, op)
			return seq
		} else {
			var seq []string = assemble_transformation(func(v any) [][]string {
				if v == nil {
					return nil
				}
				if vv, ok := v.([][]string); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return [][]string{}
					}
					out := make([][]string, len(arr))
					for i, x := range arr {
						out[i] = func(v any) []string {
							if v == nil {
								return nil
							}
							if vv, ok := v.([]string); ok {
								return vv
							}
							if arr, ok := v.([]any); ok {
								if len(arr) == 0 {
									return []string{}
								}
								out := make([]string, len(arr))
								for i, x := range arr {
									out[i] = x.(string)
								}
								return out
							}
							return v.([]string)
						}(x)
					}
					return out
				}
				return v.([][]string)
			}(ops), i, (j - 1))
			_ = seq
			seq = append(seq, op)
			return seq
		}
	}
}

func mochiMain() {
	var copy_cost int = (0 - 1)
	_ = copy_cost
	var replace_cost int = 1
	_ = replace_cost
	var delete_cost int = 2
	_ = delete_cost
	var insert_cost int = 2
	_ = insert_cost
	var src string = "Python"
	_ = src
	var dst string = "Algorithms"
	_ = dst
	var tables TransformTables = compute_transform_tables(src, dst, copy_cost, replace_cost, delete_cost, insert_cost)
	_ = tables
	var operations [][]string = tables.Ops
	_ = operations
	var m int = len(operations)
	_ = m
	var n int = len(_index(operations, 0))
	_ = n
	var sequence []string = assemble_transformation(func(v any) [][]string {
		if v == nil {
			return nil
		}
		if vv, ok := v.([][]string); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return [][]string{}
			}
			out := make([][]string, len(arr))
			for i, x := range arr {
				out[i] = func(v any) []string {
					if v == nil {
						return nil
					}
					if vv, ok := v.([]string); ok {
						return vv
					}
					if arr, ok := v.([]any); ok {
						if len(arr) == 0 {
							return []string{}
						}
						out := make([]string, len(arr))
						for i, x := range arr {
							out[i] = x.(string)
						}
						return out
					}
					return v.([]string)
				}(x)
			}
			return out
		}
		return v.([][]string)
	}(operations), (m - 1), (n - 1))
	_ = sequence
	var string_list []string = string_to_chars(src)
	_ = string_list
	var idx int = 0
	_ = idx
	var cost int = 0
	_ = cost
	var k int = 0
	_ = k
	for k < len(sequence) {
		fmt.Println(join_chars(func(v any) []string {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]string); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []string{}
				}
				out := make([]string, len(arr))
				for i, x := range arr {
					out[i] = fmt.Sprint(x)
				}
				return out
			}
			return v.([]string)
		}(string_list)))
		var op string = _index(sequence, k)
		_ = op
		var kind string = _substr(op, 0, 1)
		_ = kind
		if kind == "C" {
			cost = (cost + copy_cost)
		} else {
			if kind == "R" {
				_setIndex(string_list, idx, _substr(op, 2, 3))
				cost = (cost + replace_cost)
			} else {
				if kind == "D" {
					string_list = remove_at(func(v any) []string {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]string); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []string{}
							}
							out := make([]string, len(arr))
							for i, x := range arr {
								out[i] = fmt.Sprint(x)
							}
							return out
						}
						return v.([]string)
					}(string_list), idx)
					cost = (cost + delete_cost)
				} else {
					string_list = insert_at(func(v any) []string {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]string); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []string{}
							}
							out := make([]string, len(arr))
							for i, x := range arr {
								out[i] = fmt.Sprint(x)
							}
							return out
						}
						return v.([]string)
					}(string_list), idx, _substr(op, 1, 2))
					cost = (cost + insert_cost)
				}
			}
		}
		idx = (idx + 1)
		k = (k + 1)
	}
	fmt.Println(join_chars(func(v any) []string {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]string); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []string{}
			}
			out := make([]string, len(arr))
			for i, x := range arr {
				out[i] = fmt.Sprint(x)
			}
			return out
		}
		return v.([]string)
	}(string_list)))
	fmt.Println(("Cost: " + fmt.Sprint(cost)))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
