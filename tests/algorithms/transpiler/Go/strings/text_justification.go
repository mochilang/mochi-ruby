//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 14:36:40 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

var __name__ string = "__main__"

func repeat_str(s string, count int) string {
	var res string = ""
	_ = res
	var i int = 0
	_ = i
	for i < count {
		res = (res + s)
		i = (i + 1)
	}
	return res
}

func split_words(s string) []string {
	var res []string = []string{}
	_ = res
	var current string = ""
	_ = current
	var i int = 0
	_ = i
	for i < len(s) {
		var ch string = string([]rune(s)[i:(i + 1)])
		_ = ch
		if ch == " " {
			if current != "" {
				res = append(res, current)
				current = ""
			}
		} else {
			current = (current + ch)
		}
		i = (i + 1)
	}
	if current != "" {
		res = append(res, current)
	}
	return res
}

func justify_line(line []string, width int, max_width int) string {
	var overall_spaces_count int = (max_width - width)
	_ = overall_spaces_count
	var words_count int = len(line)
	_ = words_count
	if words_count == 1 {
		return (_index(line, 0) + repeat_str(" ", overall_spaces_count))
	}
	var spaces_to_insert_between_words int = (words_count - 1)
	_ = spaces_to_insert_between_words
	var num_spaces_between_words_list []int = []int{}
	_ = num_spaces_between_words_list
	var base int = (overall_spaces_count / spaces_to_insert_between_words)
	_ = base
	var extra int = _modi(overall_spaces_count, spaces_to_insert_between_words)
	_ = extra
	var i int = 0
	_ = i
	for i < spaces_to_insert_between_words {
		var spaces int = base
		_ = spaces
		if i < extra {
			spaces = (spaces + 1)
		}
		num_spaces_between_words_list = append(num_spaces_between_words_list, spaces)
		i = (i + 1)
	}
	var aligned string = ""
	_ = aligned
	i = 0
	for i < spaces_to_insert_between_words {
		aligned = ((aligned + _index(line, i)) + repeat_str(" ", _index(num_spaces_between_words_list, i)))
		i = (i + 1)
	}
	aligned = (aligned + _index(line, spaces_to_insert_between_words))
	return aligned
}

func text_justification(word string, max_width int) []string {
	var words []string = split_words(word)
	_ = words
	var answer []string = []string{}
	_ = answer
	var line []string = []string{}
	_ = line
	var width int = 0
	_ = width
	var idx int = 0
	_ = idx
	for idx < len(words) {
		var w string = _index(words, idx)
		_ = w
		if ((width + len(w)) + len(line)) <= max_width {
			line = append(line, w)
			width = (width + len(w))
		} else {
			answer = append(answer, justify_line(func(v any) []string {
				if v == nil {
					return nil
				}
				if vv, ok := v.([]string); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return []string{}
					}
					out := make([]string, len(arr))
					for i, x := range arr {
						out[i] = fmt.Sprint(x)
					}
					return out
				}
				return v.([]string)
			}(line), width, max_width))
			line = []string{w}
			width = len(w)
		}
		idx = (idx + 1)
	}
	var remaining_spaces int = ((max_width - width) - len(line))
	_ = remaining_spaces
	var last_line string = ""
	_ = last_line
	var j int = 0
	_ = j
	for j < len(line) {
		if j > 0 {
			last_line = (last_line + " ")
		}
		last_line = (last_line + _index(line, j))
		j = (j + 1)
	}
	last_line = (last_line + repeat_str(" ", (remaining_spaces+1)))
	answer = append(answer, last_line)
	return answer
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(text_justification("This is an example of text justification.", 16)))
		fmt.Println(fmt.Sprint(text_justification("Two roads diverged in a yellow wood", 16)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
