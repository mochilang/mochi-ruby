//go:build ignore

// Generated by Mochi v0.10.69 on 2025-08-22 13:08:02 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

var seed int

func rand() int {
	seed = _modi(((seed * 1103515245) + 12345), 2147483648)
	return (seed / 65536)
}

func randint(a int, b int) int {
	var r int = rand()
	_ = r
	_ = r
	return (a + _modi(r, ((b-a)+1)))
}

func fisher_yates_shuffle_int(data []int) []int {
	var res []int = data
	_ = res
	var i int = 0
	_ = i
	for i < len(res) {
		var a int = randint(0, (len(res) - 1))
		_ = a
		var b int = randint(0, (len(res) - 1))
		_ = b
		var temp int = _index(res, a)
		_ = temp
		_setIndex(res, a, _index(res, b))
		_setIndex(res, b, temp)
		i = (i + 1)
	}
	return res
}

func fisher_yates_shuffle_str(data []string) []string {
	var res []string = data
	_ = res
	var i int = 0
	_ = i
	for i < len(res) {
		var a int = randint(0, (len(res) - 1))
		_ = a
		var b int = randint(0, (len(res) - 1))
		_ = b
		var temp string = _index(res, a)
		_ = temp
		_setIndex(res, a, _index(res, b))
		_setIndex(res, b, temp)
		i = (i + 1)
	}
	return res
}

var integers []int

var strings_ []string

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		seed = 1
		integers = []int{0, 1, 2, 3, 4, 5, 6, 7}
		strings_ = []string{"python", "says", "hello", "!"}
		fmt.Println("Fisher-Yates Shuffle:")
		fmt.Println(((("List " + fmt.Sprint(integers)) + " ") + fmt.Sprint(strings_)))
		fmt.Println(((("FY Shuffle " + fmt.Sprint(fisher_yates_shuffle_int(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}(integers)))) + " ") + fmt.Sprint(fisher_yates_shuffle_str(func(v any) []string {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]string); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []string{}
				}
				out := make([]string, len(arr))
				for i, x := range arr {
					out[i] = fmt.Sprint(x)
				}
				return out
			}
			return v.([]string)
		}(strings_)))))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
