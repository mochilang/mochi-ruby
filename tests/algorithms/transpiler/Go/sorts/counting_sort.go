//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 01:34:38 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

func max_val(arr []int) int {
	var m int = _index(arr, 0)
	_ = m
	var i int = 1
	_ = i
	for i < len(arr) {
		if _index(arr, i) > m {
			m = _index(arr, i)
		}
		i = (i + 1)
	}
	return m
}

func min_val(arr []int) int {
	var m int = _index(arr, 0)
	_ = m
	var i int = 1
	_ = i
	for i < len(arr) {
		if _index(arr, i) < m {
			m = _index(arr, i)
		}
		i = (i + 1)
	}
	return m
}

func counting_sort(collection []int) []int {
	if len(collection) == 0 {
		return func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{})
	}
	var coll_len int = len(collection)
	_ = coll_len
	var coll_max int = max_val(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(collection))
	_ = coll_max
	var coll_min int = min_val(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(collection))
	_ = coll_min
	var counting_arr_length int = ((coll_max + 1) - coll_min)
	_ = counting_arr_length
	var counting_arr []int = []int{}
	_ = counting_arr
	var i int = 0
	_ = i
	for i < counting_arr_length {
		counting_arr = append(counting_arr, 0)
		i = (i + 1)
	}
	i = 0
	for i < coll_len {
		var number int = _index(collection, i)
		_ = number
		_setIndex(counting_arr, (number - coll_min), (_index(counting_arr, (number-coll_min)) + 1))
		i = (i + 1)
	}
	i = 1
	for i < counting_arr_length {
		_setIndex(counting_arr, i, (_index(counting_arr, i) + _index(counting_arr, (i-1))))
		i = (i + 1)
	}
	var ordered []int = []int{}
	_ = ordered
	i = 0
	for i < coll_len {
		ordered = append(ordered, 0)
		i = (i + 1)
	}
	var idx int = (coll_len - 1)
	_ = idx
	for idx >= 0 {
		var number int = _index(collection, idx)
		_ = number
		var pos int = (_index(counting_arr, (number-coll_min)) - 1)
		_ = pos
		_setIndex(ordered, pos, number)
		_setIndex(counting_arr, (number - coll_min), (_index(counting_arr, (number-coll_min)) - 1))
		idx = (idx - 1)
	}
	return ordered
}

var ascii_chars string

func chr(code int) string {
	if code == 10 {
		return "\n"
	}
	if code == 13 {
		return "\r"
	}
	if code == 9 {
		return "\t"
	}
	if (code >= 32) && (code < 127) {
		return string([]rune(ascii_chars)[(code - 32):(code - 31)])
	}
	return ""
}

func ord(ch string) int {
	if ch == "\n" {
		return 10
	}
	if ch == "\r" {
		return 13
	}
	if ch == "\t" {
		return 9
	}
	var i int = 0
	_ = i
	for i < len(ascii_chars) {
		if string([]rune(ascii_chars)[i:(i+1)]) == ch {
			return (32 + i)
		}
		i = (i + 1)
	}
	return 0
}

func counting_sort_string(s string) string {
	var codes []int = []int{}
	_ = codes
	var i int = 0
	_ = i
	for i < len(s) {
		codes = append(codes, ord(string([]rune(s)[i:(i+1)])))
		i = (i + 1)
	}
	var sorted_codes []int = counting_sort(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(codes))
	_ = sorted_codes
	var res string = ""
	_ = res
	i = 0
	for i < len(sorted_codes) {
		res = (res + chr(_index(sorted_codes, i)))
		i = (i + 1)
	}
	return res
}

var example1 []int

var example2 []int

var example3 []int

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		ascii_chars = " !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~"
		example1 = counting_sort(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{0, 5, 3, 2, 2}))
		fmt.Println(fmt.Sprint(example1))
		example2 = counting_sort(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{}))
		fmt.Println(fmt.Sprint(example2))
		example3 = counting_sort(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{func(v any) int {
			if vv, ok := v.(int); ok {
				return vv
			}
			return 0
		}((0 - 2)), func(v any) int {
			if vv, ok := v.(int); ok {
				return vv
			}
			return 0
		}((0 - 5)), func(v any) int {
			if vv, ok := v.(int); ok {
				return vv
			}
			return 0
		}((0 - 45))}))
		fmt.Println(fmt.Sprint(example3))
		fmt.Println(counting_sort_string("thisisthestring"))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
