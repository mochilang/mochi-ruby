//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 01:34:20 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _floatEqual(a, b float64) bool {
	if a > b {
		return a-b < 1e-6
	}
	return b-a < 1e-6
}

func int_to_float(x int) float64 {
	return (float64(x) * 1.0)
}

func floor_int(x float64) int {
	var i int = 0
	_ = i
	for int_to_float((i + 1)) <= x {
		i = (i + 1)
	}
	return i
}

func set_at_float(xs []float64, idx int, value float64) []float64 {
	var i int = 0
	_ = i
	var res []float64 = []float64{}
	_ = res
	for i < len(xs) {
		if i == idx {
			res = append(res, value)
		} else {
			res = append(res, _index(xs, i))
		}
		i = (i + 1)
	}
	return res
}

func set_at_list_float(xs [][]float64, idx int, value []float64) [][]float64 {
	var i int = 0
	_ = i
	var res [][]float64 = [][]float64{}
	_ = res
	for i < len(xs) {
		if i == idx {
			res = append(res, value)
		} else {
			res = append(res, _index(xs, i))
		}
		i = (i + 1)
	}
	return res
}

func sort_float(xs []float64) []float64 {
	var res []float64 = xs
	_ = res
	var i int = 1
	_ = i
	for i < len(res) {
		var key float64 = _index(res, i)
		_ = key
		var j int = (i - 1)
		_ = j
		for (j >= 0) && (_index(res, j) > key) {
			res = set_at_float(func(v any) []float64 {
				if v == nil {
					return nil
				}
				if vv, ok := v.([]float64); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return []float64{}
					}
					out := make([]float64, len(arr))
					for i, x := range arr {
						out[i] = x.(float64)
					}
					return out
				}
				return v.([]float64)
			}(res), (j + 1), _index(res, j))
			j = (j - 1)
		}
		res = set_at_float(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}(res), (j + 1), key)
		i = (i + 1)
	}
	return res
}

func bucket_sort_with_count(xs []float64, bucket_count int) []float64 {
	if (len(xs) == 0) || (bucket_count <= 0) {
		return func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{})
	}
	var min_value float64 = _index(xs, 0)
	_ = min_value
	var max_value float64 = _index(xs, 0)
	_ = max_value
	var i int = 1
	_ = i
	for i < len(xs) {
		if _index(xs, i) < min_value {
			min_value = _index(xs, i)
		}
		if _index(xs, i) > max_value {
			max_value = _index(xs, i)
		}
		i = (i + 1)
	}
	if _floatEqual(max_value, min_value) {
		return xs
	}
	var bucket_size float64 = (float64((max_value - min_value)) / float64(int_to_float(bucket_count)))
	_ = bucket_size
	var buckets [][]float64 = [][]float64{}
	_ = buckets
	i = 0
	for i < bucket_count {
		buckets = append(buckets, []float64{})
		i = (i + 1)
	}
	i = 0
	for i < len(xs) {
		var val float64 = _index(xs, i)
		_ = val
		var idx int = floor_int((float64((val - min_value)) / float64(bucket_size)))
		_ = idx
		if idx < 0 {
			idx = 0
		}
		if idx >= bucket_count {
			idx = (bucket_count - 1)
		}
		var bucket []float64 = _index(buckets, idx)
		_ = bucket
		bucket = append(bucket, val)
		buckets = set_at_list_float(func(v any) [][]float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([][]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return [][]float64{}
				}
				out := make([][]float64, len(arr))
				for i, x := range arr {
					out[i] = func(v any) []float64 {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]float64); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []float64{}
							}
							out := make([]float64, len(arr))
							for i, x := range arr {
								out[i] = x.(float64)
							}
							return out
						}
						return v.([]float64)
					}(x)
				}
				return out
			}
			return v.([][]float64)
		}(buckets), idx, func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}(bucket))
		i = (i + 1)
	}
	var result []float64 = []float64{}
	_ = result
	i = 0
	for i < len(buckets) {
		var sorted_bucket []float64 = sort_float(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}(_index(buckets, i)))
		_ = sorted_bucket
		var j int = 0
		_ = j
		for j < len(sorted_bucket) {
			result = append(result, _index(sorted_bucket, j))
			j = (j + 1)
		}
		i = (i + 1)
	}
	return result
}

func bucket_sort(xs []float64) []float64 {
	return bucket_sort_with_count(func(v any) []float64 {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]float64); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []float64{}
			}
			out := make([]float64, len(arr))
			for i, x := range arr {
				out[i] = x.(float64)
			}
			return out
		}
		return v.([]float64)
	}(xs), 10)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(bucket_sort(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{(0 - 1.0), 2.0, (0 - 5.0), 0.0}))))
		fmt.Println(fmt.Sprint(bucket_sort(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{9.0, 8.0, 7.0, 6.0, (0 - 12.0)}))))
		fmt.Println(fmt.Sprint(bucket_sort(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{0.4, 1.2, 0.1, 0.2, (0 - 0.9)}))))
		fmt.Println(fmt.Sprint(bucket_sort(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{}))))
		fmt.Println(fmt.Sprint(bucket_sort(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{(0 - 10000000000.0), 10000000000.0}))))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
