//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 01:31:04 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

func make_list(n int, value int) []int {
	var result []int = []int{}
	_ = result
	var i int = 0
	_ = i
	for i < n {
		result = append(result, value)
		i = (i + 1)
	}
	return result
}

func min_value(arr []int) int {
	var m int = _index(arr, 0)
	_ = m
	var i int = 1
	_ = i
	for i < len(arr) {
		if _index(arr, i) < m {
			m = _index(arr, i)
		}
		i = (i + 1)
	}
	return m
}

func max_value(arr []int) int {
	var m int = _index(arr, 0)
	_ = m
	var i int = 1
	_ = i
	for i < len(arr) {
		if _index(arr, i) > m {
			m = _index(arr, i)
		}
		i = (i + 1)
	}
	return m
}

func pigeon_sort(array []int) []int {
	if len(array) == 0 {
		return array
	}
	var mn int = min_value(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(array))
	_ = mn
	var mx int = max_value(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(array))
	_ = mx
	var holes_range int = ((mx - mn) + 1)
	_ = holes_range
	var holes []int = make_list(holes_range, 0)
	_ = holes
	var holes_repeat []int = make_list(holes_range, 0)
	_ = holes_repeat
	var i int = 0
	_ = i
	for i < len(array) {
		var index int = (_index(array, i) - mn)
		_ = index
		_setIndex(holes, index, _index(array, i))
		_setIndex(holes_repeat, index, (_index(holes_repeat, index) + 1))
		i = (i + 1)
	}
	var array_index int = 0
	_ = array_index
	var h int = 0
	_ = h
	for h < holes_range {
		for _index(holes_repeat, h) > 0 {
			_setIndex(array, array_index, _index(holes, h))
			array_index = (array_index + 1)
			_setIndex(holes_repeat, h, (_index(holes_repeat, h) - 1))
		}
		h = (h + 1)
	}
	return array
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(pigeon_sort(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{0, 5, 3, 2, 2}))))
		fmt.Println(fmt.Sprint(pigeon_sort(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{}))))
		fmt.Println(fmt.Sprint(pigeon_sort(func(v any) []int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []int{}
				}
				out := make([]int, len(arr))
				for i, x := range arr {
					out[i] = x.(int)
				}
				return out
			}
			return v.([]int)
		}([]int{func(v any) int {
			if vv, ok := v.(int); ok {
				return vv
			}
			return 0
		}((0 - 2)), func(v any) int {
			if vv, ok := v.(int); ok {
				return vv
			}
			return 0
		}((0 - 5)), func(v any) int {
			if vv, ok := v.(int); ok {
				return vv
			}
			return 0
		}((0 - 45))}))))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
