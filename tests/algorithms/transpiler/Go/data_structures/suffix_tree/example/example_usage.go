//go:build ignore

// Generated by Mochi v0.10.72 on 2025-08-25 00:07:34 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

var __name__ string = "__main__"

type SuffixTree struct {
	Text string `json:"text"`
}

func new_suffix_tree(text string) SuffixTree {
	return SuffixTree{
		Text: text,
	}
}

func search(tree SuffixTree, pattern string) bool {
	var n int = len(tree.Text)
	_ = n
	var m int = len(pattern)
	_ = m
	if m == 0 {
		return true
	}
	if m > n {
		return false
	}
	var i int = 0
	_ = i
	for i <= (n - m) {
		if string([]rune(tree.Text)[i:(i+m)]) == pattern {
			return true
		}
		i = (i + 1)
	}
	return false
}

func mochiMain() {
	var text string = "monkey banana"
	_ = text
	var suffix_tree SuffixTree = new_suffix_tree(text)
	_ = suffix_tree
	var patterns []string = []string{"ana", "ban", "na", "xyz", "mon"}
	_ = patterns
	var i int = 0
	_ = i
	for i < len(patterns) {
		var pattern string = _index(patterns, i)
		_ = pattern
		var found bool = search(suffix_tree, pattern)
		_ = found
		fmt.Println(((("Pattern '" + pattern) + "' found: ") + fmt.Sprint(found)))
		i = (i + 1)
	}
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
