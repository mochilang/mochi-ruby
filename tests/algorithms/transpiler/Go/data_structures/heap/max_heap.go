//go:build ignore

// Generated by Mochi v0.10.72 on 2025-08-25 00:07:26 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

var __name__ string = "__main__"

var heap []int

var size int

func swap_up(i int) {
	var temp int = _index(heap, i)
	_ = temp
	var idx int = i
	_ = idx
	for func() int {
		a := idx
		b := 2
		q := a / b
		r := a % b
		if (r != 0) && ((r < 0) != (b < 0)) {
			q--
		}
		return q
	}() > 0 {
		if _index(heap, idx) > _index(heap, func() int {
			a := idx
			b := 2
			q := a / b
			r := a % b
			if (r != 0) && ((r < 0) != (b < 0)) {
				q--
			}
			return q
		}()) {
			_setIndex(heap, idx, _index(heap, func() int {
				a := idx
				b := 2
				q := a / b
				r := a % b
				if (r != 0) && ((r < 0) != (b < 0)) {
					q--
				}
				return q
			}()))
			_setIndex(heap, func() int {
				a := idx
				b := 2
				q := a / b
				r := a % b
				if (r != 0) && ((r < 0) != (b < 0)) {
					q--
				}
				return q
			}(), temp)
		}
		idx = func() int {
			a := idx
			b := 2
			q := a / b
			r := a % b
			if (r != 0) && ((r < 0) != (b < 0)) {
				q--
			}
			return q
		}()
	}
}

func insert(value int) {
	heap = append(heap, value)
	size = (size + 1)
	swap_up(size)
}

func swap_down(i int) {
	var idx int = i
	_ = idx
	for size >= (2 * idx) {
		var bigger_child int = func() int {
			if ((2 * idx) + 1) > size {
				return (2 * idx)
			} else {
				return func() int {
					if _index(heap, (2*idx)) > _index(heap, ((2*idx)+1)) {
						return (2 * idx)
					} else {
						return ((2 * idx) + 1)
					}
				}()
			}
		}()
		_ = bigger_child
		var temp int = _index(heap, idx)
		_ = temp
		if _index(heap, idx) < _index(heap, bigger_child) {
			_setIndex(heap, idx, _index(heap, bigger_child))
			_setIndex(heap, bigger_child, temp)
		}
		idx = bigger_child
	}
}

func shrink() {
	var new_heap []int = []int{}
	_ = new_heap
	var i int = 0
	_ = i
	for i <= size {
		new_heap = append(new_heap, _index(heap, i))
		i = (i + 1)
	}
	heap = new_heap
}

func pop() int {
	var max_value int = _index(heap, 1)
	_ = max_value
	_ = max_value
	_setIndex(heap, 1, _index(heap, size))
	size = (size - 1)
	shrink()
	swap_down(1)
	return max_value
}

func get_list() []int {
	var out []int = []int{}
	_ = out
	var i int = 1
	_ = i
	for i <= size {
		out = append(out, _index(heap, i))
		i = (i + 1)
	}
	return out
}

func len_() int {
	return size
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		heap = []int{0}
		size = 0
		insert(6)
		insert(10)
		insert(15)
		insert(12)
		fmt.Println(pop())
		fmt.Println(pop())
		fmt.Println(fmt.Sprint(get_list()))
		fmt.Println(len_())
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
