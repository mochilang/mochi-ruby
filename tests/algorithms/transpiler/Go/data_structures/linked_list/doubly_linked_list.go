//go:build ignore

// Generated by Mochi v0.10.72 on 2025-08-25 00:06:19 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

var __name__ string = "__main__"

type DoublyLinkedList struct {
	Data []int `json:"data"`
}

func empty_list() DoublyLinkedList {
	return DoublyLinkedList{
		Data: []int{},
	}
}

func length(list DoublyLinkedList) int {
	return len(list.Data)
}

func is_empty(list DoublyLinkedList) bool {
	return (len(list.Data) == 0)
}

func to_string(list DoublyLinkedList) string {
	if len(list.Data) == 0 {
		return ""
	}
	var s string = fmt.Sprint(_index(list.Data, 0))
	_ = s
	var i int = 1
	_ = i
	for i < len(list.Data) {
		s = ((s + "->") + fmt.Sprint(_index(list.Data, i)))
		i = (i + 1)
	}
	return s
}

func insert_nth(list DoublyLinkedList, index int, value int) DoublyLinkedList {
	if (index < 0) || (index > len(list.Data)) {
		panic("index out of range")
	}
	var res []int = []int{}
	_ = res
	var i int = 0
	_ = i
	for i < index {
		res = append(res, _index(list.Data, i))
		i = (i + 1)
	}
	res = append(res, value)
	for i < len(list.Data) {
		res = append(res, _index(list.Data, i))
		i = (i + 1)
	}
	return DoublyLinkedList{
		Data: res,
	}
}

func insert_head(list DoublyLinkedList, value int) DoublyLinkedList {
	return insert_nth(list, 0, value)
}

func insert_tail(list DoublyLinkedList, value int) DoublyLinkedList {
	return insert_nth(list, len(list.Data), value)
}

type DeleteResult struct {
	List  DoublyLinkedList `json:"list"`
	Value int              `json:"value"`
}

func delete_nth(list DoublyLinkedList, index int) DeleteResult {
	if (index < 0) || (index >= len(list.Data)) {
		panic("index out of range")
	}
	var res []int = []int{}
	_ = res
	var i int = 0
	_ = i
	var removed int = 0
	_ = removed
	for i < len(list.Data) {
		if i == index {
			removed = _index(list.Data, i)
		} else {
			res = append(res, _index(list.Data, i))
		}
		i = (i + 1)
	}
	return DeleteResult{
		List: DoublyLinkedList{
			Data: res,
		},
		Value: removed,
	}
}

func delete_head(list DoublyLinkedList) DeleteResult {
	return delete_nth(list, 0)
}

func delete_tail(list DoublyLinkedList) DeleteResult {
	return delete_nth(list, (len(list.Data) - 1))
}

func delete_value(list DoublyLinkedList, value int) DeleteResult {
	var idx int = 0
	_ = idx
	var found bool = false
	_ = found
	for idx < len(list.Data) {
		if _index(list.Data, idx) == value {
			found = true
			break
		}
		idx = (idx + 1)
	}
	if !found {
		panic("value not found")
	}
	return delete_nth(list, idx)
}

func mochiMain() {
	var dll DoublyLinkedList = empty_list()
	_ = dll
	dll = insert_tail(dll, 1)
	dll = insert_tail(dll, 2)
	dll = insert_tail(dll, 3)
	fmt.Println(to_string(dll))
	dll = insert_head(dll, 0)
	fmt.Println(to_string(dll))
	dll = insert_nth(dll, 2, 9)
	fmt.Println(to_string(dll))
	var res DeleteResult = delete_nth(dll, 2)
	_ = res
	dll = res.List
	fmt.Println(res.Value)
	fmt.Println(to_string(dll))
	res = delete_tail(dll)
	dll = res.List
	fmt.Println(res.Value)
	fmt.Println(to_string(dll))
	res = delete_value(dll, 1)
	dll = res.List
	fmt.Println(res.Value)
	fmt.Println(to_string(dll))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
