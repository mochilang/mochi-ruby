//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 21:04:05 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func mux(input0 int, input1 int, select_ int) int {
	if (((input0 != 0) && (input0 != 1)) || ((input1 != 0) && (input1 != 1))) || ((select_ != 0) && (select_ != 1)) {
		panic("Inputs and select signal must be 0 or 1")
	}
	if select_ == 1 {
		return input1
	}
	return input0
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(mux(0, 1, 0))
		fmt.Println(mux(0, 1, 1))
		fmt.Println(mux(1, 0, 0))
		fmt.Println(mux(1, 0, 1))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
