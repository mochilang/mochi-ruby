//go:build ignore

// Generated by Mochi v0.10.59 on 2025-08-06 21:47:06 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

func strip(s string) string {
	var start int = 0
	_ = start
	var end int = len(s)
	_ = end
	for (start < end) && (_substr(s, start, (start+1)) == " ") {
		start = (start + 1)
	}
	for (end > start) && (_substr(s, (end-1), end) == " ") {
		end = (end - 1)
	}
	return _substr(s, start, end)
}

func hex_digit_value(c string) int {
	if c == "0" {
		return 0
	}
	if c == "1" {
		return 1
	}
	if c == "2" {
		return 2
	}
	if c == "3" {
		return 3
	}
	if c == "4" {
		return 4
	}
	if c == "5" {
		return 5
	}
	if c == "6" {
		return 6
	}
	if c == "7" {
		return 7
	}
	if c == "8" {
		return 8
	}
	if c == "9" {
		return 9
	}
	if (c == "a") || (c == "A") {
		return 10
	}
	if (c == "b") || (c == "B") {
		return 11
	}
	if (c == "c") || (c == "C") {
		return 12
	}
	if (c == "d") || (c == "D") {
		return 13
	}
	if (c == "e") || (c == "E") {
		return 14
	}
	if (c == "f") || (c == "F") {
		return 15
	}
	fmt.Println("Non-hexadecimal value was passed to the function")
	return 0
}

func hex_to_decimal(hex_string string) int {
	var s string = strip(hex_string)
	_ = s
	if len(s) == 0 {
		fmt.Println("Empty string was passed to the function")
		return 0
	}
	var is_negative bool = false
	_ = is_negative
	if _substr(s, 0, 1) == "-" {
		is_negative = true
		s = _substr(s, 1, len(s))
	}
	var decimal_number int = 0
	_ = decimal_number
	var i int = 0
	_ = i
	for i < len(s) {
		var c string = _substr(s, i, (i + 1))
		_ = c
		var value int = hex_digit_value(c)
		_ = value
		decimal_number = ((16 * decimal_number) + value)
		i = (i + 1)
	}
	if is_negative {
		return (0 - decimal_number)
	}
	return decimal_number
}

func mochiMain() {
	fmt.Println(fmt.Sprint(hex_to_decimal("a")))
	fmt.Println(fmt.Sprint(hex_to_decimal("12f")))
	fmt.Println(fmt.Sprint(hex_to_decimal("   12f   ")))
	fmt.Println(fmt.Sprint(hex_to_decimal("FfFf")))
	fmt.Println(fmt.Sprint(hex_to_decimal("-Ff")))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
