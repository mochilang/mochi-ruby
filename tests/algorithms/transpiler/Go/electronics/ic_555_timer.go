//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-15 06:49:15 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	math "math"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func astable_frequency(resistance_1 float64, resistance_2 float64, capacitance float64) float64 {
	if ((resistance_1 <= 0.0) || (resistance_2 <= 0.0)) || (capacitance <= 0.0) {
		panic("All values must be positive")
	}
	return ((float64(1.44) / float64(((resistance_1 + (2.0 * resistance_2)) * capacitance))) * 1000000.0)
}

func astable_duty_cycle(resistance_1 float64, resistance_2 float64) float64 {
	if (resistance_1 <= 0.0) || (resistance_2 <= 0.0) {
		panic("All values must be positive")
	}
	return ((float64((resistance_1 + resistance_2)) / float64((resistance_1 + (2.0 * resistance_2)))) * 100.0)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(func() string {
			f := float64(astable_frequency(45.0, 45.0, 7.0))
			if f == 0 && math.Signbit(f) {
				return "-0"
			}
			if f == float64(int(f)) {
				return fmt.Sprintf("%.1f", f)
			}
			return fmt.Sprint(f)
		}())
		fmt.Println(func() string {
			f := float64(astable_duty_cycle(45.0, 45.0))
			if f == 0 && math.Signbit(f) {
				return "-0"
			}
			if f == float64(int(f)) {
				return fmt.Sprintf("%.1f", f)
			}
			return fmt.Sprint(f)
		}())
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
