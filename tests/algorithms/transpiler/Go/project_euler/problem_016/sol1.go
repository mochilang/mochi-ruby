//go:build ignore

// Generated by Mochi v0.10.71 on 2025-08-23 15:34:34 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

var __name__ string = "__main__"

func power_of_two(exp int) int {
	var result int = 1
	_ = result
	var i int = 0
	_ = i
	for i < exp {
		result = (result * 2)
		i = (i + 1)
	}
	return result
}

func solution(power int) int {
	var num int = power_of_two(power)
	_ = num
	var string_num string = fmt.Sprint(num)
	_ = string_num
	var sum int = 0
	_ = sum
	var i int = 0
	_ = i
	for i < len(string_num) {
		sum = (sum + func() int { n, _ := strconv.Atoi(fmt.Sprint(string(_index([]rune(string_num), i)))); return n }())
		i = (i + 1)
	}
	return sum
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(solution(1000)))
		fmt.Println(fmt.Sprint(solution(50)))
		fmt.Println(fmt.Sprint(solution(20)))
		fmt.Println(fmt.Sprint(solution(15)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
