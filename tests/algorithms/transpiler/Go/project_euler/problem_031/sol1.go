//go:build ignore

// Generated by Mochi v0.10.73 on 2025-08-25 23:59:56 GMT+7
package main

import (
	"bufio"
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

var _scanner = bufio.NewScanner(os.Stdin)

func _input() string {
	if !_scanner.Scan() {
		return ""
	}
	return _scanner.Text()
}

var __name__ string = "__main__"

func one_pence() int {
	return 1
}

func two_pence(x int) int {
	if x < 0 {
		return 0
	}
	return (two_pence((x - 2)) + one_pence())
}

func five_pence(x int) int {
	if x < 0 {
		return 0
	}
	return (five_pence((x - 5)) + two_pence(x))
}

func ten_pence(x int) int {
	if x < 0 {
		return 0
	}
	return (ten_pence((x - 10)) + five_pence(x))
}

func twenty_pence(x int) int {
	if x < 0 {
		return 0
	}
	return (twenty_pence((x - 20)) + ten_pence(x))
}

func fifty_pence(x int) int {
	if x < 0 {
		return 0
	}
	return (fifty_pence((x - 50)) + twenty_pence(x))
}

func one_pound(x int) int {
	if x < 0 {
		return 0
	}
	return (one_pound((x - 100)) + fifty_pence(x))
}

func two_pound(x int) int {
	if x < 0 {
		return 0
	}
	return (two_pound((x - 200)) + one_pound(x))
}

func solution(n int) int {
	return two_pound(n)
}

func mochiMain() {
	var n int = func() int { n, _ := strconv.Atoi(fmt.Sprint(_input())); return n }()
	_ = n
	fmt.Println(fmt.Sprint(solution(n)))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
