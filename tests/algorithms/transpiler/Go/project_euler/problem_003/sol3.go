//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 15:02:33 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

var __name__ string = "__main__"

func largest_prime_factor(n int) int {
	if n <= 1 {
		return n
	}
	var i int = 2
	_ = i
	var ans int = 0
	_ = ans
	var m int = n
	_ = m
	if m == 2 {
		return 2
	}
	for m > 2 {
		for _modi(m, i) != 0 {
			i = (i + 1)
		}
		ans = i
		for _modi(m, i) == 0 {
			m = (m / i)
		}
		i = (i + 1)
	}
	return ans
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		func() { b, _ := json.MarshalIndent(largest_prime_factor(13195), "", "  "); fmt.Println(string(b)) }()
		func() { b, _ := json.MarshalIndent(largest_prime_factor(10), "", "  "); fmt.Println(string(b)) }()
		func() { b, _ := json.MarshalIndent(largest_prime_factor(17), "", "  "); fmt.Println(string(b)) }()
		func() {
			b, _ := json.MarshalIndent(largest_prime_factor(600851475143), "", "  ")
			fmt.Println(string(b))
		}()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
