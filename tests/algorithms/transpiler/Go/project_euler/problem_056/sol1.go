//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-22 15:34:01 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func pow_int(base int, exp int) int {
	var result int = 1
	_ = result
	var i int = 0
	_ = i
	for i < exp {
		result = (result * base)
		i = (i + 1)
	}
	return result
}

func digital_sum(n int) int {
	var s string = fmt.Sprint(n)
	_ = s
	var sum int = 0
	_ = sum
	var i int = 0
	_ = i
	for i < len(s) {
		sum = (sum + func() int { n, _ := strconv.Atoi(fmt.Sprint(string(_index([]rune(s), i)))); return n }())
		i = (i + 1)
	}
	return sum
}

func solution(a int, b int) int {
	var max_sum int = 0
	_ = max_sum
	var base int = 0
	_ = base
	for base < a {
		var power int = 0
		_ = power
		for power < b {
			var value int = pow_int(base, power)
			_ = value
			var ds int = digital_sum(value)
			_ = ds
			if ds > max_sum {
				max_sum = ds
			}
			power = (power + 1)
		}
		base = (base + 1)
	}
	return max_sum
}

func test_solution() {
	if solution(10, 10) != 45 {
		panic("solution 10 10 failed")
	}
	if solution(100, 100) != 972 {
		panic("solution 100 100 failed")
	}
	if solution(100, 200) != 1872 {
		panic("solution 100 200 failed")
	}
}

func mochiMain() {
	test_solution()
	fmt.Println(fmt.Sprint(solution(100, 100)))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
