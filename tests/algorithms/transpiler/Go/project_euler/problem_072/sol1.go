//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-22 23:15:17 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

func totients(limit int) []int {
	var is_prime []bool = []bool{}
	_ = is_prime
	var phi []int = []int{}
	_ = phi
	var primes []int = []int{}
	_ = primes
	var i int = 0
	_ = i
	for i <= limit {
		is_prime = append(is_prime, true)
		phi = append(phi, (i - 1))
		i = (i + 1)
	}
	i = 2
	for i <= limit {
		if _index(is_prime, i) {
			primes = append(primes, i)
		}
		var j int = 0
		_ = j
		for j < len(primes) {
			var p int = _index(primes, j)
			_ = p
			if (i * p) > limit {
				break
			}
			_setIndex(is_prime, (i * p), false)
			if _modi(i, p) == 0 {
				_setIndex(phi, (i * p), (_index(phi, i) * p))
				break
			}
			_setIndex(phi, (i * p), (_index(phi, i) * (p - 1)))
			j = (j + 1)
		}
		i = (i + 1)
	}
	return phi
}

func solution(limit int) int {
	var phi []int = totients(limit)
	_ = phi
	var total int = 0
	_ = total
	var k int = 2
	_ = k
	for k <= limit {
		total = (total + _index(phi, k))
		k = (k + 1)
	}
	return total
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(solution(1000000)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
