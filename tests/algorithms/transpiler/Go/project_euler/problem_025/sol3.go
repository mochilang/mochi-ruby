//go:build ignore

// Generated by Mochi v0.10.71 on 2025-08-23 15:35:40 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

var __name__ string = "__main__"

func num_digits(x int) int {
	var count int = 0
	_ = count
	var n int = x
	_ = n
	for n > 0 {
		count = (count + 1)
		n = (n / 10)
	}
	return count
}

func solution(n int) int {
	var f1 int = 1
	_ = f1
	var f2 int = 1
	_ = f2
	var index int = 2
	_ = index
	for {
		var f int = (f1 + f2)
		_ = f
		f1 = f2
		f2 = f
		index = (index + 1)
		if num_digits(f) == n {
			break
		}
	}
	return index
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(("solution(1000) = " + fmt.Sprint(solution(1000))))
		fmt.Println(("solution(100) = " + fmt.Sprint(solution(100))))
		fmt.Println(("solution(50) = " + fmt.Sprint(solution(50))))
		fmt.Println(("solution(3) = " + fmt.Sprint(solution(3))))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
