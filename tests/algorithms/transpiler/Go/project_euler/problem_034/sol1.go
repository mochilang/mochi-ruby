//go:build ignore

// Generated by Mochi v0.10.73 on 2025-08-26 00:00:02 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

var __name__ string = "__main__"

var DIGIT_FACTORIALS []int

func sum_of_digit_factorial(n int) int {
	if n == 0 {
		return _index(DIGIT_FACTORIALS, 0)
	}
	var total int = 0
	_ = total
	var num int = n
	_ = num
	for num > 0 {
		var digit int = _modi(num, 10)
		_ = digit
		total = (total + _index(DIGIT_FACTORIALS, digit))
		num = func() int {
			a := num
			b := 10
			q := a / b
			r := a % b
			if (r != 0) && ((r < 0) != (b < 0)) {
				q--
			}
			return q
		}()
	}
	return total
}

func solution() int {
	var limit int = ((7 * _index(DIGIT_FACTORIALS, 9)) + 1)
	_ = limit
	var total int = 0
	_ = total
	var i int = 3
	_ = i
	for i < limit {
		if sum_of_digit_factorial(i) == i {
			total = (total + i)
		}
		i = (i + 1)
	}
	return total
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		DIGIT_FACTORIALS = []int{1, 1, 2, 6, 24, 120, 720, 5040, 40320, 362880}
		fmt.Println(fmt.Sprint(solution()))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
