//go:build ignore

// Generated by Mochi v0.10.71 on 2025-08-23 15:34:50 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

var __name__ string = "__main__"

func factorial(n int) int {
	var result int = 1
	_ = result
	var i int = 2
	_ = i
	for i <= n {
		result = (result * i)
		i = (i + 1)
	}
	return result
}

func digit_sum(n int) int {
	var s string = fmt.Sprint(n)
	_ = s
	var total int = 0
	_ = total
	var i int = 0
	_ = i
	for i < len(s) {
		var digit_str string = _substr(s, i, (i + 1))
		_ = digit_str
		_ = digit_str
		total = (total + func() int { n, _ := strconv.Atoi(fmt.Sprint(digit_str)); return n }())
		i = (i + 1)
	}
	return total
}

func solution(num int) int {
	return digit_sum(factorial(num))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(solution(100)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
