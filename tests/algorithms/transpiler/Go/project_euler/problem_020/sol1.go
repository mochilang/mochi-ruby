//go:build ignore

// Generated by Mochi v0.10.71 on 2025-08-23 15:34:46 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

var __name__ string = "__main__"

func factorial(num int) int {
	var res int = 1
	_ = res
	var i int = 2
	_ = i
	for i <= num {
		res = (res * i)
		i = (i + 1)
	}
	return res
}

func split_and_add(number int) int {
	var sum int = 0
	_ = sum
	var n int = number
	_ = n
	for n > 0 {
		var last int = _modi(n, 10)
		_ = last
		sum = (sum + last)
		n = (n / 10)
	}
	return sum
}

func solution(num int) int {
	var nfact int = factorial(num)
	_ = nfact
	_ = nfact
	return split_and_add(nfact)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(solution(100)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
