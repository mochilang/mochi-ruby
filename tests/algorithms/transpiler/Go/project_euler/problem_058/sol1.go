//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-22 15:34:08 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

func is_prime(number int) bool {
	if (1 < number) && (number < 4) {
		return true
	}
	if ((number < 2) || (_modi(number, 2) == 0)) || (_modi(number, 3) == 0) {
		return false
	}
	var i int = 5
	_ = i
	for (i * i) <= number {
		if (_modi(number, i) == 0) || (_modi(number, (i+2)) == 0) {
			return false
		}
		i = (i + 6)
	}
	return true
}

func solution(ratio float64) int {
	var j int = 3
	_ = j
	var primes int = 3
	_ = primes
	for (float64(primes) / float64(((2 * j) - 1))) >= ratio {
		var i int = (((j * j) + j) + 1)
		_ = i
		var limit int = ((j + 2) * (j + 2))
		_ = limit
		var step int = (j + 1)
		_ = step
		for i < limit {
			if is_prime(i) {
				primes = (primes + 1)
			}
			i = (i + step)
		}
		j = (j + 2)
	}
	return j
}

func test_solution() {
	if solution(0.5) != 11 {
		panic("solution 0.5 failed")
	}
	if solution(0.2) != 309 {
		panic("solution 0.2 failed")
	}
	if solution(0.111) != 11317 {
		panic("solution 0.111 failed")
	}
}

func mochiMain() {
	test_solution()
	fmt.Println(fmt.Sprint(solution(0.1)))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
