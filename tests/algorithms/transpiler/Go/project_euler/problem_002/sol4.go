//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 15:02:23 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

var __name__ string = "__main__"

func solution(n int) int {
	if n <= 0 {
		panic("Parameter n must be greater than or equal to one.")
	}
	var a int = 1
	_ = a
	var b int = 2
	_ = b
	var total int = 0
	_ = total
	for a <= n {
		if _modi(a, 2) == 0 {
			total = (total + a)
		}
		var c int = (a + b)
		_ = c
		a = b
		b = c
	}
	return total
}

func mochiMain() {
	fmt.Println(("solution() = " + fmt.Sprint(solution(4000000))))
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
