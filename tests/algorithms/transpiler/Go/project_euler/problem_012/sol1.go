//go:build ignore

// Generated by Mochi v0.10.71 on 2025-08-23 15:34:15 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

var __name__ string = "__main__"

func count_divisors(n int) int {
	var m int = n
	_ = m
	var n_divisors int = 1
	_ = n_divisors
	var i int = 2
	_ = i
	for (i * i) <= m {
		var multiplicity int = 0
		_ = multiplicity
		for _modi(m, i) == 0 {
			m = (m / i)
			multiplicity = (multiplicity + 1)
		}
		n_divisors = (n_divisors * (multiplicity + 1))
		i = (i + 1)
	}
	if m > 1 {
		n_divisors = (n_divisors * 2)
	}
	return n_divisors
}

func solution() int {
	var t_num int = 1
	_ = t_num
	var i int = 1
	_ = i
	for {
		i = (i + 1)
		t_num = (t_num + i)
		if count_divisors(t_num) > 500 {
			break
		}
	}
	return t_num
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(solution()))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
