//go:build ignore

// Generated by Mochi v0.10.70 on 2025-08-23 15:02:38 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var nowSeed int64 = 1

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
		}
	}
}
func _now() int {
	nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
	return int(nowSeed)
}

func _modi(a, b int) int {
	if b == 0 {
		return 0
	}
	r := a % b
	if (r < 0 && b > 0) || (r > 0 && b < 0) {
		r += b
	}
	return r
}

var __name__ string = "__main__"

func is_palindrome(num int) bool {
	if num < 0 {
		return false
	}
	var n int = num
	_ = n
	var rev int = 0
	_ = rev
	for n > 0 {
		rev = ((rev * 10) + _modi(n, 10))
		n = (n / 10)
	}
	return (rev == num)
}

func solution(limit int) int {
	var answer int = 0
	_ = answer
	var i int = 999
	_ = i
	for i >= 100 {
		var j int = 999
		_ = j
		for j >= 100 {
			var product int = (i * j)
			_ = product
			if ((product < limit) && is_palindrome(product)) && (product > answer) {
				answer = product
			}
			j = (j - 1)
		}
		i = (i - 1)
	}
	return answer
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(solution(998001)))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
