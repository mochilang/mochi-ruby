//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-15 06:48:13 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

type MatrixChainResult struct {
	Matrix   [][]int `json:"matrix"`
	Solution [][]int `json:"solution"`
}

func make_2d(n int) [][]int {
	var res [][]int = [][]int{}
	_ = res
	var i int = 0
	_ = i
	for i < n {
		var row []int = []int{}
		_ = row
		var j int = 0
		_ = j
		for j < n {
			row = append(row, 0)
			j = (j + 1)
		}
		res = append(res, row)
		i = (i + 1)
	}
	return res
}

func matrix_chain_order(arr []int) MatrixChainResult {
	var n int = len(arr)
	_ = n
	var m [][]int = make_2d(n)
	_ = m
	var s [][]int = make_2d(n)
	_ = s
	var chain_length int = 2
	_ = chain_length
	for chain_length < n {
		var a int = 1
		_ = a
		for a < ((n - chain_length) + 1) {
			var b int = ((a + chain_length) - 1)
			_ = b
			_setIndex(_index(m, a), b, 1000000000)
			var c int = a
			_ = c
			for c < b {
				var cost int = ((_index(_index(m, a), c) + _index(_index(m, (c+1)), b)) + ((_index(arr, (a-1)) * _index(arr, c)) * _index(arr, b)))
				_ = cost
				if cost < _index(_index(m, a), b) {
					_setIndex(_index(m, a), b, cost)
					_setIndex(_index(s, a), b, c)
				}
				c = (c + 1)
			}
			a = (a + 1)
		}
		chain_length = (chain_length + 1)
	}
	return MatrixChainResult{
		Matrix:   m,
		Solution: s,
	}
}

func optimal_parenthesization(s [][]int, i int, j int) string {
	if i == j {
		return ("A" + fmt.Sprint(i))
	} else {
		var left string = optimal_parenthesization(func(v any) [][]int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([][]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return [][]int{}
				}
				out := make([][]int, len(arr))
				for i, x := range arr {
					out[i] = func(v any) []int {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]int); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []int{}
							}
							out := make([]int, len(arr))
							for i, x := range arr {
								out[i] = x.(int)
							}
							return out
						}
						return v.([]int)
					}(x)
				}
				return out
			}
			return v.([][]int)
		}(s), i, _index(_index(s, i), j))
		_ = left
		_ = left
		var right string = optimal_parenthesization(func(v any) [][]int {
			if v == nil {
				return nil
			}
			if vv, ok := v.([][]int); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return [][]int{}
				}
				out := make([][]int, len(arr))
				for i, x := range arr {
					out[i] = func(v any) []int {
						if v == nil {
							return nil
						}
						if vv, ok := v.([]int); ok {
							return vv
						}
						if arr, ok := v.([]any); ok {
							if len(arr) == 0 {
								return []int{}
							}
							out := make([]int, len(arr))
							for i, x := range arr {
								out[i] = x.(int)
							}
							return out
						}
						return v.([]int)
					}(x)
				}
				return out
			}
			return v.([][]int)
		}(s), (_index(_index(s, i), j) + 1), j)
		_ = right
		_ = right
		return (((("( " + left) + " ") + right) + " )")
	}
}

func mochiMain() {
	var arr []int = []int{30, 35, 15, 5, 10, 20, 25}
	_ = arr
	var n int = len(arr)
	_ = n
	var res MatrixChainResult = matrix_chain_order(func(v any) []int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []int{}
			}
			out := make([]int, len(arr))
			for i, x := range arr {
				out[i] = x.(int)
			}
			return out
		}
		return v.([]int)
	}(arr))
	_ = res
	var m [][]int = res.Matrix
	_ = m
	var s [][]int = res.Solution
	_ = s
	fmt.Println(("No. of Operation required: " + fmt.Sprint(_index(_index(m, 1), (n-1)))))
	var seq string = optimal_parenthesization(func(v any) [][]int {
		if v == nil {
			return nil
		}
		if vv, ok := v.([][]int); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return [][]int{}
			}
			out := make([][]int, len(arr))
			for i, x := range arr {
				out[i] = func(v any) []int {
					if v == nil {
						return nil
					}
					if vv, ok := v.([]int); ok {
						return vv
					}
					if arr, ok := v.([]any); ok {
						if len(arr) == 0 {
							return []int{}
						}
						out := make([]int, len(arr))
						for i, x := range arr {
							out[i] = x.(int)
						}
						return out
					}
					return v.([]int)
				}(x)
			}
			return out
		}
		return v.([][]int)
	}(s), 1, (n - 1))
	_ = seq
	fmt.Println(seq)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
