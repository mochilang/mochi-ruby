//go:build ignore

// Generated by Mochi v0.10.66 on 2025-08-15 06:48:09 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func _index[T any](s []T, i any) T {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		var zero T
		return zero
	}
	return s[idx]
}

func _setIndex[T any](s []T, i any, v T) {
	idx := func(v any) int {
		switch vv := v.(type) {
		case int:
			return vv
		case int64:
			return int(vv)
		case float64:
			return int(vv)
		case float32:
			return int(vv)
		default:
			return v.(int)
		}
	}(i)
	if idx < 0 {
		idx += len(s)
	}
	if idx < 0 || idx >= len(s) {
		return
	}
	s[idx] = v
}

func reverse(s string) string {
	var result string = ""
	_ = result
	var i int = (len(s) - 1)
	_ = i
	for i >= 0 {
		result = (result + string([]rune(s)[i:(i+1)]))
		i = (i - 1)
	}
	return result
}

func max_int(a int, b int) int {
	if a > b {
		return a
	}
	return b
}

func longest_palindromic_subsequence(s string) int {
	var rev string = reverse(s)
	_ = rev
	var n int = len(s)
	_ = n
	var m int = len(rev)
	_ = m
	var dp [][]int = [][]int{}
	_ = dp
	var i int = 0
	_ = i
	for i <= n {
		var row []int = []int{}
		_ = row
		var j int = 0
		_ = j
		for j <= m {
			row = append(row, 0)
			j = (j + 1)
		}
		dp = append(dp, row)
		i = (i + 1)
	}
	i = 1
	for i <= n {
		var j int = 1
		_ = j
		for j <= m {
			var a_char string = string([]rune(s)[(i - 1):i])
			_ = a_char
			var b_char string = string([]rune(rev)[(j - 1):j])
			_ = b_char
			if a_char == b_char {
				_setIndex(_index(dp, i), j, (1 + _index(_index(dp, (i-1)), (j-1))))
			} else {
				_setIndex(_index(dp, i), j, max_int(_index(_index(dp, (i-1)), j), _index(_index(dp, i), (j-1))))
			}
			j = (j + 1)
		}
		i = (i + 1)
	}
	return _index(_index(dp, n), m)
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(longest_palindromic_subsequence("bbbab")))
		fmt.Println(fmt.Sprint(longest_palindromic_subsequence("bbabcbcab")))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
