// Generated by Mochi transpiler v0.10.67 on 2025-08-17 13:50:15 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(6) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::vector<int64_t> bubble_sort(const std::vector<int64_t>& nums);
std::vector<int64_t> sort3(const std::vector<int64_t>& xs);
std::vector<int64_t> triplet_sum1(const std::vector<int64_t>& arr, int64_t target);
std::vector<int64_t> triplet_sum2(const std::vector<int64_t>& arr, int64_t target);
bool list_equal(const std::vector<int64_t>& a, const std::vector<int64_t>& b);
void test_triplet_sum();
int main();

std::vector<int64_t> bubble_sort(const std::vector<int64_t>& nums) {
    std::vector<int64_t> arr = {};
    int64_t i = int64_t(0);
    while ((i < nums.size())) {
        arr = ([&]{ auto __tmp = arr; __tmp.push_back(_index(nums, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t n = arr.size();
    int64_t a = int64_t(0);
    while ((a < n)) {
        int64_t b = int64_t(0);
        while ((b < ((n - a) - int64_t(1)))) {
            if ((_index(arr, b) > _index(arr, (b + int64_t(1))))) {
                int64_t tmp = _index(arr, b);
                arr[static_cast<size_t>(b)] = _index(arr, (b + int64_t(1)));
                arr[static_cast<size_t>((b + int64_t(1)))] = tmp;
            }
            b = (b + int64_t(1));
        }
        a = (a + int64_t(1));
    }
    return arr;
}

std::vector<int64_t> sort3(const std::vector<int64_t>& xs) {
    std::vector<int64_t> arr = {};
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        arr = ([&]{ auto __tmp = arr; __tmp.push_back(_index(xs, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t n = arr.size();
    int64_t a = int64_t(0);
    while ((a < n)) {
        int64_t b = int64_t(0);
        while ((b < ((n - a) - int64_t(1)))) {
            if ((_index(arr, b) > _index(arr, (b + int64_t(1))))) {
                int64_t tmp = _index(arr, b);
                arr[static_cast<size_t>(b)] = _index(arr, (b + int64_t(1)));
                arr[static_cast<size_t>((b + int64_t(1)))] = tmp;
            }
            b = (b + int64_t(1));
        }
        a = (a + int64_t(1));
    }
    return arr;
}

std::vector<int64_t> triplet_sum1(const std::vector<int64_t>& arr, int64_t target) {
    int64_t i = int64_t(0);
    while ((i < (arr.size() - int64_t(2)))) {
        int64_t j = (i + int64_t(1));
        while ((j < (arr.size() - int64_t(1)))) {
            int64_t k = (j + int64_t(1));
            while ((k < arr.size())) {
                if ((((_index(arr, i) + _index(arr, j)) + _index(arr, k)) == target)) {
                    return sort3(std::vector<int64_t>{_index(arr, i), _index(arr, j), _index(arr, k)});
                }
                k = (k + int64_t(1));
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(0)};
}

std::vector<int64_t> triplet_sum2(const std::vector<int64_t>& arr, int64_t target) {
    std::vector<int64_t> sorted = bubble_sort(arr);
    int64_t n = sorted.size();
    int64_t i = int64_t(0);
    while ((i < (n - int64_t(2)))) {
        int64_t left = (i + int64_t(1));
        int64_t right = (n - int64_t(1));
        while ((left < right)) {
            int64_t s = ((_index(sorted, i) + _index(sorted, left)) + _index(sorted, right));
            if ((s == target)) {
                return std::vector<int64_t>{_index(sorted, i), _index(sorted, left), _index(sorted, right)};
            }
            if ((s < target)) {
                left = (left + int64_t(1));
            } else {
                right = (right - int64_t(1));
            }
        }
        i = (i + int64_t(1));
    }
    return std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(0)};
}

bool list_equal(const std::vector<int64_t>& a, const std::vector<int64_t>& b) {
    if ((a.size() != b.size())) {
        return false;
    }
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        if ((_index(a, i) != _index(b, i))) {
            return false;
        }
        i = (i + int64_t(1));
    }
    return true;
}

void test_triplet_sum() {
    std::vector<int64_t> arr1 = std::vector<int64_t>{int64_t(13), int64_t(29), int64_t(7), int64_t(23), int64_t(5)};
    if (!list_equal(triplet_sum1(arr1, int64_t(35)), std::vector<int64_t>{int64_t(5), int64_t(7), int64_t(23)})) {
        throw std::runtime_error(std::string("ts1 case1 failed"));
    }
    if (!list_equal(triplet_sum2(arr1, int64_t(35)), std::vector<int64_t>{int64_t(5), int64_t(7), int64_t(23)})) {
        throw std::runtime_error(std::string("ts2 case1 failed"));
    }
    std::vector<int64_t> arr2 = std::vector<int64_t>{int64_t(37), int64_t(9), int64_t(19), int64_t(50), int64_t(44)};
    if (!list_equal(triplet_sum1(arr2, int64_t(65)), std::vector<int64_t>{int64_t(9), int64_t(19), int64_t(37)})) {
        throw std::runtime_error(std::string("ts1 case2 failed"));
    }
    if (!list_equal(triplet_sum2(arr2, int64_t(65)), std::vector<int64_t>{int64_t(9), int64_t(19), int64_t(37)})) {
        throw std::runtime_error(std::string("ts2 case2 failed"));
    }
    std::vector<int64_t> arr3 = std::vector<int64_t>{int64_t(6), int64_t(47), int64_t(27), int64_t(1), int64_t(15)};
    if (!list_equal(triplet_sum1(arr3, int64_t(11)), std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(0)})) {
        throw std::runtime_error(std::string("ts1 case3 failed"));
    }
    if (!list_equal(triplet_sum2(arr3, int64_t(11)), std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(0)})) {
        throw std::runtime_error(std::string("ts2 case3 failed"));
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        test_triplet_sum();
        std::vector<int64_t> sample = std::vector<int64_t>{int64_t(13), int64_t(29), int64_t(7), int64_t(23), int64_t(5)};
        std::vector<int64_t> res = triplet_sum2(sample, int64_t(35));
        std::cout << ((((([&]{ std::ostringstream ss; ss << std::boolalpha << _index(res, int64_t(0)); return ss.str(); }()) + std::string(" ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << _index(res, int64_t(1)); return ss.str(); }())) + std::string(" ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << _index(res, int64_t(2)); return ss.str(); }()));
        std::cout << std::endl;
    }
    return 0;
}
