// Generated by Mochi transpiler v0.10.66 on 2025-08-16 13:06:49 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

double _floor(double x);
double pow10(int64_t n);
double round(double x, int64_t n);
std::vector<std::vector<double>> clone_matrix(const std::vector<std::vector<double>>& mat);
std::vector<double> solve_simultaneous(const std::vector<std::vector<double>>& equations);
void test_solver();
int main();

double _floor(double x) {
    int64_t i = (int64_t)(x);
    if (((double)(i) > x)) {
        i = (i - int64_t(1));
    }
    return (double)(i);
}

double pow10(int64_t n) {
    double p = 1.0;
    int64_t i = int64_t(0);
    while ((i < n)) {
        p = (p * 10.0);
        i = (i + int64_t(1));
    }
    return p;
}

double round(double x, int64_t n) {
    double m = pow10(n);
    return ((double)(std::floor(((x * m) + 0.5))) / (m));
}

std::vector<std::vector<double>> clone_matrix(const std::vector<std::vector<double>>& mat) {
    std::vector<std::vector<double>> new_mat = {};
    int64_t i = int64_t(0);
    while ((i < mat.size())) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < _index(mat, i).size())) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(_index(_index(mat, i), j)); return __tmp; }());
            j = (j + int64_t(1));
        }
        new_mat = ([&]{ auto __tmp = new_mat; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return new_mat;
}

std::vector<double> solve_simultaneous(const std::vector<std::vector<double>>& equations) {
    int64_t n = equations.size();
    if ((n == int64_t(0))) {
        throw std::runtime_error(std::string("solve_simultaneous() requires n lists of length n+1"));
    }
    int64_t m = (n + int64_t(1));
    int64_t i = int64_t(0);
    while ((i < n)) {
        if ((_index(equations, i).size() != m)) {
            throw std::runtime_error(std::string("solve_simultaneous() requires n lists of length n+1"));
        }
        i = (i + int64_t(1));
    }
    std::vector<std::vector<double>> a = clone_matrix(equations);
    int64_t row = int64_t(0);
    while ((row < n)) {
        int64_t pivot = row;
        while (((pivot < n) && (_index(_index(a, pivot), row) == 0.0))) {
            pivot = (pivot + int64_t(1));
        }
        if ((pivot == n)) {
            throw std::runtime_error(std::string("solve_simultaneous() requires at least 1 full equation"));
        }
        if ((pivot != row)) {
            std::vector<double> temp = _index(a, row);
            a[static_cast<size_t>(row)] = _index(a, pivot);
            a[static_cast<size_t>(pivot)] = temp;
        }
        double pivot_val = _index(_index(a, row), row);
        int64_t col = int64_t(0);
        while ((col < m)) {
            a[static_cast<size_t>(row)][static_cast<size_t>(col)] = ((double)(_index(_index(a, row), col)) / (pivot_val));
            col = (col + int64_t(1));
        }
        int64_t r = int64_t(0);
        while ((r < n)) {
            if ((r != row)) {
                double factor = _index(_index(a, r), row);
                int64_t c = int64_t(0);
                while ((c < m)) {
                    a[static_cast<size_t>(r)][static_cast<size_t>(c)] = (_index(_index(a, r), c) - (factor * _index(_index(a, row), c)));
                    c = (c + int64_t(1));
                }
            }
            r = (r + int64_t(1));
        }
        row = (row + int64_t(1));
    }
    std::vector<double> res = {};
    int64_t k = int64_t(0);
    while ((k < n)) {
        res = ([&]{ auto __tmp = res; __tmp.push_back(round(_index(_index(a, k), (m - int64_t(1))), int64_t(5))); return __tmp; }());
        k = (k + int64_t(1));
    }
    return res;
}

void test_solver() {
    std::vector<std::vector<double>> a = std::vector<std::vector<double>>{std::vector<double>{1.0, 2.0, 3.0}, std::vector<double>{4.0, 5.0, 6.0}};
    std::vector<double> r1 = solve_simultaneous(a);
    if (!(((r1.size() == int64_t(2)) && (_index(r1, int64_t(0)) == (0.0 - 1.0))) && (_index(r1, int64_t(1)) == 2.0))) {
        throw std::runtime_error(std::string("test1 failed"));
    }
    std::vector<std::vector<double>> b = std::vector<std::vector<double>>{std::vector<double>{0.0, (0.0 - 3.0), 1.0, 7.0}, std::vector<double>{3.0, 2.0, (0.0 - 1.0), 11.0}, std::vector<double>{5.0, 1.0, (0.0 - 2.0), 12.0}};
    std::vector<double> r2 = solve_simultaneous(b);
    if (!((((r2.size() == int64_t(3)) && (_index(r2, int64_t(0)) == 6.4)) && (_index(r2, int64_t(1)) == 1.2)) && (_index(r2, int64_t(2)) == 10.6))) {
        throw std::runtime_error(std::string("test2 failed"));
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        test_solver();
        std::vector<std::vector<double>> eq = std::vector<std::vector<double>>{std::vector<double>{2.0, 1.0, 1.0, 1.0, 1.0, 4.0}, std::vector<double>{1.0, 2.0, 1.0, 1.0, 1.0, 5.0}, std::vector<double>{1.0, 1.0, 2.0, 1.0, 1.0, 6.0}, std::vector<double>{1.0, 1.0, 1.0, 2.0, 1.0, 7.0}, std::vector<double>{1.0, 1.0, 1.0, 1.0, 2.0, 8.0}};
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp1 = solve_simultaneous(eq); ss << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) ss << ' '; ss << __tmp1[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp2 = solve_simultaneous(std::vector<std::vector<double>>{std::vector<double>{4.0, 2.0}}); ss << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) ss << ' '; ss << __tmp2[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
