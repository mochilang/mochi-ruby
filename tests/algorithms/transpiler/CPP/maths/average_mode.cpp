// Generated by Mochi transpiler v0.10.67 on 2025-08-17 00:51:07 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

bool contains_int(const std::vector<int64_t>& xs, int64_t x);
bool contains_string(const std::vector<std::string>& xs, std::string x);
int64_t count_int(const std::vector<int64_t>& xs, int64_t x);
int64_t count_string(const std::vector<std::string>& xs, std::string x);
std::vector<int64_t> sort_int(const std::vector<int64_t>& xs);
std::vector<std::string> sort_string(const std::vector<std::string>& xs);
std::vector<int64_t> mode_int(const std::vector<int64_t>& lst);
std::vector<std::string> mode_string(const std::vector<std::string>& lst);
int main();

bool contains_int(const std::vector<int64_t>& xs, int64_t x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == x)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

bool contains_string(const std::vector<std::string>& xs, std::string x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == x)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

int64_t count_int(const std::vector<int64_t>& xs, int64_t x) {
    int64_t cnt = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == x)) {
            cnt = (cnt + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return cnt;
}

int64_t count_string(const std::vector<std::string>& xs, std::string x) {
    int64_t cnt = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == x)) {
            cnt = (cnt + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return cnt;
}

std::vector<int64_t> sort_int(const std::vector<int64_t>& xs) {
    std::vector<int64_t> arr = xs;
    int64_t i = int64_t(0);
    while ((i < arr.size())) {
        int64_t j = (i + int64_t(1));
        while ((j < arr.size())) {
            if ((_index(arr, j) < _index(arr, i))) {
                int64_t tmp = _index(arr, i);
                arr[static_cast<size_t>(i)] = _index(arr, j);
                arr[static_cast<size_t>(j)] = tmp;
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return arr;
}

std::vector<std::string> sort_string(const std::vector<std::string>& xs) {
    std::vector<std::string> arr = xs;
    int64_t i = int64_t(0);
    while ((i < arr.size())) {
        int64_t j = (i + int64_t(1));
        while ((j < arr.size())) {
            if ((_index(arr, j) < _index(arr, i))) {
                std::string tmp = _index(arr, i);
                arr[static_cast<size_t>(i)] = _index(arr, j);
                arr[static_cast<size_t>(j)] = tmp;
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return arr;
}

std::vector<int64_t> mode_int(const std::vector<int64_t>& lst) {
    if ((lst.size() == int64_t(0))) {
        return std::vector<int64_t>{};
    }
    std::vector<int64_t> counts = {};
    int64_t i = int64_t(0);
    while ((i < lst.size())) {
        counts = ([&]{ auto __tmp = counts; __tmp.push_back(count_int(lst, _index(lst, i))); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t max_count = int64_t(0);
    i = int64_t(0);
    while ((i < counts.size())) {
        if ((_index(counts, i) > max_count)) {
            max_count = _index(counts, i);
        }
        i = (i + int64_t(1));
    }
    std::vector<int64_t> modes = {};
    i = int64_t(0);
    while ((i < lst.size())) {
        if ((_index(counts, i) == max_count)) {
            int64_t v = _index(lst, i);
            if (!contains_int(modes, v)) {
                modes = ([&]{ auto __tmp = modes; __tmp.push_back(v); return __tmp; }());
            }
        }
        i = (i + int64_t(1));
    }
    return sort_int(modes);
}

std::vector<std::string> mode_string(const std::vector<std::string>& lst) {
    if ((lst.size() == int64_t(0))) {
        return std::vector<std::string>{};
    }
    std::vector<int64_t> counts = {};
    int64_t i = int64_t(0);
    while ((i < lst.size())) {
        counts = ([&]{ auto __tmp = counts; __tmp.push_back(count_string(lst, _index(lst, i))); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t max_count = int64_t(0);
    i = int64_t(0);
    while ((i < counts.size())) {
        if ((_index(counts, i) > max_count)) {
            max_count = _index(counts, i);
        }
        i = (i + int64_t(1));
    }
    std::vector<std::string> modes = {};
    i = int64_t(0);
    while ((i < lst.size())) {
        if ((_index(counts, i) == max_count)) {
            std::string v = _index(lst, i);
            if (!contains_string(modes, v)) {
                modes = ([&]{ auto __tmp = modes; __tmp.push_back(v); return __tmp; }());
            }
        }
        i = (i + int64_t(1));
    }
    return sort_string(modes);
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        { auto __tmp1 = mode_int(std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(4), int64_t(5), int64_t(3), int64_t(4), int64_t(2), int64_t(5), int64_t(2), int64_t(2), int64_t(4), int64_t(2), int64_t(2), int64_t(2)}); std::cout << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp1[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp2 = mode_int(std::vector<int64_t>{int64_t(3), int64_t(4), int64_t(5), int64_t(3), int64_t(4), int64_t(2), int64_t(5), int64_t(2), int64_t(2), int64_t(4), int64_t(4), int64_t(2), int64_t(2), int64_t(2)}); std::cout << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp2[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp3 = mode_int(std::vector<int64_t>{int64_t(3), int64_t(4), int64_t(5), int64_t(3), int64_t(4), int64_t(2), int64_t(5), int64_t(2), int64_t(2), int64_t(4), int64_t(4), int64_t(4), int64_t(2), int64_t(2), int64_t(4), int64_t(2)}); std::cout << "["; for(size_t i=0;i<__tmp3.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp3[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp4 = mode_string(std::vector<std::string>{std::string("x"), std::string("y"), std::string("y"), std::string("z")}); std::cout << "["; for(size_t i=0;i<__tmp4.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp4[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp5 = mode_string(std::vector<std::string>{std::string("x"), std::string("x"), std::string("y"), std::string("y"), std::string("z")}); std::cout << "["; for(size_t i=0;i<__tmp5.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp5[i]; } std::cout << "]"; }
        std::cout << std::endl;
    }
    return 0;
}
