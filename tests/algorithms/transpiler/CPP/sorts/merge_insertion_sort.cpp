// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:56:11 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::vector<int64_t> binary_search_insertion_from(const std::vector<int64_t>& sorted_list, int64_t item, int64_t start);
std::vector<int64_t> binary_search_insertion(const std::vector<int64_t>& sorted_list, int64_t item);
std::vector<std::vector<int64_t>> merge(const std::vector<std::vector<int64_t>>& left, const std::vector<std::vector<int64_t>>& right);
std::vector<std::vector<int64_t>> sortlist_2d(const std::vector<std::vector<int64_t>>& list_2d);
std::vector<int64_t> merge_insertion_sort(const std::vector<int64_t>& collection);
int main();

std::vector<int64_t> binary_search_insertion_from(const std::vector<int64_t>& sorted_list, int64_t item, int64_t start) {
    int64_t left = start;
    int64_t right = (sorted_list.size() - int64_t(1));
    while ((left <= right)) {
        int64_t middle = ((left + right) / int64_t(2));
        if ((left == right)) {
            if ((_index(sorted_list, middle) < item)) {
                left = (middle + int64_t(1));
            }
            break;
        } else         if ((_index(sorted_list, middle) < item)) {
            left = (middle + int64_t(1));
        } else {
            right = (middle - int64_t(1));
        }

    }
    std::vector<int64_t> result = {};
    int64_t i = int64_t(0);
    while ((i < left)) {
        result = ([&]{ auto __tmp = result; __tmp.push_back(_index(sorted_list, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    result = ([&]{ auto __tmp = result; __tmp.push_back(item); return __tmp; }());
    while ((i < sorted_list.size())) {
        result = ([&]{ auto __tmp = result; __tmp.push_back(_index(sorted_list, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    return result;
}

std::vector<int64_t> binary_search_insertion(const std::vector<int64_t>& sorted_list, int64_t item) {
    return binary_search_insertion_from(sorted_list, item, int64_t(0));
}

std::vector<std::vector<int64_t>> merge(const std::vector<std::vector<int64_t>>& left, const std::vector<std::vector<int64_t>>& right) {
    std::vector<std::vector<int64_t>> result = {};
    int64_t i = int64_t(0);
    int64_t j = int64_t(0);
    while (((i < left.size()) && (j < right.size()))) {
        if ((_index(_index(left, i), int64_t(0)) < _index(_index(right, j), int64_t(0)))) {
            result = ([&]{ auto __tmp = result; __tmp.push_back(_index(left, i)); return __tmp; }());
            i = (i + int64_t(1));
        } else {
            result = ([&]{ auto __tmp = result; __tmp.push_back(_index(right, j)); return __tmp; }());
            j = (j + int64_t(1));
        }
    }
    while ((i < left.size())) {
        result = ([&]{ auto __tmp = result; __tmp.push_back(_index(left, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    while ((j < right.size())) {
        result = ([&]{ auto __tmp = result; __tmp.push_back(_index(right, j)); return __tmp; }());
        j = (j + int64_t(1));
    }
    return result;
}

std::vector<std::vector<int64_t>> sortlist_2d(const std::vector<std::vector<int64_t>>& list_2d) {
    int64_t length = list_2d.size();
    if ((length <= int64_t(1))) {
        return list_2d;
    }
    int64_t middle = (length / int64_t(2));
    std::vector<std::vector<int64_t>> left = {};
    int64_t i = int64_t(0);
    while ((i < middle)) {
        left = ([&]{ auto __tmp = left; __tmp.push_back(_index(list_2d, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<std::vector<int64_t>> right = {};
    int64_t j = middle;
    while ((j < length)) {
        right = ([&]{ auto __tmp = right; __tmp.push_back(_index(list_2d, j)); return __tmp; }());
        j = (j + int64_t(1));
    }
    return merge(sortlist_2d(left), sortlist_2d(right));
}

std::vector<int64_t> merge_insertion_sort(const std::vector<int64_t>& collection) {
    if ((collection.size() <= int64_t(1))) {
        return collection;
    }
    std::vector<std::vector<int64_t>> two_paired_list = {};
    bool has_last_odd_item = false;
    int64_t i = int64_t(0);
    while ((i < collection.size())) {
        if ((i == (collection.size() - int64_t(1)))) {
            has_last_odd_item = true;
        } else {
            int64_t a = _index(collection, i);
            int64_t b = _index(collection, (i + int64_t(1)));
            if ((a < b)) {
                two_paired_list = ([&]{ auto __tmp = two_paired_list; __tmp.push_back(std::vector<int64_t>{a, b}); return __tmp; }());
            } else {
                two_paired_list = ([&]{ auto __tmp = two_paired_list; __tmp.push_back(std::vector<int64_t>{b, a}); return __tmp; }());
            }
        }
        i = (i + int64_t(2));
    }
    std::vector<std::vector<int64_t>> sorted_list_2d = sortlist_2d(two_paired_list);
    std::vector<int64_t> result = {};
    i = int64_t(0);
    while ((i < sorted_list_2d.size())) {
        result = ([&]{ auto __tmp = result; __tmp.push_back(_index(_index(sorted_list_2d, i), int64_t(0))); return __tmp; }());
        i = (i + int64_t(1));
    }
    result = ([&]{ auto __tmp = result; __tmp.push_back(_index(_index(sorted_list_2d, (sorted_list_2d.size() - int64_t(1))), int64_t(1))); return __tmp; }());
    if (has_last_odd_item) {
        result = binary_search_insertion(result, _index(collection, (collection.size() - int64_t(1))));
    }
    bool inserted_before = false;
    int64_t idx = int64_t(0);
    while ((idx < (sorted_list_2d.size() - int64_t(1)))) {
        if ((has_last_odd_item && (_index(result, idx) == _index(collection, (collection.size() - int64_t(1)))))) {
            inserted_before = true;
        }
        int64_t pivot = _index(_index(sorted_list_2d, idx), int64_t(1));
        if (inserted_before) {
            result = binary_search_insertion_from(result, pivot, (idx + int64_t(2)));
        } else {
            result = binary_search_insertion_from(result, pivot, (idx + int64_t(1)));
        }
        idx = (idx + int64_t(1));
    }
    return result;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::vector<int64_t> example1 = std::vector<int64_t>{int64_t(0), int64_t(5), int64_t(3), int64_t(2), int64_t(2)};
        std::vector<int64_t> example2 = std::vector<int64_t>{int64_t(99)};
        std::vector<int64_t> example3 = std::vector<int64_t>{-int64_t(2), -int64_t(5), -int64_t(45)};
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = merge_insertion_sort(example1); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = merge_insertion_sort(example2); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = merge_insertion_sort(example3); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
