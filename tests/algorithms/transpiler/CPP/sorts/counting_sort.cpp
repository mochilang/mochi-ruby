// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:27:59 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int64_t max_val(const std::vector<int64_t>& arr);
int64_t min_val(const std::vector<int64_t>& arr);
std::vector<int64_t> counting_sort(const std::vector<int64_t>& collection);
std::string chr(int64_t code);
int64_t ord(std::string ch);
std::string counting_sort_string(std::string s);
int main();

std::string ascii_chars = std::string(" !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~");
std::vector<int64_t> example1 = std::vector<int64_t>{};
std::vector<int64_t> example2 = std::vector<int64_t>{};
std::vector<int64_t> example3 = std::vector<int64_t>{};

int64_t max_val(const std::vector<int64_t>& arr) {
    int64_t m = _index(arr, int64_t(0));
    int64_t i = int64_t(1);
    while ((i < arr.size())) {
        if ((_index(arr, i) > m)) {
            m = _index(arr, i);
        }
        i = (i + int64_t(1));
    }
    return m;
}

int64_t min_val(const std::vector<int64_t>& arr) {
    int64_t m = _index(arr, int64_t(0));
    int64_t i = int64_t(1);
    while ((i < arr.size())) {
        if ((_index(arr, i) < m)) {
            m = _index(arr, i);
        }
        i = (i + int64_t(1));
    }
    return m;
}

std::vector<int64_t> counting_sort(const std::vector<int64_t>& collection) {
    if ((collection.size() == int64_t(0))) {
        return std::vector<int64_t>{};
    }
    int64_t coll_len = collection.size();
    int64_t coll_max = max_val(collection);
    int64_t coll_min = min_val(collection);
    int64_t counting_arr_length = ((coll_max + int64_t(1)) - coll_min);
    std::vector<int64_t> counting_arr = {};
    int64_t i = int64_t(0);
    while ((i < counting_arr_length)) {
        counting_arr = ([&]{ auto __tmp = counting_arr; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    i = int64_t(0);
    while ((i < coll_len)) {
        int64_t number = _index(collection, i);
        counting_arr[(number - coll_min)] = (_index(counting_arr, (number - coll_min)) + int64_t(1));
        i = (i + int64_t(1));
    }
    i = int64_t(1);
    while ((i < counting_arr_length)) {
        counting_arr[i] = (_index(counting_arr, i) + _index(counting_arr, (i - int64_t(1))));
        i = (i + int64_t(1));
    }
    std::vector<int64_t> ordered = {};
    i = int64_t(0);
    while ((i < coll_len)) {
        ordered = ([&]{ auto __tmp = ordered; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t idx = (coll_len - int64_t(1));
    while ((idx >= int64_t(0))) {
        int64_t number = _index(collection, idx);
        int64_t pos = (_index(counting_arr, (number - coll_min)) - int64_t(1));
        ordered[pos] = number;
        counting_arr[(number - coll_min)] = (_index(counting_arr, (number - coll_min)) - int64_t(1));
        idx = (idx - int64_t(1));
    }
    return ordered;
}

std::string chr(int64_t code) {
    if ((code == int64_t(10))) {
        return std::string("\n");
    }
    if ((code == int64_t(13))) {
        return std::string("\r");
    }
    if ((code == int64_t(9))) {
        return std::string("\t");
    }
    if (((code >= int64_t(32)) && (code < int64_t(127)))) {
        return ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr((code - int64_t(32)), (code - int64_t(31)) - (code - int64_t(32))); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+(code - int64_t(32)), __v.begin()+(code - int64_t(31))); })(ascii_chars);
    }
    return std::string("");
}

int64_t ord(std::string ch) {
    if ((ch == std::string("\n"))) {
        return int64_t(10);
    }
    if ((ch == std::string("\r"))) {
        return int64_t(13);
    }
    if ((ch == std::string("\t"))) {
        return int64_t(9);
    }
    int64_t i = int64_t(0);
    while ((i < ascii_chars.size())) {
        if ((([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(ascii_chars) == ch)) {
            return (int64_t(32) + i);
        }
        i = (i + int64_t(1));
    }
    return int64_t(0);
}

std::string counting_sort_string(std::string s) {
    std::vector<int64_t> codes = {};
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        codes = ([&]{ auto __tmp = codes; __tmp.push_back(ord(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(s))); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<int64_t> sorted_codes = counting_sort(codes);
    std::string res = std::string("");
    i = int64_t(0);
    while ((i < sorted_codes.size())) {
        res = (res + chr(_index(sorted_codes, i)));
        i = (i + int64_t(1));
    }
    return res;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        example1 = counting_sort(std::vector<int64_t>{int64_t(0), int64_t(5), int64_t(3), int64_t(2), int64_t(2)});
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = example1; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        example2 = counting_sort(std::vector<int64_t>{});
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = example2; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        example3 = counting_sort(std::vector<int64_t>{-int64_t(2), -int64_t(5), -int64_t(45)});
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = example3; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << counting_sort_string(std::string("thisisthestring"));
        std::cout << std::endl;
    }
    return 0;
}
