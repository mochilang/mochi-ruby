// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:27:45 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::vector<int64_t> bubble_sort_iterative(std::vector<int64_t>& collection);
std::vector<int64_t> bubble_sort_recursive(std::vector<int64_t>& collection);
std::vector<int64_t> copy_list(const std::vector<int64_t>& xs);
bool list_eq(const std::vector<int64_t>& a, const std::vector<int64_t>& b);
void test_bubble_sort();
int main();

std::vector<int64_t> bubble_sort_iterative(std::vector<int64_t>& collection) {
    int64_t n = collection.size();
    while ((n > int64_t(0))) {
        bool swapped = false;
        int64_t j = int64_t(0);
        while ((j < (n - int64_t(1)))) {
            if ((_index(collection, j) > _index(collection, (j + int64_t(1))))) {
                int64_t temp = _index(collection, j);
                collection[j] = _index(collection, (j + int64_t(1)));
                collection[(j + int64_t(1))] = temp;
                swapped = true;
            }
            j = (j + int64_t(1));
        }
        if (!swapped) {
            break;
        }
        n = (n - int64_t(1));
    }
    return collection;
}

std::vector<int64_t> bubble_sort_recursive(std::vector<int64_t>& collection) {
    int64_t n = collection.size();
    bool swapped = false;
    int64_t i = int64_t(0);
    while ((i < (n - int64_t(1)))) {
        if ((_index(collection, i) > _index(collection, (i + int64_t(1))))) {
            int64_t temp = _index(collection, i);
            collection[i] = _index(collection, (i + int64_t(1)));
            collection[(i + int64_t(1))] = temp;
            swapped = true;
        }
        i = (i + int64_t(1));
    }
    if (swapped) {
        return bubble_sort_recursive(collection);
    }
    return collection;
}

std::vector<int64_t> copy_list(const std::vector<int64_t>& xs) {
    std::vector<int64_t> out = {};
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        out = ([&]{ auto __tmp = out; __tmp.push_back(_index(xs, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    return out;
}

bool list_eq(const std::vector<int64_t>& a, const std::vector<int64_t>& b) {
    if ((a.size() != b.size())) {
        return false;
    }
    int64_t k = int64_t(0);
    while ((k < a.size())) {
        if ((_index(a, k) != _index(b, k))) {
            return false;
        }
        k = (k + int64_t(1));
    }
    return true;
}

void test_bubble_sort() {
    std::vector<int64_t> example = std::vector<int64_t>{int64_t(0), int64_t(5), int64_t(2), int64_t(3), int64_t(2)};
    std::vector<int64_t> expected = std::vector<int64_t>{int64_t(0), int64_t(2), int64_t(2), int64_t(3), int64_t(5)};
    if (!list_eq(([&]{ auto __arg0 = copy_list(example); return bubble_sort_iterative(__arg0); }()), expected)) {
        throw std::runtime_error(std::string("iterative failed"));
    }
    if (!list_eq(([&]{ auto __arg0 = copy_list(example); return bubble_sort_recursive(__arg0); }()), expected)) {
        throw std::runtime_error(std::string("recursive failed"));
    }
    std::vector<int64_t> empty = {};
    if ((([&]{ auto __arg0 = copy_list(empty); return bubble_sort_iterative(__arg0); }()).size() != int64_t(0))) {
        throw std::runtime_error(std::string("empty iterative failed"));
    }
    if ((([&]{ auto __arg0 = copy_list(empty); return bubble_sort_recursive(__arg0); }()).size() != int64_t(0))) {
        throw std::runtime_error(std::string("empty recursive failed"));
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        test_bubble_sort();
        std::vector<int64_t> arr = std::vector<int64_t>{int64_t(5), int64_t(1), int64_t(4), int64_t(2), int64_t(8)};
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = ([&]{ auto __arg0 = copy_list(arr); return bubble_sort_iterative(__arg0); }()); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = ([&]{ auto __arg0 = copy_list(arr); return bubble_sort_recursive(__arg0); }()); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
