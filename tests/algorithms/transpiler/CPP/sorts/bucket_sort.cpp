// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:27:48 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

double int_to_float(int64_t x);
int64_t floor_int(double x);
std::vector<double> set_at_float(const std::vector<double>& xs, int64_t idx, double value);
std::vector<std::vector<double>> set_at_list_float(const std::vector<std::vector<double>>& xs, int64_t idx, const std::vector<double>& value);
std::vector<double> sort_float(const std::vector<double>& xs);
std::vector<double> bucket_sort_with_count(const std::vector<double>& xs, int64_t bucket_count);
std::vector<double> bucket_sort(const std::vector<double>& xs);
int main();

double int_to_float(int64_t x) {
    return (x * 1.0);
}

int64_t floor_int(double x) {
    int64_t i = int64_t(0);
    while ((int_to_float((i + int64_t(1))) <= x)) {
        i = (i + int64_t(1));
    }
    return i;
}

std::vector<double> set_at_float(const std::vector<double>& xs, int64_t idx, double value) {
    int64_t i = int64_t(0);
    std::vector<double> res = {};
    while ((i < xs.size())) {
        if ((i == idx)) {
            res = ([&]{ auto __tmp = res; __tmp.push_back(value); return __tmp; }());
        } else {
            res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<std::vector<double>> set_at_list_float(const std::vector<std::vector<double>>& xs, int64_t idx, const std::vector<double>& value) {
    int64_t i = int64_t(0);
    std::vector<std::vector<double>> res = {};
    while ((i < xs.size())) {
        if ((i == idx)) {
            res = ([&]{ auto __tmp = res; __tmp.push_back(value); return __tmp; }());
        } else {
            res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<double> sort_float(const std::vector<double>& xs) {
    std::vector<double> res = xs;
    int64_t i = int64_t(1);
    while ((i < res.size())) {
        double key = _index(res, i);
        int64_t j = (i - int64_t(1));
        while (((j >= int64_t(0)) && (_index(res, j) > key))) {
            res = set_at_float(res, (j + int64_t(1)), _index(res, j));
            j = (j - int64_t(1));
        }
        res = set_at_float(res, (j + int64_t(1)), key);
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<double> bucket_sort_with_count(const std::vector<double>& xs, int64_t bucket_count) {
    if (((xs.size() == int64_t(0)) || (bucket_count <= int64_t(0)))) {
        return std::vector<double>{};
    }
    double min_value = _index(xs, int64_t(0));
    double max_value = _index(xs, int64_t(0));
    int64_t i = int64_t(1);
    while ((i < xs.size())) {
        if ((_index(xs, i) < min_value)) {
            min_value = _index(xs, i);
        }
        if ((_index(xs, i) > max_value)) {
            max_value = _index(xs, i);
        }
        i = (i + int64_t(1));
    }
    if ((max_value == min_value)) {
        return xs;
    }
    double bucket_size = ((double)((max_value - min_value)) / (int_to_float(bucket_count)));
    std::vector<std::vector<double>> buckets = {};
    i = int64_t(0);
    while ((i < bucket_count)) {
        buckets = ([&]{ auto __tmp = buckets; __tmp.push_back(std::vector<double>{}); return __tmp; }());
        i = (i + int64_t(1));
    }
    i = int64_t(0);
    while ((i < xs.size())) {
        double val = _index(xs, i);
        int64_t idx = floor_int(((double)((val - min_value)) / (bucket_size)));
        if ((idx < int64_t(0))) {
            idx = int64_t(0);
        }
        if ((idx >= bucket_count)) {
            idx = (bucket_count - int64_t(1));
        }
        std::vector<double> bucket = _index(buckets, idx);
        bucket = ([&]{ auto __tmp = bucket; __tmp.push_back(val); return __tmp; }());
        buckets = set_at_list_float(buckets, idx, bucket);
        i = (i + int64_t(1));
    }
    std::vector<double> result = {};
    i = int64_t(0);
    while ((i < buckets.size())) {
        std::vector<double> sorted_bucket = sort_float(_index(buckets, i));
        int64_t j = int64_t(0);
        while ((j < sorted_bucket.size())) {
            result = ([&]{ auto __tmp = result; __tmp.push_back(_index(sorted_bucket, j)); return __tmp; }());
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return result;
}

std::vector<double> bucket_sort(const std::vector<double>& xs) {
    return bucket_sort_with_count(xs, int64_t(10));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = bucket_sort(std::vector<double>{-1.0, 2.0, -5.0, 0.0}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = bucket_sort(std::vector<double>{9.0, 8.0, 7.0, 6.0, -12.0}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = bucket_sort(std::vector<double>{0.4, 1.2, 0.1, 0.2, -0.9}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = bucket_sort(std::vector<double>{}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = bucket_sort(std::vector<double>{-10000000000.0, 10000000000.0}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
