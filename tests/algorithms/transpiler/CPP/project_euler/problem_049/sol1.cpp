// Generated by Mochi transpiler v0.10.71 on 2025-08-23 15:56:16 GMT+7
#include <boost/multiprecision/cpp_int.hpp>
using cpp_int = boost::multiprecision::cpp_int;
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static std::string _cpp_int_to_string(cpp_int v) {
    if(v == 0) return "0";
    bool neg = v < 0;
    if(neg) v = -v;
    std::string s;
    while(v > 0){ int d = int(v % 10); s = char('0'+d) + s; v /= 10; }
    if(neg) s = '-' + s;
    return s;
}
inline std::ostream& operator<<(std::ostream& os, cpp_int v){ os << _cpp_int_to_string(v); return os; }
static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static cpp_int _parse_int_str(const std::string& s, long base) {
    if(s.empty()) return 0;
    cpp_int r = 0;
    bool neg = false; size_t i = 0;
    if(s[0]=='-'){ neg = true; i = 1; }
    for(; i < s.size(); ++i){
        char c = s[i];
        int digit = 0;
        if(c >= '0' && c <= '9') digit = c - '0';
        else if(c >= 'a' && c <= 'z') digit = c - 'a' + 10;
        else if(c >= 'A' && c <= 'Z') digit = c - 'A' + 10;
        else continue;
        r *= base;
        r += digit;
    }
    return neg ? -r : r;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(cpp_int)) os << _cpp_int_to_string(std::any_cast<cpp_int>(val));
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(cpp_int)) return (double)std::any_cast<cpp_int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(cpp_int)) return _cpp_int_to_string(std::any_cast<cpp_int>(v));
    if(v.type() == typeid(double)) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << std::any_cast<double>(v);
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
        return s + exp;
    }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, cpp_int>) {
        return _cpp_int_to_string(v);
    } else if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

bool is_prime(const cpp_int& number);
bool search(const cpp_int& target, const std::vector<cpp_int>& arr);
std::vector<cpp_int> sort_int(const std::vector<cpp_int>& xs);
std::vector<cpp_int> permutations_of_number(const cpp_int& n);
cpp_int abs_int(const cpp_int& x);
bool contains_int(const std::vector<cpp_int>& xs, const cpp_int& v);
cpp_int solution();
int main();

bool is_prime(const cpp_int& number) {
    if (((int64_t(1) < number) && (number < int64_t(4)))) {
        return true;
    }
    if ((((number < int64_t(2)) || ((number % int64_t(2)) == int64_t(0))) || ((number % int64_t(3)) == int64_t(0)))) {
        return false;
    }
    cpp_int i = int64_t(5);
    while (((i * i) <= number)) {
        if ((((number % i) == int64_t(0)) || ((number % (i + int64_t(2))) == int64_t(0)))) {
            return false;
        }
        i = (i + int64_t(6));
    }
    return true;
}

bool search(const cpp_int& target, const std::vector<cpp_int>& arr) {
    cpp_int left = int64_t(0);
    cpp_int right = (arr.size() - int64_t(1));
    while ((left <= right)) {
        cpp_int middle = ((left + right) / int64_t(2));
        if ((arr[static_cast<size_t>(middle)] == target)) {
            return true;
        }
        if ((arr[static_cast<size_t>(middle)] < target)) {
            left = (middle + int64_t(1));
        } else {
            right = (middle - int64_t(1));
        }
    }
    return false;
}

std::vector<cpp_int> sort_int(const std::vector<cpp_int>& xs) {
    std::vector<cpp_int> arr = xs;
    cpp_int i = int64_t(0);
    while ((i < arr.size())) {
        cpp_int j = (i + int64_t(1));
        while ((j < arr.size())) {
            if ((arr[static_cast<size_t>(j)] < arr[static_cast<size_t>(i)])) {
                cpp_int tmp = arr[static_cast<size_t>(i)];
                arr[static_cast<size_t>(i)] = arr[static_cast<size_t>(j)];
                arr[static_cast<size_t>(j)] = tmp;
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return arr;
}

std::vector<cpp_int> permutations_of_number(const cpp_int& n) {
    std::string s = ([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }());
    std::vector<cpp_int> d = {};
    cpp_int i = int64_t(0);
    while ((i < s.size())) {
        d = ([&]{ auto __tmp = d; __tmp.push_back(_parse_int_str(std::string(1, s[static_cast<size_t>(i)]), int64_t(10))); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<cpp_int> res = {};
    cpp_int a = int64_t(0);
    while ((a < d.size())) {
        cpp_int b = int64_t(0);
        while ((b < d.size())) {
            if ((b != a)) {
                cpp_int c = int64_t(0);
                while ((c < d.size())) {
                    if (((c != a) && (c != b))) {
                        cpp_int e = int64_t(0);
                        while ((e < d.size())) {
                            if ((((e != a) && (e != b)) && (e != c))) {
                                cpp_int val = ((((d[static_cast<size_t>(a)] * int64_t(1000)) + (d[static_cast<size_t>(b)] * int64_t(100))) + (d[static_cast<size_t>(c)] * int64_t(10))) + d[static_cast<size_t>(e)]);
                                res = ([&]{ auto __tmp = res; __tmp.push_back(val); return __tmp; }());
                            }
                            e = (e + int64_t(1));
                        }
                    }
                    c = (c + int64_t(1));
                }
            }
            b = (b + int64_t(1));
        }
        a = (a + int64_t(1));
    }
    return res;
}

cpp_int abs_int(const cpp_int& x) {
    if ((x < int64_t(0))) {
        return -x;
    }
    return x;
}

bool contains_int(const std::vector<cpp_int>& xs, const cpp_int& v) {
    cpp_int i = int64_t(0);
    while ((i < xs.size())) {
        if ((xs[static_cast<size_t>(i)] == v)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

cpp_int solution() {
    std::vector<cpp_int> prime_list = {};
    cpp_int n = int64_t(1001);
    while ((n < int64_t(10000))) {
        if (is_prime(n)) {
            prime_list = ([&]{ auto __tmp = prime_list; __tmp.push_back(n); return __tmp; }());
        }
        n = (n + int64_t(2));
    }
    std::vector<std::vector<cpp_int>> candidates = {};
    cpp_int i = int64_t(0);
    while ((i < prime_list.size())) {
        cpp_int number = prime_list[static_cast<size_t>(i)];
        std::vector<cpp_int> tmp = {};
        std::vector<cpp_int> perms = permutations_of_number(number);
        cpp_int j = int64_t(0);
        while ((j < perms.size())) {
            cpp_int prime = perms[static_cast<size_t>(j)];
            if ((((prime % int64_t(2)) != int64_t(0)) && search(prime, prime_list))) {
                tmp = ([&]{ auto __tmp = tmp; __tmp.push_back(prime); return __tmp; }());
            }
            j = (j + int64_t(1));
        }
        tmp = sort_int(tmp);
        if ((tmp.size() >= int64_t(3))) {
            candidates = ([&]{ auto __tmp = candidates; __tmp.push_back(tmp); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    std::vector<std::vector<cpp_int>> passed = {};
    i = int64_t(0);
    while ((i < candidates.size())) {
        std::vector<cpp_int> candidate = candidates[static_cast<size_t>(i)];
        bool found = false;
        cpp_int a = int64_t(0);
        while ((a < candidate.size())) {
            cpp_int b = (a + int64_t(1));
            while ((b < candidate.size())) {
                cpp_int c = (b + int64_t(1));
                while ((c < candidate.size())) {
                    cpp_int x = candidate[static_cast<size_t>(a)];
                    cpp_int y = candidate[static_cast<size_t>(b)];
                    cpp_int z = candidate[static_cast<size_t>(c)];
                    if (((((abs_int((x - y)) == abs_int((y - z))) && (x != y)) && (x != z)) && (y != z))) {
                        std::vector<cpp_int> triple = sort_int(std::vector<cpp_int>{x, y, z});
                        passed = ([&]{ auto __tmp = passed; __tmp.push_back(triple); return __tmp; }());
                        found = true;
                        break;
                    }
                    c = (c + int64_t(1));
                }
                if (found) {
                    break;
                }
                b = (b + int64_t(1));
            }
            if (found) {
                break;
            }
            a = (a + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    std::vector<cpp_int> answer_nums = {};
    i = int64_t(0);
    while ((i < passed.size())) {
        std::vector<cpp_int> seq = passed[static_cast<size_t>(i)];
        cpp_int val = _parse_int_str(((([&]{ std::ostringstream ss; ss << std::boolalpha << seq[static_cast<size_t>(int64_t(0))]; return ss.str(); }()) + ([&]{ std::ostringstream ss; ss << std::boolalpha << seq[static_cast<size_t>(int64_t(1))]; return ss.str(); }())) + ([&]{ std::ostringstream ss; ss << std::boolalpha << seq[static_cast<size_t>(int64_t(2))]; return ss.str(); }())), int64_t(10));
        if (!contains_int(answer_nums, val)) {
            answer_nums = ([&]{ auto __tmp = answer_nums; __tmp.push_back(val); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    cpp_int max_val = answer_nums[static_cast<size_t>(int64_t(0))];
    i = int64_t(1);
    while ((i < answer_nums.size())) {
        if ((answer_nums[static_cast<size_t>(i)] > max_val)) {
            max_val = answer_nums[static_cast<size_t>(i)];
        }
        i = (i + int64_t(1));
    }
    return max_val;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << solution();
        std::cout << std::endl;
    }
    return 0;
}
