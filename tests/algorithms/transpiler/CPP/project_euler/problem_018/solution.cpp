// Generated by Mochi transpiler v0.10.71 on 2025-08-23 15:47:11 GMT+7
#include <boost/multiprecision/cpp_int.hpp>
using cpp_int = boost::multiprecision::cpp_int;
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static std::string _cpp_int_to_string(cpp_int v) {
    if(v == 0) return "0";
    bool neg = v < 0;
    if(neg) v = -v;
    std::string s;
    while(v > 0){ int d = int(v % 10); s = char('0'+d) + s; v /= 10; }
    if(neg) s = '-' + s;
    return s;
}
inline std::ostream& operator<<(std::ostream& os, cpp_int v){ os << _cpp_int_to_string(v); return os; }
static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static cpp_int _parse_int_str(const std::string& s, long base) {
    if(s.empty()) return 0;
    cpp_int r = 0;
    bool neg = false; size_t i = 0;
    if(s[0]=='-'){ neg = true; i = 1; }
    for(; i < s.size(); ++i){
        char c = s[i];
        int digit = 0;
        if(c >= '0' && c <= '9') digit = c - '0';
        else if(c >= 'a' && c <= 'z') digit = c - 'a' + 10;
        else if(c >= 'A' && c <= 'Z') digit = c - 'A' + 10;
        else continue;
        r *= base;
        r += digit;
    }
    return neg ? -r : r;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(cpp_int)) os << _cpp_int_to_string(std::any_cast<cpp_int>(val));
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(cpp_int)) return (double)std::any_cast<cpp_int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(cpp_int)) return _cpp_int_to_string(std::any_cast<cpp_int>(v));
    if(v.type() == typeid(double)) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << std::any_cast<double>(v);
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
        return s + exp;
    }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, cpp_int>) {
        return _cpp_int_to_string(v);
    } else if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

cpp_int solution();
int main();

cpp_int solution() {
    std::vector<std::vector<cpp_int>> triangle = std::vector<std::vector<cpp_int>>{std::vector<cpp_int>{int64_t(75)}, std::vector<cpp_int>{int64_t(95), int64_t(64)}, std::vector<cpp_int>{int64_t(17), int64_t(47), int64_t(82)}, std::vector<cpp_int>{int64_t(18), int64_t(35), int64_t(87), int64_t(10)}, std::vector<cpp_int>{int64_t(20), int64_t(4), int64_t(82), int64_t(47), int64_t(65)}, std::vector<cpp_int>{int64_t(19), int64_t(1), int64_t(23), int64_t(75), int64_t(3), int64_t(34)}, std::vector<cpp_int>{int64_t(88), int64_t(2), int64_t(77), int64_t(73), int64_t(7), int64_t(63), int64_t(67)}, std::vector<cpp_int>{int64_t(99), int64_t(65), int64_t(4), int64_t(28), int64_t(6), int64_t(16), int64_t(70), int64_t(92)}, std::vector<cpp_int>{int64_t(41), int64_t(41), int64_t(26), int64_t(56), int64_t(83), int64_t(40), int64_t(80), int64_t(70), int64_t(33)}, std::vector<cpp_int>{int64_t(41), int64_t(48), int64_t(72), int64_t(33), int64_t(47), int64_t(32), int64_t(37), int64_t(16), int64_t(94), int64_t(29)}, std::vector<cpp_int>{int64_t(53), int64_t(71), int64_t(44), int64_t(65), int64_t(25), int64_t(43), int64_t(91), int64_t(52), int64_t(97), int64_t(51), int64_t(14)}, std::vector<cpp_int>{int64_t(70), int64_t(11), int64_t(33), int64_t(28), int64_t(77), int64_t(73), int64_t(17), int64_t(78), int64_t(39), int64_t(68), int64_t(17), int64_t(57)}, std::vector<cpp_int>{int64_t(91), int64_t(71), int64_t(52), int64_t(38), int64_t(17), int64_t(14), int64_t(91), int64_t(43), int64_t(58), int64_t(50), int64_t(27), int64_t(29), int64_t(48)}, std::vector<cpp_int>{int64_t(63), int64_t(66), int64_t(4), int64_t(68), int64_t(89), int64_t(53), int64_t(67), int64_t(30), int64_t(73), int64_t(16), int64_t(69), int64_t(87), int64_t(40), int64_t(31)}, std::vector<cpp_int>{int64_t(4), int64_t(62), int64_t(98), int64_t(27), int64_t(23), int64_t(9), int64_t(70), int64_t(98), int64_t(73), int64_t(93), int64_t(38), int64_t(53), int64_t(60), int64_t(4), int64_t(23)}};
    cpp_int i = int64_t(1);
    while ((i < triangle.size())) {
        cpp_int j = int64_t(0);
        while ((j < triangle[static_cast<size_t>(i)].size())) {
            std::vector<cpp_int> prev_row = triangle[static_cast<size_t>((i - int64_t(1)))];
            cpp_int number1 = ((j != prev_row.size()) ? prev_row[static_cast<size_t>(j)] : int64_t(0));
            cpp_int number2 = ((j > int64_t(0)) ? prev_row[static_cast<size_t>((j - int64_t(1)))] : int64_t(0));
            cpp_int max_val = ((number1 > number2) ? number1 : number2);
            triangle[static_cast<size_t>(i)][static_cast<size_t>(j)] = (triangle[static_cast<size_t>(i)][static_cast<size_t>(j)] + max_val);
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    std::vector<cpp_int> last = triangle[static_cast<size_t>((triangle.size() - int64_t(1)))];
    cpp_int k = int64_t(0);
    cpp_int best = int64_t(0);
    while ((k < last.size())) {
        if ((last[static_cast<size_t>(k)] > best)) {
            best = last[static_cast<size_t>(k)];
        }
        k = (k + int64_t(1));
    }
    return best;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << solution(); return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
