// Generated by Mochi transpiler v0.10.67 on 2025-08-17 14:38:32 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << std::any_cast<double>(v);
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
        return s + exp;
    }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Matrix;

std::ostream& operator<<(std::ostream&, const Matrix&);


struct Matrix {
    std::vector<std::vector<double>> data;
    int64_t rows;
    int64_t cols;
    auto operator<=>(const Matrix&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Matrix& v) {
    os << '{' << "'data': "<< "["; for(size_t i=0;i<v.data.size();++i){ if(i>0) os << ", "; os << v.data[i]; } os << "]"
 << ", " << "'rows': "<< v.rows
 << ", " << "'cols': "<< v.cols
 << '}';
    return os;
}

Matrix make_matrix(int64_t rows, int64_t cols, double value);
Matrix matrix_from_lists(const std::vector<std::vector<double>>& vals);
std::string matrix_to_string(const Matrix& m);
Matrix matrix_add(const Matrix& a, const Matrix& b);
Matrix matrix_sub(const Matrix& a, const Matrix& b);
Matrix matrix_mul_scalar(const Matrix& m, double k);
Matrix matrix_mul(const Matrix& a, const Matrix& b);
Matrix matrix_transpose(const Matrix& m);
Matrix sherman_morrison(const Matrix& ainv, const Matrix& u, const Matrix& v);
int main();

Matrix make_matrix(int64_t rows, int64_t cols, double value) {
    std::vector<std::vector<double>> arr = {};
    int64_t r = int64_t(0);
    while ((r < rows)) {
        std::vector<double> row = {};
        int64_t c = int64_t(0);
        while ((c < cols)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(value); return __tmp; }());
            c = (c + int64_t(1));
        }
        arr = ([&]{ auto __tmp = arr; __tmp.push_back(row); return __tmp; }());
        r = (r + int64_t(1));
    }
    return Matrix{.data = arr, .rows = rows, .cols = cols};
}

Matrix matrix_from_lists(const std::vector<std::vector<double>>& vals) {
    int64_t r = vals.size();
    int64_t c = ((r == int64_t(0)) ? int64_t(0) : _index(vals, int64_t(0)).size());
    return Matrix{.data = vals, .rows = r, .cols = c};
}

std::string matrix_to_string(const Matrix& m) {
    std::string s = std::string("");
    int64_t i = int64_t(0);
    while ((i < m.rows)) {
        s = (s + std::string("["));
        int64_t j = int64_t(0);
        while ((j < m.cols)) {
            s = (s + _to_string(_index(_index(m.data, i), j)));
            if ((j < (m.cols - int64_t(1)))) {
                s = (s + std::string(", "));
            }
            j = (j + int64_t(1));
        }
        s = (s + std::string("]"));
        if ((i < (m.rows - int64_t(1)))) {
            s = (s + std::string("\n"));
        }
        i = (i + int64_t(1));
    }
    return s;
}

Matrix matrix_add(const Matrix& a, const Matrix& b) {
    if (((a.rows != b.rows) || (a.cols != b.cols))) {
        return Matrix{.data = std::vector<std::vector<double>>{}, .rows = int64_t(0), .cols = int64_t(0)};
    }
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < a.rows)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < a.cols)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back((_index(_index(a.data, i), j) + _index(_index(b.data, i), j))); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return Matrix{.data = res, .rows = a.rows, .cols = a.cols};
}

Matrix matrix_sub(const Matrix& a, const Matrix& b) {
    if (((a.rows != b.rows) || (a.cols != b.cols))) {
        return Matrix{.data = std::vector<std::vector<double>>{}, .rows = int64_t(0), .cols = int64_t(0)};
    }
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < a.rows)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < a.cols)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back((_index(_index(a.data, i), j) - _index(_index(b.data, i), j))); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return Matrix{.data = res, .rows = a.rows, .cols = a.cols};
}

Matrix matrix_mul_scalar(const Matrix& m, double k) {
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < m.rows)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < m.cols)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back((_index(_index(m.data, i), j) * k)); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return Matrix{.data = res, .rows = m.rows, .cols = m.cols};
}

Matrix matrix_mul(const Matrix& a, const Matrix& b) {
    if ((a.cols != b.rows)) {
        return Matrix{.data = std::vector<std::vector<double>>{}, .rows = int64_t(0), .cols = int64_t(0)};
    }
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < a.rows)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < b.cols)) {
            double sum = 0.0;
            int64_t k = int64_t(0);
            while ((k < a.cols)) {
                sum = (sum + (_index(_index(a.data, i), k) * _index(_index(b.data, k), j)));
                k = (k + int64_t(1));
            }
            row = ([&]{ auto __tmp = row; __tmp.push_back(sum); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return Matrix{.data = res, .rows = a.rows, .cols = b.cols};
}

Matrix matrix_transpose(const Matrix& m) {
    std::vector<std::vector<double>> res = {};
    int64_t c = int64_t(0);
    while ((c < m.cols)) {
        std::vector<double> row = {};
        int64_t r = int64_t(0);
        while ((r < m.rows)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(_index(_index(m.data, r), c)); return __tmp; }());
            r = (r + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        c = (c + int64_t(1));
    }
    return Matrix{.data = res, .rows = m.cols, .cols = m.rows};
}

Matrix sherman_morrison(const Matrix& ainv, const Matrix& u, const Matrix& v) {
    Matrix vt = matrix_transpose(v);
    Matrix vu = matrix_mul(matrix_mul(vt, ainv), u);
    double factor = (_index(_index(vu.data, int64_t(0)), int64_t(0)) + 1.0);
    if ((factor == 0.0)) {
        return Matrix{.data = std::vector<std::vector<double>>{}, .rows = int64_t(0), .cols = int64_t(0)};
    }
    Matrix term1 = matrix_mul(ainv, u);
    Matrix term2 = matrix_mul(vt, ainv);
    Matrix numerator = matrix_mul(term1, term2);
    Matrix scaled = matrix_mul_scalar(numerator, ((double)(1.0) / (factor)));
    return matrix_sub(ainv, scaled);
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        Matrix ainv = matrix_from_lists(std::vector<std::vector<double>>{std::vector<double>{1.0, 0.0, 0.0}, std::vector<double>{0.0, 1.0, 0.0}, std::vector<double>{0.0, 0.0, 1.0}});
        Matrix u = matrix_from_lists(std::vector<std::vector<double>>{std::vector<double>{1.0}, std::vector<double>{2.0}, std::vector<double>{-3.0}});
        Matrix v = matrix_from_lists(std::vector<std::vector<double>>{std::vector<double>{4.0}, std::vector<double>{-2.0}, std::vector<double>{5.0}});
        Matrix result = sherman_morrison(ainv, u, v);
        std::cout << matrix_to_string(result);
        std::cout << std::endl;
    }
    return 0;
}
