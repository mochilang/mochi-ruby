// Generated by Mochi transpiler v0.10.66 on 2025-08-14 19:13:37 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

int64_t index_of_char(std::string s, std::string ch);
int64_t ord(std::string ch);
bool rabin_karp(std::string pattern, std::string text);
void test_rabin_karp();
int main();

int64_t alphabet_size = int64_t(256);
int64_t modulus = int64_t(1000003);

int64_t index_of_char(std::string s, std::string ch) {
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        if ((([&](const auto& __s){ auto __i = i; if (__i < 0) __i += __s.size(); return std::string(1, __s[static_cast<size_t>(__i)]); })(s) == ch)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

int64_t ord(std::string ch) {
    std::string upper = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
    std::string lower = std::string("abcdefghijklmnopqrstuvwxyz");
    std::string digits = std::string("0123456789");
    int64_t idx = index_of_char(upper, ch);
    if ((idx >= int64_t(0))) {
        return (int64_t(65) + idx);
    }
    idx = index_of_char(lower, ch);
    if ((idx >= int64_t(0))) {
        return (int64_t(97) + idx);
    }
    idx = index_of_char(digits, ch);
    if ((idx >= int64_t(0))) {
        return (int64_t(48) + idx);
    }
    if ((ch == std::string("ü"))) {
        return int64_t(252);
    }
    if ((ch == std::string("Ü"))) {
        return int64_t(220);
    }
    if ((ch == std::string(" "))) {
        return int64_t(32);
    }
    return int64_t(0);
}

bool rabin_karp(std::string pattern, std::string text) {
    int64_t p_len = pattern.size();
    int64_t t_len = text.size();
    if ((p_len > t_len)) {
        return false;
    }
    int64_t p_hash = int64_t(0);
    int64_t t_hash = int64_t(0);
    int64_t modulus_power = int64_t(1);
    int64_t i = int64_t(0);
    while ((i < p_len)) {
        p_hash = ((ord(([&](const auto& __s){ auto __i = i; if (__i < 0) __i += __s.size(); return std::string(1, __s[static_cast<size_t>(__i)]); })(pattern)) + (p_hash * alphabet_size)) % modulus);
        t_hash = ((ord(([&](const auto& __s){ auto __i = i; if (__i < 0) __i += __s.size(); return std::string(1, __s[static_cast<size_t>(__i)]); })(text)) + (t_hash * alphabet_size)) % modulus);
        if ((i != (p_len - int64_t(1)))) {
            modulus_power = ((modulus_power * alphabet_size) % modulus);
        }
        i = (i + int64_t(1));
    }
    int64_t j = int64_t(0);
    while ((j <= (t_len - p_len))) {
        if (((t_hash == p_hash) && (text.substr(j, (j + p_len) - j) == pattern))) {
            return true;
        }
        if ((j == (t_len - p_len))) {
            j = (j + int64_t(1));
            continue;
        }
        t_hash = ((((t_hash - (ord(([&](const auto& __s){ auto __i = j; if (__i < 0) __i += __s.size(); return std::string(1, __s[static_cast<size_t>(__i)]); })(text)) * modulus_power)) * alphabet_size) + ord(([&](const auto& __s){ auto __i = (j + p_len); if (__i < 0) __i += __s.size(); return std::string(1, __s[static_cast<size_t>(__i)]); })(text))) % modulus);
        if ((t_hash < int64_t(0))) {
            t_hash = (t_hash + modulus);
        }
        j = (j + int64_t(1));
    }
    return false;
}

void test_rabin_karp() {
    std::string pattern1 = std::string("abc1abc12");
    std::string text1 = std::string("alskfjaldsabc1abc1abc12k23adsfabcabc");
    std::string text2 = std::string("alskfjaldsk23adsfabcabc");
    if ((!rabin_karp(pattern1, text1) || rabin_karp(pattern1, text2))) {
        std::cout << std::string("Failure");
        std::cout << std::endl;
        return;
    }
    std::string pattern2 = std::string("ABABX");
    std::string text3 = std::string("ABABZABABYABABX");
    if (!rabin_karp(pattern2, text3)) {
        std::cout << std::string("Failure");
        std::cout << std::endl;
        return;
    }
    std::string pattern3 = std::string("AAAB");
    std::string text4 = std::string("ABAAAAAB");
    if (!rabin_karp(pattern3, text4)) {
        std::cout << std::string("Failure");
        std::cout << std::endl;
        return;
    }
    std::string pattern4 = std::string("abcdabcy");
    std::string text5 = std::string("abcxabcdabxabcdabcdabcy");
    if (!rabin_karp(pattern4, text5)) {
        std::cout << std::string("Failure");
        std::cout << std::endl;
        return;
    }
    std::string pattern5 = std::string("Lü");
    std::string text6 = std::string("Lüsai");
    if (!rabin_karp(pattern5, text6)) {
        std::cout << std::string("Failure");
        std::cout << std::endl;
        return;
    }
    std::string pattern6 = std::string("Lue");
    if (rabin_karp(pattern6, text6)) {
        std::cout << std::string("Failure");
        std::cout << std::endl;
        return;
    }
    std::cout << std::string("Success.");
    std::cout << std::endl;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        test_rabin_karp();
    }
    return 0;
}
