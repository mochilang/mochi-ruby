// Generated by Mochi transpiler v0.10.66 on 2025-08-14 19:13:55 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::vector<std::string> split_words(std::string s);
bool is_alnum(std::string c);
std::vector<std::vector<std::string>> split_input(std::string text);
std::string capitalize(std::string word);
std::string to_simple_case(std::string text);
std::string to_complex_case(std::string text, bool upper_flag, std::string sep);
std::string to_pascal_case(std::string text);
std::string to_camel_case(std::string text);
std::string to_snake_case(std::string text, bool upper_flag);
std::string to_kebab_case(std::string text, bool upper_flag);
int main();

std::vector<std::string> split_words(std::string s) {
    std::vector<std::string> words = {};
    std::string current = std::string("");
    for (char ch : s) {
        if ((ch == ' ')) {
            if ((current != std::string(""))) {
                words = ([&]{ auto __tmp = words; __tmp.push_back(current); return __tmp; }());
                current = std::string("");
            }
        } else {
            current = (current + ch);
        }
    }
    if ((current != std::string(""))) {
        words = ([&]{ auto __tmp = words; __tmp.push_back(current); return __tmp; }());
    }
    return words;
}

bool is_alnum(std::string c) {
    return (((([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(std::string("0123456789"), c) || ([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(std::string("abcdefghijklmnopqrstuvwxyz"), c)) || ([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ"), c)) || (c == std::string(" ")));
}

std::vector<std::vector<std::string>> split_input(std::string text) {
    std::vector<std::vector<std::string>> result = {};
    std::string current = std::string("");
    for (char ch : text) {
        if (is_alnum(std::string(1, ch))) {
            current = (current + ch);
        } else {
            if ((current != std::string(""))) {
                result = ([&]{ auto __tmp = result; __tmp.push_back(split_words(current)); return __tmp; }());
                current = std::string("");
            }
        }
    }
    if ((current != std::string(""))) {
        result = ([&]{ auto __tmp = result; __tmp.push_back(split_words(current)); return __tmp; }());
    }
    return result;
}

std::string capitalize(std::string word) {
    if ((word.size() == int64_t(0))) {
        return std::string("");
    }
    if ((word.size() == int64_t(1))) {
        return ([&]{ std::string __s = word; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    }
    return (([&]{ std::string __s = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), int64_t(1) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+int64_t(1)); })(word); for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }()) + ([&]{ std::string __s = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), word.size() - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+word.size()); })(word); for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }()));
}

std::string to_simple_case(std::string text) {
    std::vector<std::vector<std::string>> parts = split_input(text);
    std::string res = std::string("");
    for (std::vector<std::string> sub : parts) {
        for (std::string w : sub) {
            res = (res + capitalize(w));
        }
    }
    return res;
}

std::string to_complex_case(std::string text, bool upper_flag, std::string sep) {
    std::vector<std::vector<std::string>> parts = split_input(text);
    std::string res = std::string("");
    for (std::vector<std::string> sub : parts) {
        bool first = true;
        for (std::string w : sub) {
            std::string word = (upper_flag ? ([&]{ std::string __s = w; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }()) : ([&]{ std::string __s = w; for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }()));
            if (first) {
                res = (res + word);
                first = false;
            } else {
                res = ((res + sep) + word);
            }
        }
    }
    return res;
}

std::string to_pascal_case(std::string text) {
    return to_simple_case(text);
}

std::string to_camel_case(std::string text) {
    std::string s = to_simple_case(text);
    if ((s.size() == int64_t(0))) {
        return std::string("");
    }
    return (([&]{ std::string __s = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), int64_t(1) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+int64_t(1)); })(s); for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }()) + ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), s.size() - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+s.size()); })(s));
}

std::string to_snake_case(std::string text, bool upper_flag) {
    return to_complex_case(text, upper_flag, std::string("_"));
}

std::string to_kebab_case(std::string text, bool upper_flag) {
    return to_complex_case(text, upper_flag, std::string("-"));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << to_pascal_case(std::string("one two 31235three4four"));
        std::cout << std::endl;
        std::cout << to_camel_case(std::string("one two 31235three4four"));
        std::cout << std::endl;
        std::cout << to_snake_case(std::string("one two 31235three4four"), true);
        std::cout << std::endl;
        std::cout << to_snake_case(std::string("one two 31235three4four"), false);
        std::cout << std::endl;
        std::cout << to_kebab_case(std::string("one two 31235three4four"), true);
        std::cout << std::endl;
        std::cout << to_kebab_case(std::string("one two 31235three4four"), false);
        std::cout << std::endl;
    }
    return 0;
}
