// Generated by Mochi transpiler v0.10.66 on 2025-08-14 19:15:37 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::vector<std::vector<bool>> make_matrix_bool(int64_t rows, int64_t cols, bool init);
bool match_pattern(std::string input_string, std::string pattern);
int main();

std::vector<std::vector<bool>> make_matrix_bool(int64_t rows, int64_t cols, bool init) {
    std::vector<std::vector<bool>> matrix = {};
    for (int64_t _ = int64_t(0); _ < rows; _++ ) {
        std::vector<bool> row = {};
        for (int64_t _2 = int64_t(0); _2 < cols; _2++ ) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(init); return __tmp; }());
        }
        matrix = ([&]{ auto __tmp = matrix; __tmp.push_back(row); return __tmp; }());
    }
    return matrix;
}

bool match_pattern(std::string input_string, std::string pattern) {
    int64_t len_string = (input_string.size() + int64_t(1));
    int64_t len_pattern = (pattern.size() + int64_t(1));
    std::vector<std::vector<bool>> dp = make_matrix_bool(len_string, len_pattern, false);
    std::vector<bool> row0 = _index(dp, int64_t(0));
    row0[static_cast<size_t>(int64_t(0))] = true;
    dp[static_cast<size_t>(int64_t(0))] = row0;
    int64_t j = int64_t(1);
    while ((j < len_pattern)) {
        row0 = _index(dp, int64_t(0));
        if ((([&]{ std::string __s = pattern; long long __start = (j - int64_t(1)); long long __end = j; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })() == std::string("*"))) {
            row0[static_cast<size_t>(j)] = _index(row0, (j - int64_t(2)));
        } else {
            row0[static_cast<size_t>(j)] = false;
        }
        dp[static_cast<size_t>(int64_t(0))] = row0;
        j = (j + int64_t(1));
    }
    int64_t i = int64_t(1);
    while ((i < len_string)) {
        std::vector<bool> row = _index(dp, i);
        int64_t j2 = int64_t(1);
        while ((j2 < len_pattern)) {
            std::string s_char = ([&]{ std::string __s = input_string; long long __start = (i - int64_t(1)); long long __end = i; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
            std::string p_char = ([&]{ std::string __s = pattern; long long __start = (j2 - int64_t(1)); long long __end = j2; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
            if (((s_char == p_char) || (p_char == std::string(".")))) {
                row[static_cast<size_t>(j2)] = _index(_index(dp, (i - int64_t(1))), (j2 - int64_t(1)));
            } else             if ((p_char == std::string("*"))) {
                bool val = _index(_index(dp, i), (j2 - int64_t(2)));
                std::string prev_p = ([&]{ std::string __s = pattern; long long __start = (j2 - int64_t(2)); long long __end = (j2 - int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
                if ((!val && ((prev_p == s_char) || (prev_p == std::string("."))))) {
                    val = _index(_index(dp, (i - int64_t(1))), j2);
                }
                row[static_cast<size_t>(j2)] = val;
            } else {
                row[static_cast<size_t>(j2)] = false;
            }

            j2 = (j2 + int64_t(1));
        }
        dp[static_cast<size_t>(i)] = row;
        i = (i + int64_t(1));
    }
    return _index(_index(dp, (len_string - int64_t(1))), (len_pattern - int64_t(1)));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        if (!match_pattern(std::string("aab"), std::string("c*a*b"))) {
            throw std::runtime_error(std::string("case1 failed"));
        }
        if (match_pattern(std::string("dabc"), std::string("*abc"))) {
            throw std::runtime_error(std::string("case2 failed"));
        }
        if (match_pattern(std::string("aaa"), std::string("aa"))) {
            throw std::runtime_error(std::string("case3 failed"));
        }
        if (!match_pattern(std::string("aaa"), std::string("a.a"))) {
            throw std::runtime_error(std::string("case4 failed"));
        }
        if (match_pattern(std::string("aaab"), std::string("aa*"))) {
            throw std::runtime_error(std::string("case5 failed"));
        }
        if (!match_pattern(std::string("aaab"), std::string(".*"))) {
            throw std::runtime_error(std::string("case6 failed"));
        }
        if (match_pattern(std::string("a"), std::string("bbbb"))) {
            throw std::runtime_error(std::string("case7 failed"));
        }
        if (match_pattern(std::string(""), std::string("bbbb"))) {
            throw std::runtime_error(std::string("case8 failed"));
        }
        if (match_pattern(std::string("a"), std::string(""))) {
            throw std::runtime_error(std::string("case9 failed"));
        }
        if (!match_pattern(std::string(""), std::string(""))) {
            throw std::runtime_error(std::string("case10 failed"));
        }
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("aab"), std::string("c*a*b")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("dabc"), std::string("*abc")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("aaa"), std::string("aa")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("aaa"), std::string("a.a")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("aaab"), std::string("aa*")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("aaab"), std::string(".*")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("a"), std::string("bbbb")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string(""), std::string("bbbb")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string("a"), std::string("")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << match_pattern(std::string(""), std::string("")); return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
