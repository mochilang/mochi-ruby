// Generated by Mochi transpiler v0.10.66 on 2025-08-14 19:14:07 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <algorithm>
#include <type_traits>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct WordCount;

std::ostream& operator<<(std::ostream&, const WordCount&);


struct WordCount {
    std::string word;
    int64_t count;
    auto operator<=>(const WordCount&) const = default;
};

std::ostream& operator<<(std::ostream& os, const WordCount& v) {
    os << '{' << "'word': "<< "'" << v.word << "'"
 << ", " << "'count': "<< v.count
 << '}';
    return os;
}

void heapify(std::vector<WordCount>& arr, int64_t index, int64_t heap_size);
void build_max_heap(std::vector<WordCount>& arr);
std::vector<std::string> top_k_frequent_words(const std::vector<std::string>& words, int64_t k_value);
int main();

std::map<std::string, int64_t> freq_map = {};

void heapify(std::vector<WordCount>& arr, int64_t index, int64_t heap_size) {
    int64_t largest = index;
    int64_t left = ((int64_t(2) * index) + int64_t(1));
    int64_t right = ((int64_t(2) * index) + int64_t(2));
    if ((left < heap_size)) {
        WordCount left_item = _index(arr, left);
        WordCount largest_item = _index(arr, largest);
        if ((left_item.count > largest_item.count)) {
            largest = left;
        }
    }
    if ((right < heap_size)) {
        WordCount right_item = _index(arr, right);
        WordCount largest_item2 = _index(arr, largest);
        if ((right_item.count > largest_item2.count)) {
            largest = right;
        }
    }
    if ((largest != index)) {
        WordCount temp = _index(arr, largest);
        arr[static_cast<size_t>(largest)] = _index(arr, index);
        arr[static_cast<size_t>(index)] = temp;
        heapify(arr, largest, heap_size);
    }
}

void build_max_heap(std::vector<WordCount>& arr) {
    int64_t i = ((arr.size() / int64_t(2)) - int64_t(1));
    while ((i >= int64_t(0))) {
        heapify(arr, i, arr.size());
        i = (i - int64_t(1));
    }
}

std::vector<std::string> top_k_frequent_words(const std::vector<std::string>& words, int64_t k_value) {
    freq_map = {};
    int64_t i = int64_t(0);
    while ((i < words.size())) {
        std::string w = _index(words, i);
        if (([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(freq_map, w)) {
            freq_map[w] = (([&](const auto& __m){ auto __it = __m.find(w); if (__it == __m.end()) return int64_t{}; return __it->second; })(freq_map) + int64_t(1));
        } else {
            freq_map[w] = int64_t(1);
        }
        i = (i + int64_t(1));
    }
    std::vector<WordCount> heap = {};
    for (const auto& __p : freq_map) {
        auto w = __p.first;
        heap = ([&]{ auto __tmp = heap; __tmp.push_back(WordCount{.word = w, .count = ([&](const auto& __m){ auto __it = __m.find(w); if (__it == __m.end()) return int64_t{}; return __it->second; })(freq_map)}); return __tmp; }());
    }
    build_max_heap(heap);
    std::vector<std::string> result = {};
    int64_t heap_size = heap.size();
    int64_t limit = k_value;
    if ((limit > heap_size)) {
        limit = heap_size;
    }
    int64_t j = int64_t(0);
    while ((j < limit)) {
        WordCount item = _index(heap, int64_t(0));
        result = ([&]{ auto __tmp = result; __tmp.push_back(item.word); return __tmp; }());
        heap[static_cast<size_t>(int64_t(0))] = _index(heap, (heap_size - int64_t(1)));
        heap[static_cast<size_t>((heap_size - int64_t(1)))] = item;
        heap_size = (heap_size - int64_t(1));
        heapify(heap, int64_t(0), heap_size);
        j = (j + int64_t(1));
    }
    return result;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::vector<std::string> sample = std::vector<std::string>{std::string("a"), std::string("b"), std::string("c"), std::string("a"), std::string("c"), std::string("c")};
        { auto __tmp1 = top_k_frequent_words(sample, int64_t(3)); std::cout << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp1[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp2 = top_k_frequent_words(sample, int64_t(2)); std::cout << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp2[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp3 = top_k_frequent_words(sample, int64_t(1)); std::cout << "["; for(size_t i=0;i<__tmp3.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp3[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp4 = top_k_frequent_words(sample, int64_t(0)); std::cout << "["; for(size_t i=0;i<__tmp4.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp4[i]; } std::cout << "]"; }
        std::cout << std::endl;
    }
    return 0;
}
