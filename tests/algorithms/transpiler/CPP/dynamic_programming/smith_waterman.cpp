// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:45:43 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static std::string _format_double(double v) {
    std::ostringstream ss;
    ss << std::defaultfloat << std::setprecision(std::numeric_limits<double>::max_digits10) << v;
    auto s = ss.str();
    auto epos = s.find('e');
    if(epos == std::string::npos) epos = s.find('E');
    std::string exp;
    if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
    auto pos = s.find('.');
    if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
    return s + exp;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << _format_double(std::any_cast<double>(val));
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) return _format_double(std::any_cast<double>(v));
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        return _format_double(v);
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

int64_t score_function(std::string source_char, std::string target_char, int64_t match_score, int64_t mismatch_score, int64_t gap_score);
std::vector<std::vector<int64_t>> smith_waterman(std::string query, std::string subject, int64_t match_score, int64_t mismatch_score, int64_t gap_score);
std::string traceback(const std::vector<std::vector<int64_t>>& score, std::string query, std::string subject, int64_t match_score, int64_t mismatch_score, int64_t gap_score);
int main();

std::string query = std::string("HEAGAWGHEE");
std::string subject = std::string("PAWHEAE");
std::vector<std::vector<int64_t>> score = std::vector<std::vector<int64_t>>{};

int64_t score_function(std::string source_char, std::string target_char, int64_t match_score, int64_t mismatch_score, int64_t gap_score) {
    if (((source_char == std::string("-")) || (target_char == std::string("-")))) {
        return gap_score;
    }
    if ((source_char == target_char)) {
        return match_score;
    }
    return mismatch_score;
}

std::vector<std::vector<int64_t>> smith_waterman(std::string query, std::string subject, int64_t match_score, int64_t mismatch_score, int64_t gap_score) {
    std::string q = ([&]{ std::string __s = query; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    std::string s = ([&]{ std::string __s = subject; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    int64_t m = q.size();
    int64_t n = s.size();
    std::vector<std::vector<int64_t>> score = {};
    for (int64_t _ = int64_t(0); _ < (m + int64_t(1)); _++ ) {
        std::vector<int64_t> row = {};
        for (int64_t _2 = int64_t(0); _2 < (n + int64_t(1)); _2++ ) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(int64_t(0)); return __tmp; }());
        }
        score = ([&]{ auto __tmp = score; __tmp.push_back(row); return __tmp; }());
    }
    for (int64_t i = int64_t(1); i < (m + int64_t(1)); i++ ) {
        for (int64_t j = int64_t(1); j < (n + int64_t(1)); j++ ) {
            std::string qc = ([&]{ std::string __s = q; long long __start = (i - int64_t(1)); long long __end = i; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
            std::string sc = ([&]{ std::string __s = s; long long __start = (j - int64_t(1)); long long __end = j; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
            int64_t diag = (_index(_index(score, (i - int64_t(1))), (j - int64_t(1))) + score_function(qc, sc, match_score, mismatch_score, gap_score));
            int64_t _delete = (_index(_index(score, (i - int64_t(1))), j) + gap_score);
            int64_t insert = (_index(_index(score, i), (j - int64_t(1))) + gap_score);
            int64_t max_val = int64_t(0);
            if ((diag > max_val)) {
                max_val = diag;
            }
            if ((_delete > max_val)) {
                max_val = _delete;
            }
            if ((insert > max_val)) {
                max_val = insert;
            }
            _index(_index(score, i), j) = max_val;
        }
    }
    return score;
}

std::string traceback(const std::vector<std::vector<int64_t>>& score, std::string query, std::string subject, int64_t match_score, int64_t mismatch_score, int64_t gap_score) {
    std::string q = ([&]{ std::string __s = query; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    std::string s = ([&]{ std::string __s = subject; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    int64_t max_value = int64_t(0);
    int64_t i_max = int64_t(0);
    int64_t j_max = int64_t(0);
    for (int64_t i = int64_t(0); i < score.size(); i++ ) {
        for (int64_t j = int64_t(0); j < _index(score, i).size(); j++ ) {
            if ((_index(_index(score, i), j) > max_value)) {
                max_value = _index(_index(score, i), j);
                i_max = i;
                j_max = j;
            }
        }
    }
    int64_t i = i_max;
    int64_t j = j_max;
    std::string align1 = std::string("");
    std::string align2 = std::string("");
    int64_t gap_penalty = score_function(std::string("-"), std::string("-"), match_score, mismatch_score, gap_score);
    if (((i == int64_t(0)) || (j == int64_t(0)))) {
        return std::string("");
    }
    while (((i > int64_t(0)) && (j > int64_t(0)))) {
        std::string qc = ([&]{ std::string __s = q; long long __start = (i - int64_t(1)); long long __end = i; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
        std::string sc = ([&]{ std::string __s = s; long long __start = (j - int64_t(1)); long long __end = j; if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
        if ((_index(_index(score, i), j) == (_index(_index(score, (i - int64_t(1))), (j - int64_t(1))) + score_function(qc, sc, match_score, mismatch_score, gap_score)))) {
            align1 = (qc + align1);
            align2 = (sc + align2);
            i = (i - int64_t(1));
            j = (j - int64_t(1));
        } else         if ((_index(_index(score, i), j) == (_index(_index(score, (i - int64_t(1))), j) + gap_penalty))) {
            align1 = (qc + align1);
            align2 = (std::string("-") + align2);
            i = (i - int64_t(1));
        } else {
            align1 = (std::string("-") + align1);
            align2 = (sc + align2);
            j = (j - int64_t(1));
        }

    }
    return ((align1 + std::string("\n")) + align2);
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        score = smith_waterman(query, subject, int64_t(1), -int64_t(1), -int64_t(2));
        std::cout << traceback(score, query, subject, int64_t(1), -int64_t(1), -int64_t(2));
        std::cout << std::endl;
    }
    return 0;
}
