// Generated by Mochi transpiler v0.10.59 on 2025-08-07 09:07:53 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct KMeansResult;

std::ostream& operator<<(std::ostream&, const KMeansResult&);


struct KMeansResult {
    std::vector<std::vector<double>> centroids;
    std::vector<int64_t> assignments;
    auto operator<=>(const KMeansResult&) const = default;
};

std::ostream& operator<<(std::ostream& os, const KMeansResult& v) {
    os << '{' << "'centroids': "<< "["; for(size_t i=0;i<v.centroids.size();++i){ if(i>0) os << ", "; os << v.centroids[i]; } os << "]"
 << ", " << "'assignments': "<< "["; for(size_t i=0;i<v.assignments.size();++i){ if(i>0) os << ", "; os << v.assignments[i]; } os << "]"
 << '}';
    return os;
}

double distance_sq(const std::vector<double>& a, const std::vector<double>& b);
std::vector<double> mean(const std::vector<std::vector<double>>& vectors);
KMeansResult k_means(const std::vector<std::vector<double>>& vectors, int64_t k, int64_t iterations);
int main();

double distance_sq(const std::vector<double>& a, const std::vector<double>& b) {
    double sum = 0.0;
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        double diff = (a[i] - b[i]);
        sum = (sum + (diff * diff));
        i = (i + int64_t(1));
    }
    return sum;
}

std::vector<double> mean(const std::vector<std::vector<double>>& vectors) {
    int64_t dim = vectors[int64_t(0)].size();
    std::vector<double> res = {};
    int64_t i = int64_t(0);
    while ((i < dim)) {
        double total = 0.0;
        int64_t j = int64_t(0);
        while ((j < vectors.size())) {
            total = (total + vectors[j][i]);
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(((double)(total) / (vectors.size()))); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

KMeansResult k_means(const std::vector<std::vector<double>>& vectors, int64_t k, int64_t iterations) {
    std::vector<std::vector<double>> centroids = {};
    int64_t i = int64_t(0);
    while ((i < k)) {
        centroids = ([&]{ auto __tmp = centroids; __tmp.push_back(vectors[i]); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<int64_t> assignments = {};
    int64_t n = vectors.size();
    i = int64_t(0);
    while ((i < n)) {
        assignments = ([&]{ auto __tmp = assignments; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t it = int64_t(0);
    while ((it < iterations)) {
        int64_t v = int64_t(0);
        while ((v < n)) {
            int64_t best = int64_t(0);
            double bestDist = distance_sq(vectors[v], centroids[int64_t(0)]);
            int64_t c = int64_t(1);
            while ((c < k)) {
                double d = distance_sq(vectors[v], centroids[c]);
                if ((d < bestDist)) {
                    bestDist = d;
                    best = c;
                }
                c = (c + int64_t(1));
            }
            assignments[v] = best;
            v = (v + int64_t(1));
        }
        int64_t cIdx = int64_t(0);
        while ((cIdx < k)) {
            std::vector<std::vector<double>> cluster = {};
            int64_t v2 = int64_t(0);
            while ((v2 < n)) {
                if ((assignments[v2] == cIdx)) {
                    cluster = ([&]{ auto __tmp = cluster; __tmp.push_back(vectors[v2]); return __tmp; }());
                }
                v2 = (v2 + int64_t(1));
            }
            if ((cluster.size() > int64_t(0))) {
                centroids[cIdx] = mean(cluster);
            }
            cIdx = (cIdx + int64_t(1));
        }
        it = (it + int64_t(1));
    }
    return KMeansResult{.centroids = centroids, .assignments = assignments};
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::vector<std::vector<double>> vectors = std::vector<std::vector<double>>{std::vector<double>{1.0, 2.0}, std::vector<double>{1.5, 1.8}, std::vector<double>{5.0, 8.0}, std::vector<double>{8.0, 8.0}, std::vector<double>{1.0, 0.6}, std::vector<double>{9.0, 11.0}};
        KMeansResult result = k_means(vectors, int64_t(2), int64_t(5));
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = result.centroids; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = result.assignments; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
