// Generated by Mochi transpiler v0.10.73 on 2025-08-25 21:45:58 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static std::string _format_double(double v) {
    std::ostringstream ss;
    ss << std::defaultfloat << std::setprecision(std::numeric_limits<double>::max_digits10) << v;
    auto s = ss.str();
    auto epos = s.find('e');
    if(epos == std::string::npos) epos = s.find('E');
    std::string exp;
    if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
    auto pos = s.find('.');
    if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
    return s + exp;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << _format_double(std::any_cast<double>(val));
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) return _format_double(std::any_cast<double>(v));
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        return _format_double(v);
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::string key(std::string state, std::string obs);
std::vector<std::string> viterbi(const std::vector<std::string>& observations, const std::vector<std::string>& states, const std::map<std::string, double>& start_p, const std::map<std::string, std::map<std::string, double>>& trans_p, const std::map<std::string, std::map<std::string, double>>& emit_p);
std::string join_words(const std::vector<std::string>& words);
int main();

std::vector<std::string> observations = std::vector<std::string>{std::string("normal"), std::string("cold"), std::string("dizzy")};
std::vector<std::string> states = std::vector<std::string>{std::string("Healthy"), std::string("Fever")};
std::map<std::string, double> start_p = {{"Healthy", 0.6}, {"Fever", 0.4}};
std::map<std::string, std::map<std::string, double>> trans_p = {{"Healthy", std::map<std::string, double>{{"Healthy", 0.7}, {"Fever", 0.3}}}, {"Fever", std::map<std::string, double>{{"Healthy", 0.4}, {"Fever", 0.6}}}};
std::map<std::string, std::map<std::string, double>> emit_p = {{"Healthy", std::map<std::string, double>{{"normal", 0.5}, {"cold", 0.4}, {"dizzy", 0.1}}}, {"Fever", std::map<std::string, double>{{"normal", 0.1}, {"cold", 0.3}, {"dizzy", 0.6}}}};
std::vector<std::string> result = std::vector<std::string>{};

std::string key(std::string state, std::string obs) {
    return ((state + std::string("|")) + obs);
}

std::vector<std::string> viterbi(const std::vector<std::string>& observations, const std::vector<std::string>& states, const std::map<std::string, double>& start_p, const std::map<std::string, std::map<std::string, double>>& trans_p, const std::map<std::string, std::map<std::string, double>>& emit_p) {
    if (((observations.size() == int64_t(0)) || (states.size() == int64_t(0)))) {
        throw std::runtime_error(std::string("empty parameters"));
    }
    std::map<std::string, double> probs = {};
    std::map<std::string, std::string> ptrs = {};
    std::string first_obs = _index(observations, int64_t(0));
    int64_t i = int64_t(0);
    while ((i < states.size())) {
        std::string state = _index(states, i);
        probs[key(state, first_obs)] = (([&](const auto& __m){ auto __it = __m.find(state); if (__it == __m.end()) return double{}; return __it->second; })(start_p) * ([&](const auto& __m){ auto __it = __m.find(first_obs); if (__it == __m.end()) return double{}; return __it->second; })(([&](const auto& __m){ auto __it = __m.find(state); if (__it == __m.end()) return std::map<std::string, double>{}; return __it->second; })(emit_p)));
        ptrs[key(state, first_obs)] = std::string("");
        i = (i + int64_t(1));
    }
    int64_t t = int64_t(1);
    while ((t < observations.size())) {
        std::string obs = _index(observations, t);
        int64_t j = int64_t(0);
        while ((j < states.size())) {
            std::string state = _index(states, j);
            double max_prob = -1.0;
            std::string prev_state = std::string("");
            int64_t k = int64_t(0);
            while ((k < states.size())) {
                std::string state0 = _index(states, k);
                std::string obs0 = _index(observations, (t - int64_t(1)));
                double prob_prev = ([&](const auto& __m){ auto __it = __m.find(key(state0, obs0)); if (__it == __m.end()) return double{}; return __it->second; })(probs);
                double prob = ((prob_prev * ([&](const auto& __m){ auto __it = __m.find(state); if (__it == __m.end()) return double{}; return __it->second; })(([&](const auto& __m){ auto __it = __m.find(state0); if (__it == __m.end()) return std::map<std::string, double>{}; return __it->second; })(trans_p))) * ([&](const auto& __m){ auto __it = __m.find(obs); if (__it == __m.end()) return double{}; return __it->second; })(([&](const auto& __m){ auto __it = __m.find(state); if (__it == __m.end()) return std::map<std::string, double>{}; return __it->second; })(emit_p)));
                if ((prob > max_prob)) {
                    max_prob = prob;
                    prev_state = state0;
                }
                k = (k + int64_t(1));
            }
            probs[key(state, obs)] = max_prob;
            ptrs[key(state, obs)] = prev_state;
            j = (j + int64_t(1));
        }
        t = (t + int64_t(1));
    }
    std::vector<std::string> path = {};
    int64_t n = int64_t(0);
    while ((n < observations.size())) {
        path = ([&]{ auto __tmp = path; __tmp.push_back(std::string("")); return __tmp; }());
        n = (n + int64_t(1));
    }
    std::string last_obs = _index(observations, (observations.size() - int64_t(1)));
    double max_final = -1.0;
    std::string last_state = std::string("");
    int64_t m = int64_t(0);
    while ((m < states.size())) {
        std::string state = _index(states, m);
        double prob = ([&](const auto& __m){ auto __it = __m.find(key(state, last_obs)); if (__it == __m.end()) return double{}; return __it->second; })(probs);
        if ((prob > max_final)) {
            max_final = prob;
            last_state = state;
        }
        m = (m + int64_t(1));
    }
    int64_t last_index = (observations.size() - int64_t(1));
    _index(path, last_index) = last_state;
    int64_t idx = last_index;
    while ((idx > int64_t(0))) {
        std::string obs = _index(observations, idx);
        std::string prev = ([&](const auto& __m){ auto __it = __m.find(key(_index(path, idx), obs)); if (__it == __m.end()) return std::string{}; return __it->second; })(ptrs);
        _index(path, (idx - int64_t(1))) = prev;
        idx = (idx - int64_t(1));
    }
    return path;
}

std::string join_words(const std::vector<std::string>& words) {
    std::string res = std::string("");
    int64_t i = int64_t(0);
    while ((i < words.size())) {
        if ((i > int64_t(0))) {
            res = (res + std::string(" "));
        }
        res = (res + _index(words, i));
        i = (i + int64_t(1));
    }
    return res;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        result = viterbi(observations, states, start_p, trans_p, emit_p);
        std::cout << join_words(result);
        std::cout << std::endl;
    }
    return 0;
}
