// Generated by Mochi transpiler v0.10.66 on 2025-08-16 10:10:47 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

void panic(std::string msg);
double abs_float(double x);
std::vector<std::vector<double>> copy_matrix(const std::vector<std::vector<double>>& src);
std::vector<double> solve_linear_system(const std::vector<std::vector<double>>& matrix);
int main();

std::vector<std::vector<double>> example_matrix = std::vector<std::vector<double>>{std::vector<double>{5.0, -5.0, -3.0, 4.0, -11.0}, std::vector<double>{1.0, -4.0, 6.0, -4.0, -10.0}, std::vector<double>{-2.0, -5.0, 4.0, -5.0, -12.0}, std::vector<double>{-3.0, -3.0, 5.0, -5.0, 8.0}};
std::vector<double> solution = std::vector<double>{};

void panic(std::string msg) {
    std::cout << msg;
    std::cout << std::endl;
}

double abs_float(double x) {
    if ((x < 0.0)) {
        return -x;
    }
    return x;
}

std::vector<std::vector<double>> copy_matrix(const std::vector<std::vector<double>>& src) {
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < src.size())) {
        std::vector<double> row_src = _index(src, i);
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < row_src.size())) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(_index(row_src, j)); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<double> solve_linear_system(const std::vector<std::vector<double>>& matrix) {
    std::vector<std::vector<double>> ab = copy_matrix(matrix);
    int64_t num_rows = ab.size();
    int64_t num_cols = (_index(ab, int64_t(0)).size() - int64_t(1));
    if ((num_rows != num_cols)) {
        panic(std::string("Matrix is not square"));
        return std::vector<double>{};
    }
    int64_t column_num = int64_t(0);
    while ((column_num < num_rows)) {
        int64_t i = column_num;
        while ((i < num_cols)) {
            if ((abs_float(_index(_index(ab, i), column_num)) > abs_float(_index(_index(ab, column_num), column_num)))) {
                std::vector<double> temp = _index(ab, column_num);
                ab[static_cast<size_t>(column_num)] = _index(ab, i);
                ab[static_cast<size_t>(i)] = temp;
            }
            i = (i + int64_t(1));
        }
        if ((abs_float(_index(_index(ab, column_num), column_num)) < 1e-08)) {
            panic(std::string("Matrix is singular"));
            return std::vector<double>{};
        }
        if ((column_num != int64_t(0))) {
            i = column_num;
            while ((i < num_rows)) {
                double factor = ((double)(_index(_index(ab, i), (column_num - int64_t(1)))) / (_index(_index(ab, (column_num - int64_t(1))), (column_num - int64_t(1)))));
                int64_t j = int64_t(0);
                while ((j < _index(ab, i).size())) {
                    ab[static_cast<size_t>(i)][static_cast<size_t>(j)] = (_index(_index(ab, i), j) - (factor * _index(_index(ab, (column_num - int64_t(1))), j)));
                    j = (j + int64_t(1));
                }
                i = (i + int64_t(1));
            }
        }
        column_num = (column_num + int64_t(1));
    }
    std::vector<double> x_lst = {};
    int64_t t = int64_t(0);
    while ((t < num_rows)) {
        x_lst = ([&]{ auto __tmp = x_lst; __tmp.push_back(0.0); return __tmp; }());
        t = (t + int64_t(1));
    }
    column_num = (num_rows - int64_t(1));
    while ((column_num >= int64_t(0))) {
        double x = ((double)(_index(_index(ab, column_num), num_cols)) / (_index(_index(ab, column_num), column_num)));
        x_lst[static_cast<size_t>(column_num)] = x;
        int64_t i = (column_num - int64_t(1));
        while ((i >= int64_t(0))) {
            ab[static_cast<size_t>(i)][static_cast<size_t>(num_cols)] = (_index(_index(ab, i), num_cols) - (_index(_index(ab, i), column_num) * x));
            i = (i - int64_t(1));
        }
        column_num = (column_num - int64_t(1));
    }
    return x_lst;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << std::string("Matrix:");
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp1 = example_matrix; ss << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; { auto __tmp2 = __tmp1[static_cast<size_t>(i)]; __ss << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) __ss << ' '; __ss << __tmp2[i]; } __ss << "]"; }
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        solution = solve_linear_system(example_matrix);
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp3 = solution; ss << "["; for(size_t i=0;i<__tmp3.size();++i){ if(i>0) ss << ' '; ss << __tmp3[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
