// Generated by Mochi transpiler v0.10.66 on 2025-08-14 17:49:32 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Complex;

std::ostream& operator<<(std::ostream&, const Complex&);


struct Complex {
    double re;
    double im;
    auto operator<=>(const Complex&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Complex& v) {
    os << '{' << "'re': "<< std::fixed << std::setprecision(1) << v.re
 << ", " << "'im': "<< std::fixed << std::setprecision(1) << v.im
 << '}';
    return os;
}

Complex complex_conj(const Complex& z);
bool complex_eq(const Complex& a, const Complex& b);
Complex complex_add(const Complex& a, const Complex& b);
Complex complex_mul(const Complex& a, const Complex& b);
std::vector<Complex> conj_vector(const std::vector<Complex>& v);
std::vector<Complex> vec_mat_mul(const std::vector<Complex>& v, const std::vector<std::vector<Complex>>& m);
Complex dot(const std::vector<Complex>& a, const std::vector<Complex>& b);
bool is_hermitian(const std::vector<std::vector<Complex>>& m);
double rayleigh_quotient(const std::vector<std::vector<Complex>>& a, const std::vector<Complex>& v);
int main();

std::vector<std::vector<Complex>> a = std::vector<std::vector<Complex>>{std::vector<Complex>{Complex{.re = 2.0, .im = 0.0}, Complex{.re = 2.0, .im = 1.0}, Complex{.re = 4.0, .im = 0.0}}, std::vector<Complex>{Complex{.re = 2.0, .im = -1.0}, Complex{.re = 3.0, .im = 0.0}, Complex{.re = 0.0, .im = 1.0}}, std::vector<Complex>{Complex{.re = 4.0, .im = 0.0}, Complex{.re = 0.0, .im = -1.0}, Complex{.re = 1.0, .im = 0.0}}};
std::vector<Complex> v = std::vector<Complex>{Complex{.re = 1.0, .im = 0.0}, Complex{.re = 2.0, .im = 0.0}, Complex{.re = 3.0, .im = 0.0}};
std::vector<std::vector<Complex>> b = std::vector<std::vector<Complex>>{std::vector<Complex>{Complex{.re = 1.0, .im = 0.0}, Complex{.re = 2.0, .im = 0.0}, Complex{.re = 4.0, .im = 0.0}}, std::vector<Complex>{Complex{.re = 2.0, .im = 0.0}, Complex{.re = 3.0, .im = 0.0}, Complex{.re = -1.0, .im = 0.0}}, std::vector<Complex>{Complex{.re = 4.0, .im = 0.0}, Complex{.re = -1.0, .im = 0.0}, Complex{.re = 1.0, .im = 0.0}}};

Complex complex_conj(const Complex& z) {
    return Complex{.re = z.re, .im = -z.im};
}

bool complex_eq(const Complex& a, const Complex& b) {
    return ((a.re == b.re) && (a.im == b.im));
}

Complex complex_add(const Complex& a, const Complex& b) {
    return Complex{.re = (a.re + b.re), .im = (a.im + b.im)};
}

Complex complex_mul(const Complex& a, const Complex& b) {
    double real = ((a.re * b.re) - (a.im * b.im));
    double imag = ((a.re * b.im) + (a.im * b.re));
    return Complex{.re = real, .im = imag};
}

std::vector<Complex> conj_vector(const std::vector<Complex>& v) {
    std::vector<Complex> res = {};
    int64_t i = int64_t(0);
    while ((i < v.size())) {
        res = ([&]{ auto __tmp = res; __tmp.push_back(complex_conj(_index(v, i))); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<Complex> vec_mat_mul(const std::vector<Complex>& v, const std::vector<std::vector<Complex>>& m) {
    std::vector<Complex> result = {};
    int64_t col = int64_t(0);
    while ((col < _index(m, int64_t(0)).size())) {
        Complex sum = Complex{.re = 0.0, .im = 0.0};
        int64_t row = int64_t(0);
        while ((row < v.size())) {
            sum = complex_add(sum, complex_mul(_index(v, row), _index(_index(m, row), col)));
            row = (row + int64_t(1));
        }
        result = ([&]{ auto __tmp = result; __tmp.push_back(sum); return __tmp; }());
        col = (col + int64_t(1));
    }
    return result;
}

Complex dot(const std::vector<Complex>& a, const std::vector<Complex>& b) {
    Complex sum = Complex{.re = 0.0, .im = 0.0};
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        sum = complex_add(sum, complex_mul(_index(a, i), _index(b, i)));
        i = (i + int64_t(1));
    }
    return sum;
}

bool is_hermitian(const std::vector<std::vector<Complex>>& m) {
    int64_t i = int64_t(0);
    while ((i < m.size())) {
        int64_t j = int64_t(0);
        while ((j < m.size())) {
            if (!complex_eq(_index(_index(m, i), j), complex_conj(_index(_index(m, j), i)))) {
                return false;
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return true;
}

double rayleigh_quotient(const std::vector<std::vector<Complex>>& a, const std::vector<Complex>& v) {
    std::vector<Complex> v_star = conj_vector(v);
    std::vector<Complex> v_star_dot = vec_mat_mul(v_star, a);
    Complex num = dot(v_star_dot, v);
    Complex den = dot(v_star, v);
    return ((double)(num.re) / (den.re));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        if (is_hermitian(a)) {
            double r1 = rayleigh_quotient(a, v);
            { std::ostringstream __ss; double __dv = r1; if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
            std::cout << std::endl;
            std::cout << std::string("\n");
            std::cout << std::endl;
        }
        if (is_hermitian(b)) {
            double r2 = rayleigh_quotient(b, v);
            { std::ostringstream __ss; double __dv = r2; if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
            std::cout << std::endl;
        }
    }
    return 0;
}
