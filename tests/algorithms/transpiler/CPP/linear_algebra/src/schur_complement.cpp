// Generated by Mochi transpiler v0.10.66 on 2025-08-14 17:49:35 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename T> std::vector<T> _concat(const std::vector<T>& a, const std::vector<T>& b) {
    std::vector<T> out = a;
    out.insert(out.end(), b.begin(), b.end());
    return out;
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct OptionMatrix;

std::ostream& operator<<(std::ostream&, const OptionMatrix&);


struct OptionMatrix {
    std::vector<std::vector<double>> value;
    bool ok;
    auto operator<=>(const OptionMatrix&) const = default;
};

std::ostream& operator<<(std::ostream& os, const OptionMatrix& v) {
    os << '{' << "'value': "<< "["; for(size_t i=0;i<v.value.size();++i){ if(i>0) os << ", "; os << v.value[i]; } os << "]"
 << ", " << "'ok': "<< v.ok
 << '}';
    return os;
}

std::vector<std::vector<double>> identity(int64_t n);
std::vector<std::vector<double>> transpose(const std::vector<std::vector<double>>& mat);
std::vector<std::vector<double>> matmul(const std::vector<std::vector<double>>& a, const std::vector<std::vector<double>>& b);
std::vector<std::vector<double>> mat_sub(const std::vector<std::vector<double>>& a, const std::vector<std::vector<double>>& b);
std::vector<std::vector<double>> inverse(const std::vector<std::vector<double>>& mat);
std::vector<std::vector<double>> schur_complement(const std::vector<std::vector<double>>& mat_a, const std::vector<std::vector<double>>& mat_b, const std::vector<std::vector<double>>& mat_c, const OptionMatrix& pseudo_inv);
void print_matrix(const std::vector<std::vector<double>>& mat);
int main();

std::vector<std::vector<double>> identity(int64_t n) {
    std::vector<std::vector<double>> mat = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < n)) {
            if ((i == j)) {
                row = ([&]{ auto __tmp = row; __tmp.push_back(1.0); return __tmp; }());
            } else {
                row = ([&]{ auto __tmp = row; __tmp.push_back(0.0); return __tmp; }());
            }
            j = (j + int64_t(1));
        }
        mat = ([&]{ auto __tmp = mat; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return mat;
}

std::vector<std::vector<double>> transpose(const std::vector<std::vector<double>>& mat) {
    int64_t rows = mat.size();
    int64_t cols = _index(mat, int64_t(0)).size();
    std::vector<std::vector<double>> res = {};
    int64_t j = int64_t(0);
    while ((j < cols)) {
        std::vector<double> row = {};
        int64_t i = int64_t(0);
        while ((i < rows)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(_index(_index(mat, i), j)); return __tmp; }());
            i = (i + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        j = (j + int64_t(1));
    }
    return res;
}

std::vector<std::vector<double>> matmul(const std::vector<std::vector<double>>& a, const std::vector<std::vector<double>>& b) {
    int64_t rows = a.size();
    int64_t cols = _index(b, int64_t(0)).size();
    int64_t inner = _index(a, int64_t(0)).size();
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < rows)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < cols)) {
            double sum = 0.0;
            int64_t k = int64_t(0);
            while ((k < inner)) {
                sum = (sum + (_index(_index(a, i), k) * _index(_index(b, k), j)));
                k = (k + int64_t(1));
            }
            row = ([&]{ auto __tmp = row; __tmp.push_back(sum); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<std::vector<double>> mat_sub(const std::vector<std::vector<double>>& a, const std::vector<std::vector<double>>& b) {
    int64_t rows = a.size();
    int64_t cols = _index(a, int64_t(0)).size();
    std::vector<std::vector<double>> res = {};
    int64_t i = int64_t(0);
    while ((i < rows)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < cols)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back((_index(_index(a, i), j) - _index(_index(b, i), j))); return __tmp; }());
            j = (j + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<std::vector<double>> inverse(const std::vector<std::vector<double>>& mat) {
    int64_t n = mat.size();
    std::vector<std::vector<double>> id = identity(n);
    std::vector<std::vector<double>> aug = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        auto row = _concat(_index(mat, i), _index(id, i));
        aug = ([&]{ auto __tmp = aug; __tmp.push_back((std::vector<double>)row); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t col = int64_t(0);
    while ((col < n)) {
        std::vector<double> pivot_row = _index(aug, col);
        double pivot = _index(pivot_row, col);
        if ((pivot == 0.0)) {
            throw std::runtime_error(std::string("matrix is singular"));
        }
        int64_t j = int64_t(0);
        while ((j < (int64_t(2) * n))) {
            pivot_row[static_cast<size_t>(j)] = ((double)(_index(pivot_row, j)) / (pivot));
            j = (j + int64_t(1));
        }
        aug[static_cast<size_t>(col)] = pivot_row;
        int64_t r = int64_t(0);
        while ((r < n)) {
            if ((r != col)) {
                std::vector<double> row_r = _index(aug, r);
                double factor = _index(row_r, col);
                j = int64_t(0);
                while ((j < (int64_t(2) * n))) {
                    row_r[static_cast<size_t>(j)] = (_index(row_r, j) - (factor * _index(pivot_row, j)));
                    j = (j + int64_t(1));
                }
                aug[static_cast<size_t>(r)] = row_r;
            }
            r = (r + int64_t(1));
        }
        col = (col + int64_t(1));
    }
    std::vector<std::vector<double>> inv = {};
    int64_t r = int64_t(0);
    while ((r < n)) {
        std::vector<double> row = {};
        int64_t c = n;
        while ((c < (int64_t(2) * n))) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(_index(_index(aug, r), c)); return __tmp; }());
            c = (c + int64_t(1));
        }
        inv = ([&]{ auto __tmp = inv; __tmp.push_back(row); return __tmp; }());
        r = (r + int64_t(1));
    }
    return inv;
}

std::vector<std::vector<double>> schur_complement(const std::vector<std::vector<double>>& mat_a, const std::vector<std::vector<double>>& mat_b, const std::vector<std::vector<double>>& mat_c, const OptionMatrix& pseudo_inv) {
    int64_t a_rows = mat_a.size();
    int64_t a_cols = _index(mat_a, int64_t(0)).size();
    if ((a_rows != a_cols)) {
        throw std::runtime_error(std::string("Matrix A must be square"));
    }
    if ((a_rows != mat_b.size())) {
        throw std::runtime_error(std::string("Expected the same number of rows for A and B"));
    }
    if ((_index(mat_b, int64_t(0)).size() != _index(mat_c, int64_t(0)).size())) {
        throw std::runtime_error(std::string("Expected the same number of columns for B and C"));
    }
    std::vector<std::vector<double>> a_inv = std::vector<std::vector<double>>{};
    if (pseudo_inv.ok) {
        a_inv = pseudo_inv.value;
    } else {
        a_inv = inverse(mat_a);
    }
    std::vector<std::vector<double>> bt = transpose(mat_b);
    std::vector<std::vector<double>> a_inv_b = matmul(a_inv, mat_b);
    std::vector<std::vector<double>> bt_a_inv_b = matmul(bt, a_inv_b);
    return mat_sub(mat_c, bt_a_inv_b);
}

void print_matrix(const std::vector<std::vector<double>>& mat) {
    int64_t i = int64_t(0);
    while ((i < mat.size())) {
        std::string line = std::string("");
        int64_t j = int64_t(0);
        std::vector<double> row = _index(mat, i);
        while ((j < row.size())) {
            line = (line + _to_string(_index(row, j)));
            if (((j + int64_t(1)) < row.size())) {
                line = (line + std::string(" "));
            }
            j = (j + int64_t(1));
        }
        std::cout << line;
        std::cout << std::endl;
        i = (i + int64_t(1));
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::vector<std::vector<double>> a = std::vector<std::vector<double>>{std::vector<double>{1.0, 2.0}, std::vector<double>{2.0, 1.0}};
        std::vector<std::vector<double>> b = std::vector<std::vector<double>>{std::vector<double>{0.0, 3.0}, std::vector<double>{3.0, 0.0}};
        std::vector<std::vector<double>> c = std::vector<std::vector<double>>{std::vector<double>{2.0, 1.0}, std::vector<double>{6.0, 3.0}};
        OptionMatrix none = OptionMatrix{.value = std::vector<std::vector<double>>{}, .ok = false};
        std::vector<std::vector<double>> s = schur_complement(a, b, c, none);
        print_matrix(s);
    }
    return 0;
}
