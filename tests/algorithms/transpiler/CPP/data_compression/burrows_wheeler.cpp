// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:07:49 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct BWTResult;

std::ostream& operator<<(std::ostream&, const BWTResult&);


struct BWTResult {
    std::string bwt_string;
    int64_t idx_original_string;
    auto operator<=>(const BWTResult&) const = default;
};

std::ostream& operator<<(std::ostream& os, const BWTResult& v) {
    os << '{' << "'bwt_string': "<< "'" << v.bwt_string << "'"
 << ", " << "'idx_original_string': "<< v.idx_original_string
 << '}';
    return os;
}

std::vector<std::string> all_rotations(std::string s);
std::vector<std::string> sort_strings(std::vector<std::string> arr);
std::string join_strings(const std::vector<std::string>& arr);
BWTResult bwt_transform(std::string s);
int64_t index_of(const std::vector<std::string>& arr, std::string target);
std::string reverse_bwt(std::string bwt_string, int64_t idx_original_string);
int main();

std::string s = std::string("^BANANA");
BWTResult result = bwt_transform(s);

std::vector<std::string> all_rotations(std::string s) {
    int n = s.size();
    std::vector<std::string> rotations = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        std::string rotation = (s.substr(i, n - i) + s.substr(int64_t(0), i - int64_t(0)));
        rotations = ([&]{ auto __tmp = rotations; __tmp.push_back(rotation); return __tmp; }());
        i = (i + int64_t(1));
    }
    return rotations;
}

std::vector<std::string> sort_strings(std::vector<std::string> arr) {
    int n = arr.size();
    int64_t i = int64_t(1);
    while ((i < n)) {
        std::string key = arr[i];
        int64_t j = (i - int64_t(1));
        while (((j >= int64_t(0)) && (arr[j] > key))) {
            arr[(j + int64_t(1))] = arr[j];
            j = (j - int64_t(1));
        }
        arr[(j + int64_t(1))] = key;
        i = (i + int64_t(1));
    }
    return arr;
}

std::string join_strings(const std::vector<std::string>& arr) {
    std::string res = std::string("");
    int64_t i = int64_t(0);
    while ((i < arr.size())) {
        res = (res + arr[i]);
        i = (i + int64_t(1));
    }
    return res;
}

BWTResult bwt_transform(std::string s) {
    if ((s == std::string(""))) {
        throw std::runtime_error(std::string("input string must not be empty"));
    }
    std::vector<std::string> rotations = all_rotations(s);
    rotations = sort_strings(rotations);
    std::vector<std::string> last_col = {};
    int64_t i = int64_t(0);
    while ((i < rotations.size())) {
        std::string word = rotations[i];
        last_col = ([&]{ auto __tmp = last_col; __tmp.push_back(word.substr((word.size() - int64_t(1)), word.size() - (word.size() - int64_t(1)))); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::string bwt_string = join_strings(last_col);
    int64_t idx = index_of(rotations, s);
    return BWTResult{.bwt_string = bwt_string, .idx_original_string = idx};
}

int64_t index_of(const std::vector<std::string>& arr, std::string target) {
    int64_t i = int64_t(0);
    while ((i < arr.size())) {
        if ((arr[i] == target)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

std::string reverse_bwt(std::string bwt_string, int64_t idx_original_string) {
    if ((bwt_string == std::string(""))) {
        throw std::runtime_error(std::string("bwt string must not be empty"));
    }
    int n = bwt_string.size();
    if (((idx_original_string < int64_t(0)) || (idx_original_string >= n))) {
        throw std::runtime_error(std::string("index out of range"));
    }
    std::vector<std::string> ordered_rotations = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        ordered_rotations = ([&]{ auto __tmp = ordered_rotations; __tmp.push_back(std::string("")); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t iter = int64_t(0);
    while ((iter < n)) {
        int64_t j = int64_t(0);
        while ((j < n)) {
            std::string ch = bwt_string.substr(j, (j + int64_t(1)) - j);
            ordered_rotations[j] = (ch + ordered_rotations[j]);
            j = (j + int64_t(1));
        }
        ordered_rotations = sort_strings(ordered_rotations);
        iter = (iter + int64_t(1));
    }
    return ordered_rotations[idx_original_string];
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << result.bwt_string;
        std::cout << std::endl;
        std::cout << result.idx_original_string;
        std::cout << std::endl;
        std::cout << reverse_bwt(result.bwt_string, result.idx_original_string);
        std::cout << std::endl;
    }
    return 0;
}
