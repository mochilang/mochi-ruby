// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:49:49 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

double floor(double x);
double pow10(int64_t n);
double round_to(double x, int64_t ndigits);
double celsius_to_fahrenheit(double c, int64_t ndigits);
double celsius_to_kelvin(double c, int64_t ndigits);
double celsius_to_rankine(double c, int64_t ndigits);
double fahrenheit_to_celsius(double f, int64_t ndigits);
double fahrenheit_to_kelvin(double f, int64_t ndigits);
double fahrenheit_to_rankine(double f, int64_t ndigits);
double kelvin_to_celsius(double k, int64_t ndigits);
double kelvin_to_fahrenheit(double k, int64_t ndigits);
double kelvin_to_rankine(double k, int64_t ndigits);
double rankine_to_celsius(double r, int64_t ndigits);
double rankine_to_fahrenheit(double r, int64_t ndigits);
double rankine_to_kelvin(double r, int64_t ndigits);
double reaumur_to_kelvin(double r, int64_t ndigits);
double reaumur_to_fahrenheit(double r, int64_t ndigits);
double reaumur_to_celsius(double r, int64_t ndigits);
double reaumur_to_rankine(double r, int64_t ndigits);
int main();

double floor(double x) {
    int64_t i = (int64_t)(x);
    if (((double)(i) > x)) {
        i = (i - int64_t(1));
    }
    return (double)(i);
}

double pow10(int64_t n) {
    double p = 1.0;
    int64_t i = int64_t(0);
    while ((i < n)) {
        p = (p * 10.0);
        i = (i + int64_t(1));
    }
    return p;
}

double round_to(double x, int64_t ndigits) {
    double m = pow10(ndigits);
    return ((double)(floor(((x * m) + 0.5))) / (m));
}

double celsius_to_fahrenheit(double c, int64_t ndigits) {
    return round_to((((double)((c * 9.0)) / (5.0)) + 32.0), ndigits);
}

double celsius_to_kelvin(double c, int64_t ndigits) {
    return round_to((c + 273.15), ndigits);
}

double celsius_to_rankine(double c, int64_t ndigits) {
    return round_to((((double)((c * 9.0)) / (5.0)) + 491.67), ndigits);
}

double fahrenheit_to_celsius(double f, int64_t ndigits) {
    return round_to(((double)(((f - 32.0) * 5.0)) / (9.0)), ndigits);
}

double fahrenheit_to_kelvin(double f, int64_t ndigits) {
    return round_to((((double)(((f - 32.0) * 5.0)) / (9.0)) + 273.15), ndigits);
}

double fahrenheit_to_rankine(double f, int64_t ndigits) {
    return round_to((f + 459.67), ndigits);
}

double kelvin_to_celsius(double k, int64_t ndigits) {
    return round_to((k - 273.15), ndigits);
}

double kelvin_to_fahrenheit(double k, int64_t ndigits) {
    return round_to((((double)(((k - 273.15) * 9.0)) / (5.0)) + 32.0), ndigits);
}

double kelvin_to_rankine(double k, int64_t ndigits) {
    return round_to(((double)((k * 9.0)) / (5.0)), ndigits);
}

double rankine_to_celsius(double r, int64_t ndigits) {
    return round_to(((double)(((r - 491.67) * 5.0)) / (9.0)), ndigits);
}

double rankine_to_fahrenheit(double r, int64_t ndigits) {
    return round_to((r - 459.67), ndigits);
}

double rankine_to_kelvin(double r, int64_t ndigits) {
    return round_to(((double)((r * 5.0)) / (9.0)), ndigits);
}

double reaumur_to_kelvin(double r, int64_t ndigits) {
    return round_to(((r * 1.25) + 273.15), ndigits);
}

double reaumur_to_fahrenheit(double r, int64_t ndigits) {
    return round_to(((r * 2.25) + 32.0), ndigits);
}

double reaumur_to_celsius(double r, int64_t ndigits) {
    return round_to((r * 1.25), ndigits);
}

double reaumur_to_rankine(double r, int64_t ndigits) {
    return round_to((((r * 2.25) + 32.0) + 459.67), ndigits);
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        { std::ostringstream __ss; double __dv = celsius_to_fahrenheit(0.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = celsius_to_kelvin(0.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = celsius_to_rankine(0.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = fahrenheit_to_celsius(32.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = fahrenheit_to_kelvin(32.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = fahrenheit_to_rankine(32.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = kelvin_to_celsius(273.15, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = kelvin_to_fahrenheit(273.15, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = kelvin_to_rankine(273.15, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = rankine_to_celsius(491.67, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = rankine_to_fahrenheit(491.67, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = rankine_to_kelvin(491.67, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = reaumur_to_kelvin(80.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = reaumur_to_fahrenheit(80.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = reaumur_to_celsius(80.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = reaumur_to_rankine(80.0, int64_t(2)); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
    }
    return 0;
}
