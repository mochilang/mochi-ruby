// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:45:04 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <algorithm>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <any>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

double pow(double base, int64_t _exp);
double convert_si_prefix(double known_amount, std::string known_prefix, std::string unknown_prefix);
double convert_binary_prefix(double known_amount, std::string known_prefix, std::string unknown_prefix);
int main();

std::map<std::string, int64_t> SI_UNITS = std::map<std::string, int64_t>{{"yotta", int64_t(24)}, {"zetta", int64_t(21)}, {"exa", int64_t(18)}, {"peta", int64_t(15)}, {"tera", int64_t(12)}, {"giga", int64_t(9)}, {"mega", int64_t(6)}, {"kilo", int64_t(3)}, {"hecto", int64_t(2)}, {"deca", int64_t(1)}, {"deci", -int64_t(1)}, {"centi", -int64_t(2)}, {"milli", -int64_t(3)}, {"micro", -int64_t(6)}, {"nano", -int64_t(9)}, {"pico", -int64_t(12)}, {"femto", -int64_t(15)}, {"atto", -int64_t(18)}, {"zepto", -int64_t(21)}, {"yocto", -int64_t(24)}};
std::map<std::string, int64_t> BINARY_UNITS = std::map<std::string, int64_t>{{"yotta", int64_t(8)}, {"zetta", int64_t(7)}, {"exa", int64_t(6)}, {"peta", int64_t(5)}, {"tera", int64_t(4)}, {"giga", int64_t(3)}, {"mega", int64_t(2)}, {"kilo", int64_t(1)}};

double pow(double base, int64_t _exp) {
    if ((_exp == int64_t(0))) {
        return 1.0;
    }
    int64_t e = _exp;
    if ((e < int64_t(0))) {
        e = -e;
    }
    double result = 1.0;
    int64_t i = int64_t(0);
    while ((i < e)) {
        result = (result * base);
        i = (i + int64_t(1));
    }
    if ((_exp < int64_t(0))) {
        return ((double)(1.0) / (result));
    }
    return result;
}

double convert_si_prefix(double known_amount, std::string known_prefix, std::string unknown_prefix) {
    std::string kp = ([&]{ std::string __s = known_prefix; for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }());
    std::string up = ([&]{ std::string __s = unknown_prefix; for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }());
    if (!([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(SI_UNITS, kp)) {
        throw std::runtime_error((std::string("unknown prefix: ") + known_prefix));
    }
    if (!([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(SI_UNITS, up)) {
        throw std::runtime_error((std::string("unknown prefix: ") + unknown_prefix));
    }
    int64_t diff = (([&](const auto& __m){ auto __it = __m.find(kp); return __it != __m.end() ? __it->second : 0; })(SI_UNITS) - ([&](const auto& __m){ auto __it = __m.find(up); return __it != __m.end() ? __it->second : 0; })(SI_UNITS));
    return (known_amount * pow(10.0, diff));
}

double convert_binary_prefix(double known_amount, std::string known_prefix, std::string unknown_prefix) {
    std::string kp = ([&]{ std::string __s = known_prefix; for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }());
    std::string up = ([&]{ std::string __s = unknown_prefix; for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }());
    if (!([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(BINARY_UNITS, kp)) {
        throw std::runtime_error((std::string("unknown prefix: ") + known_prefix));
    }
    if (!([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(BINARY_UNITS, up)) {
        throw std::runtime_error((std::string("unknown prefix: ") + unknown_prefix));
    }
    int64_t diff = ((([&](const auto& __m){ auto __it = __m.find(kp); return __it != __m.end() ? __it->second : 0; })(BINARY_UNITS) - ([&](const auto& __m){ auto __it = __m.find(up); return __it != __m.end() ? __it->second : 0; })(BINARY_UNITS)) * int64_t(10));
    return (known_amount * pow(2.0, diff));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << convert_si_prefix(1.0, std::string("giga"), std::string("mega")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << convert_si_prefix(1.0, std::string("mega"), std::string("giga")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << convert_si_prefix(1.0, std::string("kilo"), std::string("kilo")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << convert_binary_prefix(1.0, std::string("giga"), std::string("mega")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << convert_binary_prefix(1.0, std::string("mega"), std::string("giga")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << convert_binary_prefix(1.0, std::string("kilo"), std::string("kilo")); return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
