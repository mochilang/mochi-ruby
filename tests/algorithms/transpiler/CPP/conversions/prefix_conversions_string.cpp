// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:46:05 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct Prefix;

std::ostream& operator<<(std::ostream&, const Prefix&);


struct Prefix {
    std::string name;
    int64_t exp;
    auto operator<=>(const Prefix&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Prefix& v) {
    os << '{' << "'name': "<< "'" << v.name << "'"
 << ", " << "'exp': "<< v.exp
 << '}';
    return os;
}

double pow(double base, int64_t _exp);
std::string add_si_prefix(double value);
std::string add_binary_prefix(double value);
int main();

std::vector<Prefix> si_positive = std::vector<Prefix>{Prefix{.name = std::string("yotta"), .exp = int64_t(24)}, Prefix{.name = std::string("zetta"), .exp = int64_t(21)}, Prefix{.name = std::string("exa"), .exp = int64_t(18)}, Prefix{.name = std::string("peta"), .exp = int64_t(15)}, Prefix{.name = std::string("tera"), .exp = int64_t(12)}, Prefix{.name = std::string("giga"), .exp = int64_t(9)}, Prefix{.name = std::string("mega"), .exp = int64_t(6)}, Prefix{.name = std::string("kilo"), .exp = int64_t(3)}, Prefix{.name = std::string("hecto"), .exp = int64_t(2)}, Prefix{.name = std::string("deca"), .exp = int64_t(1)}};
std::vector<Prefix> si_negative = std::vector<Prefix>{Prefix{.name = std::string("deci"), .exp = -int64_t(1)}, Prefix{.name = std::string("centi"), .exp = -int64_t(2)}, Prefix{.name = std::string("milli"), .exp = -int64_t(3)}, Prefix{.name = std::string("micro"), .exp = -int64_t(6)}, Prefix{.name = std::string("nano"), .exp = -int64_t(9)}, Prefix{.name = std::string("pico"), .exp = -int64_t(12)}, Prefix{.name = std::string("femto"), .exp = -int64_t(15)}, Prefix{.name = std::string("atto"), .exp = -int64_t(18)}, Prefix{.name = std::string("zepto"), .exp = -int64_t(21)}, Prefix{.name = std::string("yocto"), .exp = -int64_t(24)}};
std::vector<Prefix> binary_prefixes = std::vector<Prefix>{Prefix{.name = std::string("yotta"), .exp = int64_t(80)}, Prefix{.name = std::string("zetta"), .exp = int64_t(70)}, Prefix{.name = std::string("exa"), .exp = int64_t(60)}, Prefix{.name = std::string("peta"), .exp = int64_t(50)}, Prefix{.name = std::string("tera"), .exp = int64_t(40)}, Prefix{.name = std::string("giga"), .exp = int64_t(30)}, Prefix{.name = std::string("mega"), .exp = int64_t(20)}, Prefix{.name = std::string("kilo"), .exp = int64_t(10)}};

double pow(double base, int64_t _exp) {
    double result = 1.0;
    int64_t e = _exp;
    if ((e < int64_t(0))) {
        e = -e;
        int64_t i = int64_t(0);
        while ((i < e)) {
            result = (result * base);
            i = (i + int64_t(1));
        }
        return ((double)(1.0) / (result));
    }
    int64_t i = int64_t(0);
    while ((i < e)) {
        result = (result * base);
        i = (i + int64_t(1));
    }
    return result;
}

std::string add_si_prefix(double value) {
    std::vector<Prefix> prefixes = std::vector<Prefix>{};
    if ((value > 0.0)) {
        prefixes = si_positive;
    } else {
        prefixes = si_negative;
    }
    int64_t i = int64_t(0);
    while ((i < prefixes.size())) {
        Prefix p = prefixes[i];
        double num = ((double)(value) / (pow(10.0, p.exp)));
        if ((num > 1.0)) {
            return ((([&]{ std::ostringstream ss; ss << std::boolalpha << num; return ss.str(); }()) + std::string(" ")) + p.name);
        }
        i = (i + int64_t(1));
    }
    return ([&]{ std::ostringstream ss; ss << std::boolalpha << value; return ss.str(); }());
}

std::string add_binary_prefix(double value) {
    int64_t i = int64_t(0);
    while ((i < binary_prefixes.size())) {
        Prefix p = binary_prefixes[i];
        double num = ((double)(value) / (pow(2.0, p.exp)));
        if ((num > 1.0)) {
            return ((([&]{ std::ostringstream ss; ss << std::boolalpha << num; return ss.str(); }()) + std::string(" ")) + p.name);
        }
        i = (i + int64_t(1));
    }
    return ([&]{ std::ostringstream ss; ss << std::boolalpha << value; return ss.str(); }());
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << add_si_prefix(10000.0);
        std::cout << std::endl;
        std::cout << add_si_prefix(0.005);
        std::cout << std::endl;
        std::cout << add_binary_prefix(65536.0);
        std::cout << std::endl;
        std::cout << add_binary_prefix(512.0);
        std::cout << std::endl;
    }
    return 0;
}
