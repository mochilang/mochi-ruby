// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:18:30 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::vector<int64_t> sort_triplet(int64_t a, int64_t b, int64_t c);
bool contains_triplet(const std::vector<std::vector<int64_t>>& arr, const std::vector<int64_t>& target);
bool contains_int(const std::vector<int64_t>& arr, int64_t value);
std::vector<std::vector<int64_t>> find_triplets_with_0_sum(const std::vector<int64_t>& nums);
std::vector<std::vector<int64_t>> find_triplets_with_0_sum_hashing(const std::vector<int64_t>& arr);
int main();

std::vector<int64_t> sort_triplet(int64_t a, int64_t b, int64_t c) {
    int64_t x = a;
    int64_t y = b;
    int64_t z = c;
    if ((x > y)) {
        int64_t t = x;
        x = y;
        y = t;
    }
    if ((y > z)) {
        int64_t t = y;
        y = z;
        z = t;
    }
    if ((x > y)) {
        int64_t t = x;
        x = y;
        y = t;
    }
    return std::vector<int64_t>{x, y, z};
}

bool contains_triplet(const std::vector<std::vector<int64_t>>& arr, const std::vector<int64_t>& target) {
    for (int i = int64_t(0); i < arr.size(); i++ ) {
        std::vector<int64_t> item = arr[i];
        bool same = true;
        for (int j = int64_t(0); j < target.size(); j++ ) {
            if ((item[j] != target[j])) {
                same = false;
                break;
            }
        }
        if (same) {
            return true;
        }
    }
    return false;
}

bool contains_int(const std::vector<int64_t>& arr, int64_t value) {
    for (int i = int64_t(0); i < arr.size(); i++ ) {
        if ((arr[i] == value)) {
            return true;
        }
    }
    return false;
}

std::vector<std::vector<int64_t>> find_triplets_with_0_sum(const std::vector<int64_t>& nums) {
    int n = nums.size();
    std::vector<std::vector<int64_t>> result = {};
    for (int i = int64_t(0); i < n; i++ ) {
        for (int j = (i + int64_t(1)); j < n; j++ ) {
            for (int k = (j + int64_t(1)); k < n; k++ ) {
                int64_t a = nums[i];
                int64_t b = nums[static_cast<size_t>(j)];
                int64_t c = nums[static_cast<size_t>(k)];
                if ((((a + b) + c) == int64_t(0))) {
                    std::vector<int64_t> trip = sort_triplet(a, b, c);
                    if (!contains_triplet(result, trip)) {
                        result = ([&]{ auto __tmp = result; __tmp.push_back(trip); return __tmp; }());
                    }
                }
            }
        }
    }
    return result;
}

std::vector<std::vector<int64_t>> find_triplets_with_0_sum_hashing(const std::vector<int64_t>& arr) {
    int64_t target_sum = int64_t(0);
    std::vector<std::vector<int64_t>> output = {};
    for (int i = int64_t(0); i < arr.size(); i++ ) {
        std::vector<int64_t> seen = {};
        int64_t current_sum = (target_sum - arr[i]);
        for (int j = (i + int64_t(1)); j < arr.size(); j++ ) {
            int64_t other = arr[static_cast<size_t>(j)];
            int64_t required = (current_sum - other);
            if (contains_int(seen, required)) {
                std::vector<int64_t> trip = sort_triplet(arr[i], other, required);
                if (!contains_triplet(output, trip)) {
                    output = ([&]{ auto __tmp = output; __tmp.push_back(trip); return __tmp; }());
                }
            }
            seen = ([&]{ auto __tmp = seen; __tmp.push_back(other); return __tmp; }());
        }
    }
    return output;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum(std::vector<int64_t>{-int64_t(1), int64_t(0), int64_t(1), int64_t(2), -int64_t(1), -int64_t(4)}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum(std::vector<int64_t>{}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum(std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(0)}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum(std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(3), int64_t(0), -int64_t(1), -int64_t(2), -int64_t(3)}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum_hashing(std::vector<int64_t>{-int64_t(1), int64_t(0), int64_t(1), int64_t(2), -int64_t(1), -int64_t(4)}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum_hashing(std::vector<int64_t>{}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum_hashing(std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(0)}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = find_triplets_with_0_sum_hashing(std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(3), int64_t(0), -int64_t(1), -int64_t(2), -int64_t(3)}); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
