// Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:19:19 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static std::string _format_double(double v) {
    std::ostringstream ss;
    ss << std::defaultfloat << std::setprecision(6) << v;
    auto s = ss.str();
    auto epos = s.find('e');
    if(epos == std::string::npos) epos = s.find('E');
    std::string exp;
    if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
    auto pos = s.find('.');
    if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
    return s + exp;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << _format_double(std::any_cast<double>(val));
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) return _format_double(std::any_cast<double>(v));
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        return _format_double(v);
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::vector<double> slice_without_last(const std::vector<double>& xs);
double parse_float(std::string token);
double pow_float(double base, double exp);
double apply_op(double a, double b, std::string op);
double evaluate(const std::vector<std::string>& tokens);
int main();

std::vector<double> slice_without_last(const std::vector<double>& xs) {
    std::vector<double> res = {};
    int64_t i = int64_t(0);
    while ((i < (xs.size() - int64_t(1)))) {
        res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

double parse_float(std::string token) {
    double sign = 1.0;
    int64_t idx = int64_t(0);
    if ((token.size() > int64_t(0))) {
        std::string first = ([&]{ std::string __s = token; long long __start = int64_t(0); long long __end = int64_t(1); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
        if ((first == std::string("-"))) {
            sign = -1.0;
            idx = int64_t(1);
        } else         if ((first == std::string("+"))) {
            idx = int64_t(1);
        }

    }
    int64_t int_part = int64_t(0);
    while (((idx < token.size()) && (([&]{ std::string __s = token; long long __start = idx; long long __end = (idx + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })() != std::string(".")))) {
        int_part = ((int_part * int64_t(10)) + std::stoll(([&]{ std::string __s = token; long long __start = idx; long long __end = (idx + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })()));
        idx = (idx + int64_t(1));
    }
    double result = (1.0 * int_part);
    if (((idx < token.size()) && (([&]{ std::string __s = token; long long __start = idx; long long __end = (idx + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })() == std::string(".")))) {
        idx = (idx + int64_t(1));
        double place = 0.1;
        while ((idx < token.size())) {
            int64_t digit = std::stoll(([&]{ std::string __s = token; long long __start = idx; long long __end = (idx + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })());
            result = (result + (place * (1.0 * digit)));
            place = ((double)(place) / (double)(10.0));
            idx = (idx + int64_t(1));
        }
    }
    return (sign * result);
}

double pow_float(double base, double exp) {
    double result = 1.0;
    int64_t i = int64_t(0);
    int64_t e = (int64_t)(exp);
    while ((i < e)) {
        result = (result * base);
        i = (i + int64_t(1));
    }
    return result;
}

double apply_op(double a, double b, std::string op) {
    if ((op == std::string("+"))) {
        return (a + b);
    }
    if ((op == std::string("-"))) {
        return (a - b);
    }
    if ((op == std::string("*"))) {
        return (a * b);
    }
    if ((op == std::string("/"))) {
        return ((double)(a) / (double)(b));
    }
    if ((op == std::string("^"))) {
        return pow_float(a, b);
    }
    return 0.0;
}

double evaluate(const std::vector<std::string>& tokens) {
    if ((tokens.size() == int64_t(0))) {
        return 0.0;
    }
    std::vector<double> stack = {};
    for (std::string token : tokens) {
        if ((((((token == std::string("+")) || (token == std::string("-"))) || (token == std::string("*"))) || (token == std::string("/"))) || (token == std::string("^")))) {
            if ((((token == std::string("+")) || (token == std::string("-"))) && (stack.size() < int64_t(2)))) {
                double b = _index(stack, (stack.size() - int64_t(1)));
                stack = slice_without_last(stack);
                if ((token == std::string("-"))) {
                    stack = ([&]{ auto __tmp = stack; __tmp.push_back((0.0 - b)); return __tmp; }());
                } else {
                    stack = ([&]{ auto __tmp = stack; __tmp.push_back(b); return __tmp; }());
                }
            } else {
                double b = _index(stack, (stack.size() - int64_t(1)));
                stack = slice_without_last(stack);
                double a = _index(stack, (stack.size() - int64_t(1)));
                stack = slice_without_last(stack);
                double result = apply_op(a, b, token);
                stack = ([&]{ auto __tmp = stack; __tmp.push_back(result); return __tmp; }());
            }
        } else {
            stack = ([&]{ auto __tmp = stack; __tmp.push_back(parse_float(token)); return __tmp; }());
        }
    }
    if ((stack.size() != int64_t(1))) {
        throw std::runtime_error(std::string("Invalid postfix expression"));
    }
    return _index(stack, int64_t(0));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << _to_string(evaluate(std::vector<std::string>{std::string("2"), std::string("1"), std::string("+"), std::string("3"), std::string("*")}));
        std::cout << std::endl;
        std::cout << _to_string(evaluate(std::vector<std::string>{std::string("4"), std::string("13"), std::string("5"), std::string("/"), std::string("+")}));
        std::cout << std::endl;
        std::cout << _to_string(evaluate(std::vector<std::string>{std::string("5"), std::string("6"), std::string("9"), std::string("*"), std::string("+")}));
        std::cout << std::endl;
        std::cout << _to_string(evaluate(std::vector<std::string>{std::string("2"), std::string("-"), std::string("3"), std::string("+")}));
        std::cout << std::endl;
        std::cout << _to_string(evaluate(std::vector<std::string>{}));
        std::cout << std::endl;
    }
    return 0;
}
