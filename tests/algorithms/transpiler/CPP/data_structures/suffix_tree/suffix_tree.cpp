// Generated by Mochi transpiler v0.10.72 on 2025-08-24 22:22:57 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << std::any_cast<double>(v);
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
        return s + exp;
    }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Node;
struct SuffixTree;

std::ostream& operator<<(std::ostream&, const Node&);
std::ostream& operator<<(std::ostream&, const SuffixTree&);


struct Node {
    std::map<std::string, int64_t> children;
    bool is_end_of_string;
    int64_t start;
    int64_t end;
    auto operator<=>(const Node&) const = default;
};

struct SuffixTree {
    std::string text;
    std::vector<Node> nodes;
    auto operator<=>(const SuffixTree&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Node& v) {
    os << '{' << "'children': "<< "{"; bool first_0=true; for(const auto& p: v.children){ if(!first_0) os << ", "; first_0=false; os << p.first; os << ': '; os << p.second; } os << "}"
 << ", " << "'is_end_of_string': "<< v.is_end_of_string
 << ", " << "'start': "<< v.start
 << ", " << "'end': "<< v.end
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const SuffixTree& v) {
    os << '{' << "'text': "<< "'" << v.text << "'"
 << ", " << "'nodes': "<< "["; for(size_t i=0;i<v.nodes.size();++i){ if(i>0) os << ", "; os << v.nodes[i]; } os << "]"
 << '}';
    return os;
}

Node new_node();
bool has_key(const std::map<std::string, int64_t>& m, std::string k);
SuffixTree add_suffix(SuffixTree tree, std::string suffix, int64_t index);
SuffixTree build_suffix_tree(const SuffixTree& tree);
SuffixTree new_suffix_tree(std::string text);
bool search(const SuffixTree& tree, std::string pattern);
int main();

SuffixTree st = SuffixTree{};

Node new_node() {
    return Node{{}, false, -int64_t(1), -int64_t(1)};
}

bool has_key(const std::map<std::string, int64_t>& m, std::string k) {
    for (const auto& __p : m) {
        auto key = __p.first;
        if ((key == k)) {
            return true;
        }
    }
    return false;
}

SuffixTree add_suffix(SuffixTree tree, std::string suffix, int64_t index) {
    std::vector<Node> nodes = tree.nodes;
    int64_t node_idx = int64_t(0);
    int64_t j = int64_t(0);
    while ((j < suffix.size())) {
        std::string ch = ([&]{ std::string __s = suffix; long long __start = j; long long __end = (j + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
        Node node = _index(nodes, node_idx);
        std::map<std::string, int64_t> children = node.children;
        if (!has_key(children, ch)) {
            nodes = ([&]{ auto __tmp = nodes; __tmp.push_back(new_node()); return __tmp; }());
            int64_t new_idx = (nodes.size() - int64_t(1));
            children[ch] = new_idx;
        }
        node.children = children;
        _index(nodes, node_idx) = node;
        node_idx = ([&](const auto& __m){ auto __it = __m.find(ch); if (__it == __m.end()) return int64_t{}; return __it->second; })(children);
        j = (j + int64_t(1));
    }
    Node node = _index(nodes, node_idx);
    node.is_end_of_string = true;
    node.start = index;
    node.end = ((index + suffix.size()) - int64_t(1));
    _index(nodes, node_idx) = node;
    tree.nodes = nodes;
    return tree;
}

SuffixTree build_suffix_tree(const SuffixTree& tree) {
    std::string text = tree.text;
    int64_t n = text.size();
    int64_t i = int64_t(0);
    SuffixTree t = tree;
    while ((i < n)) {
        std::string suffix = std::string("");
        int64_t k = i;
        while ((k < n)) {
            suffix = (suffix + ([&]{ std::string __s = text; long long __start = k; long long __end = (k + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })());
            k = (k + int64_t(1));
        }
        t = add_suffix(t, suffix, i);
        i = (i + int64_t(1));
    }
    return t;
}

SuffixTree new_suffix_tree(std::string text) {
    SuffixTree tree = SuffixTree{text, std::vector<Node>{}};
    tree.nodes = ([&]{ auto __tmp = tree.nodes; __tmp.push_back(new_node()); return __tmp; }());
    tree = build_suffix_tree(tree);
    return tree;
}

bool search(const SuffixTree& tree, std::string pattern) {
    int64_t node_idx = int64_t(0);
    int64_t i = int64_t(0);
    std::vector<Node> nodes = tree.nodes;
    while ((i < pattern.size())) {
        std::string ch = ([&]{ std::string __s = pattern; long long __start = i; long long __end = (i + int64_t(1)); if(__start < 0) __start = 0; if(__end < __start) __end = __start; if(__start > static_cast<long long>(__s.size())) __start = __s.size(); if(__end > static_cast<long long>(__s.size())) __end = __s.size(); return __s.substr(static_cast<size_t>(__start), static_cast<size_t>(__end - __start)); })();
        Node node = _index(nodes, node_idx);
        std::map<std::string, int64_t> children = node.children;
        if (!has_key(children, ch)) {
            return false;
        }
        node_idx = ([&](const auto& __m){ auto __it = __m.find(ch); if (__it == __m.end()) return int64_t{}; return __it->second; })(children);
        i = (i + int64_t(1));
    }
    return true;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        st = new_suffix_tree(std::string("bananas"));
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << search(st, std::string("ana")); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << search(st, std::string("apple")); return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
