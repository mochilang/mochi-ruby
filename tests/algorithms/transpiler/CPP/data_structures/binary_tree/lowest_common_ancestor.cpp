// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:23:25 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int64_t pow2(int64_t _exp);
std::vector<std::vector<int64_t>> create_sparse(int64_t max_node, std::vector<std::vector<int64_t>> parent);
int64_t lowest_common_ancestor(int64_t u, int64_t v, const std::vector<int64_t>& level, const std::vector<std::vector<int64_t>>& parent);
void breadth_first_search(std::vector<int64_t> level, std::vector<std::vector<int64_t>> parent, int64_t max_node, const std::map<int64_t, std::vector<int64_t>>& graph, int64_t root);
int main();

int64_t pow2(int64_t _exp) {
    int64_t res = int64_t(1);
    int64_t i = int64_t(0);
    while ((i < _exp)) {
        res = (res * int64_t(2));
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<std::vector<int64_t>> create_sparse(int64_t max_node, std::vector<std::vector<int64_t>> parent) {
    int64_t j = int64_t(1);
    while ((pow2(j) < max_node)) {
        int64_t i = int64_t(1);
        while ((i <= max_node)) {
            parent[j][i] = parent[(j - int64_t(1))][parent[(j - int64_t(1))][i]];
            i = (i + int64_t(1));
        }
        j = (j + int64_t(1));
    }
    return parent;
}

int64_t lowest_common_ancestor(int64_t u, int64_t v, const std::vector<int64_t>& level, const std::vector<std::vector<int64_t>>& parent) {
    if ((level[u] < level[v])) {
        int64_t temp = u;
        u = v;
        v = temp;
    }
    int64_t i = int64_t(18);
    while ((i >= int64_t(0))) {
        if (((level[u] - pow2(i)) >= level[v])) {
            u = parent[i][u];
        }
        i = (i - int64_t(1));
    }
    if ((u == v)) {
        return u;
    }
    i = int64_t(18);
    while ((i >= int64_t(0))) {
        int64_t pu = parent[i][u];
        int64_t pv = parent[i][v];
        if (((pu != int64_t(0)) && (pu != pv))) {
            u = pu;
            v = pv;
        }
        i = (i - int64_t(1));
    }
    return parent[int64_t(0)][u];
}

void breadth_first_search(std::vector<int64_t> level, std::vector<std::vector<int64_t>> parent, int64_t max_node, const std::map<int64_t, std::vector<int64_t>>& graph, int64_t root) {
    level[root] = int64_t(0);
    std::vector<int64_t> q = {};
    q = ([&]{ auto __tmp = q; __tmp.push_back(root); return __tmp; }());
    int64_t head = int64_t(0);
    while ((head < q.size())) {
        int64_t u = q[head];
        head = (head + int64_t(1));
        std::vector<int64_t> adj = ([&](const auto& __m){ auto __it = __m.find(u); return __it != __m.end() ? __it->second : std::vector<int64_t>{}; })(graph);
        int64_t j = int64_t(0);
        while ((j < adj.size())) {
            int64_t v = adj[j];
            if ((level[v] == (int64_t(0) - int64_t(1)))) {
                level[v] = (level[u] + int64_t(1));
                parent[int64_t(0)][v] = u;
                q = ([&]{ auto __tmp = q; __tmp.push_back(v); return __tmp; }());
            }
            j = (j + int64_t(1));
        }
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        int64_t max_node = int64_t(13);
        std::vector<std::vector<int64_t>> parent = {};
        int64_t i = int64_t(0);
        while ((i < int64_t(20))) {
            std::vector<int64_t> row = {};
            int64_t j = int64_t(0);
            while ((j < (max_node + int64_t(10)))) {
                row = ([&]{ auto __tmp = row; __tmp.push_back(int64_t(0)); return __tmp; }());
                j = (j + int64_t(1));
            }
            parent = ([&]{ auto __tmp = parent; __tmp.push_back(row); return __tmp; }());
            i = (i + int64_t(1));
        }
        std::vector<int64_t> level = {};
        i = int64_t(0);
        while ((i < (max_node + int64_t(10)))) {
            level = ([&]{ auto __tmp = level; __tmp.push_back((int64_t(0) - int64_t(1))); return __tmp; }());
            i = (i + int64_t(1));
        }
        std::map<int64_t, std::vector<int64_t>> graph = std::map<int64_t, std::vector<int64_t>>{};
        graph[int64_t(1)] = std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(4)};
        graph[int64_t(2)] = std::vector<int64_t>{int64_t(5)};
        graph[int64_t(3)] = std::vector<int64_t>{int64_t(6), int64_t(7)};
        graph[int64_t(4)] = std::vector<int64_t>{int64_t(8)};
        graph[int64_t(5)] = std::vector<int64_t>{int64_t(9), int64_t(10)};
        graph[int64_t(6)] = std::vector<int64_t>{int64_t(11)};
        graph[int64_t(7)] = std::vector<int64_t>{};
        graph[int64_t(8)] = std::vector<int64_t>{int64_t(12), int64_t(13)};
        graph[int64_t(9)] = std::vector<int64_t>{};
        graph[int64_t(10)] = std::vector<int64_t>{};
        graph[int64_t(11)] = std::vector<int64_t>{};
        graph[int64_t(12)] = std::vector<int64_t>{};
        graph[int64_t(13)] = std::vector<int64_t>{};
        breadth_first_search(level, parent, max_node, graph, int64_t(1));
        parent = create_sparse(max_node, parent);
        std::cout << (std::string("LCA of node 1 and 3 is: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << lowest_common_ancestor(int64_t(1), int64_t(3), level, parent); return ss.str(); }()));
        std::cout << std::endl;
        std::cout << (std::string("LCA of node 5 and 6 is: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << lowest_common_ancestor(int64_t(5), int64_t(6), level, parent); return ss.str(); }()));
        std::cout << std::endl;
        std::cout << (std::string("LCA of node 7 and 11 is: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << lowest_common_ancestor(int64_t(7), int64_t(11), level, parent); return ss.str(); }()));
        std::cout << std::endl;
        std::cout << (std::string("LCA of node 6 and 7 is: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << lowest_common_ancestor(int64_t(6), int64_t(7), level, parent); return ss.str(); }()));
        std::cout << std::endl;
        std::cout << (std::string("LCA of node 4 and 12 is: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << lowest_common_ancestor(int64_t(4), int64_t(12), level, parent); return ss.str(); }()));
        std::cout << std::endl;
        std::cout << (std::string("LCA of node 8 and 8 is: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << lowest_common_ancestor(int64_t(8), int64_t(8), level, parent); return ss.str(); }()));
        std::cout << std::endl;
    }
    return 0;
}
