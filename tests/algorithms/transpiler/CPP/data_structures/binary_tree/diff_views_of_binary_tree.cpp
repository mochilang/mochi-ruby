// Generated by Mochi transpiler v0.10.72 on 2025-08-24 17:12:19 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << std::any_cast<double>(v);
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
        return s + exp;
    }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(17) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Tree;
struct Pair;

std::ostream& operator<<(std::ostream&, const Tree&);
std::ostream& operator<<(std::ostream&, const Pair&);


struct Tree {
    std::vector<int64_t> values;
    std::vector<int64_t> lefts;
    std::vector<int64_t> rights;
    int64_t root;
    auto operator<=>(const Tree&) const = default;
};

struct Pair {
    int64_t idx;
    int64_t hd;
    auto operator<=>(const Pair&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Tree& v) {
    os << '{' << "'values': "<< "["; for(size_t i=0;i<v.values.size();++i){ if(i>0) os << ", "; os << v.values[i]; } os << "]"
 << ", " << "'lefts': "<< "["; for(size_t i=0;i<v.lefts.size();++i){ if(i>0) os << ", "; os << v.lefts[i]; } os << "]"
 << ", " << "'rights': "<< "["; for(size_t i=0;i<v.rights.size();++i){ if(i>0) os << ", "; os << v.rights[i]; } os << "]"
 << ", " << "'root': "<< v.root
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const Pair& v) {
    os << '{' << "'idx': "<< v.idx
 << ", " << "'hd': "<< v.hd
 << '}';
    return os;
}

Tree make_tree();
int64_t index_of(const std::vector<int64_t>& xs, int64_t x);
void sort_pairs(std::vector<int64_t>& hds, std::vector<int64_t>& vals);
std::vector<int64_t> right_view(const Tree& t);
std::vector<int64_t> left_view(const Tree& t);
std::vector<int64_t> top_view(const Tree& t);
std::vector<int64_t> bottom_view(const Tree& t);
int main();

int64_t NIL = (int64_t(0) - int64_t(1));
Tree tree = Tree{};

Tree make_tree() {
    return Tree{std::vector<int64_t>{int64_t(3), int64_t(9), int64_t(20), int64_t(15), int64_t(7)}, std::vector<int64_t>{int64_t(1), NIL, int64_t(3), NIL, NIL}, std::vector<int64_t>{int64_t(2), NIL, int64_t(4), NIL, NIL}, int64_t(0)};
}

int64_t index_of(const std::vector<int64_t>& xs, int64_t x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == x)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return NIL;
}

void sort_pairs(std::vector<int64_t>& hds, std::vector<int64_t>& vals) {
    int64_t i = int64_t(0);
    while ((i < hds.size())) {
        int64_t j = i;
        while (((j > int64_t(0)) && (_index(hds, (j - int64_t(1))) > _index(hds, j)))) {
            int64_t hd_tmp = _index(hds, (j - int64_t(1)));
            _index(hds, (j - int64_t(1))) = _index(hds, j);
            _index(hds, j) = hd_tmp;
            int64_t val_tmp = _index(vals, (j - int64_t(1)));
            _index(vals, (j - int64_t(1))) = _index(vals, j);
            _index(vals, j) = val_tmp;
            j = (j - int64_t(1));
        }
        i = (i + int64_t(1));
    }
}

std::vector<int64_t> right_view(const Tree& t) {
    std::vector<int64_t> res = {};
    std::vector<int64_t> queue = std::vector<int64_t>{t.root};
    while ((queue.size() > int64_t(0))) {
        int64_t size = queue.size();
        int64_t i = int64_t(0);
        while ((i < size)) {
            int64_t idx = _index(queue, i);
            if ((_index(t.lefts, idx) != NIL)) {
                queue = ([&]{ auto __tmp = queue; __tmp.push_back(_index(t.lefts, idx)); return __tmp; }());
            }
            if ((_index(t.rights, idx) != NIL)) {
                queue = ([&]{ auto __tmp = queue; __tmp.push_back(_index(t.rights, idx)); return __tmp; }());
            }
            i = (i + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(_index(t.values, _index(queue, (size - int64_t(1))))); return __tmp; }());
        queue = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(size, queue.size() - size); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+size, __v.begin()+queue.size()); })(queue);
    }
    return res;
}

std::vector<int64_t> left_view(const Tree& t) {
    std::vector<int64_t> res = {};
    std::vector<int64_t> queue = std::vector<int64_t>{t.root};
    while ((queue.size() > int64_t(0))) {
        int64_t size = queue.size();
        int64_t i = int64_t(0);
        while ((i < size)) {
            int64_t idx = _index(queue, i);
            if ((_index(t.lefts, idx) != NIL)) {
                queue = ([&]{ auto __tmp = queue; __tmp.push_back(_index(t.lefts, idx)); return __tmp; }());
            }
            if ((_index(t.rights, idx) != NIL)) {
                queue = ([&]{ auto __tmp = queue; __tmp.push_back(_index(t.rights, idx)); return __tmp; }());
            }
            i = (i + int64_t(1));
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(_index(t.values, _index(queue, int64_t(0)))); return __tmp; }());
        queue = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(size, queue.size() - size); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+size, __v.begin()+queue.size()); })(queue);
    }
    return res;
}

std::vector<int64_t> top_view(const Tree& t) {
    std::vector<int64_t> hds = {};
    std::vector<int64_t> vals = {};
    std::vector<int64_t> queue_idx = std::vector<int64_t>{t.root};
    std::vector<int64_t> queue_hd = std::vector<int64_t>{int64_t(0)};
    while ((queue_idx.size() > int64_t(0))) {
        int64_t idx = _index(queue_idx, int64_t(0));
        queue_idx = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), queue_idx.size() - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+queue_idx.size()); })(queue_idx);
        int64_t hd = _index(queue_hd, int64_t(0));
        queue_hd = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), queue_hd.size() - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+queue_hd.size()); })(queue_hd);
        if ((index_of(hds, hd) == NIL)) {
            hds = ([&]{ auto __tmp = hds; __tmp.push_back(hd); return __tmp; }());
            vals = ([&]{ auto __tmp = vals; __tmp.push_back(_index(t.values, idx)); return __tmp; }());
        }
        if ((_index(t.lefts, idx) != NIL)) {
            queue_idx = ([&]{ auto __tmp = queue_idx; __tmp.push_back(_index(t.lefts, idx)); return __tmp; }());
            queue_hd = ([&]{ auto __tmp = queue_hd; __tmp.push_back((hd - int64_t(1))); return __tmp; }());
        }
        if ((_index(t.rights, idx) != NIL)) {
            queue_idx = ([&]{ auto __tmp = queue_idx; __tmp.push_back(_index(t.rights, idx)); return __tmp; }());
            queue_hd = ([&]{ auto __tmp = queue_hd; __tmp.push_back((hd + int64_t(1))); return __tmp; }());
        }
    }
    sort_pairs(hds, vals);
    return vals;
}

std::vector<int64_t> bottom_view(const Tree& t) {
    std::vector<int64_t> hds = {};
    std::vector<int64_t> vals = {};
    std::vector<int64_t> queue_idx = std::vector<int64_t>{t.root};
    std::vector<int64_t> queue_hd = std::vector<int64_t>{int64_t(0)};
    while ((queue_idx.size() > int64_t(0))) {
        int64_t idx = _index(queue_idx, int64_t(0));
        queue_idx = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), queue_idx.size() - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+queue_idx.size()); })(queue_idx);
        int64_t hd = _index(queue_hd, int64_t(0));
        queue_hd = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(1), queue_hd.size() - int64_t(1)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(1), __v.begin()+queue_hd.size()); })(queue_hd);
        int64_t pos = index_of(hds, hd);
        if ((pos == NIL)) {
            hds = ([&]{ auto __tmp = hds; __tmp.push_back(hd); return __tmp; }());
            vals = ([&]{ auto __tmp = vals; __tmp.push_back(_index(t.values, idx)); return __tmp; }());
        } else {
            _index(vals, pos) = _index(t.values, idx);
        }
        if ((_index(t.lefts, idx) != NIL)) {
            queue_idx = ([&]{ auto __tmp = queue_idx; __tmp.push_back(_index(t.lefts, idx)); return __tmp; }());
            queue_hd = ([&]{ auto __tmp = queue_hd; __tmp.push_back((hd - int64_t(1))); return __tmp; }());
        }
        if ((_index(t.rights, idx) != NIL)) {
            queue_idx = ([&]{ auto __tmp = queue_idx; __tmp.push_back(_index(t.rights, idx)); return __tmp; }());
            queue_hd = ([&]{ auto __tmp = queue_hd; __tmp.push_back((hd + int64_t(1))); return __tmp; }());
        }
    }
    sort_pairs(hds, vals);
    return vals;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        tree = make_tree();
        { auto __tmp1 = right_view(tree); std::cout << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp1[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp2 = left_view(tree); std::cout << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp2[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp3 = top_view(tree); std::cout << "["; for(size_t i=0;i<__tmp3.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp3[i]; } std::cout << "]"; }
        std::cout << std::endl;
        { auto __tmp4 = bottom_view(tree); std::cout << "["; for(size_t i=0;i<__tmp4.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp4[i]; } std::cout << "]"; }
        std::cout << std::endl;
    }
    return 0;
}
