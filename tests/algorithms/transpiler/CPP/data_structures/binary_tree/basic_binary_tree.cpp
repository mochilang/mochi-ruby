// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:19:02 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct Node;

std::ostream& operator<<(std::ostream&, const Node&);


struct Node {
    int64_t data;
    int64_t left;
    int64_t right;
    auto operator<=>(const Node&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Node& v) {
    os << '{' << "'data': "<< v.data
 << ", " << "'left': "<< v.left
 << ", " << "'right': "<< v.right
 << '}';
    return os;
}

std::vector<int64_t> inorder(const std::vector<Node>& nodes, int64_t index, const std::vector<int64_t>& acc);
int64_t size(const std::vector<Node>& nodes, int64_t index);
int64_t depth(const std::vector<Node>& nodes, int64_t index);
bool is_full(const std::vector<Node>& nodes, int64_t index);
std::vector<Node> small_tree();
std::vector<Node> medium_tree();
int main();

std::vector<Node> small = small_tree();
std::vector<Node> medium = medium_tree();

std::vector<int64_t> inorder(const std::vector<Node>& nodes, int64_t index, const std::vector<int64_t>& acc) {
    if ((index == (int64_t(0) - int64_t(1)))) {
        return acc;
    }
    Node node = nodes[index];
    std::vector<int64_t> res = inorder(nodes, node.left, acc);
    res = ([&]{ auto __tmp = res; __tmp.push_back(node.data); return __tmp; }());
    res = inorder(nodes, node.right, res);
    return res;
}

int64_t size(const std::vector<Node>& nodes, int64_t index) {
    if ((index == (int64_t(0) - int64_t(1)))) {
        return int64_t(0);
    }
    Node node = nodes[index];
    return ((int64_t(1) + size(nodes, node.left)) + size(nodes, node.right));
}

int64_t depth(const std::vector<Node>& nodes, int64_t index) {
    if ((index == (int64_t(0) - int64_t(1)))) {
        return int64_t(0);
    }
    Node node = nodes[index];
    int64_t left_depth = depth(nodes, node.left);
    int64_t right_depth = depth(nodes, node.right);
    if ((left_depth > right_depth)) {
        return (left_depth + int64_t(1));
    }
    return (right_depth + int64_t(1));
}

bool is_full(const std::vector<Node>& nodes, int64_t index) {
    if ((index == (int64_t(0) - int64_t(1)))) {
        return true;
    }
    Node node = nodes[index];
    if (((node.left == (int64_t(0) - int64_t(1))) && (node.right == (int64_t(0) - int64_t(1))))) {
        return true;
    }
    if (((node.left != (int64_t(0) - int64_t(1))) && (node.right != (int64_t(0) - int64_t(1))))) {
        return (is_full(nodes, node.left) && is_full(nodes, node.right));
    }
    return false;
}

std::vector<Node> small_tree() {
    std::vector<Node> arr = {};
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(2), .left = int64_t(1), .right = int64_t(2)}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(1), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(3), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}); return __tmp; }());
    return arr;
}

std::vector<Node> medium_tree() {
    std::vector<Node> arr = {};
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(4), .left = int64_t(1), .right = int64_t(4)}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(2), .left = int64_t(2), .right = int64_t(3)}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(1), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(3), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(5), .left = (int64_t(0) - int64_t(1)), .right = int64_t(5)}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(6), .left = (int64_t(0) - int64_t(1)), .right = int64_t(6)}); return __tmp; }());
    arr = ([&]{ auto __tmp = arr; __tmp.push_back(Node{.data = int64_t(7), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}); return __tmp; }());
    return arr;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << size(small, int64_t(0));
        std::cout << std::endl;
        { auto __tmp = inorder(small, int64_t(0), std::vector<int64_t>{}); std::cout << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp[i]; } std::cout << "]"; }
        std::cout << std::endl;
        std::cout << depth(small, int64_t(0));
        std::cout << std::endl;
        std::cout << (is_full(small, int64_t(0)) ? "true" : "false");
        std::cout << std::endl;
        std::cout << size(medium, int64_t(0));
        std::cout << std::endl;
        { auto __tmp = inorder(medium, int64_t(0), std::vector<int64_t>{}); std::cout << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp[i]; } std::cout << "]"; }
        std::cout << std::endl;
        std::cout << depth(medium, int64_t(0));
        std::cout << std::endl;
        std::cout << (is_full(medium, int64_t(0)) ? "true" : "false");
        std::cout << std::endl;
    }
    return 0;
}
