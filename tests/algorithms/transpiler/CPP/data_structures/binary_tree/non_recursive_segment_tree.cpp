// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:23:38 GMT+7
#include <iostream>
#include <string>
#include <functional>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::vector<int64_t> build(const std::vector<int64_t>& arr, std::function<int64_t(int64_t, int64_t)> combine);
void update(std::vector<int64_t> st, int64_t n, std::function<int64_t(int64_t, int64_t)> combine, int64_t p, int64_t v);
int64_t query(const std::vector<int64_t>& st, int64_t n, std::function<int64_t(int64_t, int64_t)> combine, int64_t left, int64_t right);
int64_t add(int64_t a, int64_t b);
int64_t min_int(int64_t a, int64_t b);
int64_t max_int(int64_t a, int64_t b);
int main();

std::vector<int64_t> arr1 = std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(3)};
std::vector<int64_t> st1 = build(arr1, add);
std::vector<int64_t> arr2 = std::vector<int64_t>{int64_t(3), int64_t(1), int64_t(2)};
std::vector<int64_t> st2 = build(arr2, min_int);
std::vector<int64_t> arr3 = std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(1)};
std::vector<int64_t> st3 = build(arr3, max_int);
std::vector<int64_t> arr4 = std::vector<int64_t>{int64_t(1), int64_t(5), int64_t(7), -int64_t(1), int64_t(6)};
int n4 = arr4.size();
std::vector<int64_t> st4 = build(arr4, add);

std::vector<int64_t> build(const std::vector<int64_t>& arr, std::function<int64_t(int64_t, int64_t)> combine) {
    int n = arr.size();
    std::vector<int64_t> st = {};
    int64_t i = int64_t(0);
    while ((i < (int64_t(2) * n))) {
        st = ([&]{ auto __tmp = st; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    i = int64_t(0);
    while ((i < n)) {
        st[static_cast<size_t>((n + i))] = arr[i];
        i = (i + int64_t(1));
    }
    i = (n - int64_t(1));
    while ((i > int64_t(0))) {
        st[i] = combine(st[(i * int64_t(2))], st[((i * int64_t(2)) + int64_t(1))]);
        i = (i - int64_t(1));
    }
    return st;
}

void update(std::vector<int64_t> st, int64_t n, std::function<int64_t(int64_t, int64_t)> combine, int64_t p, int64_t v) {
    int64_t idx = (p + n);
    st[idx] = v;
    while ((idx > int64_t(1))) {
        idx = (int64_t)((idx / int64_t(2)));
        st[idx] = combine(st[(idx * int64_t(2))], st[((idx * int64_t(2)) + int64_t(1))]);
    }
}

int64_t query(const std::vector<int64_t>& st, int64_t n, std::function<int64_t(int64_t, int64_t)> combine, int64_t left, int64_t right) {
    int64_t l = (left + n);
    int64_t r = (right + n);
    int64_t res = int64_t(0);
    bool has = false;
    while ((l <= r)) {
        if (((l % int64_t(2)) == int64_t(1))) {
            if (!has) {
                res = st[l];
                has = true;
            } else {
                res = combine(res, st[l]);
            }
            l = (l + int64_t(1));
        }
        if (((r % int64_t(2)) == int64_t(0))) {
            if (!has) {
                res = st[r];
                has = true;
            } else {
                res = combine(res, st[r]);
            }
            r = (r - int64_t(1));
        }
        l = (int64_t)((l / int64_t(2)));
        r = (int64_t)((r / int64_t(2)));
    }
    return res;
}

int64_t add(int64_t a, int64_t b) {
    return (a + b);
}

int64_t min_int(int64_t a, int64_t b) {
    if ((a < b)) {
        return a;
    } else {
        return b;
    }
}

int64_t max_int(int64_t a, int64_t b) {
    if ((a > b)) {
        return a;
    } else {
        return b;
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << query(st1, (int64_t)(arr1.size()), add, int64_t(0), int64_t(2)); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << query(st2, (int64_t)(arr2.size()), min_int, int64_t(0), int64_t(2)); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << query(st3, (int64_t)(arr3.size()), max_int, int64_t(0), int64_t(2)); return ss.str(); }());
        std::cout << std::endl;
        update(st4, (int64_t)(n4), add, int64_t(1), -int64_t(1));
        update(st4, (int64_t)(n4), add, int64_t(2), int64_t(3));
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << query(st4, (int64_t)(n4), add, int64_t(1), int64_t(2)); return ss.str(); }());
        std::cout << std::endl;
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << query(st4, (int64_t)(n4), add, int64_t(1), int64_t(1)); return ss.str(); }());
        std::cout << std::endl;
        update(st4, (int64_t)(n4), add, int64_t(4), int64_t(1));
        std::cout << ([&]{ std::ostringstream ss; ss << std::boolalpha << query(st4, (int64_t)(n4), add, int64_t(3), int64_t(4)); return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
