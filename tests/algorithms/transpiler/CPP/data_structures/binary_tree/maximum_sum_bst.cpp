// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:23:31 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct Node;
struct Info;

std::ostream& operator<<(std::ostream&, const Node&);
std::ostream& operator<<(std::ostream&, const Info&);


struct Node {
    int64_t val;
    int64_t left;
    int64_t right;
    auto operator<=>(const Node&) const = default;
};

struct Info {
    bool is_bst;
    int64_t min_val;
    int64_t max_val;
    int64_t total;
    int64_t best;
    auto operator<=>(const Info&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Node& v) {
    os << '{' << "'val': "<< v.val
 << ", " << "'left': "<< v.left
 << ", " << "'right': "<< v.right
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const Info& v) {
    os << '{' << "'is_bst': "<< v.is_bst
 << ", " << "'min_val': "<< v.min_val
 << ", " << "'max_val': "<< v.max_val
 << ", " << "'total': "<< v.total
 << ", " << "'best': "<< v.best
 << '}';
    return os;
}

int64_t min_int(int64_t a, int64_t b);
int64_t max_int(int64_t a, int64_t b);
Info solver(const std::vector<Node>& nodes, int64_t idx);
int64_t max_sum_bst(const std::vector<Node>& nodes, int64_t root);
int main();

int64_t min_int(int64_t a, int64_t b) {
    if ((a < b)) {
        return a;
    }
    return b;
}

int64_t max_int(int64_t a, int64_t b) {
    if ((a > b)) {
        return a;
    }
    return b;
}

Info solver(const std::vector<Node>& nodes, int64_t idx) {
    if ((idx == (int64_t(0) - int64_t(1)))) {
        return Info{.is_bst = true, .min_val = int64_t(2147483647), .max_val = -int64_t(2147483648), .total = int64_t(0), .best = int64_t(0)};
    }
    Node node = nodes[idx];
    Info left_info = solver(nodes, node.left);
    Info right_info = solver(nodes, node.right);
    int64_t current_best = max_int(left_info.best, right_info.best);
    if ((((left_info.is_bst && right_info.is_bst) && (left_info.max_val < node.val)) && (node.val < right_info.min_val))) {
        int64_t sum_val = ((left_info.total + right_info.total) + node.val);
        current_best = max_int(current_best, sum_val);
        return Info{.is_bst = true, .min_val = min_int(left_info.min_val, node.val), .max_val = max_int(right_info.max_val, node.val), .total = sum_val, .best = current_best};
    }
    return Info{.is_bst = false, .min_val = int64_t(0), .max_val = int64_t(0), .total = int64_t(0), .best = current_best};
}

int64_t max_sum_bst(const std::vector<Node>& nodes, int64_t root) {
    Info info = solver(nodes, root);
    return info.best;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::vector<Node> t1_nodes = std::vector<Node>{Node{.val = int64_t(4), .left = int64_t(1), .right = (int64_t(0) - int64_t(1))}, Node{.val = int64_t(3), .left = int64_t(2), .right = int64_t(3)}, Node{.val = int64_t(1), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}, Node{.val = int64_t(2), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}};
        std::cout << max_sum_bst(t1_nodes, int64_t(0));
        std::cout << std::endl;
        std::vector<Node> t2_nodes = std::vector<Node>{Node{.val = -int64_t(4), .left = int64_t(1), .right = int64_t(2)}, Node{.val = -int64_t(2), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}, Node{.val = -int64_t(5), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}};
        std::cout << max_sum_bst(t2_nodes, int64_t(0));
        std::cout << std::endl;
        std::vector<Node> t3_nodes = std::vector<Node>{Node{.val = int64_t(1), .left = int64_t(1), .right = int64_t(2)}, Node{.val = int64_t(4), .left = int64_t(3), .right = int64_t(4)}, Node{.val = int64_t(3), .left = int64_t(5), .right = int64_t(6)}, Node{.val = int64_t(2), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}, Node{.val = int64_t(4), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}, Node{.val = int64_t(2), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}, Node{.val = int64_t(5), .left = int64_t(7), .right = int64_t(8)}, Node{.val = int64_t(4), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}, Node{.val = int64_t(6), .left = (int64_t(0) - int64_t(1)), .right = (int64_t(0) - int64_t(1))}};
        std::cout << max_sum_bst(t3_nodes, int64_t(0));
        std::cout << std::endl;
    }
    return 0;
}
