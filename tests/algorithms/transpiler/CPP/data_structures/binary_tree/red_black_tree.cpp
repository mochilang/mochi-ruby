// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:23:43 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct RBTree;

std::ostream& operator<<(std::ostream&, const RBTree&);


struct RBTree {
    std::vector<std::vector<int64_t>> nodes;
    int64_t root;
    auto operator<=>(const RBTree&) const = default;
};

std::ostream& operator<<(std::ostream& os, const RBTree& v) {
    os << '{' << "'nodes': "<< "["; for(size_t i=0;i<v.nodes.size();++i){ if(i>0) os << ", "; os << v.nodes[i]; } os << "]"
 << ", " << "'root': "<< v.root
 << '}';
    return os;
}

RBTree make_tree();
RBTree rotate_left(RBTree t, int64_t x);
RBTree rotate_right(RBTree t, int64_t x);
RBTree insert_fix(RBTree t, int64_t z);
RBTree tree_insert(RBTree t, int64_t v);
std::vector<int64_t> inorder(const RBTree& t, int64_t x, std::vector<int64_t> acc);
int main();

int64_t LABEL = int64_t(0);
int64_t COLOR = int64_t(1);
int64_t PARENT = int64_t(2);
int64_t LEFT = int64_t(3);
int64_t RIGHT = int64_t(4);
int64_t NEG_ONE = -int64_t(1);

RBTree make_tree() {
    return RBTree{.nodes = std::vector<std::vector<int64_t>>{}, .root = -int64_t(1)};
}

RBTree rotate_left(RBTree t, int64_t x) {
    std::vector<std::vector<int64_t>> nodes = t.nodes;
    int64_t y = nodes[x][RIGHT];
    int64_t yLeft = nodes[y][LEFT];
    nodes[x][RIGHT] = yLeft;
    if ((yLeft != NEG_ONE)) {
        nodes[yLeft][PARENT] = x;
    }
    int64_t xParent = nodes[x][PARENT];
    nodes[y][PARENT] = xParent;
    if ((xParent == NEG_ONE)) {
        t.root = y;
    } else     if ((x == nodes[xParent][LEFT])) {
        nodes[xParent][LEFT] = y;
    } else {
        nodes[xParent][RIGHT] = y;
    }

    nodes[y][LEFT] = x;
    nodes[x][PARENT] = y;
    t.nodes = nodes;
    return t;
}

RBTree rotate_right(RBTree t, int64_t x) {
    std::vector<std::vector<int64_t>> nodes = t.nodes;
    int64_t y = nodes[x][LEFT];
    int64_t yRight = nodes[y][RIGHT];
    nodes[x][LEFT] = yRight;
    if ((yRight != NEG_ONE)) {
        nodes[yRight][PARENT] = x;
    }
    int64_t xParent = nodes[x][PARENT];
    nodes[y][PARENT] = xParent;
    if ((xParent == NEG_ONE)) {
        t.root = y;
    } else     if ((x == nodes[xParent][RIGHT])) {
        nodes[xParent][RIGHT] = y;
    } else {
        nodes[xParent][LEFT] = y;
    }

    nodes[y][RIGHT] = x;
    nodes[x][PARENT] = y;
    t.nodes = nodes;
    return t;
}

RBTree insert_fix(RBTree t, int64_t z) {
    std::vector<std::vector<int64_t>> nodes = t.nodes;
    while (((z != t.root) && (nodes[nodes[z][PARENT]][COLOR] == int64_t(1)))) {
        if ((nodes[z][PARENT] == nodes[nodes[nodes[z][PARENT]][PARENT]][LEFT])) {
            int64_t y = nodes[nodes[nodes[z][PARENT]][PARENT]][RIGHT];
            if (((y != NEG_ONE) && (nodes[y][COLOR] == int64_t(1)))) {
                nodes[nodes[z][PARENT]][COLOR] = int64_t(0);
                nodes[y][COLOR] = int64_t(0);
                int64_t gp = nodes[nodes[z][PARENT]][PARENT];
                nodes[gp][COLOR] = int64_t(1);
                z = gp;
            } else {
                if ((z == nodes[nodes[z][PARENT]][RIGHT])) {
                    z = nodes[z][PARENT];
                    t.nodes = nodes;
                    t = rotate_left(t, z);
                    nodes = t.nodes;
                }
                nodes[nodes[z][PARENT]][COLOR] = int64_t(0);
                int64_t gp = nodes[nodes[z][PARENT]][PARENT];
                nodes[gp][COLOR] = int64_t(1);
                t.nodes = nodes;
                t = rotate_right(t, gp);
                nodes = t.nodes;
            }
        } else {
            int64_t y = nodes[nodes[nodes[z][PARENT]][PARENT]][LEFT];
            if (((y != NEG_ONE) && (nodes[y][COLOR] == int64_t(1)))) {
                nodes[nodes[z][PARENT]][COLOR] = int64_t(0);
                nodes[y][COLOR] = int64_t(0);
                int64_t gp = nodes[nodes[z][PARENT]][PARENT];
                nodes[gp][COLOR] = int64_t(1);
                z = gp;
            } else {
                if ((z == nodes[nodes[z][PARENT]][LEFT])) {
                    z = nodes[z][PARENT];
                    t.nodes = nodes;
                    t = rotate_right(t, z);
                    nodes = t.nodes;
                }
                nodes[nodes[z][PARENT]][COLOR] = int64_t(0);
                int64_t gp = nodes[nodes[z][PARENT]][PARENT];
                nodes[gp][COLOR] = int64_t(1);
                t.nodes = nodes;
                t = rotate_left(t, gp);
                nodes = t.nodes;
            }
        }
    }
    nodes = t.nodes;
    nodes[t.root][COLOR] = int64_t(0);
    t.nodes = nodes;
    return t;
}

RBTree tree_insert(RBTree t, int64_t v) {
    std::vector<std::vector<int64_t>> nodes = t.nodes;
    std::vector<int64_t> node = std::vector<int64_t>{v, int64_t(1), -int64_t(1), -int64_t(1), -int64_t(1)};
    nodes = ([&]{ auto __tmp = nodes; __tmp.push_back(node); return __tmp; }());
    int64_t idx = (nodes.size() - int64_t(1));
    int64_t y = NEG_ONE;
    int64_t x = t.root;
    while ((x != NEG_ONE)) {
        y = x;
        if ((v < nodes[x][LABEL])) {
            x = nodes[x][LEFT];
        } else {
            x = nodes[x][RIGHT];
        }
    }
    nodes[idx][PARENT] = y;
    if ((y == NEG_ONE)) {
        t.root = idx;
    } else     if ((v < nodes[y][LABEL])) {
        nodes[y][LEFT] = idx;
    } else {
        nodes[y][RIGHT] = idx;
    }

    t.nodes = nodes;
    t = insert_fix(t, idx);
    return t;
}

std::vector<int64_t> inorder(const RBTree& t, int64_t x, std::vector<int64_t> acc) {
    if ((x == NEG_ONE)) {
        return acc;
    }
    acc = inorder(t, t.nodes[x][LEFT], acc);
    acc = ([&]{ auto __tmp = acc; __tmp.push_back(t.nodes[x][LABEL]); return __tmp; }());
    acc = inorder(t, t.nodes[x][RIGHT], acc);
    return acc;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        RBTree t = make_tree();
        std::vector<int64_t> values = std::vector<int64_t>{int64_t(10), int64_t(20), int64_t(30), int64_t(15), int64_t(25), int64_t(5), int64_t(1)};
        int64_t i = int64_t(0);
        while ((i < values.size())) {
            t = tree_insert(t, values[i]);
            i = (i + int64_t(1));
        }
        std::vector<int64_t> res = {};
        res = inorder(t, t.root, res);
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = res; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
