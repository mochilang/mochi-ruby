// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:23:40 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct Job;

std::ostream& operator<<(std::ostream&, const Job&);


struct Job {
    int64_t id;
    int64_t deadline;
    int64_t profit;
    auto operator<=>(const Job&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Job& v) {
    os << '{' << "'id': "<< v.id
 << ", " << "'deadline': "<< v.deadline
 << ", " << "'profit': "<< v.profit
 << '}';
    return os;
}

std::vector<Job> sort_jobs_by_profit(const std::vector<Job>& jobs);
int64_t max_deadline(const std::vector<Job>& jobs);
std::vector<int64_t> job_sequencing_with_deadlines(const std::vector<Job>& jobs);
int main();

std::vector<Job> jobs1 = {};
std::vector<Job> jobs2 = {};

std::vector<Job> sort_jobs_by_profit(const std::vector<Job>& jobs) {
    std::vector<Job> js = jobs;
    int64_t i = int64_t(0);
    while ((i < js.size())) {
        int64_t j = int64_t(0);
        while ((j < ((js.size() - i) - int64_t(1)))) {
            Job a = _index(js, j);
            Job b = _index(js, (j + int64_t(1)));
            if ((a.profit < b.profit)) {
                js[j] = b;
                js[(j + int64_t(1))] = a;
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return js;
}

int64_t max_deadline(const std::vector<Job>& jobs) {
    int64_t max_d = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < jobs.size())) {
        Job job = _index(jobs, i);
        int64_t d = job.deadline;
        if ((d > max_d)) {
            max_d = d;
        }
        i = (i + int64_t(1));
    }
    return max_d;
}

std::vector<int64_t> job_sequencing_with_deadlines(const std::vector<Job>& jobs) {
    std::vector<Job> js = sort_jobs_by_profit(jobs);
    int64_t max_d = max_deadline(js);
    std::vector<int64_t> time_slots = {};
    int64_t t = int64_t(0);
    while ((t < max_d)) {
        time_slots = ([&]{ auto __tmp = time_slots; __tmp.push_back((int64_t(0) - int64_t(1))); return __tmp; }());
        t = (t + int64_t(1));
    }
    int64_t count = int64_t(0);
    int64_t max_profit = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < js.size())) {
        Job job = _index(js, i);
        int64_t j = (job.deadline - int64_t(1));
        while ((j >= int64_t(0))) {
            if ((_index(time_slots, j) == (int64_t(0) - int64_t(1)))) {
                time_slots[j] = job.id;
                count = (count + int64_t(1));
                max_profit = (max_profit + job.profit);
                break;
            }
            j = (j - int64_t(1));
        }
        i = (i + int64_t(1));
    }
    std::vector<int64_t> result = {};
    result = ([&]{ auto __tmp = result; __tmp.push_back(count); return __tmp; }());
    result = ([&]{ auto __tmp = result; __tmp.push_back(max_profit); return __tmp; }());
    return result;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        jobs1 = ([&]{ auto __tmp = jobs1; __tmp.push_back(Job{.id = int64_t(1), .deadline = int64_t(4), .profit = int64_t(20)}); return __tmp; }());
        jobs1 = ([&]{ auto __tmp = jobs1; __tmp.push_back(Job{.id = int64_t(2), .deadline = int64_t(1), .profit = int64_t(10)}); return __tmp; }());
        jobs1 = ([&]{ auto __tmp = jobs1; __tmp.push_back(Job{.id = int64_t(3), .deadline = int64_t(1), .profit = int64_t(40)}); return __tmp; }());
        jobs1 = ([&]{ auto __tmp = jobs1; __tmp.push_back(Job{.id = int64_t(4), .deadline = int64_t(1), .profit = int64_t(30)}); return __tmp; }());
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = job_sequencing_with_deadlines(jobs1); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        jobs2 = ([&]{ auto __tmp = jobs2; __tmp.push_back(Job{.id = int64_t(1), .deadline = int64_t(2), .profit = int64_t(100)}); return __tmp; }());
        jobs2 = ([&]{ auto __tmp = jobs2; __tmp.push_back(Job{.id = int64_t(2), .deadline = int64_t(1), .profit = int64_t(19)}); return __tmp; }());
        jobs2 = ([&]{ auto __tmp = jobs2; __tmp.push_back(Job{.id = int64_t(3), .deadline = int64_t(2), .profit = int64_t(27)}); return __tmp; }());
        jobs2 = ([&]{ auto __tmp = jobs2; __tmp.push_back(Job{.id = int64_t(4), .deadline = int64_t(1), .profit = int64_t(25)}); return __tmp; }());
        jobs2 = ([&]{ auto __tmp = jobs2; __tmp.push_back(Job{.id = int64_t(5), .deadline = int64_t(1), .profit = int64_t(15)}); return __tmp; }());
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = job_sequencing_with_deadlines(jobs2); ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; ss << __tmp[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
