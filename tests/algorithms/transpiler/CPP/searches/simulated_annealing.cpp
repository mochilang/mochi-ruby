// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:25:40 GMT+7
#include <iostream>
#include <string>
#include <functional>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> decltype(auto) _index(V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
template<typename V> decltype(auto) _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}
struct SearchProblem;

std::ostream& operator<<(std::ostream&, const SearchProblem&);


struct SearchProblem {
    double x;
    double y;
    double step;
    auto operator<=>(const SearchProblem&) const = default;
};

std::ostream& operator<<(std::ostream& os, const SearchProblem& v) {
    os << '{' << "'x': "<< std::fixed << std::setprecision(1) << v.x
 << ", " << "'y': "<< std::fixed << std::setprecision(1) << v.y
 << ", " << "'step': "<< std::fixed << std::setprecision(1) << v.step
 << '}';
    return os;
}

double score(const SearchProblem& p, std::function<double(double, double)> f);
std::vector<SearchProblem> get_neighbors(const SearchProblem& p);
std::vector<SearchProblem> remove_at(const std::vector<SearchProblem>& lst, int64_t idx);
int64_t _rand();
double random_float();
int64_t randint(int64_t low, int64_t high);
double expApprox(double x);
SearchProblem simulated_annealing(const SearchProblem& search_prob, std::function<double(double, double)> f, bool find_max, double max_x, double min_x, double max_y, double min_y, double start_temp, double rate_of_decrease, double threshold_temp);
double test_f1(double x, double y);
double test_f2(double x, double y);
int main();

int64_t seed = 0;

double score(const SearchProblem& p, std::function<double(double, double)> f) {
    return f(p.x, p.y);
}

std::vector<SearchProblem> get_neighbors(const SearchProblem& p) {
    double s = p.step;
    std::vector<SearchProblem> ns = {};
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = (p.x - s), .y = (p.y - s), .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = (p.x - s), .y = p.y, .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = (p.x - s), .y = (p.y + s), .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = p.x, .y = (p.y - s), .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = p.x, .y = (p.y + s), .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = (p.x + s), .y = (p.y - s), .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = (p.x + s), .y = p.y, .step = s}); return __tmp; }());
    ns = ([&]{ auto __tmp = ns; __tmp.push_back(SearchProblem{.x = (p.x + s), .y = (p.y + s), .step = s}); return __tmp; }());
    return ns;
}

std::vector<SearchProblem> remove_at(const std::vector<SearchProblem>& lst, int64_t idx) {
    std::vector<SearchProblem> res = {};
    int64_t i = int64_t(0);
    while ((i < lst.size())) {
        if ((i != idx)) {
            res = ([&]{ auto __tmp = res; __tmp.push_back(_index(lst, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return res;
}

int64_t _rand() {
    int64_t _t = _now();
    seed = (((seed * int64_t(1103515245)) + int64_t(12345)) % int64_t(2147483648));
    return seed;
}

double random_float() {
    return ((double)((double)(_rand())) / (2147483648.0));
}

int64_t randint(int64_t low, int64_t high) {
    return ((_rand() % ((high - low) + int64_t(1))) + low);
}

double expApprox(double x) {
    double y = x;
    bool is_neg = false;
    if ((x < 0.0)) {
        is_neg = true;
        y = -x;
    }
    double term = 1.0;
    double sum = 1.0;
    int64_t n = int64_t(1);
    while ((n < int64_t(30))) {
        term = ((double)((term * y)) / ((double)(n)));
        sum = (sum + term);
        n = (n + int64_t(1));
    }
    if (is_neg) {
        return ((double)(1.0) / (sum));
    }
    return sum;
}

SearchProblem simulated_annealing(const SearchProblem& search_prob, std::function<double(double, double)> f, bool find_max, double max_x, double min_x, double max_y, double min_y, double start_temp, double rate_of_decrease, double threshold_temp) {
    bool search_end = false;
    SearchProblem current_state = search_prob;
    double current_temp = start_temp;
    SearchProblem best_state = current_state;
    while (!search_end) {
        double current_score = score(current_state, f);
        if ((score(best_state, f) < current_score)) {
            best_state = current_state;
        }
        SearchProblem next_state = current_state;
        bool found_next = false;
        std::vector<SearchProblem> neighbors = get_neighbors(current_state);
        while ((!found_next && (neighbors.size() > int64_t(0)))) {
            int64_t idx = randint(int64_t(0), (neighbors.size() - int64_t(1)));
            SearchProblem picked_neighbor = _index(neighbors, idx);
            neighbors = remove_at(neighbors, idx);
            if (((((picked_neighbor.x > max_x) || (picked_neighbor.x < min_x)) || (picked_neighbor.y > max_y)) || (picked_neighbor.y < min_y))) {
                continue;
            }
            double change = (score(picked_neighbor, f) - current_score);
            if (!find_max) {
                change = -change;
            }
            if ((change > 0.0)) {
                next_state = picked_neighbor;
                found_next = true;
            } else {
                double probability = expApprox(((double)(change) / (current_temp)));
                if ((random_float() < probability)) {
                    next_state = picked_neighbor;
                    found_next = true;
                }
            }
        }
        current_temp = (current_temp - (current_temp * rate_of_decrease));
        if (((current_temp < threshold_temp) || !found_next)) {
            search_end = true;
        } else {
            current_state = next_state;
        }
    }
    return best_state;
}

double test_f1(double x, double y) {
    return ((x * x) + (y * y));
}

double test_f2(double x, double y) {
    return (((3.0 * x) * x) - (6.0 * y));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        SearchProblem prob1 = SearchProblem{.x = 12.0, .y = 47.0, .step = 1.0};
        SearchProblem min_state = simulated_annealing(prob1, test_f1, false, 100.0, 5.0, 50.0, -5.0, 100.0, 0.01, 1.0);
        std::cout << std::string("min1");
        std::cout << " ";
        { std::ostringstream __ss; double __dv = test_f1(min_state.x, min_state.y); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        SearchProblem prob2 = SearchProblem{.x = 12.0, .y = 47.0, .step = 1.0};
        SearchProblem max_state = simulated_annealing(prob2, test_f1, true, 100.0, 5.0, 50.0, -5.0, 100.0, 0.01, 1.0);
        std::cout << std::string("max1");
        std::cout << " ";
        { std::ostringstream __ss; double __dv = test_f1(max_state.x, max_state.y); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        SearchProblem prob3 = SearchProblem{.x = 3.0, .y = 4.0, .step = 1.0};
        SearchProblem min_state2 = simulated_annealing(prob3, test_f2, false, 1000.0, -1000.0, 1000.0, -1000.0, 100.0, 0.01, 1.0);
        std::cout << std::string("min2");
        std::cout << " ";
        { std::ostringstream __ss; double __dv = test_f2(min_state2.x, min_state2.y); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        SearchProblem prob4 = SearchProblem{.x = 3.0, .y = 4.0, .step = 1.0};
        SearchProblem max_state2 = simulated_annealing(prob4, test_f2, true, 1000.0, -1000.0, 1000.0, -1000.0, 100.0, 0.01, 1.0);
        std::cout << std::string("max2");
        std::cout << " ";
        { std::ostringstream __ss; double __dv = test_f2(max_state2.x, max_state2.y); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
    }
    return 0;
}
