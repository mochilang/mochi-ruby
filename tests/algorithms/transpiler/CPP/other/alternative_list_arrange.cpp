// Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:43:28 GMT+7
#include <iostream>
#include <string>
#include <memory>
#include <sstream>
#include <vector>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static std::string _format_double(double v) {
    std::ostringstream ss;
    ss << std::defaultfloat << std::setprecision(std::numeric_limits<double>::max_digits10) << v;
    auto s = ss.str();
    auto epos = s.find('e');
    if(epos == std::string::npos) epos = s.find('E');
    std::string exp;
    if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
    auto pos = s.find('.');
    if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
    return s + exp;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << _format_double(std::any_cast<double>(val));
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) return _format_double(std::any_cast<double>(v));
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        return _format_double(v);
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Item;
struct Int;
struct Str;

std::ostream& operator<<(std::ostream&, const Item&);
std::ostream& operator<<(std::ostream&, const Int&);
std::ostream& operator<<(std::ostream&, const Str&);


struct Item {
    virtual ~Item() = default;
};

struct Int : Item {
    int64_t value;
    Int(int64_t value_) : value(value_) {}
    auto operator<=>(const Int&) const = default;
};

struct Str : Item {
    std::string value;
    Str(std::string value_) : value(value_) {}
    auto operator<=>(const Str&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Int& v) {
    os << '{' << "'value': "<< v.value
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const Str& v) {
    os << '{' << "'value': "<< "'" << v.value << "'"
 << '}';
    return os;
}

std::shared_ptr<Item> from_int(int64_t x);
std::shared_ptr<Item> from_string(std::string s);
std::string item_to_string(Item* it);
std::vector<std::shared_ptr<Item>> alternative_list_arrange(const std::vector<std::shared_ptr<Item>>& first, const std::vector<std::shared_ptr<Item>>& second);
std::string list_to_string(const std::vector<std::shared_ptr<Item>>& xs);
int main();

std::vector<std::shared_ptr<Item>> example1 = std::vector<std::shared_ptr<Item>>{};
std::vector<std::shared_ptr<Item>> example2 = std::vector<std::shared_ptr<Item>>{};
std::vector<std::shared_ptr<Item>> example3 = std::vector<std::shared_ptr<Item>>{};
std::vector<std::shared_ptr<Item>> example4 = std::vector<std::shared_ptr<Item>>{};

std::shared_ptr<Item> from_int(int64_t x) {
    return std::static_pointer_cast<Item>(std::make_shared<Int>(x));
}

std::shared_ptr<Item> from_string(std::string s) {
    return std::static_pointer_cast<Item>(std::make_shared<Str>(s));
}

std::string item_to_string(Item* it) {
    return ([&]{
    const Int* __int = dynamic_cast<const Int*>(it);
    if (__int) {
        int64_t v = __int->value;
        return ([&]{ std::ostringstream ss; ss << std::boolalpha << v; return ss.str(); }());
    }
    const Str* __str = dynamic_cast<const Str*>(it);
    if (__str) {
        std::string s = __str->value;
        return s;
    }
    return std::string{};
}());
}

std::vector<std::shared_ptr<Item>> alternative_list_arrange(const std::vector<std::shared_ptr<Item>>& first, const std::vector<std::shared_ptr<Item>>& second) {
    int64_t len1 = first.size();
    int64_t len2 = second.size();
    int64_t abs_len = ((len1 > len2) ? len1 : len2);
    std::vector<std::shared_ptr<Item>> result = {};
    int64_t i = int64_t(0);
    while ((i < abs_len)) {
        if ((i < len1)) {
            result = ([&]{ auto __tmp = result; __tmp.push_back(_index(first, i)); return __tmp; }());
        }
        if ((i < len2)) {
            result = ([&]{ auto __tmp = result; __tmp.push_back(_index(second, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return result;
}

std::string list_to_string(const std::vector<std::shared_ptr<Item>>& xs) {
    std::string s = std::string("[");
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        s = (s + item_to_string(_index(xs, i).get()));
        if ((i < (xs.size() - int64_t(1)))) {
            s = (s + std::string(", "));
        }
        i = (i + int64_t(1));
    }
    s = (s + std::string("]"));
    return s;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        example1 = alternative_list_arrange(std::vector<std::shared_ptr<Item>>{from_int(int64_t(1)), from_int(int64_t(2)), from_int(int64_t(3)), from_int(int64_t(4)), from_int(int64_t(5))}, std::vector<std::shared_ptr<Item>>{from_string(std::string("A")), from_string(std::string("B")), from_string(std::string("C"))});
        std::cout << list_to_string(example1);
        std::cout << std::endl;
        example2 = alternative_list_arrange(std::vector<std::shared_ptr<Item>>{from_string(std::string("A")), from_string(std::string("B")), from_string(std::string("C"))}, std::vector<std::shared_ptr<Item>>{from_int(int64_t(1)), from_int(int64_t(2)), from_int(int64_t(3)), from_int(int64_t(4)), from_int(int64_t(5))});
        std::cout << list_to_string(example2);
        std::cout << std::endl;
        example3 = alternative_list_arrange(std::vector<std::shared_ptr<Item>>{from_string(std::string("X")), from_string(std::string("Y")), from_string(std::string("Z"))}, std::vector<std::shared_ptr<Item>>{from_int(int64_t(9)), from_int(int64_t(8)), from_int(int64_t(7)), from_int(int64_t(6))});
        std::cout << list_to_string(example3);
        std::cout << std::endl;
        example4 = alternative_list_arrange(std::vector<std::shared_ptr<Item>>{from_int(int64_t(1)), from_int(int64_t(2)), from_int(int64_t(3)), from_int(int64_t(4)), from_int(int64_t(5))}, std::vector<std::shared_ptr<Item>>{});
        std::cout << list_to_string(example4);
        std::cout << std::endl;
    }
    return 0;
}
