// Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:46:15 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <algorithm>
#include <type_traits>
#include <map>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <any>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static std::string _format_double(double v) {
    std::ostringstream ss;
    ss << std::defaultfloat << std::setprecision(std::numeric_limits<double>::max_digits10) << v;
    auto s = ss.str();
    auto epos = s.find('e');
    if(epos == std::string::npos) epos = s.find('E');
    std::string exp;
    if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
    auto pos = s.find('.');
    if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
    return s + exp;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << _format_double(std::any_cast<double>(val));
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) return _format_double(std::any_cast<double>(v));
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        return _format_double(v);
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct NumberContainer;

std::ostream& operator<<(std::ostream&, const NumberContainer&);


struct NumberContainer {
    std::map<int64_t, std::vector<int64_t>> numbermap;
    std::map<int64_t, int64_t> indexmap;
    auto operator<=>(const NumberContainer&) const = default;
};

std::ostream& operator<<(std::ostream& os, const NumberContainer& v) {
    os << '{' << "'numbermap': "<< "{"; bool first_0=true; for(const auto& p: v.numbermap){ if(!first_0) os << ", "; first_0=false; os << p.first; os << ': '; os << p.second; } os << "}"
 << ", " << "'indexmap': "<< "{"; bool first_1=true; for(const auto& p: v.indexmap){ if(!first_1) os << ", "; first_1=false; os << p.first; os << ': '; os << p.second; } os << "}"
 << '}';
    return os;
}

std::vector<int64_t> remove_at(const std::vector<int64_t>& xs, int64_t idx);
std::vector<int64_t> insert_at(const std::vector<int64_t>& xs, int64_t idx, int64_t val);
std::vector<int64_t> binary_search_delete(const std::vector<int64_t>& array, int64_t item);
std::vector<int64_t> binary_search_insert(const std::vector<int64_t>& array, int64_t index);
NumberContainer change(const NumberContainer& cont, int64_t idx, int64_t num);
int64_t find(const NumberContainer& cont, int64_t num);
int main();

std::map<int64_t, std::vector<int64_t>> nm = {};
std::map<int64_t, int64_t> im = {};
NumberContainer cont = NumberContainer{};

std::vector<int64_t> remove_at(const std::vector<int64_t>& xs, int64_t idx) {
    std::vector<int64_t> res = {};
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((i != idx)) {
            res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<int64_t> insert_at(const std::vector<int64_t>& xs, int64_t idx, int64_t val) {
    std::vector<int64_t> res = {};
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((i == idx)) {
            res = ([&]{ auto __tmp = res; __tmp.push_back(val); return __tmp; }());
        }
        res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    if ((idx == xs.size())) {
        res = ([&]{ auto __tmp = res; __tmp.push_back(val); return __tmp; }());
    }
    return res;
}

std::vector<int64_t> binary_search_delete(const std::vector<int64_t>& array, int64_t item) {
    int64_t low = int64_t(0);
    int64_t high = (array.size() - int64_t(1));
    std::vector<int64_t> arr = array;
    while ((low <= high)) {
        int64_t mid = ((low + high) / int64_t(2));
        if ((_index(arr, mid) == item)) {
            arr = remove_at(arr, mid);
            return arr;
        } else         if ((_index(arr, mid) < item)) {
            low = (mid + int64_t(1));
        } else {
            high = (mid - int64_t(1));
        }

    }
    std::cout << std::string("ValueError: Either the item is not in the array or the array was unsorted");
    std::cout << std::endl;
    return arr;
}

std::vector<int64_t> binary_search_insert(const std::vector<int64_t>& array, int64_t index) {
    int64_t low = int64_t(0);
    int64_t high = (array.size() - int64_t(1));
    std::vector<int64_t> arr = array;
    while ((low <= high)) {
        int64_t mid = ((low + high) / int64_t(2));
        if ((_index(arr, mid) == index)) {
            arr = insert_at(arr, (mid + int64_t(1)), index);
            return arr;
        } else         if ((_index(arr, mid) < index)) {
            low = (mid + int64_t(1));
        } else {
            high = (mid - int64_t(1));
        }

    }
    arr = insert_at(arr, low, index);
    return arr;
}

NumberContainer change(const NumberContainer& cont, int64_t idx, int64_t num) {
    std::map<int64_t, std::vector<int64_t>> numbermap = cont.numbermap;
    std::map<int64_t, int64_t> indexmap = cont.indexmap;
    if (([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(indexmap, idx)) {
        int64_t old = ([&](const auto& __m){ auto __it = __m.find(idx); if (__it == __m.end()) return int64_t{}; return __it->second; })(indexmap);
        std::vector<int64_t> indexes = ([&](const auto& __m){ auto __it = __m.find(old); if (__it == __m.end()) return std::vector<int64_t>{}; return __it->second; })(numbermap);
        if ((indexes.size() == int64_t(1))) {
            numbermap[static_cast<size_t>(old)] = std::vector<int64_t>{};
        } else {
            numbermap[static_cast<size_t>(old)] = binary_search_delete(indexes, idx);
        }
    }
    indexmap[static_cast<size_t>(idx)] = num;
    if (([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(numbermap, num)) {
        numbermap[static_cast<size_t>(num)] = binary_search_insert(([&](const auto& __m){ auto __it = __m.find(num); if (__it == __m.end()) return std::vector<int64_t>{}; return __it->second; })(numbermap), idx);
    } else {
        numbermap[static_cast<size_t>(num)] = std::vector<int64_t>{idx};
    }
    return NumberContainer{numbermap, indexmap};
}

int64_t find(const NumberContainer& cont, int64_t num) {
    std::map<int64_t, std::vector<int64_t>> numbermap = cont.numbermap;
    if (([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(numbermap, num)) {
        std::vector<int64_t> arr = ([&](const auto& __m){ auto __it = __m.find(num); if (__it == __m.end()) return std::vector<int64_t>{}; return __it->second; })(numbermap);
        if ((arr.size() > int64_t(0))) {
            return _index(arr, int64_t(0));
        }
    }
    return -int64_t(1);
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        cont = NumberContainer{nm, im};
        std::cout << find(cont, int64_t(10));
        std::cout << std::endl;
        cont = change(cont, int64_t(0), int64_t(10));
        std::cout << find(cont, int64_t(10));
        std::cout << std::endl;
        cont = change(cont, int64_t(0), int64_t(20));
        std::cout << find(cont, int64_t(10));
        std::cout << std::endl;
        std::cout << find(cont, int64_t(20));
        std::cout << std::endl;
    }
    return 0;
}
