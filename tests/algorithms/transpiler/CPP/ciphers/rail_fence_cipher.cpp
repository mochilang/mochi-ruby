// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:30:33 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::string encrypt(std::string input_string, int64_t key);
std::string decrypt(std::string input_string, int64_t key);
std::map<int64_t, std::string> bruteforce(std::string input_string);
int main();

std::map<int64_t, std::string> bf = bruteforce(std::string("HWe olordll"));

std::string encrypt(std::string input_string, int64_t key) {
    if ((key <= int64_t(0))) {
        throw std::runtime_error(std::string("Height of grid can't be 0 or negative"));
    }
    if (((key == int64_t(1)) || (input_string.size() <= key))) {
        return input_string;
    }
    int64_t lowest = (key - int64_t(1));
    std::vector<std::vector<std::string>> temp_grid = {};
    int64_t i = int64_t(0);
    while ((i < key)) {
        temp_grid = ([&]{ auto __tmp = temp_grid; __tmp.push_back(std::vector<std::string>{}); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t position = int64_t(0);
    while ((position < input_string.size())) {
        int64_t num = (position % (lowest * int64_t(2)));
        int64_t alt = ((lowest * int64_t(2)) - num);
        if ((num > alt)) {
            num = alt;
        }
        std::vector<std::string> row = temp_grid[num];
        row = ([&]{ auto __tmp = row; __tmp.push_back(input_string.substr(position, (position + int64_t(1)) - position)); return __tmp; }());
        temp_grid[num] = row;
        position = (position + int64_t(1));
    }
    std::string output = std::string("");
    i = int64_t(0);
    while ((i < key)) {
        std::vector<std::string> row = temp_grid[i];
        int64_t j = int64_t(0);
        while ((j < row.size())) {
            output = (output + row[j]);
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return output;
}

std::string decrypt(std::string input_string, int64_t key) {
    if ((key <= int64_t(0))) {
        throw std::runtime_error(std::string("Height of grid can't be 0 or negative"));
    }
    if ((key == int64_t(1))) {
        return input_string;
    }
    int64_t lowest = (key - int64_t(1));
    std::vector<int64_t> counts = {};
    int64_t i = int64_t(0);
    while ((i < key)) {
        counts = ([&]{ auto __tmp = counts; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t pos = int64_t(0);
    while ((pos < input_string.size())) {
        int64_t num = (pos % (lowest * int64_t(2)));
        int64_t alt = ((lowest * int64_t(2)) - num);
        if ((num > alt)) {
            num = alt;
        }
        counts[num] = (counts[num] + int64_t(1));
        pos = (pos + int64_t(1));
    }
    std::vector<std::vector<std::string>> grid = {};
    int64_t counter = int64_t(0);
    i = int64_t(0);
    while ((i < key)) {
        int64_t length = counts[i];
        std::string slice = input_string.substr(counter, (counter + length) - counter);
        std::vector<std::string> row = {};
        int64_t j = int64_t(0);
        while ((j < slice.size())) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(std::string(1, slice[j])); return __tmp; }());
            j = (j + int64_t(1));
        }
        grid = ([&]{ auto __tmp = grid; __tmp.push_back(row); return __tmp; }());
        counter = (counter + length);
        i = (i + int64_t(1));
    }
    std::vector<int64_t> indices = {};
    i = int64_t(0);
    while ((i < key)) {
        indices = ([&]{ auto __tmp = indices; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::string output = std::string("");
    pos = int64_t(0);
    while ((pos < input_string.size())) {
        int64_t num = (pos % (lowest * int64_t(2)));
        int64_t alt = ((lowest * int64_t(2)) - num);
        if ((num > alt)) {
            num = alt;
        }
        output = (output + grid[num][indices[num]]);
        indices[num] = (indices[num] + int64_t(1));
        pos = (pos + int64_t(1));
    }
    return output;
}

std::map<int64_t, std::string> bruteforce(std::string input_string) {
    std::map<int64_t, std::string> results = std::map<int64_t, std::string>{};
    int64_t key_guess = int64_t(1);
    while ((key_guess < input_string.size())) {
        results[key_guess] = decrypt(input_string, key_guess);
        key_guess = (key_guess + int64_t(1));
    }
    return results;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << encrypt(std::string("Hello World"), int64_t(4));
        std::cout << std::endl;
        std::cout << decrypt(std::string("HWe olordll"), int64_t(4));
        std::cout << std::endl;
        std::cout << ([](const auto& __m){ auto __it = __m.find(int64_t(4)); return __it != __m.end() ? __it->second : ""; })(bf);
        std::cout << std::endl;
    }
    return 0;
}
