// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:30:20 GMT+7
#include <iostream>
#include <string>
#include <algorithm>
#include <type_traits>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

bool contains(const std::vector<std::string>& xs, std::string x);
int64_t index_of(const std::vector<std::string>& xs, std::string x);
std::string prepare_input(std::string dirty);
std::vector<std::string> generate_table(std::string key);
std::string encode(std::string plaintext, std::string key);
std::string decode(std::string cipher, std::string key);
int main();

bool contains(const std::vector<std::string>& xs, std::string x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((xs[i] == x)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

int64_t index_of(const std::vector<std::string>& xs, std::string x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((xs[i] == x)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

std::string prepare_input(std::string dirty) {
    std::string letters = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
    std::string upper_dirty = ([&]{ std::string __s = dirty; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    std::string filtered = std::string("");
    int64_t i = int64_t(0);
    while ((i < upper_dirty.size())) {
        std::string c = upper_dirty.substr(i, (i + int64_t(1)) - i);
        if (([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(letters, c)) {
            filtered = (filtered + c);
        }
        i = (i + int64_t(1));
    }
    if ((filtered.size() < int64_t(2))) {
        return filtered;
    }
    std::string clean = std::string("");
    i = int64_t(0);
    while ((i < (filtered.size() - int64_t(1)))) {
        std::string c1 = filtered.substr(i, (i + int64_t(1)) - i);
        std::string c2 = filtered.substr((i + int64_t(1)), (i + int64_t(2)) - (i + int64_t(1)));
        clean = (clean + c1);
        if ((c1 == c2)) {
            clean = (clean + std::string("X"));
        }
        i = (i + int64_t(1));
    }
    clean = (clean + filtered.substr((filtered.size() - int64_t(1)), filtered.size() - (filtered.size() - int64_t(1))));
    if (((clean.size() % int64_t(2)) == int64_t(1))) {
        clean = (clean + std::string("X"));
    }
    return clean;
}

std::vector<std::string> generate_table(std::string key) {
    std::string alphabet = std::string("ABCDEFGHIKLMNOPQRSTUVWXYZ");
    std::vector<std::string> table = {};
    std::string upper_key = ([&]{ std::string __s = key; for(auto &__c : __s){ __c = std::toupper(static_cast<unsigned char>(__c)); } return __s; }());
    int64_t i = int64_t(0);
    while ((i < upper_key.size())) {
        std::string c = upper_key.substr(i, (i + int64_t(1)) - i);
        if (([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(alphabet, c)) {
            if (!contains(table, c)) {
                table = ([&]{ auto __tmp = table; __tmp.push_back(c); return __tmp; }());
            }
        }
        i = (i + int64_t(1));
    }
    i = int64_t(0);
    while ((i < alphabet.size())) {
        std::string c = alphabet.substr(i, (i + int64_t(1)) - i);
        if (!contains(table, c)) {
            table = ([&]{ auto __tmp = table; __tmp.push_back(c); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return table;
}

std::string encode(std::string plaintext, std::string key) {
    std::vector<std::string> table = generate_table(key);
    std::string text = prepare_input(plaintext);
    std::string cipher = std::string("");
    int64_t i = int64_t(0);
    while ((i < text.size())) {
        std::string c1 = text.substr(i, (i + int64_t(1)) - i);
        std::string c2 = text.substr((i + int64_t(1)), (i + int64_t(2)) - (i + int64_t(1)));
        int64_t idx1 = index_of(table, c1);
        int64_t idx2 = index_of(table, c2);
        int64_t row1 = (idx1 / int64_t(5));
        int64_t col1 = (idx1 % int64_t(5));
        int64_t row2 = (idx2 / int64_t(5));
        int64_t col2 = (idx2 % int64_t(5));
        if ((row1 == row2)) {
            cipher = (cipher + table[((row1 * int64_t(5)) + ((col1 + int64_t(1)) % int64_t(5)))]);
            cipher = (cipher + table[((row2 * int64_t(5)) + ((col2 + int64_t(1)) % int64_t(5)))]);
        } else         if ((col1 == col2)) {
            cipher = (cipher + table[((((row1 + int64_t(1)) % int64_t(5)) * int64_t(5)) + col1)]);
            cipher = (cipher + table[((((row2 + int64_t(1)) % int64_t(5)) * int64_t(5)) + col2)]);
        } else {
            cipher = (cipher + table[((row1 * int64_t(5)) + col2)]);
            cipher = (cipher + table[((row2 * int64_t(5)) + col1)]);
        }

        i = (i + int64_t(2));
    }
    return cipher;
}

std::string decode(std::string cipher, std::string key) {
    std::vector<std::string> table = generate_table(key);
    std::string plain = std::string("");
    int64_t i = int64_t(0);
    while ((i < cipher.size())) {
        std::string c1 = cipher.substr(i, (i + int64_t(1)) - i);
        std::string c2 = cipher.substr((i + int64_t(1)), (i + int64_t(2)) - (i + int64_t(1)));
        int64_t idx1 = index_of(table, c1);
        int64_t idx2 = index_of(table, c2);
        int64_t row1 = (idx1 / int64_t(5));
        int64_t col1 = (idx1 % int64_t(5));
        int64_t row2 = (idx2 / int64_t(5));
        int64_t col2 = (idx2 % int64_t(5));
        if ((row1 == row2)) {
            plain = (plain + table[((row1 * int64_t(5)) + ((col1 + int64_t(4)) % int64_t(5)))]);
            plain = (plain + table[((row2 * int64_t(5)) + ((col2 + int64_t(4)) % int64_t(5)))]);
        } else         if ((col1 == col2)) {
            plain = (plain + table[((((row1 + int64_t(4)) % int64_t(5)) * int64_t(5)) + col1)]);
            plain = (plain + table[((((row2 + int64_t(4)) % int64_t(5)) * int64_t(5)) + col2)]);
        } else {
            plain = (plain + table[((row1 * int64_t(5)) + col2)]);
            plain = (plain + table[((row2 * int64_t(5)) + col1)]);
        }

        i = (i + int64_t(2));
    }
    return plain;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << std::string("Encoded:");
        std::cout << " ";
        std::cout << encode(std::string("BYE AND THANKS"), std::string("GREETING"));
        std::cout << std::endl;
        std::cout << std::string("Decoded:");
        std::cout << " ";
        std::cout << decode(std::string("CXRBANRLBALQ"), std::string("GREETING"));
        std::cout << std::endl;
    }
    return 0;
}
