// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:25:35 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <type_traits>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static std::string _repeat(const std::string& s, int64_t n) {
    std::string out; out.reserve(s.size()*n);
    for(int64_t i=0;i<n;i++) out += s;
    return out;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int64_t indexOfChar(std::string s, std::string ch);
int64_t ord(std::string ch);
std::string chr(int64_t code);
std::string repeat(std::string s, int64_t n);
std::string to_binary(int64_t n, int64_t bits);
int64_t binary_to_int(std::string bits);
std::string base32_encode(std::string data);
std::string base32_decode(std::string data);
int main();

std::string B32_CHARSET = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ234567");

int64_t indexOfChar(std::string s, std::string ch) {
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        if ((std::string(1, s[i]) == ch)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

int64_t ord(std::string ch) {
    std::string upper = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
    std::string lower = std::string("abcdefghijklmnopqrstuvwxyz");
    std::string digits = std::string("0123456789");
    int64_t idx = indexOfChar(upper, ch);
    if ((idx >= int64_t(0))) {
        return (int64_t(65) + idx);
    }
    idx = indexOfChar(lower, ch);
    if ((idx >= int64_t(0))) {
        return (int64_t(97) + idx);
    }
    idx = indexOfChar(digits, ch);
    if ((idx >= int64_t(0))) {
        return (int64_t(48) + idx);
    }
    if ((ch == std::string(" "))) {
        return int64_t(32);
    }
    if ((ch == std::string("!"))) {
        return int64_t(33);
    }
    return int64_t(0);
}

std::string chr(int64_t code) {
    std::string upper = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
    std::string lower = std::string("abcdefghijklmnopqrstuvwxyz");
    std::string digits = std::string("0123456789");
    if ((code == int64_t(32))) {
        return std::string(" ");
    }
    if ((code == int64_t(33))) {
        return std::string("!");
    }
    int64_t idx = (code - int64_t(65));
    if (((idx >= int64_t(0)) && (idx < upper.size()))) {
        return std::string(1, upper[idx]);
    }
    idx = (code - int64_t(97));
    if (((idx >= int64_t(0)) && (idx < lower.size()))) {
        return std::string(1, lower[idx]);
    }
    idx = (code - int64_t(48));
    if (((idx >= int64_t(0)) && (idx < digits.size()))) {
        return std::string(1, digits[idx]);
    }
    return std::string("");
}

std::string repeat(std::string s, int64_t n) {
    std::string out = std::string("");
    int64_t i = int64_t(0);
    while ((i < n)) {
        out = (out + s);
        i = (i + int64_t(1));
    }
    return out;
}

std::string to_binary(int64_t n, int64_t bits) {
    int64_t v = n;
    std::string out = std::string("");
    int64_t i = int64_t(0);
    while ((i < bits)) {
        out = (([&]{ std::ostringstream ss; ss << std::boolalpha << (v % int64_t(2)); return ss.str(); }()) + out);
        v = (v / int64_t(2));
        i = (i + int64_t(1));
    }
    return out;
}

int64_t binary_to_int(std::string bits) {
    int64_t n = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < bits.size())) {
        n = (n * int64_t(2));
        if ((std::string(1, bits[i]) == std::string("1"))) {
            n = (n + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return n;
}

std::string base32_encode(std::string data) {
    std::string binary_data = std::string("");
    int64_t i = int64_t(0);
    while ((i < data.size())) {
        binary_data = (binary_data + to_binary(ord(std::string(1, data[i])), int64_t(8)));
        i = (i + int64_t(1));
    }
    int64_t remainder = (binary_data.size() % int64_t(5));
    if ((remainder != int64_t(0))) {
        binary_data = (binary_data + _repeat(std::string("0"), (int64_t(5) - remainder)));
    }
    std::string b32_result = std::string("");
    int64_t j = int64_t(0);
    while ((j < binary_data.size())) {
        std::string chunk = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(j, (j + int64_t(5)) - j); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+j, __v.begin()+(j + int64_t(5))); })(binary_data);
        int64_t index = binary_to_int(chunk);
        b32_result = (b32_result + std::string(1, B32_CHARSET[index]));
        j = (j + int64_t(5));
    }
    int64_t rem = (b32_result.size() % int64_t(8));
    if ((rem != int64_t(0))) {
        b32_result = (b32_result + _repeat(std::string("="), (int64_t(8) - rem)));
    }
    return b32_result;
}

std::string base32_decode(std::string data) {
    std::string clean = std::string("");
    int64_t i = int64_t(0);
    while ((i < data.size())) {
        std::string ch = std::string(1, data[i]);
        if ((ch != std::string("="))) {
            clean = (clean + ch);
        }
        i = (i + int64_t(1));
    }
    std::string binary_chunks = std::string("");
    i = int64_t(0);
    while ((i < clean.size())) {
        int64_t idx = indexOfChar(B32_CHARSET, std::string(1, clean[i]));
        binary_chunks = (binary_chunks + to_binary(idx, int64_t(5)));
        i = (i + int64_t(1));
    }
    std::string result = std::string("");
    int64_t j = int64_t(0);
    while (((j + int64_t(8)) <= binary_chunks.size())) {
        std::string byte_bits = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(j, (j + int64_t(8)) - j); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+j, __v.begin()+(j + int64_t(8))); })(binary_chunks);
        int64_t code = binary_to_int(byte_bits);
        result = (result + chr(code));
        j = (j + int64_t(8));
    }
    return result;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << base32_encode(std::string("Hello World!"));
        std::cout << std::endl;
        std::cout << base32_encode(std::string("123456"));
        std::cout << std::endl;
        std::cout << base32_encode(std::string("some long complex string"));
        std::cout << std::endl;
        std::cout << base32_decode(std::string("JBSWY3DPEBLW64TMMQQQ===="));
        std::cout << std::endl;
        std::cout << base32_decode(std::string("GEZDGNBVGY======"));
        std::cout << std::endl;
        std::cout << base32_decode(std::string("ONXW2ZJANRXW4ZZAMNXW24DMMV4CA43UOJUW4ZY="));
        std::cout << std::endl;
    }
    return 0;
}
