// Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:56:58 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int64_t gcd(int64_t a, int64_t b);
int64_t mod_inverse(int64_t a, int64_t m);
int64_t find_symbol(std::string ch);
void check_keys(int64_t key_a, int64_t key_b, std::string mode);
std::string encrypt_message(int64_t key, std::string message);
std::string decrypt_message(int64_t key, std::string message);
int main();

std::string SYMBOLS = std::string(" !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`abcdefghijklmnopqrstuvwxyz{|}~");

int64_t gcd(int64_t a, int64_t b) {
    int64_t x = a;
    int64_t y = b;
    while ((y != int64_t(0))) {
        int64_t temp = (x % y);
        x = y;
        y = temp;
    }
    return x;
}

int64_t mod_inverse(int64_t a, int64_t m) {
    if ((gcd(a, m) != int64_t(1))) {
        throw std::runtime_error(((((std::string("mod inverse of ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << a; return ss.str(); }())) + std::string(" and ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << m; return ss.str(); }())) + std::string(" does not exist")));
    }
    int64_t u1 = int64_t(1);
    int64_t u2 = int64_t(0);
    int64_t u3 = a;
    int64_t v1 = int64_t(0);
    int64_t v2 = int64_t(1);
    int64_t v3 = m;
    while ((v3 != int64_t(0))) {
        int64_t q = (u3 / v3);
        int64_t t1 = (u1 - (q * v1));
        int64_t t2 = (u2 - (q * v2));
        int64_t t3 = (u3 - (q * v3));
        u1 = v1;
        u2 = v2;
        u3 = v3;
        v1 = t1;
        v2 = t2;
        v3 = t3;
    }
    int64_t res = (u1 % m);
    if ((res < int64_t(0))) {
        return (res + m);
    }
    return res;
}

int64_t find_symbol(std::string ch) {
    int64_t i = int64_t(0);
    while ((i < SYMBOLS.size())) {
        if ((std::string(1, SYMBOLS[i]) == ch)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

void check_keys(int64_t key_a, int64_t key_b, std::string mode) {
    int m = SYMBOLS.size();
    if ((mode == std::string("encrypt"))) {
        if ((key_a == int64_t(1))) {
            throw std::runtime_error(std::string("The affine cipher becomes weak when key A is set to 1. Choose different key"));
        }
        if ((key_b == int64_t(0))) {
            throw std::runtime_error(std::string("The affine cipher becomes weak when key B is set to 0. Choose different key"));
        }
    }
    if ((((key_a < int64_t(0)) || (key_b < int64_t(0))) || (key_b > (m - int64_t(1))))) {
        throw std::runtime_error((std::string("Key A must be greater than 0 and key B must be between 0 and ") + ([&]{ std::ostringstream ss; any_to_stream(ss, (m - int64_t(1)));
 return ss.str(); }())));
    }
    if ((gcd(key_a, (int64_t)(m)) != int64_t(1))) {
        throw std::runtime_error(((((std::string("Key A ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << key_a; return ss.str(); }())) + std::string(" and the symbol set size ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << m; return ss.str(); }())) + std::string(" are not relatively prime. Choose a different key.")));
    }
}

std::string encrypt_message(int64_t key, std::string message) {
    int m = SYMBOLS.size();
    int64_t key_a = (key / m);
    int64_t key_b = (key % m);
    check_keys(key_a, key_b, std::string("encrypt"));
    std::string cipher_text = std::string("");
    int64_t i = int64_t(0);
    while ((i < message.size())) {
        std::string ch = std::string(1, message[i]);
        int64_t index = find_symbol(ch);
        if ((index >= int64_t(0))) {
            cipher_text = (cipher_text + std::string(1, SYMBOLS[static_cast<size_t>((((index * key_a) + key_b) % m))]));
        } else {
            cipher_text = (cipher_text + ch);
        }
        i = (i + int64_t(1));
    }
    return cipher_text;
}

std::string decrypt_message(int64_t key, std::string message) {
    int m = SYMBOLS.size();
    int64_t key_a = (key / m);
    int64_t key_b = (key % m);
    check_keys(key_a, key_b, std::string("decrypt"));
    int64_t inv = mod_inverse(key_a, (int64_t)(m));
    std::string plain_text = std::string("");
    int64_t i = int64_t(0);
    while ((i < message.size())) {
        std::string ch = std::string(1, message[i]);
        int64_t index = find_symbol(ch);
        if ((index >= int64_t(0))) {
            int64_t n = ((index - key_b) * inv);
            int64_t pos = (n % m);
            auto final = ((pos < int64_t(0)) ? (pos + m) : pos);
            plain_text = (plain_text + std::string(1, SYMBOLS[static_cast<size_t>(final)]));
        } else {
            plain_text = (plain_text + ch);
        }
        i = (i + int64_t(1));
    }
    return plain_text;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        int64_t key = int64_t(4545);
        std::string msg = std::string("The affine cipher is a type of monoalphabetic substitution cipher.");
        std::string enc = encrypt_message(key, msg);
        std::cout << enc;
        std::cout << std::endl;
        std::cout << decrypt_message(key, enc);
        std::cout << std::endl;
    }
    return 0;
}
