// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:41:23 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int64_t _rand(int64_t n);
std::string get_random_key();
bool check_valid_key(std::string key);
int64_t index_in(std::string s, std::string ch);
std::string char_to_upper(std::string c);
std::string char_to_lower(std::string c);
bool is_upper(std::string c);
std::string translate_message(std::string key, std::string message, std::string mode);
std::string encrypt_message(std::string key, std::string message);
std::string decrypt_message(std::string key, std::string message);
int main();

std::string LETTERS = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
std::string LOWERCASE = std::string("abcdefghijklmnopqrstuvwxyz");
int64_t seed = int64_t(1);
std::string key = std::string("LFWOAYUISVKMNXPBDCRJTQEGHZ");

int64_t _rand(int64_t n) {
    seed = (((seed * int64_t(1664525)) + int64_t(1013904223)) % int64_t(2147483647));
    return (seed % n);
}

std::string get_random_key() {
    std::vector<std::string> chars = std::vector<std::string>{};
    int64_t i = int64_t(0);
    while ((i < LETTERS.size())) {
        chars = ([&]{ auto __tmp = chars; __tmp.push_back(std::string(1, LETTERS[i])); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t j = (chars.size() - int64_t(1));
    while ((j > int64_t(0))) {
        int64_t k = _rand((j + int64_t(1)));
        std::string tmp = chars[j];
        chars[j] = chars[k];
        chars[k] = tmp;
        j = (j - int64_t(1));
    }
    std::string res = std::string("");
    i = int64_t(0);
    while ((i < chars.size())) {
        res = (res + chars[i]);
        i = (i + int64_t(1));
    }
    return res;
}

bool check_valid_key(std::string key) {
    if ((key.size() != LETTERS.size())) {
        return false;
    }
    std::map<std::string, bool> used = std::map<std::string, bool>{};
    int64_t i = int64_t(0);
    while ((i < key.size())) {
        std::string ch = std::string(1, key[i]);
        if (([&](const auto& __m){ auto __it = __m.find(ch); return __it != __m.end() ? __it->second : false; })(used)) {
            return false;
        }
        used[ch] = true;
        i = (i + int64_t(1));
    }
    i = int64_t(0);
    while ((i < LETTERS.size())) {
        std::string ch = std::string(1, LETTERS[i]);
        if (!([&](const auto& __m){ auto __it = __m.find(ch); return __it != __m.end() ? __it->second : false; })(used)) {
            return false;
        }
        i = (i + int64_t(1));
    }
    return true;
}

int64_t index_in(std::string s, std::string ch) {
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        if ((std::string(1, s[i]) == ch)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

std::string char_to_upper(std::string c) {
    int64_t i = int64_t(0);
    while ((i < LOWERCASE.size())) {
        if ((c == std::string(1, LOWERCASE[i]))) {
            return std::string(1, LETTERS[i]);
        }
        i = (i + int64_t(1));
    }
    return c;
}

std::string char_to_lower(std::string c) {
    int64_t i = int64_t(0);
    while ((i < LETTERS.size())) {
        if ((c == std::string(1, LETTERS[i]))) {
            return std::string(1, LOWERCASE[i]);
        }
        i = (i + int64_t(1));
    }
    return c;
}

bool is_upper(std::string c) {
    int64_t i = int64_t(0);
    while ((i < LETTERS.size())) {
        if ((c == std::string(1, LETTERS[i]))) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

std::string translate_message(std::string key, std::string message, std::string mode) {
    std::string chars_a = LETTERS;
    std::string chars_b = key;
    if ((mode == std::string("decrypt"))) {
        std::string tmp = chars_a;
        chars_a = chars_b;
        chars_b = tmp;
    }
    std::string translated = std::string("");
    int64_t i = int64_t(0);
    while ((i < message.size())) {
        std::string symbol = std::string(1, message[i]);
        std::string upper_symbol = char_to_upper(symbol);
        int64_t idx = index_in(chars_a, upper_symbol);
        if ((idx >= int64_t(0))) {
            std::string mapped = std::string(1, chars_b[idx]);
            if (is_upper(symbol)) {
                translated = (translated + mapped);
            } else {
                translated = (translated + char_to_lower(mapped));
            }
        } else {
            translated = (translated + symbol);
        }
        i = (i + int64_t(1));
    }
    return translated;
}

std::string encrypt_message(std::string key, std::string message) {
    std::string res = translate_message(key, message, std::string("encrypt"));
    return res;
}

std::string decrypt_message(std::string key, std::string message) {
    std::string res = translate_message(key, message, std::string("decrypt"));
    return res;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << encrypt_message(key, std::string("Harshil Darji"));
        std::cout << std::endl;
        std::cout << decrypt_message(key, std::string("Ilcrism Olcvs"));
        std::cout << std::endl;
    }
    return 0;
}
