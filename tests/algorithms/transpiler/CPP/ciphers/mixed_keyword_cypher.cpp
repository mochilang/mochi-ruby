// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:29:05 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::string to_upper(std::string s);
bool contains(const std::vector<std::string>& xs, std::string x);
bool contains_char(std::string s, std::string ch);
std::string get_value(const std::vector<std::string>& keys, const std::vector<std::string>& values, std::string key);
void print_mapping(const std::vector<std::string>& keys, const std::vector<std::string>& values);
std::string mixed_keyword(std::string keyword, std::string plaintext, bool verbose);
int main();

std::string UPPER = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
std::string LOWER = std::string("abcdefghijklmnopqrstuvwxyz");

std::string to_upper(std::string s) {
    std::string res = std::string("");
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        std::string ch = std::string(1, s[i]);
        int64_t j = int64_t(0);
        bool found = false;
        while ((j < int64_t(26))) {
            if ((ch == std::string(1, LOWER[j]))) {
                res = (res + std::string(1, UPPER[j]));
                found = true;
                break;
            }
            j = (j + int64_t(1));
        }
        if ((found == false)) {
            res = (res + ch);
        }
        i = (i + int64_t(1));
    }
    return res;
}

bool contains(const std::vector<std::string>& xs, std::string x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((xs[i] == x)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

bool contains_char(std::string s, std::string ch) {
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        if ((std::string(1, s[i]) == ch)) {
            return true;
        }
        i = (i + int64_t(1));
    }
    return false;
}

std::string get_value(const std::vector<std::string>& keys, const std::vector<std::string>& values, std::string key) {
    int64_t i = int64_t(0);
    while ((i < keys.size())) {
        if ((keys[i] == key)) {
            return values[i];
        }
        i = (i + int64_t(1));
    }
    return std::string("");
}

void print_mapping(const std::vector<std::string>& keys, const std::vector<std::string>& values) {
    std::string s = std::string("{");
    int64_t i = int64_t(0);
    while ((i < keys.size())) {
        s = (((((s + std::string("'")) + keys[i]) + std::string("': '")) + values[i]) + std::string("'"));
        if (((i + int64_t(1)) < keys.size())) {
            s = (s + std::string(", "));
        }
        i = (i + int64_t(1));
    }
    s = (s + std::string("}"));
    std::cout << s;
    std::cout << std::endl;
}

std::string mixed_keyword(std::string keyword, std::string plaintext, bool verbose) {
    std::string alphabet = UPPER;
    std::string keyword_u = to_upper(keyword);
    std::string plaintext_u = to_upper(plaintext);
    std::vector<std::string> unique = {};
    int64_t i = int64_t(0);
    while ((i < keyword_u.size())) {
        std::string ch = std::string(1, keyword_u[i]);
        if ((contains_char(alphabet, ch) && (contains(unique, ch) == false))) {
            unique = ([&]{ auto __tmp = unique; __tmp.push_back(ch); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    int num_unique = unique.size();
    std::vector<std::string> shifted = {};
    i = int64_t(0);
    while ((i < unique.size())) {
        shifted = ([&]{ auto __tmp = shifted; __tmp.push_back(unique[i]); return __tmp; }());
        i = (i + int64_t(1));
    }
    i = int64_t(0);
    while ((i < alphabet.size())) {
        std::string ch = std::string(1, alphabet[i]);
        if ((contains(unique, ch) == false)) {
            shifted = ([&]{ auto __tmp = shifted; __tmp.push_back(ch); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    std::vector<std::vector<std::string>> modified = {};
    int64_t k = int64_t(0);
    while ((k < shifted.size())) {
        std::vector<std::string> row = {};
        int64_t r = int64_t(0);
        while (((r < num_unique) && ((k + r) < shifted.size()))) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(shifted[(k + r)]); return __tmp; }());
            r = (r + int64_t(1));
        }
        modified = ([&]{ auto __tmp = modified; __tmp.push_back(row); return __tmp; }());
        k = (k + num_unique);
    }
    std::vector<std::string> keys = {};
    std::vector<std::string> values = {};
    int64_t column = int64_t(0);
    int64_t letter_index = int64_t(0);
    while ((column < num_unique)) {
        int64_t row_idx = int64_t(0);
        while ((row_idx < modified.size())) {
            std::vector<std::string> row = modified[row_idx];
            if ((row.size() <= column)) {
                break;
            }
            keys = ([&]{ auto __tmp = keys; __tmp.push_back(std::string(1, alphabet[letter_index])); return __tmp; }());
            values = ([&]{ auto __tmp = values; __tmp.push_back(row[column]); return __tmp; }());
            letter_index = (letter_index + int64_t(1));
            row_idx = (row_idx + int64_t(1));
        }
        column = (column + int64_t(1));
    }
    if (verbose) {
        print_mapping(keys, values);
    }
    std::string result = std::string("");
    i = int64_t(0);
    while ((i < plaintext_u.size())) {
        std::string ch = std::string(1, plaintext_u[i]);
        std::string mapped = get_value(keys, values, ch);
        if ((mapped == any_to_string(nullptr))) {
            result = (result + ch);
        } else {
            result = (result + mapped);
        }
        i = (i + int64_t(1));
    }
    return result;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << mixed_keyword(std::string("college"), std::string("UNIVERSITY"), true);
        std::cout << std::endl;
    }
    return 0;
}
