// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:30:26 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::string to_upper(std::string s);
int64_t char_index(std::string c);
std::string rotate_right(std::string s, int64_t k);
std::vector<std::string> table_for(std::string c);
std::vector<std::vector<std::string>> generate_table(std::string key);
int64_t str_index(std::string s, std::string ch);
std::vector<int64_t> get_position(const std::vector<std::string>& table, std::string ch);
std::string get_opponent(const std::vector<std::string>& table, std::string ch);
std::string encrypt(std::string key, std::string words);
std::string decrypt(std::string key, std::string words);
int main();

std::string UPPER = std::string("ABCDEFGHIJKLMNOPQRSTUVWXYZ");
std::string LOWER = std::string("abcdefghijklmnopqrstuvwxyz");
std::string BASE_TOP = std::string("ABCDEFGHIJKLM");
std::string BASE_BOTTOM = std::string("NOPQRSTUVWXYZ");

std::string to_upper(std::string s) {
    std::string res = std::string("");
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        std::string ch = s.substr(i, (i + int64_t(1)) - i);
        int64_t j = int64_t(0);
        bool replaced = false;
        while ((j < LOWER.size())) {
            if ((LOWER.substr(j, (j + int64_t(1)) - j) == ch)) {
                res = (res + UPPER.substr(j, (j + int64_t(1)) - j));
                replaced = true;
                break;
            }
            j = (j + int64_t(1));
        }
        if (!replaced) {
            res = (res + ch);
        }
        i = (i + int64_t(1));
    }
    return res;
}

int64_t char_index(std::string c) {
    int64_t i = int64_t(0);
    while ((i < UPPER.size())) {
        if ((UPPER.substr(i, (i + int64_t(1)) - i) == c)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

std::string rotate_right(std::string s, int64_t k) {
    int n = s.size();
    int64_t shift = (k % n);
    return (s.substr((n - shift), n - (n - shift)) + s.substr(int64_t(0), (n - shift) - int64_t(0)));
}

std::vector<std::string> table_for(std::string c) {
    int64_t idx = char_index(c);
    int64_t shift = (idx / int64_t(2));
    std::string row1 = rotate_right(BASE_BOTTOM, shift);
    std::vector<std::string> pair = std::vector<std::string>{BASE_TOP, row1};
    return pair;
}

std::vector<std::vector<std::string>> generate_table(std::string key) {
    std::string up = to_upper(key);
    int64_t i = int64_t(0);
    std::vector<std::vector<std::string>> result = {};
    while ((i < up.size())) {
        std::string ch = up.substr(i, (i + int64_t(1)) - i);
        std::vector<std::string> pair = table_for(ch);
        result = ([&]{ auto __tmp = result; __tmp.push_back(pair); return __tmp; }());
        i = (i + int64_t(1));
    }
    return result;
}

int64_t str_index(std::string s, std::string ch) {
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        if ((s.substr(i, (i + int64_t(1)) - i) == ch)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return (int64_t(0) - int64_t(1));
}

std::vector<int64_t> get_position(const std::vector<std::string>& table, std::string ch) {
    int64_t row = int64_t(0);
    if ((str_index(table[int64_t(0)], ch) == (int64_t(0) - int64_t(1)))) {
        row = int64_t(1);
    }
    int64_t col = str_index(table[row], ch);
    return std::vector<int64_t>{row, col};
}

std::string get_opponent(const std::vector<std::string>& table, std::string ch) {
    std::vector<int64_t> pos = get_position(table, ch);
    int64_t row = pos[int64_t(0)];
    int64_t col = pos[int64_t(1)];
    if ((col == (int64_t(0) - int64_t(1)))) {
        return ch;
    }
    if ((row == int64_t(1))) {
        return table[int64_t(0)].substr(col, (col + int64_t(1)) - col);
    }
    return table[int64_t(1)].substr(col, (col + int64_t(1)) - col);
}

std::string encrypt(std::string key, std::string words) {
    std::vector<std::vector<std::string>> table = generate_table(key);
    std::string up_words = to_upper(words);
    std::string cipher = std::string("");
    int64_t count = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < up_words.size())) {
        std::string ch = up_words.substr(i, (i + int64_t(1)) - i);
        cipher = (cipher + get_opponent(table[count], ch));
        count = ((count + int64_t(1)) % table.size());
        i = (i + int64_t(1));
    }
    return cipher;
}

std::string decrypt(std::string key, std::string words) {
    std::string res = encrypt(key, words);
    return res;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << encrypt(std::string("marvin"), std::string("jessica"));
        std::cout << std::endl;
        std::cout << decrypt(std::string("marvin"), std::string("QRACRWU"));
        std::cout << std::endl;
    }
    return 0;
}
