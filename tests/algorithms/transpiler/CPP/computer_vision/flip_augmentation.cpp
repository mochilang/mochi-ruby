// Generated by Mochi transpiler v0.10.59 on 2025-08-06 21:42:29 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::vector<std::vector<int64_t>> flip_horizontal_image(const std::vector<std::vector<int64_t>>& img);
std::vector<std::vector<int64_t>> flip_vertical_image(const std::vector<std::vector<int64_t>>& img);
std::vector<std::vector<double>> flip_horizontal_boxes(const std::vector<std::vector<double>>& boxes);
std::vector<std::vector<double>> flip_vertical_boxes(const std::vector<std::vector<double>>& boxes);
void print_image(const std::vector<std::vector<int64_t>>& img);
int main();

std::vector<std::vector<int64_t>> image = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(3)}, std::vector<int64_t>{int64_t(4), int64_t(5), int64_t(6)}, std::vector<int64_t>{int64_t(7), int64_t(8), int64_t(9)}};
std::vector<std::vector<double>> boxes = std::vector<std::vector<double>>{std::vector<double>{0.0, 0.25, 0.25, 0.5, 0.5}, std::vector<double>{1.0, 0.75, 0.75, 0.5, 0.5}};
std::vector<std::vector<int64_t>> h_img = flip_horizontal_image(image);
std::vector<std::vector<double>> h_boxes = flip_horizontal_boxes(boxes);
std::vector<std::vector<int64_t>> v_img = flip_vertical_image(image);
std::vector<std::vector<double>> v_boxes = flip_vertical_boxes(boxes);

std::vector<std::vector<int64_t>> flip_horizontal_image(const std::vector<std::vector<int64_t>>& img) {
    std::vector<std::vector<int64_t>> flipped = {};
    int64_t i = int64_t(0);
    while ((i < img.size())) {
        std::vector<int64_t> row = img[i];
        int64_t j = (row.size() - int64_t(1));
        std::vector<int64_t> new_row = {};
        while ((j >= int64_t(0))) {
            new_row = ([&]{ auto __tmp = new_row; __tmp.push_back(row[j]); return __tmp; }());
            j = (j - int64_t(1));
        }
        flipped = ([&]{ auto __tmp = flipped; __tmp.push_back(new_row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return flipped;
}

std::vector<std::vector<int64_t>> flip_vertical_image(const std::vector<std::vector<int64_t>>& img) {
    std::vector<std::vector<int64_t>> flipped = {};
    int64_t i = (img.size() - int64_t(1));
    while ((i >= int64_t(0))) {
        flipped = ([&]{ auto __tmp = flipped; __tmp.push_back(img[i]); return __tmp; }());
        i = (i - int64_t(1));
    }
    return flipped;
}

std::vector<std::vector<double>> flip_horizontal_boxes(const std::vector<std::vector<double>>& boxes) {
    std::vector<std::vector<double>> result = {};
    int64_t i = int64_t(0);
    while ((i < boxes.size())) {
        std::vector<double> b = boxes[i];
        double x_new = (1.0 - b[int64_t(1)]);
        result = ([&]{ auto __tmp = result; __tmp.push_back(std::vector<double>{b[int64_t(0)], x_new, b[int64_t(2)], b[int64_t(3)], b[int64_t(4)]}); return __tmp; }());
        i = (i + int64_t(1));
    }
    return result;
}

std::vector<std::vector<double>> flip_vertical_boxes(const std::vector<std::vector<double>>& boxes) {
    std::vector<std::vector<double>> result = {};
    int64_t i = int64_t(0);
    while ((i < boxes.size())) {
        std::vector<double> b = boxes[i];
        double y_new = (1.0 - b[int64_t(2)]);
        result = ([&]{ auto __tmp = result; __tmp.push_back(std::vector<double>{b[int64_t(0)], b[int64_t(1)], y_new, b[int64_t(3)], b[int64_t(4)]}); return __tmp; }());
        i = (i + int64_t(1));
    }
    return result;
}

void print_image(const std::vector<std::vector<int64_t>>& img) {
    int64_t i = int64_t(0);
    while ((i < img.size())) {
        std::vector<int64_t> row = img[i];
        int64_t j = int64_t(0);
        std::string line = std::string("");
        while ((j < row.size())) {
            line = ((line + ([&]{ std::ostringstream ss; ss << std::boolalpha << row[j]; return ss.str(); }())) + std::string(" "));
            j = (j + int64_t(1));
        }
        std::cout << line;
        std::cout << std::endl;
        i = (i + int64_t(1));
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << std::string("Original image:");
        std::cout << std::endl;
        print_image(image);
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = boxes; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << std::string("Horizontal flip:");
        std::cout << std::endl;
        print_image(h_img);
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = h_boxes; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::cout << std::string("Vertical flip:");
        std::cout << std::endl;
        print_image(v_img);
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp = v_boxes; ss << "["; for(size_t i=0;i<__tmp.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; __ss << __tmp[static_cast<size_t>(i)];
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
