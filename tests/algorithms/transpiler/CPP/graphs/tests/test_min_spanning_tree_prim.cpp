// Generated by Mochi transpiler v0.10.66 on 2025-08-16 10:08:35 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <vector>
#include <algorithm>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Neighbor;
struct EdgePair;

std::ostream& operator<<(std::ostream&, const Neighbor&);
std::ostream& operator<<(std::ostream&, const EdgePair&);


struct Neighbor {
    int64_t node;
    int64_t cost;
    auto operator<=>(const Neighbor&) const = default;
};

struct EdgePair {
    int64_t u;
    int64_t v;
    auto operator<=>(const EdgePair&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Neighbor& v) {
    os << '{' << "'node': "<< v.node
 << ", " << "'cost': "<< v.cost
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const EdgePair& v) {
    os << '{' << "'u': "<< v.u
 << ", " << "'v': "<< v.v
 << '}';
    return os;
}

std::vector<EdgePair> prims_algorithm(const std::map<int64_t, std::vector<Neighbor>>& adjacency);
bool test_prim_successful_result();
int main();

std::vector<EdgePair> prims_algorithm(const std::map<int64_t, std::vector<Neighbor>>& adjacency) {
    std::map<int64_t, bool> visited = {};
    visited[static_cast<size_t>(int64_t(0))] = true;
    std::vector<EdgePair> mst = {};
    int64_t count = int64_t(1);
    int64_t total = int64_t(0);
    for (const auto& __p : adjacency) {
        auto k = __p.first;
        total = (total + int64_t(1));
    }
    while ((count < total)) {
        int64_t best_u = int64_t(0);
        int64_t best_v = int64_t(0);
        int64_t best_cost = int64_t(2147483647);
        for (const auto& __p : adjacency) {
            auto u_str = __p.first;
            int64_t u = (int64_t)(u_str);
            if (([&](const auto& __m){ auto __it = __m.find(u); if (__it == __m.end()) return bool{}; return __it->second; })(visited)) {
                for (Neighbor n : ([&](const auto& __m){ auto __it = __m.find(u); if (__it == __m.end()) return std::vector<Neighbor>{}; return __it->second; })(adjacency)) {
                    if ((!([&](const auto& __m){ auto __it = __m.find(n.node); if (__it == __m.end()) return bool{}; return __it->second; })(visited) && (n.cost < best_cost))) {
                        best_cost = n.cost;
                        best_u = u;
                        best_v = n.node;
                    }
                }
            }
        }
        visited[static_cast<size_t>(best_v)] = true;
        mst = ([&]{ auto __tmp = mst; __tmp.push_back(EdgePair{.u = best_u, .v = best_v}); return __tmp; }());
        count = (count + int64_t(1));
    }
    return mst;
}

bool test_prim_successful_result() {
    std::vector<std::vector<int64_t>> edges = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(0), int64_t(1), int64_t(4)}, std::vector<int64_t>{int64_t(0), int64_t(7), int64_t(8)}, std::vector<int64_t>{int64_t(1), int64_t(2), int64_t(8)}, std::vector<int64_t>{int64_t(7), int64_t(8), int64_t(7)}, std::vector<int64_t>{int64_t(7), int64_t(6), int64_t(1)}, std::vector<int64_t>{int64_t(2), int64_t(8), int64_t(2)}, std::vector<int64_t>{int64_t(8), int64_t(6), int64_t(6)}, std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(7)}, std::vector<int64_t>{int64_t(2), int64_t(5), int64_t(4)}, std::vector<int64_t>{int64_t(6), int64_t(5), int64_t(2)}, std::vector<int64_t>{int64_t(3), int64_t(5), int64_t(14)}, std::vector<int64_t>{int64_t(3), int64_t(4), int64_t(9)}, std::vector<int64_t>{int64_t(5), int64_t(4), int64_t(10)}, std::vector<int64_t>{int64_t(1), int64_t(7), int64_t(11)}};
    std::map<int64_t, std::vector<Neighbor>> adjacency = {};
    for (std::vector<int64_t> e : edges) {
        int64_t u = _index(e, int64_t(0));
        int64_t v = _index(e, int64_t(1));
        int64_t w = _index(e, int64_t(2));
        if (!([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(adjacency, u)) {
            adjacency[static_cast<size_t>(u)] = std::vector<Neighbor>{};
        }
        if (!([&](const auto& __c, const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__c)>, std::string>) { return __c.find(__v) != std::string::npos; } else if constexpr(requires { __c.find(__v); }) { return __c.find(__v) != __c.end(); } else { return std::find(__c.begin(), __c.end(), __v) != __c.end(); } })(adjacency, v)) {
            adjacency[static_cast<size_t>(v)] = std::vector<Neighbor>{};
        }
        adjacency[static_cast<size_t>(u)] = ([&]{ auto __tmp = ([&](const auto& __m){ auto __it = __m.find(u); if (__it == __m.end()) return std::vector<Neighbor>{}; return __it->second; })(adjacency); __tmp.push_back(Neighbor{.node = v, .cost = w}); return __tmp; }());
        adjacency[static_cast<size_t>(v)] = ([&]{ auto __tmp = ([&](const auto& __m){ auto __it = __m.find(v); if (__it == __m.end()) return std::vector<Neighbor>{}; return __it->second; })(adjacency); __tmp.push_back(Neighbor{.node = u, .cost = w}); return __tmp; }());
    }
    std::vector<EdgePair> result = prims_algorithm(adjacency);
    std::map<std::string, bool> seen = {};
    for (EdgePair e : result) {
        std::string key1 = ((([&]{ std::ostringstream ss; ss << std::boolalpha << e.u; return ss.str(); }()) + std::string(",")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << e.v; return ss.str(); }()));
        std::string key2 = ((([&]{ std::ostringstream ss; ss << std::boolalpha << e.v; return ss.str(); }()) + std::string(",")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << e.u; return ss.str(); }()));
        seen[key1] = true;
        seen[key2] = true;
    }
    std::vector<std::vector<int64_t>> expected = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(7), int64_t(6), int64_t(1)}, std::vector<int64_t>{int64_t(2), int64_t(8), int64_t(2)}, std::vector<int64_t>{int64_t(6), int64_t(5), int64_t(2)}, std::vector<int64_t>{int64_t(0), int64_t(1), int64_t(4)}, std::vector<int64_t>{int64_t(2), int64_t(5), int64_t(4)}, std::vector<int64_t>{int64_t(2), int64_t(3), int64_t(7)}, std::vector<int64_t>{int64_t(0), int64_t(7), int64_t(8)}, std::vector<int64_t>{int64_t(3), int64_t(4), int64_t(9)}};
    for (std::vector<int64_t> ans : expected) {
        std::string key = ((([&]{ std::ostringstream ss; ss << std::boolalpha << _index(ans, int64_t(0)); return ss.str(); }()) + std::string(",")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << _index(ans, int64_t(1)); return ss.str(); }()));
        if (!([&](const auto& __m){ auto __it = __m.find(key); if (__it == __m.end()) return bool{}; return __it->second; })(seen)) {
            return false;
        }
    }
    return true;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << (test_prim_successful_result() ? "true" : "false");
        std::cout << std::endl;
        std::cout << (true ? "true" : "false");
        std::cout << std::endl;
    }
    return 0;
}
