// Generated by Mochi transpiler v0.10.66 on 2025-08-16 10:08:27 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <functional>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::vector<std::vector<int64_t>> tarjan(const std::vector<std::vector<int64_t>>& g);
std::vector<std::vector<int64_t>> create_graph(int64_t n, const std::vector<std::vector<int64_t>>& edges);
int main();

std::vector<std::vector<int64_t>> tarjan(const std::vector<std::vector<int64_t>>& g) {
    int64_t n = g.size();
    std::vector<int64_t> stack = {};
    std::vector<bool> on_stack = {};
    std::vector<int64_t> index_of = {};
    std::vector<int64_t> lowlink_of = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        on_stack = ([&]{ auto __tmp = on_stack; __tmp.push_back(false); return __tmp; }());
        index_of = ([&]{ auto __tmp = index_of; __tmp.push_back((int64_t(0) - int64_t(1))); return __tmp; }());
        lowlink_of = ([&]{ auto __tmp = lowlink_of; __tmp.push_back((int64_t(0) - int64_t(1))); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<std::vector<int64_t>> components = {};
    std::function<int64_t(int64_t, int64_t)> strong_connect;
    strong_connect = [=, &strong_connect](int64_t v, int64_t index) mutable {
        index_of[static_cast<size_t>(v)] = index;
        lowlink_of[static_cast<size_t>(v)] = index;
        int64_t current_index = (index + int64_t(1));
        stack = ([&]{ auto __tmp = stack; __tmp.push_back(v); return __tmp; }());
        on_stack[static_cast<size_t>(v)] = true;
        for (int64_t w : _index(g, v)) {
            if ((_index(index_of, w) == (int64_t(0) - int64_t(1)))) {
                current_index = strong_connect(w, current_index);
                if ((_index(lowlink_of, w) < _index(lowlink_of, v))) {
                    lowlink_of[static_cast<size_t>(v)] = _index(lowlink_of, w);
                }
            } else             if (_index(on_stack, w)) {
                if ((_index(lowlink_of, w) < _index(lowlink_of, v))) {
                    lowlink_of[static_cast<size_t>(v)] = _index(lowlink_of, w);
                }
            }

        }
        if ((_index(lowlink_of, v) == _index(index_of, v))) {
            std::vector<int64_t> component = {};
            int64_t w = _index(stack, (stack.size() - int64_t(1)));
            stack = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), (stack.size() - int64_t(1)) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+(stack.size() - int64_t(1))); })(stack);
            on_stack[static_cast<size_t>(w)] = false;
            component = ([&]{ auto __tmp = component; __tmp.push_back(w); return __tmp; }());
            while ((w != v)) {
                w = _index(stack, (stack.size() - int64_t(1)));
                stack = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), (stack.size() - int64_t(1)) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+(stack.size() - int64_t(1))); })(stack);
                on_stack[static_cast<size_t>(w)] = false;
                component = ([&]{ auto __tmp = component; __tmp.push_back(w); return __tmp; }());
            }
            components = ([&]{ auto __tmp = components; __tmp.push_back(component); return __tmp; }());
        }
        return current_index;
    };
    int64_t v = int64_t(0);
    while ((v < n)) {
        if ((_index(index_of, v) == (int64_t(0) - int64_t(1)))) {
            strong_connect(v, int64_t(0));
        }
        v = (v + int64_t(1));
    }
    return components;
}

std::vector<std::vector<int64_t>> create_graph(int64_t n, const std::vector<std::vector<int64_t>>& edges) {
    std::vector<std::vector<int64_t>> g = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        g = ([&]{ auto __tmp = g; __tmp.push_back(std::vector<int64_t>{}); return __tmp; }());
        i = (i + int64_t(1));
    }
    for (std::vector<int64_t> e : edges) {
        int64_t u = _index(e, int64_t(0));
        int64_t v = _index(e, int64_t(1));
        g[static_cast<size_t>(u)] = ([&]{ auto __tmp = _index(g, u); __tmp.push_back(v); return __tmp; }());
    }
    return g;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        int64_t n_vertices = int64_t(7);
        std::vector<int64_t> source = std::vector<int64_t>{int64_t(0), int64_t(0), int64_t(1), int64_t(2), int64_t(3), int64_t(3), int64_t(4), int64_t(4), int64_t(6)};
        std::vector<int64_t> target = std::vector<int64_t>{int64_t(1), int64_t(3), int64_t(2), int64_t(0), int64_t(1), int64_t(4), int64_t(5), int64_t(6), int64_t(5)};
        std::vector<std::vector<int64_t>> edges = {};
        int64_t i = int64_t(0);
        while ((i < source.size())) {
            edges = ([&]{ auto __tmp = edges; __tmp.push_back(std::vector<int64_t>{_index(source, i), _index(target, i)}); return __tmp; }());
            i = (i + int64_t(1));
        }
        std::vector<std::vector<int64_t>> g = create_graph(n_vertices, edges);
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp1 = tarjan(g); ss << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; { auto __tmp2 = _index(__tmp1, i); __ss << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) __ss << ' '; __ss << __tmp2[i]; } __ss << "]"; }
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
