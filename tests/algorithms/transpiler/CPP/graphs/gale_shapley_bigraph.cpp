// Generated by Mochi transpiler v0.10.66 on 2025-08-15 15:19:56 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(16) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

int64_t index_of(const std::vector<int64_t>& xs, int64_t x);
std::vector<int64_t> remove_item(const std::vector<int64_t>& xs, int64_t x);
std::vector<int64_t> stable_matching(const std::vector<std::vector<int64_t>>& donor_pref, const std::vector<std::vector<int64_t>>& recipient_pref);
int main();

std::vector<std::vector<int64_t>> donor_pref = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(0), int64_t(1), int64_t(3), int64_t(2)}, std::vector<int64_t>{int64_t(0), int64_t(2), int64_t(3), int64_t(1)}, std::vector<int64_t>{int64_t(1), int64_t(0), int64_t(2), int64_t(3)}, std::vector<int64_t>{int64_t(0), int64_t(3), int64_t(1), int64_t(2)}};
std::vector<std::vector<int64_t>> recipient_pref = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(3), int64_t(1), int64_t(2), int64_t(0)}, std::vector<int64_t>{int64_t(3), int64_t(1), int64_t(0), int64_t(2)}, std::vector<int64_t>{int64_t(0), int64_t(3), int64_t(1), int64_t(2)}, std::vector<int64_t>{int64_t(1), int64_t(0), int64_t(3), int64_t(2)}};

int64_t index_of(const std::vector<int64_t>& xs, int64_t x) {
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((_index(xs, i) == x)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

std::vector<int64_t> remove_item(const std::vector<int64_t>& xs, int64_t x) {
    std::vector<int64_t> res = {};
    bool removed = false;
    int64_t i = int64_t(0);
    while ((i < xs.size())) {
        if ((!removed && (_index(xs, i) == x))) {
            removed = true;
        } else {
            res = ([&]{ auto __tmp = res; __tmp.push_back(_index(xs, i)); return __tmp; }());
        }
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<int64_t> stable_matching(const std::vector<std::vector<int64_t>>& donor_pref, const std::vector<std::vector<int64_t>>& recipient_pref) {
    if ((donor_pref.size() != recipient_pref.size())) {
        throw std::runtime_error(std::string("unequal groups"));
    }
    int64_t n = donor_pref.size();
    std::vector<int64_t> unmatched = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        unmatched = ([&]{ auto __tmp = unmatched; __tmp.push_back(i); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<int64_t> donor_record = {};
    i = int64_t(0);
    while ((i < n)) {
        donor_record = ([&]{ auto __tmp = donor_record; __tmp.push_back(-int64_t(1)); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<int64_t> rec_record = {};
    i = int64_t(0);
    while ((i < n)) {
        rec_record = ([&]{ auto __tmp = rec_record; __tmp.push_back(-int64_t(1)); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<int64_t> num_donations = {};
    i = int64_t(0);
    while ((i < n)) {
        num_donations = ([&]{ auto __tmp = num_donations; __tmp.push_back(int64_t(0)); return __tmp; }());
        i = (i + int64_t(1));
    }
    while ((unmatched.size() > int64_t(0))) {
        int64_t donor = _index(unmatched, int64_t(0));
        std::vector<int64_t> donor_preference = _index(donor_pref, donor);
        int64_t recipient = _index(donor_preference, _index(num_donations, donor));
        num_donations[static_cast<size_t>(donor)] = (_index(num_donations, donor) + int64_t(1));
        std::vector<int64_t> rec_preference = _index(recipient_pref, recipient);
        int64_t prev_donor = _index(rec_record, recipient);
        if ((prev_donor != (int64_t(0) - int64_t(1)))) {
            int64_t prev_index = index_of(rec_preference, prev_donor);
            int64_t new_index = index_of(rec_preference, donor);
            if ((prev_index > new_index)) {
                rec_record[static_cast<size_t>(recipient)] = donor;
                donor_record[static_cast<size_t>(donor)] = recipient;
                unmatched = ([&]{ auto __tmp = unmatched; __tmp.push_back(prev_donor); return __tmp; }());
                unmatched = remove_item(unmatched, donor);
            }
        } else {
            rec_record[static_cast<size_t>(recipient)] = donor;
            donor_record[static_cast<size_t>(donor)] = recipient;
            unmatched = remove_item(unmatched, donor);
        }
    }
    return donor_record;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp1 = stable_matching(donor_pref, recipient_pref); ss << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) ss << ' '; ss << __tmp1[i]; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
