// Generated by Mochi transpiler v0.10.66 on 2025-08-15 15:21:27 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <stdexcept>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct LanczosResult;
struct EigenResult;

std::ostream& operator<<(std::ostream&, const LanczosResult&);
std::ostream& operator<<(std::ostream&, const EigenResult&);


struct LanczosResult {
    std::vector<std::vector<double>> t;
    std::vector<std::vector<double>> q;
    auto operator<=>(const LanczosResult&) const = default;
};

struct EigenResult {
    std::vector<double> values;
    std::vector<std::vector<double>> vectors;
    auto operator<=>(const EigenResult&) const = default;
};

std::ostream& operator<<(std::ostream& os, const LanczosResult& v) {
    os << '{' << "'t': "<< "["; for(size_t i=0;i<v.t.size();++i){ if(i>0) os << ", "; os << v.t[i]; } os << "]"
 << ", " << "'q': "<< "["; for(size_t i=0;i<v.q.size();++i){ if(i>0) os << ", "; os << v.q[i]; } os << "]"
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const EigenResult& v) {
    os << '{' << "'values': "<< "["; for(size_t i=0;i<v.values.size();++i){ if(i>0) os << ", "; os << v.values[i]; } os << "]"
 << ", " << "'vectors': "<< "["; for(size_t i=0;i<v.vectors.size();++i){ if(i>0) os << ", "; os << v.vectors[i]; } os << "]"
 << '}';
    return os;
}

int64_t _rand();
double _random();
double sqrtApprox(double x);
double absf(double x);
double dot(const std::vector<double>& a, const std::vector<double>& b);
std::vector<double> vector_scale(const std::vector<double>& v, double s);
std::vector<double> vector_sub(const std::vector<double>& a, const std::vector<double>& b);
std::vector<double> vector_add(const std::vector<double>& a, const std::vector<double>& b);
std::vector<std::vector<double>> zeros_matrix(int64_t r, int64_t c);
std::vector<double> column(const std::vector<std::vector<double>>& m, int64_t idx);
void validate_adjacency_list(const std::vector<std::vector<int64_t>>& graph);
std::vector<double> multiply_matrix_vector(const std::vector<std::vector<int64_t>>& graph, const std::vector<double>& vector);
LanczosResult lanczos_iteration(const std::vector<std::vector<int64_t>>& graph, int64_t k);
EigenResult jacobi_eigen(const std::vector<std::vector<double>>& a_in, int64_t max_iter);
std::vector<std::vector<double>> matmul(const std::vector<std::vector<double>>& a, const std::vector<std::vector<double>>& b);
EigenResult sort_eigenpairs(const std::vector<double>& vals, const std::vector<std::vector<double>>& vecs);
EigenResult find_lanczos_eigenvectors(const std::vector<std::vector<int64_t>>& graph, int64_t k);
std::string list_to_string(const std::vector<double>& arr);
std::string matrix_to_string(const std::vector<std::vector<double>>& m);
int main();

int64_t seed = int64_t(123456789);
std::vector<std::vector<int64_t>> graph = std::vector<std::vector<int64_t>>{std::vector<int64_t>{int64_t(1), int64_t(2)}, std::vector<int64_t>{int64_t(0), int64_t(2)}, std::vector<int64_t>{int64_t(0), int64_t(1)}};
EigenResult result = EigenResult{};

int64_t _rand() {
    seed = (((seed * int64_t(1103515245)) + int64_t(12345)) % int64_t(2147483648));
    return seed;
}

double _random() {
    return ((double)((1.0 * _rand())) / (2147483648.0));
}

double sqrtApprox(double x) {
    if ((x <= 0.0)) {
        return 0.0;
    }
    double guess = x;
    int64_t i = int64_t(0);
    while ((i < int64_t(20))) {
        guess = ((double)((guess + ((double)(x) / (guess)))) / (2.0));
        i = (i + int64_t(1));
    }
    return guess;
}

double absf(double x) {
    return ((x < 0.0) ? -x : x);
}

double dot(const std::vector<double>& a, const std::vector<double>& b) {
    double s = 0.0;
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        s = (s + (_index(a, i) * _index(b, i)));
        i = (i + int64_t(1));
    }
    return s;
}

std::vector<double> vector_scale(const std::vector<double>& v, double s) {
    std::vector<double> res = {};
    int64_t i = int64_t(0);
    while ((i < v.size())) {
        res = ([&]{ auto __tmp = res; __tmp.push_back((_index(v, i) * s)); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<double> vector_sub(const std::vector<double>& a, const std::vector<double>& b) {
    std::vector<double> res = {};
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        res = ([&]{ auto __tmp = res; __tmp.push_back((_index(a, i) - _index(b, i))); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<double> vector_add(const std::vector<double>& a, const std::vector<double>& b) {
    std::vector<double> res = {};
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        res = ([&]{ auto __tmp = res; __tmp.push_back((_index(a, i) + _index(b, i))); return __tmp; }());
        i = (i + int64_t(1));
    }
    return res;
}

std::vector<std::vector<double>> zeros_matrix(int64_t r, int64_t c) {
    std::vector<std::vector<double>> m = {};
    int64_t i = int64_t(0);
    while ((i < r)) {
        std::vector<double> row = {};
        int64_t j = int64_t(0);
        while ((j < c)) {
            row = ([&]{ auto __tmp = row; __tmp.push_back(0.0); return __tmp; }());
            j = (j + int64_t(1));
        }
        m = ([&]{ auto __tmp = m; __tmp.push_back(row); return __tmp; }());
        i = (i + int64_t(1));
    }
    return m;
}

std::vector<double> column(const std::vector<std::vector<double>>& m, int64_t idx) {
    std::vector<double> col = {};
    int64_t i = int64_t(0);
    while ((i < m.size())) {
        col = ([&]{ auto __tmp = col; __tmp.push_back(_index(_index(m, i), idx)); return __tmp; }());
        i = (i + int64_t(1));
    }
    return col;
}

void validate_adjacency_list(const std::vector<std::vector<int64_t>>& graph) {
    int64_t i = int64_t(0);
    while ((i < graph.size())) {
        int64_t j = int64_t(0);
        while ((j < _index(graph, i).size())) {
            int64_t v = _index(_index(graph, i), j);
            if (((v < int64_t(0)) || (v >= graph.size()))) {
                throw std::runtime_error(std::string("Invalid neighbor"));
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
}

std::vector<double> multiply_matrix_vector(const std::vector<std::vector<int64_t>>& graph, const std::vector<double>& vector) {
    int64_t n = graph.size();
    if ((vector.size() != n)) {
        throw std::runtime_error(std::string("Vector length must match number of nodes"));
    }
    std::vector<double> result = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        double sum = 0.0;
        int64_t j = int64_t(0);
        while ((j < _index(graph, i).size())) {
            int64_t nb = _index(_index(graph, i), j);
            sum = (sum + _index(vector, nb));
            j = (j + int64_t(1));
        }
        result = ([&]{ auto __tmp = result; __tmp.push_back(sum); return __tmp; }());
        i = (i + int64_t(1));
    }
    return result;
}

LanczosResult lanczos_iteration(const std::vector<std::vector<int64_t>>& graph, int64_t k) {
    int64_t n = graph.size();
    if (((k < int64_t(1)) || (k > n))) {
        throw std::runtime_error(std::string("invalid number of eigenvectors"));
    }
    std::vector<std::vector<double>> q = zeros_matrix(n, k);
    std::vector<std::vector<double>> t = zeros_matrix(k, k);
    std::vector<double> v = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        v = ([&]{ auto __tmp = v; __tmp.push_back((double)_random()); return __tmp; }());
        i = (i + int64_t(1));
    }
    double ss = 0.0;
    i = int64_t(0);
    while ((i < n)) {
        ss = (ss + (_index(v, i) * _index(v, i)));
        i = (i + int64_t(1));
    }
    double vnorm = sqrtApprox(ss);
    i = int64_t(0);
    while ((i < n)) {
        q[static_cast<size_t>(i)][static_cast<size_t>(int64_t(0))] = ((double)(_index(v, i)) / (vnorm));
        i = (i + int64_t(1));
    }
    double beta = 0.0;
    int64_t j = int64_t(0);
    while ((j < k)) {
        std::vector<double> w = multiply_matrix_vector(graph, column(q, j));
        if ((j > int64_t(0))) {
            w = vector_sub(w, vector_scale(column(q, (j - int64_t(1))), beta));
        }
        double alpha = dot(column(q, j), w);
        w = vector_sub(w, vector_scale(column(q, j), alpha));
        double ss2 = 0.0;
        int64_t p = int64_t(0);
        while ((p < n)) {
            ss2 = (ss2 + (_index(w, p) * _index(w, p)));
            p = (p + int64_t(1));
        }
        beta = sqrtApprox(ss2);
        t[static_cast<size_t>(j)][static_cast<size_t>(j)] = alpha;
        if ((j < (k - int64_t(1)))) {
            t[static_cast<size_t>(j)][static_cast<size_t>((j + int64_t(1)))] = beta;
            t[static_cast<size_t>((j + int64_t(1)))][static_cast<size_t>(j)] = beta;
            if ((beta > 1e-10)) {
                std::vector<double> wnorm = vector_scale(w, ((double)(1.0) / (beta)));
                int64_t r = int64_t(0);
                while ((r < n)) {
                    q[static_cast<size_t>(r)][static_cast<size_t>((j + int64_t(1)))] = _index(wnorm, r);
                    r = (r + int64_t(1));
                }
            }
        }
        j = (j + int64_t(1));
    }
    return LanczosResult{.t = t, .q = q};
}

EigenResult jacobi_eigen(const std::vector<std::vector<double>>& a_in, int64_t max_iter) {
    int64_t n = a_in.size();
    std::vector<std::vector<double>> a = a_in;
    std::vector<std::vector<double>> v = zeros_matrix(n, n);
    int64_t i = int64_t(0);
    while ((i < n)) {
        v[static_cast<size_t>(i)][static_cast<size_t>(i)] = 1.0;
        i = (i + int64_t(1));
    }
    int64_t iter = int64_t(0);
    while ((iter < max_iter)) {
        int64_t p = int64_t(0);
        int64_t q = int64_t(1);
        double max = absf(_index(_index(a, p), q));
        i = int64_t(0);
        while ((i < n)) {
            int64_t j = (i + int64_t(1));
            while ((j < n)) {
                double val = absf(_index(_index(a, i), j));
                if ((val > max)) {
                    max = val;
                    p = i;
                    q = j;
                }
                j = (j + int64_t(1));
            }
            i = (i + int64_t(1));
        }
        if ((max < 1e-08)) {
            break;
        }
        double app = _index(_index(a, p), p);
        double aqq = _index(_index(a, q), q);
        double apq = _index(_index(a, p), q);
        double theta = ((double)((aqq - app)) / ((2.0 * apq)));
        double t = ((double)(1.0) / ((absf(theta) + sqrtApprox(((theta * theta) + 1.0)))));
        if ((theta < 0.0)) {
            t = -t;
        }
        double c = ((double)(1.0) / (sqrtApprox((1.0 + (t * t)))));
        double s = (t * c);
        double tau = ((double)(s) / ((1.0 + c)));
        a[static_cast<size_t>(p)][static_cast<size_t>(p)] = (app - (t * apq));
        a[static_cast<size_t>(q)][static_cast<size_t>(q)] = (aqq + (t * apq));
        a[static_cast<size_t>(p)][static_cast<size_t>(q)] = 0.0;
        a[static_cast<size_t>(q)][static_cast<size_t>(p)] = 0.0;
        int64_t k = int64_t(0);
        while ((k < n)) {
            if (((k != p) && (k != q))) {
                double akp = _index(_index(a, k), p);
                double akq = _index(_index(a, k), q);
                a[static_cast<size_t>(k)][static_cast<size_t>(p)] = (akp - (s * (akq + (tau * akp))));
                a[static_cast<size_t>(p)][static_cast<size_t>(k)] = _index(_index(a, k), p);
                a[static_cast<size_t>(k)][static_cast<size_t>(q)] = (akq + (s * (akp - (tau * akq))));
                a[static_cast<size_t>(q)][static_cast<size_t>(k)] = _index(_index(a, k), q);
            }
            k = (k + int64_t(1));
        }
        k = int64_t(0);
        while ((k < n)) {
            double vkp = _index(_index(v, k), p);
            double vkq = _index(_index(v, k), q);
            v[static_cast<size_t>(k)][static_cast<size_t>(p)] = (vkp - (s * (vkq + (tau * vkp))));
            v[static_cast<size_t>(k)][static_cast<size_t>(q)] = (vkq + (s * (vkp - (tau * vkq))));
            k = (k + int64_t(1));
        }
        iter = (iter + int64_t(1));
    }
    std::vector<double> eigenvalues = {};
    i = int64_t(0);
    while ((i < n)) {
        eigenvalues = ([&]{ auto __tmp = eigenvalues; __tmp.push_back(_index(_index(a, i), i)); return __tmp; }());
        i = (i + int64_t(1));
    }
    return EigenResult{.values = eigenvalues, .vectors = v};
}

std::vector<std::vector<double>> matmul(const std::vector<std::vector<double>>& a, const std::vector<std::vector<double>>& b) {
    int64_t rows = a.size();
    int64_t cols = _index(b, int64_t(0)).size();
    int64_t inner = b.size();
    std::vector<std::vector<double>> m = zeros_matrix(rows, cols);
    int64_t i = int64_t(0);
    while ((i < rows)) {
        int64_t j = int64_t(0);
        while ((j < cols)) {
            double s = 0.0;
            int64_t k = int64_t(0);
            while ((k < inner)) {
                s = (s + (_index(_index(a, i), k) * _index(_index(b, k), j)));
                k = (k + int64_t(1));
            }
            m[static_cast<size_t>(i)][static_cast<size_t>(j)] = s;
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return m;
}

EigenResult sort_eigenpairs(const std::vector<double>& vals, const std::vector<std::vector<double>>& vecs) {
    int64_t n = vals.size();
    std::vector<double> values = vals;
    std::vector<std::vector<double>> vectors = vecs;
    int64_t i = int64_t(0);
    while ((i < n)) {
        int64_t j = int64_t(0);
        while ((j < (n - int64_t(1)))) {
            if ((_index(values, j) < _index(values, (j + int64_t(1))))) {
                double tmp = _index(values, j);
                values[static_cast<size_t>(j)] = _index(values, (j + int64_t(1)));
                values[static_cast<size_t>((j + int64_t(1)))] = tmp;
                int64_t r = int64_t(0);
                while ((r < vectors.size())) {
                    double tv = _index(_index(vectors, r), j);
                    vectors[static_cast<size_t>(r)][static_cast<size_t>(j)] = _index(_index(vectors, r), (j + int64_t(1)));
                    vectors[static_cast<size_t>(r)][static_cast<size_t>((j + int64_t(1)))] = tv;
                    r = (r + int64_t(1));
                }
            }
            j = (j + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    return EigenResult{.values = values, .vectors = vectors};
}

EigenResult find_lanczos_eigenvectors(const std::vector<std::vector<int64_t>>& graph, int64_t k) {
    validate_adjacency_list(graph);
    LanczosResult res = lanczos_iteration(graph, k);
    EigenResult eig = jacobi_eigen(res.t, int64_t(50));
    EigenResult sorted = sort_eigenpairs(eig.values, eig.vectors);
    std::vector<std::vector<double>> final_vectors = matmul(res.q, sorted.vectors);
    return EigenResult{.values = sorted.values, .vectors = final_vectors};
}

std::string list_to_string(const std::vector<double>& arr) {
    std::string s = std::string("[");
    int64_t i = int64_t(0);
    while ((i < arr.size())) {
        s = (s + _to_string(_index(arr, i)));
        if ((i < (arr.size() - int64_t(1)))) {
            s = (s + std::string(", "));
        }
        i = (i + int64_t(1));
    }
    return (s + std::string("]"));
}

std::string matrix_to_string(const std::vector<std::vector<double>>& m) {
    std::string s = std::string("[");
    int64_t i = int64_t(0);
    while ((i < m.size())) {
        s = (s + list_to_string(_index(m, i)));
        if ((i < (m.size() - int64_t(1)))) {
            s = (s + std::string("; "));
        }
        i = (i + int64_t(1));
    }
    return (s + std::string("]"));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        result = find_lanczos_eigenvectors(graph, int64_t(2));
        std::cout << list_to_string(result.values);
        std::cout << std::endl;
        std::cout << matrix_to_string(result.vectors);
        std::cout << std::endl;
    }
    return 0;
}
