// Generated by Mochi transpiler v0.10.67 on 2025-08-17 00:50:34 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct Dataset;
struct Tree;

std::ostream& operator<<(std::ostream&, const Dataset&);
std::ostream& operator<<(std::ostream&, const Tree&);


struct Dataset {
    std::vector<std::vector<double>> data;
    std::vector<double> target;
    auto operator<=>(const Dataset&) const = default;
};

struct Tree {
    double threshold;
    double left_value;
    double right_value;
    auto operator<=>(const Tree&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Dataset& v) {
    os << '{' << "'data': "<< "["; for(size_t i=0;i<v.data.size();++i){ if(i>0) os << ", "; os << v.data[i]; } os << "]"
 << ", " << "'target': "<< "["; for(size_t i=0;i<v.target.size();++i){ if(i>0) os << ", "; os << v.target[i]; } os << "]"
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const Tree& v) {
    os << '{' << "'threshold': "<< std::fixed << std::setprecision(1) << v.threshold
 << ", " << "'left_value': "<< std::fixed << std::setprecision(1) << v.left_value
 << ", " << "'right_value': "<< std::fixed << std::setprecision(1) << v.right_value
 << '}';
    return os;
}

Dataset data_handling(const Dataset& dataset);
std::vector<double> xgboost(const std::vector<std::vector<double>>& features, const std::vector<double>& target, const std::vector<std::vector<double>>& test_features);
double mean_absolute_error(const std::vector<double>& y_true, const std::vector<double>& y_pred);
double mean_squared_error(const std::vector<double>& y_true, const std::vector<double>& y_pred);
int main();

Dataset data_handling(const Dataset& dataset) {
    return dataset;
}

std::vector<double> xgboost(const std::vector<std::vector<double>>& features, const std::vector<double>& target, const std::vector<std::vector<double>>& test_features) {
    double learning_rate = 0.5;
    int64_t n_estimators = int64_t(3);
    std::vector<Tree> trees = {};
    std::vector<double> predictions = {};
    int64_t i = int64_t(0);
    while ((i < target.size())) {
        predictions = ([&]{ auto __tmp = predictions; __tmp.push_back(0.0); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t est = int64_t(0);
    while ((est < n_estimators)) {
        std::vector<double> residuals = {};
        int64_t j = int64_t(0);
        while ((j < target.size())) {
            residuals = ([&]{ auto __tmp = residuals; __tmp.push_back((_index(target, j) - _index(predictions, j))); return __tmp; }());
            j = (j + int64_t(1));
        }
        double sum_feat = 0.0;
        j = int64_t(0);
        while ((j < features.size())) {
            sum_feat = (sum_feat + _index(_index(features, j), int64_t(0)));
            j = (j + int64_t(1));
        }
        double threshold = ((double)(sum_feat) / ((double)(features.size())));
        double left_sum = 0.0;
        int64_t left_count = int64_t(0);
        double right_sum = 0.0;
        int64_t right_count = int64_t(0);
        j = int64_t(0);
        while ((j < features.size())) {
            if ((_index(_index(features, j), int64_t(0)) <= threshold)) {
                left_sum = (left_sum + _index(residuals, j));
                left_count = (left_count + int64_t(1));
            } else {
                right_sum = (right_sum + _index(residuals, j));
                right_count = (right_count + int64_t(1));
            }
            j = (j + int64_t(1));
        }
        double left_value = 0.0;
        if ((left_count > int64_t(0))) {
            left_value = ((double)(left_sum) / ((double)(left_count)));
        }
        double right_value = 0.0;
        if ((right_count > int64_t(0))) {
            right_value = ((double)(right_sum) / ((double)(right_count)));
        }
        j = int64_t(0);
        while ((j < features.size())) {
            if ((_index(_index(features, j), int64_t(0)) <= threshold)) {
                predictions[static_cast<size_t>(j)] = (_index(predictions, j) + (learning_rate * left_value));
            } else {
                predictions[static_cast<size_t>(j)] = (_index(predictions, j) + (learning_rate * right_value));
            }
            j = (j + int64_t(1));
        }
        trees = ([&]{ auto __tmp = trees; __tmp.push_back(Tree{.threshold = threshold, .left_value = left_value, .right_value = right_value}); return __tmp; }());
        est = (est + int64_t(1));
    }
    std::vector<double> preds = {};
    int64_t t = int64_t(0);
    while ((t < test_features.size())) {
        double pred = 0.0;
        int64_t k = int64_t(0);
        while ((k < trees.size())) {
            if ((_index(_index(test_features, t), int64_t(0)) <= _index(trees, k).threshold)) {
                pred = (pred + (learning_rate * _index(trees, k).left_value));
            } else {
                pred = (pred + (learning_rate * _index(trees, k).right_value));
            }
            k = (k + int64_t(1));
        }
        preds = ([&]{ auto __tmp = preds; __tmp.push_back(pred); return __tmp; }());
        t = (t + int64_t(1));
    }
    return preds;
}

double mean_absolute_error(const std::vector<double>& y_true, const std::vector<double>& y_pred) {
    double sum = 0.0;
    int64_t i = int64_t(0);
    while ((i < y_true.size())) {
        double diff = (_index(y_true, i) - _index(y_pred, i));
        if ((diff < 0.0)) {
            diff = -diff;
        }
        sum = (sum + diff);
        i = (i + int64_t(1));
    }
    return ((double)(sum) / ((double)(y_true.size())));
}

double mean_squared_error(const std::vector<double>& y_true, const std::vector<double>& y_pred) {
    double sum = 0.0;
    int64_t i = int64_t(0);
    while ((i < y_true.size())) {
        double diff = (_index(y_true, i) - _index(y_pred, i));
        sum = (sum + (diff * diff));
        i = (i + int64_t(1));
    }
    return ((double)(sum) / ((double)(y_true.size())));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        Dataset california = Dataset{.data = std::vector<std::vector<double>>{std::vector<double>{1.0}, std::vector<double>{2.0}, std::vector<double>{3.0}, std::vector<double>{4.0}}, .target = std::vector<double>{2.0, 3.0, 4.0, 5.0}};
        Dataset ds = data_handling(california);
        std::vector<std::vector<double>> x_train = ds.data;
        std::vector<double> y_train = ds.target;
        std::vector<std::vector<double>> x_test = std::vector<std::vector<double>>{std::vector<double>{1.5}, std::vector<double>{3.5}};
        std::vector<double> y_test = std::vector<double>{2.5, 4.5};
        std::vector<double> predictions = xgboost(x_train, y_train, x_test);
        std::cout << std::string("Predictions:");
        std::cout << std::endl;
        { auto __tmp1 = predictions; std::cout << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) std::cout << ' '; std::cout << __tmp1[i]; } std::cout << "]"; }
        std::cout << std::endl;
        std::cout << std::string("Mean Absolute Error:");
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = mean_absolute_error(y_test, predictions); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
        std::cout << std::string("Mean Square Error:");
        std::cout << std::endl;
        { std::ostringstream __ss; double __dv = mean_squared_error(y_test, predictions); if(std::floor(__dv) == __dv) { __ss<<std::fixed<<std::setprecision(1)<<__dv; } else { __ss<<std::defaultfloat<<std::setprecision(17)<<__dv; } std::cout << __ss.str(); }
        std::cout << std::endl;
    }
    return 0;
}
