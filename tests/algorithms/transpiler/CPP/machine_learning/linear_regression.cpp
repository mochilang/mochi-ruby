// Generated by Mochi transpiler v0.10.67 on 2025-08-17 00:49:43 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

double dot(const std::vector<double>& x, const std::vector<double>& y);
std::vector<double> run_steep_gradient_descent(const std::vector<std::vector<double>>& data_x, const std::vector<double>& data_y, int64_t len_data, double alpha, const std::vector<double>& theta);
double sum_of_square_error(const std::vector<std::vector<double>>& data_x, const std::vector<double>& data_y, int64_t len_data, const std::vector<double>& theta);
std::vector<double> run_linear_regression(const std::vector<std::vector<double>>& data_x, const std::vector<double>& data_y);
double absf(double x);
double mean_absolute_error(const std::vector<double>& predicted_y, const std::vector<double>& original_y);
int main();

std::vector<std::vector<double>> data_x = std::vector<std::vector<double>>{std::vector<double>{1.0, 1.0}, std::vector<double>{1.0, 2.0}, std::vector<double>{1.0, 3.0}};
std::vector<double> data_y = std::vector<double>{1.0, 2.0, 3.0};
std::vector<double> theta = std::vector<double>{};
int64_t i = int64_t(0);
std::vector<double> predicted_y = std::vector<double>{3.0, -0.5, 2.0, 7.0};
std::vector<double> original_y = std::vector<double>{2.5, 0.0, 2.0, 8.0};
double mae = 0;

double dot(const std::vector<double>& x, const std::vector<double>& y) {
    double sum = 0.0;
    int64_t i = int64_t(0);
    while ((i < x.size())) {
        sum = (sum + (_index(x, i) * _index(y, i)));
        i = (i + int64_t(1));
    }
    return sum;
}

std::vector<double> run_steep_gradient_descent(const std::vector<std::vector<double>>& data_x, const std::vector<double>& data_y, int64_t len_data, double alpha, const std::vector<double>& theta) {
    std::vector<double> gradients = {};
    int64_t j = int64_t(0);
    while ((j < theta.size())) {
        gradients = ([&]{ auto __tmp = gradients; __tmp.push_back(0.0); return __tmp; }());
        j = (j + int64_t(1));
    }
    int64_t i = int64_t(0);
    while ((i < len_data)) {
        double prediction = dot(theta, _index(data_x, i));
        double error = (prediction - _index(data_y, i));
        int64_t k = int64_t(0);
        while ((k < theta.size())) {
            gradients[static_cast<size_t>(k)] = (_index(gradients, k) + (error * _index(_index(data_x, i), k)));
            k = (k + int64_t(1));
        }
        i = (i + int64_t(1));
    }
    std::vector<double> t = {};
    int64_t g = int64_t(0);
    while ((g < theta.size())) {
        t = ([&]{ auto __tmp = t; __tmp.push_back((_index(theta, g) - (((double)(alpha) / (len_data)) * _index(gradients, g)))); return __tmp; }());
        g = (g + int64_t(1));
    }
    return t;
}

double sum_of_square_error(const std::vector<std::vector<double>>& data_x, const std::vector<double>& data_y, int64_t len_data, const std::vector<double>& theta) {
    double total = 0.0;
    int64_t i = int64_t(0);
    while ((i < len_data)) {
        double prediction = dot(theta, _index(data_x, i));
        double diff = (prediction - _index(data_y, i));
        total = (total + (diff * diff));
        i = (i + int64_t(1));
    }
    return ((double)(total) / ((2.0 * len_data)));
}

std::vector<double> run_linear_regression(const std::vector<std::vector<double>>& data_x, const std::vector<double>& data_y) {
    int64_t iterations = int64_t(10);
    double alpha = 0.01;
    int64_t no_features = _index(data_x, int64_t(0)).size();
    int64_t len_data = data_x.size();
    std::vector<double> theta = {};
    int64_t i = int64_t(0);
    while ((i < no_features)) {
        theta = ([&]{ auto __tmp = theta; __tmp.push_back(0.0); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t iter = int64_t(0);
    while ((iter < iterations)) {
        theta = run_steep_gradient_descent(data_x, data_y, len_data, alpha, theta);
        double error = sum_of_square_error(data_x, data_y, len_data, theta);
        std::cout << (((std::string("At Iteration ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << (iter + int64_t(1)); return ss.str(); }())) + std::string(" - Error is ")) + _to_string(error));
        std::cout << std::endl;
        iter = (iter + int64_t(1));
    }
    return theta;
}

double absf(double x) {
    if ((x < 0.0)) {
        return -x;
    } else {
        return x;
    }
}

double mean_absolute_error(const std::vector<double>& predicted_y, const std::vector<double>& original_y) {
    double total = 0.0;
    int64_t i = int64_t(0);
    while ((i < predicted_y.size())) {
        double diff = absf((_index(predicted_y, i) - _index(original_y, i)));
        total = (total + diff);
        i = (i + int64_t(1));
    }
    return ((double)(total) / (predicted_y.size()));
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        theta = run_linear_regression(data_x, data_y);
        std::cout << std::string("Resultant Feature vector :");
        std::cout << std::endl;
        while ((i < theta.size())) {
            std::cout << _to_string(_index(theta, i));
            std::cout << std::endl;
            i = (i + int64_t(1));
        }
        mae = mean_absolute_error(predicted_y, original_y);
        std::cout << (std::string("Mean Absolute Error : ") + _to_string(mae));
        std::cout << std::endl;
    }
    return 0;
}
