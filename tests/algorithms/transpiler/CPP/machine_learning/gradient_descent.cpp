// Generated by Mochi transpiler v0.10.67 on 2025-08-17 00:53:36 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}
struct DataPoint;

std::ostream& operator<<(std::ostream&, const DataPoint&);


struct DataPoint {
    std::vector<double> x;
    double y;
    auto operator<=>(const DataPoint&) const = default;
};

std::ostream& operator<<(std::ostream& os, const DataPoint& v) {
    os << '{' << "'x': "<< "["; for(size_t i=0;i<v.x.size();++i){ if(i>0) os << ", "; os << v.x[i]; } os << "]"
 << ", " << "'y': "<< std::fixed << std::setprecision(1) << v.y
 << '}';
    return os;
}

double absf(double x);
double hypothesis_value(const std::vector<double>& input, const std::vector<double>& params);
double calc_error(const DataPoint& dp, const std::vector<double>& params);
double summation_of_cost_derivative(int64_t index, const std::vector<double>& params, const std::vector<DataPoint>& data);
double get_cost_derivative(int64_t index, const std::vector<double>& params, const std::vector<DataPoint>& data);
bool allclose(const std::vector<double>& a, const std::vector<double>& b, double atol, double rtol);
std::vector<double> run_gradient_descent(const std::vector<DataPoint>& train_data, const std::vector<double>& initial_params);
void test_gradient_descent(const std::vector<DataPoint>& test_data, const std::vector<double>& params);
int main();

std::vector<DataPoint> train_data = std::vector<DataPoint>{DataPoint{.x = std::vector<double>{5.0, 2.0, 3.0}, .y = 15.0}, DataPoint{.x = std::vector<double>{6.0, 5.0, 9.0}, .y = 25.0}, DataPoint{.x = std::vector<double>{11.0, 12.0, 13.0}, .y = 41.0}, DataPoint{.x = std::vector<double>{1.0, 1.0, 1.0}, .y = 8.0}, DataPoint{.x = std::vector<double>{11.0, 12.0, 13.0}, .y = 41.0}};
std::vector<DataPoint> test_data = std::vector<DataPoint>{DataPoint{.x = std::vector<double>{515.0, 22.0, 13.0}, .y = 555.0}, DataPoint{.x = std::vector<double>{61.0, 35.0, 49.0}, .y = 150.0}};
std::vector<double> parameter_vector = std::vector<double>{2.0, 4.0, 1.0, 5.0};

double absf(double x) {
    if ((x < 0.0)) {
        return -x;
    }
    return x;
}

double hypothesis_value(const std::vector<double>& input, const std::vector<double>& params) {
    double value = _index(params, int64_t(0));
    int64_t i = int64_t(0);
    while ((i < input.size())) {
        value = (value + (_index(input, i) * _index(params, (i + int64_t(1)))));
        i = (i + int64_t(1));
    }
    return value;
}

double calc_error(const DataPoint& dp, const std::vector<double>& params) {
    return (hypothesis_value(dp.x, params) - dp.y);
}

double summation_of_cost_derivative(int64_t index, const std::vector<double>& params, const std::vector<DataPoint>& data) {
    double sum = 0.0;
    int64_t i = int64_t(0);
    while ((i < data.size())) {
        DataPoint dp = _index(data, i);
        double e = calc_error(dp, params);
        if ((index == -int64_t(1))) {
            sum = (sum + e);
        } else {
            sum = (sum + (e * _index(dp.x, index)));
        }
        i = (i + int64_t(1));
    }
    return sum;
}

double get_cost_derivative(int64_t index, const std::vector<double>& params, const std::vector<DataPoint>& data) {
    return ((double)(summation_of_cost_derivative(index, params, data)) / ((double)(data.size())));
}

bool allclose(const std::vector<double>& a, const std::vector<double>& b, double atol, double rtol) {
    int64_t i = int64_t(0);
    while ((i < a.size())) {
        double diff = absf((_index(a, i) - _index(b, i)));
        double limit = (atol + (rtol * absf(_index(b, i))));
        if ((diff > limit)) {
            return false;
        }
        i = (i + int64_t(1));
    }
    return true;
}

std::vector<double> run_gradient_descent(const std::vector<DataPoint>& train_data, const std::vector<double>& initial_params) {
    double learning_rate = 0.009;
    double absolute_error_limit = 2e-06;
    double relative_error_limit = 0.0;
    int64_t j = int64_t(0);
    std::vector<double> params = initial_params;
    while (true) {
        j = (j + int64_t(1));
        std::vector<double> temp = {};
        int64_t i = int64_t(0);
        while ((i < params.size())) {
            double deriv = get_cost_derivative((i - int64_t(1)), params, train_data);
            temp = ([&]{ auto __tmp = temp; __tmp.push_back((_index(params, i) - (learning_rate * deriv))); return __tmp; }());
            i = (i + int64_t(1));
        }
        if (allclose(params, temp, absolute_error_limit, relative_error_limit)) {
            std::cout << (std::string("Number of iterations:") + ([&]{ std::ostringstream ss; ss << std::boolalpha << j; return ss.str(); }()));
            std::cout << std::endl;
            break;
        }
        params = temp;
    }
    return params;
}

void test_gradient_descent(const std::vector<DataPoint>& test_data, const std::vector<double>& params) {
    int64_t i = int64_t(0);
    while ((i < test_data.size())) {
        DataPoint dp = _index(test_data, i);
        std::cout << (std::string("Actual output value:") + _to_string(dp.y));
        std::cout << std::endl;
        std::cout << (std::string("Hypothesis output:") + _to_string(hypothesis_value(dp.x, params)));
        std::cout << std::endl;
        i = (i + int64_t(1));
    }
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        parameter_vector = run_gradient_descent(train_data, parameter_vector);
        std::cout << std::string("\nTesting gradient descent for a linear hypothesis function.\n");
        std::cout << std::endl;
        test_gradient_descent(test_data, parameter_vector);
    }
    return 0;
}
