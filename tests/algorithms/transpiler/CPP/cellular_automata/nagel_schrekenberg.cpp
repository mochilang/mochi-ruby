// Generated by Mochi transpiler v0.10.65 on 2025-08-14 16:29:43 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
template<typename V> auto _index(const V& v, int64_t i) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return typename V::value_type{};
    return v[static_cast<size_t>(i)];
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        std::ostringstream ss;
        ss << std::defaultfloat << std::setprecision(15) << v;
        auto s = ss.str();
        auto epos = s.find('e');
        if(epos == std::string::npos) epos = s.find('E');
        std::string exp;
        if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
        auto pos = s.find('.');
        if(pos != std::string::npos){
            while(!s.empty() && s.back() == '0') s.pop_back();
            if(!s.empty() && s.back() == '.') s.pop_back();
        }
        return s + exp;
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

int64_t _rand();
int64_t randint(int64_t a, int64_t b);
double _random();
std::vector<std::vector<int64_t>> construct_highway(int64_t number_of_cells, int64_t frequency, int64_t& initial_speed, bool random_frequency, bool random_speed, int64_t max_speed);
int64_t get_distance(const std::vector<int64_t>& highway_now, int64_t car_index);
std::vector<int64_t> update(const std::vector<int64_t>& highway_now, double probability, int64_t max_speed);
std::vector<std::vector<int64_t>> simulate(std::vector<std::vector<int64_t>>& highway, int64_t number_of_update, double probability, int64_t max_speed);
int main();

int64_t seed = int64_t(1);
int64_t NEG_ONE = -int64_t(1);

int64_t _rand() {
    seed = (((seed * int64_t(1103515245)) + int64_t(12345)) % int64_t(2147483648));
    return seed;
}

int64_t randint(int64_t a, int64_t b) {
    int64_t r = _rand();
    return (a + (r % ((b - a) + int64_t(1))));
}

double _random() {
    return ((double)((1.0 * _rand())) / (2147483648.0));
}

std::vector<std::vector<int64_t>> construct_highway(int64_t number_of_cells, int64_t frequency, int64_t& initial_speed, bool random_frequency, bool random_speed, int64_t max_speed) {
    std::vector<int64_t> row = {};
    int64_t i = int64_t(0);
    while ((i < number_of_cells)) {
        row = ([&]{ auto __tmp = row; __tmp.push_back(-int64_t(1)); return __tmp; }());
        i = (i + int64_t(1));
    }
    std::vector<std::vector<int64_t>> highway = {};
    highway = ([&]{ auto __tmp = highway; __tmp.push_back(row); return __tmp; }());
    i = int64_t(0);
    if ((initial_speed < int64_t(0))) {
        initial_speed = int64_t(0);
    }
    while ((i < number_of_cells)) {
        int64_t speed = initial_speed;
        if (random_speed) {
            speed = randint(int64_t(0), max_speed);
        }
        highway[static_cast<size_t>(int64_t(0))][static_cast<size_t>(i)] = speed;
        int64_t step = frequency;
        if (random_frequency) {
            step = randint(int64_t(1), (max_speed * int64_t(2)));
        }
        i = (i + step);
    }
    return highway;
}

int64_t get_distance(const std::vector<int64_t>& highway_now, int64_t car_index) {
    int64_t distance = int64_t(0);
    int64_t i = (car_index + int64_t(1));
    while ((i < highway_now.size())) {
        if ((_index(highway_now, i) > NEG_ONE)) {
            return distance;
        }
        distance = (distance + int64_t(1));
        i = (i + int64_t(1));
    }
    return (distance + get_distance(highway_now, -int64_t(1)));
}

std::vector<int64_t> update(const std::vector<int64_t>& highway_now, double probability, int64_t max_speed) {
    int64_t number_of_cells = highway_now.size();
    std::vector<int64_t> next_highway = {};
    int64_t i = int64_t(0);
    while ((i < number_of_cells)) {
        next_highway = ([&]{ auto __tmp = next_highway; __tmp.push_back(-int64_t(1)); return __tmp; }());
        i = (i + int64_t(1));
    }
    int64_t car_index = int64_t(0);
    while ((car_index < number_of_cells)) {
        int64_t speed = _index(highway_now, car_index);
        if ((speed > NEG_ONE)) {
            int64_t new_speed = (speed + int64_t(1));
            if ((new_speed > max_speed)) {
                new_speed = max_speed;
            }
            int64_t dn = (get_distance(highway_now, car_index) - int64_t(1));
            if ((new_speed > dn)) {
                new_speed = dn;
            }
            if ((_random() < probability)) {
                new_speed = (new_speed - int64_t(1));
                if ((new_speed < int64_t(0))) {
                    new_speed = int64_t(0);
                }
            }
            next_highway[static_cast<size_t>(car_index)] = new_speed;
        }
        car_index = (car_index + int64_t(1));
    }
    return next_highway;
}

std::vector<std::vector<int64_t>> simulate(std::vector<std::vector<int64_t>>& highway, int64_t number_of_update, double probability, int64_t max_speed) {
    int64_t number_of_cells = _index(highway, int64_t(0)).size();
    int64_t i = int64_t(0);
    while ((i < number_of_update)) {
        std::vector<int64_t> next_speeds = update(_index(highway, i), probability, max_speed);
        std::vector<int64_t> real_next = {};
        int64_t j = int64_t(0);
        while ((j < number_of_cells)) {
            real_next = ([&]{ auto __tmp = real_next; __tmp.push_back(-int64_t(1)); return __tmp; }());
            j = (j + int64_t(1));
        }
        int64_t k = int64_t(0);
        while ((k < number_of_cells)) {
            int64_t speed = _index(next_speeds, k);
            if ((speed > NEG_ONE)) {
                int64_t index = ((k + speed) % number_of_cells);
                real_next[static_cast<size_t>(index)] = speed;
            }
            k = (k + int64_t(1));
        }
        highway = ([&]{ auto __tmp = highway; __tmp.push_back(real_next); return __tmp; }());
        i = (i + int64_t(1));
    }
    return highway;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::vector<std::vector<int64_t>> ex1 = ([&]{ auto __arg0 = ([&]{ auto __arg2 = int64_t(0); return construct_highway(int64_t(6), int64_t(3), __arg2, false, false, int64_t(2)); }()); return simulate(__arg0, int64_t(2), 0.0, int64_t(2)); }());
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp1 = ex1; ss << "["; for(size_t i=0;i<__tmp1.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; { auto __tmp2 = __tmp1[static_cast<size_t>(i)]; __ss << "["; for(size_t i=0;i<__tmp2.size();++i){ if(i>0) __ss << ' '; __ss << __tmp2[i]; } __ss << "]"; }
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
        std::vector<std::vector<int64_t>> ex2 = ([&]{ auto __arg0 = ([&]{ auto __arg2 = -int64_t(2); return construct_highway(int64_t(5), int64_t(2), __arg2, false, false, int64_t(2)); }()); return simulate(__arg0, int64_t(3), 0.0, int64_t(2)); }());
        std::cout << ([&]{ std::ostringstream ss; { auto __tmp3 = ex2; ss << "["; for(size_t i=0;i<__tmp3.size();++i){ if(i>0) ss << ' '; { std::ostringstream __ss; { auto __tmp4 = __tmp3[static_cast<size_t>(i)]; __ss << "["; for(size_t i=0;i<__tmp4.size();++i){ if(i>0) __ss << ' '; __ss << __tmp4[i]; } __ss << "]"; }
 ss << __ss.str(); }; } ss << "]"; }
 return ss.str(); }());
        std::cout << std::endl;
    }
    return 0;
}
