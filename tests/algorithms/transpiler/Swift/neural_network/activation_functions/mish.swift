// Generated by Mochi transpiler v0.10.73 on 2025-08-25 23:48:56 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func exp_approx(_ x: Double) -> Double {
        var neg = false
        var y = x
        if ((x < 0.0)) {
            neg = true
            y = _num(-x)
        }
        var term = 1.0
        var sum = 1.0
        var n = 1
        while ((n < 30)) {
            term = _num(((term * y) / _num(n)))
            sum = _num((sum + term))
            n = _int((n &+ 1))
        }
        if (neg) {
            return (1.0 / sum)
        }
        return sum
    }
    func ln_series(_ x: Double) -> Double {
        let t = (_num((x - 1.0)) / _num((x + 1.0)))
        var term = t
        var acc = 0.0
        var n = 1
        while ((n <= 19)) {
            acc = _num((acc + (term / _num(n))))
            term = _num(((term * t) * t))
            n = _int((n &+ 2))
        }
        return (2.0 * acc)
    }
    func ln(_ x: Double) -> Double {
        return Foundation.log(x)
    }
    func softplus(_ x: Double) -> Double {
        return _num(ln(_num((1.0 + _num(exp_approx(_num(x)))))))
    }
    func tanh_approx(_ x: Double) -> Double {
        return (_num((2.0 / _num((1.0 + _num(exp_approx(_num((-2.0 * x)))))))) - 1.0)
    }
    func mish(_ vector: [Double]) -> [Double] {
        var result: [Double] = ([] as! [Double])
        var i = 0
        while ((i < _int(((vector).count)))) {
            let x = _num(_idx(vector, i))
            let sp = _num(softplus(_num(x)))
            let y = (x * _num(tanh_approx(_num(sp))))
            result = (_append(result, y) as! [Double])
            i = _int((i &+ 1))
        }
        return result
    }
    func main() {
        let v1: [Double] = ({
            var _arr: [Double] = []
            _arr = _append(_arr, 2.2999999999999998)
            _arr = _append(_arr, 0.59999999999999998)
            _arr = _append(_arr, -2.0)
            _arr = _append(_arr, -3.7999999999999998)
            return _arr
        }() as! [Double])
        let v2: [Double] = ({
            var _arr: [Double] = []
            _arr = _append(_arr, -9.1999999999999993)
            _arr = _append(_arr, -0.29999999999999999)
            _arr = _append(_arr, 0.45000000000000001)
            _arr = _append(_arr, -4.5599999999999996)
            return _arr
        }() as! [Double])
        print(_p("[" + (mish((v1 as! [Double])) as! [Double]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
        print(_p("[" + (mish((v2 as! [Double])) as! [Double]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
