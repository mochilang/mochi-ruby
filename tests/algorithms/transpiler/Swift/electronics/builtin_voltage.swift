// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:41:15 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func pow10(_ n: Int) -> Double {
        var result = 1.0
        var i = 0
        while ((i < n)) {
            result = _num((result * 10.0))
            i = _int((i &+ 1))
        }
        return result
    }
    let BOLTZMANN: Double = (1.380649 / _num(pow10(23)))
    let ELECTRON_VOLT: Double = (1.6021766340000001 / _num(pow10(19)))
    let TEMPERATURE: Double = 300.0
    func ln_series(_ x: Double) -> Double {
        let t = (_num((x - 1.0)) / _num((x + 1.0)))
        var term = t
        var sum = 0.0
        var n = 1
        while ((n <= 19)) {
            sum = _num((sum + (term / _num(n))))
            term = _num(((term * t) * t))
            n = _int((n &+ 2))
        }
        return (2.0 * sum)
    }
    func ln(_ x: Double) -> Double {
        var y = x
        var k = 0
        while ((y >= 10.0)) {
            y = _num((y / 10.0))
            k = _int((k &+ 1))
        }
        while ((y < 1.0)) {
            y = _num((y * 10.0))
            k = _int((k &- 1))
        }
        return (_num(ln_series(_num(y))) + (_num(k) * _num(ln_series(10.0))))
    }
    func builtin_voltage(_ donor_conc: Double, _ acceptor_conc: Double, _ intrinsic_conc: Double) -> Double {
        if ((donor_conc <= 0.0)) {
            _ = fatalError("Donor concentration should be positive")
        }
        if ((acceptor_conc <= 0.0)) {
            _ = fatalError("Acceptor concentration should be positive")
        }
        if ((intrinsic_conc <= 0.0)) {
            _ = fatalError("Intrinsic concentration should be positive")
        }
        if ((donor_conc <= intrinsic_conc)) {
            _ = fatalError("Donor concentration should be greater than intrinsic concentration")
        }
        if ((acceptor_conc <= intrinsic_conc)) {
            _ = fatalError("Acceptor concentration should be greater than intrinsic concentration")
        }
        return (((BOLTZMANN * TEMPERATURE) * _num(ln(_num((_num((donor_conc * acceptor_conc)) / _num((intrinsic_conc * intrinsic_conc))))))) / ELECTRON_VOLT)
    }
    print(_p(_p(_num(builtin_voltage(_num(pow10(17)), _num(pow10(17)), _num(pow10(10)))))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
