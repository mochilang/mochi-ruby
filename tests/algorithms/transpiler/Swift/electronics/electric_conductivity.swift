// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:41:28 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
struct Result: Codable {
    var kind: String
    var value: Double
    init() {
        self.kind = ""
        self.value = 0
    }
    init(kind: String, value: Double) {
        self.kind = kind
        self.value = value
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let ELECTRON_CHARGE: Double = 1.6021e-19
    func electric_conductivity(_ conductivity: Double, _ electron_conc: Double, _ mobility: Double) -> Result {
        var zero_count = 0
        if ((conductivity == 0.0)) {
            zero_count = _int((zero_count &+ 1))
        }
        if ((electron_conc == 0.0)) {
            zero_count = _int((zero_count &+ 1))
        }
        if ((mobility == 0.0)) {
            zero_count = _int((zero_count &+ 1))
        }
        if ((zero_count != 1)) {
            _ = fatalError("You cannot supply more or less than 2 values")
        }
        if ((conductivity < 0.0)) {
            _ = fatalError("Conductivity cannot be negative")
        }
        if ((electron_conc < 0.0)) {
            _ = fatalError("Electron concentration cannot be negative")
        }
        if ((mobility < 0.0)) {
            _ = fatalError("mobility cannot be negative")
        }
        if ((conductivity == 0.0)) {
            return Result(kind: "conductivity", value: _num(((mobility * electron_conc) * ELECTRON_CHARGE)))
        }
        if ((electron_conc == 0.0)) {
            return Result(kind: "electron_conc", value: _num((conductivity / _num((mobility * ELECTRON_CHARGE)))))
        }
        return Result(kind: "mobility", value: _num((conductivity / _num((electron_conc * ELECTRON_CHARGE)))))
    }
    let r1 = electric_conductivity(25.0, 100.0, 0.0)
    let r2 = electric_conductivity(0.0, 1600.0, 200.0)
    let r3 = electric_conductivity(1000.0, 0.0, 1200.0)
    print(_p((_p((_p(r1.kind) + _p(" "))) + _p(_p(r1.value)))))
    print(_p((_p((_p(r2.kind) + _p(" "))) + _p(_p(r2.value)))))
    print(_p((_p((_p(r3.kind) + _p(" "))) + _p(_p(r3.value)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
