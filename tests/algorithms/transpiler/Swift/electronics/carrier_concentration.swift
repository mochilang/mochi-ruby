// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:41:19 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
struct CarrierResult: Codable {
    var name: String
    var value: Double
    init() {
        self.name = ""
        self.value = 0
    }
    init(name: String, value: Double) {
        self.name = name
        self.value = value
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func sqrtApprox(_ x: Double) -> Double {
        var guess = (x / 2.0)
        var i = 0
        while ((i < 20)) {
            guess = _num((_num((guess + (x / guess))) / 2.0))
            i = _int((i &+ 1))
        }
        return guess
    }
    func carrier_concentration(_ electron_conc: Double, _ hole_conc: Double, _ intrinsic_conc: Double) -> CarrierResult {
        var zero_count = 0
        if ((electron_conc == 0.0)) {
            zero_count = _int((zero_count &+ 1))
        }
        if ((hole_conc == 0.0)) {
            zero_count = _int((zero_count &+ 1))
        }
        if ((intrinsic_conc == 0.0)) {
            zero_count = _int((zero_count &+ 1))
        }
        if ((zero_count != 1)) {
            _ = fatalError("You cannot supply more or less than 2 values")
        }
        if ((electron_conc < 0.0)) {
            _ = fatalError("Electron concentration cannot be negative in a semiconductor")
        }
        if ((hole_conc < 0.0)) {
            _ = fatalError("Hole concentration cannot be negative in a semiconductor")
        }
        if ((intrinsic_conc < 0.0)) {
            _ = fatalError("Intrinsic concentration cannot be negative in a semiconductor")
        }
        if ((electron_conc == 0.0)) {
            return CarrierResult(name: "electron_conc", value: _num((_num((intrinsic_conc * intrinsic_conc)) / hole_conc)))
        }
        if ((hole_conc == 0.0)) {
            return CarrierResult(name: "hole_conc", value: _num((_num((intrinsic_conc * intrinsic_conc)) / electron_conc)))
        }
        if ((intrinsic_conc == 0.0)) {
            return CarrierResult(name: "intrinsic_conc", value: _num(sqrtApprox(_num((electron_conc * hole_conc)))))
        }
        return CarrierResult(name: "", value: _num(-1.0))
    }
    let r1 = carrier_concentration(25.0, 100.0, 0.0)
    print(_p((_p((_p(r1.name) + _p(", "))) + _p(_p(r1.value)))))
    let r2 = carrier_concentration(0.0, 1600.0, 200.0)
    print(_p((_p((_p(r2.name) + _p(", "))) + _p(_p(r2.value)))))
    let r3 = carrier_concentration(1000.0, 0.0, 1200.0)
    print(_p((_p((_p(r3.name) + _p(", "))) + _p(_p(r3.value)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
