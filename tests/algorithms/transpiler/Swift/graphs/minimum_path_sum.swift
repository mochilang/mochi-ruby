// Generated by Mochi transpiler v0.10.66 on 2025-08-15 15:22:07 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func fill_row(_ current_row: inout [Int], _ row_above: [Int]) -> [Int] {
        current_row = _set(current_row, 0, _int(((_idx(current_row, 0) as? Int ?? 0) &+ (_idx(row_above, 0) as? Int ?? 0))))
        var cell_n = 1
        while (cell_n < _int(((current_row).count))) {
            let left = (_idx(current_row, (cell_n &- 1)) as? Int ?? 0)
            let up = (_idx(row_above, cell_n) as? Int ?? 0)
            if (left < up) {
                current_row = _set(current_row, cell_n, _int(((_idx(current_row, cell_n) as? Int ?? 0) &+ left)))
            } else {
                current_row = _set(current_row, cell_n, _int(((_idx(current_row, cell_n) as? Int ?? 0) &+ up)))
            }
            cell_n = _int((cell_n &+ 1))
        }
        return current_row
    }
    func min_path_sum(_ grid: inout [[Int]]) -> Int {
        if ((_int(((grid).count)) == 0) || (_int((((_idx(grid, 0) as! [Int])).count)) == 0)) {
            _ = fatalError("The grid does not contain the appropriate information")
        }
        var cell_n = 1
        while (cell_n < _int((((_idx(grid, 0) as! [Int])).count))) {
            var _tmp2 = (_idx(grid, 0) as! [Int])
            _tmp2 = _set(_tmp2, cell_n, _int(((_idx(_idx(grid, 0)!, cell_n) as? Int ?? 0) &+ (_idx(_idx(grid, 0)!, (cell_n &- 1)) as? Int ?? 0))))
            grid = _set(grid, 0, (_tmp2 as! [Int]))
            cell_n = _int((cell_n &+ 1))
        }
        var row_above = (_idx(grid, 0) as! [Int])
        var row_n = 1
        while (row_n < _int(((grid).count))) {
            var current_row: [Int] = (_idx(grid, row_n) as! [Int])
            grid = _set(grid, row_n, (fill_row(&current_row, (row_above as! [Int])) as! [Int]))
            row_above = (_idx(grid, row_n) as! [Int])
            row_n = _int((row_n &+ 1))
        }
        return (_idx(_idx(grid, (_int(((grid).count)) &- 1))!, (_int((((_idx(grid, 0) as! [Int])).count)) &- 1)) as? Int ?? 0)
    }
    var grid1: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    print(_p(_p(_int(min_path_sum(&grid1)))))
    var grid2: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 6)
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 8)
            _arr = _append(_arr, 9)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 2)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 9)
            _arr = _append(_arr, 6)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 8)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    print(_p(_p(_int(min_path_sum(&grid2)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
