// Generated by Mochi transpiler v0.10.65 on 2025-08-14 17:13:12 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
struct Graph: Codable {
    var graph: [String: [String]]
    var parent: [String: String]
    var source: String
    init() {
        self.graph = [:]
        self.parent = [:]
        self.source = ""
    }
    init(graph: [String: [String]], parent: [String: String], source: String) {
        self.graph = graph
        self.parent = parent
        self.source = source
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func newGraph(_ g: [String: [String]], _ s: String) -> Graph {
        return Graph(graph: (g as! [String: [String]]), parent: ([:] as! [String: String]), source: String(describing: s))
    }
    func breath_first_search(_ g: inout Graph) -> Graph {
        var parent = g.parent
        parent = _set(parent, String(describing: g.source), (g.source as! String))
        var queue: [String] = ({
            var _arr: [String] = []
            _arr = _append(_arr, g.source)
            return _arr
        }() as! [String])
        var idx = 0
        while (idx < _int(((queue).count))) {
            let vertex = (_idx(queue, idx) as! String)
            for adj in ((g.graph[String(String(describing: vertex))] ?? []) as! [String]) {
                if (!(parent[adj] != nil)) {
                    parent = _set(parent, String(describing: adj), (vertex as! String))
                    queue = (_append(queue, adj) as! [String])
                }
            }
            idx = _int((idx &+ 1))
        }
        g.parent = (parent as! [String: String])
        return g
    }
    func shortest_path(_ g: Graph, _ target: String) -> String {
        if (target == g.source) {
            return g.source
        }
        if (!(g.parent[target] != nil)) {
            return ((("No path from vertex: " + g.source) + " to vertex: ") + target)
        }
        let p = ((g.parent[String(String(describing: target))] ?? "") as! String)
        return ((String(describing: shortest_path((g as! Graph), (p as! String))) + "->") + target)
    }
    let graph: [String: [String]] = (["A": ({
        var _arr: [String] = []
        _arr = _append(_arr, "B")
        _arr = _append(_arr, "C")
        _arr = _append(_arr, "E")
        return _arr
    }() as! [String]), "B": ({
        var _arr: [String] = []
        _arr = _append(_arr, "A")
        _arr = _append(_arr, "D")
        _arr = _append(_arr, "E")
        return _arr
    }() as! [String]), "C": ({
        var _arr: [String] = []
        _arr = _append(_arr, "A")
        _arr = _append(_arr, "F")
        _arr = _append(_arr, "G")
        return _arr
    }() as! [String]), "D": ({
        var _arr: [String] = []
        _arr = _append(_arr, "B")
        return _arr
    }() as! [String]), "E": ({
        var _arr: [String] = []
        _arr = _append(_arr, "A")
        _arr = _append(_arr, "B")
        _arr = _append(_arr, "D")
        return _arr
    }() as! [String]), "F": ({
        var _arr: [String] = []
        _arr = _append(_arr, "C")
        return _arr
    }() as! [String]), "G": ({
        var _arr: [String] = []
        _arr = _append(_arr, "C")
        return _arr
    }() as! [String])] as! [String: [String]])
    var g = newGraph((graph as! [String: [String]]), "G")
    g = (breath_first_search(&g) as! Graph)
    print(_p(String(describing: shortest_path((g as! Graph), "D"))))
    print(_p(String(describing: shortest_path((g as! Graph), "G"))))
    print(_p(String(describing: shortest_path((g as! Graph), "Foo"))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
