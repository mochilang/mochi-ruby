// Generated by Mochi transpiler v0.10.66 on 2025-08-15 15:21:58 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
struct LanczosResult: Codable {
    var t: [[Double]]
    var q: [[Double]]
    init() {
        self.t = []
        self.q = []
    }
    init(t: [[Double]], q: [[Double]]) {
        self.t = t
        self.q = q
    }
}
struct EigenResult: Codable {
    var values: [Double]
    var vectors: [[Double]]
    init() {
        self.values = []
        self.vectors = []
    }
    init(values: [Double], vectors: [[Double]]) {
        self.values = values
        self.vectors = vectors
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    var seed = 123456789
    func rand() -> Int {
        seed = _int(((_int(((seed &* 1103515245) &+ 12345)) % 2147483648 + 2147483648) % 2147483648))
        return seed
    }
    func random() -> Double {
        return (Double((1.0 * Double(_int(rand())))) / 2147483648.0)
    }
    func sqrtApprox(_ x: Double) -> Double {
        if (x <= 0.0) {
            return 0.0
        }
        var guess: Double = x
        var i = 0
        while (i < 20) {
            guess = Double((Double((guess + (x / guess))) / 2.0))
            i = _int((i &+ 1))
        }
        return guess
    }
    func absf(_ x: Double) -> Double {
        return Double(((x < 0.0) ? -x : x))
    }
    func dot(_ a: [Double], _ b: [Double]) -> Double {
        var s = 0.0
        var i = 0
        while (i < _int(((a).count))) {
            s = Double((s + (Double(_idx(a, i)) * Double(_idx(b, i)))))
            i = _int((i &+ 1))
        }
        return s
    }
    func vector_scale(_ v: [Double], _ s: Double) -> [Double] {
        var res: [Double] = ([] as! [Double])
        var i = 0
        while (i < _int(((v).count))) {
            res = (_append(res, (Double(_idx(v, i)) * s)) as! [Double])
            i = _int((i &+ 1))
        }
        return res
    }
    func vector_sub(_ a: [Double], _ b: [Double]) -> [Double] {
        var res: [Double] = ([] as! [Double])
        var i = 0
        while (i < _int(((a).count))) {
            res = (_append(res, (Double(_idx(a, i)) - Double(_idx(b, i)))) as! [Double])
            i = _int((i &+ 1))
        }
        return res
    }
    func vector_add(_ a: [Double], _ b: [Double]) -> [Double] {
        var res: [Double] = ([] as! [Double])
        var i = 0
        while (i < _int(((a).count))) {
            res = (_append(res, (Double(_idx(a, i)) + Double(_idx(b, i)))) as! [Double])
            i = _int((i &+ 1))
        }
        return res
    }
    func zeros_matrix(_ r: Int, _ c: Int) -> [[Double]] {
        var m: [[Double]] = ([] as! [[Double]])
        var i = 0
        while (i < r) {
            var row: [Double] = ([] as! [Double])
            var j = 0
            while (j < c) {
                row = (_append(row, 0.0) as! [Double])
                j = _int((j &+ 1))
            }
            m = (_append(m, row) as! [[Double]])
            i = _int((i &+ 1))
        }
        return m
    }
    func column(_ m: [[Double]], _ idx: Int) -> [Double] {
        var col: [Double] = ([] as! [Double])
        var i = 0
        while (i < _int(((m).count))) {
            col = (_append(col, Double(_idx(_idx(m, i)!, idx))) as! [Double])
            i = _int((i &+ 1))
        }
        return col
    }
    func validate_adjacency_list(_ graph: [[Int]]) -> Void {
        var i = 0
        while (i < _int(((graph).count))) {
            var j = 0
            while (j < _int((((_idx(graph, i) as! [Int])).count))) {
                let v = (_idx(_idx(graph, i)!, j) as? Int ?? 0)
                if ((v < 0) || (v >= _int(((graph).count)))) {
                    _ = fatalError("Invalid neighbor")
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
    }
    func multiply_matrix_vector(_ graph: [[Int]], _ vector: [Double]) -> [Double] {
        let n = _int(((graph).count))
        if (_int(((vector).count)) != n) {
            _ = fatalError("Vector length must match number of nodes")
        }
        var result: [Double] = ([] as! [Double])
        var i = 0
        while (i < n) {
            var sum = 0.0
            var j = 0
            while (j < _int((((_idx(graph, i) as! [Int])).count))) {
                let nb = (_idx(_idx(graph, i)!, j) as? Int ?? 0)
                sum = Double((sum + Double(_idx(vector, nb))))
                j = _int((j &+ 1))
            }
            result = (_append(result, sum) as! [Double])
            i = _int((i &+ 1))
        }
        return result
    }
    func lanczos_iteration(_ graph: [[Int]], _ k: Int) -> LanczosResult {
        let n = _int(((graph).count))
        if ((k < 1) || (k > n)) {
            _ = fatalError("invalid number of eigenvectors")
        }
        var q = (zeros_matrix(n, k) as! [[Double]])
        var t = (zeros_matrix(k, k) as! [[Double]])
        var v: [Double] = ([] as! [Double])
        var i = 0
        while (i < n) {
            v = (_append(v, Double(random())) as! [Double])
            i = _int((i &+ 1))
        }
        var ss = 0.0
        i = 0
        while (i < n) {
            ss = Double((ss + (Double(_idx(v, i)) * Double(_idx(v, i)))))
            i = _int((i &+ 1))
        }
        let vnorm = Double(sqrtApprox(Double(ss)))
        i = 0
        while (i < n) {
            var _tmp20 = (_idx(q, i) as! [Double])
            _tmp20 = _set(_tmp20, 0, Double((Double(_idx(v, i)) / vnorm)))
            q = _set(q, i, (_tmp20 as! [Double]))
            i = _int((i &+ 1))
        }
        var beta = 0.0
        var j = 0
        while (j < k) {
            var w = (multiply_matrix_vector((graph as! [[Int]]), (column((q as! [[Double]]), j) as! [Double])) as! [Double])
            if (j > 0) {
                w = (vector_sub((w as! [Double]), (vector_scale((column((q as! [[Double]]), (j &- 1)) as! [Double]), Double(beta)) as! [Double])) as! [Double])
            }
            let alpha = Double(dot((column((q as! [[Double]]), j) as! [Double]), (w as! [Double])))
            w = (vector_sub((w as! [Double]), (vector_scale((column((q as! [[Double]]), j) as! [Double]), Double(alpha)) as! [Double])) as! [Double])
            var ss2 = 0.0
            var p = 0
            while (p < n) {
                ss2 = Double((ss2 + (Double(_idx(w, p)) * Double(_idx(w, p)))))
                p = _int((p &+ 1))
            }
            beta = Double(sqrtApprox(Double(ss2)))
            var _tmp21 = (_idx(t, j) as! [Double])
            _tmp21 = _set(_tmp21, j, Double(alpha))
            t = _set(t, j, (_tmp21 as! [Double]))
            if (j < (k &- 1)) {
                var _tmp22 = (_idx(t, j) as! [Double])
                _tmp22 = _set(_tmp22, (j &+ 1), Double(beta))
                t = _set(t, j, (_tmp22 as! [Double]))
                var _tmp23 = (_idx(t, (j &+ 1)) as! [Double])
                _tmp23 = _set(_tmp23, j, Double(beta))
                t = _set(t, (j &+ 1), (_tmp23 as! [Double]))
                if (beta > 0.0000000001) {
                    var wnorm = (vector_scale((w as! [Double]), Double((1.0 / beta))) as! [Double])
                    var r = 0
                    while (r < n) {
                        var _tmp24 = (_idx(q, r) as! [Double])
                        _tmp24 = _set(_tmp24, (j &+ 1), Double(_idx(wnorm, r)))
                        q = _set(q, r, (_tmp24 as! [Double]))
                        r = _int((r &+ 1))
                    }
                }
            }
            j = _int((j &+ 1))
        }
        return LanczosResult(t: (t as! [[Double]]), q: (q as! [[Double]]))
    }
    func jacobi_eigen(_ a_in: [[Double]], _ max_iter: Int) -> EigenResult {
        let n = _int(((a_in).count))
        var a = a_in
        var v = (zeros_matrix(n, n) as! [[Double]])
        var i = 0
        while (i < n) {
            var _tmp25 = (_idx(v, i) as! [Double])
            _tmp25 = _set(_tmp25, i, 1.0)
            v = _set(v, i, (_tmp25 as! [Double]))
            i = _int((i &+ 1))
        }
        var iter = 0
        while (iter < max_iter) {
            var p = 0
            var q = 1
            var max = Double(absf(Double(_idx(_idx(a, p)!, q))))
            i = 0
            while (i < n) {
                var j = (i &+ 1)
                while (j < n) {
                    let val = Double(absf(Double(_idx(_idx(a, i)!, j))))
                    if (val > max) {
                        max = Double(val)
                        p = Int(i)
                        q = Int(j)
                    }
                    j = _int((j &+ 1))
                }
                i = _int((i &+ 1))
            }
            if (max < 0.00000001) {
                break
            }
            let app = Double(_idx(_idx(a, p)!, p))
            let aqq = Double(_idx(_idx(a, q)!, q))
            let apq = Double(_idx(_idx(a, p)!, q))
            let theta = (Double((aqq - app)) / Double((2.0 * apq)))
            var t = (1.0 / Double((Double(absf(Double(theta))) + Double(sqrtApprox(Double(((theta * theta) + 1.0)))))))
            if (theta < 0.0) {
                t = Double(-t)
            }
            let c = (1.0 / Double(sqrtApprox(Double((1.0 + (t * t))))))
            let s = (t * c)
            let tau = (s / Double((1.0 + c)))
            var _tmp26 = (_idx(a, p) as! [Double])
            _tmp26 = _set(_tmp26, p, Double((app - (t * apq))))
            a = _set(a, p, (_tmp26 as! [Double]))
            var _tmp27 = (_idx(a, q) as! [Double])
            _tmp27 = _set(_tmp27, q, Double((aqq + (t * apq))))
            a = _set(a, q, (_tmp27 as! [Double]))
            var _tmp28 = (_idx(a, p) as! [Double])
            _tmp28 = _set(_tmp28, q, 0.0)
            a = _set(a, p, (_tmp28 as! [Double]))
            var _tmp29 = (_idx(a, q) as! [Double])
            _tmp29 = _set(_tmp29, p, 0.0)
            a = _set(a, q, (_tmp29 as! [Double]))
            var k = 0
            while (k < n) {
                if ((k != p) && (k != q)) {
                    let akp = Double(_idx(_idx(a, k)!, p))
                    let akq = Double(_idx(_idx(a, k)!, q))
                    var _tmp30 = (_idx(a, k) as! [Double])
                    _tmp30 = _set(_tmp30, p, Double((akp - (s * Double((akq + (tau * akp)))))))
                    a = _set(a, k, (_tmp30 as! [Double]))
                    var _tmp31 = (_idx(a, p) as! [Double])
                    _tmp31 = _set(_tmp31, k, Double(_idx(_idx(a, k)!, p)))
                    a = _set(a, p, (_tmp31 as! [Double]))
                    var _tmp32 = (_idx(a, k) as! [Double])
                    _tmp32 = _set(_tmp32, q, Double((akq + (s * Double((akp - (tau * akq)))))))
                    a = _set(a, k, (_tmp32 as! [Double]))
                    var _tmp33 = (_idx(a, q) as! [Double])
                    _tmp33 = _set(_tmp33, k, Double(_idx(_idx(a, k)!, q)))
                    a = _set(a, q, (_tmp33 as! [Double]))
                }
                k = _int((k &+ 1))
            }
            k = 0
            while (k < n) {
                let vkp = Double(_idx(_idx(v, k)!, p))
                let vkq = Double(_idx(_idx(v, k)!, q))
                var _tmp34 = (_idx(v, k) as! [Double])
                _tmp34 = _set(_tmp34, p, Double((vkp - (s * Double((vkq + (tau * vkp)))))))
                v = _set(v, k, (_tmp34 as! [Double]))
                var _tmp35 = (_idx(v, k) as! [Double])
                _tmp35 = _set(_tmp35, q, Double((vkq + (s * Double((vkp - (tau * vkq)))))))
                v = _set(v, k, (_tmp35 as! [Double]))
                k = _int((k &+ 1))
            }
            iter = _int((iter &+ 1))
        }
        var eigenvalues: [Double] = ([] as! [Double])
        i = 0
        while (i < n) {
            eigenvalues = (_append(eigenvalues, Double(_idx(_idx(a, i)!, i))) as! [Double])
            i = _int((i &+ 1))
        }
        return EigenResult(values: (eigenvalues as! [Double]), vectors: (v as! [[Double]]))
    }
    func matmul(_ a: [[Double]], _ b: [[Double]]) -> [[Double]] {
        let rows = _int(((a).count))
        let cols = _int((((_idx(b, 0) as! [Double])).count))
        let inner = _int(((b).count))
        var m = (zeros_matrix(rows, cols) as! [[Double]])
        var i = 0
        while (i < rows) {
            var j = 0
            while (j < cols) {
                var s = 0.0
                var k = 0
                while (k < inner) {
                    s = Double((s + (Double(_idx(_idx(a, i)!, k)) * Double(_idx(_idx(b, k)!, j)))))
                    k = _int((k &+ 1))
                }
                var _tmp36 = (_idx(m, i) as! [Double])
                _tmp36 = _set(_tmp36, j, Double(s))
                m = _set(m, i, (_tmp36 as! [Double]))
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        return m
    }
    func sort_eigenpairs(_ vals: [Double], _ vecs: [[Double]]) -> EigenResult {
        let n = _int(((vals).count))
        var values = vals
        var vectors = vecs
        var i = 0
        while (i < n) {
            var j = 0
            while (j < (n &- 1)) {
                if (Double(_idx(values, j)) < Double(_idx(values, (j &+ 1)))) {
                    let tmp = Double(_idx(values, j))
                    values = _set(values, j, Double(_idx(values, (j &+ 1))))
                    values = _set(values, (j &+ 1), Double(tmp))
                    var r = 0
                    while (r < _int(((vectors).count))) {
                        let tv = Double(_idx(_idx(vectors, r)!, j))
                        var _tmp37 = (_idx(vectors, r) as! [Double])
                        _tmp37 = _set(_tmp37, j, Double(_idx(_idx(vectors, r)!, (j &+ 1))))
                        vectors = _set(vectors, r, (_tmp37 as! [Double]))
                        var _tmp38 = (_idx(vectors, r) as! [Double])
                        _tmp38 = _set(_tmp38, (j &+ 1), Double(tv))
                        vectors = _set(vectors, r, (_tmp38 as! [Double]))
                        r = _int((r &+ 1))
                    }
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        return EigenResult(values: (values as! [Double]), vectors: (vectors as! [[Double]]))
    }
    func find_lanczos_eigenvectors(_ graph: [[Int]], _ k: Int) -> EigenResult {
        _ = validate_adjacency_list((graph as! [[Int]]))
        let res = lanczos_iteration((graph as! [[Int]]), k)
        let eig = jacobi_eigen((res.t as! [[Double]]), 50)
        let sorted = sort_eigenpairs((eig.values as! [Double]), (eig.vectors as! [[Double]]))
        let final_vectors: [[Double]] = (matmul((res.q as! [[Double]]), (sorted.vectors as! [[Double]])) as! [[Double]])
        return EigenResult(values: (sorted.values as! [Double]), vectors: (final_vectors as! [[Double]]))
    }
    func list_to_string(_ arr: [Double]) -> String {
        var s = "["
        var i = 0
        while (i < _int(((arr).count))) {
            s = ((s + _p(Double(_idx(arr, i)))) as! String)
            if (i < (_int(((arr).count)) &- 1)) {
                s = ((s + ", ") as! String)
            }
            i = _int((i &+ 1))
        }
        return (s + "]")
    }
    func matrix_to_string(_ m: [[Double]]) -> String {
        var s = "["
        var i = 0
        while (i < _int(((m).count))) {
            s = ((s + String(describing: list_to_string((_idx(m, i) as! [Double])))) as! String)
            if (i < (_int(((m).count)) &- 1)) {
                s = ((s + "; ") as! String)
            }
            i = _int((i &+ 1))
        }
        return (s + "]")
    }
    let graph: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 2)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 2)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    var result = find_lanczos_eigenvectors((graph as! [[Int]]), 2)
    print(_p(String(describing: list_to_string((result.values as! [Double])))))
    print(_p(String(describing: matrix_to_string((result.vectors as! [[Double]])))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
