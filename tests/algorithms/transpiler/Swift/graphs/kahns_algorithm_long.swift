// Generated by Mochi transpiler v0.10.66 on 2025-08-15 15:21:51 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func longest_distance(_ graph: [[Int]]) -> Int {
        let n = _int(((graph).count))
        var indegree: [Int] = ([] as! [Int])
        var i = 0
        while (i < n) {
            indegree = (_append(indegree, 0) as! [Int])
            i = _int((i &+ 1))
        }
        var long_dist: [Int] = ([] as! [Int])
        var j = 0
        while (j < n) {
            long_dist = (_append(long_dist, 1) as! [Int])
            j = _int((j &+ 1))
        }
        var u = 0
        while (u < n) {
            for v in (_idx(graph, u) as! [Int]) {
                indegree = _set(indegree, v, _int(((_idx(indegree, v) as? Int ?? 0) &+ 1)))
            }
            u = _int((u &+ 1))
        }
        var queue: [Int] = ([] as! [Int])
        var head = 0
        var k = 0
        while (k < n) {
            if ((_idx(indegree, k) as? Int ?? 0) == 0) {
                queue = (_append(queue, k) as! [Int])
            }
            k = _int((k &+ 1))
        }
        while (head < _int(((queue).count))) {
            let vertex = (_idx(queue, head) as? Int ?? 0)
            head = _int((head &+ 1))
            for x in (_idx(graph, vertex) as! [Int]) {
                indegree = _set(indegree, x, _int(((_idx(indegree, x) as? Int ?? 0) &- 1)))
                let new_dist = ((_idx(long_dist, vertex) as? Int ?? 0) &+ 1)
                if (new_dist > (_idx(long_dist, x) as? Int ?? 0)) {
                    long_dist = _set(long_dist, x, Int(new_dist))
                }
                if ((_idx(indegree, x) as? Int ?? 0) == 0) {
                    queue = (_append(queue, x) as! [Int])
                }
            }
        }
        var max_len = (_idx(long_dist, 0) as? Int ?? 0)
        var m = 1
        while (m < n) {
            if ((_idx(long_dist, m) as? Int ?? 0) > max_len) {
                max_len = (_idx(long_dist, m) as? Int ?? 0)
            }
            m = _int((m &+ 1))
        }
        return max_len
    }
    let graph: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 4)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 5)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 6)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, [])
        return _arr
    }() as! [[Int]])
    print(_p(_int(longest_distance((graph as! [[Int]])))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
