// Generated by Mochi transpiler v0.10.65 on 2025-08-14 17:13:31 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
struct Edge: Codable {
    var node: Int
    var weight: Int
    init() {
        self.node = 0
        self.weight = 0
    }
    init(node: Int, weight: Int) {
        self.node = node
        self.weight = weight
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func make_int_list(_ n: Int, _ value: Int) -> [Int] {
        var lst: [Int] = ([] as! [Int])
        var i = 0
        while (i < n) {
            lst = (_append(lst, value) as! [Int])
            i = _int((i &+ 1))
        }
        return lst
    }
    func make_bool_list(_ n: Int) -> [Bool] {
        var lst: [Bool] = ([] as! [Bool])
        var i = 0
        while (i < n) {
            lst = (_append(lst, false) as! [Bool])
            i = _int((i &+ 1))
        }
        return lst
    }
    func dijkstra(_ graph: [[Edge]], _ src: Int) -> [Int] {
        let n = _int(((graph).count))
        var dist = (make_int_list(n, 1000000000) as! [Int])
        var visited = (make_bool_list(n) as! [Bool])
        dist = _set(dist, src, 0)
        var count = 0
        while (count < n) {
            var u: Int = -1
            var min_dist = 1000000000
            var i = 0
            while (i < n) {
                if (!(_idx(visited, i) ?? false) && ((_idx(dist, i) as? Int ?? 0) < min_dist)) {
                    min_dist = (_idx(dist, i) as? Int ?? 0)
                    u = Int(i)
                }
                i = _int((i &+ 1))
            }
            if (u < 0) {
                break
            }
            visited = _set(visited, u, true)
            var j = 0
            while (j < _int((((_idx(graph, u) as! [Edge])).count))) {
                let e = (_idx(_idx(graph, u)!, j) as! Edge)
                let v = e.node
                let w = e.weight
                if !(_idx(visited, v) ?? false) {
                    let new_dist = ((_idx(dist, u) as? Int ?? 0) &+ w)
                    if (new_dist < (_idx(dist, v) as? Int ?? 0)) {
                        dist = _set(dist, v, Int(new_dist))
                    }
                }
                j = _int((j &+ 1))
            }
            count = _int((count &+ 1))
        }
        return dist
    }
    let graph: [[Edge]] = ({
        var _arr: [[Edge]] = []
        _arr = _append(_arr, ({
            var _arr: [Edge] = []
            _arr = _append(_arr, Edge(node: 1, weight: 10))
            _arr = _append(_arr, Edge(node: 3, weight: 5))
            return _arr
        }() as! [Edge]))
        _arr = _append(_arr, ({
            var _arr: [Edge] = []
            _arr = _append(_arr, Edge(node: 2, weight: 1))
            _arr = _append(_arr, Edge(node: 3, weight: 2))
            return _arr
        }() as! [Edge]))
        _arr = _append(_arr, ({
            var _arr: [Edge] = []
            _arr = _append(_arr, Edge(node: 4, weight: 4))
            return _arr
        }() as! [Edge]))
        _arr = _append(_arr, ({
            var _arr: [Edge] = []
            _arr = _append(_arr, Edge(node: 1, weight: 3))
            _arr = _append(_arr, Edge(node: 2, weight: 9))
            _arr = _append(_arr, Edge(node: 4, weight: 2))
            return _arr
        }() as! [Edge]))
        _arr = _append(_arr, ({
            var _arr: [Edge] = []
            _arr = _append(_arr, Edge(node: 0, weight: 7))
            _arr = _append(_arr, Edge(node: 2, weight: 6))
            return _arr
        }() as! [Edge]))
        return _arr
    }() as! [[Edge]])
    var dist: [Int] = (dijkstra((graph as! [[Edge]]), 0) as! [Int])
    print(_p(_p((_idx(dist, 0) as? Int ?? 0))))
    print(_p(_p((_idx(dist, 1) as? Int ?? 0))))
    print(_p(_p((_idx(dist, 2) as? Int ?? 0))))
    print(_p(_p((_idx(dist, 3) as? Int ?? 0))))
    print(_p(_p((_idx(dist, 4) as? Int ?? 0))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
