// Generated by Mochi transpiler v0.10.73 on 2025-08-25 22:33:04 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func is_safe(_ grid: [[Int]], _ visited: [[Bool]], _ row: Int, _ col: Int) -> Bool {
        let rows = _int(((grid).count))
        let cols = _int((((_idx(grid, 0) as! [Int])).count))
        let within_bounds = ((((row >= 0) && (row < rows)) && (col >= 0)) && (col < cols))
        if ((!within_bounds)) {
            return false
        }
        let visited_cell = (_idx(_idx(visited, row), col) as! Bool)
        let not_visited = (visited_cell == false)
        return (not_visited && ((_idx(_idx(grid, row), col) as? Int ?? 0) == 1))
    }
    func dfs(_ grid: [[Int]], _ visited: inout [[Bool]], _ row: Int, _ col: Int) {
        let row_nbr: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, -1)
            _arr = _append(_arr, -1)
            _arr = _append(_arr, -1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int])
        let col_nbr: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, -1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, -1)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, -1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int])
        var _tmp2 = (_idx(visited, row) as! [Bool])
        _tmp2 = _set(_tmp2, col, { let _tmp2 = true; return _tmp2 }())
        visited = _set(visited, row, { let _tmp3 = (_tmp2 as! [Bool]); return _tmp3 }())
        var k = 0
        while ((k < 8)) {
            let new_row = (row &+ (_idx(row_nbr, k) as? Int ?? 0))
            let new_col = (col &+ (_idx(col_nbr, k) as? Int ?? 0))
            if (is_safe((grid as! [[Int]]), (visited as! [[Bool]]), new_row, new_col)) {
                _ = dfs((grid as! [[Int]]), &visited, new_row, new_col)
            }
            k = _int((k &+ 1))
        }
    }
    func count_islands(_ grid: [[Int]]) -> Int {
        let rows = _int(((grid).count))
        let cols = _int((((_idx(grid, 0) as! [Int])).count))
        var visited: [[Bool]] = ([] as! [[Bool]])
        var i = 0
        while ((i < rows)) {
            var row_list: [Bool] = ([] as! [Bool])
            var j = 0
            while ((j < cols)) {
                row_list = (_append(row_list, false) as! [Bool])
                j = _int((j &+ 1))
            }
            visited = (_append(visited, row_list) as! [[Bool]])
            i = _int((i &+ 1))
        }
        var count = 0
        i = 0
        while ((i < rows)) {
            var j = 0
            while ((j < cols)) {
                if ((!(_idx(_idx(visited, i), j) ?? false) && ((_idx(_idx(grid, i), j) as? Int ?? 0) == 1))) {
                    _ = dfs((grid as! [[Int]]), &visited, i, j)
                    count = _int((count &+ 1))
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        return count
    }
    let grid: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    print(_p(_int(count_islands((grid as! [[Int]])))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
