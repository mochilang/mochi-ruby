// Generated by Mochi transpiler v0.10.73 on 2025-08-25 22:33:41 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func is_valid_matrix(_ matrix: [[Int]]) -> Bool {
        if ((_int(((matrix).count)) == 0)) {
            return false
        }
        let cols = _int((((_idx(matrix, 0) as! [Int])).count))
        for row in matrix {
            if ((_int(((row).count)) != cols)) {
                return false
            }
        }
        return true
    }
    func spiral_traversal(_ matrix: [[Int]]) -> [Int] {
        if ((!is_valid_matrix((matrix as! [[Int]])))) {
            return ([] as! [Int])
        }
        let rows = _int(((matrix).count))
        let cols = _int((((_idx(matrix, 0) as! [Int])).count))
        var top = 0
        var bottom = (rows &- 1)
        var left = 0
        var right = (cols &- 1)
        var result: [Int] = ([] as! [Int])
        while (((left <= right) && (top <= bottom))) {
            var i = left
            while ((i <= right)) {
                result = (_append(result, (_idx(_idx(matrix, top), i) as? Int ?? 0)) as! [Int])
                i = _int((i &+ 1))
            }
            top = _int((top &+ 1))
            i = Int(top)
            while ((i <= bottom)) {
                result = (_append(result, (_idx(_idx(matrix, i), right) as? Int ?? 0)) as! [Int])
                i = _int((i &+ 1))
            }
            right = _int((right &- 1))
            if ((top <= bottom)) {
                i = Int(right)
                while ((i >= left)) {
                    result = (_append(result, (_idx(_idx(matrix, bottom), i) as? Int ?? 0)) as! [Int])
                    i = _int((i &- 1))
                }
                bottom = _int((bottom &- 1))
            }
            if ((left <= right)) {
                i = Int(bottom)
                while ((i >= top)) {
                    result = (_append(result, (_idx(_idx(matrix, i), left) as? Int ?? 0)) as! [Int])
                    i = _int((i &- 1))
                }
                left = _int((left &+ 1))
            }
        }
        return result
    }
    func spiral_print_clockwise(_ matrix: [[Int]]) {
        for value in (spiral_traversal((matrix as! [[Int]])) as! [Int]) {
            print(_p(_p(value)))
        }
    }
    func main() {
        let a: [[Int]] = ({
            var _arr: [[Int]] = []
            _arr = _append(_arr, ({
                var _arr: [Int] = []
                _arr = _append(_arr, 1)
                _arr = _append(_arr, 2)
                _arr = _append(_arr, 3)
                _arr = _append(_arr, 4)
                return _arr
            }() as! [Int]))
            _arr = _append(_arr, ({
                var _arr: [Int] = []
                _arr = _append(_arr, 5)
                _arr = _append(_arr, 6)
                _arr = _append(_arr, 7)
                _arr = _append(_arr, 8)
                return _arr
            }() as! [Int]))
            _arr = _append(_arr, ({
                var _arr: [Int] = []
                _arr = _append(_arr, 9)
                _arr = _append(_arr, 10)
                _arr = _append(_arr, 11)
                _arr = _append(_arr, 12)
                return _arr
            }() as! [Int]))
            return _arr
        }() as! [[Int]])
        _ = spiral_print_clockwise((a as! [[Int]]))
        print(_p("[" + (spiral_traversal((a as! [[Int]])) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
