// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:26:01 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func binary_search(_ arr: [Int], _ item: Int) -> Bool {
        var low = 0
        var high = (_int(((arr).count)) &- 1)
        while (low <= high) {
            let mid = (_int((low &+ high)) / 2)
            let val = (_idx(arr, mid) as? Int ?? 0)
            if (val == item) {
                return true
            }
            if (item < val) {
                high = _int((mid &- 1))
            } else {
                low = _int((mid &+ 1))
            }
        }
        return false
    }
    var arr1: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 8)
        _arr = _append(_arr, 13)
        _arr = _append(_arr, 17)
        _arr = _append(_arr, 19)
        _arr = _append(_arr, 32)
        _arr = _append(_arr, 42)
        return _arr
    }() as! [Int])
    print(_p(binary_search((arr1 as! [Int]), 3)))
    print(_p(binary_search((arr1 as! [Int]), 13)))
    var arr2: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 6)
        _arr = _append(_arr, 7)
        return _arr
    }() as! [Int])
    print(_p(binary_search((arr2 as! [Int]), 4)))
    print(_p(binary_search((arr2 as! [Int]), -10)))
    var arr3: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, -18)
        _arr = _append(_arr, 2)
        return _arr
    }() as! [Int])
    print(_p(binary_search((arr3 as! [Int]), -18)))
    var arr4: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 5)
        return _arr
    }() as! [Int])
    print(_p(binary_search((arr4 as! [Int]), 5)))
    var arr5: [Int] = ([] as! [Int])
    print(_p(binary_search((arr5 as! [Int]), 1)))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
