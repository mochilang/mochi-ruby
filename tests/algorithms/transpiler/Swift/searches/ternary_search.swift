// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:26:07 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let precision: Int = 10
    func lin_search(_ left: Int, _ right: Int, _ array: [Int], _ target: Int) -> Int {
        var i: Int = left
        while (i < right) {
            if ((_idx(array, i) as? Int ?? 0) == target) {
                return i
            }
            i = _int((i &+ 1))
        }
        return -1
    }
    func ite_ternary_search(_ array: [Int], _ target: Int) -> Int {
        var left: Int = 0
        var right: Int = (_int(((array).count)) &- 1)
        while (left <= right) {
            if ((right &- left) < precision) {
                let idx: Int = _int(lin_search(left, (right &+ 1), (array as! [Int]), target))
                return idx
            }
            let one_third: Int = (left &+ (_int((right &- left)) / 3))
            let two_third: Int = (right &- (_int((right &- left)) / 3))
            if ((_idx(array, one_third) as? Int ?? 0) == target) {
                return one_third
            }
            if ((_idx(array, two_third) as? Int ?? 0) == target) {
                return two_third
            }
            if (target < (_idx(array, one_third) as? Int ?? 0)) {
                right = _int((one_third &- 1))
            } else if ((_idx(array, two_third) as? Int ?? 0) < target) {
                left = _int((two_third &+ 1))
            } else {
                left = _int((one_third &+ 1))
                right = _int((two_third &- 1))
            }
            
        }
        return -1
    }
    func rec_ternary_search(_ left: Int, _ right: Int, _ array: [Int], _ target: Int) -> Int {
        if (left <= right) {
            if ((right &- left) < precision) {
                let idx: Int = _int(lin_search(left, (right &+ 1), (array as! [Int]), target))
                return idx
            }
            let one_third: Int = (left &+ (_int((right &- left)) / 3))
            let two_third: Int = (right &- (_int((right &- left)) / 3))
            if ((_idx(array, one_third) as? Int ?? 0) == target) {
                return one_third
            }
            if ((_idx(array, two_third) as? Int ?? 0) == target) {
                return two_third
            }
            if (target < (_idx(array, one_third) as? Int ?? 0)) {
                return _int(rec_ternary_search(left, (one_third &- 1), (array as! [Int]), target))
            }
            if ((_idx(array, two_third) as? Int ?? 0) < target) {
                return _int(rec_ternary_search((two_third &+ 1), right, (array as! [Int]), target))
            }
            return _int(rec_ternary_search((one_third &+ 1), (two_third &- 1), (array as! [Int]), target))
        }
        return -1
    }
    func main() {
        let test_list: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 8)
            _arr = _append(_arr, 13)
            _arr = _append(_arr, 17)
            _arr = _append(_arr, 19)
            _arr = _append(_arr, 32)
            _arr = _append(_arr, 42)
            return _arr
        }() as! [Int])
        print(_p(_p(_int(ite_ternary_search((test_list as! [Int]), 3)))))
        print(_p(_p(_int(ite_ternary_search((test_list as! [Int]), 13)))))
        print(_p(_p(_int(rec_ternary_search(0, (_int(((test_list).count)) &- 1), (test_list as! [Int]), 3)))))
        print(_p(_p(_int(rec_ternary_search(0, (_int(((test_list).count)) &- 1), (test_list as! [Int]), 13)))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
