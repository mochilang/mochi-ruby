// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:25:54 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func linear_search(_ sequence: [Int], _ target: Int) -> Int {
        var i = 0
        while (i < _int(((sequence).count))) {
            if ((_idx(sequence, i) as? Int ?? 0) == target) {
                return i
            }
            i = _int((i &+ 1))
        }
        return -1
    }
    func rec_linear_search(_ sequence: [Int], _ low: Int, _ high: Int, _ target: Int) -> Int {
        if (!((((0 <= high) && (high < _int(((sequence).count)))) && (0 <= low)) && (low < _int(((sequence).count))))) {
            _ = fatalError("Invalid upper or lower bound!")
        }
        if (high < low) {
            return -1
        }
        if ((_idx(sequence, low) as? Int ?? 0) == target) {
            return low
        }
        if ((_idx(sequence, high) as? Int ?? 0) == target) {
            return high
        }
        return _int(rec_linear_search((sequence as! [Int]), (low &+ 1), (high &- 1), target))
    }
    print(_p(_p(_int(linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 7)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 15)
        return _arr
    }() as! [Int]), 0)))))
    print(_p(_p(_int(linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 7)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 15)
        return _arr
    }() as! [Int]), 15)))))
    print(_p(_p(_int(linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 7)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 15)
        return _arr
    }() as! [Int]), 5)))))
    print(_p(_p(_int(linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 7)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 15)
        return _arr
    }() as! [Int]), 6)))))
    print(_p(_p(_int(rec_linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 30)
        _arr = _append(_arr, 500)
        _arr = _append(_arr, 100)
        _arr = _append(_arr, 700)
        return _arr
    }() as! [Int]), 0, 4, 0)))))
    print(_p(_p(_int(rec_linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 30)
        _arr = _append(_arr, 500)
        _arr = _append(_arr, 100)
        _arr = _append(_arr, 700)
        return _arr
    }() as! [Int]), 0, 4, 700)))))
    print(_p(_p(_int(rec_linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 30)
        _arr = _append(_arr, 500)
        _arr = _append(_arr, 100)
        _arr = _append(_arr, 700)
        return _arr
    }() as! [Int]), 0, 4, 30)))))
    print(_p(_p(_int(rec_linear_search(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 30)
        _arr = _append(_arr, 500)
        _arr = _append(_arr, 100)
        _arr = _append(_arr, 700)
        return _arr
    }() as! [Int]), 0, 4, -6)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
