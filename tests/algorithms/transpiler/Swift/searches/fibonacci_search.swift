// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:25:45 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func fibonacci(_ k: Int) -> Int {
        if (k < 0) {
            _ = fatalError("k must be >= 0")
        }
        var a = 0
        var b = 1
        var i = 0
        while (i < k) {
            let tmp = (a &+ b)
            a = Int(b)
            b = Int(tmp)
            i = _int((i &+ 1))
        }
        return a
    }
    func min_int(_ a: Int, _ b: Int) -> Int {
        if (a < b) {
            return a
        } else {
            return b
        }
    }
    func fibonacci_search(_ arr: [Int], _ val: Int) -> Int {
        let n = _int(((arr).count))
        var m = 0
        while (_int(fibonacci(m)) < n) {
            m = _int((m &+ 1))
        }
        var offset = 0
        while (m > 0) {
            let i = _int(min_int((offset &+ _int(fibonacci((m &- 1)))), (n &- 1)))
            let item = (_idx(arr, i) as? Int ?? 0)
            if (item == val) {
                return i
            } else if (val < item) {
                m = _int((m &- 1))
            } else {
                offset = _int((offset &+ _int(fibonacci((m &- 1)))))
                m = _int((m &- 2))
            }
            
        }
        return -1
    }
    let example1: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 6)
        _arr = _append(_arr, 7)
        return _arr
    }() as! [Int])
    let example2: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, -18)
        _arr = _append(_arr, 2)
        return _arr
    }() as! [Int])
    let example3: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 15)
        _arr = _append(_arr, 20)
        _arr = _append(_arr, 25)
        _arr = _append(_arr, 30)
        return _arr
    }() as! [Int])
    print(_p(_p(_int(fibonacci_search((example1 as! [Int]), 4)))))
    print(_p(_p(_int(fibonacci_search((example1 as! [Int]), -10)))))
    print(_p(_p(_int(fibonacci_search((example2 as! [Int]), -18)))))
    print(_p(_p(_int(fibonacci_search((example3 as! [Int]), 15)))))
    print(_p(_p(_int(fibonacci_search((example3 as! [Int]), 17)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
