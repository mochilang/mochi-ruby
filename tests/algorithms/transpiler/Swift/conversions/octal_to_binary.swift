// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:09:24 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func octal_to_binary(_ octal_number: String) -> String {
        if (Int(((octal_number).count)) == 0) {
            _ = fatalError("Empty string was passed to the function")
        }
        let octal_digits = "01234567"
        var binary_number: String = ""
        var i: Int = 0
        while (i < Int(((octal_number).count))) {
            let digit = (String(Array(octal_number)[i]) as! String)
            var valid: Bool = false
            var j: Int = 0
            while (j < Int(((octal_digits).count))) {
                if (digit == (String(Array(octal_digits)[j]) as! String)) {
                    valid = true
                    break
                }
                j = Int((j &+ 1))
            }
            if (!valid) {
                _ = fatalError("Non-octal value was passed to the function")
            }
            var value: Int = Int(digit)!
            var k: Int = 0
            var binary_digit: String = ""
            while (k < 3) {
                binary_digit = ((String(describing: ((value % 2 + 2) % 2)) + binary_digit) as! String)
                value = Int((value / 2))
                k = Int((k &+ 1))
            }
            binary_number = ((binary_number + binary_digit) as! String)
            i = Int((i &+ 1))
        }
        return binary_number
    }
    print(_p(String(describing: octal_to_binary("17"))))
    print(_p(String(describing: octal_to_binary("7"))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
