// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:08:48 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let ENERGY_CONVERSION: [String: Double] = (["joule": 1.0, "kilojoule": 1000.0, "megajoule": 1000000.0, "gigajoule": 1000000000.0, "wattsecond": 1.0, "watthour": 3600.0, "kilowatthour": 3600000.0, "newtonmeter": 1.0, "calorie_nutr": 4186.8, "kilocalorie_nutr": 4186800.0, "electronvolt": 0.0000000000000000001602176634, "britishthermalunit_it": 1055.05585, "footpound": 1.355818] as! [String: Double])
    func energy_conversion(_ from_type: String, _ to_type: String, _ value: Double) -> Double {
        if (((ENERGY_CONVERSION[from_type] != nil) == false) || ((ENERGY_CONVERSION[to_type] != nil) == false)) {
            _ = fatalError("Incorrect 'from_type' or 'to_type'")
        }
        return ((value * Double(ENERGY_CONVERSION[String(String(describing: from_type))])) / Double(ENERGY_CONVERSION[String(String(describing: to_type))]))
    }
    print(_p(String(describing: Double(energy_conversion("joule", "kilojoule", 1.0)))))
    print(_p(String(describing: Double(energy_conversion("kilowatthour", "joule", 10.0)))))
    print(_p(String(describing: Double(energy_conversion("britishthermalunit_it", "footpound", 1.0)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
