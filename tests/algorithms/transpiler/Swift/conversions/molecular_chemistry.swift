// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:09:19 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func round_to_int(_ x: Double) -> Int {
        if (x >= 0.0) {
            return Int(_int((x + 0.5)))
        }
        return Int(_int((x - 0.5)))
    }
    func molarity_to_normality(_ nfactor: Double, _ moles: Double, _ volume: Double) -> Int {
        return Int(round_to_int(Double((Double((moles / volume)) * nfactor))))
    }
    func moles_to_pressure(_ volume: Double, _ moles: Double, _ temperature: Double) -> Int {
        return Int(round_to_int(Double((Double(((moles * 0.0821) * temperature)) / volume))))
    }
    func moles_to_volume(_ pressure: Double, _ moles: Double, _ temperature: Double) -> Int {
        return Int(round_to_int(Double((Double(((moles * 0.0821) * temperature)) / pressure))))
    }
    func pressure_and_volume_to_temperature(_ pressure: Double, _ moles: Double, _ volume: Double) -> Int {
        return Int(round_to_int(Double((Double((pressure * volume)) / Double((0.0821 * moles))))))
    }
    print(_p(String(describing: Int(molarity_to_normality(2.0, 3.1, 0.31)))))
    print(_p(String(describing: Int(molarity_to_normality(4.0, 11.4, 5.7)))))
    print(_p(String(describing: Int(moles_to_pressure(0.82, 3.0, 300.0)))))
    print(_p(String(describing: Int(moles_to_pressure(8.2, 5.0, 200.0)))))
    print(_p(String(describing: Int(moles_to_volume(0.82, 3.0, 300.0)))))
    print(_p(String(describing: Int(moles_to_volume(8.2, 5.0, 200.0)))))
    print(_p(String(describing: Int(pressure_and_volume_to_temperature(0.82, 1.0, 2.0)))))
    print(_p(String(describing: Int(pressure_and_volume_to_temperature(8.2, 5.0, 3.0)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
