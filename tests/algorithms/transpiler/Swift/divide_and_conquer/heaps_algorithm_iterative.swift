// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:30:27 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func copy_list(_ arr: [Int?]) -> [Int?] {
        var result: [Int?] = ([] as! [Int?])
        var i: Int = 0
        while (i < Int(((arr).count))) {
            result = (_append(result, (arr[i] as? Int ?? 0)) as! [Int?])
            i = Int((i &+ 1))
        }
        return result
    }
    func heaps(_ arr: inout [Int?]) -> [[Int?]] {
        if (Int(((arr).count)) <= 1) {
            let single: [[Int?]] = ([] as! [[Int?]])
            return (_append(single, (copy_list((arr as! [Int?])) as! [Int?])) as! [[Int?]])
        }
        let n = Int(((arr).count))
        var c: [Int?] = ([] as! [Int?])
        var i: Int = 0
        while (i < n) {
            c = (_append(c, 0) as! [Int?])
            i = Int((i &+ 1))
        }
        var res: [[Int?]] = ([] as! [[Int?]])
        res = (_append(res, (copy_list((arr as! [Int?])) as! [Int?])) as! [[Int?]])
        i = 0
        while (i < n) {
            if ((c[i] as? Int ?? 0) < i) {
                if (((i % 2 + 2) % 2) == 0) {
                    let temp = (arr[0] as? Int ?? 0)
                    arr = _set(arr, 0, (arr[i] as? Int ?? 0))
                    arr = _set(arr, i, (temp as! Int))
                } else {
                    let temp = (arr[(c[i] as? Int ?? 0)] as? Int ?? 0)
                    arr = _set(arr, (c[i] as? Int ?? 0), (arr[i] as? Int ?? 0))
                    arr = _set(arr, i, (temp as! Int))
                }
                res = (_append(res, (copy_list((arr as! [Int?])) as! [Int?])) as! [[Int?]])
                c = _set(c, i, Int(((c[i] as? Int ?? 0) &+ 1)))
                i = 0
            } else {
                c = _set(c, i, 0)
                i = Int((i &+ 1))
            }
        }
        return res
    }
    print(_p("[" + ({ () -> Any in
    var _tmp0 = [1, 2, 3]
    return heaps(&_tmp0)
}() as! [[Int?]]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
let _benchEnd = _now()
let _benchMemEnd = _mem()
print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
