// Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:58:25 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func index_of(_ s: String, _ c: String) -> Int {
        var i: Int = 0
        while (i < Int(((s).count))) {
            if ((String(Array(s)[i]) as! String) == c) {
                return i
            }
            i = Int((i &+ 1))
        }
        return Int(-1)
    }
    func atbash(_ sequence: String) -> String {
        let lower = "abcdefghijklmnopqrstuvwxyz"
        let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
        let lower_rev = "zyxwvutsrqponmlkjihgfedcba"
        let upper_rev = "ZYXWVUTSRQPONMLKJIHGFEDCBA"
        var result: String = ""
        var i: Int = 0
        while (i < Int(((sequence).count))) {
            let ch = (String(Array(sequence)[i]) as! String)
            let idx = Int(index_of((lower as! String), (ch as! String)))
            if (idx != Int(-1)) {
                result = ((result + (String(Array(lower_rev)[idx]) as! String)) as! String)
            } else {
                let idx2 = Int(index_of((upper as! String), (ch as! String)))
                if (idx2 != Int(-1)) {
                    result = ((result + (String(Array(upper_rev)[idx2]) as! String)) as! String)
                } else {
                    result = ((result + ch) as! String)
                }
            }
            i = Int((i &+ 1))
        }
        return result
    }
    print(_p(String(describing: atbash("ABCDEFGH"))))
    print(_p(String(describing: atbash("123GGjj"))))
    print(_p(String(describing: atbash("testStringtest"))))
    print(_p(String(describing: atbash("with space"))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
