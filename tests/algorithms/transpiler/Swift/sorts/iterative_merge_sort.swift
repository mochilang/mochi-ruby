// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:27:02 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func merge(_ a: [Int], _ low: Int, _ mid: Int, _ high: Int) -> [Int] {
        var a = a
        var left: [Int] = (_slice(Array(a), low, mid) as! [Int])
        var right: [Int] = (_slice(Array(a), mid, (high &+ 1)) as! [Int])
        var result: [Int] = ([] as! [Int])
        while ((_int(((left).count)) > 0) && (_int(((right).count)) > 0)) {
            if ((_idx(left, 0) as? Int ?? 0) <= (_idx(right, 0) as? Int ?? 0)) {
                result = (_append(result, (_idx(left, 0) as? Int ?? 0)) as! [Int])
                left = (_slice(Array(left), 1, left.count) as! [Int])
            } else {
                result = (_append(result, (_idx(right, 0) as? Int ?? 0)) as! [Int])
                right = (_slice(Array(right), 1, right.count) as! [Int])
            }
        }
        var i = 0
        while (i < _int(((left).count))) {
            result = (_append(result, (_idx(left, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        i = 0
        while (i < _int(((right).count))) {
            result = (_append(result, (_idx(right, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        i = 0
        while (i < _int(((result).count))) {
            a = _set(a, (low &+ i), (_idx(result, i) as? Int ?? 0))
            i = _int((i &+ 1))
        }
        return a
    }
    func iter_merge_sort(_ items: [Int]) -> [Int] {
        let n = _int(((items).count))
        if (n <= 1) {
            return items
        }
        var arr = (_slice(Array(items), 0, items.count) as! [Int])
        var p = 2
        while (p <= n) {
            var i = 0
            while (i < n) {
                var high = ((i &+ p) &- 1)
                if (high >= n) {
                    high = _int((n &- 1))
                }
                let low = i
                let mid = (_int(((low &+ high) &+ 1)) / 2)
                arr = (merge((arr as! [Int]), low, mid, high) as! [Int])
                i = _int((i &+ p))
            }
            if ((p &* 2) >= n) {
                let mid2 = (i &- p)
                arr = (merge((arr as! [Int]), 0, mid2, (n &- 1)) as! [Int])
                break
            }
            p = _int((p &* 2))
        }
        return arr
    }
    func list_to_string(_ arr: [Int]) -> String {
        var s = "["
        var i = 0
        while (i < _int(((arr).count))) {
            s = ((s + _p((_idx(arr, i) as? Int ?? 0))) as! String)
            if (i < (_int(((arr).count)) &- 1)) {
                s = ((s + ", ") as! String)
            }
            i = _int((i &+ 1))
        }
        return (s + "]")
    }
    print(_p(String(describing: list_to_string((iter_merge_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 9)
        _arr = _append(_arr, 8)
        _arr = _append(_arr, 7)
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 7)
        return _arr
    }() as! [Int])) as! [Int])))))
    print(_p(String(describing: list_to_string((iter_merge_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 1)
        return _arr
    }() as! [Int])) as! [Int])))))
    print(_p(String(describing: list_to_string((iter_merge_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 1)
        return _arr
    }() as! [Int])) as! [Int])))))
    print(_p(String(describing: list_to_string((iter_merge_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 1)
        return _arr
    }() as! [Int])) as! [Int])))))
    print(_p(String(describing: list_to_string((iter_merge_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 1)
        return _arr
    }() as! [Int])) as! [Int])))))
    print(_p(String(describing: list_to_string((iter_merge_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, -2)
        _arr = _append(_arr, -9)
        _arr = _append(_arr, -1)
        _arr = _append(_arr, -4)
        return _arr
    }() as! [Int])) as! [Int])))))
    print(_p(String(describing: list_to_string((iter_merge_sort(([] as! [Int])) as! [Int])))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
