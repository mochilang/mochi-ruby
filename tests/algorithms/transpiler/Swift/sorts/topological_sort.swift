// Generated by Mochi transpiler v0.10.63 on 2025-08-11 17:28:22 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let edges: [String: [String]] = (["a": ({
        var _arr: [String] = []
        _arr = _append(_arr, "c")
        _arr = _append(_arr, "b")
        return _arr
    }() as! [String]), "b": ({
        var _arr: [String] = []
        _arr = _append(_arr, "d")
        _arr = _append(_arr, "e")
        return _arr
    }() as! [String]), "c": [] as [Any], "d": [] as [Any], "e": [] as [Any]] as! [String: [String]])
    let vertices: [String] = ({
        var _arr: [String] = []
        _arr = _append(_arr, "a")
        _arr = _append(_arr, "b")
        _arr = _append(_arr, "c")
        _arr = _append(_arr, "d")
        _arr = _append(_arr, "e")
        return _arr
    }() as! [String])
    func topological_sort(_ start: String, _ visited: [String: Bool], _ sort: [String]) -> [String] {
        var visited = visited
        var sort = sort
        visited = _set(visited, String(describing: start), true)
        let neighbors: [String] = (edges[String(String(describing: start))] as! [String])
        var i = 0
        while (i < _int(((neighbors).count))) {
            let neighbor = (_idx(neighbors, i) as! String)
            if (!(visited[neighbor] != nil)) {
                sort = (topological_sort((neighbor as! String), (visited as! [String: Bool]), (sort as! [String])) as! [String])
            }
            i = _int((i &+ 1))
        }
        sort = (_append(sort, start) as! [String])
        if (_int(((visited).count)) != _int(((vertices).count))) {
            var j = 0
            while (j < _int(((vertices).count))) {
                let v = (_idx(vertices, j) as! String)
                if (!(visited[v] != nil)) {
                    sort = (topological_sort((v as! String), (visited as! [String: Bool]), (sort as! [String])) as! [String])
                }
                j = _int((j &+ 1))
            }
        }
        return sort
    }
    func main() {
        let result: [String] = (topological_sort("a", ([:] as! [String: Bool]), ([] as! [String])) as! [String])
        print(_p("[" + result.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
