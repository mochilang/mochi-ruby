// Generated by Mochi transpiler v0.10.63 on 2025-08-11 17:28:18 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func merge(_ xs: [Int], _ ys: [Int], _ reverse: Bool) -> [Int] {
        var result: [Int] = ([] as! [Int])
        var i = 0
        var j = 0
        while ((i < _int(((xs).count))) && (j < _int(((ys).count)))) {
            if reverse {
                if ((_idx(xs, i) as? Int ?? 0) > (_idx(ys, j) as? Int ?? 0)) {
                    result = (_append(result, (_idx(xs, i) as? Int ?? 0)) as! [Int])
                    i = _int((i &+ 1))
                } else {
                    result = (_append(result, (_idx(ys, j) as? Int ?? 0)) as! [Int])
                    j = _int((j &+ 1))
                }
            } else {
                if ((_idx(xs, i) as? Int ?? 0) < (_idx(ys, j) as? Int ?? 0)) {
                    result = (_append(result, (_idx(xs, i) as? Int ?? 0)) as! [Int])
                    i = _int((i &+ 1))
                } else {
                    result = (_append(result, (_idx(ys, j) as? Int ?? 0)) as! [Int])
                    j = _int((j &+ 1))
                }
            }
        }
        while (i < _int(((xs).count))) {
            result = (_append(result, (_idx(xs, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        while (j < _int(((ys).count))) {
            result = (_append(result, (_idx(ys, j) as? Int ?? 0)) as! [Int])
            j = _int((j &+ 1))
        }
        return result
    }
    func strand_sort_rec(_ arr: [Int], _ reverse: Bool, _ solution: [Int]) -> [Int] {
        var solution = solution
        if (_int(((arr).count)) == 0) {
            return solution
        }
        var sublist: [Int] = ([] as! [Int])
        var remaining: [Int] = ([] as! [Int])
        sublist = (_append(sublist, (_idx(arr, 0) as? Int ?? 0)) as! [Int])
        var last = (_idx(arr, 0) as? Int ?? 0)
        var k = 1
        while (k < _int(((arr).count))) {
            let item = (_idx(arr, k) as? Int ?? 0)
            if reverse {
                if (item < last) {
                    sublist = (_append(sublist, item) as! [Int])
                    last = Int(item)
                } else {
                    remaining = (_append(remaining, item) as! [Int])
                }
            } else {
                if (item > last) {
                    sublist = (_append(sublist, item) as! [Int])
                    last = Int(item)
                } else {
                    remaining = (_append(remaining, item) as! [Int])
                }
            }
            k = _int((k &+ 1))
        }
        solution = (merge((solution as! [Int]), (sublist as! [Int]), (reverse as! Bool)) as! [Int])
        return (strand_sort_rec((remaining as! [Int]), (reverse as! Bool), (solution as! [Int])) as! [Int])
    }
    func strand_sort(_ arr: [Int], _ reverse: Bool) -> [Int] {
        return (strand_sort_rec((arr as! [Int]), (reverse as! Bool), ([] as! [Int])) as! [Int])
    }
    print(_p("[" + (strand_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        return _arr
    }() as! [Int]), false) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    print(_p("[" + (strand_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        return _arr
    }() as! [Int]), true) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
