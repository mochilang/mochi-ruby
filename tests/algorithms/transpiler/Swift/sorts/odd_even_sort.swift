// Generated by Mochi transpiler v0.10.63 on 2025-08-11 17:27:12 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func odd_even_sort(_ xs: [Int]) -> [Int] {
        var arr: [Int] = ([] as! [Int])
        var i = 0
        while (i < _int(((xs).count))) {
            arr = (_append(arr, (_idx(xs, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        var n = _int(((arr).count))
        var sorted = false
        while (sorted == false) {
            sorted = true
            var j = 0
            while (j < (n &- 1)) {
                if ((_idx(arr, j) as? Int ?? 0) > (_idx(arr, (j &+ 1)) as? Int ?? 0)) {
                    let tmp = (_idx(arr, j) as? Int ?? 0)
                    arr = _set(arr, j, (_idx(arr, (j &+ 1)) as? Int ?? 0))
                    arr = _set(arr, (j &+ 1), Int(tmp))
                    sorted = false
                }
                j = _int((j &+ 2))
            }
            j = 1
            while (j < (n &- 1)) {
                if ((_idx(arr, j) as? Int ?? 0) > (_idx(arr, (j &+ 1)) as? Int ?? 0)) {
                    let tmp = (_idx(arr, j) as? Int ?? 0)
                    arr = _set(arr, j, (_idx(arr, (j &+ 1)) as? Int ?? 0))
                    arr = _set(arr, (j &+ 1), Int(tmp))
                    sorted = false
                }
                j = _int((j &+ 2))
            }
        }
        return arr
    }
    func print_list(_ xs: [Int]) {
        var i = 0
        var out = ""
        while (i < _int(((xs).count))) {
            if (i > 0) {
                out = ((out + " ") as! String)
            }
            out = ((out + _p((_idx(xs, i) as? Int ?? 0))) as! String)
            i = _int((i &+ 1))
        }
        print(_p(out))
    }
    func test_odd_even_sort() {
        let a: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int])
        let r1: [Int] = (odd_even_sort((a as! [Int])) as! [Int])
        if ((((((_idx(r1, 0) as? Int ?? 0) != 1) || ((_idx(r1, 1) as? Int ?? 0) != 2)) || ((_idx(r1, 2) as? Int ?? 0) != 3)) || ((_idx(r1, 3) as? Int ?? 0) != 4)) || ((_idx(r1, 4) as? Int ?? 0) != 5)) {
            _ = fatalError("case1 failed")
        }
        let b: [Int] = ([] as! [Int])
        let r2: [Int] = (odd_even_sort((b as! [Int])) as! [Int])
        if (_int(((r2).count)) != 0) {
            _ = fatalError("case2 failed")
        }
        let c: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, -10)
            _arr = _append(_arr, -1)
            _arr = _append(_arr, 10)
            _arr = _append(_arr, 2)
            return _arr
        }() as! [Int])
        let r3: [Int] = (odd_even_sort((c as! [Int])) as! [Int])
        if (((((_idx(r3, 0) as? Int ?? 0) != _int(-10)) || ((_idx(r3, 1) as? Int ?? 0) != _int(-1))) || ((_idx(r3, 2) as? Int ?? 0) != 2)) || ((_idx(r3, 3) as? Int ?? 0) != 10)) {
            _ = fatalError("case3 failed")
        }
        let d: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 4)
            return _arr
        }() as! [Int])
        let r4: [Int] = (odd_even_sort((d as! [Int])) as! [Int])
        if (((((_idx(r4, 0) as? Int ?? 0) != 1) || ((_idx(r4, 1) as? Int ?? 0) != 2)) || ((_idx(r4, 2) as? Int ?? 0) != 3)) || ((_idx(r4, 3) as? Int ?? 0) != 4)) {
            _ = fatalError("case4 failed")
        }
    }
    func main() {
        _ = test_odd_even_sort()
        let sample: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int])
        let sorted: [Int] = (odd_even_sort((sample as! [Int])) as! [Int])
        _ = print_list((sorted as! [Int]))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
