// Generated by Mochi transpiler v0.10.63 on 2025-08-11 17:27:29 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let RADIX = 10
    func make_buckets() -> [[Int]] {
        var buckets: [[Int]] = ([] as! [[Int]])
        var i = 0
        while (i < RADIX) {
            buckets = (((_append(buckets, ([] as! [Int])) as! [[Int]]) as! [Any?]) as! [[Int]])
            i = _int((i &+ 1))
        }
        return buckets
    }
    func max_value(_ xs: [Int]) -> Int {
        var max_val = (_idx(xs, 0) as? Int ?? 0)
        var i = 1
        while (i < _int(((xs).count))) {
            if ((_idx(xs, i) as? Int ?? 0) > max_val) {
                max_val = (_idx(xs, i) as? Int ?? 0)
            }
            i = _int((i &+ 1))
        }
        return max_val
    }
    func radix_sort(_ list_of_ints: [Int]) -> [Int] {
        var list_of_ints = list_of_ints
        var placement = 1
        let max_digit = _int(max_value((list_of_ints as! [Int])))
        while (placement <= max_digit) {
            var buckets = (make_buckets() as! [[Int]])
            var i = 0
            while (i < _int(((list_of_ints).count))) {
                let value = (_idx(list_of_ints, i) as? Int ?? 0)
                let tmp = ((_int((value / placement)) % RADIX + RADIX) % RADIX)
                buckets = _set(buckets, tmp, (_append((_idx(buckets, tmp) as! [Int]), value) as! [Int]))
                i = _int((i &+ 1))
            }
            var a = 0
            var b = 0
            while (b < RADIX) {
                var bucket = (_idx(buckets, b) as! [Int])
                var j = 0
                while (j < _int(((bucket).count))) {
                    list_of_ints = _set(list_of_ints, a, (_idx(bucket, j) as? Int ?? 0))
                    a = _int((a &+ 1))
                    j = _int((j &+ 1))
                }
                b = _int((b &+ 1))
            }
            placement = _int((placement &* RADIX))
        }
        return list_of_ints
    }
    print(_p("[" + (radix_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 2)
        return _arr
    }() as! [Int])) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    print(_p("[" + (radix_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 100)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 1000)
        return _arr
    }() as! [Int])) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    print(_p("[" + (radix_sort(({
        var _arr: [Int] = []
        _arr = _append(_arr, 15)
        _arr = _append(_arr, 14)
        _arr = _append(_arr, 13)
        _arr = _append(_arr, 12)
        _arr = _append(_arr, 11)
        _arr = _append(_arr, 10)
        _arr = _append(_arr, 9)
        _arr = _append(_arr, 8)
        _arr = _append(_arr, 7)
        _arr = _append(_arr, 6)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 0)
        return _arr
    }() as! [Int])) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
