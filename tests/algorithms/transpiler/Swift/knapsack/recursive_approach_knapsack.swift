// Generated by Mochi transpiler v0.10.65 on 2025-08-14 16:21:50 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func knapsack(_ weights: [Int], _ values: [Int], _ number_of_items: Int, _ max_weight: Int, _ index: Int) -> Int {
        if (index == number_of_items) {
            return 0
        }
        let ans1 = _int(knapsack((weights as! [Int]), (values as! [Int]), number_of_items, max_weight, (index &+ 1)))
        var ans2: Int = 0
        if ((_idx(weights, index) as? Int ?? 0) <= max_weight) {
            ans2 = _int(((_idx(values, index) as? Int ?? 0) &+ _int(knapsack((weights as! [Int]), (values as! [Int]), number_of_items, (max_weight &- (_idx(weights, index) as? Int ?? 0)), (index &+ 1)))))
        }
        if (ans1 > ans2) {
            return ans1
        }
        return ans2
    }
    func main() {
        let w1: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 5)
            return _arr
        }() as! [Int])
        let v1: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 8)
            _arr = _append(_arr, 6)
            return _arr
        }() as! [Int])
        let r1 = _int(knapsack((w1 as! [Int]), (v1 as! [Int]), 4, 5, 0))
        print(_p(_p(r1)))
        let w2: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 5)
            return _arr
        }() as! [Int])
        let v2: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 10)
            _arr = _append(_arr, 9)
            _arr = _append(_arr, 8)
            return _arr
        }() as! [Int])
        let r2 = _int(knapsack((w2 as! [Int]), (v2 as! [Int]), 3, 25, 0))
        print(_p(_p(r2)))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
