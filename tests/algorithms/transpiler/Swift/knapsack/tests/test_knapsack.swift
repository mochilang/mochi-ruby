// Generated by Mochi transpiler v0.10.65 on 2025-08-14 16:21:58 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func knapsack(_ capacity: Int, _ weights: [Int], _ values: [Int], _ counter: Int) -> Int {
        if ((counter == 0) || (capacity == 0)) {
            return 0
        }
        if ((_idx(weights, (counter &- 1)) as? Int ?? 0) > capacity) {
            return _int(knapsack(capacity, (weights as! [Int]), (values as! [Int]), (counter &- 1)))
        }
        let left_capacity = (capacity &- (_idx(weights, (counter &- 1)) as? Int ?? 0))
        let include_val = ((_idx(values, (counter &- 1)) as? Int ?? 0) &+ _int(knapsack(left_capacity, (weights as! [Int]), (values as! [Int]), (counter &- 1))))
        let exclude_val = _int(knapsack(capacity, (weights as! [Int]), (values as! [Int]), (counter &- 1)))
        if (include_val > exclude_val) {
            return include_val
        }
        return exclude_val
    }
    func test_base_case() -> Bool {
        let cap: Int = 0
        let val: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int])
        let w: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int])
        let c: Int = _int(((val).count))
        if (_int(knapsack(cap, (w as! [Int]), (val as! [Int]), c)) != 0) {
            return false
        }
        let val2: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 60)
            return _arr
        }() as! [Int])
        let w2: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 10)
            return _arr
        }() as! [Int])
        let c2: Int = _int(((val2).count))
        return (_int(knapsack(cap, (w2 as! [Int]), (val2 as! [Int]), c2)) == 0)
    }
    func test_easy_case() -> Bool {
        let cap: Int = 3
        let val: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 3)
            return _arr
        }() as! [Int])
        let w: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int])
        let c: Int = _int(((val).count))
        return (_int(knapsack(cap, (w as! [Int]), (val as! [Int]), c)) == 5)
    }
    func test_knapsack() -> Bool {
        let cap: Int = 50
        let val: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 60)
            _arr = _append(_arr, 100)
            _arr = _append(_arr, 120)
            return _arr
        }() as! [Int])
        let w: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 10)
            _arr = _append(_arr, 20)
            _arr = _append(_arr, 30)
            return _arr
        }() as! [Int])
        let c: Int = _int(((val).count))
        return (_int(knapsack(cap, (w as! [Int]), (val as! [Int]), c)) == 220)
    }
    print(_p(test_base_case()))
    print(_p(test_easy_case()))
    print(_p(test_knapsack()))
    print(_p(true))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
