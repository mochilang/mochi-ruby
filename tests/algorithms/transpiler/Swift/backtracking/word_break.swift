// Generated by Mochi transpiler v0.10.59 on 2025-08-06 18:40:20 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func contains(_ words: [String], _ target: String) -> Bool {
        for w in words {
            if (w == target) {
                return true
            }
        }
        return false
    }
    func backtrack(_ s: String, _ word_dict: [String], _ start: Int) -> Bool {
        if (start == Int(((s).count))) {
            return true
        }
        var end: Int = (start &+ 1)
        while (end <= Int(((s).count))) {
            let substr = String(describing: String(Array(String(describing: (s as! String)))[start..<end]))
            if (((word_dict as! [String]).contains((substr as! String))) && backtrack((s as! String), (word_dict as! [String]), end)) {
                return true
            }
            end = Int((end &+ 1))
        }
        return false
    }
    func word_break(_ s: String, _ word_dict: [String]) -> Bool {
        return backtrack((s as! String), (word_dict as! [String]), 0)
    }
    print(_p(String(describing: word_break("leetcode", (["leet", "code"] as! [String])))))
    print(_p(String(describing: word_break("applepenapple", (["apple", "pen"] as! [String])))))
    print(_p(String(describing: word_break("catsandog", (["cats", "dog", "sand", "and", "cat"] as! [String])))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
