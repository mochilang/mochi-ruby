// Generated by Mochi transpiler v0.10.59 on 2025-08-06 16:29:25 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func repeat_bool(_ times: Int) -> [Bool] {
        var res: [Bool] = ([] as! [Bool])
        var i: Int = 0
        while (i < times) {
            res = (_append(res, false) as! [Bool])
            i = Int((i &+ 1))
        }
        return res
    }
    func set_bool(_ xs: [Bool], _ idx: Int, _ value: Bool) -> [Bool] {
        var res: [Bool] = ([] as! [Bool])
        var i: Int = 0
        while (i < Int(((xs).count))) {
            if (i == idx) {
                res = (_append(res, value) as! [Bool])
            } else {
                res = (_append(res, (xs[i] as! Bool)) as! [Bool])
            }
            i = Int((i &+ 1))
        }
        return res
    }
    func create_state_space_tree(_ sequence: [Any?], _ current: [Any?], _ index: Int, _ used: [Bool]) {
        if (index == Int(((sequence).count))) {
            print(_p("[" + current.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
            return
        }
        var i: Int = 0
        while (i < Int(((sequence).count))) {
            if !(used[i] ?? false) {
                let next_current: [Any?] = (_append(current, sequence[i]) as! [Any?])
                let next_used: [Bool] = (set_bool((used as! [Bool]), i, true) as! [Bool])
                _ = create_state_space_tree((sequence as! [Any?]), (next_current as! [Any?]), (index &+ 1), (next_used as! [Bool]))
            }
            i = Int((i &+ 1))
        }
    }
    func generate_all_permutations(_ sequence: [Any?]) {
        let used: [Bool] = (repeat_bool(Int(((sequence).count))) as! [Bool])
        _ = create_state_space_tree((sequence as! [Any?]), ([] as! [Any?]), 0, (used as! [Bool]))
    }
    let sequence: [Any?] = (([3, 1, 2, 4] as! [Int]) as! [Any?])
    _ = generate_all_permutations((sequence as! [Any?]))
    let sequence_2: [Any?] = ((["A", "B", "C"] as! [String]) as! [Any?])
    _ = generate_all_permutations((sequence_2 as! [Any?]))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
