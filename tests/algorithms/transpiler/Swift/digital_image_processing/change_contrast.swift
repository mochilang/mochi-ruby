// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:29:39 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func change_contrast(_ img: [[Int?]], _ level: Int) -> [[Int?]] {
        let factor = (Double((259.0 * Double((Double(level) + 255.0)))) / Double((255.0 * Double((259.0 - Double(level))))))
        var result: [[Int?]] = ([] as! [[Int?]])
        var i: Int = 0
        while (i < Int(((img).count))) {
            var row: [Int?] = (img[i] as! [Int?])
            var new_row: [Int?] = ([] as! [Int?])
            var j: Int = 0
            while (j < Int(((row).count))) {
                let c = (row[j] as? Int ?? 0)
                let contrasted = Int((128.0 + (factor * Double((Double(c) - 128.0)))))
                let clamped = Int(((contrasted < 0) ? 0 : ((contrasted > 255) ? 255 : contrasted)))
                new_row = (_append(new_row, clamped) as! [Int?])
                j = Int((j &+ 1))
            }
            result = (_append(result, new_row) as! [[Int?]])
            i = Int((i &+ 1))
        }
        return result
    }
    func print_image(_ img: [[Int?]]) {
        var i: Int = 0
        while (i < Int(((img).count))) {
            var row: [Int?] = (img[i] as! [Int?])
            var j: Int = 0
            var line: String = ""
            while (j < Int(((row).count))) {
                line = (((line + String(describing: (row[j] as? Int ?? 0))) + " ") as! String)
                j = Int((j &+ 1))
            }
            print(_p(line))
            i = Int((i &+ 1))
        }
    }
    let image: [[Int?]] = ([([100, 125, 150] as! [Int?]), ([175, 200, 225] as! [Int?]), ([50, 75, 100] as! [Int?])] as! [[Int?]])
    print(_p("Original image:"))
    _ = print_image((image as! [[Int?]]))
    let contrasted: [[Int?]] = (change_contrast((image as! [[Int?]]), 170) as! [[Int?]])
    print(_p("After contrast:"))
    _ = print_image((contrasted as! [[Int?]]))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
