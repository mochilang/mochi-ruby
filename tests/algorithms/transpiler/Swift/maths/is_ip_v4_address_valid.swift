// Generated by Mochi transpiler v0.10.66 on 2025-08-16 12:50:03 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func split_by_dot(_ s: String) -> [String] {
        var res: [String] = ([] as! [String])
        var current = ""
        var i = 0
        while (i < _int(((s).count))) {
            let c = (String(Array(s)[i]) as! String)
            if (c == ".") {
                res = (_append(res, current) as! [String])
                current = ""
            } else {
                current = ((_p(current) + _p(c)) as! String)
            }
            i = _int((i &+ 1))
        }
        res = (_append(res, current) as! [String])
        return res
    }
    func is_digit_str(_ s: String) -> Bool {
        if (_int(((s).count)) == 0) {
            return false
        }
        var i = 0
        while (i < _int(((s).count))) {
            let c = (String(Array(s)[i]) as! String)
            if ((c < "0") || (c > "9")) {
                return false
            }
            i = _int((i &+ 1))
        }
        return true
    }
    func parse_decimal(_ s: String) -> Int {
        var value = 0
        var i = 0
        while (i < _int(((s).count))) {
            let c = (String(Array(s)[i]) as! String)
            value = _int(((value &* 10) &+ _int(_int(c))))
            i = _int((i &+ 1))
        }
        return value
    }
    func is_ip_v4_address_valid(_ ip: String) -> Bool {
        let octets: [String] = (split_by_dot((ip as! String)) as! [String])
        if (_int(((octets).count)) != 4) {
            return false
        }
        var i = 0
        while (i < 4) {
            let oct = (_idx(octets, i) as! String)
            if (!is_digit_str((oct as! String))) {
                return false
            }
            let number = _int(parse_decimal((oct as! String)))
            if (_int(((_p(number)).count)) != _int(((oct).count))) {
                return false
            }
            if ((number < 0) || (number > 255)) {
                return false
            }
            i = _int((i &+ 1))
        }
        return true
    }
    print(_p(_p(is_ip_v4_address_valid("192.168.0.23"))))
    print(_p(_p(is_ip_v4_address_valid("192.256.15.8"))))
    print(_p(_p(is_ip_v4_address_valid("172.100.0.8"))))
    print(_p(_p(is_ip_v4_address_valid("255.256.0.256"))))
    print(_p(_p(is_ip_v4_address_valid("1.2.33333333.4"))))
    print(_p(_p(is_ip_v4_address_valid("1.2.-3.4"))))
    print(_p(_p(is_ip_v4_address_valid("1.2.3"))))
    print(_p(_p(is_ip_v4_address_valid("1.2.3.4.5"))))
    print(_p(_p(is_ip_v4_address_valid("1.2.A.4"))))
    print(_p(_p(is_ip_v4_address_valid("0.0.0.0"))))
    print(_p(_p(is_ip_v4_address_valid("1.2.3."))))
    print(_p(_p(is_ip_v4_address_valid("1.2.3.05"))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
