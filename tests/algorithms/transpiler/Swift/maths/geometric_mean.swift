// Generated by Mochi transpiler v0.10.66 on 2025-08-16 12:44:39 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func abs(_ x: Double) -> Double {
        if (x < 0.0) {
            return -x
        }
        return x
    }
    func pow_int(_ base: Double, _ exp: Int) -> Double {
        var result = 1.0
        var i = 0
        while (i < exp) {
            result = Double((result * base))
            i = _int((i &+ 1))
        }
        return result
    }
    func nth_root(_ x: Double, _ n: Int) -> Double {
        if (x == 0.0) {
            return 0.0
        }
        var guess = x
        var i = 0
        while (i < 10) {
            var denom = Double(pow_int(Double(guess), (n &- 1)))
            guess = Double((Double(((Double((n &- 1)) * guess) + (x / denom))) / Double(n)))
            i = _int((i &+ 1))
        }
        return guess
    }
    func round_nearest(_ x: Double) -> Double {
        if (x >= 0.0) {
            let n = _int((x + 0.5))
            return Double(n)
        }
        let n = _int((x - 0.5))
        return Double(n)
    }
    func compute_geometric_mean(_ nums: [Double]) -> Double {
        if (_int(((nums).count)) == 0) {
            _ = fatalError("no numbers")
        }
        var product = 1.0
        var i = 0
        while (i < _int(((nums).count))) {
            product = Double((product * Double(_idx(nums, i))))
            i = _int((i &+ 1))
        }
        if ((product < 0.0) && (((_int(((nums).count)) % 2 + 2) % 2) == 0)) {
            _ = fatalError("Cannot Compute Geometric Mean for these numbers.")
        }
        var mean = Double(nth_root(Double(abs(Double(product))), _int(((nums).count))))
        if (product < 0.0) {
            mean = Double(-mean)
        }
        let possible = Double(round_nearest(Double(mean)))
        if (Double(pow_int(Double(possible), _int(((nums).count)))) == product) {
            mean = Double(possible)
        }
        return mean
    }
    func test_compute_geometric_mean() {
        let eps = 0.0001
        let m1 = Double(compute_geometric_mean(({
            var _arr: [Double] = []
            _arr = _append(_arr, 2.0)
            _arr = _append(_arr, 8.0)
            return _arr
        }() as! [Double])))
        if (Double(abs(Double((m1 - 4.0)))) > eps) {
            _ = fatalError("test1 failed")
        }
        let m2 = Double(compute_geometric_mean(({
            var _arr: [Double] = []
            _arr = _append(_arr, 5.0)
            _arr = _append(_arr, 125.0)
            return _arr
        }() as! [Double])))
        if (Double(abs(Double((m2 - 25.0)))) > eps) {
            _ = fatalError("test2 failed")
        }
        let m3 = Double(compute_geometric_mean(({
            var _arr: [Double] = []
            _arr = _append(_arr, 1.0)
            _arr = _append(_arr, 0.0)
            return _arr
        }() as! [Double])))
        if (Double(abs(Double((m3 - 0.0)))) > eps) {
            _ = fatalError("test3 failed")
        }
        let m4 = Double(compute_geometric_mean(({
            var _arr: [Double] = []
            _arr = _append(_arr, 1.0)
            _arr = _append(_arr, 5.0)
            _arr = _append(_arr, 25.0)
            _arr = _append(_arr, 5.0)
            return _arr
        }() as! [Double])))
        if (Double(abs(Double((m4 - 5.0)))) > eps) {
            _ = fatalError("test4 failed")
        }
        let m5 = Double(compute_geometric_mean(({
            var _arr: [Double] = []
            _arr = _append(_arr, -5.0)
            _arr = _append(_arr, 25.0)
            _arr = _append(_arr, 1.0)
            return _arr
        }() as! [Double])))
        if (Double(abs(Double((m5 + 5.0)))) > eps) {
            _ = fatalError("test5 failed")
        }
    }
    func main() {
        _ = test_compute_geometric_mean()
        print(_p(Double(compute_geometric_mean(({
            var _arr: [Double] = []
            _arr = _append(_arr, -3.0)
            _arr = _append(_arr, -27.0)
            return _arr
        }() as! [Double])))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
