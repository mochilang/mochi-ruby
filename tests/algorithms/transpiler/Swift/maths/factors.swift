// Generated by Mochi transpiler v0.10.66 on 2025-08-16 12:42:08 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _concat<T>(_ a: [T], _ b: [T]) -> [T] {
    var out = a
    out.append(contentsOf: b)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func reverse(_ xs: [Int]) -> [Int] {
        var res: [Int] = ([] as! [Int])
        var i = (_int(((xs).count)) &- 1)
        while (i >= 0) {
            res = (_append(res, (_idx(xs, i) as? Int ?? 0)) as! [Int])
            i = _int((i &- 1))
        }
        return res
    }
    func factors_of_a_number(_ num: Int) -> [Int] {
        var facs: [Int] = ([] as! [Int])
        if (num < 1) {
            return facs
        }
        var small: [Int] = ([] as! [Int])
        var large: [Int] = ([] as! [Int])
        var i = 1
        while ((i &* i) <= num) {
            if (((num % i + i) % i) == 0) {
                small = (_append(small, i) as! [Int])
                let d = (num / i)
                if (d != i) {
                    large = (_append(large, d) as! [Int])
                }
            }
            i = _int((i &+ 1))
        }
        facs = ((_concat(small, (reverse((large as! [Int])) as! [Int])) as! [Any?]) as! [Int])
        return facs
    }
    func run_tests() {
        if ((factors_of_a_number(1) as! [Int]) != ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int])) {
            _ = fatalError("case1 failed")
        }
        if ((factors_of_a_number(5) as! [Int]) != ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 5)
            return _arr
        }() as! [Int])) {
            _ = fatalError("case2 failed")
        }
        if ((factors_of_a_number(24) as! [Int]) != ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 6)
            _arr = _append(_arr, 8)
            _arr = _append(_arr, 12)
            _arr = _append(_arr, 24)
            return _arr
        }() as! [Int])) {
            _ = fatalError("case3 failed")
        }
        if ((factors_of_a_number(-24) as! [Int]) != []) {
            _ = fatalError("case4 failed")
        }
    }
    func main() {
        _ = run_tests()
        print(_p("[" + (factors_of_a_number(24) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
