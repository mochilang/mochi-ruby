// Generated by Mochi transpiler v0.10.67 on 2025-08-17 12:19:27 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func min_int(_ a: Int, _ b: Int) -> Int {
        if ((a < b)) {
            return a
        }
        return b
    }
    func int_sqrt(_ n: Int) -> Int {
        var r = 0
        while ((_int((r &+ 1)) &* _int((r &+ 1))) <= n) {
            r = _int((r &+ 1))
        }
        return r
    }
    func sieve(_ n: Int) -> [Int] {
        if ((n <= 0)) {
            _ = fatalError("Number must instead be a positive integer")
        }
        var in_prime: [Int] = ([] as! [Int])
        var start = 2
        let end = _int(int_sqrt(n))
        var temp: [Int] = ([] as! [Int])
        var i = 0
        while (i < (end &+ 1)) {
            temp = (_append(temp, 1) as! [Int])
            i = _int((i &+ 1))
        }
        var prime: [Int] = ([] as! [Int])
        while (start <= end) {
            if (((_idx(temp, start) as? Int ?? 0) == 1)) {
                in_prime = (_append(in_prime, start) as! [Int])
                var j = (start &* start)
                while (j <= end) {
                    temp = _set(temp, j, { let _tmp0 = 0; return _tmp0 }())
                    j = _int((j &+ start))
                }
            }
            start = _int((start &+ 1))
        }
        i = 0
        while (i < _int(((in_prime).count))) {
            prime = (_append(prime, (_idx(in_prime, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        var low = (end &+ 1)
        var high = _int(min_int((2 &* end), n))
        while (low <= n) {
            var tempSeg: [Int] = ([] as! [Int])
            var size = ((high &- low) &+ 1)
            var k = 0
            while (k < size) {
                tempSeg = (_append(tempSeg, 1) as! [Int])
                k = _int((k &+ 1))
            }
            var idx = 0
            while (idx < _int(((in_prime).count))) {
                let each = (_idx(in_prime, idx) as? Int ?? 0)
                var t = (_int((low / each)) &* each)
                if ((t < low)) {
                    t = _int((t &+ each))
                }
                var j2 = t
                while (j2 <= high) {
                    tempSeg = _set(tempSeg, (j2 &- low), { let _tmp1 = 0; return _tmp1 }())
                    j2 = _int((j2 &+ each))
                }
                idx = _int((idx &+ 1))
            }
            var j3 = 0
            while (j3 < _int(((tempSeg).count))) {
                if (((_idx(tempSeg, j3) as? Int ?? 0) == 1)) {
                    prime = (_append(prime, (j3 &+ low)) as! [Int])
                }
                j3 = _int((j3 &+ 1))
            }
            low = _int((high &+ 1))
            high = _int(min_int((high &+ end), n))
        }
        return prime
    }
    func lists_equal(_ a: [Int], _ b: [Int]) -> Bool {
        if ((_int(((a).count)) != _int(((b).count)))) {
            return false
        }
        var m = 0
        while (m < _int(((a).count))) {
            if (((_idx(a, m) as? Int ?? 0) != (_idx(b, m) as? Int ?? 0))) {
                return false
            }
            m = _int((m &+ 1))
        }
        return true
    }
    func test_sieve() {
        let e1: [Int] = (sieve(8) as! [Int])
        if ((!lists_equal((e1 as! [Int]), ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int])))) {
            _ = fatalError("sieve(8) failed")
        }
        let e2: [Int] = (sieve(27) as! [Int])
        if ((!lists_equal((e2 as! [Int]), ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 5)
            _arr = _append(_arr, 7)
            _arr = _append(_arr, 11)
            _arr = _append(_arr, 13)
            _arr = _append(_arr, 17)
            _arr = _append(_arr, 19)
            _arr = _append(_arr, 23)
            return _arr
        }() as! [Int])))) {
            _ = fatalError("sieve(27) failed")
        }
    }
    func main() {
        _ = test_sieve()
        print(_p("[" + (sieve(30) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
