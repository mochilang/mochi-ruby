// Generated by Mochi transpiler v0.10.67 on 2025-08-17 12:19:58 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let PI: Double = 3.141592653589793
    func abs(_ x: Double) -> Double {
        if ((x < 0.0)) {
            return -x
        }
        return x
    }
    func floor(_ x: Double) -> Double {
        var i = Int(x)
        if ((_num(i) > x)) {
            i = _int((i &- 1))
        }
        return _num(i)
    }
    func pow(_ x: Double, _ n: Int) -> Double {
        var result = 1.0
        var i = 0
        while (i < n) {
            result = _num((result * x))
            i = _int((i &+ 1))
        }
        return result
    }
    func factorial(_ n: Int) -> Double {
        var result = 1.0
        var i = 2
        while (i <= n) {
            result = _num((result * _num(i)))
            i = _int((i &+ 1))
        }
        return result
    }
    func radians(_ deg: Double) -> Double {
        return ((deg * PI) / 180.0)
    }
    func taylor_sin(_ angle_in_degrees: Double, _ accuracy: Int, _ rounded_values_count: Int) -> Double {
        let k = _num(floor(_num((angle_in_degrees / 360.0))))
        var angle = (angle_in_degrees - _num((k * 360.0)))
        let angle_in_radians = _num(radians(_num(angle)))
        var result = angle_in_radians
        var a = 3
        var sign = -1.0
        var i = 0
        while (i < accuracy) {
            result = _num((result + (_num((sign * _num(pow(_num(angle_in_radians), a)))) / _num(factorial(a)))))
            sign = _num(-sign)
            a = _int((a &+ 2))
            i = _int((i &+ 1))
        }
        return result
    }
    func test_sin() {
        let eps = 0.0000001
        if ((_num(abs(_num((_num(taylor_sin(0.0, 18, 10)) - 0.0)))) > eps)) {
            _ = fatalError("sin(0) failed")
        }
        if ((_num(abs(_num((_num(taylor_sin(90.0, 18, 10)) - 1.0)))) > eps)) {
            _ = fatalError("sin(90) failed")
        }
        if ((_num(abs(_num((_num(taylor_sin(180.0, 18, 10)) - 0.0)))) > eps)) {
            _ = fatalError("sin(180) failed")
        }
        if ((_num(abs(_num((_num(taylor_sin(270.0, 18, 10)) - _num(-1.0))))) > eps)) {
            _ = fatalError("sin(270) failed")
        }
    }
    func main() {
        _ = test_sin()
        let res = _num(taylor_sin(64.0, 18, 10))
        print(_p(res))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
