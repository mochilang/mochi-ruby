// Generated by Mochi transpiler v0.10.66 on 2025-08-16 12:49:53 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func exact_prime_factor_count(_ n: Int) -> Int {
        var count = 0
        var num = n
        if (((num % 2 + 2) % 2) == 0) {
            count = _int((count &+ 1))
            while (((num % 2 + 2) % 2) == 0) {
                num = _int((num / 2))
            }
        }
        var i = 3
        while ((i &* i) <= num) {
            if (((num % i + i) % i) == 0) {
                count = _int((count &+ 1))
                while (((num % i + i) % i) == 0) {
                    num = _int((num / i))
                }
            }
            i = _int((i &+ 2))
        }
        if (num > 2) {
            count = _int((count &+ 1))
        }
        return count
    }
    func ln(_ x: Double) -> Double {
        let ln2 = 0.6931471805599453
        var y = x
        var k = 0.0
        while (y > 2.0) {
            y = Double((y / 2.0))
            k = Double((k + ln2))
        }
        while (y < 1.0) {
            y = Double((y * 2.0))
            k = Double((k - ln2))
        }
        let t = (Double((y - 1.0)) / Double((y + 1.0)))
        var term = t
        var sum = 0.0
        var n = 1
        while (n <= 19) {
            sum = Double((sum + (term / Double(n))))
            term = Double(((term * t) * t))
            n = _int((n &+ 2))
        }
        return (k + (2.0 * sum))
    }
    func floor(_ x: Double) -> Double {
        var i = Int(x)
        if (Double(i) > x) {
            i = _int((i &- 1))
        }
        return Double(i)
    }
    func round4(_ x: Double) -> Double {
        let m = 10000.0
        return (Double(floor(Double(((x * m) + 0.5)))) / m)
    }
    func main() {
        let n = 51242183
        let count = _int(exact_prime_factor_count(n))
        print(_p((_p("The number of distinct prime factors is/are ") + _p(_p(count)))))
        let loglog = Double(ln(Double(ln(Double(n)))))
        print(_p((_p("The value of log(log(n)) is ") + _p(_p(Double(round4(Double(loglog))))))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
