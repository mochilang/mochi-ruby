// Generated by Mochi transpiler v0.10.66 on 2025-08-16 12:42:52 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func normalize_index(_ index: Int, _ n: Int) -> Int {
        if (index < 0) {
            return (n &+ index)
        }
        return index
    }
    func find_max_iterative(_ nums: [Double]) -> Double {
        if (_int(((nums).count)) == 0) {
            _ = fatalError("find_max_iterative() arg is an empty sequence")
        }
        var max_num = Double(_idx(nums, 0))
        var i = 0
        while (i < _int(((nums).count))) {
            let x = Double(_idx(nums, i))
            if (x > max_num) {
                max_num = Double(x)
            }
            i = _int((i &+ 1))
        }
        return max_num
    }
    func find_max_recursive(_ nums: [Double], _ left: Int, _ right: Int) -> Double {
        let n = _int(((nums).count))
        if (n == 0) {
            _ = fatalError("find_max_recursive() arg is an empty sequence")
        }
        if ((((left >= n) || (left < _int((0 &- n)))) || (right >= n)) || (right < _int((0 &- n)))) {
            _ = fatalError("list index out of range")
        }
        var l = _int(normalize_index(left, n))
        var r = _int(normalize_index(right, n))
        if (l == r) {
            return Double(_idx(nums, l))
        }
        let mid = (_int((l &+ r)) / 2)
        let left_max = Double(find_max_recursive((nums as! [Double]), l, mid))
        let right_max = Double(find_max_recursive((nums as! [Double]), (mid &+ 1), r))
        if (left_max >= right_max) {
            return left_max
        }
        return right_max
    }
    func test_find_max() {
        let arr: [Double] = ({
            var _arr: [Double] = []
            _arr = _append(_arr, 2.0)
            _arr = _append(_arr, 4.0)
            _arr = _append(_arr, 9.0)
            _arr = _append(_arr, 7.0)
            _arr = _append(_arr, 19.0)
            _arr = _append(_arr, 94.0)
            _arr = _append(_arr, 5.0)
            return _arr
        }() as! [Double])
        if (Double(find_max_iterative((arr as! [Double]))) != 94.0) {
            _ = fatalError("find_max_iterative failed")
        }
        if (Double(find_max_recursive((arr as! [Double]), 0, (_int(((arr).count)) &- 1))) != 94.0) {
            _ = fatalError("find_max_recursive failed")
        }
        if (Double(find_max_recursive((arr as! [Double]), -_int(((arr).count)), -1)) != 94.0) {
            _ = fatalError("negative index handling failed")
        }
    }
    func main() {
        _ = test_find_max()
        let nums: [Double] = ({
            var _arr: [Double] = []
            _arr = _append(_arr, 2.0)
            _arr = _append(_arr, 4.0)
            _arr = _append(_arr, 9.0)
            _arr = _append(_arr, 7.0)
            _arr = _append(_arr, 19.0)
            _arr = _append(_arr, 94.0)
            _arr = _append(_arr, 5.0)
            return _arr
        }() as! [Double])
        print(_p(Double(find_max_iterative((nums as! [Double])))))
        print(_p(Double(find_max_recursive((nums as! [Double]), 0, (_int(((nums).count)) &- 1)))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
