// Generated by Mochi transpiler v0.10.67 on 2025-08-16 21:28:37 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let MAX = 4294967296
    let HALF = 2147483648
    func to_unsigned(_ n: Int) -> Int {
        if (n < 0) {
            return (MAX &+ n)
        }
        return n
    }
    func from_unsigned(_ n: Int) -> Int {
        if (n >= HALF) {
            return (n &- MAX)
        }
        return n
    }
    func bit_and(_ a: Int, _ b: Int) -> Int {
        var x = a
        var y = b
        var res = 0
        var bit = 1
        var i = 0
        while (i < 32) {
            if ((((x % 2 + 2) % 2) == 1) && (((y % 2 + 2) % 2) == 1)) {
                res = _int((res &+ bit))
            }
            x = _int((x / 2))
            y = _int((y / 2))
            bit = _int((bit &* 2))
            i = _int((i &+ 1))
        }
        return res
    }
    func bit_xor(_ a: Int, _ b: Int) -> Int {
        var x = a
        var y = b
        var res = 0
        var bit = 1
        var i = 0
        while (i < 32) {
            let abit = ((x % 2 + 2) % 2)
            let bbit = ((y % 2 + 2) % 2)
            if (((_int((abit &+ bbit)) % 2 + 2) % 2) == 1) {
                res = _int((res &+ bit))
            }
            x = _int((x / 2))
            y = _int((y / 2))
            bit = _int((bit &* 2))
            i = _int((i &+ 1))
        }
        return res
    }
    func lshift1(_ num: Int) -> Int {
        return ((_int((num &* 2)) % MAX + MAX) % MAX)
    }
    func add(_ a: Int, _ b: Int) -> Int {
        var first = _int(to_unsigned(a))
        var second = _int(to_unsigned(b))
        while (second != 0) {
            let carry = _int(bit_and(first, second))
            first = _int(bit_xor(first, second))
            second = _int(lshift1(carry))
        }
        let result = _int(from_unsigned(first))
        return result
    }
    print(_p(_p(_int(add(3, 5)))))
    print(_p(_p(_int(add(13, 5)))))
    print(_p(_p(_int(add(-7, 2)))))
    print(_p(_p(_int(add(0, -7)))))
    print(_p(_p(_int(add(-321, 0)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
