// Generated by Mochi transpiler v0.10.66 on 2025-08-16 13:05:06 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func sqrt(_ x: Double) -> Double {
        var guess = Double(((x > 1.0) ? (x / 2.0) : 1.0))
        var i = 0
        while (i < 20) {
            guess = Double((0.5 * Double((guess + (x / guess)))))
            i = _int((i &+ 1))
        }
        return guess
    }
    func runge_kutta_gills(_ `func`: @escaping (Double, Double) -> Double, _ x_initial: Double, _ y_initial: Double, _ step_size: Double, _ x_final: Double) -> [Double] {
        if (x_initial >= x_final) {
            _ = fatalError("The final value of x must be greater than initial value of x.")
        }
        if (step_size <= 0.0) {
            _ = fatalError("Step size must be positive.")
        }
        let n = _int((Double((x_final - x_initial)) / step_size))
        var y: [Double] = ([] as! [Double])
        var i = 0
        while (i <= n) {
            y = (_append(y, 0.0) as! [Double])
            i = _int((i &+ 1))
        }
        y = _set(y, 0, { let _tmp0 = Double(y_initial); return _tmp0 }())
        var xi = x_initial
        var idx = 0
        let root2 = Double(sqrt(2.0))
        while (idx < n) {
            let k1 = (step_size * Double(`func`(Double(xi), Double(_idx(y, idx)))))
            let k2 = (step_size * Double(`func`(Double((xi + (step_size / 2.0))), Double((Double(_idx(y, idx)) + (k1 / 2.0))))))
            let k3 = (step_size * Double(`func`(Double((xi + (step_size / 2.0))), Double(((Double(_idx(y, idx)) + (Double((-0.5 + (1.0 / root2))) * k1)) + (Double((1.0 - (1.0 / root2))) * k2))))))
            let k4 = (step_size * Double(`func`(Double((xi + step_size)), Double(((Double(_idx(y, idx)) - (Double((1.0 / root2)) * k2)) + (Double((1.0 + (1.0 / root2))) * k3))))))
            y = _set(y, (idx &+ 1), { let _tmp1 = Double((Double(_idx(y, idx)) + (Double((((k1 + (Double((2.0 - root2)) * k2)) + (Double((2.0 + root2)) * k3)) + k4)) / 6.0))); return _tmp1 }())
            xi = Double((xi + step_size))
            idx = _int((idx &+ 1))
        }
        return y
    }
    func f1(_ x: Double, _ y: Double) -> Double {
        return (Double((x - y)) / 2.0)
    }
    let y1: [Double] = (runge_kutta_gills((f1 as! (Double, Double) -> Double), 0.0, 3.0, 0.2, 5.0) as! [Double])
    print(_p(_p(Double(_idx(y1, (_int(((y1).count)) &- 1))))))
    func f2(_ x: Double, _ y: Double) -> Double {
        return x
    }
    let y2: [Double] = (runge_kutta_gills((f2 as! (Double, Double) -> Double), Double(-1.0), 0.0, 0.2, 0.0) as! [Double])
    print(_p("[" + y2.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
