// Generated by Mochi transpiler v0.10.66 on 2025-08-16 13:02:44 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let N_STEPS = 1000
    func floor(_ x: Double) -> Double {
        var i = Int(x)
        if (Double(i) > x) {
            i = _int((i &- 1))
        }
        return Double(i)
    }
    func pow10(_ n: Int) -> Double {
        var p = 1.0
        var i = 0
        while (i < n) {
            p = Double((p * 10.0))
            i = _int((i &+ 1))
        }
        return p
    }
    func round(_ x: Double, _ n: Int) -> Double {
        let m = Double(pow10(n))
        return (Double(floor(Double(((x * m) + 0.5)))) / m)
    }
    func simpson_integration(_ f: @escaping (Double) -> Double, _ a: Double, _ b: Double, _ precision: Int) -> Double {
        if (precision <= 0) {
            _ = fatalError("precision should be positive")
        }
        let h = (Double((b - a)) / Double(N_STEPS))
        var result = (Double(f(Double(a))) + Double(f(Double(b))))
        var i = 1
        while (i < N_STEPS) {
            let x = (a + (h * Double(i)))
            if (((i % 2 + 2) % 2) == 1) {
                result = Double((result + (4.0 * Double(f(Double(x))))))
            } else {
                result = Double((result + (2.0 * Double(f(Double(x))))))
            }
            i = _int((i &+ 1))
        }
        result = Double((result * Double((h / 3.0))))
        let r = Double(round(Double(result), precision))
        return r
    }
    func square(_ x: Double) -> Double {
        return (x * x)
    }
    print(_p(_p(Double(simpson_integration((square as! (Double) -> Double), 1.0, 2.0, 3)))))
    print(_p(_p(Double(simpson_integration((square as! (Double) -> Double), 3.45, 3.2, 1)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
