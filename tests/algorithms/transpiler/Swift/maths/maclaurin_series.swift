// Generated by Mochi transpiler v0.10.66 on 2025-08-16 13:00:20 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let PI = 3.141592653589793
    func floor(_ x: Double) -> Double {
        var i: Int = Int(x)
        if (Double(i) > x) {
            i = _int((i &- 1))
        }
        return Double(i)
    }
    func pow(_ x: Double, _ n: Int) -> Double {
        var result: Double = 1.0
        var i: Int = 0
        while (i < n) {
            result = Double((result * x))
            i = _int((i &+ 1))
        }
        return result
    }
    func factorial(_ n: Int) -> Double {
        var result: Double = 1.0
        var i: Int = 2
        while (i <= n) {
            result = Double((result * Double(i)))
            i = _int((i &+ 1))
        }
        return result
    }
    func maclaurin_sin(_ theta: Double, _ accuracy: Int) -> Double {
        var t: Double = theta
        let div = Double(floor(Double((t / Double((2.0 * PI))))))
        t = Double((t - ((2.0 * div) * PI)))
        var sum: Double = 0.0
        var r: Int = 0
        while (r < accuracy) {
            let power = ((2 &* r) &+ 1)
            let sign = Double(((((r % 2 + 2) % 2) == 0) ? 1.0 : -1.0))
            sum = Double((sum + ((sign * Double(pow(Double(t), power))) / Double(factorial(power)))))
            r = _int((r &+ 1))
        }
        return sum
    }
    func maclaurin_cos(_ theta: Double, _ accuracy: Int) -> Double {
        var t: Double = theta
        let div = Double(floor(Double((t / Double((2.0 * PI))))))
        t = Double((t - ((2.0 * div) * PI)))
        var sum: Double = 0.0
        var r: Int = 0
        while (r < accuracy) {
            let power = (2 &* r)
            let sign = Double(((((r % 2 + 2) % 2) == 0) ? 1.0 : -1.0))
            sum = Double((sum + ((sign * Double(pow(Double(t), power))) / Double(factorial(power)))))
            r = _int((r &+ 1))
        }
        return sum
    }
    print(_p(_p(Double(maclaurin_sin(10.0, 30)))))
    print(_p(_p(Double(maclaurin_sin(Double(-10.0), 30)))))
    print(_p(_p(Double(maclaurin_sin(10.0, 15)))))
    print(_p(_p(Double(maclaurin_sin(Double(-10.0), 15)))))
    print(_p(_p(Double(maclaurin_cos(5.0, 30)))))
    print(_p(_p(Double(maclaurin_cos(Double(-5.0), 30)))))
    print(_p(_p(Double(maclaurin_cos(10.0, 15)))))
    print(_p(_p(Double(maclaurin_cos(Double(-10.0), 15)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
