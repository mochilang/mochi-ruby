// Generated by Mochi transpiler v0.10.67 on 2025-08-16 21:33:44 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func binary_exp_recursive(_ base: Double, _ exponent: Int) -> Double {
        if (exponent < 0) {
            _ = fatalError("exponent must be non-negative")
        }
        if (exponent == 0) {
            return 1.0
        }
        if (((exponent % 2 + 2) % 2) == 1) {
            return (Double(binary_exp_recursive(Double(base), (exponent &- 1))) * base)
        }
        let half = Double(binary_exp_recursive(Double(base), (exponent / 2)))
        return (half * half)
    }
    func binary_exp_iterative(_ base: Double, _ exponent: Int) -> Double {
        if (exponent < 0) {
            _ = fatalError("exponent must be non-negative")
        }
        var result: Double = 1.0
        var b = base
        var e = exponent
        while (e > 0) {
            if (((e % 2 + 2) % 2) == 1) {
                result = Double((result * b))
            }
            b = Double((b * b))
            e = _int((e / 2))
        }
        return result
    }
    func binary_exp_mod_recursive(_ base: Int, _ exponent: Int, _ modulus: Int) -> Int {
        if (exponent < 0) {
            _ = fatalError("exponent must be non-negative")
        }
        if (modulus <= 0) {
            _ = fatalError("modulus must be positive")
        }
        if (exponent == 0) {
            return ((1 % modulus + modulus) % modulus)
        }
        if (((exponent % 2 + 2) % 2) == 1) {
            return ((_int((_int(binary_exp_mod_recursive(base, (exponent &- 1), modulus)) &* _int(((base % modulus + modulus) % modulus)))) % modulus + modulus) % modulus)
        }
        let r = _int(binary_exp_mod_recursive(base, (exponent / 2), modulus))
        return ((_int((r &* r)) % modulus + modulus) % modulus)
    }
    func binary_exp_mod_iterative(_ base: Int, _ exponent: Int, _ modulus: Int) -> Int {
        if (exponent < 0) {
            _ = fatalError("exponent must be non-negative")
        }
        if (modulus <= 0) {
            _ = fatalError("modulus must be positive")
        }
        var result: Int = ((1 % modulus + modulus) % modulus)
        var b = ((base % modulus + modulus) % modulus)
        var e = exponent
        while (e > 0) {
            if (((e % 2 + 2) % 2) == 1) {
                result = _int(((_int((result &* b)) % modulus + modulus) % modulus))
            }
            b = _int(((_int((b &* b)) % modulus + modulus) % modulus))
            e = _int((e / 2))
        }
        return result
    }
    print(_p(Double(binary_exp_recursive(3.0, 5))))
    print(_p(Double(binary_exp_iterative(1.5, 4))))
    print(_p(_int(binary_exp_mod_recursive(3, 4, 5))))
    print(_p(_int(binary_exp_mod_iterative(11, 13, 7))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
