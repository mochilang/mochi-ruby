// Generated by Mochi transpiler v0.10.66 on 2025-08-16 13:02:34 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func abs_int(_ n: Int) -> Int {
        if (n < 0) {
            return -n
        }
        return n
    }
    func num_digits(_ n: Int) -> Int {
        var x = _int(abs_int(n))
        var digits = 1
        while (x >= 10) {
            x = _int((x / 10))
            digits = _int((digits &+ 1))
        }
        return digits
    }
    func num_digits_fast(_ n: Int) -> Int {
        var x = _int(abs_int(n))
        var digits = 1
        var power = 10
        while (x >= power) {
            power = _int((power &* 10))
            digits = _int((digits &+ 1))
        }
        return digits
    }
    func num_digits_faster(_ n: Int) -> Int {
        let s = _p(_int(abs_int(n)))
        return _int(((s).count))
    }
    func test_num_digits() {
        if (_int(num_digits(12345)) != 5) {
            _ = fatalError("num_digits 12345 failed")
        }
        if (_int(num_digits(123)) != 3) {
            _ = fatalError("num_digits 123 failed")
        }
        if (_int(num_digits(0)) != 1) {
            _ = fatalError("num_digits 0 failed")
        }
        if (_int(num_digits(-1)) != 1) {
            _ = fatalError("num_digits -1 failed")
        }
        if (_int(num_digits(-123456)) != 6) {
            _ = fatalError("num_digits -123456 failed")
        }
        if (_int(num_digits_fast(12345)) != 5) {
            _ = fatalError("num_digits_fast 12345 failed")
        }
        if (_int(num_digits_fast(123)) != 3) {
            _ = fatalError("num_digits_fast 123 failed")
        }
        if (_int(num_digits_fast(0)) != 1) {
            _ = fatalError("num_digits_fast 0 failed")
        }
        if (_int(num_digits_fast(-1)) != 1) {
            _ = fatalError("num_digits_fast -1 failed")
        }
        if (_int(num_digits_fast(-123456)) != 6) {
            _ = fatalError("num_digits_fast -123456 failed")
        }
        if (_int(num_digits_faster(12345)) != 5) {
            _ = fatalError("num_digits_faster 12345 failed")
        }
        if (_int(num_digits_faster(123)) != 3) {
            _ = fatalError("num_digits_faster 123 failed")
        }
        if (_int(num_digits_faster(0)) != 1) {
            _ = fatalError("num_digits_faster 0 failed")
        }
        if (_int(num_digits_faster(-1)) != 1) {
            _ = fatalError("num_digits_faster -1 failed")
        }
        if (_int(num_digits_faster(-123456)) != 6) {
            _ = fatalError("num_digits_faster -123456 failed")
        }
    }
    func main() {
        _ = test_num_digits()
        print(_p(_p(_int(num_digits(12345)))))
        print(_p(_p(_int(num_digits_fast(12345)))))
        print(_p(_p(_int(num_digits_faster(12345)))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
