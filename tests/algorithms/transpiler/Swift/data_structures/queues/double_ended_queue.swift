// Generated by Mochi transpiler v0.10.61 on 2025-08-08 17:10:12 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return nil
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct Deque: Codable {
    var data: [Int]
    init() {
        self.data = []
    }
    init(data: [Int]) {
        self.data = data
    }
}
struct PopResult: Codable {
    var deque: Deque
    var value: Int
    init() {
        self.deque = Deque()
        self.value = 0
    }
    init(deque: Deque, value: Int) {
        self.deque = deque
        self.value = value
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func empty_deque() -> Deque {
        return Deque(data: [])
    }
    func push_back(_ dq: Deque, _ value: Int) -> Deque {
        return Deque(data: (_append(dq.data, value) as! [Int]))
    }
    func push_front(_ dq: Deque, _ value: Int) -> Deque {
        var res: [Int] = ([value] as! [Int])
        var i: Int = 0
        while (i < _int(((dq.data).count))) {
            res = (_append(res, (_idx(dq.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        return Deque(data: res)
    }
    func extend_back(_ dq: Deque, _ values: [Int]) -> Deque {
        var res: [Int] = dq.data
        var i: Int = 0
        while (i < _int(((values).count))) {
            res = (_append(res, (_idx(values, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        return Deque(data: res)
    }
    func extend_front(_ dq: Deque, _ values: [Int]) -> Deque {
        var res: [Int] = ([] as! [Int])
        var i: Int = (_int(((values).count)) &- 1)
        while (i >= 0) {
            res = (_append(res, (_idx(values, i) as? Int ?? 0)) as! [Int])
            i = _int((i &- 1))
        }
        var j: Int = 0
        while (j < _int(((dq.data).count))) {
            res = (_append(res, (_idx(dq.data, j) as? Int ?? 0)) as! [Int])
            j = _int((j &+ 1))
        }
        return Deque(data: res)
    }
    func pop_back(_ dq: Deque) -> PopResult {
        if (_int(((dq.data).count)) == 0) {
            _ = fatalError("pop from empty deque")
        }
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < (_int(((dq.data).count)) &- 1)) {
            res = (_append(res, (_idx(dq.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        return PopResult(deque: Deque(data: res), value: (_idx(dq.data, (_int(((dq.data).count)) &- 1)) as? Int ?? 0))
    }
    func pop_front(_ dq: Deque) -> PopResult {
        if (_int(((dq.data).count)) == 0) {
            _ = fatalError("popleft from empty deque")
        }
        var res: [Int] = ([] as! [Int])
        var i: Int = 1
        while (i < _int(((dq.data).count))) {
            res = (_append(res, (_idx(dq.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        return PopResult(deque: Deque(data: res), value: (_idx(dq.data, 0) as? Int ?? 0))
    }
    func is_empty(_ dq: Deque) -> Bool {
        return (_int(((dq.data).count)) == 0)
    }
    func length(_ dq: Deque) -> Int {
        return _int(((dq.data).count))
    }
    func to_string(_ dq: Deque) -> String {
        if (_int(((dq.data).count)) == 0) {
            return "[]"
        }
        var s: String = ("[" + _p((_idx(dq.data, 0) as? Int ?? 0)))
        var i: Int = 1
        while (i < _int(((dq.data).count))) {
            s = (((s + ", ") + _p((_idx(dq.data, i) as? Int ?? 0))) as! String)
            i = _int((i &+ 1))
        }
        return (s + "]")
    }
    func main() {
        var dq: Deque = empty_deque()
        dq = (push_back((dq as! Deque), 2) as! Deque)
        dq = (push_front((dq as! Deque), 1) as! Deque)
        dq = (extend_back((dq as! Deque), ([3, 4] as! [Int])) as! Deque)
        dq = (extend_front((dq as! Deque), ([0] as! [Int])) as! Deque)
        print(_p(to_string((dq as! Deque))))
        var r: PopResult = pop_back((dq as! Deque))
        dq = (r.deque as! Deque)
        print(_p(r.value))
        r = (pop_front((dq as! Deque)) as! PopResult)
        dq = (r.deque as! Deque)
        print(_p(r.value))
        print(_p(to_string((dq as! Deque))))
        print(_p(is_empty((empty_deque() as! Deque))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
