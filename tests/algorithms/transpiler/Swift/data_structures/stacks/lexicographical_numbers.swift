// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:28:52 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func lexical_order(_ max_number: Int) -> [Int?] {
        var result: [Int?] = ([] as! [Int?])
        var stack: [Int?] = ([1] as! [Int?])
        while (Int(((stack).count)) > 0) {
            let idx = (Int(((stack).count)) &- 1)
            let num = (stack[idx] as? Int ?? 0)
            stack = (Array(stack[0..<idx]) as! [Int?])
            if (num > max_number) {
                continue
            }
            result = (_append(result, num) as! [Int?])
            if (((num % 10 + 10) % 10) != 9) {
                stack = (_append(stack, (num &+ 1)) as! [Int?])
            }
            stack = (_append(stack, (num &* 10)) as! [Int?])
        }
        return result
    }
    func join_ints(_ xs: [Int?]) -> String {
        var res: String = ""
        var i: Int = 0
        while (i < Int(((xs).count))) {
            if (i > 0) {
                res = ((res + " ") as! String)
            }
            res = ((res + String(describing: (xs[i] as? Int ?? 0))) as! String)
            i = Int((i &+ 1))
        }
        return res
    }
    print(_p(String(describing: join_ints((lexical_order(13) as! [Int?])))))
    print(_p("[" + (lexical_order(1) as! [Int?]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    print(_p(String(describing: join_ints((lexical_order(20) as! [Int?])))))
    print(_p(String(describing: join_ints((lexical_order(25) as! [Int?])))))
    print(_p("[" + (lexical_order(12) as! [Int?]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
