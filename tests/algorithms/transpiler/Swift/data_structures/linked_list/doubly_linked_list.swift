// Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:32:26 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return nil
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct DoublyLinkedList: Codable {
    var data: [Int]
    init() {
        self.data = []
    }
    init(data: [Int]) {
        self.data = data
    }
}
struct DeleteResult: Codable {
    var list: DoublyLinkedList
    var value: Int
    init() {
        self.list = DoublyLinkedList()
        self.value = 0
    }
    init(list: DoublyLinkedList, value: Int) {
        self.list = list
        self.value = value
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func empty_list() -> DoublyLinkedList {
        return DoublyLinkedList(data: [])
    }
    func length(_ list: DoublyLinkedList) -> Int {
        return _int(((list.data).count))
    }
    func is_empty(_ list: DoublyLinkedList) -> Bool {
        return (_int(((list.data).count)) == 0)
    }
    func to_string(_ list: DoublyLinkedList) -> String {
        if (_int(((list.data).count)) == 0) {
            return ""
        }
        var s: String = _p((_idx(list.data, 0) as? Int ?? 0))
        var i: Int = 1
        while (i < _int(((list.data).count))) {
            s = (((s + "->") + _p((_idx(list.data, i) as? Int ?? 0))) as! String)
            i = _int((i &+ 1))
        }
        return s
    }
    func insert_nth(_ list: DoublyLinkedList, _ index: Int, _ value: Int) -> DoublyLinkedList {
        if ((index < 0) || (index > _int(((list.data).count)))) {
            _ = fatalError("index out of range")
        }
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < index) {
            res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        res = (_append(res, value) as! [Int])
        while (i < _int(((list.data).count))) {
            res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        return DoublyLinkedList(data: res)
    }
    func insert_head(_ list: DoublyLinkedList, _ value: Int) -> DoublyLinkedList {
        return insert_nth((list as! DoublyLinkedList), 0, value)
    }
    func insert_tail(_ list: DoublyLinkedList, _ value: Int) -> DoublyLinkedList {
        return insert_nth((list as! DoublyLinkedList), _int(((list.data).count)), value)
    }
    func delete_nth(_ list: DoublyLinkedList, _ index: Int) -> DeleteResult {
        if ((index < 0) || (index >= _int(((list.data).count)))) {
            _ = fatalError("index out of range")
        }
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        var removed: Int = 0
        while (i < _int(((list.data).count))) {
            if (i == index) {
                removed = (_idx(list.data, i) as? Int ?? 0)
            } else {
                res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            }
            i = _int((i &+ 1))
        }
        return DeleteResult(list: DoublyLinkedList(data: res), value: removed)
    }
    func delete_head(_ list: DoublyLinkedList) -> DeleteResult {
        return delete_nth((list as! DoublyLinkedList), 0)
    }
    func delete_tail(_ list: DoublyLinkedList) -> DeleteResult {
        return delete_nth((list as! DoublyLinkedList), (_int(((list.data).count)) &- 1))
    }
    func delete_value(_ list: DoublyLinkedList, _ value: Int) -> DeleteResult {
        var idx: Int = 0
        var found: Bool = false
        while (idx < _int(((list.data).count))) {
            if ((_idx(list.data, idx) as? Int ?? 0) == value) {
                found = true
                break
            }
            idx = _int((idx &+ 1))
        }
        if (!found) {
            _ = fatalError("value not found")
        }
        return delete_nth((list as! DoublyLinkedList), idx)
    }
    func main() {
        var dll: DoublyLinkedList = empty_list()
        dll = (insert_tail((dll as! DoublyLinkedList), 1) as! DoublyLinkedList)
        dll = (insert_tail((dll as! DoublyLinkedList), 2) as! DoublyLinkedList)
        dll = (insert_tail((dll as! DoublyLinkedList), 3) as! DoublyLinkedList)
        print(_p(to_string((dll as! DoublyLinkedList))))
        dll = (insert_head((dll as! DoublyLinkedList), 0) as! DoublyLinkedList)
        print(_p(to_string((dll as! DoublyLinkedList))))
        dll = (insert_nth((dll as! DoublyLinkedList), 2, 9) as! DoublyLinkedList)
        print(_p(to_string((dll as! DoublyLinkedList))))
        var res: DeleteResult = delete_nth((dll as! DoublyLinkedList), 2)
        dll = (res.list as! DoublyLinkedList)
        print(_p(res.value))
        print(_p(to_string((dll as! DoublyLinkedList))))
        res = (delete_tail((dll as! DoublyLinkedList)) as! DeleteResult)
        dll = (res.list as! DoublyLinkedList)
        print(_p(res.value))
        print(_p(to_string((dll as! DoublyLinkedList))))
        res = (delete_value((dll as! DoublyLinkedList), 1) as! DeleteResult)
        dll = (res.list as! DoublyLinkedList)
        print(_p(res.value))
        print(_p(to_string((dll as! DoublyLinkedList))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
