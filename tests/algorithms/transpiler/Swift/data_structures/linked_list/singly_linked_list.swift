// Generated by Mochi transpiler v0.10.61 on 2025-08-08 17:07:23 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return nil
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct SinglyLinkedList: Codable {
    var data: [Int]
    init() {
        self.data = []
    }
    init(data: [Int]) {
        self.data = data
    }
}
struct DeleteResult: Codable {
    var list: SinglyLinkedList
    var value: Int
    init() {
        self.list = SinglyLinkedList()
        self.value = 0
    }
    init(list: SinglyLinkedList, value: Int) {
        self.list = list
        self.value = value
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func empty_list() -> SinglyLinkedList {
        return SinglyLinkedList(data: [])
    }
    func length(_ list: SinglyLinkedList) -> Int {
        return _int(((list.data).count))
    }
    func is_empty(_ list: SinglyLinkedList) -> Bool {
        return (_int(((list.data).count)) == 0)
    }
    func to_string(_ list: SinglyLinkedList) -> String {
        if (_int(((list.data).count)) == 0) {
            return ""
        }
        var s: String = _p((_idx(list.data, 0) as? Int ?? 0))
        var i: Int = 1
        while (i < _int(((list.data).count))) {
            s = (((s + " -> ") + _p((_idx(list.data, i) as? Int ?? 0))) as! String)
            i = _int((i &+ 1))
        }
        return s
    }
    func insert_nth(_ list: SinglyLinkedList, _ index: Int, _ value: Int) -> SinglyLinkedList {
        if ((index < 0) || (index > _int(((list.data).count)))) {
            _ = fatalError("index out of range")
        }
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < index) {
            res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        res = (_append(res, value) as! [Int])
        while (i < _int(((list.data).count))) {
            res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        return SinglyLinkedList(data: res)
    }
    func insert_head(_ list: SinglyLinkedList, _ value: Int) -> SinglyLinkedList {
        return insert_nth((list as! SinglyLinkedList), 0, value)
    }
    func insert_tail(_ list: SinglyLinkedList, _ value: Int) -> SinglyLinkedList {
        return insert_nth((list as! SinglyLinkedList), _int(((list.data).count)), value)
    }
    func delete_nth(_ list: SinglyLinkedList, _ index: Int) -> DeleteResult {
        if ((index < 0) || (index >= _int(((list.data).count)))) {
            _ = fatalError("index out of range")
        }
        var res: [Int] = ([] as! [Int])
        var val: Int = 0
        var i: Int = 0
        while (i < _int(((list.data).count))) {
            if (i == index) {
                val = (_idx(list.data, i) as? Int ?? 0)
            } else {
                res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            }
            i = _int((i &+ 1))
        }
        return DeleteResult(list: SinglyLinkedList(data: res), value: val)
    }
    func delete_head(_ list: SinglyLinkedList) -> DeleteResult {
        return delete_nth((list as! SinglyLinkedList), 0)
    }
    func delete_tail(_ list: SinglyLinkedList) -> DeleteResult {
        return delete_nth((list as! SinglyLinkedList), (_int(((list.data).count)) &- 1))
    }
    func get_item(_ list: SinglyLinkedList, _ index: Int) -> Int {
        if ((index < 0) || (index >= _int(((list.data).count)))) {
            _ = fatalError("index out of range")
        }
        return (_idx(list.data, index) as? Int ?? 0)
    }
    func set_item(_ list: SinglyLinkedList, _ index: Int, _ value: Int) -> SinglyLinkedList {
        if ((index < 0) || (index >= _int(((list.data).count)))) {
            _ = fatalError("index out of range")
        }
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < _int(((list.data).count))) {
            if (i == index) {
                res = (_append(res, value) as! [Int])
            } else {
                res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            }
            i = _int((i &+ 1))
        }
        return SinglyLinkedList(data: res)
    }
    func reverse_list(_ list: SinglyLinkedList) -> SinglyLinkedList {
        var res: [Int] = ([] as! [Int])
        var i: Int = (_int(((list.data).count)) &- 1)
        while (i >= 0) {
            res = (_append(res, (_idx(list.data, i) as? Int ?? 0)) as! [Int])
            i = _int((i &- 1))
        }
        return SinglyLinkedList(data: res)
    }
    func main() {
        var lst: SinglyLinkedList = empty_list()
        var i: Int = 1
        while (i <= 5) {
            lst = (insert_tail((lst as! SinglyLinkedList), i) as! SinglyLinkedList)
            i = _int((i &+ 1))
        }
        print(_p(to_string((lst as! SinglyLinkedList))))
        lst = (insert_head((lst as! SinglyLinkedList), 0) as! SinglyLinkedList)
        print(_p(to_string((lst as! SinglyLinkedList))))
        var del: DeleteResult = delete_head((lst as! SinglyLinkedList))
        lst = (del.list as! SinglyLinkedList)
        print(_p(_p(del.value)))
        del = (delete_tail((lst as! SinglyLinkedList)) as! DeleteResult)
        lst = (del.list as! SinglyLinkedList)
        print(_p(_p(del.value)))
        del = (delete_nth((lst as! SinglyLinkedList), 2) as! DeleteResult)
        lst = (del.list as! SinglyLinkedList)
        print(_p(_p(del.value)))
        lst = (set_item((lst as! SinglyLinkedList), 1, 99) as! SinglyLinkedList)
        print(_p(_p(_int(get_item((lst as! SinglyLinkedList), 1)))))
        lst = (reverse_list((lst as! SinglyLinkedList)) as! SinglyLinkedList)
        print(_p(to_string((lst as! SinglyLinkedList))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
