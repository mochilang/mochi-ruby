// Generated by Mochi transpiler v0.10.61 on 2025-08-08 17:06:53 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return nil
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
struct Node: Codable {
    var data: Int
    var next: Int
    init() {
        self.data = 0
        self.next = 0
    }
    init(data: Int, next: Int) {
        self.data = data
        self.next = next
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let NIL = (0 &- 1)
    var nodes: [Node] = ([] as! [Node])
    func make_linked_list(_ elements: [Int]) -> Int {
        if (_int(((elements).count)) == 0) {
            _ = fatalError("The Elements List is empty")
        }
        nodes = ([] as! [Node])
        nodes = (_append(nodes, Node(data: (_idx(elements, 0) as? Int ?? 0), next: NIL)) as! [Node])
        var head: Int = 0
        var current: Int = head
        var i: Int = 1
        while (i < _int(((elements).count))) {
            nodes = (_append(nodes, Node(data: (_idx(elements, i) as? Int ?? 0), next: NIL)) as! [Node])
            var _tmp2 = (_idx(nodes, current) as! Node)
            _tmp2.next = _int((_int(((nodes).count)) &- 1))
            nodes = _set(nodes, current, (_tmp2 as! Node))
            current = _int((_int(((nodes).count)) &- 1))
            i = _int((i &+ 1))
        }
        return head
    }
    func node_to_string(_ head: Int) -> String {
        var s: String = ""
        var index: Int = head
        while (index != NIL) {
            let node = (_idx(nodes, index) as! Node)
            s = ((((s + "<") + _p(node.data)) + "> ---> ") as! String)
            index = _int(node.next)
        }
        s = ((s + "<END>") as! String)
        return s
    }
    func main() {
        let list_data: [Int] = ([1, 3, 5, 32, 44, 12, 43] as! [Int])
        print(_p(("List: " + "[" + list_data.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]")))
        print(_p("Creating Linked List from List."))
        let head = _int(make_linked_list((list_data as! [Int])))
        print(_p("Linked List:"))
        print(_p(String(describing: node_to_string(head))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
