// Generated by Mochi transpiler v0.10.61 on 2025-08-08 17:07:15 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return nil
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct LinkedList: Codable {
    var data: [Int]
    init() {
        self.data = []
    }
    init(data: [Int]) {
        self.data = data
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func empty_list() -> LinkedList {
        return LinkedList(data: [])
    }
    func append_value(_ list: LinkedList, _ value: Int) -> LinkedList {
        var d: [Int] = list.data
        d = (_append(d, value) as! [Int])
        return LinkedList(data: d)
    }
    func extend_list(_ list: LinkedList, _ items: [Int]) -> LinkedList {
        var result: LinkedList = list
        var i: Int = 0
        while (i < _int(((items).count))) {
            result = (append_value((result as! LinkedList), (_idx(items, i) as? Int ?? 0)) as! LinkedList)
            i = _int((i &+ 1))
        }
        return result
    }
    func to_string(_ list: LinkedList) -> String {
        if (_int(((list.data).count)) == 0) {
            return ""
        }
        var s: String = _p((_idx(list.data, 0) as? Int ?? 0))
        var i: Int = 1
        while (i < _int(((list.data).count))) {
            s = (((s + " -> ") + _p((_idx(list.data, i) as? Int ?? 0))) as! String)
            i = _int((i &+ 1))
        }
        return s
    }
    func make_linked_list(_ items: [Int]) -> LinkedList {
        if (_int(((items).count)) == 0) {
            _ = fatalError("The Elements List is empty")
        }
        var ll: LinkedList = empty_list()
        ll = (extend_list((ll as! LinkedList), (items as! [Int])) as! LinkedList)
        return ll
    }
    func in_reverse(_ list: LinkedList) -> String {
        if (_int(((list.data).count)) == 0) {
            return ""
        }
        var i: Int = (_int(((list.data).count)) &- 1)
        var s: String = _p((_idx(list.data, i) as? Int ?? 0))
        i = _int((i &- 1))
        while (i >= 0) {
            s = (((s + " <- ") + _p((_idx(list.data, i) as? Int ?? 0))) as! String)
            i = _int((i &- 1))
        }
        return s
    }
    func main() {
        let linked_list = make_linked_list(([14, 52, 14, 12, 43] as! [Int]))
        print(_p(("Linked List:  " + to_string((linked_list as! LinkedList)))))
        print(_p(("Reverse List: " + String(describing: in_reverse((linked_list as! LinkedList))))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
