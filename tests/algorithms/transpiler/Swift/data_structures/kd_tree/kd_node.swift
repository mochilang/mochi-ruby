// Generated by Mochi transpiler v0.10.59 on 2025-08-07 00:10:14 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct KDNode: Codable {
    var point: [Double?]
    var left: Int
    var right: Int
    init() {
        self.point = []
        self.left = 0
        self.right = 0
    }
    init(point: [Double?], left: Int, right: Int) {
        self.point = point
        self.left = left
        self.right = right
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func make_kd_node(_ point: [Double?], _ left: Int, _ right: Int) -> KDNode {
        return KDNode(point: point, left: left, right: right)
    }
    var nodes: [KDNode?] = ([] as! [KDNode?])
    nodes = (_append(nodes, make_kd_node(([2.0, 3.0] as! [Double?]), 1, 2)) as! [KDNode?])
    nodes = (_append(nodes, make_kd_node(([1.0, 5.0] as! [Double?]), -1, -1)) as! [KDNode?])
    nodes = (_append(nodes, make_kd_node(([4.0, 2.0] as! [Double?]), -1, -1)) as! [KDNode?])
    let root = (nodes[0] as! KDNode)
    let left_child = (nodes[1] as! KDNode)
    let right_child = (nodes[2] as! KDNode)
    print(_p("[" + root.point.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    print(_p(String(describing: root.left)))
    print(_p(String(describing: root.right)))
    print(_p("[" + left_child.point.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    print(_p("[" + right_child.point.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
