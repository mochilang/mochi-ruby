// Generated by Mochi transpiler v0.10.72 on 2025-08-24 17:05:18 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    if let b = v as? BigInt { return b.toInt() }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
struct BigInt: Comparable, CustomStringConvertible {
    private var digits: [UInt32] = []
    init() {}
    init(_ v: Int) {
        var n = v
        while n > 0 { digits.append(UInt32(n % 1000000000)); n /= 1000000000 }
    }
    init(_ other: BigInt) { digits = other.digits }
    func toInt() -> Int {
        var res: Int64 = 0
        for d in digits.reversed() {
            if res > (Int64.max - Int64(d)) / 1000000000 {
                return Int.max
            }
            res = res * 1000000000 + Int64(d)
        }
        return Int(res)
    }
    static func +(lhs: BigInt, rhs: BigInt) -> BigInt {
        var carry: UInt64 = 0
        var result: [UInt32] = []
        let maxLen = max(lhs.digits.count, rhs.digits.count)
        for i in 0..<maxLen {
            let a = i < lhs.digits.count ? UInt64(lhs.digits[i]) : 0
            let b = i < rhs.digits.count ? UInt64(rhs.digits[i]) : 0
            let sum = a + b + carry
            result.append(UInt32(sum % 1000000000))
            carry = sum / 1000000000
        }
        if carry > 0 { result.append(UInt32(carry)) }
        var r = BigInt(); r.digits = result; return r
    }
    static func -(lhs: BigInt, rhs: BigInt) -> BigInt {
        var borrow: Int64 = 0
        var result: [UInt32] = []
        for i in 0..<lhs.digits.count {
            var a = Int64(lhs.digits[i]) - borrow
            let b = i < rhs.digits.count ? Int64(rhs.digits[i]) : 0
            var diff = a - b
            if diff < 0 { diff += 1000000000; borrow = 1 } else { borrow = 0 }
            result.append(UInt32(diff))
        }
        while result.last == 0 { result.removeLast(); if result.isEmpty { break } }
        var r = BigInt(); r.digits = result; return r
    }
    static func *(lhs: BigInt, rhs: BigInt) -> BigInt {
        if lhs.digits.isEmpty || rhs.digits.isEmpty { return BigInt() }
        var result = Array(repeating: UInt64(0), count: lhs.digits.count + rhs.digits.count)
        for i in 0..<lhs.digits.count {
            var carry: UInt64 = 0
            for j in 0..<rhs.digits.count {
                let idx = i + j
                let prod = UInt64(lhs.digits[i]) * UInt64(rhs.digits[j]) + result[idx] + carry
                result[idx] = prod % 1000000000
                carry = prod / 1000000000
            }
            if carry > 0 { result[i + rhs.digits.count] += carry }
        }
        var resDigits: [UInt32] = result.map { UInt32($0) }
        while resDigits.last == 0 { resDigits.removeLast(); if resDigits.isEmpty { break } }
        var r = BigInt(); r.digits = resDigits; return r
    }
    func bitLength() -> Int {
        if digits.isEmpty { return 0 }
        var v = digits.last!
        var bits = 0
        while v > 0 { v >>= 1; bits += 1 }
        return bits + (digits.count - 1) * 30
    }
    static func <(lhs: BigInt, rhs: BigInt) -> Bool {
        if lhs.digits.count != rhs.digits.count { return lhs.digits.count < rhs.digits.count }
        for i in stride(from: lhs.digits.count-1, through: 0, by: -1) {
            if lhs.digits[i] != rhs.digits[i] { return lhs.digits[i] < rhs.digits[i] }
        }
        return false
    }
    static func >(lhs: BigInt, rhs: BigInt) -> Bool { return rhs < lhs }
    static func <=(lhs: BigInt, rhs: BigInt) -> Bool { return !(rhs < lhs) }
    static func >=(lhs: BigInt, rhs: BigInt) -> Bool { return !(lhs < rhs) }
    static func /(lhs: BigInt, rhs: BigInt) -> BigInt {
        let divisor = rhs.toInt()
        if divisor == 0 { return BigInt() }
        var result: [UInt32] = Array(repeating: 0, count: lhs.digits.count)
        var rem: Int64 = 0
        for i in stride(from: lhs.digits.count - 1, through: 0, by: -1) {
            let cur = rem * 1000000000 + Int64(lhs.digits[i])
            result[i] = UInt32(cur / Int64(divisor))
            rem = cur % Int64(divisor)
        }
        var r = BigInt(); r.digits = result
        while r.digits.last == 0 { r.digits.removeLast(); if r.digits.isEmpty { break } }
        return r
    }
    static func %(lhs: BigInt, rhs: BigInt) -> BigInt {
        let divisor = rhs.toInt()
        if divisor == 0 { return BigInt() }
        var rem: Int64 = 0
        for d in lhs.digits.reversed() {
            rem = (rem * 1000000000 + Int64(d)) % Int64(divisor)
        }
        return BigInt(Int(rem))
    }
    static func +(lhs: BigInt, rhs: Int) -> BigInt { return lhs + BigInt(rhs) }
    static func +(lhs: Int, rhs: BigInt) -> BigInt { return BigInt(lhs) + rhs }
    static func ==(lhs: BigInt, rhs: BigInt) -> Bool { lhs.digits == rhs.digits }
    var description: String {
        if digits.isEmpty { return "0" }
        var s = String(digits.last!)
        for d in digits.dropLast().reversed() { s += String(format: "%09d", d) }
        return s
    }
}
extension Int { init(_ b: BigInt) { self = b.toInt() } }
extension Double { init(_ b: BigInt) { self = Double(b.toInt()) } }
struct Tree: Codable {
    var values: [Int]
    var lefts: [Int]
    var rights: [Int]
    var root: Int
    init() {
        self.values = []
        self.lefts = []
        self.rights = []
        self.root = 0
    }
    init(values: [Int], lefts: [Int], rights: [Int], root: Int) {
        self.values = values
        self.lefts = lefts
        self.rights = rights
        self.root = root
    }
}
struct Pair: Codable {
    var idx: Int
    var hd: Int
    init() {
        self.idx = 0
        self.hd = 0
    }
    init(idx: Int, hd: Int) {
        self.idx = idx
        self.hd = hd
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let NIL = (0 &- 1)
    func make_tree() -> Tree {
        return Tree(values: ({
            var _arr: [Int] = []
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 9)
            _arr = _append(_arr, 20)
            _arr = _append(_arr, 15)
            _arr = _append(_arr, 7)
            return _arr
        }() as! [Int]), lefts: ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, NIL)
            _arr = _append(_arr, 3)
            _arr = _append(_arr, NIL)
            _arr = _append(_arr, NIL)
            return _arr
        }() as! [Int]), rights: ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, NIL)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, NIL)
            _arr = _append(_arr, NIL)
            return _arr
        }() as! [Int]), root: 0)
    }
    func index_of(_ xs: [Int], _ x: Int) -> Int {
        var i = 0
        while ((i < _int(((xs).count)))) {
            if (((_idx(xs, i) as? Int ?? 0) == x)) {
                return i
            }
            i = _int((i &+ 1))
        }
        return NIL
    }
    func sort_pairs(_ hds: inout [Int], _ vals: inout [Int]) {
        var i = 0
        while ((i < _int(((hds).count)))) {
            var j = i
            while (((j > 0) && ((_idx(hds, (j &- 1)) as? Int ?? 0) > (_idx(hds, j) as? Int ?? 0)))) {
                let hd_tmp = (_idx(hds, (j &- 1)) as? Int ?? 0)
                hds = _set(hds, (j &- 1), { let _tmp0 = (_idx(hds, j) as? Int ?? 0); return _tmp0 }())
                hds = _set(hds, j, { let _tmp1 = Int(hd_tmp); return _tmp1 }())
                let val_tmp = (_idx(vals, (j &- 1)) as? Int ?? 0)
                vals = _set(vals, (j &- 1), { let _tmp2 = (_idx(vals, j) as? Int ?? 0); return _tmp2 }())
                vals = _set(vals, j, { let _tmp3 = Int(val_tmp); return _tmp3 }())
                j = _int((j &- 1))
            }
            i = _int((i &+ 1))
        }
    }
    func right_view(_ t: Tree) -> [Int] {
        var res: [Int] = ([] as! [Int])
        var queue: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, t.root)
            return _arr
        }() as! [Int])
        while ((_int(((queue).count)) > 0)) {
            let size = _int(((queue).count))
            var i = 0
            while ((i < size)) {
                let idx = (_idx(queue, i) as? Int ?? 0)
                if (((_idx(t.lefts, idx) as? Int ?? 0) != NIL)) {
                    queue = (_append(queue, (_idx(t.lefts, idx) as? Int ?? 0)) as! [Int])
                }
                if (((_idx(t.rights, idx) as? Int ?? 0) != NIL)) {
                    queue = (_append(queue, (_idx(t.rights, idx) as? Int ?? 0)) as! [Int])
                }
                i = _int((i &+ 1))
            }
            res = (_append(res, (_idx(t.values, (_idx(queue, (size &- 1)) as? Int ?? 0)) as? Int ?? 0)) as! [Int])
            queue = (_slice(Array(queue), size, _int(((queue).count))) as! [Int])
        }
        return res
    }
    func left_view(_ t: Tree) -> [Int] {
        var res: [Int] = ([] as! [Int])
        var queue: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, t.root)
            return _arr
        }() as! [Int])
        while ((_int(((queue).count)) > 0)) {
            let size = _int(((queue).count))
            var i = 0
            while ((i < size)) {
                let idx = (_idx(queue, i) as? Int ?? 0)
                if (((_idx(t.lefts, idx) as? Int ?? 0) != NIL)) {
                    queue = (_append(queue, (_idx(t.lefts, idx) as? Int ?? 0)) as! [Int])
                }
                if (((_idx(t.rights, idx) as? Int ?? 0) != NIL)) {
                    queue = (_append(queue, (_idx(t.rights, idx) as? Int ?? 0)) as! [Int])
                }
                i = _int((i &+ 1))
            }
            res = (_append(res, (_idx(t.values, (_idx(queue, 0) as? Int ?? 0)) as? Int ?? 0)) as! [Int])
            queue = (_slice(Array(queue), size, _int(((queue).count))) as! [Int])
        }
        return res
    }
    func top_view(_ t: Tree) -> [Int] {
        var hds: [Int] = ([] as! [Int])
        var vals: [Int] = ([] as! [Int])
        var queue_idx: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, t.root)
            return _arr
        }() as! [Int])
        var queue_hd: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int])
        while ((_int(((queue_idx).count)) > 0)) {
            let idx = (_idx(queue_idx, 0) as? Int ?? 0)
            queue_idx = (_slice(Array(queue_idx), 1, _int(((queue_idx).count))) as! [Int])
            let hd = (_idx(queue_hd, 0) as? Int ?? 0)
            queue_hd = (_slice(Array(queue_hd), 1, _int(((queue_hd).count))) as! [Int])
            if ((_int(index_of((hds as! [Int]), hd)) == NIL)) {
                hds = (_append(hds, hd) as! [Int])
                vals = (_append(vals, (_idx(t.values, idx) as? Int ?? 0)) as! [Int])
            }
            if (((_idx(t.lefts, idx) as? Int ?? 0) != NIL)) {
                queue_idx = (_append(queue_idx, (_idx(t.lefts, idx) as? Int ?? 0)) as! [Int])
                queue_hd = (_append(queue_hd, (hd &- 1)) as! [Int])
            }
            if (((_idx(t.rights, idx) as? Int ?? 0) != NIL)) {
                queue_idx = (_append(queue_idx, (_idx(t.rights, idx) as? Int ?? 0)) as! [Int])
                queue_hd = (_append(queue_hd, (hd &+ 1)) as! [Int])
            }
        }
        _ = sort_pairs(&hds, &vals)
        return vals
    }
    func bottom_view(_ t: Tree) -> [Int] {
        var hds: [Int] = ([] as! [Int])
        var vals: [Int] = ([] as! [Int])
        var queue_idx: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, t.root)
            return _arr
        }() as! [Int])
        var queue_hd: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int])
        while ((_int(((queue_idx).count)) > 0)) {
            let idx = (_idx(queue_idx, 0) as? Int ?? 0)
            queue_idx = (_slice(Array(queue_idx), 1, _int(((queue_idx).count))) as! [Int])
            let hd = (_idx(queue_hd, 0) as? Int ?? 0)
            queue_hd = (_slice(Array(queue_hd), 1, _int(((queue_hd).count))) as! [Int])
            let pos = _int(index_of((hds as! [Int]), hd))
            if ((pos == NIL)) {
                hds = (_append(hds, hd) as! [Int])
                vals = (_append(vals, (_idx(t.values, idx) as? Int ?? 0)) as! [Int])
            } else {
                vals = _set(vals, pos, { let _tmp4 = (_idx(t.values, idx) as? Int ?? 0); return _tmp4 }())
            }
            if (((_idx(t.lefts, idx) as? Int ?? 0) != NIL)) {
                queue_idx = (_append(queue_idx, (_idx(t.lefts, idx) as? Int ?? 0)) as! [Int])
                queue_hd = (_append(queue_hd, (hd &- 1)) as! [Int])
            }
            if (((_idx(t.rights, idx) as? Int ?? 0) != NIL)) {
                queue_idx = (_append(queue_idx, (_idx(t.rights, idx) as? Int ?? 0)) as! [Int])
                queue_hd = (_append(queue_hd, (hd &+ 1)) as! [Int])
            }
        }
        _ = sort_pairs(&hds, &vals)
        return vals
    }
    let tree = make_tree()
    print(_p("[" + (right_view((tree as! Tree)) as! [Int]).map{ if let s = $0 as? String { "\"" + s.replacingOccurrences(of: "\"", with: "\\\"") + "\"" } else { _p($0) } }.joined(separator: ", ") + "]"))
    print(_p("[" + (left_view((tree as! Tree)) as! [Int]).map{ if let s = $0 as? String { "\"" + s.replacingOccurrences(of: "\"", with: "\\\"") + "\"" } else { _p($0) } }.joined(separator: ", ") + "]"))
    print(_p("[" + (top_view((tree as! Tree)) as! [Int]).map{ if let s = $0 as? String { "\"" + s.replacingOccurrences(of: "\"", with: "\\\"") + "\"" } else { _p($0) } }.joined(separator: ", ") + "]"))
    print(_p("[" + (bottom_view((tree as! Tree)) as! [Int]).map{ if let s = $0 as? String { "\"" + s.replacingOccurrences(of: "\"", with: "\\\"") + "\"" } else { _p($0) } }.joined(separator: ", ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
