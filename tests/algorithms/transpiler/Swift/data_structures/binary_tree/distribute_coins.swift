// Generated by Mochi transpiler v0.10.72 on 2025-08-24 17:05:21 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct TreeNode: Codable {
    var data: Int
    var left: Int
    var right: Int
    init() {
        self.data = 0
        self.left = 0
        self.right = 0
    }
    init(data: Int, left: Int, right: Int) {
        self.data = data
        self.left = left
        self.right = right
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func count_nodes(_ nodes: [TreeNode], _ idx: Int) -> Int {
        if ((idx == 0)) {
            return 0
        }
        let node: TreeNode = (_idx(nodes, idx) as! TreeNode)
        return ((_int(count_nodes((nodes as! [TreeNode]), node.left)) &+ _int(count_nodes((nodes as! [TreeNode]), node.right))) &+ 1)
    }
    func count_coins(_ nodes: [TreeNode], _ idx: Int) -> Int {
        if ((idx == 0)) {
            return 0
        }
        let node: TreeNode = (_idx(nodes, idx) as! TreeNode)
        return ((_int(count_coins((nodes as! [TreeNode]), node.left)) &+ _int(count_coins((nodes as! [TreeNode]), node.right))) &+ node.data)
    }
    var total_moves: Int = 0
    func iabs(_ x: Int) -> Int {
        if ((x < 0)) {
            return -x
        }
        return x
    }
    func dfs(_ nodes: [TreeNode], _ idx: Int) -> Int {
        if ((idx == 0)) {
            return 0
        }
        let node: TreeNode = (_idx(nodes, idx) as! TreeNode)
        let left_excess: Int = _int(dfs((nodes as! [TreeNode]), node.left))
        let right_excess: Int = _int(dfs((nodes as! [TreeNode]), node.right))
        let abs_left: Int = _int(iabs(left_excess))
        let abs_right: Int = _int(iabs(right_excess))
        total_moves = _int(((total_moves &+ abs_left) &+ abs_right))
        return (((node.data &+ left_excess) &+ right_excess) &- 1)
    }
    func distribute_coins(_ nodes: [TreeNode], _ root: Int) -> Int {
        if ((root == 0)) {
            return 0
        }
        if ((_int(count_nodes((nodes as! [TreeNode]), root)) != _int(count_coins((nodes as! [TreeNode]), root)))) {
            _ = fatalError("The nodes number should be same as the number of coins")
        }
        total_moves = 0
        _ = _int(dfs((nodes as! [TreeNode]), root))
        return total_moves
    }
    func main() {
        let example1: [TreeNode] = ({
            var _arr: [TreeNode] = []
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            _arr = _append(_arr, TreeNode(data: 3, left: 2, right: 3))
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            return _arr
        }() as! [TreeNode])
        let example2: [TreeNode] = ({
            var _arr: [TreeNode] = []
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            _arr = _append(_arr, TreeNode(data: 0, left: 2, right: 3))
            _arr = _append(_arr, TreeNode(data: 3, left: 0, right: 0))
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            return _arr
        }() as! [TreeNode])
        let example3: [TreeNode] = ({
            var _arr: [TreeNode] = []
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            _arr = _append(_arr, TreeNode(data: 0, left: 2, right: 3))
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            _arr = _append(_arr, TreeNode(data: 3, left: 0, right: 0))
            return _arr
        }() as! [TreeNode])
        print(_p(_int(distribute_coins((example1 as! [TreeNode]), 1))))
        print(_p(_int(distribute_coins((example2 as! [TreeNode]), 1))))
        print(_p(_int(distribute_coins((example3 as! [TreeNode]), 1))))
        print(_p(_int(distribute_coins(({
            var _arr: [TreeNode] = []
            _arr = _append(_arr, TreeNode(data: 0, left: 0, right: 0))
            return _arr
        }() as! [TreeNode]), 0))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
