// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:38:41 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
struct Node: Codable {
    var value: Int
    var left: Int
    var right: Int
    init() {
        self.value = 0
        self.left = 0
        self.right = 0
    }
    init(value: Int, left: Int, right: Int) {
        self.value = value
        self.left = left
        self.right = right
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func tree_sum(_ nodes: [Node], _ idx: Int) -> Int {
        if (idx == Int(-1)) {
            return 0
        }
        let node = (nodes[idx] as! Node)
        return ((node.value &+ Int(tree_sum((nodes as! [Node]), node.left))) &+ Int(tree_sum((nodes as! [Node]), node.right)))
    }
    func is_sum_node(_ nodes: [Node], _ idx: Int) -> Bool {
        let node = (nodes[idx] as! Node)
        if ((node.left == Int(-1)) && (node.right == Int(-1))) {
            return true
        }
        let left_sum = Int(tree_sum((nodes as! [Node]), node.left))
        let right_sum = Int(tree_sum((nodes as! [Node]), node.right))
        if (node.value != (left_sum &+ right_sum)) {
            return false
        }
        var left_ok: Bool = true
        if (node.left != Int(-1)) {
            left_ok = is_sum_node((nodes as! [Node]), node.left)
        }
        var right_ok: Bool = true
        if (node.right != Int(-1)) {
            right_ok = is_sum_node((nodes as! [Node]), node.right)
        }
        return (left_ok && right_ok)
    }
    func build_a_tree() -> [Node] {
        return ([Node(value: 11, left: 1, right: 2), Node(value: 2, left: 3, right: 4), Node(value: 29, left: 5, right: 6), Node(value: 1, left: Int(-1), right: Int(-1)), Node(value: 7, left: Int(-1), right: Int(-1)), Node(value: 15, left: Int(-1), right: Int(-1)), Node(value: 40, left: 7, right: Int(-1)), Node(value: 35, left: Int(-1), right: Int(-1))] as! [Node])
    }
    func build_a_sum_tree() -> [Node] {
        return ([Node(value: 26, left: 1, right: 2), Node(value: 10, left: 3, right: 4), Node(value: 3, left: Int(-1), right: 5), Node(value: 4, left: Int(-1), right: Int(-1)), Node(value: 6, left: Int(-1), right: Int(-1)), Node(value: 3, left: Int(-1), right: Int(-1))] as! [Node])
    }
    do {
        let tree: [Node] = (build_a_tree() as! [Node])
        assert((is_sum_node((tree as! [Node]), 0) == false))
    }
    do {
        let tree: [Node] = (build_a_sum_tree() as! [Node])
        assert((is_sum_node((tree as! [Node]), 0) == true))
    }
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
