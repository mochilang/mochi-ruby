// Generated by Mochi transpiler v0.10.59 on 2025-08-06 22:37:32 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func equilibrium_index(_ arr: [Int]) -> Int {
        var total: Int = 0
        var i: Int = 0
        while (i < Int(((arr).count))) {
            total = Int((total &+ (arr[i] as? Int ?? 0)))
            i = Int((i &+ 1))
        }
        var left: Int = 0
        i = 0
        while (i < Int(((arr).count))) {
            total = Int((total &- (arr[i] as? Int ?? 0)))
            if (left == total) {
                return i
            }
            left = Int((left &+ (arr[i] as? Int ?? 0)))
            i = Int((i &+ 1))
        }
        return -1
    }
    var arr1: [Int] = ([-7, 1, 5, 2, -4, 3, 0] as! [Int])
    print(_p(Int(equilibrium_index((arr1 as! [Int])))))
    var arr2: [Int] = ([1, 2, 3, 4, 5] as! [Int])
    print(_p(Int(equilibrium_index((arr2 as! [Int])))))
    var arr3: [Int] = ([1, 1, 1, 1, 1] as! [Int])
    print(_p(Int(equilibrium_index((arr3 as! [Int])))))
    var arr4: [Int] = ([2, 4, 6, 8, 10, 3] as! [Int])
    print(_p(Int(equilibrium_index((arr4 as! [Int])))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
