// Generated by Mochi transpiler v0.10.70 on 2025-08-23 13:53:16 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let lower = "abcdefghijklmnopqrstuvwxyz"
    let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    func index_of(_ s: String, _ ch: String) -> Int {
        var i = 0
        while ((i < _int(((s).count)))) {
            if (((String(Array(s)[i]) as! String) == ch)) {
                return i
            }
            i = _int((i &+ 1))
        }
        return -1
    }
    func to_title_case(_ word: String) -> String {
        if ((_int(((word).count)) == 0)) {
            return ""
        }
        let first = String(_slice(Array(word), 0, 1))
        let idx = _int(index_of((lower as! String), (first as! String)))
        var result = String(describing: ((idx >= 0) ? String(_slice(Array(upper), idx, (idx &+ 1))) : first))
        var i = 1
        while ((i < _int(((word).count)))) {
            let ch = String(_slice(Array(word), i, (i &+ 1)))
            let uidx = _int(index_of((upper as! String), (ch as! String)))
            if ((uidx >= 0)) {
                result = ((_p(result) + _p(String(_slice(Array(lower), uidx, (uidx &+ 1))))) as! String)
            } else {
                result = ((_p(result) + _p(ch)) as! String)
            }
            i = _int((i &+ 1))
        }
        return result
    }
    func split_words(_ s: String) -> [String] {
        var words: [String] = ([] as! [String])
        var current: String = ""
        var i = 0
        while ((i < _int(((s).count)))) {
            let ch = (String(Array(s)[i]) as! String)
            if ((ch == " ")) {
                if ((_int(((current).count)) > 0)) {
                    words = (_append(words, current) as! [String])
                    current = ""
                }
            } else {
                current = ((_p(current) + _p(ch)) as! String)
            }
            i = _int((i &+ 1))
        }
        if ((_int(((current).count)) > 0)) {
            words = (_append(words, current) as! [String])
        }
        return words
    }
    func sentence_to_title_case(_ sentence: String) -> String {
        let words: [String] = (split_words((sentence as! String)) as! [String])
        var res: String = ""
        var i = 0
        while ((i < _int(((words).count)))) {
            res = ((_p(res) + _p(String(describing: to_title_case((_idx(words, i) as! String))))) as! String)
            if (((i &+ 1) < _int(((words).count)))) {
                res = ((_p(res) + _p(" ")) as! String)
            }
            i = _int((i &+ 1))
        }
        return res
    }
    print(_p(String(describing: to_title_case("Aakash"))))
    print(_p(String(describing: to_title_case("aakash"))))
    print(_p(String(describing: to_title_case("AAKASH"))))
    print(_p(String(describing: to_title_case("aAkAsH"))))
    print(_p(String(describing: sentence_to_title_case("Aakash Giri"))))
    print(_p(String(describing: sentence_to_title_case("aakash giri"))))
    print(_p(String(describing: sentence_to_title_case("AAKASH GIRI"))))
    print(_p(String(describing: sentence_to_title_case("aAkAsH gIrI"))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
