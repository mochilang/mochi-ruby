// Generated by Mochi transpiler v0.10.70 on 2025-08-23 13:53:55 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
let _dataDir = "/workspace/mochi/tests/github/TheAlgorithms/Mochi/strings"
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _read_file(_ path: String) -> String {
    let fm = FileManager.default
    var p = path
    if !fm.fileExists(atPath: p) {
        p = URL(fileURLWithPath: _dataDir).appendingPathComponent(path).path
    }
    return (try? String(contentsOfFile: p)) ?? ""
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _split(_ s: String, _ sep: String) -> [String] {
    let d = sep.isEmpty ? " " : sep
    return s.components(separatedBy: d)
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    
    func split(_ s: String, _ sep: String) -> [String] {
        var res: [String] = ([] as! [String])
        var current = ""
        var i = 0
        while ((i < _int(((s).count)))) {
            let ch = String(_slice(Array(s), i, (i &+ 1)))
            if ((ch == sep)) {
                res = (_append(res, current) as! [String])
                current = ""
            } else {
                current = ((_p(current) + _p(ch)) as! String)
            }
            i = _int((i &+ 1))
        }
        res = (_append(res, current) as! [String])
        return res
    }
    func insertion_sort(_ arr: [String]) -> [String] {
        var a = arr
        var i = 1
        while ((i < _int(((a).count)))) {
            let key = (_idx(a, i) as! String)
            var j = (i &- 1)
            while (((j >= 0) && ((_idx(a, j) as! String) > key))) {
                a = _set(a, (j &+ 1), { let _tmp0 = (_idx(a, j) as! String); return _tmp0 }())
                j = _int((j &- 1))
            }
            a = _set(a, (j &+ 1), { let _tmp1 = (key as! String); return _tmp1 }())
            i = _int((i &+ 1))
        }
        return a
    }
    func sort_chars(_ word: String) -> String {
        var chars: [String] = ([] as! [String])
        var i = 0
        while ((i < _int(((word).count)))) {
            chars = (_append(chars, String(_slice(Array(word), i, (i &+ 1)))) as! [String])
            i = _int((i &+ 1))
        }
        chars = (insertion_sort((chars as! [String])) as! [String])
        var res = ""
        i = 0
        while ((i < _int(((chars).count)))) {
            res = ((_p(res) + _p((_idx(chars, i) as! String))) as! String)
            i = _int((i &+ 1))
        }
        return res
    }
    func unique_sorted(_ words: [String]) -> [String] {
        var seen: [String: Bool] = ([:] as! [String: Bool])
        var res: [String] = ([] as! [String])
        for w in words {
            if (((w != "") && (!((seen[w] != nil)) as! Bool))) {
                res = (_append(res, w) as! [String])
                seen = _set(seen, String(describing: w), { let _tmp2 = true; return _tmp2 }())
            }
        }
        res = (insertion_sort((res as! [String])) as! [String])
        return res
    }
    var word_by_signature: [String: [String]] = ([:] as! [String: [String]])
    func build_map(_ words: [String]) {
        for w in words {
            let sig = String(describing: sort_chars((w as! String)))
            var arr: [String] = ([] as! [String])
            if ((word_by_signature[sig] != nil)) {
                arr = ((word_by_signature[String(String(describing: sig))] ?? []) as! [String])
            }
            arr = (_append(arr, w) as! [String])
            word_by_signature = _set(word_by_signature, String(describing: sig), { let _tmp3 = (arr as! [String]); return _tmp3 }())
        }
    }
    func anagram(_ my_word: String) -> [String] {
        let sig = String(describing: sort_chars((my_word as! String)))
        if ((word_by_signature[sig] != nil)) {
            return ((word_by_signature[String(String(describing: sig))] ?? []) as! [String])
        }
        return ([] as! [String])
    }
    func main() {
        let text = String(describing: _read_file("words.txt"))
        let lines: [String] = (_split(text, "\n") as! [String])
        let words: [String] = (unique_sorted((lines as! [String])) as! [String])
        _ = build_map((words as! [String]))
        for w in words {
            let anas: [String] = (anagram((w as! String)) as! [String])
            if ((_int(((anas).count)) > 1)) {
                var line = (_p(w) + _p(":"))
                var i = 0
                while ((i < _int(((anas).count)))) {
                    if ((i > 0)) {
                        line = ((_p(line) + _p(",")) as! String)
                    }
                    line = ((_p(line) + _p((_idx(anas, i) as! String))) as! String)
                    i = _int((i &+ 1))
                }
                print(_p(line))
            }
        }
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
