// Generated by Mochi transpiler v0.10.70 on 2025-08-23 13:52:56 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let VOWELS = "aeiou"
    func strip(_ s: String) -> String {
        var start = 0
        var end = _int(((s).count))
        while (((start < end) && (String(describing: String(_slice(Array(s), start, (start &+ 1)))) == " "))) {
            start = _int((start &+ 1))
        }
        while (((end > start) && (String(describing: String(_slice(Array(s), (end &- 1), end))) == " "))) {
            end = _int((end &- 1))
        }
        return String(describing: String(_slice(Array(s), start, end)))
    }
    func is_vowel(_ c: String) -> Bool {
        var i = 0
        while ((i < _int(((VOWELS).count)))) {
            if ((c == String(describing: String(_slice(Array(VOWELS), i, (i &+ 1)))))) {
                return true
            }
            i = _int((i &+ 1))
        }
        return false
    }
    func pig_latin(_ word: String) -> String {
        let trimmed = String(describing: strip((word as! String)))
        if ((_int(((trimmed).count)) == 0)) {
            return ""
        }
        let w = String(describing: (trimmed.lowercased()))
        let first = String(describing: String(_slice(Array(w), 0, 1)))
        if (is_vowel((first as! String))) {
            return (_p(w) + _p("way"))
        }
        var i = 0
        while ((i < _int(((w).count)))) {
            let ch = String(describing: String(_slice(Array(w), i, (i &+ 1))))
            if (is_vowel((ch as! String))) {
                break
            }
            i = _int((i &+ 1))
        }
        return (_p((_p(String(describing: String(_slice(Array(w), i, _int(((w).count)))))) + _p(String(describing: String(_slice(Array(w), 0, i)))))) + _p("ay"))
    }
    print(_p((_p("pig_latin('friends') = ") + _p(String(describing: pig_latin("friends"))))))
    print(_p((_p("pig_latin('smile') = ") + _p(String(describing: pig_latin("smile"))))))
    print(_p((_p("pig_latin('eat') = ") + _p(String(describing: pig_latin("eat"))))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
