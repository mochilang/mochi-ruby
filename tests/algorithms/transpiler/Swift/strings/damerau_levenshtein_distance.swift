// Generated by Mochi transpiler v0.10.70 on 2025-08-23 13:51:32 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func damerau_levenshtein_distance(_ first_string: String, _ second_string: String) -> Int {
        let len1 = _int(((first_string).count))
        let len2 = _int(((second_string).count))
        var dp_matrix: [[Int]] = ([] as! [[Int]])
        for _ in 0..<_int((len1 &+ 1)) {
            var row: [Int] = ([] as! [Int])
            for _2 in 0..<_int((len2 &+ 1)) {
                row = (_append(row, 0) as! [Int])
            }
            dp_matrix = (_append(dp_matrix, row) as! [[Int]])
        }
        for i in 0..<_int((len1 &+ 1)) {
            var row = (_idx(dp_matrix, i) as! [Int])
            row = _set(row, 0, { let _tmp0 = Int(i); return _tmp0 }())
            dp_matrix = _set(dp_matrix, i, { let _tmp1 = (row as! [Int]); return _tmp1 }())
        }
        var first_row = (_idx(dp_matrix, 0) as! [Int])
        for j in 0..<_int((len2 &+ 1)) {
            first_row = _set(first_row, j, { let _tmp2 = Int(j); return _tmp2 }())
        }
        dp_matrix = _set(dp_matrix, 0, { let _tmp3 = (first_row as! [Int]); return _tmp3 }())
        for i in 1..<_int((len1 &+ 1)) {
            var row = (_idx(dp_matrix, i) as! [Int])
            let first_char = String(describing: String(_slice(Array(first_string), (i &- 1), i)))
            for j in 1..<_int((len2 &+ 1)) {
                let second_char = String(describing: String(_slice(Array(second_string), (j &- 1), j)))
                let cost = _int(((first_char == second_char) ? 0 : 1))
                var value = ((_idx(_idx(dp_matrix, (i &- 1)), j) as? Int ?? 0) &+ 1)
                let insertion = ((_idx(row, (j &- 1)) as? Int ?? 0) &+ 1)
                if ((insertion < value)) {
                    value = Int(insertion)
                }
                let substitution = ((_idx(_idx(dp_matrix, (i &- 1)), (j &- 1)) as? Int ?? 0) &+ cost)
                if ((substitution < value)) {
                    value = Int(substitution)
                }
                row = _set(row, j, { let _tmp4 = Int(value); return _tmp4 }())
                if ({ let _tmp6 = (((i > 1) && (j > 1)) && (String(describing: String(_slice(Array(first_string), (i &- 1), i))) == String(describing: String(_slice(Array(second_string), (j &- 2), (j &- 1)))))); let _tmp7 = (String(describing: String(_slice(Array(first_string), (i &- 2), (i &- 1)))) == String(describing: String(_slice(Array(second_string), (j &- 1), j)))); return _tmp6 && _tmp7}()) {
                    let transposition = ((_idx(_idx(dp_matrix, (i &- 2)), (j &- 2)) as? Int ?? 0) &+ cost)
                    if ((transposition < (_idx(row, j) as? Int ?? 0))) {
                        row = _set(row, j, { let _tmp7 = Int(transposition); return _tmp7 }())
                    }
                }
            }
            dp_matrix = _set(dp_matrix, i, { let _tmp8 = (row as! [Int]); return _tmp8 }())
        }
        return (_idx(_idx(dp_matrix, len1), len2) as? Int ?? 0)
    }
    print(_p(_p(_int(damerau_levenshtein_distance("cat", "cut")))))
    print(_p(_p(_int(damerau_levenshtein_distance("kitten", "sitting")))))
    print(_p(_p(_int(damerau_levenshtein_distance("hello", "world")))))
    print(_p(_p(_int(damerau_levenshtein_distance("book", "back")))))
    print(_p(_p(_int(damerau_levenshtein_distance("container", "containment")))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
