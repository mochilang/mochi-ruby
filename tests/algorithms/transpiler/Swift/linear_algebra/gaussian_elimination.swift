// Generated by Mochi transpiler v0.10.65 on 2025-08-14 16:24:11 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func retroactive_resolution(_ coefficients: [[Double]], _ vector: [[Double]]) -> [[Double]] {
        let rows = _int(((coefficients).count))
        var x: [[Double]] = ([] as! [[Double]])
        var i = 0
        while (i < rows) {
            var inner: [Double] = ([] as! [Double])
            inner = (_append(inner, 0.0) as! [Double])
            x = (_append(x, inner) as! [[Double]])
            i = _int((i &+ 1))
        }
        var r = (rows &- 1)
        while (r >= 0) {
            var total = 0.0
            var c = (r &+ 1)
            while (c < rows) {
                total = Double((total + (Double(_idx(_idx(coefficients, r)!, c)) * Double(_idx(_idx(x, c)!, 0)))))
                c = _int((c &+ 1))
            }
            var _tmp3 = (_idx(x, r) as! [Double])
            _tmp3 = _set(_tmp3, 0, Double((Double((Double(_idx(_idx(vector, r)!, 0)) - total)) / Double(_idx(_idx(coefficients, r)!, r)))))
            x = _set(x, r, (_tmp3 as! [Double]))
            r = _int((r &- 1))
        }
        return x
    }
    func gaussian_elimination(_ coefficients: [[Double]], _ vector: [[Double]]) -> [[Double]] {
        let rows = _int(((coefficients).count))
        let columns = _int((((_idx(coefficients, 0) as! [Double])).count))
        if (rows != columns) {
            return ([] as! [[Double]])
        }
        var augmented: [[Double]] = ([] as! [[Double]])
        var i = 0
        while (i < rows) {
            var row: [Double] = ([] as! [Double])
            var j = 0
            while (j < columns) {
                row = (_append(row, Double(_idx(_idx(coefficients, i)!, j))) as! [Double])
                j = _int((j &+ 1))
            }
            row = (_append(row, Double(_idx(_idx(vector, i)!, 0))) as! [Double])
            augmented = (_append(augmented, row) as! [[Double]])
            i = _int((i &+ 1))
        }
        var row_idx = 0
        while (row_idx < (rows &- 1)) {
            let pivot = Double(_idx(_idx(augmented, row_idx)!, row_idx))
            var col = (row_idx &+ 1)
            while (col < rows) {
                let factor = (Double(_idx(_idx(augmented, col)!, row_idx)) / pivot)
                var k = row_idx
                while (k < (columns &+ 1)) {
                    var _tmp4 = (_idx(augmented, col) as! [Double])
                    _tmp4 = _set(_tmp4, k, Double((Double(_idx(_idx(augmented, col)!, k)) - (factor * Double(_idx(_idx(augmented, row_idx)!, k))))))
                    augmented = _set(augmented, col, (_tmp4 as! [Double]))
                    k = _int((k &+ 1))
                }
                col = _int((col &+ 1))
            }
            row_idx = _int((row_idx &+ 1))
        }
        var coeffs: [[Double]] = ([] as! [[Double]])
        var vec: [[Double]] = ([] as! [[Double]])
        var r = 0
        while (r < rows) {
            var row: [Double] = ([] as! [Double])
            var c = 0
            while (c < columns) {
                row = (_append(row, Double(_idx(_idx(augmented, r)!, c))) as! [Double])
                c = _int((c &+ 1))
            }
            coeffs = (_append(coeffs, row) as! [[Double]])
            vec = (_append(vec, ({
                var _arr: [Double] = []
                _arr = _append(_arr, Double(_idx(_idx(augmented, r)!, columns)))
                return _arr
            }() as! [Double])) as! [[Double]])
            r = _int((r &+ 1))
        }
        let x: [[Double]] = (retroactive_resolution((coeffs as! [[Double]]), (vec as! [[Double]])) as! [[Double]])
        return x
    }
    print(_p("[" + (gaussian_elimination(({
        var _arr: [[Double]] = []
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 1.0)
            _arr = _append(_arr, -4.0)
            _arr = _append(_arr, -2.0)
            return _arr
        }() as! [Double]))
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 5.0)
            _arr = _append(_arr, 2.0)
            _arr = _append(_arr, -2.0)
            return _arr
        }() as! [Double]))
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 1.0)
            _arr = _append(_arr, -1.0)
            _arr = _append(_arr, 0.0)
            return _arr
        }() as! [Double]))
        return _arr
    }() as! [[Double]]), ({
        var _arr: [[Double]] = []
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, -2.0)
            return _arr
        }() as! [Double]))
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, -3.0)
            return _arr
        }() as! [Double]))
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 4.0)
            return _arr
        }() as! [Double]))
        return _arr
    }() as! [[Double]])) as! [[Double]]).map{ if let s = $0 as? String { "\"" + s.replacingOccurrences(of: "\"", with: "\\\"") + "\"" } else { _p($0) } }.joined(separator: ", ") + "]"))
    print(_p("[" + (gaussian_elimination(({
        var _arr: [[Double]] = []
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 1.0)
            _arr = _append(_arr, 2.0)
            return _arr
        }() as! [Double]))
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 5.0)
            _arr = _append(_arr, 2.0)
            return _arr
        }() as! [Double]))
        return _arr
    }() as! [[Double]]), ({
        var _arr: [[Double]] = []
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 5.0)
            return _arr
        }() as! [Double]))
        _arr = _append(_arr, ({
            var _arr: [Double] = []
            _arr = _append(_arr, 5.0)
            return _arr
        }() as! [Double]))
        return _arr
    }() as! [[Double]])) as! [[Double]]).map{ if let s = $0 as? String { "\"" + s.replacingOccurrences(of: "\"", with: "\\\"") + "\"" } else { _p($0) } }.joined(separator: ", ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
