// Generated by Mochi transpiler v0.10.59 on 2025-08-06 18:42:16 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func to_binary4(_ n: Int) -> String {
        var result: String = ""
        var x: Int = n
        while (x > 0) {
            result = ((String(describing: (x % 2)) + result) as! String)
            x = Int((x / 2))
        }
        while (Int(((result).count)) < 4) {
            result = (("0" + result) as! String)
        }
        return result
    }
    func binary_coded_decimal(_ number: Int) -> String {
        var n: Int = number
        if (n < 0) {
            n = 0
        }
        let digits = String(describing: n)
        var out: String = "0b"
        var i: Int = 0
        while (i < Int(((digits).count))) {
            let d = (String(Array(digits)[i]) as! String)
            let d_int = Int(d)!
            out = ((out + String(describing: to_binary4(d_int))) as! String)
            i = Int((i &+ 1))
        }
        return out
    }
    print(_p(String(describing: binary_coded_decimal(-2))))
    print(_p(String(describing: binary_coded_decimal(-1))))
    print(_p(String(describing: binary_coded_decimal(0))))
    print(_p(String(describing: binary_coded_decimal(3))))
    print(_p(String(describing: binary_coded_decimal(2))))
    print(_p(String(describing: binary_coded_decimal(12))))
    print(_p(String(describing: binary_coded_decimal(987))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
