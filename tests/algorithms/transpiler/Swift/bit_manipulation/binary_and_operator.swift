// Generated by Mochi transpiler v0.10.59 on 2025-08-06 18:41:35 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func to_binary(_ n: Int) -> String {
        if (n == 0) {
            return "0"
        }
        var num: Int = n
        var res: String = ""
        while (num > 0) {
            let bit = (num % 2)
            res = ((String(describing: bit) + res) as! String)
            num = Int((num / 2))
        }
        return res
    }
    func zfill(_ s: String, _ width: Int) -> String {
        var res: String = s
        var pad: Int = (width &- Int(((s).count)))
        while (pad > 0) {
            res = (("0" + res) as! String)
            pad = Int((pad &- 1))
        }
        return res
    }
    func binary_and(_ a: Int, _ b: Int) -> String {
        if ((a < 0) || (b < 0)) {
            _ = fatalError("the value of both inputs must be positive")
        }
        let a_bin = String(describing: to_binary(a))
        let b_bin = String(describing: to_binary(b))
        var max_len: Int = Int(((a_bin).count))
        if (Int(((b_bin).count)) > max_len) {
            max_len = Int(((b_bin).count))
        }
        let a_pad = String(describing: zfill((a_bin as! String), max_len))
        let b_pad = String(describing: zfill((b_bin as! String), max_len))
        var i: Int = 0
        var res: String = ""
        while (i < max_len) {
            if (((String(Array(a_pad)[i]) as! String) == "1") && ((String(Array(b_pad)[i]) as! String) == "1")) {
                res = ((res + "1") as! String)
            } else {
                res = ((res + "0") as! String)
            }
            i = Int((i &+ 1))
        }
        return ("0b" + res)
    }
    print(_p(String(describing: binary_and(25, 32))))
    print(_p(String(describing: binary_and(37, 50))))
    print(_p(String(describing: binary_and(21, 30))))
    print(_p(String(describing: binary_and(58, 73))))
    print(_p(String(describing: binary_and(0, 255))))
    print(_p(String(describing: binary_and(256, 256))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
