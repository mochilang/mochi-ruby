// Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:39:40 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func pow2(_ exp: Int) -> Int {
        var result: Int = 1
        var i: Int = 0
        while (i < exp) {
            result = Int((result &* 2))
            i = Int((i &+ 1))
        }
        return result
    }
    func gray_code(_ bit_count: Int) -> [Int] {
        if (bit_count == 0) {
            return ([0] as! [Int])
        }
        let prev: [Int] = (gray_code((bit_count &- 1)) as! [Int])
        let add_val = Int(pow2((bit_count &- 1)))
        var res: [Int] = ([] as! [Int])
        var i: Int = 0
        while (i < Int(((prev).count))) {
            res = (_append(res, (prev[i] as? Int ?? 0)) as! [Int])
            i = Int((i &+ 1))
        }
        var j: Int = (Int(((prev).count)) &- 1)
        while (j >= 0) {
            res = (_append(res, ((prev[j] as? Int ?? 0) &+ add_val)) as! [Int])
            j = Int((j &- 1))
        }
        return res
    }
    let seq2: [Int] = (gray_code(2) as! [Int])
    print(_p("[" + seq2.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let seq1: [Int] = (gray_code(1) as! [Int])
    print(_p("[" + seq1.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let seq3: [Int] = (gray_code(3) as! [Int])
    print(_p("[" + seq3.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
