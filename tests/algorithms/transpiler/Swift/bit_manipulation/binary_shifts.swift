// Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:38:37 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func repeat_char(_ ch: String, _ count: Int) -> String {
        var res: String = ""
        var i: Int = 0
        while (i < count) {
            res = ((res + ch) as! String)
            i = Int((i &+ 1))
        }
        return res
    }
    func abs_int(_ n: Int) -> Int {
        if (n < 0) {
            return -n
        }
        return n
    }
    func pow2(_ exp: Int) -> Int {
        var res: Int = 1
        var i: Int = 0
        while (i < exp) {
            res = Int((res &* 2))
            i = Int((i &+ 1))
        }
        return res
    }
    func to_binary_no_prefix(_ n: Int) -> String {
        var v: Int = n
        if (v < 0) {
            v = Int(-v)
        }
        if (v == 0) {
            return "0"
        }
        var res: String = ""
        while (v > 0) {
            res = ((String(describing: (v % 2)) + res) as! String)
            v = Int((v / 2))
        }
        return res
    }
    func logical_left_shift(_ number: Int, _ shift_amount: Int) -> String {
        if ((number < 0) || (shift_amount < 0)) {
            _ = fatalError("both inputs must be positive integers")
        }
        let binary_number = ("0b" + String(describing: to_binary_no_prefix(number)))
        return (binary_number + String(describing: repeat_char("0", shift_amount)))
    }
    func logical_right_shift(_ number: Int, _ shift_amount: Int) -> String {
        if ((number < 0) || (shift_amount < 0)) {
            _ = fatalError("both inputs must be positive integers")
        }
        let binary_number = String(describing: to_binary_no_prefix(number))
        if (shift_amount >= Int(((binary_number).count))) {
            return "0b0"
        }
        let shifted = String(describing: String(Array(String(describing: (binary_number as! String)))[0..<(Int(((binary_number).count)) &- shift_amount)]))
        return ("0b" + shifted)
    }
    func arithmetic_right_shift(_ number: Int, _ shift_amount: Int) -> String {
        var binary_number: String = ""
        if (number >= 0) {
            binary_number = (("0" + String(describing: to_binary_no_prefix(number))) as! String)
        } else {
            let length = Int(((String(describing: to_binary_no_prefix(-number))).count))
            let intermediate = (Int(abs_int(number)) &- Int(pow2(length)))
            let bin_repr = String(describing: to_binary_no_prefix(intermediate))
            binary_number = ((("1" + String(describing: repeat_char("0", (length &- Int(((bin_repr).count)))))) + bin_repr) as! String)
        }
        if (shift_amount >= Int(((binary_number).count))) {
            let sign = String(describing: String(Array(String(describing: (binary_number as! String)))[0..<1]))
            return ("0b" + String(describing: repeat_char((sign as! String), Int(((binary_number).count)))))
        }
        let sign = String(describing: String(Array(String(describing: (binary_number as! String)))[0..<1]))
        let shifted = String(describing: String(Array(String(describing: (binary_number as! String)))[0..<(Int(((binary_number).count)) &- shift_amount)]))
        return (("0b" + String(describing: repeat_char((sign as! String), shift_amount))) + shifted)
    }
    func main() {
        print(_p(String(describing: logical_left_shift(17, 2))))
        print(_p(String(describing: logical_right_shift(1983, 4))))
        print(_p(String(describing: arithmetic_right_shift(-17, 2))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
