// Generated by Mochi transpiler v0.10.64 on 2025-08-13 16:15:16 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let PI: Double = 3.141592653589793
    let EQUATORIAL_RADIUS: Double = 6378137.0
    func to_radians(_ deg: Double) -> Double {
        return ((deg * PI) / 180.0)
    }
    func sin_approx(_ x: Double) -> Double {
        var term = x
        var sum = x
        var i = 1
        while (i < 10) {
            let k1 = (2.0 * Double(i))
            let k2 = (k1 + 1.0)
            term = Double((((-term * x) * x) / Double((k1 * k2))))
            sum = Double((sum + term))
            i = _int((i &+ 1))
        }
        return sum
    }
    func cos_approx(_ x: Double) -> Double {
        var term = 1.0
        var sum = 1.0
        var i = 1
        while (i < 10) {
            let k1 = ((2.0 * Double(i)) - 1.0)
            let k2 = (2.0 * Double(i))
            term = Double((((-term * x) * x) / Double((k1 * k2))))
            sum = Double((sum + term))
            i = _int((i &+ 1))
        }
        return sum
    }
    func sqrt_approx(_ x: Double) -> Double {
        if (x <= 0.0) {
            return 0.0
        }
        var guess = (x / 2.0)
        var i = 0
        while (i < 20) {
            guess = Double((Double((guess + (x / guess))) / 2.0))
            i = _int((i &+ 1))
        }
        return guess
    }
    func lamberts_ellipsoidal_distance(_ lat1: Double, _ lon1: Double, _ lat2: Double, _ lon2: Double) -> Double {
        let phi1 = Double(to_radians(Double(lat1)))
        let phi2 = Double(to_radians(Double(lat2)))
        let lambda1 = Double(to_radians(Double(lon1)))
        let lambda2 = Double(to_radians(Double(lon2)))
        let x = (Double((lambda2 - lambda1)) * Double(cos_approx(Double((Double((phi1 + phi2)) / 2.0)))))
        let y = (phi2 - phi1)
        return (EQUATORIAL_RADIUS * Double(sqrt_approx(Double(((x * x) + (y * y))))))
    }
    print(_p(Double(lamberts_ellipsoidal_distance(37.774856, Double(-122.424227), 37.864742, Double(-119.537521)))))
    print(_p(Double(lamberts_ellipsoidal_distance(37.774856, Double(-122.424227), 40.713019, Double(-74.012647)))))
    print(_p(Double(lamberts_ellipsoidal_distance(37.774856, Double(-122.424227), 45.443012, 12.313071))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
