// Generated by Mochi transpiler v0.10.66 on 2025-08-14 18:35:03 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func is_prime(_ number: Int) -> Bool {
        if ((1 < number) && (number < 4)) {
            return true
        } else if (((number < 2) || (((number % 2 + 2) % 2) == 0)) || (((number % 3 + 3) % 3) == 0)) {
            return false
        }
        
        var i = 5
        while ((i &* i) <= number) {
            if ((((number % i + i) % i) == 0) || (((number % _int((i &+ 2)) + _int((i &+ 2))) % _int((i &+ 2))) == 0)) {
                return false
            }
            i = _int((i &+ 6))
        }
        return true
    }
    func remove_at(_ xs: [Int], _ index: Int) -> [Int] {
        var res: [Int] = ([] as! [Int])
        var i = 0
        while (i < _int(((xs).count))) {
            if (i != index) {
                res = (_append(res, (_idx(xs, i) as? Int ?? 0)) as! [Int])
            }
            i = _int((i &+ 1))
        }
        return res
    }
    func collect_primes(_ nums: [Int], _ current: Int, _ primes: inout [Int]) -> [Int] {
        if (_int(((nums).count)) == 0) {
            if is_prime(current) {
                primes = (_append(primes, current) as! [Int])
            }
            return primes
        }
        var i = 0
        var res = primes
        while (i < _int(((nums).count))) {
            let digit = (_idx(nums, i) as? Int ?? 0)
            let remaining: [Int] = (remove_at((nums as! [Int]), i) as! [Int])
            res = (collect_primes((remaining as! [Int]), ((current &* 10) &+ digit), &res) as! [Int])
            i = _int((i &+ 1))
        }
        return res
    }
    func max_list(_ nums: [Int]) -> Int {
        var m = 0
        var i = 0
        while (i < _int(((nums).count))) {
            if ((_idx(nums, i) as? Int ?? 0) > m) {
                m = (_idx(nums, i) as? Int ?? 0)
            }
            i = _int((i &+ 1))
        }
        return m
    }
    func solution(_ n: Int) -> Int {
        var digits: [Int] = ([] as! [Int])
        var i = 1
        while (i <= n) {
            digits = (_append(digits, i) as! [Int])
            i = _int((i &+ 1))
        }
        let primes: [Int] = ({ () -> Any in
        var _tmp2: [Int] = []
        return collect_primes((digits as! [Int]), 0, &_tmp2)
    }() as! [Int])
    if (_int(((primes).count)) == 0) {
        return 0
    }
    return _int(max_list((primes as! [Int])))
}
print(_p(("solution() = " + _p(_int(solution(7))))))
let _benchEnd = _now()
let _benchMemEnd = _mem()
print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
