// Generated by Mochi transpiler v0.10.66 on 2025-08-14 18:34:35 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func is_prime(_ number: Int) -> Bool {
        if ((1 < number) && (number < 4)) {
            return true
        }
        if (((number < 2) || (((number % 2 + 2) % 2) == 0)) || (((number % 3 + 3) % 3) == 0)) {
            return false
        }
        var i = 5
        while ((i &* i) <= number) {
            if ((((number % i + i) % i) == 0) || (((number % _int((i &+ 2)) + _int((i &+ 2))) % _int((i &+ 2))) == 0)) {
                return false
            }
            i = _int((i &+ 6))
        }
        return true
    }
    func list_truncated_nums(_ n: Int) -> [Int] {
        let str_num = _p(n)
        var list_nums: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, n)
            return _arr
        }() as! [Int])
        var i = 1
        let length = _int(((str_num).count))
        while (i < length) {
            let right = _int(_int(String(describing: String(Array(String(describing: (str_num as! String)))[i..<length]))))
            let left = _int(_int(String(describing: String(Array(String(describing: (str_num as! String)))[0..<(length &- i)]))))
            list_nums = (_append(list_nums, right) as! [Int])
            list_nums = (_append(list_nums, left) as! [Int])
            i = _int((i &+ 1))
        }
        return list_nums
    }
    func validate(_ n: Int) -> Bool {
        let s = _p(n)
        let length = _int(((s).count))
        if (length > 3) {
            let last3 = _int(_int(String(describing: String(Array(String(describing: (s as! String)))[(length &- 3)..<length]))))
            let first3 = _int(_int(String(describing: String(Array(String(describing: (s as! String)))[0..<3]))))
            if (!(is_prime(last3) && is_prime(first3))) {
                return false
            }
        }
        return true
    }
    func compute_truncated_primes(_ count: Int) -> [Int] {
        var list_truncated_primes: [Int] = ([] as! [Int])
        var num = 13
        while (_int(((list_truncated_primes).count)) != count) {
            if validate(num) {
                let list_nums: [Int] = (list_truncated_nums(num) as! [Int])
                var all_prime = true
                var j = 0
                while (j < _int(((list_nums).count))) {
                    if (!is_prime((_idx(list_nums, j) as? Int ?? 0))) {
                        all_prime = false
                        break
                    }
                    j = _int((j &+ 1))
                }
                if all_prime {
                    list_truncated_primes = (_append(list_truncated_primes, num) as! [Int])
                }
            }
            num = _int((num &+ 2))
        }
        return list_truncated_primes
    }
    func solution() -> Int {
        let primes: [Int] = (compute_truncated_primes(11) as! [Int])
        var total = 0
        var i = 0
        while (i < _int(((primes).count))) {
            total = _int((total &+ (_idx(primes, i) as? Int ?? 0)))
            i = _int((i &+ 1))
        }
        return total
    }
    print(_p(("sum(compute_truncated_primes(11)) = " + _p(_int(solution())))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
