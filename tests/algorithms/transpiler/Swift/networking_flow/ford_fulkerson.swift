// Generated by Mochi transpiler v0.10.73 on 2025-08-25 23:54:41 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    let INF: Int = 1000000000
    func breadth_first_search(_ graph: [[Int]], _ source: Int, _ sink: Int, _ parent: inout [Int]) -> Bool {
        var visited: [Bool] = ([] as! [Bool])
        var i = 0
        while ((i < _int(((graph).count)))) {
            visited = (_append(visited, false) as! [Bool])
            i = _int((i &+ 1))
        }
        var queue: [Int] = ([] as! [Int])
        queue = (_append(queue, source) as! [Int])
        visited = _set(visited, source, { let _tmp4 = true; return _tmp4 }())
        var head = 0
        while ((head < _int(((queue).count)))) {
            let u = (_idx(queue, head) as? Int ?? 0)
            head = _int((head &+ 1))
            let row: [Int] = (_idx(graph, u) as! [Int])
            var ind = 0
            while ((ind < _int(((row).count)))) {
                let capacity = (_idx(row, ind) as? Int ?? 0)
                if ((((_idx(visited, ind) as! Bool) == false) && (capacity > 0))) {
                    queue = (_append(queue, ind) as! [Int])
                    visited = _set(visited, ind, { let _tmp5 = true; return _tmp5 }())
                    parent = _set(parent, ind, { let _tmp6 = Int(u); return _tmp6 }())
                }
                ind = _int((ind &+ 1))
            }
        }
        return (_idx(visited, sink) as! Bool)
    }
    func ford_fulkerson(_ graph: inout [[Int]], _ source: Int, _ sink: Int) -> Int {
        var parent: [Int] = ([] as! [Int])
        var i = 0
        while ((i < _int(((graph).count)))) {
            parent = (_append(parent, -1) as! [Int])
            i = _int((i &+ 1))
        }
        var max_flow = 0
        while (breadth_first_search((graph as! [[Int]]), source, sink, &parent)) {
            var path_flow = INF
            var s = sink
            while ((s != source)) {
                let prev = (_idx(parent, s) as? Int ?? 0)
                let cap = (_idx(_idx(graph, prev), s) as? Int ?? 0)
                if ((cap < path_flow)) {
                    path_flow = Int(cap)
                }
                s = Int(prev)
            }
            max_flow = _int((max_flow &+ path_flow))
            var v = sink
            while ((v != source)) {
                let u = (_idx(parent, v) as? Int ?? 0)
                var _tmp3 = (_idx(graph, u) as! [Int])
                _tmp3 = _set(_tmp3, v, { let _tmp7 = _int(((_idx(_idx(graph, u), v) as? Int ?? 0) &- path_flow)); return _tmp7 }())
                graph = _set(graph, u, { let _tmp8 = (_tmp3 as! [Int]); return _tmp8 }())
                var _tmp4 = (_idx(graph, v) as! [Int])
                _tmp4 = _set(_tmp4, u, { let _tmp9 = _int(((_idx(_idx(graph, v), u) as? Int ?? 0) &+ path_flow)); return _tmp9 }())
                graph = _set(graph, v, { let _tmp10 = (_tmp4 as! [Int]); return _tmp10 }())
                v = Int(u)
            }
            var j = 0
            while ((j < _int(((parent).count)))) {
                parent = _set(parent, j, { let _tmp11 = _int(-1); return _tmp11 }())
                j = _int((j &+ 1))
            }
        }
        return max_flow
    }
    var graph: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 16)
            _arr = _append(_arr, 13)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 10)
            _arr = _append(_arr, 12)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 14)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 9)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 20)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 7)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 4)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            _arr = _append(_arr, 0)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    print(_p(_p(_int(ford_fulkerson(&graph, 0, 5)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
