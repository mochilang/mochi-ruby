// Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:25:26 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func calculate_waitingtime(_ arrival_time: [Int], _ burst_time: [Int], _ no_of_processes: Int) -> [Int] {
        var waiting_time: [Int] = []
        var remaining_time: [Int] = []
        var i = 0
        while (i < no_of_processes) {
            waiting_time = (_append(waiting_time, 0) as! [Int])
            remaining_time = (_append(remaining_time, (_idx(burst_time, i) as? Int ?? 0)) as! [Int])
            i = _int((i &+ 1))
        }
        var completed = 0
        var total_time = 0
        while (completed != no_of_processes) {
            var ready_process: [Int] = ([] as! [Int])
            var target_process = -1
            var j = 0
            while (j < no_of_processes) {
                if (((_idx(arrival_time, j) as? Int ?? 0) <= total_time) && ((_idx(remaining_time, j) as? Int ?? 0) > 0)) {
                    ready_process = (_append(ready_process, j) as! [Int])
                }
                j = _int((j &+ 1))
            }
            if (_int(((ready_process).count)) > 0) {
                target_process = (_idx(ready_process, 0) as? Int ?? 0)
                var k = 0
                while (k < _int(((ready_process).count))) {
                    let idx = (_idx(ready_process, k) as? Int ?? 0)
                    if ((_idx(remaining_time, idx) as? Int ?? 0) < (_idx(remaining_time, target_process) as? Int ?? 0)) {
                        target_process = Int(idx)
                    }
                    k = _int((k &+ 1))
                }
                total_time = _int((total_time &+ (_idx(burst_time, target_process) as? Int ?? 0)))
                completed = _int((completed &+ 1))
                remaining_time = _set(remaining_time, target_process, 0)
                waiting_time = _set(waiting_time, target_process, _int(((total_time &- (_idx(arrival_time, target_process) as? Int ?? 0)) &- (_idx(burst_time, target_process) as? Int ?? 0))))
            } else {
                total_time = _int((total_time &+ 1))
            }
        }
        return waiting_time
    }
    func calculate_turnaroundtime(_ burst_time: [Int], _ no_of_processes: Int, _ waiting_time: [Int]) -> [Int] {
        var turn_around_time: [Int] = []
        var i = 0
        while (i < no_of_processes) {
            turn_around_time = (_append(turn_around_time, ((_idx(burst_time, i) as? Int ?? 0) &+ (_idx(waiting_time, i) as? Int ?? 0))) as! [Int])
            i = _int((i &+ 1))
        }
        return turn_around_time
    }
    func average(_ values: [Int]) -> Double {
        var total = 0
        var i = 0
        while (i < _int(((values).count))) {
            total = _int((total &+ (_idx(values, i) as? Int ?? 0)))
            i = _int((i &+ 1))
        }
        return (Double(total) / Double(((values).count)))
    }
    print(_p("[TEST CASE 01]"))
    let no_of_processes = 4
    let burst_time: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 5)
        _arr = _append(_arr, 3)
        _arr = _append(_arr, 7)
        return _arr
    }() as! [Int])
    let arrival_time: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 0)
        _arr = _append(_arr, 0)
        return _arr
    }() as! [Int])
    var waiting_time: [Int] = (calculate_waitingtime((arrival_time as! [Int]), (burst_time as! [Int]), no_of_processes) as! [Int])
    var turn_around_time: [Int] = (calculate_turnaroundtime((burst_time as! [Int]), no_of_processes, (waiting_time as! [Int])) as! [Int])
    print(_p("PID\tBurst Time\tArrival Time\tWaiting Time\tTurnaround Time"))
    var i = 0
    while (i < no_of_processes) {
        let pid = (i &+ 1)
        print(_p(((((((((_p(pid) + "\t") + _p((_idx(burst_time, i) as? Int ?? 0))) + "\t\t\t") + _p((_idx(arrival_time, i) as? Int ?? 0))) + "\t\t\t\t") + _p((_idx(waiting_time, i) as? Int ?? 0))) + "\t\t\t\t") + _p((_idx(turn_around_time, i) as? Int ?? 0)))))
        i = _int((i &+ 1))
    }
    let avg_wait = Double(average((waiting_time as! [Int])))
    let avg_turn = Double(average((turn_around_time as! [Int])))
    print(_p(("\nAverage waiting time = " + _p(avg_wait))))
    print(_p(("Average turnaround time = " + _p(avg_turn))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
