// Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:48:08 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func abs_float(_ x: Double) -> Double {
        if ((x < 0.0)) {
            return -x
        }
        return x
    }
    func isclose(_ a: Double, _ b: Double, _ tolerance: Double) -> Bool {
        return (_num(abs_float(_num((a - b)))) < tolerance)
    }
    func focal_length(_ distance_of_object: Double, _ distance_of_image: Double) -> Double {
        if (((distance_of_object == 0.0) || (distance_of_image == 0.0))) {
            _ = fatalError("Invalid inputs. Enter non zero values with respect to the sign convention.")
        }
        return (1.0 / _num((_num((1.0 / distance_of_object)) + _num((1.0 / distance_of_image)))))
    }
    func object_distance(_ focal_length: Double, _ distance_of_image: Double) -> Double {
        if (((distance_of_image == 0.0) || (focal_length == 0.0))) {
            _ = fatalError("Invalid inputs. Enter non zero values with respect to the sign convention.")
        }
        return (1.0 / _num((_num((1.0 / focal_length)) - _num((1.0 / distance_of_image)))))
    }
    func image_distance(_ focal_length: Double, _ distance_of_object: Double) -> Double {
        if (((distance_of_object == 0.0) || (focal_length == 0.0))) {
            _ = fatalError("Invalid inputs. Enter non zero values with respect to the sign convention.")
        }
        return (1.0 / _num((_num((1.0 / focal_length)) - _num((1.0 / distance_of_object)))))
    }
    func test_focal_length() {
        let f1 = _num(focal_length(10.0, 20.0))
        if ((!isclose(_num(f1), 6.6666666666666599, 1e-08))) {
            _ = fatalError("focal_length test1 failed")
        }
        let f2 = _num(focal_length(9.5, 6.7000000000000002))
        if ((!isclose(_num(f2), 3.9290123459999999, 1e-08))) {
            _ = fatalError("focal_length test2 failed")
        }
    }
    func test_object_distance() {
        let u1 = _num(object_distance(30.0, 20.0))
        if ((!isclose(_num(u1), _num(-60.0), 1e-08))) {
            _ = fatalError("object_distance test1 failed")
        }
        let u2 = _num(object_distance(10.5, 11.699999999999999))
        if ((!isclose(_num(u2), 102.375, 1e-08))) {
            _ = fatalError("object_distance test2 failed")
        }
    }
    func test_image_distance() {
        let v1 = _num(image_distance(10.0, 40.0))
        if ((!isclose(_num(v1), 13.33333333, 1e-08))) {
            _ = fatalError("image_distance test1 failed")
        }
        let v2 = _num(image_distance(1.5, 6.7000000000000002))
        if ((!isclose(_num(v2), 1.932692308, 1e-08))) {
            _ = fatalError("image_distance test2 failed")
        }
    }
    func main() {
        _ = test_focal_length()
        _ = test_object_distance()
        _ = test_image_distance()
        print(_p(_p(_num(focal_length(10.0, 20.0)))))
        print(_p(_p(_num(object_distance(30.0, 20.0)))))
        print(_p(_p(_num(image_distance(10.0, 40.0)))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
