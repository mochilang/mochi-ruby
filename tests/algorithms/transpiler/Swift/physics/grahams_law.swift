// Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:46:07 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func to_float(_ x: Int) -> Double {
        return (_num(x) * 1.0)
    }
    func round6(_ x: Double) -> Double {
        let factor = 1000000.0
        return (_num(Double(_int(_int(((x * factor) + 0.5))))) / factor)
    }
    func sqrtApprox(_ x: Double) -> Double {
        var guess = (x / 2.0)
        var i = 0
        while ((i < 20)) {
            guess = _num((_num((guess + (x / guess))) / 2.0))
            i = _int((i &+ 1))
        }
        return guess
    }
    func validate(_ values: [Double]) -> Bool {
        if ((_int(((values).count)) == 0)) {
            return false
        }
        var i = 0
        while ((i < _int(((values).count)))) {
            if ((_num(_idx(values, i)) <= 0.0)) {
                return false
            }
            i = _int((i &+ 1))
        }
        return true
    }
    func effusion_ratio(_ m1: Double, _ m2: Double) -> Double {
        if ((!validate(({
            var _arr: [Double] = []
            _arr = _append(_arr, m1)
            _arr = _append(_arr, m2)
            return _arr
        }() as! [Double])))) {
            print(_p("ValueError: Molar mass values must greater than 0."))
            return 0.0
        }
        return _num(round6(_num(sqrtApprox(_num((m2 / m1))))))
    }
    func first_effusion_rate(_ rate: Double, _ m1: Double, _ m2: Double) -> Double {
        if ((!validate(({
            var _arr: [Double] = []
            _arr = _append(_arr, rate)
            _arr = _append(_arr, m1)
            _arr = _append(_arr, m2)
            return _arr
        }() as! [Double])))) {
            print(_p("ValueError: Molar mass and effusion rate values must greater than 0."))
            return 0.0
        }
        return _num(round6(_num((rate * _num(sqrtApprox(_num((m2 / m1))))))))
    }
    func second_effusion_rate(_ rate: Double, _ m1: Double, _ m2: Double) -> Double {
        if ((!validate(({
            var _arr: [Double] = []
            _arr = _append(_arr, rate)
            _arr = _append(_arr, m1)
            _arr = _append(_arr, m2)
            return _arr
        }() as! [Double])))) {
            print(_p("ValueError: Molar mass and effusion rate values must greater than 0."))
            return 0.0
        }
        return _num(round6(_num((rate / _num(sqrtApprox(_num((m2 / m1))))))))
    }
    func first_molar_mass(_ mass: Double, _ r1: Double, _ r2: Double) -> Double {
        if ((!validate(({
            var _arr: [Double] = []
            _arr = _append(_arr, mass)
            _arr = _append(_arr, r1)
            _arr = _append(_arr, r2)
            return _arr
        }() as! [Double])))) {
            print(_p("ValueError: Molar mass and effusion rate values must greater than 0."))
            return 0.0
        }
        let ratio = (r1 / r2)
        return _num(round6(_num((mass / _num((ratio * ratio))))))
    }
    func second_molar_mass(_ mass: Double, _ r1: Double, _ r2: Double) -> Double {
        if ((!validate(({
            var _arr: [Double] = []
            _arr = _append(_arr, mass)
            _arr = _append(_arr, r1)
            _arr = _append(_arr, r2)
            return _arr
        }() as! [Double])))) {
            print(_p("ValueError: Molar mass and effusion rate values must greater than 0."))
            return 0.0
        }
        let ratio = (r1 / r2)
        return _num(round6(_num((_num((ratio * ratio)) / mass))))
    }
    print(_p(_num(effusion_ratio(2.016, 4.0019999999999998))))
    print(_p(_num(first_effusion_rate(1.0, 2.016, 4.0019999999999998))))
    print(_p(_num(second_effusion_rate(1.0, 2.016, 4.0019999999999998))))
    print(_p(_num(first_molar_mass(2.0, 1.4089430000000001, 0.70975200000000005))))
    print(_p(_num(second_molar_mass(2.0, 1.4089430000000001, 0.70975200000000005))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
