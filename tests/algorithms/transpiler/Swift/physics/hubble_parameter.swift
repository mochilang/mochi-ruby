// Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:47:54 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func pow(_ base: Double, _ exp: Int) -> Double {
        var result = 1.0
        var i = 0
        while ((i < exp)) {
            result = _num((result * base))
            i = _int((i &+ 1))
        }
        return result
    }
    func sqrt_approx(_ x: Double) -> Double {
        if ((x == 0.0)) {
            return 0.0
        }
        var guess = (x / 2.0)
        var i = 0
        while ((i < 20)) {
            guess = _num((_num((guess + (x / guess))) / 2.0))
            i = _int((i &+ 1))
        }
        return guess
    }
    func hubble_parameter(_ hubble_constant: Double, _ radiation_density: Double, _ matter_density: Double, _ dark_energy: Double, _ redshift: Double) -> Double {
        let parameters: [Double] = ({
            var _arr: [Double] = []
            _arr = _append(_arr, redshift)
            _arr = _append(_arr, radiation_density)
            _arr = _append(_arr, matter_density)
            _arr = _append(_arr, dark_energy)
            return _arr
        }() as! [Double])
        var i = 0
        while ((i < _int(((parameters).count)))) {
            if ((_num(_idx(parameters, i)) < 0.0)) {
                _ = fatalError("All input parameters must be positive")
            }
            i = _int((i &+ 1))
        }
        i = 1
        while ((i < 4)) {
            if ((_num(_idx(parameters, i)) > 1.0)) {
                _ = fatalError("Relative densities cannot be greater than one")
            }
            i = _int((i &+ 1))
        }
        let curvature = (1.0 - _num(((matter_density + radiation_density) + dark_energy)))
        let zp1 = (redshift + 1.0)
        let e2 = ((((radiation_density * _num(pow(_num(zp1), 4))) + (matter_density * _num(pow(_num(zp1), 3)))) + (curvature * _num(pow(_num(zp1), 2)))) + dark_energy)
        return (hubble_constant * _num(sqrt_approx(_num(e2))))
    }
    func test_hubble_parameter() {
        let h = _num(hubble_parameter(68.299999999999997, 0.0001, 0.29999999999999999, 0.69999999999999996, 0.0))
        if (((h < 68.299899999999994) || (h > 68.3001))) {
            _ = fatalError("hubble_parameter test failed")
        }
    }
    func main() {
        _ = test_hubble_parameter()
        print(_p(_num(hubble_parameter(68.299999999999997, 0.0001, 0.29999999999999999, 0.69999999999999996, 0.0))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
