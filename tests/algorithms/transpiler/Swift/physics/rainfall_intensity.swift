// Generated by Mochi transpiler v0.10.69 on 2025-08-19 16:47:03 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func exp_approx(_ x: Double) -> Double {
        var y: Double = x
        var is_neg: Bool = false
        if ((x < 0.0)) {
            is_neg = true
            y = _num(-x)
        }
        var term: Double = 1.0
        var sum: Double = 1.0
        var n: Int = 1
        while (n < 30) {
            term = _num(((term * y) / _num(n)))
            sum = _num((sum + term))
            n = _int((n &+ 1))
        }
        if (is_neg) {
            return (1.0 / sum)
        }
        return sum
    }
    func ln_series(_ x: Double) -> Double {
        let t = (_num((x - 1.0)) / _num((x + 1.0)))
        var term = t
        var sum = 0.0
        var n = 1
        while (n <= 19) {
            sum = _num((sum + (term / _num(n))))
            term = _num(((term * t) * t))
            n = _int((n &+ 2))
        }
        return (2.0 * sum)
    }
    func ln(_ x: Double) -> Double {
        var y = x
        var k = 0
        while (y >= 10.0) {
            y = _num((y / 10.0))
            k = _int((k &+ 1))
        }
        while (y < 1.0) {
            y = _num((y * 10.0))
            k = _int((k &- 1))
        }
        return (_num(ln_series(_num(y))) + (_num(k) * _num(ln_series(10.0))))
    }
    func powf(_ base: Double, _ exponent: Double) -> Double {
        return _num(exp_approx(_num((exponent * _num(ln(_num(base)))))))
    }
    func rainfall_intensity(_ coefficient_k: Double, _ coefficient_a: Double, _ coefficient_b: Double, _ coefficient_c: Double, _ return_period: Double, _ duration: Double) -> Double {
        if ((coefficient_k <= 0.0)) {
            _ = fatalError("All parameters must be positive.")
        }
        if ((coefficient_a <= 0.0)) {
            _ = fatalError("All parameters must be positive.")
        }
        if ((coefficient_b <= 0.0)) {
            _ = fatalError("All parameters must be positive.")
        }
        if ((coefficient_c <= 0.0)) {
            _ = fatalError("All parameters must be positive.")
        }
        if ((return_period <= 0.0)) {
            _ = fatalError("All parameters must be positive.")
        }
        if ((duration <= 0.0)) {
            _ = fatalError("All parameters must be positive.")
        }
        let numerator = (coefficient_k * _num(powf(_num(return_period), _num(coefficient_a))))
        let denominator = _num(powf(_num((duration + coefficient_b)), _num(coefficient_c)))
        return (numerator / denominator)
    }
    let r1 = _num(rainfall_intensity(1000.0, 0.20000000000000001, 11.6, 0.81000000000000005, 10.0, 60.0))
    print(_p(_p(r1)))
    let r2 = _num(rainfall_intensity(1000.0, 0.20000000000000001, 11.6, 0.81000000000000005, 10.0, 30.0))
    print(_p(_p(r2)))
    let r3 = _num(rainfall_intensity(1000.0, 0.20000000000000001, 11.6, 0.81000000000000005, 5.0, 60.0))
    print(_p(_p(r3)))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
