// Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:04:50 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func make_list(_ len: Int, _ value: Int) -> [Int] {
        var arr: [Int] = ([] as! [Int])
        var i = 0
        while (i < len) {
            arr = (_append(arr, value) as! [Int])
            i = _int((i &+ 1))
        }
        return arr
    }
    func count_recursive(_ array: [Int], _ target: Int) -> Int {
        if (target < 0) {
            return 0
        }
        if (target == 0) {
            return 1
        }
        var total = 0
        var i = 0
        while (i < _int(((array).count))) {
            total = _int((total &+ _int(count_recursive((array as! [Int]), (target &- (_idx(array, i) as? Int ?? 0))))))
            i = _int((i &+ 1))
        }
        return total
    }
    func combination_sum_iv(_ array: [Int], _ target: Int) -> Int {
        return _int(count_recursive((array as! [Int]), target))
    }
    func count_dp(_ array: [Int], _ target: Int, _ dp: [Int]) -> Int {
        var dp = dp
        if (target < 0) {
            return 0
        }
        if (target == 0) {
            return 1
        }
        if ((_idx(dp, target) as? Int ?? 0) > _int((0 &- 1))) {
            return (_idx(dp, target) as? Int ?? 0)
        }
        var total = 0
        var i = 0
        while (i < _int(((array).count))) {
            total = _int((total &+ _int(count_dp((array as! [Int]), (target &- (_idx(array, i) as? Int ?? 0)), (dp as! [Int])))))
            i = _int((i &+ 1))
        }
        dp = _set(dp, target, Int(total))
        return total
    }
    func combination_sum_iv_dp_array(_ array: [Int], _ target: Int) -> Int {
        var dp = (make_list((target &+ 1), -1) as! [Int])
        return _int(count_dp((array as! [Int]), target, (dp as! [Int])))
    }
    func combination_sum_iv_bottom_up(_ n: Int, _ array: [Int], _ target: Int) -> Int {
        var dp = (make_list((target &+ 1), 0) as! [Int])
        dp = _set(dp, 0, 1)
        var i = 1
        while (i <= target) {
            var j = 0
            while (j < n) {
                if ((i &- (_idx(array, j) as? Int ?? 0)) >= 0) {
                    dp = _set(dp, i, _int(((_idx(dp, i) as? Int ?? 0) &+ (_idx(dp, (i &- (_idx(array, j) as? Int ?? 0))) as? Int ?? 0))))
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        return (_idx(dp, target) as? Int ?? 0)
    }
    print(_p(_p(_int(combination_sum_iv(({
        var _arr: [Int] = []
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 5)
        return _arr
    }() as! [Int]), 5)))))
    print(_p(_p(_int(combination_sum_iv_dp_array(({
        var _arr: [Int] = []
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 5)
        return _arr
    }() as! [Int]), 5)))))
    print(_p(_p(_int(combination_sum_iv_bottom_up(3, ({
        var _arr: [Int] = []
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 5)
        return _arr
    }() as! [Int]), 5)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
