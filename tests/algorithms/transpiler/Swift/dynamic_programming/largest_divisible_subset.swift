// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:39:59 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func sort_list(_ nums: [Int]) -> [Int] {
        var arr = nums
        var i = 1
        while ((i < _int(((arr).count)))) {
            let key = (_idx(arr, i) as? Int ?? 0)
            var j = (i &- 1)
            while (((j >= 0) && ((_idx(arr, j) as? Int ?? 0) > key))) {
                arr = _set(arr, (j &+ 1), { let _tmp0 = (_idx(arr, j) as? Int ?? 0); return _tmp0 }())
                j = _int((j &- 1))
            }
            arr = _set(arr, (j &+ 1), { let _tmp1 = Int(key); return _tmp1 }())
            i = _int((i &+ 1))
        }
        return arr
    }
    func largest_divisible_subset(_ items: [Int]) -> [Int] {
        if ((_int(((items).count)) == 0)) {
            return ([] as! [Int])
        }
        var nums = (sort_list((items as! [Int])) as! [Int])
        let n = _int(((nums).count))
        var memo: [Int] = ([] as! [Int])
        var prev: [Int] = ([] as! [Int])
        var i = 0
        while ((i < n)) {
            memo = (_append(memo, 1) as! [Int])
            prev = (_append(prev, i) as! [Int])
            i = _int((i &+ 1))
        }
        i = 0
        while ((i < n)) {
            var j = 0
            while ((j < i)) {
                if ({ let _tmp3 = (((_idx(nums, j) as? Int ?? 0) == 0) || ((((_idx(nums, i) as? Int ?? 0) % (_idx(nums, j) as? Int ?? 0) + (_idx(nums, j) as? Int ?? 0)) % (_idx(nums, j) as? Int ?? 0)) == 0)); let _tmp4 = (((_idx(memo, j) as? Int ?? 0) &+ 1) > (_idx(memo, i) as? Int ?? 0)); return _tmp3 && _tmp4}()) {
                    memo = _set(memo, i, { let _tmp4 = _int(((_idx(memo, j) as? Int ?? 0) &+ 1)); return _tmp4 }())
                    prev = _set(prev, i, { let _tmp5 = Int(j); return _tmp5 }())
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        var ans = (0 &- 1)
        var last_index = (0 &- 1)
        i = 0
        while ((i < n)) {
            if (((_idx(memo, i) as? Int ?? 0) > ans)) {
                ans = (_idx(memo, i) as? Int ?? 0)
                last_index = Int(i)
            }
            i = _int((i &+ 1))
        }
        if ((last_index == (0 &- 1))) {
            return ([] as! [Int])
        }
        var result: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, (_idx(nums, last_index) as? Int ?? 0))
            return _arr
        }() as! [Int])
        while (((_idx(prev, last_index) as? Int ?? 0) != last_index)) {
            last_index = (_idx(prev, last_index) as? Int ?? 0)
            result = (_append(result, (_idx(nums, last_index) as? Int ?? 0)) as! [Int])
        }
        return result
    }
    func main() {
        let items: [Int] = ({
            var _arr: [Int] = []
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 16)
            _arr = _append(_arr, 7)
            _arr = _append(_arr, 8)
            _arr = _append(_arr, 4)
            return _arr
        }() as! [Int])
        let subset: [Int] = (largest_divisible_subset((items as! [Int])) as! [Int])
        print(_p({ let _tmp9 = { let _tmp7 = (_p((_p("The longest divisible subset of ") + _p("[" + items.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))) + _p(" is ")); let _tmp8 = "[" + subset.map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"; return (_p(_tmp7) + _p(_tmp8))}(); let _tmp10 = "."; return (_p(_tmp9) + _p(_tmp10))}()))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
