// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:40:52 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
struct Query: Codable {
    var left: Int
    var right: Int
    init() {
        self.left = 0
        self.right = 0
    }
    init(left: Int, right: Int) {
        self.left = left
        self.right = right
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func prefix_sum(_ arr: [Int], _ queries: [Query]) -> [Int] {
        var dp: [Int] = ([] as! [Int])
        var i = 0
        while ((i < _int(((arr).count)))) {
            if ((i == 0)) {
                dp = (_append(dp, (_idx(arr, 0) as? Int ?? 0)) as! [Int])
            } else {
                dp = (_append(dp, ((_idx(dp, (i &- 1)) as? Int ?? 0) &+ (_idx(arr, i) as? Int ?? 0))) as! [Int])
            }
            i = _int((i &+ 1))
        }
        var result: [Int] = ([] as! [Int])
        var j = 0
        while ((j < _int(((queries).count)))) {
            let q = (_idx(queries, j) as! Query)
            var sum = (_idx(dp, q.right) as? Int ?? 0)
            if ((q.left > 0)) {
                sum = _int((sum &- (_idx(dp, (q.left &- 1)) as? Int ?? 0)))
            }
            result = (_append(result, sum) as! [Int])
            j = _int((j &+ 1))
        }
        return result
    }
    let arr1: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 6)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 61)
        _arr = _append(_arr, 12)
        return _arr
    }() as! [Int])
    let queries1: [Query] = ({
        var _arr: [Query] = []
        _arr = _append(_arr, Query(left: 2, right: 5))
        _arr = _append(_arr, Query(left: 1, right: 5))
        _arr = _append(_arr, Query(left: 3, right: 4))
        return _arr
    }() as! [Query])
    print(_p("[" + (prefix_sum((arr1 as! [Int]), (queries1 as! [Query])) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let arr2: [Int] = ({
        var _arr: [Int] = []
        _arr = _append(_arr, 4)
        _arr = _append(_arr, 2)
        _arr = _append(_arr, 1)
        _arr = _append(_arr, 6)
        _arr = _append(_arr, 3)
        return _arr
    }() as! [Int])
    let queries2: [Query] = ({
        var _arr: [Query] = []
        _arr = _append(_arr, Query(left: 3, right: 4))
        _arr = _append(_arr, Query(left: 1, right: 3))
        _arr = _append(_arr, Query(left: 0, right: 2))
        return _arr
    }() as! [Query])
    print(_p("[" + (prefix_sum((arr2 as! [Int]), (queries2 as! [Query])) as! [Int]).map{ _p($0).replacingOccurrences(of: ", ", with: " ") }.joined(separator: " ") + "]"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
