// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:40:42 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func make_list(_ len: Int, _ value: Int) -> [Int] {
        var arr: [Int] = ([] as! [Int])
        var i = 0
        while ((i < len)) {
            arr = (_append(arr, value) as! [Int])
            i = _int((i &+ 1))
        }
        return arr
    }
    func int_sqrt(_ n: Int) -> Int {
        var r = 0
        while (((_int((r &+ 1)) &* _int((r &+ 1))) <= n)) {
            r = _int((r &+ 1))
        }
        return r
    }
    func minimum_squares_to_represent_a_number(_ number: Int) -> Int {
        if ((number < 0)) {
            _ = fatalError("the value of input must not be a negative number")
        }
        if ((number == 0)) {
            return 1
        }
        var answers = (make_list((number &+ 1), -1) as! [Int])
        answers = _set(answers, 0, { let _tmp0 = 0; return _tmp0 }())
        var i = 1
        while ((i <= number)) {
            var answer = i
            let root = _int(int_sqrt(i))
            var j = 1
            while ((j <= root)) {
                let current_answer = (1 &+ (_idx(answers, (i &- (j &* j))) as? Int ?? 0))
                if ((current_answer < answer)) {
                    answer = Int(current_answer)
                }
                j = _int((j &+ 1))
            }
            answers = _set(answers, i, { let _tmp1 = Int(answer); return _tmp1 }())
            i = _int((i &+ 1))
        }
        return (_idx(answers, number) as? Int ?? 0)
    }
    print(_p(_int(minimum_squares_to_represent_a_number(25))))
    print(_p(_int(minimum_squares_to_represent_a_number(21))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
