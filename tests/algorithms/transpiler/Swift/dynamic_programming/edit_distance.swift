// Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:04:56 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func min3(_ a: Int, _ b: Int, _ c: Int) -> Int {
        var m = a
        if (b < m) {
            m = Int(b)
        }
        if (c < m) {
            m = Int(c)
        }
        return m
    }
    func helper_top_down(_ word1: String, _ word2: String, _ dp: [[Int]], _ i: Int, _ j: Int) -> Int {
        var dp = dp
        if (i < 0) {
            return (j &+ 1)
        }
        if (j < 0) {
            return (i &+ 1)
        }
        if ((_idx(_idx(dp, i)!, j) as? Int ?? 0) != _int((0 &- 1))) {
            return (_idx(_idx(dp, i)!, j) as? Int ?? 0)
        }
        if (String(describing: String(Array(String(describing: (word1 as! String)))[i..<(i &+ 1)])) == String(describing: String(Array(String(describing: (word2 as! String)))[j..<(j &+ 1)]))) {
            var _tmp7 = (_idx(dp, i) as! [Int])
            _tmp7 = _set(_tmp7, j, _int(helper_top_down((word1 as! String), (word2 as! String), (dp as! [[Int]]), (i &- 1), (j &- 1))))
            dp = _set(dp, i, (_tmp7 as! [Int]))
        } else {
            let insert = _int(helper_top_down((word1 as! String), (word2 as! String), (dp as! [[Int]]), i, (j &- 1)))
            let delete = _int(helper_top_down((word1 as! String), (word2 as! String), (dp as! [[Int]]), (i &- 1), j))
            let replace = _int(helper_top_down((word1 as! String), (word2 as! String), (dp as! [[Int]]), (i &- 1), (j &- 1)))
            var _tmp8 = (_idx(dp, i) as! [Int])
            _tmp8 = _set(_tmp8, j, _int((1 &+ _int(min3(insert, delete, replace)))))
            dp = _set(dp, i, (_tmp8 as! [Int]))
        }
        return (_idx(_idx(dp, i)!, j) as? Int ?? 0)
    }
    func min_dist_top_down(_ word1: String, _ word2: String) -> Int {
        let m = _int(((word1).count))
        let n = _int(((word2).count))
        var dp: [[Int]] = ([] as! [[Int]])
        for _ in 0..<m {
            var row: [Int] = ([] as! [Int])
            for _2 in 0..<n {
                row = (_append(row, (0 &- 1)) as! [Int])
            }
            dp = (_append(dp, row) as! [[Int]])
        }
        return _int(helper_top_down((word1 as! String), (word2 as! String), (dp as! [[Int]]), (m &- 1), (n &- 1)))
    }
    func min_dist_bottom_up(_ word1: String, _ word2: String) -> Int {
        let m = _int(((word1).count))
        let n = _int(((word2).count))
        var dp: [[Int]] = ([] as! [[Int]])
        for _ in 0..<_int((m &+ 1)) {
            var row: [Int] = ([] as! [Int])
            for _2 in 0..<_int((n &+ 1)) {
                row = (_append(row, 0) as! [Int])
            }
            dp = (_append(dp, row) as! [[Int]])
        }
        for i in 0..<_int((m &+ 1)) {
            for j in 0..<_int((n &+ 1)) {
                if (i == 0) {
                    var _tmp9 = (_idx(dp, i) as! [Int])
                    _tmp9 = _set(_tmp9, j, Int(j))
                    dp = _set(dp, i, (_tmp9 as! [Int]))
                } else if (j == 0) {
                    var _tmp10 = (_idx(dp, i) as! [Int])
                    _tmp10 = _set(_tmp10, j, Int(i))
                    dp = _set(dp, i, (_tmp10 as! [Int]))
                } else if (String(describing: String(Array(String(describing: (word1 as! String)))[(i &- 1)..<i])) == String(describing: String(Array(String(describing: (word2 as! String)))[(j &- 1)..<j]))) {
                    var _tmp11 = (_idx(dp, i) as! [Int])
                    _tmp11 = _set(_tmp11, j, (_idx(_idx(dp, (i &- 1))!, (j &- 1)) as? Int ?? 0))
                    dp = _set(dp, i, (_tmp11 as! [Int]))
                } else {
                    let insert = (_idx(_idx(dp, i)!, (j &- 1)) as? Int ?? 0)
                    let delete = (_idx(_idx(dp, (i &- 1))!, j) as? Int ?? 0)
                    let replace = (_idx(_idx(dp, (i &- 1))!, (j &- 1)) as? Int ?? 0)
                    var _tmp12 = (_idx(dp, i) as! [Int])
                    _tmp12 = _set(_tmp12, j, _int((1 &+ _int(min3(insert, delete, replace)))))
                    dp = _set(dp, i, (_tmp12 as! [Int]))
                }
                
                
            }
        }
        return (_idx(_idx(dp, m)!, n) as? Int ?? 0)
    }
    print(_p(_p(_int(min_dist_top_down("intention", "execution")))))
    print(_p(_p(_int(min_dist_top_down("intention", "")))))
    print(_p(_p(_int(min_dist_top_down("", "")))))
    print(_p(_p(_int(min_dist_bottom_up("intention", "execution")))))
    print(_p(_p(_int(min_dist_bottom_up("intention", "")))))
    print(_p(_p(_int(min_dist_bottom_up("", "")))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
