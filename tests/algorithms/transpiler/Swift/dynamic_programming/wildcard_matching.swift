// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:41:09 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func make_bool_list(_ n: Int) -> [Bool] {
        var row: [Bool] = ([] as! [Bool])
        var i = 0
        while ((i < n)) {
            row = (_append(row, false) as! [Bool])
            i = _int((i &+ 1))
        }
        return row
    }
    func make_bool_matrix(_ rows: Int, _ cols: Int) -> [[Bool]] {
        var matrix: [[Bool]] = ([] as! [[Bool]])
        var i = 0
        while ((i < rows)) {
            matrix = (_append(matrix, (make_bool_list(cols) as! [Bool])) as! [[Bool]])
            i = _int((i &+ 1))
        }
        return matrix
    }
    func is_match(_ s: String, _ p: String) -> Bool {
        let n = _int(((s).count))
        let m = _int(((p).count))
        var dp = (make_bool_matrix((n &+ 1), (m &+ 1)) as! [[Bool]])
        var _tmp5 = (_idx(dp, 0) as! [Bool])
        _tmp5 = _set(_tmp5, 0, { let _tmp8 = true; return _tmp8 }())
        dp = _set(dp, 0, { let _tmp9 = (_tmp5 as! [Bool]); return _tmp9 }())
        var j = 1
        while ((j <= m)) {
            if ((String(_slice(Array(p), (j &- 1), j)) == "*")) {
                var _tmp6 = (_idx(dp, 0) as! [Bool])
                _tmp6 = _set(_tmp6, j, { let _tmp10 = (_idx(_idx(dp, 0), (j &- 1)) as! Bool); return _tmp10 }())
                dp = _set(dp, 0, { let _tmp11 = (_tmp6 as! [Bool]); return _tmp11 }())
            }
            j = _int((j &+ 1))
        }
        var i = 1
        while ((i <= n)) {
            var j2 = 1
            while ((j2 <= m)) {
                let pc = String(_slice(Array(p), (j2 &- 1), j2))
                let sc = String(_slice(Array(s), (i &- 1), i))
                if (((pc == sc) || (pc == "?"))) {
                    var _tmp7 = (_idx(dp, i) as! [Bool])
                    _tmp7 = _set(_tmp7, j2, { let _tmp12 = (_idx(_idx(dp, (i &- 1)), (j2 &- 1)) as! Bool); return _tmp12 }())
                    dp = _set(dp, i, { let _tmp13 = (_tmp7 as! [Bool]); return _tmp13 }())
                } else if ((pc == "*")) {
                    if (((_idx(_idx(dp, (i &- 1)), j2) as! Bool) || (_idx(_idx(dp, i), (j2 &- 1)) as! Bool))) {
                        var _tmp8 = (_idx(dp, i) as! [Bool])
                        _tmp8 = _set(_tmp8, j2, { let _tmp14 = true; return _tmp14 }())
                        dp = _set(dp, i, { let _tmp15 = (_tmp8 as! [Bool]); return _tmp15 }())
                    }
                }
                
                j2 = _int((j2 &+ 1))
            }
            i = _int((i &+ 1))
        }
        return (_idx(_idx(dp, n), m) as! Bool)
    }
    func print_bool(_ b: Bool) {
        if (b) {
            print(_p(true))
        } else {
            print(_p(false))
        }
    }
    _ = print_bool(is_match("abc", "a*c"))
    _ = print_bool(is_match("abc", "a*d"))
    _ = print_bool(is_match("baaabab", "*****ba*****ab"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
