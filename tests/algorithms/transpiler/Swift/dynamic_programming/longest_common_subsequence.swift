// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:40:01 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
struct LcsResult: Codable {
    var length: Int
    var sequence: String
    init() {
        self.length = 0
        self.sequence = ""
    }
    init(length: Int, sequence: String) {
        self.length = length
        self.sequence = sequence
    }
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func zeros_matrix(_ rows: Int, _ cols: Int) -> [[Int]] {
        var matrix: [[Int]] = ([] as! [[Int]])
        var i = 0
        while ((i <= rows)) {
            var row: [Int] = ([] as! [Int])
            var j = 0
            while ((j <= cols)) {
                row = (_append(row, 0) as! [Int])
                j = _int((j &+ 1))
            }
            matrix = (_append(matrix, row) as! [[Int]])
            i = _int((i &+ 1))
        }
        return matrix
    }
    func longest_common_subsequence(_ x: String, _ y: String) -> LcsResult {
        let m = _int(((x).count))
        let n = _int(((y).count))
        var dp = (zeros_matrix(m, n) as! [[Int]])
        var i = 1
        while ((i <= m)) {
            var j = 1
            while ((j <= n)) {
                if (((String(Array(x)[(i &- 1)]) as! String) == (String(Array(y)[(j &- 1)]) as! String))) {
                    var _tmp4 = (_idx(dp, i) as! [Int])
                    _tmp4 = _set(_tmp4, j, { let _tmp6 = _int(((_idx(_idx(dp, (i &- 1)), (j &- 1)) as? Int ?? 0) &+ 1)); return _tmp6 }())
                    dp = _set(dp, i, { let _tmp7 = (_tmp4 as! [Int]); return _tmp7 }())
                } else {
                    if (((_idx(_idx(dp, (i &- 1)), j) as? Int ?? 0) > (_idx(_idx(dp, i), (j &- 1)) as? Int ?? 0))) {
                        var _tmp5 = (_idx(dp, i) as! [Int])
                        _tmp5 = _set(_tmp5, j, { let _tmp8 = (_idx(_idx(dp, (i &- 1)), j) as? Int ?? 0); return _tmp8 }())
                        dp = _set(dp, i, { let _tmp9 = (_tmp5 as! [Int]); return _tmp9 }())
                    } else {
                        var _tmp6 = (_idx(dp, i) as! [Int])
                        _tmp6 = _set(_tmp6, j, { let _tmp10 = (_idx(_idx(dp, i), (j &- 1)) as? Int ?? 0); return _tmp10 }())
                        dp = _set(dp, i, { let _tmp11 = (_tmp6 as! [Int]); return _tmp11 }())
                    }
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        var seq = ""
        var i2 = m
        var j2 = n
        while (((i2 > 0) && (j2 > 0))) {
            if (((String(Array(x)[(i2 &- 1)]) as! String) == (String(Array(y)[(j2 &- 1)]) as! String))) {
                seq = ((_p((String(Array(x)[(i2 &- 1)]) as! String)) + _p(seq)) as! String)
                i2 = _int((i2 &- 1))
                j2 = _int((j2 &- 1))
            } else if (((_idx(_idx(dp, (i2 &- 1)), j2) as? Int ?? 0) >= (_idx(_idx(dp, i2), (j2 &- 1)) as? Int ?? 0))) {
                i2 = _int((i2 &- 1))
            } else {
                j2 = _int((j2 &- 1))
            }
            
        }
        return LcsResult(length: (_idx(_idx(dp, m), n) as? Int ?? 0), sequence: String(describing: seq))
    }
    let a = "AGGTAB"
    let b = "GXTXAYB"
    let res = longest_common_subsequence((a as! String), (b as! String))
    print(_p((_p((_p((_p("len = ") + _p(_p(res.length)))) + _p(", sub-sequence = "))) + _p(res.sequence))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
