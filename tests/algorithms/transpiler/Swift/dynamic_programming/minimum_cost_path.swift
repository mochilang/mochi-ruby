// Generated by Mochi transpiler v0.10.72 on 2025-08-25 08:40:36 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func min_int(_ a: Int, _ b: Int) -> Int {
        if ((a < b)) {
            return a
        }
        return b
    }
    func minimum_cost_path(_ matrix: inout [[Int]]) -> Int {
        let rows = _int(((matrix).count))
        let cols = _int((((_idx(matrix, 0) as! [Int])).count))
        var j = 1
        while ((j < cols)) {
            var row0: [Int] = (_idx(matrix, 0) as! [Int])
            row0 = _set(row0, j, { let _tmp0 = _int(((_idx(row0, j) as? Int ?? 0) &+ (_idx(row0, (j &- 1)) as? Int ?? 0))); return _tmp0 }())
            matrix = _set(matrix, 0, { let _tmp1 = (row0 as! [Int]); return _tmp1 }())
            j = _int((j &+ 1))
        }
        var i = 1
        while ((i < rows)) {
            var row: [Int] = (_idx(matrix, i) as! [Int])
            row = _set(row, 0, { let _tmp2 = _int(((_idx(row, 0) as? Int ?? 0) &+ (_idx(_idx(matrix, (i &- 1)), 0) as? Int ?? 0))); return _tmp2 }())
            matrix = _set(matrix, i, { let _tmp3 = (row as! [Int]); return _tmp3 }())
            i = _int((i &+ 1))
        }
        i = 1
        while ((i < rows)) {
            var row: [Int] = (_idx(matrix, i) as! [Int])
            j = 1
            while ((j < cols)) {
                let up = (_idx(_idx(matrix, (i &- 1)), j) as? Int ?? 0)
                let left = (_idx(row, (j &- 1)) as? Int ?? 0)
                let best = _int(min_int(up, left))
                row = _set(row, j, { let _tmp4 = _int(((_idx(row, j) as? Int ?? 0) &+ best)); return _tmp4 }())
                j = _int((j &+ 1))
            }
            matrix = _set(matrix, i, { let _tmp5 = (row as! [Int]); return _tmp5 }())
            i = _int((i &+ 1))
        }
        return (_idx(_idx(matrix, (rows &- 1)), (cols &- 1)) as? Int ?? 0)
    }
    var m1: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 4)
            _arr = _append(_arr, 2)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    var m2: [[Int]] = ({
        var _arr: [[Int]] = []
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 4)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            _arr = _append(_arr, 3)
            return _arr
        }() as! [Int]))
        _arr = _append(_arr, ({
            var _arr: [Int] = []
            _arr = _append(_arr, 3)
            _arr = _append(_arr, 2)
            _arr = _append(_arr, 1)
            return _arr
        }() as! [Int]))
        return _arr
    }() as! [[Int]])
    print(_p(_p(_int(minimum_cost_path(&m1)))))
    print(_p(_p(_int(minimum_cost_path(&m2)))))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
