// Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:03:45 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.rounded(.towardZero) == d {
                return String(Int64(d))
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T? {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    if idx < out.count {
        out[idx] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: idx - out.count + 1))
        out[idx] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func index_of(_ s: String, _ ch: String) -> Int {
        var i = 0
        while (i < _int(((s).count))) {
            if ((String(Array(s)[i]) as! String) == ch) {
                return i
            }
            i = _int((i &+ 1))
        }
        return -1
    }
    func ord(_ ch: String) -> Int {
        let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
        let lower = "abcdefghijklmnopqrstuvwxyz"
        var idx = _int(index_of((upper as! String), (ch as! String)))
        if (idx >= 0) {
            return (65 &+ idx)
        }
        idx = _int(index_of((lower as! String), (ch as! String)))
        if (idx >= 0) {
            return (97 &+ idx)
        }
        return 0
    }
    func chr(_ n: Int) -> String {
        let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
        let lower = "abcdefghijklmnopqrstuvwxyz"
        if ((n >= 65) && (n < 91)) {
            return String(_slice(Array(upper), (n &- 65), (n &- 64)))
        }
        if ((n >= 97) && (n < 123)) {
            return String(_slice(Array(lower), (n &- 97), (n &- 96)))
        }
        return "?"
    }
    func to_upper_char(_ c: String) -> String {
        let code = _int(ord((c as! String)))
        if ((code >= 97) && (code <= 122)) {
            return String(describing: chr((code &- 32)))
        }
        return c
    }
    func is_lower(_ c: String) -> Bool {
        let code = _int(ord((c as! String)))
        return ((code >= 97) && (code <= 122))
    }
    func abbr(_ a: String, _ b: String) -> Bool {
        let n = _int(((a).count))
        let m = _int(((b).count))
        var dp: [[Bool]] = ([] as! [[Bool]])
        var i = 0
        while (i <= n) {
            var row: [Bool] = ([] as! [Bool])
            var j = 0
            while (j <= m) {
                row = (_append(row, false) as! [Bool])
                j = _int((j &+ 1))
            }
            dp = (_append(dp, row) as! [[Bool]])
            i = _int((i &+ 1))
        }
        var _tmp4 = (_idx(dp, 0) as! [Bool])
        _tmp4 = _set(_tmp4, 0, true)
        dp = _set(dp, 0, (_tmp4 as! [Bool]))
        i = 0
        while (i < n) {
            var j = 0
            while (j <= m) {
                if (_idx(_idx(dp, i)!, j) as! Bool) {
                    if ((j < m) && (String(describing: to_upper_char((String(Array(a)[i]) as! String))) == (String(Array(b)[j]) as! String))) {
                        var _tmp5 = (_idx(dp, (i &+ 1)) as! [Bool])
                        _tmp5 = _set(_tmp5, (j &+ 1), true)
                        dp = _set(dp, (i &+ 1), (_tmp5 as! [Bool]))
                    }
                    if is_lower((String(Array(a)[i]) as! String)) {
                        var _tmp6 = (_idx(dp, (i &+ 1)) as! [Bool])
                        _tmp6 = _set(_tmp6, j, true)
                        dp = _set(dp, (i &+ 1), (_tmp6 as! [Bool]))
                    }
                }
                j = _int((j &+ 1))
            }
            i = _int((i &+ 1))
        }
        return (_idx(_idx(dp, n)!, m) as! Bool)
    }
    func print_bool(_ b: Bool) {
        if b {
            print(_p(true))
        } else {
            print(_p(false))
        }
    }
    _ = print_bool(abbr("daBcd", "ABC"))
    _ = print_bool(abbr("dBcd", "ABC"))
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
