# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def sort_points(points, axis)
    arr = points
    i = 0
    while i < _len(arr)
      j = 0
      while j < _len(arr) - 1
        if (__tmp1 = (__tmp2 = arr; __tmp2.is_a?(Hash) ? __tmp2[j] : _idx(__tmp2, j)); __tmp1.is_a?(Hash) ? __tmp1[axis] : _idx(__tmp1, axis)) > (__tmp3 = (__tmp4 = arr; __tmp4.is_a?(Hash) ? __tmp4[_add(j, 1)] : _idx(__tmp4, _add(j, 1))); __tmp3.is_a?(Hash) ? __tmp3[axis] : _idx(__tmp3, axis))
          tmp = (__tmp5 = arr; __tmp5.is_a?(Hash) ? __tmp5[j] : _idx(__tmp5, j))
          arr[j] = (__tmp6 = arr; __tmp6.is_a?(Hash) ? __tmp6[_add(j, 1)] : _idx(__tmp6, _add(j, 1)))
          arr[_add(j, 1)] = tmp
        end
        j = _add(j, 1)
      end
      i = _add(i, 1)
    end
    return arr
  end
  def build_kdtree(points, depth)
    if _eq(_len(points), 0)
      return 0 - 1
    end
    k = _len((__tmp7 = points; __tmp7.is_a?(Hash) ? __tmp7[0] : _idx(__tmp7, 0)))
    axis = depth % k
    sorted = sort_points(points, axis)
    median_idx = _len(sorted) / 2
    left_points = sorted[0...median_idx]
    right_points = sorted[_add(median_idx, 1)..._len(sorted)]
    idx = _len($tree)
    $tree = _append($tree, KDNode.new(point: (__tmp8 = sorted; __tmp8.is_a?(Hash) ? __tmp8[median_idx] : _idx(__tmp8, median_idx)), left: 0 - 1, right: 0 - 1))
    left_idx = build_kdtree(left_points, _add(depth, 1))
    right_idx = build_kdtree(right_points, _add(depth, 1))
    node = (__tmp9 = $tree; __tmp9.is_a?(Hash) ? __tmp9[idx] : _idx(__tmp9, idx))
    node["left"] = left_idx
    node["right"] = right_idx
    $tree[idx] = node
    return idx
  end
  Object.send(:remove_const, :KDNode) if Object.const_defined?(:KDNode)
  Object.const_set(:KDNode, Struct.new(:point, :left, :right, keyword_init: true))
  $tree = []
  $pts = [[2.0, 3.0], [5.0, 4.0], [9.0, 6.0], [4.0, 7.0], [8.0, 1.0], [7.0, 2.0]]
  $root = build_kdtree($pts, 0)
  puts(((x = $tree); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
  puts($root)
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
