# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def create_queue(capacity)
    data = []
    next_ = []
    prev = []
    i = 0
    while i < capacity
      data = _append(data, "")
      next_ = _append(next_, (_add(i, 1)) % capacity)
      prev = _append(prev, (_add(i - 1, capacity)) % capacity)
      i = _add(i, 1)
    end
    return CircularQueue.new(data: data, next_: next_, prev: prev, front: 0, rear: 0)
  end
  def is_empty(q)
    return _eq(q.front, q.rear) && _eq((__tmp1 = q.data; __tmp1.is_a?(Hash) ? __tmp1[q.front] : _idx(__tmp1, q.front)), "")
  end
  def check_can_perform(q)
    if is_empty(q)
      panic("Empty Queue")
    end
  end
  def check_is_full(q)
    if _eq((__tmp2 = q.next_; __tmp2.is_a?(Hash) ? __tmp2[q.rear] : _idx(__tmp2, q.rear)), q.front)
      panic("Full Queue")
    end
  end
  def peek(q)
    check_can_perform(q)
    return (__tmp3 = q.data; __tmp3.is_a?(Hash) ? __tmp3[q.front] : _idx(__tmp3, q.front))
  end
  def enqueue(q, value)
    check_is_full(q)
    if !is_empty(q)
      q.rear = (__tmp4 = q.next_; __tmp4.is_a?(Hash) ? __tmp4[q.rear] : _idx(__tmp4, q.rear))
    end
    data = q.data
    data[q.rear] = value
    q.data = data
    return q
  end
  def dequeue(q)
    check_can_perform(q)
    data = q.data
    val = (__tmp5 = data; __tmp5.is_a?(Hash) ? __tmp5[q.front] : _idx(__tmp5, q.front))
    data[q.front] = ""
    q.data = data
    if !_eq(q.front, q.rear)
      q.front = (__tmp6 = q.next_; __tmp6.is_a?(Hash) ? __tmp6[q.front] : _idx(__tmp6, q.front))
    end
    return DequeueResult.new(queue: q, value: val)
  end
  def main()
    q = create_queue(3)
    puts(_str(is_empty(q)))
    q = enqueue(q, "a")
    q = enqueue(q, "b")
    puts(peek(q))
    res = dequeue(q)
    q = res.queue
    puts(res.value)
    res = dequeue(q)
    q = res.queue
    puts(res.value)
    puts(_str(is_empty(q)))
  end
  Object.send(:remove_const, :CircularQueue) if Object.const_defined?(:CircularQueue)
  Object.const_set(:CircularQueue, Struct.new(:data, :next_, :prev, :front, :rear, keyword_init: true))
  Object.send(:remove_const, :DequeueResult) if Object.const_defined?(:DequeueResult)
  Object.const_set(:DequeueResult, Struct.new(:queue, :value, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
