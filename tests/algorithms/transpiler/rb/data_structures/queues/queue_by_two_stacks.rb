# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def new_queue(items)
    return Queue.new(stack1: items, stack2: [])
  end
  def len_queue(q)
    return _add(_len(q.stack1), _len(q.stack2))
  end
  def str_queue(q)
    items = []
    i = _len(q.stack2) - 1
    while i >= 0
      items = _append(items, (__tmp1 = q.stack2; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i)))
      i = i - 1
    end
    j = 0
    while j < _len(q.stack1)
      items = _append(items, (__tmp2 = q.stack1; __tmp2.is_a?(Hash) ? __tmp2[j] : _idx(__tmp2, j)))
      j = _add(j, 1)
    end
    s = "Queue(("
    k = 0
    while k < _len(items)
      s = _add(s, _str((__tmp3 = items; __tmp3.is_a?(Hash) ? __tmp3[k] : _idx(__tmp3, k))))
      if k < _len(items) - 1
        s = _add(s, ", ")
      end
      k = _add(k, 1)
    end
    s = _add(s, "))")
    return s
  end
  def put(q, item)
    s1 = q.stack1
    s1 = _append(s1, item)
    return Queue.new(stack1: s1, stack2: q.stack2)
  end
  def get(q)
    s1 = q.stack1
    s2 = q.stack2
    if _eq(_len(s2), 0)
      while _len(s1) > 0
        idx = _len(s1) - 1
        v = (__tmp4 = s1; __tmp4.is_a?(Hash) ? __tmp4[idx] : _idx(__tmp4, idx))
        new_s1 = []
        i = 0
        while i < idx
          new_s1 = _append(new_s1, (__tmp5 = s1; __tmp5.is_a?(Hash) ? __tmp5[i] : _idx(__tmp5, i)))
          i = _add(i, 1)
        end
        s1 = new_s1
        s2 = _append(s2, v)
      end
    end
    if _eq(_len(s2), 0)
      panic("Queue is empty")
    end
    idx2 = _len(s2) - 1
    value = (__tmp6 = s2; __tmp6.is_a?(Hash) ? __tmp6[idx2] : _idx(__tmp6, idx2))
    new_s2 = []
    j = 0
    while j < idx2
      new_s2 = _append(new_s2, (__tmp7 = s2; __tmp7.is_a?(Hash) ? __tmp7[j] : _idx(__tmp7, j)))
      j = _add(j, 1)
    end
    s2 = new_s2
    return GetResult.new(queue: Queue.new(stack1: s1, stack2: s2), value: value)
  end
  Object.send(:remove_const, :Queue) if Object.const_defined?(:Queue)
  Object.const_set(:Queue, Struct.new(:stack1, :stack2, keyword_init: true))
  Object.send(:remove_const, :GetResult) if Object.const_defined?(:GetResult)
  Object.const_set(:GetResult, Struct.new(:queue, :value, keyword_init: true))
  $q = new_queue([10, 20, 30])
  $r1 = get($q)
  $q = $r1.queue
  puts($r1.value)
  $q = put($q, 40)
  $r2 = get($q)
  $q = $r2.queue
  puts($r2.value)
  $r3 = get($q)
  $q = $r3.queue
  puts($r3.value)
  puts(len_queue($q))
  $r4 = get($q)
  $q = $r4.queue
  puts($r4.value)
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
