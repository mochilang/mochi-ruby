# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def new_queue(items)
    return Queue.new(entries: items)
  end
  def len_queue(q)
    return _len(q.entries)
  end
  def str_queue(q)
    s = "Queue(("
    i = 0
    while i < _len(q.entries)
      s = _add(s, _str((__tmp1 = q.entries; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i))))
      if i < _len(q.entries) - 1
        s = _add(s, ", ")
      end
      i = _add(i, 1)
    end
    s = _add(s, "))")
    return s
  end
  def put(q, item)
    e = q.entries
    e = _append(e, item)
    return Queue.new(entries: e)
  end
  def get(q)
    if _eq(_len(q.entries), 0)
      panic("Queue is empty")
    end
    value = (__tmp2 = q.entries; __tmp2.is_a?(Hash) ? __tmp2[0] : _idx(__tmp2, 0))
    new_entries = []
    i = 1
    while i < _len(q.entries)
      new_entries = _append(new_entries, (__tmp3 = q.entries; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)))
      i = _add(i, 1)
    end
    return GetResult.new(queue: Queue.new(entries: new_entries), value: value)
  end
  def rotate(q, rotation)
    e = q.entries
    r = 0
    while r < rotation
      if _len(e) > 0
        first = (__tmp4 = e; __tmp4.is_a?(Hash) ? __tmp4[0] : _idx(__tmp4, 0))
        rest = []
        i = 1
        while i < _len(e)
          rest = _append(rest, (__tmp5 = e; __tmp5.is_a?(Hash) ? __tmp5[i] : _idx(__tmp5, i)))
          i = _add(i, 1)
        end
        rest = _append(rest, first)
        e = rest
      end
      r = _add(r, 1)
    end
    return Queue.new(entries: e)
  end
  def get_front(q)
    return (__tmp6 = q.entries; __tmp6.is_a?(Hash) ? __tmp6[0] : _idx(__tmp6, 0))
  end
  Object.send(:remove_const, :Queue) if Object.const_defined?(:Queue)
  Object.const_set(:Queue, Struct.new(:entries, keyword_init: true))
  Object.send(:remove_const, :GetResult) if Object.const_defined?(:GetResult)
  Object.const_set(:GetResult, Struct.new(:queue, :value, keyword_init: true))
  $q = new_queue([])
  puts(len_queue($q))
  $q = put($q, 10)
  $q = put($q, 20)
  $q = put($q, 30)
  $q = put($q, 40)
  puts(str_queue($q))
  $res = get($q)
  $q = $res.queue
  puts($res.value)
  puts(str_queue($q))
  $q = rotate($q, 2)
  puts(str_queue($q))
  $front = get_front($q)
  puts($front)
  puts(str_queue($q))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
