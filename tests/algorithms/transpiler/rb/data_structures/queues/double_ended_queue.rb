# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def empty_deque()
    return Deque.new(data: [])
  end
  def push_back(dq, value)
    return Deque.new(data: _append(dq.data, value))
  end
  def push_front(dq, value)
    res = [value]
    i = 0
    while i < _len(dq.data)
      res = _append(res, (__tmp1 = dq.data; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i)))
      i = _add(i, 1)
    end
    return Deque.new(data: res)
  end
  def extend_back(dq, values)
    res = dq.data
    i = 0
    while i < _len(values)
      res = _append(res, (__tmp2 = values; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i)))
      i = _add(i, 1)
    end
    return Deque.new(data: res)
  end
  def extend_front(dq, values)
    res = []
    i = _len(values) - 1
    while i >= 0
      res = _append(res, (__tmp3 = values; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)))
      i = i - 1
    end
    j = 0
    while j < _len(dq.data)
      res = _append(res, (__tmp4 = dq.data; __tmp4.is_a?(Hash) ? __tmp4[j] : _idx(__tmp4, j)))
      j = _add(j, 1)
    end
    return Deque.new(data: res)
  end
  def pop_back(dq)
    if _eq(_len(dq.data), 0)
      panic("pop from empty deque")
    end
    res = []
    i = 0
    while i < _len(dq.data) - 1
      res = _append(res, (__tmp5 = dq.data; __tmp5.is_a?(Hash) ? __tmp5[i] : _idx(__tmp5, i)))
      i = _add(i, 1)
    end
    return PopResult.new(deque: Deque.new(data: res), value: (__tmp6 = dq.data; __tmp6.is_a?(Hash) ? __tmp6[_len(dq.data) - 1] : _idx(__tmp6, _len(dq.data) - 1)))
  end
  def pop_front(dq)
    if _eq(_len(dq.data), 0)
      panic("popleft from empty deque")
    end
    res = []
    i = 1
    while i < _len(dq.data)
      res = _append(res, (__tmp7 = dq.data; __tmp7.is_a?(Hash) ? __tmp7[i] : _idx(__tmp7, i)))
      i = _add(i, 1)
    end
    return PopResult.new(deque: Deque.new(data: res), value: (__tmp8 = dq.data; __tmp8.is_a?(Hash) ? __tmp8[0] : _idx(__tmp8, 0)))
  end
  def is_empty(dq)
    return _eq(_len(dq.data), 0)
  end
  def length(dq)
    return _len(dq.data)
  end
  def to_string(dq)
    if _eq(_len(dq.data), 0)
      return "[]"
    end
    s = _add("[", _str((__tmp9 = dq.data; __tmp9.is_a?(Hash) ? __tmp9[0] : _idx(__tmp9, 0))))
    i = 1
    while i < _len(dq.data)
      s = _add(_add(s, ", "), _str((__tmp10 = dq.data; __tmp10.is_a?(Hash) ? __tmp10[i] : _idx(__tmp10, i))))
      i = _add(i, 1)
    end
    return _add(s, "]")
  end
  def main()
    dq = empty_deque()
    dq = push_back(dq, 2)
    dq = push_front(dq, 1)
    dq = extend_back(dq, [3, 4])
    dq = extend_front(dq, [0])
    puts(to_string(dq))
    r = pop_back(dq)
    dq = r.deque
    puts(r.value)
    r = pop_front(dq)
    dq = r.deque
    puts(r.value)
    puts(to_string(dq))
    puts((is_empty(empty_deque()) ? 'true' : 'false'))
  end
  Object.send(:remove_const, :Deque) if Object.const_defined?(:Deque)
  Object.const_set(:Deque, Struct.new(:data, keyword_init: true))
  Object.send(:remove_const, :PopResult) if Object.const_defined?(:PopResult)
  Object.const_set(:PopResult, Struct.new(:deque, :value, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
