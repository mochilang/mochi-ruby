# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def new_queue()
    return LinkedQueue.new(nodes: [], front: 0 - 1, rear: 0 - 1)
  end
  def is_empty(q)
    return _eq(q.front, 0 - 1)
  end
  def put(q, item)
    node = Node.new(data: item, next_: 0 - 1)
    q.nodes = _append(q.nodes, node)
    idx = _len(q.nodes) - 1
    if _eq(q.front, 0 - 1)
      q.front = idx
      q.rear = idx
    else
      nodes = q.nodes
      (__tmp1 = nodes; __tmp1.is_a?(Hash) ? __tmp1[q.rear] : _idx(__tmp1, q.rear))["next_"] = idx
      q.nodes = nodes
      q.rear = idx
    end
  end
  def get(q)
    if is_empty(q)
      panic("dequeue from empty queue")
    end
    idx = q.front
    node = (__tmp2 = q.nodes; __tmp2.is_a?(Hash) ? __tmp2[idx] : _idx(__tmp2, idx))
    q.front = node.next_
    if _eq(q.front, 0 - 1)
      q.rear = 0 - 1
    end
    return node.data
  end
  def length(q)
    count = 0
    idx = q.front
    while !_eq(idx, 0 - 1)
      count = _add(count, 1)
      idx = (__tmp3 = q.nodes; __tmp3.is_a?(Hash) ? __tmp3[idx] : _idx(__tmp3, idx)).next_
    end
    return count
  end
  def to_string(q)
    res = ""
    idx = q.front
    first = true
    while !_eq(idx, 0 - 1)
      node = (__tmp4 = q.nodes; __tmp4.is_a?(Hash) ? __tmp4[idx] : _idx(__tmp4, idx))
      if first
        res = node.data
        first = false
      else
        res = _add(_add(res, " <- "), node.data)
      end
      idx = node.next_
    end
    return res
  end
  def clear(q)
    q.nodes = []
    q.front = 0 - 1
    q.rear = 0 - 1
  end
  Object.send(:remove_const, :Node) if Object.const_defined?(:Node)
  Object.const_set(:Node, Struct.new(:data, :next_, keyword_init: true))
  Object.send(:remove_const, :LinkedQueue) if Object.const_defined?(:LinkedQueue)
  Object.const_set(:LinkedQueue, Struct.new(:nodes, :front, :rear, keyword_init: true))
  $queue = new_queue()
  puts(_str(is_empty($queue)))
  put($queue, "5")
  put($queue, "9")
  put($queue, "python")
  puts(_str(is_empty($queue)))
  puts(get($queue))
  put($queue, "algorithms")
  puts(get($queue))
  puts(get($queue))
  puts(get($queue))
  puts(_str(is_empty($queue)))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
