# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def make_stack(limit)
    return Stack.new(items: [], limit: limit)
  end
  def is_empty(s)
    return _eq(_len(s.items), 0)
  end
  def size(s)
    return _len(s.items)
  end
  def is_full(s)
    return _len(s.items) >= s.limit
  end
  def push(s, item)
    if is_full(s)
      panic("stack overflow")
    end
    s.items = _append(s.items, item)
  end
  def pop(s)
    if is_empty(s)
      panic("stack underflow")
    end
    n = _len(s.items)
    val = (__tmp1 = s.items; __tmp1.is_a?(Hash) ? __tmp1[n - 1] : _idx(__tmp1, n - 1))
    s.items = s.items[0...n - 1]
    return val
  end
  def peek(s)
    if is_empty(s)
      panic("peek from empty stack")
    end
    return (__tmp2 = s.items; __tmp2.is_a?(Hash) ? __tmp2[_len(s.items) - 1] : _idx(__tmp2, _len(s.items) - 1))
  end
  def contains(s, item)
    i = 0
    while i < _len(s.items)
      if _eq((__tmp3 = s.items; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)), item)
        return true
      end
      i = _add(i, 1)
    end
    return false
  end
  def stack_repr(s)
    return ((x = s.items); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s)
  end
  def main()
    s = make_stack(5)
    puts(_str(is_empty(s)))
    push(s, 0)
    push(s, 1)
    push(s, 2)
    puts(_str(peek(s)))
    puts(_str(size(s)))
    puts(_str(is_full(s)))
    push(s, 3)
    push(s, 4)
    puts(_str(is_full(s)))
    puts(stack_repr(s))
    puts(_str(pop(s)))
    puts(_str(peek(s)))
    puts(_str(contains(s, 1)))
    puts(_str(contains(s, 9)))
  end
  Object.send(:remove_const, :Stack) if Object.const_defined?(:Stack)
  Object.const_set(:Stack, Struct.new(:items, :limit, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
