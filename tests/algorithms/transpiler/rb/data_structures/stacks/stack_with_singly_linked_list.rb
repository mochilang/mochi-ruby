# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def empty_stack()
    return Stack.new(nodes: [], top: (-1))
  end
  def is_empty(stack)
    return _eq(stack.top, (-1))
  end
  def push(stack, item)
    new_node = Node.new(value: item, next_: stack.top)
    new_nodes = stack.nodes
    new_nodes = _append(new_nodes, new_node)
    new_top = _len(new_nodes) - 1
    return Stack.new(nodes: new_nodes, top: new_top)
  end
  def pop(stack)
    if _eq(stack.top, (-1))
      panic("pop from empty stack")
    end
    node = ((__tmp1 = stack.nodes; __tmp1.is_a?(Hash) ? __tmp1[stack.top] : _idx(__tmp1, stack.top)))
    new_top = node.next_
    new_stack = Stack.new(nodes: stack.nodes, top: new_top)
    return PopResult.new(stack: new_stack, value: node.value)
  end
  def peek(stack)
    if _eq(stack.top, (-1))
      panic("peek from empty stack")
    end
    node = ((__tmp2 = stack.nodes; __tmp2.is_a?(Hash) ? __tmp2[stack.top] : _idx(__tmp2, stack.top)))
    return node.value
  end
  def clear(stack)
    return Stack.new(nodes: [], top: (-1))
  end
  def main()
    stack = empty_stack()
    puts((is_empty(stack) ? 'true' : 'false'))
    stack = push(stack, "5")
    stack = push(stack, "9")
    stack = push(stack, "python")
    puts((is_empty(stack) ? 'true' : 'false'))
    res = pop(stack)
    stack = res.stack
    puts(res.value)
    stack = push(stack, "algorithms")
    res = pop(stack)
    stack = res.stack
    puts(res.value)
    res = pop(stack)
    stack = res.stack
    puts(res.value)
    res = pop(stack)
    stack = res.stack
    puts(res.value)
    puts((is_empty(stack) ? 'true' : 'false'))
  end
  Object.send(:remove_const, :Node) if Object.const_defined?(:Node)
  Object.const_set(:Node, Struct.new(:value, :next_, keyword_init: true))
  Object.send(:remove_const, :Stack) if Object.const_defined?(:Stack)
  Object.const_set(:Stack, Struct.new(:nodes, :top, keyword_init: true))
  Object.send(:remove_const, :PopResult) if Object.const_defined?(:PopResult)
  Object.const_set(:PopResult, Struct.new(:stack, :value, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
