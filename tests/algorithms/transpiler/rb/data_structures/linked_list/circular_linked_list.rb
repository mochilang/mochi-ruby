# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def empty_list()
    return CircularLinkedList.new(data: [])
  end
  def length(list)
    return _len(list.data)
  end
  def is_empty(list)
    return _eq(_len(list.data), 0)
  end
  def to_string(list)
    if _eq(_len(list.data), 0)
      return ""
    end
    s = _str((__tmp1 = list.data; __tmp1.is_a?(Hash) ? __tmp1[0] : _idx(__tmp1, 0)))
    i = 1
    while i < _len(list.data)
      s = _add(_add(s, "->"), _str((__tmp2 = list.data; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i))))
      i = _add(i, 1)
    end
    return s
  end
  def insert_nth(list, index, value)
    if index < 0 || index > _len(list.data)
      panic("index out of range")
    end
    res = []
    i = 0
    while i < index
      res = _append(res, (__tmp3 = list.data; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)))
      i = _add(i, 1)
    end
    res = _append(res, value)
    while i < _len(list.data)
      res = _append(res, (__tmp4 = list.data; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)))
      i = _add(i, 1)
    end
    return CircularLinkedList.new(data: res)
  end
  def insert_head(list, value)
    return insert_nth(list, 0, value)
  end
  def insert_tail(list, value)
    return insert_nth(list, _len(list.data), value)
  end
  def delete_nth(list, index)
    if index < 0 || index >= _len(list.data)
      panic("index out of range")
    end
    res = []
    i = 0
    val = 0
    while i < _len(list.data)
      if _eq(i, index)
        val = (__tmp5 = list.data; __tmp5.is_a?(Hash) ? __tmp5[i] : _idx(__tmp5, i))
      else
        res = _append(res, (__tmp6 = list.data; __tmp6.is_a?(Hash) ? __tmp6[i] : _idx(__tmp6, i)))
      end
      i = _add(i, 1)
    end
    return DeleteResult.new(list: CircularLinkedList.new(data: res), value: val)
  end
  def delete_front(list)
    return delete_nth(list, 0)
  end
  def delete_tail(list)
    return delete_nth(list, _len(list.data) - 1)
  end
  def main()
    cll = empty_list()
    i = 0
    while i < 5
      cll = insert_tail(cll, _add(i, 1))
      i = _add(i, 1)
    end
    puts(to_string(cll))
    cll = insert_tail(cll, 6)
    puts(to_string(cll))
    cll = insert_head(cll, 0)
    puts(to_string(cll))
    res = delete_front(cll)
    cll = res.list
    puts(res.value)
    res = delete_tail(cll)
    cll = res.list
    puts(res.value)
    res = delete_nth(cll, 2)
    cll = res.list
    puts(res.value)
    puts(to_string(cll))
  end
  Object.send(:remove_const, :CircularLinkedList) if Object.const_defined?(:CircularLinkedList)
  Object.const_set(:CircularLinkedList, Struct.new(:data, keyword_init: true))
  Object.send(:remove_const, :DeleteResult) if Object.const_defined?(:DeleteResult)
  Object.const_set(:DeleteResult, Struct.new(:list, :value, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
