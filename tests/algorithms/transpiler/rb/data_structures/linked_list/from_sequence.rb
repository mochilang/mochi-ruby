# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def make_linked_list(elements)
    if _eq(_len(elements), 0)
      panic("The Elements List is empty")
    end
    $nodes = []
    $nodes = _append($nodes, Node.new(data: (__tmp1 = elements; __tmp1.is_a?(Hash) ? __tmp1[0] : _idx(__tmp1, 0)), next_: $NIL))
    head = 0
    current = head
    i = 1
    while i < _len(elements)
      $nodes = _append($nodes, Node.new(data: (__tmp2 = elements; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i)), next_: $NIL))
      (__tmp3 = $nodes; __tmp3.is_a?(Hash) ? __tmp3[current] : _idx(__tmp3, current))["next_"] = _len($nodes) - 1
      current = _len($nodes) - 1
      i = _add(i, 1)
    end
    return head
  end
  def node_to_string(head)
    s = ""
    index = head
    while !_eq(index, $NIL)
      node = (__tmp4 = $nodes; __tmp4.is_a?(Hash) ? __tmp4[index] : _idx(__tmp4, index))
      s = _add(_add(_add(s, "<"), _str((__tmp5 = node; __tmp5.is_a?(Hash) ? __tmp5["data"] : _idx(__tmp5, "data")))), "> ---> ")
      index = (__tmp6 = node; __tmp6.is_a?(Hash) ? __tmp6["next_"] : _idx(__tmp6, "next_"))
    end
    s = _add(s, "<END>")
    return s
  end
  def main()
    list_data = [1, 3, 5, 32, 44, 12, 43]
    puts(_add("List: ", ((x = list_data); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s)))
    puts("Creating Linked List from List.")
    head = make_linked_list(list_data)
    puts("Linked List:")
    puts(node_to_string(head))
  end
  Object.send(:remove_const, :Node) if Object.const_defined?(:Node)
  Object.const_set(:Node, Struct.new(:data, :next_, keyword_init: true))
  $NIL = 0 - 1
  $nodes = []
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
