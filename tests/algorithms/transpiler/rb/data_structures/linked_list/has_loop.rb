# Generated by Mochi transpiler v0.10.72 on 2025-08-24 23:17 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def has_loop(nodes, head)
    slow = head
    fast = head
    while !_eq(fast, 0 - 1)
      fast_node1 = (__tmp1 = nodes; __tmp1.is_a?(Hash) ? __tmp1[fast] : _idx(__tmp1, fast))
      if _eq(fast_node1.next_, 0 - 1)
        return false
      end
      fast_node2 = (__tmp2 = nodes; __tmp2.is_a?(Hash) ? __tmp2[fast_node1.next_] : _idx(__tmp2, fast_node1.next_))
      if _eq(fast_node2.next_, 0 - 1)
        return false
      end
      slow_node = (__tmp3 = nodes; __tmp3.is_a?(Hash) ? __tmp3[slow] : _idx(__tmp3, slow))
      slow = slow_node.next_
      fast = fast_node2.next_
      if _eq(slow, fast)
        return true
      end
    end
    return false
  end
  def make_nodes(values)
    nodes = []
    i = 0
    while i < _len(values)
      next_idx = (_eq(i, _len(values) - 1) ? 0 - 1 : _add(i, 1))
      nodes = _append(nodes, Node.new(data: (__tmp4 = values; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)), next_: next_idx))
      i = _add(i, 1)
    end
    return nodes
  end
  def main()
    list1 = make_nodes([1, 2, 3, 4])
    puts(_str(has_loop(list1, 0)))
    (__tmp5 = list1; __tmp5.is_a?(Hash) ? __tmp5[3] : _idx(__tmp5, 3))["next_"] = 1
    puts(_str(has_loop(list1, 0)))
    list2 = make_nodes([5, 6, 5, 6])
    puts(_str(has_loop(list2, 0)))
    list3 = make_nodes([1])
    puts(_str(has_loop(list3, 0)))
  end
  Object.send(:remove_const, :Node) if Object.const_defined?(:Node)
  Object.const_set(:Node, Struct.new(:data, :next_, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
