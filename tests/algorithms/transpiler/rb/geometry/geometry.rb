# Generated by Mochi transpiler v0.10.73 on 2025-08-25 16:44 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    idx += arr.length if idx < 0
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  arr = [] if arr.nil?
  x = x.clone if x.is_a?(Array)
  arr << x
  arr
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def make_angle(deg)
    if deg < 0.0 || deg > 360.0
      panic("degrees must be between 0 and 360")
    end
    return Angle.new(degrees: deg)
  end
  def make_side(length, angle)
    if length <= 0.0
      panic("length must be positive")
    end
    return Side.new(length: length, angle: angle, next_: -1)
  end
  def ellipse_area(e)
    return $PI * e.major * e.minor
  end
  def ellipse_perimeter(e)
    return $PI * (_add(e.major, e.minor))
  end
  def circle_area(c)
    e = Ellipse.new(major: c.radius, minor: c.radius)
    area = ellipse_area(e)
    return area
  end
  def circle_perimeter(c)
    e = Ellipse.new(major: c.radius, minor: c.radius)
    per = ellipse_perimeter(e)
    return per
  end
  def circle_diameter(c)
    return c.radius * 2.0
  end
  def circle_max_parts(num_cuts)
    if num_cuts < 0.0
      panic("num_cuts must be positive")
    end
    return (_add(_add(num_cuts, 2.0), num_cuts * num_cuts)) * 0.5
  end
  def make_polygon()
    s = []
    return Polygon.new(sides: s)
  end
  def polygon_add_side(p, s)
    p.sides = _append(p.sides, s)
  end
  def polygon_get_side(p, index)
    return (__tmp1 = p.sides; __tmp1.is_a?(Hash) ? __tmp1[index] : _idx(__tmp1, index))
  end
  def polygon_set_side(p, index, s)
    tmp = p.sides
    tmp[index] = s
    p.sides = tmp
  end
  def make_rectangle(short_len, long_len)
    if short_len <= 0.0 || long_len <= 0.0
      panic("length must be positive")
    end
    short = make_side(short_len, make_angle(90.0))
    long = make_side(long_len, make_angle(90.0))
    p = make_polygon()
    polygon_add_side(p, short)
    polygon_add_side(p, long)
    return Rectangle.new(short_side: short, long_side: long, poly: p)
  end
  def rectangle_perimeter(r)
    return (_add(r.short_side.length, r.long_side.length)) * 2.0
  end
  def rectangle_area(r)
    return r.short_side.length * r.long_side.length
  end
  def make_square(side_len)
    rect = make_rectangle(side_len, side_len)
    return Square.new(side: rect.short_side, rect: rect)
  end
  def square_perimeter(s)
    p = rectangle_perimeter(s.rect)
    return p
  end
  def square_area(s)
    a = rectangle_area(s.rect)
    return a
  end
  def main()
    a = make_angle(90.0)
    puts(a.degrees)
    s = make_side(5.0, a)
    puts(s.length)
    e = Ellipse.new(major: 5.0, minor: 10.0)
    puts(ellipse_area(e))
    puts(ellipse_perimeter(e))
    c = Circle.new(radius: 5.0)
    puts(circle_area(c))
    puts(circle_perimeter(c))
    puts(circle_diameter(c))
    puts(circle_max_parts(7.0))
    r = make_rectangle(5.0, 10.0)
    puts(rectangle_perimeter(r))
    puts(rectangle_area(r))
    q = make_square(5.0)
    puts(square_perimeter(q))
    puts(square_area(q))
  end
  $PI = 3.141592653589793
  Object.send(:remove_const, :Angle) if Object.const_defined?(:Angle)
  Object.const_set(:Angle, Struct.new(:degrees, keyword_init: true))
  Object.send(:remove_const, :Side) if Object.const_defined?(:Side)
  Object.const_set(:Side, Struct.new(:length, :angle, :next_, keyword_init: true))
  Object.send(:remove_const, :Ellipse) if Object.const_defined?(:Ellipse)
  Object.const_set(:Ellipse, Struct.new(:major, :minor, keyword_init: true))
  Object.send(:remove_const, :Circle) if Object.const_defined?(:Circle)
  Object.const_set(:Circle, Struct.new(:radius, keyword_init: true))
  Object.send(:remove_const, :Polygon) if Object.const_defined?(:Polygon)
  Object.const_set(:Polygon, Struct.new(:sides, keyword_init: true))
  Object.send(:remove_const, :Rectangle) if Object.const_defined?(:Rectangle)
  Object.const_set(:Rectangle, Struct.new(:short_side, :long_side, :poly, keyword_init: true))
  Object.send(:remove_const, :Square) if Object.const_defined?(:Square)
  Object.const_set(:Square, Struct.new(:side, :rect, keyword_init: true))
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
