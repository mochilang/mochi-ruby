# Generated by Mochi transpiler v0.10.67 on 2025-08-17 12:10 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def is_geometric_series(series)
    if _eq(_len(series), 0)
      panic("Input list must be a non empty list")
    end
    if _eq(_len(series), 1)
      return true
    end
    if _eq((__tmp1 = series; __tmp1.is_a?(Hash) ? __tmp1[0] : _idx(__tmp1, 0)), 0.0)
      return false
    end
    ratio = (__tmp2 = series; __tmp2.is_a?(Hash) ? __tmp2[1] : _idx(__tmp2, 1)) / (__tmp3 = series; __tmp3.is_a?(Hash) ? __tmp3[0] : _idx(__tmp3, 0))
    i = 0
    while i < _len(series) - 1
      if _eq((__tmp4 = series; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)), 0.0)
        return false
      end
      if !_eq((__tmp5 = series; __tmp5.is_a?(Hash) ? __tmp5[_add(i, 1)] : _idx(__tmp5, _add(i, 1))) / (__tmp6 = series; __tmp6.is_a?(Hash) ? __tmp6[i] : _idx(__tmp6, i)), ratio)
        return false
      end
      i = _add(i, 1)
    end
    return true
  end
  def geometric_mean(series)
    if _eq(_len(series), 0)
      panic("Input list must be a non empty list")
    end
    product = 1.0
    i = 0
    while i < _len(series)
      product = product * (__tmp7 = series; __tmp7.is_a?(Hash) ? __tmp7[i] : _idx(__tmp7, i))
      i = _add(i, 1)
    end
    n = _len(series)
    return nth_root(product, n)
  end
  def pow_float(base, exp)
    result = 1.0
    i = 0
    while i < exp
      result = result * base
      i = _add(i, 1)
    end
    return result
  end
  def nth_root(value, n)
    if _eq(value, 0.0)
      return 0.0
    end
    low = 0.0
    high = value
    if value < 1.0
      high = 1.0
    end
    mid = (_add(low, high)) / 2.0
    i = 0
    while i < 40
      mp = pow_float(mid, n)
      if mp > value
        high = mid
      else
        low = mid
      end
      mid = (_add(low, high)) / 2.0
      i = _add(i, 1)
    end
    return mid
  end
  def test_geometric()
    a = [2.0, 4.0, 8.0]
    if !is_geometric_series(a)
      panic("expected geometric series")
    end
    b = [1.0, 2.0, 3.0]
    if is_geometric_series(b)
      panic("expected non geometric series")
    end
  end
  def main()
    test_geometric()
    puts(geometric_mean([2.0, 4.0, 8.0]))
  end
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
