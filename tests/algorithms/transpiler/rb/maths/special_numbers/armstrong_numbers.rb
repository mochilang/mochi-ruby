# Generated by Mochi transpiler v0.10.67 on 2025-08-17 12:10 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def pow_int(base, exp)
    result = 1
    i = 0
    while i < exp
      result = result * base
      i = _add(i, 1)
    end
    return result
  end
  def armstrong_number(n)
    if n < 1
      return false
    end
    digits = 0
    temp = n
    while temp > 0
      temp = temp / 10
      digits = _add(digits, 1)
    end
    total = 0
    temp = n
    while temp > 0
      rem = temp % 10
      total = _add(total, pow_int(rem, digits))
      temp = temp / 10
    end
    return _eq(total, n)
  end
  def pluperfect_number(n)
    if n < 1
      return false
    end
    digit_histogram = []
    i = 0
    while i < 10
      digit_histogram = _append(digit_histogram, 0)
      i = _add(i, 1)
    end
    digit_total = 0
    temp = n
    while temp > 0
      rem = temp % 10
      digit_histogram[rem] = _add((__tmp1 = digit_histogram; __tmp1.is_a?(Hash) ? __tmp1[rem] : _idx(__tmp1, rem)), 1)
      digit_total = _add(digit_total, 1)
      temp = temp / 10
    end
    total = 0
    i = 0
    while i < 10
      if (__tmp2 = digit_histogram; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i)) > 0
        total = _add(total, (__tmp3 = digit_histogram; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)) * pow_int(i, digit_total))
      end
      i = _add(i, 1)
    end
    return _eq(total, n)
  end
  def narcissistic_number(n)
    if n < 1
      return false
    end
    digits = 0
    temp = n
    while temp > 0
      temp = temp / 10
      digits = _add(digits, 1)
    end
    temp = n
    total = 0
    while temp > 0
      rem = temp % 10
      total = _add(total, pow_int(rem, digits))
      temp = temp / 10
    end
    return _eq(total, n)
  end
  puts((armstrong_number(371) ? 'true' : 'false'))
  puts((armstrong_number(200) ? 'true' : 'false'))
  puts((pluperfect_number(371) ? 'true' : 'false'))
  puts((pluperfect_number(200) ? 'true' : 'false'))
  puts((narcissistic_number(371) ? 'true' : 'false'))
  puts((narcissistic_number(200) ? 'true' : 'false'))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
