# Generated by Mochi transpiler v0.10.67 on 2025-08-17 13:28 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def factorial(n)
    if n < 0
      panic("factorial() not defined for negative values")
    end
    value = 1
    i = 1
    while i <= n
      value = value * i
      i = _add(i, 1)
    end
    return value
  end
  def factorial_recursive(n)
    if n < 0
      panic("factorial() not defined for negative values")
    end
    if n <= 1
      return 1
    end
    return n * factorial_recursive(n - 1)
  end
  def test_zero()
    if !_eq(factorial(0), 1)
      panic("factorial(0) failed")
    end
    if !_eq(factorial_recursive(0), 1)
      panic("factorial_recursive(0) failed")
    end
  end
  def test_positive_integers()
    if !_eq(factorial(1), 1)
      panic("factorial(1) failed")
    end
    if !_eq(factorial_recursive(1), 1)
      panic("factorial_recursive(1) failed")
    end
    if !_eq(factorial(5), 120)
      panic("factorial(5) failed")
    end
    if !_eq(factorial_recursive(5), 120)
      panic("factorial_recursive(5) failed")
    end
    if !_eq(factorial(7), 5040)
      panic("factorial(7) failed")
    end
    if !_eq(factorial_recursive(7), 5040)
      panic("factorial_recursive(7) failed")
    end
  end
  def test_large_number()
    if !_eq(factorial(10), 3628800)
      panic("factorial(10) failed")
    end
    if !_eq(factorial_recursive(10), 3628800)
      panic("factorial_recursive(10) failed")
    end
  end
  def run_tests()
    test_zero()
    test_positive_integers()
    test_large_number()
  end
  def main()
    run_tests()
    puts(factorial(6))
  end
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
