# Generated by Mochi transpiler v0.10.66 on 2025-08-14 21:50 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if arr.is_a?(Array) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  a ** b
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def ucal(u, p)
    temp = u
    i = 1
    while i < p
      temp = temp * (u - ((i).to_f))
      i = _add(i, 1)
    end
    return temp
  end
  def factorial(n)
    result = 1.0
    i = 2
    while i <= n
      result = result * ((i).to_f)
      i = _add(i, 1)
    end
    return result
  end
  def newton_forward_interpolation(x, y0, value)
    n = _len(x)
    y = []
    i = 0
    while i < n
      row = []
      j = 0
      while j < n
        row = _append(row, 0.0)
        j = _add(j, 1)
      end
      y = _append(y, row)
      i = _add(i, 1)
    end
    i = 0
    while i < n
      (__tmp1 = y; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i))[0] = (__tmp2 = y0; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i))
      i = _add(i, 1)
    end
    i1 = 1
    while i1 < n
      j1 = 0
      while j1 < n - i1
        (__tmp3 = y; __tmp3.is_a?(Hash) ? __tmp3[j1] : _idx(__tmp3, j1))[i1] = (__tmp4 = (__tmp5 = y; __tmp5.is_a?(Hash) ? __tmp5[_add(j1, 1)] : _idx(__tmp5, _add(j1, 1))); __tmp4.is_a?(Hash) ? __tmp4[i1 - 1] : _idx(__tmp4, i1 - 1)) - (__tmp6 = (__tmp7 = y; __tmp7.is_a?(Hash) ? __tmp7[j1] : _idx(__tmp7, j1)); __tmp6.is_a?(Hash) ? __tmp6[i1 - 1] : _idx(__tmp6, i1 - 1))
        j1 = _add(j1, 1)
      end
      i1 = _add(i1, 1)
    end
    u = (value - (__tmp8 = x; __tmp8.is_a?(Hash) ? __tmp8[0] : _idx(__tmp8, 0))) / ((__tmp9 = x; __tmp9.is_a?(Hash) ? __tmp9[1] : _idx(__tmp9, 1)) - (__tmp10 = x; __tmp10.is_a?(Hash) ? __tmp10[0] : _idx(__tmp10, 0)))
    sum = (__tmp11 = (__tmp12 = y; __tmp12.is_a?(Hash) ? __tmp12[0] : _idx(__tmp12, 0)); __tmp11.is_a?(Hash) ? __tmp11[0] : _idx(__tmp11, 0))
    k = 1
    while k < n
      sum = _add(sum, (ucal(u, k) * (__tmp13 = (__tmp14 = y; __tmp14.is_a?(Hash) ? __tmp14[0] : _idx(__tmp14, 0)); __tmp13.is_a?(Hash) ? __tmp13[k] : _idx(__tmp13, k))) / factorial(k))
      k = _add(k, 1)
    end
    return sum
  end
  $x_points = [0.0, 1.0, 2.0, 3.0]
  $y_points = [0.0, 1.0, 8.0, 27.0]
  puts(_str(newton_forward_interpolation($x_points, $y_points, 1.5)))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
