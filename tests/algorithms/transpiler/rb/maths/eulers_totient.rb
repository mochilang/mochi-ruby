# Generated by Mochi transpiler v0.10.67 on 2025-08-16 19:42 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if arr.is_a?(Array) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def totient(n)
    is_prime = []
    totients = []
    primes = []
    i = 0
    while i <= n
      is_prime = _append(is_prime, true)
      totients = _append(totients, i - 1)
      i = _add(i, 1)
    end
    i = 2
    while i <= n
      if (__tmp1 = is_prime; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i))
        primes = _append(primes, i)
      end
      j = 0
      while j < _len(primes)
        p = (__tmp2 = primes; __tmp2.is_a?(Hash) ? __tmp2[j] : _idx(__tmp2, j))
        if i * p >= n
          break
        end
        is_prime[i * p] = false
        if _eq(i % p, 0)
          totients[i * p] = (__tmp3 = totients; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)) * p
          break
        end
        totients[i * p] = (__tmp4 = totients; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)) * (p - 1)
        j = _add(j, 1)
      end
      i = _add(i, 1)
    end
    return totients
  end
  def test_totient()
    expected = [-1, 0, 1, 2, 2, 4, 2, 6, 4, 6, 9]
    res = totient(10)
    idx = 0
    while idx < _len(expected)
      if !_eq((__tmp5 = res; __tmp5.is_a?(Hash) ? __tmp5[idx] : _idx(__tmp5, idx)), (__tmp6 = expected; __tmp6.is_a?(Hash) ? __tmp6[idx] : _idx(__tmp6, idx)))
        panic(_add("totient mismatch at ", _str(idx)))
      end
      idx = _add(idx, 1)
    end
  end
  def main()
    test_totient()
    n = 10
    res = totient(n)
    i = 1
    while i < n
      puts(_add(_add(_add(_str(i), " has "), _str((__tmp7 = res; __tmp7.is_a?(Hash) ? __tmp7[i] : _idx(__tmp7, i)))), " relative primes."))
      i = _add(i, 1)
    end
  end
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
