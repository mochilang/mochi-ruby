# Generated by Mochi transpiler v0.10.64 on 2025-08-13 07:12 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil? || !idx.is_a?(Numeric) || idx < 0 || idx >= arr.length
  arr[idx]
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:to_h)
    obj.to_h.key?(key.to_sym)
  else
    obj.respond_to?(:include?) && obj.include?(key)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def copy_list(xs)
    res = []
    i = 0
    while i < _len(xs)
      res = (res + [(__tmp1 = xs; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i))])
      i = _add(i, 1)
    end
    return res
  end
  def longest_subsequence(arr)
    n = _len(arr)
    lis = []
    i = 0
    while i < n
      single = []
      single = (single + [(__tmp2 = arr; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i))])
      lis = (lis + [single])
      i = _add(i, 1)
    end
    i = 1
    while i < n
      prev = 0
      while prev < i
        if (__tmp3 = arr; __tmp3.is_a?(Hash) ? __tmp3[prev] : _idx(__tmp3, prev)) <= (__tmp4 = arr; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)) && _add(_len((__tmp5 = lis; __tmp5.is_a?(Hash) ? __tmp5[prev] : _idx(__tmp5, prev))), 1) > _len((__tmp6 = lis; __tmp6.is_a?(Hash) ? __tmp6[i] : _idx(__tmp6, i)))
          temp = copy_list((__tmp7 = lis; __tmp7.is_a?(Hash) ? __tmp7[prev] : _idx(__tmp7, prev)))
          temp2 = (temp + [(__tmp8 = arr; __tmp8.is_a?(Hash) ? __tmp8[i] : _idx(__tmp8, i))])
          lis[i] = temp2
        end
        prev = _add(prev, 1)
      end
      i = _add(i, 1)
    end
    result = []
    i = 0
    while i < n
      if _len((__tmp9 = lis; __tmp9.is_a?(Hash) ? __tmp9[i] : _idx(__tmp9, i))) > _len(result)
        result = (__tmp10 = lis; __tmp10.is_a?(Hash) ? __tmp10[i] : _idx(__tmp10, i))
      end
      i = _add(i, 1)
    end
    return result
  end
  def main()
    puts(((x = longest_subsequence([10, 22, 9, 33, 21, 50, 41, 60, 80])); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
    puts(((x = longest_subsequence([4, 8, 7, 5, 1, 12, 2, 3, 9])); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
    puts(((x = longest_subsequence([9, 8, 7, 6, 5, 7])); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
    puts(((x = longest_subsequence([28, 26, 12, 23, 35, 39])); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
    puts(((x = longest_subsequence([1, 1, 1])); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
    puts(((x = longest_subsequence([])); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
  end
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
