# Generated by Mochi transpiler v0.10.64 on 2025-08-13 07:12 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil? || !idx.is_a?(Numeric) || idx < 0 || idx >= arr.length
  arr[idx]
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:to_h)
    obj.to_h.key?(key.to_sym)
  else
    obj.respond_to?(:include?) && obj.include?(key)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def longest_subsequence(xs)
    n = _len(xs)
    if n <= 1
      return xs
    end
    pivot = (__tmp1 = xs; __tmp1.is_a?(Hash) ? __tmp1[0] : _idx(__tmp1, 0))
    is_found = false
    i = 1
    longest_subseq = []
    while !is_found && i < n
      if (__tmp2 = xs; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i)) < pivot
        is_found = true
        temp_array = xs[i...n]
        temp_array = longest_subsequence(temp_array)
        if _len(temp_array) > _len(longest_subseq)
          longest_subseq = temp_array
        end
      else
        i = _add(i, 1)
      end
    end
    filtered = []
    j = 1
    while j < n
      if (__tmp3 = xs; __tmp3.is_a?(Hash) ? __tmp3[j] : _idx(__tmp3, j)) >= pivot
        filtered = (filtered + [(__tmp4 = xs; __tmp4.is_a?(Hash) ? __tmp4[j] : _idx(__tmp4, j))])
      end
      j = _add(j, 1)
    end
    candidate = []
    candidate = (candidate + [pivot])
    candidate = _add(candidate, longest_subsequence(filtered))
    if _len(candidate) > _len(longest_subseq)
      return candidate
    else
      return longest_subseq
    end
  end
  # test examples
  raise 'assertion failed' unless _eq(longest_subsequence([10, 22, 9, 33, 21, 50, 41, 60, 80]), [10, 22, 33, 41, 60, 80])
  raise 'assertion failed' unless _eq(longest_subsequence([4, 8, 7, 5, 1, 12, 2, 3, 9]), [1, 2, 3, 9])
  raise 'assertion failed' unless _eq(longest_subsequence([28, 26, 12, 23, 35, 39]), [12, 23, 35, 39])
  raise 'assertion failed' unless _eq(longest_subsequence([9, 8, 7, 6, 5, 7]), [5, 7])
  raise 'assertion failed' unless _eq(longest_subsequence([1, 1, 1]), [1, 1, 1])
  raise 'assertion failed' unless _eq(longest_subsequence([]), [])
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
