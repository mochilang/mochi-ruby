# Generated by Mochi transpiler v0.10.64 on 2025-08-13 07:12 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil? || !idx.is_a?(Numeric) || idx < 0 || idx >= arr.length
  arr[idx]
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:to_h)
    obj.to_h.key?(key.to_sym)
  else
    obj.respond_to?(:include?) && obj.include?(key)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def partition(m)
    memo = []
    i = 0
    while i < _add(m, 1)
      row = []
      j = 0
      while j < m
        row = (row + [0])
        j = _add(j, 1)
      end
      memo = (memo + [row])
      i = _add(i, 1)
    end
    i = 0
    while i < _add(m, 1)
      (__tmp1 = memo; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i))[0] = 1
      i = _add(i, 1)
    end
    n = 0
    while n < _add(m, 1)
      k = 1
      while k < m
        (__tmp2 = memo; __tmp2.is_a?(Hash) ? __tmp2[n] : _idx(__tmp2, n))[k] = _add((__tmp3 = (__tmp4 = memo; __tmp4.is_a?(Hash) ? __tmp4[n] : _idx(__tmp4, n)); __tmp3.is_a?(Hash) ? __tmp3[k] : _idx(__tmp3, k)), (__tmp5 = (__tmp6 = memo; __tmp6.is_a?(Hash) ? __tmp6[n] : _idx(__tmp6, n)); __tmp5.is_a?(Hash) ? __tmp5[k - 1] : _idx(__tmp5, k - 1)))
        if n - k > 0
          (__tmp7 = memo; __tmp7.is_a?(Hash) ? __tmp7[n] : _idx(__tmp7, n))[k] = _add((__tmp8 = (__tmp9 = memo; __tmp9.is_a?(Hash) ? __tmp9[n] : _idx(__tmp9, n)); __tmp8.is_a?(Hash) ? __tmp8[k] : _idx(__tmp8, k)), (__tmp10 = (__tmp11 = memo; __tmp11.is_a?(Hash) ? __tmp11[n - k - 1] : _idx(__tmp11, n - k - 1)); __tmp10.is_a?(Hash) ? __tmp10[k] : _idx(__tmp10, k)))
        end
        k = _add(k, 1)
      end
      n = _add(n, 1)
    end
    return (__tmp12 = (__tmp13 = memo; __tmp13.is_a?(Hash) ? __tmp13[m] : _idx(__tmp13, m)); __tmp12.is_a?(Hash) ? __tmp12[m - 1] : _idx(__tmp12, m - 1))
  end
  puts(partition(5))
  puts(partition(7))
  puts(partition(100))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
