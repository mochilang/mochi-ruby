# Generated by Mochi transpiler v0.10.64 on 2025-08-13 07:12 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil? || !idx.is_a?(Numeric) || idx < 0 || idx >= arr.length
  arr[idx]
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:to_h)
    obj.to_h.key?(key.to_sym)
  else
    obj.respond_to?(:include?) && obj.include?(key)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def minimum_subarray_sum(target, numbers)
    n = _len(numbers)
    if _eq(n, 0)
      return 0
    end
    if _eq(target, 0)
      i = 0
      while i < n
        if _eq((__tmp1 = numbers; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i)), 0)
          return 0
        end
        i = _add(i, 1)
      end
    end
    left = 0
    right = 0
    curr_sum = 0
    min_len = _add(n, 1)
    while right < n
      curr_sum = _add(curr_sum, (__tmp2 = numbers; __tmp2.is_a?(Hash) ? __tmp2[right] : _idx(__tmp2, right)))
      while curr_sum >= target && left <= right
        current_len = _add(right - left, 1)
        if current_len < min_len
          min_len = current_len
        end
        curr_sum = curr_sum - (__tmp3 = numbers; __tmp3.is_a?(Hash) ? __tmp3[left] : _idx(__tmp3, left))
        left = _add(left, 1)
      end
      right = _add(right, 1)
    end
    if _eq(min_len, _add(n, 1))
      return 0
    end
    return min_len
  end
  puts(_str(minimum_subarray_sum(7, [2, 3, 1, 2, 4, 3])))
  puts(_str(minimum_subarray_sum(7, [2, 3, -1, 2, 4, -3])))
  puts(_str(minimum_subarray_sum(11, [1, 1, 1, 1, 1, 1, 1, 1])))
  puts(_str(minimum_subarray_sum(0, [1, 2, 3])))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
