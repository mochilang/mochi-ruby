# Generated by Mochi transpiler v0.10.64 on 2025-08-13 07:12 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil? || !idx.is_a?(Numeric) || idx < 0 || idx >= arr.length
  arr[idx]
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:to_h)
    obj.to_h.key?(key.to_sym)
  else
    obj.respond_to?(:include?) && obj.include?(key)
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def copy_list(src)
    result = []
    i = 0
    while i < _len(src)
      result = (result + [(__tmp1 = src; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i))])
      i = _add(i, 1)
    end
    return result
  end
  def subset_combinations(elements, n)
    r = _len(elements)
    if n > r
      return []
    end
    dp = []
    i = 0
    while i <= r
      dp = (dp + [[]])
      i = _add(i, 1)
    end
    dp[0] = ((__tmp2 = dp; __tmp2.is_a?(Hash) ? __tmp2[0] : _idx(__tmp2, 0)) + [[]])
    i = 1
    while i <= r
      j = i
      while j > 0
        prevs = (__tmp3 = dp; __tmp3.is_a?(Hash) ? __tmp3[j - 1] : _idx(__tmp3, j - 1))
        k = 0
        while k < _len(prevs)
          prev = (__tmp4 = prevs; __tmp4.is_a?(Hash) ? __tmp4[k] : _idx(__tmp4, k))
          comb = copy_list(prev)
          comb = (comb + [(__tmp5 = elements; __tmp5.is_a?(Hash) ? __tmp5[i - 1] : _idx(__tmp5, i - 1))])
          dp[j] = ((__tmp6 = dp; __tmp6.is_a?(Hash) ? __tmp6[j] : _idx(__tmp6, j)) + [comb])
          k = _add(k, 1)
        end
        j = j - 1
      end
      i = _add(i, 1)
    end
    return (__tmp7 = dp; __tmp7.is_a?(Hash) ? __tmp7[n] : _idx(__tmp7, n))
  end
  puts(((x = subset_combinations([10, 20, 30, 40], 2)); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
