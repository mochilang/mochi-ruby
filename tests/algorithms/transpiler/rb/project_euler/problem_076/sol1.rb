# Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:09 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def solution(m)
    memo = [].clone
    i = 0.clone
    while i <= m
      row = [].clone
      j = 0.clone
      while j < m
        row = (row << (0)).clone
        j = _add(j, 1).clone
      end
      memo = (memo << (row)).clone
      i = _add(i, 1).clone
    end
    i = 0.clone
    while i <= m
      memo[i][0] = 1
      i = _add(i, 1).clone
    end
    n = 0.clone
    while n <= m
      k = 1.clone
      while k < m
        memo[n][k] = _add(memo[n][k], memo[n][k - 1])
        if n > k
          memo[n][k] = _add(memo[n][k], memo[n - k - 1][k])
        end
        k = _add(k, 1).clone
      end
      n = _add(n, 1).clone
    end
    return memo[m][m - 1] - 1
  end
  puts(_str(solution(100)))
  puts(_str(solution(50)))
  puts(_str(solution(30)))
  puts(_str(solution(10)))
  puts(_str(solution(5)))
  puts(_str(solution(3)))
  puts(_str(solution(2)))
  puts(_str(solution(1)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
