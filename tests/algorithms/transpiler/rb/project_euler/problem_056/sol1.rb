# Generated by Mochi transpiler v0.10.60 on 2025-08-07 16:01 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def pow_int(base, exp)
    result = 1.clone
    i = 0.clone
    while i < exp
      result = result * base.clone
      i = _add(i, 1).clone
    end
    return result
  end
  def digital_sum(n)
    s = _str(n)
    sum = 0.clone
    i = 0.clone
    while i < s.length
      sum = _add(sum, ((s[i]).to_i)).clone
      i = _add(i, 1).clone
    end
    return sum
  end
  def solution(a, b)
    max_sum = 0.clone
    base = 0.clone
    while base < a
      power = 0.clone
      while power < b
        value = pow_int(base, power)
        ds = digital_sum(value)
        if ds > max_sum
          max_sum = ds.clone
        end
        power = _add(power, 1).clone
      end
      base = _add(base, 1).clone
    end
    return max_sum
  end
  def test_solution()
    if !_eq(solution(10, 10), 45)
      panic("solution 10 10 failed")
    end
    if !_eq(solution(100, 100), 972)
      panic("solution 100 100 failed")
    end
    if !_eq(solution(100, 200), 1872)
      panic("solution 100 200 failed")
    end
  end
  def main()
    test_solution()
    puts(_str(solution(100, 100)))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
