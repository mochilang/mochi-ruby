# Generated by Mochi transpiler v0.10.70 on 2025-08-23 14:49 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    if x == x.to_i
      x.to_i.to_s
    else
      x.to_s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def contains(xs, value)
    i = 0
    while i < _len(xs)
      if _eq((__tmp1 = xs; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i)), value)
        return true
      end
      i = _add(i, 1)
    end
    return false
  end
  def solution(n)
    zmulti = []
    xmulti = []
    temp = 1
    while true
      result = 3 * temp
      if result < n
        zmulti = _append(zmulti, result)
        temp = _add(temp, 1)
      else
        break
      end
    end
    temp = 1
    while true
      result = 5 * temp
      if result < n
        xmulti = _append(xmulti, result)
        temp = _add(temp, 1)
      else
        break
      end
    end
    collection = []
    i = 0
    while i < _len(zmulti)
      v = (__tmp2 = zmulti; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i))
      if !contains(collection, v)
        collection = _append(collection, v)
      end
      i = _add(i, 1)
    end
    i = 0
    while i < _len(xmulti)
      v = (__tmp3 = xmulti; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i))
      if !contains(collection, v)
        collection = _append(collection, v)
      end
      i = _add(i, 1)
    end
    total = 0
    i = 0
    while i < _len(collection)
      total = _add(total, (__tmp4 = collection; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)))
      i = _add(i, 1)
    end
    return total
  end
  def test_solution()
    if !_eq(solution(3), 0)
      panic("solution(3) failed")
    end
    if !_eq(solution(4), 3)
      panic("solution(4) failed")
    end
    if !_eq(solution(10), 23)
      panic("solution(10) failed")
    end
    if !_eq(solution(600), 83700)
      panic("solution(600) failed")
    end
  end
  def main()
    test_solution()
    puts(_add("solution() = ", _str(solution(1000))))
  end
  main()
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
