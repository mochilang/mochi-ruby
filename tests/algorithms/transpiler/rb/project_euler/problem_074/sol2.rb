# Generated by Mochi transpiler v0.10.61 on 2025-08-08 16:09 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def digit_factorial_sum(number)
    if number < 0
      panic("Parameter number must be greater than or equal to 0")
    end
    if _eq(number, 0)
      return $DIGIT_FACTORIAL[0]
    end
    n = number.clone
    total = 0.clone
    while n > 0
      digit = n % 10
      total = _add(total, $DIGIT_FACTORIAL[digit]).clone
      n = n / 10.clone
    end
    return total
  end
  def chain_len(n, limit)
    seen = {}.clone
    length = 0.clone
    cur = n.clone
    while _eq((seen.key?(cur)), false) && length <= limit
      seen[cur] = true
      length = _add(length, 1).clone
      cur = digit_factorial_sum(cur).clone
    end
    return length
  end
  def solution(chain_length, number_limit)
    if chain_length <= 0 || number_limit <= 0
      panic("Parameters chain_length and number_limit must be greater than 0")
    end
    count = 0.clone
    start = 1.clone
    while start < number_limit
      if _eq(chain_len(start, chain_length), chain_length)
        count = _add(count, 1).clone
      end
      start = _add(start, 1).clone
    end
    return count
  end
  $DIGIT_FACTORIAL = [1, 1, 2, 6, 24, 120, 720, 5040, 40320, 362880]
  puts(_str(solution(60, 1000000)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
