# Generated by Mochi transpiler v0.10.62 on 2025-08-09 16:28 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def pow2(exp)
    result = 1
    i = 0
    while i < exp
      result = result * 2
      i = _add(i, 1)
    end
    return result
  end
  def is_bit_set(number, position)
    shifted = number / pow2(position)
    remainder = shifted % 2
    return _eq(remainder, 1)
  end
  def set_bit(number, position)
    if is_bit_set(number, position)
      return number
    end
    return _add(number, pow2(position))
  end
  def clear_bit(number, position)
    if is_bit_set(number, position)
      return number - pow2(position)
    end
    return number
  end
  def flip_bit(number, position)
    if is_bit_set(number, position)
      return number - pow2(position)
    end
    return _add(number, pow2(position))
  end
  def get_bit(number, position)
    if is_bit_set(number, position)
      return 1
    end
    return 0
  end
  puts(_str(set_bit(13, 1)))
  puts(_str(clear_bit(18, 1)))
  puts(_str(flip_bit(5, 1)))
  puts(_str(is_bit_set(10, 3)))
  puts(_str(get_bit(10, 1)))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
