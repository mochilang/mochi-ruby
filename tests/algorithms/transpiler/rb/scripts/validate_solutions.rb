# Generated by Mochi transpiler v0.10.63 on 2025-08-11 16:20 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'digest'
def _sha256(bs)
  data = bs.is_a?(Array) ? bs.pack('C*') : bs
  Digest::SHA256.digest(data).bytes
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = _now()
  def byte_to_hex(b)
    hi = b / 16
    lo = b % 16
    return _add($HEX[hi], $HEX[lo])
  end
  def bytes_to_hex(bs)
    res = ""
    i = 0
    while i < bs.length
      res = _add(res, byte_to_hex(bs[i]))
      i = _add(i, 1)
    end
    return res
  end
  def sha256_hex(s)
    return bytes_to_hex(_sha256(s))
  end
  def solution_001()
    total = 0
    n = 0
    while n < 1000
      if _eq(n % 3, 0) || _eq(n % 5, 0)
        total = _add(total, n)
      end
      n = _add(n, 1)
    end
    return _str(total)
  end
  $HEX = "0123456789abcdef"
  $expected = sha256_hex("233168")
  $answer = solution_001()
  $computed = sha256_hex($answer)
  if _eq($computed, $expected)
    puts("Problem 001 passed")
  else
    puts(_add(_add(_add("Problem 001 failed: ", $computed), " != "), $expected))
  end
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
