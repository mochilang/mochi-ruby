# Generated by Mochi transpiler v0.10.63 on 2025-08-11 15:32 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def all_digits(s)
    if _eq(s.length, 0)
      return false
    end
    i = 0
    while i < s.length
      c = s[i]
      if c < "0" || c > "9"
        return false
      end
      i = _add(i, 1)
    end
    return true
  end
  def indian_phone_validator(phone)
    s = phone
    if s.length >= 3 && _eq(s[0...3], "+91")
      s = s[3...s.length]
      if s.length > 0
        c = s[0]
        if _eq(c, "-") || _eq(c, " ")
          s = s[1...s.length]
        end
      end
    end
    if s.length > 0 && _eq(s[0], "0")
      s = s[1...s.length]
    end
    if s.length >= 2 && _eq(s[0...2], "91")
      s = s[2...s.length]
    end
    if !_eq(s.length, 10)
      return false
    end
    first = s[0]
    if !(_eq(first, "7") || _eq(first, "8") || _eq(first, "9"))
      return false
    end
    if !all_digits(s)
      return false
    end
    return true
  end
  puts(_str(indian_phone_validator("+91123456789")))
  puts(_str(indian_phone_validator("+919876543210")))
  puts(_str(indian_phone_validator("01234567896")))
  puts(_str(indian_phone_validator("919876543218")))
  puts(_str(indian_phone_validator("+91-1234567899")))
  puts(_str(indian_phone_validator("+91-9876543218")))
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
