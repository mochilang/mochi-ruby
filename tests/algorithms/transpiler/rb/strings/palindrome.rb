# Generated by Mochi transpiler v0.10.63 on 2025-08-11 15:32 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    a + b
  end
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    (a.to_f - b.to_f).abs < 1e-6
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def reverse(s)
    res = ""
    i = s.length - 1
    while i >= 0
      res = _add(res, s[i])
      i = i - 1
    end
    return res
  end
  def is_palindrome(s)
    start_i = 0
    end_i = s.length - 1
    while start_i < end_i
      if _eq(s[start_i], s[end_i])
        start_i = _add(start_i, 1)
        end_i = end_i - 1
      else
        return false
      end
    end
    return true
  end
  def is_palindrome_traversal(s)
    end_ = s.length / 2
    n = s.length
    i = 0
    while i < end_
      if !_eq(s[i], s[n - i - 1])
        return false
      end
      i = _add(i, 1)
    end
    return true
  end
  def is_palindrome_recursive(s)
    if s.length <= 1
      return true
    end
    if _eq(s[0], s[s.length - 1])
      return is_palindrome_recursive(s[1...s.length - 1])
    end
    return false
  end
  def is_palindrome_slice(s)
    return _eq(s, reverse(s))
  end
  def main()
        __tmp1 = $test_data
    if __tmp1.respond_to?(:keys) && !__tmp1.is_a?(String)
      __tmp1 = __tmp1.keys
    end
    __tmp1.each do |t|
      s = t.text
      expected = t.expected
      r1 = is_palindrome(s)
      r2 = is_palindrome_traversal(s)
      r3 = is_palindrome_recursive(s)
      r4 = is_palindrome_slice(s)
      if !_eq(r1, expected) || !_eq(r2, expected) || !_eq(r3, expected) || !_eq(r4, expected)
        panic("algorithm mismatch")
      end
      puts(_add(_add(s, " "), _str(expected)))
    end
    puts("a man a plan a canal panama")
  end
  Case = Struct.new(:text, :expected, keyword_init: true)
  $test_data = [Case.new(text: "MALAYALAM", expected: true), Case.new(text: "String", expected: false), Case.new(text: "rotor", expected: true), Case.new(text: "level", expected: true), Case.new(text: "A", expected: true), Case.new(text: "BB", expected: true), Case.new(text: "ABC", expected: false), Case.new(text: "amanaplanacanalpanama", expected: true)]
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
