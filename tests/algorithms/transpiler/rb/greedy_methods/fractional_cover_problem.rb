# Generated by Mochi transpiler v0.10.66 on 2025-08-15 09:59 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if arr.is_a?(Array) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def ratio(item)
    return ((item.value).to_f) / ((item.weight).to_f)
  end
  def fractional_cover(items, capacity)
    if capacity < 0
      panic("Capacity cannot be negative")
    end
    total = 0.0
    remaining = capacity
    sorted = (begin
      _res = []
      items.each do |it|
        _res << it
      end
      _res = _res.each_with_index.sort_by do |it, __i|
        [-ratio(it), __i]
      end.map{ |x, _| x }
    _res
  end)
  idx = 0
  while idx < _len(sorted) && remaining > 0
    item = (__tmp1 = sorted; __tmp1.is_a?(Hash) ? __tmp1[idx] : _idx(__tmp1, idx))
    take = ((__tmp2 = item; __tmp2.is_a?(Hash) ? __tmp2["weight"] : _idx(__tmp2, "weight")) < remaining ? (__tmp3 = item; __tmp3.is_a?(Hash) ? __tmp3["weight"] : _idx(__tmp3, "weight")) : remaining)
    total = _add(total, ((take).to_f) * ratio(item))
    remaining = remaining - take
    idx = _add(idx, 1)
  end
  return total
end
Object.send(:remove_const, :Item) if Object.const_defined?(:Item)
Object.const_set(:Item, Struct.new(:weight, :value, keyword_init: true))
$items1 = [Item.new(weight: 10, value: 60), Item.new(weight: 20, value: 100), Item.new(weight: 30, value: 120)]
puts(_str(fractional_cover($items1, 50)))
$items2 = [Item.new(weight: 20, value: 100), Item.new(weight: 30, value: 120), Item.new(weight: 10, value: 60)]
puts(_str(fractional_cover($items2, 25)))
$items3 = []
puts(_str(fractional_cover($items3, 50)))
$items4 = [Item.new(weight: 10, value: 60)]
puts(_str(fractional_cover($items4, 5)))
puts(_str(fractional_cover($items4, 1)))
puts(_str(fractional_cover($items4, 0)))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
