# Generated by Mochi transpiler v0.10.73 on 2025-08-25 23:47 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    idx += arr.length if idx < 0
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  arr = arr.nil? ? [] : arr.clone
  x = x.clone if x.is_a?(Array)
  arr << x
  arr
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def bfs(graph, s, t, parent)
    visited = []
    i = 0
    while i < _len(graph)
      visited = _append(visited, false)
      i = _add(i, 1)
    end
    queue = [s]
    head = 0
    visited[s] = true
    while head < _len(queue)
      u = (__tmp1 = queue; __tmp1.is_a?(Hash) ? __tmp1[head] : _idx(__tmp1, head))
      head = _add(head, 1)
      ind = 0
      while ind < _len((__tmp2 = graph; __tmp2.is_a?(Hash) ? __tmp2[u] : _idx(__tmp2, u)))
        if _eq((__tmp3 = visited; __tmp3.is_a?(Hash) ? __tmp3[ind] : _idx(__tmp3, ind)), false) && (__tmp4 = (__tmp5 = graph; __tmp5.is_a?(Hash) ? __tmp5[u] : _idx(__tmp5, u)); __tmp4.is_a?(Hash) ? __tmp4[ind] : _idx(__tmp4, ind)) > 0
          queue = _append(queue, ind)
          visited[ind] = true
          parent[ind] = u
        end
        ind = _add(ind, 1)
      end
    end
    return (__tmp6 = visited; __tmp6.is_a?(Hash) ? __tmp6[t] : _idx(__tmp6, t))
  end
  def mincut(graph, source, sink)
    g = graph
    parent = []
    i = 0
    while i < _len(g)
      parent = _append(parent, -1)
      i = _add(i, 1)
    end
    temp = []
    i = 0
    while i < _len(g)
      row = []
      j = 0
      while j < _len((__tmp7 = g; __tmp7.is_a?(Hash) ? __tmp7[i] : _idx(__tmp7, i)))
        row = _append(row, (__tmp8 = (__tmp9 = g; __tmp9.is_a?(Hash) ? __tmp9[i] : _idx(__tmp9, i)); __tmp8.is_a?(Hash) ? __tmp8[j] : _idx(__tmp8, j)))
        j = _add(j, 1)
      end
      temp = _append(temp, row)
      i = _add(i, 1)
    end
    while bfs(g, source, sink, parent)
      path_flow = 1000000000
      s = sink
      while !_eq(s, source)
        p = (__tmp10 = parent; __tmp10.is_a?(Hash) ? __tmp10[s] : _idx(__tmp10, s))
        cap = (__tmp11 = (__tmp12 = g; __tmp12.is_a?(Hash) ? __tmp12[p] : _idx(__tmp12, p)); __tmp11.is_a?(Hash) ? __tmp11[s] : _idx(__tmp11, s))
        if cap < path_flow
          path_flow = cap
        end
        s = p
      end
      v = sink
      while !_eq(v, source)
        u = (__tmp13 = parent; __tmp13.is_a?(Hash) ? __tmp13[v] : _idx(__tmp13, v))
        (__tmp14 = g; __tmp14.is_a?(Hash) ? __tmp14[u] : _idx(__tmp14, u))[v] = (__tmp15 = (__tmp16 = g; __tmp16.is_a?(Hash) ? __tmp16[u] : _idx(__tmp16, u)); __tmp15.is_a?(Hash) ? __tmp15[v] : _idx(__tmp15, v)) - path_flow
        (__tmp17 = g; __tmp17.is_a?(Hash) ? __tmp17[v] : _idx(__tmp17, v))[u] = _add((__tmp18 = (__tmp19 = g; __tmp19.is_a?(Hash) ? __tmp19[v] : _idx(__tmp19, v)); __tmp18.is_a?(Hash) ? __tmp18[u] : _idx(__tmp18, u)), path_flow)
        v = u
      end
    end
    res = []
    i = 0
    while i < _len(g)
      j = 0
      while j < _len((__tmp20 = g; __tmp20.is_a?(Hash) ? __tmp20[0] : _idx(__tmp20, 0)))
        if _eq((__tmp21 = (__tmp22 = g; __tmp22.is_a?(Hash) ? __tmp22[i] : _idx(__tmp22, i)); __tmp21.is_a?(Hash) ? __tmp21[j] : _idx(__tmp21, j)), 0) && (__tmp23 = (__tmp24 = temp; __tmp24.is_a?(Hash) ? __tmp24[i] : _idx(__tmp24, i)); __tmp23.is_a?(Hash) ? __tmp23[j] : _idx(__tmp23, j)) > 0
          res = _append(res, [i, j])
        end
        j = _add(j, 1)
      end
      i = _add(i, 1)
    end
    return res
  end
  $test_graph = [[0, 16, 13, 0, 0, 0], [0, 0, 10, 12, 0, 0], [0, 4, 0, 0, 14, 0], [0, 0, 9, 0, 0, 20], [0, 0, 0, 7, 0, 4], [0, 0, 0, 0, 0, 0]]
  $result = mincut($test_graph, 0, 5)
  puts(((x = $result); x.is_a?(Array) ? ("[" + x.map{ |x| if x.is_a?(Hash) then '{' + x.to_h.map{ |k,v| "#{k}: #{v.is_a?(String) ? v : v.to_s}" }.join(', ') + '}' else x.to_s end }.join(' ') + "]") : x.to_s))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
