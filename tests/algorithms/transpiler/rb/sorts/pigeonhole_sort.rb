# Generated by Mochi transpiler v0.10.59 on 2025-08-07 10:45 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _str(x)
  if x.is_a?(Float) && x == x.to_i
    x.to_i.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

start_mem = _mem()
start = _now()
  def pigeonhole_sort(arr)
    if arr.length == 0
      return arr
    end
    min_val = (arr.min()).to_i
    max_val = (arr.max()).to_i
    size = _add(max_val - min_val, 1)
    holes = [].clone
    i = 0.clone
    while i < size
      holes = (holes << (0)).clone
      i = _add(i, 1).clone
    end
    i = 0.clone
    while i < arr.length
      x = arr[i]
      index = x - min_val
      holes[index] = _add(holes[index], 1)
      i = _add(i, 1).clone
    end
    sorted_index = 0.clone
    count = 0.clone
    while count < size
      while holes[count] > 0
        arr[sorted_index] = _add(count, min_val)
        holes[count] = holes[count] - 1
        sorted_index = _add(sorted_index, 1).clone
      end
      count = _add(count, 1).clone
    end
    return arr
  end
  $example = [8, 3, 2, 7, 4, 6, 8]
  $result = pigeonhole_sort($example)
  $output = "Sorted order is:".clone
  $j = 0.clone
  while $j < $result.length
    $output = _add(_add($output, " "), _str($result[$j])).clone
    $j = _add($j, 1).clone
  end
  puts($output)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
