# Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:30 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def excel_title_to_column(title)
    result = 0
    i = 0
    while i < title.length
      ch = title[i..._add(i, 1)]
      value = 0
      idx = 0
      found = false
      while idx < $letters.length
        if $letters[idx..._add(idx, 1)] == ch
          value = _add(idx, 1)
          found = true
          break
        end
        idx = _add(idx, 1)
      end
      if !found
        panic("title must contain only uppercase A-Z")
      end
      result = _add(result * 26, value)
      i = _add(i, 1)
    end
    return result
  end
  def main()
    puts(excel_title_to_column("A"))
    puts(excel_title_to_column("B"))
    puts(excel_title_to_column("AB"))
    puts(excel_title_to_column("Z"))
  end
  $letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
