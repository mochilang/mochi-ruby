# Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:30 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def panic(msg)
    puts(msg)
  end
  def trim_spaces(s)
    start = 0
    end_ = s.length - 1
    while start <= end_ && s[start..._add(start, 1)] == " "
      start = _add(start, 1)
    end
    while end_ >= start && s[end_..._add(end_, 1)] == " "
      end_ = end_ - 1
    end
    if start > end_
      return ""
    end
    return s[start..._add(end_, 1)]
  end
  def char_to_digit(ch)
    if ch == "0"
      return 0
    end
    if ch == "1"
      return 1
    end
    if ch == "2"
      return 2
    end
    if ch == "3"
      return 3
    end
    if ch == "4"
      return 4
    end
    if ch == "5"
      return 5
    end
    if ch == "6"
      return 6
    end
    if ch == "7"
      return 7
    end
    panic("Non-octal value was passed to the function")
    return 0
  end
  def oct_to_decimal(oct_string)
    s = trim_spaces(oct_string)
    if s.length == 0
      panic("Empty string was passed to the function")
      return 0
    end
    is_negative = false
    if s[0...1] == "-"
      is_negative = true
      s = s[1...s.length]
    end
    if s.length == 0
      panic("Non-octal value was passed to the function")
      return 0
    end
    decimal_number = 0
    i = 0
    while i < s.length
      ch = s[i..._add(i, 1)]
      digit = char_to_digit(ch)
      decimal_number = _add(8 * decimal_number, digit)
      i = _add(i, 1)
    end
    if is_negative
      decimal_number = -decimal_number
    end
    return decimal_number
  end
  def main()
    puts((oct_to_decimal("1")).to_s)
    puts((oct_to_decimal("-1")).to_s)
    puts((oct_to_decimal("12")).to_s)
    puts((oct_to_decimal(" 12   ")).to_s)
    puts((oct_to_decimal("-45")).to_s)
    puts((oct_to_decimal("0")).to_s)
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
