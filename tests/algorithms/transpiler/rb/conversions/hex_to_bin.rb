# Generated by Mochi transpiler v0.10.59 on 2025-08-06 20:30 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if (!s || s == '') && ENV['MOCHI_BENCHMARK']
  s = '1'
end
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed += 1_000_000
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


class String
  alias each each_char
end

start_mem = _mem()
start = _now()
  def panic(msg)
    puts(msg)
  end
  def trim_spaces(s)
    start = 0
    end_ = s.length
    while start < end_ && s[start..._add(start, 1)] == " "
      start = _add(start, 1)
    end
    while end_ > start && s[end_ - 1...end_] == " "
      end_ = end_ - 1
    end
    return s[start...end_]
  end
  def hex_digit_value(ch)
    if ch == "0"
      return 0
    end
    if ch == "1"
      return 1
    end
    if ch == "2"
      return 2
    end
    if ch == "3"
      return 3
    end
    if ch == "4"
      return 4
    end
    if ch == "5"
      return 5
    end
    if ch == "6"
      return 6
    end
    if ch == "7"
      return 7
    end
    if ch == "8"
      return 8
    end
    if ch == "9"
      return 9
    end
    if ch == "a" || ch == "A"
      return 10
    end
    if ch == "b" || ch == "B"
      return 11
    end
    if ch == "c" || ch == "C"
      return 12
    end
    if ch == "d" || ch == "D"
      return 13
    end
    if ch == "e" || ch == "E"
      return 14
    end
    if ch == "f" || ch == "F"
      return 15
    end
    panic("Invalid value was passed to the function")
  end
  def hex_to_bin(hex_num)
    trimmed = trim_spaces(hex_num)
    if trimmed.length == 0
      panic("No value was passed to the function")
    end
    s = trimmed
    is_negative = false
    if s[0...1] == "-"
      is_negative = true
      s = s[1...s.length]
    end
    int_num = 0
    i = 0
    while i < s.length
      ch = s[i..._add(i, 1)]
      val = hex_digit_value(ch)
      int_num = _add(int_num * 16, val)
      i = _add(i, 1)
    end
    bin_str = ""
    n = int_num
    if n == 0
      bin_str = "0"
    end
    while n > 0
      bin_str = _add((n % 2).to_s, bin_str)
      n = n / 2
    end
    result = (bin_str).to_i
    if is_negative
      result = -result
    end
    return result
  end
  puts((hex_to_bin("AC")).to_s)
  puts((hex_to_bin("9A4")).to_s)
  puts((hex_to_bin("   12f   ")).to_s)
  puts((hex_to_bin("FfFf")).to_s)
  puts((hex_to_bin("-fFfF")).to_s)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
