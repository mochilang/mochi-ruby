# Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:36 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    idx += arr.length if idx < 0
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  arr = arr.nil? ? [] : arr.clone
  x = x.clone if x.is_a?(Array)
  arr << x
  arr
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x % 1 == 0 ? x.to_i.to_s : x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def apply_table(inp, table)
    res = ""
    i = 0
    while i < _len(table)
      idx = (__tmp1 = table; __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i)) - 1
      if idx < 0
        idx = _len(inp) - 1
      end
      res = _add(res, inp[idx..._add(idx, 1)])
      i = _add(i, 1)
    end
    return res
  end
  def left_shift(data)
    return _add(data[1..._len(data)], data[0...1])
  end
  def xor(a, b)
    res = ""
    i = 0
    while i < _len(a) && i < _len(b)
      if _eq(a[i..._add(i, 1)], b[i..._add(i, 1)])
        res = _add(res, "0")
      else
        res = _add(res, "1")
      end
      i = _add(i, 1)
    end
    return res
  end
  def int_to_binary(n)
    if _eq(n, 0)
      return "0"
    end
    res = ""
    num = n
    while num > 0
      res = _add(_str(num % 2), res)
      num = num / 2
    end
    return res
  end
  def pad_left(s, width)
    res = s
    while _len(res) < width
      res = _add("0", res)
    end
    return res
  end
  def bin_to_int(s)
    result = 0
    i = 0
    while i < _len(s)
      digit = (s[i..._add(i, 1)]).to_i
      result = _add(result * 2, digit)
      i = _add(i, 1)
    end
    return result
  end
  def apply_sbox(s, data)
    row_bits = _add(data[0...1], data[_len(data) - 1..._len(data)])
    col_bits = data[1...3]
    row = bin_to_int(row_bits)
    col = bin_to_int(col_bits)
    val = (__tmp2 = (__tmp3 = s; __tmp3.is_a?(Hash) ? __tmp3[row] : _idx(__tmp3, row)); __tmp2.is_a?(Hash) ? __tmp2[col] : _idx(__tmp2, col))
    out = int_to_binary(val)
    return out
  end
  def f(expansion, s0, s1, key, message)
    left = message[0...4]
    right = message[4...8]
    temp = apply_table(right, expansion)
    temp = xor(temp, key)
    left_bin_str = apply_sbox(s0, temp[0...4])
    right_bin_str = apply_sbox(s1, temp[4...8])
    left_bin_str = pad_left(left_bin_str, 2)
    right_bin_str = pad_left(right_bin_str, 2)
    temp = apply_table(_add(left_bin_str, right_bin_str), $p4_table)
    temp = xor(left, temp)
    return _add(temp, right)
  end
  $p4_table = [2, 4, 3, 1]
  $key = "1010000010"
  $message = "11010111"
  $p8_table = [6, 3, 7, 4, 8, 5, 10, 9]
  $p10_table = [3, 5, 2, 7, 4, 10, 1, 9, 8, 6]
  $IP = [2, 6, 3, 1, 4, 8, 5, 7]
  $IP_inv = [4, 1, 3, 5, 7, 2, 8, 6]
  $expansion = [4, 1, 2, 3, 2, 3, 4, 1]
  $s0 = [[1, 0, 3, 2], [3, 2, 1, 0], [0, 2, 1, 3], [3, 1, 3, 2]]
  $s1 = [[0, 1, 2, 3], [2, 0, 1, 3], [3, 0, 1, 0], [2, 1, 0, 3]]
  $temp = apply_table($key, $p10_table)
  $left = $temp[0...5]
  $right = $temp[5...10]
  $left = left_shift($left)
  $right = left_shift($right)
  $key1 = apply_table(_add($left, $right), $p8_table)
  $left = left_shift($left)
  $right = left_shift($right)
  $left = left_shift($left)
  $right = left_shift($right)
  $key2 = apply_table(_add($left, $right), $p8_table)
  $temp = apply_table($message, $IP)
  $temp = f($expansion, $s0, $s1, $key1, $temp)
  $temp = _add($temp[4...8], $temp[0...4])
  $temp = f($expansion, $s0, $s1, $key2, $temp)
  $CT = apply_table($temp, $IP_inv)
  puts(_add("Cipher text is: ", $CT))
  $temp = apply_table($CT, $IP)
  $temp = f($expansion, $s0, $s1, $key2, $temp)
  $temp = _add($temp[4...8], $temp[0...4])
  $temp = f($expansion, $s0, $s1, $key1, $temp)
  $PT = apply_table($temp, $IP_inv)
  puts(_add("Plain text after decypting is: ", $PT))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
