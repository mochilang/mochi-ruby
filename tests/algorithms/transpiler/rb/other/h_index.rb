# Generated by Mochi transpiler v0.10.73 on 2025-08-26 08:36 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    idx += arr.length if idx < 0
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && b.is_a?(Array)
    a + b
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  arr = arr.nil? ? [] : arr.clone
  x = x.clone if x.is_a?(Array)
  arr << x
  arr
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-8 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    "[" + x.map { |e| _str(e) }.join(' ') + "]"
  elsif x.is_a?(Float)
    x % 1 == 0 ? x.to_i.to_s : x.to_s
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def subarray(xs, start, end_)
    result = []
    k = start
    while k < end_
      result = _append(result, (__tmp1 = xs; __tmp1.is_a?(Hash) ? __tmp1[k] : _idx(__tmp1, k)))
      k = _add(k, 1)
    end
    return result
  end
  def merge(left_half, right_half)
    result = []
    i = 0
    j = 0
    while i < _len(left_half) && j < _len(right_half)
      if (__tmp2 = left_half; __tmp2.is_a?(Hash) ? __tmp2[i] : _idx(__tmp2, i)) < (__tmp3 = right_half; __tmp3.is_a?(Hash) ? __tmp3[j] : _idx(__tmp3, j))
        result = _append(result, (__tmp4 = left_half; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i)))
        i = _add(i, 1)
      else
        result = _append(result, (__tmp5 = right_half; __tmp5.is_a?(Hash) ? __tmp5[j] : _idx(__tmp5, j)))
        j = _add(j, 1)
      end
    end
    while i < _len(left_half)
      result = _append(result, (__tmp6 = left_half; __tmp6.is_a?(Hash) ? __tmp6[i] : _idx(__tmp6, i)))
      i = _add(i, 1)
    end
    while j < _len(right_half)
      result = _append(result, (__tmp7 = right_half; __tmp7.is_a?(Hash) ? __tmp7[j] : _idx(__tmp7, j)))
      j = _add(j, 1)
    end
    return result
  end
  def merge_sort(array)
    if _len(array) <= 1
      return array
    end
    middle = _len(array) / 2
    left_half = subarray(array, 0, middle)
    right_half = subarray(array, middle, _len(array))
    sorted_left = merge_sort(left_half)
    sorted_right = merge_sort(right_half)
    return merge(sorted_left, sorted_right)
  end
  def h_index(citations)
    idx = 0
    while idx < _len(citations)
      if (__tmp8 = citations; __tmp8.is_a?(Hash) ? __tmp8[idx] : _idx(__tmp8, idx)) < 0
        panic("The citations should be a list of non negative integers.")
      end
      idx = _add(idx, 1)
    end
    sorted = merge_sort(citations)
    n = _len(sorted)
    i = 0
    while i < n
      if (__tmp9 = sorted; __tmp9.is_a?(Hash) ? __tmp9[n - 1 - i] : _idx(__tmp9, n - 1 - i)) <= i
        return i
      end
      i = _add(i, 1)
    end
    return n
  end
  puts(_str(h_index([3, 0, 6, 1, 5])))
  puts(_str(h_index([1, 3, 1])))
  puts(_str(h_index([1, 2, 3])))
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
