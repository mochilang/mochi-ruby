# Generated by Mochi transpiler v0.10.67 on 2025-08-17 13:28 +0700
$VERBOSE = nil
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
if !$now_seeded && ENV['MOCHI_BENCHMARK']
  $now_seeded = true
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1_664_525 + 1_013_904_223) % 2_147_483_647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _idx(arr, idx)
  return nil if arr.nil?
  if (arr.is_a?(Array) || arr.is_a?(String)) && idx.is_a?(Numeric)
    return nil if idx < 0 || idx >= arr.length
  end
  arr[idx]
end


def _pow(a, b)
  res = (a.nil? ? 0 : a) ** (b.nil? ? 0 : b)
  res.is_a?(Float) && res == res.to_i ? res.to_i : res
end


def _len(x)
  x.respond_to?(:length) ? x.length : 0
end


def _has(obj, key)
  if obj.is_a?(Hash)
    obj.key?(key)
  elsif obj.respond_to?(:include?)
    obj.include?(key)
  elsif obj.respond_to?(:to_h)
    k = key.respond_to?(:to_sym) ? key.to_sym : key
    obj.to_h.key?(k)
  else
    false
  end
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  elsif a.is_a?(Array) && !b.is_a?(Array)
    a + [b]
  elsif !a.is_a?(Array) && b.is_a?(Array)
    [a] + b
  elsif a.is_a?(String) || b.is_a?(String)
    a.to_s + b.to_s
  else
    (a.nil? ? 0 : a) + (b.nil? ? 0 : b)
  end
end


def _append(arr, x)
  x = x.clone if x.is_a?(Array)
  (arr || []) + [x]
end


def _eq(a, b)
  if a.is_a?(Float) || b.is_a?(Float)
    diff = (a.to_f - b.to_f).abs
    scale = [a.to_f.abs, b.to_f.abs].max
    scale = 1.0 if scale == 0.0
    diff <= 1e-6 * scale
  else
    a == b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end


def _padEnd(s, len, ch)
  s.to_s.ljust(len, ch)
end


def _str(x)
  if x.is_a?(Array)
    x.map { |e| _str(e) }.join(' ')
  elsif x.is_a?(Float)
    s = x.to_s
    if s.include?('e') || s.include?('E')
      s
    elsif x == x.to_i
      x.to_i.to_s
    else
      s
    end
  else
    x.to_s
  end
end


class String
  alias each each_char
end


def panic(msg)
  raise RuntimeError, msg
end

__name__ = '__main__'
start_mem = _mem()
start = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  def abs_int(n)
    if n < 0
      return -n
    end
    return n
  end
  def make_matrix(row_size)
    size = abs_int(row_size)
    if _eq(size, 0)
      size = 4
    end
    mat = []
    y = 0
    while y < size
      row = []
      x = 0
      while x < size
        row = _append(row, _add(_add(1, x), y * size))
        x = _add(x, 1)
      end
      mat = _append(mat, row)
      y = _add(y, 1)
    end
    return mat
  end
  def transpose(mat)
    n = _len(mat)
    result = []
    i = 0
    while i < n
      row = []
      j = 0
      while j < n
        row = _append(row, (__tmp1 = (__tmp2 = mat; __tmp2.is_a?(Hash) ? __tmp2[j] : _idx(__tmp2, j)); __tmp1.is_a?(Hash) ? __tmp1[i] : _idx(__tmp1, i)))
        j = _add(j, 1)
      end
      result = _append(result, row)
      i = _add(i, 1)
    end
    return result
  end
  def reverse_row(mat)
    result = []
    i = _len(mat) - 1
    while i >= 0
      result = _append(result, (__tmp3 = mat; __tmp3.is_a?(Hash) ? __tmp3[i] : _idx(__tmp3, i)))
      i = i - 1
    end
    return result
  end
  def reverse_column(mat)
    result = []
    i = 0
    while i < _len(mat)
      row = []
      j = _len((__tmp4 = mat; __tmp4.is_a?(Hash) ? __tmp4[i] : _idx(__tmp4, i))) - 1
      while j >= 0
        row = _append(row, (__tmp5 = (__tmp6 = mat; __tmp6.is_a?(Hash) ? __tmp6[i] : _idx(__tmp6, i)); __tmp5.is_a?(Hash) ? __tmp5[j] : _idx(__tmp5, j)))
        j = j - 1
      end
      result = _append(result, row)
      i = _add(i, 1)
    end
    return result
  end
  def rotate_90(mat)
    t = transpose(mat)
    rr = reverse_row(t)
    return rr
  end
  def rotate_180(mat)
    rc = reverse_column(mat)
    rr = reverse_row(rc)
    return rr
  end
  def rotate_270(mat)
    t = transpose(mat)
    rc = reverse_column(t)
    return rc
  end
  def row_to_string(row)
    line = ""
    i = 0
    while i < _len(row)
      if _eq(i, 0)
        line = _str((__tmp7 = row; __tmp7.is_a?(Hash) ? __tmp7[i] : _idx(__tmp7, i)))
      else
        line = _add(_add(line, " "), _str((__tmp8 = row; __tmp8.is_a?(Hash) ? __tmp8[i] : _idx(__tmp8, i))))
      end
      i = _add(i, 1)
    end
    return line
  end
  def print_matrix(mat)
    i = 0
    while i < _len(mat)
      puts(row_to_string((__tmp9 = mat; __tmp9.is_a?(Hash) ? __tmp9[i] : _idx(__tmp9, i))))
      i = _add(i, 1)
    end
  end
  $mat = make_matrix(4)
  puts("\norigin:\n")
  print_matrix($mat)
  puts("\nrotate 90 counterclockwise:\n")
  $r90 = rotate_90($mat)
  print_matrix($r90)
  $mat = make_matrix(4)
  puts("\norigin:\n")
  print_matrix($mat)
  puts("\nrotate 180:\n")
  $r180 = rotate_180($mat)
  print_matrix($r180)
  $mat = make_matrix(4)
  puts("\norigin:\n")
  print_matrix($mat)
  puts("\nrotate 270 counterclockwise:\n")
  $r270 = rotate_270($mat)
  print_matrix($r270)
end_time = Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
