/*
Compute the kinetic energy of an object using the classical formula.

The kinetic energy (KE) for an object of mass m moving at velocity v is
   KE = 1/2 * m * v^2
This script validates the mass is non‑negative, takes the absolute value
of the velocity, and returns the computed energy. The calculation uses a
constant number of arithmetic operations, so the time complexity is O(1).
*/
fun kinetic_energy(mass: float, velocity: float): float {
  if mass < 0.0 {
    panic("The mass of a body cannot be negative")
  }
  var v = velocity
  if v < 0.0 {
    v = -v
  }
  return 0.5 * mass * v * v
}

print(kinetic_energy(10.0, 10.0))
print(kinetic_energy(0.0, 10.0))
print(kinetic_energy(10.0, 0.0))
print(kinetic_energy(20.0, -20.0))
print(kinetic_energy(0.0, 0.0))
print(kinetic_energy(2.0, 2.0))
print(kinetic_energy(100.0, 100.0))
