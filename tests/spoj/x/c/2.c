// Generated by Mochi 0.10.32 on 2025-08-26 14:25 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t split_len;
size_t precompute_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long *primes = NULL;
size_t primes_len = 0;
long long t = 0;
long long case_idx = 0LL;

const char* * split(const char* s, const char* sep);
long long * precompute(long long limit);
int main(void);

const char* * split(const char* s, const char* sep) {
    const char* *parts = NULL;
    size_t parts_len = 0;
    const char* cur = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        if (((strlen(sep) > 0LL) && ((i + strlen(sep)) <= strlen(s))) && (strcmp(_substring(s, i, i + strlen(sep)), sep) == 0)) {
            parts = list_append_str(parts, &parts_len, cur);
            cur = "";
            i = i + strlen(sep);
        } else {
            cur = str_concat(cur, _substring(s, i, i + 1LL));
            i = i + 1LL;
        }
    }
    parts = list_append_str(parts, &parts_len, cur);
    return split_len = parts_len, parts;
}

long long * precompute(long long limit) {
    long long *sieve = NULL;
    size_t sieve_len = 0;
    for (int i = 0LL; i < limit + 1LL; i++) {
        sieve = list_append_long_long(sieve, &sieve_len, 1LL);
    }
    sieve[(int)(0LL)] = 0LL;
    sieve[(int)(1LL)] = 0LL;
    long long p = 2LL;
    while ((p * p) <= limit) {
        if (sieve[(int)({long long _mochi_idx = p; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
            long long j = p * p;
            while (j <= limit) {
                sieve[(int)(j)] = 0LL;
                j = j + p;
            }
        }
        p = p + 1LL;
    }
    long long *primes = NULL;
    size_t primes_len = 0;
    for (int i = 2LL; i < limit + 1LL; i++) {
        if (sieve[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? sieve_len + _mochi_idx : _mochi_idx;})]) {
            primes = list_append_long_long(primes, &primes_len, i);
        }
    }
    return precompute_len = primes_len, primes;
}

int main(void) {
    {
        long long __start = _now();
        primes = precompute(32000LL);
        primes_len = precompute_len;
        t = _atoi(_input());
        while (case_idx < t) {
            const char* line = _input();
            const char* *parts = split(line, " ");
            size_t parts_len = split_len;
            long long m = _atoi(parts[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})]);
            long long n = _atoi(parts[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})]);
            long long size = (n - m) + 1LL;
            long long *segment = NULL;
            size_t segment_len = 0;
            for (int i = 0LL; i < size; i++) {
                segment = list_append_long_long(segment, &segment_len, 1LL);
            }
            for (size_t __i = 0; __i < primes_len; __i++) {
                long long p = primes[__i];
                if ((p * p) > n) {
                    break;
                }
                long long start = p * p;
                if (start < m) {
                    long long rem = m % p;
                    if (rem == 0LL) {
                        start = m;
                    } else {
                        start = m + (p - rem);
                    }
                }
                long long j = start;
                while (j <= n) {
                    segment[(int)(j - m)] = 0LL;
                    j = j + p;
                }
            }
            if (m == 1LL) {
                segment[(int)(0LL)] = 0LL;
            }
            long long i = 0LL;
            while (i < size) {
                if (segment[(int)({long long _mochi_idx = i; _mochi_idx < 0 ? segment_len + _mochi_idx : _mochi_idx;})]) {
                    printf("%d\n", i + m);
                }
                i = i + 1LL;
            }
            if (case_idx < (t - 1LL)) {
                puts("");
            }
            case_idx = case_idx + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
