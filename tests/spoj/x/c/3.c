// Generated by Mochi 0.10.32 on 2025-08-26 14:25 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t solve_len;

size_t append_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static void panic(const char *msg) {
    fputs(msg, stderr);
    fputc('\n', stderr);
    exit(1);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* *sample_input = NULL;
size_t sample_input_len = 0;

long long is_substring(const char* a, const char* b);
long long * solve(const char* * lines, size_t lines_len);
int main(void);

long long is_substring(const char* a, const char* b) {
    long long la = strlen(a);
    long long lb = strlen(b);
    long long i = 0LL;
    while ((i + lb) <= la) {
        if (strcmp(_substring(a, i, i + lb), b) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

long long * solve(const char* * lines, size_t lines_len) {
    long long *res = NULL;
    size_t res_len = 0;
    for (size_t __i = 0; __i < lines_len; __i++) {
        const char* line = lines[__i];
        const char* *parts = NULL;
        size_t parts_len = 0;
        const char* cur = "";
        long long i = 0LL;
        while (i < strlen(line)) {
            const char* ch = _substring(line, i, i + 1LL);
            if (strcmp(ch, " ") == 0) {
                parts = list_append_str(parts, &parts_len, cur);
                cur = "";
            } else {
                cur = str_concat(cur, ch);
            }
            i = i + 1LL;
        }
        parts = list_append_str(parts, &parts_len, cur);
        const char* a = parts[(int)({long long _mochi_idx = 0LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
        const char* b = parts[(int)({long long _mochi_idx = 1LL; _mochi_idx < 0 ? parts_len + _mochi_idx : _mochi_idx;})];
        res = list_append_long_long(res, &res_len, is_substring(a, b));
    }
    return solve_len = res_len, res;
}

int main(void) {
    {
        long long __start = _now();
        sample_input = malloc(2 * sizeof(const char*));
        sample_input_len = 2;
        sample_input[0] = "1010110010 10110";
        sample_input[1] = "1110111011 10011";
        {
            long long* __tmp0 = solve(sample_input, sample_input_len);
            size_t __tmp0_len = solve_len;
            for (size_t __i = 0; __i < __tmp0_len; __i++) {
                long long r = __tmp0[__i];
                printf("%d\n", r);
            }
        }
        long long __end = _now();
        long long __dur_us = (__end - __start + 999) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
