;; Generated by Mochi 0.10.74 on 2025-08-26 14:25 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/spoj/x/mochi")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [(list? seq) (length seq)] [else 0]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) 0)] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (split s sep)
  (let/ec _return (begin
(define parts (vector))
(define cur "")
(define i 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(vector? s) (vector-length s)] [(hash? s) (hash-count s)] [(list? s) (length s)] [else 0])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (and (and (let ([__l (cond [(not sep) 0] [(string? sep) (string-length sep)] [(vector? sep) (vector-length sep)] [(hash? sep) (hash-count sep)] [(list? sep) (length sep)] [else 0])] [__r 0]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])) (let ([__l (let ([__l i] [__r (cond [(not sep) 0] [(string? sep) (string-length sep)] [(vector? sep) (vector-length sep)] [(hash? sep) (hash-count sep)] [(list? sep) (length sep)] [else 0])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))] [__r (cond [(not s) 0] [(string? s) (string-length s)] [(vector? s) (vector-length s)] [(hash? s) (hash-count s)] [(list? s) (length s)] [else 0])]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))]))) (string=? (slice s i (let ([__l i] [__r (cond [(not sep) 0] [(string? sep) (string-length sep)] [(vector? sep) (vector-length sep)] [(hash? sep) (hash-count sep)] [(list? sep) (length sep)] [else 0])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) sep)) (let ()
(set! parts (appendv (or parts (vector)) (vector cur)))
(set! cur "")
(set! i (let ([__l i] [__r (cond [(not sep) 0] [(string? sep) (string-length sep)] [(vector? sep) (vector-length sep)] [(hash? sep) (hash-count sep)] [(list? sep) (length sep)] [else 0])]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! cur (string-append cur (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
(set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
    )
    (loop)) (void))))
(set! parts (appendv (or parts (vector)) (vector cur)))
(_return parts)
))
)
(define (precompute limit)
  (let/ec _return (begin
(define sieve (vector))
(let/ec _break (let ([i 0])
  (let loop ()
    (when (< i (let ([__l limit] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! sieve (appendv (or sieve (vector)) (vector #t)))
      (set! i (+ i 1))
      (loop)))
))
(vector-set! sieve (int 0) #f)
(vector-set! sieve (int 1) #f)
(define p 2)
(let/ec _break (let loop ()
  (if (let ([__l (* p p)] [__r limit]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (if sieve (if (hash? sieve) (hash-ref sieve p #f) (safe-index sieve (int p))) #f) (let ()
(define j (* p p))
(let/ec _break (let loop ()
  (if (let ([__l j] [__r limit]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! sieve (int j) #f)
      (set! j (let ([__l j] [__r p]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
) (void))
      (set! p (let ([__l p] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define primes (vector))
(let/ec _break (let ([i 2])
  (let loop ()
    (when (< i (let ([__l limit] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (if sieve (if (hash? sieve) (hash-ref sieve i #f) (safe-index sieve (int i))) #f) (let ()
(set! primes (appendv (or primes (vector)) (vector i)))
) (void))
      (set! i (+ i 1))
      (loop)))
))
(_return primes)
))
)
(define primes (precompute 32000))
(define t (int (input)))
(define case_idx 0)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(let/ec _break (let loop ()
  (if (let ([__l case_idx] [__r t]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (define line (input))
      (define parts (split line " "))
      (define m (int (if parts (if (hash? parts) (hash-ref parts 0 #f) (safe-index parts (int 0))) #f)))
      (define n (int (if parts (if (hash? parts) (hash-ref parts 1 #f) (safe-index parts (int 1))) #f)))
      (define size (let ([__l (- n m)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (define segment (vector))
      (let/ec _break (let ([i 0])
  (let loop ()
    (when (< i size)
(set! segment (appendv (or segment (vector)) (vector #t)))
      (set! i (+ i 1))
      (loop)))
))
      (let ([__seq primes]) (let/ec _break (for ([p (if (hash? __seq) (in-hash-keys __seq) __seq)] #:break (let ([__l (* p p)] [__r n]) (cond [(and (number? __l) (number? __r)) (> __l __r)] [else (> (int __l) (int __r))])))
  (let/ec _cont
(define start (* p p))
(if (let ([__l start] [__r m]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(define rem (modulo m p))
(if (equal? rem 0) (let ()
(set! start m)
) (let ()
(set! start (let ([__l m] [__r (- p rem)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
) (void))
(define j start)
(let/ec _break (let loop ()
  (if (let ([__l j] [__r n]) (cond [(and (number? __l) (number? __r)) (<= __l __r)] [else (<= (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! segment (int (- j m)) #f)
      (set! j (let ([__l j] [__r p]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
  ))))
      (if (equal? m 1) (let ()
(vector-set! segment (int 0) #f)
) (void))
      (define i 0)
      (let/ec _break (let loop ()
  (if (let ([__l i] [__r size]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (if (if segment (if (hash? segment) (hash-ref segment i #f) (safe-index segment (int i))) #f) (let ()
(displayln (to-string (let ([__l i] [__r m]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (void))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
      (if (let ([__l case_idx] [__r (- t 1)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ()
(displayln (to-string ""))
) (void))
      (set! case_idx (let ([__l case_idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
