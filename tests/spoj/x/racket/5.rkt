;; Generated by Mochi 0.10.74 on 2025-08-26 14:25 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/format racket/system racket/port racket/vector net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define _data_dir "tests/spoj/x/mochi")
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-monotonic-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (to-string x)
  (cond
    [(number? x)
     (cond
       [(and (inexact? x) (integer? x)) (format "~a.0" (inexact->exact x))]
       [else (number->string x)])]
    [(vector? x) (format "~a" (map (lambda (v) (cond [(hash? v) (read (open-input-string (to-string v)))] [(vector? v) (vector->list v)] [else v])) (vector->list x)))]
    [(hash? x)
     (format "~a"
             (for/hash ([k (in-hash-keys x)])
               (values k (let ([v (hash-ref x k)]) (if (vector? v) (vector->list v) v)))))]
    [else (format "~a" x)]))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (cond
    [(string? lst) (substring lst start end)]
    [(vector? lst) (vector->list (vector-copy lst start end))]
    [else (take (drop lst start) (- end start))]))

(define (slice seq start end)
  (define len (cond [(string? seq) (string-length seq)] [(vector? seq) (vector-length seq)] [(list? seq) (length seq)] [else 0]))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (cond
    [(string? seq) (substring seq s e)]
    [(vector? seq) (vector-copy seq s e)]
    [else (sublist seq s e)]))
(define (pad-start s width ch)
  (let ([s (to-string s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (cond
    [(string? s)
     (let loop ([i 0])
       (cond [(>= i (string-length s)) -1]
             [(string=? (substring s i (add1 i)) ch) i]
             [else (loop (add1 i))]))]
    [else
     (let loop ([i 0] [lst s])
       (cond [(null? lst) -1]
             [(equal? (car lst) ch) i]
             [else (loop (add1 i) (cdr lst))]))]))
(define (appendv a b)
  (cond
    [(vector? a) (vector-append a (cond [(vector? b) b] [(list? b) (list->vector b)] [else (vector b)]))]
    [(list? a) (append a (cond [(list? b) b] [(vector? b) (vector->list b)] [else (list b)]))]
    [else (append a b)]))
(define (safe-index lst idx) (let ([i (int idx)]) (cond [(list? lst) (if (and (>= i 0) (< i (length lst))) (list-ref lst i) 0)] [(vector? lst) (if (and (>= i 0) (< i (vector-length lst))) (vector-ref lst i) 0)] [(string? lst) (if (and (>= i 0) (< i (string-length lst))) (substring lst i (add1 i)) "")] [else 0])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_ord s) (char->integer (string-ref s 0)))
(define (read_file path)
  (define p path)
  (let ([root (getenv "MOCHI_ROOT")])
    (when (and root (not (file-exists? p)))
      (set! p (path->string (build-path root _data_dir path)))))
  (with-input-from-file p port->string))
(define (list-set lst idx val)
  (define i (int idx))
  (cond
    [(vector? lst) (when (< i (vector-length lst)) (vector-set! lst i val)) lst]
    [else
     (define len (length lst))
     (cond
       [(>= i len) (append lst (make-list (- i len) '()) (list val))]
       [(zero? i) (cons val (cdr lst))]
       [else (cons (car lst) (list-set (cdr lst) (sub1 i) val))])]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (if (string? bs) (string->bytes/utf-8 bs) (list->bytes bs)))))
(define (_fetch url [opts #f]) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))
(define (json-encode x)
  (define (sanitize v)
    (cond
      [(hash? v) (for/hash ([k (in-hash-keys v)]) (values (string->symbol k) (sanitize (hash-ref v k))))]
      [(list? v) (map sanitize v)]
      [else v]))
  (jsexpr->string (sanitize x)))
(define Object (hash "keys" (lambda (self m) (let ([lst (hash-keys m)]) (hash "join" (lambda (s sep) (string-join lst sep)))))))
(define stdout (hash "write" (lambda (s) (display s))))
(define __name__ "__main__")
(define (input) (let ([ln (read-line)]) (if (eof-object? ln) "" ln)))

(define (next_pal s)
  (let/ec _return (begin
(define digitMap (let ([h (make-hash)]) (hash-set! h "0" 0) (hash-set! h "1" 1) (hash-set! h "2" 2) (hash-set! h "3" 3) (hash-set! h "4" 4) (hash-set! h "5" 5) (hash-set! h "6" 6) (hash-set! h "7" 7) (hash-set! h "8" 8) (hash-set! h "9" 9) h))
(define n (cond [(not s) 0] [(string? s) (string-length s)] [(vector? s) (vector-length s)] [(hash? s) (hash-count s)] [(list? s) (length s)] [else 0]))
(define num (vector))
(let/ec _break (let ([i 0])
  (let loop ()
    (when (< i n)
(set! num (appendv (or num (vector)) (vector (int (if digitMap (hash-ref digitMap (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) #f) #f)))))
      (set! i (+ i 1))
      (loop)))
))
(define all9 #t)
(let ([__seq num]) (let/ec _break (for ([d (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(if (not (equal? d 9)) (let ()
(set! all9 #f)
(_break)
) (void))
  ))))
(if all9 (let ()
(define res "1")
(let/ec _break (let ([_ 0])
  (let loop ()
    (when (< _ (- n 1))
(set! res (string-append res "0"))
      (set! _ (+ _ 1))
      (loop)))
))
(set! res (string-append res "1"))
(_return res)
) (void))
(define left (- (floor (/ n 2)) 1))
(define right (if (equal? (modulo n 2) 0) (floor (/ n 2)) (let ([__l (floor (/ n 2))] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(let/ec _break (let loop ()
  (if (and (and (let ([__l left] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (let ([__l right] [__r n]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))) (equal? (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f) (if num (if (hash? num) (hash-ref num right #f) (safe-index num (int right))) #f))) (begin
    (let/ec _cont
      (set! left (- left 1))
      (set! right (let ([__l right] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(define smaller (or (let ([__l left] [__r 0]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (let ([__l (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f)] [__r (if num (if (hash? num) (hash-ref num right #f) (safe-index num (int right))) #f)]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))]))))
(set! left (- (floor (/ n 2)) 1))
(set! right (if (equal? (modulo n 2) 0) (floor (/ n 2)) (let ([__l (floor (/ n 2))] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
(let/ec _break (let loop ()
  (if (let ([__l left] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! num (int right) (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f))
      (set! left (- left 1))
      (set! right (let ([__l right] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(if smaller (let ()
(define carry 1)
(set! left (- (floor (/ n 2)) 1))
(if (equal? (modulo n 2) 1) (let ()
(define mid (floor (/ n 2)))
(vector-set! num (int mid) (let ([__l (if num (if (hash? num) (hash-ref num mid #f) (safe-index num (int mid))) #f)] [__r carry]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(set! carry (floor (/ (if num (if (hash? num) (hash-ref num mid #f) (safe-index num (int mid))) #f) 10)))
(vector-set! num (int mid) (modulo (if num (if (hash? num) (hash-ref num mid #f) (safe-index num (int mid))) #f) 10))
(set! right (let ([__l mid] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! right (floor (/ n 2)))
))
(let/ec _break (let loop ()
  (if (let ([__l left] [__r 0]) (cond [(and (number? __l) (number? __r)) (>= __l __r)] [else (>= (int __l) (int __r))])) (begin
    (let/ec _cont
      (vector-set! num (int left) (let ([__l (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f)] [__r carry]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! carry (floor (/ (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f) 10)))
      (vector-set! num (int left) (modulo (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f) 10))
      (vector-set! num (int right) (if num (if (hash? num) (hash-ref num left #f) (safe-index num (int left))) #f))
      (set! left (- left 1))
      (set! right (let ([__l right] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
) (void))
(define out "")
(let ([__seq num]) (let/ec _break (for ([d (if (hash? __seq) (in-hash-keys __seq) __seq)])
  (let/ec _cont
(set! out (string-append out (to-string d)))
  ))))
(_return out)
))
)
(define (parseIntStr str)
  (let/ec _return (begin
(define digits (let ([h (make-hash)]) (hash-set! h "0" 0) (hash-set! h "1" 1) (hash-set! h "2" 2) (hash-set! h "3" 3) (hash-set! h "4" 4) (hash-set! h "5" 5) (hash-set! h "6" 6) (hash-set! h "7" 7) (hash-set! h "8" 8) (hash-set! h "9" 9) h))
(define i 0)
(define n 0)
(let/ec _break (let loop ()
  (if (let ([__l i] [__r (cond [(not str) 0] [(string? str) (string-length str)] [(vector? str) (vector-length str)] [(hash? str) (hash-count str)] [(list? str) (length str)] [else 0])]) (cond [(and (number? __l) (number? __r)) (< __l __r)] [else (< (int __l) (int __r))])) (begin
    (let/ec _cont
      (set! n (let ([__l (* n 10)] [__r (int (if digits (hash-ref digits (slice str i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) #f) #f))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
      (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    )
    (loop)) (void))))
(_return n)
))
)
(define (main)
  (let/ec _return (begin
(define tStr (input))
(if (string=? tStr "") (let ()
(_return void)
) (void))
(define t (parseIntStr tStr))
(let/ec _break (let ([_ 0])
  (let loop ()
    (when (< _ t)
(define s (input))
(displayln (to-string (next_pal s)))
      (set! _ (+ _ 1))
      (loop)))
))
))
)
(let* ([_start_mem (current-memory-use)] [_start (current-inexact-monotonic-milliseconds)])
  (let/ec _return (begin
(main)
    (void)
  ))
  (let* ([_end (current-inexact-monotonic-milliseconds)] [_end_mem (current-memory-use)]
         [_dur_us (max 1 (exact-ceiling (* (- _end _start) 1000)))]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
