// Generated by Mochi transpiler v0.10.74 on 2025-08-27 07:10:42 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <vector>
#include <type_traits>
#include <sstream>
#include <any>
#include <iomanip>
#include <cmath>
#include <optional>
#include <cstdint>
#include <limits>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>


static std::vector<std::any> any_to_vec_any(const std::any& v) {
    if (v.type() == typeid(std::vector<std::any>)) return std::any_cast<std::vector<std::any>>(v);
    if (v.type() == typeid(std::vector<int64_t>)) {
        const auto& src = std::any_cast<const std::vector<int64_t>&>(v);
        std::vector<std::any> out; out.reserve(src.size());
        for (auto &x : src) out.push_back(std::any(x));
        return out;
    }
    return {};
}
static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static std::string _repeat(const std::string& s, int64_t n) {
    std::string out; out.reserve(s.size()*n);
    for(int64_t i=0;i<n;i++) out += s;
    return out;
}
template<typename V> auto _index(V& v, int64_t i) -> decltype(v[static_cast<size_t>(0)]) {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) { static std::remove_reference_t<decltype(v[0])> dummy{}; return dummy; }
    return v[static_cast<size_t>(i)];
}
template<typename V> auto _index(const V& v, int64_t i) -> std::remove_reference_t<decltype(v[static_cast<size_t>(0)])> {
    if (i < 0) i += v.size();
    if (i < 0 || i >= (int64_t)v.size()) return std::remove_reference_t<decltype(v[0])>{};
    return v[static_cast<size_t>(i)];
}
static std::string _format_double(double v) {
    std::ostringstream ss;
    ss << std::defaultfloat << std::setprecision(std::numeric_limits<double>::max_digits10) << v;
    auto s = ss.str();
    auto epos = s.find('e');
    if(epos == std::string::npos) epos = s.find('E');
    std::string exp;
    if(epos != std::string::npos){ exp = s.substr(epos); s = s.substr(0, epos); }
    auto pos = s.find('.');
    if(pos != std::string::npos){ while(!s.empty() && s.back() == '0') s.pop_back(); if(!s.empty() && s.back() == '.') s.pop_back(); }
    return s + exp;
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << _format_double(std::any_cast<double>(val));
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ' '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ' '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ' '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << " : "; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(int64_t)) return (double)std::any_cast<int64_t>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static int64_t any_to_int64(const std::any& v) {
    if(v.type() == typeid(int64_t)) return std::any_cast<int64_t>(v);
    if(v.type() == typeid(int)) return (int64_t)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return (int64_t)std::any_cast<double>(v);
    if(v.type() == typeid(std::string)) return std::stoll(std::any_cast<std::string>(v));
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) return _format_double(std::any_cast<double>(v));
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ' '; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    if constexpr(std::is_same_v<T, double>) {
        return _format_double(v);
    } else {
        std::ostringstream ss;
        ss << std::boolalpha << v;
        return ss.str();
    }
}

std::string repeat(std::string s, int64_t n);
std::string add_str(std::string a, std::string b);
std::string sub_str(std::string a, std::string b);
std::string mul_digit(std::string a, int64_t d);
std::map<std::string, std::any> mul_str(std::string a, std::string b);
std::string pad_left(std::string s, int64_t total);
int main();

std::map<std::string, int64_t> digitMap = {{"0", int64_t(0)}, {"1", int64_t(1)}, {"2", int64_t(2)}, {"3", int64_t(3)}, {"4", int64_t(4)}, {"5", int64_t(5)}, {"6", int64_t(6)}, {"7", int64_t(7)}, {"8", int64_t(8)}, {"9", int64_t(9)}};

std::string repeat(std::string s, int64_t n) {
    std::string r = std::string("");
    for (int64_t _ = int64_t(0); _ < n; _++ ) {
        r = (r + s);
    }
    return r;
}

std::string add_str(std::string a, std::string b) {
    int64_t i = (a.size() - int64_t(1));
    int64_t j = (b.size() - int64_t(1));
    int64_t carry = int64_t(0);
    std::string res = std::string("");
    while ((((i >= int64_t(0)) || (j >= int64_t(0))) || (carry > int64_t(0)))) {
        int64_t da = int64_t(0);
        if ((i >= int64_t(0))) {
            da = (int64_t)(([&](const auto& __m){ auto __it = __m.find(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(a)); if (__it == __m.end()) return int64_t{}; return __it->second; })(digitMap));
        }
        int64_t db = int64_t(0);
        if ((j >= int64_t(0))) {
            db = (int64_t)(([&](const auto& __m){ auto __it = __m.find(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(j, (j + int64_t(1)) - j); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+j, __v.begin()+(j + int64_t(1))); })(b)); if (__it == __m.end()) return int64_t{}; return __it->second; })(digitMap));
        }
        int64_t sum = ((da + db) + carry);
        res = (([&]{ std::ostringstream ss; ss << std::boolalpha << (sum % int64_t(10)); return ss.str(); }()) + res);
        carry = (sum / int64_t(10));
        i = (i - int64_t(1));
        j = (j - int64_t(1));
    }
    return res;
}

std::string sub_str(std::string a, std::string b) {
    int64_t i = (a.size() - int64_t(1));
    int64_t j = (b.size() - int64_t(1));
    int64_t borrow = int64_t(0);
    std::string res = std::string("");
    while ((i >= int64_t(0))) {
        int64_t da = ((int64_t)(([&](const auto& __m){ auto __it = __m.find(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(a)); if (__it == __m.end()) return int64_t{}; return __it->second; })(digitMap)) - borrow);
        int64_t db = int64_t(0);
        if ((j >= int64_t(0))) {
            db = (int64_t)(([&](const auto& __m){ auto __it = __m.find(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(j, (j + int64_t(1)) - j); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+j, __v.begin()+(j + int64_t(1))); })(b)); if (__it == __m.end()) return int64_t{}; return __it->second; })(digitMap));
        }
        if ((da < db)) {
            da = (da + int64_t(10));
            borrow = int64_t(1);
        } else {
            borrow = int64_t(0);
        }
        int64_t diff = (da - db);
        res = (([&]{ std::ostringstream ss; ss << std::boolalpha << diff; return ss.str(); }()) + res);
        i = (i - int64_t(1));
        j = (j - int64_t(1));
    }
    int64_t k = int64_t(0);
    while (((k < res.size()) && (([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(k, (k + int64_t(1)) - k); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+k, __v.begin()+(k + int64_t(1))); })(res) == std::string("0")))) {
        k = (k + int64_t(1));
    }
    if ((k == res.size())) {
        return std::string("0");
    }
    return ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(k, res.size() - k); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+k, __v.begin()+res.size()); })(res);
}

std::string mul_digit(std::string a, int64_t d) {
    if ((d == int64_t(0))) {
        return std::string("0");
    }
    int64_t i = (a.size() - int64_t(1));
    int64_t carry = int64_t(0);
    std::string res = std::string("");
    while ((i >= int64_t(0))) {
        int64_t prod = (((int64_t)(([&](const auto& __m){ auto __it = __m.find(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(a)); if (__it == __m.end()) return int64_t{}; return __it->second; })(digitMap)) * d) + carry);
        res = (([&]{ std::ostringstream ss; ss << std::boolalpha << (prod % int64_t(10)); return ss.str(); }()) + res);
        carry = (prod / int64_t(10));
        i = (i - int64_t(1));
    }
    if ((carry > int64_t(0))) {
        res = (([&]{ std::ostringstream ss; ss << std::boolalpha << carry; return ss.str(); }()) + res);
    }
    int64_t k = int64_t(0);
    while (((k < res.size()) && (([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(k, (k + int64_t(1)) - k); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+k, __v.begin()+(k + int64_t(1))); })(res) == std::string("0")))) {
        k = (k + int64_t(1));
    }
    if ((k == res.size())) {
        return std::string("0");
    }
    return ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(k, res.size() - k); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+k, __v.begin()+res.size()); })(res);
}

std::map<std::string, std::any> mul_str(std::string a, std::string b) {
    std::string result = std::string("0");
    int64_t shift = int64_t(0);
    std::vector<std::any> parts = {};
    int64_t i = (b.size() - int64_t(1));
    while ((i >= int64_t(0))) {
        int64_t d = (int64_t)(([&](const auto& __m){ auto __it = __m.find(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(b)); if (__it == __m.end()) return int64_t{}; return __it->second; })(digitMap));
        std::string part = mul_digit(a, d);
        parts = ([&]{ auto __tmp = parts; __tmp.push_back((std::any)std::map<std::string, std::any>{{"val", std::any(part)}, {"shift", std::any(shift)}}); return __tmp; }());
        std::string shifted = part;
        for (int64_t _ = int64_t(0); _ < shift; _++ ) {
            shifted = (shifted + std::string("0"));
        }
        result = add_str(result, shifted);
        shift = (shift + int64_t(1));
        i = (i - int64_t(1));
    }
    return std::map<std::string, std::any>{{"res", std::any(result)}, {"parts", std::any(parts)}};
}

std::string pad_left(std::string s, int64_t total) {
    std::string r = std::string("");
    for (int64_t _ = int64_t(0); _ < (total - s.size()); _++ ) {
        r = (r + std::string(" "));
    }
    return (r + s);
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::string tStr = ([&]{ std::string __line; std::getline(std::cin, __line); return __line; }());
        if ((tStr == std::string(""))) {
            return int64_t(0);
        }
        int64_t t = std::stoll(tStr);
        for (int64_t _ = int64_t(0); _ < t; _++ ) {
            std::string line = ([&]{ std::string __line; std::getline(std::cin, __line); return __line; }());
            if ((line == std::string(""))) {
                continue;
            }
            int64_t idx = int64_t(0);
            while ((idx < line.size())) {
                std::string ch = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(idx, (idx + int64_t(1)) - idx); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+idx, __v.begin()+(idx + int64_t(1))); })(line);
                if ((((ch == std::string("+")) || (ch == std::string("-"))) || (ch == std::string("*")))) {
                    break;
                }
                idx = (idx + int64_t(1));
            }
            std::string a = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), idx - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+idx); })(line);
            std::string op = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(idx, (idx + int64_t(1)) - idx); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+idx, __v.begin()+(idx + int64_t(1))); })(line);
            std::string b = ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr((idx + int64_t(1)), line.size() - (idx + int64_t(1))); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+(idx + int64_t(1)), __v.begin()+line.size()); })(line);
            std::string res = std::string("");
            std::vector<std::any> parts = {};
            if ((op == std::string("+"))) {
                res = add_str(a, b);
            } else             if ((op == std::string("-"))) {
                res = sub_str(a, b);
            } else {
                std::map<std::string, std::any> r = mul_str(a, b);
                res = any_to_string(([&](const auto& __m){ auto __it = __m.find(std::string("res")); if (__it == __m.end()) return std::any{}; return __it->second; })(r));
                parts = any_to_vec_any(([&](const auto& __m){ auto __it = __m.find(std::string("parts")); if (__it == __m.end()) return std::any{}; return __it->second; })(r));
            }

            int64_t width = a.size();
            int64_t secondLen = (b.size() + int64_t(1));
            if ((secondLen > width)) {
                width = secondLen;
            }
            if ((res.size() > width)) {
                width = res.size();
            }
            for (std::any p : parts) {
                int64_t l = (any_to_string(([&](const std::any& __a){ const auto& __m = std::any_cast<const std::map<std::string, std::any>&>(__a); auto __it = __m.find(std::string("val")); if (__it == __m.end()) return std::any{}; return __it->second; })(p)).size() + any_to_int64(([&](const std::any& __a){ const auto& __m = std::any_cast<const std::map<std::string, std::any>&>(__a); auto __it = __m.find(std::string("shift")); if (__it == __m.end()) return std::any{}; return __it->second; })(p)));
                if ((l > width)) {
                    width = l;
                }
            }
            std::cout << pad_left(a, width);
            std::cout << std::endl;
            std::cout << pad_left((op + b), width);
            std::cout << std::endl;
            int64_t dash1 = int64_t(0);
            if ((op == std::string("*"))) {
                if ((parts.size() > int64_t(0))) {
                    dash1 = (b.size() + int64_t(1));
                    std::string firstPart = any_to_string(([&](const std::any& __a){ const auto& __m = std::any_cast<const std::map<std::string, std::any>&>(__a); auto __it = __m.find(std::string("val")); if (__it == __m.end()) return std::any{}; return __it->second; })(_index(parts, int64_t(0))));
                    if ((firstPart.size() > dash1)) {
                        dash1 = firstPart.size();
                    }
                } else {
                    dash1 = (b.size() + int64_t(1));
                    if ((res.size() > dash1)) {
                        dash1 = res.size();
                    }
                }
            } else {
                dash1 = (b.size() + int64_t(1));
                if ((res.size() > dash1)) {
                    dash1 = res.size();
                }
            }
            std::cout << pad_left(_repeat(std::string("-"), dash1), width);
            std::cout << std::endl;
            if (((op == std::string("*")) && (b.size() > int64_t(1)))) {
                for (std::any p : parts) {
                    std::string val = any_to_string(([&](const std::any& __a){ const auto& __m = std::any_cast<const std::map<std::string, std::any>&>(__a); auto __it = __m.find(std::string("val")); if (__it == __m.end()) return std::any{}; return __it->second; })(p));
                    int64_t shift = any_to_int64(([&](const std::any& __a){ const auto& __m = std::any_cast<const std::map<std::string, std::any>&>(__a); auto __it = __m.find(std::string("shift")); if (__it == __m.end()) return std::any{}; return __it->second; })(p));
                    int64_t spaces = ((width - shift) - val.size());
                    line = std::string("");
                    for (int64_t _ = int64_t(0); _ < spaces; _++ ) {
                        line = (line + std::string(" "));
                    }
                    line = (line + val);
                    std::cout << line;
                    std::cout << std::endl;
                }
                std::cout << pad_left(_repeat(std::string("-"), res.size()), width);
                std::cout << std::endl;
            }
            std::cout << pad_left(res, width);
            std::cout << std::endl;
            std::cout << std::string("");
            std::cout << std::endl;
        }
    }
    return 0;
}
