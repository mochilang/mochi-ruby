// Generated by Mochi transpiler v0.10.74 on 2025-08-27 07:27:49 GMT+7
import Foundation
import Dispatch
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

let stdout = FileHandle.standardOutput
extension FileHandle {
    func write(_ string: String) {
        if let data = string.data(using: .utf8) {
            self.write(data)
        }
    }
}

func _p(_ v: Any?) -> String {
    if let val = v {
        if let d = val as? Double {
            if d.isNaN || d.isInfinite {
                return String(d)
            }
            if d.rounded(.towardZero) == d {
                // avoid overflow when converting to Int64
                if d <= Double(Int64.max) && d >= Double(Int64.min) {
                    return String(Int64(d))
                }
            }
        }
        return String(describing: val)
    }
    return "<nil>"
}

extension Double { init(_ v: Any) { if let d = v as? Double { self = d } else if let i = v as? Int { self = Double(i) } else if let i = v as? Int64 { self = Double(i) } else if let s = v as? String { self = Double(s) ?? 0 } else { self = 0 } } }
var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(DispatchTime.now().uptimeNanoseconds)
}
func _num(_ v: Any) -> Double {
    if let d = v as? Double { return d }
    if let i = v as? Int { return Double(i) }
    if let i = v as? Int64 { return Double(i) }
    return 0
}
func _int(_ v: Any) -> Int {
    if let s = v as? String { return Int(s) ?? 0 }
    if let i = v as? Int { return i }
    if let i = v as? Int64 { return Int(i) }
    if let d = v as? Double { return Int(d) }
    return 0
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _idx<T>(_ xs: [T], _ i: Int) -> T {
    var idx = i
    if idx < 0 { idx += xs.count }
    if idx >= 0 && idx < xs.count { return xs[idx] }
    return xs.first!
}
func _idx<K: Hashable, V>(_ m: [K: V], _ k: K) -> V? {
    return m[k]
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _set<T>(_ xs: [T], _ idx: Int, _ v: T) -> [T] {
    var out = xs
    var i = idx
    if i < 0 { i += out.count }
    if i < out.count {
        out[i] = v
    } else {
        out.append(contentsOf: Array(repeating: v, count: i - out.count + 1))
        out[i] = v
    }
    return out
}
func _set<K: Hashable, V>(_ xs: [K: V], _ key: K, _ v: V) -> [K: V] {
    var out = xs
    out[key] = v
    return out
}
func _split(_ s: String, _ sep: String) -> [String] {
    let d = sep.isEmpty ? " " : sep
    return s.components(separatedBy: d)
}
func _slice<T>(_ xs: [T], _ start: Int, _ end: Int) -> [T] {
    var s = start
    var e = end
    if s < 0 { s = 0 }
    if e > xs.count { e = xs.count }
    if s > e { s = e }
    return Array(xs[s..<e])
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func split(_ s: String, _ sep: String) -> [String] {
        var parts: [String] = ([] as! [String])
        var cur: String = ""
        var i: Int = 0
        while ((i < _int(((s).count)))) {
            if ((((_int(((sep).count)) > 0) && ((i &+ _int(((sep).count))) <= _int(((s).count)))) && (String(describing: String(_slice(Array(s), i, (i &+ _int(((sep).count)))))) == sep))) {
                parts = (_append(parts, cur) as! [String])
                cur = ""
                i = _int((i &+ _int(((sep).count))))
            } else {
                cur = ((_p(cur) + _p(String(_slice(Array(s), i, (i &+ 1))))) as! String)
                i = _int((i &+ 1))
            }
        }
        parts = (_append(parts, cur) as! [String])
        return parts
    }
    func parse_ints(_ line: String) -> [Int] {
        let pieces: [String] = (_split(line, " ") as! [String])
        var nums: [Int] = ([] as! [Int])
        for p in pieces {
            if ((_int(((p).count)) > 0)) {
                nums = (_append(nums, _int(_int(p))) as! [Int])
            }
        }
        return nums
    }
    func main() {
        let tLine = (readLine() ?? "0")
        if ((tLine == "")) {
            return
        }
        let t = _int(_int(tLine))
        var caseIdx: Int = 0
        while ((caseIdx < t)) {
            let header: [Int] = (parse_ints(((readLine() ?? "0") as! String)) as! [Int])
            let s = (_idx(header, 0) as? Int ?? 0)
            let c = (_idx(header, 1) as? Int ?? 0)
            let seq: [Int] = (parse_ints(((readLine() ?? "0") as! String)) as! [Int])
            var levels: [[Int]] = ([] as! [[Int]])
            levels = (_append(levels, seq) as! [[Int]])
            var current = seq
            while ((_int(((current).count)) > 1)) {
                var next: [Int] = ([] as! [Int])
                var i: Int = 0
                while (((i &+ 1) < _int(((current).count)))) {
                    next = (_append(next, ((_idx(current, (i &+ 1)) as? Int ?? 0) &- (_idx(current, i) as? Int ?? 0))) as! [Int])
                    i = _int((i &+ 1))
                }
                levels = (_append(levels, next) as! [[Int]])
                current = (next as! [Int])
            }
            var depth = (_int(((levels).count)) &- 1)
            var step: Int = 0
            var res: [Int] = ([] as! [Int])
            while ((step < c)) {
                var bottom = (_idx(levels, depth) as! [Int])
                bottom = (_append(bottom, (_idx(bottom, (_int(((bottom).count)) &- 1)) as? Int ?? 0)) as! [Int])
                levels = _set(levels, depth, { let _tmp0 = (bottom as! [Int]); return _tmp0 }())
                var level = (depth &- 1)
                while ((level >= 0)) {
                    var arr = (_idx(levels, level) as! [Int])
                    var arrBelow = (_idx(levels, (level &+ 1)) as! [Int])
                    var nextVal = ((_idx(arr, (_int(((arr).count)) &- 1)) as? Int ?? 0) &+ (_idx(arrBelow, (_int(((arrBelow).count)) &- 1)) as? Int ?? 0))
                    arr = (_append(arr, nextVal) as! [Int])
                    levels = _set(levels, level, { let _tmp1 = (arr as! [Int]); return _tmp1 }())
                    level = _int((level &- 1))
                }
                res = (_append(res, (_idx(_idx(levels, 0), (_int((((_idx(levels, 0) as! [Int])).count)) &- 1)) as? Int ?? 0)) as! [Int])
                step = _int((step &+ 1))
            }
            var out: String = ""
            var i2: Int = 0
            while ((i2 < _int(((res).count)))) {
                if ((i2 > 0)) {
                    out = ((_p(out) + _p(" ")) as! String)
                }
                out = ((_p(out) + _p(_p((_idx(res, i2) as? Int ?? 0)))) as! String)
                i2 = _int((i2 &+ 1))
            }
            print(_p(out))
            caseIdx = _int((caseIdx &+ 1))
        }
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
