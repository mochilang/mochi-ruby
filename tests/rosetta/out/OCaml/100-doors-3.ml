(* Generated by Mochi compiler v0.10.30 on 2025-07-19T00:24:49Z *)
exception Break
exception Continue


let result : string ref = ref ""

let () =
  try
    for i = 1 to 101 do
      try
        let j : int ref = ref 1 in
        let rec __loop0 () =
          if (((!j) * (!j)) < i) then (
            try
              j := ((!j) + 1);
              __loop0 ()
            with Continue -> ()
          ) else ()
        in try __loop0 () with Break -> ()
        ;
        if (((!j) * (!j)) = i) then (
          result := ((!result) ^ "O");
        ) else (
          result := ((!result) ^ "-");
        ) ;
      with Continue -> ()
    done
  with Break -> () ;
  print_endline ((!result));
