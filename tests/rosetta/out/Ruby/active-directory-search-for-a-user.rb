# Generated by Mochi compiler v0.10.26 on 2025-07-16T12:22:01Z
def search_user(directory, username)
	return directory[username]
end

def main()
	client = {"Base" => "dc=example,dc=com", "Host" => "ldap.example.com", "Port" => 389, "GroupFilter" => "(memberUid=%s)"}
	directory = {"username" => ["admins", "users"], "john" => ["users"]}
	groups = search_user(directory, "username")
	if ((groups).length > 0)
		out = "Groups: ["
		i = 0
		while (i < (groups).length)
			out = (((out + "\"") + groups[i]) + "\"")
			if (i < ((groups).length - 1))
				out = (out + ", ")
			end
			i = (i + 1)
		end
		out = (out + "]")
		puts(out)
	else
		puts("User not found")
	end
end

main()
