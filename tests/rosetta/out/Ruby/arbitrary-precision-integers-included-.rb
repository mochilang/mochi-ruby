# Generated by Mochi compiler v0.10.26 on 2025-07-16T09:43:43Z
def _sliceString(s, i, j)
  start = i
  finish = j
  chars = s.chars
  n = chars.length
  start += n if start < 0
  finish += n if finish < 0
  start = 0 if start < 0
  finish = n if finish > n
  finish = start if finish < start
  chars[start...finish].join
end

def pow_int(base, exp)
	result = 1
	b = base
	e = exp
	while (e > 0)
		if ((e % 2) == 1)
			result = (result * b)
		end
		b = (b * b)
		e = ((e / 2))
	end
	return result
end

def pow_big(base, exp)
	result = 1
	b = base
	e = exp
	while (e > 0)
		if ((e % 2) == 1)
			result = (result * b)
		end
		b = (b * b)
		e = ((e / 2))
	end
	return result
end

$e1 = pow_int(3, 2)
$e2 = pow_int(4, $e1)
$base = 5
$x = pow_big($base, $e2)
$s = ($x).to_s
puts(["5^(4^(3^2)) has", ($s).length, "digits:", _sliceString($s, 0, 20), "...", _sliceString($s, (($s).length - 20), ($s).length)].join(" "))
