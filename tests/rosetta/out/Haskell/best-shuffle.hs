-- Generated by Mochi compiler v0.10.26 on 2025-07-16T09:30:20Z
-- Code generated by Mochi compiler; DO NOT EDIT.
{-# LANGUAGE DeriveGeneric #-}

module Main where

import Data.List (intercalate, isInfixOf, isPrefixOf)
import qualified Data.List as List
import qualified Data.Map as Map
import Data.Maybe (fromMaybe)

forLoop :: Int -> Int -> (Int -> Maybe a) -> Maybe a
forLoop start end f = go start
  where
    go i
      | i < end =
          case f i of
            Just v -> Just v
            Nothing -> go (i + 1)
      | otherwise = Nothing

whileLoop :: (() -> Bool) -> (() -> Maybe a) -> Maybe a
whileLoop cond body = go ()
  where
    go _
      | cond () =
          case body () of
            Just v -> Just v
            Nothing -> go ()
      | otherwise = Nothing

avg :: (Integral a) => [a] -> a
avg xs
  | null xs = 0
  | otherwise = div (sum xs) (fromIntegral (length xs))

data MGroup k a = MGroup {key :: k, items :: [a]} deriving (Show)

_group_by :: (Ord k) => [a] -> (a -> k) -> [MGroup k a]
_group_by src keyfn =
  let go [] m order = (m, order)
      go (x : xs) m order =
        let k = keyfn x
         in case Map.lookup k m of
              Just is -> go xs (Map.insert k (is ++ [x]) m) order
              Nothing -> go xs (Map.insert k [x] m) (order ++ [k])
      (m, order) = go src Map.empty []
   in [MGroup k (fromMaybe [] (Map.lookup k m)) | k <- order]

_indexString :: String -> Int -> String
_indexString s i =
  let idx = if i < 0 then i + length s else i
   in if idx < 0 || idx >= length s
        then error "index out of range"
        else [s !! idx]

_append :: [a] -> a -> [a]
_append xs x = xs ++ [x]

_input :: IO String
_input = getLine

_readInput :: Maybe String -> IO String
_readInput Nothing = getContents
_readInput (Just p)
  | null p || p == "-" = getContents
  | otherwise = readFile p

_writeOutput :: Maybe String -> String -> IO ()
_writeOutput mp text = case mp of
  Nothing -> putStr text
  Just p
    | null p || p == "-" -> putStr text
    | otherwise -> writeFile p text

_split :: Char -> String -> [String]
_split _ "" = [""]
_split d s =
  let (h, t) = break (== d) s
   in h : case t of
        [] -> []
        (_ : rest) -> _split d rest

_parseCSV :: String -> Bool -> Char -> [Map.Map String String]
_parseCSV text header delim =
  let ls = filter (not . null) (lines text)
   in if null ls
        then []
        else
          let heads =
                if header
                  then _split delim (head ls)
                  else ["c" ++ show i | i <- [0 .. length (_split delim (head ls)) - 1]]
              start = if header then 1 else 0
              row line =
                let parts = _split delim line
                 in Map.fromList
                      [ (heads !! j, if j < length parts then parts !! j else "")
                        | j <- [0 .. length heads - 1]
                      ]
           in map row (drop start ls)

data AnyValue = VInt Int | VDouble Double | VString String | VBool Bool deriving (Show)

_asInt :: AnyValue -> Int
_asInt (VInt n) = n
_asInt v = error ("expected int, got " ++ show v)

_asDouble :: AnyValue -> Double
_asDouble (VDouble d) = d
_asDouble v = error ("expected double, got " ++ show v)

_asString :: AnyValue -> String
_asString (VString s) = s
_asString v = error ("expected string, got " ++ show v)

_asBool :: AnyValue -> Bool
_asBool (VBool b) = b
_asBool v = error ("expected bool, got " ++ show v)

_showAny :: AnyValue -> String
_showAny (VInt n) = show n
_showAny (VDouble d) = show d
_showAny (VString s) = s
_showAny (VBool b) = if b then "true" else "false"

nextRand :: Int -> Int
nextRand seed = ((((_asInt (seed) * 1664525) + 1013904223)) `mod` 2147483647)

shuffleChars :: String -> Int -> [()]
shuffleChars s seed =
  fromMaybe ([]) $
    (let chars = [] in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length s)) (\() -> (let chars = (chars ++ [take ((_asInt (i) + 1) - i) (drop i s)]) in (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let sd = seed in (let idx = (length chars - 1) in case whileLoop (\() -> (_asInt (idx) > 0)) (\() -> (let sd = nextRand sd in (let j = (sd `mod` ((_asInt (idx) + 1))) in (let tmp = (chars !! idx) in (let chars = Map.insert idx (chars !! j) chars in (let chars = Map.insert j tmp chars in (let idx = (_asInt (idx) - 1) in Nothing))))))) of Just v -> Just v; Nothing -> (let res = "" in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length chars)) (\() -> (let res = (res + (chars !! i)) in (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> Just ([res, sd])))))))

bestShuffle :: String -> Int -> [()]
bestShuffle s seed =
  fromMaybe ([]) $
    (let r = shuffleChars s seed in (let t = (r !! 0) in (let sd = (r !! 1) in (let arr = [] in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length t)) (\() -> (let arr = (arr ++ [take ((_asInt (i) + 1) - i) (drop i t)]) in (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let i = 0 in case whileLoop (\() -> (_asInt (i) < length arr)) (\() -> (let j = 0 in case whileLoop (\() -> (_asInt (j) < length arr)) (\() -> case if (((((i /= j) && (arr !! i)) /= take ((_asInt (j) + 1) - j) (drop j s)) && (arr !! j)) /= take ((_asInt (i) + 1) - i) (drop i s)) then (let tmp = (arr !! i) in (let arr = Map.insert i (arr !! j) arr in (let arr = Map.insert j tmp arr in Just ()))) else Nothing of Just v -> Just v; Nothing -> (let j = (_asInt (j) + 1) in Nothing)) of Just v -> Just v; Nothing -> (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let count = 0 in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length arr)) (\() -> case if ((arr !! i) == take ((_asInt (i) + 1) - i) (drop i s)) then (let count = (count + 1) in Nothing) else Nothing of Just v -> Just v; Nothing -> (let i = (_asInt (i) + 1) in Nothing)) of Just v -> Just v; Nothing -> (let out = "" in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length arr)) (\() -> (let out = (out + (arr !! i)) in (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> Just ([out, sd, count])))))))))))
  where
    r = shuffleChars s seed

main :: ()
main =
  fromMaybe (()) $
    (let ts = ["abracadabra", "seesaw", "elk", "grrrrrr", "up", "a"] in (let seed = 1 in (let i = 0 in whileLoop (\() -> (_asInt (i) < length ts)) (\() -> (let r = bestShuffle (ts !! i) seed in (let shuf = (r !! 0) in (let seed = (r !! 1) in (let cnt = (r !! 2) in case (let _ = putStrLn (_showAny (((((((ts !! i) + " -> ") + shuf) + " (") + show cnt) + ")"))) in Nothing) of Just v -> Just v; Nothing -> (let i = (_asInt (i) + 1) in Nothing)))))))))
  where
    ts = ["abracadabra", "seesaw", "elk", "grrrrrr", "up", "a"]

main :: IO ()
main = do
  main
