-- Generated by Mochi compiler v0.10.26 on 2025-07-16T09:31:23Z
-- Code generated by Mochi compiler; DO NOT EDIT.
{-# LANGUAGE DeriveGeneric #-}

module Main where

import Data.List (intercalate, isInfixOf, isPrefixOf)
import qualified Data.List as List
import Data.Maybe (fromMaybe)

forLoop :: Int -> Int -> (Int -> Maybe a) -> Maybe a
forLoop start end f = go start
  where
    go i
      | i < end =
          case f i of
            Just v -> Just v
            Nothing -> go (i + 1)
      | otherwise = Nothing

whileLoop :: (() -> Bool) -> (() -> Maybe a) -> Maybe a
whileLoop cond body = go ()
  where
    go _
      | cond () =
          case body () of
            Just v -> Just v
            Nothing -> go ()
      | otherwise = Nothing

avg :: (Integral a) => [a] -> a
avg xs
  | null xs = 0
  | otherwise = div (sum xs) (fromIntegral (length xs))

data MGroup k a = MGroup {key :: k, items :: [a]} deriving (Show)

_group_by :: (Ord k) => [a] -> (a -> k) -> [MGroup k a]
_group_by src keyfn =
  let go [] m order = (m, order)
      go (x : xs) m order =
        let k = keyfn x
         in case Map.lookup k m of
              Just is -> go xs (Map.insert k (is ++ [x]) m) order
              Nothing -> go xs (Map.insert k [x] m) (order ++ [k])
      (m, order) = go src Map.empty []
   in [MGroup k (fromMaybe [] (Map.lookup k m)) | k <- order]

_indexString :: String -> Int -> String
_indexString s i =
  let idx = if i < 0 then i + length s else i
   in if idx < 0 || idx >= length s
        then error "index out of range"
        else [s !! idx]

_append :: [a] -> a -> [a]
_append xs x = xs ++ [x]

_input :: IO String
_input = getLine

_readInput :: Maybe String -> IO String
_readInput Nothing = getContents
_readInput (Just p)
  | null p || p == "-" = getContents
  | otherwise = readFile p

_writeOutput :: Maybe String -> String -> IO ()
_writeOutput mp text = case mp of
  Nothing -> putStr text
  Just p
    | null p || p == "-" -> putStr text
    | otherwise -> writeFile p text

_split :: Char -> String -> [String]
_split _ "" = [""]
_split d s =
  let (h, t) = break (== d) s
   in h : case t of
        [] -> []
        (_ : rest) -> _split d rest

_parseCSV :: String -> Bool -> Char -> [Map.Map String String]
_parseCSV text header delim =
  let ls = filter (not . null) (lines text)
   in if null ls
        then []
        else
          let heads =
                if header
                  then _split delim (head ls)
                  else ["c" ++ show i | i <- [0 .. length (_split delim (head ls)) - 1]]
              start = if header then 1 else 0
              row line =
                let parts = _split delim line
                 in Map.fromList
                      [ (heads !! j, if j < length parts then parts !! j else "")
                        | j <- [0 .. length heads - 1]
                      ]
           in map row (drop start ls)

_updateAt :: Int -> (a -> a) -> [a] -> [a]
_updateAt i f xs = take i xs ++ [f (xs !! i)] ++ drop (i + 1) xs

listStr :: [Int] -> String
listStr xs =
  fromMaybe ("") $
    (let s = "[" in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length xs)) (\() -> (let s = (s + show (xs !! i)) in case if ((_asInt (i) + 1) < length xs) then (let s = (s + " ") in Nothing) else Nothing of Just v -> Just v; Nothing -> (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let s = (s + "]") in Just (s))))

a = [0, 0, 0, 0, 0]

s = take (4 - 0) (drop 0 a)

cap_s = 5

main :: IO ()
main = do
  putStrLn (("len(a) = " ++ show length a))
  putStrLn (("a = " ++ listStr a))
  let a = _updateAt 0 (const 3) a
  putStrLn (("a = " ++ listStr a))
  putStrLn (("a[0] = " ++ show (a !! 0)))
  putStrLn (("s = " ++ listStr s))
  putStrLn (((("len(s) = " ++ show length s) ++ "  cap(s) = ") ++ show cap_s))
  let s = take (5 - 0) (drop 0 a)
  putStrLn (("s = " ++ listStr s))
  let a = _updateAt 0 (const 22) a
  let s = _updateAt 0 (const 22) s
  putStrLn (("a = " ++ listStr a))
  putStrLn (("s = " ++ listStr s))
  let s = (s ++ [4])
  let s = (s ++ [5])
  let s = (s ++ [6])
  let cap_s = 10
  putStrLn (("s = " ++ listStr s))
  putStrLn (((("len(s) = " ++ show length s) ++ "  cap(s) = ") ++ show cap_s))
  let a = _updateAt 4 (const (-1)) a
  putStrLn (("a = " ++ listStr a))
  putStrLn (("s = " ++ listStr s))
  let s = []
  mapM_ (\i -> fromMaybe () ((let s = (s ++ [0]) in Nothing))) [0 .. 8 - 1]
  let cap_s = 8
  putStrLn (("s = " ++ listStr s))
  putStrLn (((("len(s) = " ++ show length s) ++ "  cap(s) = ") ++ show cap_s))
