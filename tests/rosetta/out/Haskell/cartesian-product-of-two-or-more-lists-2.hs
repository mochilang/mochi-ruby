-- Generated by Mochi compiler v0.10.26 on 2025-07-16T09:30:31Z
-- Code generated by Mochi compiler; DO NOT EDIT.
{-# LANGUAGE DeriveGeneric #-}

module Main where

import Data.List (intercalate, isInfixOf, isPrefixOf)
import qualified Data.List as List
import qualified Data.Map as Map
import Data.Maybe (fromMaybe)

forLoop :: Int -> Int -> (Int -> Maybe a) -> Maybe a
forLoop start end f = go start
  where
    go i
      | i < end =
          case f i of
            Just v -> Just v
            Nothing -> go (i + 1)
      | otherwise = Nothing

whileLoop :: (() -> Bool) -> (() -> Maybe a) -> Maybe a
whileLoop cond body = go ()
  where
    go _
      | cond () =
          case body () of
            Just v -> Just v
            Nothing -> go ()
      | otherwise = Nothing

avg :: (Integral a) => [a] -> a
avg xs
  | null xs = 0
  | otherwise = div (sum xs) (fromIntegral (length xs))

data MGroup k a = MGroup {key :: k, items :: [a]} deriving (Show)

_group_by :: (Ord k) => [a] -> (a -> k) -> [MGroup k a]
_group_by src keyfn =
  let go [] m order = (m, order)
      go (x : xs) m order =
        let k = keyfn x
         in case Map.lookup k m of
              Just is -> go xs (Map.insert k (is ++ [x]) m) order
              Nothing -> go xs (Map.insert k [x] m) (order ++ [k])
      (m, order) = go src Map.empty []
   in [MGroup k (fromMaybe [] (Map.lookup k m)) | k <- order]

_indexString :: String -> Int -> String
_indexString s i =
  let idx = if i < 0 then i + length s else i
   in if idx < 0 || idx >= length s
        then error "index out of range"
        else [s !! idx]

_append :: [a] -> a -> [a]
_append xs x = xs ++ [x]

_input :: IO String
_input = getLine

_readInput :: Maybe String -> IO String
_readInput Nothing = getContents
_readInput (Just p)
  | null p || p == "-" = getContents
  | otherwise = readFile p

_writeOutput :: Maybe String -> String -> IO ()
_writeOutput mp text = case mp of
  Nothing -> putStr text
  Just p
    | null p || p == "-" -> putStr text
    | otherwise -> writeFile p text

_split :: Char -> String -> [String]
_split _ "" = [""]
_split d s =
  let (h, t) = break (== d) s
   in h : case t of
        [] -> []
        (_ : rest) -> _split d rest

_parseCSV :: String -> Bool -> Char -> [Map.Map String String]
_parseCSV text header delim =
  let ls = filter (not . null) (lines text)
   in if null ls
        then []
        else
          let heads =
                if header
                  then _split delim (head ls)
                  else ["c" ++ show i | i <- [0 .. length (_split delim (head ls)) - 1]]
              start = if header then 1 else 0
              row line =
                let parts = _split delim line
                 in Map.fromList
                      [ (heads !! j, if j < length parts then parts !! j else "")
                        | j <- [0 .. length heads - 1]
                      ]
           in map row (drop start ls)

listStr :: [Int] -> String
listStr xs =
  fromMaybe ("") $
    (let s = "[" in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length xs)) (\() -> (let s = (s + show (xs !! i)) in case if ((_asInt (i) < length xs) - 1) then (let s = (s + " ") in Nothing) else Nothing of Just v -> Just v; Nothing -> (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let s = (s + "]") in Just (s))))

llStr :: [[Int]] -> String
llStr lst =
  fromMaybe ("") $
    (let s = "[" in (let i = 0 in case whileLoop (\() -> (_asInt (i) < length lst)) (\() -> (let s = (s + listStr (lst !! i)) in case if ((_asInt (i) < length lst) - 1) then (let s = (s + " ") in Nothing) else Nothing of Just v -> Just v; Nothing -> (let i = (_asInt (i) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let s = (s + "]") in Just (s))))

cartN :: () -> [[Int]]
cartN lists = fromMaybe ([]) $
  case if (lists == 0) then Just ([]) else Nothing of Just v -> Just v; Nothing -> (let a = lists in case if (length a == 0) then Just ([[]]) else Nothing of Just v -> Just v; Nothing -> (let c = 1 in case foldr (\xs acc -> case (let c = (_asInt (c) * length xs) in Nothing) of Just v -> Just v; Nothing -> acc) Nothing a of Just v -> Just v; Nothing -> case if (_asInt (c) == 0) then Just ([]) else Nothing of Just v -> Just v; Nothing -> (let res = [] in (let idx = [] in case foldr (\_ acc -> case (let idx = (idx ++ [0]) in Nothing) of Just v -> Just v; Nothing -> acc) Nothing a of Just v -> Just v; Nothing -> (let n = length a in (let count = 0 in case whileLoop (\() -> (count < c)) (\() -> (let row = [] in (let j = 0 in case whileLoop (\() -> (j < n)) (\() -> (let row = (row ++ [((a !! j) !! (idx !! j))]) in (let j = (_asInt (j) + 1) in Nothing))) of Just v -> Just v; Nothing -> (let res = (res ++ [row]) in (let k = (_asInt (n) - 1) in case whileLoop (\() -> (_asInt (k) >= 0)) (\() -> (let idx = Map.insert k (_asInt ((idx !! k)) + 1) idx in case if (_asInt ((idx !! k)) < length (a !! k)) then Just () else Nothing of Just v -> Just v; Nothing -> (let idx = Map.insert k 0 idx in (let k = (_asInt (k) - 1) in Nothing)))) of Just v -> Just v; Nothing -> (let count = (count + 1) in Nothing)))))) of Just v -> Just v; Nothing -> Just (res)))))))

main :: ()
main = fromMaybe (()) $
  case (let _ = putStrLn (llStr cartN [[1, 2], [3, 4]]) in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn (llStr cartN [[3, 4], [1, 2]]) in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn (llStr cartN [[1, 2], []]) in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn (llStr cartN [[], [1, 2]]) in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn ("") in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn ("[") in Nothing) of Just v -> Just v; Nothing -> case foldr (\p acc -> case (let _ = putStrLn ((" " ++ listStr p)) in Nothing) of Just v -> Just v; Nothing -> acc) Nothing cartN [[1776, 1789], [7, 12], [4, 14, 23], [0, 1]] of Just v -> Just v; Nothing -> case (let _ = putStrLn ("]") in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn (llStr cartN [[1, 2, 3], [30], [500, 100]]) in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn (llStr cartN [[1, 2, 3], [], [500, 100]]) in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn ("") in Nothing) of Just v -> Just v; Nothing -> case (let _ = putStrLn (llStr cartN 0) in Nothing) of Just v -> Just v; Nothing -> (let _ = putStrLn (llStr cartN []) in Nothing)

main :: IO ()
main = do
  main
