// Generated by Mochi compiler v0.10.30 on 2025-07-18T15:52:54Z
open System

let msg: string = "Hello World! "
let mutable shift: int = 0
let mutable inc: int = 1
let mutable clicks: int = 0
let mutable frames: int = 0
while clicks < 5 do
    let mutable line: string = ""
    let mutable i: int = 0
    while i < msg.Length do
        let idx = (shift + i) % msg.Length
        line <- line + msg.Substring(idx, idx + 1 - idx)
        i <- i + 1
    printfn "%s" line
    shift <- (shift + inc) % msg.Length
    frames <- frames + 1
    if frames % msg.Length = 0 then
        inc <- msg.Length - inc
        clicks <- clicks + 1
