// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:57:12Z
func lower(_ ch: String) -> String {
    let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    let lower = "abcdefghijklmnopqrstuvwxyz"
    var i = 0
    while i < upper.count {
        if ch == String(upper[upper.index(upper.startIndex, offsetBy: i)..<upper.index(upper.startIndex, offsetBy: i + 1)]) {
            return String(lower[lower.index(lower.startIndex, offsetBy: i)..<lower.index(lower.startIndex, offsetBy: i + 1)])
        }
        i = i + 1
    }
    return ch
}
var partList = ["A", "B", "C", "D"]
var nAssemblies = 3
for cycle in 1..<(nAssemblies + 1) {
    print("begin assembly cycle " + String(cycle))
    var a = ""
    for p in partList {
        print(p + " worker begins part")
        print(p + " worker completed " + lower(p))
        a = a + lower(p)
    }
    print(a + " assembled.  cycle " + String(cycle) + " complete")
}
