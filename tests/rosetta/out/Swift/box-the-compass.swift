// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:55:38Z
func padLeft(_ s: String, _ w: Int) -> String {
    var res = ""
    var n = w - s.count
    while n > 0 {
        res = res + " "
        n = n - 1
    }
    return res + s
}
func padRight(_ s: String, _ w: Int) -> String {
    var out = s
    var i = s.count
    while i < w {
        out = out + " "
        i = i + 1
    }
    return out
}
func indexOf(_ s: String, _ ch: String) -> Int {
    var i = 0
    while i < s.count {
        if String(s[s.index(s.startIndex, offsetBy: i)..<s.index(s.startIndex, offsetBy: i + 1)]) == ch {
            return i
        }
        i = i + 1
    }
    return -1
}
func format2(_ f: Double) -> String {
    var s = String(f)
    let idx = indexOf(s, ".")
    if idx < 0 {
        s = s + ".00"
    }
    else {
        var need = idx + 3
        if s.count > need {
            s = String(s[s.index(s.startIndex, offsetBy: 0)..<s.index(s.startIndex, offsetBy: need)])
        }
        else {
            while s.count < need {
                s = s + "0"
            }
        }
    }
    return s
}
func cpx(_ h: Double) -> Int {
    var x = Int(((h / 11.25) + 0.5))
    x = x % 32
    if x < 0 {
        x = x + 32
    }
    return x
}
var compassPoint = ["North", "North by east", "North-northeast", "Northeast by north", "Northeast", "Northeast by east", "East-northeast", "East by north", "East", "East by south", "East-southeast", "Southeast by east", "Southeast", "Southeast by south", "South-southeast", "South by east", "South", "South by west", "South-southwest", "Southwest by south", "Southwest", "Southwest by west", "West-southwest", "West by south", "West", "West by north", "West-northwest", "Northwest by west", "Northwest", "Northwest by north", "North-northwest", "North by west"]
func degrees2compasspoint(_ h: Double) -> String {
    return compassPoint[cpx(h)]
}
var headings = [0.0, 16.87, 16.88, 33.75, 50.62, 50.63, 67.5, 84.37, 84.38, 101.25, 118.12, 118.13, 135.0, 151.87, 151.88, 168.75, 185.62, 185.63, 202.5, 219.37, 219.38, 236.25, 253.12, 253.13, 270.0, 286.87, 286.88, 303.75, 320.62, 320.63, 337.5, 354.37, 354.38]
print("Index  Compass point         Degree")
var i = 0
while i < headings.count {
    let h = headings[i]
    let idx = i % 32 + 1
    let cp = degrees2compasspoint(h)
    print(padLeft(String(idx), 4) + "   " + padRight(cp, 19) + " " + format2(h) + "°")
    i = i + 1
}
