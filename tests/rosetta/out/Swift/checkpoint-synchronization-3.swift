// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:57:12Z
func lower(_ ch: String) -> String {
    let up = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
    let low = "abcdefghijklmnopqrstuvwxyz"
    var i = 0
    while i < up.count {
        if ch == String(up[up.index(up.startIndex, offsetBy: i)..<up.index(up.startIndex, offsetBy: i + 1)]) {
            return String(low[low.index(low.startIndex, offsetBy: i)..<low.index(low.startIndex, offsetBy: i + 1)])
        }
        i = i + 1
    }
    return ch
}
var partList = ["A", "B", "C", "D"]
var nAssemblies = 3
for p in partList {
    print(p + " worker running")
}
for cycle in 1..<(nAssemblies + 1) {
    print("begin assembly cycle " + String(cycle))
    var a = ""
    for p in partList {
        print(p + " worker begins part")
        print(p + " worker completed " + lower(p))
        a = a + lower(p)
    }
    print(a + " assembled.  cycle " + String(cycle) + " complete")
}
for p in partList {
    print(p + " worker stopped")
}
