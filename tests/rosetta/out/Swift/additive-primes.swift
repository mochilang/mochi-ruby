// Generated by Mochi compiler v0.10.26 on 2025-07-16T13:15:40Z
func isPrime(_ n: Int) -> Bool {
    if n < 2 {
        return false
    }
    if n % 2 == 0 {
        return n == 2
    }
    if n % 3 == 0 {
        return n == 3
    }
    var d = 5
    while d * d <= n {
        if n % d == 0 {
            return false
        }
        d = d + 2
        if n % d == 0 {
            return false
        }
        d = d + 4
    }
    return true
}
func sumDigits(_ n: Int) -> Int {
    var s = 0
    var x = n
    while x > 0 {
        s = s + x % 10
        x = Int((x / 10))
    }
    return s
}
func pad(_ n: Int) -> String {
    if n < 10 {
        return "  " + String(n)
    }
    if n < 100 {
        return " " + String(n)
    }
    return String(n)
}
func main() {
    print("Additive primes less than 500:")
    var count = 0
    var line = ""
    var lineCount = 0
    var i = 2
    while i < 500 {
        if isPrime(i) && isPrime(sumDigits(i)) {
            count = count + 1
            line = line + pad(i) + "  "
            lineCount = lineCount + 1
            if lineCount == 10 {
                print(String(line[line.index(line.startIndex, offsetBy: 0)..<line.index(line.startIndex, offsetBy: line.count - 2)]))
                line = ""
                lineCount = 0
            }
        }
        if i > 2 {
            i = i + 2
        }
        else {
            i = i + 1
        }
    }
    if lineCount > 0 {
        print(String(line[line.index(line.startIndex, offsetBy: 0)..<line.index(line.startIndex, offsetBy: line.count - 2)]))
    }
    print(String(count) + " additive primes found.")
}
main()
