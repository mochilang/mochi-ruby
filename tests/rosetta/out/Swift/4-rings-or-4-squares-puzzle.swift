// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:40:04Z
func validComb(_ a: Int, _ b: Int, _ c: Int, _ d: Int, _ e: Int, _ f: Int, _ g: Int) -> Bool {
    let square1 = a + b
    let square2 = b + c + d
    let square3 = d + e + f
    let square4 = f + g
    return square1 == square2 && square2 == square3 && square3 == square4
}
func isUnique(_ a: Int, _ b: Int, _ c: Int, _ d: Int, _ e: Int, _ f: Int, _ g: Int) -> Bool {
    var nums = [a, b, c, d, e, f, g]
    var i = 0
    while i < nums.count {
        var j = i + 1
        while j < nums.count {
            if nums[i] == nums[j] {
                return false
            }
            j = j + 1
        }
        i = i + 1
    }
    return true
}
func getCombs(_ low: Int, _ high: Int, _ unique: Bool) -> [String: Any] {
    var valid: [[Int]] = []
    var count = 0
    for b in low..<(high + 1) {
        for c in low..<(high + 1) {
            for d in low..<(high + 1) {
                let s = b + c + d
                for e in low..<(high + 1) {
                    for f in low..<(high + 1) {
                        let a = s - b
                        let g = s - f
                        if a < low || a > high {
                            continue
                        }
                        if g < low || g > high {
                            continue
                        }
                        if d + e + f != s {
                            continue
                        }
                        if f + g != s {
                            continue
                        }
                        if !unique || isUnique(a, b, c, d, e, f, g) {
                            valid = valid + [[a, b, c, d, e, f, g]]
                            count = count + 1
                        }
                    }
                }
            }
        }
    }
    return ["count": count, "list": valid]
}
let r1 = getCombs(1, 7, true)
print(String(describing: r1["count"]) + " unique solutions in 1 to 7")
print(r1["list"])
let r2 = getCombs(3, 9, true)
print(String(describing: r2["count"]) + " unique solutions in 3 to 9")
print(r2["list"])
let r3 = getCombs(0, 9, false)
print(String(describing: r3["count"]) + " non-unique solutions in 0 to 9")
