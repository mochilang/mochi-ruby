// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:22Z
program CheckpointSynchronization1;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser, fpjsonrtti;

type
  generic TArray<T> = array of T;

var
  cycle: integer;
  nAssemblies: integer;
  p: string;
  partList: specialize TArray<string>;

begin
  partList := specialize TArray<string>(['A', 'B', 'C', 'D']);
  nAssemblies := 3;
  for cycle := 1 to nAssemblies + 1 - 1 do
  begin
    writeln('begin assembly cycle ' + IntToStr(cycle));
    for p in partList do
    begin
      writeln(p + ' worker begins part');
    end;
    for p in partList do
    begin
      writeln(p + ' worker completes part');
    end;
    writeln('assemble.  cycle ' + IntToStr(cycle) + ' complete');
  end;
end.
