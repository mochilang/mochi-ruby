// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:11Z
program AveragesMedian1;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser, fpjsonrtti;

type
  generic TArray<T> = array of T;

function sortFloat(xs: specialize TArray<double>): specialize TArray<double>;
var
  arr: Variant;
  i: integer;
  j: integer;
  n: Variant;
  tmp: Variant;
begin
  arr := xs;
  n := Length(arr);
  i := 0;
  while (i < n) do
  begin
    j := 0;
    while (j < n - 1) do
    begin
      if (arr[j] > arr[j + 1]) then
      begin
        tmp := arr[j];
        arr[j] := arr[j + 1];
        arr[j + 1] := tmp;
      end;
      j := j + 1;
    end;
    i := i + 1;
  end;
  result := arr;
  exit;
end;

function median(a: specialize TArray<double>): double;
var
  arr: Variant;
  half: integer;
  m: Variant;
begin
  arr := sortFloat(a);
  half := Trunc(Length(arr) div 2);
  m := arr[half];
  if (Length(arr) mod 2 = 0) then ;
  result := m;
  exit;
end;

begin
  writeln(FloatToStr(median(specialize TArray<double>([3, 1, 4, 1]))));
  writeln(FloatToStr(median(specialize TArray<double>([3, 1, 4, 1, 5]))));
end.
