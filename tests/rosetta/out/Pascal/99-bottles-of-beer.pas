// Generated by Mochi compiler v0.10.30 on 1970-01-01T00:00:00Z
program _9BottlesOfBeer;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, Classes, Variants;

type
  generic TArray<T> = array of T;

function bottles(n: integer): string;
begin
  if (n = 0) then ;
  if (n = 1) then ;
  result := IntToStr(n) + ' bottles';
  exit;
end;

procedure main();
var
  i: integer;
begin
  i := 99;
  while (i > 0) do
  begin
    writeln(bottles(i) + ' of beer on the wall');
    writeln(bottles(i) + ' of beer');
    writeln('Take one down, pass it around');
    writeln(bottles(i - 1) + ' of beer on the wall');
    i := i - 1;
  end;
end;

begin
  main();
end.
