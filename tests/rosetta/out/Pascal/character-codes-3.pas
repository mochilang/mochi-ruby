// Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:21Z
program CharacterCodes3;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser, fpjsonrtti;

type
  generic TArray<T> = array of T;

function ord(ch: string): integer;
begin
  if (ch = 'a') then ;
  if (ch = 'π') then ;
  if (ch = 'A') then ;
  result := 0;
  exit;
end;

function chr(n: integer): string;
begin
  if (n = 97) then ;
  if (n = 960) then ;
  if (n = 65) then ;
  result := '?';
  exit;
end;

var
  b: Variant;
  r: Variant;
  s: string;

begin
  b := ord('a');
  r := ord('π');
  s := 'aπ';
  writeln(IntToStr(b) + ' ' + IntToStr(r) + ' ' + s);
  writeln('string cast to []rune: [' + IntToStr(b) + ' ' + IntToStr(r) + ']');
  writeln('    string range loop: ' + IntToStr(b) + ' ' + IntToStr(r));
  writeln('         string bytes: 0x61 0xcf 0x80');
end.
