// Generated by Mochi compiler v0.10.30 on 1970-01-01T00:00:00Z
program ArithmeticInteger1;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, Classes, Variants;

type
  generic TArray<T> = array of T;

procedure main();
var
  a: integer;
  b: integer;
begin
  a := 12;
  b := 8;
  writeln(IntToStr(a) + ' + ' + IntToStr(b) + ' = ' + IntToStr(a + b));
  writeln(IntToStr(a) + ' - ' + IntToStr(b) + ' = ' + IntToStr(a - b));
  writeln(IntToStr(a) + ' * ' + IntToStr(b) + ' = ' + IntToStr(a * b));
  writeln(IntToStr(a) + ' / ' + IntToStr(b) + ' = ' + IntToStr(Trunc(a div b)));
  writeln(IntToStr(a) + ' % ' + IntToStr(b) + ' = ' + IntToStr(a mod b));
end;

begin
  main();
end.
