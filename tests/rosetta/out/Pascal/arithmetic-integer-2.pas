// Generated by Mochi compiler v0.10.30 on 1970-01-01T00:00:00Z
program ArithmeticInteger2;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, Classes, Variants;

type
  generic TArray<T> = array of T;

procedure main();
var
  a: integer;
  b: integer;
begin
  a := 12345678;
  b := 98765;
  writeln(IntToStr(a) + ' + ' + IntToStr(b) + ' = ' + IntToStr(a + b));
  writeln(IntToStr(a) + ' - ' + IntToStr(b) + ' = ' + IntToStr(a - b));
  writeln(IntToStr(a) + ' * ' + IntToStr(b) + ' = ' + IntToStr(a * b));
  writeln(IntToStr(a) + ' quo ' + IntToStr(b) + ' = ' + IntToStr(Trunc(a div b)));
  writeln(IntToStr(a) + ' rem ' + IntToStr(b) + ' = ' + IntToStr(a mod b));
end;

begin
  main();
end.
