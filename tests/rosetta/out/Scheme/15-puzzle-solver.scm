; Generated by Mochi compiler v0.10.30 on 2025-07-18T18:26:45Z
(define (map-get m k)
    (let ((p (assoc k m)))
        (if p (cdr p) '()))
)
(define (map-set m k v)
    (let ((p (assoc k m)))
        (if p
            (begin (set-cdr! p v) m)
            (cons (cons k v) m)))
)

(define testpkg (list (cons 'Add (lambda (a b) (+ a b))) (cons 'Pi 3.14) (cons 'Answer 42)))
(begin (display ((map-get testpkg 'FifteenPuzzleExample) )) (newline))
