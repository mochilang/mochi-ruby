; Generated by Mochi compiler v0.10.30 on 2025-07-18T18:26:26Z
(define door '())
(define incrementer '())
(define line '())
(set! door 1)
(set! incrementer 0)
(let loop ((current 1))
  (if (< current 101)
    (begin
      (set! line (string-append (string-append "Door " (let ((s (open-output-string))) (write current s) (get-output-string s))) " "))
      (if (equal? current door)
        (begin
          (set! line (string-append line "Open"))
          (set! incrementer (+ incrementer 1))
          (set! door (+ (+ door (* 2 incrementer)) 1))
        )
        (begin
          (set! line (string-append line "Closed"))
        )
      )
      (begin (display line) (newline))
      (loop (+ current 1))
    )
  '()
)
)
