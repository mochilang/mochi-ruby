-- Generated by Mochi compiler v0.10.30 on 2025-07-19T00:34:28Z
function __add(a, b)
    if type(a) == 'table' and type(b) == 'table' then
        local out = {}
        for i = 1, #a do out[#out+1] = a[i] end
        for i = 1, #b do out[#out+1] = b[i] end
        return out
    elseif type(a) == 'string' or type(b) == 'string' then
        return tostring(a) .. tostring(b)
    else
        return a + b
    end
end
door = 1;
incrementer = 0;
for current = 1, (101)-1 do
    local line = (("Door " .. tostring(current)) .. " ");
    if (current == door) then
        line = __add(line, "Open");
        incrementer = (incrementer + 1);
        door = ((door + (2 * incrementer)) + 1);
    else
        line = __add(line, "Closed");
    end
    print(tostring(line));
end
