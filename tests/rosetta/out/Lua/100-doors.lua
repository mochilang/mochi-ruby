-- Generated by Mochi compiler v0.10.30 on 2025-07-19T00:34:37Z
function __add(a, b)
    if type(a) == 'table' and type(b) == 'table' then
        local out = {}
        for i = 1, #a do out[#out+1] = a[i] end
        for i = 1, #b do out[#out+1] = b[i] end
        return out
    elseif type(a) == 'string' or type(b) == 'string' then
        return tostring(a) .. tostring(b)
    else
        return a + b
    end
end
function __append(lst, v)
    local out = {}
    if lst then for i = 1, #lst do out[#out+1] = lst[i] end end
    out[#out+1] = v
    return out
end
doors = {};
for i = 0, (100)-1 do
    doors = __append(doors, false);
end
for pass = 1, (101)-1 do
    local idx = (pass - 1);
    while (idx < 100) do
        doors[(idx)+1] = not doors[(idx)+1];
        idx = __add(idx, pass);
    end
end
for row = 0, (10)-1 do
    local line = "";
    for col = 0, (10)-1 do
        local idx = __add((row * 10), col);
        if doors[(idx)+1] then
            line = __add(line, "1");
        else
            line = __add(line, "0");
        end
        if (col < 9) then
            line = __add(line, " ");
        end
    end
    print(tostring(line));
end
