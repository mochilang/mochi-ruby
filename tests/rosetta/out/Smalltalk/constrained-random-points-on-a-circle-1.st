" Generated by Mochi compiler v0.10.30 on 2006-01-02T15:04:05Z
"
| nPts rMin rMax span rows r row c u seen min2 max2 n x y rs col key i line j |
Object subclass: #ContinueSignal instanceVariableNames: '' classVariableNames: '' poolDictionaries: '' category: nil!.
nPts := 100.
rMin := 10.
rMax := 15.
span := (((rMax + 1)) + rMax).
rows := {}.
r := 0.
[(r < span)] whileTrue: [.
row := {}.
c := 0.
[(c < ((span * 2)))] whileTrue: [.
row := row copyWith: ' '.
c := (c + 1).
].
.
rows := rows copyWith: row.
r := (r + 1).
].
.
u := 0.
seen := Dictionary from:{}.
min2 := (rMin * rMin).
max2 := (rMax * rMax).
n := 0.
[.
[(n < nPts)] whileTrue: [.
[.
x := ((((now value) % span)) - rMax).
y := ((((now value) % span)) - rMax).
rs := (((x * x)) + ((y * y))).
((((rs < min2)) or: [((rs > max2))])) ifTrue: [
ContinueSignal signal.
] .
n := (n + 1).
row := (y + rMax).
col := (((x + rMax)) * 2).
rows at: row at: col put: '*'.
key := (((((row asString)) + ',')) + ((col asString))).
(seen at: key not) ifTrue: [
seen at: key put: true.
u := (u + 1).
] .
] on: ContinueSignal do: [:ex | ].
].
] on: BreakSignal do: [:ex | ].
i := 0.
[(i < span)] whileTrue: [.
line := ''.
j := 0.
[(j < ((span * 2)))] whileTrue: [.
line := (line + (rows at: i at: j)).
j := (j + 1).
].
.
Transcript show: (line) printString; cr.
i := (i + 1).
].
.
Transcript show: ((((u asString)) + (' unique points'))) printString; cr.
