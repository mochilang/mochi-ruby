//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 6
func printStat(fs map[string]bool, path string) {
	key0 := path
	m1 := fs
	_, ok2 := m1[key0]
	if ok2 {
		if fs[path] {
			fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(path+" is a directory")), "\n"))
		} else {
			fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(path+" is a file")), "\n"))
		}
	} else {
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("stat "+path+": no such file or directory")), "\n"))
	}
}

// line 18
func main() {
	var fs map[string]bool = map[string]bool{}
	fs["docs"] = true
	for _, p := range []string{
		"input.txt",
		"/input.txt",
		"docs",
		"/docs",
	} {
		printStat(fs, p)
	}
}

func main() {
	main()
}
