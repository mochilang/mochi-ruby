//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 5
func powf(base float64, exp int) float64 {
	result := 1.0
	i := 0
	for i < exp {
		result = (result * base)
		i = (i + 1)
	}
	return result
}

// line 16
func nthRoot(x float64, n int) float64 {
	low := 0.0
	high := x
	i := 0
	for i < 60 {
		mid := ((low + high) / 2.0)
		if powf(mid, n) > x {
			high = mid
		} else {
			low = mid
		}
		i = (i + 1)
	}
	return low
}

// line 32
func main() {
	sum := 0.0
	sumRecip := 0.0
	prod := 1.0
	n := 1
	for n <= 10 {
		f := float64(n)
		sum = (sum + f)
		sumRecip = (sumRecip + (1.0 / f))
		prod = (prod * f)
		n = (n + 1)
	}
	count := 10.0
	a := (sum / count)
	g := nthRoot(prod, 10)
	h := (count / sumRecip)
	fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("A: "+fmt.Sprint(any(a))+" G: "+fmt.Sprint(any(g))+" H: "+fmt.Sprint(any(h)))), "\n"))
	fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("A >= G >= H: "+fmt.Sprint(any(((a >= g) && (g >= h)))))), "\n"))
}

func main() {
	main()
}
