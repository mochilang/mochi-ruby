//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 4
func abs(x float64) float64 {
	if x < 0.0 {
		return -x
	}
	return x
}

// line 9
func sqrtApprox(x float64) float64 {
	guess := x
	i := 0
	for i < 20 {
		guess = ((guess + (x / guess)) / 2.0)
		i = (i + 1)
	}
	return guess
}

// line 19
func agmPi() float64 {
	a := 1.0
	g := (1.0 / sqrtApprox(2.0))
	sum := 0.0
	pow := 2.0
	for abs((a - g)) > 0.000000000000001 {
		t := ((a + g) / 2.0)
		u := sqrtApprox((a * g))
		a = t
		g = u
		pow = (pow * 2.0)
		diff := ((a * a) - (g * g))
		sum = (sum + (diff * pow))
	}
	pi := (((4.0 * a) * a) / (1.0 - sum))
	return pi
}

// line 37
func main() {
	fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(fmt.Sprint(any(agmPi())))), "\n"))
}

func main() {
	main()
}
