//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 3
func binom(n int, k int) int {
	if (k < 0) || (k > n) {
		return 0
	}
	kk := k
	if kk > (n - kk) {
		kk = (n - kk)
	}
	res := 1
	i := 0
	for i < kk {
		res = (res * (n - i))
		i = (i + 1)
		res = int((float64(res) / float64(i)))
	}
	return res
}

// line 17
func catalan(n int) int {
	return int((float64(binom((2*n), n)) / float64((n + 1))))
}

// line 21
func main() {
	for i := 0; i < 15; i++ {
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(fmt.Sprint(any(catalan(i))))), "\n"))
	}
}

func main() {
	main()
}
