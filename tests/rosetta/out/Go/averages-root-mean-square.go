//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 4
func sqrtApprox(x float64) float64 {
	guess := x
	i := 0
	for i < 20 {
		guess = ((guess + (x / guess)) / 2.0)
		i = (i + 1)
	}
	return guess
}

var x int

func main() {
	x = 1
	n := 10
	sum := 0.0
	for x <= n {
		sum = (sum + ((float64(x)) * (float64(x))))
		x = (x + 1)
	}
	rms := sqrtApprox((sum / (float64(n))))
	fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(fmt.Sprint(any(rms)))), "\n"))
}
