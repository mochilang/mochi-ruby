//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

func main() {
	nMech := 5
	detailsPerMech := 4
	for mech := 1; mech < (nMech + 1); mech++ {
		id := mech
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("worker "+fmt.Sprint(any(id))+" contracted to assemble "+fmt.Sprint(any(detailsPerMech))+" details")), "\n"))
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("worker "+fmt.Sprint(any(id))+" enters shop")), "\n"))
		d := 0
		for d < detailsPerMech {
			fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("worker "+fmt.Sprint(any(id))+" assembling")), "\n"))
			fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("worker "+fmt.Sprint(any(id))+" completed detail")), "\n"))
			d = (d + 1)
		}
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("worker "+fmt.Sprint(any(id))+" leaves shop")), "\n"))
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("mechanism "+fmt.Sprint(any(mech))+" completed")), "\n"))
	}
}
