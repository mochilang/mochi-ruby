//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
	"time"
)

type v map[string]any

// line 1
func shuffle(xs []string) []string {
	arr := xs
	i := (len(any(arr)) - 1)
	for i > 0 {
		j := (int64(time.Now().UnixNano()) % int64((i + 1)))
		tmp := arr[i]
		arr[i] = arr[j]
		arr[j] = tmp
		i = (i - 1)
	}
	return arr
}

func main() {
	for _, w := range shuffle([]string{"Enjoy", "Rosetta", "Code"}) {
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(w)), "\n"))
	}
}
