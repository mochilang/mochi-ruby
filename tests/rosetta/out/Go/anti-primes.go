//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 1
func countDivisors(n int) int {
	if n < 2 {
		return 1
	}
	count := 2
	i := 2
	for float64(i) <= (float64(n) / float64(2)) {
		if (n % i) == 0 {
			count = (count + 1)
		}
		i = (i + 1)
	}
	return count
}

// line 12
func main() {
	fmt.Println(strings.TrimSuffix(fmt.Sprintln(any("The first 20 anti-primes are:")), "\n"))
	maxDiv := 0
	count := 0
	n := 1
	line := ""
	for count < 20 {
		d := countDivisors(n)
		if d > maxDiv {
			line = line + fmt.Sprint(any(n)) + " "
			maxDiv = d
			count = (count + 1)
		}
		n = (n + 1)
	}
	line = string([]rune(line)[0:(len(any(line)) - 1)])
	fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(line)), "\n"))
}

func main() {
	main()
}
