//go:build ignore

// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 1
func toBin(n int) string {
	if n == 0 {
		return "0"
	}
	bits := ""
	var x int = n
	for x > 0 {
		bits = fmt.Sprint(any((x % 2))) + bits
		x = int((float64(x) / float64(2)))
	}
	return bits
}

func main() {
	for i := 0; i < 16; i++ {
		fmt.Println(strings.TrimSuffix(fmt.Sprintln(any(toBin(i))), "\n"))
	}
}
