// Generated by Mochi compiler v0.10.26 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static int n = 15;

int main() {
  list_int t = list_int_create(0);
  for (int tmp1 = 0; tmp1 < (n + 2); tmp1++) {
    list_int tmp2 = list_int_create(1);
    tmp2.data[0] = 0;
    t = tmp2;
  }
  t[1] = 1;
  for (int i = 1; i < (n + 1); i++) {
    int j = i;
    while (j > 1) {
      t[j] = t.data[j] + t.data[j - 1];
      j = j - 1;
    }
    t[i + 1] = t.data[i];
    j = i + 1;
    while (j > 1) {
      t[j] = t.data[j] + t.data[j - 1];
      j = j - 1;
    }
    int cat = t.data[i + 1] - t.data[i];
    if (i < 10) {
      char *tmp3 = _str(i);
      char *tmp4 = concat_string(" ", tmp3);
      char *tmp5 = concat_string(tmp4, " : ");
      char *tmp6 = _str(cat);
      char *tmp7 = concat_string(tmp5, tmp6);
      printf("%s\n", tmp7);
    } else {
      char *tmp8 = _str(i);
      char *tmp9 = concat_string(tmp8, " : ");
      char *tmp10 = _str(cat);
      char *tmp11 = concat_string(tmp9, tmp10);
      printf("%s\n", tmp11);
    }
  }
  return 0;
}
