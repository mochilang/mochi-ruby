// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:59Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:59Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
list_int sieve(int limit) {
  list_int spf = list_int_create(0);
  __auto_type i = 0;
  while (i <= limit) {
    int tmp1_data[1];
    list_int tmp1 = {1, tmp1_data};
    tmp1.data[0] = 0;
    list_int tmp2 = concat_list_int(spf, tmp1);
    spf = tmp2;
    i = i + 1;
  }
  i = 2;
  while (i <= limit) {
    if (spf.data[i] == 0) {
      spf.data[i] = i;
      if (i * i <= limit) {
        __auto_type j = i * i;
        while (j <= limit) {
          if (spf.data[j] == 0) {
            spf.data[j] = i;
          }
          j = j + i;
        }
      }
    }
    i = i + 1;
  }
  return spf;
}

list_int primesFrom(list_int spf, int limit) {
  list_int primes = list_int_create(0);
  __auto_type i = 3;
  while (i <= limit) {
    if (spf.data[i] == i) {
      int tmp3_data[1];
      list_int tmp3 = {1, tmp3_data};
      tmp3.data[0] = i;
      list_int tmp4 = concat_list_int(primes, tmp3);
      primes = tmp4;
    }
    i = i + 1;
  }
  return primes;
}

char *pad3(int n) {
  char *tmp5 = _str(n);
  __auto_type s = tmp5;
  while (strlen(s) < 3) {
    char *tmp6 = concat_string(" ", s);
    s = tmp6;
  }
  return s;
}

char *commatize(int n) {
  char *tmp7 = _str(n);
  __auto_type s = tmp7;
  __auto_type out = "";
  __auto_type i = strlen(s) - 1;
  __auto_type c = 0;
  while (i >= 0) {
    char *tmp8 = concat_string(slice_string(s, i, i + 1), out);
    out = tmp8;
    c = c + 1;
    if (c % 3 == 0 && i > 0) {
      char *tmp9 = concat_string(",", out);
      out = tmp9;
    }
    i = i - 1;
  }
  return out;
}

int primeCount(list_int primes, int last, list_int spf) {
  __auto_type lo = 0;
  __auto_type hi = primes.len;
  while (lo < hi) {
    __auto_type mid = (int)(((lo + hi) / 2));
    if (primes.data[mid] < last) {
      lo = mid + 1;
    } else {
      hi = mid;
    }
  }
  __auto_type count = lo + 1;
  if (spf.data[last] != last) {
    count = count - 1;
  }
  return count;
}

list_int arithmeticNumbers(int limit, list_int spf) {
  list_int arr = list_int_create(1);
  arr.data[0] = 1;
  __auto_type n = 3;
  while ((1 < limit)) {
    if (spf.data[n] == n) {
      int tmp10_data[1];
      list_int tmp10 = {1, tmp10_data};
      tmp10.data[0] = n;
      list_int tmp11 = concat_list_int(arr, tmp10);
      arr = tmp11;
    } else {
      __auto_type x = n;
      __auto_type sigma = 1;
      __auto_type tau = 1;
      while (x > 1) {
        __auto_type p = spf.data[x];
        if (p == 0) {
          p = x;
        }
        __auto_type cnt = 0;
        __auto_type power = p;
        __auto_type sum = 1;
        while (x % p == 0) {
          x = ((double)x) / ((double)p);
          cnt = cnt + 1;
          sum = sum + power;
          power = power * p;
        }
        sigma = sigma * sum;
        tau = tau * (cnt + 1);
      }
      if (sigma % tau == 0) {
        int tmp12_data[1];
        list_int tmp12 = {1, tmp12_data};
        tmp12.data[0] = n;
        list_int tmp13 = concat_list_int(arr, tmp12);
        arr = tmp13;
      }
    }
    n = n + 1;
  }
  return arr;
}

int mochi_main() {
  __auto_type limit = 1228663;
  __auto_type spf = sieve(limit);
  __auto_type primes = primesFrom(spf, limit);
  __auto_type arr = arithmeticNumbers(1000000, spf);
  printf("The first 100 arithmetic numbers are:\n");
  __auto_type i = 0;
  while (i < 100) {
    __auto_type line = "";
    __auto_type j = 0;
    while (j < 10) {
      char *tmp14 = concat_string(line, pad3(arr.data[i + j]));
      line = tmp14;
      if (j < 9) {
        char *tmp15 = concat_string(line, " ");
        line = tmp15;
      }
      j = j + 1;
    }
    printf("%s\n", line);
    i = i + 10;
  }
  list_int tmp16 = list_int_create(4);
  tmp16.data[0] = 1000;
  tmp16.data[1] = 10000;
  tmp16.data[2] = 100000;
  tmp16.data[3] = 1000000;
  for (int tmp17 = 0; tmp17 < 4; tmp17++) {
    int x = tmp16.data[tmp17];
    __auto_type last = arr.data[x - 1];
    __auto_type lastc = commatize(last);
    char *tmp18 = concat_string("\nThe ", commatize(x));
    char *tmp19 = concat_string(tmp18, "th arithmetic number is: ");
    char *tmp20 = concat_string(tmp19, lastc);
    printf("%s\n", tmp20);
    __auto_type pc = primeCount(primes, last, spf);
    __auto_type comp = x - pc - 1;
    char *tmp21 = concat_string("The count of such numbers <= ", lastc);
    char *tmp22 = concat_string(tmp21, " which are composite is ");
    char *tmp23 = concat_string(tmp22, commatize(comp));
    char *tmp24 = concat_string(tmp23, ".");
    printf("%s\n", tmp24);
  }
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
