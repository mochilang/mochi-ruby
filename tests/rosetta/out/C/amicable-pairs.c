// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:48Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:48Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
int pfacSum(int i) {
  __auto_type sum = 0;
  __auto_type p = 1;
  while (p <= i / 2) {
    if (i % p == 0) {
      sum = sum + p;
    }
    p = p + 1;
  }
  return sum;
}

char *pad(int n, int width) {
  char *tmp1 = _str(n);
  __auto_type s = tmp1;
  while (strlen(s) < width) {
    char *tmp2 = concat_string(" ", s);
    s = tmp2;
  }
  return s;
}

int mochi_main() {
  list_int sums = list_int_create(0);
  __auto_type i = 0;
  while (i < 20000) {
    int tmp3_data[1];
    list_int tmp3 = {1, tmp3_data};
    tmp3.data[0] = 0;
    list_int tmp4 = concat_list_int(sums, tmp3);
    sums = tmp4;
    i = i + 1;
  }
  i = 1;
  while (i < 20000) {
    sums.data[i] = pfacSum(i);
    i = i + 1;
  }
  printf("The amicable pairs below 20,000 are:\n");
  __auto_type n = 2;
  while (n < 19999) {
    __auto_type m = sums.data[n];
    if (m > n && m < 20000 && n == sums.data[m]) {
      char *tmp5 = concat_string("  ", pad(n, 5));
      char *tmp6 = concat_string(tmp5, " and ");
      char *tmp7 = concat_string(tmp6, pad(m, 5));
      printf("%s\n", tmp7);
    }
    n = n + 1;
  }
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
