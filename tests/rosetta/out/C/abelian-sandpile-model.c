// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:40Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:40Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  list_int *data;
} list_list_int;
static list_list_int list_list_int_create(int len) {
  list_list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(list_int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static list_list_int concat_list_list_int(list_list_int a, list_list_int b) {
  list_list_int r = list_list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static int dim = 16;

list_list_int newPile(int d) {
  list_list_int b = {0, NULL};
  __auto_type y = 0;
  while (y < d) {
    list_int row = list_int_create(0);
    __auto_type x = 0;
    while (x < d) {
      int tmp1_data[1];
      list_int tmp1 = {1, tmp1_data};
      tmp1.data[0] = 0;
      list_int tmp2 = concat_list_int(row, tmp1);
      row = tmp2;
      x = x + 1;
    }
    list_list_int tmp3 = list_list_int_create(1);
    tmp3.data[0] = row;
    list_list_int tmp4 = concat_list_list_int(b, tmp3);
    b = tmp4;
    y = y + 1;
  }
  return b;
}

list_list_int handlePile(list_list_int pile, int x, int y) {
  if (pile.data[y].data[x] >= 4) {
    pile.data[y].data[x] = pile.data[y].data[x] - 4;
    if (y > 0) {
      pile.data[y - 1].data[x] = pile.data[y - 1].data[x] + 1;
      if (pile.data[y - 1].data[x] >= 4) {
        pile = handlePile(pile, x, y - 1);
      }
    }
    if (x > 0) {
      pile.data[y].data[x - 1] = pile.data[y].data[x - 1] + 1;
      if (pile.data[y].data[x - 1] >= 4) {
        pile = handlePile(pile, x - 1, y);
      }
    }
    if (y < dim - 1) {
      pile.data[y + 1].data[x] = pile.data[y + 1].data[x] + 1;
      if (pile.data[y + 1].data[x] >= 4) {
        pile = handlePile(pile, x, y + 1);
      }
    }
    if (x < dim - 1) {
      pile.data[y].data[x + 1] = pile.data[y].data[x + 1] + 1;
      if (pile.data[y].data[x + 1] >= 4) {
        pile = handlePile(pile, x + 1, y);
      }
    }
    pile = handlePile(pile, x, y);
  }
  return pile;
}

int drawPile(list_list_int pile, int d) {
  list_string chars = list_string_create(4);
  chars.data[0] = " ";
  chars.data[1] = "░";
  chars.data[2] = "▓";
  chars.data[3] = "█";
  __auto_type row = 0;
  while (row < d) {
    __auto_type line = "";
    __auto_type col = 0;
    while (col < d) {
      __auto_type v = pile.data[row].data[col];
      if (v > 3) {
        v = 3;
      }
      char *tmp5 = concat_string(line, chars.data[v]);
      line = tmp5;
      col = col + 1;
    }
    printf("%s\n", line);
    row = row + 1;
  }
}

int mochi_main() {
  __auto_type pile = newPile(16);
  __auto_type hdim = 7;
  pile.data[hdim].data[hdim] = 16;
  pile = handlePile(pile, hdim, hdim);
  drawPile(pile, 16);
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
