// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:45Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:45Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
int isPrime(int n) {
  if (n < 2) {
    return 0;
  }
  if (n % 2 == 0) {
    return n == 2;
  }
  if (n % 3 == 0) {
    return n == 3;
  }
  __auto_type d = 5;
  while (d * d <= n) {
    if (n % d == 0) {
      return 0;
    }
    d = d + 2;
    if (n % d == 0) {
      return 0;
    }
    d = d + 4;
  }
  return 1;
}

int sumDigits(int n) {
  __auto_type s = 0;
  __auto_type x = n;
  while (x > 0) {
    s = s + x % 10;
    x = (int)((((double)x) / ((double)10)));
  }
  return s;
}

char *pad(int n) {
  if (n < 10) {
    char *tmp1 = _str(n);
    char *tmp2 = concat_string("  ", tmp1);
    return tmp2;
  }
  if (n < 100) {
    char *tmp3 = _str(n);
    char *tmp4 = concat_string(" ", tmp3);
    return tmp4;
  }
  char *tmp5 = _str(n);
  return tmp5;
}

int mochi_main() {
  printf("Additive primes less than 500:\n");
  __auto_type count = 0;
  __auto_type line = "";
  __auto_type lineCount = 0;
  __auto_type i = 2;
  while (i < 500) {
    if (isPrime(i) && isPrime(sumDigits(i))) {
      count = count + 1;
      char *tmp6 = concat_string(line, pad(i));
      char *tmp7 = concat_string(tmp6, "  ");
      line = tmp7;
      lineCount = lineCount + 1;
      if (lineCount == 10) {
        printf("%s\n", slice_string(line, 0, strlen(line) - 2));
        line = "";
        lineCount = 0;
      }
    }
    if (i > 2) {
      i = i + 2;
    } else {
      i = i + 1;
    }
  }
  if (lineCount > 0) {
    printf("%s\n", slice_string(line, 0, strlen(line) - 2));
  }
  char *tmp8 = _str(count);
  char *tmp9 = concat_string(tmp8, " additive primes found.");
  printf("%s\n", tmp9);
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
