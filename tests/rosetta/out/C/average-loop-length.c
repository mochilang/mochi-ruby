// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:09:32Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:09:32Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static int index_of_string(char *s, char *sub) {
  char *pos = strstr(s, sub);
  if (!pos)
    return -1;
  return pos - s;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
double absf(double x) {
  if (x < 0.0) {
    return (-x);
  }
  return x;
}

double floorf(double x) { return (double)(((int)(x))); }

int indexOf(char *s, char *ch) {
  __auto_type i = 0;
  while (i < strlen(s)) {
    if ((strcmp(slice_string(s, i, i + 1), ch) == 0)) {
      return i;
    }
    i = i + 1;
  }
  return (-1);
}

char *fmtF(double x) {
  __auto_type y = floorf(x * 10000.0 + 0.5) / 10000.0;
  char *tmp1 = _str(y);
  __auto_type s = tmp1;
  __auto_type dot = index_of_string(s, ".");
  if (dot == 0 - 1) {
    char *tmp2 = concat_string(s, ".0000");
    s = tmp2;
  } else {
    __auto_type decs = strlen(s) - dot - 1;
    if (decs > 4) {
      s = slice_string(s, 0, dot + 5);
    } else {
      while (decs < 4) {
        char *tmp3 = concat_string(s, "0");
        s = tmp3;
        decs = decs + 1;
      }
    }
  }
  return s;
}

char *padInt(int n, int width) {
  char *tmp4 = _str(n);
  __auto_type s = tmp4;
  while (strlen(s) < width) {
    char *tmp5 = concat_string(" ", s);
    s = tmp5;
  }
  return s;
}

char *padFloat(double x, int width) {
  __auto_type s = fmtF(x);
  while (strlen(s) < width) {
    char *tmp6 = concat_string(" ", s);
    s = tmp6;
  }
  return s;
}

double avgLen(int n) {
  __auto_type tests = 10000;
  __auto_type sum = 0;
  __auto_type seed = 1;
  __auto_type t = 0;
  while (t < tests) {
    list_int visited = {0, NULL};
    __auto_type i = 0;
    while (i < n) {
      int tmp7_data[1];
      list_int tmp7 = {1, tmp7_data};
      tmp7.data[0] = 0;
      list_int tmp8 = concat_list_int(visited, tmp7);
      visited = tmp8;
      i = i + 1;
    }
    __auto_type x = 0;
    while ((!visited.data[x])) {
      visited.data[x] = 1;
      sum = sum + 1;
      seed = (seed * 1664525 + 1013904223) % 2147483647;
      x = seed % n;
    }
    t = t + 1;
  }
  return ((double)(sum)) / tests;
}

double ana(int n) {
  __auto_type nn = (double)(n);
  __auto_type term = 1.0;
  __auto_type sum = 1.0;
  __auto_type i = nn - 1.0;
  while (i >= 1.0) {
    term = term * (i / nn);
    sum = sum + term;
    i = i - 1.0;
  }
  return sum;
}

int mochi_main() {
  __auto_type nmax = 20;
  printf(" N    average    analytical    (error)\n");
  printf("===  =========  ============  =========\n");
  __auto_type n = 1;
  while (n <= nmax) {
    __auto_type a = avgLen(n);
    __auto_type b = ana(n);
    __auto_type err = absf(a - b) / b * 100.0;
    char *tmp9 = concat_string(padInt(n, 3), "  ");
    char *tmp10 = concat_string(tmp9, padFloat(a, 9));
    char *tmp11 = concat_string(tmp10, "  ");
    char *tmp12 = concat_string(tmp11, padFloat(b, 12));
    char *tmp13 = concat_string(tmp12, "  (");
    char *tmp14 = concat_string(tmp13, padFloat(err, 6));
    char *tmp15 = concat_string(tmp14, "%)");
    __auto_type line = tmp15;
    printf("%s\n", line);
    n = n + 1;
  }
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
