// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:14:37Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:14:37Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static list_int doors;

int _mochi_main() {
  list_int tmp1 = list_int_create(0);
  doors = tmp1;
  for (int i = 0; i < 100; i++) {
    int tmp2_data[1];
    list_int tmp2 = {1, tmp2_data};
    tmp2.data[0] = 0;
    list_int tmp3 = concat_list_int(doors, tmp2);
    doors = tmp3;
  }
  for (int pass = 1; pass < 101; pass++) {
    __auto_type idx = pass - 1;
    while (idx < 100) {
      doors.data[idx] = (!doors.data[idx]);
      idx = idx + pass;
    }
  }
  for (int row = 0; row < 10; row++) {
    __auto_type line = "";
    for (int col = 0; col < 10; col++) {
      __auto_type idx = row * 10 + col;
      if ((doors.data[idx])) {
        char *tmp4 = concat_string(line, "1");
        line = tmp4;
      } else {
        char *tmp5 = concat_string(line, "0");
        line = tmp5;
      }
      if (col < 9) {
        char *tmp6 = concat_string(line, " ");
        line = tmp6;
      }
    }
    printf("%s\n", line);
  }
  return 0;
}
int main() { return _mochi_main(); }
