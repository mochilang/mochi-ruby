// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:09:45Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:09:44Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
int toUnsigned16(int n) {
  __auto_type u = n;
  if (u < 0) {
    u = u + 65536;
  }
  return u % 65536;
}

char *bin16(int n) {
  __auto_type u = toUnsigned16(n);
  __auto_type bits = "";
  __auto_type mask = 32768;
  for (int i = 0; i < 16; i++) {
    if (u >= mask) {
      char *tmp1 = concat_string(bits, "1");
      bits = tmp1;
      u = u - mask;
    } else {
      char *tmp2 = concat_string(bits, "0");
      bits = tmp2;
    }
    mask = (int)((((double)mask) / ((double)2)));
  }
  return bits;
}

int bit_and(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type ub = toUnsigned16(b);
  __auto_type res = 0;
  __auto_type bit = 1;
  for (int i = 0; i < 16; i++) {
    if (ua % 2 == 1 && ub % 2 == 1) {
      res = res + bit;
    }
    ua = (int)((((double)ua) / ((double)2)));
    ub = (int)((((double)ub) / ((double)2)));
    bit = bit * 2;
  }
  return res;
}

int bit_or(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type ub = toUnsigned16(b);
  __auto_type res = 0;
  __auto_type bit = 1;
  for (int i = 0; i < 16; i++) {
    if (ua % 2 == 1 || ub % 2 == 1) {
      res = res + bit;
    }
    ua = (int)((((double)ua) / ((double)2)));
    ub = (int)((((double)ub) / ((double)2)));
    bit = bit * 2;
  }
  return res;
}

int bit_xor(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type ub = toUnsigned16(b);
  __auto_type res = 0;
  __auto_type bit = 1;
  for (int i = 0; i < 16; i++) {
    __auto_type abit = ua % 2;
    __auto_type bbit = ub % 2;
    if ((abit == 1 && bbit == 0) || (abit == 0 && bbit == 1)) {
      res = res + bit;
    }
    ua = (int)((((double)ua) / ((double)2)));
    ub = (int)((((double)ub) / ((double)2)));
    bit = bit * 2;
  }
  return res;
}

int bit_not(int a) {
  __auto_type ua = toUnsigned16(a);
  return 65535 - ua;
}

int shl(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type i = 0;
  while (i < b) {
    ua = (ua * 2) % 65536;
    i = i + 1;
  }
  return ua;
}

int shr(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type i = 0;
  while (i < b) {
    ua = (int)((((double)ua) / ((double)2)));
    i = i + 1;
  }
  return ua;
}

int las(int a, int b) { return shl(a, b); }

int ras(int a, int b) {
  __auto_type val = a;
  __auto_type i = 0;
  while (i < b) {
    if (val >= 0) {
      val = (int)((((double)val) / ((double)2)));
    } else {
      val = (int)(((val - 1) / 2));
    }
    i = i + 1;
  }
  return toUnsigned16(val);
}

int rol(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type left = shl(ua, b);
  __auto_type right = shr(ua, 16 - b);
  return toUnsigned16(left + right);
}

int ror(int a, int b) {
  __auto_type ua = toUnsigned16(a);
  __auto_type right = shr(ua, b);
  __auto_type left = shl(ua, 16 - b);
  return toUnsigned16(left + right);
}

int bitwise(int a, int b) {
  char *tmp3 = concat_string("a:   ", bin16(a));
  printf("%s\n", tmp3);
  char *tmp4 = concat_string("b:   ", bin16(b));
  printf("%s\n", tmp4);
  char *tmp5 = concat_string("and: ", bin16(bit_and(a, b)));
  printf("%s\n", tmp5);
  char *tmp6 = concat_string("or:  ", bin16(bit_or(a, b)));
  printf("%s\n", tmp6);
  char *tmp7 = concat_string("xor: ", bin16(bit_xor(a, b)));
  printf("%s\n", tmp7);
  char *tmp8 = concat_string("not: ", bin16(bit_not(a)));
  printf("%s\n", tmp8);
  if (b < 0) {
    printf("Right operand is negative, but all shifts require an unsigned "
           "right operand (shift distance).\n");
    return 0;
  }
  char *tmp9 = concat_string("shl: ", bin16(shl(a, b)));
  printf("%s\n", tmp9);
  char *tmp10 = concat_string("shr: ", bin16(shr(a, b)));
  printf("%s\n", tmp10);
  char *tmp11 = concat_string("las: ", bin16(las(a, b)));
  printf("%s\n", tmp11);
  char *tmp12 = concat_string("ras: ", bin16(ras(a, b)));
  printf("%s\n", tmp12);
  char *tmp13 = concat_string("rol: ", bin16(rol(a, b)));
  printf("%s\n", tmp13);
  char *tmp14 = concat_string("ror: ", bin16(ror(a, b)));
  printf("%s\n", tmp14);
}

int _mochi_main() {
  bitwise((-460), 6);
  return 0;
}
int main() { return _mochi_main(); }
