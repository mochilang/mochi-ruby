// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:55Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:55Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
int countDivisors(int n) {
  if (n < 2) {
    return 1;
  }
  __auto_type count = 2;
  __auto_type i = 2;
  while (i <= n / 2) {
    if (n % i == 0) {
      count = count + 1;
    }
    i = i + 1;
  }
  return count;
}

int mochi_main() {
  printf("The first 20 anti-primes are:\n");
  __auto_type maxDiv = 0;
  __auto_type count = 0;
  __auto_type n = 1;
  __auto_type line = "";
  while ((count < 20)) {
    __auto_type d = countDivisors(n);
    if (d > maxDiv) {
      char *tmp1 = _str(n);
      char *tmp2 = concat_string(line, tmp1);
      char *tmp3 = concat_string(tmp2, " ");
      line = tmp3;
      maxDiv = d;
      count = count + 1;
    }
    n = n + 1;
  }
  line = slice_string(line, 0, strlen(line) - 1);
  printf("%s\n", line);
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
