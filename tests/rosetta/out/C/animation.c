// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:53Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:53Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
static char *msg = "Hello World! ";
static int shift = 0;
static int inc = 1;
static int clicks = 0;
static int frames = 0;

int _mochi_main() {
  while (clicks < 5) {
    __auto_type line = "";
    __auto_type i = 0;
    while (i < strlen(msg)) {
      __auto_type idx = (shift + i) % strlen(msg);
      char *tmp1 = slice_string(msg, idx, idx + 1);
      char *tmp2 = concat_string(line, tmp1);
      line = tmp2;
      i = i + 1;
    }
    printf("%s\n", line);
    shift = (shift + inc) % strlen(msg);
    frames = frames + 1;
    if (frames % strlen(msg) == 0) {
      inc = strlen(msg) - inc;
      clicks = clicks + 1;
    }
  }
  return 0;
}
int main() { return _mochi_main(); }
