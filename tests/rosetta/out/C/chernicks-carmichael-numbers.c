// Generated by Mochi compiler v0.10.28 on 2025-07-18T10:18:09Z
// Generated by Mochi compiler v0.10.28 on 2025-07-18T10:18:09Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static int _count(list_int v) { return v.len; }
static char *_str(int v) {
  char *buf = (char *)malloc(32);
  sprintf(buf, "%d", v);
  return buf;
}
static list_int slice_list_int(list_int v, int start, int end) {
  if (start < 0)
    start += v.len;
  if (end < 0)
    end += v.len;
  if (start < 0)
    start = 0;
  if (end > v.len)
    end = v.len;
  if (start > end)
    start = end;
  list_int r = list_int_create(end - start);
  for (int i = 0; i < r.len; i++)
    r.data[i] = v.data[start + i];
  return r;
}
int isPrime(int n) {
  if (n < 2) {
    return 0;
  }
  if (n % 2 == 0) {
    return n == 2;
  }
  if (n % 3 == 0) {
    return n == 3;
  }
  int d = 5;
  while (d * d <= n) {
    if (n % d == 0) {
      return 0;
    }
    d = d + 2;
    if (n % d == 0) {
      return 0;
    }
    d = d + 4;
  }
  return 1;
}

list_int bigTrim(list_int a) {
  int n = a.len;
  while (n > 1 && a.data[n - 1] == 0) {
    list_int tmp1 = slice_list_int(a, 0, n - 1);
    a = tmp1;
    n = n - 1;
  }
  return a;
}

list_int bigFromInt(int x) {
  if (x == 0) {
    list_int tmp2 = list_int_create(1);
    tmp2.data[0] = 0;
    return tmp2;
  }
  list_int digits = list_int_create(0);
  int n = x;
  while (n > 0) {
    digits = 0;
    n = ((double)n) / ((double)10);
  }
  return digits;
}

list_int bigMulSmall(list_int a, int m) {
  if (m == 0) {
    list_int tmp3 = list_int_create(1);
    tmp3.data[0] = 0;
    return tmp3;
  }
  list_int res = list_int_create(0);
  int carry = 0;
  int i = 0;
  while (i < a.len) {
    int prod = a.data[i] * m + carry;
    res = 0;
    carry = prod / 10;
    i = i + 1;
  }
  while (carry > 0) {
    res = 0;
    carry = ((double)carry) / ((double)10);
  }
  return bigTrim(res);
}

char *bigToString(list_int a) {
  char *s = "";
  int i = a.len - 1;
  while (i >= 0) {
    char *tmp4 = _str(a.data[i]);
    char *tmp5 = concat_string(s, tmp4);
    s = tmp5;
    i = i - 1;
  }
  return s;
}

int pow2(int k) {
  int r = 1;
  int i = 0;
  while (i < k) {
    r = r * 2;
    i = i + 1;
  }
  return r;
}

list_int ccFactors(int n, int m) {
  int p = 6 * m + 1;
  if ((!isPrime(p))) {
    list_int tmp6 = list_int_create(0);
    return tmp6;
  }
  list_int prod = bigFromInt(p);
  p = 12 * m + 1;
  if ((!isPrime(p))) {
    list_int tmp7 = list_int_create(0);
    return tmp7;
  }
  prod = bigMulSmall(prod, p);
  int i = 1;
  while (i <= n - 2) {
    p = (pow2(i) * 9 * m) + 1;
    if ((!isPrime(p))) {
      list_int tmp8 = list_int_create(0);
      return tmp8;
    }
    prod = bigMulSmall(prod, p);
    i = i + 1;
  }
  return prod;
}

int ccNumbers(int start, int end) {
  int n = start;
  while (n <= end) {
    int m = 1;
    if (n > 4) {
      m = pow2(n - 4);
    }
    while (1) {
      list_int num = ccFactors(n, m);
      if ((_count(num) > 0)) {
        char *tmp9 = _str(n);
        char *tmp10 = concat_string("a(", tmp9);
        char *tmp11 = concat_string(tmp10, ") = ");
        char *tmp12 = _str(n);
        char *tmp13 = concat_string("a(", tmp12);
        char *tmp14 = concat_string(tmp13, ") = ");
        printf("%d\n", tmp14 + bigToString(num));
        break;
      }
      if (n <= 4) {
        m = m + 1;
      } else {
        m = m + pow2(n - 4);
      }
    }
    n = n + 1;
  }
}

int _mochi_main() {
  ccNumbers(3, 9);
  return 0;
}
int main() { return _mochi_main(); }
