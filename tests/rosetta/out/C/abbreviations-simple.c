// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:39Z
// Generated by Mochi compiler v0.10.30 on 2025-07-18T17:11:39Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  char *key;
  int value;
} pair_string_int;
static pair_string_int pair_string_int_new(char *key, int value) {
  pair_string_int p;
  p.key = key;
  p.value = value;
  return p;
}
typedef struct {
  int len;
  int cap;
  pair_string_int *data;
} map_string_int;
static map_string_int map_string_int_create(int cap) {
  map_string_int m;
  m.len = 0;
  m.cap = cap;
  m.data = cap ? calloc(cap, sizeof(pair_string_int)) : NULL;
  if (cap && !m.data) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return m;
}
static void map_string_int_put(map_string_int *m, char *k, int v) {
  for (int i = 0; i < m->len; i++)
    if (strcmp(m->data[i].key, k) == 0) {
      m->data[i].value = v;
      return;
    }
  if (m->len >= m->cap) {
    m->cap = m->cap ? m->cap * 2 : 4;
    m->data =
        (pair_string_int *)realloc(m->data, sizeof(pair_string_int) * m->cap);
  }
  m->data[m->len++] = pair_string_int_new(k, v);
}
static int map_string_int_get(map_string_int m, const char *k) {
  for (int i = 0; i < m.len; i++)
    if (strcmp(m.data[i].key, k) == 0)
      return m.data[i].value;
  return 0;
}
static int map_string_int_contains(map_string_int m, const char *k) {
  for (int i = 0; i < m.len; i++)
    if (strcmp(m.data[i].key, k) == 0)
      return 1;
  return 0;
}
static int cmp_map_string_int(map_string_int a, map_string_int b) {
  int aa = map_string_int_get(a, "a");
  int ba = map_string_int_get(b, "a");
  if (aa != ba)
    return aa > ba ? 1 : -1;
  int ab = map_string_int_get(a, "b");
  int bb = map_string_int_get(b, "b");
  if (ab != bb)
    return ab > bb ? 1 : -1;
  return 0;
}
static list_int concat_list_int(list_int a, list_int b) {
  list_int r = list_int_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static list_string concat_list_string(list_string a, list_string b) {
  list_string r = list_string_create(a.len + b.len);
  for (int i = 0; i < a.len; i++)
    r.data[i] = a.data[i];
  for (int i = 0; i < b.len; i++)
    r.data[a.len + i] = b.data[i];
  return r;
}
static char *concat_string(char *a, char *b) {
  size_t len1 = strlen(a);
  size_t len2 = strlen(b);
  char *buf = (char *)malloc(len1 + len2 + 1);
  memcpy(buf, a, len1);
  memcpy(buf + len1, b, len2);
  buf[len1 + len2] = '\0';
  return buf;
}
static char *slice_string(char *s, int start, int end) {
  int len = strlen(s);
  if (start < 0)
    start += len;
  if (end < 0)
    end += len;
  if (start < 0)
    start = 0;
  if (end > len)
    end = len;
  if (start > end)
    start = end;
  char *buf = (char *)malloc(end - start + 1);
  memcpy(buf, s + start, end - start);
  buf[end - start] = '\0';
  return buf;
}
typedef struct {
  list_string commands;
  list_int mins;
} tmp_item_t;
typedef struct {
  int len;
  tmp_item_t *data;
} tmp_item_list_t;
tmp_item_list_t create_tmp_item_list(int len) {
  tmp_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

list_string fields(char *s) {
  list_string words = list_string_create(0);
  __auto_type cur = "";
  __auto_type i = 0;
  while (i < strlen(s)) {
    __auto_type ch = slice_string(s, i, i + 1);
    if ((strcmp(ch, " ") == 0) || ch == "\n" || ch == "\t") {
      if (strlen(cur) > 0) {
        list_string tmp1 = list_string_create(1);
        tmp1.data[0] = cur;
        list_string tmp2 = concat_list_string(words, tmp1);
        words = tmp2;
        cur = "";
      }
    } else {
      char *tmp3 = concat_string(cur, ch);
      cur = tmp3;
    }
    i = i + 1;
  }
  if (strlen(cur) > 0) {
    list_string tmp4 = list_string_create(1);
    tmp4.data[0] = cur;
    list_string tmp5 = concat_list_string(words, tmp4);
    words = tmp5;
  }
  return words;
}

char *padRight(char *s, int width) {
  __auto_type out = s;
  __auto_type i = strlen(s);
  while (i < width) {
    char *tmp6 = concat_string(out, " ");
    out = tmp6;
    i = i + 1;
  }
  return out;
}

char *join(list_string xs, char *sep) {
  __auto_type res = "";
  __auto_type i = 0;
  while (i < xs.len) {
    if (i > 0) {
      char *tmp7 = concat_string(res, sep);
      res = tmp7;
    }
    char *tmp8 = concat_string(res, xs.data[i]);
    res = tmp8;
    i = i + 1;
  }
  return res;
}

int parseIntStr(char *str) {
  __auto_type i = 0;
  __auto_type neg = 0;
  char *tmp9 = slice_string(str, 0, 1);
  if ((str.len > 0 && tmp9 == "-")) {
    neg = 1;
    i = 1;
  }
  __auto_type n = 0;
  map_string_int tmp10 = map_string_int_create(10);
  map_string_int_put(&tmp10, "0", 0);
  map_string_int_put(&tmp10, "1", 1);
  map_string_int_put(&tmp10, "2", 2);
  map_string_int_put(&tmp10, "3", 3);
  map_string_int_put(&tmp10, "4", 4);
  map_string_int_put(&tmp10, "5", 5);
  map_string_int_put(&tmp10, "6", 6);
  map_string_int_put(&tmp10, "7", 7);
  map_string_int_put(&tmp10, "8", 8);
  map_string_int_put(&tmp10, "9", 9);
  __auto_type digits = tmp10;
  while ((i < str.len)) {
    char *tmp11 = slice_string(str, i, i + 1);
    n = n * 10 + map_string_int_get(digits, tmp11);
    i = i + 1;
  }
  if (neg) {
    n = (-n);
  }
  return n;
}

int isDigits(char *s) {
  if (strlen(s) == 0) {
    return 0;
  }
  __auto_type i = 0;
  while (i < strlen(s)) {
    __auto_type ch = slice_string(s, i, i + 1);
    if ((strcmp(ch, "0") < 0) || ch > "9") {
      return 0;
    }
    i = i + 1;
  }
  return 1;
}

int readTable(char *table) {
  __auto_type toks = fields(table);
  list_string cmds = list_string_create(0);
  list_int mins = list_int_create(0);
  __auto_type i = 0;
  while (i < toks.len) {
    __auto_type cmd = toks.data[i];
    __auto_type minlen = strlen(cmd);
    i = i + 1;
    if (i < toks.len && isDigits(toks.data[i])) {
      __auto_type num = parseIntStr(toks.data[i]);
      if ((num >= 1 && num < strlen(cmd))) {
        minlen = num;
        i = i + 1;
      }
    }
    list_string tmp12 = list_string_create(1);
    tmp12.data[0] = cmd;
    list_string tmp13 = concat_list_string(cmds, tmp12);
    cmds = tmp13;
    int tmp14_data[1];
    list_int tmp14 = {1, tmp14_data};
    tmp14.data[0] = minlen;
    list_int tmp15 = concat_list_int(mins, tmp14);
    mins = tmp15;
  }
  return (tmp_item_t){.commands = cmds, .mins = mins};
}

list_string validate(list_string commands, list_int mins, list_string words) {
  list_string results = list_string_create(0);
  __auto_type wi = 0;
  while (wi < words.len) {
    __auto_type w = words.data[wi];
    __auto_type found = 0;
    __auto_type wlen = strlen(w);
    __auto_type ci = 0;
    while (ci < commands.len) {
      __auto_type cmd = commands.data[ci];
      if (mins.data[ci] != 0 && wlen >= mins.data[ci] && wlen <= strlen(cmd)) {
        __auto_type c = upper(cmd);
        __auto_type ww = upper(w);
        if ((strcmp(slice_string(c, 0, wlen), ww) == 0)) {
          list_string tmp16 = list_string_create(1);
          tmp16.data[0] = c;
          list_string tmp17 = concat_list_string(results, tmp16);
          results = tmp17;
          found = 1;
          break;
        }
      }
      ci = ci + 1;
    }
    if ((!found)) {
      list_string tmp18 = list_string_create(1);
      tmp18.data[0] = "*error*";
      list_string tmp19 = concat_list_string(results, tmp18);
      results = tmp19;
    }
    wi = wi + 1;
  }
  return results;
}

int mochi_main() {
  char *tmp20 = concat_string("", "add 1  alter 3  backup 2  bottom 1  Cappend "
                                  "2  change 1  Schange  Cinsert 2  Clast 3 ");
  char *tmp21 =
      concat_string(tmp20, "compress 4 copy 2 count 3 Coverlay 3 cursor 3  "
                           "delete 3 Cdelete 2  down 1  duplicate ");
  char *tmp22 =
      concat_string(tmp21, "3 xEdit 1 expand 3 extract 3  find 1 Nfind 2 "
                           "Nfindup 6 NfUP 3 Cfind 2 findUP 3 fUP 2 ");
  char *tmp23 =
      concat_string(tmp22, "forward 2  get  help 1 hexType 4  input 1 "
                           "powerInput 3  join 1 split 2 spltJOIN load ");
  char *tmp24 =
      concat_string(tmp23, "locate 1 Clocate 2 lowerCase 3 upperCase 3 Lprefix "
                           "2  macro  merge 2 modify 3 move 2 ");
  char *tmp25 =
      concat_string(tmp24, "msg  next 1 overlay 1 parse preserve 4 purge 3 put "
                           "putD query 1 quit  read recover 3 ");
  char *tmp26 =
      concat_string(tmp25, "refresh renum 3 repeat 3 replace 1 Creplace 2 "
                           "reset 3 restore 4 rgtLEFT right 2 left ");
  char *tmp27 =
      concat_string(tmp26, "2  save  set  shift 2  si  sort  sos  stack 3 "
                           "status 4 top  transfer 3  type 1  up 1 ");
  __auto_type table = tmp27;
  __auto_type sentence =
      "riG   rePEAT copies  put mo   rest    types   fup.    6\npoweRin";
  __auto_type tbl = readTable(table);
  __auto_type commands = (list_string)(tbl.data["commands"]);
  __auto_type mins = (list_int)(tbl.data["mins"]);
  __auto_type words = fields(sentence);
  __auto_type results = validate(commands, mins, words);
  __auto_type out1 = "user words:";
  __auto_type k = 0;
  while (k < words.len) {
    char *tmp28 = concat_string(out1, " ");
    out1 = tmp28;
    if (k < words.len - 1) {
      char *tmp29 =
          concat_string(out1, padRight(words.data[k], strlen(results.data[k])));
      out1 = tmp29;
    } else {
      char *tmp30 = concat_string(out1, words.data[k]);
      out1 = tmp30;
    }
    k = k + 1;
  }
  printf("%s\n", out1);
  char *tmp31 = concat_string("full words: ", join(results, " "));
  printf("%s\n", tmp31);
}

int _mochi_main() {
  mochi_main();
  return 0;
}
int main() { return _mochi_main(); }
