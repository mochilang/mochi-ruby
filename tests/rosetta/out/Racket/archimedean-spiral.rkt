; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:41:40Z
#lang racket
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(define PI 3.141592653589793)
(define (sinApprox x)
  (let/ec return
(define term x)
(define sum x)
(define n 1)
(let/ec break
  (let loop ()
    (when (_le n 10)
      (let/ec continue
(define denom (exact->inexact (if (string? (* (* 2 n) (+ (* 2 n) 1))) (string->number (* (* 2 n) (+ (* 2 n) 1))) (* (* 2 n) (+ (* 2 n) 1)))))
(set! term (/ (* (* (- term) x) x) denom))
(set! sum (+ sum term))
(set! n (+ n 1))
      )
      (loop)))
)
(return sum)
  ))
(define (cosApprox x)
  (let/ec return
(define term 1)
(define sum 1)
(define n 1)
(let/ec break
  (let loop ()
    (when (_le n 10)
      (let/ec continue
(define denom (exact->inexact (if (string? (* (- (* 2 n) 1) (* 2 n))) (string->number (* (- (* 2 n) 1) (* 2 n))) (* (- (* 2 n) 1) (* 2 n)))))
(set! term (/ (* (* (- term) x) x) denom))
(set! sum (+ sum term))
(set! n (+ n 1))
      )
      (loop)))
)
(return sum)
  ))
(define degreesIncr (/ (* 0.1 PI) 180))
(define turns 2)
(define stop (* (* (* 360 turns) 10) degreesIncr))
(define width 600)
(define centre (/ width 2))
(define a 1)
(define b 20)
(define theta 0)
(define count 0)
(let/ec break
  (let loop ()
    (when (_lt theta stop)
      (let/ec continue
(define r (+ a (* b theta)))
(define x (* r (cosApprox theta)))
(define y (* r (sinApprox theta)))
(if (equal? (remainder count 100) 0)
  (begin
(displayln (string-append (string-append (number->string (+ centre x)) ",") (number->string (- centre y))))
  )
  (void)
)
(set! theta (+ theta degreesIncr))
(set! count (+ count 1))
      )
      (loop)))
)
