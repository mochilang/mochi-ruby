; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:41Z
#lang racket
(require racket/list)
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(define (search_user directory username)
  (let/ec return
(return (cond [(string? directory) (string-ref directory username)] [(hash? directory) (hash-ref directory username)] [else (list-ref directory username)]))
  ))
(define (main )
  (let/ec return
(define client (hash "Base" "dc=example,dc=com" "Host" "ldap.example.com" "Port" 389 "GroupFilter" "(memberUid=%s)"))
(define directory (hash "username" '("admins" "users") "john" '("users")))
(define groups (search_user directory "username"))
(if (_gt (cond [(string? groups) (string-length groups)] [(hash? groups) (hash-count groups)] [else (length groups)]) 0)
  (begin
(define out "Groups: [")
(define i 0)
(let/ec break
  (let loop ()
    (when (_lt i (cond [(string? groups) (string-length groups)] [(hash? groups) (hash-count groups)] [else (length groups)]))
      (let/ec continue
(set! out (string-append (string-append (string-append out "\"") (cond [(string? groups) (string-ref groups i)] [(hash? groups) (hash-ref groups i)] [else (list-ref groups i)])) "\""))
(if (_lt i (- (cond [(string? groups) (string-length groups)] [(hash? groups) (hash-count groups)] [else (length groups)]) 1))
  (begin
(set! out (string-append out ", "))
  )
  (void)
)
(set! i (+ i 1))
      )
      (loop)))
)
(set! out (string-append out "]"))
(displayln out)
  )
  (begin
(displayln "User not found")
  )
)
  ))
(main )
