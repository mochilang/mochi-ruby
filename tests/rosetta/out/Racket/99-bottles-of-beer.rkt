; Generated by Mochi compiler v0.10.30 on 1970-01-01T00:00:00Z
#lang racket
(define (bottles n)
  (let/ec return
(if (equal? n 0)
  (begin
    (let ()
(return "No more bottles")
    )
  )
  (void)
)
(if (equal? n 1)
  (begin
    (let ()
(return "1 bottle")
    )
  )
  (void)
)
(return (string-append (number->string n) " bottles"))
  ))
(define (main )
  (let/ec return
(define i 99)
(let/ec break
  (let loop ()
    (when (> i 0)
      (let/ec continue
        (let ()
(displayln (string-append (bottles i) " of beer on the wall"))
(displayln (string-append (bottles i) " of beer"))
(displayln "Take one down, pass it around")
(displayln (string-append (bottles (- i 1)) " of beer on the wall"))
(set! i (- i 1))
        )
      )
      (loop)))
)
  ))
(main )
