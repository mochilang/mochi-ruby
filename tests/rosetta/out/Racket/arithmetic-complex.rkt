; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:37:48Z
#lang racket
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(struct Complex (re im) #:transparent #:mutable)
(define (add a b)
  (let/ec return
(return (Complex (+ (Complex-re a) (Complex-re b)) (+ (Complex-im a) (Complex-im b))))
  ))
(define (mul a b)
  (let/ec return
(return (Complex (- (* (Complex-re a) (Complex-re b)) (* (Complex-im a) (Complex-im b))) (+ (* (Complex-re a) (Complex-im b)) (* (Complex-im a) (Complex-re b)))))
  ))
(define (neg a)
  (let/ec return
(return (Complex (- (Complex-re a)) (- (Complex-im a))))
  ))
(define (inv a)
  (let/ec return
(define denom (+ (* (Complex-re a) (Complex-re a)) (* (Complex-im a) (Complex-im a))))
(return (Complex (/ (Complex-re a) denom) (/ (- (Complex-im a)) denom)))
  ))
(define (conj a)
  (let/ec return
(return (Complex (Complex-re a) (- (Complex-im a))))
  ))
(define (cstr a)
  (let/ec return
(define s (string-append "(" (number->string (Complex-re a))))
(if (_ge (Complex-im a) 0)
  (begin
(set! s (string-append (string-append (string-append s "+") (number->string (Complex-im a))) "i)"))
  )
  (begin
(set! s (string-append (+ s (number->string (Complex-im a))) "i)"))
  )
)
(return s)
  ))
(define a (Complex 1 1))
(define b (Complex 3.14159 1.25))
(displayln (string-append "a:       " (cstr a)))
(displayln (string-append "b:       " (cstr b)))
(displayln (string-append "a + b:   " (cstr (add a b))))
(displayln (string-append "a * b:   " (cstr (mul a b))))
(displayln (string-append "-a:      " (cstr (neg a))))
(displayln (string-append "1 / a:   " (cstr (inv a))))
(displayln (string-append "a̅:       " (cstr (conj a))))
