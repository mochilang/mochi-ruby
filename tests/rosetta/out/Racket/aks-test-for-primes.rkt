; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:46Z
#lang racket
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(define (poly p)
  (let/ec return
(define s "")
(define coef 1)
(define i p)
(if (not (equal? coef 1))
  (begin
(set! s (string-append s (number->string coef)))
  )
  (void)
)
(let/ec break
  (let loop ()
    (when (_gt i 0)
      (let/ec continue
(set! s (string-append s "x"))
(if (not (equal? i 1))
  (begin
(set! s (string-append (string-append s "^") (number->string i)))
  )
  (void)
)
(set! coef (string->number (/ (* coef i) (+ (- p i) 1))))
(define d coef)
(if (equal? (remainder (- p (- i 1)) 2) 1)
  (begin
(set! d (- d))
  )
  (void)
)
(if (_lt d 0)
  (begin
(set! s (string-append (string-append s " - ") (number->string (- d))))
  )
  (begin
(set! s (string-append (string-append s " + ") (number->string d)))
  )
)
(set! i (- i 1))
      )
      (loop)))
)
(if (string=? s "")
  (begin
(set! s "1")
  )
  (void)
)
(return s)
  ))
(define (aks n)
  (let/ec return
(if (_lt n 2)
  (begin
(return #f)
  )
  (void)
)
(define c n)
(define i 1)
(let/ec break
  (let loop ()
    (when (_lt i n)
      (let/ec continue
(if (not (equal? (remainder c n) 0))
  (begin
(return #f)
  )
  (void)
)
(set! c (string->number (/ (* c (- n i)) (+ i 1))))
(set! i (+ i 1))
      )
      (loop)))
)
(return #t)
  ))
(define (main )
  (let/ec return
(define p 0)
(let/ec break
  (let loop ()
    (when (_le p 7)
      (let/ec continue
(displayln (string-append (string-append (number->string p) ":  ") (poly p)))
(set! p (+ p 1))
      )
      (loop)))
)
(define first #t)
(set! p 2)
(define line "")
(let/ec break
  (let loop ()
    (when (_lt p 50)
      (let/ec continue
(if (aks p)
  (begin
(if first
  (begin
(set! line (string-append line (number->string p)))
(set! first #f)
  )
  (begin
(set! line (string-append (string-append line " ") (number->string p)))
  )
)
  )
  (void)
)
(set! p (+ p 1))
      )
      (loop)))
)
(displayln line)
  ))
(main )
