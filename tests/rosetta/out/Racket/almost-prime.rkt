; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:47Z
#lang racket
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(define (kPrime n k)
  (let/ec return
(define nf 0)
(define i 2)
(let/ec break
  (let loop ()
    (when (_le i n)
      (let/ec continue
(let/ec break
  (let loop ()
    (when (equal? (remainder n i) 0)
      (let/ec continue
(if (equal? nf k)
  (begin
(return #f)
  )
  (void)
)
(set! nf (+ nf 1))
(set! n (/ n i))
      )
      (loop)))
)
(set! i (+ i 1))
      )
      (loop)))
)
(return (equal? nf k))
  ))
(define (gen k count)
  (let/ec return
(define r '())
(define n 2)
(let/ec break
  (let loop ()
    (when (_lt (cond [(string? r) (string-length r)] [(hash? r) (hash-count r)] [else (length r)]) count)
      (let/ec continue
(if (kPrime n k)
  (begin
(set! r (append r (list n)))
  )
  (void)
)
(set! n (+ n 1))
      )
      (loop)))
)
(return r)
  ))
(define (main )
  (let/ec return
(define k 1)
(let/ec break
  (let loop ()
    (when (_le k 5)
      (let/ec continue
(displayln (string-append (string-append (number->string k) " ") (number->string (gen k 10))))
(set! k (+ k 1))
      )
      (loop)))
)
  ))
(main )
