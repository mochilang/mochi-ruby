; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:49Z
#lang racket
(require racket/list)
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(define (pfacSum i)
  (let/ec return
(define sum 0)
(define p 1)
(let/ec break
  (let loop ()
    (when (_le p (/ i 2))
      (let/ec continue
(if (equal? (remainder i p) 0)
  (begin
(set! sum (+ sum p))
  )
  (void)
)
(set! p (+ p 1))
      )
      (loop)))
)
(return sum)
  ))
(define (pad n width)
  (let/ec return
(define s (number->string n))
(let/ec break
  (let loop ()
    (when (_lt (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) width)
      (let/ec continue
(set! s (string-append " " s))
      )
      (loop)))
)
(return s)
  ))
(define (main )
  (let/ec return
(define sums '())
(define i 0)
(let/ec break
  (let loop ()
    (when (_lt i 20000)
      (let/ec continue
(set! sums (append sums (list 0)))
(set! i (+ i 1))
      )
      (loop)))
)
(set! i 1)
(let/ec break
  (let loop ()
    (when (_lt i 20000)
      (let/ec continue
(set! sums (cond [(hash? sums) (hash-set sums i (pfacSum i))] [else (list-set sums i (pfacSum i))]))
(set! i (+ i 1))
      )
      (loop)))
)
(displayln "The amicable pairs below 20,000 are:")
(define n 2)
(let/ec break
  (let loop ()
    (when (_lt n 19999)
      (let/ec continue
(define m (cond [(string? sums) (string-ref sums n)] [(hash? sums) (hash-ref sums n)] [else (list-ref sums n)]))
(if (and (and (_gt m n) (_lt m 20000)) (equal? n (cond [(string? sums) (string-ref sums m)] [(hash? sums) (hash-ref sums m)] [else (list-ref sums m)])))
  (begin
(displayln (string-append (string-append (string-append "  " (pad n 5)) " and ") (pad m 5)))
  )
  (void)
)
(set! n (+ n 1))
      )
      (loop)))
)
  ))
(main )
