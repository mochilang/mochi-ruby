; Generated by Mochi compiler v0.10.26 on 2025-07-16T11:36:37Z
#lang racket
(define (_date_number s)
  (let ([parts (string-split s "-")])
    (if (= (length parts) 3)
        (+ (* (string->number (list-ref parts 0)) 10000)
           (* (string->number (list-ref parts 1)) 100)
           (string->number (list-ref parts 2)))
        #f)))

(define (_to_string v) (format "~a" v))

(define (_lt a b)
  (cond
    [(and (number? a) (number? b)) (< a b)]
    [(and (string? a) (string? b))
     (let ([da (_date_number a)]
           [db (_date_number b)])
       (if (and da db)
           (< da db)
           (string<? a b)))]
    [(and (list? a) (list? b))
     (cond [(null? a) (not (null? b))]
           [(null? b) #f]
           [else (let ([ka (car a)] [kb (car b)])
                   (if (equal? ka kb)
                       (_lt (cdr a) (cdr b))
                       (_lt ka kb)))])]
    [else (string<? (_to_string a) (_to_string b))]))

(define (_gt a b) (_lt b a))
(define (_le a b) (or (_lt a b) (equal? a b)))
(define (_ge a b) (or (_gt a b) (equal? a b)))

(define (_min v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_lt n m) (set! m n))))
    m))

(define (_max v)
  (let* ([lst (cond [(and (hash? v) (hash-has-key? v 'items)) (hash-ref v 'items)]
                    [(list? v) v]
                    [else '()])]
         [m 0])
    (when (not (null? lst))
      (set! m (car lst))
      (for ([n (cdr lst)])
        (when (_gt n m) (set! m n))))
    m))

(define (_json-fix v)
  (cond
    [(and (number? v) (rational? v) (not (integer? v))) (real->double-flonum v)]
    [(list? v) (map _json-fix v)]
    [(hash? v) (for/hash ([(k val) v]) (values k (_json-fix val)))]
    [else v]))

(define (pow base exp)
  (let/ec return
(define result 1)
(define i 0)
(let/ec break
  (let loop ()
    (when (_lt i exp)
      (let/ec continue
(set! result (* result base))
(set! i (+ i 1))
      )
      (loop)))
)
(return result)
  ))
(define (ackermann2 m n)
  (let/ec return
(if (equal? m 0)
  (begin
(return (+ n 1))
  )
  (void)
)
(if (equal? m 1)
  (begin
(return (+ n 2))
  )
  (void)
)
(if (equal? m 2)
  (begin
(return (+ (* 2 n) 3))
  )
  (void)
)
(if (equal? m 3)
  (begin
(return (- (* 8 (pow 2 n)) 3))
  )
  (void)
)
(if (equal? n 0)
  (begin
(return (ackermann2 (- m 1) 1))
  )
  (void)
)
(return (ackermann2 (- m 1) (ackermann2 m (- n 1))))
  ))
(define (main )
  (let/ec return
(displayln (string-append "A(0, 0) = " (number->string (ackermann2 0 0))))
(displayln (string-append "A(1, 2) = " (number->string (ackermann2 1 2))))
(displayln (string-append "A(2, 4) = " (number->string (ackermann2 2 4))))
(displayln (string-append "A(3, 4) = " (number->string (ackermann2 3 4))))
  ))
(main )
