// Generated by Mochi compiler v0.10.30 on 2006-01-02T15:04:05Z
// animation.mochi
import java.util.*;

public class Animation {
    public static void main(String[] args) {
        String msg = "Hello World! ";
        int shift = 0;
        int inc = 1;
        int clicks = 0;
        int frames = 0;
        while (clicks < 5) {
            String line = "";
            int i = 0;
            while (i < msg.length()) {
                int idx = (shift + i) % msg.length();
                line = line + msg.substring(idx, idx + 1);
                i = (int)(i + 1);
            }
            System.out.println(line);
            shift = (int)((shift + inc) % msg.length());
            frames = (int)(frames + 1);
            if (Objects.equals(frames % msg.length(), 0)) {
                inc = (int)(msg.length() - inc);
                clicks = (int)(clicks + 1);
            }
        }
    }
}
