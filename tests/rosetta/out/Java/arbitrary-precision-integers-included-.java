// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
// arbitrary-precision-integers-included-.mochi
import java.util.*;

public class ArbitraryPrecisionIntegersIncluded {
    static Object base = 5;
    static int pow_int(int base, int exp) {
        int result = 1;
        int b = base;
        int e = exp;
        while (e > 0) {
            if (Objects.equals(e % 2, 1)) {
                result = (int)(result * b);
            }
            b = (int)(b * b);
            e = (int)(Integer.parseInt((e / 2)));
        }
        return result;
    }
    static Object pow_big(Object base, int exp) {
        Object result = 1;
        Object b = base;
        int e = exp;
        while (e > 0) {
            if (Objects.equals(e % 2, 1)) {
                result = result * b;
            }
            b = b * b;
            e = (int)(Integer.parseInt((e / 2)));
        }
        return result;
    }
    public static void main(String[] args) {
        int e1 = pow_int(3, 2);
        int e2 = pow_int(4, e1);
        Object x = pow_big(base, e2);
        String s = String.valueOf(x);
        System.out.println("5^(4^(3^2)) has" + " " + s.length() + " " + "digits:" + " " + s.substring(0, 20) + " " + "..." + " " + s.substring(s.length() - 20, s.length()));
    }
}
