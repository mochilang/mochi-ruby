# Generated by Mochi compiler v0.10.26 on 2025-07-16T12:46:08Z
defmodule Main do
  @target 269_696
  @modulus 1_000_000
  def main do
    # n :: integer()
    n = 1
    _ = n

    t1 = fn t1, n ->
      try do
        if true do
          square = n * n
          ending = rem(square, @modulus)

          if ending == @target do
            IO.puts(
              (("The smallest number whose square ends with " <> to_string(@target)) <> " is ") <>
                to_string(n)
            )

            throw(:break)
          end

          n = n + 1
          t1.(t1, n)
        else
          {:ok, n}
        end
      catch
        :break ->
          {:ok, n}
      end
    end

    {_, n} = t1.(t1, n)
    _ = n
  end
end

Main.main()
