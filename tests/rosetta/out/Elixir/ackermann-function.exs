# Generated by Mochi compiler v0.10.26 on 2025-07-16T12:49:42Z
defmodule Main do
  @spec ackermann(integer(), integer()) :: integer()
  def ackermann(m, n) do
    try do
      if m == 0 do
        throw({:return, n + 1})
      end

      if n == 0 do
        throw({:return, ackermann(m - 1, 1)})
      end

      throw({:return, ackermann(m - 1, ackermann(m, n - 1))})
    catch
      {:return, v} -> v
    end
  end

  @spec main() :: nil
  def main() do
    try do
      IO.puts("A(0, 0) = " <> to_string(ackermann(0, 0)))
      IO.puts("A(1, 2) = " <> to_string(ackermann(1, 2)))
      IO.puts("A(2, 4) = " <> to_string(ackermann(2, 4)))
      IO.puts("A(3, 4) = " <> to_string(ackermann(3, 4)))
    catch
      {:return, v} -> v
    end
  end

  def main do
    main()
  end
end

Main.main()
