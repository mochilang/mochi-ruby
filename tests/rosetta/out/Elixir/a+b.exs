# Generated by Mochi compiler v0.10.30 on 2025-07-19T01:02:31Z
defmodule Main do
  @spec main() :: nil
  def main() do
    try do
      a = _int(_input())
      b = _int(_input())
      IO.inspect(a + b)
    catch
      {:return, v} -> v
    end
  end

  def main do
    main()
  end

  defp _input() do
    case IO.gets("") do
      :eof -> ""
      other -> String.trim(other)
    end
  end

  defp _int(v) do
    cond do
      is_integer(v) ->
        v

      is_float(v) ->
        trunc(v)

      is_binary(v) ->
        case Integer.parse(String.trim(v)) do
          {i, ""} -> i
          _ -> raise "invalid int"
        end

      true ->
        raise "invalid int"
    end
  end
end

Main.main()
