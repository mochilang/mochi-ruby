# Generated by Mochi compiler v0.10.26 on 2025-07-16T12:44:38Z
defmodule Main do
  @spec angleDiff(float(), float()) :: float()
  def angleDiff(b1, b2) do
    try do
      d = b2 - b1

      if d < 0 - 180 do
        throw({:return, d + 360})
      end

      if d > 180 do
        throw({:return, d - 360})
      end

      throw({:return, d})
    catch
      {:return, v} -> v
    end
  end

  def main do
    # testCases :: list(list(float()))
    testCases = [
      [20, 45],
      [0 - 45, 45],
      [0 - 85, 90],
      [0 - 95, 90],
      [0 - 45, 125],
      [0 - 45, 145],
      [29.4803, 0 - 88.6381],
      [0 - 78.3251, 0 - 159.036]
    ]

    _ = testCases

    for tc <- testCases do
      IO.inspect(angleDiff(Enum.at(tc, 0), Enum.at(tc, 1)))
    end
  end
end

Main.main()
