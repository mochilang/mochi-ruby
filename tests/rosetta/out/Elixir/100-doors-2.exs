# Generated by Mochi compiler v0.10.30 on 2025-07-19T01:03:33Z
defmodule Main do
  def main do
    # door :: integer()
    door = 1
    _ = door
    # incrementer :: integer()
    incrementer = 0
    _ = incrementer

    {door, incrementer} =
      Enum.reduce(1..(101 - 1), {door, incrementer}, fn current, {door, incrementer} ->
        # line :: String.t()
        line = ("Door " <> to_string(current)) <> " "
        _ = line

        if current == door do
          line = line <> "Open"
          incrementer = incrementer + 1
          door = door + 2 * incrementer + 1
        else
          line = line <> "Closed"
        end

        IO.puts(line)
        {door, incrementer}
      end)

    _ = door
    _ = incrementer
  end
end

Main.main()
