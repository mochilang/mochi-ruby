# Generated by Mochi compiler v0.10.30 on 2025-07-19T01:02:29Z
defmodule Main do
  @spec bottles(integer()) :: String.t()
  def bottles(n) do
    try do
      if n == 0 do
        throw({:return, "No more bottles"})
      end

      if n == 1 do
        throw({:return, "1 bottle"})
      end

      throw({:return, to_string(n) <> " bottles"})
    catch
      {:return, v} -> v
    end
  end

  @spec main() :: nil
  def main() do
    try do
      # i :: integer()
      i = 99
      _ = i

      t1 = fn t1, i ->
        try do
          if i > 0 do
            IO.puts(bottles(i) <> " of beer on the wall")
            IO.puts(bottles(i) <> " of beer")
            IO.puts("Take one down, pass it around")
            IO.puts(bottles(i - 1) <> " of beer on the wall")
            i = i - 1
            t1.(t1, i)
          else
            {:ok, i}
          end
        catch
          :break ->
            {:ok, i}
        end
      end

      {_, i} = t1.(t1, i)
      _ = i
    catch
      {:return, v} -> v
    end
  end

  def main do
    main()
  end
end

Main.main()
