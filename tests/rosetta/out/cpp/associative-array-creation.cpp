// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:08Z
#include <iostream>
#include <string>
#include <unordered_map>

auto removeKey(auto m, std::string k) {
  auto out = std::unordered_map<int, int>{};
  for (auto key : m) {
    if ((key != k)) {
      out[key] = m[key];
    }
  }
  return out;
}

auto main() {
  auto x = nullptr;
  x = std::unordered_map<int, int>{};
  x[std::string("foo")] = 3;
  auto y1 = x[std::string("bar")];
  auto ok = (x.count(std::string("bar")) > 0);
  std::cout << y1 << std::endl;
  std::cout << ok << std::endl;
  x = removeKey(x, std::string("foo"));
  x = std::unordered_map<std::string, decltype(2)>{{std::string("foo"), 2},
                                                   {std::string("bar"), 42},
                                                   {std::string("baz"), -1}};
  {
    std::cout << x[std::string("foo")];
    std::cout << ' ';
    std::cout << x[std::string("bar")];
    std::cout << ' ';
    std::cout << x[std::string("baz")];
    std::cout << std::endl;
  }
}

int main() {
  main();
  return 0;
}
