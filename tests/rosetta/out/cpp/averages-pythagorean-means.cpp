// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:08Z
#include <iostream>
#include <string>

float powf(float base, int exp) {
  auto result = 1;
  auto i = 0;
  while ((i < exp)) {
    result = (result * base);
    i = (i + 1);
  }
  return result;
}

float nthRoot(float x, int n) {
  auto low = 0;
  auto high = x;
  auto i = 0;
  while ((i < 60)) {
    auto mid = (((low + high)) / 2);
    if ((powf(mid, n) > x)) {
      high = mid;
    } else {
      low = mid;
    }
    i = (i + 1);
  }
  return low;
}

auto main() {
  auto sum = 0;
  auto sumRecip = 0;
  auto prod = 1;
  auto n = 1;
  while ((n <= 10)) {
    auto f = n;
    sum = (sum + f);
    sumRecip = (sumRecip + (1 / f));
    prod = (prod * f);
    n = (n + 1);
  }
  auto count = 10;
  auto a = (sum / count);
  auto g = nthRoot(prod, 10);
  auto h = (count / sumRecip);
  std::cout << (((((std::string("A: ") + std::to_string(a)) +
                   std::string(" G: ")) +
                  std::to_string(g)) +
                 std::string(" H: ")) +
                std::to_string(h))
            << std::endl;
  std::cout << (std::string("A >= G >= H: ") +
                std::to_string(((a >= g) && (g >= h))))
            << std::endl;
}

int main() {
  main();
  return 0;
}
