// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:14Z
#include <iostream>
#include <string>

int main() {
  auto nMech = 5;
  auto detailsPerMech = 4;
  for (int mech = 1; mech < ((nMech + 1)); mech++) {
    auto id = mech;
    std::cout << ((((std::string("worker ") + std::to_string(id)) +
                    std::string(" contracted to assemble ")) +
                   std::to_string(detailsPerMech)) +
                  std::string(" details"))
              << std::endl;
    std::cout << ((std::string("worker ") + std::to_string(id)) +
                  std::string(" enters shop"))
              << std::endl;
    auto d = 0;
    while ((d < detailsPerMech)) {
      std::cout << ((std::string("worker ") + std::to_string(id)) +
                    std::string(" assembling"))
                << std::endl;
      std::cout << ((std::string("worker ") + std::to_string(id)) +
                    std::string(" completed detail"))
                << std::endl;
      d = (d + 1);
    }
    std::cout << ((std::string("worker ") + std::to_string(id)) +
                  std::string(" leaves shop"))
              << std::endl;
    std::cout << ((std::string("mechanism ") + std::to_string(mech)) +
                  std::string(" completed"))
              << std::endl;
  }
  return 0;
}
