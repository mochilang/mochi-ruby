// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:05Z
#include <iostream>
#include <string>
#include <vector>

bool kPrime(int n, int k) {
  auto nf = 0;
  auto i = 2;
  while ((i <= n)) {
    while (((n % i) == 0)) {
      if ((nf == k)) {
        return false;
      }
      nf = (nf + 1);
      n = (n / i);
    }
    i = (i + 1);
  }
  return (nf == k);
}

auto gen(int k, int count) {
  auto r = std::vector<int>{};
  auto n = 2;
  while ((r.size() < count)) {
    if (kPrime(n, k)) {
      r.push_back(n);
    }
    n = (n + 1);
  }
  return r;
}

auto main() {
  auto k = 1;
  while ((k <= 5)) {
    std::cout << ((std::to_string(k) + std::string(" ")) +
                  std::to_string(gen(k, 10)))
              << std::endl;
    k = (k + 1);
  }
}

int main() {
  main();
  return 0;
}
