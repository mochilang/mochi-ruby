// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:06Z
#include <iostream>

auto PI = 3.141592653589793;

float sinApprox(float x) {
  auto term = x;
  auto sum = x;
  auto n = 1;
  while ((n <= 10)) {
    auto denom = ((((2 * n)) * (((2 * n) + 1))));
    term = ((((-term) * x) * x) / denom);
    sum = (sum + term);
    n = (n + 1);
  }
  return sum;
}

float cosApprox(float x) {
  auto term = 1;
  auto sum = 1;
  auto n = 1;
  while ((n <= 10)) {
    auto denom = (((((2 * n) - 1)) * ((2 * n))));
    term = ((((-term) * x) * x) / denom);
    sum = (sum + term);
    n = (n + 1);
  }
  return sum;
}

float sqrtApprox(float x) {
  auto guess = x;
  auto i = 0;
  while ((i < 10)) {
    guess = (((guess + (x / guess))) / 2);
    i = (i + 1);
  }
  return guess;
}

int main() {
  auto L = 10;
  auto G = 9.81;
  auto dt = 0.2;
  auto phi0 = (PI / 4);
  auto omega = sqrtApprox((G / L));
  auto t = 0;
  for (int step = 0; step < 10; step++) {
    auto phi = (phi0 * cosApprox((omega * t)));
    auto pos = std::stoi((((10 * sinApprox(phi)) + 0.5)));
    std::cout << std::to_string(pos) << std::endl;
    t = (t + dt);
  }
  return 0;
}
