// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:08Z
#include <iostream>
#include <vector>

auto sortFloat(auto xs) {
  auto arr = xs;
  auto n = arr.size();
  auto i = 0;
  while ((i < n)) {
    auto j = 0;
    while ((j < (n - 1))) {
      if ((arr[j] > arr[(j + 1)])) {
        auto tmp = arr[j];
        arr[j] = arr[(j + 1)];
        arr[(j + 1)] = tmp;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return arr;
}

float median(auto a) {
  auto arr = sortFloat(a);
  auto half = std::stoi(((arr.size() / 2)));
  auto m = arr[half];
  if (((arr.size() % 2) == 0)) {
    m = (((m + arr[(half - 1)])) / 2);
  }
  return m;
}

int main() {
  std::cout << std::to_string(median(std::vector<int>{3, 1, 4, 1}))
            << std::endl;
  std::cout << std::to_string(median(std::vector<int>{3, 1, 4, 1, 5}))
            << std::endl;
  return 0;
}
