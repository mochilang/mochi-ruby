// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:07Z
#include <iostream>

float abs(float x) {
  if ((x < 0)) {
    return (-x);
  }
  return x;
}

float sqrtApprox(float x) {
  auto guess = x;
  auto i = 0;
  while ((i < 20)) {
    guess = (((guess + (x / guess))) / 2);
    i = (i + 1);
  }
  return guess;
}

float agm(float a, float g) {
  auto eps = 1e-14;
  while ((abs((a - g)) > (abs(a) * eps))) {
    auto newA = (((a + g)) / 2);
    auto newG = sqrtApprox((a * g));
    a = newA;
    g = newG;
  }
  return a;
}

auto main() {
  std::cout << std::to_string(agm(1, (1 / sqrtApprox(2)))) << std::endl;
}

int main() {
  main();
  return 0;
}
