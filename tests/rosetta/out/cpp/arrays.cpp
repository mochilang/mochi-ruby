// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:08Z
#include <iostream>
#include <string>
#include <vector>

std::string listStr(auto xs) {
  auto s = std::string("[");
  auto i = 0;
  while ((i < xs.size())) {
    s = (s + std::to_string(xs[i]));
    if (((i + 1) < xs.size())) {
      s = (s + std::string(" "));
    }
    i = (i + 1);
  }
  s = (s + std::string("]"));
  return s;
}

int main() {
  std::vector<int> a = {0, 0, 0, 0, 0};
  std::cout << (std::string("len(a) = ") + std::to_string(a.size()))
            << std::endl;
  std::cout << (std::string("a = ") + listStr(a)) << std::endl;
  a[0] = 3;
  std::cout << (std::string("a = ") + listStr(a)) << std::endl;
  std::cout << (std::string("a[0] = ") + std::to_string(a[0])) << std::endl;
  auto s = ([&](auto v) {
    return std::vector<decltype(a[0])>(v.begin() + 0, v.begin() + 4);
  })(a);
  auto cap_s = 5;
  std::cout << (std::string("s = ") + listStr(s)) << std::endl;
  std::cout << (((std::string("len(s) = ") + std::to_string(s.size())) +
                 std::string("  cap(s) = ")) +
                std::to_string(cap_s))
            << std::endl;
  s = ([&](auto v) {
    return std::vector<decltype(a[0])>(v.begin() + 0, v.begin() + 5);
  })(a);
  std::cout << (std::string("s = ") + listStr(s)) << std::endl;
  a[0] = 22;
  s[0] = 22;
  std::cout << (std::string("a = ") + listStr(a)) << std::endl;
  std::cout << (std::string("s = ") + listStr(s)) << std::endl;
  s.push_back(4);
  s.push_back(5);
  s.push_back(6);
  cap_s = 10;
  std::cout << (std::string("s = ") + listStr(s)) << std::endl;
  std::cout << (((std::string("len(s) = ") + std::to_string(s.size())) +
                 std::string("  cap(s) = ")) +
                std::to_string(cap_s))
            << std::endl;
  a[4] = -1;
  std::cout << (std::string("a = ") + listStr(a)) << std::endl;
  std::cout << (std::string("s = ") + listStr(s)) << std::endl;
  s = std::vector<int>{};
  for (int i = 0; i < 8; i++) {
    s.push_back(0);
  }
  cap_s = 8;
  std::cout << (std::string("s = ") + listStr(s)) << std::endl;
  std::cout << (((std::string("len(s) = ") + std::to_string(s.size())) +
                 std::string("  cap(s) = ")) +
                std::to_string(cap_s))
            << std::endl;
  return 0;
}
