// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:07Z
#include <iostream>
#include <string>
#include <vector>

int intSqrt(int x) {
  if ((x < 2)) {
    return x;
  }
  auto left = 1;
  auto right = (x / 2);
  auto ans = 0;
  while ((left <= right)) {
    auto mid = (left + (((right - left)) / 2));
    auto sq = (mid * mid);
    if ((sq == x)) {
      return mid;
    }
    if ((sq < x)) {
      left = (mid + 1);
      ans = mid;
    } else {
      right = (mid - 1);
    }
  }
  return ans;
}

int sumRecip(int n) {
  auto s = 1;
  auto limit = intSqrt(n);
  auto f = 2;
  while ((f <= limit)) {
    if (((n % f) == 0)) {
      s = (s + (n / f));
      auto f2 = (n / f);
      if ((f2 != f)) {
        s = (s + f);
      }
    }
    f = (f + 1);
  }
  return s;
}

auto main() {
  std::vector<int> nums = {6, 28, 120, 496, 672, 8128, 30240, 32760, 523776};
  for (auto n : nums) {
    auto s = sumRecip(n);
    if (((s % n) == 0)) {
      auto val = (s / n);
      auto perfect = std::string("");
      if ((val == 1)) {
        perfect = std::string("perfect!");
      }
      std::cout << (((((std::string("Sum of recipr. factors of ") +
                        std::to_string(n)) +
                       std::string(" = ")) +
                      std::to_string(val)) +
                     std::string(" exactly ")) +
                    perfect)
                << std::endl;
    }
  }
}

int main() {
  main();
  return 0;
}
