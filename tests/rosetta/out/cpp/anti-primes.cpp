// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:07Z
#include <iostream>
#include <string>

int countDivisors(int n) {
  if ((n < 2)) {
    return 1;
  }
  auto count = 2;
  auto i = 2;
  while ((i <= (n / 2))) {
    if (((n % i) == 0)) {
      count = (count + 1);
    }
    i = (i + 1);
  }
  return count;
}

auto main() {
  std::cout << std::string("The first 20 anti-primes are:") << std::endl;
  auto maxDiv = 0;
  auto count = 0;
  auto n = 1;
  auto line = std::string("");
  while ((count < 20)) {
    auto d = countDivisors(n);
    if ((d > maxDiv)) {
      line = ((line + std::to_string(n)) + std::string(" "));
      maxDiv = d;
      count = (count + 1);
    }
    n = (n + 1);
  }
  line = std::string(line).substr(0, ((line.size() - 1)) - (0));
  std::cout << line << std::endl;
}

int main() {
  main();
  return 0;
}
