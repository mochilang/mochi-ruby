// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:10Z
#include <iostream>
#include <string>

bool parseBool(std::string s) {
  auto l = lower(s);
  if ((((((l == std::string("1")) || (l == std::string("t"))) || (l == true)) ||
        (l == std::string("yes"))) ||
       (l == std::string("y")))) {
    return true;
  }
  return false;
}

auto main() {
  auto n = true;
  std::cout << (n ? "true" : "false") << std::endl;
  std::cout << std::string("bool") << std::endl;
  n = (!n);
  std::cout << (n ? "true" : "false") << std::endl;
  auto x = 5;
  auto y = 8;
  {
    std::cout << std::string("x == y:");
    std::cout << ' ';
    std::cout << ((x == y) ? "true" : "false");
    std::cout << std::endl;
  }
  {
    std::cout << std::string("x < y:");
    std::cout << ' ';
    std::cout << ((x < y) ? "true" : "false");
    std::cout << std::endl;
  }
  std::cout << std::string("\nConvert String into Boolean Data type\n")
            << std::endl;
  auto str1 = std::string("japan");
  {
    std::cout << std::string("Before :");
    std::cout << ' ';
    std::cout << std::string("string");
    std::cout << std::endl;
  }
  auto bolStr = parseBool(str1);
  {
    std::cout << std::string("After :");
    std::cout << ' ';
    std::cout << std::string("bool");
    std::cout << std::endl;
  }
}

int main() {
  main();
  return 0;
}
