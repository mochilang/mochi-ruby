// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:08Z
#include <iostream>
#include <vector>

float sel(auto list, int k) {
  auto i = 0;
  while ((i <= k)) {
    auto minIndex = i;
    auto j = (i + 1);
    while ((j < list.size())) {
      if ((list[j] < list[minIndex])) {
        minIndex = j;
      }
      j = (j + 1);
    }
    auto tmp = list[i];
    list[i] = list[minIndex];
    list[minIndex] = tmp;
    i = (i + 1);
  }
  return list[k];
}

float median(auto a) {
  auto arr = a;
  auto half = std::stoi(((arr.size() / 2)));
  auto med = sel(arr, half);
  if (((arr.size() % 2) == 0)) {
    return (((med + arr[(half - 1)])) / 2);
  }
  return med;
}

int main() {
  std::cout << std::to_string(median(std::vector<int>{3, 1, 4, 1}))
            << std::endl;
  std::cout << std::to_string(median(std::vector<int>{3, 1, 4, 1, 5}))
            << std::endl;
  return 0;
}
