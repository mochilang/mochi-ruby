// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:06Z
#include <iostream>
#include <string>

int main() {
  auto msg = std::string("Hello World! ");
  auto shift = 0;
  auto inc = 1;
  auto clicks = 0;
  auto frames = 0;
  while ((clicks < 5)) {
    auto line = std::string("");
    auto i = 0;
    while ((i < msg.size())) {
      auto idx = (((shift + i)) % msg.size());
      line = (line + std::string(msg).substr(idx, ((idx + 1)) - (idx)));
      i = (i + 1);
    }
    std::cout << line << std::endl;
    shift = (((shift + inc)) % msg.size());
    frames = (frames + 1);
    if (((frames % msg.size()) == 0)) {
      inc = (msg.size() - inc);
      clicks = (clicks + 1);
    }
  }
  return 0;
}
