// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:05Z
#include <iostream>
#include <string>

std::string poly(int p) {
  std::string s = std::string("");
  int coef = 1;
  auto i = p;
  if ((coef != 1)) {
    s = (s + std::to_string(coef));
  }
  while ((i > 0)) {
    s = (s + std::string("x"));
    if ((i != 1)) {
      s = ((s + std::string("^")) + std::to_string(i));
    }
    coef = std::stoi((((coef * i) / (((p - i) + 1)))));
    auto d = coef;
    if (((((p - ((i - 1)))) % 2) == 1)) {
      d = (-d);
    }
    if ((d < 0)) {
      s = ((s + std::string(" - ")) + std::to_string((-d)));
    } else {
      s = ((s + std::string(" + ")) + std::to_string(d));
    }
    i = (i - 1);
  }
  if ((s == std::string(""))) {
    s = std::string("1");
  }
  return s;
}

bool aks(int n) {
  if ((n < 2)) {
    return false;
  }
  int c = n;
  auto i = 1;
  while ((i < n)) {
    if (((c % n) != 0)) {
      return false;
    }
    c = std::stoi((((c * ((n - i))) / ((i + 1)))));
    i = (i + 1);
  }
  return true;
}

auto main() {
  auto p = 0;
  while ((p <= 7)) {
    std::cout << ((std::to_string(p) + std::string(":  ")) + poly(p))
              << std::endl;
    p = (p + 1);
  }
  auto first = true;
  p = 2;
  std::string line = std::string("");
  while ((p < 50)) {
    if (aks(p)) {
      if (first) {
        line = (line + std::to_string(p));
        first = false;
      } else {
        line = ((line + std::string(" ")) + std::to_string(p));
      }
    }
    p = (p + 1);
  }
  std::cout << line << std::endl;
}

int main() {
  main();
  return 0;
}
