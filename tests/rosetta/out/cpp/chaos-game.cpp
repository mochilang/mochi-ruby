// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:13Z
#include <iostream>
#include <string>
#include <vector>

auto width = 60;
auto height = std::stoi(((width * 0.86602540378)));
auto iterations = 5000;
auto grid = std::vector<int>{};
auto y = 0;

auto randInt(int s, int n) {
  auto next = ((((s * 1664525) + 1013904223)) % 2147483647);
  return std::vector<decltype(next)>{next, (next % n)};
}

int main() {
  while ((y < height)) {
    auto line = std::vector<int>{};
    auto x = 0;
    while ((x < width)) {
      line.push_back(std::string(" "));
      x = (x + 1);
    }
    grid.push_back(line);
    y = (y + 1);
  }
  auto seed = 1;
  auto vertices = std::vector<decltype(std::vector<int>{0, (height - 1)})>{
      std::vector<int>{0, (height - 1)},
      std::vector<decltype((width - 1))>{(width - 1), (height - 1)},
      std::vector<decltype(std::stoi(((width / 2))))>{std::stoi(((width / 2))),
                                                      0}};
  auto px = std::stoi(((width / 2)));
  auto py = std::stoi(((height / 2)));
  auto i = 0;
  while ((i < iterations)) {
    auto r = randInt(seed, 3);
    seed = r[0];
    auto idx = std::stoi(r[1]);
    auto v = vertices[idx];
    px = std::stoi(((((px + v[0])) / 2)));
    py = std::stoi(((((py + v[1])) / 2)));
    if (((((px >= 0) && (px < width)) && (py >= 0)) && (py < height))) {
      grid[py][px] = std::string("*");
    }
    i = (i + 1);
  }
  y = 0;
  while ((y < height)) {
    auto line = std::string("");
    auto x = 0;
    while ((x < width)) {
      line = (line + grid[y][x]);
      x = (x + 1);
    }
    std::cout << line << std::endl;
    y = (y + 1);
  }
  return 0;
}
