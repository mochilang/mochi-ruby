// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:11Z
#include <iostream>
#include <string>

auto epsilon = 1e-15;

float absf(float x) {
  if ((x < 0)) {
    return (-x);
  }
  return x;
}

float pow10(int n) {
  float r = 1;
  auto i = 0;
  while ((i < n)) {
    r = (r * 10);
    i = (i + 1);
  }
  return r;
}

std::string formatFloat(float f, int prec) {
  auto scale = pow10(prec);
  auto scaled = (((f * scale)) + 0.5);
  auto n = (std::stoi(scaled));
  auto digits = std::to_string(n);
  while ((digits.size() <= prec)) {
    digits = (std::string("0") + digits);
  }
  auto intPart = std::string(digits).substr(0, ((digits.size() - prec)) - (0));
  auto fracPart = std::string(digits).substr(
      (digits.size() - prec), (digits.size()) - ((digits.size() - prec)));
  return ((intPart + std::string(".")) + fracPart);
}

int main() {
  auto factval = 1;
  float e = 2;
  auto n = 2;
  float term = 1;
  while (true) {
    factval = (factval * n);
    n = (n + 1);
    term = (1 / (factval));
    e = (e + term);
    if ((absf(term) < epsilon)) {
      break;
    }
  }
  std::cout << (std::string("e = ") + formatFloat(e, 15)) << std::endl;
  return 0;
}
