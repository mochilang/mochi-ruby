// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:12Z
#include <iostream>
#include <string>
#include <vector>

auto cart2(auto a, auto b) {
  auto p = std::vector<int>{};
  for (auto x : a) {
    for (auto y : b) {
      p.push_back(std::vector<decltype(x)>{x, y});
    }
  }
  return p;
}

std::string llStr(auto lst) {
  auto s = std::string("[");
  auto i = 0;
  while ((i < lst.size())) {
    auto row = lst[i];
    s = (s + std::string("["));
    auto j = 0;
    while ((j < row.size())) {
      s = (s + std::to_string(row[j]));
      if ((j < (row.size() - 1))) {
        s = (s + std::string(" "));
      }
      j = (j + 1);
    }
    s = (s + std::string("]"));
    if ((i < (lst.size() - 1))) {
      s = (s + std::string(" "));
    }
    i = (i + 1);
  }
  s = (s + std::string("]"));
  return s;
}

auto main() {
  std::cout << llStr(cart2(std::vector<int>{1, 2}, std::vector<int>{3, 4}))
            << std::endl;
  std::cout << llStr(cart2(std::vector<int>{3, 4}, std::vector<int>{1, 2}))
            << std::endl;
  std::cout << llStr(cart2(std::vector<int>{1, 2}, std::vector<int>{}))
            << std::endl;
  std::cout << llStr(cart2(std::vector<int>{}, std::vector<int>{1, 2}))
            << std::endl;
}

int main() {
  main();
  return 0;
}
