// Generated by Mochi compiler v0.10.26 on 2025-07-16T09:21:07Z
#include <iostream>
#include <string>
#include <vector>

auto concatInts(auto a, auto b) {
  auto out = std::vector<int>{};
  for (auto v : a) {
    out.push_back(v);
  }
  for (auto v : b) {
    out.push_back(v);
  }
  return out;
}

auto concatAny(auto a, auto b) {
  auto out = std::vector<int>{};
  for (auto v : a) {
    out.push_back(v);
  }
  for (auto v : b) {
    out.push_back(v);
  }
  return out;
}

int main() {
  std::vector<int> a = {1, 2, 3};
  std::vector<int> b = {7, 12, 60};
  std::cout << std::to_string(concatInts(a, b)) << std::endl;
  auto i = std::vector<int>{1, 2, 3};
  auto j =
      std::vector<std::string>{std::string("Crosby"), std::string("Stills"),
                               std::string("Nash"), std::string("Young")};
  std::cout << std::to_string(concatAny(i, j)) << std::endl;
  std::vector<int> l = {1, 2, 3};
  std::vector<int> m = {7, 12, 60};
  std::cout << std::to_string(concatInts(l, m)) << std::endl;
  return 0;
}
