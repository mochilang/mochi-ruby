! Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
program averages_root_mean_square
  implicit none
  integer :: n
  real :: sum
  integer :: x
  real :: rms
  character(len=100) :: s0
    integer :: guess
    integer :: i
  n = 10
  sum = 0.0
  x = 1
  do while ((x <= n))
    sum = ((sum + (real(x))) * (real(x)))
    x = (x + 1)
  end do
  rms = sqrtApprox((sum / (real(n))))
  write(s0,'(G0)') rms
  print *, s0
  
  contains
  recursive real function sqrtApprox(x) result(res)
    real, intent(in) :: x
    guess = x
    i = 0
    do while ((i < 20))
      guess = ((((guess + x) / guess)) / 2.0)
      i = (i + 1)
    end do
    res = guess
    return
  end function sqrtApprox
end program averages_root_mean_square
