// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:35:21Z
bool isPrime(int n) {
  if (n < 2) {
    return false;
  }
  if (n % 2 == 0) {
    return n == 2;
  }
  if (n % 3 == 0) {
    return n == 3;
  }
  var d = 5;
  while (((d as num) * (d as num) as num) <= n) {
    if (n % (d as num) == 0) {
      return false;
    }
    d = (d as num) + 2;
    if (n % (d as num) == 0) {
      return false;
    }
    d = (d as num) + 4;
  }
  return true;
}

List<int> circs = [];

bool isCircular(int n) {
  var nn = n;
  var pow = 1;
  while ((nn as num) > 0) {
    pow = (pow as num) * 10;
    nn = (nn as num) / 10;
  }
  nn = n;
  while (true) {
    nn = (nn as num) * 10;
    var f = (nn as num) / (pow as num);
    nn = (nn as num) + ((f as num) * ((1 - (pow as num)) as num) as num);
    if (nn == n) {
      break;
    }
    if (!isPrime(nn)) {
      return false;
    }
  }
  return true;
}

var digits = [
  1,
  3,
  7,
  9,
];

var q = [
  1,
  2,
  3,
  5,
  7,
  9,
];

var fq = [
  1,
  2,
  3,
  5,
  7,
  9,
];

num count = 0;

String showList(List<int> xs) {
  var out = '[';
  num i = 0;
  while ((i as num) < xs.length) {
    out = out + xs[i].toString();
    if ((i as num) < xs.length - 1) {
      out = out + ', ';
    }
    i = (i as num) + 1;
  }
  return out + ']';
}

void main() {
  print('The first 19 circular primes are:');
  while (true) {
    var f = q[0];
    var fd = fq[0];
    if (isPrime(f) && isCircular(f)) {
      circs = List.from(circs)..add(f);
      count = count + 1;
      if (count == 19) {
        break;
      }
    }
    q = q.sublist(1, q.length);
    fq = fq.sublist(1, fq.length);
    if (f != 2 && f != 5) {
      for (var d in digits) {
        q = List.from(q)..add(((f as num) * 10 as num) + d);
        fq = List.from(fq)..add(fd);
      }
    }
  }
  print(showList(circs));
  print('\nThe next 4 circular primes, in repunit format, are:');
  print('[R(19) R(23) R(317) R(1031)]');
  print('\nThe following repunits are probably circular primes:');
  for (var i in [
    5003,
    9887,
    15073,
    25031,
    35317,
    49081,
  ]) {
    print('R(' + i.toString() + ') : true');
  }
}
