// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:35:14Z
String lower(String ch) {
  var up = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
  var low = 'abcdefghijklmnopqrstuvwxyz';
  num i = 0;
  while ((i as num) < up.length) {
    if (ch == up.toString().substring(i, (i as num) + 1)) {
      return low.toString().substring(i, (i as num) + 1);
    }
    i = (i as num) + 1;
  }
  return ch;
}

var partList = [
  'A',
  'B',
  'C',
  'D',
];

var nAssemblies = 3;

void main() {
  for (var p in partList) {
    print(p + ' worker running');
  }
  for (var cycle = 1; cycle < (nAssemblies + 1); cycle++) {
    print('begin assembly cycle ' + cycle.toString());
    var a = '';
    for (var p in partList) {
      print(p + ' worker begins part');
      print(p + ' worker completed ' + lower(p));
      a = a + lower(p);
    }
    print(a + ' assembled.  cycle ' + cycle.toString() + ' complete');
  }
  for (var p in partList) {
    print(p + ' worker stopped');
  }
}
