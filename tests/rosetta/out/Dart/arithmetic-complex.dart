// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:33:59Z
class Complex {
  double re;
  double im;
  Complex(this.re, this.im);
}

Complex add(Complex a, Complex b) {
  return Complex(a.re + b.re, a.im + b.im);
}

Complex mul(Complex a, Complex b) {
  return Complex(a.re * b.re - a.im * b.im, a.re * b.im + a.im * b.re);
}

Complex neg(Complex a) {
  return Complex(-(a.re as num), -(a.im as num));
}

Complex inv(Complex a) {
  var denom = a.re * a.re + a.im * a.im;
  return Complex(a.re / (denom as num), -(a.im as num) / (denom as num));
}

Complex conj(Complex a) {
  return Complex(a.re, -(a.im as num));
}

String cstr(Complex a) {
  var s = '(' + a.re.toString();
  if (a.im >= 0) {
    s = s + '+' + a.im.toString() + 'i)';
  }
  else {
    s = s + a.im.toString() + 'i)';
  }
  return s;
}

var a = Complex(1, 1);

var b = Complex(3.14159, 1.25);

void main() {
  print('a:       ' + cstr(a));
  print('b:       ' + cstr(b));
  print('a + b:   ' + cstr(add(a, b)));
  print('a * b:   ' + cstr(mul(a, b)));
  print('-a:      ' + cstr(neg(a)));
  print('1 / a:   ' + cstr(inv(a)));
  print('a̅:       ' + cstr(conj(a)));
}
