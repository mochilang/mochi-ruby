// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:35:26Z
List<String> split(String s, String sep) {
  List<String> out = [];
  num start = 0;
  num i = 0;
  var n = sep.length;
  while ((i as num) <= (s.length - (n as num) as num)) {
    if (s.toString().substring(i, (i as num) + (n as num)) == sep) {
      out = List.from(out)..add(s.toString().substring(start, i));
      i = (i as num) + (n as num);
      start = i;
    }
    else {
      i = (i as num) + 1;
    }
  }
  out = List.from(out)..add(s.toString().substring(start, s.length));
  return out;
}

String htmlEscape(String s) {
  var out = '';
  num i = 0;
  while ((i as num) < s.length) {
    var ch = s.toString().substring(i, (i as num) + 1);
    if (ch == '&') {
      out = out + '&amp;';
    }
    else 
    if (ch == '<') {
      out = out + '&lt;';
    }
    else 
    if (ch == '>') {
      out = out + '&gt;';
    }
    else {
      out = (out as num) + (ch as num);
    }
    i = (i as num) + 1;
  }
  return out;
}

var c = 'Character,Speech\n' + 'The multitude,The messiah! Show us the messiah!\n' + 'Brians mother,<angry>Now you listen here! He\'s not the messiah; he\'s a very naughty boy! Now go away!</angry>\n' + 'The multitude,Who are you?\n' + 'Brians mother,I\'m his mother; that\'s who!\n' + 'The multitude,Behold his mother! Behold his mother!';

List<List<String>> rows = [];

void main() {
  for (var line in split(c, '\n')) {
    rows = List.from(rows)..add(split(line, ','));
  }
  print('<table>');
  for (var row in rows) {
    var cells = '';
    for (var cell in row) {
      cells = cells + '<td>' + htmlEscape(cell) + '</td>';
    }
    print('    <tr>' + cells + '</tr>');
  }
  print('</table>');
}
