// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:33:38Z
var PI = 3.141592653589793;

double sinApprox(double x) {
  var term = x;
  var sum = x;
  var n = 1;
  while ((n as num) <= 12) {
    var denom = double.parse((((2 * (n as num)) as num) * (((2 * (n as num) as num) + 1) as num)));
    term = (((-(term as num) as num) * x as num) * x as num) / (denom as num);
    sum = (sum as num) + (term as num);
    n = (n as num) + 1;
  }
  return sum;
}

var dt = 0.01;

num s = 0;

num t1 = 0;

var k1 = sinApprox(0);

var i = 1;

var i2 = 1;

void main() {
  while (i <= 200) {
    var t2 = (double.parse(i)) * dt;
    var k2 = sinApprox((t2 as num) * PI);
    s = s + ((((k1 + (k2 as num)) as num) * 0.5 as num) * (((t2 as num) - t1) as num) as num);
    t1 = t2;
    k1 = k2;
    i = i + 1;
  }
  while (i2 <= 50) {
    var t2 = 2 + (double.parse(i2)) * dt;
    num k2 = 0;
    s = s + ((((k1 + (k2 as num)) as num) * 0.5 as num) * (((t2 as num) - t1) as num) as num);
    t1 = t2;
    k1 = k2;
    i2 = i2 + 1;
  }
  print(s);
}
