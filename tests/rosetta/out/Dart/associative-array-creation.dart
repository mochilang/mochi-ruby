// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:34:09Z
import 'dart:io';

bool _in(dynamic item, dynamic col) {
  if (col is Map) return col.containsKey(item);
  if (col is Iterable || col is String) return col.contains(item);
  return false;
}

Map<String, int> removeKey(Map<String, int> m, String k) {
  Map<String, int> out = {};
  for (var key in m.keys) {
    if (key != k) {
      out[key] = (m as Map)[key];
    }
  }
  return out;
}

void _main() {
  Map<String, int> x = null;
  x = {};
  x['foo'] = 3;
  var y1 = x['bar'];
  var ok = _in('bar', x);
  print(y1);
  print(ok);
  x = removeKey(x, 'foo');
  x = {'foo': 2, 'bar': 42, 'baz': -1};
  _print([x['foo'], x['bar'], x['baz']]);
}

void main() {
  _main();
}

void _print(List<dynamic> args) {
    for (var i = 0; i < args.length; i++) {
        if (i > 0) stdout.write(' ');
        var v = args[i];
        if (v is List) {
            stdout.write(v.join(' '));
        } else if (v is double && v == v.roundToDouble()) {
            stdout.write(v.toInt());
        } else {
            stdout.write(v);
        }
    }
    stdout.writeln();
}
