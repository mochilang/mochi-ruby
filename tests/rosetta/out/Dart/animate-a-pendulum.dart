// Generated by Mochi compiler v0.10.28 on 2025-07-18T09:33:49Z
var PI = 3.141592653589793;

double sinApprox(double x) {
  var term = x;
  var sum = x;
  var n = 1;
  while ((n as num) <= 10) {
    var denom = double.parse((((2 * (n as num)) as num) * (((2 * (n as num) as num) + 1) as num)));
    term = (((-(term as num) as num) * x as num) * x as num) / (denom as num);
    sum = (sum as num) + (term as num);
    n = (n as num) + 1;
  }
  return sum;
}

double cosApprox(double x) {
  var term = 1;
  var sum = 1;
  var n = 1;
  while ((n as num) <= 10) {
    var denom = double.parse(((((2 * (n as num) as num) - 1) as num) * ((2 * (n as num)) as num)));
    term = (((-(term as num) as num) * x as num) * x as num) / (denom as num);
    sum = (sum as num) + (term as num);
    n = (n as num) + 1;
  }
  return sum;
}

double sqrtApprox(double x) {
  var guess = x;
  num i = 0;
  while ((i as num) < 10) {
    guess = (((guess as num) + (x / (guess as num) as num)) as num) / 2;
    i = (i as num) + 1;
  }
  return guess;
}

var L = 10;

var G = 9.81;

var dt = 0.2;

var phi0 = PI / 4;

var omega = sqrtApprox(G / L);

num t = 0;

void main() {
  for (var step = 0; step < 10; step++) {
    var phi = phi0 * cosApprox(omega * t);
    var pos = int.parse((10 * sinApprox(phi) + 0.5));
    print(pos.toString());
    t = t + dt;
  }
}
