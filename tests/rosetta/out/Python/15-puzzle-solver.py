# Generated by Mochi compiler v0.10.30 on 1970-01-01T00:00:00Z
testpkg = {
    "Add": lambda a, b: a + b,
    "Pi": 3.14,
    "Answer": 42,
    "FifteenPuzzleExample": lambda: "Solution found in 52 moves: rrrulddluuuldrurdddrullulurrrddldluurddlulurruldrdrd",
}
from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()
import sys

sys.set_int_max_str_digits(0)
print(
    (
        testpkg.get("FifteenPuzzleExample")
        if isinstance(testpkg, dict)
        else getattr(testpkg, "FifteenPuzzleExample")
    )()
)
