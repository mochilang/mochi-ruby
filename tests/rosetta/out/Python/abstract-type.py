# Generated by Mochi compiler v0.10.30 on 1970-01-01T00:00:00Z
from __future__ import annotations
import dataclasses
from typing import Any, TypeVar, Generic, Callable

T = TypeVar("T")
K = TypeVar("K")
UNDEFINED = object()
import sys

sys.set_int_max_str_digits(0)


def beastKind(b):

    def _match0(_t0):
        match _t0:
            case Dog(k, _):
                return k
            case Cat(k, _):
                return k
            case _:
                return None

    return _match0(b)


def beastName(b):

    def _match1(_t1):
        match _t1:
            case Dog(_, n):
                return n
            case Cat(_, n):
                return n
            case _:
                return None

    return _match1(b)


def beastCry(b):

    def _match2(_t2):
        match _t2:
            case Dog(_, _):
                return "Woof"
            case Cat(_, _):
                return "Meow"
            case _:
                return None

    return _match2(b)


def bprint(b):
    print(
        beastName(b) + ", who's a " + beastKind(b) + ', cries: "' + beastCry(b) + '".'
    )


def main():
    d: Beast = Dog(kind="labrador", name="Max")
    c: Beast = Cat(kind="siamese", name="Sammy")
    bprint(d)
    bprint(c)


class Beast:
    pass


@dataclasses.dataclass
class Dog(Beast):
    kind: str
    name: str


@dataclasses.dataclass
class Cat(Beast):
    kind: str
    name: str


main()
