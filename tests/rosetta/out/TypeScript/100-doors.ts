// Generated by Mochi compiler v0.10.30 on 2025-07-19T02:08:26Z
// Source: /workspace/mochi/tests/rosetta/x/Mochi/100-doors.mochi

var doors: any[];

function main(): void {
  var doors: any[] = [];
  for (let i: number = 0; i < 100; i++) {
    doors = [...doors, false];
  }
  for (let pass: number = 1; pass < 101; pass++) {
    var idx = pass - 1;
    while ((idx < 100)) {
      doors[idx] = !doors[idx];
      idx = idx + pass;
    }
  }
  for (let row: number = 0; row < 10; row++) {
    var line = "";
    for (let col: number = 0; col < 10; col++) {
      let idx = (row * 10) + col;
      if (doors[idx]) {
        line = `${line}1`;
      } else {
        line = `${line}0`;
      }
      if ((col < 9)) {
        line = `${line} `;
      }
    }
    console.log(line);
  }
}
main();
