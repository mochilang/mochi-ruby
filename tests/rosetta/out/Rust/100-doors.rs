// Generated by Mochi compiler v0.10.30 on 2025-07-19T02:23:43Z
fn main() {
    let mut doors = vec![];
    for i in 0..100 {
        doors = { let mut tmp = doors.clone(); tmp.push(false); tmp };
    }
    for pass in 1..101 {
        let mut idx = pass - 1;
        while idx < 100 {
            doors[(idx) as usize] = !doors[(idx) as usize].clone();
            idx += pass;
        }
    }
    for row in 0..10 {
        let mut line = String::new();
        for col in 0..10 {
            let idx = row * 10 + col;
            if doors[(idx) as usize].clone() != Default::default() {
                line = format!("{}{}", line, String::from("1"));
            } else {
                line = format!("{}{}", line, String::from("0"));
            }
            if col < 9 {
                line = format!("{}{}", line, String::from(" "));
            }
        }
        println!("{}", vec![format!("{}", line)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    }
}
