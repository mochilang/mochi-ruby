// Generated by Mochi compiler v0.10.30 on 2006-01-02T15:04:05Z
fn _input() -> String {
    use std::io::{self, Read};
    let mut s = String::new();
    io::stdin().read_line(&mut s).unwrap();
    while matches!(s.chars().last(), Some('\n') | Some('\r')) { s.pop(); }
    s
}

fn _int<T: std::fmt::Display>(v: T) -> i32 {
    v.to_string().trim().parse::<i32>().unwrap_or(0)
}

fn main() {
    let main = move || -> () {
        let a = _int(_input());
        let b = _int(_input());
        println!("{}", vec![format!("{}", a + b)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    };
    main();
}
