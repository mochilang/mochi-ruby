// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
fn main() {
    fn shuffle(xs: Vec<&'static str>) -> Vec<&'static str> {
        let mut arr = xs;
        let mut i = arr.len() as i32 - 1;
        while i > 0 {
            let j = now() % (i + 1);
            let tmp = arr[i as usize];
            arr[i as usize] = arr[j as usize];
            arr[j as usize] = tmp;
            i -= 1;
        }
        return arr;
    }
    let mut i = 0;
    while i < 3 {
        println!("");
        for w in shuffle(vec!["Enjoy", "Rosetta", "Code"]) {
            println!("{}", vec![format!("{}", w)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
        }
        i += 1;
    }
}
