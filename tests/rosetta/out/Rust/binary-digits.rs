// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
fn main() {
    fn toBin(n: i32) -> &'static str {
        if n == 0 {
            return "0";
        }
        let mut bits = String::new();
        let mut x: i32 = n;
        while x > 0 {
            bits = format!("{}{}", x % 2.to_string(), bits);
            x = ((x as f64) / (2 as f64)).parse::<i32>().unwrap();
        }
        return bits;
    }
    for i in 0..16 {
        println!("{}", vec![format!("{}", toBin(i))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    }
}
