// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
fn main() {
    fn mkAdd(a: i32) -> Box<dyn Fn(i32) -> i32> {
        return Box::new(move |b: i32| a + b);
    }
    fn mysum(x: i32, y: i32) -> i32 {
        return x + y;
    }
    fn partialSum(x: i32) -> Box<dyn Fn(i32) -> i32> {
        return Box::new(move |y: i32| mysum(x, y));
    }
    fn main() -> () {
        let add2 = mkAdd(2);
        let add3 = mkAdd(3);
        println!("{}", vec![format!("{}", format!("{}{}", format!("{}{}", add2(5).to_string(), " "), add3(6).to_string()))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
        let partial = partialSum(13);
        println!("{}", vec![format!("{}", partial(5).to_string())].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    }
    main();
}
