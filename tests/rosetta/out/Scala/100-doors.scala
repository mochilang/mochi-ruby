// Generated by Mochi compiler v0.10.30 on 2006-01-02T15:04:05Z
object _100_doors {
  def main(args: Array[String]): Unit = {
    var doors = scala.collection.mutable.ArrayBuffer[Any]()
    for(i <- 0 until 100) {
      doors = doors :+ false
    }
    for(pass <- 1 until 101) {
      var idx = pass - 1
      while (idx < 100) {
        doors = doors.updated(idx, !(doors).apply(idx).asInstanceOf[Boolean])
        idx += pass
      }
    }
    for(row <- 0 until 10) {
      var line = ""
      for(col <- 0 until 10) {
        var idx = row * 10 + col
        if ((doors).apply(idx).asInstanceOf[Boolean]) {
          line += "1"
        } else {
          line += "0"
        }
        if (col < 9) {
          line += " "
        }
      }
      println(line)
    }
  }
}
