// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
case class LDAPClient(var Base: String, var Host: String, var Port: Int, var UseSSL: Boolean, var BindDN: String, var BindPassword: String, var UserFilter: String, var GroupFilter: String, var Attributes: List[String])

object active_directory_connect {
  def connect(client: LDAPClient): Boolean = client.Host != "" && client.Port > 0
  
  def main() = {
    val client = LDAPClient(Base = "dc=example,dc=com", Host = "ldap.example.com", Port = 389, UseSSL = false, BindDN = "uid=readonlyuser,ou=People,dc=example,dc=com", BindPassword = "readonlypassword", UserFilter = "(uid=%s)", GroupFilter = "(memberUid=%s)", Attributes = List("givenName", "sn", "mail", "uid"))
    if (connect(client)) {
      println("Connected to " + client.Host)
    } else {
      println("Failed to connect")
    }
  }
  
  def main(args: Array[String]): Unit = {
    main()
  }
}
