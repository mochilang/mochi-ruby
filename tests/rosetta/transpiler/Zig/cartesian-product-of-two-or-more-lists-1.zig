// Generated by Mochi Zig transpiler on 2025-08-05 00:37 +0700
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

fn cart2(a: []i64, b: []i64) [][]i64 {
    var p: [][]i64 = &[_][]i64{};
    for (a) |__it0| {
        const x = __it0;
        for (b) |__it1| {
            const y = __it1;
            p = blk: { var _tmp = std.ArrayList([]i64).init(std.heap.page_allocator); _tmp.appendSlice(@as([]const []i64, p)) catch |err| handleError(err); _tmp.append(blk0: { var _tmp0 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp0[0] = x; _tmp0[1] = y; break :blk0 _tmp0; }) catch |err| handleError(err); break :blk (_tmp.toOwnedSlice() catch |err| handleError(err)); };
        }
    }
    return p;
}

fn llStr(lst: [][]i64) []const u8 {
    var s: []const u8 = "[";
    var i: i64 = 0;
    while (i < @as(i64, @intCast(lst.len))) {
        const row: []i64 = lst[@intCast(i)];
        s = _concat_string(s, "[");
        var j: i64 = 0;
        while (j < @as(i64, @intCast(row.len))) {
            s = _concat_string(s, _str(row[@intCast(j)]));
            if (j < @as(i64, @intCast(row.len)) - 1) {
                s = _concat_string(s, " ");
            }
            j = j + 1;
        }
        s = _concat_string(s, "]");
        if (i < @as(i64, @intCast(lst.len)) - 1) {
            s = _concat_string(s, " ");
        }
        i = i + 1;
    }
    s = _concat_string(s, "]");
    return s;
}

fn mochi_main() void {
    std.debug.print("{s}\n", .{llStr(cart2(blk1: { var _tmp1 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp1[0] = 1; _tmp1[1] = 2; break :blk1 _tmp1; }, blk2: { var _tmp2 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp2[0] = 3; _tmp2[1] = 4; break :blk2 _tmp2; }))});
    std.debug.print("{s}\n", .{llStr(cart2(blk3: { var _tmp3 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp3[0] = 3; _tmp3[1] = 4; break :blk3 _tmp3; }, blk4: { var _tmp4 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp4[0] = 1; _tmp4[1] = 2; break :blk4 _tmp4; }))});
    std.debug.print("{s}\n", .{llStr(cart2(blk5: { var _tmp5 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp5[0] = 1; _tmp5[1] = 2; break :blk5 _tmp5; }, &[_]i64{}))});
    std.debug.print("{s}\n", .{llStr(cart2(&[_]i64{}, blk6: { var _tmp6 = std.heap.page_allocator.alloc(i64, 2) catch unreachable; _tmp6[0] = 1; _tmp6[1] = 2; break :blk6 _tmp6; }))});
}

pub fn main() void {
    {
        const __start = _now();
        const __start_mem: i64 = _mem();
        mochi_main();
        const __end = _now();
        const __end_mem: i64 = _mem();
        const __duration_us: i64 = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __mem_diff: i64 = __end_mem - __start_mem;
        const __memory_bytes: i64 = if (__mem_diff < 0) -__mem_diff else __mem_diff;
        std.debug.print("{{\"duration_us\":{d},\"memory_bytes\":{d},\"name\":\"main\"}}\n", .{__duration_us, __memory_bytes});
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |env_seed| {
            defer std.heap.page_allocator.free(env_seed);
            if (std.fmt.parseInt(i64, env_seed, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d}", .{v}) catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(lhs: []const u8, rhs: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    var out = alloc.alloc(u8, lhs.len + rhs.len + 1) catch unreachable;
    std.mem.copyForwards(u8, out[0..lhs.len], lhs);
    std.mem.copyForwards(u8, out[lhs.len..lhs.len + rhs.len], rhs);
    out[lhs.len + rhs.len] = 0;
    return out[0..lhs.len + rhs.len];
}

fn _mem() i64 {
    const path = "/proc/self/statm";
    var file = std.fs.openFileAbsolute(path, .{}) catch return 0;
    defer file.close();
    var buf: [64]u8 = undefined;
    const n = file.readAll(&buf) catch return 0;
    var it = std.mem.tokenizeScalar(u8, buf[0..n], ' ');
    _ = it.next(); // total program size
    if (it.next()) |tok| {
        const pages = std.fmt.parseInt(i64, tok, 10) catch return 0;
        return pages * std.mem.page_size;
    }
    return 0;
}

fn _print(v: []const u8) void {
    std.debug.print("{s}\n", .{v});
}
