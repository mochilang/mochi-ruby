// Generated by Mochi compiler v0.10.30 on 2025-07-19T00:26:04Z
const std = @import("std");

fn _input() []const u8 {
    var buf = std.ArrayList(u8).init(std.heap.page_allocator);
    defer buf.deinit();
    _ = std.io.getStdIn().reader().readUntilDelimiterOrEof(&buf, '\\x0a') catch return "";
    return std.mem.trim(u8, buf.items, " \t\r\n");
}

fn _int(v: anytype) i32 {
    return switch (@TypeOf(v)) {
        i32 => v,
        f64 => @as(i32, @intFromFloat(v)),
        bool => @intFromBool(v),
        []const u8 => std.fmt.parseInt(i32, v, 10) catch 0,
        else => 0,
    };
}

fn user_main() void {
    const a = _int(_input()); // i32
    const b = _int(_input()); // i32
    std.debug.print("{any}\n", .{(a + b)});
}

pub fn main() void {
    user_main();
}
