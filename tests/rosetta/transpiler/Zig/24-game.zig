// Generated by Mochi Zig transpiler on 2025-07-27 00:44 +0700
const std = @import("std");

fn randDigit() i64 {
    return @mod(_now(), 9) + 1;
}

fn mochi_main() void {
    var digits: []i64 = &[_]i64{};
    for (0..4) |_| {
        digits = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(digits) catch unreachable; _tmp.append(randDigit()) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; };
    }
    var numstr: []const u8 = "";
    for (0..4) |__it1| {
        const i: i64 = @intCast(__it1);
        numstr = _concat_string(numstr, _str(digits[@as(usize, @intCast(i))]));
    }
    std.debug.print("{s}\n", .{_concat_string(_concat_string("Your numbers: ", numstr), "\n")});
    std.debug.print("{s}\n", .{"Enter RPN: "});
    var expr: []const u8 = _input();
    if (std.mem.len(expr) != 7) {
        std.debug.print("{s}\n", .{"invalid. expression length must be 7. (4 numbers, 3 operators, no spaces)"});
        return;
    }
    var stack: []i64 = &[_]i64{};
    var i: i64 = 0;
    var valid: bool = true;
    while (i < std.mem.len(expr)) {
        const ch: []const u8 = expr[i..i + 1];
        if (std.mem.order(u8, ch, "0") != .lt and std.mem.order(u8, ch, "9") != .gt) {
            if (std.mem.len(digits) == 0) {
                std.debug.print("{s}\n", .{"too many numbers."});
                return;
            }
            var j: i64 = 0;
            while (digits[@as(usize, @intCast(j))] != int(ch) - int("0")) {
                j = j + 1;
                if (j == std.mem.len(digits)) {
                    std.debug.print("{s}\n", .{"wrong numbers."});
                    return;
                }
            }
            digits = digits[..j] + digits[j + 1..];
            stack = blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(stack) catch unreachable; _tmp.append(float(int(ch) - int("0"))) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; };
        } else {
            if (std.mem.len(stack) < 2) {
                std.debug.print("{s}\n", .{"invalid expression syntax."});
                valid = false;
                break;
            }
            var b: i64 = stack[@as(usize, @intCast(std.mem.len(stack) - 1))];
            var a: i64 = stack[@as(usize, @intCast(std.mem.len(stack) - 2))];
            if (std.mem.eql(u8, ch, "+")) {
                stack[@as(usize, @intCast(std.mem.len(stack) - 2))] = a + b;
            } else {
                if (std.mem.eql(u8, ch, "-")) {
                    stack[@as(usize, @intCast(std.mem.len(stack) - 2))] = a - b;
                } else {
                    if (std.mem.eql(u8, ch, "*")) {
                        stack[@as(usize, @intCast(std.mem.len(stack) - 2))] = a * b;
                    } else {
                        if (std.mem.eql(u8, ch, "/")) {
                            stack[@as(usize, @intCast(std.mem.len(stack) - 2))] = @divTrunc(a, b);
                        } else {
                            std.debug.print("{s}\n", .{_concat_string(ch, " invalid.")});
                            valid = false;
                            break;
                        }
                    }
                }
            }
            stack = stack[..std.mem.len(stack) - 1];
        }
        i = i + 1;
    }
    if (valid) {
        if (abs(stack[@as(usize, @intCast(0))] - 24.0) > 0.000001) {
            std.debug.print("{s}\n", .{_concat_string(_concat_string("incorrect. ", _str(stack[@as(usize, @intCast(0))])), " != 24")});
        } else {
            std.debug.print("{s}\n", .{"correct."});
        }
    }
}

pub fn main() void {
    {
        const __start_mem = _mem();
        const __start = _now();
        mochi_main();
        const __end = _now();
        const __end_mem = _mem();
        const __duration_us = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __memory_bytes = __end_mem - __start_mem;
        const __bench = .{ .duration_us = __duration_us, .memory_bytes = __memory_bytes, .name = "main" };
        const __bj = std.fmt.allocPrint(std.heap.page_allocator, "{f}", .{std.json.fmt(__bench, .{ .whitespace = .indent_2 })}) catch unreachable;
        std.debug.print("{s}\n", .{__bj});
        std.heap.page_allocator.free(__bj);
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |s| {
            defer std.heap.page_allocator.free(s);
            if (std.fmt.parseInt(i64, s, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d:.1}", .{v}) catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(a: []const u8, b: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    return std.mem.concat(alloc, u8, &[_][]const u8{ a, b }) catch unreachable;
}

var _in_buf = std.io.bufferedReader(std.io.getStdIn().reader());
fn _input() []const u8 {
    const opt_line = _in_buf.reader().readUntilDelimiterOrEofAlloc(std.heap.page_allocator, '\n', 1 << 20) catch return "";
    const line = opt_line orelse return "";
    if (line.len > 0 and line[line.len - 1] == '\n') {
        return line[0..line.len-1];
    }
    return line;
}

fn _mem() i64 {
    var usage: std.os.linux.rusage = undefined;
    if (std.os.linux.getrusage(std.os.linux.rusage.SELF, &usage) != 0) return 0;
    return @as(i64, usage.maxrss) * 1024;
}
