// Generated by Mochi Zig transpiler on 2025-07-27 00:44 +0700
const std = @import("std");

const Rational = struct {
    num: i64,
    denom: i64,
};

const Node = struct {
    val: Rational,
    txt: []const u8,
};

const OP_ADD: i64 = 1;
const OP_SUB: i64 = 2;
const OP_MUL: i64 = 3;
const OP_DIV: i64 = 4;
const n_cards: i64 = 4;
const goal: i64 = 24;
const digit_range: i64 = 9;

fn makeNode(n: i64) Node {
    return .{ .val = .{ .num = n, .denom = 1 }, .txt = _str(n) };
}

fn combine(op: i64, l: Node, r: Node) Node {
    var res: i64 = 0;
    if (op == OP_ADD) {
        res = .{ .num = l.val.num * r.val.denom + l.val.denom * r.val.num, .denom = l.val.denom * r.val.denom };
    } else {
        if (op == OP_SUB) {
            res = .{ .num = l.val.num * r.val.denom - l.val.denom * r.val.num, .denom = l.val.denom * r.val.denom };
        } else {
            if (op == OP_MUL) {
                res = .{ .num = l.val.num * r.val.num, .denom = l.val.denom * r.val.denom };
            } else {
                res = .{ .num = l.val.num * r.val.denom, .denom = l.val.denom * r.val.num };
            }
        }
    }
    var opstr: []const u8 = "";
    if (op == OP_ADD) {
        opstr = " + ";
    } else {
        if (op == OP_SUB) {
            opstr = " - ";
        } else {
            if (op == OP_MUL) {
                opstr = " * ";
            } else {
                opstr = " / ";
            }
        }
    }
    return .{ .val = res, .txt = _concat_string(_concat_string(_concat_string(_concat_string("(", l.txt), opstr), r.txt), ")") };
}

fn exprEval(x: Node) Rational {
    return x.val;
}

fn exprString(x: Node) []const u8 {
    return x.txt;
}

fn solve(xs: []Node) bool {
    if (std.mem.len(xs) == 1) {
        const f: Rational = exprEval(xs[@as(usize, @intCast(0))]);
        if (f.denom != 0 and f.num == f.denom * goal) {
            std.debug.print("{s}\n", .{exprString(xs[@as(usize, @intCast(0))])});
            return true;
        }
        return false;
    }
    var i: i64 = 0;
    while (i < std.mem.len(xs)) {
        var j: i64 = i + 1;
        while (j < std.mem.len(xs)) {
            var rest: []Node = &[_]Node{};
            var k: i64 = 0;
            while (k < std.mem.len(xs)) {
                if (k != i and k != j) {
                    rest = blk: { var _tmp = std.ArrayList(Node).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(rest) catch unreachable; _tmp.append(xs[@as(usize, @intCast(k))]) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; };
                }
                k = k + 1;
            }
            const a: Node = xs[@as(usize, @intCast(i))];
            const b: Node = xs[@as(usize, @intCast(j))];
            var node: i64 = 0;
            for (@constCast(&[_]i64{OP_ADD, OP_SUB, OP_MUL, OP_DIV})[0..]) |__it0| {
                const op = __it0;
                node = combine(op, a, b);
                if (solve(blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(rest) catch unreachable; _tmp.append(node) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; })) {
                    return true;
                }
            }
            node = combine(OP_SUB, b, a);
            if (solve(blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(rest) catch unreachable; _tmp.append(node) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; })) {
                return true;
            }
            node = combine(OP_DIV, b, a);
            if (solve(blk: { var _tmp = std.ArrayList(i64).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(rest) catch unreachable; _tmp.append(node) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; })) {
                return true;
            }
            j = j + 1;
        }
        i = i + 1;
    }
    return false;
}

fn mochi_main() void {
    var iter: i64 = 0;
    while (iter < 10) {
        var cards: []Node = &[_]Node{};
        var i: i64 = 0;
        while (i < n_cards) {
            const n: i64 = @mod(_now(), digit_range - 1) + 1;
            cards = blk: { var _tmp = std.ArrayList(Node).init(std.heap.page_allocator); defer _tmp.deinit(); _tmp.appendSlice(cards) catch unreachable; _tmp.append(makeNode(n)) catch unreachable; const res = _tmp.toOwnedSlice() catch unreachable; break :blk res; };
            std.debug.print("{s}\n", .{_concat_string(" ", _str(n))});
            i = i + 1;
        }
        std.debug.print("{s}\n", .{":  "});
        if (!(solve(cards))) {
            std.debug.print("{s}\n", .{"No solution"});
        }
        iter = iter + 1;
    }
}

pub fn main() void {
    {
        const __start_mem = _mem();
        const __start = _now();
        mochi_main();
        const __end = _now();
        const __end_mem = _mem();
        const __duration_us = @divTrunc(@as(i64, @intCast(__end - __start)), 1000);
        const __memory_bytes = __end_mem - __start_mem;
        const __bench = .{ .duration_us = __duration_us, .memory_bytes = __memory_bytes, .name = "main" };
        const __bj = std.fmt.allocPrint(std.heap.page_allocator, "{f}", .{std.json.fmt(__bench, .{ .whitespace = .indent_2 })}) catch unreachable;
        std.debug.print("{s}\n", .{__bj});
        std.heap.page_allocator.free(__bj);
    }
}

var _now_seed: i64 = 0;
var _now_seeded: bool = false;
fn _now() i64 {
    if (_now_seeded) {
        _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
        return _now_seed;
    }
    if (! _now_seeded) {
        if (std.process.getEnvVarOwned(std.heap.page_allocator, "MOCHI_NOW_SEED")) |s| {
            defer std.heap.page_allocator.free(s);
            if (std.fmt.parseInt(i64, s, 10)) |v| {
                _now_seed = v;
                _now_seeded = true;
                _now_seed = @mod(_now_seed * 1664525 + 1013904223, 2147483647);
                return _now_seed;
            } else |_| {}
        } else |_| {}
    }
    return @as(i64, @intCast(std.time.nanoTimestamp()));
}

fn _str(v: anytype) []const u8 {
    if (@TypeOf(v) == f64 or @TypeOf(v) == f32) {
        return std.fmt.allocPrint(std.heap.page_allocator, "{d:.1}", .{v}) catch unreachable;
    }
    return std.fmt.allocPrint(std.heap.page_allocator, "{any}", .{v}) catch unreachable;
}

fn _concat_string(a: []const u8, b: []const u8) []const u8 {
    const alloc = std.heap.page_allocator;
    return std.mem.concat(alloc, u8, &[_][]const u8{ a, b }) catch unreachable;
}

fn _mem() i64 {
    var usage: std.os.linux.rusage = undefined;
    if (std.os.linux.getrusage(std.os.linux.rusage.SELF, &usage) != 0) return 0;
    return @as(i64, usage.maxrss) * 1024;
}
