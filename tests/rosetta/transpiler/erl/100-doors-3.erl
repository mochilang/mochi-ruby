#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1]).

% Generated by Mochi transpiler v0.10.40 (6bb4e4bd23) on 2025-07-25 19:02 +0700

main(_) ->
    erlang:put('result', ""),
    Fun = fun Fun_loop(List) ->
    case List of
        [] -> {};
        [I|I_rest] ->
            J = 1,
            Fun_2 = fun Fun_2_loop(I, J) ->
    case ((J * J) < I) of
        true ->
            J_2 = (J + 1),
            Fun_2_loop(I, J_2);
        _ -> {I, J}
    end
end,
{I, J_2} = Fun_2(I, J),
            case ((J_2 * J_2) == I) of
        true -> erlang:put('result', (erlang:get('result') ++ "O"));
        _ -> erlang:put('result', (erlang:get('result') ++ "-"))
    end,
            Fun_loop(I_rest)
    end
end,
{} = Fun(lists:seq(1, (101) - 1)),
    io:format("~ts~n", [erlang:get('result')]).
