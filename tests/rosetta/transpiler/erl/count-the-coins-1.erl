#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, countchange/1]).

% Generated by Mochi transpiler v0.10.41 (4a1f4378c8) on 2025-07-27 06:30 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.

countchange(Amount) ->
    try
        Ways = [],
        I = 0,
        Fun = fun Fun_loop(Amount, I, Ways) ->
    case (I =< Amount) of
        true ->
            Ways_2 = lists:append(Ways, [0]),
            I_2 = (I + 1),
            Fun_loop(Amount, I_2, Ways_2);
        _ -> {Amount, I, Ways}
    end
end,
{Amount, I_2, Ways_2} = Fun(Amount, I, Ways),
        Ways_3 = lists:sublist(Ways_2, 0) ++ [1] ++ lists:nthtail(0 + 1, Ways_2),
        Coins = [1, 5, 10, 25],
        Idx = 0,
        Fun_3 = fun Fun_3_loop(Amount, Coins, I_2, Idx, Ways_3) ->
    case (Idx < length(Coins)) of
        true ->
            Coin = lists:nth(Idx + 1, Coins),
            J = Coin,
            Fun_2 = fun Fun_2_loop(Amount, Coin, Coins, I_2, Idx, J, Ways_3) ->
    case (J =< Amount) of
        true ->
            Ways_4 = lists:sublist(Ways_3, J) ++ [(lists:nth(J + 1, Ways_3) + lists:nth((J - Coin) + 1, Ways_3))] ++ lists:nthtail(J + 1, Ways_3),
            J_2 = (J + 1),
            Fun_2_loop(Amount, Coin, Coins, I_2, Idx, J_2, Ways_4);
        _ -> {Amount, Coin, Coins, I_2, Idx, J, Ways_3}
    end
end,
{Amount, Coin, Coins, I_2, Idx, J_2, Ways_4} = Fun_2(Amount, Coin, Coins, I_2, Idx, J, Ways_3),
            Idx_2 = (Idx + 1),
            Fun_3_loop(Amount, Coins, I_2, Idx_2, Ways_4);
        _ -> {Amount, Coins, I_2, Idx, Ways_3}
    end
end,
{Amount, Coins, I_2, Idx_2, Ways_4} = Fun_3(Amount, Coins, I_2, Idx, Ways_3),
        lists:nth(Amount + 1, Ways_4)
    catch {return, Ret} -> Ret end.

main(_) ->
    BenchStart = mochi_now(),
    BenchStartMem = erlang:memory(total),
    erlang:put('amount', 10),
    io:format("~ts~n", [((("amount, ways to make change: " ++ lists:flatten(io_lib:format("~p", [10]))) ++ " ") ++ lists:flatten(io_lib:format("~p", [countchange(10)])))]),
    BenchEnd = mochi_now(),
    BenchEndMem = erlang:memory(total),
    DurationUs = (BenchEnd - BenchStart) div 1000,
    MemBytes = erlang:abs(BenchEndMem - BenchStartMem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [DurationUs, MemBytes]).
