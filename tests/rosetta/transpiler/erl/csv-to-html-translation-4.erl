#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1]).

% Generated by Mochi transpiler v0.10.42 (4d9a199575) on 2025-07-28 04:26 UTC


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.

main(_) ->
    BenchStart = mochi_now(),
    BenchStartMem = erlang:memory(total),
    io:format("~ts~n", ["<table>"]),
    io:format("~ts~n", ["    <tr><td>Character</td><td>Speech</td></tr>"]),
    io:format("~ts~n", ["    <tr><td>The multitude</td><td>The messiah! Show us the messiah!</td></tr>"]),
    io:format("~ts~n", ["    <tr><td>Brians mother</td><td>&lt;angry&gt;Now you listen here! He&#39;s not the messiah; he&#39;s a very naughty boy! Now go away!&lt;/angry&gt;</td></tr>"]),
    io:format("~ts~n", ["    <tr><td>The multitude</td><td>Who are you?</td></tr>"]),
    io:format("~ts~n", ["    <tr><td>Brians mother</td><td>I&#39;m his mother; that&#39;s who!</td></tr>"]),
    io:format("~ts~n", ["    <tr><td>The multitude</td><td>Behold his mother! Behold his mother!</td></tr>"]),
    io:format("~ts~n", ["</table>"]),
    BenchEnd = mochi_now(),
    BenchEndMem = erlang:memory(total),
    DurationUs = (BenchEnd - BenchStart) div 1000,
    MemBytes = erlang:abs(BenchEndMem - BenchStartMem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [DurationUs, MemBytes]).
