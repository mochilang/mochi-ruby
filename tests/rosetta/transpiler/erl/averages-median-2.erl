#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, sel/2, median/1]).

% Generated by Mochi transpiler v0.10.55 (87d3efb875) on 2025-08-03 08:53 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> list_to_integer(V)
        end
    end.


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try A / B catch _:_ -> 0.0 end.

mochi_safe_fmod(A, B) ->
    try math:fmod(A, B) catch _:_ -> 0.0 end.

sel(List, K) ->
    try
        I = 0,
        Fun_2 = fun Fun_2_loop(I, K, List) ->
    case (I =< K) of
        true ->
            MinIndex = I,
            J = (I + 1),
            Fun = fun Fun_loop(I, J, K, List, MinIndex) ->
    case (J < length(List)) of
        true ->
            case ((case erlang:is_map(List) of true -> maps:get(J, List, nil); _ -> lists:nth(J + 1, List) end) < (case erlang:is_map(List) of true -> maps:get(MinIndex, List, nil); _ -> lists:nth(MinIndex + 1, List) end)) of
        true -> MinIndex_2 = J,
            MinIndex_3 = MinIndex_2;
        _ -> MinIndex_3 = MinIndex
    end,
            J_2 = (J + 1),
            Fun_loop(I, J_2, K, List, MinIndex_3);
        _ -> {I, J, K, List, MinIndex}
    end
end,
{I, J_2, K, List, MinIndex_3} = Fun(I, J, K, List, MinIndex),
            Tmp = (case erlang:is_map(List) of true -> maps:get(I, List, nil); _ -> lists:nth(I + 1, List) end),
            List_2 = lists:sublist(List, I) ++ [(case erlang:is_map(List) of true -> maps:get(MinIndex_3, List, nil); _ -> lists:nth(MinIndex_3 + 1, List) end)] ++ lists:nthtail(I + 1, List),
            List_3 = lists:sublist(List_2, MinIndex_3) ++ [Tmp] ++ lists:nthtail(MinIndex_3 + 1, List_2),
            I_2 = (I + 1),
            Fun_2_loop(I_2, K, List_3);
        _ -> {I, K, List}
    end
end,
{I_2, K, List_3} = Fun_2(I, K, List),
        (case erlang:is_map(List_3) of true -> maps:get(K, List_3, nil); _ -> lists:nth(K + 1, List_3) end)
    catch {return, Ret} -> Ret end.

median(A) ->
    try
        Arr = A,
        Half = mochi_to_int((length(Arr) div 2)),
        Med = sel(Arr, Half),
        case ((length(Arr) rem 2) == 0) of
        true -> throw({return, mochi_safe_div((Med + (case erlang:is_map(Arr) of true -> maps:get((Half - 1), Arr, nil); _ -> lists:nth((Half - 1) + 1, Arr) end)), 2)});
        _ -> ok
    end,
        Med
    catch {return, Ret} -> Ret end.

main(_) ->
    __mochi_bench_start = mochi_now(),
    __mochi_bench_start_mem = erlang:memory(total),
    io:format("~ts~n", [lists:flatten(io_lib:format("~p", [median([3, 1, 4, 1])]))]),
    io:format("~ts~n", [lists:flatten(io_lib:format("~p", [median([3, 1, 4, 1, 5])]))]),
    __mochi_bench_end = mochi_now(),
    __mochi_bench_end_mem = erlang:memory(total),
    __mochi_bench_duration_us = (__mochi_bench_end - __mochi_bench_start) div 1000,
    __mochi_bench_mem_bytes = erlang:abs(__mochi_bench_end_mem - __mochi_bench_start_mem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [__mochi_bench_duration_us, __mochi_bench_mem_bytes]).
