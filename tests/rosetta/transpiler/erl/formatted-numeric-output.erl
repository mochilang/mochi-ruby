#!/usr/bin/env escript
-module(main).
-compile([nowarn_shadow_vars, nowarn_unused_vars]).
-export([main/1, pow10/1, formatfloat/2, padleftzeros/2]).

% Generated by Mochi transpiler v0.10.55 (67b72aa5ea) on 2025-08-02 22:23 +0700


mochi_now() ->
    case erlang:get(now_seed) of
        undefined ->
            case os:getenv("MOCHI_NOW_SEED") of
                false -> erlang:system_time(nanosecond);
                S ->
                    case catch list_to_integer(S) of
                        {'EXIT', _} -> erlang:system_time(nanosecond);
                        Seed ->
                            erlang:put(now_seed, Seed),
                            mochi_now()
                    end
            end;
        Seed ->
            Seed2 = (Seed * 1664525 + 1013904223) rem 2147483647,
            erlang:put(now_seed, Seed2),
            Seed2
    end.


mochi_to_int(V) ->
    case erlang:is_integer(V) of
        true -> V;
        _ -> case erlang:is_float(V) of
            true -> trunc(V);
            _ -> list_to_integer(V)
        end
    end.


mochi_safe_mul(A, B) ->
    try A * B catch _:_ -> 1.0e308 end.

mochi_safe_div(A, B) ->
    try A / B catch _:_ -> 0.0 end.

mochi_safe_fmod(A, B) ->
    try math:fmod(A, B) catch _:_ -> 0.0 end.

pow10(N) ->
    try
        R = 1,
        I = 0,
        Fun = fun Fun_loop(I, N, R) ->
    case (I < N) of
        true ->
            R_2 = mochi_safe_mul(R, 10),
            I_2 = (I + 1),
            Fun_loop(I_2, N, R_2);
        _ -> {I, N, R}
    end
end,
{I_2, N, R_2} = Fun(I, N, R),
        R_2
    catch {return, Ret} -> Ret end.

formatfloat(F, Prec) ->
    try
        Scale = pow10(Prec),
        Scaled = (mochi_safe_mul(F, Scale) + 0.5),
        N_2 = mochi_to_int(Scaled),
        Digits = lists:flatten(io_lib:format("~p", [N_2])),
        Fun_2 = fun Fun_2_loop(Digits, F, N_2, Prec, Scale, Scaled) ->
    case (length(Digits) =< Prec) of
        true ->
            Digits_2 = ("0" ++ Digits),
            Fun_2_loop(Digits_2, F, N_2, Prec, Scale, Scaled);
        _ -> {Digits, F, N_2, Prec, Scale, Scaled}
    end
end,
{Digits_2, F, N_2, Prec, Scale, Scaled} = Fun_2(Digits, F, N_2, Prec, Scale, Scaled),
        IntPart = string:substr(Digits_2, 0 + 1, ((length(Digits_2) - Prec) - 0)),
        FracPart = string:substr(Digits_2, (length(Digits_2) - Prec) + 1, (length(Digits_2) - (length(Digits_2) - Prec))),
        ((IntPart ++ ".") ++ FracPart)
    catch {return, Ret} -> Ret end.

padleftzeros(S, Width) ->
    try
        Out = S,
        Fun_3 = fun Fun_3_loop(Out, S, Width) ->
    case (length(Out) < Width) of
        true ->
            Out_2 = ("0" ++ Out),
            Fun_3_loop(Out_2, S, Width);
        _ -> {Out, S, Width}
    end
end,
{Out_2, S, Width} = Fun_3(Out, S, Width),
        Out_2
    catch {return, Ret} -> Ret end.

main(_) ->
    BenchStart = mochi_now(),
    BenchStartMem = erlang:memory(total),
    io:format("~ts~n", [padleftzeros(formatfloat(7.125, 3), 9)]),
    BenchEnd = mochi_now(),
    BenchEndMem = erlang:memory(total),
    DurationUs = (BenchEnd - BenchStart) div 1000,
    MemBytes = erlang:abs(BenchEndMem - BenchStartMem),
    io:format("{~n  \"duration_us\": ~p,~n  \"memory_bytes\": ~p,~n  \"name\": \"main\"~n}
", [DurationUs, MemBytes]).
