# Code generated by Mochi transpiler 2025-08-05 01:09 +0700
defmodule Main do
  def _now() do
    seeded = Process.get(:_now_seeded, false)
    seed = Process.get(:_now_seed, 0)
    if !seeded do
      case System.get_env("MOCHI_NOW_SEED") do
        nil -> :ok
        s ->
          case Integer.parse(s) do
            {v, ""} ->
              Process.put(:_now_seed, v)
              Process.put(:_now_seeded, true)
              seed = v
              seeded = true
            _ -> :ok
          end
      end
    end
    if seeded do
      seed = rem(seed * 1664525 + 1013904223, 2147483647)
      Process.put(:_now_seed, seed)
      abs(seed)
    else
      System.unique_integer([:positive, :monotonic])
    end
  end
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  defp _lookup_host(host) do
    case :inet.gethostbyname(String.to_charlist(host)) do
      {:ok, {:hostent, _, _, _, _, addrs}} ->
        ips = Enum.map(addrs, &:inet.ntoa/1)
        [ips, nil]
      {:error, reason} ->
        [nil, reason]
    end
  end
  defp _clamp_slice(n, start, stop) do
    start = if start < 0, do: start + n, else: start
    stop = if stop < 0, do: stop + n, else: stop
    start = max(min(start, n), 0)
    stop = min(max(stop, start), n)
    {start, stop}
  end
  defp _slice(base, start, len) do
    cond do
      is_binary(base) ->
        chars = String.graphemes(base)
        n = length(chars)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(chars, s, e - s) |> Enum.join("")
      true ->
        n = length(base)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(base, s, e - s)
    end
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  defp _bigrat(v) do
    _bigrat(v, 1)
  end
  defp _bigrat(n, d) do
    n = trunc(n)
    d = trunc(d)
    g = Integer.gcd(n, d)
    n = div(n, g)
    d = div(d, g)
    if d < 0 do
      {-n, -d}
    else
      {n, d}
    end
  end
  defp _bigrat_add(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd + ad * bn, ad * bd)
  end
  defp _bigrat_sub(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd - ad * bn, ad * bd)
  end
  defp _bigrat_mul(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bn, ad * bd)
  end
  defp _bigrat_div(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd, ad * bn)
  end
  defp _bigrat_neg(a) do
    {n, d} = a
    {-n, d}
  end
  defp _sha256(bs) do
    bin = :erlang.list_to_binary(bs)
    :crypto.hash(:sha256, bin) |> :erlang.binary_to_list()
  end
  defp _getenv(name) do
    System.get_env(name)
  end
  defp _environ() do
    System.get_env() |> Enum.map(fn {k, v} -> "#{k}=#{v}" end)
  end
  defp _getoutput(cmd) do
    {out, 0} = System.cmd("sh", ["-c", cmd])
    String.trim(out)
  end
  def sieve(limit) do
    try do
      primes = []
      i = 0
      while_fun = fn while_fun, i, primes ->
        if i < limit do
          primes = (primes ++ [true])
          i = i + 1
          while_fun.(while_fun, i, primes)
        else
          {i, primes}
        end
      end
      {i, primes} = try do
          while_fun.(while_fun, i, primes)
        catch
          {:break, {i, primes}} -> {i, primes}
        end

      primes = List.replace_at(primes, 0, false)
      primes = List.replace_at(primes, 1, false)
      p = 2
      while_fun_2 = fn while_fun_2, p, primes ->
        if p * p < limit do
          {primes} = if Enum.at(primes, p) do
            k = p * p
            while_fun_3 = fn while_fun_3, k, primes ->
              if k < limit do
                primes = List.replace_at(primes, k, false)
                k = k + p
                while_fun_3.(while_fun_3, k, primes)
              else
                {k, primes}
              end
            end
            {k, primes} = try do
                while_fun_3.(while_fun_3, k, primes)
              catch
                {:break, {k, primes}} -> {k, primes}
              end

            {primes}
          else
            {primes}
          end
          p = p + 1
          while_fun_2.(while_fun_2, p, primes)
        else
          {p, primes}
        end
      end
      {p, primes} = try do
          while_fun_2.(while_fun_2, p, primes)
        catch
          {:break, {p, primes}} -> {p, primes}
        end

      throw {:return, primes}
    catch
      {:return, val} -> val
    end
  end
  def goldbachCount(primes, n) do
    try do
      c = 0
      i = 1
      while_fun_4 = fn while_fun_4, c, i ->
        if i <= div(n, 2) do
          {c} = if Enum.at(primes, i) && Enum.at(primes, n - i) do
            c = c + 1
            {c}
          else
            {c}
          end
          i = i + 1
          while_fun_4.(while_fun_4, c, i)
        else
          {c, i}
        end
      end
      {c, i} = try do
          while_fun_4.(while_fun_4, c, i)
        catch
          {:break, {c, i}} -> {c, i}
        end

      throw {:return, c}
    catch
      {:return, val} -> val
    end
  end
  def pad(n) do
    try do
      if n < 10 do
        throw {:return, ("  " <> Kernel.to_string(n))}
      end
      if n < 100 do
        throw {:return, (" " <> Kernel.to_string(n))}
      end
      throw {:return, Kernel.to_string(n)}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      primes = sieve(1000)
      IO.puts("The first 100 Goldbach numbers:")
      line = ""
      n = 2
      count = 0
      while_fun_5 = fn while_fun_5, count, line, n ->
        if count < 100 do
          v = goldbachCount(primes, 2 * n)
          line = ((line <> pad(v)) <> " ")
          count = count + 1
          n = n + 1
          {line} = if rem(count, 10) == 0 do
            IO.puts(Kernel.inspect(_slice(line, 0, _len(line) - 1 - (0))))
            line = ""
            {line}
          else
            {line}
          end
          while_fun_5.(while_fun_5, count, line, n)
        else
          {count, line, n}
        end
      end
      {count, line, n} = try do
          while_fun_5.(while_fun_5, count, line, n)
        catch
          {:break, {count, line, n}} -> {count, line, n}
        end

      val = goldbachCount(primes, 1000)
      IO.puts(("\nThe 1,000th Goldbach number = " <> Kernel.to_string(val)))
    catch
      {:return, val} -> val
    end
  end
  def bench_main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    main()
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
