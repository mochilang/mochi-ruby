# Code generated by Mochi transpiler 2025-07-27 22:38 +0700
defmodule Main do
  defp _now() do
    seeded = Process.get(:_now_seeded, false)
    seed = Process.get(:_now_seed, 0)
    if !seeded do
      case System.get_env("MOCHI_NOW_SEED") do
        nil -> :ok
        s ->
          case Integer.parse(s) do
            {v, ""} ->
              Process.put(:_now_seed, v)
              Process.put(:_now_seeded, true)
              seed = v
              seeded = true
            _ -> :ok
          end
      end
    end
    if seeded do
      seed = rem(seed * 1664525 + 1013904223, 2147483647)
      Process.put(:_now_seed, seed)
      abs(seed)
    else
      abs(System.os_time(:nanosecond))
    end
  end
  defp _mem() do
    :erlang.memory(:total)
  end
  defp _lookup_host(host) do
    case :inet.gethostbyname(String.to_charlist(host)) do
      {:ok, {:hostent, _, _, _, _, addrs}} ->
        ips = Enum.map(addrs, &:inet.ntoa/1)
        [ips, nil]
      {:error, reason} ->
        [nil, reason]
    end
  end
  defp _clamp_slice(n, start, stop) do
    start = if start < 0, do: start + n, else: start
    stop = if stop < 0, do: stop + n, else: stop
    start = max(min(start, n), 0)
    stop = min(max(stop, start), n)
    {start, stop}
  end
  defp _slice(base, start, len) do
    cond do
      is_binary(base) ->
        chars = String.graphemes(base)
        n = length(chars)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(chars, s, e - s) |> Enum.join("")
      true ->
        n = length(base)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(base, s, e - s)
    end
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  defp _bigrat(v) do
    _bigrat(v, 1)
  end
  defp _bigrat(n, d) do
    g = Integer.gcd(n, d)
    n = div(n, g)
    d = div(d, g)
    if d < 0 do
      {-n, -d}
    else
      {n, d}
    end
  end
  defp _bigrat_add(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd + ad * bn, ad * bd)
  end
  defp _bigrat_sub(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd - ad * bn, ad * bd)
  end
  defp _bigrat_mul(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bn, ad * bd)
  end
  defp _bigrat_div(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd, ad * bn)
  end
  defp _bigrat_neg(a) do
    {n, d} = a
    {-n, d}
  end
  defp _sha256(bs) do
    bin = :erlang.list_to_binary(bs)
    :crypto.hash(:sha256, bin) |> :erlang.binary_to_list()
  end
  def examineAndModify(f) do
    try do
      IO.puts(((((((((" v: {" <> Kernel.to_string(f[:Exported])) <> " ") <> Kernel.to_string(f.unexported)) <> "} = {") <> Kernel.to_string(f[:Exported])) <> " ") <> Kernel.to_string(f.unexported)) <> "}"))
      IO.puts("    Idx Name       Type CanSet")
      IO.puts("     0: Exported   int  true")
      IO.puts("     1: unexported int  false")
      f = Map.put(f, :Exported, 16)
      f = Map.put(f, :unexported, 44)
      IO.puts("  modified unexported field via unsafe")
      throw {:return, f}
    catch
      {:return, val} -> val
    end
  end
  def anotherExample() do
    try do
      IO.puts("bufio.ReadByte returned error: unsafely injected error value into bufio inner workings")
    catch
      {:return, val} -> val
    end
  end
  Process.put(:obj, %{Exported: 12, unexported: 42})
  def main() do
    mem_start = _mem()
    t_start = _now()
    IO.puts((((("obj: {" <> Kernel.to_string(Process.get(:obj)[:Exported])) <> " ") <> Kernel.to_string(Process.get(:obj).unexported)) <> "}"))
    Process.put(:obj, Main.examineAndModify(Process.get(:obj)))
    IO.puts((((("obj: {" <> Kernel.to_string(Process.get(:obj)[:Exported])) <> " ") <> Kernel.to_string(Process.get(:obj).unexported)) <> "}"))
    Main.anotherExample()
    duration_us = div(_now() - t_start, 1000)
    mem_diff = abs(_mem() - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.main()
