# Code generated by Mochi transpiler 2025-07-27 16:31 +0700
defmodule Main do
  defp _now() do
    seeded = Process.get(:_now_seeded, false)
    seed = Process.get(:_now_seed, 0)
    if !seeded do
      case System.get_env("MOCHI_NOW_SEED") do
        nil -> :ok
        s ->
          case Integer.parse(s) do
            {v, ""} ->
              Process.put(:_now_seed, v)
              Process.put(:_now_seeded, true)
              seed = v
              seeded = true
            _ -> :ok
          end
      end
    end
    if seeded do
      seed = rem(seed * 1664525 + 1013904223, 2147483647)
      Process.put(:_now_seed, seed)
      abs(seed)
    else
      abs(System.os_time(:nanosecond))
    end
  end
  defp _mem() do
    :erlang.memory(:total)
  end
  defp _lookup_host(host) do
    case :inet.gethostbyname(String.to_charlist(host)) do
      {:ok, {:hostent, _, _, _, _, addrs}} ->
        ips = Enum.map(addrs, &:inet.ntoa/1)
        [ips, nil]
      {:error, reason} ->
        [nil, reason]
    end
  end
  defp _clamp_slice(n, start, stop) do
    start = if start < 0, do: start + n, else: start
    stop = if stop < 0, do: stop + n, else: stop
    start = max(min(start, n), 0)
    stop = min(max(stop, start), n)
    {start, stop}
  end
  defp _slice(base, start, len) do
    cond do
      is_binary(base) ->
        chars = String.graphemes(base)
        n = length(chars)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(chars, s, e - s) |> Enum.join("")
      true ->
        n = length(base)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(base, s, e - s)
    end
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  defp _bigrat(v) do
    _bigrat(v, 1)
  end
  defp _bigrat(n, d) do
    g = Integer.gcd(n, d)
    n = div(n, g)
    d = div(d, g)
    if d < 0 do
      {-n, -d}
    else
      {n, d}
    end
  end
  defp _bigrat_add(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd + ad * bn, ad * bd)
  end
  defp _bigrat_sub(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd - ad * bn, ad * bd)
  end
  defp _bigrat_mul(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bn, ad * bd)
  end
  defp _bigrat_div(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd, ad * bn)
  end
  defp _bigrat_neg(a) do
    {n, d} = a
    {-n, d}
  end
  def pixelFromRgb(c) do
    try do
      r = rem((trunc((div(c, 65536)))), 256)
      g = rem((trunc((div(c, 256)))), 256)
      b = rem(c, 256)
      throw {:return, %{R: r, G: g, B: b}}
    catch
      {:return, val} -> val
    end
  end
  def rgbFromPixel(p) do
    try do
      throw {:return, p[:R] * 65536 + p[:G] * 256 + p[:B]}
    catch
      {:return, val} -> val
    end
  end
  def newbitmap(x, y) do
    try do
      data = []
      row = 0
      while_fun = fn while_fun, data, row ->
        if row < y do
          r = []
          col = 0
          while_fun_2 = fn while_fun_2, col, r ->
            if col < x do
              r = (r ++ [%{R: 0, G: 0, B: 0}])
              col = col + 1
              while_fun_2.(while_fun_2, col, r)
            else
              {col, r}
            end
          end
          {col, r} = try do
              while_fun_2.(while_fun_2, col, r)
            catch
              :break -> {col, r}
            end

          data = (data ++ [r])
          row = row + 1
          while_fun.(while_fun, data, row)
        else
          {data, row}
        end
      end
      {data, row} = try do
          while_fun.(while_fun, data, row)
        catch
          :break -> {data, row}
        end

      throw {:return, %{cols: x, rows: y, px: data}}
    catch
      {:return, val} -> val
    end
  end
  def fillrgb(b, c) do
    try do
      y = 0
      p = Main.pixelFromRgb(c)
      while_fun_3 = fn while_fun_3, b, y ->
        if y < b.rows do
          x = 0
          while_fun_4 = fn while_fun_4, b, x ->
            if x < b.cols do
              px = b.px
              row = Enum.at(px, y)
              row = List.replace_at(row, x, p)
              px = List.replace_at(px, y, row)
              b = Map.put(b, :px, px)
              x = x + 1
              while_fun_4.(while_fun_4, b, x)
            else
              {b, x}
            end
          end
          {b, x} = try do
              while_fun_4.(while_fun_4, b, x)
            catch
              :break -> {b, x}
            end

          y = y + 1
          while_fun_3.(while_fun_3, b, y)
        else
          {b, y}
        end
      end
      {b, y} = try do
          while_fun_3.(while_fun_3, b, y)
        catch
          :break -> {b, y}
        end

    catch
      {:return, val} -> val
    end
  end
  def setpxrgb(b, x, y, c) do
    try do
      if x < 0 || x >= b.cols || y < 0 || y >= b.rows do
        throw {:return, false}
      end
      px = b.px
      row = Enum.at(px, y)
      row = List.replace_at(row, x, Main.pixelFromRgb(c))
      px = List.replace_at(px, y, row)
      b = Map.put(b, :px, px)
      throw {:return, true}
    catch
      {:return, val} -> val
    end
  end
  def nextRand(seed) do
    try do
      throw {:return, rem((seed * 1664525 + 1013904223), 2147483648)}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      bm = newbitmap(400, 300)
      fillrgb(bm, 12615744)
      seed = _now()
      i = 0
      while_fun_5 = fn while_fun_5, i, seed ->
        if i < 2000 do
          seed = Main.nextRand(seed)
          x = rem(seed, 400)
          seed = Main.nextRand(seed)
          y = rem(seed, 300)
          setpxrgb(bm, x, y, 8405024)
          i = i + 1
          while_fun_5.(while_fun_5, i, seed)
        else
          {i, seed}
        end
      end
      {i, seed} = try do
          while_fun_5.(while_fun_5, i, seed)
        catch
          :break -> {i, seed}
        end

      x = 0
      while_fun_6 = fn while_fun_6, x ->
        if x < 400 do
          y = 240
          while_fun_7 = fn while_fun_7, y ->
            if y < 245 do
              setpxrgb(bm, x, y, 8405024)
              y = y + 1
              while_fun_7.(while_fun_7, y)
            else
              y
            end
          end
          y = try do
              while_fun_7.(while_fun_7, y)
            catch
              :break -> y
            end

          y = 260
          while_fun_8 = fn while_fun_8, y ->
            if y < 265 do
              setpxrgb(bm, x, y, 8405024)
              y = y + 1
              while_fun_8.(while_fun_8, y)
            else
              y
            end
          end
          y = try do
              while_fun_8.(while_fun_8, y)
            catch
              :break -> y
            end

          x = x + 1
          while_fun_6.(while_fun_6, x)
        else
          x
        end
      end
      x = try do
          while_fun_6.(while_fun_6, x)
        catch
          :break -> x
        end

      y = 0
      while_fun_9 = fn while_fun_9, y ->
        if y < 300 do
          x = 80
          while_fun_10 = fn while_fun_10, x ->
            if x < 85 do
              setpxrgb(bm, x, y, 8405024)
              x = x + 1
              while_fun_10.(while_fun_10, x)
            else
              x
            end
          end
          x = try do
              while_fun_10.(while_fun_10, x)
            catch
              :break -> x
            end

          x = 95
          while_fun_11 = fn while_fun_11, x ->
            if x < 100 do
              setpxrgb(bm, x, y, 8405024)
              x = x + 1
              while_fun_11.(while_fun_11, x)
            else
              x
            end
          end
          x = try do
              while_fun_11.(while_fun_11, x)
            catch
              :break -> x
            end

          y = y + 1
          while_fun_9.(while_fun_9, y)
        else
          y
        end
      end
      y = try do
          while_fun_9.(while_fun_9, y)
        catch
          :break -> y
        end

    catch
      {:return, val} -> val
    end
  end
  def bench_main() do
    mem_start = _mem()
    t_start = _now()
    main()
    duration_us = div(_now() - t_start, 1000)
    mem_diff = abs(_mem() - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
