# Code generated by Mochi transpiler 2025-08-04 11:46 +0700
defmodule Main do
  defp _now() do
    seeded = Process.get(:_now_seeded, false)
    seed = Process.get(:_now_seed, 0)
    if !seeded do
      case System.get_env("MOCHI_NOW_SEED") do
        nil -> :ok
        s ->
          case Integer.parse(s) do
            {v, ""} ->
              Process.put(:_now_seed, v)
              Process.put(:_now_seeded, true)
              seed = v
              seeded = true
            _ -> :ok
          end
      end
    end
    if seeded do
      seed = rem(seed * 1664525 + 1013904223, 2147483647)
      Process.put(:_now_seed, seed)
      abs(seed)
    else
      System.unique_integer([:positive, :monotonic])
    end
  end
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  defp _lookup_host(host) do
    case :inet.gethostbyname(String.to_charlist(host)) do
      {:ok, {:hostent, _, _, _, _, addrs}} ->
        ips = Enum.map(addrs, &:inet.ntoa/1)
        [ips, nil]
      {:error, reason} ->
        [nil, reason]
    end
  end
  defp _clamp_slice(n, start, stop) do
    start = if start < 0, do: start + n, else: start
    stop = if stop < 0, do: stop + n, else: stop
    start = max(min(start, n), 0)
    stop = min(max(stop, start), n)
    {start, stop}
  end
  defp _slice(base, start, len) do
    cond do
      is_binary(base) ->
        chars = String.graphemes(base)
        n = length(chars)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(chars, s, e - s) |> Enum.join("")
      true ->
        n = length(base)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(base, s, e - s)
    end
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  defp _bigrat(v) do
    _bigrat(v, 1)
  end
  defp _bigrat(n, d) do
    n = trunc(n)
    d = trunc(d)
    g = Integer.gcd(n, d)
    n = div(n, g)
    d = div(d, g)
    if d < 0 do
      {-n, -d}
    else
      {n, d}
    end
  end
  defp _bigrat_add(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd + ad * bn, ad * bd)
  end
  defp _bigrat_sub(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd - ad * bn, ad * bd)
  end
  defp _bigrat_mul(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bn, ad * bd)
  end
  defp _bigrat_div(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd, ad * bn)
  end
  defp _bigrat_neg(a) do
    {n, d} = a
    {-n, d}
  end
  defp _sha256(bs) do
    bin = :erlang.list_to_binary(bs)
    :crypto.hash(:sha256, bin) |> :erlang.binary_to_list()
  end
  defp _getenv(name) do
    System.get_env(name)
  end
  defp _environ() do
    System.get_env() |> Enum.map(fn {k, v} -> "#{k}=#{v}" end)
  end
  def indexOf3(s, ch, start) do
    try do
      i = start
      while_fun = fn while_fun, i ->
        if i < _len(s) do
          if _slice(s, i, i + 1 - (i)) == ch do
            throw {:return, i}
          end
          i = i + 1
          while_fun.(while_fun, i)
        else
          i
        end
      end
      i = try do
          while_fun.(while_fun, i)
        catch
          {:break, i} -> i
        end

      throw {:return, -1}
    catch
      {:return, val} -> val
    end
  end
  def ord(ch) do
    try do
      digits = "0123456789"
      idx = indexOf3(digits, ch, 0)
      if idx >= 0 do
        throw {:return, 48 + idx}
      end
      if ch == "X" do
        throw {:return, 88}
      end
      if ch == "é" do
        throw {:return, 233}
      end
      if ch == "😍" do
        throw {:return, 128525}
      end
      if ch == "🐡" do
        throw {:return, 128033}
      end
      throw {:return, 0}
    catch
      {:return, val} -> val
    end
  end
  def toHex(n) do
    try do
      digits = "0123456789ABCDEF"
      if n == 0 do
        throw {:return, "0"}
      end
      v = n
      out = ""
      while_fun_2 = fn while_fun_2, out, v ->
        if v > 0 do
          d = rem(v, 16)
          out = (_slice(digits, d, (d + 1) - d) <> out)
          v = div(v, 16)
          while_fun_2.(while_fun_2, out, v)
        else
          {out, v}
        end
      end
      {out, v} = try do
          while_fun_2.(while_fun_2, out, v)
        catch
          {:break, {out, v}} -> {out, v}
        end

      throw {:return, out}
    catch
      {:return, val} -> val
    end
  end
  def analyze(s) do
    try do
      le = _len(s)
      IO.puts((((("Analyzing \"" <> s) <> "\" which has a length of ") <> Kernel.to_string(le)) <> ":"))
      if le > 1 do
        i = 0
        while_fun_3 = fn while_fun_3, i ->
          if i < le - 1 do
            j = i + 1
            while_fun_4 = fn while_fun_4, j ->
              if j < le do
                if _slice(s, j, j + 1 - (j)) == _slice(s, i, i + 1 - (i)) do
                  ch = _slice(s, i, i + 1 - (i))
                  IO.puts("  Not all characters in the string are unique.")
                  IO.puts((((((((("  '" <> ch) <> "' (0x") <> String.downcase(toHex(ord(ch)))) <> ") is duplicated at positions ") <> Kernel.to_string(i + 1)) <> " and ") <> Kernel.to_string(j + 1)) <> ".\n"))
                  throw {:return, nil}
                end
                j = j + 1
                while_fun_4.(while_fun_4, j)
              else
                j
              end
            end
            j = try do
                while_fun_4.(while_fun_4, j)
              catch
                {:break, j} -> j
              end

            i = i + 1
            while_fun_3.(while_fun_3, i)
          else
            i
          end
        end
        i = try do
            while_fun_3.(while_fun_3, i)
          catch
            {:break, i} -> i
          end

      end
      IO.puts("  All characters in the string are unique.\n")
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      strings = ["", ".", "abcABC", "XYZ ZYX", "1234567890ABCDEFGHIJKLMN0PQRSTUVWXYZ", "01234567890ABCDEFGHIJKLMN0PQRSTUVWXYZ0X", "hétérogénéité", "🎆🎃🎇🎈", "😍😀🙌💃😍🙌", "🐠🐟🐡🦈🐬🐳🐋🐡"]
      i = 0
      while_fun_5 = fn while_fun_5, i ->
        if i < Enum.count(strings) do
          analyze(Enum.at(strings, i))
          i = i + 1
          while_fun_5.(while_fun_5, i)
        else
          i
        end
      end
      i = try do
          while_fun_5.(while_fun_5, i)
        catch
          {:break, i} -> i
        end

    catch
      {:return, val} -> val
    end
  end
  def bench_main() do
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    main()
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
