# Code generated by Mochi transpiler 2025-08-04 22:33 +0700
defmodule Main do
  def _now() do
    seeded = Process.get(:_now_seeded, false)
    seed = Process.get(:_now_seed, 0)
    if !seeded do
      case System.get_env("MOCHI_NOW_SEED") do
        nil -> :ok
        s ->
          case Integer.parse(s) do
            {v, ""} ->
              Process.put(:_now_seed, v)
              Process.put(:_now_seeded, true)
              seed = v
              seeded = true
            _ -> :ok
          end
      end
    end
    if seeded do
      seed = rem(seed * 1664525 + 1013904223, 2147483647)
      Process.put(:_now_seed, seed)
      abs(seed)
    else
      System.unique_integer([:positive, :monotonic])
    end
  end
  defp _bench_now() do
    System.monotonic_time(:microsecond)
  end
  defp _mem() do
    :erlang.process_info(self(), :memory) |> elem(1)
  end
  defp _lookup_host(host) do
    case :inet.gethostbyname(String.to_charlist(host)) do
      {:ok, {:hostent, _, _, _, _, addrs}} ->
        ips = Enum.map(addrs, &:inet.ntoa/1)
        [ips, nil]
      {:error, reason} ->
        [nil, reason]
    end
  end
  defp _clamp_slice(n, start, stop) do
    start = if start < 0, do: start + n, else: start
    stop = if stop < 0, do: stop + n, else: stop
    start = max(min(start, n), 0)
    stop = min(max(stop, start), n)
    {start, stop}
  end
  defp _slice(base, start, len) do
    cond do
      is_binary(base) ->
        chars = String.graphemes(base)
        n = length(chars)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(chars, s, e - s) |> Enum.join("")
      true ->
        n = length(base)
        {s, e} = _clamp_slice(n, start, start + len)
        Enum.slice(base, s, e - s)
    end
  end
  defp _len(x) do
    cond do
      x == nil -> 0
      is_binary(x) -> String.length(x)
      true -> length(x)
    end
  end
  defp _bigrat(v) do
    _bigrat(v, 1)
  end
  defp _bigrat(n, d) do
    n = trunc(n)
    d = trunc(d)
    g = Integer.gcd(n, d)
    n = div(n, g)
    d = div(d, g)
    if d < 0 do
      {-n, -d}
    else
      {n, d}
    end
  end
  defp _bigrat_add(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd + ad * bn, ad * bd)
  end
  defp _bigrat_sub(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd - ad * bn, ad * bd)
  end
  defp _bigrat_mul(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bn, ad * bd)
  end
  defp _bigrat_div(a, b) do
    {an, ad} = a
    {bn, bd} = b
    _bigrat(an * bd, ad * bn)
  end
  defp _bigrat_neg(a) do
    {n, d} = a
    {-n, d}
  end
  defp _sha256(bs) do
    bin = :erlang.list_to_binary(bs)
    :crypto.hash(:sha256, bin) |> :erlang.binary_to_list()
  end
  defp _getenv(name) do
    System.get_env(name)
  end
  defp _environ() do
    System.get_env() |> Enum.map(fn {k, v} -> "#{k}=#{v}" end)
  end
  defp _getoutput(cmd) do
    {out, 0} = System.cmd("sh", ["-c", cmd])
    String.trim(out)
  end
  def _mod(x, m) do
    try do
      throw {:return, x - ((fn v -> if is_binary(v), do: String.to_integer(v), else: trunc(v) end).((x / m))) * m}
    catch
      {:return, val} -> val
    end
  end
  def _sin(x) do
    try do
      y = _mod(x + Process.get(:pi), 2.0 * Process.get(:pi)) - Process.get(:pi)
      y2 = y * y
      y3 = y2 * y
      y5 = y3 * y2
      y7 = y5 * y2
      throw {:return, y - y3 / 6.0 + y5 / 120.0 - y7 / 5040.0}
    catch
      {:return, val} -> val
    end
  end
  def _cos(x) do
    try do
      y = _mod(x + Process.get(:pi), 2.0 * Process.get(:pi)) - Process.get(:pi)
      y2 = y * y
      y4 = y2 * y2
      y6 = y4 * y2
      throw {:return, 1.0 - y2 / 2.0 + y4 / 24.0 - y6 / 720.0}
    catch
      {:return, val} -> val
    end
  end
  def clearGrid() do
    try do
      g = []
      y = 0
      while_fun = fn while_fun, g, y ->
        if y < Process.get(:height) do
          row = []
          x = 0
          while_fun_2 = fn while_fun_2, row, x ->
            if x < Process.get(:width) do
              row = (row ++ [" "])
              x = x + 1
              while_fun_2.(while_fun_2, row, x)
            else
              {row, x}
            end
          end
          {row, x} = try do
              while_fun_2.(while_fun_2, row, x)
            catch
              {:break, {row, x}} -> {row, x}
            end

          g = (g ++ [row])
          y = y + 1
          while_fun.(while_fun, g, y)
        else
          {g, y}
        end
      end
      {g, y} = try do
          while_fun.(while_fun, g, y)
        catch
          {:break, {g, y}} -> {g, y}
        end

      throw {:return, g}
    catch
      {:return, val} -> val
    end
  end
  def drawPoint(g, x, y) do
    try do
      {g} = if x >= 0 && x < Process.get(:width) && y >= 0 && y < Process.get(:height) do
        row = Enum.at(g, y)
        row = List.replace_at(row, x, "#")
        g = List.replace_at(g, y, row)
        {g}
      else
        {g}
      end
    catch
      {:return, val} -> val
    end
  end
  def bresenham(x0, y0, x1, y1, g) do
    try do
      dx = x1 - x0
      {dx} = if dx < 0 do
        dx = -dx
        {dx}
      else
        {dx}
      end
      dy = y1 - y0
      {dy} = if dy < 0 do
        dy = -dy
        {dy}
      else
        {dy}
      end
      sx = -1
      {sx} = if x0 < x1 do
        sx = 1
        {sx}
      else
        {sx}
      end
      sy = -1
      {sy} = if y0 < y1 do
        sy = 1
        {sy}
      else
        {sy}
      end
      err = dx - dy
      while_fun_3 = fn while_fun_3, err, x0, y0 ->
        if true do
          drawPoint(g, x0, y0)
          if x0 == x1 && y0 == y1 do
            throw {:break, {err, x0, y0}}
          end
          e2 = 2 * err
          {err, x0} = if e2 > (-dy) do
            err = err - dy
            x0 = x0 + sx
            {err, x0}
          else
            {err, x0}
          end
          {err, y0} = if e2 < dx do
            err = err + dx
            y0 = y0 + sy
            {err, y0}
          else
            {err, y0}
          end
          while_fun_3.(while_fun_3, err, x0, y0)
        else
          {err, x0, y0}
        end
      end
      {err, x0, y0} = try do
          while_fun_3.(while_fun_3, err, x0, y0)
        catch
          {:break, {err, x0, y0}} -> {err, x0, y0}
        end

    catch
      {:return, val} -> val
    end
  end
  def ftree(g, x, y, dist, dir, d) do
    try do
      rad = dir * Process.get(:pi) / 180.0
      x2 = x + dist * _sin(rad)
      y2 = y - dist * _cos(rad)
      bresenham((fn v -> if is_binary(v), do: String.to_integer(v), else: trunc(v) end).(x), (fn v -> if is_binary(v), do: String.to_integer(v), else: trunc(v) end).(y), (fn v -> if is_binary(v), do: String.to_integer(v), else: trunc(v) end).(x2), (fn v -> if is_binary(v), do: String.to_integer(v), else: trunc(v) end).(y2), g)
      if d > 0 do
        ftree(g, x2, y2, dist * Process.get(:frac), dir - Process.get(:angle), d - 1)
        ftree(g, x2, y2, dist * Process.get(:frac), dir + Process.get(:angle), d - 1)
      end
    catch
      {:return, val} -> val
    end
  end
  def render(g) do
    try do
      out = ""
      y = 0
      while_fun_4 = fn while_fun_4, out, y ->
        if y < Process.get(:height) do
          line = ""
          x = 0
          while_fun_5 = fn while_fun_5, line, x ->
            if x < Process.get(:width) do
              line = (line <> Enum.at(Enum.at(g, y), x))
              x = x + 1
              while_fun_5.(while_fun_5, line, x)
            else
              {line, x}
            end
          end
          {line, x} = try do
              while_fun_5.(while_fun_5, line, x)
            catch
              {:break, {line, x}} -> {line, x}
            end

          out = (out <> line)
          {out} = if y < Process.get(:height) - 1 do
            out = (out <> "\n")
            {out}
          else
            {out}
          end
          y = y + 1
          while_fun_4.(while_fun_4, out, y)
        else
          {out, y}
        end
      end
      {out, y} = try do
          while_fun_4.(while_fun_4, out, y)
        catch
          {:break, {out, y}} -> {out, y}
        end

      throw {:return, out}
    catch
      {:return, val} -> val
    end
  end
  def main() do
    try do
      grid = clearGrid()
      ftree(grid, (div(Process.get(:width), 2)), (Process.get(:height) - 1), Process.get(:length), 0.0, Process.get(:depth))
      IO.puts(Kernel.inspect(render(grid)))
    catch
      {:return, val} -> val
    end
  end
  Process.put(:pi, 3.141592653589793)
  Process.put(:width, 80)
  Process.put(:height, 40)
  Process.put(:depth, 6)
  Process.put(:angle, 12.0)
  Process.put(:length, 12.0)
  Process.put(:frac, 0.8)
  def bench_main() do
    Process.put(:pi, 3.141592653589793)
    Process.put(:width, 80)
    Process.put(:height, 40)
    Process.put(:depth, 6)
    Process.put(:angle, 12.0)
    Process.put(:length, 12.0)
    Process.put(:frac, 0.8)
    :erlang.garbage_collect()
    mem_start = _mem()
    t_start = _bench_now()
    main()
    mem_end = _mem()
    duration_us = max(_bench_now() - t_start, 1)
    :erlang.garbage_collect()
    mem_diff = abs(mem_end - mem_start)
    IO.puts("{\n  \"duration_us\": #{duration_us},\n  \"memory_bytes\": #{mem_diff},\n  \"name\": \"main\"\n}")
  end
end
Main.bench_main()
