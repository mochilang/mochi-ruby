run: exit status 1
    warning: variable "seed" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 14 │               seed = v
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:14:15: Main._now/0

    warning: variable "seeded" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
    │
 15 │               seeded = true
    │               ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:15:15: Main._now/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 132 │       {i, x} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:132:8: Main.makeInf/0

     warning: variable "i" is unused (there is a variable with the same name in the context, use the pin operator (^) to match on it or prefix this variable with underscore if it is not meant to be used)
     │
 156 │       {i, x} = try do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:156:8: Main.makeMax/0

    warning: function _slice/3 is unused
    │
 50 │   defp _slice(base, start, len) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:50:8: Main (module)

     warning: function _sha256/1 is unused
     │
 109 │   defp _sha256(bs) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:109:8: Main (module)

    warning: function _lookup_host/1 is unused
    │
 34 │   defp _lookup_host(host) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:34:8: Main (module)

    warning: function _len/1 is unused
    │
 63 │   defp _len(x) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:63:8: Main (module)

     warning: function _getenv/1 is unused
     │
 113 │   defp _getenv(name) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:113:8: Main (module)

     warning: function _environ/0 is unused
     │
 116 │   defp _environ() do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:116:8: Main (module)

    warning: function _clamp_slice/3 is unused
    │
 43 │   defp _clamp_slice(n, start, stop) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:43:8: Main (module)

    warning: function _bigrat_sub/2 is unused
    │
 90 │   defp _bigrat_sub(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:90:8: Main (module)

     warning: function _bigrat_neg/1 is unused
     │
 105 │   defp _bigrat_neg(a) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:105:8: Main (module)

    warning: function _bigrat_mul/2 is unused
    │
 95 │   defp _bigrat_mul(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:95:8: Main (module)

     warning: function _bigrat_div/2 is unused
     │
 100 │   defp _bigrat_div(a, b) do
     │        ~
     │
     └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:100:8: Main (module)

    warning: function _bigrat_add/2 is unused
    │
 85 │   defp _bigrat_add(a, b) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:85:8: Main (module)

    warning: function _bigrat/2 is unused
    │
 73 │   defp _bigrat(n, d) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:73:8: Main (module)

    warning: function _bigrat/1 is unused
    │
 70 │   defp _bigrat(v) do
    │        ~
    │
    └─ /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:70:8: Main (module)

** (ArithmeticError) bad argument in arithmetic expression
    /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:125: anonymous fn/3 in Main.makeInf/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:133: Main.makeInf/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:232: Main.main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:257: Main.bench_main/0
    /workspace/mochi/tests/rosetta/transpiler/Elixir/extreme-floating-point-values.exs:265: (file)
