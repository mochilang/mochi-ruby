# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-27 22:07 +0700
import sys
sys.set_int_max_str_digits(0)

def isPrime(n):
    if n < 2:
        return False
    i = 2
    while i * i <= n:
        if n % i == 0:
            return False
        i = i + 1
    return True
def search(base, prefix, depth, limit, max):
    best = max
    d = 1
    while d < base:
        val = prefix * base + d
        if isPrime(val):
            if val > best:
                best = val
            if depth + 1 < limit:
                best = search(base, val, depth + 1, limit, best)
        d = d + 1
    return best
def largest(base):
    return search(base, 0, 0, 6, 0)
def main():
    b = 3
    while b <= 10:
        print(str(b) + ": " + str(largest(b)))
        b = b + 1
main()
