# Code generated by Mochi transpiler.
# Version 0.10.50, generated on 2025-07-30 21:05 +0700
import sys
sys.set_int_max_str_digits(0)

FASTA = ">Rosetta_Example_1\n" + "THERECANBENOSPACE\n" + ">Rosetta_Example_2\n" + "THERECANBESEVERAL\n" + "LINESBUTTHEYALLMUST\n" + "BECONCATENATED"
def splitLines(s):
    lines = []
    start = 0
    i = 0
    while i < len(s):
        if s[i:i + 1] == "\n":
            lines = lines + [s[start:i]]
            i = i + 1
            start = i
        else:
            i = i + 1
    lines = lines + [s[start:len(s)]]
    return lines
def parseFasta(text):
    key = ""
    val = ""
    out = []
    for line in splitLines(text):
        if line == "":
            continue
        if line[0:1] == ">":
            if key != "":
                out = out + [key + ": " + val]
            hdr = line[1:len(line)]
            idx = 0
            while idx < len(hdr) and hdr[idx:idx + 1] != " ":
                idx = idx + 1
            key = hdr[0:idx]
            val = ""
        else:
            if key == "":
                print("missing header")
                return []
            val = val + line
    if key != "":
        out = out + [key + ": " + val]
    return out
def main():
    res = parseFasta(FASTA)
    for line in res:
        print(line)
main()
