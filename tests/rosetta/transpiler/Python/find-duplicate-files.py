# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-27 21:41 +0700
import sys
sys.set_int_max_str_digits(0)

def findDuplicates(fs, paths):
    seen = {}
    dups = []
    for path in paths:
        content = fs[path]
        if content in seen:
            dups = dups + [[seen[content], path]]
        else:
            seen[content] = path
    return dups
def main():
    fs = {"a.txt": "hello", "b.txt": "world", "c.txt": "hello", "d.txt": "foo", "e.txt": "world"}
    paths = ["a.txt", "b.txt", "c.txt", "d.txt", "e.txt"]
    dups = findDuplicates(fs, paths)
    for pair in dups:
        print(pair[0] + " <==> " + pair[1])
main()
