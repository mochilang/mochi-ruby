# Code generated by Mochi transpiler.
# Version 0.10.41, generated on 2025-07-26 19:54 +0700
import sys
sys.set_int_max_str_digits(0)

def repeat(ch, n):
    s = ""
    i = 0
    while i < n:
        s = s + ch
        i = i + 1
    return s
def cubLine(n, dx, dy, cde):
    line = repeat(" ", n + 1) + "".join(cde[0:1])
    d = 9 * dx - 1
    while d > 0:
        line = line + "".join(cde[1:2])
        d = d - 1
    line = line + "".join(cde[0:1])
    line = line + repeat(" ", dy) + "".join(cde[2:])
    print(line)
def cuboid(dx, dy, dz):
    print("cuboid " + str(dx) + " " + str(dy) + " " + str(dz) + ":")
    cubLine(dy + 1, dx, 0, "+-")
    i = 1
    while i <= dy:
        cubLine(dy - i + 1, dx, i - 1, "/ |")
        i = i + 1
    cubLine(0, dx, dy, "+-|")
    j = 4 * dz - dy - 2
    while j > 0:
        cubLine(0, dx, dy, "| |")
        j = j - 1
    cubLine(0, dx, dy, "| +")
    i = 1
    while i <= dy:
        cubLine(0, dx, dy - i, "| /")
        i = i + 1
    cubLine(0, dx, 0, "+-\n")
cuboid(2, 3, 4)
print("")
cuboid(1, 1, 1)
print("")
cuboid(6, 2, 1)
