# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-28 00:50 +0700
import json
import os
import resource
import time

import sys
sys.set_int_max_str_digits(0)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

def Node(data):
    return {"Data": data, "Balance": 0, "Link": [None, None]}
def getLink(n, dir):
    return (n.get("Link"))[dir]
def setLink(n, dir, v):
    links = n.get("Link")
    links[dir] = v
    n["Link"] = links
def opp(dir):
    return 1 - dir
def single(root, dir):
    tmp = getLink(root, opp(dir))
    setLink(root, opp(dir), getLink(tmp, dir))
    setLink(tmp, dir, root)
    return tmp
def double(root, dir):
    tmp = getLink(getLink(root, opp(dir)), dir)
    setLink(getLink(root, opp(dir)), dir, getLink(tmp, opp(dir)))
    setLink(tmp, opp(dir), getLink(root, opp(dir)))
    setLink(root, opp(dir), tmp)
    tmp = getLink(root, opp(dir))
    setLink(root, opp(dir), getLink(tmp, dir))
    setLink(tmp, dir, root)
    return tmp
def adjustBalance(root, dir, bal):
    n = getLink(root, dir)
    nn = getLink(n, opp(dir))
    if nn.get("Balance") == 0:
        root["Balance"] = 0
        n["Balance"] = 0
    else:
        if nn.get("Balance") == bal:
            root["Balance"] = -bal
            n["Balance"] = 0
        else:
            root["Balance"] = 0
            n["Balance"] = bal
    nn["Balance"] = 0
def insertBalance(root, dir):
    n = getLink(root, dir)
    bal = 2 * dir - 1
    if n.get("Balance") == bal:
        root["Balance"] = 0
        n["Balance"] = 0
        return single(root, opp(dir))
    adjustBalance(root, dir, bal)
    return double(root, opp(dir))
def insertR(root, data):
    if root == None:
        return {"node": Node(data), "done": False}
    node = root
    dir = 0
    if (int(node.get("Data"))) < data:
        dir = 1
    r = insertR(getLink(node, dir), data)
    setLink(node, dir, r.get("node"))
    if r.get("done"):
        return {"node": node, "done": True}
    node["Balance"] = (int(node.get("Balance"))) + (2 * dir - 1)
    if node.get("Balance") == 0:
        return {"node": node, "done": True}
    if node.get("Balance") == 1 or node.get("Balance") == (-1):
        return {"node": node, "done": False}
    return {"node": insertBalance(node, dir), "done": True}
def Insert(tree, data):
    r = insertR(tree, data)
    return r.get("node")
def removeBalance(root, dir):
    n = getLink(root, opp(dir))
    bal = 2 * dir - 1
    if n.get("Balance") == (-bal):
        root["Balance"] = 0
        n["Balance"] = 0
        return {"node": single(root, dir), "done": False}
    if n.get("Balance") == bal:
        adjustBalance(root, opp(dir), (-bal))
        return {"node": double(root, dir), "done": False}
    root["Balance"] = -bal
    n["Balance"] = bal
    return {"node": single(root, dir), "done": True}
def removeR(root, data):
    if root == None:
        return {"node": None, "done": False}
    node = root
    if (int(node.get("Data"))) == data:
        if getLink(node, 0) == None:
            return {"node": getLink(node, 1), "done": False}
        if getLink(node, 1) == None:
            return {"node": getLink(node, 0), "done": False}
        heir = getLink(node, 0)
        while getLink(heir, 1) != None:
            heir = getLink(heir, 1)
        node["Data"] = heir.get("Data")
        data = int(heir.get("Data"))
    dir = 0
    if (int(node.get("Data"))) < data:
        dir = 1
    r = removeR(getLink(node, dir), data)
    setLink(node, dir, r.get("node"))
    if r.get("done"):
        return {"node": node, "done": True}
    node["Balance"] = (int(node.get("Balance"))) + 1 - 2 * dir
    if node.get("Balance") == 1 or node.get("Balance") == (-1):
        return {"node": node, "done": True}
    if node.get("Balance") == 0:
        return {"node": node, "done": False}
    return removeBalance(node, dir)
def Remove(tree, data):
    r = removeR(tree, data)
    return r.get("node")
def indentStr(n):
    s = ""
    i = 0
    while i < n:
        s = s + " "
        i = i + 1
    return s
def dumpNode(node, indent, comma):
    sp = indentStr(indent)
    if node == None:
        line = sp + "null"
        if comma:
            line = line + ","
        print(line)
    else:
        print(sp + "{")
        print(indentStr(indent + 3) + "\"Data\": " + str(node.get("Data")) + ",")
        print(indentStr(indent + 3) + "\"Balance\": " + str(node.get("Balance")) + ",")
        print(indentStr(indent + 3) + "\"Link\": [")
        dumpNode(getLink(node, 0), indent + 6, True)
        dumpNode(getLink(node, 1), indent + 6, False)
        print(indentStr(indent + 3) + "]")
        end = sp + "}"
        if comma:
            end = end + ","
        print(end)
def dump(node, indent):
    dumpNode(node, indent, False)
def main():
    _bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    _bench_start = _now()
    tree = None
    print("Empty tree:")
    dump(tree, 0)
    print("")
    print("Insert test:")
    tree = Insert(tree, 3)
    tree = Insert(tree, 1)
    tree = Insert(tree, 4)
    tree = Insert(tree, 1)
    tree = Insert(tree, 5)
    dump(tree, 0)
    print("")
    print("Remove test:")
    tree = Remove(tree, 3)
    tree = Remove(tree, 1)
    t = tree
    t["Balance"] = 0
    tree = t
    dump(tree, 0)
    _bench_end = _now()
    _bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
    print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": _bench_mem_end*1024, "name": "main"}, indent=2))
main()
