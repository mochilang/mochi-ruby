# Code generated by Mochi transpiler.
# Version 0.10.41, generated on 2025-07-26 19:54 +0700
import os
import time

import sys
sys.set_int_max_str_digits(0)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

def pow2(exp):
    r = 1
    i = 0
    while i < exp:
        r = r * 2
        i = i + 1
    return r
def bin(n, digits):
    s = ""
    i = digits - 1
    while i >= 0:
        p = pow2(i)
        if n >= p:
            s = s + "x"
            n = n - p
        else:
            s = s + " "
        if i > 0:
            s = s + "|"
        i = i - 1
    return s
t = _now() / 1000000000
sec = t % 60
mins = t / 60
min = mins % 60
hour = (mins / 60) % 24
print(bin(hour, 8))
print("")
print(bin(min, 8))
print("")
xs = ""
i = 0
while i < sec:
    xs = xs + "x"
    i = i + 1
out = ""
j = 0
while j < len(xs):
    out = out + "".join(xs[j:j + 1])
    if (j + 1) % 5 == 0 and j + 1 < len(xs):
        out = out + "|"
    j = j + 1
print(out)
