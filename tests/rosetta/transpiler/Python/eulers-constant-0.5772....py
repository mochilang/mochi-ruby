# Code generated by Mochi transpiler.
# Version 0.10.50, generated on 2025-07-31 00:19 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))

def ln(x):
    k = 0.0
    v = x
    while v >= 2.0:
        v = v / 2.0
        k = k + 1.0
    while v < 1.0:
        v = v * 2.0
        k = k - 1.0
    z = (v - 1.0) / (v + 1.0)
    zpow = z
    sum = z
    i = 3
    while i <= 9:
        zpow = zpow * z * z
        sum = sum + zpow / (float(i))
        i = i + 2
    ln2 = 0.6931471805599453
    return (k * ln2) + 2.0 * sum
def harmonic(n):
    sum = 0.0
    i = 1
    while i <= n:
        sum = sum + 1.0 / (float(i))
        i = i + 1
    return sum
def main():
    n = 100000
    gamma = harmonic(n) - ln(float(n))
    print(str(gamma))
main()
