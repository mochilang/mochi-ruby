# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-28 10:14 +0700
import json
import os
import resource
import time

import sys
sys.set_int_max_str_digits(0)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

_bench_mem_start = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
_bench_start = _now()
seed = 1
def rnd():
    global seed
    seed = (seed * 214013 + 2531011) % 2147483648
    return seed // 65536
def deal(game):
    global seed
    seed = game
    deck = []
    i = 0
    while i < 52:
        deck = deck + [51 - i]
        i = i + 1
    i = 0
    while i < 51:
        j = 51 - (rnd() % (52 - i))
        tmp = deck[i]
        deck[i] = deck[j]
        deck[j] = tmp
        i = i + 1
    return deck
suits = "CDHS"
nums = "A23456789TJQK"
def show(cards):
    i = 0
    while i < len(cards):
        c = cards[i]
        sys.stdout.write(" " + "".join(nums[c // 4:c // 4 + 1]) + "".join(suits[c % 4:c % 4 + 1]))
        if (i + 1) % 8 == 0 or i + 1 == len(cards):
            print("")
        i = i + 1
print("")
print("Game #1")
show(deal(1))
print("")
print("Game #617")
show(deal(617))
_bench_end = _now()
_bench_mem_end = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss
print(json.dumps({"duration_us": (_bench_end - _bench_start)//1000, "memory_bytes": _bench_mem_end*1024, "name": "main"}, indent=2))
