# Code generated by Mochi transpiler.
# Version 0.10.57, generated on 2025-08-04 16:02 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))

def absInt(n):
    if n < 0:
        return -n
    return n
def main():
    b = 1
    while b <= 5:
        if b != 5:
            c = 1
            while c <= 5:
                if c != 1 and c != b:
                    f = 1
                    while f <= 5:
                        if f != 1 and f != 5 and f != b and f != c and absInt(f - c) > 1:
                            m = 1
                            while m <= 5:
                                if m != b and m != c and m != f and m > c:
                                    s = 1
                                    while s <= 5:
                                        if s != b and s != c and s != f and s != m and absInt(s - f) > 1:
                                            print("Baker in " + str(b) + ", Cooper in " + str(c) + ", Fletcher in " + str(f) + ", Miller in " + str(m) + ", Smith in " + str(s) + ".")
                                            return
                                        s = s + 1
                                m = m + 1
                        f = f + 1
                c = c + 1
        b = b + 1
    print("No solution found.")
main()
