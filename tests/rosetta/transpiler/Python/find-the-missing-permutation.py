# Code generated by Mochi transpiler.
# Version 0.10.42, generated on 2025-07-27 22:12 +0700
import sys
sys.set_int_max_str_digits(0)

given = ["ABCD", "CABD", "ACDB", "DACB", "BCDA", "ACBD", "ADCB", "CDAB", "DABC", "BCAD", "CADB", "CDBA", "CBAD", "ABDC", "ADBC", "BDCA", "DCBA", "BACD", "BADC", "BDAC", "CBDA", "DBCA", "DCAB"]
def idx(ch):
    if ch == "A":
        return 0
    if ch == "B":
        return 1
    if ch == "C":
        return 2
    return 3
def main():
    res = ""
    i = 0
    while i < len(given[0]):
        counts = [0, 0, 0, 0]
        for p in given:
            ch = p[i:i + 1]
            j = idx(ch)
            counts[j] = counts[j] + 1
        j = 0
        while j < 4:
            if counts[j] % 2 == 1:
                if j == 0:
                    res = res + "A"
                else:
                    if j == 1:
                        res = res + "B"
                    else:
                        if j == 2:
                            res = res + "C"
                        else:
                            res = res + "D"
            j = j + 1
        i = i + 1
    print(res)
main()
