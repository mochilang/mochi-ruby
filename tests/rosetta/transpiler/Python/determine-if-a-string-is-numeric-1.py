# Code generated by Mochi transpiler.
# Version 0.10.57, generated on 2025-08-04 15:16 +0700
import sys
sys.set_int_max_str_digits(0)
import os
if os.path.dirname(__file__) in sys.path:
    sys.path.remove(os.path.dirname(__file__))

def isNumeric(s):
    if s == "NaN":
        return True
    i = 0
    if len(s) == 0:
        return False
    if s[0] == "+" or s[0] == "-":
        if len(s) == 1:
            return False
        i = 1
    digits = False
    dot = False
    while i < len(s):
        ch = s[i]
        if ch >= "0" and ch <= "9":
            digits = True
            i = i + 1
        else:
            if ch == "." and dot == False:
                dot = True
                i = i + 1
            else:
                if (ch == "e" or ch == "E") and digits:
                    i = i + 1
                    if i < len(s) and (s[i] == "+" or s[i] == "-"):
                        i = i + 1
                    ed = False
                    while i < len(s) and s[i] >= "0" and s[i] <= "9":
                        ed = True
                        i = i + 1
                    return ed and i == len(s)
                else:
                    return False
    return digits
def main():
    print("Are these strings numeric?")
    strs = ["1", "3.14", "-100", "1e2", "NaN", "rose"]
    for s in strs:
        print("  " + s + " -> " + str(isNumeric(s)))
main()
