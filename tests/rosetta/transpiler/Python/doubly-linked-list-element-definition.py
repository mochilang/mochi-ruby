# Code generated by Mochi transpiler.
# Version 0.10.41, generated on 2025-07-26 19:54 +0700
import sys
sys.set_int_max_str_digits(0)

def Node(value, next, prev):
    return {"value": value, "next": next, "prev": prev}
def main():
    a = Node("A", None, None)
    b = Node("B", None, a)
    a["next"] = b
    c = Node("C", None, b)
    b["next"] = c
    p = a
    line = ""
    while p != None:
        line = line + (str(p.get("value")))
        p = p.get("next")
        if p != None:
            line = line + " "
    print(line)
    p = c
    line = ""
    while p != None:
        line = line + (str(p.get("value")))
        p = p.get("prev")
        if p != None:
            line = line + " "
    print(line)
main()
