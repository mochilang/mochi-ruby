# Code generated by Mochi transpiler.
# Version 0.10.39, generated on 2025-07-24 20:06 +0700
import os
import time

import sys
sys.set_int_max_str_digits(0)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

def gzipWriter(w):
    return w
def tarWriter(w):
    return w
def tarWriteHeader(w, hdr):
    pass
def tarWrite(w, data):
    pass
def main():
    filename = "TAPE.FILE"
    data = ""
    outfile = ""
    gzipFlag = False
    w = None
    if outfile != "":
        w = None
    if gzipFlag:
        w = gzipWriter(w)
    w = tarWriter(w)
    hdr = {"Name": filename, "Mode": 432, "Size": len(data), "ModTime": _now(), "Typeflag": 0, "Uname": "guest", "Gname": "guest"}
    tarWriteHeader(w, hdr)
    tarWrite(w, data)
main()
