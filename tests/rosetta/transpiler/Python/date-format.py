# Code generated by Mochi transpiler.
# Version 0.10.39, generated on 2025-07-24 20:06 +0700
import os
import time

import sys
sys.set_int_max_str_digits(0)


_now_seed = 0
_now_seeded = False
s = os.getenv("MOCHI_NOW_SEED")
if s and s != "":
    try:
        _now_seed = int(s)
        _now_seeded = True
    except Exception:
        pass

def _now():
    global _now_seed
    if _now_seeded:
        _now_seed = (_now_seed * 1664525 + 1013904223) % 2147483647
        return _now_seed
    return int(time.time_ns())

def pad2(n):
    if n < 10:
        return "0" + str(n)
    return str(n)
def weekdayName(z):
    names = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
    return names[(z + 4) % 7]
def main():
    ts = int((_now() // 1000000000))
    days = int((ts // 86400))
    z = days + 719468
    era = int((z // 146097))
    doe = z - era * 146097
    yoe = (doe - (doe // 1460) + (doe // 36524) - (doe // 146096)) // int(365)
    y = yoe + era * 400
    doy = doe - (365 * yoe + (yoe // 4) - (yoe // 100))
    mp = (5 * doy + 2) // int(153)
    d = int((doy - ((153 * mp + 2) // int(5)) + 1))
    m = int((mp + 3))
    if m > 12:
        y = y + 1
        m = m - 12
    iso = str(y) + "-" + pad2(m) + "-" + pad2(d)
    print(iso)
    months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]
    line = weekdayName(days) + ", " + months[m - 1] + " " + str(d) + ", " + str(y)
    print(line)
main()
