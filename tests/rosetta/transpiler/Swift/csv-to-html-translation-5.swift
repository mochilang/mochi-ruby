// Generated by Mochi transpiler v0.10.52 on 2025-08-02 00:36:02 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
func _split(_ s: String, _ sep: String) -> [String] {
    let d = sep.isEmpty ? " " : sep
    return s.components(separatedBy: d)
}
func split(_ s: String, _ sep: String) -> [String] {
    var out: [String] = ([] as! [String])
    var start: Int = 0
    var i: Int = 0
    let n = Int(((sep).count))
    while (i <= (Int(((s).count)) - n)) {
        if (String(describing: String(Array(String(describing: (s as! String)))[(i as! Int)..<((i + n) as! Int)])) == sep) {
            out = (_append(out, String(describing: String(Array(String(describing: (s as! String)))[(start as! Int)..<(i as! Int)]))) as! [String])
            i = ((i + n) as! Int)
            start = (i as! Int)
        } else {
            i = ((i + 1) as! Int)
        }
    }
    out = (_append(out, String(describing: String(Array(String(describing: (s as! String)))[(start as! Int)..<Int(((s).count))]))) as! [String])
    return out
}
func htmlEscape(_ s: String) -> String {
    var out: String = ""
    var i: Int = 0
    while (i < Int(((s).count))) {
        let ch = String(describing: String(Array(String(describing: (s as! String)))[(i as! Int)..<((i + 1) as! Int)]))
        if (ch == "&") {
            out = ((out + "&amp;") as! String)
        } else if (ch == "<") {
            out = ((out + "&lt;") as! String)
        } else if (ch == ">") {
            out = ((out + "&gt;") as! String)
        } else {
            out = ((out + ch) as! String)
        }
        
        
        i = ((i + 1) as! Int)
    }
    return out
}
let c = ((((("Character,Speech\n" + "The multitude,The messiah! Show us the messiah!\n") + "Brians mother,<angry>Now you listen here! He's not the messiah; he's a very naughty boy! Now go away!</angry>\n") + "The multitude,Who are you?\n") + "Brians mother,I'm his mother; that's who!\n") + "The multitude,Behold his mother! Behold his mother!")
var rows: [[String]] = ([] as! [[String]])
for line in (_split(c, "\n") as! [String]) {
    rows = (_append(rows, (_split(line, ",") as! [String])) as! [[String]])
}
print(_p("<table>"))
for row in rows {
    var cells: String = ""
    for cell in row {
        cells = ((((cells + "<td>") + String(describing: htmlEscape((cell as! String)))) + "</td>") as! String)
    }
    print(_p((("    <tr>" + cells) + "</tr>")))
}
print(_p("</table>"))
