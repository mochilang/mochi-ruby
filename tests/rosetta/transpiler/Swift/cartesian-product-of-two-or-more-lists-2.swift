// Generated by Mochi transpiler v0.10.50 on 2025-07-31 08:16:19 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
func _append<T>(_ xs: [T], _ v: T) -> [T] {
    var out = xs
    out.append(v)
    return out
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func listStr(_ xs: [Int]) -> String {
        var s: String = "["
        var i: Int = 0
        while (i < Int(((xs).count))) {
            s = ((s + _p((xs[i] as! Int))) as! String)
            if (i < (Int(((xs).count)) - 1)) {
                s = ((s + " ") as! String)
            }
            i = ((i + 1) as! Int)
        }
        s = ((s + "]") as! String)
        return (s as! String)
    }
    func llStr(_ lst: [[Int]]) -> String {
        var s: String = "["
        var i: Int = 0
        while (i < Int(((lst).count))) {
            s = ((s + String(describing: listStr((lst[i] as! [Int])))) as! String)
            if (i < (Int(((lst).count)) - 1)) {
                s = ((s + " ") as! String)
            }
            i = ((i + 1) as! Int)
        }
        s = ((s + "]") as! String)
        return (s as! String)
    }
    func cartN(_ lists: Any?) -> [[Int]] {
        if (String(describing: lists)
        == String(describing: nil as Any?)
        ) {
            return ([] as! [[Int]])
        }
        let a: [[Int]] = (lists as! [[Int]])
        if (Int(((a).count)) == 0) {
            return (([([] as! Any)] as! [[Any]]) as! [[Int]])
        }
        var c: Int = 1
        for xs in a {
            c = ((c * Int(((xs).count))) as! Int)
        }
        if (c == 0) {
            return ([] as! [[Int]])
        }
        var res: [[Int]] = ([] as! [[Int]])
        var idx: [Int] = ([] as! [Int])
        for _ in a {
            idx = (_append(idx, 0) as! [Int])
        }
        var n: Int = Int(((a).count))
        var count: Int = 0
        while (count < c) {
            var row: [Int] = ([] as! [Int])
            var j: Int = 0
            while (j < n) {
                row = (_append(row, (a[j][(idx[j] as! Int)] as! Int)) as! [Int])
                j = ((j + 1) as! Int)
            }
            res = (_append(res, row) as! [[Int]])
            var k: Int = (n - 1)
            while (k >= 0) {
                idx[k] = (((idx[k] as! Int) + 1) as! Int)
                if ((idx[k] as! Int) < Int((((a[k] as! [Int])).count))) {
                    break
                }
                idx[k] = 0
                k = ((k - 1) as! Int)
            }
            count = ((count + 1) as! Int)
        }
        return (res as! [[Int]])
    }
    func main() {
        print(_p(String(describing: llStr((cartN(([(([(1 as! Any), (2 as! Any)] as! [Int]) as! Any), (([(3 as! Any), (4 as! Any)] as! [Int]) as! Any)] as! [[Int]])) as! [[Int]])))))
        print(_p(String(describing: llStr((cartN(([(([(3 as! Any), (4 as! Any)] as! [Int]) as! Any), (([(1 as! Any), (2 as! Any)] as! [Int]) as! Any)] as! [[Int]])) as! [[Int]])))))
        print(_p(String(describing: llStr((cartN([(([(1 as! Any), (2 as! Any)] as! [Int]) as! Any), ([] as! Any)]) as! [[Int]])))))
        print(_p(String(describing: llStr((cartN([([] as! Any), (([(1 as! Any), (2 as! Any)] as! [Int]) as! Any)]) as! [[Int]])))))
        print(_p(""))
        print(_p("["))
        for p in (cartN(([(([(1776 as! Any), (1789 as! Any)] as! [Int]) as! Any), (([(7 as! Any), (12 as! Any)] as! [Int]) as! Any), (([(4 as! Any), (14 as! Any), (23 as! Any)] as! [Int]) as! Any), (([(0 as! Any), (1 as! Any)] as! [Int]) as! Any)] as! [[Int]])) as! [[Int]]) {
            print(_p((" " + String(describing: listStr((p as! [Int]))))))
        }
        print(_p("]"))
        print(_p(String(describing: llStr((cartN(([(([(1 as! Any), (2 as! Any), (3 as! Any)] as! [Int]) as! Any), (([(30 as! Any)] as! [Int]) as! Any), (([(500 as! Any), (100 as! Any)] as! [Int]) as! Any)] as! [[Int]])) as! [[Int]])))))
        print(_p(String(describing: llStr((cartN([(([(1 as! Any), (2 as! Any), (3 as! Any)] as! [Int]) as! Any), ([] as! Any), (([(500 as! Any), (100 as! Any)] as! [Int]) as! Any)]) as! [[Int]])))))
        print(_p(""))
        print(_p(String(describing: llStr((cartN(nil) as! [[Int]])))))
        print(_p(String(describing: llStr((cartN([]) as! [[Int]])))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
