// Generated by Mochi transpiler v0.10.50 on 2025-07-31 08:10:10 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func mapString(_ s: String, _ f: (String) -> String) -> String {
        var out: String = ""
        var i: Int = 0
        while (i < Int(((s).count))) {
            out = ((out + String(describing: f((String(Array(s)[i..<(i + 1)]) as! String)))) as! String)
            i = ((i + 1) as! Int)
        }
        return (out as! String)
    }
    func main() {
        let fn = { (r: String) -> String in String(describing: ((r == " ") ? "" : r)) }
        _ = String(describing: mapString("Spaces removed", fn))
        _ = String(describing: mapString("Test", { (r: String) -> String in String(describing: (r.lowercased())) }))
        _ = String(describing: mapString("shift", { (r: String) -> String in r }))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
