// Generated by Mochi transpiler v0.10.42 on 2025-07-28 10:40:37 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func log2(_ x: Double) -> Double {
        var k: Double = 0.0
        var v: Double = x
        while (v >= 2.0) {
            v = Double((v / 2.0))
            k = Double((k + 1.0))
        }
        while (v < 1.0) {
            v = Double((v * 2.0))
            k = Double((k - 1.0))
        }
        let z = (Double((v - 1.0)) / Double((v + 1.0)))
        var zpow: Double = z
        var sum: Double = z
        var i: Int = 3
        while (i <= 9) {
            zpow = Double(((zpow * z) * z))
            sum = Double((sum + (zpow / Double(i))))
            i = Int((i + 2))
        }
        let ln2 = 0.6931471805599453
        return Double((k + ((2.0 * sum) / ln2)))
    }
    func entropy(_ data: String) -> Double {
        if (data == "") {
            return 0.0
        }
        var counts: [String: Int] = ([:] as! [String: Int])
        var i: Int = 0
        while (i < Int(((data).count))) {
            let ch = String(describing: String(Array(String(describing: String(describing: data)))[Int(i)..<Int((i + 1))]))
            if (counts[ch] != nil) {
                counts[ch] = Int((counts[ch]! + 1))
            } else {
                counts[ch] = 1
            }
            i = Int((i + 1))
        }
        var e: Double = 0.0
        let l = Double(((data).count))
        for ch in counts.keys.sorted() {
            let px = (Double(counts[ch]!) / l)
            if (px > 0.0) {
                e = Double((e - (px * Double(log2(Double(px))))))
            }
        }
        return Double(e)
    }
    let source = ((((((((((((((((((((((((((((((((((((((((((((((((((((("// Mochi translation of the Rosetta \"Entropy-Narcissist\" task\n" + "// Simplified to compute the entropy of this source string\n\n") + "fun log2(x: float): float {\n") + "  var k = 0.0\n") + "  var v = x\n") + "  while v >= 2.0 {\n") + "    v = v / 2.0\n") + "    k = k + 1.0\n") + "  }\n") + "  while v < 1.0 {\n") + "    v = v * 2.0\n") + "    k = k - 1.0\n") + "  }\n") + "  let z = (v - 1.0) / (v + 1.0)\n") + "  var zpow = z\n") + "  var sum = z\n") + "  var i = 3\n") + "  while i <= 9 {\n") + "    zpow = zpow * z * z\n") + "    sum = sum + zpow / (i as float)\n") + "    i = i + 2\n") + "  }\n") + "  let ln2 = 0.6931471805599453\n") + "  return k + 2.0 * sum / ln2\n") + "}\n\n") + "fun entropy(data: string): float {\n") + "  if data == \"\" { return 0.0 }\n") + "  var counts: map<string,int> = {}\n") + "  var i = 0\n") + "  while i < len(data) {\n") + "    let ch = substring(data, i, i+1)\n") + "    if ch in counts {\n") + "      counts[ch] = counts[ch] + 1\n") + "    } else {\n") + "      counts[ch] = 1\n") + "    }\n") + "    i = i + 1\n") + "  }\n") + "  var e = 0.0\n") + "  let l = len(data) as float\n") + "  for ch in counts {\n") + "    let px = (counts[ch] as float) / l\n") + "    if px > 0.0 {\n") + "      e = e - px * log2(px)\n") + "    }\n") + "  }\n") + "  return e\n") + "}\n\n") + "// Store the program source as a string constant\n") + "let source = ... // truncated in actual source\n") + "\nfun main() {\n") + "  print(\"Source file entropy: \" + str(entropy(source)))\n") + "}\n\n") + "main()\n")
    func main() {
        print(_p(("Source file entropy: " + _p(Double(entropy(String(describing: source)))))))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
