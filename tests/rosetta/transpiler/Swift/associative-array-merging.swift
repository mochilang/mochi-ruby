// Generated by Mochi transpiler v0.10.40 on 2025-07-26 10:27:44 GMT+7
import Foundation

func _p(_ v: Any?) -> String {
    if let val = v { return String(describing: val) }
    return "<nil>"
}

var _nowSeed = 0
var _nowSeeded = false
func _now() -> Int {
    if !_nowSeeded {
        if let s = ProcessInfo.processInfo.environment["MOCHI_NOW_SEED"], let v = Int(s) {
            _nowSeed = v
            _nowSeeded = true
        }
    }
    if _nowSeeded {
        _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
        return _nowSeed
    }
    return Int(Date().timeIntervalSince1970 * 1_000_000_000)
}
func _mem() -> Int {
    if let status = try? String(contentsOfFile: "/proc/self/status") {
        for line in status.split(separator: "\n") {
            if line.hasPrefix("VmRSS:") {
                let parts = line.split(whereSeparator: { $0 == " " || $0 == "\t" })
                if parts.count >= 2, let kb = Int(parts[1]) {
                    return kb * 1024
                }
            }
        }
    }
    return 0
}
do {
    let _benchMemStart = _mem()
    let _benchStart = _now()
    func merge(_ base: [String: Any], _ update: [String: Any]) -> [String: Any] {
        var result: [String: Any] = ([:] as! [String: Any])
        for k in base.keys.sorted() {
            result[k] = (base as! [String: Any])[k]
        }
        for k in update.keys.sorted() {
            result[k] = (update as! [String: Any])[k]
        }
        return (result as! [String: Any])
    }
    func main() {
        let base: [String: Any] = ["name": "Rocket Skates", "price": 12.75, "color": "yellow"]
        let update: [String: Any] = ["price": 15.25, "color": "red", "year": 1974]
        let result = (merge((base as! [String: Any]), (update as! [String: Any])) as! [String: Any])
        print(_p(String(describing: result)))
    }
    _ = main()
    let _benchEnd = _now()
    let _benchMemEnd = _mem()
    print("{\n  \"duration_us\": \((_benchEnd - _benchStart) / 1000),\n  \"memory_bytes\": \(_benchMemEnd - _benchMemStart),\n  \"name\": \"main\"\n}")
}
