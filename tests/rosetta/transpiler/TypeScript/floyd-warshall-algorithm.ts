// Generated by Mochi v0.10.42 on 2025-07-28 10:08:18 GMT+7

function main() {
  let INF: number = 1000000000;
  let n: number = 4;
  let dist: number[][] = [];
  let next: number[][] = [];
  let i: number = 0;
  while ((i < n)) {
    let row: number[] = [];
    let nrow: number[] = [];
    let j: number = 0;
    while ((j < n)) {
      if ((i == j)) {
        row.push(0);
      } else {
        row.push(INF);
      }
      nrow.push((0 - 1));
      j = (j + 1);
    }
    dist.push(row);
    next.push(nrow);
    i = (i + 1);
  }
  dist[0][2] = -2;
  next[0][2] = 2;
  dist[2][3] = 2;
  next[2][3] = 3;
  dist[3][1] = -1;
  next[3][1] = 1;
  dist[1][0] = 4;
  next[1][0] = 0;
  dist[1][2] = 3;
  next[1][2] = 2;
  let k: number = 0;
  while ((k < n)) {
    let i: number = 0;
    while ((i < n)) {
      let j: number = 0;
      while ((j < n)) {
        if (((dist[i][k] < INF) && (dist[k][j] < INF))) {
          let alt: bigint = (dist[i][k] + dist[k][j]);
          if ((alt < BigInt(dist[i][j]))) {
            dist[i][j] = alt;
            next[i][j] = next[i][k];
          }
        }
        j = (j + 1);
      }
      i = (i + 1);
    }
    k = (k + 1);
  }
  function path(u: number, v: number): number[] {
    let ui: number = (u - 1);
    let vi: number = (v - 1);
    if ((next[ui][vi] == (0 - 1))) {
      return [];
    }
    let p: number[] = [u];
    let cur: number = ui;
    while ((cur != vi)) {
      cur = next[cur][vi];
      p.push((cur + 1));
    }
    return p;
  }
  function pathStr(p: number[]): string {
    let s: string = "";
    let first: boolean = true;
    let idx: number = 0;
    while ((idx < (Array.isArray(p) || typeof p === 'string' ? p.length : Object.keys(p ?? {}).length))) {
      let x: number = p[idx];
      if (!first) {
        s = (s + " -> ");
      }
      s = (s + _str(x));
      first = false;
      idx = (idx + 1);
    }
    return s;
  }
  console.log(_str("pair\tdist\tpath"));
  let a: number = 0;
  while ((a < n)) {
    let b: number = 0;
    while ((b < n)) {
      if ((a != b)) {
        console.log(_str(((((((_str((a + 1)) + " -> ") + _str((b + 1))) + "\t") + _str(dist[a][b])) + "\t") + pathStr(path((a + 1), (b + 1))))));
      }
      b = (b + 1);
    }
    a = (a + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

