// Generated by Mochi v0.10.42 on 2025-07-27 21:21:22 GMT+7

function ord(ch: string): number {
  if ((ch == "5")) {
    return 53;
  }
  if ((ch == "T")) {
    return 84;
  }
  if ((ch == " ")) {
    return 32;
  }
  if ((ch == "é")) {
    return 233;
  }
  if ((ch == "🐺")) {
    return 128058;
  }
  return 0;
}
function hex(n: number): string {
  let digits: string = "0123456789abcdef";
  if ((n == 0)) {
    return "0x0";
  }
  let m: number = n;
  let out: string = "";
  while ((m > 0)) {
    let d: number = (m % 16);
    out = ((digits).substring(d, (d + 1)) + out);
    m = Math.trunc(m / 16);
  }
  return ("0x" + out);
}
function quote(s: string): string {
  return (("'" + s) + "'");
}
function analyze(s: string) {
  let le: number = (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length);
  console.log((((("Analyzing " + quote(s)) + " which has a length of ") + String(le)) + ":"));
  if ((le > 1)) {
    let i: number = 1;
    while ((i < le)) {
      let cur: string = (s).substring(i, (i + 1));
      let prev: string = (s).substring((i - 1), i);
      if ((cur != prev)) {
        console.log("  Not all characters in the string are the same.");
        console.log((((((("  " + quote(cur)) + " (") + hex(ord(cur))) + ") is different at position ") + String((i + 1))) + "."));
        console.log("");
        return;
      }
      i = (i + 1);
    }
  }
  console.log("  All characters in the string are the same.");
  console.log("");
}
function main() {
  let strings: string[] = ["", "   ", "2", "333", ".55", "tttTTT", "4444 444k", "pépé", "🐶🐶🐺🐶", "🎄🎄🎄🎄"];
  let i: number = 0;
  while ((i < (Array.isArray(strings) || typeof strings === 'string' ? strings.length : Object.keys(strings ?? {}).length))) {
    analyze(strings[i]);
    i = (i + 1);
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

