// Generated by Mochi v0.10.42 on 2025-07-28 10:40:08 GMT+7

let THRESHOLD: number = 140737488355328;
function indexOf(xs: number[], value: number): number {
  let i: number = 0;
  while ((i < (Array.isArray(xs) || typeof xs === 'string' ? xs.length : Object.keys(xs ?? {}).length))) {
    if ((xs[i] == value)) {
      return i;
    }
    i = (i + 1);
  }
  return (0 - 1);
}
function contains(xs: number[], value: number): boolean {
  return (xs.indexOf(value) != (0 - 1));
}
function maxOf(a: number, b: number): number {
  if ((a > b)) {
    return a;
  } else {
    return b;
  }
}
function intSqrt(n: number): number {
  if ((n == 0)) {
    return 0;
  }
  let x: number = n;
  let y: number = (x + 1) / BigInt(2);
  while ((y < x)) {
    x = y;
    y = (x + Math.trunc(n / x)) / BigInt(2);
  }
  return x;
}
function sumProperDivisors(n: number): number {
  if ((n < 2)) {
    return 0;
  }
  let sqrt: number = intSqrt(n);
  let sum: number = 1;
  let i: number = 2;
  while ((i <= sqrt)) {
    if (((n % i) == 0)) {
      sum = ((sum + i) + Math.trunc(n / i));
    }
    i = (i + 1);
  }
  if (((sqrt * sqrt) == n)) {
    sum = (sum - sqrt);
  }
  return sum;
}
function classifySequence(k: number): Record<string, any> {
  let last: number = k;
  let seq: number[] = [k];
  while (true) {
    last = sumProperDivisors(last);
    seq.push(last);
    let n: number = (Array.isArray(seq) || typeof seq === 'string' ? seq.length : Object.keys(seq ?? {}).length);
    let aliquot: string = "";
    if ((last == 0)) {
      aliquot = "Terminating";
    } else {
      if (((n == 2) && (last == k))) {
        aliquot = "Perfect";
      } else {
        if (((n == 3) && (last == k))) {
          aliquot = "Amicable";
        } else {
          if (((n >= 4) && (last == k))) {
            aliquot = (("Sociable[" + _str((n - 1))) + "]");
          } else {
            if ((last == seq[Math.trunc((n - 2))])) {
              aliquot = "Aspiring";
            } else {
              if (seq.slice(1, maxOf(1, (n - 2))).includes(last)) {
                let idx = seq.indexOf(last);
                aliquot = (("Cyclic[" + _str(((n - 1) - idx))) + "]");
              } else {
                if (((n == 16) || (last > THRESHOLD))) {
                  aliquot = "Non-Terminating";
                }
              }
            }
          }
        }
      }
    }
    if ((aliquot != "")) {
      return {seq, aliquot};
    }
  }
  return {seq, "aliquot": ""};
}
function padLeft(n: number, w: number): string {
  let s: string = _str(n);
  while (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) < w)) {
    s = (" " + s);
  }
  return s;
}
function padRight(s: string, w: number): string {
  let r: string = s;
  while (((Array.isArray(r) || typeof r === 'string' ? r.length : Object.keys(r ?? {}).length) < w)) {
    r = (r + " ");
  }
  return r;
}
function joinWithCommas(seq: number[]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < (Array.isArray(seq) || typeof seq === 'string' ? seq.length : Object.keys(seq ?? {}).length))) {
    s = (s + _str(seq[i]));
    if ((i < ((Array.isArray(seq) || typeof seq === 'string' ? seq.length : Object.keys(seq ?? {}).length) - 1))) {
      s = (s + ", ");
    }
    i = (i + 1);
  }
  s = (s + "]");
  return s;
}
function main() {
  console.log(_str("Aliquot classifications - periods for Sociable/Cyclic in square brackets:\n"));
  let k: number = 1;
  while ((k <= 10)) {
    let res: Record<string, any> = classifySequence(k);
    console.log(_str(((((padLeft(k, 2) + ": ") + padRight(res.aliquot, 15)) + " ") + joinWithCommas(res.seq))));
    k = (k + 1);
  }
  console.log(_str(""));
  let s: number[] = [11, 12, 28, 496, 220, 1184, 12496, 1264460, 790, 909, 562, 1064, 1488];
  let i: number = 0;
  while ((i < (Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    let val: number = s[i];
    let res: Record<string, any> = classifySequence(val);
    console.log(_str(((((padLeft(val, 7) + ": ") + padRight(res.aliquot, 15)) + " ") + joinWithCommas(res.seq))));
    i = (i + 1);
  }
  console.log(_str(""));
  let big: number = 15355717786080;
  let r: Record<string, any> = classifySequence(big);
  console.log(_str(((((_str(big) + ": ") + padRight(r.aliquot, 15)) + " ") + joinWithCommas(r.seq))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

