// Generated by Mochi v0.10.55 on 2025-08-02 17:55:51 GMT+7

export interface Point { x: number; y: number }
function ccw(a: Point, b: Point, c: Point): boolean {
  let lhs: number = ((b.x - a.x) * (c.y - a.y));
  let rhs: number = ((b.y - a.y) * (c.x - a.x));
  return (lhs > rhs);
}
function sortPoints(ps: Point[]): Point[] {
  let arr: Point[] = ps;
  let n: number = Number(Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length);
  let i: number = 0;
  while ((i < n)) {
    let j: number = 0;
    while ((j < (n - 1))) {
      let p: Point = arr[j];
      let q: Point = arr[Math.trunc((j + 1))];
      if (((p.x > q.x) || ((p.x == q.x) && (p.y > q.y)))) {
        arr[j] = q;
        arr[(j + 1)] = p;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return arr;
}
function convexHull(ps: Point[]): Point[] {
  ps = sortPoints(ps);
  let h: Point[] = [];
  for (const pt of ps) {
    while (((Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) >= 2) && (ccw(h[Math.trunc((Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 2))], h[Math.trunc((Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 1))], pt) == false))) {
      h = h.slice(0, (Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 1));
    }
    h.push(pt);
  }
  let i: number = (Number(Array.isArray(ps) || typeof ps === 'string' ? ps.length : Object.keys(ps ?? {}).length) - 2);
  let t: number = (Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) + 1);
  while ((i >= 0)) {
    let pt: Point = ps[i];
    while (((Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) >= t) && (ccw(h[Math.trunc((Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 2))], h[Math.trunc((Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 1))], pt) == false))) {
      h = h.slice(0, (Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 1));
    }
    h.push(pt);
    i = (i - 1);
  }
  return h.slice(0, (Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 1));
}
function pointStr(p: Point): string {
  return (((("(" + _str(p.x)) + ",") + _str(p.y)) + ")");
}
function hullStr(h: Point[]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length))) {
    s = (s + pointStr(h[i]));
    if ((i < (Number(Array.isArray(h) || typeof h === 'string' ? h.length : Object.keys(h ?? {}).length) - 1))) {
      s = (s + " ");
    }
    i = (i + 1);
  }
  s = (s + "]");
  return s;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let pts: Point[]
let hull: Point[]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
pts = [{"x": 16, "y": 3}, {"x": 12, "y": 17}, {"x": 0, "y": 6}, {"x": -4, "y": -6}, {"x": 16, "y": 6}, {"x": 16, "y": -7}, {"x": 16, "y": -3}, {"x": 17, "y": -4}, {"x": 5, "y": 19}, {"x": 19, "y": -8}, {"x": 3, "y": 16}, {"x": 12, "y": 13}, {"x": 3, "y": -4}, {"x": 17, "y": 5}, {"x": -3, "y": 15}, {"x": -3, "y": -9}, {"x": 0, "y": 11}, {"x": -9, "y": -3}, {"x": -4, "y": -2}, {"x": 12, "y": 10}]
hull = convexHull(pts)
  console.log(_str(("Convex Hull: " + hullStr(hull))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

