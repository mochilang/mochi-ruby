// Generated by Mochi v0.10.56 on 2025-08-03 17:23:21 GMT+7

function char(n: number): string {
  let letters: string = "abcdefghijklmnopqrstuvwxyz";
  let idx: number = (n - 97);
  if (((idx < 0) || (idx >= Number(Array.isArray(letters) || typeof letters === 'string' ? letters.length : Object.keys(letters ?? {}).length)))) {
    return "?";
  }
  return (letters).substring(idx, (idx + 1));
}
function fromBytes(bs: number[]): string {
  let s: string = "";
  let i: number = 0;
  while ((i < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
    s = (s + char(bs[i]));
    i = (i + 1);
  }
  return s;
}
let b: number[] = [98, 105, 110, 97, 114, 121];
let c: number[] = b;
let d: number[] = [];
let i: number = 0;
let f: number[] = [];
let rem: number[] = [];
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
let z: any[]
let sub: number[]
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(b)));
  console.log(_str(_str(c)));
  console.log(_str(_str((b == c))));
  while ((i < Number(Array.isArray(b) || typeof b === 'string' ? b.length : Object.keys(b ?? {}).length))) {
    d.push(b[i]);
    i = (i + 1);
  }
  d[1] = 97;
  d[4] = 110;
  console.log(_str(fromBytes(b)));
  console.log(_str(fromBytes(d)));
  console.log(_str(_str((Number(Array.isArray(b) || typeof b === 'string' ? b.length : Object.keys(b ?? {}).length) == 0))));
z = [...b, 122]
  console.log(_str(fromBytes(z)));
sub = b.slice(1, 3)
  console.log(_str(fromBytes(sub)));
  i = 0;
  while ((i < Number(Array.isArray(d) || typeof d === 'string' ? d.length : Object.keys(d ?? {}).length))) {
    let val: number = d[i];
    if ((val == 110)) {
      f.push(109);
    } else {
      f.push(val);
    }
    i = (i + 1);
  }
  console.log(_str(((fromBytes(d) + " -> ") + fromBytes(f))));
  rem.push(b[Math.trunc(0)]);
  i = 3;
  while ((i < Number(Array.isArray(b) || typeof b === 'string' ? b.length : Object.keys(b ?? {}).length))) {
    rem.push(b[i]);
    i = (i + 1);
  }
  console.log(_str(fromBytes(rem)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

