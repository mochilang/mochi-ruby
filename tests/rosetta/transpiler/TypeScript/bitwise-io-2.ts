// Generated by Mochi v0.10.56 on 2025-08-03 17:23:45 GMT+7

function pow2(n: number): number {
  let v: number = 1;
  let i: number = 0;
  while ((i < n)) {
    v = (v * 2);
    i = (i + 1);
  }
  return v;
}
function lshift(x: number, n: number): number {
  return (x * pow2(n));
}
function rshift(x: number, n: number): number {
  return Math.trunc(x / pow2(n));
}
export interface Writer { order: string; bits: number; nbits: number; data: number[] }
function NewWriter(order: string): Writer {
  return {order, "bits": 0, "nbits": 0, "data": []};
}
function writeBitsLSB(w: Writer, c: number, width: number): Writer {
  w.bits = (w.bits + lshift(c, w.nbits));
  w.nbits = (w.nbits + width);
  while ((w.nbits >= 8)) {
    let b: number = (w.bits % 256);
    w.data = [...w.data, b];
    w.bits = rshift(w.bits, 8);
    w.nbits = (w.nbits - 8);
  }
  return w;
}
function writeBitsMSB(w: Writer, c: number, width: number): Writer {
  w.bits = (w.bits + lshift(c, ((32 - width) - w.nbits)));
  w.nbits = (w.nbits + width);
  while ((w.nbits >= 8)) {
    let b: number = (rshift(w.bits, 24) % 256);
    w.data = [...w.data, b];
    w.bits = ((w.bits % pow2(24)) * 256);
    w.nbits = (w.nbits - 8);
  }
  return w;
}
function WriteBits(w: Writer, c: number, width: number): Writer {
  if ((w.order == "LSB")) {
    return writeBitsLSB(w, c, width);
  }
  return writeBitsMSB(w, c, width);
}
function CloseWriter(w: Writer): Writer {
  if ((w.nbits > 0)) {
    if ((w.order == "MSB")) {
      w.bits = rshift(w.bits, 24);
    }
    w.data = [...w.data, (w.bits % 256)];
  }
  w.bits = 0;
  w.nbits = 0;
  return w;
}
export interface Reader { order: string; data: number[]; idx: number; bits: number; nbits: number }
function NewReader(data: number[], order: string): Reader {
  return {order, data, "idx": 0, "bits": 0, "nbits": 0};
}
function readBitsLSB(r: Reader, width: number): Record<string, any> {
  while ((r.nbits < width)) {
    if ((r.idx >= Number(Array.isArray(r.data) || typeof r.data === 'string' ? r.data.length : Object.keys(r.data ?? {}).length))) {
      return {"val": 0, "eof": true};
    }
    let b: number = r.data[Math.trunc(r.idx)];
    r.idx = (r.idx + 1);
    r.bits = (r.bits + lshift(b, r.nbits));
    r.nbits = (r.nbits + 8);
  }
  let mask: number = (pow2(width) - 1);
  let out: number = (r.bits % (mask + 1));
  r.bits = rshift(r.bits, width);
  r.nbits = (r.nbits - width);
  return {"val": out, "eof": false};
}
function readBitsMSB(r: Reader, width: number): Record<string, any> {
  while ((r.nbits < width)) {
    if ((r.idx >= Number(Array.isArray(r.data) || typeof r.data === 'string' ? r.data.length : Object.keys(r.data ?? {}).length))) {
      return {"val": 0, "eof": true};
    }
    let b: number = r.data[Math.trunc(r.idx)];
    r.idx = (r.idx + 1);
    r.bits = (r.bits + lshift(b, (24 - r.nbits)));
    r.nbits = (r.nbits + 8);
  }
  let out: number = rshift(r.bits, (32 - width));
  r.bits = ((r.bits * pow2(width)) % pow2(32));
  r.nbits = (r.nbits - width);
  return {"val": out, "eof": false};
}
function ReadBits(r: Reader, width: number): Record<string, any> {
  if ((r.order == "LSB")) {
    return readBitsLSB(r, width);
  }
  return readBitsMSB(r, width);
}
function toBinary(n: number, bits: number): string {
  let b: string = "";
  let val: number = n;
  let i: number = 0;
  while ((i < bits)) {
    b = (_str((val % 2)) + b);
    val = Math.trunc(val / 2);
    i = (i + 1);
  }
  return b;
}
function bytesToBits(bs: number[]): string {
  let out: string = "[";
  let i: number = 0;
  while ((i < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
    out = (out + toBinary(bs[i], 8));
    if (((i + 1) < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
      out = (out + " ");
    }
    i = (i + 1);
  }
  out = (out + "]");
  return out;
}
function bytesToHex(bs: number[]): string {
  let digits: string = "0123456789ABCDEF";
  let out: string = "";
  let i: number = 0;
  while ((i < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
    let b: number = bs[i];
    let hi: number = Math.trunc(b / 16);
    let lo: number = (b % 16);
    out = ((out + digits.slice(hi, (hi + 1))) + digits.slice(lo, (lo + 1)));
    if (((i + 1) < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
      out = (out + " ");
    }
    i = (i + 1);
  }
  return out;
}
function ord(ch: string): number {
  let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  let lower = "abcdefghijklmnopqrstuvwxyz";
  let idx = upper.indexOf(ch);
  if ((idx >= 0)) {
    return (65 + idx);
  }
  idx = lower.indexOf(ch);
  if ((idx >= 0)) {
    return (97 + idx);
  }
  if (((ch >= "0") && (ch <= "9"))) {
    return (48 + parseIntStr(ch, 10));
  }
  if ((ch == " ")) {
    return 32;
  }
  if ((ch == ".")) {
    return 46;
  }
  return 0;
}
function chr(n: number): string {
  let upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
  let lower = "abcdefghijklmnopqrstuvwxyz";
  if (((n >= 65) && (n < 91))) {
    return upper.slice((n - 65), (n - 64));
  }
  if (((n >= 97) && (n < 123))) {
    return lower.slice((n - 97), (n - 96));
  }
  if (((n >= 48) && (n < 58))) {
    let digits: string = "0123456789";
    return digits.slice((n - 48), (n - 47));
  }
  if ((n == 32)) {
    return " ";
  }
  if ((n == 46)) {
    return ".";
  }
  return "?";
}
function bytesOfStr(s: string): number[] {
  let bs: number[] = [];
  let i: number = 0;
  while ((i < Number(Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    bs.push(ord(s.slice(i, (i + 1))));
    i = (i + 1);
  }
  return bs;
}
function bytesToDec(bs: number[]): string {
  let out: string = "";
  let i: number = 0;
  while ((i < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
    out = (out + _str(bs[i]));
    if (((i + 1) < Number(Array.isArray(bs) || typeof bs === 'string' ? bs.length : Object.keys(bs ?? {}).length))) {
      out = (out + " ");
    }
    i = (i + 1);
  }
  return out;
}
function Example() {
  let message: string = "This is a test.";
  let msgBytes: number[] = bytesOfStr(message);
  console.log(_str(((("\"" + message) + "\" as bytes: ") + bytesToDec(msgBytes))));
  console.log(_str(("    original bits: " + bytesToBits(msgBytes))));
  let bw: Writer = NewWriter("MSB");
  let i: number = 0;
  while ((i < Number(Array.isArray(msgBytes) || typeof msgBytes === 'string' ? msgBytes.length : Object.keys(msgBytes ?? {}).length))) {
    bw = WriteBits(bw, msgBytes[i], 7);
    i = (i + 1);
  }
  bw = CloseWriter(bw);
  console.log(_str(("Written bitstream: " + bytesToBits(bw.data))));
  console.log(_str(("Written bytes: " + bytesToHex(bw.data))));
  let br: Reader = NewReader(bw.data, "MSB");
  let result: string = "";
  while (true) {
    let r: Record<string, any> = ReadBits(br, 7);
    if (r.eof) {
      break
    }
    let v: number = Math.trunc(r.val);
    if ((v != 0)) {
      result = (result + chr(v));
    }
  }
  console.log(_str((("Read back as \"" + result) + "\"")));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function parseIntStr(s: string, base: number): number { return parseInt(s, Math.trunc(base)); }
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  Example();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

