// Generated by Mochi v0.10.57 on 2025-08-04 16:11:20 GMT+7

function listStr(xs: number[]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < Number(Array.isArray(xs) || typeof xs === 'string' ? xs.length : Object.keys(xs ?? {}).length))) {
    s = (s + _str(xs[i]));
    if ((i < (Number(Array.isArray(xs) || typeof xs === 'string' ? xs.length : Object.keys(xs ?? {}).length) - 1))) {
      s = (s + " ");
    }
    i = (i + 1);
  }
  s = (s + "]");
  return s;
}
function llStr(lst: number[][]): string {
  let s: string = "[";
  let i: number = 0;
  while ((i < Number(Array.isArray(lst) || typeof lst === 'string' ? lst.length : Object.keys(lst ?? {}).length))) {
    s = (s + listStr(lst[i]));
    if ((i < (Number(Array.isArray(lst) || typeof lst === 'string' ? lst.length : Object.keys(lst ?? {}).length) - 1))) {
      s = (s + " ");
    }
    i = (i + 1);
  }
  s = (s + "]");
  return s;
}
function cartN(lists): number[][] {
  if ((lists == null)) {
    return [];
  }
  let a: number[][] = lists;
  if ((Number(Array.isArray(a) || typeof a === 'string' ? a.length : Object.keys(a ?? {}).length) == 0)) {
    return [[]];
  }
  let c: number = 1;
  for (const xs of a) {
    c = (c * Number(Array.isArray(xs) || typeof xs === 'string' ? xs.length : Object.keys(xs ?? {}).length));
  }
  if ((c == 0)) {
    return [];
  }
  let res: number[][] = [];
  let idx: number[] = [];
  for (const _ of a) {
    idx.push(0);
  }
  let n: number = Number(Array.isArray(a) || typeof a === 'string' ? a.length : Object.keys(a ?? {}).length);
  let count: number = 0;
  while ((count < c)) {
    let row: number[] = [];
    let j: number = 0;
    while ((j < n)) {
      row.push(a[j][Math.trunc(idx[j])]);
      j = (j + 1);
    }
    res.push(row);
    let k: number = (n - 1);
    while ((k >= 0)) {
      idx[k] = (idx[k] + 1);
      if ((idx[k] < Number(Array.isArray(a[k]) || typeof a[k] === 'string' ? a[k].length : Object.keys(a[k] ?? {}).length))) {
        break
      }
      idx[k] = 0;
      k = (k - 1);
    }
    count = (count + 1);
  }
  return res;
}
function main() {
  console.log(_str(llStr(cartN([[1, 2], [3, 4]]))));
  console.log(_str(llStr(cartN([[3, 4], [1, 2]]))));
  console.log(_str(llStr(cartN([[1, 2], []]))));
  console.log(_str(llStr(cartN([[], [1, 2]]))));
  console.log(_str(""));
  console.log(_str("["));
  for (const p of cartN([[1776, 1789], [7, 12], [4, 14, 23], [0, 1]])) {
    console.log(_str((" " + listStr(p))));
  }
  console.log(_str("]"));
  console.log(_str(llStr(cartN([[1, 2, 3], [30], [500, 100]]))));
  console.log(_str(llStr(cartN([[1, 2, 3], [], [500, 100]]))));
  console.log(_str(""));
  console.log(_str(llStr(cartN(null))));
  console.log(_str(llStr(cartN([]))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

