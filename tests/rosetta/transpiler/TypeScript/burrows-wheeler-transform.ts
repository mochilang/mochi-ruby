// Generated by Mochi v0.10.56 on 2025-08-03 17:26:32 GMT+7

let stx: string = "\x02";
let etx: string = "\x03";
function contains(s: string, ch: string): boolean {
  let i: number = 0;
  while ((i < Number(Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    if (((s).substring(i, (i + 1)) == ch)) {
      return true;
    }
    i = (i + 1);
  }
  return false;
}
function sortStrings(xs: string[]): string[] {
  let arr: string[] = xs;
  let n: number = Number(Array.isArray(arr) || typeof arr === 'string' ? arr.length : Object.keys(arr ?? {}).length);
  let i: number = 0;
  while ((i < n)) {
    let j: number = 0;
    while ((j < (n - 1))) {
      if ((arr[j] > arr[Math.trunc((j + 1))])) {
        let tmp: string = arr[j];
        arr[j] = arr[Math.trunc((j + 1))];
        arr[(j + 1)] = tmp;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return arr;
}
function bwt(s: string): Record<string, any> {
  if ((s.includes(stx) || s.includes(etx))) {
    return {"err": true, "res": ""};
  }
  s = ((stx + s) + etx);
  let le: number = Number(Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length);
  let table: string[] = [];
  let i: number = 0;
  while ((i < le)) {
    let rot: string = ((s).substring(i, le) + (s).substring(0, i));
    table.push(rot);
    i = (i + 1);
  }
  table = sortStrings(table);
  let last: string = "";
  i = 0;
  while ((i < le)) {
    last = (last + (table[i]).substring((le - 1), le));
    i = (i + 1);
  }
  return {"err": false, "res": last};
}
function ibwt(r: string): string {
  let le: number = Number(Array.isArray(r) || typeof r === 'string' ? r.length : Object.keys(r ?? {}).length);
  let table: string[] = [];
  let i: number = 0;
  while ((i < le)) {
    table.push("");
    i = (i + 1);
  }
  let n: number = 0;
  while ((n < le)) {
    i = 0;
    while ((i < le)) {
      table[i] = ((r).substring(i, (i + 1)) + table[i]);
      i = (i + 1);
    }
    table = sortStrings(table);
    n = (n + 1);
  }
  i = 0;
  while ((i < le)) {
    if (((table[i]).substring((le - 1), le) == etx)) {
      return (table[i]).substring(1, (le - 1));
    }
    i = (i + 1);
  }
  return "";
}
function makePrintable(s: string): string {
  let out: string = "";
  let i: number = 0;
  while ((i < Number(Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    let ch: string = (s).substring(i, (i + 1));
    if ((ch == stx)) {
      out = (out + "^");
    } else {
      if ((ch == etx)) {
        out = (out + "|");
      } else {
        out = (out + ch);
      }
    }
    i = (i + 1);
  }
  return out;
}
function main() {
  let examples: string[] = ["banana", "appellee", "dogwood", "TO BE OR NOT TO BE OR WANT TO BE OR NOT?", "SIX.MIXED.PIXIES.SIFT.SIXTY.PIXIE.DUST.BOXES", "\x02ABC\x03"];
  for (const t of examples) {
    console.log(_str(makePrintable(t)));
    let res: Record<string, any> = bwt(t);
    if (res.err) {
      console.log(_str(" --> ERROR: String can't contain STX or ETX"));
      console.log(_str(" -->"));
    } else {
      let enc: string = res.res;
      console.log(_str((" --> " + makePrintable(enc))));
      let r: string = ibwt(enc);
      console.log(_str((" --> " + r)));
    }
    console.log(_str(""));
  }
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

