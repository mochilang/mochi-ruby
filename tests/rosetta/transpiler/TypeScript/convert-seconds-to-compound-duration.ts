// Generated by Mochi v0.10.55 on 2025-08-02 17:55:50 GMT+7

function timeStr(sec: number): string {
  let wks: number = Math.trunc(sec / 604800);
  sec = (sec % 604800);
  let ds: number = Math.trunc(sec / 86400);
  sec = (sec % 86400);
  let hrs: number = Math.trunc(sec / 3600);
  sec = (sec % 3600);
  let mins: number = Math.trunc(sec / 60);
  sec = (sec % 60);
  let res: string = "";
  let comma: boolean = false;
  if ((wks != 0)) {
    res = ((res + _str(wks)) + " wk");
    comma = true;
  }
  if ((ds != 0)) {
    if (comma) {
      res = (res + ", ");
    }
    res = ((res + _str(ds)) + " d");
    comma = true;
  }
  if ((hrs != 0)) {
    if (comma) {
      res = (res + ", ");
    }
    res = ((res + _str(hrs)) + " hr");
    comma = true;
  }
  if ((mins != 0)) {
    if (comma) {
      res = (res + ", ");
    }
    res = ((res + _str(mins)) + " min");
    comma = true;
  }
  if ((sec != 0)) {
    if (comma) {
      res = (res + ", ");
    }
    res = ((res + _str(sec)) + " sec");
  }
  return res;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(timeStr(7259)));
  console.log(_str(timeStr(86400)));
  console.log(_str(timeStr(6000000)));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

