// Generated by Mochi v0.10.42 on 2025-07-28 10:39:52 GMT+7

function validComb(a: number, b: number, c: number, d: number, e: number, f: number, g: number): boolean {
  let square1: number = (a + b);
  let square2: number = ((b + c) + d);
  let square3: number = ((d + e) + f);
  let square4: number = (f + g);
  return (((square1 == square2) && (square2 == square3)) && (square3 == square4));
}
function isUnique(a: number, b: number, c: number, d: number, e: number, f: number, g: number): boolean {
  let nums: number[] = [a, b, c, d, e, f, g];
  let i: number = 0;
  while ((i < (Array.isArray(nums) || typeof nums === 'string' ? nums.length : Object.keys(nums ?? {}).length))) {
    let j: number = (i + 1);
    while ((j < (Array.isArray(nums) || typeof nums === 'string' ? nums.length : Object.keys(nums ?? {}).length))) {
      if ((nums[i] == nums[j])) {
        return false;
      }
      j = (j + 1);
    }
    i = (i + 1);
  }
  return true;
}
function getCombs(low: number, high: number, unique: boolean): Record<string, any> {
  let valid: any[] = [];
  let count: number = 0;
  for (let b = low; b < (high + 1); b++) {
    for (let c = low; c < (high + 1); c++) {
      for (let d = low; d < (high + 1); d++) {
        let s: number = ((b + c) + d);
        for (let e = low; e < (high + 1); e++) {
          for (let f = low; f < (high + 1); f++) {
            let a: number = (s - b);
            let g: number = (s - f);
            if (((a < low) || (a > high))) {
              continue
            }
            if (((g < low) || (g > high))) {
              continue
            }
            if ((((d + e) + f) != s)) {
              continue
            }
            if (((f + g) != s)) {
              continue
            }
            if ((!unique || isUnique(a, b, c, d, e, f, g))) {
              valid.push([a, b, c, d, e, f, g]);
              count = (count + 1);
            }
          }
        }
      }
    }
  }
  return {count, "list": valid};
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  let r1: Record<string, any> = getCombs(1, 7, true);
  console.log(_str((_str(r1.count) + " unique solutions in 1 to 7")));
  console.log(_str(r1.list));
  let r2: Record<string, any> = getCombs(3, 9, true);
  console.log(_str((_str(r2.count) + " unique solutions in 3 to 9")));
  console.log(_str(r2.list));
  let r3: Record<string, any> = getCombs(0, 9, false);
  console.log(_str((_str(r3.count) + " non-unique solutions in 0 to 9")));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

