// Generated by Mochi v0.10.56 on 2025-08-03 17:23:28 GMT+7

function indexOf(s: string, ch: string): number {
  let i: number = 0;
  while ((i < Number(Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length))) {
    if ((s.slice(i, (i + 1)) == ch)) {
      return i;
    }
    i = (i + 1);
  }
  return -1;
}
function set58(addr: string): number[] {
  let tmpl: string = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz";
  let a: number[] = [];
  let i: number = 0;
  while ((i < 25)) {
    a.push(0);
    i = (i + 1);
  }
  let idx: number = 0;
  while ((idx < Number(Array.isArray(addr) || typeof addr === 'string' ? addr.length : Object.keys(addr ?? {}).length))) {
    let ch: string = addr.slice(idx, (idx + 1));
    let c: number = tmpl.indexOf(ch);
    if ((c < 0)) {
      return [];
    }
    let j: number = 24;
    while ((j >= 0)) {
      c = (c + (58 * a[j]));
      a[j] = (c % 256);
      c = Math.trunc(Math.trunc(c / 256));
      j = (j - 1);
    }
    if ((c > 0)) {
      return [];
    }
    idx = (idx + 1);
  }
  return a;
}
function doubleSHA256(bs: number[]): number[] {
  let first = sha256(bs);
  return sha256(first);
}
function computeChecksum(a: number[]): number[] {
  let hash: number[] = doubleSHA256(a.slice(0, 21));
  return hash.slice(0, 4);
}
function validA58(addr: string): boolean {
  let a: number[] = set58(addr);
  if ((Number(Array.isArray(a) || typeof a === 'string' ? a.length : Object.keys(a ?? {}).length) != 25)) {
    return false;
  }
  if ((a[Math.trunc(0)] != 0)) {
    return false;
  }
  let sum = computeChecksum(a);
  let i: number = 0;
  while ((i < 4)) {
    if ((a[Math.trunc((21 + i))] != sum[i])) {
      return false;
    }
    i = (i + 1);
  }
  return true;
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().heapUsed ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().heapUsed;
  }
  return 0;
}
import { createHash } from 'node:crypto';
function sha256(bs: number[]): number[] {
  const hash = createHash('sha256');
  hash.update(new Uint8Array(bs));
  return Array.from(hash.digest());
}
function _str(x: any): string {
  if (typeof x === 'number') {
    if (Object.is(x, -0)) return '-0';
    if (x === Infinity) return '+Inf';
    if (x === -Infinity) return '-Inf';
    if (Number.isNaN(x)) return 'NaN';
  }
  return String(x);
}
(() => {
  globalThis.gc?.()
  const _startMem = _mem()
  const _start = _now()
  console.log(_str(_str(validA58("1AGNa15ZQXAZUgFiqJ3i7Z2DPU2J6hW62i"))));
  console.log(_str(_str(validA58("17NdbrSGoUotzeGCcMMCqnFkEvLymoou9j"))));
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  globalThis.gc?.()
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

