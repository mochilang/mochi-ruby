// Generated by Mochi v0.10.42 on 2025-07-27 21:45:10 GMT+7

function log2(x: number): number {
  let k: number = 0.0;
  let v: number = x;
  while ((v >= 2.0)) {
    v = (v / 2.0);
    k = (k + 1.0);
  }
  while ((v < 1.0)) {
    v = (v * 2.0);
    k = (k - 1.0);
  }
  let z: number = ((v - 1.0) / (v + 1.0));
  let zpow: number = z;
  let sum: number = z;
  let i: number = 3;
  while ((i <= 9)) {
    zpow = ((zpow * z) * z);
    sum = (sum + (zpow / i));
    i = (i + 2);
  }
  let ln2: number = 0.6931471805599453;
  return (k + ((2.0 * sum) / ln2));
}
function entropy(data: string): number {
  if ((data == "")) {
    return 0.0;
  }
  let counts: Record<string, number> = {};
  let i: number = 0;
  while ((i < (Array.isArray(data) || typeof data === 'string' ? data.length : Object.keys(data ?? {}).length))) {
    let ch: string = (data).substring(i, (i + 1));
    if ((ch in counts)) {
      counts[ch] = (counts[ch] + 1);
    } else {
      counts[ch] = 1;
    }
    i = (i + 1);
  }
  let e: number = 0.0;
  let l: number = (Array.isArray(data) || typeof data === 'string' ? data.length : Object.keys(data ?? {}).length);
  for (const ch in counts) {
    let px: number = (counts[ch] / l);
    if ((px > 0.0)) {
      e = (e - (px * log2(px)));
    }
  }
  return e;
}
let source: string = ((((((((((((((((((((((((((((((((((((((((((((((((((((("// Mochi translation of the Rosetta \"Entropy-Narcissist\" task\n" + "// Simplified to compute the entropy of this source string\n\n") + "fun log2(x: float): float {\n") + "  var k = 0.0\n") + "  var v = x\n") + "  while v >= 2.0 {\n") + "    v = v / 2.0\n") + "    k = k + 1.0\n") + "  }\n") + "  while v < 1.0 {\n") + "    v = v * 2.0\n") + "    k = k - 1.0\n") + "  }\n") + "  let z = (v - 1.0) / (v + 1.0)\n") + "  var zpow = z\n") + "  var sum = z\n") + "  var i = 3\n") + "  while i <= 9 {\n") + "    zpow = zpow * z * z\n") + "    sum = sum + zpow / (i as float)\n") + "    i = i + 2\n") + "  }\n") + "  let ln2 = 0.6931471805599453\n") + "  return k + 2.0 * sum / ln2\n") + "}\n\n") + "fun entropy(data: string): float {\n") + "  if data == \"\" { return 0.0 }\n") + "  var counts: map<string,int> = {}\n") + "  var i = 0\n") + "  while i < len(data) {\n") + "    let ch = substring(data, i, i+1)\n") + "    if ch in counts {\n") + "      counts[ch] = counts[ch] + 1\n") + "    } else {\n") + "      counts[ch] = 1\n") + "    }\n") + "    i = i + 1\n") + "  }\n") + "  var e = 0.0\n") + "  let l = len(data) as float\n") + "  for ch in counts {\n") + "    let px = (counts[ch] as float) / l\n") + "    if px > 0.0 {\n") + "      e = e - px * log2(px)\n") + "    }\n") + "  }\n") + "  return e\n") + "}\n\n") + "// Store the program source as a string constant\n") + "let source = ... // truncated in actual source\n") + "\nfun main() {\n") + "  print(\"Source file entropy: \" + str(entropy(source)))\n") + "}\n\n") + "main()\n");
function main() {
  console.log(("Source file entropy: " + String(entropy(source))));
}
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  main();
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

