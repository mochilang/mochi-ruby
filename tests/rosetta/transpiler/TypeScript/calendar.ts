// Generated by Mochi v0.10.42 on 2025-07-27 20:13:10 GMT+7

let daysInMonth: number[] = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
let start: number[] = [3, 6, 6, 2, 4, 0, 2, 5, 1, 3, 6, 1];
let months: string[] = [" January ", " February", "  March  ", "  April  ", "   May   ", "   June  ", "   July  ", "  August ", "September", " October ", " November", " December"];
let days: string[] = ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"];
let qtr: number = 0;
var _nowSeed = 0;
var _nowSeeded = false;
{
  let s = "";
  if (typeof Deno !== "undefined") {
    try {
      s = Deno.env.get("MOCHI_NOW_SEED") ?? "";
    } catch (_e) {
      s = "";
    }
  } else if (typeof process !== "undefined") {
    s = process.env.MOCHI_NOW_SEED || "";
  }
  if (s) {
    const v = parseInt(s, 10);
    if (!isNaN(v)) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
function _now(): number {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  if (typeof Deno !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  if (typeof performance !== 'undefined') {
    return Math.trunc(performance.now() * 1e6);
  }
  return Date.now() * 1000;
}
function _mem(): number {
  if (typeof Deno !== 'undefined') {
    return (Deno.memoryUsage?.().rss ?? 0);
  }
  if (typeof process !== 'undefined') {
    return process.memoryUsage().rss;
  }
  return 0;
}
function _stdout_write(s: string) {
  if (typeof Deno !== 'undefined') {
    Deno.stdout.writeSync(new TextEncoder().encode(s));
  } else if (typeof process !== 'undefined') {
    process.stdout.write(s);
  } else {
    console.log(s);
  }
}
(() => {
  const _startMem = _mem()
  const _start = _now()
  console.log("                                [SNOOPY]\n");
  console.log("                                  1969\n");
  while ((qtr < 4)) {
    let mi: number = 0;
    while ((mi < 3)) {
      _stdout_write((("      " + months[Math.trunc(((qtr * 3) + mi))]) + "           "));
      mi = (mi + 1);
    }
    console.log("");
    mi = 0;
    while ((mi < 3)) {
      let d: number = 0;
      while ((d < 7)) {
        _stdout_write((" " + days[d]));
        d = (d + 1);
      }
      _stdout_write("     ");
      mi = (mi + 1);
    }
    console.log("");
    let week: number = 0;
    while ((week < 6)) {
      mi = 0;
      while ((mi < 3)) {
        let day: number = 0;
        while ((day < 7)) {
          let m: number = ((qtr * 3) + mi);
          let val: number = ((((week * 7) + day) - start[m]) + 1);
          if (((val >= 1) && (val <= daysInMonth[m]))) {
            let s: string = String(val);
            if (((Array.isArray(s) || typeof s === 'string' ? s.length : Object.keys(s ?? {}).length) == 1)) {
              s = (" " + s);
            }
            _stdout_write((" " + s));
          } else {
            _stdout_write("   ");
          }
          day = (day + 1);
        }
        _stdout_write("     ");
        mi = (mi + 1);
      }
      console.log("");
      week = (week + 1);
    }
    console.log("");
    qtr = (qtr + 1);
  }
  const _end = _now()
  const _duration = _end - _start
  const _duration_us = Math.trunc(_duration / 1000)
  const _endMem = _mem()
  const _memory_bytes = Math.max(0, _endMem - _startMem)
  console.log(JSON.stringify({
    "duration_us": _duration_us,
    "memory_bytes": _memory_bytes,
    "name": "main"
  }, null, "  "))
})();

