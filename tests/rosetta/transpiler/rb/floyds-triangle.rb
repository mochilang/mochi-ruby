# Generated by Mochi transpiler v0.10.50 on 2025-07-30 23:34 +0700

def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

def floyd(n)
  puts(_add(_add("Floyd ", (n).to_s), ":"))
  lowerLeftCorner = _add(n * (n - 1) / 2, 1)
  lastInColumn = lowerLeftCorner
  lastInRow = 1
  i = 1
  row = 1
  line = ""
  while row <= n
    w = (lastInColumn).to_s.length
    if i < lastInRow
      line = _add(_add(line, pad((i).to_s, w)), " ")
      lastInColumn = _add(lastInColumn, 1)
    else
      line = _add(line, pad((i).to_s, w))
      puts(line)
      line = ""
      row = _add(row, 1)
      lastInRow = _add(lastInRow, row)
      lastInColumn = lowerLeftCorner
    end
    i = _add(i, 1)
  end
end
def pad(s, w)
  t = s
  while t.length < w
    t = _add(" ", t)
  end
  return t
end
floyd(5)
floyd(14)
