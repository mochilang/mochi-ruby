# Generated by Mochi transpiler v0.10.50 on 2025-07-31 07:51 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  $n = 15
  $t = []
  (0...(_add($n, 2))).each do |_|
    $t = $t + [0]
  end
  $t[1] = 1
  (1...(_add($n, 1))).each do |i|
    j = i
    while j > 1
      $t[j] = _add($t[j], $t[j - 1])
      j = j - 1
    end
    $t[((_add(i, 1))).to_i] = $t[i]
    j = _add(i, 1)
    while j > 1
      $t[j] = _add($t[j], $t[j - 1])
      j = j - 1
    end
    cat = $t[_add(i, 1)] - $t[i]
    if i < 10
      puts(_add(_add(_add(" ", (i).to_s), " : "), (cat).to_s))
    else
      puts(_add(_add((i).to_s, " : "), (cat).to_s))
    end
  end
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
