# Generated by Mochi transpiler v0.10.40 on 2025-07-25 20:07 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def Node(data)
    return {"Data" => data, "Balance" => 0, "Link" => [nil, nil]}
  end
  def getLink(n, dir)
    return (n["Link"])[dir]
  end
  def setLink(n, dir, v)
    links = n["Link"]
    links[dir] = v
    n["Link"] = links
  end
  def opp(dir)
    return 1 - dir
  end
  def single(root, dir)
    tmp = getLink(root, opp(dir))
    setLink(root, opp(dir), getLink(tmp, dir))
    setLink(tmp, dir, root)
    return tmp
  end
  def double(root, dir)
    tmp = getLink(getLink(root, opp(dir)), dir)
    setLink(getLink(root, opp(dir)), dir, getLink(tmp, opp(dir)))
    setLink(tmp, opp(dir), getLink(root, opp(dir)))
    setLink(root, opp(dir), tmp)
    tmp = getLink(root, opp(dir))
    setLink(root, opp(dir), getLink(tmp, dir))
    setLink(tmp, dir, root)
    return tmp
  end
  def adjustBalance(root, dir, bal)
    n = getLink(root, dir)
    nn = getLink(n, opp(dir))
    if nn["Balance"] == 0
      root["Balance"] = 0
      n["Balance"] = 0
    else
      if nn["Balance"] == bal
        root["Balance"] = -bal
        n["Balance"] = 0
      else
        root["Balance"] = 0
        n["Balance"] = bal
      end
    end
    nn["Balance"] = 0
  end
  def insertBalance(root, dir)
    n = getLink(root, dir)
    bal = 2 * dir - 1
    if n["Balance"] == bal
      root["Balance"] = 0
      n["Balance"] = 0
      return single(root, opp(dir))
    end
    adjustBalance(root, dir, bal)
    return double(root, opp(dir))
  end
  def insertR(root, data)
    if root == nil
      return {"node" => Node(data), "done" => false}
    end
    node = root
    dir = 0
    if ((node["Data"]).to_i) < data
      dir = 1
    end
    r = insertR(getLink(node, dir), data)
    setLink(node, dir, r["node"])
    if r["done"]
      return {"node" => node, "done" => true}
    end
    node["Balance"] = _add(((node["Balance"]).to_i), (2 * dir - 1))
    if node["Balance"] == 0
      return {"node" => node, "done" => true}
    end
    if node["Balance"] == 1 || node["Balance"] == (-1)
      return {"node" => node, "done" => false}
    end
    return {"node" => insertBalance(node, dir), "done" => true}
  end
  def Insert(tree, data)
    r = insertR(tree, data)
    return r["node"]
  end
  def removeBalance(root, dir)
    n = getLink(root, opp(dir))
    bal = 2 * dir - 1
    if n["Balance"] == (-bal)
      root["Balance"] = 0
      n["Balance"] = 0
      return {"node" => single(root, dir), "done" => false}
    end
    if n["Balance"] == bal
      adjustBalance(root, opp(dir), (-bal))
      return {"node" => double(root, dir), "done" => false}
    end
    root["Balance"] = -bal
    n["Balance"] = bal
    return {"node" => single(root, dir), "done" => true}
  end
  def removeR(root, data)
    if root == nil
      return {"node" => nil, "done" => false}
    end
    node = root
    if ((node["Data"]).to_i) == data
      if getLink(node, 0) == nil
        return {"node" => getLink(node, 1), "done" => false}
      end
      if getLink(node, 1) == nil
        return {"node" => getLink(node, 0), "done" => false}
      end
      heir = getLink(node, 0)
      while getLink(heir, 1) != nil
        heir = getLink(heir, 1)
      end
      node["Data"] = heir["Data"]
      data = (heir["Data"]).to_i
    end
    dir = 0
    if ((node["Data"]).to_i) < data
      dir = 1
    end
    r = removeR(getLink(node, dir), data)
    setLink(node, dir, r["node"])
    if r["done"]
      return {"node" => node, "done" => true}
    end
    node["Balance"] = _add(((node["Balance"]).to_i), 1) - 2 * dir
    if node["Balance"] == 1 || node["Balance"] == (-1)
      return {"node" => node, "done" => true}
    end
    if node["Balance"] == 0
      return {"node" => node, "done" => false}
    end
    return removeBalance(node, dir)
  end
  def Remove(tree, data)
    r = removeR(tree, data)
    return r["node"]
  end
  def indentStr(n)
    s = ""
    i = 0
    while i < n
      s = _add(s, " ")
      i = _add(i, 1)
    end
    return s
  end
  def dumpNode(node, indent, comma)
    sp = indentStr(indent)
    if node == nil
      line = _add(sp, "null")
      if comma
        line = _add(line, ",")
      end
      puts(line)
    else
      puts(_add(sp, "{"))
      puts(_add(_add(_add(indentStr(_add(indent, 3)), "\"Data\": "), (node["Data"]).to_s), ","))
      puts(_add(_add(_add(indentStr(_add(indent, 3)), "\"Balance\": "), (node["Balance"]).to_s), ","))
      puts(_add(indentStr(_add(indent, 3)), "\"Link\": ["))
      dumpNode(getLink(node, 0), _add(indent, 6), true)
      dumpNode(getLink(node, 1), _add(indent, 6), false)
      puts(_add(indentStr(_add(indent, 3)), "]"))
      end_ = _add(sp, "}")
      if comma
        end_ = _add(end_, ",")
      end
      puts(end_)
    end
  end
  def dump(node, indent)
    dumpNode(node, indent, false)
  end
  def main()
    tree = nil
    puts("Empty tree:")
    dump(tree, 0)
    puts("")
    puts("Insert test:")
    tree = Insert(tree, 3)
    tree = Insert(tree, 1)
    tree = Insert(tree, 4)
    tree = Insert(tree, 1)
    tree = Insert(tree, 5)
    dump(tree, 0)
    puts("")
    puts("Remove test:")
    tree = Remove(tree, 3)
    tree = Remove(tree, 1)
    t = tree
    t["Balance"] = 0
    tree = t
    dump(tree, 0)
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
