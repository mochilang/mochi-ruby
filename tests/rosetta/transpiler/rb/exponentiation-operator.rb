# Generated by Mochi transpiler v0.10.50 on 2025-07-31 00:15 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def printExpI(b, p)
    if p < 0
      puts(_add(_add(_add((b).to_s, "^"), (p).to_s), ": negative power not allowed"))
      return
    end
    r = 1
    i = 1
    while i <= p
      r = r * b
      i = _add(i, 1)
    end
    puts(_add(_add(_add(_add((b).to_s, "^"), (p).to_s), ": "), (r).to_s))
  end
  def expF(b, p)
    neg = false
    if p < 0
      neg = true
      p = -p
    end
    r = 1.0
    pow = b
    while p > 0
      if p % 2 == 1
        r = r * pow
      end
      pow = pow * pow
      p = p / 2
    end
    if neg
      r = 1.0 / r
    end
    return r
  end
  def printExpF(b, p)
    if b == 0.0 && p < 0
      puts(_add(_add(_add((b).to_s, "^"), (p).to_s), ": +Inf"))
      return
    end
    puts(_add(_add(_add(_add((b).to_s, "^"), (p).to_s), ": "), (expF(b, p)).to_s))
  end
  puts("expI tests")
  printExpI(2, 10)
  printExpI(2, -10)
  printExpI(-2, 10)
  printExpI(-2, 11)
  printExpI(11, 0)
  puts("overflow undetected")
  printExpI(10, 10)
  puts("\nexpF tests:")
  printExpF(2.0, 10)
  printExpF(2.0, -10)
  printExpF(-2.0, 10)
  printExpF(-2.0, 11)
  printExpF(11.0, 0)
  puts("disallowed in expI, allowed here")
  printExpF(0.0, -1)
  puts("other interesting cases for 32 bit float type")
  printExpF(10.0, 39)
  printExpF(10.0, -39)
  printExpF(-10.0, 39)
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
