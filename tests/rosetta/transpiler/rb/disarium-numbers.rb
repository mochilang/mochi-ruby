# Generated by Mochi transpiler v0.10.42 on 2025-07-28 10:03 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def pow(base, exp)
    result = 1
    i = 0
    while i < exp
      result = result * base
      i = _add(i, 1)
    end
    return result
  end
  def isDisarium(n)
    digits = []
    x = n
    if x == 0
      digits = digits + [0]
    end
    while x > 0
      digits = digits + [x % 10]
      x = ((x / 10)).to_i
    end
    sum = 0
    pos = 1
    i = digits.length - 1
    while i >= 0
      sum = _add(sum, pow(digits[i], pos))
      pos = _add(pos, 1)
      i = i - 1
    end
    return sum == n
  end
  def main()
    count = 0
    n = 0
    while count < 19 && n < 3000000
      if isDisarium(n)
        puts((n).to_s)
        count = _add(count, 1)
      end
      n = _add(n, 1)
    end
    puts(_add(_add("\nFound the first ", (count).to_s), " Disarium numbers."))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
