# Generated by Mochi transpiler v0.10.42 on 2025-07-28 00:12 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def sinApprox(x)
    term = x
    sum = x
    n = 1
    while n <= 10
      denom = (((2 * n) * (_add(2 * n, 1)))).to_f
      term = -term * x * x / denom
      sum = _add(sum, term)
      n = _add(n, 1)
    end
    return sum
  end
  def cosApprox(x)
    term = 1.0
    sum = 1.0
    n = 1
    while n <= 10
      denom = (((2 * n - 1) * (2 * n))).to_f
      term = -term * x * x / denom
      sum = _add(sum, term)
      n = _add(n, 1)
    end
    return sum
  end
  $PI = 3.141592653589793
  $degreesIncr = 0.1 * $PI / 180.0
  $turns = 2.0
  $stop = 360.0 * $turns * 10.0 * $degreesIncr
  $width = 600.0
  $centre = $width / 2.0
  $a = 1.0
  $b = 20.0
  $theta = 0.0
  $count = 0
  while $theta < $stop
    $r = _add($a, $b * $theta)
    $x = $r * cosApprox($theta)
    $y = $r * sinApprox($theta)
    if $count % 100 == 0
      puts(_add(_add((_add($centre, $x)).to_s, ","), ($centre - $y).to_s))
    end
    $theta = _add($theta, $degreesIncr)
    $count = _add($count, 1)
  end
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
