# Generated by Mochi transpiler v0.10.42 on 2025-07-27 23:54 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def isPrime(n)
    if n < 2
      return false
    end
    if n % 2 == 0
      return n == 2
    end
    if n % 3 == 0
      return n == 3
    end
    d = 5
    while d * d <= n
      if n % d == 0
        return false
      end
      d = _add(d, 2)
      if n % d == 0
        return false
      end
      d = _add(d, 4)
    end
    return true
  end
  def sumDigits(n)
    s = 0
    x = n
    while x > 0
      s = _add(s, x % 10)
      x = ((x / 10)).to_i
    end
    return s
  end
  def pad(n)
    if n < 10
      return _add("  ", (n).to_s)
    end
    if n < 100
      return _add(" ", (n).to_s)
    end
    return (n).to_s
  end
  def main()
    puts("Additive primes less than 500:")
    count = 0
    line = ""
    lineCount = 0
    i = 2
    while i < 500
      if isPrime(i) && isPrime(sumDigits(i))
        count = _add(count, 1)
        line = _add(_add(line, pad(i)), "  ")
        lineCount = _add(lineCount, 1)
        if lineCount == 10
          puts(line[0...line.length - 2])
          line = ""
          lineCount = 0
        end
      end
      if i > 2
        i = _add(i, 2)
      else
        i = _add(i, 1)
      end
    end
    if lineCount > 0
      puts(line[0...line.length - 2])
    end
    puts(_add((count).to_s, " additive primes found."))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
