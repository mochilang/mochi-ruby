# Generated by Mochi transpiler v0.10.42 on 2025-07-28 00:12 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def d2d(d)
    return d % 360.0
  end
  def g2g(g)
    return g % 400.0
  end
  def m2m(m)
    return m % 6400.0
  end
  def r2r(r)
    return r % (2.0 * 3.141592653589793)
  end
  def d2g(d)
    return d2d(d) * 400.0 / 360.0
  end
  def d2m(d)
    return d2d(d) * 6400.0 / 360.0
  end
  def d2r(d)
    return d2d(d) * 3.141592653589793 / 180.0
  end
  def g2d(g)
    return g2g(g) * 360.0 / 400.0
  end
  def g2m(g)
    return g2g(g) * 6400.0 / 400.0
  end
  def g2r(g)
    return g2g(g) * 3.141592653589793 / 200.0
  end
  def m2d(m)
    return m2m(m) * 360.0 / 6400.0
  end
  def m2g(m)
    return m2m(m) * 400.0 / 6400.0
  end
  def m2r(m)
    return m2m(m) * 3.141592653589793 / 3200.0
  end
  def r2d(r)
    return r2r(r) * 180.0 / 3.141592653589793
  end
  def r2g(r)
    return r2r(r) * 200.0 / 3.141592653589793
  end
  def r2m(r)
    return r2r(r) * 3200.0 / 3.141592653589793
  end
  def main()
    angles = [-2.0, -1.0, 0.0, 1.0, 2.0, 6.2831853, 16.0, 57.2957795, 359.0, 399.0, 6399.0, 1000000.0]
    puts("degrees normalized_degs gradians mils radians")
    angles.each do |a|
      puts(_add(_add(_add(_add(_add(_add(_add(_add((a).to_s, " "), (d2d(a)).to_s), " "), (d2g(a)).to_s), " "), (d2m(a)).to_s), " "), (d2r(a)).to_s))
    end
    puts("\ngradians normalized_grds degrees mils radians")
    angles.each do |a|
      puts(_add(_add(_add(_add(_add(_add(_add(_add((a).to_s, " "), (g2g(a)).to_s), " "), (g2d(a)).to_s), " "), (g2m(a)).to_s), " "), (g2r(a)).to_s))
    end
    puts("\nmils normalized_mils degrees gradians radians")
    angles.each do |a|
      puts(_add(_add(_add(_add(_add(_add(_add(_add((a).to_s, " "), (m2m(a)).to_s), " "), (m2d(a)).to_s), " "), (m2g(a)).to_s), " "), (m2r(a)).to_s))
    end
    puts("\nradians normalized_rads degrees gradians mils")
    angles.each do |a|
      puts(_add(_add(_add(_add(_add(_add(_add(_add((a).to_s, " "), (r2r(a)).to_s), " "), (r2d(a)).to_s), " "), (r2g(a)).to_s), " "), (r2m(a)).to_s))
    end
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
