# Generated by Mochi transpiler v0.10.40 on 2025-07-25 20:07 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  def powf(base, exp)
    result = 1.0
    i = 0
    while i < exp
      result = result * base
      i = _add(i, 1)
    end
    return result
  end
  def nthRoot(x, n)
    low = 0.0
    high = x
    i = 0
    while i < 60
      mid = (_add(low, high)) / 2.0
      if powf(mid, n) > x
        high = mid
      else
        low = mid
      end
      i = _add(i, 1)
    end
    return low
  end
  def main()
    sum = 0.0
    sumRecip = 0.0
    prod = 1.0
    n = 1
    while n <= 10
      f = (n).to_f
      sum = _add(sum, f)
      sumRecip = _add(sumRecip, 1.0 / f)
      prod = prod * f
      n = _add(n, 1)
    end
    count = 10.0
    a = sum / count
    g = nthRoot(prod, 10)
    h = count / sumRecip
    puts(_add(_add(_add(_add(_add("A: ", (a).to_s), " G: "), (g).to_s), " H: "), (h).to_s))
    puts(_add("A >= G >= H: ", (a >= g && g >= h).to_s))
  end
  main()
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
