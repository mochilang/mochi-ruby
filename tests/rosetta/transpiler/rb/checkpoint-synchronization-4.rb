# Generated by Mochi transpiler v0.10.50 on 2025-07-31 07:51 +0700
require 'json'

$now_seed = 0
$now_seeded = false
s = ENV['MOCHI_NOW_SEED']
if s && s != ''
  begin
    $now_seed = Integer(s)
    $now_seeded = true
  rescue StandardError
  end
end
def _now()
  if $now_seeded
    $now_seed = ($now_seed * 1664525 + 1013904223) % 2147483647
    $now_seed
  else
    Process.clock_gettime(Process::CLOCK_MONOTONIC, :nanosecond)
  end
end


require 'objspace'
def _mem()
  ObjectSpace.memsize_of_all
end


def _add(a, b)
  if a.is_a?(Array) && b.is_a?(String)
    a.join + b
  elsif a.is_a?(String) && b.is_a?(Array)
    a + b.join
  else
    a + b
  end
end


def _padStart(s, len, ch)
  s.to_s.rjust(len, ch)
end

start_mem = _mem()
start = _now()
  $nMech = 5
  $detailsPerMech = 4
  (1...(_add($nMech, 1))).each do |mech|
    id = mech
    puts(_add(_add(_add(_add("worker ", (id).to_s), " contracted to assemble "), ($detailsPerMech).to_s), " details"))
    puts(_add(_add("worker ", (id).to_s), " enters shop"))
    d = 0
    while d < $detailsPerMech
      puts(_add(_add("worker ", (id).to_s), " assembling"))
      puts(_add(_add("worker ", (id).to_s), " completed detail"))
      d = _add(d, 1)
    end
    puts(_add(_add("worker ", (id).to_s), " leaves shop"))
    puts(_add(_add("mechanism ", (mech).to_s), " completed"))
  end
end_time = _now()
end_mem = _mem()
result = {"duration_us" => ((end_time - start) / 1000), "memory_bytes" => (end_mem - start_mem), "name" => "main"}
puts(JSON.pretty_generate(result))
