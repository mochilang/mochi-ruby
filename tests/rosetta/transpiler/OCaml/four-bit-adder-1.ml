(* Generated by Mochi transpiler v0.10.57 on 2025-08-04 16:02 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then
    let i = (magic v : int) in
    if i = 0 || i = 1 then string_of_bool (i <> 0)
    else string_of_int i
  else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Return

let rec xor a b =
  let __ret = ref false in
  (try
  __ret := (Obj.magic (((a && not (b)) || (not (a) && b))) : bool); raise Return
  with Return -> !__ret)

and ha a b =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  __ret := (Obj.magic ([("s", Obj.repr (xor (a) (b))); ("c", Obj.repr ((a && b)))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and fa a b c0 =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let r1 = ha (a) (c0) in
  let r2 = ha ((Obj.obj (List.assoc ("s") (r1) : Obj.t) : bool)) (b) in
  __ret := (Obj.magic ([("s", Obj.repr ((Obj.obj (List.assoc ("s") (r2) : Obj.t) : bool))); ("c", Obj.repr (((Obj.obj (List.assoc ("c") (r1) : Obj.t) : bool) || (Obj.obj (List.assoc ("c") (r2) : Obj.t) : bool))))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and add4 a3 a2 a1 a0 b3 b2 b1 b0 =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let r0 = fa (a0) (b0) (false) in
  let r1 = fa (a1) (b1) ((Obj.obj (List.assoc ("c") (r0) : Obj.t) : bool)) in
  let r2 = fa (a2) (b2) ((Obj.obj (List.assoc ("c") (r1) : Obj.t) : bool)) in
  let r3 = fa (a3) (b3) ((Obj.obj (List.assoc ("c") (r2) : Obj.t) : bool)) in
  __ret := (Obj.magic ([("v", Obj.repr ((Obj.obj (List.assoc ("c") (r3) : Obj.t) : bool))); ("s3", Obj.repr ((Obj.obj (List.assoc ("s") (r3) : Obj.t) : bool))); ("s2", Obj.repr ((Obj.obj (List.assoc ("s") (r2) : Obj.t) : bool))); ("s1", Obj.repr ((Obj.obj (List.assoc ("s") (r1) : Obj.t) : bool))); ("s0", Obj.repr ((Obj.obj (List.assoc ("s") (r0) : Obj.t) : bool)))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and b2i b =
  let __ret = ref 0 in
  (try
  if b then (
  __ret := (Obj.magic (1) : int); raise Return
  );
  __ret := (Obj.magic (0) : int); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let r = add4 (true) (false) (true) (false) (true) (false) (false) (true) in
  print_endline ((((((((((string_of_int (b2i ((Obj.obj (List.assoc ("v") (r) : Obj.t) : bool)))) ^ " ") ^ (string_of_int (b2i ((Obj.obj (List.assoc ("s3") (r) : Obj.t) : bool))))) ^ " ") ^ (string_of_int (b2i ((Obj.obj (List.assoc ("s2") (r) : Obj.t) : bool))))) ^ " ") ^ (string_of_int (b2i ((Obj.obj (List.assoc ("s1") (r) : Obj.t) : bool))))) ^ " ") ^ (string_of_int (b2i ((Obj.obj (List.assoc ("s0") (r) : Obj.t) : bool))))));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()