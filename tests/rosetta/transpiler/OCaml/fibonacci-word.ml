(* Generated by Mochi transpiler v0.10.52 on 2025-08-01 18:57 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec entropy s =
  let __ret = ref 0.0 in
  (try
  let counts = ref ([]) in
  let i = ref (0) in
  (try while (!i < String.length (s)) do
    try
  let ch = String.sub (s) !i ((!i + 1) - !i) in
  if (List.mem_assoc ch !counts) then (
  counts := ((ch, ((try List.assoc (ch) !counts with Not_found -> (Obj.magic 0)) + 1)) :: List.remove_assoc ch !counts);
  ) else (
  counts := ((ch, 1) :: List.remove_assoc ch !counts);
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let hm = ref (0.0) in
  (try List.iter (fun k ->
    try
  let c = float_of_int ((try List.assoc (k) !counts with Not_found -> (Obj.magic 0))) in
  hm := (!hm +. (c *. (log (c) /. log (2.0))));
    with Continue -> ()) ((List.map fst !counts)) with Break -> ());
  let l = float_of_int (String.length (s)) in
  __ret := (Obj.magic (((log (l) /. log (2.0)) -. (!hm /. l))) : float); raise Return
  with Return -> !__ret)

and fibonacciWord n =
  let __ret = ref "" in
  (try
  let n = (Obj.magic n : int) in
  let a = ref ("1") in
  let b = ref ("0") in
  let i = ref (1) in
  (try while (!i < n) do
    try
  let tmp = !b in
  b := (!b ^ !a);
  a := tmp;
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!a) : string); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  print_endline (((pad ("N") (Obj.repr (3)) ^ pad ("Length") (Obj.repr (9))) ^ "  Entropy      Word"));
  let n = ref (1) in
  (try while (!n < 10) do
    try
  let s = fibonacciWord (Obj.repr (!n)) in
  print_endline ((((((pad ((string_of_int (!n))) (Obj.repr (3)) ^ pad ((string_of_int (String.length (s)))) (Obj.repr (9))) ^ "  ") ^ fmt (Obj.repr (entropy (s)))) ^ "  ") ^ s));
  n := (!n + 1);
    with Continue -> ()
  done with Break -> ());
  (try while (!n <= 37) do
    try
  let s = fibonacciWord (Obj.repr (!n)) in
  print_endline ((((pad ((string_of_int (!n))) (Obj.repr (3)) ^ pad ((string_of_int (String.length (s)))) (Obj.repr (9))) ^ "  ") ^ fmt (Obj.repr (entropy (s)))));
  n := (!n + 1);
    with Continue -> ()
  done with Break -> ());
    !__ret
  with Return -> !__ret)

and pad s w =
  let __ret = ref "" in
  (try
  let w = (Obj.magic w : int) in
  let t = ref (s) in
  (try while (String.length (!t) < w) do
    try
  t := (" " ^ !t);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!t) : string); raise Return
  with Return -> !__ret)

and fmt x =
  let __ret = ref "" in
  (try
  let x = (Obj.magic x : float) in
  let y = ref ((floorf (Obj.repr (((x *. 100000000.0) +. 0.5))) /. 100000000.0)) in
  let s = ref ((Printf.sprintf "%.16g" (!y))) in
  let dot = ref ((try String.index (!s) '.' with Not_found -> -1)) in
  if (!dot = (0 - 1)) then (
  s := (!s ^ ".00000000");
  ) else (
  let d = ref (((String.length (!s) - !dot) - 1)) in
  (try while (!d < 8) do
    try
  s := (!s ^ "0");
  d := (!d + 1);
    with Continue -> ()
  done with Break -> ());
  );
  __ret := (Obj.magic (!s) : string); raise Return
  with Return -> !__ret)

and floorf x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  let y = (Obj.magic x : int) in
  __ret := (Obj.magic (float_of_int (y)) : float); raise Return
  with Return -> !__ret)

and indexOf s ch =
  let __ret = ref 0 in
  (try
  let i = ref (0) in
  (try while (!i < String.length (s)) do
    try
  if (String.sub (s) !i ((!i + 1) - !i) = ch) then (
  __ret := (Obj.magic (!i) : int); raise Return
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ((0 - 1)) : int); raise Return
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()