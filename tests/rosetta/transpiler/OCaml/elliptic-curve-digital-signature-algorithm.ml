(* Generated by Mochi transpiler v0.10.56 on 2025-08-03 21:17 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then
    let i = (magic v : int) in
    if i = 0 || i = 1 then string_of_bool (i <> 0)
    else string_of_int i
  else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Return

let rec main () =
  let __ret = ref (Obj.magic 0) in
  (try
  print_endline ("Private key:\nD: 1234567890");
  print_endline ("\nPublic key:");
  print_endline ("X: 43162711582587979080031819627904423023685561091192625653251495188141318209988");
  print_endline ("Y: 86807430002474105664458509423764867536342689150582922106807036347047552480521");
  print_endline ("\nMessage: Rosetta Code");
  print_endline ("Hash   : 0xe6f9ed0d");
  print_endline ("\nSignature:");
  print_endline ("R: 23195197793674669608400023921033380707595656606710353926508630347378485682379");
  print_endline ("S: 79415614279862633473653728365954499259635019180091322566320325357594590761922");
  print_endline ("\nSignature verified: true");
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()