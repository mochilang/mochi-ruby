(* Generated by Mochi transpiler v0.10.37 on 2025-07-22 22:31 +0700 *)

exception Break
exception Continue

let () =
  let result = ref "" in
  (try for i = 1 to (101 - 1) do
    try
  let j = ref 1 in
  (try while ((!j * !j) < i) do
    try
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  if ((!j * !j) == i) then (
  result := (!result ^ "O");
  ) else (
  result := (!result ^ "-");
  );
    with Continue -> ()
  done with Break -> ());
  print_endline !result;
