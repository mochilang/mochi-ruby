(* Generated by Mochi transpiler v0.10.52 on 2025-08-01 15:22 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let _inf = 1000000
let n = 4
let i = ref (0)
let rec floydWarshall graph =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let n = List.length (graph) in
  let dist = ref (([] : int list list)) in
  let next = ref (([] : int list list)) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  let drow = ref (([] : int list)) in
  let nrow = ref (([] : int list)) in
  let j = ref (0) in
  (try while (!j < n) do
    try
  drow := (List.append (!drow) [(Obj.magic (List.nth (List.nth (graph) (!i)) (!j)) : int)]);
  if ((List.nth (List.nth (graph) (!i)) (!j) < _inf) && (!i <> !j)) then (
  nrow := (List.append (!nrow) [(Obj.magic (!j) : int)]);
  ) else (
  nrow := (List.append (!nrow) [(Obj.magic (-(1)) : int)]);
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  dist := (List.append (!dist) [(Obj.magic (!drow) : int list)]);
  next := (List.append (!next) [(Obj.magic (!nrow) : int list)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let k = ref (0) in
  (try while (!k < n) do
    try
  let i = ref (0) in
  (try while (!i < n) do
    try
  let j = ref (0) in
  (try while (!j < n) do
    try
  if ((List.nth (List.nth (!dist) (!i)) (!k) < _inf) && (List.nth (List.nth (!dist) (!k)) (!j) < _inf)) then (
  let alt = (List.nth (List.nth (!dist) (!i)) (!k) + List.nth (List.nth (!dist) (!k)) (!j)) in
  if (alt < List.nth (List.nth (!dist) (!i)) (!j)) then (
  dist := (List.mapi (fun __i __x -> if __i = !i then (List.mapi (fun __i __x -> if __i = !j then alt else __x) (List.nth (!dist) (!i))) else __x) (!dist));
  next := (List.mapi (fun __i __x -> if __i = !i then (List.mapi (fun __i __x -> if __i = !j then List.nth (List.nth (!next) (!i)) (!k) else __x) (List.nth (!next) (!i))) else __x) (!next));
  );
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic ([("dist", Obj.repr (!dist)); ("next", Obj.repr (!next))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and path u v next =
  let __ret = ref ([] : int list) in
  (try
  let u = (Obj.magic u : int) in
  let v = (Obj.magic v : int) in
  if (List.nth (List.nth (next) (u)) (v) < 0) then (
  __ret := (Obj.magic ([]) : int list); raise Return
  );
  let p = ref ([u]) in
  let x = ref (u) in
  (try while (!x <> v) do
    try
  x := List.nth (List.nth (next) (!x)) (v);
  p := (List.append (!p) [(Obj.magic (!x) : int)]);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!p) : int list); raise Return
  with Return -> !__ret)

and pathStr p =
  let __ret = ref "" in
  (try
  let s = ref ("") in
  let i = ref (0) in
  (try while (!i < List.length (p)) do
    try
  s := (!s ^ (string_of_int ((List.nth (p) (!i) + 1))));
  if (!i < (List.length (p) - 1)) then (
  s := (!s ^ " -> ");
  );
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!s) : string); raise Return
  with Return -> !__ret)


let () =
  let g = ref (([] : int list list)) in
  let mem_start = _mem () in
  let start = _now () in
  (try for i = 0 to (n - 1) do
    try
  let row = ref (([] : int list)) in
  (try for j = 0 to (n - 1) do
    try
  if (i = j) then (
  row := (List.append (!row) [(Obj.magic (0) : int)]);
  ) else (
  row := (List.append (!row) [(Obj.magic (_inf) : int)]);
  );
    with Continue -> ()
  done with Break -> ());
  g := (List.append (!g) [(Obj.magic (!row) : int list)]);
    with Continue -> ()
  done with Break -> ());
  g := (List.mapi (fun __i __x -> if __i = 0 then (List.mapi (fun __i __x -> if __i = 2 then -(2) else __x) (List.nth (!g) (0))) else __x) (!g));
  g := (List.mapi (fun __i __x -> if __i = 2 then (List.mapi (fun __i __x -> if __i = 3 then 2 else __x) (List.nth (!g) (2))) else __x) (!g));
  g := (List.mapi (fun __i __x -> if __i = 3 then (List.mapi (fun __i __x -> if __i = 1 then -(1) else __x) (List.nth (!g) (3))) else __x) (!g));
  g := (List.mapi (fun __i __x -> if __i = 1 then (List.mapi (fun __i __x -> if __i = 0 then 4 else __x) (List.nth (!g) (1))) else __x) (!g));
  g := (List.mapi (fun __i __x -> if __i = 1 then (List.mapi (fun __i __x -> if __i = 2 then 3 else __x) (List.nth (!g) (1))) else __x) (!g));
  let res = floydWarshall (!g) in
  print_endline ("pair\tdist\tpath");
  (try while (!i < n) do
    try
  let j = ref (0) in
  (try while (!j < n) do
    try
  if (!i <> !j) then (
  let p = ref (path (!i) (!j) ((Obj.obj (List.assoc ("next") res) : int list list))) in
  print_endline ((((((((string_of_int ((!i + 1))) ^ " -> ") ^ (string_of_int ((!j + 1)))) ^ "\t") ^ (string_of_int (List.nth (List.nth ((Obj.obj (List.assoc ("dist") res) : int list list)) (!i)) (!j)))) ^ "\t") ^ pathStr (!p)));
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()