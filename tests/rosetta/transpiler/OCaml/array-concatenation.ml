(* Generated by Mochi transpiler v0.10.42 on 2025-07-27 09:26 UTC *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec concatInts a b =
  let __ret = ref ([] : int list) in
  (try
  let out = ref ([]) in
  (try List.iter (fun v ->
    try
  out := List.append !out [v];
    with Continue -> ()) a with Break -> ());
  (try List.iter (fun v ->
    try
  out := List.append !out [v];
    with Continue -> ()) b with Break -> ());
  __ret := (Obj.magic (!out) : int list); raise Return
  with Return -> !__ret)

let rec concatAny a b =
  let __ret = ref [] in
  (try
  let out = ref ([]) in
  (try List.iter (fun v ->
    try
  out := List.append !out [v];
    with Continue -> ()) a with Break -> ());
  (try List.iter (fun v ->
    try
  out := List.append !out [v];
    with Continue -> ()) b with Break -> ());
  __ret := (Obj.magic (!out) : Obj.t list); raise Return
  with Return -> !__ret)

let a = ref ([1; 2; 3])
let b = ref ([7; 12; 60])
let i = ref ([1; 2; 3])
let j = ref (["Crosby"; "Stills"; "Nash"; "Young"])
let l = ref ([1; 2; 3])
let m = ref ([7; 12; 60])
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (__show (concatInts (!a) (!b)));
  print_endline (__show (concatAny (List.map (fun v -> Obj.repr v) (!i)) (List.map (fun v -> Obj.repr v) (!j))));
  print_endline (__show (concatInts (!l) (!m)));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
