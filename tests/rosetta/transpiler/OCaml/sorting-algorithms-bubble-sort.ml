(* Generated by Mochi transpiler v0.10.57 on 2025-08-04 22:41 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then
    let i = (magic v : int) in
    if i = 0 || i = 1 then string_of_bool (i <> 0)
    else string_of_int i
  else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec bubbleSort a =
  let __ret = ref ([] : (int) list) in
  (try
  let arr = ref (a) in
  let itemCount = ref ((List.length (!arr) - 1)) in
  (try while true do
    try
  let hasChanged = ref (false) in
  let index = ref (0) in
  (try while (!index < !itemCount) do
    try
  if (List.nth (!arr) (!index) > List.nth (!arr) ((!index + 1))) then (
  let tmp = List.nth (!arr) (!index) in
  arr := (List.mapi (fun __i __x -> if __i = !index then List.nth (!arr) ((!index + 1)) else __x) (!arr));
  arr := (List.mapi (fun __i __x -> if __i = (!index + 1) then tmp else __x) (!arr));
  hasChanged := true;
  );
  index := (!index + 1);
    with Continue -> ()
  done with Break -> ());
  if not (!hasChanged) then (
  raise Break;
  );
  itemCount := (!itemCount - 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!arr) : int list); raise Return
  with Return -> !__ret)


let list = ref ([31; 41; 59; 26; 53; 58; 97; 93; 23; 84])
let () =
  let mem_start = _mem () in
  let start = _now () in
  print_endline (("unsorted: " ^ __show (!list)));
  list := bubbleSort (!list);
  print_endline (("sorted!  " ^ __show (!list)));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = Stdlib.max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()