(* Generated by Mochi transpiler v0.10.42 on 2025-07-27 17:37 UTC *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0

exception Break
exception Continue

exception Return

let daysInMonth = ref ([31; 28; 31; 30; 31; 30; 31; 31; 30; 31; 30; 31])
let start = ref ([3; 6; 6; 2; 4; 0; 2; 5; 1; 3; 6; 1])
let months = ref ([" January "; " February"; "  March  "; "  April  "; "   May   "; "   June  "; "   July  "; "  August "; "September"; " October "; " November"; " December"])
let days = ref (["Su"; "Mo"; "Tu"; "We"; "Th"; "Fr"; "Sa"])
let qtr = ref (0)
let () =
  print_endline ("                                [SNOOPY]\n");
  print_endline ("                                  1969\n");
  (try while (!qtr < 4) do
    try
  let mi = ref (0) in
  (try while (!mi < 3) do
    try
  print_endline ((("      " ^ List.nth (!months) (((!qtr * 3) + !mi))) ^ "           "));
  print_endline (string_of_bool false);
  mi := (!mi + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline ("");
  mi := 0;
  (try while (!mi < 3) do
    try
  let d = ref (0) in
  (try while (!d < 7) do
    try
  print_endline ((" " ^ List.nth (!days) (!d)));
  print_endline (string_of_bool false);
  d := (!d + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline ("     ");
  print_endline (string_of_bool false);
  mi := (!mi + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline ("");
  let week = ref (0) in
  (try while (!week < 6) do
    try
  mi := 0;
  (try while (!mi < 3) do
    try
  let day = ref (0) in
  (try while (!day < 7) do
    try
  let m = ((!qtr * 3) + !mi) in
  let val_ = ((((!week * 7) + !day) - List.nth (!start) (m)) + 1) in
  if ((val_ >= 1) && (val_ <= List.nth (!daysInMonth) (m))) then (
  let s = ref ((string_of_int (val_))) in
  if (String.length (!s) = 1) then (
  s := (" " ^ !s);
  );
  print_endline ((" " ^ !s));
  print_endline (string_of_bool false);
  ) else (
  print_endline ("   ");
  print_endline (string_of_bool false);
  );
  day := (!day + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline ("     ");
  print_endline (string_of_bool false);
  mi := (!mi + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline ("");
  week := (!week + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline ("");
  qtr := (!qtr + 1);
    with Continue -> ()
  done with Break -> ());
