(* Generated by Mochi transpiler v0.10.52 on 2025-08-02 01:52 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then
    let i = (magic v : int) in
    if i = 0 || i = 1 then string_of_bool (i <> 0)
    else string_of_int i
  else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Return

let rec divCheck x y =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let x = (Obj.magic x : int) in
  let y = (Obj.magic y : int) in
  if (y = 0) then (
  __ret := (Obj.magic ([("q", Obj.repr (0)); ("ok", Obj.repr (false))]) : (string * Obj.t) list); raise Return
  );
  __ret := (Obj.magic ([("q", Obj.repr ((x / y))); ("ok", Obj.repr (true))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and printResult r =
  let __ret = ref (Obj.magic 0) in
  (try
  print_endline ((((string_of_int ((Obj.obj (List.assoc ("q") r) : int))) ^ " ") ^ __show ((Obj.obj (List.assoc ("ok") r) : bool))));
    !__ret
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  ignore (printResult (divCheck (Obj.repr (3)) (Obj.repr (2))));
  ignore (printResult (divCheck (Obj.repr (3)) (Obj.repr (0))));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()