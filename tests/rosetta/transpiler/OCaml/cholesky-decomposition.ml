(* Generated by Mochi transpiler v0.10.52 on 2025-08-01 22:42 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec sqrtApprox x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  let guess = ref (x) in
  let i = ref (0) in
  (try while (!i < 20) do
    try
  guess := ((!guess +. (x /. !guess)) /. 2.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!guess) : float); raise Return
  with Return -> !__ret)

and cholesky a =
  let __ret = ref ([] : float list list) in
  (try
  let n = List.length (a) in
  let l = ref (([] : float list list)) in
  let i = ref (0) in
  (try while (!i < n) do
    try
  let row = ref (([] : float list)) in
  let j = ref (0) in
  (try while (!j < n) do
    try
  row := (List.append (!row) [(Obj.magic (0.0) : float)]);
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  l := (List.append (!l) [(Obj.magic (!row) : float list)]);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  i := 0;
  (try while (!i < n) do
    try
  let j = ref (0) in
  (try while (!j <= !i) do
    try
  let sum = ref (List.nth (List.nth (a) (!i)) (!j)) in
  let k = ref (0) in
  (try while (!k < !j) do
    try
  sum := (!sum -. (List.nth (List.nth (!l) (!i)) (!k) *. List.nth (List.nth (!l) (!j)) (!k)));
  k := (!k + 1);
    with Continue -> ()
  done with Break -> ());
  if (!i = !j) then (
  l := (List.mapi (fun __i __x -> if __i = !i then (List.mapi (fun __i __x -> if __i = !j then sqrtApprox (Obj.repr (!sum)) else __x) (List.nth (!l) (!i))) else __x) (!l));
  ) else (
  l := (List.mapi (fun __i __x -> if __i = !i then (List.mapi (fun __i __x -> if __i = !j then (!sum /. List.nth (List.nth (!l) (!j)) (!j)) else __x) (List.nth (!l) (!i))) else __x) (!l));
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!l) : float list list); raise Return
  with Return -> !__ret)

and printMat m =
  let __ret = ref (Obj.magic 0) in
  (try
  let i = ref (0) in
  (try while (!i < List.length (m)) do
    try
  let line = ref ("") in
  let j = ref (0) in
  (try while (!j < List.length (List.nth (m) (!i))) do
    try
  line := (!line ^ (Printf.sprintf "%.16g" (List.nth (List.nth (m) (!i)) (!j))));
  if (!j < (List.length (List.nth (m) (!i)) - 1)) then (
  line := (!line ^ " ");
  );
  j := (!j + 1);
    with Continue -> ()
  done with Break -> ());
  print_endline (__show !line);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
    !__ret
  with Return -> !__ret)

and demo a =
  let __ret = ref (Obj.magic 0) in
  (try
  print_endline ("A:");
  ignore (printMat (a));
  let l = ref (cholesky (a)) in
  print_endline ("L:");
  ignore (printMat (!l));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (demo ([[25.0; 15.0; (-.(5.0))]; [15.0; 18.0; 0.0]; [(-.(5.0)); 0.0; 11.0]]));
  ignore (demo ([[18.0; 22.0; 54.0; 42.0]; [22.0; 70.0; 86.0; 62.0]; [54.0; 86.0; 174.0; 134.0]; [42.0; 62.0; 134.0; 106.0]]));
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()