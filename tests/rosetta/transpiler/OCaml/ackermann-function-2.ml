(* Generated by Mochi transpiler v0.10.40 on 2025-07-25 12:29 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec pow base exp =
  let __ret = ref 0 in
  (try
  let result = ref (1) in
  let i = ref (0) in
  (try while (!i < exp) do
    try
  result := (!result * base);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := !result; raise Return
  with Return -> !__ret)

let rec ackermann2 m n =
  let __ret = ref 0 in
  (try
  if (m = 0) then (
  __ret := (n + 1); raise Return
  );
  if (m = 1) then (
  __ret := (n + 2); raise Return
  );
  if (m = 2) then (
  __ret := ((2 * n) + 3); raise Return
  );
  if (m = 3) then (
  __ret := ((8 * pow (2) (n)) - 3); raise Return
  );
  if (n = 0) then (
  __ret := ackermann2 ((m - 1)) (1); raise Return
  );
  __ret := ackermann2 ((m - 1)) (ackermann2 (m) ((n - 1))); raise Return
  with Return -> !__ret)

let rec main () =
  let __ret = ref (Obj.magic 0) in
  (try
  print_endline (("A(0, 0) = " ^ (string_of_int (ackermann2 (0) (0)))));
  print_endline (("A(1, 2) = " ^ (string_of_int (ackermann2 (1) (2)))));
  print_endline (("A(2, 4) = " ^ (string_of_int (ackermann2 (2) (4)))));
  print_endline (("A(3, 4) = " ^ (string_of_int (ackermann2 (3) (4)))));
    !__ret
  with Return -> !__ret)

let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
