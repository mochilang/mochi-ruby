(* Generated by Mochi transpiler v0.10.57 on 2025-08-04 15:16 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then
    let i = (magic v : int) in
    if i = 0 || i = 1 then string_of_bool (i <> 0)
    else string_of_int i
  else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec weekday y m d =
  let __ret = ref 0 in
  (try
  let y = (Obj.magic y : int) in
  let m = (Obj.magic m : int) in
  let d = (Obj.magic d : int) in
  let yy = ref (y) in
  let mm = ref (m) in
  if (!mm < 3) then (
  mm := (!mm + 12);
  yy := (!yy - 1);
  );
  let k = (!yy mod 100) in
  let j = (!yy / 100) in
  let a = ((13 * (!mm + 1)) / 5) in
  let b = (k / 4) in
  let c = (j / 4) in
  __ret := (Obj.magic (((((((d + a) + k) + b) + c) + (5 * j)) mod 7)) : int); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let months31 = ref ([1; 3; 5; 7; 8; 10; 12]) in
  let names = ref (["January"; "February"; "March"; "April"; "May"; "June"; "July"; "August"; "September"; "October"; "November"; "December"]) in
  let count = ref (0) in
  let firstY = ref (0) in
  let firstM = ref (0) in
  let lastY = ref (0) in
  let lastM = ref (0) in
  let haveNone = ref (([] : (int) list)) in
  print_endline ("Months with five weekends:");
  (try for year = 1900 to (2101 - 1) do
    try
  let hasOne = ref (false) in
  (try List.iter (fun m ->
    try
  if (weekday (Obj.repr (year)) (Obj.repr (m)) (Obj.repr (1)) = 6) then (
  print_endline (((("  " ^ (string_of_int (year))) ^ " ") ^ List.nth (!names) ((m - 1))));
  count := (!count + 1);
  hasOne := true;
  lastY := year;
  lastM := m;
  if (!firstY = 0) then (
  firstY := year;
  firstM := m;
  );
  );
    with Continue -> ()) (!months31) with Break -> ());
  if not (!hasOne) then (
  haveNone := (List.append (!haveNone) [(Obj.magic (year) : int)]);
  );
    with Continue -> ()
  done with Break -> ());
  print_endline (((string_of_int (!count)) ^ " total"));
  print_endline ("");
  print_endline ("First five dates of weekends:");
  (try for i = 0 to (5 - 1) do
    try
  let day = (1 + (7 * i)) in
  print_endline (((((("  Friday, " ^ List.nth (!names) ((!firstM - 1))) ^ " ") ^ (string_of_int (day))) ^ ", ") ^ (string_of_int (!firstY))));
    with Continue -> ()
  done with Break -> ());
  print_endline ("Last five dates of weekends:");
  (try for i = 0 to (5 - 1) do
    try
  let day = (1 + (7 * i)) in
  print_endline (((((("  Friday, " ^ List.nth (!names) ((!lastM - 1))) ^ " ") ^ (string_of_int (day))) ^ ", ") ^ (string_of_int (!lastY))));
    with Continue -> ()
  done with Break -> ());
  print_endline ("");
  print_endline ("Years with no months with five weekends:");
  (try List.iter (fun y ->
    try
  print_endline (("  " ^ (string_of_int (y))));
    with Continue -> ()) (!haveNone) with Break -> ());
  print_endline (((string_of_int (List.length (!haveNone))) ^ " total"));
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()