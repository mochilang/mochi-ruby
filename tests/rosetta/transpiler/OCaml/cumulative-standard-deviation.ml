(* Generated by Mochi transpiler v0.10.52 on 2025-08-02 00:57 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let nil = Obj.repr 0


let _now_seed = ref 0
let _now_seeded = ref false

let _now () =
  if not !_now_seeded then (
    match Sys.getenv_opt "MOCHI_NOW_SEED" with
    | Some s -> (try _now_seed := int_of_string s; _now_seeded := true with _ -> ())
    | None -> ()
  );
  if !_now_seeded then (
    _now_seed := (!(_now_seed) * 1664525 + 1013904223) mod 2147483647;
    !_now_seed
  ) else int_of_float (Sys.time () *. 1000000000.)


let _mem () =
  int_of_float (Gc.allocated_bytes ())

exception Break
exception Continue

exception Return

let rec sqrtApprox x =
  let __ret = ref 0.0 in
  (try
  let x = (Obj.magic x : float) in
  if (x <= 0.0) then (
  __ret := (Obj.magic (0.0) : float); raise Return
  );
  let g = ref (x) in
  let i = ref (0) in
  (try while (!i < 20) do
    try
  g := ((!g +. (x /. !g)) /. 2.0);
  i := (!i + 1);
    with Continue -> ()
  done with Break -> ());
  __ret := (Obj.magic (!g) : float); raise Return
  with Return -> !__ret)

and newRsdv () =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  __ret := (Obj.magic ([("n", Obj.repr (0.0)); ("a", Obj.repr (0.0)); ("q", Obj.repr (0.0))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and add r x =
  let __ret = ref ([] : (string * Obj.t) list) in
  (try
  let x = (Obj.magic x : float) in
  let n1 = ((Obj.obj (List.assoc ("n") r) : float) +. 1.0) in
  let a1 = ((Obj.obj (List.assoc ("a") r) : float) +. ((x -. (Obj.obj (List.assoc ("a") r) : float)) /. n1)) in
  let q1 = ((Obj.obj (List.assoc ("q") r) : float) +. ((x -. (Obj.obj (List.assoc ("a") r) : float)) *. (x -. a1))) in
  __ret := (Obj.magic ([("n", Obj.repr (n1)); ("a", Obj.repr (a1)); ("q", Obj.repr (q1))]) : (string * Obj.t) list); raise Return
  with Return -> !__ret)

and sd r =
  let __ret = ref 0.0 in
  (try
  __ret := (Obj.magic (sqrtApprox (Obj.repr (((Obj.obj (List.assoc ("q") r) : float) /. (Obj.obj (List.assoc ("n") r) : float))))) : float); raise Return
  with Return -> !__ret)

and main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let r = ref (newRsdv ()) in
  (try List.iter (fun x ->
    try
  r := add (!r) (Obj.repr (x));
  print_endline ((Printf.sprintf "%.16g" (sd (!r))));
    with Continue -> ()) ([2.0; 4.0; 4.0; 4.0; 5.0; 5.0; 7.0; 9.0]) with Break -> ());
    !__ret
  with Return -> !__ret)


let () =
  let mem_start = _mem () in
  let start = _now () in
  ignore (main ());
  let finish = _now () in
  let mem_end = _mem () in
  let dur = (finish - start) / 1000 in
  let mem_bytes = max 0 (mem_end - mem_start) in
  Printf.printf "{\n  \"duration_us\": %d,\n  \"memory_bytes\": %d,\n  \"name\": \"%s\"\n}\n" dur mem_bytes "main";
  ()