(* Generated by Mochi transpiler v0.10.39 on 2025-07-25 00:13 +0700 *)


let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
      let hd = field o 0 in
      let tl = field o 1 in
      let rest = list_aux tl in
      if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
  | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
  | 252 -> (magic v : string)
  | 253 -> string_of_float (magic v)
  | _ -> "<value>"


let _dyn_add a b =
  if Obj.is_int a && Obj.is_int b then
    Obj.repr ((Obj.magic a : int) + (Obj.magic b : int))
  else
    let af = if Obj.is_int a then float_of_int (Obj.magic a : int) else (Obj.magic a : float) in
    let bf = if Obj.is_int b then float_of_int (Obj.magic b : int) else (Obj.magic b : float) in
    Obj.repr (af +. bf)

let _dyn_sub a b =
  if Obj.is_int a && Obj.is_int b then
    Obj.repr ((Obj.magic a : int) - (Obj.magic b : int))
  else
    let af = if Obj.is_int a then float_of_int (Obj.magic a : int) else (Obj.magic a : float) in
    let bf = if Obj.is_int b then float_of_int (Obj.magic b : int) else (Obj.magic b : float) in
    Obj.repr (af -. bf)

let _dyn_mul a b =
  if Obj.is_int a && Obj.is_int b then
    Obj.repr ((Obj.magic a : int) * (Obj.magic b : int))
  else
    let af = if Obj.is_int a then float_of_int (Obj.magic a : int) else (Obj.magic a : float) in
    let bf = if Obj.is_int b then float_of_int (Obj.magic b : int) else (Obj.magic b : float) in
    Obj.repr (af *. bf)

let _dyn_div a b =
  let af = if Obj.is_int a then float_of_int (Obj.magic a : int) else (Obj.magic a : float) in
  let bf = if Obj.is_int b then float_of_int (Obj.magic b : int) else (Obj.magic b : float) in
  Obj.repr (af /. bf)

exception Return

let rec accumulator sum =
  let __ret = ref (Obj.magic 0) in
  (try
  let store = ref ([sum]) in
let rec add nv =
  let __ret = ref (Obj.magic 0) in
  (try
  store := (List.mapi (fun __i __x -> if __i = 0 then (_dyn_add (List.nth (!store) (0)) (nv)) else __x) (!store));
  __ret := List.nth (!store) (0); raise Return
  with Return -> !__ret) in
  __ret := add; raise Return
  with Return -> !__ret)

let rec main () =
  let __ret = ref (Obj.magic 0) in
  (try
  let x = accumulator (Obj.repr (1)) in
  x (Obj.repr (5));
  ignore (accumulator (Obj.repr (3)));
  print_endline (__show (x (Obj.repr (2.3))));
    !__ret
  with Return -> !__ret)

let () =
  ignore (main ());
