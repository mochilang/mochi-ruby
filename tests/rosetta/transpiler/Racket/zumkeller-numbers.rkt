;; Generated by Mochi 0.10.52 on 2025-08-01 19:48 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port net/url net/http-client json openssl/sha1 openssl/md5)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (let loop ([i 0])
    (cond [(>= i (string-length s)) -1]
          [(string=? (substring s i (add1 i)) ch) i]
          [else (loop (add1 i))])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (_fetch url) (let ([p (get-pure-port (string->url url))]) (define txt (port->string p)) (close-input-port p) (string->jsexpr txt)))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(define (getDivisors n)
  (let/ec _return (begin
(define divs (list 1 n))
(define i 2)
(let/ec _break (let loop ()
  (if (<= (* i i) n) (let ()
    (if (equal? (modulo n i) 0) (let ()
(define j (int (quotient n i)))
(set! divs (append divs (list i)))
(if (not (equal? i j)) (let ()
(set! divs (append divs (list j)))
) (void))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return divs)
))
)
(define (sum xs)
  (let/ec _return (begin
(define s 0)
(let/ec _break (for ([x xs])
  (let/ec _cont
(set! s (let ([__l s] [__r x]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
  )))
(_return s)
))
)
(define (isPartSum divs target)
  (let/ec _return (begin
(define possible (list))
(define i 0)
(let/ec _break (let loop ()
  (if (<= i target) (let ()
    (set! possible (append possible (list #f)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! possible (list-set possible (int 0) #t))
(let/ec _break (for ([v divs])
  (let/ec _cont
(define s target)
(let/ec _break (let loop ()
  (if (>= s v) (let ()
    (if (if possible (list-ref possible (int (- s v))) #f) (let ()
(set! possible (list-set possible (int s) #t))
) (void))
    (set! s (- s 1))
    (loop)) (void))))
  )))
(_return (if possible (list-ref possible (int target)) #f))
))
)
(define (isZumkeller n)
  (let/ec _return (begin
(define divs (getDivisors n))
(define s (apply + divs))
(if (equal? (modulo s 2) 1) (let ()
(_return #f)
) (void))
(if (equal? (modulo n 2) 1) (let ()
(define abundance (- s (* 2 n)))
(_return (and (> abundance 0) (equal? (modulo abundance 2) 0)))
) (void))
(_return (isPartSum divs (quotient s 2)))
))
)
(define (pad n width)
  (let/ec _return (begin
(define s (format "~a" n))
(let/ec _break (let loop ()
  (if (< (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) width) (let ()
    (set! s (string-append " " s))
    (loop)) (void))))
(_return s)
))
)
(define (main)
  (let/ec _return (begin
(displayln "The first 220 Zumkeller numbers are:")
(define count 0)
(define line "")
(define i 2)
(let/ec _break (let loop ()
  (if (< count 220) (let ()
    (if (isZumkeller i) (let ()
(set! line (string-append (string-append line (pad i 3)) " "))
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (equal? (modulo count 20) 0) (let ()
(displayln (slice line 0 (- (cond [(string? line) (string-length line)] [(hash? line) (hash-count line)] [else (length line)]) 1)))
(set! line "")
) (void))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln "\nThe first 40 odd Zumkeller numbers are:")
(set! count 0)
(set! line "")
(set! i 3)
(let/ec _break (let loop ()
  (if (< count 40) (let ()
    (if (isZumkeller i) (let ()
(set! line (string-append (string-append line (pad i 5)) " "))
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (equal? (modulo count 10) 0) (let ()
(displayln (slice line 0 (- (cond [(string? line) (string-length line)] [(hash? line) (hash-count line)] [else (length line)]) 1)))
(set! line "")
) (void))
) (void))
    (set! i (let ([__l i] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(displayln "\nThe first 40 odd Zumkeller numbers which don't end in 5 are:")
(set! count 0)
(set! line "")
(set! i 3)
(let/ec _break (let loop ()
  (if (< count 40) (let ()
    (if (and (not (equal? (modulo i 10) 5)) (isZumkeller i)) (let ()
(set! line (string-append (string-append line (pad i 7)) " "))
(set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (equal? (modulo count 8) 0) (let ()
(displayln (slice line 0 (- (cond [(string? line) (string-length line)] [(hash? line) (hash-count line)] [else (length line)]) 1)))
(set! line "")
) (void))
) (void))
    (set! i (let ([__l i] [__r 2]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(main)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
