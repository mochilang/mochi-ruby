;; Generated by Mochi 0.10.40 on 2025-07-25 20:07 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (pad-start s width ch)
  (if (< (string-length s) width)
      (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
      s))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define (beastKind b)
  (let/ec _return (begin
(_return (if (equal? (if b (hash-ref b "tag" #f) #f) "Dog") (let ([k (if b (hash-ref b "kind" #f) #f)]) k) (if (equal? (if b (hash-ref b "tag" #f) #f) "Cat") (let ([k (if b (hash-ref b "kind" #f) #f)]) k) #f)))
))
)
(define (beastName b)
  (let/ec _return (begin
(_return (if (equal? (if b (hash-ref b "tag" #f) #f) "Dog") (let ([n (if b (hash-ref b "name" #f) #f)]) n) (if (equal? (if b (hash-ref b "tag" #f) #f) "Cat") (let ([n (if b (hash-ref b "name" #f) #f)]) n) #f)))
))
)
(define (beastCry b)
  (let/ec _return (begin
(_return (if (equal? (if b (hash-ref b "tag" #f) #f) "Dog") "Woof" (if (equal? (if b (hash-ref b "tag" #f) #f) "Cat") "Meow" #f)))
))
)
(define (bprint b)
  (let/ec _return (begin
(displayln (string-append (string-append (string-append (string-append (string-append (beastName b) ", who's a ") (beastKind b)) ", cries: \"") (beastCry b)) "\"."))
))
)
(define (main)
  (let/ec _return (begin
(define d (hash "tag" "Dog" "kind" "labrador" "name" "Max"))
(define c (hash "tag" "Cat" "kind" "siamese" "name" "Sammy"))
(bprint d)
(bprint c)
))
)
(main)
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
