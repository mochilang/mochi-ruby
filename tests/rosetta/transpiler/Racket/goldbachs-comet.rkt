;; Generated by Mochi 0.10.52 on 2025-08-01 19:31 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port json openssl/sha1)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (let loop ([i 0])
    (cond [(>= i (string-length s)) -1]
          [(string=? (substring s i (add1 i)) ch) i]
          [else (loop (add1 i))])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(define (sieve limit)
  (let/ec _return (begin
(define primes (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i limit) (let ()
    (set! primes (append primes (list #t)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! primes (list-set primes (int 0) #f))
(set! primes (list-set primes (int 1) #f))
(define p 2)
(let/ec _break (let loop ()
  (if (< (* p p) limit) (let ()
    (if (if primes (list-ref primes (int p)) #f) (let ()
(define k (* p p))
(let/ec _break (let loop ()
  (if (< k limit) (let ()
    (set! primes (list-set primes (int k) #f))
    (set! k (let ([__l k] [__r p]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
) (void))
    (set! p (let ([__l p] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return primes)
))
)
(define (goldbachCount primes n)
  (let/ec _return (begin
(define c 0)
(define i 1)
(let/ec _break (let loop ()
  (if (<= i (quotient n 2)) (let ()
    (if (and (if primes (list-ref primes (int i)) #f) (if primes (list-ref primes (int (- n i))) #f)) (let ()
(set! c (let ([__l c] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return c)
))
)
(define (pad n)
  (let/ec _return (begin
(if (< n 10) (let ()
(_return (string-append "  " (format "~a" n)))
) (void))
(if (< n 100) (let ()
(_return (string-append " " (format "~a" n)))
) (void))
(_return (format "~a" n))
))
)
(define (main)
  (let/ec _return (begin
(define primes (sieve 1000))
(displayln "The first 100 Goldbach numbers:")
(define line "")
(define n 2)
(define count 0)
(let/ec _break (let loop ()
  (if (< count 100) (let ()
    (define v (goldbachCount primes (* 2 n)))
    (set! line (string-append (string-append line (pad v)) " "))
    (set! count (let ([__l count] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (set! n (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (if (equal? (modulo count 10) 0) (let ()
(displayln (slice line 0 (- (cond [(string? line) (string-length line)] [(hash? line) (hash-count line)] [else (length line)]) 1)))
(set! line "")
) (void))
    (loop)) (void))))
(define val (goldbachCount primes 1000))
(displayln (string-append "\nThe 1,000th Goldbach number = " (format "~a" val)))
))
)
(main)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
