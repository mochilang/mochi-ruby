;; Generated by Mochi 0.10.41 on 2025-07-27 15:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (pad-start s width ch)
  (if (< (string-length s) width)
      (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
      s))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define (bellTriangle n)
  (let/ec _return (begin
(define tri (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let ()
    (define row (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j i) (let ()
    (set! row (append row (list 0)))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! tri (append tri (list row)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! tri (list-set tri 1 (list-set (list-ref tri 1) 0 1)))
(set! i 2)
(let/ec _break (let loop ()
  (if (< i n) (let ()
    (set! tri (list-set tri i (list-set (list-ref tri i) 0 (list-ref (list-ref tri (- i 1)) (- i 2)))))
    (define j 1)
    (let/ec _break (let loop ()
  (if (< j i) (let ()
    (set! tri (list-set tri i (list-set (list-ref tri i) j (let ([__l (list-ref (list-ref tri i) (- j 1))] [__r (list-ref (list-ref tri (- i 1)) (- j 1))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return tri)
))
)
(define (main)
  (let/ec _return (begin
(define bt (bellTriangle 51))
(displayln "First fifteen and fiftieth Bell numbers:")
(let/ec _break (let ([i 1])
  (let loop ()
    (when (< i 16)
(displayln (string-append (string-append (string-append "" (pad-start (format "~a" i) 2 " ")) ": ") (format "~a" (list-ref (list-ref bt i) 0))))
      (set! i (+ i 1))
      (loop)))
))
(displayln (string-append "50: " (format "~a" (list-ref (list-ref bt 50) 0))))
(displayln "")
(displayln "The first ten rows of Bell's triangle:")
(let/ec _break (let ([i 1])
  (let loop ()
    (when (< i 11)
(displayln (list-ref bt i))
      (set! i (+ i 1))
      (loop)))
))
))
)
(main)
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
