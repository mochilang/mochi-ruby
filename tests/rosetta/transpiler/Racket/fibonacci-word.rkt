;; Generated by Mochi 0.10.50 on 2025-07-30 21:05 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json openssl/sha1)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (let loop ([i 0])
    (cond [(>= i (string-length s)) -1]
          [(string=? (substring s i (add1 i)) ch) i]
          [else (loop (add1 i))])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define math_pi pi)
(define math_e (exp 1))
(define (math_sqrt x) (sqrt x))
(define (math_pow x y) (expt x y))
(define (math_sin x) (sin x))
(define (math_log x) (log x))
(define (entropy s)
  (let/ec _return (begin
(define counts (hash))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])) (let ()
    (define ch (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (hash-has-key? counts ch) (let ()
(set! counts (hash-set (or counts (hash)) ch (let ([__l (if counts (hash-ref counts ch #f) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (let ()
(set! counts (hash-set (or counts (hash)) ch 1))
))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define hm 0.0)
(let/ec _break (for ([k (hash-keys counts)])
  (let/ec _cont
(define c (exact->inexact (if counts (hash-ref counts k #f) #f)))
(set! hm (let ([__l hm] [__r (* c (/ (math_log c) (math_log 2.0)))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
  )))
(define l (exact->inexact (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])))
(_return (- (/ (math_log l) (math_log 2.0)) (/ hm l)))
))
)
(define (fibonacciWord n)
  (let/ec _return (begin
(define a "1")
(define b "0")
(define i 1)
(let/ec _break (let loop ()
  (if (< i n) (let ()
    (define tmp b)
    (set! b (string-append b a))
    (set! a tmp)
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return a)
))
)
(define (main)
  (let/ec _return (begin
(displayln (string-append (let ([__l (pad "N" 3)] [__r (pad "Length" 9)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) "  Entropy      Word"))
(define n 1)
(let/ec _break (let loop ()
  (if (< n 10) (let ()
    (define s (fibonacciWord n))
    (displayln (string-append (string-append (string-append (string-append (let ([__l (pad (format "~a" n) 3)] [__r (pad (format "~a" (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])) 9)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) "  ") (fmt (entropy s))) "  ") s))
    (set! n (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(let/ec _break (let loop ()
  (if (<= n 37) (let ()
    (define s (fibonacciWord n))
    (displayln (string-append (string-append (let ([__l (pad (format "~a" n) 3)] [__r (pad (format "~a" (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])) 9)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) "  ") (fmt (entropy s))))
    (set! n (let ([__l n] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(define (pad s w)
  (let/ec _return (begin
(define t s)
(let/ec _break (let loop ()
  (if (< (cond [(string? t) (string-length t)] [(hash? t) (hash-count t)] [else (length t)]) w) (let ()
    (set! t (string-append " " t))
    (loop)) (void))))
(_return t)
))
)
(define (fmt x)
  (let/ec _return (begin
(define y (/ (floorf (let ([__l (* x 100000000.0)] [__r 0.5]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) 100000000.0))
(define s (format "~a" y))
(define dot (index-of s "."))
(if (equal? dot (- 0 1)) (let ()
(set! s (string-append s ".00000000"))
) (let ()
(define d (- (- (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) dot) 1))
(let/ec _break (let loop ()
  (if (< d 8) (let ()
    (set! s (string-append s "0"))
    (set! d (let ([__l d] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
(_return s)
))
)
(define (floorf x)
  (let/ec _return (begin
(define y (int x))
(_return (exact->inexact y))
))
)
(define (indexOf s ch)
  (let/ec _return (begin
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])) (let ()
    (if (string=? (slice s i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) ch) (let ()
(_return i)
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return (- 0 1))
))
)
(main)
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
