;; Generated by Mochi 0.10.50 on 2025-07-30 23:37 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json openssl/sha1)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (let loop ([i 0])
    (cond [(>= i (string-length s)) -1]
          [(string=? (substring s i (add1 i)) ch) i]
          [else (loop (add1 i))])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define (main)
  (let/ec _return (begin
(define INF 1000000000)
(define n 4)
(define dist (list))
(define next (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let ()
    (define row (list))
    (define nrow (list))
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j n) (let ()
    (if (equal? i j) (let ()
(set! row (append row (list 0)))
) (let ()
(set! row (append row (list INF)))
))
    (set! nrow (append nrow (list (- 0 1))))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! dist (append dist (list row)))
    (set! next (append next (list nrow)))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(set! dist (list-set dist 0 (list-set (list-ref dist 0) 2 (- 2))))
(set! next (list-set next 0 (list-set (list-ref next 0) 2 2)))
(set! dist (list-set dist 2 (list-set (list-ref dist 2) 3 2)))
(set! next (list-set next 2 (list-set (list-ref next 2) 3 3)))
(set! dist (list-set dist 3 (list-set (list-ref dist 3) 1 (- 1))))
(set! next (list-set next 3 (list-set (list-ref next 3) 1 1)))
(set! dist (list-set dist 1 (list-set (list-ref dist 1) 0 4)))
(set! next (list-set next 1 (list-set (list-ref next 1) 0 0)))
(set! dist (list-set dist 1 (list-set (list-ref dist 1) 2 3)))
(set! next (list-set next 1 (list-set (list-ref next 1) 2 2)))
(define k 0)
(let/ec _break (let loop ()
  (if (< k n) (let ()
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i n) (let ()
    (define j 0)
    (let/ec _break (let loop ()
  (if (< j n) (let ()
    (if (and (< (if (if dist (list-ref dist (int i)) #f) (list-ref (if dist (list-ref dist (int i)) #f) (int k)) #f) INF) (< (if (if dist (list-ref dist (int k)) #f) (list-ref (if dist (list-ref dist (int k)) #f) (int j)) #f) INF)) (let ()
(define alt (let ([__l (if (if dist (list-ref dist (int i)) #f) (list-ref (if dist (list-ref dist (int i)) #f) (int k)) #f)] [__r (if (if dist (list-ref dist (int k)) #f) (list-ref (if dist (list-ref dist (int k)) #f) (int j)) #f)]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
(if (< alt (if (if dist (list-ref dist (int i)) #f) (list-ref (if dist (list-ref dist (int i)) #f) (int j)) #f)) (let ()
(set! dist (list-set dist i (list-set (list-ref dist i) j alt)))
(set! next (list-set next i (list-set (list-ref next i) j (if (if next (list-ref next (int i)) #f) (list-ref (if next (list-ref next (int i)) #f) (int k)) #f))))
) (void))
) (void))
    (set! j (let ([__l j] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! k (let ([__l k] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(define (path u v)
  (let/ec _return (begin
(define ui (- u 1))
(define vi (- v 1))
(if (equal? (if (if next (list-ref next (int ui)) #f) (list-ref (if next (list-ref next (int ui)) #f) (int vi)) #f) (- 0 1)) (let ()
(_return (list))
) (void))
(define p (list u))
(define cur ui)
(let/ec _break (let loop ()
  (if (not (equal? cur vi)) (let ()
    (set! cur (if (if next (list-ref next (int cur)) #f) (list-ref (if next (list-ref next (int cur)) #f) (int vi)) #f))
    (set! p (append p (list (let ([__l cur] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))))
    (loop)) (void))))
(_return p)
))
)
(define (pathStr p)
  (let/ec _return (begin
(define s "")
(define first #t)
(define idx 0)
(let/ec _break (let loop ()
  (if (< idx (cond [(string? p) (string-length p)] [(hash? p) (hash-count p)] [else (length p)])) (let ()
    (define x (if p (list-ref p (int idx)) #f))
    (if (not first) (let ()
(set! s (string-append s " -> "))
) (void))
    (set! s (string-append s (format "~a" x)))
    (set! first #f)
    (set! idx (let ([__l idx] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return s)
))
)
(displayln "pair\tdist\tpath")
(define a 0)
(let/ec _break (let loop ()
  (if (< a n) (let ()
    (define b 0)
    (let/ec _break (let loop ()
  (if (< b n) (let ()
    (if (not (equal? a b)) (let ()
(displayln (string-append (string-append (string-append (string-append (string-append (string-append (format "~a" (let ([__l a] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))) " -> ") (format "~a" (let ([__l b] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))) "\t") (format "~a" (if (if dist (list-ref dist (int a)) #f) (list-ref (if dist (list-ref dist (int a)) #f) (int b)) #f))) "\t") (pathStr (path (let ([__l a] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (let ([__l b] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))))
) (void))
    (set! b (let ([__l b] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (set! a (let ([__l a] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(main)
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
