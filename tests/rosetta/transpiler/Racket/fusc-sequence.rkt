;; Generated by Mochi 0.10.50 on 2025-07-30 23:37 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json openssl/sha1)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (let loop ([i 0])
    (cond [(>= i (string-length s)) -1]
          [(string=? (substring s i (add1 i)) ch) i]
          [else (loop (add1 i))])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define (fuscVal n)
  (let/ec _return (begin
(define a 1)
(define b 0)
(define x n)
(let/ec _break (let loop ()
  (if (> x 0) (let ()
    (if (equal? (modulo x 2) 0) (let ()
(set! x (quotient x 2))
(set! a (let ([__l a] [__r b]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(set! x (quotient (- x 1) 2))
(set! b (let ([__l a] [__r b]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
))
    (loop)) (void))))
(if (equal? n 0) (let ()
(_return 0)
) (void))
(_return b)
))
)
(define (firstFusc n)
  (let/ec _return (begin
(define arr (list))
(define i 0)
(let/ec _break (let loop ()
  (if (< i n) (let ()
    (set! arr (append arr (list (fuscVal i))))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return arr)
))
)
(define (commatize n)
  (let/ec _return (begin
(define s (format "~a" n))
(define neg #f)
(if (< n 0) (let ()
(set! neg #t)
(set! s (slice s 1 (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)])))
) (void))
(define i (- (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]) 3))
(let/ec _break (let loop ()
  (if (>= i 1) (let ()
    (set! s (string-append (string-append (slice s 0 i) ",") (slice s i (cond [(string? s) (string-length s)] [(hash? s) (hash-count s)] [else (length s)]))))
    (set! i (- i 3))
    (loop)) (void))))
(if neg (let ()
(_return (string-append "-" s))
) (void))
(_return s)
))
)
(define (padLeft s w)
  (let/ec _return (begin
(define out s)
(let/ec _break (let loop ()
  (if (< (cond [(string? out) (string-length out)] [(hash? out) (hash-count out)] [else (length out)]) w) (let ()
    (set! out (string-append " " out))
    (loop)) (void))))
(_return out)
))
)
(define (main)
  (let/ec _return (begin
(displayln "The first 61 fusc numbers are:")
(displayln (format "~a" (firstFusc 61)))
(displayln "\nThe fusc numbers whose length > any previous fusc number length are:")
(define idxs (list 0 37 1173 35499 699051 19573419))
(define i 0)
(let/ec _break (let loop ()
  (if (< i (cond [(string? idxs) (string-length idxs)] [(hash? idxs) (hash-count idxs)] [else (length idxs)])) (let ()
    (define idx (if idxs (list-ref idxs (int i)) #f))
    (define val (fuscVal idx))
    (define numStr (padLeft (commatize val) 7))
    (define idxStr (padLeft (commatize idx) 10))
    (displayln (string-append (string-append (string-append numStr " (index ") idxStr) ")"))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
))
)
(main)
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
