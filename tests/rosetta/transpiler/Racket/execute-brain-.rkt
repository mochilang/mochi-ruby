;; Generated by Mochi 0.10.50 on 2025-07-31 00:48 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match racket/system racket/port json openssl/sha1)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (slice seq start end)
  (define len (if (string? seq) (string-length seq) (length seq)))
  (define s (int start))
  (define e (int end))
  (when (< s 0) (set! s (+ len s)))
  (when (< e 0) (set! e (+ len e)))
  (set! s (max 0 (min len s)))
  (set! e (max 0 (min len e)))
  (when (< e s) (set! e s))
  (if (string? seq) (substring seq s e) (sublist seq s e)))
(define (pad-start s width ch)
  (let ([s (format "~a" s)])
    (if (< (string-length s) width)
        (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
        s)))
(define (index-of s ch)
  (let loop ([i 0])
    (cond [(>= i (string-length s)) -1]
          [(string=? (substring s i (add1 i)) ch) i]
          [else (loop (add1 i))])))
(define (_repeat s n)
  (cond
    [(string? s) (apply string-append (make-list (int n) s))]
    [(list? s) (apply append (make-list (int n) s))]
    [else '()]))
(define (_parse-int-str s base) (int (string->number s base)))
(define (_sha256 bs) (bytes->list (sha256-bytes (list->bytes bs))))
(define (num r) (numerator r))
(define (denom r) (denominator r))
(define (panic msg) (error msg))

(let* ([_start_mem (current-memory-use)] [_start (now)])
  (let/ec _return (begin
(define (chr n)
  (let/ec _return (begin
(define upper "ABCDEFGHIJKLMNOPQRSTUVWXYZ")
(define lower "abcdefghijklmnopqrstuvwxyz")
(if (and (>= n 65) (< n 91)) (let ()
(_return (slice upper (- n 65) (- n 64)))
) (void))
(if (and (>= n 97) (< n 123)) (let ()
(_return (slice lower (- n 97) (- n 96)))
) (void))
(if (equal? n 32) (let ()
(_return " ")
) (void))
(if (equal? n 33) (let ()
(_return "!")
) (void))
(if (equal? n 44) (let ()
(_return ",")
) (void))
(if (equal? n 13) (let ()
(_return "")
) (void))
(if (equal? n 10) (let ()
(_return "\n")
) (void))
(_return "?")
))
)
(define (bf dLen code)
  (let/ec _return (begin
(define ds (list))
(let/ec _break (let ([i 0])
  (let loop ()
    (when (< i dLen)
(set! ds (append ds (list 0)))
      (set! i (+ i 1))
      (loop)))
))
(define dp 0)
(define ip 0)
(define out "")
(let/ec _break (let loop ()
  (if (< ip (cond [(string? code) (string-length code)] [(hash? code) (hash-count code)] [else (length code)])) (let ()
    (define ch (slice code ip (let ([__l ip] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (string=? ch ">") (let ()
(set! dp (let ([__l dp] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(if (string=? ch "<") (let ()
(set! dp (- dp 1))
) (let ()
(if (string=? ch "+") (let ()
(set! ds (list-set ds (int dp) (let ([__l (if ds (list-ref ds (int dp)) #f)] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
) (let ()
(if (string=? ch "-") (let ()
(set! ds (list-set ds (int dp) (- (if ds (list-ref ds (int dp)) #f) 1)))
) (let ()
(if (string=? ch ".") (let ()
(set! out (string-append out (chr (if ds (list-ref ds (int dp)) #f))))
) (let ()
(if (string=? ch ",") (void) (let ()
(if (string=? ch "[") (let ()
(if (equal? (if ds (list-ref ds (int dp)) #f) 0) (let ()
(define nc 1)
(let/ec _break (let loop ()
  (if (> nc 0) (let ()
    (set! ip (let ([__l ip] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (define cc (slice code ip (let ([__l ip] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (string=? cc "[") (let ()
(set! nc (let ([__l nc] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(if (string=? cc "]") (let ()
(set! nc (- nc 1))
) (void))
))
    (loop)) (void))))
) (void))
) (let ()
(if (string=? ch "]") (let ()
(if (not (equal? (if ds (list-ref ds (int dp)) #f) 0)) (let ()
(define nc 1)
(let/ec _break (let loop ()
  (if (> nc 0) (let ()
    (set! ip (- ip 1))
    (define cc (slice code ip (let ([__l ip] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
    (if (string=? cc "]") (let ()
(set! nc (let ([__l nc] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (let ()
(if (string=? cc "[") (let ()
(set! nc (- nc 1))
) (void))
))
    (loop)) (void))))
) (void))
) (void))
))
))
))
))
))
))
))
    (set! ip (let ([__l ip] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
(_return out)
))
)
(define (main)
  (let/ec _return (begin
(define prog (string-append (string-append (string-append "++++++++++[>+>+++>++++>+++++++>++++++++>+++++++++>++\n" "++++++++>+++++++++++>++++++++++++<<<<<<<<<-]>>>>+.>>>\n") ">+..<.<++++++++.>>>+.<<+.<<<<++++.<++.>>>+++++++.>>>.+++.\n") "<+++++++.--------.<<<<<+.<+++.---."))
(define out (bf 10 prog))
(displayln out)
))
)
(main)
  ))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
