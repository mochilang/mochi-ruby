;; Generated by Mochi 0.10.41 on 2025-07-27 15:57 +0700
#lang racket/base
(require racket/list racket/string racket/math racket/match json)
(define nowSeed (let ([s (getenv "MOCHI_NOW_SEED")]) (and s (string->number s))))
(define (now)
  (if nowSeed
      (begin (set! nowSeed (modulo (+ (* nowSeed 1664525) 1013904223) 2147483647)) nowSeed)
      (inexact->exact (floor (* (current-inexact-milliseconds) 1000)))))
(define (int x)
  (cond
    [(integer? x) x]
    [(number? x) (inexact->exact (truncate x))]
    [(string? x) (let ([n (string->number x)]) (if n (inexact->exact (truncate n)) 0))]
    [else 0]))
(define (float x)
  (cond
    [(number? x) (exact->inexact x)]
    [(string? x) (let ([n (string->number x)]) (if n (exact->inexact n) 0.0))]
    [else 0.0]))
(define (input) (read-line))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (sublist lst start end)
  (if (string? lst)
      (substring lst start end)
      (take (drop lst start) (- end start))))

(define (pad-start s width ch)
  (if (< (string-length s) width)
      (string-append (make-string (- width (string-length s)) (string-ref ch 0)) s)
      s))

(let* ([_start_mem (current-memory-use)] [_start (now)])
(define (qsel a k)
  (let/ec _return (begin
(define arr a)
(let/ec _break (let loop ()
  (if (> (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 1) (let ()
    (define px (modulo (now) (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)])))
    (define pv (list-ref arr px))
    (define last (- (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 1))
    (define tmp (list-ref arr px))
    (set! arr (list-set arr px (list-ref arr last)))
    (set! arr (list-set arr last tmp))
    (set! px 0)
    (define i 0)
    (let/ec _break (let loop ()
  (if (< i last) (let ()
    (define v (list-ref arr i))
    (if (< v pv) (let ()
(define tmp2 (list-ref arr px))
(set! arr (list-set arr px (list-ref arr i)))
(set! arr (list-set arr i tmp2))
(set! px (let ([__l px] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
) (void))
    (set! i (let ([__l i] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))))
    (loop)) (void))))
    (if (equal? px k) (let ()
(_return pv)
) (void))
    (if (< k px) (let ()
(set! arr (sublist arr 0 px))
) (let ()
(define tmp2 (list-ref arr px))
(set! arr (list-set arr px pv))
(set! arr (list-set arr last tmp2))
(set! arr (sublist arr (let ([__l px] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) (length arr)))
(set! k (- k (let ([__l px] [__r 1]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r)))))
))
    (loop)) (void))))
(_return (list-ref arr 0))
))
)
(define (median list_)
  (let/ec _return (begin
(define arr list_)
(define half (int (quotient (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 2)))
(define med (qsel arr half))
(if (equal? (modulo (cond [(string? arr) (string-length arr)] [(hash? arr) (hash-count arr)] [else (length arr)]) 2) 0) (let ()
(_return (/ (let ([__l med] [__r (qsel arr (- half 1))]) (if (and (string? __l) (string? __r)) (string-append __l __r) (+ __l __r))) 2.0))
) (void))
(_return med)
))
)
(displayln (format "~a" (median (list 3.0 1.0 4.0 1.0))))
(displayln (format "~a" (median (list 3.0 1.0 4.0 1.0 5.0))))
  (let* ([_end (now)] [_end_mem (current-memory-use)]
         [_dur (- _end _start)]
         [_dur_us _dur]
         [_mem (max 0 (- _end_mem _start_mem))])
    (displayln "{")
    (displayln (format "  \"duration_us\": ~a," _dur_us))
    (displayln (format "  \"memory_bytes\": ~a," _mem))
    (displayln "  \"name\": \"main\"")
    (displayln "}")
  )
)
