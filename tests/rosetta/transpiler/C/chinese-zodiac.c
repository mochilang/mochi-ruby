// Generated by Mochi 0.10.32 on 2025-08-04 20:02 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

typedef struct Info Info;

struct Info {
    const char* animal;
    const char* yinYang;
    const char* element;
    const char* stemBranch;
    long long cycle;
};

const char* animal_init[12] = {"Rat", "Ox", "Tiger", "Rabbit", "Dragon", "Snake", "Horse", "Goat", "Monkey", "Rooster", "Dog", "Pig"};
const char* *animal = animal_init;
size_t animal_len = 12;
const char* yinYang_init[2] = {"Yang", "Yin"};
const char* *yinYang = yinYang_init;
size_t yinYang_len = 2;
const char* element_init[5] = {"Wood", "Fire", "Earth", "Metal", "Water"};
const char* *element = element_init;
size_t element_len = 5;
const char* stemChArr_init[10] = {"甲", "乙", "丙", "丁", "戊", "己", "庚", "辛", "壬", "癸"};
const char* *stemChArr = stemChArr_init;
size_t stemChArr_len = 10;
const char* branchChArr_init[12] = {"子", "丑", "寅", "卯", "辰", "巳", "午", "未", "申", "酉", "戌", "亥"};
const char* *branchChArr = branchChArr_init;
size_t branchChArr_len = 12;

Info cz(long long yr, const char* * animal, size_t animal_len, const char* * yinYang, size_t yinYang_len, const char* * element, size_t element_len, const char* * sc, size_t sc_len, const char* * bc, size_t bc_len);
int main(void);

Info cz(long long yr, const char* * animal, size_t animal_len, const char* * yinYang, size_t yinYang_len, const char* * element, size_t element_len, const char* * sc, size_t sc_len, const char* * bc, size_t bc_len) {
    long long y = yr - 4LL;
    long long stem = y % 10LL;
    long long branch = y % 12LL;
    const char* sb = str_concat(sc[(int)(stem)], bc[(int)(branch)]);
    return (Info){.animal = str_int(animal[(int)(branch)]), .yinYang = str_int(yinYang[(int)(stem % 2LL)]), .element = str_int(element[(int)(stem / 2LL)]), .stemBranch = sb, .cycle = (y % 60LL) + 1LL};
}

int main(void) {
    {
        long long __start = _now();
        {
            long long yr_arr[] = {1935LL, 1938LL, 1968LL, 1972LL, 1976LL};
            size_t yr_len = sizeof(yr_arr) / sizeof(yr_arr[0]);
            for (size_t __i = 0; __i < yr_len; __i++) {
                long long yr = yr_arr[__i];
                Info r = cz(yr, animal, animal_len, yinYang, yinYang_len, element, element_len, stemChArr, stemChArr_len, branchChArr, branchChArr_len);
                puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_int(yr), ": "), r.element), " "), r.animal), ", "), r.yinYang), ", Cycle year "), str_int(r.cycle)), " "), r.stemBranch));
            }
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
