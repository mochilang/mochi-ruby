// Generated by Mochi 0.10.32 on 2025-07-25 19:53 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t search_user_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; const char ***vals; size_t *lens; size_t len; } MapSL;

static const char** map_get_sl(const char *keys[], const char **vals[], const size_t lens[], size_t len, const char *key, size_t *out_len) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) { *out_len = lens[i]; return vals[i]; }
    }
    *out_len = 0;
    return NULL;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct Anon1 Anon1;

struct Anon1 {
    const char* Base;
    const char* Host;
    int Port;
    const char* GroupFilter;
};

const char* * search_user(MapSL directory, const char* username) {
    return map_get_sl(directory.keys, directory.vals, directory.lens, directory.len, username, &search_user_len);
}

int user_main() {
    Anon1 client = (Anon1){.Base = "dc=example,dc=com", .Host = "ldap.example.com", .Port = 389, .GroupFilter = "(memberUid=%s)"};
    const char* directory_keys[18] = {"username", "john"};
    const char* directory_vals_0[2] = {"admins", "users"};
    const char* directory_vals_1[1] = {"users"};
    const const char** directory_vals[18] = {directory_vals_0, directory_vals_1};
    size_t directory_lens[18] = {2, 1};
    size_t directory_len = 2;
    MapSL directory = { directory_keys, directory_vals, directory_lens, directory_len };
    const char* *groups = search_user(directory, "username");
    size_t groups_len = search_user_len;
    if (1) {
        const char* out = "Groups: [";
        int i = 0;
        while (i < 2) {
            out = str_concat(str_concat(str_concat(out, "\""), groups[i]), "\"");
            if (i < (2 - 1)) {
                out = str_concat(out, ", ");
            }
            i = i + 1;
        }
        out = str_concat(out, "]");
        puts(out);
    } else {
        puts("User not found");
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
