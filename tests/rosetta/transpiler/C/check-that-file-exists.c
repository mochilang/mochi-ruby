// Generated by Mochi 0.10.32 on 2025-08-04 15:00 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static int contains_str(const char *arr[], size_t len, const char *val) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(arr[i], val) == 0) return 1;
    }
    return 0;
}

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; int *vals; size_t len; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(const char *keys[], int vals[], size_t *len, const char *key, int val) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

void printStat(int fs, const char* path);
void user_main();
int main(void);

void printStat(int fs, const char* path) {
    if (contains_str(fs_keys, fs_len, path)) {
        if (map_get_si(fs_keys, fs_vals, fs_len, path)) {
            puts(str_concat(path, " is a directory"));
        } else {
            puts(str_concat(path, " is a file"));
        }
    } else {
        puts(str_concat(str_concat("stat ", path), ": no such file or directory"));
    }
}

void user_main() {
    const char* fs_keys[16] = {};
    int fs_vals[16] = {};
    size_t fs_len = 0;
    MapSI fs = { fs_keys, fs_vals, fs_len };
    map_set_si(fs_keys, fs_vals, &fs_len, "docs", 1LL);
    {
        const char* p_arr[] = {"input.txt", "/input.txt", "docs", "/docs"};
        size_t p_len = sizeof(p_arr) / sizeof(p_arr[0]);
        for (size_t __i = 0; __i < p_len; __i++) {
            const char* p = p_arr[__i];
            printStat(fs, p);
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
