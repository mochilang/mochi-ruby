// Generated by Mochi 0.10.32 on 2025-08-05 02:43 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

long long daysInMonth_init[12] = {31LL, 28LL, 31LL, 30LL, 31LL, 30LL, 31LL, 31LL, 30LL, 31LL, 30LL, 31LL};
long long *daysInMonth = daysInMonth_init;
size_t daysInMonth_len = 12;
long long start_init[12] = {3LL, 6LL, 6LL, 2LL, 4LL, 0LL, 2LL, 5LL, 1LL, 3LL, 6LL, 1LL};
long long *start = start_init;
size_t start_len = 12;
const char* months_init[12] = {" January ", " February", "  March  ", "  April  ", "   May   ", "   June  ", "   July  ", "  August ", "September", " October ", " November", " December"};
const char* *months = months_init;
size_t months_len = 12;
const char* days_init[7] = {"Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"};
const char* *days = days_init;
size_t days_len = 7;
long long qtr = 0LL;

int main(void);

int main(void) {
    {
        long long __start = _now();
        puts("                                [SNOOPY]\n");
        puts("                                  1969\n");
        while (qtr < 4LL) {
            long long mi = 0LL;
            while (mi < 3LL) {
                printf("%s %d\n", str_concat(str_concat("      ", months[(int)((qtr * 3LL) + mi)]), "           "), 0LL);
                mi = mi + 1LL;
            }
            puts("");
            mi = 0LL;
            while (mi < 3LL) {
                long long d = 0LL;
                while (d < 7LL) {
                    printf("%s %d\n", str_concat(" ", days[(int)(d)]), 0LL);
                    d = d + 1LL;
                }
                printf("%s %d\n", "     ", 0LL);
                mi = mi + 1LL;
            }
            puts("");
            long long week = 0LL;
            while (week < 6LL) {
                mi = 0LL;
                while (mi < 3LL) {
                    long long day = 0LL;
                    while (day < 7LL) {
                        long long m = (qtr * 3LL) + mi;
                        long long val = (((week * 7LL) + day) - start[(int)(m)]) + 1LL;
                        if ((val >= 1LL) && (val <= daysInMonth[(int)(m)])) {
                            const char* s = str_int(val);
                            if (strlen(s) == 1LL) {
                                s = str_concat(" ", s);
                            }
                            printf("%s %d\n", str_concat(" ", s), 0LL);
                        } else {
                            printf("%s %d\n", "   ", 0LL);
                        }
                        day = day + 1LL;
                    }
                    printf("%s %d\n", "     ", 0LL);
                    mi = mi + 1LL;
                }
                puts("");
                week = week + 1LL;
            }
            puts("");
            qtr = qtr + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
