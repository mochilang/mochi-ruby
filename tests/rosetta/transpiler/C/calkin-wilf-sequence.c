// Generated by Mochi 0.10.32 on 2025-08-04 07:51 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <gmp.h>
typedef mpq_t* bigrat;
typedef mpz_t* bigint;

size_t calkinWilf_len;
size_t toContinued_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static bigrat _bigrat(long n, long d) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_set_si(*r, n, d);
    mpq_canonicalize(*r);
    return r;
}

static bigrat _add(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_add(*r, *a, *b);
    return r;
}

static bigrat _sub(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_sub(*r, *a, *b);
    return r;
}

static bigrat _mul(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_mul(*r, *a, *b);
    return r;
}

static bigrat _div(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_div(*r, *a, *b);
    return r;
}

static mpz_srcptr num(bigrat a) { return mpq_numref(*a); }
static mpz_srcptr denom(bigrat a) { return mpq_denref(*a); }

static char* repeat(const char *s, int n) {
    size_t len = strlen(s);
    char *out = malloc(len * n + 1);
    for (int i = 0; i < n; i++) memcpy(out + i * len, s, len);
    out[len * n] = 0;
    return out;
}

static long parseIntStr(const char *s, int base) {
    return strtol(s, NULL, base);
}

static bigrat* list_append_bigrat(bigrat *arr, size_t *len, bigrat val) {
    arr = realloc(arr, (*len + 1) * sizeof(bigrat));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

bigrat user_bigrat(long long a, long long b);
bigrat * calkinWilf(long long n);
long long * toContinued(bigrat r);
long long termNumber(long long * cf, size_t cf_len);
const char* commatize(long long n);
void user_main();
int main(void);

bigrat user_bigrat(long long a, long long b) {
    return _div(_bigrat(a, 1LL), _bigrat(b, 1LL));
}

bigrat * calkinWilf(long long n) {
    bigrat *seq = NULL;
    size_t seq_len = 0;
    seq = list_append_bigrat(seq, &seq_len, user_bigrat(1LL, 1LL));
    int i = 1LL;
    while (i < n) {
        bigrat prev = seq[(int)(i - 1LL)];
        long long a = mpz_get_si(num(prev));
        long long b = mpz_get_si(denom(prev));
        int f = a / b;
        bigrat t = user_bigrat(f, 1LL);
        t = _mul(t, _bigrat(2LL, 1LL));
        t = _sub(t, prev);
        t = _add(t, _bigrat(1LL, 1LL));
        t = _div(_bigrat(1LL, 1LL), t);
        seq = list_append_bigrat(seq, &seq_len, t);
        i = i + 1LL;
    }
    return calkinWilf_len = seq_len, seq;
}

long long * toContinued(bigrat r) {
    long long a = mpz_get_si(num(r));
    long long b = mpz_get_si(denom(r));
    long long *res = NULL;
    size_t res_len = 0;
    while (1LL) {
        res = list_append_long_long(res, &res_len, a / b);
        int t = a % b;
        a = b;
        b = t;
        if (a == 1LL) {
            break;
        }
    }
    if ((res_len % 2LL) == 0LL) {
        res[(int)(res_len - 1LL)] = res[(int)(res_len - 1LL)] - 1LL;
        res = list_append_long_long(res, &res_len, 1LL);
    }
    return toContinued_len = res_len, res;
}

long long termNumber(long long * cf, size_t cf_len) {
    const char* b = "";
    const char* d = "1";
    for (size_t __i = 0; __i < cf_len; __i++) {
        long long n = cf[__i];
        b = str_concat(repeat(d, n), b);
        if (1LL) {
            d = "0";
        } else {
            d = "1";
        }
    }
    return parseIntStr(b, 2LL);
}

const char* commatize(long long n) {
    const char* s = str_int(n);
    const char* out = "";
    int i = 0LL;
    int cnt = 0LL;
    int neg = 0LL;
    if (strcmp(_substring(s, 0LL, 1LL), "-") == 0) {
        neg = 1LL;
        s = _substring(s, 1LL, strlen(s));
    }
    i = strlen(s) - 1LL;
    while (i >= 0LL) {
        out = str_concat(_substring(s, i, i + 1LL), out);
        cnt = cnt + 1LL;
        if ((cnt == 3LL) && (i != 0LL)) {
            out = str_concat(",", out);
            cnt = 0LL;
        }
        i = i - 1LL;
    }
    if (neg) {
        out = str_concat("-", out);
    }
    return out;
}

void user_main() {
    bigrat *cw = calkinWilf(20LL);
    size_t cw_len = calkinWilf_len;
    puts("The first 20 terms of the Calkin-Wilf sequnence are:");
    int i = 0LL;
    while (i < 20LL) {
        bigrat r = cw[(int)(i)];
        const char* s = str_int(mpz_get_si(num(r)));
        if (mpz_get_si(denom(r)) != 1LL) {
            s = str_concat(str_concat(s, "/"), str_int(mpz_get_si(denom(r))));
        }
        puts(str_concat(str_concat(str_int(i + (int)(1LL)), ": "), s));
        i = i + 1LL;
    }
    bigrat r = user_bigrat(83116LL, 51639LL);
    long long *cf = toContinued(r);
    size_t cf_len = toContinued_len;
    long long tn = termNumber(cf, cf_len);
    puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("", str_int(mpz_get_si(num(r)))), "/"), str_int(mpz_get_si(denom(r)))), " is the "), commatize(tn)), "th term of the sequence."));
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
