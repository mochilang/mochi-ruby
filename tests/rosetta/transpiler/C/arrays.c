// Generated by Mochi 0.10.32 on 2025-07-27 10:46 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static int* _slice_int(const int *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    int *res = NULL;
    if (n) {
        res = malloc(n * sizeof(int));
        memcpy(res, arr + start, n * sizeof(int));
    }
    *out_len = n;
    return res;
}

int a[5] = {0, 0, 0, 0, 0};
size_t a_len = 5;
int cap_s = 5;

const char* listStr(int * xs, size_t xs_len);
int main(void);

const char* listStr(int * xs, size_t xs_len) {
    const char* s = "[";
    int i = 0;
    while (i < xs_len) {
        s = str_concat(s, str_int(xs[i]));
        if ((i + 1) < xs_len) {
            s = str_concat(s, " ");
        }
        i = i + 1;
    }
    s = str_concat(s, "]");
    return s;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts("len(a) = 5");
        puts(str_concat("a = ", listStr(a, a_len)));
        a[0] = 3;
        puts(str_concat("a = ", listStr(a, a_len)));
        puts(str_concat("a[0] = ", str_int(a[0])));
                size_t s_len = 0;
int *s = _slice_int(a, a_len, 0, 4, &(s_len));
        puts(str_concat("s = ", listStr(s, s_len)));
        puts(str_concat(str_concat(str_concat("len(s) = ", str_int(s_len)), "  cap(s) = "), str_int(cap_s)));
        s = _slice_int(a, a_len, 0, 5, &(s_len));
        puts(str_concat("s = ", listStr(s, s_len)));
        a[0] = 22;
        s[0] = 22;
        puts(str_concat("a = ", listStr(a, a_len)));
        puts(str_concat("s = ", listStr(s, s_len)));
        s = list_append_int(s, &s_len, 4);
        s = list_append_int(s, &s_len, 5);
        s = list_append_int(s, &s_len, 6);
        cap_s = 10;
        puts(str_concat("s = ", listStr(s, s_len)));
        puts(str_concat(str_concat(str_concat("len(s) = ", str_int(s_len)), "  cap(s) = "), str_int(cap_s)));
        a[4] = -1;
        puts(str_concat("a = ", listStr(a, a_len)));
        puts(str_concat("s = ", listStr(s, s_len)));
        s = NULL;
        for (int i = 0; i < 8; i++) {
            s = list_append_int(s, &s_len, 0);
        }
        cap_s = 8;
        puts(str_concat("s = ", listStr(s, s_len)));
        puts(str_concat(str_concat(str_concat("len(s) = ", str_int(s_len)), "  cap(s) = "), str_int(cap_s)));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
