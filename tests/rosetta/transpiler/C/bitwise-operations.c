// Generated by Mochi 0.10.32 on 2025-08-02 17:56 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int toUnsigned16(int n);
const char* bin16(int n);
int bit_and(int a, int b);
int bit_or(int a, int b);
int bit_xor(int a, int b);
int bit_not(int a);
int shl(int a, int b);
int shr(int a, int b);
int las(int a, int b);
int ras(int a, int b);
int rol(int a, int b);
int ror(int a, int b);
void bitwise(int a, int b);
int main(void);

int toUnsigned16(int n) {
    int u = n;
    if (u < 0LL) {
        u = u + 65536LL;
    }
    return u % 65536LL;
}

const char* bin16(int n) {
    int u = toUnsigned16(n);
    const char* bits = "";
    int mask = 32768LL;
    for (int i = 0LL; i < 16LL; i++) {
        if (u >= mask) {
            bits = str_concat(bits, "1");
            u = u - mask;
        } else {
            bits = str_concat(bits, "0");
        }
        mask = mask / 2LL;
    }
    return bits;
}

int bit_and(int a, int b) {
    int ua = toUnsigned16(a);
    int ub = toUnsigned16(b);
    int res = 0LL;
    int bit = 1LL;
    for (int i = 0LL; i < 16LL; i++) {
        if (((ua % 2LL) == 1LL) && ((ub % 2LL) == 1LL)) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

int bit_or(int a, int b) {
    int ua = toUnsigned16(a);
    int ub = toUnsigned16(b);
    int res = 0LL;
    int bit = 1LL;
    for (int i = 0LL; i < 16LL; i++) {
        if (((ua % 2LL) == 1LL) || ((ub % 2LL) == 1LL)) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

int bit_xor(int a, int b) {
    int ua = toUnsigned16(a);
    int ub = toUnsigned16(b);
    int res = 0LL;
    int bit = 1LL;
    for (int i = 0LL; i < 16LL; i++) {
        int abit = ua % 2LL;
        int bbit = ub % 2LL;
        if (((abit == 1LL) && (bbit == 0LL)) || ((abit == 0LL) && (bbit == 1LL))) {
            res = res + bit;
        }
        ua = ua / 2LL;
        ub = ub / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

int bit_not(int a) {
    int ua = toUnsigned16(a);
    return 65535LL - ua;
}

int shl(int a, int b) {
    int ua = toUnsigned16(a);
    int i = 0LL;
    while (i < b) {
        ua = (ua * 2LL) % 65536LL;
        i = i + 1LL;
    }
    return ua;
}

int shr(int a, int b) {
    int ua = toUnsigned16(a);
    int i = 0LL;
    while (i < b) {
        ua = ua / 2LL;
        i = i + 1LL;
    }
    return ua;
}

int las(int a, int b) {
    return shl(a, b);
}

int ras(int a, int b) {
    int val = a;
    int i = 0LL;
    while (i < b) {
        if (val >= 0LL) {
            val = val / 2LL;
        } else {
            val = (val - 1LL) / 2LL;
        }
        i = i + 1LL;
    }
    return toUnsigned16(val);
}

int rol(int a, int b) {
    int ua = toUnsigned16(a);
    int left = shl(ua, b);
    int right = shr(ua, 16LL - b);
    return toUnsigned16(left + right);
}

int ror(int a, int b) {
    int ua = toUnsigned16(a);
    int right = shr(ua, b);
    int left = shl(ua, 16LL - b);
    return toUnsigned16(left + right);
}

void bitwise(int a, int b) {
    puts(str_concat("a:   ", bin16(a)));
    puts(str_concat("b:   ", bin16(b)));
    puts(str_concat("and: ", bin16(bit_and(a, b))));
    puts(str_concat("or:  ", bin16(bit_or(a, b))));
    puts(str_concat("xor: ", bin16(bit_xor(a, b))));
    puts(str_concat("not: ", bin16(bit_not(a))));
    if (b < 0LL) {
        puts("Right operand is negative, but all shifts require an unsigned right operand (shift distance).");
        return NULL;
    }
    puts(str_concat("shl: ", bin16(shl(a, b))));
    puts(str_concat("shr: ", bin16(shr(a, b))));
    puts(str_concat("las: ", bin16(las(a, b))));
    puts(str_concat("ras: ", bin16(ras(a, b))));
    puts(str_concat("rol: ", bin16(rol(a, b))));
    puts(str_concat("ror: ", bin16(ror(a, b))));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        bitwise(-460LL, 6LL);
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
