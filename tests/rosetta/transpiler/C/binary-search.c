// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int user_bsearch(int * arr, size_t arr_len, int x);
int bsearchRec(int * arr, size_t arr_len, int x, int low, int high);
int user_main();
int main(void);

int user_bsearch(int * arr, size_t arr_len, int x) {
    int low = 0LL;
    int high = arr_len - 1LL;
    while (low <= high) {
        int mid = (low + high) / 2LL;
        if (arr[(int)(mid)] > x) {
            high = mid - 1LL;
        } else {
            if (arr[(int)(mid)] < x) {
                low = mid + 1LL;
            } else {
                return mid;
            }
        }
    }
    return -1LL;
}

int bsearchRec(int * arr, size_t arr_len, int x, int low, int high) {
    if (high < low) {
        return -1LL;
    }
    int mid = (low + high) / 2LL;
    if (arr[(int)(mid)] > x) {
        return bsearchRec(arr, arr_len, x, low, mid - 1LL);
    } else {
        if (arr[(int)(mid)] < x) {
            return bsearchRec(arr, arr_len, x, mid + 1LL, high);
        }
    }
    return mid;
}

int user_main() {
    int nums[10] = {-31LL, 0LL, 1LL, 2LL, 2LL, 4LL, 65LL, 83LL, 99LL, 782LL};
    size_t nums_len = 10;
    int x = 2LL;
    int idx = user_bsearch(nums, nums_len, x);
    if (idx >= 0LL) {
        puts(str_concat(str_concat(str_concat(str_int(x), " is at index "), str_int(idx)), "."));
    } else {
        puts(str_concat(str_int(x), " is not found."));
    }
    x = 5LL;
    idx = bsearchRec(nums, nums_len, x, 0LL, 9LL);
    if (idx >= 0LL) {
        puts(str_concat(str_concat(str_concat(str_int(x), " is at index "), str_int(idx)), "."));
    } else {
        puts(str_concat(str_int(x), " is not found."));
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
