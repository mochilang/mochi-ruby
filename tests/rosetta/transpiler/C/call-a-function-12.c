// Generated by Mochi 0.10.32 on 2025-08-01 17:27 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct MkAddClosure MkAddClosure;
typedef struct PartialSumClosure PartialSumClosure;

struct MkAddClosure {
    int a;
};

struct PartialSumClosure {
    int x;
};

MkAddClosure mkAdd(int a);
int MkAddClosure_call(int a, int b);
int mysum(int x, int y);
PartialSumClosure partialSum(int x);
int PartialSumClosure_call(int x, int y);
int user_main();
int main(void);

MkAddClosure mkAdd(int a) {
    return (MkAddClosure){.a = a};
}

int MkAddClosure_call(int a, int b) {
    return a + b;
}

int mysum(int x, int y) {
    return x + y;
}

PartialSumClosure partialSum(int x) {
    return (PartialSumClosure){.x = x};
}

int PartialSumClosure_call(int x, int y) {
    return mysum(x, y);
}

int user_main() {
    MkAddClosure add2 = mkAdd(2LL);
    MkAddClosure add3 = mkAdd(3LL);
    puts(str_concat(str_concat(str_int(MkAddClosure_call(add2.a, 5LL)), " "), str_int(MkAddClosure_call(add3.a, 6LL))));
    PartialSumClosure partial = partialSum(13LL);
    puts(str_int(PartialSumClosure_call(partial.x, 5LL)));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
