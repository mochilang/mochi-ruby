// Generated by Mochi 0.10.32 on 2025-08-03 17:40 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t initGrid_len;
size_t *initGrid_lens;
size_t circle_len;
size_t *circle_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char*** list_append_c_charptrptr(const char* **arr, size_t *len, const char* *val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* **g = NULL;
size_t g_len = 0;
size_t *g_lens = NULL;
size_t g_lens_len = 0;

const char* * * initGrid(long long size);
void set(const char* * * g, size_t g_len, size_t* g_lens, size_t g_lens_len, long long x, long long y);
const char* * * circle(long long r);
const char* trimRight(const char* * row, size_t row_len);
int main(void);

const char* * * initGrid(long long size) {
    const char* **g = NULL;
    size_t g_len = 0;
    size_t *g_lens = NULL;
    size_t g_lens_len = 0;
    int y = 0LL;
    while (y < size) {
        const char* *row = NULL;
        size_t row_len = 0;
        int x = 0LL;
        while (x < size) {
            row = list_append_str(row, &row_len, " ");
            x = x + 1LL;
        }
        g = list_append_c_charptrptr(g, &g_len, row);
        g_lens = list_append_szt(g_lens, &g_lens_len, row_len);
        y = y + 1LL;
    }
    return initGrid_lens = g_lens, initGrid_len = g_len, g;
}

void set(const char* * * g, size_t g_len, size_t* g_lens, size_t g_lens_len, long long x, long long y) {
    if ((((x >= 0LL) && (x < g_lens[0LL])) && (y >= 0LL)) && (y < g_len)) {
        g[(int)(y)][(int)(x)] = "#";
    }
}

const char* * * circle(long long r) {
    int size = (r * 2LL) + 1LL;
    const char* **g = initGrid(size);
    size_t g_len = initGrid_len;
    size_t *g_lens = initGrid_lens;
    size_t g_lens_len = initGrid_len;
    long long x = r;
    int y = 0LL;
    int err = 1LL - r;
    while (y <= x) {
        set(g, g_len, g_lens, g_len, r + x, r + y);
        set(g, g_len, g_lens, g_len, r + y, r + x);
        set(g, g_len, g_lens, g_len, r - x, r + y);
        set(g, g_len, g_lens, g_len, r - y, r + x);
        set(g, g_len, g_lens, g_len, r - x, r - y);
        set(g, g_len, g_lens, g_len, r - y, r - x);
        set(g, g_len, g_lens, g_len, r + x, r - y);
        set(g, g_len, g_lens, g_len, r + y, r - x);
        y = y + 1LL;
        if (err < 0LL) {
            err = (err + (2LL * y)) + 1LL;
        } else {
            x = x - 1LL;
            err = (err + (2LL * (y - x))) + 1LL;
        }
    }
    return circle_lens = g_lens, circle_len = g_len, g;
}

const char* trimRight(const char* * row, size_t row_len) {
    size_t end = row_len;
    while ((end > 0LL) && (strcmp(row[(int)(end - 1LL)], " ") == 0)) {
        end = end - 1LL;
    }
    const char* s = "";
    int i = 0LL;
    while (i < end) {
        s = str_concat(s, row[(int)(i)]);
        i = i + 1LL;
    }
    return s;
}

int main(void) {
    {
        long long __start = _now();
        g = circle(10LL);
        g_len = circle_len;
        g_lens = circle_lens;
        for (size_t __i = 0; __i < g_len; __i++) {
            const char* *row = g[__i];
            size_t row_len = g_lens[__i];
            puts(trimRight(row, row_len));
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
