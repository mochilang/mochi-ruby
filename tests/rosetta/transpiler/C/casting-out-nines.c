// Generated by Mochi 0.10.32 on 2025-08-04 11:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t subset_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static char* str_list_str(const char **arr, size_t len) {
    size_t cap = 2;
    for (size_t i = 0; i < len; i++) cap += strlen(arr[i]) + 1;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        size_t n = strlen(arr[i]);
        memcpy(buf + pos, arr[i], n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Anon2 Anon2;
typedef struct Data3 Data3;
typedef struct TestCases TestCases;

struct Anon2 {
    int base;
    const char* begin;
    const char* end;
    const char* *kaprekar;
    size_t kaprekar_len;
};

struct Data3 {
    long long base;
    const char* begin;
    const char* end;
    const char* *kaprekar;
    size_t kaprekar_len;
};

struct TestCases {
    long long base;
    const char* begin;
    const char* end;
    const char* *kaprekar;
    size_t kaprekar_len;
};

TestCases testCases_init[2] = {(TestCases){.base = 10LL, .begin = "1", .end = "100", .kaprekar = (const char*[]){ "1", "9", "45", "55", "99" }, .kaprekar_len = 5}, (TestCases){.base = 17LL, .begin = "10", .end = "gg", .kaprekar = (const char*[]){ "3d", "d4", "gg" }, .kaprekar_len = 3}};
TestCases *testCases = testCases_init;
size_t testCases_len = 2;
int idx = 0LL;

long long parseIntBase(const char* s, long long base);
const char* intToBase(long long n, long long base);
const char* * subset(long long base, const char* begin, const char* end);
int main(void);

long long parseIntBase(const char* s, long long base) {
    const char* digits = "0123456789abcdefghijklmnopqrstuvwxyz";
    int n = 0LL;
    int i = 0LL;
    while (i < strlen(s)) {
        int j = 0LL;
        int v = 0LL;
        while (j < strlen(digits)) {
            if (strcmp(_substring(digits, j, j + 1LL), _substring(s, i, i + 1LL)) == 0) {
                v = j;
                break;
            }
            j = j + 1LL;
        }
        n = (n * base) + v;
        i = i + 1LL;
    }
    return n;
}

const char* intToBase(long long n, long long base) {
    const char* digits = "0123456789abcdefghijklmnopqrstuvwxyz";
    if (n == 0LL) {
        return "0";
    }
    const char* out = "";
    long long v = n;
    while (v > 0LL) {
        int d = v % base;
        out = str_concat(_substring(digits, d, d + 1LL), out);
        v = v / base;
    }
    return out;
}

const char* * subset(long long base, const char* begin, const char* end) {
    long long b = parseIntBase(begin, base);
    long long e = parseIntBase(end, base);
    const char* *out = NULL;
    size_t out_len = 0;
    long long k = b;
    while (k <= e) {
        const char* ks = intToBase(k, base);
        int mod = base - 1LL;
        int r1 = parseIntBase(ks, base) % mod;
        int r2 = (parseIntBase(ks, base) * parseIntBase(ks, base)) % mod;
        if (r1 == r2) {
            out = list_append_str(out, &out_len, ks);
        }
        k = k + 1LL;
    }
    return subset_len = out_len, out;
}

int main(void) {
    {
        long long __start = _now();
        while (idx < 2LL) {
            TestCases tc = testCases[(int)(idx)];
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("\nTest case base = ", str_int(tc.base)), ", begin = "), tc.begin), ", end = "), tc.end), ":"));
            const char* *s = subset(tc.base, tc.begin, tc.end);
            size_t s_len = subset_len;
            puts(str_concat("Subset:  ", str_list_str(s, s_len)));
            puts(str_concat("Kaprekar:", str_list_str(tc.kaprekar, tc.kaprekar_len)));
            int sx = 0LL;
            int valid = 1LL;
            int i = 0LL;
            while (i < tc.kaprekar_len) {
                const char* k = tc.kaprekar[(int)(i)];
                int found = 0LL;
                while (sx < s_len) {
                    if (strcmp(s[(int)(sx)], k) == 0) {
                        found = 1LL;
                        sx = sx + 1LL;
                        break;
                    }
                    sx = sx + 1LL;
                }
                if (!(found)) {
                    puts(str_concat(str_concat("Fail:", k), " not in subset"));
                    valid = 0LL;
                    break;
                }
                i = i + 1LL;
            }
            if (valid) {
                puts("Valid subset.");
            }
            idx = idx + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
