// Generated by Mochi 0.10.32 on 2025-08-01 12:38 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct Screen Screen;
typedef struct Window Window;

struct Screen {
    int w;
    int h;
};

struct Window {
    int x;
    int y;
    int w;
    int h;
    int maximized;
};

Window maximize(Screen s, Window win);
int user_main();
int main(void);

Window maximize(Screen s, Window win) {
    win.w = s.w;
    win.h = s.h;
    win.maximized = 1LL;
    return win;
}

int user_main() {
    Screen screen = (Screen){.w = 1920LL, .h = 1080LL};
    puts(str_concat(str_concat(str_concat("Screen size: ", str_int(screen.w)), " x "), str_int(screen.h)));
    Window win = (Window){.x = 50LL, .y = 50LL, .w = 800LL, .h = 600LL, .maximized = 0LL};
    win = maximize(screen, win);
    puts(str_concat(str_concat(str_concat("Max usable : ", str_int(win.w)), " x "), str_int(win.h)));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
