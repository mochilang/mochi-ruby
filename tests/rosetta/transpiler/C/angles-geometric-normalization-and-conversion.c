// Generated by Mochi 0.10.32 on 2025-07-26 12:40 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double d2d(double d) {
    return fmod(d, 360);
}

double g2g(double g) {
    return fmod(g, 400);
}

double m2m(double m) {
    return fmod(m, 6400);
}

double r2r(double r) {
    return fmod(r, 2 * 3.141592653589793);
}

double d2g(double d) {
    return (d2d(d) * 400) / 360;
}

double d2m(double d) {
    return (d2d(d) * 6400) / 360;
}

double d2r(double d) {
    return (d2d(d) * 3.141592653589793) / 180;
}

double g2d(double g) {
    return (g2g(g) * 360) / 400;
}

double g2m(double g) {
    return (g2g(g) * 6400) / 400;
}

double g2r(double g) {
    return (g2g(g) * 3.141592653589793) / 200;
}

double m2d(double m) {
    return (m2m(m) * 360) / 6400;
}

double m2g(double m) {
    return (m2m(m) * 400) / 6400;
}

double m2r(double m) {
    return (m2m(m) * 3.141592653589793) / 3200;
}

double r2d(double r) {
    return (r2r(r) * 180) / 3.141592653589793;
}

double r2g(double r) {
    return (r2r(r) * 200) / 3.141592653589793;
}

double r2m(double r) {
    return (r2r(r) * 3200) / 3.141592653589793;
}

int user_main() {
    int angles[] = {-(2), -(1), 0, 1, 2, 6.2831853, 16, 57.2957795, 359, 399, 6399, 1e+06};
    size_t angles_len = 12;
    puts("degrees normalized_degs gradians mils radians");
    {
        double a_arr[] = {-(2), -(1), 0, 1, 2, 6.2831853, 16, 57.2957795, 359, 399, 6399, 1e+06};
        size_t a_len = sizeof(a_arr) / sizeof(a_arr[0]);
        for (size_t i = 0; i < a_len; i++) {
            double a = a_arr[i];
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_float(a), " "), str_float(d2d(a))), " "), str_float(d2g(a))), " "), str_float(d2m(a))), " "), str_float(d2r(a))));
        }
    }
    puts("\ngradians normalized_grds degrees mils radians");
    {
        double a_arr[] = {-(2), -(1), 0, 1, 2, 6.2831853, 16, 57.2957795, 359, 399, 6399, 1e+06};
        size_t a_len = sizeof(a_arr) / sizeof(a_arr[0]);
        for (size_t i = 0; i < a_len; i++) {
            double a = a_arr[i];
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_float(a), " "), str_float(g2g(a))), " "), str_float(g2d(a))), " "), str_float(g2m(a))), " "), str_float(g2r(a))));
        }
    }
    puts("\nmils normalized_mils degrees gradians radians");
    {
        double a_arr[] = {-(2), -(1), 0, 1, 2, 6.2831853, 16, 57.2957795, 359, 399, 6399, 1e+06};
        size_t a_len = sizeof(a_arr) / sizeof(a_arr[0]);
        for (size_t i = 0; i < a_len; i++) {
            double a = a_arr[i];
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_float(a), " "), str_float(m2m(a))), " "), str_float(m2d(a))), " "), str_float(m2g(a))), " "), str_float(m2r(a))));
        }
    }
    puts("\nradians normalized_rads degrees gradians mils");
    {
        double a_arr[] = {-(2), -(1), 0, 1, 2, 6.2831853, 16, 57.2957795, 359, 399, 6399, 1e+06};
        size_t a_len = sizeof(a_arr) / sizeof(a_arr[0]);
        for (size_t i = 0; i < a_len; i++) {
            double a = a_arr[i];
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_float(a), " "), str_float(r2r(a))), " "), str_float(r2d(a))), " "), str_float(r2g(a))), " "), str_float(r2m(a))));
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
