// Generated by Mochi 0.10.32 on 2025-08-05 02:43 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t shuffle_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

long long indexOf(const char* s, const char* ch);
const char* * shuffle(const char* * xs, size_t xs_len);
void user_main();
int main(void);

long long indexOf(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* * shuffle(const char* * xs, size_t xs_len) {
    const char* *arr = xs;
    size_t arr_len = xs_len;
    long long i = arr_len - 1LL;
    while (i > 0LL) {
        long long j = _now() % (i + 1LL);
        const char* tmp = arr[(int)(i)];
        arr[(int)(i)] = arr[(int)(j)];
        arr[(int)(j)] = tmp;
        i = i - 1LL;
    }
    return shuffle_len = arr_len, arr;
}

void user_main() {
    puts("Cows and Bulls");
    puts("Guess four digit number of unique digits in the range 1 to 9.");
    puts("A correct digit but not in the correct place is a cow.");
    puts("A correct digit in the correct place is a bull.");
    const char* *digits = NULL;
    size_t digits_len = 0;
    digits = list_append_str(digits, &digits_len, "1");
    digits = list_append_str(digits, &digits_len, "2");
    digits = list_append_str(digits, &digits_len, "3");
    digits = list_append_str(digits, &digits_len, "4");
    digits = list_append_str(digits, &digits_len, "5");
    digits = list_append_str(digits, &digits_len, "6");
    digits = list_append_str(digits, &digits_len, "7");
    digits = list_append_str(digits, &digits_len, "8");
    digits = list_append_str(digits, &digits_len, "9");
    digits = shuffle(digits, digits_len);
    digits_len = shuffle_len;
    const char* pat = str_concat(str_concat(str_concat(digits[(int)(0LL)], digits[(int)(1LL)]), digits[(int)(2LL)]), digits[(int)(3LL)]);
    const char* valid = "123456789";
    while (1LL) {
        puts("Guess: ");
        const char* guess = _input();
        if (strlen(guess) != 4LL) {
            puts("Please guess a four digit number.");
            continue;
        }
        long long cows = 0LL;
        long long bulls = 0LL;
        const char* seen = "";
        long long i = 0LL;
        long long malformed = 0LL;
        while (i < 4LL) {
            const char* cg = _substring(guess, i, i + 1LL);
            if (indexOf(seen, cg) != -1LL) {
                puts(str_concat("Repeated digit: ", cg));
                malformed = 1LL;
                break;
            }
            seen = str_concat(seen, cg);
            long long pos = indexOf(pat, cg);
            if (pos == -1LL) {
                if (indexOf(valid, cg) == -1LL) {
                    puts(str_concat("Invalid digit: ", cg));
                    malformed = 1LL;
                    break;
                }
            } else {
                if (pos == i) {
                    bulls = bulls + 1LL;
                } else {
                    cows = cows + 1LL;
                }
            }
            i = i + 1LL;
        }
        if (malformed) {
            continue;
        }
        puts(str_concat(str_concat(str_concat("Cows: ", str_int(cows)), ", bulls: "), str_int(bulls)));
        if (bulls == 4LL) {
            puts("You got it.");
            break;
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
