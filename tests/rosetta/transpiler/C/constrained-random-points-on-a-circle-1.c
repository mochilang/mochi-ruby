// Generated by Mochi 0.10.32 on 2025-08-05 01:28 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; int *vals; size_t len; } MapSI;

static int map_get_si(const char *keys[], const int vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return 0;
}

static void map_set_si(const char *keys[], int vals[], size_t *len, const char *key, int val) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char*** list_append_c_charptrptr(const char* **arr, size_t *len, const char* *val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long nPts = 100LL;
long long rMin = 10LL;
long long rMax = 15LL;
long long span = 0;
const char* **rows = NULL;
size_t rows_len = 0;
size_t *rows_lens = NULL;
size_t rows_lens_len = 0;
long long r = 0LL;
long long u = 0LL;
long long min2 = 0;
long long max2 = 0;
long long n = 0LL;
long long i = 0LL;

int main(void);

int main(void) {
    {
        long long __start = _now();
        span = (rMax + 1LL) + rMax;
        while (r < span) {
            const char* *row = NULL;
            size_t row_len = 0;
            long long c = 0LL;
            while (c < (span * 2LL)) {
                row = list_append_int(row, &row_len, " ");
                c = c + 1LL;
            }
            rows = list_append_c_charptrptr(rows, &rows_len, row);
            rows_lens = list_append_szt(rows_lens, &rows_lens_len, row_len);
            r = r + 1LL;
        }
        const char* seen_keys[16] = {};
        int seen_vals[16] = {};
        size_t seen_len = 0;
        MapSI seen = { seen_keys, seen_vals, seen_len };
        min2 = rMin * rMin;
        max2 = rMax * rMax;
        while (n < nPts) {
            long long x = (_now() % span) - rMax;
            long long y = (_now() % span) - rMax;
            long long rs = (x * x) + (y * y);
            if ((rs < min2) || (rs > max2)) {
                continue;
            }
            n = n + 1LL;
            long long row = y + rMax;
            long long col = (x + rMax) * 2LL;
            rows[(int)(row)][(int)(col)] = "*";
            const char* key = str_concat(str_concat(str_int(row), ","), str_int(col));
            if (!(map_get_si(seen_keys, seen_vals, seen_len, key))) {
                map_set_si(seen_keys, seen_vals, &seen_len, key, 1LL);
                u = u + 1LL;
            }
        }
        while (i < span) {
            const char* line = "";
            long long j = 0LL;
            while (j < (span * 2LL)) {
                line = str_concat(line, rows[(int)(i)][(int)(j)]);
                j = j + 1LL;
            }
            puts(line);
            i = i + 1LL;
        }
        puts(str_concat(str_int(u), " unique points"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
