// Generated by Mochi 0.10.32 on 2025-08-01 11:47 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* given[23] = {"ABCD", "CABD", "ACDB", "DACB", "BCDA", "ACBD", "ADCB", "CDAB", "DABC", "BCAD", "CADB", "CDBA", "CBAD", "ABDC", "ADBC", "BDCA", "DCBA", "BACD", "BADC", "BDAC", "CBDA", "DBCA", "DCAB"};
size_t given_len = 23;

int idx(const char* ch);
int user_main();
int main(void);

int idx(const char* ch) {
    if (strcmp(ch, "A") == 0) {
        return 0LL;
    }
    if (strcmp(ch, "B") == 0) {
        return 1LL;
    }
    if (strcmp(ch, "C") == 0) {
        return 2LL;
    }
    return 3LL;
}

int user_main() {
    const char* res = "";
    int i = 0LL;
    while (i < strlen(given[(int)(0LL)])) {
        int *counts = NULL;
        size_t counts_len = 0;
        counts = list_append_int(counts, &counts_len, 0LL);
        counts = list_append_int(counts, &counts_len, 0LL);
        counts = list_append_int(counts, &counts_len, 0LL);
        counts = list_append_int(counts, &counts_len, 0LL);
        {
            const char* p_arr[] = {"ABCD", "CABD", "ACDB", "DACB", "BCDA", "ACBD", "ADCB", "CDAB", "DABC", "BCAD", "CADB", "CDBA", "CBAD", "ABDC", "ADBC", "BDCA", "DCBA", "BACD", "BADC", "BDAC", "CBDA", "DBCA", "DCAB"};
            size_t p_len = sizeof(p_arr) / sizeof(p_arr[0]);
            for (size_t __i = 0; __i < p_len; __i++) {
                const char* p = p_arr[__i];
                const char* ch = _substring(p, i, i + 1LL);
                int j = idx(ch);
                counts[(int)(j)] = counts[(int)(j)] + 1LL;
            }
        }
        int j = 0LL;
        while (j < 4LL) {
            if ((counts[(int)(j)] % 2LL) == 1LL) {
                if (j == 0LL) {
                    res = str_concat(res, "A");
                } else {
                    if (j == 1LL) {
                        res = str_concat(res, "B");
                    } else {
                        if (j == 2LL) {
                            res = str_concat(res, "C");
                        } else {
                            res = str_concat(res, "D");
                        }
                    }
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    puts(res);
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
