// Generated by Mochi 0.10.32 on 2025-07-31 18:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <ctype.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_upper(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = toupper((unsigned char)out[i]);
    return out;
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* trimSpace(const char* s);
int isUpper(const char* ch);
const char* padLeft(const char* s, int w);
const char* snakeToCamel(const char* s);
const char* camelToSnake(const char* s);
int user_main();
int main(void);

const char* trimSpace(const char* s) {
    int start = 0LL;
    while ((start < strlen(s)) && (strcmp(_substring(s, start, start + 1LL), " ") == 0)) {
        start = start + 1LL;
    }
    int end = strlen(s);
    while ((end > start) && (strcmp(_substring(s, end - 1LL, end), " ") == 0)) {
        end = end - 1LL;
    }
    return _substring(s, start, end);
}

int isUpper(const char* ch) {
    return (strcmp(ch, "A") >= 0) && (strcmp(ch, "Z") <= 0);
}

const char* padLeft(const char* s, int w) {
    const char* res = "";
    int n = w - strlen(s);
    while (n > 0LL) {
        res = str_concat(res, " ");
        n = n - 1LL;
    }
    return str_concat(res, s);
}

const char* snakeToCamel(const char* s) {
    s = trimSpace(s);
    const char* out = "";
    int up = 0LL;
    int i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if ((((strcmp(ch, "_") == 0) || (strcmp(ch, "-") == 0)) || (strcmp(ch, " ") == 0)) || (strcmp(ch, ".") == 0)) {
            up = 1LL;
            i = i + 1LL;
            continue;
        }
        if (i == 0LL) {
            out = str_concat(out, str_lower(ch));
            up = 0LL;
            i = i + 1LL;
            continue;
        }
        if (up) {
            out = str_concat(out, str_upper(ch));
            up = 0LL;
        } else {
            out = str_concat(out, ch);
        }
        i = i + 1LL;
    }
    return out;
}

const char* camelToSnake(const char* s) {
    s = trimSpace(s);
    const char* out = "";
    int prevUnd = 0LL;
    int i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (((strcmp(ch, " ") == 0) || (strcmp(ch, "-") == 0)) || (strcmp(ch, ".") == 0)) {
            if (!(prevUnd) && (strlen(out) > 0LL)) {
                out = str_concat(out, "_");
                prevUnd = 1LL;
            }
            i = i + 1LL;
            continue;
        }
        if (strcmp(ch, "_") == 0) {
            if (!(prevUnd) && (strlen(out) > 0LL)) {
                out = str_concat(out, "_");
                prevUnd = 1LL;
            }
            i = i + 1LL;
            continue;
        }
        if (isUpper(ch)) {
            if ((i > 0LL) && !(prevUnd)) {
                out = str_concat(out, "_");
            }
            out = str_concat(out, str_lower(ch));
            prevUnd = 0LL;
        } else {
            out = str_concat(out, str_lower(ch));
            prevUnd = 0LL;
        }
        i = i + 1LL;
    }
    int start = 0LL;
    while ((start < strlen(out)) && (strcmp(_substring(out, start, start + 1LL), "_") == 0)) {
        start = start + 1LL;
    }
    int end = strlen(out);
    while ((end > start) && (strcmp(_substring(out, end - 1LL, end), "_") == 0)) {
        end = end - 1LL;
    }
    out = _substring(out, start, end);
    const char* res = "";
    int j = 0LL;
    int lastUnd = 0LL;
    while (j < strlen(out)) {
        const char* c = _substring(out, j, j + 1LL);
        if (strcmp(c, "_") == 0) {
            if (!(lastUnd)) {
                res = str_concat(res, c);
            }
            lastUnd = 1LL;
        } else {
            res = str_concat(res, c);
            lastUnd = 0LL;
        }
        j = j + 1LL;
    }
    return res;
}

int user_main() {
    const char* samples[12] = {"snakeCase", "snake_case", "snake-case", "snake case", "snake CASE", "snake.case", "variable_10_case", "variable10Case", "ɛrgo rE tHis", "hurry-up-joe!", "c://my-docs/happy_Flag-Day/12.doc", " spaces "};
    size_t samples_len = 12;
    puts("=== To snake_case ===");
    {
        const char* s_arr[] = {"snakeCase", "snake_case", "snake-case", "snake case", "snake CASE", "snake.case", "variable_10_case", "variable10Case", "ɛrgo rE tHis", "hurry-up-joe!", "c://my-docs/happy_Flag-Day/12.doc", " spaces "};
        size_t s_len = sizeof(s_arr) / sizeof(s_arr[0]);
        for (size_t __i = 0; __i < s_len; __i++) {
            const char* s = s_arr[__i];
            puts(str_concat(str_concat(padLeft(s, 34LL), " => "), camelToSnake(s)));
        }
    }
    puts("");
    puts("=== To camelCase ===");
    {
        const char* s_arr[] = {"snakeCase", "snake_case", "snake-case", "snake case", "snake CASE", "snake.case", "variable_10_case", "variable10Case", "ɛrgo rE tHis", "hurry-up-joe!", "c://my-docs/happy_Flag-Day/12.doc", " spaces "};
        size_t s_len = sizeof(s_arr) / sizeof(s_arr[0]);
        for (size_t __i = 0; __i < s_len; __i++) {
            const char* s = s_arr[__i];
            puts(str_concat(str_concat(padLeft(s, 34LL), " => "), snakeToCamel(s)));
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
