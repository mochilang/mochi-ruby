// Generated by Mochi 0.10.32 on 2025-08-05 02:43 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t sortStrings_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

typedef struct { const char **keys; const char **vals; size_t len; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

typedef struct Anon1 Anon1;
typedef struct Anon2 Anon2;

struct Anon1 {
    int err;
    const char* res;
};

struct Anon2 {
    int err;
    const char* res;
};

const char* stx = "";
const char* etx = "";

long long contains(const char* s, const char* ch);
const char* * sortStrings(const char* * xs, size_t xs_len);
Anon2 bwt(const char* s);
const char* ibwt(const char* r);
const char* makePrintable(const char* s);
void user_main();
int main(void);

long long contains(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return 1LL;
        }
        i = i + 1LL;
    }
    return 0LL;
}

const char* * sortStrings(const char* * xs, size_t xs_len) {
    const char* *arr = xs;
    size_t arr_len = xs_len;
    long long n = arr_len;
    long long i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j < (n - 1LL)) {
            if (strcmp(arr[(int)(j)], arr[(int)(j + 1LL)]) > 0) {
                const char* tmp = arr[(int)(j)];
                arr[(int)(j)] = arr[(int)(j + 1LL)];
                arr[(int)(j + 1LL)] = tmp;
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return sortStrings_len = arr_len, arr;
}

Anon2 bwt(const char* s) {
    if (strstr(s, stx) != NULL || strstr(s, etx) != NULL) {
        return (Anon2){.err = 1LL, .res = ""};
    }
    s = str_concat(str_concat(stx, s), etx);
    long long le = strlen(s);
    const char* *table = NULL;
    size_t table_len = 0;
    long long i = 0LL;
    while (i < le) {
        const char* rot = str_concat(_substring(s, i, le), _substring(s, 0LL, i));
        table = list_append_str(table, &table_len, rot);
        i = i + 1LL;
    }
    table = sortStrings(table, table_len);
    table_len = sortStrings_len;
    const char* last = "";
    i = 0LL;
    while (i < le) {
        last = str_concat(last, _substring(table[(int)(i)], le - 1LL, le));
        i = i + 1LL;
    }
    return (Anon2){.err = 0LL, .res = last};
}

const char* ibwt(const char* r) {
    long long le = strlen(r);
    const char* *table = NULL;
    size_t table_len = 0;
    long long i = 0LL;
    while (i < le) {
        table = list_append_str(table, &table_len, "");
        i = i + 1LL;
    }
    long long n = 0LL;
    while (n < le) {
        i = 0LL;
        while (i < le) {
            table[(int)(i)] = str_concat(_substring(r, i, i + 1LL), table[(int)(i)]);
            i = i + 1LL;
        }
        table = sortStrings(table, table_len);
        table_len = sortStrings_len;
        n = n + 1LL;
    }
    i = 0LL;
    while (i < le) {
        if (strcmp(_substring(table[(int)(i)], le - 1LL, le), etx) == 0) {
            return _substring(table[(int)(i)], 1LL, le - 1LL);
        }
        i = i + 1LL;
    }
    return "";
}

const char* makePrintable(const char* s) {
    const char* out = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (strcmp(ch, stx) == 0) {
            out = str_concat(out, "^");
        } else {
            if (strcmp(ch, etx) == 0) {
                out = str_concat(out, "|");
            } else {
                out = str_concat(out, ch);
            }
        }
        i = i + 1LL;
    }
    return out;
}

void user_main() {
    const char* *examples = NULL;
    size_t examples_len = 0;
    examples = list_append_str(examples, &examples_len, "banana");
    examples = list_append_str(examples, &examples_len, "appellee");
    examples = list_append_str(examples, &examples_len, "dogwood");
    examples = list_append_str(examples, &examples_len, "TO BE OR NOT TO BE OR WANT TO BE OR NOT?");
    examples = list_append_str(examples, &examples_len, "SIX.MIXED.PIXIES.SIFT.SIXTY.PIXIE.DUST.BOXES");
    examples = list_append_str(examples, &examples_len, "ABC");
    {
        const char* t_arr[] = {"banana", "appellee", "dogwood", "TO BE OR NOT TO BE OR WANT TO BE OR NOT?", "SIX.MIXED.PIXIES.SIFT.SIXTY.PIXIE.DUST.BOXES", "ABC"};
        size_t t_len = sizeof(t_arr) / sizeof(t_arr[0]);
        for (size_t __i = 0; __i < t_len; __i++) {
            const char* t = t_arr[__i];
            puts(makePrintable(t));
            Anon2 res = bwt(t);
            if (res.err) {
                puts(" --> ERROR: String can't contain STX or ETX");
                puts(" -->");
            } else {
                const char* enc = res.res;
                puts(str_concat(" --> ", makePrintable(enc)));
                const char* r = ibwt(enc);
                puts(str_concat(" --> ", r));
            }
            puts("");
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
