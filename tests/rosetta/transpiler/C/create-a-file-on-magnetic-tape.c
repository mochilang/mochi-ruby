// Generated by Mochi 0.10.32 on 2025-08-05 01:10 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

typedef struct { const char **keys; const char **vals; size_t len; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

int gzipWriter(int w);
int tarWriter(int w);
void tarWriteHeader(int w, MapSS hdr);
void tarWrite(int w, const char* data);
void user_main();
int main(void);

int gzipWriter(int w) {
    return w;
}

int tarWriter(int w) {
    return w;
}

void tarWriteHeader(int w, MapSS hdr) {
}

void tarWrite(int w, const char* data) {
}

void user_main() {
    const char* filename = "TAPE.FILE";
    const char* data = "";
    const char* outfile = "";
    long long gzipFlag = 0LL;
    long long w = NULL;
    if (0LL) {
        w = NULL;
    }
    if (gzipFlag) {
        w = gzipWriter(w);
    }
    w = tarWriter(w);
    const char* hdr_keys[23] = {"Name", "Mode", "Size", "ModTime", "Typeflag", "Uname", "Gname"};
    const char* hdr_vals[23] = {filename, str_int(432), strlen(data), _now(), str_int(0), "guest", "guest"};
    size_t hdr_len = 7;
    MapSS hdr = { hdr_keys, hdr_vals, hdr_len };
    tarWriteHeader(w, hdr);
    tarWrite(w, data);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
