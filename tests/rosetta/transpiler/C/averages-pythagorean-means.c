// Generated by Mochi 0.10.32 on 2025-07-27 18:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_bool(int v) {
    return strdup(v ? "true" : "false");
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double user_powf(double base, int exp);
double nthRoot(double x, int n);
int user_main();
int main(void);

double user_powf(double base, int exp) {
    double result = 1;
    int i = 0;
    while (i < exp) {
        result = result * base;
        i = i + 1;
    }
    return result;
}

double nthRoot(double x, int n) {
    double low = 0;
    double high = x;
    int i = 0;
    while (i < 60) {
        double mid = (low + high) / 2;
        if (user_powf(mid, n) > x) {
            high = mid;
        } else {
            low = mid;
        }
        i = i + 1;
    }
    return low;
}

int user_main() {
    double sum = 0;
    double sumRecip = 0;
    double prod = 1;
    int n = 1;
    while (n <= 10) {
        double f = (double)(n);
        sum = sum + f;
        sumRecip = sumRecip + (1 / f);
        prod = prod * f;
        n = n + 1;
    }
    double count = 10;
    double a = sum / count;
    double g = nthRoot(prod, 10);
    double h = count / sumRecip;
    puts(str_concat(str_concat(str_concat(str_concat(str_concat("A: ", str_float(a)), " G: "), str_float(g)), " H: "), str_float(h)));
    puts(str_concat("A >= G >= H: ", str_bool((a >= g) && (g >= h))));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
