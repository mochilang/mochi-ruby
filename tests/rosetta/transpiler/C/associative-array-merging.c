// Generated by Mochi 0.10.32 on 2025-07-27 15:57 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

typedef struct { const char **keys; const char **vals; size_t len; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

static void map_set_ss(const char *keys[], const char *vals[], size_t *len, const char *key, const char *val) {
    for (size_t i = 0; i < *len; i++) {
        if (strcmp(keys[i], key) == 0) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct Anon1 Anon1;
typedef struct Anon2 Anon2;

struct Anon1 {
    const char* name;
    double price;
    const char* color;
};

struct Anon2 {
    double price;
    const char* color;
    int year;
};

MapSS merge(MapSS base, MapSS update);
int user_main();
int main(void);

MapSS merge(MapSS base, MapSS update) {
    const char* result_keys[16] = {};
    const char* result_vals[16] = {};
    size_t result_len = 0;
    MapSS result = { result_keys, result_vals, result_len };
    for (size_t __i = 0; __i < base.len; __i++) {
        const char* k = base.keys[__i];
        map_set_ss(result.keys, result.vals, &result.len, k, map_get_ss(base.keys, base.vals, base.len, k));
    }
    for (size_t __i = 0; __i < update.len; __i++) {
        const char* k = update.keys[__i];
        map_set_ss(result.keys, result.vals, &result.len, k, map_get_ss(update.keys, update.vals, update.len, k));
    }
    return result;
}

int user_main() {
    const char* base_keys[19] = {"name", "price", "color"};
    const char* base_vals[19] = {"Rocket Skates", str_float(12.75), "yellow"};
    size_t base_len = 3;
    MapSS base = { base_keys, base_vals, base_len };
    const char* update_keys[19] = {"price", "color", "year"};
    const char* update_vals[19] = {str_float(15.25), "red", str_int(1974)};
    size_t update_len = 3;
    MapSS update = { update_keys, update_vals, update_len };
    MapSS result = merge(base, update);
    printf("%d\n", result);
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
