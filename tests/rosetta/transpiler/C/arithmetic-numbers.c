// Generated by Mochi 0.10.32 on 2025-07-26 18:17 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t sieve_len;
size_t primesFrom_len;
size_t arithmeticNumbers_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int * sieve(int limit);
int * primesFrom(int * spf, size_t spf_len, int limit);
const char* pad3(int n);
const char* commatize(int n);
int primeCount(int * primes, size_t primes_len, int last, int * spf, size_t spf_len);
int * arithmeticNumbers(int limit, int * spf, size_t spf_len);
int user_main();
int main(void);

int * sieve(int limit) {
    int *spf = malloc(0 * sizeof(int));
    size_t spf_len = 0;
    int i = 0;
    while (i <= limit) {
        spf = list_append_int(spf, &spf_len, 0);
        i = i + 1;
    }
    i = 2;
    while (i <= limit) {
        if (spf[i] == 0) {
            spf[i] = i;
            if ((i * i) <= limit) {
                int j = i * i;
                while (j <= limit) {
                    if (spf[j] == 0) {
                        spf[j] = i;
                    }
                    j = j + i;
                }
            }
        }
        i = i + 1;
    }
    return sieve_len = spf_len, spf;
}

int * primesFrom(int * spf, size_t spf_len, int limit) {
    int *primes = malloc(0 * sizeof(int));
    size_t primes_len = 0;
    int i = 3;
    while (i <= limit) {
        if (spf[i] == i) {
            primes = list_append_int(primes, &primes_len, i);
        }
        i = i + 1;
    }
    return primesFrom_len = primes_len, primes;
}

const char* pad3(int n) {
    const char* s = str_int(n);
    while (strlen(s) < 3) {
        s = str_concat(" ", s);
    }
    return s;
}

const char* commatize(int n) {
    const char* s = str_int(n);
    const char* out = "";
    int i = strlen(s) - 1;
    int c = 0;
    while (i >= 0) {
        out = str_concat(_substring(s, i, i + 1), out);
        c = c + 1;
        if (((c % 3) == 0) && (i > 0)) {
            out = str_concat(",", out);
        }
        i = i - 1;
    }
    return out;
}

int primeCount(int * primes, size_t primes_len, int last, int * spf, size_t spf_len) {
    int lo = 0;
    size_t hi = primes_len;
    while (lo < hi) {
        int mid = (lo + hi) / 2;
        if (primes[mid] < last) {
            lo = mid + 1;
        } else {
            hi = mid;
        }
    }
    int count = lo + 1;
    if (spf[last] != last) {
        count = count - 1;
    }
    return count;
}

int * arithmeticNumbers(int limit, int * spf, size_t spf_len) {
    int *arr = malloc(1 * sizeof(int));
    arr[0] = 1;
    size_t arr_len = 1;
    int n = 3;
    while (1 < limit) {
        if (spf[n] == n) {
            arr = (int[]){ 1, n };
        } else {
            int x = n;
            int sigma = 1;
            int tau = 1;
            while (x > 1) {
                int p = spf[x];
                if (p == 0) {
                    p = x;
                }
                int cnt = 0;
                int power = p;
                int sum = 1;
                while ((x % p) == 0) {
                    x = x / p;
                    cnt = cnt + 1;
                    sum = sum + power;
                    power = power * p;
                }
                sigma = sigma * sum;
                tau = tau * (cnt + 1);
            }
            if ((sigma % tau) == 0) {
                arr = (int[]){ 1, 3, n };
            }
        }
        n = n + 1;
    }
    return arithmeticNumbers_len = arr_len, arr;
}

int user_main() {
    int limit = 1228663;
    int *spf = sieve(limit);
    size_t spf_len = sieve_len;
    int *primes = primesFrom(spf, spf_len, limit);
    size_t primes_len = primesFrom_len;
    int *arr = arithmeticNumbers(1000000, spf, spf_len);
    size_t arr_len = arithmeticNumbers_len;
    puts("The first 100 arithmetic numbers are:");
    int i = 0;
    while (i < 100) {
        const char* line = "";
        int j = 0;
        while (j < 10) {
            line = str_concat(line, pad3(arr[i + j]));
            if (j < 9) {
                line = str_concat(line, " ");
            }
            j = j + 1;
        }
        puts(line);
        i = i + 10;
    }
    {
        int x_arr[] = {1000, 10000, 100000, 1000000};
        size_t x_len = sizeof(x_arr) / sizeof(x_arr[0]);
        for (size_t __i = 0; __i < x_len; __i++) {
            int x = x_arr[__i];
            int last = arr[x - 1];
            const char* lastc = commatize(last);
            puts(str_concat(str_concat(str_concat("\nThe ", commatize(x)), "th arithmetic number is: "), lastc));
            int pc = primeCount(primes, primes_len, last, spf, spf_len);
            int comp = (x - pc) - 1;
            puts(str_concat(str_concat(str_concat(str_concat("The count of such numbers <= ", lastc), " which are composite is "), commatize(comp)), "."));
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
