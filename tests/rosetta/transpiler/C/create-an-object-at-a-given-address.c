// Generated by Mochi 0.10.32 on 2025-08-05 01:13 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static long long* list_append_int(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

const char* listStr(long long * xs, size_t xs_len);
void pointerDemo();
void sliceDemo();
int main(void);

const char* listStr(long long * xs, size_t xs_len) {
    const char* s = "[";
    long long i = 0LL;
    while (i < xs_len) {
        s = str_concat(s, str_int(xs[(int)(i)]));
        if (i < (xs_len - 1LL)) {
            s = str_concat(s, " ");
        }
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

void pointerDemo() {
    puts("Pointer:");
    long long i = 0LL;
    puts("Before:");
    puts(str_concat(str_concat(str_concat("\t<address>: ", str_int(i)), ", "), str_int(i)));
    i = 3LL;
    puts("After:");
    puts(str_concat(str_concat(str_concat("\t<address>: ", str_int(i)), ", "), str_int(i)));
}

void sliceDemo() {
    puts("Slice:");
    long long *a = NULL;
    size_t a_len = 0;
    for (int _ = 0LL; _ < 10LL; _++) {
        a = list_append_long_long(a, &a_len, 0LL);
    }
    long long *s = a;
    size_t s_len = a_len;
    puts("Before:");
    puts(str_concat("\ts: ", listStr(s, s_len)));
    puts(str_concat("\ta: ", listStr(a, a_len)));
    long long *data = NULL;
    size_t data_len = 0;
    data = list_append_int(data, &data_len, 65LL);
    data = list_append_int(data, &data_len, 32LL);
    data = list_append_int(data, &data_len, 115LL);
    data = list_append_int(data, &data_len, 116LL);
    data = list_append_int(data, &data_len, 114LL);
    data = list_append_int(data, &data_len, 105LL);
    data = list_append_int(data, &data_len, 110LL);
    data = list_append_int(data, &data_len, 103LL);
    data = list_append_int(data, &data_len, 46LL);
    long long idx = 0LL;
    while (idx < 9LL) {
        s[(int)(idx)] = data[(int)(idx)];
        idx = idx + 1LL;
    }
    puts("After:");
    puts(str_concat("\ts: ", listStr(s, s_len)));
    puts(str_concat("\ta: ", listStr(a, a_len)));
}

int main(void) {
    {
        long long __start = _now();
        pointerDemo();
        puts("");
        sliceDemo();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
