// Generated by Mochi 0.10.32 on 2025-08-04 20:02 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t unpackSym_len;
size_t *unpackSym_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

typedef struct { const char **keys; const char **vals; size_t len; } MapSS;

static const char* map_get_ss(const char *keys[], const char *vals[], size_t len, const char *key) {
    for (size_t i = 0; i < len; i++) {
        if (strcmp(keys[i], key) == 0) return vals[i];
    }
    return "";
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

typedef struct Anon1 Anon1;

struct Anon1 {
    long long order;
    double *ele;
    size_t ele_len;
};

double sqrtApprox(double x);
Anon1 makeSym(long long order, double * elements, size_t elements_len);
double * * unpackSym(Anon1 m);
void printMat(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
void printSym(Anon1 m);
void printLower(Anon1 m);
Anon1 choleskyLower(Anon1 a);
void demo(Anon1 a);
int main(void);

double sqrtApprox(double x) {
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

Anon1 makeSym(long long order, double * elements, size_t elements_len) {
    return (Anon1){.order = order, .ele = elements, .ele_len = elements_len};
}

double * * unpackSym(Anon1 m) {
    long long n = m.order;
    double *ele = m.ele;
    size_t ele_len = m.ele_len;
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long idx = 0LL;
    long long r = 0LL;
    while (r < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long c = 0LL;
        while (c <= r) {
            row = list_append_double(row, &row_len, ele[(int)(idx)]);
            idx = idx + 1LL;
            c = c + 1LL;
        }
        while (c < n) {
            row = list_append_double(row, &row_len, 0.0);
            c = c + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        r = r + 1LL;
    }
    r = 0LL;
    while (r < n) {
        long long c = r + 1LL;
        while (c < n) {
            mat[(int)(r)][(int)(c)] = mat[(int)(c)][(int)(r)];
            c = c + 1LL;
        }
        r = r + 1LL;
    }
    return unpackSym_lens = mat_lens, unpackSym_len = mat_len, mat;
}

void printMat(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long i = 0LL;
    while (i < m_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < m_lens[i]) {
            line = str_concat(line, str_float(m[(int)(i)][(int)(j)]));
            if (j < (m_lens[i] - 1LL)) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

void printSym(Anon1 m) {
    double** __tmp1 = unpackSym(m);
    printMat(__tmp1, unpackSym_len, unpackSym_lens, unpackSym_len);
}

void printLower(Anon1 m) {
    long long n = m.order;
    double *ele = m.ele;
    size_t ele_len = m.ele_len;
    double **mat = NULL;
    size_t mat_len = 0;
    size_t *mat_lens = NULL;
    size_t mat_lens_len = 0;
    long long idx = 0LL;
    long long r = 0LL;
    while (r < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long c = 0LL;
        while (c <= r) {
            row = list_append_double(row, &row_len, ele[(int)(idx)]);
            idx = idx + 1LL;
            c = c + 1LL;
        }
        while (c < n) {
            row = list_append_double(row, &row_len, 0.0);
            c = c + 1LL;
        }
        mat = list_append_doubleptr(mat, &mat_len, row);
        mat_lens = list_append_szt(mat_lens, &mat_lens_len, row_len);
        r = r + 1LL;
    }
    printMat(mat, mat_len, mat_lens, mat_len);
}

Anon1 choleskyLower(Anon1 a) {
    long long n = a.order;
    double *ae = a.ele;
    size_t ae_len = a.ele_len;
    double *le = NULL;
    size_t le_len = 0;
    long long idx = 0LL;
    while (idx < ae_len) {
        le = list_append_double(le, &le_len, 0.0);
        idx = idx + 1LL;
    }
    long long row = 1LL;
    long long col = 1LL;
    long long dr = 0LL;
    long long dc = 0LL;
    long long i = 0LL;
    while (i < ae_len) {
        double e = ae[(int)(i)];
        if (i < dr) {
            double d = (e - le[(int)(i)]) / le[(int)(dc)];
            le[(int)(i)] = d;
            long long ci = col;
            long long cx = dc;
            long long j = i + 1LL;
            while (j <= dr) {
                cx = cx + ci;
                ci = ci + 1LL;
                le[(int)(j)] = le[(int)(j)] + (d * le[(int)(cx)]);
                j = j + 1LL;
            }
            col = col + 1LL;
            dc = dc + col;
        } else {
            le[(int)(i)] = sqrtApprox(e - le[(int)(i)]);
            row = row + 1LL;
            dr = dr + row;
            col = 1LL;
            dc = 0LL;
        }
        i = i + 1LL;
    }
    return (Anon1){.order = n, .ele = le, .ele_len = le_len};
}

void demo(Anon1 a) {
    puts("A:");
    printSym(a);
    puts("L:");
    Anon1 l = choleskyLower(a);
    printLower(l);
}

int main(void) {
    {
        long long __start = _now();
        demo(makeSym(3LL, (double[]){ 25.0, 15.0, 18.0, -(5.0), 0.0, 11.0 }, 6));
        demo(makeSym(4LL, (double[]){ 18.0, 22.0, 70.0, 54.0, 86.0, 174.0, 42.0, 62.0, 134.0, 106.0 }, 10));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
