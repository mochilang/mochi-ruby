// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <gmp.h>
typedef mpq_t* bigrat;
typedef mpz_t* bigint;

size_t bellTriangle_len;
size_t *bellTriangle_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_bigint(const mpz_t v) {
    return mpz_get_str(NULL, 10, v);
}

static bigrat _bigrat(long n, long d) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_set_si(*r, n, d);
    mpq_canonicalize(*r);
    return r;
}

static bigrat _add(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_add(*r, *a, *b);
    return r;
}

static bigrat _sub(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_sub(*r, *a, *b);
    return r;
}

static bigrat _mul(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_mul(*r, *a, *b);
    return r;
}

static bigrat _div(bigrat a, bigrat b) {
    bigrat r = malloc(sizeof(mpq_t));
    mpq_init(*r);
    mpq_div(*r, *a, *b);
    return r;
}

static mpz_srcptr num(bigrat a) { return mpq_numref(*a); }
static mpz_srcptr denom(bigrat a) { return mpq_denref(*a); }

static char* _padStart(const char *s, int width, const char *pad) {
    size_t slen = strlen(s);
    size_t plen = strlen(pad);
    if ((int)slen >= width || plen == 0) return strdup(s);
    size_t padlen = width - slen;
    char *out = malloc(width + 1);
    size_t pos = 0;
    for (size_t i = 0; i < padlen; i++) out[pos++] = pad[i % plen];
    memcpy(out + pos, s, slen);
    out[padlen + slen] = 0;
    return out;
}

static int* list_append_int_new(const int *arr, size_t len, int val) {
    int *res = malloc((len + 1) * sizeof(int));
    if (arr && len) memcpy(res, arr, len * sizeof(int));
    res[len] = val;
    return res;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char*** list_append_strptr(const char ***arr, size_t *len, const char **val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

bigint * * bellTriangle(int n);
int user_main();
int main(void);

bigint * * bellTriangle(int n) {
    bigint **tri = NULL;
    size_t tri_len = 0;
    size_t *tri_lens = NULL;
    size_t tri_lens_len = 0;
    int i = 0LL;
    while (i < n) {
        bigint *row = NULL;
        size_t row_len = 0;
        int j = 0LL;
        while (j < i) {
                        row = list_append_int_new(row, row_len, 0LL);
            j = j + 1LL;
        }
        tri = list_append_strptr(tri, &tri_len, row);
        tri_lens = list_append_szt(tri_lens, &tri_lens_len, row_len);
        i = i + 1LL;
    }
    tri[(int)(1LL)][(int)(0LL)] = 1LL;
    i = 2LL;
    while (i < n) {
        tri[(int)(i)][(int)(0LL)] = tri[(int)(i - 1LL)][(int)(i - 2LL)];
        int j = 1LL;
        while (j < i) {
            tri[(int)(i)][(int)(j)] = tri[(int)(i)][(int)(j - 1LL)] + tri[(int)(i - 1LL)][(int)(j - 1LL)];
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return bellTriangle_lens = tri_lens, bellTriangle_len = tri_len, tri;
}

int user_main() {
    bigint **bt = bellTriangle(51LL);
    size_t bt_len = bellTriangle_len;
    size_t *bt_lens = bellTriangle_lens;
    size_t bt_lens_len = bellTriangle_len;
    puts("First fifteen and fiftieth Bell numbers:");
    for (int i = 1LL; i < 16LL; i++) {
        puts(str_concat(str_concat(str_concat("", _padStart(str_int(i), 2LL, " ")), ": "), str_bigint(bt[(int)(i)][(int)(0LL)])));
    }
    puts(str_concat("50: ", str_bigint(bt[(int)(50LL)][(int)(0LL)])));
    puts("");
    puts("The first ten rows of Bell's triangle:");
    for (int i = 1LL; i < 11LL; i++) {
        printf("%d\n", bt[(int)(i)]);
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
