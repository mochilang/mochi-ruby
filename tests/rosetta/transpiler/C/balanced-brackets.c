// Generated by Mochi 0.10.32 on 2025-07-28 10:35 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int seed = 1LL;

int prng(int max);
const char* gen(int n);
int testBalanced(const char* s);
int user_main();
int main(void);

int prng(int max) {
    seed = ((seed * 1103515245LL) + 12345LL) % 2147483648LL;
    return seed % max;
}

const char* gen(int n) {
    const char* *arr = NULL;
    size_t arr_len = 0;
    int i = 0LL;
    while (i < n) {
        arr = list_append_str(arr, &arr_len, "[");
        arr = list_append_str(arr, &arr_len, "]");
        i = i + 1LL;
    }
    int j = arr_len - 1LL;
    while (j > 0LL) {
        int k = prng(j + 1LL);
        const char* tmp = arr[(int)(j)];
        arr[(int)(j)] = arr[(int)(k)];
        arr[(int)(k)] = tmp;
        j = j - 1LL;
    }
    const char* out = "";
    for (size_t __i = 0; __i < arr_len; __i++) {
        const char* ch = arr[__i];
        out = str_concat(out, ch);
    }
    return out;
}

int testBalanced(const char* s) {
    int open = 0LL;
    int i = 0LL;
    while (i < strlen(s)) {
        const char* c = _substring(s, i, i + 1LL);
        if (strcmp(c, "[") == 0) {
            open = open + 1LL;
        } else {
            if (strcmp(c, "]") == 0) {
                if (open == 0LL) {
                    puts(str_concat(s, ": not ok"));
                    return 0;
                }
                open = open - 1LL;
            } else {
                puts(str_concat(s, ": not ok"));
                return 0;
            }
        }
        i = i + 1LL;
    }
    if (open == 0LL) {
        puts(str_concat(s, ": ok"));
    } else {
        puts(str_concat(s, ": not ok"));
    }
}

int user_main() {
    int i = 0LL;
    while (i < 10LL) {
        testBalanced(gen(i));
        i = i + 1LL;
    }
    testBalanced("()");
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
