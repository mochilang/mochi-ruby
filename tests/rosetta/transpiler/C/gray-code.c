// Generated by Mochi 0.10.32 on 2025-08-01 12:37 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int xor(int a, int b);
int enc(int b);
int dec(int g);
const char* binary(int n);
const char* pad5(const char* s);
int user_main();
int main(void);

int xor(int a, int b) {
    int res = 0LL;
    int bit = 1LL;
    int x = a;
    int y = b;
    while ((x > 0LL) || (y > 0LL)) {
        if ((((x % 2LL) + (y % 2LL)) % 2LL) == 1LL) {
            res = res + bit;
        }
        x = x / 2LL;
        y = y / 2LL;
        bit = bit * 2LL;
    }
    return res;
}

int enc(int b) {
    return xor(b, b / 2LL);
}

int dec(int g) {
    int b = 0LL;
    int x = g;
    while (x > 0LL) {
        b = xor(b, x);
        x = x / 2LL;
    }
    return b;
}

const char* binary(int n) {
    if (n == 0LL) {
        return "0";
    }
    const char* s = "";
    int x = n;
    while (x > 0LL) {
        if ((x % 2LL) == 1LL) {
            s = str_concat("1", s);
        } else {
            s = str_concat("0", s);
        }
        x = x / 2LL;
    }
    return s;
}

const char* pad5(const char* s) {
    const char* p = s;
    while (strlen(p) < 5LL) {
        p = str_concat("0", p);
    }
    return p;
}

int user_main() {
    puts("decimal  binary   gray    decoded");
    int b = 0LL;
    while (b < 32LL) {
        int g = enc(b);
        int d = dec(g);
        puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("  ", pad5(binary(b))), "   "), pad5(binary(g))), "   "), pad5(binary(d))), "  "), str_int(d)));
        b = b + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
