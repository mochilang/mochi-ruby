// Generated by Mochi 0.10.32 on 2025-07-25 17:54 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t newPile_len;
size_t *newPile_lens;
size_t handlePile_len;
size_t *handlePile_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int dim = 16;

int * * newPile(int d) {
    int **b = NULL;
    size_t b_len = 0;
    size_t *b_lens = NULL;
    size_t b_lens_len = 0;
    int y = 0;
    while (y < d) {
        int *row = NULL;
        size_t row_len = 0;
        int x = 0;
        while (x < d) {
            row = list_append_int(row, &row_len, 0);
            x = x + 1;
        }
        b = list_append_intptr(b, &b_len, row);
        b_lens = list_append_szt(b_lens, &b_lens_len, row_len);
        y = y + 1;
    }
    return newPile_lens = b_lens, newPile_len = b_len, b;
}

int * * handlePile(int * * pile, size_t pile_len, size_t* pile_lens, size_t pile_lens_len, int x, int y) {
    if (pile[y][x] >= 4) {
        pile[y][x] = pile[y][x] - 4;
        if (y > 0) {
            pile[y - 1][x] = pile[y - 1][x] + 1;
            if (pile[y - 1][x] >= 4) {
                pile = handlePile(pile, pile_len, pile_lens, pile_len, x, y - 1);
                pile_len = handlePile_len;
                pile_lens = handlePile_lens;
            }
        }
        if (x > 0) {
            pile[y][x - 1] = pile[y][x - 1] + 1;
            if (pile[y][x - 1] >= 4) {
                pile = handlePile(pile, pile_len, pile_lens, pile_len, x - 1, y);
                pile_len = handlePile_len;
                pile_lens = handlePile_lens;
            }
        }
        if (y < (dim - 1)) {
            pile[y + 1][x] = pile[y + 1][x] + 1;
            if (pile[y + 1][x] >= 4) {
                pile = handlePile(pile, pile_len, pile_lens, pile_len, x, y + 1);
                pile_len = handlePile_len;
                pile_lens = handlePile_lens;
            }
        }
        if (x < (dim - 1)) {
            pile[y][x + 1] = pile[y][x + 1] + 1;
            if (pile[y][x + 1] >= 4) {
                pile = handlePile(pile, pile_len, pile_lens, pile_len, x + 1, y);
                pile_len = handlePile_len;
                pile_lens = handlePile_lens;
            }
        }
        pile = handlePile(pile, pile_len, pile_lens, pile_len, x, y);
        pile_len = handlePile_len;
        pile_lens = handlePile_lens;
    }
    return handlePile_lens = pile_lens, handlePile_len = pile_len, pile;
}

int drawPile(int * * pile, size_t pile_len, size_t* pile_lens, size_t pile_lens_len, int d) {
    const char* chars[] = {" ", "░", "▓", "█"};
    size_t chars_len = 4;
    int row = 0;
    while (row < d) {
        const char* line = "";
        int col = 0;
        while (col < d) {
            int v = pile[row][col];
            if (v > 3) {
                v = 3;
            }
            line = str_concat(line, chars[v]);
            col = col + 1;
        }
        puts(line);
        row = row + 1;
    }
}

int user_main() {
    int **pile = newPile(16);
    size_t pile_len = newPile_len;
    size_t *pile_lens = newPile_lens;
    size_t pile_lens_len = newPile_len;
    int hdim = 7;
    pile[hdim][hdim] = 16;
    pile = handlePile(pile, pile_len, pile_lens, pile_len, hdim, hdim);
    pile_len = handlePile_len;
    pile_lens = handlePile_lens;
    drawPile(pile, pile_len, pile_lens, pile_len, 16);
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
