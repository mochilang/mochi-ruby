// Generated by Mochi 0.10.32 on 2025-08-03 22:59 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

typedef struct Bitmap Bitmap;
typedef struct Colour Colour;

struct Bitmap {
    long long width;
    long long height;
    Colour **pixels;
    size_t pixels_len;
    size_t *pixels_lens;
    size_t pixels_lens_len;
};

struct Colour {
    long long R;
    long long G;
    long long B;
};

static Colour* list_append_Colour(Colour *arr, size_t *len, Colour val) {
    arr = realloc(arr, (*len + 1) * sizeof(Colour));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static Colour** list_append_Colourptr(Colour **arr, size_t *len, Colour *val) {
    arr = realloc(arr, (*len + 1) * sizeof(Colour*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Bitmap newBitmap(long long w, long long h, Colour c);
void setPixel(Bitmap b, long long x, long long y, Colour c);
void fillRect(Bitmap b, long long x, long long y, long long w, long long h, Colour c);
const char* pad(long long n, long long width);
const char* writePPMP3(Bitmap b);
void user_main();
int main(void);

Bitmap newBitmap(long long w, long long h, Colour c) {
    Colour **rows = NULL;
    size_t rows_len = 0;
    size_t *rows_lens = NULL;
    size_t rows_lens_len = 0;
    int y = 0LL;
    while (y < h) {
        Colour *row = NULL;
        size_t row_len = 0;
        int x = 0LL;
        while (x < w) {
            row = list_append_Colour(row, &row_len, c);
            x = x + 1LL;
        }
        rows = list_append_Colourptr(rows, &rows_len, row);
        rows_lens = list_append_szt(rows_lens, &rows_lens_len, row_len);
        y = y + 1LL;
    }
    return (Bitmap){.width = w, .height = h, .pixels = rows, .pixels_len = rows_len, .pixels_lens = rows_lens, .pixels_lens_len = rows_len};
}

void setPixel(Bitmap b, long long x, long long y, Colour c) {
    Colour **rows = b.pixels;
    size_t rows_len = b.pixels_len;
    size_t *rows_lens = b.pixels_lens;
    size_t rows_lens_len = b.pixels_len;
    Colour *row = rows[(int)(y)];
    size_t row_len = rows_lens[y];
    row[(int)(x)] = c;
    rows[(int)(y)] = row;
    b.pixels = rows;
}

void fillRect(Bitmap b, long long x, long long y, long long w, long long h, Colour c) {
    long long yy = y;
    while (yy < (y + h)) {
        long long xx = x;
        while (xx < (x + w)) {
            setPixel(b, xx, yy, c);
            xx = xx + 1LL;
        }
        yy = yy + 1LL;
    }
}

const char* pad(long long n, long long width) {
    const char* s = str_int(n);
    while (strlen(s) < width) {
        s = str_concat(" ", s);
    }
    return s;
}

const char* writePPMP3(Bitmap b) {
    int maxv = 0LL;
    int y = 0LL;
    while (y < b.height) {
        int x = 0LL;
        while (x < b.width) {
            Colour p = b.pixels[(int)(y)][(int)(x)];
            if (p.R > maxv) {
                maxv = p.R;
            }
            if (p.G > maxv) {
                maxv = p.G;
            }
            if (p.B > maxv) {
                maxv = p.B;
            }
            x = x + 1LL;
        }
        y = y + 1LL;
    }
    const char* out = str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("P3\n# generated from Bitmap.writeppmp3\n", str_int(b.width)), " "), str_int(b.height)), "\n"), str_int(maxv)), "\n");
    long long numsize = strlen(str_int(maxv));
    y = b.height - 1LL;
    while (y >= 0LL) {
        const char* line = "";
        int x = 0LL;
        while (x < b.width) {
            Colour p = b.pixels[(int)(y)][(int)(x)];
            line = str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(line, "   "), pad(p.R, numsize)), " "), pad(p.G, numsize)), " "), pad(p.B, numsize));
            x = x + 1LL;
        }
        out = str_concat(out, line);
        if (y > 0LL) {
            out = str_concat(out, "\n");
        } else {
            out = str_concat(out, "\n");
        }
        y = y - 1LL;
    }
    return out;
}

void user_main() {
    Colour black = (Colour){.R = 0LL, .G = 0LL, .B = 0LL};
    Colour white = (Colour){.R = 255LL, .G = 255LL, .B = 255LL};
    Bitmap bm = newBitmap(4LL, 4LL, black);
    fillRect(bm, 1LL, 0LL, 1LL, 2LL, white);
    setPixel(bm, 3LL, 3LL, (Colour){.R = 127LL, .G = 0LL, .B = 63LL});
    const char* ppm = writePPMP3(bm);
    puts(ppm);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
