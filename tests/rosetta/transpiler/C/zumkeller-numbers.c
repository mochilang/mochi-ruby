// Generated by Mochi 0.10.32 on 2025-08-01 23:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t getDivisors_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int * getDivisors(int n);
int sum(int * xs, size_t xs_len);
int isPartSum(int * divs, size_t divs_len, int target);
int isZumkeller(int n);
const char* pad(int n, int width);
int user_main();
int main(void);

int * getDivisors(int n) {
    int *divs = NULL;
    size_t divs_len = 0;
    divs = list_append_int(divs, &divs_len, 1LL);
    divs = list_append_int(divs, &divs_len, n);
    int i = 2LL;
    while ((i * i) <= n) {
        if ((n % i) == 0LL) {
            int j = n / i;
            divs = list_append_int(divs, &divs_len, i);
            if (i != j) {
                divs = list_append_int(divs, &divs_len, j);
            }
        }
        i = i + 1LL;
    }
    return getDivisors_len = divs_len, divs;
}

int sum(int * xs, size_t xs_len) {
    int s = 0LL;
    for (size_t __i = 0; __i < xs_len; __i++) {
        int x = xs[__i];
        s = s + x;
    }
    return s;
}

int isPartSum(int * divs, size_t divs_len, int target) {
    int *possible = NULL;
    size_t possible_len = 0;
    int i = 0LL;
    while (i <= target) {
        possible = list_append_int(possible, &possible_len, 0LL);
        i = i + 1LL;
    }
    possible[(int)(0LL)] = 1LL;
    for (size_t __i = 0; __i < divs_len; __i++) {
        int v = divs[__i];
        int s = target;
        while (s >= v) {
            if (possible[(int)(s - v)]) {
                possible[(int)(s)] = 1LL;
            }
            s = s - 1LL;
        }
    }
    return possible[(int)(target)];
}

int isZumkeller(int n) {
    int *divs = getDivisors(n);
    size_t divs_len = getDivisors_len;
    int s = sum(divs, divs_len);
    if ((s % 2LL) == 1LL) {
        return 0LL;
    }
    if ((n % 2LL) == 1LL) {
        int abundance = s - (2LL * n);
        return (abundance > 0LL) && ((abundance % 2LL) == 0LL);
    }
    return isPartSum(divs, divs_len, s / 2LL);
}

const char* pad(int n, int width) {
    const char* s = str_int(n);
    while (strlen(s) < width) {
        s = str_concat(" ", s);
    }
    return s;
}

int user_main() {
    puts("The first 220 Zumkeller numbers are:");
    int count = 0LL;
    const char* line = "";
    int i = 2LL;
    while (count < 220LL) {
        if (isZumkeller(i)) {
            line = str_concat(str_concat(line, pad(i, 3LL)), " ");
            count = count + 1LL;
            if ((count % 20LL) == 0LL) {
                puts(_substring(line, 0LL, strlen(line) - 1LL));
                line = "";
            }
        }
        i = i + 1LL;
    }
    puts("\nThe first 40 odd Zumkeller numbers are:");
    count = 0LL;
    line = "";
    i = 3LL;
    while (count < 40LL) {
        if (isZumkeller(i)) {
            line = str_concat(str_concat(line, pad(i, 5LL)), " ");
            count = count + 1LL;
            if ((count % 10LL) == 0LL) {
                puts(_substring(line, 0LL, strlen(line) - 1LL));
                line = "";
            }
        }
        i = i + 2LL;
    }
    puts("\nThe first 40 odd Zumkeller numbers which don't end in 5 are:");
    count = 0LL;
    line = "";
    i = 3LL;
    while (count < 40LL) {
        if (((i % 10LL) != 5LL) && isZumkeller(i)) {
            line = str_concat(str_concat(line, pad(i, 7LL)), " ");
            count = count + 1LL;
            if ((count % 8LL) == 0LL) {
                puts(_substring(line, 0LL, strlen(line) - 1LL));
                line = "";
            }
        }
        i = i + 2LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
