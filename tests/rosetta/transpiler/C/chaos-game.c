// Generated by Mochi 0.10.32 on 2025-08-04 15:00 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t randInt_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char*** list_append_c_charptrptr(const char* **arr, size_t *len, const char* *val) {
    arr = realloc(arr, (*len + 1) * sizeof(const char**));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long width = 60LL;
int height = 0;
long long iterations = 5000LL;
const char* **grid = NULL;
size_t grid_len = 0;
size_t *grid_lens = NULL;
size_t grid_lens_len = 0;
int y = 0LL;
int seed = 1LL;
long long **vertices = NULL;
size_t vertices_len = 0;
size_t *vertices_lens = NULL;
size_t vertices_lens_len = 0;
int px = 0;
int py = 0;
int i = 0LL;

long long * randInt(long long s, long long n);
int main(void);

long long * randInt(long long s, long long n) {
    int next = ((s * 1664525LL) + 1013904223LL) % 2147483647LL;
    {
        static long long __tmp0_arr[2];
        __tmp0_arr[0] = next;
        __tmp0_arr[1] = next % n;
        randInt_len = 2;
        return __tmp0_arr;
    }
}

int main(void) {
    {
        long long __start = _now();
        height = (double)(width) * 0.86602540378;
        while (y < height) {
            const char* *line = NULL;
            size_t line_len = 0;
            int x = 0LL;
            while (x < width) {
                line = list_append_str(line, &line_len, " ");
                x = x + 1LL;
            }
            grid = list_append_c_charptrptr(grid, &grid_len, line);
            grid_lens = list_append_szt(grid_lens, &grid_lens_len, line_len);
            y = y + 1LL;
        }
        vertices = (int*[]){ (long long[]){ 0LL, height - 1LL }, (int[]){ width - 1LL, height - 1LL }, (int[]){ width / 2LL, 0LL } };
        vertices_len = 3;
        vertices_lens = (size_t[]){2, 2, 2};
        vertices_lens_len = 3;
        px = width / 2LL;
        py = height / 2LL;
        while (i < iterations) {
            long long *r = randInt(seed, 3LL);
            size_t r_len = randInt_len;
            seed = r[(int)(0LL)];
            int idx = (int)(r[(int)(1LL)]);
            long long *v = vertices[(int)(idx)];
            size_t v_len = vertices_lens[idx];
            px = (px + v[(int)(0LL)]) / 2LL;
            py = (py + v[(int)(1LL)]) / 2LL;
            if ((((px >= 0LL) && (px < width)) && (py >= 0LL)) && (py < height)) {
                grid[(int)(py)][(int)(px)] = "*";
            }
            i = i + 1LL;
        }
        y = 0LL;
        while (y < height) {
            const char* line = "";
            int x = 0LL;
            while (x < width) {
                line = str_concat(line, grid[(int)(y)][(int)(x)]);
                x = x + 1LL;
            }
            puts(line);
            y = y + 1LL;
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
