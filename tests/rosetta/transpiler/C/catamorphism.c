// Generated by Mochi 0.10.32 on 2025-08-04 11:06 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

long long n_init[5] = {1LL, 2LL, 3LL, 4LL, 5LL};
long long *n = n_init;
size_t n_len = 5;

long long add(long long a, long long b);
long long sub(long long a, long long b);
long long mul(long long a, long long b);
long long fold(long long(*f)(long long, long long), long long * xs, size_t xs_len);
long long __anon0(long long a, long long b);
long long __anon1(long long a, long long b);
long long __anon2(long long a, long long b);
int main(void);

long long add(long long a, long long b) {
    return a + b;
}

long long sub(long long a, long long b) {
    return a - b;
}

long long mul(long long a, long long b) {
    return a * b;
}

long long fold(long long(*f)(long long, long long), long long * xs, size_t xs_len) {
    long long r = xs[(int)(0LL)];
    int i = 1LL;
    while (i < xs_len) {
        r = f(r, xs[(int)(i)]);
        i = i + 1LL;
    }
    return r;
}

long long __anon0(long long a, long long b) {
    return add(a, b);
}

long long __anon1(long long a, long long b) {
    return sub(a, b);
}

long long __anon2(long long a, long long b) {
    return mul(a, b);
}

int main(void) {
    {
        long long __start = _now();
        printf("%d\n", fold(__anon0, n, n_len));
        printf("%d\n", fold(__anon1, n, n_len));
        printf("%d\n", fold(__anon2, n, n_len));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
