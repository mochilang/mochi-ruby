// Generated by Mochi 0.10.32 on 2025-08-04 15:00 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

long long b = 0;
long long r = 0;
const char* s = "aπ";

long long ord(const char* ch);
const char* chr(long long n);
int main(void);

long long ord(const char* ch) {
    if (strcmp(ch, "a") == 0) {
        return 97LL;
    }
    if (strcmp(ch, "π") == 0) {
        return 960LL;
    }
    if (strcmp(ch, "A") == 0) {
        return 65LL;
    }
    return 0LL;
}

const char* chr(long long n) {
    if (n == 97LL) {
        return "a";
    }
    if (n == 960LL) {
        return "π";
    }
    if (n == 65LL) {
        return "A";
    }
    return "?";
}

int main(void) {
    {
        long long __start = _now();
        b = ord("a");
        r = ord("π");
        puts(str_concat(str_concat(str_concat(str_concat(str_int(b), " "), str_int(r)), " "), s));
        puts(str_concat(str_concat(str_concat(str_concat("string cast to []rune: [", str_int(b)), " "), str_int(r)), "]"));
        puts(str_concat(str_concat(str_concat("    string range loop: ", str_int(b)), " "), str_int(r)));
        puts("         string bytes: 0x61 0xcf 0x80");
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
