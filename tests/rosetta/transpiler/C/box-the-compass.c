// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* compassPoint[32] = {"North", "North by east", "North-northeast", "Northeast by north", "Northeast", "Northeast by east", "East-northeast", "East by north", "East", "East by south", "East-southeast", "Southeast by east", "Southeast", "Southeast by south", "South-southeast", "South by east", "South", "South by west", "South-southwest", "Southwest by south", "Southwest", "Southwest by west", "West-southwest", "West by south", "West", "West by north", "West-northwest", "Northwest by west", "Northwest", "Northwest by north", "North-northwest", "North by west"};
size_t compassPoint_len = 32;
int headings[33] = {0, 16.87, 16.88, 33.75, 50.62, 50.63, 67.5, 84.37, 84.38, 101.25, 118.12, 118.13, 135, 151.87, 151.88, 168.75, 185.62, 185.63, 202.5, 219.37, 219.38, 236.25, 253.12, 253.13, 270, 286.87, 286.88, 303.75, 320.62, 320.63, 337.5, 354.37, 354.38};
size_t headings_len = 33;
int i = 0LL;

const char* padLeft(const char* s, int w);
const char* padRight(const char* s, int w);
int indexOf(const char* s, const char* ch);
const char* format2(double f);
double cpx(double h);
const char* degrees2compasspoint(double h);
int main(void);

const char* padLeft(const char* s, int w) {
    const char* res = "";
    int n = w - strlen(s);
    while (n > 0LL) {
        res = str_concat(res, " ");
        n = n - 1LL;
    }
    return str_concat(res, s);
}

const char* padRight(const char* s, int w) {
    const char* out = s;
    int i = strlen(s);
    while (i < w) {
        out = str_concat(out, " ");
        i = i + 1LL;
    }
    return out;
}

int indexOf(const char* s, const char* ch) {
    int i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* format2(double f) {
    const char* s = str_float(f);
    int idx = indexOf(s, ".");
    if (idx < 0LL) {
        s = str_concat(s, ".00");
    } else {
        int need = idx + 3LL;
        if (strlen(s) > need) {
            s = _substring(s, 0LL, need);
        } else {
            while (strlen(s) < need) {
                s = str_concat(s, "0");
            }
        }
    }
    return s;
}

double cpx(double h) {
    double x = (h / 11.25) + 0.5;
    x = fmod(x, 32LL);
    if (x < 0LL) {
        x = x + 32LL;
    }
    return x;
}

const char* degrees2compasspoint(double h) {
    return compassPoint[(int)(cpx(h))];
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts("Index  Compass point         Degree");
        while (i < 33LL) {
            int h = headings[(int)(i)];
            int idx = (i % 32LL) + 1LL;
            const char* cp = degrees2compasspoint(h);
            puts(str_concat(str_concat(str_concat(str_concat(str_concat(padLeft(str_int(idx), 4LL), "   "), padRight(cp, 19LL)), " "), format2(h)), "°"));
            i = i + 1LL;
        }
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
