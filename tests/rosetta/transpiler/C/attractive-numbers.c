// Generated by Mochi 0.10.32 on 2025-07-27 15:57 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int isPrime(int n);
int countPrimeFactors(int n);
const char* pad4(int n);
int user_main();
int main(void);

int isPrime(int n) {
    if (n < 2) {
        return 0;
    }
    if ((n % 2) == 0) {
        return n == 2;
    }
    if ((n % 3) == 0) {
        return n == 3;
    }
    int d = 5;
    while ((d * d) <= n) {
        if ((n % d) == 0) {
            return 0;
        }
        d = d + 2;
        if ((n % d) == 0) {
            return 0;
        }
        d = d + 4;
    }
    return 1;
}

int countPrimeFactors(int n) {
    if (n == 1) {
        return 0;
    }
    if (isPrime(n)) {
        return 1;
    }
    int count = 0;
    int f = 2;
    while (1) {
        if ((n % f) == 0) {
            count = count + 1;
            n = n / f;
            if (n == 1) {
                return count;
            }
            if (isPrime(n)) {
                f = n;
            }
        } else {
            if (f >= 3) {
                f = f + 2;
            } else {
                f = 3;
            }
        }
    }
    return count;
}

const char* pad4(int n) {
    const char* s = str_int(n);
    while (strlen(s) < 4) {
        s = str_concat(" ", s);
    }
    return s;
}

int user_main() {
    int max = 120;
    puts(str_concat(str_concat("The attractive numbers up to and including ", str_int(max)), " are:"));
    int count = 0;
    const char* line = "";
    int lineCount = 0;
    int i = 1;
    while (i <= max) {
        int c = countPrimeFactors(i);
        if (isPrime(c)) {
            line = str_concat(line, pad4(i));
            count = count + 1;
            lineCount = lineCount + 1;
            if (lineCount == 20) {
                puts(line);
                line = "";
                lineCount = 0;
            }
        }
        i = i + 1;
    }
    if (lineCount > 0) {
        puts(line);
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
