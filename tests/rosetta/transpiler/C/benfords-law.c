// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>

size_t fib1000_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

double user_floorf(double x);
int indexOf(const char* s, const char* ch);
const char* fmtF3(double x);
const char* padFloat3(double x, int width);
double * fib1000();
int leadingDigit(double x);
int show(double * nums, size_t nums_len, const char* title);
int user_main();
int main(void);

double user_floorf(double x) {
    int y = (int)(x);
    return (double)(y);
}

int indexOf(const char* s, const char* ch) {
    int i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* fmtF3(double x) {
    double y = user_floorf((x * 1000) + 0.5) / 1000;
    const char* s = str_float(y);
    int dot = indexOf(s, ".");
    if (dot == (0LL - 1LL)) {
        s = str_concat(s, ".000");
    } else {
        int decs = (strlen(s) - dot) - 1LL;
        if (decs > 3LL) {
            s = _substring(s, 0LL, dot + 4LL);
        } else {
            while (decs < 3LL) {
                s = str_concat(s, "0");
                decs = decs + 1LL;
            }
        }
    }
    return s;
}

const char* padFloat3(double x, int width) {
    const char* s = fmtF3(x);
    while (strlen(s) < width) {
        s = str_concat(" ", s);
    }
    return s;
}

double * fib1000() {
    double a = 0;
    double b = 1;
    double *res = NULL;
    size_t res_len = 0;
    int i = 0LL;
    while (i < 1000LL) {
        res = list_append_double(res, &res_len, b);
        double t = b;
        b = b + a;
        a = t;
        i = i + 1LL;
    }
    return fib1000_len = res_len, res;
}

int leadingDigit(double x) {
    if (x < 0) {
        x = -(x);
    }
    while (x >= 10) {
        x = x / 10;
    }
    while ((x > 0) && (x < 1)) {
        x = x * 10;
    }
    return (int)(x);
}

int show(double * nums, size_t nums_len, const char* title) {
    int counts[9] = {0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL, 0LL};
    size_t counts_len = 9;
    for (size_t __i = 0; __i < nums_len; __i++) {
        double n = nums[__i];
        int d = leadingDigit(n);
        if ((d >= 1LL) && (d <= 9LL)) {
            counts[(int)(d - 1LL)] = counts[(int)(d - 1LL)] + 1LL;
        }
    }
    double preds[9] = {0.301, 0.176, 0.125, 0.097, 0.079, 0.067, 0.058, 0.051, 0.046};
    size_t preds_len = 9;
    size_t total = nums_len;
    puts(title);
    puts("Digit  Observed  Predicted");
    int i = 0LL;
    while (i < 9LL) {
        double obs = (double)(counts[(int)(i)]) / (double)(total);
        const char* line = str_concat(str_concat(str_concat(str_concat(str_concat("  ", str_int(i + 1LL)), "  "), padFloat3(obs, 9LL)), "  "), padFloat3(preds[(int)(i)], 8LL));
        puts(line);
        i = i + 1LL;
    }
}

int user_main() {
    size_t fib1000_len = 0;
double* __tmp1 = fib1000();
    show(__tmp1, fib1000_len, "First 1000 Fibonacci numbers");
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
