// Generated by Mochi 0.10.32 on 2025-08-03 23:52 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t stringSearch_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long indexOfStr(const char* h, const char* n);
long long stringSearchSingle(const char* h, const char* n);
long long * stringSearch(const char* h, const char* n);
const char* display(long long * nums, size_t nums_len);
void user_main();
int main(void);

long long indexOfStr(const char* h, const char* n) {
    long long hlen = strlen(h);
    long long nlen = strlen(n);
    if (nlen == 0LL) {
        return 0LL;
    }
    int i = 0LL;
    while (i <= (hlen - nlen)) {
        if (strcmp(_substring(h, i, i + nlen), n) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

long long stringSearchSingle(const char* h, const char* n) {
    return indexOfStr(h, n);
}

long long * stringSearch(const char* h, const char* n) {
    long long *result = NULL;
    size_t result_len = 0;
    int start = 0LL;
    long long hlen = strlen(h);
    long long nlen = strlen(n);
    while (start < hlen) {
        long long idx = indexOfStr(_substring(h, start, hlen), n);
        if (idx >= 0LL) {
            result = list_append_long_long(result, &result_len, start + idx);
            start = (start + idx) + nlen;
        } else {
            break;
        }
    }
    return stringSearch_len = result_len, result;
}

const char* display(long long * nums, size_t nums_len) {
    const char* s = "[";
    int i = 0LL;
    while (i < nums_len) {
        if (i > 0LL) {
            s = str_concat(s, ", ");
        }
        s = str_concat(s, str_int(nums[(int)(i)]));
        i = i + 1LL;
    }
    s = str_concat(s, "]");
    return s;
}

void user_main() {
    const char* *texts = NULL;
    size_t texts_len = 0;
    texts = list_append_str(texts, &texts_len, "GCTAGCTCTACGAGTCTA");
    texts = list_append_str(texts, &texts_len, "GGCTATAATGCGTA");
    texts = list_append_str(texts, &texts_len, "there would have been a time for such a word");
    texts = list_append_str(texts, &texts_len, "needle need noodle needle");
    texts = list_append_str(texts, &texts_len, "DKnuthusesandprogramsanimaginarycomputertheMIXanditsassociatedmachinecodeandassemblylanguages");
    texts = list_append_str(texts, &texts_len, "Nearby farms grew an acre of alfalfa on the dairy's behalf, with bales of that alfalfa exchanged for milk.");
    const char* *patterns = NULL;
    size_t patterns_len = 0;
    patterns = list_append_str(patterns, &patterns_len, "TCTA");
    patterns = list_append_str(patterns, &patterns_len, "TAATAAA");
    patterns = list_append_str(patterns, &patterns_len, "word");
    patterns = list_append_str(patterns, &patterns_len, "needle");
    patterns = list_append_str(patterns, &patterns_len, "and");
    patterns = list_append_str(patterns, &patterns_len, "alfalfa");
    int i = 0LL;
    while (i < 6LL) {
        puts(str_concat(str_concat(str_concat("text", str_int(i + 1LL)), " = "), texts[(int)(i)]));
        i = i + 1LL;
    }
    puts("");
    int j = 0LL;
    while (j < 6LL) {
        long long *idxs = stringSearch(texts[(int)(j)], patterns[(int)(j)]);
        size_t idxs_len = stringSearch_len;
        puts(str_concat(str_concat(str_concat(str_concat(str_concat("Found \"", patterns[(int)(j)]), "\" in 'text"), str_int(j + 1LL)), "' at indexes "), display(idxs, idxs_len)));
        j = j + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
