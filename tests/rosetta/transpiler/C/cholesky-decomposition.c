// Generated by Mochi 0.10.32 on 2025-08-04 20:02 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t cholesky_len;
size_t *cholesky_lens;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

static double* list_append_double(double *arr, size_t *len, double val) {
    arr = realloc(arr, (*len + 1) * sizeof(double));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static double** list_append_doubleptr(double **arr, size_t *len, double *val) {
    arr = realloc(arr, (*len + 1) * sizeof(double*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static size_t* list_append_szt(size_t *arr, size_t *len, size_t val) {
    arr = realloc(arr, (*len + 1) * sizeof(size_t));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

double sqrtApprox(double x);
double * * cholesky(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len);
void printMat(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len);
void demo(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len);
int main(void);

double sqrtApprox(double x) {
    double guess = x;
    long long i = 0LL;
    while (i < 20LL) {
        guess = (guess + (x / guess)) / 2.0;
        i = i + 1LL;
    }
    return guess;
}

double * * cholesky(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len) {
    size_t n = a_len;
    double **l = NULL;
    size_t l_len = 0;
    size_t *l_lens = NULL;
    size_t l_lens_len = 0;
    long long i = 0LL;
    while (i < n) {
        double *row = NULL;
        size_t row_len = 0;
        long long j = 0LL;
        while (j < n) {
            row = list_append_double(row, &row_len, 0.0);
            j = j + 1LL;
        }
        l = list_append_doubleptr(l, &l_len, row);
        l_lens = list_append_szt(l_lens, &l_lens_len, row_len);
        i = i + 1LL;
    }
    i = 0LL;
    while (i < n) {
        long long j = 0LL;
        while (j <= i) {
            double sum = a[(int)(i)][(int)(j)];
            long long k = 0LL;
            while (k < j) {
                sum = sum - (l[(int)(i)][(int)(k)] * l[(int)(j)][(int)(k)]);
                k = k + 1LL;
            }
            if (i == j) {
                l[(int)(i)][(int)(j)] = sqrtApprox(sum);
            } else {
                l[(int)(i)][(int)(j)] = sum / l[(int)(j)][(int)(j)];
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return cholesky_lens = l_lens, cholesky_len = l_len, l;
}

void printMat(double * * m, size_t m_len, size_t* m_lens, size_t m_lens_len) {
    long long i = 0LL;
    while (i < m_len) {
        const char* line = "";
        long long j = 0LL;
        while (j < m_lens[i]) {
            line = str_concat(line, str_float(m[(int)(i)][(int)(j)]));
            if (j < (m_lens[i] - 1LL)) {
                line = str_concat(line, " ");
            }
            j = j + 1LL;
        }
        puts(line);
        i = i + 1LL;
    }
}

void demo(double * * a, size_t a_len, size_t* a_lens, size_t a_lens_len) {
    puts("A:");
    printMat(a, a_len, a_lens, a_len);
    double **l = cholesky(a, a_len, a_lens, a_len);
    size_t l_len = cholesky_len;
    size_t *l_lens = cholesky_lens;
    size_t l_lens_len = cholesky_len;
    puts("L:");
    printMat(l, l_len, l_lens, l_len);
}

int main(void) {
    {
        long long __start = _now();
        demo((double*[]){ (double[]){ 25.0, 15.0, -(5.0) }, (double[]){ 15.0, 18.0, 0.0 }, (double[]){ -(5.0), 0.0, 11.0 } }, 3, (size_t[]){3, 3, 3}, 3);
        demo((double*[]){ (double[]){ 18.0, 22.0, 54.0, 42.0 }, (double[]){ 22.0, 70.0, 86.0, 62.0 }, (double[]){ 54.0, 86.0, 174.0, 134.0 }, (double[]){ 42.0, 62.0, 134.0, 106.0 } }, 4, (size_t[]){4, 4, 4, 4}, 4);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
