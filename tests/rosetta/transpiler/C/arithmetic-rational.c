// Generated by Mochi 0.10.32 on 2025-07-26 18:17 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int intSqrt(int x);
int sumRecip(int n);
int user_main();
int main(void);

int intSqrt(int x) {
    if (x < 2) {
        return x;
    }
    int left = 1;
    int right = x / 2;
    int ans = 0;
    while (left <= right) {
        int mid = left + ((right - left) / 2);
        int sq = mid * mid;
        if (sq == x) {
            return mid;
        }
        if (sq < x) {
            left = mid + 1;
            ans = mid;
        } else {
            right = mid - 1;
        }
    }
    return ans;
}

int sumRecip(int n) {
    int s = 1;
    int limit = intSqrt(n);
    int f = 2;
    while (f <= limit) {
        if ((n % f) == 0) {
            s = s + (n / f);
            int f2 = n / f;
            if (f2 != f) {
                s = s + f;
            }
        }
        f = f + 1;
    }
    return s;
}

int user_main() {
    int *nums = malloc(9 * sizeof(int));
    nums[0] = 6;
    nums[1] = 28;
    nums[2] = 120;
    nums[3] = 496;
    nums[4] = 672;
    nums[5] = 8128;
    nums[6] = 30240;
    nums[7] = 32760;
    nums[8] = 523776;
    size_t nums_len = 9;
    {
        int n_arr[] = {6, 28, 120, 496, 672, 8128, 30240, 32760, 523776};
        size_t n_len = sizeof(n_arr) / sizeof(n_arr[0]);
        for (size_t __i = 0; __i < n_len; __i++) {
            int n = n_arr[__i];
            int s = sumRecip(n);
            if ((s % n) == 0) {
                int val = s / n;
                const char* perfect = "";
                if (val == 1) {
                    perfect = "perfect!";
                }
                puts(str_concat(str_concat(str_concat(str_concat(str_concat("Sum of recipr. factors of ", str_int(n)), " = "), str_int(val)), " exactly "), perfect));
            }
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
