// Generated by Mochi 0.10.32 on 2025-08-04 16:38 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t bigTrim_len;
size_t bigFromInt_len;
size_t bigMulSmall_len;
size_t ccFactors_len;

size_t _slice_int_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static long long* _slice_int(const long long *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    long long *res = NULL;
    if (n) {
        res = malloc(n * sizeof(long long));
        memcpy(res, arr + start, n * sizeof(long long));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long isPrime(long long n);
long long * bigTrim(long long * a, size_t a_len);
long long * bigFromInt(long long x);
long long * bigMulSmall(long long * a, size_t a_len, long long m);
const char* bigToString(long long * a, size_t a_len);
long long pow2(long long k);
long long * ccFactors(long long n, long long m);
void ccNumbers(long long start, long long end);
int main(void);

long long isPrime(long long n) {
    if (n < 2LL) {
        return 0LL;
    }
    if ((n % 2LL) == 0LL) {
        return n == 2LL;
    }
    if ((n % 3LL) == 0LL) {
        return n == 3LL;
    }
    long long d = 5LL;
    while ((d * d) <= n) {
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 2LL;
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 4LL;
    }
    return 1LL;
}

long long * bigTrim(long long * a, size_t a_len) {
    size_t n = a_len;
    while ((n > 1LL) && (a[(int)(n - 1LL)] == 0LL)) {
        a = _slice_int(a, a_len, 0LL, n - 1LL, &(_slice_int_len));
        a_len = _slice_int_len;
        n = n - 1LL;
    }
    return bigTrim_len = a_len, a;
}

long long * bigFromInt(long long x) {
    if (x == 0LL) {
        {
            static long long __tmp0_arr[1];
            __tmp0_arr[0] = 0LL;
            bigFromInt_len = 1;
            return __tmp0_arr;
        }
    }
    long long *digits = NULL;
    size_t digits_len = 0;
    long long n = x;
    while (n > 0LL) {
        digits = list_append_long_long(digits, &digits_len, n % 10LL);
        n = n / 10LL;
    }
    return bigFromInt_len = digits_len, digits;
}

long long * bigMulSmall(long long * a, size_t a_len, long long m) {
    if (m == 0LL) {
        {
            static long long __tmp1_arr[1];
            __tmp1_arr[0] = 0LL;
            bigMulSmall_len = 1;
            return __tmp1_arr;
        }
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long carry = 0LL;
    long long i = 0LL;
    while (i < a_len) {
        long long prod = (a[(int)(i)] * m) + carry;
        res = list_append_long_long(res, &res_len, prod % 10LL);
        carry = prod / 10LL;
        i = i + 1LL;
    }
    while (carry > 0LL) {
        res = list_append_long_long(res, &res_len, carry % 10LL);
        carry = carry / 10LL;
    }
    {
        long long* __ret2 = bigTrim(res, res_len);
        bigMulSmall_len = bigTrim_len;
        return __ret2;
    }
}

const char* bigToString(long long * a, size_t a_len) {
    const char* s = "";
    long long i = a_len - 1LL;
    while (i >= 0LL) {
        s = str_concat(s, str_int(a[(int)(i)]));
        i = i - 1LL;
    }
    return s;
}

long long pow2(long long k) {
    long long r = 1LL;
    long long i = 0LL;
    while (i < k) {
        r = r * 2LL;
        i = i + 1LL;
    }
    return r;
}

long long * ccFactors(long long n, long long m) {
    long long p = (6LL * m) + 1LL;
    if (!(isPrime(p))) {
        {
            static long long __tmp3_arr[0];
            ccFactors_len = 0;
            return __tmp3_arr;
        }
    }
    long long *prod = bigFromInt(p);
    size_t prod_len = bigFromInt_len;
    p = (12LL * m) + 1LL;
    if (!(isPrime(p))) {
        {
            static long long __tmp4_arr[0];
            ccFactors_len = 0;
            return __tmp4_arr;
        }
    }
    prod = bigMulSmall(prod, prod_len, p);
    prod_len = bigMulSmall_len;
    long long i = 1LL;
    while (i <= (n - 2LL)) {
        p = ((pow2(i) * 9LL) * m) + 1LL;
        if (!(isPrime(p))) {
            {
                static long long __tmp5_arr[0];
                ccFactors_len = 0;
                return __tmp5_arr;
            }
        }
        prod = bigMulSmall(prod, prod_len, p);
        prod_len = bigMulSmall_len;
        i = i + 1LL;
    }
    return ccFactors_len = prod_len, prod;
}

void ccNumbers(long long start, long long end) {
    long long n = start;
    while (n <= end) {
        long long m = 1LL;
        if (n > 4LL) {
            m = pow2(n - 4LL);
        }
        while (1LL) {
            long long *num = ccFactors(n, m);
            size_t num_len = ccFactors_len;
            if (num_len > 0LL) {
                puts(str_concat(str_concat(str_concat("a(", str_int(n)), ") = "), bigToString(num, num_len)));
                break;
            }
            if (n <= 4LL) {
                m = m + 1LL;
            } else {
                m = m + pow2(n - 4LL);
            }
        }
        n = n + 1LL;
    }
}

int main(void) {
    {
        long long __start = _now();
        ccNumbers(3LL, 9LL);
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
