// Generated by Mochi 0.10.32 on 2025-07-25 19:53 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    if(v==0) return strdup("false");
    if(v==1) return strdup("true");
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int THRESHOLD = 140737488355328;

int indexOf(int * xs, size_t xs_len, int value) {
    int i = 0;
    while (i < xs_len) {
        if (xs[i] == value) {
            return i;
        }
        i = i + 1;
    }
    return -1;
}

int contains(int * xs, size_t xs_len, int value) {
    return indexOf(xs, xs_len, value) != (0 - 1);
}

int maxOf(int a, int b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

int intSqrt(int n) {
    if (n == 0) {
        return 0;
    }
    int x = n;
    int y = (x + 1) / 2;
    while (y < x) {
        x = y;
        y = (x + (n / x)) / 2;
    }
    return x;
}

int sumProperDivisors(int n) {
    if (n < 2) {
        return 0;
    }
    int sqrt = intSqrt(n);
    int sum = 1;
    int i = 2;
    while (i <= sqrt) {
        if ((n % i) == 0) {
            sum = (sum + i) + (n / i);
        }
        i = i + 1;
    }
    if ((sqrt * sqrt) == n) {
        sum = sum - sqrt;
    }
    return sum;
}

int classifySequence(int k) {
    int last = k;
    int seq[] = {k};
    size_t seq_len = 1;
    while (1) {
        last = sumProperDivisors(last);
        seq = list_append_int(seq, &seq_len, last);
        int n = seq_len;
        const char* aliquot = "";
        if (last == 0) {
            aliquot = "Terminating";
        } else {
            if ((n == 2) && (last == k)) {
                aliquot = "Perfect";
            } else {
                if ((n == 3) && (last == k)) {
                    aliquot = "Amicable";
                } else {
                    if ((n >= 4) && (last == k)) {
                        aliquot = str_concat(str_concat("Sociable[", str_int(n - 1)), "]");
                    } else {
                        if (last == seq[n - 2]) {
                            aliquot = "Aspiring";
                        } else {
                            if (strstr(seq, last) != NULL) {
                                int idx = indexOf(seq, seq_len, last);
                                aliquot = str_concat(str_concat("Cyclic[", str_int((n - 1) - idx)), "]");
                            } else {
                                if ((n == 16) || (last > THRESHOLD)) {
                                    aliquot = "Non-Terminating";
                                }
                            }
                        }
                    }
                }
            }
        }
        if (strcmp(aliquot, "") != 0) {
            return { {"seq": seq}, {"aliquot": aliquot} };
        }
    }
    return { {"seq": seq}, {"aliquot": ""} };
}

const char* padLeft(int n, int w) {
    const char* s = str_int(n);
    while (strlen(s) < w) {
        s = str_concat(" ", s);
    }
    return s;
}

const char* padRight(const char* s, int w) {
    const char* r = s;
    while (strlen(r) < w) {
        r = str_concat(r, " ");
    }
    return r;
}

const char* joinWithCommas(int * seq, size_t seq_len) {
    const char* s = "[";
    int i = 0;
    while (i < seq_len) {
        s = str_concat(s, str_int(seq[i]));
        if (i < (seq_len - 1)) {
            s = str_concat(s, ", ");
        }
        i = i + 1;
    }
    s = str_concat(s, "]");
    return s;
}

int user_main() {
    puts("Aliquot classifications - periods for Sociable/Cyclic in square brackets:\n");
    int k = 1;
    while (k <= 10) {
        int res = classifySequence(k);
        puts(str_concat(str_concat(str_concat(str_concat(padLeft(k, 2), ": "), padRight(res["aliquot"], 15)), " "), joinWithCommas(res, res_len)));
        k = k + 1;
    }
    puts("");
    int s[] = {11, 12, 28, 496, 220, 1184, 12496, 1264460, 790, 909, 562, 1064, 1488};
    size_t s_len = 13;
    int i = 0;
    while (i < 13) {
        int val = s[i];
        int res = classifySequence(val);
        puts(str_concat(str_concat(str_concat(str_concat(padLeft(val, 7), ": "), padRight(res["aliquot"], 15)), " "), joinWithCommas(res, res_len)));
        i = i + 1;
    }
    puts("");
    int big = 15355717786080;
    int r = classifySequence(big);
    puts(str_concat(str_concat(str_concat(str_concat(str_int(big), ": "), padRight(r["aliquot"], 15)), " "), joinWithCommas(r, r_len)));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
