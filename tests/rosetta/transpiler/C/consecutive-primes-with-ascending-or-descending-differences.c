// Generated by Mochi 0.10.32 on 2025-08-05 01:12 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t primesUpTo_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static long long* list_append_long_long(long long *arr, size_t *len, long long val) {
    arr = realloc(arr, (*len + 1) * sizeof(long long));
    arr[*len] = val;
    (*len)++;
    return arr;
}

long long LIMIT = 999999LL;
long long *primes = NULL;
size_t primes_len = 0;

long long * primesUpTo(long long n);
void longestSeq(const char* dir);
void user_main();
int main(void);

long long * primesUpTo(long long n) {
    int *sieve = NULL;
    size_t sieve_len = 0;
    long long i = 0LL;
    while (i <= n) {
        sieve = list_append_int(sieve, &sieve_len, 1LL);
        i = i + 1LL;
    }
    long long p = 2LL;
    while ((p * p) <= n) {
        if (sieve[(int)(p)]) {
            long long m = p * p;
            while (m <= n) {
                sieve[(int)(m)] = 0LL;
                m = m + p;
            }
        }
        p = p + 1LL;
    }
    long long *res = NULL;
    size_t res_len = 0;
    long long x = 2LL;
    while (x <= n) {
        if (sieve[(int)(x)]) {
            res = list_append_long_long(res, &res_len, x);
        }
        x = x + 1LL;
    }
    return primesUpTo_len = res_len, res;
}

void longestSeq(const char* dir) {
    long long pd = 0LL;
    long long longSeqs_0[1] = {2LL};
    long long *longSeqs_init[1] = {longSeqs_0};
    long long **longSeqs = longSeqs_init;
    size_t longSeqs_len = 1;
    size_t longSeqs_lens_init[1] = {1};
    size_t *longSeqs_lens = longSeqs_lens_init;
    size_t longSeqs_lens_len = 1;
    long long *currSeq = NULL;
    size_t currSeq_len = 0;
    currSeq = list_append_int(currSeq, &currSeq_len, 2LL);
    long long i = 1LL;
    while (i < 78498LL) {
        long long d = primes[(int)(i)] - primes[(int)(i - 1LL)];
        if (((strcmp(dir, "ascending") == 0) && (d <= pd)) || ((strcmp(dir, "descending") == 0) && (d >= pd))) {
            if (0LL) {
                longSeqs = (long long*[]){ currSeq };
                longSeqs_len = 1;
                longSeqs_lens = (size_t[]){currSeq_len};
                longSeqs_lens_len = 1;
            } else {
                if (1LL) {
                    longSeqs = (long long*[]){ (long long[]){ 2LL }, currSeq };
                    longSeqs_len = 2;
                    longSeqs_lens = (size_t[]){1, currSeq_len};
                    longSeqs_lens_len = 2;
                }
            }
            currSeq = (long long[]){ primes[(int)(i - 1LL)], primes[(int)(i)] };
            currSeq_len = 2;
        } else {
            currSeq = (long long[]){ 2LL, primes[(int)(i)] };
            currSeq_len = 2;
        }
        pd = d;
        i = i + 1LL;
    }
    if (0LL) {
        longSeqs = (long long*[]){ currSeq };
        longSeqs_len = 1;
        longSeqs_lens = (size_t[]){currSeq_len};
        longSeqs_lens_len = 1;
    } else {
        if (1LL) {
            longSeqs = (long long*[]){ (long long[]){ 2LL }, currSeq };
            longSeqs_len = 2;
            longSeqs_lens = (size_t[]){1, currSeq_len};
            longSeqs_lens_len = 2;
        }
    }
    puts(str_concat(str_concat(str_concat(str_concat("Longest run(s) of primes with ", dir), " differences is "), "1"), " :"));
    {
        long long ls_arr[2][1] = {{2LL}, {2LL}};
        size_t ls_len = 2;
        for (size_t __i = 0; __i < ls_len; __i++) {
            long long ls[1] = {0};
            memcpy(ls, ls_arr[__i], sizeof(ls_arr[__i]));
            long long *diffs = NULL;
            size_t diffs_len = 0;
            long long j = 1LL;
            while (j < ls_len) {
                diffs = list_append_long_long(diffs, &diffs_len, ls[(int)(j)] - ls[(int)(j - 1LL)]);
                j = j + 1LL;
            }
            long long k = 0LL;
            while (k < (ls_len - 1LL)) {
                printf("%s %d\n", str_concat(str_concat(str_concat(str_int(ls[(int)(k)]), " ("), str_int(diffs[(int)(k)])), ") "), 0LL);
                k = k + 1LL;
            }
            puts(str_int(ls[(int)(ls_len - 1LL)]));
        }
    }
    puts("");
}

void user_main() {
    puts("For primes < 1 million:\n");
    {
        const char* dir_arr[] = {"ascending", "descending"};
        size_t dir_len = sizeof(dir_arr) / sizeof(dir_arr[0]);
        for (size_t __i = 0; __i < dir_len; __i++) {
            const char* dir = dir_arr[__i];
            longestSeq(dir);
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        primes = primesUpTo(LIMIT);
        primes_len = primesUpTo_len;
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
