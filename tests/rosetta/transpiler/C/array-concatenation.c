// Generated by Mochi 0.10.32 on 2025-07-26 18:21 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t concatInts_len;
size_t concatAny_len;

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_list_int(const int *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int *a = malloc(3 * sizeof(int));
a[0] = 1;
a[1] = 2;
a[2] = 3;
size_t a_len = 3;
int *b = malloc(3 * sizeof(int));
b[0] = 7;
b[1] = 12;
b[2] = 60;
size_t b_len = 3;
int *i = malloc(3 * sizeof(int));
i[0] = 1;
i[1] = 2;
i[2] = 3;
size_t i_len = 3;
int *j = malloc(4 * sizeof(int));
j[0] = "Crosby";
j[1] = "Stills";
j[2] = "Nash";
j[3] = "Young";
size_t j_len = 4;
int *l = malloc(3 * sizeof(int));
l[0] = 1;
l[1] = 2;
l[2] = 3;
size_t l_len = 3;
int *m = malloc(3 * sizeof(int));
m[0] = 7;
m[1] = 12;
m[2] = 60;
size_t m_len = 3;

int * concatInts(int * a, size_t a_len, int * b, size_t b_len);
int * concatAny(int * a, size_t a_len, int * b, size_t b_len);
int main(void);

int * concatInts(int * a, size_t a_len, int * b, size_t b_len) {
    int *out = malloc(0 * sizeof(int));
    size_t out_len = 0;
    for (size_t __i = 0; __i < a_len; __i++) {
        int v = a[__i];
        out = list_append_int(out, &out_len, v);
    }
    for (size_t __i = 0; __i < b_len; __i++) {
        int v = b[__i];
        out = list_append_int(out, &out_len, v);
    }
    return concatInts_len = out_len, out;
}

int * concatAny(int * a, size_t a_len, int * b, size_t b_len) {
    int *out = malloc(0 * sizeof(int));
    size_t out_len = 0;
    for (size_t __i = 0; __i < a_len; __i++) {
        int v = a[__i];
        out = list_append_int(out, &out_len, v);
    }
    for (size_t __i = 0; __i < b_len; __i++) {
        int v = b[__i];
        out = list_append_int(out, &out_len, v);
    }
    return concatAny_len = out_len, out;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts(({int *__tmp3 = concatInts(a, a_len, b, b_len); char *__res = str_list_int(__tmp3, concatInts_len); __res;}));
        puts(({int *__tmp4 = concatAny(i, i_len, j, j_len); char *__res = str_list_int(__tmp4, concatAny_len); __res;}));
        puts(({int *__tmp5 = concatInts(l, l_len, m, m_len); char *__res = str_list_int(__tmp5, concatInts_len); __res;}));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
