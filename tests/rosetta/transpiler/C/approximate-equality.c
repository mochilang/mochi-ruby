// Generated by Mochi 0.10.32 on 2025-07-26 23:44 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double user_abs(double x) {
    if (x < 0) {
        return -(x);
    }
    return x;
}

double maxf(double a, double b) {
    if (a > b) {
        return a;
    }
    return b;
}

int isClose(double a, double b) {
    double relTol = 1e-09;
    int t = user_abs(a - b);
    double u = relTol * maxf(user_abs(a), user_abs(b));
    return t <= u;
}

double sqrtApprox(double x) {
    double guess = x;
    int i = 0;
    while (i < 10) {
        guess = (guess + (x / guess)) / 2;
        i = i + 1;
    }
    return guess;
}

int user_main() {
    double root2 = sqrtApprox(2);
    double pairs[9][2] = {{1.0000000000000002e+14, 1.0000000000000002e+14}, {100.01, 100.011}, {1.0000000000000002e+13 / 10000, 1.0000000000000001e+09}, {0.001, 0.0010000001}, {1.01e-22, 0}, {root2 * root2, 2}, {-(root2) * root2, -(2)}, {1e+17, 1e+17}, {3.141592653589793, 3.141592653589793}};
    size_t pairs_len = 9;
    size_t pairs_lens[] = {2, 2, 2, 2, 2, 2, 2, 2, 2};
    size_t pairs_lens_len = 9;
    for (size_t __i = 0; __i < pairs_len; __i++) {
        double pair[pairs_lens[__i]];
        memcpy(pair, pairs[__i], sizeof(pairs[__i]));
        double a = pair[0];
        double b = pair[1];
        const char* s = (isClose(a, b) ? "≈" : "≉");
        puts(str_concat(str_concat(str_concat(str_concat(str_float(a), " "), s), " "), str_float(b)));
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
