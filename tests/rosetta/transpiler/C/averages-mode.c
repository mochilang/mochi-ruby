// Generated by Mochi 0.10.32 on 2025-07-27 10:33 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static int contains_map_int(const int keys[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return 1;
    }
    return 0;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_list_int(const int *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static int map_get_ii(const int keys[], const int vals[], size_t len, int key) {
    for (size_t i = 0; i < len; i++) {
        if (keys[i] == key) return vals[i];
    }
    return 0;
}

static void map_set_ii(int keys[], int vals[], size_t *len, int key, int val) {
    for (size_t i = 0; i < *len; i++) {
        if (keys[i] == key) { vals[i] = val; return; }
    }
    keys[*len] = key; vals[*len] = val; (*len)++;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int arr1[5] = {2, 7, 1, 8, 2};
size_t arr1_len = 5;
int *keys1 = NULL;
size_t keys1_len = 0;
int i = 0;
int max1 = 0;
int *modes1 = NULL;
size_t modes1_len = 0;
int arr2[6] = {2, 7, 1, 8, 2, 8};
size_t arr2_len = 6;
int *keys2 = NULL;
size_t keys2_len = 0;
int max2 = 0;
int *modes2 = NULL;
size_t modes2_len = 0;

int main(void);

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        int counts1_keys[16] = {};
        int counts1_vals[16] = {};
        size_t counts1_len = 0;
        while (i < 5) {
            int v = arr1[i];
            if (contains_map_int(counts1_keys, counts1_len, v)) {
                map_set_ii(counts1_keys, counts1_vals, &counts1_len, v, map_get_ii(counts1_keys, counts1_vals, counts1_len, v) + 1);
            } else {
                map_set_ii(counts1_keys, counts1_vals, &counts1_len, v, 1);
                keys1 = list_append_int(keys1, &keys1_len, v);
            }
            i = i + 1;
        }
        i = 0;
        while (i < keys1_len) {
            int k = keys1[i];
            int c = map_get_ii(counts1_keys, counts1_vals, counts1_len, k);
            if (c > max1) {
                max1 = c;
            }
            i = i + 1;
        }
        i = 0;
        while (i < keys1_len) {
            int k = keys1[i];
            if (map_get_ii(counts1_keys, counts1_vals, counts1_len, k) == max1) {
                modes1 = list_append_int(modes1, &modes1_len, k);
            }
            i = i + 1;
        }
        puts(str_list_int(modes1, modes1_len));
        int counts2_keys[16] = {};
        int counts2_vals[16] = {};
        size_t counts2_len = 0;
        i = 0;
        while (i < 6) {
            int v = arr2[i];
            if (contains_map_int(counts2_keys, counts2_len, v)) {
                map_set_ii(counts2_keys, counts2_vals, &counts2_len, v, map_get_ii(counts2_keys, counts2_vals, counts2_len, v) + 1);
            } else {
                map_set_ii(counts2_keys, counts2_vals, &counts2_len, v, 1);
                keys2 = list_append_int(keys2, &keys2_len, v);
            }
            i = i + 1;
        }
        i = 0;
        while (i < keys2_len) {
            int k = keys2[i];
            int c = map_get_ii(counts2_keys, counts2_vals, counts2_len, k);
            if (c > max2) {
                max2 = c;
            }
            i = i + 1;
        }
        i = 0;
        while (i < keys2_len) {
            int k = keys2[i];
            if (map_get_ii(counts2_keys, counts2_vals, counts2_len, k) == max2) {
                modes2 = list_append_int(modes2, &modes2_len, k);
            }
            i = i + 1;
        }
        puts(str_list_int(modes2, modes2_len));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
