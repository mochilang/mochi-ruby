// Generated by Mochi 0.10.32 on 2025-08-05 02:43 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t fields_len;
size_t makePatterns_len;

size_t _slice_str_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

static char* _input(void) {
    char buf[1024];
    if (!fgets(buf, sizeof(buf), stdin)) return strdup("");
    size_t len = strlen(buf);
    if (len > 0 && buf[len-1] == '\n') buf[len-1] = 0;
    return strdup(buf);
}

static int _atoi(const char *s) {
    return atoi(s);
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

long long indexOf(const char* s, const char* ch);
const char* * fields(const char* s);
const char* * makePatterns();
void user_main();
int main(void);

long long indexOf(const char* s, const char* ch) {
    long long i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* * fields(const char* s) {
    const char* *words = NULL;
    size_t words_len = 0;
    const char* cur = "";
    long long i = 0LL;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1LL);
        if (((strcmp(ch, " ") == 0) || (strcmp(ch, "\t") == 0)) || (strcmp(ch, "\n") == 0)) {
            if (strlen(cur) > 0LL) {
                words = list_append_str(words, &words_len, cur);
                cur = "";
            }
        } else {
            cur = str_concat(cur, ch);
        }
        i = i + 1LL;
    }
    if (strlen(cur) > 0LL) {
        words = list_append_str(words, &words_len, cur);
    }
    return fields_len = words_len, words;
}

const char* * makePatterns() {
    const char* *digits = NULL;
    size_t digits_len = 0;
    digits = list_append_str(digits, &digits_len, "1");
    digits = list_append_str(digits, &digits_len, "2");
    digits = list_append_str(digits, &digits_len, "3");
    digits = list_append_str(digits, &digits_len, "4");
    digits = list_append_str(digits, &digits_len, "5");
    digits = list_append_str(digits, &digits_len, "6");
    digits = list_append_str(digits, &digits_len, "7");
    digits = list_append_str(digits, &digits_len, "8");
    digits = list_append_str(digits, &digits_len, "9");
    const char* *pats = NULL;
    size_t pats_len = 0;
    long long i = 0LL;
    while (i < 9LL) {
        long long j = 0LL;
        while (j < 9LL) {
            if (j != i) {
                long long k = 0LL;
                while (k < 9LL) {
                    if ((k != i) && (k != j)) {
                        long long l = 0LL;
                        while (l < 9LL) {
                            if (((l != i) && (l != j)) && (l != k)) {
                                pats = list_append_str(pats, &pats_len, str_concat(str_concat(str_concat(digits[(int)(i)], digits[(int)(j)]), digits[(int)(k)]), digits[(int)(l)]));
                            }
                            l = l + 1LL;
                        }
                    }
                    k = k + 1LL;
                }
            }
            j = j + 1LL;
        }
        i = i + 1LL;
    }
    return makePatterns_len = pats_len, pats;
}

void user_main() {
    puts("Cows and bulls/player\nYou think of four digit number of unique digits in the range 1 to 9.\nI guess.  You score my guess:\n    A correct digit but not in the correct place is a cow.\n    A correct digit in the correct place is a bull.\nYou give my score as two numbers separated with a space.");
    const char* *patterns = makePatterns();
    size_t patterns_len = makePatterns_len;
    while (1LL) {
        if (patterns_len == 0LL) {
            puts("Oops, check scoring.");
            return 0;
        }
        const char* guess = patterns[(int)(0LL)];
        patterns = _slice_str(patterns, patterns_len, 1LL, patterns_len, &(_slice_str_len));
        patterns_len = _slice_str_len;
        long long cows = 0LL;
        long long bulls = 0LL;
        while (1LL) {
            puts(str_concat(str_concat("My guess: ", guess), ".  Score? (c b) "));
            const char* line = _input();
            const char* *toks = fields(line);
            size_t toks_len = fields_len;
            if (toks_len == 2LL) {
                long long c = _atoi(toks[(int)(0LL)]);
                long long b = _atoi(toks[(int)(1LL)]);
                if (((((c >= 0LL) && (c <= 4LL)) && (b >= 0LL)) && (b <= 4LL)) && ((c + b) <= 4LL)) {
                    cows = c;
                    bulls = b;
                    break;
                }
            }
            puts("Score guess as two numbers: cows bulls");
        }
        if (bulls == 4LL) {
            puts("I did it. :)");
            return 0;
        }
        const char* *next = NULL;
        size_t next_len = 0;
        long long idx = 0LL;
        while (idx < patterns_len) {
            const char* pat = patterns[(int)(idx)];
            long long c = 0LL;
            long long b = 0LL;
            long long i = 0LL;
            while (i < 4LL) {
                const char* cg = _substring(guess, i, i + 1LL);
                const char* cp = _substring(pat, i, i + 1LL);
                if (strcmp(cg, cp) == 0) {
                    b = b + 1LL;
                } else {
                    if (indexOf(pat, cg) >= 0LL) {
                        c = c + 1LL;
                    }
                }
                i = i + 1LL;
            }
            if ((c == cows) && (b == bulls)) {
                next = list_append_str(next, &next_len, pat);
            }
            idx = idx + 1LL;
        }
        patterns = next;
    }
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
