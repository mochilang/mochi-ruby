// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int sameDigits(int n, int b);
int isBrazilian(int n);
int isPrime(int n);
int user_main();
int main(void);

int sameDigits(int n, int b) {
    int f = n % b;
    n = n / b;
    while (n > 0LL) {
        if ((n % b) != f) {
            return 0LL;
        }
        n = n / b;
    }
    return 1LL;
}

int isBrazilian(int n) {
    if (n < 7LL) {
        return 0LL;
    }
    if (((n % 2LL) == 0LL) && (n >= 8LL)) {
        return 1LL;
    }
    int b = 2LL;
    while (b < (n - 1LL)) {
        if (sameDigits(n, b)) {
            return 1LL;
        }
        b = b + 1LL;
    }
    return 0LL;
}

int isPrime(int n) {
    if (n < 2LL) {
        return 0LL;
    }
    if ((n % 2LL) == 0LL) {
        return n == 2LL;
    }
    if ((n % 3LL) == 0LL) {
        return n == 3LL;
    }
    int d = 5LL;
    while ((d * d) <= n) {
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 2LL;
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 4LL;
    }
    return 1LL;
}

int user_main() {
    const char* kinds[3] = {" ", " odd ", " prime "};
    size_t kinds_len = 3;
    {
        const char* kind_arr[] = {" ", " odd ", " prime "};
        size_t kind_len = sizeof(kind_arr) / sizeof(kind_arr[0]);
        for (size_t __i = 0; __i < kind_len; __i++) {
            const char* kind = kind_arr[__i];
            puts(str_concat(str_concat("First 20", kind), "Brazilian numbers:"));
            int c = 0LL;
            int n = 7LL;
            while (1LL) {
                if (isBrazilian(n)) {
                    puts(str_concat(str_int(n), " "));
                    c = c + 1LL;
                    if (c == 20LL) {
                        puts("\n");
                        break;
                    }
                }
                if (strcmp(kind, " ") == 0) {
                    n = n + 1LL;
                } else {
                    if (strcmp(kind, " odd ") == 0) {
                        n = n + 2LL;
                    } else {
                        while (1LL) {
                            n = n + 2LL;
                            if (isPrime(n)) {
                                break;
                            }
                        }
                    }
                }
            }
        }
    }
    int n = 7LL;
    int c = 0LL;
    while (c < 100000LL) {
        if (isBrazilian(n)) {
            c = c + 1LL;
        }
        n = n + 1LL;
    }
    puts(str_concat("The 100,000th Brazilian number: ", str_int(n - 1LL)));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
