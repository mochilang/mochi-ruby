// Generated by Mochi 0.10.32 on 2025-07-26 12:40 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double testCases[8][2] = {{20, 45}, {0 - 45, 45}, {0 - 85, 90}, {0 - 95, 90}, {0 - 45, 125}, {0 - 45, 145}, {29.4803, 0 - 88.6381}, {0 - 78.3251, 0 - 159.036}};
size_t testCases_len = 8;
size_t testCases_lens[] = {2, 2, 2, 2, 2, 2, 2, 2};
size_t testCases_lens_len = 8;

double angleDiff(double b1, double b2) {
    double d = b2 - b1;
    if (d < (0 - 180)) {
        return d + 360;
    }
    if (d > 180) {
        return d - 360;
    }
    return d;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        {
            double tc_arr[8][2] = {{20, 45}, {0 - 45, 45}, {0 - 85, 90}, {0 - 95, 90}, {0 - 45, 125}, {0 - 45, 145}, {29.4803, 0 - 88.6381}, {0 - 78.3251, 0 - 159.036}};
            size_t tc_len = 8;
            for (size_t i = 0; i < tc_len; i++) {
                double tc[2] = {0};
                memcpy(tc, tc_arr[i], sizeof(tc_arr[i]));
                printf("%d\n", angleDiff(tc[0], tc[1]));
            }
        }
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
