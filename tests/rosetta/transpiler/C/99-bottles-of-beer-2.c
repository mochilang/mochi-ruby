// Generated by Mochi 0.10.32 on 2025-07-25 17:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t fields_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

const char* * fields(const char* s) {
    const char* *words = NULL;
    size_t words_len = 0;
    const char* cur = "";
    int i = 0;
    while (i < strlen(s)) {
        const char* ch = _substring(s, i, i + 1);
        if (((strcmp(ch, " ") == 0) || (strcmp(ch, "\n") == 0)) || (strcmp(ch, "\t") == 0)) {
            if (strlen(cur) > 0) {
                words = list_append_str(words, &words_len, cur);
                cur = "";
            }
        } else {
            cur = str_concat(cur, ch);
        }
        i = i + 1;
    }
    if (strlen(cur) > 0) {
        words = list_append_str(words, &words_len, cur);
    }
    return fields_len = words_len, words;
}

const char* join(const char* * xs, size_t xs_len, const char* sep) {
    const char* res = "";
    int i = 0;
    while (i < xs_len) {
        if (i > 0) {
            res = str_concat(res, sep);
        }
        res = str_concat(res, xs[i]);
        i = i + 1;
    }
    return res;
}

const char* numberName(int n) {
    const char* small[] = {"no", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine", "ten", "eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"};
    size_t small_len = 20;
    const char* tens[] = {"ones", "ten", "twenty", "thirty", "forty", "fifty", "sixty", "seventy", "eighty", "ninety"};
    size_t tens_len = 10;
    if (n < 0) {
        return "";
    }
    if (n < 20) {
        return small[n];
    }
    if (n < 100) {
        const char* t = tens[n / 10];
        int s = n % 10;
        if (s > 0) {
            t = str_concat(str_concat(t, " "), small[s]);
        }
        return t;
    }
    return "";
}

const char* pluralizeFirst(const char* s, int n) {
    if (n == 1) {
        return s;
    }
    const char* *w = fields(s);
    size_t w_len = fields_len;
    if (w_len > 0) {
        w[0] = str_concat(w[0], "s");
    }
    return join(w, w_len, " ");
}

int randInt(int seed, int n) {
    int next = ((seed * 1664525) + 1013904223) % 2147483647;
    return next % n;
}

const char* slur(const char* p, int d) {
    if (strlen(p) <= 2) {
        return p;
    }
    const char* *a = NULL;
    size_t a_len = 0;
    int i = 1;
    while (i < (strlen(p) - 1)) {
        a = list_append_str(a, &a_len, _substring(p, i, i + 1));
        i = i + 1;
    }
    int idx = a_len - 1;
    int seed = d;
    while (idx >= 1) {
        seed = ((seed * 1664525) + 1013904223) % 2147483647;
        if ((seed % 100) >= d) {
            int j = seed % (idx + 1);
            const char* tmp = a[idx];
            a[idx] = a[j];
            a[j] = tmp;
        }
        idx = idx - 1;
    }
    const char* s = _substring(p, 0, 1);
    int k = 0;
    while (k < a_len) {
        s = str_concat(s, a[k]);
        k = k + 1;
    }
    s = str_concat(s, _substring(p, strlen(p) - 1, strlen(p)));
    const char* *w = fields(s);
    size_t w_len = fields_len;
    return join(w, w_len, " ");
}

int user_main() {
    int i = 99;
    while (i > 0) {
        puts(str_concat(str_concat(str_concat(str_concat(slur(numberName(i), i), " "), pluralizeFirst(slur("bottle of", i), i)), " "), slur("beer on the wall", i)));
        puts(str_concat(str_concat(str_concat(str_concat(slur(numberName(i), i), " "), pluralizeFirst(slur("bottle of", i), i)), " "), slur("beer", i)));
        puts(str_concat(str_concat(str_concat(str_concat(slur("take one", i), " "), slur("down", i)), " "), slur("pass it around", i)));
        puts(str_concat(str_concat(str_concat(str_concat(slur(numberName(i - 1), i), " "), pluralizeFirst(slur("bottle of", i), i - 1)), " "), slur("beer on the wall", i)));
        i = i - 1;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
