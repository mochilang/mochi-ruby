// Generated by Mochi 0.10.32 on 2025-08-04 20:02 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>

size_t egcd_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

long long n_init[3] = {3LL, 5LL, 7LL};
long long *n = n_init;
size_t n_len = 3;
long long a_init[3] = {2LL, 3LL, 2LL};
long long *a = a_init;
size_t a_len = 3;
long long res = 0;

long long * egcd(long long a, long long b);
long long modInv(long long a, long long m);
long long crt(long long * a, size_t a_len, long long * n, size_t n_len);
int main(void);

long long * egcd(long long a, long long b) {
    if (a == 0LL) {
        {
            static long long __tmp0_arr[3];
            __tmp0_arr[0] = b;
            __tmp0_arr[1] = 0LL;
            __tmp0_arr[2] = 1LL;
            egcd_len = 3;
            return __tmp0_arr;
        }
    }
    long long *res = egcd(b % a, a);
    size_t res_len = egcd_len;
    long long g = res[(int)(0LL)];
    long long x1 = res[(int)(1LL)];
    long long y1 = res[(int)(2LL)];
    {
        static long long __tmp1_arr[3];
        __tmp1_arr[0] = g;
        __tmp1_arr[1] = y1 - ((b / a) * x1);
        __tmp1_arr[2] = x1;
        egcd_len = 3;
        return __tmp1_arr;
    }
}

long long modInv(long long a, long long m) {
    long long *r = egcd(a, m);
    size_t r_len = egcd_len;
    if (r[(int)(0LL)] != 1LL) {
        return 0LL;
    }
    long long x = r[(int)(1LL)];
    if (x < 0LL) {
        return x + m;
    }
    return x;
}

long long crt(long long * a, size_t a_len, long long * n, size_t n_len) {
    long long prod = 1LL;
    long long i = 0LL;
    while (i < n_len) {
        prod = prod * n[(int)(i)];
        i = i + 1LL;
    }
    long long x = 0LL;
    i = 0LL;
    while (i < n_len) {
        long long ni = n[(int)(i)];
        long long ai = a[(int)(i)];
        long long p = prod / ni;
        long long inv = modInv(p % ni, ni);
        x = x + ((ai * inv) * p);
        i = i + 1LL;
    }
    return x % prod;
}

int main(void) {
    {
        long long __start = _now();
        res = crt(a, a_len, n, n_len);
        puts(str_concat(str_int(res), " <nil>"));
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
