// Generated by Mochi 0.10.32 on 2025-07-27 00:32 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double PI = 3.141592653589793;
double turns = 2;
double width = 600;
int a = 1;
double b = 20;
double theta = 0;
int count = 0;

double sinApprox(double x) {
    double term = x;
    double sum = x;
    int n = 1;
    while (n <= 10) {
        double denom = (2 * n) * ((2 * n) + 1);
        term = ((-(term) * x) * x) / denom;
        sum = sum + term;
        n = n + 1;
    }
    return sum;
}

double cosApprox(double x) {
    double term = 1;
    double sum = 1;
    int n = 1;
    while (n <= 10) {
        double denom = ((2 * n) - 1) * (2 * n);
        term = ((-(term) * x) * x) / denom;
        sum = sum + term;
        n = n + 1;
    }
    return sum;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        double degreesIncr = (0.1 * PI) / 180;
        double stop = ((360 * turns) * 10) * degreesIncr;
        double centre = width / 2;
        while (theta < stop) {
            double r = a + (b * theta);
            double x = r * cosApprox(theta);
            double y = r * sinApprox(theta);
            if ((count % 100) == 0) {
                puts(str_concat(str_concat(str_float(centre + x), ","), str_float(centre - y)));
            }
            theta = theta + degreesIncr;
            count = count + 1;
        }
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
