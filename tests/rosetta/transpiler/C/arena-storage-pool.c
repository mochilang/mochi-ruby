// Generated by Mochi 0.10.32 on 2025-07-27 00:54 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t poolPut_len;
size_t clearPool_len;

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int_new(const int *arr, size_t len, int val) {
    int *res = malloc((len + 1) * sizeof(int));
    if (arr && len) memcpy(res, arr, len * sizeof(int));
    res[len] = val;
    return res;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct Anon1 Anon1;

struct Anon1 {
    int *pool;
    size_t pool_len;
    int val;
};

int * poolPut(int * p, size_t p_len, int x) {
    return poolPut_len = p_len + 1, list_append_int_new(p, p_len, x);
}

Anon1 poolGet(int * p, size_t p_len) {
    if (p_len == 0) {
        puts("pool empty");
        return (Anon1){.pool = p, .pool_len = p_len, .val = 0};
    }
    int idx = p_len - 1;
    int v = p[idx];
    p = p;
    return (Anon1){.pool = p, .pool_len = p_len, .val = v};
}

int * clearPool(int * p, size_t p_len) {
    return clearPool_len = 0, (int[]){  };
}

int user_main() {
    int *pool = NULL;
    size_t pool_len = 0;
    int i = 1;
    int j = 2;
    puts(str_int(i + j));
    pool = poolPut(pool, pool_len, i);
    pool_len = poolPut_len;
    pool = poolPut(pool, pool_len, j);
    pool_len = poolPut_len;
    i = 0;
    j = 0;
    Anon1 res1 = poolGet(pool, pool_len);
    pool = res1.pool;
    i = res1.val;
    Anon1 res2 = poolGet(pool, pool_len);
    pool = res2.pool;
    j = res2.val;
    i = 4;
    j = 5;
    puts(str_int(i + j));
    pool = poolPut(pool, pool_len, i);
    pool_len = poolPut_len;
    pool = poolPut(pool, pool_len, j);
    pool_len = poolPut_len;
    i = 0;
    j = 0;
    pool = clearPool(pool, pool_len);
    pool_len = clearPool_len;
    Anon1 res3 = poolGet(pool, pool_len);
    pool = res3.pool;
    i = res3.val;
    Anon1 res4 = poolGet(pool, pool_len);
    pool = res4.pool;
    j = res4.val;
    i = 7;
    j = 8;
    puts(str_int(i + j));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
