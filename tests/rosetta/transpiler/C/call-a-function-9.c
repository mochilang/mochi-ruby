// Generated by Mochi 0.10.32 on 2025-08-02 12:08 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>

size_t f_len;

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int * f();
int g(int a, double b);
void h(const char* s, int * nums, size_t nums_len);
void user_main();
int main(void);

int * f() {
    return f_len = 2, (int[]){ 0LL, 0.0 };
}

int g(int a, double b) {
    return 0LL;
}

void h(const char* s, int * nums, size_t nums_len) {
}

void user_main() {
    int *ab = f();
    size_t ab_len = f_len;
    int a = ab[(int)(0LL)];
    int b = ab[(int)(1LL)];
    int cb = f()[(int)(1LL)];
    int d = g(a, cb);
    int e = g(d, b);
    int i = g(d, 2.0);
    int *list = NULL;
    size_t list_len = 0;
    list = list_append_int(list, &list_len, a);
    list = list_append_int(list, &list_len, d);
    list = list_append_int(list, &list_len, e);
    list = list_append_int(list, &list_len, i);
    i = list_len;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
