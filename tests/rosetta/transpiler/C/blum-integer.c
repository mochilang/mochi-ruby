// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

int isPrime(int n);
int firstPrimeFactor(int n);
int indexOf(const char* s, const char* ch);
const char* padLeft(int n, int width);
const char* formatFloat(double f, int prec);
int user_main();
int main(void);

int isPrime(int n) {
    if (n < 2LL) {
        return 0LL;
    }
    if ((n % 2LL) == 0LL) {
        return n == 2LL;
    }
    if ((n % 3LL) == 0LL) {
        return n == 3LL;
    }
    int d = 5LL;
    while ((d * d) <= n) {
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 2LL;
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 4LL;
    }
    return 1LL;
}

int firstPrimeFactor(int n) {
    if (n == 1LL) {
        return 1LL;
    }
    if ((n % 3LL) == 0LL) {
        return 3LL;
    }
    if ((n % 5LL) == 0LL) {
        return 5LL;
    }
    int inc[8] = {4LL, 2LL, 4LL, 2LL, 4LL, 6LL, 2LL, 6LL};
    size_t inc_len = 8;
    int k = 7LL;
    int i = 0LL;
    while ((k * k) <= n) {
        if ((n % k) == 0LL) {
            return k;
        }
        k = k + inc[(int)(i)];
        i = (i + 1LL) % 8LL;
    }
    return n;
}

int indexOf(const char* s, const char* ch) {
    int i = 0LL;
    while (i < strlen(s)) {
        if (strcmp(_substring(s, i, i + 1LL), ch) == 0) {
            return i;
        }
        i = i + 1LL;
    }
    return -1LL;
}

const char* padLeft(int n, int width) {
    const char* s = str_int(n);
    while (strlen(s) < width) {
        s = str_concat(" ", s);
    }
    return s;
}

const char* formatFloat(double f, int prec) {
    const char* s = str_float(f);
    int idx = indexOf(s, ".");
    if (idx < 0LL) {
        return s;
    }
    int need = (idx + 1LL) + prec;
    if (strlen(s) > need) {
        return _substring(s, 0LL, need);
    }
    return s;
}

int user_main() {
    int *blum = NULL;
    size_t blum_len = 0;
    int counts[4] = {0LL, 0LL, 0LL, 0LL};
    size_t counts_len = 4;
    int digits[4] = {1LL, 3LL, 7LL, 9LL};
    size_t digits_len = 4;
    int i = 1LL;
    int bc = 0LL;
    while (1LL) {
        int p = firstPrimeFactor(i);
        if ((p % 4LL) == 3LL) {
            int q = i / p;
            if (((q != p) && ((q % 4LL) == 3LL)) && isPrime(q)) {
                if (bc < 50LL) {
                    blum = list_append_int(blum, &blum_len, i);
                }
                int d = i % 10LL;
                if (d == 1LL) {
                    counts[(int)(0LL)] = counts[(int)(0LL)] + 1LL;
                } else {
                    if (d == 3LL) {
                        counts[(int)(1LL)] = counts[(int)(1LL)] + 1LL;
                    } else {
                        if (d == 7LL) {
                            counts[(int)(2LL)] = counts[(int)(2LL)] + 1LL;
                        } else {
                            if (d == 9LL) {
                                counts[(int)(3LL)] = counts[(int)(3LL)] + 1LL;
                            }
                        }
                    }
                }
                bc = bc + 1LL;
                if (bc == 50LL) {
                    puts("First 50 Blum integers:");
                    int idx = 0LL;
                    while (idx < 50LL) {
                        const char* line = "";
                        int j = 0LL;
                        while (j < 10LL) {
                            line = str_concat(str_concat(line, padLeft(blum[(int)(idx)], 3LL)), " ");
                            idx = idx + 1LL;
                            j = j + 1LL;
                        }
                        puts(_substring(line, 0LL, strlen(line) - 1LL));
                    }
                    break;
                }
            }
        }
        if ((i % 5LL) == 3LL) {
            i = i + 4LL;
        } else {
            i = i + 2LL;
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
