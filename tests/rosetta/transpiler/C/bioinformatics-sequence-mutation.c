// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t randInt_len;
size_t makeSeq_len;
size_t mutate_len;

size_t _slice_str_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static const char** _slice_str(const char **arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    const char **res = NULL;
    if (n) {
        res = malloc(n * sizeof(const char*));
        memcpy(res, arr + start, n * sizeof(const char*));
    }
    *out_len = n;
    _slice_str_len = n;
    return res;
}

int * randInt(int s, int n);
const char* padLeft(const char* s, int w);
int * makeSeq(int s, int le);
int * mutate(int s, const char* dna, int * w, size_t w_len);
int prettyPrint(const char* dna, int rowLen);
const char* wstring(int * w, size_t w_len);
int user_main();
int main(void);

int * randInt(int s, int n) {
    int next = ((s * 1664525LL) + 1013904223LL) % 2147483647LL;
    return randInt_len = 2, (int[]){ next, next % n };
}

const char* padLeft(const char* s, int w) {
    const char* res = "";
    int n = w - strlen(s);
    while (n > 0LL) {
        res = str_concat(res, " ");
        n = n - 1LL;
    }
    return str_concat(res, s);
}

int * makeSeq(int s, int le) {
    const char* bases = "ACGT";
    const char* out = "";
    int i = 0LL;
    while (i < le) {
        int *r = randInt(s, 4LL);
        size_t r_len = randInt_len;
        s = r[(int)(0LL)];
        int idx = (int)(r[(int)(1LL)]);
        out = str_concat(out, _substring(bases, idx, idx + 1LL));
        i = i + 1LL;
    }
    return makeSeq_len = 2, (int[]){ s, out };
}

int * mutate(int s, const char* dna, int * w, size_t w_len) {
    const char* bases = "ACGT";
    int le = strlen(dna);
    int *r = randInt(s, le);
    size_t r_len = randInt_len;
    s = r[(int)(0LL)];
    int p = (int)(r[(int)(1LL)]);
    r = randInt(s, 300LL);
    r_len = randInt_len;
    s = r[(int)(0LL)];
    int x = (int)(r[(int)(1LL)]);
    const char* *arr = NULL;
    size_t arr_len = 0;
    int i = 0LL;
    while (i < le) {
        arr = list_append_str(arr, &arr_len, _substring(dna, i, i + 1LL));
        i = i + 1LL;
    }
    if (x < w[(int)(0LL)]) {
        r = randInt(s, 4LL);
        r_len = randInt_len;
        s = r[(int)(0LL)];
        int idx = (int)(r[(int)(1LL)]);
        const char* b = _substring(bases, idx, idx + 1LL);
        puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("  Change @", padLeft(str_int(p), 3LL)), " '"), arr[(int)(p)]), "' to '"), b), "'"));
        arr[(int)(p)] = b;
    } else {
        if (x < (w[(int)(0LL)] + w[(int)(1LL)])) {
            puts(str_concat(str_concat(str_concat(str_concat("  Delete @", padLeft(str_int(p), 3LL)), " '"), arr[(int)(p)]), "'"));
            int j = p;
            while (j < (arr_len - 1LL)) {
                arr[(int)(j)] = arr[(int)(j + 1LL)];
                j = j + 1LL;
            }
            arr = _slice_str(arr, arr_len, 0LL, arr_len - 1LL, &(_slice_str_len));
        } else {
            r = randInt(s, 4LL);
            r_len = randInt_len;
            s = r[(int)(0LL)];
            int idx2 = (int)(r[(int)(1LL)]);
            const char* b = _substring(bases, idx2, idx2 + 1LL);
            arr = list_append_str(arr, &arr_len, "");
            int j = arr_len - 1LL;
            while (j > p) {
                arr[(int)(j)] = arr[(int)(j - 1LL)];
                j = j - 1LL;
            }
            puts(str_concat(str_concat(str_concat(str_concat("  Insert @", padLeft(str_int(p), 3LL)), " '"), b), "'"));
            arr[(int)(p)] = b;
        }
    }
    const char* out = "";
    i = 0LL;
    while (i < arr_len) {
        out = str_concat(out, arr[(int)(i)]);
        i = i + 1LL;
    }
    return mutate_len = 2, (int[]){ s, out };
}

int prettyPrint(const char* dna, int rowLen) {
    puts("SEQUENCE:");
    int le = strlen(dna);
    int i = 0LL;
    while (i < le) {
        int k = i + rowLen;
        if (k > le) {
            k = le;
        }
        puts(str_concat(str_concat(padLeft(str_int(i), 5LL), ": "), _substring(dna, i, k)));
        i = i + rowLen;
    }
    int a = 0LL;
    int c = 0LL;
    int g = 0LL;
    int t = 0LL;
    int idx = 0LL;
    while (idx < le) {
        const char* ch = _substring(dna, idx, idx + 1LL);
        if (strcmp(ch, "A") == 0) {
            a = a + 1LL;
        } else {
            if (strcmp(ch, "C") == 0) {
                c = c + 1LL;
            } else {
                if (strcmp(ch, "G") == 0) {
                    g = g + 1LL;
                } else {
                    if (strcmp(ch, "T") == 0) {
                        t = t + 1LL;
                    }
                }
            }
        }
        idx = idx + 1LL;
    }
    puts("");
    puts("BASE COUNT:");
    puts(str_concat("    A: ", padLeft(str_int(a), 3LL)));
    puts(str_concat("    C: ", padLeft(str_int(c), 3LL)));
    puts(str_concat("    G: ", padLeft(str_int(g), 3LL)));
    puts(str_concat("    T: ", padLeft(str_int(t), 3LL)));
    puts("    ------");
    puts(str_concat("    Σ: ", str_int(le)));
    puts("    ======");
}

const char* wstring(int * w, size_t w_len) {
    return str_concat(str_concat(str_concat(str_concat(str_concat(str_concat("  Change: ", str_int(w[(int)(0LL)])), "\n  Delete: "), str_int(w[(int)(1LL)])), "\n  Insert: "), str_int(w[(int)(2LL)])), "\n");
}

int user_main() {
    int seed = 1LL;
    int *res = makeSeq(seed, 250LL);
    size_t res_len = makeSeq_len;
    seed = res[(int)(0LL)];
    const char* dna = str_int(res[(int)(1LL)]);
    prettyPrint(dna, 50LL);
    int muts = 10LL;
    int w[3] = {100LL, 100LL, 100LL};
    size_t w_len = 3;
    puts("\nWEIGHTS (ex 300):");
    puts(wstring(w, w_len));
    puts(str_concat(str_concat("MUTATIONS (", str_int(muts)), "):"));
    int i = 0LL;
    while (i < muts) {
        res = mutate(seed, dna, w, w_len);
        res_len = mutate_len;
        seed = res[(int)(0LL)];
        dna = str_int(res[(int)(1LL)]);
        i = i + 1LL;
    }
    puts("");
    prettyPrint(dna, 50LL);
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
