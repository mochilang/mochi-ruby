// Generated by Mochi 0.10.32 on 2025-07-31 18:33 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int mod(int n, int m);
int isPrime(int n);
const char* pad(int n, int width);
int carmichael(int p1);
int main(void);

int mod(int n, int m) {
    return ((n % m) + m) % m;
}

int isPrime(int n) {
    if (n < 2LL) {
        return 0LL;
    }
    if ((n % 2LL) == 0LL) {
        return n == 2LL;
    }
    if ((n % 3LL) == 0LL) {
        return n == 3LL;
    }
    int d = 5LL;
    while ((d * d) <= n) {
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 2LL;
        if ((n % d) == 0LL) {
            return 0LL;
        }
        d = d + 4LL;
    }
    return 1LL;
}

const char* pad(int n, int width) {
    const char* s = str_int(n);
    while (strlen(s) < width) {
        s = str_concat(" ", s);
    }
    return s;
}

int carmichael(int p1) {
    for (int h3 = 2LL; h3 < p1; h3++) {
        for (int d = 1LL; d < h3 + p1; d++) {
            if (((((h3 + p1) * (p1 - 1LL)) % d) == 0LL) && (mod(-(p1) * p1, h3) == (d % h3))) {
                int p2 = 1LL + (((p1 - 1LL) * (h3 + p1)) / d);
                if (!(isPrime(p2))) {
                    continue;
                }
                int p3 = 1LL + ((p1 * p2) / h3);
                if (!(isPrime(p3))) {
                    continue;
                }
                if (((p2 * p3) % (p1 - 1LL)) != 1LL) {
                    continue;
                }
                int c = (p1 * p2) * p3;
                puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(pad(p1, 2LL), "   "), pad(p2, 4LL)), "   "), pad(p3, 5LL)), "     "), str_int(c)));
            }
        }
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts("The following are Carmichael munbers for p1 <= 61:\n");
        puts("p1     p2      p3     product");
        puts("==     ==      ==     =======");
        for (int p1 = 2LL; p1 < 62LL; p1++) {
            if (isPrime(p1)) {
                carmichael(p1);
            }
        }
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
