// Generated by Mochi 0.10.32 on 2025-08-01 19:26 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double sqrtApprox(double x);
double user_abs(double x);
int user_main();
int main(void);

double sqrtApprox(double x) {
    if (x <= 0) {
        return 0;
    }
    double g = x;
    int i = 0LL;
    while (i < 20LL) {
        g = (g + (x / g)) / 2;
        i = i + 1LL;
    }
    return g;
}

double user_abs(double x) {
    if (x < 0) {
        return -(x);
    }
    return x;
}

int user_main() {
    double oldPhi = 1;
    double phi = 0;
    int iters = 0LL;
    double limit = 1e-05;
    while (1LL) {
        phi = 1 + (1 / oldPhi);
        iters = iters + 1LL;
        if (user_abs(phi - oldPhi) <= limit) {
            break;
        }
        oldPhi = phi;
    }
    double actual = (1 + sqrtApprox(5)) / 2;
    puts(str_concat("Final value of phi : ", str_float(phi)));
    puts(str_concat("Number of iterations : ", str_int(iters)));
    puts(str_concat("Error (approx) : ", str_float(phi - actual)));
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
