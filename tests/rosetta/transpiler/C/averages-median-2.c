// Generated by Mochi 0.10.32 on 2025-07-27 17:22 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>


static char* str_float(double v) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%g", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

double sel(double * list, size_t list_len, int k);
double median(double * a, size_t a_len);
int main(void);

double sel(double * list, size_t list_len, int k) {
    int i = 0;
    while (i <= k) {
        int minIndex = i;
        int j = i + 1;
        while (j < list_len) {
            if (list[j] < list[minIndex]) {
                minIndex = j;
            }
            j = j + 1;
        }
        double tmp = list[i];
        list[i] = list[minIndex];
        list[minIndex] = tmp;
        i = i + 1;
    }
    return list[k];
}

double median(double * a, size_t a_len) {
    double *arr = a;
    size_t arr_len = a_len;
    int half = arr_len / 2;
    double med = sel(arr, arr_len, half);
    if ((arr_len % 2) == 0) {
        return (med + arr[half - 1]) / 2;
    }
    return med;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts(str_float(median((double[]){ 3, 1, 4, 1 }, 4)));
        puts(str_float(median((double[]){ 3, 1, 4, 1, 5 }, 5)));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
