// Generated by Mochi 0.10.32 on 2025-07-22 20:29 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

const char* result = "";

int main(void) {
    for (int i = 1; i < 101; i++) {
        int j = 1;
        while ((j * j) < i) {
            j = j + 1;
        }
        if ((j * j) == i) {
            result = str_concat(result, "O");
        } else {
            result = str_concat(result, "-");
        }
    }
    puts(result);
    return 0;
}
