// Generated by Mochi 0.10.32 on 2025-08-01 23:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct Window Window;

struct Window {
    int x;
    int y;
    int w;
    int h;
    int maximized;
    int iconified;
    int visible;
    int shifted;
};

int showState(Window w, const char* label);
Window maximize(Window w);
Window unmaximize(Window w);
Window iconify(Window w);
Window deiconify(Window w);
Window hide(Window w);
Window showWindow(Window w);
Window move(Window w);
int user_main();
int main(void);

int showState(Window w, const char* label) {
    puts(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(str_concat(label, ": pos=("), str_int(w.x)), ","), str_int(w.y)), ") size=("), str_int(w.w)), "x"), str_int(w.h)), ") max="), str_int(w.maximized)), " icon="), str_int(w.iconified)), " visible="), str_int(w.visible)));
}

Window maximize(Window w) {
    w.maximized = 1LL;
    w.w = 800LL;
    w.h = 600LL;
    return w;
}

Window unmaximize(Window w) {
    w.maximized = 0LL;
    w.w = 640LL;
    w.h = 480LL;
    return w;
}

Window iconify(Window w) {
    w.iconified = 1LL;
    w.visible = 0LL;
    return w;
}

Window deiconify(Window w) {
    w.iconified = 0LL;
    w.visible = 1LL;
    return w;
}

Window hide(Window w) {
    w.visible = 0LL;
    return w;
}

Window showWindow(Window w) {
    w.visible = 1LL;
    return w;
}

Window move(Window w) {
    if (w.shifted) {
        w.x = w.x - 10LL;
        w.y = w.y - 10LL;
    } else {
        w.x = w.x + 10LL;
        w.y = w.y + 10LL;
    }
    w.shifted = !(w.shifted);
    return w;
}

int user_main() {
    Window win = (Window){.x = 100LL, .y = 100LL, .w = 640LL, .h = 480LL, .maximized = 0LL, .iconified = 0LL, .visible = 1LL, .shifted = 0LL};
    showState(win, "Start");
    win = maximize(win);
    showState(win, "Maximize");
    win = unmaximize(win);
    showState(win, "Unmaximize");
    win = iconify(win);
    showState(win, "Iconify");
    win = deiconify(win);
    showState(win, "Deiconify");
    win = hide(win);
    showState(win, "Hide");
    win = showWindow(win);
    showState(win, "Show");
    win = move(win);
    showState(win, "Move");
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
