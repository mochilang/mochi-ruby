// Generated by Mochi 0.10.32 on 2025-07-26 18:01 +0000
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <math.h>

size_t primeFactors_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int * primeFactors(int n) {
    int *factors = NULL;
    size_t factors_len = 0;
    int x = n;
    while ((x % 2) == 0) {
        factors = list_append_int(factors, &factors_len, 2);
        x = x / 2;
    }
    int p = 3;
    while ((p * p) <= x) {
        while ((x % p) == 0) {
            factors = list_append_int(factors, &factors_len, p);
            x = x / p;
        }
        p = p + 2;
    }
    if (x > 1) {
        factors = list_append_int(factors, &factors_len, x);
    }
    return primeFactors_len = factors_len, factors;
}

const char* repeat(const char* ch, int n) {
    const char* s = "";
    int i = 0;
    while (i < n) {
        s = str_concat(s, ch);
        i = i + 1;
    }
    return s;
}

double D(double n) {
    if (n < 0) {
        return -(D(-(n)));
    }
    if (n < 2) {
        return 0;
    }
    int *factors = NULL;
    size_t factors_len = 0;
    if (n < 1e+19) {
        factors = primeFactors(n);
        factors_len = primeFactors_len;
    } else {
        int g = n / 100;
        factors = primeFactors(g);
        factors_len = primeFactors_len;
        factors = list_append_int(factors, &factors_len, 2);
        factors = list_append_int(factors, &factors_len, 2);
        factors = list_append_int(factors, &factors_len, 5);
        factors = list_append_int(factors, &factors_len, 5);
    }
    int c = factors_len;
    if (c == 1) {
        return 1;
    }
    if (c == 2) {
        return factors[0] + factors[1];
    }
    double d = n / factors[0];
    return (D(d) * factors[0]) + d;
}

const char* pad(int n) {
    const char* s = str_int(n);
    while (strlen(s) < 4) {
        s = str_concat(" ", s);
    }
    return s;
}

int user_main() {
    int *vals = NULL;
    size_t vals_len = 0;
    int n = -99;
    while (n < 101) {
        vals = list_append_int(vals, &vals_len, D((double)(n)));
        n = n + 1;
    }
    int i = 0;
    while (i < vals_len) {
        const char* line = "";
        int j = 0;
        while (j < 10) {
            line = str_concat(line, pad(vals[i + j]));
            if (j < 9) {
                line = str_concat(line, " ");
            }
            j = j + 1;
        }
        puts(line);
        i = i + 10;
    }
    double pow = 1;
    int m = 1;
    while (m < 21) {
        pow = pow * 10;
        const char* exp = str_int(m);
        if (strlen(exp) < 2) {
            exp = str_concat(exp, " ");
        }
        const char* res = str_concat(str_int(m), repeat("0", m - 1));
        puts(str_concat(str_concat(str_concat("D(10^", exp), ") / 7 = "), res));
        m = m + 1;
    }
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
