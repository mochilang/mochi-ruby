// Generated by Mochi 0.10.32 on 2025-07-31 12:13 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


size_t _slice_int_len;
static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_bool(int v) {
    return strdup(v ? "true" : "false");
}

static char* str_list_int(const int *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

static const char* _substring(const char *s, int start, int end) {
    int len = (int)strlen(s);
    if (start < 0) start = 0;
    if (end > len) end = len;
    if (start > end) start = end;
    char *res = malloc(end - start + 1);
    memcpy(res, s + start, end - start);
    res[end - start] = 0;
    return res;
}

static int* _slice_int(const int *arr, size_t len, int start, int end, size_t *out_len) {
    if (start < 0) start = 0;
    if ((size_t)end > len) end = len;
    if (start > end) start = end;
    size_t n = end - start;
    int *res = NULL;
    if (n) {
        res = malloc(n * sizeof(int));
        memcpy(res, arr + start, n * sizeof(int));
    }
    *out_len = n;
    _slice_int_len = n;
    return res;
}

int b[6] = {98LL, 105LL, 110LL, 97LL, 114LL, 121LL};
size_t b_len = 6;
int *d = NULL;
size_t d_len = 0;
int i = 0LL;
int z[7] = {98LL, 105LL, 110LL, 97LL, 114LL, 121LL, 122LL};
size_t z_len = 7;
int *f = NULL;
size_t f_len = 0;
int *rem = NULL;
size_t rem_len = 0;

const char* user_char(int n);
const char* fromBytes(int * bs, size_t bs_len);
int main(void);

const char* user_char(int n) {
    const char* letters = "abcdefghijklmnopqrstuvwxyz";
    int idx = n - 97LL;
    if ((idx < 0LL) || (idx >= strlen(letters))) {
        return "?";
    }
    return _substring(letters, idx, idx + 1LL);
}

const char* fromBytes(int * bs, size_t bs_len) {
    const char* s = "";
    int i = 0LL;
    while (i < bs_len) {
        s = str_concat(s, user_char(bs[(int)(i)]));
        i = i + 1LL;
    }
    return s;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts(str_list_int(b, b_len));
        int *c = b;
        size_t c_len = b_len;
        puts(str_list_int(c, c_len));
        printf("%d\n", str_bool(b == c));
        while (i < 6LL) {
            d = list_append_int(d, &d_len, b[(int)(i)]);
            i = i + 1LL;
        }
        d[(int)(1LL)] = 97LL;
        d[(int)(4LL)] = 110LL;
        puts(fromBytes(b, b_len));
        puts(fromBytes(d, d_len));
        puts("0");
        puts(fromBytes(z, z_len));
                size_t sub_len = 0;
int *sub = _slice_int(b, b_len, 1LL, 3LL, &(_slice_int_len));
        puts(fromBytes(sub, sub_len));
        i = 0LL;
        while (i < d_len) {
            int val = d[(int)(i)];
            if (val == 110LL) {
                f = list_append_int(f, &f_len, 109LL);
            } else {
                f = list_append_int(f, &f_len, val);
            }
            i = i + 1LL;
        }
        puts(str_concat(str_concat(fromBytes(d, d_len), " -> "), fromBytes(f, f_len)));
        rem = list_append_int(rem, &rem_len, b[(int)(0LL)]);
        i = 3LL;
        while (i < 6LL) {
            rem = list_append_int(rem, &rem_len, b[(int)(i)]);
            i = i + 1LL;
        }
        puts(fromBytes(rem, rem_len));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
