// Generated by Mochi 0.10.32 on 2025-08-04 08:14 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>
#include <math.h>

size_t f_len;

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

int * f();
long long g(long long a, double b);
void h(const char* s, long long * nums, size_t nums_len);
void user_main();
int main(void);

int * f() {
    {
        static int __tmp0_arr[] = {0LL, 0.0};
        f_len = 2;
        return __tmp0_arr;
    }
}

long long g(long long a, double b) {
    return 0LL;
}

void h(const char* s, long long * nums, size_t nums_len) {
}

void user_main() {
    f();
    g(1LL, 2.0);
    int *res = f();
    size_t res_len = f_len;
    g(res[(int)(0LL)], res[(int)(1LL)]);
    g(g(1LL, 2.0), 3.0);
}

int main(void) {
    {
        long long __start = _now();
        user_main();
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
