// Generated by Mochi 0.10.32 on 2025-08-04 17:03 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(long long v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%lld", v);
    return strdup(buf);
}

static const char** list_append_str(const char **arr, size_t *len, const char *val) {
    arr = realloc((void*)arr, (*len + 1) * sizeof(char*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    long long size = 0, rss = 0;
    FILE *f = fopen("/proc/self/statm", "r");
    if (f) {
        if (fscanf(f, "%lld %lld", &size, &rss) != 2) rss = 0;
        fclose(f);
    }
    return rss * (long long)sysconf(_SC_PAGESIZE);
}

typedef struct Birthday Birthday;

struct Birthday {
    long long month;
    long long day;
};

static Birthday* list_append_Birthday(Birthday *arr, size_t *len, Birthday val) {
    arr = realloc(arr, (*len + 1) * sizeof(Birthday));
    arr[*len] = val;
    (*len)++;
    return arr;
}

Birthday choices_init[10] = {(Birthday){.month = 5LL, .day = 15LL}, (Birthday){.month = 5LL, .day = 16LL}, (Birthday){.month = 5LL, .day = 19LL}, (Birthday){.month = 6LL, .day = 17LL}, (Birthday){.month = 6LL, .day = 18LL}, (Birthday){.month = 7LL, .day = 14LL}, (Birthday){.month = 7LL, .day = 16LL}, (Birthday){.month = 8LL, .day = 14LL}, (Birthday){.month = 8LL, .day = 15LL}, (Birthday){.month = 8LL, .day = 17LL}};
Birthday *choices = choices_init;
size_t choices_len = 10;
Birthday *filtered = NULL;
size_t filtered_len = 0;
Birthday *filtered2 = NULL;
size_t filtered2_len = 0;
Birthday *filtered3 = NULL;
size_t filtered3_len = 0;
Birthday *filtered4 = NULL;
size_t filtered4_len = 0;

long long monthUnique(Birthday b, Birthday * list, size_t list_len);
long long dayUnique(Birthday b, Birthday * list, size_t list_len);
long long monthWithUniqueDay(Birthday b, Birthday * list, size_t list_len);
const char* bstr(Birthday b);
int main(void);

long long monthUnique(Birthday b, Birthday * list, size_t list_len) {
    long long c = 0LL;
    for (size_t __i = 0; __i < list_len; __i++) {
        Birthday x = list[__i];
        if (x.month == b.month) {
            c = c + 1LL;
        }
    }
    return c == 1LL;
}

long long dayUnique(Birthday b, Birthday * list, size_t list_len) {
    long long c = 0LL;
    for (size_t __i = 0; __i < list_len; __i++) {
        Birthday x = list[__i];
        if (x.day == b.day) {
            c = c + 1LL;
        }
    }
    return c == 1LL;
}

long long monthWithUniqueDay(Birthday b, Birthday * list, size_t list_len) {
    for (size_t __i = 0; __i < list_len; __i++) {
        Birthday x = list[__i];
        if ((x.month == b.month) && dayUnique(x, list, list_len)) {
            return 1LL;
        }
    }
    return 0LL;
}

const char* bstr(Birthday b) {
    const char* *months = NULL;
    size_t months_len = 0;
    months = list_append_str(months, &months_len, "");
    months = list_append_str(months, &months_len, "January");
    months = list_append_str(months, &months_len, "February");
    months = list_append_str(months, &months_len, "March");
    months = list_append_str(months, &months_len, "April");
    months = list_append_str(months, &months_len, "May");
    months = list_append_str(months, &months_len, "June");
    months = list_append_str(months, &months_len, "July");
    months = list_append_str(months, &months_len, "August");
    months = list_append_str(months, &months_len, "September");
    months = list_append_str(months, &months_len, "October");
    months = list_append_str(months, &months_len, "November");
    months = list_append_str(months, &months_len, "December");
    return str_concat(str_concat(months[(int)(b.month)], " "), str_int(b.day));
}

int main(void) {
    {
        long long __start = _now();
        {
            Birthday bd_arr[] = {(Birthday){.month = 5LL, .day = 15LL}, (Birthday){.month = 5LL, .day = 16LL}, (Birthday){.month = 5LL, .day = 19LL}, (Birthday){.month = 6LL, .day = 17LL}, (Birthday){.month = 6LL, .day = 18LL}, (Birthday){.month = 7LL, .day = 14LL}, (Birthday){.month = 7LL, .day = 16LL}, (Birthday){.month = 8LL, .day = 14LL}, (Birthday){.month = 8LL, .day = 15LL}, (Birthday){.month = 8LL, .day = 17LL}};
            size_t bd_len = sizeof(bd_arr) / sizeof(bd_arr[0]);
            for (size_t __i = 0; __i < bd_len; __i++) {
                Birthday bd = bd_arr[__i];
                if (!(monthUnique(bd, choices, choices_len))) {
                    filtered = list_append_Birthday(filtered, &filtered_len, bd);
                }
            }
        }
        for (size_t __i = 0; __i < filtered_len; __i++) {
            Birthday bd = filtered[__i];
            if (!(monthWithUniqueDay(bd, filtered, filtered_len))) {
                filtered2 = list_append_Birthday(filtered2, &filtered2_len, bd);
            }
        }
        for (size_t __i = 0; __i < filtered2_len; __i++) {
            Birthday bd = filtered2[__i];
            if (dayUnique(bd, filtered2, filtered2_len)) {
                filtered3 = list_append_Birthday(filtered3, &filtered3_len, bd);
            }
        }
        for (size_t __i = 0; __i < filtered3_len; __i++) {
            Birthday bd = filtered3[__i];
            if (monthUnique(bd, filtered3, filtered3_len)) {
                filtered4 = list_append_Birthday(filtered4, &filtered4_len, bd);
            }
        }
        if (filtered4_len == 1LL) {
            puts(str_concat("Cheryl's birthday is ", bstr(filtered4[(int)(0LL)])));
        } else {
            puts("Something went wrong!");
        }
        long long __end = _now();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = _mem();
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
