// Generated by Mochi 0.10.32 on 2025-08-01 23:10 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>

size_t bubbleSort_len;

static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static char* str_list_int(const int *arr, size_t len) {
    size_t cap = len * 32 + 2;
    char *buf = malloc(cap);
    size_t pos = 0;
    buf[pos++] = '[';
    for (size_t i = 0; i < len; i++) {
        char tmp[32];
        snprintf(tmp, sizeof(tmp), "%d", arr[i]);
        size_t n = strlen(tmp);
        if (pos + n + 2 >= cap) { cap = cap * 2 + n + 2; buf = realloc(buf, cap); }
        memcpy(buf + pos, tmp, n);
        pos += n;
        if (i + 1 < len) buf[pos++] = ' ';
    }
    buf[pos++] = ']';
    buf[pos] = 0;
    return buf;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int list_init[10] = {31LL, 41LL, 59LL, 26LL, 53LL, 58LL, 97LL, 93LL, 23LL, 84LL};
int *list = list_init;
size_t list_len = 10;

int * bubbleSort(int * a, size_t a_len);
int main(void);

int * bubbleSort(int * a, size_t a_len) {
    int *arr = a;
    size_t arr_len = a_len;
    int itemCount = arr_len - 1LL;
    while (1LL) {
        int hasChanged = 0LL;
        int index = 0LL;
        while (index < itemCount) {
            if (arr[(int)(index)] > arr[(int)(index + 1LL)]) {
                int tmp = arr[(int)(index)];
                arr[(int)(index)] = arr[(int)(index + 1LL)];
                arr[(int)(index + 1LL)] = tmp;
                hasChanged = 1LL;
            }
            index = index + 1LL;
        }
        if (!(hasChanged)) {
            break;
        }
        itemCount = itemCount - 1LL;
    }
    return bubbleSort_len = arr_len, arr;
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        puts(str_concat("unsorted: ", str_list_int(list, list_len)));
        list = bubbleSort(list, list_len);
        list_len = bubbleSort_len;
        puts(str_concat("sorted!  ", str_list_int(list, list_len)));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
