// Generated by Mochi 0.10.32 on 2025-08-02 17:56 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>
#include <ctype.h>


static char* str_bool(int v) {
    return strdup(v ? "true" : "false");
}

static char* str_lower(const char *s) {
    size_t n = strlen(s);
    char *out = strdup(s);
    for(size_t i=0;i<n;i++) out[i] = tolower((unsigned char)out[i]);
    return out;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

int parseBool(const char* s);
void user_main();
int main(void);

int parseBool(const char* s) {
    const char* l = str_lower(s);
    if (((((strcmp(l, "1") == 0) || (strcmp(l, "t") == 0)) || (strcmp(l, str_bool(1LL)) == 0)) || (strcmp(l, "yes") == 0)) || (strcmp(l, "y") == 0)) {
        return 1LL;
    }
    return 0LL;
}

void user_main() {
    int n = 1LL;
    printf("%d\n", n);
    puts("bool");
    n = !(n);
    printf("%d\n", n);
    int x = 5LL;
    int y = 8LL;
    printf("%s %d\n", "x == y:", x == y);
    printf("%s %d\n", "x < y:", x < y);
    puts("\nConvert String into Boolean Data type\n");
    const char* str1 = "japan";
    printf("%s %s\n", "Before :", "string");
    int bolStr = parseBool(str1);
    printf("%s %s\n", "After :", "bool");
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        user_main();
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
