// Generated by Mochi 0.10.32 on 2025-07-25 17:17 +0700
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <malloc.h>


static char* str_concat(const char *a, const char *b) {
    size_t len1 = strlen(a);
    size_t len2 = strlen(b);
    char *res = malloc(len1 + len2 + 1);
    memcpy(res, a, len1);
    memcpy(res + len1, b, len2);
    res[len1 + len2] = 0;
    return res;
}

static char* str_int(int v) {
    if(v==0) return strdup("false");
    if(v==1) return strdup("true");
    char buf[32];
    snprintf(buf, sizeof(buf), "%d", v);
    return strdup(buf);
}

static int* list_append_int(int *arr, size_t *len, int val) {
    arr = realloc(arr, (*len + 1) * sizeof(int));
    arr[*len] = val;
    (*len)++;
    return arr;
}

static int** list_append_intptr(int **arr, size_t *len, int *val) {
    arr = realloc(arr, (*len + 1) * sizeof(int*));
    arr[*len] = val;
    (*len)++;
    return arr;
}

#include <time.h>
#include <stdlib.h>
static int seeded_now = 0;
static long long now_seed = 0;
static long long _now(void) {
    if (!seeded_now) {
        const char *s = getenv("MOCHI_NOW_SEED");
        if (s && *s) {
            now_seed = atoll(s);
            seeded_now = 1;
        }
    }
    if (seeded_now) {
        now_seed = (now_seed * 1664525 + 1013904223) % 2147483647;
        return now_seed;
    }
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    return (long long)(ts.tv_sec * 1000000000LL + ts.tv_nsec);
}

static long long _mem(void) {
    struct mallinfo mi = mallinfo();
    return (long long)mi.uordblks;
}

typedef struct Anon1 Anon1;

struct Anon1 {
    int count;
    int** list;
    size_t list_len;
};

int validComb(int a, int b, int c, int d, int e, int f, int g) {
    int square1 = a + b;
    int square2 = (b + c) + d;
    int square3 = (d + e) + f;
    int square4 = f + g;
    return ((square1 == square2) && (square2 == square3)) && (square3 == square4);
}

int isUnique(int a, int b, int c, int d, int e, int f, int g) {
    int nums[] = {a, b, c, d, e, f, g};
    size_t nums_len = 7;
    int i = 0;
    while (i < 7) {
        int j = i + 1;
        while (j < 7) {
            if (nums[i] == nums[j]) {
                return 0;
            }
            j = j + 1;
        }
        i = i + 1;
    }
    return 1;
}

Anon1 getCombs(int low, int high, int unique) {
    int **valid = NULL;
    size_t valid_len = 0;
    int count = 0;
    for (int b = low; b < high + 1; b++) {
        for (int c = low; c < high + 1; c++) {
            for (int d = low; d < high + 1; d++) {
                int s = (b + c) + d;
                for (int e = low; e < high + 1; e++) {
                    for (int f = low; f < high + 1; f++) {
                        int a = s - b;
                        int g = s - f;
                        if ((a < low) || (a > high)) {
                            continue;
                        }
                        if ((g < low) || (g > high)) {
                            continue;
                        }
                        if (((d + e) + f) != s) {
                            continue;
                        }
                        if ((f + g) != s) {
                            continue;
                        }
                        if (!(unique) || isUnique(a, b, c, d, e, f, g)) {
                            valid = list_append_intptr(valid, &valid_len, ({int *tmp = malloc(7 * sizeof(int)); tmp[0] = a; tmp[1] = b; tmp[2] = c; tmp[3] = d; tmp[4] = e; tmp[5] = f; tmp[6] = g; tmp;}));
                            count = count + 1;
                        }
                    }
                }
            }
        }
    }
    return (Anon1){.count = count, .list = valid, .list_len = valid_len};
}

int main(void) {
    {
        long long __start = _now();
        long long __mem_start = _mem();
        Anon1 r1 = getCombs(1, 7, 1);
        puts(str_concat(str_int(r1.count), " unique solutions in 1 to 7"));
        printf("%d\n", r1.list);
        Anon1 r2 = getCombs(3, 9, 1);
        puts(str_concat(str_int(r2.count), " unique solutions in 3 to 9"));
        printf("%d\n", r2.list);
        Anon1 r3 = getCombs(0, 9, 0);
        puts(str_concat(str_int(r3.count), " non-unique solutions in 0 to 9"));
        long long __end = _now();
        long long __mem_end = _mem();
        long long __dur_us = (__end - __start) / 1000;
        long long __mem_bytes = __mem_end - __mem_start;
        printf("{\n  \"duration_us\": %-lld,\n  \"memory_bytes\": %-lld,\n  \"name\": \"main\"\n}\n", __dur_us, __mem_bytes);
    }
    return 0;
}
