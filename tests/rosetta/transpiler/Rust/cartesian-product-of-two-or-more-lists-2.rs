// Generated by Mochi transpiler v0.10.57 on 2025-08-04 22:43 +0700
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn listStr(mut xs: Vec<i64>) -> String {
    let mut s: String = String::from("[").clone();
    let mut i: i64 = 0;
    while (i < (xs.len() as i64)) {
        s = format!("{}{}", s, xs[i as usize].to_string());
        if (i < ((xs.len() as i64) - 1)) {
            s = format!("{}{}", s, " ");
        }
        i = (i + 1);
    }
    s = format!("{}{}", s, "]");
    return s.clone()
};
    fn llStr(mut lst: Vec<Vec<i64>>) -> String {
    let mut s: String = String::from("[").clone();
    let mut i: i64 = 0;
    while (i < (lst.len() as i64)) {
        s = format!("{}{}", s, listStr(lst[i as usize].clone()));
        if (i < ((lst.len() as i64) - 1)) {
            s = format!("{}{}", s, " ");
        }
        i = (i + 1);
    }
    s = format!("{}{}", s, "]");
    return s.clone()
};
    fn cartN(mut lists: Option<Vec<Vec<i64>>>) -> Vec<Vec<i64>> {
    if lists.is_none() {
        return vec![]
    }
    let mut a: Vec<Vec<i64>> = lists.clone().unwrap();
    if ((a.len() as i64) == 0) {
        return vec![vec![]]
    }
    let mut c: i64 = 1;
    for xs in a.iter() {
        c = (c * (xs.len() as i64));
    }
    if (c == 0) {
        return vec![]
    }
    let mut res: Vec<Vec<i64>> = vec![];
    let mut idx: Vec<i64> = vec![];
    for _ in a.iter() {
        idx = { let mut _v = idx.clone(); _v.push(0); _v };
    }
    let mut n = (a.len() as i64);
    let mut count: i64 = 0;
    while (count < c) {
        let mut row: Vec<i64> = vec![];
        let mut j: i64 = 0;
        while (j < n) {
            row = { let mut _v = row.clone(); _v.push(a[j as usize].clone()[idx[j as usize] as usize]); _v };
            j = (j + 1);
        }
        res = { let mut _v = res.clone(); _v.push(row.clone()); _v };
        let mut k: i64 = (n - 1);
        while (k >= 0) {
            idx[k as usize] = (idx[k as usize] + 1);
            if (idx[k as usize] < (a[k as usize].clone().len() as i64)) {
                break
            }
            idx[k as usize] = 0;
            k = (k - 1);
        }
        count = (count + 1);
    }
    return res
};
    fn mochi_main() {
    println!("{}", llStr(cartN(Some(vec![vec![1, 2], vec![3, 4]]))));
    println!("{}", llStr(cartN(Some(vec![vec![3, 4], vec![1, 2]]))));
    println!("{}", llStr(cartN(Some(vec![vec![1, 2], vec![]]))));
    println!("{}", llStr(cartN(Some(vec![vec![], vec![1, 2]]))));
    println!("{}", "");
    println!("{}", "[");
    for p in cartN(Some(vec![vec![1776, 1789], vec![7, 12], vec![4, 14, 23], vec![0, 1]])).iter() {
        println!("{}", format!("{}{}", " ", listStr(p.clone())));
    }
    println!("{}", "]");
    println!("{}", llStr(cartN(Some(vec![vec![1, 2, 3], vec![30], vec![500, 100]]))));
    println!("{}", llStr(cartN(Some(vec![vec![1, 2, 3], vec![], vec![500, 100]]))));
    println!("{}", "");
    println!("{}", llStr(cartN(Default::default())));
    println!("{}", llStr(cartN(Some(vec![]))));
};
    mochi_main();
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
