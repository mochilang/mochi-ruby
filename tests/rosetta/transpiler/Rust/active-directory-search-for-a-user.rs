// Generated by Mochi transpiler v0.10.54 on 2025-08-02 14:06 +0700
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(p) = s.split_whitespace().next() {
                if let Ok(v) = p.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
#[derive(Debug, Clone, Default)]
struct Map {
    Base: String,
    Host: String,
    Port: i64,
    GroupFilter: String,
}
impl std::fmt::Display for Map {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "{{")?;
        write!(f, "\"Base\": \"{}\"", self.Base)?;
        write!(f, ", ")?;
        write!(f, "\"Host\": \"{}\"", self.Host)?;
        write!(f, ", ")?;
        write!(f, "\"Port\": {}", self.Port)?;
        write!(f, ", ")?;
        write!(f, "\"GroupFilter\": \"{}\"", self.GroupFilter)?;
        write!(f, "}}")
    }
}

fn main() {
        let _start_mem: i64 = _mem();
    let _start: i64 = _now();
    fn search_user(directory: &HashMap<String, Vec<String>>, username: &str) -> Vec<String> {
    return directory[username].clone()
};
    fn mochi_main() {
    let mut client: Map = Map {Base: String::from("dc=example,dc=com"), Host: String::from("ldap.example.com"), Port: 389, GroupFilter: String::from("(memberUid=%s)")};
    let mut directory = HashMap::from([(String::from("username"), vec![String::from("admins"), String::from("users")]), (String::from("john"), vec![String::from("users")])]);
    let mut groups: Vec<String> = search_user(&directory, &"username");
    if ((groups.len() as i64) > 0) {
        let mut out: String = String::from("Groups: [").clone();
        let mut i: i64 = 0;
        while (i < (groups.len() as i64)) {
            out = format!("{}{}", format!("{}{}", format!("{}{}", out, "\""), groups[i as usize].clone()), "\"");
            if (i < ((groups.len() as i64) - 1)) {
                out = format!("{}{}", out, ", ");
            }
            i = (i + 1);
        }
        out = format!("{}{}", out, "]");
        println!("{}", out);
    } else {
        println!("{}", "User not found");
    }
};
    mochi_main();
    let _end: i64 = _now();
    let _end_mem: i64 = _mem();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = (_end_mem - _start_mem);
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
