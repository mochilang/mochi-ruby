// Generated by Mochi transpiler v0.10.54 on 2025-08-02 14:06 +0700
use std::collections::HashMap;
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(p) = s.split_whitespace().next() {
                if let Ok(v) = p.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start_mem: i64 = _mem();
    let _start: i64 = _now();
    fn fields(mut s: String) -> Vec<String> {
    let mut words: Vec<String> = vec![];
    let mut cur: String = String::from("").clone();
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        let mut ch: String = { let tmp = &s; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.clone();
        if (((ch.as_str() == " ") || (ch.as_str() == "\n")) || (ch.as_str() == "\t")) {
            if ((cur.len() as i64) > 0) {
                words = { let mut _v = words.clone(); _v.push(cur.clone()); _v };
                cur = String::from("");
            }
        } else {
            cur = format!("{}{}", cur, ch);
        }
        i = (i + 1);
    }
    if ((cur.len() as i64) > 0) {
        words = { let mut _v = words.clone(); _v.push(cur.clone()); _v };
    }
    return words
};
    fn padRight(s: &str, mut width: i64) -> String {
    let mut out: String = s.to_string().clone();
    let mut i = (s.len() as i64);
    while (i < width) {
        out = format!("{}{}", out, " ");
        i = (i + 1);
    }
    return out.clone()
};
    fn join(xs: &mut Vec<String>, sep: &str) -> String {
    let mut res: String = String::from("").clone();
    let mut i: i64 = 0;
    while (i < (xs.len() as i64)) {
        if (i > 0) {
            res = format!("{}{}", res, sep);
        }
        res = format!("{}{}", res, xs[i as usize].clone());
        i = (i + 1);
    }
    return res.clone()
};
    fn parseIntStr(mut str: String) -> i64 {
    let mut i: i64 = 0;
    let mut neg: bool = false;
    if (((str.len() as i64) > 0) && (str[0 as usize..1 as usize].to_string().as_str() == "-")) {
        neg = true;
        i = 1;
    }
    let mut n: i64 = 0;
    let mut digits = HashMap::from([(String::from("0"), 0), (String::from("1"), 1), (String::from("2"), 2), (String::from("3"), 3), (String::from("4"), 4), (String::from("5"), 5), (String::from("6"), 6), (String::from("7"), 7), (String::from("8"), 8), (String::from("9"), 9)]);
    while (i < (str.len() as i64)) {
        n = ((n * 10) + digits[str[i as usize..(i + 1) as usize].to_string().as_str()]);
        i = (i + 1);
    }
    if neg {
        n = -n;
    }
    return n
};
    fn isDigits(mut s: String) -> bool {
    if ((s.len() as i64) == 0) {
        return false
    }
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        let mut ch: String = { let tmp = &s; tmp.chars().skip(i as usize).take(((i + 1) - i) as usize).collect::<String>() }.clone();
        if ((ch.as_str() < "0") || (ch.as_str() > "9")) {
            return false
        }
        i = (i + 1);
    }
    return true
};
    fn readTable(table: &str) -> HashMap<&str, Vec<String>> {
    let mut toks: Vec<String> = fields(table.to_string());
    let mut cmds: Vec<String> = vec![];
    let mut mins: Vec<i64> = vec![];
    let mut i: i64 = 0;
    while (i < (toks.len() as i64)) {
        let mut cmd: String = toks[i as usize].clone().clone();
        let mut minlen = (cmd.len() as i64);
        i = (i + 1);
        if ((i < (toks.len() as i64)) && isDigits(toks[i as usize].clone())) {
            let mut num: i64 = parseIntStr(toks[i as usize].clone());
            if ((num >= 1) && (num < (cmd.len() as i64))) {
                minlen = num;
                i = (i + 1);
            }
        }
        cmds = { let mut _v = cmds.clone(); _v.push(cmd.clone()); _v };
        mins = { let mut _v = mins.clone(); _v.push(minlen); _v };
    }
    return HashMap::from([(String::from("commands"), cmds), (String::from("mins"), mins)])
};
    fn validate(mut commands: Vec<String>, mut mins: Vec<i64>, mut words: Vec<String>) -> Vec<String> {
    let mut results: Vec<String> = vec![];
    let mut wi: i64 = 0;
    while (wi < (words.len() as i64)) {
        let mut w: String = words[wi as usize].clone().clone();
        let mut found: bool = false;
        let mut wlen = (w.len() as i64);
        let mut ci: i64 = 0;
        while (ci < (commands.len() as i64)) {
            let mut cmd: String = commands[ci as usize].clone().clone();
            if (((mins[ci as usize] != 0) && (wlen >= mins[ci as usize])) && (wlen <= (cmd.len() as i64))) {
                let mut c: String = (cmd.to_uppercase()).clone();
                let mut ww: String = (w.to_uppercase()).clone();
                if ({ let tmp = &c; tmp.chars().skip(0 as usize).take((wlen - 0) as usize).collect::<String>() }.as_str() == ww.as_str()) {
                    results = { let mut _v = results.clone(); _v.push(c.clone()); _v };
                    found = true;
                    break
                }
            }
            ci = (ci + 1);
        }
        if !found {
            results = { let mut _v = results.clone(); _v.push("*error*".to_string()); _v };
        }
        wi = (wi + 1);
    }
    return results
};
    fn mochi_main() {
    let mut table: String = format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", "", "add 1  alter 3  backup 2  bottom 1  Cappend 2  change 1  Schange  Cinsert 2  Clast 3 "), "compress 4 copy 2 count 3 Coverlay 3 cursor 3  delete 3 Cdelete 2  down 1  duplicate "), "3 xEdit 1 expand 3 extract 3  find 1 Nfind 2 Nfindup 6 NfUP 3 Cfind 2 findUP 3 fUP 2 "), "forward 2  get  help 1 hexType 4  input 1 powerInput 3  join 1 split 2 spltJOIN load "), "locate 1 Clocate 2 lowerCase 3 upperCase 3 Lprefix 2  macro  merge 2 modify 3 move 2 "), "msg  next 1 overlay 1 parse preserve 4 purge 3 put putD query 1 quit  read recover 3 "), "refresh renum 3 repeat 3 replace 1 Creplace 2 reset 3 restore 4 rgtLEFT right 2 left "), "2  save  set  shift 2  si  sort  sos  stack 3 status 4 top  transfer 3  type 1  up 1 ").clone();
    let mut sentence: String = String::from("riG   rePEAT copies  put mo   rest    types   fup.    6\npoweRin").clone();
    let mut tbl: HashMap<&str, Vec<String>> = readTable(&table);
    let mut commands: Vec<String> = tbl["commands"].clone();
    let mut mins: Vec<String> = tbl["mins"].clone();
    let mut words: Vec<String> = fields(sentence.clone());
    let mut results: Vec<String> = validate(commands.clone(), mins.clone(), words.clone());
    let mut out1: String = String::from("user words:").clone();
    let mut k: i64 = 0;
    while (k < (words.len() as i64)) {
        out1 = format!("{}{}", out1, " ");
        if (k < ((words.len() as i64) - 1)) {
            out1 = format!("{}{}", out1, padRight(&words[k as usize].clone(), (results[k as usize].clone().len() as i64)));
        } else {
            out1 = format!("{}{}", out1, words[k as usize].clone());
        }
        k = (k + 1);
    }
    println!("{}", out1);
    println!("{}", format!("{}{}", "full words: ", join(&mut results, &" ")));
};
    mochi_main();
    let _end: i64 = _now();
    let _end_mem: i64 = _mem();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = (_end_mem - _start_mem);
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
