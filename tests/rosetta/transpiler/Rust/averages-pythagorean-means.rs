// Generated by Mochi transpiler v0.10.55 on 2025-08-03 10:01 +0700
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    let _mem_start: i64 = _mem();
    fn powf(mut base: f64, mut exp: i64) -> f64 {
    let mut result: f64 = 1.0;
    let mut i: i64 = 0;
    while (i < exp) {
        result = (result * base);
        i = (i + 1);
    }
    return result
};
    fn nthRoot(mut x: f64, mut n: i64) -> f64 {
    let mut low: f64 = 0.0;
    let mut high: f64 = x;
    let mut i: i64 = 0;
    while (i < 60) {
        let mut mid: f64 = ((low + high) / 2.0);
        if (powf(mid, n) > x) {
            high = mid;
        } else {
            low = mid;
        }
        i = (i + 1);
    }
    return low
};
    fn mochi_main() {
    let mut sum: f64 = 0.0;
    let mut sumRecip: f64 = 0.0;
    let mut prod: f64 = 1.0;
    let mut n: i64 = 1;
    while (n <= 10) {
        let mut f: f64 = (n as f64);
        sum = (sum + f);
        sumRecip = (sumRecip + (1.0 / f));
        prod = (prod * f);
        n = (n + 1);
    }
    let mut count: f64 = 10.0;
    let mut a: f64 = (sum / count);
    let mut g: f64 = nthRoot(prod, 10);
    let mut h: f64 = (count / sumRecip);
    println!("{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", format!("{}{}", "A: ", a.to_string()), " G: "), g.to_string()), " H: "), h.to_string()));
    println!("{}", format!("{}{}", "A >= G >= H: ", ((a >= g) && (g >= h)).to_string()));
};
    mochi_main();
    let _end: i64 = _now();
    let _mem_end: i64 = _mem();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = if ((_mem_end - _mem_start) <= 0) { _mem_end } else { (_mem_end - _mem_start) };
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
