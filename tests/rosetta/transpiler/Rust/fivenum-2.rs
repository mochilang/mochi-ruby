// Generated by Mochi transpiler v0.10.50 on 2025-07-30 21:05 +0700
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(p) = s.split_whitespace().next() {
                if let Ok(v) = p.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
static mut g_x1: Vec<f64> = Vec::new();
static mut g_x2: Vec<f64> = Vec::new();
static mut g_x3: Vec<f64> = Vec::new();
fn main() {
    unsafe {
        g_x1 = vec![36.0, 40.0, 7.0, 39.0, 41.0, 15.0];
        g_x2 = vec![15.0, 6.0, 42.0, 41.0, 7.0, 36.0, 49.0, 40.0, 39.0, 47.0, 43.0];
        g_x3 = vec![0.14082834, 0.0974879, 1.73131507, 0.87636009, -1.95059594, 0.73438555, -0.03035726, 1.4667597, -0.74621349, -0.72588772, 0.6390516, 0.61501527, -0.9898378, -1.00447874, -0.62759469, 0.66206163, 1.04312009, -0.10305385, 0.75775634, 0.32566578];
                let _start_mem: i64 = _mem();
        let _start: i64 = _now();
        unsafe fn qsel(mut a: Vec<f64>, mut k: i64) -> f64 {
    let mut arr: Vec<f64> = a;
    while ((arr.len() as i64) > 1) {
        let mut px = (_now() % (arr.len() as i64));
        let mut pv: f64 = arr[px as usize];
        let mut last = ((arr.len() as i64) - 1);
        let mut tmp: f64 = arr[px as usize];
        arr[px as usize] = arr[last as usize];
        arr[last as usize] = tmp;
        px = 0;
        let mut i: i64 = 0;
        while (i < last) {
            let mut v: f64 = arr[i as usize];
            if (v < pv) {
                let mut t: f64 = arr[px as usize];
                arr[px as usize] = arr[i as usize];
                arr[i as usize] = t;
                px = (px + 1);
            }
            i = (i + 1);
        }
        arr[px as usize] = pv;
        if (px == k) {
            return pv
        }
        if (k < px) {
            arr = arr[0 as usize..px as usize].to_vec();
        } else {
            arr = arr[(px + 1) as usize..].to_vec();
            k = (k - (px + 1));
        }
    }
    return arr[0 as usize]
};
        unsafe fn fivenum(mut a: Vec<f64>) -> Vec<f64> {
    let mut last = ((a.len() as i64) - 1);
    let mut m: i64 = (last / 2);
    let mut n5: Vec<f64> = vec![];
    n5 = { let mut _v = n5.clone(); _v.push(qsel(a[0 as usize..m as usize].to_vec(), 0)); _v };
    n5 = { let mut _v = n5.clone(); _v.push(qsel(a[0 as usize..m as usize].to_vec(), ((a.len() as i64) / 4))); _v };
    n5 = { let mut _v = n5.clone(); _v.push(qsel(a.clone(), m)); _v };
    let mut arr2: Vec<f64> = a[m as usize..].to_vec();
    let mut q3: i64 = ((last - m) - ((a.len() as i64) / 4));
    n5 = { let mut _v = n5.clone(); _v.push(qsel(arr2.clone(), q3)); _v };
    arr2 = arr2[q3 as usize..].to_vec();
    n5 = { let mut _v = n5.clone(); _v.push(qsel(arr2.clone(), ((arr2.len() as i64) - 1))); _v };
    return n5
};
        static mut g_x1: Vec<f64> = Vec::new();;
        static mut g_x2: Vec<f64> = Vec::new();;
        static mut g_x3: Vec<f64> = Vec::new();;
        println!("{}", format!("{:?}", fivenum(g_x1.clone().clone())));
        println!("{}", format!("{:?}", fivenum(g_x2.clone().clone())));
        println!("{}", format!("{:?}", fivenum(g_x3.clone().clone())));
        let _end: i64 = _now();
        let _end_mem: i64 = _mem();
        let duration_us: i64 = ((_end - _start) / 1000);
        let memory_bytes: i64 = (_end_mem - _start_mem);
        println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

    }
}
