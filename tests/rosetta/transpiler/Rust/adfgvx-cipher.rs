// Generated by Mochi transpiler v0.10.54 on 2025-08-02 14:06 +0700
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(p) = s.split_whitespace().next() {
                if let Ok(v) = p.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
static mut g_adfgvx: String = String::new();
static mut g_alphabet: String = String::new();
fn main() {
    unsafe {
        g_adfgvx = String::from("ADFGVX").clone();
        g_alphabet = String::from("ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789").clone();
                let _start_mem: i64 = _mem();
        let _start: i64 = _now();
        static mut g_adfgvx: String = String::new();;
        static mut g_alphabet: String = String::new();;
        unsafe fn shuffleStr(mut s: String) -> String {
    let mut arr: Vec<String> = vec![];
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        arr = { let mut _v = arr.clone(); _v.push(s[i as usize..(i + 1) as usize].to_string()); _v };
        i = (i + 1);
    }
    let mut j = ((arr.len() as i64) - 1);
    while (j > 0) {
        let mut k = (_now() % (j + 1));
        let mut tmp: String = arr[j as usize].clone().clone();
        arr[j as usize] = arr[k as usize].clone();
        arr[k as usize] = tmp;
        j = (j - 1);
    }
    let mut out: String = String::from("").clone();
    i = 0;
    while (i < (arr.len() as i64)) {
        out = format!("{}{}", out, arr[i as usize].clone());
        i = (i + 1);
    }
    return out.clone()
};
        unsafe fn createPolybius() -> Vec<String> {
    let mut shuffled: String = shuffleStr(g_alphabet.clone()).clone();
    let mut labels: Vec<String> = vec![];
    let mut li: i64 = 0;
    while (li < (g_adfgvx.len() as i64)) {
        labels = { let mut _v = labels.clone(); _v.push(g_adfgvx[li as usize..(li + 1) as usize].to_string()); _v };
        li = (li + 1);
    }
    println!("{}", "6 x 6 Polybius square:\n");
    println!("{}", "  | A D F G V X");
    println!("{}", "---------------");
    let mut p: Vec<String> = vec![];
    let mut i: i64 = 0;
    while (i < 6) {
        let mut row: String = shuffled[(i * 6) as usize..((i + 1) * 6) as usize].to_string().clone();
        p = { let mut _v = p.clone(); _v.push(row.clone()); _v };
        let mut line: String = format!("{}{}", labels[i as usize].clone(), " | ").clone();
        let mut j: i64 = 0;
        while (j < 6) {
            line = format!("{}{}", format!("{}{}", line, row[j as usize..(j + 1) as usize].to_string()), " ");
            j = (j + 1);
        }
        println!("{}", line);
        i = (i + 1);
    }
    return p
};
        unsafe fn createKey(mut n: i64) -> String {
    if ((n < 7) || (n > 12)) {
        println!("{}", "Key should be within 7 and 12 letters long.");
    }
    let mut pool: String = String::from("ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789").clone();
    let mut key: String = String::from("").clone();
    let mut i: i64 = 0;
    while (i < n) {
        let mut idx = (_now() % (pool.len() as i64));
        key = format!("{}{}", key, pool.chars().nth(idx as usize).unwrap());
        pool = format!("{}{}", pool[0 as usize..idx as usize].to_string(), pool[(idx + 1) as usize..(pool.len() as i64) as usize].to_string());
        i = (i + 1);
    }
    println!("{}", format!("{}{}", "\nThe key is ", key));
    return key.clone()
};
        unsafe fn orderKey(mut key: String) -> i64 {
    let mut pairs = vec![];
    let mut i: i64 = 0;
    while (i < (key.len() as i64)) {
        pairs = { let mut _v = pairs.clone(); _v.push(vec![key[i as usize..(i + 1) as usize].to_string(), i]); _v };
        i = (i + 1);
    }
    let mut n = (pairs.len() as i64);
    let mut m: i64 = 0;
    while (m < n) {
        let mut j: i64 = 0;
        while (j < (n - 1)) {
            if (pairs[j as usize].clone()[0 as usize].clone().as_str() > pairs[(j + 1) as usize].clone()[0 as usize].clone().as_str()) {
                let mut tmp: Vec<String> = pairs[j as usize].clone();
                pairs[j as usize] = pairs[(j + 1) as usize].clone();
                pairs[(j + 1) as usize] = tmp;
            }
            j = (j + 1);
        }
        m = (m + 1);
    }
    let mut res = vec![];
    i = 0;
    while (i < n) {
        res = { let mut _v = res.clone(); _v.push({ let n: i64 = pairs[i as usize].clone()[1 as usize].clone().parse().unwrap(); n }); _v };
        i = (i + 1);
    }
    return res
};
        unsafe fn encrypt(mut polybius: Vec<String>, mut key: String, mut plainText: String) -> String {
    let mut labels: Vec<String> = vec![];
    let mut li: i64 = 0;
    while (li < (g_adfgvx.len() as i64)) {
        labels = { let mut _v = labels.clone(); _v.push(g_adfgvx[li as usize..(li + 1) as usize].to_string()); _v };
        li = (li + 1);
    }
    let mut temp: String = String::from("").clone();
    let mut i: i64 = 0;
    while (i < (plainText.len() as i64)) {
        let mut r: i64 = 0;
        while (r < 6) {
            let mut c: i64 = 0;
            while (c < 6) {
                if (polybius[r as usize].clone()[c as usize..(c + 1) as usize].to_string().as_str() == plainText[i as usize..(i + 1) as usize].to_string().as_str()) {
                    temp = format!("{}{}", format!("{}{}", temp, labels[r as usize..(r + 1) as usize].to_vec()), labels[c as usize..(c + 1) as usize].to_vec());
                }
                c = (c + 1);
            }
            r = (r + 1);
        }
        i = (i + 1);
    }
    let mut colLen = ((temp.len() as i64) / (key.len() as i64));
    if (((temp.len() as i64) % (key.len() as i64)) > 0) {
        colLen = (colLen + 1);
    }
    let mut table: Vec<Vec<String>> = vec![];
    let mut rIdx: i64 = 0;
    while (rIdx < colLen) {
        let mut row: Vec<String> = vec![];
        let mut j: i64 = 0;
        while (j < (key.len() as i64)) {
            row = { let mut _v = row.clone(); _v.push("".to_string()); _v };
            j = (j + 1);
        }
        table = { let mut _v = table.clone(); _v.push(row.clone()); _v };
        rIdx = (rIdx + 1);
    }
    let mut idx: i64 = 0;
    while (idx < (temp.len() as i64)) {
        let mut row: i64 = (idx / (key.len() as i64));
        let mut col: i64 = (idx % (key.len() as i64));
        table[row as usize][col as usize] = temp[idx as usize..(idx + 1) as usize].to_string();
        idx = (idx + 1);
    }
    let mut order: i64 = orderKey(key.clone());
    let mut cols: Vec<String> = vec![];
    let mut ci: i64 = 0;
    while (ci < (key.len() as i64)) {
        let mut colStr: String = String::from("").clone();
        let mut ri: i64 = 0;
        while (ri < colLen) {
            colStr = format!("{}{}", colStr, table[ri as usize].clone()[order[ci as usize] as usize].clone());
            ri = (ri + 1);
        }
        cols = { let mut _v = cols.clone(); _v.push(colStr.clone()); _v };
        ci = (ci + 1);
    }
    let mut result: String = String::from("").clone();
    ci = 0;
    while (ci < (cols.len() as i64)) {
        result = format!("{}{}", result, cols[ci as usize].clone());
        if (ci < ((cols.len() as i64) - 1)) {
            result = format!("{}{}", result, " ");
        }
        ci = (ci + 1);
    }
    return result.clone()
};
        unsafe fn indexOf(mut s: String, ch: &str) -> i64 {
    let mut i: i64 = 0;
    while (i < (s.len() as i64)) {
        if (s[i as usize..(i + 1) as usize].to_string() == ch) {
            return i
        }
        i = (i + 1);
    }
    return -1
};
        unsafe fn decrypt(mut polybius: Vec<String>, mut key: String, mut cipherText: String) -> String {
    let mut colStrs: Vec<String> = vec![];
    let mut start: i64 = 0;
    let mut i: i64 = 0;
    while (i <= (cipherText.len() as i64)) {
        if ((i == (cipherText.len() as i64)) || (cipherText.chars().nth(i as usize).unwrap() == " ")) {
            colStrs = { let mut _v = colStrs.clone(); _v.push(cipherText[start as usize..i as usize].to_string()); _v };
            start = (i + 1);
        }
        i = (i + 1);
    }
    let mut maxColLen: i64 = 0;
    i = 0;
    while (i < (colStrs.len() as i64)) {
        if ((colStrs[i as usize].clone().len() as i64) > maxColLen) {
            maxColLen = (colStrs[i as usize].clone().len() as i64);
        }
        i = (i + 1);
    }
    let mut cols: Vec<Vec<String>> = vec![];
    i = 0;
    while (i < (colStrs.len() as i64)) {
        let mut s: String = colStrs[i as usize].clone().clone();
        let mut ls: Vec<String> = vec![];
        let mut j: i64 = 0;
        while (j < (s.len() as i64)) {
            ls = { let mut _v = ls.clone(); _v.push(s[j as usize..(j + 1) as usize].to_string()); _v };
            j = (j + 1);
        }
        if ((s.len() as i64) < maxColLen) {
            let mut pad: Vec<String> = vec![];
            let mut k: i64 = 0;
            while (k < maxColLen) {
                if (k < (ls.len() as i64)) {
                    pad = { let mut _v = pad.clone(); _v.push(ls[k as usize].clone()); _v };
                } else {
                    pad = { let mut _v = pad.clone(); _v.push("".to_string()); _v };
                }
                k = (k + 1);
            }
            cols = { let mut _v = cols.clone(); _v.push(pad.clone()); _v };
        } else {
            cols = { let mut _v = cols.clone(); _v.push(ls.clone()); _v };
        }
        i = (i + 1);
    }
    let mut table: Vec<Vec<String>> = vec![];
    let mut r: i64 = 0;
    while (r < maxColLen) {
        let mut row: Vec<String> = vec![];
        let mut c: i64 = 0;
        while (c < (key.len() as i64)) {
            row = { let mut _v = row.clone(); _v.push("".to_string()); _v };
            c = (c + 1);
        }
        table = { let mut _v = table.clone(); _v.push(row.clone()); _v };
        r = (r + 1);
    }
    let mut order: i64 = orderKey(key.clone());
    r = 0;
    while (r < maxColLen) {
        let mut c: i64 = 0;
        while (c < (key.len() as i64)) {
            table[r as usize][order[c as usize] as usize] = cols[c as usize].clone()[r as usize].clone();
            c = (c + 1);
        }
        r = (r + 1);
    }
    let mut temp: String = String::from("").clone();
    r = 0;
    while (r < (table.len() as i64)) {
        let mut j: i64 = 0;
        while (j < (table[r as usize].clone().len() as i64)) {
            temp = format!("{}{}", temp, table[r as usize].clone()[j as usize].clone());
            j = (j + 1);
        }
        r = (r + 1);
    }
    let mut plainText: String = String::from("").clone();
    let mut idx: i64 = 0;
    while (idx < (temp.len() as i64)) {
        let mut rIdx: i64 = indexOf(g_adfgvx.clone(), &temp[idx as usize..(idx + 1) as usize].to_string());
        let mut cIdx: i64 = indexOf(g_adfgvx.clone(), &temp[(idx + 1) as usize..(idx + 2) as usize].to_string());
        plainText = format!("{}{}", plainText, polybius[rIdx as usize].clone()[cIdx as usize]);
        idx = (idx + 2);
    }
    return plainText.clone()
};
        unsafe fn mochi_main() {
    let mut plainText: String = String::from("ATTACKAT1200AM").clone();
    let mut polybius: Vec<String> = createPolybius();
    let mut key: String = createKey(9).clone();
    println!("{}", format!("{}{}", "\nPlaintext : ", plainText));
    let mut cipherText: String = encrypt(polybius.clone(), key.clone(), plainText.clone()).clone();
    println!("{}", format!("{}{}", "\nEncrypted : ", cipherText));
    let mut plainText2: String = decrypt(polybius.clone(), key.clone(), cipherText.clone()).clone();
    println!("{}", format!("{}{}", "\nDecrypted : ", plainText2));
};
        mochi_main();
        let _end: i64 = _now();
        let _end_mem: i64 = _mem();
        let duration_us: i64 = ((_end - _start) / 1000);
        let memory_bytes: i64 = (_end_mem - _start_mem);
        println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

    }
}
