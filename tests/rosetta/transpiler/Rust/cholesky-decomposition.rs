// Generated by Mochi transpiler v0.10.57 on 2025-08-05 01:37 +0700
use std::sync::atomic::{AtomicBool, AtomicI64, Ordering};
use std::time::{SystemTime, UNIX_EPOCH};
static NOW_SEEDED: AtomicBool = AtomicBool::new(false);
static NOW_SEED: AtomicI64 = AtomicI64::new(0);
fn _now() -> i64 {
    if !NOW_SEEDED.load(Ordering::SeqCst) {
        if let Ok(s) = std::env::var("MOCHI_NOW_SEED") {
            if let Ok(v) = s.parse::<i64>() {
                NOW_SEED.store(v, Ordering::SeqCst);
                NOW_SEEDED.store(true, Ordering::SeqCst);
            }
        }
    }
    if NOW_SEEDED.load(Ordering::SeqCst) {
        let seed = (NOW_SEED.load(Ordering::SeqCst)*1664525 + 1013904223) % 2147483647;
        NOW_SEED.store(seed, Ordering::SeqCst);
        seed
    } else {
        SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos() as i64
    }
}
fn _mem() -> i64 {
    if let Ok(mut f) = std::fs::File::open("/proc/self/statm") {
        let mut s = String::new();
        use std::io::Read;
        if f.read_to_string(&mut s).is_ok() {
            if let Some(rss) = s.split_whitespace().nth(1) {
                if let Ok(v) = rss.parse::<i64>() {
                    return v * 4096;
                }
            }
        }
    }
    0
}
fn main() {
        let _start: i64 = _now();
    fn sqrtApprox(mut x: f64) -> f64 {
    let mut guess: f64 = x;
    let mut i: i64 = 0;
    while (i < 20) {
        guess = ((guess + (x / guess)) / 2.0);
        i = (i + 1);
    }
    return guess
};
    fn cholesky(mut a: Vec<Vec<f64>>) -> Vec<Vec<f64>> {
    let mut n = (a.len() as i64);
    let mut l: Vec<Vec<f64>> = vec![];
    let mut i: i64 = 0;
    while (i < n) {
        let mut row: Vec<f64> = vec![];
        let mut j: i64 = 0;
        while (j < n) {
            row = { let mut _v = row.clone(); _v.push(0.0); _v };
            j = (j + 1);
        }
        l = { let mut _v = l.clone(); _v.push(row.clone()); _v };
        i = (i + 1);
    }
    i = 0;
    while (i < n) {
        let mut j: i64 = 0;
        while (j <= i) {
            let mut sum: f64 = a[i as usize].clone()[j as usize];
            let mut k: i64 = 0;
            while (k < j) {
                sum = (sum - (l[i as usize].clone()[k as usize] * l[j as usize].clone()[k as usize]));
                k = (k + 1);
            }
            if (i == j) {
                l[i as usize][j as usize] = sqrtApprox(sum);
            } else {
                l[i as usize][j as usize] = (sum / l[j as usize].clone()[j as usize]);
            }
            j = (j + 1);
        }
        i = (i + 1);
    }
    return l
};
    fn printMat(mut m: Vec<Vec<f64>>) {
    let mut i: i64 = 0;
    while (i < (m.len() as i64)) {
        let mut line: String = String::from("").clone();
        let mut j: i64 = 0;
        while (j < (m[i as usize].clone().len() as i64)) {
            line = format!("{}{}", line, m[i as usize].clone()[j as usize].to_string());
            if (j < ((m[i as usize].clone().len() as i64) - 1)) {
                line = format!("{}{}", line, " ");
            }
            j = (j + 1);
        }
        println!("{}", line);
        i = (i + 1);
    }
};
    fn demo(a: &mut Vec<Vec<f64>>) {
    println!("{}", "A:");
    printMat(a.clone());
    let mut l: Vec<Vec<f64>> = cholesky(a.clone());
    println!("{}", "L:");
    printMat(l.clone());
};
    demo(&mut vec![vec![25.0, 15.0, -5.0], vec![15.0, 18.0, 0.0], vec![-5.0, 0.0, 11.0]]);
    demo(&mut vec![vec![18.0, 22.0, 54.0, 42.0], vec![22.0, 70.0, 86.0, 62.0], vec![54.0, 86.0, 174.0, 134.0], vec![42.0, 62.0, 134.0, 106.0]]);
    let _end: i64 = _now();
    let duration_us: i64 = ((_end - _start) / 1000);
    let memory_bytes: i64 = _mem();
    println!("{{\n  \"duration_us\": {},\n  \"memory_bytes\": {},\n  \"name\": \"{}\"\n}}", duration_us, memory_bytes, "main");

}
