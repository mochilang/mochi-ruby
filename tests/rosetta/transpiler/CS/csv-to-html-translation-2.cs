// Generated by Mochi 0.10.47 on 2025-07-28 05:04 UTC
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static string c_0 = ((((("Character,Speech\n" + "The multitude,The messiah! Show us the messiah!\n") + "Brians mother,<angry>Now you listen here! He's not the messiah; he's a very naughty boy! Now go away!</angry>\n") + "The multitude,Who are you?\n") + "Brians mother,I'm his mother; that's who!\n") + "The multitude,Behold his mother! Behold his mother!");
    static string[][] rows_1 = new string[][]{};
    static bool headings_3 = true;
    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            foreach (var line_2 in c_0.Split("\n")) {
                rows_1 = (Enumerable.ToArray(Enumerable.Append(rows_1, line_2.Split(","))));
            }
            Console.WriteLine(_fmtTop("<table>"));
            if (headings_3) {
                if ((rows_1.Length > 0)) {
                    string th_4 = "";
                    foreach (var h_5 in rows_1[(int)(0)]) {
                        th_4 = (((th_4 + "<th>") + h_5) + "</th>");
                    }
                    Console.WriteLine(_fmtTop("   <thead>"));
                    Console.WriteLine(_fmtTop((("      <tr>" + th_4) + "</tr>")));
                    Console.WriteLine(_fmtTop("   </thead>"));
                    Console.WriteLine(_fmtTop("   <tbody>"));
                    long i_6 = 1;
                    while ((i_6 < rows_1.Length)) {
                        string cells_7 = "";
                        foreach (var cell_8 in rows_1[(int)(i_6)]) {
                            cells_7 = (((cells_7 + "<td>") + cell_8) + "</td>");
                        }
                        Console.WriteLine(_fmtTop((("      <tr>" + cells_7) + "</tr>")));
                        i_6 = (i_6 + 1);
                    }
                    Console.WriteLine(_fmtTop("   </tbody>"));
                }
            } else {
                foreach (var row_9 in rows_1) {
                    string cells_10 = "";
                    foreach (var cell_11 in row_9) {
                        cells_10 = (((cells_10 + "<td>") + cell_11) + "</td>");
                    }
                    Console.WriteLine(_fmtTop((("    <tr>" + cells_10) + "</tr>")));
                }
            }
            Console.WriteLine(_fmtTop("</table>"));
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
