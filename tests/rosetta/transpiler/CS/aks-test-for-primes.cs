// Generated by Mochi 0.10.52 on 2025-07-31 10:06 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static string poly(long p_0) {
        string s_1 = "";
        long coef_2 = 1;
        long i_3 = p_0;
        if ((coef_2 != 1)) {
            s_1 = (s_1 + (coef_2).ToString());
        };
        while ((i_3 > 0)) {
            s_1 = (s_1 + "x");
            if ((i_3 != 1)) {
                s_1 = ((s_1 + "^") + (i_3).ToString());
            }
            coef_2 = Convert.ToInt64(((coef_2 * i_3) / ((p_0 - i_3) + 1)));
            long d_4 = coef_2;
            if ((((p_0 - (i_3 - 1)) % 2) == 1)) {
                d_4 = -d_4;
            }
            if ((d_4 < 0)) {
                s_1 = ((s_1 + " - ") + (-d_4).ToString());
            } else {
                s_1 = ((s_1 + " + ") + (d_4).ToString());
            }
            i_3 = (i_3 - 1);
        };
        if ((s_1 == "")) {
            s_1 = "1";
        };
        return s_1;
    }

    static bool aks(long n_5) {
        if ((n_5 < 2)) {
            return false;
        };
        long c_6 = n_5;
        long i_7 = 1;
        while ((i_7 < n_5)) {
            if (((c_6 % n_5) != 0)) {
                return false;
            }
            c_6 = Convert.ToInt64(((c_6 * (n_5 - i_7)) / (i_7 + 1)));
            i_7 = (i_7 + 1);
        };
        return true;
    }

    static void main() {
        long p_8 = 0;
        while ((p_8 <= 7)) {
            Console.WriteLine(_fmtTop((((p_8).ToString() + ":  ") + poly(p_8))));
            p_8 = (p_8 + 1);
        };
        bool first_9 = true;
        p_8 = 2;
        string line_10 = "";
        while ((p_8 < 50)) {
            if (aks(p_8)) {
                if (first_9) {
                    line_10 = (line_10 + (p_8).ToString());
                    first_9 = false;
                } else {
                    line_10 = ((line_10 + " ") + (p_8).ToString());
                }
            }
            p_8 = (p_8 + 1);
        };
        Console.WriteLine(_fmtTop(line_10));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
