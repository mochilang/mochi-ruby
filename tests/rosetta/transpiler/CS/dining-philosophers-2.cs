// Generated by Mochi 0.10.55 on 2025-08-03 10:56 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    public static void main() {
        string[] philosophers_0 = new string[]{"Aristotle", "Kant", "Spinoza", "Marx", "Russell"};
        long hunger_1 = 3;
        Console.WriteLine(Program._fmtTop("table empty"));
        foreach (var p_2 in philosophers_0) {
            Console.WriteLine(Program._fmtTop((p_2 + " seated")));
        };
        long i_3 = 0;
        while ((Convert.ToDouble(i_3) < Convert.ToDouble(philosophers_0.Length))) {
            string name_4 = philosophers_0[(int)(i_3)];
            long h_5 = 0;
            while ((h_5 < hunger_1)) {
                Console.WriteLine(Program._fmtTop((name_4 + " hungry")));
                Console.WriteLine(Program._fmtTop((name_4 + " eating")));
                Console.WriteLine(Program._fmtTop((name_4 + " thinking")));
                h_5 = (h_5 + 1);
            }
            Console.WriteLine(Program._fmtTop((name_4 + " satisfied")));
            Console.WriteLine(Program._fmtTop((name_4 + " left the table")));
            i_3 = (i_3 + 1);
        };
        Console.WriteLine(Program._fmtTop("table empty"));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
