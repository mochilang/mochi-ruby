// Generated by Mochi 0.10.55 on 2025-08-02 20:58 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Diagnostics;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem(bool force) {
        if (force) GC.Collect();
        return Process.GetCurrentProcess().WorkingSet64;
    }
    static long fetchSomething() {
        return 0;
    }

    static void doPos(long x_0) {
    }

    static void doNeg(long x_1) {
    }

    static void example4() {
        long x_2 = fetchSomething();
        if ((x_2 > 0)) {
            doPos(x_2);
        } else {
            doNeg(x_2);
        };
    }

    static void Main() {
        {
            var __memStart = _mem(true);
            var __start = _now();
            var __end = _now();
            var __memEnd = _mem(true);
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
