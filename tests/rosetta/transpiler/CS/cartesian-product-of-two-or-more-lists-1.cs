// Generated by Mochi 0.10.57 on 2025-08-04 21:00 +0700
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalAllocatedBytes(true);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "true" : "false";
        return Convert.ToString(v);
    }
    public static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    public static long[][] cart2(long[] a_0, long[] b_1) {
        long[][] p_2 = new long[][]{};
        foreach (long x_3 in a_0) {
            foreach (long y_4 in b_1) {
                p_2 = (Enumerable.ToArray(Enumerable.Append(p_2, new long[]{x_3, y_4})));
            }
        };
        return p_2;
    }

    public static string llStr(long[][] lst_5) {
        string s_6 = "[";
        long i_7 = 0;
        while ((i_7 < lst_5.Length)) {
            long[] row_8 = lst_5[(int)(i_7)];
            s_6 = (s_6 + "[");
            long j_9 = 0;
            while ((j_9 < row_8.Length)) {
                s_6 = (s_6 + _fmt(row_8[(int)(j_9)]));
                if ((j_9 < (row_8.Length - 1))) {
                    s_6 = (s_6 + " ");
                }
                j_9 = (j_9 + 1);
            }
            s_6 = (s_6 + "]");
            if ((i_7 < (lst_5.Length - 1))) {
                s_6 = (s_6 + " ");
            }
            i_7 = (i_7 + 1);
        };
        s_6 = (s_6 + "]");
        return s_6;
    }

    public static void main() {
        Console.WriteLine(Program._fmtTop(Program.llStr(Program.cart2(new long[]{1, 2}, new long[]{3, 4}))));
        Console.WriteLine(Program._fmtTop(Program.llStr(Program.cart2(new long[]{3, 4}, new long[]{1, 2}))));
        Console.WriteLine(Program._fmtTop(Program.llStr(Program.cart2(new long[]{1, 2}, new long[]{}))));
        Console.WriteLine(Program._fmtTop(Program.llStr(Program.cart2(new long[]{}, new long[]{1, 2}))));
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            Program.main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            if (__dur <= 0) __dur = 1;
            var __memDiff = __memEnd - __memStart;
            if (__memDiff <= 0) __memDiff = __memEnd;
            Console.WriteLine("{\"name\":\"main\",\"duration_us\":" + __dur + ",\"memory_bytes\":" + __memDiff + "}");
        }
    }
}
