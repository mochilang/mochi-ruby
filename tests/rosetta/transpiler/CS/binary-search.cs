// Generated by Mochi 0.10.52 on 2025-07-31 04:58 UTC
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.Json;
using System.Numerics;
using System.Collections;

class Program {
    static bool seededNow = false;
    static long nowSeed = 0;
    static long _now() {
        if (!seededNow) {
            var s = Environment.GetEnvironmentVariable("MOCHI_NOW_SEED");
            if (long.TryParse(s, out var v)) {
                nowSeed = v;
                seededNow = true;
            }
        }
        if (seededNow) {
            nowSeed = unchecked(nowSeed * 1664525 + 1013904223);
            nowSeed %= 9223372036854775783L;
            if (nowSeed < 0) nowSeed += 9223372036854775783L;
            return nowSeed;
        }
        return DateTime.UtcNow.Ticks / 100;
    }
    static long _mem() {
        return GC.GetTotalMemory(false);
    }
    static string _fmt(object v) {
        if (v is Array a) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return "[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IDictionary d) {
            var keys = new List<string>();
            foreach (var k in d.Keys) keys.Add(k.ToString());
            keys.Sort();
            var parts = new List<string>();
            foreach (var k in keys) parts.Add(k + ":" + _fmt(d[k]));
            return "map[" + string.Join(" ", parts) + "]";
        }
        if (v is System.Collections.IEnumerable e && !(v is string)) {
            var parts = new List<string>();
            foreach (var x in e) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        if (v is bool b) return b ? "1" : "0";
        return Convert.ToString(v);
    }
    static string _fmtTop(object v) {
        if (v is Array a && a.Length > 0 && a.GetValue(0) is Array) {
            var parts = new List<string>();
            foreach (var x in a) parts.Add(_fmt(x));
            return string.Join(" ", parts);
        }
        return _fmt(v);
    }
    static long bsearch(long[] arr_0, long x_1) {
        long low_2 = 0;
        long high_3 = (((dynamic)(arr_0.Length)) - ((dynamic)(1)));
        while ((low_2 <= high_3)) {
            long mid_4 = ((low_2 + high_3) / 2);
            if ((arr_0[(int)(mid_4)] > x_1)) {
                high_3 = (mid_4 - 1);
            } else {
                if ((arr_0[(int)(mid_4)] < x_1)) {
                    low_2 = (mid_4 + 1);
                } else {
                    return mid_4;
                }
            }
        };
        return -1;
    }

    static long bsearchRec(long[] arr_5, long x_6, long low_7, long high_8) {
        if ((high_8 < low_7)) {
            return -1;
        };
        long mid_9 = ((low_7 + high_8) / 2);
        if ((arr_5[(int)(mid_9)] > x_6)) {
            return bsearchRec(arr_5, x_6, low_7, (mid_9 - 1));
        } else {
            if ((arr_5[(int)(mid_9)] < x_6)) {
                return bsearchRec(arr_5, x_6, (mid_9 + 1), high_8);
            }
        };
        return mid_9;
    }

    static void main() {
        long[] nums_10 = new long[]{-31, 0, 1, 2, 2, 4, 65, 83, 99, 782};
        long x_11 = 2;
        long idx_12 = bsearch(nums_10, x_11);
        if ((idx_12 >= 0)) {
            Console.WriteLine(_fmtTop(((((x_11).ToString() + " is at index ") + (idx_12).ToString()) + ".")));
        } else {
            Console.WriteLine(_fmtTop(((x_11).ToString() + " is not found.")));
        };
        x_11 = 5;
        idx_12 = bsearchRec(nums_10, x_11, 0, (((dynamic)(nums_10.Length)) - ((dynamic)(1))));
        if ((idx_12 >= 0)) {
            Console.WriteLine(_fmtTop(((((x_11).ToString() + " is at index ") + (idx_12).ToString()) + ".")));
        } else {
            Console.WriteLine(_fmtTop(((x_11).ToString() + " is not found.")));
        };
    }

    static void Main() {
        {
            var __memStart = _mem();
            var __start = _now();
            main();
            var __end = _now();
            var __memEnd = _mem();
            var __dur = (__end - __start);
            var __memDiff = __memEnd - __memStart;
            Console.WriteLine(JsonSerializer.Serialize(new SortedDictionary<string, object>{{"name", "main"}, {"duration_us", __dur}, {"memory_bytes", __memDiff}}, new JsonSerializerOptions{ WriteIndented = true }));
        }
    }
}
