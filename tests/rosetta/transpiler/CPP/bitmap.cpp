// Generated by Mochi transpiler v0.10.41 on 2025-07-26 19:34:52 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <any>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <type_traits>


static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}


struct Pixel {
    int64_t R;
    int64_t G;
    int64_t B;
    auto operator<=>(const Pixel&) const = default;
};

struct Bitmap {
    int64_t cols;
    int64_t rows;
    std::vector<std::vector<Pixel>> px;
    auto operator<=>(const Bitmap&) const = default;
};

std::ostream& operator<<(std::ostream& os, const Pixel& v) {
    os << '{' << "'R': "<< v.R
 << ", " << "'G': "<< v.G
 << ", " << "'B': "<< v.B
 << '}';
    return os;
}

std::ostream& operator<<(std::ostream& os, const Bitmap& v) {
    os << '{' << "'cols': "<< v.cols
 << ", " << "'rows': "<< v.rows
 << ", " << "'px': "<< "["; for(size_t i=0;i<v.px.size();++i){ if(i>0) os << ", "; os << v.px[i]; } os << "]"
 << '}';
    return os;
}

Pixel pixelFromRgb(int64_t c);
int64_t rgbFromPixel(Pixel p);
Bitmap NewBitmap(int64_t x, int64_t y);
std::map<std::string, int64_t> Extent(Bitmap b);
void Fill(Bitmap b, Pixel p);
void FillRgb(Bitmap b, int64_t c);
bool SetPx(Bitmap b, int64_t x, int64_t y, Pixel p);
bool SetPxRgb(Bitmap b, int64_t x, int64_t y, int64_t c);
std::map<std::string, std::any> GetPx(Bitmap b, int64_t x, int64_t y);
std::map<std::string, std::any> GetPxRgb(Bitmap b, int64_t x, int64_t y);
int64_t ppmSize(Bitmap b);
std::string pixelStr(Pixel p);
int main();

Pixel pixelFromRgb(int64_t c) {
    int64_t r = ((int64_t)((c / 65536)) % 256);
    int64_t g = ((int64_t)((c / 256)) % 256);
    int64_t b = (c % 256);
    return Pixel{r, g, b};
}

int64_t rgbFromPixel(Pixel p) {
    return (((p.R * 65536) + (p.G * 256)) + p.B);
}

Bitmap NewBitmap(int64_t x, int64_t y) {
    std::vector<std::vector<Pixel>> data = {};
    int64_t row = 0;
    while ((row < y)) {
        std::vector<Pixel> r = {};
        int64_t col = 0;
        while ((col < x)) {
            r = ([&]{ auto __tmp = r; __tmp.push_back(Pixel{0, 0, 0}); return __tmp; }());
            col = (col + 1);
        }
        data = ([&]{ auto __tmp = data; __tmp.push_back(r); return __tmp; }());
        row = (row + 1);
    }
    return Bitmap{x, y, data};
}

std::map<std::string, int64_t> Extent(Bitmap b) {
    return std::map<std::string, int64_t>{{"cols", b.cols}, {"rows", b.rows}};
}

void Fill(Bitmap b, Pixel p) {
    int64_t y = 0;
    while ((y < b.rows)) {
        int64_t x = 0;
        while ((x < b.cols)) {
            std::vector<std::vector<Pixel>> px = b.px;
            std::vector<Pixel> row = px[y];
            row[x] = p;
            px[y] = row;
            b.px = px;
            x = (x + 1);
        }
        y = (y + 1);
    }
}

void FillRgb(Bitmap b, int64_t c) {
    Fill(b, pixelFromRgb(c));
}

bool SetPx(Bitmap b, int64_t x, int64_t y, Pixel p) {
    if (((((x < 0) || (x >= b.cols)) || (y < 0)) || (y >= b.rows))) {
        return false;
    }
    std::vector<std::vector<Pixel>> px = b.px;
    std::vector<Pixel> row = px[y];
    row[x] = p;
    px[y] = row;
    b.px = px;
    return true;
}

bool SetPxRgb(Bitmap b, int64_t x, int64_t y, int64_t c) {
    return SetPx(b, x, y, pixelFromRgb(c));
}

std::map<std::string, std::any> GetPx(Bitmap b, int64_t x, int64_t y) {
    if (((((x < 0) || (x >= b.cols)) || (y < 0)) || (y >= b.rows))) {
        return std::map<std::string, std::any>{{"ok", std::any(false)}};
    }
    std::vector<Pixel> row = b.px[y];
    return std::map<std::string, std::any>{{"ok", std::any(true)}, {"pixel", std::any(row[x])}};
}

std::map<std::string, std::any> GetPxRgb(Bitmap b, int64_t x, int64_t y) {
    std::map<std::string, std::any> r = GetPx(b, x, y);
    if (!r["ok"]) {
        return std::map<std::string, std::any>{{"ok", std::any(false)}};
    }
    return std::map<std::string, std::any>{{"ok", std::any(true)}, {"rgb", std::any(rgbFromPixel((Pixel)(r["pixel"])))}};
}

int64_t ppmSize(Bitmap b) {
    std::string header = ((((std::string("P6\n# Creator: Rosetta Code http://rosettacode.org/\n") + ([&]{ std::ostringstream ss; ss << std::boolalpha << b.cols; return ss.str(); }())) + std::string(" ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << b.rows; return ss.str(); }())) + std::string("\n255\n"));
    return (header.size() + ((3 * b.cols) * b.rows));
}

std::string pixelStr(Pixel p) {
    return ((((((std::string("{") + ([&]{ std::ostringstream ss; ss << std::boolalpha << p.R; return ss.str(); }())) + std::string(" ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << p.G; return ss.str(); }())) + std::string(" ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << p.B; return ss.str(); }())) + std::string("}"));
}

int main() {
    Bitmap bm = NewBitmap(300, 240);
    FillRgb(bm, 16711680);
    SetPxRgb(bm, 10, 20, 255);
    SetPxRgb(bm, 20, 30, 0);
    SetPxRgb(bm, 30, 40, 1056816);
    std::map<std::string, std::any> c1 = GetPx(bm, 0, 0);
    std::map<std::string, std::any> c2 = GetPx(bm, 10, 20);
    std::map<std::string, std::any> c3 = GetPx(bm, 30, 40);
    std::cout << (((std::string("Image size: ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << bm.cols; return ss.str(); }())) + std::string(" × ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << bm.rows; return ss.str(); }()));
    std::cout << std::endl;
    std::cout << (([&]{ std::ostringstream ss; ss << std::boolalpha << ppmSize(bm); return ss.str(); }()) + std::string(" bytes when encoded as PPM."));
    std::cout << std::endl;
    if (c1["ok"]) {
        std::cout << (std::string("Pixel at (0,0) is ") + pixelStr((Pixel)(c1["pixel"])));
        std::cout << std::endl;
    }
    if (c2["ok"]) {
        std::cout << (std::string("Pixel at (10,20) is ") + pixelStr((Pixel)(c2["pixel"])));
        std::cout << std::endl;
    }
    if (c3["ok"]) {
        auto p = c3["pixel"];
        auto r16 = (p.R * 257);
        auto g16 = (p.G * 257);
        auto b16 = (p.B * 257);
        std::cout << (((((std::string("Pixel at (30,40) has R=") + ([&]{ std::ostringstream ss; any_to_stream(ss, r16);
 return ss.str(); }())) + std::string(", G=")) + ([&]{ std::ostringstream ss; any_to_stream(ss, g16);
 return ss.str(); }())) + std::string(", B=")) + ([&]{ std::ostringstream ss; any_to_stream(ss, b16);
 return ss.str(); }()));
        std::cout << std::endl;
    }
    return 0;
}
