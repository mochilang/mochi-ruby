// Generated by Mochi transpiler v0.10.57 on 2025-08-04 21:23:29 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <type_traits>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>
#include <unistd.h>
#include <cstdio>
#include <map>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long long _mem() {
#if defined(__linux__)
    long long rss = 0;
    FILE* f = std::fopen("/proc/self/statm", "r");
    if (f) { long long pages; if (std::fscanf(f, "%*s %lld", &pages) == 1) rss = pages * sysconf(_SC_PAGESIZE); std::fclose(f); }
    if (rss == 0) {
        f = std::fopen("/proc/self/status", "r");
        if (f) {
            char buf[256];
            while (std::fgets(buf, sizeof buf, f)) {
                long long val;
                if (std::sscanf(buf, "VmRSS: %lld", &val) == 1 || std::sscanf(buf, "VmHWM: %lld", &val) == 1) {
                    rss = val * 1024;
                    break;
                }
            }
            std::fclose(f);
        }
    }
    if (rss == 0) {
        struct rusage usage{};
        getrusage(RUSAGE_SELF, &usage);
        rss = (long long)usage.ru_maxrss * 1024;
    }
    return rss;
#elif defined(__APPLE__)
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss;
#else
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
    return (long long)usage.ru_maxrss * 1024;
#endif
}
static long long _bench_now() {
    return std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count();
}
static long _parse_int_str(const std::string& s, long base) {
    return std::stol(s, nullptr, base);
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::string toBase(int64_t n, int64_t b);
int64_t parseIntStr(std::string str);
int64_t parseIntBase(std::string s, int64_t b);
std::string reverseStr(std::string s);
bool isPalindrome(std::string s);
bool isPalindromeBin(int64_t n);
int64_t myMin(int64_t a, int64_t b);
int64_t myMax(int64_t a, int64_t b);
int64_t reverse3(int64_t n);
void show(int64_t n);
int main();

std::string toBase(int64_t n, int64_t b) {
    if ((n == int64_t(0))) {
        return std::string("0");
    }
    std::string s = std::string("");
    int64_t x = n;
    while ((x > int64_t(0))) {
        s = (([&]{ std::ostringstream ss; ss << std::boolalpha << (x % b); return ss.str(); }()) + s);
        x = (int64_t)((x / b));
    }
    return s;
}

int64_t parseIntStr(std::string str) {
    int64_t i = int64_t(0);
    bool neg = false;
    if (((str.size() > int64_t(0)) && (std::string(1, str[int64_t(0)]) == std::string("-")))) {
        neg = true;
        i = int64_t(1);
    }
    int64_t n = int64_t(0);
    while ((i < str.size())) {
        n = (((n * int64_t(10)) + std::stoll(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(str))) - std::stoll(std::string("0")));
        i = (i + int64_t(1));
    }
    if (neg) {
        n = -n;
    }
    return n;
}

int64_t parseIntBase(std::string s, int64_t b) {
    int64_t n = int64_t(0);
    int64_t i = int64_t(0);
    while ((i < s.size())) {
        n = ((n * b) + _parse_int_str(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(s), int64_t(10)));
        i = (i + int64_t(1));
    }
    return n;
}

std::string reverseStr(std::string s) {
    std::string out = std::string("");
    int64_t i = (s.size() - int64_t(1));
    while ((i >= int64_t(0))) {
        out = (out + ([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(s));
        i = (i - int64_t(1));
    }
    return out;
}

bool isPalindrome(std::string s) {
    return (s == reverseStr(s));
}

bool isPalindromeBin(int64_t n) {
    std::string b = toBase(n, int64_t(2));
    return isPalindrome(b);
}

int64_t myMin(int64_t a, int64_t b) {
    if ((a < b)) {
        return a;
    }
    return b;
}

int64_t myMax(int64_t a, int64_t b) {
    if ((a > b)) {
        return a;
    }
    return b;
}

int64_t reverse3(int64_t n) {
    int64_t x = int64_t(0);
    int64_t y = n;
    while ((y != int64_t(0))) {
        x = ((x * int64_t(3)) + (y % int64_t(3)));
        y = (int64_t)((y / int64_t(3)));
    }
    return x;
}

void show(int64_t n) {
    std::cout << (std::string("Decimal : ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }()));
    std::cout << std::endl;
    std::cout << (std::string("Binary  : ") + toBase(n, int64_t(2)));
    std::cout << std::endl;
    std::cout << (std::string("Ternary : ") + toBase(n, int64_t(3)));
    std::cout << std::endl;
    std::cout << std::string("");
    std::cout << std::endl;
}

int main() {
    {
        struct __BenchGuard {
            long long start;
            __BenchGuard() : start(_bench_now()) {}
            ~__BenchGuard() {
                    auto __bench_end = _bench_now();
                    auto __bench_mem = _mem();
                    auto __bench_dur = __bench_end - start;
                    std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
            }
        } __bench_guard;
        std::cout << std::string("The first 6 numbers which are palindromic in both binary and ternary are :\n");
        std::cout << std::endl;
        show(int64_t(0));
        int64_t count = int64_t(1);
        int64_t lo = int64_t(0);
        int64_t hi = int64_t(1);
        int64_t pow2 = int64_t(1);
        int64_t pow3 = int64_t(1);
        while (true) {
            int64_t i = lo;
            while ((i < hi)) {
                int64_t n = ((((i * int64_t(3)) + int64_t(1)) * pow3) + reverse3(i));
                if (isPalindromeBin(n)) {
                    show(n);
                    count = (count + int64_t(1));
                    if ((count >= int64_t(6))) {
                        return int64_t(0);
                    }
                }
                i = (i + int64_t(1));
            }
            if ((i == pow3)) {
                pow3 = (pow3 * int64_t(3));
            } else {
                pow2 = (pow2 * int64_t(4));
            }
            while (true) {
                while ((pow2 <= pow3)) {
                    pow2 = (pow2 * int64_t(4));
                }
                int64_t lo2 = (int64_t)((((pow2 / pow3) - int64_t(1)) / int64_t(3)));
                int64_t hi2 = ((int64_t)(((((pow2 * int64_t(2)) / pow3) - int64_t(1)) / int64_t(3))) + int64_t(1));
                int64_t lo3 = (int64_t)((pow3 / int64_t(3)));
                int64_t hi3 = pow3;
                if ((lo2 >= hi3)) {
                    pow3 = (pow3 * int64_t(3));
                } else                 if ((lo3 >= hi2)) {
                    pow2 = (pow2 * int64_t(4));
                } else {
                    lo = myMax(lo2, lo3);
                    hi = myMin(hi2, hi3);
                    break;
                }

            }
        }
    }
    return 0;
}
