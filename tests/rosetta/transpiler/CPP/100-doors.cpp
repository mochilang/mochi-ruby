// Generated by Mochi transpiler v0.10.37 on 2025-07-23 09:22:18 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include <iomanip>
#include <optional>



std::vector<int> doors = {};

int main() {
    for (int i = 0; i < 100; i++ ) {
        doors = ([&]{ auto v = doors; v.push_back(false); return v; }());
    }
    for (int pass = 1; pass < 101; pass++ ) {
        auto idx = (pass - 1);
        while ((idx < 100)) {
            doors[idx] = !doors[idx];
            idx = (idx + pass);
        }
    }
    for (int row = 0; row < 10; row++ ) {
        std::string line = std::string("");
        for (int col = 0; col < 10; col++ ) {
            auto idx = ((row * 10) + col);
            if (doors[idx]) {
                line = (line + std::string("1"));
            } else {
                line = (line + std::string("0"));
            }
            if ((col < 9)) {
                line = (line + std::string(" "));
            }
        }
        std::cout << line;
        std::cout << std::endl;
    }
    return 0;
}
