// Generated by Mochi transpiler v0.10.55 on 2025-08-03 00:26:39 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <map>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>


static long _parse_int_str(const std::string& s, long base) {
    return std::stol(s, nullptr, base);
}
static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

double sinApprox(double x);
double floor(double x);
double absFloat(double x);
int64_t absInt(int64_t n);
int64_t parseIntStr(std::string str);
std::vector<int64_t> parseDate(std::string s);
bool leap(int64_t y);
int64_t daysInMonth(int64_t y, int64_t m);
std::vector<int64_t> addDays(int64_t y, int64_t m, int64_t d, int64_t n);
std::string pad2(int64_t n);
std::string dateString(int64_t y, int64_t m, int64_t d);
int64_t day(int64_t y, int64_t m, int64_t d);
void biorhythms(std::string birth, std::string target);
int main();

double PI = 3.141592653589793;
double TWO_PI = 6.283185307179586;

double sinApprox(double x) {
    double term = x;
    double sum = x;
    int64_t n = int64_t(1);
    while ((n <= int64_t(8))) {
        double denom = (double)(((int64_t(2) * n) * ((int64_t(2) * n) + int64_t(1))));
        term = ((double)(((-term * x) * x)) / (denom));
        sum = (sum + term);
        n = (n + int64_t(1));
    }
    return sum;
}

double floor(double x) {
    int64_t i = (int64_t)(x);
    if (((double)(i) > x)) {
        i = (i - int64_t(1));
    }
    return (double)(i);
}

double absFloat(double x) {
    if ((x < 0.0)) {
        return -x;
    }
    return x;
}

int64_t absInt(int64_t n) {
    if ((n < int64_t(0))) {
        return -n;
    }
    return n;
}

int64_t parseIntStr(std::string str) {
    int64_t i = int64_t(0);
    bool neg = false;
    if (((str.size() > int64_t(0)) && (([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), int64_t(1) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+int64_t(1)); })(str) == std::string("-")))) {
        neg = true;
        i = int64_t(1);
    }
    int64_t n = int64_t(0);
    std::map<std::string, int64_t> digits = std::map<std::string, int64_t>{{"0", int64_t(0)}, {"1", int64_t(1)}, {"2", int64_t(2)}, {"3", int64_t(3)}, {"4", int64_t(4)}, {"5", int64_t(5)}, {"6", int64_t(6)}, {"7", int64_t(7)}, {"8", int64_t(8)}, {"9", int64_t(9)}};
    while ((i < str.size())) {
        n = ((n * int64_t(10)) + digits.at(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(i, (i + int64_t(1)) - i); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+i, __v.begin()+(i + int64_t(1))); })(str)));
        i = (i + int64_t(1));
    }
    if (neg) {
        n = -n;
    }
    return n;
}

std::vector<int64_t> parseDate(std::string s) {
    int64_t y = _parse_int_str(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(0), int64_t(4) - int64_t(0)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(0), __v.begin()+int64_t(4)); })(s), int64_t(10));
    int64_t m = _parse_int_str(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(5), int64_t(7) - int64_t(5)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(5), __v.begin()+int64_t(7)); })(s), int64_t(10));
    int64_t d = _parse_int_str(([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(int64_t(8), int64_t(10) - int64_t(8)); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+int64_t(8), __v.begin()+int64_t(10)); })(s), int64_t(10));
    return std::vector<int64_t>{y, m, d};
}

bool leap(int64_t y) {
    if (((y % int64_t(400)) == int64_t(0))) {
        return true;
    }
    if (((y % int64_t(100)) == int64_t(0))) {
        return false;
    }
    return ((y % int64_t(4)) == int64_t(0));
}

int64_t daysInMonth(int64_t y, int64_t m) {
    int64_t feb = (leap(y) ? int64_t(29) : int64_t(28));
    std::vector<int64_t> lengths = std::vector<int64_t>{int64_t(31), feb, int64_t(31), int64_t(30), int64_t(31), int64_t(30), int64_t(31), int64_t(31), int64_t(30), int64_t(31), int64_t(30), int64_t(31)};
    return lengths[(m - int64_t(1))];
}

std::vector<int64_t> addDays(int64_t y, int64_t m, int64_t d, int64_t n) {
    int64_t yy = y;
    int64_t mm = m;
    int64_t dd = d;
    if ((n >= int64_t(0))) {
        int64_t i = int64_t(0);
        while ((i < n)) {
            dd = (dd + int64_t(1));
            if ((dd > daysInMonth(yy, mm))) {
                dd = int64_t(1);
                mm = (mm + int64_t(1));
                if ((mm > int64_t(12))) {
                    mm = int64_t(1);
                    yy = (yy + int64_t(1));
                }
            }
            i = (i + int64_t(1));
        }
    } else {
        int64_t i = int64_t(0);
        while ((i > n)) {
            dd = (dd - int64_t(1));
            if ((dd < int64_t(1))) {
                mm = (mm - int64_t(1));
                if ((mm < int64_t(1))) {
                    mm = int64_t(12);
                    yy = (yy - int64_t(1));
                }
                dd = daysInMonth(yy, mm);
            }
            i = (i - int64_t(1));
        }
    }
    return std::vector<int64_t>{yy, mm, dd};
}

std::string pad2(int64_t n) {
    if ((n < int64_t(10))) {
        return (std::string("0") + ([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }()));
    }
    return ([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }());
}

std::string dateString(int64_t y, int64_t m, int64_t d) {
    return ((((([&]{ std::ostringstream ss; ss << std::boolalpha << y; return ss.str(); }()) + std::string("-")) + pad2(m)) + std::string("-")) + pad2(d));
}

int64_t day(int64_t y, int64_t m, int64_t d) {
    int64_t part1 = (int64_t(367) * y);
    int64_t part2 = (int64_t)(((int64_t(7) * (int64_t)((y + ((m + int64_t(9)) / int64_t(12))))) / int64_t(4)));
    int64_t part3 = (int64_t)(((int64_t(275) * m) / int64_t(9)));
    return ((((part1 - part2) + part3) + d) - int64_t(730530));
}

void biorhythms(std::string birth, std::string target) {
    std::vector<int64_t> bparts = parseDate(birth);
    int64_t by = bparts[int64_t(0)];
    int64_t bm = bparts[int64_t(1)];
    int64_t bd = bparts[int64_t(2)];
    std::vector<int64_t> tparts = parseDate(target);
    int64_t ty = tparts[int64_t(0)];
    int64_t tm = tparts[int64_t(1)];
    int64_t td = tparts[int64_t(2)];
    int64_t diff = absInt((day(ty, tm, td) - day(by, bm, bd)));
    std::cout << (((std::string("Born ") + birth) + std::string(", Target ")) + target);
    std::cout << std::endl;
    std::cout << (std::string("Day ") + ([&]{ std::ostringstream ss; ss << std::boolalpha << diff; return ss.str(); }()));
    std::cout << std::endl;
    std::vector<std::string> cycles = std::vector<std::string>{std::string("Physical day "), std::string("Emotional day"), std::string("Mental day   ")};
    std::vector<int64_t> lengths = std::vector<int64_t>{int64_t(23), int64_t(28), int64_t(33)};
    std::vector<std::vector<std::string>> quadrants = std::vector<std::vector<std::string>>{std::vector<std::string>{std::string("up and rising"), std::string("peak")}, std::vector<std::string>{std::string("up but falling"), std::string("transition")}, std::vector<std::string>{std::string("down and falling"), std::string("valley")}, std::vector<std::string>{std::string("down but rising"), std::string("transition")}};
    int64_t i = int64_t(0);
    while ((i < int64_t(3))) {
        int64_t length = lengths[i];
        std::string cycle = cycles[i];
        int64_t position = (diff % length);
        int64_t quadrant = ((position * int64_t(4)) / length);
        double percent = sinApprox(((double)(((2.0 * PI) * (double)(position))) / ((double)(length))));
        percent = ((double)(floor((percent * 1000.0))) / (10.0));
        std::string description = std::string("");
        if ((percent > 95.0)) {
            description = std::string(" peak");
        } else         if ((percent < -95.0)) {
            description = std::string(" valley");
        } else         if ((absFloat(percent) < 5.0)) {
            description = std::string(" critical transition");
        } else {
            int64_t daysToAdd = ((((quadrant + int64_t(1)) * length) / int64_t(4)) - position);
            std::vector<int64_t> res = addDays(ty, tm, td, daysToAdd);
            int64_t ny = res[int64_t(0)];
            int64_t nm = res[int64_t(1)];
            int64_t nd = res[int64_t(2)];
            std::string transition = dateString(ny, nm, nd);
            std::string trend = quadrants[quadrant][int64_t(0)];
            std::string next = quadrants[quadrant][int64_t(1)];
            std::string pct = ([&]{ std::ostringstream ss; ss << std::boolalpha << percent; return ss.str(); }());
            if (!(pct.find(std::string(".")) != std::string::npos)) {
                pct = (pct + std::string(".0"));
            }
            description = ((((((((std::string(" ") + pct) + std::string("% (")) + trend) + std::string(", next ")) + next) + std::string(" ")) + transition) + std::string(")"));
        }


        std::string posStr = ([&]{ std::ostringstream ss; ss << std::boolalpha << position; return ss.str(); }());
        if ((position < int64_t(10))) {
            posStr = (std::string(" ") + posStr);
        }
        std::cout << (((cycle + posStr) + std::string(" : ")) + description);
        std::cout << std::endl;
        i = (i + int64_t(1));
    }
    std::cout << std::string("");
    std::cout << std::endl;
}

int main() {
    std::vector<std::vector<std::string>> pairs = std::vector<std::vector<std::string>>{std::vector<std::string>{std::string("1943-03-09"), std::string("1972-07-11")}, std::vector<std::string>{std::string("1809-01-12"), std::string("1863-11-19")}, std::vector<std::string>{std::string("1809-02-12"), std::string("1863-11-19")}};
    int64_t idx = int64_t(0);
    while ((idx < pairs.size())) {
        std::vector<std::string> p = pairs[idx];
        biorhythms(p[int64_t(0)], p[int64_t(1)]);
        idx = (idx + int64_t(1));
    }
    return 0;
}
