// Generated by Mochi transpiler v0.10.57 on 2025-08-04 22:11:46 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <functional>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <type_traits>
#include <map>


static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int main();

int main() {
    int64_t INF = int64_t(1000000000);
    int64_t n = int64_t(4);
    std::vector<std::vector<int64_t>> dist = {};
    std::vector<std::vector<int64_t>> next = {};
    int64_t i = int64_t(0);
    while ((i < n)) {
        std::vector<int64_t> row = {};
        std::vector<int64_t> nrow = {};
        int64_t j = int64_t(0);
        while ((j < n)) {
            if ((i == j)) {
                row = ([&]{ auto __tmp = row; __tmp.push_back(int64_t(0)); return __tmp; }());
            } else {
                row = ([&]{ auto __tmp = row; __tmp.push_back(INF); return __tmp; }());
            }
            nrow = ([&]{ auto __tmp = nrow; __tmp.push_back((int64_t(0) - int64_t(1))); return __tmp; }());
            j = (j + int64_t(1));
        }
        dist = ([&]{ auto __tmp = dist; __tmp.push_back(row); return __tmp; }());
        next = ([&]{ auto __tmp = next; __tmp.push_back(nrow); return __tmp; }());
        i = (i + int64_t(1));
    }
    dist[int64_t(0)][int64_t(2)] = -int64_t(2);
    next[int64_t(0)][int64_t(2)] = int64_t(2);
    dist[int64_t(2)][int64_t(3)] = int64_t(2);
    next[int64_t(2)][int64_t(3)] = int64_t(3);
    dist[int64_t(3)][int64_t(1)] = -int64_t(1);
    next[int64_t(3)][int64_t(1)] = int64_t(1);
    dist[int64_t(1)][int64_t(0)] = int64_t(4);
    next[int64_t(1)][int64_t(0)] = int64_t(0);
    dist[int64_t(1)][int64_t(2)] = int64_t(3);
    next[int64_t(1)][int64_t(2)] = int64_t(2);
    int64_t k = int64_t(0);
    while ((k < n)) {
        int64_t i = int64_t(0);
        while ((i < n)) {
            int64_t j = int64_t(0);
            while ((j < n)) {
                if (((dist[i][k] < INF) && (dist[k][j] < INF))) {
                    int64_t alt = (dist[i][k] + dist[k][j]);
                    if ((alt < dist[i][j])) {
                        dist[i][j] = alt;
                        next[i][j] = next[i][k];
                    }
                }
                j = (j + int64_t(1));
            }
            i = (i + int64_t(1));
        }
        k = (k + int64_t(1));
    }
    std::function<std::vector<int64_t>(int64_t, int64_t)> path = [=](int64_t u, int64_t v) mutable {
        int64_t ui = (u - int64_t(1));
        int64_t vi = (v - int64_t(1));
        if ((next[ui][vi] == (int64_t(0) - int64_t(1)))) {
            return std::vector<int64_t>{};
        }
        std::vector<int64_t> p = std::vector<int64_t>{u};
        int64_t cur = ui;
        while ((cur != vi)) {
            cur = next[cur][vi];
            p = ([&]{ auto __tmp = p; __tmp.push_back((cur + int64_t(1))); return __tmp; }());
        }
        return p;
    };
    std::function<std::string(std::vector<int64_t>)> pathStr = [=](std::vector<int64_t> p) mutable {
        std::string s = std::string("");
        bool first = true;
        int64_t idx = int64_t(0);
        while ((idx < p.size())) {
            int64_t x = p[idx];
            if (!first) {
                s = (s + std::string(" -> "));
            }
            s = (s + ([&]{ std::ostringstream ss; ss << std::boolalpha << x; return ss.str(); }()));
            first = false;
            idx = (idx + int64_t(1));
        }
        return s;
    };
    std::cout << std::string("pair\tdist\tpath");
    std::cout << std::endl;
    int64_t a = int64_t(0);
    while ((a < n)) {
        int64_t b = int64_t(0);
        while ((b < n)) {
            if ((a != b)) {
                std::cout << ((((((([&]{ std::ostringstream ss; ss << std::boolalpha << (a + int64_t(1)); return ss.str(); }()) + std::string(" -> ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << (b + int64_t(1)); return ss.str(); }())) + std::string("\t")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << dist[a][b]; return ss.str(); }())) + std::string("\t")) + pathStr(path((a + int64_t(1)), (b + int64_t(1)))));
                std::cout << std::endl;
            }
            b = (b + int64_t(1));
        }
        a = (a + int64_t(1));
    }
    return 0;
}
