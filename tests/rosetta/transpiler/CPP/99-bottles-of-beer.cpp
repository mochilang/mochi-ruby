// Generated by Mochi transpiler v0.10.40 on 2025-07-25 18:43:19 GMT+7
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <cstdlib>
#include <chrono>
#include <sys/resource.h>


static int _now() {
    static long long seed = 0;
    static bool seeded = false;
    if (!seeded) {
        const char* s = std::getenv("MOCHI_NOW_SEED");
        if (s && *s) { seed = std::atoll(s); seeded = true; }
    }
    if (seeded) {
        seed = (seed * 1664525 + 1013904223) % 2147483647;
        return static_cast<int>(seed);
    }
    return (int)(std::chrono::duration_cast<std::chrono::microseconds>(std::chrono::system_clock::now().time_since_epoch()).count() % 2147483647);
}
static long _mem() {
    struct rusage usage{};
    getrusage(RUSAGE_SELF, &usage);
#ifdef __APPLE__
    return usage.ru_maxrss;
#else
    return usage.ru_maxrss * 1024;
#endif
}

std::string bottles(int n);
int main();

std::string bottles(int n) {
    if ((n == 0)) {
        return std::string("No more bottles");
    }
    if ((n == 1)) {
        return std::string("1 bottle");
    }
    return (([&]{ std::ostringstream ss; ss << std::boolalpha << n; return ss.str(); }()) + std::string(" bottles"));
}

int main() {
    {
        auto __bench_start = _now();
        auto __bench_mem_start = _mem();
        int i = 99;
        while ((i > 0)) {
            std::cout << (bottles(i) + std::string(" of beer on the wall"));
            std::cout << std::endl;
            std::cout << (bottles(i) + std::string(" of beer"));
            std::cout << std::endl;
            std::cout << std::string("Take one down, pass it around");
            std::cout << std::endl;
            std::cout << (bottles((i - 1)) + std::string(" of beer on the wall"));
            std::cout << std::endl;
            i = (i - 1);
        }
        auto __bench_end = _now();
        auto __bench_mem_end = _mem();
        auto __bench_dur = __bench_end - __bench_start;
        auto __bench_mem = __bench_mem_end;
        std::cout << "{\n  \"duration_us\": " << __bench_dur << ",\n  \"memory_bytes\": " << __bench_mem << ",\n  \"name\": \"main\"\n}" << std::endl;
    }
    return 0;
}
