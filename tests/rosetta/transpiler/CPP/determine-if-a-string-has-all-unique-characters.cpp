// Generated by Mochi transpiler v0.10.47 on 2025-07-28 11:47:42 GMT+7
#include <iostream>
#include <string>
#include <vector>
#include <type_traits>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <any>
#include <map>


static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

int64_t indexOf3(std::string s, std::string ch, int64_t start);
int64_t ord(std::string ch);
std::string toHex(int64_t n);
void analyze(std::string s);
int main();

int64_t indexOf3(std::string s, std::string ch, int64_t start) {
    int64_t i = start;
    while ((i < s.size())) {
        if ((s.substr(i, (i + int64_t(1)) - i) == ch)) {
            return i;
        }
        i = (i + int64_t(1));
    }
    return -int64_t(1);
}

int64_t ord(std::string ch) {
    std::string digits = std::string("0123456789");
    int64_t idx = indexOf3(digits, ch, int64_t(0));
    if ((idx >= int64_t(0))) {
        return (int64_t(48) + idx);
    }
    if ((ch == std::string("X"))) {
        return int64_t(88);
    }
    if ((ch == std::string("é"))) {
        return int64_t(233);
    }
    if ((ch == std::string("😍"))) {
        return int64_t(128525);
    }
    if ((ch == std::string("🐡"))) {
        return int64_t(128033);
    }
    return int64_t(0);
}

std::string toHex(int64_t n) {
    std::string digits = std::string("0123456789ABCDEF");
    if ((n == int64_t(0))) {
        return std::string("0");
    }
    int64_t v = n;
    std::string out = std::string("");
    while ((v > int64_t(0))) {
        int64_t d = (v % int64_t(16));
        out = (([&](const auto& __v){ if constexpr(std::is_same_v<std::decay_t<decltype(__v)>, std::string>) return __v.substr(d, (d + int64_t(1)) - d); else return std::vector<typename std::decay_t<decltype(__v)>::value_type>(__v.begin()+d, __v.begin()+(d + int64_t(1))); })(digits) + out);
        v = (v / int64_t(16));
    }
    return out;
}

void analyze(std::string s) {
    int le = s.size();
    std::cout << ((((std::string("Analyzing \"") + s) + std::string("\" which has a length of ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << le; return ss.str(); }())) + std::string(":"));
    std::cout << std::endl;
    if ((le > int64_t(1))) {
        int64_t i = int64_t(0);
        while ((i < (le - int64_t(1)))) {
            int64_t j = (i + int64_t(1));
            while ((j < le)) {
                if ((s.substr(j, (j + int64_t(1)) - j) == s.substr(i, (i + int64_t(1)) - i))) {
                    std::string ch = s.substr(i, (i + int64_t(1)) - i);
                    std::cout << std::string("  Not all characters in the string are unique.");
                    std::cout << std::endl;
                    std::cout << ((((((((std::string("  '") + ch) + std::string("' (0x")) + ([&]{ std::string __s = toHex(ord(ch)); for(auto &__c : __s){ __c = std::tolower(static_cast<unsigned char>(__c)); } return __s; }())) + std::string(") is duplicated at positions ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << (i + int64_t(1)); return ss.str(); }())) + std::string(" and ")) + ([&]{ std::ostringstream ss; ss << std::boolalpha << (j + int64_t(1)); return ss.str(); }())) + std::string(".\n"));
                    std::cout << std::endl;
                    return;
                }
                j = (j + int64_t(1));
            }
            i = (i + int64_t(1));
        }
    }
    std::cout << std::string("  All characters in the string are unique.\n");
    std::cout << std::endl;
}

int main() {
    std::vector<std::string> strings = std::vector<std::string>{std::string(""), std::string("."), std::string("abcABC"), std::string("XYZ ZYX"), std::string("1234567890ABCDEFGHIJKLMN0PQRSTUVWXYZ"), std::string("01234567890ABCDEFGHIJKLMN0PQRSTUVWXYZ0X"), std::string("hétérogénéité"), std::string("🎆🎃🎇🎈"), std::string("😍😀🙌💃😍🙌"), std::string("🐠🐟🐡🦈🐬🐳🐋🐡")};
    int64_t i = int64_t(0);
    while ((i < strings.size())) {
        analyze(strings[i]);
        i = (i + int64_t(1));
    }
    return 0;
}
