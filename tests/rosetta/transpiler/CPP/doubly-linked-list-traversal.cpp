// Generated by Mochi transpiler v0.10.56 on 2025-08-04 11:24:13 GMT+7
#include <iostream>
#include <string>
#include <map>
#include <any>
#include <sstream>
#include <iomanip>
#include <cmath>
#include <optional>
#include <vector>
#include <type_traits>


static void any_to_stream(std::ostream& os, const std::any& val) {
    if(val.type() == typeid(int)) os << std::any_cast<int>(val);
    else if(val.type() == typeid(int64_t)) os << std::any_cast<int64_t>(val);
    else if(val.type() == typeid(double)) os << std::any_cast<double>(val);
    else if(val.type() == typeid(bool)) os << (std::any_cast<bool>(val) ? "true" : "false");
    else if(val.type() == typeid(std::string)) os << std::any_cast<std::string>(val);
    else if(val.type() == typeid(std::vector<int64_t>)) { const auto& v = std::any_cast<const std::vector<int64_t>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::vector<int64_t>>)) { const auto& vv = std::any_cast<const std::vector<std::vector<int64_t>>&>(val); os << '['; for(size_t i=0;i<vv.size();++i){ if(i>0) os << ', '; const auto& v = vv[i]; os << '['; for(size_t j=0;j<v.size();++j){ if(j>0) os << ', '; os << v[j]; } os << ']'; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::string>)) { const auto& v = std::any_cast<const std::vector<std::string>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; os << v[i]; } os << ']'; }
    else if(val.type() == typeid(std::vector<std::any>)) { const auto& v = std::any_cast<const std::vector<std::any>&>(val); os << '['; for(size_t i=0;i<v.size();++i){ if(i>0) os << ', '; any_to_stream(os, v[i]); } os << ']'; }
    else if(val.type() == typeid(std::map<std::string, std::any>)) { const auto& m = std::any_cast<const std::map<std::string, std::any>&>(val); os << '{'; bool first=true; for(const auto& p : m){ if(!first) os << ', '; first=false; os << p.first << ': '; any_to_stream(os, p.second); } os << '}'; }
    else os << "<any>";
}
static double any_to_double(const std::any& v) {
    if(v.type() == typeid(int)) return (double)std::any_cast<int>(v);
    if(v.type() == typeid(double)) return std::any_cast<double>(v);
    return 0;
}
static std::string any_to_string(const std::any& v) {
    if(v.type() == typeid(std::string)) return std::any_cast<std::string>(v);
    if(v.type() == typeid(int)) return std::to_string(std::any_cast<int>(v));
    if(v.type() == typeid(int64_t)) return std::to_string(std::any_cast<int64_t>(v));
    if(v.type() == typeid(double)) { std::ostringstream ss; ss << std::any_cast<double>(v); return ss.str(); }
    if(v.type() == typeid(bool)) return std::any_cast<bool>(v) ? "true" : "false";
    return std::string();
}
template <typename T>
std::ostream& operator<<(std::ostream& os, const std::vector<T>& vec) {
    os << "[";
    for(size_t i=0;i<vec.size();++i){ if(i>0) os << ", "; if constexpr(std::is_same_v<T, std::any>) any_to_stream(os, vec[i]); else os << vec[i]; }
    os << "]";
    return os;
}


template<typename T> std::string _to_string(const T& v) {
    std::ostringstream ss;
    ss << std::boolalpha << v;
    return ss.str();
}

std::string listString();
int main();

std::map<int64_t, std::map<std::string, std::any>> nodes = {};
int64_t head = (int64_t(0) - int64_t(1));
int64_t tail = (int64_t(0) - int64_t(1));
std::string out = std::string("From tail:");
int64_t id = tail;

std::string listString() {
    if ((head == (int64_t(0) - int64_t(1)))) {
        return std::string("<nil>");
    }
    std::string r = (std::string("[") + ([&]{ std::ostringstream ss; any_to_stream(ss, ([&](const auto& __m){ auto __it = __m.find(std::string("value")); return __it != __m.end() ? __it->second : std::any{}; })(([&](const auto& __m){ auto __it = __m.find(head); return __it != __m.end() ? __it->second : std::map<std::string, std::any>{}; })(nodes)));
 return ss.str(); }()));
    int64_t id = std::any_cast<int64_t>(([&](const auto& __m){ auto __it = __m.find(std::string("next")); return __it != __m.end() ? __it->second : std::any{}; })(([&](const auto& __m){ auto __it = __m.find(head); return __it != __m.end() ? __it->second : std::map<std::string, std::any>{}; })(nodes)));
    while ((id != (int64_t(0) - int64_t(1)))) {
        r = ((r + std::string(" ")) + ([&]{ std::ostringstream ss; any_to_stream(ss, ([&](const auto& __m){ auto __it = __m.find(std::string("value")); return __it != __m.end() ? __it->second : std::any{}; })(([&](const auto& __m){ auto __it = __m.find(id); return __it != __m.end() ? __it->second : std::map<std::string, std::any>{}; })(nodes)));
 return ss.str(); }()));
        id = std::any_cast<int64_t>(([&](const auto& __m){ auto __it = __m.find(std::string("next")); return __it != __m.end() ? __it->second : std::any{}; })(([&](const auto& __m){ auto __it = __m.find(id); return __it != __m.end() ? __it->second : std::map<std::string, std::any>{}; })(nodes)));
    }
    r = (r + std::string("]"));
    return r;
}

int main() {
    std::cout << listString();
    std::cout << std::endl;
    nodes[int64_t(0)] = std::map<std::string, std::any>{{"value", std::any(std::string("A"))}, {"next", std::any((int64_t(0) - int64_t(1)))}, {"prev", std::any((int64_t(0) - int64_t(1)))}};
    head = int64_t(0);
    tail = int64_t(0);
    nodes[int64_t(1)] = std::map<std::string, std::any>{{"value", std::any(std::string("B"))}, {"next", std::any((int64_t(0) - int64_t(1)))}, {"prev", std::any(int64_t(0))}};
    nodes[int64_t(0)][std::string("next")] = int64_t(1);
    tail = int64_t(1);
    std::cout << listString();
    std::cout << std::endl;
    nodes[int64_t(2)] = std::map<std::string, std::any>{{"value", std::any(std::string("C"))}, {"next", std::any(int64_t(1))}, {"prev", std::any(int64_t(0))}};
    nodes[int64_t(1)][std::string("prev")] = int64_t(2);
    nodes[int64_t(0)][std::string("next")] = int64_t(2);
    std::cout << listString();
    std::cout << std::endl;
    while ((id != (int64_t(0) - int64_t(1)))) {
        out = ((out + std::string(" ")) + ([&]{ std::ostringstream ss; any_to_stream(ss, ([&](const auto& __m){ auto __it = __m.find(std::string("value")); return __it != __m.end() ? __it->second : std::any{}; })(([&](const auto& __m){ auto __it = __m.find(id); return __it != __m.end() ? __it->second : std::map<std::string, std::any>{}; })(nodes)));
 return ss.str(); }()));
        id = std::any_cast<int64_t>(([&](const auto& __m){ auto __it = __m.find(std::string("prev")); return __it != __m.end() ? __it->second : std::any{}; })(([&](const auto& __m){ auto __it = __m.find(id); return __it != __m.end() ? __it->second : std::map<std::string, std::any>{}; })(nodes)));
    }
    std::cout << out;
    std::cout << std::endl;
    return 0;
}
