-- Generated by Mochi v0.10.42 on 2025-07-27 16:50 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
PI = 3.141592653589793;

function sinApprox(x)
  local term = x
  local sum = x
  local n = 1
  while (n <= 10) do
    local denom = ((2 * n) * ((2 * n) + 1))
    term = ((((-term) * x) * x) / denom)
    sum = (sum + term)
    n = (n + 1)
  end
  return sum
end;

function cosApprox(x)
  local term = 1
  local sum = 1
  local n = 1
  while (n <= 10) do
    local denom = (((2 * n) - 1) * (2 * n))
    term = ((((-term) * x) * x) / denom)
    sum = (sum + term)
    n = (n + 1)
  end
  return sum
end;

function cis(x)
  return {re = cosApprox(x), im = sinApprox(x)}
end;

function add(a, b)
  return {re = ((tonumber(a.re) or 0) + (tonumber(b.re) or 0)), im = ((tonumber(a.im) or 0) + (tonumber(b.im) or 0))}
end;

function cstr(a)
  local s = ("(" .. tostring(a.re))
  if (a.im >= 0) then
    s = (((s .. "+") .. tostring(a.im)) .. "i)")
  else
    s = ((s .. tostring(a.im)) .. "i)")
  end
  return s
end;

function main()
  local result = add((function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(cis(PI)), (function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)({re = 1, im = 0}))
  print(cstr(result))
end;

main();
