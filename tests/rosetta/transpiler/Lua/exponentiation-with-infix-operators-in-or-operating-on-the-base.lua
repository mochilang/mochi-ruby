-- Generated by Mochi v0.10.42 on 2025-07-27 16:50 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function p(x, e)
  local r = 1
  local i = 0
  while (i < math.floor(e)) do
    r = (r * x)
    i = (i + 1)
  end
  return r
end;

function padInt(f)
  local s = tostring(math.floor(f))
  if (f >= 0) then
    return (" " .. s)
  end
  return s
end;

ops = {"-x.p(e)", "-(x).p(e)", "(-x).p(e)", "-(x.p(e))"};

for _, x in ipairs({(-5), 5}) do
  for _, e in ipairs({2, 3}) do
    a = (-p(x, e))
    b = (-p(x, e))
    c = p((-x), e)
    d = (-p(x, e))
    print((((((((((((((((((((("x = " .. tostring((((x < 0)) and ("") or (" ")))) .. tostring(math.floor(x))) .. " e = ") .. tostring(math.floor(e))) .. " | ") .. ops[0 + 1]) .. " = ") .. padInt(a)) .. " | ") .. ops[1 + 1]) .. " = ") .. padInt(b)) .. " | ") .. ops[2 + 1]) .. " = ") .. padInt(c)) .. " | ") .. ops[3 + 1]) .. " = ") .. padInt(d)))
  end
end;
