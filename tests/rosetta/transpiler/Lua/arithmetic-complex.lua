-- Generated by Mochi v0.10.55 on 2025-08-02 17:26 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function add(a, b)
    return {re = ((tonumber(a.re) or 0) + (tonumber(b.re) or 0)), im = ((tonumber(a.im) or 0) + (tonumber(b.im) or 0))}
  end
  function mul(a, b)
    return {re = ((a.re * b.re) - (a.im * b.im)), im = ((tonumber((a.re * b.im)) or 0) + (tonumber((a.im * b.re)) or 0))}
  end
  function neg(a)
    return {re = (-a.re), im = (-a.im)}
  end
  function inv(a)
    local denom = ((tonumber((a.re * a.re)) or 0) + (tonumber((a.im * a.im)) or 0))
    return {re = (a.re // denom), im = ((-a.im) // denom)}
  end
  function conj(a)
    return {re = a.re, im = (-a.im)}
  end
  function cstr(a)
    local s = ("(" .. tostring(a.re))
    if (a.im >= 0) then
      s = (((s .. "+") .. tostring(a.im)) .. "i)")
    else
      s = ((s .. tostring(a.im)) .. "i)")
    end
    return s
  end
  a = {re = 1, im = 1}
  b = {re = 3.14159, im = 1.25}
  print(("a:       " .. cstr(a)))
  print(("b:       " .. cstr(b)))
  print(("a + b:   " .. cstr(add((function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(a), (function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(b)))))
  print(("a * b:   " .. cstr(mul((function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(a), (function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(b)))))
  print(("-a:      " .. cstr(neg((function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(a)))))
  print(("1 / a:   " .. cstr(inv((function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(a)))))
  print(("a̅:       " .. cstr(conj((function(_t) local _c={} for k,v in pairs(_t) do _c[k]=v end return _c end)(a)))))
  local _bench_end = os.clock()
  collectgarbage()
  local _bench_end_mem = collectgarbage('count') * 1024
  local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
  local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
  print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
