-- Generated by Mochi v0.10.55 on 2025-08-02 17:26 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}

local _now_seed = 0
local _now_seeded = false
do
  local s = os.getenv("MOCHI_NOW_SEED")
  if s and s ~= "" then
    local v = tonumber(s)
    if v then
      _now_seed = v
      _now_seeded = true
    end
  end
end
local function _now()
if _now_seeded then
  -- keep the seed within safe integer range for Lua (53 bits)
  _now_seed = (_now_seed * 1664525 + 1013904223) % 9007199254740991
  return _now_seed % 1000000000
end
return os.time() * 1000000000 + math.floor(os.clock() * 1000000000)
end
do
  collectgarbage()
  local _bench_start_mem = collectgarbage('count') * 1024
  local _bench_start = os.clock()
  function d2d(d)
    return (d % 360)
  end
  function g2g(g)
    return (g % 400)
  end
  function m2m(m)
    return (m % 6400)
  end
  function r2r(r)
    return (r % (2 * 3.141592653589793))
  end
  function d2g(d)
    return ((d2d(d) * 400) / 360)
  end
  function d2m(d)
    return ((d2d(d) * 6400) / 360)
  end
  function d2r(d)
    return ((d2d(d) * 3.141592653589793) / 180)
  end
  function g2d(g)
    return ((g2g(g) * 360) / 400)
  end
  function g2m(g)
    return ((g2g(g) * 6400) / 400)
  end
  function g2r(g)
    return ((g2g(g) * 3.141592653589793) / 200)
  end
  function m2d(m)
    return ((m2m(m) * 360) / 6400)
  end
  function m2g(m)
    return ((m2m(m) * 400) / 6400)
  end
  function m2r(m)
    return ((m2m(m) * 3.141592653589793) / 3200)
  end
  function r2d(r)
    return ((r2r(r) * 180) / 3.141592653589793)
  end
  function r2g(r)
    return ((r2r(r) * 200) / 3.141592653589793)
  end
  function r2m(r)
    return ((r2r(r) * 3200) / 3.141592653589793)
  end
  function main()
    local angles = {(-2), (-1), 0, 1, 2, 6.2831853, 16, 57.2957795, 359, 399, 6399, 1e+06}
    print("degrees normalized_degs gradians mils radians")
    for _, a in ipairs(angles) do
      print(((((((((tostring(a) .. " ") .. tostring(d2d(a))) .. " ") .. tostring(d2g(a))) .. " ") .. tostring(d2m(a))) .. " ") .. tostring(d2r(a))))
    end
    print("\ngradians normalized_grds degrees mils radians")
    for _, a in ipairs(angles) do
      print(((((((((tostring(a) .. " ") .. tostring(g2g(a))) .. " ") .. tostring(g2d(a))) .. " ") .. tostring(g2m(a))) .. " ") .. tostring(g2r(a))))
    end
    print("\nmils normalized_mils degrees gradians radians")
    for _, a in ipairs(angles) do
      print(((((((((tostring(a) .. " ") .. tostring(m2m(a))) .. " ") .. tostring(m2d(a))) .. " ") .. tostring(m2g(a))) .. " ") .. tostring(m2r(a))))
    end
    print("\nradians normalized_rads degrees gradians mils")
    for _, a in ipairs(angles) do
      print(((((((((tostring(a) .. " ") .. tostring(r2r(a))) .. " ") .. tostring(r2d(a))) .. " ") .. tostring(r2g(a))) .. " ") .. tostring(r2m(a))))
    end
  end
  main()
  local _bench_end = os.clock()
  collectgarbage()
  local _bench_end_mem = collectgarbage('count') * 1024
  local _bench_duration_us = math.floor((_bench_end - _bench_start) * 1000000)
  local _bench_mem = math.floor(math.max(0, _bench_end_mem - _bench_start_mem))
  print('{\n  "duration_us": ' .. _bench_duration_us .. ',\n  "memory_bytes": ' .. _bench_mem .. ',\n  "name": "main"\n}')
end;
