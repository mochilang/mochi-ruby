-- Generated by Mochi v0.10.42 on 2025-07-27 16:50 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function parseBigInt(str)
  local i = 0
  local neg = false
  if (((function(v)
  if type(v) == 'table' and v.items ~= nil then
    return #v.items
  elseif type(v) == 'table' and (v[1] == nil) then
      local c = 0
      for _ in pairs(v) do c = c + 1 end
      return c
    elseif type(v) == 'string' or type(v) == 'table' then
        return #v
      else
        return 0
      end
    end)(str) > 0) and (string.sub(str, 0 + 1, 1) == "-")) then
      neg = true
      i = 1
    end
    local n = 0
    while (i < (function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' or type(v) == 'table' then
          return #v
        else
          return 0
        end
      end)(str)) do
        local ch = string.sub(str, i + 1, (i + 1))
        local d = math.floor(ch)
        n = ((n * 10) + d)
        i = (i + 1)
      end
      if neg then
        n = (-n)
      end
      return n
    end;
    
    function pad(n, width)
      local s = tostring(n)
      while ((function(v)
      if type(v) == 'table' and v.items ~= nil then
        return #v.items
      elseif type(v) == 'table' and (v[1] == nil) then
          local c = 0
          for _ in pairs(v) do c = c + 1 end
          return c
        elseif type(v) == 'string' or type(v) == 'table' then
            return #v
          else
            return 0
          end
        end)(s) < width) do
          s = (" " .. s)
        end
        return s
      end;
      
      function showInt(n)
        local line = (("Testing integer " .. pad(n, 3)) .. ":  ")
        if ((n % 2) == 0) then
          line = (line .. "even ")
        else
          line = (line .. " odd ")
        end
        if ((n % 2) == 0) then
          line = (line .. "even")
        else
          line = (line .. " odd")
        end
        print(line)
      end;
      
      function showBig(s)
        local b = parseBigInt(s)
        local line = (("Testing big integer " .. tostring(b)) .. ":  ")
        if ((b % 2) == 0) then
          line = (line .. "even")
        else
          line = (line .. "odd")
        end
        print(line)
      end;
      
      function main()
        showInt((-2))
        showInt((-1))
        showInt(0)
        showInt(1)
        showInt(2)
        showBig("-222222222222222222222222222222222222")
        showBig("-1")
        showBig("0")
        showBig("1")
        showBig("222222222222222222222222222222222222")
      end;
      
      main();
