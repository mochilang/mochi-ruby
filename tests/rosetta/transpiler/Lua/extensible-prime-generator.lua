-- Generated by Mochi v0.10.42 on 2025-07-27 16:50 GMT+7
function input()
  return io.read('*l')
end
local _nil = {}
function nextPrime(primes, start)
  local n = start
  while true do
    local isP = true
    local i = 0
    while (i < (function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' or type(v) == 'table' then
          return #v
        else
          return 0
        end
      end)(primes)) do
        local p = primes[i + 1]
        if ((p * p) > n) then
          break
        end
        if ((n % p) == 0) then
          isP = false
          break
        end
        i = (i + 1)
      end
      if isP then
        return n
      end
      n = (n + 2)
    end
  end;
  
  function main()
    local primes = {2}
    local cand = 3
    while ((function(v)
    if type(v) == 'table' and v.items ~= nil then
      return #v.items
    elseif type(v) == 'table' and (v[1] == nil) then
        local c = 0
        for _ in pairs(v) do c = c + 1 end
        return c
      elseif type(v) == 'string' or type(v) == 'table' then
          return #v
        else
          return 0
        end
      end)(primes) < 10000) do
        cand = nextPrime(primes, cand)
        primes = (function(lst, item)
        local res = {table.unpack(lst)}
        table.insert(res, item)
        return res
      end)(primes, cand)
      cand = (cand + 2)
    end
    local line = "First twenty:"
    local i = 0
    while (i < 20) do
      line = ((line .. " ") .. tostring(primes[i + 1]))
      i = (i + 1)
    end
    print(line)
    local idx = 0
    while (primes[idx + 1] <= 100) do
      idx = (idx + 1)
    end
    line = ("Between 100 and 150: " .. tostring(primes[idx + 1]))
    idx = (idx + 1)
    while (primes[idx + 1] < 150) do
      line = ((line .. " ") .. tostring(primes[idx + 1]))
      idx = (idx + 1)
    end
    print(line)
    while (primes[idx + 1] <= 7700) do
      idx = (idx + 1)
    end
    local count = 0
    while (primes[idx + 1] < 8000) do
      count = (count + 1)
      idx = (idx + 1)
    end
    print(("Number beween 7,700 and 8,000: " .. tostring(count)))
    print(("10,000th prime: " .. tostring(primes[9999 + 1])))
  end;
  
  main();
