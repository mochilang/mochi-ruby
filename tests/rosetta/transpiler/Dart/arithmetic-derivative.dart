// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, int start, int end) {
  var n = s.length;
  if (start < 0) start += n;
  if (end < 0) end += n;
  if (start < 0) start = 0;
  if (start > n) start = n;
  if (end < 0) end = 0;
  if (end > n) end = n;
  if (start > end) start = end;
  return s.substring(start, end);
}

String _repeat(String s, int n) => List.filled(n, s).join();

List<int> primeFactors(int n) {
  List<dynamic> factors = <dynamic>[];
  int x = n;
  while (x % 2 == 0) {
    factors = [...factors, 2];
    x = x ~/ 2 as int;
  }
  int p = 3;
  while (p * p <= x) {
    while (x % p == 0) {
    factors = [...factors, p];
    x = x ~/ p as int;
  }
    p = p + 2;
  }
  if (x > 1) {
    factors = [...factors, x];
  }
  return List<int>.from(factors);
}

String repeat(String ch, int n) {
  String s = "";
  int i = 0;
  while (i < n) {
    s = s + ch;
    i = i + 1;
  }
  return s;
}

num D(num n) {
  if (n < 0.0) {
    return -D(-n);
  }
  if (n < 2.0) {
    return 0.0;
  }
  List<dynamic> factors = <dynamic>[];
  if (n < 10000000000000000000.0) {
    factors = primeFactors((n).toInt());
  } else {
    int g = (n / 100.0).toInt();
    factors = primeFactors(g);
    factors = [...factors, 2];
    factors = [...factors, 2];
    factors = [...factors, 5];
    factors = [...factors, 5];
  }
  int c = factors.length;
  if (c == 1) {
    return 1.0;
  }
  if (c == 2) {
    return factors[0] + factors[1] as num;
  }
  num d = n / (factors[0] as num);
  return D(d) * (factors[0] as num) + d;
}

String pad(int n) {
  String s = (n).toString();
  while (s.length < 4) {
    s = " " + s;
  }
  return s;
}

void _main() {
  List<dynamic> vals = <dynamic>[];
  int n = -99;
  while (n < 101) {
    vals = [...vals, (D((n).toDouble())).toInt()];
    n = n + 1;
  }
  int i = 0;
  while (i < vals.length) {
    String line = "";
    int j = 0;
    while (j < 10) {
    line = line + pad(vals[i + j]);
    if (j < 9) {
    line = line + " ";
  }
    j = j + 1;
  }
    print(line);
    i = i + 10;
  }
  num pow = 1.0;
  int m = 1;
  while (m < 21) {
    pow = pow * 10.0;
    String exp = (m).toString();
    if (exp.length < 2) {
    exp = exp + " ";
  }
    String res = (m).toString() + _repeat("0", m - 1);
    print("D(10^" + exp + ") / 7 = " + res);
    m = m + 1;
  }
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
