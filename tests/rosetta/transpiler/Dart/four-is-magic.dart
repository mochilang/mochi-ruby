// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, int start, int end) {
  var n = s.length;
  if (start < 0) start += n;
  if (end < 0) end += n;
  if (start < 0) start = 0;
  if (start > n) start = n;
  if (end < 0) end = 0;
  if (end > n) end = n;
  if (start > end) start = end;
  return s.substring(start, end);
}

String capitalize(String s) {
  if (s.length == 0) {
    return s;
  }
  return _substr(s, 0, 1).toUpperCase() + _substr(s, 1, s.length);
}

List<String> small = ["zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine", "ten", "eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"];
List<String> tens = ["", "", "twenty", "thirty", "forty", "fifty", "sixty", "seventy", "eighty", "ninety"];
List<String> illions = ["", " thousand", " million", " billion", " trillion", " quadrillion", " quintillion"];
String say(int n) {
  String t = "";
  if (n < 0) {
    t = "negative ";
    n = -n;
  }
  if (n < 20) {
    return t + small[n];
  } else {
    if (n < 100) {
    t = tens[n ~/ 10];
    int s = n % 10;
    if (s > 0) {
    t = t + "-" + small[s];
  };
    return t;
  } else {
    if (n < 1000) {
    t = small[n ~/ 100] + " hundred";
    int s = n % 100;
    if (s > 0) {
    t = t + " " + say(s);
  };
    return t;
  };
  };
  }
  String sx = "";
  int i = 0;
  int nn = n;
  while (nn > 0) {
    int p = nn % 1000;
    nn = nn ~/ 1000;
    if (p > 0) {
    String ix = say(p) + illions[i];
    if (sx != "") {
    ix = ix + " " + sx;
  };
    sx = ix;
  }
    i = i + 1;
  }
  return t + sx;
}

String fourIsMagic(int n) {
  String s = say(n);
  s = capitalize(s);
  String t = s;
  while (n != 4) {
    n = s.length;
    s = say(n);
    t = t + " is " + s + ", " + s;
  }
  t = t + " is magic.";
  return t;
}

void _main() {
  List<int> nums = [0, 4, 6, 11, 13, 75, 100, 337, -164, 9223372036854775807];
  for (int n in nums) {
    print(fourIsMagic(n));
  }
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
