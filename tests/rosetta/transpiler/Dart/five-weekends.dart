// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

String _substr(String s, int start, int end) {
  var n = s.length;
  if (start < 0) start += n;
  if (end < 0) end += n;
  if (start < 0) start = 0;
  if (start > n) start = n;
  if (end < 0) end = 0;
  if (end > n) end = n;
  if (start > end) start = end;
  return s.substring(start, end);
}

int weekday(int y, int m, int d) {
  int yy = y;
  int mm = m;
  if (mm < 3) {
    mm = mm + 12;
    yy = yy - 1;
  }
  int k = yy % 100;
  int j = yy ~/ 100 as int;
  int a = 13 * (mm + 1) ~/ 5 as int;
  int b = k ~/ 4 as int;
  int c = j ~/ 4 as int;
  return (d + a + k + b + c + 5 * j) % 7;
}

void _main() {
  List<int> months31 = [1, 3, 5, 7, 8, 10, 12];
  List<String> names = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
  int count = 0;
  int firstY = 0;
  int firstM = 0;
  int lastY = 0;
  int lastM = 0;
  List<int> haveNone = <int>[];
  print("Months with five weekends:");
  for (int year = 1900; year < 2101; year++) {
    bool hasOne = false;
    for (int m in months31) {
    if (weekday(year, m, 1) == 6) {
    print("  " + (year).toString() + " " + names[m - 1]);
    count = count + 1;
    hasOne = true;
    lastY = year;
    lastM = m;
    if (firstY == 0) {
    firstY = year;
    firstM = m;
  };
  }
  }
    if (!hasOne) {
    haveNone = [...haveNone, year];
  }
  }
  print((count).toString() + " total");
  print("");
  print("First five dates of weekends:");
  for (int i = 0; i < 5; i++) {
    num day = 1 + 7 * i;
    print("  Friday, " + names[firstM - 1] + " " + (day).toString() + ", " + (firstY).toString());
  }
  print("Last five dates of weekends:");
  for (int i = 0; i < 5; i++) {
    num day = 1 + 7 * i;
    print("  Friday, " + names[lastM - 1] + " " + (day).toString() + ", " + (lastY).toString());
  }
  print("");
  print("Years with no months with five weekends:");
  for (int y in haveNone) {
    print("  " + (y).toString());
  }
  print((haveNone.length).toString() + " total");
}

void _start() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "_start"}));
}

void main() => _start();
