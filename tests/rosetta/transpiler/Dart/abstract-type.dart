// Generated by Mochi transpiler
import 'dart:convert';
import 'dart:io';

int _nowSeed = 0;
bool _nowSeeded = false;
void _initNow() {
  var s = Platform.environment['MOCHI_NOW_SEED'];
  if (s != null && s.isNotEmpty) {
    var v = int.tryParse(s);
    if (v != null) {
      _nowSeed = v;
      _nowSeeded = true;
    }
  }
}
int _now() {
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647;
    return _nowSeed;
  }
  return DateTime.now().microsecondsSinceEpoch;
}

void main() {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  _initNow();
  {
  var _benchMem0 = ProcessInfo.currentRss;
  var _benchSw = Stopwatch()..start();
  String beastKind(dynamic b) {
  return b["__name"] == "Dog" ? b["kind"] : b["__name"] == "Cat" ? b["kind"] : "";
}
  String beastName(dynamic b) {
  return b["__name"] == "Dog" ? b["name"] : b["__name"] == "Cat" ? b["name"] : "";
}
  String beastCry(dynamic b) {
  return b["__name"] == "Dog" ? "Woof" : b["__name"] == "Cat" ? "Meow" : "";
}
  void bprint(dynamic b) {
  print(beastName(b) + ", who's a " + beastKind(b) + ", cries: \"" + beastCry(b) + "\".");
}
  void main() {
  final d = {"__name": "Dog", "kind": "labrador", "name": "Max"};
  final c = {"__name": "Cat", "kind": "siamese", "name": "Sammy"};
  bprint(d);
  bprint(c);
}
  main();
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
  _benchSw.stop();
  var _benchMem1 = ProcessInfo.currentRss;
  print(jsonEncode({"duration_us": _benchSw.elapsedMicroseconds, "memory_bytes": (_benchMem1 - _benchMem0).abs(), "name": "main"}));
}
