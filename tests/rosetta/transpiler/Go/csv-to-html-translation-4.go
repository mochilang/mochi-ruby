//go:build ignore

// Generated by Mochi v0.10.41 on 2025-07-27 01:56:35 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println("<table>")
		fmt.Println("    <tr><td>Character</td><td>Speech</td></tr>")
		fmt.Println("    <tr><td>The multitude</td><td>The messiah! Show us the messiah!</td></tr>")
		fmt.Println("    <tr><td>Brians mother</td><td>&lt;angry&gt;Now you listen here! He&#39;s not the messiah; he&#39;s a very naughty boy! Now go away!&lt;/angry&gt;</td></tr>")
		fmt.Println("    <tr><td>The multitude</td><td>Who are you?</td></tr>")
		fmt.Println("    <tr><td>Brians mother</td><td>I&#39;m his mother; that&#39;s who!</td></tr>")
		fmt.Println("    <tr><td>The multitude</td><td>Behold his mother! Behold his mother!</td></tr>")
		fmt.Println("</table>")
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
