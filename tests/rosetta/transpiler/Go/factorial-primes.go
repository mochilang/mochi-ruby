//go:build ignore

// Generated by Mochi v0.10.55 on 2025-08-02 17:45:33 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func factorial(n int) int {
	var result int = 1
	_ = result
	var i int = 2
	_ = i
	for i <= n {
		result = (result * i)
		i = (i + 1)
	}
	return result
}

func isPrime(n int) bool {
	if n < 2 {
		return false
	}
	if (n % 2) == 0 {
		return (n == 2)
	}
	var d int = 3
	_ = d
	for (d * d) <= n {
		if (n % d) == 0 {
			return false
		}
		d = (d + 2)
	}
	return true
}

func padLeft(s string, w int) string {
	var out string = s
	_ = out
	for len(out) < w {
		out = (" " + out)
	}
	return out
}

func mochiMain() {
	var n int = 0
	_ = n
	var count int = 0
	_ = count
	for count < 10 {
		n = (n + 1)
		var f int = factorial(n)
		_ = f
		if isPrime((f - 1)) {
			count = (count + 1)
			fmt.Println(((((padLeft(fmt.Sprint(count), 2) + ": ") + padLeft(fmt.Sprint(n), 2)) + "! - 1 = ") + fmt.Sprint((f - 1))))
		}
		if (count < 10) && isPrime((f + 1)) {
			count = (count + 1)
			fmt.Println(((((padLeft(fmt.Sprint(count), 2) + ": ") + padLeft(fmt.Sprint(n), 2)) + "! + 1 = ") + fmt.Sprint((f + 1))))
		}
	}
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
