//go:build ignore

// Generated by Mochi v0.10.54 on 2025-08-02 14:40:34 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"math/big"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

var nPts int

var rMin int

var rMax int

var span *big.Int

var rows [][]string

var r int

var u int

var seen map[string]bool

var min2 *big.Int

var max2 *big.Int

var n int

var i int

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		nPts = 100
		rMin = 10
		rMax = 15
		span = big.NewInt(int64(((rMax + 1) + rMax)))
		rows = [][]string{}
		r = 0
		for r < int(span.Int64()) {
			var row []string = []string{}
			_ = row
			var c int = 0
			_ = c
			for c < int(new(big.Int).Mul(span, big.NewInt(int64(2))).Int64()) {
				row = append(row, " ")
				c = (c + 1)
			}
			rows = append(rows, row)
			r = (r + 1)
		}
		u = 0
		seen = map[string]bool{}
		min2 = big.NewInt(int64((rMin * rMin)))
		max2 = big.NewInt(int64((rMax * rMax)))
		n = 0
		for n < nPts {
			var x *big.Int = new(big.Int).Sub(new(big.Int).Mod(big.NewInt(int64(_now())), span), big.NewInt(int64(rMax)))
			_ = x
			var y *big.Int = new(big.Int).Sub(new(big.Int).Mod(big.NewInt(int64(_now())), span), big.NewInt(int64(rMax)))
			_ = y
			var rs *big.Int = new(big.Int).Add(new(big.Int).Mul(x, x), new(big.Int).Mul(y, y))
			_ = rs
			_ = rs
			if func() bool { return rs.Cmp(min2) < 0 }() || func() bool { return rs.Cmp(max2) > 0 }() {
				continue
			}
			n = (n + 1)
			var row *big.Int = new(big.Int).Add(y, big.NewInt(int64(rMax)))
			_ = row
			var col *big.Int = new(big.Int).Mul(new(big.Int).Add(x, big.NewInt(int64(rMax))), big.NewInt(int64(2)))
			_ = col
			rows[int(row.Int64())][int(col.Int64())] = "*"
			var key string = ((fmt.Sprint(row) + ",") + fmt.Sprint(col))
			_ = key
			if !seen[key] {
				seen[key] = true
				u = (u + 1)
			}
		}
		i = 0
		for i < int(span.Int64()) {
			var line string = ""
			_ = line
			var j int = 0
			_ = j
			for j < int(new(big.Int).Mul(span, big.NewInt(int64(2))).Int64()) {
				line = (line + rows[i][j])
				j = (j + 1)
			}
			fmt.Println(line)
			i = (i + 1)
		}
		fmt.Println((fmt.Sprint(u) + " unique points"))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
