//go:build ignore

// Generated by Mochi v0.10.42 on 2025-07-28 10:44:49 GMT+7
package main

import (
	"fmt"
	os "os"
)

func _substr(s string, start, end int) string {
	r := []rune(s)
	if start < 0 {
		start = 0
	}
	if end > len(r) {
		end = len(r)
	}
	if start > len(r) {
		start = len(r)
	}
	if end < start {
		end = start
	}
	return string(r[start:end])
}

func hasPrefix(s string, p string) bool {
	if len(p) > len(s) {
		return false
	}
	return (_substr(s, 0, len(p)) == p)
}

var name string

var prefix string

func main() {
	name = "SHELL"
	prefix = (name + "=")
	for _, v := range os.Environ() {
		if hasPrefix(v, prefix) {
			fmt.Println(((name + " has value ") + _substr(v, len(prefix), len(v))))
			return
		}
	}
	fmt.Println((name + " not found"))
}
