//go:build ignore

// Generated by Mochi v0.10.52 on 2025-08-02 01:03:27 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func qsel(a []float64, k int) float64 {
	var arr []float64 = a
	_ = arr
	for len(arr) > 1 {
		var px int = (_now() % len(arr))
		_ = px
		var pv float64 = arr[px]
		_ = pv
		var last int = (len(arr) - 1)
		_ = last
		var tmp float64 = arr[px]
		_ = tmp
		arr[px] = arr[last]
		arr[last] = tmp
		px = 0
		var i int = 0
		_ = i
		for i < last {
			var v float64 = arr[i]
			_ = v
			if v < pv {
				var tmp2 float64 = arr[px]
				_ = tmp2
				arr[px] = arr[i]
				arr[i] = tmp2
				px = (px + 1)
			}
			i = (i + 1)
		}
		if px == k {
			return pv
		}
		if k < px {
			arr = arr[0:px]
		} else {
			var tmp2 float64 = arr[px]
			_ = tmp2
			arr[px] = pv
			arr[last] = tmp2
			arr = arr[(px + 1):]
			k = (k - (px + 1))
		}
	}
	return arr[0]
}

func median(list []float64) float64 {
	var arr []float64 = list
	_ = arr
	var half int = int((len(arr) / 2))
	_ = half
	var med float64 = qsel(func(v any) []float64 {
		if v == nil {
			return nil
		}
		if vv, ok := v.([]float64); ok {
			return vv
		}
		if arr, ok := v.([]any); ok {
			if len(arr) == 0 {
				return []float64{}
			}
			out := make([]float64, len(arr))
			for i, x := range arr {
				out[i] = x.(float64)
			}
			return out
		}
		return v.([]float64)
	}(arr), half)
	_ = med
	_ = med
	if (len(arr) % 2) == 0 {
		return ((med + qsel(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}(arr), (half-1))) / 2.0)
	}
	return med
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		fmt.Println(fmt.Sprint(median(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{3.0, 1.0, 4.0, 1.0}))))
		fmt.Println(fmt.Sprint(median(func(v any) []float64 {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]float64); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []float64{}
				}
				out := make([]float64, len(arr))
				for i, x := range arr {
					out[i] = x.(float64)
				}
				return out
			}
			return v.([]float64)
		}([]float64{3.0, 1.0, 4.0, 1.0, 5.0}))))
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
