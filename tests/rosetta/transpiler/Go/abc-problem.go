//go:build ignore

// Generated by Mochi v0.10.52 on 2025-08-01 21:16:35 GMT+7
package main

import (
	"encoding/json"
	"fmt"
	"os"
	"runtime"
	"strconv"
	"strings"
	"time"
)

var seededNow bool
var nowSeed int64

func init() {
	if s := os.Getenv("MOCHI_NOW_SEED"); s != "" {
		if v, err := strconv.ParseInt(s, 10, 64); err == nil {
			nowSeed = v
			seededNow = true
		}
	}
}
func _now() int {
	if seededNow {
		nowSeed = (nowSeed*1664525 + 1013904223) % 2147483647
		return int(nowSeed)
	}
	return int(time.Now().UnixNano())
}

func fields(s string) []string {
	var res []string = []string{}
	_ = res
	var cur string = ""
	_ = cur
	var i int = 0
	_ = i
	for i < len(s) {
		var c string = string([]rune(s)[i:(i + 1)])
		_ = c
		if c == " " {
			if len(cur) > 0 {
				res = append(res, cur)
				cur = ""
			}
		} else {
			cur = (cur + c)
		}
		i = (i + 1)
	}
	if len(cur) > 0 {
		res = append(res, cur)
	}
	return res
}

func canSpell(word string, blks []string) bool {
	if len(word) == 0 {
		return true
	}
	var c string = strings.ToLower(string([]rune(word)[0:1]))
	_ = c
	var i int = 0
	_ = i
	for i < len(blks) {
		var b string = blks[i]
		_ = b
		_ = b
		if (c == strings.ToLower(string([]rune(b)[0:1]))) || (c == strings.ToLower(string([]rune(b)[1:2]))) {
			var rest []string = []string{}
			_ = rest
			var j int = 0
			_ = j
			for j < len(blks) {
				if j != i {
					rest = append(rest, blks[j])
				}
				j = (j + 1)
			}
			if canSpell(string([]rune(word)[1:]), func(v any) []string {
				if v == nil {
					return nil
				}
				if vv, ok := v.([]string); ok {
					return vv
				}
				if arr, ok := v.([]any); ok {
					if len(arr) == 0 {
						return []string{}
					}
					out := make([]string, len(arr))
					for i, x := range arr {
						out[i] = x.(string)
					}
					return out
				}
				return v.([]string)
			}(rest)) {
				return true
			}
		}
		i = (i + 1)
	}
	return false
}

func newSpeller(blocks string) func(string) bool {
	var bl []string = fields(blocks)
	_ = bl
	_ = bl
	return func(w string) bool {
		return canSpell(w, func(v any) []string {
			if v == nil {
				return nil
			}
			if vv, ok := v.([]string); ok {
				return vv
			}
			if arr, ok := v.([]any); ok {
				if len(arr) == 0 {
					return []string{}
				}
				out := make([]string, len(arr))
				for i, x := range arr {
					out[i] = x.(string)
				}
				return out
			}
			return v.([]string)
		}(bl))
	}
}

func mochiMain() {
	sp := newSpeller("BO XK DQ CP NA GT RE TG QD FS JW HU VI AN OB ER FS LY PC ZM")
	_ = sp
	_ = sp
	for _, word := range []string{"A", "BARK", "BOOK", "TREAT", "COMMON", "SQUAD", "CONFUSE"} {
		fmt.Println(((word + " ") + fmt.Sprint(sp(word))))
	}
}

func main() {
	func() {
		var ms runtime.MemStats
		runtime.ReadMemStats(&ms)
		startMem := ms.Alloc
		benchStart := time.Now().UnixNano()
		mochiMain()
		runtime.ReadMemStats(&ms)
		endMem := ms.Alloc
		benchEnd := time.Now().UnixNano()
		data := map[string]any{"name": "main", "duration_us": (benchEnd - benchStart) / 1000, "memory_bytes": endMem - startMem}
		out, _ := json.MarshalIndent(data, "", "  ")
		fmt.Println(string(out))
	}()
}
