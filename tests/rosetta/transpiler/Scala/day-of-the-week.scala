// Generated by Mochi v0.10.57 on 2025-08-04 21:01:36 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
object Main {
  def weekday(y: BigInt, m: BigInt, d: BigInt): BigInt = {
    var yy: BigInt = y
    var mm: BigInt = m
    if (mm < 3) {
      mm = mm + 12
      yy = yy - 1
    }
    val k: BigInt = yy % 100
    val j: BigInt = yy / 100
    val a: BigInt = 13 * (mm + 1) / 5
    val b: BigInt = k / 4
    val c: BigInt = j / 4
    return (d + a + k + b + c + 5 * j) % 7
  }
  
  def main(args: Array[String]): Unit = {
    for (year <- 2008 until 2122) {
      if (weekday(BigInt(year.toString.toDouble.toInt), 12, 25) == 1) {
        println("25 December " + String.valueOf(year) + " is Sunday")
      }
    }
  }
}
