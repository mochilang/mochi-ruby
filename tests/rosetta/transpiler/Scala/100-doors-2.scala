// Generated by Mochi v0.10.40 on 2025-07-26 09:34:38 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
object Main {
  var door: BigInt = BigInt(1)
  
  var incrementer: BigInt = BigInt(0)
  
  def main(args: Array[String]): Unit = {
    for (current <- BigInt(1) until BigInt(101)) {
      var line: String = "Door " + String.valueOf(current) + " "
      if (current == door) {
        line = line + "Open"
        incrementer = (incrementer + BigInt(1)).asInstanceOf[BigInt]
        door = (door + BigInt(2) * incrementer + BigInt(1)).asInstanceOf[BigInt]
      } else {
        line = line + "Closed"
      }
      println(line)
    }
  }
}
