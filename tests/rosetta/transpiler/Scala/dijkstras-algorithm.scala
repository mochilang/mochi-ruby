// Generated by Mochi v0.10.57 on 2025-08-04 22:53:37 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def addEdge(u: String, v: String, w: BigInt): Any = {
  if ((!(graph.contains(u)).asInstanceOf[Boolean]).asInstanceOf[Boolean]) {
    graph.update(u, scala.collection.mutable.Map())
  }
  graph.getOrElse(u.toString, null).update(v, w)
  if ((!(graph.contains(v)).asInstanceOf[Boolean]).asInstanceOf[Boolean]) {
    graph.update(v, scala.collection.mutable.Map())
  }
}

def removeAt(xs: ArrayBuffer[String], idx: BigInt): ArrayBuffer[String] = {
  var out: ArrayBuffer[String] = (ArrayBuffer()).asInstanceOf[ArrayBuffer[String]]
  var i: BigInt = 0
  for (x <- xs) {
    if (i != idx) {
      out = out :+ x
    }
    i = i + 1
  }
  return out
}

def dijkstra(source: String): scala.collection.mutable.Map[String,Any] = {
  var dist: scala.collection.mutable.Map[String,BigInt] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,BigInt]]
  var prev: scala.collection.mutable.Map[String,String] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,String]]
  for (v <- graph.keys) {
    dist.update(v, INF)
    prev.update(v, "")
  }
  dist.update(source, 0)
  var q: ArrayBuffer[String] = (ArrayBuffer()).asInstanceOf[ArrayBuffer[String]]
  for (v <- graph.keys) {
    q = q :+ v.toString
  }
  while (BigInt((q).size) > 0) {
    var bestIdx: BigInt = 0
    var u: String = q((0.toInt).toInt)
    var i: BigInt = 1
    while (i < BigInt((q).size)) {
      val v: String = q((i.toInt).toInt)
      if (dist.getOrElse(v.toString, null).asInstanceOf[BigInt] < dist.getOrElse(u.toString, null).asInstanceOf[BigInt]) {
        u = v
        bestIdx = i
      }
      i = i + 1
    }
    q = removeAt(q, bestIdx)
    for (v <- graph.getOrElse(u.toString, null).asInstanceOf[scala.collection.mutable.Map[String,BigInt]].keys) {
      val alt: BigInt = dist.getOrElse(u.toString, null).asInstanceOf[BigInt] + graph.getOrElse(u.toString, null).asInstanceOf[scala.collection.mutable.Map[String,BigInt]].getOrElse(v.toString, null).asInstanceOf[BigInt]
      if (alt < dist.getOrElse(v.toString, null).asInstanceOf[BigInt]) {
        dist.update(v, alt)
        prev.update(v, u)
      }
    }
  }
  return (scala.collection.mutable.Map("dist" -> (dist), "prev" -> (prev))).asInstanceOf[scala.collection.mutable.Map[String,Any]]
}

def path(prev: scala.collection.mutable.Map[String,String], v: String): String = {
  var s: String = v
  var cur: String = v
  while (prev.getOrElse(cur.toString, "").asInstanceOf[String] != "") {
    cur = prev.getOrElse(cur.toString, "").asInstanceOf[String]
    s = cur + s
  }
  return s
}

def main(): Any = {
  addEdge("a", "b", 7)
  addEdge("a", "c", 9)
  addEdge("a", "f", 14)
  addEdge("b", "c", 10)
  addEdge("b", "d", 15)
  addEdge("c", "d", 11)
  addEdge("c", "f", 2)
  addEdge("d", "e", 6)
  addEdge("e", "f", 9)
  val res: scala.collection.mutable.Map[String,Any] = dijkstra("a")
  val dist: scala.collection.mutable.Map[String,BigInt] = (res.getOrElse("dist", null)).asInstanceOf[scala.collection.mutable.Map[String,BigInt]]
  val prev: scala.collection.mutable.Map[String,String] = (res.getOrElse("prev", null)).asInstanceOf[scala.collection.mutable.Map[String,String]]
  println("Distance to e: " + String.valueOf(dist.getOrElse("e", null).asInstanceOf[BigInt]) + ", Path: " + path(prev, "e"))
  println("Distance to f: " + String.valueOf(dist.getOrElse("f", null).asInstanceOf[BigInt]) + ", Path: " + path(prev, "f"))
}

val INF: BigInt = 1000000000

var graph: scala.collection.mutable.Map[String,scala.collection.mutable.Map[String,BigInt]] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,scala.collection.mutable.Map[String,BigInt]]]

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
