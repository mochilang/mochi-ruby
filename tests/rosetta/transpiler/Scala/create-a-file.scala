// Generated by Mochi v0.10.57 on 2025-08-04 17:09:38 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
object Main {
  def createFile(fs: scala.collection.mutable.Map[String,Boolean], fn: String): Any = {
    if ((fs.contains(fn)).asInstanceOf[Boolean]) {
      println("open " + fn + ": file exists")
    } else {
      fs.update(fn, false)
      println("file " + fn + " created!")
    }
  }
  
  def createDir(fs: scala.collection.mutable.Map[String,Boolean], dn: String): Any = {
    if ((fs.contains(dn)).asInstanceOf[Boolean]) {
      println("mkdir " + dn + ": file exists")
    } else {
      fs.update(dn, true)
      println("directory " + dn + " created!")
    }
  }
  
  def main(): Any = {
    var fs: scala.collection.mutable.Map[String,Boolean] = (scala.collection.mutable.Map()).asInstanceOf[scala.collection.mutable.Map[String,Boolean]]
    fs.update("docs", true)
    createFile(fs, "input.txt")
    createFile(fs, "/input.txt")
    createDir(fs, "docs")
    createDir(fs, "/docs")
  }
  
  def main(args: Array[String]): Unit = {
    main()
  }
}
