// Generated by Mochi v0.10.56 on 2025-08-04 00:19:53 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def qsel(a: ArrayBuffer[Double], _k: BigInt): Double = {
  var k: BigInt = _k
  var arr: ArrayBuffer[Double] = a
  while (BigInt(((arr).size).toInt) > BigInt(1)) {
    var px: BigInt = _now() % (arr).size
    var pv: Double = arr((px.toInt).toInt)
    val last: BigInt = (arr).size - BigInt(1).toInt
    val tmp: Double = arr((px.toInt).toInt)
    arr((px).toInt) = arr((last.toInt).toInt)
    arr((last).toInt) = tmp
    px = BigInt(0)
    var i: BigInt = BigInt(0)
    while (i < last) {
      val v: Double = arr((i.toInt).toInt)
      if (v < pv) {
        val t: Double = arr((px.toInt).toInt)
        arr((px).toInt) = arr((i.toInt).toInt)
        arr((i).toInt) = t
        px = px + BigInt(1)
      }
      i = i + BigInt(1)
    }
    arr((px).toInt) = pv
    if (px == k) {
      return pv
    }
    if (k < px) {
      arr = arr.slice((BigInt(0)).toInt, (px).toInt)
    } else {
      arr = arr.slice((px + BigInt(1)).toInt, ((arr).size).toInt)
      k = k - (px + BigInt(1))
    }
  }
  return arr((BigInt(0).toInt).toInt)
}

def fivenum(a: ArrayBuffer[Double]): ArrayBuffer[Double] = {
  val last: BigInt = (a).size - BigInt(1).toInt
  val m: BigInt = last / BigInt(2)
  var n5: ArrayBuffer[Double] = ArrayBuffer()
  n5 = n5 :+ qsel(a.slice((BigInt(0)).toInt, (m).toInt), BigInt(0))
  n5 = n5 :+ qsel(a.slice((BigInt(0)).toInt, (m).toInt), (a).size / BigInt(4).toInt)
  n5 = n5 :+ qsel(a, m)
  var arr2: ArrayBuffer[Double] = a.slice((m).toInt, ((a).size).toInt)
  val q3: BigInt = last - m - (a).size / BigInt(4).toInt
  n5 = n5 :+ qsel(arr2, q3)
  arr2 = arr2.slice((q3).toInt, ((arr2).size).toInt)
  n5 = n5 :+ qsel(arr2, (arr2).size - BigInt(1).toInt)
  return n5
}

val x1: ArrayBuffer[Double] = ArrayBuffer(36.0, 40.0, 7.0, 39.0, 41.0, 15.0)

val x2: ArrayBuffer[Double] = ArrayBuffer(15.0, 6.0, 42.0, 41.0, 7.0, 36.0, 49.0, 40.0, 39.0, 47.0, 43.0)

val x3: ArrayBuffer[Any] = ArrayBuffer(0.14082834, 0.0974879, 1.73131507, 0.87636009, -1.95059594, 0.73438555, -0.03035726, 1.4667597, -0.74621349, -0.72588772, 0.6390516, 0.61501527, -0.9898378, -1.00447874, -0.62759469, 0.66206163, 1.04312009, -0.10305385, 0.75775634, 0.32566578)

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    println(String.valueOf(fivenum(x1)))
    println(String.valueOf(fivenum(x2)))
    println(String.valueOf(fivenum(x3.asInstanceOf[ArrayBuffer[Double]])))
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
