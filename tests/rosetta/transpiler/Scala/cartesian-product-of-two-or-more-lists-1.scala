// Generated by Mochi v0.10.40 on 2025-07-26 09:47:45 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    def cart2(a: ArrayBuffer[BigInt], b: ArrayBuffer[BigInt]): ArrayBuffer[ArrayBuffer[BigInt]] = {
      var p: ArrayBuffer[ArrayBuffer[BigInt]] = ArrayBuffer()
      for (x <- a) {
        for (y <- b) {
          p = p :+ (ArrayBuffer(x, y)).asInstanceOf[ArrayBuffer[BigInt]]
        }
      }
      return p
    }
    def llStr(lst: ArrayBuffer[ArrayBuffer[BigInt]]): String = {
      var s: String = "["
      var i: BigInt = BigInt(0)
      while (i < (lst).size) {
        var row: ArrayBuffer[BigInt] = lst((i).toInt)
        s = s + "["
        var j: BigInt = BigInt(0)
        while (j < (row).size) {
          s = (s + String.valueOf(row((j).toInt))).toString
          if (j < (row).size - BigInt(1)) {
            s = s + " "
          }
          j = (j + BigInt(1)).asInstanceOf[BigInt]
        }
        s = s + "]"
        if (i < (lst).size - BigInt(1)) {
          s = s + " "
        }
        i = (i + BigInt(1)).asInstanceOf[BigInt]
      }
      s = s + "]"
      return s
    }
    def main(): Any = {
      println(llStr(cart2(ArrayBuffer(BigInt(1), BigInt(2)), ArrayBuffer(BigInt(3), BigInt(4)))))
      println(llStr(cart2(ArrayBuffer(BigInt(3), BigInt(4)), ArrayBuffer(BigInt(1), BigInt(2)))))
      println(llStr(cart2(ArrayBuffer(BigInt(1), BigInt(2)), (ArrayBuffer()).asInstanceOf[ArrayBuffer[BigInt]])))
      println(llStr(cart2((ArrayBuffer()).asInstanceOf[ArrayBuffer[BigInt]], ArrayBuffer(BigInt(1), BigInt(2)))))
    }
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
