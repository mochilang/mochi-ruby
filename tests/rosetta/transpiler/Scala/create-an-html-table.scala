// Generated by Mochi v0.10.57 on 2025-08-04 17:18:30 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
object Main {
  def main(): Any = {
    var rows: ArrayBuffer[ArrayBuffer[BigInt]] = (ArrayBuffer()).asInstanceOf[ArrayBuffer[ArrayBuffer[BigInt]]]
    for (i <- 0 until 4) {
      rows = rows :+ (ArrayBuffer(i * 3, i * 3 + 1, i * 3 + 2)).asInstanceOf[ArrayBuffer[BigInt]]
    }
    println("<table>")
    println("    <tr><th></th><th>X</th><th>Y</th><th>Z</th></tr>")
    var idx: BigInt = 0
    for (row <- rows) {
      println("    <tr><td>" + String.valueOf(idx) + "</td><td>" + String.valueOf(row((0.toInt).toInt)) + "</td><td>" + String.valueOf(row((1.toInt).toInt)) + "</td><td>" + String.valueOf(row((2.toInt).toInt)) + "</td></tr>")
      idx = idx + 1
    }
    println("</table>")
  }
  
  def main(args: Array[String]): Unit = {
    main()
  }
}
