// Generated by Mochi v0.10.52 on 2025-08-02 02:08:38 GMT+7
import scala.collection.mutable.{ArrayBuffer, Map}
import scala.math.BigInt
import scala.collection.immutable.ListMap
import scala.annotation.tailrec
object Main {
  private var _nowSeed: Long = 0L
  private var _nowSeeded: Boolean = false
  private def _now(): Int = {
    if (!_nowSeeded) {
      sys.env.get("MOCHI_NOW_SEED").foreach { s =>
      try { _nowSeed = s.toInt; _nowSeeded = true } catch { case _ : NumberFormatException => () }
    }
  }
  if (_nowSeeded) {
    _nowSeed = (_nowSeed * 1664525 + 1013904223) % 2147483647
    _nowSeed.toInt
  } else {
    Math.abs((System.nanoTime() / 1000).toInt)
  }
}

class BigRat(var num: BigInt, var den: BigInt) {
  def +(o: BigRat) = BigRat(num * o.den + o.num * den, den * o.den)
  def -(o: BigRat) = BigRat(num * o.den - o.num * den, den * o.den)
  def *(o: BigRat) = BigRat(num * o.num, den * o.den)
  def /(o: BigRat) = BigRat(num * o.den, den * o.num)
  override def toString(): String = s"${num}/${den}"
}
object BigRat {
  def apply(n: BigInt, d: BigInt = BigInt(1)): BigRat = {
    val g = n.gcd(d); var nn = n / g; var dd = d / g; if (dd < 0) { nn = -nn; dd = -dd }
    new BigRat(nn, dd)
  }
}
def _bigrat(n: BigInt, d: BigInt = BigInt(1)) = BigRat(n, d)
def _bigrat(r: BigRat): BigRat = r
def num(r: BigRat): BigInt = r.num
def denom(r: BigRat): BigInt = r.den

def toJson(value: Any, indent: Int = 0): String = value match {
  case m: scala.collection.Map[_, _] =>
  val items = ListMap(m.toSeq.sortBy(_._1.toString): _*).toSeq.map{ case (k,v) => "  "*(indent+1)+"\""+k.toString+"\": "+toJson(v, indent+1) }
  "{\n"+items.mkString(",\n")+"\n"+"  "*indent+"}"
  case s: Seq[_] =>
  val items = s.map(x => "  "*(indent+1)+toJson(x, indent+1))
  "[\n"+items.mkString(",\n")+"\n"+"  "*indent+"]"
  case s: String => "\""+s+"\""
  case other => other.toString
}

private def _padStart(s: String, width: Int, pad: String): String = {
  var out = s
  while (out.length < width) { out = pad + out }
  out
}

def bernoulli(n: BigInt): BigRat = {
  var a: ArrayBuffer[BigRat] = ArrayBuffer()
  var m: BigInt = BigInt(0)
  while (m <= n) {
    a = a :+ (_bigrat(BigInt(1)) / _bigrat(m + BigInt(1))).asInstanceOf[BigRat]
    var j: BigInt = m
    while (j >= BigInt(1)) {
      a((j - BigInt(1)).toInt) = (_bigrat(j) * (a((j - BigInt(1)).toInt) - a((j).toInt))).asInstanceOf[BigRat]
      j = j - BigInt(1)
    }
    m = m + BigInt(1)
  }
  if (n != BigInt(1)) {
    return a((BigInt(0)).toInt)
  }
  return (_bigrat(BigInt(0)) - a((BigInt(0)).toInt)).asInstanceOf[BigRat]
}

def binom(n: BigInt, k: BigInt): BigInt = {
  if ((k < BigInt(0) || k > n).asInstanceOf[Boolean]) {
    return BigInt(0)
  }
  var kk: BigInt = k
  if (kk > n - kk) {
    kk = n - kk
  }
  var res: BigInt = BigInt(1)
  var i: BigInt = BigInt(0)
  while (i < kk) {
    res = res * BigInt((n - i).toInt)
    i = i + BigInt(1)
    res = res / BigInt(i.toInt)
  }
  return res
}

def faulhaberRow(p: BigInt): ArrayBuffer[BigRat] = {
  var coeffs: ArrayBuffer[BigRat] = ArrayBuffer()
  var i: BigInt = BigInt(0)
  while (i <= p) {
    coeffs = coeffs :+ _bigrat(BigInt(0))
    i = i + BigInt(1)
  }
  var j: BigInt = BigInt(0)
  var sign: BigInt = BigInt(0) - BigInt(1)
  while (j <= p) {
    sign = BigInt(0) - sign
    var c: BigRat = _bigrat(BigInt(1)) / _bigrat(p + BigInt(1))
    if (sign < BigInt(0)) {
      c = (_bigrat(BigInt(0)) - c).asInstanceOf[BigRat]
    }
    c = (c * _bigrat(binom(p + BigInt(1), j))).asInstanceOf[BigRat]
    c = (c * bernoulli(j)).asInstanceOf[BigRat]
    coeffs((p.toString.toDouble - j.toString.toDouble).toInt) = c
    j = j + BigInt(1)
  }
  return coeffs
}

def ratStr(r: BigRat): String = {
  val s: String = String.valueOf(r)
  if (endsWith(s, "/1")) {
    return s.slice((BigInt(0)).toInt, ((s).size - BigInt(2).toInt).toInt)
  }
  return s
}

def endsWith(s: String, suf: String): Boolean = {
  if ((s).size < (suf).size) {
    return false
  }
  return s.slice(((s).size - (suf).size).toInt, ((s).size).toInt) == suf
}

def main(): Any = {
  var p: BigInt = BigInt(0)
  while (p < BigInt(10)) {
    val row: ArrayBuffer[BigRat] = faulhaberRow(p)
    var line: String = ""
    var idx: BigInt = BigInt(0)
    while (idx < (row).size) {
      line = (line + _padStart(ratStr(row((idx).toInt)), BigInt(5).toInt, " ")).toString
      if (idx < (row).size - BigInt(1).toInt) {
        line = line + "  "
      }
      idx = idx + BigInt(1)
    }
    println(line)
    p = p + BigInt(1)
  }
  println("")
  val k: BigInt = BigInt(17)
  var coeffs: ArrayBuffer[BigRat] = faulhaberRow(k)
  var nn: BigRat = _bigrat(BigInt(1000))
  var np: BigRat = _bigrat(BigInt(1))
  var sum: BigRat = _bigrat(BigInt(0))
  var i: BigInt = BigInt(0)
  while (i < (coeffs).size) {
    np = (np * nn).asInstanceOf[BigRat]
    sum = (sum + coeffs((i).toInt) * np).asInstanceOf[BigRat]
    i = i + BigInt(1)
  }
  println(ratStr(sum))
}

def main(args: Array[String]): Unit = {
  {
    System.gc()
    val _startMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _start = _now()
    main()
    val _end = _now()
    System.gc()
    val _endMem = Runtime.getRuntime.totalMemory() - Runtime.getRuntime.freeMemory()
    val _durUs = (_end - _start) / 1000
    var _memDiff = _endMem - _startMem
    if (_memDiff <= 0) _memDiff = _endMem
    println(toJson(scala.collection.immutable.Map("duration_us" -> _durUs, "memory_bytes" -> _memDiff, "name" -> "main")))
  }
}
}
