;; Generated on 2025-08-04 22:39 +0700
(import (scheme base))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (srfi 69))
(import (srfi 1))
(define _list list)
(import (chibi time))
(define (_mem) (* 1024 (resource-usage-max-rss (get-resource-usage resource-usage/self))))
(import (chibi json))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_substring s start end)
  (let* ((len (string-length s))
         (s0 (max 0 (min len start)))
         (e0 (max s0 (min len end))))
    (substring s s0 e0)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (slice seq start end)
  (let* ((len (if (string? seq) (string-length seq) (length seq)))
         (s (if (< start 0) (+ len start) start))
         (e (if (< end 0) (+ len end) end)))
    (set! s (max 0 (min len s)))
    (set! e (max 0 (min len e)))
    (when (< e s) (set! e s))
    (if (string? seq)
        (_substring seq s e)
        (take (drop seq s) (- e s)))))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(define (_split s sep)
  (let* ((str (if (string? s) s (list->string s)))
         (del (cond ((char? sep) sep)
                     ((string? sep) (if (= (string-length sep) 1)
                                       (string-ref sep 0)
                                       sep))
                     (else sep))))
    (cond
     ((and (string? del) (string=? del ""))
      (map string (string->list str)))
     ((char? del)
      (string-split str del))
     (else
        (let loop ((r str) (acc '()))
          (let ((cur (string-contains r del)))
            (if cur
                (let ((idx (string-cursor->index r cur)))
                  (loop (_substring r (+ idx (string-length del)) (string-length r))
                        (cons (_substring r 0 idx) acc)))
                (reverse (cons r acc)))))))))
(define (_len x)
  (cond ((string? x) (string-length x))
        ((hash-table? x) (hash-table-size x))
        (else (length x))))
(let ((start11 (current-jiffy)) (jps14 (jiffies-per-second))) (begin (let ((serverData (alist->hash-table (_list (cons "pub" (alist->hash-table (_list (cons "somefile.bin" "This is a file from the FTP server.") (cons "readme.txt" "Hello from ftp.")))))))) (begin (let ((serverNames (alist->hash-table (_list (cons "pub" (_list "somefile.bin" "readme.txt")))))) (begin (define (connect hostport) (call/cc (lambda (ret1) (begin (_display (to-str (string-append "Connected to " hostport))) (newline) (ret1 (alist->hash-table (_list (cons "dir" "/")))))))) (define (login conn user pass) (call/cc (lambda (ret2) (begin (_display (to-str (string-append "Logged in as " user))) (newline))))) (define (changeDir conn dir) (call/cc (lambda (ret3) (hash-table-set! conn "dir" dir)))) (define (list conn) (call/cc (lambda (ret4) (let ((names (cond ((string? serverNames) (_substring serverNames (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverNames) (hash-table-ref serverNames (hash-table-ref conn "dir"))) (else (list-ref serverNames (hash-table-ref conn "dir")))))) (begin (let ((dataDir (cond ((string? serverData) (_substring serverData (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverData) (hash-table-ref serverData (hash-table-ref conn "dir"))) (else (list-ref serverData (hash-table-ref conn "dir")))))) (begin (let ((out (_list))) (begin (call/cc (lambda (break6) (letrec ((loop5 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((name (car xs))) (begin (let ((content (cond ((string? dataDir) (_substring dataDir name (+ name 1))) ((hash-table? dataDir) (hash-table-ref dataDir name)) (else (list-ref dataDir name))))) (begin (set! out (append out (_list (alist->hash-table (_list (cons "name" name) (cons "size" (_len content)) (cons "kind" "file")))))))))) (loop5 (cdr xs))))))) (loop5 names)))) (ret4 out)))))))))) (define (retrieve conn name) (call/cc (lambda (ret7) (ret7 (cond ((string? (cond ((string? serverData) (_substring serverData (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverData) (hash-table-ref serverData (hash-table-ref conn "dir"))) (else (list-ref serverData (hash-table-ref conn "dir"))))) (_substring (cond ((string? serverData) (_substring serverData (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverData) (hash-table-ref serverData (hash-table-ref conn "dir"))) (else (list-ref serverData (hash-table-ref conn "dir")))) name (+ name 1))) ((hash-table? (cond ((string? serverData) (_substring serverData (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverData) (hash-table-ref serverData (hash-table-ref conn "dir"))) (else (list-ref serverData (hash-table-ref conn "dir"))))) (hash-table-ref (cond ((string? serverData) (_substring serverData (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverData) (hash-table-ref serverData (hash-table-ref conn "dir"))) (else (list-ref serverData (hash-table-ref conn "dir")))) name)) (else (list-ref (cond ((string? serverData) (_substring serverData (hash-table-ref conn "dir") (+ (hash-table-ref conn "dir") 1))) ((hash-table? serverData) (hash-table-ref serverData (hash-table-ref conn "dir"))) (else (list-ref serverData (hash-table-ref conn "dir")))) name))))))) (define (main) (call/cc (lambda (ret8) (let ((conn (connect "localhost:21"))) (begin (login conn "anonymous" "anonymous") (changeDir conn "pub") (_display (to-str (hash-table-ref conn "dir"))) (newline) (let ((files (list conn))) (begin (call/cc (lambda (break10) (letrec ((loop9 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((f (car xs))) (begin (_display (to-str (string-append (string-append (hash-table-ref f "name") " ") (to-str (hash-table-ref f "size"))))) (newline))) (loop9 (cdr xs))))))) (loop9 files)))) (let ((data (retrieve conn "somefile.bin"))) (begin (_display (to-str (string-append (string-append "Wrote " (to-str (_len data))) " bytes to somefile.bin"))) (newline)))))))))) (main))))) (let ((end12 (current-jiffy))) (let ((dur13 (quotient (* (- end12 start11) 1000000) jps14))) (begin (_display (string-append "{\n  \"duration_us\": " (number->string dur13) ",\n  \"memory_bytes\": " (number->string (_mem)) ",\n  \"name\": \"main\"\n}")) (newline))))))
