;; Generated on 2025-07-28 10:03 +0700
(import (only (scheme base) call/cc when list-ref list-set!))
(import (rename (scheme base) (list _list)))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (scheme write))
(import (srfi 69))
(import (chibi time) (srfi 98))
(define _now_seeded #f)
(define _now_seed 0)
(define (now)
  (when (not _now_seeded)
    (let ((s (get-environment-variable "MOCHI_NOW_SEED")))
      (when (and s (string->number s))
        (set! _now_seed (string->number s))
        (set! _now_seeded #t))))
  (if _now_seeded
      (begin
        (set! _now_seed (modulo (+ (* _now_seed 1664525) 1013904223) 2147483647))
        _now_seed)
      (exact (floor (* (current-second) 1000000000))))
)
(define (_mem) (* 1024 (resource-usage-max-rss (get-resource-usage resource-usage/self))))
(import (chibi json))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(let ((start18 (now)
)
)
 (begin (define (powInt base exp)
 (call/cc (lambda (ret1)
 (let ((r 1)
)
 (begin (let ((b base)
)
 (begin (let ((e exp)
)
 (begin (call/cc (lambda (break3)
 (letrec ((loop2 (lambda ()
 (if (> e 0)
 (begin (if (equal? (modulo e 2)
 1)
 (begin (set! r (* r b)
)
)
 (quote ()
)
)
 (set! b (* b b)
)
 (set! e (quotient e (let ((v4 2)
)
 (cond ((string? v4)
 (exact (floor (string->number v4)
)
)
)
 ((boolean? v4)
 (if v4 1 0)
)
 (else (exact (floor v4)
)
)
)
)
)
)
 (loop2)
)
 (quote ()
)
)
)
)
)
 (loop2)
)
)
)
 (ret1 r)
)
)
)
)
)
)
)
)
)
 (define (minInt x y)
 (call/cc (lambda (ret5)
 (begin (if (< x y)
 (begin (ret5 x)
)
 (quote ()
)
)
 (ret5 y)
)
)
)
)
 (define (throwDie nSides nDice s counts)
 (call/cc (lambda (ret6)
 (begin (if (equal? nDice 0)
 (begin (list-set! counts s (+ (list-ref counts s)
 1)
)
 (ret6 (quote ()
)
)
)
 (quote ()
)
)
 (let ((i 1)
)
 (begin (call/cc (lambda (break8)
 (letrec ((loop7 (lambda ()
 (if (<= i nSides)
 (begin (throwDie nSides (- nDice 1)
 (+ s i)
 counts)
 (set! i (+ i 1)
)
 (loop7)
)
 (quote ()
)
)
)
)
)
 (loop7)
)
)
)
)
)
)
)
)
)
 (define (beatingProbability nSides1 nDice1 nSides2 nDice2)
 (call/cc (lambda (ret9)
 (let ((len1 (* (+ nSides1 1)
 nDice1)
)
)
 (begin (let ((c1 (_list)
)
)
 (begin (let ((i 0)
)
 (begin (call/cc (lambda (break11)
 (letrec ((loop10 (lambda ()
 (if (< i len1)
 (begin (set! c1 (append c1 (_list 0)
)
)
 (set! i (+ i 1)
)
 (loop10)
)
 (quote ()
)
)
)
)
)
 (loop10)
)
)
)
 (throwDie nSides1 nDice1 0 c1)
 (let ((len2 (* (+ nSides2 1)
 nDice2)
)
)
 (begin (let ((c2 (_list)
)
)
 (begin (let ((j 0)
)
 (begin (call/cc (lambda (break13)
 (letrec ((loop12 (lambda ()
 (if (< j len2)
 (begin (set! c2 (append c2 (_list 0)
)
)
 (set! j (+ j 1)
)
 (loop12)
)
 (quote ()
)
)
)
)
)
 (loop12)
)
)
)
 (throwDie nSides2 nDice2 0 c2)
 (let ((p12 (* (powInt nSides1 nDice1)
 (powInt nSides2 nDice2)
)
)
)
 (begin (let ((tot 0.0)
)
 (begin (set! i 0)
 (call/cc (lambda (break15)
 (letrec ((loop14 (lambda ()
 (if (< i len1)
 (begin (set! j 0)
 (let ((m (minInt i len2)
)
)
 (begin (call/cc (lambda (break17)
 (letrec ((loop16 (lambda ()
 (if (_lt j m)
 (begin (set! tot (_add tot (/ (* (list-ref c1 i)
 (list-ref c2 j)
)
 p12)
)
)
 (set! j (+ j 1)
)
 (loop16)
)
 (quote ()
)
)
)
)
)
 (loop16)
)
)
)
 (set! i (+ i 1)
)
)
)
 (loop14)
)
 (quote ()
)
)
)
)
)
 (loop14)
)
)
)
 (ret9 tot)
)
)
)
)
)
)
)
)
)
)
)
)
)
)
)
)
)
)
)
 (_display (to-str (to-str (beatingProbability 4 9 6 6)
)
)
)
 (newline)
 (_display (to-str (to-str (beatingProbability 10 5 7 6)
)
)
)
 (newline)
 (let ((end19 (now)
)
)
 (let ((dur20 (quotient (- end19 start18)
 1000)
)
)
 (begin (_display (string-append "{\n  \"duration_us\": " (number->string dur20)
 ",\n  \"memory_bytes\": " (number->string (_mem)
)
 ",\n  \"name\": \"main\"\n}")
)
 (newline)
)
)
)
)
)
