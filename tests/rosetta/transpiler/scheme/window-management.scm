;; Generated on 2025-08-04 22:39 +0700
(import (scheme base))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (srfi 69))
(import (srfi 1))
(define _list list)
(import (chibi time))
(define (_mem) (* 1024 (resource-usage-max-rss (get-resource-usage resource-usage/self))))
(import (chibi json))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_substring s start end)
  (let* ((len (string-length s))
         (s0 (max 0 (min len start)))
         (e0 (max s0 (min len end))))
    (substring s s0 e0)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (slice seq start end)
  (let* ((len (if (string? seq) (string-length seq) (length seq)))
         (s (if (< start 0) (+ len start) start))
         (e (if (< end 0) (+ len end) end)))
    (set! s (max 0 (min len s)))
    (set! e (max 0 (min len e)))
    (when (< e s) (set! e s))
    (if (string? seq)
        (_substring seq s e)
        (take (drop seq s) (- e s)))))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(define (_split s sep)
  (let* ((str (if (string? s) s (list->string s)))
         (del (cond ((char? sep) sep)
                     ((string? sep) (if (= (string-length sep) 1)
                                       (string-ref sep 0)
                                       sep))
                     (else sep))))
    (cond
     ((and (string? del) (string=? del ""))
      (map string (string->list str)))
     ((char? del)
      (string-split str del))
     (else
        (let loop ((r str) (acc '()))
          (let ((cur (string-contains r del)))
            (if cur
                (let ((idx (string-cursor->index r cur)))
                  (loop (_substring r (+ idx (string-length del)) (string-length r))
                        (cons (_substring r 0 idx) acc)))
                (reverse (cons r acc)))))))))
(define (_len x)
  (cond ((string? x) (string-length x))
        ((hash-table? x) (hash-table-size x))
        (else (length x))))
(let ((start10 (current-jiffy)) (jps13 (jiffies-per-second))) (begin (define (showState w label) (call/cc (lambda (ret1) (begin (_display (to-str (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append label ": pos=(") (to-str (hash-table-ref w "x"))) ",") (to-str (hash-table-ref w "y"))) ") size=(") (to-str (hash-table-ref w "w"))) "x") (to-str (hash-table-ref w "h"))) ") max=") (to-str (hash-table-ref w "maximized"))) " icon=") (to-str (hash-table-ref w "iconified"))) " visible=") (to-str (hash-table-ref w "visible"))))) (newline))))) (define (maximize w) (call/cc (lambda (ret2) (begin (hash-table-set! w "maximized" #t) (hash-table-set! w "w" 800) (hash-table-set! w "h" 600) (ret2 w))))) (define (unmaximize w) (call/cc (lambda (ret3) (begin (hash-table-set! w "maximized" #f) (hash-table-set! w "w" 640) (hash-table-set! w "h" 480) (ret3 w))))) (define (iconify w) (call/cc (lambda (ret4) (begin (hash-table-set! w "iconified" #t) (hash-table-set! w "visible" #f) (ret4 w))))) (define (deiconify w) (call/cc (lambda (ret5) (begin (hash-table-set! w "iconified" #f) (hash-table-set! w "visible" #t) (ret5 w))))) (define (hide w) (call/cc (lambda (ret6) (begin (hash-table-set! w "visible" #f) (ret6 w))))) (define (showWindow w) (call/cc (lambda (ret7) (begin (hash-table-set! w "visible" #t) (ret7 w))))) (define (move w) (call/cc (lambda (ret8) (begin (if (hash-table-ref w "shifted") (begin (hash-table-set! w "x" (- (hash-table-ref w "x") 10)) (hash-table-set! w "y" (- (hash-table-ref w "y") 10))) (begin (hash-table-set! w "x" (+ (hash-table-ref w "x") 10)) (hash-table-set! w "y" (+ (hash-table-ref w "y") 10)))) (hash-table-set! w "shifted" (not (hash-table-ref w "shifted"))) (ret8 w))))) (define (main) (call/cc (lambda (ret9) (let ((win (alist->hash-table (_list (cons "x" 100) (cons "y" 100) (cons "w" 640) (cons "h" 480) (cons "maximized" #f) (cons "iconified" #f) (cons "visible" #t) (cons "shifted" #f))))) (begin (showState win "Start") (set! win (maximize win)) (showState win "Maximize") (set! win (unmaximize win)) (showState win "Unmaximize") (set! win (iconify win)) (showState win "Iconify") (set! win (deiconify win)) (showState win "Deiconify") (set! win (hide win)) (showState win "Hide") (set! win (showWindow win)) (showState win "Show") (set! win (move win)) (showState win "Move")))))) (main) (let ((end11 (current-jiffy))) (let ((dur12 (quotient (* (- end11 start10) 1000000) jps13))) (begin (_display (string-append "{\n  \"duration_us\": " (number->string dur12) ",\n  \"memory_bytes\": " (number->string (_mem)) ",\n  \"name\": \"main\"\n}")) (newline))))))
