;; Generated on 2025-08-03 09:40 +0700
(import (rename (scheme base) (list _list)))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (scheme write))
(import (srfi 69))
(import (srfi 1))
(import (chibi time) (srfi 98))
(define _now_seeded #f)
(define _now_seed 0)
(define (now)
  (when (not _now_seeded)
    (let ((s (get-environment-variable "MOCHI_NOW_SEED")))
      (when (and s (string->number s))
        (set! _now_seed (string->number s))
        (set! _now_seeded #t))))
  (if _now_seeded
      (begin
        (set! _now_seed (modulo (+ (* _now_seed 1664525) 1013904223) 2147483647))
        _now_seed)
      (exact (floor (* (current-second) 1000000000))))
)
(define (_mem) (* 1024 (resource-usage-max-rss (get-resource-usage resource-usage/self))))
(import (chibi json))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_substring s start end)
  (let* ((len (string-length s))
         (s0 (max 0 (min len start)))
         (e0 (max s0 (min len end))))
    (substring s s0 e0)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (slice seq start end)
  (let* ((len (if (string? seq) (string-length seq) (length seq)))
         (s (if (< start 0) (+ len start) start))
         (e (if (< end 0) (+ len end) end)))
    (set! s (max 0 (min len s)))
    (set! e (max 0 (min len e)))
    (when (< e s) (set! e s))
    (if (string? seq)
        (_substring seq s e)
        (take (drop seq s) (- e s)))))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(define (_split s sep)
  (let* ((str (if (string? s) s (list->string s)))
         (del (cond ((char? sep) sep)
                     ((string? sep) (if (= (string-length sep) 1)
                                       (string-ref sep 0)
                                       sep))
                     (else sep))))
    (cond
     ((and (string? del) (string=? del ""))
      (map string (string->list str)))
     ((char? del)
      (string-split str del))
     (else
        (let loop ((r str) (acc '()))
          (let ((cur (string-contains r del)))
            (if cur
                (let ((idx (string-cursor->index r cur)))
                  (loop (_substring r (+ idx (string-length del)) (string-length r))
                        (cons (_substring r 0 idx) acc)))
                (reverse (cons r acc)))))))))
(let ((start13 (now))) (begin (define (copyList src) (call/cc (lambda (ret1) (let ((out (_list))) (begin (call/cc (lambda (break3) (letrec ((loop2 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((v (car xs))) (begin (set! out (append out (_list v))))) (loop2 (cdr xs))))))) (loop2 src)))) (ret1 out)))))) (define (copyMap src) (call/cc (lambda (ret4) (let ((out (alist->hash-table (_list)))) (begin (call/cc (lambda (break6) (letrec ((loop5 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((k (car xs))) (begin (hash-table-set! out k (hash-table-ref/default src k (quote ()))))) (loop5 (cdr xs))))))) (loop5 (hash-table-keys src))))) (ret4 out)))))) (define (deepcopy c) (call/cc (lambda (ret7) (ret7 (alist->hash-table (_list (cons "i" (hash-table-ref c "i")) (cons "s" (hash-table-ref c "s")) (cons "b" (copyList (hash-table-ref c "b"))) (cons "m" (copyMap (hash-table-ref c "m"))))))))) (define (cdsStr c) (call/cc (lambda (ret8) (let ((bs "[")) (begin (let ((i 0)) (begin (call/cc (lambda (break10) (letrec ((loop9 (lambda () (if (< i (cond ((string? (hash-table-ref c "b")) (string-length (hash-table-ref c "b"))) ((hash-table? (hash-table-ref c "b")) (hash-table-size (hash-table-ref c "b"))) (else (length (hash-table-ref c "b"))))) (begin (set! bs (string-append bs (to-str (list-ref (hash-table-ref c "b") i)))) (if (_lt i (- (cond ((string? (hash-table-ref c "b")) (string-length (hash-table-ref c "b"))) ((hash-table? (hash-table-ref c "b")) (hash-table-size (hash-table-ref c "b"))) (else (length (hash-table-ref c "b")))) 1)) (begin (set! bs (string-append bs " "))) (quote ())) (set! i (+ i 1)) (loop9)) (quote ()))))) (loop9)))) (set! bs (string-append bs "]")) (let ((ms "map[")) (begin (let ((first #t)) (begin (call/cc (lambda (break12) (letrec ((loop11 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((k (car xs))) (begin (if (not first) (begin (set! ms (string-append ms " "))) (quote ())) (set! ms (string-append (string-append (string-append ms (to-str k)) ":") (to-str (hash-table-ref/default (hash-table-ref c "m") k (quote ()))))) (set! first #f))) (loop11 (cdr xs))))))) (loop11 (hash-table-keys (hash-table-ref c "m")))))) (set! ms (string-append ms "]")) (ret8 (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append "{" (to-str (hash-table-ref c "i"))) " ") (hash-table-ref c "s")) " ") bs) " ") ms) "}"))))))))))))) (define c1 (alist->hash-table (_list (cons "i" 1) (cons "s" "one") (cons "b" (_list 117 110 105 116)) (cons "m" (alist->hash-table (_list (cons 1 #t))))))) (define c2 (deepcopy c1)) (_display (to-str (cdsStr c1))) (newline) (_display (to-str (cdsStr c2))) (newline) (set! c1 (alist->hash-table (_list (cons "i" 0) (cons "s" "nil") (cons "b" (_list 122 101 114 111)) (cons "m" (alist->hash-table (_list (cons 1 #f))))))) (_display (to-str (cdsStr c1))) (newline) (_display (to-str (cdsStr c2))) (newline) (let ((end14 (now))) (let ((dur15 (quotient (- end14 start13) 1000))) (begin (_display (string-append "{\n  \"duration_us\": " (number->string dur15) ",\n  \"memory_bytes\": " (number->string (_mem)) ",\n  \"name\": \"main\"\n}")) (newline))))))
