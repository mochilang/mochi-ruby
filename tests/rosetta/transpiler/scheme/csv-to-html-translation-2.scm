;; Generated on 2025-07-28 11:25 +0700
(import (only (scheme base) call/cc when list-ref list-set!))
(import (rename (scheme base) (list _list)))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (scheme write))
(import (srfi 69))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(define (_split s sep)
  (let* ((str (if (string? s) s (list->string s)))
         (del (cond ((char? sep) sep)
                     ((string? sep) (if (= (string-length sep) 1)
                                       (string-ref sep 0)
                                       sep))
                     (else sep))))
    (cond
     ((and (string? del) (string=? del ""))
      (map string (string->list str)))
     ((char? del)
      (string-split str del))
     (else
        (let loop ((r str) (acc '()))
          (let ((idx (string-contains r del)))
            (if idx
                (loop (substring r (+ idx (string-length del)))
                      (cons (substring r 0 idx) acc))
                (reverse (cons r acc)))))))))
(define c (string-append (string-append (string-append (string-append (string-append "Character,Speech\n" "The multitude,The messiah! Show us the messiah!\n")
 "Brians mother,<angry>Now you listen here! He's not the messiah; he's a very naughty boy! Now go away!</angry>\n")
 "The multitude,Who are you?\n")
 "Brians mother,I'm his mother; that's who!\n")
 "The multitude,Behold his mother! Behold his mother!")
)
(define rows (_list)
)
(call/cc (lambda (break2)
 (letrec ((loop1 (lambda (xs)
 (if (null? xs)
 (quote ()
)
 (begin (let ((line (car xs)
)
)
 (begin (set! rows (append rows (_list (_split line ",")
)
)
)
)
)
 (loop1 (cdr xs)
)
)
)
)
)
)
 (loop1 (_split c "\n")
)
)
)
)
(define headings #t)
(_display (to-str "<table>")
)
(newline)
(if headings (begin (if (> (cond ((string? rows)
 (string-length rows)
)
 ((hash-table? rows)
 (hash-table-size rows)
)
 (else (length rows)
)
)
 0)
 (begin (let ((th "")
)
 (begin (call/cc (lambda (break4)
 (letrec ((loop3 (lambda (xs)
 (if (null? xs)
 (quote ()
)
 (begin (let ((h (car xs)
)
)
 (begin (set! th (string-append (string-append (string-append th "<th>")
 h)
 "</th>")
)
)
)
 (loop3 (cdr xs)
)
)
)
)
)
)
 (loop3 (list-ref rows 0)
)
)
)
)
 (_display (to-str "   <thead>")
)
 (newline)
 (_display (to-str (string-append (string-append "      <tr>" th)
 "</tr>")
)
)
 (newline)
 (_display (to-str "   </thead>")
)
 (newline)
 (_display (to-str "   <tbody>")
)
 (newline)
 (let ((i 1)
)
 (begin (call/cc (lambda (break6)
 (letrec ((loop5 (lambda ()
 (if (< i (cond ((string? rows)
 (string-length rows)
)
 ((hash-table? rows)
 (hash-table-size rows)
)
 (else (length rows)
)
)
)
 (begin (let ((cells "")
)
 (begin (call/cc (lambda (break8)
 (letrec ((loop7 (lambda (xs)
 (if (null? xs)
 (quote ()
)
 (begin (let ((cell (car xs)
)
)
 (begin (set! cells (string-append (string-append (string-append cells "<td>")
 cell)
 "</td>")
)
)
)
 (loop7 (cdr xs)
)
)
)
)
)
)
 (loop7 (list-ref rows i)
)
)
)
)
 (_display (to-str (string-append (string-append "      <tr>" cells)
 "</tr>")
)
)
 (newline)
 (set! i (+ i 1)
)
)
)
 (loop5)
)
 (quote ()
)
)
)
)
)
 (loop5)
)
)
)
 (_display (to-str "   </tbody>")
)
 (newline)
)
)
)
)
)
 (quote ()
)
)
)
 (begin (call/cc (lambda (break10)
 (letrec ((loop9 (lambda (xs)
 (if (null? xs)
 (quote ()
)
 (begin (let ((row (car xs)
)
)
 (begin (let ((cells "")
)
 (begin (call/cc (lambda (break12)
 (letrec ((loop11 (lambda (xs)
 (if (null? xs)
 (quote ()
)
 (begin (let ((cell (car xs)
)
)
 (begin (set! cells (string-append (string-append (string-append cells "<td>")
 cell)
 "</td>")
)
)
)
 (loop11 (cdr xs)
)
)
)
)
)
)
 (loop11 row)
)
)
)
 (_display (to-str (string-append (string-append "    <tr>" cells)
 "</tr>")
)
)
 (newline)
)
)
)
)
 (loop9 (cdr xs)
)
)
)
)
)
)
 (loop9 rows)
)
)
)
)
)
(_display (to-str "</table>")
)
(newline)
