;; Generated on 2025-07-26 23:50 +0700
(import (only (scheme base) call/cc when list-ref list-set!))
(import (rename (scheme base) (list _list)))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (chibi time) (srfi 98))
(define _now_seeded #f)
(define _now_seed 0)
(define (now)
  (when (not _now_seeded)
    (let ((s (get-environment-variable "MOCHI_NOW_SEED")))
      (when (and s (string->number s))
        (set! _now_seed (string->number s))
        (set! _now_seeded #t))))
  (if _now_seeded
      (begin
        (set! _now_seed (modulo (+ (* _now_seed 1664525) 1013904223) 2147483647))
        _now_seed)
      (inexact->exact (* (current-second) 1000000000))))(import (chibi time))
(define (_mem) (* 1024 (resource-usage-max-rss (get-resource-usage resource-usage/self))))
(import (chibi json))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(let ((start6 (now)
)
)
 (begin (define (bsearch arr x)
 (call/cc (lambda (ret1)
 (let ((low 0)
)
 (begin (let ((high (- (cond ((string? arr)
 (string-length arr)
)
 ((hash-table? arr)
 (hash-table-size arr)
)
 (else (length arr)
)
)
 1)
)
)
 (begin (call/cc (lambda (break3)
 (letrec ((loop2 (lambda ()
 (if (<= low high)
 (begin (let ((mid (/ (+ low high)
 2)
)
)
 (begin (if (> (list-ref arr mid)
 x)
 (begin (set! high (- mid 1)
)
)
 (if (< (list-ref arr mid)
 x)
 (begin (set! low (+ mid 1)
)
)
 (begin (ret1 mid)
)
)
)
)
)
 (loop2)
)
 (quote ()
)
)
)
)
)
 (loop2)
)
)
)
 (ret1 (- 1)
)
)
)
)
)
)
)
)
 (define (bsearchRec arr x low high)
 (call/cc (lambda (ret4)
 (begin (if (< high low)
 (begin (ret4 (- 1)
)
)
 (quote ()
)
)
 (let ((mid (/ (+ low high)
 2)
)
)
 (begin (if (> (list-ref arr mid)
 x)
 (begin (ret4 (bsearchRec arr x low (- mid 1)
)
)
)
 (if (< (list-ref arr mid)
 x)
 (begin (ret4 (bsearchRec arr x (+ mid 1)
 high)
)
)
 (quote ()
)
)
)
 (ret4 mid)
)
)
)
)
)
)
 (define (main)
 (call/cc (lambda (ret5)
 (let ((nums (_list (- 31)
 0 1 2 2 4 65 83 99 782)
)
)
 (begin (let ((x 2)
)
 (begin (let ((idx (bsearch nums x)
)
)
 (begin (if (_ge idx 0)
 (begin (display (to-str (string-append (string-append (string-append (to-str x)
 " is at index ")
 (to-str idx)
)
 ".")
)
)
 (newline)
)
 (begin (display (to-str (string-append (to-str x)
 " is not found.")
)
)
 (newline)
)
)
 (set! x 5)
 (set! idx (bsearchRec nums x 0 (- (cond ((string? nums)
 (string-length nums)
)
 ((hash-table? nums)
 (hash-table-size nums)
)
 (else (length nums)
)
)
 1)
)
)
 (if (_ge idx 0)
 (begin (display (to-str (string-append (string-append (string-append (to-str x)
 " is at index ")
 (to-str idx)
)
 ".")
)
)
 (newline)
)
 (begin (display (to-str (string-append (to-str x)
 " is not found.")
)
)
 (newline)
)
)
)
)
)
)
)
)
)
)
)
 (main)
 (let ((end7 (now)
)
)
 (let ((dur8 (quotient (- end7 start6)
 1000)
)
)
 (begin (display (string-append "{\n  \"duration_us\": " (number->string dur8)
 ",\n  \"memory_bytes\": " (number->string (_mem)
)
 ",\n  \"name\": \"main\"\n}")
)
 (newline)
)
)
)
)
)
