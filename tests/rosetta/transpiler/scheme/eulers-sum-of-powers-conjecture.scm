;; Generated on 2025-08-04 08:17 +0700
(import (scheme base))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (srfi 69))
(import (srfi 1))
(define _list list)
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_substring s start end)
  (let* ((len (string-length s))
         (s0 (max 0 (min len start)))
         (e0 (max s0 (min len end))))
    (substring s s0 e0)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (slice seq start end)
  (let* ((len (if (string? seq) (string-length seq) (length seq)))
         (s (if (< start 0) (+ len start) start))
         (e (if (< end 0) (+ len end) end)))
    (set! s (max 0 (min len s)))
    (set! e (max 0 (min len e)))
    (when (< e s) (set! e s))
    (if (string? seq)
        (_substring seq s e)
        (take (drop seq s) (- e s)))))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(define (_split s sep)
  (let* ((str (if (string? s) s (list->string s)))
         (del (cond ((char? sep) sep)
                     ((string? sep) (if (= (string-length sep) 1)
                                       (string-ref sep 0)
                                       sep))
                     (else sep))))
    (cond
     ((and (string? del) (string=? del ""))
      (map string (string->list str)))
     ((char? del)
      (string-split str del))
     (else
        (let loop ((r str) (acc '()))
          (let ((cur (string-contains r del)))
            (if cur
                (let ((idx (string-cursor->index r cur)))
                  (loop (_substring r (+ idx (string-length del)) (string-length r))
                        (cons (_substring r 0 idx) acc)))
                (reverse (cons r acc)))))))))
(define (eulerSum) (call/cc (lambda (ret1) (let ((pow5 (_list))) (begin (let ((i 0)) (begin (call/cc (lambda (break3) (letrec ((loop2 (lambda () (if (< i 250) (begin (set! pow5 (append pow5 (_list (* (* (* (* i i) i) i) i)))) (set! i (+ i 1)) (loop2)) (quote ()))))) (loop2)))) (let ((sums (alist->hash-table (_list)))) (begin (let ((x2 2)) (begin (call/cc (lambda (break5) (letrec ((loop4 (lambda () (if (< x2 250) (begin (let ((x3 1)) (begin (call/cc (lambda (break7) (letrec ((loop6 (lambda () (if (< x3 x2) (begin (let ((s (+ (list-ref pow5 x2) (list-ref pow5 x3)))) (begin (if (not (cond ((string? sums) (if (string-contains sums s) #t #f)) ((hash-table? sums) (if (hash-table-exists? sums s) #t #f)) (else (if (member s sums) #t #f)))) (begin (hash-table-set! sums s (_list x2 x3))) (quote ())) (set! x3 (+ x3 1)))) (loop6)) (quote ()))))) (loop6)))) (set! x2 (+ x2 1)))) (loop4)) (quote ()))))) (loop4)))) (let ((x0 4)) (begin (call/cc (lambda (break9) (letrec ((loop8 (lambda () (if (< x0 250) (begin (let ((x1 3)) (begin (call/cc (lambda (break11) (letrec ((loop10 (lambda () (if (< x1 x0) (begin (let ((y (+ x0 1))) (begin (call/cc (lambda (break13) (letrec ((loop12 (lambda () (if (< y 250) (begin (let ((rem (- (- (list-ref pow5 y) (list-ref pow5 x0)) (list-ref pow5 x1)))) (begin (if (cond ((string? sums) (if (string-contains sums rem) #t #f)) ((hash-table? sums) (if (hash-table-exists? sums rem) #t #f)) (else (if (member rem sums) #t #f))) (begin (let ((pair (hash-table-ref/default sums rem (quote ())))) (begin (let ((a (list-ref pair 0))) (begin (let ((b (list-ref pair 1))) (begin (if (and (> x1 a) (> a b)) (begin (ret1 (_list x0 x1 a b y))) (quote ()))))))))) (quote ())) (set! y (+ y 1)))) (loop12)) (quote ()))))) (loop12)))) (set! x1 (+ x1 1)))) (loop10)) (quote ()))))) (loop10)))) (set! x0 (+ x0 1)))) (loop8)) (quote ()))))) (loop8)))) (ret1 (_list 0 0 0 0 0)))))))))))))))
(define (main) (call/cc (lambda (ret14) (let ((r (eulerSum))) (begin (_display (to-str (string-append (string-append (string-append (string-append (string-append (string-append (string-append (string-append (to-str (cond ((string? r) (_substring r 0 (+ 0 1))) ((hash-table? r) (hash-table-ref r 0)) (else (list-ref r 0)))) " ") (to-str (cond ((string? r) (_substring r 1 (+ 1 1))) ((hash-table? r) (hash-table-ref r 1)) (else (list-ref r 1))))) " ") (to-str (cond ((string? r) (_substring r 2 (+ 2 1))) ((hash-table? r) (hash-table-ref r 2)) (else (list-ref r 2))))) " ") (to-str (cond ((string? r) (_substring r 3 (+ 3 1))) ((hash-table? r) (hash-table-ref r 3)) (else (list-ref r 3))))) " ") (to-str (cond ((string? r) (_substring r 4 (+ 4 1))) ((hash-table? r) (hash-table-ref r 4)) (else (list-ref r 4))))))) (newline))))))
(main)
