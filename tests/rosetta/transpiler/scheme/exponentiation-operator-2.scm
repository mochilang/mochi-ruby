;; Generated on 2025-08-04 09:21 +0700
(import (scheme base))
(import (scheme time))
(import (chibi string))
(import (only (scheme char) string-upcase string-downcase))
(import (srfi 69))
(import (srfi 1))
(define _list list)
(import (chibi time))
(define (_mem) (* 1024 (resource-usage-max-rss (get-resource-usage resource-usage/self))))
(import (chibi json))
(define (to-str x)
  (cond ((pair? x)
         (string-append "[" (string-join (map to-str x) ", ") "]"))
        ((hash-table? x)
         (let* ((ks (hash-table-keys x))
                (pairs (map (lambda (k)
                              (string-append (to-str k) ":" (to-str (hash-table-ref x k))))
                            ks)))
           (string-append "{" (string-join pairs ", ") "}")))
        ((null? x) "")
        ((string? x) x)
        ((boolean? x) (if x "1" "0"))
        (else (number->string x))))
(define (upper s) (string-upcase s))
(define (lower s) (string-downcase s))
(define (fmod a b) (- a (* (floor (/ a b)) b)))
(define (_gt a b) (cond ((and (number? a) (number? b)) (> a b)) ((and (string? a) (string? b)) (string>? a b)) (else (> a b))))
(define (_lt a b) (cond ((and (number? a) (number? b)) (< a b)) ((and (string? a) (string? b)) (string<? a b)) (else (< a b))))
(define (_ge a b) (cond ((and (number? a) (number? b)) (>= a b)) ((and (string? a) (string? b)) (string>=? a b)) (else (>= a b))))
(define (_le a b) (cond ((and (number? a) (number? b)) (<= a b)) ((and (string? a) (string? b)) (string<=? a b)) (else (<= a b))))
(define (_add a b)
  (cond ((and (number? a) (number? b)) (+ a b))
        ((string? a) (string-append a (to-str b)))
        ((string? b) (string-append (to-str a) b))
        ((and (list? a) (list? b)) (append a b))
        (else (+ a b))))
(define (indexOf s sub) (let ((cur (string-contains s sub)))   (if cur (string-cursor->index s cur) -1)))
(define (_display . args) (apply display args))
(define (padStart s width pad)
  (let loop ((out s))
    (if (< (string-length out) width)
        (loop (string-append pad out))
        out)))
(define (_substring s start end)
  (let* ((len (string-length s))
         (s0 (max 0 (min len start)))
         (e0 (max s0 (min len end))))
    (substring s s0 e0)))
(define (_repeat s n)
  (let loop ((i 0) (out ""))
    (if (< i n)
        (loop (+ i 1) (string-append out s))
        out)))
(define (slice seq start end)
  (let* ((len (if (string? seq) (string-length seq) (length seq)))
         (s (if (< start 0) (+ len start) start))
         (e (if (< end 0) (+ len end) end)))
    (set! s (max 0 (min len s)))
    (set! e (max 0 (min len e)))
    (when (< e s) (set! e s))
    (if (string? seq)
        (_substring seq s e)
        (take (drop seq s) (- e s)))))
(define (_parseIntStr s base)
  (let* ((b (if (number? base) base 10))
         (n (string->number (if (list? s) (list->string s) s) b)))
    (if n (inexact->exact (truncate n)) 0)))
(define (_split s sep)
  (let* ((str (if (string? s) s (list->string s)))
         (del (cond ((char? sep) sep)
                     ((string? sep) (if (= (string-length sep) 1)
                                       (string-ref sep 0)
                                       sep))
                     (else sep))))
    (cond
     ((and (string? del) (string=? del ""))
      (map string (string->list str)))
     ((char? del)
      (string-split str del))
     (else
        (let loop ((r str) (acc '()))
          (let ((cur (string-contains r del)))
            (if cur
                (let ((idx (string-cursor->index r cur)))
                  (loop (_substring r (+ idx (string-length del)) (string-length r))
                        (cons (_substring r 0 idx) acc)))
                (reverse (cons r acc)))))))))
(define (_len x)
  (cond ((string? x) (string-length x))
        ((hash-table? x) (hash-table-size x))
        (else (length x))))
(let ((start13 (current-jiffy)) (jps16 (jiffies-per-second))) (begin (define (expI b p) (call/cc (lambda (ret1) (let ((r 1)) (begin (let ((i 0)) (begin (call/cc (lambda (break3) (letrec ((loop2 (lambda () (if (< i p) (begin (set! r (* r b)) (set! i (+ i 1)) (loop2)) (quote ()))))) (loop2)))) (ret1 r)))))))) (define (expF b p) (call/cc (lambda (ret4) (let ((r 1.0)) (begin (let ((pow b)) (begin (let ((n p)) (begin (let ((neg #f)) (begin (if (< p 0) (begin (set! n (- p)) (set! neg #t)) (quote ())) (call/cc (lambda (break6) (letrec ((loop5 (lambda () (if (> n 0) (begin (if (equal? (modulo n 2) 1) (begin (set! r (* r pow))) (quote ())) (set! pow (* pow pow)) (set! n (quotient n 2)) (loop5)) (quote ()))))) (loop5)))) (if neg (begin (set! r (/ 1.0 r))) (quote ())) (ret4 r)))))))))))) (define (printExpF b p) (call/cc (lambda (ret7) (if (and (equal? b 0.0) (< p 0)) (begin (_display (to-str (string-append (string-append (string-append (to-str b) "^") (to-str p)) ": +Inf"))) (newline)) (begin (_display (to-str (string-append (string-append (string-append (string-append (to-str b) "^") (to-str p)) ": ") (to-str (expF b p))))) (newline)))))) (define (main) (call/cc (lambda (ret8) (begin (_display (to-str "expI tests")) (newline) (call/cc (lambda (break10) (letrec ((loop9 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((pair (car xs))) (begin (if (_lt (cond ((string? pair) (_substring pair 1 (+ 1 1))) ((hash-table? pair) (hash-table-ref pair 1)) (else (list-ref pair 1))) 0) (begin (_display (to-str (string-append (string-append (string-append (to-str (cond ((string? pair) (_substring pair 0 (+ 0 1))) ((hash-table? pair) (hash-table-ref pair 0)) (else (list-ref pair 0)))) "^") (to-str (cond ((string? pair) (_substring pair 1 (+ 1 1))) ((hash-table? pair) (hash-table-ref pair 1)) (else (list-ref pair 1))))) ": negative power not allowed"))) (newline)) (begin (_display (to-str (string-append (string-append (string-append (string-append (to-str (cond ((string? pair) (_substring pair 0 (+ 0 1))) ((hash-table? pair) (hash-table-ref pair 0)) (else (list-ref pair 0)))) "^") (to-str (cond ((string? pair) (_substring pair 1 (+ 1 1))) ((hash-table? pair) (hash-table-ref pair 1)) (else (list-ref pair 1))))) ": ") (to-str (expI (cond ((string? pair) (_substring pair 0 (+ 0 1))) ((hash-table? pair) (hash-table-ref pair 0)) (else (list-ref pair 0))) (cond ((string? pair) (_substring pair 1 (+ 1 1))) ((hash-table? pair) (hash-table-ref pair 1)) (else (list-ref pair 1)))))))) (newline))))) (loop9 (cdr xs))))))) (loop9 (_list (_list 2 10) (_list 2 (- 10)) (_list (- 2) 10) (_list (- 2) 11) (_list 11 0)))))) (_display (to-str "overflow undetected")) (newline) (_display (to-str (string-append "10^10: " (to-str (expI 10 10))))) (newline) (_display (to-str "\nexpF tests:")) (newline) (call/cc (lambda (break12) (letrec ((loop11 (lambda (xs) (if (null? xs) (quote ()) (begin (let ((pair (car xs))) (begin (printExpF (cond ((string? pair) (_substring pair 0 (+ 0 1))) ((hash-table? pair) (hash-table-ref pair 0)) (else (list-ref pair 0))) (cond ((string? pair) (_substring pair 1 (+ 1 1))) ((hash-table? pair) (hash-table-ref pair 1)) (else (list-ref pair 1)))))) (loop11 (cdr xs))))))) (loop11 (_list (_list 2.0 10) (_list 2.0 (- 10)) (_list (- 2.0) 10) (_list (- 2.0) 11) (_list 11.0 0)))))) (_display (to-str "disallowed in expI, allowed here")) (newline) (printExpF 0.0 (- 1)) (_display (to-str "other interesting cases for 32 bit float type")) (newline) (printExpF 10.0 39) (printExpF 10.0 (- 39)) (printExpF (- 10.0) 39))))) (main) (let ((end14 (current-jiffy))) (let ((dur15 (quotient (* (- end14 start13) 1000000) jps16))) (begin (_display (string-append "{\n  \"duration_us\": " (number->string dur15) ",\n  \"memory_bytes\": " (number->string (_mem)) ",\n  \"name\": \"main\"\n}")) (newline))))))
