let max = 20
let words = {3: "Fizz", 5: "Buzz", 7: "Baxx"}
let keys = [3,5,7]

for i in 1..(max+1) {
  var text = ""
  for n in keys {
    if i % n == 0 { text = text + words[n] }
  }
  if text == "" { text = str(i) }
  print(text)
}
