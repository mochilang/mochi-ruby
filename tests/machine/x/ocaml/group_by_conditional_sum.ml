(* Generated by Mochi compiler v0.10.28 on 2025-07-18T08:12:04Z *)
let rec __show v =
  let open Obj in
  let rec list_aux o =
    if is_int o && (magic (obj o) : int) = 0 then "" else
     let hd = field o 0 in
     let tl = field o 1 in
     let rest = list_aux tl in
     if rest = "" then __show (obj hd) else __show (obj hd) ^ "; " ^ rest
  in
  let r = repr v in
  if is_int r then string_of_int (magic v) else
  match tag r with
    | 0 -> if size r = 0 then "[]" else "[" ^ list_aux r ^ "]"
    | 252 -> (magic v : string)
    | 253 -> string_of_float (magic v)
    | _ -> "<value>"

type ('k,'v) group = { key : 'k; items : 'v list }

type record1 = { mutable cat : string; mutable val_ : int; mutable flag : bool }
type record3 = { mutable cat : string; mutable share : float }

let items : record1 list = [{ cat = "a"; val_ = 10; flag = true };{ cat = "a"; val_ = 5; flag = false };{ cat = "b"; val_ = 20; flag = true }]
let result : record3 list = (let (__groups0 : (string * record1 list) list ref) = ref [] in
  List.iter (fun (i : record1) ->
      let (key : string) = i.cat in
      let cur = try List.assoc key !__groups0 with Not_found -> [] in
      __groups0 := (key, i :: cur) :: List.remove_assoc key !__groups0;
  ) items;
  let __res0 = ref [] in
  List.iter (fun ((gKey : string), gItems) ->
    let g = { key = gKey; items = List.rev gItems } in
    __res0 := { cat = g.key; share = ((float_of_int (List.fold_left (+) 0 (let __res1 = ref [] in
  List.iter (fun x ->
      __res1 := (if x.flag then x.val_ else 0) :: !__res1;
  ) g.items;
List.rev !__res1)
)) /. (float_of_int (List.fold_left (+) 0 (let __res2 = ref [] in
  List.iter (fun x ->
      __res2 := x.val_ :: !__res2;
  ) g.items;
List.rev !__res2)
))) } :: !__res0
  ) !__groups0;
  List.rev !__res0)


let () =
  print_endline (__show (result));
