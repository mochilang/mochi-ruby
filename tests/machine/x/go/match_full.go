//go:build ignore

// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z

package main

import (
	"fmt"
	"reflect"
	"strings"
)

type v map[string]any

// line 32
func classify(n int) string {
	return func() string {
		_t := n
		if _equal(_t, 0) {
			return "zero"
		}
		if _equal(_t, 1) {
			return "one"
		}
		return "many"
	}()
}

func main() {
	x := 2
	label := func() string {
		_t := x
		if _equal(_t, 1) {
			return "one"
		}
		if _equal(_t, 2) {
			return "two"
		}
		if _equal(_t, 3) {
			return "three"
		}
		return "unknown"
	}()
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(label))))
	day := "sun"
	mood := func() string {
		_t := day
		if _equal(_t, "mon") {
			return "tired"
		}
		if _equal(_t, "fri") {
			return "excited"
		}
		if _equal(_t, "sun") {
			return "relaxed"
		}
		return "normal"
	}()
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(mood))))
	ok := true
	status := func() string {
		_t := ok
		if _equal(_t, true) {
			return "confirmed"
		}
		if _equal(_t, false) {
			return "denied"
		}
		var zeroVal string
		return zeroVal
	}()
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(status))))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(classify(0)))))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(classify(5)))))
}

func _convertMapAny(m map[any]any) map[string]any {
	out := make(map[string]any, len(m))
	for k, v := range m {
		key := fmt.Sprint(k)
		if sub, ok := v.(map[any]any); ok {
			out[key] = _convertMapAny(sub)
		} else {
			out[key] = v
		}
	}
	return out
}

func _copyToMap(dst map[string]any, src any) {
	switch m := src.(type) {
	case map[string]any:
		for k, v := range m {
			dst[k] = v
		}
	case map[string]string:
		for k, v := range m {
			dst[k] = v
		}
	case map[any]any:
		for k, v := range _convertMapAny(m) {
			dst[k] = v
		}
	default:
		rv := reflect.ValueOf(m)
		if rv.Kind() == reflect.Struct {
			rt := rv.Type()
			for i := 0; i < rv.NumField(); i++ {
				name := rt.Field(i).Name
				if tag := rt.Field(i).Tag.Get("json"); tag != "" {
					if c := strings.Index(tag, ","); c >= 0 {
						tag = tag[:c]
					}
					if tag != "-" {
						name = tag
					}
				}
				dst[name] = rv.Field(i).Interface()
			}
		}
	}
}

func _equal(a, b any) bool {
	av := reflect.ValueOf(a)
	bv := reflect.ValueOf(b)
	if av.Kind() == reflect.Struct && bv.Kind() == reflect.Map {
		am := map[string]any{}
		_copyToMap(am, a)
		bm := map[string]any{}
		_copyToMap(bm, b)
		return _equal(am, bm)
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Struct {
		am := map[string]any{}
		_copyToMap(am, a)
		bm := map[string]any{}
		_copyToMap(bm, b)
		return _equal(am, bm)
	}
	if av.Kind() == reflect.Slice && bv.Kind() == reflect.Slice {
		if av.Len() != bv.Len() {
			return false
		}
		for i := 0; i < av.Len(); i++ {
			if !_equal(av.Index(i).Interface(), bv.Index(i).Interface()) {
				return false
			}
		}
		return true
	}
	if av.Kind() == reflect.Map && bv.Kind() == reflect.Map {
		if av.Len() != bv.Len() {
			return false
		}
		for _, k := range av.MapKeys() {
			bvVal := bv.MapIndex(k)
			if !bvVal.IsValid() {
				return false
			}
			if !_equal(av.MapIndex(k).Interface(), bvVal.Interface()) {
				return false
			}
		}
		return true
	}
	if (av.Kind() == reflect.Int || av.Kind() == reflect.Int64 || av.Kind() == reflect.Float64) &&
		(bv.Kind() == reflect.Int || bv.Kind() == reflect.Int64 || bv.Kind() == reflect.Float64) {
		return av.Convert(reflect.TypeOf(float64(0))).Float() == bv.Convert(reflect.TypeOf(float64(0))).Float()
	}
	return reflect.DeepEqual(a, b)
}
