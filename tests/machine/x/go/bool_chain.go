//go:build ignore

// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 1
func boom() bool {
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any("boom"))))
	return true
}

func main() {
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any((((1 < 2) && (2 < 3)) && (3 < 4))) {
			return 1
		}
		return 0
	}())))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any((((1 < 2) && (2 > 3)) && boom())) {
			return 1
		}
		return 0
	}())))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any(((((1 < 2) && (2 < 3)) && (3 > 4)) && boom())) {
			return 1
		}
		return 0
	}())))
}
