//go:build ignore

// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z

package main

import (
	"fmt"
	"slices"
	"strings"
)

type v map[string]any

func main() {
	xs := []int{1, 2, 3}
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any(slices.Contains(xs, 2)) {
			return 1
		}
		return 0
	}())))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any(!(slices.Contains(xs, 5))) {
			return 1
		}
		return 0
	}())))
}
