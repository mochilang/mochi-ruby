//go:build ignore

// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

func main() {
	prefix := "fore"
	s1 := "forest"
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any((string([]rune(s1)[0:4]) == prefix)) {
			return 1
		}
		return 0
	}())))
	s2 := "desert"
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any((string([]rune(s2)[0:4]) == prefix)) {
			return 1
		}
		return 0
	}())))
}
