//go:build ignore

// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

// line 1
func twoSum(nums []int, target int) []int {
	n := len(any(nums))
	for i := 0; i < n; i++ {
		for j := (i + 1); j < n; j++ {
			if (nums[i] + nums[j]) == target {
				return []int{i, j}
			}
		}
	}
	return []int{-1, -1}
}

func main() {
	result := twoSum([]int{
		2,
		7,
		11,
		15,
	}, 9)
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(result[0]))))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(result[1]))))
}
