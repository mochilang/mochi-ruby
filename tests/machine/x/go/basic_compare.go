//go:build ignore

// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z

package main

import (
	"fmt"
	"strings"
)

type v map[string]any

func main() {
	a := (10 - 3)
	b := (2 + 2)
	fmt.Println(strings.TrimSpace(fmt.Sprintln(any(a))))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any((a == 7)) {
			return 1
		}
		return 0
	}())))
	fmt.Println(strings.TrimSpace(fmt.Sprintln(func() int {
		if any((b < 5)) {
			return 1
		}
		return 0
	}())))
}
