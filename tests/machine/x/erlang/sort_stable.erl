#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T08:58:51Z
% sort_stable.erl - generated from sort_stable.mochi

main(_) ->
    Items = [#{n => 1, v => "a"}, #{n => 1, v => "b"}, #{n => 2, v => "c"}],
    Result = [V || {_, V} <- lists:keysort(1, [{mochi_get(n, I), mochi_get(v, I)} || I <- Items])],
    io:format("~p~n", [Result]).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
