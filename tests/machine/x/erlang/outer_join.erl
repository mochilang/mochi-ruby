#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T08:58:44Z
% outer_join.erl - generated from outer_join.mochi

main(_) ->
    Customers = [#{id => 1, name => "Alice"}, #{id => 2, name => "Bob"}, #{id => 3, name => "Charlie"}, #{id => 4, name => "Diana"}],
    Orders = [#{id => 100, customerId => 1, total => 250}, #{id => 101, customerId => 2, total => 125}, #{id => 102, customerId => 1, total => 300}, #{id => 103, customerId => 5, total => 80}],
    Result = [#{order => O, customer => C} || {O, C} <- mochi_outer_join(Orders, Customers, fun(O, C) -> (mochi_get(customerId, O) == mochi_get(id, C)) end)],
    io:format("~p~n", ["--- Outer Join using syntax ---"]),
    lists:foreach(fun(Row) -> (case mochi_get(order, Row) of undefined -> io:format("~p ~p ~p~n", ["Customer", mochi_get(name, mochi_get(customer, Row)), "has no orders"]); false -> io:format("~p ~p ~p~n", ["Customer", mochi_get(name, mochi_get(customer, Row)), "has no orders"]); _ -> (case mochi_get(customer, Row) of undefined -> io:format("~p ~p ~p ~p ~p ~p~n", ["Order", mochi_get(id, mochi_get(order, Row)), "by", "Unknown", "- $", mochi_get(total, mochi_get(order, Row))]); false -> io:format("~p ~p ~p ~p ~p ~p~n", ["Order", mochi_get(id, mochi_get(order, Row)), "by", "Unknown", "- $", mochi_get(total, mochi_get(order, Row))]); _ -> io:format("~p ~p ~p ~p ~p ~p~n", ["Order", mochi_get(id, mochi_get(order, Row)), "by", mochi_get(name, mochi_get(customer, Row)), "- $", mochi_get(total, mochi_get(order, Row))]) end) end) end, Result).

mochi_left_join_item(A, B, Fun) ->
    Matches = [ {A, J} || J <- B, Fun(A, J) ],
    case Matches of
        [] -> [{A, undefined}];
        _ -> Matches
    end.

mochi_left_join(L, R, Fun) ->
    lists:flatmap(fun(X) -> mochi_left_join_item(X, R, Fun) end, L).

mochi_right_join_item(B, A, Fun) ->
    Matches = [ {I, B} || I <- A, Fun(I, B) ],
    case Matches of
        [] -> [{undefined, B}];
        _ -> Matches
    end.

mochi_right_join(L, R, Fun) ->
    lists:flatmap(fun(Y) -> mochi_right_join_item(Y, L, Fun) end, R).

mochi_outer_join(L, R, Fun) ->
    Left = mochi_left_join(L, R, Fun),
    Right = [ P || P = {undefined, _} <- mochi_right_join(L, R, Fun) ],
    Left ++ Right.

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
