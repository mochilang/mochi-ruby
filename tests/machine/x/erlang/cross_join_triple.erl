#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T12:01:54Z
% cross_join_triple.erl - generated from cross_join_triple.mochi

main(_) ->
    Nums = [1, 2],
    Letters = ["A", "B"],
    Bools = [true, false],
    Combos = [#{n => N, l => L, b => B} || N <- Nums, L <- Letters, B <- Bools],
    io:format("~p~n", ["--- Cross Join of three lists ---"]),
    lists:foreach(fun(C) -> io:format("~p ~p ~p~n", [maps:get(n, C, undefined), maps:get(l, C, undefined), maps:get(b, C, undefined)]) end, Combos).
