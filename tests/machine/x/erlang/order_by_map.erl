#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T08:58:43Z
% order_by_map.erl - generated from order_by_map.mochi

main(_) ->
    Data = [#{a => 1, b => 2}, #{a => 1, b => 1}, #{a => 0, b => 5}],
    Sorted = [V || {_, V} <- lists:keysort(1, [{#{a => mochi_get(a, X), b => mochi_get(b, X)}, X} || X <- Data])],
    io:format("~p~n", [Sorted]).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
