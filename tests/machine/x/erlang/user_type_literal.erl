#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T08:59:00Z
% user_type_literal.erl - generated from user_type_literal.mochi

main(_) ->
    Book = #{"__name" => "Book", title => "Go", author => #{"__name" => "Person", name => "Bob", age => 42}},
    io:format("~p~n", [mochi_get(name, maps:get(author, Book, undefined))]).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
