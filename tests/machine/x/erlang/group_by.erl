#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T16:28:28Z
% group_by.erl - generated from group_by.mochi

main(_) ->
    People = [#{name => "Alice", age => 30, city => "Paris"}, #{name => "Bob", age => 15, city => "Hanoi"}, #{name => "Charlie", age => 65, city => "Paris"}, #{name => "Diana", age => 45, city => "Hanoi"}, #{name => "Eve", age => 70, city => "Paris"}, #{name => "Frank", age => 22, city => "Hanoi"}],
    Stats = [#{city => Key0, count => (case Val0 of #{items := It0} -> length(It0); _ -> length(Val0) end), avg_age => (lists:sum([maps:get(age, P, undefined) || P <- Val0]) / length([maps:get(age, P, undefined) || P <- Val0]))} || {Key0, Val0} <- maps:to_list(lists:foldl(fun({Key0, Val0}, Acc0) -> L = maps:get(Key0, Acc0, []), maps:put(Key0, [Val0 | L], Acc0) end, #{}, [{maps:get(city, Person, undefined), Person} || Person <- People]))],
    io:format("~p~n", ["--- People grouped by city ---"]),
    lists:foreach(fun(S) -> io:format("~p ~p ~p ~p ~p~n", [maps:get(city, S, undefined), ": count =", maps:get(count, S, undefined), ", avg_age =", maps:get(avg_age, S, undefined)]) end, Stats).
