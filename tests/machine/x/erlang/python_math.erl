#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T08:58:47Z
% python_math.erl - generated from python_math.mochi

main(_) ->
    Area = (math:pi() * math:pow(3, 2)),
    Root = math:sqrt(49),
    Sin45 = math:sin((math:pi() / 4)),
    Log_e = math:log(math:exp(1)),
    io:format("~p ~p ~p ~p~n", ["Circle area with r =", 3, "=>", Area]),
    io:format("~p ~p~n", ["Square root of 49:", Root]),
    io:format("~p ~p~n", ["sin(π/4):", Sin45]),
    io:format("~p ~p~n", ["log(e):", Log_e]).

mochi_get(K, M) ->
    case maps:find(K, M) of
        {ok, V} -> V;
        error ->
            Name = atom_to_list(K),
            case string:tokens(Name, "_") of
                [Pref|_] ->
                    P = list_to_atom(Pref),
                    case maps:find(P, M) of
                        {ok, Sub} when is_map(Sub) -> maps:get(K, Sub, undefined);
                        _ -> undefined
                    end;
                _ -> undefined
            end
        end.
