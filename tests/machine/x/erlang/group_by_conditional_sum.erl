#!/usr/bin/env escript
%% Generated by Mochi compiler v0.10.27 on 2025-07-17T16:28:35Z
% group_by_conditional_sum.erl - generated from group_by_conditional_sum.mochi

main(_) ->
    Items = [#{cat => "a", val => 10, flag => true}, #{cat => "a", val => 5, flag => false}, #{cat => "b", val => 20, flag => true}],
    Result = [V || {_, V} <- lists:keysort(1, [{Key0, #{cat => Key0, share => (lists:sum([(case maps:get(flag, X, undefined) of undefined -> 0; false -> 0; _ -> maps:get(val, X, undefined) end) || X <- Val0]) / lists:sum([maps:get(val, X, undefined) || X <- Val0]))}} || {Key0, Val0} <- maps:to_list(lists:foldl(fun({Key0, Val0}, Acc0) -> L = maps:get(Key0, Acc0, []), maps:put(Key0, [Val0 | L], Acc0) end, #{}, [{maps:get(cat, I, undefined), I} || I <- Items]))])],
    io:format("~p~n", [Result]).
