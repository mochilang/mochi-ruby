// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int id;
  char *name;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int customer_id;
  int total;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *customer_name;
  order_t order;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int _mochi_main() {
  customer_t customers[] = {(customer_t){.id = 1, .name = "Alice"},
                            (customer_t){.id = 2, .name = "Bob"},
                            (customer_t){.id = 3, .name = "Charlie"},
                            (customer_t){.id = 4, .name = "Diana"}};
  int customers_len = sizeof(customers) / sizeof(customers[0]);
  order_t orders[] = {(order_t){.id = 100, .customer_id = 1, .total = 250},
                      (order_t){.id = 101, .customer_id = 2, .total = 125},
                      (order_t){.id = 102, .customer_id = 1, .total = 300}};
  int orders_len = sizeof(orders) / sizeof(orders[0]);
  result_item_list_t tmp1 = create_result_item_list(orders_len * customers_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < orders_len; tmp3++) {
    order_t o = orders[tmp3];
    int tmp4 = 0;
    for (int tmp5 = 0; tmp5 < customers_len; tmp5++) {
      customer_t c = customers[tmp5];
      if (!(o.customer_id == c.id)) {
        continue;
      }
      tmp4 = 1;
      tmp1.data[tmp2] = (result_item_t){.customer_name = c.name, .order = o};
      tmp2++;
    }
    if (!tmp4) {
      customer_t c = (customer_t){0};
      tmp1.data[tmp2] = (result_item_t){.customer_name = c.name, .order = o};
      tmp2++;
    }
  }
  tmp1.len = tmp2;
  result_item_list_t result = tmp1;
  printf("--- Right Join using syntax ---\n");
  for (int tmp6 = 0; tmp6 < result.len; tmp6++) {
    result_item_t entry = result.data[tmp6];
    if (memcmp(&entry.order, &(order_t){0}, sizeof(order_t)) != 0) {
      printf("Customer %s has order %d - $ %d\n", entry.customer_name,
             entry.order.id, entry.order.total);
    } else {
      printf("Customer %s has no orders\n", entry.customer_name);
    }
  }
  return 0;
}
int main() { return _mochi_main(); }
