// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int inner;
} outer_item_t;
typedef struct {
  int len;
  outer_item_t *data;
} outer_item_list_t;
outer_item_list_t create_outer_item_list(int len) {
  outer_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(outer_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  outer_item_t outer;
} data_item_t;
typedef struct {
  int len;
  data_item_t *data;
} data_item_list_t;
data_item_list_t create_data_item_list(int len) {
  data_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(data_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int _mochi_main() {
  data_item_t data = (data_item_t){.outer = (outer_item_t){.inner = 1}};
  data.outer.inner = 2;
  printf("%d\n", data.outer.inner);
  return 0;
}
int main() { return _mochi_main(); }
