// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int id;
  char *name;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int customer_id;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *name;
  int count;
} stats_item_t;
typedef struct {
  int len;
  stats_item_t *data;
} stats_item_list_t;
stats_item_list_t create_stats_item_list(int len) {
  stats_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(stats_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int _mochi_main() {
  customer_t customers[] = {(customer_t){.id = 1, .name = "Alice"},
                            (customer_t){.id = 2, .name = "Bob"},
                            (customer_t){.id = 3, .name = "Charlie"}};
  int customers_len = sizeof(customers) / sizeof(customers[0]);
  order_t orders[] = {(order_t){.id = 100, .customer_id = 1},
                      (order_t){.id = 101, .customer_id = 1},
                      (order_t){.id = 102, .customer_id = 2}};
  int orders_len = sizeof(orders) / sizeof(orders[0]);
  stats_item_t stats[10];
  int stats_len = 0;
  for (int i1 = 0; i1 < customers_len; i1++) {
    customer_t c = customers[i1];
    for (int i2 = 0; i2 < orders_len; i2++) {
      order_t o = orders[i2];
      if (!(o.customer_id == c.id)) {
        continue;
      }
      int tmp3 = -1;
      for (int i4 = 0; i4 < stats_len; i4++) {
        if (strcmp(stats[i4].name, c.name) == 0) {
          tmp3 = i4;
          break;
        }
      }
      if (tmp3 == -1) {
        stats[stats_len].name = c.name;
        stats[stats_len].count = 1;
        tmp3 = stats_len;
        stats_len++;
      } else {
        stats[tmp3].count++;
      }
    }
  }
  printf("--- Group Left Join ---\n");
  for (int tmp5 = 0; tmp5 < stats_len; tmp5++) {
    stats_item_t s = stats[tmp5];
    printf("%s orders: %d\n", s.name, s.count);
  }
  return 0;
}
int main() { return _mochi_main(); }
