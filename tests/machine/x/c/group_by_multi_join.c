// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  double *data;
} list_float;
static list_float list_float_create(int len) {
  list_float l;
  l.len = len;
  l.data = calloc(len, sizeof(double));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
static double _sum_float(list_float v) {
  double sum = 0;
  for (int i = 0; i < v.len; i++)
    sum += v.data[i];
  return sum;
}
typedef struct {
  int key;
  list_int items;
} _GroupInt;
typedef struct {
  int len;
  int cap;
  _GroupInt *data;
} list_group_int;
static list_group_int _group_by_int(list_int src) {
  list_group_int res;
  res.len = 0;
  res.cap = 0;
  res.data = NULL;
  for (int i = 0; i < src.len; i++) {
    int key = src.data[i];
    int idx = -1;
    for (int j = 0; j < res.len; j++)
      if (res.data[j].key == key) {
        idx = j;
        break;
      }
    if (idx == -1) {
      if (res.len >= res.cap) {
        res.cap = res.cap ? res.cap * 2 : 4;
        res.data = (_GroupInt *)realloc(res.data, sizeof(_GroupInt) * res.cap);
      }
      res.data[res.len].key = key;
      res.data[res.len].items = list_int_create(0);
      idx = res.len++;
    }
    _GroupInt *g = &res.data[idx];
    g->items.data =
        (int *)realloc(g->items.data, sizeof(int) * (g->items.len + 1));
    g->items.data[g->items.len++] = src.data[i];
  }
  return res;
}
typedef struct {
  int id;
  char *name;
} nation_t;
typedef struct {
  int len;
  nation_t *data;
} nation_list_t;
nation_list_t create_nation_list(int len) {
  nation_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(nation_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int nation;
} supplier_t;
typedef struct {
  int len;
  supplier_t *data;
} supplier_list_t;
supplier_list_t create_supplier_list(int len) {
  supplier_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(supplier_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int part;
  int supplier;
  double cost;
  int qty;
} partsupp_t;
typedef struct {
  int len;
  partsupp_t *data;
} partsupp_list_t;
partsupp_list_t create_partsupp_list(int len) {
  partsupp_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(partsupp_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int part;
  double value;
} filtered_item_t;
typedef struct {
  int len;
  filtered_item_t *data;
} filtered_item_list_t;
filtered_item_list_t create_filtered_item_list(int len) {
  filtered_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(filtered_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int part;
  double total;
} grouped_item_t;
typedef struct {
  int len;
  grouped_item_t *data;
} grouped_item_list_t;
grouped_item_list_t create_grouped_item_list(int len) {
  grouped_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(grouped_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int _mochi_main() {
  nation_t nations[] = {(nation_t){.id = 1, .name = "A"},
                        (nation_t){.id = 2, .name = "B"}};
  int nations_len = sizeof(nations) / sizeof(nations[0]);
  supplier_t suppliers[] = {(supplier_t){.id = 1, .nation = 1},
                            (supplier_t){.id = 2, .nation = 2}};
  int suppliers_len = sizeof(suppliers) / sizeof(suppliers[0]);
  partsupp_t partsupp[] = {
      (partsupp_t){.part = 100, .supplier = 1, .cost = 10.0, .qty = 2},
      (partsupp_t){.part = 100, .supplier = 2, .cost = 20.0, .qty = 1},
      (partsupp_t){.part = 200, .supplier = 1, .cost = 5.0, .qty = 3}};
  int partsupp_len = sizeof(partsupp) / sizeof(partsupp[0]);
  filtered_item_list_t tmp1 =
      create_filtered_item_list(partsupp_len * suppliers_len * nations_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < partsupp_len; tmp3++) {
    partsupp_t ps = partsupp[tmp3];
    for (int tmp4 = 0; tmp4 < suppliers_len; tmp4++) {
      supplier_t s = suppliers[tmp4];
      if (!(s.id == ps.supplier)) {
        continue;
      }
      for (int tmp5 = 0; tmp5 < nations_len; tmp5++) {
        nation_t n = nations[tmp5];
        if (!(n.id == s.nation)) {
          continue;
        }
        if (!((strcmp(n.name, "A") == 0))) {
          continue;
        }
        tmp1.data[tmp2] =
            (filtered_item_t){.part = ps.part, .value = ps.cost * ps.qty};
        tmp2++;
      }
    }
  }
  tmp1.len = tmp2;
  filtered_item_list_t filtered = tmp1;
  filtered_item_list_t tmp6 = create_filtered_item_list(filtered.len);
  int tmp7_data[filtered.len];
  list_int tmp7 = {0, tmp7_data};
  int tmp8 = 0;
  for (int i9 = 0; i9 < filtered.len; i9++) {
    filtered_item_t x = filtered.data[i9];
    tmp6.data[tmp8] = x;
    tmp7.data[tmp8] = x.part;
    tmp8++;
  }
  tmp6.len = tmp8;
  tmp7.len = tmp8;
  list_group_int tmp10 = _group_by_int(tmp7);
  grouped_item_list_t tmp11 = create_grouped_item_list(tmp10.len);
  int tmp12 = 0;
  for (int gi = 0; gi < tmp10.len; gi++) {
    _GroupInt _gp = tmp10.data[gi];
    filtered_item_list_t tmp13 = create_filtered_item_list(_gp.items.len);
    for (int i14 = 0; i14 < _gp.items.len; i14++) {
      tmp13.data[i14] = tmp6.data[_gp.items.data[i14]];
    }
    tmp13.len = _gp.items.len;
    struct {
      int key;
      filtered_item_list_t items;
    } g = {_gp.key, tmp13};
    list_float tmp15 = list_float_create(g.items.len);
    int tmp16 = 0;
    for (int i17 = 0; i17 < g.items.len; i17++) {
      filtered_item_t r = g.items.data[i17];
      tmp15.data[tmp16] = r.value;
      tmp16++;
    }
    tmp15.len = tmp16;
    tmp11.data[tmp12] =
        (grouped_item_t){.part = g.key, .total = _sum_float(tmp15)};
    tmp12++;
  }
  tmp11.len = tmp12;
  grouped_item_list_t grouped = tmp11;
  for (int i18 = 0; i18 < grouped.len; i18++) {
    grouped_item_t it = grouped.data[i18];
    if (i18 > 0)
      printf(" ");
    printf("map[");
    printf("part:");
    printf("%d", it.part);
    printf(" ");
    printf("total:");
    printf("%.17g", it.total);
    printf("]");
  }
  printf("\n");
  return 0;
}
int main() { return _mochi_main(); }
