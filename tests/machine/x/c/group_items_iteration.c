// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = calloc(len, sizeof(int));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  int len;
  char **data;
} list_string;
static list_string list_string_create(int len) {
  list_string l;
  l.len = len;
  l.data = calloc(len, sizeof(char *));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}
typedef struct {
  char *key;
  list_int items;
} _GroupString;
typedef struct {
  int len;
  int cap;
  _GroupString *data;
} list_group_string;
static list_group_string _group_by_string(list_string src) {
  list_group_string res;
  res.len = 0;
  res.cap = 0;
  res.data = NULL;
  for (int i = 0; i < src.len; i++) {
    char *key = src.data[i];
    int idx = -1;
    for (int j = 0; j < res.len; j++)
      if (strcmp(res.data[j].key, key) == 0) {
        idx = j;
        break;
      }
    if (idx == -1) {
      if (res.len >= res.cap) {
        res.cap = res.cap ? res.cap * 2 : 4;
        res.data =
            (_GroupString *)realloc(res.data, sizeof(_GroupString) * res.cap);
      }
      res.data[res.len].key = key;
      res.data[res.len].items = list_int_create(0);
      idx = res.len++;
    }
    _GroupString *g = &res.data[idx];
    g->items.data =
        (int *)realloc(g->items.data, sizeof(int) * (g->items.len + 1));
    g->items.data[g->items.len++] = i;
  }
  return res;
}
typedef struct {
  char *tag;
  int val;
} data_t;
typedef struct {
  int len;
  data_t *data;
} data_list_t;
data_list_t create_data_list(int len) {
  data_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(data_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *key;
  data_list_t items;
} groupdata_t_t_t;
typedef struct {
  int len;
  groupdata_t_t_t *data;
} groupdata_t_t_list_t;
groupdata_t_t_list_t create_groupdata_t_t_list(int len) {
  groupdata_t_t_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(groupdata_t_t_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  char *tag;
  int total;
} tmp_item_t;
typedef struct {
  int len;
  tmp_item_t *data;
} tmp_item_list_t;
tmp_item_list_t create_tmp_item_list(int len) {
  tmp_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(tmp_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int _mochi_main() {
  data_t data[] = {(data_t){.tag = "a", .val = 1},
                   (data_t){.tag = "a", .val = 2},
                   (data_t){.tag = "b", .val = 3}};
  int data_len = sizeof(data) / sizeof(data[0]);
  data_list_t tmp1 = create_data_list(data_len);
  list_string tmp2 = list_string_create(data_len);
  int tmp3 = 0;
  for (int i4 = 0; i4 < data_len; i4++) {
    data_t d = data[i4];
    tmp1.data[tmp3] = d;
    tmp2.data[tmp3] = d.tag;
    tmp3++;
  }
  tmp1.len = tmp3;
  tmp2.len = tmp3;
  list_group_string tmp5 = _group_by_string(tmp2);
  groupdata_t_t_list_t tmp6 = create_groupdata_t_t_list(tmp5.len);
  int tmp7 = 0;
  for (int gi = 0; gi < tmp5.len; gi++) {
    _GroupString _gp = tmp5.data[gi];
    data_list_t tmp8 = create_data_list(_gp.items.len);
    for (int i9 = 0; i9 < _gp.items.len; i9++) {
      tmp8.data[i9] = tmp1.data[_gp.items.data[i9]];
    }
    tmp8.len = _gp.items.len;
    groupdata_t_t_t g = {_gp.key, tmp8};
    tmp6.data[tmp7] = g;
    tmp7++;
  }
  tmp6.len = tmp7;
  groupdata_t_t_list_t groups = tmp6;
  tmp_item_list_t tmp = {0, NULL};
  for (int tmp10 = 0; tmp10 < groups.len; tmp10++) {
    groupdata_t_t_t g = groups.data[tmp10];
    int total = 0;
    for (int tmp11 = 0; tmp11 < g.items.len; tmp11++) {
      data_t x = g.items.data[tmp11];
      total = total + x.val;
    }
    tmp_item_list_t tmp12 = create_tmp_item_list(tmp.len + 1);
    for (int i13 = 0; i13 < tmp.len; i13++) {
      tmp12.data[i13] = tmp.data[i13];
    }
    tmp12.data[tmp.len] = (tmp_item_t){.tag = g.key, .total = total};
    tmp12.len = tmp.len + 1;
    tmp = tmp12;
  }
  tmp_item_list_t tmp14 = create_tmp_item_list(tmp.len);
  char **tmp17 = (char **)malloc(sizeof(char *) * tmp.len);
  int tmp15 = 0;
  for (int tmp16 = 0; tmp16 < tmp.len; tmp16++) {
    tmp_item_t r = tmp.data[tmp16];
    tmp14.data[tmp15] = r;
    tmp17[tmp15] = r.tag;
    tmp15++;
  }
  tmp14.len = tmp15;
  for (int i20 = 0; i20 < tmp15 - 1; i20++) {
    for (int i21 = i20 + 1; i21 < tmp15; i21++) {
      if (strcmp(tmp17[i20], tmp17[i21]) > 0) {
        char *tmp18 = tmp17[i20];
        tmp17[i20] = tmp17[i21];
        tmp17[i21] = tmp18;
        tmp_item_t tmp19 = tmp14.data[i20];
        tmp14.data[i20] = tmp14.data[i21];
        tmp14.data[i21] = tmp19;
      }
    }
  }
  tmp_item_list_t result = tmp14;
  for (int i22 = 0; i22 < result.len; i22++) {
    tmp_item_t it = result.data[i22];
    if (i22 > 0)
      printf(" ");
    printf("map[");
    printf("tag:");
    printf("%s", it.tag);
    printf(" ");
    printf("total:");
    printf("%d", it.total);
    printf("]");
  }
  printf("\n");
  free(result.data);
  return 0;
}
int main() { return _mochi_main(); }
