// Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int id;
  char *name;
} customer_t;
typedef struct {
  int len;
  customer_t *data;
} customer_list_t;
customer_list_t create_customer_list(int len) {
  customer_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(customer_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int id;
  int customer_id;
} order_t;
typedef struct {
  int len;
  order_t *data;
} order_list_t;
order_list_t create_order_list(int len) {
  order_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(order_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int order_id;
  char *sku;
} item_t;
typedef struct {
  int len;
  item_t *data;
} item_list_t;
item_list_t create_item_list(int len) {
  item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

typedef struct {
  int order_id;
  char *name;
  item_t item;
} result_item_t;
typedef struct {
  int len;
  result_item_t *data;
} result_item_list_t;
result_item_list_t create_result_item_list(int len) {
  result_item_list_t l;
  l.len = len;
  l.data = calloc(len, sizeof(result_item_t));
  if (!l.data && len > 0) {
    fprintf(stderr, "alloc failed\n");
    exit(1);
  }
  return l;
}

int _mochi_main() {
  customer_t customers[] = {(customer_t){.id = 1, .name = "Alice"},
                            (customer_t){.id = 2, .name = "Bob"}};
  int customers_len = sizeof(customers) / sizeof(customers[0]);
  order_t orders[] = {(order_t){.id = 100, .customer_id = 1},
                      (order_t){.id = 101, .customer_id = 2}};
  int orders_len = sizeof(orders) / sizeof(orders[0]);
  item_t items[] = {(item_t){.order_id = 100, .sku = "a"}};
  int items_len = sizeof(items) / sizeof(items[0]);
  result_item_list_t tmp1 =
      create_result_item_list(orders_len * customers_len * items_len);
  int tmp2 = 0;
  for (int tmp3 = 0; tmp3 < orders_len; tmp3++) {
    order_t o = orders[tmp3];
    for (int tmp4 = 0; tmp4 < customers_len; tmp4++) {
      customer_t c = customers[tmp4];
      if (!(o.customer_id == c.id)) {
        continue;
      }
      int tmp6 = 0;
      for (int tmp5 = 0; tmp5 < items_len; tmp5++) {
        item_t i = items[tmp5];
        if (!(o.id == i.order_id)) {
          continue;
        }
        tmp6 = 1;
        tmp1.data[tmp2] =
            (result_item_t){.order_id = o.id, .name = c.name, .item = i};
        tmp2++;
      }
      if (!tmp6) {
        item_t i = (item_t){0};
        tmp1.data[tmp2] =
            (result_item_t){.order_id = o.id, .name = c.name, .item = i};
        tmp2++;
      }
    }
  }
  tmp1.len = tmp2;
  result_item_list_t result = tmp1;
  printf("--- Left Join Multi ---\n");
  for (int tmp7 = 0; tmp7 < result.len; tmp7++) {
    result_item_t r = result.data[tmp7];
    printf("%d ", r.order_id);
    printf("%s ", r.name);
    if (memcmp(&r.item, &(item_t){0}, sizeof(item_t)) == 0) {
      printf("<nil>");
    } else {
      printf("map[");
      printf("orderId:");
      printf("%d", r.item.order_id);
      printf(" ");
      printf("sku:");
      printf("%s", r.item.sku);
      printf("]");
    }
    printf("\n");
  }
  return 0;
}
int main() { return _mochi_main(); }
