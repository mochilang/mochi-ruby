// Generated by Mochi compiler v0.10.27 on 1970-01-01T00:00:00Z
// Source: /workspace/mochi/tests/vm/valid/cross_join_triple.mochi

let bools: boolean[];
let combos: Record<string, any>[];
let letters: string[];
let nums: number[];

function main(): void {
  nums = [1, 2];
  letters = ["A", "B"];
  bools = [true, false];
  combos = (() => {
    const _src = nums;
    const _res = [];
    for (const n of _src) {
      for (const l of letters) {
        for (const b of bools) {
          _res.push({
            "n": n,
            "l": l,
            "b": b,
          });
        }
      }
    }
    return _res;
  })();
  console.log("--- Cross Join of three lists ---");
  for (const c of combos) {
    console.log(
      [c.n, c.l, c.b].map((a) => {
        if (Array.isArray(a)) return a.join(" ");
        if (typeof a === "boolean") return a ? "True" : "False";
        return String(a);
      }).join(" ").trimEnd(),
    );
  }
}
main();
