// Generated by Mochi compiler v0.10.27 on 1970-01-01T00:00:00Z
// Source: /workspace/mochi/tests/vm/valid/python_math.mochi

const math = {
  pi: Math.PI,
  e: Math.E,
  sqrt: Math.sqrt,
  pow: Math.pow,
  sin: Math.sin,
  log: Math.log,
};

let area: number;
let log_e: number;
let r: number;
let root: number;
let sin45: number;

function main(): void {
  r = 3;
  area = math.pi * math.pow(r, 2);
  root = math.sqrt(49);
  sin45 = math.sin(math.pi / 4);
  log_e = math.log(math.e);
  console.log(
    ["Circle area with r =", r, "=>", area].map((a) => {
      if (Array.isArray(a)) return a.join(" ");
      if (typeof a === "boolean") return a ? "1" : "0";
      return String(a);
    }).join(" ").trimEnd(),
  );
  console.log(
    ["Square root of 49:", root].map((a) => {
      if (Array.isArray(a)) return a.join(" ");
      if (typeof a === "boolean") return a ? "1" : "0";
      return String(a);
    }).join(" ").trimEnd(),
  );
  console.log(
    ["sin(π/4):", sin45].map((a) => {
      if (Array.isArray(a)) return a.join(" ");
      if (typeof a === "boolean") return a ? "1" : "0";
      return String(a);
    }).join(" ").trimEnd(),
  );
  console.log(
    ["log(e):", log_e].map((a) => {
      if (Array.isArray(a)) return a.join(" ");
      if (typeof a === "boolean") return a ? "1" : "0";
      return String(a);
    }).join(" ").trimEnd(),
  );
}
main();
