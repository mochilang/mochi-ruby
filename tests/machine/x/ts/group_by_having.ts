// Generated by Mochi compiler v0.10.27 on 1970-01-01T00:00:00Z
// Source: /workspace/mochi/tests/vm/valid/group_by_having.mochi

let big: Record<string, any>[];
let people: { [key: string]: string }[];

function main(): void {
  people = [
    {
      "name": "Alice",
      "city": "Paris",
    },
    {
      "name": "Bob",
      "city": "Hanoi",
    },
    {
      "name": "Charlie",
      "city": "Paris",
    },
    {
      "name": "Diana",
      "city": "Hanoi",
    },
    {
      "name": "Eve",
      "city": "Paris",
    },
    {
      "name": "Frank",
      "city": "Hanoi",
    },
    {
      "name": "George",
      "city": "Paris",
    },
  ];
  big = (() => {
    const _src = people;
    const _map = new Map<string, any>();
    var _items = [];
    for (const p of _src) {
      const _key = p.city;
      const _ks = JSON.stringify(_key);
      let _g = _map.get(_ks);
      if (!_g) {
        _g = { key: _key, items: [] };
        _map.set(_ks, _g);
      }
      _g.items.push({ ...p, p: p });
    }
    let _groups = Array.from(_map.values());
    _groups = _groups.filter((g) => (g.items.length >= 4));
    const _res = [];
    for (const g of _groups) {
      _res.push({
        "city": g.key,
        "num": g.items.length,
      });
    }
    return _res;
  })();
  console.log(_json(big));
}
function _json(v: any): string {
  function _sort(x: any): any {
    if (Array.isArray(x)) return x.map(_sort);
    if (x && typeof x === "object") {
      const keys = Object.keys(x).sort();
      const o: any = {};
      for (const k of keys) o[k] = _sort(x[k]);
      return o;
    }
    return x;
  }
  return JSON.stringify(_sort(v));
}

main();
