// Generated by Mochi compiler v0.10.27 on 2025-07-17T11:53:04Z
import 'dart:convert';

var items = [
  {'cat': 'a', 'val': 3},
  {'cat': 'a', 'val': 1},
  {'cat': 'b', 'val': 5},
  {'cat': 'b', 'val': 2},
];

var grouped = (() {
  var _q0 = <dynamic>[];
  var _g1 = <String, List<dynamic>>{};
  for (var i in items) {
    var _k3 = i['cat'];
    var _k3_s = jsonEncode(_k3);
    _g1.putIfAbsent(_k3_s, () => <dynamic>[]).add(i);
  }
  for (var entry in _g1.entries) {
    var g = entry.value;
    var _k3 = jsonDecode(entry.key);
    _q0.add([-(_sum(g.map((x) => x['val'])) as num), {
  'cat': _k3,
  'total': _sum(g.map((x) => x['val'])),
}]);
  }
  _q0.sort((a,b) => (a[0] as Comparable).compareTo(b[0]));
  _q0 = [for (var x in _q0) x[1]];
  return _q0;
})();

void main() {
  print(grouped);
}

dynamic _min(dynamic v) {
    List<dynamic>? list;
    if (v is List) list = v;
    else if (v is Map && v['items'] is List) list = (v['items'] as List);
    else if (v is Map && v['Items'] is List) list = (v['Items'] as List);
    else { try { var it = (v as dynamic).items; if (it is List) list = it; } catch (_) {} }
    if (list == null || list.isEmpty) return 0;
    var m = list[0];
    for (var n in list) { if ((n as Comparable).compareTo(m) < 0) m = n; }
    return m;
}

num _sum(dynamic v) {
    Iterable<dynamic>? list;
    if (v is Iterable) list = v;
    else if (v is Map && v['items'] is Iterable) list = (v['items'] as Iterable);
    else if (v is Map && v['Items'] is Iterable) list = (v['Items'] as Iterable);
    else { try { var it = (v as dynamic).items; if (it is Iterable) list = it; } catch (_) {} }
    if (list == null) return 0;
    num s = 0;
    for (var n in list) s += (n as num);
    return s;
}
