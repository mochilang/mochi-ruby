// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:59:22Z
const std = @import("std");

const r = 3.0; // f64
const area = (std.math.pi * std.math.pow(f64, r, 2.0)); // f64
const root = std.math.sqrt(49.0); // f64
const sin45 = std.math.sin((std.math.pi / 4.0)); // f64
const log_e = std.math.log(f64, std.math.e, std.math.e); // f64

pub fn main() void {
    std.debug.print("Circle area with r = {d:.1} => {d:.1}\n", .{r, area});
    std.debug.print("Square root of 49: {d:.1}\n", .{root});
    std.debug.print("sin(π/4): {d:.1}\n", .{sin45});
    std.debug.print("log(e): {d:.1}\n", .{log_e});
}
