// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:59:22Z
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const nums = &[_]i32{
    1,
    2,
    3,
}; // []const i32
const letters = &[_][]const u8{
    "A",
    "B",
}; // []const []const u8
const PairsItem = struct {
    n: i32,
    l: []const u8,
};
var pairs: []const PairsItem = undefined; // []const PairsItem

pub fn main() void {
    pairs = blk0: { var _tmp1 = std.ArrayList(PairsItem).init(std.heap.page_allocator); for (nums) |n| { for (letters) |l| { if (!((@mod(n, 2) == 0))) continue; _tmp1.append(PairsItem{
    .n = n,
    .l = l,
}) catch |err| handleError(err); } } const _tmp2 = _tmp1.toOwnedSlice() catch |err| handleError(err); break :blk0 _tmp2; };
    std.debug.print("--- Even pairs ---\n", .{});
    for (pairs) |p| {
        std.debug.print("{any} {any}\n", .{p.n, p.l});
    }
}
