// Generated by Mochi compiler v0.10.27 on 2025-07-17T17:59:22Z
const std = @import("std");

fn handleError(err: anyerror) noreturn {
    std.debug.panic("{any}", .{err});
}

const CustomersItem = struct {
    id: i32,
    name: []const u8,
};
const customers = &[_]CustomersItem{
    CustomersItem{
    .id = 1,
    .name = "Alice",
},
    CustomersItem{
    .id = 2,
    .name = "Bob",
},
    CustomersItem{
    .id = 3,
    .name = "Charlie",
},
    CustomersItem{
    .id = 4,
    .name = "Diana",
},
}; // []const CustomersItem
const OrdersItem = struct {
    id: i32,
    customerId: i32,
    total: i32,
};
const orders = &[_]OrdersItem{
    OrdersItem{
    .id = 100,
    .customerId = 1,
    .total = 250,
},
    OrdersItem{
    .id = 101,
    .customerId = 2,
    .total = 125,
},
    OrdersItem{
    .id = 102,
    .customerId = 1,
    .total = 300,
},
    OrdersItem{
    .id = 103,
    .customerId = 5,
    .total = 80,
},
}; // []const OrdersItem
var result: []const ResultItem = undefined; // []const ResultItem

pub fn main() void {
    result = blk0: { var _tmp0 = std.ArrayList(struct {
    order: OrdersItem,
    customer: CustomersItem,
}).init(std.heap.page_allocator); var _tmp1 = std.AutoHashMap(usize, bool).init(std.heap.page_allocator); for (orders, 0..) |o, _i| { var c: ?CustomersItem = null; var mi: usize = 0; for (customers, 0..) |j, ji| { if (!((o.customerId == c.id))) continue; c = j; mi = ji; _tmp1.put(ji, true) catch {}; break; } _tmp0.append(ResultItem{
    .order = o,
    .customer = c,
}) catch |err| handleError(err); } for (customers, 0..) |j, ji| { if (!_tmp1.contains(ji)) { const o: ?OrdersItem = null; c = j; _tmp0.append(ResultItem{
    .order = o,
    .customer = c,
}) catch |err| handleError(err); } } const res = _tmp0.toOwnedSlice() catch |err| handleError(err); break :blk0 res; };
    std.debug.print("--- Outer Join using syntax ---\n", .{});
    for (result) |row| {
        if (row.order) {
            if (row.customer) {
                std.debug.print("Order {any} by {any} - $ {any}\n", .{row.order.id, row.customer.name, row.order.total});
            } else {
                std.debug.print("Order {any} by Unknown - $ {any}\n", .{row.order.id, row.order.total});
            }
        } else {
            std.debug.print("Customer {any} has no orders\n", .{row.customer.name});
        }
    }
}
