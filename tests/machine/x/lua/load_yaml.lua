-- Generated by Mochi compiler v0.10.28 on 2006-01-02T15:04:05Z
Person = {}
Person.__index = Person
function Person.new(o)
    o = o or {}
    setmetatable(o, Person)
    return o
end

people = {{["name"]="Alice", ["age"]=30, ["email"]="alice@example.com"}, {["email"]="bob@example.com", ["name"]="Bob", ["age"]=15}, {["email"]="charlie@example.com", ["name"]="Charlie", ["age"]=20}};
adults = (function()
    local _res = {}
    for _, p in ipairs(people) do
        if (p.age >= 18) then
            _res[#_res+1] = {["name"]=p.name, ["email"]=p.email}
        end
    end
    return _res
end)();
for _, a in ipairs(adults) do
    print(table.concat({tostring(a.name), tostring(a.email)}, ' '));
end
