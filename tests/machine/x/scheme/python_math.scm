; Generated by Mochi compiler v0.10.27 on 2025-07-17T18:04:22Z
(define (map-get m k)
    (let ((p (assoc k m)))
        (if p (cdr p) '()))
)
(define (map-set m k v)
    (let ((p (assoc k m)))
        (if p
            (begin (set-cdr! p v) m)
            (cons (cons k v) m)))
)

(define area '())
(define log_e '())
(define r '())
(define root '())
(define sin45 '())
(define math (list (cons 'pi 3.141592653589793) (cons 'e 2.718281828459045) (cons 'sqrt (lambda (x) (sqrt x))) (cons 'pow (lambda (x y) (expt x y))) (cons 'sin (lambda (x) (sin x))) (cons 'log (lambda (x) (log x)))))
(define math_pi '())
(define math_e '())
(define (math_sqrt x) (error "extern function"))
(define (math_pow x y) (error "extern function"))
(define (math_sin x) (error "extern function"))
(define (math_log x) (error "extern function"))
(set! r 3.0)
(set! area (* (map-get math 'pi) ((map-get math 'pow) r 2.0)))
(set! root ((map-get math 'sqrt) 49.0))
(set! sin45 ((map-get math 'sin) (/ (map-get math 'pi) 4.0)))
(set! log_e ((map-get math 'log) (map-get math 'e)))
(begin (display "Circle area with r =") (display " ") (display r) (display " ") (display "=>") (display " ") (display area) (newline))
(begin (display "Square root of 49:") (display " ") (display root) (newline))
(begin (display "sin(π/4):") (display " ") (display sin45) (newline))
(begin (display "log(e):") (display " ") (display log_e) (newline))
