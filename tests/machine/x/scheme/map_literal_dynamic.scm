; Generated by Mochi compiler v0.10.27 on 2025-07-17T18:04:02Z
(define (map-get m k)
    (let ((p (assoc k m)))
        (if p (cdr p) '()))
)
(define (map-set m k v)
    (let ((p (assoc k m)))
        (if p
            (begin (set-cdr! p v) m)
            (cons (cons k v) m)))
)

(define m '())
(define x '())
(define y '())
(set! x 3)
(set! y 4)
(set! m (list (cons "a" x) (cons "b" y)))
(begin (display (map-get m "a")) (display " ") (display (map-get m "b")) (newline))
