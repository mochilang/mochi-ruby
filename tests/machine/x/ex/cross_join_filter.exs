# Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:31Z
defmodule Main do
  @nums [1, 2, 3]
  @letters ["A", "B"]
  def main do
    # pairs :: list(map())
    pairs = for n <- @nums, l <- @letters, rem(n, 2) == 0, do: %{n: n, l: l}
    IO.puts("--- Even pairs ---")

    for p <- pairs do
      IO.puts(Enum.join(Enum.map([p.n, p.l], &inspect(&1)), " "))
    end
  end
end

Main.main()
