# Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:30Z
defmodule Main do
  def main do
    # customers :: list(map())
    customers = [%{id: 1, name: "Alice"}, %{id: 2, name: "Bob"}, %{id: 3, name: "Charlie"}]
    # orders :: list(map())
    orders = [
      %{id: 100, customerId: 1, total: 250},
      %{id: 101, customerId: 2, total: 125},
      %{id: 102, customerId: 1, total: 300}
    ]

    # result :: list(map())
    result =
      for o <- orders,
          c <- customers,
          do: %{
            orderId: o.id,
            orderCustomerId: o.customerId,
            pairedCustomerName: c.name,
            orderTotal: o.total
          }

    IO.puts("--- Cross Join: All order-customer pairs ---")

    for entry <- result do
      IO.puts(
        Enum.join(
          Enum.map(
            [
              "Order",
              entry.orderId,
              "(customerId:",
              entry.orderCustomerId,
              ", total: $",
              entry.orderTotal,
              ") paired with",
              entry.pairedCustomerName
            ],
            &inspect(&1)
          ),
          " "
        )
      )
    end
  end
end

Main.main()
