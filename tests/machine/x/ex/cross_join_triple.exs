# Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:32Z
defmodule Main do
  @nums [1, 2]
  @letters ["A", "B"]
  @bools [true, false]
  def main do
    # combos :: list(map())
    combos = for n <- @nums, l <- @letters, b <- @bools, do: %{n: n, l: l, b: b}
    IO.puts("--- Cross Join of three lists ---")

    for c <- combos do
      IO.puts(Enum.join(Enum.map([c.n, c.l, c.b], &inspect(&1)), " "))
    end
  end
end

Main.main()
