// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
// cross_join_triple.mochi
import java.util.*;

class Combo {
    Integer n;
    String l;
    Boolean b;
    Combo(Integer n, String l, Boolean b) {
        this.n = n;
        this.l = l;
        this.b = b;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof Combo other)) return false;
        return Objects.equals(this.n, other.n) && Objects.equals(this.l, other.l) && Objects.equals(this.b, other.b);
    }
    @Override public int hashCode() {
        return Objects.hash(n, l, b);
    }
    int size() { return 3; }
}
public class CrossJoinTriple {
    public static void main(String[] args) {
        List<Integer> nums = new ArrayList<>(Arrays.asList(1, 2));
        List<String> letters = new ArrayList<>(Arrays.asList("A", "B"));
        List<Boolean> bools = new ArrayList<>(Arrays.asList(true, false));
        List<Combo> combos = (new java.util.function.Supplier<List<Combo>>(){public List<Combo> get(){
    List<Combo> res0 = new ArrayList<>();
    for (var n : nums) {
        for (var l : letters) {
            for (var b : bools) {
                res0.add(new Combo(n, l, b));
            }
        }
    }
    return res0;
}}).get();
        System.out.println("--- Cross Join of three lists ---");
        for (Combo c : combos) {
            System.out.println(c.n + " " + c.l + " " + c.b);
        }
    }
}
