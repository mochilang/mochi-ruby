// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
// group_by.mochi
import java.util.*;

class People {
    String name;
    int age;
    String city;
    People(String name, int age, String city) {
        this.name = name;
        this.age = age;
        this.city = city;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof People other)) return false;
        return Objects.equals(this.name, other.name) && Objects.equals(this.age, other.age) && Objects.equals(this.city, other.city);
    }
    @Override public int hashCode() {
        return Objects.hash(name, age, city);
    }
    int size() { return 3; }
}
class Stat {
    String city;
    int count;
    double avg_age;
    Stat(String city, int count, double avg_age) {
        this.city = city;
        this.count = count;
        this.avg_age = avg_age;
    }
    @Override public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof Stat other)) return false;
        return Objects.equals(this.city, other.city) && Objects.equals(this.count, other.count) && Objects.equals(this.avg_age, other.avg_age);
    }
    @Override public int hashCode() {
        return Objects.hash(city, count, avg_age);
    }
    int size() { return 3; }
}
public class GroupBy {
    static class Group<K,V> implements Iterable<V> {
        K key;
        List<V> items;
        Group(K key, List<V> items) { this.key = key; this.items = items; }
        public Iterator<V> iterator() { return items.iterator(); }
        int size() { return items.size(); }
    }
    public static void main(String[] args) {
        List<People> people = new ArrayList<>(Arrays.asList(new People("Alice", 30, "Paris"), new People("Bob", 15, "Hanoi"), new People("Charlie", 65, "Paris"), new People("Diana", 45, "Hanoi"), new People("Eve", 70, "Paris"), new People("Frank", 22, "Hanoi")));
        List<Stat> stats = (new java.util.function.Supplier<List<Stat>>(){public List<Stat> get(){
    List<Stat> res0 = new ArrayList<>();
    Map<String,List<People>> groups1 = new LinkedHashMap<>();
    for (var person : people) {
        var row2 = person;
        String key3 = person.city;
        List<People> bucket4 = groups1.get(key3);
        if (bucket4 == null) { bucket4 = new ArrayList<>(); groups1.put(key3, bucket4); }
        bucket4.add(row2);
    }
    for (Map.Entry<String,List<People>> __e : groups1.entrySet()) {
        String g_key = __e.getKey();
        Group<String,People> g = new Group<>(g_key, __e.getValue());
        res0.add(new Stat(g.key, g.size(), (new java.util.function.Supplier<List<Integer>>(){public List<Integer> get(){
    List<Integer> res5 = new ArrayList<>();
    for (var p : g) {
        res5.add(p.age);
    }
    return res5;
}}).get().stream().mapToDouble(n -> ((Number)n).doubleValue()).average().orElse(0)));
    }
    return res0;
}}).get();
        System.out.println("--- People grouped by city ---");
        for (Stat s : stats) {
            System.out.println(s.city + " " + ": count =" + " " + s.count + " " + ", avg_age =" + " " + s.avg_age);
        }
    }
}
