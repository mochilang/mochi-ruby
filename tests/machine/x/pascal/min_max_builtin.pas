// Generated by Mochi compiler v0.10.27 on 2025-07-17T18:09:11Z
program MinMaxBuiltin;
{$mode objfpc}
{$modeswitch nestedprocvars}
uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser, fpjsonrtti;

type
  generic TArray<T> = array of T;
generic function _minList<T>(arr: specialize TArray<T>): T;
var i: integer; m: T;
begin
  if Length(arr) = 0 then exit(Default(T));
  m := arr[0];
  for i := 1 to High(arr) do
    if _variantLess(arr[i], m) then m := arr[i];
  Result := m;
end;

function _variantLess(a, b: Variant): Boolean;
begin
  Result := VarCompareValue(a, b) = vrLessThan;
end;

generic function _maxList<T>(arr: specialize TArray<T>): T;
var i: integer; m: T;
begin
  if Length(arr) = 0 then exit(Default(T));
  m := arr[0];
  for i := 1 to High(arr) do
    if _variantLess(m, arr[i]) then m := arr[i];
  Result := m;
end;


var
  nums: specialize TArray<integer>;

begin
  nums := specialize TArray<integer>([3, 1, 4]);
  writeln(specialize _minList<integer>(nums));
  writeln(specialize _maxList<integer>(nums));
end.
