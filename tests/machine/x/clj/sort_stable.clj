; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:25Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(defn _sort_key [k]
  (cond
    (map? k) (pr-str (into (sorted-map) k))
    (sequential? k) (vec k)
    :else k))
(declare items result)

(defn -main []
  (def items [{:n 1 :v "a"} {:n 1 :v "b"} {:n 2 :v "c"}]) ;; list of
  (def result (vec (->> (for [i items] (:v i)) (sort-by (fn [i] (_sort_key (:n i))))))) ;; list of string
  (_print result)
)

(-main)
