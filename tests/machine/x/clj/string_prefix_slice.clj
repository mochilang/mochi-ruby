; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:34Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare prefix s1 s2)

(defn -main []
  (def prefix "fore") ;; string
  (def s1 "forest") ;; string
  (_print (= (subs s1 0 (count prefix)) prefix))
  (def s2 "desert") ;; string
  (_print (= (subs s2 0 (count prefix)) prefix))
)

(-main)
