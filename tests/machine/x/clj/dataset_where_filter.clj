; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:02:01Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare people adults)

(defn -main []
  (def people [{:name "Alice" :age 30} {:name "Bob" :age 15} {:name "Charlie" :age 65} {:name "Diana" :age 45}]) ;; list of
  (def adults (vec (->> (for [person people :when (>= (:age person) 18)] {:name (:name person) :age (:age person) :is_senior (>= (:age person) 60)})))) ;; list of map of string to any
  (_print "--- Adults ---")
  (loop [_tmp0 (seq adults)]
    (when _tmp0
      (let [person (clojure.core/first _tmp0)]
        (let [r (try
          (_print (:name person) "is" (:age person) (if (:is_senior person) " (senior)" ""))
          :next
        (catch clojure.lang.ExceptionInfo e
          (cond
            (= (.getMessage e) "continue") :next
            (= (.getMessage e) "break") :break
            :else (throw e))
          )
        )]
      (cond
        (= r :break) nil
        :else (recur (next _tmp0))
      )
    )
  )
)
)
)

(-main)
