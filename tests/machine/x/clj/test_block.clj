; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:39Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(defn test_addition_works []
  (def x (+ 1 2)) ;; int
  (assert (= x 3) "expect failed")
)

(defn -main []
  (_print "ok")
  (test_addition_works)
)

(-main)
