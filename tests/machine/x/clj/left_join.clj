; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:02:36Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare customers orders result)

(defn -main []
  (def customers [{:id 1 :name "Alice"} {:id 2 :name "Bob"}]) ;; list of
  (def orders [{:id 100 :customerId 1 :total 250} {:id 101 :customerId 3 :total 80}]) ;; list of
  (def result (vec (for [o orders] (let [c (some (fn [c] (when (= (:customerId o) (:id c)) c)) customers)] {:orderId (:id o) :customer c :total (:total o)})))) ;; list of map of string to any
  (_print "--- Left Join ---")
  (loop [_tmp0 (seq result)]
    (when _tmp0
      (let [entry (clojure.core/first _tmp0)]
        (let [r (try
          (_print "Order" (:orderId entry) "customer" (:customer entry) "total" (:total entry))
          :next
        (catch clojure.lang.ExceptionInfo e
          (cond
            (= (.getMessage e) "continue") :next
            (= (.getMessage e) "break") :break
            :else (throw e))
          )
        )]
      (cond
        (= r :break) nil
        :else (recur (next _tmp0))
      )
    )
  )
)
)
)

(-main)
