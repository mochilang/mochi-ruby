; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:02:57Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare m)

(defn -main []
  (def m {"a" 1 "b" 2}) ;; map of string to int
  (_print (contains? m "a"))
  (_print (contains? m "c"))
)

(-main)
