; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:02:46Z
(ns main)

(defn _indexList [xs i]
  (let [idx (if (neg? i) (+ i (count xs)) i)]
    (if (or (< idx 0) (>= idx (count xs)))
      (throw (ex-info "index out of range" {}))
      (nth xs idx))))

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare matrix)

(defn -main []
  (def matrix [[1 2] [3 4]]) ;; list of list of int
  (def matrix (assoc-in matrix [1 0] 5)) ;; int
  (_print (_indexList (_indexList matrix 1) 0))
)

(-main)
