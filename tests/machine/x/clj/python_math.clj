; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:03:16Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare r area root sin45 log_e)

(defn -main []
  (def r 3.0) ;; float
  (def area (* Math/PI (Math/pow r 2.0))) ;; float
  (def root (Math/sqrt 49.0)) ;; float
  (def sin45 (Math/sin (/ Math/PI 4.0))) ;; float
  (def log_e (Math/log Math/E)) ;; float
  (_print "Circle area with r =" r "=>" area)
  (_print "Square root of 49:" root)
  (_print "sin(π/4):" sin45)
  (_print "log(e):" log_e)
)

(-main)
