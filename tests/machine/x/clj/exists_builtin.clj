; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:02:03Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare data flag)

(defn -main []
  (def data [1 2]) ;; list of int
  (def flag (boolean (seq (vec (->> (for [x data :when (= x 1)] x)))))) ;; bool
  (_print flag)
)

(-main)
