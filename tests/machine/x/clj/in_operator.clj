; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:02:30Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare xs)

(defn -main []
  (def xs [1 2 3]) ;; list of int
  (_print (some #(= 2 %) xs))
  (_print (not (some #(= 5 %) xs)))
)

(-main)
