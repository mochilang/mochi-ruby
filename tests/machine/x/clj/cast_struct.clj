; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:01:51Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(defn _cast_struct [ctor m]
  (let [fields (or (some->> ctor meta :arglists first (map keyword))
                   (keys m))]
    (apply ctor (map #(get m %) fields))))
(declare todo)

(defn Todo [title]
  {:__name "Todo" :title title}
)


(defn -main []
  (def todo (_cast_struct Todo {"title" "hi"})) ;; Todo
  (_print (:title todo))
)

(-main)
