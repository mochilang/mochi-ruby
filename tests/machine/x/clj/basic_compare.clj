; Generated by Mochi compiler v0.10.28 on 2025-07-18T07:01:44Z
(ns main)

(defn _print [& args]
  (letfn [(pv [v]
            (cond
              (true? v) (print 1)
              (false? v) (print 0)
              (sequential? v) (doseq [[i x] (map-indexed vector v)]
                                (when (> i 0) (print " "))
                                (pv x))
              :else (print v)))]
    (doseq [[i a] (map-indexed vector args)]
      (when (> i 0) (print " "))
      (pv a))
    (println)))
(declare a b)

(defn -main []
  (def a (- 10 3)) ;; int
  (def b (+ 2 2)) ;; int
  (_print a)
  (_print (= a 7))
  (_print (< b 5))
)

(-main)
