// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
fn main() {
    let x = 2;
    let label = match x {1 => "one", 2 => "two", 3 => "three", _ => "unknown", };
    println!("{}", vec![format!("{}", label)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    let day = "sun";
    let mood = match day {"mon" => "tired", "fri" => "excited", "sun" => "relaxed", _ => "normal", };
    println!("{}", vec![format!("{}", mood)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    let ok = true;
    let status = match ok {true => "confirmed", false => "denied", };
    println!("{}", vec![format!("{}", status)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    fn classify(n: i32) -> &'static str {
        return match n {0 => "zero", 1 => "one", _ => "many", };
    }
    println!("{}", vec![format!("{}", classify(0))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    println!("{}", vec![format!("{}", classify(5))].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
}
