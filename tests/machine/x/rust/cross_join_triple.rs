// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
#[derive(Default, Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord)]
struct Result {
    n: i32,
    l: &'static str,
    b: bool,
}

fn main() {
    let nums = vec![1, 2];
    let letters = vec!["A", "B"];
    let bools = vec![true, false];
    let combos = { let mut tmp1 = Vec::new();for &n in &nums { for &l in &letters { for &b in &bools { tmp1.push(Result { n: n, l: l, b: b }); } } } tmp1 };
    println!("--- Cross Join of three lists ---");
    for c in combos {
        println!("{}", vec![format!("{}", c.n), format!("{}", c.l), format!("{}", if c.b {1} else {0})].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    }
}
