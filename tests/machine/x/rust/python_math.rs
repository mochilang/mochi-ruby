// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
fn main() {
    mod math {
        pub const pi: f64 = std::f64::consts::PI;
        pub const e: f64 = std::f64::consts::E;
        pub fn sqrt(x: f64) -> f64 { x.sqrt() }
        pub fn pow(x: f64, y: f64) -> f64 { x.powf(y) }
        pub fn sin(x: f64) -> f64 { x.sin() }
        pub fn log(x: f64) -> f64 { x.ln() }
    }
    let r = 3.0;
    let area = math::pi * math::pow(r, 2.0);
    let root = math::sqrt(49.0);
    let sin45 = math::sin(math::pi / 4.0);
    let log_e = math::log(math::e);
    println!("{}", vec![format!("{}", "Circle area with r ="), format!("{}", r), format!("{}", "=>"), format!("{}", area)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    println!("{}", vec![format!("{}", "Square root of 49:"), format!("{}", root)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    println!("{}", vec![format!("{}", "sin(π/4):"), format!("{}", sin45)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
    println!("{}", vec![format!("{}", "log(e):"), format!("{}", log_e)].into_iter().filter(|s| !s.is_empty()).collect::<Vec<_>>().join(" ") );
}
