// Generated by Mochi compiler v0.10.28 on 1970-01-01T00:00:00Z
#[derive(Default, Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord)]
struct Item {
    cat: &'static str,
    val: i32,
    flag: bool,
}

#[derive(Default, Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord)]
struct Group {
    key: &'static str,
    items: Vec<Item>,
}

#[derive(Default, Debug, Clone, PartialEq, PartialOrd)]
struct Result {
    cat: &'static str,
    share: f64,
}

fn main() {
    let items = vec![Item { cat: "a", val: 10, flag: true }, Item { cat: "a", val: 5, flag: false }, Item { cat: "b", val: 20, flag: true }];
    let result = { let mut tmp1 = std::collections::HashMap::new();for i in &items { let key = i.cat; tmp1.entry(key).or_insert_with(Vec::new).push(i.clone()); } let mut tmp2 = Vec::<Group>::new(); for (k,v) in tmp1 { tmp2.push(Group { key: k, items: v }); } tmp2.sort_by(|a,b| a.key.partial_cmp(&b.key).unwrap()); tmp2.sort_by(|a,b| (a.key).partial_cmp(&(b.key)).unwrap()); let mut result = Vec::new(); for g in tmp2 { result.push(Result { cat: g.key, share: ({ let mut tmp3 = Vec::new();for x in &g.clone().items { tmp3.push(if x.flag != Default::default() { x.val } else { 0 }); } tmp3 }.iter().copied().sum::<i32>() as f64) / ({ let mut tmp4 = Vec::new();for x in &g.clone().items { tmp4.push(x.val); } tmp4 }.iter().copied().sum::<i32>() as f64) }); } result };
    { for (i, it) in result.iter().enumerate() { if i > 0 { print!(" "); } print!("{:?}", it); } println!(); };
}
