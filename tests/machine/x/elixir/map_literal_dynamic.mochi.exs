# Generated by Mochi Elixir compiler
defmodule Main do
  def main do
    # x :: integer()
    x = 3
    _ = x
    # y :: integer()
    y = 4
    _ = y
    # m :: map()
    m = %{a: x, b: y}
    _ = m
    IO.puts(Enum.join(Enum.map([Map.get(m, "a"), Map.get(m, "b")], &to_string(&1)), " "))
  end
end

Main.main()
