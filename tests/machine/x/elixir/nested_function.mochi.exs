# Generated by Mochi Elixir compiler
defmodule Main do
  @spec outer(integer()) :: integer()
  def outer(x) do
    try do
      inner = fn y ->
        throw({:return, x + y})
      end

      throw({:return, inner.(5)})
    catch
      {:return, v} -> v
    end
  end

  def main do
    IO.inspect(outer(3))
  end
end

Main.main()
