# Generated by Mochi Elixir compiler
defmodule Main do
  def main do
    IO.inspect(_count([1, 2, 3]))
  end

  defp _count(v) do
    cond do
      is_list(v) -> length(v)
      is_map(v) and Map.has_key?(v, :items) -> length(v[:items])
      true -> raise "count() expects list or group"
    end
  end
end

Main.main()
