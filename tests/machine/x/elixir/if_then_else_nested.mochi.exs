# Generated by Mochi Elixir compiler
defmodule Main do
  def main do
    # x :: integer()
    x = 8
    # msg :: String.t()
    msg =
      (fn ->
         if x > 10 do
           "big"
         else
           (fn ->
              if x > 5 do
                "medium"
              else
                "small"
              end
            end).()
         end
       end).()

    IO.inspect(msg)
  end
end

Main.main()
