program main;
{$mode objfpc}

uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser;

type
  generic TArray<T> = array of T;

begin
  if not ((2 + 2 = 4)) then raise Exception.Create('expect failed');
  writeln('ok');
end.
