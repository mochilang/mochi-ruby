program main;
{$mode objfpc}

uses SysUtils, fgl, fphttpclient, Classes, Variants, fpjson, jsonparser;

type
  generic TArray<T> = array of T;
function helper_double(x: integer): integer;
begin
  result := x * 2;
  exit;
end;


begin
  writeln(helper_double(5));
end.
