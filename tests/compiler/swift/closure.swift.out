import Foundation

func makeAdder(_ n: Int) -> (Int) -> Int {
  let n = n

  return { (x: Int) -> Int in
    let x = x

    return x + n
  }
}

let add10 = makeAdder(10)
func main() {
  print(add10(7))
}
main()
