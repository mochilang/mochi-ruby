#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int len;
  int *data;
} list_int;
static list_int list_int_create(int len) {
  list_int l;
  l.len = len;
  l.data = (int *)malloc(sizeof(int) * len);
  return l;
}
int main() {
  for (int _t1 = 0; "hi"[_t1] != '\0'; _t1++) {
    char ch[2];
    ch[0] = "hi"[_t1];
    ch[1] = '\0';
    printf("%s\n", ch);
  }
  return 0;
}
