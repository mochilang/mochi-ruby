// Generated by Mochi TypeScript compiler

function plusOne(digits: number[]): number[] {
  var carry: number = 1;
  var i: number = digits.length - 1;
  var result: number[] = digits;
  while (((i >= 0) && (carry > 0))) {
    var sum: number = result[i] + carry;
    result[i] = sum % 10;
    carry = Math.trunc(sum / 10);
    i = i - 1;
  }
  if ((carry > 0)) {
    return [carry].concat(result);
  }
  return result;
}

function main(): void {
  console.log(plusOne([
    1,
    2,
    3,
  ]));
}
main();
