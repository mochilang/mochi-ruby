// Generated by Mochi TypeScript compiler

type Info = {
  msg: string;
};

let info: Info;

function main(): void {
  info = _gen_struct<Info>('{"msg": "hello"}', null, null);
  console.log(info.msg);
}
function _gen_struct<T>(
  prompt: string,
  model: string | null,
  params: any | null,
): T {
  // TODO: integrate with your preferred LLM and parse JSON
  return JSON.parse(prompt) as T;
}

main();
