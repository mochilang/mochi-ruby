type Tree {}
type Leaf {}
type Node {
  left: Tree
  value: int
  right: Tree
}
let t = Node(left=Leaf(), value=42, right=Leaf())
fun main() {
  let t = Node(left=Leaf(), value=42, right=Leaf())
  print(t.value)
}
