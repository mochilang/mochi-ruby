# Generated by Mochi C# compiler
type Program {
}
fun Program.Main() {
  var nums = new long[] { 1, 2 }
  var letters = new string[] { "A", "B" }
  var bools = new bool[] { true, false }
  var combos = new Func<List<Dictionary<string, dynamic>>>(() => {
  var _res = new List<Dictionary<string, dynamic>>()
  for n in nums {
  for l in letters {
  for b in bools {
  _res.Add(new Dictionary<string, dynamic> { { "n", n }, { "l", l }, { "b", b } })
  }
  }
  }
  return _res
  })()
  print("--- Cross Join of three lists ---")
  for c in combos {
  print(string.Join(" ", new [] { Convert.ToString(c.n), Convert.ToString(c.l), Convert.ToString(c.b) }))
  }
}
