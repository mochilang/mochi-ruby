# Generated by Mochi C# compiler
type Product {
  name: string
  price: int
}
type Program {
}
fun Program.Main() {
  var products = new Product[] { new Product { name = "Laptop", price = 1500 }, new Product { name = "Smartphone", price = 900 }, new Product { name = "Tablet", price = 600 }, new Product { name = "Monitor", price = 300 }, new Product { name = "Keyboard", price = 100 }, new Product { name = "Mouse", price = 50 }, new Product { name = "Headphones", price = 200 } }
  var expensive = new List<dynamic>(products.OrderBy(p => (-p.price)).Skip(1).Take(3).Select(p => p))
  print("--- Top products (excluding most expensive) ---")
  for item in expensive {
  print(string.Join(" ", new [] { Convert.ToString(item.name), Convert.ToString("costs $"), Convert.ToString(item.price) }))
  }
}
