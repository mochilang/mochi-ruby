# Generated by Mochi C# compiler
type Program {
}
fun Program.Main() {
  var sum = mathutils.add(3, 5)
  var sq = mathutils.square(4)
  print(string.Join(" ", new [] { Convert.ToString("3 + 5 ="), Convert.ToString(sum) }))
  print(string.Join(" ", new [] { Convert.ToString("4 squared ="), Convert.ToString(sq) }))
}
