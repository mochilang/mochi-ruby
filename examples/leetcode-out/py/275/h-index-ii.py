# Generated by Mochi Python compiler
from __future__ import annotations

import typing

def hIndex(citations: list[int]) -> int:
	n = len(citations)
	left = 0
	right = (n - 1)
	result = 0
	while (left <= right):
		mid = (left + (((right - left)) // 2))
		h = (n - mid)
		if (citations[mid] >= h):
			result = h
			right = (mid - 1)
		else:
			left = (mid + 1)
	return result

def example_1():
	assert (hIndex([0, 1, 3, 5, 6]) == 3)

def all_zeros():
	assert (hIndex([0, 0, 0]) == 0)

def mixed():
	assert (hIndex([0, 0, 4, 4]) == 2)

def main():
	example_1()
	all_zeros()
	mixed()

if __name__ == "__main__":
	main()
