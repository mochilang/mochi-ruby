# Generated by Mochi Python compiler
from __future__ import annotations

import dataclasses
import typing

def max(a: int, b: int) -> int:
	if (a > b):
		return a
	else:
		return b

def maxDepth(root: Tree) -> int:
	return (lambda _t0=root: 0 if isinstance(_t0, Leaf) else (lambda l, r: (max(maxDepth(l), maxDepth(r)) + 1))(_t0.left, _t0.right) if isinstance(_t0, Node) else None)()

class Tree:
	pass
@dataclasses.dataclass
class Leaf(Tree):
	pass
@dataclasses.dataclass
class Node(Tree):
	left: Tree
	value: int
	right: Tree

def example_1():
	tree = Node(left=Node(left=Leaf(), value=9, right=Leaf()), value=3, right=Node(left=Node(left=Leaf(), value=15, right=Leaf()), value=20, right=Node(left=Leaf(), value=7, right=Leaf())))
	assert (maxDepth(tree) == 3)

def example_2():
	tree = Node(left=Leaf(), value=1, right=Node(left=Leaf(), value=2, right=Leaf()))
	assert (maxDepth(tree) == 2)

def single_node():
	assert (maxDepth(Node(left=Leaf(), value=0, right=Leaf())) == 1)

def empty():
	assert (maxDepth(Leaf()) == 0)

def main():
	example_1()
	example_2()
	single_node()
	empty()

if __name__ == "__main__":
	main()
