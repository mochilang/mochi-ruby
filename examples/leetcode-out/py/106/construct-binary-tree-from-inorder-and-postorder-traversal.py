# Generated by Mochi Python compiler
from __future__ import annotations

import dataclasses
import typing

def buildTree(inorder: list[int], postorder: list[int]) -> Tree:
	if (len(inorder) == 0):
		return Leaf()
	rootVal = postorder[(len(postorder) - 1)]
	idx = 0
	while ((idx < len(inorder)) and (inorder[idx] != rootVal)):
		idx = (idx + 1)
	leftIn = inorder[0:idx]
	rightIn = inorder[(idx + 1):len(inorder)]
	leftPost = postorder[0:idx]
	rightPost = postorder[idx:(len(postorder) - 1)]
	return Node(left=buildTree(leftIn, leftPost), value=rootVal, right=buildTree(rightIn, rightPost))

def isSameTree(a: Tree, b: Tree) -> bool:
	return (lambda _t0=a: (lambda _t1=b: True if isinstance(_t1, Leaf) else False)() if isinstance(_t0, Leaf) else (lambda al, av, ar: (lambda _t2=b: (lambda bl, bv, br: (((av == bv) and isSameTree(al, bl)) and isSameTree(ar, br)))(_t2.left, _t2.value, _t2.right) if isinstance(_t2, Node) else False)())(_t0.left, _t0.value, _t0.right) if isinstance(_t0, Node) else None)()

class Tree:
	pass
@dataclasses.dataclass
class Leaf(Tree):
	pass
@dataclasses.dataclass
class Node(Tree):
	left: Tree
	value: int
	right: Tree

def example_1():
	inorder = [9, 3, 15, 20, 7]
	postorder = [9, 15, 7, 20, 3]
	expected = Node(left=Node(left=Leaf(), value=9, right=Leaf()), value=3, right=Node(left=Node(left=Leaf(), value=15, right=Leaf()), value=20, right=Node(left=Leaf(), value=7, right=Leaf())))
	assert (isSameTree(buildTree(inorder, postorder), expected) == True)

def example_2():
	inorder = [(-1)]
	postorder = [(-1)]
	expected = Node(left=Leaf(), value=(-1), right=Leaf())
	assert (isSameTree(buildTree(inorder, postorder), expected) == True)

def empty():
	result = buildTree([], [])
	assert (lambda _t3=result: True if isinstance(_t3, Leaf) else False)()

def main():
	example_1()
	example_2()
	empty()

if __name__ == "__main__":
	main()
