# Generated by Mochi Python compiler
from __future__ import annotations

import typing

def plusOne(head: list[int]) -> list[int]:
	carry = 1
	i = (len(head) - 1)
	result = head
	while ((i >= 0) and (carry > 0)):
		sum = (result[i] + carry)
		result[i] = (sum % 10)
		carry = (sum // 10)
		i = (i - 1)
	if (carry > 0):
		return ([carry] + result)
	return result

def example_1():
	assert (plusOne([1, 2, 3]) == [1, 2, 4])

def example_2():
	assert (plusOne([0]) == [1])

def carry_through():
	assert (plusOne([9, 9, 9]) == [1, 0, 0, 0])

def middle_carry():
	assert (plusOne([1, 2, 9]) == [1, 3, 0])

def main():
	example_1()
	example_2()
	carry_through()
	middle_carry()

if __name__ == "__main__":
	main()
