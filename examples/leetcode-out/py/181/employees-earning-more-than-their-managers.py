# Generated by Mochi Python compiler
from __future__ import annotations

import dataclasses
import typing

def higherSalaryThanManager(employees: list[Employee]) -> list[str]:
	result = []
	for e in employees:
		if (e.managerId != 0):
			for m in employees:
				if (m.id == e.managerId):
					if (e.salary > m.salary):
						result = (result + [e.name])
					break
	return result

@dataclasses.dataclass
class Employee:
	id: int
	name: str
	salary: int
	managerId: int

employees = [Employee(id=1, name="Joe", salary=70000, managerId=3), Employee(id=2, name="Henry", salary=80000, managerId=4), Employee(id=3, name="Sam", salary=60000, managerId=0), Employee(id=4, name="Max", salary=90000, managerId=0)]

def example():
	assert (higherSalaryThanManager(employees) == ["Joe"])

def main():
	employees = [Employee(id=1, name="Joe", salary=70000, managerId=3), Employee(id=2, name="Henry", salary=80000, managerId=4), Employee(id=3, name="Sam", salary=60000, managerId=0), Employee(id=4, name="Max", salary=90000, managerId=0)]
	example()

if __name__ == "__main__":
	main()
