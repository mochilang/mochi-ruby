# Generated by Mochi Python compiler
from __future__ import annotations

import typing

def rankScores(scores: list[int]) -> list[int]:
	_sorted = [ s for s in sorted([ s for s in scores ], key=lambda s: (-s)) ]
	rankMap = {}
	rank = 1
	hasPrev = False
	prev = 0
	for s in _sorted:
		if ((not hasPrev) or (s != prev)):
			rankMap[s] = rank
			prev = s
			hasPrev = True
		rank = (rank + 1)
	result = []
	for s in scores:
		result = (result + [rankMap[s]])
	return result

def example_1():
	assert (rankScores([100, 90, 90, 80]) == [1, 2, 2, 4])

def example_2():
	assert (rankScores([10, 20, 30]) == [3, 2, 1])

def main():
	example_1()
	example_2()

if __name__ == "__main__":
	main()
