# Generated by Mochi Python compiler
from __future__ import annotations

import typing

def toList(s: str) -> list[str]:
	out = []
	for i in range(0, len(s)):
		out = (out + [s[i]])
	return out

def fromList(arr: list[str]) -> str:
	out = ""
	for ch in arr:
		out = (out + ch)
	return out

def reverseString(chars: list[str]) -> list[str]:
	arr = chars
	left = 0
	right = (len(arr) - 1)
	while (left < right):
		temp = arr[left]
		arr[left] = arr[right]
		arr[right] = temp
		left = (left + 1)
		right = (right - 1)
	return arr

def example_1():
	input = toList("hello")
	result = reverseString(input)
	assert (fromList(result) == "olleh")

def example_2():
	input = toList("Hannah")
	result = reverseString(input)
	assert (fromList(result) == "hannaH")

def empty():
	input = []
	result = reverseString(input)
	assert (len(result) == 0)

def main():
	example_1()
	example_2()
	empty()

if __name__ == "__main__":
	main()
