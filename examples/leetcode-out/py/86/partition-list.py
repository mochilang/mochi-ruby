# Generated by Mochi Python compiler
from __future__ import annotations

import typing

def partition(head: list[int], x: int) -> list[int]:
	less = []
	greater = []
	idx = 0
	while (idx < len(head)):
		val = head[idx]
		if (val < x):
			less = (less + [val])
		else:
			greater = (greater + [val])
		idx = (idx + 1)
	j = 0
	while (j < len(greater)):
		val = greater[j]
		less = (less + [val])
		j = (j + 1)
	return less

def example_1():
	assert (partition([1, 4, 3, 2, 5, 2], 3) == [1, 2, 2, 4, 3, 5])

def example_2():
	assert (partition([2, 1], 2) == [1, 2])

def all_less():
	assert (partition([1, 1, 1], 5) == [1, 1, 1])

def main():
	example_1()
	example_2()
	all_less()

if __name__ == "__main__":
	main()
