// Generated by Mochi TypeScript compiler

function threeSumClosest(nums: Array<number>, target: number): number {
  let sorted: Array<number> = (() => {
    const _src = nums;
    let _items = [];
    for (const n of _src) {
      _items.push(n);
    }
    let _pairs = _items.map((it) => {
      const n = it;
      return { item: it, key: n };
    });
    _pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    _items = _pairs.map((p) => p.item);
    const _res = [];
    for (const n of _items) {
      _res.push(n);
    }
    return _res;
  })();
  (globalThis as any).sorted = sorted;
  let n: number = sorted.length;
  (globalThis as any).n = n;
  let best: number = (sorted[0] + sorted[1]) + sorted[2];
  (globalThis as any).best = best;
  for (let i: number = 0; i < n; i++) {
    let left: number = i + 1;
    (globalThis as any).left = left;
    let right: number = n - 1;
    (globalThis as any).right = right;
    while ((left < right)) {
      let sum: number = (sorted[i] + sorted[left]) + sorted[right];
      (globalThis as any).sum = sum;
      if ((sum == target)) {
        return target;
      }
      let diff: number = 0;
      (globalThis as any).diff = diff;
      if ((sum > target)) {
        diff = sum - target;
      } else {
        diff = target - sum;
      }
      let bestDiff: number = 0;
      (globalThis as any).bestDiff = bestDiff;
      if ((best > target)) {
        bestDiff = best - target;
      } else {
        bestDiff = target - best;
      }
      if ((diff < bestDiff)) {
        best = sum;
      }
      if ((sum < target)) {
        left = left + 1;
      } else {
        right = right - 1;
      }
    }
  }
  return best;
}

function test_example_1(): void {
  if (
    !(threeSumClosest([
      -1,
      2,
      1,
      -4,
    ], 1) == 2)
  ) throw new Error("expect failed");
}

function test_example_2(): void {
  if (
    !(threeSumClosest([
      0,
      0,
      0,
    ], 1) == 0)
  ) throw new Error("expect failed");
}

function test_additional(): void {
  if (
    !(threeSumClosest([
      1,
      1,
      1,
      0,
    ], -100) == 2)
  ) throw new Error("expect failed");
}

function main(): void {
  test_example_1();
  test_example_2();
  test_additional();
}
main();
