// Generated by Mochi TypeScript compiler

function topKFrequent(nums: Array<number>, k: number): Array<number> {
  let counts: Record<number, number> = {};
  (globalThis as any).counts = counts;
  for (const n of nums) {
    let c: number = 0;
    (globalThis as any).c = c;
    if (Object.prototype.hasOwnProperty.call(counts, String(n))) {
      c = counts[n];
    }
    counts[n] = c + 1;
  }
  let pairs: Array<Array<number>> = [];
  (globalThis as any).pairs = pairs;
  for (const keyKey of Object.keys(counts)) {
    const key: number = Number(keyKey);
    pairs = pairs.concat([
      [
        key,
        counts[key],
      ],
    ]);
  }
  let sorted: Array<Array<number>> = (() => {
    const _src = pairs;
    let _items = [];
    for (const p of _src) {
      _items.push(p);
    }
    let _pairs = _items.map((it) => {
      const p = it;
      return { item: it, key: (-p[1]) };
    });
    _pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    _items = _pairs.map((p) => p.item);
    const _res = [];
    for (const p of _items) {
      _res.push(p);
    }
    return _res;
  })();
  (globalThis as any).sorted = sorted;
  let result: Array<number> = [];
  (globalThis as any).result = result;
  let i: number = 0;
  (globalThis as any).i = i;
  while (((i < sorted.length) && (i < k))) {
    result = result.concat([sorted[i][0]]);
    i = i + 1;
  }
  return result;
}

function test_example_1(): void {
  if (
    !(_equal(
      topKFrequent([
        1,
        1,
        1,
        2,
        2,
        3,
      ], 2),
      [
        1,
        2,
      ],
    ))
  ) throw new Error("expect failed");
}

function test_example_2(): void {
  if (!(_equal(topKFrequent([1], 1), [1]))) throw new Error("expect failed");
}

function main(): void {
  test_example_1();
  test_example_2();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

main();
