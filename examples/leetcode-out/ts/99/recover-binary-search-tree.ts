// Generated by Mochi TypeScript compiler

type Leaf = {
  __name: "Leaf";
};

type _Node = {
  __name: "Node";
  left: Tree;
  value: number;
  right: Tree;
};

type Tree = Leaf | _Node;

function inorder(t: Tree): Array<number> {
  return (() => {
    const _t = t;
    if (_t.__name === "Leaf") return [];
    if (_t.__name === "Node") {
      return ((l, v, r) => inorder(l).concat([v]).concat(inorder(r)))(
        _t.left,
        _t.value,
        _t.right,
      );
    }
    return undefined;
  })();
}

function recoverTree(t: Tree): Tree {
  let vals: Array<number> = inorder(t);
  (globalThis as any).vals = vals;
  let sortedVals: Array<number> = (() => {
    const _src = vals;
    let _items = [];
    for (const x of _src) {
      _items.push(x);
    }
    let _pairs = _items.map((it) => {
      const x = it;
      return { item: it, key: x };
    });
    _pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    _items = _pairs.map((p) => p.item);
    const _res = [];
    for (const x of _items) {
      _res.push(x);
    }
    return _res;
  })();
  (globalThis as any).sortedVals = sortedVals;
  function build(lo: number, hi: number): Tree {
    if ((lo >= hi)) {
      return { __name: "Leaf" };
    }
    let mid: number = Math.trunc((lo + hi) / 2);
    (globalThis as any).mid = mid;
    return {
      __name: "Node",
      left: build(lo, mid),
      value: sortedVals[mid],
      right: build(mid + 1, hi),
    };
  }
  return build(0, sortedVals.length);
}

function main(): void {
  let ex: Tree = {
    __name: "Node",
    left: {
      __name: "Node",
      left: { __name: "Leaf" },
      value: 3,
      right: { __name: "Leaf" },
    },
    value: 1,
    right: {
      __name: "Node",
      left: { __name: "Leaf" },
      value: 4,
      right: { __name: "Leaf" },
    },
  };
  (globalThis as any).ex = ex;
  let fixed: Tree = recoverTree(ex);
  (globalThis as any).fixed = fixed;
  if (
    !(_equal(inorder(fixed), [
      1,
      3,
      4,
    ]))
  ) throw new Error("expect failed");
  let ex2: Tree = {
    __name: "Node",
    left: {
      __name: "Node",
      left: { __name: "Leaf" },
      value: 2,
      right: { __name: "Leaf" },
    },
    value: 4,
    right: {
      __name: "Node",
      left: { __name: "Leaf" },
      value: 1,
      right: { __name: "Leaf" },
    },
  };
  (globalThis as any).ex2 = ex2;
  let fixed2: Tree = recoverTree(ex2);
  (globalThis as any).fixed2 = fixed2;
  if (
    !(_equal(inorder(fixed2), [
      1,
      2,
      4,
    ]))
  ) throw new Error("expect failed");
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

main();
