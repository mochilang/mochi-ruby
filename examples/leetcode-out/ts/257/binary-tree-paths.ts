// Generated by Mochi TypeScript compiler

function Leaf(): Record<string, any> {
  return { "__name": "Leaf" };
}

function _Node(
  left: Record<string, any>,
  value: number,
  right: Record<string, any>,
): Record<string, any> {
  return {
    "__name": "Node",
    "left": left,
    "value": value,
    "right": right,
  };
}

function isLeaf(t: Record<string, any>): boolean {
  return _equal(t["__name"], "Leaf");
}

function left(t: Record<string, any>): Record<string, any> {
  return t["left"];
}

function right(t: Record<string, any>): Record<string, any> {
  return t["right"];
}

function value(t: Record<string, any>): number {
  return t["value"];
}

function binaryTreePaths(root: Record<string, any>): Array<string> {
  function dfs(t: Record<string, any>, path: string): Array<string> {
    if (isLeaf(t)) {
      return [];
    }
    let current: string = path;
    (globalThis as any).current = current;
    if ((path == "")) {
      current = String(value(t));
    } else {
      current = path + "->" + String(value(t));
    }
    let l: Record<string, any> = left(t);
    (globalThis as any).l = l;
    let r: Record<string, any> = right(t);
    (globalThis as any).r = r;
    if ((isLeaf(l) && isLeaf(r))) {
      return [current];
    }
    let res: Array<string> = [];
    (globalThis as any).res = res;
    if ((!isLeaf(l))) {
      res = res.concat(dfs(l, current));
    }
    if ((!isLeaf(r))) {
      res = res.concat(dfs(r, current));
    }
    return res;
  }
  return dfs(root, "");
}

function test_example_1(): void {
  if (
    !(_equal(binaryTreePaths(example1), [
      "1->2->5",
      "1->3",
    ]))
  ) throw new Error("expect failed");
}

function test_single_node(): void {
  if (!(_equal(binaryTreePaths(_Node(Leaf(), 1, Leaf())), ["1"]))) {
    throw new Error("expect failed");
  }
}

function test_empty(): void {
  if (!(_equal(binaryTreePaths(Leaf()), []))) throw new Error("expect failed");
}

function main(): void {
  let example1: Record<string, any> = _Node(
    _Node(Leaf(), 2, _Node(Leaf(), 5, Leaf())),
    1,
    _Node(Leaf(), 3, Leaf()),
  );
  (globalThis as any).example1 = example1;
  test_example_1();
  test_single_node();
  test_empty();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

main();
