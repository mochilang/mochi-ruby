// Generated by Mochi TypeScript compiler

function deleteDuplicates(nums: Array<number>): Array<number> {
  let n: number = nums.length;
  (globalThis as any).n = n;
  if ((n == 0)) {
    return [];
  }
  let result: Array<number> = [nums[0]];
  (globalThis as any).result = result;
  let i: number = 1;
  (globalThis as any).i = i;
  while ((i < n)) {
    if ((nums[i] != result[result.length - 1])) {
      result = result.concat([nums[i]]);
    }
    i = i + 1;
  }
  return result;
}

function test_example_1(): void {
  if (
    !(_equal(
      deleteDuplicates([
        1,
        1,
        2,
      ]),
      [
        1,
        2,
      ],
    ))
  ) throw new Error("expect failed");
}

function test_example_2(): void {
  if (
    !(_equal(
      deleteDuplicates([
        1,
        1,
        2,
        3,
        3,
      ]),
      [
        1,
        2,
        3,
      ],
    ))
  ) throw new Error("expect failed");
}

function test_empty_list(): void {
  if (!(_equal(deleteDuplicates([]), []))) throw new Error("expect failed");
}

function test_single_element(): void {
  if (!(_equal(deleteDuplicates([0]), [0]))) throw new Error("expect failed");
}

function main(): void {
  test_example_1();
  test_example_2();
  test_empty_list();
  test_single_element();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

main();
