// Generated by Mochi TypeScript compiler

function Leaf(): Record<string, any> {
  return { "__name": "Leaf" };
}

function _Node(
  left: Record<string, any>,
  value: number,
  right: Record<string, any>,
): Record<string, any> {
  return {
    "__name": "Node",
    "left": left,
    "value": value,
    "right": right,
  };
}

function isLeaf(t: Record<string, any>): boolean {
  return _equal(t["__name"], "Leaf");
}

function left(t: Record<string, any>): Record<string, any> {
  return t["left"];
}

function right(t: Record<string, any>): Record<string, any> {
  return t["right"];
}

function value(t: Record<string, any>): number {
  return t["value"];
}

function max(a: number, b: number): number {
  if ((a > b)) {
    return a;
  }
  return b;
}

function positive(x: number): number {
  if ((x > 0)) {
    return x;
  }
  return 0;
}

function maxPathSum(root: Record<string, any>): number {
  let best: number = -2147483648;
  (globalThis as any).best = best;
  function dfs(t: Record<string, any>): number {
    if (isLeaf(t)) {
      return 0;
    }
    let leftVal: number = dfs(left(t));
    (globalThis as any).leftVal = leftVal;
    let rightVal: number = dfs(right(t));
    (globalThis as any).rightVal = rightVal;
    let leftPos: number = positive(leftVal);
    (globalThis as any).leftPos = leftPos;
    let rightPos: number = positive(rightVal);
    (globalThis as any).rightPos = rightPos;
    let candidate: number = (value(t) + leftPos) + rightPos;
    (globalThis as any).candidate = candidate;
    if ((candidate > best)) {
      best = candidate;
    }
    return positive(value(t) + _max(leftPos, rightPos));
  }
  dfs(root);
  return best;
}

function test_example_1(): void {
  let tree: Record<string, any> = _Node(
    _Node(Leaf(), 2, Leaf()),
    1,
    _Node(Leaf(), 3, Leaf()),
  );
  (globalThis as any).tree = tree;
  if (!(maxPathSum(tree) == 6)) throw new Error("expect failed");
}

function test_example_2(): void {
  let tree: Record<string, any> = _Node(
    _Node(Leaf(), 9, Leaf()),
    -10,
    _Node(_Node(Leaf(), 15, Leaf()), 20, _Node(Leaf(), 7, Leaf())),
  );
  (globalThis as any).tree = tree;
  if (!(maxPathSum(tree) == 42)) throw new Error("expect failed");
}

function test_single_negative(): void {
  if (!(maxPathSum(_Node(Leaf(), -3, Leaf())) == (-3))) {
    throw new Error("expect failed");
  }
}

function test_all_negative(): void {
  let tree: Record<string, any> = _Node(
    _Node(Leaf(), -5, Leaf()),
    -2,
    _Node(Leaf(), -4, Leaf()),
  );
  (globalThis as any).tree = tree;
  if (!(maxPathSum(tree) == (-2))) throw new Error("expect failed");
}

function main(): void {
  test_example_1();
  test_example_2();
  test_single_negative();
  test_all_negative();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

function _max(v: any): number {
  let list: any[] | null = null;
  if (Array.isArray(v)) list = v;
  else if (v && typeof v === "object") {
    if (Array.isArray((v as any).items)) list = (v as any).items;
    else if (Array.isArray((v as any).Items)) list = (v as any).Items;
  }
  if (!list || list.length === 0) return 0;
  let m = Number(list[0]);
  for (const n of list) {
    const num = Number(n);
    if (num > m) m = num;
  }
  return m;
}

main();
