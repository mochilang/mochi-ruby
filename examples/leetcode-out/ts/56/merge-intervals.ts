// Generated by Mochi TypeScript compiler

function merge(intervals: Array<Array<number>>): Array<Array<number>> {
  if ((intervals.length == 0)) {
    return [];
  }
  let sorted: Array<Array<number>> = (() => {
    const _src = intervals;
    let _items = [];
    for (const x of _src) {
      _items.push(x);
    }
    let _pairs = _items.map((it) => {
      const x = it;
      return { item: it, key: x[0] };
    });
    _pairs.sort((a, b) => {
      const ak = a.key;
      const bk = b.key;
      if (typeof ak === "number" && typeof bk === "number") return ak - bk;
      if (typeof ak === "string" && typeof bk === "string") {
        return ak < bk
          ? -1
          : (ak > bk ? 1 : 0);
      }
      return String(ak) < String(bk) ? -1 : (String(ak) > String(bk) ? 1 : 0);
    });
    _items = _pairs.map((p) => p.item);
    const _res = [];
    for (const x of _items) {
      _res.push(x);
    }
    return _res;
  })();
  (globalThis as any).sorted = sorted;
  let result: Array<Array<number>> = [];
  (globalThis as any).result = result;
  for (const inter of sorted) {
    if ((result.length == 0)) {
      result = result.concat([inter]);
    } else if ((result[result.length - 1][1] < inter[0])) {
      result = result.concat([inter]);
    } else if ((inter[1] > result[result.length - 1][1])) {
      result[result.length - 1][1] = inter[1];
    }
  }
  return result;
}

function test_example_1(): void {
  if (
    !(_equal(
      merge([
        [
          1,
          3,
        ],
        [
          2,
          6,
        ],
        [
          8,
          10,
        ],
        [
          15,
          18,
        ],
      ]),
      [
        [
          1,
          6,
        ],
        [
          8,
          10,
        ],
        [
          15,
          18,
        ],
      ],
    ))
  ) throw new Error("expect failed");
}

function test_example_2(): void {
  if (
    !(_equal(
      merge([
        [
          1,
          4,
        ],
        [
          4,
          5,
        ],
      ]),
      [
        [
          1,
          5,
        ],
      ],
    ))
  ) throw new Error("expect failed");
}

function test_single_interval(): void {
  if (
    !(_equal(
      merge([
        [
          1,
          4,
        ],
      ]),
      [
        [
          1,
          4,
        ],
      ],
    ))
  ) throw new Error("expect failed");
}

function test_empty_list(): void {
  if (!(_equal(merge([]), []))) throw new Error("expect failed");
}

function main(): void {
  test_example_1();
  test_example_2();
  test_single_interval();
  test_empty_list();
}
function _equal(a: any, b: any): boolean {
  if (Array.isArray(a) && Array.isArray(b)) {
    if (a.length !== b.length) return false;
    for (let i = 0; i < a.length; i++) if (!_equal(a[i], b[i])) return false;
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    const ak = Object.keys(a);
    const bk = Object.keys(b);
    if (ak.length !== bk.length) return false;
    for (const k of ak) {
      if (!bk.includes(k) || !_equal((a as any)[k], (b as any)[k])) {
        return false;
      }
    }
    return true;
  }
  return a === b;
}

main();
