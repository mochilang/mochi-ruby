// Generated by Mochi C++ compiler
#include <bits/stdc++.h>
using namespace std;

vector<int> removeNthFromEnd(vector<int> nums, int n) {
  auto idx = nums.size() - n;
  auto result = vector<int>{};
  auto i = 0;
  while (i < nums.size()) {
    if (i != idx) {
      result = ([&](vector<int> a, vector<int> b) {
        a.insert(a.end(), b.begin(), b.end());
        return a;
      })(result, vector<int>{nums[i]});
    }
    i = i + 1;
  }
  return result;
}

int main() {
  auto test_example_1 = [&]() {
    if (!(removeNthFromEnd(vector<int>{1, 2, 3, 4, 5}, 2) ==
          vector<int>{1, 2, 3, 5})) {
      std::cerr << "expect failed\n";
      exit(1);
    }
  };
  test_example_1();
  auto test_example_2 = [&]() {
    if (!(removeNthFromEnd(vector<int>{1}, 1) == vector<int>{})) {
      std::cerr << "expect failed\n";
      exit(1);
    }
  };
  test_example_2();
  auto test_example_3 = [&]() {
    if (!(removeNthFromEnd(vector<int>{1, 2}, 1) == vector<int>{1})) {
      std::cerr << "expect failed\n";
      exit(1);
    }
  };
  test_example_3();
  auto test_remove_first = [&]() {
    if (!(removeNthFromEnd(vector<int>{7, 8, 9}, 3) == vector<int>{8, 9})) {
      std::cerr << "expect failed\n";
      exit(1);
    }
  };
  test_remove_first();
  auto test_remove_last = [&]() {
    if (!(removeNthFromEnd(vector<int>{7, 8, 9}, 1) == vector<int>{7, 8})) {
      std::cerr << "expect failed\n";
      exit(1);
    }
  };
  test_remove_last();
  return 0;
}
