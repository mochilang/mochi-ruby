# Generated by Mochi Elixir compiler
defmodule Main do
  def removeNthFromEnd(nums, n) do
    try do
      idx = length(nums) - n
      result = []
      _ = result
      i = 0
      _ = i

      t1 = fn t1, i, result ->
        try do
          if i < length(nums) do
            if i != idx do
              result = result ++ [Enum.at(nums, i)]
            end

            i = i + 1
            t1.(t1, i, result)
          else
            {:ok, i, result}
          end
        catch
          :break ->
            {:ok, i, result}
        end
      end

      {_, i, result} = t1.(t1, i, result)
      _ = i
      _ = result
      throw({:return, result})
    catch
      {:return, v} -> v
    end
  end

  def main do
  end
end

Main.main()
